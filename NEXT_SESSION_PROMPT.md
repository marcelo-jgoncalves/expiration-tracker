# Expiration Tracker — Estado Atual + Próxima Ação

> Este arquivo é estado atual + próxima ação (`AGENTS.md` §2), nunca fonte normativa e nunca histórico narrativo — história detalhada vive em `docs/architecture/{session-log,decisions-log}.md` e nas pastas `reviews/`. Recompactado em 2026-08-29 (reconciliação de engenharia de contexto, ver `docs/architecture/reviews/context-engineering-reconciliation/`) para parar de acumular narrativa sessão-a-sessão já duplicada nesses dois arquivos — a versão anterior tinha 1067 linhas, a maior parte delas o histórico completo de D-058 a D-083, palavra por palavra já presente em `decisions-log.md`.

## Branch / as-of

**Não confie nesta seção sem confirmar.** `git branch --show-current` deve ser `develop`; `git log --oneline -5`, `git status` e `git pull` antes de assumir qualquer coisa abaixo como pendente ou concluído — múltiplas sessões/agentes trabalham neste repo em paralelo.

**Padrão de trabalho autônomo (reforçado explicitamente por Marcelo, 2026-09-01 — vale para toda sessão futura, não só esta):** o agente deve prosseguir de forma contínua enquanto houver qualquer trabalho de engenharia real a fazer — nunca parar para pedir "posso continuar?" nem esperar confirmação para dar o próximo passo óbvio. Só pausar/perguntar quando o próximo passo depender genuinamente de uma decisão que só Marcelo pode tomar (decisão de produto, execução destrutiva/irreversível, gasto real de infra não trivial, ou algo que o protocolo Claude↔Codex do `AGENTS.md` §4 exige elevar a ele). Nesses casos: registrar o pendente claramente aqui/no `decisions-log.md`, seguir para a PRÓXIMA frente de trabalho independente disponível (nunca ficar ocioso enquanto houver outra tarefa executável), e só então esperar. Padrão operacional já em uso nesta sessão: commit/push incremental para `develop` a cada camada concluída, PR→`main` quando a suíte estiver verde, watch de CI/CD em background sem bloquear, e uso de `AskUserQuestion` OU mensagem de texto direta apenas para decisões genuinamente dele (ex.: aplicar infra real com custo/IAM novo) — nunca para "confirmar que devo continuar o óbvio".

**Estado em 2026-09-02 (fim de sessão, continuação da sessão anterior no mesmo fio)**: terminou o spike de auditoria 4/9/10 de D-161 e avançou a decisão fundacional #1 do zero até uma primeira fatia de código real, tudo confirmado ao vivo contra `dev`/CI:
1. **D-162** (bug fix mecânico, nível 1-2, sem protocolo): `createDocument()` nunca gravava GSI2 (Documents-by-Subject) apesar de comentário afirmando o contrário — achado de D-161, confirmado por leitura direta, corrigido, G-V3 ao vivo. PR #179, CD `success`.
2. **D-163** (`DocumentFile`, decisão fundacional #1 de D-161): protocolo Claude↔Codex completo, 4 rodadas (5,2→7,1→8,3→9,4/10). Reclassificada de "provavelmente nível 3-4" (estimativa otimista do handoff anterior) para **nível 5 confirmado** — falta um subsistema inteiro (entidade+storage+invariantes transacionais+reconciliação), não um data-write num campo já modelado. Fechada `APPROVED (design)` — ver `docs/architecture/reviews/document-file-scoping/`.
3. **D-164** (implementação real, primeira fatia): entidade `DocumentFile`, `reserveFiles()` (fecha a corrida de PRINCIPAL numa `TransactWriteItems` real, não só validação de input), `ConditionCheck` transacional do PRINCIPAL em `acceptVersion()`. G-V3 confirmado ao vivo nos dois mecanismos. PR #180, CD `success`, `QUARANTINE_BUCKET_NAME` confirmado ao vivo no Lambda via `aws lambda get-function-configuration --profile claude-dev`.
4. **D-165/D-166/D-167/D-168** (implementação real, fatias 2 a 6, todas 2026-09-02): transição terminal do scan + correlação de evidência simétrica (D-165); worker de reconciliação GSI5 (D-166); presign real + rota HTTP de `reserveFiles()` (D-167); gate `fileSetSealed` em `commitUpload()`, fechando o arco inteiro (D-168). `DocumentFile` (D-163) está **100% COMPLETO**. Ver "Próxima ação" abaixo para o detalhe de cada fatia e o próximo item real (item 8 da macro-ordem de D-161, Document Types configuráveis).

**Continuação da mesma sessão (2026-09-02, depois de D-171)**: **D-172** — D-D de D-136 (`EphemeralTelemetryMutation`) implementado direto (design já `APPROVED`, sem novo protocolo). Detalhe completo em `decisions-log.md` D-172.

**Continuação (2026-09-02, sessão nova, depois de D-173)**: **D-174** — itens 1-2 (domínio + CRUD do catálogo) do design `DocumentType` (D-173) implementados diretos, sem nova rodada Claude↔Codex. `domain/document-type.ts` novo + `normalizeDisplayName()` promovido para `src/shared/text/` (item 1); `createDocumentType()`/`renameDocumentType()`/`deprecateDocumentType()`/`reactivateDocumentType()` em `DocumentArchiveService`, todos via `executeTenantBusinessMutation`, seguindo D-173 §3 exatamente — dedupe pointer, dois ramos de rename (nome normalizado igual vs. diferente), fence FROM-status em deprecate/reactivate, `DocumentTypeNameConflictError` novo, RBAC `docarchive:documenttype-*` (item 2). Nenhuma rota HTTP wireada (deliberado, item 5). G-V3 confirmado ao vivo em 3 mecanismos (corrida do pointer, fence OCC + guarda FROM-status, branching same-name/changed-name do rename). Detalhe completo em `decisions-log.md` D-174.

**Continuação (2026-09-02, sessão nova, depois de D-174)**: **D-175** — item 3 do design `DocumentType` (D-173), **parcial**: `createDocument()` migrado para `executeTenantBusinessMutation` com o `ConditionCheck(DocumentType.status=ACTIVE)` novo (`DocumentTypeNotActiveError`), G-V3 confirmado (flip real para DEPRECATED via `transactWrite`, não só "tipo inexistente"). **`submitEvidence()` (guest) deliberadamente NÃO tocado** — achado real que contradiz o texto do design: `SubmitEvidenceInput.documentType` é OPCIONAL no schema guest real e cai para `requirementId` quando ausente (rota HTTP guest já viva, não um stub); aplicar o `ConditionCheck` incondicional como o design descreve quebraria toda submissão guest sem `documentType` explícito (a maioria hoje), regressão real, não proteção nova. Detalhe completo (achado, opções propostas) em `decisions-log.md` D-175.

**Continuação (2026-09-02, sessão nova, depois de D-175)**: **D-176** — item 4 do design `DocumentType` (D-173): `Document.documentType`→`documentTypeId` renomeado ponta a ponta no caminho autenticado (`Document` entity, `CreateDocumentInput`, `documentGsi2Keys()`, schema HTTP `docarchive-create-request.v1.json`, todos os call sites/fixtures — grep confirmou nenhum call site fora dos já conhecidos). GSI2SK passa de `DOCTYPE#<documentType>#...` para `DOCTYPE#<documentTypeId>#...`, mesmo formato físico, sem estado híbrido (D-093 dispensa migração de dado `dev`). **`submitEvidence()`/o fluxo guest permanece INTOCADO no comportamento** (D-175's decisão a/b/c segue aberta para Marcelo) — a única mudança ali foi o nome do atributo físico que o `Document` struct grava (`documentTypeId:` em vez de `documentType:`, já que é o mesmo tipo `Document` compartilhado), o valor que flui para dentro (`input.documentType ?? requirementId`, free string) não mudou em nada. G-V3: teste novo prova `documentGsi2Keys()`/GSI2SK permanece byte-idêntico depois de renomear o `displayName` de um `DocumentType` real via `renameDocumentType()` — só o `documentTypeId` (nunca o nome) determina a posição no índice. Suíte completa 1743/1743, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos verdes. Detalhe completo em `decisions-log.md` D-176.

**Continuação (2026-09-02, sessão nova, depois de D-176)**: **D-177** — item 5 do design `DocumentType` (D-173): as 4 operações de CRUD do catálogo (D-174) + leitura ganham rotas HTTP reais. Handlers novos em `document-archive-handlers.ts` (create/get/list/rename/deprecate/reactivate), `listDocumentTypes()` novo em `DocumentArchiveService` (primeiro uso real de `queryIndexPage`, GSI1 `DOCTYPESTATUS`, filtro `?status=` default ACTIVE). Rotas `POST /document-archive/document-types`, `GET .../document-types`, `GET .../document-types/{documentTypeId}`, `PATCH .../document-types/{documentTypeId}`, `POST .../deprecate`, `POST .../reactivate` — wireadas em `infra/modules/api-gateway/main.tf` E `proxy-allowlist.ts` (disciplina D-117/D-120), 4 schemas novos registrados em `schema-validator.ts` (checado explicitamente contra a classe de bug 500-em-vez-de-400 que D-167 documentou). RBAC já existia desde D-174, sem mudança. G-V3: 9 testes HTTP novos (fluxo completo, 403 real para VIEWER nas 4 mutações, 200 para VIEWER em leitura, 400 para 3 corpos malformados) + 10 testes de contrato novos. **Achado pré-existente registrado, não corrigido nesta fatia**: a rota `reserve_files` (D-163/D-167) está ausente de `proxy-allowlist.ts` — confirmado durante a leitura do arquivo para este item, fora do escopo de D-177. Suíte completa 1761/1761, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21, via `AWS_PROFILE=claude-dev`) todos verdes. Detalhe completo em `decisions-log.md` D-177.

**Continuação (2026-09-02, sessão nova, depois de D-177)**: **D-178** — fix mecânico nível 1-2, fecha o achado registrado em D-177: rota `POST /document-archive/documents/{documentId}/versions/{seq}/files` (`reserveFiles()`, D-163/D-167) estava ausente de `src/modules/bff/domain/proxy-allowlist.ts` (mesma classe D-117/D-120 — Lambda/Terraform já existiam, só o BFF nunca proxyava). Entrada adicionada, teste novo em `test/unit/bff/proxy-allowlist.test.ts` prova o match (G-V3: falha sem o fix). Suíte completa 1762/1762, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos verdes. Detalhe completo em `decisions-log.md` D-178.

**Continuação (2026-09-02, sessão nova, depois de D-178)**: **D-179** — design `MaintenanceDueIndex` `APPROVED` (protocolo Claude↔Codex completo, 4 rodadas reais, Claude 9,2/Codex 9,3), resolvendo o achado #5 de D-170 (starvation estrutural em 9 workers de manutenção via `Scan`+`Limit`+páginas sem cursor persistido). Decisão: GSI8 global esparso novo (`PK=WORK#<workerType>`, `SK=<dueAtIso>#TENANT#<tenantId>#<entityId>`, `KEYS_ONLY`), isolamento IAM real via `dynamodb:LeadingKeys`, revalidação atômica via `TransactWriteItems`, `deriveMaintenanceDue()` pura por worker, backfill cobrindo vencimento futuro, poison-record/quarentena idempotente, observabilidade subindo ao handler. Achado prévio real: GSI1-GSI7 já ocupados, GSI8 é o próximo slot livre. `requirement-reindex` confirmado no escopo (tem `dueAt` real via `evidenceValidUntil`). DESIGN-ONLY — nenhum código/GSI/worker alterado. Ver `docs/architecture/reviews/maintenance-due-index-scoping/` (5 arquivos) e `decisions-log.md` D-179.

**Continuação (2026-09-02, sessão nova, depois de D-179)**: **D-180/D-181** — piloto real de D-179: `membership-purge` migrado 100% ponta a ponta para GSI8 (infra+IAM+código+backfill+testes), com um bug real de deploy (Sid não-alfanumérico) pego pelo próprio CD e corrigido em D-181, mais a prova ao vivo de isolamento IAM (`aws iam simulate-principal-policy`, positivo+negativo). Ver `decisions-log.md` D-180/D-181.

**Continuação (2026-09-02, sessão nova, depois de D-181)**: **D-182** — segunda fatia de D-179: `invitation-purge` migrado 100% ponta a ponta para GSI8, mesmo padrão do piloto, com uma nuance real de timing (o ponteiro PENDING é gravado NA CRIAÇÃO, não numa transição posterior, já que `expiresAt` é conhecido desde o início). PR #201 mergeado, CI/CD `success` confirmados via `gh run view`, prova ao vivo de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo+negativo) confirmada contra `dev`. **2 de 9 workers migrados.** Ver `decisions-log.md` D-182.

**Continuação (2026-09-02, sessão nova, depois de D-182)**: **D-183** — terceira fatia de D-179: `document-file-reconciliation` migrado 100% ponta a ponta para GSI8. **Achado real que resolveu a ambiguidade GSI5-vs-GSI8 pedida por esta fatia**: o mecanismo GSI5 que D-164/D-166 desenharam para este worker nunca teve um writer real (`reserveFiles()` nunca chamava `fileReconciliationGsi5Keys()`) — o worker era código morto funcional em produção desde que foi implementado, apesar de "IMPLEMENTADO E VERIFICADO" nos testes (que seedavam `GSI5PK` manualmente). GSI8 substitui por completo, não convive com nada — não foi necessário reabrir D-163 Decisão 2. Devido a isso, o port do worker ficou mais simples que `membership-purge`/`invitation-purge` (só `queryDue()`, sem `getFile()`/`transactWrite()` próprios — a claim já vive inteira em `applyFileScanTimeout()`), e as duas varreduras por status de D-166 colapsaram numa única `Query` (GSI8PK é uniforme, sem partição por status). Sem poison-record/DLQ (nunca houve fence de tenant `ACTIVE` para este worker). Achado fora de escopo registrado, não corrigido: `document_archive_handler` (HTTP principal) tem a mesma lacuna de IAM `TransactWriteItems` que este worker tinha antes desta fatia. Ver `decisions-log.md` D-183 para PR/CI/CD/prova de isolamento IAM ao vivo. **3 de 9 workers migrados.** Próximo worker recomendado (não normativo): qualquer um dos 6 restantes (`requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`).

**Continuação (2026-09-02, sessão nova, depois de D-183)**: **D-184** — resolve a decisão pendente de D-175 (`submitEvidence()`/guest flow, opções a/b/c). Protocolo Claude↔Codex completo, 3 rodadas reais (`codex exec`), notas finais E-014 9,3/10 e design 9,5/10 — **APROVADO**. Decisão: opção (b) modificada — `ConditionCheck(DocumentType.status=ACTIVE)` aplicado em `submitEvidence()` só quando `input.documentType !== undefined` (presença explícita, não truthy); ausente continua caindo em `requirementId` sem validação, byte-idêntico a antes. Achado que descartou (a) pura: nem `DocumentRequest` nem `Requirement` referenciam hoje um `DocumentType` esperado — não existe mecanismo de descoberta para um guest saber qual `documentTypeId` é correto, tornar o campo obrigatório agora pioraria o estado atual. Achado real de replay (Rodada 2 do Codex): `existingReplay` roda ANTES de qualquer validação — decisão explícita de manter essa ordem (idempotencyKey identifica uma operação lógica, não um payload por tentativa, propriedade pré-existente de D-143 Decision 4, não uma lacuna nova). Implementado na mesma sessão: guard + `ConditionCheck` condicional em `guest-document-access-service.ts`, 5 testes novos (`test/unit/document-archive/guest-document-access-service.test.ts`). Suíte completa 1827/1827 (scoped a 2 arquivos, sem overlap com WIP concorrente de D-183/requirement-reindex nesta mesma sessão paralela), `typecheck`/`lint` limpos. **Item 6 do arco D-173 (schema HTTP guest obrigatório + mecanismo `Requirement`→`DocumentType`) permanece próxima fatia independente, não decidida.** Ver `docs/architecture/reviews/submit-evidence-document-type-scoping/estado-final-consolidado.md` e `decisions-log.md` D-184.

**Continuação (2026-09-02, sessão nova, depois de D-183, EM PARALELO com D-184/submitEvidence de outra sessão no mesmo repo)**: **D-185** — quarta fatia de D-179: `requirement-reindex` migrado 100% ponta a ponta para GSI8, mesmo padrão de D-180/D-182/D-183. Verificação prévia obrigatória confirmou que o worker só faz a drift pura SATISFIED→NOT_SATISFIED via `evidenceValidUntil`, nunca uma "reindexação de applicability" mais ampla — a correção da Rodada 3 de D-179 está correta. `scanSatisfiedRequirements` (mecanismo Scan antigo) removido do port, sem outro call site. Sem poison-record/DLQ (mesma decisão de D-183 — nunca houve fence de tenant ACTIVE). Suíte local 1827/1827, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` 22/22 (`gsi8_worker_isolation` confirmado `pass`). Commit `720d881` empacotado no PR #205 (aberto pela sessão concorrente de D-184, mesmo `develop`) — CI real (`33665953101`) e CD real (`33666281116`) `success`, confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: GSI8 `ACTIVE`, role `exptrk-dev-requirement-reindex-handler-role` com as 3 políticas esperadas, Lambda `Active`/`Successful`, schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`)**: positivo no próprio namespace (`WORK#REQUIREMENT_REINDEX` → `allowed`), negativo nos 3 namespaces já migrados (`MEMBERSHIP_PURGE`/`INVITATION_PURGE`/`DOCUMENT_FILE_RECONCILIATION` → `implicitDeny`). Ver `decisions-log.md` D-185 para o detalhe completo. **4 de 9 workers migrados** (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`); **próxima fatia recomendada (não normativa)**: qualquer um dos 5 restantes — `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`.

**Continuação (2026-09-02, sessão nova, depois de D-185)**: **D-186** — quinta fatia de D-179: `quota-telemetry-purge` migrado 100% ponta a ponta para GSI8, mesmo padrão de D-180/D-182 (com poison-record/DLQ, já que este worker TEM fence de tenant `ACTIVE`, ao contrário de D-183/D-185). Forma distinta das 4 fatias anteriores: cobre DOIS tipos de entidade (`TenantQuotaRecord`+`EphemeralTelemetryMutation`, `quota.ts`), nenhum dos dois com `version` — o delete/backoff/DLQ do worker e o backfill novo condicionam em `resetAt` exato, não `version`. `deriveQuotaTelemetryMaintenanceDue()` nunca retorna `undefined` (sem estado terminal, ao contrário de Membership/Invitation) — por isso não há branch de self-heal de ponteiro obsoleto, diferente dos workers anteriores. 4 pontos de escrita (`consume()`'s ramo de criação/rollover, `incrementTelemetryCounter()` da lane `EphemeralTelemetryMutation`) gravam/refrescam o ponteiro GSI8 atomicamente no mesmo write que seta `resetAt` — `identity-store.ts` (persistence) nunca importa `quota.ts` (application), o GSI8 já vem calculado via `TelemetryIncrementInput.gsi8`. Suíte local 1841/1841, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` 22/22 (`gsi8_worker_isolation` confirmado `pass`). PR #208 mergeado (commit `a898c1d`→`a280110` em `main`), CI real (`33687474935`/`33687503644`) e CD real (`33688100421`) `success`, confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: GSI8 `ACTIVE`, role `exptrk-dev-quota-telemetry-purge-handler-role` com as 3 políticas esperadas, Lambda `Active`/`Successful`, schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`)**: positivo no próprio namespace (`WORK#QUOTA_TELEMETRY` → `allowed`), negativo nos 4 namespaces já migrados → `implicitDeny`. **Backfill real executado contra `dev`**: 24 candidatos, 24 ponteiros GSI8 escritos, confirmado por `Query GSI8`. Ver `decisions-log.md` D-186 para o detalhe completo. **5 de 9 workers migrados** (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`); **próxima fatia recomendada (não normativa)**: qualquer um dos 4 restantes — `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`.

**Achado real de bug pré-existente, encontrado nesta sessão ao tentar rodar o backfill de D-186 — pendente real, nomeado explicitamente para a próxima sessão**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` usado por 7 scripts em `scripts/` (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,quota-telemetry-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) para decidir se `main()` roda quando executado diretamente NUNCA casa no Windows/Git Bash deste ambiente (`import.meta.url` usa barras normais, `process.argv[1]` usa contrabarras) — `main()` silenciosamente nunca executava, sem erro, exit code 0. Corrigido SÓ em `backfill-gsi8-quota-telemetry-purge.ts` (trocado por `fileURLToPath(import.meta.url) === process.argv[1]`, confirmado rodando de verdade depois do fix). **Os outros 6 permanecem com o bug, não corrigidos** — significa que qualquer execução anterior desses scripts (backfills de D-180/D-182/D-183/D-185, e qualquer rodada de `reset-dev-data.ts`) reportada como "rodada com sucesso" pode ter silenciosamente não escrito nada, se executada pelo mesmo shell/mecanismo. **Ação recomendada para a próxima sessão**: aplicar o mesmo fix de uma linha nos outros 6 scripts (nível 1-2, mecânico) e reverificar/reexecutar os backfills de D-180/D-182/D-183/D-185 contra `dev` para confirmar se os ponteiros GSI8 daquelas fatias realmente foram escritos (`Query GSI8 WORK#<tipo>` e comparar contagem esperada) — não presumir que já estão corretos só porque a sessão anterior relatou sucesso.

**Continuação (2026-09-02, sessão nova, depois de D-186)**: **D-187** — sexta fatia de D-179: `security-audit-purge` migrado 100% ponta a ponta para GSI8, mesmo padrão de D-180/D-182/D-186 (com poison-record/DLQ, já que este worker TEM fence de tenant `ACTIVE`). Forma distinta: cobre QUATRO tipos de entidade (`AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent`, as mesmas 4 partições de `GET /activity`), todas append-only, sem `version`. Novo `src/shared/security-audit-gsi8.ts` (fora de qualquer `domain/` de módulo específico, já que a entidade cobre 4 módulos). **Simplificação real habilitada pela forma do código**: o ponteiro GSI8 é gravado na própria função pura `build*Event()` de cada entidade (4 funções), não em cada um dos 19 call sites que as chamam — como todo call site passa por elas antes de `appendXAuditToTransaction`, isso cobre automaticamente presente e futuro sem tocar nenhum call site individualmente. `MembershipAuditEvent`'s `organizationId` normalizado para `tenantId`, mesmo padrão pré-existente. Suíte local 1867/1867 (175 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` 22/22 (`gsi8_worker_isolation` confirmado `pass`). PR #210 mergeado (commit `5be0743`), CI real (`33691359230`/`33691948793`) e CD real (`33692216726`) `success`, confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: GSI8 `ACTIVE`, role `exptrk-dev-security-audit-purge-handler-role` com as 3 políticas esperadas, Lambda `Active`/`Successful`, schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8 — achado de processo: sem ele o simulate retorna `implicitDeny` até para o caso positivo)**: positivo no próprio namespace (`WORK#SECURITY_AUDIT` → `allowed`), negativo nos 5 namespaces já migrados → `implicitDeny`. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o fix do guard `main()` roda de fato): 0 candidatos encontrados — a tabela `dev` não tem hoje nenhuma linha `AuditEvent`-family (confirmado por `Scan` bruto de `entityType`), nada para escrever; confirmado por `Query GSI8 WORK#SECURITY_AUDIT` retornando `Count=0`. Ver `decisions-log.md` D-187 para o detalhe completo. **6 de 9 workers migrados** (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`); **próxima fatia recomendada (não normativa)**: qualquer um dos 3 restantes — `transient-purge`, `delivery-record-purge`, `core-user-data-purge`.

**Continuação (2026-09-02, sessão nova, depois de D-187)**: **D-188** — sétima fatia de D-179: `transient-purge` migrado 100% ponta a ponta para GSI8, mesmo padrão de D-180/D-182/D-186/D-187 (com poison-record/DLQ, já que este worker TEM fence de tenant `ACTIVE`). Forma distinta de todas as fatias anteriores: cobre DOIS tipos de entidade com dinâmicas DIFERENTES — `WebhookInbox` (criado uma vez/imutável, ponteiro gravado uma única vez na criação em `ses-callback-workflow.ts`, mesma forma de security-audit-purge/D-187) e `UploadSlot` (transições reais de status, ponteiro gravado/atualizado atomicamente a cada transição relevante — `advance-after-evidence.ts` no `CONSUMED`, `upload-slot-reconciliation/reconciliation.ts` no `EXPIRED` — mesma forma de invitation-purge/D-182); `RESERVED` nunca recebe ponteiro. Novo `src/shared/transient-purge-gsi8.ts`. **Achado de fronteira de módulo real**: a primeira versão do novo arquivo compartilhado importava `computeUploadSlotPurgeAfter()`/`UploadSlotStatus` de `modules/document/domain/**`, violando a regra `shared-must-not-reach-modules` do `dependency-cruiser` — capturado por `test/architecture/tenant-fence-boundary.test.ts` já na primeira rodada de testes, corrigido reimplementando a fórmula (um-liner estável de `privacy-lgpd.md` §4) localmente em vez de inverter a fronteira. `RELEASED` é um status declarado no union mas sem nenhum write path real hoje (confirmado por grep) — nenhum terceiro escritor precisou de mudança. Suíte local 1894/1894 (176 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` 22/22 (`gsi8_worker_isolation` confirmado `pass`). PR #212 mergeado (commit `d32957f`→`ca50bcf`), CI real (`33695255525`/`33695850896`) e CD real (`33696134854`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: GSI8 `ACTIVE`, role `exptrk-dev-transient-purge-handler-role` com as 3 políticas esperadas, Lambda `Active`/`Successful`, schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: positivo no próprio namespace (`WORK#TRANSIENT` → `allowed`), negativo nos 6 namespaces já migrados → `implicitDeny`. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `main()` já corrigido roda de fato): 0 candidatos — a tabela `dev` não tem hoje nenhuma linha `WebhookInbox`/`UploadSlot` (confirmado por `Scan` bruto de `entityType` cobrindo as 58 linhas da tabela inteira, sem `LastEvaluatedKey`), nada para escrever; confirmado por `Query GSI8 WORK#TRANSIENT` retornando `Count=0`. Ver `decisions-log.md` D-188 para o detalhe completo. **7 de 9 workers migrados** (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`); **próxima fatia recomendada (não normativa)**: qualquer um dos 2 restantes — `delivery-record-purge`, `core-user-data-purge`.

**Continuação (2026-09-02/03, sessão nova, depois de D-188)**: **D-189** — oitava fatia de D-179: `delivery-record-purge` migrado ponta a ponta para GSI8, mesmo padrão de D-187/D-188 (poison-record/DLQ, worker TEM fence de tenant `ACTIVE`). Cobre `NotificationIntent`/`NotificationAttempt`, elegibilidade `createdAt+180d`. Diferença real vs. D-187: AMBAS entidades têm `version` OCC real (mesma forma de D-188) — claim/backfill condicionam em `version`+`createdAt`. Novo `src/shared/delivery-record-gsi8.ts`; 3 pontos de escrita reais confirmados por grep (`reminder-dispatch/dispatch.ts`, `notification-router-workflow.ts`'s `applyStaleDecision`/`applyRoutedDecision`) gravam o ponteiro uma única vez na criação. Gap conhecido herdado de D-152 (`NotificationAttemptLookup` órfão) reconfirmado ainda válido, não tocado. Suíte local completa verde (1912 testes), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` 22/22 (`gsi8_worker_isolation` pass). PR #215 mergeado, CI (`33699314795`/`33699331683`) e CD (`33699827095`) reais `success`. Verificação ao vivo completa contra `dev`: GSI8 `ACTIVE`, role com as 3 políticas esperadas, Lambda `Active`/`Successful`, schedule `ENABLED`, isolamento cross-namespace confirmado (`simulate-principal-policy` positivo no próprio namespace, `implicitDeny` nos 7 já migrados). Backfill dry-run confirmou 0 candidatos reais (58 linhas na tabela, 0 `NotificationIntent`/`NotificationAttempt`) — execução real seria no-op, não executada (mesma disciplina de D-187/D-188). Ver `decisions-log.md` D-189 para o detalhe completo. **8 de 9 workers migrados** (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`); **próxima fatia (última, não normativa)**: `core-user-data-purge`.

**Continuação (2026-09-02/03, sessão nova, depois de D-189)**: **D-190 — nona e ÚLTIMA fatia de D-179. D-179 (MaintenanceDueIndex) FECHADO POR COMPLETO, 9 de 9 workers migrados.** `core-user-data-purge` migrado ponta a ponta para GSI8, mesma forma do piloto original (D-180/`membership-purge`) — elegibilidade gated numa transição de soft-delete (`deletedAt` sendo setado), não criação, diferente da forma create-once de D-186/D-187/D-188/D-189. Cobre `ExpirationItem`/`ReminderPolicy`, `deletedAt+30d`. **Achado real pré-existente, confirmado por grep amplo**: `ExpirationItem.deletedAt` tem exatamente 1 write path real (`expiration-service.ts#deleteItem`); `ReminderPolicy.deletedAt` NÃO TEM write path real nenhum hoje (`disablePolicy` só seta `enabled: false`) — gap pré-existente, o worker `Scan`-based anterior já tinha o mesmo gap, não introduzido por esta migração. Novo `src/shared/core-user-data-gsi8.ts`. Mudança de forma esperada vs. o texto original de D-151: o worker passa a usar `TransactWriteItems` (antes era `DeleteCommand` simples) porque o `ConditionCheck` de tenant `ACTIVE` da claim GSI8 precisa estar na mesma transação. Suíte local completa 1929/1929 (179 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` 22/22. PR #217 mergeado (`1633d6c`→`1e483c1`), CI (`33702974051`) e CD (`33703151890`) reais `success`. **Nota de processo**: o agente que implementou o código (PR #217) atingiu o limite de sessão logo após o CD ter sucesso, antes de completar a verificação ao vivo/backfill/docs — retomado e finalizado pela sessão coordenadora. Verificação ao vivo completa contra `dev`: GSI8 `ACTIVE`, role `exptrk-dev-core-user-data-purge-handler-role` com as 3 políticas esperadas, Lambda `Active`/`Successful`, schedule `ENABLED` (`cron(0 5 * * ? *)`). **Prova de isolamento cross-namespace — a mais ampla do programa, 8 verificações negativas** (`simulate-principal-policy`, com `--resource-arns`): `WORK#CORE_USER_DATA` → `allowed`; os 8 namespaces já migrados → `implicitDeny` em todos. Backfill dry-run + real confirmaram 0 candidatos — `dev` não tem hoje NENHUMA linha `ExpirationItem`/`ReminderPolicy` (58 linhas na tabela, `Count=0` em ambos os filtros). Ver `decisions-log.md` D-190 para o detalhe completo. **Os 9 workers de D-179, todos migrados e verificados ao vivo**: `membership-purge` (D-180/D-181), `invitation-purge` (D-182), `document-file-reconciliation` (D-183), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186), `security-audit-purge` (D-187), `transient-purge` (D-188), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190).

**Item pendente carregado de D-186/D-187/D-188/D-189, ainda NÃO resolvido — agora o item de maior prioridade real desta área, já que D-179 fechou**: o bug do guard `main()` (`if (import.meta.url === \`file://${process.argv[1]}\`)` nunca casa no Windows/Git Bash deste ambiente) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — corrigido em `backfill-gsi8-{quota-telemetry-purge,security-audit-purge,transient-purge,delivery-record-purge,core-user-data-purge}.ts`, todos escritos já com o fix desde o início. **Ação recomendada para a próxima sessão, se retomada antes do roadmap P0**: aplicar o mesmo fix de uma linha nos outros 6 scripts (nível 1-2, mecânico) e reverificar/reexecutar os backfills de D-180/D-182/D-183/D-185 contra `dev` para confirmar se os ponteiros GSI8 daquelas fatias realmente foram escritos (`Query GSI8 WORK#<tipo>` e comparar contagem esperada) — não presumir que já estão corretos só porque a sessão anterior relatou sucesso. Não é bloqueante para começar o roadmap P0 (pode ser feito em paralelo ou logo depois).

**Continuação (2026-09-03, sessão nova, depois de D-190 — PRIMEIRO ITEM DO ROADMAP DE LANÇAMENTO)**: **D-191 — `RequirementTemplate` (item 1/P0.1 do roadmap), design `APPROVED` E implementado ponta a ponta na mesma sessão.** Protocolo Claude↔Codex completo, 4 rodadas reais (`codex exec`), com a régua contestada na Rodada 1 e reconciliada em 4 versões (fluxo de `research-protocol.md`): R1 Claude 8,2 / Codex régua 6,4 design 6,6 → R2 9,2/9,1 vs 8,3/8,1 → R3 9,4/9,3 vs 8,8/8,7 → **R4 9,4/9,3 vs 9,1/9,0**, ambos ≥9,0 nas duas dimensões sem arredondar. E-014 `SIM PARCIAL` com 5 fontes externas datadas (Drata, Vanta, Asana, Atlassian, HighLevel) — padrão convergente encontrado: **materialização por CÓPIA no apply, nunca live-link**. Escopo real confirmado por leitura direta: a entidade-alvo é `Requirement` (document-archive, D-143/D-145), NÃO a legada `RequirementAssignment` (que declara o deferimento e fica intocada). Entregue: entidade + itens embutidos (cap 30) + ponteiro de nome do template + `RequirementNamePointer` (torna "nome de `Requirement` único por Subject" uma regra TRANSACIONAL, não um read-then-write, e essa regra foi **decidida explicitamente como decisão de produto** com gatilho de reversão nomeado, não inferida do precedente de `DocumentType`); 9 operações (create/get/list/update/duplicate/archive/unarchive/preview/apply); 9 rotas HTTP wireadas em `api-gateway/main.tf` E `proxy-allowlist.ts` (disciplina D-117/D-120/D-178, asseverada por teste no ato); 7 schemas + 11 testes de contrato; RBAC novo (catálogo `ADMIN_ROLES`, `apply` `WRITE_ROLES`, leitura `READ_ONLY_ROLES`). **5 achados reais fechados, todos levantados pelo Codex e verificados por leitura de código, não por argumento**: `createRequirement` nunca verificava o Subject (criava `Requirement` sob Subject inexistente/ARCHIVED — lacuna pré-existente, agora fenceada na transação); `status <> DELETED` deixaria `ARCHIVED` passar; dois itens de template normalizando para o mesmo nome virariam dois `Put` na MESMA chave numa transação (500 opaco em vez de 409); o fence do template passa a ser `status = ACTIVE AND version = <lida>`; e nenhum erro afirma causa que o DynamoDB não revelou (releitura que escolhia entre 404/409 removida). Contrato do apply: `2N+3` ações, cap 30 abaixo do teto 32 imposto pelo pior caso previsto `3N+3`. **Erro real meu corrigido na Rodada 4**: a justificativa anterior da reserva do cap era aritmeticamente impossível. Gate local completo verde: `typecheck`, `lint`, **1931/1931** (`test/unit`+`integration`+`contract`, 22 testes adversariais G-V3 novos), `check-docs`, `validate-schemas`, `build:lambdas`, `dependency-cruiser` 0 violações, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). Nota de ambiente: os 2 arquivos de `test/architecture/` falham neste worktree por falta de `node_modules` para processos filhos (`tsc`/`dependency-cruiser`) e falham IDENTICAMENTE na árvore sem a mudança (confirmado por `git stash`) — não é regressão; rodar `npm test` inteiro no checkout principal antes do merge. Detalhe completo em `decisions-log.md` D-191 e `docs/architecture/reviews/requirement-template-scoping/` (9 arquivos).

**Verificação ao vivo de D-191 — FEITA E CONFIRMADA (2026-09-03)**: PR #219 mergeado (`72ce4bb`→`4c90d44`), CI real (`33713015703` em `develop`, `33713051982` no PR, `33713271496` em `main`) e CD real (`33713455836`) todos `success`, confirmados via `gh run view` direto. Contra `dev` via `aws --profile claude-dev`: `exptrk-dev-document-archive-handler` `Active`/`Successful` (`LastModified` 2026-09-03T04:07:29Z, ou seja, o deploy desta fatia); **as 9 rotas `requirement-templates` confirmadas presentes** na API `uav7id1muh` (`aws apigatewayv2 get-routes`) — `POST`/`GET` da coleção, `GET`/`PATCH` do item, e `duplicate`/`archive`/`unarchive`/`preview`/`apply`. Nota importante de método: o CI verde **também** confirmou que os 2 arquivos de `test/architecture/` passam num ambiente com `node_modules` — as falhas observadas durante a implementação eram artefato do worktree isolado (processos filhos `tsc`/`dependency-cruiser` sem resolução de módulo), como o `git stash` já havia indicado, e não regressão.

**Pendentes reais deixados por D-191, nomeados explicitamente:**
1. **`RT-LANE-FALLBACK-01`** (achado pré-existente, fatia própria, nível 3-4): `src/shared/tenant-lifecycle/tenant-business-mutation.ts` linhas 213-223 classifica `CancellationReasons` ausente/malformado como `TenantNotActiveError`, afirmando uma causa que o DynamoDB não revelou. O correto sob o critério C7 de D-191 é um `ConflictError` genérico/indeterminado. Muda comportamento de uma lane compartilhada por todos os escritores tenant-scoped, por isso NÃO foi feito no meio de D-191 (mesma disciplina de D-177→D-178). Teste adversarial exigido: injetar `CancellationReasons` ausente e exigir `ConflictError`.
2. **Backfill de `RequirementNamePointer`** (`scripts/backfill-requirement-name-pointers.ts`, ainda NÃO escrito): `Requirement` já existentes em `dev` não têm ponteiro, então um homônimo ainda é criável até que sejam renomeados/recriados. Não é migração de risco (`AGENTS.md` §1), é estabelecer a baseline. Escrever já com o guard `fileURLToPath(import.meta.url) === process.argv[1]` (bug de D-186).
3. **Auditoria por `Requirement`**: `document-archive-service.ts` não emite `SubjectAuditEvent` (o `RequirementService` legado emite). O cap 30 de D-191 já foi dimensionado para que fechar isso (`3N+3`) não exija revisitá-lo.
4. ~~Verificação ao vivo de D-191~~ — **FEITA**, ver o parágrafo acima (PR #219 mergeado, CD `success`, Lambda e as 9 rotas confirmadas contra `dev`).
5. O bug do guard `main()` em 6 scripts de backfill (item carregado de D-186→D-190) continua aberto, inalterado por D-191.

## Roadmap de lançamento (`docs/project/roadmap-competitivo-2026-09-01.md`) — PRÓXIMA FRENTE DE TRABALHO AGORA (D-179 fechado)

Marcelo confirmou nesta sessão (2026-09-02): terminar todos os workers de D-179 primeiro (feito — 9/9, ver D-190 acima), e SÓ DEPOIS entrar na fila de funcionalidades do roadmap de lançamento. **Essa condição está satisfeita agora — a próxima sessão deve começar o item 1 do roadmap (Requirement Templates) diretamente**, salvo se Marcelo redirecionar.

`docs/project/roadmap-competitivo-2026-09-01.md` é a fonte normativa da ordem de funcionalidades **P0** necessárias antes do lançamento comercial (11 itens, ordem já decidida no documento — não reordenar sem motivo real). Auditoria rápida do estado real de cada item, feita nesta sessão por leitura direta do código (não presumida):

1. **Requirement Templates** — 🟢 **IMPLEMENTADO (D-191, 2026-09-03)**, gate local completo verde; mergeado (PR #219) e **confirmado ao vivo contra `dev`** (Lambda `Active`/`Successful`, 9 rotas presentes). Sobre `Requirement` (document-archive), não sobre a legada `RequirementAssignment`, que segue deferida como o próprio arquivo declara.
2. **Bulk onboarding/importação em massa** — 🟡 parcial; `src/modules/import/` existe e funciona (CSV parse/commit real), mas não confirmado se cobre Documents+Requirements como o roadmap pede (provavelmente só Subjects/ExpirationItems) — precisa investigação antes de assumir escopo.
3. **WhatsApp operacional** — ❌ não implementado; só modelado no type system (`NotificationChannelKind`, `notification-entitlements.ts`'s `whatsapp: {enabled}`) — `notification-router.ts`'s `SUPPORTED_CHANNELS = ["EMAIL"]` no código real, comentário confirma "later submilestone (kill switch AppConfig WHATSAPP)".
4. **IA/OCR integrada ao novo Document Lifecycle** — 🟡 parcial; M7 é E2E PROVEN mas contra o Document Lifecycle ANTIGO — reconciliar com `DocumentFile`/`DocumentType` (que só existem desde D-163+) é trabalho real ainda não feito, já nomeado como item 4 da macro-ordem de D-161.
5. **Busca e filtros documentais** — ❓ não investigado ainda nesta sessão.
6. **Dashboard operacional/compliance básico** — ❌ nenhuma implementação real encontrada (grep não achou rota/serviço de compliance dashboard).
7. **Relatórios + exportação + audit trail** — 🟡 parcial; export CSV existe (D-126), Admin Activity Log existe (D-149), mas não é o conjunto completo de relatórios do roadmap (documentos vencidos/vencendo/por Subject/por responsável/solicitações pendentes/renovações).
8. **Document Types configuráveis** — 🟢 bem avançado (D-173→D-186): CRUD completo, rotas HTTP, RBAC; falta só o item 6 do design original (schema HTTP guest obrigatório + rota pública de leitura de tipos).
9. **Consolidar Guest Upload + Requests + Review + Recurrence** — 🟡 backend avançado (D-143 a D-147: guest access, requirements, recorrência), mas "solicitar novamente"/histórico completo do roadmap não confirmado item a item.
10. **Consolidar Storage + Versioning + Renewal** — 🟢 avançado; `DocumentFile` fechou por completo (D-163 a D-168).
11. **Frontend completo do P0** — ❌ não iniciado, deliberadamente por último (a própria estratégia do roadmap pede isso — não antecipar).

**Próxima ação real depois do MaintenanceDueIndex fechar (9/9)**: seguir a ordem prática do roadmap (§6 do documento) — item 1 (Requirement Templates) é o próximo, por ter a melhor relação esforço/valor segundo o próprio documento. Antes de começar qualquer item, escopar de verdade (mesma disciplina de sempre — não presumir tamanho/nível de risco sem ler o código real primeiro); itens que envolvem modelo de dados novo ou definem um padrão que sistemas fora do projeto já resolveram (Requirement Templates parece nível 5-6, candidato a protocolo Claude↔Codex/E-014) devem passar pelo protocolo do `AGENTS.md` §4 antes de implementar.

`develop`/`main` sincronizados (diff zero) até D-188 inclusive (D-188 mergeado via PR #212, commit `ca50bcf`). Confirmar `git status`/`git log`/`git diff origin/main..develop --stat` antes de presumir qualquer coisa. Ver a seção "Próxima ação" abaixo.

**Estado em 2026-09-01 (fim de sessão, atualizado após verificação ao vivo)**: sessão de execução contínua e autônoma, autorizada explicitamente por Marcelo a fazer applies e merges reais (não só design). Tudo abaixo foi **confirmado ao vivo contra `dev`** via `aws --profile claude-dev`, não presumido.

**Domínio Documental (D-143) — Núcleo 1 + Núcleo 2, 100% completos em código E deployados**: Núcleo 1 (Document/DocumentVersion, D-144) já vinha de sessão anterior. **Núcleo 2 fechado nesta sessão**: Requirement (D-145, PR #141), guest access (D-146, PR #142), recorrência (D-147, PR #143) — as 9 decisões técnicas de D-143 estão todas implementadas E aplicadas em `dev`. **Achado de processo importante desta sessão**: `cd.yml` aplica automaticamente em todo push a `main` (OIDC, `AGENTS.md` §7) — não é preciso `terraform apply` manual após um merge normal; bastou confirmar ao vivo (`aws lambda get-function`/`aws apigatewayv2 get-routes`/`aws scheduler list-schedules`) que cada Lambda/rota/schedule apareceu. Confirmado: `exptrk-dev-document-archive-guest-handler`, `exptrk-dev-document-request-recurrence-handler`, `exptrk-dev-requirement-reindex-handler` todos `Active`/`Successful`; rotas `/document-archive/series*`, `/document-archive/guest/*`, `/document-archive/requirements/*` todas presentes; schedulers `exptrk-dev-requirement-reindex`/`exptrk-dev-document-request-recurrence` `ENABLED`.

**Backlog adicional fechado na mesma sessão, também deployado e confirmado ao vivo**:
- Dependency Freshness Standard (D-139→D-148, PR #144) — `dependabot.yml`, `scripts/check-dependency-freshness.ts`, fix real de drift `ci.yml`/`cd.yml`.
- Admin Activity/Audit Log (D-131→D-149, PR #157) — `GET /activity`, `ActivityService` com merge k-way de 4 partições + cursor composto, tela `ActivityLog.tsx`.
- Quarentena `HELD_FOR_RECOVERY` (D-127→D-150, PR #158) — mecanismo completo no grafo de `TenantLifecycleRecord`, `CancelOrganizationClosureService`, ASL estendido, sweeper com reconciliação estrita. Confirmado ao vivo via `aws stepfunctions describe-state-machine` (estado `HELD_FOR_RECOVERY` presente na definição real).
- **6 dos 7 workers de purga LGPD de D-127, todos deployados e confirmados**: `CORE_USER_DATA` (D-151, PR #159), `DELIVERY_RECORD` (D-152, PR #160), `SECURITY_AUDIT` (D-153, PR #161), `QUOTA_TELEMETRY` (D-154, PR #162, só `TenantQuotaRecord` — os outros 4 candidatos já tinham TTL nativo), `ACCOUNT_ACTIVE`-não-fechamento (D-155+D-169, **`Invitation`+`Membership` completos** — `Channel`/`NotificationPreferences` genuinamente fora de escopo, sem estado terminal próprio), `TRANSIENT` remanescente (D-156, PR #164, achado real: `UploadSlot.purgeAfter` era campo morto com nome errado do atributo TTL). **`LEGAL_EVIDENCE` (Prioridade 7) permanece corretamente bloqueada** por trava jurídica, não tocada.
- 9 PRs do Dependabot mergeados (versões menores/patch, sem breaking change).

Suíte de testes: 1345 → 1658 (backend), G-V3 aplicado em toda lógica não-trivial. `develop`/`main` sincronizados (diff zero confirmado) ao final da sessão.

**Itens abertos reais para a próxima sessão** (nenhum é "óbvio, prossiga sem perguntar" — todos exigem decisão de produto/arquitetura de Marcelo ou carregam risco elevado nomeado):
1. ~~PRs #152/#156 (Dependabot, ESLint 8.57→10.9)~~ — **RESOLVIDO, D-157, 2026-09-01** (protocolo Claude↔Codex rodada única, nota 8,4/10, nível 4). Root migrado para ESLint 10 + flat config; frontend bloqueado por upstream (`eslint-plugin-jsx-a11y`), dívida EOL registrada.
2. ~~`Membership.removedAt` (achado de D-155)~~ — **RESOLVIDO, D-158, 2026-09-01** (protocolo Claude↔Codex rodada única, nota 8,7/10, nível 4). Campo adicionado, `RemoveMembershipService`/`LeaveOrganizationService` gravam, `AcceptInvitationService` limpa na reativação. ~~Worker de purga físico de `Membership`~~ — **RESOLVIDO, D-169, 2026-09-02** (nível 3-4, implementação direta, sem protocolo): `MembershipPurgeWorker` deployado, mesmo layout de `invitation-purge`. `NotificationPreferences`/`Channel` seguem fora de escopo (sem estado terminal próprio, achado de investigação de D-155, não revisitado). Prioridade 5 de D-127 fecha por completo. Ordem confirmada por Marcelo, 2026-09-01: ESLint → Membership.removedAt → D-C/D-D hot path → Design System por último.
3. **Wave 1b — Design System Implementation Gap** — componentes restantes (`Modal`/`Tooltip`/`Popover`/`DropdownMenu`/`Drawer`/`Combobox`/`DateInput`) exigem decisão de padrão overlay/focus-trap ainda não tomada, candidatos a protocolo Claude↔Codex completo quando abordados.
4. ~~D-D da otimização de hot path~~ — **RESOLVIDO, D-172, 2026-09-02** (implementação direta de design já `APPROVED`, sem novo protocolo). `EphemeralTelemetryMutation` lane implementada em `quota.ts`, D-154 alargado para cobrir o novo `entityType`, teste adversarial do DoD confirmado ao vivo. Ver `decisions-log.md` D-172.
5. **D-C da otimização de hot path** (`IdentityStore.ConsistentRead` genérico) — achado real de que mudar `createProfileIfAbsent()` exige mudar um contrato genérico mais amplo; decisão de arquitetura pendente, provavelmente via protocolo.

Nenhuma infra nova pendente de apply — tudo que foi implementado nesta sessão já está em `dev`.

**Estado em 2026-08-31 17:xx**: as 3 lacunas de produto (D-121/122/123) estão **100% completas — design → implementação → deploy → verificação ao vivo**: D-124 (orquestrador W3-07, PR #122), D-125 (reatribuição de responsabilidade, PR #123), D-126 (exportação CSV, PR #124, PR #125 corrigiu um bug real de `depends_on` faltante que quebrou o primeiro CD) — todos com CD `success` confirmado, `exptrk-dev-export-handler` confirmado `Active`/`Successful` via `aws lambda get-function --profile claude-dev`. Multi-User B2B (15 waves) `DONE`. Wave B2B-14 (roteiro manual convite/aceite 2ª conta) segue adiada pelo Marcelo, não bloqueante. **2 bugs reais de CD encontrados e corrigidos nesta sessão** (PR #125: `depends_on` faltante; rerun do CD por race condition de lock com a própria CI) — nenhum recorrente, apenas registrar como classe de achado real desta rodada de implementação. **D-135 (2026-08-31)**: accent do design system revertido de azul-indigo (`#2F4FD0`) para roxo (`#7C3AED` família) — decisão direta de Marcelo, protocolo §4 dispensado sob exceção registrada de `ai-governance.md` §2, contraste WCAG verificado sem regressão, `tokens.css` já atualizado e suíte completa verde. Prosa de `visual-language-and-design-system.md`/`design-system.md` §0 ainda narra a escolha antiga do azul — pendência de atualização de texto nomeada, não bloqueia código.

**Ampliação de autoridade registrada nesta sessão (Marcelo, 2026-08-31, `docs/engineering/ai-governance.md` §1)**: para os itens da lista abaixo, resíduos de decisão de produto (não só o mecanismo técnico) também podem ser decididos por Claude+Codex via protocolo completo (pesquisa externa + rodada adversarial + nota cega ≥9,0) — revertível por ele depois, não dispensa o protocolo em si, NÃO se estende a execução real/destrutiva.

**Próximos itens autorizados a tratar autonomamente, NESTA ORDEM (Marcelo confirmou a ordem e a autoridade ampliada, 2026-08-31)**:
1. ~~Estratégia de quarentena antes de exclusão física de dado persistido + as 7 classes de retenção LGPD restantes~~ — **`APPROVED` (design), D-127, 2026-08-31**, protocolo completo 11 rodadas (Claude 9,4/Codex 9,3). Ver `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`. Mecanismo: novo estado `HELD_FOR_RECOVERY` (30 dias, cancelável) no grafo de `TenantLifecycleRecord`, reusa o Step Functions de W3-07/D-124. Priorização LGPD das 7 classes sem purga real dentro de tenant `ACTIVE`: `CORE_USER_DATA`→`DELIVERY_RECORD`→`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`(não-fechamento)→`TRANSIENT` remanescente, com `LEGAL_EVIDENCE` em lane bloqueada separada (trava jurídica). **Design-only, deliberado** — implementação real fica para sessão(ões) futura(s) dedicada(s), tamanho comparável a uma wave, não a um dia.
2. ~~`AppError.retryable` (comportamento de retry/DLQ do SQS)~~ — **`APPROVED` + `IMPLEMENTADO` (documentação apenas), D-128, 2026-08-31**, protocolo completo 3 rodadas (Claude 9,3/Codex 9,6). Fecha a pendência de `docs/engineering/decisions-log.md` E-011. Decisão: nenhum handler SQS ramifica sobre `retryable` — mecanismo de retry-vs-terminal continua exclusivamente `maxReceiveCount=5`+DLQ nativo do SQS, `retryable` permanece metadado diagnóstico. Pesquisa externa (AWS Lambda+SQS docs) confirmou nenhum padrão de referência recomenda branching por tipo de erro. Ver `docs/architecture/reviews/appError-retryable-sqs-scoping/`.
3. ~~Supersessão de GTR-01 (`UserProfile.requesterDisplayName`→`Organization.displayName`)~~ — **`APPROVED` + `IMPLEMENTADO`, D-129, 2026-08-31**, protocolo completo 3 rodadas (Claude 9,5/Codex 9,8). Decisão: REPLACE (não coexiste) — `Organization.displayName` é agora a única identidade guest-facing "quem está solicitando", `UserProfile.requesterDisplayName` removido por completo (campo, `GET/PUT /profile`, Lambda `profile-handler`, actions RBAC, rotas Terraform). Ver `docs/architecture/reviews/gtr-01-supersession-scoping/estado-final-consolidado.md`.
4. ~~Wave 1 — Design System reconciliation~~ — **`APPROVED` (documentação), D-130, 2026-08-31**, protocolo completo 5 rodadas (Claude 9,2/Codex 9,5). Ver `docs/architecture/reviews/design-system-reconciliation-scoping/estado-final-consolidado.md`. Decisões: `design-system-v1-proposal.md` → ADOTAR COM EMENDA (renomeado `docs/frontend/design-system.md`, valores primitivos concretos substituídos pelos já `APPROVED` em `visual-language-and-design-system.md`/`tokens.css`, nenhum gate `VL-G` reaberto); `frontend-engineering-quality-standard-v1-proposal.md` → ADOTAR (renomeado `docs/frontend/frontend-engineering-quality-standard.md`, coexiste com `interface-quality-standard.md` via crosswalk de ownership); `bff-frontend-quality-standard-proposal.md` → SUPERSEDED. **Puramente documental, deliberado** — nenhum código de frontend alterado; reconciliação do frontend real contra o catálogo de componentes agora normativo fica para **Wave 1b — Design System Implementation Gap**, trabalho futuro não iniciado.
5. ~~Tela de atividade/audit log para ADMIN/OWNER~~ — **`APPROVED` (design), D-131, 2026-08-31**; **`IMPLEMENTADO` (código completo), D-149, 2026-09-01** (infra escrita/validada, NÃO aplicada em `dev`). Ver `docs/architecture/reviews/admin-activity-log-scoping/estado-final-consolidado.md` (design) e `decisions-log.md` D-149 (implementação — handler `GET /activity`, `ActivityService`/merge/cursor, tela `ActivityLog.tsx`, rota Terraform, testes). Pendente real: aplicar a rota `GET /activity` em `dev` (`terraform apply`), decisão do Marcelo/sessão orquestradora.
6. ~~Implementação real do MECANISMO de quarentena de D-127~~ — **`IMPLEMENTADO`, D-150, 2026-09-01** (código completo, infra escrita/validada, NÃO aplicada em `dev`). Estado `HELD_FOR_RECOVERY` real no grafo, `CancelOrganizationClosureService`, ASL estendido (`WaitForRecovery`/`CheckCancelled`/`RecoveryCancelled`), sweeper com reconciliação estrita, legal-hold preservando `recoveryDeadline`. Ver `decisions-log.md` D-150. **Pendente real, nomeado explicitamente, ainda não iniciado**: migração dos `ReminderOccurrence` `CANCELLED`-writers para `cancelOccurrenceUpdate()`, regra ESLint de enforcement (round-11 do design). Dos 7 workers de purga LGPD, **`CORE_USER_DATA` (Prioridade 1) `IMPLEMENTADO`, D-151**, **`DELIVERY_RECORD` (Prioridade 2) `IMPLEMENTADO`, D-152**, **`SECURITY_AUDIT` (Prioridade 3) `IMPLEMENTADO`, D-153, 2026-09-01**, **`QUOTA_TELEMETRY` (Prioridade 4) `IMPLEMENTADO`, D-154, 2026-09-01**, **`ACCOUNT_ACTIVE (não-fechamento)` (Prioridade 5) `IMPLEMENTADO PARCIALMENTE`, D-155, 2026-09-01** (só `Invitation`; ver itens 6b/6c/6d/6e/6f abaixo) e **`TRANSIENT` remanescente (Prioridade 6) `IMPLEMENTADO`, D-156, 2026-09-01** (`WebhookInbox`+`UploadSlot`; ver item 6g abaixo) — só `LEGAL_EVIDENCE` (Prioridade 7, bloqueada por trava jurídica) segue sem início, das 7 prioridades LGPD originais de D-127. Aplicar a rota `POST /organizations/cancel-close` e o ASL estendido em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora.
6b. ~~Worker de purga `CORE_USER_DATA` (Prioridade 1 de 7 dos workers LGPD, D-127)~~ — **`IMPLEMENTADO`, D-151, 2026-09-01** (código completo, infra escrita, NÃO aplicada em `dev`). `ExpirationItem`/`ReminderPolicy` (`deletedAt`) ganham worker Lambda diário novo (`src/workers/core-user-data-purge/`, Scan full-table filtrado, delete condicionado por versão+`deletedAt` exato, fence de tenant `ACTIVE`); `ReminderOccurrence` ganha `purgeAfterTtl` (TTL nativo DynamoDB, atributo já habilitado na tabela, setado na materialização a partir da própria `scheduledAt`) — SEM worker, exatamente como D-127 pediu. Ver `decisions-log.md` D-151. **Pendente real, nomeado explicitamente**: os 6 workers LGPD restantes na época (`DELIVERY_RECORD`→`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada); backfill de `purgeAfterTtl` em `ReminderOccurrence`s pré-existentes (aberto, não implementado, ver D-151); aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora.
6c. ~~Worker de purga `DELIVERY_RECORD` (Prioridade 2 de 7 dos workers LGPD, D-127)~~ — **`IMPLEMENTADO`, D-152, 2026-09-01** (código completo, infra escrita/validada, NÃO aplicada em `dev`). `privacy-lgpd.md` §4 "intents/attempts" mapeado ao código real: `NotificationIntent` (`src/modules/reminder/domain/notification-intent.ts`) e `NotificationAttempt` (`src/modules/notification/domain/notification-attempt.ts`) ganham worker Lambda diário novo (`src/workers/delivery-record-purge/`, mesmo padrão de D-151 — Scan full-table filtrado por `entityType`/`attribute_exists(createdAt)`, delete condicionado por versão+`createdAt` exato, fence de tenant `ACTIVE`). Diferença de forma de elegibilidade vs. D-151: aqui é só idade (`createdAt+180d`), sem gate de "foi deletado" — todo registro de entrega expira pelo prazo fixo, independente de status. **Verificado explicitamente que NÃO é `AuditEvent`/`MembershipAuditEvent`** (essas são `SECURITY_AUDIT`, Prioridade 3, D-149's Admin Activity Log feed as usa — classe diferente, fora de escopo ali). Ver `decisions-log.md` D-152. **Pendente real, nomeado explicitamente**: `NotificationAttemptLookup` (pointer derivado, `PK=TENANT#t#ATTEMPT#id`) não é purgado por este worker — fica órfão após a purga do `NotificationAttempt` correspondente, achado nomeado mas não corrigido nesta sessão (sem efeito colateral externo, só um `GetItem` inútil se algo ainda o consultasse, o que não ocorre 180 dias depois de criado); aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora.
6d. ~~Worker de purga `SECURITY_AUDIT` (Prioridade 3 de 7 dos workers LGPD, D-127)~~ — **`IMPLEMENTADO`, D-153, 2026-09-01** (código completo, infra escrita/validada, NÃO aplicada em `dev`). `privacy-lgpd.md` §4 "AuditEvent/logs redigidos, MembershipAuditEvent" mapeado ao código real, confirmado por leitura direta: as 4 entidades `AuditEvent`-family (`AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` — as mesmas 4 partições que `GET /activity`'s `ActivityService`, D-149, já faz merge k-way) ganham worker Lambda diário novo (`src/workers/security-audit-purge/`, Scan full-table filtrado por `entityType`/`attribute_exists(occurredAt)`, fence de tenant `ACTIVE`). `occurredAt` é o relógio de idade das 4 (nenhuma declara `createdAt`). **Desvio de forma vs. D-151/D-152, documentado**: nenhuma das 4 tem `version` (append-only por construção) — `buildConditionalDelete` novo em `occ.ts` (irmão simétrico de `buildConditionalPut`) substitui `buildVersionedDelete`, condicionando o delete em `occurredAt` exato observado no scan. `MembershipAuditEvent`'s `organizationId` é normalizado para `tenantId` no adapter (mesmo valor, nome de campo diferente). **Investigação de corrida com paginação de `GET /activity` concluída SEM achado de bug** (cursor da activity usa `ExclusiveStartKey` como posição, não como referência a item vivo — ver `decisions-log.md` D-153 e header de `purge.ts` para o raciocínio completo). Ver `decisions-log.md` D-153. **Pendente real, nomeado explicitamente**: os 4 workers LGPD restantes à época (`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) na ordem já decidida; aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora.
6e. ~~Worker de purga `QUOTA_TELEMETRY` (Prioridade 4 de 7 dos workers LGPD, D-127)~~ — **`IMPLEMENTADO`, D-154, 2026-09-01** (código completo, infra escrita/validada, NÃO aplicada em `dev`). Investigação real (esta prioridade não tinha escopo trivial, ao contrário das 3 anteriores): das 5 entidades reais de rate-limit/quota do código (`MembershipInviteRateLimitRecord`, `GuestRateLimitRecord`, `InitialInviteRateLimitRecord`, `DocumentArchiveGuestRateLimitRecord`, `TenantQuotaRecord`), 4 — incluindo o exemplo nomeado em `privacy-lgpd.md` §4 — já têm `purgeAfterTtl` wireado ao TTL nativo da tabela, já resolvidas; só `TenantQuotaRecord` (`src/modules/identity/application/quota.ts`) não tinha nenhuma purga. Worker Lambda diário novo (`src/workers/quota-telemetry-purge/`, mesmo padrão de D-151/152/153 — Scan full-table filtrado por `entityType`, delete condicionado por `resetAt` exato re-afirmado, fence de tenant `ACTIVE`); elegibilidade = `resetAt` (fim da janela do token bucket) + 30 dias. Ver `decisions-log.md` D-154. **Pendente real, nomeado explicitamente**: os 3 workers LGPD restantes (`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) na ordem já decidida; aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora.
6f. ~~Worker de purga `ACCOUNT_ACTIVE (não-fechamento)` (Prioridade 5 de 7 dos workers LGPD, D-127)~~ — **`IMPLEMENTADO PARCIALMENTE`, D-155, 2026-09-01** (só `Invitation`; código completo, infra escrita/validada, NÃO aplicada em `dev`). Escopo nomeado pelo design (`Invitation`→`Membership`→`Channel`) investigado item a item: `Invitation` tem worker novo (`src/workers/invitation-purge/`, mesmo padrão dos anteriores — candidato = `REVOKED` via `revokedAt` OU `PENDING` com `expiresAt` já passado tratado como o `EXPIRED` de-facto nunca escrito por código nenhum, fence de tenant `ACTIVE`, delete condicionado por `version` real da entidade). **`Membership` e `Channel` NÃO implementados — achado real de bloqueio, não escopo abandonado**: `Membership.status = REMOVED` nunca grava timestamp nenhum (`remove-membership.ts`/`leave-organization.ts` só setam `status`+`version`) — sem relógio de elegibilidade "termo + 30 dias" para checar, e adicionar um campo novo a 2 write paths existentes é decisão além de implementar o worker já aprovado; `Channel` não existe como entidade persistida no código (o candidato mais próximo, `NotificationPreferences`, herda o mesmo bloqueio de relógio ausente por depender do `status` de `Membership`). Ver `decisions-log.md` D-155 para a investigação completa. **Pendente real, nomeado explicitamente**: decisão do Marcelo/sessão orquestradora sobre adicionar `removedAt` a `Membership` (destrava `Membership` e, por extensão, `Channel`/`NotificationPreferences`); aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora.
6g. ~~Worker de purga `TRANSIENT` remanescente (Prioridade 6 de 7 dos workers LGPD, D-127)~~ — **`IMPLEMENTADO`, D-156, 2026-09-01** (código completo, infra escrita/validada, NÃO aplicada em `dev`). `privacy-lgpd.md` §4 nomeia `WebhookInbox, UploadSlot, InvitationTokenPointer` na linha `TRANSIENT`; `InvitationTokenPointer` já resolvido via TTL nativo (não tocado). Worker novo único (`src/workers/transient-purge/`, um só `Scan` cobrindo `entityType IN (WebhookInbox, UploadSlot)`, fence de tenant `ACTIVE`, delete condicionado por `version` real de cada entidade): `WebhookInbox` (`ses-callback-workflow.ts`) purgado por `createdAt+7d`, sem mecanismo prévio nenhum (achado genuíno, como o `TenantQuotaRecord` de D-154). `UploadSlot` purgado reusando `computeUploadSlotPurgeAfter()` (já existia em `document/domain/retention.ts`, mas era código morto — nunca chamada por nenhum writer, e o campo `purgeAfter` que a entidade carrega é uma string ISO comum, não o atributo `purgeAfterTtl` que o TTL nativo da tabela realmente observa): `RESERVED` nunca é candidato (reserva ativa em voo), `CONSUMED` usa a janela de 7 dias, `EXPIRED`/`RELEASED` usam a janela de 24h ("slot incompleto"). Ver `decisions-log.md` D-156 para a investigação completa. **Pendente real**: nenhum item de escopo aberto nesta prioridade — aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) é decisão do Marcelo/sessão orquestradora; só resta `LEGAL_EVIDENCE` (Prioridade 7, bloqueada por trava jurídica) das 7 prioridades LGPD originais de D-127.

**Item 7 (execução destrutiva `reset-dev-data.ts --confirm`) explicitamente POSTERGADO pelo Marcelo — não tratar, não perguntar de novo até ele sinalizar.**

Cada um dos 4 itens acima segue o mesmo padrão desta sessão: rodada Claude↔Codex completa (pesquisa externa quando aplicável, 3+ rodadas, nota cega, gate 9,0) → se pequeno o bastante, implementação direta na mesma sessão (D-124/125/126 pattern) → `decisions-log.md` novo D-number (próximo é **D-127**, confirmar lendo o arquivo) → atualizar este arquivo → commit/push `develop` autônomo → PR→`main` se suite verde. **Cuidado com conflito**: todos tocam `decisions-log.md`/este arquivo — rodar SEQUENCIALMENTE, nunca em paralelo (lição aprendida nesta sessão com D-121, evitar delegação recursiva de agentes — instruir explicitamente "não spawnar sub-agente, fazer o trabalho você mesmo").

## Fase atual

`Consolidation + Pilot Readiness` concluído com recomendação **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`). M0-M11 implementados e deployados; M7 (extração/OCR) code-complete e **E2E PROVEN** em `dev`; Full BFF + Frontend Production Foundation `APPROVED` e implementados; planejamento de interface 8/9 etapas `APPROVED` (só falta User Validation, em suspenso a pedido do Marcelo). W3-07 (exclusão física de tenant/DSR/LGPD) tem design `APPROVED WITH CONDITIONS` (D-066/D-067) e está em implementação incremental por chunks (D-068 a D-104, última emenda de privacidade B2B-9). M12 (billing) bloqueado por decisão de fornecedor (D-052). **Multi-User B2B (D-084 a D-108): design técnico `APPROVED` via protocolo Claude↔Codex, physical model formalizado (Wave B2B-1, D-086) e timing decidido diretamente pelo Marcelo — proceder agora, supersedendo o gatilho comercial que antes gated M13.** Waves B2B-0 a B2B-11 `DONE` — **B2B-5 (RequestContext Cutover) implementada (D-096): login não cria mais tenant sozinho, `RequestContextResolver` resolve organização real via `Membership`, `POST /bff/organizations` fecha o loop de onboarding com cap transacional. B2B-7 (RBAC) implementada (D-097/D-098): `Membership.role=ADMIN` real na matriz de autorização, primeira aplicação real de `docs/engineering/research-protocol.md`/E-014. B2B-8 (Invitations/Team) implementada (D-099/D-100): `Invitation`/aceite/revogação/gerência de membros (role-change/remove/leave) com last-owner protection transacional, segunda aplicação de E-014, primeiro writer real de `Membership` além da criação — novo Lambda `memberships-handler`, 7 rotas API Gateway novas. B2B-6 (BFF Organization Context) implementada (D-101/D-102): `X-Organization-Id` (BFF-derived, nunca do browser) transportado até o `RequestContextResolver`, fecha o `InternalError` 500 que B2B-8 tornou explorável, `POST /bff/organization/select`/`GET /bff/organizations` novos, terceira aplicação de E-014. B2B-9 (W3-07/Privacy Reconciliation) implementada (D-103/D-104): fecha o `InvitationTokenPointer` órfão pós-exclusão de Organization (3ª cláusula OR no scan/`PURGE_DELETE`), formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md` §4.1, quarta aplicação de E-014. B2B-10 (Tenant-aware Frontend) implementada (D-105/D-106): corrige uma regressão real (frontend tratava todo usuário autenticado como deslogado desde B2B-5), isolamento de cache real entre tenants (`ActiveOrganizationProvider`, gate `switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta), switcher/members/settings novos, primeira aplicação do protocolo a uma decisão de frontend. B2B-11 (Responsibility + Notifications) implementada (D-107/D-108): `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch.userId` migrados de validação vestigial (`UserProfile`) para `Membership`+`GlobalUser` reais, quinta aplicação de E-014 — NÃO inclui a supersessão de GTR-01 (`Organization.displayName`), ainda pendente. B2B-12 (Cutover de dev) implementada (D-110/D-111): `scripts/reset-dev-data.ts` novo (Fase A inventário+snapshot sempre, Fase B delete real só `--confirm`, allowlist de tabela/conta, verificação final fail-loud), `LEGACY_TENANT_ONLY` removido (backend+frontend+teste), sexta aplicação de E-014 (declaração `NÃO`) — Fase A (leitura) já verificada de fato contra `dev` real; **execução real destrutiva (`--confirm`/`--include-cognito`) permanece pendente de confirmação explícita do Marcelo, registrada como o único item aberto dessa wave.** B2B-13 (E2E/Adversarial Security) implementada (D-112/D-113): auditoria completa contra as 25 perguntas de `roadmap-evolution/17` §121 (20/25 já cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assíncrona de notificação — `notification.ts`/`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` já testado, 2 ganham teste novo de revogação/isolamento de role, 1 é auditoria de fixture IDs sem achado), sétima aplicação de E-014. **Multi-User B2B — todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** B2B-14 (Operational Evidence) `EM ANDAMENTO` — 7 achados reais corrigidos (D-114 a D-120), teste de convite/aceite com segunda conta explicitamente adiado pelo Marcelo. B2B-15 (Documentation Reconciliation) `EM ANDAMENTO` em paralelo. 3 decisões de produto novas (purga W3-07/orquestrador, exportação de dados, reatribuição de responsabilidade) fecharam design `APROVADO` via protocolo (D-121/D-122/D-123); a primeira (orquestrador de purga) já foi implementada em D-124, as outras duas seguem só com design. Ver "O que está em andamento" abaixo. **Lembrete de calibração (D-093, `AGENTS.md` §1)**: projeto sem usuário real nem produção — não pesar "quebraria conta/sessão existente em `dev`" como risco bloqueador.

Ver `docs/architecture/README.md` (linha `Design maturity`/bloco de status no topo) para o resumo executivo mais completo — este arquivo não duplica aquele bloco, só aponta para ele.

## O que já está implementado (referência, não repetir aqui)

- **Backend por módulo/milestone**: `AGENTS.md` §7 (invariantes estáveis) + `ARCHITECTURE.md` (visão consolidada).
- **Frontend/planejamento de interface**: `docs/frontend/README.md` (índice completo, blockers técnicos BLOCKER-A/B/C todos resolvidos, GTR-01 com decisão de produto pendente W5-01 já fechada).
- **Engenharia/qualidade**: `docs/engineering/README.md` (padrões, gates, backlog do programa de pilot readiness).
- **Infra**: Terraform (`infra/`, ADR-0009), CI/CD via GitHub Actions OIDC.
- **Reconciliação de engenharia de contexto (2026-08-29)**: `DONE`, mergeada em `main` (PR #84) — root cleanup + `AGENTS.md`/este arquivo reconciliados + 2 guardrails novos em `check-doc-drift.ts`, revisão Claude↔Codex 9,3/10. Registro completo: `docs/architecture/reviews/context-engineering-reconciliation/`.
- **E-011 (logging/tracing) — junção `correlationId`↔X-Ray**: `E2E PROVEN` (2026-08-29) — smoke test real em `dev` via `aws --profile claude-dev` confirmou `xrayTraceId` no log idêntico ao trace real do X-Ray. Detalhe: `docs/architecture/correlationid-xray-trace-join.md`.
- **E-012 (2026-08-29) — Definition of Done por item de todo list**: `APPROVED` via protocolo Claude↔Codex (3 rodadas, Claude 9,1/Codex 9,2). Regra de processo permanente, vigente a partir de agora em toda sessão: nenhum item de todo list que produza/altere código real é `completed` sem passar pelo gate do seu nível de risco — ver `docs/engineering/definition-of-done.md` (granularidade, gate por nível, registro de evidência `DoD:`). **Emenda E-013 (2026-08-30)**: aplicação de `test-engineering-standard.md` tornada operacional (não só referencial) — G-V3 (mutação nomeada por escrito) exigido explicitamente por teste, não só "suíte verde". Achado do Marcelo, corrigido retroativamente nos testes de B2B-2.
- **D-093 (2026-08-30) — sem usuário real nem produção, não pesar esse risco**: `AGENTS.md` §1 ganhou parágrafo permanente — projeto em fase de construção, sem usuário real, sem produção (deploy só em `dev`, dado sintético resetável), sem prazo. Nunca tratar "quebraria conta/sessão existente em `dev`" como risco bloqueador por si só; não dispensa rigor de engenharia/segurança/protocolo §4. Achado do Marcelo, motivado por D-092 ter invocado essa categoria de risco indevidamente (a conclusão técnica de D-092 continua válida por outras razões).
- **D-094 (2026-08-30) — Wave B2B-4 (Onboarding) implementada, `DONE`**: `OnboardingStateResolver` (`src/modules/organization/application/onboarding-state.ts`) exatamente per escopo D-092 — classificador puro, hidrata Membership contra a partição base em vez de confiar na projeção do GSI4 (physical model §6), 7 testes com G-V3 aplicado desde a escrita. Detalhe completo em `docs/architecture/multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-094.
- **D-095/D-096 (2026-08-30) — Wave B2B-5 (RequestContext Cutover), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-095) e implementado (D-096) no mesmo dia, com sinal do Marcelo entre as duas etapas. Login não cria mais tenant sozinho (`bootstrapUser()` 2 itens); `RequestContextResolver` reescrito resolve a Organization real via `Membership`, com gate de lifecycle da própria Organization (achado real, corrigido antes de fechar — faltava no design original) e assert nomeado para `Membership.role` não suportado (`ADMIN`); `POST /bff/organizations` (novo) fecha o loop de onboarding com cap transacional (`GlobalUser.hasCreatedOrganization`, mesma `TransactWriteItems` da criação, nunca check-then-act); sessão BFF com self-heal. Achado real corrigido: `UserProfile` **não** foi removido como o escopo original prescrevia — ainda serve `profile-service.ts`/guest flows; provisionamento só mudou de lugar (lazy, por Organization). Suíte completa 1134/1134, zero regressão, `terraform test`/`build:lambdas` verdes. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b5-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-095/D-096.
- **E-014 (2026-08-30) — Pesquisa externa antes de decisões Type 1**: `APPROVED` via protocolo Claude↔Codex (3 rodadas, Claude 9,2/Codex 9,2). Regra de processo permanente: uma decisão nível 5-6 que define um padrão que sistemas fora deste projeto já resolveram (RBAC, convite, sessão multi-tenant) exige declaração `SIM`/`SIM PARCIAL`/`NÃO` na Rodada 1 e, se aplicável, um checklist de critérios pesados derivado da pesquisa que vira a régua de nota da rodada (nunca julgamento livre depois) — ver `docs/engineering/research-protocol.md`. Pedido do Marcelo, mesmo padrão de E-012. **Aplicada 6 vezes: Wave B2B-7 (RBAC, D-097), Wave B2B-8 (Invitations/Team, D-099), Wave B2B-6 (BFF Organization Context, D-101), Wave B2B-9 (W3-07/Privacy Reconciliation, D-103), Wave B2B-10 (Tenant-aware Frontend, D-105, primeira aplicação a uma decisão de frontend — query keys escopadas por tenant, padrão documentado pela própria TanStack Query v5) e Wave B2B-11 (Responsibility + Notifications, D-107, GitHub/Linear "assignee deve ser membro real e ativo") — nas 6 a Rodada 1 do Codex contestou o próprio checklist ou achou bloqueante real, reconciliado nas rodadas seguintes.**
- **D-097/D-098 (2026-08-30) — Wave B2B-7 (RBAC), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-097, primeira aplicação real de E-014) e implementado no mesmo dia (D-098). Pesquisa real (GitHub/Linear/Slack/Notion/NIST-ANSI-INCITS-359) não convergiu para uma regra única "ADMIN=OWNER" — a Rodada 1 do Codex contestou o próprio checklist de critérios de nota (não só o design), reconciliado na Rodada 2 (régua 9,1/9,1) antes da nota do design voltar a contar. `Role` ganha `ADMIN`, paritário com `OWNER` nas 4 actions de deleção de recurso e no bypass de ownership de `authorize()`; `notification:configure` reclassificada de `ADMIN_ROLES` para `READ_ONLY_ROLES` (achado real, bug fix — bloquearia `MEMBER`/`VIEWER` reais de configurar a própria preferência pessoal, contradizendo o próprio comentário do código citando essa action como precedente de self-service); `tenant:configure-document-request-delivery` isolada em novo tier `OWNER_ROLES` (config. externa/reputacional do tenant). Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b7-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-097/D-098.
- **D-099/D-100 (2026-08-30) — Wave B2B-8 (Invitations/Team), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-099, segunda aplicação de E-014, declaração `SIM PARCIAL`) e implementado no mesmo dia (D-100). Pesquisa real confirmou (não redesenhou) o mecanismo `ownerCount > :one` já `APPROVED` em D-086 §8 (convergência 4/4 GitHub/Slack/Notion/Linear em last-owner protection) e informou a hierarquia de gerência de membros (ADMIN-tier-e-acima gerencia membros; só OWNER promove/demove OWNER). A Rodada 1 do Codex achou 3 bugs de design reais (convite pendente com a mesma tier de privacidade que lista de membros; token de convite nunca consumido dentro da transação de aceite; colisão de chave de rate-limit com `initial-invite-rate-limiter.ts`) — todos corrigidos nas Rodadas 2/3. `Invitation`+token pointer+dedup (D-086 §7); `AcceptInvitationService` (transação de 6 itens); `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService` com builder único de `ownerCount`; novo Lambda `memberships-handler` + 7 rotas API Gateway. Gap registrado, não escondido: envio real de e-mail de convite via SES não conectado nesta wave (mecanismo pronto, ativação requer decisão de kill-switch). Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b8-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-099/D-100.
- **D-101/D-102 (2026-08-30) — Wave B2B-6 (BFF Organization Context), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-101, terceira aplicação de E-014) e implementado no mesmo dia (D-102). Achado real que motivou a wave: `RequestContextResolver.resolveActiveMembership()` lançava `InternalError` (500) para >1 `Membership` `ACTIVE` — inatingível até B2B-8, agora explorável de verdade. Pesquisa (padrão header+revalidação server-side, OWASP Multi Tenant Security) confirma a postura já em uso neste projeto (revalidação via banco, não cache de claims). A Rodada 1 do Codex contestou o checklist (faltava OWASP; faltava boundary BFF/browser; `select` sem CSRF; lista sem filtro de lifecycle; estado ambíguo sem contrato tipado) — reconciliado nas Rodadas 2/3. `X-Organization-Id` injetado só pelo BFF (nunca lido do browser, `ProxyService.forward()`); `resolveWorkingOrganization()`/`OrganizationUnavailableError`(403)/`OrganizationSelectionRequiredError`(409) substituem o `InternalError` antigo e um `AuthenticationError` pré-existente inconsistente; `organizationIdHint` obrigatório (não opcional) threadeado por 56 call sites reais/13 arquivos; `POST /bff/organization/select`/`GET /bff/organizations` novos. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b6-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-101/D-102.
- **D-103/D-104 (2026-08-30) — Wave B2B-9 (W3-07/Privacy Reconciliation), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-103, quarta aplicação de E-014) e implementado no mesmo dia (D-104). Achado real de código: `InvitationTokenPointer` (`organization/domain/invitation-token.ts`) declara `organizationId`, não `tenantId` — o scan/`PURGE_DELETE` de purga (ampliados em D-082/B1 só para o nome `tenantId`) nunca alcançavam esse pointer, deixando-o órfão para sempre após exclusão de uma Organization. Pesquisa real (GitHub/Slack/Atlassian, convergência 3/3) fundamentou a nova seção `privacy-lgpd.md` §4.1 (User-level vs. Organization-level erasure — titular individual nunca cascateia para dado de negócio organization-owned; invariante de último OWNER documentada, sem guard de código, sem call site real hoje). A Rodada 1 do Codex achou 3 bloqueantes reais (fronteira User/Organization incompleta — faltava `DeviceSession`/`Session`/`IdentityMapping`/`UserProfile`; `MembershipInviteRateLimitRecord` ausente da retenção; plano de teste não provava o adapter real) — todos corrigidos na Rodada 2, Rodada 3 foi tréplica de polimento sem achado novo. Fix: 3ª cláusula `OR organizationId = :tenantId` no scan (`tenant-purge-scan.ts`) e no `PURGE_DELETE` (`system-mutation.ts`). Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b9-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-103/D-104.
- **D-105/D-106 (2026-08-30) — Wave B2B-10 (Tenant-aware Frontend), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-105, primeira aplicação do protocolo a uma decisão de frontend nesta sessão) e implementado no mesmo dia (D-106). Achado real #1: `frontend/src/auth/AuthContext.tsx`'s `probe()` exigia campos (`tenantId`/`userId`) que `GET /bff/session` nunca retorna desde B2B-5/D-095 — todo usuário autenticado era tratado como `SESSION_MISSING`, regressão real em `develop`/`main`, corrigida. Achado real #2: nenhum serviço de B2B-8 atualiza `Organization.displayName`/`timezone` — `UpdateOrganizationSettingsService` novo (`organization:update-settings`/`OWNER_ROLES`). A Rodada 1 do Codex achou a proposta original subestimando uma corrida real de isolamento de cache (`organizationId` não é uma variável que o `queryFn` real usa — o BFF injeta `X-Organization-Id` a partir da sessão server-side, não do cliente); Rodada 2 fechou a corrida com um gate `switching`+`cancelQueries`, mas achou que `useActiveOrganization()` precisava ser Context único (não hook reimplementado por chamador); Rodada 3 exigiu `AbortSignal` propagado ponta-a-ponta para o `cancelQueries()` funcionar de verdade. `api/queryKeys.ts` (factory central), `auth/ActiveOrganizationContext.tsx` (`ActiveOrganizationProvider`), `components/OrganizationSwitcher.tsx`, `routes/{Members,Settings}.tsx` novos. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b10-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-105/D-106.
- **D-107/D-108 (2026-08-30) — Wave B2B-11 (Responsibility + Notifications), `DONE`**: escopo aprovado via protocolo Claude↔Codex 3 rodadas (D-107, sexta aplicação de E-014) e implementado no mesmo dia (D-108). 4 achados reais: `NotificationRecipientResolver` validava contra `UserProfile` (vestigial desde B2B-5); `resolveCandidateUserId()`'s fallback `?? tenantId` estruturalmente quebrado pós-B2B; `ItemWatchService.addWatcher`/`assigneeUserId` sem validação nenhuma contra `Membership` real. A Rodada 1 do Codex achou 3 bloqueantes (mudança de assinatura do fallback quebraria o caller real + um teste existente; migração `UserProfile→Membership` colapsava indevidamente a distinção `RECIPIENT_NOT_FOUND`/`RECIPIENT_NOT_ELIGIBLE`; e-mail de entrega ainda lia `UserProfile`, quebrando para um membro recém-aceito que nunca logou naquela Organization) — corrigidos na Rodada 2 (fallback vira `""` mantendo tipo `string`; distinção de estados preservada; e-mail migrado para `GlobalUser`). Rodada 2→3: elegibilidade precisa exigir `Membership ACTIVE` **e** `GlobalUser.identityStatus ACTIVE` (mesma regra dupla de `resolve-request-context.ts`). Port novo `MemberEligibilityChecker` (`expiration/ports/`). **Nota de escopo real**: não inclui a supersessão de GTR-01 (`UserProfile.requesterDisplayName`→`Organization.displayName`), prevista em B2B-5 mas nunca debatida nas 3 rodadas — continua pendente. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b11-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-107/D-108.

## O que está em andamento

1. **Multi-User B2B — autorizado a prosseguir, Waves B2B-0 a B2B-13 `DONE` (D-084 a D-113).** Design técnico `APPROVED` (protocolo completo em `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` §125) e timing decidido diretamente pelo Marcelo. Estado detalhado por subitem (evidência `DoD:`, judgment calls de sequenciamento documentados) vive em `docs/architecture/multi-user-b2b-wave-tracker.md` — não duplicado aqui. Resumo: **B2B-0** inventário read-only; **B2B-1** physical model formalizado via protocolo Claude↔Codex 5 rodadas (D-086, `docs/architecture/multi-user-b2b-physical-model.md`); **B2B-2** fundação de identidade global aditiva (D-087/D-088); **B2B-3** `Organization`/`Membership`/`CreateOrganizationService` (D-089/D-090/D-091); **B2B-4** `OnboardingStateResolver` (D-092/D-094); **B2B-5** `RequestContext Cutover` (D-095/D-096) — login não cria mais tenant sozinho, `RequestContextResolver` resolve a Organization real, `POST /bff/organizations` fecha o loop de onboarding; **B2B-7** `RBAC` (D-097/D-098) — `Membership.role=ADMIN` real na matriz de autorização; **B2B-8** `Invitations/Team` (D-099/D-100) — `Invitation`/aceite/revogação/gerência de membros com last-owner protection, primeiro writer real de `Membership` além da criação, `ADMIN` finalmente alcançável em produção via convite; **B2B-6** `BFF Organization Context` (D-101/D-102) — `X-Organization-Id` transportado do BFF ao recurso, multi-org selecionável de verdade, fecha o `InternalError` 500 que B2B-8 tornou explorável; **B2B-9** `W3-07/Privacy Reconciliation` (D-103/D-104) — fecha o `InvitationTokenPointer` órfão pós-exclusão de Organization, formaliza User-level vs. Organization-level erasure em `privacy-lgpd.md` §4.1; **B2B-10** `Tenant-aware Frontend` (D-105/D-106) — corrige a regressão real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; **B2B-11** `Responsibility + Notifications` (D-107/D-108) — `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch.userId` migrados para `Membership`+`GlobalUser` reais (NÃO inclui a supersessão de GTR-01, ainda pendente); **B2B-12** `Cutover de dev` (D-110/D-111) — `scripts/reset-dev-data.ts` (Fase A inventário+snapshot sempre, Fase B delete real só `--confirm`, allowlist de tabela/conta, verificação final fail-loud), `LEGACY_TENANT_ONLY` removido (backend+frontend+teste), Fase A (leitura) já verificada de fato contra `dev` real; execução real destrutiva permanece pendente de confirmação explícita do Marcelo; **B2B-13** `E2E/Adversarial Security` (D-112/D-113) — auditoria contra as 25 perguntas de §121 (20/25 já cobertas por waves anteriores, 2 fecham com fix real de TOCTOU em entrega assíncrona de notificação, 2 ganham teste novo de revogação/isolamento de role, 1 é auditoria de fixture IDs sem achado).

   **Multi-User B2B — todas as 14 waves originais (B2B-0 a B2B-13) `DONE`. B2B-14 (Operational
   Evidence) `EM ANDAMENTO`, 7 achados reais severos já corrigidos (D-114 a D-120), todos só
   descobertos por exercitar o fluxo de verdade contra `dev` pela primeira vez** — exatamente o
   job desta wave (mesmo espírito de E-011/M7's E2E PROVEN): (1) `app_origin` placeholder nunca
   trocado, OAuth nunca funcionou (D-114); (2) `bff-handler` crashava em TODA invocação desde
   B2B-8, `GUEST_TOKEN_PEPPER` nunca wireado (D-115); (3) `gsi4_read_policy_json` nunca anexado
   aos 10 Lambdas reais que o consomem (D-116); (4) `GET /bff/organizations`/`POST
   /bff/organization/select` nunca tiveram rota real no API Gateway (D-117); (5) nenhuma tela de
   "criar organização" existia no frontend, todo usuário sem Organization ficava preso em
   "Carregando…" para sempre (D-118, feature nova `Onboarding.tsx`+`OnboardingGate.tsx`); (6)
   `ExpressionAttributeNames: {}` explícito quebrava `TransactWriteItems` real com
   `ValidationException` em 3 arquivos, o mais severo sendo o guard de última-proteção-de-OWNER
   (D-119); (7) aceitar convite nunca teve tela nem rota real no API Gateway (mesma classe do
   D-117) e o e-mail real de convite nunca foi ativado — SES sandbox sem NENHUM endereço
   verificado no ambiente inteiro (D-120, PR #121, CD `success` confirmado — 2 endereços reais
   verificados com o Marcelo, `LeaveOrganizationSection` novo, `handleListMembers` parou de
   vazar o item bruto do DynamoDB). **Testado ao vivo** com o cookie de sessão real do Marcelo:
   save de Settings, `GET /bff/organizations`, `GET /bff/api/organizations/members` todos
   confirmados OK; `POST /bff/organizations` corretamente bloqueado por `CONFLICT` (cap de uma
   organização por `GlobalUser`, confirma que troca de organização só é testável depois do
   convite/aceite fechar o ciclo). **Pendente para fechar B2B-14**: teste de convite/aceite com
   uma segunda conta e o resto do roteiro manual (trocar role, trocar de organização, revogar
   membership, excluir organização) — **Marcelo confirmou explicitamente que isso fica para uma
   sessão futura, não bloqueia nada agora**. B2B-15 (Documentation Reconciliation, checklist
   `AGENTS.md` §6) já começou em paralelo (não depende de B2B-14 fechar) — ver item da tabela de
   gates abaixo para o que já foi feito/falta.

   **3 lacunas reais de produto, fora do escopo original de B2B-0..15, levantadas pelo Marcelo em
   2026-08-30** (detalhe completo nas memórias de sessão — ver rodapé deste arquivo): (a) o
   pipeline de purga de tenant (W3-07) está implementado e testado, mas nada o aciona
   automaticamente quando uma conta/organização era fechada — **`APROVADO` em D-121,
   `IMPLEMENTADO` em D-124, mergeado em `main` (PR #122) e CD `success` em 2026-08-31 — state
   machine `exptrk-dev-tenant-purge` e sweeper `exptrk-dev-tenant-purge-sweeper` (`ENABLED`)
   confirmados ao vivo contra `dev` via `aws --profile claude-dev`. Totalmente resolvido; falta só
   uma purga real de ponta a ponta para virar `E2E PROVEN`, sem urgência**; (b) exportação de dados (CSV/planilha)
   nunca foi implementada, só importação (M10) — lacuna real dado que LGPD exige portabilidade
   como direito do titular, escopo ainda não debatido; (c) quando um `Membership` é removido,
   `ExpirationItem.assigneeUserId`/`ItemWatch` não são reatribuídos nem limpos — o campo fica
   órfão indefinidamente e a notificação só para de disparar silenciosamente, sem fallback nem
   fluxo de reatribuição — escopo ainda não debatido.
2. **W3-07 — purge pipeline durável + orquestrador**: pipeline implementado/revisado (D-081/D-082/D-083, Codex 9,1/10) e emendado para as entidades B2B (B2B-9/D-104). **Orquestrador `IMPLEMENTADO` (D-124, 2026-08-31)** — exatamente o mecanismo `APROVADO` em D-121, implementação direta sem rodada nova: módulo `infra/modules/tenant-purge-workflow/` + `infra/state-machines/tenant-purge.asl.json` (Wait 1800s → transições → `RunPurge` → `Choice` com bound `PURGE_RETRY_LIMIT=20` → `VERIFIED`/`DELETED`, ramos `MarkBlocked*` → `Fail`), 3 Lambdas novos (transição única parametrizada por `from`/`to`, wrapper de `purgeTenant()` devolvendo o envelope compacto, sweeper), 2 alarmes `AWS/States` novos (`ExecutionsFailed`+`ExecutionsTimedOut`), EventBridge Scheduler `rate(1 day)`, `CloseOrganizationService`+action `organization:close` (`OWNER_ROLES`)+rota `POST /organizations/close`+UI type-to-confirm, composition root real fechando o KNOWN LIMIT de prefixo↔bucket de `purge-tenant.ts` (tabela FECHADA bucket→raiz). `transitionTenantLifecycle()`/`purgeTenant()` deixam de ser código sem chamador. **Mergeado em `main` (PR #122, 2026-08-31) e CD `success`** — confirmado ao vivo contra `dev` (`aws stepfunctions list-state-machines`/`aws scheduler list-schedules` via `--profile claude-dev`): `exptrk-dev-tenant-purge` existe, `exptrk-dev-tenant-purge-sweeper` está `ENABLED` e rodando `rate(1 day)`. **Downstream ainda pendente**: nenhuma purga real de tenant executou ainda de ponta a ponta (não há tenant genuinamente `DELETING` em `dev` hoje) — fica para quando `CloseOrganizationService` for exercitado de verdade, mesmo padrão de "código deployado, sem E2E PROVEN ainda" que outras waves já registraram.
3. **W3-07 — fencing dos writers de negócio** (`TenantBusinessMutation`): a maioria dos writers reais já fenced (chunks D-068 a D-080, ver `decisions-log.md`). Gap residual documentado, não explorável hoje: entradas sem PK `TENANT#`-prefixed E sem `tenantId` declarado (`LoginAttempt`/`GuestRateLimitRecord`) passam sem verificação — nenhum call site real produz isso.

## Gates / bloqueios abertos

| Item | Precisa de | Onde está o detalhe |
|---|---|---|
| Wave B2B-14 — fechar o roteiro manual (convite/aceite com 2ª conta, troca de role, switch de organização, revogação, exclusão de organização) | **Adiado pelo próprio Marcelo para uma sessão futura** — não bloqueia nada, não perguntar de novo até ele sinalizar | `decisions-log.md` D-120, `multi-user-b2b-wave-tracker.md` linha B2B-14 |
| Execução real destrutiva de `scripts/reset-dev-data.ts` (`--confirm`/`--include-cognito` contra `dev`) | Confirmação explícita do Marcelo antes de rodar — design/implementação/verificação de leitura já `DONE` (D-110/D-111) | `docs/architecture/multi-user-b2b-wave-b2b12-scope.md`, `decisions-log.md` D-111 |
| `AppError.retryable` — deveria decidir comportamento real de SQS retry/DLQ? | Decisão de produto do Marcelo | `docs/engineering/decisions-log.md` E-011 |
| 7 de 9 classes de retenção LGPD sem purga física real dentro de tenant `ACTIVE` (`privacy-lgpd.md` §4) | **`APPROVED` (design + priorização), D-127, 2026-08-31 — 6 dos 7 workers `IMPLEMENTADO` E `DEPLOYADO` em `dev` (confirmado ao vivo via `aws lambda get-function`)**: `CORE_USER_DATA` D-151, `DELIVERY_RECORD` D-152, `SECURITY_AUDIT` D-153, `QUOTA_TELEMETRY` D-154 (só `TenantQuotaRecord`), `TRANSIENT` remanescente D-156, todos 2026-09-01; `ACCOUNT_ACTIVE (não-fechamento)` (Prioridade 5) **`IMPLEMENTADO PARCIALMENTE E DEPLOYADO`, D-155** — só `Invitation`; `Membership`/`Channel` bloqueados por timestamp de elegibilidade ausente (`Membership.removedAt`), decisão de Marcelo pendente antes de destravar. Só `LEGAL_EVIDENCE` (Prioridade 7, bloqueada por trava jurídica) segue sem início, corretamente | `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`, `decisions-log.md` D-151 a D-156 |
| User Validation (planejamento de interface) | Sinal explícito do Marcelo para retomar | `docs/frontend/README.md` |
| Wave 1b — Design System Implementation Gap: `Divider`/`IconButton` implementados (D-133); `Link`/`Checkbox`/`RadioGroup`/`Switch` implementados (D-134, 2026-08-31); Button `lg`/motion `slow` decididos como "não agora" (D-132); z-index já 100% implementado (não era gap); breakpoints nomeados decididos como "não agora" pelo mesmo critério de D-132. **Ainda faltam do catálogo de ~30** (`design-system.md` §29), todos com estratégia de overlay/portal/focus-trap não decidida ou composição maior não-trivial, deliberadamente não implementados: `Combobox`, `DateInput`/`DatePicker`, `Tooltip`, `Popover`, `DropdownMenu`, `Modal`/`Dialog`, `Drawer` (candidatos a protocolo Claude↔Codex completo quando abordados — não há padrão de overlay/focus-trap já implementado no projeto para reusar), `Tabs`, `Pagination`, `Breadcrumb`, `Avatar` (composição maior, sem ambiguidade de padrão). `Card` (§40) tem conflito real com decisão já em produção em `Layout.tsx` ("no Card component, on purpose") — não implementado, gap nomeado para decisão explícita futura, não silenciosa. Nenhum dos 4 novos componentes de D-134 (`Link`/`Checkbox`/`RadioGroup`/`Switch`) está consumido em nenhuma tela ainda — mesmo padrão de `Divider`/`IconButton` em D-133, primitivos de catálogo prontos para uso, não wireados retroativamente em telas existentes nesta sessão. | Sessão futura dedicada — lista acima é o escopo remanescente exato, não "reconciliar tudo" | `docs/architecture/reviews/wave1b-button-lg-motion-scoping/estado-final-consolidado.md`, `decisions-log.md` D-132/D-133/D-134, `docs/architecture/reviews/design-system-reconciliation-scoping/estado-final-consolidado.md` |
| **Estratégia comum de quarentena antes de exclusão física de dado persistido** | **`APPROVED` (design) D-127 → `IMPLEMENTADO` E `DEPLOYADO`, D-150, 2026-09-01** — estado `HELD_FOR_RECOVERY` (30 dias, cancelável) real entre `ACTIVE` e `DELETING` no grafo do W3-07, `CancelOrganizationClosureService`, ASL estendido, sweeper com reconciliação estrita; confirmado ao vivo (`aws stepfunctions describe-state-machine` mostra `HELD_FOR_RECOVERY` na definição real). Pendente: migração de writers `ReminderOccurrence`/regra ESLint (round-11 do design) e a lane bloqueada `LEGAL_EVIDENCE` (Prioridade 7, trava jurídica) — as outras 6 prioridades LGPD já têm worker `IMPLEMENTADO`(_PARCIALMENTE_ no caso de `ACCOUNT_ACTIVE`) e deployado | `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`, `decisions-log.md` D-127/D-150 |
| **PRs #152/#156 (Dependabot: ESLint 8.57.1→10.9.1, root + frontend)** | **RESOLVIDO, D-157, 2026-09-01** — root migrado para ESLint 10 + flat config (`eslint.config.js`), PR #152 fechado (superseded); frontend permanece bloqueado por upstream (`eslint-plugin-jsx-a11y` sem suporte a ESLint 10 ainda), PR #156 fechado sem merge, dívida EOL registrada, reavaliar quando o plugin publicar suporte | `decisions-log.md` D-157 |
| ~~`Membership.removedAt` ausente~~ (achado real de D-155) | **RESOLVIDO, D-158, 2026-09-01** — campo adicionado e gravado/limpo nos 3 write paths reais; worker de purga físico de `Membership`/`Channel` (Prioridade 5 remanescente de D-127) segue como próxima frente, agora desbloqueada | `decisions-log.md` D-155, D-158 |
| **Otimização de performance do hot path (D-136, 2026-08-31)** — `APPROVED` (design), 3 rodadas, Claude 9,3/Codex 9,3. Gatilho real: Marcelo reportou lentidão para mostrar conteúdo + mensagem "validando sessão" desnecessária. 5 decisões design-only, status atualizado em 2026-08-31/09-01: **D-A `IMPLEMENTADO`** (D-140 — sessão frontend única query, waterfall removido, skeleton sem texto técnico), **D-B `IMPLEMENTADO`** (D-141 — session touch coalescing, `ConsistentRead` mantido), **D-C teve Rodada 1 tentada e REJEITADA nesta sessão (D-159, 2026-09-01, nota 5,5/10, nível 5 confirmado pelo Codex, não nível 4)** — a proposta original (tornar a leitura de `createProfileIfAbsent()` eventualmente consistente) não satisfaz o texto já `APPROVED` ("sai do hot path"), só reduz o custo sem eliminar o round-trip síncrono; achado real mais importante: `UserProfile` (a entidade que esse método escreve) não tem NENHUM leitor confirmado em todo o código — a pergunta certa para a próxima rodada é remover-ou-relocar a entidade, não mudar `IdentityStore.get()`, **D-D `IMPLEMENTADO`, D-172, 2026-09-02** (lane `EphemeralTelemetryMutation` real para `API_REQUEST`, D-154 alargado, teste adversarial do DoD confirmado ao vivo — ver `decisions-log.md` D-172), **D-E `APPROVED` e `IMPLEMENTADO`** (D-142, 4 rodadas Claude 9,3/Codex 9,6 — paginação real via cursor no dashboard/coleção, `queryGsi1Page`/`DashboardPage`/`useItemsDashboardBounded`+`useItemsDashboardPage`, corrige o bug de skip de cursor e o overflow do cap de export achados nas rodadas). Auditorias de origem em `docs/architecture/reviews/performance-hotpath-scoping/`. **Node 20→24 (D-137/D-138) já implementado e deployado**. Itens mecânicos ainda pendentes (ADOT, Lambda memory/ARM64, SQS batch, React.lazy, staleTime por query, N+1 BatchGetItem) ficam para implementação direta futura, sem protocolo | D-C (decisão de arquitetura — começar pela pergunta "UserProfile ainda precisa existir?", ver D-159) e D-D (implementação + teste adversarial) ficam para sessão futura dedicada, protocolo completo (mínimo 3 rodadas, nota ≥9,0) | `docs/architecture/reviews/performance-hotpath-scoping/estado-final-consolidado.md`, `decisions-log.md` D-136/D-140/D-141/D-142/D-159 |
| **Node 20→24 + AWS provider v5→v6 (D-137/D-138, 2026-08-31) — `IMPLEMENTADO` e `DEPLOYADO`.** Node 20 já depreciado pela AWS (30/04/2026); migração revelou bloqueio real (`nodejs24.x` exige `hashicorp/aws >= 6.19.0`, projeto pinado em `~5.0` em 22 módulos) — resolvido com upgrade do provider (pesquisa externa confirmou nenhum breaking change do v6 afeta este projeto). CI real (`infra/tests/stack.tftest.hcl`) 19/19, `terraform validate` limpo (14 avisos de depreciação `range_key`/`hash_key`→`key_schema`, não bloqueante, não corrigido). PR #131 mergeado, CD `success`, **confirmado ao vivo**: `exptrk-dev-bff-handler` rodando `nodejs24.x`/`Active` via `aws lambda get-function-configuration --profile claude-dev`. Lambda memory (256MB uniforme) deliberadamente não alterado — Power Tuning real fica para sessão futura | Nenhuma — implementado e verificado ao vivo | `decisions-log.md` D-137/D-138 |
| **Dependency Freshness Standard (D-139 design → D-148 implementação, 2026-09-01) — `IMPLEMENTADO`.** `.github/dependabot.yml` (npm root+`/frontend`, terraform `/infra`+`/infra/modules/*` via glob, github-actions `/`, weekly, sem auto-merge); `scripts/check-dependency-freshness.ts` + `docs/engineering/dependency-freshness-policy.json` (9 entradas reais, `node`/`lambda-runtime` com `lifecycle` populado com EOL real pesquisado — ambos 2028-04-30, fontes `endoflife.date`/AWS Lambda runtimes doc, janela `ok` hoje); `npm run check-dependency-freshness` wired em `ci.yml`; fix real do `ci.yml`/`cd.yml` hardcodando `terraform_version` separadamente (agora `env.TERRAFORM_VERSION` em cada workflow, checker prova sincronia). 15 testes novos, G-V3 confirmado ao vivo na fronteira 6/12 meses | Nenhum item aberto — os 3 entregáveis de D-139/§7 estão completos e `npm run check-dependency-freshness` passa limpo contra o repo real | `decisions-log.md` D-148, `docs/engineering/dependency-freshness-standard.md`, `docs/engineering/dependency-freshness-policy.json`, `scripts/check-dependency-freshness.ts` |
| **Domínio Documental — arquitetura técnica (D-143, 2026-09-01)** — `APPROVED` via protocolo Claude↔Codex, **6 rodadas** (4,6→5,9→7,6→8,4→8,8→9,3), Claude 9,3/Codex 9,3. Implementa as decisões funcionais D1–D10/C1–C6 já `APROVADO` pelo Marcelo (`docs/frontend/document-domain-functional-decisions.md`) — pedido explícito dele: analisar os 6 docs de planejamento, pesquisar externamente, rodar o protocolo, só então implementar. 9 decisões técnicas fechadas: state machines Document/DocumentVersion com transação única para o fluxo comprimido C3; armazenamento single-table com GSI2/GSI5 novos (verificados livres por grep, nunca GSI3/GSI4/GSI6 já ocupados por reminder/membership) e transação `acceptVersion` de até 10 ações com idempotência real; `DocumentVersionEvent` append-only distinto de `OutboxEvent`; guest access em 3 camadas (credencial longa+revogável/sessão curta com interstitial/submit idempotente); `Requirement.applicability` como fato persistido + job diário de reindexação; arquivos com scan individual (`pendingFileScans`/`infectedFileScans`); `REJECTED` nunca removível (corrige contradição real com J9 encontrada na Rodada 1); recorrência com `seriesId`/`occurrenceId`/`requestId`/`attemptIndex` e materialização atômica; RBAC tenant-wide reaproveitado, download por Viewer (A1) deixado genuinamente aberto. **Implementação do Núcleo 1 — camada de aplicação COMPLETA (2026-09-01)**: módulo `src/modules/document-archive/` com `domain/`+`ports/`+`persistence/`+`application/`+`http/` todos implementados e testados — `DocumentArchiveService` (`reserveUpload`/`commitUpload`/`claimReview`/`acceptVersion`/`rejectVersion`) com RBAC real (`docarchive:create/read/upload/review`, novas actions em `authorization.ts`) e `assertReviewerOrAdmin()` (D-143 Decision 1/Bloqueador 6 — MEMBER que não reivindicou uma Version não pode decidi-la, ADMIN/OWNER sempre podem); `http/document-archive-handlers.ts` no mesmo pipeline de `item-handlers.ts`; 6 schemas JSON novos (`schemas/api/docarchive-*.json`) registrados no `defaultSchemaRegistry`; composition root (`src/runtime/aws/composition/document-archive.ts`) e Lambda handler (`src/runtime/aws/handlers/document-archive-handler.ts`, mesmo padrão de `items-handler.ts`). Suíte: 30 testes unitários de serviço/domínio + 79 testes de contrato + 4 testes de integração end-to-end reais (create→reserveUpload→commitUpload→claimReview→acceptVersion via HTTP handlers reais; reject-então-reupload provando que a version rejeitada nunca some do histórico; VIEWER negado com 403 real via `authorize()`; 400 de schema) — G-V3 confirmado 3x ao vivo (grafo de transição, gate de scan de arquivo, gate de reviewer/admin). `npm test` 1345/1345, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos. **Núcleo 1 100% IMPLEMENTADO e DEPLOYADO (2026-09-01)**: infra Terraform real aplicada em `dev` — `exptrk-dev-document-archive-handler` Lambda (sem política IAM dedicada, GSI2/GSI5 já cobertos por `tenant_facing_read_write_policy_json`) + 8 rotas `/document-archive/*` no API Gateway + `build-lambdas.ts`/`proxy-allowlist.ts` atualizados no mesmo commit (evita a classe de bug D-117/D-120 de rota/handler órfão). `terraform test` (suíte real de CI) 19/19; apply via plano `-target`-escopado (16 a adicionar, 0 a mudar, 0 a destruir — excluindo deliberadamente ~76 diffs de `source_code_hash` de um rebuild completo de lambdas, fora do escopo desta mudança); confirmado ao vivo via `aws lambda get-function` (Active) e `aws apigatewayv2 get-routes` (8 rotas presentes). Achados reais corrigidos durante a implementação: o fake de teste (`in-memory-store.ts`) inicialmente avaliava CADA par nome/valor de `ExpressionAttributeNames`/`Values` como condição, confundindo placeholders de SET com condições reais — corrigido com um parser pequeno e real da gramática AND/OR que os builders de `occ.ts` produzem; e a validação de transição rodava ANTES da checagem de idempotência, classificando erroneamente um replay legítimo como transição inválida — corrigido invertendo a ordem. **Núcleo 2 (Requirement/D-145, guest access/D-146, recorrência/D-147) 100% completo em código E DEPLOYADO (2026-09-01)** — as 9 decisões técnicas de D-143 estão todas implementadas e confirmadas ao vivo em `dev` (Lambdas `Active`, rotas presentes, schedulers `ENABLED`); nenhum escopo do Domínio Documental fica sem código ou sem deploy | Nenhuma — Domínio Documental fechado por completo. Próximo Núcleo do roadmap (se houver) é decisão de produto do Marcelo | `docs/architecture/reviews/document-domain-scoping/estado-final-consolidado.md`, `decisions-log.md` D-143/D-145/D-146/D-147, `src/modules/document-archive/`, `infra/main.tf` |

## Decisões deliberadamente adiadas (já decididas como "não agora", não esquecidas)

- M12 (Billing) — bloqueado por escolha de fornecedor de pagamento (D-052).
- Opção B (atributo de span OpenTelemetry para correlationId↔trace) — candidata futura, 3 pré-requisitos nomeados, não perseguida agora (`correlationid-xray-trace-join.md` §2).
- Document Lifecycle Management — mesma classe, arquivado como informativo (`docs/architecture/roadmap-evolution/16-document-lifecycle-strategic-analysis.md`).
- BFF/frontend quality standard (rubrica mais ampla que `interface-quality-standard.md`) — proposta não adotada (`docs/frontend/bff-frontend-quality-standard-proposal.md`).
- Itens explicitamente fora de escopo do próprio Multi-User B2B (`roadmap-evolution/17` §98): custom roles, ABAC, SAML/SCIM, seat billing, platform admin/impersonation, department hierarchy.
- **Synthetic Persona Evaluation Framework** (`docs/architecture/roadmap-evolution/18-20`, trazido pelo Marcelo 2026-08-31) — **decisão explícita dele (2026-08-31): não é prioridade agora, mas precisa estar pronto quando chegar a hora de testes mais robustos antes de testes com humanos de verdade.** Gatilho de retomada: quando a sessão decidir avançar para essa fase de testes pré-User-Validation (não antes — `User Validation` em si também está em suspenso, ver linha acima). Nessa hora, retomar nesta ordem: (1) confirmar com o Marcelo se ainda é a prioridade certa naquele momento; (2) fechar primeiro a lacuna mais barata que o doc 19 já nomeia — Playwright hoje roda majoritariamente contra BFF mockado (`page.route()`), não contra o sistema real; (3) submeter o doc 20 (trial identity, tecnicamente sólido nesta leitura — metadata nunca em entidade de domínio, propagação via outbox `metadata`/nunca `payload`, gate de ambiente, fail-closed) à Rodada 1 do protocolo Claude↔Codex antes de qualquer código.

## Próxima ação, em ordem de valor esperado

**Estado no início da próxima sessão**: ESLint (D-157), `Membership.removedAt` (D-158) e D-C do hot path (D-160, `UserProfile` removido) foram todos fechados numa mesma sessão (2026-09-01), incluindo merge em `main`. Nesta MESMA sessão, Marcelo trouxe um roadmap competitivo novo (`docs/project/roadmap-competitivo-2026-09-01.md`) e pediu reconciliação + macro-ordem via protocolo — **fechado como D-161** (`docs/architecture/reviews/competitive-roadmap-reconciliation/estado-final-consolidado.md`, Codex 9,6/10). Isso muda a prioridade #1 da lista abaixo — não é mais "pergunte a Marcelo o que priorizar entre itens desconexos", é "comece pelo roadmap competitivo, seguindo a ordem já decidida":

1. **Roadmap Competitivo (D-161)** — spike de auditoria dos itens 4/9/10 **fechado nesta sessão** (2026-09-01, continuação), e a decisão fundacional #1 (`DocumentFile`) **fechada como design, D-163**:
   - **Item 9 (guest upload) re-confirmado ao vivo**: `commitUpload()` (`document-archive-service.ts` linhas 157-177) só flipa `DocumentVersion.state` para `RECEIVED` — nenhum S3/presign/scan real. Achado de D-161 confirmado sem ambiguidade.
   - **Item 5 (bug do GSI2) corrigido — D-162**: `createDocument()` nunca gravava GSI2 apesar do comentário afirmando que gravava. Fix mecânico aplicado, testado (G-V3 ao vivo), 1663/1663 verde. Leitura por GSI2 (a feature "Busca por Subject" em si) continua greenfield, não implementada — só a escrita foi corrigida.
   - **Decisão fundacional #1 (`DocumentFile`) reclassificada e fechada como design — D-163**: o scoping real (não a estimativa otimista do handoff anterior) confirmou **nível 5**, não 3-4 — falta todo um subsistema (entidade + storage + invariantes transacionais + reconciliação), não só um data-write num campo já modelado. Protocolo completo, 4 rodadas (5,2→7,1→8,3→9,4), `APPROVED (design)`. Ver `docs/architecture/reviews/document-file-scoping/estado-final-consolidado.md` para o design completo.
   - **Implementação real, primeira fatia — D-164 (2026-09-02)**: entidade `DocumentFile` + `reserveFiles()` (fecha a corrida de PRINCIPAL numa `TransactWriteItems` real) + `ConditionCheck` transacional do PRINCIPAL em `acceptVersion()`, ambos com G-V3 confirmado ao vivo.
   - **Implementação real, segunda fatia — D-165 (2026-09-02)**: `applyFileScanResult()`/`confirmFileScanClean()` (`src/modules/document-archive/application/apply-file-scan-result.ts`) — transição terminal do scan (PENDING_UPLOAD/SCANNING → CLEAN/REJECTED/UNSUPPORTED/TIMEOUT) + correlação simétrica de evidência S3/GuardDuty, reusando `decideNextAction()` de M6 verbatim; wireado em `DocumentArchiveService`. G-V3 confirmado ao vivo nos 3 mecanismos pedidos (corrida simétrica, idempotência terminal, remoção do ponteiro GSI5). Suíte completa 1677/1677. Ver `decisions-log.md` D-165.
   - **Implementação real, terceira fatia — D-166 (2026-09-02)**: `DocumentFileReconciliationWorker` (`src/workers/document-file-reconciliation/`), generaliza `UploadSlotReconciliationWorker` de M6 para o namespace GSI5 esparso de `DocumentFile` (`TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`, `fileReconciliationGsi5Keys()` de D-164, sem chamador até agora). `applyFileScanTimeout()` novo em `apply-file-scan-result.ts` faz a transição terminal por deadline (espelha o branch REJECT existente, mesmo `buildVersionedUpdate`/mesma remoção de ponteiro GSI5), com uma condição extra `GSI5PK = :observado AND GSI5SK = :observado` que fecha a corrida "candidato já avançou entre a varredura e a escrita" (round4-claude-final.md §3). **Desvio nomeado do design**: GSI5PK de `DocumentFile` é tenant-scoped (diferente do GSI6 global que permite ao worker de M6 fazer uma `Query` cross-tenant real) — este worker descobre candidatos via `Scan` na tabela base filtrado por `entityType`/`scanStatus`/`attribute_exists(GSI5PK)`, nunca GSI6, mesmo trade-off já aceito por `scanSatisfiedRequirements`/`scanActiveSeries` deste módulo. Lambda novo `document-file-reconciliation-handler`, schedule `rate(15 minutes)`, só `tenant_facing_read_write_policy_json` (sem GSI6/GSI3 novo, confirmado por teste Terraform). G-V3 confirmado ao vivo em 2 pontos (condição exata do ponteiro; comparador de deadline). Suíte completa 1685/1685, `terraform test` 21/21. Ver `decisions-log.md` D-166. **Implementação real, quarta e quinta fatia — D-167 (2026-09-02)**: itens 3+4 da ordem, ambos fechados na mesma sessão. Item 3 (presign real): `DocumentArchiveService.reserveFiles()` agora presigna cada `DocumentFile` de verdade via `UploadUrlSigner` (mesma porta genérica de M6, `S3UploadUrlSigner` reusado sem adapter novo), presign acontece DEPOIS da `TransactWriteItems` comitar (mesma ordem de M6), retorno mudou de `DocumentFile[]` para `ReservedFile[]` (`{file, uploadUrl, requiredHeaders}`). Item 4 (rota HTTP): `POST /document-archive/documents/{documentId}/versions/{seq}/files` nova (`handleReserveFiles`, schema `docarchive-reserve-files-request.v1.json`, mesma RBAC `docarchive:upload` de `reserveUpload`/`commitUpload`), IAM novo `document_archive_presign_quarantine_put` (mesmo par PutObject+GenerateDataKey escopado que `documents_handler` já tem, nunca mais amplo). **Achado real corrigido no caminho**: o schema novo devolvia HTTP 500 até ser registrado em `src/shared/contracts/schema-validator.ts` — `defaultSchemaRegistry` só carrega schemas estaticamente importados, mesma classe de bug já documentada ali para M7 item 8 (2026-08-27). G-V3 confirmado ao vivo (presign real + rota HTTP 201/400/403, quebrando e restaurando cada mecanismo). Suíte completa 1692/1692, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21) todos limpos. Ver `decisions-log.md` D-167. **Pendente real, nomeado explicitamente, próxima e última fatia deste arco (item 5, deliberadamente NÃO implementado nesta sessão)**: ativar o gate `fileSetSealed` em `commitUpload()` (D-163 §4, desenhado mas não ligado — alguns fluxos/testes legados ainda chamam `commitUpload()` sem nunca passar por `reserveFiles()` primeiro, então ativar o gate exige primeiro migrar/decidir esses caminhos). O worker S3/GuardDuty real que chama `applyFileScanResult()`/`confirmFileScanClean()` em produção também segue pendente (fora do escopo dos itens 3-4). Infra desta fatia (rota+IAM) está escrita/validada mas ainda não aplicada em `dev` — aplica automaticamente no próximo CD de push a `main` (`AGENTS.md` §7), não precisa de `terraform apply` manual.
   - **Implementação real, sexta e última fatia — D-168 (2026-09-02)**: item 5 da ordem, ativação do gate `fileSetSealed` em `commitUpload()` (D-163 §4) — o bloqueador nomeado por D-164/D-166/D-167 (nenhuma rota HTTP chamava `reserveFiles()` antes de `commitUpload()`) estava resolvido desde D-167 (rota HTTP real de `reserveFiles()`). Gate em duas camadas: pré-check em memória + `extraConditions` transacional TOCTOU-safe (`#sealed = :true`), mesmo padrão do fence do PRINCIPAL de D-164. Item 5 secundário do escopo (recuperação de `DRAFT` com PRINCIPAL infectado, `round4-claude-final.md` §6) já estava fechado pelo design, nenhuma mudança de código necessária — confirmado por leitura antes de escrever qualquer código. 15 testes pré-existentes que chamavam `commitUpload()` sem `reserveFiles()` corrigidos (achado real esperado, nomeado por todos os handoffs anteriores). G-V3 confirmado ao vivo (remover pré-check + `extraConditions` simultaneamente faz o teste novo falhar). Suíte completa 1693/1693, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21) todos limpos. Ver `decisions-log.md` D-168.
   - **`DocumentFile` (D-163→D-168) está 100% COMPLETO** — entidade, `reserveFiles()`/fence do PRINCIPAL, `applyFileScanResult`/`confirmFileScanClean`/`applyFileScanTimeout`, worker de reconciliação GSI5, presign real, rota HTTP de `reserveFiles()`, e o gate `fileSetSealed` em `commitUpload()`, todos implementados e testados. Único item fora de escopo do arco inteiro (nomeado desde D-163, nunca escondido): o worker S3/GuardDuty real que chamaria `applyFileScanResult()`/`confirmFileScanClean()` em produção — evento de infraestrutura física, sessão dedicada futura, não bloqueia o resto do roadmap.
   - **Item 8 da macro-ordem de D-161 (Document Types configuráveis) — `APPROVED (design)` (D-173, 2026-09-02).** Protocolo Claude↔Codex completo, **5 rodadas reais** contra o `codex exec` de verdade: Rodada 1 → Codex 6,4/10; Rodada 2 → régua 7,8/design 8,4; Rodada 3 (depois do reset de cota que bloqueou a sessão anterior, confirmado real via `codex exec "hello"` em foreground antes de prosseguir) → régua 9,2/10 (**fechada, estável**)/design 8,5; Rodada 4 → design 8,6 (corrigiu linguagem do schema HTTP + migrou CRUD do catálogo para `TenantBusinessMutation`, mas errou a identidade da 4ª entrada de `submitEvidence()`); Rodada 5 → design **9,4/10** (corrigiu por leitura direta do código real: a 4ª entrada é `Put(IdempotencyRecord)`, não `DocumentFile`, e o guest flow não ganha mapeamento posicional novo — o `catch` existente já colapsa por anti-enumeração). **Gate ≥9,0/≥9,0 atingido, sem arredondar.** Decisão: catálogo `DocumentType` tenant-scoped (PK `TENANT#t#DOCTYPE#id`, `documentTypeId` ULID opaco imutável, `displayName` renomeável, soft-state `ACTIVE`/`DEPRECATED`, dedupe via pointer transacional); os 4 writers de CRUD do catálogo e `createDocument()` migram para a lane `TenantBusinessMutation`; `submitEvidence()` (guest, já na lane) ganha só o `ConditionCheck` novo na posição `[0]`; `Document.documentType`→`documentTypeId`, GSI2 migra. Ver `docs/architecture/reviews/document-type-scoping/estado-final-consolidado.md` (design completo) e `decisions-log.md` D-173. **Itens 1-2 (domain entity + CRUD do catálogo na lane `executeTenantBusinessMutation`) implementados em D-174 (2026-09-02)** — ver `decisions-log.md` D-174. **Item 3 parcial em D-175 (2026-09-02)**: `createDocument()` migrado para `executeTenantBusinessMutation` com o `ConditionCheck(DocumentType.status=ACTIVE)` novo na posição `[0]` (`DocumentTypeNotActiveError` nova, category CONFLICT) — feito, testado (G-V3), verde em toda a suíte. `submitEvidence()` (guest) **NÃO migrado** — achado real: `SubmitEvidenceInput.documentType` é opcional no schema guest e cai para `requirementId` quando ausente numa rota já viva; aplicar o `ConditionCheck` incondicional (como D-173 §4 descreve) quebraria toda submissão guest sem `documentType` explícito. **Decisão de Marcelo sobre `submitEvidence()`/guest continua ABERTA, não resolvida por esta sessão** (ver `decisions-log.md` D-175 para o achado completo): escolher entre (a) tornar `documentTypeId` obrigatório no schema guest agora, adiantando parte do item 6; (b) aplicar o `ConditionCheck` só quando `documentType` vier explícito no input (guarda condicional não descrita no design original — precisaria voltar ao protocolo Claude↔Codex se tratado como decisão Type 1); (c) manter `submitEvidence()` como está até o item 6 formal ser implementado, e fechar o item 3 como "parcial, aceito assim" nesta forma. **Item 4 (`Document.documentType`→`documentTypeId`, GSI2 migra) IMPLEMENTADO — D-176, 2026-09-02** (ver `decisions-log.md` D-176) — rename ponta a ponta no caminho autenticado, guest flow's comportamento/schema intocados, exatamente a fronteira que D-175 deixou aberta. **Item 5 (rotas HTTP `POST/GET/PATCH /document-archive/document-types*` para o CRUD do catálogo) IMPLEMENTADO — D-177, 2026-09-02** (ver `decisions-log.md` D-177) — as 4 operações de CRUD (D-174) + `listDocumentTypes()` novo, todas alcançáveis via HTTP real, RBAC/schema validation confirmados por teste (G-V3). **Decisão de Marcelo sobre `submitEvidence()`/guest — RESOLVIDA, D-184 (2026-09-02), via protocolo Claude↔Codex completo pedido explicitamente por Marcelo (3 rodadas reais, E-014 9,3/design 9,5, `APPROVED`)**: opção (b) modificada — o `ConditionCheck(DocumentType.status=ACTIVE)` só se aplica quando `input.documentType !== undefined` (presença explícita, não truthy); ausente permanece byte-idêntico ao comportamento anterior (fallback pra `requirementId`, sem validação). Opção (a) rejeitada — nem `DocumentRequest` nem `Requirement` referenciam um `DocumentType` hoje, sem mecanismo de descoberta para o guest saber o id certo. Achado real do Codex na Rodada 2 (replay idempotente ignora a validação nova) mantido deliberadamente — propriedade preexistente do `idempotencyKey`, documentada e testada, não um gap novo. Ver `docs/architecture/reviews/submit-evidence-document-type-scoping/estado-final-consolidado.md` e `decisions-log.md` D-184. **Único item restante do arco D-173, agora não-bloqueado, mas ainda não iniciado**: item 6 (schema HTTP guest obrigatório + rota pública de leitura de tipos) — pode ser retomado a qualquer momento, sem decisão pendente.
2. **MaintenanceDueIndex (D-179 `APPROVED (design)`, 2026-09-02) — implementação incremental worker-a-worker, EM ANDAMENTO, 7 de 9 migrados.** Design completo em `docs/architecture/reviews/maintenance-due-index-scoping/estado-final-consolidado.md`. GSI8 real (`GSI8PK`/`GSI8SK`, `KEYS_ONLY`) já existe na tabela `dev` (`describe-table` confirma). Migrados e VERIFICADOS AO VIVO, cada um com prova real de isolamento IAM cross-namespace (`aws iam simulate-principal-policy`, positivo+negativo): **`membership-purge`** (D-180, piloto — achou e corrigiu em D-181 um bug real de deploy, `Sid` não-alfanumérico rejeitado pelo IAM, pego pelo CD real depois do CI verde); **`invitation-purge`** (D-182 — nuance real: ponteiro PENDING gravado NA CRIAÇÃO, não numa transição posterior, já que `expiresAt` é conhecido desde o início); **`document-file-reconciliation`** (D-183 — achado real: o mecanismo GSI5 anterior deste worker, D-164/D-166, nunca teve um writer real, código morto funcional apesar de "IMPLEMENTADO E VERIFICADO"; GSI8 substituiu por completo); **`requirement-reindex`** (D-185 — confirmou que o worker só faz a drift SATISFIED→NOT_SATISFIED via `evidenceValidUntil`, nunca uma reindexação de applicability mais ampla); **`quota-telemetry-purge`** (D-186 — dois tipos de entidade sem `version`, condicionado em `resetAt` exato; achou e corrigiu, só neste script, o bug do guard `main()` no Windows/Git Bash); **`security-audit-purge`** (D-187 — quatro tipos de entidade append-only, ponteiro GSI8 centralizado nas 4 funções `build*Event()` em vez de cada call site); **`transient-purge`** (D-188 — dois tipos de entidade com dinâmicas DIFERENTES no mesmo worker, `WebhookInbox` create-once e `UploadSlot` multi-transição; achou e corrigiu um erro real de fronteira `shared→modules` no novo arquivo compartilhado, pego por `test/architecture/tenant-fence-boundary.test.ts` na primeira rodada de testes). Suíte 1894/1894 (176 arquivos) ao final da sétima fatia, todos os gates verdes, `terraform test` 22/22, tudo mergeado em `main` e aplicado em `dev`. **Pendente real, nomeado explicitamente**: (a) migrar os 2 workers restantes (`delivery-record-purge`, `core-user-data-purge` — ordem não normativa, qualquer um serve), cada fatia reusando o mesmo mecanismo já validado 7x E o mesmo cuidado de checar o CD real (não só CI) antes de declarar uma fatia concluída — achado de processo real em D-181: uma mensagem externa alegando "CD completou com sucesso" foi corretamente rejeitada por checagem direta via `gh run view`, que mostrou `failure`; nunca confiar numa alegação de CD sem verificar; (b) aplicar o fix de uma linha do guard `main()` (`fileURLToPath(import.meta.url) === process.argv[1]`, não a comparação de string bruta que nunca casa no Windows) nos 6 scripts irmãos ainda afetados (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) e reverificar se os backfills de D-180/D-182/D-183/D-185 realmente escreveram os ponteiros GSI8 esperados contra `dev`.
3. **Wave 1b (Design System)** — quais componentes com overlay/focus-trap abordar primeiro, se for a hora. **Deliberadamente por último**, por pedido explícito de Marcelo (2026-09-01).
4. ~~D-D da otimização de hot path~~ — **RESOLVIDO, D-172, 2026-09-02** (ver acima).
5. Ou uma nova frente que Marcelo trouxer.

Itens abaixo são de sessões anteriores, todos já resolvidos ou explicitamente adiados por Marcelo — não são trabalho pendente, mantidos só como referência rápida caso o assunto volte: Wave B2B-14 (roteiro manual convite/2ª conta, adiado), execução destrutiva de `scripts/reset-dev-data.ts --confirm` (aguarda sinal dele), `AppError.retryable` (já fechado, D-128).

## Leitura obrigatória antes da próxima ação

`AGENTS.md` §2 (início de sessão) → `docs/architecture/README.md` (mapa vigente) → a linha da tabela "Gates / bloqueios abertos" relevante à tarefa escolhida, e só então o documento/pasta de review que ela referencia.

## Status de evidência (não presumir E2E sem checar)

| Item | Status |
|---|---|
| M7 extração/OCR | `E2E PROVEN` (verificação real 2026-08-27 contra `dev`) |
| Full BFF + Frontend Production Foundation | `APPROVED` + implementado, `E2E PROVEN` |
| W3-06 (`USER_DOCUMENT` purge) | `IMPLEMENTED`/`E2E PROVEN` (`terraform plan`/`test` reais contra `dev`) |
| W3-07 purge pipeline (D-081-083, emendado B2B-9/D-104) | `IMPLEMENTED`/`UNIT TESTED` — entidades B2B incluídas, sem orquestrador wireado, sem teste de integração AWS real |
| E-011 correlationId↔X-Ray | `E2E PROVEN` (smoke test real 2026-08-29 contra `dev`) |
| Visual Language + Design System | `APPROVED ... PROVISIONAL PENDING USER VALIDATION` |
| B2B-10 Tenant-aware Frontend (D-105/D-106) | `IMPLEMENTED`/`UNIT+COMPONENT+E2E TESTED` (139/139 vitest, 24/24 Playwright, mutação verificada em 3 pontos) — os 5 specs E2E mockavam `GET /bff/session` com a forma antiga (`tenantId`/`userId`), achado só pelo CI após o push inicial, corrigido; sem E2E dedicado a multi-organização/switcher ainda (fica para B2B-13) |
| B2B-11 Responsibility + Notifications (D-107/D-108) | `IMPLEMENTED`/`UNIT TESTED` (1210/1210 backend, mutação verificada em 2 pontos) — sem verificação E2E real contra `dev` (fica para B2B-14, Operational Evidence) |
| B2B-12 Cutover de dev (D-110/D-111) | `IMPLEMENTED`/`UNIT TESTED` (1232/1232 backend + 139/139 vitest + 24/24 Playwright frontend, mutação verificada em 3 pontos) — Fase A (read-only) do script **`E2E PROVEN`** (rodada de fato contra `dev` real via `aws --profile claude-dev`, manifest gravado com sucesso); Fase B (`--confirm` real, destrutiva) nunca executada — aguarda confirmação explícita do Marcelo |
| B2B-13 E2E/Adversarial Security (D-112/D-113) | `IMPLEMENTED`/`UNIT TESTED` (1235/1235 backend, mutação verificada em 4 pontos) — auditoria contra as 25 perguntas de §121 completa (matriz Q→arquivo:linha em `multi-user-b2b-wave-b2b13-scope.md`); sem verificação E2E real contra `dev` (fica para B2B-14, Operational Evidence) |
| B2B-14 Operational Evidence (D-114 a D-120) | `EM ANDAMENTO` — 7 achados reais corrigidos e mergeados (D-120 é o mais recente, PR #121, CD `success` confirmado); login OAuth, onboarding, GSI4/rotas de organização, save de Settings, listagem de organizações/membros — todos exercitados de verdade em `dev` (Settings/organizações/membros via cookie de sessão real do Marcelo). Roteiro manual de convite/aceite com 2ª conta/troca de role/switch/revogação/exclusão de organização **adiado pelo Marcelo para uma sessão futura**, não bloqueante |
| W3-07 orquestrador de purga (D-121 design, D-124 implementação) | `IMPLEMENTED`/`DEPLOYED` (1267/1267 backend, 156/156 vitest frontend, `terraform test` 19/19 raiz + 9/9 módulo novo + 1/1 api-gateway + 7/7 bff-api-gateway; mutação verificada e revertida em 3 pontos; mergeado em `main` PR #122, CD `success` 2026-08-31, state machine + sweeper `ENABLED` confirmados ao vivo contra `dev`) — **ainda não `E2E PROVEN`**: nenhuma purga real de tenant executou de ponta a ponta ainda |
| Reatribuição de responsabilidade (D-122 design, D-125 implementação) | `IMPLEMENTED`/`UNIT TESTED` (1249/1249 backend, 156/156 vitest frontend, mutação verificada e revertida em 3 pontos) — precondição best-effort real em `RemoveMembershipService`/`LeaveOrganizationService`, porta `AssignedActiveItemsLookup` paginada a exaustão via GSI1; sem verificação E2E real contra `dev` ainda |
| Exportação de dados CSV (D-123 design, D-126 implementação) | `IMPLEMENTED`/`UNIT TESTED` (1277/1277 backend testes coletados, 1275 passando — 2 falhas pré-existentes não relacionadas, ver D-125/D-126; mutação verificada em 4 pontos) — `item:export`/`ADMIN_ROLES`, `ExpirationService.exportItems()` com orçamento decrescente de 2.000 itens entre 3 queries GSI1, guard de 4 MB em `buildExportCsv()`, `csv-export-writer.ts` (RFC4180+mitigação de fórmula), Lambda dedicada `export-handler`/rota `GET /items/export` real (`terraform test` limpo); sem verificação E2E real contra `dev` ainda |

## Material trazido pelo Marcelo, ainda não avaliado (organizado em 2026-08-31)

- **Domínio Documental** (6 documentos de planejamento funcional/wireframes) — movidos para `docs/frontend/`, ver a nova seção "Domínio Documental — proposta trazida pelo Marcelo" em `docs/frontend/README.md`. Não avaliado ainda, não passou pelo protocolo Claude↔Codex.
- **Estratégia de aquisição dos primeiros clientes** — `docs/project/first-customers-acquisition-strategy.md` (movido da raiz). Plano comercial, fora do escopo técnico deste arquivo — não avaliado por Claude.
- ~~Roadmap competitivo de funcionalidades (2026-09-01)~~ — **RECONCILIADO E ORDEM DEFINIDA, D-161, 2026-09-01** (protocolo Claude↔Codex completo, 3 rodadas, Codex 9,6/10). `docs/project/roadmap-competitivo-2026-09-01.md` continua o documento-fonte (preço/limites das seções 12-13 seguem não avaliados, decisão comercial pura); a reconciliação técnica + macro-ordem + escalonamento de 4 decisões fundacionais está em `docs/architecture/reviews/competitive-roadmap-reconciliation/estado-final-consolidado.md`. Ver item 1 de "Próxima ação" acima.
- ~~Auditoria de Performance (2026-09-02)~~ — **RECONCILIADA, D-170, 2026-09-02** (investigação factual, sem protocolo Claude↔Codex — fact-finding, mesmo tratamento que D-161 teve antes de virar decisão). `docs/project/performance-audit-2026-09-02.md` continua o documento-fonte; a reconciliação linha-a-linha contra o código real está em `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md`. Resumo do confirmado: `RequestContext` caro (confirmado — `onboarding.resolve()` hidrata todas as Memberships via GSI4 incondicionalmente, mesmo com `organizationIdHint`, gap já conhecido e adiado em D-136/D-C); quota `API_REQUEST` fazendo `Get`+`TransactWriteItems` por request — **RESOLVIDO, D-172, 2026-09-02**: lane `EphemeralTelemetryMutation` real implementada (`consumeEphemeralTelemetry()` em `quota.ts`, `UpdateItem` `ADD`-only via `IdentityStore.incrementTelemetryCounter`, sem `Get`/`TransactWriteItems`/`ConditionCheck` de lifecycle repetido), D-154 (`QuotaTelemetryPurgeWorker`) alargado para cobrir o novo `entityType` como garantia de remoção real além do TTL best-effort, teste adversarial obrigatório do DoD confirmado ao vivo (residual pós-DELETING nunca lido como entitlement, cobertura estrutural provada). Deferido, não fabricado: alarme/métrica operacional de residual sobrevivente (infra-level, sessão futura). Reminder Producer/Dispatch com loops sequenciais e N+1 (confirmados, sem design prévio); Capacity Model desatualizado (confirmado). **Achado mais grave, além do que o documento externo supôs**: o padrão `Scan`+`Limit=100`+`MAX_PAGES=25` sem cursor persistido entre invocações agendadas aparece em 9 dos 10 workers de manutenção/purga (incluindo `membership-purge` D-169 e `document-file-reconciliation` D-166, implementados NESTA sessão) — starvation estrutural real, não hipotética, e afeta correção de retenção LGPD já tratada como fechada. Só `document-purge` (D-061, `Query` sobre GSI6 por data) está livre do defeito — é o precedente real para a solução `MaintenanceDueIndex`. **Reminder Dispatch — CONCLUÍDO, D-171, 2026-09-02**: os 3 achados mecânicos (N+1 `queryByItem`+`.find()` trocado por `GetItem` direto; `Get ExpirationItem`/`Get ReminderPolicy` paralelizados via `Promise.all`; loop sequencial do batch SQS trocado por `mapWithConcurrency` — helper novo, `src/shared/concurrency/map-with-concurrency.ts`, concorrência 5 — preservando `batchItemFailures` por item) implementados direto, sem protocolo. Suíte completa 1726/1726, todos os gates limpos. Detalhe completo em D-171 (`docs/architecture/decisions-log.md`).

**Próxima ação real**: D-D de D-136 **fechado (D-172, 2026-09-02)**. Mapear os consumidores de `onboarding.resolve()` para fechar D-C segue pendente; Reminder Producer (loop triplo sequencial + claim/outbox sequencial por ocorrência) segue pendente, não coberto por D-171; depois abrir Rodada 1 Claude↔Codex para o Scan-based starvation primeiro entre os itens nível 5-6 (mais urgente que RequestContext fast path e Reminder Producer v2).

**Bulk Import — Documents + Requirements + mapeamento de coluna (P0.2) — DESIGN `APPROVED` (D-192, 2026-09-03), IMPLEMENTAÇÃO EM ANDAMENTO, primeira fatia entregue nesta sessão**: protocolo Claude↔Codex completo (5 rodadas, Claude 9,4/Codex 9,1, ambos ≥9,0), ver `docs/architecture/reviews/bulk-import-documents-requirements-scoping/estado-final-consolidado.md`. **Fatia 1 (`TrackedSubject.externalId` + `SubjectExternalIdPointer`) CONCLUÍDA**: `externalId?: string` novo em `TrackedSubject` (create-only, sem path de update), `SubjectExternalIdPointer` (`domain/tracked-subject.ts`) gravado na MESMA `TransactWriteItems` de `createSubject()`, `SubjectExternalIdConflictError` novo (`shared/errors/app-error.ts`), `SubjectService.getSubjectByExternalId()` novo (lookup que as fatias de import de Document/Requirement vão usar para resolver `subjectRefKind="EXTERNAL_ID"`). G-V3 real: teste adversarial de duas criações concorrentes disputando o mesmo `externalId` no mesmo tenant — uma vence, a outra recebe `SubjectExternalIdConflictError`, nunca duas `TrackedSubject` com o mesmo `externalId` persistidas. Gate local completo verde (typecheck/lint/test/check-docs/validate-schemas/build:lambdas); `terraform test` deliberadamente NÃO rodado (sem mudança de infra nesta fatia). **Fatia 2 (fence de Subject em `createDocument()` + `buildCreateDocumentEntries`) CONCLUÍDA (2026-09-03)**: (A) `createDocument()` (`document-archive-service.ts`) ganhou o `ConditionCheck(TrackedSubject: attribute_exists(PK) AND status=ACTIVE)` que faltava — gap pré-existente `DA-SUBJECT-FENCE-01` (§5 do design), corrigido pela via `{entries,labels}`/`throwClassifiedCancellation` já usada por `createRequirement()`/`applyTemplate()` (dois `TransactEntryLabel` novos, `DOCUMENT_TYPE_FENCE`/`DOCUMENT`, sem quebrar a classificação existente); `SubjectPreconditionFailedError` (já existente, criado para `createRequirement()`) reusado tal qual — nenhuma classe de erro nova precisou ser criada. G-V3 real: 3 testes adversariais novos em `document-archive-service.test.ts` — Subject inexistente e Subject ARCHIVED ambos rejeitam com `SubjectPreconditionFailedError`, Subject ACTIVE continua criando normalmente (guarda de regressão); todas as fixtures pré-existentes do arquivo (`makeService()` default + os 2 stores customizados) ganharam `seedActiveTrackedSubject(TENANT, "s1"/"subject-1")` para não quebrar em massa. (B) `buildCreateDocumentEntries()` novo, exportado de `document-archive-service.ts` — planejador PURO (sem I/O) que constrói `{document, entries, labels}` (DocumentType fence + Subject fence + Put(Document), ordem fixa), reusado pelo próprio `createDocument()` (zero mudança de contrato HTTP) e preparado para o commit worker de import reusar depois — mas **AINDA NÃO conectado ao módulo `import/`** (decisão deliberada: por design §4, a resolução de `subjectRef`/`documentTypeRef` — incluindo `subjectRefKind="EXTERNAL_ID"` via `getSubjectByExternalId()` da fatia 1 — é um `BatchGetItem` em duas fases feito PELO módulo de import antes de chamar o planejador, nunca uma leitura por linha dentro dele; `document-archive` também não pode importar `subject/**` por `.dependency-cruiser.cjs`, então um lookup de externalId nunca poderia viver dentro desta função de qualquer forma). 3 testes novos isolados provam determinismo/pureza/ordem das entries. Gate local completo verde (typecheck/lint/test — 1974/1974/check-docs/validate-schemas/build:lambdas); `terraform test` deliberadamente NÃO rodado (sem mudança de infra nesta fatia). **Fatia 3 (`buildCreateRequirementEntries`, irmã de `buildCreateDocumentEntries`) CONCLUÍDA (2026-09-03)**: `buildCreateRequirementEntries()` novo, exportado de `document-archive-service.ts` — mesmo padrão da fatia 2: planejador PURO (sem I/O) que recebe `subjectId` já resolvido (nenhum lookup de externalId dentro dele) e devolve `{requirement, entries, labels}` (Put(Requirement) + Put(RequirementNamePointer) + Subject fence, ordem fixa — a MESMA ordem que `createRequirement()` já usava, ao contrário da ordem "fence primeiro" do Document). Extraído do corpo pré-existente de `createRequirement()` — que agora delega ao planejador com zero mudança de contrato — junto com 2 helpers module-level novos (`buildRequirementNamePointer`/`buildSubjectFence`) que o planejador reusa; os 3 métodos privados homônimos da classe (usados por `updateRequirement`/`applyTemplate`, que ainda constroem a partir de `this`) foram mantidos intactos, não deduplicados nesta fatia. A suite `createRequirement` pré-existente (name-conflict → `RequirementNameConflictError`, subject-fence-failure → `SubjectPreconditionFailedError`) passou SEM MODIFICAÇÃO como guarda de regressão. 5 testes novos isolados no planejador (derivação de status MISSING/NOT_APPLICABLE, omissão de `notes`/GSI8 quando ausentes, ordem/forma das 3 entries, determinismo). Gate local completo verde (typecheck/lint/test — 1979/1979/check-docs/validate-schemas/build:lambdas); `terraform test` deliberadamente NÃO rodado (sem mudança de infra nesta fatia). Commit `410db16`. **Fatia 4 (state machine `AWAITING_MAPPING` + `ColumnMapping` + claim OCC) CONCLUÍDA (2026-09-03)**: domain/state-machine puro, per design §3/§2 — nada de HTTP `/schema`/`/mapping`, outbox/SQS ou planejadores das fatias 2/3 conectados ainda (fora de escopo explícito desta fatia). (A) `import-job.ts`: `ImportJobStatus` ganhou `AWAITING_MAPPING` (entre `UPLOADED` e `PARSING`); `ImportTargetEntityType` estendido para `"TrackedSubject" | "Document" | "Requirement"`; `ColumnMapping` (união discriminada real, 3 variantes por `targetKind`, `schemaVersion: 1`) substitui o `mappingVersion: number` morto — `ImportJob.columnMapping?`/`columnMappingSha256?` agora opcionais (ausentes enquanto o job aguarda mapeamento); `DEFAULT_TRACKED_SUBJECT_COLUMN_MAPPING` novo (mapeamento fixo v1, preenchido por `reserveImport()` na criação — garante backward-compat: um job `TrackedSubject` NUNCA passa por `AWAITING_MAPPING`, vai direto `UPLOADED`->`PARSING` como hoje). `buildImportJobClaim()` novo — o claim OCC atômico da transição de status (`buildVersionedUpdate` + `extraConditions` checando `status = fromStatus`, reusável por qualquer transição futura, não só esta). (B) `import-parse-service.ts`'s `parseImportJob()` (o trigger S3, única função que hoje conduz a state machine): a claim ANTES não era OCC-safe de verdade (`store.update()` fazia um `Put` incondicional para `PARSING` — bug real pré-existente, corrigido aqui) — agora a PRIMEIRA mutação é sempre `buildImportJobClaim()` via `transactWrite`: sem `columnMapping` e `status=UPLOADED` -> claim para `AWAITING_MAPPING` (novo outcome `{kind:"AWAITING_MAPPING"}`, nunca lê S3); já `AWAITING_MAPPING` sem mapeamento -> no-op (aguarda `POST /mapping`, fatia futura); com `columnMapping` presente (de `UPLOADED` OU `AWAITING_MAPPING`) -> claim para `PARSING`, segue o parse normal a partir de um `claimedJob` local (nunca do `job` pré-claim). Claim perdido (`TransactionCanceledException`) -> novo outcome `{kind:"SKIPPED_ALREADY_CLAIMED"}`, nunca lança. `ImportService.reserveImport()` popula `columnMapping`/`columnMappingSha256` com o mapeamento fixo (SHA-256 via `JSON.stringify` simples — `canonicalJsonStringify()` de chaves ordenadas, §8 do design, fica para a fatia do contrato de mapping/schema, registrado como pré-requisito, não implementado aqui). G-V3 real (`import-parse-service.test.ts`): duas chamadas concorrentes de `parseImportJob` sobre o MESMO job pré-claim (`Promise.all`) — exatamente uma reporta `AWAITING_MAPPING`, a outra `SKIPPED_ALREADY_CLAIMED`, `version` avança exatamente uma vez (nunca duas); mais um teste direto no `buildImportJobClaim()` provando que uma segunda tentativa com `expectedVersion` obsoleto é rejeitada por `TransactionCanceledException` mesmo após a primeira já ter avançado a row. Regressão: todos os testes `TrackedSubject` pré-existentes passaram INALTERADOS (fixtures só trocaram `mappingVersion: 1` por `columnMapping: DEFAULT_TRACKED_SUBJECT_COLUMN_MAPPING`), provando que o caminho hoje em produção (Subject-only) nunca vê `AWAITING_MAPPING`. `ReserveImportInput`/schema HTTP `reserve-import-request.v1.json` deliberadamente INTOCADOS (só `TrackedSubject` é alcançável via HTTP nesta fatia — jobs `Document`/`Requirement` só existem construídos diretamente em teste, provando a forma do estado sem abrir superfície HTTP morta). Gate local completo verde (typecheck/lint/test — 1984/1984/check-docs/validate-schemas/build:lambdas); `terraform test` deliberadamente NÃO rodado (sem mudança de infra nesta fatia). **Fatia 5 (resolução batched de referência de Subject, `resolveSubjectReferences()`) CONCLUÍDA (2026-09-03)**: por ordem de fases do design (§4, "Resolução de referência"), a METADE de Subject do mecanismo batched de duas fases de `BatchGetItem` — a metade de DocumentType (`documentTypeNamePointerKey`) fica deliberadamente para a fatia seguinte, per a própria instrução desta fatia de preferir terminar uma metade limpa a fazer as duas pela metade. (A) `SubjectStore.batchGet<T>(keys: EntityKey[]): Promise<T[]>` novo no port (`subject/ports/subject-store.ts`) — nenhum `BatchGetItem` existia em lugar nenhum do codebase antes desta fatia. `DynamoDbSubjectStore.batchGet()` (`subject/persistence/dynamodb-subject-store.ts`) implementa em chunks de 100 chaves (limite real do SDK) com retry de `UnprocessedKeys` até esvaziar cada chunk — não é uma falha de rede, é um 200 OK parcial, então o retry é explícito, não coberto por `maxAttempts` do client. `InMemorySubjectStore.batchGet()` (`test/unit/subject/in-memory-store.ts`) é o fake correspondente, com `batchGetCallCount`/`batchGetKeyCount` expostos para os testes provarem que o CHAMADOR dedupou antes de bater no store, não que o fake dedupou por conta própria. Dois `SubjectStore` mockados à mão em testes pré-existentes (`submission-finalizer.test.ts`/`submission-malware-result.test.ts`) ganharam `batchGet: store.batchGet.bind(store)` só para satisfazer a interface — nenhum comportamento deles mudou. (B) `resolveSubjectReferences()` novo (`import/application/resolve-subject-references.ts`) — função batched de duas fases: fase 1 (só quando `refKind="EXTERNAL_ID"`) dedupa os valores brutos do batch inteiro num `Set`, um único `batchGet` de `SubjectExternalIdPointer` -> `subjectId`; fase 2 dedupa os `subjectId`s resultantes (dedup De NOVO — dois `externalId` diferentes podem apontar pro mesmo Subject, e linhas `SUBJECT_ID`-kind podem repetir o mesmo id), um único `batchGet` de `TrackedSubject`, `status` confirmado NA HORA (nunca herdado do pointer, que não carrega status). `refKind="SUBJECT_ID"` pula a fase 1 inteira (o valor bruto já É o id a resolver na fase 2). Referência inexistente OU resolvendo a Subject não-`ACTIVE` (`ARCHIVED`/`DELETED`) -> `{kind:"NOT_FOUND"}`, o MESMO motivo do design (§4: "referência não resolvida -> REJECT reason=SUBJECT_REFERENCE_NOT_FOUND" cobre as duas causas por igual, espelhando o fence enumerado `status=ACTIVE` que `buildSubjectFence()`/`createDocument()` já usam, nunca `<> DELETED`). Devolve um `Map` indexado pelo VALOR BRUTO da referência (não pelo `subjectId`), para o chamador futuro (a fatia que vai de fato ler linhas de Document/Requirement) olhar o resultado por linha sem re-derivar nada. **Deliberadamente NÃO conectado a `import-parse-service.ts`** — o loop de parse hoje é inteiramente TrackedSubject-shaped (colunas `displayName`/`type`/`externalId` fixas, nenhuma leitura de `ColumnMapping` para Document/Requirement ainda); conectar este resolver ao loop está empacotado com o resolver de DocumentType na fatia seguinte, para o loop só precisar ramificar em `targetEntityType` uma vez, não em duas fatias separadas. G-V3 real (`test/unit/import/resolve-subject-references.test.ts`, 7 testes): duas/três ocorrências do MESMO `externalId` no batch resolvem UMA vez só (`batchGetCallCount`/`batchGetKeyCount` provam a dedup, não só o resultado); `externalId` inexistente -> `NOT_FOUND` sem lançar; Subject `ARCHIVED` -> `NOT_FOUND`; Subject `DELETED` -> `NOT_FOUND`; `refKind="SUBJECT_ID"` resolve direto, pulando a fase 1; batch vazio não chama `batchGet` nenhuma vez; batch misto (um resolve, um não) resolve cada referência independentemente. Regressão: `import-parse-service.ts`/`import-row.ts` totalmente INTOCADOS nesta fatia — a suite `TrackedSubject` pré-existente passa sem qualquer mudança de fixture, prova por construção (não só por teste) que o caminho hoje em produção é inatingível por este código novo. Gate local completo verde (typecheck/lint/test — 1991/1991/check-docs/validate-schemas/build:lambdas); `terraform test` deliberadamente NÃO rodado (sem mudança de infra nesta fatia). **Próxima fatia (6)**: a metade de DocumentType do mesmo mecanismo (`documentTypeNamePointerKey`/D-173, `documentTypeRefKind="DISPLAY_NAME"|"DOCUMENT_TYPE_ID"`, mesmo desenho de duas fases/dedup/status-`ACTIVE`-na-hora), MAIS finalmente conectar as duas resolvers (`resolveSubjectReferences()` desta fatia + a nova de DocumentType) ao loop de `parseImportJob()` para jobs `targetEntityType="Document"|"Requirement"` — o que por sua vez exige estender `import-row.ts`/o loop para ler `ColumnMapping` (não mais colunas fixas) e produzir `ImportRowPlanEntry` variantes com `subjectId`/`documentTypeId` já CONGELADOS. Isso é o pré-requisito direto para a fatia seguinte (7) poder finalmente CHAMAR `buildCreateDocumentEntries`/`buildCreateRequirementEntries` (fatias 2/3) a partir do commit worker (design §6) — ainda não é essa fatia 7 em si (essa exige o `ImportRowOutcome`/fallback de linha do §6, não decidido ainda) nem a orquestração completa `GET /schema`/`POST /mapping` (§3, HTTP) que consome o `AWAITING_MAPPING` da fatia 4 pela outra ponta.

**Próxima fatia real, não iniciada**: extensão da state machine do `ImportJob` — estado `AWAITING_MAPPING` entre `UPLOADED` e `PARSING` com claim OCC atômico, tipo `ColumnMapping` (união discriminada por `targetKind`, §2 do design) substituindo o `mappingVersion: number` morto, `ImportTargetEntityType` ganhando `"Document" | "Requirement"` (hoje só `"TrackedSubject"`) — seguir o design consolidado linha a linha (§2/§3), não reabrir o protocolo salvo achado real durante a implementação. A fiação dos dois planejadores (`buildCreateDocumentEntries`/`buildCreateRequirementEntries`, ambos prontos) no commit worker de import, a resolução de referência batched externalId→subjectId (§4), o dispatch outbox/SQS, e os novos handlers HTTP (`/schema`, `/mapping`) continuam todos sem começar — fatias futuras separadas.

## Links para histórico (não reler por padrão — só sob demanda)

- `docs/architecture/session-log.md` — linha do tempo compacta, uma entrada por sessão.
- `docs/architecture/decisions-log.md` / `docs/engineering/decisions-log.md` — toda decisão com nota Claude/Codex e status (numeração D-0xx / E-0xx, não sequencial na ordem das linhas).
- `docs/architecture/reviews/` — artefatos de cada rodada Claude↔Codex por tema.
- `docs/project/handoffs/` — prompts de handoff de sessões anteriores, superseded por este arquivo, preservados como evidência.
- `docs/frontend/` — os 8 documentos de planejamento de interface + `interface-quality-standard.md` + `prototype/`.
