# Prompt de início — próxima sessão (gerado em 2026-09-01)

> Cole o texto abaixo (a partir de "Leia") como a primeira mensagem da próxima sessão sobre o `expiration-tracker`. Este arquivo é histórico/handoff (`AGENTS.md` §5) — o `NEXT_SESSION_PROMPT.md` é a fonte de estado real, este arquivo só empacota o prompt de arranque.

---

Leia `NEXT_SESSION_PROMPT.md` e `AGENTS.md` inteiro antes de qualquer coisa. Confirme `git branch --show-current` (deve ser `develop`), rode `git pull`, e confirme que `develop` está sincronizado com `main` (`git fetch origin main && git diff origin/main..develop --stat` deve dar vazio).

Padrão de trabalho: continue de forma totalmente autônoma enquanto houver qualquer trabalho de engenharia real e não-ambíguo a fazer. Nunca pare para perguntar "posso continuar?" — só pause quando o próximo passo depender genuinamente de uma decisão que só eu posso tomar (decisão de produto, ação destrutiva/irreversível, gasto real de infra novo, ou algo que o protocolo Claude↔Codex do `AGENTS.md` §4 exige elevar a mim). Nesses casos, registre o pendente claramente e siga para a próxima frente de trabalho independente disponível — nunca fique ocioso esperando resposta enquanto houver outra tarefa executável. Se precisar tomar uma decisão de arquitetura/segurança/dado (Type 1, nível 5-6 da escala de risco), inicie o protocolo Claude↔Codex completo (pesquisa externa quando aplicável, mínimo 3 rodadas, nota cega, gate ≥9.0 sem arredondar).

**Estado atual (ao final da sessão de 2026-09-01)**: Domínio Documental (D-143) fechado por completo — Núcleo 1 (Document/DocumentVersion) e Núcleo 2 (Requirement/D-145, guest access/D-146, recorrência/D-147) 100% implementados, testados e **deployados em `dev`**, confirmado ao vivo via `aws --profile claude-dev`. Na mesma sessão também foram fechados e deployados: Dependency Freshness Standard (D-139→D-148), Admin Activity/Audit Log (D-131→D-149), o mecanismo de quarentena `HELD_FOR_RECOVERY` (D-127→D-150), e 6 dos 7 workers de purga LGPD de D-127 (`CORE_USER_DATA`/D-151, `DELIVERY_RECORD`/D-152, `SECURITY_AUDIT`/D-153, `QUOTA_TELEMETRY`/D-154, `ACCOUNT_ACTIVE`-não-fechamento/D-155 parcial, `TRANSIENT` remanescente/D-156) — `LEGAL_EVIDENCE` (Prioridade 7) permanece corretamente bloqueada por trava jurídica. Achado de processo importante: `cd.yml` aplica automaticamente em todo push a `main` — não é preciso `terraform apply` manual após um merge normal, só confirmar ao vivo que o recurso apareceu.

**Não há mais nenhuma frente de trabalho "óbvia e segura" no backlog imediato.** Antes de inventar trabalho novo por conta própria, verifique com Marcelo qual destes itens abertos é prioridade (todos exigem decisão de produto/arquitetura ou carregam risco elevado nomeado, ver a tabela "Gates / bloqueios abertos" do `NEXT_SESSION_PROMPT.md` para o detalhe completo de cada um):

1. **PRs #152/#156 (Dependabot: ESLint 8.57→10.9)** — CI falhou de verdade (breaking change real de config, ESLint 10 exige flat config e `.eslintrc.cjs` é incompatível), não é o flake de lock do S3 visto em outros PRs. Decidir se migra agora ou adia.
2. **`Membership.removedAt` ausente** (achado de D-155) — sem esse campo não há relógio para destravar a purga LGPD de `Membership`/`Channel`. Mudança pequena mas toca write paths de produção existentes (`remove-membership.ts`/`leave-organization.ts`) — decisão antes de implementar.
3. **Wave 1b (Design System Implementation Gap)** — componentes restantes do catálogo (`Modal`, `Tooltip`, `Popover`, `DropdownMenu`, `Drawer`, `Combobox`, `DateInput`) todos exigem decisão de padrão overlay/focus-trap ainda não tomada.
4. **D-C/D-D da otimização de hot path** (`docs/architecture/reviews/performance-hotpath-scoping/`) — D-C precisa de decisão de arquitetura sobre `IdentityStore.ConsistentRead`; D-D (`EphemeralTelemetryMutation`) é nomeado como maior risco do design inteiro, precisa de sessão dedicada com teste adversarial obrigatório.
5. Ou uma nova frente que Marcelo trouxer.

Se ele não responder nada e a sessão precisar prosseguir sozinha mesmo assim, comece pelo item 1 (mais barato/mecânico de investigar) e registre explicitamente que prosseguiu sem confirmação explícita por falta de resposta, não por autorização.

## Padrões operacionais validados nesta sessão (repita-os)

- **Delegação para background agents**: para tarefas de implementação bem escopadas (uma entidade/worker/feature de cada vez), lance um agente em background com um brief completo e auto-contido (arquivos a ler primeiro, precedentes exatos a espelhar, o que NÃO fazer, testes G-V3 exigidos) e a instrução explícita "não delegue a outro sub-agente, faça o trabalho você mesmo com suas próprias ferramentas" — um agente já tentou delegar recursivamente e não fez trabalho real; a instrução explícita resolveu isso nas tentativas seguintes.
- **Nunca confie no auto-relato do agente sem verificar `git log`/`git status` reais** — já aconteceu um agente "relatar sucesso" sem ter commitado nada.
- **Revise pessoalmente qualquer diff que toque dado real ou pipeline crítico já deployado** (purga física de dados, `TenantLifecycleRecord`, RBAC) antes de mergear — não baste "CI verde". Nesta sessão isso incluiu ler os guards de delete condicional e a lógica de detecção de ambiguidade linha por linha antes de aprovar.
- **Rode a suíte completa de gates você mesmo antes de cada push** (`typecheck`, `lint --max-warnings=0`, `npm test`, `check-boundaries`, `check-docs`, `validate-schemas`, `check-dependency-freshness`, `build:lambdas`, mais `terraform fmt`/`validate`/`test` quando a mudança toca infra) — não confie só no CI remoto antes de decidir que está pronto para PR.
- **PRs sequenciais, nunca paralelos, quando tocam `decisions-log.md`/`NEXT_SESSION_PROMPT.md`** — evita conflito de merge e permite revisão real de cada peça antes da próxima começar.
- **CD aplica sozinho em todo push a `main`** — depois de mergear, confirme via `aws lambda get-function`/`aws apigatewayv2 get-routes`/`aws scheduler list-schedules --profile claude-dev` (ou equivalente) em vez de rodar `terraform apply` manualmente. Se o CD falhar por lock do S3 (`Error acquiring the state lock`) ou erro transitório da AWS (`Lambda ... unexpected state 'Failed'`), isso é um flake benigno conhecido desta sessão — re-rode (`gh run rerun <id> --failed`) em vez de investigar como se fosse um bug real.
- **Ao usar `EnterWorktree`**, lembre que isso trava TODAS as chamadas de shell da sessão (inclusive de agentes em background já rodando) ao caminho do worktree — nunca entre num worktree enquanto um agente em background ainda estiver trabalhando no diretório principal; prefira `git worktree add` isolado se realmente precisar rodar algo em paralelo (embora nesta sessão isso também tenha esbarrado no classificador de permissão automático — o caminho mais simples acabou sendo confirmar que o CD já aplica sozinho e evitar `terraform apply`/`init` manual quando possível).

## Leitura obrigatória antes da próxima ação

`AGENTS.md` §2 → `NEXT_SESSION_PROMPT.md` (estado atual, já atualizado) → a linha da tabela "Gates / bloqueios abertos" relevante à decisão que Marcelo indicar → só então o documento/pasta de review que ela referencia.
