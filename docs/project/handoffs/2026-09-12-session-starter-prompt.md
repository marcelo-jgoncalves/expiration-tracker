# Prompt de início — próxima sessão (gerado em 2026-09-12, atualizado)

> Cole o texto abaixo (a partir de "Retomando") como a primeira mensagem da próxima sessão sobre o `expiration-tracker`. Este arquivo é histórico/handoff (`AGENTS.md` §5) — `NEXT_SESSION_PROMPT.md` é a fonte de estado real, este arquivo só empacota o prompt de arranque no momento em que foi gerado (pode já estar desatualizado quando você o usar — confirme sempre contra `NEXT_SESSION_PROMPT.md`/`git`/CI antes de agir). Substitui a versão anterior do mesmo dia (estado mudou bastante desde então: auditoria externa registrada, relatório realocado, `NEXT_SESSION_PROMPT.md` recompactado).

---

Retomando expiration-tracker. Antes de qualquer coisa:

1. Rode `ListAgents` e `CronList` — pode haver fork/heartbeat ainda ativos de antes do clear.
2. Leia `NEXT_SESSION_PROMPT.md` (estado atual + próxima ação) e as últimas entradas de `docs/architecture/decisions-log.md` (D-268 em diante) para se recontextualizar — não confie em nada que eu disser sem verificar contra o código/git real primeiro.
3. Autonomia total já concedida (commit/push/merge `develop→main`, terraform em `develop`, decisões de engenharia via protocolo Claude↔Codex sem me esperar). **Padrão revisado nesta sessão**: default é **fork serial** (um agente por vez, herda o contexto da conversa), não orquestração paralela via `Workflow` — paralelizar estourou limite de sessão sem ganho líquido de token. Só paralelizar se eu pedir velocidade explicitamente. Ver `AGENTS.md` §1.
4. **Estado real da working tree em `develop`**: rascunho REAL, NÃO COMMITADO, NÃO TESTADO da `ExternalShareLink` slice 2/3 — `authorization.ts` (+12 linhas, 3 actions novas), `document-archive-handlers.ts` (+89 linhas, handlers em progresso), `schema-validator.ts` (+4 linhas), 2 schemas novos em `schemas/api/`. Leia antes de continuar, trate como não confiável. Faltava: terminar handlers, decidir wiring da rota anônima (`GET /external-share/{shareId}/{token}`), allowlist do BFF, terraform, testes, 1 rodada Codex, gate completo. Ver item 8 do backlog P1 em `NEXT_SESSION_PROMPT.md`.
5. **Próxima ação**: terminar `ExternalShareLink` slice 2/3 (último item do backlog P1).
6. **Depois disso, considerar em ordem de custo/benefício** (ver seção "Auditoria crítica externa" em `NEXT_SESSION_PROMPT.md` para o detalhe, documento completo em `docs/engineering/reviews/external-audit-2026-09-11-critica-repositorio.md`):
   - 3 achados de uma auditoria externa (~8,2/10, não é rodada Claude↔Codex) **já verificados diretamente contra código/GitHub real** nesta sessão: CI de `main` exige só o check `guardrails` (correção barata: required-status-check agregador); `ReminderPolicy` permite N por item enquanto frontend modela 1 (decisão de domínio pendente — 1:1 com fence vs. N explícito); IP bruto na PK do guest rate limiter (correção barata: HMAC).
   - Achados adicionais da mesma auditoria, citados mas **ainda não verificados linha a linha**: falta suíte "Real System E2E" contra `dev`; `RequestContext` caro no hot path; Reminder Producer não sustenta o SLO declarado; Capacity Model desatualizado; sem load testing; frontend sem code-splitting — verificar antes de agir sobre qualquer um deles.
   - Eixos do full-audit round2 com trabalho decisão-independente real: E-021 (dashboard/EMF ausente, fan-out sem cap), E-016 (filas desatualizadas do `reset-dev-data.ts`), E-017 (gate de evidência ponta-a-ponta, limite de profundidade de subagente).
7. **Workstream paralelo, não bloqueia engenharia**: identidade visual — Fase 1 (pesquisa) concluída por Marcelo fora desta sessão, 2 arquivos na raiz (`Relatorio_Fase_1_...md`, `Proximas_Tarefas_Identidade_Visual.md`) **deliberadamente fora do allowlist de `.md` da raiz** — nunca commitar sem mover para `docs/project/` primeiro (é exatamente o que fiz com o relatório de auditoria crítica nesta sessão — mesmo padrão). Fase 2 (3 territórios criativos) só se eu pedir.
8. Lições de processo desta sessão: nunca aceite autorrelato de subagente sem verificar `git log`/`gh run list`; se um fork parar dizendo só "ainda esperando em background", instrua a rodar em foreground; corte técnico de turno = retome na hora, rate-limit de sessão/Codex = só retome se o horário de reset já passou; nunca dois agentes escrevendo nos mesmos arquivos sem coordenação.
9. Continue de forma superautônoma: nunca fique ocioso, adie só decisão genuinamente minha (lista consolidada em `NEXT_SESSION_PROMPT.md`), use fork + heartbeat de cron para trabalho longo em background — 1 fork por vez.

Se a pipeline estiver rodando, observe e corrija problemas (contenção de lock do terraform entre CI/CD concorrentes é artefato conhecido, não bug real — `AGENTS.md` §7).
