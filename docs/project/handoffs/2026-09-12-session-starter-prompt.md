# Prompt de início — próxima sessão (gerado em 2026-09-12)

> Cole o texto abaixo (a partir de "Retomando") como a primeira mensagem da próxima sessão sobre o `expiration-tracker`. Este arquivo é histórico/handoff (`AGENTS.md` §5) — `NEXT_SESSION_PROMPT.md` é a fonte de estado real, este arquivo só empacota o prompt de arranque no momento em que foi gerado (pode já estar desatualizado quando você o usar — confirme sempre contra `NEXT_SESSION_PROMPT.md`/`git`/CI antes de agir).

---

Retomando expiration-tracker. Antes de qualquer coisa:

1. Rode `ListAgents` e `CronList` — pode haver fork/heartbeat ainda ativos de antes do clear (aconteceu nas últimas sessões).
2. Leia `NEXT_SESSION_PROMPT.md` (estado atual + próxima ação) e as últimas entradas de `docs/architecture/decisions-log.md` (D-268 em diante) para se recontextualizar — não confie em nada que eu disser sem verificar contra o código/git real primeiro.
3. Autonomia total já concedida (commit/push/merge `develop→main`, terraform em `develop`, decisões de engenharia via protocolo Claude↔Codex sem me esperar). **Padrão revisado nesta sessão**: default agora é **fork serial** (um agente por vez, herda o contexto da conversa), não orquestração paralela via `Workflow` — paralelizar consumiu tokens rápido demais e estourou limite de sessão sem ganho líquido; só usar `Workflow`/forks concorrentes se eu pedir velocidade explicitamente. Ver `AGENTS.md` §1.
4. Estado real ao terminar a sessão anterior: **working tree em `develop` tem rascunho REAL, NÃO COMMITADO, NÃO TESTADO** da `ExternalShareLink` slice 2/3 (retomada e parada no meio a meu pedido, não por bloqueio) — `authorization.ts` (+12 linhas, 3 actions novas), `document-archive-handlers.ts` (+89 linhas, handlers em progresso), `schema-validator.ts` (+4 linhas), 2 schemas novos em `schemas/api/`. Leia antes de continuar, trate como rascunho não confiável. Faltava: terminar handlers, decidir wiring da rota anônima (`GET /external-share/{shareId}/{token}` — reusar Lambda guest existente ou criar nova), allowlist do BFF, terraform, testes, 1 rodada Codex, gate completo. Ver item 8 do backlog P1 em `NEXT_SESSION_PROMPT.md` para o detalhe exato.
5. **Próxima ação**: terminar `ExternalShareLink` slice 2/3 (é o último item do backlog P1). Depois, o que for possível do restante: eixos decisão-independentes do full-audit round2 (E-021 dashboard/fan-out cap, E-016 filas desatualizadas em `reset-dev-data.ts`, E-017 gate de evidência/limite de subagente) — E-023 já está resolvido exceto por uma decisão minha pendente (`coverage.thresholds`), não decida isso por mim.
6. **Workstream paralelo, não bloqueia engenharia**: identidade visual — Fase 1 (pesquisa) concluída por mim fora desta sessão, 2 arquivos na raiz do repo (`Relatorio_Fase_1_...md`, `Proximas_Tarefas_Identidade_Visual.md`) **deliberadamente fora do allowlist de `.md` da raiz** — nunca commitar sem mover para `docs/project/` ou atualizar `ROOT_MD_ALLOWLIST` primeiro. Fase 2 (3 territórios criativos) é o próximo passo desse workstream, mas só se eu pedir — não é implícito.
7. Lições de processo desta sessão (já em `AGENTS.md`/memória, mas reforçando): nunca aceite autorrelato de subagente sem verificar `git log`/`gh run list` antes de encadear o próximo passo; se um fork parar dizendo só "ainda esperando em background", instrua a rodar em foreground em vez de fazer polling; corte técnico de turno = retome na hora, rate-limit de sessão/Codex = só retome se o horário de reset já passou (não force loop); nunca dois agentes escrevendo nos mesmos arquivos ao mesmo tempo sem coordenação.
8. Continue de forma superautônoma: nunca fique ocioso, adie só decisão genuinamente minha (lista consolidada em `NEXT_SESSION_PROMPT.md`), use fork + heartbeat de cron para trabalho longo em background — mas prefira 1 fork por vez (item 3 acima).

Além disso, se a pipeline estiver rodando, observe e corrija se houver problema (cuidado com contenção de lock do terraform entre CI/CD concorrentes — é artefato conhecido, não bug real, ver `AGENTS.md` §7).
