2026-08-28T20:21:41.060013Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a04a09-0b1a-7391-8c04-cd70d7e34d79
--------
user
Rodada 2 do protocolo Claude<->Codex para W3-07 (mecanismo de cascata de exclusao fisica por
tenantId). Rodada 1: nota 3,4/10, 8 achados bloqueantes (Scan sem convergencia, tenantId nao
universal, formas incompativeis com OCC generico, branch generico fail-open, colisao com GSI6
de outros workflows, falha de lote inteiro por 1 item conflitante, purgeAfter contradiz matriz
de 30 dias, COMPLETED sem semantica verificavel), arquivada em
docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/codex-round1-critique-full.txt.

Claude respondeu em duas partes, leia AMBAS na ordem:
1. docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md
   (resolve os 8 achados: remove reuso de GSI6 - delete direto condicionado sem fila; taxonomia
   fechada por entityType com fail-closed para tipo desconhecido; convergencia via re-Scan ate
   zero linhas ou maxPasses; fallback sequencial por item quando um lote de 25 falha; remove o
   conceito de purgeAfter deste mecanismo - exclusao e imediata, quem decide QUANDO chamar fica
   fora de escopo)
2. docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md
   (autocritica da propria Claude sobre a Rodada 2: a tabela de classificacao por entityType da
   Rodada 2 nao tinha sido verificada linha a linha contra o codigo real - Claude refez o
   levantamento exaustivo dos 39 entityType reais do sistema, corrigiu classificacoes erradas, e
   achou uma tabela fisica separada - bff-session-table - que nenhuma rodada anterior
   considerava).

Verifique contra o codigo real (grep de "entityType:" em src/modules, os arquivos de dominio
citados, infra/main.tf para a bff-session-table) se a Rodada 3 fechou os achados da Rodada 1 de
verdade, e se a nova tabela de classificacao dos 39 entityType esta correta agora (a Rodada 1
ja pegou erros aqui uma vez - verifique com ceticismo, nao aceite a alegacao de "verificado
exaustivamente" sem conferir pelo menos uma amostra voce mesmo). Ache qualquer entityType mal
classificado, qualquer efeito colateral que a exclusao "sem versao" possa ter (ex: TenantQuota
sendo apagado enquanto uma escrita concorrente de quota esta em andamento), e se a convergencia
via re-Scan realmente fecha o achado 1 da Rodada 1 ou so o move.

De uma nota de 0 a 10 (sem arredondar), gate 9.0. Formato: ## Achados bloqueantes / ## Achados
nao-bloqueantes / ## Nota: X/10 / ## Veredito.

codex
Vou fazer a revisão como protocolo formal: primeiro carrego o estado e os mapas exigidos pelo repositório, depois confronto as duas propostas e a crítica anterior com código, domínio e Terraform. Como é uma decisão Type 1, vou registrar apenas a avaliação desta rodada, sem alterar arquivos.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 177ms:
# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o

> Este arquivo Ã© estado atual + prÃ³xima aÃ§Ã£o (`AGENTS.md` Â§2), nÃ£o histÃ³rico. Para a linha do tempo completa por sessÃ£o, ver `docs/architecture/session-log.md`; para toda decisÃ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) â€” confirmar `git log`/branch atual antes de assumir que o PR jÃ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program â€” Waves 0/2/3/5 concluÃ­das, Wave 4 parcial (gated), Wave 1 nÃ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃ£o de consolidaÃ§Ã£o/pilot-readiness. **Backlog canÃ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃ­ntese final/recomendaÃ§Ã£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃ¡vel do prompt mestre Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) â€” nÃ£o duplicar detalhe aqui, sÃ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃ§Ã£o) `DONE`. **Wave 2 (evidÃªncia operacional) `DONE`** (2026-08-28) â€” os 6 drills (W2-03 a W2-08) executados com evidÃªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO â‰ˆ3min44s, RPO nÃ£o medido â€” item aberto), load test real (977 invocaÃ§Ãµes, cota `API_REQUEST` segurando sob carga), alarme forÃ§ado com notificaÃ§Ã£o SNSâ†’e-mail real confirmada. **RPO (W2-06), dedupe pÃ³s-commit (W2-05) e o pipeline de alarme do W2-08 fechados na mesma sessÃ£o (2026-08-28)** â€” ver `pilot-readiness-program.md` seÃ§Ã£o Wave 2 para o detalhe completo (inclui o achado real de que "credential-compromise via HTTP" Ã© estruturalmente inalcanÃ§Ã¡vel hoje, nÃ£o uma lacuna). Claims especÃ­ficas por drill: ver `docs/engineering/test-engineering-standard.md` Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada. **`W3-06-DECISION` fechada e implementada nesta sessÃ£o (2026-08-28)**: desenho do mecanismo de purga real de `USER_DOCUMENT` aprovado via protocolo Claudeâ†”Codex completo (6 rodadas, Codex 9,2/Claude 9,1, D-061 em `decisions-log.md`) e implementado de ponta a ponta â€” `DocumentPurgeWorker` real (claim/lease sobre GSI6, apaga o objeto S3 real via `cleanObject`/evidÃªncia â€” nunca `quarantineObject` â€” e a linha `Document`, grava `DocumentPurgeReceipt` nÃ£o sensÃ­vel), agendado a cada 6h, verificado com `terraform plan`/`terraform test` reais contra `dev` (ver `docs/architecture/reviews/w3-06-user-document-purge-design/`). Wave 4 (Identity/RBAC) `PARTIAL` â€” confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃ£o de negÃ³cio jÃ¡ registrada (`AGENTS.md` Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) â€” `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃ£o (2026-08-28) â€” auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard â€” novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados com fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa precisa da Wave 2. Protocolo Claudeâ†”Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃ­cito do Marcelo, acima do 9,0 padrÃ£o) â€” trajetÃ³ria Codex 6.35â†’7.85â†’8.70â†’9.18â†’9.34â†’9.46â†’9.48â†’**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃ§Ã£o D-0xx). EvidÃªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃ£o vale para qualquer teste/drill novo a partir de agora** â€” antes de declarar um teste ou drill "concluÃ­do", checar contra os gates de Â§3 (nunca reversÃ£o silenciosa, nunca claim mais ampla que a evidÃªncia).

**RecomendaÃ§Ã£o da sÃ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃªs gates reais nomeados na sÃ­ntese original â€” guest trust (`GTR-01`/`W5-01`), evidÃªncia operacional (Wave 2), e retenÃ§Ã£o/purga real (`W3-06`) â€” **fecharam todos nesta sessÃ£o**, e todos os itens secundÃ¡rios de Wave 2 (RPO, dedupe pÃ³s-commit, pipeline de alarme de credential-compromise) tambÃ©m fecharam na mesma sessÃ£o. Ãšnico item nÃ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).

954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃ§Ã£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃ§Ã£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃ£o** (W2-06 RPO, W2-05 dedupe pÃ³s-commit, W2-08 pipeline de alarme). **PrÃ³xima aÃ§Ã£o real**: W3-07 (cascata DSR completa â€” hoje sÃ³ flip de status por entidade, sem cascata nem purga fÃ­sica; pode reusar o mesmo mecanismo GSI6 claim/lease + `DocumentPurgeWorker` do W3-06) â€” feature de produto maior, nÃ£o bloqueia pilot readiness estreito, precisa do mesmo rigor de desenho (protocolo Claudeâ†”Codex, Type 1) antes de implementar.

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃ³rio e passou a ter a primeira linguagem visual real do produto â€” arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃ§Ã£o **Operational Calm** aplicada Ã s 5 superfÃ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION â€” PROVISIONAL PENDING USER VALIDATION` â€” a palavra PROVISIONAL Ã© literal: nenhuma sessÃ£o com usuÃ¡rio aconteceu, e as **15** hipÃ³teses de design (hierarquia de navegaÃ§Ã£o, densidade final, rÃ³tulos, colocaÃ§Ã£o de aÃ§Ãµes secundÃ¡rias, accent, e as duas afirmaÃ§Ãµes subjetivas sobre a prÃ³pria direÃ§Ã£o) estÃ£o registradas como adiamentos explÃ­citos no Â§35 do documento, nÃ£o como fatos. **Zero dependÃªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de Ã­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃ§a estrutural Ãºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃ¢ntica com urgÃªncia e situaÃ§Ã£o em colunas separadas â€” mesmos dados/ordenaÃ§Ã£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃªnticos, 3 grupos de urgÃªncia) foi verificada contra o cÃ³digo real do frontend **pela primeira vez** (antes sÃ³ contra o protÃ³tipo), com asserÃ§Ã£o automatizada. Protocolo Claudeâ†”Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 â€” `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃ§Ã£o normal, entÃ£o o botÃ£o de submit continuava clicÃ¡vel durante a mutaÃ§Ã£o exibindo "Renovandoâ€¦"; e os cabeÃ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃ§am linhas â€” ambos corrigidos com teste de regressÃ£o. O protocolo **nÃ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Â§4) â€” o protocolo mais longo jÃ¡ executado neste repositÃ³rio. TrÃªs rodadas acharam defeitos **criados pela rodada de correÃ§Ã£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃ£o foi cÃ³digo errado e sim **documentaÃ§Ã£o afirmando prova mais ampla que a evidÃªncia nomeada**, que apareceu seis vezes: a alegaÃ§Ã£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃ§Ã£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃµes futuras deste repositÃ³rio. Acessibilidade virou executÃ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃ§Ãµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃ§Ãµes declaradas e registro de decisÃµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃªncia real registrada, nÃ£o bloqueante**: as baselines de regressÃ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃ£o por plataforma, entÃ£o o projeto Playwright `visual` Ã© deliberadamente separado de `npm run test:e2e` e **nÃ£o** estÃ¡ gatilhado no CI hoje (plugÃ¡-lo falharia por baseline ausente, nÃ£o por regressÃ£o real). O caminho de adoÃ§Ã£o estÃ¡ escrito no Â§31 do documento: gravar as baselines num runner Linux, commitÃ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃ© lÃ¡ o CI cobre as mesmas superfÃ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃ©m nÃ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃ­veis no ambiente) â€” `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos â€” Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration â€” sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real â†’ BFF real â†’ API real â†’ persistÃªncia real). Nenhuma rota nova de BFF foi necessÃ¡ria (todas as 6 rotas de item jÃ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo Claudeâ†”Codex (Round B adversarial achou 4 bugs reais â€” 1 S1: corrida TOCTOU real na reaquisiÃ§Ã£o de um registro de idempotÃªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃ§Ã£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃ³ `idempotency.complete()` falhasse depois], hash de renovaÃ§Ã£o ambÃ­guo quando `cycle` Ã© enviado independente de `newDueDate`, bug de fuso horÃ¡rio na formataÃ§Ã£o de data da `Overview` â€” todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃ§Ã£o tambÃ©m foi corrigido um bug real prÃ©-existente de liveness de idempotÃªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) â€” a correÃ§Ã£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃ§ado pela correÃ§Ã£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃ§Ã£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃ§Ã£o), 621 testes de backend â€” nenhuma regressÃ£o. Pendente ao fim desta sessÃ£o: push, PR para `develop`, CI, merge (ver "PrÃ³xima aÃ§Ã£o" abaixo se ainda nÃ£o concluÃ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) â€” todos deployados em `main`/`dev`. **M7 (extraÃ§Ã£o/OCR) teve implementaÃ§Ã£o iniciada em 2026-08-25** (D-057, decisÃ£o direta do Marcelo â€” "prossiga entÃ£o") â€” ver seÃ§Ã£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃ£o disparou.

**Full BFF (autenticaÃ§Ã£o de browser)**: design fechado via protocolo Claudeâ†”Codex em duas rodadas â€” D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` â€” 76 testes unitÃ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃ§Ã£o de frontend de produÃ§Ã£o real (`frontend/` â€” Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃ¡rio/componente + 6 smoke E2E via Playwright). Protocolo Claudeâ†”Codex completo (Rodada D levou **6 passagens** atÃ© convergir â€” 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃ³digo prÃ©-correÃ§Ã£o: consumo nÃ£o-atÃ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃ£o revogada concorrentemente; releituras pÃ³s-refresh nÃ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃ£o checando expiraÃ§Ã£o/idle-TTL antes de agir; idle-TTL da prÃ³pria `Session` nunca validado contra o relÃ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Âº-8Âº), cada um fechado via protocolo Claudeâ†”Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` â€” papÃ©is, JTBD, inventÃ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` â€” modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` â€” 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` â€” 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes Surfaceâ†”Journey/Concept/Transition. RevisÃ£o Codex achou 3 furos reais de mesma causa raiz â€” `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃ§a em `SCANNING`, nÃ£o sÃ³ em `CLEAN`) â€” todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` â€” wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃ£o Codex achou 5 furos reais + 1 divergÃªncia factual â€” aÃ§Ã£o primÃ¡ria ambÃ­gua em 4 coleÃ§Ãµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas â€” todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃ³digo executÃ¡vel real, HTML/CSS/JS sem dependÃªncias) â€” protÃ³tipo navegÃ¡vel cobrindo as 17 superfÃ­cies e J-01â€“J-08 com 34 Prototype Scenario IDs determinÃ­sticos, verificado com testes automatizados de navegador headless. RevisÃ£o Codex achou 6 furos reais de 4 causas raiz â€” compressÃ£o de estados no guest upload (faltava validaÃ§Ã£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃ§Ã£o de coleta externa violando epistemic integrity (anunciava verificaÃ§Ã£o de seguranÃ§a concluÃ­da ao operador, que nÃ£o pode observar isso), dois campos sem `<label>`, e reincidÃªncia de menÃ§Ã£o a WhatsApp â€” todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` â€” avaliaÃ§Ã£o do protÃ³tipo **executÃ¡vel** (nÃ£o sÃ³ a documentaÃ§Ã£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃ¢ntica/forms testados em navegador headless real, nÃ£o sÃ³ lidos), `axe-core` (0 violaÃ§Ãµes em todos os estados testados, nas duas rodadas), re-execuÃ§Ã£o de J-01â€“J-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** Claudeâ†”Codex (nÃ£o as 2-3 rodadas tÃ­picas de etapas anteriores): Rodada A (autoavaliaÃ§Ã£o, 9 achados corrigidos) concluiu aprovaÃ§Ã£o â€” **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃ£o vistos pela autoavaliaÃ§Ã£o: o mais grave, a prÃ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃ§Ã£o em 3 formulÃ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃ§Ã£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃ¡-los no fake-backend â€” uma violaÃ§Ã£o epistÃªmica real, nÃ£o cosmÃ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃ³prio ao manter um texto "SIMULATED FOR UX VALIDATION â€” NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃ­cie pÃºblica de guest (jargÃ£o tÃ©cnico visÃ­vel a um usuÃ¡rio externo nÃ£o-tÃ©cnico). Rodada D (nova adversarial sobre o cÃ³digo jÃ¡ corrigido) achou essa lacuna de conteÃºdo e uma correÃ§Ã£o incompleta nova (`submitAlert` reabilitava o botÃ£o sÃ³ na branch de erro, nÃ£o na de sucesso) â€” ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃ£o arredondado; calculado refletindo esse histÃ³rico de 4 rodadas, nÃ£o sÃ³ o estado final do cÃ³digo) â€” `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` â€” "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃ£o) no protÃ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃ§Ã£o, nunca marcado como backend-resolvido; cenÃ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃ§Ãµes) que achou e corrigiu uma falta real de ordenaÃ§Ã£o por urgÃªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃ£o sÃ³ no protÃ³tipo) â€” `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃ©tricas de validaÃ§Ã£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃ¡ em uso desde a 1Âª etapa, agora formal, nenhum critÃ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃ§Ã£o tÃ©cnica em Participant Mode, teste de idempotÃªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃµes de documentaÃ§Ã£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃ©-existentes desde a Rodada A) â€” corrigidos no fechamento, zero contaminaÃ§Ã£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** â€” **fechado nesta sessÃ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` â€” `DocumentService.listDocuments`/`getDocument`, autorizaÃ§Ã£o `document:read` (aÃ§Ã£o jÃ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃ§Ã£o do item jÃ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` â€” `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃ§Ã£o `requirement:read` jÃ¡ existente (sem action nova reservada de antemÃ£o, diferente de `document:read`), mesma leitura por partiÃ§Ã£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform â€” nÃ£o Ã© desta sessÃ£o, jÃ¡ estava corrigido antes (comentÃ¡rio no prÃ³prio `main.tf`), sÃ³ confirmado que segue correto.

  Ambas as metades: mudanÃ§a mecÃ¢nica sobre um padrÃ£o de acesso jÃ¡ aprovado no data model â€” nÃ£o passaram pelo protocolo `AGENTS.md` Â§4 completo (nÃ£o Ã© decisÃ£o de arquitetura nova, Ã© fechamento de gap de leitura jÃ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃ­cio da sessÃ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃ£o. **Achado real corrigido depois, na mesma sessÃ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` â€” mesma classe de gap que as rotas de `DocumentRequest` jÃ¡ tiveram antes (comentÃ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** â€” **implementaÃ§Ã£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃ§Ã£o abaixo). Antes desta branch, a materializaÃ§Ã£o automÃ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃ§Ã£o/ediÃ§Ã£o de item. PendÃªncia real restante: decisÃ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) â€” nÃ£o bloqueia mais nada alÃ©m dessa decisÃ£o em si.
- **BLOCKER-C** â€” **decisÃ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃ£o humana explÃ­cita)**, confirmando a hipÃ³tese lÃ­der jÃ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Â§22, `interface-conceptual-model-and-information-architecture.md` Â§37, `interface-critical-user-journeys.md` Â§37 â€” esses documentos continuam como registro histÃ³rico do debate, nÃ£o reescritos, per `AGENTS.md` regra de precedÃªncia de `history/`/artefatos aprovados). InvestigaÃ§Ã£o confirmou que Variante A (fechamento automÃ¡tico) nÃ£o Ã© viÃ¡vel hoje: `DocumentSubmission` nÃ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃ§a a qual `ExpirationItem` uma submissÃ£o pertence â€” decidir automaticamente seria adivinhar, nÃ£o resolver. **Mecanismo de backend para a Variante B jÃ¡ existe, nenhum cÃ³digo novo necessÃ¡rio**: visibilidade do operador sobre submissÃµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃ§Ã£o jÃ¡ existente (`POST .../link`, prÃ©-existente). **Frontend implementado na mesma sessÃ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject â€” `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores â†’ detalhe com requisitos â†’ "Revisar" expande evidÃªncia enviada (`DocumentSubmission`) â†’ operador informa o `itemId` de um vencimento jÃ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃ§Ã£o de subject nem sugestÃ£o automÃ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃ³tulos seguem a mesma disciplina de Epistemic Integrity jÃ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃ§a) â€” conteÃºdo nÃ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta Ãºltima etapa: **`GTR-01`** (identidade do solicitante nÃ£o exposta ao guest) agora **simulada** no protÃ³tipo para User Validation, mas ainda nÃ£o resolvida no backend â€” `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃªncia) foi **resolvido no backend real** â€” `createItem` ganhou `idempotencyKey` opcional â€” e, na sessÃ£o do Full BFF/frontend, o `requestHash` de detecÃ§Ã£o de payload divergente foi migrado de concatenaÃ§Ã£o por delimitador (colisÃ£o real possÃ­vel) para JSON canÃ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` â€” a lacuna "nenhum frontend/BFF real envia o header" estÃ¡ fechada para o caminho de criaÃ§Ã£o de item.

**PrÃ³xima etapa natural do design** (ainda nÃ£o iniciada): **User Validation** â€” usando o protÃ³tipo jÃ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode Ã© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Â§15-18/Â§23-24 (tese de produto, mÃ©tricas de validaÃ§Ã£o, tarefas candidatas derivadas de outcomes, limitaÃ§Ãµes conhecidas do protÃ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃ£o produzido ainda. NÃ£o obriga inÃ­cio imediato de Visual Design/Design System.

## BLOCKER-B â€” implementaÃ§Ã£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃ£o de 2026-08-24/25 (continuaÃ§Ã£o apÃ³s a sessÃ£o anterior ter sido abortada por limite de tokens sÃ³ com reconhecimento parcial â€” handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo Claudeâ†”Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` â€” esse documento Ã© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃ§Ã£o real (nÃ£o sÃ³ design) tambÃ©m revisada via protocolo Claudeâ†”Codex (2 rodadas, nota final 9,2/10, `APPROVED` â€” achados reais corrigidos: reclassificaÃ§Ã£o de exceÃ§Ã£o em dois mÃ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃ§Ã£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃ§Ã£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃªs novos mÃ©todos de reconciliaÃ§Ã£o em `ReminderMaterializer` (fenced por versÃ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃ§Ã£o: o "caminho genÃ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃ³digo â€” `router_queue` nÃ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Â§8). **Implementado nesta sessÃ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃ§Ã£o da criaÃ§Ã£o do item (sem `ConditionCheck` de existÃªncia â€” o item novo Ã© garantido pela prÃ³pria transaÃ§Ã£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃ³ `ExpirationItem` (mudanÃ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" â€” reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃ£o estava instalado no inÃ­cio desta sessÃ£o (`npm ci` rodado para poder validar) â€” ambiente, nÃ£o cÃ³digo.

**PrÃ³xima aÃ§Ã£o**: **M7 (extraÃ§Ã£o/OCR) â€” todos os 9 itens numerados originais da lista estÃ£o CONCLUÃDOS (2026-08-26)**, cÃ³digo+infra reais para itens 1-9, nÃ£o sÃ³ camada pura â€” ver as seÃ§Ãµes dedicadas "Item N â€” CONCLUÃDO" na seÃ§Ã£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) â€” causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃ­ficos com `States.ALL` num Ãºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃ£o pega esse erro, sÃ³ a chamada real de criaÃ§Ã£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** â€” o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃ§Ã£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** â€” a nota de uma sessÃ£o anterior dizendo "nenhum profile corresponde Ã  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃµes de escrita necessÃ¡rias e a **verificaÃ§Ã£o E2E real foi executada** (ver `## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃ§a de Terraform foi cogitada para isso â€” a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃ³ o gatilho automÃ¡tico via EventBridge Ã© que Ã© gated). **RecorrÃªncia fechada na mesma sessÃ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` â€” a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃ³ descoberto no meio de um `terraform apply` real. NÃ£o requer credencial de escrita (Ã© uma chamada de validaÃ§Ã£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃ£o retomar sem sinal explÃ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA â€” etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA â€” etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃ¡ aprovada em nÃ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃ¡vel por hash; `implementation-blueprint.md` Â§12/Â§23: CSP com hashes estÃ¡ticos calculados no build, sem nonce dinÃ¢mico â€” Day 0 nÃ£o tem compute de borda). O que falta Ã© o desenho de implementaÃ§Ã£o real e o cÃ³digo â€” nenhuma decisÃ£o de produto pendente, mas hÃ¡ **uma decisÃ£o de arquitetura real ainda em aberto** que a prÃ³xima sessÃ£o precisa resolver antes de implementar (Type 1, nÃ­vel 5-6 da escala de risco â€” protocolo `AGENTS.md` Â§4 aplicÃ¡vel):

1. **DecisÃ£o de arquitetura a fechar primeiro â€” FECHADA nesta sessÃ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo Claudeâ†”Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** â€” `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` Ã© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma Ãºnica distribution, behaviors `/bff` e `/bff/*` â†’ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior â†’ S3 com OAC + CloudFront Function de SPA routing (sÃ³ nesse behavior, nunca no do BFF). Zero mudanÃ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃ©-existentes descobertos pela revisÃ£o, a aplicar na implementaÃ§Ã£o: `proxy-service.ts` nÃ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃ£o implementado): acesso direto ao `execute-api` pÃºblico precisa de mitigaÃ§Ã£o (header estÃ¡tico + WAF) antes de produÃ§Ã£o pÃºblica real.
2. **MÃ³dulo Terraform `infra/modules/spa-hosting` â€” IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃ­citos â†’ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default â†’ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃ©todo GET/HEAD, associada SÃ“ ao default behavior â€” nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃ©-existentes do ADR-0011 tambÃ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃ§Ã£o errada do prÃ³prio ADR-0011): `var.spa_content_security_policy` nÃ£o precisa de hashes calculados por build â€” verificado contra o `frontend/dist/index.html` real, o build do Vite nÃ£o emite nenhum `<script>`/`<style>` inline, todo asset Ã© carregado por URL externa jÃ¡ hasheada, entÃ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃ¡ existe em `frontend/index.html`) bastam sem hash nenhum â€” a variÃ¡vel ganhou um default determinÃ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃ£o consegue expressar). **MÃ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) â€” corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃ£o bloqueia o primeiro apply, sÃ³ o domÃ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro â€” confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃ£o sÃ³ contra mocks.
3. **Deploy imutÃ¡vel por hash â€” IMPLEMENTADO (2026-08-25)**: Vite jÃ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra Ã© necessÃ¡rio (o hash no nome do arquivo jÃ¡ Ã© o mecanismo de imutabilidade). `index.html` Ã© tratado Ã  parte (nÃ£o hasheado, `Cache-Control: no-cache`, upload por Ãºltimo).
4. **IntegraÃ§Ã£o com `cd.yml` â€” IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃ£o um job novo) â€” `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** â€” assets antigos nunca sÃ£o removidos, Ã© isso que mantÃ©m um rollback de `index.html` funcional, jÃ¡ que as referÃªncias antigas continuam servÃ­veis), upload de `index.html` por Ãºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃ¡ existente. **PendÃªncia real registrada, nÃ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃ³ cobre Lambdas â€” nÃ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃ£o `rollback.yml` (mecanismo existente) nÃ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃ£o bloqueia o deploy normal.
5. **VerificaÃ§Ã£o real (Camada 3) â€” CONCLUÃDA (2026-08-25), achou e corrigiu um bug real de produÃ§Ã£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** â€” isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃ­tica fixa nÃ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃ§Ã£o, tudo confirmado contra a distribution real:
   - `GET /` â†’ 200, `text/html`.
   - `GET /assets/index-*.js` â†’ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃ£o, V6 do ADR-0011) â†’ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) â†’ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) â€” nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) â†’ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃ£o (V4) â†’ 401 `application/json` (`AUTH_REQUIRED`) â€” nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃ£o (V5-404) â†’ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado â€” o caso 404-por-allowlist com sessÃ£o vÃ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃ£o foram exercitados por nÃ£o serem automatizÃ¡veis via `curl` sozinho; a propriedade crÃ­tica de seguranÃ§a â€” nenhum erro do BFF vira HTML da SPA â€” jÃ¡ estÃ¡ confirmada pelos casos acima).
   - Headers de seguranÃ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy â€” exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃ£o sÃ³ via `terraform plan`.
   **PendÃªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃ£o lidos via CloudFront/OAC (sÃ³ por IAM de role de Lambda), entÃ£o nÃ£o tÃªm este bug â€” mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃ£o corrigido preventivamente nesta sessÃ£o (fora do escopo desta decisÃ£o, nenhum uso real hoje), sÃ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃ§Ã£o/OCR) â€” implementaÃ§Ã£o iniciada (2026-08-25, D-057)

Design jÃ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) â€” esta seÃ§Ã£o Ã© sobre a IMPLEMENTAÃ‡ÃƒO, o design nÃ£o muda. **Escopo Ã© grande** (Step Functions, Textract assÃ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃ§Ã£o) â€” vai continuar por mÃºltiplas sessÃµes, nÃ£o Ã© razoÃ¡vel fechar num Ãºnico slice. Estado exato ao fim desta sessÃ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Â§4 ganhou a classe `EXTRACTION_TRANSIENT` â€” prÃ©-requisito de design que o prÃ³prio D-035 exige estar registrado antes de qualquer implementaÃ§Ã£o (nÃ£o Ã© pendÃªncia pÃ³s-produÃ§Ã£o).
2. MÃ³dulo novo `src/modules/extraction/domain/` (sÃ³ domÃ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` â€” `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃ³pria chave de idempotÃªncia da execuÃ§Ã£o).
   - `extracted-field.ts` â€” `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Â§12.5.
   - `field-schema.ts` â€” schema v1 com sÃ³ `expirationDate` (Ãºnico campo que o design aprovado nomeia concretamente â€” deliberadamente nÃ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` â€” `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` â€” helper de safety net de 24h para o artefato transitÃ³rio (a exclusÃ£o real Ã© explÃ­cita por `ExtractionValidationTaskHandler`, este helper Ã© sÃ³ a rede de seguranÃ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` â€” **ainda sem PR/merge para `main`**, dado que nÃ£o hÃ¡ nenhuma infra/runtime ativÃ¡vel ainda, nada a fazer deploy).

**Item 1 â€” CONCLUÃDO nesta sessÃ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃ§Ã£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃ³dulo corrigido pelo design â€” transversal, nÃ£o `extraction-appconfig`, jÃ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃªs defaults `false` (fail-closed, prÃ©-condiÃ§Ãµes externas de M7 nÃ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Â§1.6, default `false`) â€” gate Terraform separado do pipeline inteiro, ainda nÃ£o referenciado por nenhum recurso (sÃ³ os workers futuros vÃ£o condicionar-se a ele). 2 `terraform test` do mÃ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) â€” CAMADA PURA CONCLUÃDA nesta sessÃ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃ£o que o item 0 seguiu: domÃ­nio+aplicaÃ§Ã£o primeiro, depois handler Lambda/infra numa fatia posterior â€” deliberado, nÃ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` â€” cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` â€” entidade `TextractJob` (chave prÃ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃ£o tenant-scoped â€” `COMPLETE_OCR` sÃ³ tem `jobId` disponÃ­vel da notificaÃ§Ã£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃ§Ã£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` â€” classificador heurÃ­stico puro (magic bytes > extensÃ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃ§a) quando nenhum sinal Ã© conclusivo â€” quem decide que isso Ã© `UnsupportedDocumentTypeError` Ã© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed Ã© responsabilidade do caller, nÃ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃ©todo de delete â€” a exclusÃ£o do artefato Ã© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) â€” orquestra classificaÃ§Ã£o â†’ kill switch `OCR` (fail-closed) â†’ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` â€” um retry do mesmo run encontra `QuotaExceededError` contra a prÃ³pria reserva anterior e trata como "jÃ¡ reservado", nÃ£o como falha real) â†’ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃ­stico) â†’ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` â€” retorna void, a execuÃ§Ã£o do Step Functions fica parada no `taskToken` atÃ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) â€” busca `TextractJob` por `jobId`; caso Ã³rfÃ£o (nenhum job encontrado) faz UMA chamada de confirmaÃ§Ã£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃµe um mÃ©todo de delete); `taskTokenCiphertext` Ã© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃ§ado, sem tocar no registro) em qualquer outro erro â€” a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto â€” `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃ£o Ã© revalidado como JSON aninhado, mesma convenÃ§Ã£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` â€” `RunTextract` Ã© o Ãºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃ£o existem.
- `infra/modules/extraction-workflow/` â€” mÃ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃ§Ã£o + role de execuÃ§Ã£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒO chamado em `infra/main.tf`** â€” um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃ£o sÃ³ em runtime), e os 3 handlers dos itens 5-7 nÃ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃ³prio diretÃ³rio do mÃ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃ³dulo especificamente â€” pendÃªncia explÃ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos Ã  mÃ£o (sem `vi.mock`), cobrindo: classificaÃ§Ã£o por magic-bytes/extensÃ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃ§Ã£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃªncia da reserva de quota sob retry do mesmo run; retry local de persistÃªncia do `TextractJob` (sucesso apÃ³s 1 falha, esgotamento apÃ³s N); `completeOcr`: job Ã³rfÃ£o (com e sem falha na confirmaÃ§Ã£o), jÃ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃ§Ã£o multi-pÃ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃ³rio de `SendTask*` relanÃ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃ£o expÃµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na Ã¡rvore inteira de `infra/`.

**Item 4 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃªs decisÃµes pendentes acima foram todas tomadas e implementadas (nÃ£o deixadas como pendÃªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃ£o final** (nÃ£o a extensÃ£o via layer) â€” `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/Ãºltimos flags conhecidos em memÃ³ria do processo. Motivo: a extensÃ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` â€” nÃ£o existe nenhum precedente disso no repo (sÃ³ `adot_layer_arn`, uma layer diferente), e este handler sÃ³ lÃª os flags uma vez por invocaÃ§Ã£o de `START_OCR` (nÃ£o por pÃ¡gina/bloco), entÃ£o o custo/latÃªncia de uma chamada de API direta por invocaÃ§Ã£o Ã© equivalente ao que a extensÃ£o teria numa invocaÃ§Ã£o fria de qualquer forma. Revisitar sÃ³ se o volume tornar isso um custo real (a extensÃ£o amortiza entre invocaÃ§Ãµes warm via cache local, este adapter sÃ³ amortiza dentro de uma sessÃ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃ£o reuso da CMK do BFF â€” mesma disciplina D-054: um token de callback do Step Functions Ã© uma credencial viva tÃ£o sensÃ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃ³dulos independentemente deployÃ¡veis acoplaria rotaÃ§Ã£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃ³dulo, mesma razÃ£o do port).
- **TÃ³pico SNS de conclusÃ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar â€” distinta da role de execuÃ§Ã£o do prÃ³prio Lambda.

Real (cÃ³digo + infra, nÃ£o sÃ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃ§Ã£o real `TaskTimedOut`/`TaskDoesNotExist`â†’`TERMINAL_QUIET`, `InvalidToken`â†’`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃ§ado), `AppConfigFeatureFlagsReader` â€” todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` â€” um Ãºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃ§Ã£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃ£o SSE-KMS â€” evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃ³pico SNS, permissÃ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃ¡ escopada Ã  ARN determinÃ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** â€” mesma lÃ³gica do item 2: todo recurso aqui Ã© inerte sozinho, porque `StartDocumentTextDetection` (a Ãºnica chamada paga) sÃ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27â†’28 funÃ§Ãµes (2 asserts).
- 11 testes novos (794 totais no backend) â€” sÃ³ para os dois adapters com lÃ³gica real de branching (`SfnTaskTokenSender`'s classificaÃ§Ã£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃ£o ganharam teste unitÃ¡rio dedicado, seguindo o MESMO precedente jÃ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃ©m nÃ£o tÃªm) â€” verificados via `terraform plan` real contra `dev`, nÃ£o via mock unitÃ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target â€” 0 erros, 0 destroys inesperados (sÃ³ o prÃ³prio slice + o ruÃ­do de republish de versÃ£o jÃ¡ prÃ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) â€” **ainda sem PR/merge para `main`** no momento em que este parÃ¡grafo foi escrito (abrir o PR Ã© o prÃ³ximo passo imediato desta mesma sessÃ£o, ver "PrÃ³xima aÃ§Ã£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 â€” a ASL's `RunDeterministicParser` Ã© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃ£o hÃ¡ task token nenhum neste handler), entÃ£o Ã© uma Ãºnica invocaÃ§Ã£o sÃ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` â€” `extractExpirationDateCandidates()`, regex determinÃ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃ¡ a atÃ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃ§a fraca (0.5, abaixo do threshold) quando nÃ£o hÃ¡ palavra-chave prÃ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃ©rico que trunca o texto processado â€” a Ãºnica reutilizaÃ§Ã£o real de M6 possÃ­vel aqui, jÃ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃ£o sobre texto OCR, e nunca extrai campos/datas (sÃ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃ³digo `DeterministicParserFailed`) â€” nÃ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃ¡ Ã© o genÃ©rico `States.ALL`.
- AplicaÃ§Ã£o nova `src/modules/extraction/application/run-deterministic-parser.ts` â€” `runDeterministicParser()`: lÃª o artefato OCR (quando presente) â†’ extrai candidatos de `expirationDate` via `getFieldSchema()` â†’ chama `needsBedrock()` (item 0) â†’ lÃª o kill switch `AI_EXTRACTION` (fail-closed) â†’ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** â€” retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃ¡ implica `true`) e a decisÃ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃ£o duplicada) `OcrArtifactStore` ganhou `get()` â€” o item 4 sÃ³ tinha `put()` (quem escreve o artefato); o item 5 Ã© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada â€” sÃ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃ§Ã£o, nÃ£o uma decisÃ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃ£o tinha `ResultPath` na transiÃ§Ã£o de sucesso do ASL, entÃ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃ­a inteiramente** o `$` do Step Functions â€” todo o contexto original da execuÃ§Ã£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃ£o sÃ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃ³prio `job`) ao payload de sucesso. MudanÃ§a aditiva/reversÃ­vel sem migraÃ§Ã£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃ³rico real).
- **DECISÃƒO PENDENTE (registrada aqui, default conservador aplicado)**: o design (Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃ£o implementado â€” nenhum campo de metadado de PDF (data de criaÃ§Ã£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃ£o de produto nÃ£o revisada, nÃ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃ§Ã£o na prÃ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` â€” nÃ£o muda nenhum `Next`/fluxo, sÃ³ corrige o path de leitura para bater com a semÃ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃ§Ã£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` â€” sÃ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` â€” invocaÃ§Ã£o sÃ­ncrona Ãºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` â€” sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃ¡ escopado Ã  ARN determinÃ­stica da state machine futura (mesmo padrÃ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28â†’29 funÃ§Ãµes (3 asserts: nomes, distinÃ§Ã£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃ§Ãµes dia/mÃªs impossÃ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar â€” sÃ³ republish de versÃ£o prÃ©-existente em todas as Lambdas, mesmo ruÃ­do que o item 4 jÃ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` â€” drift **prÃ©-existente do item 4**, nÃ£o introduzido nesta sessÃ£o, causado por uma normalizaÃ§Ã£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃ¡ detectava antes desta sessÃ£o comeÃ§ar; nÃ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 â€” ambos concluÃ­dos em sessÃµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ â€” **CONCLUÃDO 2026-08-26**, ver "Item 3 â€” CONCLUÃDO nesta sessÃ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ â€” **CONCLUÃDO 2026-08-26**, ver seÃ§Ã£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` â€” sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃ§Ã£o, na prÃ³pria ASL** (nÃ£o uma decisÃ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` â€” `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta â€” sem isso, `BedrockExtractionTaskHandler` nÃ£o teria como saber onde ler o texto OCR (o design Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃ§Ã£o de contexto que `completeOcr` (item 4/5) jÃ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃ³digo + infra, nÃ£o sÃ³ camada pura)**:
- DomÃ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` â€” `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃ¡vel, nunca concatenada com input do usuÃ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` â€” nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃ³ `textArtifact` â€” tipo local `BedrockTextArtifactRef`, estruturalmente idÃªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃ­be domÃ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) â€” nenhum import do SDK Bedrock fora do adapter.
- AplicaÃ§Ã£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) â€” re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃ¡ filtra isso mas a chamada paga nunca confia sÃ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃ£o hÃ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz â€” nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) â€” Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃªncia de tool-call, tool errado) â€” qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃ§Ã£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` â€” DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃ£o configurÃ¡vel via `bedrock_region`, distinta da regiÃ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` â€” invocaÃ§Ã£o sÃ­ncrona Ãºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃ£o Bedrock: configurÃ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) â€” design Â§4 bloqueia sÃ³ a escolha/validaÃ§Ã£o real para produÃ§Ã£o, nunca a testabilidade em `dev`; a permissÃ£o IAM jÃ¡ Ã© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota â€” mesmo padrÃ£o do item 4 â€”, `feature_flags_read_policy_json`, `s3:GetObject` sÃ³ leitura no bucket `EXTRACTION_TRANSIENT` â€” nunca escrita, mesma disciplina de blast-radius do item 5 â€”, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃ¡ escopado Ã  ARN determinÃ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29â†’30 funÃ§Ãµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 â€” caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃ§Ã£o de quota em falha, **14Âº caso adversarial de custo-abuso**: execuÃ§Ã£o retentada/duplicada do mesmo `runId` reserva contra a prÃ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 â€” corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Â§1.11) nÃ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) â€” o corpus construÃ­do cobre as mesmas classes de ameaÃ§a descritas ali (injeÃ§Ã£o via conteÃºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Âº caso de custo-abuso) a partir de princÃ­pios, documentado no comentÃ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar â€” os do item 5 ainda nÃ£o aplicado + os do item 6 â€”, 56 a mudar â€” mesmo ruÃ­do de republish de versÃ£o jÃ¡ documentado nos itens 4/5 â€”, 1 destroy-and-recreate â€” o MESMO drift prÃ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃ£o introduzido nesta sessÃ£o). **Nunca `terraform apply` local**.
- **DecisÃ£o pendente registrada, nÃ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃ©m de `expirationDate`, entÃ£o o schema fechado do tool `submit_extraction` sÃ³ tem essa uma propriedade â€” `buildSubmitExtractionToolSchema()` jÃ¡ Ã© genÃ©rico sobre `FIELD_SCHEMA_V1`, entÃ£o um campo novo no schema nÃ£o precisa de mudanÃ§a de cÃ³digo aqui, sÃ³ uma nova versÃ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) â€” todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃ¡gio, design Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃ£o mais importante desta sessÃ£o â€” a limpeza do artefato transitÃ³rio (design Â§3, "achado mais sÃ©rio")**: `OcrArtifactStore.delete()` Ã© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃ³fica do parser determinÃ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` â€” design cita esse estado explicitamente como ponto de deleÃ§Ã£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` â€” rodam antes do run alcanÃ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado â€” zero atÃ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` â€” `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃ£o-vazio. Um candidato invÃ¡lido nunca Ã© descartado silenciosamente â€” Ã© marcado `valid: false` e tratado como "essa fonte nÃ£o produziu nada" na comparaÃ§Ã£o.
- `compare-extractors.ts` â€” `compareExtractors(candidates)`: 0 candidatos usÃ¡veis â†’ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` â€” jÃ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` â€” nÃ£o representa separadamente; decisÃ£o registrada aqui em vez de alargar o tipo jÃ¡ commitado). 1 candidato â†’ `SINGLE_SOURCE` com valor. 2+ concordando â†’ `MATCH` (confianÃ§a = a maior das duas). 2+ discordando â†’ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" â€” `candidateValue` carrega sÃ³ o de maior confianÃ§a, para exibiÃ§Ã£o, mas Ã© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` â€” `decideFieldOutcome(comparison)`: sem `candidateValue` â†’ `PENDING_CONFIRMATION`; `MISMATCH` â†’ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) â†’ `PENDING_CONFIRMATION`; senÃ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) â†’ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** â€” esse estado sÃ³ Ã© alcanÃ§Ã¡vel pela rota HTTP de rejeiÃ§Ã£o humana (item 8, ainda nÃ£o implementada).

**DecisÃ£o de arquitetura tomada nesta sessÃ£o (nÃ£o estava no design, resolvida por inferÃªncia direta do enum jÃ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃ£o tem um estado "aguardando confirmaÃ§Ã£o" separado â€” entÃ£o a distinÃ§Ã£o "run completa vs. precisa de revisÃ£o humana" vive inteiramente no nÃ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃ¡lido â†’ comparaÃ§Ã£o â†’ persistÃªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃ­da, hoje sÃ³ para log/observabilidade â€” nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` Ã© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho â€” o prÃ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa Ãºnica operaÃ§Ã£o, porque Ã© o Ãºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, Ã© uma decisÃ£o de produto pequena e reversÃ­vel (mudar sÃ³ a decisÃ£o de estado, nÃ£o a mecÃ¢nica de comparaÃ§Ã£o/persistÃªncia) â€” registrado aqui para reabrir se necessÃ¡rio.

**Descarte por exclusÃ£o concorrente (design Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃ£o do `Document` lida nÃ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃ£o sÃ³ um "ler status DELETED e torcer"). Se o `Document` jÃ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃ§Ã£o â€” chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃ§Ã£o falhar dentro da transaÃ§Ã£o (mudanÃ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃ§Ã£o como descarte â€” decisÃ£o conservadora documentada no comentÃ¡rio do adapter, jÃ¡ que distinguir a causa exata do cancelamento por cÃ³digo de erro do SDK seria frÃ¡gil entre versÃµes).

**Ports/persistÃªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) â€” expÃµe SÃ“ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num Ãºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` Ã© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` â€” mÃ©todo standalone, usado SÃ“ no caminho de descarte (onde nÃ£o hÃ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` â€” **mudanÃ§a de contrato deliberada**: o item 4 tinha deixado esse mÃ©todo deliberadamente FORA do port ("a exclusÃ£o do artefato Ã© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" â€” tornava a exclusÃ£o acidental do chamador errado estruturalmente impossÃ­vel). Agora adicionado porque este Ã‰ o chamador certo â€” `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃ³pria API do S3, deletar uma key ausente nÃ£o lanÃ§a). Os testes que antes afirmavam "o port nem expÃµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃ¡ que o mÃ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃ¡-lo).

**AplicaÃ§Ã£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃ§Ãµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um Ãºnico `ValidationContext` que evolui atravÃ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃ£o exportado) que encapsula a leitura do `Document`+decisÃ£o discard-ou-commit. Um erro genuÃ­no de commit (nÃ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` â€” o `Catch`/`Retry` da ASL trata como transitÃ³rio).

**Achado real corrigido nesta sessÃ£o, em cÃ³digo do item 6 jÃ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` â€” a MESMA classe de bug que o item 5 jÃ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Â§3's "achado mais sÃ©rio" Ã© sobre limpeza de artefato, mas essa categoria de "output nÃ£o reanexa o contexto anterior" jÃ¡ apareceu 3 vezes agora: item 4â†’5, item 5â†’6, e este item 6â†’7). Corrigido de forma aditiva (campos novos sÃ³ ecoam o que jÃ¡ vinha em `input`, sem recomputar nada) â€” os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃ§Ã£o (nÃ£o fazem `toEqual` no objeto inteiro). **PadrÃ£o a vigiar em qualquer implementaÃ§Ã£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃ³ximo estado vai precisar, nunca assumir que "sÃ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃ©m ganhou `ResultSelector`/`OutputPath` (nÃ£o tinha nenhum antes â€” outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` â€” o footprint mais estreito dos quatro Lambdas de extraÃ§Ã£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` â€” este Ã© o Ãºnico Lambda do pipeline inteiro com permissÃ£o de deletar lÃ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma Ãºnica `aws_lambda_permission` cobre as cinco invocaÃ§Ãµes (todas vÃªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30â†’31 funÃ§Ãµes (3 asserts).

**VerificaÃ§Ã£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃ­cio da sessÃ£o â€” nÃ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃ£o, nÃºmero incorreto de digitaÃ§Ã£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃ§Ã£o do artefato descrito acima), mais 16 testes prÃ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir â€” o Ãºnico destroy Ã© o MESMO drift prÃ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃµes dos itens 5/6, nÃ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃ£o de produto pendente registrada, nÃ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` â€” isso Ã© o item 8 (rotas de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7), ainda nÃ£o implementado. AtÃ© lÃ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃ£o hÃ¡ nenhuma forma de um operador vÃª-lo ou agir sobre ele pela API.

**Item 3 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora Ã© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃ¡ existentes (items 4-7) e uma role de execuÃ§Ã£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` â€” o mesmo nome que `local.extraction_state_machine_arn` jÃ¡ esperava desde o item 2, verificado por `terraform test` (nÃ£o confiado sÃ³ Ã  leitura do cÃ³digo).

- **IAM da role de execuÃ§Ã£o do prÃ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado Ã s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃªncia documentada da prÃ³pria AWS para `logging_configuration` de state machine â€” opera no subsistema de log delivery a nÃ­vel de conta, nÃ£o num log group especÃ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃ§Ãµes que `AWSXRayDaemonWriteAccess`, que o mÃ³dulo `lambda-function` jÃ¡ anexa a toda funÃ§Ã£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃ¡-lo, escopado Ã  ARN determinÃ­stica do state machine) jÃ¡ estava pronto desde os itens 4-7 â€” nÃ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃ§Ã£o 30 dias, mesmo padrÃ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃ­vel `ERROR` (nÃ£o `ALL`) com `include_execution_data = false` â€” decisÃ£o deliberada espelhando a disciplina de privacidade do prÃ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃ§Ã£o pode carregar texto OCR/valores de campo extraÃ­dos; nunca devem cair num log group com retenÃ§Ã£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` â€” todo Lambda do repo jÃ¡ roda com ADOT/X-Ray ativo (AGENTS.md Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` â€” o state machine, sua role e as 4 permissÃµes de invocaÃ§Ã£o sempre existem (inspecionÃ¡veis/deployÃ¡veis). RaciocÃ­nio registrado no comentÃ¡rio do prÃ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃ¡ gateia o ÃšNICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que Ã© quem chama `StartExecution`) â€” com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃ© ser iniciado manualmente para uma execuÃ§Ã£o de teste, mas nunca recebe trÃ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) â€” 13â†’15 passando. MÃ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃªncia explÃ­cita registrada pelo item 7) â€” 5 `run` via `mock_provider`+`apply` (substituiÃ§Ã£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃ¡s; `type = STANDARD`; role passada atravÃ©s sem modificaÃ§Ã£o; tracing/logging configurados) â€” 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) â€” **a verificaÃ§Ã£o crÃ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` â†’ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" â€” a definiÃ§Ã£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** â€” o Ãºnico destroy Ã© o MESMO drift prÃ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃ§Ã£o de `function_name` com/sem qualificador `:live`), jÃ¡ documentado nas sessÃµes dos itens 5/6/7, nÃ£o introduzido aqui. Nenhuma issue de integraÃ§Ã£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃ­ram corretamente â€” nÃ£o foi necessÃ¡rio nenhum fix no `replace()` do mÃ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃ£o â€” sessÃ£o nÃ£o tocou cÃ³digo de aplicaÃ§Ã£o, sÃ³ infra). `terraform fmt -check -recursive` limpo em toda a Ã¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃ£o precisar de `command = apply` contra o provider real). Novo output no mÃ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃ£o (branch `develop`, PR #60 `developâ†’main` ainda aberto â€” push deve atualizÃ¡-lo automaticamente): instanciaÃ§Ã£o do mÃ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: `ExtractionStarterWorker` completo â€” domÃ­nio/aplicaÃ§Ã£o/persistÃªncia/handler real, nÃ£o sÃ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` â€” portas prÃ³prias, nÃ£o reusa `DocumentStore` do mÃ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃ¡ existia â€” gated sÃ³ no registro criaria um run Ã³rfÃ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real Ã© a idempotÃªncia nativa do `StartExecution` por nome de execuÃ§Ã£o determinÃ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃ£o S3â†’EventBridgeâ†’SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃ§Ã£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` â€” insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) â€” mudanÃ§a na M6 (jÃ¡ em produÃ§Ã£o), mas segura: `cleanObject` Ã© sempre um valor opaco armazenado na entidade, nunca reconstruÃ­do/reparsed em nenhum outro lugar do cÃ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis), mas a regra EventBridge que liga o bucket limpo Ã  fila Ã© o ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` â€” com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) â€” jÃ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃªncias) â€” comportamento condicional verificado, nÃ£o sÃ³ lido no cÃ³digo. **Ainda sem PR/merge** (mesma lÃ³gica do item 1: mergeÃ¡vel isoladamente, mas ainda aguardando decisÃ£o de "quando" â€” ver abaixo).
3. ASL â€” camada pura (o arquivo `.asl.json` e o mÃ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** (cÃ³digo+infra, nÃ£o sÃ³ camada pura) â€” ver a seÃ§Ã£o dedicada acima ("Item 4 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, arquivos tocados, decisÃµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 5 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, arquivos tocados, decisÃµes tomadas (reanexaÃ§Ã£o de contexto ao payload de sucesso de `completeOcr`, correÃ§Ã£o do path `$.Payload.*` nas Choice states) e a decisÃ£o pendente registrada (sem heurÃ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 6 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, decisÃµes tomadas (modelo/regiÃ£o placeholder configurÃ¡veis, correÃ§Ã£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃ§Ã£o do `artifact` ao output do item 5) e o corpus adversarial construÃ­do.
7. `ExtractionValidationTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 7 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, decisÃµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃ¡ commitado, invariante de deleÃ§Ã£o do artefato) e o que falta (item 8 ainda nÃ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃ§Ã£o â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta**: ver seÃ§Ã£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃ§Ã£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: ver "Item 3 â€” CONCLUÃDO nesta sessÃ£o" na seÃ§Ã£o M7 acima (topo do arquivo) para o detalhe completo â€” `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃ§Ã£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃ§Ã£o "pendÃªncias explÃ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real â€” nÃ£o bloqueia implementaÃ§Ã£o, sÃ³ produÃ§Ã£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` â€” o Ãºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) â€” idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃª o `ExtractedField` jÃ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` Ã© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) â€” lÃª os quatro, valida as 4 versÃµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃ§Ã£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` â€” primeiro 422 do projeto). `reject` Ã© OCC de 3 vias (nunca lÃª/toca `ExpirationItem` â€” nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoâ†’atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃ³ `expirationDate` -> `dueDate` no schema v1) â€” quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃ§Ã£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃ§Ã£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` â€” `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` â€” um plain read eventualmente consistente, nunca usado pelos OCC writes jÃ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃ¡ existente) reaproveitado por `documents`/`items` na composition root â€” `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` â€” `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) â€” mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` â†’ `authorize()` internamente via a application function â†’ `TenantQuotaService` `API_REQUEST` 100/60s â†’ schema Ajv â†’ chamada). AutorizaÃ§Ã£o: `extraction:confirm` (aÃ§Ã£o **jÃ¡ existia** na matriz desde uma sessÃ£o anterior â€” cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃ³pias desse mapa nos outros mÃ³dulos nÃ£o precisam, deixadas como estÃ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃ¡ tem `tenant_facing_read_write_policy_json` completo na tabela â€” zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃ£o de adapter `DynamoLike` que `ExpirationService` jÃ¡ usa para sua prÃ³pria `IdempotencyStore`, ao invÃ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` â€” o `source_arn` do `aws_lambda_permission.documents` jÃ¡ Ã© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃ­do de republish de `source_code_hash` jÃ¡ documentado em toda sessÃ£o anterior deste milestone â€” o prÃ³prio `documents-handler` bundle mudou porque o cÃ³digo mudou, os outros 15 sÃ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas â€” nÃ£o repetido o gap histÃ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` â€” `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` â€” reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃ¡lido/invÃ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos Ã  mÃ£o sobre uma tabela DynamoDB em memÃ³ria com OCC real â€” `InMemoryTable.write()` sÃ³ aplica se a versÃ£o bate) â€” feliz confirm (item atualizado + versÃµes incrementadas), feliz reject (item intocado), idempotÃªncia de replay dos dois (sem re-executar a transaÃ§Ã£o), 409 individual para cada uma das 4/3 versÃµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃ¡lido, 404 para cada uma das 4 entidades ausente, negaÃ§Ã£o de autorizaÃ§Ã£o sem membership. 2 fakes prÃ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃ§Ã£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃ­cio da sessÃ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃ aconteceu, sÃ³ a persistÃªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒO libera a quota** â€” investigado nesta sessÃ£o e confirmado correto, nÃ£o uma lacuna: como a reserva Ã© por-execuÃ§Ã£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃ³pria reserva anterior, que `startOcr` jÃ¡ trata genericamente como "jÃ¡ reservado, nÃ£o Ã© exaustÃ£o real" (linhas 86-90) â€” **independente de a reserva ter sido liberada ou nÃ£o**. Liberar aqui nÃ£o destravaria nada que jÃ¡ nÃ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃ£o exato â€” falha da chamada Bedrock em si (apÃ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃ¡ existente, confirmado). NÃ£o hÃ¡ nenhum caminho de falha PÃ“S-chamada-bem-sucedida neste handler (ao contrÃ¡rio do Textract, o resultado do Bedrock Ã© sÃ³ re-anexado ao payload de saÃ­da, nunca persistido localmente por este handler) â€” nÃ£o hÃ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃ¡ completa e correta â€” nenhum cÃ³digo novo necessÃ¡rio, nenhuma lacuna real encontrada. NÃ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador â€” fora de escopo do design aprovado, nÃ£o registrado como pendÃªncia.

## M7 â€” estado final desta sessÃ£o: cÃ³digo-completo para `dev`

Com os itens 1-9 concluÃ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃ¡ code-complete para testar em `dev`** â€” nÃ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃ£o: nÃ£o hÃ¡ nenhum mÃ³dulo/recurso do pipeline de extraÃ§Ã£o pendente de instanciaÃ§Ã£o). O Ãºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃ§Ã£o end-to-end real~~ â€” **FEITA em 2026-08-27**, ver a seÃ§Ã£o `## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃ£o, 1 documentado como decisÃ£o pendente).
- Runbook de calibraÃ§Ã£o para job Textract "preso" (mencionado no item 3) â€” nÃ£o bloqueia, Ã© operacional.

## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃ£o `StartExecution` na mÃ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃ§Ã£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` â†’ `exptrk-dev-extraction-starter-handler:live` â†’ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O Ãºnico elo originado Ã  mÃ£o foi a mensagem SQS, porque a regra EventBridge que a produziria Ã© o Ãºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃ¡-la exigiria `terraform apply` â€” proibido pelo Â§3 do `AGENTS.md`. Nada alÃ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃ£o Ã© automatizÃ¡vel por CLI. **Nenhum resultado de extraÃ§Ã£o foi falsificado** â€” todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃ§Ã£o foi produzido pelo pipeline real.

**As 3 execuÃ§Ãµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃ§Ã£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,â€¦}}`) | RunTextract(falhou) â†’ RunDeterministicParser â†’ NeedsBedrock â†’ CheckAiKillSwitch â†’ ValidateSchema â†’ CompareExtractors â†’ PersistExtractedFields â†’ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃ¡ria, envelope canÃ´nico com `OCR:true` | idÃªnticos ao anterior | SUCCEEDED, **ainda degradado** â€” evidÃªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃ¡ria, envelope canÃ´nico **+ cÃ³pia top-level** dos 3 booleanos | RunTextract â†’ RunDeterministicParser â†’ NeedsBedrock â†’ ValidateSchema â†’ CompareExtractors â†’ PersistExtractedFields â†’ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃªncia do caminho feliz (`output` real da execuÃ§Ã£o `run_c1aefâ€¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefâ€¦/42d064cf-â€¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃ­stico extraiu a data correta**. `RunBedrock` NÃƒO foi exercitado (`needsBedrock:false` â€” o candidato determinÃ­stico jÃ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃ³sito). `CompleteRun` apagou o artefato transitÃ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃ§Ã£o/quota/idempotÃªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcâ€¦` â†’ **200**, campo `PENDING_CONFIRMATION` â†’ `CONFIRMED` (`version` 1â†’2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) â†’ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃ§Ã£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` â†’ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃ£o reaplicou).
- `reject` no campo de `run_a22dfâ€¦` â†’ **200**, `PENDING_CONFIRMATION` â†’ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefâ€¦` â†’ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) â€” ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado â€” pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Â§17.3 especifica) `{"features":{"AI_EXTRACTION":â€¦,"OCR":â€¦,"WHATSAPP":â€¦}}`, mas o adapter lia `parsed.OCR` no nÃ­vel raiz â€” logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃªncia isolada: a execuÃ§Ã£o `run_a22dfâ€¦` rodou com a versÃ£o AppConfig 2 (`OCR:true` dentro do envelope canÃ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefâ€¦`, cuja Ãºnica diferenÃ§a era a cÃ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃ­vel raiz) + teste de regressÃ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados Ã  lista de imports estÃ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃ³pria doc-string manda "add both the file AND a static import line". Toda chamada Ã s duas rotas retornava 500 antes de qualquer lÃ³gica de negÃ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` â€” o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃ³ria dos testes unitÃ¡rios ignoram atributos extras na chave, por isso a suÃ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃ³ depois dessa correÃ§Ã£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` â€” GAP REAL, NÃƒO CORRIGIDO (decisÃ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃ§a â€” foi exatamente o que aconteceu com `run_c1aefâ€¦`, o caso de sucesso do pipeline. Mas o **Ãºnico** cÃ³digo que escreve `ExpirationItem.dueDate` Ã© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃ¡ `CONFIRMED` (verificado, ver acima). ConsequÃªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** Ã© atualizado e nÃ£o existe rota que o atualize. NÃ£o corrigido de propÃ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃ§Ã£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" Ã© decisÃ£o de produto/design (Type 1, Â§4 do `AGENTS.md`), nÃ£o conserto mecÃ¢nico.

### Achado menor (nÃ£o corrigido, sem impacto observado nesta verificaÃ§Ã£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, â€¦), mas o Step Functions casa contra o `errorType` do Lambda, que Ã© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, â€¦). Confirmado no histÃ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃ£o** casou â€” quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃ­ **hÃ¡** perda de comportamento (uma falha transitÃ³ria de commit nÃ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD â€” deixado para uma sessÃ£o de infra.

### Estado do ambiente apÃ³s a sessÃ£o

Flags AppConfig **revertidas**: versÃ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃµes temporÃ¡rias 2/3 deletadas â€” `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃ³ o ruÃ­do jÃ¡ conhecido de republicaÃ§Ã£o de versÃ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃµes deletadas), `ocr/` no bucket transitÃ³rio volta 0. Os histÃ³ricos das 3 execuÃ§Ãµes do Step Functions ficam (artefato normal da AWS, nÃ£o Ã© dado de teste a limpar).

**As correÃ§Ãµes dos bugs 1 e 2 estÃ£o apenas em `develop`, ainda NÃƒO implantadas em `dev`** (o CD sÃ³ aplica em push para `main`). A verificaÃ§Ã£o do `confirm` real rodou contra o bundle esbuild jÃ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃ© o prÃ³ximo merge para `main`.

**Bloqueado sÃ³ para produÃ§Ã£o real** (nÃ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃ§Ã£o de modelo Bedrock + regiÃ£o, RIPD formal (`privacy-lgpd.md` Â§6, gatilho jÃ¡ registrado).

## DecisÃµes pendentes do Marcelo

1. **M7 (extraÃ§Ã£o/OCR)**: design jÃ¡ aprovado (D-035), zero decisÃ£o de produto pendente â€” sÃ³ falta decidir *quando* comeÃ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) â€” nÃ£o retomar sem sinal explÃ­cito dele.
3. **Estado real de `git`/deploy no inÃ­cio da prÃ³xima sessÃ£o nÃ£o deve ser presumido** â€” confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃªncias residuais nÃ£o bloqueantes (registradas, nÃ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃ©-existente nÃ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ â€” **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃºmeros (9 vulnerabilidades reais, nÃ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃ£o documentada (`testcontainers`â†’`dockerode`â†’`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃ£o reverificado nesta sessÃ£o).
- Observabilidade por funÃ§Ã£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃ©rica jÃ¡ cobre "estÃ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃ§Ã£o de upload slot expirado) nunca exercitada contra AWS real~~ â€” **executada em 2026-08-25, achou um bug real de produÃ§Ã£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado â€” confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃ³s a transiÃ§Ã£o (ponteiro Ã³rfÃ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃ§Ã£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** â€” commit `2fab80f`, jÃ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃ£o deployado" estava desatualizada (verificado nesta sessÃ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso â€” sÃ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` Ã© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃ³ Ã© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃ§Ã£o defensiva do status real da occurrence antes de agir â€” um ponteiro Ã³rfÃ£o nunca Ã© revisitado fora dessa janela, e mesmo se fosse, Ã© ignorado com seguranÃ§a. Diferente do bug do `upload-slot` (falha total â€” nada funcionava), este Ã© um descompasso comentÃ¡rio-vs-cÃ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃ¡ revisado 9,2/10 pelo protocolo Claudeâ†”Codex e ativo em produÃ§Ã£o real â€” nÃ£o corrigido nesta sessÃ£o deliberadamente, para nÃ£o mexer num trecho crÃ­tico jÃ¡ extensivamente revisado sem uma rodada de revisÃ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃ§Ã£o real, Â§13-30) que, se adotada como padrÃ£o oficial, deveria passar pela mesma convergÃªncia independente Claudeâ†”Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃ¡ usaram â€” nÃ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃ£o de 12 eixos especÃ­fico do planejamento de interface, jÃ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃªncias (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)

- `docs/architecture/session-log.md` â€” linha do tempo compacta, uma entrada por sessÃ£o.
- `docs/architecture/decisions-log.md` â€” todas as decisÃµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` â€” os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` â€” o protÃ³tipo interativo executÃ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` â€” debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` â€” implementaÃ§Ã£o real do Full BFF + fundaÃ§Ã£o de frontend de produÃ§Ã£o, achados do protocolo Claudeâ†”Codex sobre o cÃ³digo, Final Status.

# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃ§Ã£o LGPD sem purga fÃ­sica real e DSR 100% design-only, ambos registrados como decisÃ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃ­ntese final com recomendaÃ§Ã£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` â€” ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃ§Ã£o/OCR) code-complete e deployado em `dev`** â€” todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃ­stico/Bedrock/validaÃ§Ã£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃ§Ã£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3â†’SQSâ†’Step Functionsâ†’Textractâ†’parserâ†’persistÃªncia, ver `NEXT_SESSION_PROMPT.md`) â€” achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃ£o corrigido e aguardando decisÃ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` â€” nenhuma rota escreve isso hoje); M12 bloqueado por decisÃ£o de produto (D-052); Full BFF â€” D-053/D-054 â€” **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo Claudeâ†”Codex completo em `docs/frontend/frontend-production-foundation.md` â€” Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃ§a de sessÃ£o atÃ© convergir); planejamento de interface concluÃ­do â€” 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃ£o **todos resolvidos** (backend + frontend onde aplicÃ¡vel â€” BLOCKER-C via Variante B, revisÃ£o humana explÃ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada â€” ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃ³xima aÃ§Ã£o concreta, este bloco pode ficar temporariamente atrÃ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃ§Ã£o nÃ£o Ã© sequencial na ordem das linhas â€” D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO â€” 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃ³ Contexto bateu o gate â‰¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.


codex
O handoff confirma que W3-07 é a próxima decisão arquitetural e que a crítica é evidência histórica, não fonte normativa. Agora vou conferir a alegação de inventário exaustivo a partir dos escritores reais — inclusive valores construídos por constantes/interpolação, que um grep literal pode omitir — e separar tabela principal da tabela BFF.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/codex-round1-critique-full.txt; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md; Get-Content -Raw docs/engineering/joint-review-criteria.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 215ms:
2026-08-28T20:12:28.618616Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a04a00-9ce1-7fb2-a2f5-4ea44a361de4
--------
user
Voce e o revisor adversarial (Codex) no protocolo Claude<->Codex deste repositorio
(expiration-tracker). Leia
docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round1.md
(desenho do mecanismo de cascata de exclusao fisica por tenantId, W3-07, escopo reduzido a
pedido do Marcelo: SO o mecanismo, sem confirmacao/exportacao/rotas HTTP/state machine DSR
completa) e critique adversarialmente contra o codigo real.

Contexto essencial ja fechado na sessao anterior (W3-06/D-061, ver
docs/architecture/reviews/w3-06-user-document-purge-design/claude-reconciliation-final-design.md
e o codigo real em src/workers/document-purge/purge.ts,
src/modules/document/ports/document-store.ts,
src/modules/document/application/document-deletion-service.ts): existe hoje um
DocumentPurgeWorker real com claim/lease sobre GSI6 (WORKSTATE#PURGE_PENDING/CLAIMED),
condicoes de claim via extraConditions em occ.ts (buildVersionedUpdate/buildVersionedDelete),
um campo legalHold em Document com exclusao mutua garantida contra o claim via
"attribute_not_exists(GSI6PK) OR GSI6PK <> WORKSTATE#PURGE_CLAIMED". A nova proposta quer
generalizar esse worker para um terceiro branch generico (qualquer entityType sem S3) e
adicionar um TenantCascadeDeletionService que descobre TODAS as linhas de um tenant via Scan
(filtro tenantId) e as enfileira no mesmo GSI6.

Verifique contra o codigo real: confirme que toda entidade realmente carrega tenantId como
atributo proprio (nao so na PK) - src/modules/*/domain/*.ts, docs/architecture/data-model.md
Â§1/Â§2. Avalie os 3 riscos que a proposta pede revisao (Scan tenant-wide como operacao rara,
generalizacao do worker para um branch generico sem S3, purgeAfter imediato sem os 30 dias).
Ache problemas REAIS: condicoes de corrida entre o Scan (nao transacional, snapshot
inconsistente) e escritas concorrentes durante a descoberta, itens novos criados APOS o Scan
mas ANTES da exclusao completar (um usuario ainda usando o tenant enquanto a exclusao roda em
background - nao ha bloqueio imediato, e a propria proposta admite isso como fora de escopo,
mas isso pode ser uma falha de design mais serio do que a proposta reconhece), paginacao de
Scan grande (milhares de itens) e timeout de Lambda, TransactWriteItems em lotes de 25 sobre
linhas que podem ja ter sido apagadas por outro processo entre o Scan e o batch write,
seguranca do worker generico apagar um tipo de entidade que na verdade precisava de um efeito
colateral especifico (ex: NotificationOutcome/OutboxEvent que talvez devessem ser preservados
por auditoria, nao apagados).

De uma nota de 0 a 10 (sem arredondar) e liste achados bloqueantes vs nao-bloqueantes.
Formato: ## Achados bloqueantes / ## Achados nao-bloqueantes / ## Nota: X/10 / ## Veredito.

codex
Vou fazer a revisÃ£o adversarial confrontando a proposta com as fontes normativas e o cÃ³digo, incluindo a cobertura real de `tenantId`, sem extrapolar o escopo reduzido. Como isto Ã© uma etapa formal do protocolo, tambÃ©m vou aplicar a rubrica de engenharia indicada pelo repositÃ³rio.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round1.md; Get-Content -Raw docs/architecture/reviews/w3-06-user-document-purge-design/claude-reconciliation-final-design.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 247ms:
# Expiration Tracker Ã¢â‚¬â€ Status e PrÃƒÂ³xima SessÃƒÂ£o

> Este arquivo ÃƒÂ© estado atual + prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o (`AGENTS.md` Ã‚Â§2), nÃƒÂ£o histÃƒÂ³rico. Para a linha do tempo completa por sessÃƒÂ£o, ver `docs/architecture/session-log.md`; para toda decisÃƒÂ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃƒÂ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Ã‚Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) Ã¢â‚¬â€ confirmar `git log`/branch atual antes de assumir que o PR jÃƒÂ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃƒÂ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program Ã¢â‚¬â€ Waves 0/2/3/5 concluÃƒÂ­das, Wave 4 parcial (gated), Wave 1 nÃƒÂ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃƒÂ£o de consolidaÃƒÂ§ÃƒÂ£o/pilot-readiness. **Backlog canÃƒÂ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃƒÂ­ntese final/recomendaÃƒÂ§ÃƒÂ£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃƒÂ¡vel do prompt mestre Ã‚Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) Ã¢â‚¬â€ nÃƒÂ£o duplicar detalhe aqui, sÃƒÂ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃƒÂ§ÃƒÂ£o) `DONE`. **Wave 2 (evidÃƒÂªncia operacional) `DONE`** (2026-08-28) Ã¢â‚¬â€ os 6 drills (W2-03 a W2-08) executados com evidÃƒÂªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO Ã¢â€°Ë†3min44s, RPO nÃƒÂ£o medido Ã¢â‚¬â€ item aberto), load test real (977 invocaÃƒÂ§ÃƒÂµes, cota `API_REQUEST` segurando sob carga), alarme forÃƒÂ§ado com notificaÃƒÂ§ÃƒÂ£o SNSÃ¢â€ â€™e-mail real confirmada. **RPO (W2-06), dedupe pÃƒÂ³s-commit (W2-05) e o pipeline de alarme do W2-08 fechados na mesma sessÃƒÂ£o (2026-08-28)** Ã¢â‚¬â€ ver `pilot-readiness-program.md` seÃƒÂ§ÃƒÂ£o Wave 2 para o detalhe completo (inclui o achado real de que "credential-compromise via HTTP" ÃƒÂ© estruturalmente inalcanÃƒÂ§ÃƒÂ¡vel hoje, nÃƒÂ£o uma lacuna). Claims especÃƒÂ­ficas por drill: ver `docs/engineering/test-engineering-standard.md` Ã‚Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada. **`W3-06-DECISION` fechada e implementada nesta sessÃƒÂ£o (2026-08-28)**: desenho do mecanismo de purga real de `USER_DOCUMENT` aprovado via protocolo ClaudeÃ¢â€ â€Codex completo (6 rodadas, Codex 9,2/Claude 9,1, D-061 em `decisions-log.md`) e implementado de ponta a ponta Ã¢â‚¬â€ `DocumentPurgeWorker` real (claim/lease sobre GSI6, apaga o objeto S3 real via `cleanObject`/evidÃƒÂªncia Ã¢â‚¬â€ nunca `quarantineObject` Ã¢â‚¬â€ e a linha `Document`, grava `DocumentPurgeReceipt` nÃƒÂ£o sensÃƒÂ­vel), agendado a cada 6h, verificado com `terraform plan`/`terraform test` reais contra `dev` (ver `docs/architecture/reviews/w3-06-user-document-purge-design/`). Wave 4 (Identity/RBAC) `PARTIAL` Ã¢â‚¬â€ confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃƒÂ£o de negÃƒÂ³cio jÃƒÂ¡ registrada (`AGENTS.md` Ã‚Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) Ã¢â‚¬â€ `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃƒÂ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃƒÂ£o (2026-08-28) Ã¢â‚¬â€ auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard Ã¢â‚¬â€ novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃƒÂ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados com fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa precisa da Wave 2. Protocolo ClaudeÃ¢â€ â€Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃƒÂ­cito do Marcelo, acima do 9,0 padrÃƒÂ£o) Ã¢â‚¬â€ trajetÃƒÂ³ria Codex 6.35Ã¢â€ â€™7.85Ã¢â€ â€™8.70Ã¢â€ â€™9.18Ã¢â€ â€™9.34Ã¢â€ â€™9.46Ã¢â€ â€™9.48Ã¢â€ â€™**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃƒÂ§ÃƒÂ£o D-0xx). EvidÃƒÂªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃƒÂ£o vale para qualquer teste/drill novo a partir de agora** Ã¢â‚¬â€ antes de declarar um teste ou drill "concluÃƒÂ­do", checar contra os gates de Ã‚Â§3 (nunca reversÃƒÂ£o silenciosa, nunca claim mais ampla que a evidÃƒÂªncia).

**RecomendaÃƒÂ§ÃƒÂ£o da sÃƒÂ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃƒÂªs gates reais nomeados na sÃƒÂ­ntese original Ã¢â‚¬â€ guest trust (`GTR-01`/`W5-01`), evidÃƒÂªncia operacional (Wave 2), e retenÃƒÂ§ÃƒÂ£o/purga real (`W3-06`) Ã¢â‚¬â€ **fecharam todos nesta sessÃƒÂ£o**, e todos os itens secundÃƒÂ¡rios de Wave 2 (RPO, dedupe pÃƒÂ³s-commit, pipeline de alarme de credential-compromise) tambÃƒÂ©m fecharam na mesma sessÃƒÂ£o. ÃƒÅ¡nico item nÃƒÂ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).

954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃƒÂ§ÃƒÂ£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃƒÂ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃƒÂ§ÃƒÂ£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃƒÂ£o** (W2-06 RPO, W2-05 dedupe pÃƒÂ³s-commit, W2-08 pipeline de alarme). **PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o real**: W3-07 (cascata DSR completa Ã¢â‚¬â€ hoje sÃƒÂ³ flip de status por entidade, sem cascata nem purga fÃƒÂ­sica; pode reusar o mesmo mecanismo GSI6 claim/lease + `DocumentPurgeWorker` do W3-06) Ã¢â‚¬â€ feature de produto maior, nÃƒÂ£o bloqueia pilot readiness estreito, precisa do mesmo rigor de desenho (protocolo ClaudeÃ¢â€ â€Codex, Type 1) antes de implementar.

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃƒÂ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃƒÂ³rio e passou a ter a primeira linguagem visual real do produto Ã¢â‚¬â€ arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃƒÂ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃƒÂ§ÃƒÂ£o **Operational Calm** aplicada ÃƒÂ s 5 superfÃƒÂ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION Ã¢â‚¬â€ PROVISIONAL PENDING USER VALIDATION` Ã¢â‚¬â€ a palavra PROVISIONAL ÃƒÂ© literal: nenhuma sessÃƒÂ£o com usuÃƒÂ¡rio aconteceu, e as **15** hipÃƒÂ³teses de design (hierarquia de navegaÃƒÂ§ÃƒÂ£o, densidade final, rÃƒÂ³tulos, colocaÃƒÂ§ÃƒÂ£o de aÃƒÂ§ÃƒÂµes secundÃƒÂ¡rias, accent, e as duas afirmaÃƒÂ§ÃƒÂµes subjetivas sobre a prÃƒÂ³pria direÃƒÂ§ÃƒÂ£o) estÃƒÂ£o registradas como adiamentos explÃƒÂ­citos no Ã‚Â§35 do documento, nÃƒÂ£o como fatos. **Zero dependÃƒÂªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de ÃƒÂ­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃƒÂ§a estrutural ÃƒÂºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃƒÂ¢ntica com urgÃƒÂªncia e situaÃƒÂ§ÃƒÂ£o em colunas separadas Ã¢â‚¬â€ mesmos dados/ordenaÃƒÂ§ÃƒÂ£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃƒÂªnticos, 3 grupos de urgÃƒÂªncia) foi verificada contra o cÃƒÂ³digo real do frontend **pela primeira vez** (antes sÃƒÂ³ contra o protÃƒÂ³tipo), com asserÃƒÂ§ÃƒÂ£o automatizada. Protocolo ClaudeÃ¢â€ â€Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 Ã¢â‚¬â€ `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃƒÂ§ÃƒÂ£o normal, entÃƒÂ£o o botÃƒÂ£o de submit continuava clicÃƒÂ¡vel durante a mutaÃƒÂ§ÃƒÂ£o exibindo "RenovandoÃ¢â‚¬Â¦"; e os cabeÃƒÂ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃƒÂ§am linhas Ã¢â‚¬â€ ambos corrigidos com teste de regressÃƒÂ£o. O protocolo **nÃƒÂ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃƒÂ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Ã‚Â§4) Ã¢â‚¬â€ o protocolo mais longo jÃƒÂ¡ executado neste repositÃƒÂ³rio. TrÃƒÂªs rodadas acharam defeitos **criados pela rodada de correÃƒÂ§ÃƒÂ£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃƒÂ£o foi cÃƒÂ³digo errado e sim **documentaÃƒÂ§ÃƒÂ£o afirmando prova mais ampla que a evidÃƒÂªncia nomeada**, que apareceu seis vezes: a alegaÃƒÂ§ÃƒÂ£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃƒÂ§ÃƒÂ£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃƒÂµes futuras deste repositÃƒÂ³rio. Acessibilidade virou executÃƒÂ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃƒÂ§ÃƒÂµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃƒÂ§ÃƒÂµes declaradas e registro de decisÃƒÂµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃƒÂ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃƒÂ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃƒÂªncia real registrada, nÃƒÂ£o bloqueante**: as baselines de regressÃƒÂ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃƒÂ£o por plataforma, entÃƒÂ£o o projeto Playwright `visual` ÃƒÂ© deliberadamente separado de `npm run test:e2e` e **nÃƒÂ£o** estÃƒÂ¡ gatilhado no CI hoje (plugÃƒÂ¡-lo falharia por baseline ausente, nÃƒÂ£o por regressÃƒÂ£o real). O caminho de adoÃƒÂ§ÃƒÂ£o estÃƒÂ¡ escrito no Ã‚Â§31 do documento: gravar as baselines num runner Linux, commitÃƒÂ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃƒÂ© lÃƒÂ¡ o CI cobre as mesmas superfÃƒÂ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃƒÂ©m nÃƒÂ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃƒÂ­veis no ambiente) Ã¢â‚¬â€ `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos Ã¢â‚¬â€ Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration Ã¢â‚¬â€ sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real Ã¢â€ â€™ BFF real Ã¢â€ â€™ API real Ã¢â€ â€™ persistÃƒÂªncia real). Nenhuma rota nova de BFF foi necessÃƒÂ¡ria (todas as 6 rotas de item jÃƒÂ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo ClaudeÃ¢â€ â€Codex (Round B adversarial achou 4 bugs reais Ã¢â‚¬â€ 1 S1: corrida TOCTOU real na reaquisiÃƒÂ§ÃƒÂ£o de um registro de idempotÃƒÂªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃƒÂ§ÃƒÂ£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃƒÂ³ `idempotency.complete()` falhasse depois], hash de renovaÃƒÂ§ÃƒÂ£o ambÃƒÂ­guo quando `cycle` ÃƒÂ© enviado independente de `newDueDate`, bug de fuso horÃƒÂ¡rio na formataÃƒÂ§ÃƒÂ£o de data da `Overview` Ã¢â‚¬â€ todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃƒÂ§ÃƒÂ£o tambÃƒÂ©m foi corrigido um bug real prÃƒÂ©-existente de liveness de idempotÃƒÂªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃƒÂªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) Ã¢â‚¬â€ a correÃƒÂ§ÃƒÂ£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃƒÂ§ado pela correÃƒÂ§ÃƒÂ£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃƒÂ§ÃƒÂ£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃƒÂ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃƒÂ§ÃƒÂ£o), 621 testes de backend Ã¢â‚¬â€ nenhuma regressÃƒÂ£o. Pendente ao fim desta sessÃƒÂ£o: push, PR para `develop`, CI, merge (ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" abaixo se ainda nÃƒÂ£o concluÃƒÂ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) Ã¢â‚¬â€ todos deployados em `main`/`dev`. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) teve implementaÃƒÂ§ÃƒÂ£o iniciada em 2026-08-25** (D-057, decisÃƒÂ£o direta do Marcelo Ã¢â‚¬â€ "prossiga entÃƒÂ£o") Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃƒÂ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃƒÂ£o disparou.

**Full BFF (autenticaÃƒÂ§ÃƒÂ£o de browser)**: design fechado via protocolo ClaudeÃ¢â€ â€Codex em duas rodadas Ã¢â‚¬â€ D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃƒÂ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` Ã¢â‚¬â€ 76 testes unitÃƒÂ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o real (`frontend/` Ã¢â‚¬â€ Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃƒÂ¡rio/componente + 6 smoke E2E via Playwright). Protocolo ClaudeÃ¢â€ â€Codex completo (Rodada D levou **6 passagens** atÃƒÂ© convergir Ã¢â‚¬â€ 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃƒÂ³digo prÃƒÂ©-correÃƒÂ§ÃƒÂ£o: consumo nÃƒÂ£o-atÃƒÂ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃƒÂ£o revogada concorrentemente; releituras pÃƒÂ³s-refresh nÃƒÂ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃƒÂ£o checando expiraÃƒÂ§ÃƒÂ£o/idle-TTL antes de agir; idle-TTL da prÃƒÂ³pria `Session` nunca validado contra o relÃƒÂ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃƒÂªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Ã‚Âº-8Ã‚Âº), cada um fechado via protocolo ClaudeÃ¢â€ â€Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` Ã¢â‚¬â€ papÃƒÂ©is, JTBD, inventÃƒÂ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` Ã¢â‚¬â€ modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` Ã¢â‚¬â€ 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` Ã¢â‚¬â€ 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes SurfaceÃ¢â€ â€Journey/Concept/Transition. RevisÃƒÂ£o Codex achou 3 furos reais de mesma causa raiz Ã¢â‚¬â€ `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃƒÂ§a em `SCANNING`, nÃƒÂ£o sÃƒÂ³ em `CLEAN`) Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` Ã¢â‚¬â€ wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃƒÂ£o Codex achou 5 furos reais + 1 divergÃƒÂªncia factual Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o primÃƒÂ¡ria ambÃƒÂ­gua em 4 coleÃƒÂ§ÃƒÂµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃƒÂ³digo executÃƒÂ¡vel real, HTML/CSS/JS sem dependÃƒÂªncias) Ã¢â‚¬â€ protÃƒÂ³tipo navegÃƒÂ¡vel cobrindo as 17 superfÃƒÂ­cies e J-01Ã¢â‚¬â€œJ-08 com 34 Prototype Scenario IDs determinÃƒÂ­sticos, verificado com testes automatizados de navegador headless. RevisÃƒÂ£o Codex achou 6 furos reais de 4 causas raiz Ã¢â‚¬â€ compressÃƒÂ£o de estados no guest upload (faltava validaÃƒÂ§ÃƒÂ£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃƒÂ§ÃƒÂ£o de coleta externa violando epistemic integrity (anunciava verificaÃƒÂ§ÃƒÂ£o de seguranÃƒÂ§a concluÃƒÂ­da ao operador, que nÃƒÂ£o pode observar isso), dois campos sem `<label>`, e reincidÃƒÂªncia de menÃƒÂ§ÃƒÂ£o a WhatsApp Ã¢â‚¬â€ todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` Ã¢â‚¬â€ avaliaÃƒÂ§ÃƒÂ£o do protÃƒÂ³tipo **executÃƒÂ¡vel** (nÃƒÂ£o sÃƒÂ³ a documentaÃƒÂ§ÃƒÂ£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃƒÂ¢ntica/forms testados em navegador headless real, nÃƒÂ£o sÃƒÂ³ lidos), `axe-core` (0 violaÃƒÂ§ÃƒÂµes em todos os estados testados, nas duas rodadas), re-execuÃƒÂ§ÃƒÂ£o de J-01Ã¢â‚¬â€œJ-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** ClaudeÃ¢â€ â€Codex (nÃƒÂ£o as 2-3 rodadas tÃƒÂ­picas de etapas anteriores): Rodada A (autoavaliaÃƒÂ§ÃƒÂ£o, 9 achados corrigidos) concluiu aprovaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃƒÂ£o vistos pela autoavaliaÃƒÂ§ÃƒÂ£o: o mais grave, a prÃƒÂ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃƒÂ§ÃƒÂ£o em 3 formulÃƒÂ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃƒÂ§ÃƒÂ£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃƒÂ¡-los no fake-backend Ã¢â‚¬â€ uma violaÃƒÂ§ÃƒÂ£o epistÃƒÂªmica real, nÃƒÂ£o cosmÃƒÂ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃƒÂ³prio ao manter um texto "SIMULATED FOR UX VALIDATION Ã¢â‚¬â€ NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃƒÂ­cie pÃƒÂºblica de guest (jargÃƒÂ£o tÃƒÂ©cnico visÃƒÂ­vel a um usuÃƒÂ¡rio externo nÃƒÂ£o-tÃƒÂ©cnico). Rodada D (nova adversarial sobre o cÃƒÂ³digo jÃƒÂ¡ corrigido) achou essa lacuna de conteÃƒÂºdo e uma correÃƒÂ§ÃƒÂ£o incompleta nova (`submitAlert` reabilitava o botÃƒÂ£o sÃƒÂ³ na branch de erro, nÃƒÂ£o na de sucesso) Ã¢â‚¬â€ ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃƒÂ£o arredondado; calculado refletindo esse histÃƒÂ³rico de 4 rodadas, nÃƒÂ£o sÃƒÂ³ o estado final do cÃƒÂ³digo) Ã¢â‚¬â€ `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` Ã¢â‚¬â€ "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃƒÂ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃƒÂ£o) no protÃƒÂ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃƒÂ§ÃƒÂ£o, nunca marcado como backend-resolvido; cenÃƒÂ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃƒÂ§ÃƒÂµes) que achou e corrigiu uma falta real de ordenaÃƒÂ§ÃƒÂ£o por urgÃƒÂªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃƒÂ£o sÃƒÂ³ no protÃƒÂ³tipo) Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃƒÂ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃƒÂ¡ em uso desde a 1Ã‚Âª etapa, agora formal, nenhum critÃƒÂ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃƒÂ§ÃƒÂ£o tÃƒÂ©cnica em Participant Mode, teste de idempotÃƒÂªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃƒÂµes de documentaÃƒÂ§ÃƒÂ£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃƒÂ©-existentes desde a Rodada A) Ã¢â‚¬â€ corrigidos no fechamento, zero contaminaÃƒÂ§ÃƒÂ£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃƒÂ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** Ã¢â‚¬â€ **fechado nesta sessÃƒÂ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` Ã¢â‚¬â€ `DocumentService.listDocuments`/`getDocument`, autorizaÃƒÂ§ÃƒÂ£o `document:read` (aÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃƒÂ§ÃƒÂ£o do item jÃƒÂ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃƒÂ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` Ã¢â‚¬â€ `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃƒÂ§ÃƒÂ£o `requirement:read` jÃƒÂ¡ existente (sem action nova reservada de antemÃƒÂ£o, diferente de `document:read`), mesma leitura por partiÃƒÂ§ÃƒÂ£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃƒÂ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© desta sessÃƒÂ£o, jÃƒÂ¡ estava corrigido antes (comentÃƒÂ¡rio no prÃƒÂ³prio `main.tf`), sÃƒÂ³ confirmado que segue correto.

  Ambas as metades: mudanÃƒÂ§a mecÃƒÂ¢nica sobre um padrÃƒÂ£o de acesso jÃƒÂ¡ aprovado no data model Ã¢â‚¬â€ nÃƒÂ£o passaram pelo protocolo `AGENTS.md` Ã‚Â§4 completo (nÃƒÂ£o ÃƒÂ© decisÃƒÂ£o de arquitetura nova, ÃƒÂ© fechamento de gap de leitura jÃƒÂ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃƒÂ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃƒÂ­cio da sessÃƒÂ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃƒÂ£o. **Achado real corrigido depois, na mesma sessÃƒÂ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` Ã¢â‚¬â€ mesma classe de gap que as rotas de `DocumentRequest` jÃƒÂ¡ tiveram antes (comentÃƒÂ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** Ã¢â‚¬â€ **implementaÃƒÂ§ÃƒÂ£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃƒÂ§ÃƒÂ£o abaixo). Antes desta branch, a materializaÃƒÂ§ÃƒÂ£o automÃƒÂ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃƒÂ§ÃƒÂ£o/ediÃƒÂ§ÃƒÂ£o de item. PendÃƒÂªncia real restante: decisÃƒÂ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) Ã¢â‚¬â€ nÃƒÂ£o bloqueia mais nada alÃƒÂ©m dessa decisÃƒÂ£o em si.
- **BLOCKER-C** Ã¢â‚¬â€ **decisÃƒÂ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃƒÂ£o humana explÃƒÂ­cita)**, confirmando a hipÃƒÂ³tese lÃƒÂ­der jÃƒÂ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Ã‚Â§22, `interface-conceptual-model-and-information-architecture.md` Ã‚Â§37, `interface-critical-user-journeys.md` Ã‚Â§37 Ã¢â‚¬â€ esses documentos continuam como registro histÃƒÂ³rico do debate, nÃƒÂ£o reescritos, per `AGENTS.md` regra de precedÃƒÂªncia de `history/`/artefatos aprovados). InvestigaÃƒÂ§ÃƒÂ£o confirmou que Variante A (fechamento automÃƒÂ¡tico) nÃƒÂ£o ÃƒÂ© viÃƒÂ¡vel hoje: `DocumentSubmission` nÃƒÂ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃƒÂ§a a qual `ExpirationItem` uma submissÃƒÂ£o pertence Ã¢â‚¬â€ decidir automaticamente seria adivinhar, nÃƒÂ£o resolver. **Mecanismo de backend para a Variante B jÃƒÂ¡ existe, nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio**: visibilidade do operador sobre submissÃƒÂµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃƒÂ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existente (`POST .../link`, prÃƒÂ©-existente). **Frontend implementado na mesma sessÃƒÂ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject Ã¢â‚¬â€ `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃƒÂ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores Ã¢â€ â€™ detalhe com requisitos Ã¢â€ â€™ "Revisar" expande evidÃƒÂªncia enviada (`DocumentSubmission`) Ã¢â€ â€™ operador informa o `itemId` de um vencimento jÃƒÂ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃƒÂ§ÃƒÂ£o de subject nem sugestÃƒÂ£o automÃƒÂ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃƒÂ³tulos seguem a mesma disciplina de Epistemic Integrity jÃƒÂ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃƒÂ§a) Ã¢â‚¬â€ conteÃƒÂºdo nÃƒÂ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta ÃƒÂºltima etapa: **`GTR-01`** (identidade do solicitante nÃƒÂ£o exposta ao guest) agora **simulada** no protÃƒÂ³tipo para User Validation, mas ainda nÃƒÂ£o resolvida no backend Ã¢â‚¬â€ `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃƒÂªncia) foi **resolvido no backend real** Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional Ã¢â‚¬â€ e, na sessÃƒÂ£o do Full BFF/frontend, o `requestHash` de detecÃƒÂ§ÃƒÂ£o de payload divergente foi migrado de concatenaÃƒÂ§ÃƒÂ£o por delimitador (colisÃƒÂ£o real possÃƒÂ­vel) para JSON canÃƒÂ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` Ã¢â‚¬â€ a lacuna "nenhum frontend/BFF real envia o header" estÃƒÂ¡ fechada para o caminho de criaÃƒÂ§ÃƒÂ£o de item.

**PrÃƒÂ³xima etapa natural do design** (ainda nÃƒÂ£o iniciada): **User Validation** Ã¢â‚¬â€ usando o protÃƒÂ³tipo jÃƒÂ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode ÃƒÂ© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Ã‚Â§15-18/Ã‚Â§23-24 (tese de produto, mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o, tarefas candidatas derivadas de outcomes, limitaÃƒÂ§ÃƒÂµes conhecidas do protÃƒÂ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃƒÂ£o produzido ainda. NÃƒÂ£o obriga inÃƒÂ­cio imediato de Visual Design/Design System.

## BLOCKER-B Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃƒÂ£o de 2026-08-24/25 (continuaÃƒÂ§ÃƒÂ£o apÃƒÂ³s a sessÃƒÂ£o anterior ter sido abortada por limite de tokens sÃƒÂ³ com reconhecimento parcial Ã¢â‚¬â€ handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃƒÂ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo ClaudeÃ¢â€ â€Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` Ã¢â‚¬â€ esse documento ÃƒÂ© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃƒÂ§ÃƒÂ£o real (nÃƒÂ£o sÃƒÂ³ design) tambÃƒÂ©m revisada via protocolo ClaudeÃ¢â€ â€Codex (2 rodadas, nota final 9,2/10, `APPROVED` Ã¢â‚¬â€ achados reais corrigidos: reclassificaÃƒÂ§ÃƒÂ£o de exceÃƒÂ§ÃƒÂ£o em dois mÃƒÂ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃƒÂ§ÃƒÂ£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃƒÂ§ÃƒÂ£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃƒÂªs novos mÃƒÂ©todos de reconciliaÃƒÂ§ÃƒÂ£o em `ReminderMaterializer` (fenced por versÃƒÂ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃƒÂ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃƒÂ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃƒÂ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃƒÂ§ÃƒÂ£o: o "caminho genÃƒÂ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃƒÂ³digo Ã¢â‚¬â€ `router_queue` nÃƒÂ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃƒÂ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃƒÂ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃƒÂ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Ã‚Â§8). **Implementado nesta sessÃƒÂ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃƒÂ§ÃƒÂ£o da criaÃƒÂ§ÃƒÂ£o do item (sem `ConditionCheck` de existÃƒÂªncia Ã¢â‚¬â€ o item novo ÃƒÂ© garantido pela prÃƒÂ³pria transaÃƒÂ§ÃƒÂ£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃƒÂ³ `ExpirationItem` (mudanÃƒÂ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" Ã¢â‚¬â€ reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃƒÂ£o estava instalado no inÃƒÂ­cio desta sessÃƒÂ£o (`npm ci` rodado para poder validar) Ã¢â‚¬â€ ambiente, nÃƒÂ£o cÃƒÂ³digo.

**PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o**: **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ todos os 9 itens numerados originais da lista estÃƒÂ£o CONCLUÃƒÂDOS (2026-08-26)**, cÃƒÂ³digo+infra reais para itens 1-9, nÃƒÂ£o sÃƒÂ³ camada pura Ã¢â‚¬â€ ver as seÃƒÂ§ÃƒÂµes dedicadas "Item N Ã¢â‚¬â€ CONCLUÃƒÂDO" na seÃƒÂ§ÃƒÂ£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Ã‚Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃƒÂ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) Ã¢â‚¬â€ causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃƒÂ­ficos com `States.ALL` num ÃƒÂºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃƒÂ£o pega esse erro, sÃƒÂ³ a chamada real de criaÃƒÂ§ÃƒÂ£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** Ã¢â‚¬â€ o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃƒÂ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃƒÂ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃƒÂ§ÃƒÂ£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃƒÂ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** Ã¢â‚¬â€ a nota de uma sessÃƒÂ£o anterior dizendo "nenhum profile corresponde ÃƒÂ  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃƒÂµes de escrita necessÃƒÂ¡rias e a **verificaÃƒÂ§ÃƒÂ£o E2E real foi executada** (ver `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃƒÂ§a de Terraform foi cogitada para isso Ã¢â‚¬â€ a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃƒÂ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃƒÂ³ o gatilho automÃƒÂ¡tico via EventBridge ÃƒÂ© que ÃƒÂ© gated). **RecorrÃƒÂªncia fechada na mesma sessÃƒÂ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃƒÂ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` Ã¢â‚¬â€ a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃƒÂ³ descoberto no meio de um `terraform apply` real. NÃƒÂ£o requer credencial de escrita (ÃƒÂ© uma chamada de validaÃƒÂ§ÃƒÂ£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃƒÂ£o retomar sem sinal explÃƒÂ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃƒÂ¡ aprovada em nÃƒÂ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Ã‚Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃƒÂ¡vel por hash; `implementation-blueprint.md` Ã‚Â§12/Ã‚Â§23: CSP com hashes estÃƒÂ¡ticos calculados no build, sem nonce dinÃƒÂ¢mico Ã¢â‚¬â€ Day 0 nÃƒÂ£o tem compute de borda). O que falta ÃƒÂ© o desenho de implementaÃƒÂ§ÃƒÂ£o real e o cÃƒÂ³digo Ã¢â‚¬â€ nenhuma decisÃƒÂ£o de produto pendente, mas hÃƒÂ¡ **uma decisÃƒÂ£o de arquitetura real ainda em aberto** que a prÃƒÂ³xima sessÃƒÂ£o precisa resolver antes de implementar (Type 1, nÃƒÂ­vel 5-6 da escala de risco Ã¢â‚¬â€ protocolo `AGENTS.md` Ã‚Â§4 aplicÃƒÂ¡vel):

1. **DecisÃƒÂ£o de arquitetura a fechar primeiro Ã¢â‚¬â€ FECHADA nesta sessÃƒÂ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo ClaudeÃ¢â€ â€Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** Ã¢â‚¬â€ `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` ÃƒÂ© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃƒÂªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma ÃƒÂºnica distribution, behaviors `/bff` e `/bff/*` Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior Ã¢â€ â€™ S3 com OAC + CloudFront Function de SPA routing (sÃƒÂ³ nesse behavior, nunca no do BFF). Zero mudanÃƒÂ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃƒÂ©-existentes descobertos pela revisÃƒÂ£o, a aplicar na implementaÃƒÂ§ÃƒÂ£o: `proxy-service.ts` nÃƒÂ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃƒÂ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃƒÂ£o implementado): acesso direto ao `execute-api` pÃƒÂºblico precisa de mitigaÃƒÂ§ÃƒÂ£o (header estÃƒÂ¡tico + WAF) antes de produÃƒÂ§ÃƒÂ£o pÃƒÂºblica real.
2. **MÃƒÂ³dulo Terraform `infra/modules/spa-hosting` Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃƒÂºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃƒÂ­citos Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default Ã¢â€ â€™ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃƒÂ©todo GET/HEAD, associada SÃƒâ€œ ao default behavior Ã¢â‚¬â€ nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃƒÂ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃƒÂ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃƒÂ©-existentes do ADR-0011 tambÃƒÂ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃƒÂ§ÃƒÂ£o errada do prÃƒÂ³prio ADR-0011): `var.spa_content_security_policy` nÃƒÂ£o precisa de hashes calculados por build Ã¢â‚¬â€ verificado contra o `frontend/dist/index.html` real, o build do Vite nÃƒÂ£o emite nenhum `<script>`/`<style>` inline, todo asset ÃƒÂ© carregado por URL externa jÃƒÂ¡ hasheada, entÃƒÂ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃƒÂ¡ existe em `frontend/index.html`) bastam sem hash nenhum Ã¢â‚¬â€ a variÃƒÂ¡vel ganhou um default determinÃƒÂ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃƒÂ£o consegue expressar). **MÃƒÂ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) Ã¢â‚¬â€ corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃƒÂ£o bloqueia o primeiro apply, sÃƒÂ³ o domÃƒÂ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro Ã¢â‚¬â€ confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃƒÂ£o sÃƒÂ³ contra mocks.
3. **Deploy imutÃƒÂ¡vel por hash Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: Vite jÃƒÂ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra ÃƒÂ© necessÃƒÂ¡rio (o hash no nome do arquivo jÃƒÂ¡ ÃƒÂ© o mecanismo de imutabilidade). `index.html` ÃƒÂ© tratado ÃƒÂ  parte (nÃƒÂ£o hasheado, `Cache-Control: no-cache`, upload por ÃƒÂºltimo).
4. **IntegraÃƒÂ§ÃƒÂ£o com `cd.yml` Ã¢â‚¬â€ IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃƒÂ£o um job novo) Ã¢â‚¬â€ `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** Ã¢â‚¬â€ assets antigos nunca sÃƒÂ£o removidos, ÃƒÂ© isso que mantÃƒÂ©m um rollback de `index.html` funcional, jÃƒÂ¡ que as referÃƒÂªncias antigas continuam servÃƒÂ­veis), upload de `index.html` por ÃƒÂºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃƒÂ¡ existente. **PendÃƒÂªncia real registrada, nÃƒÂ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃƒÂ³ cobre Lambdas Ã¢â‚¬â€ nÃƒÂ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃƒÂ£o `rollback.yml` (mecanismo existente) nÃƒÂ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃƒÂ£o bloqueia o deploy normal.
5. **VerificaÃƒÂ§ÃƒÂ£o real (Camada 3) Ã¢â‚¬â€ CONCLUÃƒÂDA (2026-08-25), achou e corrigiu um bug real de produÃƒÂ§ÃƒÂ£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃƒÂ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** Ã¢â‚¬â€ isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃƒÂ­tica fixa nÃƒÂ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃƒÂ§ÃƒÂ£o, tudo confirmado contra a distribution real:
   - `GET /` Ã¢â€ â€™ 200, `text/html`.
   - `GET /assets/index-*.js` Ã¢â€ â€™ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃƒÂ£o, V6 do ADR-0011) Ã¢â€ â€™ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) Ã¢â€ â€™ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) Ã¢â‚¬â€ nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) Ã¢â€ â€™ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃƒÂ£o (V4) Ã¢â€ â€™ 401 `application/json` (`AUTH_REQUIRED`) Ã¢â‚¬â€ nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃƒÂ£o (V5-404) Ã¢â€ â€™ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado Ã¢â‚¬â€ o caso 404-por-allowlist com sessÃƒÂ£o vÃƒÂ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃƒÂ£o foram exercitados por nÃƒÂ£o serem automatizÃƒÂ¡veis via `curl` sozinho; a propriedade crÃƒÂ­tica de seguranÃƒÂ§a Ã¢â‚¬â€ nenhum erro do BFF vira HTML da SPA Ã¢â‚¬â€ jÃƒÂ¡ estÃƒÂ¡ confirmada pelos casos acima).
   - Headers de seguranÃƒÂ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃƒÂ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy Ã¢â‚¬â€ exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃƒÂ£o sÃƒÂ³ via `terraform plan`.
   **PendÃƒÂªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃƒÂ£o lidos via CloudFront/OAC (sÃƒÂ³ por IAM de role de Lambda), entÃƒÂ£o nÃƒÂ£o tÃƒÂªm este bug Ã¢â‚¬â€ mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃƒÂ£o corrigido preventivamente nesta sessÃƒÂ£o (fora do escopo desta decisÃƒÂ£o, nenhum uso real hoje), sÃƒÂ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃƒÂ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o iniciada (2026-08-25, D-057)

Design jÃƒÂ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) Ã¢â‚¬â€ esta seÃƒÂ§ÃƒÂ£o ÃƒÂ© sobre a IMPLEMENTAÃƒâ€¡ÃƒÆ’O, o design nÃƒÂ£o muda. **Escopo ÃƒÂ© grande** (Step Functions, Textract assÃƒÂ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ vai continuar por mÃƒÂºltiplas sessÃƒÂµes, nÃƒÂ£o ÃƒÂ© razoÃƒÂ¡vel fechar num ÃƒÂºnico slice. Estado exato ao fim desta sessÃƒÂ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Ã‚Â§4 ganhou a classe `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ prÃƒÂ©-requisito de design que o prÃƒÂ³prio D-035 exige estar registrado antes de qualquer implementaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o ÃƒÂ© pendÃƒÂªncia pÃƒÂ³s-produÃƒÂ§ÃƒÂ£o).
2. MÃƒÂ³dulo novo `src/modules/extraction/domain/` (sÃƒÂ³ domÃƒÂ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` Ã¢â‚¬â€ `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃƒÂ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃƒÂ³pria chave de idempotÃƒÂªncia da execuÃƒÂ§ÃƒÂ£o).
   - `extracted-field.ts` Ã¢â‚¬â€ `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Ã‚Â§12.5.
   - `field-schema.ts` Ã¢â‚¬â€ schema v1 com sÃƒÂ³ `expirationDate` (ÃƒÂºnico campo que o design aprovado nomeia concretamente Ã¢â‚¬â€ deliberadamente nÃƒÂ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` Ã¢â‚¬â€ `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` Ã¢â‚¬â€ helper de safety net de 24h para o artefato transitÃƒÂ³rio (a exclusÃƒÂ£o real ÃƒÂ© explÃƒÂ­cita por `ExtractionValidationTaskHandler`, este helper ÃƒÂ© sÃƒÂ³ a rede de seguranÃƒÂ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃƒÂ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` Ã¢â‚¬â€ **ainda sem PR/merge para `main`**, dado que nÃƒÂ£o hÃƒÂ¡ nenhuma infra/runtime ativÃƒÂ¡vel ainda, nada a fazer deploy).

**Item 1 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃƒÂ§ÃƒÂ£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃƒÂ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃƒÂ³dulo corrigido pelo design Ã¢â‚¬â€ transversal, nÃƒÂ£o `extraction-appconfig`, jÃƒÂ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃƒÂªs defaults `false` (fail-closed, prÃƒÂ©-condiÃƒÂ§ÃƒÂµes externas de M7 nÃƒÂ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Ã‚Â§1.6, default `false`) Ã¢â‚¬â€ gate Terraform separado do pipeline inteiro, ainda nÃƒÂ£o referenciado por nenhum recurso (sÃƒÂ³ os workers futuros vÃƒÂ£o condicionar-se a ele). 2 `terraform test` do mÃƒÂ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) Ã¢â‚¬â€ CAMADA PURA CONCLUÃƒÂDA nesta sessÃƒÂ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃƒÂ£o que o item 0 seguiu: domÃƒÂ­nio+aplicaÃƒÂ§ÃƒÂ£o primeiro, depois handler Lambda/infra numa fatia posterior Ã¢â‚¬â€ deliberado, nÃƒÂ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` Ã¢â‚¬â€ cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` Ã¢â‚¬â€ entidade `TextractJob` (chave prÃƒÂ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃƒÂ£o tenant-scoped Ã¢â‚¬â€ `COMPLETE_OCR` sÃƒÂ³ tem `jobId` disponÃƒÂ­vel da notificaÃƒÂ§ÃƒÂ£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃƒÂ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃƒÂ§ÃƒÂ£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` Ã¢â‚¬â€ classificador heurÃƒÂ­stico puro (magic bytes > extensÃƒÂ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃƒÂ§a) quando nenhum sinal ÃƒÂ© conclusivo Ã¢â‚¬â€ quem decide que isso ÃƒÂ© `UnsupportedDocumentTypeError` ÃƒÂ© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed ÃƒÂ© responsabilidade do caller, nÃƒÂ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃƒÂ©todo de delete Ã¢â‚¬â€ a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃƒÂ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃƒÂ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) Ã¢â‚¬â€ orquestra classificaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ kill switch `OCR` (fail-closed) Ã¢â€ â€™ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` Ã¢â‚¬â€ um retry do mesmo run encontra `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior e trata como "jÃƒÂ¡ reservado", nÃƒÂ£o como falha real) Ã¢â€ â€™ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃƒÂ­stico) Ã¢â€ â€™ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` Ã¢â‚¬â€ retorna void, a execuÃƒÂ§ÃƒÂ£o do Step Functions fica parada no `taskToken` atÃƒÂ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃƒÂ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) Ã¢â‚¬â€ busca `TextractJob` por `jobId`; caso ÃƒÂ³rfÃƒÂ£o (nenhum job encontrado) faz UMA chamada de confirmaÃƒÂ§ÃƒÂ£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃƒÂ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃƒÂµe um mÃƒÂ©todo de delete); `taskTokenCiphertext` ÃƒÂ© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃƒÂ§ado, sem tocar no registro) em qualquer outro erro Ã¢â‚¬â€ a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto Ã¢â‚¬â€ `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃƒÂ£o ÃƒÂ© revalidado como JSON aninhado, mesma convenÃƒÂ§ÃƒÂ£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` Ã¢â‚¬â€ `RunTextract` ÃƒÂ© o ÃƒÂºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃƒÂ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃƒÂ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃƒÂ£o existem.
- `infra/modules/extraction-workflow/` Ã¢â‚¬â€ mÃƒÂ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃƒÂ§ÃƒÂ£o + role de execuÃƒÂ§ÃƒÂ£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒÆ’O chamado em `infra/main.tf`** Ã¢â‚¬â€ um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃƒÂ£o sÃƒÂ³ em runtime), e os 3 handlers dos itens 5-7 nÃƒÂ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃƒÂ³prio diretÃƒÂ³rio do mÃƒÂ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃƒÂ³dulo especificamente Ã¢â‚¬â€ pendÃƒÂªncia explÃƒÂ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos ÃƒÂ  mÃƒÂ£o (sem `vi.mock`), cobrindo: classificaÃƒÂ§ÃƒÂ£o por magic-bytes/extensÃƒÂ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃƒÂ§ÃƒÂ£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃƒÂªncia da reserva de quota sob retry do mesmo run; retry local de persistÃƒÂªncia do `TextractJob` (sucesso apÃƒÂ³s 1 falha, esgotamento apÃƒÂ³s N); `completeOcr`: job ÃƒÂ³rfÃƒÂ£o (com e sem falha na confirmaÃƒÂ§ÃƒÂ£o), jÃƒÂ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃƒÂ§ÃƒÂ£o multi-pÃƒÂ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃƒÂ³rio de `SendTask*` relanÃƒÂ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃƒÂ£o expÃƒÂµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na ÃƒÂ¡rvore inteira de `infra/`.

**Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃƒÂªs decisÃƒÂµes pendentes acima foram todas tomadas e implementadas (nÃƒÂ£o deixadas como pendÃƒÂªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃƒÂ£o final** (nÃƒÂ£o a extensÃƒÂ£o via layer) Ã¢â‚¬â€ `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/ÃƒÂºltimos flags conhecidos em memÃƒÂ³ria do processo. Motivo: a extensÃƒÂ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` Ã¢â‚¬â€ nÃƒÂ£o existe nenhum precedente disso no repo (sÃƒÂ³ `adot_layer_arn`, uma layer diferente), e este handler sÃƒÂ³ lÃƒÂª os flags uma vez por invocaÃƒÂ§ÃƒÂ£o de `START_OCR` (nÃƒÂ£o por pÃƒÂ¡gina/bloco), entÃƒÂ£o o custo/latÃƒÂªncia de uma chamada de API direta por invocaÃƒÂ§ÃƒÂ£o ÃƒÂ© equivalente ao que a extensÃƒÂ£o teria numa invocaÃƒÂ§ÃƒÂ£o fria de qualquer forma. Revisitar sÃƒÂ³ se o volume tornar isso um custo real (a extensÃƒÂ£o amortiza entre invocaÃƒÂ§ÃƒÂµes warm via cache local, este adapter sÃƒÂ³ amortiza dentro de uma sessÃƒÂ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃƒÂ£o reuso da CMK do BFF Ã¢â‚¬â€ mesma disciplina D-054: um token de callback do Step Functions ÃƒÂ© uma credencial viva tÃƒÂ£o sensÃƒÂ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃƒÂ³dulos independentemente deployÃƒÂ¡veis acoplaria rotaÃƒÂ§ÃƒÂ£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃƒÂªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃƒÂ³dulo, mesma razÃƒÂ£o do port).
- **TÃƒÂ³pico SNS de conclusÃƒÂ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar Ã¢â‚¬â€ distinta da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio Lambda.

Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃƒÂ§ÃƒÂ£o real `TaskTimedOut`/`TaskDoesNotExist`Ã¢â€ â€™`TERMINAL_QUIET`, `InvalidToken`Ã¢â€ â€™`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃƒÂ§ado), `AppConfigFeatureFlagsReader` Ã¢â‚¬â€ todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` Ã¢â‚¬â€ um ÃƒÂºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃƒÂ§ÃƒÂ£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃƒÂ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃƒÂ£o SSE-KMS Ã¢â‚¬â€ evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃƒÂ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃƒÂ³pico SNS, permissÃƒÂ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃƒÂ¡ escopada ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃƒÂ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** Ã¢â‚¬â€ mesma lÃƒÂ³gica do item 2: todo recurso aqui ÃƒÂ© inerte sozinho, porque `StartDocumentTextDetection` (a ÃƒÂºnica chamada paga) sÃƒÂ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃƒÂ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃƒÂ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27Ã¢â€ â€™28 funÃƒÂ§ÃƒÂµes (2 asserts).
- 11 testes novos (794 totais no backend) Ã¢â‚¬â€ sÃƒÂ³ para os dois adapters com lÃƒÂ³gica real de branching (`SfnTaskTokenSender`'s classificaÃƒÂ§ÃƒÂ£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃƒÂ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃƒÂ£o ganharam teste unitÃƒÂ¡rio dedicado, seguindo o MESMO precedente jÃƒÂ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃƒÂ©m nÃƒÂ£o tÃƒÂªm) Ã¢â‚¬â€ verificados via `terraform plan` real contra `dev`, nÃƒÂ£o via mock unitÃƒÂ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target Ã¢â‚¬â€ 0 erros, 0 destroys inesperados (sÃƒÂ³ o prÃƒÂ³prio slice + o ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ prÃƒÂ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Ã‚Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) Ã¢â‚¬â€ **ainda sem PR/merge para `main`** no momento em que este parÃƒÂ¡grafo foi escrito (abrir o PR ÃƒÂ© o prÃƒÂ³ximo passo imediato desta mesma sessÃƒÂ£o, ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 Ã¢â‚¬â€ a ASL's `RunDeterministicParser` ÃƒÂ© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃƒÂ£o hÃƒÂ¡ task token nenhum neste handler), entÃƒÂ£o ÃƒÂ© uma ÃƒÂºnica invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃƒÂ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` Ã¢â‚¬â€ `extractExpirationDateCandidates()`, regex determinÃƒÂ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃƒÂ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃƒÂ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃƒÂ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃƒÂ¡ a atÃƒÂ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃƒÂ§a fraca (0.5, abaixo do threshold) quando nÃƒÂ£o hÃƒÂ¡ palavra-chave prÃƒÂ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃƒÂ©rico que trunca o texto processado Ã¢â‚¬â€ a ÃƒÂºnica reutilizaÃƒÂ§ÃƒÂ£o real de M6 possÃƒÂ­vel aqui, jÃƒÂ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃƒÂ£o sobre texto OCR, e nunca extrai campos/datas (sÃƒÂ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃƒÂ³digo `DeterministicParserFailed`) Ã¢â‚¬â€ nÃƒÂ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃƒÂ¡ ÃƒÂ© o genÃƒÂ©rico `States.ALL`.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-deterministic-parser.ts` Ã¢â‚¬â€ `runDeterministicParser()`: lÃƒÂª o artefato OCR (quando presente) Ã¢â€ â€™ extrai candidatos de `expirationDate` via `getFieldSchema()` Ã¢â€ â€™ chama `needsBedrock()` (item 0) Ã¢â€ â€™ lÃƒÂª o kill switch `AI_EXTRACTION` (fail-closed) Ã¢â€ â€™ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** Ã¢â‚¬â€ retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃƒÂ¡ implica `true`) e a decisÃƒÂ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃƒÂ£o duplicada) `OcrArtifactStore` ganhou `get()` Ã¢â‚¬â€ o item 4 sÃƒÂ³ tinha `put()` (quem escreve o artefato); o item 5 ÃƒÂ© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada Ã¢â‚¬â€ sÃƒÂ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o uma decisÃƒÂ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃƒÂ£o tinha `ResultPath` na transiÃƒÂ§ÃƒÂ£o de sucesso do ASL, entÃƒÂ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃƒÂ­a inteiramente** o `$` do Step Functions Ã¢â‚¬â€ todo o contexto original da execuÃƒÂ§ÃƒÂ£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃƒÂ£o sÃƒÂ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃƒÂ³prio `job`) ao payload de sucesso. MudanÃƒÂ§a aditiva/reversÃƒÂ­vel sem migraÃƒÂ§ÃƒÂ£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃƒÂ³rico real).
- **DECISÃƒÆ’O PENDENTE (registrada aqui, default conservador aplicado)**: o design (Ã‚Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃƒÂ£o implementado Ã¢â‚¬â€ nenhum campo de metadado de PDF (data de criaÃƒÂ§ÃƒÂ£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃƒÂ£o de produto nÃƒÂ£o revisada, nÃƒÂ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃƒÂ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃƒÂ§ÃƒÂ£o na prÃƒÂ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃƒÂ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` Ã¢â‚¬â€ nÃƒÂ£o muda nenhum `Next`/fluxo, sÃƒÂ³ corrige o path de leitura para bater com a semÃƒÂ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃƒÂ§ÃƒÂ£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` Ã¢â‚¬â€ sÃƒÂ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃƒÂ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` Ã¢â‚¬â€ sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (mesmo padrÃƒÂ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28Ã¢â€ â€™29 funÃƒÂ§ÃƒÂµes (3 asserts: nomes, distinÃƒÂ§ÃƒÂ£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃƒÂ§ÃƒÂµes dia/mÃƒÂªs impossÃƒÂ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar Ã¢â‚¬â€ sÃƒÂ³ republish de versÃƒÂ£o prÃƒÂ©-existente em todas as Lambdas, mesmo ruÃƒÂ­do que o item 4 jÃƒÂ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` Ã¢â‚¬â€ drift **prÃƒÂ©-existente do item 4**, nÃƒÂ£o introduzido nesta sessÃƒÂ£o, causado por uma normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃƒÂ¡ detectava antes desta sessÃƒÂ£o comeÃƒÂ§ar; nÃƒÂ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 Ã¢â‚¬â€ ambos concluÃƒÂ­dos em sessÃƒÂµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver seÃƒÂ§ÃƒÂ£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃƒÂ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` Ã¢â‚¬â€ sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, na prÃƒÂ³pria ASL** (nÃƒÂ£o uma decisÃƒÂ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃƒÂ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃƒÂ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃƒÂ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃƒÂ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃƒÂ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` Ã¢â‚¬â€ `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃƒÂ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta Ã¢â‚¬â€ sem isso, `BedrockExtractionTaskHandler` nÃƒÂ£o teria como saber onde ler o texto OCR (o design Ã‚Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃƒÂ§ÃƒÂ£o de contexto que `completeOcr` (item 4/5) jÃƒÂ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura)**:
- DomÃƒÂ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` Ã¢â‚¬â€ `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃƒÂ¡vel, nunca concatenada com input do usuÃƒÂ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` Ã¢â‚¬â€ nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃƒÂ³ `textArtifact` Ã¢â‚¬â€ tipo local `BedrockTextArtifactRef`, estruturalmente idÃƒÂªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃƒÂ­be domÃƒÂ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) Ã¢â‚¬â€ nenhum import do SDK Bedrock fora do adapter.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) Ã¢â‚¬â€ re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃƒÂ¡ filtra isso mas a chamada paga nunca confia sÃƒÂ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃƒÂ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃƒÂ£o hÃƒÂ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz Ã¢â‚¬â€ nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) Ã¢â‚¬â€ Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃƒÂ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃƒÂªncia de tool-call, tool errado) Ã¢â‚¬â€ qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃƒÂ§ÃƒÂ£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` Ã¢â‚¬â€ DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃƒÂ£o configurÃƒÂ¡vel via `bedrock_region`, distinta da regiÃƒÂ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃƒÂ£o Bedrock: configurÃƒÂ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) Ã¢â‚¬â€ design Ã‚Â§4 bloqueia sÃƒÂ³ a escolha/validaÃƒÂ§ÃƒÂ£o real para produÃƒÂ§ÃƒÂ£o, nunca a testabilidade em `dev`; a permissÃƒÂ£o IAM jÃƒÂ¡ ÃƒÂ© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota Ã¢â‚¬â€ mesmo padrÃƒÂ£o do item 4 Ã¢â‚¬â€, `feature_flags_read_policy_json`, `s3:GetObject` sÃƒÂ³ leitura no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ nunca escrita, mesma disciplina de blast-radius do item 5 Ã¢â‚¬â€, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃƒÂ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29Ã¢â€ â€™30 funÃƒÂ§ÃƒÂµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 Ã¢â‚¬â€ caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃƒÂ§ÃƒÂ£o de quota em falha, **14Ã‚Âº caso adversarial de custo-abuso**: execuÃƒÂ§ÃƒÂ£o retentada/duplicada do mesmo `runId` reserva contra a prÃƒÂ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 Ã¢â‚¬â€ corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃƒÂ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃƒÂ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃƒÂ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Ã‚Â§1.11) nÃƒÂ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) Ã¢â‚¬â€ o corpus construÃƒÂ­do cobre as mesmas classes de ameaÃƒÂ§a descritas ali (injeÃƒÂ§ÃƒÂ£o via conteÃƒÂºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Ã‚Âº caso de custo-abuso) a partir de princÃƒÂ­pios, documentado no comentÃƒÂ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar Ã¢â‚¬â€ os do item 5 ainda nÃƒÂ£o aplicado + os do item 6 Ã¢â‚¬â€, 56 a mudar Ã¢â‚¬â€ mesmo ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ documentado nos itens 4/5 Ã¢â‚¬â€, 1 destroy-and-recreate Ã¢â‚¬â€ o MESMO drift prÃƒÂ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃƒÂ£o introduzido nesta sessÃƒÂ£o). **Nunca `terraform apply` local**.
- **DecisÃƒÂ£o pendente registrada, nÃƒÂ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃƒÂ©m de `expirationDate`, entÃƒÂ£o o schema fechado do tool `submit_extraction` sÃƒÂ³ tem essa uma propriedade Ã¢â‚¬â€ `buildSubmitExtractionToolSchema()` jÃƒÂ¡ ÃƒÂ© genÃƒÂ©rico sobre `FIELD_SCHEMA_V1`, entÃƒÂ£o um campo novo no schema nÃƒÂ£o precisa de mudanÃƒÂ§a de cÃƒÂ³digo aqui, sÃƒÂ³ uma nova versÃƒÂ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) Ã¢â‚¬â€ todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃƒÂ¡gio, design Ã‚Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃƒÂ£o mais importante desta sessÃƒÂ£o Ã¢â‚¬â€ a limpeza do artefato transitÃƒÂ³rio (design Ã‚Â§3, "achado mais sÃƒÂ©rio")**: `OcrArtifactStore.delete()` ÃƒÂ© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃƒÂ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃƒÂ³fica do parser determinÃƒÂ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` Ã¢â‚¬â€ design cita esse estado explicitamente como ponto de deleÃƒÂ§ÃƒÂ£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` Ã¢â‚¬â€ rodam antes do run alcanÃƒÂ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃƒÂªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado Ã¢â‚¬â€ zero atÃƒÂ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃƒÂ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` Ã¢â‚¬â€ `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃƒÂ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃƒÂ£o-vazio. Um candidato invÃƒÂ¡lido nunca ÃƒÂ© descartado silenciosamente Ã¢â‚¬â€ ÃƒÂ© marcado `valid: false` e tratado como "essa fonte nÃƒÂ£o produziu nada" na comparaÃƒÂ§ÃƒÂ£o.
- `compare-extractors.ts` Ã¢â‚¬â€ `compareExtractors(candidates)`: 0 candidatos usÃƒÂ¡veis Ã¢â€ â€™ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` Ã¢â‚¬â€ jÃƒÂ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` Ã¢â‚¬â€ nÃƒÂ£o representa separadamente; decisÃƒÂ£o registrada aqui em vez de alargar o tipo jÃƒÂ¡ commitado). 1 candidato Ã¢â€ â€™ `SINGLE_SOURCE` com valor. 2+ concordando Ã¢â€ â€™ `MATCH` (confianÃƒÂ§a = a maior das duas). 2+ discordando Ã¢â€ â€™ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" Ã¢â‚¬â€ `candidateValue` carrega sÃƒÂ³ o de maior confianÃƒÂ§a, para exibiÃƒÂ§ÃƒÂ£o, mas ÃƒÂ© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` Ã¢â‚¬â€ `decideFieldOutcome(comparison)`: sem `candidateValue` Ã¢â€ â€™ `PENDING_CONFIRMATION`; `MISMATCH` Ã¢â€ â€™ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃƒÂ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) Ã¢â€ â€™ `PENDING_CONFIRMATION`; senÃƒÂ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) Ã¢â€ â€™ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** Ã¢â‚¬â€ esse estado sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ÃƒÂ¡vel pela rota HTTP de rejeiÃƒÂ§ÃƒÂ£o humana (item 8, ainda nÃƒÂ£o implementada).

**DecisÃƒÂ£o de arquitetura tomada nesta sessÃƒÂ£o (nÃƒÂ£o estava no design, resolvida por inferÃƒÂªncia direta do enum jÃƒÂ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃƒÂ£o tem um estado "aguardando confirmaÃƒÂ§ÃƒÂ£o" separado Ã¢â‚¬â€ entÃƒÂ£o a distinÃƒÂ§ÃƒÂ£o "run completa vs. precisa de revisÃƒÂ£o humana" vive inteiramente no nÃƒÂ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃƒÂ¡lido Ã¢â€ â€™ comparaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ persistÃƒÂªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃƒÂ­da, hoje sÃƒÂ³ para log/observabilidade Ã¢â‚¬â€ nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` ÃƒÂ© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho Ã¢â‚¬â€ o prÃƒÂ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa ÃƒÂºnica operaÃƒÂ§ÃƒÂ£o, porque ÃƒÂ© o ÃƒÂºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, ÃƒÂ© uma decisÃƒÂ£o de produto pequena e reversÃƒÂ­vel (mudar sÃƒÂ³ a decisÃƒÂ£o de estado, nÃƒÂ£o a mecÃƒÂ¢nica de comparaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia) Ã¢â‚¬â€ registrado aqui para reabrir se necessÃƒÂ¡rio.

**Descarte por exclusÃƒÂ£o concorrente (design Ã‚Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃƒÂ£o do `Document` lida nÃƒÂ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃƒÂ£o sÃƒÂ³ um "ler status DELETED e torcer"). Se o `Document` jÃƒÂ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃƒÂ§ÃƒÂ£o falhar dentro da transaÃƒÂ§ÃƒÂ£o (mudanÃƒÂ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃƒÂ§ÃƒÂ£o como descarte Ã¢â‚¬â€ decisÃƒÂ£o conservadora documentada no comentÃƒÂ¡rio do adapter, jÃƒÂ¡ que distinguir a causa exata do cancelamento por cÃƒÂ³digo de erro do SDK seria frÃƒÂ¡gil entre versÃƒÂµes).

**Ports/persistÃƒÂªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) Ã¢â‚¬â€ expÃƒÂµe SÃƒâ€œ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num ÃƒÂºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` ÃƒÂ© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` Ã¢â‚¬â€ mÃƒÂ©todo standalone, usado SÃƒâ€œ no caminho de descarte (onde nÃƒÂ£o hÃƒÂ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` Ã¢â‚¬â€ **mudanÃƒÂ§a de contrato deliberada**: o item 4 tinha deixado esse mÃƒÂ©todo deliberadamente FORA do port ("a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" Ã¢â‚¬â€ tornava a exclusÃƒÂ£o acidental do chamador errado estruturalmente impossÃƒÂ­vel). Agora adicionado porque este Ãƒâ€° o chamador certo Ã¢â‚¬â€ `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃƒÂ³pria API do S3, deletar uma key ausente nÃƒÂ£o lanÃƒÂ§a). Os testes que antes afirmavam "o port nem expÃƒÂµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃƒÂ¡ que o mÃƒÂ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃƒÂ¡-lo).

**AplicaÃƒÂ§ÃƒÂ£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃƒÂ§ÃƒÂµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um ÃƒÂºnico `ValidationContext` que evolui atravÃƒÂ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃƒÂ£o exportado) que encapsula a leitura do `Document`+decisÃƒÂ£o discard-ou-commit. Um erro genuÃƒÂ­no de commit (nÃƒÂ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` Ã¢â‚¬â€ o `Catch`/`Retry` da ASL trata como transitÃƒÂ³rio).

**Achado real corrigido nesta sessÃƒÂ£o, em cÃƒÂ³digo do item 6 jÃƒÂ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` Ã¢â‚¬â€ a MESMA classe de bug que o item 5 jÃƒÂ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Ã‚Â§3's "achado mais sÃƒÂ©rio" ÃƒÂ© sobre limpeza de artefato, mas essa categoria de "output nÃƒÂ£o reanexa o contexto anterior" jÃƒÂ¡ apareceu 3 vezes agora: item 4Ã¢â€ â€™5, item 5Ã¢â€ â€™6, e este item 6Ã¢â€ â€™7). Corrigido de forma aditiva (campos novos sÃƒÂ³ ecoam o que jÃƒÂ¡ vinha em `input`, sem recomputar nada) Ã¢â‚¬â€ os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃƒÂ§ÃƒÂ£o (nÃƒÂ£o fazem `toEqual` no objeto inteiro). **PadrÃƒÂ£o a vigiar em qualquer implementaÃƒÂ§ÃƒÂ£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃƒÂ³ximo estado vai precisar, nunca assumir que "sÃƒÂ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃƒÂ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃƒÂ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃƒÂ©m ganhou `ResultSelector`/`OutputPath` (nÃƒÂ£o tinha nenhum antes Ã¢â‚¬â€ outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` Ã¢â‚¬â€ o footprint mais estreito dos quatro Lambdas de extraÃƒÂ§ÃƒÂ£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ este ÃƒÂ© o ÃƒÂºnico Lambda do pipeline inteiro com permissÃƒÂ£o de deletar lÃƒÂ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma ÃƒÂºnica `aws_lambda_permission` cobre as cinco invocaÃƒÂ§ÃƒÂµes (todas vÃƒÂªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30Ã¢â€ â€™31 funÃƒÂ§ÃƒÂµes (3 asserts).

**VerificaÃƒÂ§ÃƒÂ£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃƒÂ­cio da sessÃƒÂ£o Ã¢â‚¬â€ nÃƒÂ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃƒÂ£o, nÃƒÂºmero incorreto de digitaÃƒÂ§ÃƒÂ£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃƒÂ§ÃƒÂ£o do artefato descrito acima), mais 16 testes prÃƒÂ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃƒÂµes dos itens 5/6, nÃƒÂ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃƒÂ£o de produto pendente registrada, nÃƒÂ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` Ã¢â‚¬â€ isso ÃƒÂ© o item 8 (rotas de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7), ainda nÃƒÂ£o implementado. AtÃƒÂ© lÃƒÂ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃƒÂ£o hÃƒÂ¡ nenhuma forma de um operador vÃƒÂª-lo ou agir sobre ele pela API.

**Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora ÃƒÂ© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃƒÂ¡ existentes (items 4-7) e uma role de execuÃƒÂ§ÃƒÂ£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` Ã¢â‚¬â€ o mesmo nome que `local.extraction_state_machine_arn` jÃƒÂ¡ esperava desde o item 2, verificado por `terraform test` (nÃƒÂ£o confiado sÃƒÂ³ ÃƒÂ  leitura do cÃƒÂ³digo).

- **IAM da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado ÃƒÂ s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃƒÂªncia documentada da prÃƒÂ³pria AWS para `logging_configuration` de state machine Ã¢â‚¬â€ opera no subsistema de log delivery a nÃƒÂ­vel de conta, nÃƒÂ£o num log group especÃƒÂ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃƒÂ§ÃƒÂµes que `AWSXRayDaemonWriteAccess`, que o mÃƒÂ³dulo `lambda-function` jÃƒÂ¡ anexa a toda funÃƒÂ§ÃƒÂ£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃƒÂ¡-lo, escopado ÃƒÂ  ARN determinÃƒÂ­stica do state machine) jÃƒÂ¡ estava pronto desde os itens 4-7 Ã¢â‚¬â€ nÃƒÂ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃƒÂ§ÃƒÂ£o 30 dias, mesmo padrÃƒÂ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃƒÂ­vel `ERROR` (nÃƒÂ£o `ALL`) com `include_execution_data = false` Ã¢â‚¬â€ decisÃƒÂ£o deliberada espelhando a disciplina de privacidade do prÃƒÂ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃƒÂ§ÃƒÂ£o pode carregar texto OCR/valores de campo extraÃƒÂ­dos; nunca devem cair num log group com retenÃƒÂ§ÃƒÂ£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` Ã¢â‚¬â€ todo Lambda do repo jÃƒÂ¡ roda com ADOT/X-Ray ativo (AGENTS.md Ã‚Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Ã‚Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ o state machine, sua role e as 4 permissÃƒÂµes de invocaÃƒÂ§ÃƒÂ£o sempre existem (inspecionÃƒÂ¡veis/deployÃƒÂ¡veis). RaciocÃƒÂ­nio registrado no comentÃƒÂ¡rio do prÃƒÂ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃƒÂ¡ gateia o ÃƒÅ¡NICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que ÃƒÂ© quem chama `StartExecution`) Ã¢â‚¬â€ com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃƒÂ© ser iniciado manualmente para uma execuÃƒÂ§ÃƒÂ£o de teste, mas nunca recebe trÃƒÂ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) Ã¢â‚¬â€ 13Ã¢â€ â€™15 passando. MÃƒÂ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃƒÂªncia explÃƒÂ­cita registrada pelo item 7) Ã¢â‚¬â€ 5 `run` via `mock_provider`+`apply` (substituiÃƒÂ§ÃƒÂ£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃƒÂ¡s; `type = STANDARD`; role passada atravÃƒÂ©s sem modificaÃƒÂ§ÃƒÂ£o; tracing/logging configurados) Ã¢â‚¬â€ 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) Ã¢â‚¬â€ **a verificaÃƒÂ§ÃƒÂ£o crÃƒÂ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` Ã¢â€ â€™ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" Ã¢â‚¬â€ a definiÃƒÂ§ÃƒÂ£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃƒÂ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live`), jÃƒÂ¡ documentado nas sessÃƒÂµes dos itens 5/6/7, nÃƒÂ£o introduzido aqui. Nenhuma issue de integraÃƒÂ§ÃƒÂ£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃƒÂ­ram corretamente Ã¢â‚¬â€ nÃƒÂ£o foi necessÃƒÂ¡rio nenhum fix no `replace()` do mÃƒÂ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃƒÂ£o Ã¢â‚¬â€ sessÃƒÂ£o nÃƒÂ£o tocou cÃƒÂ³digo de aplicaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ infra). `terraform fmt -check -recursive` limpo em toda a ÃƒÂ¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃƒÂ£o precisar de `command = apply` contra o provider real). Novo output no mÃƒÂ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃƒÂ£o (branch `develop`, PR #60 `developÃ¢â€ â€™main` ainda aberto Ã¢â‚¬â€ push deve atualizÃƒÂ¡-lo automaticamente): instanciaÃƒÂ§ÃƒÂ£o do mÃƒÂ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: `ExtractionStarterWorker` completo Ã¢â‚¬â€ domÃƒÂ­nio/aplicaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia/handler real, nÃƒÂ£o sÃƒÂ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` Ã¢â‚¬â€ portas prÃƒÂ³prias, nÃƒÂ£o reusa `DocumentStore` do mÃƒÂ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃƒÂ¡ existia Ã¢â‚¬â€ gated sÃƒÂ³ no registro criaria um run ÃƒÂ³rfÃƒÂ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real ÃƒÂ© a idempotÃƒÂªncia nativa do `StartExecution` por nome de execuÃƒÂ§ÃƒÂ£o determinÃƒÂ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃƒÂ£o S3Ã¢â€ â€™EventBridgeÃ¢â€ â€™SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃƒÂ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` Ã¢â‚¬â€ insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃƒÂ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) Ã¢â‚¬â€ mudanÃƒÂ§a na M6 (jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o), mas segura: `cleanObject` ÃƒÂ© sempre um valor opaco armazenado na entidade, nunca reconstruÃƒÂ­do/reparsed em nenhum outro lugar do cÃƒÂ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃƒÂ¡veis/inspecionÃƒÂ¡veis), mas a regra EventBridge que liga o bucket limpo ÃƒÂ  fila ÃƒÂ© o ÃƒÅ¡NICO recurso condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃƒÂ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` ÃƒÂ© determinÃƒÂ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) Ã¢â‚¬â€ jÃƒÂ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃƒÂªncias) Ã¢â‚¬â€ comportamento condicional verificado, nÃƒÂ£o sÃƒÂ³ lido no cÃƒÂ³digo. **Ainda sem PR/merge** (mesma lÃƒÂ³gica do item 1: mergeÃƒÂ¡vel isoladamente, mas ainda aguardando decisÃƒÂ£o de "quando" Ã¢â‚¬â€ ver abaixo).
3. ASL Ã¢â‚¬â€ camada pura (o arquivo `.asl.json` e o mÃƒÂ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** (cÃƒÂ³digo+infra, nÃƒÂ£o sÃƒÂ³ camada pura) Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃƒÂ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 5 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (reanexaÃƒÂ§ÃƒÂ£o de contexto ao payload de sucesso de `completeOcr`, correÃƒÂ§ÃƒÂ£o do path `$.Payload.*` nas Choice states) e a decisÃƒÂ£o pendente registrada (sem heurÃƒÂ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 6 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (modelo/regiÃƒÂ£o placeholder configurÃƒÂ¡veis, correÃƒÂ§ÃƒÂ£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃƒÂ§ÃƒÂ£o do `artifact` ao output do item 5) e o corpus adversarial construÃƒÂ­do.
7. `ExtractionValidationTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 7 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃƒÂ¡ commitado, invariante de deleÃƒÂ§ÃƒÂ£o do artefato) e o que falta (item 8 ainda nÃƒÂ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" na seÃƒÂ§ÃƒÂ£o M7 acima (topo do arquivo) para o detalhe completo Ã¢â‚¬â€ `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃƒÂ§ÃƒÂ£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃƒÂ§ÃƒÂ£o "pendÃƒÂªncias explÃƒÂ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real Ã¢â‚¬â€ nÃƒÂ£o bloqueia implementaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ produÃƒÂ§ÃƒÂ£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` Ã¢â‚¬â€ o ÃƒÂºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃƒÂ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) Ã¢â‚¬â€ idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃƒÂª o `ExtractedField` jÃƒÂ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` ÃƒÂ© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) Ã¢â‚¬â€ lÃƒÂª os quatro, valida as 4 versÃƒÂµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃƒÂ§ÃƒÂ£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` Ã¢â‚¬â€ primeiro 422 do projeto). `reject` ÃƒÂ© OCC de 3 vias (nunca lÃƒÂª/toca `ExpirationItem` Ã¢â‚¬â€ nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoÃ¢â€ â€™atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃƒÂ³ `expirationDate` -> `dueDate` no schema v1) Ã¢â‚¬â€ quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃƒÂ§ÃƒÂ£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃƒÂ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃƒÂ§ÃƒÂ£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃƒÂ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` Ã¢â‚¬â€ `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃƒÂ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃƒÂ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` Ã¢â‚¬â€ um plain read eventualmente consistente, nunca usado pelos OCC writes jÃƒÂ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃƒÂ¡ existente) reaproveitado por `documents`/`items` na composition root Ã¢â‚¬â€ `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃƒÂ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` Ã¢â‚¬â€ `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) Ã¢â‚¬â€ mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` Ã¢â€ â€™ `authorize()` internamente via a application function Ã¢â€ â€™ `TenantQuotaService` `API_REQUEST` 100/60s Ã¢â€ â€™ schema Ajv Ã¢â€ â€™ chamada). AutorizaÃƒÂ§ÃƒÂ£o: `extraction:confirm` (aÃƒÂ§ÃƒÂ£o **jÃƒÂ¡ existia** na matriz desde uma sessÃƒÂ£o anterior Ã¢â‚¬â€ cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃƒÂ³pias desse mapa nos outros mÃƒÂ³dulos nÃƒÂ£o precisam, deixadas como estÃƒÂ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃƒÂ¡ tem `tenant_facing_read_write_policy_json` completo na tabela Ã¢â‚¬â€ zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃƒÂ£o de adapter `DynamoLike` que `ExpirationService` jÃƒÂ¡ usa para sua prÃƒÂ³pria `IdempotencyStore`, ao invÃƒÂ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` Ã¢â‚¬â€ o `source_arn` do `aws_lambda_permission.documents` jÃƒÂ¡ ÃƒÂ© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃƒÂ­do de republish de `source_code_hash` jÃƒÂ¡ documentado em toda sessÃƒÂ£o anterior deste milestone Ã¢â‚¬â€ o prÃƒÂ³prio `documents-handler` bundle mudou porque o cÃƒÂ³digo mudou, os outros 15 sÃƒÂ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas Ã¢â‚¬â€ nÃƒÂ£o repetido o gap histÃƒÂ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` Ã¢â‚¬â€ `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃƒÂ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` Ã¢â‚¬â€ reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃƒÂ¡lido/invÃƒÂ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos ÃƒÂ  mÃƒÂ£o sobre uma tabela DynamoDB em memÃƒÂ³ria com OCC real Ã¢â‚¬â€ `InMemoryTable.write()` sÃƒÂ³ aplica se a versÃƒÂ£o bate) Ã¢â‚¬â€ feliz confirm (item atualizado + versÃƒÂµes incrementadas), feliz reject (item intocado), idempotÃƒÂªncia de replay dos dois (sem re-executar a transaÃƒÂ§ÃƒÂ£o), 409 individual para cada uma das 4/3 versÃƒÂµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃƒÂ¡lido, 404 para cada uma das 4 entidades ausente, negaÃƒÂ§ÃƒÂ£o de autorizaÃƒÂ§ÃƒÂ£o sem membership. 2 fakes prÃƒÂ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃƒÂ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃƒÂ§ÃƒÂ£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃƒÂ­cio da sessÃƒÂ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃƒÂ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃƒÂ aconteceu, sÃƒÂ³ a persistÃƒÂªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒÆ’O libera a quota** Ã¢â‚¬â€ investigado nesta sessÃƒÂ£o e confirmado correto, nÃƒÂ£o uma lacuna: como a reserva ÃƒÂ© por-execuÃƒÂ§ÃƒÂ£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior, que `startOcr` jÃƒÂ¡ trata genericamente como "jÃƒÂ¡ reservado, nÃƒÂ£o ÃƒÂ© exaustÃƒÂ£o real" (linhas 86-90) Ã¢â‚¬â€ **independente de a reserva ter sido liberada ou nÃƒÂ£o**. Liberar aqui nÃƒÂ£o destravaria nada que jÃƒÂ¡ nÃƒÂ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃƒÂ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃƒÂ£o exato Ã¢â‚¬â€ falha da chamada Bedrock em si (apÃƒÂ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃƒÂ¡ existente, confirmado). NÃƒÂ£o hÃƒÂ¡ nenhum caminho de falha PÃƒâ€œS-chamada-bem-sucedida neste handler (ao contrÃƒÂ¡rio do Textract, o resultado do Bedrock ÃƒÂ© sÃƒÂ³ re-anexado ao payload de saÃƒÂ­da, nunca persistido localmente por este handler) Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃƒÂ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃƒÂ¡ completa e correta Ã¢â‚¬â€ nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio, nenhuma lacuna real encontrada. NÃƒÂ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador Ã¢â‚¬â€ fora de escopo do design aprovado, nÃƒÂ£o registrado como pendÃƒÂªncia.

## M7 Ã¢â‚¬â€ estado final desta sessÃƒÂ£o: cÃƒÂ³digo-completo para `dev`

Com os itens 1-9 concluÃƒÂ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃƒÂ¡ code-complete para testar em `dev`** Ã¢â‚¬â€ nÃƒÂ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃƒÂ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃƒÂ£o: nÃƒÂ£o hÃƒÂ¡ nenhum mÃƒÂ³dulo/recurso do pipeline de extraÃƒÂ§ÃƒÂ£o pendente de instanciaÃƒÂ§ÃƒÂ£o). O ÃƒÂºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃƒÂ§ÃƒÂ£o end-to-end real~~ Ã¢â‚¬â€ **FEITA em 2026-08-27**, ver a seÃƒÂ§ÃƒÂ£o `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃƒÂ£o, 1 documentado como decisÃƒÂ£o pendente).
- Runbook de calibraÃƒÂ§ÃƒÂ£o para job Textract "preso" (mencionado no item 3) Ã¢â‚¬â€ nÃƒÂ£o bloqueia, ÃƒÂ© operacional.

## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃƒÂ£o `StartExecution` na mÃƒÂ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃƒÂ§ÃƒÂ£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃƒÂ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` Ã¢â€ â€™ `exptrk-dev-extraction-starter-handler:live` Ã¢â€ â€™ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O ÃƒÂºnico elo originado ÃƒÂ  mÃƒÂ£o foi a mensagem SQS, porque a regra EventBridge que a produziria ÃƒÂ© o ÃƒÂºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃƒÂ¡-la exigiria `terraform apply` Ã¢â‚¬â€ proibido pelo Ã‚Â§3 do `AGENTS.md`. Nada alÃƒÂ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃƒÂ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃƒÂ£o ÃƒÂ© automatizÃƒÂ¡vel por CLI. **Nenhum resultado de extraÃƒÂ§ÃƒÂ£o foi falsificado** Ã¢â‚¬â€ todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃƒÂ§ÃƒÂ£o foi produzido pelo pipeline real.

**As 3 execuÃƒÂ§ÃƒÂµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃƒÂ§ÃƒÂ£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,Ã¢â‚¬Â¦}}`) | RunTextract(falhou) Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ CheckAiKillSwitch Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃƒÂ¡ria, envelope canÃƒÂ´nico com `OCR:true` | idÃƒÂªnticos ao anterior | SUCCEEDED, **ainda degradado** Ã¢â‚¬â€ evidÃƒÂªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃƒÂ¡ria, envelope canÃƒÂ´nico **+ cÃƒÂ³pia top-level** dos 3 booleanos | RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃƒÂªncia do caminho feliz (`output` real da execuÃƒÂ§ÃƒÂ£o `run_c1aefÃ¢â‚¬Â¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefÃ¢â‚¬Â¦/42d064cf-Ã¢â‚¬Â¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃƒÂ­stico extraiu a data correta**. `RunBedrock` NÃƒÆ’O foi exercitado (`needsBedrock:false` Ã¢â‚¬â€ o candidato determinÃƒÂ­stico jÃƒÂ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃƒÂ³sito). `CompleteRun` apagou o artefato transitÃƒÂ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃƒÂ§ÃƒÂ£o/quota/idempotÃƒÂªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, campo `PENDING_CONFIRMATION` Ã¢â€ â€™ `CONFIRMED` (`version` 1Ã¢â€ â€™2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) Ã¢â€ â€™ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃƒÂ§ÃƒÂ£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` Ã¢â€ â€™ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃƒÂ£o reaplicou).
- `reject` no campo de `run_a22dfÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, `PENDING_CONFIRMATION` Ã¢â€ â€™ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefÃ¢â‚¬Â¦` Ã¢â€ â€™ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) Ã¢â‚¬â€ ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado Ã¢â‚¬â€ pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃƒÂ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Ã‚Â§17.3 especifica) `{"features":{"AI_EXTRACTION":Ã¢â‚¬Â¦,"OCR":Ã¢â‚¬Â¦,"WHATSAPP":Ã¢â‚¬Â¦}}`, mas o adapter lia `parsed.OCR` no nÃƒÂ­vel raiz Ã¢â‚¬â€ logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃƒÂªncia isolada: a execuÃƒÂ§ÃƒÂ£o `run_a22dfÃ¢â‚¬Â¦` rodou com a versÃƒÂ£o AppConfig 2 (`OCR:true` dentro do envelope canÃƒÂ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefÃ¢â‚¬Â¦`, cuja ÃƒÂºnica diferenÃƒÂ§a era a cÃƒÂ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃƒÂ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃƒÂ­vel raiz) + teste de regressÃƒÂ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃƒÂ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados ÃƒÂ  lista de imports estÃƒÂ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃƒÂ³pria doc-string manda "add both the file AND a static import line". Toda chamada ÃƒÂ s duas rotas retornava 500 antes de qualquer lÃƒÂ³gica de negÃƒÂ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` Ã¢â‚¬â€ o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃƒÂ³ria dos testes unitÃƒÂ¡rios ignoram atributos extras na chave, por isso a suÃƒÂ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃƒÂ³ depois dessa correÃƒÂ§ÃƒÂ£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` Ã¢â‚¬â€ GAP REAL, NÃƒÆ’O CORRIGIDO (decisÃƒÂ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃƒÂ§a Ã¢â‚¬â€ foi exatamente o que aconteceu com `run_c1aefÃ¢â‚¬Â¦`, o caso de sucesso do pipeline. Mas o **ÃƒÂºnico** cÃƒÂ³digo que escreve `ExpirationItem.dueDate` ÃƒÂ© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃƒÂ¡ `CONFIRMED` (verificado, ver acima). ConsequÃƒÂªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** ÃƒÂ© atualizado e nÃƒÂ£o existe rota que o atualize. NÃƒÂ£o corrigido de propÃƒÂ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃƒÂ§ÃƒÂ£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" ÃƒÂ© decisÃƒÂ£o de produto/design (Type 1, Ã‚Â§4 do `AGENTS.md`), nÃƒÂ£o conserto mecÃƒÂ¢nico.

### Achado menor (nÃƒÂ£o corrigido, sem impacto observado nesta verificaÃƒÂ§ÃƒÂ£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, Ã¢â‚¬Â¦), mas o Step Functions casa contra o `errorType` do Lambda, que ÃƒÂ© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, Ã¢â‚¬Â¦). Confirmado no histÃƒÂ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃƒÂ£o** casou Ã¢â‚¬â€ quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃƒÂ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃƒÂ­ **hÃƒÂ¡** perda de comportamento (uma falha transitÃƒÂ³ria de commit nÃƒÂ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD Ã¢â‚¬â€ deixado para uma sessÃƒÂ£o de infra.

### Estado do ambiente apÃƒÂ³s a sessÃƒÂ£o

Flags AppConfig **revertidas**: versÃƒÂ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃƒÂµes temporÃƒÂ¡rias 2/3 deletadas Ã¢â‚¬â€ `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃƒÂ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃƒÂ³ o ruÃƒÂ­do jÃƒÂ¡ conhecido de republicaÃƒÂ§ÃƒÂ£o de versÃƒÂ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃƒÂµes deletadas), `ocr/` no bucket transitÃƒÂ³rio volta 0. Os histÃƒÂ³ricos das 3 execuÃƒÂ§ÃƒÂµes do Step Functions ficam (artefato normal da AWS, nÃƒÂ£o ÃƒÂ© dado de teste a limpar).

**As correÃƒÂ§ÃƒÂµes dos bugs 1 e 2 estÃƒÂ£o apenas em `develop`, ainda NÃƒÆ’O implantadas em `dev`** (o CD sÃƒÂ³ aplica em push para `main`). A verificaÃƒÂ§ÃƒÂ£o do `confirm` real rodou contra o bundle esbuild jÃƒÂ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃƒÂ© o prÃƒÂ³ximo merge para `main`.

**Bloqueado sÃƒÂ³ para produÃƒÂ§ÃƒÂ£o real** (nÃƒÂ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃƒÂ§ÃƒÂ£o de modelo Bedrock + regiÃƒÂ£o, RIPD formal (`privacy-lgpd.md` Ã‚Â§6, gatilho jÃƒÂ¡ registrado).

## DecisÃƒÂµes pendentes do Marcelo

1. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR)**: design jÃƒÂ¡ aprovado (D-035), zero decisÃƒÂ£o de produto pendente Ã¢â‚¬â€ sÃƒÂ³ falta decidir *quando* comeÃƒÂ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) Ã¢â‚¬â€ nÃƒÂ£o retomar sem sinal explÃƒÂ­cito dele.
3. **Estado real de `git`/deploy no inÃƒÂ­cio da prÃƒÂ³xima sessÃƒÂ£o nÃƒÂ£o deve ser presumido** Ã¢â‚¬â€ confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃƒÂªncias residuais nÃƒÂ£o bloqueantes (registradas, nÃƒÂ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃƒÂ©-existente nÃƒÂ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ Ã¢â‚¬â€ **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃƒÂºmeros (9 vulnerabilidades reais, nÃƒÂ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃƒÂ£o documentada (`testcontainers`Ã¢â€ â€™`dockerode`Ã¢â€ â€™`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃƒÂ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃƒÂ£o reverificado nesta sessÃƒÂ£o).
- Observabilidade por funÃƒÂ§ÃƒÂ£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃƒÂ©rica jÃƒÂ¡ cobre "estÃƒÂ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃƒÂ§ÃƒÂ£o de upload slot expirado) nunca exercitada contra AWS real~~ Ã¢â‚¬â€ **executada em 2026-08-25, achou um bug real de produÃƒÂ§ÃƒÂ£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado Ã¢â‚¬â€ confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃƒÂ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃƒÂ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃƒÂ³s a transiÃƒÂ§ÃƒÂ£o (ponteiro ÃƒÂ³rfÃƒÂ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃƒÂ§ÃƒÂ£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃƒÂ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃƒÂ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** Ã¢â‚¬â€ commit `2fab80f`, jÃƒÂ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃƒÂ£o deployado" estava desatualizada (verificado nesta sessÃƒÂ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃƒÂ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso Ã¢â‚¬â€ sÃƒÂ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` ÃƒÂ© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃƒÂ³ ÃƒÂ© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃƒÂ§ÃƒÂ£o defensiva do status real da occurrence antes de agir Ã¢â‚¬â€ um ponteiro ÃƒÂ³rfÃƒÂ£o nunca ÃƒÂ© revisitado fora dessa janela, e mesmo se fosse, ÃƒÂ© ignorado com seguranÃƒÂ§a. Diferente do bug do `upload-slot` (falha total Ã¢â‚¬â€ nada funcionava), este ÃƒÂ© um descompasso comentÃƒÂ¡rio-vs-cÃƒÂ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃƒÂ¡ revisado 9,2/10 pelo protocolo ClaudeÃ¢â€ â€Codex e ativo em produÃƒÂ§ÃƒÂ£o real Ã¢â‚¬â€ nÃƒÂ£o corrigido nesta sessÃƒÂ£o deliberadamente, para nÃƒÂ£o mexer num trecho crÃƒÂ­tico jÃƒÂ¡ extensivamente revisado sem uma rodada de revisÃƒÂ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃƒÂ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃƒÂ§ÃƒÂ£o real, Ã‚Â§13-30) que, se adotada como padrÃƒÂ£o oficial, deveria passar pela mesma convergÃƒÂªncia independente ClaudeÃ¢â€ â€Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃƒÂ¡ usaram Ã¢â‚¬â€ nÃƒÂ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃƒÂ£o de 12 eixos especÃƒÂ­fico do planejamento de interface, jÃƒÂ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃƒÂªncias (nÃƒÂ£o reler por padrÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ sob demanda)

- `docs/architecture/session-log.md` Ã¢â‚¬â€ linha do tempo compacta, uma entrada por sessÃƒÂ£o.
- `docs/architecture/decisions-log.md` Ã¢â‚¬â€ todas as decisÃƒÂµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` Ã¢â‚¬â€ os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` Ã¢â‚¬â€ o protÃƒÂ³tipo interativo executÃƒÂ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` Ã¢â‚¬â€ debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o real do Full BFF + fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o, achados do protocolo ClaudeÃ¢â€ â€Codex sobre o cÃƒÂ³digo, Final Status.

# docs/architecture/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃƒÂ§ÃƒÂ£o real (cÃƒÂ³digo/infra/testes) Ã¢â‚¬â€ Implementation Blueprint concluÃƒÂ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃƒÂ§ÃƒÂ£o LGPD sem purga fÃƒÂ­sica real e DSR 100% design-only, ambos registrados como decisÃƒÂ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃƒÂ­ntese final com recomendaÃƒÂ§ÃƒÂ£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` Ã¢â‚¬â€ ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) code-complete e deployado em `dev`** Ã¢â‚¬â€ todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃƒÂ­stico/Bedrock/validaÃƒÂ§ÃƒÂ£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃƒÂ§ÃƒÂ£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3Ã¢â€ â€™SQSÃ¢â€ â€™Step FunctionsÃ¢â€ â€™TextractÃ¢â€ â€™parserÃ¢â€ â€™persistÃƒÂªncia, ver `NEXT_SESSION_PROMPT.md`) Ã¢â‚¬â€ achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃƒÂ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃƒÂ£o corrigido e aguardando decisÃƒÂ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` Ã¢â‚¬â€ nenhuma rota escreve isso hoje); M12 bloqueado por decisÃƒÂ£o de produto (D-052); Full BFF Ã¢â‚¬â€ D-053/D-054 Ã¢â‚¬â€ **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo ClaudeÃ¢â€ â€Codex completo em `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o atÃƒÂ© convergir); planejamento de interface concluÃƒÂ­do Ã¢â‚¬â€ 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃƒÂ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃƒÂ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃƒÂ£o **todos resolvidos** (backend + frontend onde aplicÃƒÂ¡vel Ã¢â‚¬â€ BLOCKER-C via Variante B, revisÃƒÂ£o humana explÃƒÂ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada Ã¢â‚¬â€ ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta, este bloco pode ficar temporariamente atrÃƒÂ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

1. `AGENTS.md` (raiz) Ã¢â‚¬â€ processo de trabalho dos agentes, sempre vence sobre conteÃƒÂºdo de arquitetura.
2. ADR aceito em `adr/` Ã¢â‚¬â€ decisÃƒÂ£o arquitetural especÃƒÂ­fica e formal.
3. Documento temÃƒÂ¡tico corrente (tabela abaixo, coluna "normativo atual") Ã¢â‚¬â€ especificaÃƒÂ§ÃƒÂ£o detalhada do domÃƒÂ­nio.
4. `ARCHITECTURE.md` Ã¢â‚¬â€ visÃƒÂ£o consolidada e ÃƒÂ­ndice executivo; nÃƒÂ£o sobrescreve silenciosamente um ADR ou documento temÃƒÂ¡tico divergente Ã¢â‚¬â€ divergÃƒÂªncia entre eles ÃƒÂ© defeito a corrigir, nÃƒÂ£o licenÃƒÂ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` Ã¢â‚¬â€ estado de execuÃƒÂ§ÃƒÂ£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` Ã¢â‚¬â€ evidÃƒÂªncia histÃƒÂ³rica de como se chegou a uma decisÃƒÂ£o, nunca normativo.

## ÃƒÂndice por documento

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/ÃƒÂ­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃƒÂ³rico/processo (ciclo concluÃƒÂ­do) | Processo ClaudeÃ¢â€ â€Codex que produziu o design; nÃƒÂ£o ÃƒÂ© ponto de entrada de sessÃƒÂ£o |
| `quality-criteria.md` | normativo atual | 12 critÃƒÂ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃƒÂ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃƒÂ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃƒÂµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃƒÂ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃƒÂ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃƒÂ§ÃƒÂ£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃƒÂ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃƒÂ£o de domÃƒÂ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃƒÂ§ÃƒÂµes de estÃƒÂ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃƒÂ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃƒÂ§ÃƒÂ£o 33 Ã¢â‚¬â€ APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃƒÂ§ÃƒÂ£o 60 Ã¢â‚¬â€ componentes, interfaces, eventos/schemas, ordem de deploy, milestones Ã¢â‚¬â€ APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) Ã¢â‚¬â€ adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas Ã¢â‚¬â€ APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B Ã¢â‚¬â€ pipeline real de materializaÃƒÂ§ÃƒÂ£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃƒÂªncia (dispatch + reconciliaÃƒÂ§ÃƒÂ£o), backfill Ã¢â‚¬â€ APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃƒÂ§ÃƒÂ£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃƒÂ³rico/evidÃƒÂªncia (citado por `reminder-delivery-pipeline.md` Ã‚Â§3/Ã‚Â§3.2) | Recon prÃƒÂ©-implementaÃƒÂ§ÃƒÂ£o de BLOCKER-B (materializer/trigger de materializaÃƒÂ§ÃƒÂ£o, infra Terraform) confirmado contra o cÃƒÂ³digo real antes do design ser escrito; BLOCKER-B estÃƒÂ¡ implementado e mergeado (PR #50) Ã¢â‚¬â€ este documento sÃƒÂ³ registra a evidÃƒÂªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃƒÂ³rico/evidÃƒÂªncia | Texto verbatim do prompt de missÃƒÂ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃƒÂ³ existia em histÃƒÂ³rico de conversa de uma sessÃƒÂ£o especÃƒÂ­fica |
| `decisions-log.md` | decisÃƒÂ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃƒÂ§ÃƒÂ£o nÃƒÂ£o ÃƒÂ© sequencial na ordem das linhas Ã¢â‚¬â€ D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada | Artefatos do protocolo ClaudeÃ¢â€ â€Codex de M7 (proposta Claude, proposta Codex, crÃƒÂ­tica, reconciliaÃƒÂ§ÃƒÂ£o final aprovada Ã¢â‚¬â€ D-035) |
| `adr/` | decisÃƒÂ£o/ADR | 11 ADRs formais para decisÃƒÂµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada (protocolo `AGENTS.md` Ã‚Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃƒÂªncia CloudFront + Full BFF) Ã¢â‚¬â€ nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/ÃƒÂ­ndice (visual) | **O documento de status do projeto** Ã¢â‚¬â€ painel visual (timeline de marcos, achados reais, pendÃƒÂªncias); abrir no navegador para uma visÃƒÂ£o executiva rÃƒÂ¡pida, mais legÃƒÂ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃƒÂ£o) |
| `session-log.md` | histÃƒÂ³rico | Log cronolÃƒÂ³gico compacto por sessÃƒÂ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃƒÂ£o normativo) | Fase 1 da evoluÃƒÂ§ÃƒÂ£o estratÃƒÂ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃƒÂ§ÃƒÂ£o de cada capacidade comercial proposta contra o cÃƒÂ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃƒÂ­nio + protocolo ClaudeÃ¢â€ â€Codex por tema), nunca decisÃƒÂ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃƒÂ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 Ã¢â‚¬â€ achado central: billing por sujeito rastreado ÃƒÂ© padrÃƒÂ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃƒÂ­nio (`TrackedSubject`+`RequirementAssignment`) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃƒÂ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃƒÂ§ÃƒÂ£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃƒÂ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃƒÂ§ÃƒÂ£o de mini-revisÃƒÂ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃƒÂ£o direta de padrÃƒÂ£o jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o (mesma partiÃƒÂ§ÃƒÂ£o de `Document`/M6); digest registrado como questÃƒÂ£o aberta, nÃƒÂ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, lista de rejeitados formal sÃƒÂ³ na Fase 3) | Fase 2b, sexto cluster Ã¢â‚¬â€ custom fields genÃƒÂ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃƒÂ£o (nota 9,1/9,0), valor jÃƒÂ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, sÃƒÂ©timo e ÃƒÂºltimo cluster (CSV import/export) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o na entrada); plano linha-a-linha em S3, nÃƒÂ£o DynamoDB; **Fase 2b concluÃƒÂ­da, 7/7 clusters Ã¢â€°Â¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃƒÂ­ntese proposta, nÃƒÂ£o autorizaÃƒÂ§ÃƒÂ£o de implementaÃƒÂ§ÃƒÂ£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph Ã¢â‚¬â€ consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃƒÂ­ntese proposta, ADRs formais sÃƒÂ³ com decisÃƒÂ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃƒÂ§a/persistÃƒÂªncia/custo, lista de 10 ADRs candidatos, estratÃƒÂ©gia de teste/migraÃƒÂ§ÃƒÂ£o, perguntas abertas reais, capacidades rejeitadas Ã¢â‚¬â€ inclui revisÃƒÂ£o adversarial final de coerÃƒÂªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃƒÂ§ÃƒÂ£o de prÃƒÂ©-requisito (D-046) | Mini-revisÃƒÂ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) Ã¢â‚¬â€ pico orgÃƒÂ¢nico ~220Ãƒâ€” abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/ÃƒÂ­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-047/D-048) | Fecha D-047 Ã¢â‚¬â€ entrega/reenvio do link de guest upload: rotaÃƒÂ§ÃƒÂ£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-049) | Fecha "DecisÃƒÂ£o B" de D-048 Ã¢â‚¬â€ automatiza o convite inicial de guest upload (hoje manual) atrÃƒÂ¡s de preferÃƒÂªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-052) | PÃƒÂ³s-M11: M12 (Billing) fica bloqueado por decisÃƒÂ£o de produto (fornecedor de pagamento) Ã¢â‚¬â€ zero cÃƒÂ³digo novo; achado real de que o projeto nÃƒÂ£o tem conceito de "platform staff" cross-tenant, descartando atÃƒÂ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃƒÂ£o do browser Ã¢â‚¬â€ decide o mecanismo de autenticaÃƒÂ§ÃƒÂ£o das chamadas de recurso que o "BFF de sessÃƒÂ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃƒÂ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃƒÂ§ÃƒÂ£o nativa do Cognito no refresh (nÃƒÂ£o contador local Ã¢â‚¬â€ evita falso-positivo de invalidaÃƒÂ§ÃƒÂ£o), tabela de sessÃƒÂ£o dedicada IAM-isolada, cookies login/sessÃƒÂ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) Ã¢â‚¬â€ ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃƒÂ§ÃƒÂ£o e do protocolo ClaudeÃ¢â€ â€Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o corrigidos) |
| `reviews/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada (protocolo `AGENTS.md` Ã‚Â§4, pÃƒÂ³s-M0) | Artefatos de revisÃƒÂ£o ClaudeÃ¢â€ â€Codex de implementaÃƒÂ§ÃƒÂ£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) Ã¢â‚¬â€ mesmo papel de `history/` (evidÃƒÂªncia, nunca normativo), mas para rodadas ocorridas depois que cÃƒÂ³digo passou a existir, em vez das rodadas de design conceitual prÃƒÂ©-implementaÃƒÂ§ÃƒÂ£o |
| `history/` | histÃƒÂ³rico/supersedido | Artefatos de rodada (propostas, crÃƒÂ­ticas, red team) que produziram os documentos normativos acima Ã¢â‚¬â€ ver subseÃƒÂ§ÃƒÂ£o |

## `history/` Ã¢â‚¬â€ evidÃƒÂªncia de rodada, por tema

Cada subpasta contÃƒÂ©m os artefatos de proposta/crÃƒÂ­tica/trÃƒÂ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃƒÂ£o vigente Ã¢â‚¬â€ sÃƒÂ³ como prova de como o consenso foi alcanÃƒÂ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃƒÂDO Ã¢â‚¬â€ 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃƒÂ³ Contexto bateu o gate Ã¢â€°Â¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃƒÂ³rio trata de **como o trabalho de engenharia ÃƒÂ© medido e revisado** (rubrica, critÃƒÂ©rios por eixo, protocolo de debate, achados de auditoria) Ã¢â‚¬â€ nÃƒÂ£o confundir com `docs/architecture/`, que trata do que o sistema Ãƒâ€° (design, modelo de dados, decisÃƒÂµes de arquitetura). Se a dÃƒÂºvida for "o GSI3 ÃƒÂ© consultÃƒÂ¡vel por quem", vÃƒÂ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃƒÂ³rio ÃƒÂ© o certo.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

Mesma regra de `docs/architecture/README.md` Ã¢â‚¬â€ `AGENTS.md` (raiz) sempre vence sobre conteÃƒÂºdo deste diretÃƒÂ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) ÃƒÂ© evidÃƒÂªncia de uma rodada especÃƒÂ­fica, nunca redefine os pesos/critÃƒÂ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃƒÂ¡ em vez de duplicar, ver seu prÃƒÂ³prio rodapÃƒÂ© "Como adicionar um novo eixo").

## Quando carregar o quÃƒÂª (roteamento por tipo de tarefa)

| Sua tarefa ÃƒÂ©... | Carregue |
|---|---|
| Rodar o protocolo ClaudeÃ¢â€ â€Codex num eixo especÃƒÂ­fico (`AGENTS.md` Ã‚Â§4) | `joint-review-criteria.md` (seÃƒÂ§ÃƒÂ£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃƒÂ­fico do cÃƒÂ³digo tira / que critÃƒÂ©rio ele afeta | `joint-review-criteria.md` (nÃƒÂ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional ÃƒÂ© vÃƒÂ¡lido/de qualidade suficiente (gate binÃƒÂ¡rio, critÃƒÂ©rio ponderado, nota mÃƒÂ­nima) | `test-engineering-standard.md` Ã¢â‚¬â€ rÃƒÂ©gua concreta que `joint-review-criteria.md`'s critÃƒÂ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃƒÂ§a precisa de ADR/protocolo formal ou ÃƒÂ© correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica | `change-risk-scale.md` (a rÃƒÂ©gua concreta) Ã¢â‚¬â€ `AGENTS.md` Ã‚Â§4 sÃƒÂ³ distingue os dois extremos de forma binÃƒÂ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` Ã¢â‚¬â€ mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃƒÂ§ÃƒÂ£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃƒÂ£o | `exceptions.md` |
| Ver o histÃƒÂ³rico completo de decisÃƒÂµes de engenharia (nÃƒÂ£o de arquitetura) com motivo | `decisions-log.md` Ã¢â‚¬â€ E-000 em diante, mesmo padrÃƒÂ£o de `docs/architecture/decisions-log.md` mas para decisÃƒÂµes de processo/qualidade, nÃƒÂ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃƒÂ©rios/gates G1-G6 (histÃƒÂ³rico, prÃƒÂ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA Ã¢â‚¬â€ mudanÃƒÂ§a exige nova rodada formal, nÃƒÂ£o ediÃƒÂ§ÃƒÂ£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃƒÂ³rio no inÃƒÂ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` Ã¢â‚¬â€ histÃƒÂ³rico, nÃƒÂ£o estado atual (ver aviso no prÃƒÂ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃƒÂ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃƒÂ§ÃƒÂ£o de nomenclatura abaixo) |
| Consultar evidÃƒÂªncia bruta de uma rodada especÃƒÂ­fica (nota cega, prompt do Codex, saÃƒÂ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` Ã¢â‚¬â€ evidÃƒÂªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃƒÂ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) Ã¢â‚¬â€ link relativo quebrado + referÃƒÂªncia `AGENTS.md Ã‚Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## ÃƒÂndice por documento (nÃƒÂ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃƒÂ­pios de engenharia adotados (proporcionalidade, evidÃƒÂªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃƒÂ§a NÃƒÂ­vel 1-6, rÃƒÂ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃƒÂ©rios/pesos por eixo das revisÃƒÂµes conjuntas ClaudeÃ¢â€ â€Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo ClaudeÃ¢â€ â€Codex 8 rodadas, gate elevado 9,5/10) | PadrÃƒÂ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados, fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃƒÂ§ÃƒÂµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃƒÂ£o (log vivo) | DecisÃƒÂµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃƒÂ³rico/registro vivo | DivergÃƒÂªncias materiais ClaudeÃ¢â€ â€Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Ã‚Â§4 pode ser dispensado, inventÃƒÂ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃƒÂ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃƒÂ³rico (CONGELADA) | Rubrica original de 12 critÃƒÂ©rios/gates G1-G6 da Engineering Maturity Review Ã¢â‚¬â€ antecessora conceitual de `joint-review-criteria.md`, nÃƒÂ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃƒÂ§ÃƒÂµes executÃƒÂ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃƒÂ³rico/fundamentaÃƒÂ§ÃƒÂ£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃƒÂ³rico | Estado do repositÃƒÂ³rio no inÃƒÂ­cio da Engineering Maturity Review Ã¢â‚¬â€ nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃƒÂ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) Ã¢â‚¬â€ DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃƒÂ­ntese/recomendaÃƒÂ§ÃƒÂ£o (entregÃƒÂ¡vel final do programa, prompt mestre Ã‚Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃƒÂªncia de `pilot-readiness-program.md` Ã¢â‚¬â€ nÃƒÂ£o repete achado, sÃƒÂ³ aponta; nÃƒÂ£o ÃƒÂ© aprovaÃƒÂ§ÃƒÂ£o final, ÃƒÂ© insumo para a decisÃƒÂ£o do Marcelo |
| `reviews/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada | Artefatos de toda rodada ClaudeÃ¢â€ â€Codex de processo/qualidade Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o prÃƒÂ³pria abaixo |

## `reviews/` Ã¢â‚¬â€ convenÃƒÂ§ÃƒÂ£o de nomenclatura e classificaÃƒÂ§ÃƒÂ£o

EvidÃƒÂªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃƒÂµes de arquitetura: nunca normativo, sÃƒÂ³ prova de como uma nota/decisÃƒÂ£o foi alcanÃƒÂ§ada. TrÃƒÂªs geraÃƒÂ§ÃƒÂµes de conteÃƒÂºdo coexistem aqui, nesta ordem cronolÃƒÂ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃƒÂ­vel raiz de `reviews/`) Ã¢â‚¬â€ produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos ÃƒÂ© intencional (agrupa antes de outros nomes em ordem alfabÃƒÂ©tica, sinaliza "transcript bruto, nÃƒÂ£o leia direto").
2. **ConvergÃƒÂªncia dos critÃƒÂ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) Ã¢â‚¬â€ produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) Ã¢â‚¬â€ a rodada em andamento nesta sessÃƒÂ£o. PadrÃƒÂ£o de nomenclatura por eixo (`<eixo>` Ã¢Ë†Ë† `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... Ã¢â‚¬â€ nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... Ã¢â‚¬â€ prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... Ã¢â‚¬â€ saÃƒÂ­da bruta do Codex CLI. Alguns trechos que ecoam saÃƒÂ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃƒÂ§ÃƒÂµes via PowerShell) Ã¢â‚¬â€ revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃƒÂ£o sÃƒÂ³ mitigado); a saÃƒÂ­da de uma rodada em andamento pode ainda conter mojibake atÃƒÂ© a rodada seguinte revisÃƒÂ¡-la, por ser evidÃƒÂªncia datada do que o Codex realmente viu naquele momento Ã¢â‚¬â€ nunca editada retroativamente. Se precisar do conteÃƒÂºdo, prefira o `-summary.md` correspondente, que jÃƒÂ¡ extrai a informaÃƒÂ§ÃƒÂ£o sem o ruÃƒÂ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` Ã¢â‚¬â€ **o ÃƒÂºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃƒÂ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃƒÂ§ÃƒÂ£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃƒÂ£o fonte de verdade por si sÃƒÂ³ Ã¢â‚¬â€ sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃƒÂ­tica de proveniÃƒÂªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃƒÂ£o Ã¢â‚¬â€ exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃƒÂ¡ que sua proveniÃƒÂªncia ÃƒÂ© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃƒÂ§ÃƒÂ£o cronolÃƒÂ³gica descrita nesta seÃƒÂ§ÃƒÂ£o. Isso ÃƒÂ© uma decisÃƒÂ£o de design, nÃƒÂ£o uma lacuna: um transcript nunca ÃƒÂ© lido isoladamente por convenÃƒÂ§ÃƒÂ£o (ver acima), entÃƒÂ£o nÃƒÂ£o precisa carregar sua prÃƒÂ³pria metadata.

# W3-07 Ã¢â‚¬â€ Mecanismo de cascata de exclusÃƒÂ£o fÃƒÂ­sica por tenant Ã¢â‚¬â€ Round 1 (Claude proposal)

> Type 1 (`change-risk-scale.md` nÃƒÂ­vel 5-6), protocolo `AGENTS.md` Ã‚Â§4, gate padrÃƒÂ£o 9.0/10.
> **Escopo explicitamente reduzido** (decisÃƒÂ£o do Marcelo, 2026-08-28): sÃƒÂ³ o **mecanismo fÃƒÂ­sico
> de descoberta+exclusÃƒÂ£o em cascata por `tenantId`**, reusando/generalizando o
> `DocumentPurgeWorker` (W3-06/D-061). **Fora de escopo aqui** (feature de produto maior,
> `pilot-readiness-program.md` jÃƒÂ¡ classifica assim): confirmaÃƒÂ§ÃƒÂ£o (Ã¢â€°Â¤15 dias), exportaÃƒÂ§ÃƒÂ£o
> (Ã¢â€°Â¤30 dias, JSON/CSV+documentos+manifesto), a state machine `DataSubjectRequest` completa,
> rotas HTTP, e o efeito de "bloqueio imediato" durante `HELD/PURGING` (exigiria um estado
> `HELD` reconhecido por toda rota de leitura de todo mÃƒÂ³dulo Ã¢â‚¬â€ trabalho de produto real, nÃƒÂ£o
> mecanismo). Este documento entrega sÃƒÂ³ a capacidade: dado um `tenantId`, apagar fisicamente
> todos os seus dados de forma idempotente e auditÃƒÂ¡vel.

## 1. Problema

`pilot-readiness-program.md` (auditoria W3-07): a exclusÃƒÂ£o hoje ÃƒÂ© sÃƒÂ³ um flip de status
independente por entidade (`deleteSubject`/`deleteItem`/`deleteDocument`/
`deleteRequirementAssignment`) Ã¢â‚¬â€ nenhum encadeia para entidades relacionadas. NÃƒÂ£o existe
mecanismo de descoberta tenant-wide nenhum. `privacy-lgpd.md` Ã‚Â§3 exige, para o passo de
exclusÃƒÂ£o do DSR: "InventÃƒÂ¡rio por tenantId em DynamoDB, S3, ÃƒÂ­ndices e provedores... purge
idempotente via GSI6".

## 2. Descoberta tenant-wide Ã¢â‚¬â€ por que Scan ÃƒÂ© a escolha certa aqui (diferente do W3-06)

Confirmado por leitura de `data-model.md` Ã‚Â§1/Ã‚Â§2: **toda entidade carrega `tenantId` como
atributo prÃƒÂ³prio** (nÃƒÂ£o sÃƒÂ³ embutido na PK) Ã¢â‚¬â€ convenÃƒÂ§ÃƒÂ£o "atributos comuns a toda entidade" jÃƒÂ¡
documentada. As ÃƒÂºnicas 3 exceÃƒÂ§ÃƒÂµes de chave tenantless (`IdentityMapping`,
`GuestTokenPointer`/`GuestTokenRateLimit`) tambÃƒÂ©m carregam `tenantId` como atributo "para
reconstruÃƒÂ§ÃƒÂ£o de contexto pÃƒÂ³s-lookup" Ã¢â‚¬â€ confirmado em `data-model.md` linha 59. Logo, um ÃƒÂºnico
`Scan` da tabela com `FilterExpression tenantId = :t` (paginado) descobre **toda** a superfÃƒÂ­cie
de dados de um tenant, sem precisar enumerar tipos de entidade nem suas chaves fÃƒÂ­sicas
distintas (`TENANT#t#ITEM#i`, `TENANT#t#SUBJECT#s`, `TENANT#t#IMPORTJOB#j`, `GUESTTOKEN#...`,
`IDENTITY#...`) uma por uma.

**Por que isso nÃƒÂ£o repete o erro que W3-06 rejeitou** ("Scan geral... rejeitado" no design
anterior): lÃƒÂ¡ o Scan seria um mecanismo de **polling contÃƒÂ­nuo** (rodando a cada N minutos para
sempre) Ã¢â‚¬â€ caro e desnecessÃƒÂ¡rio quando GSI6 jÃƒÂ¡ resolve o caso de uso recorrente. Aqui a
descoberta tenant-wide ÃƒÂ© um evento **raro e deliberado** (uma exclusÃƒÂ£o de conta ÃƒÂ©, na melhor das
hipÃƒÂ³teses, um evento por tenant na vida do tenant, nunca um job recorrente) Ã¢â‚¬â€ o custo de um
Scan completo da tabela, pago uma vez por exclusÃƒÂ£o real, ÃƒÂ© aceitÃƒÂ¡vel e nenhum GSI plausÃƒÂ­vel
evitaria essa caracterÃƒÂ­stica (um GSI por `tenantId` sozinho teria a MESMA cardinalidade de
partiÃƒÂ§ÃƒÂ£o que o Scan jÃƒÂ¡ visita, sem ganho real, e criaria um ÃƒÂ­ndice novo cujo ÃƒÂºnico propÃƒÂ³sito ÃƒÂ©
um evento raro Ã¢â‚¬â€ desproporcional).

## 3. Mecanismo proposto

1. **`TenantCascadeDeletionService.requestDeletion(tenantId)`** (novo, mÃƒÂ³dulo novo
   `src/modules/tenant-deletion/` Ã¢â‚¬â€ deliberadamente nÃƒÂ£o dentro de `document/` ou `expiration/`,
   jÃƒÂ¡ que atravessa todos os mÃƒÂ³dulos): inicia um `Scan` paginado (`Limit` por pÃƒÂ¡gina, cursor
   `ExclusiveStartKey`) filtrando `tenantId = :t`. Para cada pÃƒÂ¡gina, `TransactWriteItems` em
   lotes de atÃƒÂ© 25 (limite real do DynamoDB) marcando cada linha com o mesmo par
   `GSI6PK/GSI6SK = WORKSTATE#PURGE_PENDING/<purgeAfter>#TENANT#<t>#<entityType>#<sk-derivado>`
   Ã¢â‚¬â€ **generalizaÃƒÂ§ÃƒÂ£o direta do par jÃƒÂ¡ criado para `Document`/`DocumentPurgeReceipt`** em
   `document-store.ts`, movida para um local compartilhado (`src/shared/dynamodb/purge-gsi6.ts`)
   jÃƒÂ¡ que passa a ser usada por mÃƒÂºltiplos mÃƒÂ³dulos, nÃƒÂ£o sÃƒÂ³ `document`.
   - `purgeAfter` aqui ÃƒÂ© **imediato** (`now`, nÃƒÂ£o os 30 dias do soft-delete individual) Ã¢â‚¬â€ uma
     exclusÃƒÂ£o de tenant jÃƒÂ¡ ÃƒÂ© a decisÃƒÂ£o final (LGPD art. 18, direito de eliminaÃƒÂ§ÃƒÂ£o), nÃƒÂ£o reabre
     uma janela de arrependimento por entidade. Este ÃƒÂ© o ÃƒÅ¡NICO ponto onde este mecanismo diverge
     do padrÃƒÂ£o "soft-delete + purgeAfter futuro" do resto do sistema Ã¢â‚¬â€ decisÃƒÂ£o explÃƒÂ­cita, nÃƒÂ£o
     descuido.
   - `legalHold` (campo hoje sÃƒÂ³ em `Document`, W3-06) precisa existir em qualquer entidade que
     este mecanismo tocar, OU o mecanismo verifica hold sÃƒÂ³ no que jÃƒÂ¡ o tem (`Document`) e aceita
     que outras entidades nÃƒÂ£o tÃƒÂªm hold hoje (nenhuma tem, ÃƒÂ© um campo novo do W3-06) Ã¢â‚¬â€ **decisÃƒÂ£o
     proposta**: manter a condiÃƒÂ§ÃƒÂ£o `attribute_not_exists(legalHold) OR legalHold = :false` em
     TODAS as escritas deste mecanismo (nÃƒÂ£o sÃƒÂ³ `Document`), entÃƒÂ£o qualquer entidade que ganhar
     `legalHold` no futuro jÃƒÂ¡ ÃƒÂ© respeitada por este mecanismo sem mudanÃƒÂ§a nenhuma nele.
2. **GeneralizaÃƒÂ§ÃƒÂ£o do `DocumentPurgeWorker` Ã¢â€ â€™ `EntityPurgeWorker`** (renomeado, ou mantido como
   estÃƒÂ¡ mas com um terceiro branch): candidatos com `entityType` fora de
   `{Document, DocumentPurgeReceipt}` seguem um caminho **genÃƒÂ©rico, sem S3**: claim (mesma
   `extraCondition` de fence) Ã¢â€ â€™ `Delete` direto, sem chamada a `DocumentObjectStore`. Documentos
   (`entityType === "Document"`) continuam pelo caminho jÃƒÂ¡ existente (apaga o objeto S3 real
   antes do `Delete`). **Nenhuma mudanÃƒÂ§a no branch `Document` existente.**
3. **Sem novo estado terminal `STUCK` genÃƒÂ©rico ainda** Ã¢â‚¬â€ reusa o jÃƒÂ¡ existente (`purgeAttempts`/
   `purgeStatus: "STUCK"`), mas como campo genÃƒÂ©rico gravado por este mecanismo em qualquer
   entidade (nÃƒÂ£o exige que o tipo jÃƒÂ¡ tivesse esses campos Ã¢â‚¬â€ sÃƒÂ£o atributos DynamoDB soltos,
   schemaless por natureza, presentes sÃƒÂ³ nas linhas que passaram por este mecanismo).
4. **IdempotÃƒÂªncia do prÃƒÂ³prio `requestDeletion`**: um `TenantDeletionRequest` (novo, pequeno)
   registra `tenantId`/`requestedAt`/`status: "DISCOVERING"|"COMPLETED"`/`lastScanCursor` Ã¢â‚¬â€
   permite retomar um Scan interrompido (Lambda timeout, crash) do cursor exato em vez de
   recomeÃƒÂ§ar do zero, e serve de tombstone auditÃƒÂ¡vel de que a exclusÃƒÂ£o foi solicitada e
   concluÃƒÂ­da (sem reter nenhum dado do tenant em si).

## 4. Fora de escopo, explicitamente

- Bloqueio imediato de uso (exigiria checar um estado `HELD` em toda `authorize()`/leitura de
  todo mÃƒÂ³dulo Ã¢â‚¬â€ produto, nÃƒÂ£o mecanismo).
- RevogaÃƒÂ§ÃƒÂ£o de canais/links externos (WhatsApp/e-mail/tokens de guest ainda vÃƒÂ¡lidos) Ã¢â‚¬â€ precisa
  de lÃƒÂ³gica por canal, nÃƒÂ£o genÃƒÂ©rica.
- Provedores externos (SES/Textract/Bedrock nÃƒÂ£o retÃƒÂªm dado nosso por design Ã¢â‚¬â€ confirmar isso ÃƒÂ©
  tarefa de auditoria de subprocessadores, `privacy-lgpd.md` Ã‚Â§5, nÃƒÂ£o deste mecanismo).
- Rota HTTP / autorizaÃƒÂ§ÃƒÂ£o de quem pode chamar `requestDeletion` Ã¢â‚¬â€ este documento entrega a
  capacidade programÃƒÂ¡tica; quem a invoca (admin interno, futura rota DSR) ÃƒÂ© decisÃƒÂ£o de W3-07
  completo, nÃƒÂ£o deste mecanismo.

## 5. Pergunta para Rodada B (Codex)

(a) O Scan tenant-wide como evento raro ÃƒÂ© uma escolha defensÃƒÂ¡vel, ou existe um argumento real
contra mesmo nesse regime de uso? (b) A generalizaÃƒÂ§ÃƒÂ£o do worker para um terceiro branch
"genÃƒÂ©rico sem S3" introduz algum risco que o branch `Document` nÃƒÂ£o tem? (c) `purgeAfter`
imediato (sem os 30 dias) para exclusÃƒÂ£o de tenant ÃƒÂ© coerente com `privacy-lgpd.md`, ou falta
alguma salvaguarda (ex.: um perÃƒÂ­odo de confirmaÃƒÂ§ÃƒÂ£o antes do Scan sequer comeÃƒÂ§ar)?

# W3-06 Ã¢â‚¬â€ Desenho final do mecanismo de purga real de `USER_DOCUMENT` (Document)

> Protocolo ClaudeÃ¢â€ â€Codex completo, 6 rodadas, gate padrÃƒÂ£o 9.0/10. Codex final: **9,2/10,
> APROVADO**. Claude final: **9,1/10** (nenhum achado bloqueante residual identificado
> independentemente; only a implementaÃƒÂ§ÃƒÂ£o real, nÃƒÂ£o mais este desenho, poderÃƒÂ¡ revelar detalhe
> novo Ã¢â‚¬â€ mesma disciplina de "nota nÃƒÂ£o ÃƒÂ© garantia de zero achado na implementaÃƒÂ§ÃƒÂ£o" jÃƒÂ¡ registrada
> em outras decisÃƒÂµes deste repositÃƒÂ³rio). HistÃƒÂ³rico completo rodada-a-rodada:
> `claude-proposal-round{1..6}.md` / `codex-round{1..5}-critique-full.txt` (Rodada 6 sem arquivo
> de crÃƒÂ­tica solto Ã¢â‚¬â€ aprovaÃƒÂ§ÃƒÂ£o estÃƒÂ¡ na cauda de `codex-round6-out.txt` referenciado aqui).

## Resumo executivo do mecanismo aprovado

1. **Fila primÃƒÂ¡ria Ã¢â‚¬â€ claim por lease sobre GSI6** (reuso do idioma `WORKSTATE#PENDING/CLAIMED`
   jÃƒÂ¡ provado por `ReminderReconciliation`): `Document` ganha `GSI6PK`/`GSI6SK` escritos na MESMA
   transaÃƒÂ§ÃƒÂ£o do soft-delete (`document-deletion-service.ts`), valor inicial
   `GSI6PK = "WORKSTATE#PURGE_PENDING"`, `GSI6SK = <purgeAfter>#TENANT#<t>#DOCUMENT#<d>`.
2. **`DocumentPurgeWorker`** (agendado via `aws_scheduler_schedule`, cadÃƒÂªncia 6h, mesmo padrÃƒÂ£o de
   `upload_slot_reconciliation`): a cada execuÃƒÂ§ÃƒÂ£o, sem cursor entre invocaÃƒÂ§ÃƒÂµes (atÃƒÂ© 25
   candidatos/consulta):
   - Query A: `WORKSTATE#PURGE_PENDING`, `GSI6SK < now` Ã¢â€ â€™ tenta **claim** de cada candidato via
     `TransactWriteItems` condicionado (`buildVersionedUpdate` + `extraConditions` novas, ver
     abaixo) movendo para `WORKSTATE#PURGE_CLAIMED` com lease de 15min.
   - Query B (reconciliaÃƒÂ§ÃƒÂ£o de lease): `WORKSTATE#PURGE_CLAIMED`, `GSI6SK < now` Ã¢â€ â€™ devolve a
     `PENDING` (se `purgeAttempts < 5`) ou marca `purgeStatus: "STUCK"` e remove de ambas as filas
     (se `purgeAttempts >= 5`), sempre condicionado ÃƒÂ  mesma versÃƒÂ£o/GSI6SK lidos.
   - ApÃƒÂ³s claim bem-sucedido de um `Document`: deleta o objeto S3 real Ã¢â‚¬â€
     `doc.cleanObject` se existir; senÃƒÂ£o `doc.uploadEvidence?.object ?? doc.malwareEvidence?.object`
     se existir; senÃƒÂ£o nenhuma chamada S3 (nunca usar `doc.quarantineObject` diretamente Ã¢â‚¬â€ seu
     `versionId` ÃƒÂ© sempre `""`, confirmado em cÃƒÂ³digo real). Depois, `TransactWriteItems` final:
     `Delete` da linha `Document` + `Put DocumentPurgeReceipt` (mesma transaÃƒÂ§ÃƒÂ£o).
   - ApÃƒÂ³s claim de um `DocumentPurgeReceipt` (`entityType` discrimina o branch): sem chamada S3,
     sÃƒÂ³ `Delete` da prÃƒÂ³pria linha.
3. **`legalHold?: boolean`** novo campo em `Document`. Claim do purge condiciona
   `attribute_not_exists(legalHold) OR legalHold = :false`. **Regra normativa vinculante desta
   decisÃƒÂ£o** para qualquer escritor futuro de `legalHold = true`: deve incluir a condiÃƒÂ§ÃƒÂ£o
   `attribute_not_exists(GSI6PK) OR GSI6PK <> :purgeClaimed` na prÃƒÂ³pria escrita OCC Ã¢â‚¬â€ torna as
   duas transaÃƒÂ§ÃƒÂµes mutuamente exclusivas por construÃƒÂ§ÃƒÂ£o (prova completa nas Rodadas 5-6).
4. **Sem lifecycle S3 incondicional** (removido apÃƒÂ³s achado real da Rodada 1 Ã¢â‚¬â€ um
   `expiration.days` de bucket inteiro destruiria documentos ativos, jÃƒÂ¡ que o prazo de negÃƒÂ³cio ÃƒÂ©
   variÃƒÂ¡vel desde um evento posterior ÃƒÂ  criaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o a prÃƒÂ³pria criaÃƒÂ§ÃƒÂ£o). Rede de seguranÃƒÂ§a:
   alarme CloudWatch sobre idade do candidato mais antigo em `PENDING`/`CLAIMED` (duas sÃƒÂ©ries) +
   alarme dedicado `document_purge_stuck_total`.
5. **`purgeAfterTtl`** (TTL nativo jÃƒÂ¡ habilitado na tabela) **nunca ÃƒÂ© populado por `Document`**
   nesta decisÃƒÂ£o Ã¢â‚¬â€ colidiria com o prÃƒÂ³prio mecanismo primÃƒÂ¡rio (achado real da Rodada 2).
6. **`DocumentPurgeReceipt`** novo, nÃƒÂ£o sensÃƒÂ­vel: `retentionClass: "DELIVERY_RECORD"`,
   `purgeAfter = purgedAt + 180 dias`, ganha seu prÃƒÂ³prio ponteiro GSI6 na mesma transaÃƒÂ§ÃƒÂ£o que o
   cria Ã¢â‚¬â€ autopurgado pelo mesmÃƒÂ­ssimo worker (branch por `entityType`).
7. **`buildVersionedUpdate` (`occ.ts`) ganha `extraConditions?: Array<{ expression, names?, values? }>`**
   Ã¢â‚¬â€ extensÃƒÂ£o aditiva, com checagem de colisÃƒÂ£o de placeholder (nunca reusar `:now`/`#version`/etc,
   lanÃƒÂ§ar erro descritivo se colidir).
8. **IAM**: `DocumentPurgeWorker` ÃƒÂ© a **quarta** role com `gsi6_read_policy_json` (atualizar todos
   os comentÃƒÂ¡rios "EXACTLY THREE" para "EXACTLY FOUR" em `main.tf`/`dynamo-table/main.tf`);
   `dynamodb:TransactWriteItems` na tabela base (nunca no ÃƒÂ­ndice); `s3:DeleteObjectVersion`
   (nunca `DeleteObject`/`GetObject`) nos buckets `clean`+`quarantine`, escopado sÃƒÂ³ a esta role.

## Escopo explicitamente fora desta decisÃƒÂ£o (W3-07 e alÃƒÂ©m)

- Cascata de deleÃƒÂ§ÃƒÂ£o DSR completa (`DataSubjectRequest` state machine, export, rotas HTTP) Ã¢â‚¬â€
  W3-07 ÃƒÂ© feature de produto maior, fora de escopo aqui (D-059 jÃƒÂ¡ registrava isso).
- GeneralizaÃƒÂ§ÃƒÂ£o do par GSI6/worker para `entityType` arbitrÃƒÂ¡rio alÃƒÂ©m de
  `Document`/`DocumentPurgeReceipt` Ã¢â‚¬â€ W3-07 decide isso quando tiver requisitos reais.
- Workflow completo de aprovaÃƒÂ§ÃƒÂ£o/`reviewAt` de `legalHold` (`privacy-lgpd.md` Ã‚Â§3) Ã¢â‚¬â€ sÃƒÂ³ o campo
  booleano e a condiÃƒÂ§ÃƒÂ£o de exclusÃƒÂ£o mÃƒÂºtua nascem aqui; setter real ÃƒÂ© trabalho futuro.
- Rota HTTP de reversÃƒÂ£o manual de `purgeStatus: "STUCK"` Ã¢â‚¬â€ reversÃƒÂ£o inicial ÃƒÂ© operaÃƒÂ§ÃƒÂ£o manual
  direta na tabela (break-glass), mesma disciplina de qualquer intervenÃƒÂ§ÃƒÂ£o administrativa hoje.

## PrÃƒÂ³ximo passo

ImplementaÃƒÂ§ÃƒÂ£o seguindo o mesmo rigor de evidÃƒÂªncia real (Camada 3 quando aplicÃƒÂ¡vel) usado na
Wave 2 Ã¢â‚¬â€ ver checklist de implementaÃƒÂ§ÃƒÂ£o no handoff da sessÃƒÂ£o.


codex
A proposta jÃ¡ revela duas tensÃµes centrais que precisam ser provadas no cÃ³digo: o cursor persistido nÃ£o cria um snapshot estÃ¡vel, e â€œqualquer `entityType` sem S3â€ pressupÃµe que ausÃªncia de S3 equivale a ausÃªncia de semÃ¢ntica de retenÃ§Ã£o/efeito colateral. Agora estou mapeando os tipos e as condiÃ§Ãµes OCC reais para separar falhas bloqueantes de simples lacunas de detalhamento.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"export (interface|type)|interface .*\\{|entityType|tenantId|GSI6PK|TransactWrite|buildVersioned(Delete|Update)|NotificationOutcome|OutboxEvent\" src/modules src/workers/document-purge src/shared/dynamodb/occ.ts docs/architecture/data-model.md | Select-Object -First 1200" in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-28T20:12:54.129461Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1 seconds
Total output lines: 1200
Output:
docs/architecture/data-model.md:17:`tenantId` obrigatÃ³rio em toda chave DynamoDB, idempotency key, objeto S3, evento e mensagem (SCALE-004). MVP: `tenantId=userId`; futuro: `tenantId=organizationId`. Identidade autenticÃ¡vel global no IdP (Cognito); `User` representa o perfil dentro do tenant, com `identitySubject` desacoplado do `sub` do Cognito (decisÃ£o Â§4 da arquitetura).
docs/architecture/data-model.md:21:ConvenÃ§Ã£o fÃ­sica: `PK = TENANT#<tenantId>#<aggregate>`, `SK = <entityType>#<id>[#...]`. IDs UUIDv7/ULID (ordenÃ¡veis por tempo, evitam hot-key sequencial de auto-incremento). Datas ISO-8601 UTC; timezones IANA. Atributos comuns a toda entidade (design-target, ver nota de status abaixo): `entityType`, `schemaVersion`, `createdAt`, `updatedAt`, `version`, `deletedAt?`, `retentionClass`, `purgeAfter?`.
docs/architecture/data-model.md:43:| IdentityMapping | cognitoSub, userId, tenantId â€” criado atomicamente (`ConditionExpression attribute_not_exists`) no primeiro login | `IDENTITY#cognitoSub#<sub>` / `MAP` |
docs/architecture/data-model.md:50:| GuestTokenPointer *(M10, D-037, D-045 â€” terceira exceÃ§Ã£o tenantless)* | selectorHash (na PK), secretHash (HMAC-SHA256+pepper â€” nunca o secret em texto puro), tenantId/subjectId/assignmentId/documentRequestId (para reconstruÃ§Ã£o de contexto pÃ³s-lookup), tokenExpiresAt (`min(now+14d, deadline)`) | `GUESTTOKEN#<selectorHash>` / `POINTER` |
docs/architecture/data-model.md:59:**IdentityMapping â€” segunda de trÃªs exceÃ§Ãµes de particionamento (`implementation-blueprint.md` Â§23.1)**: fora do agregado `TENANT#...` porque a busca `cognitoSub â†’ userId/tenantId` ocorre *antes* de `tenantId` ser conhecido â€” mesmo motivo estrutural do GSI3 (a chave nÃ£o pode depender de um dado ainda nÃ£o resolvido no momento da consulta). **GuestTokenPointer/GuestTokenRateLimit (M10) sÃ£o a terceira**, pelo mesmo motivo: o convidado apresenta sÃ³ o token opaco, `tenantId` sÃ³ Ã© conhecido depois do lookup pelo `selectorHash`.
docs/architecture/data-model.md:61:## 3. GSIs (todos com chave iniciando por `TENANT#tenantId`, exceto GSI3 â€” ver exceÃ§Ã£o documentada abaixo)
docs/architecture/data-model.md:64:- **GSI3 â€” scheduler global de ocorrÃªncias agendadas** *(entendimento atualizado em M10/D-039/D-046, `roadmap-evolution/12-automated-chasing-capacity-review.md` â€” mudanÃ§a de documentaÃ§Ã£o, nÃ£o de mecanismo)*: `PK=GSI3PK=DUE#yyyyMMddHHmm#NN`, `SK=GSI3SK` discriminado por `entityType` na prÃ³pria forma da chave â€” `TENANT#t#OCCURRENCE#<occurrenceId>` para `ReminderOccurrence` (formato original, zero migraÃ§Ã£o das linhas jÃ¡ em produÃ§Ã£o) e `TENANT#t#CHASING#<occurrenceId>` para `DocumentChasingOccurrence` (M10 cluster 4, agregado-irmÃ£o â€” nunca generaliza `ReminderOccurrence`). 4 shards/minuto inicial (decisÃ£o Fase 3 Rodada 4); produtor condiciona por status/versÃ£o, branca por `entityType` ao processar cada linha lida, e falha-fechado com alarme se a forma da SK nÃ£o corresponder a nenhum discriminador conhecido. **CorreÃ§Ã£o Type 1 (ratificada em `implementation-blueprint.md` Â§9.2/Â§9.3, decisions-log.md D-028)**: a chave original (`PK=TENANT#t#DUE#...`) nÃ£o Ã© consultÃ¡vel pelo `ReminderProducer` â€” `Query` do DynamoDB exige PK exata, e o producer do minuto `M` nÃ£o sabe a priori quais tenants tÃªm ocorrÃªncia em `M`. GSI3 Ã© portanto a **Ãºnica exceÃ§Ã£o** Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`": a PK Ã© global (sem `tenantId`), com `tenantId` preservado na SK e no item base para reconstruÃ§Ã£o segura do contexto. Salvaguardas obrigatÃ³rias (detalhadas em `implementation-blueprint.md` Â§9.2, mantidas para os dois `entityType`): IAM do Ã­ndice restrito exclusivamente ao producer (nenhuma rota tenant-facing tem `Query` neste GSI); projeÃ§Ã£o mÃ­nima, sem conteÃºdo de negÃ³cio sensÃ­vel; teste de isolamento automatizado provando que nenhuma API tenant-facing o alcanÃ§a, direta ou indiretamente; dimensionamento de shards por pico global, nÃ£o por tenant; alarmes segmentados por `entityType` (D-046) para que um pico anÃ´malo de um tipo nÃ£o se esconda no agregado do outro.
docs/architecture/data-model.md:74:| MaterializaÃ§Ã£o de ReminderOccurrence | `tenantId\|itemId\|itemVersion\|policyId\|policyVersion\|triggerId\|scheduledAtUtc` |
docs/architecture/data-model.md:75:| NotificationIntent | `tenantId\|occurrenceId\|channel\|recipient\|intentKind` |
docs/architecture/data-model.md:76:| NotificationAttempt | `tenantId\|intentId\|provider\|attemptNumber\|redriveGeneration` |
docs/architecture/data-model.md:77:| ExtraÃ§Ã£o (ExtractedField) | `tenantId\|documentId\|documentVersion\|pipelineVersion` |
docs/architecture/data-model.md:78:| RenovaÃ§Ã£o de item | `tenantId\|sourceItemId\|sourceVersion\|cycle` |
docs/architecture/data-model.md:80:| Upload | `UploadSlot` reservado atomicamente via `TransactWriteItems` no momento da geraÃ§Ã£o da URL presigned (decisÃ£o Fase 3 Rodada 6) |
docs/architecture/data-model.md:83:Toda entidade mutÃ¡vel usa `version`; updates exigem `ConditionExpression: version=:expected` e incrementam atomicamente (optimistic concurrency control, decisÃ£o Fase 3 Rodada 6). Alterar vencimento grava o item, cancela ocorrÃªncias antigas e cria o evento de outbox crÃ­tico (`ItemDueDateChanged`) numa Ãºnica transaÃ§Ã£o (`TransactWriteItems`) â€” fecha a corrida identificada no Red Team cenÃ¡rio 13. AplicaÃ§Ã£o de resultado de extraÃ§Ã£o verifica simultaneamente as versÃµes de `Document` e `ExpirationItem`; divergÃªncia ou tombstone descarta o resultado (Red Team cenÃ¡rio 14).
docs/architecture/data-model.md:101:**ExtractedField â€” modelo hÃ­brido com ExtractionRun**: adicionada entidade `ExtractionRun` como item-pai da execuÃ§Ã£o (estado agregado, `pipelineVersion`, `documentVersion`, timestamps de inÃ­cio/fim, resumo), com `ExtractedField` individuais referenciando `runId`. Corrige tambÃ©m a chave de idempotÃªncia, que antes correspondia Ã  execuÃ§Ã£o como um todo mas era declarada ambiguamente sobre mÃºltiplos itens de campo: agora a idempotÃªncia da **execuÃ§Ã£o** Ã© `tenantId|documentId|documentVersion|pipelineVersion` (chave do `ExtractionRun`), e cada `ExtractedField` Ã© unicamente identificado por `runId + fieldName` (nÃ£o precisa de chave de idempotÃªncia prÃ³pria, pois Ã© sempre escrito uma vez por `ExtractionRun` jÃ¡ idempotente).
docs/architecture/data-model.md:125:| TextractJob | jobId, tenantId, itemId, documentId, documentVersion, runId, clientRequestToken, status (STARTED/COMPLETED/FAILED), `taskTokenCiphertext?` (cifrado, presente sÃ³ enquanto o callback pode ainda resolver a task â€” removido em qualquer desfecho terminal, design Â§3), ttl (curto, diagnÃ³stico/reconciliaÃ§Ã£o) | `TEXTRACTJOB#<jobId>` / `TEXTRACTJOB#<jobId>` |
docs/architecture/data-model.md:127:**Chave deliberadamente nÃ£o tenant-scoped** (Ãºnica exceÃ§Ã£o nesta tabela): `COMPLETE_OCR` sÃ³ recebe `JobId`/`JobTag` da notificaÃ§Ã£o SNS/SQS, sem contexto de tenant disponÃ­vel antes de ler este registro â€” uma chave `TENANT#t#...` exigiria um GSI sÃ³ para permitir "encontre o job com este jobId", para uma entidade cujo propÃ³sito inteiro Ã‰ essa busca. Um item self-keyed evita o GSI. `ClientRequestToken` (determinÃ­stico, `sha256(tenantId|documentId|documentVersion|pipelineVersion|runId)`) Ã© o mecanismo de reconciliaÃ§Ã£o para o caso de job Ã³rfÃ£o (notificaÃ§Ã£o sem `TextractJob` correspondente â€” design Â§2): confirma-se via uma chamada a `GetDocumentTextDetection`, descarta-se o resultado, nunca Ã© tratado como erro.
src/shared/dynamodb/occ.ts:10:export interface EntityKey {
src/shared/dynamodb/occ.ts:15:export interface VersionedUpdateInput {
src/shared/dynamodb/occ.ts:18:  tenantId: string;
src/shared/dynamodb/occ.ts:23:   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
src/shared/dynamodb/occ.ts:31:   * `legalHold`/`GSI6PK`/`purgeAfter` atomically alongside the version check). Each entry's
src/shared/dynamodb/occ.ts:34:   * with the base condition's (#version/#tenantId/#updatedAt/:expectedVersion/:tenantId/:one/:now)
src/shared/dynamodb/occ.ts:35:   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
src/shared/dynamodb/occ.ts:41:export interface DynamoUpdateCommandInput {
src/shared/dynamodb/occ.ts:53: * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
src/shared/dynamodb/occ.ts:55:export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
src/shared/dynamodb/occ.ts:60:    "#tenantId": "tenantId",
src/shared/dynamodb/occ.ts:65:    ":tenantId": input.tenantId,
src/shared/dynamodb/occ.ts:95:  const baseConditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
src/shared/dynamodb/occ.ts:118:export interface DynamoPutCommandInput {
src/shared/dynamodb/occ.ts:127: * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
src/shared/dynamodb/occ.ts:138: * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
src/shared/dynamodb/occ.ts:147:export interface TransactPutEntry {
src/shared/dynamodb/occ.ts:151:export interface TransactUpdateEntry {
src/shared/dynamodb/occ.ts:155:export interface DynamoConditionCheckCommandInput {
src/shared/dynamodb/occ.ts:163:export interface TransactConditionCheckEntry {
src/shared/dynamodb/occ.ts:167:export interface DynamoDeleteCommandInput {
src/shared/dynamodb/occ.ts:175:export interface TransactDeleteEntry {
src/shared/dynamodb/occ.ts:182: * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
src/shared/dynamodb/occ.ts:184: * `buildVersionedUpdate` - see that function's docs.
src/shared/dynamodb/occ.ts:186:export function buildVersionedDelete(input: {
src/shared/dynamodb/occ.ts:189:  tenantId: string;
src/shared/dynamodb/occ.ts:193:  const names: Record<string, string> = { "#version": "version", "#tenantId": "tenantId" };
src/shared/dynamodb/occ.ts:194:  const values: Record<string, unknown> = { ":expectedVersion": input.expectedVersion, ":tenantId": input.tenantId };
src/shared/dynamodb/occ.ts:195:  const conditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
src/shared/dynamodb/occ.ts:218:export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;
src/shared/dynamodb/occ.ts:221: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src/shared/dynamodb/occ.ts:227: * re-asserted atomically inside the same TransactWriteItems as the write it gates).
src/shared/dynamodb/occ.ts:299:/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
src/workers/document-purge\purge.ts:23: *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
src/workers/document-purge\purge.ts:24: *    the claimed version + `GSI6PK = WORKSTATE#PURGE_CLAIMED`) + `Put` a `DocumentPurgeReceipt`
src/workers/document-purge\purge.ts:34: *    `attribute_not_exists(GSI6PK) OR GSI6PK <> "WORKSTATE#PURGE_CLAIMED"` in its own
src/workers/document-purge\purge.ts:40:  buildVersionedDelete,
src/workers/document-purge\purge.ts:41:  buildVersionedUpdate,
src/workers/document-purge\purge.ts:45:  GSI6PK_PURGE_CLAIMED,
src/workers/document-purge\purge.ts:46:  GSI6PK_PURGE_PENDING,
src/workers/document-purge\purge.ts:60:export interface DocumentPurgeCandidate {
src/workers/document-purge\purge.ts:61:  entityType: "Document";
src/workers/document-purge\purge.ts:64:  tenantId: string;
src/workers/document-purge\purge.ts:69:  GSI6PK: string;
src/workers/document-purge\purge.ts:80:export interface ReceiptPurgeCandidate {
src/workers/document-purge\purge.ts:81:  entityType: "DocumentPurgeReceipt";
src/workers/document-purge\purge.ts:84:  tenantId: string;
src/workers/document-purge\purge.ts:87:  GSI6PK: string;
src/workers/document-purge\purge.ts:92:export type PurgeCandidate = DocumentPurgeCandidate | ReceiptPurgeCandidate;
src/workers/document-purge\purge.ts:94:export interface DocumentPurgeWorkerDeps {
src/workers/document-purge\purge.ts:102:export interface PurgeRunResult {
src/workers/document-purge\purge.ts:124:        Update: buildVersionedUpdate({
src/workers/document-purge\purge.ts:127:          tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:130:            GSI6PK: GSI6PK_PURGE_CLAIMED,
src/workers/document-purge\purge.ts:131:            GSI6SK: buildDocumentPurgeClaimGsi6Sk(claimExpiresAt, candidate.tenantId, candidate.documentId),
src/workers/document-purge\purge.ts:137:            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
src/workers/document-purge\purge.ts:155:    ...documentPurgeReceiptKey(candidate.tenantId, candidate.documentId),
src/workers/document-purge\purge.ts:156:    entityType: "DocumentPurgeReceipt",
src/workers/document-purge\purge.ts:157:    tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:166:    GSI6PK: GSI6PK_PURGE_PENDING,
src/workers/document-purge\purge.ts:167:    GSI6SK: buildPurgeReceiptGsi6Sk(receiptPurgeAfter, candidate.tenantId, candidate.documentId),
src/workers/document-purge\purge.ts:175:      Delete: buildVersionedDelete({
src/workers/document-purge\purge.ts:178:        tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:180:        extraConditions: [{ expression: "GSI6PK = :expectedClaimed", values: { ":expectedClaimed": GSI6PK_PURGE_CLAIMED } }],
src/workers/document-purge\purge.ts:194:        Delete: buildVersionedDelete({
src/workers/document-purge\purge.ts:197:          tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:200:            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
src/workers/document-purge\purge.ts:220:          Update: buildVersionedUpdate({
src/workers/document-purge\purge.ts:223:            tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:226:            remove: ["GSI6PK", "GSI6SK"],
src/workers/document-purge\purge.ts:227:            extraConditions: [{ expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_CLAIMED, ":expectedSk": candidate.GSI6SK } }],
src/workers/document-purge\purge.ts:235:        Update: buildVersionedUpdate({
src/workers/document-purge\purge.ts:238:          tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:240:          set: { GSI6PK: GSI6PK_PURGE_PENDING, GSI6SK: buildDocumentPurgeGsi6Sk(candidate.purgeAfter, candidate.tenantId, candidate.documentId) },
src/workers/document-purge\purge.ts:241:          extraConditions: [{ expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_CLAIMED, ":expectedSk": candidate.GSI6SK } }],
src/workers/document-purge\purge.ts:259:    if (candidate.entityType === "Document") {
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:11:import { GSI6PK_PURGE_CLAIMED, GSI6PK_PURGE_PENDING } from "../../modules/document/ports/document-store.js";
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:31:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:32:          ExpressionAttributeValues: { ":pk": GSI6PK_PURGE_PENDING, ":before": input.before },
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:53:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:54:          ExpressionAttributeValues: { ":pk": GSI6PK_PURGE_CLAIMED, ":before": input.before },
src/workers/document-purge\document-purge-candidate-source.ts:9:export interface Page<T> {
src/workers/document-purge\document-purge-candidate-source.ts:14:export interface DocumentPurgeCandidateSource {
src/workers/document-purge\document-purge-candidate-source.ts:15:  /** GSI6PK="WORKSTATE#PURGE_PENDING" (global), GSI6SK < now. Mixed `Document`/
src/workers/document-purge\document-purge-candidate-source.ts:16:   * `DocumentPurgeReceipt` rows, discriminated by `entityType`. */
src/workers/document-purge\document-purge-candidate-source.ts:18:  /** GSI6PK="WORKSTATE#PURGE_CLAIMED" (global), GSI6SK < now (lease already expired). Only
src/modules\identity\ports\identity-store.ts:8:export interface EntityKey {
src/modules\identity\ports\identity-store.ts:13:export interface IdentityStore {
src/modules\notification\providers\ses-email-adapter.ts:60:            { Name: "et_tenant_id", Value: input.tags.tenantId },
src/modules\expiration\ports\expiration-store.ts:7: * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
src/modules\expiration\ports\expiration-store.ts:11:import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\expiration\ports\expiration-store.ts:15:export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
src/modules\expiration\ports\expiration-store.ts:18:export interface Gsi1QueryInput {
src/modules\expiration\ports\expiration-store.ts:24:export interface ExpirationStore {
src/modules\expiration\ports\expiration-store.ts:36:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\subject\ports\subject-store.ts:5: * agregado + contador + audit numa Ãºnica TransactWriteItems (nunca separado, ver
src/modules\subject\ports\subject-store.ts:8:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\subject\ports\subject-store.ts:10:export type { EntityKey, TransactWriteEntry };
src/modules\subject\ports\subject-store.ts:13:export interface Gsi7QueryInput {
src/modules\subject\ports\subject-store.ts:19:export interface SubjectStore {
src/modules\subject\ports\subject-store.ts:30:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\ports\import-store.ts:7:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\import\ports\import-store.ts:9:export type { EntityKey, TransactWriteEntry };
src/modules\import\ports\import-store.ts:12:export interface ImportStore {
src/modules\import\ports\import-store.ts:16:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\ports\import-object-store.ts:10:export interface ImportObjectStore {
src/modules\subject\ports\expiration-item-lookup.ts:7:export interface ExpirationItemLookup {
src/modules\subject\ports\expiration-item-lookup.ts:10:  itemExists(tenantId: string, itemId: string): Promise<boolean>;
src/modules\document\ports\upload-url-signer.ts:4:export interface PresignUploadInput {
src/modules\document\ports\upload-url-signer.ts:14:export interface PresignUploadResult {
src/modules\document\ports\upload-url-signer.ts:19:export interface UploadUrlSigner {
src/modules\expiration\domain\item-watch.ts:10:export type ItemWatchStatus = "ACTIVE" | "REMOVED";
src/modules\expiration\domain\item-watch.ts:12:export interface ItemWatch extends EntityKey {
src/modules\expiration\domain\item-watch.ts:13:  entityType: "ItemWatch";
src/modules\expiration\domain\item-watch.ts:15:  tenantId: string;
src/modules\expiration\domain\item-watch.ts:23:export function itemWatchKey(tenantId: string, itemId: string, userId: string): EntityKey {
src/modules\expiration\domain\item-watch.ts:24:  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `WATCH#USER#${userId}` };
src/modules\expiration\persistence\dynâ€¦28901 tokens truncatedâ€¦rseGsi3Sk(gsi3sk: string): { tenantId: string; occurrenceId: string } {
src/modules\reminder\domain\gsi3-parse.ts:7:  return { tenantId: match[1] as string, occurrenceId: match[2] as string };
src/modules\notification\domain\notification-attempt.ts:11:export type NotificationAttemptStatus =
src/modules\notification\domain\notification-attempt.ts:23:export interface NotificationAttempt extends EntityKey {
src/modules\notification\domain\notification-attempt.ts:24:  // PK = TENANT#<tenantId>#INTENT#<intentId>, SK = ATTEMPT#<attemptNumber padded>#<attemptId>
src/modules\notification\domain\notification-attempt.ts:25:  entityType: "NotificationAttempt";
src/modules\notification\domain\notification-attempt.ts:26:  tenantId: string;
src/modules\notification\domain\notification-attempt.ts:77:export function notificationAttemptKey(tenantId: string, intentId: string, attemptNumber: number, attemptId: string): EntityKey {
src/modules\notification\domain\notification-attempt.ts:78:  return { PK: `TENANT#${tenantId}#INTENT#${intentId}`, SK: `ATTEMPT#${pad(attemptNumber)}#${attemptId}` };
src/modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
src/modules\notification\domain\notification-attempt.ts:89:export interface NotificationAttemptLookup extends EntityKey {
src/modules\notification\domain\notification-attempt.ts:90:  // PK = TENANT#<tenantId>#ATTEMPT#<attemptId>, SK = LOOKUP
src/modules\notification\domain\notification-attempt.ts:91:  entityType: "NotificationAttemptLookup";
src/modules\notification\domain\notification-attempt.ts:92:  tenantId: string;
src/modules\notification\domain\notification-attempt.ts:99:export function notificationAttemptLookupKey(tenantId: string, attemptId: string): EntityKey {
src/modules\notification\domain\notification-attempt.ts:100:  return { PK: `TENANT#${tenantId}#ATTEMPT#${attemptId}`, SK: "LOOKUP" };
src/modules\notification\domain\notification-attempt.ts:104:  attempt: Pick<NotificationAttempt, "tenantId" | "intentId" | "attemptId" | "attemptNumber" | "provider" | "providerAccountId">,
src/modules\notification\domain\notification-attempt.ts:107:    ...notificationAttemptLookupKey(attempt.tenantId, attempt.attemptId),
src/modules\notification\domain\notification-attempt.ts:108:    entityType: "NotificationAttemptLookup",
src/modules\notification\domain\notification-attempt.ts:109:    tenantId: attempt.tenantId,
src/modules\notification\domain\notification-attempt.ts:111:    attemptSk: notificationAttemptKey(attempt.tenantId, attempt.intentId, attempt.attemptNumber, attempt.attemptId).SK,
src/modules\notification\application\quiet-hours.ts:14:export interface QuietHoursConfig {
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:8:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:35:  async updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean> {
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:36:    const command = buildVersionedUpdate({
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:39:      tenantId,
src/modules\reminder\domain\recurrence.ts:19:export type ZonedResolutionKind = "NORMAL" | "AMBIGUOUS" | "NONEXISTENT";
src/modules\reminder\domain\recurrence.ts:21:export interface ZonedResolution {
src/modules\reminder\domain\recurrence.ts:26:export interface LocalDateTimeParts {
src/modules\bff\application\proxy-service.ts:17:export interface ProxyRequest {
src/modules\bff\application\proxy-service.ts:25:export interface ProxyResponse {
src/modules\bff\application\proxy-service.ts:31:export interface BackendFetcher {
src/modules\extraction\ports\extracted-field-store.ts:9: * terminal value must happen atomically, in the SAME `TransactWriteItems` that re-asserts the
src/modules\extraction\ports\extracted-field-store.ts:18:export type CommitRunOutcomeResult = "COMMITTED" | "DOCUMENT_DISCARDED";
src/modules\extraction\ports\extracted-field-store.ts:20:export interface CommitRunOutcomeInput {
src/modules\extraction\ports\extracted-field-store.ts:33:   * item is updated with OCC in the SAME `TransactWriteItems` as the field rows and the run
src/modules\extraction\ports\extracted-field-store.ts:42:export interface CommitItemUpdate {
src/modules\extraction\ports\extracted-field-store.ts:44:  tenantId: string;
src/modules\extraction\ports\extracted-field-store.ts:49:export interface ExtractedFieldStore {
src/modules\extraction\ports\extracted-field-store.ts:52:   * `TransactWriteItems` is all-or-nothing), transitions the run to a terminal status, and
src/modules\extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (Â§1.7): one `TransactWriteItems` â€”
src/modules\extraction\ports\extracted-field-store.ts:87:export type FieldTransitionResult = "COMMITTED" | "VERSION_CONFLICT";
src/modules\extraction\ports\extracted-field-store.ts:89:export interface ConfirmFieldInput {
src/modules\extraction\ports\extracted-field-store.ts:107:export interface RejectFieldInput {
src/modules\notification\application\email-delivery.ts:12:export type SendAction =
src/modules\notification\application\email-delivery.ts:38:export type SendOutcome =
src/modules\notification\application\email-delivery-workflow.ts:21:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\notification\application\email-delivery-workflow.ts:27:export interface EmailDeliverCommandData {
src/modules\notification\application\email-delivery-workflow.ts:28:  tenantId: string;
src/modules\notification\application\email-delivery-workflow.ts:40:export interface EmailDeliveryWorkflowDeps {
src/modules\notification\application\email-delivery-workflow.ts:44:  resolveRecipientEmail: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src/modules\notification\application\email-delivery-workflow.ts:51:export type EmailDeliveryOutcome =
src/modules\notification\application\email-delivery-workflow.ts:68:  const lookup = await deps.store.get<NotificationAttemptLookup>(notificationAttemptLookupKey(command.tenantId, command.attemptId), true);
src/modules\notification\application\email-delivery-workflow.ts:71:  const intentPk = `TENANT#${lookup.tenantId}#INTENT#${lookup.intentId}`;
src/modules\notification\application\email-delivery-workflow.ts:96:  const item = await deps.store.get<ExpirationItem>(itemKey(command.tenantId, command.itemId), true);
src/modules\notification\application\email-delivery-workflow.ts:117:  const to = recipientUserId ? await deps.resolveRecipientEmail({ tenantId: command.tenantId, userId: recipientUserId }) : undefined;
src/modules\notification\application\email-delivery-workflow.ts:134:      tags: { attemptId: attempt.attemptId, intentId: command.intentId, tenantId: command.tenantId, correlationId: command.correlationId },
src/modules\notification\application\email-delivery-workflow.ts:160:        Update: buildVersionedUpdate({
src/modules\notification\application\email-delivery-workflow.ts:163:          tenantId: attempt.tenantId,
src/modules\notification\application\email-delivery-workflow.ts:192:        Update: buildVersionedUpdate({
src/modules\notification\application\email-delivery-workflow.ts:195:          tenantId: submittingAttempt.tenantId,
src/modules\subject\domain\document-submission.ts:17:export type { DocumentStatus, UploadEvidence, MalwareEvidence, DocumentObjectReference };
src/modules\subject\domain\document-submission.ts:19:export interface DocumentSubmission extends EntityKey {
src/modules\subject\domain\document-submission.ts:20:  entityType: "DocumentSubmission";
src/modules\subject\domain\document-submission.ts:22:  tenantId: string;
src/modules\subject\domain\document-submission.ts:41:export function documentSubmissionKey(tenantId: string, subjectId: string, assignmentId: string, submissionId: string): EntityKey {
src/modules\subject\domain\document-submission.ts:42:  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: `REQASSIGN#${assignmentId}#SUBMISSION#${submissionId}` };
src/modules\document\application\document-service.ts:17:import { GSI6PK_RECON_UPLOAD_PENDING, buildUploadSlotGsi6Sk, type DocumentStore, type TransactWriteEntry } from "../ports/document-store.js";
src/modules\document\application\document-service.ts:26:export interface ReserveUploadInput {
src/modules\document\application\document-service.ts:33:export interface ReserveUploadResult {
src/modules\document\application\document-service.ts:41:export interface DocumentServiceDeps {
src/modules\document\application\document-service.ts:76:    authorize({ context: ctx, action: "document:reserve-upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document\application\document-service.ts:93:      tenantId: ctx.tenant.tenantId,
src/modules\document\application\document-service.ts:110:        PK: `TENANT#${ctx.tenant.tenantId}#IDEMPOTENCY#${OPERATION}`,
src/modules\document\application\document-service.ts:117:      const existingDocument = await this.store.get<Document>(documentKey(ctx.tenant.tenantId, itemId, existingDocumentId));
src/modules\document\application\document-service.ts:127:      // Key intentionally encodes tenantId/itemId/documentId/uploadSlotId (M6 design Â§2 "fluxo
src/modules\document\application\document-service.ts:132:      // tenantId/itemId/documentId are internal identifiers, not personal data.
src/modules\document\application\document-service.ts:133:      quarantineKey = `tenant/${ctx.tenant.tenantId}/item/${itemId}/document/${documentId}/slot/${uploadSlotId}/${randomUUID()}`;
src/modules\document\application\document-service.ts:138:        ...documentKey(ctx.tenant.tenantId, itemId, documentId),
src/modules\document\application\document-service.ts:139:        entityType: "Document",
src/modules\document\application\document-service.ts:140:        tenantId: ctx.tenant.tenantId,
src/modules\document\application\document-service.ts:156:        ...uploadSlotKey(ctx.tenant.tenantId, uploadSlotId),
src/modules\document\application\document-service.ts:157:        entityType: "UploadSlot",
src/modules\document\application\document-service.ts:158:        tenantId: ctx.tenant.tenantId,
src/modules\document\application\document-service.ts:176:        GSI6PK: GSI6PK_RECON_UPLOAD_PENDING,
src/modules\document\application\document-service.ts:177:        GSI6SK: buildUploadSlotGsi6Sk(expiresAt, ctx.tenant.tenantId, uploadSlotId),
src/modules\document\application\document-service.ts:180:      const entries: TransactWriteEntry[] = [
src/modules\document\application\document-service.ts:197:      metadata: { documentId, uploadSlotId, tenantId: ctx.tenant.tenantId },
src/modules\document\application\document-service.ts:202:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: documentId });
src/modules\document\application\document-service.ts:218:    authorize({ context: ctx, action: "document:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document\application\document-service.ts:219:    const document = await this.store.get<Document>(documentKey(ctx.tenant.tenantId, itemId, documentId));
src/modules\document\application\document-service.ts:230:    authorize({ context: ctx, action: "document:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document\application\document-service.ts:231:    const documents = await this.store.queryByPk<Document>(`TENANT#${ctx.tenant.tenantId}#ITEM#${itemId}`, "DOC#");
src/modules\notification\application\corrective-intent-service.ts:15:export interface CorrectiveIntentDecision {
src/modules\notification\application\corrective-intent-service.ts:35:  tenantId: string;
src/modules\notification\application\corrective-intent-service.ts:40:  return `${input.tenantId}|${input.supersededIntentId}|${input.currentItemVersion}|${input.kind}`;
src/modules\reminder\application\reminder-policy-service.ts:9: * the SAME TransactWriteItems as the policy row itself:
src/modules\reminder\application\reminder-policy-service.ts:24:import { buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\application\reminder-policy-service.ts:29:import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../ports/reminder-store.js";
src/modules\reminder\application\reminder-policy-service.ts:34:export interface ReminderPolicyServiceDeps {
src/modules\reminder\application\reminder-policy-service.ts:55:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:58:    const tenantId = ctx.tenant.tenantId;
src/modules\reminder\application\reminder-policy-service.ts:62:      ...policyKey(tenantId, policyId),
src/modules\reminder\application\reminder-policy-service.ts:63:      entityType: "ReminderPolicy",
src/modules\reminder\application\reminder-policy-service.ts:65:      tenantId,
src/modules\reminder\application\reminder-policy-service.ts:80:    const entries: TransactWriteEntry[] = [{ Put: buildVersionedCreate(this.tableName, policy as unknown as Record<string, unknown> & { PK: string; SK: string }) }];
src/modules\reminder\application\reminder-policy-service.ts:81:    this.appendItemLinkage(entries, { tenantId, itemId: input.itemId, scope: input.scope, policyId });
src/modules\reminder\application\reminder-policy-service.ts:96:    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
src/modules\reminder\application\reminder-policy-service.ts:97:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:107:    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
src/modules\reminder\application\reminder-policy-service.ts:108:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:111:    const tenantId = policy.tenantId;
src/modules\reminder\application\reminder-policy-service.ts:124:    const entries: TransactWriteEntry[] = [
src/modules\reminder\application\reminder-policy-service.ts:126:        Update: buildVersionedUpdate({
src/modules\reminder\application\reminder-policy-service.ts:128:          key: policyKey(tenantId, policyId),
src/modules\reminder\application\reminder-policy-service.ts:129:          tenantId,
src/modules\reminder\application\reminder-policy-service.ts:141:      entries.push({ Delete: { TableName: this.tableName, Key: policyRefKey(tenantId, policy.itemId!, policyId) } });
src/modules\reminder\application\reminder-policy-service.ts:155:    this.appendItemLinkage(entries, { tenantId, itemId: input.scope === "ITEM" ? input.itemId : undefined, scope: input.scope, policyId, skipPointerWrite: !needsNewPointer });
src/modules\reminder\application\reminder-policy-service.ts:181:    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
src/modules\reminder\application\reminder-policy-service.ts:182:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:184:    const entries: TransactWriteEntry[] = [
src/modules\reminder\application\reminder-policy-service.ts:186:        Update: buildVersionedUpdate({
src/modules\reminder\application\reminder-policy-service.ts:188:          key: policyKey(policy.tenantId, policyId),
src/modules\reminder\application\reminder-policy-service.ts:189:          tenantId: policy.tenantId,
src/modules\reminder\application\reminder-policy-service.ts:214:  private async readActivePolicy(tenantId: string, policyId: string): Promise<ReminderPolicy> {
src/modules\reminder\application\reminder-policy-service.ts:215:    const policy = await this.store.get<ReminderPolicy>(policyKey(tenantId, policyId));
src/modules\reminder\application\reminder-policy-service.ts:224:    entries: TransactWriteEntry[],
src/modules\reminder\application\reminder-policy-service.ts:225:    input: { tenantId: string; itemId: string | undefined; scope: PutPolicyInput["scope"]; policyId: string; skipPointerWrite?: boolean },
src/modules\reminder\application\reminder-policy-service.ts:231:        key: itemKey(input.tenantId, input.itemId),
src/modules\reminder\application\reminder-policy-service.ts:232:        extra: { tenantId: input.tenantId, status: "ACTIVE" },
src/modules\reminder\application\reminder-policy-service.ts:239:          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
src/modules\reminder\application\reminder-policy-service.ts:247:    entries: TransactWriteEntry[],
src/modules\reminder\application\reminder-policy-service.ts:259:      tenantId: ctx.tenant.tenantId,
src/modules\reminder\domain\notification-intent.ts:8:export type NotificationIntentStatus = "PENDING" | "CANCELLED" | "DISPATCHED" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:16:export type NotificationIntentKind = "EXPIRATION_REMINDER" | "REPLACEMENT" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:18:export type NotificationChannel = "EMAIL" | "WHATSAPP";
src/modules\reminder\domain\notification-intent.ts:21:export type NotificationChannelCancellationReason =
src/modules\reminder\domain\notification-intent.ts:32:export interface NotificationIntent extends EntityKey {
src/modules\reminder\domain\notification-intent.ts:34:  entityType: "NotificationIntent";
src/modules\reminder\domain\notification-intent.ts:36:  tenantId: string;
src/modules\reminder\domain\notification-intent.ts:60:export function intentKey(tenantId: string, intentId: string): { PK: string; SK: "META" } {
src/modules\reminder\domain\notification-intent.ts:61:  return { PK: `TENANT#${tenantId}#INTENT#${intentId}`, SK: "META" };
src/modules\reminder\application\id-generator.ts:2:export interface ReminderIdGenerator {
src/modules\bff\application\bff-auth-service.ts:8: * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src/modules\bff\application\bff-auth-service.ts:28:export interface BffAuthServiceDeps {
src/modules\bff\application\bff-auth-service.ts:44:export interface StartedLogin {
src/modules\bff\application\bff-auth-service.ts:73:      entityType: "LoginAttempt",
src/modules\bff\application\bff-auth-service.ts:115:    if (!attempt || attempt.entityType !== "LoginAttempt" || attempt.consumedAt) {
src/modules\bff\application\bff-auth-service.ts:158:    // MVP: tenantId=userId (data-model.md Â§7.3, same rule RequestContextResolver.resolve()
src/modules\bff\application\bff-auth-service.ts:162:    let profile = await this.deps.users.getProfile(mapping.tenantId, mapping.userId);
src/modules\bff\application\bff-auth-service.ts:166:        tenantId: mapping.tenantId,
src/modules\bff\application\bff-auth-service.ts:179:      PK: `TENANT#${mapping.tenantId}#USER#${mapping.userId}`,
src/modules\bff\application\bff-auth-service.ts:181:      entityType: "DeviceSession",
src/modules\bff\application\bff-auth-service.ts:182:      tenantId: mapping.tenantId,
src/modules\bff\application\bff-auth-service.ts:200:      entityType: "Session",
src/modules\bff\application\bff-auth-service.ts:203:      tenantId: mapping.tenantId,
src/modules\bff\application\bff-auth-service.ts:236:    if (!session || session.entityType !== "Session" || session.revokedAt) {

 exited 1 in 1032ms:
docs/architecture/data-model.md:17:`tenantId` obrigatÃ³rio em toda chave DynamoDB, idempotency key, objeto S3, evento e mensagem (SCALE-004). MVP: `tenantId=userId`; futuro: `tenantId=organizationId`. Identidade autenticÃ¡vel global no IdP (Cognito); `User` representa o perfil dentro do tenant, com `identitySubject` desacoplado do `sub` do Cognito (decisÃ£o Â§4 da arquitetura).
docs/architecture/data-model.md:21:ConvenÃ§Ã£o fÃ­sica: `PK = TENANT#<tenantId>#<aggregate>`, `SK = <entityType>#<id>[#...]`. IDs UUIDv7/ULID (ordenÃ¡veis por tempo, evitam hot-key sequencial de auto-incremento). Datas ISO-8601 UTC; timezones IANA. Atributos comuns a toda entidade (design-target, ver nota de status abaixo): `entityType`, `schemaVersion`, `createdAt`, `updatedAt`, `version`, `deletedAt?`, `retentionClass`, `purgeAfter?`.
docs/architecture/data-model.md:43:| IdentityMapping | cognitoSub, userId, tenantId â€” criado atomicamente (`ConditionExpression attribute_not_exists`) no primeiro login | `IDENTITY#cognitoSub#<sub>` / `MAP` |
docs/architecture/data-model.md:50:| GuestTokenPointer *(M10, D-037, D-045 â€” terceira exceÃ§Ã£o tenantless)* | selectorHash (na PK), secretHash (HMAC-SHA256+pepper â€” nunca o secret em texto puro), tenantId/subjectId/assignmentId/documentRequestId (para reconstruÃ§Ã£o de contexto pÃ³s-lookup), tokenExpiresAt (`min(now+14d, deadline)`) | `GUESTTOKEN#<selectorHash>` / `POINTER` |
docs/architecture/data-model.md:59:**IdentityMapping â€” segunda de trÃªs exceÃ§Ãµes de particionamento (`implementation-blueprint.md` Â§23.1)**: fora do agregado `TENANT#...` porque a busca `cognitoSub â†’ userId/tenantId` ocorre *antes* de `tenantId` ser conhecido â€” mesmo motivo estrutural do GSI3 (a chave nÃ£o pode depender de um dado ainda nÃ£o resolvido no momento da consulta). **GuestTokenPointer/GuestTokenRateLimit (M10) sÃ£o a terceira**, pelo mesmo motivo: o convidado apresenta sÃ³ o token opaco, `tenantId` sÃ³ Ã© conhecido depois do lookup pelo `selectorHash`.
docs/architecture/data-model.md:61:## 3. GSIs (todos com chave iniciando por `TENANT#tenantId`, exceto GSI3 â€” ver exceÃ§Ã£o documentada abaixo)
docs/architecture/data-model.md:64:- **GSI3 â€” scheduler global de ocorrÃªncias agendadas** *(entendimento atualizado em M10/D-039/D-046, `roadmap-evolution/12-automated-chasing-capacity-review.md` â€” mudanÃ§a de documentaÃ§Ã£o, nÃ£o de mecanismo)*: `PK=GSI3PK=DUE#yyyyMMddHHmm#NN`, `SK=GSI3SK` discriminado por `entityType` na prÃ³pria forma da chave â€” `TENANT#t#OCCURRENCE#<occurrenceId>` para `ReminderOccurrence` (formato original, zero migraÃ§Ã£o das linhas jÃ¡ em produÃ§Ã£o) e `TENANT#t#CHASING#<occurrenceId>` para `DocumentChasingOccurrence` (M10 cluster 4, agregado-irmÃ£o â€” nunca generaliza `ReminderOccurrence`). 4 shards/minuto inicial (decisÃ£o Fase 3 Rodada 4); produtor condiciona por status/versÃ£o, branca por `entityType` ao processar cada linha lida, e falha-fechado com alarme se a forma da SK nÃ£o corresponder a nenhum discriminador conhecido. **CorreÃ§Ã£o Type 1 (ratificada em `implementation-blueprint.md` Â§9.2/Â§9.3, decisions-log.md D-028)**: a chave original (`PK=TENANT#t#DUE#...`) nÃ£o Ã© consultÃ¡vel pelo `ReminderProducer` â€” `Query` do DynamoDB exige PK exata, e o producer do minuto `M` nÃ£o sabe a priori quais tenants tÃªm ocorrÃªncia em `M`. GSI3 Ã© portanto a **Ãºnica exceÃ§Ã£o** Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`": a PK Ã© global (sem `tenantId`), com `tenantId` preservado na SK e no item base para reconstruÃ§Ã£o segura do contexto. Salvaguardas obrigatÃ³rias (detalhadas em `implementation-blueprint.md` Â§9.2, mantidas para os dois `entityType`): IAM do Ã­ndice restrito exclusivamente ao producer (nenhuma rota tenant-facing tem `Query` neste GSI); projeÃ§Ã£o mÃ­nima, sem conteÃºdo de negÃ³cio sensÃ­vel; teste de isolamento automatizado provando que nenhuma API tenant-facing o alcanÃ§a, direta ou indiretamente; dimensionamento de shards por pico global, nÃ£o por tenant; alarmes segmentados por `entityType` (D-046) para que um pico anÃ´malo de um tipo nÃ£o se esconda no agregado do outro.
docs/architecture/data-model.md:74:| MaterializaÃ§Ã£o de ReminderOccurrence | `tenantId\|itemId\|itemVersion\|policyId\|policyVersion\|triggerId\|scheduledAtUtc` |
docs/architecture/data-model.md:75:| NotificationIntent | `tenantId\|occurrenceId\|channel\|recipient\|intentKind` |
docs/architecture/data-model.md:76:| NotificationAttempt | `tenantId\|intentId\|provider\|attemptNumber\|redriveGeneration` |
docs/architecture/data-model.md:77:| ExtraÃ§Ã£o (ExtractedField) | `tenantId\|documentId\|documentVersion\|pipelineVersion` |
docs/architecture/data-model.md:78:| RenovaÃ§Ã£o de item | `tenantId\|sourceItemId\|sourceVersion\|cycle` |
docs/architecture/data-model.md:80:| Upload | `UploadSlot` reservado atomicamente via `TransactWriteItems` no momento da geraÃ§Ã£o da URL presigned (decisÃ£o Fase 3 Rodada 6) |
docs/architecture/data-model.md:83:Toda entidade mutÃ¡vel usa `version`; updates exigem `ConditionExpression: version=:expected` e incrementam atomicamente (optimistic concurrency control, decisÃ£o Fase 3 Rodada 6). Alterar vencimento grava o item, cancela ocorrÃªncias antigas e cria o evento de outbox crÃ­tico (`ItemDueDateChanged`) numa Ãºnica transaÃ§Ã£o (`TransactWriteItems`) â€” fecha a corrida identificada no Red Team cenÃ¡rio 13. AplicaÃ§Ã£o de resultado de extraÃ§Ã£o verifica simultaneamente as versÃµes de `Document` e `ExpirationItem`; divergÃªncia ou tombstone descarta o resultado (Red Team cenÃ¡rio 14).
docs/architecture/data-model.md:101:**ExtractedField â€” modelo hÃ­brido com ExtractionRun**: adicionada entidade `ExtractionRun` como item-pai da execuÃ§Ã£o (estado agregado, `pipelineVersion`, `documentVersion`, timestamps de inÃ­cio/fim, resumo), com `ExtractedField` individuais referenciando `runId`. Corrige tambÃ©m a chave de idempotÃªncia, que antes correspondia Ã  execuÃ§Ã£o como um todo mas era declarada ambiguamente sobre mÃºltiplos itens de campo: agora a idempotÃªncia da **execuÃ§Ã£o** Ã© `tenantId|documentId|documentVersion|pipelineVersion` (chave do `ExtractionRun`), e cada `ExtractedField` Ã© unicamente identificado por `runId + fieldName` (nÃ£o precisa de chave de idempotÃªncia prÃ³pria, pois Ã© sempre escrito uma vez por `ExtractionRun` jÃ¡ idempotente).
docs/architecture/data-model.md:125:| TextractJob | jobId, tenantId, itemId, documentId, documentVersion, runId, clientRequestToken, status (STARTED/COMPLETED/FAILED), `taskTokenCiphertext?` (cifrado, presente sÃ³ enquanto o callback pode ainda resolver a task â€” removido em qualquer desfecho terminal, design Â§3), ttl (curto, diagnÃ³stico/reconciliaÃ§Ã£o) | `TEXTRACTJOB#<jobId>` / `TEXTRACTJOB#<jobId>` |
docs/architecture/data-model.md:127:**Chave deliberadamente nÃ£o tenant-scoped** (Ãºnica exceÃ§Ã£o nesta tabela): `COMPLETE_OCR` sÃ³ recebe `JobId`/`JobTag` da notificaÃ§Ã£o SNS/SQS, sem contexto de tenant disponÃ­vel antes de ler este registro â€” uma chave `TENANT#t#...` exigiria um GSI sÃ³ para permitir "encontre o job com este jobId", para uma entidade cujo propÃ³sito inteiro Ã‰ essa busca. Um item self-keyed evita o GSI. `ClientRequestToken` (determinÃ­stico, `sha256(tenantId|documentId|documentVersion|pipelineVersion|runId)`) Ã© o mecanismo de reconciliaÃ§Ã£o para o caso de job Ã³rfÃ£o (notificaÃ§Ã£o sem `TextractJob` correspondente â€” design Â§2): confirma-se via uma chamada a `GetDocumentTextDetection`, descarta-se o resultado, nunca Ã© tratado como erro.
src/shared/dynamodb/occ.ts:10:export interface EntityKey {
src/shared/dynamodb/occ.ts:15:export interface VersionedUpdateInput {
src/shared/dynamodb/occ.ts:18:  tenantId: string;
src/shared/dynamodb/occ.ts:23:   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
src/shared/dynamodb/occ.ts:31:   * `legalHold`/`GSI6PK`/`purgeAfter` atomically alongside the version check). Each entry's
src/shared/dynamodb/occ.ts:34:   * with the base condition's (#version/#tenantId/#updatedAt/:expectedVersion/:tenantId/:one/:now)
src/shared/dynamodb/occ.ts:35:   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
src/shared/dynamodb/occ.ts:41:export interface DynamoUpdateCommandInput {
src/shared/dynamodb/occ.ts:53: * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
src/shared/dynamodb/occ.ts:55:export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
src/shared/dynamodb/occ.ts:60:    "#tenantId": "tenantId",
src/shared/dynamodb/occ.ts:65:    ":tenantId": input.tenantId,
src/shared/dynamodb/occ.ts:95:  const baseConditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
src/shared/dynamodb/occ.ts:118:export interface DynamoPutCommandInput {
src/shared/dynamodb/occ.ts:127: * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
src/shared/dynamodb/occ.ts:138: * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
src/shared/dynamodb/occ.ts:147:export interface TransactPutEntry {
src/shared/dynamodb/occ.ts:151:export interface TransactUpdateEntry {
src/shared/dynamodb/occ.ts:155:export interface DynamoConditionCheckCommandInput {
src/shared/dynamodb/occ.ts:163:export interface TransactConditionCheckEntry {
src/shared/dynamodb/occ.ts:167:export interface DynamoDeleteCommandInput {
src/shared/dynamodb/occ.ts:175:export interface TransactDeleteEntry {
src/shared/dynamodb/occ.ts:182: * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
src/shared/dynamodb/occ.ts:184: * `buildVersionedUpdate` - see that function's docs.
src/shared/dynamodb/occ.ts:186:export function buildVersionedDelete(input: {
src/shared/dynamodb/occ.ts:189:  tenantId: string;
src/shared/dynamodb/occ.ts:193:  const names: Record<string, string> = { "#version": "version", "#tenantId": "tenantId" };
src/shared/dynamodb/occ.ts:194:  const values: Record<string, unknown> = { ":expectedVersion": input.expectedVersion, ":tenantId": input.tenantId };
src/shared/dynamodb/occ.ts:195:  const conditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
src/shared/dynamodb/occ.ts:218:export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;
src/shared/dynamodb/occ.ts:221: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src/shared/dynamodb/occ.ts:227: * re-asserted atomically inside the same TransactWriteItems as the write it gates).
src/shared/dynamodb/occ.ts:299:/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
src/workers/document-purge\purge.ts:23: *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
src/workers/document-purge\purge.ts:24: *    the claimed version + `GSI6PK = WORKSTATE#PURGE_CLAIMED`) + `Put` a `DocumentPurgeReceipt`
src/workers/document-purge\purge.ts:34: *    `attribute_not_exists(GSI6PK) OR GSI6PK <> "WORKSTATE#PURGE_CLAIMED"` in its own
src/workers/document-purge\purge.ts:40:  buildVersionedDelete,
src/workers/document-purge\purge.ts:41:  buildVersionedUpdate,
src/workers/document-purge\purge.ts:45:  GSI6PK_PURGE_CLAIMED,
src/workers/document-purge\purge.ts:46:  GSI6PK_PURGE_PENDING,
src/workers/document-purge\purge.ts:60:export interface DocumentPurgeCandidate {
src/workers/document-purge\purge.ts:61:  entityType: "Document";
src/workers/document-purge\purge.ts:64:  tenantId: string;
src/workers/document-purge\purge.ts:69:  GSI6PK: string;
src/workers/document-purge\purge.ts:80:export interface ReceiptPurgeCandidate {
src/workers/document-purge\purge.ts:81:  entityType: "DocumentPurgeReceipt";
src/workers/document-purge\purge.ts:84:  tenantId: string;
src/workers/document-purge\purge.ts:87:  GSI6PK: string;
src/workers/document-purge\purge.ts:92:export type PurgeCandidate = DocumentPurgeCandidate | ReceiptPurgeCandidate;
src/workers/document-purge\purge.ts:94:export interface DocumentPurgeWorkerDeps {
src/workers/document-purge\purge.ts:102:export interface PurgeRunResult {
src/workers/document-purge\purge.ts:124:        Update: buildVersionedUpdate({
src/workers/document-purge\purge.ts:127:          tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:130:            GSI6PK: GSI6PK_PURGE_CLAIMED,
src/workers/document-purge\purge.ts:131:            GSI6SK: buildDocumentPurgeClaimGsi6Sk(claimExpiresAt, candidate.tenantId, candidate.documentId),
src/workers/document-purge\purge.ts:137:            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
src/workers/document-purge\purge.ts:155:    ...documentPurgeReceiptKey(candidate.tenantId, candidate.documentId),
src/workers/document-purge\purge.ts:156:    entityType: "DocumentPurgeReceipt",
src/workers/document-purge\purge.ts:157:    tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:166:    GSI6PK: GSI6PK_PURGE_PENDING,
src/workers/document-purge\purge.ts:167:    GSI6SK: buildPurgeReceiptGsi6Sk(receiptPurgeAfter, candidate.tenantId, candidate.documentId),
src/workers/document-purge\purge.ts:175:      Delete: buildVersionedDelete({
src/workers/document-purge\purge.ts:178:        tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:180:        extraConditions: [{ expression: "GSI6PK = :expectedClaimed", values: { ":expectedClaimed": GSI6PK_PURGE_CLAIMED } }],
src/workers/document-purge\purge.ts:194:        Delete: buildVersionedDelete({
src/workers/document-purge\purge.ts:197:          tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:200:            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
src/workers/document-purge\purge.ts:220:          Update: buildVersionedUpdate({
src/workers/document-purge\purge.ts:223:            tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:226:            remove: ["GSI6PK", "GSI6SK"],
src/workers/document-purge\purge.ts:227:            extraConditions: [{ expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_CLAIMED, ":expectedSk": candidate.GSI6SK } }],
src/workers/document-purge\purge.ts:235:        Update: buildVersionedUpdate({
src/workers/document-purge\purge.ts:238:          tenantId: candidate.tenantId,
src/workers/document-purge\purge.ts:240:          set: { GSI6PK: GSI6PK_PURGE_PENDING, GSI6SK: buildDocumentPurgeGsi6Sk(candidate.purgeAfter, candidate.tenantId, candidate.documentId) },
src/workers/document-purge\purge.ts:241:          extraConditions: [{ expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_CLAIMED, ":expectedSk": candidate.GSI6SK } }],
src/workers/document-purge\purge.ts:259:    if (candidate.entityType === "Document") {
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:11:import { GSI6PK_PURGE_CLAIMED, GSI6PK_PURGE_PENDING } from "../../modules/document/ports/document-store.js";
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:31:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:32:          ExpressionAttributeValues: { ":pk": GSI6PK_PURGE_PENDING, ":before": input.before },
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:53:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
src/workers/document-purge\dynamodb-document-purge-candidate-source.ts:54:          ExpressionAttributeValues: { ":pk": GSI6PK_PURGE_CLAIMED, ":before": input.before },
src/workers/document-purge\document-purge-candidate-source.ts:9:export interface Page<T> {
src/workers/document-purge\document-purge-candidate-source.ts:14:export interface DocumentPurgeCandidateSource {
src/workers/document-purge\document-purge-candidate-source.ts:15:  /** GSI6PK="WORKSTATE#PURGE_PENDING" (global), GSI6SK < now. Mixed `Document`/
src/workers/document-purge\document-purge-candidate-source.ts:16:   * `DocumentPurgeReceipt` rows, discriminated by `entityType`. */
src/workers/document-purge\document-purge-candidate-source.ts:18:  /** GSI6PK="WORKSTATE#PURGE_CLAIMED" (global), GSI6SK < now (lease already expired). Only
src/modules\identity\ports\identity-store.ts:8:export interface EntityKey {
src/modules\identity\ports\identity-store.ts:13:export interface IdentityStore {
src/modules\notification\providers\ses-email-adapter.ts:60:            { Name: "et_tenant_id", Value: input.tags.tenantId },
src/modules\expiration\ports\expiration-store.ts:7: * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
src/modules\expiration\ports\expiration-store.ts:11:import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\expiration\ports\expiration-store.ts:15:export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
src/modules\expiration\ports\expiration-store.ts:18:export interface Gsi1QueryInput {
src/modules\expiration\ports\expiration-store.ts:24:export interface ExpirationStore {
src/modules\expiration\ports\expiration-store.ts:36:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\subject\ports\subject-store.ts:5: * agregado + contador + audit numa Ãºnica TransactWriteItems (nunca separado, ver
src/modules\subject\ports\subject-store.ts:8:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\subject\ports\subject-store.ts:10:export type { EntityKey, TransactWriteEntry };
src/modules\subject\ports\subject-store.ts:13:export interface Gsi7QueryInput {
src/modules\subject\ports\subject-store.ts:19:export interface SubjectStore {
src/modules\subject\ports\subject-store.ts:30:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\ports\import-store.ts:7:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\import\ports\import-store.ts:9:export type { EntityKey, TransactWriteEntry };
src/modules\import\ports\import-store.ts:12:export interface ImportStore {
src/modules\import\ports\import-store.ts:16:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\ports\import-object-store.ts:10:export interface ImportObjectStore {
src/modules\subject\ports\expiration-item-lookup.ts:7:export interface ExpirationItemLookup {
src/modules\subject\ports\expiration-item-lookup.ts:10:  itemExists(tenantId: string, itemId: string): Promise<boolean>;
src/modules\document\ports\upload-url-signer.ts:4:export interface PresignUploadInput {
src/modules\document\ports\upload-url-signer.ts:14:export interface PresignUploadResult {
src/modules\document\ports\upload-url-signer.ts:19:export interface UploadUrlSigner {
src/modules\expiration\domain\item-watch.ts:10:export type ItemWatchStatus = "ACTIVE" | "REMOVED";
src/modules\expiration\domain\item-watch.ts:12:export interface ItemWatch extends EntityKey {
src/modules\expiration\domain\item-watch.ts:13:  entityType: "ItemWatch";
src/modules\expiration\domain\item-watch.ts:15:  tenantId: string;
src/modules\expiration\domain\item-watch.ts:23:export function itemWatchKey(tenantId: string, itemId: string, userId: string): EntityKey {
src/modules\expiration\domain\item-watch.ts:24:  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `WATCH#USER#${userId}` };
src/modules\expiration\persistence\dynamodb-expiration-store.ts:3: * TransactWriteItems parameter shapes already produced by shared/dynamodb/occ.ts's
src/modules\expiration\persistence\dynamodb-expiration-store.ts:4: * builders (buildVersionedCreate/buildVersionedUpdate) and shared/outbox/outbox.ts's
src/modules\expiration\persistence\dynamodb-expiration-store.ts:8:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\expiration\persistence\dynamodb-expiration-store.ts:10:import type { EntityKey, ExpirationStore, Gsi1QueryInput, TransactWriteEntry } from "../ports/expiration-store.js";
src/modules\expiration\persistence\dynamodb-expiration-store.ts:57:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src/modules\expiration\persistence\dynamodb-expiration-store.ts:60:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\notification\providers\email-templates.ts:12:export interface RenderedEmailContent {
src/modules\document\ports\pdf-parser.ts:6:export type PdfParseOutcome = "VALID" | "INVALID_STRUCTURE" | "LIMIT_EXCEEDED" | "UNSUPPORTED_FORMAT";
src/modules\document\ports\pdf-parser.ts:8:export interface PdfParseResult {
src/modules\document\ports\pdf-parser.ts:13:export interface PdfParser {
src/modules\bff\ports\token-encryptor.ts:6:export interface TokenEncryptor {
src/modules\expiration\domain\expiration-item.ts:11:export type ExpirationItemStatus = "ACTIVE" | "ARCHIVED" | "RENEWED" | "DELETED";
src/modules\expiration\domain\expiration-item.ts:13:export interface ExpirationItem extends EntityKey {
src/modules\expiration\domain\expiration-item.ts:15:  entityType: "ExpirationItem";
src/modules\expiration\domain\expiration-item.ts:17:  tenantId: string;
src/modules\expiration\domain\expiration-item.ts:40:export function itemKey(tenantId: string, itemId: string): { PK: string; SK: "META" } {
src/modules\expiration\domain\expiration-item.ts:41:  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: "META" };
src/modules\expiration\domain\expiration-item.ts:45:export function gsi1Keys(tenantId: string, status: ExpirationItemStatus, dueDate: string, itemId: string): { GSI1PK: string; GSI1SK: string } {
src/modules\expiration\domain\expiration-item.ts:47:    GSI1PK: `TENANT#${tenantId}#ITEMSTATUS#${status}`,
src/modules\expiration\domain\expiration-item.ts:65:export interface CreateItemInput {
src/modules\expiration\domain\expiration-item.ts:80:export interface UpdateItemInput {
src/modules\expiration\domain\expiration-item.ts:94:export interface RenewItemInput {
src/modules\expiration\domain\expiration-item.ts:96:  /** Idempotency cycle discriminator (data-model.md Â§4: "tenantId|sourceItemId|sourceVersion|cycle"). Defaults to newDueDate when omitted. */
src/modules\identity\persistence\user-repository.ts:13:export interface UserProfile {
src/modules\identity\persistence\user-repository.ts:16:  entityType: "User";
src/modules\identity\persistence\user-repository.ts:18:  tenantId: string;
src/modules\identity\persistence\user-repository.ts:35:export interface DeviceSession {
src/modules\identity\persistence\user-repository.ts:38:  entityType: "DeviceSession";
src/modules\identity\persistence\user-repository.ts:39:  tenantId: string;
src/modules\identity\persistence\user-repository.ts:51:export function userProfileKey(tenantId: string, userId: string): EntityKey {
src/modules\identity\persistence\user-repository.ts:52:  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: "PROFILE" };
src/modules\identity\persistence\user-repository.ts:55:export function deviceSessionKey(tenantId: string, userId: string, deviceId: string): EntityKey {
src/modules\identity\persistence\user-repository.ts:56:  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: `SESSION#${deviceId}` };
src/modules\identity\persistence\user-repository.ts:65:  async getProfile(tenantId: string, userId: string): Promise<UserProfile | undefined> {
src/modules\identity\persistence\user-repository.ts:66:    return this.store.get<UserProfile>(userProfileKey(tenantId, userId));
src/modules\identity\persistence\user-repository.ts:69:  async createProfileIfAbsent(profile: Omit<UserProfile, "PK" | "SK" | "entityType" | "createdAt" | "updatedAt" | "version">): Promise<UserProfile> {
src/modules\identity\persistence\user-repository.ts:70:    const key = userProfileKey(profile.tenantId, profile.userId);
src/modules\identity\persistence\user-repository.ts:71:    const existing = await this.getProfile(profile.tenantId, profile.userId);
src/modules\identity\persistence\user-repository.ts:79:      entityType: "User",
src/modules\identity\persistence\user-repository.ts:89:  async getDeviceSession(tenantId: string, userId: string, deviceId: string): Promise<DeviceSession | undefined> {
src/modules\identity\persistence\user-repository.ts:90:    return this.store.get<DeviceSession>(deviceSessionKey(tenantId, userId, deviceId));
src/modules\identity\persistence\user-repository.ts:98:  async logoutAll(tenantId: string, userId: string): Promise<void> {
src/modules\identity\persistence\user-repository.ts:99:    const profile = await this.getProfile(tenantId, userId);
src/modules\identity\persistence\user-repository.ts:105:  async logoutDevice(tenantId: string, userId: string, deviceId: string): Promise<void> {
src/modules\identity\persistence\user-repository.ts:106:    const session = await this.getDeviceSession(tenantId, userId, deviceId);
src/modules\document\domain\upload-slot.ts:10:export type UploadSlotStatus = "RESERVED" | "CONSUMED" | "EXPIRED" | "RELEASED";
src/modules\document\domain\upload-slot.ts:12:export interface UploadSlot extends EntityKey {
src/modules\document\domain\upload-slot.ts:13:  entityType: "UploadSlot";
src/modules\document\domain\upload-slot.ts:14:  tenantId: string;
src/modules\document\domain\upload-slot.ts:28:   * `GSI6PK_RECON_UPLOAD_PENDING`/`buildUploadSlotGsi6Sk`) - removed the moment the slot
src/modules\document\domain\upload-slot.ts:32:  GSI6PK?: string;
src/modules\document\domain\upload-slot.ts:36:export function uploadSlotKey(tenantId: string, uploadSlotId: string): EntityKey {
src/modules\document\domain\upload-slot.ts:37:  return { PK: `TENANT#${tenantId}#UPLOAD`, SK: `SLOT#${uploadSlotId}` };
src/modules\bff\ports\session-store.ts:6:export interface EntityKey {
src/modules\bff\ports\session-store.ts:11:export interface SessionStore {
src/modules\document\ports\document-store.ts:3:export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src/modules\document\ports\document-store.ts:5:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\document\ports\document-store.ts:7:export interface DocumentStore {
src/modules\document\ports\document-store.ts:11:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\document\ports\document-store.ts:26:export const GSI6PK_RECON_UPLOAD_PENDING = "RECON#UPLOAD#PENDING";
src/modules\document\ports\document-store.ts:29: * `reserveUpload` never actually wrote `GSI6PK`/`GSI6SK` onto the `UploadSlot` it created -
src/modules\document\ports\document-store.ts:34: * `<sortKey>#TENANT#<tenantId>#<ENTITY>#<id>` uniqueness-suffix convention as
src/modules\document\ports\document-store.ts:36: * GSI6SK must be unique per GSI6PK partition, and two slots can share the same expiresAt. */
src/modules\document\ports\document-store.ts:37:export function buildUploadSlotGsi6Sk(expiresAt: string, tenantId: string, uploadSlotId: string): string {
src/modules\document\ports\document-store.ts:38:  return `${expiresAt}#TENANT#${tenantId}#SLOT#${uploadSlotId}`;
src/modules\document\ports\document-store.ts:47:export const GSI6PK_PURGE_PENDING = "WORKSTATE#PURGE_PENDING";
src/modules\document\ports\document-store.ts:48:export const GSI6PK_PURGE_CLAIMED = "WORKSTATE#PURGE_CLAIMED";
src/modules\document\ports\document-store.ts:50:export function buildDocumentPurgeGsi6Sk(purgeAfter: string, tenantId: string, documentId: string): string {
src/modules\document\ports\document-store.ts:51:  return `${purgeAfter}#TENANT#${tenantId}#DOCUMENT#${documentId}`;
src/modules\document\ports\document-store.ts:54:export function buildDocumentPurgeClaimGsi6Sk(claimExpiresAt: string, tenantId: string, documentId: string): string {
src/modules\document\ports\document-store.ts:55:  return `${claimExpiresAt}#TENANT#${tenantId}#DOCUMENT#${documentId}`;
src/modules\document\ports\document-store.ts:58:export function buildPurgeReceiptGsi6Sk(purgeAfter: string, tenantId: string, documentId: string): string {
src/modules\document\ports\document-store.ts:59:  return `${purgeAfter}#TENANT#${tenantId}#PURGERECEIPT#${documentId}`;
src/modules\document\ports\document-store.ts:62:export interface Gsi6QueryInput {
src/modules\document\ports\document-store.ts:69:export interface Page<T> {
src/modules\document\ports\document-store.ts:76:export interface UploadSlotReconciliationSource {
src/modules\reminder\ports\reminder-store.ts:23:export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\ports\reminder-store.ts:25:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\ports\reminder-store.ts:27:export interface ReminderStore {
src/modules\reminder\ports\reminder-store.ts:31:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\reminder\ports\reminder-store.ts:39:  queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string, skPrefix?: string): Promise<T[]>;
src/modules\reminder\ports\reminder-store.ts:42:export interface Gsi3QueryInput {
src/modules\reminder\ports\reminder-store.ts:47:export interface ReminderProducerStore {
src/modules\reminder\ports\reminder-store.ts:49:  /** Strongly consistent read of the base item, used by the producer to reconstruct tenant context before claiming (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
src/modules\reminder\ports\reminder-store.ts:52:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\subject\persistence\dynamodb-subject-store.ts:6:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\subject\persistence\dynamodb-subject-store.ts:8:import type { EntityKey, Gsi7QueryInput, SubjectStore, TransactWriteEntry } from "../ports/subject-store.js";
src/modules\subject\persistence\dynamodb-subject-store.ts:68:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src/modules\subject\persistence\dynamodb-subject-store.ts:71:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\document\domain\retention.ts:11:export type DocumentRetentionClass = "USER_DOCUMENT" | "TRANSIENT";
src/modules\document\ports\document-object-store.ts:5:export interface ObjectMetadata {
src/modules\document\ports\document-object-store.ts:11:export interface DocumentObjectStore {
src/modules\bff\ports\cognito-oidc-client.ts:8:export interface CognitoTokenResponse {
src/modules\bff\ports\cognito-oidc-client.ts:15:export type CognitoRefreshOutcome =
src/modules\bff\ports\cognito-oidc-client.ts:21:export interface CognitoOidcClient {
src/modules\bff\ports\cognito-oidc-client.ts:35:export interface IdTokenVerifier {
src/modules\expiration\http\item-watch-handlers.ts:10:export interface HttpRequest {
src/modules\expiration\http\item-watch-handlers.ts:19:export interface HttpResponse {
src/modules\expiration\http\item-watch-handlers.ts:24:export interface ItemWatchHttpDeps {
src/modules\expiration\http\item-watch-handlers.ts:72:  await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\import\persistence\dynamodb-import-store.ts:6:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\import\persistence\dynamodb-import-store.ts:8:import type { EntityKey, ImportStore, TransactWriteEntry } from "../ports/import-store.js";
src/modules\import\persistence\dynamodb-import-store.ts:48:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src/modules\import\persistence\dynamodb-import-store.ts:51:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\identity\persistence\identity-mapping-repository.ts:5: * of the two documented exceptions to the "every key starts with TENANT#tenantId" rule
src/modules\identity\persistence\identity-mapping-repository.ts:6: * (the other is GSI3) â€” the lookup happens *before* tenantId is known.
src/modules\identity\persistence\identity-mapping-repository.ts:11:export interface IdentityMapping {
src/modules\identity\persistence\identity-mapping-repository.ts:14:  entityType: "IdentityMapping";
src/modules\identity\persistence\identity-mapping-repository.ts:17:  tenantId: string;
src/modules\identity\persistence\identity-mapping-repository.ts:36:   * Atomically creates the mapping on first login. MVP: tenantId=userId (data-model.md
src/modules\identity\persistence\identity-mapping-repository.ts:37:   * Â§7.3 "No MVP, tenantId=userId, mas o cÃ³digo recebe ambos como conceitos separados"),
src/modules\identity\persistence\identity-mapping-repository.ts:55:      entityType: "IdentityMapping",
src/modules\identity\persistence\identity-mapping-repository.ts:58:      tenantId: newTenantId,
src/modules\document\domain\quarantine-key.ts:2: * Parses the tenantId/itemId/documentId out of a quarantine object key built by
src/modules\document\domain\quarantine-key.ts:3: * DocumentService.reserveUpload() (`tenant/<tenantId>/item/<itemId>/document/<documentId>/
src/modules\document\domain\quarantine-key.ts:8:export interface ParsedQuarantineKey {
src/modules\document\domain\quarantine-key.ts:9:  tenantId: string;
src/modules\document\domain\quarantine-key.ts:20:  const [, tenantId, itemId, documentId, uploadSlotId] = match;
src/modules\document\domain\quarantine-key.ts:21:  if (!tenantId || !itemId || !documentId || !uploadSlotId) return undefined;
src/modules\document\domain\quarantine-key.ts:22:  return { tenantId, itemId, documentId, uploadSlotId };
src/modules\expiration\http\item-handlers.ts:27:    tenantId: context.tenant.tenantId,
src/modules\expiration\http\item-handlers.ts:54:export interface HttpRequest<TBody = unknown> {
src/modules\expiration\http\item-handlers.ts:64:export interface HttpResponse {
src/modules\expiration\http\item-handlers.ts:69:export interface ExpirationHttpDeps {
src/modules\subject\application\subject-service.ts:7: * mutaÃ§Ã£o muda o nÃºmero de subjects ACTIVE) em uma Ãºnica TransactWriteItems.
src/modules\subject\application\subject-service.ts:12:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\subject-service.ts:24:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src/modules\subject\application\subject-service.ts:27:export interface SubjectServiceDeps {
src/modules\subject\application\subject-service.ts:34:export interface SubjectListQuery {
src/modules\subject\application\subject-service.ts:59:    authorize({ context: ctx, action: "subject:create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\subject\application\subject-service.ts:62:      const entitlement = await this.ensureEntitlement(ctx.tenant.tenantId);
src/modules\subject\application\subject-service.ts:65:          tenantId: ctx.tenant.tenantId,
src/modules\subject\application\subject-service.ts:75:        ...subjectKey(ctx.tenant.tenantId, subjectId),
src/modules\subject\application\subject-service.ts:76:        entityType: "TrackedSubject",
src/modules\subject\application\subject-service.ts:78:        tenantId: ctx.tenant.tenantId,
src/modules\subject\application\subject-service.ts:88:        ...gsi7Keys(ctx.tenant.tenantId, "ACTIVE", input.type, displayNameNormalized, subjectId),
src/modules\subject\application\subject-service.ts:91:      const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:93:          Update: buildVersionedUpdate({
src/modules\subject\application\subject-service.ts:95:            key: entitlementKey(ctx.tenant.tenantId),
src/modules\subject\application\subject-service.ts:96:            tenantId: ctx.tenant.tenantId,
src/modules\subject\application\subject-service.ts:122:    throw new ConflictError("Could not create subject under contention.", { tenantId: ctx.tenant.tenantId });
src/modules\subject\application\subject-service.ts:126:    const subject = await this.readActiveSubject(ctx.tenant.tenantId, subjectId);
src/modules\subject\application\subject-service.ts:127:    authorize({ context: ctx, action: "subject:read", resource: { tenantId: subject.tenantId } });
src/modules\subject\application\subject-service.ts:132:    const subject = await this.readActiveSubject(ctx.tenant.tenantId, subjectId);
src/modules\subject\application\subject-service.ts:133:    authorize({ context: ctx, action: "subject:update", resource: { tenantId: subject.tenantId } });
src/modules\subject\application\subject-service.ts:155:    const gsi7 = gsi7Keys(subject.tenantId, subject.status, subject.type, nextDisplayNameNormalized, subjectId);
src/modules\subject\application\subject-service.ts:159:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:161:        Update: buildVersionedUpdate({
src/modules\subject\application\subject-service.ts:163:          key: subjectKey(subject.tenantId, subjectId),
src/modules\subject\application\subject-service.ts:164:          tenantId: subject.tenantId,
src/modules\subject\application\subject-service.ts:185:    const subject = await this.readActiveSubject(ctx.tenant.tenantId, subjectId);
src/modules\subject\application\subject-service.ts:186:    authorize({ context: ctx, action: "subject:update", resource: { tenantId: subject.tenantId } });
src/modules\subject\application\subject-service.ts:191:    const subject = await this.readActiveSubject(ctx.tenant.tenantId, subjectId);
src/modules\subject\application\subject-service.ts:192:    authorize({ context: ctx, action: "subject:delete", resource: { tenantId: subject.tenantId } });
src/modules\subject\application\subject-service.ts:201:    authorize({ context: ctx, action: "subject:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\subject\application\subject-service.ts:203:      gsi7pk: `TENANT#${ctx.tenant.tenantId}#SUBJECTSTATUS#${query.status}`,
src/modules\subject\application\subject-service.ts:219:      ...gsi7Keys(subject.tenantId, status, subject.type, subject.displayNameNormalized, subject.subjectId),
src/modules\subject\application\subject-service.ts:222:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:224:        Update: buildVersionedUpdate({
src/modules\subject\application\subject-service.ts:226:          key: subjectKey(subject.tenantId, subject.subjectId),
src/modules\subject\application\subject-service.ts:227:          tenantId: subject.tenantId,
src/modules\subject\application\subject-service.ts:235:      const entitlement = await this.ensureEntitlement(subject.tenantId);
src/modules\subject\application\subject-service.ts:237:        Update: buildVersionedUpdate({
src/modules\subject\application\subject-service.ts:239:          key: entitlementKey(subject.tenantId),
src/modules\subject\application\subject-service.ts:240:          tenantId: subject.tenantId,
src/modules\subject\application\subject-service.ts:260:    entries: TransactWriteEntry[],
src/modules\subject\application\subject-service.ts:274:      tenantId: ctx.tenant.tenantId,
src/modules\subject\application\subject-service.ts:290:  private async ensureEntitlement(tenantId: string): Promise<TenantEntitlement> {
src/modules\subject\application\subject-service.ts:291:    const key = entitlementKey(tenantId);
src/modules\subject\application\subject-service.ts:294:    const created = defaultEntitlement(tenantId, this.now());
src/modules\subject\application\subject-service.ts:299:    if (!fresh) throw new ConflictError("Entitlement record vanished after creation race.", { tenantId });
src/modules\subject\application\subject-service.ts:303:  private async readActiveSubject(tenantId: string, subjectId: string): Promise<TrackedSubject> {
src/modules\subject\application\subject-service.ts:304:    const subject = await this.store.get<TrackedSubject>(subjectKey(tenantId, subjectId));
src/modules\subject\application\subject-service.ts:311:  private async commit(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\ports\reconciliation-candidate-source.ts:10: * know a priori which tenants have an expired claim or a DST candidate. `tenantId` is
src/modules\reminder\ports\reconciliation-candidate-source.ts:20:export type ExpiredClaimCandidate = Record<string, unknown> & {
src/modules\reminder\ports\reconciliation-candidate-source.ts:23:  tenantId: string;
src/modules\reminder\ports\reconciliation-candidate-source.ts:35:export type DstReconciliationCandidate = Record<string, unknown> & {
src/modules\reminder\ports\reconciliation-candidate-source.ts:38:  tenantId: string;
src/modules\reminder\ports\reconciliation-candidate-source.ts:43:export interface Page<T> {
src/modules\reminder\ports\reconciliation-candidate-source.ts:48:export interface ReminderReconciliationCandidateSource {
src/modules\reminder\ports\reconciliation-candidate-source.ts:49:  /** GSI6PK="WORKSTATE#CLAIMED" (global), GSI6SK=<claimExpiresAt>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
src/modules\reminder\ports\reconciliation-candidate-source.ts:51:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
src/modules\reminder\ports\reconciliation-candidate-source.ts:55:export const GSI6PK_WORKSTATE_CLAIMED = "WORKSTATE#CLAIMED";
src/modules\reminder\ports\reconciliation-candidate-source.ts:56:export const GSI6PK_WORKSTATE_DST_PENDING = "WORKSTATE#DST_PENDING";
src/modules\reminder\ports\reconciliation-candidate-source.ts:58:export function buildExpiredClaimGsi6Sk(claimExpiresAt: string, tenantId: string, occurrenceId: string): string {
src/modules\reminder\ports\reconciliation-candidate-source.ts:59:  return `${claimExpiresAt}#TENANT#${tenantId}#OCCURRENCE#${occurrenceId}`;
src/modules\reminder\ports\reconciliation-candidate-source.ts:62:export function buildDstCandidateGsi6Sk(windowStart: string, tenantId: string, occurrenceId: string): string {
src/modules\reminder\ports\reconciliation-candidate-source.ts:63:  return `${windowStart}#TENANT#${tenantId}#OCCURRENCE#${occurrenceId}`;
src/modules\expiration\domain\audit-event.ts:5: * every mutation appends its audit record inside the SAME TransactWriteItems as the
src/modules\expiration\domain\audit-event.ts:11:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\expiration\domain\audit-event.ts:13:export type AuditAction = "CREATE" | "UPDATE" | "ARCHIVE" | "RENEW" | "DELETE";
src/modules\expiration\domain\audit-event.ts:15:export interface AuditEvent extends EntityKey {
src/modules\expiration\domain\audit-event.ts:16:  entityType: "AuditEvent";
src/modules\expiration\domain\audit-event.ts:18:  tenantId: string;
src/modules\expiration\domain\audit-event.ts:37:export function auditKey(tenantId: string, occurredAt: string, auditEventId: string): EntityKey {
src/modules\expiration\domain\audit-event.ts:39:    PK: `TENANT#${tenantId}#AUDIT#${monthShard(occurredAt)}`,
src/modules\expiration\domain\audit-event.ts:44:export interface BuildAuditEventInput {
src/modules\expiration\domain\audit-event.ts:46:  tenantId: string;
src/modules\expiration\domain\audit-event.ts:60:  const key = auditKey(input.tenantId, input.occurredAt, input.auditEventId);
src/modules\expiration\domain\audit-event.ts:63:    entityType: "AuditEvent",
src/modules\expiration\domain\audit-event.ts:65:    tenantId: input.tenantId,
src/modules\expiration\domain\audit-event.ts:80:/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction (accepts the full Put|Update union so callers don't need an unsafe cast for a mixed transaction). */
src/modules\expiration\domain\audit-event.ts:81:export function appendAuditToTransaction(tx: TransactWriteEntry[], tableName: string, event: AuditEvent): void {
src/modules\notification\ports\recipient-resolver.ts:3: * `candidateUserId = item.assigneeUserId ?? tenantId` (valid only while the product remains
src/modules\notification\ports\recipient-resolver.ts:4: * single-user-per-tenant, tenantId=userId) behind a port so the future multiuser
src/modules\notification\ports\recipient-resolver.ts:16:export interface ResolvedRecipient {
src/modules\notification\ports\recipient-resolver.ts:18:  tenantId: string;
src/modules\notification\ports\recipient-resolver.ts:22:export interface NotificationRecipientResolver {
src/modules\notification\ports\recipient-resolver.ts:23:  resolve(input: { tenantId: string; candidateUserId: string }): Promise<ResolvedRecipient | undefined>;
src/modules\notification\ports\recipient-resolver.ts:29:export function resolveCandidateUserId(input: { tenantId: string; assigneeUserId?: string }): string {
src/modules\notification\ports\recipient-resolver.ts:31:  return assignee && assignee.length > 0 ? assignee : input.tenantId;
src/modules\import\http\import-handlers.ts:15:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\import\http\import-handlers.ts:23:export interface HttpRequest<TBody = unknown> {
src/modules\import\http\import-handlers.ts:32:export interface HttpResponse {
src/modules\import\http\import-handlers.ts:37:export interface ImportHttpDeps {
src/modules\subject\http\subject-handlers.ts:18:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\subject-handlers.ts:31:export interface HttpRequest<TBody = unknown> {
src/modules\subject\http\subject-handlers.ts:41:export interface HttpResponse {
src/modules\subject\http\subject-handlers.ts:46:export interface SubjectHttpDeps {
src/modules\document\persistence\dynamodb-document-store.ts:3:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\document\persistence\dynamodb-document-store.ts:5:import type { EntityKey, DocumentStore, TransactWriteEntry } from "../ports/document-store.js";
src/modules\document\persistence\dynamodb-document-store.ts:42:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src/modules\bff\http\http-types.ts:5:export interface BffHttpRequest {
src/modules\bff\http\http-types.ts:13:export interface BffHttpResponse {
src/modules\extraction\ports\textract-job-store.ts:6:export interface TextractJobStore {
src/modules\document\domain\malware-scan-result.ts:12:export type GuardDutyScanStatus = "NO_THREATS_FOUND" | "THREATS_FOUND" | "UNSUPPORTED" | "ACCESS_DENIED" | "FAILED";
src/modules\document\domain\malware-scan-result.ts:14:export interface MalwareEvidence {
src/modules\identity\http\test-route-handler.ts:14:export interface TestRouteHttpRequest {
src/modules\identity\http\test-route-handler.ts:20:export interface TestRouteHttpResponse {
src/modules\identity\http\test-route-handler.ts:25:export interface TestRouteDeps {
src/modules\identity\http\test-route-handler.ts:50:    authorize({ context, action: "system:ping", resource: { tenantId: context.tenant.tenantId } });
src/modules\identity\http\test-route-handler.ts:54:      tenantId: context.tenant.tenantId,
src/modules\identity\http\test-route-handler.ts:65:        tenantId: context.tenant.tenantId,
src/modules\import\application\import-service.ts:13:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\import\application\import-service.ts:18:import { isTransactionCanceled, type ImportStore, type TransactWriteEntry } from "../ports/import-store.js";
src/modules\import\application\import-service.ts:26:export interface ReserveImportInput {
src/modules\import\application\import-service.ts:31:export interface ReserveImportResult {
src/modules\import\application\import-service.ts:41: * tudo que command-envelope.v1.json exige no nÃ­vel raiz (tenantId incluso). */
src/modules\import\application\import-service.ts:42:export interface ImportCommitCommand {
src/modules\import\application\import-service.ts:48:  tenantId: string;
src/modules\import\application\import-service.ts:53:export interface ImportServiceDeps {
src/modules\import\application\import-service.ts:91:    authorize({ context: ctx, action: "import:create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\import\application\import-service.ts:104:    await this.quota.consume({ tenantId: ctx.tenant.tenantId, quotaType: "IMPORT_COUNT", window: "current", limit: 10, windowSeconds: 60 * 60 });
src/modules\import\application\import-service.ts:105:    await this.quota.consume({ tenantId: ctx.tenant.tenantId, quotaType: "IMPORT_BYTES", window: "current", limit: 50 * MAX_IMPORT_FILE_BYTES, windowSeconds: 60 * 60 });
src/modules\import\application\import-service.ts:111:    const begin = await this.idempotency.begin({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, requestHash, expiresAt });
src/modules\import\application\import-service.ts:115:      const record = await this.store.get({ PK: `TENANT#${ctx.tenant.tenantId}#IDEMPOTENCY#${OPERATION}`, SK: `KEY#${idempotencyKey}` });
src/modules\import\application\import-service.ts:123:        ...importJobKey(ctx.tenant.tenantId, jobId),
src/modules\import\application\import-service.ts:124:        entityType: "ImportJob",
src/modules\import\application\import-service.ts:126:        tenantId: ctx.tenant.tenantId,
src/modules\import\application\import-service.ts:138:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: jobId });
src/modules\import\application\import-service.ts:141:    const key = this.rawObjectKey(ctx.tenant.tenantId, jobId);
src/modules\import\application\import-service.ts:148:      metadata: { tenantId: ctx.tenant.tenantId, jobId },
src/modules\import\application\import-service.ts:156:    authorize({ context: ctx, action: "import:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\import\application\import-service.ts:157:    const job = await this.store.get<ImportJob>(importJobKey(ctx.tenant.tenantId, jobId));
src/modules\import\application\import-service.ts:163:    authorize({ context: ctx, action: "import:commit", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\import\application\import-service.ts:164:    const job = await this.store.get<ImportJob>(importJobKey(ctx.tenant.tenantId, jobId));
src/modules\import\application\import-service.ts:171:    const entries: TransactWriteEntry[] = [
src/modules\import\application\import-service.ts:173:        Update: buildVersionedUpdate({
src/modules\import\application\import-service.ts:175:          key: importJobKey(ctx.tenant.tenantId, jobId),
src/modules\import\application\import-service.ts:176:          tenantId: ctx.tenant.tenantId,
src/modules\import\application\import-service.ts:184:    // (messageVersion/messageId/tenantId/deduplicationKey no nÃ­vel raiz), mesmo padrÃ£o do
src/modules\import\application\import-service.ts:186:    // tenantId disponÃ­vel na mensagem SQS.
src/modules\import\application\import-service.ts:193:      tenantId: ctx.tenant.tenantId,
src/modules\import\application\import-service.ts:194:      deduplicationKey: `${ctx.tenant.tenantId}|${jobId}|${expectedVersion + 1}`,
src/modules\import\application\import-service.ts:204:      tenantId: ctx.tenant.tenantId,
src/modules\import\application\import-service.ts:219:  private rawObjectKey(tenantId: string, jobId: string): string {
src/modules\import\application\import-service.ts:221:    return `tenant/${tenantId}/imports/${jobId}/raw.csv`;
src/modules\subject\application\requirement-service.ts:12:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\requirement-service.ts:23:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src/modules\subject\application\requirement-service.ts:27:export interface RequirementServiceDeps {
src/modules\subject\application\requirement-service.ts:51:    const subject = await this.readActiveSubject(ctx.tenant.tenantId, subjectId);
src/modules\subject\application\requirement-service.ts:52:    authorize({ context: ctx, action: "requirement:assign", resource: { tenantId: subject.tenantId } });
src/modules\subject\application\requirement-service.ts:57:      ...requirementAssignmentKey(subject.tenantId, subjectId, assignmentId),
src/modules\subject\application\requirement-service.ts:58:      entityType: "RequirementAssignment",
src/modules\subject\application\requirement-service.ts:61:      tenantId: subject.tenantId,
src/modules\subject\application\requirement-service.ts:71:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:88:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:89:    authorize({ context: ctx, action: "requirement:read", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:95:    const subject = await this.readActiveSubject(ctx.tenant.tenantId, subjectId);
src/modules\subject\application\requirement-service.ts:96:    authorize({ context: ctx, action: "requirement:read", resource: { tenantId: subject.tenantId } });
src/modules\subject\application\requirement-service.ts:97:    const rows = await this.store.queryByPk<RequirementAssignment>(subjectKey(subject.tenantId, subjectId).PK, REQUIREMENT_ASSIGNMENT_SK_PREFIX);
src/modules\subject\application\requirement-service.ts:108:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:109:    authorize({ context: ctx, action: "requirement:update", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:125:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:127:        Update: buildVersionedUpdate({
src/modules\subject\application\requirement-service.ts:129:          key: requirementAssignmentKey(assignment.tenantId, subjectId, assignmentId),
src/modules\subject\application\requirement-service.ts:130:          tenantId: assignment.tenantId,
src/modules\subject\application\requirement-service.ts:152:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:153:    authorize({ context: ctx, action: "requirement:review", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:155:    const exists = await this.itemLookup.itemExists(assignment.tenantId, itemId);
src/modules\subject\application\requirement-service.ts:162:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:164:        Update: buildVersionedUpdate({
src/modules\subject\application\requirement-service.ts:166:          key: requirementAssignmentKey(assignment.tenantId, subjectId, assignmentId),
src/modules\subject\application\requirement-service.ts:167:          tenantId: assignment.tenantId,
src/modules\subject\application\requirement-service.ts:188:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:189:    authorize({ context: ctx, action: "requirement:review", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:192:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:194:        Update: buildVersionedUpdate({
src/modules\subject\application\requirement-service.ts:196:          key: requirementAssignmentKey(assignment.tenantId, subjectId, assignmentId),
src/modules\subject\application\requirement-service.ts:197:          tenantId: assignment.tenantId,
src/modules\subject\application\requirement-service.ts:223:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:224:    authorize({ context: ctx, action: "requirement:read", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:225:    const submission = await this.store.get<DocumentSubmission>(documentSubmissionKey(assignment.tenantId, subjectId, assignmentId, submissionId));
src/modules\subject\application\requirement-service.ts:235:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:236:    authorize({ context: ctx, action: "requirement:read", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:238:      subjectKey(assignment.tenantId, subjectId).PK,
src/modules\subject\application\requirement-service.ts:245:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\requirement-service.ts:246:    authorize({ context: ctx, action: "requirement:delete", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\requirement-service.ts:249:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:251:        Update: buildVersionedUpdate({
src/modules\subject\application\requirement-service.ts:253:          key: requirementAssignmentKey(assignment.tenantId, subjectId, assignmentId),
src/modules\subject\application\requirement-service.ts:254:          tenantId: assignment.tenantId,
src/modules\subject\application\requirement-service.ts:273:    entries: TransactWriteEntry[],
src/modules\subject\application\requirement-service.ts:287:      tenantId: ctx.tenant.tenantId,
src/modules\subject\application\requirement-service.ts:302:  private async readActiveSubject(tenantId: string, subjectId: string): Promise<TrackedSubject> {
src/modules\subject\application\requirement-service.ts:303:    const subject = await this.store.get<TrackedSubject>(subjectKey(tenantId, subjectId));
src/modules\subject\application\requirement-service.ts:310:  private async readActiveAssignment(tenantId: string, subjectId: string, assignmentId: string): Promise<RequirementAssignment> {
src/modules\subject\application\requirement-service.ts:311:    const assignment = await this.store.get<RequirementAssignment>(requirementAssignmentKey(tenantId, subjectId, assignmentId));
src/modules\subject\application\requirement-service.ts:318:  private async commit(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\persistence\dynamodb-reminder-store.ts:3:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\reminder\persistence\dynamodb-reminder-store.ts:5:import type { EntityKey, ReminderStore, TransactWriteEntry } from "../ports/reminder-store.js";
src/modules\reminder\persistence\dynamodb-reminder-store.ts:52:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src/modules\reminder\persistence\dynamodb-reminder-store.ts:55:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\reminder\persistence\dynamodb-reminder-store.ts:61:  async queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string, skPrefix = "OCC#"): Promise<T[]> {
src/modules\reminder\persistence\dynamodb-reminder-store.ts:70:            ExpressionAttributeValues: { ":pk": `TENANT#${tenantId}#ITEM#${itemId}`, ":skPrefix": skPrefix },
src/modules\expiration\application\item-watch-service.ts:11:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\expiration\application\item-watch-service.ts:14:import { isTransactionCanceled, type ExpirationStore, type TransactWriteEntry } from "../ports/expiration-store.js";
src/modules\expiration\application\item-watch-service.ts:16:export interface ItemWatchServiceDeps {
src/modules\expiration\application\item-watch-service.ts:34:    await this.requireActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\item-watch-service.ts:35:    authorize({ context: ctx, action: "item:watch", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\expiration\application\item-watch-service.ts:37:    const key = itemWatchKey(ctx.tenant.tenantId, itemId, userId);
src/modules\expiration\application\item-watch-service.ts:43:      const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src/modules\expiration\application\item-watch-service.ts:57:        const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src/modules\expiration\application\item-watch-service.ts:62:      const entries: TransactWriteEntry[] = [
src/modules\expiration\application\item-watch-service.ts:63:        { Update: buildVersionedUpdate({ tableName: this.tableName, key, tenantId: ctx.tenant.tenantId, expectedVersion: current.version, set: { status: "ACTIVE" } }) },
src/modules\expiration\application\item-watch-service.ts:77:    await this.requireActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\item-watch-service.ts:78:    authorize({ context: ctx, action: "item:watch", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\expiration\application\item-watch-service.ts:80:    const key = itemWatchKey(ctx.tenant.tenantId, itemId, userId);
src/modules\expiration\application\item-watch-service.ts:84:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\item-watch-service.ts:85:      { Update: buildVersionedUpdate({ tableName: this.tableName, key, tenantId: ctx.tenant.tenantId, expectedVersion: existing.version, set: { status: "REMOVED" } }) },
src/modules\expiration\application\item-watch-service.ts:99:    await this.requireActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\item-watch-service.ts:100:    authorize({ context: ctx, action: "item:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\expiration\application\item-watch-service.ts:102:    const rows = await this.store.queryByPk<ItemWatch>(itemKey(ctx.tenant.tenantId, itemId).PK, ITEM_WATCH_SK_PREFIX);
src/modules\expiration\application\item-watch-service.ts:106:  private async requireActiveItem(tenantId: string, itemId: string): Promise<void> {
src/modules\expiration\application\item-watch-service.ts:107:    const item = await this.store.get(itemKey(tenantId, itemId));
src/modules\notification\ports\notification-store.ts:5:export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src/modules\notification\ports\notification-store.ts:7:import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\notification\ports\notification-store.ts:9:export interface NotificationStore {
src/modules\notification\ports\notification-store.ts:13:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\notification\ports\notification-store.ts:18:  queryAttemptsByIntent<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, intentId: string): Promise<T[]>;
src/modules\extraction\ports\textract-client.ts:4:export interface StartDocumentTextDetectionInput {
src/modules\extraction\ports\textract-client.ts:9:   * never the raw tenantId/documentId concatenation a log scrape could correlate. */
src/modules\extraction\ports\textract-client.ts:15:export interface StartDocumentTextDetectionResult {
src/modules\extraction\ports\textract-client.ts:19:export type TextractBlock = Record<string, unknown>;
src/modules\extraction\ports\textract-client.ts:21:export type TextractJobStatusResult = "SUCCEEDED" | "FAILED" | "PARTIAL_SUCCESS" | "IN_PROGRESS";
src/modules\extraction\ports\textract-client.ts:23:export interface GetDocumentTextDetectionPage {
src/modules\extraction\ports\textract-client.ts:30:export interface TextractClient {
src/modules\subject\http\requirement-handlers.ts:27:export interface RequirementHttpDeps extends SubjectHttpDeps {
src/modules\subject\http\requirement-handlers.ts:43:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:53:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:64:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:81:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:93:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:107:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:119:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:137:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:150:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\bff\http\bff-handlers.ts:43:export interface BffHttpDeps {
src/modules\bff\http\bff-handlers.ts:94:    return { statusCode: 200, body: { authenticated: true, tenantId: session.tenantId, userId: session.userId } };
src/modules\document\persistence\dynamodb-document-candidate-source.ts:25:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
src/modules\document\domain\document.ts:16:export type DocumentStatus = "PENDING_UPLOAD" | "SCANNING" | "CLEAN" | "REJECTED" | "UNSUPPORTED" | "TIMEOUT" | "DELETED";
src/modules\document\domain\document.ts:18:export interface UploadEvidence {
src/modules\document\domain\document.ts:31:export interface Document extends EntityKey {
src/modules\document\domain\document.ts:32:  entityType: "Document";
src/modules\document\domain\document.ts:33:  tenantId: string;
src/modules\document\domain\document.ts:51:   * `GSI6PK_RECON_UPLOAD_PENDING`. Present only while a purge candidate is pending/claimed;
src/modules\document\domain\document.ts:53:  GSI6PK?: string;
src/modules\document\domain\document.ts:58:   * `attribute_not_exists(GSI6PK) OR GSI6PK <> :purgeClaimed` (D-061) - that condition, not this
src/modules\document\domain\document.ts:73:export function documentKey(tenantId: string, itemId: string, documentId: string): EntityKey {
src/modules\document\domain\document.ts:74:  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `DOC#${documentId}` };
src/modules\identity\http\profile-handlers.ts:14:    tenantId: context.tenant.tenantId,
src/modules\identity\http\profile-handlers.ts:31:export interface HttpRequest<TBody = unknown> {
src/modules\identity\http\profile-handlers.ts:39:export interface HttpResponse {
src/modules\identity\http\profile-handlers.ts:44:export interface ProfileHttpDeps {
src/modules\identity\http\profile-handlers.ts:50:export interface UpdateProfileRequestBody {
src/modules\import\domain\import-row.ts:24:export type ImportRowWarningCode = "FORMULA_LIKE_VALUE";
src/modules\import\domain\import-row.ts:25:export type ImportRowRejectionCode =
src/modules\import\domain\import-row.ts:37:export interface RawImportRow {
src/modules\import\domain\import-row.ts:46:export interface ValidatedImportRow {
src/modules\import\domain\import-row.ts:56:export type ImportRowPlanEntry =
src/modules\import\application\import-parse-service.ts:25:export interface ImportParseDeps {
src/modules\import\application\import-parse-service.ts:36:export type ImportParseOutcome =
src/modules\import\application\import-parse-service.ts:41:function planObjectKey(tenantId: string, jobId: string): string {
src/modules\import\application\import-parse-service.ts:42:  return `tenant/${tenantId}/imports/${jobId}/plan/page-0.jsonl`;
src/modules\import\application\import-parse-service.ts:45:export async function parseImportJob(deps: ImportParseDeps, tenantId: string, jobId: string): Promise<ImportParseOutcome> {
src/modules\import\application\import-parse-service.ts:46:  const job = await deps.store.get<ImportJob>(importJobKey(tenantId, jobId));
src/modules\import\application\import-parse-service.ts:55:    const rawKey = `tenant/${tenantId}/imports/${jobId}/raw.csv`;
src/modules\import\application\import-parse-service.ts:58:      return await failJob(deps, tenantId, job, "FILE_TOO_LARGE");
src/modules\import\application\import-parse-service.ts:63:      return await failJob(deps, tenantId, job, "TOO_MANY_ROWS");
src/modules\import\application\import-parse-service.ts:79:      gsi7pk: `TENANT#${tenantId}#SUBJECTSTATUS#ACTIVE`,
src/modules\import\application\import-parse-service.ts:106:        const existingDedup = await deps.store.get(importDedupKey(tenantId, row.externalId));
src/modules\import\application\import-parse-service.ts:124:    await deps.quota.consume({ tenantId, quotaType: "IMPORT_ROWS", window: "current", limit: 20000, windowSeconds: 60 * 60 });
src/modules\import\application\import-parse-service.ts:128:    const key = planObjectKey(tenantId, jobId);
src/modules\import\application\import-parse-service.ts:147:    return await failJob(deps, tenantId, job, reason);
src/modules\import\application\import-parse-service.ts:151:async function failJob(deps: ImportParseDeps, tenantId: string, job: ImportJob, reason: string): Promise<ImportParseOutcome> {
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:8:import { GetCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:10:import type { EntityKey, Gsi3QueryInput, ReminderProducerStore, TransactWriteEntry } from "../ports/reminder-store.js";
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:64:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:67:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\subject\application\initial-invite-rate-limiter.ts:18:export interface InitialInviteRateLimitRecord extends EntityKey {
src/modules\subject\application\initial-invite-rate-limiter.ts:20:  entityType: "InitialInviteRateLimit";
src/modules\subject\application\initial-invite-rate-limiter.ts:47:  async consumeInitialInvite(tenantId: string, recipientEmail: string): Promise<void> {
src/modules\subject\application\initial-invite-rate-limiter.ts:48:    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS`, SK: "RATE" }, 20, 60 * 60);
src/modules\subject\application\initial-invite-rate-limiter.ts:49:    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS`, SK: "RATE_DAILY" }, 100, 24 * 60 * 60);
src/modules\subject\application\initial-invite-rate-limiter.ts:50:    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS#RECIPIENT#${recipientHash(recipientEmail)}`, SK: "RATE" }, 3, 24 * 60 * 60);
src/modules\subject\application\initial-invite-rate-limiter.ts:62:          entityType: "InitialInviteRateLimit",
src/modules\expiration\application\id-generator.ts:2:export interface ExpirationIdGenerator {
src/modules\extraction\ports\task-token-sender.ts:6:export type SendTaskOutcome = "SENT" | "TERMINAL_QUIET" | "TERMINAL_WARN_INVALID_TOKEN";
src/modules\extraction\ports\task-token-sender.ts:8:export interface TaskTokenSender {
src/modules\notification\ports\email-provider.ts:14:export interface EmailSendInput {
src/modules\notification\ports\email-provider.ts:22:  tags: { attemptId: string; intentId: string; tenantId: string; correlationId: string };
src/modules\notification\ports\email-provider.ts:25:export interface EmailSendResult {
src/modules\notification\ports\email-provider.ts:29:export type EmailSendFailureKind = "CONCLUSIVE_RETRYABLE" | "CONCLUSIVE_TERMINAL" | "AMBIGUOUS";
src/modules\notification\ports\email-provider.ts:41:export interface EmailProviderAdapter {
src/modules\subject\http\guest-handlers.ts:18:export interface GuestHttpRequest<TBody = unknown> {
src/modules\subject\http\guest-handlers.ts:23:export interface GuestHttpResponse {
src/modules\subject\http\guest-handlers.ts:28:export interface GuestHttpDeps {
src/modules\import\domain\import-raw-key.ts:2: * Parses the tenantId/jobId out of a raw CSV object key built by
src/modules\import\domain\import-raw-key.ts:3: * ImportService.reserveImport() (`tenant/<tenantId>/imports/<jobId>/raw.csv`). Used only at
src/modules\import\domain\import-raw-key.ts:10:export interface ParsedImportRawKey {
src/modules\import\domain\import-raw-key.ts:11:  tenantId: string;
src/modules\import\domain\import-raw-key.ts:20:  const [, tenantId, jobId] = match;
src/modules\import\domain\import-raw-key.ts:21:  if (!tenantId || !jobId) return undefined;
src/modules\import\domain\import-raw-key.ts:22:  return { tenantId, jobId };
src/modules\document\domain\document-state-machine.ts:16:export type EvidenceDecision =
src/modules\document\domain\document-state-machine.ts:24:export interface AdvanceAfterEvidenceInput {
src/modules\identity\domain\request-context.ts:3: * authenticated handler receives; no handler/repository accepts tenantId/userId/roles
src/modules\identity\domain\request-context.ts:4: * from a client DTO (blueprint Â§4.1: "O router remove ou rejeita tenantId, userId, roles
src/modules\identity\domain\request-context.ts:7:export interface RequestContext {
src/modules\identity\domain\request-context.ts:17:    readonly tenantId: string;
src/modules\document\http\document-handlers.ts:16:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "UPLOAD_COUNT", window: "current", limit: 20, windowSeconds: 60 });
src/modules\document\http\document-handlers.ts:24:export interface HttpRequest<TBody = unknown> {
src/modules\document\http\document-handlers.ts:33:export interface HttpResponse {
src/modules\document\http\document-handlers.ts:38:export interface DocumentHttpDeps {
src/modules\bff\domain\session.ts:13:export interface Session {
src/modules\bff\domain\session.ts:16:  entityType: "Session";
src/modules\bff\domain\session.ts:19:  tenantId: string;
src/modules\bff\domain\session.ts:55:export interface LoginAttempt {
src/modules\bff\domain\session.ts:58:  entityType: "LoginAttempt";
src/modules\import\application\import-commit-service.ts:33:export interface ImportCommitDeps {
src/modules\import\application\import-commit-service.ts:41:export type ImportCommitOutcome =
src/modules\import\application\import-commit-service.ts:52:  const job = await deps.store.get<ImportJob>(importJobKey(ctx.tenant.tenantId, jobId));
src/modules\import\application\import-commit-service.ts:89:      ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
src/modules\import\application\import-commit-service.ts:90:      entityType: "ImportDedupRecord",
src/modules\import\application\import-commit-service.ts:91:      tenantId: ctx.tenant.tenantId,
src/modules\import\application\import-commit-service.ts:106:          ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
src/modules\import\application\import-commit-service.ts:107:          entityType: "ImportDedupRecord",
src/modules\import\application\import-commit-service.ts:108:          tenantId: ctx.tenant.tenantId,
src/modules\subject\http\document-request-handlers.ts:24:export interface DocumentRequestHttpDeps extends SubjectHttpDeps {
src/modules\subject\http\document-request-handlers.ts:41:  await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\application\id-generator.ts:2:export interface SubjectIdGenerator {
src/modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:12:  GSI6PK_WORKSTATE_CLAIMED,
src/modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:13:  GSI6PK_WORKSTATE_DST_PENDING,
src/modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:36:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
src/modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:37:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_CLAIMED, ":before": input.before },
src/modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:64:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK BETWEEN :start AND :end",
src/modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:65:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_DST_PENDING, ":start": input.window.start, ":end": input.window.end },
src/modules\extraction\ports\task-token-encryptor.ts:6:export interface TaskTokenEncryptor {
src/modules\expiration\application\expiration-service.ts:8: *     changes dueDate) in a single TransactWriteItems - implementing data-model.md Â§5:
src/modules\expiration\application\expiration-service.ts:18:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\expiration\application\expiration-service.ts:35:  type TransactWriteEntry,
src/modules\expiration\application\expiration-service.ts:47:export interface ExpirationServiceDeps {
src/modules\expiration\application\expiration-service.ts:54:export interface DashboardQuery {
src/modules\expiration\application\expiration-service.ts:94:    authorize({ context: ctx, action: "item:create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\expiration\application\expiration-service.ts:128:        tenantId: ctx.tenant.tenantId,
src/modules\expiration\application\expiration-service.ts:137:          PK: `TENANT#${ctx.tenant.tenantId}#IDEMPOTENCY#${operation}`,
src/modules\expiration\application\expiration-service.ts:153:      ...itemKey(ctx.tenant.tenantId, itemId),
src/modules\expiration\application\expiration-service.ts:154:      entityType: "ExpirationItem",
src/modules\expiration\application\expiration-service.ts:156:      tenantId: ctx.tenant.tenantId,
src/modules\expiration\application\expiration-service.ts:173:      ...gsi1Keys(ctx.tenant.tenantId, "ACTIVE", input.dueDate, itemId),
src/modules\expiration\application\expiration-service.ts:176:    const entries: TransactWriteEntry[] = [{ Put: buildVersionedCreate(this.tableName, item as unknown as Record<string, unknown> & { PK: string; SK: string }) }];
src/modules\expiration\application\expiration-service.ts:191:      tenantId: ctx.tenant.tenantId,
src/modules\expiration\application\expiration-service.ts:216:        await this.idempotency.abort({ tenantId: ctx.tenant.tenantId, operation, key: idempotencyState.key });
src/modules\expiration\application\expiration-service.ts:222:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation, key: idempotencyState.key, responseRef: itemId });
src/modules\expiration\application\expiration-service.ts:229:    const item = await this.readActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\expiration-service.ts:230:    authorize({ context: ctx, action: "item:read", resource: { tenantId: item.tenantId } });
src/modules\expiration\application\expiration-service.ts:240:    const item = await this.readActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\expiration-service.ts:241:    authorize({ context: ctx, action: "item:update", resource: { tenantId: item.tenantId } });
src/modules\expiration\application\expiration-service.ts:275:    const gsi1 = gsi1Keys(item.tenantId, item.status, nextDueDate, itemId);
src/modules\expiration\application\expiration-service.ts:279:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:281:        Update: buildVersionedUpdate({
src/modules\expiration\application\expiration-service.ts:283:          key: itemKey(item.tenantId, itemId),
src/modules\expiration\application\expiration-service.ts:284:          tenantId: item.tenantId,
src/modules\expiration\application\expiration-service.ts:301:        tenantId: item.tenantId,
src/modules\expiration\application\expiration-service.ts:328:    const item = await this.readActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\expiration-service.ts:329:    authorize({ context: ctx, action: "item:update", resource: { tenantId: item.tenantId } });
src/modules\expiration\application\expiration-service.ts:334:    const item = await this.readActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\expiration-service.ts:335:    authorize({ context: ctx, action: "item:delete", resource: { tenantId: item.tenantId } });
src/modules\expiration\application\expiration-service.ts:343:   * "tenantId|sourceItemId|sourceVersion|cycle".
src/modules\expiration\application\expiration-service.ts:358:    const source = await this.readActiveItem(ctx.tenant.tenantId, itemId);
src/modules\expiration\application\expiration-service.ts:359:    authorize({ context: ctx, action: "item:update", resource: { tenantId: source.tenantId } });
src/modules\expiration\application\expiration-service.ts:378:      tenantId: ctx.tenant.tenantId,
src/modules\expiration\application\expiration-service.ts:387:        PK: `TENANT#${ctx.tenant.tenantId}#IDEMPOTENCY#${operation}`,
src/modules\expiration\application\expiration-service.ts:399:      const copiedReminderPolicyIds = await this.findItemPolicyIds(ctx.tenant.tenantId, newItemId);
src/modules\expiration\application\expiration-service.ts:427:      await this.idempotency.abort({ tenantId: ctx.tenant.tenantId, operation, key });
src/modules\expiration\application\expiration-service.ts:431:    await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation, key, responseRef: newItem.itemId });
src/modules\expiration\application\expiration-service.ts:440:  private async findItemPolicyIds(tenantId: string, forItemId: string): Promise<string[]> {
src/modules\expiration\application\expiration-service.ts:441:    const pointers = await this.store.queryByPk<PolicyRef>(itemKey(tenantId, forItemId).PK, POLICY_REF_SK_PREFIX);
src/modules\expiration\application\expiration-service.ts:457:      ...itemKey(source.tenantId, newItemId),
src/modules\expiration\application\expiration-service.ts:458:      entityType: "ExpirationItem",
src/modules\expiration\application\expiration-service.ts:460:      tenantId: source.tenantId,
src/modules\expiration\application\expiration-service.ts:478:      ...gsi1Keys(source.tenantId, "ACTIVE", input.newDueDate, newItemId),
src/modules\expiration\application\expiration-service.ts:481:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:483:        Update: buildVersionedUpdate({
src/modules\expiration\application\expiration-service.ts:485:          key: itemKey(source.tenantId, itemId),
src/modules\expiration\application\expiration-service.ts:486:          tenantId: source.tenantId,
src/modules\expiration\application\expiration-service.ts:488:          set: { status: "RENEWED", ...gsi1Keys(source.tenantId, "RENEWED", source.dueDate, itemId) },
src/modules\expiration\application\expiration-service.ts:501:      tenantId: source.tenantId,
src/modules\expiration\application\expiration-service.ts:513:    this.appendItemDeactivated(entries, ctx, { itemId, itemVersion: newVersion, tenantId: source.tenantId });
src/modules\expiration\application\expiration-service.ts:533:    const sourcePointers = await this.store.queryByPk<PolicyRef>(itemKey(source.tenantId, itemId).PK, POLICY_REF_SK_PREFIX);
src/modules\expiration\application\expiration-service.ts:535:      const sourcePolicy = await this.store.get<ReminderPolicy>(policyKey(source.tenantId, pointer.policyId));
src/modules\expiration\application\expiration-service.ts:543:        ...policyKey(source.tenantId, newPolicyId),
src/modules\expiration\application\expiration-service.ts:544:        entityType: "ReminderPolicy",
src/modules\expiration\application\expiration-service.ts:546:        tenantId: source.tenantId,
src/modules\expiration\application\expiration-service.ts:564:          Item: { ...policyRefKey(source.tenantId, newItemId, newPolicyId), entityType: "ReminderPolicyRef", policyId: newPolicyId },
src/modules\expiration\application\expiration-service.ts:578:    authorize({ context: ctx, action: "item:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\expiration\application\expiration-service.ts:580:      gsi1pk: `TENANT#${ctx.tenant.tenantId}#ITEMSTATUS#${query.status}`,
src/modules\expiration\application\expiration-service.ts:596:      ...gsi1Keys(item.tenantId, status, item.dueDate, item.itemId),
src/modules\expiration\application\expiration-service.ts:600:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:602:        Update: buildVersionedUpdate({
src/modules\expiration\application\expiration-service.ts:604:          key: itemKey(item.tenantId, item.itemId),
src/modules\expiration\application\expiration-service.ts:605:          tenantId: item.tenantId,
src/modules\expiration\application\expiration-service.ts:629:  /** BLOCKER-B: appends `expiration.item-deactivated.v1` to `entries` - `tenantId` defaults to `ctx.tenant.tenantId` (every caller except completeRenewal, which already has `source.tenantId` on hand and can skip the extra property access). */
src/modules\expiration\application\expiration-service.ts:631:    entries: TransactWriteEntry[],
src/modules\expiration\application\expiration-service.ts:633:    input: { itemId: string; itemVersion: number; tenantId?: string },
src/modules\expiration\application\expiration-service.ts:635:    const tenantId = input.tenantId ?? ctx.tenant.tenantId;
src/modules\expiration\application\expiration-service.ts:643:      tenantId,
src/modules\expiration\application\expiration-service.ts:652:    entries: TransactWriteEntry[],
src/modules\expiration\application\expiration-service.ts:664:      tenantId: ctx.tenant.tenantId,
src/modules\expiration\application\expiration-service.ts:677:  private async readActiveItem(tenantId: string, itemId: string): Promise<ExpirationItem> {
src/modules\expiration\application\expiration-service.ts:678:    const item = await this.store.get<ExpirationItem>(itemKey(tenantId, itemId));
src/modules\expiration\application\expiration-service.ts:685:  private async commit(entries: TransactWriteEntry[]): Promise<void> {
src/modules\document\domain\document-purge-receipt.ts:11:export interface DocumentPurgeReceipt extends EntityKey {
src/modules\document\domain\document-purge-receipt.ts:12:  entityType: "DocumentPurgeReceipt";
src/modules\document\domain\document-purge-receipt.ts:13:  tenantId: string;
src/modules\document\domain\document-purge-receipt.ts:24:  GSI6PK?: string;
src/modules\document\domain\document-purge-receipt.ts:31:export function documentPurgeReceiptKey(tenantId: string, documentId: string): EntityKey {
src/modules\document\domain\document-purge-receipt.ts:32:  return { PK: `TENANT#${tenantId}#PURGERECEIPT#${documentId}`, SK: "META" };
src/modules\import\domain\import-job.ts:19:export type ImportTargetEntityType = "TrackedSubject";
src/modules\import\domain\import-job.ts:21:export type ImportJobStatus =
src/modules\import\domain\import-job.ts:30:export interface ImportJob extends EntityKey {
src/modules\import\domain\import-job.ts:32:  entityType: "ImportJob";
src/modules\import\domain\import-job.ts:34:  tenantId: string;
src/modules\import\domain\import-job.ts:68:export function importJobKey(tenantId: string, jobId: string): { PK: string; SK: "META" } {
src/modules\import\domain\import-job.ts:69:  return { PK: `TENANT#${tenantId}#IMPORTJOB#${jobId}`, SK: "META" };
src/modules\extraction\ports\document-reader.ts:9:export interface DocumentReader {
src/modules\identity\domain\authorization.ts:7:export type Action =
src/modules\identity\domain\authorization.ts:24:  // RequirementAssignment + ItemWatch. Mesmo padrÃ£o resolver-deriva-tenantId de todo mÃ³dulo
src/modules\identity\domain\authorization.ts:25:  // existente â€” nenhuma dessas actions confia em tenantId fornecido pelo cliente.
src/modules\identity\domain\authorization.ts:56:export interface AuthorizedResource {
src/modules\identity\domain\authorization.ts:57:  tenantId: string;
src/modules\identity\domain\authorization.ts:63:export interface AuthorizationInput {
src/modules\identity\domain\authorization.ts:70: * Minimal role model for M1 (MVP: tenantId=userId, single-owner tenants). Membership/roles
src/modules\identity\domain\authorization.ts:74:export type Role = "OWNER" | "MEMBER" | "VIEWER";
src/modules\identity\domain\authorization.ts:112:export type AuthorizationDenialReason = "TENANT_MISMATCH" | "NO_MEMBERSHIP" | "INSUFFICIENT_ROLE" | "RESOURCE_OWNERSHIP_MISMATCH";
src/modules\identity\domain\authorization.ts:132: *  2. if a resource is supplied, resource.tenantId MUST equal context.tenant.tenantId â€”
src/modules\identity\domain\authorization.ts:136: *     authenticated tenantId, never from client input â€” see repository contract).
src/modules\identity\domain\authorization.ts:151:  if (resource && resource.tenantId !== context.tenant.tenantId) {
src/modules\bff\domain\refresh-outcome.ts:8:export type RefreshOutcome =
src/modules\import\application\id-generator.ts:2:export interface ImportIdGenerator {
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:7: * <userId>`, so a `GetItem` scoped to the CALLER's `tenantId` (never the candidate's) for a
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:9: * that key), not another tenant's data. `resolve()` still asserts `profile.tenantId ===
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:10: * input.tenantId` explicitly as defense in depth (never trust that the key alone is
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:21:interface UserProfileRow {
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:22:  tenantId: string;
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:33:  async resolve(input: { tenantId: string; candidateUserId: string }): Promise<ResolvedRecipient | undefined> {
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:39:          Key: { PK: `TENANT#${input.tenantId}#USER#${input.candidateUserId}`, SK: "PROFILE" },
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:49:    if (profile.tenantId !== input.tenantId) return undefined; // defense in depth, see header
src/modules\notification\persistence\dynamodb-recipient-resolver.ts:50:    return { userId: profile.userId, tenantId: profile.tenantId, active: profile.status === "ACTIVE" };
src/modules\subject\domain\tracked-subject.ts:11:export type TrackedSubjectType = "COMPANY" | "VENDOR" | "CLIENT" | "EMPLOYEE" | "ASSET" | "LOCATION" | "CUSTOM";
src/modules\subject\domain\tracked-subject.ts:12:export type TrackedSubjectStatus = "ACTIVE" | "ARCHIVED" | "DELETED";
src/modules\subject\domain\tracked-subject.ts:14:export interface TrackedSubject extends EntityKey {
src/modules\subject\domain\tracked-subject.ts:16:  entityType: "TrackedSubject";
src/modules\subject\domain\tracked-subject.ts:18:  tenantId: string;
src/modules\subject\domain\tracked-subject.ts:35:export function subjectKey(tenantId: string, subjectId: string): { PK: string; SK: "META" } {
src/modules\subject\domain\tracked-subject.ts:36:  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: "META" };
src/modules\subject\domain\tracked-subject.ts:42:  tenantId: string,
src/modules\subject\domain\tracked-subject.ts:49:    GSI7PK: `TENANT#${tenantId}#SUBJECTSTATUS#${status}`,
src/modules\subject\domain\tracked-subject.ts:68:export interface CreateSubjectInput {
src/modules\subject\domain\tracked-subject.ts:75:export interface UpdateSubjectInput {
src/modules\extraction\ports\ocr-artifact-store.ts:5:export interface ExtractionArtifactRef {
src/modules\extraction\ports\ocr-artifact-store.ts:10:export interface OcrArtifactStore {
src/modules\subject\application\guest-submission-service.ts:10:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\guest-submission-service.ts:15:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src/modules\subject\application\guest-submission-service.ts:35:export interface GuestRequestInfo {
src/modules\subject\application\guest-submission-service.ts:50:export interface StartGuestSubmissionInput {
src/modules\subject\application\guest-submission-service.ts:57:export interface StartGuestSubmissionResult {
src/modules\subject\application\guest-submission-service.ts:64:export interface GuestSubmissionServiceDeps {
src/modules\subject\application\guest-submission-service.ts:76:  resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src/modules\subject\application\guest-submission-service.ts:80:interface ResolvedToken {
src/modules\subject\application\guest-submission-service.ts:93:  private readonly resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src/modules\subject\application\guest-submission-service.ts:111:      requirementAssignmentKey(resolved.request.tenantId, resolved.request.subjectId, resolved.request.assignmentId),
src/modules\subject\application\guest-submission-service.ts:120:      tenantId: resolved.request.tenantId,
src/modules\subject\application\guest-submission-service.ts:146:    const { tenantId, subjectId, assignmentId, documentRequestId } = resolved.pointer;
src/modules\subject\application\guest-submission-service.ts:152:    const quarantineKey = `tenant/${tenantId}/subject/${subjectId}/assignment/${assignmentId}/submission/${submissionId}/document/${submissionId}/slot/${submissionId}/${randomUUID()}`;
src/modules\subject\application\guest-submission-service.ts:155:      ...documentSubmissionKey(tenantId, subjectId, assignmentId, submissionId),
src/modules\subject\application\guest-submission-service.ts:156:      entityType: "DocumentSubmission",
src/modules\subject\application\guest-submission-service.ts:158:      tenantId,
src/modules\subject\application\guest-submission-service.ts:173:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\guest-submission-service.ts:176:        Update: buildVersionedUpdate({
src/modules\subject\application\guest-submission-service.ts:178:          key: documentRequestKey(tenantId, subjectId, assignmentId, documentRequestId),
src/modules\subject\application\guest-submission-service.ts:179:          tenantId,
src/modules\subject\application\guest-submission-service.ts:205:      metadata: { submissionId, tenantId },
src/modules\subject\application\guest-submission-service.ts:246:    const request = await this.store.get<DocumentRequest>(documentRequestKey(pointer.tenantId, pointer.subjectId, pointer.assignmentId, pointer.documentRequestId));
src/modules\subject\application\guest-submission-service.ts:262:          Update: buildVersionedUpdate({
src/modules\subject\application\guest-submission-service.ts:264:            key: documentRequestKey(request.tenantId, request.subjectId, request.assignmentId, request.documentRequestId),
src/modules\subject\application\guest-submission-service.ts:265:            tenantId: request.tenantId,
src/modules\reminder\http\policy-handlers.ts:16:    tenantId: context.tenant.tenantId,
src/modules\reminder\http\policy-handlers.ts:35:export interface HttpRequest<TBody = unknown> {
src/modules\reminder\http\policy-handlers.ts:44:export interface HttpResponse {
src/modules\reminder\http\policy-handlers.ts:49:export interface ReminderHttpDeps {
src/modules\document\domain\document-object-reference.ts:10:export interface DocumentObjectReference {
src/modules\import\domain\import-dedup.ts:10:export interface ImportDedupRecord extends EntityKey {
src/modules\import\domain\import-dedup.ts:11:  entityType: "ImportDedupRecord";
src/modules\import\domain\import-dedup.ts:12:  tenantId: string;
src/modules\import\domain\import-dedup.ts:18:export function importDedupKey(tenantId: string, externalId: string): EntityKey {
src/modules\import\domain\import-dedup.ts:19:  return { PK: `TENANT#${tenantId}#IMPORTDEDUP#SUBJECT`, SK: `EXT#${externalId}` };
src/modules\subject\application\document-chasing-producer.ts:13:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\document-chasing-producer.ts:15:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\document-chasing-producer.ts:17:import { GSI6PK_WORKSTATE_CLAIMED } from "../../reminder/ports/reconciliation-candidate-source.js";
src/modules\subject\application\document-chasing-producer.ts:20:export interface ChasingProducerStore {
src/modules\subject\application\document-chasing-producer.ts:22:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\subject\application\document-chasing-producer.ts:25:export interface ChasingDispatchCommand {
src/modules\subject\application\document-chasing-producer.ts:31:  tenantId: string;
src/modules\subject\application\document-chasing-producer.ts:45:export interface ChasingClaimDeps {
src/modules\subject\application\document-chasing-producer.ts:54:export type ChasingClaimOutcome =
src/modules\subject\application\document-chasing-producer.ts:69:  const { tenantId, occurrenceId } = occurrence;
src/modules\subject\application\document-chasing-producer.ts:81:    tenantId,
src/modules\subject\application\document-chasing-producer.ts:82:    deduplicationKey: `${tenantId}|${occurrenceId}|${newVersion}`,
src/modules\subject\application\document-chasing-producer.ts:102:    tenantId,
src/modules\subject\application\document-chasing-producer.ts:113:        Update: buildVersionedUpdate({
src/modules\subject\application\document-chasing-producer.ts:116:          tenantId,
src/modules\subject\application\document-chasing-producer.ts:122:            GSI6PK: GSI6PK_WORKSTATE_CLAIMED,
src/modules\subject\application\document-chasing-producer.ts:123:            GSI6SK: buildChasingClaimGsi6Sk(claimExpiresAt, tenantId, occurrenceId),
src/modules\extraction\ports\bedrock-client.ts:8:export interface BedrockClient {
src/modules\import\application\csv-parser.ts:10:export interface ParsedCsv {
src/modules\bff\domain\proxy-allowlist.ts:14:export interface AllowlistedRoute {
src/modules\subject\domain\submission-quarantine-key.ts:2: * Parses tenantId/subjectId/assignmentId/submissionId/documentId/uploadSlotId out of a
src/modules\subject\domain\submission-quarantine-key.ts:4: * `tenant/<tenantId>/subject/<subjectId>/assignment/<assignmentId>/submission/<submissionId>/
src/modules\subject\domain\submission-quarantine-key.ts:20:export interface ParsedSubmissionQuarantineKey {
src/modules\subject\domain\submission-quarantine-key.ts:21:  tenantId: string;
src/modules\subject\domain\submission-quarantine-key.ts:35:  const [, tenantId, subjectId, assignmentId, submissionId, documentId, uploadSlotId] = match;
src/modules\subject\domain\submission-quarantine-key.ts:36:  if (!tenantId || !subjectId || !assignmentId || !submissionId || !documentId || !uploadSlotId) return undefined;
src/modules\subject\domain\submission-quarantine-key.ts:37:  return { tenantId, subjectId, assignmentId, submissionId, documentId, uploadSlotId };
src/modules\notification\persistence\dynamodb-notification-store.ts:12:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src/modules\notification\persistence\dynamodb-notification-store.ts:14:import type { EntityKey, NotificationStore, TransactWriteEntry } from "../ports/notification-store.js";
src/modules\notification\persistence\dynamodb-notification-store.ts:61:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
src/modules\notification\persistence\dynamodb-notification-store.ts:64:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\notification\persistence\dynamodb-notification-store.ts:69:  async queryAttemptsByIntent<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, intentId: string): Promise<T[]> {
src/modules\notification\persistence\dynamodb-notification-store.ts:78:            ExpressionAttributeValues: { ":pk": `TENANT#${tenantId}#INTENT#${intentId}`, ":attemptPrefix": "ATTEMPT#" },
src/modules\identity\application\resolve-request-context.ts:18:export interface ValidatedClaims {
src/modules\identity\application\resolve-request-context.ts:26:export interface ResolveRequestContextInput {
src/modules\identity\application\resolve-request-context.ts:32:export interface IdGenerator {
src/modules\identity\application\resolve-request-context.ts:47:    // Step 2: cognitoSub -> userId/tenantId. MVP tenantId=userId (data-model.md Â§7.3),
src/modules\identity\application\resolve-request-context.ts:55:    let profile = await this.users.getProfile(mapping.tenantId, mapping.userId);
src/modules\identity\application\resolve-request-context.ts:59:        tenantId: mapping.tenantId,
src/modules\identity\application\resolve-request-context.ts:78:      deviceSession = await this.users.getDeviceSession(mapping.tenantId, mapping.userId, claims.deviceId);
src/modules\identity\application\resolve-request-context.ts:101:        tenantId: profile.tenantId,
src/modules\document\domain\clean-key.ts:2: * Parses the tenantId/itemId/documentId out of a clean-bucket object key built by
src/modules\document\domain\clean-key.ts:3: * advanceAfterEvidence()'s promotion copy (`clean/<tenantId>/<itemId>/<documentId>`). Used
src/modules\document\domain\clean-key.ts:9:export interface ParsedCleanKey {
src/modules\document\domain\clean-key.ts:10:  tenantId: string;
src/modules\document\domain\clean-key.ts:20:  const [, tenantId, itemId, documentId] = match;
src/modules\document\domain\clean-key.ts:21:  if (!tenantId || !itemId || !documentId) return undefined;
src/modules\document\domain\clean-key.ts:22:  return { tenantId, itemId, documentId };
src/modules\subject\application\guest-rate-limiter.ts:13:export interface GuestRateLimitRecord extends EntityKey {
src/modules\subject\application\guest-rate-limiter.ts:15:  entityType: "GuestTokenRateLimit";
src/modules\subject\application\guest-rate-limiter.ts:48:          entityType: "GuestTokenRateLimit",
src/modules\extraction\ports\feature-flags-reader.ts:11:export interface FeatureFlags {
src/modules\extraction\ports\feature-flags-reader.ts:17:export interface FeatureFlagsReader {
src/modules\reminder\domain\shard-config.ts:14:export interface ShardGeneration {
src/modules\reminder\domain\shard-config.ts:21:export interface ShardConfig {
src/modules\subject\domain\requirement-assignment.ts:20:export type RequirementAssignmentStatus =
src/modules\subject\domain\requirement-assignment.ts:28:export interface RequirementAssignment extends EntityKey {
src/modules\subject\domain\requirement-assignment.ts:29:  entityType: "RequirementAssignment";
src/modules\subject\domain\requirement-assignment.ts:32:  tenantId: string;
src/modules\subject\domain\requirement-assignment.ts:50:export function requirementAssignmentKey(tenantId: string, subjectId: string, assignmentId: string): EntityKey {
src/modules\subject\domain\requirement-assignment.ts:51:  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: `REQASSIGN#${assignmentId}` };
src/modules\subject\domain\requirement-assignment.ts:57:export interface AssignRequirementInput {
src/modules\subject\domain\requirement-assignment.ts:63:export interface UpdateRequirementAssignmentInput {
src/modules\bff\domain\opaque-token.ts:15:export interface TokenCrypto {
src/modules\bff\domain\opaque-token.ts:25:export interface IssuedOpaqueToken {
src/modules\bff\domain\opaque-token.ts:44:export interface ParsedOpaqueToken {
src/modules\identity\application\quota.ts:10:export type QuotaType =
src/modules\identity\application\quota.ts:22:export interface TenantQuotaRecord {
src/modules\identity\application\quota.ts:25:  entityType: "TenantQuota";
src/modules\identity\application\quota.ts:26:  tenantId: string;
src/modules\identity\application\quota.ts:35:export function tenantQuotaKey(tenantId: string, quotaType: QuotaType, window: string): EntityKey {
src/modules\identity\application\quota.ts:36:  return { PK: `TENANT#${tenantId}#QUOTA`, SK: `TYPE#${quotaType}#${window}` };
src/modules\identity\application\quota.ts:39:export interface QuotaCheckInput {
src/modules\identity\application\quota.ts:40:  tenantId: string;
src/modules\identity\application\quota.ts:75:      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
src/modules\identity\application\quota.ts:83:          entityType: "TenantQuota",
src/modules\identity\application\quota.ts:84:          tenantId: input.tenantId,
src/modules\identity\application\quota.ts:97:        throw new QuotaExceededError("Quota blocked by kill switch.", { tenantId: input.tenantId, quotaType: input.quotaType });
src/modules\identity\application\quota.ts:105:          tenantId: input.tenantId,
src/modules\identity\application\quota.ts:121:      tenantId: input.tenantId,
src/modules\identity\application\quota.ts:135:      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
src/modules\identity\application\quota.ts:148:    throw new QuotaExceededError("Quota release could not complete under contention.", { tenantId: input.tenantId, quotaType: input.quotaType });
src/modules\notification\domain\notification-preferences.ts:15:export type NotificationConsentSource = "ONBOARDING" | "USER_SETTINGS" | "MIGRATED_DEFAULT";
src/modules\notification\domain\notification-preferences.ts:17:export interface NotificationPreferences extends EntityKey {
src/modules\notification\domain\notification-preferences.ts:19:  entityType: "NotificationPreferences";
src/modules\notification\domain\notification-preferences.ts:20:  tenantId: string;
src/modules\notification\domain\notification-preferences.ts:36:export function notificationPreferencesKey(tenantId: string, userId: string): { PK: string; SK: "NOTIFICATION_PREFERENCES" } {
src/modules\notification\domain\notification-preferences.ts:37:  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: "NOTIFICATION_PREFERENCES" };
src/modules\notification\domain\notification-preferences.ts:42:  tenantId: string;
src/modules\notification\domain\notification-preferences.ts:49:    ...notificationPreferencesKey(input.tenantId, input.userId),
src/modules\notification\domain\notification-preferences.ts:50:    entityType: "NotificationPreferences",
src/modules\notification\domain\notification-preferences.ts:51:    tenantId: input.tenantId,
src/modules\notification\application\ses-callback-workflow.ts:10: * #2): if the tags (attemptId/intentId/tenantId) are missing or don't resolve to a real,
src/modules\notification\application\ses-callback-workflow.ts:28:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\notification\application\ses-callback-workflow.ts:31:export interface ParsedSesCallbackEvent {
src/modules\notification\application\ses-callback-workflow.ts:35:  tags: { attemptId?: string; intentId?: string; tenantId?: string };
src/modules\notification\application\ses-callback-workflow.ts:39:export interface SesCallbackWorkflowDeps {
src/modules\notification\application\ses-callback-workflow.ts:46:export type SesCallbackOutcome =
src/modules\notification\application\ses-callback-workflow.ts:53: * boundary for a specific SNS delivery of this SES event, keyed only once tenantId is
src/modules\notification\application\ses-callback-workflow.ts:55:function webhookInboxKey(tenantId: string, providerAccountId: string, snsMessageId: string) {
src/modules\notification\application\ses-callback-workflow.ts:56:  return { PK: `TENANT#${tenantId}#WEBHOOK#SES#${providerAccountId}`, SK: `EVENT#${snsMessageId}` };
src/modules\notification\application\ses-callback-workflow.ts:60:  const { attemptId, intentId, tenantId } = event.tags;
src/modules\notification\application\ses-callback-workflow.ts:61:  if (!attemptId || !intentId || !tenantId) {
src/modules\notification\application\ses-callback-workflow.ts:66:  const inboxKey = webhookInboxKey(tenantId, deps.providerAccountId, event.snsMessageId);
src/modules\notification\application\ses-callback-workflow.ts:69:    entityType: "WebhookInbox",
src/modules\notification\application\ses-callback-workflow.ts:70:    tenantId,
src/modules\notification\application\ses-callback-workflow.ts:87:  const lookup = await deps.store.get<NotificationAttemptLookup>(notificationAttemptLookupKey(tenantId, attemptId), true);
src/modules\notification\application\ses-callback-workflow.ts:88:  if (!lookup || lookup.intentId !== intentId || lookup.tenantId !== tenantId || lookup.providerAccountId !== deps.providerAccountId) {
src/modules\notification\application\ses-callback-workflow.ts:93:  const intentPk = `TENANT#${tenantId}#INTENT#${intentId}`;
src/modules\notification\application\ses-callback-workflow.ts:95:  if (!attempt || attempt.tenantId !== tenantId) {
src/modules\notification\application\ses-callback-workflow.ts:112:        Update: buildVersionedUpdate({
src/modules\notification\application\ses-callback-workflow.ts:115:          tenantId,
src/modules\notification\application\ses-callback-workflow.ts:130:    suppressed = await suppressEmailForRecipient(deps, tenantId, intent.recipientUserId, now);
src/modules\notification\application\ses-callback-workflow.ts:142:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: inbox["tenantId"] as string, expectedVersion: inbox.version, now, set: { processingStatus: "PROCESSED" } }) },
src/modules\notification\application\ses-callback-workflow.ts:154:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: inbox["tenantId"] as string, expectedVersion: inbox.version, now, set: { processingStatus: "UNMATCHED" } }) },
src/modules\notification\application\ses-callback-workflow.ts:166:async function suppressEmailForRecipient(deps: SesCallbackWorkflowDeps, tenantId: string, userId: string, now: string): Promise<boolean> {
src/modules\notification\application\ses-callback-workflow.ts:167:  const key = notificationPreferencesKey(tenantId, userId);
src/modules\notification\application\ses-callback-workflow.ts:173:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId, expectedVersion: preferences.version, now, set: { emailEnabled: false } }) },
src/modules\extraction\persistence\sfn-extraction-execution-starter.ts:29:      // tenantId/documentId/documentVersion/pipelineVersion this name was derived from.
src/modules\subject\application\document-chasing-materializer.ts:20:export interface ChasingMaterializerStore {
src/modules\subject\application\document-chasing-materializer.ts:33:function idempotencyKey(input: { tenantId: string; documentRequestId: string; documentRequestVersion: number; tier: DocumentChasingTier; scheduledAt: string }): string {
src/modules\subject\application\document-chasing-materializer.ts:34:  return [input.tenantId, input.documentRequestId, input.documentRequestVersion, input.tier, input.scheduledAt].join("|");
src/modules\subject\application\document-chasing-materializer.ts:37:export interface MaterializeChasingInput {
src/modules\subject\application\document-chasing-materializer.ts:38:  tenantId: string;
src/modules\subject\application\document-chasing-materializer.ts:47:export interface MaterializeChasingResult {
src/modules\subject\application\document-chasing-materializer.ts:86:        tenantId: input.tenantId,
src/modules\subject\application\document-chasing-materializer.ts:95:        tenantId: input.tenantId,
src/modules\subject\application\document-chasing-materializer.ts:102:        ...documentChasingOccurrenceKey(input.tenantId, input.subjectId, input.assignmentId, input.documentRequestId, scheduledAt, occurrenceId),
src/modules\subject\application\document-chasing-materializer.ts:103:        entityType: "DocumentChasingOccurrence",
src/modules\subject\application\document-chasing-materializer.ts:105:        tenantId: input.tenantId,
src/modules\reminder\domain\reminder-policy.ts:9:export type ReminderPolicyScope = "TEMPLATE" | "ITEM";
src/modules\reminder\domain\reminder-policy.ts:10:export type NotificationChannelKind = "EMAIL" | "WHATSAPP";
src/modules\reminder\domain\reminder-policy.ts:14:export interface ReminderTrigger {
src/modules\reminder\domain\reminder-policy.ts:22:export interface QuietHours {
src/modules\reminder\domain\reminder-policy.ts:27:export interface ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:36:export interface ReminderPolicy extends EntityKey, ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:38:  entityType: "ReminderPolicy";
src/modules\reminder\domain\reminder-policy.ts:40:  tenantId: string;
src/modules\reminder\domain\reminder-policy.ts:50:export function policyKey(tenantId: string, policyId: string): { PK: string; SK: "META" } {
src/modules\reminder\domain\reminder-policy.ts:51:  return { PK: `TENANT#${tenantId}#POLICY#${policyId}`, SK: "META" };
src/modules\reminder\domain\reminder-policy.ts:63:export function policyRefKey(tenantId: string, itemId: string, policyId: string): { PK: string; SK: string } {
src/modules\reminder\domain\reminder-policy.ts:64:  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `POLICYREF#${policyId}` };
src/modules\reminder\domain\reminder-policy.ts:69:export interface PolicyRef extends EntityKey {
src/modules\reminder\domain\reminder-policy.ts:70:  entityType: "ReminderPolicyRef";
src/modules\reminder\domain\reminder-policy.ts:74:export interface PutPolicyInput {
src/modules\extraction\ports\extraction-run-store.ts:3:export type { EntityKey } from "../../../shared/dynamodb/occ.js";
src/modules\extraction\ports\extraction-run-store.ts:6:export interface ExtractionRunStore {
src/modules\extraction\ports\extraction-run-store.ts:24:  updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean>;
src/modules\subject\application\document-request-service.ts:9:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\document-request-service.ts:20:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src/modules\subject\application\document-request-service.ts:28:export interface DocumentRequestServiceDeps {
src/modules\subject\application\document-request-service.ts:49:  resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src/modules\subject\application\document-request-service.ts:53:export interface CreatedDocumentRequest {
src/modules\subject\application\document-request-service.ts:71:  private readonly resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src/modules\subject\application\document-request-service.ts:92:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\document-request-service.ts:93:    authorize({ context: ctx, action: "requirement:request-document", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\document-request-service.ts:99:    const tenantDeliveryDefault = await this.getDocumentRequestDeliveryPreferenceDefault(assignment.tenantId);
src/modules\subject\application\document-request-service.ts:112:        await this.initialInviteRateLimiter.consumeInitialInvite(assignment.tenantId, input.recipientEmail);
src/modules\subject\application\document-request-service.ts:130:      ...documentRequestKey(assignment.tenantId, subjectId, assignmentId, documentRequestId),
src/modules\subject\application\document-request-service.ts:131:      entityType: "DocumentRequest",
src/modules\subject\application\document-request-service.ts:133:      tenantId: assignment.tenantId,
src/modules\subject\application\document-request-service.ts:152:      entityType: "GuestTokenPointer",
src/modules\subject\application\document-request-service.ts:155:      tenantId: assignment.tenantId,
src/modules\subject\application\document-request-service.ts:168:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\document-request-service.ts:194:      tenantId: assignment.tenantId,
src/modules\subject\application\document-request-service.ts:217:    const requesterName = await this.resolveRequesterDisplayName?.({ tenantId: ctx.tenant.tenantId, userId: ctx.principal.userId });
src/modules\subject\application\document-request-service.ts:230:        tags: { attemptId: documentRequestId, intentId: documentRequestId, tenantId: assignment.tenantId, correlationId: ctx.correlationId },
src/modules\subject\application\document-request-service.ts:243:    authorize({ context: ctx, action: "tenant:configure-document-request-delivery", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\subject\application\document-request-service.ts:244:    return this.getDocumentRequestDeliveryPreferenceDefault(ctx.tenant.tenantId);
src/modules\subject\application\document-request-service.ts:248:    authorize({ context: ctx, action: "tenant:configure-document-request-delivery", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\subject\application\document-request-service.ts:251:    const key = documentRequestDeliveryPreferenceKey(ctx.tenant.tenantId);
src/modules\subject\application\document-request-service.ts:253:    const entries: TransactWriteEntry[] = existing
src/modules\subject\application\document-request-service.ts:256:            Update: buildVersionedUpdate({
src/modules\subject\application\document-request-service.ts:259:              tenantId: ctx.tenant.tenantId,
src/modules\subject\application\document-request-service.ts:269:              entityType: "DocumentRequestDeliveryPreference",
src/modules\subject\application\document-request-service.ts:270:              tenantId: ctx.tenant.tenantId,
src/modules\subject\application\document-request-service.ts:281:      resourceId: ctx.tenant.tenantId,
src/modules\subject\application\document-request-service.ts:282:      subjectId: ctx.tenant.tenantId, // preferÃªncia Ã© de tenant, nÃ£o de subject - sem subject real a referenciar
src/modules\subject\application\document-request-service.ts:292:      if (isTransactionCanceled(err)) throw new ConflictError("Failed to update document request delivery preference under contention.", { tenantId: ctx.tenant.tenantId });
src/modules\subject\application\document-request-service.ts:297:  private async getDocumentRequestDeliveryPreferenceDefault(tenantId: string): Promise<DocumentRequestDeliveryMode> {
src/modules\subject\application\document-request-service.ts:298:    const preference = await this.store.get<DocumentRequestDeliveryPreference>(documentRequestDeliveryPreferenceKey(tenantId));
src/modules\subject\application\document-request-service.ts:308:    const entries: TransactWriteEntry[] = [];
src/modules\subject\application\document-request-service.ts:312:      subjectId: ctx.tenant.tenantId, // trilha por tenant - sem e-mail bruto (D-049), subjectId real nÃ£o Ã© necessÃ¡rio aqui
src/modules\subject\application\document-request-service.ts:322:    const request = await this.readActiveRequest(ctx.tenant.tenantId, subjectId, documentRequestId);
src/modules\subject\application\document-request-service.ts:323:    authorize({ context: ctx, action: "requirement:read", resource: { tenantId: request.tenantId } });
src/modules\subject\application\document-request-service.ts:328:    const assignment = await this.readActiveAssignment(ctx.tenant.tenantId, subjectId, assignmentId);
src/modules\subject\application\document-request-service.ts:329:    authorize({ context: ctx, action: "requirement:read", resource: { tenantId: assignment.tenantId } });
src/modules\subject\application\document-request-service.ts:331:      requirementAssignmentKey(assignment.tenantId, subjectId, assignmentId).PK,
src/modules\subject\application\document-request-service.ts:338:    const request = await this.readActiveRequest(ctx.tenant.tenantId, subjectId, documentRequestId);
src/modules\subject\application\document-request-service.ts:339:    authorize({ context: ctx, action: "requirement:update", resource: { tenantId: request.tenantId } });
src/modules\subject\application\document-request-service.ts:342:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\document-request-service.ts:344:        Update: buildVersionedUpdate({
src/modules\subject\application\document-request-service.ts:346:          key: documentRequestKey(request.tenantId, subjectId, request.assignmentId, documentRequestId),
src/modules\subject\application\document-request-service.ts:347:          tenantId: request.tenantId,
src/modules\subject\application\document-request-service.ts:353:        Update: buildVersionedUpdate({
src/modules\subject\application\document-request-service.ts:356:          tenantId: request.tenantId,
src/modules\subject\application\document-request-service.ts:377:    entries: TransactWriteEntry[],
src/modules\subject\application\document-request-service.ts:383:      tenantId: ctx.tenant.tenantId,
src/modules\subject\application\document-request-service.ts:398:  private async readActiveAssignment(tenantId: string, subjectId: string, assignmentId: string): Promise<RequirementAssignment> {
src/modules\subject\application\document-request-service.ts:399:    const assignment = await this.store.get<RequirementAssignment>(requirementAssignmentKey(tenantId, subjectId, assignmentId));
src/modules\subject\application\document-request-service.ts:404:  private async readActiveRequest(tenantId: string, subjectId: string, documentRequestId: string): Promise<DocumentRequest> {
src/modules\subject\application\document-request-service.ts:408:    const rows = await this.store.queryByPk<DocumentRequest>(`TENANT#${tenantId}#SUBJECT#${subjectId}`, REQUIREMENT_ASSIGNMENT_SK_PREFIX);
src/modules\subject\application\document-request-service.ts:409:    const found = rows.find((r) => r.entityType === "DocumentRequest" && r.documentRequestId === documentRequestId);
src/modules\subject\domain\guest-token.ts:10: * `TENANT#tenantId` porque o lookup acontece ANTES de `tenantId` ser conhecido, mesmo motivo
src/modules\subject\domain\guest-token.ts:18:export interface GuestTokenPointer extends EntityKey {
src/modules\subject\domain\guest-token.ts:20:  entityType: "GuestTokenPointer";
src/modules\subject\domain\guest-token.ts:23:  tenantId: string;
src/modules\subject\domain\guest-token.ts:51:export interface GuestTokenCrypto {
src/modules\subject\domain\guest-token.ts:61:export interface IssuedGuestToken {
src/modules\subject\domain\guest-token.ts:81:export interface ParsedGuestToken {
src/modules\bff\domain\csrf.ts:35:export interface CsrfCheckInput {
src/modules\notification\http\preferences-handlers.ts:14:    tenantId: context.tenant.tenantId,
src/modules\notification\http\preferences-handlers.ts:31:export interface HttpRequest<TBody = unknown> {
src/modules\notification\http\preferences-handlers.ts:39:export interface HttpResponse {
src/modules\notification\http\preferences-handlers.ts:44:export interface NotificationHttpDeps {
src/modules\document\application\upload-validation.ts:8:export interface DeclaredUpload {
src/modules\document\application\upload-validation.ts:14:export interface ObservedUpload {
src/modules\document\application\upload-validation.ts:20:export type UploadValidationResult = "VALID" | "SIZE_MISMATCH" | "SIZE_EXCEEDS_LIMIT" | "MEDIA_TYPE_MISMATCH" | "CHECKSUM_MISMATCH";
src/modules\identity\application\profile-service.ts:17:export interface ProfileServiceDeps {
src/modules\identity\application\profile-service.ts:29:    authorize({ context: ctx, action: "profile:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\identity\application\profile-service.ts:36:    authorize({ context: ctx, action: "profile:update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\identity\application\profile-service.ts:42:    const profile = await this.users.getProfile(ctx.tenant.tenantId, ctx.principal.userId);
src/modules\notification\domain\notification-entitlements.ts:11:export interface NotificationEntitlements extends EntityKey {
src/modules\notification\domain\notification-entitlements.ts:13:  entityType: "NotificationEntitlements";
src/modules\notification\domain\notification-entitlements.ts:14:  tenantId: string;
src/modules\notification\domain\notification-entitlements.ts:24:export function notificationEntitlementsKey(tenantId: string): { PK: string; SK: "ENTITLEMENTS" } {
src/modules\notification\domain\notification-entitlements.ts:25:  return { PK: `TENANT#${tenantId}#NOTIFICATION`, SK: "ENTITLEMENTS" };
src/modules\notification\application\ses-callback-processor.ts:11:export type SesCallbackEventKind = "DELIVERY" | "BOUNCE" | "COMPLAINT";
src/modules\notification\application\ses-callback-processor.ts:34:export type CallbackApplication = { apply: true; nextStatus: NotificationAttemptStatus } | { apply: false; reason: "NO_OP_NOT_HIGHER_PRECEDENCE" };
src/modules\extraction\persistence\dynamodb-textract-job-store.ts:44:  /** No `tenantId` equality check here (unlike `buildVersionedUpdate`'s usual tenant-scoped
src/modules\reminder\domain\reminder-occurrence.ts:9:export type ReminderOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED" | "ACKED";
src/modules\reminder\domain\reminder-occurrence.ts:11:export interface ReminderOccurrence extends EntityKey {
src/modules\reminder\domain\reminder-occurrence.ts:12:  entityType: "ReminderOccurrence";
src/modules\reminder\domain\reminder-occurrence.ts:14:  tenantId: string;
src/modules\reminder\domain\reminder-occurrence.ts:39:  GSI6PK?: string;
src/modules\reminder\domain\reminder-occurrence.ts:43:export function occurrenceKey(tenantId: string, itemId: string, scheduledAt: string, occurrenceId: string): { PK: string; SK: string } {
src/modules\reminder\domain\reminder-occurrence.ts:44:  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `OCC#${scheduledAt}#${occurrenceId}` };
src/modules\reminder\domain\reminder-occurrence.ts:80:  tenantId: string;
src/modules\reminder\domain\reminder-occurrence.ts:89:    GSI3SK: `TENANT#${input.tenantId}#OCCURRENCE#${input.occurrenceId}`,
src/modules\subject\application\advance-after-submission-evidence.ts:9:import { buildVersionedUpdate, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src/modules\subject\application\advance-after-submission-evidence.ts:14:import type { SubjectStore, TransactWriteEntry } from "../ports/subject-store.js";
src/modules\subject\application\advance-after-submission-evidence.ts:16:export interface AdvanceAfterSubmissionEvidenceDeps {
src/modules\subject\application\advance-after-submission-evidence.ts:23:export type AdvanceSubmissionOutcome = "PROMOTED" | "REJECTED" | "AWAITING" | "IGNORED_STALE" | "IGNORED_WRONG_VERSION";
src/modules\subject\application\advance-after-submission-evidence.ts:29:  input: { tenantId: string; subjectId: string; assignmentId: string; submissionId: string; expectedObject: { bucket: string; key: string; versionId: string } },
src/modules\subject\application\advance-after-submission-evidence.ts:32:    const key = documentSubmissionKey(input.tenantId, input.subjectId, input.assignmentId, input.submissionId);
src/modules\subject\application\advance-after-submission-evidence.ts:55:          { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: decision.status } }) },
src/modules\subject\application\advance-after-submission-evidence.ts:65:    const cleanKey = `clean/${submission.tenantId}/${submission.submissionId}`;
src/modules\subject\application\advance-after-submission-evidence.ts:72:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\advance-after-submission-evidence.ts:73:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: "CLEAN", cleanObject } }) },
src/modules\extraction\ports\extraction-execution-starter.ts:7:export interface ExtractionExecutionInput {
src/modules\extraction\ports\extraction-execution-starter.ts:8:  tenantId: string;
src/modules\extraction\ports\extraction-execution-starter.ts:25:export interface ExtractionExecutionStarter {
src/modules\subject\domain\entitlement.ts:21:export interface TenantEntitlement extends EntityKey {
src/modules\subject\domain\entitlement.ts:23:  entityType: "TenantEntitlement";
src/modules\subject\domain\entitlement.ts:24:  tenantId: string;
src/modules\subject\domain\entitlement.ts:33:export function entitlementKey(tenantId: string): { PK: string; SK: "PLAN" } {
src/modules\subject\domain\entitlement.ts:34:  return { PK: `TENANT#${tenantId}#ENTITLEMENT`, SK: "PLAN" };
src/modules\subject\domain\entitlement.ts:37:export function defaultEntitlement(tenantId: string, now: string): TenantEntitlement {
src/modules\subject\domain\entitlement.ts:39:    ...entitlementKey(tenantId),
src/modules\subject\domain\entitlement.ts:40:    entityType: "TenantEntitlement",
src/modules\subject\domain\entitlement.ts:41:    tenantId,
src/modules\notification\application\notification-preferences-service.ts:7: * current MVP model where tenantId=userId/single-owner tenants, per authorization.ts:36),
src/modules\notification\application\notification-preferences-service.ts:22:import { buildVersionedUpdate, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src/modules\notification\application\notification-preferences-service.ts:30:export interface UpdateNotificationPreferencesInput {
src/modules\notification\application\notification-preferences-service.ts:36:export interface NotificationPreferencesServiceDeps {
src/modules\notification\application\notification-preferences-service.ts:54:    authorize({ context: ctx, action: "notification:configure", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\notification\application\notification-preferences-service.ts:56:    const key = notificationPreferencesKey(ctx.tenant.tenantId, ctx.principal.userId);
src/modules\notification\application\notification-preferences-service.ts:61:      tenantId: ctx.tenant.tenantId,
src/modules\notification\application\notification-preferences-service.ts:98:          Update: buildVersionedUpdate({
src/modules\notification\application\notification-preferences-service.ts:100:            key: notificationPreferencesKey(current.tenantId, current.userId),
src/modules\notification\application\notification-preferences-service.ts:101:            tenantId: current.tenantId,
src/modules\bff\domain\cookies.ts:18:export interface CookieAttributes {
src/modules\document\application\id-generator.ts:2:export interface DocumentIdGenerator {
src/modules\reminder\domain\gsi3-parse.ts:1:/** Parses `GSI3SK = TENANT#<tenantId>#OCCURRENCE#<occurrenceId>` back into its parts - used by the ReminderProducer to reconstruct tenant context from a GSI3 query result before the strongly-consistent base-item read (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
src/modules\reminder\domain\gsi3-parse.ts:2:export function parseGsi3Sk(gsi3sk: string): { tenantId: string; occurrenceId: string } {
src/modules\reminder\domain\gsi3-parse.ts:7:  return { tenantId: match[1] as string, occurrenceId: match[2] as string };
src/modules\notification\domain\notification-attempt.ts:11:export type NotificationAttemptStatus =
src/modules\notification\domain\notification-attempt.ts:23:export interface NotificationAttempt extends EntityKey {
src/modules\notification\domain\notification-attempt.ts:24:  // PK = TENANT#<tenantId>#INTENT#<intentId>, SK = ATTEMPT#<attemptNumber padded>#<attemptId>
src/modules\notification\domain\notification-attempt.ts:25:  entityType: "NotificationAttempt";
src/modules\notification\domain\notification-attempt.ts:26:  tenantId: string;
src/modules\notification\domain\notification-attempt.ts:77:export function notificationAttemptKey(tenantId: string, intentId: string, attemptNumber: number, attemptId: string): EntityKey {
src/modules\notification\domain\notification-attempt.ts:78:  return { PK: `TENANT#${tenantId}#INTENT#${intentId}`, SK: `ATTEMPT#${pad(attemptNumber)}#${attemptId}` };
src/modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
src/modules\notification\domain\notification-attempt.ts:89:export interface NotificationAttemptLookup extends EntityKey {
src/modules\notification\domain\notification-attempt.ts:90:  // PK = TENANT#<tenantId>#ATTEMPT#<attemptId>, SK = LOOKUP
src/modules\notification\domain\notification-attempt.ts:91:  entityType: "NotificationAttemptLookup";
src/modules\notification\domain\notification-attempt.ts:92:  tenantId: string;
src/modules\notification\domain\notification-attempt.ts:99:export function notificationAttemptLookupKey(tenantId: string, attemptId: string): EntityKey {
src/modules\notification\domain\notification-attempt.ts:100:  return { PK: `TENANT#${tenantId}#ATTEMPT#${attemptId}`, SK: "LOOKUP" };
src/modules\notification\domain\notification-attempt.ts:104:  attempt: Pick<NotificationAttempt, "tenantId" | "intentId" | "attemptId" | "attemptNumber" | "provider" | "providerAccountId">,
src/modules\notification\domain\notification-attempt.ts:107:    ...notificationAttemptLookupKey(attempt.tenantId, attempt.attemptId),
src/modules\notification\domain\notification-attempt.ts:108:    entityType: "NotificationAttemptLookup",
src/modules\notification\domain\notification-attempt.ts:109:    tenantId: attempt.tenantId,
src/modules\notification\domain\notification-attempt.ts:111:    attemptSk: notificationAttemptKey(attempt.tenantId, attempt.intentId, attempt.attemptNumber, attempt.attemptId).SK,
src/modules\notification\application\quiet-hours.ts:14:export interface QuietHoursConfig {
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:8:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:35:  async updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean> {
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:36:    const command = buildVersionedUpdate({
src/modules\extraction\persistence\dynamodb-extraction-run-store.ts:39:      tenantId,
src/modules\reminder\domain\recurrence.ts:19:export type ZonedResolutionKind = "NORMAL" | "AMBIGUOUS" | "NONEXISTENT";
src/modules\reminder\domain\recurrence.ts:21:export interface ZonedResolution {
src/modules\reminder\domain\recurrence.ts:26:export interface LocalDateTimeParts {
src/modules\bff\application\proxy-service.ts:17:export interface ProxyRequest {
src/modules\bff\application\proxy-service.ts:25:export interface ProxyResponse {
src/modules\bff\application\proxy-service.ts:31:export interface BackendFetcher {
src/modules\extraction\ports\extracted-field-store.ts:9: * terminal value must happen atomically, in the SAME `TransactWriteItems` that re-asserts the
src/modules\extraction\ports\extracted-field-store.ts:18:export type CommitRunOutcomeResult = "COMMITTED" | "DOCUMENT_DISCARDED";
src/modules\extraction\ports\extracted-field-store.ts:20:export interface CommitRunOutcomeInput {
src/modules\extraction\ports\extracted-field-store.ts:33:   * item is updated with OCC in the SAME `TransactWriteItems` as the field rows and the run
src/modules\extraction\ports\extracted-field-store.ts:42:export interface CommitItemUpdate {
src/modules\extraction\ports\extracted-field-store.ts:44:  tenantId: string;
src/modules\extraction\ports\extracted-field-store.ts:49:export interface ExtractedFieldStore {
src/modules\extraction\ports\extracted-field-store.ts:52:   * `TransactWriteItems` is all-or-nothing), transitions the run to a terminal status, and
src/modules\extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (Â§1.7): one `TransactWriteItems` â€”
src/modules\extraction\ports\extracted-field-store.ts:87:export type FieldTransitionResult = "COMMITTED" | "VERSION_CONFLICT";
src/modules\extraction\ports\extracted-field-store.ts:89:export interface ConfirmFieldInput {
src/modules\extraction\ports\extracted-field-store.ts:107:export interface RejectFieldInput {
src/modules\notification\application\email-delivery.ts:12:export type SendAction =
src/modules\notification\application\email-delivery.ts:38:export type SendOutcome =
src/modules\notification\application\email-delivery-workflow.ts:21:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src/modules\notification\application\email-delivery-workflow.ts:27:export interface EmailDeliverCommandData {
src/modules\notification\application\email-delivery-workflow.ts:28:  tenantId: string;
src/modules\notification\application\email-delivery-workflow.ts:40:export interface EmailDeliveryWorkflowDeps {
src/modules\notification\application\email-delivery-workflow.ts:44:  resolveRecipientEmail: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src/modules\notification\application\email-delivery-workflow.ts:51:export type EmailDeliveryOutcome =
src/modules\notification\application\email-delivery-workflow.ts:68:  const lookup = await deps.store.get<NotificationAttemptLookup>(notificationAttemptLookupKey(command.tenantId, command.attemptId), true);
src/modules\notification\application\email-delivery-workflow.ts:71:  const intentPk = `TENANT#${lookup.tenantId}#INTENT#${lookup.intentId}`;
src/modules\notification\application\email-delivery-workflow.ts:96:  const item = await deps.store.get<ExpirationItem>(itemKey(command.tenantId, command.itemId), true);
src/modules\notification\application\email-delivery-workflow.ts:117:  const to = recipientUserId ? await deps.resolveRecipientEmail({ tenantId: command.tenantId, userId: recipientUserId }) : undefined;
src/modules\notification\application\email-delivery-workflow.ts:134:      tags: { attemptId: attempt.attemptId, intentId: command.intentId, tenantId: command.tenantId, correlationId: command.correlationId },
src/modules\notification\application\email-delivery-workflow.ts:160:        Update: buildVersionedUpdate({
src/modules\notification\application\email-delivery-workflow.ts:163:          tenantId: attempt.tenantId,
src/modules\notification\application\email-delivery-workflow.ts:192:        Update: buildVersionedUpdate({
src/modules\notification\application\email-delivery-workflow.ts:195:          tenantId: submittingAttempt.tenantId,
src/modules\subject\domain\document-submission.ts:17:export type { DocumentStatus, UploadEvidence, MalwareEvidence, DocumentObjectReference };
src/modules\subject\domain\document-submission.ts:19:export interface DocumentSubmission extends EntityKey {
src/modules\subject\domain\document-submission.ts:20:  entityType: "DocumentSubmission";
src/modules\subject\domain\document-submission.ts:22:  tenantId: string;
src/modules\subject\domain\document-submission.ts:41:export function documentSubmissionKey(tenantId: string, subjectId: string, assignmentId: string, submissionId: string): EntityKey {
src/modules\subject\domain\document-submission.ts:42:  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: `REQASSIGN#${assignmentId}#SUBMISSION#${submissionId}` };
src/modules\document\application\document-service.ts:17:import { GSI6PK_RECON_UPLOAD_PENDING, buildUploadSlotGsi6Sk, type DocumentStore, type TransactWriteEntry } from "../ports/document-store.js";
src/modules\document\application\document-service.ts:26:export interface ReserveUploadInput {
src/modules\document\application\document-service.ts:33:export interface ReserveUploadResult {
src/modules\document\application\document-service.ts:41:export interface DocumentServiceDeps {
src/modules\document\application\document-service.ts:76:    authorize({ context: ctx, action: "document:reserve-upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document\application\document-service.ts:93:      tenantId: ctx.tenant.tenantId,
src/modules\document\application\document-service.ts:110:        PK: `TENANT#${ctx.tenant.tenantId}#IDEMPOTENCY#${OPERATION}`,
src/modules\document\application\document-service.ts:117:      const existingDocument = await this.store.get<Document>(documentKey(ctx.tenant.tenantId, itemId, existingDocumentId));
src/modules\document\application\document-service.ts:127:      // Key intentionally encodes tenantId/itemId/documentId/uploadSlotId (M6 design Â§2 "fluxo
src/modules\document\application\document-service.ts:132:      // tenantId/itemId/documentId are internal identifiers, not personal data.
src/modules\document\application\document-service.ts:133:      quarantineKey = `tenant/${ctx.tenant.tenantId}/item/${itemId}/document/${documentId}/slot/${uploadSlotId}/${randomUUID()}`;
src/modules\document\application\document-service.ts:138:        ...documentKey(ctx.tenant.tenantId, itemId, documentId),
src/modules\document\application\document-service.ts:139:        entityType: "Document",
src/modules\document\application\document-service.ts:140:        tenantId: ctx.tenant.tenantId,
src/modules\document\application\document-service.ts:156:        ...uploadSlotKey(ctx.tenant.tenantId, uploadSlotId),
src/modules\document\application\document-service.ts:157:        entityType: "UploadSlot",
src/modules\document\application\document-service.ts:158:        tenantId: ctx.tenant.tenantId,
src/modules\document\application\document-service.ts:176:        GSI6PK: GSI6PK_RECON_UPLOAD_PENDING,
src/modules\document\application\document-service.ts:177:        GSI6SK: buildUploadSlotGsi6Sk(expiresAt, ctx.tenant.tenantId, uploadSlotId),
src/modules\document\application\document-service.ts:180:      const entries: TransactWriteEntry[] = [
src/modules\document\application\document-service.ts:197:      metadata: { documentId, uploadSlotId, tenantId: ctx.tenant.tenantId },
src/modules\document\application\document-service.ts:202:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: documentId });
src/modules\document\application\document-service.ts:218:    authorize({ context: ctx, action: "document:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document\application\document-service.ts:219:    const document = await this.store.get<Document>(documentKey(ctx.tenant.tenantId, itemId, documentId));
src/modules\document\application\document-service.ts:230:    authorize({ context: ctx, action: "document:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document\application\document-service.ts:231:    const documents = await this.store.queryByPk<Document>(`TENANT#${ctx.tenant.tenantId}#ITEM#${itemId}`, "DOC#");
src/modules\notification\application\corrective-intent-service.ts:15:export interface CorrectiveIntentDecision {
src/modules\notification\application\corrective-intent-service.ts:35:  tenantId: string;
src/modules\notification\application\corrective-intent-service.ts:40:  return `${input.tenantId}|${input.supersededIntentId}|${input.currentItemVersion}|${input.kind}`;
src/modules\reminder\application\reminder-policy-service.ts:9: * the SAME TransactWriteItems as the policy row itself:
src/modules\reminder\application\reminder-policy-service.ts:24:import { buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\application\reminder-policy-service.ts:29:import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../ports/reminder-store.js";
src/modules\reminder\application\reminder-policy-service.ts:34:export interface ReminderPolicyServiceDeps {
src/modules\reminder\application\reminder-policy-service.ts:55:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:58:    const tenantId = ctx.tenant.tenantId;
src/modules\reminder\application\reminder-policy-service.ts:62:      ...policyKey(tenantId, policyId),
src/modules\reminder\application\reminder-policy-service.ts:63:      entityType: "ReminderPolicy",
src/modules\reminder\application\reminder-policy-service.ts:65:      tenantId,
src/modules\reminder\application\reminder-policy-service.ts:80:    const entries: TransactWriteEntry[] = [{ Put: buildVersionedCreate(this.tableName, policy as unknown as Record<string, unknown> & { PK: string; SK: string }) }];
src/modules\reminder\application\reminder-policy-service.ts:81:    this.appendItemLinkage(entries, { tenantId, itemId: input.itemId, scope: input.scope, policyId });
src/modules\reminder\application\reminder-policy-service.ts:96:    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
src/modules\reminder\application\reminder-policy-service.ts:97:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:107:    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
src/modules\reminder\application\reminder-policy-service.ts:108:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:111:    const tenantId = policy.tenantId;
src/modules\reminder\application\reminder-policy-service.ts:124:    const entries: TransactWriteEntry[] = [
src/modules\reminder\application\reminder-policy-service.ts:126:        Update: buildVersionedUpdate({
src/modules\reminder\application\reminder-policy-service.ts:128:          key: policyKey(tenantId, policyId),
src/modules\reminder\application\reminder-policy-service.ts:129:          tenantId,
src/modules\reminder\application\reminder-policy-service.ts:141:      entries.push({ Delete: { TableName: this.tableName, Key: policyRefKey(tenantId, policy.itemId!, policyId) } });
src/modules\reminder\application\reminder-policy-service.ts:155:    this.appendItemLinkage(entries, { tenantId, itemId: input.scope === "ITEM" ? input.itemId : undefined, scope: input.scope, policyId, skipPointerWrite: !needsNewPointer });
src/modules\reminder\application\reminder-policy-service.ts:181:    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
src/modules\reminder\application\reminder-policy-service.ts:182:    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
src/modules\reminder\application\reminder-policy-service.ts:184:    const entries: TransactWriteEntry[] = [
src/modules\reminder\application\reminder-policy-service.ts:186:        Update: buildVersionedUpdate({
src/modules\reminder\application\reminder-policy-service.ts:188:          key: policyKey(policy.tenantId, policyId),
src/modules\reminder\application\reminder-policy-service.ts:189:          tenantId: policy.tenantId,
src/modules\reminder\application\reminder-policy-service.ts:214:  private async readActivePolicy(tenantId: string, policyId: string): Promise<ReminderPolicy> {
src/modules\reminder\application\reminder-policy-service.ts:215:    const policy = await this.store.get<ReminderPolicy>(policyKey(tenantId, policyId));
src/modules\reminder\application\reminder-policy-service.ts:224:    entries: TransactWriteEntry[],
src/modules\reminder\application\reminder-policy-service.ts:225:    input: { tenantId: string; itemId: string | undefined; scope: PutPolicyInput["scope"]; policyId: string; skipPointerWrite?: boolean },
src/modules\reminder\application\reminder-policy-service.ts:231:        key: itemKey(input.tenantId, input.itemId),
src/modules\reminder\application\reminder-policy-service.ts:232:        extra: { tenantId: input.tenantId, status: "ACTIVE" },
src/modules\reminder\application\reminder-policy-service.ts:239:          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
src/modules\reminder\application\reminder-policy-service.ts:247:    entries: TransactWriteEntry[],
src/modules\reminder\application\reminder-policy-service.ts:259:      tenantId: ctx.tenant.tenantId,
src/modules\reminder\domain\notification-intent.ts:8:export type NotificationIntentStatus = "PENDING" | "CANCELLED" | "DISPATCHED" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:16:export type NotificationIntentKind = "EXPIRATION_REMINDER" | "REPLACEMENT" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:18:export type NotificationChannel = "EMAIL" | "WHATSAPP";
src/modules\reminder\domain\notification-intent.ts:21:export type NotificationChannelCancellationReason =
src/modules\reminder\domain\notification-intent.ts:32:export interface NotificationIntent extends EntityKey {
src/modules\reminder\domain\notification-intent.ts:34:  entityType: "NotificationIntent";
src/modules\reminder\domain\notification-intent.ts:36:  tenantId: string;
src/modules\reminder\domain\notification-intent.ts:60:export function intentKey(tenantId: string, intentId: string): { PK: string; SK: "META" } {
src/modules\reminder\domain\notification-intent.ts:61:  return { PK: `TENANT#${tenantId}#INTENT#${intentId}`, SK: "META" };
src/modules\reminder\application\id-generator.ts:2:export interface ReminderIdGenerator {
src/modules\bff\application\bff-auth-service.ts:8: * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src/modules\bff\application\bff-auth-service.ts:28:export interface BffAuthServiceDeps {
src/modules\bff\application\bff-auth-service.ts:44:export interface StartedLogin {
src/modules\bff\application\bff-auth-service.ts:73:      entityType: "LoginAttempt",
src/modules\bff\application\bff-auth-service.ts:115:    if (!attempt || attempt.entityType !== "LoginAttempt" || attempt.consumedAt) {
src/modules\bff\application\bff-auth-service.ts:158:    // MVP: tenantId=userId (data-model.md Â§7.3, same rule RequestContextResolver.resolve()
src/modules\bff\application\bff-auth-service.ts:162:    let profile = await this.deps.users.getProfile(mapping.tenantId, mapping.userId);
src/modules\bff\application\bff-auth-service.ts:166:        tenantId: mapping.tenantId,
src/modules\bff\application\bff-auth-service.ts:179:      PK: `TENANT#${mapping.tenantId}#USER#${mapping.userId}`,
src/modules\bff\application\bff-auth-service.ts:181:      entityType: "DeviceSession",
src/modules\bff\application\bff-auth-service.ts:182:      tenantId: mapping.tenantId,
src/modules\bff\application\bff-auth-service.ts:200:      entityType: "Session",
src/modules\bff\application\bff-auth-service.ts:203:      tenantId: mapping.tenantId,
src/modules\bff\application\bff-auth-service.ts:236:    if (!session || session.entityType !== "Session" || session.revokedAt) {

2026-08-28T20:13:01.444264Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -l \"entityType\" src | Sort-Object; rg -n -C 8 \"entityType: \\\"(ReminderPolicyRef|OutboxEvent|NotificationOutcome|IdentityMapping|GuestTokenPointer|GuestTokenRateLimit|TextractJob)\\\"\" src; Get-Content -Raw src/workers/document-purge/purge.ts; Get-Content -Raw src/modules/document/ports/document-store.ts; Get-Content -Raw src/modules/document/application/document-deletion-service.ts"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -l \"entityType\" src | Sort-Object; rg -n -C 8 \"entityType: \\\"(ReminderPolicyRef|OutboxEvent|NotificationOutcome|IdentityMapping|GuestTokenPointer|GuestTokenRateLimit|TextractJob)\\\"\" src; Get-Content -Raw src/workers/document-purge/purge.ts; Get-Content -Raw src/modules/document/ports/document-store.ts; Get-Content -Raw src/modules/document/application/document-deletion-service.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -l \"entityType\" src | Sort-Object; rg -n -C 8 \"entityType: \\\"(ReminderPolicyRef|OutboxEvent|NotificationOutcome|IdentityMapping|GuestTokenPointer|GuestTokenRateLimit|TextractJob)\\\"\" src; Get-Content -Raw src/workers/document-purge/purge.ts; Get-Content -Raw src/modules/document/ports/document-store.ts; Get-Content -Raw src/modules/document/application/document-deletion-service.ts"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -l entityType src' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 174ms:
src\workers\reminder-dispatch\dispatch.ts
src\workers\document-purge\purge.ts
src\workers\document-purge\document-purge-candidate-source.ts
src\workers\reminder-producer\producer.ts
src\workers\document-chasing-dispatch\dispatch.ts
src\shared\idempotency\idempotency.ts
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts
src\modules\identity\persistence\user-repository.ts
src\modules\identity\persistence\identity-mapping-repository.ts
src\modules\subject\application\subject-service.ts
src\modules\expiration\domain\item-watch.ts
src\modules\expiration\domain\expiration-item.ts
src\modules\expiration\domain\audit-event.ts
src\modules\subject\application\requirement-service.ts
src\shared\outbox\outbox.ts
src\modules\subject\application\initial-invite-rate-limiter.ts
src\modules\subject\domain\document-request-delivery-preference.ts
src\modules\expiration\application\item-watch-service.ts
src\modules\subject\application\guest-submission-service.ts
src\modules\subject\domain\document-chasing.ts
src\modules\expiration\application\expiration-service.ts
src\modules\subject\application\guest-rate-limiter.ts
src\modules\subject\domain\audit-event.ts
src\modules\subject\application\document-request-service.ts
src\modules\subject\domain\tracked-subject.ts
src\modules\subject\domain\entitlement.ts
src\modules\subject\domain\document-submission.ts
src\modules\subject\domain\requirement-assignment.ts
src\modules\subject\domain\document-request.ts
src\modules\subject\application\document-chasing-materializer.ts
src\modules\bff\domain\session.ts
src\modules\subject\domain\guest-token.ts
src\modules\document\domain\upload-slot.ts
src\modules\reminder\application\reminder-policy-service.ts
src\modules\bff\application\bff-auth-service.ts
src\modules\document\domain\document.ts
src\modules\reminder\application\reminder-materializer.ts
src\modules\import\application\import-service.ts
src\modules\document\domain\document-purge-receipt.ts
src\modules\reminder\domain\reminder-policy.ts
src\modules\import\domain\import-job.ts
src\modules\reminder\domain\notification-intent.ts
src\modules\import\application\import-commit-service.ts
src\modules\reminder\domain\reminder-occurrence.ts
src\modules\import\domain\import-dedup.ts
src\modules\document\application\document-service.ts
src\modules\extraction\domain\extraction-run.ts
src\modules\extraction\domain\extracted-field.ts
src\modules\identity\application\quota.ts
src\modules\extraction\domain\textract-job.ts
src\modules\notification\domain\notification-preferences.ts
src\runtime\aws\handlers\notification-router-handler.ts
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts
src\modules\notification\domain\notification-entitlements.ts
src\modules\notification\application\notification-router-workflow.ts
src\modules\notification\domain\notification-attempt.ts
src\modules\extraction\application\run-extraction-validation.ts
src\modules\extraction\application\start-ocr.ts
src\modules\extraction\application\start-extraction-run.ts
src\modules\notification\application\ses-callback-workflow.ts

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 7 "ReminderPolicyRef|OutboxEvent|NotificationOutcome|IdentityMapping|GuestTokenPointer|GuestTokenRateLimit|TextractJob" src' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 195ms:
src\workers\reminder-producer\producer.ts-217-              },
src\workers\reminder-producer\producer.ts-218-            };
src\workers\reminder-producer\producer.ts-219-
src\workers\reminder-producer\producer.ts-220-            // M3.5 "DecisÃ£o central: outbox durÃ¡vel": TransactWrite(claim) followed by a
src\workers\reminder-producer\producer.ts-221-            // direct SendMessage in the handler is NOT atomic (Lambda can die between the
src\workers\reminder-producer\producer.ts-222-            // two steps, leaving a CLAIMED occurrence with no queued command - neither the
src\workers\reminder-producer\producer.ts-223-            // producer's own lookback window nor the daily DST reconciliation reliably
src\workers\reminder-producer\producer.ts:224:            // covers that gap). The fix: write a durable OutboxEvent
src\workers\reminder-producer\producer.ts-225-            // (destination=SQS_REMINDER_DISPATCH_V1) in the SAME transaction as the claim;
src\workers\reminder-producer\producer.ts-226-            // DispatchOutboxRelay (DynamoDB Streams) publishes it to SQS, a sweeper recovers
src\workers\reminder-producer\producer.ts-227-            // publication failures. Also set the GSI6 WORKSTATE#CLAIMED pointer so the
src\workers\reminder-producer\producer.ts-228-            // reconciliation job can find this claim if it expires unrecovered - removed
src\workers\reminder-producer\producer.ts-229-            // when ReminderDispatch advances to TRIGGERED or reconciliation reverts it.
src\workers\reminder-producer\producer.ts-230-            const event: DomainEvent = {
src\workers\reminder-producer\producer.ts-231-              specVersion: "1.0",
--
src\workers\document-chasing-dispatch\dispatch.ts-14- * ValidaÃ§Ã£o de staleness (mesma ordem de dispatch.ts do reminder): ocorrÃªncia precisa estar
src\workers\document-chasing-dispatch\dispatch.ts-15- * CLAIMED; DocumentRequest precisa existir e estar REQUESTED/OPENED (nunca reenviar depois de
src\workers\document-chasing-dispatch\dispatch.ts-16- * SUBMITTED/CANCELLED/REVOKED/EXPIRED); versÃµes precisam bater; agendamento dentro da
src\workers\document-chasing-dispatch\dispatch.ts-17- * tolerÃ¢ncia. Qualquer falha aqui cancela a ocorrÃªncia (CLAIMED -> CANCELLED) em vez de
src\workers\document-chasing-dispatch\dispatch.ts-18- * silenciosamente descartar - reparo fica a cargo da reconciliaÃ§Ã£o (mesmo princÃ­pio de Â§9.4).
src\workers\document-chasing-dispatch\dispatch.ts-19- *
src\workers\document-chasing-dispatch\dispatch.ts-20- * RotaÃ§Ã£o de token (D-048): SÃ“ para tiers T7/T3 (destinatÃ¡rio externo) - emite um
src\workers\document-chasing-dispatch\dispatch.ts:21: * `GuestTokenPointer` novo e atualiza `DocumentRequest.tokenSelectorHash`/`tokenVersion` NA
src\workers\document-chasing-dispatch\dispatch.ts-22- * MESMA transaÃ§Ã£o da claim, nunca separado. Pointer antigo nÃ£o Ã© revogado ativamente (expira
src\workers\document-chasing-dispatch\dispatch.ts-23- * pela prÃ³pria `expiresAt`/`deadline`, defesa em profundidade jÃ¡ existente em
src\workers\document-chasing-dispatch\dispatch.ts-24- * `resolveToken()`). Tier EXPIRED NUNCA rotaciona nem envia link externo - notifica
src\workers\document-chasing-dispatch\dispatch.ts-25- * `DocumentRequest.requestedByUserId` em vez do destinatÃ¡rio externo.
src\workers\document-chasing-dispatch\dispatch.ts-26- */
src\workers\document-chasing-dispatch\dispatch.ts-27-import { buildVersionedUpdate, buildVersionedCreate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\document-chasing-dispatch\dispatch.ts-28-import type { TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\document-chasing-dispatch\dispatch.ts-29-import { documentChasingOccurrenceKey, documentChasingIntentKey, type DocumentChasingOccurrence, type DocumentChasingIntent, type DocumentChasingRecipientRef } from "../../modules/subject/domain/document-chasing.js";
src\workers\document-chasing-dispatch\dispatch.ts-30-import { documentRequestKey, type DocumentRequest } from "../../modules/subject/domain/document-request.js";
src\workers\document-chasing-dispatch\dispatch.ts-31-import { requirementAssignmentKey, type RequirementAssignment } from "../../modules/subject/domain/requirement-assignment.js";
src\workers\document-chasing-dispatch\dispatch.ts:32:import { guestTokenPointerKey, issueGuestToken, epochSecondsFromIso, type GuestTokenPointer } from "../../modules/subject/domain/guest-token.js";
src\workers\document-chasing-dispatch\dispatch.ts-33-import type { SubjectStore } from "../../modules/subject/ports/subject-store.js";
src\workers\document-chasing-dispatch\dispatch.ts-34-import type { EmailProviderAdapter } from "../../modules/notification/ports/email-provider.js";
src\workers\document-chasing-dispatch\dispatch.ts-35-import { sanitizeTenantText } from "../../modules/notification/providers/email-templates.js";
src\workers\document-chasing-dispatch\dispatch.ts-36-import type { ChasingDispatchCommand } from "../../modules/subject/application/document-chasing-producer.js";
src\workers\document-chasing-dispatch\dispatch.ts-37-
src\workers\document-chasing-dispatch\dispatch.ts-38-const ACTIVE_REQUEST_STATUSES = new Set(["REQUESTED", "OPENED"]);
src\workers\document-chasing-dispatch\dispatch.ts-39-
--
src\workers\document-chasing-dispatch\dispatch.ts-197-      return { kind: "SEND_FAILED", intent };
src\workers\document-chasing-dispatch\dispatch.ts-198-    }
src\workers\document-chasing-dispatch\dispatch.ts-199-  }
src\workers\document-chasing-dispatch\dispatch.ts-200-
src\workers\document-chasing-dispatch\dispatch.ts-201-  // T7/T3: rotaciona o token (D-048) na MESMA transaÃ§Ã£o da claim - nunca separado.
src\workers\document-chasing-dispatch\dispatch.ts-202-  const issued = issueGuestToken(deps.guestTokenPepper);
src\workers\document-chasing-dispatch\dispatch.ts-203-  const newTokenVersion = request.tokenVersion + 1;
src\workers\document-chasing-dispatch\dispatch.ts:204:  const pointer: GuestTokenPointer = {
src\workers\document-chasing-dispatch\dispatch.ts-205-    ...guestTokenPointerKey(issued.selectorHash),
src\workers\document-chasing-dispatch\dispatch.ts:206:    entityType: "GuestTokenPointer",
src\workers\document-chasing-dispatch\dispatch.ts-207-    selectorHash: issued.selectorHash,
src\workers\document-chasing-dispatch\dispatch.ts-208-    secretHash: issued.secretHash,
src\workers\document-chasing-dispatch\dispatch.ts-209-    tenantId,
src\workers\document-chasing-dispatch\dispatch.ts-210-    subjectId,
src\workers\document-chasing-dispatch\dispatch.ts-211-    assignmentId,
src\workers\document-chasing-dispatch\dispatch.ts-212-    documentRequestId,
src\workers\document-chasing-dispatch\dispatch.ts-213-    tokenVersion: newTokenVersion,
--
src\shared\errors\app-error.ts-172-export class TextractPartialFailureError extends AppError {
src\shared\errors\app-error.ts-173-  constructor(message = "Textract job did not complete usably.", details?: Record<string, unknown>) {
src\shared\errors\app-error.ts-174-    super({ code: "TextractPartialFailure", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts-175-    this.name = "TextractPartialFailureError";
src\shared\errors\app-error.ts-176-  }
src\shared\errors\app-error.ts-177-}
src\shared\errors\app-error.ts-178-
src\shared\errors\app-error.ts:179:/** `StartDocumentTextDetection` succeeded but persisting the `TextractJob` correlation record
src\shared\errors\app-error.ts-180- * failed even after the handler's own local retry (design Â§2, "RecuperaÃ§Ã£o do intervalo") â€” the
src\shared\errors\app-error.ts-181- * Textract job is now orphaned (no waiting callback can ever resolve it), but the run itself
src\shared\errors\app-error.ts-182- * must not block on that: it falls through to the deterministic parser like any other
src\shared\errors\app-error.ts-183- * `RunTextract` failure. */
src\shared\errors\app-error.ts:184:export class TextractJobPersistenceFailedError extends AppError {
src\shared\errors\app-error.ts:185:  constructor(message = "Failed to persist TextractJob correlation record after StartDocumentTextDetection succeeded.", details?: Record<string, unknown>) {
src\shared\errors\app-error.ts:186:    super({ code: "TextractJobPersistenceFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:187:    this.name = "TextractJobPersistenceFailedError";
src\shared\errors\app-error.ts-188-  }
src\shared\errors\app-error.ts-189-}
src\shared\errors\app-error.ts-190-
src\shared\errors\app-error.ts-191-/** `PdfParserTaskHandler`'s `RunDeterministicParser` state (M7 item 5, D-035 Â§1.3) failed to
src\shared\errors\app-error.ts-192- * even attempt parsing (e.g. the OCR artifact was declared available but could not be read/
src\shared\errors\app-error.ts-193- * parsed as valid Textract block JSON). This is the ONE failure the ASL routes straight to
src\shared\errors\app-error.ts-194- * `MarkPendingConfirmation` without trying Bedrock (design Â§1.2) - the ASL's `Catch` for this
--
src\shared\outbox\outbox.ts-31-   * destination-routed through DispatchOutboxRelay; this destination follows the same
src\shared\outbox\outbox.ts-32-   * proven mechanism rather than the never-built one the original design doc assumed. */
src\shared\outbox\outbox.ts-33-  | "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1";
src\shared\outbox\outbox.ts-34-
src\shared\outbox\outbox.ts-35-export interface OutboxRecord {
src\shared\outbox\outbox.ts-36-  PK: string;
src\shared\outbox\outbox.ts-37-  SK: string;
src\shared\outbox\outbox.ts:38:  entityType: "OutboxEvent";
src\shared\outbox\outbox.ts-39-  /** BLOCKER-B addition: previously only embedded in `PK` (`TENANT#<t>#OUTBOX#<shard>`),
src\shared\outbox\outbox.ts-40-   * never its own field - a sender whose payload doesn't already self-describe its tenant
src\shared\outbox\outbox.ts-41-   * (unlike DispatchCommand) needs this explicitly, without every caller re-parsing `PK`. */
src\shared\outbox\outbox.ts-42-  tenantId: string;
src\shared\outbox\outbox.ts-43-  eventId: string;
src\shared\outbox\outbox.ts-44-  eventType: string;
src\shared\outbox\outbox.ts-45-  aggregateType: string;
--
src\shared\outbox\outbox.ts-70-}
src\shared\outbox\outbox.ts-71-
src\shared\outbox\outbox.ts-72-export function buildOutboxRecord(event: DomainEvent, destination?: OutboxDestination): OutboxRecord {
src\shared\outbox\outbox.ts-73-  const shard = monthShard(event.occurredAt);
src\shared\outbox\outbox.ts-74-  return {
src\shared\outbox\outbox.ts-75-    PK: `TENANT#${event.tenantId}#OUTBOX#${shard}`,
src\shared\outbox\outbox.ts-76-    SK: `EVENT#${event.occurredAt}#${event.eventId}`,
src\shared\outbox\outbox.ts:77:    entityType: "OutboxEvent",
src\shared\outbox\outbox.ts-78-    tenantId: event.tenantId,
src\shared\outbox\outbox.ts-79-    eventId: event.eventId,
src\shared\outbox\outbox.ts-80-    eventType: event.eventType,
src\shared\outbox\outbox.ts-81-    aggregateType: event.aggregate.type,
src\shared\outbox\outbox.ts-82-    aggregateId: event.aggregate.id,
src\shared\outbox\outbox.ts-83-    aggregateVersion: event.aggregate.version,
src\shared\outbox\outbox.ts-84-    status: "PENDING",
--
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-22-  for (const record of records) {
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-23-    if (record.eventName !== "INSERT" && record.eventName !== "MODIFY") continue;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-24-    const image = record.dynamodb?.NewImage;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-25-    if (!image) continue;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-26-
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-27-    try {
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-28-      const item = unmarshall(image as Record<string, never>) as OutboxRecord;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:29:      if (item.entityType !== "OutboxEvent") continue;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-30-
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-31-      // m5-observability-design.md #2: DynamoDB Streams fallback is the record's own
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-32-      // SequenceNumber, not eventId (that fallback is the sweeper's, via
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-33-      // outboxRecordCorrelationId - a different source per the design's table).
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-34-      const correlationId = item.correlationId ?? record.dynamodb?.SequenceNumber ?? record.eventID ?? "unknown";
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-35-      await runWithContext({ correlationId }, async () => {
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-36-        // try/catch stays INSIDE runWithContext so a failure log still carries this
--
src\runtime\aws\composition\bff.ts-1-/** Composition root for the BFF module against real DynamoDB/KMS/Cognito. */
src\runtime\aws\composition\bff.ts-2-import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\bff.ts-3-import { DynamoDbIdentityStore } from "../../../modules/identity/persistence/dynamodb-identity-store.js";
src\runtime\aws\composition\bff.ts:4:import { IdentityMappingRepository } from "../../../modules/identity/persistence/identity-mapping-repository.js";
src\runtime\aws\composition\bff.ts-5-import { UserRepository } from "../../../modules/identity/persistence/user-repository.js";
src\runtime\aws\composition\bff.ts-6-import { DynamoDbSessionStore } from "../../../modules/bff/persistence/dynamodb-session-store.js";
src\runtime\aws\composition\bff.ts-7-import { KmsTokenEncryptor, createKmsClient } from "../../../modules/bff/persistence/kms-token-encryptor.js";
src\runtime\aws\composition\bff.ts-8-import { FetchCognitoOidcClient } from "../../../modules/bff/persistence/fetch-cognito-oidc-client.js";
src\runtime\aws\composition\bff.ts-9-import { AwsJwtIdTokenVerifier } from "../../../modules/bff/persistence/aws-jwt-id-token-verifier.js";
src\runtime\aws\composition\bff.ts-10-import { BffAuthService } from "../../../modules/bff/application/bff-auth-service.js";
src\runtime\aws\composition\bff.ts-11-import { ProxyService, type BackendFetcher } from "../../../modules/bff/application/proxy-service.js";
--
src\runtime\aws\composition\bff.ts-34-    });
src\runtime\aws\composition\bff.ts-35-    return { statusCode: res.status, headers, body: await res.text() };
src\runtime\aws\composition\bff.ts-36-  },
src\runtime\aws\composition\bff.ts-37-};
src\runtime\aws\composition\bff.ts-38-
src\runtime\aws\composition\bff.ts-39-export function buildBffDeps(mainClient: DynamoDBDocumentClient, sessionClient: DynamoDBDocumentClient, config: BffConfig) {
src\runtime\aws\composition\bff.ts-40-  const identityStore = new DynamoDbIdentityStore(mainClient, config.mainTableName);
src\runtime\aws\composition\bff.ts:41:  const identityMappings = new IdentityMappingRepository(identityStore);
src\runtime\aws\composition\bff.ts-42-  const users = new UserRepository(identityStore);
src\runtime\aws\composition\bff.ts-43-
src\runtime\aws\composition\bff.ts-44-  const sessionStore = new DynamoDbSessionStore(sessionClient, config.sessionTableName);
src\runtime\aws\composition\bff.ts-45-  const tokenEncryptor = new KmsTokenEncryptor(createKmsClient(), config.kmsKeyId);
src\runtime\aws\composition\bff.ts-46-  const cognitoClient = new FetchCognitoOidcClient(config.cognitoDomain, config.cognitoClientId, config.cognitoClientSecret);
src\runtime\aws\composition\bff.ts-47-  const idTokenVerifier = new AwsJwtIdTokenVerifier(config.cognitoUserPoolId, config.cognitoClientId);
src\runtime\aws\composition\bff.ts-48-  const ids = new UlidIdGenerator();
--
src\runtime\aws\composition\identity.ts-1-/** Composition root for the identity module against real DynamoDB (M3.5). */
src\runtime\aws\composition\identity.ts-2-import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\identity.ts-3-import { DynamoDbIdentityStore } from "../../../modules/identity/persistence/dynamodb-identity-store.js";
src\runtime\aws\composition\identity.ts:4:import { IdentityMappingRepository } from "../../../modules/identity/persistence/identity-mapping-repository.js";
src\runtime\aws\composition\identity.ts-5-import { UserRepository } from "../../../modules/identity/persistence/user-repository.js";
src\runtime\aws\composition\identity.ts-6-import { RequestContextResolver } from "../../../modules/identity/application/resolve-request-context.js";
src\runtime\aws\composition\identity.ts-7-import { TenantQuotaService } from "../../../modules/identity/application/quota.js";
src\runtime\aws\composition\identity.ts-8-import { ProfileService } from "../../../modules/identity/application/profile-service.js";
src\runtime\aws\composition\identity.ts-9-import { UlidIdGenerator } from "../ids.js";
src\runtime\aws\composition\identity.ts-10-
src\runtime\aws\composition\identity.ts-11-export function buildIdentityDeps(client: DynamoDBDocumentClient, tableName: string) {
src\runtime\aws\composition\identity.ts-12-  const store = new DynamoDbIdentityStore(client, tableName);
src\runtime\aws\composition\identity.ts-13-  const ids = new UlidIdGenerator();
src\runtime\aws\composition\identity.ts:14:  const identityMappings = new IdentityMappingRepository(store);
src\runtime\aws\composition\identity.ts-15-  const users = new UserRepository(store);
src\runtime\aws\composition\identity.ts-16-  const resolver = new RequestContextResolver(identityMappings, users, ids);
src\runtime\aws\composition\identity.ts-17-  const quota = new TenantQuotaService(store);
src\runtime\aws\composition\identity.ts-18-  return { store, resolver, quota };
src\runtime\aws\composition\identity.ts-19-}
src\runtime\aws\composition\identity.ts-20-
src\runtime\aws\composition\identity.ts-21-export function buildProfileHttpDeps(client: DynamoDBDocumentClient, tableName: string) {
--
src\modules\identity\persistence\identity-mapping-repository.ts-1-/**
src\modules\identity\persistence\identity-mapping-repository.ts:2: * IdentityMapping repository â€” data-model.md Â§2/Â§"IdentityMapping" +
src\modules\identity\persistence\identity-mapping-repository.ts-3- * implementation-blueprint.md Â§23.1/Â§7.3: PK=IDENTITY#cognitoSub#<sub>, SK=MAP, created
src\modules\identity\persistence\identity-mapping-repository.ts-4- * atomically (ConditionExpression attribute_not_exists(PK)) on first login. This is one
src\modules\identity\persistence\identity-mapping-repository.ts-5- * of the two documented exceptions to the "every key starts with TENANT#tenantId" rule
src\modules\identity\persistence\identity-mapping-repository.ts-6- * (the other is GSI3) â€” the lookup happens *before* tenantId is known.
src\modules\identity\persistence\identity-mapping-repository.ts-7- */
src\modules\identity\persistence\identity-mapping-repository.ts-8-import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
src\modules\identity\persistence\identity-mapping-repository.ts-9-import { InternalError } from "../../../shared/errors/app-error.js";
src\modules\identity\persistence\identity-mapping-repository.ts-10-
src\modules\identity\persistence\identity-mapping-repository.ts:11:export interface IdentityMapping {
src\modules\identity\persistence\identity-mapping-repository.ts-12-  PK: string;
src\modules\identity\persistence\identity-mapping-repository.ts-13-  SK: "MAP";
src\modules\identity\persistence\identity-mapping-repository.ts:14:  entityType: "IdentityMapping";
src\modules\identity\persistence\identity-mapping-repository.ts-15-  cognitoSub: string;
src\modules\identity\persistence\identity-mapping-repository.ts-16-  userId: string;
src\modules\identity\persistence\identity-mapping-repository.ts-17-  tenantId: string;
src\modules\identity\persistence\identity-mapping-repository.ts-18-  createdAt: string;
src\modules\identity\persistence\identity-mapping-repository.ts-19-}
src\modules\identity\persistence\identity-mapping-repository.ts-20-
src\modules\identity\persistence\identity-mapping-repository.ts-21-export function identityMappingKey(cognitoSub: string): EntityKey {
src\modules\identity\persistence\identity-mapping-repository.ts-22-  return { PK: `IDENTITY#cognitoSub#${cognitoSub}`, SK: "MAP" };
src\modules\identity\persistence\identity-mapping-repository.ts-23-}
src\modules\identity\persistence\identity-mapping-repository.ts-24-
src\modules\identity\persistence\identity-mapping-repository.ts:25:export class IdentityMappingRepository {
src\modules\identity\persistence\identity-mapping-repository.ts-26-  constructor(
src\modules\identity\persistence\identity-mapping-repository.ts-27-    private readonly store: IdentityStore,
src\modules\identity\persistence\identity-mapping-repository.ts-28-    private readonly now: () => string = () => new Date().toISOString(),
src\modules\identity\persistence\identity-mapping-repository.ts-29-  ) {}
src\modules\identity\persistence\identity-mapping-repository.ts-30-
src\modules\identity\persistence\identity-mapping-repository.ts:31:  async find(cognitoSub: string): Promise<IdentityMapping | undefined> {
src\modules\identity\persistence\identity-mapping-repository.ts:32:    return this.store.get<IdentityMapping>(identityMappingKey(cognitoSub));
src\modules\identity\persistence\identity-mapping-repository.ts-33-  }
src\modules\identity\persistence\identity-mapping-repository.ts-34-
src\modules\identity\persistence\identity-mapping-repository.ts-35-  /**
src\modules\identity\persistence\identity-mapping-repository.ts-36-   * Atomically creates the mapping on first login. MVP: tenantId=userId (data-model.md
src\modules\identity\persistence\identity-mapping-repository.ts-37-   * Â§7.3 "No MVP, tenantId=userId, mas o cÃ³digo recebe ambos como conceitos separados"),
src\modules\identity\persistence\identity-mapping-repository.ts-38-   * so newUserId is used for both â€” callers must never derive one from the other outside
src\modules\identity\persistence\identity-mapping-repository.ts-39-   * this function once Organizations (FUT-001) exist.
src\modules\identity\persistence\identity-mapping-repository.ts-40-   *
src\modules\identity\persistence\identity-mapping-repository.ts-41-   * Idempotent under races: if two concurrent first-logins for the same sub both call
src\modules\identity\persistence\identity-mapping-repository.ts-42-   * this, exactly one PutItem wins (attribute_not_exists(PK)); the loser re-reads and
src\modules\identity\persistence\identity-mapping-repository.ts-43-   * returns the winner's mapping instead of erroring, so first-login is safe to retry.
src\modules\identity\persistence\identity-mapping-repository.ts-44-   */
src\modules\identity\persistence\identity-mapping-repository.ts:45:  async findOrCreate(cognitoSub: string, newUserId: string, newTenantId: string): Promise<IdentityMapping> {
src\modules\identity\persistence\identity-mapping-repository.ts-46-    const existing = await this.find(cognitoSub);
src\modules\identity\persistence\identity-mapping-repository.ts-47-    if (existing) {
src\modules\identity\persistence\identity-mapping-repository.ts-48-      return existing;
src\modules\identity\persistence\identity-mapping-repository.ts-49-    }
src\modules\identity\persistence\identity-mapping-repository.ts-50-
src\modules\identity\persistence\identity-mapping-repository.ts-51-    const key = identityMappingKey(cognitoSub);
src\modules\identity\persistence\identity-mapping-repository.ts:52:    const candidate: IdentityMapping = {
src\modules\identity\persistence\identity-mapping-repository.ts-53-      ...key,
src\modules\identity\persistence\identity-mapping-repository.ts-54-      SK: "MAP",
src\modules\identity\persistence\identity-mapping-repository.ts:55:      entityType: "IdentityMapping",
src\modules\identity\persistence\identity-mapping-repository.ts-56-      cognitoSub,
src\modules\identity\persistence\identity-mapping-repository.ts-57-      userId: newUserId,
src\modules\identity\persistence\identity-mapping-repository.ts-58-      tenantId: newTenantId,
src\modules\identity\persistence\identity-mapping-repository.ts-59-      createdAt: this.now(),
src\modules\identity\persistence\identity-mapping-repository.ts-60-    };
src\modules\identity\persistence\identity-mapping-repository.ts-61-
src\modules\identity\persistence\identity-mapping-repository.ts-62-    const created = await this.store.putIfAbsent(candidate);
src\modules\identity\persistence\identity-mapping-repository.ts-63-    if (created) {
src\modules\identity\persistence\identity-mapping-repository.ts-64-      return candidate;
src\modules\identity\persistence\identity-mapping-repository.ts-65-    }
src\modules\identity\persistence\identity-mapping-repository.ts-66-
src\modules\identity\persistence\identity-mapping-repository.ts-67-    // Lost the race: someone else created it first-login-atomically between our
src\modules\identity\persistence\identity-mapping-repository.ts-68-    // find() and putIfAbsent(). Re-read; if it's still missing something is badly
src\modules\identity\persistence\identity-mapping-repository.ts:69:    // wrong (a delete of an IdentityMapping is not a supported operation).
src\modules\identity\persistence\identity-mapping-repository.ts-70-    const winner = await this.find(cognitoSub);
src\modules\identity\persistence\identity-mapping-repository.ts-71-    if (!winner) {
src\modules\identity\persistence\identity-mapping-repository.ts:72:      throw new InternalError("IdentityMapping vanished after losing creation race.", { cognitoSub });
src\modules\identity\persistence\identity-mapping-repository.ts-73-    }
src\modules\identity\persistence\identity-mapping-repository.ts-74-    return winner;
src\modules\identity\persistence\identity-mapping-repository.ts-75-  }
src\modules\identity\persistence\identity-mapping-repository.ts-76-}
--
src\modules\subject\domain\guest-token.ts-1-/**
src\modules\subject\domain\guest-token.ts-2- * Guest token â€” 04-domain-model-guest-upload.md (D-037). Token opaco `selector.secret`: o
src\modules\subject\domain\guest-token.ts-3- * `selector` Ã© pÃºblico (usado sÃ³ para lookup), o `secret` sÃ³ Ã© mostrado uma vez ao emitir o
src\modules\subject\domain\guest-token.ts-4- * link. Persistidos apenas os hashes (nunca o valor bruto), com pepper versionado.
src\modules\subject\domain\guest-token.ts-5- *
src\modules\subject\domain\guest-token.ts-6- * Lookup via item ponteiro na tabela base (`GUESTTOKEN#<selectorHash>`/`POINTER`) â€” MESMO
src\modules\subject\domain\guest-token.ts:7: * padrÃ£o de `IdentityMapping` (`src/modules/identity/persistence/identity-mapping-repository.ts`,
src\modules\subject\domain\guest-token.ts-8- * `IDENTITY#cognitoSub#<sub>`/`MAP`), nunca GSI novo. **Terceira exceÃ§Ã£o tenantless
src\modules\subject\domain\guest-token.ts:9: * documentada** (depois de `IdentityMapping` e GSI3) â€” a chave nÃ£o pode comeÃ§ar com
src\modules\subject\domain\guest-token.ts-10- * `TENANT#tenantId` porque o lookup acontece ANTES de `tenantId` ser conhecido, mesmo motivo
src\modules\subject\domain\guest-token.ts-11- * estrutural das outras duas exceÃ§Ãµes.
src\modules\subject\domain\guest-token.ts-12- */
src\modules\subject\domain\guest-token.ts-13-import { createHmac, randomBytes, timingSafeEqual } from "node:crypto";
src\modules\subject\domain\guest-token.ts-14-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\subject\domain\guest-token.ts-15-
src\modules\subject\domain\guest-token.ts-16-export const GUEST_TOKEN_TTL_SECONDS = 14 * 24 * 60 * 60; // 14 dias (04-domain-model-guest-upload.md).
src\modules\subject\domain\guest-token.ts-17-
src\modules\subject\domain\guest-token.ts:18:export interface GuestTokenPointer extends EntityKey {
src\modules\subject\domain\guest-token.ts-19-  SK: "POINTER";
src\modules\subject\domain\guest-token.ts:20:  entityType: "GuestTokenPointer";
src\modules\subject\domain\guest-token.ts-21-  selectorHash: string;
src\modules\subject\domain\guest-token.ts-22-  secretHash: string;
src\modules\subject\domain\guest-token.ts-23-  tenantId: string;
src\modules\subject\domain\guest-token.ts-24-  subjectId: string;
src\modules\subject\domain\guest-token.ts-25-  assignmentId: string;
src\modules\subject\domain\guest-token.ts-26-  documentRequestId: string;
src\modules\subject\domain\guest-token.ts-27-  tokenVersion: number;
src\modules\subject\domain\guest-token.ts-28-  expiresAt: string;
src\modules\subject\domain\guest-token.ts-29-  /** TTL fÃ­sico real da tabela (`infra/modules/dynamo-table/main.tf`'s `ttl.attribute_name`) â€”
src\modules\subject\domain\guest-token.ts-30-   * achado real de D-047/D-048: `expiresAt` sozinho Ã© sÃ³ um campo lido por `resolveToken()`,
src\modules\subject\domain\guest-token.ts:31:   * nunca aciona a exclusÃ£o fÃ­sica do DynamoDB. Sem este campo, cada `GuestTokenPointer`
src\modules\subject\domain\guest-token.ts-32-   * (inclusive cada rotaÃ§Ã£o de chasing, D-048) seria uma linha permanente. */
src\modules\subject\domain\guest-token.ts-33-  purgeAfterTtl: number;
src\modules\subject\domain\guest-token.ts-34-  revokedAt?: string;
src\modules\subject\domain\guest-token.ts-35-  createdAt: string;
src\modules\subject\domain\guest-token.ts-36-  updatedAt: string;
src\modules\subject\domain\guest-token.ts-37-  version: number;
src\modules\subject\domain\guest-token.ts-38-}
--
src\modules\identity\application\resolve-request-context.ts-1-/**
src\modules\identity\application\resolve-request-context.ts-2- * resolveRequestContext â€” implementation-blueprint.md Â§4.1/Â§7.2, the central resolver.
src\modules\identity\application\resolve-request-context.ts-3- * Steps (verbatim from the blueprint):
src\modules\identity\application\resolve-request-context.ts-4- *  1. validation already performed by the API Gateway authorizer (JWT signature/exp) â€”
src\modules\identity\application\resolve-request-context.ts-5- *     this function receives already-validated claims, it does not verify JWT signatures;
src\modules\identity\application\resolve-request-context.ts:6: *  2. consistent lookup of cognitoSub -> userId (IdentityMapping, atomic first-login create);
src\modules\identity\application\resolve-request-context.ts-7- *  3. session/device resolution;
src\modules\identity\application\resolve-request-context.ts-8- *  4. tenant/membership resolution;
src\modules\identity\application\resolve-request-context.ts-9- *  5. reject tokens issued before globalLogoutAfter/deviceLogoutAfter;
src\modules\identity\application\resolve-request-context.ts-10- *  6. build the immutable RequestContext.
src\modules\identity\application\resolve-request-context.ts-11- */
src\modules\identity\application\resolve-request-context.ts-12-import { AuthenticationError } from "../../../shared/errors/app-error.js";
src\modules\identity\application\resolve-request-context.ts:13:import { IdentityMappingRepository } from "../persistence/identity-mapping-repository.js";
src\modules\identity\application\resolve-request-context.ts-14-import { UserRepository } from "../persistence/user-repository.js";
src\modules\identity\application\resolve-request-context.ts-15-import type { RequestContext } from "../domain/request-context.js";
src\modules\identity\application\resolve-request-context.ts-16-
src\modules\identity\application\resolve-request-context.ts-17-/** Claims already validated (signature + expiry) by the API Gateway JWT authorizer. */
src\modules\identity\application\resolve-request-context.ts-18-export interface ValidatedClaims {
src\modules\identity\application\resolve-request-context.ts-19-  sub: string;
src\modules\identity\application\resolve-request-context.ts-20-  tokenId: string; // jti
--
src\modules\identity\application\resolve-request-context.ts-32-export interface IdGenerator {
src\modules\identity\application\resolve-request-context.ts-33-  newUserId(): string;
src\modules\identity\application\resolve-request-context.ts-34-  newSessionId(): string;
src\modules\identity\application\resolve-request-context.ts-35-}
src\modules\identity\application\resolve-request-context.ts-36-
src\modules\identity\application\resolve-request-context.ts-37-export class RequestContextResolver {
src\modules\identity\application\resolve-request-context.ts-38-  constructor(
src\modules\identity\application\resolve-request-context.ts:39:    private readonly identityMappings: IdentityMappingRepository,
src\modules\identity\application\resolve-request-context.ts-40-    private readonly users: UserRepository,
src\modules\identity\application\resolve-request-context.ts-41-    private readonly ids: IdGenerator,
src\modules\identity\application\resolve-request-context.ts-42-  ) {}
src\modules\identity\application\resolve-request-context.ts-43-
src\modules\identity\application\resolve-request-context.ts-44-  async resolve(input: ResolveRequestContextInput): Promise<RequestContext> {
src\modules\identity\application\resolve-request-context.ts-45-    const { claims } = input;
src\modules\identity\application\resolve-request-context.ts-46-
--
src\modules\bff\domain\session.ts-3- * its `tenant_facing_read_write` policy already grants GetItem/Query to ~20+ resource roles,
src\modules\bff\domain\session.ts-4- * exactly the over-broad exposure D-054 flagged). Distinct from
src\modules\bff\domain\session.ts-5- * src/modules/identity/domain/request-context.ts's `sessionId`/`DeviceSession` - those are the
src\modules\bff\domain\session.ts-6- * JWT-authorizer-side revocation watermark (M1), this is the BFF-side browser session that
src\modules\bff\domain\session.ts-7- * *produces* the bearer token DeviceSession later revokes. Never reuse the same name for both.
src\modules\bff\domain\session.ts-8- *
src\modules\bff\domain\session.ts-9- * PK/SK: `SESSION#<selectorHash>` / `POINTER` - single dedicated table, no tenant prefix
src\modules\bff\domain\session.ts:10: * needed (unlike GuestTokenPointer's tenantless-exception note, this table has no other
src\modules\bff\domain\session.ts-11- * tenant-scoped data to isolate from; the whole table is BFF-only, per D-054).
src\modules\bff\domain\session.ts-12- */
src\modules\bff\domain\session.ts-13-export interface Session {
src\modules\bff\domain\session.ts-14-  PK: string;
src\modules\bff\domain\session.ts-15-  SK: "POINTER";
src\modules\bff\domain\session.ts-16-  entityType: "Session";
src\modules\bff\domain\session.ts-17-  selectorHash: string;
--
src\modules\bff\domain\session.ts-27-   * every proxied request; refreshed proactively, never trusted past its own expiry. */
src\modules\bff\domain\session.ts-28-  accessToken: string;
src\modules\bff\domain\session.ts-29-  accessTokenExpiresAt: string;
src\modules\bff\domain\session.ts-30-  /** Fixed at creation, never extended by refresh (D-054: absolute session lifetime ceiling
src\modules\bff\domain\session.ts-31-   * independent of activity). */
src\modules\bff\domain\session.ts-32-  absoluteExpiresAt: string;
src\modules\bff\domain\session.ts-33-  /** DynamoDB TTL attribute (epoch seconds) - physical deletion, same convention as
src\modules\bff\domain\session.ts:34:   * GuestTokenPointer.purgeAfterTtl. Recomputed on every activity as `now + idle TTL`, capped
src\modules\bff\domain\session.ts-35-   * at `absoluteExpiresAt`. */
src\modules\bff\domain\session.ts-36-  purgeAfterTtl: number;
src\modules\bff\domain\session.ts-37-  refreshState: "IDLE" | "IN_PROGRESS";
src\modules\bff\domain\session.ts-38-  refreshLeaseId?: string;
src\modules\bff\domain\session.ts-39-  refreshLeaseUntil?: string;
src\modules\bff\domain\session.ts-40-  revokedAt?: string;
src\modules\bff\domain\session.ts-41-  createdAt: string;
--
src\modules\bff\domain\opaque-token.ts-1-/**
src\modules\bff\domain\opaque-token.ts-2- * Opaque selector.secret token â€” the exact same mechanism as
src\modules\bff\domain\opaque-token.ts:3: * src/modules/subject/domain/guest-token.ts's GuestTokenPointer, generalized for the BFF's
src\modules\bff\domain\opaque-token.ts-4- * two ephemeral/session tokens (LoginAttempt handle, Session handle) instead of duplicating
src\modules\bff\domain\opaque-token.ts-5- * it twice. Never invent a third pattern for the same problem (AGENTS.md Â§4's "reuse the
src\modules\bff\domain\opaque-token.ts-6- * existing pattern" principle, applied here to a pattern proven within this same session).
src\modules\bff\domain\opaque-token.ts-7- *
src\modules\bff\domain\opaque-token.ts-8- * `selector` is the DynamoDB lookup key (public, low-entropy-safe since it never authorizes
src\modules\bff\domain\opaque-token.ts-9- * anything by itself). `secret` is verified via HMAC + timingSafeEqual and is what actually
src\modules\bff\domain\opaque-token.ts-10- * proves possession of the cookie - only its hash is ever persisted, exactly like
src\modules\bff\domain\opaque-token.ts:11: * GuestTokenPointer's selectorHash/secretHash split.
src\modules\bff\domain\opaque-token.ts-12- */
src\modules\bff\domain\opaque-token.ts-13-import { createHmac, randomBytes, timingSafeEqual } from "node:crypto";
src\modules\bff\domain\opaque-token.ts-14-
src\modules\bff\domain\opaque-token.ts-15-export interface TokenCrypto {
src\modules\bff\domain\opaque-token.ts-16-  hash(pepper: string, value: string): string;
src\modules\bff\domain\opaque-token.ts-17-}
src\modules\bff\domain\opaque-token.ts-18-
--
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-19-  for (const record of records) {
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-20-    if (record.eventName !== "INSERT" && record.eventName !== "MODIFY") continue;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-21-    const image = record.dynamodb?.NewImage;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-22-    if (!image) continue;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-23-
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-24-    try {
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-25-      const item = unmarshall(image as Record<string, never>) as OutboxRecord;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:26:      if (item.entityType !== "OutboxEvent") continue;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-27-
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-28-      // m5-observability-design.md #2: DynamoDB Streams fallback is the record's own
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-29-      // SequenceNumber, not eventId (that fallback is the sweeper's, via
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-30-      // outboxRecordCorrelationId - a different source per the design's table).
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-31-      const correlationId = item.correlationId ?? record.dynamodb?.SequenceNumber ?? record.eventID ?? "unknown";
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-32-      await runWithContext({ correlationId }, async () => {
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-33-        // try/catch stays INSIDE runWithContext - see dispatch-outbox-relay-processor.ts's
--
src\modules\extraction\ports\textract-job-store.ts:1:/** DynamoDB surface for `TextractJob` â€” narrow, matching the ExtractionRunStore/DocumentReader
src\modules\extraction\ports\textract-job-store.ts-2- * pattern already established in this module (AGENTS.md Â§7). */
src\modules\extraction\ports\textract-job-store.ts-3-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\extraction\ports\textract-job-store.ts:4:import type { TextractJob } from "../domain/textract-job.js";
src\modules\extraction\ports\textract-job-store.ts-5-
src\modules\extraction\ports\textract-job-store.ts:6:export interface TextractJobStore {
src\modules\extraction\ports\textract-job-store.ts:7:  /** Creates a brand-new TextractJob record. Called only AFTER StartDocumentTextDetection has
src\modules\extraction\ports\textract-job-store.ts-8-   * already succeeded (design Â§2) â€” a failure here (even after the caller's own local retry)
src\modules\extraction\ports\textract-job-store.ts:9:   * must surface as `TextractJobPersistenceFailedError`, never be swallowed. */
src\modules\extraction\ports\textract-job-store.ts:10:  create(job: TextractJob): Promise<void>;
src\modules\extraction\ports\textract-job-store.ts-11-
src\modules\extraction\ports\textract-job-store.ts-12-  /** Lookup by `jobId` alone â€” the only correlation `COMPLETE_OCR` has available from the SNS
src\modules\extraction\ports\textract-job-store.ts-13-   * notification. `null` when no matching job exists (the orphan-job case, design Â§2). */
src\modules\extraction\ports\textract-job-store.ts:14:  getByJobId(jobId: string): Promise<TextractJob | null>;
src\modules\extraction\ports\textract-job-store.ts-15-
src\modules\extraction\ports\textract-job-store.ts-16-  /** Conditional update used by `COMPLETE_OCR` to record the terminal outcome and clear
src\modules\extraction\ports\textract-job-store.ts-17-   * `taskTokenCiphertext` (design Â§3) â€” `expectedVersion` guards against concurrent SQS
src\modules\extraction\ports\textract-job-store.ts-18-   * redelivery of the same completion message racing itself. Returns false (not throw) on a
src\modules\extraction\ports\textract-job-store.ts-19-   * lost OCC race, matching `ExtractionRunStore`/`TenantQuotaService`'s own convention, so the
src\modules\extraction\ports\textract-job-store.ts-20-   * caller can decide idempotently (a lost race here means another delivery already finished
src\modules\extraction\ports\textract-job-store.ts-21-   * the same work â€” safe to treat as success, never as a hard failure).
src\modules\extraction\ports\textract-job-store.ts-22-   */
src\modules\extraction\ports\textract-job-store.ts:23:  updateConditional(job: TextractJob, expected: { version: number }): Promise<boolean>;
src\modules\extraction\ports\textract-job-store.ts-24-
src\modules\extraction\ports\textract-job-store.ts-25-  key(jobId: string): EntityKey;
src\modules\extraction\ports\textract-job-store.ts-26-}
--
src\modules\extraction\ports\textract-client.ts-14-
src\modules\extraction\ports\textract-client.ts-15-export interface StartDocumentTextDetectionResult {
src\modules\extraction\ports\textract-client.ts-16-  jobId: string;
src\modules\extraction\ports\textract-client.ts-17-}
src\modules\extraction\ports\textract-client.ts-18-
src\modules\extraction\ports\textract-client.ts-19-export type TextractBlock = Record<string, unknown>;
src\modules\extraction\ports\textract-client.ts-20-
src\modules\extraction\ports\textract-client.ts:21:export type TextractJobStatusResult = "SUCCEEDED" | "FAILED" | "PARTIAL_SUCCESS" | "IN_PROGRESS";
src\modules\extraction\ports\textract-client.ts-22-
src\modules\extraction\ports\textract-client.ts-23-export interface GetDocumentTextDetectionPage {
src\modules\extraction\ports\textract-client.ts:24:  status: TextractJobStatusResult;
src\modules\extraction\ports\textract-client.ts-25-  blocks: readonly TextractBlock[];
src\modules\extraction\ports\textract-client.ts-26-  nextToken?: string;
src\modules\extraction\ports\textract-client.ts-27-  warnings?: readonly string[];
src\modules\extraction\ports\textract-client.ts-28-}
src\modules\extraction\ports\textract-client.ts-29-
src\modules\extraction\ports\textract-client.ts-30-export interface TextractClient {
src\modules\extraction\ports\textract-client.ts-31-  startDocumentTextDetection(input: StartDocumentTextDetectionInput): Promise<StartDocumentTextDetectionResult>;
--
src\modules\reminder\domain\reminder-policy.ts-63-export function policyRefKey(tenantId: string, itemId: string, policyId: string): { PK: string; SK: string } {
src\modules\reminder\domain\reminder-policy.ts-64-  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `POLICYREF#${policyId}` };
src\modules\reminder\domain\reminder-policy.ts-65-}
src\modules\reminder\domain\reminder-policy.ts-66-
src\modules\reminder\domain\reminder-policy.ts-67-export const POLICY_REF_SK_PREFIX = "POLICYREF#";
src\modules\reminder\domain\reminder-policy.ts-68-
src\modules\reminder\domain\reminder-policy.ts-69-export interface PolicyRef extends EntityKey {
src\modules\reminder\domain\reminder-policy.ts:70:  entityType: "ReminderPolicyRef";
src\modules\reminder\domain\reminder-policy.ts-71-  policyId: string;
src\modules\reminder\domain\reminder-policy.ts-72-}
src\modules\reminder\domain\reminder-policy.ts-73-
src\modules\reminder\domain\reminder-policy.ts-74-export interface PutPolicyInput {
src\modules\reminder\domain\reminder-policy.ts-75-  scope: ReminderPolicyScope;
src\modules\reminder\domain\reminder-policy.ts-76-  itemId?: string;
src\modules\reminder\domain\reminder-policy.ts-77-  rule: ReminderRule;
--
src\modules\subject\application\document-request-service.ts-5- */
src\modules\subject\application\document-request-service.ts-6-import type { RequestContext } from "../../identity/domain/request-context.js";
src\modules\subject\application\document-request-service.ts-7-import { authorize } from "../../identity/domain/authorization.js";
src\modules\subject\application\document-request-service.ts-8-import { ConflictError, NotFoundError, QuotaExceededError } from "../../../shared/errors/app-error.js";
src\modules\subject\application\document-request-service.ts-9-import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\document-request-service.ts-10-import { requirementAssignmentKey, REQUIREMENT_ASSIGNMENT_SK_PREFIX, type RequirementAssignment } from "../domain/requirement-assignment.js";
src\modules\subject\application\document-request-service.ts-11-import { documentRequestKey, type DocumentRequest, type CreateDocumentRequestInput } from "../domain/document-request.js";
src\modules\subject\application\document-request-service.ts:12:import { guestTokenPointerKey, issueGuestToken, epochSecondsFromIso, GUEST_TOKEN_TTL_SECONDS, type GuestTokenPointer } from "../domain/guest-token.js";
src\modules\subject\application\document-request-service.ts-13-import { buildSubjectAuditEvent, appendSubjectAuditToTransaction, type SubjectAuditAction, type SubjectAuditResourceType } from "../domain/audit-event.js";
src\modules\subject\application\document-request-service.ts-14-import {
src\modules\subject\application\document-request-service.ts-15-  documentRequestDeliveryPreferenceKey,
src\modules\subject\application\document-request-service.ts-16-  resolveInitialInviteDeliveryMode,
src\modules\subject\application\document-request-service.ts-17-  type DocumentRequestDeliveryPreference,
src\modules\subject\application\document-request-service.ts-18-  type DocumentRequestDeliveryMode,
src\modules\subject\application\document-request-service.ts-19-} from "../domain/document-request-delivery-preference.js";
--
src\modules\subject\application\document-request-service.ts-143-      tokenVersion: 1,
src\modules\subject\application\document-request-service.ts-144-      tokenExpiresAt,
src\modules\subject\application\document-request-service.ts-145-      submissionCount: 0,
src\modules\subject\application\document-request-service.ts-146-      createdAt: now,
src\modules\subject\application\document-request-service.ts-147-      updatedAt: now,
src\modules\subject\application\document-request-service.ts-148-      version: 1,
src\modules\subject\application\document-request-service.ts-149-    };
src\modules\subject\application\document-request-service.ts:150:    const pointer: GuestTokenPointer = {
src\modules\subject\application\document-request-service.ts-151-      ...guestTokenPointerKey(issued.selectorHash),
src\modules\subject\application\document-request-service.ts:152:      entityType: "GuestTokenPointer",
src\modules\subject\application\document-request-service.ts-153-      selectorHash: issued.selectorHash,
src\modules\subject\application\document-request-service.ts-154-      secretHash: issued.secretHash,
src\modules\subject\application\document-request-service.ts-155-      tenantId: assignment.tenantId,
src\modules\subject\application\document-request-service.ts-156-      subjectId,
src\modules\subject\application\document-request-service.ts-157-      assignmentId,
src\modules\subject\application\document-request-service.ts-158-      documentRequestId,
src\modules\subject\application\document-request-service.ts-159-      tokenVersion: 1,
--
src\modules\subject\application\document-request-service.ts-364-    } catch (err) {
src\modules\subject\application\document-request-service.ts-365-      if (isTransactionCanceled(err)) throw new ConflictError("VERSION_CONFLICT", { cause: "transaction condition failed" });
src\modules\subject\application\document-request-service.ts-366-      throw err;
src\modules\subject\application\document-request-service.ts-367-    }
src\modules\subject\application\document-request-service.ts-368-  }
src\modules\subject\application\document-request-service.ts-369-
src\modules\subject\application\document-request-service.ts-370-  private async pointerVersion(selectorHash: string): Promise<number> {
src\modules\subject\application\document-request-service.ts:371:    const pointer = await this.store.get<GuestTokenPointer>(guestTokenPointerKey(selectorHash));
src\modules\subject\application\document-request-service.ts-372-    if (!pointer) throw new ConflictError("Guest token pointer vanished before revocation.", { selectorHash });
src\modules\subject\application\document-request-service.ts-373-    return pointer.version;
src\modules\subject\application\document-request-service.ts-374-  }
src\modules\subject\application\document-request-service.ts-375-
src\modules\subject\application\document-request-service.ts-376-  private appendAudit(
src\modules\subject\application\document-request-service.ts-377-    entries: TransactWriteEntry[],
src\modules\subject\application\document-request-service.ts-378-    ctx: RequestContext,
--
src\modules\bff\application\bff-auth-service.ts-5- *
src\modules\bff\application\bff-auth-service.ts-6- * Reuses src/modules/identity as a library (RequestContextResolver's constituent repos), the
src\modules\bff\application\bff-auth-service.ts-7- * same way src/workers/reminder-producer imports src/modules/reminder in-process - no new
src\modules\bff\application\bff-auth-service.ts-8- * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src\modules\bff\application\bff-auth-service.ts-9- */
src\modules\bff\application\bff-auth-service.ts-10-import { randomUUID } from "node:crypto";
src\modules\bff\application\bff-auth-service.ts-11-import { AuthenticationError, ConflictError, DependencyUnavailableError, ValidationError } from "../../../shared/errors/app-error.js";
src\modules\bff\application\bff-auth-service.ts:12:import { IdentityMappingRepository } from "../../identity/persistence/identity-mapping-repository.js";
src\modules\bff\application\bff-auth-service.ts-13-import { UserRepository } from "../../identity/persistence/user-repository.js";
src\modules\bff\application\bff-auth-service.ts-14-import type { SessionStore } from "../ports/session-store.js";
src\modules\bff\application\bff-auth-service.ts-15-import type { CognitoOidcClient, IdTokenVerifier } from "../ports/cognito-oidc-client.js";
src\modules\bff\application\bff-auth-service.ts-16-import type { TokenEncryptor } from "../ports/token-encryptor.js";
src\modules\bff\application\bff-auth-service.ts-17-import { issueOpaqueToken, parseOpaqueToken, opaqueTokenSecretMatches, hmacTokenCrypto } from "../domain/opaque-token.js";
src\modules\bff\application\bff-auth-service.ts-18-import { issueCsrfSecret } from "../domain/csrf.js";
src\modules\bff\application\bff-auth-service.ts-19-import {
--
src\modules\bff\application\bff-auth-service.ts-26-import type { RefreshOutcome } from "../domain/refresh-outcome.js";
src\modules\bff\application\bff-auth-service.ts-27-
src\modules\bff\application\bff-auth-service.ts-28-export interface BffAuthServiceDeps {
src\modules\bff\application\bff-auth-service.ts-29-  sessionStore: SessionStore;
src\modules\bff\application\bff-auth-service.ts-30-  cognitoClient: CognitoOidcClient;
src\modules\bff\application\bff-auth-service.ts-31-  idTokenVerifier: IdTokenVerifier;
src\modules\bff\application\bff-auth-service.ts-32-  tokenEncryptor: TokenEncryptor;
src\modules\bff\application\bff-auth-service.ts:33:  identityMappings: IdentityMappingRepository;
src\modules\bff\application\bff-auth-service.ts-34-  users: UserRepository;
src\modules\bff\application\bff-auth-service.ts-35-  pepper: string;
src\modules\bff\application\bff-auth-service.ts-36-  redirectUri: string;
src\modules\bff\application\bff-auth-service.ts-37-  authorizeUrl: string; // Cognito Hosted UI /oauth2/authorize base URL (from Terraform output/env, not hardcoded)
src\modules\bff\application\bff-auth-service.ts-38-  clientId: string;
src\modules\bff\application\bff-auth-service.ts-39-  now: () => string;
src\modules\bff\application\bff-auth-service.ts-40-  newUserId: () => string;
--
src\modules\expiration\application\expiration-service.ts-557-        createdAt: now,
src\modules\expiration\application\expiration-service.ts-558-        updatedAt: now,
src\modules\expiration\application\expiration-service.ts-559-      };
src\modules\expiration\application\expiration-service.ts-560-      entries.push({ Put: buildVersionedCreate(this.tableName, newPolicy as unknown as Record<string, unknown> & { PK: string; SK: string }) });
src\modules\expiration\application\expiration-service.ts-561-      entries.push({
src\modules\expiration\application\expiration-service.ts-562-        Put: {
src\modules\expiration\application\expiration-service.ts-563-          TableName: this.tableName,
src\modules\expiration\application\expiration-service.ts:564:          Item: { ...policyRefKey(source.tenantId, newItemId, newPolicyId), entityType: "ReminderPolicyRef", policyId: newPolicyId },
src\modules\expiration\application\expiration-service.ts-565-          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\expiration\application\expiration-service.ts-566-        },
src\modules\expiration\application\expiration-service.ts-567-      });
src\modules\expiration\application\expiration-service.ts-568-      copiedReminderPolicyIds.push(newPolicyId);
src\modules\expiration\application\expiration-service.ts-569-    }
src\modules\expiration\application\expiration-service.ts-570-
src\modules\expiration\application\expiration-service.ts-571-    await this.commit(entries);
--
src\modules\subject\application\guest-submission-service.ts-4- * token. Toda falha de validaÃ§Ã£o retorna o MESMO erro genÃ©rico (`GuestTokenInvalidError`),
src\modules\subject\application\guest-submission-service.ts-5- * SEM detalhe algum no corpo da resposta (anti-enumeration â€” `AppError.toJSON()` inclui
src\modules\subject\application\guest-submission-service.ts-6- * `details` na resposta HTTP, entÃ£o este mÃ³dulo nunca populates `details` com o motivo real).
src\modules\subject\application\guest-submission-service.ts-7- */
src\modules\subject\application\guest-submission-service.ts-8-import { randomUUID } from "node:crypto";
src\modules\subject\application\guest-submission-service.ts-9-import { AppError, ValidationError } from "../../../shared/errors/app-error.js";
src\modules\subject\application\guest-submission-service.ts-10-import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\guest-submission-service.ts:11:import { parseGuestToken, secretMatches, guestTokenPointerKey, hmacGuestTokenCrypto, type GuestTokenPointer } from "../domain/guest-token.js";
src\modules\subject\application\guest-submission-service.ts-12-import { documentRequestKey, type DocumentRequest } from "../domain/document-request.js";
src\modules\subject\application\guest-submission-service.ts-13-import { requirementAssignmentKey, type RequirementAssignment } from "../domain/requirement-assignment.js";
src\modules\subject\application\guest-submission-service.ts-14-import { documentSubmissionKey, type DocumentSubmission } from "../domain/document-submission.js";
src\modules\subject\application\guest-submission-service.ts-15-import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src\modules\subject\application\guest-submission-service.ts-16-import type { SubjectIdGenerator } from "./id-generator.js";
src\modules\subject\application\guest-submission-service.ts-17-import type { GuestRateLimiter } from "./guest-rate-limiter.js";
src\modules\subject\application\guest-submission-service.ts-18-import { MAX_UPLOAD_BYTES } from "../../document/application/upload-validation.js";
--
src\modules\subject\application\guest-submission-service.ts-74-   * `resolveInternalUserEmail` in `document-chasing-dispatch`'s dispatch.ts, deliberately NOT a
src\modules\subject\application\guest-submission-service.ts-75-   * direct identity-module import (composition-root wiring, not a cross-module dependency). */
src\modules\subject\application\guest-submission-service.ts-76-  resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
src\modules\subject\application\guest-submission-service.ts-77-  now?: () => string;
src\modules\subject\application\guest-submission-service.ts-78-}
src\modules\subject\application\guest-submission-service.ts-79-
src\modules\subject\application\guest-submission-service.ts-80-interface ResolvedToken {
src\modules\subject\application\guest-submission-service.ts:81:  pointer: GuestTokenPointer;
src\modules\subject\application\guest-submission-service.ts-82-  request: DocumentRequest;
src\modules\subject\application\guest-submission-service.ts-83-}
src\modules\subject\application\guest-submission-service.ts-84-
src\modules\subject\application\guest-submission-service.ts-85-export class GuestSubmissionService {
src\modules\subject\application\guest-submission-service.ts-86-  private readonly store: SubjectStore;
src\modules\subject\application\guest-submission-service.ts-87-  private readonly tableName: string;
src\modules\subject\application\guest-submission-service.ts-88-  private readonly quarantineBucket: string;
--
src\modules\subject\application\guest-submission-service.ts-226-    // abaixo, nunca 429 diferenciado nesta superfÃ­cie pÃºblica.
src\modules\subject\application\guest-submission-service.ts-227-    try {
src\modules\subject\application\guest-submission-service.ts-228-      await this.rateLimiter.consume({ selectorHash, limit: 30, windowSeconds: 60 });
src\modules\subject\application\guest-submission-service.ts-229-    } catch {
src\modules\subject\application\guest-submission-service.ts-230-      throw new GuestTokenInvalidError();
src\modules\subject\application\guest-submission-service.ts-231-    }
src\modules\subject\application\guest-submission-service.ts-232-
src\modules\subject\application\guest-submission-service.ts:233:    const pointer = await this.store.get<GuestTokenPointer>(guestTokenPointerKey(selectorHash));
src\modules\subject\application\guest-submission-service.ts-234-
src\modules\subject\application\guest-submission-service.ts-235-    // Caminho dummy anti-timing (achado real de revisÃ£o adversarial): mesmo quando o ponteiro
src\modules\subject\application\guest-submission-service.ts-236-    // nÃ£o existe, ainda computamos um hash e uma comparaÃ§Ã£o timingSafeEqual equivalente, para
src\modules\subject\application\guest-submission-service.ts-237-    // que o tempo de resposta nÃ£o dependa de o selector existir ou nÃ£o. O hash dummy nunca
src\modules\subject\application\guest-submission-service.ts-238-    // pode colidir com um secretHash real (prefixo fixo que issueGuestToken() nunca produz).
src\modules\subject\application\guest-submission-service.ts-239-    const targetSecretHash = pointer?.secretHash ?? hmacGuestTokenCrypto.hash(this.pepper, `dummy:${selectorHash}`);
src\modules\subject\application\guest-submission-service.ts-240-    const secretOk = secretMatches(this.pepper, parsed.secret, targetSecretHash);
--
src\modules\extraction\application\complete-ocr.ts-1-/**
src\modules\extraction\application\complete-ocr.ts-2- * `TextractTaskHandler`'s `COMPLETE_OCR` operation â€” triggered by the SQS queue subscribed to
src\modules\extraction\application\complete-ocr.ts-3- * Textract's SNS job-completion topic, NOT by Step Functions directly (`claude-reconciliation-
src\modules\extraction\application\complete-ocr.ts:4: * final-design.md` Â§1.2/Â§2/Â§3). Looks up the `TextractJob` by `jobId`, pages
src\modules\extraction\application\complete-ocr.ts-5- * `GetDocumentTextDetection`, persists the OCR artifact, and resolves the parked
src\modules\extraction\application\complete-ocr.ts-6- * `waitForTaskToken` task via `SendTaskSuccess`/`SendTaskFailure`.
src\modules\extraction\application\complete-ocr.ts-7- *
src\modules\extraction\application\complete-ocr.ts-8- * Governing invariant (design Â§3, closed after 3 correction rounds): this function must NEVER
src\modules\extraction\application\complete-ocr.ts-9- * delete the transient OCR artifact, in any outcome â€” only `ExtractionValidationTaskHandler`
src\modules\extraction\application\complete-ocr.ts-10- * (a later handler, not yet implemented) does that, at run closure. `taskTokenCiphertext` is
src\modules\extraction\application\complete-ocr.ts-11- * cleared only on a TERMINAL SendTask* outcome (success, or the three terminal errors); any
src\modules\extraction\application\complete-ocr.ts-12- * other SendTask* error is rethrown so the SQS message redelivers with the token still intact.
src\modules\extraction\application\complete-ocr.ts-13- */
src\modules\extraction\application\complete-ocr.ts:14:import { textractJobKey, type TextractJob } from "../domain/textract-job.js";
src\modules\extraction\application\complete-ocr.ts-15-import { TextractPartialFailureError } from "../../../shared/errors/app-error.js";
src\modules\extraction\application\complete-ocr.ts:16:import type { TextractClient, TextractJobStatusResult } from "../ports/textract-client.js";
src\modules\extraction\application\complete-ocr.ts:17:import type { TextractJobStore } from "../ports/textract-job-store.js";
src\modules\extraction\application\complete-ocr.ts-18-import type { OcrArtifactStore, ExtractionArtifactRef } from "../ports/ocr-artifact-store.js";
src\modules\extraction\application\complete-ocr.ts-19-import type { TaskTokenEncryptor } from "../ports/task-token-encryptor.js";
src\modules\extraction\application\complete-ocr.ts-20-import type { TaskTokenSender } from "../ports/task-token-sender.js";
src\modules\extraction\application\complete-ocr.ts-21-
src\modules\extraction\application\complete-ocr.ts-22-export interface CompleteOcrDeps {
src\modules\extraction\application\complete-ocr.ts-23-  textract: TextractClient;
src\modules\extraction\application\complete-ocr.ts:24:  jobs: TextractJobStore;
src\modules\extraction\application\complete-ocr.ts-25-  artifacts: OcrArtifactStore;
src\modules\extraction\application\complete-ocr.ts-26-  tokenEncryptor: TaskTokenEncryptor;
src\modules\extraction\application\complete-ocr.ts-27-  sender: TaskTokenSender;
src\modules\extraction\application\complete-ocr.ts-28-  now?: () => string;
src\modules\extraction\application\complete-ocr.ts-29-}
src\modules\extraction\application\complete-ocr.ts-30-
src\modules\extraction\application\complete-ocr.ts-31-export interface CompleteOcrInput {
--
src\modules\extraction\application\complete-ocr.ts-35-export type CompleteOcrOutcome =
src\modules\extraction\application\complete-ocr.ts-36-  | "SUCCEEDED"
src\modules\extraction\application\complete-ocr.ts-37-  | "PARTIAL_SUCCEEDED"
src\modules\extraction\application\complete-ocr.ts-38-  | "FAILED_REPORTED"
src\modules\extraction\application\complete-ocr.ts-39-  | "ORPHAN_JOB"
src\modules\extraction\application\complete-ocr.ts-40-  | "ALREADY_FINALIZED";
src\modules\extraction\application\complete-ocr.ts-41-
src\modules\extraction\application\complete-ocr.ts:42:async function collectPages(textract: TextractClient, jobId: string): Promise<{ status: TextractJobStatusResult; blocks: unknown[]; warnings: string[] }> {
src\modules\extraction\application\complete-ocr.ts-43-  const blocks: unknown[] = [];
src\modules\extraction\application\complete-ocr.ts-44-  const warnings = new Set<string>();
src\modules\extraction\application\complete-ocr.ts:45:  let status: TextractJobStatusResult = "IN_PROGRESS";
src\modules\extraction\application\complete-ocr.ts-46-  let nextToken: string | undefined;
src\modules\extraction\application\complete-ocr.ts-47-  do {
src\modules\extraction\application\complete-ocr.ts-48-    const page = await textract.getDocumentTextDetectionPage(jobId, nextToken);
src\modules\extraction\application\complete-ocr.ts-49-    status = page.status;
src\modules\extraction\application\complete-ocr.ts-50-    blocks.push(...page.blocks);
src\modules\extraction\application\complete-ocr.ts-51-    for (const w of page.warnings ?? []) warnings.add(w);
src\modules\extraction\application\complete-ocr.ts-52-    nextToken = page.nextToken;
--
src\modules\extraction\application\complete-ocr.ts-117-  // is NOT caught here â€” it propagates to the SQS consumer for redelivery, taskTokenCiphertext
src\modules\extraction\application\complete-ocr.ts-118-  // left untouched, exactly per design Â§3's rodada-7 correction.
src\modules\extraction\application\complete-ocr.ts-119-
src\modules\extraction\application\complete-ocr.ts-120-  // Every outcome reachable here (SENT, TERMINAL_QUIET, TERMINAL_WARN_INVALID_TOKEN) is terminal
src\modules\extraction\application\complete-ocr.ts-121-  // for this token â€” clear it. A lost OCC race on the update means a concurrent delivery already
src\modules\extraction\application\complete-ocr.ts-122-  // did this; treated as success, not retried.
src\modules\extraction\application\complete-ocr.ts-123-  const now = deps.now?.() ?? new Date().toISOString();
src\modules\extraction\application\complete-ocr.ts:124:  const updated: TextractJob = {
src\modules\extraction\application\complete-ocr.ts-125-    ...job,
src\modules\extraction\application\complete-ocr.ts-126-    ...textractJobKey(job.jobId),
src\modules\extraction\application\complete-ocr.ts-127-    status: sendSuccess ? "COMPLETED" : "FAILED",
src\modules\extraction\application\complete-ocr.ts-128-    taskTokenCiphertext: undefined,
src\modules\extraction\application\complete-ocr.ts-129-    version: job.version + 1,
src\modules\extraction\application\complete-ocr.ts-130-    updatedAt: now,
src\modules\extraction\application\complete-ocr.ts-131-  };
--
src\modules\subject\application\guest-rate-limiter.ts-8- */
src\modules\subject\application\guest-rate-limiter.ts-9-import { QuotaExceededError } from "../../../shared/errors/app-error.js";
src\modules\subject\application\guest-rate-limiter.ts-10-import { epochSecondsFromIso } from "../domain/guest-token.js";
src\modules\subject\application\guest-rate-limiter.ts-11-import type { EntityKey, SubjectStore } from "../ports/subject-store.js";
src\modules\subject\application\guest-rate-limiter.ts-12-
src\modules\subject\application\guest-rate-limiter.ts-13-export interface GuestRateLimitRecord extends EntityKey {
src\modules\subject\application\guest-rate-limiter.ts-14-  SK: "RATE";
src\modules\subject\application\guest-rate-limiter.ts:15:  entityType: "GuestTokenRateLimit";
src\modules\subject\application\guest-rate-limiter.ts-16-  selectorHash: string;
src\modules\subject\application\guest-rate-limiter.ts-17-  limit: number;
src\modules\subject\application\guest-rate-limiter.ts-18-  windowSeconds: number;
src\modules\subject\application\guest-rate-limiter.ts-19-  count: number;
src\modules\subject\application\guest-rate-limiter.ts-20-  resetAt: string;
src\modules\subject\application\guest-rate-limiter.ts:21:  /** TTL fÃ­sico real da tabela â€” achado real de D-047/D-048, mesmo motivo de `GuestTokenPointer.purgeAfterTtl`: `resetAt` sozinho nunca aciona a exclusÃ£o fÃ­sica do DynamoDB. AvanÃ§a a cada escrita para acompanhar `resetAt`. */
src\modules\subject\application\guest-rate-limiter.ts-22-  purgeAfterTtl: number;
src\modules\subject\application\guest-rate-limiter.ts-23-}
src\modules\subject\application\guest-rate-limiter.ts-24-
src\modules\subject\application\guest-rate-limiter.ts-25-function guestRateLimitKey(selectorHash: string): { PK: string; SK: "RATE" } {
src\modules\subject\application\guest-rate-limiter.ts-26-  return { PK: `GUESTTOKEN#${selectorHash}#RATE`, SK: "RATE" };
src\modules\subject\application\guest-rate-limiter.ts-27-}
src\modules\subject\application\guest-rate-limiter.ts-28-
--
src\modules\subject\application\guest-rate-limiter.ts-41-      const nowIso = this.now();
src\modules\subject\application\guest-rate-limiter.ts-42-      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();
src\modules\subject\application\guest-rate-limiter.ts-43-
src\modules\subject\application\guest-rate-limiter.ts-44-      const existing = await this.store.get<GuestRateLimitRecord>(key);
src\modules\subject\application\guest-rate-limiter.ts-45-      if (!existing) {
src\modules\subject\application\guest-rate-limiter.ts-46-        const created = await this.store.putIfAbsent<GuestRateLimitRecord>({
src\modules\subject\application\guest-rate-limiter.ts-47-          ...key,
src\modules\subject\application\guest-rate-limiter.ts:48:          entityType: "GuestTokenRateLimit",
src\modules\subject\application\guest-rate-limiter.ts-49-          selectorHash: input.selectorHash,
src\modules\subject\application\guest-rate-limiter.ts-50-          limit: input.limit,
src\modules\subject\application\guest-rate-limiter.ts-51-          windowSeconds: input.windowSeconds,
src\modules\subject\application\guest-rate-limiter.ts-52-          count: 1,
src\modules\subject\application\guest-rate-limiter.ts-53-          resetAt,
src\modules\subject\application\guest-rate-limiter.ts-54-          purgeAfterTtl: epochSecondsFromIso(resetAt),
src\modules\subject\application\guest-rate-limiter.ts-55-        });
--
src\modules\extraction\composition\extraction.ts-8-import type { S3Client } from "@aws-sdk/client-s3";
src\modules\extraction\composition\extraction.ts-9-import type { SFNClient } from "@aws-sdk/client-sfn";
src\modules\extraction\composition\extraction.ts-10-import { KMSClient } from "@aws-sdk/client-kms";
src\modules\extraction\composition\extraction.ts-11-import { AppConfigDataClient } from "@aws-sdk/client-appconfigdata";
src\modules\extraction\composition\extraction.ts-12-import { TextractClient as AwsTextractClient } from "@aws-sdk/client-textract";
src\modules\extraction\composition\extraction.ts-13-import { DynamoDbIdentityStore } from "../../identity/persistence/dynamodb-identity-store.js";
src\modules\extraction\composition\extraction.ts-14-import { TenantQuotaService } from "../../identity/application/quota.js";
src\modules\extraction\composition\extraction.ts:15:import { DynamoDbTextractJobStore } from "../persistence/dynamodb-textract-job-store.js";
src\modules\extraction\composition\extraction.ts-16-import { TextractSdkClient } from "../persistence/textract-sdk-client.js";
src\modules\extraction\composition\extraction.ts-17-import { S3OcrArtifactStore } from "../persistence/s3-ocr-artifact-store.js";
src\modules\extraction\composition\extraction.ts-18-import { KmsTaskTokenEncryptor } from "../persistence/kms-task-token-encryptor.js";
src\modules\extraction\composition\extraction.ts-19-import { SfnTaskTokenSender } from "../persistence/sfn-task-token-sender.js";
src\modules\extraction\composition\extraction.ts-20-import { AppConfigFeatureFlagsReader } from "../persistence/appconfig-feature-flags-reader.js";
src\modules\extraction\composition\extraction.ts-21-import { BedrockRuntimeClient } from "@aws-sdk/client-bedrock-runtime";
src\modules\extraction\composition\extraction.ts-22-import type { StartOcrDeps } from "../application/start-ocr.js";
--
src\modules\extraction\composition\extraction.ts-53-    appConfigData: AppConfigDataClient;
src\modules\extraction\composition\extraction.ts-54-    textract: AwsTextractClient;
src\modules\extraction\composition\extraction.ts-55-  },
src\modules\extraction\composition\extraction.ts-56-  config: TextractTaskWorkerConfig,
src\modules\extraction\composition\extraction.ts-57-): TextractTaskWorkerDeps {
src\modules\extraction\composition\extraction.ts-58-  const identityStore = new DynamoDbIdentityStore(clients.dynamo, config.tableName);
src\modules\extraction\composition\extraction.ts-59-  const quota = new TenantQuotaService(identityStore);
src\modules\extraction\composition\extraction.ts:60:  const jobs = new DynamoDbTextractJobStore(clients.dynamo, config.tableName);
src\modules\extraction\composition\extraction.ts-61-  const textract = new TextractSdkClient(clients.textract);
src\modules\extraction\composition\extraction.ts-62-  const artifacts = new S3OcrArtifactStore(clients.s3, config.extractionTransientBucket);
src\modules\extraction\composition\extraction.ts-63-  const tokenEncryptor = new KmsTaskTokenEncryptor(clients.kms, config.taskTokenKmsKeyId);
src\modules\extraction\composition\extraction.ts-64-  const sender = new SfnTaskTokenSender(clients.sfn);
src\modules\extraction\composition\extraction.ts-65-  const featureFlags = new AppConfigFeatureFlagsReader(clients.appConfigData, config.appConfig);
src\modules\extraction\composition\extraction.ts-66-
src\modules\extraction\composition\extraction.ts-67-  return {
--
src\modules\reminder\application\reminder-policy-service.ts-232-        extra: { tenantId: input.tenantId, status: "ACTIVE" },
src\modules\reminder\application\reminder-policy-service.ts-233-      }),
src\modules\reminder\application\reminder-policy-service.ts-234-    );
src\modules\reminder\application\reminder-policy-service.ts-235-    if (!input.skipPointerWrite) {
src\modules\reminder\application\reminder-policy-service.ts-236-      entries.push({
src\modules\reminder\application\reminder-policy-service.ts-237-        Put: {
src\modules\reminder\application\reminder-policy-service.ts-238-          TableName: this.tableName,
src\modules\reminder\application\reminder-policy-service.ts:239:          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
src\modules\reminder\application\reminder-policy-service.ts-240-          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\reminder\application\reminder-policy-service.ts-241-        },
src\modules\reminder\application\reminder-policy-service.ts-242-      });
src\modules\reminder\application\reminder-policy-service.ts-243-    }
src\modules\reminder\application\reminder-policy-service.ts-244-  }
src\modules\reminder\application\reminder-policy-service.ts-245-
src\modules\reminder\application\reminder-policy-service.ts-246-  private appendPolicyChangedEvent(
--
src\modules\notification\application\notification-router-workflow.ts-319-  return { kind: "ROUTED", routedChannels: decision.routedChannels };
src\modules\notification\application\notification-router-workflow.ts-320-}
src\modules\notification\application\notification-router-workflow.ts-321-
src\modules\notification\application\notification-router-workflow.ts-322-function monthShard(isoTimestamp: string): string {
src\modules\notification\application\notification-router-workflow.ts-323-  return isoTimestamp.slice(0, 7).replace("-", "");
src\modules\notification\application\notification-router-workflow.ts-324-}
src\modules\notification\application\notification-router-workflow.ts-325-
src\modules\notification\application\notification-router-workflow.ts:326:/** Builds the OutboxEvent record for the notification.email-deliver.v1 command, same shape
src\modules\notification\application\notification-router-workflow.ts-327- * shared/outbox/outbox.ts's buildOutboxRecord produces, with `destination:
src\modules\notification\application\notification-router-workflow.ts-328- * "SQS_NOTIFICATION_EMAIL_V1"` (fechamento Â§7 do design) - inlined here (not via
src\modules\notification\application\notification-router-workflow.ts-329- * appendToTransaction, which takes a DomainEvent) because the payload IS the SQS command
src\modules\notification\application\notification-router-workflow.ts-330- * envelope directly, matching the same pattern reminder-producer.ts uses for its own
src\modules\notification\application\notification-router-workflow.ts-331- * dispatch command outbox record. */
src\modules\notification\application\notification-router-workflow.ts-332-function buildEmailOutboxRecord(intent: NotificationIntent, attempt: NotificationAttempt, deliverNotBefore: string | undefined, now: string): Record<string, unknown> {
src\modules\notification\application\notification-router-workflow.ts-333-  const shard = monthShard(now);
src\modules\notification\application\notification-router-workflow.ts-334-  const eventId = attempt.attemptId;
src\modules\notification\application\notification-router-workflow.ts-335-  return {
src\modules\notification\application\notification-router-workflow.ts-336-    PK: `TENANT#${intent.tenantId}#OUTBOX#${shard}`,
src\modules\notification\application\notification-router-workflow.ts-337-    SK: `EVENT#${now}#${eventId}`,
src\modules\notification\application\notification-router-workflow.ts:338:    entityType: "OutboxEvent",
src\modules\notification\application\notification-router-workflow.ts-339-    // BLOCKER-B added tenantId as an explicit OutboxRecord field (previously only embedded
src\modules\notification\application\notification-router-workflow.ts-340-    // in PK) for a different destination's sender to use - added here too for the same
src\modules\notification\application\notification-router-workflow.ts:341:    // reason (and so every real OutboxEvent row is consistently shaped), even though this
src\modules\notification\application\notification-router-workflow.ts-342-    // destination's own payload already self-describes its tenant.
src\modules\notification\application\notification-router-workflow.ts-343-    tenantId: intent.tenantId,
src\modules\notification\application\notification-router-workflow.ts-344-    eventId,
src\modules\notification\application\notification-router-workflow.ts-345-    eventType: "notification.email-deliver.v1",
src\modules\notification\application\notification-router-workflow.ts-346-    aggregateType: "NotificationAttempt",
src\modules\notification\application\notification-router-workflow.ts-347-    aggregateId: attempt.attemptId,
src\modules\notification\application\notification-router-workflow.ts-348-    aggregateVersion: 1,
--
src\modules\extraction\application\start-ocr.ts-5- * `startExtractionRun` (hand-written fakes, no `vi.mock`). Deliberately never calls
src\modules\extraction\application\start-ocr.ts-6- * `SendTaskSuccess` â€” on a clean return the Step Functions execution stays parked on the task
src\modules\extraction\application\start-ocr.ts-7- * token until `completeOcr` (a LATER, separate SQS-triggered invocation) resolves it.
src\modules\extraction\application\start-ocr.ts-8- */
src\modules\extraction\application\start-ocr.ts-9-import { classifyDocumentType } from "../domain/document-classifier.js";
src\modules\extraction\application\start-ocr.ts-10-import {
src\modules\extraction\application\start-ocr.ts-11-  deriveTextractClientRequestToken,
src\modules\extraction\application\start-ocr.ts:12:  computeTextractJobTtl,
src\modules\extraction\application\start-ocr.ts-13-  textractJobKey,
src\modules\extraction\application\start-ocr.ts:14:  type TextractJob,
src\modules\extraction\application\start-ocr.ts-15-} from "../domain/textract-job.js";
src\modules\extraction\application\start-ocr.ts-16-import {
src\modules\extraction\application\start-ocr.ts-17-  OcrDisabledError,
src\modules\extraction\application\start-ocr.ts-18-  QuotaExceededError,
src\modules\extraction\application\start-ocr.ts:19:  TextractJobPersistenceFailedError,
src\modules\extraction\application\start-ocr.ts-20-  TextractUnsupportedDocumentError,
src\modules\extraction\application\start-ocr.ts-21-  UnsupportedDocumentTypeError,
src\modules\extraction\application\start-ocr.ts-22-} from "../../../shared/errors/app-error.js";
src\modules\extraction\application\start-ocr.ts-23-import type { FeatureFlagsReader } from "../ports/feature-flags-reader.js";
src\modules\extraction\application\start-ocr.ts-24-import type { TextractClient } from "../ports/textract-client.js";
src\modules\extraction\application\start-ocr.ts:25:import type { TextractJobStore } from "../ports/textract-job-store.js";
src\modules\extraction\application\start-ocr.ts-26-import type { TaskTokenEncryptor } from "../ports/task-token-encryptor.js";
src\modules\extraction\application\start-ocr.ts-27-import { TenantQuotaService } from "../../identity/application/quota.js";
src\modules\extraction\application\start-ocr.ts-28-
src\modules\extraction\application\start-ocr.ts-29-export interface StartOcrDeps {
src\modules\extraction\application\start-ocr.ts-30-  featureFlags: FeatureFlagsReader;
src\modules\extraction\application\start-ocr.ts-31-  quota: TenantQuotaService;
src\modules\extraction\application\start-ocr.ts-32-  textract: TextractClient;
src\modules\extraction\application\start-ocr.ts:33:  jobs: TextractJobStore;
src\modules\extraction\application\start-ocr.ts-34-  tokenEncryptor: TaskTokenEncryptor;
src\modules\extraction\application\start-ocr.ts-35-  snsTopicArn: string;
src\modules\extraction\application\start-ocr.ts-36-  snsRoleArn: string;
src\modules\extraction\application\start-ocr.ts:37:  /** Bounded local retry before propagating `TextractJobPersistenceFailedError`, per design Â§2
src\modules\extraction\application\start-ocr.ts-38-   * ("RecuperaÃ§Ã£o do intervalo StartDocumentTextDetection -> persistÃªncia"). */
src\modules\extraction\application\start-ocr.ts-39-  jobPersistAttempts?: number;
src\modules\extraction\application\start-ocr.ts-40-  now?: () => string;
src\modules\extraction\application\start-ocr.ts-41-}
src\modules\extraction\application\start-ocr.ts-42-
src\modules\extraction\application\start-ocr.ts-43-export interface StartOcrInput {
src\modules\extraction\application\start-ocr.ts-44-  taskToken: string;
--
src\modules\extraction\application\start-ocr.ts-107-    // design Â§1.8 before propagating.
src\modules\extraction\application\start-ocr.ts-108-    await deps.quota.release({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
src\modules\extraction\application\start-ocr.ts-109-    throw new TextractUnsupportedDocumentError(`StartDocumentTextDetection failed for document ${input.documentId}.`, { documentId: input.documentId, cause: err instanceof Error ? err.message : String(err) });
src\modules\extraction\application\start-ocr.ts-110-  }
src\modules\extraction\application\start-ocr.ts-111-
src\modules\extraction\application\start-ocr.ts-112-  const now = deps.now?.() ?? new Date().toISOString();
src\modules\extraction\application\start-ocr.ts-113-  const taskTokenCiphertext = await deps.tokenEncryptor.encrypt(input.taskToken);
src\modules\extraction\application\start-ocr.ts:114:  const job: TextractJob = {
src\modules\extraction\application\start-ocr.ts-115-    ...textractJobKey(jobId),
src\modules\extraction\application\start-ocr.ts:116:    entityType: "TextractJob",
src\modules\extraction\application\start-ocr.ts-117-    jobId,
src\modules\extraction\application\start-ocr.ts-118-    tenantId: input.tenantId,
src\modules\extraction\application\start-ocr.ts-119-    itemId: input.itemId,
src\modules\extraction\application\start-ocr.ts-120-    documentId: input.documentId,
src\modules\extraction\application\start-ocr.ts-121-    documentVersion: input.documentVersion,
src\modules\extraction\application\start-ocr.ts-122-    runId: input.runId,
src\modules\extraction\application\start-ocr.ts-123-    pipelineVersion: input.pipelineVersion,
src\modules\extraction\application\start-ocr.ts-124-    clientRequestToken,
src\modules\extraction\application\start-ocr.ts-125-    status: "STARTED",
src\modules\extraction\application\start-ocr.ts-126-    taskTokenCiphertext,
src\modules\extraction\application\start-ocr.ts:127:    ttl: computeTextractJobTtl(now),
src\modules\extraction\application\start-ocr.ts-128-    version: 1,
src\modules\extraction\application\start-ocr.ts-129-    createdAt: now,
src\modules\extraction\application\start-ocr.ts-130-    updatedAt: now,
src\modules\extraction\application\start-ocr.ts-131-  };
src\modules\extraction\application\start-ocr.ts-132-
src\modules\extraction\application\start-ocr.ts-133-  const attempts = Math.max(1, deps.jobPersistAttempts ?? 2);
src\modules\extraction\application\start-ocr.ts-134-  let lastErr: unknown;
--
src\modules\extraction\application\start-ocr.ts-136-    try {
src\modules\extraction\application\start-ocr.ts-137-      await deps.jobs.create(job);
src\modules\extraction\application\start-ocr.ts-138-      return; // deliberately no SendTaskSuccess here â€” see module doc comment.
src\modules\extraction\application\start-ocr.ts-139-    } catch (err) {
src\modules\extraction\application\start-ocr.ts-140-      lastErr = err;
src\modules\extraction\application\start-ocr.ts-141-    }
src\modules\extraction\application\start-ocr.ts-142-  }
src\modules\extraction\application\start-ocr.ts:143:  throw new TextractJobPersistenceFailedError(`Failed to persist TextractJob ${jobId} after ${attempts} attempt(s).`, {
src\modules\extraction\application\start-ocr.ts-144-    jobId,
src\modules\extraction\application\start-ocr.ts-145-    runId: input.runId,
src\modules\extraction\application\start-ocr.ts-146-    cause: lastErr instanceof Error ? lastErr.message : String(lastErr),
src\modules\extraction\application\start-ocr.ts-147-  });
src\modules\extraction\application\start-ocr.ts-148-}
--
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:1:/** Real DynamoDB adapter for `TextractJobStore`. Uses the shared single table (main
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:2: * `TABLE_NAME`) â€” `TextractJob` is keyed by `jobId` alone (see domain/textract-job.ts's own
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-3- * doc comment on why), never tenant-scoped, so this adapter never needs a tenant argument. */
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-4-import { PutCommand, GetCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-5-import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-6-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:7:import type { TextractJobStore } from "../ports/textract-job-store.js";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:8:import { textractJobKey, type TextractJob } from "../domain/textract-job.js";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-9-import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-10-
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:11:export class DynamoDbTextractJobStore implements TextractJobStore {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-12-  constructor(
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-13-    private readonly client: DynamoDBDocumentClient,
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-14-    private readonly tableName: string,
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-15-  ) {}
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-16-
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-17-  key(jobId: string): EntityKey {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-18-    return textractJobKey(jobId);
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-19-  }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-20-
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:21:  async create(job: TextractJob): Promise<void> {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-22-    try {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-23-      await this.client.send(
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-24-        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-25-          TableName: this.tableName,
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-26-          Item: job,
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-27-          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-28-        }),
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-29-      );
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-30-    } catch (err) {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:31:      throw mapDynamoError(err, "TextractJobStore.create");
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-32-    }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-33-  }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-34-
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:35:  async getByJobId(jobId: string): Promise<TextractJob | null> {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-36-    try {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-37-      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: this.key(jobId) }));
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:38:      return (result.Item as TextractJob | undefined) ?? null;
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-39-    } catch (err) {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:40:      throw mapDynamoError(err, "TextractJobStore.getByJobId");
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-41-    }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-42-  }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-43-
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-44-  /** No `tenantId` equality check here (unlike `buildVersionedUpdate`'s usual tenant-scoped
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:45:   * convention) â€” `TextractJob` is deliberately not tenant-scoped (see domain doc comment), so
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-46-   * the only fencing fact available is `version` itself. */
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:47:  async updateConditional(job: TextractJob, expected: { version: number }): Promise<boolean> {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-48-    try {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-49-      await this.client.send(
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-50-        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-51-          TableName: this.tableName,
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-52-          Key: this.key(job.jobId),
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-53-          ConditionExpression: "attribute_exists(PK) AND attribute_exists(SK) AND #version = :expectedVersion",
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-54-          UpdateExpression: job.taskTokenCiphertext === undefined ? "SET #status = :status, #updatedAt = :updatedAt, #version = :newVersion REMOVE #taskTokenCiphertext" : "SET #status = :status, #updatedAt = :updatedAt, #version = :newVersion, #taskTokenCiphertext = :taskTokenCiphertext",
--
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-66-            ...(job.taskTokenCiphertext === undefined ? {} : { ":taskTokenCiphertext": job.taskTokenCiphertext }),
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-67-          },
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-68-        }),
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-69-      );
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-70-      return true;
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-71-    } catch (err) {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-72-      if (isConditionalCheckFailed(err)) return false;
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:73:      throw mapDynamoError(err, "TextractJobStore.updateConditional");
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-74-    }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-75-  }
src\modules\extraction\persistence\dynamodb-textract-job-store.ts-76-}
--
src\modules\extraction\persistence\kms-task-token-encryptor.ts-1-/**
src\modules\extraction\persistence\kms-task-token-encryptor.ts:2: * Real KMS adapter for the Step Functions task-token ciphertext (`TextractJob.taskTokenCiphertext`).
src\modules\extraction\persistence\kms-task-token-encryptor.ts-3- * Structurally identical to `src/modules/bff/persistence/kms-token-encryptor.ts` (same
src\modules\extraction\persistence\kms-task-token-encryptor.ts-4- * envelope-encryption pattern, D-054), but declared/instantiated independently per the module
src\modules\extraction\persistence\kms-task-token-encryptor.ts-5- * boundary discipline documented in `ports/task-token-encryptor.ts` â€” extraction and bff stay
src\modules\extraction\persistence\kms-task-token-encryptor.ts-6- * independently deployable/testable, each against its OWN dedicated CMK (never sharing a key,
src\modules\extraction\persistence\kms-task-token-encryptor.ts-7- * so a compromise/rotation of one credential class never touches the other).
src\modules\extraction\persistence\kms-task-token-encryptor.ts-8- */
src\modules\extraction\persistence\kms-task-token-encryptor.ts-9-import { KMSClient, EncryptCommand, DecryptCommand } from "@aws-sdk/client-kms";
--
src\modules\extraction\domain\textract-job.ts-1-/**
src\modules\extraction\domain\textract-job.ts:2: * TextractJob â€” transient correlation record between a Step Functions `waitForTaskToken`
src\modules\extraction\domain\textract-job.ts-3- * invocation (`START_OCR`) and the eventual SNS->SQS completion notification (`COMPLETE_OCR`),
src\modules\extraction\domain\textract-job.ts-4- * per `claude-reconciliation-final-design.md` Â§1.2/Â§2/Â§3 (D-035, M7 runtime design, GATE 9,2/9,3).
src\modules\extraction\domain\textract-job.ts-5- *
src\modules\extraction\domain\textract-job.ts-6- * Keyed by `jobId` alone (not tenant-scoped PK/SK like every other entity in this codebase) â€”
src\modules\extraction\domain\textract-job.ts-7- * `COMPLETE_OCR` is triggered by a Textract SNS notification that only carries `JobId`/`JobTag`,
src\modules\extraction\domain\textract-job.ts-8- * with no tenant/document context available before this record is read. A tenant-scoped key
src\modules\extraction\domain\textract-job.ts-9- * would require a GSI just to look up "the job with this jobId", for a record whose entire
--
src\modules\extraction\domain\textract-job.ts-16- * logged, never included in a DLQ redrive payload. Â§3 requires it be cleared (not the whole
src\modules\extraction\domain\textract-job.ts-17- * record) on any terminal SendTaskSuccess/Failure outcome, and preserved on a transient
src\modules\extraction\domain\textract-job.ts-18- * SendTask* error so a later delivery attempt can still resolve the callback.
src\modules\extraction\domain\textract-job.ts-19- */
src\modules\extraction\domain\textract-job.ts-20-import { createHash } from "node:crypto";
src\modules\extraction\domain\textract-job.ts-21-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\extraction\domain\textract-job.ts-22-
src\modules\extraction\domain\textract-job.ts:23:export type TextractJobStatus = "STARTED" | "COMPLETED" | "FAILED";
src\modules\extraction\domain\textract-job.ts-24-
src\modules\extraction\domain\textract-job.ts:25:export interface TextractJob extends EntityKey {
src\modules\extraction\domain\textract-job.ts:26:  entityType: "TextractJob";
src\modules\extraction\domain\textract-job.ts-27-  jobId: string;
src\modules\extraction\domain\textract-job.ts-28-  tenantId: string;
src\modules\extraction\domain\textract-job.ts-29-  itemId: string;
src\modules\extraction\domain\textract-job.ts-30-  documentId: string;
src\modules\extraction\domain\textract-job.ts-31-  documentVersion: number;
src\modules\extraction\domain\textract-job.ts-32-  runId: string;
src\modules\extraction\domain\textract-job.ts-33-  /** Added for item 5 (`PdfParserTaskHandler`): `completeOcr`'s `SendTaskSuccess` payload
--
src\modules\extraction\domain\textract-job.ts-36-   * pipelineVersion/...) would otherwise be lost to every state after `RunTextract` on the
src\modules\extraction\domain\textract-job.ts-37-   * happy path. Carrying it here lets `completeOcr` re-attach that context to its own
src\modules\extraction\domain\textract-job.ts-38-   * payload. Not present in the design doc explicitly â€” recorded as a pending decision in
src\modules\extraction\domain\textract-job.ts-39-   * NEXT_SESSION_PROMPT.md (conservative, additive, no persisted-data migration concern:
src\modules\extraction\domain\textract-job.ts-40-   * this record's max lifetime is the 24h TTL, never real historical data yet). */
src\modules\extraction\domain\textract-job.ts-41-  pipelineVersion: string;
src\modules\extraction\domain\textract-job.ts-42-  clientRequestToken: string;
src\modules\extraction\domain\textract-job.ts:43:  status: TextractJobStatus;
src\modules\extraction\domain\textract-job.ts-44-  /** Present only while a callback might still need to resolve the Step Functions task.
src\modules\extraction\domain\textract-job.ts-45-   * Cleared (not the record) on any terminal COMPLETE_OCR outcome, per design Â§3. */
src\modules\extraction\domain\textract-job.ts-46-  taskTokenCiphertext?: string;
src\modules\extraction\domain\textract-job.ts-47-  /** Epoch seconds â€” short TTL, diagnÃ³stico/reconciliaÃ§Ã£o only (design Â§2/Â§3), never relied on
src\modules\extraction\domain\textract-job.ts-48-   * for correctness (the S3 24h lifecycle is the real safety net for the OCR artifact itself). */
src\modules\extraction\domain\textract-job.ts-49-  ttl: number;
src\modules\extraction\domain\textract-job.ts-50-  version: number;
--
src\modules\extraction\domain\textract-job.ts-69-  return createHash("sha256").update(`${tenantId}|${documentId}|${documentVersion}|${pipelineVersion}|${runId}`).digest("hex");
src\modules\extraction\domain\textract-job.ts-70-}
src\modules\extraction\domain\textract-job.ts-71-
src\modules\extraction\domain\textract-job.ts-72-/** Short TTL per design Â§2/Â§3 â€” 24h matches the EXTRACTION_TRANSIENT S3 safety net lifecycle,
src\modules\extraction\domain\textract-job.ts-73- * so the correlation record and the artifact it points at expire on the same horizon. */
src\modules\extraction\domain\textract-job.ts-74-export const TEXTRACT_JOB_TTL_SECONDS = 24 * 60 * 60;
src\modules\extraction\domain\textract-job.ts-75-
src\modules\extraction\domain\textract-job.ts:76:export function computeTextractJobTtl(nowIso: string): number {
src\modules\extraction\domain\textract-job.ts-77-  return Math.floor(Date.parse(nowIso) / 1000) + TEXTRACT_JOB_TTL_SECONDS;
src\modules\extraction\domain\textract-job.ts-78-}
--
src\modules\extraction\persistence\textract-sdk-client.ts-7-  GetDocumentTextDetectionCommand,
src\modules\extraction\persistence\textract-sdk-client.ts-8-} from "@aws-sdk/client-textract";
src\modules\extraction\persistence\textract-sdk-client.ts-9-import type {
src\modules\extraction\persistence\textract-sdk-client.ts-10-  TextractClient,
src\modules\extraction\persistence\textract-sdk-client.ts-11-  StartDocumentTextDetectionInput,
src\modules\extraction\persistence\textract-sdk-client.ts-12-  StartDocumentTextDetectionResult,
src\modules\extraction\persistence\textract-sdk-client.ts-13-  GetDocumentTextDetectionPage,
src\modules\extraction\persistence\textract-sdk-client.ts:14:  TextractJobStatusResult,
src\modules\extraction\persistence\textract-sdk-client.ts-15-} from "../ports/textract-client.js";
src\modules\extraction\persistence\textract-sdk-client.ts-16-
src\modules\extraction\persistence\textract-sdk-client.ts:17:function mapStatus(status: string | undefined): TextractJobStatusResult {
src\modules\extraction\persistence\textract-sdk-client.ts-18-  if (status === "SUCCEEDED" || status === "FAILED" || status === "PARTIAL_SUCCESS") return status;
src\modules\extraction\persistence\textract-sdk-client.ts-19-  return "IN_PROGRESS";
src\modules\extraction\persistence\textract-sdk-client.ts-20-}
src\modules\extraction\persistence\textract-sdk-client.ts-21-
src\modules\extraction\persistence\textract-sdk-client.ts-22-export class TextractSdkClient implements TextractClient {
src\modules\extraction\persistence\textract-sdk-client.ts-23-  constructor(private readonly client: AwsTextractClient) {}
src\modules\extraction\persistence\textract-sdk-client.ts-24-

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/reminder/application/reminder-policy-service.ts; Get-Content -Raw src/modules/identity/persistence/identity-mapping-repository.ts; Get-Content -Raw src/modules/subject/domain/guest-token.ts; Get-Content -Raw src/shared/idempotency/idempotency.ts; Get-Content -Raw src/workers/document-purge/purge.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 185ms:
/**
 * ReminderPolicyService Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§9.1's `ReminderPolicyService`
 * interface. Mirrors ExpirationService's shape: authorize() (action `reminder:manage`,
 * already declared in the M1 authorization matrix), OCC via shared/dynamodb/occ.ts,
 * strongly-consistent reads.
 *
 * BLOCKER-B (reminder-delivery-pipeline.md, Codex Round H APPROVED 9.2/10): policy writes
 * now do three things beyond the plain CRUD this file used to be limited to, all inside
 * the SAME TransactWriteItems as the policy row itself:
 *  1. ITEM-scoped policies condition-check that their target item exists, is ACTIVE, and
 *     belongs to the same tenant (Ã‚Â§5's ITEM policy integrity fix) - a policy can never
 *     reference a nonexistent/foreign/inactive item.
 *  2. The `POLICYREF#` discovery pointer under the item's own partition (Ã‚Â§5) is
 *     created/moved/removed to match the policy's current scope/itemId - discovery-only,
 *     never authoritative (the trigger worker always dereferences the real policy row).
 *  3. `reminder.policy-changed.v1` is appended to the transactional outbox (Ã‚Â§4) - the
 *     event that wakes the reminder-materialization-trigger worker. `previousItemId` is
 *     included only when a move/scope-change away from ITEM makes the OLD item's
 *     partition no longer discoverable via its pointer.
 */
import type { RequestContext } from "../../identity/domain/request-context.js";
import { authorize } from "../../identity/domain/authorization.js";
import { ConflictError, NotFoundError } from "../../../shared/errors/app-error.js";
import { buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
import { appendToTransaction } from "../../../shared/outbox/outbox.js";
import type { DomainEvent } from "../../../shared/contracts/events.js";
import { itemKey } from "../../expiration/domain/expiration-item.js";
import { policyKey, policyRefKey, validatePolicyScope, type ReminderPolicy, type PutPolicyInput } from "../domain/reminder-policy.js";
import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../ports/reminder-store.js";
import type { ReminderIdGenerator } from "./id-generator.js";

const POLICY_CHANGED = "reminder.policy-changed.v1";

export interface ReminderPolicyServiceDeps {
  store: ReminderStore;
  tableName: string;
  ids: ReminderIdGenerator;
  now?: () => string;
}

export class ReminderPolicyService {
  private readonly store: ReminderStore;
  private readonly tableName: string;
  private readonly ids: ReminderIdGenerator;
  private readonly now: () => string;

  constructor(deps: ReminderPolicyServiceDeps) {
    this.store = deps.store;
    this.tableName = deps.tableName;
    this.ids = deps.ids;
    this.now = deps.now ?? (() => new Date().toISOString());
  }

  async createPolicy(ctx: RequestContext, input: PutPolicyInput): Promise<ReminderPolicy> {
    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: ctx.tenant.tenantId } });
    validatePolicyScope(input);

    const tenantId = ctx.tenant.tenantId;
    const policyId = this.ids.newPolicyId();
    const now = this.now();
    const policy: ReminderPolicy = {
      ...policyKey(tenantId, policyId),
      entityType: "ReminderPolicy",
      policyId,
      tenantId,
      scope: input.scope,
      itemId: input.itemId,
      name: input.rule.name,
      triggers: input.rule.triggers,
      timeZone: input.rule.timeZone,
      quietHours: input.rule.quietHours,
      channels: input.rule.channels,
      optOutChannels: input.rule.optOutChannels,
      enabled: input.enabled ?? true,
      version: 1,
      createdAt: now,
      updatedAt: now,
    };

    const entries: TransactWriteEntry[] = [{ Put: buildVersionedCreate(this.tableName, policy as unknown as Record<string, unknown> & { PK: string; SK: string }) }];
    this.appendItemLinkage(entries, { tenantId, itemId: input.itemId, scope: input.scope, policyId });
    this.appendPolicyChangedEvent(entries, ctx, { policyId, itemId: input.scope === "ITEM" ? input.itemId : undefined }, 1);

    try {
      await this.store.transactWrite(entries);
    } catch (err) {
      if (isTransactionCanceled(err)) {
        throw new ConflictError("Unable to create policy - target item may not exist, be inactive, or belong to another tenant.", { policyId, itemId: input.itemId });
      }
      throw err;
    }
    return policy;
  }

  async getPolicy(ctx: RequestContext, policyId: string): Promise<ReminderPolicy> {
    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
    return policy;
  }

  async updatePolicy(
    ctx: RequestContext,
    policyId: string,
    input: PutPolicyInput,
    expectedVersion: number,
  ): Promise<ReminderPolicy> {
    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });
    validatePolicyScope(input);

    const tenantId = policy.tenantId;
    const set: Record<string, unknown> = {
      scope: input.scope,
      itemId: input.itemId,
      name: input.rule.name,
      triggers: input.rule.triggers,
      timeZone: input.rule.timeZone,
      quietHours: input.rule.quietHours,
      channels: input.rule.channels,
      optOutChannels: input.rule.optOutChannels,
      enabled: input.enabled ?? policy.enabled,
    };

    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: this.tableName,
          key: policyKey(tenantId, policyId),
          tenantId,
          expectedVersion,
          set,
        }),
      },
    ];

    // Ã‚Â§5's pointer-move invariant: remove the OLD pointer whenever the policy stops being
    // this exact ITEM's policy (scope left ITEM, or itemId changed) - BEFORE adding a new
    // one, so a move and a plain re-save of the same itemId are told apart correctly.
    const movedAwayFromItem = policy.scope === "ITEM" && (input.scope !== "ITEM" || input.itemId !== policy.itemId);
    if (movedAwayFromItem) {
      entries.push({ Delete: { TableName: this.tableName, Key: policyRefKey(tenantId, policy.itemId!, policyId) } });
    }
    // Only write a NEW pointer when the target item actually changed (or scope just became
    // ITEM) - re-Put-ing an unchanged pointer with attribute_not_exists would otherwise
    // fail its own condition every time an unrelated field (e.g. triggers) is edited.
    // Codex implementation-review finding (real defect): an earlier version passed
    // `itemId: undefined` for the unchanged-target case to suppress the pointer Put, but
    // that ALSO suppressed the item existence/ACTIVE/tenant ConditionCheck entirely - a
    // same-item policy edit could commit without ever re-asserting its target is still
    // valid, even though every OTHER ITEM-scoped write does. The two concerns (which
    // itemId's existence to assert vs. whether to also write a pointer for it) are now
    // separated: `itemId` always carries the real target when scope is ITEM;
    // `skipPointerWrite` alone controls the pointer.
    const needsNewPointer = input.scope === "ITEM" && (policy.scope !== "ITEM" || input.itemId !== policy.itemId);
    this.appendItemLinkage(entries, { tenantId, itemId: input.scope === "ITEM" ? input.itemId : undefined, scope: input.scope, policyId, skipPointerWrite: !needsNewPointer });

    this.appendPolicyChangedEvent(
      entries,
      ctx,
      {
        policyId,
        itemId: input.scope === "ITEM" ? input.itemId : undefined,
        previousItemId: movedAwayFromItem ? policy.itemId : undefined,
      },
      expectedVersion + 1,
    );

    try {
      await this.store.transactWrite(entries);
    } catch (err) {
      if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) {
        throw new ConflictError("VERSION_CONFLICT (or target item does not exist/is inactive/belongs to another tenant).", { policyId });
      }
      throw err;
    }

    return { ...policy, ...(set as Partial<ReminderPolicy>), version: expectedVersion + 1, updatedAt: this.now() };
  }

  async disablePolicy(ctx: RequestContext, policyId: string, expectedVersion: number): Promise<void> {
    const policy = await this.readActivePolicy(ctx.tenant.tenantId, policyId);
    authorize({ context: ctx, action: "reminder:manage", resource: { tenantId: policy.tenantId } });

    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: this.tableName,
          key: policyKey(policy.tenantId, policyId),
          tenantId: policy.tenantId,
          expectedVersion,
          set: { enabled: false },
        }),
      },
    ];
    // Pointer is intentionally left in place (Ã‚Â§5: "a disabled policy must still be
    // reachable so its occurrences get cancelled, not orphaned") - no pointer mutation here.
    this.appendPolicyChangedEvent(
      entries,
      ctx,
      { policyId, itemId: policy.scope === "ITEM" ? policy.itemId : undefined },
      expectedVersion + 1,
    );

    try {
      await this.store.transactWrite(entries);
    } catch (err) {
      if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) {
        throw new ConflictError("VERSION_CONFLICT", { policyId });
      }
      throw err;
    }
  }

  private async readActivePolicy(tenantId: string, policyId: string): Promise<ReminderPolicy> {
    const policy = await this.store.get<ReminderPolicy>(policyKey(tenantId, policyId));
    if (!policy || policy.deletedAt) {
      throw new NotFoundError("ReminderPolicy not found.", { policyId });
    }
    return policy;
  }

  /** Appends the ITEM-existence ConditionCheck + (unless `skipPointerWrite`) the new pointer Put, when `itemId` is present. */
  private appendItemLinkage(
    entries: TransactWriteEntry[],
    input: { tenantId: string; itemId: string | undefined; scope: PutPolicyInput["scope"]; policyId: string; skipPointerWrite?: boolean },
  ): void {
    if (input.scope !== "ITEM" || !input.itemId) return;
    entries.push(
      buildExistenceConditionCheck({
        tableName: this.tableName,
        key: itemKey(input.tenantId, input.itemId),
        extra: { tenantId: input.tenantId, status: "ACTIVE" },
      }),
    );
    if (!input.skipPointerWrite) {
      entries.push({
        Put: {
          TableName: this.tableName,
          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
        },
      });
    }
  }

  private appendPolicyChangedEvent(
    entries: TransactWriteEntry[],
    ctx: RequestContext,
    data: { policyId: string; itemId?: string; previousItemId?: string },
    aggregateVersion: number,
  ): void {
    const event: DomainEvent = {
      specVersion: "1.0",
      eventId: this.ids.newEventId(),
      eventType: POLICY_CHANGED,
      source: "expiration-tracker.reminder",
      occurredAt: this.now(),
      correlationId: ctx.correlationId,
      tenantId: ctx.tenant.tenantId,
      actor: { type: "USER", userId: ctx.principal.userId },
      aggregate: { type: "ReminderPolicy", id: data.policyId, version: aggregateVersion },
      // Explicit null rather than an omitted/undefined key for "not applicable" - matches
      // the ItemDueDateChanged convention (previousDueDate: null) and keeps the field
      // required-but-nullable in the schema instead of optional-and-possibly-undefined,
      // which DynamoDB marshalling and JSON Schema validation both handle more predictably.
      data: { policyId: data.policyId, itemId: data.itemId ?? null, previousItemId: data.previousItemId ?? null },
    };
    appendToTransaction(entries, this.tableName, event, "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1");
  }

  /** Marker so createPolicy can also build the first Put via the shared OCC builder if a caller needs the raw command (kept for symmetry/documentation). */
  static buildCreateCommand(tableName: string, policy: ReminderPolicy) {
    return buildVersionedCreate(tableName, policy as unknown as Record<string, unknown> & { PK: string; SK: string });
  }
}

/**
 * IdentityMapping repository Ã¢â‚¬â€ data-model.md Ã‚Â§2/Ã‚Â§"IdentityMapping" +
 * implementation-blueprint.md Ã‚Â§23.1/Ã‚Â§7.3: PK=IDENTITY#cognitoSub#<sub>, SK=MAP, created
 * atomically (ConditionExpression attribute_not_exists(PK)) on first login. This is one
 * of the two documented exceptions to the "every key starts with TENANT#tenantId" rule
 * (the other is GSI3) Ã¢â‚¬â€ the lookup happens *before* tenantId is known.
 */
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
import { InternalError } from "../../../shared/errors/app-error.js";

export interface IdentityMapping {
  PK: string;
  SK: "MAP";
  entityType: "IdentityMapping";
  cognitoSub: string;
  userId: string;
  tenantId: string;
  createdAt: string;
}

export function identityMappingKey(cognitoSub: string): EntityKey {
  return { PK: `IDENTITY#cognitoSub#${cognitoSub}`, SK: "MAP" };
}

export class IdentityMappingRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async find(cognitoSub: string): Promise<IdentityMapping | undefined> {
    return this.store.get<IdentityMapping>(identityMappingKey(cognitoSub));
  }

  /**
   * Atomically creates the mapping on first login. MVP: tenantId=userId (data-model.md
   * Ã‚Â§7.3 "No MVP, tenantId=userId, mas o cÃƒÂ³digo recebe ambos como conceitos separados"),
   * so newUserId is used for both Ã¢â‚¬â€ callers must never derive one from the other outside
   * this function once Organizations (FUT-001) exist.
   *
   * Idempotent under races: if two concurrent first-logins for the same sub both call
   * this, exactly one PutItem wins (attribute_not_exists(PK)); the loser re-reads and
   * returns the winner's mapping instead of erroring, so first-login is safe to retry.
   */
  async findOrCreate(cognitoSub: string, newUserId: string, newTenantId: string): Promise<IdentityMapping> {
    const existing = await this.find(cognitoSub);
    if (existing) {
      return existing;
    }

    const key = identityMappingKey(cognitoSub);
    const candidate: IdentityMapping = {
      ...key,
      SK: "MAP",
      entityType: "IdentityMapping",
      cognitoSub,
      userId: newUserId,
      tenantId: newTenantId,
      createdAt: this.now(),
    };

    const created = await this.store.putIfAbsent(candidate);
    if (created) {
      return candidate;
    }

    // Lost the race: someone else created it first-login-atomically between our
    // find() and putIfAbsent(). Re-read; if it's still missing something is badly
    // wrong (a delete of an IdentityMapping is not a supported operation).
    const winner = await this.find(cognitoSub);
    if (!winner) {
      throw new InternalError("IdentityMapping vanished after losing creation race.", { cognitoSub });
    }
    return winner;
  }
}

/**
 * Guest token Ã¢â‚¬â€ 04-domain-model-guest-upload.md (D-037). Token opaco `selector.secret`: o
 * `selector` ÃƒÂ© pÃƒÂºblico (usado sÃƒÂ³ para lookup), o `secret` sÃƒÂ³ ÃƒÂ© mostrado uma vez ao emitir o
 * link. Persistidos apenas os hashes (nunca o valor bruto), com pepper versionado.
 *
 * Lookup via item ponteiro na tabela base (`GUESTTOKEN#<selectorHash>`/`POINTER`) Ã¢â‚¬â€ MESMO
 * padrÃƒÂ£o de `IdentityMapping` (`src/modules/identity/persistence/identity-mapping-repository.ts`,
 * `IDENTITY#cognitoSub#<sub>`/`MAP`), nunca GSI novo. **Terceira exceÃƒÂ§ÃƒÂ£o tenantless
 * documentada** (depois de `IdentityMapping` e GSI3) Ã¢â‚¬â€ a chave nÃƒÂ£o pode comeÃƒÂ§ar com
 * `TENANT#tenantId` porque o lookup acontece ANTES de `tenantId` ser conhecido, mesmo motivo
 * estrutural das outras duas exceÃƒÂ§ÃƒÂµes.
 */
import { createHmac, randomBytes, timingSafeEqual } from "node:crypto";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";

export const GUEST_TOKEN_TTL_SECONDS = 14 * 24 * 60 * 60; // 14 dias (04-domain-model-guest-upload.md).

export interface GuestTokenPointer extends EntityKey {
  SK: "POINTER";
  entityType: "GuestTokenPointer";
  selectorHash: string;
  secretHash: string;
  tenantId: string;
  subjectId: string;
  assignmentId: string;
  documentRequestId: string;
  tokenVersion: number;
  expiresAt: string;
  /** TTL fÃƒÂ­sico real da tabela (`infra/modules/dynamo-table/main.tf`'s `ttl.attribute_name`) Ã¢â‚¬â€
   * achado real de D-047/D-048: `expiresAt` sozinho ÃƒÂ© sÃƒÂ³ um campo lido por `resolveToken()`,
   * nunca aciona a exclusÃƒÂ£o fÃƒÂ­sica do DynamoDB. Sem este campo, cada `GuestTokenPointer`
   * (inclusive cada rotaÃƒÂ§ÃƒÂ£o de chasing, D-048) seria uma linha permanente. */
  purgeAfterTtl: number;
  revokedAt?: string;
  createdAt: string;
  updatedAt: string;
  version: number;
}

export function guestTokenPointerKey(selectorHash: string): { PK: string; SK: "POINTER" } {
  return { PK: `GUESTTOKEN#${selectorHash}`, SK: "POINTER" };
}

/** DynamoDB TTL exige epoch seconds numÃƒÂ©rico, nunca a string ISO Ã¢â‚¬â€ achado real de D-048. */
export function epochSecondsFromIso(iso: string): number {
  return Math.floor(Date.parse(iso) / 1000);
}

/** Pepper vem de config/Secrets Manager no composition root (nunca hardcoded aqui) Ã¢â‚¬â€ este
 * mÃƒÂ³dulo sÃƒÂ³ implementa a mecÃƒÂ¢nica de hash/comparaÃƒÂ§ÃƒÂ£o, nunca decide onde o pepper vive. */
export interface GuestTokenCrypto {
  hash(pepper: string, value: string): string;
}

export const hmacGuestTokenCrypto: GuestTokenCrypto = {
  hash(pepper: string, value: string): string {
    return createHmac("sha256", pepper).update(value).digest("hex");
  },
};

export interface IssuedGuestToken {
  /** Valor completo a ser embutido no link enviado ao convidado Ã¢â‚¬â€ nunca persistido bruto. */
  token: string;
  selector: string;
  selectorHash: string;
  secretHash: string;
}

/** Gera um novo par selector.secret de alta entropia (256 bits cada). */
export function issueGuestToken(pepper: string, crypto: GuestTokenCrypto = hmacGuestTokenCrypto): IssuedGuestToken {
  const selector = randomBytes(16).toString("hex");
  const secret = randomBytes(32).toString("hex");
  return {
    token: `${selector}.${secret}`,
    selector,
    selectorHash: crypto.hash(pepper, selector),
    secretHash: crypto.hash(pepper, secret),
  };
}

export interface ParsedGuestToken {
  selector: string;
  secret: string;
}

/** Parse estrutural Ã¢â‚¬â€ nunca lanÃƒÂ§a, retorna undefined para qualquer formato inesperado (caminho dummy anti-enumeration). */
export function parseGuestToken(raw: string): ParsedGuestToken | undefined {
  const parts = raw.split(".");
  if (parts.length !== 2) return undefined;
  const [selector, secret] = parts;
  if (!selector || !secret || !/^[a-f0-9]{32}$/.test(selector) || !/^[a-f0-9]{64}$/.test(secret)) return undefined;
  return { selector, secret };
}

/** ComparaÃƒÂ§ÃƒÂ£o `timingSafeEqual` Ã¢â‚¬â€ nunca `===` em valor derivado de segredo (04-domain-model-guest-upload.md). */
export function secretMatches(pepper: string, secret: string, expectedSecretHash: string, crypto: GuestTokenCrypto = hmacGuestTokenCrypto): boolean {
  const actual = Buffer.from(crypto.hash(pepper, secret), "hex");
  const expected = Buffer.from(expectedSecretHash, "hex");
  if (actual.length !== expected.length) return false;
  return timingSafeEqual(actual, expected);
}

/**
 * Idempotency helper - implementation-blueprint.md #5.1 (IdempotencyPort) + data-model.md
 * Ã‚Â§4 (per-operation idempotency key table). Materializes keys with
 * `PutItem attribute_not_exists(PK)` as required by data-model.md Ã‚Â§4.
 *
 * Like occ.ts, this stays SDK-client-agnostic: callers inject a minimal `DynamoLike` port
 * (put/get) so the module is unit-testable in M0 without real DynamoDB, and M1 supplies
 * the real DocumentClient-backed adapter once the Idempotency table exists.
 */
import { InternalError } from "../errors/app-error.js";
import { isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";

export type IdempotencyResult = "ACQUIRED" | "COMPLETED_SAME_REQUEST";

export interface IdempotencyBeginInput {
  tenantId: string;
  operation: string;
  key: string;
  requestHash: string;
  expiresAt: string;
}

export interface IdempotencyCompleteInput {
  tenantId: string;
  operation: string;
  key: string;
  responseRef?: string;
}

interface IdempotencyRecord {
  PK: string;
  SK: string;
  entityType: "IdempotencyRecord";
  tenantId: string;
  operation: string;
  requestHash: string;
  status: "IN_PROGRESS" | "COMPLETED" | "ABORTED";
  responseRef?: string;
  expiresAt: string;
  createdAt: string;
  completedAt?: string;
}

export interface IdempotencyAbortInput {
  tenantId: string;
  operation: string;
  key: string;
}

/** Minimal DynamoDB surface this module needs - implemented against the real
 * DocumentClient in M1's persistence adapters. */
export interface DynamoLike {
  putIfAbsent(item: IdempotencyRecord): Promise<"PUT" | "ALREADY_EXISTS">;
  get(key: { PK: string; SK: string }): Promise<IdempotencyRecord | undefined>;
  update(item: IdempotencyRecord): Promise<void>;
  /**
   * Conditional replace: applies `item` only if the STORED record's status is still exactly
   * `expectedStatus` at write time. Real Codex Round B finding: begin()'s ABORTED-reacquisition
   * branch used to do a plain get() then unconditional update(), so two concurrent retries
   * could both observe ABORTED and both "win" the reacquisition, double-executing the guarded
   * operation - the exact failure mode this whole module exists to prevent. The identical
   * primitive also closes the same class of race in abort()'s IN_PROGRESS->ABORTED transition
   * (never flagged by Round B, but structurally the same TOCTOU window - a concurrent
   * complete() landing between abort()'s get() and update() would otherwise be silently
   * clobbered back to ABORTED). Returns false if the condition failed (another caller already
   * transitioned it first) - see transitionIdempotencyStatus() below for the shared
   * transactWrite-based implementation every adapter reuses.
   */
  transitionIfStatus(item: IdempotencyRecord, expectedStatus: IdempotencyRecord["status"]): Promise<boolean>;
}

export interface TransactWriteCapableStore {
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
}

/**
 * Shared DynamoLike.transitionIfStatus implementation, built from any store already exposing
 * transactWrite (every module wiring up an IdempotencyStore - expiration/document/import -
 * already has one for its own aggregate writes) - a single-entry conditional TransactWriteItems,
 * reused identically rather than each module hand-rolling the same ConditionExpression.
 */
export function transitionIdempotencyStatus(
  store: TransactWriteCapableStore,
  tableName: string,
  item: IdempotencyRecord,
  expectedStatus: IdempotencyRecord["status"],
): Promise<boolean> {
  const names: Record<string, string> = { "#status": "status", "#requestHash": "requestHash", "#responseRef": "responseRef", "#completedAt": "completedAt" };
  const values: Record<string, unknown> = { ":expected": expectedStatus, ":status": item.status, ":requestHash": item.requestHash };
  const setClauses = ["#status = :status", "#requestHash = :requestHash"];
  const removeClauses: string[] = [];

  if (item.responseRef !== undefined) {
    values[":responseRef"] = item.responseRef;
    setClauses.push("#responseRef = :responseRef");
  } else {
    removeClauses.push("#responseRef");
  }
  if (item.completedAt !== undefined) {
    values[":completedAt"] = item.completedAt;
    setClauses.push("#completedAt = :completedAt");
  } else {
    removeClauses.push("#completedAt");
  }

  const entries: TransactWriteEntry[] = [
    {
      Update: {
        TableName: tableName,
        Key: { PK: item.PK, SK: item.SK },
        UpdateExpression: removeClauses.length > 0 ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}` : `SET ${setClauses.join(", ")}`,
        ConditionExpression: "#status = :expected",
        ExpressionAttributeNames: names,
        ExpressionAttributeValues: values,
      },
    },
  ];

  return store
    .transactWrite(entries)
    .then(() => true)
    .catch((err) => {
      if (isTransactionCanceled(err)) return false;
      throw err;
    });
}

export function buildIdempotencyKey(tableName: string, tenantId: string, operation: string, key: string) {
  return {
    tableName,
    PK: `TENANT#${tenantId}#IDEMPOTENCY#${operation}`,
    SK: `KEY#${key}`,
  };
}

export class ConcurrentOperationError extends InternalError {
  constructor(operation: string, key: string) {
    super(`Idempotency key already in progress for a different request: ${operation}/${key}`, {
      operation,
      key,
    });
    this.name = "ConcurrentOperationError";
  }
}

export class IdempotencyStore {
  constructor(
    private readonly client: DynamoLike,
    private readonly tableName: string,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  /**
   * begin() implements IdempotencyPort.begin from implementation-blueprint.md #5.1:
   * - first caller for a key: ACQUIRED, proceed with the operation.
   * - a caller retrying the exact same request (same requestHash) after completion:
   *   COMPLETED_SAME_REQUEST, safe to return the cached result.
   * - a caller with a different requestHash for the same key while IN_PROGRESS or
   *   COMPLETED with a different hash: this is a genuine conflict (key reuse across
   *   different logical requests), surfaced as ConcurrentOperationError.
   */
  async begin(input: IdempotencyBeginInput): Promise<IdempotencyResult> {
    const { PK, SK } = buildIdempotencyKey(this.tableName, input.tenantId, input.operation, input.key);
    const record: IdempotencyRecord = {
      PK,
      SK,
      entityType: "IdempotencyRecord",
      tenantId: input.tenantId,
      operation: input.operation,
      requestHash: input.requestHash,
      status: "IN_PROGRESS",
      expiresAt: input.expiresAt,
      createdAt: this.now(),
    };

    const putResult = await this.client.putIfAbsent(record);
    if (putResult === "PUT") {
      return "ACQUIRED";
    }

    const existing = await this.client.get({ PK, SK });
    if (!existing) {
      // Extremely unlikely race (deleted between put-conflict and get) - treat as acquirable.
      return "ACQUIRED";
    }

    if (existing.status === "ABORTED") {
      // The previous holder of this key never completed its guarded operation - it failed for
      // a reason unrelated to "this exact request already succeeded" (e.g. an OCC version
      // conflict on the aggregate write, ExpirationService.renewItem calling abort() in its
      // catch block) and explicitly released the lock via abort(). The aborted record's stale
      // requestHash is irrelevant here - unlike a genuine key-reuse conflict (below), there is
      // no cached success this new attempt could collide with, so it would be safe to acquire
      // fresh regardless of whether the new hash matches the aborted one - PROVIDED the
      // reacquisition itself is atomic. A plain get()-then-update() here (as this used to do)
      // is a real TOCTOU race: two concurrent retries can both read ABORTED and both then
      // "win" an unconditional update(), double-executing the guarded operation - so
      // transitionIfStatus() below is a conditional write that only one concurrent caller can
      // win.
      const reacquired = await this.client.transitionIfStatus(
        { ...existing, status: "IN_PROGRESS", requestHash: input.requestHash, responseRef: undefined, completedAt: undefined },
        "ABORTED",
      );
      if (reacquired) return "ACQUIRED";
      // Lost the race - another concurrent caller reacquired (or has since completed) this key
      // first. Treated the same as any other concurrent collision below: the caller retries
      // later rather than risk double-executing the guarded operation.
      throw new ConcurrentOperationError(input.operation, input.key);
    }

    if (existing.requestHash !== input.requestHash) {
      throw new ConcurrentOperationError(input.operation, input.key);
    }

    if (existing.status === "COMPLETED") {
      return "COMPLETED_SAME_REQUEST";
    }

    // Same request hash, still IN_PROGRESS (e.g. concurrent SQS redelivery of the exact
    // same message before the first attempt finished). Caller should treat this as a
    // transient collision and retry later, not double-execute the side effect.
    throw new ConcurrentOperationError(input.operation, input.key);
  }

  async complete(input: IdempotencyCompleteInput): Promise<void> {
    const { PK, SK } = buildIdempotencyKey(this.tableName, input.tenantId, input.operation, input.key);
    const existing = await this.client.get({ PK, SK });
    if (!existing) {
      throw new InternalError(`Cannot complete unknown idempotency record: ${input.operation}/${input.key}`);
    }
    await this.client.update({
      ...existing,
      status: "COMPLETED",
      responseRef: input.responseRef,
      completedAt: this.now(),
    });
  }

  /**
   * Releases a lock this caller acquired via begin() but never completed, because the
   * guarded operation itself failed (mission's "idempotency liveness residual", docs/frontend
   * core-expiration-vertical-slice.md - discovered building Renew's OCC-conflict path: without
   * this, a version conflict on the aggregate write would leave the key permanently
   * IN_PROGRESS, and every retry - even with a freshly re-fetched expectedVersion - would hit
   * ConcurrentOperationError forever, since begin() never re-acquires an IN_PROGRESS record).
   * A no-op if the record is missing or already COMPLETED (a real cached success must never be
   * discarded just because a caller mistakenly aborts after the fact) - the transitionIfStatus()
   * condition (expects IN_PROGRESS) makes this atomic against a concurrent complete(), too: if
   * complete() lands between this method's get() and its conditional write, the write loses the
   * race and simply returns without effect, rather than clobbering a legitimate success back to
   * ABORTED.
   */
  async abort(input: IdempotencyAbortInput): Promise<void> {
    const { PK, SK } = buildIdempotencyKey(this.tableName, input.tenantId, input.operation, input.key);
    const existing = await this.client.get({ PK, SK });
    if (!existing || existing.status !== "IN_PROGRESS") return;
    await this.client.transitionIfStatus({ ...existing, status: "ABORTED" }, "IN_PROGRESS");
  }
}

/**
 * DocumentPurgeWorker Ã¢â‚¬â€ W3-06 (D-061), pure logic, clock-injected, same layout as
 * reminder-reconciliation/upload-slot-reconciliation. Physically purges `Document` rows
 * (`retentionClass: "USER_DOCUMENT"`) and their real S3 object once `purgeAfter` has passed,
 * and separately purges `DocumentPurgeReceipt` rows once THEIR `purgeAfter` (180 days,
 * DELIVERY_RECORD) has passed. Candidate batches (GSI6 query results) are supplied by the
 * caller, exactly like `reconcileExpiredClaims`/`reconcileDst` in
 * `workers/reminder-reconciliation/reconciliation.ts` Ã¢â‚¬â€ this module never touches GSI6 itself.
 *
 * Mechanism summary (full design + 6-round Claude<->Codex protocol:
 * `docs/architecture/reviews/w3-06-user-document-purge-design/`):
 *  - `Document` candidates go through a claim/lease (`WORKSTATE#PURGE_PENDING` ->
 *    `WORKSTATE#PURGE_CLAIMED`, 15min lease) BEFORE any S3 call, fenced by an OCC-conditioned
 *    `Update` checking version, `legalHold`, `status`, and the exact GSI6 pointer read Ã¢â‚¬â€ the
 *    claim itself is the fence against concurrency/hold/restoration, not a later best-effort
 *    recheck.
 *  - The real S3 object is `doc.cleanObject` (promoted documents) or
 *    `doc.uploadEvidence?.object ?? doc.malwareEvidence?.object` (rejected/unsupported
 *    documents that had real evidence) Ã¢â‚¬â€ NEVER `doc.quarantineObject`, whose `versionId` is
 *    always `""` (see `document-service.ts`/`advance-after-evidence.ts`). A document that never
 *    received any evidence has nothing to delete by version; the quarantine bucket's own
 *    lifecycle rule is the backstop for that case.
 *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
 *    the claimed version + `GSI6PK = WORKSTATE#PURGE_CLAIMED`) + `Put` a `DocumentPurgeReceipt`
 *    (non-sensitive proof of purge, `DELIVERY_RECORD` retention, own GSI6 pointer for
 *    self-purge 180 days later).
 *  - `DocumentPurgeReceipt` candidates have no external side effect to protect, so they are
 *    deleted directly from `PENDING` in a single conditioned transaction Ã¢â‚¬â€ no `CLAIMED` state.
 *  - Lease reconciliation (`WORKSTATE#PURGE_CLAIMED` past its lease) reverts to `PENDING` unless
 *    `purgeAttempts >= 5`, in which case the candidate is moved to a materialized terminal state
 *    (`purgeStatus: "STUCK"`, removed from both GSI6 worklists) instead of being reclaimed
 *    forever Ã¢â‚¬â€ a metric alone is not a durable record of a stuck candidate.
 *  - `legalHold`: no setter exists anywhere in this codebase yet. Any future one MUST include
 *    `attribute_not_exists(GSI6PK) OR GSI6PK <> "WORKSTATE#PURGE_CLAIMED"` in its own
 *    OCC-conditioned write (D-061, normative) Ã¢â‚¬â€ that is what makes hold and purge mutually
 *    exclusive by construction, not this worker's claim condition alone.
 */
import {
  buildVersionedCreate,
  buildVersionedDelete,
  buildVersionedUpdate,
  isTransactionCanceled,
} from "../../shared/dynamodb/occ.js";
import {
  GSI6PK_PURGE_CLAIMED,
  GSI6PK_PURGE_PENDING,
  buildDocumentPurgeClaimGsi6Sk,
  buildDocumentPurgeGsi6Sk,
  buildPurgeReceiptGsi6Sk,
  type DocumentStore,
} from "../../modules/document/ports/document-store.js";
import type { DocumentObjectStore } from "../../modules/document/ports/document-object-store.js";
import type { DocumentObjectReference } from "../../modules/document/domain/document-object-reference.js";
import { documentPurgeReceiptKey, type DocumentPurgeReceipt } from "../../modules/document/domain/document-purge-receipt.js";
import { computeDeliveryRecordPurgeAfter } from "../../modules/document/domain/retention.js";

export const PURGE_LEASE_MS = 15 * 60 * 1000;
export const MAX_PURGE_ATTEMPTS = 5;

export interface DocumentPurgeCandidate {
  entityType: "Document";
  PK: string;
  SK: string;
  tenantId: string;
  documentId: string;
  itemId: string;
  version: number;
  status: string;
  GSI6PK: string;
  GSI6SK: string;
  purgeAfter: string;
  legalHold?: boolean;
  purgeAttempts?: number;
  deletedAt?: string;
  cleanObject?: DocumentObjectReference;
  uploadEvidence?: { object: DocumentObjectReference };
  malwareEvidence?: { object: DocumentObjectReference };
}

export interface ReceiptPurgeCandidate {
  entityType: "DocumentPurgeReceipt";
  PK: string;
  SK: string;
  tenantId: string;
  documentId: string;
  version: number;
  GSI6PK: string;
  GSI6SK: string;
  purgeAfter: string;
}

export type PurgeCandidate = DocumentPurgeCandidate | ReceiptPurgeCandidate;

export interface DocumentPurgeWorkerDeps {
  store: DocumentStore;
  objects: DocumentObjectStore;
  tableName: string;
  now: () => string;
  correlationId?: () => string | undefined;
}

export interface PurgeRunResult {
  documentsPurged: number;
  receiptsPurged: number;
  claimsSkipped: number;
  leasesReverted: number;
  leasesStuck: number;
}

/** Never `doc.quarantineObject` Ã¢â‚¬â€ its `versionId` is always `""` (reserveUpload sets it before
 * the real object exists, see `document-service.ts`/`advance-after-evidence.ts`). */
function pickObjectToDelete(candidate: DocumentPurgeCandidate): DocumentObjectReference | undefined {
  return candidate.cleanObject ?? candidate.uploadEvidence?.object ?? candidate.malwareEvidence?.object;
}

async function claimAndPurgeDocument(deps: DocumentPurgeWorkerDeps, candidate: DocumentPurgeCandidate): Promise<"PURGED" | "SKIPPED"> {
  const claimAt = deps.now();
  const claimExpiresAt = new Date(Date.parse(claimAt) + PURGE_LEASE_MS).toISOString();
  const nextAttempts = (candidate.purgeAttempts ?? 0) + 1;

  try {
    await deps.store.transactWrite([
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: { PK: candidate.PK, SK: candidate.SK },
          tenantId: candidate.tenantId,
          expectedVersion: candidate.version,
          set: {
            GSI6PK: GSI6PK_PURGE_CLAIMED,
            GSI6SK: buildDocumentPurgeClaimGsi6Sk(claimExpiresAt, candidate.tenantId, candidate.documentId),
            purgeAttempts: nextAttempts,
          },
          extraConditions: [
            { expression: "attribute_not_exists(#legalHold) OR #legalHold = :false", names: { "#legalHold": "legalHold" }, values: { ":false": false } },
            { expression: "#status = :deleted", names: { "#status": "status" }, values: { ":deleted": "DELETED" } },
            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
            { expression: "purgeAfter <= :purgeCutoff", values: { ":purgeCutoff": claimAt } },
          ],
        }),
      },
    ]);
  } catch (err) {
    if (isTransactionCanceled(err)) return "SKIPPED";
    throw err;
  }

  const claimedVersion = candidate.version + 1;
  const objectToDelete = pickObjectToDelete(candidate);
  if (objectToDelete) await deps.objects.deleteObjectVersion(objectToDelete);

  const purgedAt = deps.now();
  const receiptPurgeAfter = computeDeliveryRecordPurgeAfter(purgedAt);
  const receipt: DocumentPurgeReceipt = {
    ...documentPurgeReceiptKey(candidate.tenantId, candidate.documentId),
    entityType: "DocumentPurgeReceipt",
    tenantId: candidate.tenantId,
    documentId: candidate.documentId,
    itemId: candidate.itemId,
    retentionClassPurged: "USER_DOCUMENT",
    deletedAtOriginal: candidate.deletedAt ?? candidate.purgeAfter,
    purgedAt,
    correlationId: deps.correlationId?.(),
    retentionClass: "DELIVERY_RECORD",
    purgeAfter: receiptPurgeAfter,
    GSI6PK: GSI6PK_PURGE_PENDING,
    GSI6SK: buildPurgeReceiptGsi6Sk(receiptPurgeAfter, candidate.tenantId, candidate.documentId),
    version: 1,
    createdAt: purgedAt,
    updatedAt: purgedAt,
  };

  await deps.store.transactWrite([
    {
      Delete: buildVersionedDelete({
        tableName: deps.tableName,
        key: { PK: candidate.PK, SK: candidate.SK },
        tenantId: candidate.tenantId,
        expectedVersion: claimedVersion,
        extraConditions: [{ expression: "GSI6PK = :expectedClaimed", values: { ":expectedClaimed": GSI6PK_PURGE_CLAIMED } }],
      }),
    },
    { Put: buildVersionedCreate(deps.tableName, receipt as unknown as Record<string, unknown> & { PK: string; SK: string }) },
  ]);

  return "PURGED";
}

async function purgeReceipt(deps: DocumentPurgeWorkerDeps, candidate: ReceiptPurgeCandidate): Promise<"PURGED" | "SKIPPED"> {
  const now = deps.now();
  try {
    await deps.store.transactWrite([
      {
        Delete: buildVersionedDelete({
          tableName: deps.tableName,
          key: { PK: candidate.PK, SK: candidate.SK },
          tenantId: candidate.tenantId,
          expectedVersion: candidate.version,
          extraConditions: [
            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
            { expression: "purgeAfter <= :purgeCutoff", values: { ":purgeCutoff": now } },
          ],
        }),
      },
    ]);
    return "PURGED";
  } catch (err) {
    if (isTransactionCanceled(err)) return "SKIPPED";
    throw err;
  }
}

/** Second query: `WORKSTATE#PURGE_CLAIMED` with `GSI6SK < now` (lease already expired). */
async function reconcileExpiredPurgeClaim(deps: DocumentPurgeWorkerDeps, candidate: DocumentPurgeCandidate): Promise<"REVERTED" | "STUCK" | "SKIPPED"> {
  const stuck = (candidate.purgeAttempts ?? 0) >= MAX_PURGE_ATTEMPTS;
  try {
    if (stuck) {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: candidate.PK, SK: candidate.SK },
            tenantId: candidate.tenantId,
            expectedVersion: candidate.version,
            set: { purgeStatus: "STUCK" },
            remove: ["GSI6PK", "GSI6SK"],
            extraConditions: [{ expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_CLAIMED, ":expectedSk": candidate.GSI6SK } }],
          }),
        },
      ]);
      return "STUCK";
    }
    await deps.store.transactWrite([
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: { PK: candidate.PK, SK: candidate.SK },
          tenantId: candidate.tenantId,
          expectedVersion: candidate.version,
          set: { GSI6PK: GSI6PK_PURGE_PENDING, GSI6SK: buildDocumentPurgeGsi6Sk(candidate.purgeAfter, candidate.tenantId, candidate.documentId) },
          extraConditions: [{ expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_CLAIMED, ":expectedSk": candidate.GSI6SK } }],
        }),
      },
    ]);
    return "REVERTED";
  } catch (err) {
    if (isTransactionCanceled(err)) return "SKIPPED";
    throw err;
  }
}

export async function runPurgeCycle(
  deps: DocumentPurgeWorkerDeps,
  input: { pendingCandidates: PurgeCandidate[]; claimedCandidates: DocumentPurgeCandidate[] },
): Promise<PurgeRunResult> {
  const result: PurgeRunResult = { documentsPurged: 0, receiptsPurged: 0, claimsSkipped: 0, leasesReverted: 0, leasesStuck: 0 };

  for (const candidate of input.pendingCandidates) {
    if (candidate.entityType === "Document") {
      const outcome = await claimAndPurgeDocument(deps, candidate);
      if (outcome === "PURGED") result.documentsPurged += 1;
      else result.claimsSkipped += 1;
    } else {
      const outcome = await purgeReceipt(deps, candidate);
      if (outcome === "PURGED") result.receiptsPurged += 1;
      else result.claimsSkipped += 1;
    }
  }

  for (const candidate of input.claimedCandidates) {
    const outcome = await reconcileExpiredPurgeClaim(deps, candidate);
    if (outcome === "REVERTED") result.leasesReverted += 1;
    else if (outcome === "STUCK") result.leasesStuck += 1;
  }

  return result;
}


2026-08-28T20:13:27.767769Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 5 \"exclus|30 dias|legalHold|legal hold|reten|purgeAfter|DSR\" docs/architecture/privacy-lgpd.md docs/architecture/data-model.md; rg -n -C 5 \"entityType: \\\"["'^'"\\\"]+\\\"\" src/modules/subject/application src/modules/reminder/application src/modules/identity/application src/modules/import/application | Select-Object -First 700"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 5 \"exclus|30 dias|legalHold|legal hold|reten|purgeAfter|DSR\" docs/architecture/privacy-lgpd.md docs/architecture/data-model.md; rg -n -C 5 \"entityType: \\\"["'^'"\\\"]+\\\"\" src/modules/subject/application src/modules/reminder/application src/modules/identity/application src/modules/import/application | Select-Object -First 700" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 5 \"exclus|30 dias|legalHold|legal hold|reten|purgeAfter|DSR\" docs/architecture/privacy-lgpd.md docs/architecture/data-model.md; rg -n -C 5 \"entityType: \\\"["'^'"\\\"]+\\\"\" src/modules/subject/application src/modules/reminder/application src/modules/identity/application src/modules/import/application | Select-Object -First 700"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 5 "purgeAfter|legalHold|DSR|30 dias" docs/architecture/privacy-lgpd.md docs/architecture/data-model.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 171ms:
docs/architecture/data-model.md-16-## 1. PrincÃ­pios e agregados
docs/architecture/data-model.md-17-`tenantId` obrigatÃ³rio em toda chave DynamoDB, idempotency key, objeto S3, evento e mensagem (SCALE-004). MVP: `tenantId=userId`; futuro: `tenantId=organizationId`. Identidade autenticÃ¡vel global no IdP (Cognito); `User` representa o perfil dentro do tenant, com `identitySubject` desacoplado do `sub` do Cognito (decisÃ£o Â§4 da arquitetura).
docs/architecture/data-model.md-18-
docs/architecture/data-model.md-19-Agregados: **Tenant/Access** (Organization, User, Membership) Â· **Expiration** (ExpirationItem raiz; ReminderPolicy, ReminderOccurrence, Document, ExtractedField referenciam item e sua versÃ£o) Â· **Notification** (NotificationIntent como efeito lÃ³gico Ãºnico; NotificationAttempt por interaÃ§Ã£o) Â· **Integration** (Channel, Provider, WebhookInbox, UploadSlot) Â· **Compliance** (AuditEvent, append-only).
docs/architecture/data-model.md-20-
docs/architecture/data-model.md:21:ConvenÃ§Ã£o fÃ­sica: `PK = TENANT#<tenantId>#<aggregate>`, `SK = <entityType>#<id>[#...]`. IDs UUIDv7/ULID (ordenÃ¡veis por tempo, evitam hot-key sequencial de auto-incremento). Datas ISO-8601 UTC; timezones IANA. Atributos comuns a toda entidade (design-target, ver nota de status abaixo): `entityType`, `schemaVersion`, `createdAt`, `updatedAt`, `version`, `deletedAt?`, `retentionClass`, `purgeAfter?`.
docs/architecture/data-model.md-22-
docs/architecture/data-model.md:23:**Status de implementaÃ§Ã£o (adicionado full-audit round1/eixo Privacidade, 2026-08-20):** `retentionClass`/`purgeAfter` sÃ£o compromisso de design (`docs/architecture/privacy-lgpd.md` Â§4), nÃ£o implementaÃ§Ã£o atual â€” nenhuma entidade em `src/` carrega esses dois campos ainda (M0-M3 nÃ£o os materializou), e o GSI6 fÃ­sico (`infra/lib/dynamo-table.ts`) estÃ¡ hoje restrito por IAM exclusivamente a `ReminderReconciliation`/`OutboxSweeperReminderDispatch`, sem papel de purge sancionado. Registrar aqui explicitamente para nÃ£o ler este parÃ¡grafo como "jÃ¡ implementado" â€” trabalho de M4+ (worker de purge, migraÃ§Ã£o de escrita para popular os campos, ampliaÃ§Ã£o da policy de GSI6) fica pendente e rastreado por este parÃ¡grafo, nÃ£o por drift silencioso.
docs/architecture/data-model.md-24-
docs/architecture/data-model.md-25-## 2. Entidades e armazenamento
docs/architecture/data-model.md-26-| Entidade | Atributos principais | PK / SK |
docs/architecture/data-model.md-27-|---|---|---|
docs/architecture/data-model.md-28-| User | userId, identitySubject, emailNormalized, name, timezone, locale, preferÃªncias de notificaÃ§Ã£o, status | `TENANT#t#USER#u` / `PROFILE` |
--
docs/architecture/data-model.md-62-- **GSI1 â€” vencimentos/dashboard**: `PK=TENANT#t#ITEMSTATUS#<status>`, `SK=DUE#<dueDate>#ITEM#i`. Lista prÃ³ximos/vencidos/arquivados.
docs/architecture/data-model.md-63-- **GSI2 â€” responsÃ¡vel/categoria**: `PK=TENANT#t#ASSIGNEE#u` ou `TENANT#t#CATEGORY#c`, `SK=DUE#<dueDate>#ITEM#i`. Ãndices separados se ambos forem padrÃµes frequentes de acesso.
docs/architecture/data-model.md-64-- **GSI3 â€” scheduler global de ocorrÃªncias agendadas** *(entendimento atualizado em M10/D-039/D-046, `roadmap-evolution/12-automated-chasing-capacity-review.md` â€” mudanÃ§a de documentaÃ§Ã£o, nÃ£o de mecanismo)*: `PK=GSI3PK=DUE#yyyyMMddHHmm#NN`, `SK=GSI3SK` discriminado por `entityType` na prÃ³pria forma da chave â€” `TENANT#t#OCCURRENCE#<occurrenceId>` para `ReminderOccurrence` (formato original, zero migraÃ§Ã£o das linhas jÃ¡ em produÃ§Ã£o) e `TENANT#t#CHASING#<occurrenceId>` para `DocumentChasingOccurrence` (M10 cluster 4, agregado-irmÃ£o â€” nunca generaliza `ReminderOccurrence`). 4 shards/minuto inicial (decisÃ£o Fase 3 Rodada 4); produtor condiciona por status/versÃ£o, branca por `entityType` ao processar cada linha lida, e falha-fechado com alarme se a forma da SK nÃ£o corresponder a nenhum discriminador conhecido. **CorreÃ§Ã£o Type 1 (ratificada em `implementation-blueprint.md` Â§9.2/Â§9.3, decisions-log.md D-028)**: a chave original (`PK=TENANT#t#DUE#...`) nÃ£o Ã© consultÃ¡vel pelo `ReminderProducer` â€” `Query` do DynamoDB exige PK exata, e o producer do minuto `M` nÃ£o sabe a priori quais tenants tÃªm ocorrÃªncia em `M`. GSI3 Ã© portanto a **Ãºnica exceÃ§Ã£o** Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`": a PK Ã© global (sem `tenantId`), com `tenantId` preservado na SK e no item base para reconstruÃ§Ã£o segura do contexto. Salvaguardas obrigatÃ³rias (detalhadas em `implementation-blueprint.md` Â§9.2, mantidas para os dois `entityType`): IAM do Ã­ndice restrito exclusivamente ao producer (nenhuma rota tenant-facing tem `Query` neste GSI); projeÃ§Ã£o mÃ­nima, sem conteÃºdo de negÃ³cio sensÃ­vel; teste de isolamento automatizado provando que nenhuma API tenant-facing o alcanÃ§a, direta ou indiretamente; dimensionamento de shards por pico global, nÃ£o por tenant; alarmes segmentados por `entityType` (D-046) para que um pico anÃ´malo de um tipo nÃ£o se esconda no agregado do outro.
docs/architecture/data-model.md-65-- **GSI4 â€” membership por usuÃ¡rio**: `PK=TENANT#t#USER#u`, `SK=ORG#o#MEMBERSHIP#m`.
docs/architecture/data-model.md-66-- **GSI5 â€” provider callback**: `PK=TENANT#t#PROVIDER#p`, `SK=MSG#<providerMessageId>`. Localiza a tentativa correta a partir de webhook de delivery/bounce.
docs/architecture/data-model.md:67:- **GSI6 â€” retenÃ§Ã£o/reconciliaÃ§Ã£o**: `PK=TENANT#t#PURGE#yyyyMM` ou `...#WORKSTATE#<status>`, `SK=<purgeAfter/expiresAt>#<type>#<id>`. Suporta purge, upload slots vencidos, inbox/outbox e jobs pendentes num Ã­ndice Ãºnico. TTL Ã© limpeza auxiliar, nunca gatilho operacional (evita depender de timing assÃ­ncrono do TTL para correÃ§Ã£o â€” mesma liÃ§Ã£o do reconciliador de upload da Fase 3 Rodada 6).
docs/architecture/data-model.md-68-- **GSI7 â€” listagem de `TrackedSubject`** *(M9, D-036)*: `PK=TENANT#t#SUBJECTSTATUS#<status>`, `SK=TYPE#<type>#NAME#<displayNameNormalized>#SUBJECT#<subjectId>`. Escopo Ãºnico (sÃ³ listagem de subject por status/tipo/nome â€” decisÃ£o explÃ­cita de nÃ£o misturar com outro access pattern no mesmo Ã­ndice, `roadmap-evolution/03-...md` rodada 2). Tenant-scoped, entra na polÃ­tica geral `tenant_facing_read_write`/`tenant_facing_read`, nunca isolado como GSI3/GSI6. `RequirementAssignment` nÃ£o usa GSI novo â€” Ã© coleÃ§Ã£o sob a partiÃ§Ã£o do subject (ver Â§2).
docs/architecture/data-model.md-69-
docs/architecture/data-model.md-70-## 4. IdempotÃªncia
docs/architecture/data-model.md-71-Chaves materializadas com `PutItem attribute_not_exists(PK)`:
docs/architecture/data-model.md-72-| OperaÃ§Ã£o | Chave de idempotÃªncia |
--
docs/architecture/data-model.md-80-| Upload | `UploadSlot` reservado atomicamente via `TransactWriteItems` no momento da geraÃ§Ã£o da URL presigned (decisÃ£o Fase 3 Rodada 6) |
docs/architecture/data-model.md-81-
docs/architecture/data-model.md-82-## 5. ConcorrÃªncia, exclusÃ£o e consistÃªncia
docs/architecture/data-model.md-83-Toda entidade mutÃ¡vel usa `version`; updates exigem `ConditionExpression: version=:expected` e incrementam atomicamente (optimistic concurrency control, decisÃ£o Fase 3 Rodada 6). Alterar vencimento grava o item, cancela ocorrÃªncias antigas e cria o evento de outbox crÃ­tico (`ItemDueDateChanged`) numa Ãºnica transaÃ§Ã£o (`TransactWriteItems`) â€” fecha a corrida identificada no Red Team cenÃ¡rio 13. AplicaÃ§Ã£o de resultado de extraÃ§Ã£o verifica simultaneamente as versÃµes de `Document` e `ExpirationItem`; divergÃªncia ou tombstone descarta o resultado (Red Team cenÃ¡rio 14).
docs/architecture/data-model.md-84-
docs/architecture/data-model.md:85:Soft delete: `deletedAt` + status + nova versÃ£o; leituras normais excluem tombstones. ExclusÃ£o LGPD: workflow auditÃ¡vel, atendimento em atÃ© 30 dias (PRIV-003), remoÃ§Ã£o de DynamoDB/S3/Ã­ndices e propagaÃ§Ã£o a backups em atÃ© 90 dias (PRIV-006). `purgeAfter` deriva da tabela de retenÃ§Ã£o por tipo de dado exigida por PRIV-004; retenÃ§Ã£o legal suspende purge com justificativa explÃ­cita registrada.
docs/architecture/data-model.md-86-
docs/architecture/data-model.md-87-Leitura da PK base Ã© fortemente consistente quando necessÃ¡rio (autorizaÃ§Ã£o, ediÃ§Ã£o, prÃ©-envio de notificaÃ§Ã£o â€” a revalidaÃ§Ã£o de versÃ£o do Red Team cenÃ¡rio 13 depende disso). GSIs sÃ£o eventualmente consistentes: dashboards podem atrasar brevemente; nenhuma decisÃ£o crÃ­tica (cancelamento, disparo, aplicaÃ§Ã£o de extraÃ§Ã£o) depende exclusivamente de um GSI. Scheduler, inbox, upload e outbox toleram duplicaÃ§Ã£o por condiÃ§Ãµes/idempotÃªncia e sÃ£o cobertos por reconciliaÃ§Ã£o periÃ³dica (mesmo padrÃ£o em todos os casos).
docs/architecture/data-model.md-88-
docs/architecture/data-model.md-89-## GovernanÃ§a do single-table (adicionado apÃ³s revisÃ£o externa pÃ³s-aprovaÃ§Ã£o)
docs/architecture/data-model.md-90-
--
docs/architecture/privacy-lgpd.md-1-# Privacy / LGPD â€” Expiration Tracker (Consolidado)
docs/architecture/privacy-lgpd.md-2-
docs/architecture/privacy-lgpd.md-3-Status: **APPROVED** (Design Maturity) â€” Claude ~9.15 / Codex 9.1, ambos â‰¥9.0, nenhum gate violado. ConsolidaÃ§Ã£o usando a proposta do Codex como base (substancialmente mais detalhada: state machine de exclusÃ£o, 8 classes de retenÃ§Ã£o vs. 4 do Claude, bloqueio de regiÃ£o por IaC/SCP). **NÃ£o constitui parecer jurÃ­dico** â€” implica PRIV-001..008.
docs/architecture/privacy-lgpd.md:4:Base: `docs/architecture/requirements.md`, `docs/architecture/data-model.md`, `docs/architecture/disaster-recovery.md` (dependÃªncia da matriz `retentionClass`/`legalHold`, fechada aqui).
docs/architecture/privacy-lgpd.md-5-
docs/architecture/privacy-lgpd.md-6-## 1. Mapa de dados pessoais
docs/architecture/privacy-lgpd.md-7-| Entidade/dado | Finalidade | Base legal hipotÃ©tica |
docs/architecture/privacy-lgpd.md-8-|---|---|---|
docs/architecture/privacy-lgpd.md-9-| `User` â€” IdP, nome, e-mail, locale/timezone, preferÃªncias | autenticaÃ§Ã£o, conta e comunicaÃ§Ã£o do serviÃ§o | execuÃ§Ã£o de contrato; legÃ­timo interesse para seguranÃ§a |
--
docs/architecture/privacy-lgpd.md-22-SÃ³ e-mail, timezone e identidade federada sÃ£o necessÃ¡rios Ã  conta; nome e demais campos sÃ£o opcionais (FR-010). Documento bruto nunca vai a logs/eventos/notificaÃ§Ãµes. OCR/IA recebe apenas pÃ¡ginas e campos necessÃ¡rios. Auditoria Ã© redigida (dados sensÃ­veis mascarados); payloads de webhook limitados; allowlist de MIME/tamanho; uploads incompletos expiram (`UploadSlot`, `data-model.md`). Segredos no Secrets Manager; Ã­ndices nÃ£o contÃªm conteÃºdo documental. KMS, isolamento por `tenantId`, menor privilÃ©gio, acesso administrativo auditado, IDs pseudÃ´nimos em mÃ©tricas.
docs/architecture/privacy-lgpd.md-23-
docs/architecture/privacy-lgpd.md-24-## 3. Direitos do titular (PRIV-003)
docs/architecture/privacy-lgpd.md-25-Endpoint autenticado + canal alternativo verificado criam `DataSubjectRequest` (tipo, escopo, verificaÃ§Ã£o de identidade, prazos, decisÃµes).
docs/architecture/privacy-lgpd.md-26-- **ConfirmaÃ§Ã£o** (â‰¤15 dias, LGPD art. 19): categorias, finalidades, origem e compartilhamentos, de forma legÃ­vel.
docs/architecture/privacy-lgpd.md:27:- **ExportaÃ§Ã£o** (â‰¤30 dias corridos): JSON/CSV, documentos originais, manifesto/checksums, URL presigned curta â€” exclui segredos internos e dados de terceiros sem autorizaÃ§Ã£o.
docs/architecture/privacy-lgpd.md:28:- **ExclusÃ£o**: state machine `RECEIVED â†’ VERIFIED â†’ DISCOVERED â†’ HELD/PURGING â†’ COMPLETED`. InventÃ¡rio por `tenantId` em DynamoDB, S3, Ã­ndices e provedores; bloqueio imediato de notificaÃ§Ãµes/uso; tombstone transacional; purge idempotente via GSI6 (`data-model.md`); revogaÃ§Ã£o de canais e links. ProrrogaÃ§Ã£o de 30 dias exige justificativa e aviso registrados (PRIV-003).
docs/architecture/privacy-lgpd.md-29-
docs/architecture/privacy-lgpd.md:30:`legalHold=true` exige fundamento, escopo, aprovador e `reviewAt` â€” retÃ©m apenas o mÃ­nimo necessÃ¡rio. Backups nÃ£o sÃ£o regravados: DynamoDB PITR expira em 35 dias; restores consultam denylist de exclusÃµes e executam purge pÃ³s-restore; nenhuma cÃ³pia persiste por mais de 90 dias (PRIV-006, teto jÃ¡ fixado).
docs/architecture/privacy-lgpd.md-31-
docs/architecture/privacy-lgpd.md-32-## 4. Matriz de retenÃ§Ã£o e DR (fecha a dependÃªncia criada em `disaster-recovery.md`)
docs/architecture/privacy-lgpd.md:33:`purgeAfter` deriva do evento indicado; `legalHold` apenas suspende o purge, nunca o cancela permanentemente sem revisÃ£o.
docs/architecture/privacy-lgpd.md-34-
docs/architecture/privacy-lgpd.md-35-| `retentionClass` | Dados | Prazo padrÃ£o | Hold | Cross-region/Object Lock |
docs/architecture/privacy-lgpd.md-36-|---|---|---|---|---|
docs/architecture/privacy-lgpd.md:37:| `ACCOUNT_ACTIVE` | User, Organization, Membership, Channel | encerramento + 30 dias | litÃ­gio/obrigaÃ§Ã£o confirmada | nÃ£o |
docs/architecture/privacy-lgpd.md:38:| `CORE_USER_DATA` | itens, polÃ­ticas, ocorrÃªncias | exclusÃ£o/encerramento + 30 dias | obrigaÃ§Ã£o ligada ao item | nÃ£o |
docs/architecture/privacy-lgpd.md:39:| `USER_DOCUMENT` | Document/S3, campos e runs | exclusÃ£o/encerramento + 30 dias; runs falhos/descartados: 7 dias | obrigaÃ§Ã£o especÃ­fica | nÃ£o |
docs/architecture/privacy-lgpd.md-40-| `LEGAL_EVIDENCE` | documento expressamente classificado | prazo legal/contratual com data final obrigatÃ³ria | sim; revisÃ£o periÃ³dica | somente apÃ³s aprovaÃ§Ã£o jurÃ­dica; KMS independente e Object Lock temporÃ¡rio |
docs/architecture/privacy-lgpd.md-41-| `DELIVERY_RECORD` | intents/attempts | criaÃ§Ã£o + 180 dias | disputa/incidente | nÃ£o |
docs/architecture/privacy-lgpd.md-42-| `TRANSIENT` | WebhookInbox, UploadSlot | 7 dias; slot incompleto: 24h | nÃ£o | nÃ£o |
docs/architecture/privacy-lgpd.md-43-| `SECURITY_AUDIT` | AuditEvent/logs redigidos | criaÃ§Ã£o + 365 dias | incidente/litÃ­gio | backup regional |
docs/architecture/privacy-lgpd.md:44:| `QUOTA_TELEMETRY` | quotas/mÃ©tricas identificÃ¡veis | fim da janela + 30 dias | nÃ£o | nÃ£o |
docs/architecture/privacy-lgpd.md-45-| `EXTRACTION_TRANSIENT` | texto OCR (Textract) do pipeline de extraÃ§Ã£o M7, artefato transitÃ³rio em bucket/prefixo dedicado | exclusÃ£o explÃ­cita ao concluir/falhar/descartar o run; lifecycle S3 de 24h como safety net (nunca o prazo real esperado) | nÃ£o | nÃ£o |
docs/architecture/privacy-lgpd.md-46-
docs/architecture/privacy-lgpd.md-47-`EXTRACTION_TRANSIENT` (adicionado 2026-08-25, prÃ©-requisito de design registrado antes de qualquer implementaÃ§Ã£o de M7 â€” `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` Â§1.4/Â§4, GATE atingido 9,2/9,3): o texto OCR nunca Ã© o dado final do sistema (`ExtractedField`/`USER_DOCUMENT` acima sÃ£o as classes do resultado persistido) â€” Ã© um artefato de trabalho intermediÃ¡rio entre `RunTextract` e `ExtractionValidationTaskHandler`, sem versionamento/backup/replicaÃ§Ã£o (nada aqui deve sobreviver a uma restauraÃ§Ã£o de disaster recovery), nunca entra em DynamoDB/logs/traces/eventos/DLQ. Ãšnico bucket/prefixo cujo prazo padrÃ£o Ã© medido em horas, nÃ£o dias â€” reflete que seu Ãºnico propÃ³sito Ã© existir pelo tempo mÃ­nimo entre duas etapas de um mesmo pipeline.
docs/architecture/privacy-lgpd.md-48-
docs/architecture/privacy-lgpd.md-49-Nenhuma classe aceita prazo nulo, salvo conta ainda ativa. `LEGAL_EVIDENCE` sem fundamento e data final regride automaticamente para `USER_DOCUMENT` â€” nunca fica em limbo indefinido. **Validar juridicamente**: prazos, documentos probatÃ³rios, obrigaÃ§Ãµes fiscais/consumeristas/contratuais.
--
docs/architecture/privacy-lgpd.md-69-Sem esses gatilhos, e enquanto o projeto permanece prÃ©-produÃ§Ã£o sem usuÃ¡rios reais, nÃ£o produzir RIPD especulativo Ã© proporcional (`docs/engineering/principles.md` #1) â€” este parÃ¡grafo existe para que a ausÃªncia de RIPD hoje seja decisÃ£o registrada, nÃ£o lacuna silenciosa.
docs/architecture/privacy-lgpd.md-70-
docs/architecture/privacy-lgpd.md-71-## 7. Nota de status de implementaÃ§Ã£o (adicionado full-audit round1/eixo Privacidade, 2026-08-20)
docs/architecture/privacy-lgpd.md-72-Este documento Ã© design aprovado (Design Maturity `APPROVED`), nÃ£o um relatÃ³rio do que jÃ¡ existe em cÃ³digo. Estado real na sessÃ£o que fechou o eixo Privacidade e GovernanÃ§a de Dados do full-audit round1 (`docs/engineering/reviews/full-audit-round1-privacidade-summary.md`):
docs/architecture/privacy-lgpd.md-73-- **Implementado**: minimizaÃ§Ã£o de telemetria (`src/shared/observability/redactor.ts` + `schemas/sensitive-fields.json`), trilha de auditoria transacional e redigida para `ExpirationItem` (`src/modules/expiration/domain/audit-event.ts`).
docs/architecture/privacy-lgpd.md:74:- **NÃ£o implementado ainda** (design-only): `DataSubjectRequest` e qualquer endpoint de confirmaÃ§Ã£o/acesso/correÃ§Ã£o/exportaÃ§Ã£o/oposiÃ§Ã£o/exclusÃ£o (Â§3); `retentionClass`/`purgeAfter` materializados em entidades e worker de purge (Â§4, ver tambÃ©m `data-model.md` Â§1); allowlist/SCP de regiÃ£o e inventÃ¡rio versionado de subprocessadores (Â§5).
docs/architecture/privacy-lgpd.md:75:- ClassificaÃ§Ã£o: construir DSR endpoints e worker de purge reais Ã© escopo maior (feature de produto, M4+), nÃ£o point-fix de documentaÃ§Ã£o; regiÃ£o/subprocessadores Ã© impedimento externo (decisÃ£o de negÃ³cio + parecer jurÃ­dico pendentes). Nenhum dos dois Ã© drift silencioso â€” ambos rastreados aqui e no eixo de auditoria correspondente.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/expiration/domain/audit-event.ts; Get-Content -Raw src/modules/subject/domain/audit-event.ts; rg -n -C 4 "NotificationOutcome|retentionClass|DELIVERY_RECORD|SECURITY_AUDIT|append-only" src/modules src/shared' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 218ms:
/**
 * AuditEvent Ã¢â‚¬â€ data-model.md Ã‚Â§2: "append-only, sem update/delete normal".
 * PK=TENANT#t#AUDIT#<yyyyMM>, SK=EVT#<timestamp>#<id>. This module only builds the
 * item + its transactional Put entry (mirrors src/shared/outbox/outbox.ts's shape) so
 * every mutation appends its audit record inside the SAME TransactWriteItems as the
 * aggregate write - never as a separate, droppable follow-up call. There is
 * deliberately no update()/delete() exported anywhere for this entity.
 */
import { defaultRedactor } from "../../../shared/observability/redactor.js";
import type { Actor } from "../../../shared/contracts/events.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export type AuditAction = "CREATE" | "UPDATE" | "ARCHIVE" | "RENEW" | "DELETE";

export interface AuditEvent extends EntityKey {
  entityType: "AuditEvent";
  auditEventId: string;
  tenantId: string;
  resourceType: "ExpirationItem";
  resourceId: string;
  itemId: string;
  action: AuditAction;
  actor: Actor;
  previousVersion?: number;
  newVersion: number;
  /** Redacted diff of changed fields (before/after), never raw values that might carry PII (implementation-blueprint.md #14.1 redactor). */
  changes: Record<string, unknown>;
  occurredAt: string;
  correlationId: string;
  causationId?: string;
}

function monthShard(isoTimestamp: string): string {
  return isoTimestamp.slice(0, 7).replace("-", "");
}

export function auditKey(tenantId: string, occurredAt: string, auditEventId: string): EntityKey {
  return {
    PK: `TENANT#${tenantId}#AUDIT#${monthShard(occurredAt)}`,
    SK: `EVT#${occurredAt}#${auditEventId}`,
  };
}

export interface BuildAuditEventInput {
  auditEventId: string;
  tenantId: string;
  itemId: string;
  action: AuditAction;
  actor: Actor;
  previousVersion?: number;
  newVersion: number;
  /** Raw before/after values - redacted internally before being stored. */
  changes: Record<string, unknown>;
  occurredAt: string;
  correlationId: string;
  causationId?: string;
}

export function buildAuditEvent(input: BuildAuditEventInput): AuditEvent {
  const key = auditKey(input.tenantId, input.occurredAt, input.auditEventId);
  return {
    ...key,
    entityType: "AuditEvent",
    auditEventId: input.auditEventId,
    tenantId: input.tenantId,
    resourceType: "ExpirationItem",
    resourceId: input.itemId,
    itemId: input.itemId,
    action: input.action,
    actor: input.actor,
    previousVersion: input.previousVersion,
    newVersion: input.newVersion,
    changes: defaultRedactor.redact(input.changes) as Record<string, unknown>,
    occurredAt: input.occurredAt,
    correlationId: input.correlationId,
    causationId: input.causationId,
  };
}

/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction (accepts the full Put|Update union so callers don't need an unsafe cast for a mixed transaction). */
export function appendAuditToTransaction(tx: TransactWriteEntry[], tableName: string, event: AuditEvent): void {
  tx.push({
    Put: {
      TableName: tableName,
      Item: { ...event },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });
}

/**
 * AuditEvent do mÃƒÂ³dulo subject Ã¢â‚¬â€ agregado-irmÃƒÂ£o do AuditEvent de expiration
 * (src/modules/expiration/domain/audit-event.ts), mesma forma/mesmas garantias
 * (append-only, redigido, sempre na mesma transaÃƒÂ§ÃƒÂ£o do agregado), mas resourceType prÃƒÂ³prio
 * (TrackedSubject/RequirementAssignment) em vez de generalizar o tipo jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€
 * mesmo princÃƒÂ­pio de agregados-irmÃƒÂ£os jÃƒÂ¡ aplicado em 06-domain-model-automated-chasing.md.
 */
import { defaultRedactor } from "../../../shared/observability/redactor.js";
import type { Actor } from "../../../shared/contracts/events.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export type SubjectAuditAction =
  | "CREATE"
  | "UPDATE"
  | "ARCHIVE"
  | "DELETE"
  | "LINK_ITEM"
  | "UNLINK_ITEM"
  | "ASSIGN_REQUIREMENT"
  | "DELETE_REQUIREMENT"
  // M10 cluster 4 (D-049): convite inicial automatizado - trilha completa dos 5 desfechos
  // possÃƒÂ­veis (nunca e-mail bruto no diff, `changes` jÃƒÂ¡ passa por `defaultRedactor` abaixo).
  | "INITIAL_INVITE_EMAIL_REQUESTED"
  | "INITIAL_INVITE_EMAIL_SENT"
  | "INITIAL_INVITE_EMAIL_FAILED"
  | "INITIAL_INVITE_EMAIL_RATE_LIMITED"
  | "INITIAL_INVITE_EMAIL_DISABLED_BY_KILL_SWITCH"
  | "CONFIGURE_DOCUMENT_REQUEST_DELIVERY";
export type SubjectAuditResourceType = "TrackedSubject" | "RequirementAssignment" | "DocumentRequest" | "DocumentRequestDeliveryPreference";

export interface SubjectAuditEvent extends EntityKey {
  entityType: "SubjectAuditEvent";
  auditEventId: string;
  tenantId: string;
  resourceType: SubjectAuditResourceType;
  resourceId: string;
  subjectId: string;
  action: SubjectAuditAction;
  actor: Actor;
  previousVersion?: number;
  newVersion: number;
  changes: Record<string, unknown>;
  occurredAt: string;
  correlationId: string;
}

function monthShard(isoTimestamp: string): string {
  return isoTimestamp.slice(0, 7).replace("-", "");
}

export function subjectAuditKey(tenantId: string, occurredAt: string, auditEventId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#SUBJECTAUDIT#${monthShard(occurredAt)}`, SK: `EVT#${occurredAt}#${auditEventId}` };
}

export interface BuildSubjectAuditEventInput {
  auditEventId: string;
  tenantId: string;
  resourceType: SubjectAuditResourceType;
  resourceId: string;
  subjectId: string;
  action: SubjectAuditAction;
  actor: Actor;
  previousVersion?: number;
  newVersion: number;
  changes: Record<string, unknown>;
  occurredAt: string;
  correlationId: string;
}

export function buildSubjectAuditEvent(input: BuildSubjectAuditEventInput): SubjectAuditEvent {
  const key = subjectAuditKey(input.tenantId, input.occurredAt, input.auditEventId);
  return {
    ...key,
    entityType: "SubjectAuditEvent",
    auditEventId: input.auditEventId,
    tenantId: input.tenantId,
    resourceType: input.resourceType,
    resourceId: input.resourceId,
    subjectId: input.subjectId,
    action: input.action,
    actor: input.actor,
    previousVersion: input.previousVersion,
    newVersion: input.newVersion,
    changes: defaultRedactor.redact(input.changes) as Record<string, unknown>,
    occurredAt: input.occurredAt,
    correlationId: input.correlationId,
  };
}

export function appendSubjectAuditToTransaction(tx: TransactWriteEntry[], tableName: string, event: SubjectAuditEvent): void {
  tx.push({
    Put: {
      TableName: tableName,
      Item: { ...event },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });
}

src/modules\expiration\domain\audit-event.ts-1-/**
src/modules\expiration\domain\audit-event.ts:2: * AuditEvent â€” data-model.md Â§2: "append-only, sem update/delete normal".
src/modules\expiration\domain\audit-event.ts-3- * PK=TENANT#t#AUDIT#<yyyyMM>, SK=EVT#<timestamp>#<id>. This module only builds the
src/modules\expiration\domain\audit-event.ts-4- * item + its transactional Put entry (mirrors src/shared/outbox/outbox.ts's shape) so
src/modules\expiration\domain\audit-event.ts-5- * every mutation appends its audit record inside the SAME TransactWriteItems as the
src/modules\expiration\domain\audit-event.ts-6- * aggregate write - never as a separate, droppable follow-up call. There is
--
src/modules\document\application\document-service.ts-146-        contentLength: input.contentLength,
src/modules\document\application\document-service.ts-147-        checksumSha256: input.checksumSha256,
src/modules\document\application\document-service.ts-148-        status: "PENDING_UPLOAD",
src/modules\document\application\document-service.ts-149-        quarantineObject: { bucket: this.quarantineBucket, key: quarantineKey, versionId: "" },
src/modules\document\application\document-service.ts:150:        retentionClass: "USER_DOCUMENT",
src/modules\document\application\document-service.ts-151-        version: 1,
src/modules\document\application\document-service.ts-152-        createdAt: now,
src/modules\document\application\document-service.ts-153-        updatedAt: now,
src/modules\document\application\document-service.ts-154-      };
--
src/modules\document\application\document-service.ts-162-        status: "RESERVED",
src/modules\document\application\document-service.ts-163-        quarantineKey,
src/modules\document\application\document-service.ts-164-        reservedAt: now,
src/modules\document\application\document-service.ts-165-        expiresAt,
src/modules\document\application\document-service.ts:166:        retentionClass: "TRANSIENT",
src/modules\document\application\document-service.ts-167-        purgeAfter: expiresAt,
src/modules\document\application\document-service.ts-168-        version: 1,
src/modules\document\application\document-service.ts-169-        updatedAt: now,
src/modules\document\application\document-service.ts-170-        // Real bug found via Camada 3 verification against AWS real (2026-08-25): this write
--
src/modules\document\application\document-deletion-service.ts-1-/**
src/modules\document\application\document-deletion-service.ts-2- * Logical deletion â€” M6 design Â§3.6 / blueprint Â§12.6. Version-conditioned, idempotent,
src/modules\document\application\document-deletion-service.ts-3- * never physically deletes the object here (physical purge is a separate, later step driven
src/modules\document\application\document-deletion-service.ts:4: * by `retentionClass`/`purgeAfter` â€” out of scope for M6's acceptance criteria beyond marking
src/modules\document\application\document-deletion-service.ts-5- * the document DELETED and scheduling the clock).
src/modules\document\application\document-deletion-service.ts-6- */
src/modules\document\application\document-deletion-service.ts-7-import type { RequestContext } from "../../identity/domain/request-context.js";
src/modules\document\application\document-deletion-service.ts-8-import { authorize } from "../../identity/domain/authorization.js";
--
src/modules\document\domain\document-purge-receipt.ts-2- * W3-06 (D-061): non-sensitive proof-of-purge record, written transactionally alongside the
src/modules\document\domain\document-purge-receipt.ts-3- * physical `Document` deletion so the evidence and the deletion itself can never diverge (the
src/modules\document\domain\document-purge-receipt.ts-4- * transaction that removes the Document row is the same one that creates this). Deliberately
src/modules\document\domain\document-purge-receipt.ts-5- * carries no document content and no third-party data - only internal identifiers and
src/modules\document\domain\document-purge-receipt.ts:6: * timestamps, `retentionClassPurged` for audit, and `correlationId` to trace back to the worker
src/modules\document\domain\document-purge-receipt.ts-7- * invocation that performed the purge.
src/modules\document\domain\document-purge-receipt.ts-8- */
src/modules\document\domain\document-purge-receipt.ts-9-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src/modules\document\domain\document-purge-receipt.ts-10-
--
src/modules\document\domain\document-purge-receipt.ts-12-  entityType: "DocumentPurgeReceipt";
src/modules\document\domain\document-purge-receipt.ts-13-  tenantId: string;
src/modules\document\domain\document-purge-receipt.ts-14-  documentId: string;
src/modules\document\domain\document-purge-receipt.ts-15-  itemId: string;
src/modules\document\domain\document-purge-receipt.ts:16:  retentionClassPurged: "USER_DOCUMENT";
src/modules\document\domain\document-purge-receipt.ts-17-  deletedAtOriginal: string;
src/modules\document\domain\document-purge-receipt.ts-18-  purgedAt: string;
src/modules\document\domain\document-purge-receipt.ts-19-  correlationId?: string;
src/modules\document\domain\document-purge-receipt.ts:20:  /** Reuses `DELIVERY_RECORD`'s existing "criaÃ§Ã£o + 180 dias" retention (privacy-lgpd.md Â§4) -
src/modules\document\domain\document-purge-receipt.ts-21-   * same rationale (proof of process, not third-party data), not a new class for one entity. */
src/modules\document\domain\document-purge-receipt.ts:22:  retentionClass: "DELIVERY_RECORD";
src/modules\document\domain\document-purge-receipt.ts-23-  purgeAfter: string;
src/modules\document\domain\document-purge-receipt.ts-24-  GSI6PK?: string;
src/modules\document\domain\document-purge-receipt.ts-25-  GSI6SK?: string;
src/modules\document\domain\document-purge-receipt.ts-26-  version: number;
--
src/modules\document\domain\document.ts-4- * DELETED (implementation-blueprint.md Â§12.2-12.3; TIMEOUT is driven by
src/modules\document\domain\document.ts-5- * UploadSlotReconciliationWorker for scans that never resolve, not by the malware/finalizer
src/modules\document\domain\document.ts-6- * workers directly). M6 design Â§2.1 adds uploadEvidence/malwareEvidence (independent evidence
src/modules\document\domain\document.ts-7- * fields, never presuming which arrives first - see document-state-machine.ts) and
src/modules\document\domain\document.ts:8: * retentionClass/purgeAfter (privacy-lgpd.md Â§4 USER_DOCUMENT class materialized on the entity
src/modules\document\domain\document.ts-9- * from day one, not bolted on later).
src/modules\document\domain\document.ts-10- */
src/modules\document\domain\document.ts-11-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src/modules\document\domain\document.ts-12-import type { DocumentObjectReference } from "./document-object-reference.js";
--
src/modules\document\domain\document.ts-44-  cleanObject?: DocumentObjectReference;
src/modules\document\domain\document.ts-45-  uploadEvidence?: UploadEvidence;
src/modules\document\domain\document.ts-46-  malwareEvidence?: MalwareEvidence;
src/modules\document\domain\document.ts-47-  extractionRunId?: string; // M7, unused by M6, kept for schema forward-compatibility only.
src/modules\document\domain\document.ts:48:  retentionClass: Extract<DocumentRetentionClass, "USER_DOCUMENT">;
src/modules\document\domain\document.ts-49-  purgeAfter?: string; // set only once a terminal/deletion event establishes the clock.
src/modules\document\domain\document.ts-50-  /** W3-06: global (non-tenant-prefixed) GSI6 pointer for the purge worker - same convention as
src/modules\document\domain\document.ts-51-   * `GSI6PK_RECON_UPLOAD_PENDING`. Present only while a purge candidate is pending/claimed;
src/modules\document\domain\document.ts-52-   * removed by the same transaction that physically deletes the row. */
--
src/modules\document\domain\retention.ts-2- * Retention classes â€” closed union matching `privacy-lgpd.md` Â§4's retention matrix exactly
src/modules\document\domain\retention.ts-3- * (only the two classes real Document/UploadSlot entities actually use; the others belong to
src/modules\document\domain\retention.ts-4- * different aggregates and are declared elsewhere or not yet materialized anywhere - full
src/modules\document\domain\retention.ts-5- * cross-entity retention/purge worker is explicitly out of scope for M6, per
src/modules\document\domain\retention.ts:6: * `privacy-lgpd.md` line 69: "nÃ£o implementado ainda... retentionClass/purgeAfter
src/modules\document\domain\retention.ts-7- * materializados em entidades e worker de purge"). M6 closes the entity half of that gap for
src/modules\document\domain\retention.ts:8: * Document/UploadSlot specifically; a general-purpose purge worker across all retentionClass
src/modules\document\domain\retention.ts-9- * values remains a separate, larger piece of future work.
src/modules\document\domain\retention.ts-10- */
src/modules\document\domain\retention.ts-11-export type DocumentRetentionClass = "USER_DOCUMENT" | "TRANSIENT";
src/modules\document\domain\retention.ts-12-
--
src/modules\document\domain\retention.ts-17-export function computeUserDocumentPurgeAfter(deletionOrClosureIso: string): string {
src/modules\document\domain\retention.ts-18-  return new Date(Date.parse(deletionOrClosureIso) + 30 * MS_PER_DAY).toISOString();
src/modules\document\domain\retention.ts-19-}
src/modules\document\domain\retention.ts-20-
src/modules\document\domain\retention.ts:21:/** `privacy-lgpd.md` Â§4: DELIVERY_RECORD = "criaÃ§Ã£o + 180 dias". Used here for
src/modules\document\domain\retention.ts-22- * `DocumentPurgeReceipt` (W3-06) - a non-sensitive proof-of-purge record, same retention
src/modules\document\domain\retention.ts-23- * rationale as intents/attempts (process evidence, not third-party data), reusing the existing
src/modules\document\domain\retention.ts-24- * class instead of inventing a new one for a single new entity. */
src/modules\document\domain\retention.ts-25-export function computeDeliveryRecordPurgeAfter(createdAtIso: string): string {
--
src/modules\subject\domain\audit-event.ts-1-/**
src/modules\subject\domain\audit-event.ts-2- * AuditEvent do mÃ³dulo subject â€” agregado-irmÃ£o do AuditEvent de expiration
src/modules\subject\domain\audit-event.ts-3- * (src/modules/expiration/domain/audit-event.ts), mesma forma/mesmas garantias
src/modules\subject\domain\audit-event.ts:4: * (append-only, redigido, sempre na mesma transaÃ§Ã£o do agregado), mas resourceType prÃ³prio
src/modules\subject\domain\audit-event.ts-5- * (TrackedSubject/RequirementAssignment) em vez de generalizar o tipo jÃ¡ em produÃ§Ã£o â€”
src/modules\subject\domain\audit-event.ts-6- * mesmo princÃ­pio de agregados-irmÃ£os jÃ¡ aplicado em 06-domain-model-automated-chasing.md.
src/modules\subject\domain\audit-event.ts-7- */
src/modules\subject\domain\audit-event.ts-8-import { defaultRedactor } from "../../../shared/observability/redactor.js";
--
src/modules\document\domain\upload-slot.ts-18-  status: UploadSlotStatus;
src/modules\document\domain\upload-slot.ts-19-  quarantineKey: string;
src/modules\document\domain\upload-slot.ts-20-  reservedAt: string;
src/modules\document\domain\upload-slot.ts-21-  expiresAt: string;
src/modules\document\domain\upload-slot.ts:22:  retentionClass: Extract<DocumentRetentionClass, "TRANSIENT">;
src/modules\document\domain\upload-slot.ts-23-  purgeAfter: string;
src/modules\document\domain\upload-slot.ts-24-  version: number;
src/modules\document\domain\upload-slot.ts-25-  updatedAt: string;
src/modules\document\domain\upload-slot.ts-26-  /** Present only while `status === "RESERVED"` (discovery pointer for

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 8 "entityType:" src/modules/subject/application/guest-rate-limiter.ts src/modules/subject/application/initial-invite-rate-limiter.ts src/modules/identity/application/quota.ts src/modules/import/application/import-service.ts src/modules/import/application/import-commit-service.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 176ms:
src/modules/import/application/import-commit-service.ts-82-  // nÃ£o do version original lido no inÃ­cio da funÃ§Ã£o).
src/modules/import/application/import-commit-service.ts-83-  let current: ImportJob = job;
src/modules/import/application/import-commit-service.ts-84-  let createdCount = 0;
src/modules/import/application/import-commit-service.ts-85-
src/modules/import/application/import-commit-service.ts-86-  for (const entry of pending) {
src/modules/import/application/import-commit-service.ts-87-    const dedupExternalId = entry.row.externalId ?? syntheticRowDedupKey(jobId, entry.rowNumber);
src/modules/import/application/import-commit-service.ts-88-    const claimed = await deps.store.putIfAbsent<ImportDedupRecord>({
src/modules/import/application/import-commit-service.ts-89-      ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
src/modules/import/application/import-commit-service.ts:90:      entityType: "ImportDedupRecord",
src/modules/import/application/import-commit-service.ts-91-      tenantId: ctx.tenant.tenantId,
src/modules/import/application/import-commit-service.ts-92-      externalId: dedupExternalId,
src/modules/import/application/import-commit-service.ts-93-      subjectId: "", // preenchido abaixo, apÃ³s createSubject() confirmar - placeholder aceitÃ¡vel aqui pois a claim em si (nÃ£o o valor) Ã© o que garante idempotÃªncia.
src/modules/import/application/import-commit-service.ts-94-      createdAt: deps.now(),
src/modules/import/application/import-commit-service.ts-95-    });
src/modules/import/application/import-commit-service.ts-96-
src/modules/import/application/import-commit-service.ts-97-    if (claimed) {
src/modules/import/application/import-commit-service.ts-98-      try {
src/modules/import/application/import-commit-service.ts-99-        const subject = await deps.subjects.createSubject(ctx, {
src/modules/import/application/import-commit-service.ts-100-          type: entry.row.type,
src/modules/import/application/import-commit-service.ts-101-          displayName: entry.row.displayName,
src/modules/import/application/import-commit-service.ts-102-          notes: entry.row.notes,
src/modules/import/application/import-commit-service.ts-103-          tags: entry.row.tags,
src/modules/import/application/import-commit-service.ts-104-        });
src/modules/import/application/import-commit-service.ts-105-        await deps.store.update<ImportDedupRecord>({
src/modules/import/application/import-commit-service.ts-106-          ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
src/modules/import/application/import-commit-service.ts:107:          entityType: "ImportDedupRecord",
src/modules/import/application/import-commit-service.ts-108-          tenantId: ctx.tenant.tenantId,
src/modules/import/application/import-commit-service.ts-109-          externalId: dedupExternalId,
src/modules/import/application/import-commit-service.ts-110-          subjectId: subject.subjectId,
src/modules/import/application/import-commit-service.ts-111-          createdAt: deps.now(),
src/modules/import/application/import-commit-service.ts-112-        });
src/modules/import/application/import-commit-service.ts-113-        createdCount += 1;
src/modules/import/application/import-commit-service.ts-114-      } catch (err) {
src/modules/import/application/import-commit-service.ts-115-        if (err instanceof QuotaExceededError) {
--
src/modules/import/application/import-service.ts-116-      const existingJobId = (record as { responseRef?: string } | undefined)?.responseRef;
src/modules/import/application/import-service.ts-117-      if (!existingJobId) throw new ConflictError("reserveImport idempotency record missing responseRef.");
src/modules/import/application/import-service.ts-118-      jobId = existingJobId;
src/modules/import/application/import-service.ts-119-    } else {
src/modules/import/application/import-service.ts-120-      jobId = this.ids.newImportJobId();
src/modules/import/application/import-service.ts-121-      const jobExpiresAt = new Date(Date.parse(now) + IMPORT_JOB_TTL_SECONDS * 1000).toISOString();
src/modules/import/application/import-service.ts-122-      const job: ImportJob = {
src/modules/import/application/import-service.ts-123-        ...importJobKey(ctx.tenant.tenantId, jobId),
src/modules/import/application/import-service.ts:124:        entityType: "ImportJob",
src/modules/import/application/import-service.ts-125-        jobId,
src/modules/import/application/import-service.ts-126-        tenantId: ctx.tenant.tenantId,
src/modules/import/application/import-service.ts-127-        targetEntityType: "TrackedSubject",
src/modules/import/application/import-service.ts-128-        status: "UPLOADED",
src/modules/import/application/import-service.ts-129-        createdByUserId: ctx.principal.userId,
src/modules/import/application/import-service.ts-130-        checksumSha256: input.checksumSha256,
src/modules/import/application/import-service.ts-131-        mappingVersion: 1,
src/modules/import/application/import-service.ts-132-        expiresAt: jobExpiresAt,
--
src/modules/subject/application/initial-invite-rate-limiter.ts-12- * mais barato que esse acoplamento), reaproveitado 3 vezes com chaves diferentes.
src/modules/subject/application/initial-invite-rate-limiter.ts-13- */
src/modules/subject/application/initial-invite-rate-limiter.ts-14-import { QuotaExceededError } from "../../../shared/errors/app-error.js";
src/modules/subject/application/initial-invite-rate-limiter.ts-15-import type { EntityKey, SubjectStore } from "../ports/subject-store.js";
src/modules/subject/application/initial-invite-rate-limiter.ts-16-import { createHash } from "node:crypto";
src/modules/subject/application/initial-invite-rate-limiter.ts-17-
src/modules/subject/application/initial-invite-rate-limiter.ts-18-export interface InitialInviteRateLimitRecord extends EntityKey {
src/modules/subject/application/initial-invite-rate-limiter.ts-19-  SK: "RATE" | "RATE_DAILY";
src/modules/subject/application/initial-invite-rate-limiter.ts:20:  entityType: "InitialInviteRateLimit";
src/modules/subject/application/initial-invite-rate-limiter.ts-21-  limit: number;
src/modules/subject/application/initial-invite-rate-limiter.ts-22-  windowSeconds: number;
src/modules/subject/application/initial-invite-rate-limiter.ts-23-  count: number;
src/modules/subject/application/initial-invite-rate-limiter.ts-24-  resetAt: string;
src/modules/subject/application/initial-invite-rate-limiter.ts-25-  purgeAfterTtl: number;
src/modules/subject/application/initial-invite-rate-limiter.ts-26-}
src/modules/subject/application/initial-invite-rate-limiter.ts-27-
src/modules/subject/application/initial-invite-rate-limiter.ts-28-function epochSeconds(iso: string): number {
--
src/modules/subject/application/initial-invite-rate-limiter.ts-54-    for (let attempt = 0; attempt < MAX_CONTENTION_RETRIES; attempt++) {
src/modules/subject/application/initial-invite-rate-limiter.ts-55-      const nowIso = this.now();
src/modules/subject/application/initial-invite-rate-limiter.ts-56-      const resetAt = new Date(Date.parse(nowIso) + windowSeconds * 1000).toISOString();
src/modules/subject/application/initial-invite-rate-limiter.ts-57-
src/modules/subject/application/initial-invite-rate-limiter.ts-58-      const existing = await this.store.get<InitialInviteRateLimitRecord>(key);
src/modules/subject/application/initial-invite-rate-limiter.ts-59-      if (!existing) {
src/modules/subject/application/initial-invite-rate-limiter.ts-60-        const created = await this.store.putIfAbsent<InitialInviteRateLimitRecord>({
src/modules/subject/application/initial-invite-rate-limiter.ts-61-          ...key,
src/modules/subject/application/initial-invite-rate-limiter.ts:62:          entityType: "InitialInviteRateLimit",
src/modules/subject/application/initial-invite-rate-limiter.ts-63-          limit,
src/modules/subject/application/initial-invite-rate-limiter.ts-64-          windowSeconds,
src/modules/subject/application/initial-invite-rate-limiter.ts-65-          count: 1,
src/modules/subject/application/initial-invite-rate-limiter.ts-66-          resetAt,
src/modules/subject/application/initial-invite-rate-limiter.ts-67-          purgeAfterTtl: epochSeconds(resetAt),
src/modules/subject/application/initial-invite-rate-limiter.ts-68-        });
src/modules/subject/application/initial-invite-rate-limiter.ts-69-        if (created) return;
src/modules/subject/application/initial-invite-rate-limiter.ts-70-        continue;
--
src/modules/subject/application/guest-rate-limiter.ts-7- * (nunca `TENANT#...` â€” o convidado nÃ£o tem tenant resolvido antes da validaÃ§Ã£o do token).
src/modules/subject/application/guest-rate-limiter.ts-8- */
src/modules/subject/application/guest-rate-limiter.ts-9-import { QuotaExceededError } from "../../../shared/errors/app-error.js";
src/modules/subject/application/guest-rate-limiter.ts-10-import { epochSecondsFromIso } from "../domain/guest-token.js";
src/modules/subject/application/guest-rate-limiter.ts-11-import type { EntityKey, SubjectStore } from "../ports/subject-store.js";
src/modules/subject/application/guest-rate-limiter.ts-12-
src/modules/subject/application/guest-rate-limiter.ts-13-export interface GuestRateLimitRecord extends EntityKey {
src/modules/subject/application/guest-rate-limiter.ts-14-  SK: "RATE";
src/modules/subject/application/guest-rate-limiter.ts:15:  entityType: "GuestTokenRateLimit";
src/modules/subject/application/guest-rate-limiter.ts-16-  selectorHash: string;
src/modules/subject/application/guest-rate-limiter.ts-17-  limit: number;
src/modules/subject/application/guest-rate-limiter.ts-18-  windowSeconds: number;
src/modules/subject/application/guest-rate-limiter.ts-19-  count: number;
src/modules/subject/application/guest-rate-limiter.ts-20-  resetAt: string;
src/modules/subject/application/guest-rate-limiter.ts-21-  /** TTL fÃ­sico real da tabela â€” achado real de D-047/D-048, mesmo motivo de `GuestTokenPointer.purgeAfterTtl`: `resetAt` sozinho nunca aciona a exclusÃ£o fÃ­sica do DynamoDB. AvanÃ§a a cada escrita para acompanhar `resetAt`. */
src/modules/subject/application/guest-rate-limiter.ts-22-  purgeAfterTtl: number;
src/modules/subject/application/guest-rate-limiter.ts-23-}
--
src/modules/subject/application/guest-rate-limiter.ts-40-    for (let attempt = 0; attempt < MAX_CONTENTION_RETRIES; attempt++) {
src/modules/subject/application/guest-rate-limiter.ts-41-      const nowIso = this.now();
src/modules/subject/application/guest-rate-limiter.ts-42-      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();
src/modules/subject/application/guest-rate-limiter.ts-43-
src/modules/subject/application/guest-rate-limiter.ts-44-      const existing = await this.store.get<GuestRateLimitRecord>(key);
src/modules/subject/application/guest-rate-limiter.ts-45-      if (!existing) {
src/modules/subject/application/guest-rate-limiter.ts-46-        const created = await this.store.putIfAbsent<GuestRateLimitRecord>({
src/modules/subject/application/guest-rate-limiter.ts-47-          ...key,
src/modules/subject/application/guest-rate-limiter.ts:48:          entityType: "GuestTokenRateLimit",
src/modules/subject/application/guest-rate-limiter.ts-49-          selectorHash: input.selectorHash,
src/modules/subject/application/guest-rate-limiter.ts-50-          limit: input.limit,
src/modules/subject/application/guest-rate-limiter.ts-51-          windowSeconds: input.windowSeconds,
src/modules/subject/application/guest-rate-limiter.ts-52-          count: 1,
src/modules/subject/application/guest-rate-limiter.ts-53-          resetAt,
src/modules/subject/application/guest-rate-limiter.ts-54-          purgeAfterTtl: epochSecondsFromIso(resetAt),
src/modules/subject/application/guest-rate-limiter.ts-55-        });
src/modules/subject/application/guest-rate-limiter.ts-56-        if (created) return;
--
src/modules/identity/application/quota.ts-17-  // import Ã© uma superfÃ­cie de processamento em massa com um perfil de abuso diferente.
src/modules/identity/application/quota.ts-18-  | "IMPORT_COUNT"
src/modules/identity/application/quota.ts-19-  | "IMPORT_BYTES"
src/modules/identity/application/quota.ts-20-  | "IMPORT_ROWS";
src/modules/identity/application/quota.ts-21-
src/modules/identity/application/quota.ts-22-export interface TenantQuotaRecord {
src/modules/identity/application/quota.ts-23-  PK: string;
src/modules/identity/application/quota.ts-24-  SK: string;
src/modules/identity/application/quota.ts:25:  entityType: "TenantQuota";
src/modules/identity/application/quota.ts-26-  tenantId: string;
src/modules/identity/application/quota.ts-27-  quotaType: QuotaType;
src/modules/identity/application/quota.ts-28-  limit: number;
src/modules/identity/application/quota.ts-29-  windowSeconds: number;
src/modules/identity/application/quota.ts-30-  count: number;
src/modules/identity/application/quota.ts-31-  resetAt: string;
src/modules/identity/application/quota.ts-32-  killSwitchOverride?: boolean;
src/modules/identity/application/quota.ts-33-}
--
src/modules/identity/application/quota.ts-75-      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
src/modules/identity/application/quota.ts-76-      const nowIso = this.now();
src/modules/identity/application/quota.ts-77-      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();
src/modules/identity/application/quota.ts-78-
src/modules/identity/application/quota.ts-79-      const existing = await this.store.get<TenantQuotaRecord>(key);
src/modules/identity/application/quota.ts-80-      if (!existing) {
src/modules/identity/application/quota.ts-81-        const created = await this.store.putIfAbsent({
src/modules/identity/application/quota.ts-82-          ...key,
src/modules/identity/application/quota.ts:83:          entityType: "TenantQuota",
src/modules/identity/application/quota.ts-84-          tenantId: input.tenantId,
src/modules/identity/application/quota.ts-85-          quotaType: input.quotaType,
src/modules/identity/application/quota.ts-86-          limit: input.limit,
src/modules/identity/application/quota.ts-87-          windowSeconds: input.windowSeconds,
src/modules/identity/application/quota.ts-88-          count: 1,
src/modules/identity/application/quota.ts-89-          resetAt,
src/modules/identity/application/quota.ts-90-        });
src/modules/identity/application/quota.ts-91-        if (created) return;

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 4 "function .*Key|const key" src/modules/subject/application/initial-invite-rate-limiter.ts src/modules/subject/application/guest-rate-limiter.ts; git branch --show-current; git status --short' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 215ms:
src/modules/subject/application/guest-rate-limiter.ts-21-  /** TTL fÃ­sico real da tabela â€” achado real de D-047/D-048, mesmo motivo de `GuestTokenPointer.purgeAfterTtl`: `resetAt` sozinho nunca aciona a exclusÃ£o fÃ­sica do DynamoDB. AvanÃ§a a cada escrita para acompanhar `resetAt`. */
src/modules/subject/application/guest-rate-limiter.ts-22-  purgeAfterTtl: number;
src/modules/subject/application/guest-rate-limiter.ts-23-}
src/modules/subject/application/guest-rate-limiter.ts-24-
src/modules/subject/application/guest-rate-limiter.ts:25:function guestRateLimitKey(selectorHash: string): { PK: string; SK: "RATE" } {
src/modules/subject/application/guest-rate-limiter.ts-26-  return { PK: `GUESTTOKEN#${selectorHash}#RATE`, SK: "RATE" };
src/modules/subject/application/guest-rate-limiter.ts-27-}
src/modules/subject/application/guest-rate-limiter.ts-28-
src/modules/subject/application/guest-rate-limiter.ts-29-const MAX_CONTENTION_RETRIES = 20;
--
src/modules/subject/application/guest-rate-limiter.ts-35-  ) {}
src/modules/subject/application/guest-rate-limiter.ts-36-
src/modules/subject/application/guest-rate-limiter.ts-37-  /** Consome 1 unidade da janela; lanÃ§a QuotaExceededError quando esgotado (mapeado a 429). */
src/modules/subject/application/guest-rate-limiter.ts-38-  async consume(input: { selectorHash: string; limit: number; windowSeconds: number }): Promise<void> {
src/modules/subject/application/guest-rate-limiter.ts:39:    const key = guestRateLimitKey(input.selectorHash);
src/modules/subject/application/guest-rate-limiter.ts-40-    for (let attempt = 0; attempt < MAX_CONTENTION_RETRIES; attempt++) {
src/modules/subject/application/guest-rate-limiter.ts-41-      const nowIso = this.now();
src/modules/subject/application/guest-rate-limiter.ts-42-      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();
src/modules/subject/application/guest-rate-limiter.ts-43-
develop
?? docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/subject/application/initial-invite-rate-limiter.ts | Select-Object -First 65' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 233ms:
/**
 * Rate limit do convite inicial automatizado (M10 cluster 4, D-049) Ã¢â‚¬â€ 3 limites concretos
 * (nÃƒÂ£o "a decidir depois"), verificados ANTES da criaÃƒÂ§ÃƒÂ£o do `DocumentRequest` quando o envio
 * ÃƒÂ© solicitado: excedÃƒÂª-los bloqueia a criaÃƒÂ§ÃƒÂ£o com `QuotaExceededError`/429, nunca cria
 * parcialmente (mesma disciplina fail-closed de `TenantEntitlement`, D-038) Ã¢â‚¬â€ diferente de
 * falha de SES pÃƒÂ³s-criaÃƒÂ§ÃƒÂ£o, que ÃƒÂ© best-effort (`DocumentRequestService`).
 *
 * Mesmo mecanismo de janela fixa de `GuestRateLimiter` (nÃƒÂ£o `TenantQuotaService` do mÃƒÂ³dulo
 * identity Ã¢â‚¬â€ reusar isso exigiria adicionar tipos de quota especÃƒÂ­ficos de `subject` a um
 * union type fechado de outro mÃƒÂ³dulo, o acoplamento reverso que `shared-must-not-reach-modules`
 * jÃƒÂ¡ existe para prevenir num nÃƒÂ­vel anÃƒÂ¡logo; duplicar esse mecanismo pequeno e jÃƒÂ¡ testado ÃƒÂ©
 * mais barato que esse acoplamento), reaproveitado 3 vezes com chaves diferentes.
 */
import { QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { EntityKey, SubjectStore } from "../ports/subject-store.js";
import { createHash } from "node:crypto";

export interface InitialInviteRateLimitRecord extends EntityKey {
  SK: "RATE" | "RATE_DAILY";
  entityType: "InitialInviteRateLimit";
  limit: number;
  windowSeconds: number;
  count: number;
  resetAt: string;
  purgeAfterTtl: number;
}

function epochSeconds(iso: string): number {
  return Math.floor(Date.parse(iso) / 1000);
}

function recipientHash(recipientEmail: string): string {
  return createHash("sha256").update(recipientEmail.trim().toLowerCase()).digest("hex").slice(0, 32);
}

const MAX_CONTENTION_RETRIES = 20;

export class InitialInviteRateLimiter {
  constructor(
    private readonly store: SubjectStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  /** Consome as 3 janelas (hora/dia por tenant, 24h por tenant+destinatÃƒÂ¡rio) - lanÃƒÂ§a no
   * PRIMEIRO limite excedido, nunca consome as demais depois de falhar (evita gastar quota
   * de janelas mais largas por uma tentativa que jÃƒÂ¡ vai ser rejeitada). */
  async consumeInitialInvite(tenantId: string, recipientEmail: string): Promise<void> {
    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS`, SK: "RATE" }, 20, 60 * 60);
    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS`, SK: "RATE_DAILY" }, 100, 24 * 60 * 60);
    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS#RECIPIENT#${recipientHash(recipientEmail)}`, SK: "RATE" }, 3, 24 * 60 * 60);
  }

  private async consume(key: { PK: string; SK: "RATE" | "RATE_DAILY" }, limit: number, windowSeconds: number): Promise<void> {
    for (let attempt = 0; attempt < MAX_CONTENTION_RETRIES; attempt++) {
      const nowIso = this.now();
      const resetAt = new Date(Date.parse(nowIso) + windowSeconds * 1000).toISOString();

      const existing = await this.store.get<InitialInviteRateLimitRecord>(key);
      if (!existing) {
        const created = await this.store.putIfAbsent<InitialInviteRateLimitRecord>({
          ...key,
          entityType: "InitialInviteRateLimit",
          limit,
          windowSeconds,
          count: 1,

codex
## Achados bloqueantes

1. **O algoritmo nÃ£o garante a exclusÃ£o de â€œtodos os dadosâ€ do tenant.** Um `Scan` paginado nÃ£o oferece snapshot transacional entre pÃ¡ginas. Escritas criadas ou movidas para uma parte jÃ¡ percorrida da tabela podem nunca ser observadas. Persistir `lastScanCursor` sÃ³ retoma a mesma travessia inconsistente; nÃ£o fecha a corrida. Depois de `COMPLETED`, nÃ£o hÃ¡ segunda varredura convergente nem fence que impeÃ§a novas escritas. Como o bloqueio imediato foi excluÃ­do do escopo, o mecanismo precisa de outra barreira de criaÃ§Ã£o ou de varreduras repetidas atÃ© convergÃªncia. Sem isso, a promessa central Ã© falsa.

2. **A afirmaÃ§Ã£o de que toda linha persistida possui `tenantId` prÃ³prio Ã© falsa no cÃ³digo real.** As entidades de domÃ­nio principais geralmente o possuem, inclusive `IdentityMapping`, `GuestTokenPointer` e `TextractJob`, mas hÃ¡ exceÃ§Ãµes reais:

   - `ReminderPolicyRef` Ã© gravado sem `tenantId` e sem `version` em [reminder-policy-service.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-policy-service.ts:239).
   - `InitialInviteRateLimitRecord` tem chave contendo o tenant, mas nÃ£o atributo `tenantId`, em [initial-invite-rate-limiter.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/initial-invite-rate-limiter.ts:18).
   - `GuestTokenRateLimit` nÃ£o possui `tenantId`; conhece apenas `selectorHash`, em [guest-rate-limiter.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/guest-rate-limiter.ts:13). Isso contradiz especificamente a alegaÃ§Ã£o de `data-model.md` de que essa exceÃ§Ã£o tenantless carrega `tenantId`.

   Portanto, `FilterExpression tenantId = :t` nÃ£o descobre toda a superfÃ­cie. No caso do rate limit de guest, seria necessÃ¡rio relacionÃ¡-lo ao `GuestTokenPointer` ou aceitar sua expiraÃ§Ã£o por TTL como parte explicitamente provada do mecanismo.

3. **O branch genÃ©rico nÃ£o Ã© compatÃ­vel com as formas reais das linhas.** `buildVersionedUpdate` e `buildVersionedDelete` exigem `version` e `tenantId`; vÃ¡rias linhas auxiliares/append-only nÃ£o possuem um ou ambos. `AuditEvent`, por exemplo, Ã© deliberadamente append-only e nÃ£o possui `version` em [audit-event.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/expiration/domain/audit-event.ts:13). A proposta nÃ£o define uma taxonomia de formas nem uma estratÃ©gia segura por tipo, logo nÃ£o consegue sequer enfileirar ou apagar todas as linhas encontradas usando o mecanismo OCC proposto.

4. **â€œQualquer `entityType` fora de Documentâ€ Ã© uma polÃ­tica fail-open destrutiva.** AusÃªncia de S3 nÃ£o significa ausÃªncia de efeitos colaterais ou regras de retenÃ§Ã£o. `AuditEvent`/`SubjectAuditEvent`, `OutboxEvent`, intents, attempts, receipts, jobs e ponteiros tÃªm semÃ¢nticas distintas. Um tipo novo entraria automaticamente na exclusÃ£o fÃ­sica sem revisÃ£o. Isso tambÃ©m permite apagar outbox ainda nÃ£o publicada, evidÃªncia de entrega e auditoria de seguranÃ§a/processo. O worker precisa de allowlist fechada e estratÃ©gia explÃ­cita por tipo/retention class; tipo desconhecido deve falhar fechado e alarmar.

5. **A proposta sobrescreve um GSI6 jÃ¡ usado como mÃ¡quina de trabalho por outros processos.** Linhas como `UploadSlot` e ocorrÃªncias podem estar em estados de reconciliaÃ§Ã£o `PENDING`/`CLAIMED`. Substituir `GSI6PK/GSI6SK` por `PURGE_PENDING` rouba o item de seu workflow corrente. A condiÃ§Ã£o proposta verifica `legalHold`, mas nÃ£o define precondiÃ§Ãµes por estado nem exclusÃ£o mÃºtua com writers dos demais workflows. Uma escrita concorrente posterior tambÃ©m pode remover ou substituir o ponteiro de purge, pois somente o futuro setter de `legalHold` conhece hoje a fence `PURGE_CLAIMED`.

6. **O tratamento de concorrÃªncia entre Scan e batch Ã© incompleto e pode impedir progresso indefinidamente.** Se uma das linhas tiver sido apagada ou atualizada depois do Scan, a condiÃ§Ã£o OCC cancela todo o `TransactWriteItems`. NÃ£o hÃ¡ algoritmo para identificar a entrada conflitante, descartÃ¡-la/reler e continuar com as demais. Repetir cegamente o mesmo lote mantÃ©m atÃ© 24 itens vÃ¡lidos presos atrÃ¡s de um conflito.

7. **`purgeAfter = now` contradiz a fonte normativa vigente.** [privacy-lgpd.md](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/privacy-lgpd.md:37) estabelece encerramento/exclusÃ£o `+30 dias` para `ACCOUNT_ACTIVE`, `CORE_USER_DATA` e `USER_DOCUMENT`; o cÃ³digo materializa essa regra para documentos em [retention.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/domain/retention.ts:17). O escopo reduzido por Marcelo nÃ£o registra uma revogaÃ§Ã£o dessa matriz. O direito de eliminaÃ§Ã£o nÃ£o implica ignorar a polÃ­tica aprovada por classe nem holds legais. O mecanismo pode aceitar uma decisÃ£o final jÃ¡ amadurecida, mas deve calcular `purgeAfter` conforme a classe ou receber um cutoff autorizado por uma decisÃ£o normativa explÃ­cita.

8. **O estado `COMPLETED` nÃ£o tem semÃ¢ntica verificÃ¡vel.** O serviÃ§o apenas enfileira linhas; GSI6 Ã© eventualmente consistente e o worker pode produzir `STUCK`. A proposta nÃ£o define como provar ausÃªncia residual em DynamoDB/S3, ausÃªncia de candidatos pendentes/claimed/stuck ou tÃ©rmino de efeitos externos. Marcar `COMPLETED` ao terminar o Scan seria â€œdescoberta concluÃ­daâ€, nÃ£o â€œexclusÃ£o concluÃ­daâ€.

## Achados nÃ£o-bloqueantes

1. **Scan raro pode ser defensÃ¡vel, mas a justificativa de custo estÃ¡ incompleta.** Um `Scan` filtrado consome leitura sobre a tabela inteira, nÃ£o apenas sobre as linhas do tenant. Seu custo e impacto crescem com todos os tenants. SÃ£o necessÃ¡rios limite de consumo, backoff, mÃ©tricas, limite temporal e orquestraÃ§Ã£o continuÃ¡vel. Em escala pequena, ainda pode ser a escolha proporcional.

2. **PaginaÃ§Ã£o com cursor nÃ£o resolve sozinha timeout de Lambda.** Falta definir quem agenda a continuaÃ§Ã£o, como o cursor e a pÃ¡gina sÃ£o committed atomicamente, lease da execuÃ§Ã£o, tratamento de duas invocaÃ§Ãµes concorrentes e orÃ§amento por invocaÃ§Ã£o. O request precisa de OCC/lease, nÃ£o apenas `lastScanCursor`.

3. **â€œ25 Ã© o limite real de `TransactWriteItems`â€ estÃ¡ desatualizado.** O limite atual da operaÃ§Ã£o Ã© 100 aÃ§Ãµes, embora usar 25 possa continuar sendo uma decisÃ£o conservadora. O documento deve chamar 25 de batch escolhido, nÃ£o limite do DynamoDB.

4. **`TenantDeletionRequest` pode apagar a si prÃ³prio.** Se carregar o mesmo `tenantId` na tabela escaneada, serÃ¡ descoberto e enfileirado, contrariando seu papel de tombstone auditÃ¡vel. Deve viver fora do conjunto eliminÃ¡vel ou ser excluÃ­do explicitamente, com retenÃ§Ã£o definida. Isso reforÃ§a a necessidade de allowlist por tipo.

5. **O esquema genÃ©rico de `GSI6SK` usa `entityType`/â€œSK derivadoâ€ sem codificaÃ§Ã£o canÃ´nica.** Chaves podem conter delimitadores e alguns itens nÃ£o possuem identificador simples. A chave deve ser construÃ­da com PK/SK ou identificador codificado sem ambiguidade e testado contra colisÃµes/tamanho mÃ¡ximo.

6. **`STUCK` genÃ©rico nÃ£o Ã© suficiente como modelo operacional.** Faltam `lastErrorCode`, timestamps, razÃ£o do bloqueio e estratÃ©gia de redrive. Para falhas sem efeito externo, cinco leases expirados tambÃ©m nÃ£o representam necessariamente cinco tentativas de delete.

## Nota: 3,4/10

## Veredito

**REPROVADO â€” reabrir a rodada.**

O `Scan` raro Ã© potencialmente aceitÃ¡vel como primitive de descoberta, e o reuso de claim/lease Ã© uma boa base, mas o desenho atual nÃ£o entrega sua garantia declarada. Ele perde linhas reais sem `tenantId`, nÃ£o converge diante de escritas concorrentes, sobrepÃµe workflows existentes do GSI6, aplica deleÃ§Ã£o genÃ©rica fail-open e viola a retenÃ§Ã£o normativa de 30 dias.

A rodada seguinte precisa, no mÃ­nimo, introduzir: barreira ou convergÃªncia pÃ³s-Scan; inventÃ¡rio real das formas persistidas; allowlist fechada por `entityType` com estratÃ©gia/retention class; coordenaÃ§Ã£o com estados GSI6 existentes; isolamento de conflitos por item; e definiÃ§Ã£o verificÃ¡vel de conclusÃ£o.
tokens used
66.635
## Achados bloqueantes

1. **O algoritmo nÃ£o garante a exclusÃ£o de â€œtodos os dadosâ€ do tenant.** Um `Scan` paginado nÃ£o oferece snapshot transacional entre pÃ¡ginas. Escritas criadas ou movidas para uma parte jÃ¡ percorrida da tabela podem nunca ser observadas. Persistir `lastScanCursor` sÃ³ retoma a mesma travessia inconsistente; nÃ£o fecha a corrida. Depois de `COMPLETED`, nÃ£o hÃ¡ segunda varredura convergente nem fence que impeÃ§a novas escritas. Como o bloqueio imediato foi excluÃ­do do escopo, o mecanismo precisa de outra barreira de criaÃ§Ã£o ou de varreduras repetidas atÃ© convergÃªncia. Sem isso, a promessa central Ã© falsa.

2. **A afirmaÃ§Ã£o de que toda linha persistida possui `tenantId` prÃ³prio Ã© falsa no cÃ³digo real.** As entidades de domÃ­nio principais geralmente o possuem, inclusive `IdentityMapping`, `GuestTokenPointer` e `TextractJob`, mas hÃ¡ exceÃ§Ãµes reais:

   - `ReminderPolicyRef` Ã© gravado sem `tenantId` e sem `version` em [reminder-policy-service.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-policy-service.ts:239).
   - `InitialInviteRateLimitRecord` tem chave contendo o tenant, mas nÃ£o atributo `tenantId`, em [initial-invite-rate-limiter.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/initial-invite-rate-limiter.ts:18).
   - `GuestTokenRateLimit` nÃ£o possui `tenantId`; conhece apenas `selectorHash`, em [guest-rate-limiter.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/guest-rate-limiter.ts:13). Isso contradiz especificamente a alegaÃ§Ã£o de `data-model.md` de que essa exceÃ§Ã£o tenantless carrega `tenantId`.

   Portanto, `FilterExpression tenantId = :t` nÃ£o descobre toda a superfÃ­cie. No caso do rate limit de guest, seria necessÃ¡rio relacionÃ¡-lo ao `GuestTokenPointer` ou aceitar sua expiraÃ§Ã£o por TTL como parte explicitamente provada do mecanismo.

3. **O branch genÃ©rico nÃ£o Ã© compatÃ­vel com as formas reais das linhas.** `buildVersionedUpdate` e `buildVersionedDelete` exigem `version` e `tenantId`; vÃ¡rias linhas auxiliares/append-only nÃ£o possuem um ou ambos. `AuditEvent`, por exemplo, Ã© deliberadamente append-only e nÃ£o possui `version` em [audit-event.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/expiration/domain/audit-event.ts:13). A proposta nÃ£o define uma taxonomia de formas nem uma estratÃ©gia segura por tipo, logo nÃ£o consegue sequer enfileirar ou apagar todas as linhas encontradas usando o mecanismo OCC proposto.

4. **â€œQualquer `entityType` fora de Documentâ€ Ã© uma polÃ­tica fail-open destrutiva.** AusÃªncia de S3 nÃ£o significa ausÃªncia de efeitos colaterais ou regras de retenÃ§Ã£o. `AuditEvent`/`SubjectAuditEvent`, `OutboxEvent`, intents, attempts, receipts, jobs e ponteiros tÃªm semÃ¢nticas distintas. Um tipo novo entraria automaticamente na exclusÃ£o fÃ­sica sem revisÃ£o. Isso tambÃ©m permite apagar outbox ainda nÃ£o publicada, evidÃªncia de entrega e auditoria de seguranÃ§a/processo. O worker precisa de allowlist fechada e estratÃ©gia explÃ­cita por tipo/retention class; tipo desconhecido deve falhar fechado e alarmar.

5. **A proposta sobrescreve um GSI6 jÃ¡ usado como mÃ¡quina de trabalho por outros processos.** Linhas como `UploadSlot` e ocorrÃªncias podem estar em estados de reconciliaÃ§Ã£o `PENDING`/`CLAIMED`. Substituir `GSI6PK/GSI6SK` por `PURGE_PENDING` rouba o item de seu workflow corrente. A condiÃ§Ã£o proposta verifica `legalHold`, mas nÃ£o define precondiÃ§Ãµes por estado nem exclusÃ£o mÃºtua com writers dos demais workflows. Uma escrita concorrente posterior tambÃ©m pode remover ou substituir o ponteiro de purge, pois somente o futuro setter de `legalHold` conhece hoje a fence `PURGE_CLAIMED`.

6. **O tratamento de concorrÃªncia entre Scan e batch Ã© incompleto e pode impedir progresso indefinidamente.** Se uma das linhas tiver sido apagada ou atualizada depois do Scan, a condiÃ§Ã£o OCC cancela todo o `TransactWriteItems`. NÃ£o hÃ¡ algoritmo para identificar a entrada conflitante, descartÃ¡-la/reler e continuar com as demais. Repetir cegamente o mesmo lote mantÃ©m atÃ© 24 itens vÃ¡lidos presos atrÃ¡s de um conflito.

7. **`purgeAfter = now` contradiz a fonte normativa vigente.** [privacy-lgpd.md](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/privacy-lgpd.md:37) estabelece encerramento/exclusÃ£o `+30 dias` para `ACCOUNT_ACTIVE`, `CORE_USER_DATA` e `USER_DOCUMENT`; o cÃ³digo materializa essa regra para documentos em [retention.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/domain/retention.ts:17). O escopo reduzido por Marcelo nÃ£o registra uma revogaÃ§Ã£o dessa matriz. O direito de eliminaÃ§Ã£o nÃ£o implica ignorar a polÃ­tica aprovada por classe nem holds legais. O mecanismo pode aceitar uma decisÃ£o final jÃ¡ amadurecida, mas deve calcular `purgeAfter` conforme a classe ou receber um cutoff autorizado por uma decisÃ£o normativa explÃ­cita.

8. **O estado `COMPLETED` nÃ£o tem semÃ¢ntica verificÃ¡vel.** O serviÃ§o apenas enfileira linhas; GSI6 Ã© eventualmente consistente e o worker pode produzir `STUCK`. A proposta nÃ£o define como provar ausÃªncia residual em DynamoDB/S3, ausÃªncia de candidatos pendentes/claimed/stuck ou tÃ©rmino de efeitos externos. Marcar `COMPLETED` ao terminar o Scan seria â€œdescoberta concluÃ­daâ€, nÃ£o â€œexclusÃ£o concluÃ­daâ€.

## Achados nÃ£o-bloqueantes

1. **Scan raro pode ser defensÃ¡vel, mas a justificativa de custo estÃ¡ incompleta.** Um `Scan` filtrado consome leitura sobre a tabela inteira, nÃ£o apenas sobre as linhas do tenant. Seu custo e impacto crescem com todos os tenants. SÃ£o necessÃ¡rios limite de consumo, backoff, mÃ©tricas, limite temporal e orquestraÃ§Ã£o continuÃ¡vel. Em escala pequena, ainda pode ser a escolha proporcional.

2. **PaginaÃ§Ã£o com cursor nÃ£o resolve sozinha timeout de Lambda.** Falta definir quem agenda a continuaÃ§Ã£o, como o cursor e a pÃ¡gina sÃ£o committed atomicamente, lease da execuÃ§Ã£o, tratamento de duas invocaÃ§Ãµes concorrentes e orÃ§amento por invocaÃ§Ã£o. O request precisa de OCC/lease, nÃ£o apenas `lastScanCursor`.

3. **â€œ25 Ã© o limite real de `TransactWriteItems`â€ estÃ¡ desatualizado.** O limite atual da operaÃ§Ã£o Ã© 100 aÃ§Ãµes, embora usar 25 possa continuar sendo uma decisÃ£o conservadora. O documento deve chamar 25 de batch escolhido, nÃ£o limite do DynamoDB.

4. **`TenantDeletionRequest` pode apagar a si prÃ³prio.** Se carregar o mesmo `tenantId` na tabela escaneada, serÃ¡ descoberto e enfileirado, contrariando seu papel de tombstone auditÃ¡vel. Deve viver fora do conjunto eliminÃ¡vel ou ser excluÃ­do explicitamente, com retenÃ§Ã£o definida. Isso reforÃ§a a necessidade de allowlist por tipo.

5. **O esquema genÃ©rico de `GSI6SK` usa `entityType`/â€œSK derivadoâ€ sem codificaÃ§Ã£o canÃ´nica.** Chaves podem conter delimitadores e alguns itens nÃ£o possuem identificador simples. A chave deve ser construÃ­da com PK/SK ou identificador codificado sem ambiguidade e testado contra colisÃµes/tamanho mÃ¡ximo.

6. **`STUCK` genÃ©rico nÃ£o Ã© suficiente como modelo operacional.** Faltam `lastErrorCode`, timestamps, razÃ£o do bloqueio e estratÃ©gia de redrive. Para falhas sem efeito externo, cinco leases expirados tambÃ©m nÃ£o representam necessariamente cinco tentativas de delete.

## Nota: 3,4/10

## Veredito

**REPROVADO â€” reabrir a rodada.**

O `Scan` raro Ã© potencialmente aceitÃ¡vel como primitive de descoberta, e o reuso de claim/lease Ã© uma boa base, mas o desenho atual nÃ£o entrega sua garantia declarada. Ele perde linhas reais sem `tenantId`, nÃ£o converge diante de escritas concorrentes, sobrepÃµe workflows existentes do GSI6, aplica deleÃ§Ã£o genÃ©rica fail-open e viola a retenÃ§Ã£o normativa de 30 dias.

A rodada seguinte precisa, no mÃ­nimo, introduzir: barreira ou convergÃªncia pÃ³s-Scan; inventÃ¡rio real das formas persistidas; allowlist fechada por `entityType` com estratÃ©gia/retention class; coordenaÃ§Ã£o com estados GSI6 existentes; isolamento de conflitos por item; e definiÃ§Ã£o verificÃ¡vel de conclusÃ£o.

# W3-07 â€” Round 2 (Claude trÃ©plica, respondendo Ã  Rodada 1 do Codex, nota 3,4/10, 8 achados bloqueantes)

Rodada 1 achou problemas reais e mudou o desenho de fundo, nÃ£o sÃ³ detalhes. Resposta reorganiza
o mecanismo: **cascata de exclusÃ£o deixa de reusar a fila GSI6** (achado 5) e vira um
discover-then-delete direto e conditioned, com convergÃªncia explÃ­cita (achado 1/8) e uma
taxonomia fechada por `entityType` (achados 2/3/4).

## ResoluÃ§Ã£o achado 5 (colisÃ£o de GSI6) â€” mudanÃ§a estrutural: sem fila GSI6 para cascata

Aceito por completo: reusar `WORKSTATE#PURGE_PENDING/CLAIMED` roubaria linhas de workflows jÃ¡
donos legÃ­timos daquele ponteiro (`UploadSlot` reconciliaÃ§Ã£o, ocorrÃªncias CLAIMED). **Cascata
de tenant nÃ£o usa GSI6 de forma nenhuma.** Novo mecanismo: `TenantCascadeDeletionService`
executa, por pÃ¡gina de `Scan`, um `TransactWriteItems` de **`Delete` direto, condicionado**
(sem intermediÃ¡rio de fila/claim) â€” nenhum ponteiro GSI6 Ã© lido, escrito ou removido por este
mecanismo. GSI6 continua exclusivo dos seus 4 donos existentes.

Isso tambÃ©m simplifica o branch "genÃ©rico" que a achado 4 criticava: nÃ£o existe mais um
worker genÃ©rico reusando `DocumentPurgeWorker` â€” a exclusÃ£o acontece diretamente no
`TenantCascadeDeletionService`, sÃ­ncrona ao Scan (dentro do orÃ§amento de uma invocaÃ§Ã£o, ver
achado 1 abaixo para paginaÃ§Ã£o entre invocaÃ§Ãµes).

## ResoluÃ§Ã£o achado 4 (fail-open) + achado 3 (formas incompatÃ­veis) â€” allowlist fechada por entityType

Nenhum `entityType` desconhecido Ã© tocado â€” **fail-closed**: um `entityType` fora da tabela
abaixo interrompe o processamento daquela linha, loga um alarme dedicado
(`tenant_cascade_unknown_entity_type`) e marca o `TenantDeletionRequest` como
`BLOCKED_UNKNOWN_ENTITY_TYPE` (nÃ£o avanÃ§a, nÃ£o apaga nada alÃ©m do jÃ¡ processado) â€” exige
revisÃ£o humana antes de estender a tabela. TrÃªs categorias, mapeadas explicitamente por
`entityType` real (levantamento contra `data-model.md` Â§2 + os `entityType` literais no
cÃ³digo):

| Categoria | `entityType`s | EstratÃ©gia de exclusÃ£o |
|---|---|---|
| **OCC versionado padrÃ£o** | `ExpirationItem`, `ReminderPolicy`, `ReminderOccurrence`, `Document` (+ S3 real via `pickObjectToDelete`, mesma funÃ§Ã£o do W3-06), `DocumentPurgeReceipt`, `ExtractedField`, `ExtractionRun`, `NotificationIntent`, `NotificationAttempt`, `Channel`, `Provider`, `TrackedSubject`, `RequirementAssignment`, `TenantEntitlement`, `ItemWatch`, `DocumentRequest`, `DocumentSubmission`, `DocumentChasingOccurrence`, `DocumentChasingIntent`, `ImportJob`, `ImportDedupRecord`, `User`, `IdentityMapping`, `GuestTokenPointer` | `Delete` condicionado (`attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t`) â€” mesmo shape de `buildVersionedDelete`, sem `extraConditions` de GSI6 |
| **Append-only, sem `version`** | `AuditEvent`, `WebhookInbox` (evento jÃ¡ processado, nunca mutado apÃ³s criaÃ§Ã£o) | `Delete` condicionado sÃ³ em existÃªncia (`attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t`) â€” sem checagem de versÃ£o, correto porque nada mais escreve nessas linhas depois de criadas |
| **Auto-purgÃ¡vel, excluÃ­do do Scan** | `GuestTokenRateLimit`, `InitialInviteRateLimitRecord` | **NÃ£o tocado por este mecanismo.** Achado real confirmado no cÃ³digo (`guest-rate-limiter.ts`/`initial-invite-rate-limiter.ts`): ambos jÃ¡ carregam `purgeAfterTtl` (TTL real da tabela) e nunca guardam PII alÃ©m de um hash â€” jÃ¡ se autoextinguem independente de qualquer exclusÃ£o de tenant, e (`GuestTokenRateLimit` especificamente) nem tem `tenantId` disponÃ­vel no momento da escrita por design (convidado ainda nÃ£o resolvido). Documentado como exclusÃ£o deliberada, nÃ£o uma lacuna. |

**CorreÃ§Ã£o mecÃ¢nica que a achado 2/3 forÃ§a, feita agora**: `PolicyRef`
(`reminder-policy.ts:69-72`) ganha `tenantId` (valor jÃ¡ disponÃ­vel em todo call site,
`reminder-policy-service.ts:239` jÃ¡ tem `input.tenantId` em mÃ£o) â€” sem isso, `PolicyRef` nem
seria descoberto pelo Scan (achado 2) nem teria o que uma condiÃ§Ã£o `tenantId = :t` checaria.
AdiÃ§Ã£o puramente aditiva, nenhum call site existente quebra (o campo era simplesmente omitido,
nunca lido por ninguÃ©m hoje). CorreÃ§Ã£o real de um gap prÃ©-existente descoberto por esta rodada,
nÃ£o uma mudanÃ§a de comportamento do mÃ³dulo reminder.

## ResoluÃ§Ã£o achado 1 + achado 8 (convergÃªncia/"COMPLETED" sem garantia real)

`TenantDeletionRequest.status` ganha uma mÃ¡quina de passos explÃ­cita, nÃ£o um Ãºnico Scan:

```
DISCOVERING (passo N) â†’ CONVERGENCE_CHECK â†’ [zero linhas encontradas? COMPLETED : DISCOVERING (passo N+1), atÃ© maxPasses]
                                            â†’ excedeu maxPasses com linhas restantes? â†’ INCOMPLETE_NEEDS_REVIEW
```

Cada passo `DISCOVERING`: `Scan` completo paginado (cursor persistido em
`TenantDeletionRequest.lastScanCursor` para retomar entre invocaÃ§Ãµes/timeout de Lambda â€” achado
nÃ£o-bloqueante 2 da Rodada 1), deletando cada linha encontrada pela tabela de categorias acima.
Ao fim do Scan completo (cursor esgotado), um **novo** `Scan` (`CONVERGENCE_CHECK`) confirma se
`tenantId = :t` ainda retorna alguma linha:
- **Zero linhas**: `COMPLETED` â€” esta Ã© a Ãºnica condiÃ§Ã£o que justifica esse status (resolve
  achado 8: "completo" agora significa "uma varredura real nÃ£o encontrou mais nada", nÃ£o "o
  primeiro Scan terminou").
- **Linhas restantes** (escritas concorrentes legÃ­timas durante o passo anterior, ou itens que
  falharam sua condiÃ§Ã£o de exclusÃ£o numa passagem anterior): mais um passo `DISCOVERING` sobre
  as linhas remanescentes, atÃ© `maxPasses` (proposto: 5 â€” generoso, jÃ¡ que cada passo sÃ³
  processa o que sobrou, converge rÃ¡pido na prÃ¡tica jÃ¡ que nÃ£o hÃ¡ um fluxo real criando dado
  novo indefinidamente para um tenant sendo excluÃ­do).
- **`INCOMPLETE_NEEDS_REVIEW`** apÃ³s `maxPasses`: estado terminal honesto, nÃ£o um falso
  `COMPLETED` â€” **limitaÃ§Ã£o explÃ­cita e documentada** (nÃ£o escondida): sem bloqueio imediato de
  uso (fora de escopo, decisÃ£o do Marcelo), um tenant que continua sendo usado ativamente
  durante a exclusÃ£o pode, em tese, criar dado mais rÃ¡pido do que os passos conseguem drenar;
  isso Ã© o preÃ§o concreto de nÃ£o ter bloqueio imediato, registrado aqui para quem decidir sobre
  a feature de produto completa (W3-07 alÃ©m deste mecanismo) saber exatamente o que estÃ¡
  aceitando ao adiar aquele trabalho.

## ResoluÃ§Ã£o achado 6 (falha de lote inteiro por um item conflitante)

`TransactWriteItems` em lote de 25 (mantido como valor escolhido, nÃ£o limite da API â€” correÃ§Ã£o
do achado nÃ£o-bloqueante 3 da Rodada 1, que apontou 100 como o limite real) que falhar cai para
**exclusÃ£o sequencial item a item** dentro do mesmo lote (condicionada individualmente, sem
transaÃ§Ã£o) â€” isola qual item especÃ­fico perdeu a condiÃ§Ã£o (jÃ¡ apagado por outro processo
legÃ­timo, ou mutado concorrentemente) sem bloquear os outros 24. Um item que falhe sua condiÃ§Ã£o
sequencialmente Ã© simplesmente pulado (nunca reportado como erro) â€” se ainda pertencer ao
tenant na prÃ³xima passagem de convergÃªncia, serÃ¡ descoberto e reprocessado lÃ¡.

## ResoluÃ§Ã£o achado 7 (`purgeAfter` imediato contradiz a matriz de 30 dias)

Aceito â€” remoÃ§Ã£o completa do conceito de `purgeAfter` deste mecanismo. `TenantCascadeDeletionService.execute(tenantId)`
**nÃ£o decide quando apagar** â€” apaga imediatamente quando chamado, e a responsabilidade de sÃ³
chamÃ¡-lo depois que a janela normativa (30 dias, revisÃ£o de `legalHold`, confirmaÃ§Ã£o de
identidade) jÃ¡ se encerrou pertence inteiramente ao chamador (a feature DSR completa, fora de
escopo aqui). Este documento nÃ£o reabre nem reinterpreta `privacy-lgpd.md` Â§4 â€” sÃ³ entrega a
capacidade de "apagar agora, dado que apagar agora jÃ¡ foi autorizado por outro processo".
`legalHold` (achado jÃ¡ resolvido no W3-06) continua respeitado: a condiÃ§Ã£o de exclusÃ£o de
qualquer `Document` inclui `attribute_not_exists(legalHold) OR legalHold = :false`, mesma
condiÃ§Ã£o jÃ¡ normativa desde D-061.

## Estado do design apÃ³s Rodada 2

MudanÃ§a estrutural (sem GSI6), taxonomia fechada de 3 categorias cobrindo todo `entityType`
real do sistema (com um gap real fechado, `PolicyRef.tenantId`), convergÃªncia via Scan repetido
atÃ© zero ou `maxPasses`, isolamento de conflito por item, e remoÃ§Ã£o do conceito de `purgeAfter`
deste mecanismo. PeÃ§o reavaliaÃ§Ã£o completa.

# W3-07 â€” Round 3 (Claude, autocrÃ­tica antes de reenviar â€” correÃ§Ã£o de uma alegaÃ§Ã£o nÃ£o verificada da Rodada 2)

Antes de pedir reavaliaÃ§Ã£o, refiz o levantamento de `entityType` que a Rodada 1 (achado 2/3) jÃ¡
tinha provado errado uma vez â€” a Rodada 2 listou uma tabela sem verificar `version`/`tenantId`
linha a linha contra o cÃ³digo real. Fiz isso agora de verdade (grep exaustivo de todo
`entityType: "..."` em `src/modules/**`, 39 tipos reais, cada um com `version`/`tenantId`
confirmados por leitura direta do arquivo). Dois achados novos, mais sÃ©rios que uma correÃ§Ã£o de
tabela:

## Achado novo 1 â€” segunda tabela fÃ­sica, nÃ£o coberta por nenhuma versÃ£o anterior deste desenho

`Session`/`LoginAttempt`/`DeviceSession` (mÃ³dulo `bff`) vivem em uma **tabela DynamoDB
inteiramente separada** (`bff-session-table`, `infra/main.tf:317`/
`infra/modules/bff-session-table/`), nÃ£o na tabela principal (`exptrk-dev-table`). Nenhuma
versÃ£o anterior deste documento mencionava isso â€” um `Scan` de `exptrk-dev-table`, por mais
correto que fosse, **nunca encontraria essas 3 entidades**. Corrigido: `TenantCascadeDeletionService`
recebe uma lista de tabelas a varrer (`[{ tableName: mainTable, ... }, { tableName: bffSessionTable, ... }]`),
mesma lÃ³gica de descoberta+exclusÃ£o aplicada a cada uma independentemente. As trÃªs jÃ¡ tÃªm
`purgeAfterTtl` real (TTL nativo da tabela, `session.ts:33-36`/`68`) â€” isso NÃƒO as isenta de
inclusÃ£o aqui (diferente de `GuestTokenRateLimit`, ver achado 2): elas carregam dado de sessÃ£o
real (refresh token cifrado, `cognitoSubject`) que o direito de eliminaÃ§Ã£o cobre â€” esperar o TTL
natural (atÃ© dias) em vez de apagar imediatamente na exclusÃ£o de tenant seria mais fraco que o
precedente jÃ¡ estabelecido (`Document`/W3-06 nunca trata TTL como mecanismo primÃ¡rio).

## Achado novo 2 â€” tabela de classificaÃ§Ã£o corrigida (verificada, nÃ£o presumida)

Duas categorias reais (a terceira, exclusÃ£o do Scan, continua sÃ³ para os 2 casos jÃ¡
justificados na Rodada 2), com a lista completa dos 39 `entityType`s reais do sistema:

| Categoria | CondiÃ§Ã£o de exclusÃ£o | `entityType`s (verificados) |
|---|---|---|
| **OCC versionado** (`version` presente) | `Delete` condicionado: `attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t` (+ `legalHold` para `Document`) | `DeviceSession`, `Document`\*, `DocumentChasingIntent`, `DocumentChasingOccurrence`, `DocumentPurgeReceipt`, `DocumentRequest`, `DocumentRequestDeliveryPreference`, `DocumentSubmission`, `ExpirationItem`, `ExtractedField`, `ExtractionRun`, `GuestTokenPointer`, `ImportJob`, `ItemWatch`, `LoginAttempt`, `NotificationAttempt`, `NotificationEntitlements`, `NotificationIntent`, `NotificationPreferences`, `ReminderOccurrence`, `ReminderPolicy`, `RequirementAssignment`, `Session`, `TextractJob`, `TrackedSubject`, `UploadSlot`, `User` (\* `Document` tambÃ©m apaga o objeto S3 real antes, via `pickObjectToDelete` do W3-06) |
| **Sem versÃ£o** (nunca mutado apÃ³s criaÃ§Ã£o â€” ledger/pointer/counter) | `Delete` condicionado sÃ³ em existÃªncia: `attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t` | `AuditEvent`, `SubjectAuditEvent`, `IdentityMapping`, `ImportDedupRecord`, `NotificationAttemptLookup`, `OutboxEvent`, `WebhookInbox`, `IdempotencyRecord`, `TenantQuota`, `ReminderPolicyRef` (apÃ³s correÃ§Ã£o abaixo) |
| **ExcluÃ­do do Scan, autopurgÃ¡vel** | NÃ£o tocado | `GuestTokenRateLimit`, `InitialInviteRateLimit` â€” sem `tenantId` no momento da escrita (por design, convidado ainda nÃ£o resolvido / mesmo mecanismo duplicado), jÃ¡ carregam `purgeAfterTtl` real, nunca guardam PII alÃ©m de um hash |

**CorreÃ§Ã£o de cÃ³digo real que este levantamento forÃ§a** (achado genuÃ­no, nÃ£o cosmÃ©tico):
`ReminderPolicyRef` (`reminder-policy.ts:69-72`) hoje **nÃ£o tem `tenantId`** (confirmado por
leitura direta, zero ocorrÃªncias no bloco da interface) â€” a Rodada 2 jÃ¡ tinha notado isso, mas
por engano o classificou junto da categoria versionada; `PolicyRef` tambÃ©m nÃ£o tem `version`
(Ã© um ponteiro criado uma vez via `attribute_not_exists`, nunca atualizado). CorreÃ§Ã£o: adicionar
sÃ³ `tenantId` (valor jÃ¡ disponÃ­vel em `reminder-policy-service.ts:239`, `input.tenantId`) e
classificar na categoria **sem versÃ£o** (existÃªncia apenas) â€” nÃ£o na OCC-versionada.

Nenhum `entityType` fora desta tabela Ã© tocado â€” mesma regra fail-closed jÃ¡ proposta na Rodada 2
(achado 4 original: `BLOCKED_UNKNOWN_ENTITY_TYPE`, alarme dedicado, revisÃ£o humana antes de
estender a tabela).

## Estado do design apÃ³s Rodada 3

Mecanismo (sem GSI6, convergÃªncia via re-scan atÃ© zero, fallback sequencial por item conflitante
em lote, sem `purgeAfter`/imediato por definiÃ§Ã£o) mantido da Rodada 2. Esta rodada corrige uma
alegaÃ§Ã£o que nÃ£o tinha sido verificada linha a linha (mesmo erro de categoria que a Rodada 1 jÃ¡
tinha pego uma vez â€” nÃ£o repetido desta vez, confirmado por grep exaustivo) e adiciona a segunda
tabela fÃ­sica (`bff-session-table`) que nenhuma rodada anterior tinha considerado. PeÃ§o
reavaliaÃ§Ã£o completa, com atenÃ§Ã£o especial a qualquer `entityType` que este levantamento ainda
possa ter classificado errado.

---
status: active
owner: engineering
authority: normative
---

# CritÃ©rios de revisÃ£o conjunta Claudeâ†”Codex, por eixo

Fonte Ãºnica dos critÃ©rios de avaliaÃ§Ã£o (nome, peso, definiÃ§Ã£o) usados nas revisÃµes conjuntas Claudeâ†”Codex (`AGENTS.md` Â§4). Achados/rodadas de uma execuÃ§Ã£o especÃ­fica do protocolo (ex. `docs/architecture/reviews/m3.5-runtime-design/`) registram evidÃªncia de uma revisÃ£o pontual â€” nunca redefinem ou duplicam a tabela de pesos; apenas linkam para a seÃ§Ã£o correspondente aqui.

Adaptado (nÃ£o copiado) do padrÃ£o equivalente do projeto irmÃ£o `event-discovery-platform` (mesmo usuÃ¡rio) â€” mesmas fontes normativas e mesmos nomes/definiÃ§Ãµes de critÃ©rio (sÃ£o padrÃµes de indÃºstria: ISO/IEC 25010, AWS Well-Architected, ATAM, DORA/Core-4, mais a literatura de context engineering para dev assistido por IA), mas com **pesos recalibrados por convergÃªncia independente Claudeâ†”Codex especificamente para este repositÃ³rio** â€” nunca herdados sem revisÃ£o. Rodada de convergÃªncia completa (2 propostas independentes + reconciliaÃ§Ã£o) em `docs/engineering/reviews/joint-review-criteria-round1-{claude,codex-prompt,codex-output}.*`; ambas as partes â‰¥9.0 de confianÃ§a na proposta prÃ³pria, sem necessidade de rodada formal de desacordo (mesma condiÃ§Ã£o de convergÃªncia rÃ¡pida que o projeto irmÃ£o documenta para o eixo de contexto).

Cada eixo evolui apenas se o prÃ³prio critÃ©rio se mostrar mal calibrado em uso real â€” registrar por que, junto com a mudanÃ§a, e nÃ£o reabrir a cada nova revisÃ£o sobre o mesmo eixo.

**Eixos formalizados (9)**: Arquitetura, Qualidade de Engenharia, Engenharia de Contexto, SeguranÃ§a da InformaÃ§Ã£o e AppSec, Privacidade e GovernanÃ§a de Dados, OperaÃ§Ãµes/SRE e Continuidade de NegÃ³cio, GovernanÃ§a de IA e Controles Internos, GovernanÃ§a JurÃ­dica/Contratual/Terceiros, GovernanÃ§a de Produto e ServiÃ§o Multi-tenant. **FinOps fica como 10Âº eixo, aprovado por decisÃ£o do usuÃ¡rio mas ainda sem critÃ©rios definidos** â€” nÃ£o usar atÃ© ganhar seÃ§Ã£o prÃ³pria aqui; economia unitÃ¡ria hoje Ã© sÃ³ um critÃ©rio dentro de Produto/ServiÃ§o Multi-tenant, nÃ£o um substituto do eixo.

---

## Eixo: Arquitetura

Convergido em 2026-08-19. Pesos puxados para cima em Reliability & Fault Recovery e Data Model & Consistency â€” refletem que G8 (recuperaÃ§Ã£o real de falha assÃ­ncrona) Ã© o gate de engenharia mais caro e mais recentemente testado deste projeto, e que o single-table DynamoDB com 6 GSIs e duas exceÃ§Ãµes de particionamento (GSI3/GSI6, chaves globais) jÃ¡ produziu bugs reais (inconsultabilidade de PK, ponteiro removido incorretamente) â€” risco concreto, nÃ£o hipotÃ©tico.

| # | CritÃ©rio | Peso |
|---:|---|---:|
| 1 | Domain Fit & Simplicity | 8% |
| 2 | Reliability & Fault Recovery | 16% |
| 3 | Event & Integration Correctness | 11% |
| 4 | Data Model & Consistency | 13% |
| 5 | Security & Privacy | 13% |
| 6 | Modifiability & Evolvability | 7% |
| 7 | Observability & Operability | 8% |
| 8 | Testability & Delivery Safety | 8% |
| 9 | Cost & Resource Governance | 5% |
| 10 | Performance & Scalability Fitness | 4% |
| 11 | Architecture Governance & Traceability | 7% |

## Eixo: Qualidade de engenharia

Convergido em 2026-08-19. Eixo distinto do de Arquitetura â€” craft de cÃ³digo, disciplina de testes, rigor de CI, tooling, disciplina de documentaÃ§Ã£o/processo, gestÃ£o de dÃ­vida tÃ©cnica; nÃ£o redecide design de sistema. Ajuste principal: **Delivery, Release & Recovery Discipline** subiu de forma expressiva (padrÃ£o de origem tinha 8%) â€” nÃ£o porque a entrega jÃ¡ seja madura, mas justamente o oposto: nenhum deploy real foi validado ainda neste projeto, e o Ãºnico bloqueador restante para fechar G8 Ã© exatamente executar deploy real + IAM negativo + DLQ/redrive + EventBridge Scheduler em sandbox AWS. Uma lacuna real de evidÃªncia pesa mais, nÃ£o menos â€” mesmo raciocÃ­nio aplicado ao eixo de contexto abaixo.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Code Correctness & Defensive Design | 11% | PreservaÃ§Ã£o de invariantes via validaÃ§Ã£o explÃ­cita, tratamento de falha, transiÃ§Ãµes de estado seguras, ausÃªncia de corrupÃ§Ã£o silenciosa. |
| 2 | Test Effectiveness & Coverage Discipline | 15% | EvidÃªncia crÃ­vel de unit/integration/contract/negative/failure/regression focada em risco real, nÃ£o apenas contagem/cobertura de linha. SuÃ­te extensa baseada sÃ³ em fakes nÃ£o pontua alto â€” precisa provar propriedades reais (DynamoDB/IAM/SQS/EventBridge) quando o risco Ã© real. |
| 3 | CI Quality Gates & Merge Safety | 11% | Enforcement determinÃ­stico de checks obrigatÃ³rios em toda mudanÃ§a, sem bypass informal, exceÃ§Ãµes formalmente governadas. |
| 4 | Type Safety, Static Analysis & Automated Enforcement | 9% | Uso efetivo de tipagem estrita, lint, dependency-cruiser (grafo real de imports) e fitness functions customizadas para prevenir classes de defeito automaticamente. |
| 5 | Readability, Consistency & Implementation Maintainability | 8% | CÃ³digo coeso, nomeado claramente, estilo consistente, DRY/KISS/YAGNI aplicado com julgamento, barato de modificar. |
| 6 | Delivery, Release & Recovery Discipline | 11% | Artefatos/ambientes reproduzÃ­veis, promoÃ§Ã£o segura, evidÃªncia de deploy, rollback, validaÃ§Ã£o de recuperaÃ§Ã£o â€” critÃ©rio de saÃ­da real de G8, nÃ£o secundÃ¡rio neste estÃ¡gio. |
| 7 | Dependency & Supply-Chain Hygiene | 7% | DependÃªncias controladas, lockfiles, actions/tools pinados, triagem de vulnerabilidade com `expiresAt` formal (`docs/engineering/exceptions.md`), SBOM. |
| 8 | Debuggability & Operational Feedback | 7% | DiagnÃ³sticos estruturados e privacy-safe (`SecureLogger`/`Redactor`), correlation context, erros acionÃ¡veis, suficientes para investigar falhas assÃ­ncronas. |
| 9 | Developer Experience & Reproducibility | 5% | Instalar/validar/testar/rodar o repositÃ³rio a partir de checkout limpo, com comandos pinados e documentados, de baixo atrito. |
| 10 | Documentation Quality & Process Discipline | 5% | Specs, standards, decisÃµes e registros de mudanÃ§a claros e autoritativos, com rigor de processo proporcional a projeto solo. |
| 11 | Documentationâ€“Implementation Drift Control | 6% | Checks determinÃ­sticos ou executados regularmente que detectam divergÃªncia entre documentaÃ§Ã£o, cÃ³digo, infra, config de CI e realidade implantada. |
| 12 | Technical-Debt & Continuous-Improvement Practice | 5% | Registro honesto de atalhos e controles falhos, com dono, trigger, expiry quando aplicÃ¡vel, e follow-through baseado em evidÃªncia. |

## Eixo: Engenharia de contexto

Convergido em 2026-08-19. Avalia a qualidade do prÃ³prio sistema de documentaÃ§Ã£o/contexto do projeto â€” nÃ£o cÃ³digo, nÃ£o arquitetura de sistema, nÃ£o craft de engenharia (eixos distintos, jÃ¡ cobertos acima). Ajuste principal: **Context Routing & Progressive Disclosure** subiu de forma expressiva (padrÃ£o de origem tinha 13%) pelo mesmo raciocÃ­nio do eixo anterior â€” este projeto nÃ£o tem um "context router" dedicado equivalente ao `system-overview.md` do projeto irmÃ£o (`AGENTS.md` Â§2 cumpre parcialmente esse papel, mas Ã© mais fraco), e o volume de documentos normativos/histÃ³ricos/handoffs jÃ¡ torna caro escolher o read-set mÃ­nimo correto. Uma lacuna estrutural real pesa mais no critÃ©rio que ela mede, nunca menos.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Canonicalidade, Autoridade & NÃ£o-DuplicaÃ§Ã£o | 15% | Cada fato normativo tem exatamente uma fonte de verdade; derivados (Ã­ndices, mapas, resumos) sÃ£o explicitamente nÃ£o autoritativos e referenciam a fonte em vez de copiÃ¡-la. Ver `docs/architecture/README.md` Â§"PrecedÃªncia de fontes". |
| 2 | Clareza de PapÃ©is & Proporcionalidade | 9% | Cada documento tem propÃ³sito, escopo e autoridade inequÃ­vocos, sem sobreposiÃ§Ã£o semÃ¢ntica relevante. Estrutura proporcional ao tamanho real do projeto â€” sem sofisticaÃ§Ã£o antecipada (`docs/engineering/principles.md` #1). |
| 3 | Context Routing & Progressive Disclosure | 15% | Humanos e agentes conseguem identificar e carregar o menor conjunto suficiente de contexto para cada tipo de tarefa. Lacuna reconhecida: sem context router dedicado hoje â€” `AGENTS.md` Â§2 Ã© o candidato mais prÃ³ximo, mais fraco que o padrÃ£o do projeto irmÃ£o. |
| 4 | CorrespondÃªncia com a Realidade & Controle de Drift | 16% | O sistema distingue intenÃ§Ã£o documentada, implementaÃ§Ã£o declarada e estado efetivamente observado, reconciliando divergÃªncias explicitamente. `ARCHITECTURE STATUS`/gates G1-G8 sÃ³ mudam com evidÃªncia operacional real (`docs/engineering/principles.md` #6). |
| 5 | Lifecycle, ProveniÃªncia & EvoluÃ§Ã£o do Conhecimento | 12% | Arquitetura vigente vs. histÃ³rica, ADR aceito imutÃ¡vel e supersedido por ADR novo, metadata de status/autoridade, lifecycle de evidÃªncias (`docs/architecture/history/`). |
| 6 | Rastreabilidade de DecisÃµes, Trabalho & Triggers | 10% | DecisÃµes caras, trabalho adiado, dÃ­vida tÃ©cnica e evidÃªncias permanecem conectados; gatilhos de reavaliaÃ§Ã£o concretos e verificÃ¡veis (`docs/engineering/exceptions.md`, `decisions-log.md`). |
| 7 | Higiene de Contexto & Sinal-RuÃ­do | 8% | SÃ³ conhecimento durÃ¡vel Ã© promovido a destinos canÃ´nicos; ausÃªncia de versÃµes concorrentes ou documentos Ã³rfÃ£os. |
| 8 | Portabilidade AgnÃ³stica entre Agentes de IA | 6% | Regras essenciais utilizÃ¡veis por diferentes agentes/ferramentas â€” `AGENTS.md`/`CLAUDE.md` jÃ¡ seguem essa separaÃ§Ã£o. |
| 9 | Auditabilidade & Enforcement do Sistema de Contexto | 9% | AfirmaÃ§Ãµes sobre a saÃºde do contexto verificÃ¡veis por evidÃªncia datada e revisÃµes reproduzÃ­veis; achados com lifecycle claro (aberto/corrigido/adiado/arquivado). |

## Eixo: SeguranÃ§a da InformaÃ§Ã£o e AppSec

Convergido em 2026-08-19 (fontes: OWASP ASVS 5.0, OWASP Top 10:2025, OWASP SAMM, prÃ¡ticas de least-privilege IAM AWS Lambda/DynamoDB). Rodada de convergÃªncia completa em `docs/engineering/reviews/security-axis-criteria-round1-{claude,codex-prompt,codex-output}.*` â€” convergÃªncia forte: os dois critÃ©rios de maior peso (isolamento multi-tenant, least-privilege IAM) saÃ­ram com pesos **idÃªnticos** nas duas propostas independentes, sem precisar de reconciliaÃ§Ã£o. Eixo avalia confidencialidade/integridade/disponibilidade/isolamento diante de abuso intencional e falhas de fronteira de confianÃ§a â€” nÃ£o reavalia craft de cÃ³digo, arquitetura funcional ou disciplina de teste geral (eixos distintos); sÃ³ quando esses elementos sÃ£o controle ou evidÃªncia especificamente de seguranÃ§a.

Pesos calibrados pelo risco real jÃ¡ observado neste projeto: GSI3/GSI6 sÃ£o exceÃ§Ãµes de particionamento com blast radius cross-tenant deliberado que jÃ¡ produziram bug real nesta sessÃ£o (nÃ£o risco hipotÃ©tico); o pipeline assÃ­ncrono (M3.5/G8) passou por 3 rodadas reais de revisÃ£o que encontraram bugs de contrato, race condition e limpeza de Ã­ndice.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Isolamento Multi-Tenant & AutorizaÃ§Ã£o por Objeto | 18% | `tenantId` derivado exclusivamente da identidade validada e propagado por chaves, queries, eventos, idempotÃªncia e auditoria; proteÃ§Ã£o contra IDOR e acesso cross-tenant em toda rota/worker. Maior peso do eixo â€” risco mais concreto e jÃ¡ testado deste projeto (GSI3/GSI6). |
| 2 | Least-Privilege IAM & ContenÃ§Ã£o de Blast Radius | 14% | Capability/role especÃ­fica por Lambda (`ScopedLambdaFunction`), sem grants implÃ­citos/curinga. `gsi3Read()`/`gsi6Read()` como capabilities exclusivas de lista fechada de workers â€” permissÃµes tenant-facing devem produzir `AccessDenied` real (Camada 3, ainda pendente). |
| 3 | AutenticaÃ§Ã£o, SessÃ£o & GestÃ£o de Identidade | 11% | ValidaÃ§Ã£o completa de JWT Cognito (assinatura/issuer/audience/expiraÃ§Ã£o/claims), revogaÃ§Ã£o global/por dispositivo, TTL/rotaÃ§Ã£o, MFA proporcional. Nenhuma autorizaÃ§Ã£o confia em identidade/tenant fornecido pelo cliente. |
| 4 | Integridade do Pipeline AssÃ­ncrono & Fronteiras de Mensagem | 14% | Eventos EventBridge/SQS/Streams/outbox autenticados pela infra, validados por schema, vinculados ao tenant, protegidos contra replay/duplicaÃ§Ã£o/roteamento cruzado. Claim/estado/outbox preservam atomicidade; retry/DLQ/redrive/sweeper nÃ£o contornam autorizaÃ§Ã£o. Peso alto: Ã© onde G8 jÃ¡ encontrou bugs reais. |
| 5 | ValidaÃ§Ã£o de Entrada, Injection & Fail-Closed | 9% | ValidaÃ§Ã£o server-side por contrato (Ajv) em toda borda â€” HTTP, filas, dados persistidos. Entrada invÃ¡lida/estado impossÃ­vel/erro de autorizaÃ§Ã£o falha de modo fechado, sem mutaÃ§Ã£o parcial nem detalhe interno vazado. |
| 6 | ProteÃ§Ã£o de Dados SensÃ­veis, Segredos & Criptografia | 9% | MinimizaÃ§Ã£o/classificaÃ§Ã£o de PII e tokens, criptografia em trÃ¢nsito/repouso, segredos fora de cÃ³digo/log/artefato. `SecureLogger`/`Redactor` deve valer tambÃ©m em exceÃ§Ãµes, traces, eventos e DLQ â€” nÃ£o sÃ³ no caminho feliz. |
| 7 | Logging Seguro, DetecÃ§Ã£o & Resposta a Incidentes | 8% | Eventos de autenticaÃ§Ã£o/autorizaÃ§Ã£o negada/alteraÃ§Ã£o privilegiada/acesso a GSI global produzem trilha Ã­ntegra, correlacionÃ¡vel, privacy-safe (OWASP A09:2025). Alarmes com threshold acionÃ¡vel e runbook de contenÃ§Ã£o/revogaÃ§Ã£o/investigaÃ§Ã£o. |
| 8 | ConfiguraÃ§Ã£o Segura da Plataforma & SuperfÃ­cie Exposta | 6% | API Gateway/Cognito/Lambda/DynamoDB/SQS/EventBridge/Streams com defaults seguros, exposiÃ§Ã£o mÃ­nima, retenÃ§Ã£o adequada. MudanÃ§as de CDK devem ser capazes de detectar regressÃ£o (wildcard de Ã­ndice, grant excessivo, handler placeholder â€” jÃ¡ aconteceu nesta sessÃ£o). |
| 9 | ResistÃªncia a Abuso, DoS & ExaustÃ£o de Custo | 5% | Rate limits, quotas por tenant, concorrÃªncia reservada, limites de batch/paginaÃ§Ã£o impedem que um tenant, poison message ou evento amplificado degrade outros tenants ou gere custo descontrolado; retries/reconciliaÃ§Ã£o nÃ£o podem virar multiplicador de ataque. |
| 10 | VerificaÃ§Ã£o Adversarial & GestÃ£o ContÃ­nua de Risco | 6% | EvidÃªncia especÃ­fica de seguranÃ§a (threat model vivo, testes de abuso/cross-tenant/replay, assertions de IAM/IaC) â€” inclusive teste em AWS real quando emulaÃ§Ã£o nÃ£o prova o controle (Camada 3). Achados com severidade/dono/prazo; nova superfÃ­cie (upload, provedor externo, frontend, webhook) reabre a anÃ¡lise antes da implementaÃ§Ã£o. |

## Eixo: Privacidade e GovernanÃ§a de Dados

Convergido em 2026-08-19 (fontes: LGPD/orientaÃ§Ãµes ANPD sobre RIPD/direitos/incidentes/transferÃªncias, DAMA-DMBOK). Rodada completa em `docs/engineering/reviews/remaining-axes-round1-{claude,codex-prompt,codex-output}.*`. Avalia licitude, finalidade, governanÃ§a e ciclo de vida dos dados â€” nÃ£o controles tÃ©cnicos de acesso (isolamento multi-tenant/IAM jÃ¡ pertencem ao eixo de SeguranÃ§a, nÃ£o duplicado aqui). Maior peso do eixo em RetenÃ§Ã£o/ExclusÃ£o porque Ã© o workflow mais greenfield e envolve mÃºltiplos stores (DynamoDB, S3, filas, backups, providers).

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | InventÃ¡rio, ClassificaÃ§Ã£o, Ownership & Linhagem de Dados | 15% | InventÃ¡rio versionado de dados pessoais, sensÃ­veis, documentos, metadados, logs, eventos e backups; finalidade, origem, destino, responsÃ¡vel e `retentionClass` rastreÃ¡veis. Nova entidade/campo/integraÃ§Ã£o nÃ£o entra sem classificaÃ§Ã£o. |
| 2 | Base Legal, Finalidade & MinimizaÃ§Ã£o | 16% | Base legal e finalidade definidas por tratamento, com teste de necessidade/proporcionalidade. Uso secundÃ¡rio exige reavaliaÃ§Ã£o, nÃ£o reutilizaÃ§Ã£o implÃ­cita. |
| 3 | Direitos do Titular & Portabilidade Efetiva | 16% | ConfirmaÃ§Ã£o/acesso/correÃ§Ã£o/exportaÃ§Ã£o/oposiÃ§Ã£o/exclusÃ£o com fluxo autenticado, prazos, estados e evidÃªncia de conclusÃ£o. |
| 4 | RetenÃ§Ã£o, Legal Hold, ExclusÃ£o VerificÃ¡vel & Backups | 17% | Prazo por classe com evento inicial, prazo final e mecanismo executÃ¡vel. ExclusÃ£o alcanÃ§a DynamoDB/S3/Ã­ndices/filas/providers/restores; testes provam que dado excluÃ­do nÃ£o ressurge apÃ³s restore. |
| 5 | LocalizaÃ§Ã£o, TransferÃªncia Internacional & Subprocessamento | 14% | RegiÃ£o de produÃ§Ã£o, paÃ­ses de tratamento, subprocessadores e mecanismos jurÃ­dicos de transferÃªncia decididos antes de produÃ§Ã£o â€” decisÃ£o hoje pendente por LGPD, nÃ£o drift silencioso. |
| 6 | RIPD, Risco aos Titulares & Privacy by Design | 10% | CritÃ©rios objetivos de quando elaborar/atualizar RIPD; tratamento de alto risco exige decisÃ£o humana registrada. |
| 7 | Qualidade, CorreÃ§Ã£o & ProveniÃªncia dos Dados | 7% | Dados exatos/completos/atuais vinculados Ã  origem/versÃ£o; inferÃªncia de IA distinguÃ­vel de dado confirmado pelo usuÃ¡rio. |
| 8 | Accountability, EvidÃªncia & Monitoramento de Privacidade | 5% | ResponsÃ¡veis, aprovaÃ§Ãµes, DSRs, holds e exceÃ§Ãµes produzem evidÃªncia auditÃ¡vel, sem virar telemetria excessiva de dado pessoal. |

## Eixo: OperaÃ§Ãµes, SRE e Continuidade de NegÃ³cio

Convergido em 2026-08-19 (fontes: Google SRE Book â€” SLO/error budget dirige decisÃ£o operacional, nÃ£o sÃ³ relatÃ³rio â€”, AWS Well-Architected SaaS Lens Â§Operate). Avalia capacidade de operar/detectar/responder/recuperar o serviÃ§o real â€” o desenho de SLO/DR jÃ¡ existe (`slo.md`/`disaster-recovery.md`) mas nunca foi operacionalizado com dado de produÃ§Ã£o real. Maior peso em Backup/Restore porque "documentado mas nunca testado" Ã© o padrÃ£o de risco mais repetido observado neste projeto (G8 sÃ³ fechou depois de testar de verdade).

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | SLIs, SLOs & Error Budgets AcionÃ¡veis | 16% | SLIs representam resultado percebido (API, reminder freshness, backlog, reconciliaÃ§Ã£o); consumo de error budget condiciona release/priorizaÃ§Ã£o, nÃ£o sÃ³ relatÃ³rio. |
| 2 | Observabilidade Operacional & VisÃ£o por Tenant | 11% | Detecta impacto agregado e noisy neighbor entre tenants â€” `SecureLogger` maduro nÃ£o basta sem dashboard/sinal real. |
| 3 | DetecÃ§Ã£o, Resposta & ComunicaÃ§Ã£o de Incidentes | 15% | Alertas com threshold acionÃ¡vel/dono/escalonamento/runbook; comunicaÃ§Ã£o a tenants/titulares/ANPD quando aplicÃ¡vel; exercÃ­cios comprovam funcionamento sob pressÃ£o. |
| 4 | SaÃºde do Pipeline AssÃ­ncrono & RecuperaÃ§Ã£o de Backlog | 14% | Idade/profundidade de fila, DLQ, outbox, claims, sweeper monitorados; redrive/replay idempotentes e limitados. Peso alto pela criticidade do produto + Camada 3 ainda pendente. |
| 5 | Backup, Restore, RTO/RPO & Continuidade | 18% | PITR/backup configurado nÃ£o basta â€” nota depende de restore real periÃ³dico com RTO/RPO observado. Falha regional Ã© risco aceito explicitamente (regiÃ£o Ãºnica, Stage 0-2), nÃ£o ignorado. |
| 6 | ProntidÃ£o de Deploy, Rollback & MudanÃ§a Operacional | 10% | Deploy com artefato identificÃ¡vel, checks prÃ©/pÃ³s, rollback/roll-forward; mudanÃ§a de schema/GSI/KMS/provider aciona validaÃ§Ã£o proporcional. |
| 7 | Capacidade, DependÃªncias & DegradaÃ§Ã£o Controlada | 9% | Limites AWS/providers/concorrÃªncia/custo conhecidos e testados; kill switches preservam trabalho recuperÃ¡vel. |
| 8 | Post-mortem, ExercÃ­cios & Melhoria ContÃ­nua | 7% | Post-mortem sem culpa com aÃ§Ã£o/dono/prazo; falha recorrente ou aÃ§Ã£o vencida reduz nota. |

## Eixo: GovernanÃ§a de IA e Controles Internos

Convergido em 2026-08-19 (fontes: NIST AI RMF â€” ciclo Govern/Map/Measure/Manage â€”, ISO/IEC 42001). Abrange Claude/Codex construindo e operando o projeto, e futuros componentes de IA/OCR do produto. Avalia autoridade, supervisÃ£o, independÃªncia, proveniÃªncia e gestÃ£o de risco â€” nÃ£o a qualidade genÃ©rica do cÃ³digo gerado (eixo de Engenharia). Maior peso em IndependÃªncia da RevisÃ£o porque Ã© o controle interno central deste projeto (protocolo `AGENTS.md` Â§4).

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Limites de Autoridade, PermissÃµes & SupervisÃ£o Humana | 18% | AÃ§Ãµes permitidas/proibidas/sujeitas a aprovaÃ§Ã£o humana por agente; deploy/merge/exclusÃ£o/produÃ§Ã£o/Type-1/comunicaÃ§Ã£o externa seguem least authority e fail-closed. |
| 2 | AtribuiÃ§Ã£o, ProveniÃªncia & Reprodutibilidade das AÃ§Ãµes | 15% | Toda mudanÃ§a relevante atribuÃ­vel a agente/modelo/sessÃ£o/diff/teste/aprovador â€” reconstruÃ­vel sem exigir armazenamento indiscriminado de prompt sensÃ­vel. |
| 3 | IndependÃªncia da RevisÃ£o & SegregaÃ§Ã£o de FunÃ§Ãµes | 15% | Nota cega, mÃ­nimo de rodadas, mesmo agente nunca simula aprovaÃ§Ã£o independente de si mesmo. Controle interno central do projeto â€” sem ele, "revisÃ£o Claudeâ†”Codex" viraria teatro. |
| 4 | InventÃ¡rio de Casos de Uso & GestÃ£o do Risco de IA | 13% | Usos de IA inventariados por finalidade/impacto/dado/autonomia/reversibilidade; mudanÃ§a relevante reabre avaliaÃ§Ã£o (Govern-Map-Measure-Manage). |
| 5 | AvaliaÃ§Ã£o de CorreÃ§Ã£o, LimitaÃ§Ãµes & Impacto | 12% | Nota alta sem evidÃªncia de arquivo:linha concreta nÃ£o fecha revisÃ£o (jÃ¡ Ã© a prÃ¡tica deste projeto â€” formalizado aqui como critÃ©rio). |
| 6 | ProteÃ§Ã£o de Contexto, Dados & Segredos no Uso de IA | 10% | Apenas contexto necessÃ¡rio enviado a modelo/ferramenta; segredos/dado pessoal/documento de tenant obedecem classificaÃ§Ã£o e retenÃ§Ã£o. |
| 7 | GestÃ£o de Modelos, Ferramentas, Fornecedores & MudanÃ§as | 9% | VersÃ£o/capacidade de modelo/CLI conhecida; upgrade tem avaliaÃ§Ã£o de regressÃ£o e impacto â€” comportamento de fornecedor nÃ£o Ã© controle interno garantido. |
| 8 | Incidentes de IA, ExceÃ§Ãµes & Melhoria ContÃ­nua | 8% | Mecanismo de suspender uso/reverter/registrar falha; bypass/exceÃ§Ã£o com fundamento, prazo e aprovaÃ§Ã£o. |

## Eixo: GovernanÃ§a JurÃ­dica, Contratual e de Terceiros

Convergido em 2026-08-19 (fontes: SOC 2 Trust Services Criteria â€” gestÃ£o de fornecedor, nÃ£o certificaÃ§Ã£o prematura â€”, licenciamento OSS). Avalia obrigaÃ§Ã£o/licenÃ§a/contrato/responsabilidade/risco de dependÃªncia externa â€” SBOM, pinning e integridade tÃ©cnica de build continuam nos eixos de Engenharia/SeguranÃ§a, nÃ£o duplicados aqui.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | PapÃ©is JurÃ­dicos, Responsabilidades & Modelo Contratual | 16% | Define quando o serviÃ§o Ã© controlador/operador/ambos; decisÃ£o pendente recebe dono e gate antes do lanÃ§amento. |
| 2 | InventÃ¡rio, Due Diligence & Monitoramento de Terceiros | 16% | Registro de AWS/modelos de IA/OCR/mensageria/CI com serviÃ§o, dados, criticidade, regiÃ£o, certificaÃ§Ã£o, lock-in e responsÃ¡vel. |
| 3 | Licenciamento OSS, Propriedade Intelectual & Uso de ConteÃºdo | 14% | DependÃªncias/cÃ³digo gerado com licenÃ§a/proveniÃªncia compatÃ­vel com operaÃ§Ã£o comercial; autoria por IA nÃ£o presume ausÃªncia de risco de licenÃ§a. |
| 4 | Termos de Uso, Aviso de Privacidade & Consentimentos | 14% | Termos/avisos compatÃ­veis com comportamento real do produto â€” nÃ£o existem ainda, prÃ©-requisito antes do primeiro usuÃ¡rio real. |
| 5 | DPAs, TransferÃªncias, Incidentes & ObrigaÃ§Ãµes de Fornecedor | 13% | Contrato com operador/suboperador cobre instruÃ§Ãµes, seguranÃ§a, localizaÃ§Ã£o, exclusÃ£o e comunicaÃ§Ã£o tempestiva de incidente (LGPD art. 39). |
| 6 | Compromissos Comerciais, SLA & ProteÃ§Ã£o do Consumidor | 13% | NÃ£o prometer entrega de mensagem quando sÃ³ hÃ¡ tentativa do provider, nem disponibilidade sem arquitetura correspondente. |
| 7 | AprovaÃ§Ã£o, EvidÃªncia, ExceÃ§Ãµes & MudanÃ§a RegulatÃ³ria | 7% | Gatilho de reavaliaÃ§Ã£o em mudanÃ§a regulatÃ³ria/produto/regiÃ£o/provider; aprovaÃ§Ã£o sem dono nÃ£o pontua como controle. |
| 8 | Continuidade, Portabilidade & SaÃ­da de Fornecedor | 7% | DependÃªncia crÃ­tica tem estratÃ©gia proporcional de substituiÃ§Ã£o/exportaÃ§Ã£o â€” dado nÃ£o fica irrecuperavelmente preso sem risco aceito e documentado. |

## Eixo: GovernanÃ§a de Produto e ServiÃ§o Multi-tenant

Convergido em 2026-08-19 (fontes: AWS Well-Architected SaaS Lens â€” onboarding automatizado no control plane, crypto-shredding no offboarding, tenant como conceito de primeira classe). Avalia o serviÃ§o percebido/administrado pelo tenant ao longo do ciclo de vida â€” nÃ£o reavalia isolamento/IAM tÃ©cnico (eixo de SeguranÃ§a). **FinOps fica como eixo prÃ³prio por decisÃ£o do usuÃ¡rio, nÃ£o formalizado ainda** â€” economia unitÃ¡ria aqui Ã© sÃ³ um critÃ©rio de produto, nÃ£o compete em peso com esse eixo futuro.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Lifecycle Automatizado de Tenant | 18% | Onboarding/ativaÃ§Ã£o/convite/mudanÃ§a de plano/suspensÃ£o/offboarding como estados explÃ­citos, idempotentes, auditÃ¡veis â€” hoje o tenant nasce implicitamente no primeiro login (MVP `tenantId=userId`), sem control plane dedicado. |
| 2 | Offboarding, ExportaÃ§Ã£o & DestruiÃ§Ã£o CriptogrÃ¡fica | 16% | "NÃ£o conseguimos deletar um tenant" Ã© falha de compliance, nÃ£o sÃ³ tÃ©cnica; crypto-shredding coordenado com retenÃ§Ã£o/backup/legal hold. |
| 3 | Planos, Entitlements, Quotas & Fairness | 13% | Capacidades/limites como polÃ­tica centralizada e versionada; `TenantQuota` jÃ¡ existe (M1), falta plano de billing real. NÃ£o duplica rate-limiting de seguranÃ§a â€” mede correÃ§Ã£o da oferta do produto. |
| 4 | CorreÃ§Ã£o do ServiÃ§o de Lembretes & ProteÃ§Ã£o do UsuÃ¡rio | 15% | Timezone/DST/opt-out/quiet hours/renovaÃ§Ã£o corrigÃ­veis; lembrete perdido/duplicado/obsoleto tem detecÃ§Ã£o e reparo â€” Ã© a falha mais grave possÃ­vel do produto. |
| 5 | TransparÃªncia, Usabilidade & Acessibilidade | 10% | Estado/erro/atraso comunicado em linguagem clara; ainda nÃ£o se aplica plenamente (sem frontend), mas entra no radar em M4+. |
| 6 | AdministraÃ§Ã£o, Suporte & OperaÃ§Ã£o sob a Ã“tica do Tenant | 10% | Suporte autorizado diagnostica saÃºde/uso/config por tenant com trilha de auditoria, sem acesso informal a conteÃºdo. |
| 7 | MÃ©tricas de Valor, Consumo & Economia UnitÃ¡ria | 10% | AdoÃ§Ã£o/sucesso de lembrete/consumo atribuÃ­vel por tenant de forma privacy-safe â€” germe do futuro eixo FinOps, nÃ£o o eixo em si. |
| 8 | EvoluÃ§Ã£o Unificada & Controle de CustomizaÃ§Ã£o | 8% | Nenhuma feature cria "fork" por tenant; variaÃ§Ã£o por plano usa mecanismo governado (mesmo princÃ­pio jÃ¡ aplicado ao reshard versionado do GSI3). |

## Como adicionar um novo eixo

NÃ£o criar a tabela de critÃ©rios dentro do primeiro doc de auditoria do eixo novo. Seguir o mesmo procedimento de convergÃªncia independente (`AGENTS.md` Â§4) e, ao final, adicionar aqui uma seÃ§Ã£o nova nesse mesmo formato â€” o registro de auditoria referencia a seÃ§Ã£o, nÃ£o a duplica.


2026-08-28T20:22:00.558472Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md,docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md -Pattern '"'^#'"','"'^'"\\| "'`'"','maxPasses','COMPLETED','TenantQuota','bff','Scan','version' | ForEach-Object { \"{0}:{1}: {2}\" -f "'$_.Path,$_.LineNumber,$_.Line }; rg -n --glob '"'*.ts' \"entityType\\s*:\" src/modules; rg -n \"bff_session|bff-session|bff_session_table|module \\\"bff\" infra/main.tf infra -g '*.tf'"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md,docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md -Pattern '"'^#'"','"'^'"\\| "'`'"','maxPasses','COMPLETED','TenantQuota','bff','Scan','version' | ForEach-Object { \"{0}:{1}: {2}\" -f "'$_.Path,$_.LineNumber,$_.Line }; rg -n --glob '"'*.ts' \"entityType\\s*:\" src/modules; rg -n \"bff_session|bff-session|bff_session_table|module \\\"bff\" infra/main.tf infra -g '*.tf'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md,docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md -Pattern '"'^#'"','"'^'"\\| "'`'"','maxPasses','COMPLETED','TenantQuota','bff','Scan','version' | ForEach-Object { \"{0}:{1}: {2}\" -f "'$_.Path,$_.LineNumber,$_.Line }; rg -n --glob '"'*.ts' \"entityType\\s*:\" src/modules; rg -n \"bff_session|bff-session|bff_session_table|module \\\"bff\" infra/main.tf infra -g '*.tf'"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n --glob *.ts 'entityType\\s*:' src/modules" in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'bff.session|bff-session|module \"bff' infra -g *.tf" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 322ms:
# W3-07 â€” Round 2 (Claude trÃ©plica, respondendo Ã  Rodada 1 do Codex, nota 3,4/10, 8 achados bloqueantes)

Rodada 1 achou problemas reais e mudou o desenho de fundo, nÃ£o sÃ³ detalhes. Resposta reorganiza
o mecanismo: **cascata de exclusÃ£o deixa de reusar a fila GSI6** (achado 5) e vira um
discover-then-delete direto e conditioned, com convergÃªncia explÃ­cita (achado 1/8) e uma
taxonomia fechada por `entityType` (achados 2/3/4).

## ResoluÃ§Ã£o achado 5 (colisÃ£o de GSI6) â€” mudanÃ§a estrutural: sem fila GSI6 para cascata

Aceito por completo: reusar `WORKSTATE#PURGE_PENDING/CLAIMED` roubaria linhas de workflows jÃ¡
donos legÃ­timos daquele ponteiro (`UploadSlot` reconciliaÃ§Ã£o, ocorrÃªncias CLAIMED). **Cascata
de tenant nÃ£o usa GSI6 de forma nenhuma.** Novo mecanismo: `TenantCascadeDeletionService`
executa, por pÃ¡gina de `Scan`, um `TransactWriteItems` de **`Delete` direto, condicionado**
(sem intermediÃ¡rio de fila/claim) â€” nenhum ponteiro GSI6 Ã© lido, escrito ou removido por este
mecanismo. GSI6 continua exclusivo dos seus 4 donos existentes.

Isso tambÃ©m simplifica o branch "genÃ©rico" que a achado 4 criticava: nÃ£o existe mais um
worker genÃ©rico reusando `DocumentPurgeWorker` â€” a exclusÃ£o acontece diretamente no
`TenantCascadeDeletionService`, sÃ­ncrona ao Scan (dentro do orÃ§amento de uma invocaÃ§Ã£o, ver
achado 1 abaixo para paginaÃ§Ã£o entre invocaÃ§Ãµes).

## ResoluÃ§Ã£o achado 4 (fail-open) + achado 3 (formas incompatÃ­veis) â€” allowlist fechada por entityType

Nenhum `entityType` desconhecido Ã© tocado â€” **fail-closed**: um `entityType` fora da tabela
abaixo interrompe o processamento daquela linha, loga um alarme dedicado
(`tenant_cascade_unknown_entity_type`) e marca o `TenantDeletionRequest` como
`BLOCKED_UNKNOWN_ENTITY_TYPE` (nÃ£o avanÃ§a, nÃ£o apaga nada alÃ©m do jÃ¡ processado) â€” exige
revisÃ£o humana antes de estender a tabela. TrÃªs categorias, mapeadas explicitamente por
`entityType` real (levantamento contra `data-model.md` Â§2 + os `entityType` literais no
cÃ³digo):

| Categoria | `entityType`s | EstratÃ©gia de exclusÃ£o |
|---|---|---|
| **OCC versionado padrÃ£o** | `ExpirationItem`, `ReminderPolicy`, `ReminderOccurrence`, `Document` (+ S3 real via `pickObjectToDelete`, mesma funÃ§Ã£o do W3-06), `DocumentPurgeReceipt`, `ExtractedField`, `ExtractionRun`, `NotificationIntent`, `NotificationAttempt`, `Channel`, `Provider`, `TrackedSubject`, `RequirementAssignment`, `TenantEntitlement`, `ItemWatch`, `DocumentRequest`, `DocumentSubmission`, `DocumentChasingOccurrence`, `DocumentChasingIntent`, `ImportJob`, `ImportDedupRecord`, `User`, `IdentityMapping`, `GuestTokenPointer` | `Delete` condicionado (`attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t`) â€” mesmo shape de `buildVersionedDelete`, sem `extraConditions` de GSI6 |
| **Append-only, sem `version`** | `AuditEvent`, `WebhookInbox` (evento jÃ¡ processado, nunca mutado apÃ³s criaÃ§Ã£o) | `Delete` condicionado sÃ³ em existÃªncia (`attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t`) â€” sem checagem de versÃ£o, correto porque nada mais escreve nessas linhas depois de criadas |
| **Auto-purgÃ¡vel, excluÃ­do do Scan** | `GuestTokenRateLimit`, `InitialInviteRateLimitRecord` | **NÃ£o tocado por este mecanismo.** Achado real confirmado no cÃ³digo (`guest-rate-limiter.ts`/`initial-invite-rate-limiter.ts`): ambos jÃ¡ carregam `purgeAfterTtl` (TTL real da tabela) e nunca guardam PII alÃ©m de um hash â€” jÃ¡ se autoextinguem independente de qualquer exclusÃ£o de tenant, e (`GuestTokenRateLimit` especificamente) nem tem `tenantId` disponÃ­vel no momento da escrita por design (convidado ainda nÃ£o resolvido). Documentado como exclusÃ£o deliberada, nÃ£o uma lacuna. |

**CorreÃ§Ã£o mecÃ¢nica que a achado 2/3 forÃ§a, feita agora**: `PolicyRef`
(`reminder-policy.ts:69-72`) ganha `tenantId` (valor jÃ¡ disponÃ­vel em todo call site,
`reminder-policy-service.ts:239` jÃ¡ tem `input.tenantId` em mÃ£o) â€” sem isso, `PolicyRef` nem
seria descoberto pelo Scan (achado 2) nem teria o que uma condiÃ§Ã£o `tenantId = :t` checaria.
AdiÃ§Ã£o puramente aditiva, nenhum call site existente quebra (o campo era simplesmente omitido,
nunca lido por ninguÃ©m hoje). CorreÃ§Ã£o real de um gap prÃ©-existente descoberto por esta rodada,
nÃ£o uma mudanÃ§a de comportamento do mÃ³dulo reminder.

## ResoluÃ§Ã£o achado 1 + achado 8 (convergÃªncia/"COMPLETED" sem garantia real)

`TenantDeletionRequest.status` ganha uma mÃ¡quina de passos explÃ­cita, nÃ£o um Ãºnico Scan:

```
DISCOVERING (passo N) â†’ CONVERGENCE_CHECK â†’ [zero linhas encontradas? COMPLETED : DISCOVERING (passo N+1), atÃ© maxPasses]
                                            â†’ excedeu maxPasses com linhas restantes? â†’ INCOMPLETE_NEEDS_REVIEW
```

Cada passo `DISCOVERING`: `Scan` completo paginado (cursor persistido em
`TenantDeletionRequest.lastScanCursor` para retomar entre invocaÃ§Ãµes/timeout de Lambda â€” achado
nÃ£o-bloqueante 2 da Rodada 1), deletando cada linha encontrada pela tabela de categorias acima.
Ao fim do Scan completo (cursor esgotado), um **novo** `Scan` (`CONVERGENCE_CHECK`) confirma se
`tenantId = :t` ainda retorna alguma linha:
- **Zero linhas**: `COMPLETED` â€” esta Ã© a Ãºnica condiÃ§Ã£o que justifica esse status (resolve
  achado 8: "completo" agora significa "uma varredura real nÃ£o encontrou mais nada", nÃ£o "o
  primeiro Scan terminou").
- **Linhas restantes** (escritas concorrentes legÃ­timas durante o passo anterior, ou itens que
  falharam sua condiÃ§Ã£o de exclusÃ£o numa passagem anterior): mais um passo `DISCOVERING` sobre
  as linhas remanescentes, atÃ© `maxPasses` (proposto: 5 â€” generoso, jÃ¡ que cada passo sÃ³
  processa o que sobrou, converge rÃ¡pido na prÃ¡tica jÃ¡ que nÃ£o hÃ¡ um fluxo real criando dado
  novo indefinidamente para um tenant sendo excluÃ­do).
- **`INCOMPLETE_NEEDS_REVIEW`** apÃ³s `maxPasses`: estado terminal honesto, nÃ£o um falso
  `COMPLETED` â€” **limitaÃ§Ã£o explÃ­cita e documentada** (nÃ£o escondida): sem bloqueio imediato de
  uso (fora de escopo, decisÃ£o do Marcelo), um tenant que continua sendo usado ativamente
  durante a exclusÃ£o pode, em tese, criar dado mais rÃ¡pido do que os passos conseguem drenar;
  isso Ã© o preÃ§o concreto de nÃ£o ter bloqueio imediato, registrado aqui para quem decidir sobre
  a feature de produto completa (W3-07 alÃ©m deste mecanismo) saber exatamente o que estÃ¡
  aceitando ao adiar aquele trabalho.

## ResoluÃ§Ã£o achado 6 (falha de lote inteiro por um item conflitante)

`TransactWriteItems` em lote de 25 (mantido como valor escolhido, nÃ£o limite da API â€” correÃ§Ã£o
do achado nÃ£o-bloqueante 3 da Rodada 1, que apontou 100 como o limite real) que falhar cai para
**exclusÃ£o sequencial item a item** dentro do mesmo lote (condicionada individualmente, sem
transaÃ§Ã£o) â€” isola qual item especÃ­fico perdeu a condiÃ§Ã£o (jÃ¡ apagado por outro processo
legÃ­timo, ou mutado concorrentemente) sem bloquear os outros 24. Um item que falhe sua condiÃ§Ã£o
sequencialmente Ã© simplesmente pulado (nunca reportado como erro) â€” se ainda pertencer ao
tenant na prÃ³xima passagem de convergÃªncia, serÃ¡ descoberto e reprocessado lÃ¡.

## ResoluÃ§Ã£o achado 7 (`purgeAfter` imediato contradiz a matriz de 30 dias)

Aceito â€” remoÃ§Ã£o completa do conceito de `purgeAfter` deste mecanismo. `TenantCascadeDeletionService.execute(tenantId)`
**nÃ£o decide quando apagar** â€” apaga imediatamente quando chamado, e a responsabilidade de sÃ³
chamÃ¡-lo depois que a janela normativa (30 dias, revisÃ£o de `legalHold`, confirmaÃ§Ã£o de
identidade) jÃ¡ se encerrou pertence inteiramente ao chamador (a feature DSR completa, fora de
escopo aqui). Este documento nÃ£o reabre nem reinterpreta `privacy-lgpd.md` Â§4 â€” sÃ³ entrega a
capacidade de "apagar agora, dado que apagar agora jÃ¡ foi autorizado por outro processo".
`legalHold` (achado jÃ¡ resolvido no W3-06) continua respeitado: a condiÃ§Ã£o de exclusÃ£o de
qualquer `Document` inclui `attribute_not_exists(legalHold) OR legalHold = :false`, mesma
condiÃ§Ã£o jÃ¡ normativa desde D-061.

## Estado do design apÃ³s Rodada 2

MudanÃ§a estrutural (sem GSI6), taxonomia fechada de 3 categorias cobrindo todo `entityType`
real do sistema (com um gap real fechado, `PolicyRef.tenantId`), convergÃªncia via Scan repetido
atÃ© zero ou `maxPasses`, isolamento de conflito por item, e remoÃ§Ã£o do conceito de `purgeAfter`
deste mecanismo. PeÃ§o reavaliaÃ§Ã£o completa.


 succeeded in 306ms:
src/modules\identity\application\quota.ts:25:  entityType: "TenantQuota";
src/modules\identity\application\quota.ts:83:          entityType: "TenantQuota",
src/modules\expiration\domain\item-watch.ts:13:  entityType: "ItemWatch";
src/modules\expiration\domain\audit-event.ts:16:  entityType: "AuditEvent";
src/modules\expiration\domain\audit-event.ts:63:    entityType: "AuditEvent",
src/modules\expiration\domain\expiration-item.ts:15:  entityType: "ExpirationItem";
src/modules\expiration\application\expiration-service.ts:154:      entityType: "ExpirationItem",
src/modules\expiration\application\expiration-service.ts:458:      entityType: "ExpirationItem",
src/modules\expiration\application\expiration-service.ts:544:        entityType: "ReminderPolicy",
src/modules\expiration\application\expiration-service.ts:564:          Item: { ...policyRefKey(source.tenantId, newItemId, newPolicyId), entityType: "ReminderPolicyRef", policyId: newPolicyId },
src/modules\expiration\application\item-watch-service.ts:43:      const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src/modules\expiration\application\item-watch-service.ts:57:        const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src/modules\identity\persistence\user-repository.ts:16:  entityType: "User";
src/modules\identity\persistence\user-repository.ts:38:  entityType: "DeviceSession";
src/modules\identity\persistence\user-repository.ts:79:      entityType: "User",
src/modules\identity\persistence\identity-mapping-repository.ts:14:  entityType: "IdentityMapping";
src/modules\identity\persistence\identity-mapping-repository.ts:55:      entityType: "IdentityMapping",
src/modules\extraction\application\run-extraction-validation.ts:256:      entityType: "ExtractedField",
src/modules\extraction\application\run-extraction-validation.ts:303:    entityType: "ExtractedField",
src/modules\bff\application\bff-auth-service.ts:73:      entityType: "LoginAttempt",
src/modules\bff\application\bff-auth-service.ts:181:      entityType: "DeviceSession",
src/modules\bff\application\bff-auth-service.ts:200:      entityType: "Session",
src/modules\extraction\application\start-ocr.ts:116:    entityType: "TextractJob",
src/modules\extraction\application\start-extraction-run.ts:55:    entityType: "ExtractionRun",
src/modules\bff\domain\session.ts:16:  entityType: "Session";
src/modules\bff\domain\session.ts:58:  entityType: "LoginAttempt";
src/modules\extraction\domain\textract-job.ts:26:  entityType: "TextractJob";
src/modules\subject\domain\entitlement.ts:23:  entityType: "TenantEntitlement";
src/modules\subject\domain\entitlement.ts:40:    entityType: "TenantEntitlement",
src/modules\subject\domain\document-request-delivery-preference.ts:21:  entityType: "DocumentRequestDeliveryPreference";
src/modules\subject\domain\tracked-subject.ts:16:  entityType: "TrackedSubject";
src/modules\subject\domain\document-submission.ts:20:  entityType: "DocumentSubmission";
src/modules\subject\domain\audit-event.ts:32:  entityType: "SubjectAuditEvent";
src/modules\subject\domain\audit-event.ts:74:    entityType: "SubjectAuditEvent",
src/modules\subject\domain\document-request.ts:13:  entityType: "DocumentRequest";
src/modules\extraction\domain\extraction-run.ts:19:  entityType: "ExtractionRun";
src/modules\subject\domain\guest-token.ts:20:  entityType: "GuestTokenPointer";
src/modules\extraction\domain\extracted-field.ts:28:  entityType: "ExtractedField";
src/modules\subject\domain\requirement-assignment.ts:29:  entityType: "RequirementAssignment";
src/modules\subject\domain\document-chasing.ts:27:  entityType: "DocumentChasingOccurrence";
src/modules\subject\domain\document-chasing.ts:102:  entityType: "DocumentChasingIntent";
src/modules\subject\application\subject-service.ts:76:        entityType: "TrackedSubject",
src/modules\subject\application\requirement-service.ts:58:      entityType: "RequirementAssignment",
src/modules\subject\application\initial-invite-rate-limiter.ts:20:  entityType: "InitialInviteRateLimit";
src/modules\subject\application\initial-invite-rate-limiter.ts:62:          entityType: "InitialInviteRateLimit",
src/modules\subject\application\guest-submission-service.ts:156:      entityType: "DocumentSubmission",
src/modules\subject\application\guest-rate-limiter.ts:15:  entityType: "GuestTokenRateLimit";
src/modules\subject\application\guest-rate-limiter.ts:48:          entityType: "GuestTokenRateLimit",
src/modules\subject\application\document-request-service.ts:131:      entityType: "DocumentRequest",
src/modules\subject\application\document-request-service.ts:152:      entityType: "GuestTokenPointer",
src/modules\subject\application\document-request-service.ts:269:              entityType: "DocumentRequestDeliveryPreference",
src/modules\subject\application\document-chasing-materializer.ts:103:        entityType: "DocumentChasingOccurrence",
src/modules\document\application\document-service.ts:139:        entityType: "Document",
src/modules\document\application\document-service.ts:157:        entityType: "UploadSlot",
src/modules\document\domain\document-purge-receipt.ts:12:  entityType: "DocumentPurgeReceipt";
src/modules\import\domain\import-dedup.ts:11:  entityType: "ImportDedupRecord";
src/modules\reminder\application\reminder-materializer.ts:180:        entityType: "ReminderOccurrence",
src/modules\import\domain\import-job.ts:32:  entityType: "ImportJob";
src/modules\import\application\import-service.ts:124:        entityType: "ImportJob",
src/modules\import\application\import-commit-service.ts:90:      entityType: "ImportDedupRecord",
src/modules\import\application\import-commit-service.ts:107:          entityType: "ImportDedupRecord",
src/modules\reminder\application\reminder-policy-service.ts:63:      entityType: "ReminderPolicy",
src/modules\reminder\application\reminder-policy-service.ts:239:          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
src/modules\document\domain\document.ts:32:  entityType: "Document";
src/modules\notification\application\notification-router-workflow.ts:187:    entityType: "NotificationIntent",
src/modules\notification\application\notification-router-workflow.ts:224:          entityType: "IdempotencyRecord",
src/modules\notification\application\notification-router-workflow.ts:280:      entityType: "NotificationAttempt",
src/modules\notification\application\notification-router-workflow.ts:338:    entityType: "OutboxEvent",
src/modules\notification\domain\notification-preferences.ts:19:  entityType: "NotificationPreferences";
src/modules\notification\domain\notification-preferences.ts:50:    entityType: "NotificationPreferences",
src/modules\notification\application\ses-callback-workflow.ts:69:    entityType: "WebhookInbox",
src/modules\reminder\domain\notification-intent.ts:34:  entityType: "NotificationIntent";
src/modules\notification\domain\notification-attempt.ts:25:  entityType: "NotificationAttempt";
src/modules\notification\domain\notification-attempt.ts:91:  entityType: "NotificationAttemptLookup";
src/modules\notification\domain\notification-attempt.ts:108:    entityType: "NotificationAttemptLookup",
src/modules\reminder\domain\reminder-occurrence.ts:12:  entityType: "ReminderOccurrence";
src/modules\reminder\domain\reminder-policy.ts:38:  entityType: "ReminderPolicy";
src/modules\reminder\domain\reminder-policy.ts:70:  entityType: "ReminderPolicyRef";
src/modules\document\domain\upload-slot.ts:13:  entityType: "UploadSlot";
src/modules\notification\domain\notification-entitlements.ts:13:  entityType: "NotificationEntitlements";

 succeeded in 338ms:
# W3-07 â€” Round 3 (Claude, autocrÃ­tica antes de reenviar â€” correÃ§Ã£o de uma alegaÃ§Ã£o nÃ£o verificada da Rodada 2)

Antes de pedir reavaliaÃ§Ã£o, refiz o levantamento de `entityType` que a Rodada 1 (achado 2/3) jÃ¡
tinha provado errado uma vez â€” a Rodada 2 listou uma tabela sem verificar `version`/`tenantId`
linha a linha contra o cÃ³digo real. Fiz isso agora de verdade (grep exaustivo de todo
`entityType: "..."` em `src/modules/**`, 39 tipos reais, cada um com `version`/`tenantId`
confirmados por leitura direta do arquivo). Dois achados novos, mais sÃ©rios que uma correÃ§Ã£o de
tabela:

## Achado novo 1 â€” segunda tabela fÃ­sica, nÃ£o coberta por nenhuma versÃ£o anterior deste desenho

`Session`/`LoginAttempt`/`DeviceSession` (mÃ³dulo `bff`) vivem em uma **tabela DynamoDB
inteiramente separada** (`bff-session-table`, `infra/main.tf:317`/
`infra/modules/bff-session-table/`), nÃ£o na tabela principal (`exptrk-dev-table`). Nenhuma
versÃ£o anterior deste documento mencionava isso â€” um `Scan` de `exptrk-dev-table`, por mais
correto que fosse, **nunca encontraria essas 3 entidades**. Corrigido: `TenantCascadeDeletionService`
recebe uma lista de tabelas a varrer (`[{ tableName: mainTable, ... }, { tableName: bffSessionTable, ... }]`),
mesma lÃ³gica de descoberta+exclusÃ£o aplicada a cada uma independentemente. As trÃªs jÃ¡ tÃªm
`purgeAfterTtl` real (TTL nativo da tabela, `session.ts:33-36`/`68`) â€” isso NÃƒO as isenta de
inclusÃ£o aqui (diferente de `GuestTokenRateLimit`, ver achado 2): elas carregam dado de sessÃ£o
real (refresh token cifrado, `cognitoSubject`) que o direito de eliminaÃ§Ã£o cobre â€” esperar o TTL
natural (atÃ© dias) em vez de apagar imediatamente na exclusÃ£o de tenant seria mais fraco que o
precedente jÃ¡ estabelecido (`Document`/W3-06 nunca trata TTL como mecanismo primÃ¡rio).

## Achado novo 2 â€” tabela de classificaÃ§Ã£o corrigida (verificada, nÃ£o presumida)

Duas categorias reais (a terceira, exclusÃ£o do Scan, continua sÃ³ para os 2 casos jÃ¡
justificados na Rodada 2), com a lista completa dos 39 `entityType`s reais do sistema:

| Categoria | CondiÃ§Ã£o de exclusÃ£o | `entityType`s (verificados) |
|---|---|---|
| **OCC versionado** (`version` presente) | `Delete` condicionado: `attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t` (+ `legalHold` para `Document`) | `DeviceSession`, `Document`\*, `DocumentChasingIntent`, `DocumentChasingOccurrence`, `DocumentPurgeReceipt`, `DocumentRequest`, `DocumentRequestDeliveryPreference`, `DocumentSubmission`, `ExpirationItem`, `ExtractedField`, `ExtractionRun`, `GuestTokenPointer`, `ImportJob`, `ItemWatch`, `LoginAttempt`, `NotificationAttempt`, `NotificationEntitlements`, `NotificationIntent`, `NotificationPreferences`, `ReminderOccurrence`, `ReminderPolicy`, `RequirementAssignment`, `Session`, `TextractJob`, `TrackedSubject`, `UploadSlot`, `User` (\* `Document` tambÃ©m apaga o objeto S3 real antes, via `pickObjectToDelete` do W3-06) |
| **Sem versÃ£o** (nunca mutado apÃ³s criaÃ§Ã£o â€” ledger/pointer/counter) | `Delete` condicionado sÃ³ em existÃªncia: `attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t` | `AuditEvent`, `SubjectAuditEvent`, `IdentityMapping`, `ImportDedupRecord`, `NotificationAttemptLookup`, `OutboxEvent`, `WebhookInbox`, `IdempotencyRecord`, `TenantQuota`, `ReminderPolicyRef` (apÃ³s correÃ§Ã£o abaixo) |
| **ExcluÃ­do do Scan, autopurgÃ¡vel** | NÃ£o tocado | `GuestTokenRateLimit`, `InitialInviteRateLimit` â€” sem `tenantId` no momento da escrita (por design, convidado ainda nÃ£o resolvido / mesmo mecanismo duplicado), jÃ¡ carregam `purgeAfterTtl` real, nunca guardam PII alÃ©m de um hash |

**CorreÃ§Ã£o de cÃ³digo real que este levantamento forÃ§a** (achado genuÃ­no, nÃ£o cosmÃ©tico):
`ReminderPolicyRef` (`reminder-policy.ts:69-72`) hoje **nÃ£o tem `tenantId`** (confirmado por
leitura direta, zero ocorrÃªncias no bloco da interface) â€” a Rodada 2 jÃ¡ tinha notado isso, mas
por engano o classificou junto da categoria versionada; `PolicyRef` tambÃ©m nÃ£o tem `version`
(Ã© um ponteiro criado uma vez via `attribute_not_exists`, nunca atualizado). CorreÃ§Ã£o: adicionar
sÃ³ `tenantId` (valor jÃ¡ disponÃ­vel em `reminder-policy-service.ts:239`, `input.tenantId`) e
classificar na categoria **sem versÃ£o** (existÃªncia apenas) â€” nÃ£o na OCC-versionada.

Nenhum `entityType` fora desta tabela Ã© tocado â€” mesma regra fail-closed jÃ¡ proposta na Rodada 2
(achado 4 original: `BLOCKED_UNKNOWN_ENTITY_TYPE`, alarme dedicado, revisÃ£o humana antes de
estender a tabela).

## Estado do design apÃ³s Rodada 3

Mecanismo (sem GSI6, convergÃªncia via re-scan atÃ© zero, fallback sequencial por item conflitante
em lote, sem `purgeAfter`/imediato por definiÃ§Ã£o) mantido da Rodada 2. Esta rodada corrige uma
alegaÃ§Ã£o que nÃ£o tinha sido verificada linha a linha (mesmo erro de categoria que a Rodada 1 jÃ¡
tinha pego uma vez â€” nÃ£o repetido desta vez, confirmado por grep exaustivo) e adiciona a segunda
tabela fÃ­sica (`bff-session-table`) que nenhuma rodada anterior tinha considerado. PeÃ§o
reavaliaÃ§Ã£o completa, com atenÃ§Ã£o especial a qualquer `entityType` que este levantamento ainda
possa ter classificado errado.


2026-08-28T20:22:21.093706Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 14.1 seconds
Output:
command timed out after 14052 milliseconds

 exited 124 in 14052ms:
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^(#|'"\\|)|maxPasses|COMPLETED|TenantQuota|bff|Scan|version|entityType\" docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round3.md" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 180ms:
1:# W3-07 — Round 3 (Claude, autocrítica antes de reenviar — correção de uma alegação não verificada da Rodada 2)
3:Antes de pedir reavaliação, refiz o levantamento de `entityType` que a Rodada 1 (achado 2/3) já
4:tinha provado errado uma vez — a Rodada 2 listou uma tabela sem verificar `version`/`tenantId`
6:`entityType: "..."` em `src/modules/**`, 39 tipos reais, cada um com `version`/`tenantId`
10:## Achado novo 1 — segunda tabela física, não coberta por nenhuma versão anterior deste desenho
12:`Session`/`LoginAttempt`/`DeviceSession` (módulo `bff`) vivem em uma **tabela DynamoDB
13:inteiramente separada** (`bff-session-table`, `infra/main.tf:317`/
14:`infra/modules/bff-session-table/`), não na tabela principal (`exptrk-dev-table`). Nenhuma
15:versão anterior deste documento mencionava isso — um `Scan` de `exptrk-dev-table`, por mais
17:recebe uma lista de tabelas a varrer (`[{ tableName: mainTable, ... }, { tableName: bffSessionTable, ... }]`),
25:## Achado novo 2 — tabela de classificação corrigida (verificada, não presumida)
27:Duas categorias reais (a terceira, exclusão do Scan, continua só para os 2 casos já
28:justificados na Rodada 2), com a lista completa dos 39 `entityType`s reais do sistema:
30:| Categoria | Condição de exclusão | `entityType`s (verificados) |
31:|---|---|---|
32:| **OCC versionado** (`version` presente) | `Delete` condicionado: `attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t` (+ `legalHold` para `Document`) | `DeviceSession`, `Document`\*, `DocumentChasingIntent`, `DocumentChasingOccurrence`, `DocumentPurgeReceipt`, `DocumentRequest`, `DocumentRequestDeliveryPreference`, `DocumentSubmission`, `ExpirationItem`, `ExtractedField`, `ExtractionRun`, `GuestTokenPointer`, `ImportJob`, `ItemWatch`, `LoginAttempt`, `NotificationAttempt`, `NotificationEntitlements`, `NotificationIntent`, `NotificationPreferences`, `ReminderOccurrence`, `ReminderPolicy`, `RequirementAssignment`, `Session`, `TextractJob`, `TrackedSubject`, `UploadSlot`, `User` (\* `Document` também apaga o objeto S3 real antes, via `pickObjectToDelete` do W3-06) |
33:| **Sem versão** (nunca mutado após criação — ledger/pointer/counter) | `Delete` condicionado só em existência: `attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t` | `AuditEvent`, `SubjectAuditEvent`, `IdentityMapping`, `ImportDedupRecord`, `NotificationAttemptLookup`, `OutboxEvent`, `WebhookInbox`, `IdempotencyRecord`, `TenantQuota`, `ReminderPolicyRef` (após correção abaixo) |
34:| **Excluído do Scan, autopurgável** | Não tocado | `GuestTokenRateLimit`, `InitialInviteRateLimit` — sem `tenantId` no momento da escrita (por design, convidado ainda não resolvido / mesmo mecanismo duplicado), já carregam `purgeAfterTtl` real, nunca guardam PII além de um hash |
39:por engano o classificou junto da categoria versionada; `PolicyRef` também não tem `version`
42:classificar na categoria **sem versão** (existência apenas) — não na OCC-versionada.
44:Nenhum `entityType` fora desta tabela é tocado — mesma regra fail-closed já proposta na Rodada 2
48:## Estado do design após Rodada 3
54:tabela física (`bff-session-table`) que nenhuma rodada anterior tinha considerado. Peço
55:reavaliação completa, com atenção especial a qualquer `entityType` que este levantamento ainda

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n --glob *.ts 'entityType\\s*:' src/modules" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 154ms:
src/modules\subject\domain\audit-event.ts:32:  entityType: "SubjectAuditEvent";
src/modules\subject\domain\audit-event.ts:74:    entityType: "SubjectAuditEvent",
src/modules\subject\domain\document-request-delivery-preference.ts:21:  entityType: "DocumentRequestDeliveryPreference";
src/modules\subject\domain\document-chasing.ts:27:  entityType: "DocumentChasingOccurrence";
src/modules\subject\domain\document-chasing.ts:102:  entityType: "DocumentChasingIntent";
src/modules\subject\domain\entitlement.ts:23:  entityType: "TenantEntitlement";
src/modules\subject\domain\entitlement.ts:40:    entityType: "TenantEntitlement",
src/modules\document\domain\upload-slot.ts:13:  entityType: "UploadSlot";
src/modules\subject\domain\document-submission.ts:20:  entityType: "DocumentSubmission";
src/modules\subject\application\document-chasing-materializer.ts:103:        entityType: "DocumentChasingOccurrence",
src/modules\subject\domain\document-request.ts:13:  entityType: "DocumentRequest";
src/modules\subject\domain\tracked-subject.ts:16:  entityType: "TrackedSubject";
src/modules\document\domain\document.ts:32:  entityType: "Document";
src/modules\subject\application\guest-submission-service.ts:156:      entityType: "DocumentSubmission",
src/modules\subject\domain\requirement-assignment.ts:29:  entityType: "RequirementAssignment";
src/modules\subject\application\guest-rate-limiter.ts:15:  entityType: "GuestTokenRateLimit";
src/modules\subject\application\guest-rate-limiter.ts:48:          entityType: "GuestTokenRateLimit",
src/modules\subject\domain\guest-token.ts:20:  entityType: "GuestTokenPointer";
src/modules\document\domain\document-purge-receipt.ts:12:  entityType: "DocumentPurgeReceipt";
src/modules\subject\application\document-request-service.ts:131:      entityType: "DocumentRequest",
src/modules\subject\application\document-request-service.ts:152:      entityType: "GuestTokenPointer",
src/modules\subject\application\document-request-service.ts:269:              entityType: "DocumentRequestDeliveryPreference",
src/modules\subject\application\subject-service.ts:76:        entityType: "TrackedSubject",
src/modules\identity\persistence\user-repository.ts:16:  entityType: "User";
src/modules\identity\persistence\user-repository.ts:38:  entityType: "DeviceSession";
src/modules\identity\persistence\user-repository.ts:79:      entityType: "User",
src/modules\subject\application\initial-invite-rate-limiter.ts:20:  entityType: "InitialInviteRateLimit";
src/modules\subject\application\initial-invite-rate-limiter.ts:62:          entityType: "InitialInviteRateLimit",
src/modules\subject\application\requirement-service.ts:58:      entityType: "RequirementAssignment",
src/modules\expiration\application\item-watch-service.ts:43:      const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src/modules\expiration\application\item-watch-service.ts:57:        const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src/modules\expiration\application\expiration-service.ts:154:      entityType: "ExpirationItem",
src/modules\expiration\application\expiration-service.ts:458:      entityType: "ExpirationItem",
src/modules\expiration\application\expiration-service.ts:544:        entityType: "ReminderPolicy",
src/modules\expiration\application\expiration-service.ts:564:          Item: { ...policyRefKey(source.tenantId, newItemId, newPolicyId), entityType: "ReminderPolicyRef", policyId: newPolicyId },
src/modules\expiration\domain\item-watch.ts:13:  entityType: "ItemWatch";
src/modules\identity\persistence\identity-mapping-repository.ts:14:  entityType: "IdentityMapping";
src/modules\identity\persistence\identity-mapping-repository.ts:55:      entityType: "IdentityMapping",
src/modules\expiration\domain\audit-event.ts:16:  entityType: "AuditEvent";
src/modules\expiration\domain\audit-event.ts:63:    entityType: "AuditEvent",
src/modules\expiration\domain\expiration-item.ts:15:  entityType: "ExpirationItem";
src/modules\bff\domain\session.ts:16:  entityType: "Session";
src/modules\bff\domain\session.ts:58:  entityType: "LoginAttempt";
src/modules\bff\application\bff-auth-service.ts:73:      entityType: "LoginAttempt",
src/modules\bff\application\bff-auth-service.ts:181:      entityType: "DeviceSession",
src/modules\bff\application\bff-auth-service.ts:200:      entityType: "Session",
src/modules\reminder\application\reminder-policy-service.ts:63:      entityType: "ReminderPolicy",
src/modules\reminder\application\reminder-policy-service.ts:239:          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
src/modules\reminder\application\reminder-materializer.ts:180:        entityType: "ReminderOccurrence",
src/modules\identity\application\quota.ts:25:  entityType: "TenantQuota";
src/modules\identity\application\quota.ts:83:          entityType: "TenantQuota",
src/modules\reminder\domain\reminder-policy.ts:38:  entityType: "ReminderPolicy";
src/modules\reminder\domain\reminder-policy.ts:70:  entityType: "ReminderPolicyRef";
src/modules\reminder\domain\reminder-occurrence.ts:12:  entityType: "ReminderOccurrence";
src/modules\document\application\document-service.ts:139:        entityType: "Document",
src/modules\document\application\document-service.ts:157:        entityType: "UploadSlot",
src/modules\reminder\domain\notification-intent.ts:34:  entityType: "NotificationIntent";
src/modules\notification\domain\notification-preferences.ts:19:  entityType: "NotificationPreferences";
src/modules\notification\domain\notification-preferences.ts:50:    entityType: "NotificationPreferences",
src/modules\notification\domain\notification-entitlements.ts:13:  entityType: "NotificationEntitlements";
src/modules\notification\domain\notification-attempt.ts:25:  entityType: "NotificationAttempt";
src/modules\notification\domain\notification-attempt.ts:91:  entityType: "NotificationAttemptLookup";
src/modules\notification\domain\notification-attempt.ts:108:    entityType: "NotificationAttemptLookup",
src/modules\extraction\domain\textract-job.ts:26:  entityType: "TextractJob";
src/modules\extraction\domain\extraction-run.ts:19:  entityType: "ExtractionRun";
src/modules\extraction\domain\extracted-field.ts:28:  entityType: "ExtractedField";
src/modules\import\domain\import-job.ts:32:  entityType: "ImportJob";
src/modules\import\domain\import-dedup.ts:11:  entityType: "ImportDedupRecord";
src/modules\notification\application\ses-callback-workflow.ts:69:    entityType: "WebhookInbox",
src/modules\extraction\application\start-ocr.ts:116:    entityType: "TextractJob",
src/modules\extraction\application\start-extraction-run.ts:55:    entityType: "ExtractionRun",
src/modules\notification\application\notification-router-workflow.ts:187:    entityType: "NotificationIntent",
src/modules\notification\application\notification-router-workflow.ts:224:          entityType: "IdempotencyRecord",
src/modules\notification\application\notification-router-workflow.ts:280:      entityType: "NotificationAttempt",
src/modules\notification\application\notification-router-workflow.ts:338:    entityType: "OutboxEvent",
src/modules\extraction\application\run-extraction-validation.ts:256:      entityType: "ExtractedField",
src/modules\extraction\application\run-extraction-validation.ts:303:    entityType: "ExtractedField",
src/modules\import\application\import-service.ts:124:        entityType: "ImportJob",
src/modules\import\application\import-commit-service.ts:90:      entityType: "ImportDedupRecord",
src/modules\import\application\import-commit-service.ts:107:          entityType: "ImportDedupRecord",

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^(#|'"\\|)|maxPasses|COMPLETED|fallback|Scan|Delete|version|legalHold|unknown|Unknown|tenantId\" docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 178ms:
1:# W3-07 — Round 2 (Claude tréplica, respondendo à Rodada 1 do Codex, nota 3,4/10, 8 achados bloqueantes)
8:## Resolução achado 5 (colisão de GSI6) — mudança estrutural: sem fila GSI6 para cascata
13:executa, por página de `Scan`, um `TransactWriteItems` de **`Delete` direto, condicionado**
19:`TenantCascadeDeletionService`, síncrona ao Scan (dentro do orçamento de uma invocação, ver
22:## Resolução achado 4 (fail-open) + achado 3 (formas incompatíveis) — allowlist fechada por entityType
26:(`tenant_cascade_unknown_entity_type`) e marca o `TenantDeletionRequest` como
32:| Categoria | `entityType`s | Estratégia de exclusão |
33:|---|---|---|
34:| **OCC versionado padrão** | `ExpirationItem`, `ReminderPolicy`, `ReminderOccurrence`, `Document` (+ S3 real via `pickObjectToDelete`, mesma função do W3-06), `DocumentPurgeReceipt`, `ExtractedField`, `ExtractionRun`, `NotificationIntent`, `NotificationAttempt`, `Channel`, `Provider`, `TrackedSubject`, `RequirementAssignment`, `TenantEntitlement`, `ItemWatch`, `DocumentRequest`, `DocumentSubmission`, `DocumentChasingOccurrence`, `DocumentChasingIntent`, `ImportJob`, `ImportDedupRecord`, `User`, `IdentityMapping`, `GuestTokenPointer` | `Delete` condicionado (`attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t`) — mesmo shape de `buildVersionedDelete`, sem `extraConditions` de GSI6 |
35:| **Append-only, sem `version`** | `AuditEvent`, `WebhookInbox` (evento já processado, nunca mutado após criação) | `Delete` condicionado só em existência (`attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t`) — sem checagem de versão, correto porque nada mais escreve nessas linhas depois de criadas |
36:| **Auto-purgável, excluído do Scan** | `GuestTokenRateLimit`, `InitialInviteRateLimitRecord` | **Não tocado por este mecanismo.** Achado real confirmado no código (`guest-rate-limiter.ts`/`initial-invite-rate-limiter.ts`): ambos já carregam `purgeAfterTtl` (TTL real da tabela) e nunca guardam PII além de um hash — já se autoextinguem independente de qualquer exclusão de tenant, e (`GuestTokenRateLimit` especificamente) nem tem `tenantId` disponível no momento da escrita por design (convidado ainda não resolvido). Documentado como exclusão deliberada, não uma lacuna. |
39:(`reminder-policy.ts:69-72`) ganha `tenantId` (valor já disponível em todo call site,
40:`reminder-policy-service.ts:239` já tem `input.tenantId` em mão) — sem isso, `PolicyRef` nem
41:seria descoberto pelo Scan (achado 2) nem teria o que uma condição `tenantId = :t` checaria.
46:## Resolução achado 1 + achado 8 (convergência/"COMPLETED" sem garantia real)
48:`TenantDeletionRequest.status` ganha uma máquina de passos explícita, não um único Scan:
51:DISCOVERING (passo N) → CONVERGENCE_CHECK → [zero linhas encontradas? COMPLETED : DISCOVERING (passo N+1), até maxPasses]
52:                                            → excedeu maxPasses com linhas restantes? → INCOMPLETE_NEEDS_REVIEW
55:Cada passo `DISCOVERING`: `Scan` completo paginado (cursor persistido em
56:`TenantDeletionRequest.lastScanCursor` para retomar entre invocações/timeout de Lambda — achado
58:Ao fim do Scan completo (cursor esgotado), um **novo** `Scan` (`CONVERGENCE_CHECK`) confirma se
59:`tenantId = :t` ainda retorna alguma linha:
60:- **Zero linhas**: `COMPLETED` — esta é a única condição que justifica esse status (resolve
62:  primeiro Scan terminou").
65:  as linhas remanescentes, até `maxPasses` (proposto: 5 — generoso, já que cada passo só
68:- **`INCOMPLETE_NEEDS_REVIEW`** após `maxPasses`: estado terminal honesto, não um falso
69:  `COMPLETED` — **limitação explícita e documentada** (não escondida): sem bloqueio imediato de
76:## Resolução achado 6 (falha de lote inteiro por um item conflitante)
86:## Resolução achado 7 (`purgeAfter` imediato contradiz a matriz de 30 dias)
88:Aceito — remoção completa do conceito de `purgeAfter` deste mecanismo. `TenantCascadeDeletionService.execute(tenantId)`
90:chamá-lo depois que a janela normativa (30 dias, revisão de `legalHold`, confirmação de
94:`legalHold` (achado já resolvido no W3-06) continua respeitado: a condição de exclusão de
95:qualquer `Document` inclui `attribute_not_exists(legalHold) OR legalHold = :false`, mesma
98:## Estado do design após Rodada 2
101:real do sistema (com um gap real fechado, `PolicyRef.tenantId`), convergência via Scan repetido
102:até zero ou `maxPasses`, isolamento de conflito por item, e remoção do conceito de `purgeAfter`

2026-08-28T20:22:35.216601Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md'; "'$l=Get-Content $p; $l[8..105]'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md'; "'$l=Get-Content $p; $l[8..105]' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md'; "'$l=Get-Content $p; $l[8..105]'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-proposal-round2.md | Select-Object -Skip 8 -First 98' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 216ms:

Aceito por completo: reusar `WORKSTATE#PURGE_PENDING/CLAIMED` roubaria linhas de workflows jÃ¡
donos legÃ­timos daquele ponteiro (`UploadSlot` reconciliaÃ§Ã£o, ocorrÃªncias CLAIMED). **Cascata
de tenant nÃ£o usa GSI6 de forma nenhuma.** Novo mecanismo: `TenantCascadeDeletionService`
executa, por pÃ¡gina de `Scan`, um `TransactWriteItems` de **`Delete` direto, condicionado**
(sem intermediÃ¡rio de fila/claim) â€” nenhum ponteiro GSI6 Ã© lido, escrito ou removido por este
mecanismo. GSI6 continua exclusivo dos seus 4 donos existentes.

Isso tambÃ©m simplifica o branch "genÃ©rico" que a achado 4 criticava: nÃ£o existe mais um
worker genÃ©rico reusando `DocumentPurgeWorker` â€” a exclusÃ£o acontece diretamente no
`TenantCascadeDeletionService`, sÃ­ncrona ao Scan (dentro do orÃ§amento de uma invocaÃ§Ã£o, ver
achado 1 abaixo para paginaÃ§Ã£o entre invocaÃ§Ãµes).

## ResoluÃ§Ã£o achado 4 (fail-open) + achado 3 (formas incompatÃ­veis) â€” allowlist fechada por entityType

Nenhum `entityType` desconhecido Ã© tocado â€” **fail-closed**: um `entityType` fora da tabela
abaixo interrompe o processamento daquela linha, loga um alarme dedicado
(`tenant_cascade_unknown_entity_type`) e marca o `TenantDeletionRequest` como
`BLOCKED_UNKNOWN_ENTITY_TYPE` (nÃ£o avanÃ§a, nÃ£o apaga nada alÃ©m do jÃ¡ processado) â€” exige
revisÃ£o humana antes de estender a tabela. TrÃªs categorias, mapeadas explicitamente por
`entityType` real (levantamento contra `data-model.md` Â§2 + os `entityType` literais no
cÃ³digo):

| Categoria | `entityType`s | EstratÃ©gia de exclusÃ£o |
|---|---|---|
| **OCC versionado padrÃ£o** | `ExpirationItem`, `ReminderPolicy`, `ReminderOccurrence`, `Document` (+ S3 real via `pickObjectToDelete`, mesma funÃ§Ã£o do W3-06), `DocumentPurgeReceipt`, `ExtractedField`, `ExtractionRun`, `NotificationIntent`, `NotificationAttempt`, `Channel`, `Provider`, `TrackedSubject`, `RequirementAssignment`, `TenantEntitlement`, `ItemWatch`, `DocumentRequest`, `DocumentSubmission`, `DocumentChasingOccurrence`, `DocumentChasingIntent`, `ImportJob`, `ImportDedupRecord`, `User`, `IdentityMapping`, `GuestTokenPointer` | `Delete` condicionado (`attribute_exists(PK) AND attribute_exists(SK) AND version = :v AND tenantId = :t`) â€” mesmo shape de `buildVersionedDelete`, sem `extraConditions` de GSI6 |
| **Append-only, sem `version`** | `AuditEvent`, `WebhookInbox` (evento jÃ¡ processado, nunca mutado apÃ³s criaÃ§Ã£o) | `Delete` condicionado sÃ³ em existÃªncia (`attribute_exists(PK) AND attribute_exists(SK) AND tenantId = :t`) â€” sem checagem de versÃ£o, correto porque nada mais escreve nessas linhas depois de criadas |
| **Auto-purgÃ¡vel, excluÃ­do do Scan** | `GuestTokenRateLimit`, `InitialInviteRateLimitRecord` | **NÃ£o tocado por este mecanismo.** Achado real confirmado no cÃ³digo (`guest-rate-limiter.ts`/`initial-invite-rate-limiter.ts`): ambos jÃ¡ carregam `purgeAfterTtl` (TTL real da tabela) e nunca guardam PII alÃ©m de um hash â€” jÃ¡ se autoextinguem independente de qualquer exclusÃ£o de tenant, e (`GuestTokenRateLimit` especificamente) nem tem `tenantId` disponÃ­vel no momento da escrita por design (convidado ainda nÃ£o resolvido). Documentado como exclusÃ£o deliberada, nÃ£o uma lacuna. |

**CorreÃ§Ã£o mecÃ¢nica que a achado 2/3 forÃ§a, feita agora**: `PolicyRef`
(`reminder-policy.ts:69-72`) ganha `tenantId` (valor jÃ¡ disponÃ­vel em todo call site,
`reminder-policy-service.ts:239` jÃ¡ tem `input.tenantId` em mÃ£o) â€” sem isso, `PolicyRef` nem
seria descoberto pelo Scan (achado 2) nem teria o que uma condiÃ§Ã£o `tenantId = :t` checaria.
AdiÃ§Ã£o puramente aditiva, nenhum call site existente quebra (o campo era simplesmente omitido,
nunca lido por ninguÃ©m hoje). CorreÃ§Ã£o real de um gap prÃ©-existente descoberto por esta rodada,
nÃ£o uma mudanÃ§a de comportamento do mÃ³dulo reminder.

## ResoluÃ§Ã£o achado 1 + achado 8 (convergÃªncia/"COMPLETED" sem garantia real)

`TenantDeletionRequest.status` ganha uma mÃ¡quina de passos explÃ­cita, nÃ£o um Ãºnico Scan:

```
DISCOVERING (passo N) â†’ CONVERGENCE_CHECK â†’ [zero linhas encontradas? COMPLETED : DISCOVERING (passo N+1), atÃ© maxPasses]
                                            â†’ excedeu maxPasses com linhas restantes? â†’ INCOMPLETE_NEEDS_REVIEW
```

Cada passo `DISCOVERING`: `Scan` completo paginado (cursor persistido em
`TenantDeletionRequest.lastScanCursor` para retomar entre invocaÃ§Ãµes/timeout de Lambda â€” achado
nÃ£o-bloqueante 2 da Rodada 1), deletando cada linha encontrada pela tabela de categorias acima.
Ao fim do Scan completo (cursor esgotado), um **novo** `Scan` (`CONVERGENCE_CHECK`) confirma se
`tenantId = :t` ainda retorna alguma linha:
- **Zero linhas**: `COMPLETED` â€” esta Ã© a Ãºnica condiÃ§Ã£o que justifica esse status (resolve
  achado 8: "completo" agora significa "uma varredura real nÃ£o encontrou mais nada", nÃ£o "o
  primeiro Scan terminou").
- **Linhas restantes** (escritas concorrentes legÃ­timas durante o passo anterior, ou itens que
  falharam sua condiÃ§Ã£o de exclusÃ£o numa passagem anterior): mais um passo `DISCOVERING` sobre
  as linhas remanescentes, atÃ© `maxPasses` (proposto: 5 â€” generoso, jÃ¡ que cada passo sÃ³
  processa o que sobrou, converge rÃ¡pido na prÃ¡tica jÃ¡ que nÃ£o hÃ¡ um fluxo real criando dado
  novo indefinidamente para um tenant sendo excluÃ­do).
- **`INCOMPLETE_NEEDS_REVIEW`** apÃ³s `maxPasses`: estado terminal honesto, nÃ£o um falso
  `COMPLETED` â€” **limitaÃ§Ã£o explÃ­cita e documentada** (nÃ£o escondida): sem bloqueio imediato de
  uso (fora de escopo, decisÃ£o do Marcelo), um tenant que continua sendo usado ativamente
  durante a exclusÃ£o pode, em tese, criar dado mais rÃ¡pido do que os passos conseguem drenar;
  isso Ã© o preÃ§o concreto de nÃ£o ter bloqueio imediato, registrado aqui para quem decidir sobre
  a feature de produto completa (W3-07 alÃ©m deste mecanismo) saber exatamente o que estÃ¡
  aceitando ao adiar aquele trabalho.

## ResoluÃ§Ã£o achado 6 (falha de lote inteiro por um item conflitante)

`TransactWriteItems` em lote de 25 (mantido como valor escolhido, nÃ£o limite da API â€” correÃ§Ã£o
do achado nÃ£o-bloqueante 3 da Rodada 1, que apontou 100 como o limite real) que falhar cai para
**exclusÃ£o sequencial item a item** dentro do mesmo lote (condicionada individualmente, sem
transaÃ§Ã£o) â€” isola qual item especÃ­fico perdeu a condiÃ§Ã£o (jÃ¡ apagado por outro processo
legÃ­timo, ou mutado concorrentemente) sem bloquear os outros 24. Um item que falhe sua condiÃ§Ã£o
sequencialmente Ã© simplesmente pulado (nunca reportado como erro) â€” se ainda pertencer ao
tenant na prÃ³xima passagem de convergÃªncia, serÃ¡ descoberto e reprocessado lÃ¡.

## ResoluÃ§Ã£o achado 7 (`purgeAfter` imediato contradiz a matriz de 30 dias)

Aceito â€” remoÃ§Ã£o completa do conceito de `purgeAfter` deste mecanismo. `TenantCascadeDeletionService.execute(tenantId)`
**nÃ£o decide quando apagar** â€” apaga imediatamente quando chamado, e a responsabilidade de sÃ³
chamÃ¡-lo depois que a janela normativa (30 dias, revisÃ£o de `legalHold`, confirmaÃ§Ã£o de
identidade) jÃ¡ se encerrou pertence inteiramente ao chamador (a feature DSR completa, fora de
escopo aqui). Este documento nÃ£o reabre nem reinterpreta `privacy-lgpd.md` Â§4 â€” sÃ³ entrega a
capacidade de "apagar agora, dado que apagar agora jÃ¡ foi autorizado por outro processo".
`legalHold` (achado jÃ¡ resolvido no W3-06) continua respeitado: a condiÃ§Ã£o de exclusÃ£o de
qualquer `Document` inclui `attribute_not_exists(legalHold) OR legalHold = :false`, mesma
condiÃ§Ã£o jÃ¡ normativa desde D-061.

## Estado do design apÃ³s Rodada 2

MudanÃ§a estrutural (sem GSI6), taxonomia fechada de 3 categorias cobrindo todo `entityType`
real do sistema (com um gap real fechado, `PolicyRef.tenantId`), convergÃªncia via Scan repetido
atÃ© zero ou `maxPasses`, isolamento de conflito por item, e remoÃ§Ã£o do conceito de `purgeAfter`
deste mecanismo. PeÃ§o reavaliaÃ§Ã£o completa.

2026-08-28T20:22:43.516165Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/identity/application/quota.ts; Get-Content -Raw src/modules/subject/domain/entitlement.ts; Get-Content -Raw src/modules/bff/domain/session.ts; Get-Content -Raw infra/main.tf | Select-String -Pattern 'bff_session' -Context 4,12"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/identity/application/quota.ts; Get-Content -Raw src/modules/subject/domain/entitlement.ts; Get-Content -Raw src/modules/bff/domain/session.ts; Get-Content -Raw infra/main.tf | Select-String -Pattern 'bff_session' -Context 4,12" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/identity/application/quota.ts; Get-Content -Raw src/modules/subject/domain/entitlement.ts; Get-Content -Raw src/modules/bff/domain/session.ts; Get-Content -Raw infra/main.tf | Select-String -Pattern 'bff_session' -Context 4,12"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/bff/domain/session.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/subject/domain/entitlement.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw infra/main.tf' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/identity/application/quota.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 292ms:
/**
 * Session â€” the new dedicated table D-054 requires (never the single-table main aggregate:
 * its `tenant_facing_read_write` policy already grants GetItem/Query to ~20+ resource roles,
 * exactly the over-broad exposure D-054 flagged). Distinct from
 * src/modules/identity/domain/request-context.ts's `sessionId`/`DeviceSession` - those are the
 * JWT-authorizer-side revocation watermark (M1), this is the BFF-side browser session that
 * *produces* the bearer token DeviceSession later revokes. Never reuse the same name for both.
 *
 * PK/SK: `SESSION#<selectorHash>` / `POINTER` - single dedicated table, no tenant prefix
 * needed (unlike GuestTokenPointer's tenantless-exception note, this table has no other
 * tenant-scoped data to isolate from; the whole table is BFF-only, per D-054).
 */
export interface Session {
  PK: string;
  SK: "POINTER";
  entityType: "Session";
  selectorHash: string;
  secretHash: string;
  tenantId: string;
  userId: string;
  cognitoSubject: string;
  deviceId: string;
  csrfSecret: string;
  /** Encrypted at rest via the dedicated CMK (D-054) - never plaintext in this record. */
  encryptedRefreshToken: string;
  /** Cached access token, short-lived (<=15 min per D-054) - avoids a Cognito round trip on
   * every proxied request; refreshed proactively, never trusted past its own expiry. */
  accessToken: string;
  accessTokenExpiresAt: string;
  /** Fixed at creation, never extended by refresh (D-054: absolute session lifetime ceiling
   * independent of activity). */
  absoluteExpiresAt: string;
  /** DynamoDB TTL attribute (epoch seconds) - physical deletion, same convention as
   * GuestTokenPointer.purgeAfterTtl. Recomputed on every activity as `now + idle TTL`, capped
   * at `absoluteExpiresAt`. */
  purgeAfterTtl: number;
  refreshState: "IDLE" | "IN_PROGRESS";
  refreshLeaseId?: string;
  refreshLeaseUntil?: string;
  revokedAt?: string;
  createdAt: string;
  updatedAt: string;
  version: number;
}

export function sessionKey(selectorHash: string): { PK: string; SK: "POINTER" } {
  return { PK: `SESSION#${selectorHash}`, SK: "POINTER" };
}

/**
 * LoginAttempt â€” ephemeral, single-use (D-054). PK/SK: `LOGINATTEMPT#<selectorHash>` /
 * `POINTER`, same dedicated table (co-located, not a second table - it exists only to bridge
 * the redirect to Cognito and back, nothing else needs it).
 */
export interface LoginAttempt {
  PK: string;
  SK: "POINTER";
  entityType: "LoginAttempt";
  selectorHash: string;
  secretHash: string;
  state: string;
  nonce: string;
  codeVerifier: string;
  /** Where to return the user after a successful login (Â§23 of the Validation Readiness
   * mission's "return context" concept, carried into real auth) - validated against an
   * allowlist of same-origin app paths before ever being used, never an open redirect. */
  returnTo: string;
  purgeAfterTtl: number;
  consumedAt?: string;
  createdAt: string;
  /** Guards single-use consumption via SessionStore.updateConditional - without this, two
   * concurrent callbacks (double-click, browser back+resubmit) racing on the same LoginAttempt
   * could both read it as not-yet-consumed before either write lands (found in review). */
  version: number;
}

export function loginAttemptKey(selectorHash: string): { PK: string; SK: "POINTER" } {
  return { PK: `LOGINATTEMPT#${selectorHash}`, SK: "POINTER" };
}


 succeeded in 291ms:
/**
 * TenantEntitlement â€” 05-domain-model-organization-billing.md (D-038): Entitlement/UsageQuota
 * mÃ­nimo local, plano default/free, limite de ACTIVE_TRACKED_SUBJECTS â€” sem depender de
 * provider de billing externo (esse entra sÃ³ em M12). Billing por TrackedSubject, nÃ£o por
 * assento (02-market-research.md: nenhum concorrente pesquisado cobra por usuÃ¡rio).
 *
 * Contador atÃ´mico incrementado/decrementado na MESMA transaÃ§Ã£o que cria/remove um
 * TrackedSubject (nunca uma chamada separada) â€” evita a mesma classe de lost-update jÃ¡
 * encontrada e corrigida em TenantQuotaService (identity/application/quota.ts), e Ã© mais
 * forte que o padrÃ£o de "release" best-effort dessa classe: aqui a entidade e o contador
 * SEMPRE mudam juntos ou nenhum muda.
 */
import type { EntityKey } from "../../../shared/dynamodb/occ.js";

export const DEFAULT_PLAN_ID = "free";
/** ReferÃªncia de mercado real (02-market-research.md): bcs oferece 25 vendors grÃ¡tis, sem
 * cartÃ£o. Mesma ordem de grandeza adotada aqui como default do plano free â€” ajustÃ¡vel quando
 * billing real (M12) introduzir planos pagos. */
export const DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT = 25;

export interface TenantEntitlement extends EntityKey {
  SK: "PLAN";
  entityType: "TenantEntitlement";
  tenantId: string;
  planId: string;
  activeTrackedSubjectsLimit: number;
  activeTrackedSubjectsCount: number;
  createdAt: string;
  updatedAt: string;
  version: number;
}

export function entitlementKey(tenantId: string): { PK: string; SK: "PLAN" } {
  return { PK: `TENANT#${tenantId}#ENTITLEMENT`, SK: "PLAN" };
}

export function defaultEntitlement(tenantId: string, now: string): TenantEntitlement {
  return {
    ...entitlementKey(tenantId),
    entityType: "TenantEntitlement",
    tenantId,
    planId: DEFAULT_PLAN_ID,
    activeTrackedSubjectsLimit: DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT,
    activeTrackedSubjectsCount: 0,
    createdAt: now,
    updatedAt: now,
    version: 1,
  };
}


 succeeded in 296ms:
# Root wiring â€” ADR-0009 step 2. Terraform equivalent of infra/lib/expiration-tracker-stack.ts
# (+ infra/bin/app.ts for naming/region). Instantiates all 7+1 modules for the dev
# environment: dynamo-table, cognito, api-gateway, lambda-function (x8), sqs-worker-queue,
# reminder-schedule, reminder-observability, cost-budget.
#
# Lambda artifacts are NOT built by this configuration â€” `npm run build:lambdas`
# (scripts/build-lambdas.ts) must run first to populate dist/lambda/<handler>/index.js,
# same contract documented in the lambda-function module's variables.tf.

module "table" {
  source = "./modules/dynamo-table"

  table_name     = "${local.name_prefix}-table"
  aws_region     = var.aws_region
  aws_account_id = var.aws_account_id
  tags           = { Project = local.project_name, Environment = var.environment }
}

module "auth" {
  source = "./modules/cognito"

  user_pool_name = "${local.name_prefix}-user-pool"
  mfa_policy     = var.mfa_policy
  # Full BFF (D-053/D-054): the only real caller of this callback URL is the BFF Lambda's own
  # /bff/callback route (src/modules/bff/http/bff-handlers.ts's handleCallback), never the
  # browser directly - derived from var.app_origin so it can never drift from
  # local.bff_redirect_uri, which the BFF Lambda's BFF_REDIRECT_URI env var also uses below.
  callback_urls = [local.bff_redirect_uri]
  domain_prefix = var.bff_cognito_domain_prefix
  tags          = { Project = local.project_name, Environment = var.environment }
}

# --- Lambda functions --------------------------------------------------------------------
# Each function gets EXACTLY the IAM capabilities expiration-tracker-stack.ts grants it.
# ReminderProducer is the ONLY function granted gsi3_read; ReminderReconciliation,
# OutboxSweeperReminderDispatch, UploadSlotReconciliationWorker, and DocumentPurgeWorker are
# the ONLY four granted gsi6_read (AGENTS.md Â§7's GSI isolation rule â€” the property this whole
# migration exists to preserve; the fourth role is W3-06/D-061, acknowledged explicitly, not
# silently expanded).

locals {
  common_env = { TABLE_NAME = module.table.table_name }
  dist_dir   = "${path.module}/../dist/lambda"
}

module "test_ping_handler" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-test-ping-handler"
  handler_name          = "test-ping-handler"
  source_dir            = "${local.dist_dir}/test-ping-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [module.table.tenant_facing_read_write_policy_json]
  tags                  = { Project = local.project_name, Environment = var.environment }
}

module "items_handler" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-items-handler"
  handler_name          = "items-handler"
  source_dir            = "${local.dist_dir}/items-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [module.table.tenant_facing_read_write_policy_json]
  tags                  = { Project = local.project_name, Environment = var.environment }
}

# M10 (D-037): pepper de hash do guest token. Achado real de revisÃ£o adversarial (Codex):
# GUEST_TOKEN_PEPPER precisa chegar a QUALQUER Lambda que valide/emita token de convidado -
# faltava aqui, o que quebraria o cold start de subjects_handler e guest_documents_handler em
# runtime real. Trade-off consciente (registrado, nÃ£o escondido): valor vai como env var
# Lambda (nunca hardcoded/commitado), nÃ£o via Secrets Manager fetch em runtime - proporcional
# ao estÃ¡gio atual sem dado real de tenant em risco. Upgrade fica como follow-up.
resource "random_password" "guest_token_pepper" {
  length  = 64
  special = false
}

module "subjects_handler" {
  source = "./modules/lambda-function"

  # M9 (D-036/D-040): TrackedSubject + RequirementAssignment + ItemWatch (watchers ficam no
  # items_handler existente, reaproveitando a mesma Lambda de expiration - ver api-gateway).
  # Sem capability nova alem da geral: GSI7 e tenant-scoped, ja incluido em
  # tenant_facing_read_write_policy_json (dynamo-table module).
  function_name  = "${local.name_prefix}-subjects-handler"
  handler_name   = "subjects-handler"
  source_dir     = "${local.dist_dir}/subjects-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    GUEST_TOKEN_PEPPER = random_password.guest_token_pepper.result
    # M10 cluster 4 (D-049): mecanismo de convite inicial automatizado Ã© sempre implementado -
    # SES_FROM_ADDRESS/SES_CONFIGURATION_SET sempre wireados (reaproveita o MESMO SES jÃ¡ usado
    # por EmailDeliveryWorker/DocumentChasingDispatch, nenhum recurso novo), mas o ENVIO real
    # sÃ³ acontece se o kill switch abaixo estiver true - default false em todos os ambientes.
    DOCUMENT_REQUEST_INITIAL_INVITE_EMAIL_ENABLED = tostring(var.document_request_initial_invite_email_enabled)
    SES_FROM_ADDRESS                              = var.ses_from_address
    SES_CONFIGURATION_SET                         = module.ses_notifications.configuration_set_name
    # GUEST_UPLOAD_BASE_URL deliberadamente NÃƒO setado - mesmo placeholder documentado do
    # document_chasing_dispatch_handler (ver comentÃ¡rio lÃ¡).
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.ses_send_email.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

module "reminders_handler" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-reminders-handler"
  handler_name          = "reminders-handler"
  source_dir            = "${local.dist_dir}/reminders-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [module.table.tenant_facing_read_write_policy_json]
  tags                  = { Project = local.project_name, Environment = var.environment }
}

module "notifications_handler" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-notifications-handler"
  handler_name          = "notifications-handler"
  source_dir            = "${local.dist_dir}/notifications-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [module.table.tenant_facing_read_write_policy_json]
  tags                  = { Project = local.project_name, Environment = var.environment }
}

module "profile_handler" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-profile-handler"
  handler_name          = "profile-handler"
  source_dir            = "${local.dist_dir}/profile-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [module.table.tenant_facing_read_write_policy_json]
  tags                  = { Project = local.project_name, Environment = var.environment }
}

module "reminder_producer" {
  source = "./modules/lambda-function"

  function_name                  = "${local.name_prefix}-reminder-producer"
  handler_name                   = "reminder-producer-handler"
  source_dir                     = "${local.dist_dir}/reminder-producer-handler"
  adot_layer_arn                 = var.adot_layer_arn
  environment_variables          = local.common_env
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 2 : null
  # The ONLY function granted gsi3_read â€” never add this capability to any other function.
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.table.gsi3_read_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

module "reminder_dispatch" {
  source = "./modules/lambda-function"

  function_name                  = "${local.name_prefix}-reminder-dispatch"
  handler_name                   = "reminder-dispatch-handler"
  source_dir                     = "${local.dist_dir}/reminder-dispatch-handler"
  adot_layer_arn                 = var.adot_layer_arn
  environment_variables          = local.common_env
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 10 : null
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.dispatch_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

module "reminder_reconciliation" {
  source = "./modules/lambda-function"

  function_name                  = "${local.name_prefix}-reminder-reconciliation"
  handler_name                   = "reminder-reconciliation-handler"
  source_dir                     = "${local.dist_dir}/reminder-reconciliation-handler"
  adot_layer_arn                 = var.adot_layer_arn
  environment_variables          = local.common_env
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 1 : null
  # One of EXACTLY THREE roles granted gsi6_read (the others are OutboxSweeperReminderDispatch
  # and, since M6, UploadSlotReconciliationWorker - see security-audit.ts's
  # GlobalIndexComponent "upload-slot-reconciliation").
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.table.gsi6_read_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# DynamoDB Streams read permissions â€” Terraform's aws_lambda_event_source_mapping does not
# auto-grant IAM the way CDK's addEventSource(DynamoEventSource) does; this is the explicit
# equivalent, scoped to this table's stream ARN only. Shared by every Streams-triggered
# consumer of this table (DispatchOutboxRelay, and M4's NotificationRouter/
# NotificationEmailOutboxRelay) - the name predates M4 but the policy itself was already
# generic (table-stream-scoped, not reminder-specific).
data "aws_iam_policy_document" "dispatch_outbox_relay_stream_read" {
  statement {
    sid = "DynamoStreamRead"
    actions = [
      "dynamodb:GetRecords",
      "dynamodb:GetShardIterator",
      "dynamodb:DescribeStream",
      "dynamodb:ListStreams",
    ]
    resources = [module.table.stream_arn]
  }
}

module "dispatch_outbox_relay" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-dispatch-outbox-relay"
  handler_name   = "dispatch-outbox-relay-handler"
  source_dir     = "${local.dist_dir}/dispatch-outbox-relay-handler"
  adot_layer_arn = var.adot_layer_arn
  # M10 cluster 4 (D-039/D-046/D-048): second destination on this SAME relay Lambda/DynamoDB
  # Streams event source mapping (below) - never a new relay function just for one more queue.
  # M11 (D-042) adds a third destination; BLOCKER-B adds a fourth, same reasoning.
  environment_variables = merge(local.common_env, {
    DISPATCH_QUEUE_URL                         = module.dispatch_queue.queue_url
    DOCUMENT_CHASING_DISPATCH_QUEUE_URL        = module.document_chasing_dispatch_queue.queue_url
    IMPORT_COMMIT_QUEUE_URL                    = module.import_commit_queue.queue_url
    REMINDER_MATERIALIZATION_TRIGGER_QUEUE_URL = module.reminder_materialization_trigger_queue.queue_url
  })
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 2 : null
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.dispatch_queue.send_policy_json,
    module.document_chasing_dispatch_queue.send_policy_json,
    module.import_commit_queue.send_policy_json,
    module.reminder_materialization_trigger_queue.send_policy_json,
    data.aws_iam_policy_document.dispatch_outbox_relay_stream_read.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

module "outbox_sweeper" {
  source = "./modules/lambda-function"

  # Name preserved as-is (not renamed to reflect its now-broader M4 scope) - this function
  # is already deployed in the dev account since M3.5; renaming an aws_lambda_function forces
  # a destroy+recreate in Terraform, which is an unnecessary destructive change for a pure
  # naming cleanup. The comment above documents the real, broader scope instead.
  function_name  = "${local.name_prefix}-outbox-sweeper-reminder-dispatch"
  handler_name   = "outbox-sweeper-handler"
  source_dir     = "${local.dist_dir}/outbox-sweeper-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    DISPATCH_QUEUE_URL                         = module.dispatch_queue.queue_url
    EMAIL_DELIVER_QUEUE_URL                    = module.email_deliver_queue.queue_url
    DOCUMENT_CHASING_DISPATCH_QUEUE_URL        = module.document_chasing_dispatch_queue.queue_url
    IMPORT_COMMIT_QUEUE_URL                    = module.import_commit_queue.queue_url
    REMINDER_MATERIALIZATION_TRIGGER_QUEUE_URL = module.reminder_materialization_trigger_queue.queue_url
  })
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 2 : null
  # The second of EXACTLY THREE roles granted gsi6_read (see reminder_reconciliation above).
  # M4 extends this SAME privileged role to also send to the notification email queue
  # (m4-notification-engine-design.md Â§7.4: one sweeper covering multiple destinations, not a
  # second sweeper querying the same global GSI6 partition) - M10 cluster 4 extends it again
  # for document-chasing-dispatch, M11 (D-042) once more for import-commit, and BLOCKER-B once
  # more for the reminder-materialization-trigger queue, same reasoning.
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.table.gsi6_read_policy_json,
    module.dispatch_queue.send_policy_json,
    module.email_deliver_queue.send_policy_json,
    module.document_chasing_dispatch_queue.send_policy_json,
    module.import_commit_queue.send_policy_json,
    module.reminder_materialization_trigger_queue.send_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# --- API Gateway ---------------------------------------------------------------------------

module "api" {
  source = "./modules/api-gateway"

  api_name            = "${local.name_prefix}-api"
  user_pool_id        = module.auth.user_pool_id
  user_pool_client_id = module.auth.user_pool_client_id
  aws_region          = var.aws_region
  # Rollback design entrega 1: API Gateway integrates against the `live` alias (never
  # $LATEST) so an emergency alias repoint actually changes what a real request invokes.
  test_ping_invoke_arn          = module.test_ping_handler.live_alias_invoke_arn
  test_ping_function_name       = module.test_ping_handler.function_name
  items_invoke_arn              = module.items_handler.live_alias_invoke_arn
  items_function_name           = module.items_handler.function_name
  reminders_invoke_arn          = module.reminders_handler.live_alias_invoke_arn
  reminders_function_name       = module.reminders_handler.function_name
  notifications_invoke_arn      = module.notifications_handler.live_alias_invoke_arn
  notifications_function_name   = module.notifications_handler.function_name
  profile_invoke_arn            = module.profile_handler.live_alias_invoke_arn
  profile_function_name         = module.profile_handler.function_name
  documents_invoke_arn          = module.documents_handler.live_alias_invoke_arn
  documents_function_name       = module.documents_handler.function_name
  subjects_invoke_arn           = module.subjects_handler.live_alias_invoke_arn
  subjects_function_name        = module.subjects_handler.function_name
  guest_documents_invoke_arn    = module.guest_documents_handler.live_alias_invoke_arn
  guest_documents_function_name = module.guest_documents_handler.function_name
  imports_invoke_arn            = module.imports_handler.live_alias_invoke_arn
  imports_function_name         = module.imports_handler.function_name
  tags                          = { Project = local.project_name, Environment = var.environment }
}

# --- Full BFF (D-053/D-054) ---------------------------------------------------------------

module "bff_session_table" {
  source = "./modules/bff-session-table"

  table_name     = "${local.name_prefix}-bff-session"
  aws_region     = var.aws_region
  aws_account_id = var.aws_account_id
  tags           = { Project = local.project_name, Environment = var.environment }
}

# Same posture as guest_token_pepper above: env var only (never Secrets Manager), a
# conscious trade-off proportional to this stage without real tenant data at risk yet.
resource "random_password" "session_token_pepper" {
  length  = 64
  special = false
}

module "bff_handler" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-bff-handler"
  handler_name   = "bff-handler"
  source_dir     = "${local.dist_dir}/bff-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    BFF_SESSION_TABLE_NAME = module.bff_session_table.table_name
    SESSION_TOKEN_PEPPER   = random_password.session_token_pepper.result
    SESSION_KMS_KEY_ID     = module.bff_session_table.kms_key_id
    COGNITO_USER_POOL_ID   = module.auth.user_pool_id
    COGNITO_CLIENT_ID      = module.auth.user_pool_client_id
    COGNITO_CLIENT_SECRET  = module.auth.user_pool_client_secret
    COGNITO_DOMAIN         = "https://${module.auth.hosted_ui_domain}.auth.${var.aws_region}.amazoncognito.com"
    BFF_REDIRECT_URI       = local.bff_redirect_uri
    APP_ORIGIN             = var.app_origin
    # Server-to-server calls stay on the real API's default execution endpoint (never
    # through this same BFF's own API Gateway) - the JWT authorizer there is unaffected by
    # any of this, exactly as designed (D-053: Full BFF is additive, resource routes keep
    # accepting a direct Bearer token from any other caller too).
    API_BASE_URL = module.api.api_endpoint
  })
  # Identity module reads/writes (User/IdentityMapping/DeviceSession) on the MAIN table -
  # the SAME policy items_handler already uses, nothing new granted there. Session-table
  # access is the separate, narrowly-scoped policy from bff_session_table (D-054: never
  # merged into the general tenant_facing policy).
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.bff_session_table.bff_session_access_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

module "bff_api" {
  source = "./modules/bff-api-gateway"

  api_name          = "${local.name_prefix}-bff-api"
  bff_invoke_arn    = module.bff_handler.live_alias_invoke_arn
  bff_function_name = module.bff_handler.function_name
  app_origin        = var.app_origin
  tags              = { Project = local.project_name, Environment = var.environment }
}

# --- SPA hosting: CloudFront + S3, coexisting with the Full BFF above (ADR-0011) -----------
# docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md - a single distribution with
# dedicated /bff, /bff/* behaviors targeting module.bff_api above, default behavior serving
# the SPA from S3+OAC. No custom domain/ACM certificate yet (CloudFront's own domain is
# explicitly acceptable for dev per the ADR) - var.app_origin stays a placeholder until the
# first apply's real distribution_domain_name is known and fed back via `-var app_origin=...`,
# same placeholder-until-verified posture already used for ses_from_address/
# bff_cognito_domain_prefix in this same file.

module "spa_hosting" {
  source = "./modules/spa-hosting"

  name_prefix      = local.name_prefix
  bff_api_endpoint = module.bff_api.api_endpoint
  tags             = { Project = local.project_name, Environment = var.environment }
}

# --- WAF (M10, D-037) â€” REMOVIDO (D-051): AWS WAFv2 nÃ£o suporta associaÃ§Ã£o com API Gateway
# HTTP API v2 (sÃ³ REST API v1/ALB/AppSync/Cognito/App Runner/Verified Access/Amplify) -
# achado real no primeiro `terraform apply` de fato deste recurso (WAFInvalidParameterException
# na AssociateWebACL). O mÃ³dulo `infra/modules/waf/` foi deletado (nÃ£o sÃ³ desligado do wiring -
# a abstraÃ§Ã£o como estava era estruturalmente invÃ¡lida, reconstruir do zero quando existir
# CloudFront Ã© mais seguro que reaproveitar cÃ³digo que nunca poderia funcionar standalone).
# MitigaÃ§Ã£o imediata: throttling nativo do HTTP API (ver `route_settings`/
# `default_route_settings` no mÃ³dulo api-gateway). CloudFront+WAF registrado como dÃ©bito
# tÃ©cnico bloqueante antes de trÃ¡fego pÃºblico real de produÃ§Ã£o (`decisions-log.md` D-051).

# --- Observability: SNS alert topic (m5-observability-design.md Â§4) -----------------------
# Instantiated before the queues/observability module below so its ARN is available to wire
# into every aws_cloudwatch_metric_alarm's alarm_actions.

module "alert_topic" {
  source = "./modules/alert-topic"

  name_prefix = local.name_prefix
  alert_email = var.alert_email
  tags        = { Project = local.project_name, Environment = var.environment }
}

# --- SQS: ReminderDispatchQueue + DLQ, consumed by ReminderDispatch -----------------------

module "dispatch_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-reminder-dispatch"
  consumer_timeout_seconds = 10
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "reminder_dispatch_from_queue" {
  event_source_arn        = module.dispatch_queue.queue_arn
  function_name           = module.reminder_dispatch.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# --- SQS: ReminderMaterializationTriggerQueue + DLQ (BLOCKER-B) ---------------------------
# reminder-delivery-pipeline.md Â§4 (Codex Round H APPROVED 9.2/10): the trigger that closes
# BLOCKER-B - consumed by ReminderMaterializationTrigger, fed by the same DispatchOutboxRelay/
# OutboxSweeperReminderDispatch roles every other destination already uses (the "generic
# EventBridge path" the design doc originally assumed doesn't actually exist in this
# codebase - see outbox.ts's OutboxDestination comment).

module "reminder_materialization_trigger_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-reminder-materialization-trigger"
  consumer_timeout_seconds = 10
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

module "reminder_materialization_trigger" {
  source = "./modules/lambda-function"

  function_name                  = "${local.name_prefix}-reminder-materialization-trigger"
  handler_name                   = "reminder-materialization-trigger-handler"
  source_dir                     = "${local.dist_dir}/reminder-materialization-trigger-handler"
  adot_layer_arn                 = var.adot_layer_arn
  environment_variables          = local.common_env
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 5 : null
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.reminder_materialization_trigger_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "reminder_materialization_trigger_from_queue" {
  event_source_arn        = module.reminder_materialization_trigger_queue.queue_arn
  function_name           = module.reminder_materialization_trigger.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

resource "aws_lambda_event_source_mapping" "dispatch_outbox_relay_from_stream" {
  event_source_arn        = module.table.stream_arn
  function_name           = module.dispatch_outbox_relay.live_alias_arn
  starting_position       = "LATEST"
  batch_size              = 25
  function_response_types = ["ReportBatchItemFailures"]
}

# --- M4: Notification Engine queues, SES/SNS, workers -------------------------------------
# docs/architecture/m4-notification-engine-design.md (APPROVED, Claude 9.3/Codex 9.4).

module "router_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-notification-router"
  consumer_timeout_seconds = 10
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

module "email_deliver_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-notification-email-deliver"
  consumer_timeout_seconds = 10
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

module "ses_callback_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-ses-callback"
  consumer_timeout_seconds = 10
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

module "ses_notifications" {
  source = "./modules/ses-notifications"

  name_prefix        = local.name_prefix
  callback_queue_arn = module.ses_callback_queue.queue_arn
  aws_region         = var.aws_region
  aws_account_id     = var.aws_account_id
  tags               = { Project = local.project_name, Environment = var.environment }
}

resource "aws_sqs_queue_policy" "ses_callback_queue" {
  queue_url = module.ses_callback_queue.queue_url
  policy    = module.ses_notifications.queue_policy_json
}

# EventBridge (M3 pattern) delivers notification.intent-created.v1 to the router queue -
# the outbox already publishes it there (OutboxPublisher's generic EventBridge path, no new
# destination discriminator needed for THIS hop, only for router->email which uses the
# SQS_NOTIFICATION_EMAIL_V1 outbox destination per the design).
resource "aws_cloudwatch_event_rule" "notification_intent_created" {
  name           = "${local.name_prefix}-notification-intent-created"
  event_bus_name = "default"
  event_pattern = jsonencode({
    "detail-type" = ["notification.intent-created.v1"]
  })
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_cloudwatch_event_target" "notification_intent_created_to_router_queue" {
  rule      = aws_cloudwatch_event_rule.notification_intent_created.name
  target_id = "router-queue"
  arn       = module.router_queue.queue_arn
}

data "aws_iam_policy_document" "eventbridge_to_router_queue" {
  statement {
    sid       = "AllowEventBridgeRuleToSendMessage"
    effect    = "Allow"
    actions   = ["sqs:SendMessage"]
    resources = [module.router_queue.queue_arn]
    principals {
      type        = "Service"
      identifiers = ["events.amazonaws.com"]
    }
    condition {
      test     = "ArnEquals"
      variable = "aws:SourceArn"
      values   = [aws_cloudwatch_event_rule.notification_intent_created.arn]
    }
  }
}

resource "aws_sqs_queue_policy" "router_queue" {
  queue_url = module.router_queue.queue_url
  policy    = data.aws_iam_policy_document.eventbridge_to_router_queue.json
}

# ses:SendEmail scoped to nothing table/queue-related - a distinct capability EmailDelivery
# alone gets (docs/architecture/reviews/m4-notification-engine-design/codex-proposal-round1.md
# Â§12.4: "EmailDeliveryWorker: ... e ses:SendEmail; sem GSI6").
data "aws_iam_policy_document" "ses_send_email" {
  statement {
    sid       = "SesSendEmail"
    actions   = ["ses:SendEmail"]
    resources = ["*"] # SESv2 SendEmail doesn't support resource-level restriction by FromEmailAddress; scoped narrowly to this single action instead.
  }
}

module "notification_router" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-notification-router"
  handler_name          = "notification-router-handler"
  source_dir            = "${local.dist_dir}/notification-router-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.dispatch_outbox_relay_stream_read.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "notification_router_from_stream" {
  event_source_arn        = module.table.stream_arn
  function_name           = module.notification_router.live_alias_arn
  starting_position       = "LATEST"
  batch_size              = 25
  function_response_types = ["ReportBatchItemFailures"]
}

module "notification_email_outbox_relay" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-notification-email-outbox-relay"
  handler_name          = "notification-email-outbox-relay-handler"
  source_dir            = "${local.dist_dir}/notification-email-outbox-relay-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = merge(local.common_env, { EMAIL_DELIVER_QUEUE_URL = module.email_deliver_queue.queue_url })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.email_deliver_queue.send_policy_json,
    data.aws_iam_policy_document.dispatch_outbox_relay_stream_read.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "notification_email_outbox_relay_from_stream" {
  event_source_arn        = module.table.stream_arn
  function_name           = module.notification_email_outbox_relay.live_alias_arn
  starting_position       = "LATEST"
  batch_size              = 25
  function_response_types = ["ReportBatchItemFailures"]
}

module "email_delivery" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-email-delivery"
  handler_name   = "email-delivery-handler"
  source_dir     = "${local.dist_dir}/email-delivery-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    SES_FROM_ADDRESS      = var.ses_from_address
    SES_CONFIGURATION_SET = module.ses_notifications.configuration_set_name
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.email_deliver_queue.consume_policy_json,
    data.aws_iam_policy_document.ses_send_email.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "email_delivery_from_queue" {
  event_source_arn        = module.email_deliver_queue.queue_arn
  function_name           = module.email_delivery.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

module "ses_callback" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-ses-callback"
  handler_name          = "ses-callback-handler"
  source_dir            = "${local.dist_dir}/ses-callback-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = merge(local.common_env, { SES_ACCOUNT_ALIAS = "default" })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.ses_callback_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "ses_callback_from_queue" {
  event_source_arn        = module.ses_callback_queue.queue_arn
  function_name           = module.ses_callback.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# --- EventBridge Scheduler: producer / reconciliation (CLAIMS+DST) / outbox sweeper -------

module "schedule" {
  source = "./modules/reminder-schedule"

  # Rollback design entrega 1: EventBridge Scheduler targets + its invoke permission both use
  # the `live` alias ARN (never $LATEST/the bare function ARN) so an emergency alias repoint
  # actually changes what the schedule invokes.
  reminder_producer_function_arn        = module.reminder_producer.live_alias_arn
  reminder_producer_function_name       = module.reminder_producer.function_name
  reminder_reconciliation_function_arn  = module.reminder_reconciliation.live_alias_arn
  reminder_reconciliation_function_name = module.reminder_reconciliation.function_name
  outbox_sweeper_function_arn           = module.outbox_sweeper.live_alias_arn
  outbox_sweeper_function_name          = module.outbox_sweeper.function_name
  schedules_enabled                     = var.schedules_enabled
  tags                                  = { Project = local.project_name, Environment = var.environment }
}

# --- CloudWatch observability: per-function error alarms + dispatch queue backlog age -----

module "observability" {
  source = "./modules/reminder-observability"

  reminder_producer_function_name         = module.reminder_producer.function_name
  reminder_dispatch_function_name         = module.reminder_dispatch.function_name
  reminder_reconciliation_function_name   = module.reminder_reconciliation.function_name
  dispatch_outbox_relay_function_name     = module.dispatch_outbox_relay.function_name
  outbox_sweeper_function_name            = module.outbox_sweeper.function_name
  document_chasing_dispatch_function_name = module.document_chasing_dispatch_handler.function_name
  dispatch_queue_name                     = module.dispatch_queue.queue_name
  alert_topic_arn                         = module.alert_topic.topic_arn
  tags                                    = { Project = local.project_name, Environment = var.environment }
}

# --- Cost governance ------------------------------------------------------------------------

module "cost_budget" {
  source = "./modules/cost-budget"

  name                = "${local.name_prefix}-monthly-cost"
  monthly_limit_usd   = var.monthly_budget_usd
  notification_emails = var.budget_notification_emails
}

# --- Rollback design entrega 1: deploy manifest bucket -------------------------------------
# docs/architecture/reviews/rollback-mechanism-design/codex-round2-final-design.md Â§3.
# Operational-only bucket (deploy manifests, current-healthy pointer, rollback records) -
# never tenant data or PII, deliberately separate from the document buckets.

module "deploy_manifests" {
  source = "./modules/deploy-manifest-bucket"

  bucket_name = "${local.name_prefix}-deploy-manifests"
  tags        = { Project = local.project_name, Environment = var.environment }
}

# --- Trilha de auditoria de seguranÃ§a (MVP desta sessÃ£o) -----------------------------------
# docs/architecture/reviews/security-audit-trail-design/codex-reconciliation-round2-final-design.md
# Detecta os 2 achados reais abertos (SeguranÃ§a-Logging/OWASP A09, SRE-DetecÃ§Ã£o): negaÃ§Ã£o de
# autorizaÃ§Ã£o e acesso a GSI3/GSI6 nÃ£o tinham trilha dedicada. Reusa o alert-topic real de M5.

module "security_audit_observability" {
  source = "./modules/security-audit-observability"

  http_function_names = [
    module.items_handler.function_name,
    module.reminders_handler.function_name,
    module.notifications_handler.function_name,
    module.profile_handler.function_name,
    module.test_ping_handler.function_name,
  ]
  global_index_function_names = [
    module.reminder_producer.function_name,
    module.reminder_reconciliation.function_name,
    module.outbox_sweeper.function_name,
  ]
  alert_topic_arn = module.alert_topic.topic_arn
  tags            = { Project = local.project_name, Environment = var.environment }
}

# --- M6: Document upload e malware boundary ------------------------------------------------
# docs/architecture/reviews/m6-document-upload-design/codex-reconciliation-round2-final-design.md
# (Claude 9.4/Codex 9.6). Two physically separate buckets (quarantine/clean, own KMS keys),
# GuardDuty Malware Protection behind the `malware_protection_enabled` toggle, 5 new Lambda
# functions. No business-facing Lambda role (items-handler etc.) is ever granted any
# permission on either bucket - only the 5 functions below.

module "document_buckets" {
  source = "./modules/document-buckets"

  name_prefix = local.name_prefix
  tags        = { Project = local.project_name, Environment = var.environment }
}

module "malware_result_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-malware-result"
  consumer_timeout_seconds = 15
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

module "upload_finalizer_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-upload-finalizer"
  consumer_timeout_seconds = 30 # covers the synchronous parser-sandbox invocation below
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

# S3 "Object Created" in the quarantine bucket -> UploadFinalizerWorker. The quarantine
# bucket's own aws_s3_bucket_notification (document-buckets module) forwards to EventBridge;
# this is the rule/target/queue-policy triad that routes it onward (same shape as M4's
# notification.intent-created.v1 rule below).
resource "aws_cloudwatch_event_rule" "quarantine_object_created" {
  name           = "${local.name_prefix}-quarantine-object-created"
  event_bus_name = "default"
  event_pattern = jsonencode({
    source      = ["aws.s3"]
    detail-type = ["Object Created"]
    detail = {
      bucket = { name = [module.document_buckets.quarantine_bucket_name] }
    }
  })
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_cloudwatch_event_target" "quarantine_object_created_to_finalizer_queue" {
  rule      = aws_cloudwatch_event_rule.quarantine_object_created.name
  target_id = "upload-finalizer-queue"
  arn       = module.upload_finalizer_queue.queue_arn
}

data "aws_iam_policy_document" "eventbridge_to_upload_finalizer_queue" {
  statement {
    sid       = "AllowEventBridgeRuleToSendMessage"
    effect    = "Allow"
    actions   = ["sqs:SendMessage"]
    resources = [module.upload_finalizer_queue.queue_arn]
    principals {
      type        = "Service"
      identifiers = ["events.amazonaws.com"]
    }
    condition {
      test     = "ArnEquals"
      variable = "aws:SourceArn"
      values   = [aws_cloudwatch_event_rule.quarantine_object_created.arn]
    }
  }
}

resource "aws_sqs_queue_policy" "upload_finalizer_queue" {
  queue_url = module.upload_finalizer_queue.queue_url
  policy    = data.aws_iam_policy_document.eventbridge_to_upload_finalizer_queue.json
}

# --- ParserSandbox: isolated Lambda, no VPC/DynamoDB/clean-bucket access (M6 design) --------

data "aws_iam_policy_document" "parser_sandbox_read_quarantine" {
  statement {
    sid       = "ReadQuarantineObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:GetObjectVersion"]
    resources = ["${module.document_buckets.quarantine_bucket_arn}/*"]
  }
  statement {
    sid       = "DecryptQuarantineObjects"
    effect    = "Allow"
    actions   = ["kms:Decrypt"]
    resources = [module.document_buckets.quarantine_kms_key_arn]
  }
}

module "parser_sandbox" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-parser-sandbox"
  handler_name    = "parser-sandbox-handler"
  source_dir      = "${local.dist_dir}/parser-sandbox-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30  # M6 design: 30s hard limit on PDF structural parsing.
  memory_size     = 512 # pdf-lib needs headroom beyond the 256MB default for a 10MiB PDF.
  policy_documents_json = [
    data.aws_iam_policy_document.parser_sandbox_read_quarantine.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# --- DocumentsHandler: HTTP (POST reserve upload, DELETE document) --------------------------

data "aws_iam_policy_document" "documents_presign_quarantine_put" {
  statement {
    sid       = "PresignQuarantinePut"
    effect    = "Allow"
    actions   = ["s3:PutObject"]
    resources = ["${module.document_buckets.quarantine_bucket_arn}/*"]
  }
  statement {
    sid       = "EncryptQuarantineObjects"
    effect    = "Allow"
    actions   = ["kms:GenerateDataKey"]
    resources = [module.document_buckets.quarantine_kms_key_arn]
  }
}

module "documents_handler" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-documents-handler"
  handler_name   = "documents-handler"
  source_dir     = "${local.dist_dir}/documents-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    QUARANTINE_BUCKET_NAME = module.document_buckets.quarantine_bucket_name
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.documents_presign_quarantine_put.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# --- GuestDocumentsHandler: /guest/document-requests/{token}* (M10, D-037) ------------------
# PRIMEIRA Lambda do projeto atrÃ¡s de rota pÃºblica (sem JWT) - reusa exatamente a mesma
# capability de presign de documents_handler (mesmo bucket de quarentena), nunca uma nova.

module "guest_documents_handler" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-guest-documents-handler"
  handler_name   = "guest-documents-handler"
  source_dir     = "${local.dist_dir}/guest-documents-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    QUARANTINE_BUCKET_NAME = module.document_buckets.quarantine_bucket_name
    GUEST_TOKEN_PEPPER     = random_password.guest_token_pepper.result
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.documents_presign_quarantine_put.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# --- DocumentChasingDispatch: automated document chasing (M10 cluster 4, D-039/D-046/D-048) -

module "document_chasing_dispatch_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-document-chasing-dispatch"
  consumer_timeout_seconds = 10
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

module "document_chasing_dispatch_handler" {
  source = "./modules/lambda-function"

  # Worker de dispatch+delivery fundido (D-048: chasing v1 Ã© single-channel, sem lease/retry -
  # nÃ£o justifica um par dispatch/delivery separado como o de M3/M4). Reaproveita o mesmo SES
  # jÃ¡ usado por EmailDeliveryWorker (mesma policy data.aws_iam_policy_document.ses_send_email,
  # mesmo configuration set) - nenhum recurso SES novo.
  function_name  = "${local.name_prefix}-document-chasing-dispatch"
  handler_name   = "document-chasing-dispatch-handler"
  source_dir     = "${local.dist_dir}/document-chasing-dispatch-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    GUEST_TOKEN_PEPPER    = random_password.guest_token_pepper.result
    SES_FROM_ADDRESS      = var.ses_from_address
    SES_CONFIGURATION_SET = module.ses_notifications.configuration_set_name
    # GUEST_UPLOAD_BASE_URL deliberadamente NÃƒO setado - cÃ³digo tem um placeholder documentado
    # (https://app.example.invalid/guest/document-requests, mesma postura jÃ¡ aceita para
    # cors_allow_origins, implementation-blueprint.md Â§4.2) atÃ© existir domÃ­nio real de
    # frontend (D-047: frontend nÃ£o tem milestone atribuÃ­do ainda).
  })
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 2 : null
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.document_chasing_dispatch_queue.consume_policy_json,
    data.aws_iam_policy_document.ses_send_email.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "document_chasing_dispatch_from_queue" {
  event_source_arn        = module.document_chasing_dispatch_queue.queue_arn
  function_name           = module.document_chasing_dispatch_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# --- UploadFinalizerWorker: S3 event (via queue) -> validate + invoke ParserSandbox ---------

# Real bug found via Camada 3 (2026-08-22): UploadFinalizerWorker only had READ access to the
# quarantine bucket, but advanceAfterEvidence() (called identically by both workers) performs
# the actual quarantine->clean promotion copy whichever worker's evidence completes the pair
# LAST - which is not always MalwareResultWorker. A real upload where the finalizer's own
# evidence-persist step landed after the malware scan result hit exactly this: the finalizer
# won the PROMOTE race and got a real AccessDenied trying s3:PutObject on the clean bucket.
# Both workers need IDENTICAL object-access permissions for this reason - kept as two
# separately-named data sources (not a single shared one) so each function's IAM role
# document stays self-explanatory in `aws iam` output, but the statements are deliberately
# identical to malware_result_object_access below.
data "aws_iam_policy_document" "upload_finalizer_object_access" {
  statement {
    sid       = "ReadDeleteQuarantineObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:GetObjectVersion", "s3:DeleteObjectVersion"]
    resources = ["${module.document_buckets.quarantine_bucket_arn}/*"]
  }
  statement {
    # Real bug found via Camada 3 (2026-08-22): advanceAfterEvidence() calls headObject() on
    # the clean bucket right after copyObject() to VERIFY the promotion copy actually landed
    # before confirming CLEAN - S3's HeadObject API requires the s3:GetObject IAM action (a
    # well-known S3 quirk: there is no separate "HeadObject" IAM action), which was never
    # granted here (only PutObject was). The copy itself succeeded every time; only the
    # verification step 403'd, surfaced by the AWS SDK as an opaque "UnknownError" rather than
    # a clean AccessDenied.
    sid       = "ReadWriteCleanObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:GetObjectVersion", "s3:PutObject"]
    resources = ["${module.document_buckets.clean_bucket_arn}/*"]
  }
  statement {
    sid       = "DecryptQuarantineObjects"
    effect    = "Allow"
    actions   = ["kms:Decrypt"]
    resources = [module.document_buckets.quarantine_kms_key_arn]
  }
  statement {
    sid       = "EncryptCleanObjects"
    effect    = "Allow"
    actions   = ["kms:GenerateDataKey", "kms:Decrypt"]
    resources = [module.document_buckets.clean_kms_key_arn]
  }
}

data "aws_iam_policy_document" "upload_finalizer_invoke_parser_sandbox" {
  statement {
    sid       = "InvokeParserSandbox"
    effect    = "Allow"
    actions   = ["lambda:InvokeFunction"]
    resources = [module.parser_sandbox.function_arn]
  }
}

module "upload_finalizer_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-upload-finalizer-handler"
  handler_name    = "upload-finalizer-handler"
  source_dir      = "${local.dist_dir}/upload-finalizer-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30
  environment_variables = merge(local.common_env, {
    CLEAN_BUCKET_NAME            = module.document_buckets.clean_bucket_name
    PARSER_SANDBOX_FUNCTION_NAME = module.parser_sandbox.function_name
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.upload_finalizer_object_access.json,
    data.aws_iam_policy_document.upload_finalizer_invoke_parser_sandbox.json,
    module.upload_finalizer_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "upload_finalizer_from_queue" {
  event_source_arn        = module.upload_finalizer_queue.queue_arn
  function_name           = module.upload_finalizer_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# --- MalwareResultWorker: GuardDuty scan result (via queue) -> promote or reject -----------

data "aws_iam_policy_document" "malware_result_object_access" {
  statement {
    sid       = "ReadDeleteQuarantineObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:GetObjectVersion", "s3:DeleteObjectVersion"]
    resources = ["${module.document_buckets.quarantine_bucket_arn}/*"]
  }
  statement {
    # Real bug found via Camada 3 (2026-08-22): advanceAfterEvidence() calls headObject() on
    # the clean bucket right after copyObject() to VERIFY the promotion copy actually landed
    # before confirming CLEAN - S3's HeadObject API requires the s3:GetObject IAM action (a
    # well-known S3 quirk: there is no separate "HeadObject" IAM action), which was never
    # granted here (only PutObject was). The copy itself succeeded every time; only the
    # verification step 403'd, surfaced by the AWS SDK as an opaque "UnknownError" rather than
    # a clean AccessDenied.
    sid       = "ReadWriteCleanObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:GetObjectVersion", "s3:PutObject"]
    resources = ["${module.document_buckets.clean_bucket_arn}/*"]
  }
  statement {
    sid       = "DecryptQuarantineObjects"
    effect    = "Allow"
    actions   = ["kms:Decrypt"]
    resources = [module.document_buckets.quarantine_kms_key_arn]
  }
  statement {
    sid       = "EncryptCleanObjects"
    effect    = "Allow"
    actions   = ["kms:GenerateDataKey", "kms:Decrypt"]
    resources = [module.document_buckets.clean_kms_key_arn]
  }
}

module "malware_result_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-malware-result-handler"
  handler_name    = "malware-result-handler"
  source_dir      = "${local.dist_dir}/malware-result-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30
  environment_variables = merge(local.common_env, {
    CLEAN_BUCKET_NAME            = module.document_buckets.clean_bucket_name
    PARSER_SANDBOX_FUNCTION_NAME = module.parser_sandbox.function_name
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.malware_result_object_access.json,
    module.malware_result_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "malware_result_from_queue" {
  event_source_arn        = module.malware_result_queue.queue_arn
  function_name           = module.malware_result_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

module "document_malware_protection" {
  source = "./modules/document-malware-protection"

  malware_protection_enabled = var.malware_protection_enabled
  environment                = var.environment
  name_prefix                = local.name_prefix
  quarantine_bucket_name     = module.document_buckets.quarantine_bucket_name
  quarantine_bucket_arn      = module.document_buckets.quarantine_bucket_arn
  quarantine_kms_key_arn     = module.document_buckets.quarantine_kms_key_arn
  malware_result_queue_arn   = module.malware_result_queue.queue_arn
  malware_result_queue_url   = module.malware_result_queue.queue_url
  tags                       = { Project = local.project_name, Environment = var.environment }
}

# --- UploadSlotReconciliationWorker: EventBridge Scheduler, every 15 minutes ---------------
# Third (of exactly three, alongside ReminderReconciliation/OutboxSweeperReminderDispatch)
# role ever granted gsi6_read - see security-audit.ts's GlobalIndexComponent
# "upload-slot-reconciliation" and stack.tftest.hcl's updated GSI6 isolation assertions.

module "upload_slot_reconciliation_handler" {
  source = "./modules/lambda-function"

  function_name         = "${local.name_prefix}-upload-slot-reconciliation-handler"
  handler_name          = "upload-slot-reconciliation-handler"
  source_dir            = "${local.dist_dir}/upload-slot-reconciliation-handler"
  adot_layer_arn        = var.adot_layer_arn
  environment_variables = local.common_env
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.table.gsi6_read_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_iam_role" "upload_slot_reconciliation_schedule" {
  name = "${module.upload_slot_reconciliation_handler.function_name}-schedule-role"
  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "SchedulerAssumeRole"
        Effect    = "Allow"
        Action    = "sts:AssumeRole"
        Principal = { Service = "scheduler.amazonaws.com" }
      }
    ]
  })
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_iam_role_policy" "upload_slot_reconciliation_schedule_invoke" {
  name = "invoke"
  role = aws_iam_role.upload_slot_reconciliation_schedule.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid      = "InvokeUploadSlotReconciliation"
        Effect   = "Allow"
        Action   = "lambda:InvokeFunction"
        Resource = module.upload_slot_reconciliation_handler.live_alias_arn
      }
    ]
  })
}

# jsonencode() HTML-escapes "<"/">" (reminder-schedule/main.tf's real bug, 2026-08-21) - this
# schedule's input is a literal HCL string for the same reason, even though it has no
# scheduler context attribute today (defense against ever adding one here without noticing).
resource "aws_scheduler_schedule" "upload_slot_reconciliation" {
  name                = "upload-slot-reconciliation"
  schedule_expression = "rate(15 minutes)"
  state               = var.schedules_enabled ? "ENABLED" : "DISABLED"

  flexible_time_window {
    mode = "OFF"
  }

  target {
    arn      = module.upload_slot_reconciliation_handler.live_alias_arn
    role_arn = aws_iam_role.upload_slot_reconciliation_schedule.arn
    input    = "{}"
  }
}

# --- DocumentPurgeWorker: EventBridge Scheduler, every 6 hours (W3-06/D-061) ----------------
# Fourth (of exactly four) role ever granted gsi6_read - see security-audit.ts's
# GlobalIndexComponent "document-purge" and stack.tftest.hcl's updated GSI6 isolation
# assertions. Minimal, purpose-scoped S3 permission: `s3:DeleteObjectVersion` only (never
# `GetObject`/`DeleteObject`) on both document buckets - the worker only ever deletes a
# specific, already-known object version (`cleanObject` or upload/malware evidence, never
# `quarantineObject` - see `purge.ts`'s module doc), it never reads content.
data "aws_iam_policy_document" "document_purge_object_access" {
  statement {
    sid       = "DeleteCleanObjectVersion"
    effect    = "Allow"
    actions   = ["s3:DeleteObjectVersion"]
    resources = ["${module.document_buckets.clean_bucket_arn}/*"]
  }
  statement {
    sid       = "DeleteQuarantineObjectVersion"
    effect    = "Allow"
    actions   = ["s3:DeleteObjectVersion"]
    resources = ["${module.document_buckets.quarantine_bucket_arn}/*"]
  }
}

module "document_purge_handler" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-document-purge-handler"
  handler_name   = "document-purge-handler"
  source_dir     = "${local.dist_dir}/document-purge-handler"
  adot_layer_arn = var.adot_layer_arn
  # CDK-parity default (10s) is too short for up to 25 candidates x (claim + S3 delete +
  # finalize transaction) round trips per invocation - 60s leaves the 15min purge lease
  # (PURGE_LEASE_MS, purge.ts) at a 15x margin over the worst-case invocation duration.
  timeout_seconds       = 60
  environment_variables = local.common_env
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.table.gsi6_read_policy_json,
    data.aws_iam_policy_document.document_purge_object_access.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_iam_role" "document_purge_schedule" {
  name = "${module.document_purge_handler.function_name}-schedule-role"
  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "SchedulerAssumeRole"
        Effect    = "Allow"
        Action    = "sts:AssumeRole"
        Principal = { Service = "scheduler.amazonaws.com" }
      }
    ]
  })
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_iam_role_policy" "document_purge_schedule_invoke" {
  name = "invoke"
  role = aws_iam_role.document_purge_schedule.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid      = "InvokeDocumentPurge"
        Effect   = "Allow"
        Action   = "lambda:InvokeFunction"
        Resource = module.document_purge_handler.live_alias_arn
      }
    ]
  })
}

# 6h cadence (D-061 Â§3): the business deadline is 30 days, so this leaves ~120x margin even
# with the 25-candidate-per-invocation cap and no cross-invocation cursor (D-061 Â§"resoluÃ§Ã£o
# achado 4"). jsonencode() HTML-escapes "<"/">" - literal HCL string for the same reason as
# upload_slot_reconciliation's schedule input, even though this one has no scheduler context
# attribute either.
resource "aws_scheduler_schedule" "document_purge" {
  name                = "document-purge"
  schedule_expression = "rate(6 hours)"
  state               = var.schedules_enabled ? "ENABLED" : "DISABLED"

  flexible_time_window {
    mode = "OFF"
  }

  target {
    arn      = module.document_purge_handler.live_alias_arn
    role_arn = aws_iam_role.document_purge_schedule.arn
    input    = "{}"
  }
}

module "document_observability" {
  source = "./modules/document-observability"

  malware_result_function_name             = module.malware_result_handler.function_name
  upload_slot_reconciliation_function_name = module.upload_slot_reconciliation_handler.function_name
  alert_topic_arn                          = module.alert_topic.topic_arn
  tags                                     = { Project = local.project_name, Environment = var.environment }
}

# --- M11: CSV Import de TrackedSubject (D-042) ---------------------------------------------
# Um Ãºnico bucket (raw CSV + plano JSONL, nunca quarentena/malware scanning - fora do escopo
# de v1) e 3 novas funÃ§Ãµes: ImportsHandler (HTTP), ImportParseWorker (S3 event via fila) e
# ImportCommitWorker (SQS_IMPORT_COMMIT_V1, roteado pelo dispatch_outbox_relay/outbox_sweeper
# jÃ¡ existentes acima - nunca um relay/sweeper novo). Alarme de observabilidade por funÃ§Ã£o
# (module.import_observability, abaixo) fecha o residual documentado em D-050 - a idade da DLQ
# de cada fila (jÃ¡ embutida em sqs-worker-queue) continua sendo a rede mÃ­nima herdada, agora
# complementada por sinal de causa (exceÃ§Ã£o do worker vs. FAILED_INTEGRITY_MISMATCH real).

module "import_bucket" {
  source = "./modules/import-bucket"

  name_prefix = local.name_prefix
  tags        = { Project = local.project_name, Environment = var.environment }
}

module "import_parse_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-import-parse"
  consumer_timeout_seconds = 30
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

# S3 "Object Created" in the import bucket -> ImportParseWorker. Filtered to the literal
# `raw.csv` suffix (same bucket also receives the parse worker's OWN plan JSONL writes, which
# must never re-trigger it - defense in depth alongside parseImportRawKey's own key check).
resource "aws_cloudwatch_event_rule" "import_raw_object_created" {
  name           = "${local.name_prefix}-import-raw-object-created"
  event_bus_name = "default"
  event_pattern = jsonencode({
    source      = ["aws.s3"]
    detail-type = ["Object Created"]
    detail = {
      bucket = { name = [module.import_bucket.bucket_name] }
      object = { key = [{ suffix = "raw.csv" }] }
    }
  })
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_cloudwatch_event_target" "import_raw_object_created_to_parse_queue" {
  rule      = aws_cloudwatch_event_rule.import_raw_object_created.name
  target_id = "import-parse-queue"
  arn       = module.import_parse_queue.queue_arn
}

data "aws_iam_policy_document" "eventbridge_to_import_parse_queue" {
  statement {
    sid       = "AllowEventBridgeRuleToSendMessage"
    effect    = "Allow"
    actions   = ["sqs:SendMessage"]
    resources = [module.import_parse_queue.queue_arn]
    principals {
      type        = "Service"
      identifiers = ["events.amazonaws.com"]
    }
    condition {
      test     = "ArnEquals"
      variable = "aws:SourceArn"
      values   = [aws_cloudwatch_event_rule.import_raw_object_created.arn]
    }
  }
}

resource "aws_sqs_queue_policy" "import_parse_queue" {
  queue_url = module.import_parse_queue.queue_url
  policy    = data.aws_iam_policy_document.eventbridge_to_import_parse_queue.json
}

data "aws_iam_policy_document" "import_parse_object_access" {
  statement {
    sid       = "ReadWriteImportObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:PutObject"]
    resources = ["${module.import_bucket.bucket_arn}/*"]
  }
  statement {
    sid       = "DecryptEncryptImportObjects"
    effect    = "Allow"
    actions   = ["kms:Decrypt", "kms:GenerateDataKey"]
    resources = [module.import_bucket.kms_key_arn]
  }
}

module "import_parse_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-import-parse-handler"
  handler_name    = "import-parse-handler"
  source_dir      = "${local.dist_dir}/import-parse-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30
  environment_variables = merge(local.common_env, {
    IMPORT_RAW_BUCKET_NAME  = module.import_bucket.bucket_name
    IMPORT_PLAN_BUCKET_NAME = module.import_bucket.bucket_name
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.import_parse_object_access.json,
    module.import_parse_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "import_parse_from_queue" {
  event_source_arn        = module.import_parse_queue.queue_arn
  function_name           = module.import_parse_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# --- ImportCommitWorker: SQS_IMPORT_COMMIT_V1, fed by dispatch_outbox_relay/outbox_sweeper --

module "import_commit_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-import-commit"
  consumer_timeout_seconds = 60 # replaying up to 5.000 linhas via SubjectService.createSubject() pode levar um tempo real
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

data "aws_iam_policy_document" "import_commit_plan_object_access" {
  statement {
    sid       = "ReadPlanObjects"
    effect    = "Allow"
    actions   = ["s3:GetObject"]
    resources = ["${module.import_bucket.bucket_arn}/*"]
  }
  statement {
    sid       = "DecryptImportObjects"
    effect    = "Allow"
    actions   = ["kms:Decrypt"]
    resources = [module.import_bucket.kms_key_arn]
  }
}

module "import_commit_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-import-commit-handler"
  handler_name    = "import-commit-handler"
  source_dir      = "${local.dist_dir}/import-commit-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 60
  environment_variables = merge(local.common_env, {
    IMPORT_PLAN_BUCKET_NAME = module.import_bucket.bucket_name
  })
  reserved_concurrent_executions = var.enable_reserved_concurrency ? 2 : null
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.import_commit_plan_object_access.json,
    module.import_commit_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "import_commit_from_queue" {
  event_source_arn        = module.import_commit_queue.queue_arn
  function_name           = module.import_commit_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# --- ImportsHandler: HTTP (POST /imports, GET /imports/{jobId}, POST .../commit) -----------

data "aws_iam_policy_document" "imports_presign_raw_put" {
  statement {
    sid       = "PresignImportRawPut"
    effect    = "Allow"
    actions   = ["s3:PutObject"]
    resources = ["${module.import_bucket.bucket_arn}/*"]
  }
  statement {
    sid       = "EncryptImportObjects"
    effect    = "Allow"
    actions   = ["kms:GenerateDataKey"]
    resources = [module.import_bucket.kms_key_arn]
  }
}

module "imports_handler" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-imports-handler"
  handler_name   = "imports-handler"
  source_dir     = "${local.dist_dir}/imports-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    IMPORT_RAW_BUCKET_NAME = module.import_bucket.bucket_name
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.imports_presign_raw_put.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

module "import_observability" {
  source = "./modules/import-observability"

  import_parse_function_name  = module.import_parse_handler.function_name
  import_commit_function_name = module.import_commit_handler.function_name
  alert_topic_arn             = module.alert_topic.topic_arn
  tags                        = { Project = local.project_name, Environment = var.environment }
}

# --- M7 (extraÃ§Ã£o/OCR): feature-flags (D-035 Â§1.5/Â§1.6) ------------------------------------
# AppConfig real para os kill switches AI_EXTRACTION/OCR/WHATSAPP - primeira peÃ§a de infra do
# M7 (item 1 de NEXT_SESSION_PROMPT.md's "M7 - o que falta"), prÃ©-requisito dos workers
# futuros (TextractTaskHandler/BedrockExtractionTaskHandler consultam o kill switch antes de
# qualquer chamada paga). var.extraction_pipeline_enabled Ã© o gate Terraform separado do
# pipeline inteiro (D-035 Â§1.6) - nenhum recurso do mÃ³dulo abaixo depende dele porque o
# mÃ³dulo em si Ã© sÃ³ a entrega do AppConfig, nÃ£o o pipeline de extraÃ§Ã£o (ainda nÃ£o
# implementado); os workers futuros (itens 2+ de NEXT_SESSION_PROMPT.md) Ã© que ficam
# condicionados a esse gate.

module "feature_flags" {
  source = "./modules/feature-flags"

  name_prefix    = local.name_prefix
  aws_region     = var.aws_region
  aws_account_id = var.aws_account_id
  tags           = { Project = local.project_name, Environment = var.environment }
}

# --- M7 (extraÃ§Ã£o/OCR): ExtractionStarterWorker (item 2, D-035 Â§12.5) ---------------------
# S3 "Object Created" no bucket limpo -> ExtractionStarterWorker: cria `ExtractionRun`
# idempotente e inicia a execuÃ§Ã£o Step Functions Standard (item 3, ASL, ainda nÃ£o
# implementada). Lambda/fila/IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis em `dev`), mas o
# ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` Ã© a regra EventBridge que
# liga o bucket limpo Ã  fila - sem o gate, nenhum evento real chega ao worker, mesmo com o
# trÃ¡fego normal de upload do M6 jÃ¡ rodando continuamente em `dev` (mesma lÃ³gica descrita no
# comentÃ¡rio do mÃ³dulo feature-flags acima: o gate controla o pipeline, nÃ£o a entrega do
# AppConfig).
#
# `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3
# (ASL/Step Functions) DEVE usar para a mÃ¡quina de estados
# (`aws_sfn_state_machine "document_extraction"`, nome `${local.name_prefix}-document-
# extraction`) - convenÃ§Ã£o fixada aqui deliberadamente para que este ARN jÃ¡ esteja correto
# quando a mÃ¡quina real for criada, sem precisar trocar um placeholder depois. Inofensivo
# enquanto o gate estiver `false`: `states:StartExecution` contra uma ARN de mÃ¡quina que nÃ£o
# existe nunca Ã© chamado, porque a regra EventBridge acima nunca entrega nenhum evento.

locals {
  extraction_state_machine_arn = "arn:aws:states:${var.aws_region}:${var.aws_account_id}:stateMachine:${local.name_prefix}-document-extraction"
}

module "extraction_starter_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-extraction-starter"
  consumer_timeout_seconds = 15
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

data "aws_iam_policy_document" "extraction_starter_start_execution" {
  statement {
    sid       = "StartExtractionStateMachine"
    effect    = "Allow"
    actions   = ["states:StartExecution"]
    resources = [local.extraction_state_machine_arn]
  }
}

module "extraction_starter_handler" {
  source = "./modules/lambda-function"

  function_name  = "${local.name_prefix}-extraction-starter-handler"
  handler_name   = "extraction-starter-handler"
  source_dir     = "${local.dist_dir}/extraction-starter-handler"
  adot_layer_arn = var.adot_layer_arn
  environment_variables = merge(local.common_env, {
    EXTRACTION_STATE_MACHINE_ARN = local.extraction_state_machine_arn
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    data.aws_iam_policy_document.extraction_starter_start_execution.json,
    module.extraction_starter_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "extraction_starter_from_queue" {
  event_source_arn        = module.extraction_starter_queue.queue_arn
  function_name           = module.extraction_starter_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

resource "aws_cloudwatch_event_rule" "clean_object_created" {
  count          = var.extraction_pipeline_enabled ? 1 : 0
  name           = "${local.name_prefix}-clean-object-created"
  event_bus_name = "default"
  event_pattern = jsonencode({
    source      = ["aws.s3"]
    detail-type = ["Object Created"]
    detail = {
      bucket = { name = [module.document_buckets.clean_bucket_name] }
    }
  })
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_cloudwatch_event_target" "clean_object_created_to_extraction_starter_queue" {
  count     = var.extraction_pipeline_enabled ? 1 : 0
  rule      = aws_cloudwatch_event_rule.clean_object_created[0].name
  target_id = "extraction-starter-queue"
  arn       = module.extraction_starter_queue.queue_arn
}

data "aws_iam_policy_document" "eventbridge_to_extraction_starter_queue" {
  count = var.extraction_pipeline_enabled ? 1 : 0
  statement {
    sid       = "AllowEventBridgeRuleToSendMessage"
    effect    = "Allow"
    actions   = ["sqs:SendMessage"]
    resources = [module.extraction_starter_queue.queue_arn]
    principals {
      type        = "Service"
      identifiers = ["events.amazonaws.com"]
    }
    condition {
      test     = "ArnEquals"
      variable = "aws:SourceArn"
      values   = [aws_cloudwatch_event_rule.clean_object_created[0].arn]
    }
  }
}

resource "aws_sqs_queue_policy" "extraction_starter_queue" {
  count     = var.extraction_pipeline_enabled ? 1 : 0
  queue_url = module.extraction_starter_queue.queue_url
  policy    = data.aws_iam_policy_document.eventbridge_to_extraction_starter_queue[0].json
}

# --- M7 (extraÃ§Ã£o/OCR): TextractTaskHandler (items 3/4, D-035) -----------------------------
# Real runtime for the ASL's only real Task state so far (`RunTextract`,
# infra/state-machines/document-extraction.asl.json). Deliberately NOT gated by
# `var.extraction_pipeline_enabled` for the same reason item 2's Lambda/queue/IAM aren't
# gated either: the resources below are inert on their own. `START_OCR` can only ever run if
# Step Functions invokes this function via `lambda:invoke.waitForTaskToken`, which requires
# the `document_extraction` state machine to exist - it doesn't yet (`infra/modules/
# extraction-workflow` is still deliberately uninstantiated, items 5-7's Lambdas don't exist).
# Without that invocation, `startDocumentTextDetection` (the only paid call in this whole
# handler) is never reached by any real event source, so leaving this infra always-on/
# inspectable in `dev` carries the same "deployable but never triggered by real traffic" risk
# profile item 2 already accepted for the identical reason.
#
# `module.textract_task_handler.live_alias_arn` is the ARN item 3's Terraform module
# (extraction-workflow) needs once it's finally instantiated - exposed via
# `local.textract_task_handler_function_arn` below so that wiring is a small diff, not a
# redesign (same intent as `local.extraction_state_machine_arn` above).

# Dedicated CMK for the Step Functions task-token ciphertext (`TextractJob.taskTokenCiphertext`)
# - same D-054 disciplina as `infra/modules/bff-session-table`'s refresh-token CMK: a live
# Step Functions callback credential is exactly the class of "live credential, not just
# storage-at-rest" value that decision reserved a dedicated CMK for, distinct from the
# AWS-managed key `document-buckets` uses for document blobs (a much less sensitive value).
# Reusing an existing CMK was considered (see NEXT_SESSION_PROMPT.md) and rejected: sharing a
# key across two independently-deployable modules (bff/extraction) would couple their key
# policies/rotation and blur the "only this module's Lambda role may ever use this key"
# invariant D-054 established.
resource "aws_kms_key" "task_token" {
  description             = "CMK for TextractTaskHandler's Step Functions task-token ciphertext at rest (D-035)"
  deletion_window_in_days = 30
  enable_key_rotation     = true
  tags                    = { Project = local.project_name, Environment = var.environment }
}

resource "aws_kms_alias" "task_token" {
  name          = "alias/${local.name_prefix}-task-token"
  target_key_id = aws_kms_key.task_token.key_id
}

# `EXTRACTION_TRANSIENT` S3 class (privacy-lgpd.md Â§4) - dedicated bucket, no versioning/
# backup/replication (the artifact is disposable OCR text, never a document of record), 24h
# lifecycle safety net matching `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS` (retention.ts) exactly
# (24h = 1 day, S3 lifecycle `expiration.days` has no hour granularity). Explicit deletion is
# always `ExtractionValidationTaskHandler`'s job (item 7, implemented - run-extraction-validation.ts
# calls artifacts.delete() at both terminal states) - this lifecycle rule only catches a run
# that never reaches a terminal state.
resource "aws_s3_bucket" "extraction_transient" {
  bucket        = "${local.name_prefix}-extraction-transient"
  force_destroy = false
  tags          = { Project = local.project_name, Environment = var.environment }
}

resource "aws_s3_bucket_public_access_block" "extraction_transient" {
  bucket                  = aws_s3_bucket.extraction_transient.id
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

resource "aws_s3_bucket_ownership_controls" "extraction_transient" {
  bucket = aws_s3_bucket.extraction_transient.id
  rule {
    object_ownership = "BucketOwnerEnforced"
  }
}

resource "aws_s3_bucket_server_side_encryption_configuration" "extraction_transient" {
  bucket = aws_s3_bucket.extraction_transient.id
  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "AES256" # same lesson as spa-hosting's SSE-KMS/OAC incident - no
      # cross-service (Lambda-only) reader here needs KMS, plain SSE-S3 avoids that whole
      # class of bug for a bucket nothing but this Lambda's own role ever touches.
    }
    bucket_key_enabled = true
  }
}

resource "aws_s3_bucket_lifecycle_configuration" "extraction_transient" {
  bucket = aws_s3_bucket.extraction_transient.id
  rule {
    id     = "expire-transient-ocr-artifacts"
    status = "Enabled"
    filter {} # the whole bucket is transient by design.
    expiration {
      days = 1 # EXTRACTION_TRANSIENT_LIFECYCLE_HOURS (retention.ts) = 24h = 1 day.
    }
    abort_incomplete_multipart_upload {
      days_after_initiation = 1
    }
  }
}

resource "aws_s3_bucket_policy" "extraction_transient" {
  bucket = aws_s3_bucket.extraction_transient.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyInsecureTransport"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource  = [aws_s3_bucket.extraction_transient.arn, "${aws_s3_bucket.extraction_transient.arn}/*"]
        Condition = { Bool = { "aws:SecureTransport" = "false" } }
      }
    ]
  })
}

# SNS topic Textract publishes job-completion notifications to (StartDocumentTextDetection's
# NotificationChannel). The role below is what Textract itself assumes to call sns:Publish -
# distinct from this Lambda's own execution role.
resource "aws_sns_topic" "textract_completion" {
  name = "${local.name_prefix}-textract-completion"
  tags = { Project = local.project_name, Environment = var.environment }
}

data "aws_iam_policy_document" "textract_assume_role" {
  statement {
    effect  = "Allow"
    actions = ["sts:AssumeRole"]
    principals {
      type        = "Service"
      identifiers = ["textract.amazonaws.com"]
    }
  }
}

resource "aws_iam_role" "textract_sns_publisher" {
  name               = "${local.name_prefix}-textract-sns-publisher"
  assume_role_policy = data.aws_iam_policy_document.textract_assume_role.json
  tags               = { Project = local.project_name, Environment = var.environment }
}

data "aws_iam_policy_document" "textract_sns_publish" {
  statement {
    sid       = "TextractPublishCompletion"
    effect    = "Allow"
    actions   = ["sns:Publish"]
    resources = [aws_sns_topic.textract_completion.arn]
  }
}

resource "aws_iam_role_policy" "textract_sns_publisher" {
  name   = "publish"
  role   = aws_iam_role.textract_sns_publisher.id
  policy = data.aws_iam_policy_document.textract_sns_publish.json
}

# SQS queue + DLQ for COMPLETE_OCR, subscribed to the SNS topic above.
module "textract_completion_queue" {
  source = "./modules/sqs-worker-queue"

  queue_name               = "${local.name_prefix}-textract-completion"
  consumer_timeout_seconds = 30
  aws_region               = var.aws_region
  aws_account_id           = var.aws_account_id
  alert_topic_arn          = module.alert_topic.topic_arn
  tags                     = { Project = local.project_name, Environment = var.environment }
}

resource "aws_sns_topic_subscription" "textract_completion_to_queue" {
  topic_arn = aws_sns_topic.textract_completion.arn
  protocol  = "sqs"
  endpoint  = module.textract_completion_queue.queue_arn
}

data "aws_iam_policy_document" "sns_to_textract_completion_queue" {
  statement {
    sid       = "AllowSnsToSendMessage"
    effect    = "Allow"
    actions   = ["sqs:SendMessage"]
    resources = [module.textract_completion_queue.queue_arn]
    principals {
      type        = "Service"
      identifiers = ["sns.amazonaws.com"]
    }
    condition {
      test     = "ArnEquals"
      variable = "aws:SourceArn"
      values   = [aws_sns_topic.textract_completion.arn]
    }
  }
}

resource "aws_sqs_queue_policy" "textract_completion_queue" {
  queue_url = module.textract_completion_queue.queue_url
  policy    = data.aws_iam_policy_document.sns_to_textract_completion_queue.json
}

# --- IAM for TextractTaskHandler's own execution role --------------------------------------

data "aws_iam_policy_document" "textract_task_textract_calls" {
  statement {
    sid       = "TextractStartAndGet"
    effect    = "Allow"
    actions   = ["textract:StartDocumentTextDetection", "textract:GetDocumentTextDetection"]
    resources = ["*"] # Textract's async detection APIs are not resource-scopable (no ARN
    # concept for a job before it exists) - same posture AWS's own example IAM policies for
    # this API use.
  }
  statement {
    sid       = "TextractPassSnsPublisherRole"
    effect    = "Allow"
    actions   = ["iam:PassRole"]
    resources = [aws_iam_role.textract_sns_publisher.arn]
  }
}

data "aws_iam_policy_document" "textract_task_send_task_outcome" {
  statement {
    sid    = "SendTaskOutcome"
    effect = "Allow"
    actions = [
      "states:SendTaskSuccess",
      "states:SendTaskFailure",
      "states:SendTaskHeartbeat",
    ]
    resources = ["*"] # SendTask* is authorized by possession of the task token itself, not by
    # a resource ARN Step Functions can check ahead of time (AWS's own documented posture for
    # this API family).
  }
}

data "aws_iam_policy_document" "textract_task_kms" {
  statement {
    sid       = "TaskTokenCrypto"
    effect    = "Allow"
    actions   = ["kms:Encrypt", "kms:Decrypt", "kms:GenerateDataKey"]
    resources = [aws_kms_key.task_token.arn]
  }
}

data "aws_iam_policy_document" "textract_task_s3" {
  statement {
    sid       = "ReadCleanBucket"
    effect    = "Allow"
    actions   = ["s3:GetObject"]
    resources = ["${module.document_buckets.clean_bucket_arn}/*"]
  }
  statement {
    sid       = "DecryptCleanBucketKey"
    effect    = "Allow"
    actions   = ["kms:Decrypt"]
    resources = [module.document_buckets.clean_kms_key_arn]
  }
  statement {
    sid       = "ReadWriteExtractionTransientBucket"
    effect    = "Allow"
    actions   = ["s3:GetObject", "s3:PutObject"]
    resources = ["${aws_s3_bucket.extraction_transient.arn}/*"]
  }
}

locals {
  textract_task_handler_appconfig_ids = {
    application_id           = module.feature_flags.application_id
    environment_id           = module.feature_flags.environment_id
    configuration_profile_id = module.feature_flags.configuration_profile_id
  }
}

module "textract_task_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-textract-task-handler"
  handler_name    = "textract-task-handler"
  source_dir      = "${local.dist_dir}/textract-task-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30
  environment_variables = merge(local.common_env, {
    EXTRACTION_TRANSIENT_BUCKET_NAME   = aws_s3_bucket.extraction_transient.bucket
    TASK_TOKEN_KMS_KEY_ID              = aws_kms_key.task_token.key_id
    TEXTRACT_SNS_TOPIC_ARN             = aws_sns_topic.textract_completion.arn
    TEXTRACT_SNS_ROLE_ARN              = aws_iam_role.textract_sns_publisher.arn
    APPCONFIG_APPLICATION_ID           = local.textract_task_handler_appconfig_ids.application_id
    APPCONFIG_ENVIRONMENT_ID           = local.textract_task_handler_appconfig_ids.environment_id
    APPCONFIG_CONFIGURATION_PROFILE_ID = local.textract_task_handler_appconfig_ids.configuration_profile_id
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json,
    module.feature_flags.feature_flags_read_policy_json,
    data.aws_iam_policy_document.textract_task_textract_calls.json,
    data.aws_iam_policy_document.textract_task_send_task_outcome.json,
    data.aws_iam_policy_document.textract_task_kms.json,
    data.aws_iam_policy_document.textract_task_s3.json,
    module.textract_completion_queue.consume_policy_json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

resource "aws_lambda_event_source_mapping" "textract_task_from_completion_queue" {
  event_source_arn        = module.textract_completion_queue.queue_arn
  function_name           = module.textract_task_handler.live_alias_arn
  batch_size              = 10
  function_response_types = ["ReportBatchItemFailures"]
}

# `START_OCR` invocation permission for Step Functions - deliberately granted here even
# though no state machine exists yet (same "deployable, not yet wired" posture as the rest of
# this section): item 3's extraction-workflow module needs this permission to already exist
# when it's finally instantiated, and granting invoke to a specific state machine ARN (rather
# than "*") is safe to create ahead of time (`local.extraction_state_machine_arn`, already
# defined above, is deterministic even before that state machine is created).
resource "aws_lambda_permission" "textract_task_from_state_machine" {
  statement_id  = "AllowInvokeFromDocumentExtractionStateMachine"
  action        = "lambda:InvokeFunction"
  function_name = module.textract_task_handler.live_alias_arn
  qualifier     = module.textract_task_handler.live_alias_name
  principal     = "states.amazonaws.com"
  source_arn    = local.extraction_state_machine_arn
}

locals {
  # Item 3's extraction-workflow module wiring point - see the module's own `variables.tf`
  # (parameterized by Lambda ARN variables, still not instantiated from infra/main.tf).
  textract_task_handler_function_arn = module.textract_task_handler.live_alias_arn
}

# --- M7 (extraÃ§Ã£o/OCR): PdfParserTaskHandler (item 5, D-035 Â§1.3) --------------------------
# Real runtime for the ASL's `RunDeterministicParser` state - a MUCH narrower footprint than
# TextractTaskHandler: no DynamoDB, no Textract, no KMS, no SNS/SQS, no Step Functions client
# (the ASL state is a plain `arn:aws:states:::lambda:invoke`, not `waitForTaskToken` - there
# is no task token anywhere in this handler). Only reads the `EXTRACTION_TRANSIENT` bucket and
# the shared `feature-flags` AppConfig application. Same "deployable but inert until the state
# machine exists" posture as items 2/4 above - not gated by `var.extraction_pipeline_enabled`.

data "aws_iam_policy_document" "pdf_parser_task_s3" {
  statement {
    sid       = "ReadExtractionTransientBucket"
    effect    = "Allow"
    actions   = ["s3:GetObject"]
    resources = ["${aws_s3_bucket.extraction_transient.arn}/*"]
  }
}

locals {
  pdf_parser_task_handler_appconfig_ids = {
    application_id           = module.feature_flags.application_id
    environment_id           = module.feature_flags.environment_id
    configuration_profile_id = module.feature_flags.configuration_profile_id
  }
}

module "pdf_parser_task_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-pdf-parser-task-handler"
  handler_name    = "pdf-parser-task-handler"
  source_dir      = "${local.dist_dir}/pdf-parser-task-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30
  environment_variables = merge(local.common_env, {
    EXTRACTION_TRANSIENT_BUCKET_NAME   = aws_s3_bucket.extraction_transient.bucket
    APPCONFIG_APPLICATION_ID           = local.pdf_parser_task_handler_appconfig_ids.application_id
    APPCONFIG_ENVIRONMENT_ID           = local.pdf_parser_task_handler_appconfig_ids.environment_id
    APPCONFIG_CONFIGURATION_PROFILE_ID = local.pdf_parser_task_handler_appconfig_ids.configuration_profile_id
  })
  policy_documents_json = [
    module.feature_flags.feature_flags_read_policy_json,
    data.aws_iam_policy_document.pdf_parser_task_s3.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# `RunDeterministicParser` invocation permission for Step Functions - same "deployable, not
# yet wired" posture as `aws_lambda_permission.textract_task_from_state_machine` above.
resource "aws_lambda_permission" "pdf_parser_task_from_state_machine" {
  statement_id  = "AllowInvokeFromDocumentExtractionStateMachine"
  action        = "lambda:InvokeFunction"
  function_name = module.pdf_parser_task_handler.live_alias_arn
  qualifier     = module.pdf_parser_task_handler.live_alias_name
  principal     = "states.amazonaws.com"
  source_arn    = local.extraction_state_machine_arn
}

locals {
  # Item 3's extraction-workflow module wiring point (same intent as
  # local.textract_task_handler_function_arn above).
  pdf_parser_task_handler_function_arn = module.pdf_parser_task_handler.live_alias_arn
}

# --- M7 (extraÃ§Ã£o/OCR): BedrockExtractionTaskHandler (item 6, D-035 Â§1.9/Â§1.11) ------------
# Real runtime for the ASL's `RunBedrock` state - same "plain synchronous lambda:invoke, no
# task token" shape as PdfParserTaskHandler above, but needs DynamoDB (TenantQuotaService's
# AI_CALL reservation, same table/pattern as TextractTaskHandler), S3 read-only on the
# EXTRACTION_TRANSIENT bucket (never write - this handler never produces a new OCR artifact,
# only reads the one PdfParserTaskHandler already read), the shared feature-flags AppConfig
# application, and `bedrock:InvokeModel`/`bedrock:Converse` scoped to the placeholder model ARN
# pattern (var.bedrock_model_id/var.bedrock_region - design Â§4, model/region selection
# deliberately out of scope for this session). No VPC, no Textract/other-service access - same
# blast-radius isolation discipline as item 5. Same "deployable but inert until the state
# machine exists" posture as items 2/4/5 - not gated by var.extraction_pipeline_enabled.

data "aws_iam_policy_document" "bedrock_extraction_task_s3" {
  statement {
    sid       = "ReadExtractionTransientBucket"
    effect    = "Allow"
    actions   = ["s3:GetObject"]
    resources = ["${aws_s3_bucket.extraction_transient.arn}/*"]
  }
}

data "aws_iam_policy_document" "bedrock_extraction_task_bedrock_calls" {
  statement {
    sid     = "BedrockConverse"
    effect  = "Allow"
    actions = ["bedrock:InvokeModel", "bedrock:Converse"]
    # Scoped to the placeholder model ID's ARN pattern in the configured region/account, not
    # "*" - narrowly scoped even though the model ID itself is a placeholder (design Â§4: the
    # PERMISSION shape is not blocked on the model decision, only the model choice is).
    resources = [
      "arn:aws:bedrock:${var.bedrock_region}::foundation-model/${var.bedrock_model_id}",
    ]
  }
}

locals {
  bedrock_extraction_task_handler_appconfig_ids = {
    application_id           = module.feature_flags.application_id
    environment_id           = module.feature_flags.environment_id
    configuration_profile_id = module.feature_flags.configuration_profile_id
  }
}

module "bedrock_extraction_task_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-bedrock-extraction-task-handler"
  handler_name    = "bedrock-extraction-task-handler"
  source_dir      = "${local.dist_dir}/bedrock-extraction-task-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 60 # a Converse call is slower than the deterministic parser's plain S3 read
  environment_variables = merge(local.common_env, {
    EXTRACTION_TRANSIENT_BUCKET_NAME   = aws_s3_bucket.extraction_transient.bucket
    BEDROCK_MODEL_ID                   = var.bedrock_model_id
    BEDROCK_REGION                     = var.bedrock_region
    APPCONFIG_APPLICATION_ID           = local.bedrock_extraction_task_handler_appconfig_ids.application_id
    APPCONFIG_ENVIRONMENT_ID           = local.bedrock_extraction_task_handler_appconfig_ids.environment_id
    APPCONFIG_CONFIGURATION_PROFILE_ID = local.bedrock_extraction_task_handler_appconfig_ids.configuration_profile_id
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json, # TenantQuotaService's AI_CALL reservation
    module.feature_flags.feature_flags_read_policy_json,
    data.aws_iam_policy_document.bedrock_extraction_task_s3.json,
    data.aws_iam_policy_document.bedrock_extraction_task_bedrock_calls.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# `RunBedrock` invocation permission for Step Functions - same "deployable, not yet wired"
# posture as the other task-handler permissions above.
resource "aws_lambda_permission" "bedrock_extraction_task_from_state_machine" {
  statement_id  = "AllowInvokeFromDocumentExtractionStateMachine"
  action        = "lambda:InvokeFunction"
  function_name = module.bedrock_extraction_task_handler.live_alias_arn
  qualifier     = module.bedrock_extraction_task_handler.live_alias_name
  principal     = "states.amazonaws.com"
  source_arn    = local.extraction_state_machine_arn
}

locals {
  # Item 3's extraction-workflow module wiring point (same intent as
  # local.textract_task_handler_function_arn/local.pdf_parser_task_handler_function_arn above).
  bedrock_extraction_task_handler_function_arn = module.bedrock_extraction_task_handler.live_alias_arn
}

# --- M7 (extraÃ§Ã£o/OCR): ExtractionValidationTaskHandler (item 7, D-035 Â§2/Â§3) --------------
# Real runtime for the ASL's `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/
# `MarkPendingConfirmation`/`CompleteRun` states - all five invoke this SAME Lambda with a
# distinct `operation` payload field (kept as separate Task states in the ASL for per-stage
# audit/Catch, never collapsed - design Â§2's closing paragraph). Narrowest footprint of the
# four extraction Lambdas: only DynamoDB (read the `Document` discard-guard, write
# `ExtractedField`/update `ExtractionRun`) and S3 delete on the `EXTRACTION_TRANSIENT` bucket
# (the ONE Lambda in the whole pipeline allowed to delete there, per design Â§3 - deliberately
# scoped to `s3:DeleteObject` only, no `s3:GetObject`/`PutObject`, this handler never reads or
# writes the artifact's contents itself). No Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions
# client, no AppConfig (the kill switches were already read/enforced by items 4/5/6 upstream).
# Same "deployable but inert until the state machine exists" posture as items 2/4/5/6 - not
# gated by var.extraction_pipeline_enabled.

data "aws_iam_policy_document" "extraction_validation_task_s3_delete" {
  statement {
    sid       = "DeleteExtractionTransientArtifact"
    effect    = "Allow"
    actions   = ["s3:DeleteObject"]
    resources = ["${aws_s3_bucket.extraction_transient.arn}/*"]
  }
}

module "extraction_validation_task_handler" {
  source = "./modules/lambda-function"

  function_name   = "${local.name_prefix}-extraction-validation-task-handler"
  handler_name    = "extraction-validation-task-handler"
  source_dir      = "${local.dist_dir}/extraction-validation-task-handler"
  adot_layer_arn  = var.adot_layer_arn
  timeout_seconds = 30
  environment_variables = merge(local.common_env, {
    EXTRACTION_TRANSIENT_BUCKET_NAME = aws_s3_bucket.extraction_transient.bucket
  })
  policy_documents_json = [
    module.table.tenant_facing_read_write_policy_json, # Document read, ExtractedField/ExtractionRun writes
    data.aws_iam_policy_document.extraction_validation_task_s3_delete.json,
  ]
  tags = { Project = local.project_name, Environment = var.environment }
}

# Step Functions invokes this Lambda for FIVE distinct states (ValidateSchema/CompareExtractors/
# PersistExtractedFields/MarkPendingConfirmation/CompleteRun) - one permission covers all of
# them, since `lambda:InvokeFunction` isn't per-state.
resource "aws_lambda_permission" "extraction_validation_task_from_state_machine" {
  statement_id  = "AllowInvokeFromDocumentExtractionStateMachine"
  action        = "lambda:InvokeFunction"
  function_name = module.extraction_validation_task_handler.live_alias_arn
  qualifier     = module.extraction_validation_task_handler.live_alias_name
  principal     = "states.amazonaws.com"
  source_arn    = local.extraction_state_machine_arn
}

locals {
  # Item 3's extraction-workflow module wiring point (same intent as the other three ARNs
  # above) - with this, ALL FOUR Lambdas the ASL references now exist for real, so item 3
  # (instantiating the actual aws_sfn_state_machine resource) is unblocked. See
  # NEXT_SESSION_PROMPT.md.
  extraction_validation_task_handler_function_arn = module.extraction_validation_task_handler.live_alias_arn
}

# --- M7 item 3: the real document-extraction Step Functions Standard state machine ---------
# All four Lambdas the ASL references (items 4-7) now exist for real above - this instantiates
# infra/modules/extraction-workflow/, which was deliberately left uncalled until now (an
# aws_sfn_state_machine whose `definition` embeds a Lambda ARN fails terraform apply itself,
# not just runtime, if any of those four functions doesn't exist yet).
#
# Gate discipline (same posture as items 2/4/5/6/7 above, D-035 Â§1.6): this resource, its
# execution IAM role, and the four states.amazonaws.com invoke permissions (already granted on
# each handler's own role, items 4-7) always exist - inspectable/deployable regardless of
# var.extraction_pipeline_enabled. Nothing here is gated, because gating it would be
# redundant: item 2 already gates the ONLY live entry point that can ever call
# aws_sfn_state_machine:StartExecution on this state machine (the EventBridge rule connecting
# the M6 clean-bucket event to ExtractionStarterWorker) - with that gate at `false` (default),
# this state machine can exist, be inspected, and even be started manually for a scratch/test
# execution, but never receives real traffic. A second gate at this layer would just duplicate
# item 2's without adding any real protection.
data "aws_iam_policy_document" "extraction_workflow_assume_role" {
  statement {
    sid     = "StatesAssumeRole"
    effect  = "Allow"
    actions = ["sts:AssumeRole"]
    principals {
      type        = "Service"
      identifiers = ["states.amazonaws.com"]
    }
  }
}

resource "aws_iam_role" "extraction_workflow_state_machine" {
  name               = "${local.name_prefix}-extraction-workflow-role"
  assume_role_policy = data.aws_iam_policy_document.extraction_workflow_assume_role.json
  tags               = { Project = local.project_name, Environment = var.environment }
}

# lambda:InvokeFunction on the exact four live-alias ARNs the ASL's Task states target -
# never a wildcard. Each handler's own execution role already grants states.amazonaws.com
# permission to invoke IT (aws_lambda_permission.*_from_state_machine, items 4-7 above,
# scoped to this same state machine's deterministic ARN) - this is the other half of that
# trust relationship, the state machine's own permission to call out to them.
data "aws_iam_policy_document" "extraction_workflow_invoke_lambdas" {
  statement {
    sid    = "InvokeExtractionPipelineLambdas"
    effect = "Allow"
    actions = [
      "lambda:InvokeFunction",
    ]
    resources = [
      local.textract_task_handler_function_arn,
      local.pdf_parser_task_handler_function_arn,
      local.bedrock_extraction_task_handler_function_arn,
      local.extraction_validation_task_handler_function_arn,
    ]
  }

  # CloudWatch Logs delivery for aws_sfn_state_machine's logging_configuration - AWS requires
  # these exact actions on "*" (they operate on the account-level log delivery subsystem, not
  # a specific log group ARN); documented by AWS as the minimum IAM for state machine logging.
  statement {
    sid    = "StateMachineLogDelivery"
    effect = "Allow"
    actions = [
      "logs:CreateLogDelivery",
      "logs:GetLogDelivery",
      "logs:UpdateLogDelivery",
      "logs:DeleteLogDelivery",
      "logs:ListLogDeliveries",
      "logs:PutResourcePolicy",
      "logs:DescribeResourcePolicies",
      "logs:DescribeLogGroups",
    ]
    resources = ["*"]
  }

  # X-Ray - same AWSXRayDaemonWriteAccess-equivalent actions the lambda-function module
  # attaches to every Lambda when tracing_active is true (main.tf's adot_layer_arn wiring),
  # replicated here for the state machine's own X-Ray participation (tracing_configuration
  # above). AWS's own X-Ray managed policy for Step Functions uses this same "*" resource.
  statement {
    sid    = "StateMachineXRayWrite"
    effect = "Allow"
    actions = [
      "xray:PutTraceSegments",
      "xray:PutTelemetryRecords",
      "xray:GetSamplingRules",
      "xray:GetSamplingTargets",
    ]
    resources = ["*"]
  }
}

resource "aws_iam_role_policy" "extraction_workflow_state_machine" {
  name   = "${local.name_prefix}-extraction-workflow-policy"
  role   = aws_iam_role.extraction_workflow_state_machine.id
  policy = data.aws_iam_policy_document.extraction_workflow_invoke_lambdas.json
}

module "extraction_workflow" {
  source = "./modules/extraction-workflow"

  name_prefix                             = local.name_prefix
  textract_task_function_arn              = local.textract_task_handler_function_arn
  pdf_parser_task_function_arn            = local.pdf_parser_task_handler_function_arn
  bedrock_extraction_task_function_arn    = local.bedrock_extraction_task_handler_function_arn
  extraction_validation_task_function_arn = local.extraction_validation_task_handler_function_arn
  state_machine_role_arn                  = aws_iam_role.extraction_workflow_state_machine.arn
  tags                                    = { Project = local.project_name, Environment = var.environment }
}


 succeeded in 299ms:
/**
 * TenantQuota token bucket â€” data-model.md Â§"Rastreabilidade a gates de abuso/custo":
 * PK=TENANT#t#QUOTA, SK=TYPE#<quotaType>#<window>, decremented via ConditionExpression
 * "no race condition between concurrent requests of the same tenant" (same pattern as
 * UploadSlot). Enforced at the API/Lambda boundary per M1 deliverable list.
 */
import { QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";

export type QuotaType =
  | "API_REQUEST"
  | "UPLOAD_BYTES"
  | "UPLOAD_COUNT"
  | "AI_CALL"
  | "NOTIFICATION_EMAIL"
  // M11 (CSV import/export, D-042, 09-domain-model-csv-import.md): separados de UPLOAD_* (M6) -
  // import Ã© uma superfÃ­cie de processamento em massa com um perfil de abuso diferente.
  | "IMPORT_COUNT"
  | "IMPORT_BYTES"
  | "IMPORT_ROWS";

export interface TenantQuotaRecord {
  PK: string;
  SK: string;
  entityType: "TenantQuota";
  tenantId: string;
  quotaType: QuotaType;
  limit: number;
  windowSeconds: number;
  count: number;
  resetAt: string;
  killSwitchOverride?: boolean;
}

export function tenantQuotaKey(tenantId: string, quotaType: QuotaType, window: string): EntityKey {
  return { PK: `TENANT#${tenantId}#QUOTA`, SK: `TYPE#${quotaType}#${window}` };
}

export interface QuotaCheckInput {
  tenantId: string;
  quotaType: QuotaType;
  window: string; // e.g. rolling window identifier/bucket, caller-defined granularity
  limit: number;
  windowSeconds: number;
}

/**
 * Fixed-window token bucket (simplest correct primitive satisfying "atomic decrement,
 * no race condition" from data-model.md; a true sliding-window/leaky-bucket is a
 * documented possible refinement, not required by the blueprint text). Consumes 1 unit
 * per call; throws QuotaExceededError (mapped to 429 by the http layer) when exhausted.
 * killSwitchOverride, when true, denies regardless of remaining count (AppConfig-driven
 * emergency block, per data-model.md).
 */
export class TenantQuotaService {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  /**
   * Bounded retry count for the read-check-conditionalWrite loop below (full audit round1,
   * eixo GovernanÃ§a de Produto, critÃ©rio 3 â€” the previous unconditional `store.update` allowed
   * lost updates between two concurrent `consume` calls for the same tenant/quotaType: both
   * could read the same `count`, both compute `count+1`, and the second overwrite would
   * silently discard the first increment, letting a tenant exceed its quota). 20 attempts
   * covers a burst of simultaneous requests against the same partition key without needing
   * backoff/jitter for this fixed-window primitive's expected contention level; exhausting it
   * means genuine sustained hot contention, not a normal path.
   */
  private static readonly MAX_CONTENTION_RETRIES = 20;

  async consume(input: QuotaCheckInput): Promise<void> {
    for (let attempt = 0; attempt < TenantQuotaService.MAX_CONTENTION_RETRIES; attempt++) {
      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
      const nowIso = this.now();
      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();

      const existing = await this.store.get<TenantQuotaRecord>(key);
      if (!existing) {
        const created = await this.store.putIfAbsent({
          ...key,
          entityType: "TenantQuota",
          tenantId: input.tenantId,
          quotaType: input.quotaType,
          limit: input.limit,
          windowSeconds: input.windowSeconds,
          count: 1,
          resetAt,
        });
        if (created) return;
        // Lost the create race; loop to re-read the record another caller just created.
        continue;
      }

      if (existing.killSwitchOverride) {
        throw new QuotaExceededError("Quota blocked by kill switch.", { tenantId: input.tenantId, quotaType: input.quotaType });
      }

      const windowExpired = existing.resetAt < nowIso;
      const effectiveCount = windowExpired ? 0 : existing.count;

      if (effectiveCount >= existing.limit) {
        throw new QuotaExceededError("Quota exceeded.", {
          tenantId: input.tenantId,
          quotaType: input.quotaType,
          limit: existing.limit,
        });
      }

      const nextResetAt = windowExpired ? resetAt : existing.resetAt;
      const wrote = await this.store.updateConditional(
        { ...existing, count: effectiveCount + 1, resetAt: nextResetAt },
        { count: existing.count, resetAt: existing.resetAt },
      );
      if (wrote) return;
      // Another concurrent consume() won the write race; re-read and retry against fresh state.
    }

    throw new QuotaExceededError("Quota check could not complete under contention.", {
      tenantId: input.tenantId,
      quotaType: input.quotaType,
    });
  }

  /**
   * Releases 1 previously-consumed unit â€” M6 design Â§3.5 (UploadSlotReconciliationWorker
   * "libera quota idempotentemente" for a slot that expired unconfirmed). Idempotent: never
   * decrements below 0, and calling this twice for the same already-released slot floors at 0
   * rather than double-crediting. A window that has already reset naturally (count implicitly
   * 0) is a no-op, not an error - the quota already recovered on its own.
   */
  async release(input: Omit<QuotaCheckInput, "limit"> & { limit?: number }): Promise<void> {
    for (let attempt = 0; attempt < TenantQuotaService.MAX_CONTENTION_RETRIES; attempt++) {
      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
      const nowIso = this.now();
      const existing = await this.store.get<TenantQuotaRecord>(key);
      if (!existing) return; // nothing consumed yet for this window - nothing to release.

      const windowExpired = existing.resetAt < nowIso;
      if (windowExpired) return; // window already reset naturally - already recovered.

      const nextCount = Math.max(0, existing.count - 1);
      const wrote = await this.store.updateConditional({ ...existing, count: nextCount }, { count: existing.count, resetAt: existing.resetAt });
      if (wrote) return;
      // Lost a concurrent write race; re-read and retry against fresh state.
    }
    throw new QuotaExceededError("Quota release could not complete under contention.", { tenantId: input.tenantId, quotaType: input.quotaType });
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 6 'interface DeviceSession|entityType: \"DeviceSession\"|updateConditional|putIfAbsent' src/modules/identity/persistence/user-repository.ts src/modules/bff/application/bff-auth-service.ts src/modules/bff/persistence -g *.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 173ms:
src/modules/identity/persistence/user-repository.ts-29-  requesterDisplayName?: string;
src/modules/identity/persistence/user-repository.ts-30-  createdAt: string;
src/modules/identity/persistence/user-repository.ts-31-  updatedAt: string;
src/modules/identity/persistence/user-repository.ts-32-  version: number;
src/modules/identity/persistence/user-repository.ts-33-}
src/modules/identity/persistence/user-repository.ts-34-
src/modules/identity/persistence/user-repository.ts:35:export interface DeviceSession {
src/modules/identity/persistence/user-repository.ts-36-  PK: string;
src/modules/identity/persistence/user-repository.ts-37-  SK: string; // SESSION#<deviceId>
src/modules/identity/persistence/user-repository.ts-38-  entityType: "DeviceSession";
src/modules/identity/persistence/user-repository.ts-39-  tenantId: string;
src/modules/identity/persistence/user-repository.ts-40-  userId: string;
src/modules/identity/persistence/user-repository.ts-41-  deviceId: string;
--
src/modules/identity/persistence/user-repository.ts-79-      entityType: "User",
src/modules/identity/persistence/user-repository.ts-80-      ...profile,
src/modules/identity/persistence/user-repository.ts-81-      createdAt: now,
src/modules/identity/persistence/user-repository.ts-82-      updatedAt: now,
src/modules/identity/persistence/user-repository.ts-83-      version: 1,
src/modules/identity/persistence/user-repository.ts-84-    };
src/modules/identity/persistence/user-repository.ts:85:    await this.store.putIfAbsent(item);
src/modules/identity/persistence/user-repository.ts-86-    return item;
src/modules/identity/persistence/user-repository.ts-87-  }
src/modules/identity/persistence/user-repository.ts-88-
src/modules/identity/persistence/user-repository.ts-89-  async getDeviceSession(tenantId: string, userId: string, deviceId: string): Promise<DeviceSession | undefined> {
src/modules/identity/persistence/user-repository.ts-90-    return this.store.get<DeviceSession>(deviceSessionKey(tenantId, userId, deviceId));
src/modules/identity/persistence/user-repository.ts-91-  }
--
src/modules/identity/persistence/user-repository.ts-107-    if (!session) return;
src/modules/identity/persistence/user-repository.ts-108-    await this.store.update({ ...session, status: "REVOKED", deviceLogoutAfter: this.now() });
src/modules/identity/persistence/user-repository.ts-109-  }
src/modules/identity/persistence/user-repository.ts-110-
src/modules/identity/persistence/user-repository.ts-111-  /** W5-01/GTR-01: sets or clears (`undefined`) the name shown to guests as the requester's
src/modules/identity/persistence/user-repository.ts-112-   * identity. Same unconditional-overwrite pattern as `logoutAll`/`logoutDevice` above — this
src/modules/identity/persistence/user-repository.ts:113:   * port has no per-item OCC beyond `updateConditional`'s counter-only use (quota.ts), and a
src/modules/identity/persistence/user-repository.ts-114-   * lost update on this single self-editable text field carries the same negligible,
src/modules/identity/persistence/user-repository.ts-115-   * already-accepted risk as those two fields. Caller (`ProfileService`) is responsible for
src/modules/identity/persistence/user-repository.ts-116-   * confirming the profile exists first. */
src/modules/identity/persistence/user-repository.ts-117-  async setRequesterDisplayName(profile: UserProfile, requesterDisplayName: string | undefined): Promise<UserProfile> {
src/modules/identity/persistence/user-repository.ts-118-    const updated: UserProfile = { ...profile, requesterDisplayName, updatedAt: this.now() };
src/modules/identity/persistence/user-repository.ts-119-    await this.store.update(updated);
--
src/modules/bff/application/bff-auth-service.ts-78-      codeVerifier,
src/modules/bff/application/bff-auth-service.ts-79-      returnTo: safeReturnTo,
src/modules/bff/application/bff-auth-service.ts-80-      purgeAfterTtl: epochSeconds(now) + LOGIN_ATTEMPT_TTL_SECONDS,
src/modules/bff/application/bff-auth-service.ts-81-      createdAt: now,
src/modules/bff/application/bff-auth-service.ts-82-      version: 1,
src/modules/bff/application/bff-auth-service.ts-83-    };
src/modules/bff/application/bff-auth-service.ts:84:    const created = await this.deps.sessionStore.putIfAbsent(attempt);
src/modules/bff/application/bff-auth-service.ts-85-    if (!created) {
src/modules/bff/application/bff-auth-service.ts-86-      // selectorHash collision is astronomically unlikely (128 bits of entropy) - if it
src/modules/bff/application/bff-auth-service.ts-87-      // ever happens, fail closed rather than silently overwrite another attempt in flight.
src/modules/bff/application/bff-auth-service.ts-88-      throw new ConflictError("Could not start a new login attempt.");
src/modules/bff/application/bff-auth-service.ts-89-    }
src/modules/bff/application/bff-auth-service.ts-90-
--
src/modules/bff/application/bff-auth-service.ts-126-    }
src/modules/bff/application/bff-auth-service.ts-127-    // Single-use: consume immediately, before the Cognito round trip, so a duplicate/retried
src/modules/bff/application/bff-auth-service.ts-128-    // callback (double-click, browser back button) can never replay the same code twice
src/modules/bff/application/bff-auth-service.ts-129-    // against a still-valid LoginAttempt. Conditional on `version` (not a plain update) -
src/modules/bff/application/bff-auth-service.ts-130-    // two concurrent callbacks could otherwise both read consumedAt=undefined and both
src/modules/bff/application/bff-auth-service.ts-131-    // proceed before either write landed (found in review); only one wins this race.
src/modules/bff/application/bff-auth-service.ts:132:    const consumed = await this.deps.sessionStore.updateConditional<LoginAttempt>(
src/modules/bff/application/bff-auth-service.ts-133-      { ...attempt, consumedAt: this.deps.now(), version: attempt.version + 1 },
src/modules/bff/application/bff-auth-service.ts-134-      { version: attempt.version },
src/modules/bff/application/bff-auth-service.ts-135-    );
src/modules/bff/application/bff-auth-service.ts-136-    if (!consumed) {
src/modules/bff/application/bff-auth-service.ts-137-      throw new AuthenticationError("Login session not found or already used.");
src/modules/bff/application/bff-auth-service.ts-138-    }
--
src/modules/bff/application/bff-auth-service.ts-212-      purgeAfterTtl: epochSeconds(now) + SESSION_IDLE_TTL_SECONDS,
src/modules/bff/application/bff-auth-service.ts-213-      refreshState: "IDLE",
src/modules/bff/application/bff-auth-service.ts-214-      createdAt: now,
src/modules/bff/application/bff-auth-service.ts-215-      updatedAt: now,
src/modules/bff/application/bff-auth-service.ts-216-      version: 1,
src/modules/bff/application/bff-auth-service.ts-217-    };
src/modules/bff/application/bff-auth-service.ts:218:    const created = await this.deps.sessionStore.putIfAbsent(session);
src/modules/bff/application/bff-auth-service.ts-219-    if (!created) {
src/modules/bff/application/bff-auth-service.ts-220-      throw new ConflictError("Could not create session.");
src/modules/bff/application/bff-auth-service.ts-221-    }
src/modules/bff/application/bff-auth-service.ts-222-
src/modules/bff/application/bff-auth-service.ts-223-    void accessClaims; // decoded for future use (e.g. audit logging tokenId); not persisted raw
src/modules/bff/application/bff-auth-service.ts-224-    return { sessionToken: issuedSession.token, csrfToken: csrfSecret, returnTo: attempt.returnTo };
--
src/modules/bff/application/bff-auth-service.ts-300-    // `version` (not a plain update) - the same residual variant of the Item 11 bug found in
src/modules/bff/application/bff-auth-service.ts-301-    // review: an unconditional write here using this function's own (possibly slightly stale)
src/modules/bff/application/bff-auth-service.ts-302-    // `session` snapshot could silently overwrite a `revokedAt` a concurrent logout had just
src/modules/bff/application/bff-auth-service.ts-303-    // written, resurrecting a session the user explicitly logged out of.
src/modules/bff/application/bff-auth-service.ts-304-    const nextPurge = Math.min(epochSeconds(now) + SESSION_IDLE_TTL_SECONDS, epochSeconds(session.absoluteExpiresAt));
src/modules/bff/application/bff-auth-service.ts-305-    if (nextPurge !== session.purgeAfterTtl) {
src/modules/bff/application/bff-auth-service.ts:306:      const bumped = await this.deps.sessionStore.updateConditional<Session>(
src/modules/bff/application/bff-auth-service.ts-307-        { ...session, purgeAfterTtl: nextPurge, updatedAt: now, version: session.version + 1 },
src/modules/bff/application/bff-auth-service.ts-308-        { version: session.version },
src/modules/bff/application/bff-auth-service.ts-309-      );
src/modules/bff/application/bff-auth-service.ts-310-      if (!bumped) {
src/modules/bff/application/bff-auth-service.ts-311-        // Something else modified this session concurrently - re-read rather than returning
src/modules/bff/application/bff-auth-service.ts-312-        // a snapshot that might now be stale or revoked. If someone else's write superseded
--
src/modules/bff/application/bff-auth-service.ts-339-    if (session.refreshState === "IN_PROGRESS" && session.refreshLeaseUntil && now < session.refreshLeaseUntil) {
src/modules/bff/application/bff-auth-service.ts-340-      return { kind: "CONCURRENT_REFRESH" };
src/modules/bff/application/bff-auth-service.ts-341-    }
src/modules/bff/application/bff-auth-service.ts-342-
src/modules/bff/application/bff-auth-service.ts-343-    const leaseId = randomUUID();
src/modules/bff/application/bff-auth-service.ts-344-    const leaseUntil = new Date(Date.parse(now) + 5000).toISOString();
src/modules/bff/application/bff-auth-service.ts:345:    const acquired = await this.deps.sessionStore.updateConditional<Session>(
src/modules/bff/application/bff-auth-service.ts-346-      { ...session, refreshState: "IN_PROGRESS", refreshLeaseId: leaseId, refreshLeaseUntil: leaseUntil, updatedAt: now, version: session.version + 1 },
src/modules/bff/application/bff-auth-service.ts-347-      { version: session.version },
src/modules/bff/application/bff-auth-service.ts-348-    );
src/modules/bff/application/bff-auth-service.ts-349-    if (!acquired) {
src/modules/bff/application/bff-auth-service.ts-350-      return { kind: "CONCURRENT_REFRESH" };
src/modules/bff/application/bff-auth-service.ts-351-    }
--
src/modules/bff/application/bff-auth-service.ts-383-    const refreshedAt = this.deps.now();
src/modules/bff/application/bff-auth-service.ts-384-    // Conditional on the lease's own version (session.version + 1, set when the lease was
src/modules/bff/application/bff-auth-service.ts-385-    // acquired above) - a plain unconditional overwrite here (the bug found in review) could
src/modules/bff/application/bff-auth-service.ts-386-    // silently resurrect a session that was revoked (e.g. a concurrent logout) between lease
src/modules/bff/application/bff-auth-service.ts-387-    // acquisition and this commit, since it would blindly spread the pre-lease `session`
src/modules/bff/application/bff-auth-service.ts-388-    // snapshot back over whatever the revocation had written.
src/modules/bff/application/bff-auth-service.ts:389:    const committed = await this.deps.sessionStore.updateConditional<Session>(
src/modules/bff/application/bff-auth-service.ts-390-      {
src/modules/bff/application/bff-auth-service.ts-391-        ...session,
src/modules/bff/application/bff-auth-service.ts-392-        accessToken: response.accessToken,
src/modules/bff/application/bff-auth-service.ts-393-        accessTokenExpiresAt: new Date(Date.parse(refreshedAt) + response.expiresInSeconds * 1000).toISOString(),
src/modules/bff/application/bff-auth-service.ts-394-        encryptedRefreshToken: await this.deps.tokenEncryptor.encrypt(response.refreshToken),
src/modules/bff/application/bff-auth-service.ts-395-        refreshState: "IDLE",
--
src/modules/bff/application/bff-auth-service.ts-430-   * be missing here too on the very next pass). */
src/modules/bff/application/bff-auth-service.ts-431-  private isPastIdleTimeout(session: Session): boolean {
src/modules/bff/application/bff-auth-service.ts-432-    return epochSeconds(this.deps.now()) >= session.purgeAfterTtl;
src/modules/bff/application/bff-auth-service.ts-433-  }
src/modules/bff/application/bff-auth-service.ts-434-
src/modules/bff/application/bff-auth-service.ts-435-  private async releaseLease(session: Session, leaseId: string, now: string): Promise<void> {
src/modules/bff/application/bff-auth-service.ts:436:    await this.deps.sessionStore.updateConditional<Session>(
src/modules/bff/application/bff-auth-service.ts-437-      { ...session, refreshState: "IDLE", refreshLeaseId: undefined, refreshLeaseUntil: undefined, updatedAt: now, version: session.version + 2 },
src/modules/bff/application/bff-auth-service.ts-438-      { version: session.version + 1 },
src/modules/bff/application/bff-auth-service.ts-439-    );
src/modules/bff/application/bff-auth-service.ts-440-    void leaseId;
src/modules/bff/application/bff-auth-service.ts-441-  }
src/modules/bff/application/bff-auth-service.ts-442-
--
src/modules/bff/persistence\dynamodb-session-store.ts-20-      return result.Item as T | undefined;
src/modules/bff/persistence\dynamodb-session-store.ts-21-    } catch (err) {
src/modules/bff/persistence\dynamodb-session-store.ts-22-      throw mapDynamoError(err, "SessionStore.get");
src/modules/bff/persistence\dynamodb-session-store.ts-23-    }
src/modules/bff/persistence\dynamodb-session-store.ts-24-  }
src/modules/bff/persistence\dynamodb-session-store.ts-25-
src/modules/bff/persistence\dynamodb-session-store.ts:26:  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
src/modules/bff/persistence\dynamodb-session-store.ts-27-    try {
src/modules/bff/persistence\dynamodb-session-store.ts-28-      await this.client.send(
src/modules/bff/persistence\dynamodb-session-store.ts-29-        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src/modules/bff/persistence\dynamodb-session-store.ts-30-      );
src/modules/bff/persistence\dynamodb-session-store.ts-31-      return true;
src/modules/bff/persistence\dynamodb-session-store.ts-32-    } catch (err) {
src/modules/bff/persistence\dynamodb-session-store.ts-33-      if (isConditionalCheckFailed(err)) return false;
src/modules/bff/persistence\dynamodb-session-store.ts:34:      throw mapDynamoError(err, "SessionStore.putIfAbsent");
src/modules/bff/persistence\dynamodb-session-store.ts-35-    }
src/modules/bff/persistence\dynamodb-session-store.ts-36-  }
src/modules/bff/persistence\dynamodb-session-store.ts-37-
src/modules/bff/persistence\dynamodb-session-store.ts-38-  async update<T extends EntityKey>(item: T): Promise<void> {
src/modules/bff/persistence\dynamodb-session-store.ts-39-    try {
src/modules/bff/persistence\dynamodb-session-store.ts-40-      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src/modules/bff/persistence\dynamodb-session-store.ts-41-    } catch (err) {
src/modules/bff/persistence\dynamodb-session-store.ts-42-      throw mapDynamoError(err, "SessionStore.update");
src/modules/bff/persistence\dynamodb-session-store.ts-43-    }
src/modules/bff/persistence\dynamodb-session-store.ts-44-  }
src/modules/bff/persistence\dynamodb-session-store.ts-45-
src/modules/bff/persistence\dynamodb-session-store.ts:46:  async updateConditional<T extends EntityKey>(item: T, expected: { version: number }): Promise<boolean> {
src/modules/bff/persistence\dynamodb-session-store.ts-47-    try {
src/modules/bff/persistence\dynamodb-session-store.ts-48-      await this.client.send(
src/modules/bff/persistence\dynamodb-session-store.ts-49-        new PutCommand({
src/modules/bff/persistence\dynamodb-session-store.ts-50-          TableName: this.tableName,
src/modules/bff/persistence\dynamodb-session-store.ts-51-          Item: item,
src/modules/bff/persistence\dynamodb-session-store.ts-52-          ConditionExpression: "attribute_exists(PK) AND #version = :expectedVersion",
--
src/modules/bff/persistence\dynamodb-session-store.ts-54-          ExpressionAttributeValues: { ":expectedVersion": expected.version },
src/modules/bff/persistence\dynamodb-session-store.ts-55-        }),
src/modules/bff/persistence\dynamodb-session-store.ts-56-      );
src/modules/bff/persistence\dynamodb-session-store.ts-57-      return true;
src/modules/bff/persistence\dynamodb-session-store.ts-58-    } catch (err) {
src/modules/bff/persistence\dynamodb-session-store.ts-59-      if (isConditionalCheckFailed(err)) return false;
src/modules/bff/persistence\dynamodb-session-store.ts:60:      throw mapDynamoError(err, "SessionStore.updateConditional");
src/modules/bff/persistence\dynamodb-session-store.ts-61-    }
src/modules/bff/persistence\dynamodb-session-store.ts-62-  }
src/modules/bff/persistence\dynamodb-session-store.ts-63-}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/identity/persistence/user-repository.ts | Select-Object -Skip 34 -First 80; Get-Content src/modules/bff/application/bff-auth-service.ts | Select-Object -Skip 165 -First 55' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 224ms:
export interface DeviceSession {
  PK: string;
  SK: string; // SESSION#<deviceId>
  entityType: "DeviceSession";
  tenantId: string;
  userId: string;
  deviceId: string;
  sessionId: string;
  refreshFamilyId: string;
  deviceLogoutAfter?: string;
  createdAt: string;
  lastSeenAt: string;
  expiresAt: string;
  status: "ACTIVE" | "REVOKED";
}

export function userProfileKey(tenantId: string, userId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: "PROFILE" };
}

export function deviceSessionKey(tenantId: string, userId: string, deviceId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: `SESSION#${deviceId}` };
}

export class UserRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async getProfile(tenantId: string, userId: string): Promise<UserProfile | undefined> {
    return this.store.get<UserProfile>(userProfileKey(tenantId, userId));
  }

  async createProfileIfAbsent(profile: Omit<UserProfile, "PK" | "SK" | "entityType" | "createdAt" | "updatedAt" | "version">): Promise<UserProfile> {
    const key = userProfileKey(profile.tenantId, profile.userId);
    const existing = await this.getProfile(profile.tenantId, profile.userId);
    if (existing) {
      return existing;
    }
    const now = this.now();
    const item: UserProfile = {
      ...key,
      SK: "PROFILE",
      entityType: "User",
      ...profile,
      createdAt: now,
      updatedAt: now,
      version: 1,
    };
    await this.store.putIfAbsent(item);
    return item;
  }

  async getDeviceSession(tenantId: string, userId: string, deviceId: string): Promise<DeviceSession | undefined> {
    return this.store.get<DeviceSession>(deviceSessionKey(tenantId, userId, deviceId));
  }

  async upsertDeviceSession(session: DeviceSession): Promise<void> {
    await this.store.update(session);
  }

  /** Logout global â€” implementation-blueprint.md Â§4.2: revokes every token issued before now. */
  async logoutAll(tenantId: string, userId: string): Promise<void> {
    const profile = await this.getProfile(tenantId, userId);
    if (!profile) return;
    await this.store.update({ ...profile, globalLogoutAfter: this.now(), updatedAt: this.now() });
  }

  /** Logout by device â€” revokes only this device's refresh family. */
  async logoutDevice(tenantId: string, userId: string, deviceId: string): Promise<void> {
    const session = await this.getDeviceSession(tenantId, userId, deviceId);
    if (!session) return;
    await this.store.update({ ...session, status: "REVOKED", deviceLogoutAfter: this.now() });
  }

  /** W5-01/GTR-01: sets or clears (`undefined`) the name shown to guests as the requester's
   * identity. Same unconditional-overwrite pattern as `logoutAll`/`logoutDevice` above â€” this
   * port has no per-item OCC beyond `updateConditional`'s counter-only use (quota.ts), and a
   * lost update on this single self-editable text field carries the same negligible,
        tenantId: mapping.tenantId,
        identitySubject: idClaims.subject,
        emailNormalized: (idClaims.email ?? "").toLowerCase(),
        roles: ["OWNER"],
        status: "ACTIVE",
      });
    }
    if (profile.status !== "ACTIVE") {
      throw new AuthenticationError("User is not active.");
    }

    const deviceId = this.deps.newDeviceId();
    await this.deps.users.upsertDeviceSession({
      PK: `TENANT#${mapping.tenantId}#USER#${mapping.userId}`,
      SK: `SESSION#${deviceId}`,
      entityType: "DeviceSession",
      tenantId: mapping.tenantId,
      userId: mapping.userId,
      deviceId,
      sessionId: randomUUID(),
      refreshFamilyId: randomUUID(),
      createdAt: this.deps.now(),
      lastSeenAt: this.deps.now(),
      expiresAt: new Date(Date.parse(this.deps.now()) + SESSION_ABSOLUTE_TTL_SECONDS * 1000).toISOString(),
      status: "ACTIVE",
    });

    const issuedSession = issueOpaqueToken(this.deps.pepper);
    const csrfSecret = issueCsrfSecret();
    const now = this.deps.now();
    const absoluteExpiresAt = new Date(Date.parse(now) + SESSION_ABSOLUTE_TTL_SECONDS * 1000).toISOString();
    const session: Session = {
      ...sessionKey(issuedSession.selectorHash),
      SK: "POINTER",
      entityType: "Session",
      selectorHash: issuedSession.selectorHash,
      secretHash: issuedSession.secretHash,
      tenantId: mapping.tenantId,
      userId: mapping.userId,
      cognitoSubject: idClaims.subject,
      deviceId,
      csrfSecret,
      encryptedRefreshToken: await this.deps.tokenEncryptor.encrypt(tokens.refreshToken),
      accessToken: tokens.accessToken,
      accessTokenExpiresAt: new Date(Date.parse(now) + tokens.expiresInSeconds * 1000).toISOString(),
      absoluteExpiresAt,
      purgeAfterTtl: epochSeconds(now) + SESSION_IDLE_TTL_SECONDS,
      refreshState: "IDLE",
      createdAt: now,
      updatedAt: now,
      version: 1,
    };
    const created = await this.deps.sessionStore.putIfAbsent(session);
    if (!created) {
      throw new ConflictError("Could not create session.");

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 8 'updateConditional' src/modules/identity/persistence src/modules/identity/ports -g *.ts; rg -n -C 5 'TenantEntitlement|activeTrackedSubjectsCount' src/modules/subject -g *.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 191ms:
src/modules/identity/ports\identity-store.ts-9-  PK: string;
src/modules/identity/ports\identity-store.ts-10-  SK: string;
src/modules/identity/ports\identity-store.ts-11-}
src/modules/identity/ports\identity-store.ts-12-
src/modules/identity/ports\identity-store.ts-13-export interface IdentityStore {
src/modules/identity/ports\identity-store.ts-14-  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
src/modules/identity/ports\identity-store.ts-15-  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
src/modules/identity/ports\identity-store.ts-16-  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
src/modules/identity/ports\identity-store.ts:17:  /** Unconditional overwrite - only for bookkeeping writes with no concurrent-writer risk (see quota.ts for the counter case, which needs updateConditional instead). */
src/modules/identity/ports\identity-store.ts-18-  update<T extends EntityKey>(item: T): Promise<void>;
src/modules/identity/ports\identity-store.ts-19-  /**
src/modules/identity/ports\identity-store.ts-20-   * PutItem gated on the counter field(s) still matching `expected` at write time (full audit
src/modules/identity/ports\identity-store.ts-21-   * round1, eixo Governança de Produto, criterio 3 - TenantQuota.consume() did a
src/modules/identity/ports\identity-store.ts-22-   * read-modify-write over the unconditional `update`, allowing lost updates under concurrent
src/modules/identity/ports\identity-store.ts-23-   * requests for the same tenant/quotaType). Returns false on conditional-check failure so the
src/modules/identity/ports\identity-store.ts-24-   * caller can re-read and retry instead of silently overwriting a concurrent writer's count.
src/modules/identity/ports\identity-store.ts-25-   */
src/modules/identity/ports\identity-store.ts:26:  updateConditional<T extends EntityKey>(item: T, expected: { count: number; resetAt: string }): Promise<boolean>;
src/modules/identity/ports\identity-store.ts-27-}
--
src/modules/identity/persistence\dynamodb-identity-store.ts-47-  async update<T extends EntityKey>(item: T): Promise<void> {
src/modules/identity/persistence\dynamodb-identity-store.ts-48-    try {
src/modules/identity/persistence\dynamodb-identity-store.ts-49-      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src/modules/identity/persistence\dynamodb-identity-store.ts-50-    } catch (err) {
src/modules/identity/persistence\dynamodb-identity-store.ts-51-      throw mapDynamoError(err, "IdentityStore.update");
src/modules/identity/persistence\dynamodb-identity-store.ts-52-    }
src/modules/identity/persistence\dynamodb-identity-store.ts-53-  }
src/modules/identity/persistence\dynamodb-identity-store.ts-54-
src/modules/identity/persistence\dynamodb-identity-store.ts:55:  async updateConditional<T extends EntityKey>(
src/modules/identity/persistence\dynamodb-identity-store.ts-56-    item: T,
src/modules/identity/persistence\dynamodb-identity-store.ts-57-    expected: { count: number; resetAt: string },
src/modules/identity/persistence\dynamodb-identity-store.ts-58-  ): Promise<boolean> {
src/modules/identity/persistence\dynamodb-identity-store.ts-59-    try {
src/modules/identity/persistence\dynamodb-identity-store.ts-60-      await this.client.send(
src/modules/identity/persistence\dynamodb-identity-store.ts-61-        new PutCommand({
src/modules/identity/persistence\dynamodb-identity-store.ts-62-          TableName: this.tableName,
src/modules/identity/persistence\dynamodb-identity-store.ts-63-          Item: item,
--
src/modules/identity/persistence\dynamodb-identity-store.ts-76-            ":expectedCount": expected.count,
src/modules/identity/persistence\dynamodb-identity-store.ts-77-            ":expectedResetAt": expected.resetAt,
src/modules/identity/persistence\dynamodb-identity-store.ts-78-          },
src/modules/identity/persistence\dynamodb-identity-store.ts-79-        }),
src/modules/identity/persistence\dynamodb-identity-store.ts-80-      );
src/modules/identity/persistence\dynamodb-identity-store.ts-81-      return true;
src/modules/identity/persistence\dynamodb-identity-store.ts-82-    } catch (err) {
src/modules/identity/persistence\dynamodb-identity-store.ts-83-      if (isConditionalCheckFailed(err)) return false;
src/modules/identity/persistence\dynamodb-identity-store.ts:84:      throw mapDynamoError(err, "IdentityStore.updateConditional");
src/modules/identity/persistence\dynamodb-identity-store.ts-85-    }
src/modules/identity/persistence\dynamodb-identity-store.ts-86-  }
src/modules/identity/persistence\dynamodb-identity-store.ts-87-}
--
src/modules/identity/persistence\user-repository.ts-105-  async logoutDevice(tenantId: string, userId: string, deviceId: string): Promise<void> {
src/modules/identity/persistence\user-repository.ts-106-    const session = await this.getDeviceSession(tenantId, userId, deviceId);
src/modules/identity/persistence\user-repository.ts-107-    if (!session) return;
src/modules/identity/persistence\user-repository.ts-108-    await this.store.update({ ...session, status: "REVOKED", deviceLogoutAfter: this.now() });
src/modules/identity/persistence\user-repository.ts-109-  }
src/modules/identity/persistence\user-repository.ts-110-
src/modules/identity/persistence\user-repository.ts-111-  /** W5-01/GTR-01: sets or clears (`undefined`) the name shown to guests as the requester's
src/modules/identity/persistence\user-repository.ts-112-   * identity. Same unconditional-overwrite pattern as `logoutAll`/`logoutDevice` above — this
src/modules/identity/persistence\user-repository.ts:113:   * port has no per-item OCC beyond `updateConditional`'s counter-only use (quota.ts), and a
src/modules/identity/persistence\user-repository.ts-114-   * lost update on this single self-editable text field carries the same negligible,
src/modules/identity/persistence\user-repository.ts-115-   * already-accepted risk as those two fields. Caller (`ProfileService`) is responsible for
src/modules/identity/persistence\user-repository.ts-116-   * confirming the profile exists first. */
src/modules/identity/persistence\user-repository.ts-117-  async setRequesterDisplayName(profile: UserProfile, requesterDisplayName: string | undefined): Promise<UserProfile> {
src/modules/identity/persistence\user-repository.ts-118-    const updated: UserProfile = { ...profile, requesterDisplayName, updatedAt: this.now() };
src/modules/identity/persistence\user-repository.ts-119-    await this.store.update(updated);
src/modules/identity/persistence\user-repository.ts-120-    return updated;
src/modules/identity/persistence\user-repository.ts-121-  }
src/modules/subject\application\document-request-service.ts-104-      await this.writeInitialInviteAudit(ctx, assignmentId, "INITIAL_INVITE_EMAIL_DISABLED_BY_KILL_SWITCH", {});
src/modules/subject\application\document-request-service.ts-105-    }
src/modules/subject\application\document-request-service.ts-106-
src/modules/subject\application\document-request-service.ts-107-    // Rate limit verificado ANTES da criação, quando o envio foi de fato solicitado -
src/modules/subject\application\document-request-service.ts-108-    // bloqueia com 429, nunca cria parcialmente (mesma disciplina fail-closed de
src/modules/subject\application\document-request-service.ts:109:    // TenantEntitlement/D-038) - diferente de falha de SES pós-criação, que é best-effort.
src/modules/subject\application\document-request-service.ts-110-    if (willAttemptEmail) {
src/modules/subject\application\document-request-service.ts-111-      try {
src/modules/subject\application\document-request-service.ts-112-        await this.initialInviteRateLimiter.consumeInitialInvite(assignment.tenantId, input.recipientEmail);
src/modules/subject\application\document-request-service.ts-113-      } catch (err) {
src/modules/subject\application\document-request-service.ts-114-        if (err instanceof QuotaExceededError) {
--
src/modules/subject\application\initial-invite-rate-limiter.ts-1-/**
src/modules/subject\application\initial-invite-rate-limiter.ts-2- * Rate limit do convite inicial automatizado (M10 cluster 4, D-049) — 3 limites concretos
src/modules/subject\application\initial-invite-rate-limiter.ts-3- * (não "a decidir depois"), verificados ANTES da criação do `DocumentRequest` quando o envio
src/modules/subject\application\initial-invite-rate-limiter.ts-4- * é solicitado: excedê-los bloqueia a criação com `QuotaExceededError`/429, nunca cria
src/modules/subject\application\initial-invite-rate-limiter.ts:5: * parcialmente (mesma disciplina fail-closed de `TenantEntitlement`, D-038) — diferente de
src/modules/subject\application\initial-invite-rate-limiter.ts-6- * falha de SES pós-criação, que é best-effort (`DocumentRequestService`).
src/modules/subject\application\initial-invite-rate-limiter.ts-7- *
src/modules/subject\application\initial-invite-rate-limiter.ts-8- * Mesmo mecanismo de janela fixa de `GuestRateLimiter` (não `TenantQuotaService` do módulo
src/modules/subject\application\initial-invite-rate-limiter.ts-9- * identity — reusar isso exigiria adicionar tipos de quota específicos de `subject` a um
src/modules/subject\application\initial-invite-rate-limiter.ts-10- * union type fechado de outro módulo, o acoplamento reverso que `shared-must-not-reach-modules`
--
src/modules/subject\application\subject-service.ts-17-  type TrackedSubject,
src/modules/subject\application\subject-service.ts-18-  type TrackedSubjectStatus,
src/modules/subject\application\subject-service.ts-19-  type CreateSubjectInput,
src/modules/subject\application\subject-service.ts-20-  type UpdateSubjectInput,
src/modules/subject\application\subject-service.ts-21-} from "../domain/tracked-subject.js";
src/modules/subject\application\subject-service.ts:22:import { entitlementKey, defaultEntitlement, type TenantEntitlement } from "../domain/entitlement.js";
src/modules/subject\application\subject-service.ts-23-import { buildSubjectAuditEvent, appendSubjectAuditToTransaction, type SubjectAuditAction } from "../domain/audit-event.js";
src/modules/subject\application\subject-service.ts-24-import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src/modules/subject\application\subject-service.ts-25-import type { SubjectIdGenerator } from "./id-generator.js";
src/modules/subject\application\subject-service.ts-26-
src/modules/subject\application\subject-service.ts-27-export interface SubjectServiceDeps {
--
src/modules/subject\application\subject-service.ts-58-  async createSubject(ctx: RequestContext, input: CreateSubjectInput): Promise<TrackedSubject> {
src/modules/subject\application\subject-service.ts-59-    authorize({ context: ctx, action: "subject:create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/subject\application\subject-service.ts-60-
src/modules/subject\application\subject-service.ts-61-    for (let attempt = 0; attempt < MAX_CONTENTION_RETRIES; attempt++) {
src/modules/subject\application\subject-service.ts-62-      const entitlement = await this.ensureEntitlement(ctx.tenant.tenantId);
src/modules/subject\application\subject-service.ts:63:      if (entitlement.activeTrackedSubjectsCount >= entitlement.activeTrackedSubjectsLimit) {
src/modules/subject\application\subject-service.ts-64-        throw new QuotaExceededError("Active tracked subject limit reached for this plan.", {
src/modules/subject\application\subject-service.ts-65-          tenantId: ctx.tenant.tenantId,
src/modules/subject\application\subject-service.ts-66-          planId: entitlement.planId,
src/modules/subject\application\subject-service.ts-67-          limit: entitlement.activeTrackedSubjectsLimit,
src/modules/subject\application\subject-service.ts-68-        });
--
src/modules/subject\application\subject-service.ts-93-          Update: buildVersionedUpdate({
src/modules/subject\application\subject-service.ts-94-            tableName: this.tableName,
src/modules/subject\application\subject-service.ts-95-            key: entitlementKey(ctx.tenant.tenantId),
src/modules/subject\application\subject-service.ts-96-            tenantId: ctx.tenant.tenantId,
src/modules/subject\application\subject-service.ts-97-            expectedVersion: entitlement.version,
src/modules/subject\application\subject-service.ts:98:            set: { activeTrackedSubjectsCount: entitlement.activeTrackedSubjectsCount + 1 },
src/modules/subject\application\subject-service.ts-99-          }),
src/modules/subject\application\subject-service.ts-100-        },
src/modules/subject\application\subject-service.ts-101-        { Put: buildVersionedCreate(this.tableName, subject as unknown as Record<string, unknown> & { PK: string; SK: string }) },
src/modules/subject\application\subject-service.ts-102-      ];
src/modules/subject\application\subject-service.ts-103-      this.appendAudit(entries, ctx, {
--
src/modules/subject\application\subject-service.ts-237-        Update: buildVersionedUpdate({
src/modules/subject\application\subject-service.ts-238-          tableName: this.tableName,
src/modules/subject\application\subject-service.ts-239-          key: entitlementKey(subject.tenantId),
src/modules/subject\application\subject-service.ts-240-          tenantId: subject.tenantId,
src/modules/subject\application\subject-service.ts-241-          expectedVersion: entitlement.version,
src/modules/subject\application\subject-service.ts:242:          set: { activeTrackedSubjectsCount: Math.max(0, entitlement.activeTrackedSubjectsCount - 1) },
src/modules/subject\application\subject-service.ts-243-        }),
src/modules/subject\application\subject-service.ts-244-      });
src/modules/subject\application\subject-service.ts-245-    }
src/modules/subject\application\subject-service.ts-246-
src/modules/subject\application\subject-service.ts-247-    this.appendAudit(entries, ctx, {
--
src/modules/subject\application\subject-service.ts-285-    });
src/modules/subject\application\subject-service.ts-286-    appendSubjectAuditToTransaction(entries, this.tableName, event);
src/modules/subject\application\subject-service.ts-287-  }
src/modules/subject\application\subject-service.ts-288-
src/modules/subject\application\subject-service.ts-289-  /** Get-or-create do plano default (mesmo padrão de auto-provisionamento de TenantQuotaService). */
src/modules/subject\application\subject-service.ts:290:  private async ensureEntitlement(tenantId: string): Promise<TenantEntitlement> {
src/modules/subject\application\subject-service.ts-291-    const key = entitlementKey(tenantId);
src/modules/subject\application\subject-service.ts:292:    const existing = await this.store.get<TenantEntitlement>(key);
src/modules/subject\application\subject-service.ts-293-    if (existing) return existing;
src/modules/subject\application\subject-service.ts-294-    const created = defaultEntitlement(tenantId, this.now());
src/modules/subject\application\subject-service.ts-295-    const wrote = await this.store.putIfAbsent(created);
src/modules/subject\application\subject-service.ts-296-    if (wrote) return created;
src/modules/subject\application\subject-service.ts-297-    // Perdeu a corrida de criação - outra chamada concorrente já criou; relê o estado real.
src/modules/subject\application\subject-service.ts:298:    const fresh = await this.store.get<TenantEntitlement>(key);
src/modules/subject\application\subject-service.ts-299-    if (!fresh) throw new ConflictError("Entitlement record vanished after creation race.", { tenantId });
src/modules/subject\application\subject-service.ts-300-    return fresh;
src/modules/subject\application\subject-service.ts-301-  }
src/modules/subject\application\subject-service.ts-302-
src/modules/subject\application\subject-service.ts-303-  private async readActiveSubject(tenantId: string, subjectId: string): Promise<TrackedSubject> {
--
src/modules/subject\domain\entitlement.ts-1-/**
src/modules/subject\domain\entitlement.ts:2: * TenantEntitlement — 05-domain-model-organization-billing.md (D-038): Entitlement/UsageQuota
src/modules/subject\domain\entitlement.ts-3- * mínimo local, plano default/free, limite de ACTIVE_TRACKED_SUBJECTS — sem depender de
src/modules/subject\domain\entitlement.ts-4- * provider de billing externo (esse entra só em M12). Billing por TrackedSubject, não por
src/modules/subject\domain\entitlement.ts-5- * assento (02-market-research.md: nenhum concorrente pesquisado cobra por usuário).
src/modules/subject\domain\entitlement.ts-6- *
src/modules/subject\domain\entitlement.ts-7- * Contador atômico incrementado/decrementado na MESMA transação que cria/remove um
--
src/modules/subject\domain\entitlement.ts-16-/** Referência de mercado real (02-market-research.md): bcs oferece 25 vendors grátis, sem
src/modules/subject\domain\entitlement.ts-17- * cartão. Mesma ordem de grandeza adotada aqui como default do plano free — ajustável quando
src/modules/subject\domain\entitlement.ts-18- * billing real (M12) introduzir planos pagos. */
src/modules/subject\domain\entitlement.ts-19-export const DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT = 25;
src/modules/subject\domain\entitlement.ts-20-
src/modules/subject\domain\entitlement.ts:21:export interface TenantEntitlement extends EntityKey {
src/modules/subject\domain\entitlement.ts-22-  SK: "PLAN";
src/modules/subject\domain\entitlement.ts:23:  entityType: "TenantEntitlement";
src/modules/subject\domain\entitlement.ts-24-  tenantId: string;
src/modules/subject\domain\entitlement.ts-25-  planId: string;
src/modules/subject\domain\entitlement.ts-26-  activeTrackedSubjectsLimit: number;
src/modules/subject\domain\entitlement.ts:27:  activeTrackedSubjectsCount: number;
src/modules/subject\domain\entitlement.ts-28-  createdAt: string;
src/modules/subject\domain\entitlement.ts-29-  updatedAt: string;
src/modules/subject\domain\entitlement.ts-30-  version: number;
src/modules/subject\domain\entitlement.ts-31-}
src/modules/subject\domain\entitlement.ts-32-
src/modules/subject\domain\entitlement.ts-33-export function entitlementKey(tenantId: string): { PK: string; SK: "PLAN" } {
src/modules/subject\domain\entitlement.ts-34-  return { PK: `TENANT#${tenantId}#ENTITLEMENT`, SK: "PLAN" };
src/modules/subject\domain\entitlement.ts-35-}
src/modules/subject\domain\entitlement.ts-36-
src/modules/subject\domain\entitlement.ts:37:export function defaultEntitlement(tenantId: string, now: string): TenantEntitlement {
src/modules/subject\domain\entitlement.ts-38-  return {
src/modules/subject\domain\entitlement.ts-39-    ...entitlementKey(tenantId),
src/modules/subject\domain\entitlement.ts:40:    entityType: "TenantEntitlement",
src/modules/subject\domain\entitlement.ts-41-    tenantId,
src/modules/subject\domain\entitlement.ts-42-    planId: DEFAULT_PLAN_ID,
src/modules/subject\domain\entitlement.ts-43-    activeTrackedSubjectsLimit: DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT,
src/modules/subject\domain\entitlement.ts:44:    activeTrackedSubjectsCount: 0,
src/modules/subject\domain\entitlement.ts-45-    createdAt: now,
src/modules/subject\domain\entitlement.ts-46-    updatedAt: now,
src/modules/subject\domain\entitlement.ts-47-    version: 1,
src/modules/subject\domain\entitlement.ts-48-  };
src/modules/subject\domain\entitlement.ts-49-}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/identity/persistence/dynamodb-identity-store.ts | Select-Object -Skip 55 -First 30' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 221ms:
    item: T,
    expected: { count: number; resetAt: string },
  ): Promise<boolean> {
    try {
      await this.client.send(
        new PutCommand({
          TableName: this.tableName,
          Item: item,
          // Real production bug found via a live smoke test against
          // exptrk-dev-notifications-handler (2026-08-21): `count` is a DynamoDB reserved
          // word (https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
          // - using it as a bare attribute name in a ConditionExpression, instead of through
          // an ExpressionAttributeNames placeholder, makes every real DynamoDB call fail with
          // ValidationException. Never caught by any test because the in-memory
          // IdentityStore fake (test/unit/identity/in-memory-store.ts) doesn't parse
          // condition expressions the way real DynamoDB does - this path was never actually
          // exercised against real DynamoDB before this session's real deploy.
          ConditionExpression: "#count = :expectedCount AND resetAt = :expectedResetAt",
          ExpressionAttributeNames: { "#count": "count" },
          ExpressionAttributeValues: {
            ":expectedCount": expected.count,
            ":expectedResetAt": expected.resetAt,
          },
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "IdentityStore.updateConditional");
    }

codex
A amostra já refutou a premissa central da Rodada 3: o código contém `TenantEntitlement`, omitido dos “39”; `LoginAttempt` não possui `tenantId`; e o mesmo `entityType: DeviceSession` tem dois shapes distintos, sendo o da tabela principal mutável e sem `version`. Vou fechar agora a análise de corridas (incluindo possível ressurreição por `PutItem`) e da prova de convergência.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 5 'interface Idempotency|entityType: \"IdempotencyRecord\"|interface Outbox|entityType: \"OutboxEvent\"|interface Webhook|entityType: \"WebhookInbox\"' src -g *.ts; rg -n -C 5 'pickObjectToDelete|cleanObject|legalHold' src/workers/document-purge/purge.ts src/modules/document/application/document-deletion-service.ts src/modules/document/domain/document.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 223ms:
src\shared\outbox\relay-store.ts-4- * ExpirationStore/ReminderStore - this is generic outbox bookkeeping, not domain state.
src\shared\outbox\relay-store.ts-5- */
src\shared\outbox\relay-store.ts-6-import type { EntityKey } from "../dynamodb/occ.js";
src\shared\outbox\relay-store.ts-7-import type { OutboxDestination, OutboxRecord } from "./outbox.js";
src\shared\outbox\relay-store.ts-8-
src\shared\outbox\relay-store.ts:9:export interface OutboxRelayStore {
src\shared\outbox\relay-store.ts-10-  /** Conditional lease acquisition - `UpdateItem` with
src\shared\outbox\relay-store.ts-11-   * `ConditionExpression: attribute_not_exists(leaseOwner) OR leaseExpiresAt < :now`.
src\shared\outbox\relay-store.ts-12-   * Returns false if another relay/sweeper execution already holds the lease. */
src\shared\outbox\relay-store.ts-13-  tryAcquireLease(key: EntityKey, leaseOwner: string, leaseExpiresAt: string, now: string): Promise<boolean>;
src\shared\outbox\relay-store.ts-14-  /** `UpdateItem` transition PENDING -> PUBLISHED, only called after SendMessage confirmed. */
--
src\shared\outbox\outbox.ts-30-   * implementation, not previously known. Every outbox-driven consumer in production is
src\shared\outbox\outbox.ts-31-   * destination-routed through DispatchOutboxRelay; this destination follows the same
src\shared\outbox\outbox.ts-32-   * proven mechanism rather than the never-built one the original design doc assumed. */
src\shared\outbox\outbox.ts-33-  | "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1";
src\shared\outbox\outbox.ts-34-
src\shared\outbox\outbox.ts:35:export interface OutboxRecord {
src\shared\outbox\outbox.ts-36-  PK: string;
src\shared\outbox\outbox.ts-37-  SK: string;
src\shared\outbox\outbox.ts-38-  entityType: "OutboxEvent";
src\shared\outbox\outbox.ts-39-  /** BLOCKER-B addition: previously only embedded in `PK` (`TENANT#<t>#OUTBOX#<shard>`),
src\shared\outbox\outbox.ts-40-   * never its own field - a sender whose payload doesn't already self-describe its tenant
--
src\shared\idempotency\idempotency.ts-10-import { InternalError } from "../errors/app-error.js";
src\shared\idempotency\idempotency.ts-11-import { isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";
src\shared\idempotency\idempotency.ts-12-
src\shared\idempotency\idempotency.ts-13-export type IdempotencyResult = "ACQUIRED" | "COMPLETED_SAME_REQUEST";
src\shared\idempotency\idempotency.ts-14-
src\shared\idempotency\idempotency.ts:15:export interface IdempotencyBeginInput {
src\shared\idempotency\idempotency.ts-16-  tenantId: string;
src\shared\idempotency\idempotency.ts-17-  operation: string;
src\shared\idempotency\idempotency.ts-18-  key: string;
src\shared\idempotency\idempotency.ts-19-  requestHash: string;
src\shared\idempotency\idempotency.ts-20-  expiresAt: string;
src\shared\idempotency\idempotency.ts-21-}
src\shared\idempotency\idempotency.ts-22-
src\shared\idempotency\idempotency.ts:23:export interface IdempotencyCompleteInput {
src\shared\idempotency\idempotency.ts-24-  tenantId: string;
src\shared\idempotency\idempotency.ts-25-  operation: string;
src\shared\idempotency\idempotency.ts-26-  key: string;
src\shared\idempotency\idempotency.ts-27-  responseRef?: string;
src\shared\idempotency\idempotency.ts-28-}
src\shared\idempotency\idempotency.ts-29-
src\shared\idempotency\idempotency.ts:30:interface IdempotencyRecord {
src\shared\idempotency\idempotency.ts-31-  PK: string;
src\shared\idempotency\idempotency.ts-32-  SK: string;
src\shared\idempotency\idempotency.ts-33-  entityType: "IdempotencyRecord";
src\shared\idempotency\idempotency.ts-34-  tenantId: string;
src\shared\idempotency\idempotency.ts-35-  operation: string;
--
src\shared\idempotency\idempotency.ts-39-  expiresAt: string;
src\shared\idempotency\idempotency.ts-40-  createdAt: string;
src\shared\idempotency\idempotency.ts-41-  completedAt?: string;
src\shared\idempotency\idempotency.ts-42-}
src\shared\idempotency\idempotency.ts-43-
src\shared\idempotency\idempotency.ts:44:export interface IdempotencyAbortInput {
src\shared\idempotency\idempotency.ts-45-  tenantId: string;
src\shared\idempotency\idempotency.ts-46-  operation: string;
src\shared\idempotency\idempotency.ts-47-  key: string;
src\shared\idempotency\idempotency.ts-48-}
src\shared\idempotency\idempotency.ts-49-
src/modules/document/domain/document.ts-39-  contentLength: number;
src/modules/document/domain/document.ts-40-  checksumSha256: string;
src/modules/document/domain/document.ts-41-  status: DocumentStatus;
src/modules/document/domain/document.ts-42-  quarantineObject: DocumentObjectReference;
src/modules/document/domain/document.ts-43-  /** Only present once promoted - never populated before status === "CLEAN". */
src/modules/document/domain/document.ts:44:  cleanObject?: DocumentObjectReference;
src/modules/document/domain/document.ts-45-  uploadEvidence?: UploadEvidence;
src/modules/document/domain/document.ts-46-  malwareEvidence?: MalwareEvidence;
src/modules/document/domain/document.ts-47-  extractionRunId?: string; // M7, unused by M6, kept for schema forward-compatibility only.
src/modules/document/domain/document.ts-48-  retentionClass: Extract<DocumentRetentionClass, "USER_DOCUMENT">;
src/modules/document/domain/document.ts-49-  purgeAfter?: string; // set only once a terminal/deletion event establishes the clock.
src/modules/document/domain/document.ts-50-  /** W3-06: global (non-tenant-prefixed) GSI6 pointer for the purge worker - same convention as
src/modules/document/domain/document.ts-51-   * `GSI6PK_RECON_UPLOAD_PENDING`. Present only while a purge candidate is pending/claimed;
src/modules/document/domain/document.ts-52-   * removed by the same transaction that physically deletes the row. */
src/modules/document/domain/document.ts-53-  GSI6PK?: string;
src/modules/document/domain/document.ts-54-  GSI6SK?: string;
src/modules/document/domain/document.ts:55:  /** W3-06: minimal hold flag - `docs/architecture/privacy-lgpd.md` §3/§4's `legalHold`, scoped
src/modules/document/domain/document.ts-56-   * here to just the boolean the purge claim checks. No setter exists yet anywhere in this
src/modules/document/domain/document.ts-57-   * codebase; any future one MUST write it via an OCC-conditioned update including
src/modules/document/domain/document.ts-58-   * `attribute_not_exists(GSI6PK) OR GSI6PK <> :purgeClaimed` (D-061) - that condition, not this
src/modules/document/domain/document.ts-59-   * field alone, is what makes hold and purge mutually exclusive by construction. */
src/modules/document/domain/document.ts:60:  legalHold?: boolean;
src/modules/document/domain/document.ts-61-  /** W3-06: incremented on every purge claim; reconciliation moves a candidate past 5 failed
src/modules/document/domain/document.ts-62-   * attempts to `purgeStatus: "STUCK"` instead of reclaiming it forever. */
src/modules/document/domain/document.ts-63-  purgeAttempts?: number;
src/modules/document/domain/document.ts-64-  /** W3-06: terminal marker for a purge candidate that failed repeatedly - removed from both
src/modules/document/domain/document.ts-65-   * GSI6 purge worklists, requires manual intervention to re-enqueue. */
--
src/workers/document-purge/purge.ts-9- *
src/workers/document-purge/purge.ts-10- * Mechanism summary (full design + 6-round Claude<->Codex protocol:
src/workers/document-purge/purge.ts-11- * `docs/architecture/reviews/w3-06-user-document-purge-design/`):
src/workers/document-purge/purge.ts-12- *  - `Document` candidates go through a claim/lease (`WORKSTATE#PURGE_PENDING` ->
src/workers/document-purge/purge.ts-13- *    `WORKSTATE#PURGE_CLAIMED`, 15min lease) BEFORE any S3 call, fenced by an OCC-conditioned
src/workers/document-purge/purge.ts:14: *    `Update` checking version, `legalHold`, `status`, and the exact GSI6 pointer read — the
src/workers/document-purge/purge.ts-15- *    claim itself is the fence against concurrency/hold/restoration, not a later best-effort
src/workers/document-purge/purge.ts-16- *    recheck.
src/workers/document-purge/purge.ts:17: *  - The real S3 object is `doc.cleanObject` (promoted documents) or
src/workers/document-purge/purge.ts-18- *    `doc.uploadEvidence?.object ?? doc.malwareEvidence?.object` (rejected/unsupported
src/workers/document-purge/purge.ts-19- *    documents that had real evidence) — NEVER `doc.quarantineObject`, whose `versionId` is
src/workers/document-purge/purge.ts-20- *    always `""` (see `document-service.ts`/`advance-after-evidence.ts`). A document that never
src/workers/document-purge/purge.ts-21- *    received any evidence has nothing to delete by version; the quarantine bucket's own
src/workers/document-purge/purge.ts-22- *    lifecycle rule is the backstop for that case.
--
src/workers/document-purge/purge.ts-28- *    deleted directly from `PENDING` in a single conditioned transaction — no `CLAIMED` state.
src/workers/document-purge/purge.ts-29- *  - Lease reconciliation (`WORKSTATE#PURGE_CLAIMED` past its lease) reverts to `PENDING` unless
src/workers/document-purge/purge.ts-30- *    `purgeAttempts >= 5`, in which case the candidate is moved to a materialized terminal state
src/workers/document-purge/purge.ts-31- *    (`purgeStatus: "STUCK"`, removed from both GSI6 worklists) instead of being reclaimed
src/workers/document-purge/purge.ts-32- *    forever — a metric alone is not a durable record of a stuck candidate.
src/workers/document-purge/purge.ts:33: *  - `legalHold`: no setter exists anywhere in this codebase yet. Any future one MUST include
src/workers/document-purge/purge.ts-34- *    `attribute_not_exists(GSI6PK) OR GSI6PK <> "WORKSTATE#PURGE_CLAIMED"` in its own
src/workers/document-purge/purge.ts-35- *    OCC-conditioned write (D-061, normative) — that is what makes hold and purge mutually
src/workers/document-purge/purge.ts-36- *    exclusive by construction, not this worker's claim condition alone.
src/workers/document-purge/purge.ts-37- */
src/workers/document-purge/purge.ts-38-import {
--
src/workers/document-purge/purge.ts-67-  version: number;
src/workers/document-purge/purge.ts-68-  status: string;
src/workers/document-purge/purge.ts-69-  GSI6PK: string;
src/workers/document-purge/purge.ts-70-  GSI6SK: string;
src/workers/document-purge/purge.ts-71-  purgeAfter: string;
src/workers/document-purge/purge.ts:72:  legalHold?: boolean;
src/workers/document-purge/purge.ts-73-  purgeAttempts?: number;
src/workers/document-purge/purge.ts-74-  deletedAt?: string;
src/workers/document-purge/purge.ts:75:  cleanObject?: DocumentObjectReference;
src/workers/document-purge/purge.ts-76-  uploadEvidence?: { object: DocumentObjectReference };
src/workers/document-purge/purge.ts-77-  malwareEvidence?: { object: DocumentObjectReference };
src/workers/document-purge/purge.ts-78-}
src/workers/document-purge/purge.ts-79-
src/workers/document-purge/purge.ts-80-export interface ReceiptPurgeCandidate {
--
src/workers/document-purge/purge.ts-107-  leasesStuck: number;
src/workers/document-purge/purge.ts-108-}
src/workers/document-purge/purge.ts-109-
src/workers/document-purge/purge.ts-110-/** Never `doc.quarantineObject` — its `versionId` is always `""` (reserveUpload sets it before
src/workers/document-purge/purge.ts-111- * the real object exists, see `document-service.ts`/`advance-after-evidence.ts`). */
src/workers/document-purge/purge.ts:112:function pickObjectToDelete(candidate: DocumentPurgeCandidate): DocumentObjectReference | undefined {
src/workers/document-purge/purge.ts:113:  return candidate.cleanObject ?? candidate.uploadEvidence?.object ?? candidate.malwareEvidence?.object;
src/workers/document-purge/purge.ts-114-}
src/workers/document-purge/purge.ts-115-
src/workers/document-purge/purge.ts-116-async function claimAndPurgeDocument(deps: DocumentPurgeWorkerDeps, candidate: DocumentPurgeCandidate): Promise<"PURGED" | "SKIPPED"> {
src/workers/document-purge/purge.ts-117-  const claimAt = deps.now();
src/workers/document-purge/purge.ts-118-  const claimExpiresAt = new Date(Date.parse(claimAt) + PURGE_LEASE_MS).toISOString();
--
src/workers/document-purge/purge.ts-130-            GSI6PK: GSI6PK_PURGE_CLAIMED,
src/workers/document-purge/purge.ts-131-            GSI6SK: buildDocumentPurgeClaimGsi6Sk(claimExpiresAt, candidate.tenantId, candidate.documentId),
src/workers/document-purge/purge.ts-132-            purgeAttempts: nextAttempts,
src/workers/document-purge/purge.ts-133-          },
src/workers/document-purge/purge.ts-134-          extraConditions: [
src/workers/document-purge/purge.ts:135:            { expression: "attribute_not_exists(#legalHold) OR #legalHold = :false", names: { "#legalHold": "legalHold" }, values: { ":false": false } },
src/workers/document-purge/purge.ts-136-            { expression: "#status = :deleted", names: { "#status": "status" }, values: { ":deleted": "DELETED" } },
src/workers/document-purge/purge.ts-137-            { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": GSI6PK_PURGE_PENDING, ":expectedSk": candidate.GSI6SK } },
src/workers/document-purge/purge.ts-138-            { expression: "purgeAfter <= :purgeCutoff", values: { ":purgeCutoff": claimAt } },
src/workers/document-purge/purge.ts-139-          ],
src/workers/document-purge/purge.ts-140-        }),
--
src/workers/document-purge/purge.ts-144-    if (isTransactionCanceled(err)) return "SKIPPED";
src/workers/document-purge/purge.ts-145-    throw err;
src/workers/document-purge/purge.ts-146-  }
src/workers/document-purge/purge.ts-147-
src/workers/document-purge/purge.ts-148-  const claimedVersion = candidate.version + 1;
src/workers/document-purge/purge.ts:149:  const objectToDelete = pickObjectToDelete(candidate);
src/workers/document-purge/purge.ts-150-  if (objectToDelete) await deps.objects.deleteObjectVersion(objectToDelete);
src/workers/document-purge/purge.ts-151-
src/workers/document-purge/purge.ts-152-  const purgedAt = deps.now();
src/workers/document-purge/purge.ts-153-  const receiptPurgeAfter = computeDeliveryRecordPurgeAfter(purgedAt);
src/workers/document-purge/purge.ts-154-  const receipt: DocumentPurgeReceipt = {

codex
## Achados bloqueantes

1. **O inventário “exaustivo de 39 `entityType`s” continua incorreto.** O código contém `TenantEntitlement` em `src/modules/subject/domain/entitlement.ts`, mas a tabela da Rodada 3 o omite. Portanto, a allowlist fail-closed bloquearia uma exclusão real ao encontrá-lo.

2. **`DeviceSession` está mal classificado.** Existem dois contextos:

   - O `DeviceSession` da tabela principal, em `user-repository.ts`, não possui `version` e é atualizado por `PutItem` incondicional em `upsertDeviceSession()`/`logoutDevice()`.
   - A Rodada 3 o classifica como OCC versionado.
   
   O delete exigindo `version = :v` jamais apagaria esse shape. Pior: um `PutItem` concorrente iniciado antes da exclusão pode recriar a linha depois do delete.

3. **`LoginAttempt` está mal classificado e não é descobrível pelo algoritmo proposto.** Ele possui `version`, mas não possui `tenantId`, pois nasce antes da autenticação resolver o tenant. Logo, um `Scan` filtrado por `tenantId = :t` nunca o encontra, e a condição de delete proposta nunca poderia ser satisfeita. Ele deveria ser explicitamente classificado como registro não atribuível ao tenant e autopurgável por TTL, não como entidade tenant-scoped versionada.

4. **`TenantQuota` não é “nunca mutado após criação”.** `TenantQuotaService.consume()` e `release()` fazem atualizações condicionais frequentes de `count/resetAt`. A exclusão sem versão não necessariamente perde uma atualização já concluída, mas pode causar:

   - o loop de quota reler ausência e recriar o registro via `putIfAbsent`;
   - uma nova requisição recriar a quota após ela ser apagada;
   - consumo ativo continuar enquanto a cascata declara conclusão.

   Isso reforça que classificação por shape de delete não substitui um fence de lifecycle do tenant.

5. **A categoria “sem versão — nunca mutado” agrupa entidades com semânticas incompatíveis.** Em particular:

   - `OutboxEvent` possui lease e transições para publicação; não é append-only imóvel. Apagá-lo pode perder um evento pendente ou correr com publicação externa já iniciada.
   - `IdempotencyRecord` muda entre `IN_PROGRESS`, `COMPLETED` e `ABORTED`. Apagá-lo durante uma operação permite nova aquisição da mesma chave e possível execução duplicada.
   - `TenantQuota`, como acima, é contador mutável.
   
   A ausência de `version` não autoriza delete genérico. Cada tipo precisa de condição/fence compatível com seu protocolo de escrita e seus efeitos externos.

6. **A revarredura não fecha o achado de convergência da Rodada 1; apenas o limita temporalmente.** Há uma janela inevitável:

   1. `CONVERGENCE_CHECK` observa zero;
   2. uma escrita previamente iniciada ou uma nova operação cria/recria uma linha;
   3. o request é marcado `COMPLETED`.

   Sem um estado de tenant consultado por todos os writers — ou outro fence equivalente — “zero observado” não implica “permanecerá zero”. A proposta tampouco exige `ConsistentRead: true`; um `Scan` eventualmente consistente pode retornar zero mesmo quando já existem linhas. Mesmo um Scan fortemente consistente não é snapshot transacional e não impede escrita posterior. `maxPasses` evita loop infinito e torna a falha honesta enquanto linhas são vistas, mas não torna `COMPLETED` verificavelmente terminal.

7. **A segunda tabela não resolve o problema; amplia a janela de não atomicidade.** A Rodada 3 não define uma barreira consistente entre os checks da tabela principal e da BFF. Além disso, login/refresh podem criar ou atualizar `Session` durante e depois da varredura. Ver zero nas duas tabelas em momentos distintos não prova que ambas estão simultaneamente vazias nem impede nova sessão posterior.

8. **A exclusão de `Document` perde o fence que tornava W3-06 seguro.** O worker existente primeiro adquire claim OCC condicionado e só depois apaga o objeto S3. A nova proposta fala em apagar o objeto via `pickObjectToDelete` e depois deletar a linha diretamente. Se o documento mudar entre leitura, S3 e delete condicionado, o objeto pode ser apagado e o delete DynamoDB falhar, deixando uma linha viva apontando para conteúdo inexistente. Checar apenas `legalHold` no delete final não protege o efeito S3 anterior.

9. **O lifecycle do próprio `TenantDeletionRequest` não está definido.** Se ele ficar na tabela principal com o mesmo `tenantId`, será encontrado pelo Scan:

   - se seu `entityType` não estiver na allowlist, bloqueará a execução;
   - se for apagado, perde-se cursor, estado, evidência e capacidade de marcar `COMPLETED`;
   - se for excluído deliberadamente do filtro, o significado de “zero linhas” precisa formalmente ignorá-lo.

   A proposta precisa definir tabela/partição de controle, exclusão explícita do predicado e retenção da evidência terminal.

## Achados não-bloqueantes

1. A remoção do reuso de GSI6 fecha corretamente a colisão com workflows existentes.

2. A allowlist fail-closed é a direção correta, embora seu inventário e suas estratégias ainda estejam incorretos.

3. O fallback item a item evita que um conflito impeça a exclusão dos outros 24 itens. Entretanto, “pular qualquer falha condicional” deveria distinguir item ausente de item mutado; hoje ambos desaparecem na mesma categoria operacional.

4. Remover `purgeAfter` deste mecanismo elimina a contradição direta com a janela normativa de 30 dias. O contrato do chamador ainda precisará ser impossível de contornar na implementação, não apenas uma convenção documental.

5. `INCOMPLETE_NEEDS_REVIEW` após `maxPasses` é semanticamente melhor que um falso sucesso, mas precisa de retry/reentrada explícita e métricas/alarme operacional.

## Nota: 5,1/10

## Veredito

**REPROVADO — abaixo do gate 9,0.**

As Rodadas 2–3 corrigiram de verdade GSI6, fail-open, batching e a contradição de `purgeAfter`. Porém, os dois fundamentos do mecanismo continuam abertos: o inventário de entidades ainda está factualmente errado e `COMPLETED` continua sem garantia de não ressurreição.

A próxima rodada precisa, no mínimo, introduzir um fence de tenant obedecido por todos os writers relevantes, corrigir o inventário por tabela e por shape real, tratar `DeviceSession`, `LoginAttempt` e `TenantEntitlement`, definir o lifecycle do request de controle e preservar o claim/fence antes de qualquer efeito destrutivo em S3.
tokens used
80.477
## Achados bloqueantes

1. **O inventário “exaustivo de 39 `entityType`s” continua incorreto.** O código contém `TenantEntitlement` em `src/modules/subject/domain/entitlement.ts`, mas a tabela da Rodada 3 o omite. Portanto, a allowlist fail-closed bloquearia uma exclusão real ao encontrá-lo.

2. **`DeviceSession` está mal classificado.** Existem dois contextos:

   - O `DeviceSession` da tabela principal, em `user-repository.ts`, não possui `version` e é atualizado por `PutItem` incondicional em `upsertDeviceSession()`/`logoutDevice()`.
   - A Rodada 3 o classifica como OCC versionado.
   
   O delete exigindo `version = :v` jamais apagaria esse shape. Pior: um `PutItem` concorrente iniciado antes da exclusão pode recriar a linha depois do delete.

3. **`LoginAttempt` está mal classificado e não é descobrível pelo algoritmo proposto.** Ele possui `version`, mas não possui `tenantId`, pois nasce antes da autenticação resolver o tenant. Logo, um `Scan` filtrado por `tenantId = :t` nunca o encontra, e a condição de delete proposta nunca poderia ser satisfeita. Ele deveria ser explicitamente classificado como registro não atribuível ao tenant e autopurgável por TTL, não como entidade tenant-scoped versionada.

4. **`TenantQuota` não é “nunca mutado após criação”.** `TenantQuotaService.consume()` e `release()` fazem atualizações condicionais frequentes de `count/resetAt`. A exclusão sem versão não necessariamente perde uma atualização já concluída, mas pode causar:

   - o loop de quota reler ausência e recriar o registro via `putIfAbsent`;
   - uma nova requisição recriar a quota após ela ser apagada;
   - consumo ativo continuar enquanto a cascata declara conclusão.

   Isso reforça que classificação por shape de delete não substitui um fence de lifecycle do tenant.

5. **A categoria “sem versão — nunca mutado” agrupa entidades com semânticas incompatíveis.** Em particular:

   - `OutboxEvent` possui lease e transições para publicação; não é append-only imóvel. Apagá-lo pode perder um evento pendente ou correr com publicação externa já iniciada.
   - `IdempotencyRecord` muda entre `IN_PROGRESS`, `COMPLETED` e `ABORTED`. Apagá-lo durante uma operação permite nova aquisição da mesma chave e possível execução duplicada.
   - `TenantQuota`, como acima, é contador mutável.
   
   A ausência de `version` não autoriza delete genérico. Cada tipo precisa de condição/fence compatível com seu protocolo de escrita e seus efeitos externos.

6. **A revarredura não fecha o achado de convergência da Rodada 1; apenas o limita temporalmente.** Há uma janela inevitável:

   1. `CONVERGENCE_CHECK` observa zero;
   2. uma escrita previamente iniciada ou uma nova operação cria/recria uma linha;
   3. o request é marcado `COMPLETED`.

   Sem um estado de tenant consultado por todos os writers — ou outro fence equivalente — “zero observado” não implica “permanecerá zero”. A proposta tampouco exige `ConsistentRead: true`; um `Scan` eventualmente consistente pode retornar zero mesmo quando já existem linhas. Mesmo um Scan fortemente consistente não é snapshot transacional e não impede escrita posterior. `maxPasses` evita loop infinito e torna a falha honesta enquanto linhas são vistas, mas não torna `COMPLETED` verificavelmente terminal.

7. **A segunda tabela não resolve o problema; amplia a janela de não atomicidade.** A Rodada 3 não define uma barreira consistente entre os checks da tabela principal e da BFF. Além disso, login/refresh podem criar ou atualizar `Session` durante e depois da varredura. Ver zero nas duas tabelas em momentos distintos não prova que ambas estão simultaneamente vazias nem impede nova sessão posterior.

8. **A exclusão de `Document` perde o fence que tornava W3-06 seguro.** O worker existente primeiro adquire claim OCC condicionado e só depois apaga o objeto S3. A nova proposta fala em apagar o objeto via `pickObjectToDelete` e depois deletar a linha diretamente. Se o documento mudar entre leitura, S3 e delete condicionado, o objeto pode ser apagado e o delete DynamoDB falhar, deixando uma linha viva apontando para conteúdo inexistente. Checar apenas `legalHold` no delete final não protege o efeito S3 anterior.

9. **O lifecycle do próprio `TenantDeletionRequest` não está definido.** Se ele ficar na tabela principal com o mesmo `tenantId`, será encontrado pelo Scan:

   - se seu `entityType` não estiver na allowlist, bloqueará a execução;
   - se for apagado, perde-se cursor, estado, evidência e capacidade de marcar `COMPLETED`;
   - se for excluído deliberadamente do filtro, o significado de “zero linhas” precisa formalmente ignorá-lo.

   A proposta precisa definir tabela/partição de controle, exclusão explícita do predicado e retenção da evidência terminal.

## Achados não-bloqueantes

1. A remoção do reuso de GSI6 fecha corretamente a colisão com workflows existentes.

2. A allowlist fail-closed é a direção correta, embora seu inventário e suas estratégias ainda estejam incorretos.

3. O fallback item a item evita que um conflito impeça a exclusão dos outros 24 itens. Entretanto, “pular qualquer falha condicional” deveria distinguir item ausente de item mutado; hoje ambos desaparecem na mesma categoria operacional.

4. Remover `purgeAfter` deste mecanismo elimina a contradição direta com a janela normativa de 30 dias. O contrato do chamador ainda precisará ser impossível de contornar na implementação, não apenas uma convenção documental.

5. `INCOMPLETE_NEEDS_REVIEW` após `maxPasses` é semanticamente melhor que um falso sucesso, mas precisa de retry/reentrada explícita e métricas/alarme operacional.

## Nota: 5,1/10

## Veredito

**REPROVADO — abaixo do gate 9,0.**

As Rodadas 2–3 corrigiram de verdade GSI6, fail-open, batching e a contradição de `purgeAfter`. Porém, os dois fundamentos do mecanismo continuam abertos: o inventário de entidades ainda está factualmente errado e `COMPLETED` continua sem garantia de não ressurreição.

A próxima rodada precisa, no mínimo, introduzir um fence de tenant obedecido por todos os writers relevantes, corrigir o inventário por tabela e por shape real, tratar `DeviceSession`, `LoginAttempt` e `TenantEntitlement`, definir o lifecycle do request de controle e preservar o claim/fence antes de qualquer efeito destrutivo em S3.
