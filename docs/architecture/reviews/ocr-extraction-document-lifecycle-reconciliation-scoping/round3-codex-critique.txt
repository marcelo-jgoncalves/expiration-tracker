2026-09-03T11:43:46.510053Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 93 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a06715-0a9e-7a92-8f42-fa5ab9122b52
--------
user
You are Codex, continuing as independent reviewer in this repo's Claude<->Codex debate protocol
(AGENTS.md section 4). This is Round 3. Round 2 scored 7.8/10 with 11 numbered required changes
(see round2-codex-critique.txt tail, or re-derive from your own critique if needed).

Read, in this order:
1. docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round2-claude-revision.md
2. docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round3-claude-final.md
   (Claude's Round 3, responding point-by-point to your 11 Round 2 items: concurrent promotion
   idempotency instead of false mutual-exclusion claim, explicit tenant-fence parameter instead
   of "inherited", ordered two-flag rollout to close the CLEAN-before-starter window, a concrete
   outbox destination (new OutboxDestination + SQS queue + DispatchOutboxRelay route, following
   the exact SQS_IMPORT_PARSE_V1 D-192 precedent) instead of assuming default EventBridge, a
   refresh worker that always re-reads current DocumentVersion/Requirement state instead of
   trusting the event payload (fixing out-of-order events), extending the EXISTING daily
   requirement-reindex worker to re-read live DocumentVersion instead of its own cache (closing
   the "unbounded staleness" gap you found), precise 3-aggregate/4-action vs 2-aggregate/2-action
   transaction naming, a fuller shared planner (planDocumentVersionValidityEffect) covering
   eligible states/no-op/provenance/exactly-once outbox for both auto-confirm and manual confirm,
   and an outcome-based (not solution-conformance) E-014 checklist rewrite.

Verify against real code again where relevant:
- src/shared/outbox/outbox.ts (does the proposed new OutboxDestination + DispatchOutboxRelay
  routing pattern match how SQS_IMPORT_PARSE_V1 or another existing destination is actually wired
  end to end? look at src/runtime/aws/handlers or composition for the relay if it exists)
- src/modules/document-archive/application/apply-file-scan-result.ts (does adding an explicit
  tenantActiveCondition parameter to applyFileScanResult/confirmFileScanClean's TransactWriteItems
  make sense given how buildVersionedUpdate/ConditionCheck work in this codebase?)
- the daily requirement-reindex worker (find it - likely src/workers/requirement-reindex/ or
  similar) - is "re-read live DocumentVersion instead of cached evidenceValidUntil" a coherent,
  minimal change to that worker's actual current logic, or does Round 3 hand-wave something about
  how it currently works?
- src/modules/extraction/domain/extraction-run.ts and wherever startExtractionRun is implemented
  - does the claimed attribute_not_exists idempotent-create pattern actually match how this
  codebase's OCC/idempotent-create builders work?

This is very likely the round that could close the protocol (minimum 3 rounds have now happened,
AGENTS.md section 4 requires both scores >= 9.0, no rounding, before APPROVED). Do not soften your
standard to reach convergence - if real gaps remain, say so plainly and list exactly what a Round
4 would need. But equally, do not invent nitpicks beyond what the evidence supports - a proposal
that has now closed the concrete gaps you listed in Round 2 deserves credit for that, scored on
its actual remaining risk.

End with an explicit numeric score out of 10 (no rounding, e.g. "Nota: 9.1/10"), and if it is not
yet >= 9.0, a precise numbered list of exactly what would need to change for Round 4.

codex
Vou revisar a Rodada 3 contra a revisão anterior e os caminhos reais de implementação citados. Primeiro confirmo o estado documental; depois rastreio outbox, transações, worker de reindex e criação idempotente antes de fechar a nota.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round2-claude-revision.md'; Get-Content -Raw 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round3-claude-final.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 322ms:
# Round 2 (Claude) ƒ?" Revision

**Claude's own blind score for Round 1 (recorded now that Codex's Round 1 score exists): 5.8/10.**
I agree with the substance of the critique: the authoritative-pipeline finding conflated three
distinct claims, Option A was unjustified scope creep, and the "3+N Requirements" transaction was
not sound (open cardinality, phantom race, no reverse index, ignored the existing auto-confirm
path). Every point below is addressed on the merits, not to chase a number.

## 1. Achado central ƒ?" reescrito, trÇ¦s conceitos separados

Corrigindo o excesso apontado: "autoritativo" tinha trÇ¦s sentidos diferentes na Rodada 1, e o
cÇüdigo sÇü sustenta o primeiro.

- **Pipeline fisicamente completo hoje**: sÇü o mÇüdulo `document` (antigo). Confirmado agora dos
  dois lados da correlaÇõÇœo simÇ¸trica, nÇœo sÇü do lado de malware result ƒ?" **achado novo desta
  rodada**: `upload-finalizer-handler.ts` (o consumidor do evento de upload finalizado, o outro
  sinal fÇðsico que `apply-file-scan-result.ts`'s correlaÇõÇœo exige) reconhece apenas
  `parseQuarantineKey` (antigo) e `parseSubmissionQuarantineKey` (subject) ƒ?" o mesmo buraco do
  `malware-result-handler.ts`, no sinal oposto. Um arquivo enviado via `document-archive` fica
  preso em `PENDING_UPLOAD` (nunca chega a `SCANNING`), nÇœo sÇü em `SCANNING` como a Rodada 1
  disse ƒ?" pior do que eu tinha reportado.
- **API que recebe trÇ­fego HTTP**: ambos os handlers estÇœo deployados e roteÇ­veis
  (`infra/main.tf`). Retiro a afirmaÇõÇœo de "trÇ­fego real" para `document_archive_handler` ƒ?" nÇœo
  verificÇ­vel a partir de Terraform/comentÇ­rio histÇürico, sÇü CloudWatch real (fora do escopo
  desta rodada de design). Mantenho, sem enfraquecer, que `documents_handler` tem o comentÇ­rio
  histÇürico citando trÇ­fego real ƒ?" Ç¸ evidÇ¦ncia de uso passado do antigo, nÇœo prova de ausÇ¦ncia de
  uso do novo.
- **Autoridade conceitual futura**: `document-archive`/`DocumentVersion`/`Requirement` ƒ?" nunca
  esteve em disputa; D-143 jÇ­ Ç¸ a arquitetura aprovada, este documento nÇœo a reabre.

**ConclusÇœo corrigida (adotada)**: o mÇüdulo antigo Ç¸ hoje o Ç§nico pipeline fisicamente completo
no cÇüdigo implantÇ­vel, nos dois lados da correlaÇõÇœo (upload finalizer E malware result).
`document-archive` Ç¸ o modelo de domÇðnio aprovado, com entrada HTTP/presign real, mas sem NENHUM
consumidor fÇðsico real para nenhum dos dois sinais que sua prÇüpria correlaÇõÇœo de evidÇ¦ncia exige.

## 2. OpÇõÇœo C adotada ƒ?" sequenciamento, nÇœo escopo indivisÇðvel

Abandono a falsa dicotomia A/B da Rodada 1. Adoto a **OpÇõÇœo C do Codex** como a decisÇœo desta
rodada:

1. Esta rodada decide a identidade/contrato de extraÇõÇœo contra `DocumentVersion`/`DocumentFile`
   (seÇõÇæes 3-6 abaixo) e o contrato do evento clean + precondiÇõÇæes de validaÇõÇœo no starter.
2. A implementaÇõÇœo da extraÇõÇœo re-chaveada Ç¸ construÇðda e testada contra esse contrato, mas o
   trigger real (`clean_object_created` reconhecendo o novo prefixo) fica **desligado por feature
   flag** (mesmo mecanismo jÇ­ usado por `extraction_pipeline_enabled` em `infra/main.tf:1965`,
   `1979`, `1986` ƒ?" um segundo flag, nÇœo um mecanismo novo) atÇ¸ a fatia #2 existir.
2'. A fatia de ingestÇœo fÇðsica (upload-finalizer + malware-result + promoÇõÇœo, item 3 abaixo) Ç¸
   implementada e testada independentemente.
3. O flag do trigger novo sÇü Ç¸ ligado quando ambas as fatias estÇœo prontas e verificadas em
   `dev`.

Isso nÇœo exige re-key duas vezes ƒ?" a extraÇõÇœo Ç¸ desenhada uma vez contra o alvo certo.

## 3. Arquitetura do promoter ƒ?" sem worker/fila novos, estendendo os dois handlers existentes

Rejeito a "nova fila SQS" da Rodada 1 (a crÇðtica estÇ­ certa: nebulosa, duplicava lÇügica). Em vez
disso, mesma forma que o mÇüdulo `subject` jÇ­ usa para coexistir com o mÇüdulo `document` no MESMO
par de handlers (`malware-result-handler.ts` jÇ­ ramifica em `parseQuarantineKey` vs
`parseSubmissionQuarantineKey` ƒ?" dois branches, um handler, duas filas de origem jÇ­ existentes
reaproveitadas):

- **`upload-finalizer-handler.ts`**: adicionar um terceiro branch, `parseDocumentArchiveQuarantineKey`
  (novo parser, mesmo estilo de `quarantine-key.ts`), chamando `applyFileScanResult` com
  `uploadEvidence` preenchido (em vez do `processMalwareResult`/`processSubmissionMalwareResult`
  do mÇüdulo antigo/subject) ƒ?" reaproveita a MESMA fila `upload_finalizer_queue`/EventBridge rule
  jÇ­ cabeada ao bucket de quarentena inteiro (`infra/main.tf:1027-1048`, nÇœo filtra por prefixo),
  sÇü adiciona uma terceira ramificaÇõÇœo de parsing no handler que jÇ­ a consome.
- **`malware-result-handler.ts`**: mesmo padrÇœo, terceiro branch chamando `applyFileScanResult`
  com `malwareEvidence` preenchido.
- **Quem copia quarentenaƒÅ'clean e chama `confirmFileScanClean`**: o MESMO handler cujo branch
  produziu `outcome: "READY_TO_PROMOTE"` faz o `CopyObject` (quarantineƒÅ'clean, verificaÇõÇœo de
  tamanho/checksum, mesma disciplina de `advance-after-evidence.ts`) e sÇü entÇœo chama
  `confirmFileScanClean` ƒ?" nunca os dois branches (upload-finalizer e malware-result) competindo
  para promover o mesmo arquivo, porque `applyFileScanResult` jÇ­ Ç¸ o Ç§nico ponto de decisÇœo
  (`decision.action === "PROMOTE"` sÇü Ç¸ alcanÇõado depois que AMBAS as evidÇ¦ncias consolidaram,
  nÇœo importa qual handler entregou a segunda ƒ?" `apply-file-scan-result.ts`'s doc comment jÇ­
  garante isso, reaproveitado sem mudanÇõa).
- **Retry/DLQ/ownership**: idÇ¦nticos aos que jÇ­ existem para o branch antigo nos mesmos dois
  handlers ƒ?" nenhum mecanismo novo, sÇü um terceiro `if (parsed) { ... }` ao lado dos dois jÇ­
  existentes em cada handler.
- **Limpeza do objeto de quarentena**: fora de escopo desta rodada ƒ?" o mÇüdulo antigo tambÇ¸m nÇœo
  limpa o objeto de quarentena apÇüs promoÇõÇœo (`advance-after-evidence.ts` nÇœo faz
  `DeleteObject`), entÇœo esta rodada mantÇ¸m paridade comportamental, nÇœo introduz uma lacuna
  nova.
- **Tenant lifecycle fence**: reaproveita o fence que `processMalwareResult`/`processSubmissionMalwareResult`
  jÇ­ aplicam antes de escrever (checagem de tenant ACTIVE) ƒ?" o novo branch chama a mesma
  checagem, nunca pula essa etapa.

## 4. Chave clean ƒ?" versionId, nÇœo seq (correÇõÇœo do achado)

Corrigindo a Rodada 1: a nova convenÇõÇœo usa o `versionId` imutÇ­vel de `DocumentVersion`, nunca o
`seq` mutÇ­vel-por-natureza-de-Çðndice: `document-archive/clean/<tenantId>/<documentId>/<versionId>/<fileId>`.
`versionId` jÇ­ existe como identificador estÇ­vel e imutÇ­vel de uma `DocumentVersion`
(`document-version.ts`), evitando o problema apontado pelo Codex de reindexaÇõÇœo sob retry/GSI. O
novo parser (`parseDocumentArchiveCleanKey`, ao lado do jÇ­ existente `parseCleanKey` do mÇüdulo
antigo ƒ?" os dois formatos coexistem, nenhum removido nesta rodada) devolve
`{tenantId, documentId, versionId, fileId}`.

## 5. Starter ƒ?" o evento S3 nunca Ç¸ confiado sozinho

Corrigindo o achado do Codex (evento clean pode chegar antes/fora de ordem do commit
`confirmFileScanClean`): `extraction-starter-handler.ts`, ao reconhecer o novo prefixo, SEMPRE
re-lÇ¦ `DocumentFile` fresco (nunca confia sÇü na chave do evento S3) e sÇü prossegue se TODAS as
condiÇõÇæes valem:

1. `file.scanStatus === "CLEAN"`;
2. `file.cleanObject` bate exatamente (bucket/key/versionId) com o objeto observado pelo evento
   S3 ƒ?" mesma disciplina de correlaÇõÇœo simÇ¸trica que `apply-file-scan-result.ts` jÇ­ usa entre
   evidÇ¦ncias;
3. `file.role === "PRINCIPAL"` (polÇðtica decidida no item 6 abaixo ƒ?" anexos nunca disparam OCR
   nesta fase);
4. a `DocumentVersion` correspondente estÇ­ em um estado elegÇðvel: `RECEIVED` ou `UNDER_REVIEW`
   (nunca `WITHDRAWN`/`REJECTED`/`SUPERSEDED` ƒ?" se a versÇœo jÇ­ saiu de consideraÇõÇœo entre a
   promoÇõÇœo fÇðsica e este evento, a extraÇõÇœo Ç¸ descartada como `IGNORED_STALE`, mesmo vocabulÇ­rio
   que `applyFileScanResult` jÇ­ usa). `ACCEPTED` tambÇ¸m Ç¸ elegÇðvel (evidÇ¦ncia jÇ­ aceita pode
   ainda precisar de extraÇõÇœo de campos de validade que a revisÇœo humana nÇœo preencheu).

Uma falha em qualquer condiÇõÇœo Ç¸ um `IGNORED_STALE`/log, nunca uma tentativa de iniciar extraÇõÇœo
sobre um objeto ÇürfÇœo.

## 6. Multi-arquivo ƒ?" sÇü PRINCIPAL dispara OCR (decisÇœo explÇðcita, nÇœo default acidental)

`DocumentFile` permite um `PRINCIPAL` e anexos (`document-file.ts`). DecisÇœo explÇðcita desta
rodada: **sÇü a promoÇõÇœo do arquivo `PRINCIPAL` gera um `ExtractionRun`.** Anexos promovidos nunca
disparam extraÇõÇœo nesta fase ƒ?" decisÇœo de produto deliberadamente conservadora (anexos
tipicamente sÇœo material de apoio, nÇœo o documento cujo campo de validade importa), registrada
aqui como escopo explÇðcito, revisÇ­vel por um item de trabalho futuro caso o produto precise de
extraÇõÇœo multi-arquivo. Isso elimina inteiramente a corrida "mÇ§ltiplos arquivos disparando
execuÇõÇæes concorrentes para a mesma Version" apontada pelo Codex ƒ?" nÇœo porque a corrida foi
resolvida por sincronizaÇõÇœo, mas porque sÇü existe um candidato possÇðvel por Version.

## 7. Identidade de `ExtractionRun`/`ExtractedField` ƒ?" redesenhada, sem ambiguidade

- `ExtractionRun` passa a ser identificado por `{tenantId, documentId, versionId}` ƒ?" nunca mais
  `itemId` (nÇœo existe no novo modelo) nem o campo ambÇðguo `documentVersion: number` (que hoje
  mistura o conceito de "nÇ§mero de versÇœo OCC do agregado Document" com "qual versÇœo lÇügica do
  documento" ƒ?" os dois precisam se separar). `PK = TENANT#t#DOC#d`, `SK = RUN#<versionId>#<runId>`
  ƒ?" mesmo padrÇœo de particionamento por Document que jÇ­ existe, sÇü troca o que qualifica a versÇœo
  lÇügica.
- `ExtractedField` mantÇ¸m sua chave atual (`extracted-field.ts:51-53`, `PK=TENANT#t#DOC#d`,
  `SK=FIELD#<fieldName>#<runId>`) sem mudanÇõa ƒ?" jÇ­ nÇœo dependia de `itemId`.
- Novos campos em `ExtractedField`: `confirmedBy: string` (o `RequestContext` do ator que
  confirmou/rejeitou ƒ?" nunca "SYSTEM" para o caminho manual) e `confirmedAt: string`, setados
  junto com `state` na mesma escrita que jÇ­ seta `state`/`confirmedValue`/`correctionReason` ƒ?"
  fecha o achado do Codex de que o checklist da Rodada 1 prometia auditabilidade que a entidade
  nÇœo sustentava. Caminho de auto-confirmaÇõÇœo (item 8) grava `confirmedBy: "SYSTEM_AUTOCONFIRM"`
  explicitamente, nunca omite o campo.

## 8. Auto-confirmaÇõÇœo ƒ?" unificada com o confirm manual via funÇõÇœo pura compartilhada

Corrigindo o bloqueante 3.5 do Codex: `run-extraction-validation.ts`/`dynamodb-extracted-field-store.ts`'s
`commitRunOutcome` jÇ­ escreve o agregado de negÇücio para campos auto-confirmados hoje ƒ?" a
proposta da Rodada 1 (sÇü `confirmField` grava valor de negÇücio) estava errada e contradizia o
cÇüdigo real.

CorreÇõÇœo: extrair uma funÇõÇœo pura `buildDocumentVersionValidityUpdate(fieldName, confirmedValue,
currentVersionState)` (nova, em `extraction/domain/`, testÇ­vel sem I/O ƒ?" mesmo padrÇœo que
`item-field-mapping.ts`'s `buildItemAttributeUpdate` jÇ­ estabelece) que os DOIS caminhos chamam:

- `doConfirmField` (HTTP, humano) ƒ?" chama-a com `confirmedBy = ctx.identity`, dentro da mesma
  transaÇõÇœo 3-way (item 9).
- `commitRunOutcome` (auto-confirm, `run-extraction-validation.ts`) ƒ?" chama-a com
  `confirmedBy = "SYSTEM_AUTOCONFIRM"`, dentro da MESMA transaÇõÇœo que jÇ­ escreve `ExtractedField`
  auto-confirmado (nunca uma segunda transaÇõÇœo separada).

Os dois caminhos produzem exatamente o mesmo efeito colateral sobre `DocumentVersion` ƒ?" que campo
foi atualizado, com qual valor ƒ?" atravÇ¸s da mesma funÇõÇœo, nunca duas implementaÇõÇæes divergentes
(mesmo princÇðpio que `confirm-reject-field.ts`'s doc comment jÇ­ cita para o `ExpirationItem`
antigo: "os dois caminhos devem produzir efeito de item idÇ¦ntico").

## 9. TransaÇõÇœo de confirmaÇõÇœo ƒ?" 3-way fixo, Requirement NUNCA na mesma transaÇõÇœo

Abandono por completo o "3+N Requirements" da Rodada 1 ƒ?" o Codex estÇ­ certo de que nÇœo fecha
(sem Çðndice reverso, corrida fantasma, teto de 100 aÇõÇæes, cardinalidade aberta). A correÇõÇœo nÇœo Ç¸
consertar o fan-out sÇðncrono, Ç¸ removÇ¦-lo:

- **`confirmField`**: transaÇõÇœo 3-way fixa ƒ?" `DocumentVersion` (usa a funÇõÇœo do item 8),
  `ExtractionRun` (proteÇõÇœo OCC, sem mudanÇõa de dado), `ExtractedField` (stateƒÅ'CONFIRMED,
  confirmedValue, confirmedBy, confirmedAt). Exatamente a mesma cardinalidade fixa que a Rodada 1
  jÇ­ tinha para o 3-way original ƒ?" sÇü troca `Document`(antigo) por `DocumentVersion`.
- **`rejectField`**: 2-way ƒ?" `ExtractionRun` (proteÇõÇœo) + `ExtractedField` (stateƒÅ'REJECTED) ƒ?"
  NUNCA toca `DocumentVersion`, corrigindo a formulaÇõÇœo "3 vias sempre" da Rodada 1 apontada como
  excessivamente ampla no achado 3.7. Reject Ç¸ estritamente mais simples que hoje (era 3-way
  contra `Document` antigo; passa a nÇœo precisar nem tocar o agregado de documento, jÇ­ que rejeitar
  um campo nunca muda `validUntil`).
- **`Requirement` nunca entra em nenhuma dessas transaÇõÇæes.** Em vez disso: quando
  `buildDocumentVersionValidityUpdate` produz uma mudanÇõa real de `validUntil`, a MESMA transaÇõÇœo
  emite um evento outbox (`src/shared/outbox/outbox.ts`, jÇ­ obrigatÇürio para eventos crÇðticos no
  mesmo `TransactWriteItems` do agregado ƒ?" `AGENTS.md` ¶õ7) `DocumentVersionValidityChanged
  {tenantId, documentId, versionId, validUntil}`.
- Um novo worker leve e idempotente, `requirement-evidence-refresh`, consome esse evento e
  re-deriva CADA `Requirement` vinculado, UM DE CADA VEZ, com seu prÇüprio loop de retry OCC ƒ?"
  exatamente a mesma disciplina que o worker diÇ­rio `requirement-reindex` (D-179/D-185) jÇ­ usa
  para o drift SATISFIEDƒÅ'NOT_SATISFIED por passagem de tempo (`requirement.ts`'s
  `deriveRequirementMaintenanceDue`). Este nÇœo Ç¸ um mecanismo novo de consistÇ¦ncia: Ç¸ o MESMO
  worker de reconciliaÇõÇœo assÇðncrona jÇ­ aprovado neste projeto, disparado por evento em vez de sÇü
  por agenda diÇ­ria.
- **Isto nÇœo Ç¸ uma fuga de responsabilidade ƒ?" Ç¸ o invariante que `requirement.ts` jÇ­ declara e
  aceita explicitamente** (linha 68-70 do arquivo: *"a DocumentVersion whose state changes AFTER
  the link ... is caught the next time something re-links or re-derives this Requirement, not
  instantly ƒ?" the same bounded-staleness window Decision 5 accepts"*). Um `confirmField` que muda
  `validUntil` Ç¸ exatamente essa classe de evento ƒ?" a Rodada 1 tentou fechar de forma sÇðncrona uma
  janela que o prÇüprio design de D-143 jÇ­ aceita como asynchronamente convergente. Esta rodada sÇü
  estreita a janela (evento-disparado, tipicamente segundos, contra "prÇüxima reindexaÇõÇœo diÇ­ria
  ou prÇüximo link/unlink manual").
- **Reverse lookup para o worker encontrar os Requirements de uma Version**: novo Çðndice esparso
  `GSI_EVIDENCE` (nome de infraestrutura a decidir na implementaÇõÇœo ƒ?" nÇœo Ç¸ GSI3/GSI6, nÇœo exige
  a exceÇõÇœo de isolamento de `AGENTS.md` ¶õ7), escrito/removido transacionalmente DENTRO de
  `linkEvidence`/`unlinkEvidence` (que jÇ­ escrevem `Requirement` numa transaÇõÇœo prÇüpria ƒ?" sÇü
  adicionam um par de atributos ao mesmo item, sem nova escrita separada):
  `GSI_EVIDENCE_PK = TENANT#t#DOCVERSION#<versionId>`, `GSI_EVIDENCE_SK = REQUIREMENT#<requirementId>`.
  Isso resolve 3.1 (Çðndice reverso existe) sem resolver 3.2 (corrida entre a query do worker e um
  `linkEvidence` concorrente) por sincronizaÇõÇœo ƒ?" resolve-a por ACEITAÇÎÇŸO EXPLÇ?CITA do mesmo
  bounded-staleness: se C se vincula depois que o worker jÇ­ leu A/B, C simplesmente lÇ¦
  `DocumentVersion.validUntil` jÇ­ atualizado no momento do seu PRÇ"PRIO `linkEvidence` (que sempre
  lÇ¦ a versÇœo corrente, nunca uma cÇüpia cacheada) ƒ?" C nunca fica desatualizado, Ç¸ A/B que ficariam
  presos ao valor antigo por um ciclo a mais se o worker corresse ANTES do link de C, cenÇ­rio
  idÇ¦ntico ao que o reindex diÇ­rio jÇ­ tolera hoje. Nenhuma cardinalidade aberta em nenhuma
  transaÇõÇœo ƒ?" o worker processa Requirements um a um, sem teto de 100 aÇõÇæes porque nunca hÇ­ uma
  transaÇõÇœo com N itens dinÇ½micos.

## 10. IdempotÇ¦ncia ƒ?" simplificada pela remoÇõÇœo do fan-out sÇðncrono

Com `Requirement` fora da transaÇõÇœo, o request hash de `confirmField`/`rejectField` volta a ser
exatamente do mesmo formato de hoje (`confirm-reject-field.ts:102-116`), sÇü trocando
`expectedDocumentVersion` por `expectedDocumentVersionVersion` (a versÇœo OCC de `DocumentVersion`,
nome literal para nÇœo colidir com `versionId`) ƒ?" nenhum campo dinÇ½mico de Requirement entra no
hash, porque nenhuma operaÇõÇœo em Requirement acontece dentro desta transaÇõÇœo. A janela
commit-then-fail-before-`complete()` jÇ­ existente (`idempotency.begin`/`transactWrite`/`complete`)
nÇœo muda de comportamento ƒ?" mesmo padrÇœo, mesmo risco residual jÇ­ aceito pelo cÇüdigo atual, nÇœo
introduzido nem agravado por esta rodada.

## 11. Checklist E-014 ƒ?" reponderado, aceitando a crÇðtica do Codex quase verbatim

Adoto a reponderaÇõÇœo do Codex, com pequenos ajustes de Ç½ncora explÇðcita:

1. **(peso 25%) Identidade/idempotÇ¦ncia corretas por `DocumentVersion`/`DocumentFile`.** Atende:
   `ExtractionRun` chaveado por `versionId` imutÇ­vel, starter revalida `DocumentFile` fresco antes
   de iniciar, nenhuma ambiguidade `seq` vs `versionId`. NÇœo atende: qualquer identidade que ainda
   dependa de `itemId` ou de um "nÇ§mero de versÇœo" ambÇðguo.
2. **(peso 25%) `validUntil`/`Requirement` convergem sem fan-out transacional aberto.** Atende: a
   transaÇõÇœo de confirmaÇõÇœo tem cardinalidade FIXA (3-way ou 2-way), Requirement atualiza via
   worker assÇðncrono idempotente um-a-um, dentro do bounded-staleness jÇ­ aceito por D-143. NÇœo
   atende: qualquer transaÇõÇœo cujo tamanho dependa do nÇ§mero de Requirements vinculados.
3. **(peso 20%) Trigger seguro contra a corrida S3-copy-antes-de-confirm.** Atende: o starter
   sempre re-lÇ¦ `DocumentFile` e verifica scanStatus/cleanObject/role/estado da Version antes de
   iniciar um run. NÇœo atende: qualquer caminho que inicie extraÇõÇœo sÇü a partir da chave do
   evento S3.
4. **(peso 15%) SemÇ½ntica multi-arquivo explÇðcita, nÇœo acidental.** Atende: sÇü PRINCIPAL dispara
   OCR, decisÇœo registrada como escopo (nÇœo uma omissÇœo). NÇœo atende: qualquer ambiguidade sobre
   o que acontece quando mÇ§ltiplos arquivos de uma Version sÇœo promovidos.
5. **(peso 10%) Preserva corretamente auto-confirmaÇõÇœo E revisÇœo humana com o MESMO efeito
   colateral.** Atende: `buildDocumentVersionValidityUpdate` compartilhada pelos dois caminhos.
   NÇœo atende: qualquer divergÇ¦ncia de efeito entre o path automÇ­tico e o manual.
6. **(peso 5%) ProveniÇ¦ncia auditÇ­vel de verdade.** Atende: `confirmedBy`/`confirmedAt`
   realmente existem em `ExtractedField` e sÇœo sempre preenchidos (inclusive
   `SYSTEM_AUTOCONFIRM`). NÇœo atende: alegar auditabilidade sem o campo persistido.

## Pontos ainda em aberto para a crÇðtica do Codex nesta rodada

- O nome/posicionamento exato do `GSI_EVIDENCE` (qual GSI fÇðsico do zero vs slot reaproveitado) ƒ?"
  deliberadamente deixado como detalhe de implementaÇõÇœo, nÇœo decisÇœo de protocolo, mas abro para
  o Codex contestar se isso Ç¸ council-worthy nesta rodada.
- Se `PRINCIPAL`-only Ç¸ a polÇðtica de produto correta de longo prazo, ou apenas a mais simples
  para nÇœo bloquear esta reconciliaÇõÇœo ƒ?" registrado como decisÇœo explÇðcita e revisÇ­vel, nÇœo uma
  afirmaÇõÇœo de que Ç¸ definitivamente certa para sempre.

# Round 3 (Claude) ƒ?" Final Revision

**Claude's own blind score for Round 2 (recorded now that Codex's Round 2 score, 7.8/10, exists):
7.6/10.** The direction (async convergence, dropped fan-out) was right, but Codex is correct that
I asserted mutual exclusion, tenant-fence inheritance, and outbox delivery that the code doesn't
actually back ƒ?" three real gaps, not nitpicks, since a delivery-path decision this leans on
literally doesn't exist yet in `outbox.ts`. Addressing all 11 points below.

## 1. PromoÇõÇœo concorrente ƒ?" nÇœo afirmo mais exclusividade mÇ§tua

Corrigido: os dois branches (upload-finalizer, malware-result) PODEM ambos observar
`READY_TO_PROMOTE` e ambos executar `CopyObject`. Isto Ç¸ seguro, nÇœo porque sÇü um execute, mas
porque:

- A chave de destino Ç¸ determinÇðstica (`document-archive/clean/<tenantId>/<documentId>/<versionId>/<fileId>`,
  item 4 da Rodada 2) ƒ?" um segundo `CopyObject` do MESMO source para o MESMO destino Ç¸
  idempotente (mesmo conteÇ§do, nunca dados divergentes).
- `confirmFileScanClean` jÇ­ Ç¸ o Ç§nico portÇœo real (`apply-file-scan-result.ts`'s
  `isNonTerminalFileScanStatus` + `buildVersionedUpdate` OCC): o primeiro `transactWrite` a
  chegar transiciona `SCANNING ƒÅ' CLEAN`; o segundo recebe `isTransactionCanceled` e a chamada
  jÇ­ teria retornado antes por `isNonTerminalFileScanStatus(file.scanStatus)` ser falso na
  releitura fresca no topo do loop (`confirmFileScanClean`'s primeira linha de cada tentativa) ƒ?"
  vira `IGNORED_STALE`, comportamento que a funÇõÇœo jÇ­ tem, sem mudanÇõa de cÇüdigo.
- O `CopyObject` "perdedor" Ç¸ trabalho redundante e inofensivo, nunca uma condiÇõÇœo de corrida real
  ƒ?" nÇœo hÇ­ escrita dupla no DynamoDB nem dois eventos S3 diferentes competindo por
  significados diferentes.

Registro explÇðcito (nÇœo estava no design): cada branch verifica o objeto copiado (tamanho, e
quando disponÇðvel, checksum ƒ?" mesma disciplina de `advance-after-evidence.ts`) ANTES de chamar
`confirmFileScanClean`, entÇœo o segundo `CopyObject` redundante ainda passa pela mesma
verificaÇõÇœo antes de tentar confirmar ƒ?" nunca confirma um objeto corrompido sÇü porque o outro
branch jÇ­ promoveu.

## 2. Cerca de tenant ƒ?" adicionada explicitamente, nunca "herdada"

Corrigido: `applyFileScanResult`/`confirmFileScanClean` ganham um novo parÇ½metro obrigatÇürio
`tenantActiveCondition: TransactConditionCheck` (o MESMO `ConditionCheck` que
`processMalwareResult`/`processSubmissionMalwareResult` jÇ­ constroem hoje contra o item
`Tenant`) ƒ?" os dois novos branches passam essa condiÇõÇœo explicitamente na MESMA
`TransactWriteItems` que jÇ­ grava `DocumentFile`/`DocumentVersion`, nunca uma checagem separada
antes da transaÇõÇœo (que teria TOCTOU). Isto Ç¸ uma mudanÇõa de assinatura real das duas funÇõÇæes de
`document-archive/application/apply-file-scan-result.ts`, registrada aqui como parte do contrato
desta rodada, nÇœo um detalhe de implementaÇõÇœo a inventar depois.

## 3. Rollout ƒ?" ordem de ativaÇõÇœo definida, sem janela de eventos perdidos

Corrigido: a Rodada 2 permitia ativar o promoter e o starter em qualquer ordem, criando a janela
que o Codex apontou (arquivo chega a CLEAN, emite seu Ç§nico evento S3, starter ainda desligado,
extraÇõÇœo nunca dispara ƒ?" sem replay de eventos S3 histÇüricos). Ordem obrigatÇüria, registrada
como sequÇ¦ncia de deploy desta decisÇœo (nÇœo um detalhe de implementaÇõÇœo livre):

1. Deploy do starter novo com o flag `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED=true` PRIMEIRO
   ƒ?" nenhum arquivo `document-archive` chega a `CLEAN` ainda (promoter ainda desligado), entÇœo o
   starter fica ocioso, sem risco.
2. SÇü depois, deploy dos dois branches do promoter
   (`DOCUMENT_ARCHIVE_PROMOTION_ENABLED=true`). A partir deste ponto todo arquivo que chega a
   `CLEAN` jÇ­ tem um listener pronto ƒ?" nunca existe uma janela onde `CLEAN` acontece sem o
   starter escutando.
3. Nunca a ordem inversa. Esta sequÇ¦ncia Ç¸ o mecanismo que fecha a lacuna ƒ?" nÇœo backfill, nÇœo um
   segundo trigger durÇ­vel: a own ordenaÇõÇœo evita o problema por construÇõÇœo.

## 4. Destino real do outbox ƒ?" definido, seguindo o padrÇœo jÇ­ estabelecido (D-192)

Corrigido: `outbox.ts` confirma que nÇœo existe caminho genÇ¸rico EventBridge ƒ?" todo consumidor real
Ç¸ roteado explicitamente via `OutboxDestination` + `DispatchOutboxRelay` para uma fila SQS
especÇðfica (mesmo padrÇœo que `SQS_IMPORT_PARSE_V1`, D-192). Adoto o mesmo padrÇœo, sem inventar
mecanismo novo:

- Novo `OutboxDestination = "SQS_REQUIREMENT_EVIDENCE_REFRESH_V1"` em `outbox.ts`.
- Nova fila SQS `requirement-evidence-refresh-queue` + DLQ, mesmo mÇüdulo Terraform
  (`sqs-worker-queue`) jÇ­ usado por toda fila deste projeto ƒ?" nenhum mÇüdulo de infra novo.
- `DispatchOutboxRelay` ganha uma nova rota (`destination === "SQS_REQUIREMENT_EVIDENCE_REFRESH_V1"`
  ƒÅ' essa fila), mesmo mecanismo de roteamento que jÇ­ existe para as outras 5 entradas do enum.
- Novo handler `requirement-evidence-refresh-handler.ts` (SQS), IAM escopado sÇü a essa fila +
  leitura/escrita de `Requirement` ƒ?" mesmo padrÇœo de least-privilege jÇ­ convencionado
  (`AGENTS.md` ¶õ7).
- Payload do evento: `{tenantId, documentId, versionId}` ƒ?" **deliberadamente NUNCA inclui
  `validUntil`** (ver item 5, o worker sempre relÇ¦ o valor corrente, o evento Ç¸ sÇü um "acorde e
  recompute", nunca um portador de valor).

## 5. Worker de refresh ƒ?" releitura sempre fresca, nunca aplica o payload cegamente

Corrigido o risco de ordem/perda apontado pelo Codex: o worker NUNCA aplica `validUntil` do
payload do evento ƒ?" ele Ç¸ sÇü um sinal de "algo mudou, recompute". Em cada tentativa (loop OCC,
mesmo padrÇœo de `applyFileScanResult`):

1. RelÇ¦ `DocumentVersion` fresca pelo `{tenantId, documentId, versionId}` do evento ƒ?" se essa
   versÇœo nÇœo existe mais ou foi superseded, segue mesmo assim (uma versÇœo superseded ainda pode
   ser a evidÇ¦ncia histÇürica correta de um Requirement, `deriveRequirementStatus` jÇ­ lida com
   qualquer `DocumentVersionState`).
2. Via `GSI_EVIDENCE` (item 9 da Rodada 2), lista os `Requirement`s cujo item aponta para esse
   `versionId` ƒ?" mas ANTES de escrever qualquer um, relÇ¦ CADA `Requirement` individualmente e
   verifica `requirement.evidenceVersionId === versionId` no momento da escrita (nÇœo confia na
   entrada do Çðndice sozinha ƒ?" `GSI_EVIDENCE` Ç¸ sÇü o candidato, a condiÇõÇœo real Ç¸ a releitura do
   item base). Se o Requirement jÇ­ foi religado a OUTRA versÇœo ou desvinculado nesse meio-tempo,
   pula silenciosamente (`IGNORED_RELINKED` ƒ?" nunca sobrescreve um vÇðnculo mais novo).
3. Recalcula `deriveRequirementStatus`/`evidenceValidUntil` a partir da `DocumentVersion` FRESCA
   lida no passo 1, nunca do payload do evento ƒ?" isto elimina o problema de ordem: um evento
   antigo processado depois de um mais novo simplesmente recalcula o mesmo estado atual e Ç¸ um
   no-op (escreve o mesmo valor, ou falha a condiÇõÇœo de versÇœo OCC e tenta de novo ƒ?" nunca
   regride o Requirement para um `validUntil` antigo).
4. Escreve via `buildVersionedUpdate` OCC padrÇœo, um Requirement por vez ƒ?" nunca uma transaÇõÇœo
   com N itens.

Isto fecha os dois problemas apontados: eventos fora de ordem (o worker recomputa do estado atual,
nunca aplica um valor "carregado" pelo evento) e a lacuna de eventual consistency do GSI (mesmo
que o Çðndice ainda nÇœo reflita um link recÇ¸m-criado, o PRÇ"XIMO evento relacionado a essa mesma
`DocumentVersion` ƒ?" incluindo o disparado pelo prÇüprio `linkEvidence` que criou o link, ver item 9
ƒ?" vai reencontrÇ­-lo; e a rede de reparo do item 6 fecha o caso residual).

## 6. Rede de reparo autoritativa ƒ?" o reindex diÇ­rio passa a reler `DocumentVersion` ao vivo

Corrigido o achado central do Codex ("o reindex diÇ­rio usa o prÇüprio cache do Requirement, nÇœo Ç¸
uma rede de reparo real"): o worker diÇ­rio `requirement-reindex` (D-179/D-185) jÇ­ existe e jÇ­
escaneia todo Requirement `SATISFIED` com `evidenceValidUntil` definido
(`REQUIREMENT_REINDEX_WORK_TYPE`, `requirement.ts`). MudanÇõa mÇðnima e cirÇ§rgica: seu passo de
re-derivaÇõÇœo passa a reler a `DocumentVersion` viva (via `evidenceDocumentId`/`evidenceSeq` ou
`evidenceVersionId`, jÇ­ denormalizados no prÇüprio Requirement) em vez de comparar sÇü
`evidenceValidUntil` cacheado contra `now`. Isso transforma o worker diÇ­rio, que jÇ­ roda e jÇ­ Ç¸
aprovado, na rede de reparo autoritativa contra QUALQUER perda de evento/GSI/outbox ƒ?" o pior caso
de staleness deixa de ser "ilimitado" e passa a ser "atÇ¸ 24h", o mesmo bounded-staleness que
`requirement.ts`'s comentÇ­rio jÇ­ aceita para o caso original de tempo passando. Nenhum worker
novo para isto ƒ?" uma extensÇœo pontual de um worker jÇ­ existente e jÇ­ aprovado.

## 7. Cardinalidade ƒ?" nomeada com precisÇœo, nÇœo mais "3-way"

Corrigido: `confirmField` Ç¸ uma transaÇõÇœo de **3 agregados / 4 aÇõÇæes**
(`DocumentVersion` Update, `ExtractionRun` ConditionCheck, `ExtractedField` Update, `Outbox` Put
ƒ?" sÇü quando `validUntil` realmente muda, ver item 8's checagem de no-op). `rejectField` continua
**2 agregados / 2 aÇõÇæes** (`ExtractionRun` ConditionCheck, `ExtractedField` Update) ƒ?" nunca emite
outbox, nunca toca `DocumentVersion`. Ambas cardinalidades sÇœo FIXAS (nÇœo dependem de N
Requirements) ƒ?" a propriedade que resolve o achado 3.3 do Codex continua vÇ­lida, sÇü a contagem
estava imprecisa.

## 8. EquivalÇ¦ncia auto-confirm vs. manual ƒ?" planner Ç§nico, nÇœo sÇü o `set`

Corrigido: `buildDocumentVersionValidityUpdate` (Rodada 2) vira um planner mais completo,
`planDocumentVersionValidityEffect`, chamado por AMBOS os caminhos, que decide (nÇœo sÇü o `set`):

- **Estados elegÇðveis**: idÇ¦ntico para os dois caminhos ƒ?" `DocumentVersion.state` precisa estar
  em `RECEIVED | UNDER_REVIEW | ACCEPTED` (mesma lista do item 5 da Rodada 2); fora disso, retorna
  `{ effect: "SKIPPED_INELIGIBLE_STATE" }` para os dois caminhos igualmente.
- **No-op check**: se o `confirmedValue` (ou o valor auto-confirmado) produzir o MESMO
  `validUntil` que a `DocumentVersion` jÇ­ tem, retorna `{ effect: "NO_CHANGE" }` ƒ?" nem
  `DocumentVersion` nem outbox sÇœo escritos (evita bump de OCC e emissÇœo de evento por um valor
  idÇ¦ntico, para os dois caminhos).
- **`confirmedBy`/`confirmedAt`**: parÇ½metro explÇðcito do planner (`ctx.identity` no caminho
  manual, `"SYSTEM_AUTOCONFIRM"` no caminho automÇ­tico) ƒ?" nunca um default implÇðcito que um dos
  dois caminhos esqueÇõa de passar.
- **MÇ§ltiplos campos mapeando para `validUntil`**: `field-schema.ts` jÇ­ define, por
  `pipelineVersion`, qual `fieldName` Ç¸ o campo de validade ƒ?" Ç¸ uma invariante do schema que
  exatamente zero ou um campo de validade existe por pipeline, nunca um runtime concern; o
  planner recebe o `fieldName` jÇ­ resolvido pelo schema, nunca decide sozinho "qual campo Ç¸
  validade".
- **EmissÇœo do outbox exactly-once lÇügica**: o `Put` do outbox estÇ­ na MESMA `TransactWriteItems`
  que o `Update` de `DocumentVersion` ƒ?" atÇïmico por construÇõÇœo (mesma garantia que
  `AGENTS.md` ¶õ7 jÇ­ exige para todo evento crÇðtico), nunca uma segunda escrita separada em
  nenhum dos dois caminhos.

`doConfirmField` e `commitRunOutcome` chamam o MESMO `planDocumentVersionValidityEffect` e
aplicam o resultado dentro de sua prÇüpria `TransactWriteItems` jÇ­ existente ƒ?" nenhuma lÇügica de
decisÇœo duplicada entre os dois.

## 9. PRINCIPAL-only ƒ?" mantido como polÇðtica provisÇüria explÇðcita, com idempotÇ¦ncia de duplicata

Mantenho a decisÇœo (registrada como decisÇœo de produto REVISÇ?VEL, nÇœo verdade geral ƒ?" aceito o
enquadramento do Codex). Adiciono o que faltava: duplicatas do PRÇ"PRIO evento do arquivo
PRINCIPAL (reentrega S3/SQS) sÇœo absorvidas pela identidade de `ExtractionRun` jÇ­ ser chaveada
por `versionId` (item 7 da Rodada 2) atravÇ¸s de uma escrita condicional (`attribute_not_exists`)
em `startExtractionRun` ƒ?" uma segunda tentativa de criar o mesmo `ExtractionRun` falha a condiÇõÇœo
e Ç¸ tratada como sucesso idempotente (mesmo padrÇœo que toda escrita idempotente deste projeto jÇ­
usa, `occ.ts`'s builders). NÇœo Ç¸ mecanismo novo, Ç¸ a aplicaÇõÇœo da identidade jÇ­ corrigida na
Rodada 2.

## 10. Checklist E-014 ƒ?" reescrito em propriedades de resultado, nÇœo em conformidade com a soluÇõÇœo

Aceito o achado do Codex de que o checklist da Rodada 2 avaliava "usa minha soluÇõÇœo?" em vez do
resultado. Reescrito:

1. **(peso 25%) ConvergÇ¦ncia de `Requirement`/`validUntil` sob duplicaÇõÇœo, perda temporÇ­ria,
   reordenaÇõÇœo e relink concorrente ƒ?" bounded e demonstrÇ­vel, nÇœo "eventualmente, sem prazo".**
   Atende: existe um teto explÇðcito de staleness (o reindex diÇ­rio, item 6) independente de
   qualquer mecanismo de entrega em tempo real funcionar. NÇœo atende: qualquer design cuja Ç§nica
   rede de seguranÇõa seja "o evento chega".
2. **(peso 25%) Nenhuma extraÇõÇœo inicia a partir de um objeto S3 nÇœo autoritativo.** Atende: o
   starter sempre releitura o estado do agregado antes de agir, nunca confia sÇü na chave do
   evento. NÇœo atende: qualquer caminho que trate o evento S3 como fonte de verdade.
3. **(peso 20%) Cardinalidade de toda transaÇõÇœo Ç¸ fixa, independente de N (Requirements, arquivos,
   candidatos).** Atende: cada transaÇõÇœo nomeada tem uma contagem de agregados/aÇõÇæes constante.
   NÇœo atende: qualquer transaÇõÇœo cujo tamanho cresÇõa com dados do tenant.
4. **(peso 15%) PolÇðtica multi-arquivo Ç¸ uma decisÇœo de produto registrada, explÇðcita e revisÇ­vel
   ƒ?" nunca uma omissÇœo.** Atende: PRINCIPAL-only documentado como provisÇürio, com critÇ¸rio claro
   de quando revisar. NÇœo atende: ambiguidade sobre o que acontece com anexos.
5. **(peso 10%) Caminho automÇ­tico e caminho humano produzem exatamente o mesmo efeito
   observÇ­vel no agregado de negÇücio, por construÇõÇœo (planner Ç§nico), nÇœo por convenÇõÇœo.** Atende:
   um Ç§nico planner decide os dois. NÇœo atende: qualquer lÇügica de decisÇœo duplicada.
6. **(peso 5%) ProveniÇ¦ncia (`confirmedBy`/`confirmedAt`) sempre presente e nunca inferida.**
   Atende: campo obrigatÇürio do planner, nunca opcional.

## Escopo de infraestrutura desta rodada (registrado, nÇœo implementado)

Itens 2 (tenant fence), 4 (outbox/fila/handler novos), 6 (extensÇœo do reindex diÇ­rio) sÇœo mudanÇõas
de nÇðvel 5 cada (nova fila, novo destino de outbox, mudanÇõa de contrato de leitura de um worker jÇ­
aprovado) ƒ?" nenhuma delas nova em relaÇõÇœo ao que jÇ­ foi debatido, mas registro explicitamente que
a implementaÇõÇœo de cada uma Ç¸ trabalho de fase 3 (implementaÇõÇœo), nÇœo desta rodada de design.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round2-codex-critique.txt' -Tail 180; rg -n \"OutboxDestination|SQS_IMPORT_PARSE_V1|DispatchOutboxRelay|requirement-reindex|REQUIREMENT_REINDEX|startExtractionRun|attribute_not_exists|confirmFileScanClean|applyFileScanResult|buildVersionedUpdate|ConditionCheck\" src infra test" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 407ms:
- ƒ?ousa `buildDocumentVersionValidityUpdate`ƒ??.

Isso avalia conformidade com a prÇüpria proposta, nÇœo o resultado arquitetural. AlÇ¸m disso, a pesquisa externa citada sobre human-in-the-loop nÇœo sustenta diretamente identidade DynamoDB, promoter S3, GSI reverso ou entrega outbox.

A rÇ¸gua deve ser formulada em termos de propriedades verificÇ­veis: convergÇ¦ncia mesmo com duplicaÇõÇœo, perda temporÇ­ria, reorder e relink; cardinalidade limitada; nenhuma extraÇõÇœo de objeto nÇœo autoritativo; polÇðtica multi-arquivo explicitamente aprovada; equivalÇ¦ncia observÇ­vel entre auto e manual.

## O que deve mudar para a Rodada 3

1. Corrigir a afirmaÇõÇœo de que apenas um handler pode promover. Assumir concorrÇ¦ncia/duplicaÇõÇœo e especificar idempotÇ¦ncia completa de `CopyObject ƒÅ' verify ƒÅ' confirm`.

2. Definir uma cerca de tenant real para o novo branch, idealmente na mesma transaÇõÇœo de cada mutaÇõÇœo de `DocumentFile`/`DocumentVersion`, em vez de alegar heranÇõa dos workers antigos.

3. Especificar o rollout da OpÇõÇœo C para impedir arquivos `CLEAN` cujo evento ocorreu enquanto o starter estava desligado.

4. Definir o destino real do outbox: `OutboxDestination`, fila, DLQ, relay, schema, IAM e polÇðtica de redrive. O ƒ?odefault EventBridgeƒ?? nÇœo existe no cÇüdigo atual.

5. Exigir que o refresh worker releia a `DocumentVersion` corrente e o Requirement corrente em cada tentativa, verifique `evidenceVersionId`, e nunca aplique cegamente o `validUntil` do evento.

6. Definir proteÇõÇœo contra eventos fora de ordem, preferencialmente usando a versÇœo OCC/aggregateVersion da `DocumentVersion` e tratando eventos antigos como apenas um sinal para reler o estado atual.

7. Adicionar uma rede de reparo autoritativa. O reindex diÇ­rio atual nÇœo basta porque usa o cache do prÇüprio Requirement. Deve haver redrive/sweeper que releia a `DocumentVersion` ou outra garantia demonstrÇ­vel de convergÇ¦ncia limitada.

8. Corrigir a contagem das transaÇõÇæes: confirm com outbox nÇœo Ç¸ 3-way; documentar separadamente cardinalidade de aÇõÇæes e nÇ§mero de agregados.

9. Especificar a equivalÇ¦ncia completa entre auto-confirm e confirmaÇõÇœo humana, alÇ¸m da funÇõÇœo que produz o `set`: estados elegÇðveis, OCC, outbox, provenance e mÇ§ltiplos campos mapeÇ­veis.

10. Manter PRINCIPAL-only como polÇðtica provisÇüria explÇðcita, exigir idempotÇ¦ncia contra eventos duplicados do principal e registrar que anexos nÇœo sÇœo inspecionados nesta fase.

11. Reescrever o checklist E-014 em propriedades de resultado, incluindo entrega durÇ­vel, reorder, duplicaÇõÇœo, relink/unlink concorrente e reparo; nÇœo usar componentes especÇðficos da soluÇõÇœo como a prÇüpria definiÇõÇœo de aprovaÇõÇœo.

A revisÇœo saiu de um desenho estruturalmente inviÇ­vel para uma arquitetura plausÇðvel, mas ainda depende de um canal outbox inexistente e contÇ¸m duas alegaÇõÇæes de seguranÇõa incorretas: promoÇõÇœo exclusiva e cerca de tenant herdada.

Nota: 7.8/10
tokens used
59.002
A Rodada 2 corrige a direÇõÇœo arquitetural, mas ainda nÇœo estÇ­ pronta para implementaÇõÇœo. O principal avanÇõo Ç¸ abandonar o fan-out sÇðncrono e separar corretamente completude fÇðsica, trÇ­fego HTTP e autoridade futura. Os principais bloqueantes restantes estÇœo no promoter concorrente, na entrega real do outbox e na convergÇ¦ncia do worker sob eventos fora de ordem.

## AvaliaÇõÇœo

1. O novo enquadramento de autoridade estÇ­ correto

A separaÇõÇœo agora Ç¸ precisa e adequadamente limitada:

- O mÇüdulo antigo Ç¸ o Ç§nico pipeline fisicamente completo.
- Terraform demonstra implantaÇõÇœo/roteabilidade, nÇœo trÇ­fego efetivo.
- `document-archive` Ç¸ a autoridade conceitual futura por decisÇœo arquitetural anterior.

TambÇ¸m confirmei o achado novo: [upload-finalizer-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/upload-finalizer-handler.ts) reconhece apenas as chaves antigas de `document` e `subject`, assim como [malware-result-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/malware-result-handler.ts). Portanto, `document-archive` realmente nÇœo recebe nenhum dos dois sinais fÇðsicos necessÇ­rios.

A conclusÇœo de que o arquivo fica inicialmente em `PENDING_UPLOAD`, e nÇœo simplesmente em `SCANNING`, Ç¸ correta.

2. A OpÇõÇœo C Ç¸ conceitualmente sÇülida, mas o rollout ainda estÇ­ incompleto

Separar contrato, implementaÇõÇœo e ativaÇõÇœo Ç¸ a soluÇõÇœo correta. Entretanto, hÇ­ uma lacuna operacional:

- Se o promoter for ativado enquanto o starter novo continua desabilitado, arquivos podem chegar a `CLEAN`, emitir seu Ç§nico evento S3 e nunca iniciar extraÇõÇœo.
- Ligar o flag posteriormente nÇœo reproduz eventos S3 histÇüricos.

ƒ?oImplementar e testar independentementeƒ?? Ç¸ seguro; ƒ?oimplantar e deixar ativo independentementeƒ?? nÇœo Ç¸. A revisÇœo precisa definir uma destas alternativas:

- ativar primeiro o starter e depois o promoter;
- manter ambos os branches novos desabilitados atÇ¸ a ativaÇõÇœo coordenada;
- persistir um trigger durÇ­vel/outbox de extraÇõÇœo;
- ou executar backfill explÇðcito de arquivos `CLEAN` sem `ExtractionRun`.

Sem isso, a feature flag pode criar uma lacuna permanente, nÇœo apenas atraso transitÇürio.

3. Os terceiros branches sÇœo uma arquitetura plausÇðvel, mas a alegaÇõÇœo de exclusividade Ç¸ falsa

Reusar as duas filas e os dois handlers existentes Ç¸ melhor que introduzir uma fila mal definida. Retry e DLQ realmente podem ser herdados pelos novos branches se erros forem propagados como `batchItemFailures`.

PorÇ¸m, [apply-file-scan-result.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/apply-file-scan-result.ts) retorna `READY_TO_PROMOTE` sem fazer escrita alguma. Depois que as duas evidÇ¦ncias jÇ­ estÇœo persistidas:

- uma duplicata de qualquer evento pode retornar `READY_TO_PROMOTE`;
- upload-finalizer e malware-result podem ambos observar o estado consolidado;
- os dois podem executar `CopyObject`;
- somente a confirmaÇõÇœo DynamoDB subsequente Ç¸ protegida pela transiÇõÇœo terminal.

Portanto, ƒ?onunca os dois branches competindoƒ?? nÇœo Ç¸ sustentado pelo cÇüdigo. A promoÇõÇœo Ç¸, no mÇ­ximo, concorrente porÇ¸m potencialmente idempotente. Isso pode ser aceitÇ­vel, mas precisa ser desenhado como tal: chave de destino determinÇðstica, cÇüpia condicionada/verificada, comportamento com mÇ§ltiplas versÇæes S3 e tratamento explÇðcito de uma segunda cÇüpia apÇüs a primeira confirmaÇõÇœo.

A cerca de tenant tambÇ¸m foi afirmada sem base suficiente. `processMalwareResult` e os workers antigos terem sua prÇüpria checagem nÇœo faz com que um branch novo chamando diretamente `applyFileScanResult` a herde. Essa funÇõÇœo usa `store.transactWrite` e nÇœo contÇ¸m uma condiÇõÇœo de tenant `ACTIVE`. O novo branch precisa chamar explicitamente a cerca compartilhada ou incluÇð-la na transaÇõÇœo.

Deixar a limpeza da quarentena fora do escopo Ç¸ aceitÇ­vel como paridade temporÇ­ria, desde que registrada como dÇðvida operacional. NÇœo resolve retenÇõÇœo/custo, mas nÇœo Ç¸ bloqueante desta decisÇœo.

4. A convergÇ¦ncia assÇðncrona Ç¸ a direÇõÇœo correta, mas o worker ainda estÇ­ subespecificado

A mudanÇõa resolve os trÇ¦s defeitos estruturais da Rodada 1:

- existe agora access pattern reverso;
- nenhuma transaÇõÇœo depende de `N`;
- a phantom race de novos links nÇœo precisa ser fechada por uma transaÇõÇœo global, porque `linkEvidence` lÇ¦ a versÇœo corrente.

O comentÇ­rio em [requirement.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/domain/requirement.ts) realmente aceita bounded staleness para mudanÇõas posteriores da `DocumentVersion`. Portanto, consistÇ¦ncia assÇðncrona Ç¸ compatÇðvel com o modelo existente.

Contudo, surgem quatro requisitos ainda ausentes:

- Eventos podem chegar fora de ordem. O worker nÇœo pode aplicar `validUntil` do payload cegamente; deve reler a `DocumentVersion` corrente e verificar que o Requirement ainda aponta para aquele `versionId`.
- Cada retry OCC deve repetir tanto a leitura do Requirement quanto a validaÇõÇœo do vÇðnculo. Caso contrÇ­rio, um `unlinkEvidence` ou relink para outra versÇœo pode ser sobrescrito.
- GSI Ç¸ eventualmente consistente. Um evento processado antes de o ponteiro do Çðndice ficar visÇðvel pode nÇœo encontrar um Requirement que jÇ­ estava vinculado. O argumento de que um link posterior lÇ¦ o valor atual resolve links posteriores, mas nÇœo formaliza todos os casos prÇüximos Çÿ propagaÇõÇœo do Çðndice.
- O reindex diÇ­rio atual deriva a partir de `evidenceState`/`evidenceValidUntil` cacheados; ele nÇœo relÇ¦ a `DocumentVersion`. Logo, ele nÇœo Ç¸ automaticamente uma rede de reparo para um evento perdido ou um candidato omitido pelo GSI. Sem sweeper/redrive/reconciliaÇõÇœo autoritativa, a inconsistÇ¦ncia pode ser ilimitada, contrariando ƒ?oboundedƒ??.

HÇ­ ainda uma imprecisÇœo de cardinalidade: confirm nÇœo Ç¸ realmente ƒ?o3-wayƒ?? se tambÇ¸m grava o outbox; sÇœo quatro aÇõÇæes. O reject Ç¸ 2-way. O auto-confirm continua tendo cardinalidade proporcional ao nÇ§mero de `ExtractedField` criados, embora limitada pelo schema, e acrescentarÇ­ `DocumentVersion` e outbox.

5. A funÇõÇœo compartilhada Ç¸ uma correÇõÇœo real, mas insuficiente sozinha

[run-extraction-validation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-extraction-validation.ts) e [dynamodb-extracted-field-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/dynamodb-extracted-field-store.ts) confirmam que o auto-confirm jÇ­ grava o agregado de negÇücio dentro de `commitRunOutcome`. A Rodada 2 corrigiu corretamente o erro factual da proposta original.

Uma funÇõÇœo pura compartilhada elimina divergÇ¦ncia na traduÇõÇœo `fieldName + confirmedValue ƒÅ' mutaÇõÇœo de DocumentVersion`. Mas ela nÇœo garante, por si sÇü, equivalÇ¦ncia total entre os caminhos. Ambos tambÇ¸m precisam compartilhar ou provar equivalÇ¦ncia em:

- estados elegÇðveis da `DocumentVersion`;
- OCC e leitura da versÇœo corrente;
- emissÇœo exatamente uma vez lÇügica do outbox;
- `confirmedBy`/`confirmedAt`;
- tratamento de mÇ§ltiplos campos auto-confirmados que eventualmente mapeiem para `validUntil`;
- comportamento quando nÇœo hÇ­ mudanÇõa efetiva de valor.

A proposta deve definir a funÇõÇœo como planner de toda a mutaÇõÇœo relevante ƒ?" ou especificar builders adicionais compartilhados ƒ?" e nÇœo apenas do `set` de `validUntil`.

6. O outbox citado nÇœo entrega esse evento atualmente

[outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts) permite anexar atomicamente um registro Çÿ transaÇõÇœo. Essa parte funciona como assumido.

Mas o prÇüprio arquivo registra que o caminho genÇ¸rico para EventBridge nunca foi implementado. Os eventos reais dependem de destinos SQS explÇðcitos, e `OutboxDestination` nÇœo contÇ¸m um destino para `DocumentVersionValidityChanged`.

Assim, ƒ?oemitir no outbox e um worker consumirƒ?? nÇœo Ç¸ um contrato executÇ­vel com a infraestrutura atual. A Rodada 3 precisa definir:

- novo `OutboxDestination`;
- fila, DLQ e IAM;
- roteamento no relay;
- schema do envelope/evento;
- retry/redrive;
- mecanismo de reparo apÇüs DLQ ou omissÇœo eventual do GSI.

Esse Ç¸ um bloqueante central, porque sem isso Requirements nunca convergem.

7. PRINCIPAL-only Ç¸ representÇ­vel, mas a justificativa Ç¸ de produto

[document-file.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/domain/document-file.ts) contÇ¸m `role: "PRINCIPAL" | "ATTACHMENT"` e exige exatamente um principal por lote. Logo, a polÇðtica Ç¸ tecnicamente representÇ­vel e elimina mÇ§ltiplos candidatos normais por versÇœo.

Ela nÇœo elimina duplicatas do evento do prÇüprio principal, que ainda exigem identidade idempotente de `ExtractionRun`. TambÇ¸m Ç¸ uma decisÇœo de produto Type 1: ƒ?oanexos tipicamente sÇœo apoioƒ?? nÇœo Ç¸ evidÇ¦ncia suficiente de que nunca carregam a pÇ­gina com validade. Como polÇðtica provisÇüria, reversÇðvel e explicitamente limitada Çÿ fase atual, Ç¸ aceitÇ­vel; deve ser registrada como restriÇõÇœo funcional, nÇœo como verdade geral.

8. O checklist melhorou, mas ainda Ç¸ parcialmente circular

Os pesos agora cobrem os riscos corretos e somam 100%. PorÇ¸m, vÇ­rias Ç½ncoras definem a soluÇõÇœo proposta como condiÇõÇœo de sucesso:

- ƒ?oRequirement atualiza via worker assÇðncronoƒ??;
- ƒ?osÇü PRINCIPAL disparaƒ??;
- ƒ?ousa `buildDocumentVersionValidityUpdate`ƒ??.

Isso avalia conformidade com a prÇüpria proposta, nÇœo o resultado arquitetural. AlÇ¸m disso, a pesquisa externa citada sobre human-in-the-loop nÇœo sustenta diretamente identidade DynamoDB, promoter S3, GSI reverso ou entrega outbox.

A rÇ¸gua deve ser formulada em termos de propriedades verificÇ­veis: convergÇ¦ncia mesmo com duplicaÇõÇœo, perda temporÇ­ria, reorder e relink; cardinalidade limitada; nenhuma extraÇõÇœo de objeto nÇœo autoritativo; polÇðtica multi-arquivo explicitamente aprovada; equivalÇ¦ncia observÇ­vel entre auto e manual.

## O que deve mudar para a Rodada 3

1. Corrigir a afirmaÇõÇœo de que apenas um handler pode promover. Assumir concorrÇ¦ncia/duplicaÇõÇœo e especificar idempotÇ¦ncia completa de `CopyObject ƒÅ' verify ƒÅ' confirm`.

2. Definir uma cerca de tenant real para o novo branch, idealmente na mesma transaÇõÇœo de cada mutaÇõÇœo de `DocumentFile`/`DocumentVersion`, em vez de alegar heranÇõa dos workers antigos.

3. Especificar o rollout da OpÇõÇœo C para impedir arquivos `CLEAN` cujo evento ocorreu enquanto o starter estava desligado.

4. Definir o destino real do outbox: `OutboxDestination`, fila, DLQ, relay, schema, IAM e polÇðtica de redrive. O ƒ?odefault EventBridgeƒ?? nÇœo existe no cÇüdigo atual.

5. Exigir que o refresh worker releia a `DocumentVersion` corrente e o Requirement corrente em cada tentativa, verifique `evidenceVersionId`, e nunca aplique cegamente o `validUntil` do evento.

6. Definir proteÇõÇœo contra eventos fora de ordem, preferencialmente usando a versÇœo OCC/aggregateVersion da `DocumentVersion` e tratando eventos antigos como apenas um sinal para reler o estado atual.

7. Adicionar uma rede de reparo autoritativa. O reindex diÇ­rio atual nÇœo basta porque usa o cache do prÇüprio Requirement. Deve haver redrive/sweeper que releia a `DocumentVersion` ou outra garantia demonstrÇ­vel de convergÇ¦ncia limitada.

8. Corrigir a contagem das transaÇõÇæes: confirm com outbox nÇœo Ç¸ 3-way; documentar separadamente cardinalidade de aÇõÇæes e nÇ§mero de agregados.

9. Especificar a equivalÇ¦ncia completa entre auto-confirm e confirmaÇõÇœo humana, alÇ¸m da funÇõÇœo que produz o `set`: estados elegÇðveis, OCC, outbox, provenance e mÇ§ltiplos campos mapeÇ­veis.

10. Manter PRINCIPAL-only como polÇðtica provisÇüria explÇðcita, exigir idempotÇ¦ncia contra eventos duplicados do principal e registrar que anexos nÇœo sÇœo inspecionados nesta fase.

11. Reescrever o checklist E-014 em propriedades de resultado, incluindo entrega durÇ­vel, reorder, duplicaÇõÇœo, relink/unlink concorrente e reparo; nÇœo usar componentes especÇðficos da soluÇõÇœo como a prÇüpria definiÇõÇœo de aprovaÇõÇœo.

A revisÇœo saiu de um desenho estruturalmente inviÇ­vel para uma arquitetura plausÇðvel, mas ainda depende de um canal outbox inexistente e contÇ¸m duas alegaÇõÇæes de seguranÇõa incorretas: promoÇõÇœo exclusiva e cerca de tenant herdada.

Nota: 7.8/10
test\architecture\tenant-fence-boundary.test.ts:95:  it("BYPASS ATTEMPT 2: a fake module calling `new TransactWriteCommand(...)` without routing through the lifecycle ConditionCheck is caught", () => {
test\architecture\tenant-fence-boundary.test.ts:101:        "// skipping executeTenantBusinessMutation entirely - so no lifecycle ConditionCheck",
test\architecture\tenant-fence-boundary.test.ts:104:        "// from here regardless of whether the payload happens to include a ConditionCheck.",
src\workers\malware-result\result-processor.ts:4:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\malware-result\result-processor.ts:65:      entries: [{ Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: doc.version, set: { malwareEvidence } }) }],
src\workers\upload-slot-reconciliation\reconciliation.ts:4:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\upload-slot-reconciliation\reconciliation.ts:66:        Update: buildVersionedUpdate({
src\workers\upload-slot-reconciliation\reconciliation.ts:93:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: slot.tenantId, expectedVersion: doc.version, set: { status: "TIMEOUT" } }) },
test\unit\dispatch-outbox-relay\relay.test.ts:58:describe("DispatchOutboxRelay / OutboxSweeperReminderDispatch core logic (M3.5)", () => {
src\workers\dispatch-outbox-relay\relay.ts:2: * DispatchOutboxRelay + OutboxSweeperReminderDispatch core logic (M3.5,
src\workers\dispatch-outbox-relay\relay.ts:19:import { outboxRecordCorrelationId, type OutboxRecord, type OutboxDestination } from "../../shared/outbox/outbox.js";
src\workers\dispatch-outbox-relay\relay.ts:22:export const SQS_REMINDER_DISPATCH_V1: OutboxDestination = "SQS_REMINDER_DISPATCH_V1";
src\workers\dispatch-outbox-relay\relay.ts:23:export const SQS_NOTIFICATION_EMAIL_V1: OutboxDestination = "SQS_NOTIFICATION_EMAIL_V1";
src\workers\dispatch-outbox-relay\relay.ts:34:  Record<OutboxDestination, (payload: Record<string, unknown>, correlationId: string, tenantId: string, eventType: string) => Promise<void>>
src\workers\dispatch-outbox-relay\relay.ts:108:/** DispatchOutboxRelay / NotificationEmailOutboxRelay entrypoint logic: one call per
src\workers\dispatch-outbox-relay\relay.ts:125:  destinations?: OutboxDestination[];
src\workers\dispatch-outbox-relay\relay.ts:143:  const destinations = deps.destinations ?? (Object.keys(deps.senders) as OutboxDestination[]);
test\architecture\security-audit-observability-coverage.test.ts:55:  { file: "src/workers/requirement-reindex/dynamodb-candidate-source.ts", component: "requirement-reindex", lambdaModule: "requirement_reindex_handler" },
src\workers\upload-finalizer\finalizer.ts:3:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\upload-finalizer\finalizer.ts:86:          Update: buildVersionedUpdate({
test\integration\document-archive-lifecycle.test.ts:34:import { buildVersionedUpdate } from "../../src/shared/dynamodb/occ.js";
test\integration\document-archive-lifecycle.test.ts:77: * has no route yet for the real scan pipeline (applyFileScanResult/confirmFileScanClean, D-165
test\integration\document-archive-lifecycle.test.ts:89:    { Update: buildVersionedUpdate({ tableName: "MainTable", key: { PK: principalFile.PK, SK: principalFile.SK }, tenantId, expectedVersion: principalFile.version, set: { scanStatus: "CLEAN" } }) },
test\integration\document-archive-lifecycle.test.ts:90:    { Update: buildVersionedUpdate({ tableName: "MainTable", key: { PK: version.PK, SK: version.SK }, tenantId, expectedVersion: version.version, set: { pendingFileScans: 0 } }) },
test\integration\document-archive-lifecycle.test.ts:120:    // fences on TenantLifecycleRecord.status=ACTIVE AND the ConditionCheck against a real
test\unit\document-archive\apply-file-scan-result.test.ts:3:import { applyFileScanResult, applyFileScanTimeout, confirmFileScanClean } from "../../../src/modules/document-archive/application/apply-file-scan-result.js";
test\unit\document-archive\apply-file-scan-result.test.ts:80:describe("applyFileScanResult â€” D-163 Â§1/Â§5 symmetric evidence correlation + terminal transition", () => {
test\unit\document-archive\apply-file-scan-result.test.ts:84:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:97:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:108:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:118:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:138:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:156:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:165:    expect(version.pendingFileScans).toBe(1); // untouched until confirmFileScanClean.
test\unit\document-archive\apply-file-scan-result.test.ts:171:    const outcome = await applyFileScanResult(
test\unit\document-archive\apply-file-scan-result.test.ts:181:describe("confirmFileScanClean â€” PROMOTE confirmation step, called only after the caller's own copy+verify", () => {
test\unit\document-archive\apply-file-scan-result.test.ts:185:    const outcome = await confirmFileScanClean(
test\unit\document-archive\apply-file-scan-result.test.ts:202:    const outcome = await confirmFileScanClean(
src\workers\invitation-purge\purge.ts:10: * claim/delete uses a `TransactWriteItems` `ConditionCheck` on the owning tenant's
src\workers\invitation-purge\purge.ts:14: * `ConditionCheck` specifically fails, this worker increments `maintenanceAttemptCount` and pushes
src\workers\invitation-purge\purge.ts:146:          ConditionCheck: {
src\workers\delivery-record-purge\purge.ts:13: * `ConditionCheck` on the owning tenant's `TenantLifecycleRecord.status = ACTIVE` in the SAME
src\workers\delivery-record-purge\purge.ts:28: * when the tenant-ACTIVE `ConditionCheck` specifically fails, this worker increments
src\workers\delivery-record-purge\purge.ts:131:          ConditionCheck: {
src\workers\invitation-purge\dynamodb-candidate-source.ts:83:            return { ConditionCheck: entry.ConditionCheck };
src\workers\transient-purge\purge.ts:15: * before any write, and the actual claim/delete uses a `TransactWriteItems` `ConditionCheck` on
src\workers\transient-purge\purge.ts:30: * the tenant-ACTIVE `ConditionCheck` specifically fails, this worker increments
src\workers\transient-purge\purge.ts:176:          ConditionCheck: {
src\workers\delivery-record-purge\dynamodb-candidate-source.ts:96:            return { ConditionCheck: entry.ConditionCheck };
test\unit\bff\bff-auth-service.test.ts:283:  // MutaÃ§Ã£o: gravar o cap num item novo sem `attribute_not_exists` (ou usar
test\unit\bff\bff-auth-service.test.ts:284:  // buildVersionedUpdate, cuja condiÃ§Ã£o reservada exige tenantId - GlobalUser nÃ£o tem) faria
src\workers\delivery-record-purge\candidate-source.ts:47: * build the tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\delivery-record-purge\candidate-source.ts:66:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
src\workers\transient-purge\dynamodb-candidate-source.ts:150:            return { ConditionCheck: entry.ConditionCheck };
src\workers\invitation-purge\candidate-source.ts:33: * tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\invitation-purge\candidate-source.ts:51:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
src\workers\core-user-data-purge\purge.ts:10: * before any write, and the actual claim/delete uses a `TransactWriteItems` `ConditionCheck` on
src\workers\core-user-data-purge\purge.ts:22: * when the tenant-ACTIVE `ConditionCheck` specifically fails, this worker increments
src\workers\core-user-data-purge\purge.ts:121:          ConditionCheck: {
src\workers\core-user-data-purge\dynamodb-candidate-source.ts:94:            return { ConditionCheck: entry.ConditionCheck };
src\workers\document-request-recurrence\materializer.ts:6: * (same pragmatic collapse `requirement-reindex/reindex.ts` already makes for a single daily
src\workers\document-request-recurrence\materializer.ts:10: * tradeoff as `requirement-reindex/reindex.ts`'s `scanSatisfiedRequirements` â€” this module has
src\workers\document-request-recurrence\materializer.ts:19: * invocation) â€” same posture as `requirement-reindex/reindex.ts`.
src\workers\document-request-recurrence\materializer.ts:43: * `requirement-reindex/reindex.ts`'s `MAX_PAGES`: bounds a single invocation against a
src\workers\core-user-data-purge\candidate-source.ts:45: * build the tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\core-user-data-purge\candidate-source.ts:64:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
test\unit\document-archive\requirement-service.test.ts:8:import { buildVersionedUpdate } from "../../../src/shared/dynamodb/occ.js";
test\unit\document-archive\requirement-service.test.ts:67:  // zero, standing in for the real terminal scan transition (applyFileScanResult, D-165).
test\unit\document-archive\requirement-service.test.ts:69:    { Update: buildVersionedUpdate({ tableName: "test-table", key: documentFileKey(TENANT, doc.documentId, draft.seq, principal.fileId), tenantId: TENANT, expectedVersion: principal.version, set: { scanStatus: "CLEAN" } }) },
test\unit\document-archive\requirement-service.test.ts:70:    { Update: buildVersionedUpdate({ tableName: "test-table", key: { PK: sealed.PK, SK: sealed.SK }, tenantId: TENANT, expectedVersion: sealed.version, set: { pendingFileScans: 0 } }) },
test\unit\document-archive\requirement-service.test.ts:83:    const { buildVersionedUpdate } = await import("../../../src/shared/dynamodb/occ.js");
test\unit\document-archive\requirement-service.test.ts:86:      { Update: buildVersionedUpdate({ tableName: "test-table", key: { PK: accepted.PK, SK: accepted.SK }, tenantId: TENANT, expectedVersion: accepted.version, set: { validUntil } }) },
src\workers\transient-purge\candidate-source.ts:54: * build the tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\transient-purge\candidate-source.ts:73:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
infra\tests\stack.tftest.hcl:78:  # DispatchOutboxRelay's third capability document (DynamoDB Streams read) is built from
infra\tests\stack.tftest.hcl:88:    error_message = "DispatchOutboxRelay's table-access policy must NOT reference GSI3"
infra\tests\stack.tftest.hcl:92:    error_message = "DispatchOutboxRelay's queue-send policy must NOT reference GSI3"
infra\tests\stack.tftest.hcl:190:    error_message = "DispatchOutboxRelay's table-access policy must NOT reference GSI6"
infra\tests\stack.tftest.hcl:194:    error_message = "DispatchOutboxRelay's queue-send policy must NOT reference GSI6"
infra\tests\stack.tftest.hcl:222:  # DispatchOutboxRelay above: the other capability documents in these functions' lists embed
infra\tests\stack.tftest.hcl:497:  # targets base-table items (ConditionCheck/Update/Delete), never a GSI directly.
infra\tests\stack.tftest.hcl:599:      strcontains(p, "WORK#REQUIREMENT_REINDEX") && strcontains(p, "DLQ#REQUIREMENT_REINDEX")
infra\tests\stack.tftest.hcl:631:      ]) : strcontains(p, "REQUIREMENT_REINDEX")
infra\tests\stack.tftest.hcl:633:    error_message = "No previously migrated worker may reference REQUIREMENT_REINDEX's namespace"
infra\tests\stack.tftest.hcl:667:      strcontains(p, "MEMBERSHIP_PURGE") || strcontains(p, "INVITATION_PURGE") || strcontains(p, "DOCUMENT_FILE_RECONCILIATION") || strcontains(p, "REQUIREMENT_REINDEX")
infra\tests\stack.tftest.hcl:669:    error_message = "QuotaTelemetryPurgeWorker must NOT reference MEMBERSHIP_PURGE's, INVITATION_PURGE's, DOCUMENT_FILE_RECONCILIATION's, or REQUIREMENT_REINDEX's namespace"
infra\tests\stack.tftest.hcl:716:      strcontains(p, "MEMBERSHIP_PURGE") || strcontains(p, "INVITATION_PURGE") || strcontains(p, "DOCUMENT_FILE_RECONCILIATION") || strcontains(p, "REQUIREMENT_REINDEX") || strcontains(p, "QUOTA_TELEMETRY")
infra\tests\stack.tftest.hcl:718:    error_message = "SecurityAuditPurgeWorker must NOT reference MEMBERSHIP_PURGE's, INVITATION_PURGE's, DOCUMENT_FILE_RECONCILIATION's, REQUIREMENT_REINDEX's, or QUOTA_TELEMETRY's namespace"
infra\tests\stack.tftest.hcl:766:      strcontains(p, "MEMBERSHIP_PURGE") || strcontains(p, "INVITATION_PURGE") || strcontains(p, "DOCUMENT_FILE_RECONCILIATION") || strcontains(p, "REQUIREMENT_REINDEX") || strcontains(p, "QUOTA_TELEMETRY") || strcontains(p, "SECURITY_AUDIT")
infra\tests\stack.tftest.hcl:768:    error_message = "TransientPurgeWorker must NOT reference MEMBERSHIP_PURGE's, INVITATION_PURGE's, DOCUMENT_FILE_RECONCILIATION's, REQUIREMENT_REINDEX's, QUOTA_TELEMETRY's, or SECURITY_AUDIT's namespace"
infra\tests\stack.tftest.hcl:817:      strcontains(p, "MEMBERSHIP_PURGE") || strcontains(p, "INVITATION_PURGE") || strcontains(p, "DOCUMENT_FILE_RECONCILIATION") || strcontains(p, "REQUIREMENT_REINDEX") || strcontains(p, "QUOTA_TELEMETRY") || strcontains(p, "SECURITY_AUDIT") || strcontains(p, "TRANSIENT")
infra\tests\stack.tftest.hcl:819:    error_message = "DeliveryRecordPurgeWorker must NOT reference MEMBERSHIP_PURGE's, INVITATION_PURGE's, DOCUMENT_FILE_RECONCILIATION's, REQUIREMENT_REINDEX's, QUOTA_TELEMETRY's, SECURITY_AUDIT's, or TRANSIENT's namespace"
infra\tests\stack.tftest.hcl:871:      strcontains(p, "MEMBERSHIP_PURGE") || strcontains(p, "INVITATION_PURGE") || strcontains(p, "DOCUMENT_FILE_RECONCILIATION") || strcontains(p, "REQUIREMENT_REINDEX") || strcontains(p, "QUOTA_TELEMETRY") || strcontains(p, "SECURITY_AUDIT") || strcontains(p, "TRANSIENT") || strcontains(p, "DELIVERY_RECORD")
infra\tests\stack.tftest.hcl:873:    error_message = "CoreUserDataPurgeWorker must NOT reference MEMBERSHIP_PURGE's, INVITATION_PURGE's, DOCUMENT_FILE_RECONCILIATION's, REQUIREMENT_REINDEX's, QUOTA_TELEMETRY's, SECURITY_AUDIT's, TRANSIENT's, or DELIVERY_RECORD's namespace"
infra\tests\stack.tftest.hcl:931:    error_message = "DispatchOutboxRelay reservedConcurrentExecutions must be 2"
test\unit\document-archive\in-memory-store.ts:40:/** D-173 item 3: `createDocument()`'s transactional `ConditionCheck` requires a real
test\unit\document-archive\in-memory-store.ts:74: * each clause either `attribute_exists(PK|SK)`, `attribute_not_exists(PK|SK)`, a bare
test\unit\document-archive\in-memory-store.ts:100:    // Named-attribute attribute_exists/attribute_not_exists â€” distinct from the bare
test\unit\document-archive\in-memory-store.ts:102:    // group (only D-163's `reserveFiles()` fence does: "attribute_not_exists(#sealed) OR
test\unit\document-archive\in-memory-store.ts:104:    const notExists = /^\s*attribute_not_exists\((#\w+)\)\s*$/.exec(clause);
test\unit\document-archive\in-memory-store.ts:123:    if (clause === "attribute_not_exists(PK)" || clause === "attribute_not_exists(SK)") {
test\unit\document-archive\in-memory-store.ts:219:      } else if ("ConditionCheck" in entry) {
test\unit\document-archive\in-memory-store.ts:220:        const check = entry.ConditionCheck;
test\unit\document-archive\in-memory-store.ts:270:      // ConditionCheck never mutates.
test\unit\document-archive\in-memory-store.ts:306:   * matching in one page. `requirement-reindex` used to have a sibling fake here
src\workers\document-purge\purge.ts:34: *    `attribute_not_exists(GSI6PK) OR GSI6PK <> "WORKSTATE#PURGE_CLAIMED"` in its own
src\workers\document-purge\purge.ts:41:  buildVersionedUpdate,
src\workers\document-purge\purge.ts:124:        Update: buildVersionedUpdate({
src\workers\document-purge\purge.ts:135:            { expression: "attribute_not_exists(#legalHold) OR #legalHold = :false", names: { "#legalHold": "legalHold" }, values: { ":false": false } },
src\workers\document-purge\purge.ts:220:          Update: buildVersionedUpdate({
src\workers\document-purge\purge.ts:235:        Update: buildVersionedUpdate({
test\unit\document-archive\guest-document-access-service.test.ts:11:import { buildVersionedUpdate } from "../../../src/shared/dynamodb/occ.js";
test\unit\document-archive\guest-document-access-service.test.ts:264:   * byte-identical to before this change: no ConditionCheck runs, requirementId is never
test\unit\document-archive\guest-document-access-service.test.ts:266:   * (always add the ConditionCheck) makes this test fail, since requirementId never names a real
test\unit\document-archive\guest-document-access-service.test.ts:283:    /** Mutation check: removing the `documentTypeSupplied` guard's ConditionCheck entirely (or
test\unit\document-archive\guest-document-access-service.test.ts:317:     * ConditionCheck runs inside the same TransactWriteItems as the Document Put, never a
test\unit\document-archive\guest-document-access-service.test.ts:326:          Update: buildVersionedUpdate({
test\unit\document\in-memory-store.ts:52:   * W3-07 (evidence-mutation worker fencing): now also evaluates `ConditionCheck` entries (the
test\unit\document\in-memory-store.ts:71:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
test\unit\document\in-memory-store.ts:75:      } else if ("ConditionCheck" in entry) {
test\unit\document\in-memory-store.ts:76:        const check = entry.ConditionCheck;
test\unit\document-archive\document-type-service.test.ts:107:  // NOT `attribute_not_exists`-conditioned (or omitted from the transaction entirely), both
test\unit\document-archive\document-file-reconciliation.test.ts:4:import { applyFileScanTimeout, confirmFileScanClean } from "../../../src/modules/document-archive/application/apply-file-scan-result.js";
test\unit\document-archive\document-file-reconciliation.test.ts:165:    const confirmOutcome = await confirmFileScanClean(
test\unit\document-archive\document-archive-service.test.ts:8:import { buildVersionedUpdate } from "../../../src/shared/dynamodb/occ.js";
test\unit\document-archive\document-archive-service.test.ts:80: * zero, standing in for the real terminal scan transition (`applyFileScanResult`/
test\unit\document-archive\document-archive-service.test.ts:81: * `confirmFileScanClean`, D-165) â€” most fixtures in this suite drive the lifecycle all the way
test\unit\document-archive\document-archive-service.test.ts:97:      Update: buildVersionedUpdate({
test\unit\document-archive\document-archive-service.test.ts:106:      Update: buildVersionedUpdate({
test\unit\document-archive\document-archive-service.test.ts:163:  /** D-173 Â§4/item 3: proves the new `ConditionCheck` genuinely blocks `createDocument()` when
test\unit\document-archive\document-archive-service.test.ts:171:        Update: buildVersionedUpdate({
test\unit\document-archive\document-archive-service.test.ts:327:        Update: buildVersionedUpdate({
test\unit\document-archive\document-archive-service.test.ts:466:          Update: buildVersionedUpdate({
test\unit\document-archive\document-archive-service.test.ts:475:          Update: buildVersionedUpdate({
test\unit\document-archive\document-archive-service.test.ts:524:    expect(entries[0]).toHaveProperty("ConditionCheck");
test\unit\document-archive\document-archive-service.test.ts:525:    expect(entries[1]).toHaveProperty("ConditionCheck");
test\unit\document-archive\document-archive-service.test.ts:527:    expect((entries[0] as { ConditionCheck: { Key: { PK: string } } }).ConditionCheck.Key.PK).toBe(`TENANT#${BASE.tenantId}#DOCTYPE#${BASE.documentTypeId}`);
test\unit\document-archive\document-archive-service.test.ts:528:    expect((entries[1] as { ConditionCheck: { Key: { PK: string } } }).ConditionCheck.Key.PK).toBe(`TENANT#${BASE.tenantId}#SUBJECT#${BASE.subjectId}`);
test\unit\document-archive\document-archive-service.test.ts:593:    expect(entries[2]).toHaveProperty("ConditionCheck");
test\unit\document-archive\document-archive-service.test.ts:594:    // entries[1] is the RequirementNamePointer whose attribute_not_exists condition is what
test\unit\document-archive\document-archive-service.test.ts:602:    const fence = entries[2] as { ConditionCheck: { Key: { PK: string } } };
test\unit\document-archive\document-archive-service.test.ts:603:    expect(fence.ConditionCheck.Key.PK).toBe(`TENANT#${BASE.tenantId}#SUBJECT#${BASE.subjectId}`);
src\workers\document-file-reconciliation\candidate-source.ts:18: * OCC retry loop + exact-pointer `ConditionCheck`) â€” this port therefore only needs `queryDue()`,
src\shared\errors\app-error.ts:292: * a tenant-scoped business mutation's ConditionCheck against `TenantLifecycleRecord.status =
src\shared\errors\app-error.ts:373: * `AcceptInvitation` call failed its transactional guard (`attribute_not_exists(consumedAt) AND
src\shared\errors\app-error.ts:476: * thrown when `createDocument()`'s transactional `ConditionCheck` against
src\shared\errors\app-error.ts:504: * `ConditionCheck` (`status = ACTIVE AND version = <expected>`) fails inside `applyTemplate`'s
src\shared\errors\app-error.ts:518: * `ConditionCheck` (`attribute_exists(PK) AND status = ACTIVE`) failed.
src\shared\errors\app-error.ts:534: * `Put` (`attribute_not_exists`) loses the race â€” the tenant already has a different
test\unit\organization\in-memory-store.ts:13: * Operadores de condiÃ§Ã£o suportados: `attribute_not_exists(x)`, `attribute_exists(x)`,
test\unit\organization\in-memory-store.ts:57:    const notExists = /^attribute_not_exists\(([^)]+)\)$/.exec(trimmed);
src\workers\document-chasing-dispatch\dispatch.ts:27:import { buildVersionedUpdate, buildVersionedCreate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\document-chasing-dispatch\dispatch.ts:83:  // coberta pela checagem de status (CLAIMED) e pela condiÃ§Ã£o OCC do prÃ³prio buildVersionedUpdate
src\workers\document-chasing-dispatch\dispatch.ts:115:          Update: buildVersionedUpdate({
src\workers\document-chasing-dispatch\dispatch.ts:160:        Update: buildVersionedUpdate({
src\workers\document-chasing-dispatch\dispatch.ts:243:      Update: buildVersionedUpdate({
src\workers\document-chasing-dispatch\dispatch.ts:253:      Update: buildVersionedUpdate({
src\shared\dynamodb\tenant-purge-scan.ts:100:   * (`attribute_not_exists(PK)` half), and (B5 fix) re-asserts the stored `tenantId` attribute
src\shared\dynamodb\tenant-purge-scan.ts:114:          ConditionExpression: "attribute_not_exists(PK) OR tenantId = :expectedTenantId",
test\unit\import\in-memory-store.ts:131:  /** W3-07 (D-070 chunk 8/N): extended with a `ConditionCheck` branch (previously silently a
test\unit\import\in-memory-store.ts:132:   * no-op, since the loop only matched "Put"/"Update" - the lifecycle fence's ConditionCheck
test\unit\import\in-memory-store.ts:146:        if (entry.Put.ConditionExpression?.includes("attribute_not_exists(PK)") && this.items.has(key)) {
test\unit\import\in-memory-store.ts:167:          if (clause.startsWith("attribute_exists(") || clause.startsWith("attribute_not_exists(")) continue;
test\unit\import\in-memory-store.ts:174:      } else if ("ConditionCheck" in entry) {
test\unit\import\in-memory-store.ts:175:        const check = entry.ConditionCheck;
test\unit\import\in-memory-store.ts:210:        // buildVersionedUpdate convention) - transitionIdempotencyStatus's update has no notion
src\shared\delivery-record-gsi8.ts:44: * back out for the tenant-ACTIVE `ConditionCheck`). */
src\shared\dynamodb\occ.ts:35:   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
src\shared\dynamodb\occ.ts:50:   * `buildVersionedUpdate()` below never hits this (it always maps at least #version/
src\shared\dynamodb\occ.ts:61:export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
src\shared\dynamodb\occ.ts:134: * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
src\shared\dynamodb\occ.ts:135: * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
src\shared\dynamodb\occ.ts:141:    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\shared\dynamodb\occ.ts:155: * `buildVersionedUpdate`'s `extraConditions`.
src\shared\dynamodb\occ.ts:191:export interface DynamoConditionCheckCommandInput {
src\shared\dynamodb\occ.ts:199:export interface TransactConditionCheckEntry {
src\shared\dynamodb\occ.ts:200:  ConditionCheck: DynamoConditionCheckCommandInput;
src\shared\dynamodb\occ.ts:218: * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
src\shared\dynamodb\occ.ts:220: * `buildVersionedUpdate` - see that function's docs.
src\shared\dynamodb\occ.ts:283:export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;
src\shared\dynamodb\occ.ts:286: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src\shared\dynamodb\occ.ts:294:export function buildVersionConditionCheck(input: {
src\shared\dynamodb\occ.ts:300:}): TransactConditionCheckEntry {
src\shared\dynamodb\occ.ts:316:    ConditionCheck: {
src\shared\dynamodb\occ.ts:327: * Sibling of `buildVersionConditionCheck` for facts that have no version to pin - e.g.
src\shared\dynamodb\occ.ts:334:export function buildExistenceConditionCheck(input: {
src\shared\dynamodb\occ.ts:338:}): TransactConditionCheckEntry {
src\shared\dynamodb\occ.ts:354:    ConditionCheck: {
src\shared\dynamodb\occ.ts:400: * `buildVersionedUpdate`, whose reserved condition requires `tenantId` and is therefore unusable
src\shared\dynamodb\occ.ts:405: * `buildVersionedUpdate`, not a generalization of this narrow helper.
src\shared\dynamodb\occ.ts:412:    ConditionExpression: "attribute_not_exists(#attr)",
test\unit\import\import-service.test.ts:288:    it("submitImportMapping on a valid mapping transitions AWAITING_MAPPING->PARSING and dispatches SQS_IMPORT_PARSE_V1 in the SAME transaction", async () => {
test\unit\import\import-service.test.ts:308:      expect(outboxRecords[0]?.["destination"]).toBe("SQS_IMPORT_PARSE_V1");
src\shared\security-audit-gsi8.ts:41: * back out for the tenant-ACTIVE `ConditionCheck`). */
src\shared\tenant-lifecycle\tenant-business-mutation.ts:7: * `ConditionCheck` against `TenantLifecycleRecord.status = ACTIVE` to the caller's own
src\shared\tenant-lifecycle\tenant-business-mutation.ts:9: * `shared/dynamodb/occ.ts`'s existing `buildExistenceConditionCheck` builder â€” never a
src\shared\tenant-lifecycle\tenant-business-mutation.ts:28:import { buildExistenceConditionCheck, isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";
src\shared\tenant-lifecycle\tenant-business-mutation.ts:48: *    builder enforces it); `buildVersionedUpdate`/`buildVersionedDelete` read it from
src\shared\tenant-lifecycle\tenant-business-mutation.ts:54: *    targets the table the fence's own `ConditionCheck` is written against) and its physical
src\shared\tenant-lifecycle\tenant-business-mutation.ts:55: *    key's `PK` (`Item.PK` for Put, `Key.PK` for Update/Delete/ConditionCheck) â€” every tenant-
src\shared\tenant-lifecycle\tenant-business-mutation.ts:84: * `ConditionCheck` entries participate in the `PK`-based check (their `Key.PK` is inspected same
src\shared\tenant-lifecycle\tenant-business-mutation.ts:85: * as Update/Delete) but not the declared-`tenantId` check â€” `buildExistenceConditionCheck`/
src\shared\tenant-lifecycle\tenant-business-mutation.ts:86: * `buildVersionConditionCheck` have no `tenantId` convention, and forcing one would be a false
src\shared\tenant-lifecycle\tenant-business-mutation.ts:102:  return entry.ConditionCheck.TableName;
src\shared\tenant-lifecycle\tenant-business-mutation.ts:106:  const pk = "Put" in entry ? (entry.Put.Item as { PK?: unknown }).PK : "Update" in entry ? entry.Update.Key.PK : "Delete" in entry ? entry.Delete.Key.PK : entry.ConditionCheck.Key.PK;
src\shared\tenant-lifecycle\tenant-business-mutation.ts:126:    // ConditionCheck: no declared-tenantId convention to check (see doc above) - falls through
src\shared\tenant-lifecycle\tenant-business-mutation.ts:149:  /** The caller's own TransactWriteItems entries (Put/Update/Delete/ConditionCheck) built
src\shared\tenant-lifecycle\tenant-business-mutation.ts:157: * Commits `entries` plus a `ConditionCheck` asserting `TenantLifecycleRecord.status =
src\shared\tenant-lifecycle\tenant-business-mutation.ts:183:  const fence = buildExistenceConditionCheck({
src\shared\outbox\relay-store.ts:2: * Narrow port for DispatchOutboxRelay and OutboxSweeperReminderDispatch (M3.5,
src\shared\outbox\relay-store.ts:7:import type { OutboxDestination, OutboxRecord } from "./outbox.js";
src\shared\outbox\relay-store.ts:11:   * `ConditionExpression: attribute_not_exists(leaseOwner) OR leaseExpiresAt < :now`.
src\shared\outbox\relay-store.ts:17:  listPendingReminderDispatch(input: { destination: OutboxDestination; olderThan: string; pageSize?: number }): Promise<OutboxRecord[]>;
src\shared\tenant-lifecycle\system-mutation.ts:55:  buildVersionedUpdate,
src\shared\tenant-lifecycle\system-mutation.ts:121:       * `attribute_not_exists(PK) OR begins_with(PK, "TENANT#<tenantId>#")` â€” deleting an
src\shared\tenant-lifecycle\system-mutation.ts:131:       * codebase, which routes through `buildVersionedUpdate`/`buildVersionedDelete`): a purge
src\shared\tenant-lifecycle\system-mutation.ts:186:          // of the version check buildVersionedUpdate already does â€” belt-and-suspenders
src\shared\tenant-lifecycle\system-mutation.ts:244:          Update: buildVersionedUpdate({
src\shared\tenant-lifecycle\system-mutation.ts:264:            // `#set0`/`:set0` naming deliberately mirrors what `buildVersionedUpdate` generates
src\shared\tenant-lifecycle\system-mutation.ts:341:              "attribute_not_exists(PK) OR begins_with(PK, :purgeTenantPrefix) OR tenantId = :purgeTenantId OR organizationId = :purgeTenantId",
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:94: * commita em DynamoDB sem ConditionCheck de TenantLifecycleRecord.status = ACTIVE").
test\unit\organization\accept-invitation.test.ts:131:  // (deixar sÃ³ `attribute_not_exists(consumedAt)`) faria esta asserÃ§Ã£o nÃ£o lanÃ§ar - fecha a
test\unit\organization\accept-invitation.test.ts:148:  // MutaÃ§Ã£o: remover a `ConditionExpression: "attribute_not_exists(PK) OR #status = :removed"`
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:2: * DispatchOutboxRelay and OutboxSweeperReminderDispatch composition roots - the two
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:25:          ConditionExpression: "attribute_not_exists(leaseOwner) OR leaseExpiresAt < :now",
src\shared\outbox\outbox.ts:16: * `DispatchOutboxRelay` (new, -> SQS ReminderDispatchQueue) never both claim the same
src\shared\outbox\outbox.ts:21:export type OutboxDestination =
src\shared\outbox\outbox.ts:31:   * destination-routed through DispatchOutboxRelay; this destination follows the same
src\shared\outbox\outbox.ts:41:  | "SQS_IMPORT_PARSE_V1";
src\shared\outbox\outbox.ts:64:  destination?: OutboxDestination;
src\shared\outbox\outbox.ts:80:export function buildOutboxRecord(event: DomainEvent, destination?: OutboxDestination): OutboxRecord {
src\shared\outbox\outbox.ts:134:  destination?: OutboxDestination,
src\shared\outbox\outbox.ts:141:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
test\unit\occ.test.ts:5:  buildVersionedUpdate,
test\unit\occ.test.ts:9:describe("buildVersionedUpdate", () => {
test\unit\occ.test.ts:11:    const cmd = buildVersionedUpdate({
test\unit\occ.test.ts:26:    const cmd = buildVersionedUpdate({
test\unit\occ.test.ts:40:    const cmd = buildVersionedUpdate({
test\unit\occ.test.ts:47:    // buildVersionedUpdate() always populates ExpressionAttributeNames (at minimum
test\unit\occ.test.ts:56:    const cmd = buildVersionedUpdate({
test\unit\occ.test.ts:63:        { expression: "attribute_not_exists(#legalHold) OR #legalHold = :false", names: { "#legalHold": "legalHold" }, values: { ":false": false } },
test\unit\occ.test.ts:69:        " AND (attribute_not_exists(#legalHold) OR #legalHold = :false) AND (purgeAfter <= :purgeCutoff)",
test\unit\occ.test.ts:78:      buildVersionedUpdate({
test\unit\occ.test.ts:89:      buildVersionedUpdate({
test\unit\occ.test.ts:102:  it("builds the attribute_not_exists ConditionExpression for first creation", () => {
test\unit\occ.test.ts:108:    expect(cmd.ConditionExpression).toBe("attribute_not_exists(PK) AND attribute_not_exists(SK)");
src\runtime\aws\composition\reminder.ts:111:      ...(importParseQueueUrl ? { SQS_IMPORT_PARSE_V1: send(importParseQueueUrl) } : {}),
test\unit\identity\in-memory-store.ts:11: * module's condition actually uses: `attribute_not_exists`, `begins_with`, a plain `field = :value`
test\unit\identity\in-memory-store.ts:20:    const notExistsMatch = atom.match(/^attribute_not_exists\(([^)]+)\)$/);
test\unit\identity\in-memory-store.ts:43: * required by the identity module (attribute_not_exists(PK) for putIfAbsent). Used by
test\unit\identity\in-memory-store.ts:86:   * lane): occ.ts's `attribute_not_exists(PK) AND attribute_not_exists(SK)` creation
test\unit\identity\in-memory-store.ts:88:   * existence+equality condition (ConditionCheck, from buildExistenceConditionCheck - the
test\unit\identity\in-memory-store.ts:106:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
test\unit\identity\in-memory-store.ts:117:        // ConditionCheck/Update branches below use. Known-shape fake (see file header) -
test\unit\identity\in-memory-store.ts:131:      } else if ("ConditionCheck" in entry) {
test\unit\identity\in-memory-store.ts:132:        const check = entry.ConditionCheck;
test\unit\identity\in-memory-store.ts:167:          // extraConditions (occ.ts's buildVersionedUpdate) add extra #cN/:cN or caller-named
test\unit\identity\in-memory-store.ts:171:          // ConditionCheck branch above - required for that fence to actually be exercised by
test\unit\identity\in-memory-store.ts:200:    // Pass 2: apply (Put/Update only - ConditionCheck never mutates).
test\unit\identity\in-memory-store.ts:250:   * attribute_not_exists condition), and check presence. */
src\shared\observability\security-audit.ts:43: * slice 3, 3rd of 9), "requirement-reindex" (D-179/D-185, slice 4 of 9), and
src\shared\observability\security-audit.ts:57:  | "requirement-reindex"
src\shared\idempotency\idempotency.ts:4: * `PutItem attribute_not_exists(PK)` as required by data-model.md Â§4.
test\unit\extraction\start-extraction-run.test.ts:2:import { startExtractionRun, DocumentNotCleanYetError } from "../../../src/modules/extraction/application/start-extraction-run.js";
test\unit\extraction\start-extraction-run.test.ts:43:    throw new Error("not used by startExtractionRun");
test\unit\extraction\start-extraction-run.test.ts:52:    throw new Error("not used by startExtractionRun");
test\unit\extraction\start-extraction-run.test.ts:65:describe("startExtractionRun", () => {
test\unit\extraction\start-extraction-run.test.ts:69:    const outcome = await startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts:81:      startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts:92:    const outcome = await startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts:123:    await startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts:138:    const first = await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts:139:    const second = await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts:159:    await expect(startExtractionRun(deps, input)).rejects.toThrow("transient SFN throttling");
test\unit\extraction\start-extraction-run.test.ts:162:    const outcome = await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts:172:    await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts:175:    const outcome = await startExtractionRun(deps2, input);
src\runtime\aws\composition\document-archive.ts:30:  // shared with `applyFileScanResult`/`confirmFileScanClean`, which do use it).
test\unit\tenant-lifecycle.test.ts:10:import { buildVersionedCreate, buildVersionedUpdate, buildVersionedDelete, type TransactWriteEntry } from "../../src/shared/dynamodb/occ.js";
test\unit\tenant-lifecycle.test.ts:196:        Update: buildVersionedUpdate({
test\unit\tenant-lifecycle.test.ts:287:        Update: buildVersionedUpdate({
test\unit\extraction\run-extraction-validation.test.ts:283:      // The real adapter reports an already-existing ExtractedField row (attribute_not_exists
test\unit\workers\transient-purge-fakes.ts:112:      if ("ConditionCheck" in entry) {
test\unit\workers\transient-purge-fakes.ts:113:        const c = entry.ConditionCheck;
test\unit\workers\transient-purge-fakes.ts:148:      // ConditionCheck entries never mutate anything.
test\unit\system-mutation.test.ts:50:    expect(record?.version).toBe(2); // buildVersionedUpdate increments version atomically
src\runtime\aws\handlers\import-parse-handler.ts:31:/** D-192 slice 9: the SQS_IMPORT_PARSE_V1 envelope (`import-service.ts#submitImportMapping`'s
src\runtime\aws\handlers\import-parse-handler.ts:56:            logger.info("import-parse outcome (SQS_IMPORT_PARSE_V1 trigger)", { jobId: message.jobId, outcome });
src\runtime\aws\handlers\import-parse-handler.ts:63:          logger.error("import-parse malformed message (neither S3 event nor SQS_IMPORT_PARSE_V1 trigger)", { messageId: record.messageId });
test\unit\subject\subject-service.test.ts:160:    // then interleave their transactWrite commits - the in-memory store's attribute_not_exists
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
test\unit\notification\in-memory-store.ts:29:   * W3-07 (D-067, SES fence): now also evaluates `ConditionCheck` entries (the
test\unit\notification\in-memory-store.ts:34:   * actually exercised by tests instead of silently no-op'd (a ConditionCheck-blind fake would
test\unit\notification\in-memory-store.ts:44:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
test\unit\notification\in-memory-store.ts:48:      } else if ("ConditionCheck" in entry) {
test\unit\notification\in-memory-store.ts:49:        const check = entry.ConditionCheck;
test\unit\workers\membership-purge-fakes.ts:99:      if ("ConditionCheck" in entry) {
test\unit\workers\membership-purge-fakes.ts:100:        const c = entry.ConditionCheck;
test\unit\workers\membership-purge-fakes.ts:135:      // ConditionCheck entries never mutate anything.
test\unit\workers\delivery-record-purge-fakes.ts:104:      if ("ConditionCheck" in entry) {
test\unit\workers\delivery-record-purge-fakes.ts:105:        const c = entry.ConditionCheck;
test\unit\workers\delivery-record-purge-fakes.ts:140:      // ConditionCheck entries never mutate anything.
test\unit\extraction\dynamodb-extracted-field-store.test.ts:21:  ConditionCheck?: { Key: { PK: string; SK: string } };
test\unit\extraction\dynamodb-extracted-field-store.test.ts:71:  it("emits field Put + run Update + document ConditionCheck only, when there is no item update", async () => {
test\unit\expiration\item-watch-service.test.ts:126:    // ConditionCheck in the SAME TransactWriteItems as the mutation rejects it.
src\runtime\aws\handlers\extraction-starter-handler.ts:8:import { startExtractionRun } from "../../../modules/extraction/application/start-extraction-run.js";
src\runtime\aws\handlers\extraction-starter-handler.ts:56:          const outcome = await startExtractionRun(deps, {
test\unit\subject\in-memory-store.ts:60:   * evaluates ConditionCheck entries (the lifecycle fence) and threads a per-entry Code through
test\unit\subject\in-memory-store.ts:70:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
test\unit\subject\in-memory-store.ts:74:      } else if ("ConditionCheck" in entry) {
test\unit\subject\in-memory-store.ts:75:        const check = entry.ConditionCheck;
test\unit\expiration\in-memory-store.ts:52: * attribute_not_exists(PK) AND attribute_not_exists(SK) creation condition, and
test\unit\expiration\in-memory-store.ts:101:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
test\unit\expiration\in-memory-store.ts:130:      } else if ("ConditionCheck" in entry) {
test\unit\expiration\in-memory-store.ts:132:        // (buildExistenceConditionCheck's "attribute_exists(PK) AND #c0 = :c0 [...]" shape) -
test\unit\expiration\in-memory-store.ts:134:        const check = entry.ConditionCheck;
test\unit\workers\invitation-purge-fakes.ts:100:      if ("ConditionCheck" in entry) {
test\unit\workers\invitation-purge-fakes.ts:101:        const c = entry.ConditionCheck;
test\unit\workers\invitation-purge-fakes.ts:136:      // ConditionCheck entries never mutate anything.
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:2: * D-179 slice 4 GSI8 backfill script coverage (requirement-reindex) â€” mirrors
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:9:import { decodeKey, encodeKey, parseArgs, processPage } from "../../../scripts/backfill-gsi8-requirement-reindex.js";
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:36:describe("backfill-gsi8-requirement-reindex: parseArgs", () => {
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:53:describe("backfill-gsi8-requirement-reindex: encodeKey/decodeKey round-trip", () => {
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:66:describe("backfill-gsi8-requirement-reindex: processPage", () => {
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:95:    expect(writes[0]!.gsi8pk).toBe("WORK#REQUIREMENT_REINDEX");
test\unit\workers\backfill-gsi8-requirement-reindex.test.ts:111:    const requirement = makeRequirement({ GSI8PK: "WORK#REQUIREMENT_REINDEX", GSI8SK: "2026-12-31T00:00:00.000Z#TENANT#t1#REQUIREMENT#req-1" });
test\unit\workers\core-user-data-purge-fakes.ts:105:      if ("ConditionCheck" in entry) {
test\unit\workers\core-user-data-purge-fakes.ts:106:        const c = entry.ConditionCheck;
test\unit\workers\core-user-data-purge-fakes.ts:141:      // ConditionCheck entries never mutate anything.
test\unit\workers\document-purge.test.ts:2:import { buildVersionedUpdate, isTransactionCanceled } from "../../../src/shared/dynamodb/occ.js";
test\unit\workers\document-purge.test.ts:199:      Update: buildVersionedUpdate({
test\unit\workers\document-purge.test.ts:205:        extraConditions: [{ expression: "attribute_not_exists(GSI6PK) OR GSI6PK <> :purgeClaimed", values: { ":purgeClaimed": GSI6PK_PURGE_CLAIMED } }],
test\unit\workers\tenant-purge\purge-tenant.test.ts:194:    // attribute_not_exists(PK) match (never-existed key) is the idempotent no-op path, not a
src\runtime\aws\handlers\transient-purge-handler.ts:7: * D-179/D-188 (GSI8 migration, 7th slice): the tenant-ACTIVE fence is now a `ConditionCheck` inside
test\unit\workers\delivery-record-purge-pointer-creation.test.ts:21:import { buildVersionedUpdate } from "../../../src/shared/dynamodb/occ.js";
test\unit\workers\delivery-record-purge-pointer-creation.test.ts:85:        Update: buildVersionedUpdate({
test\unit\workers\document-purge-fakes.ts:8: * `purge.ts` builds via `buildVersionedUpdate`/`buildVersionedDelete` â€” not a stand-in that only
test\unit\workers\document-purge-fakes.ts:10: * exactly the operators this design's conditions use: `attribute_exists`/`attribute_not_exists`,
test\unit\workers\document-purge-fakes.ts:22:    const notExistsMatch = atom.match(/^attribute_not_exists\(([^)]+)\)$/);
test\unit\workers\document-purge-fakes.ts:110:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
src\runtime\aws\handlers\document-request-recurrence-handler.ts:3: * `event.detail`" contract as `requirement-reindex-handler.ts`/`reminder-reconciliation-handler.ts`
src\runtime\aws\handlers\document-request-recurrence-handler.ts:28:  // requirement-reindex-handler.ts) â€” new correlationId per invocation.
test\unit\workers\security-audit-purge-fakes.ts:104:      if ("ConditionCheck" in entry) {
test\unit\workers\security-audit-purge-fakes.ts:105:        const c = entry.ConditionCheck;
test\unit\workers\security-audit-purge-fakes.ts:140:      // ConditionCheck entries never mutate anything.
test\unit\workers\quota-telemetry-purge-fakes.ts:98:      if ("ConditionCheck" in entry) {
test\unit\workers\quota-telemetry-purge-fakes.ts:99:        const c = entry.ConditionCheck;
test\unit\workers\quota-telemetry-purge-fakes.ts:134:      // ConditionCheck entries never mutate anything.
test\unit\workers\membership-purge.test.ts:245:  // failing the atomic ConditionCheck every run. MutaÃ§Ã£o: removing the MAX_ATTEMPTS gate (or the
infra\modules\sqs-worker-queue\outputs.tf:22:# DispatchOutboxRelay/OutboxSweeperReminderDispatch's roles (relay/sweeper capability
test\unit\shared\dynamodb\tenant-purge-scan.test.ts:77:    expect(sent[0]!.input.ConditionExpression).toBe("attribute_not_exists(PK) OR tenantId = :expectedTenantId");
src\workers\submission-malware-result\result-processor.ts:3:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\submission-malware-result\result-processor.ts:53:      entries: [{ Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { malwareEvidence } }) }],
test\unit\workers\requirement-reindex.test.ts:2:import { runRequirementReindex } from "../../../src/workers/requirement-reindex/reindex.js";
test\unit\workers\requirement-reindex.test.ts:5:import type { RequirementGsi8Candidate, RequirementGsi8Page, RequirementReindexCandidateSource } from "../../../src/workers/requirement-reindex/candidate-source.js";
test\unit\workers\requirement-reindex.test.ts:6:import { buildVersionedUpdate, type EntityKey } from "../../../src/shared/dynamodb/occ.js";
test\unit\workers\requirement-reindex.test.ts:147:    const relink = buildVersionedUpdate({
src\runtime\aws\handlers\document-file-reconciliation-handler.ts:18:// requirement-reindex-handler.ts).
infra\main.tf:260:# consumer of this table (DispatchOutboxRelay, and M4's NotificationRouter/
infra\main.tf:585:# BLOCKER-B - consumed by ReminderMaterializationTrigger, fed by the same DispatchOutboxRelay/
infra\main.tf:588:# codebase - see outbox.ts's OutboxDestination comment).
infra\main.tf:1737:# --- ImportParseDispatchQueue: SQS_IMPORT_PARSE_V1, fed by dispatch_outbox_relay/outbox_sweeper,
infra\main.tf:2964:# src/workers/requirement-reindex/reindex.ts's doc comment. Migrated off the base-table Scan onto
infra\main.tf:2966:# scoped to WORK#REQUIREMENT_REINDEX/DLQ#REQUIREMENT_REINDEX, same pattern as
infra\main.tf:2971:  function_name         = "${local.name_prefix}-requirement-reindex-handler"
infra\main.tf:2972:  handler_name          = "requirement-reindex-handler"
infra\main.tf:2973:  source_dir            = "${local.dist_dir}/requirement-reindex-handler"
infra\main.tf:3018:  name                         = "${local.name_prefix}-requirement-reindex"
infra\main.tf:3645:  schedule_expression          = "cron(30 4 * * ? *)" # daily 04:30 UTC, after requirement-reindex (04:00)
src\workers\submission-finalizer\finalizer.ts:5:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\submission-finalizer\finalizer.ts:69:      entries: [{ Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: "SCANNING", uploadEvidence } }) }],
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:1:/** Pure(ish) per-batch processing logic for DispatchOutboxRelay, split out of
src\workers\security-audit-purge\purge.ts:10: * D-185's requirement-reindex/document-file-reconciliation): candidates now come from a `Query`
src\workers\security-audit-purge\purge.ts:13: * before any write, and the actual claim/delete uses a `TransactWriteItems` `ConditionCheck` on
src\workers\security-audit-purge\purge.ts:36: * quota-telemetry-purge): when the tenant-ACTIVE `ConditionCheck` specifically fails, this worker
src\workers\security-audit-purge\purge.ts:137:          ConditionCheck: {
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:1:/** Real handler for DispatchOutboxRelay (DynamoDB Streams NEW_IMAGE), replacing the 501
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:24:// OutboxDestination comment), so this is the only real delivery mechanism available.
src\runtime\aws\handlers\requirement-reindex-handler.ts:8:import { DynamoDbRequirementReindexCandidateSource } from "../../../workers/requirement-reindex/dynamodb-candidate-source.js";
src\runtime\aws\handlers\requirement-reindex-handler.ts:9:import { runRequirementReindex } from "../../../workers/requirement-reindex/reindex.js";
src\runtime\aws\handlers\requirement-reindex-handler.ts:20:const logger = new SecureLogger({ baseContext: { service: "requirement-reindex" } });
src\runtime\aws\handlers\requirement-reindex-handler.ts:35:  logger.info("requirement-reindex complete", { scheduledTime: event.scheduledTime, ...result });
src\workers\security-audit-purge\dynamodb-candidate-source.ts:133:            return { ConditionCheck: entry.ConditionCheck };
infra\modules\lambda-function\variables.tf:60:    DispatchOutboxRelay=2, OutboxSweeperReminderDispatch=2). -1 (the aws_lambda_function
test\unit\reminder\reminder-materializer.test.ts:138:      await store.putIfAbsent(v2); // policy row must exist for the ConditionCheck fence
src\workers\security-audit-purge\candidate-source.ts:52: * build the tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\security-audit-purge\candidate-source.ts:71:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
test\unit\reminder\reminder-materialization-trigger.test.ts:95:    // TenantBusinessMutation, whose ConditionCheck is appended to the SAME entries array
src\runtime\aws\handlers\core-user-data-purge-handler.ts:4: * `event.detail`" contract as `requirement-reindex-handler.ts`. Wired to real infra (Lambda
src\runtime\aws\handlers\core-user-data-purge-handler.ts:24:  // requirement-reindex-handler.ts) â€” new correlationId per invocation.
src\workers\requirement-reindex\reindex.ts:24:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\requirement-reindex\reindex.ts:90:      const update = buildVersionedUpdate({
test\unit\reminder\reconciliation.test.ts:17:import { buildVersionedUpdate } from "../../../src/shared/dynamodb/occ.js";
test\unit\reminder\reconciliation.test.ts:300:      // real claim transaction (occ.ts's buildVersionedUpdate SET path).
test\unit\reminder\reconciliation.test.ts:303:          Update: buildVersionedUpdate({
src\workers\requirement-reindex\dynamodb-candidate-source.ts:6: * `WORK#REQUIREMENT_REINDEX`/`DLQ#REQUIREMENT_REINDEX`, `infra/modules/dynamo-table/main.tf`) â€”
src\workers\requirement-reindex\dynamodb-candidate-source.ts:16:const GSI8PK_REQUIREMENT_REINDEX = "WORK#REQUIREMENT_REINDEX";
src\workers\requirement-reindex\dynamodb-candidate-source.ts:23:  if (!match || !match[1] || !match[2]) throw new Error(`Malformed base PK for requirement-reindex: ${pk}`);
src\workers\requirement-reindex\dynamodb-candidate-source.ts:30:  if (!match || !match[1]) throw new Error(`Malformed base SK for requirement-reindex: ${sk}`);
src\workers\requirement-reindex\dynamodb-candidate-source.ts:47:          ExpressionAttributeValues: { ":pk": GSI8PK_REQUIREMENT_REINDEX, ":before": input.before },
src\workers\requirement-reindex\dynamodb-candidate-source.ts:58:      auditGlobalIndexAccess({ indexName: "GSI8", operation: "Query", component: "requirement-reindex", pageCount: 1, resultCount: items.length });
src\workers\requirement-reindex\dynamodb-candidate-source.ts:62:        auditGlobalIndexAccessDenied({ indexName: "GSI8", operation: "Query", component: "requirement-reindex", awsErrorCode: "AccessDeniedException" });
test\unit\reminder\in-memory-store.ts:18: * from the freshness-fence ConditionChecks losing a race against a concurrent change. */
test\unit\reminder\in-memory-store.ts:49:        if (entry.Put.ConditionExpression.includes("attribute_not_exists(PK)") && exists) {
test\unit\reminder\in-memory-store.ts:52:      } else if ("ConditionCheck" in entry) {
test\unit\reminder\in-memory-store.ts:53:        const existing = this.items.get(this.k(entry.ConditionCheck.Key));
test\unit\reminder\in-memory-store.ts:54:        const values = entry.ConditionCheck.ExpressionAttributeValues ?? {};
test\unit\reminder\in-memory-store.ts:55:        const names = entry.ConditionCheck.ExpressionAttributeNames ?? {};
test\unit\reminder\in-memory-store.ts:60:          throw { name: "TransactionCanceledException", message: "ConditionalCheckFailed on ConditionCheck", CancellationReasons: cancellationReasons(entries, index) };
test\unit\reminder\in-memory-store.ts:82:      if ("ConditionCheck" in entry) {
src\runtime\aws\handlers\outbox-sweeper-handler.ts:70:    SQS_IMPORT_PARSE_V1: send(importParseQueueUrl),
src\runtime\aws\handlers\invitation-purge-handler.ts:6: * tenant-ACTIVE check is now a `ConditionCheck` inside the worker's own claim transaction. Wired to
src\workers\requirement-reindex\candidate-source.ts:7: * no other caller ever used it) with a `Query` against GSI8 (`GSI8PK=WORK#REQUIREMENT_REINDEX`,
src\workers\requirement-reindex\candidate-source.ts:36:  /** `Query GSI8PK = "WORK#REQUIREMENT_REINDEX" AND GSI8SK < :before`, ordered by due date.
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:2: * generalized relay logic as DispatchOutboxRelay - see
src\workers\reminder-reconciliation\reconciliation.ts:21:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\reminder-reconciliation\reconciliation.ts:89:          Update: buildVersionedUpdate({
src\workers\reminder-reconciliation\reconciliation.ts:172:                Update: buildVersionedUpdate({
src\workers\reminder-reconciliation\reconciliation.ts:192:            Update: buildVersionedUpdate({
test\unit\reminder\dispatch.test.ts:21:import { buildVersionedUpdate, type EntityKey } from "../../../src/shared/dynamodb/occ.js";
test\unit\reminder\dispatch.test.ts:100:      Update: buildVersionedUpdate({
src\workers\reminder-producer\producer.ts:30:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\reminder-producer\producer.ts:226:            // DispatchOutboxRelay (DynamoDB Streams) publishes it to SQS, a sweeper recovers
src\workers\reminder-producer\producer.ts:247:                Update: buildVersionedUpdate({
test\unit\outbox.test.ts:53:    expect(tx[1].Put.ConditionExpression).toBe("attribute_not_exists(PK) AND attribute_not_exists(SK)");
src\modules\expiration\ports\expiration-store.ts:41:  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
src\workers\reminder-dispatch\dispatch.ts:19:import { buildVersionConditionCheck, buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src\workers\reminder-dispatch\dispatch.ts:115:          Update: buildVersionedUpdate({
src\workers\reminder-dispatch\dispatch.ts:180:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\workers\reminder-dispatch\dispatch.ts:184:      Update: buildVersionedUpdate({
src\workers\reminder-dispatch\dispatch.ts:197:    // longer current by the time this transaction is durable. Re-asserted as ConditionChecks
src\workers\reminder-dispatch\dispatch.ts:200:    buildVersionConditionCheck({ tableName: deps.tableName, key: itemKey(tenantId, itemId), expectedVersion: itemVersion, extra: { status: "ACTIVE" } }),
src\workers\reminder-dispatch\dispatch.ts:201:    buildVersionConditionCheck({
src\workers\reminder-dispatch\dispatch.ts:227:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\expiration\persistence\dynamodb-expiration-store.ts:4: * builders (buildVersionedCreate/buildVersionedUpdate) and shared/outbox/outbox.ts's
src\modules\expiration\persistence\dynamodb-expiration-store.ts:36:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\subject\domain\audit-event.ts:108:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\workers\quota-telemetry-purge\purge.ts:11: * any write, and the actual claim/delete uses a `TransactWriteItems` `ConditionCheck` on the
src\workers\quota-telemetry-purge\purge.ts:21: * tenant-ACTIVE `ConditionCheck` specifically fails, this worker increments
src\workers\quota-telemetry-purge\purge.ts:128:          ConditionCheck: {
src\modules\reminder\persistence\dynamodb-reminder-store.ts:31:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\workers\membership-purge\purge.ts:12: * before any write, and the actual claim/delete uses a `TransactWriteItems` `ConditionCheck` on
src\workers\membership-purge\purge.ts:16: * Poison-record handling (D-179 Â§8): when the tenant-ACTIVE `ConditionCheck` specifically fails
src\workers\membership-purge\purge.ts:156:          ConditionCheck: {
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:88:            return { ConditionCheck: entry.ConditionCheck };
infra\modules\dynamo-table\main.tf:229:    requirement_reindex          = "REQUIREMENT_REINDEX"
infra\modules\dynamo-table\main.tf:252:      "dynamodb:ConditionCheckItem",
infra\modules\dynamo-table\main.tf:333:# `tenant_facing_read_write` grants Get/Put/Update/Delete/ConditionCheckItem but never
infra\modules\dynamo-table\main.tf:337:# always targets the base table's own items (ConditionCheck/Delete/Update), never a GSI directly.
infra\modules\reminder-observability\main.tf:14:    DispatchOutboxRelay           = var.dispatch_outbox_relay_function_name
src\workers\quota-telemetry-purge\candidate-source.ts:32: * tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\quota-telemetry-purge\candidate-source.ts:50:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
src\workers\membership-purge\dynamodb-candidate-source.ts:83:            return { ConditionCheck: entry.ConditionCheck };
src\workers\membership-purge\dynamodb-candidate-source.ts:90:      // getCancellationReasonCodes() itself to tell "a specific ConditionCheck lost the race,
infra\modules\reminder-observability\variables.tf:22:  description = "Name of the DispatchOutboxRelay Lambda."
src\workers\membership-purge\candidate-source.ts:34: * tenant-ACTIVE `ConditionCheck` without a second read). */
src\workers\membership-purge\candidate-source.ts:53:  /** Real `TransactWriteItems` â€” used both for the claim/delete (ConditionCheck + Delete) and for
src\modules\expiration\application\item-watch-service.ts:11:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\expiration\application\item-watch-service.ts:75:        { Update: buildVersionedUpdate({ tableName: this.tableName, key, tenantId: ctx.tenant.tenantId, expectedVersion: current.version, set: { status: "ACTIVE" } }) },
src\modules\expiration\application\item-watch-service.ts:105:      { Update: buildVersionedUpdate({ tableName: this.tableName, key, tenantId: ctx.tenant.tenantId, expectedVersion: existing.version, set: { status: "REMOVED" } }) },
infra\modules\reminder-observability\tests\reminder_observability.tftest.hcl:49:    condition     = aws_cloudwatch_metric_alarm.function_errors["DispatchOutboxRelay"].dimensions["FunctionName"] == "dispatch-outbox-relay"
infra\modules\reminder-observability\tests\reminder_observability.tftest.hcl:50:    error_message = "DispatchOutboxRelay alarm must watch the dispatch-outbox-relay function"
infra\modules\reminder-observability\tests\reminder_observability.tftest.hcl:76:    condition     = strcontains(aws_cloudwatch_metric_alarm.function_errors["DispatchOutboxRelay"].alarm_description, "DispatchOutboxRelay")
infra\modules\reminder-observability\tests\reminder_observability.tftest.hcl:77:    error_message = "DispatchOutboxRelay alarm description must mention DispatchOutboxRelay by name"
src\modules\expiration\application\expiration-service.ts:19:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\expiration\application\expiration-service.ts:312:        Update: buildVersionedUpdate({
src\modules\expiration\application\expiration-service.ts:523:        Update: buildVersionedUpdate({
src\modules\expiration\application\expiration-service.ts:566:    // (item created, copy silently dropped). No item-existence ConditionCheck is needed the
src\modules\expiration\application\expiration-service.ts:605:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\expiration\application\expiration-service.ts:678:   * `attribute_not_exists(PK)` condition, while two independently legitimate same-day exports
src\modules\expiration\application\expiration-service.ts:719:        Update: buildVersionedUpdate({
src\modules\expiration\application\expiration-service.ts:819:   * `TenantLifecycleRecord.status = ACTIVE` `ConditionCheck` to the SAME `TransactWriteItems`
src\modules\expiration\domain\audit-event.ts:99:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\subject\application\subject-service.ts:12:import { buildVersionedCreate, buildVersionedUpdate, getCancellationReasonCodes } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\subject-service.ts:96:          Update: buildVersionedUpdate({
src\modules\subject\application\subject-service.ts:205:        Update: buildVersionedUpdate({
src\modules\subject\application\subject-service.ts:268:        Update: buildVersionedUpdate({
src\modules\subject\application\subject-service.ts:281:        Update: buildVersionedUpdate({
src\modules\subject\application\requirement-service.ts:12:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\requirement-service.ts:127:        Update: buildVersionedUpdate({
src\modules\subject\application\requirement-service.ts:164:        Update: buildVersionedUpdate({
src\modules\subject\application\requirement-service.ts:194:        Update: buildVersionedUpdate({
src\modules\subject\application\requirement-service.ts:251:        Update: buildVersionedUpdate({
src\modules\document-archive\ports\document-archive-store.ts:57:  /** PutItem conditioned on attribute_not_exists(PK) AND attribute_not_exists(SK) â€” true if
src\modules\activity\domain\tenant-audit-event.ts:95:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\activity\domain\tenant-audit-event.ts:104: * existe sÃ³ para o ConditionExpression attribute_not_exists(PK) da transaÃ§Ã£o, nÃ£o Ã© lido de
src\modules\activity\domain\tenant-audit-event.ts:132:      ConditionExpression: "attribute_not_exists(PK)",
src\modules\notification\application\ses-callback-workflow.ts:28:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\ses-callback-workflow.ts:119:        Update: buildVersionedUpdate({
src\modules\notification\application\ses-callback-workflow.ts:149:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: inbox["tenantId"] as string, expectedVersion: inbox.version, now, set: { processingStatus: "PROCESSED" } }) },
src\modules\notification\application\ses-callback-workflow.ts:161:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: inbox["tenantId"] as string, expectedVersion: inbox.version, now, set: { processingStatus: "UNMATCHED" } }) },
src\modules\notification\application\ses-callback-workflow.ts:180:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId, expectedVersion: preferences.version, now, set: { emailEnabled: false } }) },
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\application\bff-auth-service.ts:624:   * `GlobalUser` has no `tenantId` for `buildVersionedUpdate`'s reserved condition to check)
src\modules\document-archive\persistence\dynamodb-document-archive-store.ts:42:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\document-archive\persistence\dynamodb-document-archive-store.ts:127:   * comment for the accepted cost tradeoff. `requirement-reindex` used to have a sibling method
src\modules\subject\application\guest-submission-service.ts:10:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\guest-submission-service.ts:174:        Update: buildVersionedUpdate({
src\modules\subject\application\guest-submission-service.ts:193:      // unchanged; this only adds the lifecycle ConditionCheck to the SAME TransactWriteItems.
src\modules\subject\application\guest-submission-service.ts:268:          Update: buildVersionedUpdate({
src\modules\document-archive\domain\requirement.ts:195:/** GSI8 namespace for `requirement-reindex` (D-179/D-185, 4th of 9 MaintenanceDueIndex
src\modules\document-archive\domain\requirement.ts:196: * migrations) â€” see the module doc comment / `requirement-reindex/reindex.ts` for why this is
src\modules\document-archive\domain\requirement.ts:200:export const REQUIREMENT_REINDEX_WORK_TYPE = "REQUIREMENT_REINDEX";
src\modules\document-archive\domain\requirement.ts:218:    GSI8PK: `WORK#${REQUIREMENT_REINDEX_WORK_TYPE}`,
src\modules\notification\application\email-delivery-workflow.ts:21:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\email-delivery-workflow.ts:171:        Update: buildVersionedUpdate({
src\modules\notification\application\email-delivery-workflow.ts:210:          Update: buildVersionedUpdate({
src\modules\notification\application\email-delivery-workflow.ts:244:        Update: buildVersionedUpdate({
src\modules\document-archive\domain\requirement-template.ts:29: * (N Put(Requirement) + N Put(pointer) + ConditionCheck do template + ConditionCheck do Subject
src\modules\notification\application\notification-router-workflow.ts:24:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\notification-router-workflow.ts:107:          Update: buildVersionedUpdate({
src\modules\notification\application\notification-router-workflow.ts:209:      Update: buildVersionedUpdate({
src\modules\notification\application\notification-router-workflow.ts:230:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\notification\application\notification-router-workflow.ts:249:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\notification\application\notification-router-workflow.ts:273:      Update: buildVersionedUpdate({
src\modules\notification\application\notification-router-workflow.ts:326:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\notification\application\notification-router-workflow.ts:331:    entries.push({ Put: { TableName: deps.tableName, Item: { ...lookup }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } });
src\modules\notification\application\notification-router-workflow.ts:337:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\document\application\advance-after-evidence.ts:7:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\document\application\advance-after-evidence.ts:47:    Update: buildVersionedUpdate({
src\modules\document\application\advance-after-evidence.ts:95:        { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: doc.version, set: { status: decision.status } }) },
src\modules\document\application\advance-after-evidence.ts:141:        Update: buildVersionedUpdate({
src\modules\subject\application\document-request-service.ts:9:import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\document-request-service.ts:257:            Update: buildVersionedUpdate({
src\modules\subject\application\document-request-service.ts:345:        Update: buildVersionedUpdate({
src\modules\subject\application\document-request-service.ts:354:        Update: buildVersionedUpdate({
src\modules\document\application\document-deletion-service.ts:10:import { buildVersionedUpdate, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\document\application\document-deletion-service.ts:47:          Update: buildVersionedUpdate({
src\modules\notification\application\notification-preferences-service.ts:24:import { buildVersionedUpdate, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\notification-preferences-service.ts:100:          Update: buildVersionedUpdate({
src\modules\subject\application\document-chasing-producer.ts:13:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\document-chasing-producer.ts:113:        Update: buildVersionedUpdate({
src\modules\identity\ports\identity-store.ts:21:  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
src\modules\identity\ports\identity-store.ts:48:   * lifecycle `ConditionCheck` `transactWrite` pays on every call â€” the design's approved trade
src\modules\subject\application\advance-after-submission-evidence.ts:9:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\advance-after-submission-evidence.ts:60:        entries: [{ Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: decision.status } }) }],
src\modules\subject\application\advance-after-submission-evidence.ts:84:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: "CLEAN", cleanObject } }) },
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\reminder\application\reminder-policy-service.ts:24:import { buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src\modules\reminder\application\reminder-policy-service.ts:126:        Update: buildVersionedUpdate({
src\modules\reminder\application\reminder-policy-service.ts:144:    // ITEM) - re-Put-ing an unchanged pointer with attribute_not_exists would otherwise
src\modules\reminder\application\reminder-policy-service.ts:148:    // that ALSO suppressed the item existence/ACTIVE/tenant ConditionCheck entirely - a
src\modules\reminder\application\reminder-policy-service.ts:186:        Update: buildVersionedUpdate({
src\modules\reminder\application\reminder-policy-service.ts:222:  /** Appends the ITEM-existence ConditionCheck + (unless `skipPointerWrite`) the new pointer Put, when `itemId` is present. */
src\modules\reminder\application\reminder-policy-service.ts:229:      buildExistenceConditionCheck({
src\modules\reminder\application\reminder-policy-service.ts:240:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\document\domain\document.ts:58:   * `attribute_not_exists(GSI6PK) OR GSI6PK <> :purgeClaimed` (D-061) - that condition, not this
src\modules\identity\persistence\identity-mapping-repository.ts:4: * attribute_not_exists(PK)) on first login. One of the two documented exceptions to the
src\modules\identity\persistence\dynamodb-identity-store.ts:36:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\reminder\application\reminder-materializer.ts:13: * attribute_not_exists(PK) condition - the occurrence row IS the idempotency record, same
src\modules\reminder\application\reminder-materializer.ts:30:import { buildVersionConditionCheck, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\reminder\application\reminder-materializer.ts:234:            Update: buildVersionedUpdate({
src\modules\reminder\application\reminder-materializer.ts:268:   * `shouldCancel`, each in its own TransactWriteItems ALSO containing a ConditionCheck on
src\modules\reminder\application\reminder-materializer.ts:270:   * `buildVersionConditionCheck`, the same mechanism as the dispatch fence, commit
src\modules\reminder\application\reminder-materializer.ts:290:            Update: buildVersionedUpdate({
src\modules\reminder\application\reminder-materializer.ts:299:          buildVersionConditionCheck({
src\modules\reminder\application\reminder-materializer.ts:362:            Update: buildVersionedUpdate({
src\modules\notification\persistence\dynamodb-notification-store.ts:40:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\domain\import-job.ts:17:import { buildVersionedUpdate, type EntityKey, type TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src\modules\import\domain\import-job.ts:188: * `buildVersionedUpdate` jÃ¡ Ã© suficiente para rejeitar qualquer gravaÃ§Ã£o concorrente entre a
src\modules\import\domain\import-job.ts:203:    Update: buildVersionedUpdate({
src\modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
src\modules\import\application\import-commit-service.ts:29: * (fences de domÃ­nio + Put) + Put(`ImportDedupRecord`, `attribute_not_exists`, sÃ³ quando a
src\modules\import\application\import-commit-service.ts:30: * linha tem `externalId` - Â§7) + Put(`ImportRowOutcome` COMMITTED, `attribute_not_exists`,
src\modules\import\application\import-commit-service.ts:38: * `attribute_not_exists`, `failureReason=<cÃ³digo>`) + Update(cursor) - a linha fica marcada como
src\modules\import\application\import-commit-service.ts:59:import { buildVersionedUpdate, getCancellationReasonCodes, isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\import\application\import-commit-service.ts:73:   * `Put`/`Update`/`ConditionCheck` dos planejadores de document-archive precisam do
src\modules\import\application\import-commit-service.ts:302:    Update: buildVersionedUpdate({
src\modules\import\application\import-commit-service.ts:345:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\import\application\import-commit-service.ts:353:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\import\application\import-commit-service.ts:418:        ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\import\domain\import-dedup.ts:3: * cobrir os 3 `ImportTargetEntityType`s (antes sÃ³ `SUBJECT`). `Put attribute_not_exists` na
src\modules\document-archive\application\guest-document-access-service.ts:28:import { buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\application\guest-document-access-service.ts:383:        ? [buildExistenceConditionCheck({ tableName: this.tableName, key: documentTypeKey(tenantId, documentType), extra: { status: "ACTIVE" } })]
src\modules\document-archive\application\guest-document-access-service.ts:390:        Update: buildVersionedUpdate({
src\modules\document-archive\application\guest-document-access-service.ts:423:          Update: buildVersionedUpdate({
src\modules\import\application\resolve-document-type-references.ts:24: * caught by the commit's own `ConditionCheck` (a row-level commit failure, never a job-level
src\modules\import\application\import-service.ts:13:import { buildVersionedUpdate, buildVersionedCreate } from "../../../shared/dynamodb/occ.js";
src\modules\import\application\import-service.ts:319:   * transition from `buildImportJobClaim()`, dispatching `SQS_IMPORT_PARSE_V1` in the SAME
src\modules\import\application\import-service.ts:392:      appendToTransaction(entries, this.tableName, event, "SQS_IMPORT_PARSE_V1");
src\modules\import\application\import-service.ts:424:        Update: buildVersionedUpdate({
src\modules\organization\persistence\dynamodb-organization-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\organization\domain\audit-event.ts:101:      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\extraction\application\confirm-reject-field.ts:170:    // MUST be the bare {PK,SK} key, never the whole `field` entity: `buildVersionedUpdate`
src\modules\extraction\application\start-extraction-run.ts:47:export async function startExtractionRun(deps: StartExtractionRunDeps, input: StartExtractionRunInput): Promise<StartExtractionRunOutcome> {
src\modules\extraction\application\run-extraction-validation.ts:218: * all-or-nothing `TransactWriteItems` as the field rows, whose per-row `attribute_not_exists`
src\modules\document-archive\application\apply-file-scan-result.ts:2: * applyFileScanResult / confirmFileScanClean â€” D-163 Â§1/Â§5, first real slice of DocumentFile's
src\modules\document-archive\application\apply-file-scan-result.ts:14: * `confirmFileScanClean()` is the confirmation step, called only once that caller's own S3
src\modules\document-archive\application\apply-file-scan-result.ts:18:import { buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\application\apply-file-scan-result.ts:62:export async function applyFileScanResult(deps: ApplyFileScanResultDeps, input: ApplyFileScanResultInput): Promise<ApplyFileScanResultOutcome> {
src\modules\document-archive\application\apply-file-scan-result.ts:107:            Update: buildVersionedUpdate({
src\modules\document-archive\application\apply-file-scan-result.ts:146:        Update: buildVersionedUpdate({
src\modules\document-archive\application\apply-file-scan-result.ts:159:        Update: buildVersionedUpdate({
src\modules\document-archive\application\apply-file-scan-result.ts:190:  throw new Error(`applyFileScanResult exhausted retries for file ${input.fileId} under contention.`);
src\modules\document-archive\application\apply-file-scan-result.ts:204: * confirmFileScanClean - the PROMOTE confirmation step, called only once the caller (a future
src\modules\document-archive\application\apply-file-scan-result.ts:211:export async function confirmFileScanClean(deps: ApplyFileScanResultDeps, input: ConfirmFileScanCleanInput): Promise<ConfirmFileScanCleanOutcome> {
src\modules\document-archive\application\apply-file-scan-result.ts:226:        Update: buildVersionedUpdate({
src\modules\document-archive\application\apply-file-scan-result.ts:237:        Update: buildVersionedUpdate({
src\modules\document-archive\application\apply-file-scan-result.ts:256:  throw new Error(`confirmFileScanClean exhausted retries for file ${input.fileId} under contention.`);
src\modules\document-archive\application\apply-file-scan-result.ts:275: * (PENDING_UPLOAD/SCANNING -> TIMEOUT), symmetric to applyFileScanResult's REJECT branch but
src\modules\document-archive\application\apply-file-scan-result.ts:277: * exact same counter/GSI8-removal mechanism (buildVersionedUpdate, no new pattern) - the only
src\modules\document-archive\application\apply-file-scan-result.ts:297:        Update: buildVersionedUpdate({
src\modules\document-archive\application\apply-file-scan-result.ts:317:        Update: buildVersionedUpdate({
src\modules\document-archive\application\document-request-recurrence-service.ts:13: * `RequestContext` â€” same split as `requirement-reindex/reindex.ts`, which also builds/executes
src\modules\document-archive\application\document-request-recurrence-service.ts:14: * `buildVersionedUpdate` directly against the store rather than round-tripping through
src\modules\document-archive\application\document-request-recurrence-service.ts:17:import { buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\application\document-request-recurrence-service.ts:87:      Update: buildVersionedUpdate({
src\modules\document-archive\application\document-request-recurrence-service.ts:166:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-request-recurrence-service.ts:221:    const update = buildVersionedUpdate({
src\modules\extraction\ports\extracted-field-store.ts:50:  /** Atomically writes every `fields` row (first-time create, `attribute_not_exists` per row â€”
src\modules\extraction\ports\extracted-field-store.ts:70:   * `ExtractedField.state` -> `CONFIRMED` (+ `confirmedValue`), `ConditionCheck`s pinning
src\modules\extraction\ports\extracted-field-store.ts:74:   * `ExpirationItem` row; otherwise a `ConditionCheck` pinning the item's version instead (the
src\modules\extraction\ports\extracted-field-store.ts:77:   * throws â€” when any single `ConditionCheck`/conditional `Update` fails, so the caller can map
src\modules\extraction\ports\extracted-field-store.ts:82:   * optional `correctionReason`) plus `ConditionCheck`s pinning `ExtractionRun`/`Document` â€”
src\modules\extraction\ports\extraction-execution-starter.ts:4: * own narrow port (not a generic "AWS SDK wrapper") so `startExtractionRun` stays testable
src\modules\extraction\ports\extraction-execution-starter.ts:38:   * top of the `ExtractionRun.putIfAbsent` check `startExtractionRun` already does first).
src\modules\identity\application\bootstrap-identity.ts:9: * ambos condicionados a `attribute_not_exists(PK)`. Nenhuma `Organization`/`TenantLifecycleRecord`/
src\modules\identity\application\bootstrap-identity.ts:17: * (`attribute_not_exists(PK)` falhou em pelo menos um dos itens), re-lÃª e resolve contra a
src\modules\identity\application\quota.ts:174:   * `ConditionCheck` (`shared/tenant-lifecycle/tenant-business-mutation.ts`) â€” same pattern
src\modules\identity\application\quota.ts:300:   * `ConditionCheck`, just one atomic `ADD` `UpdateItem` (`IdentityStore.incrementTelemetryCounter`).
src\modules\organization\domain\invitation-token.ts:32:   * `attribute_not_exists(consumedAt)` â€” nunca setado por uma leitura solta antes da decisÃ£o. */
src\modules\organization\application\accept-invitation.ts:90:        ConditionExpression: "attribute_not_exists(PK) OR #status = :removed",
src\modules\organization\application\accept-invitation.ts:124:        ConditionExpression: "attribute_not_exists(consumedAt) AND expiresAt > :now",
src\modules\extraction\application\start-ocr.ts:5: * `startExtractionRun` (hand-written fakes, no `vi.mock`). Deliberately never calls
src\modules\organization\domain\membership.ts:12: * `attribute_not_exists(PK) OR #status = :REMOVED`, nÃ£o implementado ainda nesta wave).
src\modules\organization\domain\membership.ts:52:   * Â§8 poison-record handling) â€” incremented only when the atomic tenant-ACTIVE ConditionCheck
src\modules\organization\domain\membership.ts:107: * the atomic tenant-ACTIVE `ConditionCheck` straight off a `KEYS_ONLY` Query result, without a
src\modules\organization\application\create-invitation.ts:5: * (`ConditionCheck attribute_not_exists` â€” se jÃ¡ existir um convite PENDING para (org, e-mail),
src\modules\organization\application\create-invitation.ts:144:      { Put: { TableName: this.tableName, Item: invitation as unknown as Record<string, unknown>, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
src\modules\organization\application\create-invitation.ts:145:      { Put: { TableName: this.tableName, Item: tokenPointer as unknown as Record<string, unknown>, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
src\modules\organization\application\create-invitation.ts:146:      { Put: { TableName: this.tableName, Item: dedupPointer as unknown as Record<string, unknown>, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
src\modules\organization\application\create-invitation.ts:221:      { Put: { TableName: this.tableName, Item: tokenPointer as unknown as Record<string, unknown>, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
src\modules\organization\application\create-organization.ts:15: * dois logins concorrentes podem mirar o mesmo `cognitoSub`) â€” se `attribute_not_exists(PK)`
src\modules\document-archive\application\document-archive-service.ts:18:  buildExistenceConditionCheck,
src\modules\document-archive\application\document-archive-service.ts:21:  buildVersionConditionCheck,
src\modules\document-archive\application\document-archive-service.ts:22:  buildVersionedUpdate,
src\modules\document-archive\application\document-archive-service.ts:99:  applyFileScanResult,
src\modules\document-archive\application\document-archive-service.ts:100:  confirmFileScanClean,
src\modules\document-archive\application\document-archive-service.ts:207:    buildExistenceConditionCheck({
src\modules\document-archive\application\document-archive-service.ts:245:  return buildExistenceConditionCheck({
src\modules\document-archive\application\document-archive-service.ts:362:   * applyFileScanResult / confirmFileScanClean â€” D-163 Â§1/Â§5. Event-driven (S3 Object Created /
src\modules\document-archive\application\document-archive-service.ts:369:   * `sourceObject` -> `confirmFileScanClean`'s `cleanObject`) is owned by the future S3/
src\modules\document-archive\application\document-archive-service.ts:372:  async applyFileScanResult(input: ApplyFileScanResultInput): Promise<ApplyFileScanResultOutcome> {
src\modules\document-archive\application\document-archive-service.ts:373:    return applyFileScanResult({ store: this.store, tableName: this.tableName, ids: this.ids, now: this.now }, input);
src\modules\document-archive\application\document-archive-service.ts:376:  async confirmFileScanClean(input: ConfirmFileScanCleanInput): Promise<ConfirmFileScanCleanOutcome> {
src\modules\document-archive\application\document-archive-service.ts:377:    return confirmFileScanClean({ store: this.store, tableName: this.tableName, ids: this.ids, now: this.now }, input);
src\modules\document-archive\application\document-archive-service.ts:384:   * `ConditionCheck` runs inside the SAME `TransactWriteItems`, never a separate read.
src\modules\document-archive\application\document-archive-service.ts:529:        Update: buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:538:            { expression: "attribute_not_exists(#sealed) OR #sealed = :false", names: { "#sealed": "fileSetSealed" }, values: { ":false": false } },
src\modules\document-archive\application\document-archive-service.ts:584:   * a clear `ConflictError` message, and a transactional `ConditionCheck` (TOCTOU-safe, same
src\modules\document-archive\application\document-archive-service.ts:597:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:628:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:693:      Update: buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:709:          Update: buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:732:      Update: buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:751:    // TransactWriteItems cancels (TOCTOU-safe), same as any other ConditionCheck here. Only
src\modules\document-archive\application\document-archive-service.ts:756:        buildExistenceConditionCheck({
src\modules\document-archive\application\document-archive-service.ts:806:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:840:    // `attribute_not_exists` (not by a read-then-write), and the Subject's existence/status is
src\modules\document-archive\application\document-archive-service.ts:883:    return buildExistenceConditionCheck({
src\modules\document-archive\application\document-archive-service.ts:978:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1063:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1096:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1146:   * createDocumentType â€” D-173 Â§3. `[0] Put(DocumentType, attribute_not_exists), [1]
src\modules\document-archive\application\document-archive-service.ts:1147:   * Put(pointer, attribute_not_exists), [2] fence]`. Position 1 (the pointer) is what actually
src\modules\document-archive\application\document-archive-service.ts:1233:   * documentTypeId=:self), [2] Put(new pointer, attribute_not_exists), [3] fence]`.
src\modules\document-archive\application\document-archive-service.ts:1246:      const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1264:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1333:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1356:  /** Same shape as `createDocumentType` (D-173 Â§3): `[0] Put(template, attribute_not_exists),
src\modules\document-archive\application\document-archive-service.ts:1357:   * [1] Put(name pointer, attribute_not_exists), [2] fence]`. Position 1 is what actually closes
src\modules\document-archive\application\document-archive-service.ts:1433:    const update = buildVersionedUpdate({
src\modules\document-archive\application\document-archive-service.ts:1539:   * N Ã— Put(Requirement, attribute_not_exists) + N Ã— Put(name pointer, attribute_not_exists)
src\modules\document-archive\application\document-archive-service.ts:1540:   *   + ConditionCheck(template: status = ACTIVE AND version = <expected>)
src\modules\document-archive\application\document-archive-service.ts:1541:   *   + ConditionCheck(Subject:  attribute_exists(PK) AND status = ACTIVE)
src\modules\document-archive\application\document-archive-service.ts:1612:    // `buildVersionConditionCheck` pins version AND status in one action â€” closing the TOCTOU
src\modules\document-archive\application\document-archive-service.ts:1615:    entries.push(buildVersionConditionCheck({
src\modules\document-archive\application\document-archive-service.ts:1716:    const update = buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:3: * `attribute_not_exists` Put per field row (first-time create, `buildVersionedCreate`), one
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:4: * versioned Update on the parent `ExtractionRun` (`buildVersionedUpdate`), and one
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:5: * `ConditionCheck` re-asserting the `Document` row is still at the version the caller read
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:6: * moments earlier (`buildVersionConditionCheck`) â€” the TOCTOU close for the concurrent-discard
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:11:import { buildVersionedCreate, buildVersionedUpdate, buildVersionConditionCheck, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:34:      Update: buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:43:    const documentGuard = buildVersionConditionCheck({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:55:            Update: buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:100:      Update: buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:110:    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:111:    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:115:          Update: buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:124:      : buildVersionConditionCheck({ tableName: this.tableName, key: input.itemKey, expectedVersion: input.itemExpectedVersion });
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:142:      Update: buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:152:    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:153:    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:8:import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:36:    const command = buildVersionedUpdate({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:27:          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:44:  /** No `tenantId` equality check here (unlike `buildVersionedUpdate`'s usual tenant-scoped
src\modules\extraction\persistence\sfn-extraction-execution-starter.ts:4: * of the DynamoDB `ExtractionRun.putIfAbsent` check in `startExtractionRun`. */

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Write-Output '=== src/shared/outbox/outbox.ts'; Get-Content -Raw 'src/shared/outbox/outbox.ts'; Write-Output '=== src/workers/dispatch-outbox-relay/relay.ts'; Get-Content -Raw 'src/workers/dispatch-outbox-relay/relay.ts'; Write-Output '=== src/runtime/aws/handlers/dispatch-outbox-relay-handler.ts'; Get-Content -Raw 'src/runtime/aws/handlers/dispatch-outbox-relay-handler.ts'; Write-Output '=== src/modules/document-archive/application/apply-file-scan-result.ts'; Get-Content -Raw 'src/modules/document-archive/application/apply-file-scan-result.ts'; Write-Output '=== src/shared/dynamodb/occ.ts'; Get-Content -Raw 'src/shared/dynamodb/occ.ts'; Write-Output '=== src/modules/extraction/application/start-extraction-run.ts'; Get-Content -Raw 'src/modules/extraction/application/start-extraction-run.ts'; Write-Output '=== src/modules/extraction/persistence/dynamodb-extraction-run-store.ts'; Get-Content -Raw 'src/modules/extraction/persistence/dynamodb-extraction-run-store.ts'; Write-Output '=== src/modules/extraction/domain/extraction-run.ts'; Get-Content -Raw 'src/modules/extraction/domain/extraction-run.ts'; Write-Output '=== src/workers/requirement-reindex/reindex.ts'; Get-Content -Raw 'src/workers/requirement-reindex/reindex.ts'; Write-Output '=== src/workers/requirement-reindex/dynamodb-candidate-source.ts'; Get-Content -Raw 'src/workers/requirement-reindex/dynamodb-candidate-source.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 528ms:
=== src/shared/outbox/outbox.ts
/**
 * Transactional outbox helper - implementation-blueprint.md #5.1 (OutboxPort) and #5.3
 * (record shape + OutboxPublisher contract). Builds the outbox item + its
 * TransactWriteItems `Put` entry so callers can append it atomically alongside the
 * aggregate write (e.g. ExpirationItem update + ItemDueDateChanged in one transaction,
 * per data-model.md ¶õ5).
 */
import type { DomainEvent } from "../contracts/events.js";
import type { TransactPutEntry, TransactWriteEntry } from "../dynamodb/occ.js";

export type OutboxStatus = "PENDING" | "PUBLISHED";

/**
 * M3.5 (docs/architecture/m3.5-runtime-design.md, "DecisÇœo central: outbox durÇ­vel"):
 * routing discriminator so `OutboxPublisher` (generic, -> EventBridge) and
 * `DispatchOutboxRelay` (new, -> SQS ReminderDispatchQueue) never both claim the same
 * record. `undefined`/absent means "OutboxPublisher's default EventBridge path" (every
 * M2/M3 caller today) - `OutboxPublisher` must explicitly skip any record whose
 * `destination` it doesn't recognize as its own default, never process-by-omission.
 */
export type OutboxDestination =
  | "SQS_REMINDER_DISPATCH_V1"
  | "SQS_NOTIFICATION_EMAIL_V1"
  | "SQS_DOCUMENT_CHASING_DISPATCH_V1"
  | "SQS_IMPORT_COMMIT_V1"
  /** BLOCKER-B (reminder-delivery-pipeline.md ¶õ4): the ONLY real delivery path for an
   * outbox record - the "generic EventBridge path" every other destination's comment
   * describes as the default for an unset `destination` was never actually implemented in
   * this codebase (no PutEventsCommand call exists anywhere) - confirmed during BLOCKER-B
   * implementation, not previously known. Every outbox-driven consumer in production is
   * destination-routed through DispatchOutboxRelay; this destination follows the same
   * proven mechanism rather than the never-built one the original design doc assumed. */
  | "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1"
  /** D-192 slice 9 (`bulk-import-documents-requirements-scoping/estado-final-consolidado.md`
   * ¶õ3): dispatched in the SAME `TransactWriteItems` as `POST /import-jobs/{jobId}/mapping`'s
   * `AWAITING_MAPPING`->`PARSING` claim, whenever that POST is the one that resolves the claim
   * (never when the job stays `UPLOADED`, mapping-only write). Consumed by the SAME
   * `parseImportJob()`/`import-parse-handler.ts` the S3-event trigger already uses ƒ?" the two
   * triggers are discriminated only by envelope shape in the Lambda handler, never inside the
   * pure function, per the design. */
  | "SQS_IMPORT_PARSE_V1";

export interface OutboxRecord {
  PK: string;
  SK: string;
  entityType: "OutboxEvent";
  /** BLOCKER-B addition: previously only embedded in `PK` (`TENANT#<t>#OUTBOX#<shard>`),
   * never its own field - a sender whose payload doesn't already self-describe its tenant
   * (unlike DispatchCommand) needs this explicitly, without every caller re-parsing `PK`. */
  tenantId: string;
  eventId: string;
  eventType: string;
  aggregateType: string;
  aggregateId: string;
  aggregateVersion: number;
  status: OutboxStatus;
  occurredAt: string;
  payload: Record<string, unknown>;
  publishAttempts: number;
  nextAttemptAt: string;
  createdAt: string;
  GSI6PK: string;
  GSI6SK: string;
  destination?: OutboxDestination;
  /**
   * m5-observability-design.md #2: copied from `DomainEvent.correlationId` (already mandatory
   * on every event) at write time - never read from the ambient AsyncLocalStorage context here,
   * so this stays a pure, environment-agnostic builder. Optional only because records persisted
   * before M5 don't have it (historical data, never written without it again) - readers
   * (relay/sweeper) must fall back to `eventId` when absent, per the design's fallback table.
   */
  correlationId?: string;
}

/** Monthly shard for the outbox partition, matching #5.3's `TENANT#t#OUTBOX#202608` example. */
function monthShard(isoTimestamp: string): string {
  return isoTimestamp.slice(0, 7).replace("-", "");
}

export function buildOutboxRecord(event: DomainEvent, destination?: OutboxDestination): OutboxRecord {
  const shard = monthShard(event.occurredAt);
  return {
    PK: `TENANT#${event.tenantId}#OUTBOX#${shard}`,
    SK: `EVENT#${event.occurredAt}#${event.eventId}`,
    entityType: "OutboxEvent",
    tenantId: event.tenantId,
    eventId: event.eventId,
    eventType: event.eventType,
    aggregateType: event.aggregate.type,
    aggregateId: event.aggregate.id,
    aggregateVersion: event.aggregate.version,
    status: "PENDING",
    occurredAt: event.occurredAt,
    payload: event.data,
    publishAttempts: 0,
    nextAttemptAt: event.occurredAt,
    createdAt: event.occurredAt,
    GSI6PK: "RECON#OUTBOX#PENDING",
    GSI6SK: `${event.occurredAt}#${event.eventId}`,
    correlationId: event.correlationId,
    ...(destination ? { destination } : {}),
  };
}

/**
 * m5-observability-design.md #2: correlation id a relay/sweeper should use for the record
 * being processed - read from the persisted `OutboxRecord`, never from the caller's own
 * ambient context. Falls back to `eventId` only for records persisted before M5 (missing
 * `correlationId`); this is historical-data compatibility, not a new write path.
 */
export function outboxRecordCorrelationId(record: OutboxRecord): string {
  return record.correlationId ?? record.eventId;
}

/** @deprecated kept as an alias of the shared `TransactPutEntry` (src/shared/dynamodb/occ.ts)
 * for backward compatibility with existing imports - full-audit round1/qualidade found this
 * was a duplicate, structurally-identical declaration of the same shape (same pattern as the
 * expiration-store.ts/reminder-store.ts duplication decisions-log.md E-008 already fixed).
 * Do not add fields here; add them to the shared type instead. */
export type DynamoTransactPutEntry = TransactPutEntry;

/**
 * OutboxPort.append from implementation-blueprint.md #5.1: appends the outbox Put to a
 * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
 * owns the transaction and includes the aggregate's own conditional update alongside it.
 * Accepts the full `TransactWriteEntry` union (Put | Update) so callers whose transaction
 * also contains Update entries (e.g. reminder dispatch's CLAIMED->TRIGGERED transition) can
 * pass their real array directly, without an unsafe cast to satisfy a narrower Put-only type.
 */
export function appendToTransaction(
  tx: TransactWriteEntry[],
  tableName: string,
  event: DomainEvent,
  destination?: OutboxDestination,
): void {
  const record = buildOutboxRecord(event, destination);
  tx.push({
    Put: {
      TableName: tableName,
      Item: { ...record },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });
}

/** Backoff schedule for OutboxPublisher retries (#5.3 step 6: "incrementa tentativas e
 * aplica backoff"). Exponential with a cap; judgment call - blueprint doesn't pin exact values. */
export function nextAttemptDelayMs(publishAttempts: number): number {
  const base = 1_000;
  const cap = 5 * 60_000;
  return Math.min(cap, base * 2 ** publishAttempts);
}

=== src/workers/dispatch-outbox-relay/relay.ts
/**
 * DispatchOutboxRelay + OutboxSweeperReminderDispatch core logic (M3.5,
 * docs/architecture/m3.5-runtime-design.md "DecisÇœo central: outbox durÇ­vel"). Pure(ish) -
 * injected store/queue/clock, no AWS SDK/Lambda runtime dependency, same pattern as
 * src/workers/reminder-producer/producer.ts.
 *
 * Both the relay (triggered per-record by DynamoDB Streams) and the sweeper (triggered on
 * a schedule, sourcing candidates via `listPendingReminderDispatch`) call the SAME
 * `publishOne` - the only difference is how they discover which records to attempt.
 *
 * M4 (docs/architecture/m4-notification-engine-design.md ¶õ7.4, fechamento de rodada 1)
 * generalizes this from a single hardcoded destination (SQS_REMINDER_DISPATCH_V1) to an
 * explicit router keyed by `destination` - a second sweeper querying the SAME global GSI6
 * partition (`RECON#OUTBOX#PENDING`) for a second destination would be redundant; the
 * existing sweeper role is already privileged for GSI6 and just needs to dispatch to the
 * right queue sender per record.
 */
import { runWithContext } from "../../shared/observability/context.js";
import { outboxRecordCorrelationId, type OutboxRecord, type OutboxDestination } from "../../shared/outbox/outbox.js";
import type { OutboxRelayStore } from "../../shared/outbox/relay-store.js";

export const SQS_REMINDER_DISPATCH_V1: OutboxDestination = "SQS_REMINDER_DISPATCH_V1";
export const SQS_NOTIFICATION_EMAIL_V1: OutboxDestination = "SQS_NOTIFICATION_EMAIL_V1";

/** One sender per recognized destination - a record whose `destination` has no entry here
 * is not this relay/sweeper's to touch (SKIPPED_WRONG_DESTINATION), same exclusivity
 * discipline as the single-destination M3.5 version. `tenantId`/`eventType` (BLOCKER-B,
 * reminder-delivery-pipeline.md ¶õ4) are the record's own envelope fields, passed through so
 * a sender whose payload doesn't already self-describe them (unlike DispatchCommand, which
 * embeds its own tenantId/commandType) can still build a self-contained SQS message -
 * existing senders (dispatch/email/chasing/import, whose payloads already carry everything
 * they need) simply ignore the two extra arguments. */
export type DestinationSenders = Partial<
  Record<OutboxDestination, (payload: Record<string, unknown>, correlationId: string, tenantId: string, eventType: string) => Promise<void>>
>;

export interface RelayDeps {
  store: OutboxRelayStore;
  senders: DestinationSenders;
  now: () => string;
  leaseOwner: string;
  /** How long a lease is held while attempting SendMessage - default 30s, comfortably
   * longer than one SQS SendMessage call, short enough that a crashed relay invocation's
   * lease is quickly reclaimable by the next relay/sweeper attempt. */
  leaseDurationMs?: number;
}

export type PublishOutcome =
  | { kind: "PUBLISHED" }
  | { kind: "SKIPPED_WRONG_DESTINATION" }
  | { kind: "SKIPPED_ALREADY_PUBLISHED" }
  | { kind: "SKIPPED_LEASE_HELD" }
  | { kind: "FAILED"; error: unknown };

/** Attempts to publish exactly one outbox record. Never throws for expected outcomes
 * (wrong destination / already published / lease contention) - only for genuinely
 * unexpected failures during acquire/send/mark, surfaced as `FAILED` so the caller (relay
 * Lambda: partial batch failure; sweeper: per-item log+continue) decides retry policy. */
export async function publishOne(deps: RelayDeps, record: OutboxRecord): Promise<PublishOutcome> {
  // Exclusivity (docs/architecture/m3.5-runtime-design.md ¶õ"DecisÇœo central"): this relay/
  // sweeper NEVER touches a record it doesn't own the destination for - OutboxPublisher
  // (generic, EventBridge) owns everything else, including records with no `destination`.
  const send = record.destination ? deps.senders[record.destination] : undefined;
  if (!send) {
    return { kind: "SKIPPED_WRONG_DESTINATION" };
  }
  if (record.status === "PUBLISHED") {
    return { kind: "SKIPPED_ALREADY_PUBLISHED" };
  }

  // m5-observability-design.md #2: one runWithContext per record - correlationId read from
  // the persisted OutboxRecord (the business operation that originally created it), never
  // from this invocation's own ambient context.
  return runWithContext({ correlationId: outboxRecordCorrelationId(record) }, () => publishAcquiredOrPending(deps, record, send));
}

async function publishAcquiredOrPending(
  deps: RelayDeps,
  record: OutboxRecord,
  send: (payload: Record<string, unknown>, correlationId: string, tenantId: string, eventType: string) => Promise<void>,
): Promise<PublishOutcome> {
  const leaseDurationMs = deps.leaseDurationMs ?? 30_000;
  const now = deps.now();
  const leaseExpiresAt = new Date(Date.parse(now) + leaseDurationMs).toISOString();

  const acquired = await deps.store.tryAcquireLease({ PK: record.PK, SK: record.SK }, deps.leaseOwner, leaseExpiresAt, now);
  if (!acquired) {
    return { kind: "SKIPPED_LEASE_HELD" };
  }

  try {
    await send(record.payload, outboxRecordCorrelationId(record), record.tenantId, record.eventType);
  } catch (error) {
    // Failure here is expected and recoverable: the lease expires, the sweeper (or a later
    // Stream retry) will retry. A duplicate SendMessage after a retry is absorbed by the
    // downstream worker's own idempotency - never treated as a reason to skip retrying.
    return { kind: "FAILED", error };
  }

  // Transition PENDING -> PUBLISHED only AFTER SendMessage confirmed (m3.5-runtime-design.md:
  // "falha entre (2) e (3) Ç¸ aceitÇ­vel - duplicata Ç¸ absorvida pela idempotÇ¦ncia do
  // dispatch"). Failure here also just means the sweeper resends a message that was
  // already delivered once - still recoverable, not a correctness issue.
  await deps.store.markPublished({ PK: record.PK, SK: record.SK });
  return { kind: "PUBLISHED" };
}

/** DispatchOutboxRelay / NotificationEmailOutboxRelay entrypoint logic: one call per
 * DynamoDB Streams record (NEW_IMAGE). Which record types actually flow through a given
 * Lambda's Streams filter, and thus which `senders` it needs, is a wiring concern, not this
 * function's - it stays destination-agnostic. */
export async function relayStreamRecord(deps: RelayDeps, record: OutboxRecord): Promise<PublishOutcome> {
  return publishOne(deps, record);
}

export interface SweeperDeps extends RelayDeps {
  /** Minimum age (ms) before a PENDING record is swept - covers relay/Stream failure, not
   * normal in-flight latency (m3.5-runtime-design.md: "occurredAt mais antigo que um
   * limiar (2 min)"). */
  minAgeMs?: number;
  pageSize?: number;
  /** Which destinations to sweep this run - the sweeper queries GSI6 once per destination
   * (same global partition, `RECON#OUTBOX#PENDING`, filtered by `destination` server-side by
   * the store). Defaults to every destination this deps' `senders` map recognizes. */
  destinations?: OutboxDestination[];
}

export interface SweepResult {
  attempted: number;
  published: number;
  failed: number;
  stillPending: number;
}

/** OutboxSweeperReminderDispatch/OutboxSweeperNotificationEmail entrypoint logic: no backoff
 * of its own - every scheduled execution IS one retry attempt (m3.5-runtime-design.md:
 * "nenhum backoff prÇüprio"). One sweeper instance can cover multiple destinations (M4
 * ¶õ7.4: "nÇœo proponho outro GSI nem scan... o sweeper existente deve evoluir para um
 * roteador explÇðcito por destination"). */
export async function sweepPendingDispatch(deps: SweeperDeps): Promise<SweepResult> {
  const minAgeMs = deps.minAgeMs ?? 2 * 60_000;
  const olderThan = new Date(Date.parse(deps.now()) - minAgeMs).toISOString();
  const destinations = deps.destinations ?? (Object.keys(deps.senders) as OutboxDestination[]);

  let attempted = 0;
  let published = 0;
  let failed = 0;
  let stillPending = 0;

  for (const destination of destinations) {
    const candidates = await deps.store.listPendingReminderDispatch({
      destination,
      olderThan,
      pageSize: deps.pageSize,
    });

    attempted += candidates.length;
    for (const record of candidates) {
      const outcome = await publishOne(deps, record);
      if (outcome.kind === "PUBLISHED") published += 1;
      else if (outcome.kind === "FAILED") failed += 1;
      else stillPending += 1;
    }
  }

  return { attempted, published, failed, stillPending };
}

=== src/runtime/aws/handlers/dispatch-outbox-relay-handler.ts
/** Real handler for DispatchOutboxRelay (DynamoDB Streams NEW_IMAGE), replacing the 501
 * placeholder. Partial batch failure so a poison record doesn't block the rest of the
 * shard's batch (m3.5-runtime-design.md ¶õ"DecisÇœo central"). Per-record processing logic
 * lives in dispatch-outbox-relay-processor.ts (no module-level side effects, unit-testable)
 * - this file is only the thin AWS entrypoint: real env vars, real clients, real deps. */
import type { DynamoDBBatchResponse, DynamoDBStreamEvent } from "aws-lambda";
import { SQSClient } from "@aws-sdk/client-sqs";
import { createDocumentClient } from "../../../shared/dynamodb/client.js";
import { buildOutboxRelayDeps } from "../composition/reminder.js";
import { SecureLogger } from "../../../shared/observability/logger.js";
import { processStreamRecords } from "./dispatch-outbox-relay-processor.js";

const client = createDocumentClient();
const tableName = process.env["TABLE_NAME"];
const queueUrl = process.env["DISPATCH_QUEUE_URL"];
// M10 cluster 4 (D-039/D-046/D-048): second destination on the SAME relay Lambda/DynamoDB
// Streams event source mapping - never a new relay function just for this one extra queue.
const chasingQueueUrl = process.env["DOCUMENT_CHASING_DISPATCH_QUEUE_URL"];
// M11 (D-042): third destination, same reasoning.
const importCommitQueueUrl = process.env["IMPORT_COMMIT_QUEUE_URL"];
// BLOCKER-B (reminder-delivery-pipeline.md ¶õ4): fourth destination, same reasoning - the
// generic "no destination -> EventBridge" path this design doc originally assumed was never
// actually implemented in this codebase (confirmed during implementation, see outbox.ts's
// OutboxDestination comment), so this is the only real delivery mechanism available.
const materializationTriggerQueueUrl = process.env["REMINDER_MATERIALIZATION_TRIGGER_QUEUE_URL"];
// D-192 slice 9: fifth destination, same reasoning.
const importParseQueueUrl = process.env["IMPORT_PARSE_QUEUE_URL"];
if (!tableName) throw new Error("TABLE_NAME env var is required.");
if (!queueUrl) throw new Error("DISPATCH_QUEUE_URL env var is required.");
if (!chasingQueueUrl) throw new Error("DOCUMENT_CHASING_DISPATCH_QUEUE_URL env var is required.");
if (!importCommitQueueUrl) throw new Error("IMPORT_COMMIT_QUEUE_URL env var is required.");
if (!materializationTriggerQueueUrl) throw new Error("REMINDER_MATERIALIZATION_TRIGGER_QUEUE_URL env var is required.");
if (!importParseQueueUrl) throw new Error("IMPORT_PARSE_QUEUE_URL env var is required.");
const deps = buildOutboxRelayDeps(client, tableName, queueUrl, new SQSClient({}), chasingQueueUrl, importCommitQueueUrl, materializationTriggerQueueUrl, importParseQueueUrl);
const logger = new SecureLogger({ baseContext: { service: "dispatch-outbox-relay" } });

export async function handler(event: DynamoDBStreamEvent): Promise<DynamoDBBatchResponse> {
  const batchItemFailures = await processStreamRecords(deps, logger, event.Records);
  return { batchItemFailures };
}

=== src/modules/document-archive/application/apply-file-scan-result.ts
/**
 * applyFileScanResult / confirmFileScanClean ƒ?" D-163 ¶õ1/¶õ5, first real slice of DocumentFile's
 * scan-result state machine. Reuses `document/domain/document-state-machine.ts`'s
 * `decideNextAction()` VERBATIM (never forked/redesigned ƒ?" `DocumentFileScanStatus`'s taxonomy
 * is a structural subset of `DocumentStatus`, see `document-file.ts`'s module doc comment).
 *
 * Split into two functions rather than M6's single `advanceAfterEvidence()`, because unlike
 * M6's Document (whose `quarantineObject.versionId` is a permanent "" placeholder,
 * `document.ts`'s doc comment), a `DocumentFile.quarantineObject.versionId` IS the field the
 * symmetric evidence correlation consolidates into (D-163 ¶õ1) - so this function never needs to
 * reach into S3 itself just to decide PROMOTE-vs-not: it returns `READY_TO_PROMOTE` with the
 * `sourceObject` a worker copies from, and does zero writes for that outcome (never claims CLEAN
 * for a copy that hasn't happened, same discipline M6's PROMOTE branch already established).
 * `confirmFileScanClean()` is the confirmation step, called only once that caller's own S3
 * copy+verify has succeeded - mirrors M6's `advanceAfterEvidence()` PROMOTE branch's contract,
 * just as a separate, independently retryable step instead of inlined in the same function.
 */
import { buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
import { decideNextAction } from "../../document/domain/document-state-machine.js";
import type { UploadEvidence } from "../../document/domain/document.js";
import type { DocumentObjectReference } from "../../document/domain/document-object-reference.js";
import type { MalwareEvidence } from "../../document/domain/malware-scan-result.js";
import { documentFileKey, isNonTerminalFileScanStatus, sameObjectVersion, type DocumentFile, type DocumentFileScanStatus } from "../domain/document-file.js";
import { documentVersionEventKey, type DocumentVersionEvent } from "../domain/document-version-event.js";
import { documentVersionKey, type DocumentVersion } from "../domain/document-version.js";
import type { DocumentArchiveStore } from "../ports/document-archive-store.js";
import type { DocumentArchiveIdGenerator } from "./id-generator.js";

export interface ApplyFileScanResultDeps {
  store: DocumentArchiveStore;
  tableName: string;
  ids: DocumentArchiveIdGenerator;
  now?: () => string;
}

export interface ApplyFileScanResultInput {
  tenantId: string;
  documentId: string;
  seq: number;
  fileId: string;
  /** The object reference THIS physical event actually observed (S3 Object Created / GuardDuty
   * finding) - never the stored `DocumentFile.quarantineObject`, per D-163 ¶õ1: the first event
   * to arrive consolidates FROM this triple, the second is verified AGAINST what got
   * consolidated, never re-derived from the Version or fabricated. */
  observedObject: DocumentObjectReference;
  uploadEvidence?: UploadEvidence;
  malwareEvidence?: MalwareEvidence;
}

export type ApplyFileScanResultOutcome =
  | { outcome: "AWAITING" }
  | { outcome: "IGNORED_WRONG_VERSION" }
  | { outcome: "IGNORED_STALE" }
  | { outcome: "REJECTED"; status: Extract<DocumentFileScanStatus, "REJECTED" | "UNSUPPORTED"> }
  | { outcome: "READY_TO_PROMOTE"; sourceObject: DocumentObjectReference };

const MAX_OCC_RETRIES = 10;

/** Re-reads the file fresh on every attempt - never assumes the caller's in-memory copy is
 * current, since the upload-finalizer and malware-scan events for the SAME file may land
 * concurrently, exactly the corridor `decideNextAction()`'s doc comment describes for M6. */
export async function applyFileScanResult(deps: ApplyFileScanResultDeps, input: ApplyFileScanResultInput): Promise<ApplyFileScanResultOutcome> {
  const now = deps.now ?? (() => new Date().toISOString());
  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);

  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
    const file = await deps.store.get<DocumentFile>(key);
    if (!file) return { outcome: "IGNORED_STALE" };

    // Terminal-state idempotency FIRST, before any correlation check: a repeated/late event
    // against a file already at CLEAN/REJECTED/UNSUPPORTED/TIMEOUT is a legitimate replay, never
    // re-evaluated against the consolidated triple (D-163 ¶õ1's correlation only ever applies
    // while `scanStatus IN (PENDING_UPLOAD, SCANNING)`).
    if (!isNonTerminalFileScanStatus(file.scanStatus)) return { outcome: "IGNORED_STALE" };

    // D-163 ¶õ1 symmetric evidence correlation: `quarantineObject.versionId === ""` means no
    // physical event has consolidated it yet (`reserveFiles()`'s placeholder) - this event IS
    // the first, so it wins unconditionally. Once consolidated, every further event must match
    // that exact triple or is a stale/duplicate/wrong-object event, never re-consolidated.
    const consolidated = file.quarantineObject.versionId !== "";
    if (consolidated && !sameObjectVersion(file.quarantineObject, input.observedObject)) {
      return { outcome: "IGNORED_WRONG_VERSION" };
    }

    const mergedUploadEvidence = input.uploadEvidence ?? file.uploadEvidence;
    const mergedMalwareEvidence = input.malwareEvidence ?? file.malwareEvidence;

    const decision = decideNextAction({
      currentStatus: file.scanStatus,
      uploadValid: mergedUploadEvidence?.valid,
      uploadEvidence: mergedUploadEvidence,
      malwareEvidence: mergedMalwareEvidence,
    });

    const evidenceSet: Record<string, unknown> = {};
    if (input.uploadEvidence) evidenceSet["uploadEvidence"] = input.uploadEvidence;
    if (input.malwareEvidence) evidenceSet["malwareEvidence"] = input.malwareEvidence;
    const quarantineObjectSet = consolidated ? {} : { quarantineObject: input.observedObject };

    if (decision.action === "IGNORE_STALE_EVENT") return { outcome: "IGNORED_STALE" };

    if (decision.action === "AWAIT_MORE_EVIDENCE") {
      const nowTs = now();
      try {
        await deps.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: deps.tableName,
              key,
              tenantId: input.tenantId,
              expectedVersion: file.version,
              // SCANNING marks "at least one physical event has landed" independent of
              // decideNextAction's own vocabulary (it only ever returns actions, never this
              // specific status) - a no-op re-SET when the file was already SCANNING.
              set: { scanStatus: "SCANNING", ...quarantineObjectSet, ...evidenceSet },
              now: nowTs,
            }),
          },
        ]);
        return { outcome: "AWAITING" };
      } catch (err) {
        if (isTransactionCanceled(err)) continue; // concurrent half of evidence landed - retry fresh.
        throw err;
      }
    }

    if (decision.action === "PROMOTE") {
      // Never claims CLEAN itself - zero writes here, same discipline M6's PROMOTE branch
      // established (`advance-after-evidence.ts`'s doc comment). The evidence merge that got us
      // here was already persisted by an earlier AWAITING call (PROMOTE is only reachable once
      // BOTH evidences exist, and only one arrives per call) - nothing new to consolidate.
      const sourceObject = consolidated ? file.quarantineObject : input.observedObject;
      return { outcome: "READY_TO_PROMOTE", sourceObject };
    }

    // decision.action === "REJECT": terminal transition - counters + GSI8 removal happen in the
    // SAME transaction as the file's own write (Decision 6/Bloqueador 9: a crash between the two
    // would leave pendingFileScans stuck above zero forever, permanently blocking acceptVersion).
    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
    if (!version) return { outcome: "IGNORED_STALE" }; // Version cannot be removed once files exist - fail closed if this ever changes.

    const infected = mergedMalwareEvidence?.status === "THREATS_FOUND";
    const nowTs = now();
    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key,
          tenantId: input.tenantId,
          expectedVersion: file.version,
          set: { scanStatus: decision.status, ...quarantineObjectSet, ...evidenceSet },
          // D-163 ¶õ5/¶õ6: any transaction reaching a terminal scanStatus removes the sparse GSI8
          // reconciliation pointer in the same write - never a separate cleanup step.
          remove: ["GSI8PK", "GSI8SK"],
          now: nowTs,
        }),
      },
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
          tenantId: input.tenantId,
          expectedVersion: version.version,
          set: { pendingFileScans: version.pendingFileScans - 1, ...(infected ? { infectedFileScans: version.infectedFileScans + 1 } : {}) },
          now: nowTs,
        }),
      },
    ];
    // D-163 ¶õ2 (Rodada 4): FILE_REJECTED_INFECTED is emitted only for the infection-caused
    // REJECTED transition, never for a plain upload-invalid REJECTED or an UNSUPPORTED one - the
    // audit trail exists specifically so "why was this file removed from consideration" is
    // answerable without re-deriving it from malwareEvidence.
    if (infected) {
      entries.push({
        Put: buildVersionedCreate(
          deps.tableName,
          buildFileRejectedEvent(deps, input.tenantId, input.documentId, input.seq, version.versionId, version.state, input.fileId, file.scanStatus, decision.status, nowTs) as unknown as Record<string, unknown> & EntityKey,
        ),
      });
    }
    try {
      await deps.store.transactWrite(entries);
      return { outcome: "REJECTED", status: decision.status };
    } catch (err) {
      if (isTransactionCanceled(err)) continue;
      throw err;
    }
  }

  throw new Error(`applyFileScanResult exhausted retries for file ${input.fileId} under contention.`);
}

export type ConfirmFileScanCleanOutcome = "CONFIRMED" | "IGNORED_STALE";

export interface ConfirmFileScanCleanInput {
  tenantId: string;
  documentId: string;
  seq: number;
  fileId: string;
  cleanObject: DocumentObjectReference;
}

/**
 * confirmFileScanClean - the PROMOTE confirmation step, called only once the caller (a future
 * S3/GuardDuty event worker, out of scope for this slice) has already copied `sourceObject` to
 * `cleanObject` and verified it (size/checksum, same discipline M6's `advanceAfterEvidence()`
 * requires before it will persist CLEAN). Idempotent against a file that already moved off
 * SCANNING/PENDING_UPLOAD by the time this is called (e.g. concurrently rejected) - never
 * resurrects a terminal file back toward CLEAN.
 */
export async function confirmFileScanClean(deps: ApplyFileScanResultDeps, input: ConfirmFileScanCleanInput): Promise<ConfirmFileScanCleanOutcome> {
  const now = deps.now ?? (() => new Date().toISOString());
  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);

  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
    const file = await deps.store.get<DocumentFile>(key);
    if (!file) return "IGNORED_STALE";
    if (!isNonTerminalFileScanStatus(file.scanStatus)) return "IGNORED_STALE";

    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
    if (!version) return "IGNORED_STALE";

    const nowTs = now();
    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key,
          tenantId: input.tenantId,
          expectedVersion: file.version,
          set: { scanStatus: "CLEAN", cleanObject: input.cleanObject },
          remove: ["GSI8PK", "GSI8SK"],
          now: nowTs,
        }),
      },
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
          tenantId: input.tenantId,
          expectedVersion: version.version,
          set: { pendingFileScans: version.pendingFileScans - 1 },
          now: nowTs,
        }),
      },
    ];
    try {
      await deps.store.transactWrite(entries);
      return "CONFIRMED";
    } catch (err) {
      if (isTransactionCanceled(err)) continue;
      throw err;
    }
  }

  throw new Error(`confirmFileScanClean exhausted retries for file ${input.fileId} under contention.`);
}

export type ApplyFileScanTimeoutOutcome = "TIMED_OUT" | "IGNORED_STALE";

export interface ApplyFileScanTimeoutInput {
  tenantId: string;
  documentId: string;
  seq: number;
  fileId: string;
  /** The exact GSI8 pointer the reconciliation scan observed (D-163 round4 ¶õ3) - the
   * transaction below conditions on it verbatim, so a candidate whose deadline changed or
   * that already reached a terminal state between the scan and this write is naturally
   * skipped (ConditionalCheckFailed -> IGNORED_STALE) rather than double-processed. */
  observedGsi8Pointer: { GSI8PK: string; GSI8SK: string };
}

/**
 * applyFileScanTimeout - the reconciliation worker's terminal transition
 * (PENDING_UPLOAD/SCANNING -> TIMEOUT), symmetric to applyFileScanResult's REJECT branch but
 * reached by deadline rather than by a physical S3/GuardDuty event. Deliberately reuses the
 * exact same counter/GSI8-removal mechanism (buildVersionedUpdate, no new pattern) - the only
 * new ingredient is conditioning on the observed GSI8 pointer itself, which a physical-event
 * caller never needs (it always re-reads the file fresh and checks scanStatus, never a stale
 * discovery pointer).
 */
export async function applyFileScanTimeout(deps: ApplyFileScanResultDeps, input: ApplyFileScanTimeoutInput): Promise<ApplyFileScanTimeoutOutcome> {
  const now = deps.now ?? (() => new Date().toISOString());
  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);

  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
    const file = await deps.store.get<DocumentFile>(key);
    if (!file) return "IGNORED_STALE";
    if (!isNonTerminalFileScanStatus(file.scanStatus)) return "IGNORED_STALE";

    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
    if (!version) return "IGNORED_STALE"; // Version cannot be removed once files exist - fail closed if this ever changes.

    const nowTs = now();
    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key,
          tenantId: input.tenantId,
          expectedVersion: file.version,
          set: { scanStatus: "TIMEOUT" },
          remove: ["GSI8PK", "GSI8SK"],
          // Exact-pointer fence (round4-claude-final.md ¶õ3): closes the race where the
          // candidate this scan observed already advanced (new deadline, or terminal) by the
          // time this write lands - never conditioned on `file.version` alone, since a
          // concurrent SCANNING->TIMEOUT-eligible re-write could bump version without changing
          // eligibility in a way the scan already accounted for.
          extraConditions: [
            { expression: "#gsi8pk = :gsi8pk", names: { "#gsi8pk": "GSI8PK" }, values: { ":gsi8pk": input.observedGsi8Pointer.GSI8PK } },
            { expression: "#gsi8sk = :gsi8sk", names: { "#gsi8sk": "GSI8SK" }, values: { ":gsi8sk": input.observedGsi8Pointer.GSI8SK } },
          ],
          now: nowTs,
        }),
      },
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
          tenantId: input.tenantId,
          expectedVersion: version.version,
          set: { pendingFileScans: version.pendingFileScans - 1 },
          now: nowTs,
        }),
      },
    ];
    try {
      await deps.store.transactWrite(entries);
      return "TIMED_OUT";
    } catch (err) {
      if (isTransactionCanceled(err)) return "IGNORED_STALE"; // lost the race - a concurrent event/sweep already claimed this file.
      throw err;
    }
  }

  throw new Error(`applyFileScanTimeout exhausted retries for file ${input.fileId} under contention.`);
}

function buildFileRejectedEvent(
  deps: ApplyFileScanResultDeps,
  tenantId: string,
  documentId: string,
  seq: number,
  versionId: string,
  versionState: DocumentVersionEvent["toState"],
  fileId: string,
  fromFileScanStatus: DocumentFileScanStatus,
  toFileScanStatus: DocumentFileScanStatus,
  occurredAt: string,
): DocumentVersionEvent {
  return {
    ...documentVersionEventKey(tenantId, documentId, seq, deps.ids.newEventId()),
    entityType: "DocumentVersionEvent",
    tenantId,
    documentId,
    versionId,
    type: "FILE_REJECTED_INFECTED",
    // `fromState`/`toState` record the Version's own (unchanged) state as informational context
    // only - the real transition this event carries is fromFileScanStatus/toFileScanStatus
    // below, per document-version-event.ts's doc comment on this event type.
    fromState: versionState,
    toState: versionState,
    fileId,
    fromFileScanStatus,
    toFileScanStatus,
    actor: "SYSTEM",
    occurredAt,
  };
}

=== src/shared/dynamodb/occ.ts
/**
 * Optimistic concurrency control helper - implementation-blueprint.md #5.2 / M0 deliverable.
 *
 * M0 ships no tables/Lambdas yet (that's M1+), so this module only builds the DynamoDB
 * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
 * so it stays testable without AWS credentials/mocks. M1 wires these builders into the
 * actual DocumentClient calls once tables exist.
 */

export interface EntityKey {
  PK: string;
  SK: string;
}

export interface VersionedUpdateInput {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  /** Additional SET clauses beyond version/updatedAt, e.g. { status: "CANCELLED" }. */
  set: Record<string, unknown>;
  /** Attribute names to REMOVE atomically in the same conditional update - e.g. dropping
   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
   * since M3 (occ.ts was SET-only, cancelled GSI3 pointers were left orphaned because
   * nothing actively queried them); M3.5 needs REMOVE for real because GSI6 pointers ARE
   * actively queried by reconciliation. */
  remove?: string[];
  now?: string;
  /** Extra ConditionExpression clauses ANDed to the base condition, each with its own
   * caller-supplied names/values (W3-06 purge worker's claim/fence conditions - e.g. checking
   * `legalHold`/`GSI6PK`/`purgeAfter` atomically alongside the version check). Each entry's
   * `expression` is wrapped in its own parentheses before being ANDed in, so callers never need
   * to hand-balance parens against the base condition. Placeholder names/values MUST NOT collide
   * with the base condition's (#version/#tenantId/#updatedAt/:expectedVersion/:tenantId/:one/:now)
   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
   * throws before building an invalid ConditionExpression rather than silently overwriting one
   * caller's placeholder with another's. */
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}

export interface DynamoUpdateCommandInput {
  TableName: string;
  Key: EntityKey;
  UpdateExpression: string;
  ConditionExpression: string;
  /** Optional, not `Record<string, string>` with an empty-object default (Wave B2B-14, D-119
   * real finding): DynamoDB's TransactWriteItems rejects an explicitly-empty
   * ExpressionAttributeNames outright (`ValidationException: ExpressionAttributeNames must
   * not be empty`) - the key must be OMITTED, never `{}`, when a caller has nothing to map.
   * `buildVersionedUpdate()` below never hits this (it always maps at least #version/
   * #tenantId/#updatedAt), but a hand-rolled Update (update-organization-settings.ts) can. */
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues: Record<string, unknown>;
}

/**
 * Builds an UpdateItem input enforcing implementation-blueprint.md #5.2's exact
 * ConditionExpression: attribute_exists(PK) AND attribute_exists(SK) AND
 * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
 */
export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
  const now = input.now ?? new Date().toISOString();

  const names: Record<string, string> = {
    "#version": "version",
    "#tenantId": "tenantId",
    "#updatedAt": "updatedAt",
  };
  const values: Record<string, unknown> = {
    ":expectedVersion": input.expectedVersion,
    ":tenantId": input.tenantId,
    ":one": 1,
    ":now": now,
  };

  const setClauses = ["#version = #version + :one", "#updatedAt = :now"];
  let i = 0;
  for (const [field, value] of Object.entries(input.set)) {
    const nameKey = `#set${i}`;
    const valueKey = `:set${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    setClauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  const removeClauses: string[] = [];
  let j = 0;
  for (const field of input.remove ?? []) {
    const nameKey = `#rem${j}`;
    names[nameKey] = field;
    removeClauses.push(nameKey);
    j += 1;
  }

  const expression =
    removeClauses.length > 0
      ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}`
      : `SET ${setClauses.join(", ")}`;

  const baseConditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved/generated key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved/generated key: ${valueKey}`);
      values[valueKey] = value;
    }
    baseConditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: expression,
    ConditionExpression: baseConditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export interface DynamoPutCommandInput {
  TableName: string;
  Item: Record<string, unknown>;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

/**
 * Builds a PutItem input for first-time creation of a versioned entity, per
 * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
 * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
 */
export function buildVersionedCreate(tableName: string, item: Record<string, unknown> & EntityKey): DynamoPutCommandInput {
  return {
    TableName: tableName,
    Item: item,
    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
  };
}

/**
 * Builds a PutItem input that overwrites an existing item gated on a caller-supplied
 * equality condition over specific fields (not a `version` counter) - the pattern
 * `TenantQuotaService`'s `updateConditional`/`putIfAbsent` already use standalone
 * (`identity-store.ts`'s `updateConditional`: full-item PutCommand conditioned on
 * `count`/`resetAt` still matching what was read, not an UpdateItem). Exists so quota's
 * transaction-boundary migration (W3-07 writer inventory) can build the same conditional
 * Put as a `TransactWriteItems` entry via `occ.ts` instead of a hand-written
 * ConditionExpression, per this module's own "never hand-written ConditionExpression"
 * discipline. `names`/`values` follow the same collision-checked merge as
 * `buildVersionedUpdate`'s `extraConditions`.
 */
export function buildConditionalPut(input: {
  tableName: string;
  item: Record<string, unknown> & EntityKey;
  conditionExpression: string;
  names?: Record<string, string>;
  values?: Record<string, unknown>;
}): DynamoPutCommandInput {
  return {
    TableName: input.tableName,
    Item: input.item,
    ConditionExpression: input.conditionExpression,
    ExpressionAttributeNames: input.names,
    ExpressionAttributeValues: input.values,
  };
}

/**
 * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
 * (2026-08-19, Engineering Maturity Review): that was the only place they were declared,
 * but reminder/ports/reminder-store.ts already re-exported them from there (a cross-module
 * ports->ports dependency), and expiration/domain/audit-event.ts imported them directly,
 * which is a domain->ports boundary violation dependency-cruiser catches but ESLint's
 * text-matching no-restricted-imports rule cannot (it only matches literal specifier
 * substrings, and this codebase's relative imports never contain "modules/"). Reuses the
 * inner Put/Update shapes already defined above instead of re-declaring them a third time.
 */
export interface TransactPutEntry {
  Put: DynamoPutCommandInput;
}

export interface TransactUpdateEntry {
  Update: DynamoUpdateCommandInput;
}

export interface DynamoConditionCheckCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactConditionCheckEntry {
  ConditionCheck: DynamoConditionCheckCommandInput;
}

export interface DynamoDeleteCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression?: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactDeleteEntry {
  Delete: DynamoDeleteCommandInput;
}

/**
 * Builds a version-conditioned Delete input (W3-06 purge worker's terminal step - a Document
 * row can only be physically removed if it is still at the exact version/state the claim
 * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
 * `UpdateItem`). `extraConditions` uses the same collision-checked merge as
 * `buildVersionedUpdate` - see that function's docs.
 */
export function buildVersionedDelete(input: {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}): DynamoDeleteCommandInput {
  const names: Record<string, string> = { "#version": "version", "#tenantId": "tenantId" };
  const values: Record<string, unknown> = { ":expectedVersion": input.expectedVersion, ":tenantId": input.tenantId };
  const conditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];

  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved key: ${valueKey}`);
      values[valueKey] = value;
    }
    conditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    ConditionExpression: conditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

/**
 * Sibling of `buildConditionalPut` for `DeleteItem` - symmetric reasoning: `buildVersionedDelete`
 * above always ANDs `#version = :expectedVersion`, which only makes sense for a versioned
 * (mutable) entity. An append-only entity that is NEVER updated after creation (e.g. the
 * `AuditEvent` family - `expiration`/`organization`/`subject`/`activity` modules' audit rows,
 * D-153) has no `version` counter to check at all, so forcing one through `buildVersionedDelete`
 * would require faking a field that does not exist on the row. Callers instead supply their own
 * equality condition over whatever field they observed at scan time (same
 * "hasn't changed since scan" intent as `buildVersionedDelete`'s `extraConditions`, just without
 * a version to anchor it to) - `names`/`values` follow the same collision-free-by-caller-control
 * discipline as `buildConditionalPut` (no generated placeholders to collide with here, since this
 * builder adds none of its own beyond `attribute_exists`).
 */
export function buildConditionalDelete(input: {
  tableName: string;
  key: EntityKey;
  conditionExpression: string;
  names?: Record<string, string>;
  values?: Record<string, unknown>;
}): DynamoDeleteCommandInput {
  return {
    TableName: input.tableName,
    Key: input.key,
    ConditionExpression: input.conditionExpression,
    ExpressionAttributeNames: input.names,
    ExpressionAttributeValues: input.values,
  };
}

export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;

/**
 * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
 * expected version (and, optionally, still holds a given attribute value) - used to fence
 * a commit against a fact read earlier in the same operation but not itself written by
 * this transaction (e.g. reminder dispatch asserting the ReminderPolicy it read is still
 * enabled at the version it read, per the BLOCKER-B freshness-fencing fix: reading a row
 * and later writing elsewhere based on that read is a TOCTOU gap unless the read fact is
 * re-asserted atomically inside the same TransactWriteItems as the write it gates).
 */
export function buildVersionConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  expectedVersion: number;
  /** Extra attribute=value equality conditions beyond version, e.g. { enabled: true, status: "ACTIVE" }. */
  extra?: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = { "#version": "version" };
  const values: Record<string, unknown> = { ":version": input.expectedVersion };
  const clauses = ["#version = :version"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra ?? {})) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/**
 * Sibling of `buildVersionConditionCheck` for facts that have no version to pin - e.g.
 * BLOCKER-B's ITEM-scoped-policy integrity check (a policy references an item's
 * existence/status/tenant, not a specific item version - referencing an item doesn't pin
 * it to a version the way an occurrence's materialization does). `extra` must include at
 * least one condition (an existence-only check with no `extra` would degenerate to
 * `attribute_exists(PK)`, which every real row satisfies trivially - not useful on its own).
 */
export function buildExistenceConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  extra: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = {};
  const values: Record<string, unknown> = {};
  const clauses = ["attribute_exists(PK)"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra)) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
 * ConditionExpression - used by callers to distinguish OCC/idempotency conflicts from other
 * DynamoDB errors without importing the SDK type. */
export const TRANSACTION_CANCELED = "TransactionCanceledException";

export function isTransactionCanceled(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === TRANSACTION_CANCELED
  );
}

/**
 * Reads the per-item `CancellationReasons` array off a `TransactionCanceledException` (verify
 * with `isTransactionCanceled` first) - the real AWS SDK error carries one entry per transact
 * item, each with a `Code` ("None" for an item that did not cause the cancellation,
 * "ConditionalCheckFailed" for one that did). Wave B2B-5 (D-095, Codex Rodada 3 achado): a
 * `TransactWriteItems` with more than one conditioned entry must never collapse "the
 * transaction was canceled" into "MY condition specifically failed" - a caller distinguishing,
 * say, a cap-guard `Update` at index 0 from an unrelated `Put` collision at index 2 needs the
 * reason for the SPECIFIC index it cares about, not just the fact that something failed.
 * Returns `undefined` (never throws) for any error shape that doesn't carry this array - callers
 * fall back to treating the cancellation as "index unknown" rather than guessing.
 */
export function getCancellationReasonCodes(err: unknown): string[] | undefined {
  if (typeof err !== "object" || err === null || !("CancellationReasons" in err)) return undefined;
  const reasons = (err as { CancellationReasons?: unknown }).CancellationReasons;
  if (!Array.isArray(reasons)) return undefined;
  return reasons.map((reason) => (typeof reason === "object" && reason !== null && "Code" in reason ? String((reason as { Code?: unknown }).Code) : "Unknown"));
}

/**
 * Builds an UpdateItem input for a narrow, single-purpose guard write: set one attribute exactly
 * once, gated on it not already existing - no `version` counter, no `tenantId` condition (unlike
 * `buildVersionedUpdate`, whose reserved condition requires `tenantId` and is therefore unusable
 * for a genuinely tenantless entity). Wave B2B-5 (D-095, Codex Rodada 2/3 achado): `GlobalUser`
 * (identity/persistence/global-user-repository.ts) is tenantless, so its
 * `hasCreatedOrganization` cap flag needs this instead. Not a general-purpose OCC update - an
 * entity that needs real optimistic concurrency later should get its own tenantless variant of
 * `buildVersionedUpdate`, not a generalization of this narrow helper.
 */
export function buildAttributeOnceUpdate(input: { tableName: string; key: EntityKey; attribute: string; value: unknown }): DynamoUpdateCommandInput {
  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: "SET #attr = :value",
    ConditionExpression: "attribute_not_exists(#attr)",
    ExpressionAttributeNames: { "#attr": input.attribute },
    ExpressionAttributeValues: { ":value": input.value },
  };
}

/** Name of the AWS SDK error thrown when a ConditionExpression fails - used by callers
 * to distinguish OCC conflicts from other DynamoDB errors without importing the SDK type. */
export const CONDITIONAL_CHECK_FAILED = "ConditionalCheckFailedException";

export function isConditionalCheckFailed(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === CONDITIONAL_CHECK_FAILED
  );
}

=== src/modules/extraction/application/start-extraction-run.ts
/**
 * ExtractionStarterWorker's pure orchestration (`implementation-blueprint.md` ¶õ12.5,
 * `claude-reconciliation-final-design.md` ¶õ1.1/¶õ2 "LoadMetadata"). Triggered by the S3
 * "Object Created" event on the clean bucket; creates the idempotent `ExtractionRun` and, only
 * on the FIRST successful creation, starts the Step Functions Standard execution.
 */
import { documentKey, type Document } from "../../document/domain/document.js";
import { extractionRunKey, deriveExtractionRunId, type ExtractionRun } from "../domain/extraction-run.js";
import { PIPELINE_VERSION_V1 } from "../domain/field-schema.js";
import type { DocumentReader } from "../ports/document-reader.js";
import type { ExtractionRunStore } from "../ports/extraction-run-store.js";
import type { ExtractionExecutionStarter } from "../ports/extraction-execution-starter.js";

export interface StartExtractionRunDeps {
  documents: DocumentReader;
  runs: ExtractionRunStore;
  executions: ExtractionExecutionStarter;
  now?: () => string;
}

export interface StartExtractionRunInput {
  tenantId: string;
  itemId: string;
  documentId: string;
  /** The business correlationId established by the caller (extraction-starter-handler.ts) ƒ?"
   * threaded into the Step Functions execution input so every downstream task handler can
   * join its logs back to this same run. See ExtractionExecutionInput's doc comment. */
  correlationId: string;
  cleanObject: { bucket: string; key: string; versionId: string };
}

export type StartExtractionRunOutcome = "STARTED" | "ALREADY_RUNNING" | "DOCUMENT_NOT_FOUND";

/** The clean-bucket promotion copy and the Document's own `status: "CLEAN"` transition are two
 * separate writes (advanceAfterEvidence() copies the object BEFORE the transactional status
 * update) - a real, expected race where the S3 event can arrive before the Document read below
 * observes CLEAN yet. Distinct from DOCUMENT_NOT_FOUND (never retryable - the document genuinely
 * doesn't exist), this IS retryable: thrown, not returned, so the runtime handler's normal SQS
 * batch-item-failure path redelivers it instead of silently dropping a real, in-flight upload. */
export class DocumentNotCleanYetError extends Error {
  constructor(documentId: string, status: string) {
    super(`Document ${documentId} is not CLEAN yet (status=${status}) - retrying.`);
    this.name = "DocumentNotCleanYetError";
  }
}

export async function startExtractionRun(deps: StartExtractionRunDeps, input: StartExtractionRunInput): Promise<StartExtractionRunOutcome> {
  const doc = await deps.documents.get<Document>(documentKey(input.tenantId, input.itemId, input.documentId), true);
  if (!doc) return "DOCUMENT_NOT_FOUND";
  if (doc.status !== "CLEAN") throw new DocumentNotCleanYetError(input.documentId, doc.status);

  const documentVersion = doc.version;
  const pipelineVersion = PIPELINE_VERSION_V1;
  const runId = deriveExtractionRunId(input.tenantId, input.documentId, documentVersion, pipelineVersion);
  const now = deps.now?.() ?? new Date().toISOString();

  const run: ExtractionRun = {
    ...extractionRunKey(input.tenantId, input.documentId, runId),
    entityType: "ExtractionRun",
    tenantId: input.tenantId,
    itemId: input.itemId,
    documentId: input.documentId,
    documentVersion,
    runId,
    pipelineVersion,
    status: "RUNNING",
    startedAt: now,
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const created = await deps.runs.putIfAbsent(run);

  // startExecution is called EVERY time, whether the DynamoDB record was just created or
  // already existed - deliberately not gated on `created`. Gating it would orphan a run
  // whenever putIfAbsent succeeds but the subsequent startExecution call fails transiently
  // (network blip, throttling): the record would sit in RUNNING forever with no real Step
  // Functions execution behind it, and a retry would find the record already there and skip
  // starting one. Instead, the real dedup mechanism is Step Functions' own execution-name
  // uniqueness (`runId` as `name`, see ExtractionExecutionStarter's doc comment) - calling
  // StartExecution again with the same name and the same input is itself idempotent at the
  // AWS API level, so this stays safe to call unconditionally on every retry.
  await deps.executions.startExecution({
    name: runId,
    input: {
      tenantId: input.tenantId,
      itemId: input.itemId,
      documentId: input.documentId,
      documentVersion,
      runId,
      pipelineVersion,
      correlationId: input.correlationId,
      cleanObject: input.cleanObject,
      // RunTextract's classifier needs these (see ExtractionExecutionInput's doc comment) -
      // `doc` was already fetched above, no extra read.
      fileName: doc.fileName,
      contentType: doc.mediaType,
    },
  });

  return created ? "STARTED" : "ALREADY_RUNNING";
}

=== src/modules/extraction/persistence/dynamodb-extraction-run-store.ts
/** Real DynamoDB adapter for ExtractionRunStore. Same translation pattern as
 * DynamoDbDocumentStore/DynamoDbReminderStore. */
import { GetCommand, PutCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey } from "../ports/extraction-run-store.js";
import type { ExtractionRunStore } from "../ports/extraction-run-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";

export class DynamoDbExtractionRunStore implements ExtractionRunStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
      return result.Item as T | undefined;
    } catch (err) {
      throw mapDynamoError(err, "ExtractionRunStore.get");
    }
  }

  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "ExtractionRunStore.putIfAbsent");
    }
  }

  async updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean> {
    const command = buildVersionedUpdate({
      tableName: this.tableName,
      key,
      tenantId,
      expectedVersion,
      set: { status, completedAt },
    });
    try {
      await this.client.send(
        new UpdateCommand({
          TableName: command.TableName,
          Key: command.Key,
          UpdateExpression: command.UpdateExpression,
          ConditionExpression: command.ConditionExpression,
          ExpressionAttributeNames: command.ExpressionAttributeNames,
          ExpressionAttributeValues: command.ExpressionAttributeValues,
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "ExtractionRunStore.updateStatus");
    }
  }
}

=== src/modules/extraction/domain/extraction-run.ts
/**
 * ExtractionRun ƒ?" item-pai de uma execuÇõÇœo do pipeline de extraÇõÇœo (M7, D-035/
 * `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md`,
 * GATE atingido 9,2/9,3). `data-model.md` linha 105: `TENANT#t#DOC#d` / `RUN#<runId>`.
 *
 * IdempotÇ¦ncia da EXECUÇÎÇŸO (nÇœo de cada campo individual): `tenantId|documentId|
 * documentVersion|pipelineVersion` ƒ?" reexecutar a mesma versÇœo de documento sob a mesma versÇœo
 * de pipeline nunca cria um segundo run (data-model.md linha 101). `runId` em si Ç¸ derivado
 * dessa mesma chave (determinÇðstico), nunca gerado aleatoriamente, para que uma segunda
 * invocaÇõÇœo de `ExtractionStarterWorker` para o mesmo evento S3 encontre o run jÇ­ existente em
 * vez de criar um duplicado.
 */
import { createHash } from "node:crypto";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";

export type ExtractionRunStatus = "RUNNING" | "COMPLETED" | "FAILED" | "DISCARDED";

export interface ExtractionRun extends EntityKey {
  entityType: "ExtractionRun";
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  status: ExtractionRunStatus;
  startedAt: string;
  completedAt?: string;
  version: number;
  createdAt: string;
  updatedAt: string;
}

export function extractionRunKey(tenantId: string, documentId: string, runId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#DOC#${documentId}`, SK: `RUN#${runId}` };
}

/** Deterministic runId - reexecuting the same idempotency key (tenantId|documentId|
 * documentVersion|pipelineVersion) always resolves to the same runId, so a second
 * `ExtractionStarterWorker` invocation for a duplicate S3 event (at-least-once delivery)
 * finds the existing run instead of racing to create a second one. Not a security-sensitive
 * value (unlike the guest-token/CSRF hashes elsewhere in this codebase) - no pepper needed,
 * this only needs to be deterministic and collision-resistant. */
export function deriveExtractionRunId(tenantId: string, documentId: string, documentVersion: number, pipelineVersion: string): string {
  const hash = createHash("sha256").update(`${tenantId}|${documentId}|${documentVersion}|${pipelineVersion}`).digest("hex");
  return `run_${hash.slice(0, 32)}`;
}

=== src/workers/requirement-reindex/reindex.ts
/**
 * RequirementReindex ƒ?" D-143 Decision 5 (`requirement.ts`'s module doc comment): the daily
 * job that keeps GSI1's REQSTATUS index coherent with pure TIME-based drift. A Requirement's
 * `status` is written eagerly on every mutation (`linkEvidence`/`unlinkEvidence`/
 * `updateRequirement`), but SATISFIED -> NOT_SATISFIED as `evidenceValidUntil` passes with NO
 * other write ever touching the Requirement is a transition nothing else triggers ƒ?" this
 * worker is that trigger, run once a day (EventBridge Scheduler, same shape as
 * `reminder-reconciliation-handler.ts`'s DST pass).
 *
 * Migrated off the base-table `Scan` onto GSI8 by D-179 slice 4 (4th of 9 workers,
 * `candidate-source.ts`'s doc comment has the full rationale). GSI8 is discovery-only ƒ?" every
 * candidate is re-fetched fresh via `store.get()` before acting, never trusted from the KEYS_ONLY
 * query result, same "GSI8 is never a source of eligibility" posture every other GSI8 consumer
 * holds. No poison-record/DLQ mechanism here (unlike `membership-purge`/`invitation-purge`):
 * there is no tenant-lifecycle fence that can fail permanently for the same candidate, only a
 * transient OCC conflict against a concurrent caller mutation, and that already self-heals on the
 * next scheduled run ƒ?" same reasoning `document-file-reconciliation` used to skip it too.
 *
 * Reads ONLY the Requirement's own denormalized `evidenceState`/`evidenceValidUntil` (never
 * re-fetches the evidence DocumentVersion ƒ?" see `requirement.ts`'s doc comment on why those
 * fields are cached), so this worker is a pure comparison against `now`, not a second source
 * of truth for evidence state.
 */
import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
import type { DocumentArchiveStore } from "../../modules/document-archive/ports/document-archive-store.js";
import { deriveRequirementStatus, requirementGsi1Keys, requirementKey, type Requirement } from "../../modules/document-archive/domain/requirement.js";
import type { RequirementReindexCandidateSource } from "./candidate-source.js";

export interface RequirementReindexDeps {
  store: DocumentArchiveStore;
  candidates: RequirementReindexCandidateSource;
  tableName: string;
  now: () => string;
}

export interface RequirementReindexResult {
  scanned: number;
  transitioned: number;
  skippedConcurrentlyModified: number;
  /** A candidate the GSI8 query returned but a fresh re-read no longer supports transitioning ƒ?"
   * evidence re-linked with a later `evidenceValidUntil` (or cleared entirely) between the query
   * and this worker's own read. Defensive only, same posture as the other GSI8 workers'
   * `skippedNotDue`/`skippedStale`. */
  skippedNotDue: number;
  /** Age in seconds of the oldest due candidate this run's GSI8 query returned. `undefined` when
   * no candidate was returned at all - same observability shape membership-purge/invitation-purge/
   * document-file-reconciliation added for their own GSI8 migrations. */
  oldestCandidateAgeSeconds: number | undefined;
}

/** Hard cap on pages drained per invocation ƒ?" same rationale as
 * `reminder-reconciliation-handler.ts`'s `MAX_PAGES`: bounds a single invocation against a
 * pathological backlog; anything beyond this is picked up by tomorrow's run. */
const MAX_PAGES = 25;

export async function runRequirementReindex(deps: RequirementReindexDeps): Promise<RequirementReindexResult> {
  const result: RequirementReindexResult = { scanned: 0, transitioned: 0, skippedConcurrentlyModified: 0, skippedNotDue: 0, oldestCandidateAgeSeconds: undefined };
  const nowIso = deps.now();
  const nowMs = new Date(nowIso).getTime();

  let exclusiveStartKey: Record<string, unknown> | undefined;
  for (let page = 0; page < MAX_PAGES; page++) {
    const gsi8Page = await deps.candidates.queryDue({ before: nowIso, exclusiveStartKey });

    if (page === 0 && gsi8Page.items.length > 0) {
      const oldest = gsi8Page.items[0]!;
      result.oldestCandidateAgeSeconds = Math.max(0, Math.floor((nowMs - Date.parse(oldest.dueAtIso)) / 1000));
    }

    for (const candidate of gsi8Page.items) {
      result.scanned += 1;
      const requirement = await deps.store.get<Requirement>(requirementKey(candidate.tenantId, candidate.subjectId, candidate.requirementId));
      // Defensive only ƒ?" queryDue()'s own `GSI8SK < :before` filter means this should never be
      // reachable in practice, but eligibility is always re-derived here, never assumed (same
      // posture as invitation-purge/membership-purge's own defensive check): a concurrent
      // linkEvidence pushing evidenceValidUntil forward, or unlinkEvidence clearing it, between
      // the query and this read, is caught here rather than acted on stale.
      if (!requirement || !requirement.evidenceValidUntil || new Date(requirement.evidenceValidUntil).getTime() >= nowMs) {
        result.skippedNotDue += 1;
        continue;
      }

      const nextStatus = deriveRequirementStatus(
        requirement.applicability,
        requirement.evidenceState ? { state: requirement.evidenceState, validUntil: requirement.evidenceValidUntil } : undefined,
        new Date(nowIso),
      );
      if (nextStatus === requirement.status) continue; // already reflects reality (e.g. a concurrent write beat us to it).

      const update = buildVersionedUpdate({
        tableName: deps.tableName,
        key: requirementKey(requirement.tenantId, requirement.subjectId, requirement.requirementId),
        tenantId: requirement.tenantId,
        expectedVersion: requirement.version,
        set: { status: nextStatus, ...requirementGsi1Keys(requirement.tenantId, nextStatus, nowIso, requirement.requirementId) },
        // `nextStatus` is never SATISFIED here (`deriveRequirementStatus` only reaches this branch
        // once `evidenceValidUntil` has already passed `now`) ƒ?" the GSI8 pointer always clears,
        // unconditionally, same as `unlinkEvidence`'s unconditional clear.
        remove: ["GSI8PK", "GSI8SK"],
        now: nowIso,
      });
      try {
        await deps.store.transactWrite([{ Update: update }]);
        result.transitioned += 1;
      } catch (err) {
        // A concurrent write (a caller's own linkEvidence/unlinkEvidence/updateRequirement)
        // already moved this Requirement past the version this scan observed ƒ?" not this
        // worker's job to retry mid-run; the NEXT scheduled run re-evaluates it from a fresh
        // read, same "eventually consistent, never silently wrong" posture as the DST pass.
        if (isTransactionCanceled(err)) {
          result.skippedConcurrentlyModified += 1;
          continue;
        }
        throw err;
      }
    }
    if (!gsi8Page.lastEvaluatedKey) break;
    exclusiveStartKey = gsi8Page.lastEvaluatedKey;
  }

  return result;
}

=== src/workers/requirement-reindex/dynamodb-candidate-source.ts
/**
 * Real DynamoDB adapter for `RequirementReindexCandidateSource` (D-179 slice 4) ƒ?" separate class,
 * wired only into the RequirementReindexWorker Lambda's composition root, same pattern as
 * `document-file-reconciliation/dynamodb-candidate-source.ts`. `queryDue()` is the ONLY GSI8
 * access this role's IAM policy permits (`dynamodb:LeadingKeys` scoped to
 * `WORK#REQUIREMENT_REINDEX`/`DLQ#REQUIREMENT_REINDEX`, `infra/modules/dynamo-table/main.tf`) ƒ?"
 * this worker never touches GSI3/GSI6 either.
 */
import { QueryCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import { mapDynamoError } from "../../shared/dynamodb/sdk-errors.js";
import { auditGlobalIndexAccess, auditGlobalIndexAccessDenied, isAccessDeniedError } from "../../shared/observability/security-audit.js";
import type { RequirementGsi8Candidate, RequirementGsi8Page, RequirementReindexCandidateSource } from "./candidate-source.js";

const PER_INVOCATION_LIMIT = 100;
const GSI8PK_REQUIREMENT_REINDEX = "WORK#REQUIREMENT_REINDEX";

/** Base table `PK` is `TENANT#<tenantId>#SUBJECT#<subjectId>` (`requirementKey()`, `modules/
 * document-archive/domain/requirement.ts`) ƒ?" parsed here, not re-exported from the domain
 * module, since only this adapter ever sees a raw GSI8 row. */
function parseTenantAndSubjectIdFromPk(pk: string): { tenantId: string; subjectId: string } {
  const match = /^TENANT#(.+)#SUBJECT#(.+)$/.exec(pk);
  if (!match || !match[1] || !match[2]) throw new Error(`Malformed base PK for requirement-reindex: ${pk}`);
  return { tenantId: match[1], subjectId: match[2] };
}

/** Base table `SK` is `REQUIREMENT#<requirementId>` (`requirementKey()`). */
function parseRequirementIdFromSk(sk: string): string {
  const match = /^REQUIREMENT#(.+)$/.exec(sk);
  if (!match || !match[1]) throw new Error(`Malformed base SK for requirement-reindex: ${sk}`);
  return match[1];
}

export class DynamoDbRequirementReindexCandidateSource implements RequirementReindexCandidateSource {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async queryDue(input: { before: string; exclusiveStartKey?: Record<string, unknown> }): Promise<RequirementGsi8Page> {
    try {
      const result = await this.client.send(
        new QueryCommand({
          TableName: this.tableName,
          IndexName: "GSI8",
          KeyConditionExpression: "GSI8PK = :pk AND GSI8SK < :before",
          ExpressionAttributeValues: { ":pk": GSI8PK_REQUIREMENT_REINDEX, ":before": input.before },
          Limit: PER_INVOCATION_LIMIT,
          ExclusiveStartKey: input.exclusiveStartKey,
        }),
      );
      const items: RequirementGsi8Candidate[] = (result.Items ?? []).map((raw) => {
        const row = raw as { PK: string; SK: string; GSI8SK: string };
        const { tenantId, subjectId } = parseTenantAndSubjectIdFromPk(row.PK);
        const requirementId = parseRequirementIdFromSk(row.SK);
        return { PK: row.PK, SK: row.SK, dueAtIso: row.GSI8SK.split("#TENANT#")[0]!, tenantId, subjectId, requirementId };
      });
      auditGlobalIndexAccess({ indexName: "GSI8", operation: "Query", component: "requirement-reindex", pageCount: 1, resultCount: items.length });
      return { items, lastEvaluatedKey: result.LastEvaluatedKey };
    } catch (err) {
      if (isAccessDeniedError(err)) {
        auditGlobalIndexAccessDenied({ indexName: "GSI8", operation: "Query", component: "requirement-reindex", awsErrorCode: "AccessDeniedException" });
      }
      throw mapDynamoError(err, "RequirementReindexCandidateSource.queryDue");
    }
  }
}


codex
A maior parte das 11 correções está sustentada pelo código, inclusive o roteador explícito do outbox e a possibilidade de acrescentar um `ConditionCheck` à mesma transação. Encontrei, porém, uma diferença material no “reparo diário”: o worker atual não percorre todos os Requirements satisfeitos; ele descobre apenas candidatos já vencidos pelo GSI8, o que muda a garantia alegada de até 24 horas. Estou verificando também a semântica exata do starter para não confundir criação idempotente do registro com início idempotente da execução.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'src/modules/document-archive/application/apply-file-scan-result.ts'; Get-Content "'$p | Select-Object -Skip 45 -First 230; $p='"'src/shared/dynamodb/occ.ts'; Get-Content "'$p | Select-Object -First 260; rg -n "buildOutboxRelayDeps|IMPORT_PARSE_QUEUE_URL|importParse|redrive|dead_letter|sqs-worker-queue|requirement_reindex" src/runtime/aws/composition infra/main.tf infra/modules schemas test/contract' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 410ms:
  uploadEvidence?: UploadEvidence;
  malwareEvidence?: MalwareEvidence;
}

export type ApplyFileScanResultOutcome =
  | { outcome: "AWAITING" }
  | { outcome: "IGNORED_WRONG_VERSION" }
  | { outcome: "IGNORED_STALE" }
  | { outcome: "REJECTED"; status: Extract<DocumentFileScanStatus, "REJECTED" | "UNSUPPORTED"> }
  | { outcome: "READY_TO_PROMOTE"; sourceObject: DocumentObjectReference };

const MAX_OCC_RETRIES = 10;

/** Re-reads the file fresh on every attempt - never assumes the caller's in-memory copy is
 * current, since the upload-finalizer and malware-scan events for the SAME file may land
 * concurrently, exactly the corridor `decideNextAction()`'s doc comment describes for M6. */
export async function applyFileScanResult(deps: ApplyFileScanResultDeps, input: ApplyFileScanResultInput): Promise<ApplyFileScanResultOutcome> {
  const now = deps.now ?? (() => new Date().toISOString());
  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);

  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
    const file = await deps.store.get<DocumentFile>(key);
    if (!file) return { outcome: "IGNORED_STALE" };

    // Terminal-state idempotency FIRST, before any correlation check: a repeated/late event
    // against a file already at CLEAN/REJECTED/UNSUPPORTED/TIMEOUT is a legitimate replay, never
    // re-evaluated against the consolidated triple (D-163 란1's correlation only ever applies
    // while `scanStatus IN (PENDING_UPLOAD, SCANNING)`).
    if (!isNonTerminalFileScanStatus(file.scanStatus)) return { outcome: "IGNORED_STALE" };

    // D-163 란1 symmetric evidence correlation: `quarantineObject.versionId === ""` means no
    // physical event has consolidated it yet (`reserveFiles()`'s placeholder) - this event IS
    // the first, so it wins unconditionally. Once consolidated, every further event must match
    // that exact triple or is a stale/duplicate/wrong-object event, never re-consolidated.
    const consolidated = file.quarantineObject.versionId !== "";
    if (consolidated && !sameObjectVersion(file.quarantineObject, input.observedObject)) {
      return { outcome: "IGNORED_WRONG_VERSION" };
    }

    const mergedUploadEvidence = input.uploadEvidence ?? file.uploadEvidence;
    const mergedMalwareEvidence = input.malwareEvidence ?? file.malwareEvidence;

    const decision = decideNextAction({
      currentStatus: file.scanStatus,
      uploadValid: mergedUploadEvidence?.valid,
      uploadEvidence: mergedUploadEvidence,
      malwareEvidence: mergedMalwareEvidence,
    });

    const evidenceSet: Record<string, unknown> = {};
    if (input.uploadEvidence) evidenceSet["uploadEvidence"] = input.uploadEvidence;
    if (input.malwareEvidence) evidenceSet["malwareEvidence"] = input.malwareEvidence;
    const quarantineObjectSet = consolidated ? {} : { quarantineObject: input.observedObject };

    if (decision.action === "IGNORE_STALE_EVENT") return { outcome: "IGNORED_STALE" };

    if (decision.action === "AWAIT_MORE_EVIDENCE") {
      const nowTs = now();
      try {
        await deps.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: deps.tableName,
              key,
              tenantId: input.tenantId,
              expectedVersion: file.version,
              // SCANNING marks "at least one physical event has landed" independent of
              // decideNextAction's own vocabulary (it only ever returns actions, never this
              // specific status) - a no-op re-SET when the file was already SCANNING.
              set: { scanStatus: "SCANNING", ...quarantineObjectSet, ...evidenceSet },
              now: nowTs,
            }),
          },
        ]);
        return { outcome: "AWAITING" };
      } catch (err) {
        if (isTransactionCanceled(err)) continue; // concurrent half of evidence landed - retry fresh.
        throw err;
      }
    }

    if (decision.action === "PROMOTE") {
      // Never claims CLEAN itself - zero writes here, same discipline M6's PROMOTE branch
      // established (`advance-after-evidence.ts`'s doc comment). The evidence merge that got us
      // here was already persisted by an earlier AWAITING call (PROMOTE is only reachable once
      // BOTH evidences exist, and only one arrives per call) - nothing new to consolidate.
      const sourceObject = consolidated ? file.quarantineObject : input.observedObject;
      return { outcome: "READY_TO_PROMOTE", sourceObject };
    }

    // decision.action === "REJECT": terminal transition - counters + GSI8 removal happen in the
    // SAME transaction as the file's own write (Decision 6/Bloqueador 9: a crash between the two
    // would leave pendingFileScans stuck above zero forever, permanently blocking acceptVersion).
    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
    if (!version) return { outcome: "IGNORED_STALE" }; // Version cannot be removed once files exist - fail closed if this ever changes.

    const infected = mergedMalwareEvidence?.status === "THREATS_FOUND";
    const nowTs = now();
    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key,
          tenantId: input.tenantId,
          expectedVersion: file.version,
          set: { scanStatus: decision.status, ...quarantineObjectSet, ...evidenceSet },
          // D-163 란5/란6: any transaction reaching a terminal scanStatus removes the sparse GSI8
          // reconciliation pointer in the same write - never a separate cleanup step.
          remove: ["GSI8PK", "GSI8SK"],
          now: nowTs,
        }),
      },
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
          tenantId: input.tenantId,
          expectedVersion: version.version,
          set: { pendingFileScans: version.pendingFileScans - 1, ...(infected ? { infectedFileScans: version.infectedFileScans + 1 } : {}) },
          now: nowTs,
        }),
      },
    ];
    // D-163 란2 (Rodada 4): FILE_REJECTED_INFECTED is emitted only for the infection-caused
    // REJECTED transition, never for a plain upload-invalid REJECTED or an UNSUPPORTED one - the
    // audit trail exists specifically so "why was this file removed from consideration" is
    // answerable without re-deriving it from malwareEvidence.
    if (infected) {
      entries.push({
        Put: buildVersionedCreate(
          deps.tableName,
          buildFileRejectedEvent(deps, input.tenantId, input.documentId, input.seq, version.versionId, version.state, input.fileId, file.scanStatus, decision.status, nowTs) as unknown as Record<string, unknown> & EntityKey,
        ),
      });
    }
    try {
      await deps.store.transactWrite(entries);
      return { outcome: "REJECTED", status: decision.status };
    } catch (err) {
      if (isTransactionCanceled(err)) continue;
      throw err;
    }
  }

  throw new Error(`applyFileScanResult exhausted retries for file ${input.fileId} under contention.`);
}

export type ConfirmFileScanCleanOutcome = "CONFIRMED" | "IGNORED_STALE";

export interface ConfirmFileScanCleanInput {
  tenantId: string;
  documentId: string;
  seq: number;
  fileId: string;
  cleanObject: DocumentObjectReference;
}

/**
 * confirmFileScanClean - the PROMOTE confirmation step, called only once the caller (a future
 * S3/GuardDuty event worker, out of scope for this slice) has already copied `sourceObject` to
 * `cleanObject` and verified it (size/checksum, same discipline M6's `advanceAfterEvidence()`
 * requires before it will persist CLEAN). Idempotent against a file that already moved off
 * SCANNING/PENDING_UPLOAD by the time this is called (e.g. concurrently rejected) - never
 * resurrects a terminal file back toward CLEAN.
 */
export async function confirmFileScanClean(deps: ApplyFileScanResultDeps, input: ConfirmFileScanCleanInput): Promise<ConfirmFileScanCleanOutcome> {
  const now = deps.now ?? (() => new Date().toISOString());
  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);

  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
    const file = await deps.store.get<DocumentFile>(key);
    if (!file) return "IGNORED_STALE";
    if (!isNonTerminalFileScanStatus(file.scanStatus)) return "IGNORED_STALE";

    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
    if (!version) return "IGNORED_STALE";

    const nowTs = now();
    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key,
          tenantId: input.tenantId,
          expectedVersion: file.version,
          set: { scanStatus: "CLEAN", cleanObject: input.cleanObject },
          remove: ["GSI8PK", "GSI8SK"],
          now: nowTs,
        }),
      },
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
          tenantId: input.tenantId,
          expectedVersion: version.version,
          set: { pendingFileScans: version.pendingFileScans - 1 },
          now: nowTs,
        }),
      },
    ];
    try {
      await deps.store.transactWrite(entries);
      return "CONFIRMED";
    } catch (err) {
      if (isTransactionCanceled(err)) continue;
      throw err;
    }
  }

  throw new Error(`confirmFileScanClean exhausted retries for file ${input.fileId} under contention.`);
}

export type ApplyFileScanTimeoutOutcome = "TIMED_OUT" | "IGNORED_STALE";

export interface ApplyFileScanTimeoutInput {
  tenantId: string;
  documentId: string;
  seq: number;
  fileId: string;
  /** The exact GSI8 pointer the reconciliation scan observed (D-163 round4 란3) - the
   * transaction below conditions on it verbatim, so a candidate whose deadline changed or
   * that already reached a terminal state between the scan and this write is naturally
   * skipped (ConditionalCheckFailed -> IGNORED_STALE) rather than double-processed. */
  observedGsi8Pointer: { GSI8PK: string; GSI8SK: string };
}

/**
 * applyFileScanTimeout - the reconciliation worker's terminal transition
 * (PENDING_UPLOAD/SCANNING -> TIMEOUT), symmetric to applyFileScanResult's REJECT branch but
/**
 * Optimistic concurrency control helper - implementation-blueprint.md #5.2 / M0 deliverable.
 *
 * M0 ships no tables/Lambdas yet (that's M1+), so this module only builds the DynamoDB
 * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
 * so it stays testable without AWS credentials/mocks. M1 wires these builders into the
 * actual DocumentClient calls once tables exist.
 */

export interface EntityKey {
  PK: string;
  SK: string;
}

export interface VersionedUpdateInput {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  /** Additional SET clauses beyond version/updatedAt, e.g. { status: "CANCELLED" }. */
  set: Record<string, unknown>;
  /** Attribute names to REMOVE atomically in the same conditional update - e.g. dropping
   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
   * since M3 (occ.ts was SET-only, cancelled GSI3 pointers were left orphaned because
   * nothing actively queried them); M3.5 needs REMOVE for real because GSI6 pointers ARE
   * actively queried by reconciliation. */
  remove?: string[];
  now?: string;
  /** Extra ConditionExpression clauses ANDed to the base condition, each with its own
   * caller-supplied names/values (W3-06 purge worker's claim/fence conditions - e.g. checking
   * `legalHold`/`GSI6PK`/`purgeAfter` atomically alongside the version check). Each entry's
   * `expression` is wrapped in its own parentheses before being ANDed in, so callers never need
   * to hand-balance parens against the base condition. Placeholder names/values MUST NOT collide
   * with the base condition's (#version/#tenantId/#updatedAt/:expectedVersion/:tenantId/:one/:now)
   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
   * throws before building an invalid ConditionExpression rather than silently overwriting one
   * caller's placeholder with another's. */
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}

export interface DynamoUpdateCommandInput {
  TableName: string;
  Key: EntityKey;
  UpdateExpression: string;
  ConditionExpression: string;
  /** Optional, not `Record<string, string>` with an empty-object default (Wave B2B-14, D-119
   * real finding): DynamoDB's TransactWriteItems rejects an explicitly-empty
   * ExpressionAttributeNames outright (`ValidationException: ExpressionAttributeNames must
   * not be empty`) - the key must be OMITTED, never `{}`, when a caller has nothing to map.
   * `buildVersionedUpdate()` below never hits this (it always maps at least #version/
   * #tenantId/#updatedAt), but a hand-rolled Update (update-organization-settings.ts) can. */
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues: Record<string, unknown>;
}

/**
 * Builds an UpdateItem input enforcing implementation-blueprint.md #5.2's exact
 * ConditionExpression: attribute_exists(PK) AND attribute_exists(SK) AND
 * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
 */
export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
  const now = input.now ?? new Date().toISOString();

  const names: Record<string, string> = {
    "#version": "version",
    "#tenantId": "tenantId",
    "#updatedAt": "updatedAt",
  };
  const values: Record<string, unknown> = {
    ":expectedVersion": input.expectedVersion,
    ":tenantId": input.tenantId,
    ":one": 1,
    ":now": now,
  };

  const setClauses = ["#version = #version + :one", "#updatedAt = :now"];
  let i = 0;
  for (const [field, value] of Object.entries(input.set)) {
    const nameKey = `#set${i}`;
    const valueKey = `:set${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    setClauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  const removeClauses: string[] = [];
  let j = 0;
  for (const field of input.remove ?? []) {
    const nameKey = `#rem${j}`;
    names[nameKey] = field;
    removeClauses.push(nameKey);
    j += 1;
  }

  const expression =
    removeClauses.length > 0
      ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}`
      : `SET ${setClauses.join(", ")}`;

  const baseConditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved/generated key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved/generated key: ${valueKey}`);
      values[valueKey] = value;
    }
    baseConditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: expression,
    ConditionExpression: baseConditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export interface DynamoPutCommandInput {
  TableName: string;
  Item: Record<string, unknown>;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

/**
 * Builds a PutItem input for first-time creation of a versioned entity, per
 * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
 * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
 */
export function buildVersionedCreate(tableName: string, item: Record<string, unknown> & EntityKey): DynamoPutCommandInput {
  return {
    TableName: tableName,
    Item: item,
    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
  };
}

/**
 * Builds a PutItem input that overwrites an existing item gated on a caller-supplied
 * equality condition over specific fields (not a `version` counter) - the pattern
 * `TenantQuotaService`'s `updateConditional`/`putIfAbsent` already use standalone
 * (`identity-store.ts`'s `updateConditional`: full-item PutCommand conditioned on
 * `count`/`resetAt` still matching what was read, not an UpdateItem). Exists so quota's
 * transaction-boundary migration (W3-07 writer inventory) can build the same conditional
 * Put as a `TransactWriteItems` entry via `occ.ts` instead of a hand-written
 * ConditionExpression, per this module's own "never hand-written ConditionExpression"
 * discipline. `names`/`values` follow the same collision-checked merge as
 * `buildVersionedUpdate`'s `extraConditions`.
 */
export function buildConditionalPut(input: {
  tableName: string;
  item: Record<string, unknown> & EntityKey;
  conditionExpression: string;
  names?: Record<string, string>;
  values?: Record<string, unknown>;
}): DynamoPutCommandInput {
  return {
    TableName: input.tableName,
    Item: input.item,
    ConditionExpression: input.conditionExpression,
    ExpressionAttributeNames: input.names,
    ExpressionAttributeValues: input.values,
  };
}

/**
 * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
 * (2026-08-19, Engineering Maturity Review): that was the only place they were declared,
 * but reminder/ports/reminder-store.ts already re-exported them from there (a cross-module
 * ports->ports dependency), and expiration/domain/audit-event.ts imported them directly,
 * which is a domain->ports boundary violation dependency-cruiser catches but ESLint's
 * text-matching no-restricted-imports rule cannot (it only matches literal specifier
 * substrings, and this codebase's relative imports never contain "modules/"). Reuses the
 * inner Put/Update shapes already defined above instead of re-declaring them a third time.
 */
export interface TransactPutEntry {
  Put: DynamoPutCommandInput;
}

export interface TransactUpdateEntry {
  Update: DynamoUpdateCommandInput;
}

export interface DynamoConditionCheckCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactConditionCheckEntry {
  ConditionCheck: DynamoConditionCheckCommandInput;
}

export interface DynamoDeleteCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression?: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactDeleteEntry {
  Delete: DynamoDeleteCommandInput;
}

/**
 * Builds a version-conditioned Delete input (W3-06 purge worker's terminal step - a Document
 * row can only be physically removed if it is still at the exact version/state the claim
 * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
 * `UpdateItem`). `extraConditions` uses the same collision-checked merge as
 * `buildVersionedUpdate` - see that function's docs.
 */
export function buildVersionedDelete(input: {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}): DynamoDeleteCommandInput {
  const names: Record<string, string> = { "#version": "version", "#tenantId": "tenantId" };
  const values: Record<string, unknown> = { ":expectedVersion": input.expectedVersion, ":tenantId": input.tenantId };
  const conditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];

  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved key: ${valueKey}`);
      values[valueKey] = value;
    }
    conditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    ConditionExpression: conditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

/**
 * Sibling of `buildConditionalPut` for `DeleteItem` - symmetric reasoning: `buildVersionedDelete`
 * above always ANDs `#version = :expectedVersion`, which only makes sense for a versioned
 * (mutable) entity. An append-only entity that is NEVER updated after creation (e.g. the
 * `AuditEvent` family - `expiration`/`organization`/`subject`/`activity` modules' audit rows,
 * D-153) has no `version` counter to check at all, so forcing one through `buildVersionedDelete`
 * would require faking a field that does not exist on the row. Callers instead supply their own
infra/main.tf:3:# environment: dynamo-table, cognito, api-gateway, lambda-function (x8), sqs-worker-queue,
infra/main.tf:291:    IMPORT_PARSE_QUEUE_URL                     = module.import_parse_dispatch_queue.queue_url
infra/main.tf:323:    IMPORT_PARSE_QUEUE_URL                     = module.import_parse_dispatch_queue.queue_url
infra/main.tf:566:  source = "./modules/sqs-worker-queue"
infra/main.tf:591:  source = "./modules/sqs-worker-queue"
infra/main.tf:636:  source = "./modules/sqs-worker-queue"
infra/main.tf:647:  source = "./modules/sqs-worker-queue"
infra/main.tf:658:  source = "./modules/sqs-worker-queue"
infra/main.tf:973:    module.requirement_reindex_handler.function_name,
infra/main.tf:1006:  source = "./modules/sqs-worker-queue"
infra/main.tf:1017:  source = "./modules/sqs-worker-queue"
infra/main.tf:1166:  source = "./modules/sqs-worker-queue"
infra/main.tf:1627:# de cada fila (j찼 embutida em sqs-worker-queue) continua sendo a rede m챠nima herdada, agora
infra/main.tf:1638:  source = "./modules/sqs-worker-queue"
infra/main.tf:1746:  source = "./modules/sqs-worker-queue"
infra/main.tf:1766:  source = "./modules/sqs-worker-queue"
infra/main.tf:1920:  source = "./modules/sqs-worker-queue"
infra/main.tf:2163:  source = "./modules/sqs-worker-queue"
infra/main.tf:2968:module "requirement_reindex_handler" {
infra/main.tf:2979:    module.table.gsi8_read_policy_json["requirement_reindex"],
infra/main.tf:2980:    module.table.worker_transact_write_policy_json["requirement_reindex"],
infra/main.tf:2985:resource "aws_iam_role" "requirement_reindex_schedule" {
infra/main.tf:2986:  name = "${module.requirement_reindex_handler.function_name}-schedule-role"
infra/main.tf:3001:resource "aws_iam_role_policy" "requirement_reindex_schedule_invoke" {
infra/main.tf:3003:  role = aws_iam_role.requirement_reindex_schedule.id
infra/main.tf:3011:        Resource = module.requirement_reindex_handler.live_alias_arn
infra/main.tf:3017:resource "aws_scheduler_schedule" "requirement_reindex" {
infra/main.tf:3029:    arn      = module.requirement_reindex_handler.live_alias_arn
infra/main.tf:3030:    role_arn = aws_iam_role.requirement_reindex_schedule.arn
infra/main.tf:3043:# requirement_reindex_handler above: this worker's Scan is a base-table operation, not a new
infra/main.tf:3301:# DLQ#QUOTA_TELEMETRY, same pattern as requirement_reindex_handler above.
src/runtime/aws/composition\reminder.ts:65:export function buildOutboxRelayDeps(
src/runtime/aws/composition\reminder.ts:77:  importParseQueueUrl?: string,
src/runtime/aws/composition\reminder.ts:111:      ...(importParseQueueUrl ? { SQS_IMPORT_PARSE_V1: send(importParseQueueUrl) } : {}),
infra/modules\import-observability\variables.tf:7:# on top of the DLQ-age alarm each queue already has via sqs-worker-queue.
infra/modules\dynamo-table\main.tf:218:  # 4's requirement_reindex, D-179/D-186 slice 5's quota_telemetry_purge, D-179/D-187 slice 6's
infra/modules\dynamo-table\main.tf:229:    requirement_reindex          = "REQUIREMENT_REINDEX"
infra/modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:20:    condition     = strcontains(aws_sqs_queue.this.redrive_policy, "\"maxReceiveCount\":5")
infra/modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:21:    error_message = "Main queue's redrive policy must set maxReceiveCount to 5"
infra/modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:25:    condition     = strcontains(aws_sqs_queue.this.redrive_policy, "deadLetterTargetArn")
infra/modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:26:    error_message = "Main queue's redrive policy must target the DLQ"
infra/modules\sqs-worker-queue\outputs.tf:34:# A literal 5, not decoded from aws_sqs_queue.this.redrive_policy - that JSON string also
infra/modules\sqs-worker-queue\main.tf:24:  redrive_policy = jsonencode({
infra/modules\sqs-worker-queue\main.tf:43:  alarm_description   = "ReminderDispatchQueue DLQ has messages older than 1 hour - investigate and redrive per runbook (never blind redrive)."
infra/modules\document-malware-protection\variables.tf:48:  description = "ARN of the SQS queue (sqs-worker-queue module instance) that MalwareResultWorker consumes. The EventBridge rule created here targets it."
infra/modules\ses-notifications\main.tf:31:# the same plan-time-known-policy-document rationale as dynamo-table/sqs-worker-queue.
infra/modules\ses-notifications\outputs.tf:10:# this module doesn't own that queue resource (it's created by the sqs-worker-queue module
infra/modules\spa-hosting\tests\spa_hosting.tftest.hcl:13:  # rendered JSON (same root cause documented in sqs-worker-queue/tests: content assertions on
infra/modules\ses-notifications\variables.tf:19:  description = "AWS region - used only to construct the SNS topic ARN deterministically for the subscription/queue policy documents, same rationale as the dynamo-table/sqs-worker-queue modules (keeps policy documents plan-time-known)."

codex
A Rodada 3 fecha substancialmente os 11 pontos anteriores, mas ainda não alcança o gate de 9,0. Resta uma falha central na garantia de reparo autoritativo e duas especificações menores, porém necessárias para implementação segura.

## Avaliação

1. Promoção concorrente: corrigida

A revisão agora descreve corretamente a concorrência:

- `applyFileScanResult` pode retornar `READY_TO_PROMOTE` para entregas concorrentes ou duplicadas.
- A exclusão efetiva ocorre apenas em `confirmFileScanClean`, mediante releitura fresca e OCC.
- Duas cópias para a mesma chave podem gerar versões S3 diferentes, mas somente a versão registrada pelo primeiro `confirmFileScanClean` torna-se autoritativa; o starter rejeitará o evento da outra versão ao comparar o `cleanObject`.

Isso é coerente com [apply-file-scan-result.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/apply-file-scan-result.ts).

2. Tenant fence: desenho correto

Acrescentar um `ConditionCheck` do Tenant à lista de entradas de cada `TransactWriteItems` é compatível com o código:

- `buildVersionedUpdate` protege apenas existência, OCC e igualdade de `tenantId`; não verifica lifecycle.
- A entrada adicional fecha o TOCTOU na mesma transação.
- Ela precisa estar presente nas transações `AWAITING`, `REJECTED` e `CONFIRMED`, não apenas na confirmação final.

Há apenas uma imprecisão nominal: o tipo existente é `TransactConditionCheckEntry`, não `TransactConditionCheck`. Isso é mecânico e não reduz materialmente a arquitetura.

3. Rollout em duas flags: corrigido

Ativar primeiro o starter e depois o promoter elimina a janela permanente `CLEAN` sem consumidor. A ordem agora está inequívoca e operacionalmente verificável.

4. Destino do outbox: direção correta e aderente ao precedente

O padrão proposto corresponde ao caminho real:

- `OutboxDestination` é uma união fechada.
- `DispatchOutboxRelay` seleciona um sender pelo destino.
- A composição injeta URLs de fila, como já ocorre com `SQS_IMPORT_PARSE_V1`.
- O sweeper consulta pendências por destino.
- `sqs-worker-queue` já fornece DLQ e `maxReceiveCount`.

Portanto, um novo destino, fila, sender na composição e handler SQS é o padrão correto. Isso confere com [outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts), [relay.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/dispatch-outbox-relay/relay.ts) e [dispatch-outbox-relay-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/dispatch-outbox-relay-handler.ts).

Ainda faltou registrar explicitamente que o IAM do refresh também precisa de:

- leitura da `DocumentVersion`;
- `Query` no novo índice reverso;
- consumo da fila.

A frase “fila + leitura/escrita de Requirement” não cobre todas as operações descritas pelo próprio algoritmo.

5. Refresh orientado a estado corrente: corrigido

Usar o evento apenas como wake-up signal e reler `DocumentVersion` e cada `Requirement` resolve corretamente:

- eventos fora de ordem;
- duplicação;
- relink/unlink concorrente;
- resultados eventualmente consistentes do índice como simples candidatos;
- retry OCC sem reaplicar evidência antiga.

Esse ponto está suficientemente especificado.

6. O reindex diário ainda não oferece a garantia alegada

Este é o principal bloqueante.

A Rodada 3 diz que o worker diário “escaneia todo Requirement `SATISFIED` com `evidenceValidUntil` definido”. O código atual não faz isso. [reindex.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/requirement-reindex/reindex.ts) recebe apenas candidatos retornados por `queryDue`, e [dynamodb-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/requirement-reindex/dynamodb-candidate-source.ts) executa:

```text
GSI8PK = WORK#REQUIREMENT_REINDEX AND GSI8SK < now
```

Logo, ele só descobre Requirements cujo `evidenceValidUntil` cacheado já venceu. Apenas trocar a derivação interna para reler a `DocumentVersion` não transforma esse conjunto de descoberta em um sweeper autoritativo.

Exemplos que continuam sem reparo em até 24 horas:

- `validUntil` vivo muda para uma data anterior, mas o valor cacheado no GSI8 ainda está longe no futuro: o Requirement só será descoberto na data antiga.
- Evidência muda de um estado elegível para inelegível, mas o ponteiro GSI8 continua com vencimento futuro.
- Um Requirement sem ponteiro GSI8 fica inconsistente após perda do evento: o reindex atual nunca o encontra.
- Um valor vivo muda de `undefined` para definido, mas o cache permanece sem candidato de reindex.

Portanto, o checklist afirma bounded staleness de 24 horas sem existir um access pattern que enumere todos os registros possivelmente divergentes. Esta é a mesma classe de problema identificada na Rodada 2, agora parcialmente corrigida na releitura, mas não na descoberta.

7. Cardinalidade: corrigida

A distinção “3 agregados / 4 ações” versus “2 agregados / 2 ações” está correta para a confirmação manual proposta. O outbox foi devidamente contado como ação, sem ser tratado como quarto agregado de negócio.

8. Planner compartilhado: suficientemente melhorado

Estados elegíveis, no-op, proveniência, efeito sobre `DocumentVersion` e outbox atômico estão agora contemplados. A regra de zero ou um campo de validade por schema também elimina ambiguidade relevante.

“Exactly-once lógico” é defensável aqui porque o update OCC e o outbox são atômicos; após um commit ambíguo, a releitura observa o mesmo `validUntil` e o planner produz `NO_CHANGE`.

9. Idempotência do starter: correta, mas incompleta na descrição

O código real usa exatamente `PutCommand` com:

```text
attribute_not_exists(PK) AND attribute_not_exists(SK)
```

Logo, a criação idempotente proposta está alinhada com [dynamodb-extraction-run-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/dynamodb-extraction-run-store.ts).

Entretanto, [start-extraction-run.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts) não trata simplesmente o `putIfAbsent=false` como conclusão. Ele chama `startExecution` mesmo quando o run já existia, usando o mesmo nome determinístico. Isso evita o caso:

1. o `Put` do run confirma;
2. `StartExecution` falha;
3. o retry encontra o run existente;
4. se retornasse sucesso imediatamente, o run ficaria órfão para sempre.

A migração precisa preservar essa segunda camada: run determinístico por `versionId + pipelineVersion` e execução Step Functions com nome determinístico, chamada também no retry. Apenas “falha condicional tratada como sucesso idempotente” é insuficiente.

10. Checklist E-014: corrigido

A régua agora mede propriedades observáveis e não conformidade com componentes escolhidos. Os pesos são coerentes e somam 100%. O problema não está mais na formulação, mas no fato de a implementação de reparo proposta ainda não satisfazer o primeiro critério.

## Mudanças exatas necessárias para a Rodada 4

1. Definir um access pattern de reparo que realmente enumere, pelo menos uma vez por dia, todos os Requirements capazes de estar divergentes — por exemplo, um ponteiro GSI de reconciliação permanente independente de `evidenceValidUntil`, paginação por todos os Requirements com evidência, ou outro sweeper completo. Reler `DocumentVersion` somente depois da descoberta pelo atual `GSI8SK < now` não garante staleness de 24 horas.

2. Preservar explicitamente a idempotência ponta a ponta do starter: `putIfAbsent` absorve duplicação do registro, mas retries devem continuar chamando `StartExecution` com nome e input determinísticos. Também esclarecer que a identidade inclui `pipelineVersion`, para que uma nova versão do pipeline possa legitimamente gerar um novo run.

3. Completar o contrato operacional do novo destino: schema versionado do evento/envelope e IAM explícito para consumo SQS, leitura de `DocumentVersion`, query do índice reverso e leitura/escrita de `Requirement`. A política de DLQ/redrive pode referenciar diretamente o comportamento já fornecido por `sqs-worker-queue`.

A arquitetura está muito próxima de convergir, e dez dos onze grupos de correção avançaram de forma convincente. O reparo diário, contudo, ainda promete uma cobertura que seu mecanismo real de descoberta não possui.

Nota: 8.7/10
tokens used
51.100
A Rodada 3 fecha substancialmente os 11 pontos anteriores, mas ainda não alcança o gate de 9,0. Resta uma falha central na garantia de reparo autoritativo e duas especificações menores, porém necessárias para implementação segura.

## Avaliação

1. Promoção concorrente: corrigida

A revisão agora descreve corretamente a concorrência:

- `applyFileScanResult` pode retornar `READY_TO_PROMOTE` para entregas concorrentes ou duplicadas.
- A exclusão efetiva ocorre apenas em `confirmFileScanClean`, mediante releitura fresca e OCC.
- Duas cópias para a mesma chave podem gerar versões S3 diferentes, mas somente a versão registrada pelo primeiro `confirmFileScanClean` torna-se autoritativa; o starter rejeitará o evento da outra versão ao comparar o `cleanObject`.

Isso é coerente com [apply-file-scan-result.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/apply-file-scan-result.ts).

2. Tenant fence: desenho correto

Acrescentar um `ConditionCheck` do Tenant à lista de entradas de cada `TransactWriteItems` é compatível com o código:

- `buildVersionedUpdate` protege apenas existência, OCC e igualdade de `tenantId`; não verifica lifecycle.
- A entrada adicional fecha o TOCTOU na mesma transação.
- Ela precisa estar presente nas transações `AWAITING`, `REJECTED` e `CONFIRMED`, não apenas na confirmação final.

Há apenas uma imprecisão nominal: o tipo existente é `TransactConditionCheckEntry`, não `TransactConditionCheck`. Isso é mecânico e não reduz materialmente a arquitetura.

3. Rollout em duas flags: corrigido

Ativar primeiro o starter e depois o promoter elimina a janela permanente `CLEAN` sem consumidor. A ordem agora está inequívoca e operacionalmente verificável.

4. Destino do outbox: direção correta e aderente ao precedente

O padrão proposto corresponde ao caminho real:

- `OutboxDestination` é uma união fechada.
- `DispatchOutboxRelay` seleciona um sender pelo destino.
- A composição injeta URLs de fila, como já ocorre com `SQS_IMPORT_PARSE_V1`.
- O sweeper consulta pendências por destino.
- `sqs-worker-queue` já fornece DLQ e `maxReceiveCount`.

Portanto, um novo destino, fila, sender na composição e handler SQS é o padrão correto. Isso confere com [outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts), [relay.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/dispatch-outbox-relay/relay.ts) e [dispatch-outbox-relay-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/dispatch-outbox-relay-handler.ts).

Ainda faltou registrar explicitamente que o IAM do refresh também precisa de:

- leitura da `DocumentVersion`;
- `Query` no novo índice reverso;
- consumo da fila.

A frase “fila + leitura/escrita de Requirement” não cobre todas as operações descritas pelo próprio algoritmo.

5. Refresh orientado a estado corrente: corrigido

Usar o evento apenas como wake-up signal e reler `DocumentVersion` e cada `Requirement` resolve corretamente:

- eventos fora de ordem;
- duplicação;
- relink/unlink concorrente;
- resultados eventualmente consistentes do índice como simples candidatos;
- retry OCC sem reaplicar evidência antiga.

Esse ponto está suficientemente especificado.

6. O reindex diário ainda não oferece a garantia alegada

Este é o principal bloqueante.

A Rodada 3 diz que o worker diário “escaneia todo Requirement `SATISFIED` com `evidenceValidUntil` definido”. O código atual não faz isso. [reindex.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/requirement-reindex/reindex.ts) recebe apenas candidatos retornados por `queryDue`, e [dynamodb-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/requirement-reindex/dynamodb-candidate-source.ts) executa:

```text
GSI8PK = WORK#REQUIREMENT_REINDEX AND GSI8SK < now
```

Logo, ele só descobre Requirements cujo `evidenceValidUntil` cacheado já venceu. Apenas trocar a derivação interna para reler a `DocumentVersion` não transforma esse conjunto de descoberta em um sweeper autoritativo.

Exemplos que continuam sem reparo em até 24 horas:

- `validUntil` vivo muda para uma data anterior, mas o valor cacheado no GSI8 ainda está longe no futuro: o Requirement só será descoberto na data antiga.
- Evidência muda de um estado elegível para inelegível, mas o ponteiro GSI8 continua com vencimento futuro.
- Um Requirement sem ponteiro GSI8 fica inconsistente após perda do evento: o reindex atual nunca o encontra.
- Um valor vivo muda de `undefined` para definido, mas o cache permanece sem candidato de reindex.

Portanto, o checklist afirma bounded staleness de 24 horas sem existir um access pattern que enumere todos os registros possivelmente divergentes. Esta é a mesma classe de problema identificada na Rodada 2, agora parcialmente corrigida na releitura, mas não na descoberta.

7. Cardinalidade: corrigida

A distinção “3 agregados / 4 ações” versus “2 agregados / 2 ações” está correta para a confirmação manual proposta. O outbox foi devidamente contado como ação, sem ser tratado como quarto agregado de negócio.

8. Planner compartilhado: suficientemente melhorado

Estados elegíveis, no-op, proveniência, efeito sobre `DocumentVersion` e outbox atômico estão agora contemplados. A regra de zero ou um campo de validade por schema também elimina ambiguidade relevante.

“Exactly-once lógico” é defensável aqui porque o update OCC e o outbox são atômicos; após um commit ambíguo, a releitura observa o mesmo `validUntil` e o planner produz `NO_CHANGE`.

9. Idempotência do starter: correta, mas incompleta na descrição

O código real usa exatamente `PutCommand` com:

```text
attribute_not_exists(PK) AND attribute_not_exists(SK)
```

Logo, a criação idempotente proposta está alinhada com [dynamodb-extraction-run-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/dynamodb-extraction-run-store.ts).

Entretanto, [start-extraction-run.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts) não trata simplesmente o `putIfAbsent=false` como conclusão. Ele chama `startExecution` mesmo quando o run já existia, usando o mesmo nome determinístico. Isso evita o caso:

1. o `Put` do run confirma;
2. `StartExecution` falha;
3. o retry encontra o run existente;
4. se retornasse sucesso imediatamente, o run ficaria órfão para sempre.

A migração precisa preservar essa segunda camada: run determinístico por `versionId + pipelineVersion` e execução Step Functions com nome determinístico, chamada também no retry. Apenas “falha condicional tratada como sucesso idempotente” é insuficiente.

10. Checklist E-014: corrigido

A régua agora mede propriedades observáveis e não conformidade com componentes escolhidos. Os pesos são coerentes e somam 100%. O problema não está mais na formulação, mas no fato de a implementação de reparo proposta ainda não satisfazer o primeiro critério.

## Mudanças exatas necessárias para a Rodada 4

1. Definir um access pattern de reparo que realmente enumere, pelo menos uma vez por dia, todos os Requirements capazes de estar divergentes — por exemplo, um ponteiro GSI de reconciliação permanente independente de `evidenceValidUntil`, paginação por todos os Requirements com evidência, ou outro sweeper completo. Reler `DocumentVersion` somente depois da descoberta pelo atual `GSI8SK < now` não garante staleness de 24 horas.

2. Preservar explicitamente a idempotência ponta a ponta do starter: `putIfAbsent` absorve duplicação do registro, mas retries devem continuar chamando `StartExecution` com nome e input determinísticos. Também esclarecer que a identidade inclui `pipelineVersion`, para que uma nova versão do pipeline possa legitimamente gerar um novo run.

3. Completar o contrato operacional do novo destino: schema versionado do evento/envelope e IAM explícito para consumo SQS, leitura de `DocumentVersion`, query do índice reverso e leitura/escrita de `Requirement`. A política de DLQ/redrive pode referenciar diretamente o comportamento já fornecido por `sqs-worker-queue`.

A arquitetura está muito próxima de convergir, e dez dos onze grupos de correção avançaram de forma convincente. O reparo diário, contudo, ainda promete uma cobertura que seu mecanismo real de descoberta não possui.

Nota: 8.7/10
