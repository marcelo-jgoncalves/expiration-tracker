2026-09-03T11:39:29.895424Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 93 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a06711-1f9a-7d33-8109-8a2492428e3c
--------
user
You are Codex, continuing as the independent reviewer in this repo's Claude<->Codex debate
protocol (AGENTS.md section 4). This is Round 2. Round 1 is complete: Claude proposed a design,
you critiqued it and scored it 5.6/10 (see
docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round1-codex-critique.md),
Claude self-scored Round 1 at 5.8/10.

Read, in order:
1. docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round1-claude-proposal.md (original proposal, for context)
2. docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round1-codex-critique.md (your own Round 1 critique - verify this matches your actual concerns)
3. docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round2-claude-revision.md (Claude's Round 2 revision responding to every one of your 14 numbered "what must change" items)

Verify the revision against the real code again where it makes new factual claims, especially:
- src/runtime/aws/handlers/upload-finalizer-handler.ts (does it really only recognize the old
  document/subject quarantine key formats, confirming the new PENDING_UPLOAD-stuck claim?)
- src/modules/extraction/application/run-extraction-validation.ts and
  src/modules/extraction/persistence/dynamodb-extracted-field-store.ts (the auto-confirm path
  the revision now explicitly addresses via a shared buildDocumentVersionValidityUpdate function)
- src/modules/document-archive/domain/requirement.ts (the bounded-staleness comment the revision
  cites to justify making Requirement convergence asynchronous instead of transactional fan-out)
- src/modules/document-archive/domain/document-file.ts (PRINCIPAL/attachment role field, to check
  the "only PRINCIPAL triggers OCR" claim is representable)
- src/shared/outbox/outbox.ts (does the outbox mechanism the revision relies on for
  DocumentVersionValidityChanged actually work the way the revision assumes?)

Assess specifically:
1. Is the corrected "which pipeline is authoritative" framing (physical completeness vs. HTTP
   traffic vs. future domain authority, kept separate) now accurate and appropriately hedged?
2. Is Option C (design now, build the promoter as an independent slice, activate behind a feature
   flag) a sound resolution of your Round 1 objection, or does it introduce new problems?
3. Is extending upload-finalizer-handler.ts and malware-result-handler.ts with a third branch (no
   new queue) an adequate promoter architecture, or did Round 2 hand-wave any of the concerns you
   raised (ordering, retry/DLQ, quarantine cleanup, tenant fence)?
4. Is dropping the synchronous "3+N Requirements" transaction in favor of a fixed-cardinality
   3-way (confirm) / 2-way (reject) transaction plus an async idempotent
   requirement-evidence-refresh worker (riding the already-accepted bounded-staleness invariant
   in requirement.ts, with a new sparse GSI_EVIDENCE reverse index) actually sound? Does it fully
   resolve your phantom-race/cardinality/reverse-index objections, or does the async worker
   introduce a new race or correctness gap you need to name explicitly?
5. Is the shared buildDocumentVersionValidityUpdate() function a real fix for the
   auto-confirm-vs-manual-confirm inconsistency you found in Round 1, or does it paper over a
   remaining divergence?
6. Is the reworked E-014 checklist (25/25/20/15/10/5, per your own Round 1 suggested weights)
   correctly anchored and non-circular now?
7. Any remaining architectural or data-integrity risk, including anything new introduced by this
   revision itself (e.g., in the new async worker, the PRINCIPAL-only policy, or the outbox
   event).

Be as rigorous and skeptical as Round 1 - do not soften just because Round 2 responded to your
list point-by-point; a point can be "addressed" shallowly. Give a numbered list of what (if
anything) must still change for Round 3, and end with an explicit numeric score out of 10 for
this Round 2 revision (no rounding, e.g. "Nota: 8.3/10").

codex
Vou revisar os três documentos na ordem pedida e cruzar cada nova afirmação com os cinco pontos de código indicados. Depois entrego uma crítica autônoma, com mudanças obrigatórias numeradas e nota explícita.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round1-claude-proposal.md'; Get-Content -Raw 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round1-codex-critique.md'; Get-Content -Raw 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round2-claude-revision.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 365ms:
# Round 1 (Claude) ƒ?" OCR/AI Extraction ƒÅ" Document Lifecycle Reconciliation, Design Proposal

> Phase 2 of the P0.4 divide-and-conquer plan. Phase 1 (scoping) found M7's extraction pipeline
> live and dual-wired against the OLD `document` module, disconnected from the new Document
> Lifecycle (`document-archive`, D-143+). This round investigates the open question Phase 1 left
> unresolved (which upload pipeline is authoritative today) and proposes a reconciliation design.
> Change-risk-scale level: **5-6** (re-keys a live event trigger, redesigns a cross-aggregate OCC
> transaction, defines a new cross-module data-flow contract ƒ?" Requirement/RequirementTemplate
> have never received AI-derived data before). Protocol `AGENTS.md` ¶õ4 applies.

## Pesquisa externa considerada (E-014)

**SIM PARCIAL.**

The specific pattern this decision touches ƒ?" OCR/document-AI extraction with a
suggested/confirmed human-review loop and confidence scoring ƒ?" is a pattern well-established
outside this project:

- **AWS Textract + Augmented AI (A2I) "Human Review Workflows"** (docs.aws.amazon.com/textract/latest/dg/a2i-textract.html,
  accessed 2026-09-03): the reference architecture for exactly this shape ƒ?" a model produces
  candidate field values with confidence scores, values below a confidence threshold (or a
  sampled percentage) are routed to a human reviewer, and the *reviewer's corrected value*, not
  the raw model output, is what downstream systems commit. This project's
  `ExtractedField.state = PENDING_CONFIRMATION|CONFIRMED|REJECTED` +
  `candidateValue` vs `confirmedValue` is structurally the same shape.
- **Hyperscience "Hyperscience Platform" human-in-the-loop documentation**
  (hyperscience.com/platform/human-in-the-loop, accessed 2026-09-03): confirms the same
  three-way pattern ƒ?" auto-accept above a confidence bar, human review below it, and a
  cross-extractor **agreement/disagreement** signal (multiple models disagreeing forces review
  even above the individual confidence bar). This project's `ExtractionAgreement =
  SINGLE_SOURCE|MATCH|MISMATCH` (`extracted-field.ts:20-23`) mirrors this directly ƒ?" a MISMATCH
  always routes to `PENDING_CONFIRMATION` regardless of either source's individual confidence.
- **DocuSign Insight / Intelligent Insights** (support.docusign.com, "Extracted data review",
  accessed 2026-09-03) documents the same suggested-vs-confirmed distinction for contract
  metadata extraction, confirming this is not just an AWS-specific idiom but a cross-vendor
  convention for document-AI products generally.

**Representativeness**: these three span (a) the underlying cloud OCR primitive this project
already builds on (Textract/A2I), (b) a specialized document-intelligence platform vendor
(Hyperscience), and (c) a contract-lifecycle-adjacent product (DocuSign) ƒ?" reducing single-vendor
bias for the specific claim "suggested value + confidence + human confirm/reject is the
established shape," which is the part of this decision informed by external pattern.

**Escopo (why PARCIAL, not SIM)**: the *human-review-loop shape itself* (state machine,
confidence/agreement semantics, suggested-vs-confirmed distinction) is already correctly built in
this codebase (`extracted-field.ts`, `confirm-reject-field.ts`) and is **not being redesigned by
this round** ƒ?" it is retained as-is. What this round actually decides is internal: which
DynamoDB aggregates the OCC transaction touches, which S3 key format triggers extraction, and how
extracted values flow into `Requirement`/`RequirementTemplate`. No external pattern answers "does
`DocumentFile` or `DocumentVersion` own the S3 object reference in this specific schema" ƒ?" that
depends only on this project's own D-143/D-163 key design, already fixed by prior decisions this
round does not reopen. The checklist below is scoped only to the SIM-PARCIAL part.

### Checklist de critÇ¸rios de nota (sub-rubrica desta decisÇœo)

1. **(peso 30%) Preserva o mecanismo de revisÇœo humana existente sem redesenhar sua semÇ½ntica.**
   Atende: `PENDING_CONFIRMATION`/`CONFIRMED`/`REJECTED`, `confidence`, `sources`, `agreement`,
   `candidateValue`/`confirmedValue` continuam exatamente como estÇœo ƒ?" a proposta sÇü muda QUE
   agregados a transaÇõÇœo de confirmaÇõÇœo toca, nunca a mÇ­quina de estados do campo em si. NÇœo
   atende: qualquer redesign do vocabulÇ­rio SUGGESTED/CONFIRMED ou de `ExtractionAgreement`.
2. **(peso 25%) Nunca perde a garantia MISMATCH-sempre-revisÇœo.** Atende: a proposta nÇœo introduz
   nenhum caminho que auto-confirme um campo com `agreement = MISMATCH`. NÇœo atende: qualquer
   atalho que resolva mismatch automaticamente para reduzir carga operacional.
3. **(peso 25%) O valor que chega em `Requirement`/`DocumentVersion` Ç¸ sempre o `confirmedValue`
   humano, nunca o `candidateValue` bruto do modelo.** Atende: o fluxo de escrita em
   `Requirement.evidenceValidUntil`/`DocumentVersion.validUntil` sÇü Ç¸ alcanÇõÇ­vel a partir de
   `confirmField`, nunca de `startOcr`/`run-bedrock-extraction`/`run-deterministic-parser`
   diretamente. NÇœo atende: qualquer caminho que grave um `candidateValue` nÇœo confirmado num
   campo de negÇücio real.
4. **(peso 20%) ConfianÇõa/proveniÇ¦ncia continuam auditÇ­veis fim a fim** (qual fonte, qual
   confianÇõa, quem confirmou, quando) mesmo depois da re-chave para os novos agregados. Atende:
   nenhum campo de proveniÇ¦ncia existente Ç¸ descartado na migraÇõÇœo. NÇœo atende: perda de
   `sources`/`confidence`/`agreement` ao trocar `Document`(old) por `DocumentFile`/`DocumentVersion`.

## Achado central: qual pipeline Ç¸ autoritativo hoje em `dev`

**O mÇüdulo `document` (antigo) Ç¸ o Ç§nico pipeline ponta-a-ponta funcional hoje. `document-archive`
recebe trÇ­fego HTTP real (ambos os handlers estÇœo montados e roteÇ­veis), mas seu prÇüprio
mecanismo de promoÇõÇœo quarentenaƒÅ'clean estÇ­ incompleto em produÇõÇœo ƒ?" arquivos enviados por ele
nunca saem de `SCANNING`.** NÇœo Ç¸ um caso de "rename in place"; Ç¸ migrar um trigger vivo mais
completar um sub-sistema que ficou pela metade.

EvidÇ¦ncia (leitura direta, nÇœo inferÇ¦ncia):

1. **Ambos os handlers HTTP estÇœo live.** `infra/main.tf:370-402` (`document_archive_handler`,
   `document_archive_guest_handler`) e `infra/main.tf:1122-1165` (`documents_handler`,
   `guest_documents_handler`) sÇœo todos mÇüdulos `lambda-function` reais, montados no mesmo API
   Gateway (`infra/main.tf:432-443` passa os quatro ARNs de invocaÇõÇœo ao roteador). Um comentÇ­rio
   prÇ¸-existente confirma trÇ­fego real do lado antigo: `infra/main.tf:916` ƒ?" *"`documents_handler`
   (real traffic already)"*.
2. **Os dois mÇüdulos escrevem no MESMO par de buckets quarentena/clean**
   (`module.document_buckets`), mas com formatos de chave deliberadamente diferentes e
   namespaced para nunca colidir: o antigo usa `tenant/<t>/item/<i>/document/<d>/slot/<s>/<random>`
   (comentÇ­rio em `document-archive-service.ts:567-571`, citando a convenÇõÇœo do M6); o novo usa
   `document-archive/tenant/<t>/document/<d>/version/<s>/file/<f>`
   (`document-archive-service.ts:572-574`, cujo prÇüprio comentÇ­rio jÇ­ registra: *"the S3 event
   handler routes on this prefix to pick the right parser (D-163 ¶õ7, **deferred**)"*).
3. **O Ç§nico handler real de resultado de scan de malware (`malware-result-handler.ts`,
   `infra/main.tf:1331-1353`, fila real `malware_result_queue`) sÇü reconhece DOIS formatos de
   chave de quarentena**: `parseQuarantineKey` (mÇüdulo `document` antigo) e
   `parseSubmissionQuarantineKey` (mÇüdulo `subject`) ƒ?" `malware-result-handler.ts:8,11,59,75`. Se
   nenhum dos dois casar, o handler loga `"malware-result unrecognized key shape"` e retorna sem
   nunca chamar `applyFileScanResult`/`confirmFileScanClean` (`malware-result-handler.ts:75-79`) ƒ?"
   **e sem empurrar o item para `batchItemFailures`, entÇœo a SQS o trata como processado com
   sucesso e o descarta.** O prefixo `document-archive/...` nunca Ç¸ reconhecido por este handler.
4. **`applyFileScanResult`/`confirmFileScanClean` (`document-archive/application/apply-file-scan-result.ts`)
   existem no cÇüdigo do mÇüdulo novo mas nÇœo tÇ¦m NENHUM call site em `src/runtime/aws/handlers/**`**
   (grep confirmado, zero ocorrÇ¦ncias) ƒ?" a doc-comment de `confirmFileScanClean`
   (linhas 203-210) jÇ­ registra isso explicitamente: *"called only once the caller (**a future
   S3/GuardDuty event worker, out of scope for this slice**) has already copied `sourceObject` to
   `cleanObject`"*. Esse worker nunca foi construÇðdo.
5. ConsequÇ¦ncia: um arquivo enviado hoje via `document-archive` fica preso em
   `scanStatus = PENDING_UPLOAD`/`SCANNING` para sempre (atÇ¸ o `document_file_reconciliation_handler`
   ƒ?" `infra/main.tf:1453-1511` ƒ?" eventualmente marcar `TIMEOUT` pelo reconciliador GSI8), nunca
   chega a `CLEAN`, nunca Ç¸ copiado para o clean bucket, e portanto nunca poderia disparar
   extraÇõÇœo mesmo que o trigger `clean_object_created` (`infra/main.tf:1964-1983`) jÇ­ entendesse
   seu formato de chave (nÇœo entende ƒ?" `parseCleanKey`, usado sÇü por
   `extraction-starter-handler.ts:9,42`, casa apenas `clean/<t>/<i>/<d>` de 3 segmentos, o que o
   caminho `document-archive/tenant/.../file/...` de 4 pares nunca produziria mesmo se fosse
   copiado literalmente).

**ConclusÇœo**: `document` (antigo) Ç¸ autoritativo para trÇ­fego real de upload em `dev` hoje ƒ?" Ç¸ o
Ç§nico caminho que completa o ciclo uploadƒÅ'quarentenaƒÅ'scanƒÅ'clean. `document-archive` estÇ­ montado,
recebe requisiÇõÇæes HTTP (criaÇõÇœo de `Document`/`DocumentVersion`/reserva de `DocumentFile`,
geraÇõÇœo de presigned URL), mas nenhum arquivo enviado por ele jamais termina de ser processado ƒ?"
nÇœo porque D-163-D-168 tenham removido algo do antigo, mas porque a metade final do novo pipeline
(promoÇõÇœo quarentenaƒÅ'clean) nunca foi implementada. Isso Ç¸ maior que uma lacuna de re-chaveamento
de extraÇõÇœo: Ç¸ uma lacuna de produto prÇ¸-existente que este item de trabalho tropeÇõa, nÇœo causa.

## Escopo desta decisÇœo (o que ESTE protocolo decide, nÇœo a implementaÇõÇœo completa do gap acima)

Fechar o gap #5 acima (`WorkerPromoteDocumentArchiveFile`, quarentenaƒÅ'clean para
`document-archive`) Ç% prÇ¸-requisito fÇðsico para qualquer re-chaveamento de extraÇõÇœo ƒ?" sem isso
nÇœo existe evento "clean" para o `document-archive` disparar. Duas opÇõÇæes:

**OpÇõÇœo A (proposta)**: construir o worker de promoÇõÇœo que falta, then re-key extraction contra
`DocumentFile`/`DocumentVersion`. Um Ç§nico item de trabalho maior, sequenciado internamente.

**OpÇõÇœo B**: reconciliar extraÇõÇœo deixando-a acoplada ao mÇüdulo antigo por mais um milestone,
tratando o gap de promoÇõÇœo do `document-archive` como item de backlog separado e nÇœo-relacionado.

Proponho **OpÇõÇœo A** ƒ?" nÇœo porque a promoÇõÇœo pendente seja no escopo original do P0.4, mas porque
tratar extraÇõÇœo como "resolvida" enquanto ela continua lendo o mÇüdulo `document` cujo prÇüprio
ciclo de vida (`Document.status` ACTIVE/ARCHIVED sem estado de revisÇœo real,
`advance-after-evidence.ts`) jÇ­ foi suplantado conceitualmente por `DocumentVersion`/`Requirement`
produziria uma segunda dÇðvida de reconciliaÇõÇœo idÇ¦ntica a esta assim que o worker de promoÇõÇœo for
finalmente construÇðdo por outro item de trabalho. Re-chavear uma vez, contra o alvo certo, Ç¸ mais
barato que re-chavear duas vezes.

### 1. Trigger de extraÇõÇœo ƒ?" de `Document`(antigo) para `DocumentFile`/`DocumentVersion`

- Novo worker `document-archive-file-promoter` (SQS, alimentado pelo `malware_result_handler`
  existente ƒ?" adicionar reconhecimento do prefixo `document-archive/` ao lado dos dois jÇ­
  existentes, roteando para `applyFileScanResult`/`confirmFileScanClean` em vez de
  `processMalwareResult`). Ao promover, grava em uma NOVA convenÇõÇœo de clean key,
  `document-archive/clean/<tenantId>/<documentId>/<seq>/<fileId>`, e emite `S3 Object Created`
  no clean bucket como hoje.
- Novo `parseDocumentArchiveCleanKey` (mesmo padrÇœo de `clean-key.ts`, arquivo prÇüprio em
  `document-archive/domain/`, nunca reaproveitando `parseCleanKey` do mÇüdulo antigo ƒ?" os dois
  formatos coexistem permanentemente, um por mÇüdulo, atÇ¸ o antigo ser desligado).
- `extraction-starter-handler.ts` passa a tentar `parseDocumentArchiveCleanKey` PRIMEIRO,
  fallback para `parseCleanKey` (antigo) ƒ?" os dois pipelines continuam coexistindo durante a
  transiÇõÇœo, nenhum removido nesta rodada (D-190/D-191 jÇ­ estabeleceram o padrÇœo de migraÇõÇœo
  progressiva por GSI, aplico o mesmo princÇðpio: o trigger novo primeiro, desligamento do antigo
  como item de trabalho subsequente, nÇœo parte desta decisÇœo).
- `startExtractionRun` passa a aceitar `{tenantId, documentId, seq, fileId}` em vez de
  `{tenantId, itemId, documentId}` ƒ?" `ExtractionRun`/`ExtractedField` jÇ­ sÇœo chaveados sÇü por
  `documentId` (`TENANT#t#DOC#d`), entÇœo o `documentId` do `document-archive` (que jÇ­ Ç¸
  globalmente Ç§nico por Document, nÇœo por-item como o antigo) substitui o antigo sem mudar o
  formato de chave do `ExtractionRun` em si ƒ?" apenas a fonte do valor.

### 2. TransaÇõÇœo OCC de confirmaÇõÇœo ƒ?" de 4-way (`ExpirationItem`/`Document`/`ExtractionRun`/`ExtractedField`) para `DocumentVersion`/`ExtractionRun`/`ExtractedField` (3-way) + `Requirement` condicional (4-way sÇü quando aplicÇ­vel)

- `Document`(old)/`ExpirationItem` saem da transaÇõÇœo de confirmaÇõÇœo. `DocumentVersion` entra no
  lugar de `Document`(old) ƒ?" Ç¸ o agregado que hoje carrega `validUntil` e Ç¸ o alvo natural de
  "este campo extraÇðdo Ç¸ uma data de validade".
- Diferente do antigo (`ExpirationItem` sempre presente), nem toda `ExtractedField` confirmada
  tem uma `Requirement` esperando o valor ƒ?" sÇü campos cujo `fieldName` mapeia para "data de
  validade" (`field-schema.ts` jÇ­ distingue tipos de campo) precisam propagar a um `Requirement`.
  Proponho uma variante do 3-way base + `Requirement` condicional (4-way sÇü quando o campo
  confirmado for do tipo validade E a `DocumentVersion` correspondente estiver linkada como
  evidÇ¦ncia de pelo menos um `Requirement` ativo) ƒ?" mantendo o princÇðpio jÇ­ estabelecido em
  `confirm-reject-field.ts`'s doc comment ("reject nunca toca ExpirationItem") mas generalizado:
  a transaÇõÇœo sempre atualiza `DocumentVersion`, e condicionalmente atualiza o(s) `Requirement`(s)
  cujo `evidenceVersionId` aponta para essa versÇœo ƒ?" reusando a MESMA funÇõÇœo `linkEvidence`'s
  cache-refresh interno (`document-archive-service.ts:1056-1060`) para nunca duplicar a lÇügica de
  derivaÇõÇœo de status.
- Isto Ç¸ uma mudanÇõa de FORMA em relaÇõÇœo ao 4-way fixo antigo (agora Ç¸ "3-way sempre + N
  Requirements condicionais", nÇœo um nÇ§mero fixo) ƒ?" decisÇœo explÇðcita a submeter Çÿ crÇðtica do
  Codex, nÇœo um detalhe implÇðcito.

### 3. `document-classifier.ts` ƒ?" achado que corrige o enquadramento da Fase 1

`document-classifier.ts` **nÇœo Ç¸** um classificador de tipo de documento de negÇücio (nÇœo decide
"isto Ç¸ uma CNH" vs "isto Ç¸ uma ApÇülice") ƒ?" Ç¸ um classificador de FORMATO de arquivo para escolher
a chamada correta do Textract (PDF/JPEG/PNG/TIFF, `document-classifier.ts:14`). A Fase 1 leu o
nome do arquivo e presumiu uma conexÇœo com `DocumentType.documentTypeId` (D-176) que a
implementaÇõÇœo real nÇœo sustenta ƒ?" nÇœo existe hoje NENHUM classificador de tipo de documento de
negÇücio na extraÇõÇœo, sugerido por IA ou nÇœo. Proponho **nÇœo** inventar um nesta rodada (fora do
escopo do P0.4 conforme lido ƒ?" P0.4 Ç¸ sobre extraÇõÇœo de dados de validade, nÇœo sobre
auto-categorizaÇõÇœo de tipo de documento) e apenas corrigir a lacuna de nomenclatura: renomear
`document-classifier.ts` para `document-format-classifier.ts` (rename mecÇ½nico, nÇðvel 1-2, nÇœo
precisa do protocolo) para que a prÇüxima sessÇœo nÇœo repita o mesmo engano de leitura.

### 4. Fluxo de dados de validade ƒ?" de `ExpirationItem` para `DocumentVersion.validUntil` (e daÇð, automaticamente, `Requirement`)

- Novo caminho: `confirmField` (quando o campo Ç¸ do tipo "validade") grava
  `DocumentVersion.validUntil` diretamente (nova operaÇõÇœo, nÇœo existe hoje um setter pÇüs-criaÇõÇœo
  ƒ?" `validUntil` hoje sÇü Ç¸ setado na criaÇõÇœo da Version). Uma vez setado, QUALQUER `Requirement`
  jÇ­ linkado a essa versÇœo via `evidenceVersionId` precisa ter seu `evidenceValidUntil`/`status`
  recalculados na MESMA transaÇõÇœo (reusando `deriveRequirementStatus`, puro, jÇ­ testÇ­vel) ƒ?" nunca
  como uma segunda escrita fire-and-forget.
- `RequirementTemplate` (D-191) nÇœo recebe nada de extraÇõÇœo diretamente ƒ?" ele jÇ­ Ç¸ sÇü um molde
  aplicado uma vez (`applyTemplate`, snapshot). NÇœo hÇ­ "valor extraÇðdo" que se aplique a um
  template; a Fase 1 provavelmente quis dizer "Requirement", que Ç¸ o que este item #4 jÇ­ cobre.

## Fora de escopo desta rodada (explÇðcito, para a crÇðtica do Codex focar no que importa)

- Desligar o mÇüdulo `document` antigo (D-190/D-191 style de migraÇõÇœo progressiva ƒ?" item de
  trabalho subsequente, quando confianÇõa suficiente existir).
- Qualquer classificador de tipo de documento de negÇücio por IA.
- Mudar a UX/HTTP de `document-archive` alÇ¸m do necessÇ­rio para receber o resultado de extraÇõÇœo.
- Migrar dados jÇ­ extraÇðdos contra o mÇüdulo antigo (dev nÇœo tem usuÇ­rio real, `AGENTS.md` ¶õ1 ƒ?"
  nÇœo Ç¸ um requisito de migraÇõÇœo de dados, Ç¸ um requisito de qual pipeline NOVO trÇ­fego usa).

# Round 1 (Codex) ƒ?" Critique

> Extracted from the raw exec transcript (`round1-codex-critique.txt`) ƒ?" Codex's first attempt
> at this critique bled together with an unrelated prior review (`bulk-import-documents-...`)
> Codex read as a "structural reference"; only the final, on-topic critique (which explicitly
> re-verified every claim against this repo's real files) is kept here, verbatim from the
> transcript's last occurrence.

A Rodada 1 encontrou corretamente a desconexÇœo fÇðsica entre os pipelines, mas ainda nÇœo apresenta um design aceitÇ­vel para implementaÇõÇœo. O diagnÇüstico factual Ç¸ parcialmente sÇülido; a conclusÇœo de "pipeline autoritativo", a incorporaÇõÇœo obrigatÇüria do worker ao escopo e, sobretudo, a transaÇõÇœo `3 + N Requirements` vÇœo alÇ¸m do que a evidÇ¦ncia sustenta.

## 1. Pipeline autoritativo: achado correto, conclusÇœo superestimada

Confirmei no cÇüdigo: `malware-result-handler.ts:59` tenta apenas `parseQuarantineKey`/`parseSubmissionQuarantineKey`; uma chave `document-archive/...` cai no branch nÇœo reconhecido, Ç¸ logada e descartada sem `batchItemFailures` (nÇœo Ç¸ "silencioso" ƒ?" hÇ­ log de erro ƒ?" mas Ç¸ descarte definitivo sem retry/DLQ). `confirmFileScanClean` nÇœo tem call site em handler real. `buildQuarantineKey` produz o namespace `document-archive/tenant/...`. `extraction-starter-handler.ts:42` sÇü entende o formato antigo. `document-classifier.ts` classifica formato fÇðsico, nÇœo `DocumentType` de negÇücio ƒ?" achado correto.

TrÇ¦s excessos: (1) Terraform prova deploy/roteabilidade, nÇœo volume/recÇ¦ncia de trÇ­fego ƒ?" o comentÇ­rio "real traffic already" Ç¸ evidÇ¦ncia histÇürica de log group, nÇœo de autoridade de produto; (2) "autoritativo" mistura trÇ¦s conceitos (pipeline fisicamente completo / API que recebe trÇ­fego / autoridade conceitual futura) ƒ?" o cÇüdigo sÇü sustenta o primeiro; (3) a proposta omite que **`upload-finalizer-handler.ts` tambÇ¸m sÇü reconhece os formatos antigo e de submission** ƒ?" nÇœo basta corrigir `malware-result-handler`, o lado de upload finalizado do `document-archive` tambÇ¸m Ç¸ descartado, deixando arquivos presos em `PENDING_UPLOAD`, nÇœo sÇü `SCANNING`.

**ConclusÇœo corrigida**: o mÇüdulo antigo Ç¸ hoje o Ç§nico pipeline fisicamente completo no cÇüdigo implantÇ­vel. `document-archive` Ç¸ o modelo de domÇðnio aprovado com entrada HTTP/presign real, mas nÇœo tem consumidores reais para NENHUM dos dois sinais fÇðsicos (upload finalizer E malware result) que a correlaÇõÇœo de scan exige.

## 2. OpÇõÇœo A Ç¸ um falso dilema e amplia indevidamente o escopo

O worker de promoÇõÇœo Ç¸ prÇ¸-requisito para ATIVAR o pipeline novo ponta-a-ponta, nÇœo para DECIDIR corretamente o contrato de reconciliaÇõÇœo. Existe uma OpÇõÇœo C melhor delimitada: (1) desenhar a identidade de extraÇõÇœo contra `DocumentVersion`/`DocumentFile` agora; (2) definir o contrato do evento clean e as precondiÇõÇæes que o starter deve validar; (3) implementar/testar a extraÇõÇœo re-chaveada sem ligÇ­-la ao trigger real; (4) implementar upload-finalizer + malware-result + promoÇõÇœo como fatia independente; (5) ativar o trigger novo sÇü quando ambas as fatias estiverem prontas. Isso nÇœo exige re-key duas vezes.

AlÇ¸m disso, o worker de promoÇõÇœo proposto estÇ­ nebuloso: nÇœo define fila/schema do handoff, ordem publish-antes-ou-depois de `applyFileScanResult`, quem consome o S3 Object Created da quarentena, retry/DLQ, ownership do `CopyObject`, limpeza do objeto de quarentena, tenant lifecycle fence.

## 3. A transaÇõÇœo "3 + N Requirements" nÇœo estÇ­ sound

- **3.1 Sem access pattern reverso**: `Requirement` mora na partiÇõÇœo do Subject (`PK=TENANT#t#SUBJECT#s`); `evidenceVersionId` Ç¸ sÇü atributo, sem Çðndice "todas as Requirements que apontam para esta versÇœo" ƒ?" "atualizar todos os Requirements" nÇœo Ç¸ descobrÇðvel sem Scan.
- **3.2 Phantom race**: uma query prÇ¸-transaÇõÇœo nÇœo fecha a corrida contra um `linkEvidence` concorrente que vincula um Requirement C Çÿ mesma versÇœo entre a leitura e o commit ƒ?" OCC de A/B nÇœo protege a ausÇ¦ncia de C.
- **3.3 Limite transacional**: `TransactWriteItems` tem teto de 100 aÇõÇæes; cardinalidade aberta por vÇðnculos Ç¸ design inaceitÇ­vel mesmo abaixo do limite tÇ¸cnico (contenÇõÇœo/custo imprevisÇðveis).
- **3.4 `linkEvidence()` nÇœo Ç¸ reusÇ­vel como builder** ƒ?" faz autorizaÇõÇœo + leitura + derivaÇõÇœo + update + sua prÇüpria transaÇõÇœo; nÇœo pode ser chamado dentro de outro `TransactWriteItems` sem extraÇõÇœo explÇðcita de um planner puro.
- **3.5 Auto-confirmaÇõÇœo esquecida (bloqueante central)**: `run-extraction-validation.ts:229` + `dynamodb-extracted-field-store.ts:52` jÇ­ escrevem o agregado de negÇücio dentro de `commitRunOutcome` para campos auto-confirmados ƒ?" a proposta afirma (contraditoriamente) que sÇü `confirmField` grava valor de negÇücio.
- **3.6 IdempotÇ¦ncia subespecificada**: request hash nÇœo diz como incorpora seq/versionId/fileId, versÇæes esperadas de Requirements descobertos dinamicamente, nem o comportamento da janela commit-then-fail-before-complete().
- **3.7 Confirm/reject nÇœo separados com precisÇœo** para o novo modelo (reject nunca deveria tocar `validUntil`; auto-confirm precisa da mesma semÇ½ntica do confirm manual).

## 4. Riscos adicionais omitidos

- **Multi-arquivo/PRINCIPAL**: `DocumentFile` permite PRINCIPAL + anexos; se cada objeto promovido gerar evento clean, mÇ§ltiplos arquivos podem disparar OCR concorrente para a mesma Version. PolÇðtica (sÇü PRINCIPAL dispara? cada arquivo tem run prÇüprio?) nÇœo definida. Identidade `{documentId,seq,fileId}` proposta nÇœo bate com o `ExtractionRun` atual (ainda tem `itemId` e `documentVersion: number` ambÇðguo).
- **Evento clean antes da confirmaÇõÇœo DynamoDB**: `CopyObject` gera S3 Object Created antes/possivelmente-fora-de-ordem da transaÇõÇœo `confirmFileScanClean` ƒ?" o starter nÇœo pode confiar sÇü na chave, precisa reconsultar `DocumentFile` (scanStatus=CLEAN, cleanObject bate exatamente, Ç¸ PRINCIPAL, versÇœo nÇœo retirada, tenant lifecycle ok).
- **ConvenÇõÇœo de chave clean subespecificada** (por que `seq` e nÇœo `versionId` imutÇ­vel; encoding/estabilidade sob retry).
- **Estados de `DocumentVersion` elegÇðveis para extraÇõÇœo** nÇœo definidos (o que acontece se rejeitada/retirada/superseded durante o run).
- **ProveniÇ¦ncia humana insuficiente**: `ExtractedField` nÇœo tem `confirmedBy`/`confirmedAt` ƒ?" o checklist da proposta afirma auditabilidade que a prÇüpria entidade nÇœo sustenta (falha seu prÇüprio critÇ¸rio 4).

## 5. E-014 e checklist

`SIM PARCIAL` Ç¸ razoÇ­vel, mas a pesquisa Ç¸ perifÇ¸rica Çÿ decisÇœo real (identidade, fan-out transacional, promoÇõÇœo S3, consistÇ¦ncia entre agregados ƒ?" nÇœo human-in-the-loop, que nÇœo estÇ­ sendo redesenhado). Checklist mal ponderado: 55% repete a mesma garantia (preservar review/nÇœo autoaceitar mismatch), 25% contradiz a auto-confirmaÇõÇœo jÇ­ existente, 20% exige `confirmedBy` que o modelo nÇœo persiste, e nenhum peso cobre atomicidade/idempotÇ¦ncia/identidade/multi-file/corrida copy-before-confirm/cardinalidade ƒ?" uma proposta poderia tirar nota mÇ­xima nele e ainda perder updates de Requirement ou extrair um objeto ÇürfÇœo.

**ReponderaÇõÇœo sugerida para Rodada 2**: 25% identidade/idempotÇ¦ncia por DocumentVersion/DocumentFile; 25% atomicidade/convergÇ¦ncia de `validUntil`+Requirements; 20% seguranÇõa do trigger e corrida S3-copy/DynamoDB-confirm; 15% semÇ½ntica multi-arquivo; 10% preservaÇõÇœo de auto-confirmaÇõÇœo/revisÇœo humana; 5% proveniÇ¦ncia/auditoria.

## O que deve mudar na Rodada 2 (lista completa do Codex)

1. Reescrever o achado central distinguindo completude fÇðsica, trÇ­fego observado e autoridade futura.
2. Incluir o gap real do `upload-finalizer-handler`, nÇœo sÇü o resultado GuardDuty.
3. Adicionar a OpÇõÇœo C: design/re-key independente, promoter separado, ativaÇõÇœo sÇü apÇüs ambos prontos.
4. Definir precisamente a arquitetura do promoter (fila/schema, ordem, retry/DLQ, ownership do CopyObject, cleanup, tenant fence).
5. Especificar ordem copy/confirm, objeto ÇürfÇœo, validaÇõÇœo autoritativa no starter.
6. Decidir polÇðtica PRINCIPAL vs ATTACHMENT.
7. Redesenhar identidade de `ExtractionRun` (eliminar ambiguidade OCC `version` vs `DocumentVersion.seq/versionId`).
8. Tratar explicitamente o caminho de auto-confirmaÇõÇœo, nÇœo sÇü o confirm HTTP.
9. Abandonar ou redesenhar "3+N Requirements": access pattern reverso, cardinalidade, phantom fence, limite de 100 aÇõÇæes, estratÇ¸gia de convergÇ¦ncia.
10. Especificar expected versions e request hash de idempotÇ¦ncia, incl. falha entre commit e complete().
11. Separar transaÇõÇæes de confirm/reject/auto-confirm por efeitos reais.
12. Definir estados de `DocumentVersion` elegÇðveis e comportamento sob transiÇõÇœo concorrente.
13. Corrigir checklist E-014 para medir os riscos reais.
14. NÇœo afirmar auditabilidade de "quem confirmou" sem fonte durÇ­vel para isso.

**Nota (cega): 5.6/10.**

# Round 2 (Claude) ƒ?" Revision

**Claude's own blind score for Round 1 (recorded now that Codex's Round 1 score exists): 5.8/10.**
I agree with the substance of the critique: the authoritative-pipeline finding conflated three
distinct claims, Option A was unjustified scope creep, and the "3+N Requirements" transaction was
not sound (open cardinality, phantom race, no reverse index, ignored the existing auto-confirm
path). Every point below is addressed on the merits, not to chase a number.

## 1. Achado central ƒ?" reescrito, trÇ¦s conceitos separados

Corrigindo o excesso apontado: "autoritativo" tinha trÇ¦s sentidos diferentes na Rodada 1, e o
cÇüdigo sÇü sustenta o primeiro.

- **Pipeline fisicamente completo hoje**: sÇü o mÇüdulo `document` (antigo). Confirmado agora dos
  dois lados da correlaÇõÇœo simÇ¸trica, nÇœo sÇü do lado de malware result ƒ?" **achado novo desta
  rodada**: `upload-finalizer-handler.ts` (o consumidor do evento de upload finalizado, o outro
  sinal fÇðsico que `apply-file-scan-result.ts`'s correlaÇõÇœo exige) reconhece apenas
  `parseQuarantineKey` (antigo) e `parseSubmissionQuarantineKey` (subject) ƒ?" o mesmo buraco do
  `malware-result-handler.ts`, no sinal oposto. Um arquivo enviado via `document-archive` fica
  preso em `PENDING_UPLOAD` (nunca chega a `SCANNING`), nÇœo sÇü em `SCANNING` como a Rodada 1
  disse ƒ?" pior do que eu tinha reportado.
- **API que recebe trÇ­fego HTTP**: ambos os handlers estÇœo deployados e roteÇ­veis
  (`infra/main.tf`). Retiro a afirmaÇõÇœo de "trÇ­fego real" para `document_archive_handler` ƒ?" nÇœo
  verificÇ­vel a partir de Terraform/comentÇ­rio histÇürico, sÇü CloudWatch real (fora do escopo
  desta rodada de design). Mantenho, sem enfraquecer, que `documents_handler` tem o comentÇ­rio
  histÇürico citando trÇ­fego real ƒ?" Ç¸ evidÇ¦ncia de uso passado do antigo, nÇœo prova de ausÇ¦ncia de
  uso do novo.
- **Autoridade conceitual futura**: `document-archive`/`DocumentVersion`/`Requirement` ƒ?" nunca
  esteve em disputa; D-143 jÇ­ Ç¸ a arquitetura aprovada, este documento nÇœo a reabre.

**ConclusÇœo corrigida (adotada)**: o mÇüdulo antigo Ç¸ hoje o Ç§nico pipeline fisicamente completo
no cÇüdigo implantÇ­vel, nos dois lados da correlaÇõÇœo (upload finalizer E malware result).
`document-archive` Ç¸ o modelo de domÇðnio aprovado, com entrada HTTP/presign real, mas sem NENHUM
consumidor fÇðsico real para nenhum dos dois sinais que sua prÇüpria correlaÇõÇœo de evidÇ¦ncia exige.

## 2. OpÇõÇœo C adotada ƒ?" sequenciamento, nÇœo escopo indivisÇðvel

Abandono a falsa dicotomia A/B da Rodada 1. Adoto a **OpÇõÇœo C do Codex** como a decisÇœo desta
rodada:

1. Esta rodada decide a identidade/contrato de extraÇõÇœo contra `DocumentVersion`/`DocumentFile`
   (seÇõÇæes 3-6 abaixo) e o contrato do evento clean + precondiÇõÇæes de validaÇõÇœo no starter.
2. A implementaÇõÇœo da extraÇõÇœo re-chaveada Ç¸ construÇðda e testada contra esse contrato, mas o
   trigger real (`clean_object_created` reconhecendo o novo prefixo) fica **desligado por feature
   flag** (mesmo mecanismo jÇ­ usado por `extraction_pipeline_enabled` em `infra/main.tf:1965`,
   `1979`, `1986` ƒ?" um segundo flag, nÇœo um mecanismo novo) atÇ¸ a fatia #2 existir.
2'. A fatia de ingestÇœo fÇðsica (upload-finalizer + malware-result + promoÇõÇœo, item 3 abaixo) Ç¸
   implementada e testada independentemente.
3. O flag do trigger novo sÇü Ç¸ ligado quando ambas as fatias estÇœo prontas e verificadas em
   `dev`.

Isso nÇœo exige re-key duas vezes ƒ?" a extraÇõÇœo Ç¸ desenhada uma vez contra o alvo certo.

## 3. Arquitetura do promoter ƒ?" sem worker/fila novos, estendendo os dois handlers existentes

Rejeito a "nova fila SQS" da Rodada 1 (a crÇðtica estÇ­ certa: nebulosa, duplicava lÇügica). Em vez
disso, mesma forma que o mÇüdulo `subject` jÇ­ usa para coexistir com o mÇüdulo `document` no MESMO
par de handlers (`malware-result-handler.ts` jÇ­ ramifica em `parseQuarantineKey` vs
`parseSubmissionQuarantineKey` ƒ?" dois branches, um handler, duas filas de origem jÇ­ existentes
reaproveitadas):

- **`upload-finalizer-handler.ts`**: adicionar um terceiro branch, `parseDocumentArchiveQuarantineKey`
  (novo parser, mesmo estilo de `quarantine-key.ts`), chamando `applyFileScanResult` com
  `uploadEvidence` preenchido (em vez do `processMalwareResult`/`processSubmissionMalwareResult`
  do mÇüdulo antigo/subject) ƒ?" reaproveita a MESMA fila `upload_finalizer_queue`/EventBridge rule
  jÇ­ cabeada ao bucket de quarentena inteiro (`infra/main.tf:1027-1048`, nÇœo filtra por prefixo),
  sÇü adiciona uma terceira ramificaÇõÇœo de parsing no handler que jÇ­ a consome.
- **`malware-result-handler.ts`**: mesmo padrÇœo, terceiro branch chamando `applyFileScanResult`
  com `malwareEvidence` preenchido.
- **Quem copia quarentenaƒÅ'clean e chama `confirmFileScanClean`**: o MESMO handler cujo branch
  produziu `outcome: "READY_TO_PROMOTE"` faz o `CopyObject` (quarantineƒÅ'clean, verificaÇõÇœo de
  tamanho/checksum, mesma disciplina de `advance-after-evidence.ts`) e sÇü entÇœo chama
  `confirmFileScanClean` ƒ?" nunca os dois branches (upload-finalizer e malware-result) competindo
  para promover o mesmo arquivo, porque `applyFileScanResult` jÇ­ Ç¸ o Ç§nico ponto de decisÇœo
  (`decision.action === "PROMOTE"` sÇü Ç¸ alcanÇõado depois que AMBAS as evidÇ¦ncias consolidaram,
  nÇœo importa qual handler entregou a segunda ƒ?" `apply-file-scan-result.ts`'s doc comment jÇ­
  garante isso, reaproveitado sem mudanÇõa).
- **Retry/DLQ/ownership**: idÇ¦nticos aos que jÇ­ existem para o branch antigo nos mesmos dois
  handlers ƒ?" nenhum mecanismo novo, sÇü um terceiro `if (parsed) { ... }` ao lado dos dois jÇ­
  existentes em cada handler.
- **Limpeza do objeto de quarentena**: fora de escopo desta rodada ƒ?" o mÇüdulo antigo tambÇ¸m nÇœo
  limpa o objeto de quarentena apÇüs promoÇõÇœo (`advance-after-evidence.ts` nÇœo faz
  `DeleteObject`), entÇœo esta rodada mantÇ¸m paridade comportamental, nÇœo introduz uma lacuna
  nova.
- **Tenant lifecycle fence**: reaproveita o fence que `processMalwareResult`/`processSubmissionMalwareResult`
  jÇ­ aplicam antes de escrever (checagem de tenant ACTIVE) ƒ?" o novo branch chama a mesma
  checagem, nunca pula essa etapa.

## 4. Chave clean ƒ?" versionId, nÇœo seq (correÇõÇœo do achado)

Corrigindo a Rodada 1: a nova convenÇõÇœo usa o `versionId` imutÇ­vel de `DocumentVersion`, nunca o
`seq` mutÇ­vel-por-natureza-de-Çðndice: `document-archive/clean/<tenantId>/<documentId>/<versionId>/<fileId>`.
`versionId` jÇ­ existe como identificador estÇ­vel e imutÇ­vel de uma `DocumentVersion`
(`document-version.ts`), evitando o problema apontado pelo Codex de reindexaÇõÇœo sob retry/GSI. O
novo parser (`parseDocumentArchiveCleanKey`, ao lado do jÇ­ existente `parseCleanKey` do mÇüdulo
antigo ƒ?" os dois formatos coexistem, nenhum removido nesta rodada) devolve
`{tenantId, documentId, versionId, fileId}`.

## 5. Starter ƒ?" o evento S3 nunca Ç¸ confiado sozinho

Corrigindo o achado do Codex (evento clean pode chegar antes/fora de ordem do commit
`confirmFileScanClean`): `extraction-starter-handler.ts`, ao reconhecer o novo prefixo, SEMPRE
re-lÇ¦ `DocumentFile` fresco (nunca confia sÇü na chave do evento S3) e sÇü prossegue se TODAS as
condiÇõÇæes valem:

1. `file.scanStatus === "CLEAN"`;
2. `file.cleanObject` bate exatamente (bucket/key/versionId) com o objeto observado pelo evento
   S3 ƒ?" mesma disciplina de correlaÇõÇœo simÇ¸trica que `apply-file-scan-result.ts` jÇ­ usa entre
   evidÇ¦ncias;
3. `file.role === "PRINCIPAL"` (polÇðtica decidida no item 6 abaixo ƒ?" anexos nunca disparam OCR
   nesta fase);
4. a `DocumentVersion` correspondente estÇ­ em um estado elegÇðvel: `RECEIVED` ou `UNDER_REVIEW`
   (nunca `WITHDRAWN`/`REJECTED`/`SUPERSEDED` ƒ?" se a versÇœo jÇ­ saiu de consideraÇõÇœo entre a
   promoÇõÇœo fÇðsica e este evento, a extraÇõÇœo Ç¸ descartada como `IGNORED_STALE`, mesmo vocabulÇ­rio
   que `applyFileScanResult` jÇ­ usa). `ACCEPTED` tambÇ¸m Ç¸ elegÇðvel (evidÇ¦ncia jÇ­ aceita pode
   ainda precisar de extraÇõÇœo de campos de validade que a revisÇœo humana nÇœo preencheu).

Uma falha em qualquer condiÇõÇœo Ç¸ um `IGNORED_STALE`/log, nunca uma tentativa de iniciar extraÇõÇœo
sobre um objeto ÇürfÇœo.

## 6. Multi-arquivo ƒ?" sÇü PRINCIPAL dispara OCR (decisÇœo explÇðcita, nÇœo default acidental)

`DocumentFile` permite um `PRINCIPAL` e anexos (`document-file.ts`). DecisÇœo explÇðcita desta
rodada: **sÇü a promoÇõÇœo do arquivo `PRINCIPAL` gera um `ExtractionRun`.** Anexos promovidos nunca
disparam extraÇõÇœo nesta fase ƒ?" decisÇœo de produto deliberadamente conservadora (anexos
tipicamente sÇœo material de apoio, nÇœo o documento cujo campo de validade importa), registrada
aqui como escopo explÇðcito, revisÇ­vel por um item de trabalho futuro caso o produto precise de
extraÇõÇœo multi-arquivo. Isso elimina inteiramente a corrida "mÇ§ltiplos arquivos disparando
execuÇõÇæes concorrentes para a mesma Version" apontada pelo Codex ƒ?" nÇœo porque a corrida foi
resolvida por sincronizaÇõÇœo, mas porque sÇü existe um candidato possÇðvel por Version.

## 7. Identidade de `ExtractionRun`/`ExtractedField` ƒ?" redesenhada, sem ambiguidade

- `ExtractionRun` passa a ser identificado por `{tenantId, documentId, versionId}` ƒ?" nunca mais
  `itemId` (nÇœo existe no novo modelo) nem o campo ambÇðguo `documentVersion: number` (que hoje
  mistura o conceito de "nÇ§mero de versÇœo OCC do agregado Document" com "qual versÇœo lÇügica do
  documento" ƒ?" os dois precisam se separar). `PK = TENANT#t#DOC#d`, `SK = RUN#<versionId>#<runId>`
  ƒ?" mesmo padrÇœo de particionamento por Document que jÇ­ existe, sÇü troca o que qualifica a versÇœo
  lÇügica.
- `ExtractedField` mantÇ¸m sua chave atual (`extracted-field.ts:51-53`, `PK=TENANT#t#DOC#d`,
  `SK=FIELD#<fieldName>#<runId>`) sem mudanÇõa ƒ?" jÇ­ nÇœo dependia de `itemId`.
- Novos campos em `ExtractedField`: `confirmedBy: string` (o `RequestContext` do ator que
  confirmou/rejeitou ƒ?" nunca "SYSTEM" para o caminho manual) e `confirmedAt: string`, setados
  junto com `state` na mesma escrita que jÇ­ seta `state`/`confirmedValue`/`correctionReason` ƒ?"
  fecha o achado do Codex de que o checklist da Rodada 1 prometia auditabilidade que a entidade
  nÇœo sustentava. Caminho de auto-confirmaÇõÇœo (item 8) grava `confirmedBy: "SYSTEM_AUTOCONFIRM"`
  explicitamente, nunca omite o campo.

## 8. Auto-confirmaÇõÇœo ƒ?" unificada com o confirm manual via funÇõÇœo pura compartilhada

Corrigindo o bloqueante 3.5 do Codex: `run-extraction-validation.ts`/`dynamodb-extracted-field-store.ts`'s
`commitRunOutcome` jÇ­ escreve o agregado de negÇücio para campos auto-confirmados hoje ƒ?" a
proposta da Rodada 1 (sÇü `confirmField` grava valor de negÇücio) estava errada e contradizia o
cÇüdigo real.

CorreÇõÇœo: extrair uma funÇõÇœo pura `buildDocumentVersionValidityUpdate(fieldName, confirmedValue,
currentVersionState)` (nova, em `extraction/domain/`, testÇ­vel sem I/O ƒ?" mesmo padrÇœo que
`item-field-mapping.ts`'s `buildItemAttributeUpdate` jÇ­ estabelece) que os DOIS caminhos chamam:

- `doConfirmField` (HTTP, humano) ƒ?" chama-a com `confirmedBy = ctx.identity`, dentro da mesma
  transaÇõÇœo 3-way (item 9).
- `commitRunOutcome` (auto-confirm, `run-extraction-validation.ts`) ƒ?" chama-a com
  `confirmedBy = "SYSTEM_AUTOCONFIRM"`, dentro da MESMA transaÇõÇœo que jÇ­ escreve `ExtractedField`
  auto-confirmado (nunca uma segunda transaÇõÇœo separada).

Os dois caminhos produzem exatamente o mesmo efeito colateral sobre `DocumentVersion` ƒ?" que campo
foi atualizado, com qual valor ƒ?" atravÇ¸s da mesma funÇõÇœo, nunca duas implementaÇõÇæes divergentes
(mesmo princÇðpio que `confirm-reject-field.ts`'s doc comment jÇ­ cita para o `ExpirationItem`
antigo: "os dois caminhos devem produzir efeito de item idÇ¦ntico").

## 9. TransaÇõÇœo de confirmaÇõÇœo ƒ?" 3-way fixo, Requirement NUNCA na mesma transaÇõÇœo

Abandono por completo o "3+N Requirements" da Rodada 1 ƒ?" o Codex estÇ­ certo de que nÇœo fecha
(sem Çðndice reverso, corrida fantasma, teto de 100 aÇõÇæes, cardinalidade aberta). A correÇõÇœo nÇœo Ç¸
consertar o fan-out sÇðncrono, Ç¸ removÇ¦-lo:

- **`confirmField`**: transaÇõÇœo 3-way fixa ƒ?" `DocumentVersion` (usa a funÇõÇœo do item 8),
  `ExtractionRun` (proteÇõÇœo OCC, sem mudanÇõa de dado), `ExtractedField` (stateƒÅ'CONFIRMED,
  confirmedValue, confirmedBy, confirmedAt). Exatamente a mesma cardinalidade fixa que a Rodada 1
  jÇ­ tinha para o 3-way original ƒ?" sÇü troca `Document`(antigo) por `DocumentVersion`.
- **`rejectField`**: 2-way ƒ?" `ExtractionRun` (proteÇõÇœo) + `ExtractedField` (stateƒÅ'REJECTED) ƒ?"
  NUNCA toca `DocumentVersion`, corrigindo a formulaÇõÇœo "3 vias sempre" da Rodada 1 apontada como
  excessivamente ampla no achado 3.7. Reject Ç¸ estritamente mais simples que hoje (era 3-way
  contra `Document` antigo; passa a nÇœo precisar nem tocar o agregado de documento, jÇ­ que rejeitar
  um campo nunca muda `validUntil`).
- **`Requirement` nunca entra em nenhuma dessas transaÇõÇæes.** Em vez disso: quando
  `buildDocumentVersionValidityUpdate` produz uma mudanÇõa real de `validUntil`, a MESMA transaÇõÇœo
  emite um evento outbox (`src/shared/outbox/outbox.ts`, jÇ­ obrigatÇürio para eventos crÇðticos no
  mesmo `TransactWriteItems` do agregado ƒ?" `AGENTS.md` ¶õ7) `DocumentVersionValidityChanged
  {tenantId, documentId, versionId, validUntil}`.
- Um novo worker leve e idempotente, `requirement-evidence-refresh`, consome esse evento e
  re-deriva CADA `Requirement` vinculado, UM DE CADA VEZ, com seu prÇüprio loop de retry OCC ƒ?"
  exatamente a mesma disciplina que o worker diÇ­rio `requirement-reindex` (D-179/D-185) jÇ­ usa
  para o drift SATISFIEDƒÅ'NOT_SATISFIED por passagem de tempo (`requirement.ts`'s
  `deriveRequirementMaintenanceDue`). Este nÇœo Ç¸ um mecanismo novo de consistÇ¦ncia: Ç¸ o MESMO
  worker de reconciliaÇõÇœo assÇðncrona jÇ­ aprovado neste projeto, disparado por evento em vez de sÇü
  por agenda diÇ­ria.
- **Isto nÇœo Ç¸ uma fuga de responsabilidade ƒ?" Ç¸ o invariante que `requirement.ts` jÇ­ declara e
  aceita explicitamente** (linha 68-70 do arquivo: *"a DocumentVersion whose state changes AFTER
  the link ... is caught the next time something re-links or re-derives this Requirement, not
  instantly ƒ?" the same bounded-staleness window Decision 5 accepts"*). Um `confirmField` que muda
  `validUntil` Ç¸ exatamente essa classe de evento ƒ?" a Rodada 1 tentou fechar de forma sÇðncrona uma
  janela que o prÇüprio design de D-143 jÇ­ aceita como asynchronamente convergente. Esta rodada sÇü
  estreita a janela (evento-disparado, tipicamente segundos, contra "prÇüxima reindexaÇõÇœo diÇ­ria
  ou prÇüximo link/unlink manual").
- **Reverse lookup para o worker encontrar os Requirements de uma Version**: novo Çðndice esparso
  `GSI_EVIDENCE` (nome de infraestrutura a decidir na implementaÇõÇœo ƒ?" nÇœo Ç¸ GSI3/GSI6, nÇœo exige
  a exceÇõÇœo de isolamento de `AGENTS.md` ¶õ7), escrito/removido transacionalmente DENTRO de
  `linkEvidence`/`unlinkEvidence` (que jÇ­ escrevem `Requirement` numa transaÇõÇœo prÇüpria ƒ?" sÇü
  adicionam um par de atributos ao mesmo item, sem nova escrita separada):
  `GSI_EVIDENCE_PK = TENANT#t#DOCVERSION#<versionId>`, `GSI_EVIDENCE_SK = REQUIREMENT#<requirementId>`.
  Isso resolve 3.1 (Çðndice reverso existe) sem resolver 3.2 (corrida entre a query do worker e um
  `linkEvidence` concorrente) por sincronizaÇõÇœo ƒ?" resolve-a por ACEITAÇÎÇŸO EXPLÇ?CITA do mesmo
  bounded-staleness: se C se vincula depois que o worker jÇ­ leu A/B, C simplesmente lÇ¦
  `DocumentVersion.validUntil` jÇ­ atualizado no momento do seu PRÇ"PRIO `linkEvidence` (que sempre
  lÇ¦ a versÇœo corrente, nunca uma cÇüpia cacheada) ƒ?" C nunca fica desatualizado, Ç¸ A/B que ficariam
  presos ao valor antigo por um ciclo a mais se o worker corresse ANTES do link de C, cenÇ­rio
  idÇ¦ntico ao que o reindex diÇ­rio jÇ­ tolera hoje. Nenhuma cardinalidade aberta em nenhuma
  transaÇõÇœo ƒ?" o worker processa Requirements um a um, sem teto de 100 aÇõÇæes porque nunca hÇ­ uma
  transaÇõÇœo com N itens dinÇ½micos.

## 10. IdempotÇ¦ncia ƒ?" simplificada pela remoÇõÇœo do fan-out sÇðncrono

Com `Requirement` fora da transaÇõÇœo, o request hash de `confirmField`/`rejectField` volta a ser
exatamente do mesmo formato de hoje (`confirm-reject-field.ts:102-116`), sÇü trocando
`expectedDocumentVersion` por `expectedDocumentVersionVersion` (a versÇœo OCC de `DocumentVersion`,
nome literal para nÇœo colidir com `versionId`) ƒ?" nenhum campo dinÇ½mico de Requirement entra no
hash, porque nenhuma operaÇõÇœo em Requirement acontece dentro desta transaÇõÇœo. A janela
commit-then-fail-before-`complete()` jÇ­ existente (`idempotency.begin`/`transactWrite`/`complete`)
nÇœo muda de comportamento ƒ?" mesmo padrÇœo, mesmo risco residual jÇ­ aceito pelo cÇüdigo atual, nÇœo
introduzido nem agravado por esta rodada.

## 11. Checklist E-014 ƒ?" reponderado, aceitando a crÇðtica do Codex quase verbatim

Adoto a reponderaÇõÇœo do Codex, com pequenos ajustes de Ç½ncora explÇðcita:

1. **(peso 25%) Identidade/idempotÇ¦ncia corretas por `DocumentVersion`/`DocumentFile`.** Atende:
   `ExtractionRun` chaveado por `versionId` imutÇ­vel, starter revalida `DocumentFile` fresco antes
   de iniciar, nenhuma ambiguidade `seq` vs `versionId`. NÇœo atende: qualquer identidade que ainda
   dependa de `itemId` ou de um "nÇ§mero de versÇœo" ambÇðguo.
2. **(peso 25%) `validUntil`/`Requirement` convergem sem fan-out transacional aberto.** Atende: a
   transaÇõÇœo de confirmaÇõÇœo tem cardinalidade FIXA (3-way ou 2-way), Requirement atualiza via
   worker assÇðncrono idempotente um-a-um, dentro do bounded-staleness jÇ­ aceito por D-143. NÇœo
   atende: qualquer transaÇõÇœo cujo tamanho dependa do nÇ§mero de Requirements vinculados.
3. **(peso 20%) Trigger seguro contra a corrida S3-copy-antes-de-confirm.** Atende: o starter
   sempre re-lÇ¦ `DocumentFile` e verifica scanStatus/cleanObject/role/estado da Version antes de
   iniciar um run. NÇœo atende: qualquer caminho que inicie extraÇõÇœo sÇü a partir da chave do
   evento S3.
4. **(peso 15%) SemÇ½ntica multi-arquivo explÇðcita, nÇœo acidental.** Atende: sÇü PRINCIPAL dispara
   OCR, decisÇœo registrada como escopo (nÇœo uma omissÇœo). NÇœo atende: qualquer ambiguidade sobre
   o que acontece quando mÇ§ltiplos arquivos de uma Version sÇœo promovidos.
5. **(peso 10%) Preserva corretamente auto-confirmaÇõÇœo E revisÇœo humana com o MESMO efeito
   colateral.** Atende: `buildDocumentVersionValidityUpdate` compartilhada pelos dois caminhos.
   NÇœo atende: qualquer divergÇ¦ncia de efeito entre o path automÇ­tico e o manual.
6. **(peso 5%) ProveniÇ¦ncia auditÇ­vel de verdade.** Atende: `confirmedBy`/`confirmedAt`
   realmente existem em `ExtractedField` e sÇœo sempre preenchidos (inclusive
   `SYSTEM_AUTOCONFIRM`). NÇœo atende: alegar auditabilidade sem o campo persistido.

## Pontos ainda em aberto para a crÇðtica do Codex nesta rodada

- O nome/posicionamento exato do `GSI_EVIDENCE` (qual GSI fÇðsico do zero vs slot reaproveitado) ƒ?"
  deliberadamente deixado como detalhe de implementaÇõÇœo, nÇœo decisÇœo de protocolo, mas abro para
  o Codex contestar se isso Ç¸ council-worthy nesta rodada.
- Se `PRINCIPAL`-only Ç¸ a polÇðtica de produto correta de longo prazo, ou apenas a mais simples
  para nÇœo bloquear esta reconciliaÇõÇœo ƒ?" registrado como decisÇœo explÇðcita e revisÇ­vel, nÇœo uma
  afirmaÇõÇœo de que Ç¸ definitivamente certa para sempre.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -Encoding UTF8 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round1-codex-critique.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 283ms:
# Round 1 (Codex) - Critique

> Extracted from the raw exec transcript (`round1-codex-critique.txt`) - Codex's first attempt
> at this critique bled together with an unrelated prior review (`bulk-import-documents-...`)
> Codex read as a "structural reference"; only the final, on-topic critique (which explicitly
> re-verified every claim against this repo's real files) is kept here, verbatim from the
> transcript's last occurrence.

A Rodada 1 encontrou corretamente a desconexÆo f¡sica entre os pipelines, mas ainda nÆo apresenta um design aceit vel para implementa‡Æo. O diagn¢stico factual ‚ parcialmente s¢lido; a conclusÆo de "pipeline autoritativo", a incorpora‡Æo obrigat¢ria do worker ao escopo e, sobretudo, a transa‡Æo `3 + N Requirements` vÆo al‚m do que a evidˆncia sustenta.

## 1. Pipeline autoritativo: achado correto, conclusÆo superestimada

Confirmei no c¢digo: `malware-result-handler.ts:59` tenta apenas `parseQuarantineKey`/`parseSubmissionQuarantineKey`; uma chave `document-archive/...` cai no branch nÆo reconhecido, ‚ logada e descartada sem `batchItemFailures` (nÆo ‚ "silencioso" - h  log de erro - mas ‚ descarte definitivo sem retry/DLQ). `confirmFileScanClean` nÆo tem call site em handler real. `buildQuarantineKey` produz o namespace `document-archive/tenant/...`. `extraction-starter-handler.ts:42` s¢ entende o formato antigo. `document-classifier.ts` classifica formato f¡sico, nÆo `DocumentType` de neg¢cio - achado correto.

Trˆs excessos: (1) Terraform prova deploy/roteabilidade, nÆo volume/recˆncia de tr fego - o coment rio "real traffic already" ‚ evidˆncia hist¢rica de log group, nÆo de autoridade de produto; (2) "autoritativo" mistura trˆs conceitos (pipeline fisicamente completo / API que recebe tr fego / autoridade conceitual futura) - o c¢digo s¢ sustenta o primeiro; (3) a proposta omite que **`upload-finalizer-handler.ts` tamb‚m s¢ reconhece os formatos antigo e de submission** - nÆo basta corrigir `malware-result-handler`, o lado de upload finalizado do `document-archive` tamb‚m ‚ descartado, deixando arquivos presos em `PENDING_UPLOAD`, nÆo s¢ `SCANNING`.

**ConclusÆo corrigida**: o m¢dulo antigo ‚ hoje o £nico pipeline fisicamente completo no c¢digo implant vel. `document-archive` ‚ o modelo de dom¡nio aprovado com entrada HTTP/presign real, mas nÆo tem consumidores reais para NENHUM dos dois sinais f¡sicos (upload finalizer E malware result) que a correla‡Æo de scan exige.

## 2. Op‡Æo A ‚ um falso dilema e amplia indevidamente o escopo

O worker de promo‡Æo ‚ pr‚-requisito para ATIVAR o pipeline novo ponta-a-ponta, nÆo para DECIDIR corretamente o contrato de reconcilia‡Æo. Existe uma Op‡Æo C melhor delimitada: (1) desenhar a identidade de extra‡Æo contra `DocumentVersion`/`DocumentFile` agora; (2) definir o contrato do evento clean e as precondi‡äes que o starter deve validar; (3) implementar/testar a extra‡Æo re-chaveada sem lig -la ao trigger real; (4) implementar upload-finalizer + malware-result + promo‡Æo como fatia independente; (5) ativar o trigger novo s¢ quando ambas as fatias estiverem prontas. Isso nÆo exige re-key duas vezes.

Al‚m disso, o worker de promo‡Æo proposto est  nebuloso: nÆo define fila/schema do handoff, ordem publish-antes-ou-depois de `applyFileScanResult`, quem consome o S3 Object Created da quarentena, retry/DLQ, ownership do `CopyObject`, limpeza do objeto de quarentena, tenant lifecycle fence.

## 3. A transa‡Æo "3 + N Requirements" nÆo est  sound

- **3.1 Sem access pattern reverso**: `Requirement` mora na parti‡Æo do Subject (`PK=TENANT#t#SUBJECT#s`); `evidenceVersionId` ‚ s¢ atributo, sem ¡ndice "todas as Requirements que apontam para esta versÆo" - "atualizar todos os Requirements" nÆo ‚ descobr¡vel sem Scan.
- **3.2 Phantom race**: uma query pr‚-transa‡Æo nÆo fecha a corrida contra um `linkEvidence` concorrente que vincula um Requirement C … mesma versÆo entre a leitura e o commit - OCC de A/B nÆo protege a ausˆncia de C.
- **3.3 Limite transacional**: `TransactWriteItems` tem teto de 100 a‡äes; cardinalidade aberta por v¡nculos ‚ design inaceit vel mesmo abaixo do limite t‚cnico (conten‡Æo/custo imprevis¡veis).
- **3.4 `linkEvidence()` nÆo ‚ reus vel como builder** - faz autoriza‡Æo + leitura + deriva‡Æo + update + sua pr¢pria transa‡Æo; nÆo pode ser chamado dentro de outro `TransactWriteItems` sem extra‡Æo expl¡cita de um planner puro.
- **3.5 Auto-confirma‡Æo esquecida (bloqueante central)**: `run-extraction-validation.ts:229` + `dynamodb-extracted-field-store.ts:52` j  escrevem o agregado de neg¢cio dentro de `commitRunOutcome` para campos auto-confirmados - a proposta afirma (contraditoriamente) que s¢ `confirmField` grava valor de neg¢cio.
- **3.6 Idempotˆncia subespecificada**: request hash nÆo diz como incorpora seq/versionId/fileId, versäes esperadas de Requirements descobertos dinamicamente, nem o comportamento da janela commit-then-fail-before-complete().
- **3.7 Confirm/reject nÆo separados com precisÆo** para o novo modelo (reject nunca deveria tocar `validUntil`; auto-confirm precisa da mesma semƒntica do confirm manual).

## 4. Riscos adicionais omitidos

- **Multi-arquivo/PRINCIPAL**: `DocumentFile` permite PRINCIPAL + anexos; se cada objeto promovido gerar evento clean, m£ltiplos arquivos podem disparar OCR concorrente para a mesma Version. Pol¡tica (s¢ PRINCIPAL dispara? cada arquivo tem run pr¢prio?) nÆo definida. Identidade `{documentId,seq,fileId}` proposta nÆo bate com o `ExtractionRun` atual (ainda tem `itemId` e `documentVersion: number` amb¡guo).
- **Evento clean antes da confirma‡Æo DynamoDB**: `CopyObject` gera S3 Object Created antes/possivelmente-fora-de-ordem da transa‡Æo `confirmFileScanClean` - o starter nÆo pode confiar s¢ na chave, precisa reconsultar `DocumentFile` (scanStatus=CLEAN, cleanObject bate exatamente, ‚ PRINCIPAL, versÆo nÆo retirada, tenant lifecycle ok).
- **Conven‡Æo de chave clean subespecificada** (por que `seq` e nÆo `versionId` imut vel; encoding/estabilidade sob retry).
- **Estados de `DocumentVersion` eleg¡veis para extra‡Æo** nÆo definidos (o que acontece se rejeitada/retirada/superseded durante o run).
- **Proveniˆncia humana insuficiente**: `ExtractedField` nÆo tem `confirmedBy`/`confirmedAt` - o checklist da proposta afirma auditabilidade que a pr¢pria entidade nÆo sustenta (falha seu pr¢prio crit‚rio 4).

## 5. E-014 e checklist

`SIM PARCIAL` ‚ razo vel, mas a pesquisa ‚ perif‚rica … decisÆo real (identidade, fan-out transacional, promo‡Æo S3, consistˆncia entre agregados - nÆo human-in-the-loop, que nÆo est  sendo redesenhado). Checklist mal ponderado: 55% repete a mesma garantia (preservar review/nÆo autoaceitar mismatch), 25% contradiz a auto-confirma‡Æo j  existente, 20% exige `confirmedBy` que o modelo nÆo persiste, e nenhum peso cobre atomicidade/idempotˆncia/identidade/multi-file/corrida copy-before-confirm/cardinalidade - uma proposta poderia tirar nota m xima nele e ainda perder updates de Requirement ou extrair um objeto ¢rfÆo.

**Repondera‡Æo sugerida para Rodada 2**: 25% identidade/idempotˆncia por DocumentVersion/DocumentFile; 25% atomicidade/convergˆncia de `validUntil`+Requirements; 20% seguran‡a do trigger e corrida S3-copy/DynamoDB-confirm; 15% semƒntica multi-arquivo; 10% preserva‡Æo de auto-confirma‡Æo/revisÆo humana; 5% proveniˆncia/auditoria.

## O que deve mudar na Rodada 2 (lista completa do Codex)

1. Reescrever o achado central distinguindo completude f¡sica, tr fego observado e autoridade futura.
2. Incluir o gap real do `upload-finalizer-handler`, nÆo s¢ o resultado GuardDuty.
3. Adicionar a Op‡Æo C: design/re-key independente, promoter separado, ativa‡Æo s¢ ap¢s ambos prontos.
4. Definir precisamente a arquitetura do promoter (fila/schema, ordem, retry/DLQ, ownership do CopyObject, cleanup, tenant fence).
5. Especificar ordem copy/confirm, objeto ¢rfÆo, valida‡Æo autoritativa no starter.
6. Decidir pol¡tica PRINCIPAL vs ATTACHMENT.
7. Redesenhar identidade de `ExtractionRun` (eliminar ambiguidade OCC `version` vs `DocumentVersion.seq/versionId`).
8. Tratar explicitamente o caminho de auto-confirma‡Æo, nÆo s¢ o confirm HTTP.
9. Abandonar ou redesenhar "3+N Requirements": access pattern reverso, cardinalidade, phantom fence, limite de 100 a‡äes, estrat‚gia de convergˆncia.
10. Especificar expected versions e request hash de idempotˆncia, incl. falha entre commit e complete().
11. Separar transa‡äes de confirm/reject/auto-confirm por efeitos reais.
12. Definir estados de `DocumentVersion` eleg¡veis e comportamento sob transi‡Æo concorrente.
13. Corrigir checklist E-014 para medir os riscos reais.
14. NÆo afirmar auditabilidade de "quem confirmou" sem fonte dur vel para isso.

**Nota (cega): 5.6/10.**


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -Encoding UTF8 'docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/round2-claude-revision.md'; Get-Content -Raw -Encoding UTF8 'src/runtime/aws/handlers/upload-finalizer-handler.ts'; Get-Content -Raw -Encoding UTF8 'src/runtime/aws/handlers/malware-result-handler.ts'; Get-Content -Raw -Encoding UTF8 'src/modules/extraction/application/run-extraction-validation.ts'; Get-Content -Raw -Encoding UTF8 'src/modules/extraction/persistence/dynamodb-extracted-field-store.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 303ms:
# Round 2 (Claude) - Revision

**Claude's own blind score for Round 1 (recorded now that Codex's Round 1 score exists): 5.8/10.**
I agree with the substance of the critique: the authoritative-pipeline finding conflated three
distinct claims, Option A was unjustified scope creep, and the "3+N Requirements" transaction was
not sound (open cardinality, phantom race, no reverse index, ignored the existing auto-confirm
path). Every point below is addressed on the merits, not to chase a number.

## 1. Achado central - reescrito, trˆs conceitos separados

Corrigindo o excesso apontado: "autoritativo" tinha trˆs sentidos diferentes na Rodada 1, e o
c¢digo s¢ sustenta o primeiro.

- **Pipeline fisicamente completo hoje**: s¢ o m¢dulo `document` (antigo). Confirmado agora dos
  dois lados da correla‡Æo sim‚trica, nÆo s¢ do lado de malware result - **achado novo desta
  rodada**: `upload-finalizer-handler.ts` (o consumidor do evento de upload finalizado, o outro
  sinal f¡sico que `apply-file-scan-result.ts`'s correla‡Æo exige) reconhece apenas
  `parseQuarantineKey` (antigo) e `parseSubmissionQuarantineKey` (subject) - o mesmo buraco do
  `malware-result-handler.ts`, no sinal oposto. Um arquivo enviado via `document-archive` fica
  preso em `PENDING_UPLOAD` (nunca chega a `SCANNING`), nÆo s¢ em `SCANNING` como a Rodada 1
  disse - pior do que eu tinha reportado.
- **API que recebe tr fego HTTP**: ambos os handlers estÆo deployados e rote veis
  (`infra/main.tf`). Retiro a afirma‡Æo de "tr fego real" para `document_archive_handler` - nÆo
  verific vel a partir de Terraform/coment rio hist¢rico, s¢ CloudWatch real (fora do escopo
  desta rodada de design). Mantenho, sem enfraquecer, que `documents_handler` tem o coment rio
  hist¢rico citando tr fego real - ‚ evidˆncia de uso passado do antigo, nÆo prova de ausˆncia de
  uso do novo.
- **Autoridade conceitual futura**: `document-archive`/`DocumentVersion`/`Requirement` - nunca
  esteve em disputa; D-143 j  ‚ a arquitetura aprovada, este documento nÆo a reabre.

**ConclusÆo corrigida (adotada)**: o m¢dulo antigo ‚ hoje o £nico pipeline fisicamente completo
no c¢digo implant vel, nos dois lados da correla‡Æo (upload finalizer E malware result).
`document-archive` ‚ o modelo de dom¡nio aprovado, com entrada HTTP/presign real, mas sem NENHUM
consumidor f¡sico real para nenhum dos dois sinais que sua pr¢pria correla‡Æo de evidˆncia exige.

## 2. Op‡Æo C adotada - sequenciamento, nÆo escopo indivis¡vel

Abandono a falsa dicotomia A/B da Rodada 1. Adoto a **Op‡Æo C do Codex** como a decisÆo desta
rodada:

1. Esta rodada decide a identidade/contrato de extra‡Æo contra `DocumentVersion`/`DocumentFile`
   (se‡äes 3-6 abaixo) e o contrato do evento clean + precondi‡äes de valida‡Æo no starter.
2. A implementa‡Æo da extra‡Æo re-chaveada ‚ constru¡da e testada contra esse contrato, mas o
   trigger real (`clean_object_created` reconhecendo o novo prefixo) fica **desligado por feature
   flag** (mesmo mecanismo j  usado por `extraction_pipeline_enabled` em `infra/main.tf:1965`,
   `1979`, `1986` - um segundo flag, nÆo um mecanismo novo) at‚ a fatia #2 existir.
2'. A fatia de ingestÆo f¡sica (upload-finalizer + malware-result + promo‡Æo, item 3 abaixo) ‚
   implementada e testada independentemente.
3. O flag do trigger novo s¢ ‚ ligado quando ambas as fatias estÆo prontas e verificadas em
   `dev`.

Isso nÆo exige re-key duas vezes - a extra‡Æo ‚ desenhada uma vez contra o alvo certo.

## 3. Arquitetura do promoter - sem worker/fila novos, estendendo os dois handlers existentes

Rejeito a "nova fila SQS" da Rodada 1 (a cr¡tica est  certa: nebulosa, duplicava l¢gica). Em vez
disso, mesma forma que o m¢dulo `subject` j  usa para coexistir com o m¢dulo `document` no MESMO
par de handlers (`malware-result-handler.ts` j  ramifica em `parseQuarantineKey` vs
`parseSubmissionQuarantineKey` - dois branches, um handler, duas filas de origem j  existentes
reaproveitadas):

- **`upload-finalizer-handler.ts`**: adicionar um terceiro branch, `parseDocumentArchiveQuarantineKey`
  (novo parser, mesmo estilo de `quarantine-key.ts`), chamando `applyFileScanResult` com
  `uploadEvidence` preenchido (em vez do `processMalwareResult`/`processSubmissionMalwareResult`
  do m¢dulo antigo/subject) - reaproveita a MESMA fila `upload_finalizer_queue`/EventBridge rule
  j  cabeada ao bucket de quarentena inteiro (`infra/main.tf:1027-1048`, nÆo filtra por prefixo),
  s¢ adiciona uma terceira ramifica‡Æo de parsing no handler que j  a consome.
- **`malware-result-handler.ts`**: mesmo padrÆo, terceiro branch chamando `applyFileScanResult`
  com `malwareEvidence` preenchido.
- **Quem copia quarentenaclean e chama `confirmFileScanClean`**: o MESMO handler cujo branch
  produziu `outcome: "READY_TO_PROMOTE"` faz o `CopyObject` (quarantineclean, verifica‡Æo de
  tamanho/checksum, mesma disciplina de `advance-after-evidence.ts`) e s¢ entÆo chama
  `confirmFileScanClean` - nunca os dois branches (upload-finalizer e malware-result) competindo
  para promover o mesmo arquivo, porque `applyFileScanResult` j  ‚ o £nico ponto de decisÆo
  (`decision.action === "PROMOTE"` s¢ ‚ alcan‡ado depois que AMBAS as evidˆncias consolidaram,
  nÆo importa qual handler entregou a segunda - `apply-file-scan-result.ts`'s doc comment j 
  garante isso, reaproveitado sem mudan‡a).
- **Retry/DLQ/ownership**: idˆnticos aos que j  existem para o branch antigo nos mesmos dois
  handlers - nenhum mecanismo novo, s¢ um terceiro `if (parsed) { ... }` ao lado dos dois j 
  existentes em cada handler.
- **Limpeza do objeto de quarentena**: fora de escopo desta rodada - o m¢dulo antigo tamb‚m nÆo
  limpa o objeto de quarentena ap¢s promo‡Æo (`advance-after-evidence.ts` nÆo faz
  `DeleteObject`), entÆo esta rodada mant‚m paridade comportamental, nÆo introduz uma lacuna
  nova.
- **Tenant lifecycle fence**: reaproveita o fence que `processMalwareResult`/`processSubmissionMalwareResult`
  j  aplicam antes de escrever (checagem de tenant ACTIVE) - o novo branch chama a mesma
  checagem, nunca pula essa etapa.

## 4. Chave clean - versionId, nÆo seq (corre‡Æo do achado)

Corrigindo a Rodada 1: a nova conven‡Æo usa o `versionId` imut vel de `DocumentVersion`, nunca o
`seq` mut vel-por-natureza-de-¡ndice: `document-archive/clean/<tenantId>/<documentId>/<versionId>/<fileId>`.
`versionId` j  existe como identificador est vel e imut vel de uma `DocumentVersion`
(`document-version.ts`), evitando o problema apontado pelo Codex de reindexa‡Æo sob retry/GSI. O
novo parser (`parseDocumentArchiveCleanKey`, ao lado do j  existente `parseCleanKey` do m¢dulo
antigo - os dois formatos coexistem, nenhum removido nesta rodada) devolve
`{tenantId, documentId, versionId, fileId}`.

## 5. Starter - o evento S3 nunca ‚ confiado sozinho

Corrigindo o achado do Codex (evento clean pode chegar antes/fora de ordem do commit
`confirmFileScanClean`): `extraction-starter-handler.ts`, ao reconhecer o novo prefixo, SEMPRE
re-lˆ `DocumentFile` fresco (nunca confia s¢ na chave do evento S3) e s¢ prossegue se TODAS as
condi‡äes valem:

1. `file.scanStatus === "CLEAN"`;
2. `file.cleanObject` bate exatamente (bucket/key/versionId) com o objeto observado pelo evento
   S3 - mesma disciplina de correla‡Æo sim‚trica que `apply-file-scan-result.ts` j  usa entre
   evidˆncias;
3. `file.role === "PRINCIPAL"` (pol¡tica decidida no item 6 abaixo - anexos nunca disparam OCR
   nesta fase);
4. a `DocumentVersion` correspondente est  em um estado eleg¡vel: `RECEIVED` ou `UNDER_REVIEW`
   (nunca `WITHDRAWN`/`REJECTED`/`SUPERSEDED` - se a versÆo j  saiu de considera‡Æo entre a
   promo‡Æo f¡sica e este evento, a extra‡Æo ‚ descartada como `IGNORED_STALE`, mesmo vocabul rio
   que `applyFileScanResult` j  usa). `ACCEPTED` tamb‚m ‚ eleg¡vel (evidˆncia j  aceita pode
   ainda precisar de extra‡Æo de campos de validade que a revisÆo humana nÆo preencheu).

Uma falha em qualquer condi‡Æo ‚ um `IGNORED_STALE`/log, nunca uma tentativa de iniciar extra‡Æo
sobre um objeto ¢rfÆo.

## 6. Multi-arquivo - s¢ PRINCIPAL dispara OCR (decisÆo expl¡cita, nÆo default acidental)

`DocumentFile` permite um `PRINCIPAL` e anexos (`document-file.ts`). DecisÆo expl¡cita desta
rodada: **s¢ a promo‡Æo do arquivo `PRINCIPAL` gera um `ExtractionRun`.** Anexos promovidos nunca
disparam extra‡Æo nesta fase - decisÆo de produto deliberadamente conservadora (anexos
tipicamente sÆo material de apoio, nÆo o documento cujo campo de validade importa), registrada
aqui como escopo expl¡cito, revis vel por um item de trabalho futuro caso o produto precise de
extra‡Æo multi-arquivo. Isso elimina inteiramente a corrida "m£ltiplos arquivos disparando
execu‡äes concorrentes para a mesma Version" apontada pelo Codex - nÆo porque a corrida foi
resolvida por sincroniza‡Æo, mas porque s¢ existe um candidato poss¡vel por Version.

## 7. Identidade de `ExtractionRun`/`ExtractedField` - redesenhada, sem ambiguidade

- `ExtractionRun` passa a ser identificado por `{tenantId, documentId, versionId}` - nunca mais
  `itemId` (nÆo existe no novo modelo) nem o campo amb¡guo `documentVersion: number` (que hoje
  mistura o conceito de "n£mero de versÆo OCC do agregado Document" com "qual versÆo l¢gica do
  documento" - os dois precisam se separar). `PK = TENANT#t#DOC#d`, `SK = RUN#<versionId>#<runId>`
  - mesmo padrÆo de particionamento por Document que j  existe, s¢ troca o que qualifica a versÆo
  l¢gica.
- `ExtractedField` mant‚m sua chave atual (`extracted-field.ts:51-53`, `PK=TENANT#t#DOC#d`,
  `SK=FIELD#<fieldName>#<runId>`) sem mudan‡a - j  nÆo dependia de `itemId`.
- Novos campos em `ExtractedField`: `confirmedBy: string` (o `RequestContext` do ator que
  confirmou/rejeitou - nunca "SYSTEM" para o caminho manual) e `confirmedAt: string`, setados
  junto com `state` na mesma escrita que j  seta `state`/`confirmedValue`/`correctionReason` -
  fecha o achado do Codex de que o checklist da Rodada 1 prometia auditabilidade que a entidade
  nÆo sustentava. Caminho de auto-confirma‡Æo (item 8) grava `confirmedBy: "SYSTEM_AUTOCONFIRM"`
  explicitamente, nunca omite o campo.

## 8. Auto-confirma‡Æo - unificada com o confirm manual via fun‡Æo pura compartilhada

Corrigindo o bloqueante 3.5 do Codex: `run-extraction-validation.ts`/`dynamodb-extracted-field-store.ts`'s
`commitRunOutcome` j  escreve o agregado de neg¢cio para campos auto-confirmados hoje - a
proposta da Rodada 1 (s¢ `confirmField` grava valor de neg¢cio) estava errada e contradizia o
c¢digo real.

Corre‡Æo: extrair uma fun‡Æo pura `buildDocumentVersionValidityUpdate(fieldName, confirmedValue,
currentVersionState)` (nova, em `extraction/domain/`, test vel sem I/O - mesmo padrÆo que
`item-field-mapping.ts`'s `buildItemAttributeUpdate` j  estabelece) que os DOIS caminhos chamam:

- `doConfirmField` (HTTP, humano) - chama-a com `confirmedBy = ctx.identity`, dentro da mesma
  transa‡Æo 3-way (item 9).
- `commitRunOutcome` (auto-confirm, `run-extraction-validation.ts`) - chama-a com
  `confirmedBy = "SYSTEM_AUTOCONFIRM"`, dentro da MESMA transa‡Æo que j  escreve `ExtractedField`
  auto-confirmado (nunca uma segunda transa‡Æo separada).

Os dois caminhos produzem exatamente o mesmo efeito colateral sobre `DocumentVersion` - que campo
foi atualizado, com qual valor - atrav‚s da mesma fun‡Æo, nunca duas implementa‡äes divergentes
(mesmo princ¡pio que `confirm-reject-field.ts`'s doc comment j  cita para o `ExpirationItem`
antigo: "os dois caminhos devem produzir efeito de item idˆntico").

## 9. Transa‡Æo de confirma‡Æo - 3-way fixo, Requirement NUNCA na mesma transa‡Æo

Abandono por completo o "3+N Requirements" da Rodada 1 - o Codex est  certo de que nÆo fecha
(sem ¡ndice reverso, corrida fantasma, teto de 100 a‡äes, cardinalidade aberta). A corre‡Æo nÆo ‚
consertar o fan-out s¡ncrono, ‚ removˆ-lo:

- **`confirmField`**: transa‡Æo 3-way fixa - `DocumentVersion` (usa a fun‡Æo do item 8),
  `ExtractionRun` (prote‡Æo OCC, sem mudan‡a de dado), `ExtractedField` (stateCONFIRMED,
  confirmedValue, confirmedBy, confirmedAt). Exatamente a mesma cardinalidade fixa que a Rodada 1
  j  tinha para o 3-way original - s¢ troca `Document`(antigo) por `DocumentVersion`.
- **`rejectField`**: 2-way - `ExtractionRun` (prote‡Æo) + `ExtractedField` (stateREJECTED) -
  NUNCA toca `DocumentVersion`, corrigindo a formula‡Æo "3 vias sempre" da Rodada 1 apontada como
  excessivamente ampla no achado 3.7. Reject ‚ estritamente mais simples que hoje (era 3-way
  contra `Document` antigo; passa a nÆo precisar nem tocar o agregado de documento, j  que rejeitar
  um campo nunca muda `validUntil`).
- **`Requirement` nunca entra em nenhuma dessas transa‡äes.** Em vez disso: quando
  `buildDocumentVersionValidityUpdate` produz uma mudan‡a real de `validUntil`, a MESMA transa‡Æo
  emite um evento outbox (`src/shared/outbox/outbox.ts`, j  obrigat¢rio para eventos cr¡ticos no
  mesmo `TransactWriteItems` do agregado - `AGENTS.md` õ7) `DocumentVersionValidityChanged
  {tenantId, documentId, versionId, validUntil}`.
- Um novo worker leve e idempotente, `requirement-evidence-refresh`, consome esse evento e
  re-deriva CADA `Requirement` vinculado, UM DE CADA VEZ, com seu pr¢prio loop de retry OCC -
  exatamente a mesma disciplina que o worker di rio `requirement-reindex` (D-179/D-185) j  usa
  para o drift SATISFIEDNOT_SATISFIED por passagem de tempo (`requirement.ts`'s
  `deriveRequirementMaintenanceDue`). Este nÆo ‚ um mecanismo novo de consistˆncia: ‚ o MESMO
  worker de reconcilia‡Æo ass¡ncrona j  aprovado neste projeto, disparado por evento em vez de s¢
  por agenda di ria.
- **Isto nÆo ‚ uma fuga de responsabilidade - ‚ o invariante que `requirement.ts` j  declara e
  aceita explicitamente** (linha 68-70 do arquivo: *"a DocumentVersion whose state changes AFTER
  the link ... is caught the next time something re-links or re-derives this Requirement, not
  instantly - the same bounded-staleness window Decision 5 accepts"*). Um `confirmField` que muda
  `validUntil` ‚ exatamente essa classe de evento - a Rodada 1 tentou fechar de forma s¡ncrona uma
  janela que o pr¢prio design de D-143 j  aceita como asynchronamente convergente. Esta rodada s¢
  estreita a janela (evento-disparado, tipicamente segundos, contra "pr¢xima reindexa‡Æo di ria
  ou pr¢ximo link/unlink manual").
- **Reverse lookup para o worker encontrar os Requirements de uma Version**: novo ¡ndice esparso
  `GSI_EVIDENCE` (nome de infraestrutura a decidir na implementa‡Æo - nÆo ‚ GSI3/GSI6, nÆo exige
  a exce‡Æo de isolamento de `AGENTS.md` õ7), escrito/removido transacionalmente DENTRO de
  `linkEvidence`/`unlinkEvidence` (que j  escrevem `Requirement` numa transa‡Æo pr¢pria - s¢
  adicionam um par de atributos ao mesmo item, sem nova escrita separada):
  `GSI_EVIDENCE_PK = TENANT#t#DOCVERSION#<versionId>`, `GSI_EVIDENCE_SK = REQUIREMENT#<requirementId>`.
  Isso resolve 3.1 (¡ndice reverso existe) sem resolver 3.2 (corrida entre a query do worker e um
  `linkEvidence` concorrente) por sincroniza‡Æo - resolve-a por ACEITA€ÇO EXPLÖCITA do mesmo
  bounded-staleness: se C se vincula depois que o worker j  leu A/B, C simplesmente lˆ
  `DocumentVersion.validUntil` j  atualizado no momento do seu PRàPRIO `linkEvidence` (que sempre
  lˆ a versÆo corrente, nunca uma c¢pia cacheada) - C nunca fica desatualizado, ‚ A/B que ficariam
  presos ao valor antigo por um ciclo a mais se o worker corresse ANTES do link de C, cen rio
  idˆntico ao que o reindex di rio j  tolera hoje. Nenhuma cardinalidade aberta em nenhuma
  transa‡Æo - o worker processa Requirements um a um, sem teto de 100 a‡äes porque nunca h  uma
  transa‡Æo com N itens dinƒmicos.

## 10. Idempotˆncia - simplificada pela remo‡Æo do fan-out s¡ncrono

Com `Requirement` fora da transa‡Æo, o request hash de `confirmField`/`rejectField` volta a ser
exatamente do mesmo formato de hoje (`confirm-reject-field.ts:102-116`), s¢ trocando
`expectedDocumentVersion` por `expectedDocumentVersionVersion` (a versÆo OCC de `DocumentVersion`,
nome literal para nÆo colidir com `versionId`) - nenhum campo dinƒmico de Requirement entra no
hash, porque nenhuma opera‡Æo em Requirement acontece dentro desta transa‡Æo. A janela
commit-then-fail-before-`complete()` j  existente (`idempotency.begin`/`transactWrite`/`complete`)
nÆo muda de comportamento - mesmo padrÆo, mesmo risco residual j  aceito pelo c¢digo atual, nÆo
introduzido nem agravado por esta rodada.

## 11. Checklist E-014 - reponderado, aceitando a cr¡tica do Codex quase verbatim

Adoto a repondera‡Æo do Codex, com pequenos ajustes de ƒncora expl¡cita:

1. **(peso 25%) Identidade/idempotˆncia corretas por `DocumentVersion`/`DocumentFile`.** Atende:
   `ExtractionRun` chaveado por `versionId` imut vel, starter revalida `DocumentFile` fresco antes
   de iniciar, nenhuma ambiguidade `seq` vs `versionId`. NÆo atende: qualquer identidade que ainda
   dependa de `itemId` ou de um "n£mero de versÆo" amb¡guo.
2. **(peso 25%) `validUntil`/`Requirement` convergem sem fan-out transacional aberto.** Atende: a
   transa‡Æo de confirma‡Æo tem cardinalidade FIXA (3-way ou 2-way), Requirement atualiza via
   worker ass¡ncrono idempotente um-a-um, dentro do bounded-staleness j  aceito por D-143. NÆo
   atende: qualquer transa‡Æo cujo tamanho dependa do n£mero de Requirements vinculados.
3. **(peso 20%) Trigger seguro contra a corrida S3-copy-antes-de-confirm.** Atende: o starter
   sempre re-lˆ `DocumentFile` e verifica scanStatus/cleanObject/role/estado da Version antes de
   iniciar um run. NÆo atende: qualquer caminho que inicie extra‡Æo s¢ a partir da chave do
   evento S3.
4. **(peso 15%) Semƒntica multi-arquivo expl¡cita, nÆo acidental.** Atende: s¢ PRINCIPAL dispara
   OCR, decisÆo registrada como escopo (nÆo uma omissÆo). NÆo atende: qualquer ambiguidade sobre
   o que acontece quando m£ltiplos arquivos de uma Version sÆo promovidos.
5. **(peso 10%) Preserva corretamente auto-confirma‡Æo E revisÆo humana com o MESMO efeito
   colateral.** Atende: `buildDocumentVersionValidityUpdate` compartilhada pelos dois caminhos.
   NÆo atende: qualquer divergˆncia de efeito entre o path autom tico e o manual.
6. **(peso 5%) Proveniˆncia audit vel de verdade.** Atende: `confirmedBy`/`confirmedAt`
   realmente existem em `ExtractedField` e sÆo sempre preenchidos (inclusive
   `SYSTEM_AUTOCONFIRM`). NÆo atende: alegar auditabilidade sem o campo persistido.

## Pontos ainda em aberto para a cr¡tica do Codex nesta rodada

- O nome/posicionamento exato do `GSI_EVIDENCE` (qual GSI f¡sico do zero vs slot reaproveitado) -
  deliberadamente deixado como detalhe de implementa‡Æo, nÆo decisÆo de protocolo, mas abro para
  o Codex contestar se isso ‚ council-worthy nesta rodada.
- Se `PRINCIPAL`-only ‚ a pol¡tica de produto correta de longo prazo, ou apenas a mais simples
  para nÆo bloquear esta reconcilia‡Æo - registrado como decisÆo expl¡cita e revis vel, nÆo uma
  afirma‡Æo de que ‚ definitivamente certa para sempre.

/** Real handler for UploadFinalizerWorker (SQS, fed by S3 "Object Created" events on the
 * quarantine bucket routed through EventBridge). M6 design õ3.2/õ4. */
import type { SQSBatchResponse, SQSEvent } from "aws-lambda";
import { randomUUID } from "node:crypto";
import { createDocumentClient } from "../../../shared/dynamodb/client.js";
import { buildDocumentWorkerDeps } from "../composition/document.js";
import { finalizeUpload } from "../../../workers/upload-finalizer/finalizer.js";
import { parseQuarantineKey } from "../../../modules/document/domain/quarantine-key.js";
// M10 (D-037): branch puramente aditivo para o namespace de key de guest upload
// (`tenant/.../subject/...`) - nunca sobrepäe o formato item-anchored acima
// (`tenant/.../item/...`), que continua resolvido pelo parser/fluxo originais sem nenhuma
// mudan‡a de comportamento.
import { parseSubmissionQuarantineKey } from "../../../modules/subject/domain/submission-quarantine-key.js";
import { finalizeSubmissionUpload } from "../../../workers/submission-finalizer/finalizer.js";
import { buildSubjectWorkerDeps } from "../composition/subject.js";
import { runWithContext } from "../../../shared/observability/context.js";
import { SecureLogger } from "../../../shared/observability/logger.js";

const client = createDocumentClient();
const tableName = process.env["TABLE_NAME"];
const cleanBucket = process.env["CLEAN_BUCKET_NAME"];
const parserFunctionName = process.env["PARSER_SANDBOX_FUNCTION_NAME"];
if (!tableName) throw new Error("TABLE_NAME env var is required.");
if (!cleanBucket) throw new Error("CLEAN_BUCKET_NAME env var is required.");
if (!parserFunctionName) throw new Error("PARSER_SANDBOX_FUNCTION_NAME env var is required.");
const deps = buildDocumentWorkerDeps(client, tableName, cleanBucket, parserFunctionName);
const submissionDeps = buildSubjectWorkerDeps(client, tableName, cleanBucket, parserFunctionName);
const logger = new SecureLogger({ baseContext: { service: "upload-finalizer" } });

/** Real EventBridge "Object Created" detail shape for an S3 source
 * (docs.aws.amazon.com/AmazonS3/latest/userguide/EventBridge.html). */
interface S3ObjectCreatedDetail {
  bucket: { name: string };
  object: { key: string; "version-id"?: string; size?: number };
}

export async function handler(event: SQSEvent): Promise<SQSBatchResponse> {
  const batchItemFailures: { itemIdentifier: string }[] = [];

  for (const record of event.Records) {
    await runWithContext({ correlationId: randomUUID() }, async () => {
      try {
        const message = JSON.parse(record.body) as { detail?: S3ObjectCreatedDetail };
        const detail = message.detail;
        if (!detail?.bucket?.name || !detail.object?.key || !detail.object["version-id"]) {
          logger.error("upload-finalizer malformed S3 event", { messageId: record.messageId });
          batchItemFailures.push({ itemIdentifier: record.messageId });
          return;
        }

        const parsed = parseQuarantineKey(detail.object.key);
        if (parsed) {
          await runWithContext({ correlationId: randomUUID(), tenantId: parsed.tenantId }, async () => {
            const outcome = await finalizeUpload(deps, {
              tenantId: parsed.tenantId,
              itemId: parsed.itemId,
              documentId: parsed.documentId,
              object: { bucket: detail.bucket.name, key: detail.object.key, versionId: detail.object["version-id"]! },
            });
            logger.info("upload-finalizer outcome", { documentId: parsed.documentId, outcome });
          });
          return;
        }

        // M10: nÆo ‚ uma key item-anchored (M6) - tenta o namespace de guest submission antes
        // de desistir. Os dois formatos nunca colidem (segmentos "item" vs "subject").
        const parsedSubmission = parseSubmissionQuarantineKey(detail.object.key);
        if (!parsedSubmission) {
          // A key this handler doesn't recognize (never produced by either reserveUpload flow)
          // is not a retryable failure - it can never resolve on retry. Log and drop, never DLQ-loop.
          logger.error("upload-finalizer unrecognized key shape", { key: detail.object.key });
          return;
        }

        await runWithContext({ correlationId: randomUUID(), tenantId: parsedSubmission.tenantId }, async () => {
          const outcome = await finalizeSubmissionUpload(submissionDeps, {
            tenantId: parsedSubmission.tenantId,
            subjectId: parsedSubmission.subjectId,
            assignmentId: parsedSubmission.assignmentId,
            submissionId: parsedSubmission.submissionId,
            object: { bucket: detail.bucket.name, key: detail.object.key, versionId: detail.object["version-id"]! },
          });
          logger.info("upload-finalizer submission outcome", { submissionId: parsedSubmission.submissionId, outcome });
        });
      } catch (err) {
        // Real incident (2026-08-22): a bare `err.message` of "UnknownError" (AWS SDK v3's
        // fallback for a response it couldn't classify) gave zero diagnostic signal for a real
        // production-blocking promotion failure. Logging name/code/$metadata too - none of
        // these are secrets (HTTP status, request id, AWS error code), safe for SecureLogger.
        const e = err as { name?: string; message?: string; Code?: string; $metadata?: unknown } | undefined;
        logger.error("upload-finalizer failed", {
          messageId: record.messageId,
          error: err instanceof Error ? err.message : String(err),
          errorName: e?.name,
          errorCode: e?.Code,
          errorMetadata: e?.$metadata,
        });
        batchItemFailures.push({ itemIdentifier: record.messageId });
      }
    });
  }

  return { batchItemFailures };
}

/** Real handler for MalwareResultWorker (SQS, fed by GuardDuty Malware Protection for S3
 * finding events routed through EventBridge). M6 design õ3.4/õ4. */
import type { SQSBatchResponse, SQSEvent } from "aws-lambda";
import { randomUUID } from "node:crypto";
import { createDocumentClient } from "../../../shared/dynamodb/client.js";
import { buildDocumentWorkerDeps } from "../composition/document.js";
import { processMalwareResult } from "../../../workers/malware-result/result-processor.js";
import { parseQuarantineKey } from "../../../modules/document/domain/quarantine-key.js";
import type { GuardDutyScanStatus } from "../../../modules/document/domain/malware-scan-result.js";
// M10 (D-037): mesmo branch aditivo do upload-finalizer-handler.ts, ver coment rio l .
import { parseSubmissionQuarantineKey } from "../../../modules/subject/domain/submission-quarantine-key.js";
import { processSubmissionMalwareResult } from "../../../workers/submission-malware-result/result-processor.js";
import { buildSubjectWorkerDeps } from "../composition/subject.js";
import { runWithContext } from "../../../shared/observability/context.js";
import { SecureLogger } from "../../../shared/observability/logger.js";

const client = createDocumentClient();
const tableName = process.env["TABLE_NAME"];
const cleanBucket = process.env["CLEAN_BUCKET_NAME"];
const parserFunctionName = process.env["PARSER_SANDBOX_FUNCTION_NAME"];
if (!tableName) throw new Error("TABLE_NAME env var is required.");
if (!cleanBucket) throw new Error("CLEAN_BUCKET_NAME env var is required.");
if (!parserFunctionName) throw new Error("PARSER_SANDBOX_FUNCTION_NAME env var is required.");
const deps = buildDocumentWorkerDeps(client, tableName, cleanBucket, parserFunctionName);
const submissionDeps = buildSubjectWorkerDeps(client, tableName, cleanBucket, parserFunctionName);
const logger = new SecureLogger({ baseContext: { service: "malware-result" } });

const REAL_STATUSES = new Set<GuardDutyScanStatus>(["NO_THREATS_FOUND", "THREATS_FOUND", "UNSUPPORTED", "ACCESS_DENIED", "FAILED"]);

/** Real GuardDuty Malware Protection for S3 scan-result EventBridge detail shape
 * (docs.aws.amazon.com/guardduty/latest/ug/how-malware-protection-for-s3-gdu-works.html). */
interface GuardDutyScanResultDetail {
  s3ObjectDetails: { bucketName: string; objectKey: string; versionId?: string };
  scanResultDetails: { scanResultStatus: string };
  scanStatus: string;
}

export async function handler(event: SQSEvent): Promise<SQSBatchResponse> {
  const batchItemFailures: { itemIdentifier: string }[] = [];

  for (const record of event.Records) {
    await runWithContext({ correlationId: randomUUID() }, async () => {
      try {
        const message = JSON.parse(record.body) as { detail?: GuardDutyScanResultDetail; id?: string };
        const detail = message.detail;
        if (!detail?.s3ObjectDetails?.bucketName || !detail.s3ObjectDetails.objectKey) {
          logger.error("malware-result malformed GuardDuty event", { messageId: record.messageId });
          batchItemFailures.push({ itemIdentifier: record.messageId });
          return;
        }

        const status = detail.scanResultDetails?.scanResultStatus;
        if (!status || !REAL_STATUSES.has(status as GuardDutyScanStatus)) {
          logger.error("malware-result unrecognized scan status", { status });
          batchItemFailures.push({ itemIdentifier: record.messageId });
          return;
        }

        const parsed = parseQuarantineKey(detail.s3ObjectDetails.objectKey);
        if (parsed) {
          await runWithContext({ correlationId: randomUUID(), tenantId: parsed.tenantId }, async () => {
            const outcome = await processMalwareResult(deps, {
              tenantId: parsed.tenantId,
              itemId: parsed.itemId,
              documentId: parsed.documentId,
              object: { bucket: detail.s3ObjectDetails.bucketName, key: detail.s3ObjectDetails.objectKey, versionId: detail.s3ObjectDetails.versionId ?? "" },
              status: status as GuardDutyScanStatus,
              scanResultId: message.id ?? record.messageId,
            });
            logger.info("malware-result outcome", { documentId: parsed.documentId, outcome, status });
          });
          return;
        }

        const parsedSubmission = parseSubmissionQuarantineKey(detail.s3ObjectDetails.objectKey);
        if (!parsedSubmission) {
          logger.error("malware-result unrecognized key shape", { key: detail.s3ObjectDetails.objectKey });
          return;
        }

        await runWithContext({ correlationId: randomUUID(), tenantId: parsedSubmission.tenantId }, async () => {
          const outcome = await processSubmissionMalwareResult(submissionDeps, {
            tenantId: parsedSubmission.tenantId,
            subjectId: parsedSubmission.subjectId,
            assignmentId: parsedSubmission.assignmentId,
            submissionId: parsedSubmission.submissionId,
            object: { bucket: detail.s3ObjectDetails.bucketName, key: detail.s3ObjectDetails.objectKey, versionId: detail.s3ObjectDetails.versionId ?? "" },
            status: status as GuardDutyScanStatus,
            scanResultId: message.id ?? record.messageId,
          });
          logger.info("malware-result submission outcome", { submissionId: parsedSubmission.submissionId, outcome, status });
        });
      } catch (err) {
        // Real incident (2026-08-22): a bare `err.message` of "UnknownError" (AWS SDK v3's
        // fallback for a response it couldn't classify) gave zero diagnostic signal for a real
        // production-blocking promotion failure. Logging name/code/$metadata too - none of
        // these are secrets (HTTP status, request id, AWS error code), safe for SecureLogger.
        const e = err as { name?: string; message?: string; Code?: string; $metadata?: unknown } | undefined;
        logger.error("malware-result failed", {
          messageId: record.messageId,
          error: err instanceof Error ? err.message : String(err),
          errorName: e?.name,
          errorCode: e?.Code,
          errorMetadata: e?.$metadata,
        });
        batchItemFailures.push({ itemIdentifier: record.messageId });
      }
    });
  }

  return { batchItemFailures };
}

/**
 * `ExtractionValidationTaskHandler`'s five operations (M7 item 7, D-035 õ2/õ3) - the ASL's
 * `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/
 * `CompleteRun` states, all invoking this one Lambda (`extraction-validation-task:live`) with a
 * distinct `operation` per state (kept as separate Task states for per-stage `Catch`/audit,
 * design õ2's closing paragraph - never collapsed into one call).
 *
 * Context threading: each state's ASL `Parameters.Payload` is `{ operation, input.$: "$" }`,
 * and `ResultSelector`/`OutputPath` unwrap the handler's real return value back onto `$` (same
 * pattern item 5/6 already established for `RunDeterministicParser`) - so each operation here
 * receives the FULL accumulated execution context and returns it augmented with whatever this
 * stage adds, which becomes `$` for the next state directly.
 *
 * The single most load-bearing invariant in this file (design õ3, the design's own "most
 * serious finding"): the transient OCR artifact is deleted ONLY by `completeRun()` and
 * `markPendingConfirmation()` - never by `validateSchema()`/`compareExtractorsStage()`/
 * `persistExtractedFieldsStage()`. Those three run BEFORE the run has reached any terminal
 * state (a retry of any of them must still be able to re-read the artifact), so none of them
 * may touch `OcrArtifactStore.delete()` under any circumstance.
 */
import { getFieldSchema } from "../domain/field-schema.js";
import { isValidFieldValue } from "../domain/validate-field-value.js";
import { compareExtractors, type SourceCandidate, type ComparisonResult } from "../domain/compare-extractors.js";
import { decideFieldOutcome } from "../domain/decide-field-outcome.js";
import { extractionRunKey } from "../domain/extraction-run.js";
import { extractedFieldKey, type ExtractedField, type ExtractedFieldValueType, type ExtractionSource } from "../domain/extracted-field.js";
import { documentKey, type Document } from "../../document/domain/document.js";
import { itemKey, type ExpirationItem } from "../../expiration/domain/expiration-item.js";
import { buildItemAttributeUpdate, ITEM_ATTRIBUTE_BY_FIELD_NAME } from "./item-field-mapping.js";
import type { DocumentReader } from "../ports/document-reader.js";
import type { EntityReader } from "../ports/entity-reader.js";
import type { CommitItemUpdate } from "../ports/extracted-field-store.js";
import type { ExtractionRunStore } from "../ports/extraction-run-store.js";
import type { ExtractedFieldStore } from "../ports/extracted-field-store.js";
import type { ExtractionArtifactRef, OcrArtifactStore } from "../ports/ocr-artifact-store.js";
import { ExtractionCommitFailedError } from "../../../shared/errors/app-error.js";

export interface RunExtractionValidationDeps {
  documents: DocumentReader;
  /** Read-only access to the `ExpirationItem` - needed ONLY by `PERSIST_EXTRACTED_FIELDS`, to
   * read the version/status the auto-confirm `dueDate` update must be OCC-guarded against
   * (W2-01-DECISION). No write capability: the write itself goes through
   * `ExtractedFieldStore.commitRunOutcome`'s single transaction. */
  items: EntityReader;
  runs: ExtractionRunStore;
  fields: ExtractedFieldStore;
  artifacts: OcrArtifactStore;
  now?: () => string;
}

/** One entry per extractor for one field - the shape `run-deterministic-parser.ts`/
 * `run-bedrock-extraction.ts` already produce, threaded through unchanged. */
export interface ValidationFieldCandidate {
  fieldName: string;
  valueType: ExtractedFieldValueType;
  candidateValue?: string;
  confidence?: number;
  source: ExtractionSource;
  /** Added by `validateSchema()` - absent on the way in. */
  valid?: boolean;
}

/** The full execution context threaded through all five operations. Deliberately permissive
 * (`Record<string, unknown>`-free but every extra ASL passthrough field like `ocrFailure`/
 * `parserFailure`/`bedrockFailure` is optional and never read here) - this handler never needs
 * to interpret a Catch's captured error details, only preserve them across the JSON round-trip. */
export interface ValidationContext {
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  /** The run's one business correlationId (ExtractionExecutionInput's doc comment). Would
   * survive the round-trip anyway via this interface's `[passthrough: string]: unknown` index
   * signature, but named explicitly here (like every other identity field above) rather than
   * relying on that signature - consistent with how this codebase treats passthrough fields
   * elsewhere, and greppable for anyone tracing correlationId through this pipeline. */
  correlationId: string;
  ocrAvailable?: boolean;
  extractedFields?: ValidationFieldCandidate[];
  bedrockFields?: ValidationFieldCandidate[];
  needsBedrock?: boolean;
  aiExtractionEnabled?: boolean;
  artifact?: ExtractionArtifactRef;
  comparedFields?: Array<{
    fieldName: string;
    valueType: ExtractedFieldValueType;
    agreement: ComparisonResult["agreement"];
    sources: readonly ExtractionSource[];
    candidateValue?: string;
    confidence?: number;
  }>;
  runOutcome?: "COMPLETED" | "FAILED" | "DISCARDED";
  requiresReview?: boolean;
  [passthrough: string]: unknown;
}

// ---------------------------------------------------------------------------------------------
// VALIDATE_SCHEMA
// ---------------------------------------------------------------------------------------------

export function validateSchema(input: ValidationContext): ValidationContext {
  const mark = (c: ValidationFieldCandidate): ValidationFieldCandidate => ({
    ...c,
    valid: c.candidateValue === undefined ? false : isValidFieldValue(c.valueType, c.candidateValue),
  });

  return {
    ...input,
    extractedFields: (input.extractedFields ?? []).map(mark),
    bedrockFields: input.bedrockFields ? input.bedrockFields.map(mark) : input.bedrockFields,
  };
}

// ---------------------------------------------------------------------------------------------
// COMPARE_EXTRACTORS
// ---------------------------------------------------------------------------------------------

export function compareExtractorsStage(input: ValidationContext): ValidationContext {
  const schema = getFieldSchema(input.pipelineVersion);
  const deterministicByName = new Map((input.extractedFields ?? []).map((f) => [f.fieldName, f]));
  const bedrockByName = new Map((input.bedrockFields ?? []).map((f) => [f.fieldName, f]));

  const comparedFields = schema.map((field) => {
    const candidates: SourceCandidate[] = [];
    const det = deterministicByName.get(field.fieldName);
    if (det) candidates.push({ source: det.source, value: det.candidateValue, confidence: det.confidence, valid: det.valid ?? false });
    const brk = bedrockByName.get(field.fieldName);
    if (brk) candidates.push({ source: brk.source, value: brk.candidateValue, confidence: brk.confidence, valid: brk.valid ?? false });

    const result = compareExtractors(candidates);
    return {
      fieldName: field.fieldName,
      valueType: field.valueType,
      agreement: result.agreement,
      sources: result.sources,
      candidateValue: result.candidateValue,
      confidence: result.confidence,
    };
  });

  return { ...input, comparedFields };
}

// ---------------------------------------------------------------------------------------------
// Shared commit-or-discard helper (used by PERSIST_EXTRACTED_FIELDS and
// MARK_PENDING_CONFIRMATION - both write ExtractedField rows + transition the run atomically,
// guarded by the same Document-not-concurrently-changed check).
// ---------------------------------------------------------------------------------------------

async function commitOrDiscard(
  deps: RunExtractionValidationDeps,
  ctx: ValidationContext,
  fields: ExtractedField[],
  runStatus: "COMPLETED" | "FAILED",
  itemUpdate?: CommitItemUpdate,
): Promise<"COMPLETED" | "FAILED" | "DISCARDED"> {
  const now = deps.now?.() ?? new Date().toISOString();
  const docKey = documentKey(ctx.tenantId, ctx.itemId, ctx.documentId);
  const runKey = extractionRunKey(ctx.tenantId, ctx.documentId, ctx.runId);
  // `ExtractionRun` is only ever created once (start-extraction-run.ts's putIfAbsent, version 1)
  // and never updated by anything else before this handler runs - documented invariant, not
  // read back here to avoid an extra consistent GetItem for a value that cannot have changed.
  const runExpectedVersion = 1;

  const doc = await deps.documents.get<Document>(docKey, true);
  if (!doc || doc.status === "DELETED") {
    await deps.runs.updateStatus(runKey, ctx.tenantId, runExpectedVersion, "DISCARDED", now);
    return "DISCARDED";
  }

  let result: "COMMITTED" | "DOCUMENT_DISCARDED";
  try {
    result = await deps.fields.commitRunOutcome({
      fields,
      runKey,
      runTenantId: ctx.tenantId,
      runExpectedVersion,
      runStatus,
      completedAt: now,
      documentKey: docKey,
      documentExpectedVersion: doc.version,
      ...(itemUpdate ? { itemUpdate } : {}),
    });
  } catch (err) {
    throw new ExtractionCommitFailedError(`Failed to commit outcome for run ${ctx.runId}.`, { runId: ctx.runId, documentId: ctx.documentId }, err);
  }

  if (result === "DOCUMENT_DISCARDED") {
    await deps.runs.updateStatus(runKey, ctx.tenantId, runExpectedVersion, "DISCARDED", now);
    return "DISCARDED";
  }
  return runStatus;
}

// ---------------------------------------------------------------------------------------------
// PERSIST_EXTRACTED_FIELDS
// ---------------------------------------------------------------------------------------------

/**
 * W2-01-DECISION (Marcelo, product owner): when the pipeline auto-confirms a field - high
 * confidence, no source disagreement, no human review needed - it must reach the SAME outcome
 * as a human clicking confirm, which includes writing `ExpirationItem.dueDate`. Before this,
 * the pipeline's most successful path persisted an `ExtractedField` in `CONFIRMED` state that
 * no route could ever apply to the item (the HTTP confirm route requires
 * `PENDING_CONFIRMATION` and answers 422 for an already-`CONFIRMED` field).
 *
 * Returns the `ExpirationItem` leg for `commitRunOutcome`'s transaction, or `undefined` when
 * there is nothing to write. Deliberate non-goals:
 *  - only auto-`CONFIRMED` fields count; `PENDING_CONFIRMATION` still waits for a human;
 *  - only fields the schema actually maps to an item attribute (`expirationDate` -> `dueDate`)
 *    - no invented handling for hypothetical future fields;
 *  - a missing or `DELETED` item is NOT an error here: the run still commits its fields (same
 *    tolerance the `Document` guard already has), it simply writes no item attribute.
 *
 * Idempotency/retry: this adds no new retry surface. The item `Update` rides inside the same
 * all-or-nothing `TransactWriteItems` as the field rows, whose per-row `attribute_not_exists`
 * guard already makes a re-run of an ALREADY-committed `PERSIST_EXTRACTED_FIELDS` cancel the
 * whole transaction (reported as `DOCUMENT_DISCARDED`). So a Lambda retry/redrive can never
 * apply the `dueDate` write twice, and a retry of a transaction that genuinely never committed
 * re-reads the item's current version here before trying again.
 */
async function buildAutoConfirmItemUpdate(
  deps: RunExtractionValidationDeps,
  ctx: ValidationContext,
  fields: readonly ExtractedField[],
): Promise<CommitItemUpdate | undefined> {
  const autoConfirmed = fields.filter((f) => f.state === "CONFIRMED" && f.confirmedValue !== undefined && ITEM_ATTRIBUTE_BY_FIELD_NAME[f.fieldName] !== undefined);
  if (autoConfirmed.length === 0) return undefined;

  const key = itemKey(ctx.tenantId, ctx.itemId);
  const item = await deps.items.get<ExpirationItem>(key, true);
  if (!item || item.tenantId !== ctx.tenantId || item.status === "DELETED") return undefined;

  const set: Record<string, unknown> = {};
  for (const field of autoConfirmed) {
    const attributeUpdate = buildItemAttributeUpdate({
      tenantId: ctx.tenantId,
      itemId: ctx.itemId,
      itemStatus: item.status,
      fieldName: field.fieldName,
      // Narrowed by the `filter` above; `noUncheckedIndexedAccess`-safe fallback for the
      // type-level `string | undefined` that the filter can't express.
      confirmedValue: field.confirmedValue ?? "",
    });
    Object.assign(set, attributeUpdate);
  }

  if (Object.keys(set).length === 0) return undefined;
  return { key, tenantId: ctx.tenantId, expectedVersion: item.version, set };
}

export async function persistExtractedFieldsStage(deps: RunExtractionValidationDeps, input: ValidationContext): Promise<ValidationContext> {
  const now = deps.now?.() ?? new Date().toISOString();
  const compared = input.comparedFields ?? [];

  const fields: ExtractedField[] = compared.map((cf) => {
    const outcome = decideFieldOutcome({ agreement: cf.agreement, sources: cf.sources, candidateValue: cf.candidateValue, confidence: cf.confidence });
    return {
      ...extractedFieldKey(input.tenantId, input.documentId, cf.fieldName, input.runId),
      entityType: "ExtractedField",
      tenantId: input.tenantId,
      documentId: input.documentId,
      runId: input.runId,
      fieldName: cf.fieldName,
      valueType: cf.valueType,
      candidateValue: cf.candidateValue,
      confidence: cf.confidence,
      sources: cf.sources,
      agreement: cf.agreement,
      state: outcome.state,
      confirmedValue: outcome.confirmedValue,
      documentVersion: input.documentVersion,
      pipelineVersion: input.pipelineVersion,
      version: 1,
      createdAt: now,
      updatedAt: now,
    };
  });

  const itemUpdate = await buildAutoConfirmItemUpdate(deps, input, fields);

  // NEVER deletes the artifact here - PersistExtractedFields runs before the run has reached a
  // terminal state (CompleteRun is the very next state); a retry of this state must still be
  // able to observe the same Document row it read a moment ago, and CompleteRun still needs
  // `input.artifact` intact to know what to delete (design õ3).
  const outcome = await commitOrDiscard(deps, input, fields, "COMPLETED", itemUpdate);
  const requiresReview = outcome === "COMPLETED" && fields.some((f) => f.state === "PENDING_CONFIRMATION");

  return { ...input, runOutcome: outcome, requiresReview };
}

// ---------------------------------------------------------------------------------------------
// MARK_PENDING_CONFIRMATION - reached ONLY from RunDeterministicParser's own hard failure (the
// ASL's `parserFailure` Catch, design õ1.2's "a £nica falha que vai direto para
// MarkPendingConfirmation") - no ValidateSchema/CompareExtractors/PersistExtractedFields ever
// ran for this execution, so this operation does both the DB commit AND the artifact deletion
// itself (design õ3 names this state explicitly as one of the deletion points, "em caminho
// FAILED").
// ---------------------------------------------------------------------------------------------

export async function markPendingConfirmationStage(deps: RunExtractionValidationDeps, input: ValidationContext): Promise<ValidationContext> {
  const now = deps.now?.() ?? new Date().toISOString();
  const schema = getFieldSchema(input.pipelineVersion);

  const fields: ExtractedField[] = schema.map((field) => ({
    ...extractedFieldKey(input.tenantId, input.documentId, field.fieldName, input.runId),
    entityType: "ExtractedField",
    tenantId: input.tenantId,
    documentId: input.documentId,
    runId: input.runId,
    fieldName: field.fieldName,
    valueType: field.valueType,
    sources: [],
    agreement: "SINGLE_SOURCE",
    state: "PENDING_CONFIRMATION",
    documentVersion: input.documentVersion,
    pipelineVersion: input.pipelineVersion,
    version: 1,
    createdAt: now,
    updatedAt: now,
  }));

  const outcome = await commitOrDiscard(deps, input, fields, "FAILED");

  // This branch reached the end of the line for this run - whatever OCR artifact exists (it may
  // never have been created at all, if RunTextract itself failed before the parser even ran) is
  // deleted now, unconditionally of `outcome` (FAILED or DISCARDED are both genuinely terminal).
  if (input.artifact) {
    await deps.artifacts.delete(input.artifact);
  }

  return { ...input, runOutcome: outcome, requiresReview: outcome === "FAILED" };
}

// ---------------------------------------------------------------------------------------------
// COMPLETE_RUN - the normal-path terminal state, reached only after PersistExtractedFields
// already committed the DB outcome (COMPLETED or, on a concurrent-discard race, DISCARDED).
// This operation's own job is ONLY the artifact deletion (design õ3: "o £nico ponto do sistema
// que sabe com certeza que nenhum estado subsequente vai mais ler o artefato" - true here
// because CompleteRun has no `Next`, it's the ASL's terminal state on this path).
// ---------------------------------------------------------------------------------------------

export async function completeRunStage(deps: RunExtractionValidationDeps, input: ValidationContext): Promise<ValidationContext> {
  if (input.artifact) {
    await deps.artifacts.delete(input.artifact);
  }
  return { ...input };
}

export type ExtractionValidationOperation = "VALIDATE_SCHEMA" | "COMPARE_EXTRACTORS" | "PERSIST_EXTRACTED_FIELDS" | "MARK_PENDING_CONFIRMATION" | "COMPLETE_RUN";

export async function runExtractionValidation(deps: RunExtractionValidationDeps, operation: ExtractionValidationOperation, input: ValidationContext): Promise<ValidationContext> {
  switch (operation) {
    case "VALIDATE_SCHEMA":
      return validateSchema(input);
    case "COMPARE_EXTRACTORS":
      return compareExtractorsStage(input);
    case "PERSIST_EXTRACTED_FIELDS":
      return persistExtractedFieldsStage(deps, input);
    case "MARK_PENDING_CONFIRMATION":
      return markPendingConfirmationStage(deps, input);
    case "COMPLETE_RUN":
      return completeRunStage(deps, input);
  }
}

/** Real DynamoDB adapter for `ExtractedFieldStore` (M7 item 7) - the first real writer of
 * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
 * `attribute_not_exists` Put per field row (first-time create, `buildVersionedCreate`), one
 * versioned Update on the parent `ExtractionRun` (`buildVersionedUpdate`), and one
 * `ConditionCheck` re-asserting the `Document` row is still at the version the caller read
 * moments earlier (`buildVersionConditionCheck`) - the TOCTOU close for the concurrent-discard
 * race (design õ3). All three succeed or none do. */
import { GetCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { TransactWriteCommandInput } from "@aws-sdk/lib-dynamodb";
import { buildVersionedCreate, buildVersionedUpdate, buildVersionConditionCheck, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import { mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
import type {
  CommitRunOutcomeInput,
  CommitRunOutcomeResult,
  ConfirmFieldInput,
  ExtractedFieldStore,
  FieldTransitionResult,
  RejectFieldInput,
} from "../ports/extracted-field-store.js";
import type { ExtractedField } from "../domain/extracted-field.js";

export class DynamoDbExtractedFieldStore implements ExtractedFieldStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async commitRunOutcome(input: CommitRunOutcomeInput): Promise<CommitRunOutcomeResult> {
    const fieldPuts = input.fields.map((field) => ({ Put: buildVersionedCreate(this.tableName, field as unknown as Record<string, unknown> & { PK: string; SK: string }) }));

    const runUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.runKey,
        tenantId: input.runTenantId,
        expectedVersion: input.runExpectedVersion,
        set: { status: input.runStatus, completedAt: input.completedAt },
      }),
    };

    const documentGuard = buildVersionConditionCheck({
      tableName: this.tableName,
      key: input.documentKey,
      expectedVersion: input.documentExpectedVersion,
    });

    // W2-01-DECISION: when the run auto-confirmed a field that maps to an `ExpirationItem`
    // attribute, that item write joins THIS transaction - never a follow-up write - so the
    // auto-confirm outcome is as atomic as the manual `confirmField` one.
    const itemUpdate = input.itemUpdate
      ? [
          {
            Update: buildVersionedUpdate({
              tableName: this.tableName,
              key: input.itemUpdate.key,
              tenantId: input.itemUpdate.tenantId,
              expectedVersion: input.itemUpdate.expectedVersion,
              set: input.itemUpdate.set,
              now: input.completedAt,
            }),
          },
        ]
      : [];

    const transactItems = [...fieldPuts, runUpdate, documentGuard, ...itemUpdate] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      // A cancellation here can only come from one of: a field row already existing (should
      // never happen - runId makes each field's SK unique per run, so this would indicate a
      // genuine retry-after-partial-commit bug, not a real race), the run's own version having
      // moved (nothing else in the system updates ExtractionRun.status before this handler
      // runs), the Document guard (the actual race this method exists to detect), or the
      // optional `ExpirationItem` update's own version having moved. Rather
      // than parsing per-item cancellation reasons (fragile across SDK versions), any
      // cancellation here is treated uniformly as DOCUMENT_DISCARDED - the safe, conservative
      // reading is "something about the world this commit assumed is no longer true", and the
      // caller's fallback (mark the run DISCARDED, persist zero fields) is safe even in the
      // (extremely unlikely) case the true cause was one of the other two conditions.
      if (isTransactionCanceled(err)) return "DOCUMENT_DISCARDED";
      throw mapDynamoError(err, "ExtractedFieldStore.commitRunOutcome");
    }
  }

  async get(key: EntityKey): Promise<ExtractedField | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
      return result.Item as ExtractedField | undefined;
    } catch (err) {
      throw mapDynamoError(err, "ExtractedFieldStore.get");
    }
  }

  async confirmField(input: ConfirmFieldInput): Promise<FieldTransitionResult> {
    const fieldUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.fieldKey,
        tenantId: input.fieldTenantId,
        expectedVersion: input.fieldExpectedVersion,
        set: { state: "CONFIRMED", confirmedValue: input.confirmedValue },
        now: input.now,
      }),
    };

    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });

    const itemEntry = input.itemUpdate
      ? {
          Update: buildVersionedUpdate({
            tableName: this.tableName,
            key: input.itemKey,
            tenantId: input.itemTenantId,
            expectedVersion: input.itemExpectedVersion,
            set: input.itemUpdate,
            now: input.now,
          }),
        }
      : buildVersionConditionCheck({ tableName: this.tableName, key: input.itemKey, expectedVersion: input.itemExpectedVersion });

    const transactItems = [fieldUpdate, runGuard, documentGuard, itemEntry] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
      throw mapDynamoError(err, "ExtractedFieldStore.confirmField");
    }
  }

  async rejectField(input: RejectFieldInput): Promise<FieldTransitionResult> {
    const set: Record<string, unknown> = { state: "REJECTED" };
    if (input.correctionReason !== undefined) set["correctionReason"] = input.correctionReason;

    const fieldUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.fieldKey,
        tenantId: input.fieldTenantId,
        expectedVersion: input.fieldExpectedVersion,
        set,
        now: input.now,
      }),
    };

    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });

    const transactItems = [fieldUpdate, runGuard, documentGuard] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
      throw mapDynamoError(err, "ExtractedFieldStore.rejectField");
    }
  }
}


codex
A revisão melhora substancialmente o enquadramento e remove o fan-out transacional aberto. Estou agora testando as promessas mais frágeis: exclusividade real da promoção, cerca de tenant, atomicidade do outbox e convergência do índice reverso sob link/unlink concorrente.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -C 8 \"parseQuarantineKey|parseSubmissionQuarantineKey|batchItemFailures|processRecord|ACTIVE|PROMOTE|CopyObject|confirmFileScanClean\" src/runtime/aws/handlers/upload-finalizer-handler.ts src/runtime/aws/handlers/malware-result-handler.ts src/modules/document-archive/application/apply-file-scan-result.ts; rg -n -C 10 \"bounded|staleness|deriveRequirement|evidenceVersionId|link\" src/modules/document-archive/domain/requirement.ts; rg -n -C 8 \"role|PRINCIPAL|ATTACHMENT\" src/modules/document-archive/domain/document-file.ts; Get-Content -Raw -Encoding UTF8 'src/shared/outbox/outbox.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 378ms:
src/modules/document-archive/application/apply-file-scan-result.ts-1-/**
src/modules/document-archive/application/apply-file-scan-result.ts:2: * applyFileScanResult / confirmFileScanClean â€” D-163 Â§1/Â§5, first real slice of DocumentFile's
src/modules/document-archive/application/apply-file-scan-result.ts-3- * scan-result state machine. Reuses `document/domain/document-state-machine.ts`'s
src/modules/document-archive/application/apply-file-scan-result.ts-4- * `decideNextAction()` VERBATIM (never forked/redesigned â€” `DocumentFileScanStatus`'s taxonomy
src/modules/document-archive/application/apply-file-scan-result.ts-5- * is a structural subset of `DocumentStatus`, see `document-file.ts`'s module doc comment).
src/modules/document-archive/application/apply-file-scan-result.ts-6- *
src/modules/document-archive/application/apply-file-scan-result.ts-7- * Split into two functions rather than M6's single `advanceAfterEvidence()`, because unlike
src/modules/document-archive/application/apply-file-scan-result.ts-8- * M6's Document (whose `quarantineObject.versionId` is a permanent "" placeholder,
src/modules/document-archive/application/apply-file-scan-result.ts-9- * `document.ts`'s doc comment), a `DocumentFile.quarantineObject.versionId` IS the field the
src/modules/document-archive/application/apply-file-scan-result.ts-10- * symmetric evidence correlation consolidates into (D-163 Â§1) - so this function never needs to
src/modules/document-archive/application/apply-file-scan-result.ts:11: * reach into S3 itself just to decide PROMOTE-vs-not: it returns `READY_TO_PROMOTE` with the
src/modules/document-archive/application/apply-file-scan-result.ts-12- * `sourceObject` a worker copies from, and does zero writes for that outcome (never claims CLEAN
src/modules/document-archive/application/apply-file-scan-result.ts:13: * for a copy that hasn't happened, same discipline M6's PROMOTE branch already established).
src/modules/document-archive/application/apply-file-scan-result.ts:14: * `confirmFileScanClean()` is the confirmation step, called only once that caller's own S3
src/modules/document-archive/application/apply-file-scan-result.ts:15: * copy+verify has succeeded - mirrors M6's `advanceAfterEvidence()` PROMOTE branch's contract,
src/modules/document-archive/application/apply-file-scan-result.ts-16- * just as a separate, independently retryable step instead of inlined in the same function.
src/modules/document-archive/application/apply-file-scan-result.ts-17- */
src/modules/document-archive/application/apply-file-scan-result.ts-18-import { buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src/modules/document-archive/application/apply-file-scan-result.ts-19-import { decideNextAction } from "../../document/domain/document-state-machine.js";
src/modules/document-archive/application/apply-file-scan-result.ts-20-import type { UploadEvidence } from "../../document/domain/document.js";
src/modules/document-archive/application/apply-file-scan-result.ts-21-import type { DocumentObjectReference } from "../../document/domain/document-object-reference.js";
src/modules/document-archive/application/apply-file-scan-result.ts-22-import type { MalwareEvidence } from "../../document/domain/malware-scan-result.js";
src/modules/document-archive/application/apply-file-scan-result.ts-23-import { documentFileKey, isNonTerminalFileScanStatus, sameObjectVersion, type DocumentFile, type DocumentFileScanStatus } from "../domain/document-file.js";
--
src/modules/document-archive/application/apply-file-scan-result.ts-47-  malwareEvidence?: MalwareEvidence;
src/modules/document-archive/application/apply-file-scan-result.ts-48-}
src/modules/document-archive/application/apply-file-scan-result.ts-49-
src/modules/document-archive/application/apply-file-scan-result.ts-50-export type ApplyFileScanResultOutcome =
src/modules/document-archive/application/apply-file-scan-result.ts-51-  | { outcome: "AWAITING" }
src/modules/document-archive/application/apply-file-scan-result.ts-52-  | { outcome: "IGNORED_WRONG_VERSION" }
src/modules/document-archive/application/apply-file-scan-result.ts-53-  | { outcome: "IGNORED_STALE" }
src/modules/document-archive/application/apply-file-scan-result.ts-54-  | { outcome: "REJECTED"; status: Extract<DocumentFileScanStatus, "REJECTED" | "UNSUPPORTED"> }
src/modules/document-archive/application/apply-file-scan-result.ts:55:  | { outcome: "READY_TO_PROMOTE"; sourceObject: DocumentObjectReference };
src/modules/document-archive/application/apply-file-scan-result.ts-56-
src/modules/document-archive/application/apply-file-scan-result.ts-57-const MAX_OCC_RETRIES = 10;
src/modules/document-archive/application/apply-file-scan-result.ts-58-
src/modules/document-archive/application/apply-file-scan-result.ts-59-/** Re-reads the file fresh on every attempt - never assumes the caller's in-memory copy is
src/modules/document-archive/application/apply-file-scan-result.ts-60- * current, since the upload-finalizer and malware-scan events for the SAME file may land
src/modules/document-archive/application/apply-file-scan-result.ts-61- * concurrently, exactly the corridor `decideNextAction()`'s doc comment describes for M6. */
src/modules/document-archive/application/apply-file-scan-result.ts-62-export async function applyFileScanResult(deps: ApplyFileScanResultDeps, input: ApplyFileScanResultInput): Promise<ApplyFileScanResultOutcome> {
src/modules/document-archive/application/apply-file-scan-result.ts-63-  const now = deps.now ?? (() => new Date().toISOString());
--
src/modules/document-archive/application/apply-file-scan-result.ts-119-        ]);
src/modules/document-archive/application/apply-file-scan-result.ts-120-        return { outcome: "AWAITING" };
src/modules/document-archive/application/apply-file-scan-result.ts-121-      } catch (err) {
src/modules/document-archive/application/apply-file-scan-result.ts-122-        if (isTransactionCanceled(err)) continue; // concurrent half of evidence landed - retry fresh.
src/modules/document-archive/application/apply-file-scan-result.ts-123-        throw err;
src/modules/document-archive/application/apply-file-scan-result.ts-124-      }
src/modules/document-archive/application/apply-file-scan-result.ts-125-    }
src/modules/document-archive/application/apply-file-scan-result.ts-126-
src/modules/document-archive/application/apply-file-scan-result.ts:127:    if (decision.action === "PROMOTE") {
src/modules/document-archive/application/apply-file-scan-result.ts:128:      // Never claims CLEAN itself - zero writes here, same discipline M6's PROMOTE branch
src/modules/document-archive/application/apply-file-scan-result.ts-129-      // established (`advance-after-evidence.ts`'s doc comment). The evidence merge that got us
src/modules/document-archive/application/apply-file-scan-result.ts:130:      // here was already persisted by an earlier AWAITING call (PROMOTE is only reachable once
src/modules/document-archive/application/apply-file-scan-result.ts-131-      // BOTH evidences exist, and only one arrives per call) - nothing new to consolidate.
src/modules/document-archive/application/apply-file-scan-result.ts-132-      const sourceObject = consolidated ? file.quarantineObject : input.observedObject;
src/modules/document-archive/application/apply-file-scan-result.ts:133:      return { outcome: "READY_TO_PROMOTE", sourceObject };
src/modules/document-archive/application/apply-file-scan-result.ts-134-    }
src/modules/document-archive/application/apply-file-scan-result.ts-135-
src/modules/document-archive/application/apply-file-scan-result.ts-136-    // decision.action === "REJECT": terminal transition - counters + GSI8 removal happen in the
src/modules/document-archive/application/apply-file-scan-result.ts-137-    // SAME transaction as the file's own write (Decision 6/Bloqueador 9: a crash between the two
src/modules/document-archive/application/apply-file-scan-result.ts-138-    // would leave pendingFileScans stuck above zero forever, permanently blocking acceptVersion).
src/modules/document-archive/application/apply-file-scan-result.ts-139-    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
src/modules/document-archive/application/apply-file-scan-result.ts-140-    if (!version) return { outcome: "IGNORED_STALE" }; // Version cannot be removed once files exist - fail closed if this ever changes.
src/modules/document-archive/application/apply-file-scan-result.ts-141-
--
src/modules/document-archive/application/apply-file-scan-result.ts-196-  tenantId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-197-  documentId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-198-  seq: number;
src/modules/document-archive/application/apply-file-scan-result.ts-199-  fileId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-200-  cleanObject: DocumentObjectReference;
src/modules/document-archive/application/apply-file-scan-result.ts-201-}
src/modules/document-archive/application/apply-file-scan-result.ts-202-
src/modules/document-archive/application/apply-file-scan-result.ts-203-/**
src/modules/document-archive/application/apply-file-scan-result.ts:204: * confirmFileScanClean - the PROMOTE confirmation step, called only once the caller (a future
src/modules/document-archive/application/apply-file-scan-result.ts-205- * S3/GuardDuty event worker, out of scope for this slice) has already copied `sourceObject` to
src/modules/document-archive/application/apply-file-scan-result.ts-206- * `cleanObject` and verified it (size/checksum, same discipline M6's `advanceAfterEvidence()`
src/modules/document-archive/application/apply-file-scan-result.ts-207- * requires before it will persist CLEAN). Idempotent against a file that already moved off
src/modules/document-archive/application/apply-file-scan-result.ts-208- * SCANNING/PENDING_UPLOAD by the time this is called (e.g. concurrently rejected) - never
src/modules/document-archive/application/apply-file-scan-result.ts-209- * resurrects a terminal file back toward CLEAN.
src/modules/document-archive/application/apply-file-scan-result.ts-210- */
src/modules/document-archive/application/apply-file-scan-result.ts:211:export async function confirmFileScanClean(deps: ApplyFileScanResultDeps, input: ConfirmFileScanCleanInput): Promise<ConfirmFileScanCleanOutcome> {
src/modules/document-archive/application/apply-file-scan-result.ts-212-  const now = deps.now ?? (() => new Date().toISOString());
src/modules/document-archive/application/apply-file-scan-result.ts-213-  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);
src/modules/document-archive/application/apply-file-scan-result.ts-214-
src/modules/document-archive/application/apply-file-scan-result.ts-215-  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
src/modules/document-archive/application/apply-file-scan-result.ts-216-    const file = await deps.store.get<DocumentFile>(key);
src/modules/document-archive/application/apply-file-scan-result.ts-217-    if (!file) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-218-    if (!isNonTerminalFileScanStatus(file.scanStatus)) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-219-
--
src/modules/document-archive/application/apply-file-scan-result.ts-248-      await deps.store.transactWrite(entries);
src/modules/document-archive/application/apply-file-scan-result.ts-249-      return "CONFIRMED";
src/modules/document-archive/application/apply-file-scan-result.ts-250-    } catch (err) {
src/modules/document-archive/application/apply-file-scan-result.ts-251-      if (isTransactionCanceled(err)) continue;
src/modules/document-archive/application/apply-file-scan-result.ts-252-      throw err;
src/modules/document-archive/application/apply-file-scan-result.ts-253-    }
src/modules/document-archive/application/apply-file-scan-result.ts-254-  }
src/modules/document-archive/application/apply-file-scan-result.ts-255-
src/modules/document-archive/application/apply-file-scan-result.ts:256:  throw new Error(`confirmFileScanClean exhausted retries for file ${input.fileId} under contention.`);
src/modules/document-archive/application/apply-file-scan-result.ts-257-}
src/modules/document-archive/application/apply-file-scan-result.ts-258-
src/modules/document-archive/application/apply-file-scan-result.ts-259-export type ApplyFileScanTimeoutOutcome = "TIMED_OUT" | "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-260-
src/modules/document-archive/application/apply-file-scan-result.ts-261-export interface ApplyFileScanTimeoutInput {
src/modules/document-archive/application/apply-file-scan-result.ts-262-  tenantId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-263-  documentId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-264-  seq: number;
--
src/runtime/aws/handlers/malware-result-handler.ts-1-/** Real handler for MalwareResultWorker (SQS, fed by GuardDuty Malware Protection for S3
src/runtime/aws/handlers/malware-result-handler.ts-2- * finding events routed through EventBridge). M6 design Â§3.4/Â§4. */
src/runtime/aws/handlers/malware-result-handler.ts-3-import type { SQSBatchResponse, SQSEvent } from "aws-lambda";
src/runtime/aws/handlers/malware-result-handler.ts-4-import { randomUUID } from "node:crypto";
src/runtime/aws/handlers/malware-result-handler.ts-5-import { createDocumentClient } from "../../../shared/dynamodb/client.js";
src/runtime/aws/handlers/malware-result-handler.ts-6-import { buildDocumentWorkerDeps } from "../composition/document.js";
src/runtime/aws/handlers/malware-result-handler.ts-7-import { processMalwareResult } from "../../../workers/malware-result/result-processor.js";
src/runtime/aws/handlers/malware-result-handler.ts:8:import { parseQuarantineKey } from "../../../modules/document/domain/quarantine-key.js";
src/runtime/aws/handlers/malware-result-handler.ts-9-import type { GuardDutyScanStatus } from "../../../modules/document/domain/malware-scan-result.js";
src/runtime/aws/handlers/malware-result-handler.ts-10-// M10 (D-037): mesmo branch aditivo do upload-finalizer-handler.ts, ver comentÃ¡rio lÃ¡.
src/runtime/aws/handlers/malware-result-handler.ts:11:import { parseSubmissionQuarantineKey } from "../../../modules/subject/domain/submission-quarantine-key.js";
src/runtime/aws/handlers/malware-result-handler.ts-12-import { processSubmissionMalwareResult } from "../../../workers/submission-malware-result/result-processor.js";
src/runtime/aws/handlers/malware-result-handler.ts-13-import { buildSubjectWorkerDeps } from "../composition/subject.js";
src/runtime/aws/handlers/malware-result-handler.ts-14-import { runWithContext } from "../../../shared/observability/context.js";
src/runtime/aws/handlers/malware-result-handler.ts-15-import { SecureLogger } from "../../../shared/observability/logger.js";
src/runtime/aws/handlers/malware-result-handler.ts-16-
src/runtime/aws/handlers/malware-result-handler.ts-17-const client = createDocumentClient();
src/runtime/aws/handlers/malware-result-handler.ts-18-const tableName = process.env["TABLE_NAME"];
src/runtime/aws/handlers/malware-result-handler.ts-19-const cleanBucket = process.env["CLEAN_BUCKET_NAME"];
--
src/runtime/aws/handlers/malware-result-handler.ts-31- * (docs.aws.amazon.com/guardduty/latest/ug/how-malware-protection-for-s3-gdu-works.html). */
src/runtime/aws/handlers/malware-result-handler.ts-32-interface GuardDutyScanResultDetail {
src/runtime/aws/handlers/malware-result-handler.ts-33-  s3ObjectDetails: { bucketName: string; objectKey: string; versionId?: string };
src/runtime/aws/handlers/malware-result-handler.ts-34-  scanResultDetails: { scanResultStatus: string };
src/runtime/aws/handlers/malware-result-handler.ts-35-  scanStatus: string;
src/runtime/aws/handlers/malware-result-handler.ts-36-}
src/runtime/aws/handlers/malware-result-handler.ts-37-
src/runtime/aws/handlers/malware-result-handler.ts-38-export async function handler(event: SQSEvent): Promise<SQSBatchResponse> {
src/runtime/aws/handlers/malware-result-handler.ts:39:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers/malware-result-handler.ts-40-
src/runtime/aws/handlers/malware-result-handler.ts-41-  for (const record of event.Records) {
src/runtime/aws/handlers/malware-result-handler.ts-42-    await runWithContext({ correlationId: randomUUID() }, async () => {
src/runtime/aws/handlers/malware-result-handler.ts-43-      try {
src/runtime/aws/handlers/malware-result-handler.ts-44-        const message = JSON.parse(record.body) as { detail?: GuardDutyScanResultDetail; id?: string };
src/runtime/aws/handlers/malware-result-handler.ts-45-        const detail = message.detail;
src/runtime/aws/handlers/malware-result-handler.ts-46-        if (!detail?.s3ObjectDetails?.bucketName || !detail.s3ObjectDetails.objectKey) {
src/runtime/aws/handlers/malware-result-handler.ts-47-          logger.error("malware-result malformed GuardDuty event", { messageId: record.messageId });
src/runtime/aws/handlers/malware-result-handler.ts:48:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/malware-result-handler.ts-49-          return;
src/runtime/aws/handlers/malware-result-handler.ts-50-        }
src/runtime/aws/handlers/malware-result-handler.ts-51-
src/runtime/aws/handlers/malware-result-handler.ts-52-        const status = detail.scanResultDetails?.scanResultStatus;
src/runtime/aws/handlers/malware-result-handler.ts-53-        if (!status || !REAL_STATUSES.has(status as GuardDutyScanStatus)) {
src/runtime/aws/handlers/malware-result-handler.ts-54-          logger.error("malware-result unrecognized scan status", { status });
src/runtime/aws/handlers/malware-result-handler.ts:55:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/malware-result-handler.ts-56-          return;
src/runtime/aws/handlers/malware-result-handler.ts-57-        }
src/runtime/aws/handlers/malware-result-handler.ts-58-
src/runtime/aws/handlers/malware-result-handler.ts:59:        const parsed = parseQuarantineKey(detail.s3ObjectDetails.objectKey);
src/runtime/aws/handlers/malware-result-handler.ts-60-        if (parsed) {
src/runtime/aws/handlers/malware-result-handler.ts-61-          await runWithContext({ correlationId: randomUUID(), tenantId: parsed.tenantId }, async () => {
src/runtime/aws/handlers/malware-result-handler.ts-62-            const outcome = await processMalwareResult(deps, {
src/runtime/aws/handlers/malware-result-handler.ts-63-              tenantId: parsed.tenantId,
src/runtime/aws/handlers/malware-result-handler.ts-64-              itemId: parsed.itemId,
src/runtime/aws/handlers/malware-result-handler.ts-65-              documentId: parsed.documentId,
src/runtime/aws/handlers/malware-result-handler.ts-66-              object: { bucket: detail.s3ObjectDetails.bucketName, key: detail.s3ObjectDetails.objectKey, versionId: detail.s3ObjectDetails.versionId ?? "" },
src/runtime/aws/handlers/malware-result-handler.ts-67-              status: status as GuardDutyScanStatus,
src/runtime/aws/handlers/malware-result-handler.ts-68-              scanResultId: message.id ?? record.messageId,
src/runtime/aws/handlers/malware-result-handler.ts-69-            });
src/runtime/aws/handlers/malware-result-handler.ts-70-            logger.info("malware-result outcome", { documentId: parsed.documentId, outcome, status });
src/runtime/aws/handlers/malware-result-handler.ts-71-          });
src/runtime/aws/handlers/malware-result-handler.ts-72-          return;
src/runtime/aws/handlers/malware-result-handler.ts-73-        }
src/runtime/aws/handlers/malware-result-handler.ts-74-
src/runtime/aws/handlers/malware-result-handler.ts:75:        const parsedSubmission = parseSubmissionQuarantineKey(detail.s3ObjectDetails.objectKey);
src/runtime/aws/handlers/malware-result-handler.ts-76-        if (!parsedSubmission) {
src/runtime/aws/handlers/malware-result-handler.ts-77-          logger.error("malware-result unrecognized key shape", { key: detail.s3ObjectDetails.objectKey });
src/runtime/aws/handlers/malware-result-handler.ts-78-          return;
src/runtime/aws/handlers/malware-result-handler.ts-79-        }
src/runtime/aws/handlers/malware-result-handler.ts-80-
src/runtime/aws/handlers/malware-result-handler.ts-81-        await runWithContext({ correlationId: randomUUID(), tenantId: parsedSubmission.tenantId }, async () => {
src/runtime/aws/handlers/malware-result-handler.ts-82-          const outcome = await processSubmissionMalwareResult(submissionDeps, {
src/runtime/aws/handlers/malware-result-handler.ts-83-            tenantId: parsedSubmission.tenantId,
--
src/runtime/aws/handlers/malware-result-handler.ts-98-        const e = err as { name?: string; message?: string; Code?: string; $metadata?: unknown } | undefined;
src/runtime/aws/handlers/malware-result-handler.ts-99-        logger.error("malware-result failed", {
src/runtime/aws/handlers/malware-result-handler.ts-100-          messageId: record.messageId,
src/runtime/aws/handlers/malware-result-handler.ts-101-          error: err instanceof Error ? err.message : String(err),
src/runtime/aws/handlers/malware-result-handler.ts-102-          errorName: e?.name,
src/runtime/aws/handlers/malware-result-handler.ts-103-          errorCode: e?.Code,
src/runtime/aws/handlers/malware-result-handler.ts-104-          errorMetadata: e?.$metadata,
src/runtime/aws/handlers/malware-result-handler.ts-105-        });
src/runtime/aws/handlers/malware-result-handler.ts:106:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/malware-result-handler.ts-107-      }
src/runtime/aws/handlers/malware-result-handler.ts-108-    });
src/runtime/aws/handlers/malware-result-handler.ts-109-  }
src/runtime/aws/handlers/malware-result-handler.ts-110-
src/runtime/aws/handlers/malware-result-handler.ts:111:  return { batchItemFailures };
src/runtime/aws/handlers/malware-result-handler.ts-112-}
--
src/runtime/aws/handlers/upload-finalizer-handler.ts-1-/** Real handler for UploadFinalizerWorker (SQS, fed by S3 "Object Created" events on the
src/runtime/aws/handlers/upload-finalizer-handler.ts-2- * quarantine bucket routed through EventBridge). M6 design Â§3.2/Â§4. */
src/runtime/aws/handlers/upload-finalizer-handler.ts-3-import type { SQSBatchResponse, SQSEvent } from "aws-lambda";
src/runtime/aws/handlers/upload-finalizer-handler.ts-4-import { randomUUID } from "node:crypto";
src/runtime/aws/handlers/upload-finalizer-handler.ts-5-import { createDocumentClient } from "../../../shared/dynamodb/client.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-6-import { buildDocumentWorkerDeps } from "../composition/document.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-7-import { finalizeUpload } from "../../../workers/upload-finalizer/finalizer.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts:8:import { parseQuarantineKey } from "../../../modules/document/domain/quarantine-key.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-9-// M10 (D-037): branch puramente aditivo para o namespace de key de guest upload
src/runtime/aws/handlers/upload-finalizer-handler.ts-10-// (`tenant/.../subject/...`) - nunca sobrepÃµe o formato item-anchored acima
src/runtime/aws/handlers/upload-finalizer-handler.ts-11-// (`tenant/.../item/...`), que continua resolvido pelo parser/fluxo originais sem nenhuma
src/runtime/aws/handlers/upload-finalizer-handler.ts-12-// mudanÃ§a de comportamento.
src/runtime/aws/handlers/upload-finalizer-handler.ts:13:import { parseSubmissionQuarantineKey } from "../../../modules/subject/domain/submission-quarantine-key.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-14-import { finalizeSubmissionUpload } from "../../../workers/submission-finalizer/finalizer.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-15-import { buildSubjectWorkerDeps } from "../composition/subject.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-16-import { runWithContext } from "../../../shared/observability/context.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-17-import { SecureLogger } from "../../../shared/observability/logger.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-18-
src/runtime/aws/handlers/upload-finalizer-handler.ts-19-const client = createDocumentClient();
src/runtime/aws/handlers/upload-finalizer-handler.ts-20-const tableName = process.env["TABLE_NAME"];
src/runtime/aws/handlers/upload-finalizer-handler.ts-21-const cleanBucket = process.env["CLEAN_BUCKET_NAME"];
--
src/runtime/aws/handlers/upload-finalizer-handler.ts-30-/** Real EventBridge "Object Created" detail shape for an S3 source
src/runtime/aws/handlers/upload-finalizer-handler.ts-31- * (docs.aws.amazon.com/AmazonS3/latest/userguide/EventBridge.html). */
src/runtime/aws/handlers/upload-finalizer-handler.ts-32-interface S3ObjectCreatedDetail {
src/runtime/aws/handlers/upload-finalizer-handler.ts-33-  bucket: { name: string };
src/runtime/aws/handlers/upload-finalizer-handler.ts-34-  object: { key: string; "version-id"?: string; size?: number };
src/runtime/aws/handlers/upload-finalizer-handler.ts-35-}
src/runtime/aws/handlers/upload-finalizer-handler.ts-36-
src/runtime/aws/handlers/upload-finalizer-handler.ts-37-export async function handler(event: SQSEvent): Promise<SQSBatchResponse> {
src/runtime/aws/handlers/upload-finalizer-handler.ts:38:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers/upload-finalizer-handler.ts-39-
src/runtime/aws/handlers/upload-finalizer-handler.ts-40-  for (const record of event.Records) {
src/runtime/aws/handlers/upload-finalizer-handler.ts-41-    await runWithContext({ correlationId: randomUUID() }, async () => {
src/runtime/aws/handlers/upload-finalizer-handler.ts-42-      try {
src/runtime/aws/handlers/upload-finalizer-handler.ts-43-        const message = JSON.parse(record.body) as { detail?: S3ObjectCreatedDetail };
src/runtime/aws/handlers/upload-finalizer-handler.ts-44-        const detail = message.detail;
src/runtime/aws/handlers/upload-finalizer-handler.ts-45-        if (!detail?.bucket?.name || !detail.object?.key || !detail.object["version-id"]) {
src/runtime/aws/handlers/upload-finalizer-handler.ts-46-          logger.error("upload-finalizer malformed S3 event", { messageId: record.messageId });
src/runtime/aws/handlers/upload-finalizer-handler.ts:47:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/upload-finalizer-handler.ts-48-          return;
src/runtime/aws/handlers/upload-finalizer-handler.ts-49-        }
src/runtime/aws/handlers/upload-finalizer-handler.ts-50-
src/runtime/aws/handlers/upload-finalizer-handler.ts:51:        const parsed = parseQuarantineKey(detail.object.key);
src/runtime/aws/handlers/upload-finalizer-handler.ts-52-        if (parsed) {
src/runtime/aws/handlers/upload-finalizer-handler.ts-53-          await runWithContext({ correlationId: randomUUID(), tenantId: parsed.tenantId }, async () => {
src/runtime/aws/handlers/upload-finalizer-handler.ts-54-            const outcome = await finalizeUpload(deps, {
src/runtime/aws/handlers/upload-finalizer-handler.ts-55-              tenantId: parsed.tenantId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-56-              itemId: parsed.itemId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-57-              documentId: parsed.documentId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-58-              object: { bucket: detail.bucket.name, key: detail.object.key, versionId: detail.object["version-id"]! },
src/runtime/aws/handlers/upload-finalizer-handler.ts-59-            });
src/runtime/aws/handlers/upload-finalizer-handler.ts-60-            logger.info("upload-finalizer outcome", { documentId: parsed.documentId, outcome });
src/runtime/aws/handlers/upload-finalizer-handler.ts-61-          });
src/runtime/aws/handlers/upload-finalizer-handler.ts-62-          return;
src/runtime/aws/handlers/upload-finalizer-handler.ts-63-        }
src/runtime/aws/handlers/upload-finalizer-handler.ts-64-
src/runtime/aws/handlers/upload-finalizer-handler.ts-65-        // M10: nÃ£o Ã© uma key item-anchored (M6) - tenta o namespace de guest submission antes
src/runtime/aws/handlers/upload-finalizer-handler.ts-66-        // de desistir. Os dois formatos nunca colidem (segmentos "item" vs "subject").
src/runtime/aws/handlers/upload-finalizer-handler.ts:67:        const parsedSubmission = parseSubmissionQuarantineKey(detail.object.key);
src/runtime/aws/handlers/upload-finalizer-handler.ts-68-        if (!parsedSubmission) {
src/runtime/aws/handlers/upload-finalizer-handler.ts-69-          // A key this handler doesn't recognize (never produced by either reserveUpload flow)
src/runtime/aws/handlers/upload-finalizer-handler.ts-70-          // is not a retryable failure - it can never resolve on retry. Log and drop, never DLQ-loop.
src/runtime/aws/handlers/upload-finalizer-handler.ts-71-          logger.error("upload-finalizer unrecognized key shape", { key: detail.object.key });
src/runtime/aws/handlers/upload-finalizer-handler.ts-72-          return;
src/runtime/aws/handlers/upload-finalizer-handler.ts-73-        }
src/runtime/aws/handlers/upload-finalizer-handler.ts-74-
src/runtime/aws/handlers/upload-finalizer-handler.ts-75-        await runWithContext({ correlationId: randomUUID(), tenantId: parsedSubmission.tenantId }, async () => {
--
src/runtime/aws/handlers/upload-finalizer-handler.ts-90-        const e = err as { name?: string; message?: string; Code?: string; $metadata?: unknown } | undefined;
src/runtime/aws/handlers/upload-finalizer-handler.ts-91-        logger.error("upload-finalizer failed", {
src/runtime/aws/handlers/upload-finalizer-handler.ts-92-          messageId: record.messageId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-93-          error: err instanceof Error ? err.message : String(err),
src/runtime/aws/handlers/upload-finalizer-handler.ts-94-          errorName: e?.name,
src/runtime/aws/handlers/upload-finalizer-handler.ts-95-          errorCode: e?.Code,
src/runtime/aws/handlers/upload-finalizer-handler.ts-96-          errorMetadata: e?.$metadata,
src/runtime/aws/handlers/upload-finalizer-handler.ts-97-        });
src/runtime/aws/handlers/upload-finalizer-handler.ts:98:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/upload-finalizer-handler.ts-99-      }
src/runtime/aws/handlers/upload-finalizer-handler.ts-100-    });
src/runtime/aws/handlers/upload-finalizer-handler.ts-101-  }
src/runtime/aws/handlers/upload-finalizer-handler.ts-102-
src/runtime/aws/handlers/upload-finalizer-handler.ts:103:  return { batchItemFailures };
src/runtime/aws/handlers/upload-finalizer-handler.ts-104-}
2- * Requirement â€” D-143 (`docs/architecture/reviews/document-domain-scoping/
3- * estado-final-consolidado.md` Decision 5) + `document-domain-functional-decisions.md` D9.
4- * "Algo que um Subject precisa possuir, apresentar ou manter vÃ¡lido." Entity #1 of Nucleus 2
5- * (guest access and recurrence are separate, deferred follow-ups per D-143 â€” not implemented
6- * here).
7- *
8- * `applicability` is a PERSISTED fact (Decision 5 explicitly corrects an earlier draft that
9- * tried to derive it) â€” it is never inferred from evidence, only ever set by an explicit
10- * caller action (`createRequirement`/`updateRequirement`).
11- *
12: * `status` is a DERIVED value, computed by `deriveRequirementStatus` below from
13: * `applicability` + `evidenceVersionId` + the referenced DocumentVersion's `state`/
14- * `validUntil` â€” it is stored (denormalized, for GSI1 status-filtered listing without a full
15- * table scan) but re-derived and rewritten on every mutation that could change its inputs
16: * (`linkEvidence`/`unlinkEvidence`/`updateRequirement`'s applicability change, and the daily
17- * reindex worker for pure time-based drift â€” SATISFIED -> NOT_SATISFIED as `validUntil`
18- * passes with no other write ever touching the Requirement).
19- *
20- * `EXPIRING` is deliberately NOT part of `RequirementStatus` â€” Decision 5/D9: it is a
21- * read-time subdivision of SATISFIED, computed the same way `frontend/src/api/presentation.ts`'s
22- * `presentItemUrgency` already subdivides an ACTIVE ExpirationItem into "vence em breve"/no
23: * urgency (`SOON_THRESHOLD_DAYS = 7`) â€” mirrored here as `deriveRequirementUrgency` so the same
24- * 7-day window means the same thing across both domains, without a second persisted state.
25- */
26-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
27-import type { DocumentVersionState } from "./document-version.js";
28-
29-/** Persisted fact â€” never derived (Decision 5). */
30-export type RequirementApplicability = "APPLICABLE" | "NOT_APPLICABLE";
31-
32-/** Persisted, derived status â€” see module doc comment for the derivation contract.
33- * `EXPIRING` is intentionally absent (D9: read-time subdivision of SATISFIED only). */
--
38-  requirementId: string;
39-  tenantId: string;
40-  subjectId: string;
41-  /** Free-text label of what is required (e.g. "CertidÃ£o de antecedentes"), same style as
42-   * the older subject-module `RequirementAssignment.requirementName` â€” a distinct concept
43-   * per D9, not reused (see `identity/domain/authorization.ts`'s naming-collision comment). */
44-  name: string;
45-  notes?: string;
46-  applicability: RequirementApplicability;
47-  /** Singular by design (Decision 5 explicitly corrects an earlier draft that modeled this as
48:   * a list) â€” a Requirement links to at most one CURRENT evidence DocumentVersion at a time. */
49:  evidenceVersionId?: string;
50:  /** The owning Document of `evidenceVersionId` â€” stored alongside it (not resolved via a GSI
51-   * lookup) because D-143 Decision 2's `versionLookupGsi5Keys` (AP11, reserved for exactly
52-   * this "resolve a versionId to its Document" need) shares its GSI5PK/GSI5SK attributes with
53-   * the sparse review-queue index (`document-version.ts`'s `reviewQueueGsi5Keys`), which
54-   * REMOVES them the moment a version leaves RECEIVED/UNDER_REVIEW â€” permanently occupying
55-   * that same attribute pair for version-lookup would make it disappear right when a version
56-   * becomes ACCEPTED, the only state a Requirement actually needs to look it up in. Denormalizing
57-   * `evidenceDocumentId` here avoids that conflict entirely without touching GSI5's existing
58:   * contract. Always present together with `evidenceVersionId` (both set/cleared atomically). */
59-  evidenceDocumentId?: string;
60-  /** The `seq` of the evidence DocumentVersion within its Document â€” combined with
61-   * `evidenceDocumentId`, lets a caller navigate straight to `documentVersionKey()` without a
62:   * second lookup. Always present together with `evidenceVersionId`/`evidenceDocumentId`. */
63-  evidenceSeq?: number;
64-  /** `state`/`validUntil` of the evidence DocumentVersion AT LINK TIME â€” the exact inputs
65:   * `deriveRequirementStatus` needs, cached here (not re-read live) so the daily reindex worker
66-   * (module doc comment) only ever compares `evidenceValidUntil` against "now", never re-fetches
67:   * the DocumentVersion row. Refreshed together with `evidenceVersionId` on every `linkEvidence`
68:   * call; a DocumentVersion whose state changes AFTER the link (e.g. accepted evidence later
69:   * superseded elsewhere) is caught the next time something re-links or re-derives this
70:   * Requirement, not instantly â€” the same bounded-staleness window Decision 5 accepts. */
71-  evidenceState?: DocumentVersionState;
72-  evidenceValidUntil?: string;
73-  status: RequirementStatus;
74-  /** Provenance of a Requirement materialized by `applyTemplate` (P0.1) â€” AUDIT TRAIL ONLY.
75-   * No read path, derivation or worker ever consults these three: applying a RequirementTemplate
76:   * is a SNAPSHOT (copy), never a live link, so editing/archiving/deleting the template later
77-   * must not reach a Requirement that already exists. `sourceTemplateAppliedVersion` is the
78-   * template version the apply transaction actually FENCED on (`status = ACTIVE AND version =`),
79-   * so it is a protected fact rather than "the version I happened to read before the race". */
80-  sourceTemplateId?: string;
81-  sourceTemplateItemId?: string;
82-  sourceTemplateAppliedVersion?: number;
83-  createdAt: string;
84-  updatedAt: string;
85-  version: number;
86-  GSI1PK: string;
87-  GSI1SK: string;
88-  /** MaintenanceDueIndex pointer (D-179/D-185, 4th of 9 workers migrated) â€” present only while
89:   * `status === "SATISFIED"` AND `evidenceValidUntil` is set (see `deriveRequirementMaintenanceDue`
90-   * below); absent otherwise (MISSING/PENDING/NOT_SATISFIED/NOT_APPLICABLE, or a SATISFIED
91-   * evidence with no expiration, never drifts on its own). */
92-  GSI8PK?: string;
93-  GSI8SK?: string;
94-}
95-
96-/**
97- * Co-located under the owning Subject's partition (Decision 5), the same collection pattern
98- * already used by the older `subject/domain/requirement-assignment.ts`
99- * (`TENANT#t#SUBJECT#s`/`REQASSIGN#a`) and by `subject/domain/document-request.ts`'s
100- * co-location convention â€” a Requirement is not co-located under its Document (unlike
101- * DocumentVersion/DocumentVersionEvent, which physically belong to one Document) because a
102- * Requirement's lifecycle is owned by the Subject, not by whichever Document currently
103: * satisfies it; `evidenceVersionId` is the only pointer across that boundary.
104- */
105-export function requirementKey(tenantId: string, subjectId: string, requirementId: string): EntityKey {
106-  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: `REQUIREMENT#${requirementId}` };
107-}
108-
109-/** Prefix for `Query(PK, begins_with(SK, ...))` listing every Requirement of a Subject â€”
110- * no GSI needed for this access pattern, same convention as `REQUIREMENT_ASSIGNMENT_SK_PREFIX`. */
111-export const REQUIREMENT_SK_PREFIX = "REQUIREMENT#";
112-
113-/**
--
124-}
125-
126-export interface CreateRequirementInput {
127-  subjectId: string;
128-  name: string;
129-  notes?: string;
130-  applicability: RequirementApplicability;
131-}
132-
133-/** Fields an authenticated caller may change directly via `updateRequirement`. Never includes
134: * `evidenceVersionId`/`status`/`version` â€” those are controlled by `linkEvidence`/
135: * `unlinkEvidence` and the derivation function, never a free-form field update. */
136-export interface UpdateRequirementInput {
137-  name?: string;
138-  notes?: string;
139-  applicability?: RequirementApplicability;
140-}
141-
142:/** The minimal shape of the linked evidence DocumentVersion this derivation needs â€” never the
143- * full `DocumentVersion` type, so a caller can pass a partial read/projection. */
144-export interface EvidenceVersionForDerivation {
145-  state: DocumentVersionState;
146-  validUntil?: string;
147-}
148-
149-/**
150- * Pure status-derivation function (Decision 5) â€” no I/O, unit-testable in isolation, same
151- * style/discipline as `document-version.ts`'s `assertValidDocumentVersionTransition`.
152- *
153- * Branch order matters and is deliberately fail-closed-toward-MISSING/NOT_SATISFIED:
154- *  1. `applicability = NOT_APPLICABLE` always wins â€” status is NOT_APPLICABLE regardless of
155: *     any evidence link (Decision 5: applicability is a persisted fact, evidence never
156- *     overrides it).
157: *  2. No evidence linked at all -> MISSING.
158: *  3. Evidence linked but its DocumentVersion has not reached ACCEPTED yet (still DRAFT/
159- *     RECEIVED/UNDER_REVIEW, or terminal-but-not-accepted like REJECTED/WITHDRAWN/SUPERSEDED
160- *     â€” the latter two should never realistically be the CURRENT evidence pointer, but the
161- *     function stays total rather than assuming a caller-side invariant) -> PENDING.
162- *  4. Evidence ACCEPTED, no `validUntil` (document doesn't expire) or `validUntil` still in
163- *     the future (`>= now`, not `> now` â€” a document valid through end-of-day today is not
164- *     yet expired) -> SATISFIED.
165- *  5. Evidence ACCEPTED but `validUntil` in the past -> NOT_SATISFIED.
166- */
167:export function deriveRequirementStatus(
168-  applicability: RequirementApplicability,
169-  evidenceVersion: EvidenceVersionForDerivation | undefined,
170-  now: Date,
171-): RequirementStatus {
172-  if (applicability === "NOT_APPLICABLE") return "NOT_APPLICABLE";
173-  if (!evidenceVersion) return "MISSING";
174-  if (evidenceVersion.state !== "ACCEPTED") return "PENDING";
175-  if (!evidenceVersion.validUntil) return "SATISFIED";
176-  return new Date(evidenceVersion.validUntil).getTime() >= now.getTime() ? "SATISFIED" : "NOT_SATISFIED";
177-}
--
197- * `evidenceValidUntil`, not a second "reindex schedule" concept: the daily worker's ONLY job is
198- * the pure time-based SATISFIED -> NOT_SATISFIED drift, and `evidenceValidUntil` compared against
199- * `now` is exactly that `dueAt` (D-179 Round 3's correction). */
200-export const REQUIREMENT_REINDEX_WORK_TYPE = "REQUIREMENT_REINDEX";
201-
202-/** Due only while `status === "SATISFIED"` AND an `evidenceValidUntil` exists â€” a SATISFIED
203- * requirement whose evidence never expires (no `validUntil`) is SATISFIED forever, by
204- * construction, and never needs a reindex. Every other status is never a candidate (MISSING/
205- * PENDING/NOT_SATISFIED/NOT_APPLICABLE do not drift on their own â€” some other explicit mutation
206- * is always the trigger for those). */
207:export function deriveRequirementMaintenanceDue(status: RequirementStatus, evidenceValidUntil: string | undefined): { dueAtIso: string } | undefined {
208-  if (status !== "SATISFIED" || !evidenceValidUntil) return undefined;
209-  return { dueAtIso: evidenceValidUntil };
210-}
211-
212-/** `GSI8SK` embeds `requirementId` (not `subjectId`) for uniqueness â€” the candidate source
213- * recovers `tenantId`/`subjectId`/`requirementId` from the base table's own `PK`/`SK`
214- * (`requirementKey()`'s shape), same "KEYS_ONLY already returns them for free" posture as
215- * `documentFileGsi8Keys()`. */
216-export function requirementGsi8Keys(input: { dueAtIso: string; tenantId: string; requirementId: string }): { GSI8PK: string; GSI8SK: string } {
217-  return {
15-import type { MalwareEvidence } from "../../document/domain/malware-scan-result.js";
16-import { formatVersionSeq } from "./document-version.js";
17-
18-// D-163 Â§1 (symmetric evidence correlation): reused verbatim, not redeclared â€” the first
19-// physical event to arrive consolidates `quarantineObject` from its own observed triple, the
20-// second verifies exact equality against it via this same function.
21-export { sameObjectVersion } from "../../document/domain/document-object-reference.js";
22-
23:export type DocumentFileRole = "PRINCIPAL" | "ATTACHMENT";
24-
25-export type DocumentFileScanStatus = "PENDING_UPLOAD" | "SCANNING" | "CLEAN" | "REJECTED" | "UNSUPPORTED" | "TIMEOUT";
26-
27-/** Non-terminal states â€” a file in one of these can still be advanced by a physical event
28- * (S3 Object Created / GuardDuty finding) or by reconciliation timeout. */
29-const NON_TERMINAL_SCAN_STATUSES: readonly DocumentFileScanStatus[] = ["PENDING_UPLOAD", "SCANNING"];
30-
31-export function isNonTerminalFileScanStatus(status: DocumentFileScanStatus): boolean {
--
34-
35-export interface DocumentFile extends EntityKey {
36-  entityType: "DocumentFile";
37-  tenantId: string;
38-  documentId: string;
39-  versionId: string;
40-  seq: number;
41-  fileId: string;
42:  role: DocumentFileRole;
43-  scanStatus: DocumentFileScanStatus;
44-  mediaType: string;
45-  contentLength: number;
46-  checksumSha256: string;
47-  quarantineObject: DocumentObjectReference;
48-  cleanObject?: DocumentObjectReference;
49-  uploadEvidence?: UploadEvidence;
50-  malwareEvidence?: MalwareEvidence;
--
80- * reference (`infra/modules/dynamo-table/main.tf`), alongside the DLQ counterpart. */
81-export const DOCUMENT_FILE_RECONCILIATION_WORK_TYPE = "DOCUMENT_FILE_RECONCILIATION";
82-
83-export interface MaintenanceDue {
84-  dueAtIso: string;
85-}
86-
87-/**
88: * deriveDocumentFileMaintenanceDue â€” D-179 slice 3's pure due-date function, same role as
89- * `deriveMembershipMaintenanceDue()`/`deriveInvitationMaintenanceDue()`: single source of truth
90- * reused by the write path (`reserveFiles()`), the backfill script, and the worker's own
91- * processing (via `applyFileScanTimeout`'s fresh re-read).
92- *
93- * Closer to Invitation's PENDING branch than to Membership's REMOVED branch: the due date is
94- * fully known at creation (`reserveFiles()`) â€” `createdAt + FILE_SCAN_TIMEOUT_SECONDS` â€” there is
95- * no later transition ("scan started") to hang it on, since SCANNING is just evidence arriving
96- * within the same fixed window, never a new clock start. `undefined` once terminal (never a real
--
108-export function documentFileGsi8Keys(input: { dueAtIso: string; tenantId: string; fileId: string }): { GSI8PK: string; GSI8SK: string } {
109-  return {
110-    GSI8PK: `WORK#${DOCUMENT_FILE_RECONCILIATION_WORK_TYPE}`,
111-    GSI8SK: `${input.dueAtIso}#TENANT#${input.tenantId}#${input.fileId}`,
112-  };
113-}
114-
115-export interface FileUploadSpec {
116:  role: DocumentFileRole;
117-  mediaType: string;
118-  contentLength: number;
119-  checksumSha256: string;
120-}
121-
122-export class InvalidFileSetError extends Error {
123-  constructor(reason: string) {
124-    super(`Invalid file set for reserveFiles: ${reason}`);
125-    this.name = "InvalidFileSetError";
126-  }
127-}
128-
129:/** D-143 Decision 6: exactly one PRINCIPAL per batch. Pure validation, checked before any
130- * write is attempted â€” the actual concurrency race between two batches reserving the same
131- * DocumentVersion is closed separately, by the transaction's `fileSetSealed` fence in
132- * `DocumentArchiveService.reserveFiles()` (D-163 Â§2), never by this check alone. */
133-export function assertExactlyOnePrincipal(files: readonly FileUploadSpec[]): void {
134-  if (files.length === 0) throw new InvalidFileSetError("at least one file is required");
135:  const principals = files.filter((f) => f.role === "PRINCIPAL");
136:  if (principals.length !== 1) throw new InvalidFileSetError(`exactly one PRINCIPAL is required, got ${principals.length}`);
137-}
138-
139-/** D-163 Â§2: caps a single `reserveFiles` batch well under `TransactWriteItems`'s 100-action
140- * limit (N Put + 1 Update = N+1 actions in the worst case). */
141-export const MAX_FILES_PER_VERSION = 20;
/**
 * Transactional outbox helper - implementation-blueprint.md #5.1 (OutboxPort) and #5.3
 * (record shape + OutboxPublisher contract). Builds the outbox item + its
 * TransactWriteItems `Put` entry so callers can append it atomically alongside the
 * aggregate write (e.g. ExpirationItem update + ItemDueDateChanged in one transaction,
 * per data-model.md õ5).
 */
import type { DomainEvent } from "../contracts/events.js";
import type { TransactPutEntry, TransactWriteEntry } from "../dynamodb/occ.js";

export type OutboxStatus = "PENDING" | "PUBLISHED";

/**
 * M3.5 (docs/architecture/m3.5-runtime-design.md, "DecisÆo central: outbox dur vel"):
 * routing discriminator so `OutboxPublisher` (generic, -> EventBridge) and
 * `DispatchOutboxRelay` (new, -> SQS ReminderDispatchQueue) never both claim the same
 * record. `undefined`/absent means "OutboxPublisher's default EventBridge path" (every
 * M2/M3 caller today) - `OutboxPublisher` must explicitly skip any record whose
 * `destination` it doesn't recognize as its own default, never process-by-omission.
 */
export type OutboxDestination =
  | "SQS_REMINDER_DISPATCH_V1"
  | "SQS_NOTIFICATION_EMAIL_V1"
  | "SQS_DOCUMENT_CHASING_DISPATCH_V1"
  | "SQS_IMPORT_COMMIT_V1"
  /** BLOCKER-B (reminder-delivery-pipeline.md õ4): the ONLY real delivery path for an
   * outbox record - the "generic EventBridge path" every other destination's comment
   * describes as the default for an unset `destination` was never actually implemented in
   * this codebase (no PutEventsCommand call exists anywhere) - confirmed during BLOCKER-B
   * implementation, not previously known. Every outbox-driven consumer in production is
   * destination-routed through DispatchOutboxRelay; this destination follows the same
   * proven mechanism rather than the never-built one the original design doc assumed. */
  | "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1"
  /** D-192 slice 9 (`bulk-import-documents-requirements-scoping/estado-final-consolidado.md`
   * õ3): dispatched in the SAME `TransactWriteItems` as `POST /import-jobs/{jobId}/mapping`'s
   * `AWAITING_MAPPING`->`PARSING` claim, whenever that POST is the one that resolves the claim
   * (never when the job stays `UPLOADED`, mapping-only write). Consumed by the SAME
   * `parseImportJob()`/`import-parse-handler.ts` the S3-event trigger already uses - the two
   * triggers are discriminated only by envelope shape in the Lambda handler, never inside the
   * pure function, per the design. */
  | "SQS_IMPORT_PARSE_V1";

export interface OutboxRecord {
  PK: string;
  SK: string;
  entityType: "OutboxEvent";
  /** BLOCKER-B addition: previously only embedded in `PK` (`TENANT#<t>#OUTBOX#<shard>`),
   * never its own field - a sender whose payload doesn't already self-describe its tenant
   * (unlike DispatchCommand) needs this explicitly, without every caller re-parsing `PK`. */
  tenantId: string;
  eventId: string;
  eventType: string;
  aggregateType: string;
  aggregateId: string;
  aggregateVersion: number;
  status: OutboxStatus;
  occurredAt: string;
  payload: Record<string, unknown>;
  publishAttempts: number;
  nextAttemptAt: string;
  createdAt: string;
  GSI6PK: string;
  GSI6SK: string;
  destination?: OutboxDestination;
  /**
   * m5-observability-design.md #2: copied from `DomainEvent.correlationId` (already mandatory
   * on every event) at write time - never read from the ambient AsyncLocalStorage context here,
   * so this stays a pure, environment-agnostic builder. Optional only because records persisted
   * before M5 don't have it (historical data, never written without it again) - readers
   * (relay/sweeper) must fall back to `eventId` when absent, per the design's fallback table.
   */
  correlationId?: string;
}

/** Monthly shard for the outbox partition, matching #5.3's `TENANT#t#OUTBOX#202608` example. */
function monthShard(isoTimestamp: string): string {
  return isoTimestamp.slice(0, 7).replace("-", "");
}

export function buildOutboxRecord(event: DomainEvent, destination?: OutboxDestination): OutboxRecord {
  const shard = monthShard(event.occurredAt);
  return {
    PK: `TENANT#${event.tenantId}#OUTBOX#${shard}`,
    SK: `EVENT#${event.occurredAt}#${event.eventId}`,
    entityType: "OutboxEvent",
    tenantId: event.tenantId,
    eventId: event.eventId,
    eventType: event.eventType,
    aggregateType: event.aggregate.type,
    aggregateId: event.aggregate.id,
    aggregateVersion: event.aggregate.version,
    status: "PENDING",
    occurredAt: event.occurredAt,
    payload: event.data,
    publishAttempts: 0,
    nextAttemptAt: event.occurredAt,
    createdAt: event.occurredAt,
    GSI6PK: "RECON#OUTBOX#PENDING",
    GSI6SK: `${event.occurredAt}#${event.eventId}`,
    correlationId: event.correlationId,
    ...(destination ? { destination } : {}),
  };
}

/**
 * m5-observability-design.md #2: correlation id a relay/sweeper should use for the record
 * being processed - read from the persisted `OutboxRecord`, never from the caller's own
 * ambient context. Falls back to `eventId` only for records persisted before M5 (missing
 * `correlationId`); this is historical-data compatibility, not a new write path.
 */
export function outboxRecordCorrelationId(record: OutboxRecord): string {
  return record.correlationId ?? record.eventId;
}

/** @deprecated kept as an alias of the shared `TransactPutEntry` (src/shared/dynamodb/occ.ts)
 * for backward compatibility with existing imports - full-audit round1/qualidade found this
 * was a duplicate, structurally-identical declaration of the same shape (same pattern as the
 * expiration-store.ts/reminder-store.ts duplication decisions-log.md E-008 already fixed).
 * Do not add fields here; add them to the shared type instead. */
export type DynamoTransactPutEntry = TransactPutEntry;

/**
 * OutboxPort.append from implementation-blueprint.md #5.1: appends the outbox Put to a
 * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
 * owns the transaction and includes the aggregate's own conditional update alongside it.
 * Accepts the full `TransactWriteEntry` union (Put | Update) so callers whose transaction
 * also contains Update entries (e.g. reminder dispatch's CLAIMED->TRIGGERED transition) can
 * pass their real array directly, without an unsafe cast to satisfy a narrower Put-only type.
 */
export function appendToTransaction(
  tx: TransactWriteEntry[],
  tableName: string,
  event: DomainEvent,
  destination?: OutboxDestination,
): void {
  const record = buildOutboxRecord(event, destination);
  tx.push({
    Put: {
      TableName: tableName,
      Item: { ...record },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });
}

/** Backoff schedule for OutboxPublisher retries (#5.3 step 6: "incrementa tentativas e
 * aplica backoff"). Exponential with a cap; judgment call - blueprint doesn't pin exact values. */
export function nextAttemptDelayMs(publishAttempts: number): number {
  const base = 1_000;
  const cap = 5 * 60_000;
  return Math.min(cap, base * 2 ** publishAttempts);
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 6 "tenant|ACTIVE|lifecycle|READY_TO_PROMOTE|parseSubmission|unrecognized key" src/runtime/aws/handlers/upload-finalizer-handler.ts src/runtime/aws/handlers/malware-result-handler.ts src/workers/upload-finalizer src/workers/malware-result src/workers/submission* src/modules/document-archive/application/apply-file-scan-result.ts; rg -n "OutboxDestination|destination|PutEventsCommand|SQS_.*V1|DispatchOutboxRelay" src | Select-Object -First 200; rg -n -C 8 "linkEvidence|unlinkEvidence|evidenceValidUntil|evidenceState" src/modules/document-archive/application src/modules/document-archive/persistence src/modules/document-archive/http' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 405ms:
src/modules/document-archive/application/apply-file-scan-result.ts-5- * is a structural subset of `DocumentStatus`, see `document-file.ts`'s module doc comment).
src/modules/document-archive/application/apply-file-scan-result.ts-6- *
src/modules/document-archive/application/apply-file-scan-result.ts-7- * Split into two functions rather than M6's single `advanceAfterEvidence()`, because unlike
src/modules/document-archive/application/apply-file-scan-result.ts-8- * M6's Document (whose `quarantineObject.versionId` is a permanent "" placeholder,
src/modules/document-archive/application/apply-file-scan-result.ts-9- * `document.ts`'s doc comment), a `DocumentFile.quarantineObject.versionId` IS the field the
src/modules/document-archive/application/apply-file-scan-result.ts-10- * symmetric evidence correlation consolidates into (D-163 §1) - so this function never needs to
src/modules/document-archive/application/apply-file-scan-result.ts:11: * reach into S3 itself just to decide PROMOTE-vs-not: it returns `READY_TO_PROMOTE` with the
src/modules/document-archive/application/apply-file-scan-result.ts-12- * `sourceObject` a worker copies from, and does zero writes for that outcome (never claims CLEAN
src/modules/document-archive/application/apply-file-scan-result.ts-13- * for a copy that hasn't happened, same discipline M6's PROMOTE branch already established).
src/modules/document-archive/application/apply-file-scan-result.ts-14- * `confirmFileScanClean()` is the confirmation step, called only once that caller's own S3
src/modules/document-archive/application/apply-file-scan-result.ts-15- * copy+verify has succeeded - mirrors M6's `advanceAfterEvidence()` PROMOTE branch's contract,
src/modules/document-archive/application/apply-file-scan-result.ts-16- * just as a separate, independently retryable step instead of inlined in the same function.
src/modules/document-archive/application/apply-file-scan-result.ts-17- */
--
src/modules/document-archive/application/apply-file-scan-result.ts-31-  tableName: string;
src/modules/document-archive/application/apply-file-scan-result.ts-32-  ids: DocumentArchiveIdGenerator;
src/modules/document-archive/application/apply-file-scan-result.ts-33-  now?: () => string;
src/modules/document-archive/application/apply-file-scan-result.ts-34-}
src/modules/document-archive/application/apply-file-scan-result.ts-35-
src/modules/document-archive/application/apply-file-scan-result.ts-36-export interface ApplyFileScanResultInput {
src/modules/document-archive/application/apply-file-scan-result.ts:37:  tenantId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-38-  documentId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-39-  seq: number;
src/modules/document-archive/application/apply-file-scan-result.ts-40-  fileId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-41-  /** The object reference THIS physical event actually observed (S3 Object Created / GuardDuty
src/modules/document-archive/application/apply-file-scan-result.ts-42-   * finding) - never the stored `DocumentFile.quarantineObject`, per D-163 §1: the first event
src/modules/document-archive/application/apply-file-scan-result.ts-43-   * to arrive consolidates FROM this triple, the second is verified AGAINST what got
--
src/modules/document-archive/application/apply-file-scan-result.ts-49-
src/modules/document-archive/application/apply-file-scan-result.ts-50-export type ApplyFileScanResultOutcome =
src/modules/document-archive/application/apply-file-scan-result.ts-51-  | { outcome: "AWAITING" }
src/modules/document-archive/application/apply-file-scan-result.ts-52-  | { outcome: "IGNORED_WRONG_VERSION" }
src/modules/document-archive/application/apply-file-scan-result.ts-53-  | { outcome: "IGNORED_STALE" }
src/modules/document-archive/application/apply-file-scan-result.ts-54-  | { outcome: "REJECTED"; status: Extract<DocumentFileScanStatus, "REJECTED" | "UNSUPPORTED"> }
src/modules/document-archive/application/apply-file-scan-result.ts:55:  | { outcome: "READY_TO_PROMOTE"; sourceObject: DocumentObjectReference };
src/modules/document-archive/application/apply-file-scan-result.ts-56-
src/modules/document-archive/application/apply-file-scan-result.ts-57-const MAX_OCC_RETRIES = 10;
src/modules/document-archive/application/apply-file-scan-result.ts-58-
src/modules/document-archive/application/apply-file-scan-result.ts-59-/** Re-reads the file fresh on every attempt - never assumes the caller's in-memory copy is
src/modules/document-archive/application/apply-file-scan-result.ts-60- * current, since the upload-finalizer and malware-scan events for the SAME file may land
src/modules/document-archive/application/apply-file-scan-result.ts-61- * concurrently, exactly the corridor `decideNextAction()`'s doc comment describes for M6. */
src/modules/document-archive/application/apply-file-scan-result.ts-62-export async function applyFileScanResult(deps: ApplyFileScanResultDeps, input: ApplyFileScanResultInput): Promise<ApplyFileScanResultOutcome> {
src/modules/document-archive/application/apply-file-scan-result.ts-63-  const now = deps.now ?? (() => new Date().toISOString());
src/modules/document-archive/application/apply-file-scan-result.ts:64:  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);
src/modules/document-archive/application/apply-file-scan-result.ts-65-
src/modules/document-archive/application/apply-file-scan-result.ts-66-  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
src/modules/document-archive/application/apply-file-scan-result.ts-67-    const file = await deps.store.get<DocumentFile>(key);
src/modules/document-archive/application/apply-file-scan-result.ts-68-    if (!file) return { outcome: "IGNORED_STALE" };
src/modules/document-archive/application/apply-file-scan-result.ts-69-
src/modules/document-archive/application/apply-file-scan-result.ts-70-    // Terminal-state idempotency FIRST, before any correlation check: a repeated/late event
--
src/modules/document-archive/application/apply-file-scan-result.ts-104-      try {
src/modules/document-archive/application/apply-file-scan-result.ts-105-        await deps.store.transactWrite([
src/modules/document-archive/application/apply-file-scan-result.ts-106-          {
src/modules/document-archive/application/apply-file-scan-result.ts-107-            Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-108-              tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts-109-              key,
src/modules/document-archive/application/apply-file-scan-result.ts:110:              tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-111-              expectedVersion: file.version,
src/modules/document-archive/application/apply-file-scan-result.ts-112-              // SCANNING marks "at least one physical event has landed" independent of
src/modules/document-archive/application/apply-file-scan-result.ts-113-              // decideNextAction's own vocabulary (it only ever returns actions, never this
src/modules/document-archive/application/apply-file-scan-result.ts-114-              // specific status) - a no-op re-SET when the file was already SCANNING.
src/modules/document-archive/application/apply-file-scan-result.ts-115-              set: { scanStatus: "SCANNING", ...quarantineObjectSet, ...evidenceSet },
src/modules/document-archive/application/apply-file-scan-result.ts-116-              now: nowTs,
--
src/modules/document-archive/application/apply-file-scan-result.ts-127-    if (decision.action === "PROMOTE") {
src/modules/document-archive/application/apply-file-scan-result.ts-128-      // Never claims CLEAN itself - zero writes here, same discipline M6's PROMOTE branch
src/modules/document-archive/application/apply-file-scan-result.ts-129-      // established (`advance-after-evidence.ts`'s doc comment). The evidence merge that got us
src/modules/document-archive/application/apply-file-scan-result.ts-130-      // here was already persisted by an earlier AWAITING call (PROMOTE is only reachable once
src/modules/document-archive/application/apply-file-scan-result.ts-131-      // BOTH evidences exist, and only one arrives per call) - nothing new to consolidate.
src/modules/document-archive/application/apply-file-scan-result.ts-132-      const sourceObject = consolidated ? file.quarantineObject : input.observedObject;
src/modules/document-archive/application/apply-file-scan-result.ts:133:      return { outcome: "READY_TO_PROMOTE", sourceObject };
src/modules/document-archive/application/apply-file-scan-result.ts-134-    }
src/modules/document-archive/application/apply-file-scan-result.ts-135-
src/modules/document-archive/application/apply-file-scan-result.ts-136-    // decision.action === "REJECT": terminal transition - counters + GSI8 removal happen in the
src/modules/document-archive/application/apply-file-scan-result.ts-137-    // SAME transaction as the file's own write (Decision 6/Bloqueador 9: a crash between the two
src/modules/document-archive/application/apply-file-scan-result.ts-138-    // would leave pendingFileScans stuck above zero forever, permanently blocking acceptVersion).
src/modules/document-archive/application/apply-file-scan-result.ts:139:    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
src/modules/document-archive/application/apply-file-scan-result.ts-140-    if (!version) return { outcome: "IGNORED_STALE" }; // Version cannot be removed once files exist - fail closed if this ever changes.
src/modules/document-archive/application/apply-file-scan-result.ts-141-
src/modules/document-archive/application/apply-file-scan-result.ts-142-    const infected = mergedMalwareEvidence?.status === "THREATS_FOUND";
src/modules/document-archive/application/apply-file-scan-result.ts-143-    const nowTs = now();
src/modules/document-archive/application/apply-file-scan-result.ts-144-    const entries: TransactWriteEntry[] = [
src/modules/document-archive/application/apply-file-scan-result.ts-145-      {
src/modules/document-archive/application/apply-file-scan-result.ts-146-        Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-147-          tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts-148-          key,
src/modules/document-archive/application/apply-file-scan-result.ts:149:          tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-150-          expectedVersion: file.version,
src/modules/document-archive/application/apply-file-scan-result.ts-151-          set: { scanStatus: decision.status, ...quarantineObjectSet, ...evidenceSet },
src/modules/document-archive/application/apply-file-scan-result.ts-152-          // D-163 §5/§6: any transaction reaching a terminal scanStatus removes the sparse GSI8
src/modules/document-archive/application/apply-file-scan-result.ts-153-          // reconciliation pointer in the same write - never a separate cleanup step.
src/modules/document-archive/application/apply-file-scan-result.ts-154-          remove: ["GSI8PK", "GSI8SK"],
src/modules/document-archive/application/apply-file-scan-result.ts-155-          now: nowTs,
src/modules/document-archive/application/apply-file-scan-result.ts-156-        }),
src/modules/document-archive/application/apply-file-scan-result.ts-157-      },
src/modules/document-archive/application/apply-file-scan-result.ts-158-      {
src/modules/document-archive/application/apply-file-scan-result.ts-159-        Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-160-          tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts:161:          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
src/modules/document-archive/application/apply-file-scan-result.ts:162:          tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-163-          expectedVersion: version.version,
src/modules/document-archive/application/apply-file-scan-result.ts-164-          set: { pendingFileScans: version.pendingFileScans - 1, ...(infected ? { infectedFileScans: version.infectedFileScans + 1 } : {}) },
src/modules/document-archive/application/apply-file-scan-result.ts-165-          now: nowTs,
src/modules/document-archive/application/apply-file-scan-result.ts-166-        }),
src/modules/document-archive/application/apply-file-scan-result.ts-167-      },
src/modules/document-archive/application/apply-file-scan-result.ts-168-    ];
--
src/modules/document-archive/application/apply-file-scan-result.ts-171-    // audit trail exists specifically so "why was this file removed from consideration" is
src/modules/document-archive/application/apply-file-scan-result.ts-172-    // answerable without re-deriving it from malwareEvidence.
src/modules/document-archive/application/apply-file-scan-result.ts-173-    if (infected) {
src/modules/document-archive/application/apply-file-scan-result.ts-174-      entries.push({
src/modules/document-archive/application/apply-file-scan-result.ts-175-        Put: buildVersionedCreate(
src/modules/document-archive/application/apply-file-scan-result.ts-176-          deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts:177:          buildFileRejectedEvent(deps, input.tenantId, input.documentId, input.seq, version.versionId, version.state, input.fileId, file.scanStatus, decision.status, nowTs) as unknown as Record<string, unknown> & EntityKey,
src/modules/document-archive/application/apply-file-scan-result.ts-178-        ),
src/modules/document-archive/application/apply-file-scan-result.ts-179-      });
src/modules/document-archive/application/apply-file-scan-result.ts-180-    }
src/modules/document-archive/application/apply-file-scan-result.ts-181-    try {
src/modules/document-archive/application/apply-file-scan-result.ts-182-      await deps.store.transactWrite(entries);
src/modules/document-archive/application/apply-file-scan-result.ts-183-      return { outcome: "REJECTED", status: decision.status };
--
src/modules/document-archive/application/apply-file-scan-result.ts-190-  throw new Error(`applyFileScanResult exhausted retries for file ${input.fileId} under contention.`);
src/modules/document-archive/application/apply-file-scan-result.ts-191-}
src/modules/document-archive/application/apply-file-scan-result.ts-192-
src/modules/document-archive/application/apply-file-scan-result.ts-193-export type ConfirmFileScanCleanOutcome = "CONFIRMED" | "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-194-
src/modules/document-archive/application/apply-file-scan-result.ts-195-export interface ConfirmFileScanCleanInput {
src/modules/document-archive/application/apply-file-scan-result.ts:196:  tenantId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-197-  documentId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-198-  seq: number;
src/modules/document-archive/application/apply-file-scan-result.ts-199-  fileId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-200-  cleanObject: DocumentObjectReference;
src/modules/document-archive/application/apply-file-scan-result.ts-201-}
src/modules/document-archive/application/apply-file-scan-result.ts-202-
--
src/modules/document-archive/application/apply-file-scan-result.ts-207- * requires before it will persist CLEAN). Idempotent against a file that already moved off
src/modules/document-archive/application/apply-file-scan-result.ts-208- * SCANNING/PENDING_UPLOAD by the time this is called (e.g. concurrently rejected) - never
src/modules/document-archive/application/apply-file-scan-result.ts-209- * resurrects a terminal file back toward CLEAN.
src/modules/document-archive/application/apply-file-scan-result.ts-210- */
src/modules/document-archive/application/apply-file-scan-result.ts-211-export async function confirmFileScanClean(deps: ApplyFileScanResultDeps, input: ConfirmFileScanCleanInput): Promise<ConfirmFileScanCleanOutcome> {
src/modules/document-archive/application/apply-file-scan-result.ts-212-  const now = deps.now ?? (() => new Date().toISOString());
src/modules/document-archive/application/apply-file-scan-result.ts:213:  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);
src/modules/document-archive/application/apply-file-scan-result.ts-214-
src/modules/document-archive/application/apply-file-scan-result.ts-215-  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
src/modules/document-archive/application/apply-file-scan-result.ts-216-    const file = await deps.store.get<DocumentFile>(key);
src/modules/document-archive/application/apply-file-scan-result.ts-217-    if (!file) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-218-    if (!isNonTerminalFileScanStatus(file.scanStatus)) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-219-
src/modules/document-archive/application/apply-file-scan-result.ts:220:    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
src/modules/document-archive/application/apply-file-scan-result.ts-221-    if (!version) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-222-
src/modules/document-archive/application/apply-file-scan-result.ts-223-    const nowTs = now();
src/modules/document-archive/application/apply-file-scan-result.ts-224-    const entries: TransactWriteEntry[] = [
src/modules/document-archive/application/apply-file-scan-result.ts-225-      {
src/modules/document-archive/application/apply-file-scan-result.ts-226-        Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-227-          tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts-228-          key,
src/modules/document-archive/application/apply-file-scan-result.ts:229:          tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-230-          expectedVersion: file.version,
src/modules/document-archive/application/apply-file-scan-result.ts-231-          set: { scanStatus: "CLEAN", cleanObject: input.cleanObject },
src/modules/document-archive/application/apply-file-scan-result.ts-232-          remove: ["GSI8PK", "GSI8SK"],
src/modules/document-archive/application/apply-file-scan-result.ts-233-          now: nowTs,
src/modules/document-archive/application/apply-file-scan-result.ts-234-        }),
src/modules/document-archive/application/apply-file-scan-result.ts-235-      },
src/modules/document-archive/application/apply-file-scan-result.ts-236-      {
src/modules/document-archive/application/apply-file-scan-result.ts-237-        Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-238-          tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts:239:          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
src/modules/document-archive/application/apply-file-scan-result.ts:240:          tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-241-          expectedVersion: version.version,
src/modules/document-archive/application/apply-file-scan-result.ts-242-          set: { pendingFileScans: version.pendingFileScans - 1 },
src/modules/document-archive/application/apply-file-scan-result.ts-243-          now: nowTs,
src/modules/document-archive/application/apply-file-scan-result.ts-244-        }),
src/modules/document-archive/application/apply-file-scan-result.ts-245-      },
src/modules/document-archive/application/apply-file-scan-result.ts-246-    ];
--
src/modules/document-archive/application/apply-file-scan-result.ts-256-  throw new Error(`confirmFileScanClean exhausted retries for file ${input.fileId} under contention.`);
src/modules/document-archive/application/apply-file-scan-result.ts-257-}
src/modules/document-archive/application/apply-file-scan-result.ts-258-
src/modules/document-archive/application/apply-file-scan-result.ts-259-export type ApplyFileScanTimeoutOutcome = "TIMED_OUT" | "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-260-
src/modules/document-archive/application/apply-file-scan-result.ts-261-export interface ApplyFileScanTimeoutInput {
src/modules/document-archive/application/apply-file-scan-result.ts:262:  tenantId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-263-  documentId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-264-  seq: number;
src/modules/document-archive/application/apply-file-scan-result.ts-265-  fileId: string;
src/modules/document-archive/application/apply-file-scan-result.ts-266-  /** The exact GSI8 pointer the reconciliation scan observed (D-163 round4 §3) - the
src/modules/document-archive/application/apply-file-scan-result.ts-267-   * transaction below conditions on it verbatim, so a candidate whose deadline changed or
src/modules/document-archive/application/apply-file-scan-result.ts-268-   * that already reached a terminal state between the scan and this write is naturally
--
src/modules/document-archive/application/apply-file-scan-result.ts-278- * new ingredient is conditioning on the observed GSI8 pointer itself, which a physical-event
src/modules/document-archive/application/apply-file-scan-result.ts-279- * caller never needs (it always re-reads the file fresh and checks scanStatus, never a stale
src/modules/document-archive/application/apply-file-scan-result.ts-280- * discovery pointer).
src/modules/document-archive/application/apply-file-scan-result.ts-281- */
src/modules/document-archive/application/apply-file-scan-result.ts-282-export async function applyFileScanTimeout(deps: ApplyFileScanResultDeps, input: ApplyFileScanTimeoutInput): Promise<ApplyFileScanTimeoutOutcome> {
src/modules/document-archive/application/apply-file-scan-result.ts-283-  const now = deps.now ?? (() => new Date().toISOString());
src/modules/document-archive/application/apply-file-scan-result.ts:284:  const key = documentFileKey(input.tenantId, input.documentId, input.seq, input.fileId);
src/modules/document-archive/application/apply-file-scan-result.ts-285-
src/modules/document-archive/application/apply-file-scan-result.ts-286-  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
src/modules/document-archive/application/apply-file-scan-result.ts-287-    const file = await deps.store.get<DocumentFile>(key);
src/modules/document-archive/application/apply-file-scan-result.ts-288-    if (!file) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-289-    if (!isNonTerminalFileScanStatus(file.scanStatus)) return "IGNORED_STALE";
src/modules/document-archive/application/apply-file-scan-result.ts-290-
src/modules/document-archive/application/apply-file-scan-result.ts:291:    const version = await deps.store.get<DocumentVersion>(documentVersionKey(input.tenantId, input.documentId, input.seq));
src/modules/document-archive/application/apply-file-scan-result.ts-292-    if (!version) return "IGNORED_STALE"; // Version cannot be removed once files exist - fail closed if this ever changes.
src/modules/document-archive/application/apply-file-scan-result.ts-293-
src/modules/document-archive/application/apply-file-scan-result.ts-294-    const nowTs = now();
src/modules/document-archive/application/apply-file-scan-result.ts-295-    const entries: TransactWriteEntry[] = [
src/modules/document-archive/application/apply-file-scan-result.ts-296-      {
src/modules/document-archive/application/apply-file-scan-result.ts-297-        Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-298-          tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts-299-          key,
src/modules/document-archive/application/apply-file-scan-result.ts:300:          tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-301-          expectedVersion: file.version,
src/modules/document-archive/application/apply-file-scan-result.ts-302-          set: { scanStatus: "TIMEOUT" },
src/modules/document-archive/application/apply-file-scan-result.ts-303-          remove: ["GSI8PK", "GSI8SK"],
src/modules/document-archive/application/apply-file-scan-result.ts-304-          // Exact-pointer fence (round4-claude-final.md §3): closes the race where the
src/modules/document-archive/application/apply-file-scan-result.ts-305-          // candidate this scan observed already advanced (new deadline, or terminal) by the
src/modules/document-archive/application/apply-file-scan-result.ts-306-          // time this write lands - never conditioned on `file.version` alone, since a
--
src/modules/document-archive/application/apply-file-scan-result.ts-313-          now: nowTs,
src/modules/document-archive/application/apply-file-scan-result.ts-314-        }),
src/modules/document-archive/application/apply-file-scan-result.ts-315-      },
src/modules/document-archive/application/apply-file-scan-result.ts-316-      {
src/modules/document-archive/application/apply-file-scan-result.ts-317-        Update: buildVersionedUpdate({
src/modules/document-archive/application/apply-file-scan-result.ts-318-          tableName: deps.tableName,
src/modules/document-archive/application/apply-file-scan-result.ts:319:          key: documentVersionKey(input.tenantId, input.documentId, input.seq),
src/modules/document-archive/application/apply-file-scan-result.ts:320:          tenantId: input.tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-321-          expectedVersion: version.version,
src/modules/document-archive/application/apply-file-scan-result.ts-322-          set: { pendingFileScans: version.pendingFileScans - 1 },
src/modules/document-archive/application/apply-file-scan-result.ts-323-          now: nowTs,
src/modules/document-archive/application/apply-file-scan-result.ts-324-        }),
src/modules/document-archive/application/apply-file-scan-result.ts-325-      },
src/modules/document-archive/application/apply-file-scan-result.ts-326-    ];
--
src/modules/document-archive/application/apply-file-scan-result.ts-335-
src/modules/document-archive/application/apply-file-scan-result.ts-336-  throw new Error(`applyFileScanTimeout exhausted retries for file ${input.fileId} under contention.`);
src/modules/document-archive/application/apply-file-scan-result.ts-337-}
src/modules/document-archive/application/apply-file-scan-result.ts-338-
src/modules/document-archive/application/apply-file-scan-result.ts-339-function buildFileRejectedEvent(
src/modules/document-archive/application/apply-file-scan-result.ts-340-  deps: ApplyFileScanResultDeps,
src/modules/document-archive/application/apply-file-scan-result.ts:341:  tenantId: string,
src/modules/document-archive/application/apply-file-scan-result.ts-342-  documentId: string,
src/modules/document-archive/application/apply-file-scan-result.ts-343-  seq: number,
src/modules/document-archive/application/apply-file-scan-result.ts-344-  versionId: string,
src/modules/document-archive/application/apply-file-scan-result.ts-345-  versionState: DocumentVersionEvent["toState"],
src/modules/document-archive/application/apply-file-scan-result.ts-346-  fileId: string,
src/modules/document-archive/application/apply-file-scan-result.ts-347-  fromFileScanStatus: DocumentFileScanStatus,
src/modules/document-archive/application/apply-file-scan-result.ts-348-  toFileScanStatus: DocumentFileScanStatus,
src/modules/document-archive/application/apply-file-scan-result.ts-349-  occurredAt: string,
src/modules/document-archive/application/apply-file-scan-result.ts-350-): DocumentVersionEvent {
src/modules/document-archive/application/apply-file-scan-result.ts-351-  return {
src/modules/document-archive/application/apply-file-scan-result.ts:352:    ...documentVersionEventKey(tenantId, documentId, seq, deps.ids.newEventId()),
src/modules/document-archive/application/apply-file-scan-result.ts-353-    entityType: "DocumentVersionEvent",
src/modules/document-archive/application/apply-file-scan-result.ts:354:    tenantId,
src/modules/document-archive/application/apply-file-scan-result.ts-355-    documentId,
src/modules/document-archive/application/apply-file-scan-result.ts-356-    versionId,
src/modules/document-archive/application/apply-file-scan-result.ts-357-    type: "FILE_REJECTED_INFECTED",
src/modules/document-archive/application/apply-file-scan-result.ts-358-    // `fromState`/`toState` record the Version's own (unchanged) state as informational context
src/modules/document-archive/application/apply-file-scan-result.ts-359-    // only - the real transition this event carries is fromFileScanStatus/toFileScanStatus
src/modules/document-archive/application/apply-file-scan-result.ts-360-    // below, per document-version-event.ts's doc comment on this event type.
--
src/runtime/aws/handlers/malware-result-handler.ts-5-import { createDocumentClient } from "../../../shared/dynamodb/client.js";
src/runtime/aws/handlers/malware-result-handler.ts-6-import { buildDocumentWorkerDeps } from "../composition/document.js";
src/runtime/aws/handlers/malware-result-handler.ts-7-import { processMalwareResult } from "../../../workers/malware-result/result-processor.js";
src/runtime/aws/handlers/malware-result-handler.ts-8-import { parseQuarantineKey } from "../../../modules/document/domain/quarantine-key.js";
src/runtime/aws/handlers/malware-result-handler.ts-9-import type { GuardDutyScanStatus } from "../../../modules/document/domain/malware-scan-result.js";
src/runtime/aws/handlers/malware-result-handler.ts-10-// M10 (D-037): mesmo branch aditivo do upload-finalizer-handler.ts, ver comentário lá.
src/runtime/aws/handlers/malware-result-handler.ts:11:import { parseSubmissionQuarantineKey } from "../../../modules/subject/domain/submission-quarantine-key.js";
src/runtime/aws/handlers/malware-result-handler.ts-12-import { processSubmissionMalwareResult } from "../../../workers/submission-malware-result/result-processor.js";
src/runtime/aws/handlers/malware-result-handler.ts-13-import { buildSubjectWorkerDeps } from "../composition/subject.js";
src/runtime/aws/handlers/malware-result-handler.ts-14-import { runWithContext } from "../../../shared/observability/context.js";
src/runtime/aws/handlers/malware-result-handler.ts-15-import { SecureLogger } from "../../../shared/observability/logger.js";
src/runtime/aws/handlers/malware-result-handler.ts-16-
src/runtime/aws/handlers/malware-result-handler.ts-17-const client = createDocumentClient();
--
src/runtime/aws/handlers/malware-result-handler.ts-55-          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/malware-result-handler.ts-56-          return;
src/runtime/aws/handlers/malware-result-handler.ts-57-        }
src/runtime/aws/handlers/malware-result-handler.ts-58-
src/runtime/aws/handlers/malware-result-handler.ts-59-        const parsed = parseQuarantineKey(detail.s3ObjectDetails.objectKey);
src/runtime/aws/handlers/malware-result-handler.ts-60-        if (parsed) {
src/runtime/aws/handlers/malware-result-handler.ts:61:          await runWithContext({ correlationId: randomUUID(), tenantId: parsed.tenantId }, async () => {
src/runtime/aws/handlers/malware-result-handler.ts-62-            const outcome = await processMalwareResult(deps, {
src/runtime/aws/handlers/malware-result-handler.ts:63:              tenantId: parsed.tenantId,
src/runtime/aws/handlers/malware-result-handler.ts-64-              itemId: parsed.itemId,
src/runtime/aws/handlers/malware-result-handler.ts-65-              documentId: parsed.documentId,
src/runtime/aws/handlers/malware-result-handler.ts-66-              object: { bucket: detail.s3ObjectDetails.bucketName, key: detail.s3ObjectDetails.objectKey, versionId: detail.s3ObjectDetails.versionId ?? "" },
src/runtime/aws/handlers/malware-result-handler.ts-67-              status: status as GuardDutyScanStatus,
src/runtime/aws/handlers/malware-result-handler.ts-68-              scanResultId: message.id ?? record.messageId,
src/runtime/aws/handlers/malware-result-handler.ts-69-            });
src/runtime/aws/handlers/malware-result-handler.ts-70-            logger.info("malware-result outcome", { documentId: parsed.documentId, outcome, status });
src/runtime/aws/handlers/malware-result-handler.ts-71-          });
src/runtime/aws/handlers/malware-result-handler.ts-72-          return;
src/runtime/aws/handlers/malware-result-handler.ts-73-        }
src/runtime/aws/handlers/malware-result-handler.ts-74-
src/runtime/aws/handlers/malware-result-handler.ts:75:        const parsedSubmission = parseSubmissionQuarantineKey(detail.s3ObjectDetails.objectKey);
src/runtime/aws/handlers/malware-result-handler.ts-76-        if (!parsedSubmission) {
src/runtime/aws/handlers/malware-result-handler.ts:77:          logger.error("malware-result unrecognized key shape", { key: detail.s3ObjectDetails.objectKey });
src/runtime/aws/handlers/malware-result-handler.ts-78-          return;
src/runtime/aws/handlers/malware-result-handler.ts-79-        }
src/runtime/aws/handlers/malware-result-handler.ts-80-
src/runtime/aws/handlers/malware-result-handler.ts:81:        await runWithContext({ correlationId: randomUUID(), tenantId: parsedSubmission.tenantId }, async () => {
src/runtime/aws/handlers/malware-result-handler.ts-82-          const outcome = await processSubmissionMalwareResult(submissionDeps, {
src/runtime/aws/handlers/malware-result-handler.ts:83:            tenantId: parsedSubmission.tenantId,
src/runtime/aws/handlers/malware-result-handler.ts-84-            subjectId: parsedSubmission.subjectId,
src/runtime/aws/handlers/malware-result-handler.ts-85-            assignmentId: parsedSubmission.assignmentId,
src/runtime/aws/handlers/malware-result-handler.ts-86-            submissionId: parsedSubmission.submissionId,
src/runtime/aws/handlers/malware-result-handler.ts-87-            object: { bucket: detail.s3ObjectDetails.bucketName, key: detail.s3ObjectDetails.objectKey, versionId: detail.s3ObjectDetails.versionId ?? "" },
src/runtime/aws/handlers/malware-result-handler.ts-88-            status: status as GuardDutyScanStatus,
src/runtime/aws/handlers/malware-result-handler.ts-89-            scanResultId: message.id ?? record.messageId,
--
src/runtime/aws/handlers/upload-finalizer-handler.ts-4-import { randomUUID } from "node:crypto";
src/runtime/aws/handlers/upload-finalizer-handler.ts-5-import { createDocumentClient } from "../../../shared/dynamodb/client.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-6-import { buildDocumentWorkerDeps } from "../composition/document.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-7-import { finalizeUpload } from "../../../workers/upload-finalizer/finalizer.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-8-import { parseQuarantineKey } from "../../../modules/document/domain/quarantine-key.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-9-// M10 (D-037): branch puramente aditivo para o namespace de key de guest upload
src/runtime/aws/handlers/upload-finalizer-handler.ts:10:// (`tenant/.../subject/...`) - nunca sobrepõe o formato item-anchored acima
src/runtime/aws/handlers/upload-finalizer-handler.ts:11:// (`tenant/.../item/...`), que continua resolvido pelo parser/fluxo originais sem nenhuma
src/runtime/aws/handlers/upload-finalizer-handler.ts-12-// mudança de comportamento.
src/runtime/aws/handlers/upload-finalizer-handler.ts:13:import { parseSubmissionQuarantineKey } from "../../../modules/subject/domain/submission-quarantine-key.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-14-import { finalizeSubmissionUpload } from "../../../workers/submission-finalizer/finalizer.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-15-import { buildSubjectWorkerDeps } from "../composition/subject.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-16-import { runWithContext } from "../../../shared/observability/context.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-17-import { SecureLogger } from "../../../shared/observability/logger.js";
src/runtime/aws/handlers/upload-finalizer-handler.ts-18-
src/runtime/aws/handlers/upload-finalizer-handler.ts-19-const client = createDocumentClient();
--
src/runtime/aws/handlers/upload-finalizer-handler.ts-47-          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers/upload-finalizer-handler.ts-48-          return;
src/runtime/aws/handlers/upload-finalizer-handler.ts-49-        }
src/runtime/aws/handlers/upload-finalizer-handler.ts-50-
src/runtime/aws/handlers/upload-finalizer-handler.ts-51-        const parsed = parseQuarantineKey(detail.object.key);
src/runtime/aws/handlers/upload-finalizer-handler.ts-52-        if (parsed) {
src/runtime/aws/handlers/upload-finalizer-handler.ts:53:          await runWithContext({ correlationId: randomUUID(), tenantId: parsed.tenantId }, async () => {
src/runtime/aws/handlers/upload-finalizer-handler.ts-54-            const outcome = await finalizeUpload(deps, {
src/runtime/aws/handlers/upload-finalizer-handler.ts:55:              tenantId: parsed.tenantId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-56-              itemId: parsed.itemId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-57-              documentId: parsed.documentId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-58-              object: { bucket: detail.bucket.name, key: detail.object.key, versionId: detail.object["version-id"]! },
src/runtime/aws/handlers/upload-finalizer-handler.ts-59-            });
src/runtime/aws/handlers/upload-finalizer-handler.ts-60-            logger.info("upload-finalizer outcome", { documentId: parsed.documentId, outcome });
src/runtime/aws/handlers/upload-finalizer-handler.ts-61-          });
src/runtime/aws/handlers/upload-finalizer-handler.ts-62-          return;
src/runtime/aws/handlers/upload-finalizer-handler.ts-63-        }
src/runtime/aws/handlers/upload-finalizer-handler.ts-64-
src/runtime/aws/handlers/upload-finalizer-handler.ts-65-        // M10: não é uma key item-anchored (M6) - tenta o namespace de guest submission antes
src/runtime/aws/handlers/upload-finalizer-handler.ts-66-        // de desistir. Os dois formatos nunca colidem (segmentos "item" vs "subject").
src/runtime/aws/handlers/upload-finalizer-handler.ts:67:        const parsedSubmission = parseSubmissionQuarantineKey(detail.object.key);
src/runtime/aws/handlers/upload-finalizer-handler.ts-68-        if (!parsedSubmission) {
src/runtime/aws/handlers/upload-finalizer-handler.ts-69-          // A key this handler doesn't recognize (never produced by either reserveUpload flow)
src/runtime/aws/handlers/upload-finalizer-handler.ts-70-          // is not a retryable failure - it can never resolve on retry. Log and drop, never DLQ-loop.
src/runtime/aws/handlers/upload-finalizer-handler.ts:71:          logger.error("upload-finalizer unrecognized key shape", { key: detail.object.key });
src/runtime/aws/handlers/upload-finalizer-handler.ts-72-          return;
src/runtime/aws/handlers/upload-finalizer-handler.ts-73-        }
src/runtime/aws/handlers/upload-finalizer-handler.ts-74-
src/runtime/aws/handlers/upload-finalizer-handler.ts:75:        await runWithContext({ correlationId: randomUUID(), tenantId: parsedSubmission.tenantId }, async () => {
src/runtime/aws/handlers/upload-finalizer-handler.ts-76-          const outcome = await finalizeSubmissionUpload(submissionDeps, {
src/runtime/aws/handlers/upload-finalizer-handler.ts:77:            tenantId: parsedSubmission.tenantId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-78-            subjectId: parsedSubmission.subjectId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-79-            assignmentId: parsedSubmission.assignmentId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-80-            submissionId: parsedSubmission.submissionId,
src/runtime/aws/handlers/upload-finalizer-handler.ts-81-            object: { bucket: detail.bucket.name, key: detail.object.key, versionId: detail.object["version-id"]! },
src/runtime/aws/handlers/upload-finalizer-handler.ts-82-          });
src/runtime/aws/handlers/upload-finalizer-handler.ts-83-          logger.info("upload-finalizer submission outcome", { submissionId: parsedSubmission.submissionId, outcome });
--
src/workers/malware-result\result-processor.ts-4-import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
src/workers/malware-result\result-processor.ts-5-import { advanceAfterEvidence } from "../../modules/document/application/advance-after-evidence.js";
src/workers/malware-result\result-processor.ts-6-import { documentKey, type Document } from "../../modules/document/domain/document.js";
src/workers/malware-result\result-processor.ts-7-import type { GuardDutyScanStatus } from "../../modules/document/domain/malware-scan-result.js";
src/workers/malware-result\result-processor.ts-8-import type { DocumentStore } from "../../modules/document/ports/document-store.js";
src/workers/malware-result\result-processor.ts-9-import type { DocumentObjectStore } from "../../modules/document/ports/document-object-store.js";
src/workers/malware-result\result-processor.ts:10:import { tryTenantBusinessMutation } from "../../shared/tenant-lifecycle/tenant-business-mutation.js";
src/workers/malware-result\result-processor.ts-11-
src/workers/malware-result\result-processor.ts-12-export interface MalwareResultInput {
src/workers/malware-result\result-processor.ts:13:  tenantId: string;
src/workers/malware-result\result-processor.ts-14-  itemId: string;
src/workers/malware-result\result-processor.ts-15-  documentId: string;
src/workers/malware-result\result-processor.ts-16-  object: { bucket: string; key: string; versionId: string };
src/workers/malware-result\result-processor.ts-17-  status: GuardDutyScanStatus;
src/workers/malware-result\result-processor.ts-18-  scanResultId: string;
src/workers/malware-result\result-processor.ts-19-}
--
src/workers/malware-result\result-processor.ts-23-  objects: DocumentObjectStore;
src/workers/malware-result\result-processor.ts-24-  tableName: string;
src/workers/malware-result\result-processor.ts-25-  cleanBucket: string;
src/workers/malware-result\result-processor.ts-26-  now?: () => string;
src/workers/malware-result\result-processor.ts-27-}
src/workers/malware-result\result-processor.ts-28-
src/workers/malware-result\result-processor.ts:29:export type MalwareResultOutcome = "APPLIED" | "IGNORED_UNKNOWN_DOCUMENT" | "IGNORED_WRONG_OBJECT" | "IGNORED_DUPLICATE_SCAN" | "IGNORED_STALE" | "IGNORED_TENANT_NOT_ACTIVE";
src/workers/malware-result\result-processor.ts-30-
src/workers/malware-result\result-processor.ts-31-const MAX_OCC_RETRIES = 10;
src/workers/malware-result\result-processor.ts-32-
src/workers/malware-result\result-processor.ts-33-/**
src/workers/malware-result\result-processor.ts-34- * Real bug found via Camada 3 exercise (2026-08-22), symmetric to the one fixed in
src/workers/malware-result\result-processor.ts-35- * finalizer.ts: persisting this worker's OWN evidence used to give up on the FIRST OCC
--
src/workers/malware-result\result-processor.ts-37- * The much more common case is UploadFinalizerWorker concurrently persisting its own half of
src/workers/malware-result\result-processor.ts-38- * the evidence at the same moment - retryable, not real staleness. Fixed with the same bounded
src/workers/malware-result\result-processor.ts-39- * read-decide-write retry loop advanceAfterEvidence already uses.
src/workers/malware-result\result-processor.ts-40- */
src/workers/malware-result\result-processor.ts-41-export async function processMalwareResult(deps: MalwareResultDeps, input: MalwareResultInput): Promise<MalwareResultOutcome> {
src/workers/malware-result\result-processor.ts-42-  const now = deps.now ?? (() => new Date().toISOString());
src/workers/malware-result\result-processor.ts:43:  const key = documentKey(input.tenantId, input.itemId, input.documentId);
src/workers/malware-result\result-processor.ts-44-
src/workers/malware-result\result-processor.ts-45-  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
src/workers/malware-result\result-processor.ts-46-    const doc = await deps.store.get<Document>(key, true);
src/workers/malware-result\result-processor.ts-47-    if (!doc) return "IGNORED_UNKNOWN_DOCUMENT";
src/workers/malware-result\result-processor.ts-48-
src/workers/malware-result\result-processor.ts-49-    // Fail-closed: never apply a finding to a different bucket/key than the one this document
--
src/workers/malware-result\result-processor.ts-58-
src/workers/malware-result\result-processor.ts-59-    const malwareEvidence = { object: input.object, status: input.status, scanResultId: input.scanResultId, observedAt: now() };
src/workers/malware-result\result-processor.ts-60-
src/workers/malware-result\result-processor.ts-61-    const evidenceResult = await tryTenantBusinessMutation({
src/workers/malware-result\result-processor.ts-62-      store: deps.store,
src/workers/malware-result\result-processor.ts-63-      tableName: deps.tableName,
src/workers/malware-result\result-processor.ts:64:      tenantId: input.tenantId,
src/workers/malware-result\result-processor.ts:65:      entries: [{ Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: doc.version, set: { malwareEvidence } }) }],
src/workers/malware-result\result-processor.ts-66-    });
src/workers/malware-result\result-processor.ts-67-    if (!evidenceResult.ok) {
src/workers/malware-result\result-processor.ts-68-      if (evidenceResult.reason === "OCC_CONFLICT") continue; // concurrent evidence write raced us - re-read fresh and retry.
src/workers/malware-result\result-processor.ts:69:      return "IGNORED_TENANT_NOT_ACTIVE";
src/workers/malware-result\result-processor.ts-70-    }
src/workers/malware-result\result-processor.ts-71-
src/workers/malware-result\result-processor.ts-72-    const advanceOutcome = await advanceAfterEvidence(
src/workers/malware-result\result-processor.ts-73-      { store: deps.store, objects: deps.objects, tableName: deps.tableName, cleanBucket: deps.cleanBucket },
src/workers/malware-result\result-processor.ts:74:      { tenantId: input.tenantId, itemId: input.itemId, documentId: input.documentId, expectedObject: input.object },
src/workers/malware-result\result-processor.ts-75-    );
src/workers/malware-result\result-processor.ts:76:    if (advanceOutcome === "IGNORED_TENANT_NOT_ACTIVE") return "IGNORED_TENANT_NOT_ACTIVE";
src/workers/malware-result\result-processor.ts-77-    return "APPLIED";
src/workers/malware-result\result-processor.ts-78-  }
src/workers/malware-result\result-processor.ts-79-
src/workers/malware-result\result-processor.ts-80-  throw new Error(`processMalwareResult exhausted retries for document ${input.documentId} under contention.`);
src/workers/malware-result\result-processor.ts-81-}
--
src/workers/upload-finalizer\finalizer.ts-4-import { validateObservedUpload } from "../../modules/document/application/upload-validation.js";
src/workers/upload-finalizer\finalizer.ts-5-import { advanceAfterEvidence } from "../../modules/document/application/advance-after-evidence.js";
src/workers/upload-finalizer\finalizer.ts-6-import { documentKey, type Document } from "../../modules/document/domain/document.js";
src/workers/upload-finalizer\finalizer.ts-7-import type { DocumentStore } from "../../modules/document/ports/document-store.js";
src/workers/upload-finalizer\finalizer.ts-8-import type { DocumentObjectStore } from "../../modules/document/ports/document-object-store.js";
src/workers/upload-finalizer\finalizer.ts-9-import type { PdfParser } from "../../modules/document/ports/pdf-parser.js";
src/workers/upload-finalizer\finalizer.ts:10:import { tryTenantBusinessMutation } from "../../shared/tenant-lifecycle/tenant-business-mutation.js";
src/workers/upload-finalizer\finalizer.ts-11-
src/workers/upload-finalizer\finalizer.ts-12-export interface FinalizeUploadInput {
src/workers/upload-finalizer\finalizer.ts:13:  tenantId: string;
src/workers/upload-finalizer\finalizer.ts-14-  itemId: string;
src/workers/upload-finalizer\finalizer.ts-15-  documentId: string;
src/workers/upload-finalizer\finalizer.ts-16-  object: { bucket: string; key: string; versionId: string };
src/workers/upload-finalizer\finalizer.ts-17-}
src/workers/upload-finalizer\finalizer.ts-18-
src/workers/upload-finalizer\finalizer.ts-19-export interface FinalizeUploadDeps {
--
src/workers/upload-finalizer\finalizer.ts-22-  parser: PdfParser;
src/workers/upload-finalizer\finalizer.ts-23-  tableName: string;
src/workers/upload-finalizer\finalizer.ts-24-  cleanBucket: string;
src/workers/upload-finalizer\finalizer.ts-25-  now?: () => string;
src/workers/upload-finalizer\finalizer.ts-26-}
src/workers/upload-finalizer\finalizer.ts-27-
src/workers/upload-finalizer\finalizer.ts:28:export type FinalizeOutcome = "CONFIRMED" | "REJECTED_INVALID" | "IGNORED_UNKNOWN_SLOT" | "IGNORED_STALE" | "IGNORED_TENANT_NOT_ACTIVE";
src/workers/upload-finalizer\finalizer.ts-29-
src/workers/upload-finalizer\finalizer.ts-30-const MAX_OCC_RETRIES = 10;
src/workers/upload-finalizer\finalizer.ts-31-
src/workers/upload-finalizer\finalizer.ts-32-/**
src/workers/upload-finalizer\finalizer.ts-33- * Fail-closed at every branch (M6 design §3.2: "Eventos sem slot, com identidade divergente ou
src/workers/upload-finalizer\finalizer.ts-34- * versão inesperada são fail-closed"). Never confirms a document based on a mismatched bucket/
--
src/workers/upload-finalizer\finalizer.ts-44- * an otherwise-clean document in PENDING_UPLOAD forever (confirmed via a real EventBridge-fed
src/workers/upload-finalizer\finalizer.ts-45- * GuardDuty scan landing a few seconds before this worker's own read-modify-write finished).
src/workers/upload-finalizer\finalizer.ts-46- * Fixed with the same bounded read-decide-write retry loop advanceAfterEvidence already uses.
src/workers/upload-finalizer\finalizer.ts-47- */
src/workers/upload-finalizer\finalizer.ts-48-export async function finalizeUpload(deps: FinalizeUploadDeps, input: FinalizeUploadInput): Promise<FinalizeOutcome> {
src/workers/upload-finalizer\finalizer.ts-49-  const now = deps.now ?? (() => new Date().toISOString());
src/workers/upload-finalizer\finalizer.ts:50:  const key = documentKey(input.tenantId, input.itemId, input.documentId);
src/workers/upload-finalizer\finalizer.ts-51-
src/workers/upload-finalizer\finalizer.ts-52-  for (let attempt = 0; attempt < MAX_OCC_RETRIES; attempt++) {
src/workers/upload-finalizer\finalizer.ts-53-    const doc = await deps.store.get<Document>(key, true);
src/workers/upload-finalizer\finalizer.ts-54-    if (!doc) return "IGNORED_UNKNOWN_SLOT";
src/workers/upload-finalizer\finalizer.ts-55-    if (doc.quarantineObject.key !== input.object.key || doc.quarantineObject.bucket !== input.object.bucket) return "IGNORED_UNKNOWN_SLOT";
src/workers/upload-finalizer\finalizer.ts-56-    if (doc.status !== "PENDING_UPLOAD" && doc.status !== "SCANNING") return "IGNORED_STALE";
--
src/workers/upload-finalizer\finalizer.ts-77-
src/workers/upload-finalizer\finalizer.ts-78-    // W3-07 (Round F finding): uploadEvidence is itself a TenantBusinessMutation, not just the
src/workers/upload-finalizer\finalizer.ts-79-    // final CLEAN promotion advanceAfterEvidence() owns.
src/workers/upload-finalizer\finalizer.ts-80-    const evidenceResult = await tryTenantBusinessMutation({
src/workers/upload-finalizer\finalizer.ts-81-      store: deps.store,
src/workers/upload-finalizer\finalizer.ts-82-      tableName: deps.tableName,
src/workers/upload-finalizer\finalizer.ts:83:      tenantId: input.tenantId,
src/workers/upload-finalizer\finalizer.ts-84-      entries: [
src/workers/upload-finalizer\finalizer.ts-85-        {
src/workers/upload-finalizer\finalizer.ts-86-          Update: buildVersionedUpdate({
src/workers/upload-finalizer\finalizer.ts-87-            tableName: deps.tableName,
src/workers/upload-finalizer\finalizer.ts-88-            key,
src/workers/upload-finalizer\finalizer.ts:89:            tenantId: input.tenantId,
src/workers/upload-finalizer\finalizer.ts-90-            expectedVersion: doc.version,
src/workers/upload-finalizer\finalizer.ts-91-            set: { status: "SCANNING", uploadEvidence },
src/workers/upload-finalizer\finalizer.ts-92-          }),
src/workers/upload-finalizer\finalizer.ts-93-        },
src/workers/upload-finalizer\finalizer.ts-94-      ],
src/workers/upload-finalizer\finalizer.ts-95-    });
src/workers/upload-finalizer\finalizer.ts-96-    if (!evidenceResult.ok) {
src/workers/upload-finalizer\finalizer.ts-97-      if (evidenceResult.reason === "OCC_CONFLICT") continue; // concurrent evidence write raced us - re-read fresh and retry.
src/workers/upload-finalizer\finalizer.ts:98:      return "IGNORED_TENANT_NOT_ACTIVE";
src/workers/upload-finalizer\finalizer.ts-99-    }
src/workers/upload-finalizer\finalizer.ts-100-
src/workers/upload-finalizer\finalizer.ts-101-    const outcome = await advanceAfterEvidence(
src/workers/upload-finalizer\finalizer.ts-102-      { store: deps.store, objects: deps.objects, tableName: deps.tableName, cleanBucket: deps.cleanBucket },
src/workers/upload-finalizer\finalizer.ts:103:      { tenantId: input.tenantId, itemId: input.itemId, documentId: input.documentId, expectedObject: input.object },
src/workers/upload-finalizer\finalizer.ts-104-    );
src/workers/upload-finalizer\finalizer.ts-105-    if (outcome === "REJECTED") return "REJECTED_INVALID";
src/workers/upload-finalizer\finalizer.ts:106:    if (outcome === "IGNORED_TENANT_NOT_ACTIVE") return "IGNORED_TENANT_NOT_ACTIVE";
src/workers/upload-finalizer\finalizer.ts-107-    return "CONFIRMED";
src/workers/upload-finalizer\finalizer.ts-108-  }
src/workers/upload-finalizer\finalizer.ts-109-
src/workers/upload-finalizer\finalizer.ts-110-  throw new Error(`finalizeUpload exhausted retries for document ${input.documentId} under contention.`);
src/workers/upload-finalizer\finalizer.ts-111-}
src\workers\reminder-producer\producer.ts:225:            // (destination=SQS_REMINDER_DISPATCH_V1) in the SAME transaction as the claim;
src\workers\reminder-producer\producer.ts:226:            // DispatchOutboxRelay (DynamoDB Streams) publishes it to SQS, a sweeper recovers
src\workers\reminder-producer\producer.ts:243:            appendToTransaction(outboxEntries, deps.tableName, event, "SQS_REMINDER_DISPATCH_V1");
src\runtime\aws\composition\reminder.ts:61: * `outbox-sweeper-handler.ts`'s own "one shared privileged role, router keyed by destination"
src\runtime\aws\composition\reminder.ts:63: * second destination). M11 (D-042) adds `importCommitQueueUrl` as a THIRD optional sender on
src\runtime\aws\composition\reminder.ts:74:  // AWAITING_MAPPING->PARSING transition dispatches this destination in the same TWI as the
src\runtime\aws\composition\reminder.ts:76:  // payload shape as SQS_IMPORT_COMMIT_V1 (tenantId embedded, no extra envelope wrapping).
src\runtime\aws\composition\reminder.ts:89:  // BLOCKER-B (reminder-delivery-pipeline.md §4): unlike DispatchCommand, this destination's
src\runtime\aws\composition\reminder.ts:108:      SQS_REMINDER_DISPATCH_V1: send(queueUrl),
src\runtime\aws\composition\reminder.ts:109:      ...(chasingQueueUrl ? { SQS_DOCUMENT_CHASING_DISPATCH_V1: send(chasingQueueUrl) } : {}),
src\runtime\aws\composition\reminder.ts:110:      ...(importCommitQueueUrl ? { SQS_IMPORT_COMMIT_V1: send(importCommitQueueUrl) } : {}),
src\runtime\aws\composition\reminder.ts:111:      ...(importParseQueueUrl ? { SQS_IMPORT_PARSE_V1: send(importParseQueueUrl) } : {}),
src\runtime\aws\composition\reminder.ts:112:      ...(materializationTriggerQueueUrl ? { SQS_REMINDER_MATERIALIZATION_TRIGGER_V1: sendMaterializationTrigger(materializationTriggerQueueUrl) } : {}),
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:1:/** Pure(ish) per-batch processing logic for DispatchOutboxRelay, split out of
src\runtime\aws\composition\notification.ts:35:  // `destination` rather than being hardcoded to a single queue.
src\runtime\aws\composition\notification.ts:38:      SQS_NOTIFICATION_EMAIL_V1: async (payload: Record<string, unknown>, correlationId: string) => {
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:1:/** Real handler for DispatchOutboxRelay (DynamoDB Streams NEW_IMAGE), replacing the 501
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:16:// M10 cluster 4 (D-039/D-046/D-048): second destination on the SAME relay Lambda/DynamoDB
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:19:// M11 (D-042): third destination, same reasoning.
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:21:// BLOCKER-B (reminder-delivery-pipeline.md §4): fourth destination, same reasoning - the
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:22:// generic "no destination -> EventBridge" path this design doc originally assumed was never
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:24:// OutboxDestination comment), so this is the only real delivery mechanism available.
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:26:// D-192 slice 9: fifth destination, same reasoning.
src\runtime\aws\handlers\import-parse-handler.ts:31:/** D-192 slice 9: the SQS_IMPORT_PARSE_V1 envelope (`import-service.ts#submitImportMapping`'s
src\runtime\aws\handlers\import-parse-handler.ts:56:            logger.info("import-parse outcome (SQS_IMPORT_PARSE_V1 trigger)", { jobId: message.jobId, outcome });
src\runtime\aws\handlers\import-parse-handler.ts:63:          logger.error("import-parse malformed message (neither S3 event nor SQS_IMPORT_PARSE_V1 trigger)", { messageId: record.messageId });
src\modules\import\application\import-service.ts:52:/** Envelope da mensagem SQS_IMPORT_COMMIT_V1 - schemas/queues/import-commit.v1.json. Mesmo
src\modules\import\application\import-service.ts:319:   * transition from `buildImportJobClaim()`, dispatching `SQS_IMPORT_PARSE_V1` in the SAME
src\modules\import\application\import-service.ts:392:      appendToTransaction(entries, this.tableName, event, "SQS_IMPORT_PARSE_V1");
src\modules\import\application\import-service.ts:460:    appendToTransaction(entries, this.tableName, event, "SQS_IMPORT_COMMIT_V1");
src\workers\reminder-materialization-trigger\trigger.ts:190: * SQS_REMINDER_MATERIALIZATION_TRIGGER_V1 outbox sender (composition/reminder.ts), which
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:2: * generalized relay logic as DispatchOutboxRelay - see
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:4: * `destination` instead of being hardcoded to reminder dispatch). This handler only wires
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:5: * the `senders` map for `SQS_NOTIFICATION_EMAIL_V1` - it never touches
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:6: * `SQS_REMINDER_DISPATCH_V1` records (routing exclusivity is enforced in publishOne itself,
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:7: * this handler's senders map simply doesn't recognize that destination). Per-record
src\runtime\aws\handlers\outbox-sweeper-handler.ts:2: * BOTH SQS_REMINDER_DISPATCH_V1 (M3.5) and SQS_NOTIFICATION_EMAIL_V1 (M4) in a single
src\runtime\aws\handlers\outbox-sweeper-handler.ts:5: * destination", not a second sweeper querying the same global GSI6 partition. Recovers
src\runtime\aws\handlers\outbox-sweeper-handler.ts:7: * destination - m3.5-runtime-design.md §"Decisão central". */
src\runtime\aws\handlers\outbox-sweeper-handler.ts:20:// M10 cluster 4 (D-039/D-046/D-048): third destination on this SAME shared privileged
src\runtime\aws\handlers\outbox-sweeper-handler.ts:21:// sweeper role - same "router keyed by destination" pattern §7.4 already established for
src\runtime\aws\handlers\outbox-sweeper-handler.ts:22:// SQS_NOTIFICATION_EMAIL_V1, never a second sweeper querying the same global GSI6 partition.
src\runtime\aws\handlers\outbox-sweeper-handler.ts:24:// M11 (D-042): fourth destination, same reasoning.
src\runtime\aws\handlers\outbox-sweeper-handler.ts:26:// BLOCKER-B (reminder-delivery-pipeline.md §4): fifth destination, same reasoning.
src\runtime\aws\handlers\outbox-sweeper-handler.ts:28:// D-192 slice 9: sixth destination, same reasoning.
src\runtime\aws\handlers\outbox-sweeper-handler.ts:49:// BLOCKER-B: unlike the other destinations' payloads, this one is the bare domain event
src\runtime\aws\handlers\outbox-sweeper-handler.ts:66:    SQS_REMINDER_DISPATCH_V1: send(reminderDispatchQueueUrl),
src\runtime\aws\handlers\outbox-sweeper-handler.ts:67:    SQS_NOTIFICATION_EMAIL_V1: send(emailDeliverQueueUrl),
src\runtime\aws\handlers\outbox-sweeper-handler.ts:68:    SQS_DOCUMENT_CHASING_DISPATCH_V1: send(chasingDispatchQueueUrl),
src\runtime\aws\handlers\outbox-sweeper-handler.ts:69:    SQS_IMPORT_COMMIT_V1: send(importCommitQueueUrl),
src\runtime\aws\handlers\outbox-sweeper-handler.ts:70:    SQS_IMPORT_PARSE_V1: send(importParseQueueUrl),
src\runtime\aws\handlers\outbox-sweeper-handler.ts:71:    SQS_REMINDER_MATERIALIZATION_TRIGGER_V1: sendMaterializationTrigger(materializationTriggerQueueUrl),
src\workers\dispatch-outbox-relay\relay.ts:2: * DispatchOutboxRelay + OutboxSweeperReminderDispatch core logic (M3.5,
src\workers\dispatch-outbox-relay\relay.ts:12: * generalizes this from a single hardcoded destination (SQS_REMINDER_DISPATCH_V1) to an
src\workers\dispatch-outbox-relay\relay.ts:13: * explicit router keyed by `destination` - a second sweeper querying the SAME global GSI6
src\workers\dispatch-outbox-relay\relay.ts:14: * partition (`RECON#OUTBOX#PENDING`) for a second destination would be redundant; the
src\workers\dispatch-outbox-relay\relay.ts:19:import { outboxRecordCorrelationId, type OutboxRecord, type OutboxDestination } from "../../shared/outbox/outbox.js";
src\workers\dispatch-outbox-relay\relay.ts:22:export const SQS_REMINDER_DISPATCH_V1: OutboxDestination = "SQS_REMINDER_DISPATCH_V1";
src\workers\dispatch-outbox-relay\relay.ts:23:export const SQS_NOTIFICATION_EMAIL_V1: OutboxDestination = "SQS_NOTIFICATION_EMAIL_V1";
src\workers\dispatch-outbox-relay\relay.ts:25:/** One sender per recognized destination - a record whose `destination` has no entry here
src\workers\dispatch-outbox-relay\relay.ts:27: * discipline as the single-destination M3.5 version. `tenantId`/`eventType` (BLOCKER-B,
src\workers\dispatch-outbox-relay\relay.ts:34:  Record<OutboxDestination, (payload: Record<string, unknown>, correlationId: string, tenantId: string, eventType: string) => Promise<void>>
src\workers\dispatch-outbox-relay\relay.ts:56: * (wrong destination / already published / lease contention) - only for genuinely
src\workers\dispatch-outbox-relay\relay.ts:61:  // sweeper NEVER touches a record it doesn't own the destination for - OutboxPublisher
src\workers\dispatch-outbox-relay\relay.ts:62:  // (generic, EventBridge) owns everything else, including records with no `destination`.
src\workers\dispatch-outbox-relay\relay.ts:63:  const send = record.destination ? deps.senders[record.destination] : undefined;
src\workers\dispatch-outbox-relay\relay.ts:108:/** DispatchOutboxRelay / NotificationEmailOutboxRelay entrypoint logic: one call per
src\workers\dispatch-outbox-relay\relay.ts:111: * function's - it stays destination-agnostic. */
src\workers\dispatch-outbox-relay\relay.ts:122:  /** Which destinations to sweep this run - the sweeper queries GSI6 once per destination
src\workers\dispatch-outbox-relay\relay.ts:123:   * (same global partition, `RECON#OUTBOX#PENDING`, filtered by `destination` server-side by
src\workers\dispatch-outbox-relay\relay.ts:124:   * the store). Defaults to every destination this deps' `senders` map recognizes. */
src\workers\dispatch-outbox-relay\relay.ts:125:  destinations?: OutboxDestination[];
src\workers\dispatch-outbox-relay\relay.ts:137: * "nenhum backoff próprio"). One sweeper instance can cover multiple destinations (M4
src\workers\dispatch-outbox-relay\relay.ts:139: * roteador explícito por destination"). */
src\workers\dispatch-outbox-relay\relay.ts:143:  const destinations = deps.destinations ?? (Object.keys(deps.senders) as OutboxDestination[]);
src\workers\dispatch-outbox-relay\relay.ts:150:  for (const destination of destinations) {
src\workers\dispatch-outbox-relay\relay.ts:152:      destination,
src\modules\subject\application\document-chasing-producer.ts:108:  appendToTransaction(outboxEntries, deps.tableName, event, "SQS_DOCUMENT_CHASING_DISPATCH_V1");
src\modules\document\ports\document-object-store.ts:14:  /** Copies the exact source version to a new key in the destination bucket; returns the new
src\modules\document\ports\document-object-store.ts:17:  copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference>;
src\modules\document\persistence\s3-document-object-store.ts:27:  async copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference> {
src\modules\document\persistence\s3-document-object-store.ts:39:        Bucket: destinationBucket,
src\modules\document\persistence\s3-document-object-store.ts:40:        Key: destinationKey,
src\modules\document\persistence\s3-document-object-store.ts:45:    return { bucket: destinationBucket, key: destinationKey, versionId: result.VersionId ?? "" };
src\modules\document\application\advance-after-evidence.ts:125:      // attempts, but re-copying the same source version to the same destination key is).
src\modules\reminder\application\reminder-policy-service.ts:268:    appendToTransaction(entries, this.tableName, event, "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1");
src\shared\outbox\relay-store.ts:2: * Narrow port for DispatchOutboxRelay and OutboxSweeperReminderDispatch (M3.5,
src\shared\outbox\relay-store.ts:7:import type { OutboxDestination, OutboxRecord } from "./outbox.js";
src\shared\outbox\relay-store.ts:16:  /** Sweeper only: GSI6PK=RECON#OUTBOX#PENDING, filtered by `destination` and age. */
src\shared\outbox\relay-store.ts:17:  listPendingReminderDispatch(input: { destination: OutboxDestination; olderThan: string; pageSize?: number }): Promise<OutboxRecord[]>;
src\shared\outbox\outbox.ts:16: * `DispatchOutboxRelay` (new, -> SQS ReminderDispatchQueue) never both claim the same
src\shared\outbox\outbox.ts:19: * `destination` it doesn't recognize as its own default, never process-by-omission.
src\shared\outbox\outbox.ts:21:export type OutboxDestination =
src\shared\outbox\outbox.ts:22:  | "SQS_REMINDER_DISPATCH_V1"
src\shared\outbox\outbox.ts:23:  | "SQS_NOTIFICATION_EMAIL_V1"
src\shared\outbox\outbox.ts:24:  | "SQS_DOCUMENT_CHASING_DISPATCH_V1"
src\shared\outbox\outbox.ts:25:  | "SQS_IMPORT_COMMIT_V1"
src\shared\outbox\outbox.ts:27:   * outbox record - the "generic EventBridge path" every other destination's comment
src\shared\outbox\outbox.ts:28:   * describes as the default for an unset `destination` was never actually implemented in
src\shared\outbox\outbox.ts:29:   * this codebase (no PutEventsCommand call exists anywhere) - confirmed during BLOCKER-B
src\shared\outbox\outbox.ts:31:   * destination-routed through DispatchOutboxRelay; this destination follows the same
src\shared\outbox\outbox.ts:33:  | "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1"
src\shared\outbox\outbox.ts:41:  | "SQS_IMPORT_PARSE_V1";
src\shared\outbox\outbox.ts:64:  destination?: OutboxDestination;
src\shared\outbox\outbox.ts:80:export function buildOutboxRecord(event: DomainEvent, destination?: OutboxDestination): OutboxRecord {
src\shared\outbox\outbox.ts:101:    ...(destination ? { destination } : {}),
src\shared\outbox\outbox.ts:134:  destination?: OutboxDestination,
src\shared\outbox\outbox.ts:136:  const record = buildOutboxRecord(event, destination);
src\modules\expiration\application\expiration-service.ts:220:    appendToTransaction(entries, this.tableName, createdEvent, "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1");
src\modules\expiration\application\expiration-service.ts:342:      appendToTransaction(entries, this.tableName, event, "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1");
src\modules\expiration\application\expiration-service.ts:546:    appendToTransaction(entries, this.tableName, event, "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1");
src\modules\expiration\application\expiration-service.ts:765:    appendToTransaction(entries, this.tableName, event, "SQS_REMINDER_MATERIALIZATION_TRIGGER_V1");
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:2: * DispatchOutboxRelay and OutboxSweeperReminderDispatch composition roots - the two
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:57:  async listPendingReminderDispatch(input: { destination: string; olderThan: string; pageSize?: number }): Promise<OutboxRecord[]> {
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:68:            FilterExpression: "destination = :destination",
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:72:              ":destination": input.destination,
src\modules\notification\domain\notification-attempt.ts:38:  destinationHash: string;
src\modules\notification\domain\notification-attempt.ts:56:/** Attempt states in which a real delivery attempt could have reached the destination -
src\modules\notification\application\notification-router-workflow.ts:307:      destinationHash: "",
src\modules\notification\application\notification-router-workflow.ts:356: * shared/outbox/outbox.ts's buildOutboxRecord produces, with `destination:
src\modules\notification\application\notification-router-workflow.ts:357: * "SQS_NOTIFICATION_EMAIL_V1"` (fechamento §7 do design) - inlined here (not via
src\modules\notification\application\notification-router-workflow.ts:369:    // in PK) for a different destination's sender to use - added here too for the same
src\modules\notification\application\notification-router-workflow.ts:371:    // destination's own payload already self-describes its tenant.
src\modules\notification\application\notification-router-workflow.ts:407:    destination: "SQS_NOTIFICATION_EMAIL_V1",
src/modules/document-archive/http\document-archive-handlers.ts-324-  req: HttpRequest<{ expectedVersion: number; documentId: string; versionId: string }>,
src/modules/document-archive/http\document-archive-handlers.ts-325-): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts-326-  return withErrorMapping(async () => {
src/modules/document-archive/http\document-archive-handlers.ts-327-    const subjectId = requireSubjectId(req);
src/modules/document-archive/http\document-archive-handlers.ts-328-    const requirementId = requireRequirementId(req);
src/modules/document-archive/http\document-archive-handlers.ts-329-    if (!req.body) throw new ValidationError("Missing request body.");
src/modules/document-archive/http\document-archive-handlers.ts-330-    validateAgainstSchema(REQUIREMENT_LINK_EVIDENCE_SCHEMA_ID, req.body);
src/modules/document-archive/http\document-archive-handlers.ts-331-    const context = await resolve(deps, req);
src/modules/document-archive/http\document-archive-handlers.ts:332:    const requirement = await deps.documentArchive.linkEvidence(context, subjectId, requirementId, req.body.expectedVersion, req.body.documentId, req.body.versionId);
src/modules/document-archive/http\document-archive-handlers.ts-333-    return { statusCode: 200, body: { requirement } };
src/modules/document-archive/http\document-archive-handlers.ts-334-  });
src/modules/document-archive/http\document-archive-handlers.ts-335-}
src/modules/document-archive/http\document-archive-handlers.ts-336-
src/modules/document-archive/http\document-archive-handlers.ts:337:export async function handleUnlinkEvidence(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts-338-  return withErrorMapping(async () => {
src/modules/document-archive/http\document-archive-handlers.ts-339-    const subjectId = requireSubjectId(req);
src/modules/document-archive/http\document-archive-handlers.ts-340-    const requirementId = requireRequirementId(req);
src/modules/document-archive/http\document-archive-handlers.ts-341-    if (!req.body) throw new ValidationError("Missing request body.");
src/modules/document-archive/http\document-archive-handlers.ts-342-    validateAgainstSchema(REQUIREMENT_UNLINK_EVIDENCE_SCHEMA_ID, req.body);
src/modules/document-archive/http\document-archive-handlers.ts-343-    const context = await resolve(deps, req);
src/modules/document-archive/http\document-archive-handlers.ts:344:    const requirement = await deps.documentArchive.unlinkEvidence(context, subjectId, requirementId, req.body.expectedVersion);
src/modules/document-archive/http\document-archive-handlers.ts-345-    return { statusCode: 200, body: { requirement } };
src/modules/document-archive/http\document-archive-handlers.ts-346-  });
src/modules/document-archive/http\document-archive-handlers.ts-347-}
src/modules/document-archive/http\document-archive-handlers.ts-348-
src/modules/document-archive/http\document-archive-handlers.ts-349-export async function handleDeleteRequirement(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts-350-  return withErrorMapping(async () => {
src/modules/document-archive/http\document-archive-handlers.ts-351-    const subjectId = requireSubjectId(req);
src/modules/document-archive/http\document-archive-handlers.ts-352-    const requirementId = requireRequirementId(req);
--
src/modules/document-archive/application\document-archive-service.ts-3- * machine (`domain/document-version.ts`) against real transactional writes
src/modules/document-archive/application\document-archive-service.ts-4- * (`ports/document-archive-store.ts`). `acceptVersion`'s transaction here only builds the
src/modules/document-archive/application\document-archive-service.ts-5- * Document/DocumentVersion/DocumentVersionEvent/idempotency actions (up to 6 of the up-to-10
src/modules/document-archive/application\document-archive-service.ts-6- * the full design describes); DocumentRequest fencing (items 7-10) is added when that module
src/modules/document-archive/application\document-archive-service.ts-7- * exists. Out of scope for this increment: DocumentRequest origin, guest access, recurrence,
src/modules/document-archive/application\document-archive-service.ts-8- * DocumentFile persistence/malware scanning.
src/modules/document-archive/application\document-archive-service.ts-9- *
src/modules/document-archive/application\document-archive-service.ts-10- * D-143 Nucleus 2, entity 1 (Decision 5/D9, D-145): also hosts Requirement CRUD +
src/modules/document-archive/application\document-archive-service.ts:11: * linkEvidence/unlinkEvidence (`createRequirement`.. below) — kept in this same service rather
src/modules/document-archive/application\document-archive-service.ts-12- * than a new class because Requirement's only cross-entity dependency is DocumentVersion, which
src/modules/document-archive/application\document-archive-service.ts-13- * this service already reads via `findVersionById`; a second service class would only add an
src/modules/document-archive/application\document-archive-service.ts-14- * extra composition-root wire for no real separation of concerns at this module's current size.
src/modules/document-archive/application\document-archive-service.ts-15- */
src/modules/document-archive/application\document-archive-service.ts-16-import {
src/modules/document-archive/application\document-archive-service.ts-17-  buildConditionalDelete,
src/modules/document-archive/application\document-archive-service.ts-18-  buildExistenceConditionCheck,
src/modules/document-archive/application\document-archive-service.ts-19-  buildVersionedCreate,
--
src/modules/document-archive/application\document-archive-service.ts-245-  return buildExistenceConditionCheck({
src/modules/document-archive/application\document-archive-service.ts-246-    tableName,
src/modules/document-archive/application\document-archive-service.ts-247-    key: trackedSubjectKeyForFence(tenantId, subjectId),
src/modules/document-archive/application\document-archive-service.ts-248-    extra: { status: SUBJECT_STATUS_ACCEPTING_REQUIREMENTS },
src/modules/document-archive/application\document-archive-service.ts-249-  });
src/modules/document-archive/application\document-archive-service.ts-250-}
src/modules/document-archive/application\document-archive-service.ts-251-
src/modules/document-archive/application\document-archive-service.ts-252-/** Shared by all 4 Requirement mutation sites (D-179/D-185) plus `buildCreateRequirementEntries()`
src/modules/document-archive/application\document-archive-service.ts:253: * — computes the GSI8 pointer fields to `set` (SATISFIED + `evidenceValidUntil`) or the sentinel
src/modules/document-archive/application\document-archive-service.ts-254- * meaning "remove instead" (every other case), keeping `deriveRequirementMaintenanceDue` the
src/modules/document-archive/application\document-archive-service.ts-255- * single source of truth for eligibility rather than re-deriving it at each call site. Module-level
src/modules/document-archive/application\document-archive-service.ts-256- * for the same "pure planner needs no class instance" reason as the two helpers above. */
src/modules/document-archive/application\document-archive-service.ts:257:function buildRequirementGsi8Fields(status: RequirementStatus, evidenceValidUntil: string | undefined, tenantId: string, requirementId: string): { GSI8PK: string; GSI8SK: string } | Record<string, never> {
src/modules/document-archive/application\document-archive-service.ts:258:  const due = deriveRequirementMaintenanceDue(status, evidenceValidUntil);
src/modules/document-archive/application\document-archive-service.ts-259-  return due ? requirementGsi8Keys({ dueAtIso: due.dueAtIso, tenantId, requirementId }) : {};
src/modules/document-archive/application\document-archive-service.ts-260-}
src/modules/document-archive/application\document-archive-service.ts-261-
src/modules/document-archive/application\document-archive-service.ts-262-/** Input to `buildCreateRequirementEntries()` — the sibling planner to
src/modules/document-archive/application\document-archive-service.ts-263- * `buildCreateDocumentEntries()` above (D-192 §6 slice 3). No externalId/subjectId resolution
src/modules/document-archive/application\document-archive-service.ts-264- * happens here either — same division of labor: the import module resolves references in a
src/modules/document-archive/application\document-archive-service.ts-265- * batched phase before ever calling this pure function. */
src/modules/document-archive/application\document-archive-service.ts-266-export interface BuildCreateRequirementEntriesInput {
--
src/modules/document-archive/application\document-archive-service.ts-925-    }
src/modules/document-archive/application\document-archive-service.ts-926-    if (failed.some((label) => label.kind === "DOCUMENT")) {
src/modules/document-archive/application\document-archive-service.ts-927-      throw new ConflictError("Document already exists.", details);
src/modules/document-archive/application\document-archive-service.ts-928-    }
src/modules/document-archive/application\document-archive-service.ts-929-    throw new ConflictError("The transaction was rejected by a condition check.", details);
src/modules/document-archive/application\document-archive-service.ts-930-  }
src/modules/document-archive/application\document-archive-service.ts-931-
src/modules/document-archive/application\document-archive-service.ts-932-  /** Shared by all 4 Requirement mutation sites (D-179/D-185) — computes the GSI8 pointer fields
src/modules/document-archive/application\document-archive-service.ts:933:   * to `set` (SATISFIED + `evidenceValidUntil`) or the sentinel meaning "remove instead" (every
src/modules/document-archive/application\document-archive-service.ts-934-   * other case), keeping `deriveRequirementMaintenanceDue` the single source of truth for
src/modules/document-archive/application\document-archive-service.ts-935-   * eligibility rather than re-deriving it at each call site. */
src/modules/document-archive/application\document-archive-service.ts:936:  private requirementGsi8Fields(status: RequirementStatus, evidenceValidUntil: string | undefined, tenantId: string, requirementId: string): { GSI8PK: string; GSI8SK: string } | Record<string, never> {
src/modules/document-archive/application\document-archive-service.ts:937:    const due = deriveRequirementMaintenanceDue(status, evidenceValidUntil);
src/modules/document-archive/application\document-archive-service.ts-938-    return due ? requirementGsi8Keys({ dueAtIso: due.dueAtIso, tenantId, requirementId }) : {};
src/modules/document-archive/application\document-archive-service.ts-939-  }
src/modules/document-archive/application\document-archive-service.ts-940-
src/modules/document-archive/application\document-archive-service.ts-941-  async getRequirement(ctx: RequestContext, subjectId: string, requirementId: string): Promise<Requirement> {
src/modules/document-archive/application\document-archive-service.ts-942-    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive/application\document-archive-service.ts-943-    return this.getRequirementUnchecked(ctx.tenant.tenantId, subjectId, requirementId);
src/modules/document-archive/application\document-archive-service.ts-944-  }
src/modules/document-archive/application\document-archive-service.ts-945-
--
src/modules/document-archive/application\document-archive-service.ts-964-    const nextApplicability = input.applicability ?? current.applicability;
src/modules/document-archive/application\document-archive-service.ts-965-    const now = this.now();
src/modules/document-archive/application\document-archive-service.ts-966-    let status = current.status;
src/modules/document-archive/application\document-archive-service.ts-967-    if (nextApplicability !== current.applicability) {
src/modules/document-archive/application\document-archive-service.ts-968-      const evidence = this.cachedEvidenceForDerivation(current);
src/modules/document-archive/application\document-archive-service.ts-969-      status = deriveRequirementStatus(nextApplicability, evidence, new Date(now));
src/modules/document-archive/application\document-archive-service.ts-970-    }
src/modules/document-archive/application\document-archive-service.ts-971-    // Applicability is the only field here that can change `status` (name/notes never do), and
src/modules/document-archive/application\document-archive-service.ts:972:    // `evidenceValidUntil` itself never changes in this call — only whether the GSI8 pointer
src/modules/document-archive/application\document-archive-service.ts-973-    // built from it should exist follows `status`.
src/modules/document-archive/application\document-archive-service.ts:974:    const gsi8Fields = this.requirementGsi8Fields(status, current.evidenceValidUntil, tenantId, requirementId);
src/modules/document-archive/application\document-archive-service.ts-975-    const set: Record<string, unknown> = { applicability: nextApplicability, status, ...requirementGsi1Keys(tenantId, status, now, requirementId), ...gsi8Fields };
src/modules/document-archive/application\document-archive-service.ts-976-    if (input.name !== undefined) set["name"] = input.name;
src/modules/document-archive/application\document-archive-service.ts-977-    if (input.notes !== undefined) set["notes"] = input.notes;
src/modules/document-archive/application\document-archive-service.ts-978-    const update = buildVersionedUpdate({
src/modules/document-archive/application\document-archive-service.ts-979-      tableName: this.tableName,
src/modules/document-archive/application\document-archive-service.ts-980-      key: requirementKey(tenantId, subjectId, requirementId),
src/modules/document-archive/application\document-archive-service.ts-981-      tenantId,
src/modules/document-archive/application\document-archive-service.ts-982-      expectedVersion,
--
src/modules/document-archive/application\document-archive-service.ts-1018-      await executeTenantBusinessMutation({ store: this.store, tableName: this.tableName, tenantId, entries });
src/modules/document-archive/application\document-archive-service.ts-1019-    } catch (err) {
src/modules/document-archive/application\document-archive-service.ts-1020-      this.throwClassifiedCancellation(err, labels, { subjectId, requirementId });
src/modules/document-archive/application\document-archive-service.ts-1021-    }
src/modules/document-archive/application\document-archive-service.ts-1022-    return { ...current, ...set, version: expectedVersion + 1, updatedAt: now } as Requirement;
src/modules/document-archive/application\document-archive-service.ts-1023-  }
src/modules/document-archive/application\document-archive-service.ts-1024-
src/modules/document-archive/application\document-archive-service.ts-1025-  /**
src/modules/document-archive/application\document-archive-service.ts:1026:   * linkEvidence — sets the (singular) `evidenceVersionId` and re-derives `status`
src/modules/document-archive/application\document-archive-service.ts-1027-   * transactionally alongside the GSI1 REQSTATUS update, so a reader querying by status never
src/modules/document-archive/application\document-archive-service.ts-1028-   * observes a Requirement whose `status` attribute is stale relative to its own
src/modules/document-archive/application\document-archive-service.ts-1029-   * `evidenceVersionId` (D-143 Decision 5).
src/modules/document-archive/application\document-archive-service.ts-1030-   *
src/modules/document-archive/application\document-archive-service.ts-1031-   * Takes `documentId` explicitly (see `evidenceDocumentId`'s doc comment on the domain entity
src/modules/document-archive/application\document-archive-service.ts-1032-   * for why this is stored rather than resolved via GSI5 at read time — that attribute pair is
src/modules/document-archive/application\document-archive-service.ts-1033-   * already claimed by the sparse review-queue index and is removed exactly when a version
src/modules/document-archive/application\document-archive-service.ts-1034-   * becomes ACCEPTED).
src/modules/document-archive/application\document-archive-service.ts-1035-   */
src/modules/document-archive/application\document-archive-service.ts:1036:  async linkEvidence(ctx: RequestContext, subjectId: string, requirementId: string, expectedVersion: number, documentId: string, versionId: string): Promise<Requirement> {
src/modules/document-archive/application\document-archive-service.ts-1037-    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive/application\document-archive-service.ts-1038-    const tenantId = ctx.tenant.tenantId;
src/modules/document-archive/application\document-archive-service.ts-1039-    const current = await this.getRequirementUnchecked(tenantId, subjectId, requirementId);
src/modules/document-archive/application\document-archive-service.ts-1040-    const evidenceVersion = await this.findVersionById(tenantId, documentId, versionId);
src/modules/document-archive/application\document-archive-service.ts-1041-    if (!evidenceVersion) throw new NotFoundError("DocumentVersion not found.", { documentId, versionId });
src/modules/document-archive/application\document-archive-service.ts-1042-    const now = this.now();
src/modules/document-archive/application\document-archive-service.ts-1043-    const status = deriveRequirementStatus(current.applicability, evidenceVersion, new Date(now));
src/modules/document-archive/application\document-archive-service.ts-1044-    const set: Record<string, unknown> = {
src/modules/document-archive/application\document-archive-service.ts-1045-      evidenceVersionId: versionId,
src/modules/document-archive/application\document-archive-service.ts-1046-      evidenceDocumentId: documentId,
src/modules/document-archive/application\document-archive-service.ts-1047-      evidenceSeq: evidenceVersion.seq,
src/modules/document-archive/application\document-archive-service.ts:1048:      evidenceState: evidenceVersion.state,
src/modules/document-archive/application\document-archive-service.ts-1049-      status,
src/modules/document-archive/application\document-archive-service.ts-1050-      ...requirementGsi1Keys(tenantId, status, now, requirementId),
src/modules/document-archive/application\document-archive-service.ts-1051-    };
src/modules/document-archive/application\document-archive-service.ts-1052-    // `undefined` is not a valid DynamoDB attribute value (occ.ts's SET-clause builder would
src/modules/document-archive/application\document-archive-service.ts-1053-    // otherwise emit an ExpressionAttributeValue literally equal to `undefined`) — only include
src/modules/document-archive/application\document-archive-service.ts:1054:    // evidenceValidUntil when the evidence version actually carries one (D3: a document without
src/modules/document-archive/application\document-archive-service.ts-1055-    // an expiration date is a legitimate first-class case).
src/modules/document-archive/application\document-archive-service.ts:1056:    if (evidenceVersion.validUntil !== undefined) set["evidenceValidUntil"] = evidenceVersion.validUntil;
src/modules/document-archive/application\document-archive-service.ts-1057-    const gsi8Fields = this.requirementGsi8Fields(status, evidenceVersion.validUntil, tenantId, requirementId);
src/modules/document-archive/application\document-archive-service.ts-1058-    Object.assign(set, gsi8Fields);
src/modules/document-archive/application\document-archive-service.ts-1059-    const remove = [
src/modules/document-archive/application\document-archive-service.ts:1060:      ...(evidenceVersion.validUntil === undefined ? ["evidenceValidUntil"] : []),
src/modules/document-archive/application\document-archive-service.ts-1061-      ...(Object.keys(gsi8Fields).length === 0 ? ["GSI8PK", "GSI8SK"] : []),
src/modules/document-archive/application\document-archive-service.ts-1062-    ];
src/modules/document-archive/application\document-archive-service.ts-1063-    const update = buildVersionedUpdate({
src/modules/document-archive/application\document-archive-service.ts-1064-      tableName: this.tableName,
src/modules/document-archive/application\document-archive-service.ts-1065-      key: requirementKey(tenantId, subjectId, requirementId),
src/modules/document-archive/application\document-archive-service.ts-1066-      tenantId,
src/modules/document-archive/application\document-archive-service.ts-1067-      expectedVersion,
src/modules/document-archive/application\document-archive-service.ts-1068-      set,
--
src/modules/document-archive/application\document-archive-service.ts-1070-    });
src/modules/document-archive/application\document-archive-service.ts-1071-    try {
src/modules/document-archive/application\document-archive-service.ts-1072-      await this.store.transactWrite([{ Update: update }]);
src/modules/document-archive/application\document-archive-service.ts-1073-    } catch (err) {
src/modules/document-archive/application\document-archive-service.ts-1074-      if (isTransactionCanceled(err)) throw new ConflictError("Requirement was concurrently modified.", { requirementId });
src/modules/document-archive/application\document-archive-service.ts-1075-      throw err;
src/modules/document-archive/application\document-archive-service.ts-1076-    }
src/modules/document-archive/application\document-archive-service.ts-1077-    const next = { ...current, ...set, version: expectedVersion + 1, updatedAt: now } as Requirement;
src/modules/document-archive/application\document-archive-service.ts:1078:    if (evidenceVersion.validUntil === undefined) delete next.evidenceValidUntil;
src/modules/document-archive/application\document-archive-service.ts-1079-    return next;
src/modules/document-archive/application\document-archive-service.ts-1080-  }
src/modules/document-archive/application\document-archive-service.ts-1081-
src/modules/document-archive/application\document-archive-service.ts-1082-  /** Unlinks evidence unconditionally back to MISSING/NOT_APPLICABLE (never PENDING/
src/modules/document-archive/application\document-archive-service.ts-1083-   * NOT_SATISFIED — `deriveRequirementStatus` with `evidenceVersion=undefined` only ever
src/modules/document-archive/application\document-archive-service.ts-1084-   * returns one of those two, by construction). */
src/modules/document-archive/application\document-archive-service.ts:1085:  async unlinkEvidence(ctx: RequestContext, subjectId: string, requirementId: string, expectedVersion: number): Promise<Requirement> {
src/modules/document-archive/application\document-archive-service.ts-1086-    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive/application\document-archive-service.ts-1087-    const tenantId = ctx.tenant.tenantId;
src/modules/document-archive/application\document-archive-service.ts-1088-    const current = await this.getRequirementUnchecked(tenantId, subjectId, requirementId);
src/modules/document-archive/application\document-archive-service.ts-1089-    const now = this.now();
src/modules/document-archive/application\document-archive-service.ts-1090-    const status = deriveRequirementStatus(current.applicability, undefined, new Date(now));
src/modules/document-archive/application\document-archive-service.ts-1091-    const set = { status, ...requirementGsi1Keys(tenantId, status, now, requirementId) };
src/modules/document-archive/application\document-archive-service.ts-1092-    // Never SATISFIED here (deriveRequirementStatus with no evidence only ever returns
src/modules/document-archive/application\document-archive-service.ts-1093-    // MISSING/NOT_APPLICABLE) — the GSI8 pointer is always cleared unconditionally, no
src/modules/document-archive/application\document-archive-service.ts-1094-    // requirementGsi8Fields() branch needed.
src/modules/document-archive/application\document-archive-service.ts:1095:    const removedFields = ["evidenceVersionId", "evidenceDocumentId", "evidenceSeq", "evidenceState", "evidenceValidUntil", "GSI8PK", "GSI8SK"] as const;
src/modules/document-archive/application\document-archive-service.ts-1096-    const update = buildVersionedUpdate({
src/modules/document-archive/application\document-archive-service.ts-1097-      tableName: this.tableName,
src/modules/document-archive/application\document-archive-service.ts-1098-      key: requirementKey(tenantId, subjectId, requirementId),
src/modules/document-archive/application\document-archive-service.ts-1099-      tenantId,
src/modules/document-archive/application\document-archive-service.ts-1100-      expectedVersion,
src/modules/document-archive/application\document-archive-service.ts-1101-      set,
src/modules/document-archive/application\document-archive-service.ts-1102-      remove: [...removedFields],
src/modules/document-archive/application\document-archive-service.ts-1103-    });
--
src/modules/document-archive/application\document-archive-service.ts-1730-      if (isTransactionCanceled(err)) {
src/modules/document-archive/application\document-archive-service.ts-1731-        throw new ConflictError(`RequirementTemplate is not ${fromStatus} (already ${toStatus}, or concurrently modified).`, { templateId });
src/modules/document-archive/application\document-archive-service.ts-1732-      }
src/modules/document-archive/application\document-archive-service.ts-1733-      throw err;
src/modules/document-archive/application\document-archive-service.ts-1734-    }
src/modules/document-archive/application\document-archive-service.ts-1735-    return { ...current, status: toStatus, ...gsi1, version: expectedVersion + 1, updatedAt: now };
src/modules/document-archive/application\document-archive-service.ts-1736-  }
src/modules/document-archive/application\document-archive-service.ts-1737-
src/modules/document-archive/application\document-archive-service.ts:1738:  /** Projects a Requirement's own denormalized `evidenceState`/`evidenceValidUntil` (written by
src/modules/document-archive/application\document-archive-service.ts:1739:   * `linkEvidence`, see the domain entity's doc comment) into the shape `deriveRequirementStatus`
src/modules/document-archive/application\document-archive-service.ts-1740-   * needs — pure, no I/O. Used by `updateRequirement` when an `applicability` change requires
src/modules/document-archive/application\document-archive-service.ts-1741-   * re-deriving `status` without also re-linking evidence; correctly returns `undefined` when no
src/modules/document-archive/application\document-archive-service.ts:1742:   * evidence is linked (`evidenceState` absent), matching `deriveRequirementStatus`'s MISSING
src/modules/document-archive/application\document-archive-service.ts-1743-   * branch. */
src/modules/document-archive/application\document-archive-service.ts-1744-  private cachedEvidenceForDerivation(current: Requirement): { state: DocumentVersionState; validUntil?: string } | undefined {
src/modules/document-archive/application\document-archive-service.ts:1745:    if (!current.evidenceState) return undefined;
src/modules/document-archive/application\document-archive-service.ts:1746:    return { state: current.evidenceState, validUntil: current.evidenceValidUntil };
src/modules/document-archive/application\document-archive-service.ts-1747-  }
src/modules/document-archive/application\document-archive-service.ts-1748-
src/modules/document-archive/application\document-archive-service.ts-1749-  /** Translates the domain layer's `InvalidDocumentVersionTransitionError` into the
src/modules/document-archive/application\document-archive-service.ts-1750-   * application's normalized `ConflictError` at this module's boundary — from a caller's
src/modules/document-archive/application\document-archive-service.ts-1751-   * perspective, "the version's state changed since you read it" IS a conflict, whether the
src/modules/document-archive/application\document-archive-service.ts-1752-   * real race is caught here (state already moved) or later by the transaction's own OCC
src/modules/document-archive/application\document-archive-service.ts-1753-   * condition. The domain layer stays free of any HTTP/AppError concern. */
src/modules/document-archive/application\document-archive-service.ts-1754-  private assertTransitionOrConflict(from: DocumentVersionState, to: DocumentVersionState, documentId: string, seq: number): void {
rg: src/workers/submission*: A sintaxe do nome do arquivo, do nome do diretório ou do rótulo do volume está incorreta. (os error 123)

codex
A Rodada 2 corrige a direção arquitetural, mas ainda não está pronta para implementação. O principal avanço é abandonar o fan-out síncrono e separar corretamente completude física, tráfego HTTP e autoridade futura. Os principais bloqueantes restantes estão no promoter concorrente, na entrega real do outbox e na convergência do worker sob eventos fora de ordem.

## Avaliação

1. O novo enquadramento de autoridade está correto

A separação agora é precisa e adequadamente limitada:

- O módulo antigo é o único pipeline fisicamente completo.
- Terraform demonstra implantação/roteabilidade, não tráfego efetivo.
- `document-archive` é a autoridade conceitual futura por decisão arquitetural anterior.

Também confirmei o achado novo: [upload-finalizer-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/upload-finalizer-handler.ts) reconhece apenas as chaves antigas de `document` e `subject`, assim como [malware-result-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/malware-result-handler.ts). Portanto, `document-archive` realmente não recebe nenhum dos dois sinais físicos necessários.

A conclusão de que o arquivo fica inicialmente em `PENDING_UPLOAD`, e não simplesmente em `SCANNING`, é correta.

2. A Opção C é conceitualmente sólida, mas o rollout ainda está incompleto

Separar contrato, implementação e ativação é a solução correta. Entretanto, há uma lacuna operacional:

- Se o promoter for ativado enquanto o starter novo continua desabilitado, arquivos podem chegar a `CLEAN`, emitir seu único evento S3 e nunca iniciar extração.
- Ligar o flag posteriormente não reproduz eventos S3 históricos.

“Implementar e testar independentemente” é seguro; “implantar e deixar ativo independentemente” não é. A revisão precisa definir uma destas alternativas:

- ativar primeiro o starter e depois o promoter;
- manter ambos os branches novos desabilitados até a ativação coordenada;
- persistir um trigger durável/outbox de extração;
- ou executar backfill explícito de arquivos `CLEAN` sem `ExtractionRun`.

Sem isso, a feature flag pode criar uma lacuna permanente, não apenas atraso transitório.

3. Os terceiros branches são uma arquitetura plausível, mas a alegação de exclusividade é falsa

Reusar as duas filas e os dois handlers existentes é melhor que introduzir uma fila mal definida. Retry e DLQ realmente podem ser herdados pelos novos branches se erros forem propagados como `batchItemFailures`.

Porém, [apply-file-scan-result.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/apply-file-scan-result.ts) retorna `READY_TO_PROMOTE` sem fazer escrita alguma. Depois que as duas evidências já estão persistidas:

- uma duplicata de qualquer evento pode retornar `READY_TO_PROMOTE`;
- upload-finalizer e malware-result podem ambos observar o estado consolidado;
- os dois podem executar `CopyObject`;
- somente a confirmação DynamoDB subsequente é protegida pela transição terminal.

Portanto, “nunca os dois branches competindo” não é sustentado pelo código. A promoção é, no máximo, concorrente porém potencialmente idempotente. Isso pode ser aceitável, mas precisa ser desenhado como tal: chave de destino determinística, cópia condicionada/verificada, comportamento com múltiplas versões S3 e tratamento explícito de uma segunda cópia após a primeira confirmação.

A cerca de tenant também foi afirmada sem base suficiente. `processMalwareResult` e os workers antigos terem sua própria checagem não faz com que um branch novo chamando diretamente `applyFileScanResult` a herde. Essa função usa `store.transactWrite` e não contém uma condição de tenant `ACTIVE`. O novo branch precisa chamar explicitamente a cerca compartilhada ou incluí-la na transação.

Deixar a limpeza da quarentena fora do escopo é aceitável como paridade temporária, desde que registrada como dívida operacional. Não resolve retenção/custo, mas não é bloqueante desta decisão.

4. A convergência assíncrona é a direção correta, mas o worker ainda está subespecificado

A mudança resolve os três defeitos estruturais da Rodada 1:

- existe agora access pattern reverso;
- nenhuma transação depende de `N`;
- a phantom race de novos links não precisa ser fechada por uma transação global, porque `linkEvidence` lê a versão corrente.

O comentário em [requirement.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/domain/requirement.ts) realmente aceita bounded staleness para mudanças posteriores da `DocumentVersion`. Portanto, consistência assíncrona é compatível com o modelo existente.

Contudo, surgem quatro requisitos ainda ausentes:

- Eventos podem chegar fora de ordem. O worker não pode aplicar `validUntil` do payload cegamente; deve reler a `DocumentVersion` corrente e verificar que o Requirement ainda aponta para aquele `versionId`.
- Cada retry OCC deve repetir tanto a leitura do Requirement quanto a validação do vínculo. Caso contrário, um `unlinkEvidence` ou relink para outra versão pode ser sobrescrito.
- GSI é eventualmente consistente. Um evento processado antes de o ponteiro do índice ficar visível pode não encontrar um Requirement que já estava vinculado. O argumento de que um link posterior lê o valor atual resolve links posteriores, mas não formaliza todos os casos próximos à propagação do índice.
- O reindex diário atual deriva a partir de `evidenceState`/`evidenceValidUntil` cacheados; ele não relê a `DocumentVersion`. Logo, ele não é automaticamente uma rede de reparo para um evento perdido ou um candidato omitido pelo GSI. Sem sweeper/redrive/reconciliação autoritativa, a inconsistência pode ser ilimitada, contrariando “bounded”.

Há ainda uma imprecisão de cardinalidade: confirm não é realmente “3-way” se também grava o outbox; são quatro ações. O reject é 2-way. O auto-confirm continua tendo cardinalidade proporcional ao número de `ExtractedField` criados, embora limitada pelo schema, e acrescentará `DocumentVersion` e outbox.

5. A função compartilhada é uma correção real, mas insuficiente sozinha

[run-extraction-validation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-extraction-validation.ts) e [dynamodb-extracted-field-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/dynamodb-extracted-field-store.ts) confirmam que o auto-confirm já grava o agregado de negócio dentro de `commitRunOutcome`. A Rodada 2 corrigiu corretamente o erro factual da proposta original.

Uma função pura compartilhada elimina divergência na tradução `fieldName + confirmedValue → mutação de DocumentVersion`. Mas ela não garante, por si só, equivalência total entre os caminhos. Ambos também precisam compartilhar ou provar equivalência em:

- estados elegíveis da `DocumentVersion`;
- OCC e leitura da versão corrente;
- emissão exatamente uma vez lógica do outbox;
- `confirmedBy`/`confirmedAt`;
- tratamento de múltiplos campos auto-confirmados que eventualmente mapeiem para `validUntil`;
- comportamento quando não há mudança efetiva de valor.

A proposta deve definir a função como planner de toda a mutação relevante — ou especificar builders adicionais compartilhados — e não apenas do `set` de `validUntil`.

6. O outbox citado não entrega esse evento atualmente

[outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts) permite anexar atomicamente um registro à transação. Essa parte funciona como assumido.

Mas o próprio arquivo registra que o caminho genérico para EventBridge nunca foi implementado. Os eventos reais dependem de destinos SQS explícitos, e `OutboxDestination` não contém um destino para `DocumentVersionValidityChanged`.

Assim, “emitir no outbox e um worker consumir” não é um contrato executável com a infraestrutura atual. A Rodada 3 precisa definir:

- novo `OutboxDestination`;
- fila, DLQ e IAM;
- roteamento no relay;
- schema do envelope/evento;
- retry/redrive;
- mecanismo de reparo após DLQ ou omissão eventual do GSI.

Esse é um bloqueante central, porque sem isso Requirements nunca convergem.

7. PRINCIPAL-only é representável, mas a justificativa é de produto

[document-file.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/domain/document-file.ts) contém `role: "PRINCIPAL" | "ATTACHMENT"` e exige exatamente um principal por lote. Logo, a política é tecnicamente representável e elimina múltiplos candidatos normais por versão.

Ela não elimina duplicatas do evento do próprio principal, que ainda exigem identidade idempotente de `ExtractionRun`. Também é uma decisão de produto Type 1: “anexos tipicamente são apoio” não é evidência suficiente de que nunca carregam a página com validade. Como política provisória, reversível e explicitamente limitada à fase atual, é aceitável; deve ser registrada como restrição funcional, não como verdade geral.

8. O checklist melhorou, mas ainda é parcialmente circular

Os pesos agora cobrem os riscos corretos e somam 100%. Porém, várias âncoras definem a solução proposta como condição de sucesso:

- “Requirement atualiza via worker assíncrono”;
- “só PRINCIPAL dispara”;
- “usa `buildDocumentVersionValidityUpdate`”.

Isso avalia conformidade com a própria proposta, não o resultado arquitetural. Além disso, a pesquisa externa citada sobre human-in-the-loop não sustenta diretamente identidade DynamoDB, promoter S3, GSI reverso ou entrega outbox.

A régua deve ser formulada em termos de propriedades verificáveis: convergência mesmo com duplicação, perda temporária, reorder e relink; cardinalidade limitada; nenhuma extração de objeto não autoritativo; política multi-arquivo explicitamente aprovada; equivalência observável entre auto e manual.

## O que deve mudar para a Rodada 3

1. Corrigir a afirmação de que apenas um handler pode promover. Assumir concorrência/duplicação e especificar idempotência completa de `CopyObject → verify → confirm`.

2. Definir uma cerca de tenant real para o novo branch, idealmente na mesma transação de cada mutação de `DocumentFile`/`DocumentVersion`, em vez de alegar herança dos workers antigos.

3. Especificar o rollout da Opção C para impedir arquivos `CLEAN` cujo evento ocorreu enquanto o starter estava desligado.

4. Definir o destino real do outbox: `OutboxDestination`, fila, DLQ, relay, schema, IAM e política de redrive. O “default EventBridge” não existe no código atual.

5. Exigir que o refresh worker releia a `DocumentVersion` corrente e o Requirement corrente em cada tentativa, verifique `evidenceVersionId`, e nunca aplique cegamente o `validUntil` do evento.

6. Definir proteção contra eventos fora de ordem, preferencialmente usando a versão OCC/aggregateVersion da `DocumentVersion` e tratando eventos antigos como apenas um sinal para reler o estado atual.

7. Adicionar uma rede de reparo autoritativa. O reindex diário atual não basta porque usa o cache do próprio Requirement. Deve haver redrive/sweeper que releia a `DocumentVersion` ou outra garantia demonstrável de convergência limitada.

8. Corrigir a contagem das transações: confirm com outbox não é 3-way; documentar separadamente cardinalidade de ações e número de agregados.

9. Especificar a equivalência completa entre auto-confirm e confirmação humana, além da função que produz o `set`: estados elegíveis, OCC, outbox, provenance e múltiplos campos mapeáveis.

10. Manter PRINCIPAL-only como política provisória explícita, exigir idempotência contra eventos duplicados do principal e registrar que anexos não são inspecionados nesta fase.

11. Reescrever o checklist E-014 em propriedades de resultado, incluindo entrega durável, reorder, duplicação, relink/unlink concorrente e reparo; não usar componentes específicos da solução como a própria definição de aprovação.

A revisão saiu de um desenho estruturalmente inviável para uma arquitetura plausível, mas ainda depende de um canal outbox inexistente e contém duas alegações de segurança incorretas: promoção exclusiva e cerca de tenant herdada.

Nota: 7.8/10
tokens used
59.002
A Rodada 2 corrige a direção arquitetural, mas ainda não está pronta para implementação. O principal avanço é abandonar o fan-out síncrono e separar corretamente completude física, tráfego HTTP e autoridade futura. Os principais bloqueantes restantes estão no promoter concorrente, na entrega real do outbox e na convergência do worker sob eventos fora de ordem.

## Avaliação

1. O novo enquadramento de autoridade está correto

A separação agora é precisa e adequadamente limitada:

- O módulo antigo é o único pipeline fisicamente completo.
- Terraform demonstra implantação/roteabilidade, não tráfego efetivo.
- `document-archive` é a autoridade conceitual futura por decisão arquitetural anterior.

Também confirmei o achado novo: [upload-finalizer-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/upload-finalizer-handler.ts) reconhece apenas as chaves antigas de `document` e `subject`, assim como [malware-result-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/malware-result-handler.ts). Portanto, `document-archive` realmente não recebe nenhum dos dois sinais físicos necessários.

A conclusão de que o arquivo fica inicialmente em `PENDING_UPLOAD`, e não simplesmente em `SCANNING`, é correta.

2. A Opção C é conceitualmente sólida, mas o rollout ainda está incompleto

Separar contrato, implementação e ativação é a solução correta. Entretanto, há uma lacuna operacional:

- Se o promoter for ativado enquanto o starter novo continua desabilitado, arquivos podem chegar a `CLEAN`, emitir seu único evento S3 e nunca iniciar extração.
- Ligar o flag posteriormente não reproduz eventos S3 históricos.

“Implementar e testar independentemente” é seguro; “implantar e deixar ativo independentemente” não é. A revisão precisa definir uma destas alternativas:

- ativar primeiro o starter e depois o promoter;
- manter ambos os branches novos desabilitados até a ativação coordenada;
- persistir um trigger durável/outbox de extração;
- ou executar backfill explícito de arquivos `CLEAN` sem `ExtractionRun`.

Sem isso, a feature flag pode criar uma lacuna permanente, não apenas atraso transitório.

3. Os terceiros branches são uma arquitetura plausível, mas a alegação de exclusividade é falsa

Reusar as duas filas e os dois handlers existentes é melhor que introduzir uma fila mal definida. Retry e DLQ realmente podem ser herdados pelos novos branches se erros forem propagados como `batchItemFailures`.

Porém, [apply-file-scan-result.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/apply-file-scan-result.ts) retorna `READY_TO_PROMOTE` sem fazer escrita alguma. Depois que as duas evidências já estão persistidas:

- uma duplicata de qualquer evento pode retornar `READY_TO_PROMOTE`;
- upload-finalizer e malware-result podem ambos observar o estado consolidado;
- os dois podem executar `CopyObject`;
- somente a confirmação DynamoDB subsequente é protegida pela transição terminal.

Portanto, “nunca os dois branches competindo” não é sustentado pelo código. A promoção é, no máximo, concorrente porém potencialmente idempotente. Isso pode ser aceitável, mas precisa ser desenhado como tal: chave de destino determinística, cópia condicionada/verificada, comportamento com múltiplas versões S3 e tratamento explícito de uma segunda cópia após a primeira confirmação.

A cerca de tenant também foi afirmada sem base suficiente. `processMalwareResult` e os workers antigos terem sua própria checagem não faz com que um branch novo chamando diretamente `applyFileScanResult` a herde. Essa função usa `store.transactWrite` e não contém uma condição de tenant `ACTIVE`. O novo branch precisa chamar explicitamente a cerca compartilhada ou incluí-la na transação.

Deixar a limpeza da quarentena fora do escopo é aceitável como paridade temporária, desde que registrada como dívida operacional. Não resolve retenção/custo, mas não é bloqueante desta decisão.

4. A convergência assíncrona é a direção correta, mas o worker ainda está subespecificado

A mudança resolve os três defeitos estruturais da Rodada 1:

- existe agora access pattern reverso;
- nenhuma transação depende de `N`;
- a phantom race de novos links não precisa ser fechada por uma transação global, porque `linkEvidence` lê a versão corrente.

O comentário em [requirement.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/domain/requirement.ts) realmente aceita bounded staleness para mudanças posteriores da `DocumentVersion`. Portanto, consistência assíncrona é compatível com o modelo existente.

Contudo, surgem quatro requisitos ainda ausentes:

- Eventos podem chegar fora de ordem. O worker não pode aplicar `validUntil` do payload cegamente; deve reler a `DocumentVersion` corrente e verificar que o Requirement ainda aponta para aquele `versionId`.
- Cada retry OCC deve repetir tanto a leitura do Requirement quanto a validação do vínculo. Caso contrário, um `unlinkEvidence` ou relink para outra versão pode ser sobrescrito.
- GSI é eventualmente consistente. Um evento processado antes de o ponteiro do índice ficar visível pode não encontrar um Requirement que já estava vinculado. O argumento de que um link posterior lê o valor atual resolve links posteriores, mas não formaliza todos os casos próximos à propagação do índice.
- O reindex diário atual deriva a partir de `evidenceState`/`evidenceValidUntil` cacheados; ele não relê a `DocumentVersion`. Logo, ele não é automaticamente uma rede de reparo para um evento perdido ou um candidato omitido pelo GSI. Sem sweeper/redrive/reconciliação autoritativa, a inconsistência pode ser ilimitada, contrariando “bounded”.

Há ainda uma imprecisão de cardinalidade: confirm não é realmente “3-way” se também grava o outbox; são quatro ações. O reject é 2-way. O auto-confirm continua tendo cardinalidade proporcional ao número de `ExtractedField` criados, embora limitada pelo schema, e acrescentará `DocumentVersion` e outbox.

5. A função compartilhada é uma correção real, mas insuficiente sozinha

[run-extraction-validation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-extraction-validation.ts) e [dynamodb-extracted-field-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/dynamodb-extracted-field-store.ts) confirmam que o auto-confirm já grava o agregado de negócio dentro de `commitRunOutcome`. A Rodada 2 corrigiu corretamente o erro factual da proposta original.

Uma função pura compartilhada elimina divergência na tradução `fieldName + confirmedValue → mutação de DocumentVersion`. Mas ela não garante, por si só, equivalência total entre os caminhos. Ambos também precisam compartilhar ou provar equivalência em:

- estados elegíveis da `DocumentVersion`;
- OCC e leitura da versão corrente;
- emissão exatamente uma vez lógica do outbox;
- `confirmedBy`/`confirmedAt`;
- tratamento de múltiplos campos auto-confirmados que eventualmente mapeiem para `validUntil`;
- comportamento quando não há mudança efetiva de valor.

A proposta deve definir a função como planner de toda a mutação relevante — ou especificar builders adicionais compartilhados — e não apenas do `set` de `validUntil`.

6. O outbox citado não entrega esse evento atualmente

[outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts) permite anexar atomicamente um registro à transação. Essa parte funciona como assumido.

Mas o próprio arquivo registra que o caminho genérico para EventBridge nunca foi implementado. Os eventos reais dependem de destinos SQS explícitos, e `OutboxDestination` não contém um destino para `DocumentVersionValidityChanged`.

Assim, “emitir no outbox e um worker consumir” não é um contrato executável com a infraestrutura atual. A Rodada 3 precisa definir:

- novo `OutboxDestination`;
- fila, DLQ e IAM;
- roteamento no relay;
- schema do envelope/evento;
- retry/redrive;
- mecanismo de reparo após DLQ ou omissão eventual do GSI.

Esse é um bloqueante central, porque sem isso Requirements nunca convergem.

7. PRINCIPAL-only é representável, mas a justificativa é de produto

[document-file.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/domain/document-file.ts) contém `role: "PRINCIPAL" | "ATTACHMENT"` e exige exatamente um principal por lote. Logo, a política é tecnicamente representável e elimina múltiplos candidatos normais por versão.

Ela não elimina duplicatas do evento do próprio principal, que ainda exigem identidade idempotente de `ExtractionRun`. Também é uma decisão de produto Type 1: “anexos tipicamente são apoio” não é evidência suficiente de que nunca carregam a página com validade. Como política provisória, reversível e explicitamente limitada à fase atual, é aceitável; deve ser registrada como restrição funcional, não como verdade geral.

8. O checklist melhorou, mas ainda é parcialmente circular

Os pesos agora cobrem os riscos corretos e somam 100%. Porém, várias âncoras definem a solução proposta como condição de sucesso:

- “Requirement atualiza via worker assíncrono”;
- “só PRINCIPAL dispara”;
- “usa `buildDocumentVersionValidityUpdate`”.

Isso avalia conformidade com a própria proposta, não o resultado arquitetural. Além disso, a pesquisa externa citada sobre human-in-the-loop não sustenta diretamente identidade DynamoDB, promoter S3, GSI reverso ou entrega outbox.

A régua deve ser formulada em termos de propriedades verificáveis: convergência mesmo com duplicação, perda temporária, reorder e relink; cardinalidade limitada; nenhuma extração de objeto não autoritativo; política multi-arquivo explicitamente aprovada; equivalência observável entre auto e manual.

## O que deve mudar para a Rodada 3

1. Corrigir a afirmação de que apenas um handler pode promover. Assumir concorrência/duplicação e especificar idempotência completa de `CopyObject → verify → confirm`.

2. Definir uma cerca de tenant real para o novo branch, idealmente na mesma transação de cada mutação de `DocumentFile`/`DocumentVersion`, em vez de alegar herança dos workers antigos.

3. Especificar o rollout da Opção C para impedir arquivos `CLEAN` cujo evento ocorreu enquanto o starter estava desligado.

4. Definir o destino real do outbox: `OutboxDestination`, fila, DLQ, relay, schema, IAM e política de redrive. O “default EventBridge” não existe no código atual.

5. Exigir que o refresh worker releia a `DocumentVersion` corrente e o Requirement corrente em cada tentativa, verifique `evidenceVersionId`, e nunca aplique cegamente o `validUntil` do evento.

6. Definir proteção contra eventos fora de ordem, preferencialmente usando a versão OCC/aggregateVersion da `DocumentVersion` e tratando eventos antigos como apenas um sinal para reler o estado atual.

7. Adicionar uma rede de reparo autoritativa. O reindex diário atual não basta porque usa o cache do próprio Requirement. Deve haver redrive/sweeper que releia a `DocumentVersion` ou outra garantia demonstrável de convergência limitada.

8. Corrigir a contagem das transações: confirm com outbox não é 3-way; documentar separadamente cardinalidade de ações e número de agregados.

9. Especificar a equivalência completa entre auto-confirm e confirmação humana, além da função que produz o `set`: estados elegíveis, OCC, outbox, provenance e múltiplos campos mapeáveis.

10. Manter PRINCIPAL-only como política provisória explícita, exigir idempotência contra eventos duplicados do principal e registrar que anexos não são inspecionados nesta fase.

11. Reescrever o checklist E-014 em propriedades de resultado, incluindo entrega durável, reorder, duplicação, relink/unlink concorrente e reparo; não usar componentes específicos da solução como a própria definição de aprovação.

A revisão saiu de um desenho estruturalmente inviável para uma arquitetura plausível, mas ainda depende de um canal outbox inexistente e contém duas alegações de segurança incorretas: promoção exclusiva e cerca de tenant herdada.

Nota: 7.8/10
