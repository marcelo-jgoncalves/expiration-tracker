Você é revisor independente (Codex) num protocolo Claude↔Codex de nota cega, mínimo 3 rodadas, gate >=9.0 sem arredondar, do repositório expiration-tracker (AGENTS.md §4).

CONTEXTO: Wave 1b — Design System Implementation Gap, nomeada em D-130 (docs/architecture/reviews/design-system-reconciliation-scoping/estado-final-consolidado.md). Duas decisões residuais nomeadas explicitamente como não bloqueantes, avaliar se vale implementar agora:

1) Button `lg` variant. Hoje `frontend/src/components/ui/Button.tsx` só implementa `sm`/`md` (ButtonSize = "sm" | "md"), default `md` = `--control-height-md` (36px). O token `--control-height-lg` (44px) já existe em `frontend/src/styles/tokens.css` mas não é consumido por nenhuma variante do Button. `docs/frontend/design-system.md` §30 menciona "height: 44px" como tamanho padrão do botão, mas isso foi reconciliado em D-130 como referindo-se ao protótipo anterior, não ao sistema real — não é um requisito ativo. Nenhuma tela real hoje precisa de um botão 44px (WCAG target size mínimo já cumprido por sm/md, ambos >=32px com espaçamento). Pergunta: adicionar variante `lg` ao Button agora (trabalho especulativo, sem consumidor real) ou não adicionar até haver um caso de uso real (ex. CTA mobile full-width)?

2) Token de motion `slow`. `tokens.css` hoje define `--duration-instant` (0ms), `--duration-fast` (120ms), `--duration-default` (160ms). `design-system.md` §21 nomeia `motion.slow` (240ms no texto original, mas D-130 reconciliou que o valor concreto do texto é obsoleto). Nenhuma transição real no CSS do projeto hoje usa nada além de fast/default. Pergunta: adicionar `--duration-slow` agora especulativamente, ou não até haver uma transição real que precise dela?

Proponha uma decisão para cada item (pode ser "não implementar agora, manter como resíduo nomeado" — isso é uma decisão válida, não uma fuga) com justificativa breve, alinhada ao princípio do projeto de nunca introduzir token/variante sem consumidor real ("Proibição de valores arbitrários"/"não criar token especulativo"). Dê uma nota de 0 a 10 à SUA PRÓPRIA proposta (nota cega, não veja nenhuma nota anterior). Seja direto e conciso (max ~400 palavras).
