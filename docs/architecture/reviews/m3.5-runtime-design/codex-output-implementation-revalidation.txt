OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a01c1c-3846-7881-acbd-b6d411f97f50
--------
user
Revalidacao da IMPLEMENTACAO do milestone M3.5, expiration-tracker, protocolo Claude<->Codex (AGENTS.md secao 4). Voce revisou a implementacao anteriormente (docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt) e deu nota 5,8/10, apontando 5 problemas: (P1) contrato incompativel entre EventBridge Scheduler e os handlers (event.detail.mode / event.time nunca existiriam, quebrando toda invocacao real); (P1) ponteiro GSI6 WORKSTATE#DST_PENDING nunca removido quando a reconciliacao CONFIRMA que o schedule segue correto, alem do criterio de quando o ponteiro e criado ser mais estreito que o design; (P2) paginacao do port ignorada pelo handler de reconciliacao; (P2) markPublished do outbox relay nao remove GSI6PK/GSI6SK do indice RECON#OUTBOX#PENDING.

Corrigi os 5 pontos no commit a6679d4 (branch develop, HEAD atual): rode `git log --oneline -3` e `git show a6679d4 --stat` para orientar-se, depois leia os diffs reais (`git show a6679d4`). Resumo das correcoes: (1) infra/lib/reminder-schedule.ts agora envia {mode?, scheduledTime: "<aws.scheduler.scheduled-time>"} no nivel raiz do Input para todos os 4 schedules; reminder-producer-handler.ts e reminder-reconciliation-handler.ts foram retipados para ler esses campos diretamente (sem event.detail/event.time); teste de contrato novo em test/infra/stack.test.ts prova isso via CDK synth. (2) reconcileDst (reconciliation.ts) agora remove GSI6PK/GSI6SK tambem no caminho "confirmado, nao diverge" (nao so no cancelamento); reminder-materializer.ts amplia o criterio de escrita do ponteiro para incluir qualquer timeZone que observa DST no ano do agendamento (novo helper timeZoneObservesDst em recurrence.ts), nao so horarios literalmente ambiguos/inexistentes. Teste novo em reconciliation.test.ts prova a remocao no caso confirmado. (3) reminder-reconciliation-handler.ts drena paginas ate MAX_PAGES=25 usando o cursor que o adapter ja retornava. (4) dynamodb-outbox-relay-store.ts's markPublished agora inclui "REMOVE GSI6PK, GSI6SK" no UpdateExpression.

145 testes totais, typecheck/lint/check-boundaries limpos (verifique voce mesmo se quiser, mas nao precisa rodar rebuild completo - foque em ler o codigo).

Verifique se as 5 correcoes realmente resolvem os problemas apontados, sem introduzir bugs novos. Preste atencao especial a: o campo `scheduledTime` do EventBridge Scheduler runtime realmente populado corretamente pela sintaxe "<aws.scheduler.scheduled-time>" (voce pode nao ter certeza sem doc AWS - se nao tiver confianca na sintaxe exata, diga isso explicitamente em vez de supor); se o helper timeZoneObservesDst tem algum caso extremo errado (fuso horario que muda de regra de DST entre janeiro e julho de forma que o teste de dois pontos falhe); se a remocao do ponteiro no caso "confirmado" do reconcileDst esta usando a versao correta do item (race condition possivel).

De uma nota final 0-10 na implementacao corrigida como pronta para as Camadas 2/3 de teste. Responda em portugues, direto ao ponto.

codex
Vou revalidar o commit contra o parecer anterior e as fontes normativas atuais. Como isso envolve uma decisão de qualidade no protocolo do repositório, vou registrar evidências precisas do diff e verificar na documentação oficial da AWS a sintaxe do campo de tempo do Scheduler.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; git branch --show-current; git log --oneline -3; git show a6679d4 --stat; git status --short' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 526ms:
# Expiration Tracker — Status e Próxima Sessão

## Próxima ação obrigatória (2026-08-19, sessão de implementação M3.5 — mais recente, leia isto primeiro)

Design do milestone M3.5 (runtime real do Reminder Engine / fechamento de G8) **APPROVED** (Claude 9.0/Codex 9.3, `docs/architecture/m3.5-runtime-design.md`). **Implementação substancial concluída e mergeada em `main`** (PR #2): wiring real na stack CDK (fila+DLQ+Streams+4 schedules, zero placeholders `501`), handlers Lambda reais (8 funções, composition roots), adapters DynamoDB reais (5 ports), outbox relay+sweeper com teste de concorrência real, outbox durável no claim do producer + ciclo de vida completo do ponteiro GSI6 (`WORKSTATE#CLAIMED` e `WORKSTATE#DST_PENDING`, incluindo `ReminderMaterializer` escrevendo o ponteiro DST — gap fechado nesta sessão, com testes). `npm test` com 143 testes verdes, `typecheck`/`lint`/`check-boundaries` limpos.

**Ambiente desta sessão não tinha Docker nem credenciais AWS** (só registry npm) — isso bloqueou a Camada 2 (DynamoDB Local/LocalStack) e a Camada 3 (sandbox AWS efêmero) do plano de testes do design, que são gates obrigatórios antes de declarar G8 fechado. O que foi executado e verificado nesta sessão é só a Camada 1 (unit/contract/synth CDK, incluindo isolamento de IAM do GSI6 e ciclo de vida do ponteiro DST).

**Próxima ação real**: (1) rodar Camada 2 (DynamoDB Local + LocalStack/Testcontainers) e Camada 3 (sandbox AWS efêmero) num ambiente com Docker/credenciais reais — nenhuma delas foi executada ainda; (2) revisão final Claude+Codex do milestone completo (ainda não feita — só o design passou pelo protocolo, a implementação não). Não declarar G8 fechado antes dessas duas coisas.

---

## Próxima ação obrigatória (histórico — superada pela seção acima, preservada como contexto de G8)

Engineering Maturity Review concluída (checkpoints 0, 1, 2-9, 12; ver `ENGINEERING.md` na raiz para o relatório completo). Veredito: `ENGINEERING FOUNDATION STATUS: NOT APPROVED`, bloqueador único e conhecido: **G8 (recuperação real de falha assíncrona)**.

**Decisão do usuário (2026-08-19)**: tratar o fechamento pleno de G8 como **novo milestone dedicado** (mesma disciplina de M0-M3: pesquisa/design → implementação → teste real → revisão Claude+Codex), não como remediação de sessão. Escopo desse milestone, per `ENGINEERING.md` (seção "Decisão pendente: escopo de G8"):

- Adapters DynamoDB reais implementando os ports (`ReminderStore`, `ExpirationStore`, `IdentityStore`) contra AWS real — hoje só existem fakes em memória para teste.
- Handlers Lambda reais substituindo os placeholders `exports.handler = async () => ({statusCode: 501})` em `infra/lib/expiration-tracker-stack.ts`.
- Filas SQS + DLQ com redrive policy para o pipeline producerâ†’dispatch.
- EventBridge Scheduled Rule disparando o `ReminderProducer` periodicamente (a cada minuto) e a `ReminderReconciliation` periodicamente.
- Testes de fault injection contra esse runtime real (timeout de dependência, poison message, redrive).

Antes de começar esse milestone: ler `docs/engineering/reviews/checkpoint-12-redteam/summary.md` (red team formal já identificou 5 P1 relacionados: pipeline sem recuperação, idempotência não provada no limite do efeito externo, false-green CI, concorrência/estado obsoleto entre renew/cancel/materialização/dispatch/reconciliation, blast radius cross-tenant do GSI3 via workers privilegiados) — usar como input de design, não redescobrir do zero.

Trabalho já feito nesta sessão de engenharia (não repetir): CI real corrigido e confirmado verde em 5 execuções; branch `develop` estabelecida como padrão de trabalho (`AGENTS.md` §3), `main` protegida; `dependency-cruiser` como enforcement real de boundary (achou e corrigiu 2 violações genuínas de arquitetura); testes de `reconciliation.ts`/`producer.ts` que não existiam; tentativa de fechar a vulnerabilidade de devDependency (EX-001) via upgrade do Vitest foi revertida por quebrar CI real (bug upstream do npm, não repetir sem verificar correção).

---

**Correção de 2026-08-19 (Engineering Maturity Review, Checkpoint 2-9)**: as seções abaixo (escritas ao longo das sessões de M0-M3) afirmam "nada foi commitado" e citam contagens de teste por marco que, somadas, não batem com a realidade medida agora. Estado real verificado por execução: M0-M3 **já está commitado** num único commit (`154d6e0`), e `npm test` roda **123 testes** no total (19 arquivos), não a soma das contagens individuais citadas abaixo. As seções não foram reescritas (preservadas como histórico de sessão), mas não devem ser lidas como estado de commit/contagem de teste vigente — confiar em `git log`/`npm test` reais, não nesses números. Ver `docs/engineering/03-repository-baseline.md` e `docs/engineering/reviews/checkpoint-02-09-consolidated/` para a análise completa desta divergência.

Projeto: micro-SaaS de controle de vencimentos/renovações. Pasta: `c:\Users\Usuario\Desktop\projects\expiration-tracker\`. Repo GitHub: `marcelo-jgoncalves/expiration-tracker` (privado).

Mapa completo de documentação, status vigente e regra de precedência: `docs/architecture/README.md`. Regras de processo e ferramentas: `AGENTS.md`. Log cronológico de sessões: `docs/architecture/session-log.md`.

## Status atual

```text
DESIGN MATURITY STATUS: APPROVED (arquitetura conceitual + Implementation Blueprint)
ARCHITECTURE STATUS: NOT APPROVED
```

Todo o processo de design (Fases 0-3 do prompt mestre + os 14 entregáveis das seções 35-52 + threat model seção 33 + Implementation Blueprint seção 60) está completo e aprovado — ver `ARCHITECTURE.md` na raiz para o documento consolidado. `ARCHITECTURE STATUS: NOT APPROVED` é o estado normativo correto até haver implementação real testada sob falha/carga (rubrica B, `requirements.md` §13.1) — **não é reprovação de mérito, e não muda com a conclusão do blueprint**: o blueprint é design detalhado, não evidência operacional.

## Concluído nesta sessão — Implementation Blueprint (seção 60) — APPROVED

`docs/architecture/implementation-blueprint.md`. Componentes/módulos (Identity, Expiration, Reminder, Notification, Document, Audit + workers assíncronos), interfaces concretas, eventos/schemas reais (grounded em `data-model.md`), ordem de deploy, milestones M0-M7, critérios de aceite técnicos por componente, e as 7 lacunas do `threat-model.md` incorporadas como requisito desde o início (não apêndice). Processo: 2 propostas independentes (Claude/Codex) → crítica cruzada (17 problemas reais encontrados, incluindo um erro técnico presente nas duas propostas — chave do GSI3 do scheduler não era consultável) → convergência → 5 rodadas de nota cega com correção pontual a cada achado (cabeçalho prematuro, contradição de kill switch, tabela incompleta, decisões Type 1 não propagadas às seções operativas, mapeamento de estado da Step Functions incompleto). **Nota final: Claude 9.20 / Codex 9.2 (exato)** — ambos ≥9.0 sem arredondar, 8 rodadas totais, nenhum gate violado. Decisão Type 1 nova registrada: chave global do GSI3 (`GSI3PK=DUE#yyyyMMddHHmm#NN`, exceção documentada à regra de toda chave começar por `TENANT#tenantId`) — a decisão está fechada no blueprint; falta só o registro mecânico em `data-model.md` (não bloqueia implementação).

## Concluído nesta sessão — M0 "Guardrails e contratos" (implementation-blueprint.md §19)

Todas as entregas de M0 implementadas e testadas (53 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): estrutura TypeScript (`src/shared/*`, alinhada a `implementation-blueprint.md` §2), schemas JSON (`schemas/{events,queues,api}` + `sensitive-fields.json`, validados via Ajv em `src/shared/contracts/schema-validator.ts`), `SecureLogger`+`Redactor` central (`src/shared/observability/`, corpus de teste com valores canário provando que nada vaza), configuração tipada fail-fast (`src/shared/config/config.ts`), taxonomia de erro normalizada retryable/terminal (`src/shared/errors/app-error.ts`), idempotência via `PutItem attribute_not_exists(PK)` (`src/shared/idempotency/idempotency.ts`, chaves por operação conforme `data-model.md` §4), OCC com `ConditionExpression: version=:expected` (`src/shared/dynamodb/occ.ts`, `data-model.md` §5), outbox transacional (`src/shared/outbox/outbox.ts`, shape de `implementation-blueprint.md` §5.3), pipeline supply-chain (`.github/workflows/ci.yml`: npm ci imutável, actions pinadas por SHA, SBOM CycloneDX, audit, gate de schema). `AGENTS.md` §6 atualizado com comandos/convenções. Nada foi commitado (working tree aberto para revisão).

Judgment calls (blueprint estava silente): Ajv+ajv-formats para validação de JSON Schema; Vitest como test runner; ESLint `no-console` (com exceção para `src/shared/observability/**`) como o mecanismo que faz "chamada direta a `console.*` falhar no lint" (`implementation-blueprint.md` §14.1); idempotência/OCC/outbox construídos como builders puros de parâmetros DynamoDB (sem `@aws-sdk` ainda) para ficarem testáveis sem tabelas reais, que só existem a partir de M1; SLSA/assinatura de artefato adiada para M1+ (não há alvo de deploy ainda); SHAs de actions no CI pinados no momento da escrita — revisar antes do primeiro run real.

## Concluído nesta sessão — M1 "Foundation, Identity e isolamento" (implementation-blueprint.md §19)

Todas as entregas implementadas e testadas (89 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/identity/{domain,application,ports,persistence,http}` (resolver central, matriz de autorização como código, `IdentityMapping`/`User`/`DeviceSession`/`TenantQuota` via portas SDK-agnósticas) e `infra/{lib,bin}` (`ExpirationTrackerTable`, `ExpirationTrackerAuth`, `ScopedLambdaFunction`, `ExpirationTrackerApi` com a rota `GET /test/ping`). Suíte cross-tenant negativa em `test/integration/cross-tenant.test.ts` (9 casos, exit criterion do marco) e synth de infra em `test/infra/stack.test.ts` (6 casos, via `aws-cdk-lib/assertions`, sem AWS CLI). Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente ou pendência externa já conhecida): (1) MFA implementado como prop configurável (`OFF`/`OPTIONAL`/`REQUIRED`, default `OPTIONAL`) pois UNK-006 segue pendente de pesquisa externa — não bloqueava M1; (2) CSP/CloudFront Response Headers Policy **não implementado nesta sessão** — não há distribuição CloudFront/frontend ainda no repositório, e o texto do blueprint (§4.2) coloca CSP no contexto do SPA estático servido por CloudFront; tratado como pertencente ao milestone que introduzir o frontend, não forçado em M1 (revisar se essa leitura estiver errada); (3) sessão/revogação modelada como campo `globalLogoutAfter` no item `User` + item filho `DeviceSession` (`TENANT#t#USER#u`/`SESSION#<deviceId>`) em vez de uma entidade `Session` de primeira classe — `data-model.md` não define uma; (4) `ScopedLambdaFunction` mantém a lista de entidades por capability como metadado/documentação, mas o IAM real concedido é table-level (`grantReadWriteData`) — DynamoDB IAM não expressa restrição por SK/entidade da forma que a sintaxe do blueprint sugere visualmente; per-entity IAM mais fino fica como follow-up; (5) `TenantQuota` implementado como janela fixa (fixed-window), não sliding-window/leaky-bucket — mais simples e ainda satisfaz "decremento atômico, sem race condition" do `data-model.md`; (6) BFF de sessão (`/session/refresh`, `/session/logout`) **não implementado como rota HTTP nesta sessão** — o Cognito client já está configurado para o padrão (client secret, tokens de vida curta), mas os endpoints do BFF em si ficam para quando a Expiration/Notification API skeleton crescer além da rota de teste; revisar se isso deveria ter sido parte do exit criterion. `cdk synth` real via CLI não foi executado (o pacote `aws-cdk` CLI não está instalado, só a lib `aws-cdk-lib`); a sintetização foi verificada programaticamente via `Template.fromStack` nos testes, que é equivalente para fins de validação estática.

## Concluído nesta sessão — M2 "Expiration core e Audit" (implementation-blueprint.md §19)

Todas as entregas implementadas e testadas (134 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/expiration/{domain,application,ports,http}` — CRUD/renew (`ExpirationService`: createItem/getItem/updateItem/archiveItem/deleteItem/renewItem/listDashboard), OCC via `shared/dynamodb/occ.ts`, `ItemDueDateChanged` por outbox (`shared/outbox/outbox.ts`) na MESMA `TransactWriteItems` do item quando `dueDate` muda (updateItem e renewItem), `AuditEvent` append-only gravado em toda mutação na mesma transação, dashboard via GSI1. `renewItem` cria um item sucessor (`renewedFromId`) em vez de mutar `dueDate` no agregado de origem, que transiciona para `RENEWED`; é idempotente via `IdempotencyStore` de M0. Rotas HTTP CDK adicionadas em `infra/lib/api.ts`/`expiration-tracker-stack.ts` (`ItemsHandler`, mesmo authorizer JWT de M1). Suíte de integração `test/integration/expiration-lifecycle.test.ts` prova o exit criterion do marco end-to-end via handlers HTTP reais. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) evento `ItemDueDateChanged` segue exatamente o schema já existente desde M0 (`schemas/events/item-due-date-changed.v1.json`: `itemId`/`previousDueDate`/`newDueDate`/`itemVersion`, `additionalProperties: false`) em vez do exemplo mais rico do texto do blueprint §8.3 (que inclui `timeZone`/`reminderPolicyId`/`changeReason`) — o schema já testado/versionado é a fonte de verdade de contrato per `AGENTS.md` §6, o exemplo em prosa não; campos adicionais ficam para quando M3 (Reminder) precisar deles, exigindo `.v2` aditivo; (2) `renewItem` também dispara `ItemDueDateChanged` (para o item novo, `previousDueDate: null`) — o blueprint só descreve esse evento no contexto de `updateItem`, mas renovação também é uma "mudança de vencimento" do ponto de vista do futuro `ReminderProducer` (M3), e a alternativa (renovação silenciosa) deixaria o dashboard/scheduler sem sinal de que um novo item precisa de agendamento; (3) `archiveItem` usa a ação de autorização `item:update` (não uma ação dedicada — a matriz de M1 não define uma para archive) enquanto `deleteItem` usa `item:delete` (ADMIN_ROLES), seguindo a granularidade que já existe; (4) idempotência de renovação aceita uma `idempotencyKey` explícita do chamador (header `Idempotency-Key`) com fallback determinístico `itemId|expectedVersion|cycle` quando ausente, já que o blueprint define a CHAVE de dedupe mas não de onde ela vem por HTTP; (5) `ExpirationStore.transactWrite` foi adicionado ao port (não existe em `IdentityStore`) porque nenhuma escrita de M1 precisava de `TransactWriteItems` multi-item — a fake em memória (`test/unit/expiration/in-memory-store.ts`) só entende as duas formas exatas de `ConditionExpression` que este código produz (mesma limitação documentada, mesmo espírito de `InMemoryIdentityStore`).

## Concluído nesta sessão — M3 "Reminder Engine" (implementation-blueprint.md §19)

Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversão local→UTC IANA-aware — DST tratado explicitamente, horário inexistente e ambíguo — reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lógica pura testável com relógio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMED→TRIGGERED`+`NotificationIntent`+outbox numa única transação; reconciliação é um único job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluíam `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER função da tabela (inclusive as tenant-facing de M1/M2) — a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explícitos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que só `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) `occurrenceId` derivado deterministicamente (hash da chave de idempotência de data-model.md §4) em vez de UUID + registro `IdempotencyStore` separado — o próprio `putIfAbsent` condicional já garante a idempotência de materialização, mesmo padrão que data-model.md documenta para `WebhookInbox`; (2) cancelamento de ocorrência stale não remove `GSI3PK`/`GSI3SK` (o builder de `occ.ts` é SET-only, sem REMOVE) — deixa um ponteiro órfão no índice, mas o `Query` condicional `SCHEDULED→CLAIMED` do producer falha sobre ele de forma inofensiva; (3) tolerância de dispatch (`toleranceMs`, default 30min) e TTL de claim (default 2min) não estão fixados no blueprint — valores razoáveis documentados no código, revisar contra dados reais de produção; (4) reconciliação DST/claim-expiry recebe os candidatos como parâmetro (batch) em vez de fazer sua própria varredura via GSI6 — a wiring desse índice de "políticas ativas"/"claims expirados" real fica como follow-up de infra, o mecanismo de reconciliação em si (o que a tarefa pediu) está implementado e testado; (5) Lambda handlers reais (bundling com `@aws-sdk`) não foram escritos — mesmo estágio que M0-M2, CDK usa código inline placeholder via `ScopedLambdaFunction`; a lógica testável é o entregável real desta fase, igual às anteriores.

## Próxima ação obrigatória (histórico — superada pela seção do topo)

**Superada em 2026-08-19**: a Engineering Maturity Review identificou G8 (recuperação real de falha assíncrona) como bloqueador de engenharia — o milestone de runtime real (adapters/handlers/filas/EventBridge, ver seção do topo) é pré-requisito antes de M4 fazer sentido operacionalmente, ainda que M4 não dependa dele estruturalmente. Lista original preservada abaixo como histórico, não como próxima ação vigente.

1. **M4 — Notification Engine** (`implementation-blueprint.md` §19, depende de M3 ✅): router de `NotificationIntent`→canal, delivery workers (email/WhatsApp stub-first), `NotificationAttempt`, resolução de destinatário/template.
2. Decidir e fechar os itens abaixo (BFF de sessão como rotas reais, CSP/CloudFront quando o frontend existir) antes de considerar a lacuna de "session theft" do threat model totalmente fechada — ainda não resolvido, re-flagueado a cada sessão desde M1 para não se perder: M1 fechou o mecanismo de revogação/matriz de autorização, não o endpoint BFF completo; M2/M3 não tinham escopo de frontend/sessão (confirmado no blueprint) então também não o resolveram.
3. **Ratificar formalmente em `data-model.md` a exceção do GSI3** (chave global do scheduler, já decidida e justificada em `implementation-blueprint.md` §9.2/§23.3 item 10) — passo de manutenção documental, não nova rodada de debate. M3 já corrigiu o enforcement real do isolamento em IAM; falta só o registro mecânico no documento.
4. Wiring real do GSI6 (ou índice equivalente) para alimentar a reconciliação de M3 com o batch real de "políticas ativas"/"claims a verificar" em produção — hoje o mecanismo de reconciliação recebe esse batch como parâmetro injetado (testável), a query real fica pendente.
5. Testes de carga real (cenários de drenagem do pico extremo, `capacity-model.md`, com SLO de drenagem UNK-CAP-006 a formalizar em `slo.md` antes do gate de M7), teste de restore real (gate já definido em `disaster-recovery.md` §6), exercício do runbook de credencial comprometida.
6. Reavaliação sob rubrica (B) — Operational Evidence — só então `ARCHITECTURE STATUS` pode legitimamente virar `APPROVED`. **Não declarar isso antes de haver evidência operacional real.** Implementar M0/M1/M2/M3 não é evidência operacional.
7. Decisões ainda pendentes de pesquisa externa (não bloqueiam M4 estruturalmente, mas bloqueiam habilitar os canais/features específicos, ver `implementation-blueprint.md` §23.3): provider inicial de e-mail, BSP WhatsApp (pricing real, UNK-003), modelo Bedrock específico, região AWS (bloqueante para LGPD/transferência internacional), MFA obrigatório vs. opcional (UNK-006), ferramenta de backup S3 (RPO≤24h), ferramenta de assinatura/provenance do pipeline de CI.

# docs/architecture/ — Índice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           Implementação real (código/infra/testes) — Implementation Blueprint concluído
Last verified:           2026-08-19
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a próxima ação concreta.

## Precedência de fontes (quando houver divergência)

1. `AGENTS.md` (raiz) — processo de trabalho dos agentes, sempre vence sobre conteúdo de arquitetura.
2. ADR aceito em `adr/` — decisão arquitetural específica e formal.
3. Documento temático corrente (tabela abaixo, coluna "normativo atual") — especificação detalhada do domínio.
4. `ARCHITECTURE.md` — visão consolidada e índice executivo; não sobrescreve silenciosamente um ADR ou documento temático divergente — divergência entre eles é defeito a corrigir, não licença para escolher.
5. `NEXT_SESSION_PROMPT.md` — estado de execução, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` — evidência histórica de como se chegou a uma decisão, nunca normativo.

## Índice por documento

| Documento | Classificação | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/índice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histórico/processo (ciclo concluído) | Processo Claude↔Codex que produziu o design; não é ponto de entrada de sessão |
| `quality-criteria.md` | normativo atual | 12 critérios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critérios |
| `requirements.md` | normativo atual | Requisitos funcionais/não-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisões numeradas |
| `data-model.md` | normativo atual | Modelo de domínio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `privacy-lgpd.md` | normativo atual | Classes de retenção, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estágio |
| `mcp-readiness.md` | normativo atual | Prontidão de domínio para MCP futuro |
| `evolution.md` | normativo atual | Transições de estágio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | Revisão pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seção 33 — APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seção 60 — componentes, interfaces, eventos/schemas, ordem de deploy, milestones — APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) — adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas — APPROVED (Claude 9.0 / Codex 9.3) |
| `decisions-log.md` | decisão/ADR (log vivo) | 26 decisões (D-000 a D-025), nota Claude/Codex, status |
| `adr/` | decisão/ADR | 8 ADRs formais para decisões Type 1 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/status-projeto.html` | resumo/índice (visual) | Painel visual do status do projeto |
| `session-log.md` | histórico | Log cronológico compacto por sessão |
| `history/` | histórico/supersedido | Artefatos de rodada (propostas, críticas, red team) que produziram os documentos normativos acima — ver subseção |

## `history/` — evidência de rodada, por tema

Cada subpasta contém os artefatos de proposta/crítica/tréplica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisão vigente — só como prova de como o consenso foi alcançado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

develop
a6679d4 M3.5: corrigir 3 bugs P1/P2 encontrados na revisao de implementacao Codex
a5b4899 M3.5: fechar gap do GSI6 WORKSTATE#DST_PENDING no ReminderMaterializer
292dea5 Corrigir CI: atualizar AWS SDK v3 pinado para versao sem vulnerabilidades
commit a6679d4b7426aa73a949ad17b239fadfa2ddd267
Author: marcelo-jgoncalves <243885363+marcelo-jgoncalves@users.noreply.github.com>
Date:   Wed Aug 19 19:03:32 2026 -0300

    M3.5: corrigir 3 bugs P1/P2 encontrados na revisao de implementacao Codex
    
    Revisao final Claude+Codex da IMPLEMENTACAO (nao mais do design,
    protocolo AGENTS.md secao 4) encontrou 5 problemas reais, confianca
    inicial 5,8/10. Todos corrigidos:
    
    - P1 CRITICO: EventBridge Scheduler nao envelopa o payload em `detail`
      como as Rules legadas - o evento e exatamente o `input` configurado.
      reminder-producer-handler.ts lia event.time (undefined -> Invalid
      Date) e reminder-reconciliation-handler.ts lia event.detail.mode
      (undefined -> toda invocacao quebraria). Corrigido: schedules agora
      enviam {mode?, scheduledTime: "<aws.scheduler.scheduled-time>"} no
      nivel raiz; handlers tipados e lendo os campos corretos. Teste de
      contrato novo em stack.test.ts prova que Input nunca tem `detail` e
      sempre tem scheduledTime.
    
    - P1: pointer GSI6 WORKSTATE#DST_PENDING nunca era removido quando a
      reconciliacao CONFIRMAVA que o schedule seguia correto (so no
      cancelamento por divergencia) - ficaria sendo reavaliado todo dia
      para sempre. Corrigido em reconcileDst: candidatos confirmados
      tambem tem o ponteiro removido. Tambem ampliado o criterio de quando
      o materializer grava o ponteiro: nao so horario ambiguo/inexistente
      (dstKind!==NORMAL), mas qualquer timeZone que observa DST no ano do
      agendamento (novo helper timeZoneObservesDst em recurrence.ts) - um
      lembrete as 09:00 normal pode precisar reavaliacao apos mudanca de
      offset mesmo sem ter sido ambiguo na materializacao.
    
    - P2: reminder-reconciliation-handler.ts so lia a primeira pagina de
      candidatos (claims e DST), ignorando o cursor que o adapter ja
      retornava - corrigido com drenagem ate MAX_PAGES=25.
    
    - P2: markPublished (outbox relay) nao removia GSI6PK/GSI6SK do
      indice RECON#OUTBOX#PENDING - registros publicados ficavam
      indexados para sempre, o sweeper lendo e descartando lixo
      crescente. Corrigido: REMOVE GSI6PK, GSI6SK adicionado ao
      UpdateExpression.
    
    3 testes novos: contrato de payload Scheduler/handler (synth), remocao
    do ponteiro DST quando confirmado (reconciliation.test.ts). 145 testes
    totais, typecheck/lint/check-boundaries limpos.
    
    Nao corrigido nesta rodada (fora do escopo dos 5 achados, anotado pelo
    Codex como observacao, nao bug): ausencia de um OutboxPublisher
    generico real no repo para provar o lado inverso da exclusividade de
    roteamento - OutboxPublisher em si e trabalho de M2/M3 ja existente
    como builder de outbox, nao um worker Lambda separado ainda.
    
    Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

 .../codex-output-implementation-review.txt         | 4434 ++++++++++++++++++++
 .../codex-prompt-implementation-review.txt         |   22 +
 infra/lib/reminder-schedule.ts                     |   22 +-
 .../reminder/application/reminder-materializer.ts  |   33 +-
 src/modules/reminder/domain/recurrence.ts          |   17 +
 .../aws/handlers/reminder-producer-handler.ts      |   23 +-
 .../handlers/reminder-reconciliation-handler.ts    |   79 +-
 .../persistence/dynamodb-outbox-relay-store.ts     |    7 +-
 .../reminder-reconciliation/reconciliation.ts      |   28 +-
 test/infra/stack.test.ts                           |   21 +-
 test/unit/reminder/reconciliation.test.ts          |   39 +
 11 files changed, 4669 insertions(+), 56 deletions(-)
 M .claude/settings.json
 M infra/bin/app.ts
 M package-lock.json
 M package.json
?? cdk.json
?? docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-revalidation.txt
?? docs/architecture/reviews/m3.5-runtime-design/codex-prompt-implementation-revalidation.txt

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git show --format=fuller --find-renames a6679d4 -- infra/lib/reminder-schedule.ts src/modules/reminder/application/reminder-materializer.ts src/modules/reminder/domain/recurrence.ts src/runtime/aws/handlers/reminder-producer-handler.ts src/runtime/aws/handlers/reminder-reconciliation-handler.ts src/runtime/aws/persistence/dynamodb-outbox-relay-store.ts src/workers/reminder-reconciliation/reconciliation.ts test/infra/stack.test.ts test/unit/reminder/reconciliation.test.ts; rg -n "DST_PENDING|timeZoneObservesDst|reconcileDst|scheduledTime|MAX_PAGES|markPublished|scheduled-time|reconciliation" docs/architecture/m3.5-runtime-design.md docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt src test infra' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 1422ms:
commit a6679d4b7426aa73a949ad17b239fadfa2ddd267
Author:     marcelo-jgoncalves <243885363+marcelo-jgoncalves@users.noreply.github.com>
AuthorDate: Wed Aug 19 19:03:32 2026 -0300
Commit:     marcelo-jgoncalves <243885363+marcelo-jgoncalves@users.noreply.github.com>
CommitDate: Wed Aug 19 19:03:32 2026 -0300

    M3.5: corrigir 3 bugs P1/P2 encontrados na revisao de implementacao Codex
    
    Revisao final Claude+Codex da IMPLEMENTACAO (nao mais do design,
    protocolo AGENTS.md secao 4) encontrou 5 problemas reais, confianca
    inicial 5,8/10. Todos corrigidos:
    
    - P1 CRITICO: EventBridge Scheduler nao envelopa o payload em `detail`
      como as Rules legadas - o evento e exatamente o `input` configurado.
      reminder-producer-handler.ts lia event.time (undefined -> Invalid
      Date) e reminder-reconciliation-handler.ts lia event.detail.mode
      (undefined -> toda invocacao quebraria). Corrigido: schedules agora
      enviam {mode?, scheduledTime: "<aws.scheduler.scheduled-time>"} no
      nivel raiz; handlers tipados e lendo os campos corretos. Teste de
      contrato novo em stack.test.ts prova que Input nunca tem `detail` e
      sempre tem scheduledTime.
    
    - P1: pointer GSI6 WORKSTATE#DST_PENDING nunca era removido quando a
      reconciliacao CONFIRMAVA que o schedule seguia correto (so no
      cancelamento por divergencia) - ficaria sendo reavaliado todo dia
      para sempre. Corrigido em reconcileDst: candidatos confirmados
      tambem tem o ponteiro removido. Tambem ampliado o criterio de quando
      o materializer grava o ponteiro: nao so horario ambiguo/inexistente
      (dstKind!==NORMAL), mas qualquer timeZone que observa DST no ano do
      agendamento (novo helper timeZoneObservesDst em recurrence.ts) - um
      lembrete as 09:00 normal pode precisar reavaliacao apos mudanca de
      offset mesmo sem ter sido ambiguo na materializacao.
    
    - P2: reminder-reconciliation-handler.ts so lia a primeira pagina de
      candidatos (claims e DST), ignorando o cursor que o adapter ja
      retornava - corrigido com drenagem ate MAX_PAGES=25.
    
    - P2: markPublished (outbox relay) nao removia GSI6PK/GSI6SK do
      indice RECON#OUTBOX#PENDING - registros publicados ficavam
      indexados para sempre, o sweeper lendo e descartando lixo
      crescente. Corrigido: REMOVE GSI6PK, GSI6SK adicionado ao
      UpdateExpression.
    
    3 testes novos: contrato de payload Scheduler/handler (synth), remocao
    do ponteiro DST quando confirmado (reconciliation.test.ts). 145 testes
    totais, typecheck/lint/check-boundaries limpos.
    
    Nao corrigido nesta rodada (fora do escopo dos 5 achados, anotado pelo
    Codex como observacao, nao bug): ausencia de um OutboxPublisher
    generico real no repo para provar o lado inverso da exclusividade de
    roteamento - OutboxPublisher em si e trabalho de M2/M3 ja existente
    como builder de outbox, nao um worker Lambda separado ainda.
    
    Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

diff --git a/infra/lib/reminder-schedule.ts b/infra/lib/reminder-schedule.ts
index 163843a..ac7a2cd 100644
--- a/infra/lib/reminder-schedule.ts
+++ b/infra/lib/reminder-schedule.ts
@@ -32,6 +32,15 @@ export class ReminderSchedule extends Construct {
 
     const state = (props.schedulesEnabled ?? true) ? "ENABLED" : "DISABLED";
 
+    // IMPORTANT (bug found by Codex implementation review): EventBridge Scheduler's Lambda
+    // target does NOT wrap the payload in a `detail` envelope the way legacy EventBridge
+    // Rules do - whatever `input` is set to (or `{}` if omitted) becomes the event object
+    // handed to the Lambda directly. Every schedule below MUST set an explicit `input`
+    // matching exactly what its handler reads (`event.mode`, `event.scheduledTime` - see
+    // reminder-producer-handler.ts / reminder-reconciliation-handler.ts), never
+    // `event.detail.*` / `event.time` (those are Rule-shaped fields that don't exist here).
+    // `<aws.scheduler.scheduled-time>` is a Scheduler context attribute, substituted at
+    // invoke time with the schedule's fire time (ISO-8601 UTC).
     new scheduler.CfnSchedule(this, "ReminderProducerSchedule", {
       scheduleExpression: "rate(1 minute)",
       state,
@@ -39,12 +48,13 @@ export class ReminderSchedule extends Construct {
       target: {
         arn: props.reminderProducer.functionArn,
         roleArn: grantInvoke(this, "ReminderProducer", props.reminderProducer).roleArn,
+        input: JSON.stringify({ scheduledTime: "<aws.scheduler.scheduled-time>" }),
       },
     });
 
-    // Two independent schedules invoking the SAME ReminderReconciliation function with
-    // different `detail.mode` (m3.5-runtime-design.md: "mesmo handler pode receber mode,
-    // mantendo lógicas e métricas separadas").
+    // Two independent schedules invoking the SAME ReminderReconciliation function with a
+    // different `mode` (m3.5-runtime-design.md: "mesmo handler pode receber mode, mantendo
+    // lógicas e métricas separadas") - field is top-level, not under `detail`.
     new scheduler.CfnSchedule(this, "ReminderClaimReconciliationSchedule", {
       scheduleExpression: "rate(5 minutes)",
       state,
@@ -52,7 +62,7 @@ export class ReminderSchedule extends Construct {
       target: {
         arn: props.reminderReconciliation.functionArn,
         roleArn: grantInvoke(this, "ReminderClaimReconciliation", props.reminderReconciliation).roleArn,
-        input: JSON.stringify({ mode: "CLAIMS" }),
+        input: JSON.stringify({ mode: "CLAIMS", scheduledTime: "<aws.scheduler.scheduled-time>" }),
       },
     });
 
@@ -64,7 +74,7 @@ export class ReminderSchedule extends Construct {
       target: {
         arn: props.reminderReconciliation.functionArn,
         roleArn: grantInvoke(this, "ReminderDstReconciliation", props.reminderReconciliation).roleArn,
-        input: JSON.stringify({ mode: "DST" }),
+        input: JSON.stringify({ mode: "DST", scheduledTime: "<aws.scheduler.scheduled-time>" }),
       },
     });
 
@@ -75,8 +85,8 @@ export class ReminderSchedule extends Construct {
       target: {
         arn: props.outboxSweeper.functionArn,
         roleArn: grantInvoke(this, "OutboxSweeperReminderDispatch", props.outboxSweeper).roleArn,
+        input: JSON.stringify({ scheduledTime: "<aws.scheduler.scheduled-time>" }),
       },
     });
-
   }
 }
diff --git a/src/modules/reminder/application/reminder-materializer.ts b/src/modules/reminder/application/reminder-materializer.ts
index 1775589..55052c1 100644
--- a/src/modules/reminder/application/reminder-materializer.ts
+++ b/src/modules/reminder/application/reminder-materializer.ts
@@ -21,6 +21,7 @@ import {
   addCalendarDays,
   parseDayOffset,
   parseLocalTime,
+  timeZoneObservesDst,
 } from "../domain/recurrence.js";
 import { gsi3Keys, occurrenceKey, stableHash, type ReminderOccurrence } from "../domain/reminder-occurrence.js";
 import type { ReminderPolicy, QuietHours } from "../domain/reminder-policy.js";
@@ -157,22 +158,22 @@ export class ReminderMaterializer {
         shardCount: generation.shardCount,
       });
 
-      // M3.5 (docs/architecture/m3.5-runtime-design.md §"Reconciliação"): occurrences whose
-      // schedule was computed at an ambiguous or nonexistent local time (dstKind !==
-      // "NORMAL") are exactly the ones a DST transition can make wrong later - e.g.
-      // materialized against one UTC offset, but the actual transition date/rule changes
-      // before the occurrence fires. These get a GSI6 WORKSTATE#DST_PENDING pointer so the
-      // daily DST reconciliation pass can find and re-evaluate them without scanning every
-      // occurrence in the system. Judgment call: narrower than "every occurrence with any
-      // timeZone" - it targets the concrete edge cases computeSchedule already flags, not a
-      // speculative full-timezone-database forecast of future transitions.
-      const dstPending =
-        schedule.dstKind !== "NORMAL"
-          ? {
-              GSI6PK: GSI6PK_WORKSTATE_DST_PENDING,
-              GSI6SK: buildDstCandidateGsi6Sk(schedule.scheduledAtUtc, input.tenantId, occurrenceId),
-            }
-          : {};
+      // M3.5 (docs/architecture/m3.5-runtime-design.md §"Reconciliação"; broadened after
+      // Codex's implementation review found the original AMBIGUOUS/NONEXISTENT-only trigger
+      // too narrow): a GSI6 WORKSTATE#DST_PENDING pointer is set whenever EITHER the
+      // schedule was computed at an ambiguous/nonexistent local time (dstKind !== "NORMAL")
+      // OR the policy's timeZone observes DST at all in the occurrence's target year - a
+      // plain 09:00 reminder in a DST-observing zone can still need re-evaluation after an
+      // offset change even though its own computed instant was never ambiguous. Occurrences
+      // in a fixed-offset timeZone (e.g. America/Sao_Paulo since 2019) never get the pointer.
+      const dstRelevant =
+        schedule.dstKind !== "NORMAL" || timeZoneObservesDst(input.policy.timeZone, toCalendarDate(schedule.scheduledAtUtc).year);
+      const dstPending = dstRelevant
+        ? {
+            GSI6PK: GSI6PK_WORKSTATE_DST_PENDING,
+            GSI6SK: buildDstCandidateGsi6Sk(schedule.scheduledAtUtc, input.tenantId, occurrenceId),
+          }
+        : {};
 
       const occurrence: ReminderOccurrence = {
         ...occurrenceKey(input.tenantId, input.itemId, schedule.scheduledAtUtc, occurrenceId),
diff --git a/src/modules/reminder/domain/recurrence.ts b/src/modules/reminder/domain/recurrence.ts
index 9fd6069..3e4799b 100644
--- a/src/modules/reminder/domain/recurrence.ts
+++ b/src/modules/reminder/domain/recurrence.ts
@@ -160,3 +160,20 @@ export function parseLocalTime(hhmm: string): { hour: number; minute: number } {
   }
   return { hour: Number(match[1]), minute: Number(match[2]) };
 }
+
+/**
+ * M3.5 (docs/architecture/m3.5-runtime-design.md §"Reconciliação"; broadened after Codex's
+ * implementation review found the original AMBIGUOUS/NONEXISTENT-only trigger too narrow):
+ * true if `timeZone` observes DST at all during `year` - compares the UTC offset at two
+ * fixed reference instants (Jan 15 and Jul 15 noon UTC), one of which is guaranteed to fall
+ * in each side of a DST transition for any zone that has one. A fixed-offset zone (e.g.
+ * America/Sao_Paulo since 2019, or any UTC+N zone) always returns false. Used to flag EVERY
+ * occurrence in a DST-observing zone as a reconciliation candidate, not just the literally
+ * ambiguous/nonexistent ones - a plain 09:00 reminder can still need re-evaluation after an
+ * offset change even though its own computed instant was never ambiguous.
+ */
+export function timeZoneObservesDst(timeZone: string, year: number): boolean {
+  const januaryOffset = offsetMinutesAt(Date.UTC(year, 0, 15, 12, 0, 0), timeZone);
+  const julyOffset = offsetMinutesAt(Date.UTC(year, 6, 15, 12, 0, 0), timeZone);
+  return januaryOffset !== julyOffset;
+}
diff --git a/src/runtime/aws/handlers/reminder-producer-handler.ts b/src/runtime/aws/handlers/reminder-producer-handler.ts
index 48c91a1..aabc6dd 100644
--- a/src/runtime/aws/handlers/reminder-producer-handler.ts
+++ b/src/runtime/aws/handlers/reminder-producer-handler.ts
@@ -1,10 +1,15 @@
-/** Real handler for ReminderProducer (EventBridge Scheduler, 1 min), replacing the 501 placeholder. */
-import type { ScheduledEvent } from "aws-lambda";
+/** Real handler for ReminderProducer (EventBridge Scheduler, 1 min), replacing the 501
+ * placeholder. EventBridge Scheduler's Lambda target does NOT wrap the payload in a
+ * `detail` envelope the way legacy EventBridge Rules do - the event IS whatever `input`
+ * the schedule sets (infra/lib/reminder-schedule.ts), never `event.time` (a Rule-shaped
+ * field that doesn't exist for Scheduler invocations - bug found by Codex implementation
+ * review, event.time was always undefined -> Invalid Date). */
 import { createDocumentClient } from "../../../shared/dynamodb/client.js";
 import { buildReminderProducerDeps } from "../composition/reminder.js";
 import { runProducerTick } from "../../../workers/reminder-producer/producer.js";
 import { defaultShardConfig } from "../../../modules/reminder/domain/shard-config.js";
 import { SecureLogger } from "../../../shared/observability/logger.js";
+import { ValidationError } from "../../../shared/errors/app-error.js";
 
 const client = createDocumentClient();
 const tableName = process.env["TABLE_NAME"];
@@ -12,8 +17,18 @@ if (!tableName) throw new Error("TABLE_NAME env var is required.");
 const deps = buildReminderProducerDeps(client, tableName);
 const logger = new SecureLogger({ baseContext: { service: "reminder-producer" } });
 
-export async function handler(event: ScheduledEvent): Promise<void> {
-  const tickMinute = new Date(event.time);
+export interface ReminderProducerEvent {
+  scheduledTime: string;
+}
+
+export async function handler(event: ReminderProducerEvent): Promise<void> {
+  if (!event.scheduledTime) {
+    throw new ValidationError("reminder-producer: missing scheduledTime in event payload.");
+  }
+  const tickMinute = new Date(event.scheduledTime);
+  if (Number.isNaN(tickMinute.getTime())) {
+    throw new ValidationError("reminder-producer: scheduledTime is not a valid date.", { scheduledTime: event.scheduledTime });
+  }
   const result = await runProducerTick({ ...deps, shardConfig: defaultShardConfig() }, tickMinute);
   logger.info("reminder-producer tick complete", {
     scanned: result.scanned,
diff --git a/src/runtime/aws/handlers/reminder-reconciliation-handler.ts b/src/runtime/aws/handlers/reminder-reconciliation-handler.ts
index f35fa80..3d65b71 100644
--- a/src/runtime/aws/handlers/reminder-reconciliation-handler.ts
+++ b/src/runtime/aws/handlers/reminder-reconciliation-handler.ts
@@ -1,8 +1,13 @@
 /** Real handler for ReminderReconciliation (EventBridge Scheduler, `mode` field), replacing
- * the 501 placeholder. Two schedules invoke the same Lambda with different `detail.mode`
+ * the 501 placeholder. Two schedules invoke the same Lambda with different `mode`
  * (m3.5-runtime-design.md §"Reconciliação"): CLAIMS (every 5 min) and DST (daily). Also one
- * of exactly two roles granted gsi6Read() - see infra/lib/dynamo-table.ts. */
-import type { ScheduledEvent } from "aws-lambda";
+ * of exactly two roles granted gsi6Read() - see infra/lib/dynamo-table.ts.
+ *
+ * EventBridge Scheduler's Lambda target does NOT wrap the payload in a `detail` envelope
+ * the way legacy EventBridge Rules do - the event IS whatever `input` the schedule sets
+ * (infra/lib/reminder-schedule.ts: `{mode, scheduledTime}` at the top level), never
+ * `event.detail.mode` (bug found by Codex implementation review - event.detail was always
+ * undefined, both schedules would throw on every invocation). */
 import { createDocumentClient } from "../../../shared/dynamodb/client.js";
 import { buildReconciliationDeps } from "../composition/reminder.js";
 import { runReconciliation, type DstReconciliationCandidate as FullDstCandidate } from "../../../workers/reminder-reconciliation/reconciliation.js";
@@ -20,46 +25,68 @@ const { store, candidateSource, now } = buildReconciliationDeps(client, tableNam
 const shardConfig = defaultShardConfig();
 const logger = new SecureLogger({ baseContext: { service: "reminder-reconciliation" } });
 
-interface ReconciliationEventDetail {
+export interface ReminderReconciliationEvent {
   mode: "CLAIMS" | "DST";
+  scheduledTime: string;
 }
 
-export async function handler(event: ScheduledEvent<ReconciliationEventDetail>): Promise<void> {
-  const mode = event.detail.mode;
+/** Hard cap on pages drained per invocation - matches the design's "página lógica máx. 200"
+ * per page with a bounded number of pages, so a pathological backlog can't make a single
+ * invocation run indefinitely; anything beyond this is picked up by the NEXT scheduled
+ * run (every 5 min for claims, daily for DST) rather than blocking this one. */
+const MAX_PAGES = 25;
 
-  let expiredClaimCandidates: ReminderOccurrence[] = [];
+export async function handler(event: ReminderReconciliationEvent): Promise<void> {
+  const mode = event.mode;
+
+  const expiredClaimCandidates: ReminderOccurrence[] = [];
   const dstCandidates: FullDstCandidate[] = [];
 
   if (mode === "CLAIMS") {
     // GSI6 is ALL-projected - each candidate row already IS the full ReminderOccurrence
     // item (see ExpiredClaimCandidate's doc comment), so no extra fetch is needed.
-    const page = await candidateSource.listExpiredClaims({ before: now() });
-    expiredClaimCandidates = page.items as unknown as ReminderOccurrence[];
+    // Pagination is drained (bug found by Codex implementation review: the first version
+    // only ever read page 1, silently leaving any backlog over one page unprocessed).
+    let cursor: string | undefined;
+    for (let page = 0; page < MAX_PAGES; page++) {
+      const result = await candidateSource.listExpiredClaims({ before: now(), cursor });
+      expiredClaimCandidates.push(...(result.items as unknown as ReminderOccurrence[]));
+      if (!result.cursor) break;
+      cursor = result.cursor;
+    }
   }
 
   if (mode === "DST") {
     // ReminderMaterializer (M3.5) writes a WORKSTATE#DST_PENDING GSI6 pointer whenever a
-    // trigger's schedule was computed at an ambiguous/nonexistent local time (dstKind !==
-    // "NORMAL") - see reminder-materializer.ts. This query finds those candidates for
-    // re-evaluation; occurrences that were never DST-ambiguous never appear here.
+    // trigger's schedule was computed at an ambiguous/nonexistent local time, OR whose
+    // timeZone observes DST at all (see reminder-materializer.ts - broadened per Codex
+    // implementation review: a non-ambiguous 09:00 reminder in a DST-observing zone can
+    // still need re-evaluation after an offset change, not just literally-ambiguous ones).
+    // This query finds those candidates; occurrences in a fixed-offset timeZone never
+    // appear here at all.
     const windowStart = now();
     const windowEnd = new Date(Date.parse(windowStart) + 7 * 24 * 60 * 60_000).toISOString();
-    const page = await candidateSource.listDstCandidates({ window: { start: windowStart, end: windowEnd } });
-    for (const light of page.items) {
-      const policy = await store.get<ReminderPolicy>(policyKey(light.tenantId, light.policyId));
-      if (!policy) continue;
-      const item = await store.get<{ PK: string; SK: string; dueDate: string; version: number }>(itemKey(light.tenantId, light.itemId));
-      if (!item) continue;
-      dstCandidates.push({
-        tenantId: light.tenantId,
-        itemId: light.itemId,
-        itemVersion: item.version,
-        itemDueDate: item.dueDate,
-        policy,
-      });
+    let cursor: string | undefined;
+    for (let page = 0; page < MAX_PAGES; page++) {
+      const result = await candidateSource.listDstCandidates({ window: { start: windowStart, end: windowEnd }, cursor });
+      for (const light of result.items) {
+        const policy = await store.get<ReminderPolicy>(policyKey(light.tenantId, light.policyId));
+        if (!policy) continue;
+        const item = await store.get<{ PK: string; SK: string; dueDate: string; version: number }>(itemKey(light.tenantId, light.itemId));
+        if (!item) continue;
+        dstCandidates.push({
+          tenantId: light.tenantId,
+          itemId: light.itemId,
+          itemVersion: item.version,
+          itemDueDate: item.dueDate,
+          policy,
+        });
+      }
+      if (!result.cursor) break;
+      cursor = result.cursor;
     }
   }
 
   const result = await runReconciliation({ store, tableName, now, shardConfig }, { expiredClaimCandidates, dstCandidates });
-  logger.info("reminder-reconciliation complete", { mode, ...result });
+  logger.info("reminder-reconciliation complete", { mode, scheduledTime: event.scheduledTime, ...result });
 }
diff --git a/src/workers/reminder-reconciliation/reconciliation.ts b/src/workers/reminder-reconciliation/reconciliation.ts
index bd2f22a..28e78e1 100644
--- a/src/workers/reminder-reconciliation/reconciliation.ts
+++ b/src/workers/reminder-reconciliation/reconciliation.ts
@@ -130,7 +130,33 @@ export async function reconcileDst(
     for (const occurrence of liveExisting) {
       const expected = expectedByTrigger.get(occurrence.triggerId);
       const diverges = !expected || expected.scheduledAtUtc !== occurrence.scheduledAt;
-      if (!diverges) continue;
+      if (!diverges) {
+        // M3.5 (bug found by Codex implementation review - the original code left
+        // WORKSTATE#DST_PENDING pointers in place forever whenever recomputation confirmed
+        // the schedule was still correct, so confirmed-correct occurrences were
+        // re-evaluated on every single daily run indefinitely). The window is processed for
+        // this occurrence either way - clear the pointer once confirmed, not only on
+        // cancellation.
+        if (occurrence.GSI6PK) {
+          try {
+            await deps.store.transactWrite([
+              {
+                Update: buildVersionedUpdate({
+                  tableName: deps.tableName,
+                  key: { PK: occurrence.PK, SK: occurrence.SK },
+                  tenantId: candidate.tenantId,
+                  expectedVersion: occurrence.version,
+                  set: {},
+                  remove: ["GSI6PK", "GSI6SK"],
+                }),
+              },
+            ]);
+          } catch (err) {
+            if (!isTransactionCanceled(err)) throw err; // lost a race - fine, not our job to force it
+          }
+        }
+        continue;
+      }
       divergences += 1;
       try {
         await deps.store.transactWrite([
diff --git a/test/infra/stack.test.ts b/test/infra/stack.test.ts
index c32abb6..c9d3b8b 100644
--- a/test/infra/stack.test.ts
+++ b/test/infra/stack.test.ts
@@ -163,11 +163,28 @@ describe("ExpirationTrackerStack (M1 infra synth)", () => {
     const template = synthTemplate();
     const schedules = template.findResources("AWS::Scheduler::Schedule");
     expect(Object.keys(schedules).length).toBe(4); // producer, claims-reconciliation, dst-reconciliation, sweeper
-    const withInput = Object.values(schedules).filter((s) => typeof (s.Properties.Target as { Input?: string }).Input === "string");
-    const modes = withInput.map((s) => (JSON.parse((s.Properties.Target as { Input: string }).Input) as { mode: string }).mode);
+    const withMode = Object.values(schedules)
+      .map((s) => JSON.parse((s.Properties.Target as { Input: string }).Input) as { mode?: string })
+      .filter((parsed): parsed is { mode: string } => typeof parsed.mode === "string");
+    const modes = withMode.map((parsed) => parsed.mode);
     expect(modes.sort()).toEqual(["CLAIMS", "DST"]);
   });
 
+  it("M3.5 contract (bug found by Codex implementation review): every schedule's Input is a top-level JSON payload with scheduledTime, matching what the handlers read - EventBridge Scheduler does NOT wrap payloads in a `detail` envelope like legacy Rules, so event.detail.mode / event.time would always be undefined", () => {
+    const template = synthTemplate();
+    const schedules = template.findResources("AWS::Scheduler::Schedule");
+    for (const [logicalId, def] of Object.entries(schedules)) {
+      const input = (def.Properties.Target as { Input?: string }).Input;
+      expect(input, `${logicalId} has no Input - handler would receive {} and fail`).toBeTruthy();
+      const parsed = JSON.parse(input!) as Record<string, unknown>;
+      expect(parsed["scheduledTime"], `${logicalId} Input is missing scheduledTime`).toBe("<aws.scheduler.scheduled-time>");
+      expect(parsed["detail"], `${logicalId} Input must not be Rule-shaped (no detail wrapper)`).toBeUndefined();
+      if (logicalId.includes("Reconciliation")) {
+        expect(["CLAIMS", "DST"]).toContain(parsed["mode"]);
+      }
+    }
+  });
+
   it("GSI3 exists with a global (non-tenant-prefixed) key shape and minimal projection", () => {
     const app = new App();
     const stack = new Stack(app, "Gsi3Stack");
diff --git a/test/unit/reminder/reconciliation.test.ts b/test/unit/reminder/reconciliation.test.ts
index 1248aa9..1ec7cf2 100644
--- a/test/unit/reminder/reconciliation.test.ts
+++ b/test/unit/reminder/reconciliation.test.ts
@@ -230,6 +230,45 @@ describe("reconciliation.ts", () => {
       expect(result.created).toBe(0);
     });
 
+    it("M3.5: removes the GSI6 WORKSTATE#DST_PENDING pointer once recomputation CONFIRMS the schedule is still correct (bug found by Codex implementation review - the pointer used to stay forever in this case, causing daily re-evaluation indefinitely)", async () => {
+      const policy = await policies.createPolicy(ctx, {
+        scope: "ITEM",
+        itemId: ITEM_ID,
+        rule: {
+          name: "same day 09:00",
+          triggers: [{ triggerId: "trig1", offsetIso: "P0D", localTime: "09:00" }],
+          timeZone: "America/New_York", // DST-observing zone -> materializer sets the pointer
+          channels: ["EMAIL"],
+        },
+      });
+      const materializer = new ReminderMaterializer(store, TABLE, now);
+      const materialized = await materializer.materialize({
+        tenantId: TENANT,
+        itemId: ITEM_ID,
+        itemVersion: 1,
+        itemDueDate: "2026-09-10T00:00:00.000Z",
+        policy,
+        shardConfig: defaultShardConfig(),
+      });
+      const occ = materialized.created[0]!;
+      expect(occ.GSI6PK).toBe("WORKSTATE#DST_PENDING"); // sanity: the pointer IS present before reconciliation
+
+      clock.current = "2026-09-05T00:00:00.000Z"; // within the 7-day window, schedule unchanged since materialization
+
+      const result = await reconcileDst(
+        { store, tableName: TABLE, now, shardConfig: defaultShardConfig() },
+        [{ tenantId: TENANT, itemId: ITEM_ID, itemVersion: 1, itemDueDate: "2026-09-10T00:00:00.000Z", policy }],
+      );
+
+      expect(result.divergences).toBe(0);
+      expect(result.cancelled).toBe(0);
+
+      const confirmed = await store.get<ReminderOccurrence>({ PK: occ.PK, SK: occ.SK });
+      expect(confirmed?.status).toBe("SCHEDULED"); // still live, only the pointer was cleared
+      expect(confirmed?.GSI6PK).toBeUndefined();
+      expect(confirmed?.GSI6SK).toBeUndefined();
+    });
+
     it("skips a disabled policy entirely (no cancellation, no materialization)", async () => {
       const enabled = await policies.createPolicy(ctx, {
         scope: "ITEM",
docs/architecture/m3.5-runtime-design.md:27:- Remoção das chaves `GSI6PK`/`GSI6SK` (`WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING`) ocorre **atomicamente na mesma `TransactWriteItems`/`UpdateItem` da mudança de estado correspondente** (claim→triggered, claim revertido, janela DST processada) — nunca como passo separado que pode falhar independente e deixar o índice inconsistente com o estado real.
docs/architecture/m3.5-runtime-design.md:33:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#POLICY#<policyId> — projeção: PK, SK, tenantId, policyId, timeZone, windowStart, windowEnd. Filtro: windowStart dentro da janela de 7 dias já usada por M3. Paginação: idêntica. */
docs/architecture/m3.5-runtime-design.md:40:- `GSI6PK="WORKSTATE#DST_PENDING"`/`GSI6SK` gravados pelo `ReminderMaterializer` (M3, já existente) ao criar/atualizar uma `ReminderOccurrence` cuja janela de recorrência cruza uma transição de DST conhecida (mesma lógica IANA-aware de M3, só passa a persistir o resultado como candidato indexado em vez de recalcular do zero); removidos quando a reconciliação DST processa a janela.
docs/architecture/m3.5-runtime-design.md:42:Namespace `WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING` (global, sem tenant) não colide com `RECON#OUTBOX#PENDING` (global, outbox) nem com `TENANT#t#WORKSTATE#<status>` de outros domínios (Document/Upload, M5+, que continuam tenant-scoped por não precisarem de reconciliação cross-tenant) — são famílias de chave `GSI6PK` distintas por prefixo, coexistindo no mesmo índice físico. Port separado de `ReminderStore` (mesmo princípio de isolamento que levou a `ReminderProducerStore` a ser separado — não dar a rotas tenant-facing acesso a uma query que elas não precisam). Isso muda o escopo original: o "wiring real do GSI6" (item 4 da lista de pendências pós-M3) deixa de ser follow-up separado e entra neste milestone — sem ele, a EventBridge Rule/Scheduler de reconciliação (item 4 do escopo de G8) dispara sobre nada.
docs/architecture/m3.5-runtime-design.md:51:4. **EventBridge Scheduler** (não Rule legado — decisão já fixada em `implementation-blueprint.md` linha 162, Type 1 já aprovada, não reaberta aqui sem necessidade técnica nova): `ReminderProducerSchedule` (**1 min**, `FlexibleTimeWindow: OFF`), `ReminderClaimReconciliationSchedule` (**5 min**, `FlexibleTimeWindow: OFF`), `ReminderDstReconciliationSchedule` (**diário 03:00 UTC, `FlexibleTimeWindow: 15 min`**), `OutboxSweeperReminderDispatchSchedule` (**5 min**, `FlexibleTimeWindow: OFF`). Reserved concurrency preservada do blueprint (producer 2, dispatch 10, reconciliation 1); relay/sweeper concurrency 2 (mesma ordem de grandeza do producer, sem DLQ dedicada para o sweeper — ele mesmo é o mecanismo de recuperação).
docs/architecture/m3.5-runtime-design.md:67:- **P1 #4 (concorrência/estado obsoleto)**: provado entre renew/cancel/materialização/dispatch/reconciliation. Não prova corridas envolvendo envio externo/callbacks de M4.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:22:- src/modules/reminder/application/reminder-materializer.ts (ponteiro GSI6 WORKSTATE#DST_PENDING)
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:45:Design do milestone M3.5 (runtime real do Reminder Engine / fechamento de G8) **APPROVED** (Claude 9.0/Codex 9.3, `docs/architecture/m3.5-runtime-design.md`). **Implementação substancial concluída e mergeada em `main`** (PR #2): wiring real na stack CDK (fila+DLQ+Streams+4 schedules, zero placeholders `501`), handlers Lambda reais (8 funções, composition roots), adapters DynamoDB reais (5 ports), outbox relay+sweeper com teste de concorrência real, outbox durável no claim do producer + ciclo de vida completo do ponteiro GSI6 (`WORKSTATE#CLAIMED` e `WORKSTATE#DST_PENDING`, incluindo `ReminderMaterializer` escrevendo o ponteiro DST — gap fechado nesta sessão, com testes). `npm test` com 143 testes verdes, `typecheck`/`lint`/`check-boundaries` limpos.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:65:Antes de começar esse milestone: ler `docs/engineering/reviews/checkpoint-12-redteam/summary.md` (red team formal já identificou 5 P1 relacionados: pipeline sem recuperação, idempotência não provada no limite do efeito externo, false-green CI, concorrência/estado obsoleto entre renew/cancel/materialização/dispatch/reconciliation, blast radius cross-tenant do GSI3 via workers privilegiados) — usar como input de design, não redescobrir do zero.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:67:Trabalho já feito nesta sessão de engenharia (não repetir): CI real corrigido e confirmado verde em 5 execuções; branch `develop` estabelecida como padrão de trabalho (`AGENTS.md` §3), `main` protegida; `dependency-cruiser` como enforcement real de boundary (achou e corrigiu 2 violações genuínas de arquitetura); testes de `reconciliation.ts`/`producer.ts` que não existiam; tentativa de fechar a vulnerabilidade de devDependency (EX-001) via upgrade do Vitest foi revertida por quebrar CI real (bug upstream do npm, não repetir sem verificar correção).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:110:Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversão local→UTC IANA-aware — DST tratado explicitamente, horário inexistente e ambíguo — reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lógica pura testável com relógio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMED→TRIGGERED`+`NotificationIntent`+outbox numa única transação; reconciliação é um único job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluíam `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER função da tabela (inclusive as tenant-facing de M1/M2) — a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explícitos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que só `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:192:a5b4899 M3.5: fechar gap do GSI6 WORKSTATE#DST_PENDING no ReminderMaterializer
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:234: .../dynamodb-reconciliation-candidate-source.ts    |   69 +
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:237: .../ports/reconciliation-candidate-source.ts       |   64 +
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:246: .../handlers/reminder-reconciliation-handler.ts    |   65 +
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:260: .../reminder-reconciliation/reconciliation.ts      |    4 +
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:306:src/shared/dynamodb/occ.ts:23:   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:309:src/shared/dynamodb/occ.ts:26:   * actively queried by reconciliation. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:620:src/workers/dispatch-outbox-relay/relay.ts:72:  await deps.store.markPublished({ PK: record.PK, SK: record.SK });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:694:src/workers/reminder-producer/producer.ts:26:import { GSI6PK_WORKSTATE_CLAIMED, buildExpiredClaimGsi6Sk } from "../../modules/reminder/ports/reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:716:src/workers/reminder-producer/producer.ts:48:  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (§9.5). */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:798:src/workers/reminder-producer/producer.ts:130:            // producer's own lookback window nor the daily DST reconciliation reliably
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:803:src/workers/reminder-producer/producer.ts:135:            // reconciliation job can find this claim if it expires unrecovered - removed
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:804:src/workers/reminder-producer/producer.ts:136:            // when ReminderDispatch advances to TRIGGERED or reconciliation reverts it.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:890:src/modules/reminder/application/reminder-materializer.ts:31:import { GSI6PK_WORKSTATE_DST_PENDING, buildDstCandidateGsi6Sk } from "../ports/reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1023:src/modules/reminder/application/reminder-materializer.ts:164:      // before the occurrence fires. These get a GSI6 WORKSTATE#DST_PENDING pointer so the
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1024:src/modules/reminder/application/reminder-materializer.ts:165:      // daily DST reconciliation pass can find and re-evaluate them without scanning every
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1031:src/modules/reminder/application/reminder-materializer.ts:172:              GSI6PK: GSI6PK_WORKSTATE_DST_PENDING,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1099:src/modules/reminder/application/reminder-materializer.ts:240:              // unlike GSI3, ARE actively queried by reconciliation, so a stale
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1100:src/modules/reminder/application/reminder-materializer.ts:241:              // WORKSTATE#DST_PENDING pointer on a CANCELLED occurrence would be a real bug
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1101:src/modules/reminder/application/reminder-materializer.ts:242:              // (reconciliation would keep re-evaluating dead work) - removed here.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1118:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^" src/workers/reminder-producer/producer.ts src/workers/dispatch-outbox-relay/relay.ts src/shared/outbox/relay-store.ts src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts src/modules/reminder/persistence/dynamodb-reminder-producer-store.ts src/modules/reminder/persistence/dynamodb-reminder-store.ts src/workers/dispatch-outbox-relay/persistence/dynamodb-outbox-relay-store.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1123:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:1:/**
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1124:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:2: * Real DynamoDB adapter for ReminderReconciliationCandidateSource (M3.5). Separate class,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1125:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:3: * only wired into the ReminderReconciliation Lambda's composition root - same isolation
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1126:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:4: * principle as DynamoDbReminderProducerStore. Queries GSI6's global (non-tenant-prefixed)
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1127:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:5: * WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING key families - see
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1128:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:6: * src/modules/reminder/ports/reconciliation-candidate-source.ts and
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1129:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:7: * docs/architecture/m3.5-runtime-design.md for why these keys are global.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1130:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:8: */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1131:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:9:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1132:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:10:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1133:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:11:import {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1134:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:12:  GSI6PK_WORKSTATE_CLAIMED,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1135:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:13:  GSI6PK_WORKSTATE_DST_PENDING,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1136:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:14:  type DstReconciliationCandidate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1137:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:15:  type ExpiredClaimCandidate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1138:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:16:  type Page,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1139:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:17:  type ReminderReconciliationCandidateSource,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1140:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:18:} from "../ports/reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1141:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:19:import { mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1142:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:20:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1143:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:21:const DEFAULT_PAGE_SIZE = 200;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1144:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:22:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1145:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:23:export class DynamoDbReminderReconciliationCandidateSource implements ReminderReconciliationCandidateSource {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1146:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:24:  constructor(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1147:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:25:    private readonly client: DynamoDBDocumentClient,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1148:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:26:    private readonly tableName: string,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1149:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:27:  ) {}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1150:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:28:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1151:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:29:  async listExpiredClaims(input: { before: string; pageSize?: number; cursor?: string }): Promise<Page<ExpiredClaimCandidate>> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1152:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:30:    try {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1153:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:31:      const result = await this.client.send(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1154:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:32:        new QueryCommand({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1155:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:33:          TableName: this.tableName,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1156:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:34:          IndexName: "GSI6",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1157:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:35:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1158:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:36:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_CLAIMED, ":before": input.before },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1159:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:37:          Limit: input.pageSize ?? DEFAULT_PAGE_SIZE,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1160:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:38:          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1161:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:39:        }),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1162:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:40:      );
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1163:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:41:      // GSI6 is ALL-projected (infra/lib/dynamo-table.ts) - each row already IS the full
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1164:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:42:      // ReminderOccurrence item, no separate fetch needed.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1165:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:43:      const items = (result.Items ?? []) as ExpiredClaimCandidate[];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1166:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:44:      return { items, cursor: result.LastEvaluatedKey ? JSON.stringify(result.LastEvaluatedKey) : undefined };
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1167:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:45:    } catch (err) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1168:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:46:      throw mapDynamoError(err, "ReminderReconciliationCandidateSource.listExpiredClaims");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1169:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:47:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1170:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:48:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1171:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:49:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1172:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:50:  async listDstCandidates(input: { window: { start: string; end: string }; pageSize?: number; cursor?: string }): Promise<Page<DstReconciliationCandidate>> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1173:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:51:    try {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1174:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:52:      const result = await this.client.send(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1175:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:53:        new QueryCommand({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1176:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:54:          TableName: this.tableName,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1177:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:55:          IndexName: "GSI6",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1178:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:56:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK BETWEEN :start AND :end",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1179:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:57:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_DST_PENDING, ":start": input.window.start, ":end": input.window.end },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1180:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:58:          Limit: input.pageSize ?? DEFAULT_PAGE_SIZE,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1181:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:59:          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1182:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:60:        }),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1183:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:61:      );
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1184:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:62:      // GSI6 is ALL-projected - each row already IS the full ReminderOccurrence item.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1185:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:63:      const items = (result.Items ?? []) as DstReconciliationCandidate[];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1186:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:64:      return { items, cursor: result.LastEvaluatedKey ? JSON.stringify(result.LastEvaluatedKey) : undefined };
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1187:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:65:    } catch (err) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1188:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:66:      throw mapDynamoError(err, "ReminderReconciliationCandidateSource.listDstCandidates");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1189:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:67:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1190:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:68:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1191:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:69:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1322:src/shared/outbox/relay-store.ts:15:  markPublished(key: EntityKey): Promise<void>;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1351:src/workers/reminder-producer/producer.ts:26:import { GSI6PK_WORKSTATE_CLAIMED, buildExpiredClaimGsi6Sk } from "../../modules/reminder/ports/reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1373:src/workers/reminder-producer/producer.ts:48:  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (§9.5). */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1455:src/workers/reminder-producer/producer.ts:130:            // producer's own lookback window nor the daily DST reconciliation reliably
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1460:src/workers/reminder-producer/producer.ts:135:            // reconciliation job can find this claim if it expires unrecovered - removed
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1461:src/workers/reminder-producer/producer.ts:136:            // when ReminderDispatch advances to TRIGGERED or reconciliation reverts it.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1520:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:1:/**
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1521:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:2: * Real DynamoDB adapter for ReminderReconciliationCandidateSource (M3.5). Separate class,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1522:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:3: * only wired into the ReminderReconciliation Lambda's composition root - same isolation
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1523:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:4: * principle as DynamoDbReminderProducerStore. Queries GSI6's global (non-tenant-prefixed)
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1524:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:5: * WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING key families - see
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1525:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:6: * src/modules/reminder/ports/reconciliation-candidate-source.ts and
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1526:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:7: * docs/architecture/m3.5-runtime-design.md for why these keys are global.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1527:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:8: */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1528:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:9:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1529:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:10:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1530:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:11:import {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1531:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:12:  GSI6PK_WORKSTATE_CLAIMED,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1532:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:13:  GSI6PK_WORKSTATE_DST_PENDING,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1533:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:14:  type DstReconciliationCandidate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1534:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:15:  type ExpiredClaimCandidate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1535:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:16:  type Page,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1536:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:17:  type ReminderReconciliationCandidateSource,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1537:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:18:} from "../ports/reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1538:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:19:import { mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1539:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:20:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1540:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:21:const DEFAULT_PAGE_SIZE = 200;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1541:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:22:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1542:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:23:export class DynamoDbReminderReconciliationCandidateSource implements ReminderReconciliationCandidateSource {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1543:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:24:  constructor(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1544:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:25:    private readonly client: DynamoDBDocumentClient,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1545:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:26:    private readonly tableName: string,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1546:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:27:  ) {}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1547:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:28:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1548:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:29:  async listExpiredClaims(input: { before: string; pageSize?: number; cursor?: string }): Promise<Page<ExpiredClaimCandidate>> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1549:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:30:    try {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1550:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:31:      const result = await this.client.send(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1551:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:32:        new QueryCommand({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1552:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:33:          TableName: this.tableName,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1553:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:34:          IndexName: "GSI6",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1554:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:35:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1555:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:36:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_CLAIMED, ":before": input.before },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1556:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:37:          Limit: input.pageSize ?? DEFAULT_PAGE_SIZE,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1557:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:38:          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1558:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:39:        }),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1559:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:40:      );
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1560:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:41:      // GSI6 is ALL-projected (infra/lib/dynamo-table.ts) - each row already IS the full
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1561:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:42:      // ReminderOccurrence item, no separate fetch needed.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1562:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:43:      const items = (result.Items ?? []) as ExpiredClaimCandidate[];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1563:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:44:      return { items, cursor: result.LastEvaluatedKey ? JSON.stringify(result.LastEvaluatedKey) : undefined };
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1564:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:45:    } catch (err) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1565:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:46:      throw mapDynamoError(err, "ReminderReconciliationCandidateSource.listExpiredClaims");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1566:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:47:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1567:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:48:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1568:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:49:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1569:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:50:  async listDstCandidates(input: { window: { start: string; end: string }; pageSize?: number; cursor?: string }): Promise<Page<DstReconciliationCandidate>> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1570:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:51:    try {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1571:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:52:      const result = await this.client.send(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1572:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:53:        new QueryCommand({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1573:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:54:          TableName: this.tableName,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1574:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:55:          IndexName: "GSI6",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1575:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:56:          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK BETWEEN :start AND :end",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1576:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:57:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_DST_PENDING, ":start": input.window.start, ":end": input.window.end },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1577:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:58:          Limit: input.pageSize ?? DEFAULT_PAGE_SIZE,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1578:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:59:          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1579:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:60:        }),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1580:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:61:      );
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1581:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:62:      // GSI6 is ALL-projected - each row already IS the full ReminderOccurrence item.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1582:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:63:      const items = (result.Items ?? []) as DstReconciliationCandidate[];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1583:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:64:      return { items, cursor: result.LastEvaluatedKey ? JSON.stringify(result.LastEvaluatedKey) : undefined };
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1584:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:65:    } catch (err) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1585:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:66:      throw mapDynamoError(err, "ReminderReconciliationCandidateSource.listDstCandidates");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1586:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:67:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1587:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:68:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1588:src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:69:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1743:src/workers/dispatch-outbox-relay/relay.ts:72:  await deps.store.markPublished({ PK: record.PK, SK: record.SK });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1867:src/shared/outbox/relay-store.ts:15:  markPublished(key: EntityKey): Promise<void>;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1896:src/workers/reminder-producer/producer.ts:26:import { GSI6PK_WORKSTATE_CLAIMED, buildExpiredClaimGsi6Sk } from "../../modules/reminder/ports/reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:1918:src/workers/reminder-producer/producer.ts:48:  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (§9.5). */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2000:src/workers/reminder-producer/producer.ts:130:            // producer's own lookback window nor the daily DST reconciliation reliably
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2005:src/workers/reminder-producer/producer.ts:135:            // reconciliation job can find this claim if it expires unrecovered - removed
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2006:src/workers/reminder-producer/producer.ts:136:            // when ReminderDispatch advances to TRIGGERED or reconciliation reverts it.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2065:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^" src/workers/dispatch-outbox-relay/relay.ts src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts src/runtime/aws/handlers/dispatch-outbox-relay-handler.ts src/runtime/aws/handlers/outbox-sweeper-handler.ts src/runtime/aws/handlers/reminder-dispatch-handler.ts src/runtime/aws/handlers/reminder-producer-handler.ts src/runtime/aws/handlers/reminder-reconciliation-handler.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2096:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:1:/** Real handler for ReminderReconciliation (EventBridge Scheduler, `mode` field), replacing
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2097:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:2: * the 501 placeholder. Two schedules invoke the same Lambda with different `detail.mode`
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2098:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:3: * (m3.5-runtime-design.md §"Reconciliação"): CLAIMS (every 5 min) and DST (daily). Also one
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2099:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:4: * of exactly two roles granted gsi6Read() - see infra/lib/dynamo-table.ts. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2100:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:5:import type { ScheduledEvent } from "aws-lambda";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2101:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:6:import { createDocumentClient } from "../../../shared/dynamodb/client.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2102:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:7:import { buildReconciliationDeps } from "../composition/reminder.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2103:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:8:import { runReconciliation, type DstReconciliationCandidate as FullDstCandidate } from "../../../workers/reminder-reconciliation/reconciliation.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2104:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:9:import { itemKey } from "../../../modules/expiration/domain/expiration-item.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2105:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:10:import { policyKey, type ReminderPolicy } from "../../../modules/reminder/domain/reminder-policy.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2106:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:11:import { defaultShardConfig } from "../../../modules/reminder/domain/shard-config.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2107:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:12:import type { ReminderOccurrence } from "../../../modules/reminder/domain/reminder-occurrence.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2108:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:13:import { SecureLogger } from "../../../shared/observability/logger.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2109:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:14:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2110:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:15:const client = createDocumentClient();
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2111:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:16:const envTableName = process.env["TABLE_NAME"];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2112:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:17:if (!envTableName) throw new Error("TABLE_NAME env var is required.");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2113:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:18:const tableName: string = envTableName;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2114:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:19:const { store, candidateSource, now } = buildReconciliationDeps(client, tableName);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2115:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:20:const shardConfig = defaultShardConfig();
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2116:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:21:const logger = new SecureLogger({ baseContext: { service: "reminder-reconciliation" } });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2117:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:22:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2118:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:23:interface ReconciliationEventDetail {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2119:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:24:  mode: "CLAIMS" | "DST";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2120:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:25:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2121:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:26:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2122:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:27:export async function handler(event: ScheduledEvent<ReconciliationEventDetail>): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2123:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:28:  const mode = event.detail.mode;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2124:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:29:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2125:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:30:  let expiredClaimCandidates: ReminderOccurrence[] = [];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2126:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:31:  const dstCandidates: FullDstCandidate[] = [];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2127:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:32:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2128:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:33:  if (mode === "CLAIMS") {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2129:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:34:    // GSI6 is ALL-projected - each candidate row already IS the full ReminderOccurrence
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2130:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:35:    // item (see ExpiredClaimCandidate's doc comment), so no extra fetch is needed.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2131:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:36:    const page = await candidateSource.listExpiredClaims({ before: now() });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2132:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:37:    expiredClaimCandidates = page.items as unknown as ReminderOccurrence[];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2133:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:38:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2134:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:39:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2135:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:40:  if (mode === "DST") {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2136:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:41:    // ReminderMaterializer (M3.5) writes a WORKSTATE#DST_PENDING GSI6 pointer whenever a
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2137:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:42:    // trigger's schedule was computed at an ambiguous/nonexistent local time (dstKind !==
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2138:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:43:    // "NORMAL") - see reminder-materializer.ts. This query finds those candidates for
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2139:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:44:    // re-evaluation; occurrences that were never DST-ambiguous never appear here.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2140:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:45:    const windowStart = now();
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2141:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:46:    const windowEnd = new Date(Date.parse(windowStart) + 7 * 24 * 60 * 60_000).toISOString();
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2142:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:47:    const page = await candidateSource.listDstCandidates({ window: { start: windowStart, end: windowEnd } });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2143:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:48:    for (const light of page.items) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2144:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:49:      const policy = await store.get<ReminderPolicy>(policyKey(light.tenantId, light.policyId));
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2145:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:50:      if (!policy) continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2146:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:51:      const item = await store.get<{ PK: string; SK: string; dueDate: string; version: number }>(itemKey(light.tenantId, light.itemId));
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2147:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:52:      if (!item) continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2148:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:53:      dstCandidates.push({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2149:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:54:        tenantId: light.tenantId,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2150:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:55:        itemId: light.itemId,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2151:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:56:        itemVersion: item.version,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2152:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:57:        itemDueDate: item.dueDate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2153:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:58:        policy,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2154:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:59:      });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2155:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:60:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2156:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:61:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2157:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:62:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2158:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:63:  const result = await runReconciliation({ store, tableName, now, shardConfig }, { expiredClaimCandidates, dstCandidates });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2159:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:64:  logger.info("reminder-reconciliation complete", { mode, ...result });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2160:src/runtime/aws/handlers/reminder-reconciliation-handler.ts:65:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2296:src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts:35:  async markPublished(key: EntityKey): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2308:src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts:47:      throw mapDynamoError(err, "OutboxRelayStore.markPublished");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2412:src/workers/dispatch-outbox-relay/relay.ts:72:  await deps.store.markPublished({ PK: record.PK, SK: record.SK });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2463:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "GSI6|remove|DST|CLAIMED|TRIGGERED|SCHEDULED" src/workers/reminder-dispatch/dispatch.ts src/workers/reminder-reconciliation/reconciliation.ts src/modules/reminder/ports/reconciliation-candidate-source.ts docs/architecture/m3.5-runtime-design.md infra/lib/expiration-tracker-stack.ts infra/lib/dynamo-table.ts infra/lib/scoped-lambda-function.ts test/infra/stack.test.ts test/unit/dispatch-outbox-relay/relay.test.ts test/unit/reminder/reminder-materializer.test.ts test/integration/reminder-engine.test.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2471:test/integration/reminder-engine.test.ts:197:  it("claim-expiry reconciliation reverts an unconfirmed claim to SCHEDULED so the next producer tick can re-claim it", async () => {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2477:test/unit/reminder/reminder-materializer.test.ts:112:  describe("M3.5: GSI6 WORKSTATE#DST_PENDING pointer lifecycle", () => {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2479:test/unit/reminder/reminder-materializer.test.ts:128:      expect(occ.GSI6PK).toBe("WORKSTATE#DST_PENDING");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2492:infra/lib/scoped-lambda-function.ts:97:    /** GSI6 (reconciliation/retention) read-only - reserved EXCLUSIVELY for
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2493:infra/lib/scoped-lambda-function.ts:104:        description: "GSI6 (reconciliation) read-only - ReminderReconciliation/OutboxSweeperReminderDispatch only",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2501:infra/lib/dynamo-table.ts:13: * families - `WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING` (reminder reconciliation
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2511:infra/lib/dynamo-table.ts:176:   * `ReminderReconciliation` (WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING candidates) and
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2520:docs/architecture/m3.5-runtime-design.md:27:- Remoção das chaves `GSI6PK`/`GSI6SK` (`WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING`) ocorre **atomicamente na mesma `TransactWriteItems`/`UpdateItem` da mudança de estado correspondente** (claim→triggered, claim revertido, janela DST processada) — nunca como passo separado que pode falhar independente e deixar o índice inconsistente com o estado real.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2522:docs/architecture/m3.5-runtime-design.md:33:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#POLICY#<policyId> — projeção: PK, SK, tenantId, policyId, timeZone, windowStart, windowEnd. Filtro: windowStart dentro da janela de 7 dias já usada por M3. Paginação: idêntica. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2524:docs/architecture/m3.5-runtime-design.md:40:- `GSI6PK="WORKSTATE#DST_PENDING"`/`GSI6SK` gravados pelo `ReminderMaterializer` (M3, já existente) ao criar/atualizar uma `ReminderOccurrence` cuja janela de recorrência cruza uma transição de DST conhecida (mesma lógica IANA-aware de M3, só passa a persistir o resultado como candidato indexado em vez de recalcular do zero); removidos quando a reconciliação DST processa a janela.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2525:docs/architecture/m3.5-runtime-design.md:42:Namespace `WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING` (global, sem tenant) não colide com `RECON#OUTBOX#PENDING` (global, outbox) nem com `TENANT#t#WORKSTATE#<status>` de outros domínios (Document/Upload, M5+, que continuam tenant-scoped por não precisarem de reconciliação cross-tenant) — são famílias de chave `GSI6PK` distintas por prefixo, coexistindo no mesmo índice físico. Port separado de `ReminderStore` (mesmo princípio de isolamento que levou a `ReminderProducerStore` a ser separado — não dar a rotas tenant-facing acesso a uma query que elas não precisam). Isso muda o escopo original: o "wiring real do GSI6" (item 4 da lista de pendências pós-M3) deixa de ser follow-up separado e entra neste milestone — sem ele, a EventBridge Rule/Scheduler de reconciliação (item 4 do escopo de G8) dispara sobre nada.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2551:src/workers/reminder-reconciliation/reconciliation.ts:5: * and §9.5 itself (DST):
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2552:src/workers/reminder-reconciliation/reconciliation.ts:7: *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2553:src/workers/reminder-reconciliation/reconciliation.ts:9: *      SCHEDULED, conditionally, so the next producer tick's lookback window picks it up
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2554:src/workers/reminder-reconciliation/reconciliation.ts:11: *  (b) DST-affected re-evaluation (§9.5 steps 1-6): for each (item, enabled policy) pair
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2555:src/workers/reminder-reconciliation/reconciliation.ts:14: *      materialized, cancel divergent SCHEDULED occurrences and materialize missing ones
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2556:src/workers/reminder-reconciliation/reconciliation.ts:15: *      idempotently - TRIGGERED/ACKED history is never touched.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2557:src/workers/reminder-reconciliation/reconciliation.ts:18: * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2558:src/workers/reminder-reconciliation/reconciliation.ts:35:/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2559:src/workers/reminder-reconciliation/reconciliation.ts:36: * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2560:src/workers/reminder-reconciliation/reconciliation.ts:54:/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2561:src/workers/reminder-reconciliation/reconciliation.ts:62:    if (occurrence.status !== "CLAIMED") continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2562:src/workers/reminder-reconciliation/reconciliation.ts:72:            set: { status: "SCHEDULED" },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2563:src/workers/reminder-reconciliation/reconciliation.ts:73:            // M3.5: this claim is resolved (reverted) - its WORKSTATE#CLAIMED GSI6 pointer
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2564:src/workers/reminder-reconciliation/reconciliation.ts:75:            remove: ["GSI6PK", "GSI6SK"],
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2565:src/workers/reminder-reconciliation/reconciliation.ts:87:/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (§9.5 steps 1-6). */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2566:src/workers/reminder-reconciliation/reconciliation.ts:109:        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2567:src/workers/reminder-reconciliation/reconciliation.ts:128:    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2568:src/workers/reminder-reconciliation/reconciliation.ts:144:              remove: ["GSI6PK", "GSI6SK"],
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2569:src/workers/reminder-reconciliation/reconciliation.ts:154:    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2570:src/modules/reminder/ports/reconciliation-candidate-source.ts:8: * Backed by GSI6 with GLOBAL (non-tenant-prefixed) partition keys - same structural
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2571:src/modules/reminder/ports/reconciliation-candidate-source.ts:10: * know a priori which tenants have an expired claim or a DST candidate. `tenantId` is
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2572:src/modules/reminder/ports/reconciliation-candidate-source.ts:11: * preserved in GSI6SK for context reconstruction. Exactly two IAM roles may query GSI6:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2573:src/modules/reminder/ports/reconciliation-candidate-source.ts:16:/** GSI6 has ALL projection (unlike GSI3's KEYS_ONLY) - a queried row already IS the full
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2574:src/modules/reminder/ports/reconciliation-candidate-source.ts:30:/** Same ALL-projection reasoning as ExpiredClaimCandidate: the DST_PENDING pointer is
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2575:src/modules/reminder/ports/reconciliation-candidate-source.ts:32: * occurrence's schedule crosses a known DST transition), not on the ReminderPolicy - a
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2576:src/modules/reminder/ports/reconciliation-candidate-source.ts:49:  /** GSI6PK="WORKSTATE#CLAIMED" (global), GSI6SK=<claimExpiresAt>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2577:src/modules/reminder/ports/reconciliation-candidate-source.ts:51:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2578:src/modules/reminder/ports/reconciliation-candidate-source.ts:55:export const GSI6PK_WORKSTATE_CLAIMED = "WORKSTATE#CLAIMED";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2579:src/modules/reminder/ports/reconciliation-candidate-source.ts:56:export const GSI6PK_WORKSTATE_DST_PENDING = "WORKSTATE#DST_PENDING";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2582:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"OutboxPublisher|destination|RECON#OUTBOX#PENDING|markPublished|gsi6Read\\(\" src infra test --glob \"*.ts\"" in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2586:src\modules\reminder\ports\reconciliation-candidate-source.ts:12: * ReminderReconciliation (this port) and OutboxSweeperReminderDispatch (RECON#OUTBOX#PENDING,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2596:src\workers\dispatch-outbox-relay\relay.ts:72:  await deps.store.markPublished({ PK: record.PK, SK: record.SK });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2600:src\runtime\aws\handlers\reminder-reconciliation-handler.ts:4: * of exactly two roles granted gsi6Read() - see infra/lib/dynamo-table.ts. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2604:test\unit\dispatch-outbox-relay\relay.test.ts:46:  async markPublished(key: { PK: string; SK: string }): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2608:src\shared\outbox\relay-store.ts:15:  markPublished(key: EntityKey): Promise<void>;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2624:src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:35:  async markPublished(key: EntityKey): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2625:src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:47:      throw mapDynamoError(err, "OutboxRelayStore.markPublished");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2803:test/infra/stack.test.ts:88:  it("M3: creates the ReminderPolicy HTTP handler and the three reminder workers (producer/dispatch/reconciliation)", () => {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2824:test/infra/stack.test.ts:109:    // policy (items handler, reminders handler, dispatch, reconciliation, test route) must
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2880:test/infra/stack.test.ts:165:    expect(Object.keys(schedules).length).toBe(4); // producer, claims-reconciliation, dst-reconciliation, sweeper
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:2961:infra/lib/dynamo-table.ts:13: * families - `WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING` (reminder reconciliation
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3124:infra/lib/dynamo-table.ts:176:   * `ReminderReconciliation` (WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING candidates) and
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3267:infra/lib/expiration-tracker-stack.ts:129:      entry: `${HANDLERS_DIR}/reminder-reconciliation-handler.ts`,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3416:infra/lib/scoped-lambda-function.ts:97:    /** GSI6 (reconciliation/retention) read-only - reserved EXCLUSIVELY for
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3423:infra/lib/scoped-lambda-function.ts:104:        description: "GSI6 (reconciliation) read-only - ReminderReconciliation/OutboxSweeperReminderDispatch only",
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3541:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "DynamoDb|reminder-producer-handler|reminder-reconciliation-handler|ScheduledEvent|markPublished|REMOVE GSI6|UpdateExpression|DST_PENDING|dstKind" test src/runtime/aws/handlers infra/lib/reminder-schedule.ts src/workers/reminder-reconciliation/reconciliation.ts src/workers/reminder-dispatch/dispatch.ts; rg -n "''^" src/runtime/aws/composition/reminder.ts src/modules/reminder/ports/reconciliation-candidate-source.ts test/unit/dispatch-outbox-relay/relay.test.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3543:src/runtime/aws/handlers\reminder-reconciliation-handler.ts:5:import type { ScheduledEvent } from "aws-lambda";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3544:src/runtime/aws/handlers\reminder-reconciliation-handler.ts:27:export async function handler(event: ScheduledEvent<ReconciliationEventDetail>): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3545:src/runtime/aws/handlers\reminder-reconciliation-handler.ts:41:    // ReminderMaterializer (M3.5) writes a WORKSTATE#DST_PENDING GSI6 pointer whenever a
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3546:src/runtime/aws/handlers\reminder-reconciliation-handler.ts:42:    // trigger's schedule was computed at an ambiguous/nonexistent local time (dstKind !==
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3549:test\unit\dispatch-outbox-relay\relay.test.ts:46:  async markPublished(key: { PK: string; SK: string }): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3553:test\unit\reminder\reminder-materializer.test.ts:112:  describe("M3.5: GSI6 WORKSTATE#DST_PENDING pointer lifecycle", () => {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3554:test\unit\reminder\reminder-materializer.test.ts:128:      expect(occ.GSI6PK).toBe("WORKSTATE#DST_PENDING");
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3555:src/modules/reminder/ports/reconciliation-candidate-source.ts:1:/**
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3556:src/modules/reminder/ports/reconciliation-candidate-source.ts:2: * Narrow port for the ReminderReconciliation worker (M3.5, docs/architecture/m3.5-runtime-design.md
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3557:src/modules/reminder/ports/reconciliation-candidate-source.ts:3: * "Reconciliação: precisa de fonte real de candidatos"). Deliberately SEPARATE from
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3558:src/modules/reminder/ports/reconciliation-candidate-source.ts:4: * ReminderStore - same isolation principle that split ReminderProducerStore off in M3: a
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3559:src/modules/reminder/ports/reconciliation-candidate-source.ts:5: * query surface that only a privileged, non-tenant-facing worker needs shouldn't be
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3560:src/modules/reminder/ports/reconciliation-candidate-source.ts:6: * reachable from tenant-facing code.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3561:src/modules/reminder/ports/reconciliation-candidate-source.ts:7: *
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3562:src/modules/reminder/ports/reconciliation-candidate-source.ts:8: * Backed by GSI6 with GLOBAL (non-tenant-prefixed) partition keys - same structural
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3563:src/modules/reminder/ports/reconciliation-candidate-source.ts:9: * exception as GSI3 (data-model.md §3), for the same reason: the reconciliation job doesn't
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3564:src/modules/reminder/ports/reconciliation-candidate-source.ts:10: * know a priori which tenants have an expired claim or a DST candidate. `tenantId` is
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3565:src/modules/reminder/ports/reconciliation-candidate-source.ts:11: * preserved in GSI6SK for context reconstruction. Exactly two IAM roles may query GSI6:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3566:src/modules/reminder/ports/reconciliation-candidate-source.ts:12: * ReminderReconciliation (this port) and OutboxSweeperReminderDispatch (RECON#OUTBOX#PENDING,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3567:src/modules/reminder/ports/reconciliation-candidate-source.ts:13: * shared/outbox) - see infra/lib/dynamo-table.ts `grantGsi6ReadTo`.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3568:src/modules/reminder/ports/reconciliation-candidate-source.ts:14: */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3569:src/modules/reminder/ports/reconciliation-candidate-source.ts:15:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3570:src/modules/reminder/ports/reconciliation-candidate-source.ts:16:/** GSI6 has ALL projection (unlike GSI3's KEYS_ONLY) - a queried row already IS the full
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3571:src/modules/reminder/ports/reconciliation-candidate-source.ts:17: * ReminderOccurrence item (PK/SK/status/claimExpiresAt/version/...), so the candidate type
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3572:src/modules/reminder/ports/reconciliation-candidate-source.ts:18: * is intentionally the same shape rather than a stripped-down reference: no extra
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3573:src/modules/reminder/ports/reconciliation-candidate-source.ts:19: * store.get roundtrip is needed to act on a claim-expiry candidate. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3574:src/modules/reminder/ports/reconciliation-candidate-source.ts:20:export type ExpiredClaimCandidate = Record<string, unknown> & {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3575:src/modules/reminder/ports/reconciliation-candidate-source.ts:21:  PK: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3576:src/modules/reminder/ports/reconciliation-candidate-source.ts:22:  SK: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3577:src/modules/reminder/ports/reconciliation-candidate-source.ts:23:  tenantId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3578:src/modules/reminder/ports/reconciliation-candidate-source.ts:24:  occurrenceId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3579:src/modules/reminder/ports/reconciliation-candidate-source.ts:25:  claimExpiresAt: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3580:src/modules/reminder/ports/reconciliation-candidate-source.ts:26:  version: number;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3581:src/modules/reminder/ports/reconciliation-candidate-source.ts:27:  status: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3582:src/modules/reminder/ports/reconciliation-candidate-source.ts:28:};
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3583:src/modules/reminder/ports/reconciliation-candidate-source.ts:29:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3584:src/modules/reminder/ports/reconciliation-candidate-source.ts:30:/** Same ALL-projection reasoning as ExpiredClaimCandidate: the DST_PENDING pointer is
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3585:src/modules/reminder/ports/reconciliation-candidate-source.ts:31: * written on the ReminderOccurrence row itself (by ReminderMaterializer, when a materialized
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3586:src/modules/reminder/ports/reconciliation-candidate-source.ts:32: * occurrence's schedule crosses a known DST transition), not on the ReminderPolicy - a
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3587:src/modules/reminder/ports/reconciliation-candidate-source.ts:33: * (occurrence, policy) pair is what needs re-evaluating, and the occurrence row already
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3588:src/modules/reminder/ports/reconciliation-candidate-source.ts:34: * carries itemId/policyId/itemVersion/policyVersion needed to resolve the full pair. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3589:src/modules/reminder/ports/reconciliation-candidate-source.ts:35:export type DstReconciliationCandidate = Record<string, unknown> & {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3590:src/modules/reminder/ports/reconciliation-candidate-source.ts:36:  PK: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3591:src/modules/reminder/ports/reconciliation-candidate-source.ts:37:  SK: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3592:src/modules/reminder/ports/reconciliation-candidate-source.ts:38:  tenantId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3593:src/modules/reminder/ports/reconciliation-candidate-source.ts:39:  itemId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3594:src/modules/reminder/ports/reconciliation-candidate-source.ts:40:  policyId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3595:src/modules/reminder/ports/reconciliation-candidate-source.ts:41:};
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3596:src/modules/reminder/ports/reconciliation-candidate-source.ts:42:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3597:src/modules/reminder/ports/reconciliation-candidate-source.ts:43:export interface Page<T> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3598:src/modules/reminder/ports/reconciliation-candidate-source.ts:44:  items: T[];
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3599:src/modules/reminder/ports/reconciliation-candidate-source.ts:45:  cursor?: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3600:src/modules/reminder/ports/reconciliation-candidate-source.ts:46:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3601:src/modules/reminder/ports/reconciliation-candidate-source.ts:47:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3602:src/modules/reminder/ports/reconciliation-candidate-source.ts:48:export interface ReminderReconciliationCandidateSource {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3603:src/modules/reminder/ports/reconciliation-candidate-source.ts:49:  /** GSI6PK="WORKSTATE#CLAIMED" (global), GSI6SK=<claimExpiresAt>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3604:src/modules/reminder/ports/reconciliation-candidate-source.ts:50:  listExpiredClaims(input: { before: string; pageSize?: number; cursor?: string }): Promise<Page<ExpiredClaimCandidate>>;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3605:src/modules/reminder/ports/reconciliation-candidate-source.ts:51:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3606:src/modules/reminder/ports/reconciliation-candidate-source.ts:52:  listDstCandidates(input: { window: { start: string; end: string }; pageSize?: number; cursor?: string }): Promise<Page<DstReconciliationCandidate>>;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3607:src/modules/reminder/ports/reconciliation-candidate-source.ts:53:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3608:src/modules/reminder/ports/reconciliation-candidate-source.ts:54:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3609:src/modules/reminder/ports/reconciliation-candidate-source.ts:55:export const GSI6PK_WORKSTATE_CLAIMED = "WORKSTATE#CLAIMED";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3610:src/modules/reminder/ports/reconciliation-candidate-source.ts:56:export const GSI6PK_WORKSTATE_DST_PENDING = "WORKSTATE#DST_PENDING";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3611:src/modules/reminder/ports/reconciliation-candidate-source.ts:57:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3612:src/modules/reminder/ports/reconciliation-candidate-source.ts:58:export function buildExpiredClaimGsi6Sk(claimExpiresAt: string, tenantId: string, occurrenceId: string): string {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3613:src/modules/reminder/ports/reconciliation-candidate-source.ts:59:  return `${claimExpiresAt}#TENANT#${tenantId}#OCCURRENCE#${occurrenceId}`;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3614:src/modules/reminder/ports/reconciliation-candidate-source.ts:60:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3615:src/modules/reminder/ports/reconciliation-candidate-source.ts:61:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3616:src/modules/reminder/ports/reconciliation-candidate-source.ts:62:export function buildDstCandidateGsi6Sk(windowStart: string, tenantId: string, occurrenceId: string): string {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3617:src/modules/reminder/ports/reconciliation-candidate-source.ts:63:  return `${windowStart}#TENANT#${tenantId}#OCCURRENCE#${occurrenceId}`;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3618:src/modules/reminder/ports/reconciliation-candidate-source.ts:64:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3624:src/runtime/aws/composition/reminder.ts:6:import { DynamoDbReminderReconciliationCandidateSource } from "../../../modules/reminder/persistence/dynamodb-reconciliation-candidate-source.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3722:test/unit/dispatch-outbox-relay/relay.test.ts:46:  async markPublished(key: { PK: string; SK: string }): Promise<void> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3823:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^" src/workers/reminder-reconciliation/reconciliation.ts src/workers/reminder-dispatch/dispatch.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3834:src/workers/reminder-dispatch/dispatch.ts:10: * silently dropped, and repair is left to reconciliation (§9.5) - never a new ad-hoc
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:3934:src/workers/reminder-dispatch/dispatch.ts:110:            // reconciliation job find a claim that's already been handled.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4044:src/workers/reminder-reconciliation/reconciliation.ts:1:/**
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4045:src/workers/reminder-reconciliation/reconciliation.ts:2: * Reconciliation - implementation-blueprint.md §9.5, run as ONE job/Lambda serving TWO
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4046:src/workers/reminder-reconciliation/reconciliation.ts:3: * purposes (per the task brief: "don't build two separate reconciliation paths"), both
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4047:src/workers/reminder-reconciliation/reconciliation.ts:4: * documented in §9.3 point 4 ("claims expirados... via reconciliação, mesmo job da §9.5")
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4048:src/workers/reminder-reconciliation/reconciliation.ts:5: * and §9.5 itself (DST):
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4049:src/workers/reminder-reconciliation/reconciliation.ts:6: *
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4050:src/workers/reminder-reconciliation/reconciliation.ts:7: *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4051:src/workers/reminder-reconciliation/reconciliation.ts:8: *      `claimExpiresAt` (dispatch worker crashed/timed out before completing) reverts to
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4052:src/workers/reminder-reconciliation/reconciliation.ts:9: *      SCHEDULED, conditionally, so the next producer tick's lookback window picks it up
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4053:src/workers/reminder-reconciliation/reconciliation.ts:10: *      again;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4054:src/workers/reminder-reconciliation/reconciliation.ts:11: *  (b) DST-affected re-evaluation (§9.5 steps 1-6): for each (item, enabled policy) pair
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4055:src/workers/reminder-reconciliation/reconciliation.ts:12: *      in the reconciliation batch, recompute the expected schedule from the policy's
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4056:src/workers/reminder-reconciliation/reconciliation.ts:13: *      IANA timeZone + rule against a `[now, now+7d]` window, compare to what's actually
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4057:src/workers/reminder-reconciliation/reconciliation.ts:14: *      materialized, cancel divergent SCHEDULED occurrences and materialize missing ones
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4058:src/workers/reminder-reconciliation/reconciliation.ts:15: *      idempotently - TRIGGERED/ACKED history is never touched.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4059:src/workers/reminder-reconciliation/reconciliation.ts:16: *
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4060:src/workers/reminder-reconciliation/reconciliation.ts:17: * Both passes reuse the same store/materializer/clock and are invoked together by
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4061:src/workers/reminder-reconciliation/reconciliation.ts:18: * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4062:src/workers/reminder-reconciliation/reconciliation.ts:19: * unblocks the producer sooner).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4063:src/workers/reminder-reconciliation/reconciliation.ts:20: */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4064:src/workers/reminder-reconciliation/reconciliation.ts:21:import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4065:src/workers/reminder-reconciliation/reconciliation.ts:22:import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4066:src/workers/reminder-reconciliation/reconciliation.ts:23:import { ReminderMaterializer } from "../../modules/reminder/application/reminder-materializer.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4067:src/workers/reminder-reconciliation/reconciliation.ts:24:import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4068:src/workers/reminder-reconciliation/reconciliation.ts:25:import type { ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4069:src/workers/reminder-reconciliation/reconciliation.ts:26:import type { ShardConfig } from "../../modules/reminder/domain/shard-config.js";
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4070:src/workers/reminder-reconciliation/reconciliation.ts:27:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4071:src/workers/reminder-reconciliation/reconciliation.ts:28:export interface ReconciliationDeps {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4072:src/workers/reminder-reconciliation/reconciliation.ts:29:  store: ReminderStore;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4073:src/workers/reminder-reconciliation/reconciliation.ts:30:  tableName: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4074:src/workers/reminder-reconciliation/reconciliation.ts:31:  now: () => string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4075:src/workers/reminder-reconciliation/reconciliation.ts:32:  shardConfig: ShardConfig;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4076:src/workers/reminder-reconciliation/reconciliation.ts:33:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4077:src/workers/reminder-reconciliation/reconciliation.ts:34:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4078:src/workers/reminder-reconciliation/reconciliation.ts:35:/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4079:src/workers/reminder-reconciliation/reconciliation.ts:36: * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4080:src/workers/reminder-reconciliation/reconciliation.ts:37: * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4081:src/workers/reminder-reconciliation/reconciliation.ts:38: * wiring); tests/callers supply the batch directly. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4082:src/workers/reminder-reconciliation/reconciliation.ts:39:export interface DstReconciliationCandidate {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4083:src/workers/reminder-reconciliation/reconciliation.ts:40:  tenantId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4084:src/workers/reminder-reconciliation/reconciliation.ts:41:  itemId: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4085:src/workers/reminder-reconciliation/reconciliation.ts:42:  itemVersion: number;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4086:src/workers/reminder-reconciliation/reconciliation.ts:43:  itemDueDate: string;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4087:src/workers/reminder-reconciliation/reconciliation.ts:44:  policy: ReminderPolicy;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4088:src/workers/reminder-reconciliation/reconciliation.ts:45:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4089:src/workers/reminder-reconciliation/reconciliation.ts:46:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4090:src/workers/reminder-reconciliation/reconciliation.ts:47:export interface ReconciliationResult {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4091:src/workers/reminder-reconciliation/reconciliation.ts:48:  claimsReverted: number;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4092:src/workers/reminder-reconciliation/reconciliation.ts:49:  dstCancelled: number;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4093:src/workers/reminder-reconciliation/reconciliation.ts:50:  dstCreated: number;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4094:src/workers/reminder-reconciliation/reconciliation.ts:51:  dstDivergences: number;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4095:src/workers/reminder-reconciliation/reconciliation.ts:52:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4096:src/workers/reminder-reconciliation/reconciliation.ts:53:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4097:src/workers/reminder-reconciliation/reconciliation.ts:54:/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4098:src/workers/reminder-reconciliation/reconciliation.ts:55:export async function reconcileExpiredClaims(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4099:src/workers/reminder-reconciliation/reconciliation.ts:56:  deps: ReconciliationDeps,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4100:src/workers/reminder-reconciliation/reconciliation.ts:57:  candidateOccurrences: ReminderOccurrence[],
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4101:src/workers/reminder-reconciliation/reconciliation.ts:58:): Promise<number> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4102:src/workers/reminder-reconciliation/reconciliation.ts:59:  let reverted = 0;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4103:src/workers/reminder-reconciliation/reconciliation.ts:60:  const now = deps.now();
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4104:src/workers/reminder-reconciliation/reconciliation.ts:61:  for (const occurrence of candidateOccurrences) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4105:src/workers/reminder-reconciliation/reconciliation.ts:62:    if (occurrence.status !== "CLAIMED") continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4106:src/workers/reminder-reconciliation/reconciliation.ts:63:    if (!occurrence.claimExpiresAt || occurrence.claimExpiresAt > now) continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4107:src/workers/reminder-reconciliation/reconciliation.ts:64:    try {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4108:src/workers/reminder-reconciliation/reconciliation.ts:65:      await deps.store.transactWrite([
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4109:src/workers/reminder-reconciliation/reconciliation.ts:66:        {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4110:src/workers/reminder-reconciliation/reconciliation.ts:67:          Update: buildVersionedUpdate({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4111:src/workers/reminder-reconciliation/reconciliation.ts:68:            tableName: deps.tableName,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4112:src/workers/reminder-reconciliation/reconciliation.ts:69:            key: { PK: occurrence.PK, SK: occurrence.SK },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4113:src/workers/reminder-reconciliation/reconciliation.ts:70:            tenantId: occurrence.tenantId,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4114:src/workers/reminder-reconciliation/reconciliation.ts:71:            expectedVersion: occurrence.version,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4115:src/workers/reminder-reconciliation/reconciliation.ts:72:            set: { status: "SCHEDULED" },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4116:src/workers/reminder-reconciliation/reconciliation.ts:73:            // M3.5: this claim is resolved (reverted) - its WORKSTATE#CLAIMED GSI6 pointer
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4117:src/workers/reminder-reconciliation/reconciliation.ts:74:            // must go too, or the next reconciliation pass would find it again forever.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4118:src/workers/reminder-reconciliation/reconciliation.ts:75:            remove: ["GSI6PK", "GSI6SK"],
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4119:src/workers/reminder-reconciliation/reconciliation.ts:76:          }),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4120:src/workers/reminder-reconciliation/reconciliation.ts:77:        },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4121:src/workers/reminder-reconciliation/reconciliation.ts:78:      ]);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4122:src/workers/reminder-reconciliation/reconciliation.ts:79:      reverted += 1;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4123:src/workers/reminder-reconciliation/reconciliation.ts:80:    } catch (err) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4124:src/workers/reminder-reconciliation/reconciliation.ts:81:      if (!isTransactionCanceled(err)) throw err;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4125:src/workers/reminder-reconciliation/reconciliation.ts:82:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4126:src/workers/reminder-reconciliation/reconciliation.ts:83:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4127:src/workers/reminder-reconciliation/reconciliation.ts:84:  return reverted;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4128:src/workers/reminder-reconciliation/reconciliation.ts:85:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4129:src/workers/reminder-reconciliation/reconciliation.ts:86:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4130:src/workers/reminder-reconciliation/reconciliation.ts:87:/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (§9.5 steps 1-6). */
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4131:src/workers/reminder-reconciliation/reconciliation.ts:88:export async function reconcileDst(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4132:src/workers/reminder-reconciliation/reconciliation.ts:89:  deps: ReconciliationDeps,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4133:src/workers/reminder-reconciliation/reconciliation.ts:90:  candidates: DstReconciliationCandidate[],
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4134:src/workers/reminder-reconciliation/reconciliation.ts:91:): Promise<{ cancelled: number; created: number; divergences: number }> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4135:src/workers/reminder-reconciliation/reconciliation.ts:92:  const materializer = new ReminderMaterializer(deps.store, deps.tableName, deps.now);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4136:src/workers/reminder-reconciliation/reconciliation.ts:93:  const windowStart = Date.parse(deps.now());
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4137:src/workers/reminder-reconciliation/reconciliation.ts:94:  const windowEnd = windowStart + 7 * 24 * 60 * 60_000;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4138:src/workers/reminder-reconciliation/reconciliation.ts:95:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4139:src/workers/reminder-reconciliation/reconciliation.ts:96:  let cancelled = 0;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4140:src/workers/reminder-reconciliation/reconciliation.ts:97:  let created = 0;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4141:src/workers/reminder-reconciliation/reconciliation.ts:98:  let divergences = 0;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4142:src/workers/reminder-reconciliation/reconciliation.ts:99:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4143:src/workers/reminder-reconciliation/reconciliation.ts:100:  for (const candidate of candidates) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4144:src/workers/reminder-reconciliation/reconciliation.ts:101:    if (!candidate.policy.enabled) continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4145:src/workers/reminder-reconciliation/reconciliation.ts:102:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4146:src/workers/reminder-reconciliation/reconciliation.ts:103:    const existing = await deps.store.queryByItem<ReminderOccurrence>(candidate.tenantId, candidate.itemId);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4147:src/workers/reminder-reconciliation/reconciliation.ts:104:    const liveExisting = existing.filter(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4148:src/workers/reminder-reconciliation/reconciliation.ts:105:      (o) =>
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4149:src/workers/reminder-reconciliation/reconciliation.ts:106:        o.policyId === candidate.policy.policyId &&
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4150:src/workers/reminder-reconciliation/reconciliation.ts:107:        o.itemVersion === candidate.itemVersion &&
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4151:src/workers/reminder-reconciliation/reconciliation.ts:108:        o.policyVersion === candidate.policy.version &&
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4152:src/workers/reminder-reconciliation/reconciliation.ts:109:        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4153:src/workers/reminder-reconciliation/reconciliation.ts:110:    );
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4154:src/workers/reminder-reconciliation/reconciliation.ts:111:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4155:src/workers/reminder-reconciliation/reconciliation.ts:112:    // Expected schedule per trigger, recomputed fresh from the IANA rule (never a frozen offset).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4156:src/workers/reminder-reconciliation/reconciliation.ts:113:    const expectedByTrigger = new Map<string, { scheduledAtUtc: string }>();
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4157:src/workers/reminder-reconciliation/reconciliation.ts:114:    for (const trigger of candidate.policy.triggers) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4158:src/workers/reminder-reconciliation/reconciliation.ts:115:      const schedule = materializer.computeSchedule({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4159:src/workers/reminder-reconciliation/reconciliation.ts:116:        itemDueDate: candidate.itemDueDate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4160:src/workers/reminder-reconciliation/reconciliation.ts:117:        offsetIso: trigger.offsetIso,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4161:src/workers/reminder-reconciliation/reconciliation.ts:118:        localTime: trigger.localTime,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4162:src/workers/reminder-reconciliation/reconciliation.ts:119:        timeZone: candidate.policy.timeZone,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4163:src/workers/reminder-reconciliation/reconciliation.ts:120:        quietHours: candidate.policy.quietHours,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4164:src/workers/reminder-reconciliation/reconciliation.ts:121:      });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4165:src/workers/reminder-reconciliation/reconciliation.ts:122:      const t = Date.parse(schedule.scheduledAtUtc);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4166:src/workers/reminder-reconciliation/reconciliation.ts:123:      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4167:src/workers/reminder-reconciliation/reconciliation.ts:124:      expectedByTrigger.set(trigger.triggerId, { scheduledAtUtc: schedule.scheduledAtUtc });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4168:src/workers/reminder-reconciliation/reconciliation.ts:125:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4169:src/workers/reminder-reconciliation/reconciliation.ts:126:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4170:src/workers/reminder-reconciliation/reconciliation.ts:127:    // Cancel occurrences whose triggerId is no longer expected, or whose scheduledAt now
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4171:src/workers/reminder-reconciliation/reconciliation.ts:128:    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4172:src/workers/reminder-reconciliation/reconciliation.ts:129:    // liveExisting already excludes those statuses).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4173:src/workers/reminder-reconciliation/reconciliation.ts:130:    for (const occurrence of liveExisting) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4174:src/workers/reminder-reconciliation/reconciliation.ts:131:      const expected = expectedByTrigger.get(occurrence.triggerId);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4175:src/workers/reminder-reconciliation/reconciliation.ts:132:      const diverges = !expected || expected.scheduledAtUtc !== occurrence.scheduledAt;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4176:src/workers/reminder-reconciliation/reconciliation.ts:133:      if (!diverges) continue;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4177:src/workers/reminder-reconciliation/reconciliation.ts:134:      divergences += 1;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4178:src/workers/reminder-reconciliation/reconciliation.ts:135:      try {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4179:src/workers/reminder-reconciliation/reconciliation.ts:136:        await deps.store.transactWrite([
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4180:src/workers/reminder-reconciliation/reconciliation.ts:137:          {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4181:src/workers/reminder-reconciliation/reconciliation.ts:138:            Update: buildVersionedUpdate({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4182:src/workers/reminder-reconciliation/reconciliation.ts:139:              tableName: deps.tableName,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4183:src/workers/reminder-reconciliation/reconciliation.ts:140:              key: { PK: occurrence.PK, SK: occurrence.SK },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4184:src/workers/reminder-reconciliation/reconciliation.ts:141:              tenantId: candidate.tenantId,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4185:src/workers/reminder-reconciliation/reconciliation.ts:142:              expectedVersion: occurrence.version,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4186:src/workers/reminder-reconciliation/reconciliation.ts:143:              set: { status: "CANCELLED" },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4187:src/workers/reminder-reconciliation/reconciliation.ts:144:              remove: ["GSI6PK", "GSI6SK"],
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4188:src/workers/reminder-reconciliation/reconciliation.ts:145:            }),
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4189:src/workers/reminder-reconciliation/reconciliation.ts:146:          },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4190:src/workers/reminder-reconciliation/reconciliation.ts:147:        ]);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4191:src/workers/reminder-reconciliation/reconciliation.ts:148:        cancelled += 1;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4192:src/workers/reminder-reconciliation/reconciliation.ts:149:      } catch (err) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4193:src/workers/reminder-reconciliation/reconciliation.ts:150:        if (!isTransactionCanceled(err)) throw err;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4194:src/workers/reminder-reconciliation/reconciliation.ts:151:      }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4195:src/workers/reminder-reconciliation/reconciliation.ts:152:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4196:src/workers/reminder-reconciliation/reconciliation.ts:153:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4197:src/workers/reminder-reconciliation/reconciliation.ts:154:    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4198:src/workers/reminder-reconciliation/reconciliation.ts:155:    // materialize() itself is idempotent (putIfAbsent keyed by the deterministic
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4199:src/workers/reminder-reconciliation/reconciliation.ts:156:    // occurrenceId hash), so re-running it for triggers that already matched is a safe no-op.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4200:src/workers/reminder-reconciliation/reconciliation.ts:157:    const stillMissing = candidate.policy.triggers.some((t) => {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4201:src/workers/reminder-reconciliation/reconciliation.ts:158:      const expected = expectedByTrigger.get(t.triggerId);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4202:src/workers/reminder-reconciliation/reconciliation.ts:159:      if (!expected) return false;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4203:src/workers/reminder-reconciliation/reconciliation.ts:160:      return !liveExisting.some((o) => o.triggerId === t.triggerId && o.scheduledAt === expected.scheduledAtUtc);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4204:src/workers/reminder-reconciliation/reconciliation.ts:161:    });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4205:src/workers/reminder-reconciliation/reconciliation.ts:162:    if (stillMissing) {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4206:src/workers/reminder-reconciliation/reconciliation.ts:163:      const result = await materializer.materialize({
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4207:src/workers/reminder-reconciliation/reconciliation.ts:164:        tenantId: candidate.tenantId,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4208:src/workers/reminder-reconciliation/reconciliation.ts:165:        itemId: candidate.itemId,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4209:src/workers/reminder-reconciliation/reconciliation.ts:166:        itemVersion: candidate.itemVersion,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4210:src/workers/reminder-reconciliation/reconciliation.ts:167:        itemDueDate: candidate.itemDueDate,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4211:src/workers/reminder-reconciliation/reconciliation.ts:168:        policy: candidate.policy,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4212:src/workers/reminder-reconciliation/reconciliation.ts:169:        shardConfig: deps.shardConfig,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4213:src/workers/reminder-reconciliation/reconciliation.ts:170:      });
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4214:src/workers/reminder-reconciliation/reconciliation.ts:171:      created += result.created.length;
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4215:src/workers/reminder-reconciliation/reconciliation.ts:172:    }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4216:src/workers/reminder-reconciliation/reconciliation.ts:173:  }
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4217:src/workers/reminder-reconciliation/reconciliation.ts:174:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4218:src/workers/reminder-reconciliation/reconciliation.ts:175:  return { cancelled, created, divergences };
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4219:src/workers/reminder-reconciliation/reconciliation.ts:176:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4220:src/workers/reminder-reconciliation/reconciliation.ts:177:
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4221:src/workers/reminder-reconciliation/reconciliation.ts:178:export async function runReconciliation(
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4222:src/workers/reminder-reconciliation/reconciliation.ts:179:  deps: ReconciliationDeps,
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4223:src/workers/reminder-reconciliation/reconciliation.ts:180:  input: { expiredClaimCandidates: ReminderOccurrence[]; dstCandidates: DstReconciliationCandidate[] },
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4224:src/workers/reminder-reconciliation/reconciliation.ts:181:): Promise<ReconciliationResult> {
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4225:src/workers/reminder-reconciliation/reconciliation.ts:182:  const claimsReverted = await reconcileExpiredClaims(deps, input.expiredClaimCandidates);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4226:src/workers/reminder-reconciliation/reconciliation.ts:183:  const dst = await reconcileDst(deps, input.dstCandidates);
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4227:src/workers/reminder-reconciliation/reconciliation.ts:184:  return { claimsReverted, dstCancelled: dst.cancelled, dstCreated: dst.created, dstDivergences: dst.divergences };
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4228:src/workers/reminder-reconciliation/reconciliation.ts:185:}
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4251:Porém o handler lê `event.detail.mode` em [reminder-reconciliation-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/reminder-reconciliation-handler.ts:27). Com o payload configurado, `event.detail` será `undefined`, e ambas as invocações falharão antes de reconciliar qualquer coisa.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4255:Correção necessária: padronizar payloads explícitos, por exemplo `{mode, scheduledTime: "<aws.scheduler.scheduled-time>"}`, e tipar os handlers segundo esse contrato; alternativamente, o producer pode usar o relógio injetado.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4257:### P1 — Lifecycle de `WORKSTATE#DST_PENDING` não fecha
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4261:O código só remove o ponteiro se a ocorrência divergir e for cancelada, em [reconciliation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-reconciliation/reconciliation.ts:130). Quando a recomputação confirma que o horário continua correto, o fluxo apenas continua e deixa `GSI6PK/GSI6SK` intactos.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4263:Resultado: candidatos corretamente processados continuam em `WORKSTATE#DST_PENDING` e são reprocessados diariamente indefinidamente.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4265:Além disso, a criação do ponteiro está mais estreita que o contrato aprovado. O código indexa somente horários locais exatamente `AMBIGUOUS` ou `NONEXISTENT` em [reminder-materializer.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:160). O design e o próprio port descrevem ocorrências cuja janela cruza uma transição DST conhecida, em [reconciliation-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/ports/reconciliation-candidate-source.ts:30). Um lembrete normal às 09:00 depois de uma mudança de offset não é ambíguo nem inexistente, mas pode ser justamente o caso que precisa ser reavaliado.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4271:Os adapters retornam `cursor` e limitam páginas a 200 itens em [dynamodb-reconciliation-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:29).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4273:O handler lê somente a primeira página, tanto para claims quanto para DST, em [reminder-reconciliation-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/reminder-reconciliation-handler.ts:33) e [reminder-reconciliation-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/reminder-reconciliation-handler.ts:40). O `page.cursor` nunca é consumido.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4279:`markPublished()` altera o status, mas não remove `GSI6PK/GSI6SK` em [dynamodb-outbox-relay-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts:35). Assim, registros publicados permanecem em `RECON#OUTBOX#PENDING`.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4291:- **Chaves globais GSI6:** `WORKSTATE#CLAIMED`, `WORKSTATE#DST_PENDING` e `RECON#OUTBOX#PENDING` são globais, com tenant no SK/item, conforme [reconciliation-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/ports/reconciliation-candidate-source.ts:55) e [outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts:64).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4292:- **Exatamente duas roles com `gsi6Read()`:** sim: reconciliation em [expiration-tracker-stack.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/lib/expiration-tracker-stack.ts:124) e sweeper em [expiration-tracker-stack.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/lib/expiration-tracker-stack.ts:159). O teste synth faz a checagem positiva e negativa em [stack.test.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/test/infra/stack.test.ts:117).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4293:- **Remoção atômica nas transições:** correta em `CLAIMED→TRIGGERED` e cancelamento stale, [dispatch.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:99) e [dispatch.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:144); correta na reversão de claim, [reconciliation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-reconciliation/reconciliation.ts:64); incompleta na janela DST processada sem divergência.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4299:1. Contrato Scheduler/handler incompatível para producer e reconciliation.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4325:Também vale fazer `markPublished` reutilizar o padrão de update condicional com `REMOVE`, embora não necessariamente o builder OCC versionado.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4331:A parte transacional principal — claim, outbox, dispatch idempotente, isolamento IAM e remoção do ponteiro de claim — está bem construída. Entretanto, os schedules de producer/reconciliation provavelmente quebram na primeira invocação real, e o lifecycle DST não implementa integralmente o design aprovado. Portanto, minha resposta ao critério final é **não**: eu corrigiria esses pontos e acrescentaria testes de contrato CDK→handler antes de confiar nos resultados das Camadas 2/3.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4354:Porém o handler lê `event.detail.mode` em [reminder-reconciliation-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/reminder-reconciliation-handler.ts:27). Com o payload configurado, `event.detail` será `undefined`, e ambas as invocações falharão antes de reconciliar qualquer coisa.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4358:Correção necessária: padronizar payloads explícitos, por exemplo `{mode, scheduledTime: "<aws.scheduler.scheduled-time>"}`, e tipar os handlers segundo esse contrato; alternativamente, o producer pode usar o relógio injetado.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4360:### P1 — Lifecycle de `WORKSTATE#DST_PENDING` não fecha
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4364:O código só remove o ponteiro se a ocorrência divergir e for cancelada, em [reconciliation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-reconciliation/reconciliation.ts:130). Quando a recomputação confirma que o horário continua correto, o fluxo apenas continua e deixa `GSI6PK/GSI6SK` intactos.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4366:Resultado: candidatos corretamente processados continuam em `WORKSTATE#DST_PENDING` e são reprocessados diariamente indefinidamente.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4368:Além disso, a criação do ponteiro está mais estreita que o contrato aprovado. O código indexa somente horários locais exatamente `AMBIGUOUS` ou `NONEXISTENT` em [reminder-materializer.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:160). O design e o próprio port descrevem ocorrências cuja janela cruza uma transição DST conhecida, em [reconciliation-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/ports/reconciliation-candidate-source.ts:30). Um lembrete normal às 09:00 depois de uma mudança de offset não é ambíguo nem inexistente, mas pode ser justamente o caso que precisa ser reavaliado.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4374:Os adapters retornam `cursor` e limitam páginas a 200 itens em [dynamodb-reconciliation-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts:29).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4376:O handler lê somente a primeira página, tanto para claims quanto para DST, em [reminder-reconciliation-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/reminder-reconciliation-handler.ts:33) e [reminder-reconciliation-handler.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/runtime/aws/handlers/reminder-reconciliation-handler.ts:40). O `page.cursor` nunca é consumido.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4382:`markPublished()` altera o status, mas não remove `GSI6PK/GSI6SK` em [dynamodb-outbox-relay-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts:35). Assim, registros publicados permanecem em `RECON#OUTBOX#PENDING`.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4394:- **Chaves globais GSI6:** `WORKSTATE#CLAIMED`, `WORKSTATE#DST_PENDING` e `RECON#OUTBOX#PENDING` são globais, com tenant no SK/item, conforme [reconciliation-candidate-source.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/ports/reconciliation-candidate-source.ts:55) e [outbox.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/outbox/outbox.ts:64).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4395:- **Exatamente duas roles com `gsi6Read()`:** sim: reconciliation em [expiration-tracker-stack.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/lib/expiration-tracker-stack.ts:124) e sweeper em [expiration-tracker-stack.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/lib/expiration-tracker-stack.ts:159). O teste synth faz a checagem positiva e negativa em [stack.test.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/test/infra/stack.test.ts:117).
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4396:- **Remoção atômica nas transições:** correta em `CLAIMED→TRIGGERED` e cancelamento stale, [dispatch.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:99) e [dispatch.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:144); correta na reversão de claim, [reconciliation.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-reconciliation/reconciliation.ts:64); incompleta na janela DST processada sem divergência.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4402:1. Contrato Scheduler/handler incompatível para producer e reconciliation.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4428:Também vale fazer `markPublished` reutilizar o padrão de update condicional com `REMOVE`, embora não necessariamente o builder OCC versionado.
docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt:4434:A parte transacional principal — claim, outbox, dispatch idempotente, isolamento IAM e remoção do ponteiro de claim — está bem construída. Entretanto, os schedules de producer/reconciliation provavelmente quebram na primeira invocação real, e o lifecycle DST não implementa integralmente o design aprovado. Portanto, minha resposta ao critério final é **não**: eu corrigiria esses pontos e acrescentaria testes de contrato CDK→handler antes de confiar nos resultados das Camadas 2/3.
src\shared\outbox\relay-store.ts:15:  markPublished(key: EntityKey): Promise<void>;
test\infra\stack.test.ts:88:  it("M3: creates the ReminderPolicy HTTP handler and the three reminder workers (producer/dispatch/reconciliation)", () => {
test\infra\stack.test.ts:109:    // policy (items handler, reminders handler, dispatch, reconciliation, test route) must
test\infra\stack.test.ts:165:    expect(Object.keys(schedules).length).toBe(4); // producer, claims-reconciliation, dst-reconciliation, sweeper
test\infra\stack.test.ts:173:  it("M3.5 contract (bug found by Codex implementation review): every schedule's Input is a top-level JSON payload with scheduledTime, matching what the handlers read - EventBridge Scheduler does NOT wrap payloads in a `detail` envelope like legacy Rules, so event.detail.mode / event.time would always be undefined", () => {
test\infra\stack.test.ts:180:      expect(parsed["scheduledTime"], `${logicalId} Input is missing scheduledTime`).toBe("<aws.scheduler.scheduled-time>");
test\integration\reminder-engine.test.ts:16:import { reconcileExpiredClaims } from "../../src/workers/reminder-reconciliation/reconciliation.js";
test\integration\reminder-engine.test.ts:25: * exercises ReminderPolicyService/materializer/producer/dispatch/reconciliation directly
test\integration\reminder-engine.test.ts:197:  it("claim-expiry reconciliation reverts an unconfirmed claim to SCHEDULED so the next producer tick can re-claim it", async () => {
src\workers\reminder-reconciliation\reconciliation.ts:3: * purposes (per the task brief: "don't build two separate reconciliation paths"), both
src\workers\reminder-reconciliation\reconciliation.ts:7: *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
src\workers\reminder-reconciliation\reconciliation.ts:12: *      in the reconciliation batch, recompute the expected schedule from the policy's
src\workers\reminder-reconciliation\reconciliation.ts:37: * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
src\workers\reminder-reconciliation\reconciliation.ts:74:            // must go too, or the next reconciliation pass would find it again forever.
src\workers\reminder-reconciliation\reconciliation.ts:88:export async function reconcileDst(
src\workers\reminder-reconciliation\reconciliation.ts:123:      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
src\workers\reminder-reconciliation\reconciliation.ts:135:        // WORKSTATE#DST_PENDING pointers in place forever whenever recomputation confirmed
src\workers\reminder-reconciliation\reconciliation.ts:209:  const dst = await reconcileDst(deps, input.dstCandidates);
src\runtime\aws\composition\reminder.ts:6:import { DynamoDbReminderReconciliationCandidateSource } from "../../../modules/reminder/persistence/dynamodb-reconciliation-candidate-source.js";
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:35:  async markPublished(key: EntityKey): Promise<void> {
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:52:      throw mapDynamoError(err, "OutboxRelayStore.markPublished");
infra\lib\scoped-lambda-function.ts:97:    /** GSI6 (reconciliation/retention) read-only - reserved EXCLUSIVELY for
infra\lib\scoped-lambda-function.ts:104:        description: "GSI6 (reconciliation) read-only - ReminderReconciliation/OutboxSweeperReminderDispatch only",
infra\lib\dynamo-table.ts:13: * families - `WORKSTATE#CLAIMED`/`WORKSTATE#DST_PENDING` (reminder reconciliation
infra\lib\dynamo-table.ts:176:   * `ReminderReconciliation` (WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING candidates) and
src\workers\reminder-producer\producer.ts:26:import { GSI6PK_WORKSTATE_CLAIMED, buildExpiredClaimGsi6Sk } from "../../modules/reminder/ports/reconciliation-candidate-source.js";
src\workers\reminder-producer\producer.ts:48:  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (§9.5). */
src\workers\reminder-producer\producer.ts:130:            // producer's own lookback window nor the daily DST reconciliation reliably
src\workers\reminder-producer\producer.ts:135:            // reconciliation job can find this claim if it expires unrecovered - removed
src\workers\reminder-producer\producer.ts:136:            // when ReminderDispatch advances to TRIGGERED or reconciliation reverts it.
test\unit\reminder\reminder-materializer.test.ts:112:  describe("M3.5: GSI6 WORKSTATE#DST_PENDING pointer lifecycle", () => {
test\unit\reminder\reminder-materializer.test.ts:128:      expect(occ.GSI6PK).toBe("WORKSTATE#DST_PENDING");
infra\lib\reminder-schedule.ts:39:    // matching exactly what its handler reads (`event.mode`, `event.scheduledTime` - see
infra\lib\reminder-schedule.ts:40:    // reminder-producer-handler.ts / reminder-reconciliation-handler.ts), never
infra\lib\reminder-schedule.ts:42:    // `<aws.scheduler.scheduled-time>` is a Scheduler context attribute, substituted at
infra\lib\reminder-schedule.ts:51:        input: JSON.stringify({ scheduledTime: "<aws.scheduler.scheduled-time>" }),
infra\lib\reminder-schedule.ts:65:        input: JSON.stringify({ mode: "CLAIMS", scheduledTime: "<aws.scheduler.scheduled-time>" }),
infra\lib\reminder-schedule.ts:77:        input: JSON.stringify({ mode: "DST", scheduledTime: "<aws.scheduler.scheduled-time>" }),
infra\lib\reminder-schedule.ts:88:        input: JSON.stringify({ scheduledTime: "<aws.scheduler.scheduled-time>" }),
infra\lib\expiration-tracker-stack.ts:129:      entry: `${HANDLERS_DIR}/reminder-reconciliation-handler.ts`,
src\workers\reminder-dispatch\dispatch.ts:10: * silently dropped, and repair is left to reconciliation (§9.5) - never a new ad-hoc
src\workers\reminder-dispatch\dispatch.ts:110:            // reconciliation job find a claim that's already been handled.
src\shared\dynamodb\occ.ts:23:   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
src\shared\dynamodb\occ.ts:26:   * actively queried by reconciliation. */
test\unit\reminder\reconciliation.test.ts:2: * Dedicated unit coverage for reconciliation.ts (implementation-blueprint.md §9.5), closing
test\unit\reminder\reconciliation.test.ts:4: * expiry reconciliation had scenario coverage only inside the M3 integration test, and
test\unit\reminder\reconciliation.test.ts:5: * `reconcileDst` (the DST-affected re-evaluation pass) had ZERO coverage anywhere, direct or
test\unit\reminder\reconciliation.test.ts:6: * indirect - not even imported outside reconciliation.ts itself. This is the "replay/
test\unit\reminder\reconciliation.test.ts:7: * reconciliation são testados" evidence gate G8 depends on.
test\unit\reminder\reconciliation.test.ts:14:import { reconcileExpiredClaims, reconcileDst } from "../../../src/workers/reminder-reconciliation/reconciliation.js";
test\unit\reminder\reconciliation.test.ts:29:describe("reconciliation.ts", () => {
test\unit\reminder\reconciliation.test.ts:129:  describe("reconcileDst", () => {
test\unit\reminder\reconciliation.test.ts:177:      // reconcileDst's window is [now, now+7d] - advance the clock to within 7 days of the
test\unit\reminder\reconciliation.test.ts:182:      const result = await reconcileDst(
test\unit\reminder\reconciliation.test.ts:221:      clock.current = "2026-09-05T00:00:00.000Z"; // within the 7-day reconciliation window of Sep 10
test\unit\reminder\reconciliation.test.ts:223:      const result = await reconcileDst(
test\unit\reminder\reconciliation.test.ts:233:    it("M3.5: removes the GSI6 WORKSTATE#DST_PENDING pointer once recomputation CONFIRMS the schedule is still correct (bug found by Codex implementation review - the pointer used to stay forever in this case, causing daily re-evaluation indefinitely)", async () => {
test\unit\reminder\reconciliation.test.ts:254:      expect(occ.GSI6PK).toBe("WORKSTATE#DST_PENDING"); // sanity: the pointer IS present before reconciliation
test\unit\reminder\reconciliation.test.ts:258:      const result = await reconcileDst(
test\unit\reminder\reconciliation.test.ts:294:      const result = await reconcileDst(
test\unit\reminder\reconciliation.test.ts:305:      const { runReconciliation } = await import("../../../src/workers/reminder-reconciliation/reconciliation.js");
src\workers\dispatch-outbox-relay\relay.ts:72:  await deps.store.markPublished({ PK: record.PK, SK: record.SK });
src\modules\reminder\ports\reminder-store.ts:9: * reconciliation.
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:8: * (infra/lib/reminder-schedule.ts: `{mode, scheduledTime}` at the top level), never
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:13:import { runReconciliation, type DstReconciliationCandidate as FullDstCandidate } from "../../../workers/reminder-reconciliation/reconciliation.js";
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:26:const logger = new SecureLogger({ baseContext: { service: "reminder-reconciliation" } });
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:30:  scheduledTime: string;
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:37:const MAX_PAGES = 25;
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:51:    for (let page = 0; page < MAX_PAGES; page++) {
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:60:    // ReminderMaterializer (M3.5) writes a WORKSTATE#DST_PENDING GSI6 pointer whenever a
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:70:    for (let page = 0; page < MAX_PAGES; page++) {
src\runtime\aws\handlers\reminder-reconciliation-handler.ts:91:  logger.info("reminder-reconciliation complete", { mode, scheduledTime: event.scheduledTime, ...result });
test\unit\dispatch-outbox-relay\relay.test.ts:46:  async markPublished(key: { PK: string; SK: string }): Promise<void> {
src\modules\reminder\ports\reconciliation-candidate-source.ts:9: * exception as GSI3 (data-model.md §3), for the same reason: the reconciliation job doesn't
src\modules\reminder\ports\reconciliation-candidate-source.ts:30:/** Same ALL-projection reasoning as ExpiredClaimCandidate: the DST_PENDING pointer is
src\modules\reminder\ports\reconciliation-candidate-source.ts:51:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
src\modules\reminder\ports\reconciliation-candidate-source.ts:56:export const GSI6PK_WORKSTATE_DST_PENDING = "WORKSTATE#DST_PENDING";
src\runtime\aws\handlers\reminder-producer-handler.ts:21:  scheduledTime: string;
src\runtime\aws\handlers\reminder-producer-handler.ts:25:  if (!event.scheduledTime) {
src\runtime\aws\handlers\reminder-producer-handler.ts:26:    throw new ValidationError("reminder-producer: missing scheduledTime in event payload.");
src\runtime\aws\handlers\reminder-producer-handler.ts:28:  const tickMinute = new Date(event.scheduledTime);
src\runtime\aws\handlers\reminder-producer-handler.ts:30:    throw new ValidationError("reminder-producer: scheduledTime is not a valid date.", { scheduledTime: event.scheduledTime });
src\modules\reminder\domain\shard-config.ts:4: * invisible. `shardFnVersion` is the generation id; the producer/reconciliation query
src\modules\reminder\domain\shard-config.ts:34:/** Generations the producer/reconciliation must query "now" (implementation-blueprint.md
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:5: * WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING key families - see
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:6: * src/modules/reminder/ports/reconciliation-candidate-source.ts and
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:13:  GSI6PK_WORKSTATE_DST_PENDING,
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:18:} from "../ports/reconciliation-candidate-source.js";
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:57:          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_DST_PENDING, ":start": input.window.start, ":end": input.window.end },
src\modules\reminder\domain\recurrence.ts:171: * occurrence in a DST-observing zone as a reconciliation candidate, not just the literally
src\modules\reminder\domain\recurrence.ts:175:export function timeZoneObservesDst(timeZone: string, year: number): boolean {
src\modules\reminder\domain\reminder-occurrence.ts:34:  /** M3.5: WORKSTATE#CLAIMED (set by the producer's claim) or WORKSTATE#DST_PENDING (set
src\modules\reminder\domain\reminder-occurrence.ts:37:   * SCHEDULED so GSI6 only ever contains live reconciliation work. See
src\modules\reminder\domain\reminder-occurrence.ts:94:/** Enumerates the GSI3PK values to query for a given UTC minute across all shards of a given generation - used by both the producer (current minute + lookback) and reconciliation. */
src\modules\reminder\application\reminder-materializer.ts:24:  timeZoneObservesDst,
src\modules\reminder\application\reminder-materializer.ts:32:import { GSI6PK_WORKSTATE_DST_PENDING, buildDstCandidateGsi6Sk } from "../ports/reconciliation-candidate-source.js";
src\modules\reminder\application\reminder-materializer.ts:163:      // too narrow): a GSI6 WORKSTATE#DST_PENDING pointer is set whenever EITHER the
src\modules\reminder\application\reminder-materializer.ts:170:        schedule.dstKind !== "NORMAL" || timeZoneObservesDst(input.policy.timeZone, toCalendarDate(schedule.scheduledAtUtc).year);
src\modules\reminder\application\reminder-materializer.ts:173:            GSI6PK: GSI6PK_WORKSTATE_DST_PENDING,
src\modules\reminder\application\reminder-materializer.ts:241:              // unlike GSI3, ARE actively queried by reconciliation, so a stale
src\modules\reminder\application\reminder-materializer.ts:242:              // WORKSTATE#DST_PENDING pointer on a CANCELLED occurrence would be a real bug
src\modules\reminder\application\reminder-materializer.ts:243:              // (reconciliation would keep re-evaluating dead work) - removed here.

2026-08-19T22:20:30.306724Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'src/workers/reminder-reconciliation/reconciliation.ts','src/modules/reminder/domain/recurrence.ts','src/modules/reminder/application/reminder-materializer.ts','src/runtime/aws/handlers/reminder-reconciliation-handler.ts','src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts','src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts','src/shared/dynamodb/occ.ts','test/unit/reminder/reconciliation.test.ts','test/unit/reminder/reminder-materializer.test.ts','test/infra/stack.test.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=0; Get-Content $f | ForEach-Object { $n++; "${n}:$_" } }; rg -n "interface ReminderOccurrence|version:|buildOccUpdate|expectedVersion|removeAttributes" src/modules/reminder src/shared test/unit/reminder/in-memory-store.ts'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'src/workers/reminder-reconciliation/reconciliation.ts','src/modules/reminder/domain/recurrence.ts','src/modules/reminder/application/reminder-materializer.ts','src/runtime/aws/handlers/reminder-reconciliation-handler.ts','src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts','src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts','src/shared/dynamodb/occ.ts','test/unit/reminder/reconciliation.test.ts','test/unit/reminder/reminder-materializer.test.ts','test/infra/stack.test.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=0; Get-Content $f | ForEach-Object { $n++; "${n}:$_" } }; rg -n "interface ReminderOccurrence|version:|buildOccUpdate|expectedVersion|removeAttributes" src/modules/reminder src/shared test/unit/reminder/in-memory-store.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'src/workers/reminder-reconciliation/reconciliation.ts','src/modules/reminder/domain/recurrence.ts','src/modules/reminder/application/reminder-materializer.ts','src/runtime/aws/handlers/reminder-reconciliation-handler.ts','src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts','src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts','src/shared/dynamodb/occ.ts','test/unit/reminder/reconciliation.test.ts','test/unit/reminder/reminder-materializer.test.ts','test/infra/stack.test.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=0; Get-Content $f | ForEach-Object { $n++; "${n}:$_" } }; rg -n "interface ReminderOccurrence|version:|buildOccUpdate|expectedVersion|removeAttributes" src/modules/reminder src/shared test/unit/reminder/in-memory-store.ts'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/workers/reminder-reconciliation/reconciliation.ts; Get-Content src/modules/reminder/domain/recurrence.ts; Get-Content src/runtime/aws/handlers/reminder-reconciliation-handler.ts; Get-Content src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts; Get-Content src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts; Get-Content src/shared/dynamodb/occ.ts; rg -n "interface ReminderOccurrence|version:|expectedVersion|removeAttributes" src/modules/reminder src/shared test/unit/reminder' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 409ms:
/**
 * Reconciliation - implementation-blueprint.md Â§9.5, run as ONE job/Lambda serving TWO
 * purposes (per the task brief: "don't build two separate reconciliation paths"), both
 * documented in §9.3 point 4 ("claims expirados... via reconciliação, mesmo job da §9.5")
 * and Â§9.5 itself (DST):
 *
 *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
 *      `claimExpiresAt` (dispatch worker crashed/timed out before completing) reverts to
 *      SCHEDULED, conditionally, so the next producer tick's lookback window picks it up
 *      again;
 *  (b) DST-affected re-evaluation (Â§9.5 steps 1-6): for each (item, enabled policy) pair
 *      in the reconciliation batch, recompute the expected schedule from the policy's
 *      IANA timeZone + rule against a `[now, now+7d]` window, compare to what's actually
 *      materialized, cancel divergent SCHEDULED occurrences and materialize missing ones
 *      idempotently - TRIGGERED/ACKED history is never touched.
 *
 * Both passes reuse the same store/materializer/clock and are invoked together by
 * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
 * unblocks the producer sooner).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
import { ReminderMaterializer } from "../../modules/reminder/application/reminder-materializer.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import type { ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ShardConfig } from "../../modules/reminder/domain/shard-config.js";

export interface ReconciliationDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  shardConfig: ShardConfig;
}

/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
 * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
 * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
 * wiring); tests/callers supply the batch directly. */
export interface DstReconciliationCandidate {
  tenantId: string;
  itemId: string;
  itemVersion: number;
  itemDueDate: string;
  policy: ReminderPolicy;
}

export interface ReconciliationResult {
  claimsReverted: number;
  dstCancelled: number;
  dstCreated: number;
  dstDivergences: number;
}

/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
export async function reconcileExpiredClaims(
  deps: ReconciliationDeps,
  candidateOccurrences: ReminderOccurrence[],
): Promise<number> {
  let reverted = 0;
  const now = deps.now();
  for (const occurrence of candidateOccurrences) {
    if (occurrence.status !== "CLAIMED") continue;
    if (!occurrence.claimExpiresAt || occurrence.claimExpiresAt > now) continue;
    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId: occurrence.tenantId,
            expectedVersion: occurrence.version,
            set: { status: "SCHEDULED" },
            // M3.5: this claim is resolved (reverted) - its WORKSTATE#CLAIMED GSI6 pointer
            // must go too, or the next reconciliation pass would find it again forever.
            remove: ["GSI6PK", "GSI6SK"],
          }),
        },
      ]);
      reverted += 1;
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
  }
  return reverted;
}

/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (Â§9.5 steps 1-6). */
export async function reconcileDst(
  deps: ReconciliationDeps,
  candidates: DstReconciliationCandidate[],
): Promise<{ cancelled: number; created: number; divergences: number }> {
  const materializer = new ReminderMaterializer(deps.store, deps.tableName, deps.now);
  const windowStart = Date.parse(deps.now());
  const windowEnd = windowStart + 7 * 24 * 60 * 60_000;

  let cancelled = 0;
  let created = 0;
  let divergences = 0;

  for (const candidate of candidates) {
    if (!candidate.policy.enabled) continue;

    const existing = await deps.store.queryByItem<ReminderOccurrence>(candidate.tenantId, candidate.itemId);
    const liveExisting = existing.filter(
      (o) =>
        o.policyId === candidate.policy.policyId &&
        o.itemVersion === candidate.itemVersion &&
        o.policyVersion === candidate.policy.version &&
        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
    );

    // Expected schedule per trigger, recomputed fresh from the IANA rule (never a frozen offset).
    const expectedByTrigger = new Map<string, { scheduledAtUtc: string }>();
    for (const trigger of candidate.policy.triggers) {
      const schedule = materializer.computeSchedule({
        itemDueDate: candidate.itemDueDate,
        offsetIso: trigger.offsetIso,
        localTime: trigger.localTime,
        timeZone: candidate.policy.timeZone,
        quietHours: candidate.policy.quietHours,
      });
      const t = Date.parse(schedule.scheduledAtUtc);
      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
      expectedByTrigger.set(trigger.triggerId, { scheduledAtUtc: schedule.scheduledAtUtc });
    }

    // Cancel occurrences whose triggerId is no longer expected, or whose scheduledAt now
    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
    // liveExisting already excludes those statuses).
    for (const occurrence of liveExisting) {
      const expected = expectedByTrigger.get(occurrence.triggerId);
      const diverges = !expected || expected.scheduledAtUtc !== occurrence.scheduledAt;
      if (!diverges) {
        // M3.5 (bug found by Codex implementation review - the original code left
        // WORKSTATE#DST_PENDING pointers in place forever whenever recomputation confirmed
        // the schedule was still correct, so confirmed-correct occurrences were
        // re-evaluated on every single daily run indefinitely). The window is processed for
        // this occurrence either way - clear the pointer once confirmed, not only on
        // cancellation.
        if (occurrence.GSI6PK) {
          try {
            await deps.store.transactWrite([
              {
                Update: buildVersionedUpdate({
                  tableName: deps.tableName,
                  key: { PK: occurrence.PK, SK: occurrence.SK },
                  tenantId: candidate.tenantId,
                  expectedVersion: occurrence.version,
                  set: {},
                  remove: ["GSI6PK", "GSI6SK"],
                }),
              },
            ]);
          } catch (err) {
            if (!isTransactionCanceled(err)) throw err; // lost a race - fine, not our job to force it
          }
        }
        continue;
      }
      divergences += 1;
      try {
        await deps.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: deps.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: candidate.tenantId,
              expectedVersion: occurrence.version,
              set: { status: "CANCELLED" },
              remove: ["GSI6PK", "GSI6SK"],
            }),
          },
        ]);
        cancelled += 1;
      } catch (err) {
        if (!isTransactionCanceled(err)) throw err;
      }
    }

    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
    // materialize() itself is idempotent (putIfAbsent keyed by the deterministic
    // occurrenceId hash), so re-running it for triggers that already matched is a safe no-op.
    const stillMissing = candidate.policy.triggers.some((t) => {
      const expected = expectedByTrigger.get(t.triggerId);
      if (!expected) return false;
      return !liveExisting.some((o) => o.triggerId === t.triggerId && o.scheduledAt === expected.scheduledAtUtc);
    });
    if (stillMissing) {
      const result = await materializer.materialize({
        tenantId: candidate.tenantId,
        itemId: candidate.itemId,
        itemVersion: candidate.itemVersion,
        itemDueDate: candidate.itemDueDate,
        policy: candidate.policy,
        shardConfig: deps.shardConfig,
      });
      created += result.created.length;
    }
  }

  return { cancelled, created, divergences };
}

export async function runReconciliation(
  deps: ReconciliationDeps,
  input: { expiredClaimCandidates: ReminderOccurrence[]; dstCandidates: DstReconciliationCandidate[] },
): Promise<ReconciliationResult> {
  const claimsReverted = await reconcileExpiredClaims(deps, input.expiredClaimCandidates);
  const dst = await reconcileDst(deps, input.dstCandidates);
  return { claimsReverted, dstCancelled: dst.cancelled, dstCreated: dst.created, dstDivergences: dst.divergences };
}
/**
 * IANA-timezone-aware local-wall-clock -> UTC conversion, with explicit DST edge-case
 * handling (implementation-blueprint.md §9.2/§9.5: "regra de recorrência + timezone IANA
 * devem ser reavaliadas, não um offset UTC congelado").
 *
 * No date library dependency (none is in package.json and the blueprint doesn't mandate
 * one) - this uses the standard Intl.DateTimeFormat offset-probing technique (the same
 * core algorithm libraries like date-fns-tz/luxon use under the hood): format a UTC
 * instant back into the target timeZone, compare to the requested wall-clock time, and
 * iterate until the offset stabilizes. Two DST edge cases are handled explicitly:
 *  - AMBIGUOUS (fall-back): the wall-clock time occurs twice. We deterministically pick
 *    the EARLIER UTC instant (the first occurrence), a documented judgment call - the
 *    blueprint requires the case be "covered", not a specific tie-break.
 *  - NONEXISTENT (spring-forward gap): the wall-clock time never occurs. We shift forward
 *    by the size of the gap (typically 60 minutes) to the first valid instant at/after
 *    the requested wall-clock time - never silently drop the reminder.
 */

export type ZonedResolutionKind = "NORMAL" | "AMBIGUOUS" | "NONEXISTENT";

export interface ZonedResolution {
  utcMillis: number;
  kind: ZonedResolutionKind;
}

export interface LocalDateTimeParts {
  year: number;
  month: number; // 1-12
  day: number;
  hour: number;
  minute: number;
  second?: number;
}

function formatParts(instantMillis: number, timeZone: string): LocalDateTimeParts {
  const dtf = new Intl.DateTimeFormat("en-US", {
    timeZone,
    hourCycle: "h23",
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
  });
  const parts = dtf.formatToParts(new Date(instantMillis));
  const map: Record<string, string> = {};
  for (const p of parts) {
    if (p.type !== "literal") map[p.type] = p.value;
  }
  return {
    year: Number(map["year"]),
    month: Number(map["month"]),
    day: Number(map["day"]),
    hour: Number(map["hour"]),
    minute: Number(map["minute"]),
    second: Number(map["second"]),
  };
}

/** Offset (minutes) such that: asUtcMillis(localPartsAt(instant)) - instant === offset*60000. */
function offsetMinutesAt(instantMillis: number, timeZone: string): number {
  const p = formatParts(instantMillis, timeZone);
  const asUtc = Date.UTC(p.year, p.month - 1, p.day, p.hour, p.minute, p.second ?? 0);
  return (asUtc - instantMillis) / 60_000;
}

function sameWallClock(a: LocalDateTimeParts, b: LocalDateTimeParts): boolean {
  return a.year === b.year && a.month === b.month && a.day === b.day && a.hour === b.hour && a.minute === b.minute;
}

/**
 * Resolves a local wall-clock date/time in `timeZone` to a UTC instant.
 * `requested` seconds default to 0.
 */
export function zonedTimeToUtc(requested: LocalDateTimeParts, timeZone: string): ZonedResolution {
  const naiveUtc = Date.UTC(
    requested.year,
    requested.month - 1,
    requested.day,
    requested.hour,
    requested.minute,
    requested.second ?? 0,
  );

  const offset0 = offsetMinutesAt(naiveUtc, timeZone);
  const guess1 = naiveUtc - offset0 * 60_000;
  const offset1 = offsetMinutesAt(guess1, timeZone);
  const guess2 = naiveUtc - offset1 * 60_000;
  const offset2 = offsetMinutesAt(guess2, timeZone);

  const candidateA = naiveUtc - offset0 * 60_000;
  const candidateB = naiveUtc - offset1 * 60_000;
  const localA = formatParts(candidateA, timeZone);
  const localB = formatParts(candidateB, timeZone);
  const matchesA = sameWallClock(localA, requested);
  const matchesB = sameWallClock(localB, requested);

  if (offset1 === offset2 && matchesA && matchesB) {
    // Both iterations converged to the same instant AND it round-trips - but that alone
    // doesn't rule out AMBIGUOUS: a fall-back wall clock also round-trips at its first
    // (pre-transition) instant, since the offset-probing walk naturally lands there first.
    // Probe a neighboring instant one typical DST-shift (60 min) on the other side of the
    // transition to check whether the SAME wall clock reoccurs there too (documented
    // judgment call: 60 min covers every real-world IANA transition this product targets;
    // sub-hour shifts are historical/rare and out of scope for M3).
    const probeLater = candidateA + 60 * 60_000;
    const probeEarlier = candidateA - 60 * 60_000;
    const laterMatches = sameWallClock(formatParts(probeLater, timeZone), requested);
    const earlierMatches = sameWallClock(formatParts(probeEarlier, timeZone), requested);
    if (laterMatches) return { utcMillis: Math.min(candidateA, probeLater), kind: "AMBIGUOUS" };
    if (earlierMatches) return { utcMillis: Math.min(candidateA, probeEarlier), kind: "AMBIGUOUS" };
    return { utcMillis: guess2, kind: "NORMAL" };
  }

  if (matchesA && matchesB) {
    // Fall-back: two UTC instants produce the same wall clock. Deterministic tie-break:
    // earlier instant (first occurrence).
    return { utcMillis: Math.min(candidateA, candidateB), kind: "AMBIGUOUS" };
  }
  if (matchesA) return { utcMillis: candidateA, kind: "NORMAL" };
  if (matchesB) return { utcMillis: candidateB, kind: "NORMAL" };

  // Spring-forward gap: neither candidate reproduces the requested wall clock because it
  // never occurred. Resolve to the first valid instant at/after the requested wall clock,
  // by applying the offset that is in effect AFTER the transition (candidateB, which used
  // offset1 - probed slightly closer to the post-transition regime).
  return { utcMillis: Math.max(candidateA, candidateB), kind: "NONEXISTENT" };
}

/** Parses "YYYY-MM-DD" (date-only) or an ISO date-time, returning just the calendar date parts (UTC-anchored, no time-of-day). */
export function toCalendarDate(isoDateOrDateTime: string): { year: number; month: number; day: number } {
  const datePart = isoDateOrDateTime.slice(0, 10);
  const [y, m, d] = datePart.split("-").map(Number);
  return { year: y as number, month: m as number, day: d as number };
}

/** Adds a signed number of days to a calendar date, in pure calendar arithmetic (no timezone involved yet). */
export function addCalendarDays(date: { year: number; month: number; day: number }, days: number): { year: number; month: number; day: number } {
  const asUtcMillis = Date.UTC(date.year, date.month - 1, date.day) + days * 86_400_000;
  const d = new Date(asUtcMillis);
  return { year: d.getUTCFullYear(), month: d.getUTCMonth() + 1, day: d.getUTCDate() };
}

/** Parses an ISO-8601 duration of the restricted form used by ReminderTrigger.offsetIso: optional sign, "P", integer days, "D" (e.g. "-P7D", "P0D", "-P1D"). Sufficient for the relative-offset-from-dueDate model documented in data-model.md/blueprint Â§9.1 - broader ISO-8601 duration grammar (weeks/months/time components) is out of scope for M3. */
export function parseDayOffset(offsetIso: string): number {
  const match = /^(-)?P(\d+)D$/.exec(offsetIso);
  if (!match) {
    throw new Error(`Unsupported offset format: ${offsetIso} (expected [-]P<N>D)`);
  }
  const sign = match[1] === "-" ? -1 : 1;
  return sign * Number(match[2]);
}

/** Parses "HH:mm" into hour/minute. */
export function parseLocalTime(hhmm: string): { hour: number; minute: number } {
  const match = /^(\d{2}):(\d{2})$/.exec(hhmm);
  if (!match) {
    throw new Error(`Unsupported local time format: ${hhmm} (expected HH:mm)`);
  }
  return { hour: Number(match[1]), minute: Number(match[2]) };
}

/**
 * M3.5 (docs/architecture/m3.5-runtime-design.md §"Reconciliação"; broadened after Codex's
 * implementation review found the original AMBIGUOUS/NONEXISTENT-only trigger too narrow):
 * true if `timeZone` observes DST at all during `year` - compares the UTC offset at two
 * fixed reference instants (Jan 15 and Jul 15 noon UTC), one of which is guaranteed to fall
 * in each side of a DST transition for any zone that has one. A fixed-offset zone (e.g.
 * America/Sao_Paulo since 2019, or any UTC+N zone) always returns false. Used to flag EVERY
 * occurrence in a DST-observing zone as a reconciliation candidate, not just the literally
 * ambiguous/nonexistent ones - a plain 09:00 reminder can still need re-evaluation after an
 * offset change even though its own computed instant was never ambiguous.
 */
export function timeZoneObservesDst(timeZone: string, year: number): boolean {
  const januaryOffset = offsetMinutesAt(Date.UTC(year, 0, 15, 12, 0, 0), timeZone);
  const julyOffset = offsetMinutesAt(Date.UTC(year, 6, 15, 12, 0, 0), timeZone);
  return januaryOffset !== julyOffset;
}
/** Real handler for ReminderReconciliation (EventBridge Scheduler, `mode` field), replacing
 * the 501 placeholder. Two schedules invoke the same Lambda with different `mode`
 * (m3.5-runtime-design.md §"Reconciliação"): CLAIMS (every 5 min) and DST (daily). Also one
 * of exactly two roles granted gsi6Read() - see infra/lib/dynamo-table.ts.
 *
 * EventBridge Scheduler's Lambda target does NOT wrap the payload in a `detail` envelope
 * the way legacy EventBridge Rules do - the event IS whatever `input` the schedule sets
 * (infra/lib/reminder-schedule.ts: `{mode, scheduledTime}` at the top level), never
 * `event.detail.mode` (bug found by Codex implementation review - event.detail was always
 * undefined, both schedules would throw on every invocation). */
import { createDocumentClient } from "../../../shared/dynamodb/client.js";
import { buildReconciliationDeps } from "../composition/reminder.js";
import { runReconciliation, type DstReconciliationCandidate as FullDstCandidate } from "../../../workers/reminder-reconciliation/reconciliation.js";
import { itemKey } from "../../../modules/expiration/domain/expiration-item.js";
import { policyKey, type ReminderPolicy } from "../../../modules/reminder/domain/reminder-policy.js";
import { defaultShardConfig } from "../../../modules/reminder/domain/shard-config.js";
import type { ReminderOccurrence } from "../../../modules/reminder/domain/reminder-occurrence.js";
import { SecureLogger } from "../../../shared/observability/logger.js";

const client = createDocumentClient();
const envTableName = process.env["TABLE_NAME"];
if (!envTableName) throw new Error("TABLE_NAME env var is required.");
const tableName: string = envTableName;
const { store, candidateSource, now } = buildReconciliationDeps(client, tableName);
const shardConfig = defaultShardConfig();
const logger = new SecureLogger({ baseContext: { service: "reminder-reconciliation" } });

export interface ReminderReconciliationEvent {
  mode: "CLAIMS" | "DST";
  scheduledTime: string;
}

/** Hard cap on pages drained per invocation - matches the design's "página lógica máx. 200"
 * per page with a bounded number of pages, so a pathological backlog can't make a single
 * invocation run indefinitely; anything beyond this is picked up by the NEXT scheduled
 * run (every 5 min for claims, daily for DST) rather than blocking this one. */
const MAX_PAGES = 25;

export async function handler(event: ReminderReconciliationEvent): Promise<void> {
  const mode = event.mode;

  const expiredClaimCandidates: ReminderOccurrence[] = [];
  const dstCandidates: FullDstCandidate[] = [];

  if (mode === "CLAIMS") {
    // GSI6 is ALL-projected - each candidate row already IS the full ReminderOccurrence
    // item (see ExpiredClaimCandidate's doc comment), so no extra fetch is needed.
    // Pagination is drained (bug found by Codex implementation review: the first version
    // only ever read page 1, silently leaving any backlog over one page unprocessed).
    let cursor: string | undefined;
    for (let page = 0; page < MAX_PAGES; page++) {
      const result = await candidateSource.listExpiredClaims({ before: now(), cursor });
      expiredClaimCandidates.push(...(result.items as unknown as ReminderOccurrence[]));
      if (!result.cursor) break;
      cursor = result.cursor;
    }
  }

  if (mode === "DST") {
    // ReminderMaterializer (M3.5) writes a WORKSTATE#DST_PENDING GSI6 pointer whenever a
    // trigger's schedule was computed at an ambiguous/nonexistent local time, OR whose
    // timeZone observes DST at all (see reminder-materializer.ts - broadened per Codex
    // implementation review: a non-ambiguous 09:00 reminder in a DST-observing zone can
    // still need re-evaluation after an offset change, not just literally-ambiguous ones).
    // This query finds those candidates; occurrences in a fixed-offset timeZone never
    // appear here at all.
    const windowStart = now();
    const windowEnd = new Date(Date.parse(windowStart) + 7 * 24 * 60 * 60_000).toISOString();
    let cursor: string | undefined;
    for (let page = 0; page < MAX_PAGES; page++) {
      const result = await candidateSource.listDstCandidates({ window: { start: windowStart, end: windowEnd }, cursor });
      for (const light of result.items) {
        const policy = await store.get<ReminderPolicy>(policyKey(light.tenantId, light.policyId));
        if (!policy) continue;
        const item = await store.get<{ PK: string; SK: string; dueDate: string; version: number }>(itemKey(light.tenantId, light.itemId));
        if (!item) continue;
        dstCandidates.push({
          tenantId: light.tenantId,
          itemId: light.itemId,
          itemVersion: item.version,
          itemDueDate: item.dueDate,
          policy,
        });
      }
      if (!result.cursor) break;
      cursor = result.cursor;
    }
  }

  const result = await runReconciliation({ store, tableName, now, shardConfig }, { expiredClaimCandidates, dstCandidates });
  logger.info("reminder-reconciliation complete", { mode, scheduledTime: event.scheduledTime, ...result });
}
/**
 * Real DynamoDB adapter for ReminderReconciliationCandidateSource (M3.5). Separate class,
 * only wired into the ReminderReconciliation Lambda's composition root - same isolation
 * principle as DynamoDbReminderProducerStore. Queries GSI6's global (non-tenant-prefixed)
 * WORKSTATE#CLAIMED/WORKSTATE#DST_PENDING key families - see
 * src/modules/reminder/ports/reconciliation-candidate-source.ts and
 * docs/architecture/m3.5-runtime-design.md for why these keys are global.
 */
import { QueryCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import {
  GSI6PK_WORKSTATE_CLAIMED,
  GSI6PK_WORKSTATE_DST_PENDING,
  type DstReconciliationCandidate,
  type ExpiredClaimCandidate,
  type Page,
  type ReminderReconciliationCandidateSource,
} from "../ports/reconciliation-candidate-source.js";
import { mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";

const DEFAULT_PAGE_SIZE = 200;

export class DynamoDbReminderReconciliationCandidateSource implements ReminderReconciliationCandidateSource {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async listExpiredClaims(input: { before: string; pageSize?: number; cursor?: string }): Promise<Page<ExpiredClaimCandidate>> {
    try {
      const result = await this.client.send(
        new QueryCommand({
          TableName: this.tableName,
          IndexName: "GSI6",
          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_CLAIMED, ":before": input.before },
          Limit: input.pageSize ?? DEFAULT_PAGE_SIZE,
          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
        }),
      );
      // GSI6 is ALL-projected (infra/lib/dynamo-table.ts) - each row already IS the full
      // ReminderOccurrence item, no separate fetch needed.
      const items = (result.Items ?? []) as ExpiredClaimCandidate[];
      return { items, cursor: result.LastEvaluatedKey ? JSON.stringify(result.LastEvaluatedKey) : undefined };
    } catch (err) {
      throw mapDynamoError(err, "ReminderReconciliationCandidateSource.listExpiredClaims");
    }
  }

  async listDstCandidates(input: { window: { start: string; end: string }; pageSize?: number; cursor?: string }): Promise<Page<DstReconciliationCandidate>> {
    try {
      const result = await this.client.send(
        new QueryCommand({
          TableName: this.tableName,
          IndexName: "GSI6",
          KeyConditionExpression: "GSI6PK = :pk AND GSI6SK BETWEEN :start AND :end",
          ExpressionAttributeValues: { ":pk": GSI6PK_WORKSTATE_DST_PENDING, ":start": input.window.start, ":end": input.window.end },
          Limit: input.pageSize ?? DEFAULT_PAGE_SIZE,
          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
        }),
      );
      // GSI6 is ALL-projected - each row already IS the full ReminderOccurrence item.
      const items = (result.Items ?? []) as DstReconciliationCandidate[];
      return { items, cursor: result.LastEvaluatedKey ? JSON.stringify(result.LastEvaluatedKey) : undefined };
    } catch (err) {
      throw mapDynamoError(err, "ReminderReconciliationCandidateSource.listDstCandidates");
    }
  }
}
/** Real DynamoDB adapter for OutboxRelayStore (M3.5). Wired into both
 * DispatchOutboxRelay and OutboxSweeperReminderDispatch composition roots - the two
 * EXACTLY-two roles with `gsi6Read()` (see infra/lib/dynamo-table.ts). */
import { QueryCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey } from "../../dynamodb/occ.js";
import type { OutboxRecord } from "../outbox.js";
import type { OutboxRelayStore } from "../relay-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../dynamodb/sdk-errors.js";

export class DynamoDbOutboxRelayStore implements OutboxRelayStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async tryAcquireLease(key: EntityKey, leaseOwner: string, leaseExpiresAt: string, now: string): Promise<boolean> {
    try {
      await this.client.send(
        new UpdateCommand({
          TableName: this.tableName,
          Key: key,
          UpdateExpression: "SET leaseOwner = :owner, leaseExpiresAt = :expires",
          ConditionExpression: "attribute_not_exists(leaseOwner) OR leaseExpiresAt < :now",
          ExpressionAttributeValues: { ":owner": leaseOwner, ":expires": leaseExpiresAt, ":now": now },
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "OutboxRelayStore.tryAcquireLease");
    }
  }

  async markPublished(key: EntityKey): Promise<void> {
    try {
      await this.client.send(
        new UpdateCommand({
          TableName: this.tableName,
          Key: key,
          // Bug found by Codex implementation review: without also removing GSI6PK/GSI6SK,
          // published records stayed indexed under RECON#OUTBOX#PENDING forever - not a
          // duplicate-send risk (publishOne already checks status === "PUBLISHED"), but the
          // sweeper's query would keep growing to include every record ever published,
          // reading and discarding them on every run.
          UpdateExpression: "SET #status = :published REMOVE leaseOwner, leaseExpiresAt, GSI6PK, GSI6SK",
          ExpressionAttributeNames: { "#status": "status" },
          ExpressionAttributeValues: { ":published": "PUBLISHED" },
        }),
      );
    } catch (err) {
      throw mapDynamoError(err, "OutboxRelayStore.markPublished");
    }
  }

  async listPendingReminderDispatch(input: { destination: string; olderThan: string; pageSize?: number }): Promise<OutboxRecord[]> {
    try {
      const items: OutboxRecord[] = [];
      let exclusiveStartKey: Record<string, unknown> | undefined;
      do {
        const result = await this.client.send(
          new QueryCommand({
            TableName: this.tableName,
            IndexName: "GSI6",
            KeyConditionExpression: "GSI6PK = :pk AND GSI6SK < :before",
            FilterExpression: "destination = :destination",
            ExpressionAttributeValues: {
              ":pk": "RECON#OUTBOX#PENDING",
              ":before": input.olderThan,
              ":destination": input.destination,
            },
            Limit: input.pageSize,
            ExclusiveStartKey: exclusiveStartKey,
          }),
        );
        items.push(...((result.Items ?? []) as OutboxRecord[]));
        exclusiveStartKey = result.LastEvaluatedKey;
      } while (exclusiveStartKey);
      return items;
    } catch (err) {
      throw mapDynamoError(err, "OutboxRelayStore.listPendingReminderDispatch");
    }
  }
}
/**
 * Optimistic concurrency control helper - implementation-blueprint.md #5.2 / M0 deliverable.
 *
 * M0 ships no tables/Lambdas yet (that's M1+), so this module only builds the DynamoDB
 * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
 * so it stays testable without AWS credentials/mocks. M1 wires these builders into the
 * actual DocumentClient calls once tables exist.
 */

export interface EntityKey {
  PK: string;
  SK: string;
}

export interface VersionedUpdateInput {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  /** Additional SET clauses beyond version/updatedAt, e.g. { status: "CANCELLED" }. */
  set: Record<string, unknown>;
  /** Attribute names to REMOVE atomically in the same conditional update - e.g. dropping
   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
   * since M3 (occ.ts was SET-only, cancelled GSI3 pointers were left orphaned because
   * nothing actively queried them); M3.5 needs REMOVE for real because GSI6 pointers ARE
   * actively queried by reconciliation. */
  remove?: string[];
  now?: string;
}

export interface DynamoUpdateCommandInput {
  TableName: string;
  Key: EntityKey;
  UpdateExpression: string;
  ConditionExpression: string;
  ExpressionAttributeNames: Record<string, string>;
  ExpressionAttributeValues: Record<string, unknown>;
}

/**
 * Builds an UpdateItem input enforcing implementation-blueprint.md #5.2's exact
 * ConditionExpression: attribute_exists(PK) AND attribute_exists(SK) AND
 * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
 */
export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
  const now = input.now ?? new Date().toISOString();

  const names: Record<string, string> = {
    "#version": "version",
    "#tenantId": "tenantId",
    "#updatedAt": "updatedAt",
  };
  const values: Record<string, unknown> = {
    ":expectedVersion": input.expectedVersion,
    ":tenantId": input.tenantId,
    ":one": 1,
    ":now": now,
  };

  const setClauses = ["#version = #version + :one", "#updatedAt = :now"];
  let i = 0;
  for (const [field, value] of Object.entries(input.set)) {
    const nameKey = `#set${i}`;
    const valueKey = `:set${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    setClauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  const removeClauses: string[] = [];
  let j = 0;
  for (const field of input.remove ?? []) {
    const nameKey = `#rem${j}`;
    names[nameKey] = field;
    removeClauses.push(nameKey);
    j += 1;
  }

  const expression =
    removeClauses.length > 0
      ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}`
      : `SET ${setClauses.join(", ")}`;

  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: expression,
    ConditionExpression:
      "attribute_exists(PK) AND attribute_exists(SK) AND #version = :expectedVersion AND #tenantId = :tenantId",
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export interface DynamoPutCommandInput {
  TableName: string;
  Item: Record<string, unknown>;
  ConditionExpression: string;
}

/**
 * Builds a PutItem input for first-time creation of a versioned entity, per
 * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
 * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
 */
export function buildVersionedCreate(tableName: string, item: Record<string, unknown> & EntityKey): DynamoPutCommandInput {
  return {
    TableName: tableName,
    Item: item,
    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
  };
}

/**
 * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
 * (2026-08-19, Engineering Maturity Review): that was the only place they were declared,
 * but reminder/ports/reminder-store.ts already re-exported them from there (a cross-module
 * ports->ports dependency), and expiration/domain/audit-event.ts imported them directly,
 * which is a domain->ports boundary violation dependency-cruiser catches but ESLint's
 * text-matching no-restricted-imports rule cannot (it only matches literal specifier
 * substrings, and this codebase's relative imports never contain "modules/"). Reuses the
 * inner Put/Update shapes already defined above instead of re-declaring them a third time.
 */
export interface TransactPutEntry {
  Put: DynamoPutCommandInput;
}

export interface TransactUpdateEntry {
  Update: DynamoUpdateCommandInput;
}

export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry;

/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
 * ConditionExpression - used by callers to distinguish OCC/idempotency conflicts from other
 * DynamoDB errors without importing the SDK type. */
export const TRANSACTION_CANCELED = "TransactionCanceledException";

export function isTransactionCanceled(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === TRANSACTION_CANCELED
  );
}

/** Name of the AWS SDK error thrown when a ConditionExpression fails - used by callers
 * to distinguish OCC conflicts from other DynamoDB errors without importing the SDK type. */
export const CONDITIONAL_CHECK_FAILED = "ConditionalCheckFailedException";

export function isConditionalCheckFailed(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === CONDITIONAL_CHECK_FAILED
  );
}
test/unit/reminder\reminder-materializer.test.ts:22:    version: 1,
test/unit/reminder\reconciliation.test.ts:55:      version: 1,
test/unit/reminder\reconciliation.test.ts:81:        version: 1,
test/unit/reminder\reconciliation.test.ts:114:        version: 1,
test/unit/reminder\reconciliation.test.ts:169:        version: 1,
test/unit/reminder\producer.test.ts:74:      version: 1,
test/unit/reminder\in-memory-store.ts:51:          const expectedVersion = entry.Update.ExpressionAttributeValues[":expectedVersion"];
test/unit/reminder\in-memory-store.ts:53:          if (existing["version"] !== expectedVersion || existing["tenantId"] !== expectedTenantId) {
src/modules/reminder\ports\reconciliation-candidate-source.ts:26:  version: number;
src/shared\contracts\events.ts:13:  version: number;
src/shared\dynamodb\occ.ts:19:  expectedVersion: number;
src/shared\dynamodb\occ.ts:43: * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
src/shared\dynamodb\occ.ts:54:    ":expectedVersion": input.expectedVersion,
src/shared\dynamodb\occ.ts:90:      "attribute_exists(PK) AND attribute_exists(SK) AND #version = :expectedVersion AND #tenantId = :tenantId",
src/modules/reminder\http\policy-handlers.ts:91:    const expectedVersion = requireExpectedVersion(req);
src/modules/reminder\http\policy-handlers.ts:93:    const policy = await deps.policies.updatePolicy(context, policyId, req.body, expectedVersion);
src/modules/reminder\http\policy-handlers.ts:101:    const expectedVersion = requireExpectedVersion(req);
src/modules/reminder\http\policy-handlers.ts:103:    await deps.policies.disablePolicy(context, policyId, expectedVersion);
src/modules/reminder\application\reminder-materializer.ts:195:        version: 1,
src/modules/reminder\application\reminder-materializer.ts:235:              expectedVersion: occurrence.version,
src/modules/reminder\application\reminder-policy-service.ts:59:      version: 1,
src/modules/reminder\application\reminder-policy-service.ts:81:    expectedVersion: number,
src/modules/reminder\application\reminder-policy-service.ts:105:            expectedVersion,
src/modules/reminder\application\reminder-policy-service.ts:117:    return { ...policy, ...(set as Partial<ReminderPolicy>), version: expectedVersion + 1, updatedAt: this.now() };
src/modules/reminder\application\reminder-policy-service.ts:120:  async disablePolicy(ctx: RequestContext, policyId: string, expectedVersion: number): Promise<void> {
src/modules/reminder\application\reminder-policy-service.ts:130:            expectedVersion,
src/modules/reminder\domain\notification-intent.ts:27:  version: number;
src/modules/reminder\domain\reminder-policy.ts:43:  version: number;
src/modules/reminder\domain\reminder-occurrence.ts:11:export interface ReminderOccurrence extends EntityKey {
src/modules/reminder\domain\reminder-occurrence.ts:29:  version: number;

codex
Os quatro consertos estruturais estão presentes, mas encontrei um ponto que merece rebaixar o veredito: o detector de DST afirma uma garantia que não existe ao comparar apenas 15/jan e 15/jul. Estou confirmando o impacto e também a semântica exata do placeholder do Scheduler antes da nota.
web search: 
web search: site:docs.aws.amazon.com scheduler templated target context attributes aws.scheduler.scheduled-time Input EventBridge Scheduler ...
2026-08-19T22:20:56.542341Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "node -e \"const zones=Intl.supportedValuesOf('timeZone'); const off=(z,m)=>{const d=new Date(Date.UTC(2026,m,15,12));const p=Object.fromEntries(new Intl.DateTimeFormat('en-US',{timeZone:z,hourCycle:'h23',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit'}).formatToParts(d).filter(x=>x.type"'!=='"'literal').map(x=>[x.type,x.value]));return (Date.UTC(+p.year,+p.month-1,+p.day,+p.hour,+p.minute,+p.second)-d)/60000}; for(const z of zones){const a=Array.from({length:12},(_,m)=>off(z,m)); if(a[0]===a[6]&&new Set(a).size>1) console.log(z,a.join(','));}\"; rg -n -C 5 \"jan|july|timeZoneObservesDst|DST_PENDING\" test/unit/reminder/reminder-materializer.test.ts test/unit/reminder/reconciliation.test.ts; git diff a6679d4"'^ a6679d4 --check'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "node -e \"const zones=Intl.supportedValuesOf('timeZone'); const off=(z,m)=>{const d=new Date(Date.UTC(2026,m,15,12));const p=Object.fromEntries(new Intl.DateTimeFormat('en-US',{timeZone:z,hourCycle:'h23',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit'}).formatToParts(d).filter(x=>x.type"'!=='"'literal').map(x=>[x.type,x.value]));return (Date.UTC(+p.year,+p.month-1,+p.day,+p.hour,+p.minute,+p.second)-d)/60000}; for(const z of zones){const a=Array.from({length:12},(_,m)=>off(z,m)); if(a[0]===a[6]&&new Set(a).size>1) console.log(z,a.join(','));}\"; rg -n -C 5 \"jan|july|timeZoneObservesDst|DST_PENDING\" test/unit/reminder/reminder-materializer.test.ts test/unit/reminder/reconciliation.test.ts; git diff a6679d4"'^ a6679d4 --check' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "node -e \"const zones=Intl.supportedValuesOf('timeZone'); const off=(z,m)=>{const d=new Date(Date.UTC(2026,m,15,12));const p=Object.fromEntries(new Intl.DateTimeFormat('en-US',{timeZone:z,hourCycle:'h23',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit'}).formatToParts(d).filter(x=>x.type"'!=='"'literal').map(x=>[x.type,x.value]));return (Date.UTC(+p.year,+p.month-1,+p.day,+p.hour,+p.minute,+p.second)-d)/60000}; for(const z of zones){const a=Array.from({length:12},(_,m)=>off(z,m)); if(a[0]===a[6]&&new Set(a).size>1) console.log(z,a.join(','));}\"; rg -n -C 5 \"jan|july|timeZoneObservesDst|DST_PENDING\" test/unit/reminder/reminder-materializer.test.ts test/unit/reminder/reconciliation.test.ts; git diff a6679d4"'^ a6679d4 --check'` rejected: blocked by policy
codex
## Veredito

A correção ainda **não está pronta para as Camadas 2/3**. Quatro problemas foram corrigidos de forma adequada, mas a alteração do lifecycle DST introduziu uma race condition importante e o detector de DST continua incompleto.

### P1 — remoção pode apagar um ponteiro `WORKSTATE#CLAIMED` válido

Em `reconcileDst`, o item é relido por `queryByItem`, portanto `occurrence.version` é recente. Isso, porém, não torna a operação segura:

1. A consulta ao GSI6 encontra a ocorrência como `DST_PENDING`.
2. O producer pode depois fazer claim, mudando-a para `CLAIMED`, substituindo o GSI6 por `WORKSTATE#CLAIMED` e incrementando a versão.
3. `queryByItem` lê justamente essa nova versão `CLAIMED`.
4. O caminho “confirmado” aceita `SCHEDULED` **ou `CLAIMED`** e remove `GSI6PK/GSI6SK` usando a versão atual.
5. A condição OCC passa e o ponteiro de recuperação do claim é apagado.

Portanto, a versão usada é atual, mas o **estado/index family protegido pela condição está errado**. O update deveria exigir atomicamente algo como:

```text
status = SCHEDULED
AND GSI6PK = WORKSTATE#DST_PENDING
AND version = expectedVersion
```

No mínimo, ocorrências `CLAIMED` não deveriam entrar no caminho de limpeza do ponteiro DST. O mesmo merece revisão no caminho de cancelamento divergente.

### P2 — `timeZoneObservesDst` tem falsos negativos

Comparar somente os offsets de 15/jan e 15/jul não garante detectar toda zona com transições no ano. Zonas podem:

- suspender DST durante um intervalo curto e voltar ao mesmo offset;
- ter mais de duas transições;
- mudar regras sem que janeiro e julho terminem com offsets diferentes.

`Africa/Casablanca`, com mudanças associadas ao Ramadã, é o exemplo clássico dessa categoria. A afirmação no comentário de que um dos dois pontos é “garantido” estar em cada lado da transição é incorreta.

Uma solução simples seria amostrar todos os meses — ou, com melhor precisão, percorrer o ano em intervalos regulares e detectar qualquer mudança de offset. Também faltou teste unitário direto desse helper, incluindo uma zona com transição interna e offsets iguais em janeiro/julho.

### Correções confirmadas

- **Contrato Scheduler → handlers:** correto. A AWS documenta oficialmente que `<aws.scheduler.scheduled-time>` é substituído no payload pelo horário agendado, em ISO-8601. O `Input` JSON no nível raiz está correto. [Documentação oficial](https://docs.aws.amazon.com/scheduler/latest/UserGuide/managing-schedule-context-attributes.html)
- **Remoção do `DST_PENDING` após confirmação:** a intenção foi implementada, mas está insegura pela race acima.
- **Critério de criação do `DST_PENDING`:** ampliado corretamente em conceito, porém o helper não cobre todos os fusos.
- **Paginação:** agora consome cursores corretamente, com limite operacional explícito de 25 páginas.
- **Outbox `markPublished`:** `REMOVE GSI6PK, GSI6SK` resolve o crescimento permanente do índice e ocorre atomicamente com a mudança de status.

**Nota final: 7,4/10.**

A implementação melhorou substancialmente em relação aos 5,8, mas permanece abaixo do gate 9,0 do protocolo. Eu corrigiria a condição atômica da limpeza DST e substituiria/testaria o detector de transições antes de usar as Camadas 2/3 como evidência de aprovação.
tokens used
64.560
## Veredito

A correção ainda **não está pronta para as Camadas 2/3**. Quatro problemas foram corrigidos de forma adequada, mas a alteração do lifecycle DST introduziu uma race condition importante e o detector de DST continua incompleto.

### P1 — remoção pode apagar um ponteiro `WORKSTATE#CLAIMED` válido

Em `reconcileDst`, o item é relido por `queryByItem`, portanto `occurrence.version` é recente. Isso, porém, não torna a operação segura:

1. A consulta ao GSI6 encontra a ocorrência como `DST_PENDING`.
2. O producer pode depois fazer claim, mudando-a para `CLAIMED`, substituindo o GSI6 por `WORKSTATE#CLAIMED` e incrementando a versão.
3. `queryByItem` lê justamente essa nova versão `CLAIMED`.
4. O caminho “confirmado” aceita `SCHEDULED` **ou `CLAIMED`** e remove `GSI6PK/GSI6SK` usando a versão atual.
5. A condição OCC passa e o ponteiro de recuperação do claim é apagado.

Portanto, a versão usada é atual, mas o **estado/index family protegido pela condição está errado**. O update deveria exigir atomicamente algo como:

```text
status = SCHEDULED
AND GSI6PK = WORKSTATE#DST_PENDING
AND version = expectedVersion
```

No mínimo, ocorrências `CLAIMED` não deveriam entrar no caminho de limpeza do ponteiro DST. O mesmo merece revisão no caminho de cancelamento divergente.

### P2 — `timeZoneObservesDst` tem falsos negativos

Comparar somente os offsets de 15/jan e 15/jul não garante detectar toda zona com transições no ano. Zonas podem:

- suspender DST durante um intervalo curto e voltar ao mesmo offset;
- ter mais de duas transições;
- mudar regras sem que janeiro e julho terminem com offsets diferentes.

`Africa/Casablanca`, com mudanças associadas ao Ramadã, é o exemplo clássico dessa categoria. A afirmação no comentário de que um dos dois pontos é “garantido” estar em cada lado da transição é incorreta.

Uma solução simples seria amostrar todos os meses — ou, com melhor precisão, percorrer o ano em intervalos regulares e detectar qualquer mudança de offset. Também faltou teste unitário direto desse helper, incluindo uma zona com transição interna e offsets iguais em janeiro/julho.

### Correções confirmadas

- **Contrato Scheduler → handlers:** correto. A AWS documenta oficialmente que `<aws.scheduler.scheduled-time>` é substituído no payload pelo horário agendado, em ISO-8601. O `Input` JSON no nível raiz está correto. [Documentação oficial](https://docs.aws.amazon.com/scheduler/latest/UserGuide/managing-schedule-context-attributes.html)
- **Remoção do `DST_PENDING` após confirmação:** a intenção foi implementada, mas está insegura pela race acima.
- **Critério de criação do `DST_PENDING`:** ampliado corretamente em conceito, porém o helper não cobre todos os fusos.
- **Paginação:** agora consome cursores corretamente, com limite operacional explícito de 25 páginas.
- **Outbox `markPublished`:** `REMOVE GSI6PK, GSI6SK` resolve o crescimento permanente do índice e ocorre atomicamente com a mudança de status.

**Nota final: 7,4/10.**

A implementação melhorou substancialmente em relação aos 5,8, mas permanece abaixo do gate 9,0 do protocolo. Eu corrigiria a condição atômica da limpeza DST e substituiria/testaria o detector de transições antes de usar as Camadas 2/3 como evidência de aprovação.
