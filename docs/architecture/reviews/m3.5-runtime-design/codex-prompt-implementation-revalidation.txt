Revalidacao da IMPLEMENTACAO do milestone M3.5, expiration-tracker, protocolo Claude<->Codex (AGENTS.md secao 4). Voce revisou a implementacao anteriormente (docs/architecture/reviews/m3.5-runtime-design/codex-output-implementation-review.txt) e deu nota 5,8/10, apontando 5 problemas: (P1) contrato incompativel entre EventBridge Scheduler e os handlers (event.detail.mode / event.time nunca existiriam, quebrando toda invocacao real); (P1) ponteiro GSI6 WORKSTATE#DST_PENDING nunca removido quando a reconciliacao CONFIRMA que o schedule segue correto, alem do criterio de quando o ponteiro e criado ser mais estreito que o design; (P2) paginacao do port ignorada pelo handler de reconciliacao; (P2) markPublished do outbox relay nao remove GSI6PK/GSI6SK do indice RECON#OUTBOX#PENDING.

Corrigi os 5 pontos no commit a6679d4 (branch develop, HEAD atual): rode `git log --oneline -3` e `git show a6679d4 --stat` para orientar-se, depois leia os diffs reais (`git show a6679d4`). Resumo das correcoes: (1) infra/lib/reminder-schedule.ts agora envia {mode?, scheduledTime: "<aws.scheduler.scheduled-time>"} no nivel raiz do Input para todos os 4 schedules; reminder-producer-handler.ts e reminder-reconciliation-handler.ts foram retipados para ler esses campos diretamente (sem event.detail/event.time); teste de contrato novo em test/infra/stack.test.ts prova isso via CDK synth. (2) reconcileDst (reconciliation.ts) agora remove GSI6PK/GSI6SK tambem no caminho "confirmado, nao diverge" (nao so no cancelamento); reminder-materializer.ts amplia o criterio de escrita do ponteiro para incluir qualquer timeZone que observa DST no ano do agendamento (novo helper timeZoneObservesDst em recurrence.ts), nao so horarios literalmente ambiguos/inexistentes. Teste novo em reconciliation.test.ts prova a remocao no caso confirmado. (3) reminder-reconciliation-handler.ts drena paginas ate MAX_PAGES=25 usando o cursor que o adapter ja retornava. (4) dynamodb-outbox-relay-store.ts's markPublished agora inclui "REMOVE GSI6PK, GSI6SK" no UpdateExpression.

145 testes totais, typecheck/lint/check-boundaries limpos (verifique voce mesmo se quiser, mas nao precisa rodar rebuild completo - foque em ler o codigo).

Verifique se as 5 correcoes realmente resolvem os problemas apontados, sem introduzir bugs novos. Preste atencao especial a: o campo `scheduledTime` do EventBridge Scheduler runtime realmente populado corretamente pela sintaxe "<aws.scheduler.scheduled-time>" (voce pode nao ter certeza sem doc AWS - se nao tiver confianca na sintaxe exata, diga isso explicitamente em vez de supor); se o helper timeZoneObservesDst tem algum caso extremo errado (fuso horario que muda de regra de DST entre janeiro e julho de forma que o teste de dois pontos falhe); se a remocao do ponteiro no caso "confirmado" do reconcileDst esta usando a versao correta do item (race condition possivel).

De uma nota final 0-10 na implementacao corrigida como pronta para as Camadas 2/3 de teste. Responda em portugues, direto ao ponto.
