Continuando o protocolo de nota cega Claude<->Codex (AGENTS.md secao 4, expiration-tracker) para o milestone M3.5 (runtime real do Reminder Engine / fechamento de G8). Esta e a Rodada 2.

Sua proposta independente da Rodada 1 esta em docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt (voce ja a escreveu). A proposta independente da Claude na Rodada 1 esta em docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md - leia agora, e leia tambem a critica da Claude a SUA proposta em docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md (mesmo diretorio).

Resumo do que a critica da Claude faz: (1) concorda que a proposta do Codex identificou dois gaps reais que a proposta da Claude nao tinha visto - falta de durabilidade entre claim (TransactWrite) e SendMessage direto (adota outbox+relay via DynamoDB Streams), e reconciliacao sem fonte real de candidatos (GSI6 nao consultavel ainda, precisa entrar no escopo do milestone via um port novo estreito); (2) concorda com sandbox AWS efemero como terceira camada de teste, cadencia curta para reconciliacao de claims expirados separada de DST diario, e Toxiproxy para fault injection de timeout; (3) diverge em tres pontos: (a) usar DynamoDB Local (nao LocalStack) para a suite rapida que so prova paridade adapter-vs-fake em memoria, reservando LocalStack/Testcontainers para a camada que precisa de SQS+Streams+EventBridge reais - argumento: menos peso/determinismo para o que nao precisa de SQS; (b) EventBridge Scheduler (nao Rule legado) porque implementation-blueprint.md linha 162 ja nomeia "EventBridge Scheduler" explicitamente para o producer e essa e uma decisao Type 1 ja aprovada (Claude 9.20/Codex 9.2) no blueprint, nao deveria ser reaberta neste milestone sem necessidade tecnica forte; (c) reusar o OutboxEvent/shared/outbox/outbox.ts ja existente (mesma TransactWriteItems do claim, com eventType novo) para o registro de dispatch outbox, em vez de um mecanismo de outbox paralelo novo.

Escreva sua treplica: (1) responda especificamente aos 3 pontos de divergencia (a, b, c acima) - concorda, discorda com justificativa tecnica, ou propoe uma terceira opcao; (2) confirme se os dois gaps que voce identificou (outbox/relay, GSI6/reconciliation candidate source) continuam sua leitura correta depois de ver a proposta original da Claude; (3) de uma nota de confianca 0-10 na proposta CONVERGENTE resultante (nao mais na sua proposta isolada) considerando os ajustes descritos acima. Se a nota for abaixo de 9.0, aponte exatamente o que falta para chegar a 9.0.

Responda em portugues, direto ao ponto, sem preambulo.
