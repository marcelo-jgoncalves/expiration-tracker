OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a01b34-b35d-72d3-aea9-c1bae9e78322
--------
user
Validacao final do documento convergido do protocolo Claude<->Codex (AGENTS.md secao 4, expiration-tracker) para o milestone M3.5 (runtime real do Reminder Engine / fechamento de G8).

Voce ja participou das Rodadas 1 e 2 (suas respostas em codex-output-round1.txt e codex-output-round2.txt, mesmo diretorio docs/architecture/reviews/m3.5-runtime-design/). Na Rodada 2 voce deu nota de confianca 9,3/10 na proposta convergente descrita em prosa pela Claude.

Agora leia o DOCUMENTO NORMATIVO FINAL que a Claude escreveu a partir dessa convergencia: docs/architecture/m3.5-runtime-design.md (raiz de docs/architecture/, nao dentro de reviews/).

Sua tarefa agora e VALIDACAO, nao mais proposta nem critica de conteudo tecnico novo:

1. O documento representa fielmente o que foi convergido nas Rodadas 1 e 2, ou introduz alguma mudanca silenciosa, imprecisao, contradicao interna, ou promessa que os dois lados nao debateram de fato?
2. Alguma das 5 secoes de "Componentes do milestone" ficou vaga ou subespecificada a ponto de nao dar para comecar a implementar sem re-decidir algo em pleno codigo?
3. A secao "O que este milestone explicitamente NAO fecha" continua precisa em relacao aos 5 P1 do red team (checkpoint-12)?
4. De uma nota FINAL 0-10 a este documento como especificacao pronta para implementacao (nao a ideia em abstrato). Se abaixo de 9.0, liste exatamente o que teria que mudar no arquivo para chegar a 9.0 - mudancas pontuais, nao nova rodada de design.

Responda em portugues, direto ao ponto, sem preambulo.

codex
Vou confrontar o documento final com as duas respostas registradas, o checkpoint-12 e o status normativo vigente, buscando apenas divergências ou lacunas de especificação.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt; Get-Content -Raw docs/architecture/reviews/m3.5-runtime-design/codex-output-round2.txt; Get-Content -Raw docs/architecture/m3.5-runtime-design.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 338ms:
# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o

## PrÃ³xima aÃ§Ã£o obrigatÃ³ria (2026-08-19, sessÃ£o de design M3.5 â€” mais recente, leia isto primeiro)

Fase de **design** do milestone M3.5 (runtime real do Reminder Engine / fechamento de G8) concluÃ­da e **APPROVED** (Claude 9.0/Codex 9.3, protocolo `AGENTS.md` Â§4, 2 rodadas). Documento normativo: `docs/architecture/m3.5-runtime-design.md`. Artefatos de rodada (propostas independentes, crÃ­tica cruzada, trÃ©plica) em `docs/architecture/reviews/m3.5-runtime-design/`.

**MudanÃ§a de escopo real, decidida na convergÃªncia de design (nÃ£o estava no escopo original)**: wiring real do GSI6 (antes item 4 da lista de pendÃªncias pÃ³s-M3, tratado como follow-up separado) **entra neste milestone** â€” sem uma fonte real de candidatos de reconciliaÃ§Ã£o, a EventBridge Rule de reconciliaÃ§Ã£o dispararia sobre arrays vazios (false-green). TambÃ©m decidido: outbox durÃ¡vel (reuso de `shared/outbox/outbox.ts`) entre o claim do producer e a publicaÃ§Ã£o em SQS â€” `TransactWrite(claim)` seguido de `SendMessage` direto nÃ£o Ã© atÃ´mico, gap que nenhuma das duas propostas independentes tinha visto sozinha, sÃ³ apareceu na crÃ­tica cruzada.

**PrÃ³xima aÃ§Ã£o real**: implementar o milestone seguindo `docs/architecture/m3.5-runtime-design.md` seÃ§Ã£o "Componentes do milestone" (5 itens: adapters DynamoDB reais, handlers Lambda reais, SQS+DLQ+outbox relay, EventBridge Scheduler+GSI6, testes em 3 camadas incluindo sandbox AWS efÃªmero). Nada disso foi implementado ainda nesta sessÃ£o â€” sÃ³ o design. NÃ£o pular a implementaÃ§Ã£o do outbox/relay nem do port de reconciliaÃ§Ã£o achando que "fila+DLQ bastam" â€” foi exatamente o erro que a Rodada 1 cometeu isoladamente dos dois lados.

---

## PrÃ³xima aÃ§Ã£o obrigatÃ³ria (histÃ³rico â€” superada pela seÃ§Ã£o acima, preservada como contexto de G8)

Engineering Maturity Review concluÃ­da (checkpoints 0, 1, 2-9, 12; ver `ENGINEERING.md` na raiz para o relatÃ³rio completo). Veredito: `ENGINEERING FOUNDATION STATUS: NOT APPROVED`, bloqueador Ãºnico e conhecido: **G8 (recuperaÃ§Ã£o real de falha assÃ­ncrona)**.

**DecisÃ£o do usuÃ¡rio (2026-08-19)**: tratar o fechamento pleno de G8 como **novo milestone dedicado** (mesma disciplina de M0-M3: pesquisa/design â†’ implementaÃ§Ã£o â†’ teste real â†’ revisÃ£o Claude+Codex), nÃ£o como remediaÃ§Ã£o de sessÃ£o. Escopo desse milestone, per `ENGINEERING.md` (seÃ§Ã£o "DecisÃ£o pendente: escopo de G8"):

- Adapters DynamoDB reais implementando os ports (`ReminderStore`, `ExpirationStore`, `IdentityStore`) contra AWS real â€” hoje sÃ³ existem fakes em memÃ³ria para teste.
- Handlers Lambda reais substituindo os placeholders `exports.handler = async () => ({statusCode: 501})` em `infra/lib/expiration-tracker-stack.ts`.
- Filas SQS + DLQ com redrive policy para o pipeline producerâ†’dispatch.
- EventBridge Scheduled Rule disparando o `ReminderProducer` periodicamente (a cada minuto) e a `ReminderReconciliation` periodicamente.
- Testes de fault injection contra esse runtime real (timeout de dependÃªncia, poison message, redrive).

Antes de comeÃ§ar esse milestone: ler `docs/engineering/reviews/checkpoint-12-redteam/summary.md` (red team formal jÃ¡ identificou 5 P1 relacionados: pipeline sem recuperaÃ§Ã£o, idempotÃªncia nÃ£o provada no limite do efeito externo, false-green CI, concorrÃªncia/estado obsoleto entre renew/cancel/materializaÃ§Ã£o/dispatch/reconciliation, blast radius cross-tenant do GSI3 via workers privilegiados) â€” usar como input de design, nÃ£o redescobrir do zero.

Trabalho jÃ¡ feito nesta sessÃ£o de engenharia (nÃ£o repetir): CI real corrigido e confirmado verde em 5 execuÃ§Ãµes; branch `develop` estabelecida como padrÃ£o de trabalho (`AGENTS.md` Â§3), `main` protegida; `dependency-cruiser` como enforcement real de boundary (achou e corrigiu 2 violaÃ§Ãµes genuÃ­nas de arquitetura); testes de `reconciliation.ts`/`producer.ts` que nÃ£o existiam; tentativa de fechar a vulnerabilidade de devDependency (EX-001) via upgrade do Vitest foi revertida por quebrar CI real (bug upstream do npm, nÃ£o repetir sem verificar correÃ§Ã£o).

---

**CorreÃ§Ã£o de 2026-08-19 (Engineering Maturity Review, Checkpoint 2-9)**: as seÃ§Ãµes abaixo (escritas ao longo das sessÃµes de M0-M3) afirmam "nada foi commitado" e citam contagens de teste por marco que, somadas, nÃ£o batem com a realidade medida agora. Estado real verificado por execuÃ§Ã£o: M0-M3 **jÃ¡ estÃ¡ commitado** num Ãºnico commit (`154d6e0`), e `npm test` roda **123 testes** no total (19 arquivos), nÃ£o a soma das contagens individuais citadas abaixo. As seÃ§Ãµes nÃ£o foram reescritas (preservadas como histÃ³rico de sessÃ£o), mas nÃ£o devem ser lidas como estado de commit/contagem de teste vigente â€” confiar em `git log`/`npm test` reais, nÃ£o nesses nÃºmeros. Ver `docs/engineering/03-repository-baseline.md` e `docs/engineering/reviews/checkpoint-02-09-consolidated/` para a anÃ¡lise completa desta divergÃªncia.

Projeto: micro-SaaS de controle de vencimentos/renovaÃ§Ãµes. Pasta: `c:\Users\Usuario\Desktop\projects\expiration-tracker\`. Repo GitHub: `marcelo-jgoncalves/expiration-tracker` (privado).

Mapa completo de documentaÃ§Ã£o, status vigente e regra de precedÃªncia: `docs/architecture/README.md`. Regras de processo e ferramentas: `AGENTS.md`. Log cronolÃ³gico de sessÃµes: `docs/architecture/session-log.md`.

## Status atual

```text
DESIGN MATURITY STATUS: APPROVED (arquitetura conceitual + Implementation Blueprint)
ARCHITECTURE STATUS: NOT APPROVED
```

Todo o processo de design (Fases 0-3 do prompt mestre + os 14 entregÃ¡veis das seÃ§Ãµes 35-52 + threat model seÃ§Ã£o 33 + Implementation Blueprint seÃ§Ã£o 60) estÃ¡ completo e aprovado â€” ver `ARCHITECTURE.md` na raiz para o documento consolidado. `ARCHITECTURE STATUS: NOT APPROVED` Ã© o estado normativo correto atÃ© haver implementaÃ§Ã£o real testada sob falha/carga (rubrica B, `requirements.md` Â§13.1) â€” **nÃ£o Ã© reprovaÃ§Ã£o de mÃ©rito, e nÃ£o muda com a conclusÃ£o do blueprint**: o blueprint Ã© design detalhado, nÃ£o evidÃªncia operacional.

## ConcluÃ­do nesta sessÃ£o â€” Implementation Blueprint (seÃ§Ã£o 60) â€” APPROVED

`docs/architecture/implementation-blueprint.md`. Componentes/mÃ³dulos (Identity, Expiration, Reminder, Notification, Document, Audit + workers assÃ­ncronos), interfaces concretas, eventos/schemas reais (grounded em `data-model.md`), ordem de deploy, milestones M0-M7, critÃ©rios de aceite tÃ©cnicos por componente, e as 7 lacunas do `threat-model.md` incorporadas como requisito desde o inÃ­cio (nÃ£o apÃªndice). Processo: 2 propostas independentes (Claude/Codex) â†’ crÃ­tica cruzada (17 problemas reais encontrados, incluindo um erro tÃ©cnico presente nas duas propostas â€” chave do GSI3 do scheduler nÃ£o era consultÃ¡vel) â†’ convergÃªncia â†’ 5 rodadas de nota cega com correÃ§Ã£o pontual a cada achado (cabeÃ§alho prematuro, contradiÃ§Ã£o de kill switch, tabela incompleta, decisÃµes Type 1 nÃ£o propagadas Ã s seÃ§Ãµes operativas, mapeamento de estado da Step Functions incompleto). **Nota final: Claude 9.20 / Codex 9.2 (exato)** â€” ambos â‰¥9.0 sem arredondar, 8 rodadas totais, nenhum gate violado. DecisÃ£o Type 1 nova registrada: chave global do GSI3 (`GSI3PK=DUE#yyyyMMddHHmm#NN`, exceÃ§Ã£o documentada Ã  regra de toda chave comeÃ§ar por `TENANT#tenantId`) â€” a decisÃ£o estÃ¡ fechada no blueprint; falta sÃ³ o registro mecÃ¢nico em `data-model.md` (nÃ£o bloqueia implementaÃ§Ã£o).

## ConcluÃ­do nesta sessÃ£o â€” M0 "Guardrails e contratos" (implementation-blueprint.md Â§19)

Todas as entregas de M0 implementadas e testadas (53 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): estrutura TypeScript (`src/shared/*`, alinhada a `implementation-blueprint.md` Â§2), schemas JSON (`schemas/{events,queues,api}` + `sensitive-fields.json`, validados via Ajv em `src/shared/contracts/schema-validator.ts`), `SecureLogger`+`Redactor` central (`src/shared/observability/`, corpus de teste com valores canÃ¡rio provando que nada vaza), configuraÃ§Ã£o tipada fail-fast (`src/shared/config/config.ts`), taxonomia de erro normalizada retryable/terminal (`src/shared/errors/app-error.ts`), idempotÃªncia via `PutItem attribute_not_exists(PK)` (`src/shared/idempotency/idempotency.ts`, chaves por operaÃ§Ã£o conforme `data-model.md` Â§4), OCC com `ConditionExpression: version=:expected` (`src/shared/dynamodb/occ.ts`, `data-model.md` Â§5), outbox transacional (`src/shared/outbox/outbox.ts`, shape de `implementation-blueprint.md` Â§5.3), pipeline supply-chain (`.github/workflows/ci.yml`: npm ci imutÃ¡vel, actions pinadas por SHA, SBOM CycloneDX, audit, gate de schema). `AGENTS.md` Â§6 atualizado com comandos/convenÃ§Ãµes. Nada foi commitado (working tree aberto para revisÃ£o).

Judgment calls (blueprint estava silente): Ajv+ajv-formats para validaÃ§Ã£o de JSON Schema; Vitest como test runner; ESLint `no-console` (com exceÃ§Ã£o para `src/shared/observability/**`) como o mecanismo que faz "chamada direta a `console.*` falhar no lint" (`implementation-blueprint.md` Â§14.1); idempotÃªncia/OCC/outbox construÃ­dos como builders puros de parÃ¢metros DynamoDB (sem `@aws-sdk` ainda) para ficarem testÃ¡veis sem tabelas reais, que sÃ³ existem a partir de M1; SLSA/assinatura de artefato adiada para M1+ (nÃ£o hÃ¡ alvo de deploy ainda); SHAs de actions no CI pinados no momento da escrita â€” revisar antes do primeiro run real.

## ConcluÃ­do nesta sessÃ£o â€” M1 "Foundation, Identity e isolamento" (implementation-blueprint.md Â§19)

Todas as entregas implementadas e testadas (89 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/identity/{domain,application,ports,persistence,http}` (resolver central, matriz de autorizaÃ§Ã£o como cÃ³digo, `IdentityMapping`/`User`/`DeviceSession`/`TenantQuota` via portas SDK-agnÃ³sticas) e `infra/{lib,bin}` (`ExpirationTrackerTable`, `ExpirationTrackerAuth`, `ScopedLambdaFunction`, `ExpirationTrackerApi` com a rota `GET /test/ping`). SuÃ­te cross-tenant negativa em `test/integration/cross-tenant.test.ts` (9 casos, exit criterion do marco) e synth de infra em `test/infra/stack.test.ts` (6 casos, via `aws-cdk-lib/assertions`, sem AWS CLI). Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente ou pendÃªncia externa jÃ¡ conhecida): (1) MFA implementado como prop configurÃ¡vel (`OFF`/`OPTIONAL`/`REQUIRED`, default `OPTIONAL`) pois UNK-006 segue pendente de pesquisa externa â€” nÃ£o bloqueava M1; (2) CSP/CloudFront Response Headers Policy **nÃ£o implementado nesta sessÃ£o** â€” nÃ£o hÃ¡ distribuiÃ§Ã£o CloudFront/frontend ainda no repositÃ³rio, e o texto do blueprint (Â§4.2) coloca CSP no contexto do SPA estÃ¡tico servido por CloudFront; tratado como pertencente ao milestone que introduzir o frontend, nÃ£o forÃ§ado em M1 (revisar se essa leitura estiver errada); (3) sessÃ£o/revogaÃ§Ã£o modelada como campo `globalLogoutAfter` no item `User` + item filho `DeviceSession` (`TENANT#t#USER#u`/`SESSION#<deviceId>`) em vez de uma entidade `Session` de primeira classe â€” `data-model.md` nÃ£o define uma; (4) `ScopedLambdaFunction` mantÃ©m a lista de entidades por capability como metadado/documentaÃ§Ã£o, mas o IAM real concedido Ã© table-level (`grantReadWriteData`) â€” DynamoDB IAM nÃ£o expressa restriÃ§Ã£o por SK/entidade da forma que a sintaxe do blueprint sugere visualmente; per-entity IAM mais fino fica como follow-up; (5) `TenantQuota` implementado como janela fixa (fixed-window), nÃ£o sliding-window/leaky-bucket â€” mais simples e ainda satisfaz "decremento atÃ´mico, sem race condition" do `data-model.md`; (6) BFF de sessÃ£o (`/session/refresh`, `/session/logout`) **nÃ£o implementado como rota HTTP nesta sessÃ£o** â€” o Cognito client jÃ¡ estÃ¡ configurado para o padrÃ£o (client secret, tokens de vida curta), mas os endpoints do BFF em si ficam para quando a Expiration/Notification API skeleton crescer alÃ©m da rota de teste; revisar se isso deveria ter sido parte do exit criterion. `cdk synth` real via CLI nÃ£o foi executado (o pacote `aws-cdk` CLI nÃ£o estÃ¡ instalado, sÃ³ a lib `aws-cdk-lib`); a sintetizaÃ§Ã£o foi verificada programaticamente via `Template.fromStack` nos testes, que Ã© equivalente para fins de validaÃ§Ã£o estÃ¡tica.

## ConcluÃ­do nesta sessÃ£o â€” M2 "Expiration core e Audit" (implementation-blueprint.md Â§19)

Todas as entregas implementadas e testadas (134 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/expiration/{domain,application,ports,http}` â€” CRUD/renew (`ExpirationService`: createItem/getItem/updateItem/archiveItem/deleteItem/renewItem/listDashboard), OCC via `shared/dynamodb/occ.ts`, `ItemDueDateChanged` por outbox (`shared/outbox/outbox.ts`) na MESMA `TransactWriteItems` do item quando `dueDate` muda (updateItem e renewItem), `AuditEvent` append-only gravado em toda mutaÃ§Ã£o na mesma transaÃ§Ã£o, dashboard via GSI1. `renewItem` cria um item sucessor (`renewedFromId`) em vez de mutar `dueDate` no agregado de origem, que transiciona para `RENEWED`; Ã© idempotente via `IdempotencyStore` de M0. Rotas HTTP CDK adicionadas em `infra/lib/api.ts`/`expiration-tracker-stack.ts` (`ItemsHandler`, mesmo authorizer JWT de M1). SuÃ­te de integraÃ§Ã£o `test/integration/expiration-lifecycle.test.ts` prova o exit criterion do marco end-to-end via handlers HTTP reais. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) evento `ItemDueDateChanged` segue exatamente o schema jÃ¡ existente desde M0 (`schemas/events/item-due-date-changed.v1.json`: `itemId`/`previousDueDate`/`newDueDate`/`itemVersion`, `additionalProperties: false`) em vez do exemplo mais rico do texto do blueprint Â§8.3 (que inclui `timeZone`/`reminderPolicyId`/`changeReason`) â€” o schema jÃ¡ testado/versionado Ã© a fonte de verdade de contrato per `AGENTS.md` Â§6, o exemplo em prosa nÃ£o; campos adicionais ficam para quando M3 (Reminder) precisar deles, exigindo `.v2` aditivo; (2) `renewItem` tambÃ©m dispara `ItemDueDateChanged` (para o item novo, `previousDueDate: null`) â€” o blueprint sÃ³ descreve esse evento no contexto de `updateItem`, mas renovaÃ§Ã£o tambÃ©m Ã© uma "mudanÃ§a de vencimento" do ponto de vista do futuro `ReminderProducer` (M3), e a alternativa (renovaÃ§Ã£o silenciosa) deixaria o dashboard/scheduler sem sinal de que um novo item precisa de agendamento; (3) `archiveItem` usa a aÃ§Ã£o de autorizaÃ§Ã£o `item:update` (nÃ£o uma aÃ§Ã£o dedicada â€” a matriz de M1 nÃ£o define uma para archive) enquanto `deleteItem` usa `item:delete` (ADMIN_ROLES), seguindo a granularidade que jÃ¡ existe; (4) idempotÃªncia de renovaÃ§Ã£o aceita uma `idempotencyKey` explÃ­cita do chamador (header `Idempotency-Key`) com fallback determinÃ­stico `itemId|expectedVersion|cycle` quando ausente, jÃ¡ que o blueprint define a CHAVE de dedupe mas nÃ£o de onde ela vem por HTTP; (5) `ExpirationStore.transactWrite` foi adicionado ao port (nÃ£o existe em `IdentityStore`) porque nenhuma escrita de M1 precisava de `TransactWriteItems` multi-item â€” a fake em memÃ³ria (`test/unit/expiration/in-memory-store.ts`) sÃ³ entende as duas formas exatas de `ConditionExpression` que este cÃ³digo produz (mesma limitaÃ§Ã£o documentada, mesmo espÃ­rito de `InMemoryIdentityStore`).

## ConcluÃ­do nesta sessÃ£o â€” M3 "Reminder Engine" (implementation-blueprint.md Â§19)

Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversÃ£o localâ†’UTC IANA-aware â€” DST tratado explicitamente, horÃ¡rio inexistente e ambÃ­guo â€” reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lÃ³gica pura testÃ¡vel com relÃ³gio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMEDâ†’TRIGGERED`+`NotificationIntent`+outbox numa Ãºnica transaÃ§Ã£o; reconciliaÃ§Ã£o Ã© um Ãºnico job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluÃ­am `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER funÃ§Ã£o da tabela (inclusive as tenant-facing de M1/M2) â€” a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explÃ­citos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que sÃ³ `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) `occurrenceId` derivado deterministicamente (hash da chave de idempotÃªncia de data-model.md Â§4) em vez de UUID + registro `IdempotencyStore` separado â€” o prÃ³prio `putIfAbsent` condicional jÃ¡ garante a idempotÃªncia de materializaÃ§Ã£o, mesmo padrÃ£o que data-model.md documenta para `WebhookInbox`; (2) cancelamento de ocorrÃªncia stale nÃ£o remove `GSI3PK`/`GSI3SK` (o builder de `occ.ts` Ã© SET-only, sem REMOVE) â€” deixa um ponteiro Ã³rfÃ£o no Ã­ndice, mas o `Query` condicional `SCHEDULEDâ†’CLAIMED` do producer falha sobre ele de forma inofensiva; (3) tolerÃ¢ncia de dispatch (`toleranceMs`, default 30min) e TTL de claim (default 2min) nÃ£o estÃ£o fixados no blueprint â€” valores razoÃ¡veis documentados no cÃ³digo, revisar contra dados reais de produÃ§Ã£o; (4) reconciliaÃ§Ã£o DST/claim-expiry recebe os candidatos como parÃ¢metro (batch) em vez de fazer sua prÃ³pria varredura via GSI6 â€” a wiring desse Ã­ndice de "polÃ­ticas ativas"/"claims expirados" real fica como follow-up de infra, o mecanismo de reconciliaÃ§Ã£o em si (o que a tarefa pediu) estÃ¡ implementado e testado; (5) Lambda handlers reais (bundling com `@aws-sdk`) nÃ£o foram escritos â€” mesmo estÃ¡gio que M0-M2, CDK usa cÃ³digo inline placeholder via `ScopedLambdaFunction`; a lÃ³gica testÃ¡vel Ã© o entregÃ¡vel real desta fase, igual Ã s anteriores.

## PrÃ³xima aÃ§Ã£o obrigatÃ³ria (histÃ³rico â€” superada pela seÃ§Ã£o do topo)

**Superada em 2026-08-19**: a Engineering Maturity Review identificou G8 (recuperaÃ§Ã£o real de falha assÃ­ncrona) como bloqueador de engenharia â€” o milestone de runtime real (adapters/handlers/filas/EventBridge, ver seÃ§Ã£o do topo) Ã© prÃ©-requisito antes de M4 fazer sentido operacionalmente, ainda que M4 nÃ£o dependa dele estruturalmente. Lista original preservada abaixo como histÃ³rico, nÃ£o como prÃ³xima aÃ§Ã£o vigente.

1. **M4 â€” Notification Engine** (`implementation-blueprint.md` Â§19, depende de M3 âœ…): router de `NotificationIntent`â†’canal, delivery workers (email/WhatsApp stub-first), `NotificationAttempt`, resoluÃ§Ã£o de destinatÃ¡rio/template.
2. Decidir e fechar os itens abaixo (BFF de sessÃ£o como rotas reais, CSP/CloudFront quando o frontend existir) antes de considerar a lacuna de "session theft" do threat model totalmente fechada â€” ainda nÃ£o resolvido, re-flagueado a cada sessÃ£o desde M1 para nÃ£o se perder: M1 fechou o mecanismo de revogaÃ§Ã£o/matriz de autorizaÃ§Ã£o, nÃ£o o endpoint BFF completo; M2/M3 nÃ£o tinham escopo de frontend/sessÃ£o (confirmado no blueprint) entÃ£o tambÃ©m nÃ£o o resolveram.
3. **Ratificar formalmente em `data-model.md` a exceÃ§Ã£o do GSI3** (chave global do scheduler, jÃ¡ decidida e justificada em `implementation-blueprint.md` Â§9.2/Â§23.3 item 10) â€” passo de manutenÃ§Ã£o documental, nÃ£o nova rodada de debate. M3 jÃ¡ corrigiu o enforcement real do isolamento em IAM; falta sÃ³ o registro mecÃ¢nico no documento.
4. Wiring real do GSI6 (ou Ã­ndice equivalente) para alimentar a reconciliaÃ§Ã£o de M3 com o batch real de "polÃ­ticas ativas"/"claims a verificar" em produÃ§Ã£o â€” hoje o mecanismo de reconciliaÃ§Ã£o recebe esse batch como parÃ¢metro injetado (testÃ¡vel), a query real fica pendente.
5. Testes de carga real (cenÃ¡rios de drenagem do pico extremo, `capacity-model.md`, com SLO de drenagem UNK-CAP-006 a formalizar em `slo.md` antes do gate de M7), teste de restore real (gate jÃ¡ definido em `disaster-recovery.md` Â§6), exercÃ­cio do runbook de credencial comprometida.
6. ReavaliaÃ§Ã£o sob rubrica (B) â€” Operational Evidence â€” sÃ³ entÃ£o `ARCHITECTURE STATUS` pode legitimamente virar `APPROVED`. **NÃ£o declarar isso antes de haver evidÃªncia operacional real.** Implementar M0/M1/M2/M3 nÃ£o Ã© evidÃªncia operacional.
7. DecisÃµes ainda pendentes de pesquisa externa (nÃ£o bloqueiam M4 estruturalmente, mas bloqueiam habilitar os canais/features especÃ­ficos, ver `implementation-blueprint.md` Â§23.3): provider inicial de e-mail, BSP WhatsApp (pricing real, UNK-003), modelo Bedrock especÃ­fico, regiÃ£o AWS (bloqueante para LGPD/transferÃªncia internacional), MFA obrigatÃ³rio vs. opcional (UNK-006), ferramenta de backup S3 (RPOâ‰¤24h), ferramenta de assinatura/provenance do pipeline de CI.

# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-19
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | 26 decisÃµes (D-000 a D-025), nota Claude/Codex, status |
| `adr/` | decisÃ£o/ADR | 8 ADRs formais para decisÃµes Type 1 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/status-projeto.html` | resumo/Ã­ndice (visual) | Painel visual do status do projeto |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a01b2d-0c11-74d1-b775-7005fd779b52
--------
user
VocÃª Ã© o segundo engenheiro independente num protocolo de nota cega Claude<->Codex (AGENTS.md secao 4 do repo expiration-tracker, c:/Users/Usuario/Desktop/projects/expiration-tracker). NAO leia claude-proposal.md neste diretorio - sua proposta precisa ser independente, sera comparada depois.

Contexto: micro-SaaS serverless AWS (TypeScript, CDK, DynamoDB, SQS, EventBridge). M0-M3 ja implementados e testados (123 testes) mas SEM runtime real: adapters DynamoDB sao fakes em memoria, handlers Lambda sao placeholders `exports.handler = async () => ({statusCode: 501})`, sem filas/EventBridge wired. Um red team formal (checkpoint-12) apontou isso como bloqueador de aprovacao de arquitetura (G8), com 5 P1: (1) pipeline assincrono sem recuperacao real, (2) idempotencia nao provada no limite do efeito externo (at-least-once classico), (3) false-green entre logica pura e comportamento implantado, (4) concorrencia/estado obsoleto entre renew/cancel/materializacao/dispatch/reconciliation, (5) blast radius cross-tenant do GSI3 (IAM ja limita, falta teste negativo contra adapter real).

O usuario decidiu tratar o fechamento de G8 como um milestone dedicado (mesma disciplina de M0-M3: design -> implementacao -> teste real -> revisao Claude+Codex), com este escopo fixo:
1. Adapters DynamoDB reais implementando os ports existentes (ReminderStore, ExpirationStore, IdentityStore) contra AWS real.
2. Handlers Lambda reais substituindo os placeholders 501.
3. Filas SQS + DLQ com redrive policy para o pipeline producer->dispatch.
4. EventBridge Scheduled Rule disparando o ReminderProducer periodicamente (a cada minuto) e a ReminderReconciliation periodicamente.
5. Testes de fault injection contra esse runtime real (timeout de dependencia, poison message, redrive).

Arquitetura relevante ja existente e fixa (nao redesenhar, so integrar):
- Ports SDK-agnosticos ja definidos: ReminderStore (get/putIfAbsent/update/transactWrite/queryByItem), ReminderProducerStore (queryGsi3/get/transactWrite, isolado do resto por desenho), ExpirationStore, IdentityStore - todos em src/modules/*/ports/.
- shared/dynamodb/occ.ts ja produz os parametros de ConditionExpression/TransactWriteItems (builders puros, sem SDK ainda).
- shared/outbox/outbox.ts, shared/idempotency/idempotency.ts, shared/errors/app-error.ts (taxonomia retryable/terminal) ja existem.
- infra/lib/scoped-lambda-function.ts: todo Lambda deve nascer via ScopedLambdaFunction (unico ponto de enforcement de IAM least-privilege), hoje usa lambda.Code.fromInline para os placeholders.
- infra/lib/expiration-tracker-stack.ts ja declara os 5 ScopedLambdaFunction do Reminder Engine (RemindersHandler HTTP, ReminderProducer com gsi3Read exclusivo, ReminderDispatch, ReminderReconciliation) com as capabilities de tabela corretas - falta so trocar o codigo placeholder por bundle real e adicionar fila/schedule.
- GSI3 e deliberadamente isolado: so ReminderProducerStore/ReminderProducer pode fazer Query nele (protecao contra P1 #5), enforcement via IAM real (Table.grantReadWriteData do CDK inclui TODOS os indices por padrao - ja corrigido com PolicyStatement explicito).
- Reserved concurrency e DLQ ja documentados no blueprint (implementation-blueprint.md secao 26): ReminderDispatchWorker concurrency 10, DLQ maxReceiveCount=5; ReminderProducer concurrency 2, sem DLQ (lookback [M-5min,M] cobre falha de tick); ReminderReconciliation concurrency 1, sem DLQ (idempotente, diario).

Produza uma proposta tecnica concreta cobrindo os 5 itens do escopo: escolhas de biblioteca/pacote AWS SDK v3, estrutura de arquivos dos adapters DynamoDB (um por port, mantendo os ports SDK-agnosticos como estao), estrategia de bundling dos handlers Lambda reais, desenho da fila+DLQ (nomes, redrive policy, batch size, partial batch failure), desenho do EventBridge Schedule (Scheduler vs Rule legado, flexible time window), e como voce testaria fault injection de forma real (nao mockada) sem tornar o CI lento/fragil demais - inclua sua escolha entre DynamoDB Local, LocalStack, ou testcontainers, e por que. Aponte explicitamente o que este milestone NAO resolve dos 5 P1 do red team (idempotencia ponta-a-ponta contra efeito externo real depende de Notification/M4, que nao existe ainda) para evitar falsa sensacao de "G8 fechado = tudo resolvido". Termine com uma nota de confianca 0-10 na sua propria proposta e os riscos que mais te preocupam.

Responda em portugues, formato markdown, direto ao ponto (nao precisa de preambulo).

codex
Vou inspecionar apenas as fontes normativas e o cÃ³digo existente necessÃ¡rios para ancorar a proposta; nÃ£o abrirei `claude-proposal.md`. Como a tarefa Ã© uma proposta independente, nÃ£o farei alteraÃ§Ãµes no repositÃ³rio.
2026-08-19T17:59:02.703433Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -LiteralPath NEXT_SESSION_PROMPT.md; Get-Content -LiteralPath docs/architecture/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 287ms:
# Expiration Tracker Ã¢â‚¬â€ Status e PrÃƒÂ³xima SessÃƒÂ£o

## PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o obrigatÃƒÂ³ria (2026-08-19, mais recente Ã¢â‚¬â€ leia isto primeiro)

Engineering Maturity Review concluÃƒÂ­da (checkpoints 0, 1, 2-9, 12; ver `ENGINEERING.md` na raiz para o relatÃƒÂ³rio completo). Veredito: `ENGINEERING FOUNDATION STATUS: NOT APPROVED`, bloqueador ÃƒÂºnico e conhecido: **G8 (recuperaÃƒÂ§ÃƒÂ£o real de falha assÃƒÂ­ncrona)**.

**DecisÃƒÂ£o do usuÃƒÂ¡rio (2026-08-19)**: tratar o fechamento pleno de G8 como **novo milestone dedicado** (mesma disciplina de M0-M3: pesquisa/design Ã¢â€ â€™ implementaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ teste real Ã¢â€ â€™ revisÃƒÂ£o Claude+Codex), nÃƒÂ£o como remediaÃƒÂ§ÃƒÂ£o de sessÃƒÂ£o. Escopo desse milestone, per `ENGINEERING.md` (seÃƒÂ§ÃƒÂ£o "DecisÃƒÂ£o pendente: escopo de G8"):

- Adapters DynamoDB reais implementando os ports (`ReminderStore`, `ExpirationStore`, `IdentityStore`) contra AWS real Ã¢â‚¬â€ hoje sÃƒÂ³ existem fakes em memÃƒÂ³ria para teste.
- Handlers Lambda reais substituindo os placeholders `exports.handler = async () => ({statusCode: 501})` em `infra/lib/expiration-tracker-stack.ts`.
- Filas SQS + DLQ com redrive policy para o pipeline producerÃ¢â€ â€™dispatch.
- EventBridge Scheduled Rule disparando o `ReminderProducer` periodicamente (a cada minuto) e a `ReminderReconciliation` periodicamente.
- Testes de fault injection contra esse runtime real (timeout de dependÃƒÂªncia, poison message, redrive).

Antes de comeÃƒÂ§ar esse milestone: ler `docs/engineering/reviews/checkpoint-12-redteam/summary.md` (red team formal jÃƒÂ¡ identificou 5 P1 relacionados: pipeline sem recuperaÃƒÂ§ÃƒÂ£o, idempotÃƒÂªncia nÃƒÂ£o provada no limite do efeito externo, false-green CI, concorrÃƒÂªncia/estado obsoleto entre renew/cancel/materializaÃƒÂ§ÃƒÂ£o/dispatch/reconciliation, blast radius cross-tenant do GSI3 via workers privilegiados) Ã¢â‚¬â€ usar como input de design, nÃƒÂ£o redescobrir do zero.

Trabalho jÃƒÂ¡ feito nesta sessÃƒÂ£o de engenharia (nÃƒÂ£o repetir): CI real corrigido e confirmado verde em 5 execuÃƒÂ§ÃƒÂµes; branch `develop` estabelecida como padrÃƒÂ£o de trabalho (`AGENTS.md` Ã‚Â§3), `main` protegida; `dependency-cruiser` como enforcement real de boundary (achou e corrigiu 2 violaÃƒÂ§ÃƒÂµes genuÃƒÂ­nas de arquitetura); testes de `reconciliation.ts`/`producer.ts` que nÃƒÂ£o existiam; tentativa de fechar a vulnerabilidade de devDependency (EX-001) via upgrade do Vitest foi revertida por quebrar CI real (bug upstream do npm, nÃƒÂ£o repetir sem verificar correÃƒÂ§ÃƒÂ£o).

---

**CorreÃƒÂ§ÃƒÂ£o de 2026-08-19 (Engineering Maturity Review, Checkpoint 2-9)**: as seÃƒÂ§ÃƒÂµes abaixo (escritas ao longo das sessÃƒÂµes de M0-M3) afirmam "nada foi commitado" e citam contagens de teste por marco que, somadas, nÃƒÂ£o batem com a realidade medida agora. Estado real verificado por execuÃƒÂ§ÃƒÂ£o: M0-M3 **jÃƒÂ¡ estÃƒÂ¡ commitado** num ÃƒÂºnico commit (`154d6e0`), e `npm test` roda **123 testes** no total (19 arquivos), nÃƒÂ£o a soma das contagens individuais citadas abaixo. As seÃƒÂ§ÃƒÂµes nÃƒÂ£o foram reescritas (preservadas como histÃƒÂ³rico de sessÃƒÂ£o), mas nÃƒÂ£o devem ser lidas como estado de commit/contagem de teste vigente Ã¢â‚¬â€ confiar em `git log`/`npm test` reais, nÃƒÂ£o nesses nÃƒÂºmeros. Ver `docs/engineering/03-repository-baseline.md` e `docs/engineering/reviews/checkpoint-02-09-consolidated/` para a anÃƒÂ¡lise completa desta divergÃƒÂªncia.

Projeto: micro-SaaS de controle de vencimentos/renovaÃƒÂ§ÃƒÂµes. Pasta: `c:\Users\Usuario\Desktop\projects\expiration-tracker\`. Repo GitHub: `marcelo-jgoncalves/expiration-tracker` (privado).

Mapa completo de documentaÃƒÂ§ÃƒÂ£o, status vigente e regra de precedÃƒÂªncia: `docs/architecture/README.md`. Regras de processo e ferramentas: `AGENTS.md`. Log cronolÃƒÂ³gico de sessÃƒÂµes: `docs/architecture/session-log.md`.

## Status atual

```text
DESIGN MATURITY STATUS: APPROVED (arquitetura conceitual + Implementation Blueprint)
ARCHITECTURE STATUS: NOT APPROVED
```

Todo o processo de design (Fases 0-3 do prompt mestre + os 14 entregÃƒÂ¡veis das seÃƒÂ§ÃƒÂµes 35-52 + threat model seÃƒÂ§ÃƒÂ£o 33 + Implementation Blueprint seÃƒÂ§ÃƒÂ£o 60) estÃƒÂ¡ completo e aprovado Ã¢â‚¬â€ ver `ARCHITECTURE.md` na raiz para o documento consolidado. `ARCHITECTURE STATUS: NOT APPROVED` ÃƒÂ© o estado normativo correto atÃƒÂ© haver implementaÃƒÂ§ÃƒÂ£o real testada sob falha/carga (rubrica B, `requirements.md` Ã‚Â§13.1) Ã¢â‚¬â€ **nÃƒÂ£o ÃƒÂ© reprovaÃƒÂ§ÃƒÂ£o de mÃƒÂ©rito, e nÃƒÂ£o muda com a conclusÃƒÂ£o do blueprint**: o blueprint ÃƒÂ© design detalhado, nÃƒÂ£o evidÃƒÂªncia operacional.

## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ Implementation Blueprint (seÃƒÂ§ÃƒÂ£o 60) Ã¢â‚¬â€ APPROVED

`docs/architecture/implementation-blueprint.md`. Componentes/mÃƒÂ³dulos (Identity, Expiration, Reminder, Notification, Document, Audit + workers assÃƒÂ­ncronos), interfaces concretas, eventos/schemas reais (grounded em `data-model.md`), ordem de deploy, milestones M0-M7, critÃƒÂ©rios de aceite tÃƒÂ©cnicos por componente, e as 7 lacunas do `threat-model.md` incorporadas como requisito desde o inÃƒÂ­cio (nÃƒÂ£o apÃƒÂªndice). Processo: 2 propostas independentes (Claude/Codex) Ã¢â€ â€™ crÃƒÂ­tica cruzada (17 problemas reais encontrados, incluindo um erro tÃƒÂ©cnico presente nas duas propostas Ã¢â‚¬â€ chave do GSI3 do scheduler nÃƒÂ£o era consultÃƒÂ¡vel) Ã¢â€ â€™ convergÃƒÂªncia Ã¢â€ â€™ 5 rodadas de nota cega com correÃƒÂ§ÃƒÂ£o pontual a cada achado (cabeÃƒÂ§alho prematuro, contradiÃƒÂ§ÃƒÂ£o de kill switch, tabela incompleta, decisÃƒÂµes Type 1 nÃƒÂ£o propagadas ÃƒÂ s seÃƒÂ§ÃƒÂµes operativas, mapeamento de estado da Step Functions incompleto). **Nota final: Claude 9.20 / Codex 9.2 (exato)** Ã¢â‚¬â€ ambos Ã¢â€°Â¥9.0 sem arredondar, 8 rodadas totais, nenhum gate violado. DecisÃƒÂ£o Type 1 nova registrada: chave global do GSI3 (`GSI3PK=DUE#yyyyMMddHHmm#NN`, exceÃƒÂ§ÃƒÂ£o documentada ÃƒÂ  regra de toda chave comeÃƒÂ§ar por `TENANT#tenantId`) Ã¢â‚¬â€ a decisÃƒÂ£o estÃƒÂ¡ fechada no blueprint; falta sÃƒÂ³ o registro mecÃƒÂ¢nico em `data-model.md` (nÃƒÂ£o bloqueia implementaÃƒÂ§ÃƒÂ£o).

## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M0 "Guardrails e contratos" (implementation-blueprint.md Ã‚Â§19)

Todas as entregas de M0 implementadas e testadas (53 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): estrutura TypeScript (`src/shared/*`, alinhada a `implementation-blueprint.md` Ã‚Â§2), schemas JSON (`schemas/{events,queues,api}` + `sensitive-fields.json`, validados via Ajv em `src/shared/contracts/schema-validator.ts`), `SecureLogger`+`Redactor` central (`src/shared/observability/`, corpus de teste com valores canÃƒÂ¡rio provando que nada vaza), configuraÃƒÂ§ÃƒÂ£o tipada fail-fast (`src/shared/config/config.ts`), taxonomia de erro normalizada retryable/terminal (`src/shared/errors/app-error.ts`), idempotÃƒÂªncia via `PutItem attribute_not_exists(PK)` (`src/shared/idempotency/idempotency.ts`, chaves por operaÃƒÂ§ÃƒÂ£o conforme `data-model.md` Ã‚Â§4), OCC com `ConditionExpression: version=:expected` (`src/shared/dynamodb/occ.ts`, `data-model.md` Ã‚Â§5), outbox transacional (`src/shared/outbox/outbox.ts`, shape de `implementation-blueprint.md` Ã‚Â§5.3), pipeline supply-chain (`.github/workflows/ci.yml`: npm ci imutÃƒÂ¡vel, actions pinadas por SHA, SBOM CycloneDX, audit, gate de schema). `AGENTS.md` Ã‚Â§6 atualizado com comandos/convenÃƒÂ§ÃƒÂµes. Nada foi commitado (working tree aberto para revisÃƒÂ£o).

Judgment calls (blueprint estava silente): Ajv+ajv-formats para validaÃƒÂ§ÃƒÂ£o de JSON Schema; Vitest como test runner; ESLint `no-console` (com exceÃƒÂ§ÃƒÂ£o para `src/shared/observability/**`) como o mecanismo que faz "chamada direta a `console.*` falhar no lint" (`implementation-blueprint.md` Ã‚Â§14.1); idempotÃƒÂªncia/OCC/outbox construÃƒÂ­dos como builders puros de parÃƒÂ¢metros DynamoDB (sem `@aws-sdk` ainda) para ficarem testÃƒÂ¡veis sem tabelas reais, que sÃƒÂ³ existem a partir de M1; SLSA/assinatura de artefato adiada para M1+ (nÃƒÂ£o hÃƒÂ¡ alvo de deploy ainda); SHAs de actions no CI pinados no momento da escrita Ã¢â‚¬â€ revisar antes do primeiro run real.

## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M1 "Foundation, Identity e isolamento" (implementation-blueprint.md Ã‚Â§19)

Todas as entregas implementadas e testadas (89 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/identity/{domain,application,ports,persistence,http}` (resolver central, matriz de autorizaÃƒÂ§ÃƒÂ£o como cÃƒÂ³digo, `IdentityMapping`/`User`/`DeviceSession`/`TenantQuota` via portas SDK-agnÃƒÂ³sticas) e `infra/{lib,bin}` (`ExpirationTrackerTable`, `ExpirationTrackerAuth`, `ScopedLambdaFunction`, `ExpirationTrackerApi` com a rota `GET /test/ping`). SuÃƒÂ­te cross-tenant negativa em `test/integration/cross-tenant.test.ts` (9 casos, exit criterion do marco) e synth de infra em `test/infra/stack.test.ts` (6 casos, via `aws-cdk-lib/assertions`, sem AWS CLI). Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente ou pendÃƒÂªncia externa jÃƒÂ¡ conhecida): (1) MFA implementado como prop configurÃƒÂ¡vel (`OFF`/`OPTIONAL`/`REQUIRED`, default `OPTIONAL`) pois UNK-006 segue pendente de pesquisa externa Ã¢â‚¬â€ nÃƒÂ£o bloqueava M1; (2) CSP/CloudFront Response Headers Policy **nÃƒÂ£o implementado nesta sessÃƒÂ£o** Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ distribuiÃƒÂ§ÃƒÂ£o CloudFront/frontend ainda no repositÃƒÂ³rio, e o texto do blueprint (Ã‚Â§4.2) coloca CSP no contexto do SPA estÃƒÂ¡tico servido por CloudFront; tratado como pertencente ao milestone que introduzir o frontend, nÃƒÂ£o forÃƒÂ§ado em M1 (revisar se essa leitura estiver errada); (3) sessÃƒÂ£o/revogaÃƒÂ§ÃƒÂ£o modelada como campo `globalLogoutAfter` no item `User` + item filho `DeviceSession` (`TENANT#t#USER#u`/`SESSION#<deviceId>`) em vez de uma entidade `Session` de primeira classe Ã¢â‚¬â€ `data-model.md` nÃƒÂ£o define uma; (4) `ScopedLambdaFunction` mantÃƒÂ©m a lista de entidades por capability como metadado/documentaÃƒÂ§ÃƒÂ£o, mas o IAM real concedido ÃƒÂ© table-level (`grantReadWriteData`) Ã¢â‚¬â€ DynamoDB IAM nÃƒÂ£o expressa restriÃƒÂ§ÃƒÂ£o por SK/entidade da forma que a sintaxe do blueprint sugere visualmente; per-entity IAM mais fino fica como follow-up; (5) `TenantQuota` implementado como janela fixa (fixed-window), nÃƒÂ£o sliding-window/leaky-bucket Ã¢â‚¬â€ mais simples e ainda satisfaz "decremento atÃƒÂ´mico, sem race condition" do `data-model.md`; (6) BFF de sessÃƒÂ£o (`/session/refresh`, `/session/logout`) **nÃƒÂ£o implementado como rota HTTP nesta sessÃƒÂ£o** Ã¢â‚¬â€ o Cognito client jÃƒÂ¡ estÃƒÂ¡ configurado para o padrÃƒÂ£o (client secret, tokens de vida curta), mas os endpoints do BFF em si ficam para quando a Expiration/Notification API skeleton crescer alÃƒÂ©m da rota de teste; revisar se isso deveria ter sido parte do exit criterion. `cdk synth` real via CLI nÃƒÂ£o foi executado (o pacote `aws-cdk` CLI nÃƒÂ£o estÃƒÂ¡ instalado, sÃƒÂ³ a lib `aws-cdk-lib`); a sintetizaÃƒÂ§ÃƒÂ£o foi verificada programaticamente via `Template.fromStack` nos testes, que ÃƒÂ© equivalente para fins de validaÃƒÂ§ÃƒÂ£o estÃƒÂ¡tica.

## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M2 "Expiration core e Audit" (implementation-blueprint.md Ã‚Â§19)

Todas as entregas implementadas e testadas (134 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/expiration/{domain,application,ports,http}` Ã¢â‚¬â€ CRUD/renew (`ExpirationService`: createItem/getItem/updateItem/archiveItem/deleteItem/renewItem/listDashboard), OCC via `shared/dynamodb/occ.ts`, `ItemDueDateChanged` por outbox (`shared/outbox/outbox.ts`) na MESMA `TransactWriteItems` do item quando `dueDate` muda (updateItem e renewItem), `AuditEvent` append-only gravado em toda mutaÃƒÂ§ÃƒÂ£o na mesma transaÃƒÂ§ÃƒÂ£o, dashboard via GSI1. `renewItem` cria um item sucessor (`renewedFromId`) em vez de mutar `dueDate` no agregado de origem, que transiciona para `RENEWED`; ÃƒÂ© idempotente via `IdempotencyStore` de M0. Rotas HTTP CDK adicionadas em `infra/lib/api.ts`/`expiration-tracker-stack.ts` (`ItemsHandler`, mesmo authorizer JWT de M1). SuÃƒÂ­te de integraÃƒÂ§ÃƒÂ£o `test/integration/expiration-lifecycle.test.ts` prova o exit criterion do marco end-to-end via handlers HTTP reais. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) evento `ItemDueDateChanged` segue exatamente o schema jÃƒÂ¡ existente desde M0 (`schemas/events/item-due-date-changed.v1.json`: `itemId`/`previousDueDate`/`newDueDate`/`itemVersion`, `additionalProperties: false`) em vez do exemplo mais rico do texto do blueprint Ã‚Â§8.3 (que inclui `timeZone`/`reminderPolicyId`/`changeReason`) Ã¢â‚¬â€ o schema jÃƒÂ¡ testado/versionado ÃƒÂ© a fonte de verdade de contrato per `AGENTS.md` Ã‚Â§6, o exemplo em prosa nÃƒÂ£o; campos adicionais ficam para quando M3 (Reminder) precisar deles, exigindo `.v2` aditivo; (2) `renewItem` tambÃƒÂ©m dispara `ItemDueDateChanged` (para o item novo, `previousDueDate: null`) Ã¢â‚¬â€ o blueprint sÃƒÂ³ descreve esse evento no contexto de `updateItem`, mas renovaÃƒÂ§ÃƒÂ£o tambÃƒÂ©m ÃƒÂ© uma "mudanÃƒÂ§a de vencimento" do ponto de vista do futuro `ReminderProducer` (M3), e a alternativa (renovaÃƒÂ§ÃƒÂ£o silenciosa) deixaria o dashboard/scheduler sem sinal de que um novo item precisa de agendamento; (3) `archiveItem` usa a aÃƒÂ§ÃƒÂ£o de autorizaÃƒÂ§ÃƒÂ£o `item:update` (nÃƒÂ£o uma aÃƒÂ§ÃƒÂ£o dedicada Ã¢â‚¬â€ a matriz de M1 nÃƒÂ£o define uma para archive) enquanto `deleteItem` usa `item:delete` (ADMIN_ROLES), seguindo a granularidade que jÃƒÂ¡ existe; (4) idempotÃƒÂªncia de renovaÃƒÂ§ÃƒÂ£o aceita uma `idempotencyKey` explÃƒÂ­cita do chamador (header `Idempotency-Key`) com fallback determinÃƒÂ­stico `itemId|expectedVersion|cycle` quando ausente, jÃƒÂ¡ que o blueprint define a CHAVE de dedupe mas nÃƒÂ£o de onde ela vem por HTTP; (5) `ExpirationStore.transactWrite` foi adicionado ao port (nÃƒÂ£o existe em `IdentityStore`) porque nenhuma escrita de M1 precisava de `TransactWriteItems` multi-item Ã¢â‚¬â€ a fake em memÃƒÂ³ria (`test/unit/expiration/in-memory-store.ts`) sÃƒÂ³ entende as duas formas exatas de `ConditionExpression` que este cÃƒÂ³digo produz (mesma limitaÃƒÂ§ÃƒÂ£o documentada, mesmo espÃƒÂ­rito de `InMemoryIdentityStore`).

## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M3 "Reminder Engine" (implementation-blueprint.md Ã‚Â§19)

Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversÃƒÂ£o localÃ¢â€ â€™UTC IANA-aware Ã¢â‚¬â€ DST tratado explicitamente, horÃƒÂ¡rio inexistente e ambÃƒÂ­guo Ã¢â‚¬â€ reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lÃƒÂ³gica pura testÃƒÂ¡vel com relÃƒÂ³gio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMEDÃ¢â€ â€™TRIGGERED`+`NotificationIntent`+outbox numa ÃƒÂºnica transaÃƒÂ§ÃƒÂ£o; reconciliaÃƒÂ§ÃƒÂ£o ÃƒÂ© um ÃƒÂºnico job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluÃƒÂ­am `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER funÃƒÂ§ÃƒÂ£o da tabela (inclusive as tenant-facing de M1/M2) Ã¢â‚¬â€ a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explÃƒÂ­citos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que sÃƒÂ³ `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) `occurrenceId` derivado deterministicamente (hash da chave de idempotÃƒÂªncia de data-model.md Ã‚Â§4) em vez de UUID + registro `IdempotencyStore` separado Ã¢â‚¬â€ o prÃƒÂ³prio `putIfAbsent` condicional jÃƒÂ¡ garante a idempotÃƒÂªncia de materializaÃƒÂ§ÃƒÂ£o, mesmo padrÃƒÂ£o que data-model.md documenta para `WebhookInbox`; (2) cancelamento de ocorrÃƒÂªncia stale nÃƒÂ£o remove `GSI3PK`/`GSI3SK` (o builder de `occ.ts` ÃƒÂ© SET-only, sem REMOVE) Ã¢â‚¬â€ deixa um ponteiro ÃƒÂ³rfÃƒÂ£o no ÃƒÂ­ndice, mas o `Query` condicional `SCHEDULEDÃ¢â€ â€™CLAIMED` do producer falha sobre ele de forma inofensiva; (3) tolerÃƒÂ¢ncia de dispatch (`toleranceMs`, default 30min) e TTL de claim (default 2min) nÃƒÂ£o estÃƒÂ£o fixados no blueprint Ã¢â‚¬â€ valores razoÃƒÂ¡veis documentados no cÃƒÂ³digo, revisar contra dados reais de produÃƒÂ§ÃƒÂ£o; (4) reconciliaÃƒÂ§ÃƒÂ£o DST/claim-expiry recebe os candidatos como parÃƒÂ¢metro (batch) em vez de fazer sua prÃƒÂ³pria varredura via GSI6 Ã¢â‚¬â€ a wiring desse ÃƒÂ­ndice de "polÃƒÂ­ticas ativas"/"claims expirados" real fica como follow-up de infra, o mecanismo de reconciliaÃƒÂ§ÃƒÂ£o em si (o que a tarefa pediu) estÃƒÂ¡ implementado e testado; (5) Lambda handlers reais (bundling com `@aws-sdk`) nÃƒÂ£o foram escritos Ã¢â‚¬â€ mesmo estÃƒÂ¡gio que M0-M2, CDK usa cÃƒÂ³digo inline placeholder via `ScopedLambdaFunction`; a lÃƒÂ³gica testÃƒÂ¡vel ÃƒÂ© o entregÃƒÂ¡vel real desta fase, igual ÃƒÂ s anteriores.

## PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o obrigatÃƒÂ³ria (histÃƒÂ³rico Ã¢â‚¬â€ superada pela seÃƒÂ§ÃƒÂ£o do topo)

**Superada em 2026-08-19**: a Engineering Maturity Review identificou G8 (recuperaÃƒÂ§ÃƒÂ£o real de falha assÃƒÂ­ncrona) como bloqueador de engenharia Ã¢â‚¬â€ o milestone de runtime real (adapters/handlers/filas/EventBridge, ver seÃƒÂ§ÃƒÂ£o do topo) ÃƒÂ© prÃƒÂ©-requisito antes de M4 fazer sentido operacionalmente, ainda que M4 nÃƒÂ£o dependa dele estruturalmente. Lista original preservada abaixo como histÃƒÂ³rico, nÃƒÂ£o como prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o vigente.

1. **M4 Ã¢â‚¬â€ Notification Engine** (`implementation-blueprint.md` Ã‚Â§19, depende de M3 Ã¢Å“â€¦): router de `NotificationIntent`Ã¢â€ â€™canal, delivery workers (email/WhatsApp stub-first), `NotificationAttempt`, resoluÃƒÂ§ÃƒÂ£o de destinatÃƒÂ¡rio/template.
2. Decidir e fechar os itens abaixo (BFF de sessÃƒÂ£o como rotas reais, CSP/CloudFront quando o frontend existir) antes de considerar a lacuna de "session theft" do threat model totalmente fechada Ã¢â‚¬â€ ainda nÃƒÂ£o resolvido, re-flagueado a cada sessÃƒÂ£o desde M1 para nÃƒÂ£o se perder: M1 fechou o mecanismo de revogaÃƒÂ§ÃƒÂ£o/matriz de autorizaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o o endpoint BFF completo; M2/M3 nÃƒÂ£o tinham escopo de frontend/sessÃƒÂ£o (confirmado no blueprint) entÃƒÂ£o tambÃƒÂ©m nÃƒÂ£o o resolveram.
3. **Ratificar formalmente em `data-model.md` a exceÃƒÂ§ÃƒÂ£o do GSI3** (chave global do scheduler, jÃƒÂ¡ decidida e justificada em `implementation-blueprint.md` Ã‚Â§9.2/Ã‚Â§23.3 item 10) Ã¢â‚¬â€ passo de manutenÃƒÂ§ÃƒÂ£o documental, nÃƒÂ£o nova rodada de debate. M3 jÃƒÂ¡ corrigiu o enforcement real do isolamento em IAM; falta sÃƒÂ³ o registro mecÃƒÂ¢nico no documento.
4. Wiring real do GSI6 (ou ÃƒÂ­ndice equivalente) para alimentar a reconciliaÃƒÂ§ÃƒÂ£o de M3 com o batch real de "polÃƒÂ­ticas ativas"/"claims a verificar" em produÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ hoje o mecanismo de reconciliaÃƒÂ§ÃƒÂ£o recebe esse batch como parÃƒÂ¢metro injetado (testÃƒÂ¡vel), a query real fica pendente.
5. Testes de carga real (cenÃƒÂ¡rios de drenagem do pico extremo, `capacity-model.md`, com SLO de drenagem UNK-CAP-006 a formalizar em `slo.md` antes do gate de M7), teste de restore real (gate jÃƒÂ¡ definido em `disaster-recovery.md` Ã‚Â§6), exercÃƒÂ­cio do runbook de credencial comprometida.
6. ReavaliaÃƒÂ§ÃƒÂ£o sob rubrica (B) Ã¢â‚¬â€ Operational Evidence Ã¢â‚¬â€ sÃƒÂ³ entÃƒÂ£o `ARCHITECTURE STATUS` pode legitimamente virar `APPROVED`. **NÃƒÂ£o declarar isso antes de haver evidÃƒÂªncia operacional real.** Implementar M0/M1/M2/M3 nÃƒÂ£o ÃƒÂ© evidÃƒÂªncia operacional.
7. DecisÃƒÂµes ainda pendentes de pesquisa externa (nÃƒÂ£o bloqueiam M4 estruturalmente, mas bloqueiam habilitar os canais/features especÃƒÂ­ficos, ver `implementation-blueprint.md` Ã‚Â§23.3): provider inicial de e-mail, BSP WhatsApp (pricing real, UNK-003), modelo Bedrock especÃƒÂ­fico, regiÃƒÂ£o AWS (bloqueante para LGPD/transferÃƒÂªncia internacional), MFA obrigatÃƒÂ³rio vs. opcional (UNK-006), ferramenta de backup S3 (RPOÃ¢â€°Â¤24h), ferramenta de assinatura/provenance do pipeline de CI.
# docs/architecture/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃƒÂ§ÃƒÂ£o real (cÃƒÂ³digo/infra/testes) Ã¢â‚¬â€ Implementation Blueprint concluÃƒÂ­do
Last verified:           2026-08-19
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

1. `AGENTS.md` (raiz) Ã¢â‚¬â€ processo de trabalho dos agentes, sempre vence sobre conteÃƒÂºdo de arquitetura.
2. ADR aceito em `adr/` Ã¢â‚¬â€ decisÃƒÂ£o arquitetural especÃƒÂ­fica e formal.
3. Documento temÃƒÂ¡tico corrente (tabela abaixo, coluna "normativo atual") Ã¢â‚¬â€ especificaÃƒÂ§ÃƒÂ£o detalhada do domÃƒÂ­nio.
4. `ARCHITECTURE.md` Ã¢â‚¬â€ visÃƒÂ£o consolidada e ÃƒÂ­ndice executivo; nÃƒÂ£o sobrescreve silenciosamente um ADR ou documento temÃƒÂ¡tico divergente Ã¢â‚¬â€ divergÃƒÂªncia entre eles ÃƒÂ© defeito a corrigir, nÃƒÂ£o licenÃƒÂ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` Ã¢â‚¬â€ estado de execuÃƒÂ§ÃƒÂ£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` Ã¢â‚¬â€ evidÃƒÂªncia histÃƒÂ³rica de como se chegou a uma decisÃƒÂ£o, nunca normativo.

## ÃƒÂndice por documento

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/ÃƒÂ­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃƒÂ³rico/processo (ciclo concluÃƒÂ­do) | Processo ClaudeÃ¢â€ â€Codex que produziu o design; nÃƒÂ£o ÃƒÂ© ponto de entrada de sessÃƒÂ£o |
| `quality-criteria.md` | normativo atual | 12 critÃƒÂ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃƒÂ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃƒÂ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃƒÂµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃƒÂ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃƒÂ§ÃƒÂ£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃƒÂ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃƒÂ£o de domÃƒÂ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃƒÂ§ÃƒÂµes de estÃƒÂ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃƒÂ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃƒÂ§ÃƒÂ£o 33 Ã¢â‚¬â€ APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃƒÂ§ÃƒÂ£o 60 Ã¢â‚¬â€ componentes, interfaces, eventos/schemas, ordem de deploy, milestones Ã¢â‚¬â€ APPROVED (Claude 9.20 / Codex 9.2) |
| `decisions-log.md` | decisÃƒÂ£o/ADR (log vivo) | 26 decisÃƒÂµes (D-000 a D-025), nota Claude/Codex, status |
| `adr/` | decisÃƒÂ£o/ADR | 8 ADRs formais para decisÃƒÂµes Type 1 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/status-projeto.html` | resumo/ÃƒÂ­ndice (visual) | Painel visual do status do projeto |
| `session-log.md` | histÃƒÂ³rico | Log cronolÃƒÂ³gico compacto por sessÃƒÂ£o |
| `history/` | histÃƒÂ³rico/supersedido | Artefatos de rodada (propostas, crÃƒÂ­ticas, red team) que produziram os documentos normativos acima Ã¢â‚¬â€ ver subseÃƒÂ§ÃƒÂ£o |

## `history/` Ã¢â‚¬â€ evidÃƒÂªncia de rodada, por tema

Cada subpasta contÃƒÂ©m os artefatos de proposta/crÃƒÂ­tica/trÃƒÂ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃƒÂ£o vigente Ã¢â‚¬â€ sÃƒÂ³ como prova de como o consenso foi alcanÃƒÂ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^export interface|''^export type|''^export class|ReminderProducerStore|501|ScopedLambdaFunction|reservedConcurrent|GSI3|ReminderDispatch|ReminderReconciliation" src/modules src/workers infra/lib docs/architecture/implementation-blueprint.md package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 256ms:
docs/architecture/implementation-blueprint.md:11:1. **Scheduler GSI3 corrigido** â€” erro tÃ©cnico real presente nas duas propostas independentes: uma partition key `TENANT#t#DUE#yyyyMMddHHmm#NN` nÃ£o Ã© consultÃ¡vel pelo producer do minuto sem jÃ¡ saber quais tenants tÃªm ocorrÃªncia naquele minuto (`Query` exige PK exata). Corrigido em todo o documento para `GSI3PK=DUE#yyyyMMddHHmm#NN` / `GSI3SK=TENANT#tenantId#OCCURRENCE#occurrenceId` â€” exceÃ§Ã£o documentada e justificada Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`" de `data-model.md` Â§3, com as salvaguardas da Â§9.2 abaixo. Esta Ã© uma mudanÃ§a Type 1 sobre `data-model.md` e deve ser ratificada lÃ¡ (ver `decisions-log.md`).
docs/architecture/implementation-blueprint.md:162:| `ReminderProducer` | EventBridge Scheduler, 1 minuto | Consultar GSI3 nos shards do minuto e enviar comandos para SQS |
docs/architecture/implementation-blueprint.md:163:| `ReminderDispatchWorker` | `ReminderDispatchQueue` | Revalidar ocorrÃªncia/item e produzir `NotificationIntent` |
docs/architecture/implementation-blueprint.md:164:| `ReminderReconciliationWorker` | EventBridge diÃ¡rio | Recalcular janela de sete dias e reparar ocorrÃªncias |
docs/architecture/implementation-blueprint.md:181:**Decidido (Â§23.1): Textract, parser determinÃ­stico e Bedrock sÃ£o Lambdas separadas desde o primeiro release**, nÃ£o um `ExtractionTaskHandler` Ãºnico consolidado depois. Justificativa: o sandbox de PDF (lacuna #2 do threat model, limites em Â§12.4/Â§23.1) exige isolamento de IAM/rede que uma funÃ§Ã£o combinada dilui â€” separar desde o dia 1 Ã© mais simples de auditar do que consolidar e depois desmembrar sob pressÃ£o de produÃ§Ã£o. Cada uma das trÃªs superfÃ­cies de risco isoladas (OCR gerenciado, parsing nÃ£o confiÃ¡vel em sandbox, chamada a LLM) tem sua prÃ³pria funÃ§Ã£o nomeada com `ScopedLambdaFunction` (Â§17.1) â€” nÃ£o Ã© "um estado, uma funÃ§Ã£o": estados de orquestraÃ§Ã£o pura (`NeedsBedrock?`) sÃ£o Choice states nativos sem Lambda, e estados que compartilham a mesma superfÃ­cie de IAM/dados (validaÃ§Ã£o de schema, comparaÃ§Ã£o entre extratores, persistÃªncia, fail-closed) compartilham `ExtractionValidationTaskHandler`. O mapeamento estadoâ†’handler completo estÃ¡ em Â§12.5.
docs/architecture/implementation-blueprint.md:201:export interface RequestContext {
docs/architecture/implementation-blueprint.md:623:  "GSI3PK": "DUE#202609101200#02",
docs/architecture/implementation-blueprint.md:624:  "GSI3SK": "TENANT#t_01#OCCURRENCE#occ_01"
docs/architecture/implementation-blueprint.md:628:**CorreÃ§Ã£o de convergÃªncia (Rodada 3)**: a chave original avaliada em ambas as propostas independentes (`GSI3PK=TENANT#t#DUE#...`) nÃ£o Ã© consultÃ¡vel pelo producer do minuto â€” `Query` no DynamoDB exige a partition key exata, e o producer nÃ£o sabe a priori quais tenants tÃªm ocorrÃªncia em `M`. Corrigido para uma **chave global** (`GSI3PK=DUE#yyyyMMddHHmm#NN`, sem `tenantId`), com `tenantId` preservado no `GSI3SK` e no item base para reconstruÃ§Ã£o segura do contexto. Esta Ã© uma **exceÃ§Ã£o explÃ­cita e documentada** Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`" de `data-model.md` Â§3 â€” Type 1, deve ser ratificada em `data-model.md`/`decisions-log.md`, nÃ£o apenas aqui. Salvaguardas obrigatÃ³rias:
docs/architecture/implementation-blueprint.md:629:- IAM do Ã­ndice restrito exclusivamente ao `ReminderProducer` â€” nenhuma rota tenant-facing tem permissÃ£o de `Query` no GSI3;
docs/architecture/implementation-blueprint.md:647:2. consulta GSI3 pelos `N` shards globais de `M` **e de uma janela de lookback** (ex. `[M-5min, M]`) para cobrir minutos cuja publicaÃ§Ã£o falhou â€” sem lookback, "o prÃ³ximo tick reprocessa" Ã© falso, pois o prÃ³ximo tick consulta uma chave diferente (`M+1`), deixando `M` definitivamente para trÃ¡s em caso de falha;
docs/architecture/implementation-blueprint.md:1084:| # | Estado | Lambda (`ScopedLambdaFunction` prÃ³prio) |
docs/architecture/implementation-blueprint.md:1379:### 17.1 `ScopedLambdaFunction`
docs/architecture/implementation-blueprint.md:1384:new ScopedLambdaFunction(this, "ReminderDispatch", {
docs/architecture/implementation-blueprint.md:1529:- `ScopedLambdaFunction`;
docs/architecture/implementation-blueprint.md:1557:- GSI3;
docs/architecture/implementation-blueprint.md:1783:2. **Lookup `cognitoSub â†’ userId`**: **decidido â€” item de mapeamento dedicado** `PK=IDENTITY#cognitoSub#<sub>` / `SK=MAP`, criado atomicamente (`ConditionExpression attribute_not_exists`) no primeiro login, apontando para `userId`/`tenantId` internos. Fora do agregado `TENANT#...` porque a busca ocorre *antes* de `tenantId` ser conhecido â€” Ãºnica exceÃ§Ã£o adicional de particionamento alÃ©m do GSI3 (Â§9.2), pelo mesmo motivo estrutural (chave nÃ£o pode depender de um dado ainda nÃ£o resolvido).
docs/architecture/implementation-blueprint.md:1797:10. **RatificaÃ§Ã£o formal em `data-model.md`/`decisions-log.md` da chave global do GSI3**: a *decisÃ£o* jÃ¡ estÃ¡ fechada aqui (Â§9.2, com justificativa completa e salvaguardas); o que resta Ã© o registro mecÃ¢nico no documento normativo â€” nÃ£o uma lacuna de design, um passo de manutenÃ§Ã£o documental (AGENTS.md Â§5) a executar no fechamento desta sessÃ£o.
docs/architecture/implementation-blueprint.md:1824:| GSI3 global (scheduler) + shards versionados | NFR-002, NFR-011 | Pico extremo 16.667â€“278 agendamentos/s (`capacity-model.md`), SLO de drenagem UNK-CAP-006 (`slo.md`) |
docs/architecture/implementation-blueprint.md:1843:| `ReminderDispatchWorker` | `ReminderDispatchQueue` (comando do producer) | Reminder/Notification | 10 | DLQ, `maxReceiveCount=5` |
docs/architecture/implementation-blueprint.md:1844:| `ReminderReconciliationWorker` | EventBridge diÃ¡rio | Reminder | 1 | sem DLQ (idempotente, roda diariamente) |
infra/lib\scoped-lambda-function.ts:2: * ScopedLambdaFunction â€” implementation-blueprint.md Â§17.1. "O construct gera grants
infra/lib\scoped-lambda-function.ts:23:export interface AccessCapability {
infra/lib\scoped-lambda-function.ts:52:    /** GSI3 (scheduler) read-only - reserved EXCLUSIVELY for ReminderProducer
infra/lib\scoped-lambda-function.ts:59:        description: "GSI3 (scheduler) read-only - ReminderProducer only",
infra/lib\scoped-lambda-function.ts:113:export interface ScopedLambdaFunctionProps extends Omit<lambda.FunctionProps, "code"> {
infra/lib\scoped-lambda-function.ts:120: * Every Lambda in this system MUST be constructed via ScopedLambdaFunction, never
infra/lib\scoped-lambda-function.ts:124:export class ScopedLambdaFunction extends Construct {
infra/lib\scoped-lambda-function.ts:127:  constructor(scope: Construct, id: string, props: ScopedLambdaFunctionProps) {
infra/lib\scoped-lambda-function.ts:134:          `ScopedLambdaFunction(${id}): unknown access capability kind "${capability.kind}". ` +
infra/lib\scoped-lambda-function.ts:147:      code: code ?? lambda.Code.fromInline("exports.handler = async () => ({ statusCode: 501 });"),
infra/lib\expiration-tracker-stack.ts:2: * M1 stack: wires DynamoDB, Cognito, the identity test route (ScopedLambdaFunction) and
infra/lib\expiration-tracker-stack.ts:11:import { ScopedLambdaFunction, tableAccessFor } from "./scoped-lambda-function.js";
infra/lib\expiration-tracker-stack.ts:14:export interface ExpirationTrackerStackProps extends StackProps {
infra/lib\expiration-tracker-stack.ts:18:export class ExpirationTrackerStack extends Stack {
infra/lib\expiration-tracker-stack.ts:22:  readonly remindersHandler: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:23:  readonly reminderProducer: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:24:  readonly reminderDispatch: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:25:  readonly reminderReconciliation: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:35:    const testRouteHandler = new ScopedLambdaFunction(this, "TestPingHandler", {
infra/lib\expiration-tracker-stack.ts:43:    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
infra/lib\expiration-tracker-stack.ts:46:    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
infra/lib\expiration-tracker-stack.ts:58:    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
infra/lib\expiration-tracker-stack.ts:59:    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
infra/lib\expiration-tracker-stack.ts:69:    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
infra/lib\expiration-tracker-stack.ts:75:    // ReminderDispatch (Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
infra/lib\expiration-tracker-stack.ts:77:    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
infra/lib\expiration-tracker-stack.ts:78:    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
infra/lib\expiration-tracker-stack.ts:92:    // ReminderReconciliation (Â§9.5): single job for claim-expiry revert + DST
infra/lib\expiration-tracker-stack.ts:93:    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
infra/lib\expiration-tracker-stack.ts:95:    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
infra/lib\cognito.ts:12:export type MfaPolicy = "OFF" | "OPTIONAL" | "REQUIRED";
infra/lib\cognito.ts:14:export interface ExpirationTrackerAuthProps {
infra/lib\cognito.ts:26:export class ExpirationTrackerAuth extends Construct {
infra/lib\dynamo-table.ts:6: * GSI3 is the documented exception: its PK (GSI3PK) is deliberately NOT prefixed with
infra/lib\dynamo-table.ts:9: * `grantGsi3ReadTo` method instead of a general grantReadData-on-GSI3 path, so callers
infra/lib\dynamo-table.ts:18:export interface ExpirationTrackerTableProps {
infra/lib\dynamo-table.ts:23:export class ExpirationTrackerTable extends Construct {
infra/lib\dynamo-table.ts:53:    // GSI3 - scheduler. Global PK (no tenantId), IAM-restricted to ReminderProducer only
infra/lib\dynamo-table.ts:56:      indexName: "GSI3",
infra/lib\dynamo-table.ts:57:      partitionKey: { name: "GSI3PK", type: dynamodb.AttributeType.STRING },
infra/lib\dynamo-table.ts:58:      sortKey: { name: "GSI3SK", type: dynamodb.AttributeType.STRING },
infra/lib\dynamo-table.ts:85:   * Resource ARNs for every GSI EXCEPT GSI3 (data-model.md Â§3's isolation safeguard).
infra/lib\dynamo-table.ts:90:   * for tenant-facing functions would silently grant `dynamodb:Query` on GSI3 to every
infra/lib\dynamo-table.ts:93:   * PolicyStatements with an explicit resource list (base table + the 5 non-GSI3 indexes)
infra/lib\dynamo-table.ts:105:    return `${this.table.tableArn}/index/GSI3`;
infra/lib\dynamo-table.ts:108:  /** General read/write grant on the base table + GSI1/GSI2/GSI4/GSI5/GSI6 (tenant-scoped indexes). Never includes GSI3 - see tenantFacingResources() above. */
infra/lib\dynamo-table.ts:144:  /** The ONLY sanctioned way to read GSI3 - resource is the GSI3 index ARN exclusively (never the table or any other index), intended solely for the M3 ReminderProducer. test/infra/stack.test.ts's isolation test asserts no other function's synthesized IAM policy references this index. */
infra/lib\api.ts:3: * (GET /test/ping) wired through a Cognito JWT authorizer + the ScopedLambdaFunction
infra/lib\api.ts:11:import type { ScopedLambdaFunction } from "./scoped-lambda-function.js";
infra/lib\api.ts:14:export interface ExpirationTrackerApiProps {
infra/lib\api.ts:16:  testRouteHandler: ScopedLambdaFunction;
infra/lib\api.ts:22:  itemsHandler?: ScopedLambdaFunction;
infra/lib\api.ts:25:export class ExpirationTrackerApi extends Construct {
src/workers\reminder-dispatch\dispatch.ts:32:export interface DispatchDeps {
src/workers\reminder-dispatch\dispatch.ts:43:export type DispatchOutcome =
src/workers\reminder-reconciliation\reconciliation.ts:28:export interface ReconciliationDeps {
src/workers\reminder-reconciliation\reconciliation.ts:39:export interface DstReconciliationCandidate {
src/workers\reminder-reconciliation\reconciliation.ts:47:export interface ReconciliationResult {
src/workers\reminder-producer\producer.ts:3: * injected `ReminderProducerStore` and clock, no AWS SDK/Lambda runtime dependency, so it
src/workers\reminder-producer\producer.ts:4: * is directly unit/integration-testable with a fake clock and fake GSI3 index (per the
src/workers\reminder-producer\producer.ts:9: *  2. query GSI3 across N shards of minute M AND a lookback window [M-lookback, M] (default
src/workers\reminder-producer\producer.ts:23:import type { ReminderProducerStore } from "../../modules/reminder/ports/reminder-store.js";
src/workers\reminder-producer\producer.ts:26:export interface DispatchCommand {
src/workers\reminder-producer\producer.ts:40:export interface ProducerDeps {
src/workers\reminder-producer\producer.ts:41:  store: ReminderProducerStore;
src/workers\reminder-producer\producer.ts:51:export interface ProducerTickResult {
src/workers\reminder-producer\producer.ts:89:        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
src/workers\reminder-producer\producer.ts:92:          const { tenantId, occurrenceId } = parseGsi3Sk(row.GSI3SK);
src/modules\expiration\ports\expiration-store.ts:15:export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
src/modules\expiration\ports\expiration-store.ts:18:export interface Gsi1QueryInput {
src/modules\expiration\ports\expiration-store.ts:24:export interface ExpirationStore {
src/modules\reminder\application\reminder-materializer.ts:3: * interface + Â§9.2 (occurrence shape/GSI3 keys) + Â§8.3 (reacting to ItemDueDateChanged:
src/modules\reminder\application\reminder-materializer.ts:32:export interface MaterializeInput {
src/modules\reminder\application\reminder-materializer.ts:41:export interface MaterializeResult {
src/modules\reminder\application\reminder-materializer.ts:83:export class ReminderMaterializer {
src/modules\reminder\application\reminder-materializer.ts:179:        GSI3PK: gsi3.GSI3PK,
src/modules\reminder\application\reminder-materializer.ts:180:        GSI3SK: gsi3.GSI3SK,
src/modules\reminder\application\reminder-materializer.ts:216:              // GSI3PK/GSI3SK are intentionally left in place (occ.ts's builder is
src/modules\reminder\ports\reminder-store.ts:11: * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
src/modules\reminder\ports\reminder-store.ts:13: * tem permissÃ£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
src/modules\reminder\ports\reminder-store.ts:15: * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
src/modules\reminder\ports\reminder-store.ts:23:export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\ports\reminder-store.ts:27:export interface ReminderStore {
src/modules\reminder\ports\reminder-store.ts:36:export interface Gsi3QueryInput {
src/modules\reminder\ports\reminder-store.ts:41:export interface ReminderProducerStore {
src/modules\reminder\application\id-generator.ts:2:export interface ReminderIdGenerator {
src/modules\expiration\http\item-handlers.ts:14:export interface HttpRequest<TBody = unknown> {
src/modules\expiration\http\item-handlers.ts:24:export interface HttpResponse {
src/modules\expiration\http\item-handlers.ts:29:export interface ExpirationHttpDeps {
src/modules\expiration\application\id-generator.ts:2:export interface ExpirationIdGenerator {
src/modules\reminder\http\policy-handlers.ts:8:export interface HttpRequest<TBody = unknown> {
src/modules\reminder\http\policy-handlers.ts:17:export interface HttpResponse {
src/modules\reminder\http\policy-handlers.ts:22:export interface ReminderHttpDeps {
src/modules\expiration\application\expiration-service.ts:42:export interface ExpirationServiceDeps {
src/modules\expiration\application\expiration-service.ts:49:export interface DashboardQuery {
src/modules\expiration\application\expiration-service.ts:55:export class ExpirationService {
src/modules\reminder\domain\recurrence.ts:19:export type ZonedResolutionKind = "NORMAL" | "AMBIGUOUS" | "NONEXISTENT";
src/modules\reminder\domain\recurrence.ts:21:export interface ZonedResolution {
src/modules\reminder\domain\recurrence.ts:26:export interface LocalDateTimeParts {
src/modules\identity\application\quota.ts:10:export type QuotaType = "API_REQUEST" | "UPLOAD_BYTES" | "UPLOAD_COUNT" | "AI_CALL";
src/modules\identity\application\quota.ts:12:export interface TenantQuotaRecord {
src/modules\identity\application\quota.ts:29:export interface QuotaCheckInput {
src/modules\identity\application\quota.ts:45:export class TenantQuotaService {
src/modules\reminder\domain\gsi3-parse.ts:1:/** Parses `GSI3SK = TENANT#<tenantId>#OCCURRENCE#<occurrenceId>` back into its parts - used by the ReminderProducer to reconstruct tenant context from a GSI3 query result before the strongly-consistent base-item read (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
src/modules\reminder\domain\gsi3-parse.ts:5:    throw new Error(`Malformed GSI3SK: ${gsi3sk}`);
src/modules\reminder\domain\reminder-occurrence.ts:3: * co-located under the parent ExpirationItem's partition) and Â§3's GSI3 scheduler index
src/modules\reminder\domain\reminder-occurrence.ts:9:export type ReminderOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED" | "ACKED";
src/modules\reminder\domain\reminder-occurrence.ts:11:export interface ReminderOccurrence extends EntityKey {
src/modules\reminder\domain\reminder-occurrence.ts:24:  shard: string; // zero-padded shard number, string form as used in GSI3PK
src/modules\reminder\domain\reminder-occurrence.ts:32:  GSI3PK?: string; // present only while status === SCHEDULED (or CLAIMED, see note in materializer) - removed once TRIGGERED/CANCELLED so the scheduler index only ever contains live work
src/modules\reminder\domain\reminder-occurrence.ts:33:  GSI3SK?: string;
src/modules\reminder\domain\reminder-occurrence.ts:68: * Builds the GSI3 keys for a materialized occurrence. `shardCount`/`shardFnVersion` are
src/modules\reminder\domain\reminder-occurrence.ts:77:}): { GSI3PK: string; GSI3SK: string; shard: string } {
src/modules\reminder\domain\reminder-occurrence.ts:81:    GSI3PK: `DUE#${minuteBucket(input.scheduledAt)}#${shard}`,
src/modules\reminder\domain\reminder-occurrence.ts:82:    GSI3SK: `TENANT#${input.tenantId}#OCCURRENCE#${input.occurrenceId}`,
src/modules\reminder\domain\reminder-occurrence.ts:87:/** Enumerates the GSI3PK values to query for a given UTC minute across all shards of a given generation - used by both the producer (current minute + lookback) and reconciliation. */
src/modules\identity\application\resolve-request-context.ts:18:export interface ValidatedClaims {
src/modules\identity\application\resolve-request-context.ts:26:export interface ResolveRequestContextInput {
src/modules\identity\application\resolve-request-context.ts:32:export interface IdGenerator {
src/modules\identity\application\resolve-request-context.ts:37:export class RequestContextResolver {
src/modules\identity\domain\authorization.ts:7:export type Action =
src/modules\identity\domain\authorization.ts:22:export interface AuthorizedResource {
src/modules\identity\domain\authorization.ts:29:export interface AuthorizationInput {
src/modules\identity\domain\authorization.ts:40:export type Role = "OWNER" | "MEMBER" | "VIEWER";
src/modules\identity\domain\authorization.ts:61:export class AuthorizationDeniedError extends Error {
src/modules\reminder\domain\reminder-policy.ts:8:export type ReminderPolicyScope = "TEMPLATE" | "ITEM";
src/modules\reminder\domain\reminder-policy.ts:9:export type NotificationChannelKind = "EMAIL" | "WHATSAPP";
src/modules\reminder\domain\reminder-policy.ts:13:export interface ReminderTrigger {
src/modules\reminder\domain\reminder-policy.ts:21:export interface QuietHours {
src/modules\reminder\domain\reminder-policy.ts:26:export interface ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:35:export interface ReminderPolicy extends EntityKey, ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:53:export interface PutPolicyInput {
src/modules\expiration\domain\audit-event.ts:13:export type AuditAction = "CREATE" | "UPDATE" | "ARCHIVE" | "RENEW" | "DELETE";
src/modules\expiration\domain\audit-event.ts:15:export interface AuditEvent extends EntityKey {
src/modules\expiration\domain\audit-event.ts:44:export interface BuildAuditEventInput {
src/modules\reminder\domain\notification-intent.ts:8:export type NotificationIntentStatus = "PENDING" | "CANCELLED" | "DISPATCHED" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:9:export type NotificationIntentKind = "EXPIRATION_REMINDER" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:11:export interface NotificationIntent extends EntityKey {
src/modules\reminder\domain\shard-config.ts:14:export interface ShardGeneration {
src/modules\reminder\domain\shard-config.ts:21:export interface ShardConfig {
src/modules\identity\domain\request-context.ts:7:export interface RequestContext {
src/modules\identity\ports\identity-store.ts:8:export interface EntityKey {
src/modules\identity\ports\identity-store.ts:13:export interface IdentityStore {
src/modules\reminder\application\reminder-policy-service.ts:20:export interface ReminderPolicyServiceDeps {
src/modules\reminder\application\reminder-policy-service.ts:27:export class ReminderPolicyService {
src/modules\expiration\domain\expiration-item.ts:11:export type ExpirationItemStatus = "ACTIVE" | "ARCHIVED" | "RENEWED" | "DELETED";
src/modules\expiration\domain\expiration-item.ts:13:export interface ExpirationItem extends EntityKey {
src/modules\expiration\domain\expiration-item.ts:65:export interface CreateItemInput {
src/modules\expiration\domain\expiration-item.ts:80:export interface UpdateItemInput {
src/modules\expiration\domain\expiration-item.ts:94:export interface RenewItemInput {
src/modules\identity\http\test-route-handler.ts:13:export interface TestRouteHttpRequest {
src/modules\identity\http\test-route-handler.ts:19:export interface TestRouteHttpResponse {
src/modules\identity\http\test-route-handler.ts:24:export interface TestRouteDeps {
src/modules\identity\persistence\identity-mapping-repository.ts:6: * (the other is GSI3) â€” the lookup happens *before* tenantId is known.
src/modules\identity\persistence\identity-mapping-repository.ts:11:export interface IdentityMapping {
src/modules\identity\persistence\identity-mapping-repository.ts:25:export class IdentityMappingRepository {
src/modules\identity\persistence\user-repository.ts:13:export interface UserProfile {
src/modules\identity\persistence\user-repository.ts:29:export interface DeviceSession {
src/modules\identity\persistence\user-repository.ts:53:export class UserRepository {

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/reminder/ports/reminder-store.ts; Get-Content src/modules/expiration/ports/expiration-store.ts; Get-Content src/modules/identity/ports/identity-store.ts; Get-Content infra/lib/scoped-lambda-function.ts; Get-Content infra/lib/expiration-tracker-stack.ts; Get-Content src/workers/reminder-producer/producer.ts; Get-Content src/workers/reminder-dispatch/dispatch.ts; Get-Content src/workers/reminder-reconciliation/reconciliation.ts; Get-Content package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 368ms:
/**
 * DynamoDB surface the Reminder module needs - same SDK-agnostic port pattern as
 * src/modules/expiration/ports/expiration-store.ts (AGENTS.md Ã‚Â§6: "qualquer mÃƒÂ³dulo futuro
 * com a mesma necessidade deve reusar esse formato de port, nÃƒÂ£o inventar um novo"). This
 * IS that reuse: identical shape (get/putIfAbsent/update/transactWrite/queryGsi1-style),
 * plus one addition this module needs and Expiration didn't - `queryByItem`, to list an
 * item's existing ReminderOccurrence rows (co-located under the item's own PK per
 * data-model.md Ã‚Â§2) for materialization diffing, due-date-change cancellation and DST
 * reconciliation.
 *
 * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
 * - implementation-blueprint.md Ã‚Â§9.2's isolation safeguard ("nenhuma rota tenant-facing
 * tem permissÃƒÂ£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
 * the policy service, materializer and HTTP handlers - all tenant-facing) has NO method
 * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
 * ReminderProducer worker, exposes it. test/integration/gsi3-isolation.test.ts asserts
 * this structurally (no tenant-facing dependency graph can reach queryGsi3).
 */
// 2026-08-19 (Engineering Maturity Review): imports from shared/dynamodb/occ.ts directly
// rather than re-exporting through expiration/ports/expiration-store.js - the previous
// indirection was an accidental cross-module ports->ports dependency (this module doesn't
// actually need anything Expiration-specific here, just the generic DynamoDB shapes).
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface ReminderStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** All rows under an item's own partition whose SK begins with "OCC#" (ReminderOccurrence rows). Strongly consistent - base-partition read, not a GSI (data-model.md Ã‚Â§5). */
  queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string): Promise<T[]>;
}

export interface Gsi3QueryInput {
  gsi3pk: string;
}

/** Narrow port, injected ONLY into the ReminderProducer worker (see file header). */
export interface ReminderProducerStore {
  queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]>;
  /** Strongly consistent read of the base item, used by the producer to reconstruct tenant context before claiming (data-model.md Ã‚Â§3: "tenantId preservado... para reconstruÃƒÂ§ÃƒÂ£o segura do contexto"). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** Conditional SCHEDULED -> CLAIMED transition (implementation-blueprint.md Ã‚Â§9.3 point 3). */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
}
/**
 * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
 * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
 * inject an implementation, so domain/application logic is unit-testable without live
 * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
 * data-model.md Ã‚Â§5's load-bearing sentence requires item update + outbox record (and,
 * for M2, the AuditEvent) to commit in a single `TransactWriteItems` Ã¢â‚¬â€ the aggregate
 * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
 * be individually visible without the others.
 */
import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

// Re-exported for backward compatibility with existing importers (application/http layers
// of this and other modules) - canonical definitions now live in shared/dynamodb/occ.ts.
export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";

export interface Gsi1QueryInput {
  gsi1pk: string;
  ascending?: boolean;
  limit?: number;
}

export interface ExpirationStore {
  /** Strongly consistent single-item read (data-model.md Ã‚Â§5: authorization/edition/pre-send reads must be consistent). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  /** Unconditional overwrite of an existing item - used only for non-OCC bookkeeping records (e.g. idempotency status transitions), never for ExpirationItem/AuditEvent. */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * Commits every entry atomically. Throws an error recognized by
   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
   * assume partial application.
   */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** Eventually consistent GSI1 query (data-model.md Ã‚Â§3: vencimentos/dashboard). */
  queryGsi1<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi1QueryInput): Promise<T[]>;
}
/**
 * Minimal DynamoDB surface the identity module needs, following the SDK-agnostic port
 * pattern established in src/shared/idempotency/idempotency.ts (M0): callers inject an
 * implementation of this port, so the module is unit-testable without live AWS. M1's
 * infra layer (infra/lib) provisions the real table; src/modules/identity/persistence
 * wires a DocumentClient-backed adapter to this same port for production use.
 */
export interface EntityKey {
  PK: string;
  SK: string;
}

export interface IdentityStore {
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  update<T extends EntityKey>(item: T): Promise<void>;
}
/**
 * ScopedLambdaFunction Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§17.1. "O construct gera grants
 * especÃƒÂ­ficos e falha synth quando o recurso solicitado nÃƒÂ£o corresponde a uma capability
 * conhecida."
 *
 * Judgment call (blueprint is schematic, not exhaustive, about capability granularity):
 * DynamoDB IAM cannot restrict by entity type (SK prefix) the way the blueprint's example
 * syntax (`tableAccess.readWriteKeys("ReminderOccurrence", "ExpirationItem")`) visually
 * suggests - IAM conditions operate on dynamodb:LeadingKeys (PK) at best, not on SK
 * patterns. This implementation is honest about that limit: the `entities` list is kept
 * as construct metadata (visible in `cdk synth`, useful for audit/least-privilege review)
 * but the actual IAM grant is table-level read/write via the well-tested CDK
 * `Table.grantReadWriteData`. Tighter per-entity IAM (via a Query/Scan condition on SK
 * prefix, not currently exposed by CDK's grant helpers) is deferred; noted in the M1 report.
 */
import * as lambda from "aws-cdk-lib/aws-lambda";
import * as iam from "aws-cdk-lib/aws-iam";
import * as sqs from "aws-cdk-lib/aws-sqs";
import { Duration } from "aws-cdk-lib";
import { Construct } from "constructs";
import type { ExpirationTrackerTable } from "./dynamo-table.js";

export interface AccessCapability {
  readonly kind: string;
  readonly description: string;
  grant(fn: lambda.Function): void;
}

export function tableAccessFor(table: ExpirationTrackerTable) {
  return {
    readWriteKeys(...entities: string[]): AccessCapability {
      return {
        kind: "table:read-write",
        description: `read/write on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadWriteData(fn),
      };
    },
    readKeys(...entities: string[]): AccessCapability {
      return {
        kind: "table:read",
        description: `read-only on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadData(fn),
      };
    },
    create(...entities: string[]): AccessCapability {
      return {
        kind: "table:create",
        description: `create-only on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadWriteData(fn), // PutItem-only IAM condition not expressible via CDK grant helpers; documented limitation
      };
    },
    /** GSI3 (scheduler) read-only - reserved EXCLUSIVELY for ReminderProducer
     * (data-model.md Ã‚Â§3 / implementation-blueprint.md Ã‚Â§9.2's isolation safeguard). Routed
     * through ExpirationTrackerTable.grantGsi3ReadTo, never table.grantReadData, so no
     * other function can acquire this permission via this helper. */
    gsi3Read(): AccessCapability {
      return {
        kind: "table:gsi3-read",
        description: "GSI3 (scheduler) read-only - ReminderProducer only",
        grant: (fn) => table.grantGsi3ReadTo(fn),
      };
    },
  };
}

export function queueAccessFor() {
  return {
    consume(queue: sqs.IQueue): AccessCapability {
      return {
        kind: "queue:consume",
        description: `consume from ${queue.queueName}`,
        grant: (fn) => queue.grantConsumeMessages(fn),
      };
    },
    send(queue: sqs.IQueue): AccessCapability {
      return {
        kind: "queue:send",
        description: `send to ${queue.queueName}`,
        grant: (fn) => queue.grantSendMessages(fn),
      };
    },
  };
}

export function appConfigAccessFor() {
  return {
    read(profileName: string): AccessCapability {
      return {
        kind: "appconfig:read",
        description: `read AppConfig profile ${profileName}`,
        grant: (fn) =>
          fn.addToRolePolicy(
            new iam.PolicyStatement({
              actions: ["appconfig:GetLatestConfiguration", "appconfig:StartConfigurationSession"],
              resources: ["*"], // AppConfig resource ARNs are per-deploy; scoped once the AppConfig construct exists (M1+ follow-up)
            }),
          ),
      };
    },
  };
}

const KNOWN_CAPABILITY_KINDS = new Set([
  "table:read-write",
  "table:read",
  "table:create",
  "table:gsi3-read",
  "queue:consume",
  "queue:send",
  "appconfig:read",
]);

export interface ScopedLambdaFunctionProps extends Omit<lambda.FunctionProps, "code"> {
  access: AccessCapability[];
  /** Defaults to bundling entryFile via NodejsFunction-equivalent asset if provided; else code must be set via `code` prop override (tests use inline code). */
  code?: lambda.Code;
}

/**
 * Every Lambda in this system MUST be constructed via ScopedLambdaFunction, never
 * `new lambda.Function` directly (implementation-blueprint.md Ã‚Â§17.1) - it is the single
 * enforcement point for least-privilege IAM per function.
 */
export class ScopedLambdaFunction extends Construct {
  readonly function: lambda.Function;

  constructor(scope: Construct, id: string, props: ScopedLambdaFunctionProps) {
    super(scope, id);

    for (const capability of props.access) {
      if (!KNOWN_CAPABILITY_KINDS.has(capability.kind)) {
        // "falha synth quando o recurso solicitado nÃƒÂ£o corresponde a uma capability conhecida"
        throw new Error(
          `ScopedLambdaFunction(${id}): unknown access capability kind "${capability.kind}". ` +
            `Known kinds: ${[...KNOWN_CAPABILITY_KINDS].join(", ")}.`,
        );
      }
    }

    const { access, code, ...rest } = props;

    this.function = new lambda.Function(this, "Function", {
      timeout: Duration.seconds(10),
      memorySize: 256,
      tracing: lambda.Tracing.ACTIVE,
      ...rest,
      code: code ?? lambda.Code.fromInline("exports.handler = async () => ({ statusCode: 501 });"),
    });

    for (const capability of access) {
      capability.grant(this.function);
    }
  }
}
/**
 * M1 stack: wires DynamoDB, Cognito, the identity test route (ScopedLambdaFunction) and
 * the API skeleton together. Later milestones (M2+) add stacks/constructs for their own
 * modules, importing this table/auth rather than re-declaring them.
 */
import { Stack, type StackProps } from "aws-cdk-lib";
import type { Construct } from "constructs";
import * as lambda from "aws-cdk-lib/aws-lambda";
import { ExpirationTrackerTable } from "./dynamo-table.js";
import { ExpirationTrackerAuth, type MfaPolicy } from "./cognito.js";
import { ScopedLambdaFunction, tableAccessFor } from "./scoped-lambda-function.js";
import { ExpirationTrackerApi } from "./api.js";

export interface ExpirationTrackerStackProps extends StackProps {
  mfaPolicy?: MfaPolicy;
}

export class ExpirationTrackerStack extends Stack {
  readonly table: ExpirationTrackerTable;
  readonly auth: ExpirationTrackerAuth;
  readonly api: ExpirationTrackerApi;
  readonly remindersHandler: ScopedLambdaFunction;
  readonly reminderProducer: ScopedLambdaFunction;
  readonly reminderDispatch: ScopedLambdaFunction;
  readonly reminderReconciliation: ScopedLambdaFunction;

  constructor(scope: Construct, id: string, props: ExpirationTrackerStackProps = {}) {
    super(scope, id, props);

    this.table = new ExpirationTrackerTable(this, "Table");
    this.auth = new ExpirationTrackerAuth(this, "Auth", { mfaPolicy: props.mfaPolicy });

    const tableAccess = tableAccessFor(this.table);

    const testRouteHandler = new ScopedLambdaFunction(this, "TestPingHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("IdentityMapping", "User", "TenantQuota")],
    });

    // M2: ExpirationItem/AuditEvent CRUD+renew+dashboard, plus outbox writes for
    // ItemDueDateChanged. GSI1 (dashboard) is table-level read/write per
    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
    // header comment) - never grantGsi3ReadTo, which stays reserved for M3's
    // ReminderProducer.
    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ExpirationItem", "AuditEvent", "OutboxEvent", "IdempotencyRecord")],
    });

    this.api = new ExpirationTrackerApi(this, "Api", {
      auth: this.auth,
      testRouteHandler,
      itemsHandler,
    });

    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ReminderPolicy", "ReminderOccurrence")],
    });

    // ReminderProducer (Ã‚Â§9.3) is the ONLY function in the system granted gsi3Read() - the
    // narrow capability routed through ExpirationTrackerTable.grantGsi3ReadTo, which
    // itself never appears on any tenant-facing function (test/infra/reminder-engine.test.ts
    // asserts this at the synthesized-template level).
    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ReminderOccurrence"), tableAccess.gsi3Read()],
    });

    // ReminderDispatch (Ã‚Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
    // one TransactWriteItems - needs write access to occurrences/intents/outbox/idempotency
    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [
        tableAccess.readWriteKeys(
          "ReminderOccurrence",
          "NotificationIntent",
          "OutboxEvent",
          "IdempotencyRecord",
        ),
        tableAccess.readKeys("ExpirationItem", "ReminderPolicy"),
      ],
    });

    // ReminderReconciliation (Ã‚Â§9.5): single job for claim-expiry revert + DST
    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
    // the scheduler index).
    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [
        tableAccess.readWriteKeys("ReminderOccurrence"),
        tableAccess.readKeys("ExpirationItem", "ReminderPolicy"),
      ],
    });
  }
}
/**
 * ReminderProducer core logic - implementation-blueprint.md Ã‚Â§9.3. Pure(ish) - takes an
 * injected `ReminderProducerStore` and clock, no AWS SDK/Lambda runtime dependency, so it
 * is directly unit/integration-testable with a fake clock and fake GSI3 index (per the
 * task's requirement to test time-sensitive logic without real wall-clock waiting).
 *
 * Steps (Ã‚Â§9.3):
 *  1. read active shard generations (current + still-active legacy, Ã‚Â§9.2's reshard runbook);
 *  2. query GSI3 across N shards of minute M AND a lookback window [M-lookback, M] (default
 *     5 minutes) - "sem lookback, 'o prÃƒÂ³ximo tick reprocessa' ÃƒÂ© falso";
 *  3. per eligible occurrence, conditional SCHEDULED -> CLAIMED with a short claim
 *     `claimExpiresAt`, and emit one `reminder.dispatch.v1` command;
 *  4. partial-batch-failure handling: only failed entries are surfaced in `failed`, callers
 *     retry just those (never the whole batch);
 *  5. returns counters for `scheduler_lag_seconds`/per-shard/lookback-depth metrics (left to
 *     the Lambda handler to emit via SecureLogger/EMF - this module stays observability-agnostic).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { gsi3PartitionsForMinute } from "../../modules/reminder/domain/reminder-occurrence.js";
import { parseGsi3Sk } from "../../modules/reminder/domain/gsi3-parse.js";
import { activeGenerations, type ShardConfig } from "../../modules/reminder/domain/shard-config.js";
import { isTransactionCanceled } from "../../modules/reminder/ports/reminder-store.js";
import type { ReminderProducerStore } from "../../modules/reminder/ports/reminder-store.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";

export interface DispatchCommand {
  commandType: "reminder.dispatch.v1";
  tenantId: string;
  deduplicationKey: string;
  data: {
    itemId: string;
    occurrenceId: string;
    occurrenceVersion: number;
    scheduledAt: string;
    itemVersion: number;
    policyVersion: number;
  };
}

export interface ProducerDeps {
  store: ReminderProducerStore;
  shardConfig: ShardConfig;
  tableName: string;
  now: () => string;
  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (Ã‚Â§9.5). */
  claimTtlMs?: number;
  /** Lookback window in minutes, inclusive of the current minute (implementation-blueprint.md Ã‚Â§9.3 example: [M-5min, M]). */
  lookbackMinutes?: number;
}

export interface ProducerTickResult {
  claimed: DispatchCommand[];
  failed: { occurrenceId: string; tenantId: string; error: unknown }[];
  scanned: number;
  minutesScanned: string[];
  shardPartitionsScanned: number;
}

function minuteFloor(d: Date): Date {
  return new Date(Math.floor(d.getTime() / 60_000) * 60_000);
}

/** Runs one producer tick for wall-clock minute `tickMinute` (already floored to the minute by the caller/Lambda trigger). */
export async function runProducerTick(deps: ProducerDeps, tickMinute: Date): Promise<ProducerTickResult> {
  const generations = activeGenerations(deps.shardConfig, deps.now());
  const lookback = deps.lookbackMinutes ?? 5;
  const claimTtlMs = deps.claimTtlMs ?? 2 * 60_000;

  const minutesToScan: Date[] = [];
  for (let i = lookback; i >= 0; i--) {
    minutesToScan.push(minuteFloor(new Date(tickMinute.getTime() - i * 60_000)));
  }

  const claimed: DispatchCommand[] = [];
  const failed: { occurrenceId: string; tenantId: string; error: unknown }[] = [];
  let scanned = 0;
  let partitions = 0;

  // Dedup guard: the same occurrence could show up in more than one scanned minute only if
  // materialization ever wrote it under a stale bucket, which shouldn't happen - but a
  // Set guards against double-claiming within a single tick regardless.
  const seen = new Set<string>();

  for (const minute of minutesToScan) {
    for (const generation of generations) {
      const partitionKeys = gsi3PartitionsForMinute(minute, generation.shardCount);
      partitions += partitionKeys.length;
      for (const gsi3pk of partitionKeys) {
        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
        for (const row of rows) {
          scanned += 1;
          const { tenantId, occurrenceId } = parseGsi3Sk(row.GSI3SK);
          if (seen.has(occurrenceId)) continue;
          seen.add(occurrenceId);

          try {
            const occurrence = await deps.store.get<ReminderOccurrence>({ PK: row.PK, SK: row.SK });
            if (!occurrence || occurrence.status !== "SCHEDULED") {
              // Already claimed/cancelled/triggered by a prior tick or race - not a failure.
              continue;
            }

            const claimExpiresAt = new Date(Date.parse(deps.now()) + claimTtlMs).toISOString();
            await deps.store.transactWrite([
              {
                Update: buildVersionedUpdate({
                  tableName: deps.tableName,
                  key: { PK: row.PK, SK: row.SK },
                  tenantId,
                  expectedVersion: occurrence.version,
                  set: { status: "CLAIMED", claimedAt: deps.now(), claimExpiresAt },
                }),
              },
            ]);

            claimed.push({
              commandType: "reminder.dispatch.v1",
              tenantId,
              deduplicationKey: `${tenantId}|${occurrenceId}|${occurrence.version + 1}`,
              data: {
                itemId: occurrence.itemId,
                occurrenceId,
                occurrenceVersion: occurrence.version + 1,
                scheduledAt: occurrence.scheduledAt,
                itemVersion: occurrence.itemVersion,
                policyVersion: occurrence.policyVersion,
              },
            });
          } catch (err) {
            if (isTransactionCanceled(err)) {
              // Lost the claim race to a concurrent producer tick - not a failure to retry.
              continue;
            }
            failed.push({ occurrenceId, tenantId, error: err });
          }
        }
      }
    }
  }

  return {
    claimed,
    failed,
    scanned,
    minutesScanned: minutesToScan.map((d) => d.toISOString()),
    shardPartitionsScanned: partitions,
  };
}
/**
 * Reminder dispatch worker core logic - implementation-blueprint.md Ã‚Â§9.4. Consumes one
 * `reminder.dispatch.v1` command (produced by the ReminderProducer, Ã‚Â§9.3) and performs the
 * CLAIMED -> TRIGGERED transition, creating exactly one `NotificationIntent` idempotently.
 *
 * Validation order per Ã‚Â§9.4: occurrence must be CLAIMED (not SCHEDULED directly - the claim
 * prevents double-processing by two concurrent dispatch workers); item must be ACTIVE;
 * versions must match; schedule must be within tolerance; policy must still exist. If any
 * check fails, the occurrence is conditionally cancelled (CLAIMED -> CANCELLED) instead of
 * silently dropped, and repair is left to reconciliation (Ã‚Â§9.5) - never a new ad-hoc
 * materialization from inside dispatch.
 *
 * On success, everything commits in ONE TransactWriteItems (Ã‚Â§9.4: "corrige a lacuna de
 * durabilidade... antes o evento era tratado como reconstruÃƒÂ­vel sem outbox"):
 * NotificationIntent Put + occurrence CLAIMED->TRIGGERED Update + idempotency record Put
 * (`tenantId|occurrenceId`, Ã‚Â§9.4's exact key for NotificationIntentCreated consumers) +
 * outbox `notification.intent-created.v1` Put.
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { appendToTransaction } from "../../shared/outbox/outbox.js";
import { buildIdempotencyKey } from "../../shared/idempotency/idempotency.js";
import type { DomainEvent } from "../../shared/contracts/events.js";
import { itemKey } from "../../modules/expiration/domain/expiration-item.js";
import { policyKey, type ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import { intentKey, type NotificationIntent } from "../../modules/reminder/domain/notification-intent.js";
import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../../modules/reminder/ports/reminder-store.js";
import type { DispatchCommand } from "../reminder-producer/producer.js";

const NOTIFICATION_INTENT_CREATED = "notification.intent-created.v1";

export interface DispatchDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  newIntentId: () => string;
  newEventId: () => string;
  correlationId: () => string;
  /** Max allowed lag between scheduledAt and now before a claimed occurrence is considered stale (default 30 minutes - generous vs. the claim TTL, covers legitimate SQS/Lambda retry delay). */
  toleranceMs?: number;
}

export type DispatchOutcome =
  | { kind: "TRIGGERED"; intent: NotificationIntent }
  | { kind: "ALREADY_TRIGGERED" }
  | { kind: "CANCELLED_STALE"; reason: string }
  | { kind: "SKIPPED_NOT_CLAIMED" };

/** Looks up the occurrence by (itemId, occurrenceId) - occurrences are co-located under the item's own partition (data-model.md Ã‚Â§2), but the command doesn't carry the SK's scheduledAt segment, so the worker queries the item's occurrences and finds the match by occurrenceId. Kept here rather than in ReminderStore to avoid growing the port for a single caller. */
async function findOccurrence(store: ReminderStore, tenantId: string, itemId: string, occurrenceId: string): Promise<ReminderOccurrence | undefined> {
  const rows = await store.queryByItem<ReminderOccurrence>(tenantId, itemId);
  return rows.find((r) => r.occurrenceId === occurrenceId);
}

export async function dispatchOccurrence(deps: DispatchDeps, command: DispatchCommand): Promise<DispatchOutcome> {
  const { tenantId } = command;
  const { itemId, occurrenceId, itemVersion, policyVersion } = command.data;

  const occurrence = await findOccurrence(deps.store, tenantId, itemId, occurrenceId);
  if (!occurrence) {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }
  if (occurrence.status === "TRIGGERED") {
    return { kind: "ALREADY_TRIGGERED" };
  }
  if (occurrence.status !== "CLAIMED") {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }

  const item = await deps.store.get<{ PK: string; SK: string; status: string; version: number }>(itemKey(tenantId, itemId));
  const policy = await deps.store.get<ReminderPolicy>(policyKey(tenantId, occurrence.policyId));

  const toleranceMs = deps.toleranceMs ?? 30 * 60_000;
  const withinTolerance = Math.abs(Date.parse(deps.now()) - Date.parse(occurrence.scheduledAt)) <= toleranceMs;

  const stale =
    !item ||
    item.status !== "ACTIVE" ||
    item.version !== itemVersion ||
    occurrence.itemVersion !== itemVersion ||
    !policy ||
    policy.version !== policyVersion ||
    !policy.enabled ||
    !withinTolerance;

  if (stale) {
    const reason = !item
      ? "ITEM_NOT_FOUND"
      : item.status !== "ACTIVE"
        ? "ITEM_NOT_ACTIVE"
        : item.version !== itemVersion || occurrence.itemVersion !== itemVersion
          ? "ITEM_VERSION_MISMATCH"
          : !policy || !policy.enabled
            ? "POLICY_GONE_OR_DISABLED"
            : policy.version !== policyVersion
              ? "POLICY_VERSION_MISMATCH"
              : "OUT_OF_TOLERANCE";

    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId,
            expectedVersion: occurrence.version,
            set: { status: "CANCELLED" },
          }),
        },
      ]);
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
    return { kind: "CANCELLED_STALE", reason };
  }

  const now = deps.now();
  const intentId = deps.newIntentId();
  const intent: NotificationIntent = {
    ...intentKey(tenantId, intentId),
    entityType: "NotificationIntent",
    intentId,
    tenantId,
    kind: "EXPIRATION_REMINDER",
    itemId,
    occurrenceId,
    itemVersion,
    policyId: occurrence.policyId,
    policyVersion,
    scheduledAt: occurrence.scheduledAt,
    requestedChannels: policy.channels.filter((c) => !(policy.optOutChannels ?? []).includes(c)),
    status: "PENDING",
    supersedesIntentId: null,
    correctionReason: null,
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const entries: TransactWriteEntry[] = [
    { Put: { TableName: deps.tableName, Item: { ...intent }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
    {
      Update: buildVersionedUpdate({
        tableName: deps.tableName,
        key: { PK: occurrence.PK, SK: occurrence.SK },
        tenantId,
        expectedVersion: occurrence.version,
        set: { status: "TRIGGERED" },
      }),
    },
  ];

  // Idempotency record for NotificationIntentCreated consumers (Ã‚Â§9.4: "tenantId|occurrenceId").
  const idem = buildIdempotencyKey(deps.tableName, tenantId, "reminder.dispatch", occurrenceId);
  entries.push({
    Put: {
      TableName: deps.tableName,
      Item: {
        PK: idem.PK,
        SK: idem.SK,
        entityType: "IdempotencyRecord",
        tenantId,
        operation: "reminder.dispatch",
        requestHash: occurrenceId,
        status: "COMPLETED",
        responseRef: intentId,
        expiresAt: new Date(Date.parse(now) + 7 * 24 * 60 * 60_000).toISOString(),
        createdAt: now,
        completedAt: now,
      },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });

  const event: DomainEvent = {
    specVersion: "1.0",
    eventId: deps.newEventId(),
    eventType: NOTIFICATION_INTENT_CREATED,
    source: "expiration-tracker.reminder",
    occurredAt: now,
    correlationId: deps.correlationId(),
    tenantId,
    actor: { type: "SYSTEM" },
    aggregate: { type: "NotificationIntent", id: intentId, version: 1 },
    data: {
      intentId,
      kind: "EXPIRATION_REMINDER",
      itemId,
      occurrenceId,
      itemVersion,
      policyId: occurrence.policyId,
      policyVersion,
      scheduledAt: occurrence.scheduledAt,
      requestedChannels: intent.requestedChannels,
      status: "PENDING",
      supersedesIntentId: null,
      correctionReason: null,
    },
  };
  appendToTransaction(entries as never, deps.tableName, event);

  try {
    await deps.store.transactWrite(entries);
  } catch (err) {
    if (isTransactionCanceled(err)) {
      // Duplicate delivery of the same command (SQS at-least-once) racing a prior success -
      // either the idempotency Put or the occurrence's CLAIMED condition already advanced.
      return { kind: "ALREADY_TRIGGERED" };
    }
    throw err;
  }

  return { kind: "TRIGGERED", intent };
}
/**
 * Reconciliation - implementation-blueprint.md Ã‚Â§9.5, run as ONE job/Lambda serving TWO
 * purposes (per the task brief: "don't build two separate reconciliation paths"), both
 * documented in Ã‚Â§9.3 point 4 ("claims expirados... via reconciliaÃƒÂ§ÃƒÂ£o, mesmo job da Ã‚Â§9.5")
 * and Ã‚Â§9.5 itself (DST):
 *
 *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
 *      `claimExpiresAt` (dispatch worker crashed/timed out before completing) reverts to
 *      SCHEDULED, conditionally, so the next producer tick's lookback window picks it up
 *      again;
 *  (b) DST-affected re-evaluation (Ã‚Â§9.5 steps 1-6): for each (item, enabled policy) pair
 *      in the reconciliation batch, recompute the expected schedule from the policy's
 *      IANA timeZone + rule against a `[now, now+7d]` window, compare to what's actually
 *      materialized, cancel divergent SCHEDULED occurrences and materialize missing ones
 *      idempotently - TRIGGERED/ACKED history is never touched.
 *
 * Both passes reuse the same store/materializer/clock and are invoked together by
 * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
 * unblocks the producer sooner).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
import { ReminderMaterializer } from "../../modules/reminder/application/reminder-materializer.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import type { ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ShardConfig } from "../../modules/reminder/domain/shard-config.js";

export interface ReconciliationDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  shardConfig: ShardConfig;
}

/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
 * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
 * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
 * wiring); tests/callers supply the batch directly. */
export interface DstReconciliationCandidate {
  tenantId: string;
  itemId: string;
  itemVersion: number;
  itemDueDate: string;
  policy: ReminderPolicy;
}

export interface ReconciliationResult {
  claimsReverted: number;
  dstCancelled: number;
  dstCreated: number;
  dstDivergences: number;
}

/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
export async function reconcileExpiredClaims(
  deps: ReconciliationDeps,
  candidateOccurrences: ReminderOccurrence[],
): Promise<number> {
  let reverted = 0;
  const now = deps.now();
  for (const occurrence of candidateOccurrences) {
    if (occurrence.status !== "CLAIMED") continue;
    if (!occurrence.claimExpiresAt || occurrence.claimExpiresAt > now) continue;
    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId: occurrence.tenantId,
            expectedVersion: occurrence.version,
            set: { status: "SCHEDULED" },
          }),
        },
      ]);
      reverted += 1;
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
  }
  return reverted;
}

/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (Ã‚Â§9.5 steps 1-6). */
export async function reconcileDst(
  deps: ReconciliationDeps,
  candidates: DstReconciliationCandidate[],
): Promise<{ cancelled: number; created: number; divergences: number }> {
  const materializer = new ReminderMaterializer(deps.store, deps.tableName, deps.now);
  const windowStart = Date.parse(deps.now());
  const windowEnd = windowStart + 7 * 24 * 60 * 60_000;

  let cancelled = 0;
  let created = 0;
  let divergences = 0;

  for (const candidate of candidates) {
    if (!candidate.policy.enabled) continue;

    const existing = await deps.store.queryByItem<ReminderOccurrence>(candidate.tenantId, candidate.itemId);
    const liveExisting = existing.filter(
      (o) =>
        o.policyId === candidate.policy.policyId &&
        o.itemVersion === candidate.itemVersion &&
        o.policyVersion === candidate.policy.version &&
        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
    );

    // Expected schedule per trigger, recomputed fresh from the IANA rule (never a frozen offset).
    const expectedByTrigger = new Map<string, { scheduledAtUtc: string }>();
    for (const trigger of candidate.policy.triggers) {
      const schedule = materializer.computeSchedule({
        itemDueDate: candidate.itemDueDate,
        offsetIso: trigger.offsetIso,
        localTime: trigger.localTime,
        timeZone: candidate.policy.timeZone,
        quietHours: candidate.policy.quietHours,
      });
      const t = Date.parse(schedule.scheduledAtUtc);
      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
      expectedByTrigger.set(trigger.triggerId, { scheduledAtUtc: schedule.scheduledAtUtc });
    }

    // Cancel occurrences whose triggerId is no longer expected, or whose scheduledAt now
    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
    // liveExisting already excludes those statuses).
    for (const occurrence of liveExisting) {
      const expected = expectedByTrigger.get(occurrence.triggerId);
      const diverges = !expected || expected.scheduledAtUtc !== occurrence.scheduledAt;
      if (!diverges) continue;
      divergences += 1;
      try {
        await deps.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: deps.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: candidate.tenantId,
              expectedVersion: occurrence.version,
              set: { status: "CANCELLED" },
            }),
          },
        ]);
        cancelled += 1;
      } catch (err) {
        if (!isTransactionCanceled(err)) throw err;
      }
    }

    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
    // materialize() itself is idempotent (putIfAbsent keyed by the deterministic
    // occurrenceId hash), so re-running it for triggers that already matched is a safe no-op.
    const stillMissing = candidate.policy.triggers.some((t) => {
      const expected = expectedByTrigger.get(t.triggerId);
      if (!expected) return false;
      return !liveExisting.some((o) => o.triggerId === t.triggerId && o.scheduledAt === expected.scheduledAtUtc);
    });
    if (stillMissing) {
      const result = await materializer.materialize({
        tenantId: candidate.tenantId,
        itemId: candidate.itemId,
        itemVersion: candidate.itemVersion,
        itemDueDate: candidate.itemDueDate,
        policy: candidate.policy,
        shardConfig: deps.shardConfig,
      });
      created += result.created.length;
    }
  }

  return { cancelled, created, divergences };
}

export async function runReconciliation(
  deps: ReconciliationDeps,
  input: { expiredClaimCandidates: ReminderOccurrence[]; dstCandidates: DstReconciliationCandidate[] },
): Promise<ReconciliationResult> {
  const claimsReverted = await reconcileExpiredClaims(deps, input.expiredClaimCandidates);
  const dst = await reconcileDst(deps, input.dstCandidates);
  return { claimsReverted, dstCancelled: dst.cancelled, dstCreated: dst.created, dstDivergences: dst.divergences };
}
{
  "name": "expiration-tracker",
  "version": "0.0.0",
  "private": true,
  "description": "Micro-SaaS de controle de vencimentos/renovacoes - guardrails e contratos (M0)",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "validate-schemas": "tsx scripts/validate-schemas.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "@vitest/coverage-v8": "^1.6.0",
    "aws-cdk-lib": "2.265.0",
    "aws-jwt-verify": "5.2.1",
    "constructs": "10.8.1",
    "dependency-cruiser": "17.4.3",
    "eslint": "^8.57.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "ajv": "^8.16.0",
    "ajv-formats": "^2.1.1",
    "ulid": "^2.3.0"
  }
}

codex
# Proposta tÃ©cnica â€” milestone Runtime Real / G8

## 1. DecisÃ£o central

O milestone deve transformar o fluxo atual em runtime implantÃ¡vel sem contaminar os ports com tipos do AWS SDK:

```text
EventBridge Rule
      â”‚
      â–¼
ReminderProducer Lambda
      â”‚
      â”œâ”€ TransactWrite:
      â”‚    SCHEDULED â†’ CLAIMED
      â”‚    + registro durÃ¡vel DispatchOutbox
      â–¼
DynamoDB Stream / relay
      â”‚
      â–¼
ReminderDispatchQueue
      â”‚
      â–¼
ReminderDispatch Lambda
      â”‚
      â”œâ”€ revalida estado/versionamento
      â””â”€ TransactWrite:
           CLAIMED â†’ TRIGGERED
           + NotificationIntent
           + OutboxEvent
           + IdempotencyRecord
```

A gravaÃ§Ã£o durÃ¡vel intermediÃ¡ria Ã© necessÃ¡ria. Fazer `TransactWrite(CLAIMED)` e depois `SendMessage` diretamente no handler mantÃ©m uma janela de perda:

1. DynamoDB confirma o claim.
2. Lambda cai antes de enviar Ã  SQS.
3. A ocorrÃªncia fica `CLAIMED`, mas nÃ£o existe mensagem.

A reconciliaÃ§Ã£o diÃ¡ria nÃ£o fecha adequadamente essa janela: quando reverter o claim, a ocorrÃªncia poderÃ¡ estar fora do lookback de cinco minutos do producer. Portanto, fila e DLQ, isoladamente, nÃ£o bastam para fechar o P1 #1.

Minha recomendaÃ§Ã£o Ã© um registro `DispatchOutbox` gravado atomicamente com o claim e publicado por DynamoDB Streams. Uma alternativa aceitÃ¡vel Ã© EventBridge Pipes entre o stream e a SQS, desde que seja demonstrado que filtragem, transformaÃ§Ã£o do stream record e retries funcionam no ambiente AWS real. Eu prefiro um pequeno `DispatchOutboxRelay` Lambda porque oferece controle explÃ­cito de schema, observabilidade, partial batch failure e evoluÃ§Ã£o do contrato.

## 2. DependÃªncias

Adicionar como dependÃªncias de runtime:

```json
{
  "@aws-sdk/client-dynamodb": "versÃ£o exata",
  "@aws-sdk/lib-dynamodb": "versÃ£o exata",
  "@aws-sdk/client-sqs": "versÃ£o exata"
}
```

E como dependÃªncias de desenvolvimento:

```json
{
  "esbuild": "versÃ£o exata",
  "@types/aws-lambda": "versÃ£o exata",
  "testcontainers": "versÃ£o exata"
}
```

Escolhas:

- `DynamoDBClient`: transporte, configuraÃ§Ã£o de retry e endpoint.
- `DynamoDBDocumentClient`: objetos JavaScript, compatÃ­vel com os builders existentes.
- `SQSClient`: publicaÃ§Ã£o explÃ­cita pelo relay.
- `esbuild`: bundling determinÃ­stico dos entrypoints ESM para Node.js 20.
- VersÃµes exatas, nÃ£o ranges, mantendo lockfile e `npm ci`.

ConfiguraÃ§Ã£o do `DynamoDBDocumentClient`:

```ts
DynamoDBDocumentClient.from(client, {
  marshallOptions: {
    removeUndefinedValues: true,
    convertClassInstanceToMap: false,
  },
})
```

NÃ£o habilitaria conversÃµes numÃ©ricas silenciosas. Campos de versÃ£o precisam continuar sendo nÃºmeros e valores grandes devem ser avaliados explicitamente antes de usar `wrapNumbers`.

O cliente deve usar retry adaptativo ou standard com nÃºmero pequeno e explÃ­cito de tentativas, por exemplo trÃªs. O timeout total precisa caber com folga no timeout da Lambda; nÃ£o se deve deixar o SDK consumir todo o orÃ§amento da invocaÃ§Ã£o.

## 3. Estrutura dos adapters

```text
src/
  runtime/
    aws/
      dynamodb/
        client.ts
        errors.ts
        execute-transaction.ts
        identity-store-dynamodb.ts
        expiration-store-dynamodb.ts
        reminder-store-dynamodb.ts
        reminder-producer-store-dynamodb.ts
        reconciliation-candidate-source-dynamodb.ts
      sqs/
        dispatch-command-publisher.ts

    handlers/
      test-ping-handler.ts
      items-handler.ts
      reminders-handler.ts
      reminder-producer-handler.ts
      reminder-dispatch-handler.ts
      reminder-reconciliation-handler.ts
      dispatch-outbox-relay-handler.ts

    composition/
      identity.ts
      expiration.ts
      reminder.ts
```

Um adapter por port:

- `DynamoDbIdentityStore implements IdentityStore`
- `DynamoDbExpirationStore implements ExpirationStore`
- `DynamoDbReminderStore implements ReminderStore`
- `DynamoDbReminderProducerStore implements ReminderProducerStore`

NÃ£o criaria uma classe â€œuniversalâ€ pÃºblica implementando todos os ports. Isso permitiria injetar acidentalmente acesso ao GSI3 em cÃ³digo tenant-facing.

Pode haver helpers internos compartilhados para `GetCommand`, `PutCommand`, `UpdateCommand` e `TransactWriteCommand`, mas o mÃ©todo `queryGsi3` existe somente em `reminder-producer-store-dynamodb.ts`.

### Mapeamento das operaÃ§Ãµes

| Port | Comando AWS |
|---|---|
| `get` | `GetCommand`, `ConsistentRead: true` |
| `putIfAbsent` | `PutCommand` com condiÃ§Ã£o existente; retorna `false` apenas para falha condicional |
| `update` | `PutCommand` de overwrite, restrito aos usos jÃ¡ documentados pelo port |
| `transactWrite` | `TransactWriteCommand` usando diretamente os builders existentes |
| `queryGsi1` | `QueryCommand`, `IndexName: GSI1`, eventual consistency |
| `queryByItem` | `QueryCommand` na tabela base, `PK = :pk AND begins_with(SK, :occ)` e leitura consistente |
| `queryGsi3` | `QueryCommand`, `IndexName: GSI3`, somente no adapter privilegiado |

PaginaÃ§Ã£o deve ser completa por padrÃ£o. `limit` representa limite lÃ³gico total, nÃ£o apenas o `Limit` de uma Ãºnica pÃ¡gina.

### TraduÃ§Ã£o de erros

O adapter deve traduzuzir, sem esconder a causa:

- `ConditionalCheckFailedException` em `putIfAbsent` â†’ `false`.
- `TransactionCanceledException` â†’ erro reconhecÃ­vel por `isTransactionCanceled`.
- throttling, timeout, falha 5xx e indisponibilidade â†’ `AppError` retryable.
- validation error, item/schema invÃ¡lido â†’ erro terminal.
- `AccessDeniedException` â†’ erro terminal operacional e alarme; nÃ£o deve virar retry infinito.

Preservar `$metadata.requestId`, nome da operaÃ§Ã£o e causa no log, sem registrar item completo nem dados pessoais.

## 4. Lacuna da reconciliaÃ§Ã£o

O handler de reconciliaÃ§Ã£o atual nÃ£o consegue operar sozinho: `runReconciliation` recebe arrays de candidatos, mas `ReminderStore` nÃ£o oferece uma consulta que os produza.

Executar a Lambda diariamente com arrays vazios seria outro false-green.

O milestone precisa introduzir um port estreito, por exemplo:

```ts
interface ReconciliationCandidateSource {
  listExpiredClaims(input: PageInput): AsyncIterable<ReminderOccurrence>;
  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
}
```

O adapter consultaria o GSI6 ou o Ã­ndice efetivamente normativo. Isso Ã© integraÃ§Ã£o mÃ­nima necessÃ¡ria, nÃ£o redesenho do domÃ­nio.

Se o GSI6 ainda nÃ£o representa claims expirados e polÃ­ticas ativas de maneira consultÃ¡vel, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo estÃ¡ apenas agendado, nÃ£o funcional.

AlÃ©m da execuÃ§Ã£o diÃ¡ria de DST, recuperaÃ§Ã£o de claims precisa ter cadÃªncia curta. Recomendo:

- reconciliaÃ§Ã£o de claims expirados: a cada minuto ou cinco minutos;
- reconciliaÃ§Ã£o DST/materializaÃ§Ã£o: diÃ¡ria.

Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durÃ¡vel, a reconciliaÃ§Ã£o curta continua Ãºtil para reparar estados excepcionais.

## 5. Handlers Lambda

Cada arquivo em `src/runtime/handlers` Ã© um composition root:

1. valida variÃ¡veis de ambiente;
2. instancia clientes e adapters fora do handler para reuso em warm starts;
3. converte o evento AWS para os tipos internos;
4. invoca a lÃ³gica existente;
5. converte resultado/erro para o protocolo da origem;
6. registra mÃ©tricas e logs via `SecureLogger`.

Nenhuma lÃ³gica de negÃ³cio deve morar no wrapper Lambda.

VariÃ¡veis mÃ­nimas:

```text
TABLE_NAME
DISPATCH_QUEUE_URL
SHARD_CONFIG
PRODUCER_LOOKBACK_MINUTES
CLAIM_TTL_SECONDS
LOG_LEVEL
```

Para HTTP, os handlers reais devem interpretar API Gateway HTTP API v2, extrair claims jÃ¡ validadas pelo authorizer, construir `RequestContext` e mapear `AppError` para status HTTP consistente.

Para schedules, validar `source`, `detail-type` e um campo explÃ­cito `mode`. Evento inesperado Ã© terminal.

Para SQS, validar cada body contra `schemas/queues/reminder.dispatch.v1.json` antes de executar o domÃ­nio.

## 6. Bundling

Eu estenderia `ScopedLambdaFunction` com `entry` e `handlerExport`, mantendo-o como Ãºnico ponto de criaÃ§Ã£o de Lambda. Internamente, usaria `lambda.Code.fromAsset` com bundling `esbuild`, ou incorporaria a semÃ¢ntica de `NodejsFunction` sem permitir que stacks criem Lambdas por fora do construct.

Exemplo conceitual:

```ts
new ScopedLambdaFunction(this, "ReminderDispatch", {
  entry: "src/runtime/handlers/reminder-dispatch-handler.ts",
  handlerExport: "handler",
  runtime: lambda.Runtime.NODEJS_20_X,
  access: [...],
});
```

ConfiguraÃ§Ã£o:

- target `node20`;
- platform `node`;
- formato ESM compatÃ­vel com o projeto;
- sourcemap externo;
- minificaÃ§Ã£o desabilitada inicialmente para stack traces auditÃ¡veis;
- arquivo de lock explÃ­cito;
- bundle independente por funÃ§Ã£o;
- AWS SDK v3 incluÃ­do no bundle para versÃ£o reproduzÃ­vel.

NÃ£o confiaria na versÃ£o do SDK fornecida pela runtime Lambda.

Testes de infra devem falhar se qualquer funÃ§Ã£o continuar usando inline code ou contiver o corpo 501.

## 7. SQS e DLQ

### Recursos

```text
ExpirationTracker-ReminderDispatch
ExpirationTracker-ReminderDispatch-DLQ
```

Os nomes fÃ­sicos podem receber stage, por exemplo:

```text
expiration-tracker-dev-reminder-dispatch
expiration-tracker-dev-reminder-dispatch-dlq
```

ConfiguraÃ§Ã£o proposta:

| Propriedade | Queue | DLQ |
|---|---:|---:|
| Tipo | Standard | Standard |
| RetenÃ§Ã£o | 4 dias | 14 dias |
| Long polling | 20 s | n/a |
| `maxReceiveCount` | 5 | n/a |
| Criptografia | SQS managed ou KMS do ambiente | igual |
| Visibility timeout | â‰¥ 6 Ã— timeout da Lambda | n/a |

A fila deve ser Standard, pois OCC e idempotÃªncia jÃ¡ sÃ£o necessÃ¡rios independentemente da ordenaÃ§Ã£o; FIFO adicionaria throughput, group-id e dedup-window sem eliminar a necessidade de idempotÃªncia durÃ¡vel.

### Event source mapping

- batch size inicial: `10`;
- batching window: `0â€“1 s`;
- `reportBatchItemFailures: true`;
- reserved concurrency da funÃ§Ã£o: `10`;
- maximum concurrency do event source mapping: `10`;
- `bisectBatchOnError` nÃ£o se aplica a SQS;
- payload schema validado item a item.

Resposta:

```ts
return {
  batchItemFailures: failures.map(({ messageId }) => ({
    itemIdentifier: messageId,
  })),
};
```

ClassificaÃ§Ã£o:

- sucesso, duplicata ou estado jÃ¡ avanÃ§ado â†’ ACK;
- erro retryable â†’ incluir em `batchItemFailures`;
- poison message/schema invÃ¡lido â†’ terminal, mas precisa chegar Ã  DLQ.

Para poison message, hÃ¡ duas polÃ­ticas vÃ¡lidas. Eu recomendo retornar falha atÃ© `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observÃ¡vel. Para evitar custo inÃºtil, registrar a primeira detecÃ§Ã£o e emitir mÃ©trica por receive count. NÃ£o deletar manualmente nem enviar diretamente Ã  DLQ.

O relay do outbox tambÃ©m deve usar partial batch failure no DynamoDB Stream. Um registro sÃ³ Ã© marcado publicado depois de `SendMessage` confirmado; duplicaÃ§Ã£o entre envio e marcaÃ§Ã£o Ã© esperada e absorvida pelo dispatch idempotente.

## 8. EventBridge

Escolho **EventBridge Rule legado com `ScheduleExpression`**, nÃ£o EventBridge Scheduler, para estas cadÃªncias fixas.

Motivos:

- jÃ¡ Ã© a tecnologia especificada no escopo;
- `rate(1 minute)` Ã© simples e suficiente;
- integraÃ§Ã£o direta com Lambda e retry policy madura;
- nÃ£o hÃ¡ milhÃµes de schedules por tenant;
- flexible time window nÃ£o existe em Scheduled Rule e nÃ£o Ã© necessÃ¡ria para o producer.

Regras:

```text
ReminderProducerSchedule:
  rate(1 minute)

ReminderClaimReconciliationSchedule:
  rate(1 minute) ou rate(5 minutes)

ReminderDstReconciliationSchedule:
  cron diÃ¡rio em UTC
```

O evento deve carregar:

```json
{
  "mode": "PRODUCE"
}
```

ou:

```json
{
  "mode": "CLAIMS"
}
```

ou:

```json
{
  "mode": "DST"
}
```

Reserved concurrency:

- producer: `2`;
- reconciliation: `1`;
- dispatch: `10`.

Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contÃ­nuas precisam de alarme por invocaÃ§Ãµes com erro e por scheduler lag.

### Por que nÃ£o Scheduler

EventBridge Scheduler seria preferÃ­vel se houvesse schedules individuais, timezone por schedule, execuÃ§Ã£o Ãºnica ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.

Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliaÃ§Ã£o diÃ¡ria, uma janela flexÃ­vel seria aceitÃ¡vel, mas nÃ£o justifica misturar dois mecanismos.

## 9. Testes

### Camada 1 â€” testes rÃ¡pidos

Continuam executando em todo PR:

- unitÃ¡rios do domÃ­nio;
- contratos de schema;
- CDK assertions;
- teste do bundle de cada entrypoint;
- teste que proÃ­be `fromInline` nos handlers de produÃ§Ã£o;
- teste estrutural que assegura que somente producer tem ARN de GSI3.

### Camada 2 â€” integraÃ§Ã£o em containers

Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade especÃ­fica.

DynamoDB Local, sozinho, nÃ£o testa:

- redrive SQS;
- DLQ;
- event source mapping;
- EventBridge;
- interaÃ§Ã£o DynamoDB â†’ relay â†’ SQS.

LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suÃ­te, portas dinÃ¢micas e cleanup previsÃ­vel.

Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.

Casos obrigatÃ³rios:

1. CondiÃ§Ã£o de OCC concorrente: apenas um writer vence.
2. Claim + dispatch outbox sÃ£o atÃ´micos.
3. Falha do relay preserva o registro para retry.
4. Duas entregas SQS criam um Ãºnico `NotificationIntent`.
5. Falha em um item do batch nÃ£o repete os itens bem-sucedidos.
6. Poison message chega Ã  DLQ apÃ³s cinco receives.
7. Claim expirado Ã© recuperado e volta a produzir trabalho.
8. Renew/cancel concorrente faz dispatch cancelar por versÃ£o obsoleta.
9. Query GSI3 encontra mÃºltiplos tenants, mas o producer nunca mistura o contexto reconstruÃ­do.
10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.

Para timeout real de dependÃªncia, usaria um proxy de rede controlÃ¡vel, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexÃ£o acima do timeout configurado e verifica:

- erro classificado como retryable;
- mensagem nÃ£o reconhecida como sucesso;
- retry/redrive;
- nenhuma transaÃ§Ã£o parcial.

Isso testa falha de transporte real, nÃ£o um `mockRejectedValue`.

### Camada 3 â€” sandbox AWS efÃªmero

LocalStack nÃ£o prova semÃ¢ntica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 nÃ£o deve ser aprovado somente com containers.

Criaria uma suÃ­te curta contra uma stack AWS efÃªmera:

- deploy em conta/regiÃ£o de teste;
- TTL mÃ¡ximo da stack e tags de cleanup;
- seed de poucos itens;
- invocaÃ§Ã£o do producer;
- observaÃ§Ã£o da mensagem e do dispatch;
- poison message atÃ© DLQ;
- teste negativo assumindo a role de uma Lambda tenant-facing e tentando `Query` em GSI3;
- validaÃ§Ã£o de schedule por invocaÃ§Ã£o manual com o mesmo target, sem esperar o relÃ³gio;
- destruiÃ§Ã£o da stack ao final.

CadÃªncia:

- containers em todo PR;
- sandbox AWS no merge para `develop` ou workflow manual obrigatÃ³rio do milestone;
- teste completo de schedule real em workflow noturno, pois esperar ticks torna PRs lentos e sujeitos a jitter.

O artefato da execuÃ§Ã£o AWS â€” CloudWatch request IDs, mÃ©tricas, ARN da stack e resultados â€” deve ser preservado como evidÃªncia do gate.

## 10. Fault injection exigida

| Falha | InjeÃ§Ã£o | EvidÃªncia esperada |
|---|---|---|
| Timeout DynamoDB/SQS | Toxiproxy interrompe conexÃ£o | erro retryable, retry, sem estado parcial |
| Queda apÃ³s claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
| Queda apÃ³s `SendMessage` | falhar antes de marcar outbox | possÃ­vel duplicata, um Ãºnico efeito lÃ³gico |
| Poison message | body invÃ¡lido real em SQS | cinco receives e mensagem na DLQ |
| Falha parcial de batch | uma mensagem invÃ¡lida entre vÃ¡lidas | somente invÃ¡lida retorna em `batchItemFailures` |
| Renew concorrente | atualizar versÃ£o apÃ³s claim | dispatch cancela stale, sem intent incorreto |
| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
| Duplo producer | duas invocaÃ§Ãµes simultÃ¢neas | um claim vence por condiÃ§Ã£o |
| Tentativa GSI3 nÃ£o autorizada | role real tenant-facing | `AccessDenied`, nenhum dado retornado |

## 11. CritÃ©rios de saÃ­da do milestone

O milestone sÃ³ pode alegar fechamento operacional de G8 quando houver:

- zero handlers 501;
- bundles reais sintetizados;
- adapters reais cobrindo todos os ports;
- reconciliaÃ§Ã£o alimentada por consultas reais, nÃ£o arrays vazios;
- claim e publicaÃ§Ã£o conectados por mecanismo durÃ¡vel;
- fila, DLQ e redrive sintetizados e exercitados;
- partial batch failure comprovado;
- timeout, poison message e redrive exercitados;
- teste AWS negativo de GSI3 com role real;
- teste concorrente renew/cancel/dispatch;
- evidÃªncia registrada da execuÃ§Ã£o em sandbox AWS.

## 12. O que este milestone nÃ£o resolve

â€œG8 fechadoâ€ nÃ£o significa que os cinco P1 desapareceram integralmente.

- **P1 #1 â€” recuperaÃ§Ã£o assÃ­ncrona:** fecha o trecho producer â†’ dispatch se houver outbox/relay e reconciliaÃ§Ã£o consultÃ¡vel. NÃ£o fecha pipelines futuros de Notification.
- **P1 #2 â€” idempotÃªncia no efeito externo:** nÃ£o pode ser provada ainda. `NotificationIntent` Ã© apenas uma intenÃ§Ã£o durÃ¡vel; M4 deverÃ¡ provar idempotÃªncia no limite do provider de e-mail/WhatsApp, inclusive timeout apÃ³s o provider aceitar a solicitaÃ§Ã£o e antes de responder.
- **P1 #3 â€” false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. NÃ£o desaparece atÃ© testes AWS reais de M4 e operaÃ§Ã£o continuada.
- **P1 #4 â€” concorrÃªncia/estado obsoleto:** o milestone pode provar OCC e revalidaÃ§Ã£o entre renew/cancel/materializaÃ§Ã£o/dispatch. NÃ£o prova todas as corridas com envio externo, callbacks e correÃ§Ãµes futuras de M4.
- **P1 #5 â€” blast radius do GSI3:** um teste IAM real prova que roles tenant-facing nÃ£o consultam o Ã­ndice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa funÃ§Ã£o ainda tÃªm blast radius global. SÃ£o necessÃ¡rios alarmes, logs sem PII, limites de batch e revisÃ£o especÃ­fica desse handler.

TambÃ©m nÃ£o resolve carga extrema, restore real, regiÃ£o/LGPD, provider de notificaÃ§Ã£o, BFF de sessÃ£o nem aprovaÃ§Ã£o global da arquitetura operacional.

## Nota e riscos

**ConfianÃ§a: 8,8/10.**

Principais riscos:

1. O port atual da reconciliaÃ§Ã£o Ã© insuficiente para um handler autÃ´nomo; o GSI6 precisa ser concretamente consultÃ¡vel.
2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS Ã© indispensÃ¡vel.
4. O relay adiciona uma funÃ§Ã£o e semÃ¢ntica de publicaÃ§Ã£o que precisam de observabilidade e retenÃ§Ã£o/cleanup do outbox.
5. A idempotÃªncia externa continuarÃ¡ nÃ£o comprovada atÃ© M4; qualquer declaraÃ§Ã£o mais ampla de encerramento dos cinco P1 seria enganosa.
tokens used
38.889
# Proposta tÃ©cnica â€” milestone Runtime Real / G8

## 1. DecisÃ£o central

O milestone deve transformar o fluxo atual em runtime implantÃ¡vel sem contaminar os ports com tipos do AWS SDK:

```text
EventBridge Rule
      â”‚
      â–¼
ReminderProducer Lambda
      â”‚
      â”œâ”€ TransactWrite:
      â”‚    SCHEDULED â†’ CLAIMED
      â”‚    + registro durÃ¡vel DispatchOutbox
      â–¼
DynamoDB Stream / relay
      â”‚
      â–¼
ReminderDispatchQueue
      â”‚
      â–¼
ReminderDispatch Lambda
      â”‚
      â”œâ”€ revalida estado/versionamento
      â””â”€ TransactWrite:
           CLAIMED â†’ TRIGGERED
           + NotificationIntent
           + OutboxEvent
           + IdempotencyRecord
```

A gravaÃ§Ã£o durÃ¡vel intermediÃ¡ria Ã© necessÃ¡ria. Fazer `TransactWrite(CLAIMED)` e depois `SendMessage` diretamente no handler mantÃ©m uma janela de perda:

1. DynamoDB confirma o claim.
2. Lambda cai antes de enviar Ã  SQS.
3. A ocorrÃªncia fica `CLAIMED`, mas nÃ£o existe mensagem.

A reconciliaÃ§Ã£o diÃ¡ria nÃ£o fecha adequadamente essa janela: quando reverter o claim, a ocorrÃªncia poderÃ¡ estar fora do lookback de cinco minutos do producer. Portanto, fila e DLQ, isoladamente, nÃ£o bastam para fechar o P1 #1.

Minha recomendaÃ§Ã£o Ã© um registro `DispatchOutbox` gravado atomicamente com o claim e publicado por DynamoDB Streams. Uma alternativa aceitÃ¡vel Ã© EventBridge Pipes entre o stream e a SQS, desde que seja demonstrado que filtragem, transformaÃ§Ã£o do stream record e retries funcionam no ambiente AWS real. Eu prefiro um pequeno `DispatchOutboxRelay` Lambda porque oferece controle explÃ­cito de schema, observabilidade, partial batch failure e evoluÃ§Ã£o do contrato.

## 2. DependÃªncias

Adicionar como dependÃªncias de runtime:

```json
{
  "@aws-sdk/client-dynamodb": "versÃ£o exata",
  "@aws-sdk/lib-dynamodb": "versÃ£o exata",
  "@aws-sdk/client-sqs": "versÃ£o exata"
}
```

E como dependÃªncias de desenvolvimento:

```json
{
  "esbuild": "versÃ£o exata",
  "@types/aws-lambda": "versÃ£o exata",
  "testcontainers": "versÃ£o exata"
}
```

Escolhas:

- `DynamoDBClient`: transporte, configuraÃ§Ã£o de retry e endpoint.
- `DynamoDBDocumentClient`: objetos JavaScript, compatÃ­vel com os builders existentes.
- `SQSClient`: publicaÃ§Ã£o explÃ­cita pelo relay.
- `esbuild`: bundling determinÃ­stico dos entrypoints ESM para Node.js 20.
- VersÃµes exatas, nÃ£o ranges, mantendo lockfile e `npm ci`.

ConfiguraÃ§Ã£o do `DynamoDBDocumentClient`:

```ts
DynamoDBDocumentClient.from(client, {
  marshallOptions: {
    removeUndefinedValues: true,
    convertClassInstanceToMap: false,
  },
})
```

NÃ£o habilitaria conversÃµes numÃ©ricas silenciosas. Campos de versÃ£o precisam continuar sendo nÃºmeros e valores grandes devem ser avaliados explicitamente antes de usar `wrapNumbers`.

O cliente deve usar retry adaptativo ou standard com nÃºmero pequeno e explÃ­cito de tentativas, por exemplo trÃªs. O timeout total precisa caber com folga no timeout da Lambda; nÃ£o se deve deixar o SDK consumir todo o orÃ§amento da invocaÃ§Ã£o.

## 3. Estrutura dos adapters

```text
src/
  runtime/
    aws/
      dynamodb/
        client.ts
        errors.ts
        execute-transaction.ts
        identity-store-dynamodb.ts
        expiration-store-dynamodb.ts
        reminder-store-dynamodb.ts
        reminder-producer-store-dynamodb.ts
        reconciliation-candidate-source-dynamodb.ts
      sqs/
        dispatch-command-publisher.ts

    handlers/
      test-ping-handler.ts
      items-handler.ts
      reminders-handler.ts
      reminder-producer-handler.ts
      reminder-dispatch-handler.ts
      reminder-reconciliation-handler.ts
      dispatch-outbox-relay-handler.ts

    composition/
      identity.ts
      expiration.ts
      reminder.ts
```

Um adapter por port:

- `DynamoDbIdentityStore implements IdentityStore`
- `DynamoDbExpirationStore implements ExpirationStore`
- `DynamoDbReminderStore implements ReminderStore`
- `DynamoDbReminderProducerStore implements ReminderProducerStore`

NÃ£o criaria uma classe â€œuniversalâ€ pÃºblica implementando todos os ports. Isso permitiria injetar acidentalmente acesso ao GSI3 em cÃ³digo tenant-facing.

Pode haver helpers internos compartilhados para `GetCommand`, `PutCommand`, `UpdateCommand` e `TransactWriteCommand`, mas o mÃ©todo `queryGsi3` existe somente em `reminder-producer-store-dynamodb.ts`.

### Mapeamento das operaÃ§Ãµes

| Port | Comando AWS |
|---|---|
| `get` | `GetCommand`, `ConsistentRead: true` |
| `putIfAbsent` | `PutCommand` com condiÃ§Ã£o existente; retorna `false` apenas para falha condicional |
| `update` | `PutCommand` de overwrite, restrito aos usos jÃ¡ documentados pelo port |
| `transactWrite` | `TransactWriteCommand` usando diretamente os builders existentes |
| `queryGsi1` | `QueryCommand`, `IndexName: GSI1`, eventual consistency |
| `queryByItem` | `QueryCommand` na tabela base, `PK = :pk AND begins_with(SK, :occ)` e leitura consistente |
| `queryGsi3` | `QueryCommand`, `IndexName: GSI3`, somente no adapter privilegiado |

PaginaÃ§Ã£o deve ser completa por padrÃ£o. `limit` representa limite lÃ³gico total, nÃ£o apenas o `Limit` de uma Ãºnica pÃ¡gina.

### TraduÃ§Ã£o de erros

O adapter deve traduzuzir, sem esconder a causa:

- `ConditionalCheckFailedException` em `putIfAbsent` â†’ `false`.
- `TransactionCanceledException` â†’ erro reconhecÃ­vel por `isTransactionCanceled`.
- throttling, timeout, falha 5xx e indisponibilidade â†’ `AppError` retryable.
- validation error, item/schema invÃ¡lido â†’ erro terminal.
- `AccessDeniedException` â†’ erro terminal operacional e alarme; nÃ£o deve virar retry infinito.

Preservar `$metadata.requestId`, nome da operaÃ§Ã£o e causa no log, sem registrar item completo nem dados pessoais.

## 4. Lacuna da reconciliaÃ§Ã£o

O handler de reconciliaÃ§Ã£o atual nÃ£o consegue operar sozinho: `runReconciliation` recebe arrays de candidatos, mas `ReminderStore` nÃ£o oferece uma consulta que os produza.

Executar a Lambda diariamente com arrays vazios seria outro false-green.

O milestone precisa introduzir um port estreito, por exemplo:

```ts
interface ReconciliationCandidateSource {
  listExpiredClaims(input: PageInput): AsyncIterable<ReminderOccurrence>;
  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
}
```

O adapter consultaria o GSI6 ou o Ã­ndice efetivamente normativo. Isso Ã© integraÃ§Ã£o mÃ­nima necessÃ¡ria, nÃ£o redesenho do domÃ­nio.

Se o GSI6 ainda nÃ£o representa claims expirados e polÃ­ticas ativas de maneira consultÃ¡vel, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo estÃ¡ apenas agendado, nÃ£o funcional.

AlÃ©m da execuÃ§Ã£o diÃ¡ria de DST, recuperaÃ§Ã£o de claims precisa ter cadÃªncia curta. Recomendo:

- reconciliaÃ§Ã£o de claims expirados: a cada minuto ou cinco minutos;
- reconciliaÃ§Ã£o DST/materializaÃ§Ã£o: diÃ¡ria.

Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durÃ¡vel, a reconciliaÃ§Ã£o curta continua Ãºtil para reparar estados excepcionais.

## 5. Handlers Lambda

Cada arquivo em `src/runtime/handlers` Ã© um composition root:

1. valida variÃ¡veis de ambiente;
2. instancia clientes e adapters fora do handler para reuso em warm starts;
3. converte o evento AWS para os tipos internos;
4. invoca a lÃ³gica existente;
5. converte resultado/erro para o protocolo da origem;
6. registra mÃ©tricas e logs via `SecureLogger`.

Nenhuma lÃ³gica de negÃ³cio deve morar no wrapper Lambda.

VariÃ¡veis mÃ­nimas:

```text
TABLE_NAME
DISPATCH_QUEUE_URL
SHARD_CONFIG
PRODUCER_LOOKBACK_MINUTES
CLAIM_TTL_SECONDS
LOG_LEVEL
```

Para HTTP, os handlers reais devem interpretar API Gateway HTTP API v2, extrair claims jÃ¡ validadas pelo authorizer, construir `RequestContext` e mapear `AppError` para status HTTP consistente.

Para schedules, validar `source`, `detail-type` e um campo explÃ­cito `mode`. Evento inesperado Ã© terminal.

Para SQS, validar cada body contra `schemas/queues/reminder.dispatch.v1.json` antes de executar o domÃ­nio.

## 6. Bundling

Eu estenderia `ScopedLambdaFunction` com `entry` e `handlerExport`, mantendo-o como Ãºnico ponto de criaÃ§Ã£o de Lambda. Internamente, usaria `lambda.Code.fromAsset` com bundling `esbuild`, ou incorporaria a semÃ¢ntica de `NodejsFunction` sem permitir que stacks criem Lambdas por fora do construct.

Exemplo conceitual:

```ts
new ScopedLambdaFunction(this, "ReminderDispatch", {
  entry: "src/runtime/handlers/reminder-dispatch-handler.ts",
  handlerExport: "handler",
  runtime: lambda.Runtime.NODEJS_20_X,
  access: [...],
});
```

ConfiguraÃ§Ã£o:

- target `node20`;
- platform `node`;
- formato ESM compatÃ­vel com o projeto;
- sourcemap externo;
- minificaÃ§Ã£o desabilitada inicialmente para stack traces auditÃ¡veis;
- arquivo de lock explÃ­cito;
- bundle independente por funÃ§Ã£o;
- AWS SDK v3 incluÃ­do no bundle para versÃ£o reproduzÃ­vel.

NÃ£o confiaria na versÃ£o do SDK fornecida pela runtime Lambda.

Testes de infra devem falhar se qualquer funÃ§Ã£o continuar usando inline code ou contiver o corpo 501.

## 7. SQS e DLQ

### Recursos

```text
ExpirationTracker-ReminderDispatch
ExpirationTracker-ReminderDispatch-DLQ
```

Os nomes fÃ­sicos podem receber stage, por exemplo:

```text
expiration-tracker-dev-reminder-dispatch
expiration-tracker-dev-reminder-dispatch-dlq
```

ConfiguraÃ§Ã£o proposta:

| Propriedade | Queue | DLQ |
|---|---:|---:|
| Tipo | Standard | Standard |
| RetenÃ§Ã£o | 4 dias | 14 dias |
| Long polling | 20 s | n/a |
| `maxReceiveCount` | 5 | n/a |
| Criptografia | SQS managed ou KMS do ambiente | igual |
| Visibility timeout | â‰¥ 6 Ã— timeout da Lambda | n/a |

A fila deve ser Standard, pois OCC e idempotÃªncia jÃ¡ sÃ£o necessÃ¡rios independentemente da ordenaÃ§Ã£o; FIFO adicionaria throughput, group-id e dedup-window sem eliminar a necessidade de idempotÃªncia durÃ¡vel.

### Event source mapping

- batch size inicial: `10`;
- batching window: `0â€“1 s`;
- `reportBatchItemFailures: true`;
- reserved concurrency da funÃ§Ã£o: `10`;
- maximum concurrency do event source mapping: `10`;
- `bisectBatchOnError` nÃ£o se aplica a SQS;
- payload schema validado item a item.

Resposta:

```ts
return {
  batchItemFailures: failures.map(({ messageId }) => ({
    itemIdentifier: messageId,
  })),
};
```

ClassificaÃ§Ã£o:

- sucesso, duplicata ou estado jÃ¡ avanÃ§ado â†’ ACK;
- erro retryable â†’ incluir em `batchItemFailures`;
- poison message/schema invÃ¡lido â†’ terminal, mas precisa chegar Ã  DLQ.

Para poison message, hÃ¡ duas polÃ­ticas vÃ¡lidas. Eu recomendo retornar falha atÃ© `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observÃ¡vel. Para evitar custo inÃºtil, registrar a primeira detecÃ§Ã£o e emitir mÃ©trica por receive count. NÃ£o deletar manualmente nem enviar diretamente Ã  DLQ.

O relay do outbox tambÃ©m deve usar partial batch failure no DynamoDB Stream. Um registro sÃ³ Ã© marcado publicado depois de `SendMessage` confirmado; duplicaÃ§Ã£o entre envio e marcaÃ§Ã£o Ã© esperada e absorvida pelo dispatch idempotente.

## 8. EventBridge

Escolho **EventBridge Rule legado com `ScheduleExpression`**, nÃ£o EventBridge Scheduler, para estas cadÃªncias fixas.

Motivos:

- jÃ¡ Ã© a tecnologia especificada no escopo;
- `rate(1 minute)` Ã© simples e suficiente;
- integraÃ§Ã£o direta com Lambda e retry policy madura;
- nÃ£o hÃ¡ milhÃµes de schedules por tenant;
- flexible time window nÃ£o existe em Scheduled Rule e nÃ£o Ã© necessÃ¡ria para o producer.

Regras:

```text
ReminderProducerSchedule:
  rate(1 minute)

ReminderClaimReconciliationSchedule:
  rate(1 minute) ou rate(5 minutes)

ReminderDstReconciliationSchedule:
  cron diÃ¡rio em UTC
```

O evento deve carregar:

```json
{
  "mode": "PRODUCE"
}
```

ou:

```json
{
  "mode": "CLAIMS"
}
```

ou:

```json
{
  "mode": "DST"
}
```

Reserved concurrency:

- producer: `2`;
- reconciliation: `1`;
- dispatch: `10`.

Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contÃ­nuas precisam de alarme por invocaÃ§Ãµes com erro e por scheduler lag.

### Por que nÃ£o Scheduler

EventBridge Scheduler seria preferÃ­vel se houvesse schedules individuais, timezone por schedule, execuÃ§Ã£o Ãºnica ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.

Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliaÃ§Ã£o diÃ¡ria, uma janela flexÃ­vel seria aceitÃ¡vel, mas nÃ£o justifica misturar dois mecanismos.

## 9. Testes

### Camada 1 â€” testes rÃ¡pidos

Continuam executando em todo PR:

- unitÃ¡rios do domÃ­nio;
- contratos de schema;
- CDK assertions;
- teste do bundle de cada entrypoint;
- teste que proÃ­be `fromInline` nos handlers de produÃ§Ã£o;
- teste estrutural que assegura que somente producer tem ARN de GSI3.

### Camada 2 â€” integraÃ§Ã£o em containers

Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade especÃ­fica.

DynamoDB Local, sozinho, nÃ£o testa:

- redrive SQS;
- DLQ;
- event source mapping;
- EventBridge;
- interaÃ§Ã£o DynamoDB â†’ relay â†’ SQS.

LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suÃ­te, portas dinÃ¢micas e cleanup previsÃ­vel.

Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.

Casos obrigatÃ³rios:

1. CondiÃ§Ã£o de OCC concorrente: apenas um writer vence.
2. Claim + dispatch outbox sÃ£o atÃ´micos.
3. Falha do relay preserva o registro para retry.
4. Duas entregas SQS criam um Ãºnico `NotificationIntent`.
5. Falha em um item do batch nÃ£o repete os itens bem-sucedidos.
6. Poison message chega Ã  DLQ apÃ³s cinco receives.
7. Claim expirado Ã© recuperado e volta a produzir trabalho.
8. Renew/cancel concorrente faz dispatch cancelar por versÃ£o obsoleta.
9. Query GSI3 encontra mÃºltiplos tenants, mas o producer nunca mistura o contexto reconstruÃ­do.
10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.

Para timeout real de dependÃªncia, usaria um proxy de rede controlÃ¡vel, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexÃ£o acima do timeout configurado e verifica:

- erro classificado como retryable;
- mensagem nÃ£o reconhecida como sucesso;
- retry/redrive;
- nenhuma transaÃ§Ã£o parcial.

Isso testa falha de transporte real, nÃ£o um `mockRejectedValue`.

### Camada 3 â€” sandbox AWS efÃªmero

LocalStack nÃ£o prova semÃ¢ntica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 nÃ£o deve ser aprovado somente com containers.

Criaria uma suÃ­te curta contra uma stack AWS efÃªmera:

- deploy em conta/regiÃ£o de teste;
- TTL mÃ¡ximo da stack e tags de cleanup;
- seed de poucos itens;
- invocaÃ§Ã£o do producer;
- observaÃ§Ã£o da mensagem e do dispatch;
- poison message atÃ© DLQ;
- teste negativo assumindo a role de uma Lambda tenant-facing e tentando `Query` em GSI3;
- validaÃ§Ã£o de schedule por invocaÃ§Ã£o manual com o mesmo target, sem esperar o relÃ³gio;
- destruiÃ§Ã£o da stack ao final.

CadÃªncia:

- containers em todo PR;
- sandbox AWS no merge para `develop` ou workflow manual obrigatÃ³rio do milestone;
- teste completo de schedule real em workflow noturno, pois esperar ticks torna PRs lentos e sujeitos a jitter.

O artefato da execuÃ§Ã£o AWS â€” CloudWatch request IDs, mÃ©tricas, ARN da stack e resultados â€” deve ser preservado como evidÃªncia do gate.

## 10. Fault injection exigida

| Falha | InjeÃ§Ã£o | EvidÃªncia esperada |
|---|---|---|
| Timeout DynamoDB/SQS | Toxiproxy interrompe conexÃ£o | erro retryable, retry, sem estado parcial |
| Queda apÃ³s claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
| Queda apÃ³s `SendMessage` | falhar antes de marcar outbox | possÃ­vel duplicata, um Ãºnico efeito lÃ³gico |
| Poison message | body invÃ¡lido real em SQS | cinco receives e mensagem na DLQ |
| Falha parcial de batch | uma mensagem invÃ¡lida entre vÃ¡lidas | somente invÃ¡lida retorna em `batchItemFailures` |
| Renew concorrente | atualizar versÃ£o apÃ³s claim | dispatch cancela stale, sem intent incorreto |
| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
| Duplo producer | duas invocaÃ§Ãµes simultÃ¢neas | um claim vence por condiÃ§Ã£o |
| Tentativa GSI3 nÃ£o autorizada | role real tenant-facing | `AccessDenied`, nenhum dado retornado |

## 11. CritÃ©rios de saÃ­da do milestone

O milestone sÃ³ pode alegar fechamento operacional de G8 quando houver:

- zero handlers 501;
- bundles reais sintetizados;
- adapters reais cobrindo todos os ports;
- reconciliaÃ§Ã£o alimentada por consultas reais, nÃ£o arrays vazios;
- claim e publicaÃ§Ã£o conectados por mecanismo durÃ¡vel;
- fila, DLQ e redrive sintetizados e exercitados;
- partial batch failure comprovado;
- timeout, poison message e redrive exercitados;
- teste AWS negativo de GSI3 com role real;
- teste concorrente renew/cancel/dispatch;
- evidÃªncia registrada da execuÃ§Ã£o em sandbox AWS.

## 12. O que este milestone nÃ£o resolve

â€œG8 fechadoâ€ nÃ£o significa que os cinco P1 desapareceram integralmente.

- **P1 #1 â€” recuperaÃ§Ã£o assÃ­ncrona:** fecha o trecho producer â†’ dispatch se houver outbox/relay e reconciliaÃ§Ã£o consultÃ¡vel. NÃ£o fecha pipelines futuros de Notification.
- **P1 #2 â€” idempotÃªncia no efeito externo:** nÃ£o pode ser provada ainda. `NotificationIntent` Ã© apenas uma intenÃ§Ã£o durÃ¡vel; M4 deverÃ¡ provar idempotÃªncia no limite do provider de e-mail/WhatsApp, inclusive timeout apÃ³s o provider aceitar a solicitaÃ§Ã£o e antes de responder.
- **P1 #3 â€” false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. NÃ£o desaparece atÃ© testes AWS reais de M4 e operaÃ§Ã£o continuada.
- **P1 #4 â€” concorrÃªncia/estado obsoleto:** o milestone pode provar OCC e revalidaÃ§Ã£o entre renew/cancel/materializaÃ§Ã£o/dispatch. NÃ£o prova todas as corridas com envio externo, callbacks e correÃ§Ãµes futuras de M4.
- **P1 #5 â€” blast radius do GSI3:** um teste IAM real prova que roles tenant-facing nÃ£o consultam o Ã­ndice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa funÃ§Ã£o ainda tÃªm blast radius global. SÃ£o necessÃ¡rios alarmes, logs sem PII, limites de batch e revisÃ£o especÃ­fica desse handler.

TambÃ©m nÃ£o resolve carga extrema, restore real, regiÃ£o/LGPD, provider de notificaÃ§Ã£o, BFF de sessÃ£o nem aprovaÃ§Ã£o global da arquitetura operacional.

## Nota e riscos

**ConfianÃ§a: 8,8/10.**

Principais riscos:

1. O port atual da reconciliaÃ§Ã£o Ã© insuficiente para um handler autÃ´nomo; o GSI6 precisa ser concretamente consultÃ¡vel.
2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS Ã© indispensÃ¡vel.
4. O relay adiciona uma funÃ§Ã£o e semÃ¢ntica de publicaÃ§Ã£o que precisam de observabilidade e retenÃ§Ã£o/cleanup do outbox.
5. A idempotÃªncia externa continuarÃ¡ nÃ£o comprovada atÃ© M4; qualquer declaraÃ§Ã£o mais ampla de encerramento dos cinco P1 seria enganosa.

OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a01b30-3691-7e21-b9f5-05a55d9e2722
--------
user
Continuando o protocolo de nota cega Claude<->Codex (AGENTS.md secao 4, expiration-tracker) para o milestone M3.5 (runtime real do Reminder Engine / fechamento de G8). Esta e a Rodada 2.

Sua proposta independente da Rodada 1 esta em docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt (voce ja a escreveu). A proposta independente da Claude na Rodada 1 esta em docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md - leia agora, e leia tambem a critica da Claude a SUA proposta em docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md (mesmo diretorio).

Resumo do que a critica da Claude faz: (1) concorda que a proposta do Codex identificou dois gaps reais que a proposta da Claude nao tinha visto - falta de durabilidade entre claim (TransactWrite) e SendMessage direto (adota outbox+relay via DynamoDB Streams), e reconciliacao sem fonte real de candidatos (GSI6 nao consultavel ainda, precisa entrar no escopo do milestone via um port novo estreito); (2) concorda com sandbox AWS efemero como terceira camada de teste, cadencia curta para reconciliacao de claims expirados separada de DST diario, e Toxiproxy para fault injection de timeout; (3) diverge em tres pontos: (a) usar DynamoDB Local (nao LocalStack) para a suite rapida que so prova paridade adapter-vs-fake em memoria, reservando LocalStack/Testcontainers para a camada que precisa de SQS+Streams+EventBridge reais - argumento: menos peso/determinismo para o que nao precisa de SQS; (b) EventBridge Scheduler (nao Rule legado) porque implementation-blueprint.md linha 162 ja nomeia "EventBridge Scheduler" explicitamente para o producer e essa e uma decisao Type 1 ja aprovada (Claude 9.20/Codex 9.2) no blueprint, nao deveria ser reaberta neste milestone sem necessidade tecnica forte; (c) reusar o OutboxEvent/shared/outbox/outbox.ts ja existente (mesma TransactWriteItems do claim, com eventType novo) para o registro de dispatch outbox, em vez de um mecanismo de outbox paralelo novo.

Escreva sua treplica: (1) responda especificamente aos 3 pontos de divergencia (a, b, c acima) - concorda, discorda com justificativa tecnica, ou propoe uma terceira opcao; (2) confirme se os dois gaps que voce identificou (outbox/relay, GSI6/reconciliation candidate source) continuam sua leitura correta depois de ver a proposta original da Claude; (3) de uma nota de confianca 0-10 na proposta CONVERGENTE resultante (nao mais na sua proposta isolada) considerando os ajustes descritos acima. Se a nota for abaixo de 9.0, aponte exatamente o que falta para chegar a 9.0.

Responda em portugues, direto ao ponto, sem preambulo.

codex
Vou confrontar os trÃªs documentos e a decisÃ£o jÃ¡ aprovada no blueprint, preservando a independÃªncia da nota cega.
2026-08-19T18:02:27.586480Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt'; Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md'; Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md'; Select-String -Path 'docs/architecture/implementation-blueprint.md' -Pattern 'EventBridge Scheduler|GSI6|Reminder|outbox' -Context 2,3"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt'; Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md'; Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md'; Select-String -Path 'docs/architecture/implementation-blueprint.md' -Pattern 'EventBridge Scheduler|GSI6|Reminder|outbox' -Context 2,3" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt'; Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md'; Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md'; Select-String -Path 'docs/architecture/implementation-blueprint.md' -Pattern 'EventBridge Scheduler|GSI6|Reminder|outbox' -Context 2,3"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 'docs/architecture/implementation-blueprint.md' -Pattern 'EventBridge Scheduler|GSI6|Reminder|outbox' -Context 2,3" in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-19T18:02:33.750758Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 'docs/architecture/implementation-blueprint.md' -Pattern 'EventBridge Scheduler|GSI6|Reminder|outbox' -Context 2,3"` rejected: blocked by policy
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 'docs/architecture/implementation-blueprint.md' -Pattern 'EventBridge Scheduler|GSI6|Reminder|outbox' -Context 2,3"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 3 "EventBridge Scheduler|GSI6|Reminder|outbox" docs/architecture/implementation-blueprint.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 393ms:
11-1. **Scheduler GSI3 corrigido** â€” erro tÃ©cnico real presente nas duas propostas independentes: uma partition key `TENANT#t#DUE#yyyyMMddHHmm#NN` nÃ£o Ã© consultÃ¡vel pelo producer do minuto sem jÃ¡ saber quais tenants tÃªm ocorrÃªncia naquele minuto (`Query` exige PK exata). Corrigido em todo o documento para `GSI3PK=DUE#yyyyMMddHHmm#NN` / `GSI3SK=TENANT#tenantId#OCCURRENCE#occurrenceId` â€” exceÃ§Ã£o documentada e justificada Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`" de `data-model.md` Â§3, com as salvaguardas da Â§9.2 abaixo. Esta Ã© uma mudanÃ§a Type 1 sobre `data-model.md` e deve ser ratificada lÃ¡ (ver `decisions-log.md`).
12-2. **CSP MVP usa hashes estÃ¡ticos, nÃ£o nonce dinÃ¢mico** â€” o frontend Ã© SPA estÃ¡tica servida por CloudFront sem compute de borda no Day 0; nonce por resposta exigiria geraÃ§Ã£o dinÃ¢mica de HTML, nÃ£o disponÃ­vel nesse desenho. DecisÃ£o explÃ­cita: eliminar inline scripts/styles ou usar CSP hash-based estÃ¡tico.
13-3. **E-mail nÃ£o Ã© sujeito a kill switch** â€” decisÃ£o explÃ­cita (ausente nas duas propostas Rodada 1): `AppConfig` cobre `AI`, `OCR`, `WHATSAPP` (`architecture-fase3-consolidada.md` Â§14); e-mail Ã© o canal padrÃ£o, sempre ativo, sem toggle de emergÃªncia dedicado.
14:4. **CritÃ©rio de aceite do Reminder Engine amarrado quantitativamente** aos trÃªs cenÃ¡rios de drenagem do pico extremo jÃ¡ modelados em `capacity-model.md` (16.667/3.333/278 agendamentos/s), nÃ£o apenas "teste de carga" genÃ©rico.
15-5. **Mapeamento para requisitos/capacity-model** adicionado como Â§25 (ausente nas duas propostas Rodada 1).
16-6. **ApÃªndice de referÃªncia rÃ¡pida de Lambdas** (tabela funÃ§Ã£oÃ—gatilhoÃ—concorrÃªncia/DLQ) adicionado como Â§26, complementando o detalhamento por seÃ§Ã£o.
17-
--
27-2. Toda entidade mutÃ¡vel usa optimistic concurrency control com `version` e `ConditionExpression`.
28-3. Leituras crÃ­ticas de autorizaÃ§Ã£o, alteraÃ§Ã£o, cancelamento, disparo e aplicaÃ§Ã£o de extraÃ§Ã£o usam leitura fortemente consistente na chave base. GSI nunca Ã© autoridade para decisÃ£o crÃ­tica.
29-4. Eventos e comandos assÃ­ncronos usam envelope comum versionado; payloads permanecem especÃ­ficos por evento/canal.
30:5. OperaÃ§Ãµes crÃ­ticas que alteram estado e exigem publicaÃ§Ã£o usam outbox transacional.
31-6. Consumidores assÃ­ncronos sÃ£o idempotentes e toleram duplicaÃ§Ã£o, reordenaÃ§Ã£o e entrega tardia.
32-7. MudanÃ§as de schema, evento e workflow Step Functions usam expand/contract.
33-8. Dados sensÃ­veis sÃ£o redigidos antes de logs, traces, eventos operacionais e mensagens de DLQ.
--
90-      api/
91-    dynamodb/
92-    idempotency/
93:    outbox/
94-    observability/
95-    security/
96-    config/
97-    errors/
98-  workers/
99:    outbox-publisher/
100-    reminder-producer/
101-    reminder-dispatch/
102-    reminder-reconciliation/
--
157-
158-| FunÃ§Ã£o | Gatilho | Responsabilidade |
159-|---|---|---|
160:| `ApiHandler` | API Gateway HTTP API | Rotas sÃ­ncronas dos mÃ³dulos Identity, Expiration, Reminder, Notification, Document e Audit |
161:| `OutboxPublisher` | DynamoDB Streams + agendamento | Publicar outbox no EventBridge e confirmar publicaÃ§Ã£o |
162:| `ReminderProducer` | EventBridge Scheduler, 1 minuto | Consultar GSI3 nos shards do minuto e enviar comandos para SQS |
163:| `ReminderDispatchWorker` | `ReminderDispatchQueue` | Revalidar ocorrÃªncia/item e produzir `NotificationIntent` |
164:| `ReminderReconciliationWorker` | EventBridge diÃ¡rio | Recalcular janela de sete dias e reparar ocorrÃªncias |
165-| `NotificationRouterWorker` | EventBridge/SQS | Aplicar entitlement, quiet hours e opt-out; rotear por canal |
166-| `EmailDeliveryWorker` | `EmailDeliveryQueue` | Enviar e-mail e registrar tentativa |
167-| `WhatsAppDeliveryWorker` | `WhatsAppDeliveryQueue` | Enviar WhatsApp e registrar tentativa |
--
175-| `BedrockExtractionTaskHandler` | Step Functions | ExtraÃ§Ã£o via LLM, condicionada a kill switch `AI`/`OCR` |
176-| `ExtractionValidationTaskHandler` | Step Functions | ValidaÃ§Ã£o de schema, comparaÃ§Ã£o entre extratores, persistÃªncia de `ExtractedField` |
177-| `UploadSlotReconciliationWorker` | EventBridge agendado | Restituir slots abandonados com transaÃ§Ã£o idempotente |
178:| `RetentionReconciliationWorker` | EventBridge agendado | Processar GSI6 para retenÃ§Ã£o, purga e pendÃªncias |
179-| `DlqRedriveWorker` | invocaÃ§Ã£o manual/runbook | Redrive controlado, auditado e limitado |
180-
181-**Decidido (Â§23.1): Textract, parser determinÃ­stico e Bedrock sÃ£o Lambdas separadas desde o primeiro release**, nÃ£o um `ExtractionTaskHandler` Ãºnico consolidado depois. Justificativa: o sandbox de PDF (lacuna #2 do threat model, limites em Â§12.4/Â§23.1) exige isolamento de IAM/rede que uma funÃ§Ã£o combinada dilui â€” separar desde o dia 1 Ã© mais simples de auditar do que consolidar e depois desmembrar sob pressÃ£o de produÃ§Ã£o. Cada uma das trÃªs superfÃ­cies de risco isoladas (OCR gerenciado, parsing nÃ£o confiÃ¡vel em sandbox, chamada a LLM) tem sua prÃ³pria funÃ§Ã£o nomeada com `ScopedLambdaFunction` (Â§17.1) â€” nÃ£o Ã© "um estado, uma funÃ§Ã£o": estados de orquestraÃ§Ã£o pura (`NeedsBedrock?`) sÃ£o Choice states nativos sem Lambda, e estados que compartilham a mesma superfÃ­cie de IAM/dados (validaÃ§Ã£o de schema, comparaÃ§Ã£o entre extratores, persistÃªncia, fail-closed) compartilham `ExtractionValidationTaskHandler`. O mapeamento estadoâ†’handler completo estÃ¡ em Â§12.5.
--
363-  "publishAttempts": 0,
364-  "nextAttemptAt": "2026-08-19T14:03:22.481Z",
365-  "createdAt": "2026-08-19T14:03:22.481Z",
366:  "GSI6PK": "RECON#OUTBOX#PENDING",
367:  "GSI6SK": "2026-08-19T14:03:22.481Z#evt_01"
368-}
369-```
370-
371-`OutboxPublisher`:
372-
373:1. recebe stream ou busca pendÃªncias em GSI6;
374-2. adquire lease condicional por `eventId`;
375-3. publica uma entrada por `PutEvents`;
376-4. considera falha individual de `PutEvents`;
--
378-6. ao erro, mantÃ©m `PENDING`, incrementa tentativas e aplica backoff;
379-7. nÃ£o assume exactly-once; consumidores deduplicam por `eventId`.
380-
381:O sweeper periÃ³dico consulta GSI6 e cobre eventos perdidos apÃ³s a retenÃ§Ã£o do Streams.
382-
383----
384-
--
529-- dashboard: GSI1 por status/dueDate, seguido de verificaÃ§Ã£o base quando houver aÃ§Ã£o;
530-- filtro: GSI2 por assignee/category;
531-- polÃ­ticas/ocorrÃªncias/documentos: mesmo agregado do item;
532:- renovaÃ§Ã£o: transaÃ§Ã£o entre item, idempotÃªncia, auditoria/outbox quando aplicÃ¡vel.
533-
534-### 8.3 MudanÃ§a de vencimento
535-
--
538-1. lÃª item consistentemente;
539-2. autoriza;
540-3. valida `expectedVersion`;
541:4. em `TransactWriteItems`, atualiza item e cria outbox `ItemDueDateChanged`;
542-5. nÃ£o tenta alterar ocorrÃªncias diretamente na requisiÃ§Ã£o HTTP.
543-
544-Evento:
--
562-}
563-```
564-
565:Consumer do Reminder cancela ocorrÃªncias antigas por condiÃ§Ã£o de versÃ£o (`itemVersion` divergente da atual invalida a ocorrÃªncia â€” nenhuma transaÃ§Ã£o tenta cancelar um nÃºmero ilimitado de ocorrÃªncias de uma sÃ³ vez, o que estouraria os limites de `TransactWriteItems`, 100 itens/4 MB) e materializa as novas em lotes idempotentes. Evento repetido produz o mesmo resultado.
566-
567-### 8.4 Aceite
568-
--
574-
575----
576-
577:## 9. MÃ³dulo Reminder
578-
579-### 9.1 ServiÃ§os
580-
581-```ts
582:interface ReminderPolicyService {
583-  putPolicy(
584-    ctx: RequestContext,
585-    itemId: string,
586:    policy: ReminderRule,
587-    expectedItemVersion: number
588:  ): Promise<ReminderPolicy>;
589-}
590-
591:interface ReminderMaterializer {
592-  materialize(input: {
593-    tenantId: string;
594-    itemId: string;
--
604-
605-### 9.2 MaterializaÃ§Ã£o
606-
607:Cada `ReminderOccurrence` persiste:
608-
609-```json
610-{
--
626-```
627-
628-**CorreÃ§Ã£o de convergÃªncia (Rodada 3)**: a chave original avaliada em ambas as propostas independentes (`GSI3PK=TENANT#t#DUE#...`) nÃ£o Ã© consultÃ¡vel pelo producer do minuto â€” `Query` no DynamoDB exige a partition key exata, e o producer nÃ£o sabe a priori quais tenants tÃªm ocorrÃªncia em `M`. Corrigido para uma **chave global** (`GSI3PK=DUE#yyyyMMddHHmm#NN`, sem `tenantId`), com `tenantId` preservado no `GSI3SK` e no item base para reconstruÃ§Ã£o segura do contexto. Esta Ã© uma **exceÃ§Ã£o explÃ­cita e documentada** Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`" de `data-model.md` Â§3 â€” Type 1, deve ser ratificada em `data-model.md`/`decisions-log.md`, nÃ£o apenas aqui. Salvaguardas obrigatÃ³rias:
629:- IAM do Ã­ndice restrito exclusivamente ao `ReminderProducer` â€” nenhuma rota tenant-facing tem permissÃ£o de `Query` no GSI3;
630-- projeÃ§Ã£o do GSI mÃ­nima (sem conteÃºdo de negÃ³cio sensÃ­vel, sÃ³ o necessÃ¡rio para produzir o comando de dispatch);
631-- teste de isolamento automatizado provando que nenhuma API tenant-facing consegue consultar este Ã­ndice, mesmo indiretamente;
632-- dimensionamento de shards por pico **global** (todos os tenants), nÃ£o por tenant.
--
641-
642-### 9.3 Producer por minuto
643-
644:Para minuto `M`, `ReminderProducer`:
645-
646-1. obtÃ©m configuraÃ§Ã£o de shards (`N`, `shardFnVersion` ativo(s));
647-2. consulta GSI3 pelos `N` shards globais de `M` **e de uma janela de lookback** (ex. `[M-5min, M]`) para cobrir minutos cuja publicaÃ§Ã£o falhou â€” sem lookback, "o prÃ³ximo tick reprocessa" Ã© falso, pois o prÃ³ximo tick consulta uma chave diferente (`M+1`), deixando `M` definitivamente para trÃ¡s em caso de falha;
--
678-- horÃ¡rio deve estar dentro da tolerÃ¢ncia;
679-- polÃ­tica ainda deve existir.
680-
681:Se vÃ¡lido, transaÃ§Ã£o atÃ´mica (`TransactWriteItems`, corrige a lacuna de durabilidade apontada na crÃ­tica cruzada â€” antes o evento `ReminderTriggered` era tratado como "reconstruÃ­vel" sem outbox, o que podia perder ou duplicar o disparo):
682-
683-- cria `NotificationIntent`;
684-- muda ocorrÃªncia para `TRIGGERED`;
685-- registra idempotÃªncia (`tenantId|occurrenceId` â€” consumidores de `NotificationIntentCreated` deduplicam por esta chave);
686:- cria outbox `NotificationIntentCreated` na mesma transaÃ§Ã£o.
687-
688-O dispatcher sÃ³ publica no EventBridge apÃ³s a transaÃ§Ã£o persistir com sucesso; falha de publicaÃ§Ã£o (nÃ£o de persistÃªncia) Ã© recuperada pelo `OutboxPublisher`/sweeper (Â§5.3), nunca reprocessada como se a ocorrÃªncia nÃ£o tivesse sido tratada.
689-
--
710-- teste de transiÃ§Ã£o DST cobre horÃ¡rio inexistente e ambÃ­guo;
711-- alteraÃ§Ã£o de item invalida ocorrÃªncia antiga;
712-- reconciliaÃ§Ã£o repara ocorrÃªncia ausente e cancela excedente;
713:- quatro shards globais sÃ£o consultados e monitorados (mÃ©trica prÃ³pria do producer, jÃ¡ que CloudWatch nÃ£o expÃµe consumo por valor individual de partition key â€” `ConsumedReadCapacity` Ã© por tabela/Ã­ndice, nÃ£o por shard lÃ³gico; a granularidade por shard vem de EMF/custom metrics emitidas pelo prÃ³prio `ReminderProducer`);
714-- runbook de expansÃ£o de shards testado, incluindo leitura simultÃ¢nea de geraÃ§Ãµes antigas e novas de `shardFnVersion` sem tornar ocorrÃªncias jÃ¡ materializadas invisÃ­veis;
715-- atraso do scheduler tem SLO e alarme;
716-- **critÃ©rio de aceite quantitativo (convergÃªncia Rodada 3)**: teste de carga sintÃ©tico cobrindo os trÃªs cenÃ¡rios de drenagem do pico extremo jÃ¡ modelados em `capacity-model.md` â€” drenar 1.000.000 de ocorrÃªncias em 1 min (~16.667 agendamentos/s), 5 min (~3.333/s) e 60 min (~278/s) â€” sem perda de ocorrÃªncia (medida por reconciliaÃ§Ã£o) em pelo menos um dos trÃªs cenÃ¡rios, com o SLO de drenagem aceitÃ¡vel formalizado em `slo.md` (UNK-CAP-006) antes deste teste ser executado como gate de M7.
--
926-  },
927-  "processingStatus": "PENDING",
928-  "version": 1,
929:  "GSI6PK": "RECON#WEBHOOK#PENDING",
930:  "GSI6SK": "2026-09-10T12:01:02Z#provider_event_987"
931-}
932-```
933-
--
1142-2. autoriza;
1143-3. exige `expectedItemVersion`, `expectedDocumentVersion` e `expectedFieldVersion`;
1144-4. em transaÃ§Ã£o, marca campo confirmado e altera item;
1145:5. se alterar vencimento, cria outbox `ItemDueDateChanged`.
1146-
1147-### 12.6 ExclusÃ£o concorrente
1148-
--
1151-- muda estado lÃ³gico com condiÃ§Ã£o de versÃ£o;
1152-- invalida novas aplicaÃ§Ãµes de extraÃ§Ã£o;
1153-- workflow em andamento verifica versÃ£o/estado antes de persistir;
1154:- purga fÃ­sica posterior usa GSI6;
1155-- cÃ³pia ou resultado tardio nÃ£o ressuscita documento.
1156-
1157-### 12.7 Aceite
--
1210-
1211-Eventos sÃ£o append-only. A role de aplicaÃ§Ã£o possui `PutItem` condicional, nÃ£o `UpdateItem`/`DeleteItem`, para itens de auditoria â€” mas essa restriÃ§Ã£o de IAM garante sÃ³ a imutabilidade, nÃ£o a durabilidade da chegada do evento.
1212-
1213:**CorreÃ§Ã£o de convergÃªncia (Rodada 3)**: "consumidor Ãºnico, alarme direto, sem DLQ" nÃ£o Ã© estratÃ©gia de recuperaÃ§Ã£o â€” alarmar nÃ£o recupera eventos perdidos apÃ³s o perÃ­odo de retry do EventBridge. O `audit-consumer` tem DLQ dedicada (mensagens de auditoria nunca sÃ£o descartadas silenciosamente), com: retry documentado, redrive controlado (mesmo runbook de DLQ das demais filas, Â§22.2), e reconciliaÃ§Ã£o periÃ³dica que compara a contagem/hash de eventos de domÃ­nio crÃ­ticos publicados no outbox contra os `AuditEvent` correspondentes gravados, alertando divergÃªncia.
1214-
1215-AÃ§Ãµes crÃ­ticas incluem login/logout, negativa de autorizaÃ§Ã£o, item create/update/delete/renew, mudanÃ§a de vencimento, upload, malware, extraÃ§Ã£o confirmada/rejeitada, configuraÃ§Ã£o de canal, envio, webhook aplicado e redrive.
1216-
--
1258-### 14.2 MÃ©tricas e alarmes
1259-
1260-- API: latÃªncia, 4xx/5xx, auth denied, quota denied;
1261:- Reminder: producer lag, ocorrÃªncias por shard, stale occurrences;
1262-- SQS: oldest message age, visible count, DLQ age;
1263-- Notification: attempts, acceptance, bounce, provider throttling;
1264-- Document: malware timeout, unsupported, extraction duration/confidence/divergence;
--
1381-Cada funÃ§Ã£o declara:
1382-
1383-```ts
1384:new ScopedLambdaFunction(this, "ReminderDispatch", {
1385-  handler: "...",
1386-  access: [
1387:    tableAccess.readWriteKeys("ReminderOccurrence", "ExpirationItem"),
1388-    tableAccess.create("NotificationIntent"),
1389-    tableAccess.create("Idempotency"),
1390-    tableAccess.create("OutboxEvent"),
--
1453-             |
1454-             v
1455-Application
1456:(API monolith, outbox, reminder, notification, webhook)
1457-             |
1458-             v
1459-Document Processing
--
1481-9. Step Functions e tarefas de documento.
1482-10. dashboards, alarmes e canÃ¡rios.
1483-11. smoke tests com eventos sintÃ©ticos.
1484:12. habilitaÃ§Ã£o gradual: outbox â†’ webhook â†’ reminder â†’ notification â†’ documento.
1485-13. frontend por diretÃ³rio/hash imutÃ¡vel e troca atÃ´mica de origem/manifest.
1486-14. WAF antes do lanÃ§amento pÃºblico de produÃ§Ã£o.
1487-
--
1510-- padrÃ£o de erros;
1511-- idempotÃªncia;
1512-- OCC;
1513:- outbox;
1514-- pipeline supply-chain.
1515-
1516-DependÃªncia: nenhuma.
--
1542-- dashboard GSI1;
1543-- OCC;
1544-- audit append-only;
1545:- `ItemDueDateChanged` por outbox.
1546-
1547-Depende de M1.
1548-
1549-SaÃ­da: item gerenciado end-to-end sem reminders.
1550-
1551:### M3 â€” Reminder Engine
1552-
1553-Entregas:
1554-
--
1560-- reconciliaÃ§Ã£o DST;
1561-- runbook de shards.
1562-
1563:Depende de M2 e outbox de M0.
1564-
1565-SaÃ­da: `NotificationIntent` criado deterministicamente, sem delivery externo.
1566-
--
1610-- confirmaÃ§Ã£o transacional;
1611-- kill switch.
1612-
1613:Depende de M5 e outbox/M2.
1614-
1615-SaÃ­da: candidato extraÃ­do nunca altera item sem aÃ§Ã£o confirmatÃ³ria.
1616-
--
1652-- toda mutaÃ§Ã£o exige versÃ£o esperada;
1653-- consumers sÃ£o testados com duplicaÃ§Ã£o, reordenaÃ§Ã£o e retry;
1654-- efeitos externos tÃªm chave idempotente;
1655:- operaÃ§Ãµes crÃ­ticas usam outbox na mesma transaÃ§Ã£o;
1656:- sweeper recupera outbox apÃ³s simulaÃ§Ã£o de perda do Stream.
1657-
1658-### 20.3 Contratos
1659-
--
1837-
1838-| FunÃ§Ã£o | Gatilho | MÃ³dulo | Reserved concurrency (inicial) | DLQ / recuperaÃ§Ã£o |
1839-|---|---|---|---|---|
1840:| `ApiHandler` | API Gateway HTTP API | Identity/Expiration/Reminder/Notification/Document/Audit | conta com limite de conta compartilhado; sem reserva dedicada no MVP, protegido por quota por tenant (Â§17.1) | N/A (sÃ­ncrono, erro mapeado para resposta HTTP) |
1841:| `OutboxPublisher` | DynamoDB Streams + agendamento (sweeper) | transversal (outbox) | 5 | sem DLQ â€” falha mantÃ©m `PENDING`, sweeper reenfileira alÃ©m da retenÃ§Ã£o de 24h do Streams |
1842:| `ReminderProducer` | EventBridge Scheduler, 1 minuto | Reminder | 2 (uma execuÃ§Ã£o por minuto, folga para overlap) | sem DLQ â€” lookback `[M-5min, M]` cobre falha de tick; claims expirados voltam a `SCHEDULED` via reconciliaÃ§Ã£o |
1843:| `ReminderDispatchWorker` | `ReminderDispatchQueue` (comando do producer) | Reminder/Notification | 10 | DLQ, `maxReceiveCount=5` |
1844:| `ReminderReconciliationWorker` | EventBridge diÃ¡rio | Reminder | 1 | sem DLQ (idempotente, roda diariamente) |
1845-| `NotificationRouterWorker` | EventBridge/SQS | Notification | 10 | DLQ, `maxReceiveCount=5` |
1846-| `EmailDeliveryWorker` | `EmailDeliveryQueue` | Notification | 10, token bucket prÃ³prio do provider | DLQ por canal, sem kill switch (e-mail Ã© canal essencial, Â§10.3/Â§17.3) |
1847-| `WhatsAppDeliveryWorker` | `WhatsAppDeliveryQueue` | Notification | 5, token bucket prÃ³prio do provider | DLQ por canal, kill switch AppConfig `WHATSAPP` |
--
1855-| `BedrockExtractionTaskHandler` | Step Functions | Document | 2, respeita kill switch `AI`/`OCR` | timeout por step, fail-closed para `PENDING_CONFIRMATION` |
1856-| `ExtractionValidationTaskHandler` (schema validation, cross-extractor compare, persist) | Step Functions | Document/Expiration | 2 | timeout por step, fail-closed para `PENDING_CONFIRMATION` |
1857-| `UploadSlotReconciliationWorker` | EventBridge agendado | Document | 1 | sem DLQ (idempotente, restitui slots vencidos) |
1858:| `RetentionReconciliationWorker` | EventBridge agendado | transversal (via DAL) | 1 | sem DLQ (idempotente, consulta GSI6) |
1859-| `DlqRedriveWorker` | invocaÃ§Ã£o manual/runbook | transversal | 1, execuÃ§Ã£o manual controlada | N/A (operaÃ§Ã£o de recuperaÃ§Ã£o, auditada) |
1860-| `audit-consumer` | EventBridge (eventos de domÃ­nio relevantes) | Audit | 5 | **DLQ dedicada + reconciliaÃ§Ã£o periÃ³dica** (corrigido na convergÃªncia â€” auditoria nÃ£o pode depender sÃ³ de alarme, Â§13.1) |

 succeeded in 426ms:
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a01b2d-0c11-74d1-b775-7005fd779b52
--------
user
VocÃƒÂª ÃƒÂ© o segundo engenheiro independente num protocolo de nota cega Claude<->Codex (AGENTS.md secao 4 do repo expiration-tracker, c:/Users/Usuario/Desktop/projects/expiration-tracker). NAO leia claude-proposal.md neste diretorio - sua proposta precisa ser independente, sera comparada depois.

Contexto: micro-SaaS serverless AWS (TypeScript, CDK, DynamoDB, SQS, EventBridge). M0-M3 ja implementados e testados (123 testes) mas SEM runtime real: adapters DynamoDB sao fakes em memoria, handlers Lambda sao placeholders `exports.handler = async () => ({statusCode: 501})`, sem filas/EventBridge wired. Um red team formal (checkpoint-12) apontou isso como bloqueador de aprovacao de arquitetura (G8), com 5 P1: (1) pipeline assincrono sem recuperacao real, (2) idempotencia nao provada no limite do efeito externo (at-least-once classico), (3) false-green entre logica pura e comportamento implantado, (4) concorrencia/estado obsoleto entre renew/cancel/materializacao/dispatch/reconciliation, (5) blast radius cross-tenant do GSI3 (IAM ja limita, falta teste negativo contra adapter real).

O usuario decidiu tratar o fechamento de G8 como um milestone dedicado (mesma disciplina de M0-M3: design -> implementacao -> teste real -> revisao Claude+Codex), com este escopo fixo:
1. Adapters DynamoDB reais implementando os ports existentes (ReminderStore, ExpirationStore, IdentityStore) contra AWS real.
2. Handlers Lambda reais substituindo os placeholders 501.
3. Filas SQS + DLQ com redrive policy para o pipeline producer->dispatch.
4. EventBridge Scheduled Rule disparando o ReminderProducer periodicamente (a cada minuto) e a ReminderReconciliation periodicamente.
5. Testes de fault injection contra esse runtime real (timeout de dependencia, poison message, redrive).

Arquitetura relevante ja existente e fixa (nao redesenhar, so integrar):
- Ports SDK-agnosticos ja definidos: ReminderStore (get/putIfAbsent/update/transactWrite/queryByItem), ReminderProducerStore (queryGsi3/get/transactWrite, isolado do resto por desenho), ExpirationStore, IdentityStore - todos em src/modules/*/ports/.
- shared/dynamodb/occ.ts ja produz os parametros de ConditionExpression/TransactWriteItems (builders puros, sem SDK ainda).
- shared/outbox/outbox.ts, shared/idempotency/idempotency.ts, shared/errors/app-error.ts (taxonomia retryable/terminal) ja existem.
- infra/lib/scoped-lambda-function.ts: todo Lambda deve nascer via ScopedLambdaFunction (unico ponto de enforcement de IAM least-privilege), hoje usa lambda.Code.fromInline para os placeholders.
- infra/lib/expiration-tracker-stack.ts ja declara os 5 ScopedLambdaFunction do Reminder Engine (RemindersHandler HTTP, ReminderProducer com gsi3Read exclusivo, ReminderDispatch, ReminderReconciliation) com as capabilities de tabela corretas - falta so trocar o codigo placeholder por bundle real e adicionar fila/schedule.
- GSI3 e deliberadamente isolado: so ReminderProducerStore/ReminderProducer pode fazer Query nele (protecao contra P1 #5), enforcement via IAM real (Table.grantReadWriteData do CDK inclui TODOS os indices por padrao - ja corrigido com PolicyStatement explicito).
- Reserved concurrency e DLQ ja documentados no blueprint (implementation-blueprint.md secao 26): ReminderDispatchWorker concurrency 10, DLQ maxReceiveCount=5; ReminderProducer concurrency 2, sem DLQ (lookback [M-5min,M] cobre falha de tick); ReminderReconciliation concurrency 1, sem DLQ (idempotente, diario).

Produza uma proposta tecnica concreta cobrindo os 5 itens do escopo: escolhas de biblioteca/pacote AWS SDK v3, estrutura de arquivos dos adapters DynamoDB (um por port, mantendo os ports SDK-agnosticos como estao), estrategia de bundling dos handlers Lambda reais, desenho da fila+DLQ (nomes, redrive policy, batch size, partial batch failure), desenho do EventBridge Schedule (Scheduler vs Rule legado, flexible time window), e como voce testaria fault injection de forma real (nao mockada) sem tornar o CI lento/fragil demais - inclua sua escolha entre DynamoDB Local, LocalStack, ou testcontainers, e por que. Aponte explicitamente o que este milestone NAO resolve dos 5 P1 do red team (idempotencia ponta-a-ponta contra efeito externo real depende de Notification/M4, que nao existe ainda) para evitar falsa sensacao de "G8 fechado = tudo resolvido". Termine com uma nota de confianca 0-10 na sua propria proposta e os riscos que mais te preocupam.

Responda em portugues, formato markdown, direto ao ponto (nao precisa de preambulo).

codex
Vou inspecionar apenas as fontes normativas e o cÃƒÂ³digo existente necessÃƒÂ¡rios para ancorar a proposta; nÃƒÂ£o abrirei `claude-proposal.md`. Como a tarefa ÃƒÂ© uma proposta independente, nÃƒÂ£o farei alteraÃƒÂ§ÃƒÂµes no repositÃƒÂ³rio.
2026-08-19T17:59:02.703433Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -LiteralPath NEXT_SESSION_PROMPT.md; Get-Content -LiteralPath docs/architecture/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 287ms:
# Expiration Tracker ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â Status e PrÃƒÆ’Ã‚Â³xima SessÃƒÆ’Ã‚Â£o

## PrÃƒÆ’Ã‚Â³xima aÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o obrigatÃƒÆ’Ã‚Â³ria (2026-08-19, mais recente ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â leia isto primeiro)

Engineering Maturity Review concluÃƒÆ’Ã‚Â­da (checkpoints 0, 1, 2-9, 12; ver `ENGINEERING.md` na raiz para o relatÃƒÆ’Ã‚Â³rio completo). Veredito: `ENGINEERING FOUNDATION STATUS: NOT APPROVED`, bloqueador ÃƒÆ’Ã‚Âºnico e conhecido: **G8 (recuperaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o real de falha assÃƒÆ’Ã‚Â­ncrona)**.

**DecisÃƒÆ’Ã‚Â£o do usuÃƒÆ’Ã‚Â¡rio (2026-08-19)**: tratar o fechamento pleno de G8 como **novo milestone dedicado** (mesma disciplina de M0-M3: pesquisa/design ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ implementaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ teste real ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ revisÃƒÆ’Ã‚Â£o Claude+Codex), nÃƒÆ’Ã‚Â£o como remediaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de sessÃƒÆ’Ã‚Â£o. Escopo desse milestone, per `ENGINEERING.md` (seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o "DecisÃƒÆ’Ã‚Â£o pendente: escopo de G8"):

- Adapters DynamoDB reais implementando os ports (`ReminderStore`, `ExpirationStore`, `IdentityStore`) contra AWS real ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â hoje sÃƒÆ’Ã‚Â³ existem fakes em memÃƒÆ’Ã‚Â³ria para teste.
- Handlers Lambda reais substituindo os placeholders `exports.handler = async () => ({statusCode: 501})` em `infra/lib/expiration-tracker-stack.ts`.
- Filas SQS + DLQ com redrive policy para o pipeline producerÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢dispatch.
- EventBridge Scheduled Rule disparando o `ReminderProducer` periodicamente (a cada minuto) e a `ReminderReconciliation` periodicamente.
- Testes de fault injection contra esse runtime real (timeout de dependÃƒÆ’Ã‚Âªncia, poison message, redrive).

Antes de comeÃƒÆ’Ã‚Â§ar esse milestone: ler `docs/engineering/reviews/checkpoint-12-redteam/summary.md` (red team formal jÃƒÆ’Ã‚Â¡ identificou 5 P1 relacionados: pipeline sem recuperaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, idempotÃƒÆ’Ã‚Âªncia nÃƒÆ’Ã‚Â£o provada no limite do efeito externo, false-green CI, concorrÃƒÆ’Ã‚Âªncia/estado obsoleto entre renew/cancel/materializaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o/dispatch/reconciliation, blast radius cross-tenant do GSI3 via workers privilegiados) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â usar como input de design, nÃƒÆ’Ã‚Â£o redescobrir do zero.

Trabalho jÃƒÆ’Ã‚Â¡ feito nesta sessÃƒÆ’Ã‚Â£o de engenharia (nÃƒÆ’Ã‚Â£o repetir): CI real corrigido e confirmado verde em 5 execuÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes; branch `develop` estabelecida como padrÃƒÆ’Ã‚Â£o de trabalho (`AGENTS.md` Ãƒâ€šÃ‚Â§3), `main` protegida; `dependency-cruiser` como enforcement real de boundary (achou e corrigiu 2 violaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes genuÃƒÆ’Ã‚Â­nas de arquitetura); testes de `reconciliation.ts`/`producer.ts` que nÃƒÆ’Ã‚Â£o existiam; tentativa de fechar a vulnerabilidade de devDependency (EX-001) via upgrade do Vitest foi revertida por quebrar CI real (bug upstream do npm, nÃƒÆ’Ã‚Â£o repetir sem verificar correÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o).

---

**CorreÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de 2026-08-19 (Engineering Maturity Review, Checkpoint 2-9)**: as seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes abaixo (escritas ao longo das sessÃƒÆ’Ã‚Âµes de M0-M3) afirmam "nada foi commitado" e citam contagens de teste por marco que, somadas, nÃƒÆ’Ã‚Â£o batem com a realidade medida agora. Estado real verificado por execuÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o: M0-M3 **jÃƒÆ’Ã‚Â¡ estÃƒÆ’Ã‚Â¡ commitado** num ÃƒÆ’Ã‚Âºnico commit (`154d6e0`), e `npm test` roda **123 testes** no total (19 arquivos), nÃƒÆ’Ã‚Â£o a soma das contagens individuais citadas abaixo. As seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes nÃƒÆ’Ã‚Â£o foram reescritas (preservadas como histÃƒÆ’Ã‚Â³rico de sessÃƒÆ’Ã‚Â£o), mas nÃƒÆ’Ã‚Â£o devem ser lidas como estado de commit/contagem de teste vigente ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â confiar em `git log`/`npm test` reais, nÃƒÆ’Ã‚Â£o nesses nÃƒÆ’Ã‚Âºmeros. Ver `docs/engineering/03-repository-baseline.md` e `docs/engineering/reviews/checkpoint-02-09-consolidated/` para a anÃƒÆ’Ã‚Â¡lise completa desta divergÃƒÆ’Ã‚Âªncia.

Projeto: micro-SaaS de controle de vencimentos/renovaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes. Pasta: `c:\Users\Usuario\Desktop\projects\expiration-tracker\`. Repo GitHub: `marcelo-jgoncalves/expiration-tracker` (privado).

Mapa completo de documentaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, status vigente e regra de precedÃƒÆ’Ã‚Âªncia: `docs/architecture/README.md`. Regras de processo e ferramentas: `AGENTS.md`. Log cronolÃƒÆ’Ã‚Â³gico de sessÃƒÆ’Ã‚Âµes: `docs/architecture/session-log.md`.

## Status atual

```text
DESIGN MATURITY STATUS: APPROVED (arquitetura conceitual + Implementation Blueprint)
ARCHITECTURE STATUS: NOT APPROVED
```

Todo o processo de design (Fases 0-3 do prompt mestre + os 14 entregÃƒÆ’Ã‚Â¡veis das seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes 35-52 + threat model seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o 33 + Implementation Blueprint seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o 60) estÃƒÆ’Ã‚Â¡ completo e aprovado ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â ver `ARCHITECTURE.md` na raiz para o documento consolidado. `ARCHITECTURE STATUS: NOT APPROVED` ÃƒÆ’Ã‚Â© o estado normativo correto atÃƒÆ’Ã‚Â© haver implementaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o real testada sob falha/carga (rubrica B, `requirements.md` Ãƒâ€šÃ‚Â§13.1) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â **nÃƒÆ’Ã‚Â£o ÃƒÆ’Ã‚Â© reprovaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de mÃƒÆ’Ã‚Â©rito, e nÃƒÆ’Ã‚Â£o muda com a conclusÃƒÆ’Ã‚Â£o do blueprint**: o blueprint ÃƒÆ’Ã‚Â© design detalhado, nÃƒÆ’Ã‚Â£o evidÃƒÆ’Ã‚Âªncia operacional.

## ConcluÃƒÆ’Ã‚Â­do nesta sessÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â Implementation Blueprint (seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o 60) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â APPROVED

`docs/architecture/implementation-blueprint.md`. Componentes/mÃƒÆ’Ã‚Â³dulos (Identity, Expiration, Reminder, Notification, Document, Audit + workers assÃƒÆ’Ã‚Â­ncronos), interfaces concretas, eventos/schemas reais (grounded em `data-model.md`), ordem de deploy, milestones M0-M7, critÃƒÆ’Ã‚Â©rios de aceite tÃƒÆ’Ã‚Â©cnicos por componente, e as 7 lacunas do `threat-model.md` incorporadas como requisito desde o inÃƒÆ’Ã‚Â­cio (nÃƒÆ’Ã‚Â£o apÃƒÆ’Ã‚Âªndice). Processo: 2 propostas independentes (Claude/Codex) ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ crÃƒÆ’Ã‚Â­tica cruzada (17 problemas reais encontrados, incluindo um erro tÃƒÆ’Ã‚Â©cnico presente nas duas propostas ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â chave do GSI3 do scheduler nÃƒÆ’Ã‚Â£o era consultÃƒÆ’Ã‚Â¡vel) ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ convergÃƒÆ’Ã‚Âªncia ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢ 5 rodadas de nota cega com correÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o pontual a cada achado (cabeÃƒÆ’Ã‚Â§alho prematuro, contradiÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de kill switch, tabela incompleta, decisÃƒÆ’Ã‚Âµes Type 1 nÃƒÆ’Ã‚Â£o propagadas ÃƒÆ’Ã‚Â s seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes operativas, mapeamento de estado da Step Functions incompleto). **Nota final: Claude 9.20 / Codex 9.2 (exato)** ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â ambos ÃƒÂ¢Ã¢â‚¬Â°Ã‚Â¥9.0 sem arredondar, 8 rodadas totais, nenhum gate violado. DecisÃƒÆ’Ã‚Â£o Type 1 nova registrada: chave global do GSI3 (`GSI3PK=DUE#yyyyMMddHHmm#NN`, exceÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o documentada ÃƒÆ’Ã‚Â  regra de toda chave comeÃƒÆ’Ã‚Â§ar por `TENANT#tenantId`) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â a decisÃƒÆ’Ã‚Â£o estÃƒÆ’Ã‚Â¡ fechada no blueprint; falta sÃƒÆ’Ã‚Â³ o registro mecÃƒÆ’Ã‚Â¢nico em `data-model.md` (nÃƒÆ’Ã‚Â£o bloqueia implementaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o).

## ConcluÃƒÆ’Ã‚Â­do nesta sessÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â M0 "Guardrails e contratos" (implementation-blueprint.md Ãƒâ€šÃ‚Â§19)

Todas as entregas de M0 implementadas e testadas (53 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): estrutura TypeScript (`src/shared/*`, alinhada a `implementation-blueprint.md` Ãƒâ€šÃ‚Â§2), schemas JSON (`schemas/{events,queues,api}` + `sensitive-fields.json`, validados via Ajv em `src/shared/contracts/schema-validator.ts`), `SecureLogger`+`Redactor` central (`src/shared/observability/`, corpus de teste com valores canÃƒÆ’Ã‚Â¡rio provando que nada vaza), configuraÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o tipada fail-fast (`src/shared/config/config.ts`), taxonomia de erro normalizada retryable/terminal (`src/shared/errors/app-error.ts`), idempotÃƒÆ’Ã‚Âªncia via `PutItem attribute_not_exists(PK)` (`src/shared/idempotency/idempotency.ts`, chaves por operaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o conforme `data-model.md` Ãƒâ€šÃ‚Â§4), OCC com `ConditionExpression: version=:expected` (`src/shared/dynamodb/occ.ts`, `data-model.md` Ãƒâ€šÃ‚Â§5), outbox transacional (`src/shared/outbox/outbox.ts`, shape de `implementation-blueprint.md` Ãƒâ€šÃ‚Â§5.3), pipeline supply-chain (`.github/workflows/ci.yml`: npm ci imutÃƒÆ’Ã‚Â¡vel, actions pinadas por SHA, SBOM CycloneDX, audit, gate de schema). `AGENTS.md` Ãƒâ€šÃ‚Â§6 atualizado com comandos/convenÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes. Nada foi commitado (working tree aberto para revisÃƒÆ’Ã‚Â£o).

Judgment calls (blueprint estava silente): Ajv+ajv-formats para validaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de JSON Schema; Vitest como test runner; ESLint `no-console` (com exceÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o para `src/shared/observability/**`) como o mecanismo que faz "chamada direta a `console.*` falhar no lint" (`implementation-blueprint.md` Ãƒâ€šÃ‚Â§14.1); idempotÃƒÆ’Ã‚Âªncia/OCC/outbox construÃƒÆ’Ã‚Â­dos como builders puros de parÃƒÆ’Ã‚Â¢metros DynamoDB (sem `@aws-sdk` ainda) para ficarem testÃƒÆ’Ã‚Â¡veis sem tabelas reais, que sÃƒÆ’Ã‚Â³ existem a partir de M1; SLSA/assinatura de artefato adiada para M1+ (nÃƒÆ’Ã‚Â£o hÃƒÆ’Ã‚Â¡ alvo de deploy ainda); SHAs de actions no CI pinados no momento da escrita ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â revisar antes do primeiro run real.

## ConcluÃƒÆ’Ã‚Â­do nesta sessÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â M1 "Foundation, Identity e isolamento" (implementation-blueprint.md Ãƒâ€šÃ‚Â§19)

Todas as entregas implementadas e testadas (89 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/identity/{domain,application,ports,persistence,http}` (resolver central, matriz de autorizaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o como cÃƒÆ’Ã‚Â³digo, `IdentityMapping`/`User`/`DeviceSession`/`TenantQuota` via portas SDK-agnÃƒÆ’Ã‚Â³sticas) e `infra/{lib,bin}` (`ExpirationTrackerTable`, `ExpirationTrackerAuth`, `ScopedLambdaFunction`, `ExpirationTrackerApi` com a rota `GET /test/ping`). SuÃƒÆ’Ã‚Â­te cross-tenant negativa em `test/integration/cross-tenant.test.ts` (9 casos, exit criterion do marco) e synth de infra em `test/infra/stack.test.ts` (6 casos, via `aws-cdk-lib/assertions`, sem AWS CLI). Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente ou pendÃƒÆ’Ã‚Âªncia externa jÃƒÆ’Ã‚Â¡ conhecida): (1) MFA implementado como prop configurÃƒÆ’Ã‚Â¡vel (`OFF`/`OPTIONAL`/`REQUIRED`, default `OPTIONAL`) pois UNK-006 segue pendente de pesquisa externa ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â nÃƒÆ’Ã‚Â£o bloqueava M1; (2) CSP/CloudFront Response Headers Policy **nÃƒÆ’Ã‚Â£o implementado nesta sessÃƒÆ’Ã‚Â£o** ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â nÃƒÆ’Ã‚Â£o hÃƒÆ’Ã‚Â¡ distribuiÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o CloudFront/frontend ainda no repositÃƒÆ’Ã‚Â³rio, e o texto do blueprint (Ãƒâ€šÃ‚Â§4.2) coloca CSP no contexto do SPA estÃƒÆ’Ã‚Â¡tico servido por CloudFront; tratado como pertencente ao milestone que introduzir o frontend, nÃƒÆ’Ã‚Â£o forÃƒÆ’Ã‚Â§ado em M1 (revisar se essa leitura estiver errada); (3) sessÃƒÆ’Ã‚Â£o/revogaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o modelada como campo `globalLogoutAfter` no item `User` + item filho `DeviceSession` (`TENANT#t#USER#u`/`SESSION#<deviceId>`) em vez de uma entidade `Session` de primeira classe ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â `data-model.md` nÃƒÆ’Ã‚Â£o define uma; (4) `ScopedLambdaFunction` mantÃƒÆ’Ã‚Â©m a lista de entidades por capability como metadado/documentaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, mas o IAM real concedido ÃƒÆ’Ã‚Â© table-level (`grantReadWriteData`) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â DynamoDB IAM nÃƒÆ’Ã‚Â£o expressa restriÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o por SK/entidade da forma que a sintaxe do blueprint sugere visualmente; per-entity IAM mais fino fica como follow-up; (5) `TenantQuota` implementado como janela fixa (fixed-window), nÃƒÆ’Ã‚Â£o sliding-window/leaky-bucket ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â mais simples e ainda satisfaz "decremento atÃƒÆ’Ã‚Â´mico, sem race condition" do `data-model.md`; (6) BFF de sessÃƒÆ’Ã‚Â£o (`/session/refresh`, `/session/logout`) **nÃƒÆ’Ã‚Â£o implementado como rota HTTP nesta sessÃƒÆ’Ã‚Â£o** ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â o Cognito client jÃƒÆ’Ã‚Â¡ estÃƒÆ’Ã‚Â¡ configurado para o padrÃƒÆ’Ã‚Â£o (client secret, tokens de vida curta), mas os endpoints do BFF em si ficam para quando a Expiration/Notification API skeleton crescer alÃƒÆ’Ã‚Â©m da rota de teste; revisar se isso deveria ter sido parte do exit criterion. `cdk synth` real via CLI nÃƒÆ’Ã‚Â£o foi executado (o pacote `aws-cdk` CLI nÃƒÆ’Ã‚Â£o estÃƒÆ’Ã‚Â¡ instalado, sÃƒÆ’Ã‚Â³ a lib `aws-cdk-lib`); a sintetizaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o foi verificada programaticamente via `Template.fromStack` nos testes, que ÃƒÆ’Ã‚Â© equivalente para fins de validaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o estÃƒÆ’Ã‚Â¡tica.

## ConcluÃƒÆ’Ã‚Â­do nesta sessÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â M2 "Expiration core e Audit" (implementation-blueprint.md Ãƒâ€šÃ‚Â§19)

Todas as entregas implementadas e testadas (134 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/expiration/{domain,application,ports,http}` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â CRUD/renew (`ExpirationService`: createItem/getItem/updateItem/archiveItem/deleteItem/renewItem/listDashboard), OCC via `shared/dynamodb/occ.ts`, `ItemDueDateChanged` por outbox (`shared/outbox/outbox.ts`) na MESMA `TransactWriteItems` do item quando `dueDate` muda (updateItem e renewItem), `AuditEvent` append-only gravado em toda mutaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o na mesma transaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, dashboard via GSI1. `renewItem` cria um item sucessor (`renewedFromId`) em vez de mutar `dueDate` no agregado de origem, que transiciona para `RENEWED`; ÃƒÆ’Ã‚Â© idempotente via `IdempotencyStore` de M0. Rotas HTTP CDK adicionadas em `infra/lib/api.ts`/`expiration-tracker-stack.ts` (`ItemsHandler`, mesmo authorizer JWT de M1). SuÃƒÆ’Ã‚Â­te de integraÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o `test/integration/expiration-lifecycle.test.ts` prova o exit criterion do marco end-to-end via handlers HTTP reais. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) evento `ItemDueDateChanged` segue exatamente o schema jÃƒÆ’Ã‚Â¡ existente desde M0 (`schemas/events/item-due-date-changed.v1.json`: `itemId`/`previousDueDate`/`newDueDate`/`itemVersion`, `additionalProperties: false`) em vez do exemplo mais rico do texto do blueprint Ãƒâ€šÃ‚Â§8.3 (que inclui `timeZone`/`reminderPolicyId`/`changeReason`) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â o schema jÃƒÆ’Ã‚Â¡ testado/versionado ÃƒÆ’Ã‚Â© a fonte de verdade de contrato per `AGENTS.md` Ãƒâ€šÃ‚Â§6, o exemplo em prosa nÃƒÆ’Ã‚Â£o; campos adicionais ficam para quando M3 (Reminder) precisar deles, exigindo `.v2` aditivo; (2) `renewItem` tambÃƒÆ’Ã‚Â©m dispara `ItemDueDateChanged` (para o item novo, `previousDueDate: null`) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â o blueprint sÃƒÆ’Ã‚Â³ descreve esse evento no contexto de `updateItem`, mas renovaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o tambÃƒÆ’Ã‚Â©m ÃƒÆ’Ã‚Â© uma "mudanÃƒÆ’Ã‚Â§a de vencimento" do ponto de vista do futuro `ReminderProducer` (M3), e a alternativa (renovaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o silenciosa) deixaria o dashboard/scheduler sem sinal de que um novo item precisa de agendamento; (3) `archiveItem` usa a aÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de autorizaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o `item:update` (nÃƒÆ’Ã‚Â£o uma aÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o dedicada ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â a matriz de M1 nÃƒÆ’Ã‚Â£o define uma para archive) enquanto `deleteItem` usa `item:delete` (ADMIN_ROLES), seguindo a granularidade que jÃƒÆ’Ã‚Â¡ existe; (4) idempotÃƒÆ’Ã‚Âªncia de renovaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o aceita uma `idempotencyKey` explÃƒÆ’Ã‚Â­cita do chamador (header `Idempotency-Key`) com fallback determinÃƒÆ’Ã‚Â­stico `itemId|expectedVersion|cycle` quando ausente, jÃƒÆ’Ã‚Â¡ que o blueprint define a CHAVE de dedupe mas nÃƒÆ’Ã‚Â£o de onde ela vem por HTTP; (5) `ExpirationStore.transactWrite` foi adicionado ao port (nÃƒÆ’Ã‚Â£o existe em `IdentityStore`) porque nenhuma escrita de M1 precisava de `TransactWriteItems` multi-item ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â a fake em memÃƒÆ’Ã‚Â³ria (`test/unit/expiration/in-memory-store.ts`) sÃƒÆ’Ã‚Â³ entende as duas formas exatas de `ConditionExpression` que este cÃƒÆ’Ã‚Â³digo produz (mesma limitaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o documentada, mesmo espÃƒÆ’Ã‚Â­rito de `InMemoryIdentityStore`).

## ConcluÃƒÆ’Ã‚Â­do nesta sessÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â M3 "Reminder Engine" (implementation-blueprint.md Ãƒâ€šÃ‚Â§19)

Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversÃƒÆ’Ã‚Â£o localÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢UTC IANA-aware ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â DST tratado explicitamente, horÃƒÆ’Ã‚Â¡rio inexistente e ambÃƒÆ’Ã‚Â­guo ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lÃƒÆ’Ã‚Â³gica pura testÃƒÆ’Ã‚Â¡vel com relÃƒÆ’Ã‚Â³gio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMEDÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢TRIGGERED`+`NotificationIntent`+outbox numa ÃƒÆ’Ã‚Âºnica transaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o; reconciliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o ÃƒÆ’Ã‚Â© um ÃƒÆ’Ã‚Âºnico job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluÃƒÆ’Ã‚Â­am `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER funÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o da tabela (inclusive as tenant-facing de M1/M2) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explÃƒÆ’Ã‚Â­citos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que sÃƒÆ’Ã‚Â³ `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) `occurrenceId` derivado deterministicamente (hash da chave de idempotÃƒÆ’Ã‚Âªncia de data-model.md Ãƒâ€šÃ‚Â§4) em vez de UUID + registro `IdempotencyStore` separado ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â o prÃƒÆ’Ã‚Â³prio `putIfAbsent` condicional jÃƒÆ’Ã‚Â¡ garante a idempotÃƒÆ’Ã‚Âªncia de materializaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, mesmo padrÃƒÆ’Ã‚Â£o que data-model.md documenta para `WebhookInbox`; (2) cancelamento de ocorrÃƒÆ’Ã‚Âªncia stale nÃƒÆ’Ã‚Â£o remove `GSI3PK`/`GSI3SK` (o builder de `occ.ts` ÃƒÆ’Ã‚Â© SET-only, sem REMOVE) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â deixa um ponteiro ÃƒÆ’Ã‚Â³rfÃƒÆ’Ã‚Â£o no ÃƒÆ’Ã‚Â­ndice, mas o `Query` condicional `SCHEDULEDÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢CLAIMED` do producer falha sobre ele de forma inofensiva; (3) tolerÃƒÆ’Ã‚Â¢ncia de dispatch (`toleranceMs`, default 30min) e TTL de claim (default 2min) nÃƒÆ’Ã‚Â£o estÃƒÆ’Ã‚Â£o fixados no blueprint ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â valores razoÃƒÆ’Ã‚Â¡veis documentados no cÃƒÆ’Ã‚Â³digo, revisar contra dados reais de produÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o; (4) reconciliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o DST/claim-expiry recebe os candidatos como parÃƒÆ’Ã‚Â¢metro (batch) em vez de fazer sua prÃƒÆ’Ã‚Â³pria varredura via GSI6 ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â a wiring desse ÃƒÆ’Ã‚Â­ndice de "polÃƒÆ’Ã‚Â­ticas ativas"/"claims expirados" real fica como follow-up de infra, o mecanismo de reconciliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o em si (o que a tarefa pediu) estÃƒÆ’Ã‚Â¡ implementado e testado; (5) Lambda handlers reais (bundling com `@aws-sdk`) nÃƒÆ’Ã‚Â£o foram escritos ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â mesmo estÃƒÆ’Ã‚Â¡gio que M0-M2, CDK usa cÃƒÆ’Ã‚Â³digo inline placeholder via `ScopedLambdaFunction`; a lÃƒÆ’Ã‚Â³gica testÃƒÆ’Ã‚Â¡vel ÃƒÆ’Ã‚Â© o entregÃƒÆ’Ã‚Â¡vel real desta fase, igual ÃƒÆ’Ã‚Â s anteriores.

## PrÃƒÆ’Ã‚Â³xima aÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o obrigatÃƒÆ’Ã‚Â³ria (histÃƒÆ’Ã‚Â³rico ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â superada pela seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o do topo)

**Superada em 2026-08-19**: a Engineering Maturity Review identificou G8 (recuperaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o real de falha assÃƒÆ’Ã‚Â­ncrona) como bloqueador de engenharia ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â o milestone de runtime real (adapters/handlers/filas/EventBridge, ver seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o do topo) ÃƒÆ’Ã‚Â© prÃƒÆ’Ã‚Â©-requisito antes de M4 fazer sentido operacionalmente, ainda que M4 nÃƒÆ’Ã‚Â£o dependa dele estruturalmente. Lista original preservada abaixo como histÃƒÆ’Ã‚Â³rico, nÃƒÆ’Ã‚Â£o como prÃƒÆ’Ã‚Â³xima aÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o vigente.

1. **M4 ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â Notification Engine** (`implementation-blueprint.md` Ãƒâ€šÃ‚Â§19, depende de M3 ÃƒÂ¢Ã…â€œÃ¢â‚¬Â¦): router de `NotificationIntent`ÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬â„¢canal, delivery workers (email/WhatsApp stub-first), `NotificationAttempt`, resoluÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de destinatÃƒÆ’Ã‚Â¡rio/template.
2. Decidir e fechar os itens abaixo (BFF de sessÃƒÆ’Ã‚Â£o como rotas reais, CSP/CloudFront quando o frontend existir) antes de considerar a lacuna de "session theft" do threat model totalmente fechada ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â ainda nÃƒÆ’Ã‚Â£o resolvido, re-flagueado a cada sessÃƒÆ’Ã‚Â£o desde M1 para nÃƒÆ’Ã‚Â£o se perder: M1 fechou o mecanismo de revogaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o/matriz de autorizaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, nÃƒÆ’Ã‚Â£o o endpoint BFF completo; M2/M3 nÃƒÆ’Ã‚Â£o tinham escopo de frontend/sessÃƒÆ’Ã‚Â£o (confirmado no blueprint) entÃƒÆ’Ã‚Â£o tambÃƒÆ’Ã‚Â©m nÃƒÆ’Ã‚Â£o o resolveram.
3. **Ratificar formalmente em `data-model.md` a exceÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o do GSI3** (chave global do scheduler, jÃƒÆ’Ã‚Â¡ decidida e justificada em `implementation-blueprint.md` Ãƒâ€šÃ‚Â§9.2/Ãƒâ€šÃ‚Â§23.3 item 10) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â passo de manutenÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o documental, nÃƒÆ’Ã‚Â£o nova rodada de debate. M3 jÃƒÆ’Ã‚Â¡ corrigiu o enforcement real do isolamento em IAM; falta sÃƒÆ’Ã‚Â³ o registro mecÃƒÆ’Ã‚Â¢nico no documento.
4. Wiring real do GSI6 (ou ÃƒÆ’Ã‚Â­ndice equivalente) para alimentar a reconciliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o de M3 com o batch real de "polÃƒÆ’Ã‚Â­ticas ativas"/"claims a verificar" em produÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â hoje o mecanismo de reconciliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o recebe esse batch como parÃƒÆ’Ã‚Â¢metro injetado (testÃƒÆ’Ã‚Â¡vel), a query real fica pendente.
5. Testes de carga real (cenÃƒÆ’Ã‚Â¡rios de drenagem do pico extremo, `capacity-model.md`, com SLO de drenagem UNK-CAP-006 a formalizar em `slo.md` antes do gate de M7), teste de restore real (gate jÃƒÆ’Ã‚Â¡ definido em `disaster-recovery.md` Ãƒâ€šÃ‚Â§6), exercÃƒÆ’Ã‚Â­cio do runbook de credencial comprometida.
6. ReavaliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o sob rubrica (B) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â Operational Evidence ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â sÃƒÆ’Ã‚Â³ entÃƒÆ’Ã‚Â£o `ARCHITECTURE STATUS` pode legitimamente virar `APPROVED`. **NÃƒÆ’Ã‚Â£o declarar isso antes de haver evidÃƒÆ’Ã‚Âªncia operacional real.** Implementar M0/M1/M2/M3 nÃƒÆ’Ã‚Â£o ÃƒÆ’Ã‚Â© evidÃƒÆ’Ã‚Âªncia operacional.
7. DecisÃƒÆ’Ã‚Âµes ainda pendentes de pesquisa externa (nÃƒÆ’Ã‚Â£o bloqueiam M4 estruturalmente, mas bloqueiam habilitar os canais/features especÃƒÆ’Ã‚Â­ficos, ver `implementation-blueprint.md` Ãƒâ€šÃ‚Â§23.3): provider inicial de e-mail, BSP WhatsApp (pricing real, UNK-003), modelo Bedrock especÃƒÆ’Ã‚Â­fico, regiÃƒÆ’Ã‚Â£o AWS (bloqueante para LGPD/transferÃƒÆ’Ã‚Âªncia internacional), MFA obrigatÃƒÆ’Ã‚Â³rio vs. opcional (UNK-006), ferramenta de backup S3 (RPOÃƒÂ¢Ã¢â‚¬Â°Ã‚Â¤24h), ferramenta de assinatura/provenance do pipeline de CI.
# docs/architecture/ ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â ÃƒÆ’Ã‚Ândice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o real (cÃƒÆ’Ã‚Â³digo/infra/testes) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â Implementation Blueprint concluÃƒÆ’Ã‚Â­do
Last verified:           2026-08-19
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃƒÆ’Ã‚Â³xima aÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o concreta.

## PrecedÃƒÆ’Ã‚Âªncia de fontes (quando houver divergÃƒÆ’Ã‚Âªncia)

1. `AGENTS.md` (raiz) ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â processo de trabalho dos agentes, sempre vence sobre conteÃƒÆ’Ã‚Âºdo de arquitetura.
2. ADR aceito em `adr/` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â decisÃƒÆ’Ã‚Â£o arquitetural especÃƒÆ’Ã‚Â­fica e formal.
3. Documento temÃƒÆ’Ã‚Â¡tico corrente (tabela abaixo, coluna "normativo atual") ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â especificaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o detalhada do domÃƒÆ’Ã‚Â­nio.
4. `ARCHITECTURE.md` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â visÃƒÆ’Ã‚Â£o consolidada e ÃƒÆ’Ã‚Â­ndice executivo; nÃƒÆ’Ã‚Â£o sobrescreve silenciosamente um ADR ou documento temÃƒÆ’Ã‚Â¡tico divergente ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â divergÃƒÆ’Ã‚Âªncia entre eles ÃƒÆ’Ã‚Â© defeito a corrigir, nÃƒÆ’Ã‚Â£o licenÃƒÆ’Ã‚Â§a para escolher.
5. `NEXT_SESSION_PROMPT.md` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â estado de execuÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â evidÃƒÆ’Ã‚Âªncia histÃƒÆ’Ã‚Â³rica de como se chegou a uma decisÃƒÆ’Ã‚Â£o, nunca normativo.

## ÃƒÆ’Ã‚Ândice por documento

| Documento | ClassificaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/ÃƒÆ’Ã‚Â­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃƒÆ’Ã‚Â³rico/processo (ciclo concluÃƒÆ’Ã‚Â­do) | Processo ClaudeÃƒÂ¢Ã¢â‚¬Â Ã¢â‚¬ÂCodex que produziu o design; nÃƒÆ’Ã‚Â£o ÃƒÆ’Ã‚Â© ponto de entrada de sessÃƒÆ’Ã‚Â£o |
| `quality-criteria.md` | normativo atual | 12 critÃƒÆ’Ã‚Â©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃƒÆ’Ã‚Â©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃƒÆ’Ã‚Â£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃƒÆ’Ã‚Âµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃƒÆ’Ã‚Â­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃƒÆ’Ã‚Â¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃƒÆ’Ã‚Â£o de domÃƒÆ’Ã‚Â­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Âµes de estÃƒÆ’Ã‚Â¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃƒÆ’Ã‚Â£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o 33 ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o 60 ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â componentes, interfaces, eventos/schemas, ordem de deploy, milestones ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â APPROVED (Claude 9.20 / Codex 9.2) |
| `decisions-log.md` | decisÃƒÆ’Ã‚Â£o/ADR (log vivo) | 26 decisÃƒÆ’Ã‚Âµes (D-000 a D-025), nota Claude/Codex, status |
| `adr/` | decisÃƒÆ’Ã‚Â£o/ADR | 8 ADRs formais para decisÃƒÆ’Ã‚Âµes Type 1 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/status-projeto.html` | resumo/ÃƒÆ’Ã‚Â­ndice (visual) | Painel visual do status do projeto |
| `session-log.md` | histÃƒÆ’Ã‚Â³rico | Log cronolÃƒÆ’Ã‚Â³gico compacto por sessÃƒÆ’Ã‚Â£o |
| `history/` | histÃƒÆ’Ã‚Â³rico/supersedido | Artefatos de rodada (propostas, crÃƒÆ’Ã‚Â­ticas, red team) que produziram os documentos normativos acima ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â ver subseÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o |

## `history/` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â evidÃƒÆ’Ã‚Âªncia de rodada, por tema

Cada subpasta contÃƒÆ’Ã‚Â©m os artefatos de proposta/crÃƒÆ’Ã‚Â­tica/trÃƒÆ’Ã‚Â©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃƒÆ’Ã‚Â£o vigente ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â sÃƒÆ’Ã‚Â³ como prova de como o consenso foi alcanÃƒÆ’Ã‚Â§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^export interface|''^export type|''^export class|ReminderProducerStore|501|ScopedLambdaFunction|reservedConcurrent|GSI3|ReminderDispatch|ReminderReconciliation" src/modules src/workers infra/lib docs/architecture/implementation-blueprint.md package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 256ms:
docs/architecture/implementation-blueprint.md:11:1. **Scheduler GSI3 corrigido** Ã¢â‚¬â€ erro tÃƒÂ©cnico real presente nas duas propostas independentes: uma partition key `TENANT#t#DUE#yyyyMMddHHmm#NN` nÃƒÂ£o ÃƒÂ© consultÃƒÂ¡vel pelo producer do minuto sem jÃƒÂ¡ saber quais tenants tÃƒÂªm ocorrÃƒÂªncia naquele minuto (`Query` exige PK exata). Corrigido em todo o documento para `GSI3PK=DUE#yyyyMMddHHmm#NN` / `GSI3SK=TENANT#tenantId#OCCURRENCE#occurrenceId` Ã¢â‚¬â€ exceÃƒÂ§ÃƒÂ£o documentada e justificada ÃƒÂ  regra "toda chave comeÃƒÂ§a com `TENANT#tenantId`" de `data-model.md` Ã‚Â§3, com as salvaguardas da Ã‚Â§9.2 abaixo. Esta ÃƒÂ© uma mudanÃƒÂ§a Type 1 sobre `data-model.md` e deve ser ratificada lÃƒÂ¡ (ver `decisions-log.md`).
docs/architecture/implementation-blueprint.md:162:| `ReminderProducer` | EventBridge Scheduler, 1 minuto | Consultar GSI3 nos shards do minuto e enviar comandos para SQS |
docs/architecture/implementation-blueprint.md:163:| `ReminderDispatchWorker` | `ReminderDispatchQueue` | Revalidar ocorrÃƒÂªncia/item e produzir `NotificationIntent` |
docs/architecture/implementation-blueprint.md:164:| `ReminderReconciliationWorker` | EventBridge diÃƒÂ¡rio | Recalcular janela de sete dias e reparar ocorrÃƒÂªncias |
docs/architecture/implementation-blueprint.md:181:**Decidido (Ã‚Â§23.1): Textract, parser determinÃƒÂ­stico e Bedrock sÃƒÂ£o Lambdas separadas desde o primeiro release**, nÃƒÂ£o um `ExtractionTaskHandler` ÃƒÂºnico consolidado depois. Justificativa: o sandbox de PDF (lacuna #2 do threat model, limites em Ã‚Â§12.4/Ã‚Â§23.1) exige isolamento de IAM/rede que uma funÃƒÂ§ÃƒÂ£o combinada dilui Ã¢â‚¬â€ separar desde o dia 1 ÃƒÂ© mais simples de auditar do que consolidar e depois desmembrar sob pressÃƒÂ£o de produÃƒÂ§ÃƒÂ£o. Cada uma das trÃƒÂªs superfÃƒÂ­cies de risco isoladas (OCR gerenciado, parsing nÃƒÂ£o confiÃƒÂ¡vel em sandbox, chamada a LLM) tem sua prÃƒÂ³pria funÃƒÂ§ÃƒÂ£o nomeada com `ScopedLambdaFunction` (Ã‚Â§17.1) Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© "um estado, uma funÃƒÂ§ÃƒÂ£o": estados de orquestraÃƒÂ§ÃƒÂ£o pura (`NeedsBedrock?`) sÃƒÂ£o Choice states nativos sem Lambda, e estados que compartilham a mesma superfÃƒÂ­cie de IAM/dados (validaÃƒÂ§ÃƒÂ£o de schema, comparaÃƒÂ§ÃƒÂ£o entre extratores, persistÃƒÂªncia, fail-closed) compartilham `ExtractionValidationTaskHandler`. O mapeamento estadoÃ¢â€ â€™handler completo estÃƒÂ¡ em Ã‚Â§12.5.
docs/architecture/implementation-blueprint.md:201:export interface RequestContext {
docs/architecture/implementation-blueprint.md:623:  "GSI3PK": "DUE#202609101200#02",
docs/architecture/implementation-blueprint.md:624:  "GSI3SK": "TENANT#t_01#OCCURRENCE#occ_01"
docs/architecture/implementation-blueprint.md:628:**CorreÃƒÂ§ÃƒÂ£o de convergÃƒÂªncia (Rodada 3)**: a chave original avaliada em ambas as propostas independentes (`GSI3PK=TENANT#t#DUE#...`) nÃƒÂ£o ÃƒÂ© consultÃƒÂ¡vel pelo producer do minuto Ã¢â‚¬â€ `Query` no DynamoDB exige a partition key exata, e o producer nÃƒÂ£o sabe a priori quais tenants tÃƒÂªm ocorrÃƒÂªncia em `M`. Corrigido para uma **chave global** (`GSI3PK=DUE#yyyyMMddHHmm#NN`, sem `tenantId`), com `tenantId` preservado no `GSI3SK` e no item base para reconstruÃƒÂ§ÃƒÂ£o segura do contexto. Esta ÃƒÂ© uma **exceÃƒÂ§ÃƒÂ£o explÃƒÂ­cita e documentada** ÃƒÂ  regra "toda chave comeÃƒÂ§a com `TENANT#tenantId`" de `data-model.md` Ã‚Â§3 Ã¢â‚¬â€ Type 1, deve ser ratificada em `data-model.md`/`decisions-log.md`, nÃƒÂ£o apenas aqui. Salvaguardas obrigatÃƒÂ³rias:
docs/architecture/implementation-blueprint.md:629:- IAM do ÃƒÂ­ndice restrito exclusivamente ao `ReminderProducer` Ã¢â‚¬â€ nenhuma rota tenant-facing tem permissÃƒÂ£o de `Query` no GSI3;
docs/architecture/implementation-blueprint.md:647:2. consulta GSI3 pelos `N` shards globais de `M` **e de uma janela de lookback** (ex. `[M-5min, M]`) para cobrir minutos cuja publicaÃƒÂ§ÃƒÂ£o falhou Ã¢â‚¬â€ sem lookback, "o prÃƒÂ³ximo tick reprocessa" ÃƒÂ© falso, pois o prÃƒÂ³ximo tick consulta uma chave diferente (`M+1`), deixando `M` definitivamente para trÃƒÂ¡s em caso de falha;
docs/architecture/implementation-blueprint.md:1084:| # | Estado | Lambda (`ScopedLambdaFunction` prÃƒÂ³prio) |
docs/architecture/implementation-blueprint.md:1379:### 17.1 `ScopedLambdaFunction`
docs/architecture/implementation-blueprint.md:1384:new ScopedLambdaFunction(this, "ReminderDispatch", {
docs/architecture/implementation-blueprint.md:1529:- `ScopedLambdaFunction`;
docs/architecture/implementation-blueprint.md:1557:- GSI3;
docs/architecture/implementation-blueprint.md:1783:2. **Lookup `cognitoSub Ã¢â€ â€™ userId`**: **decidido Ã¢â‚¬â€ item de mapeamento dedicado** `PK=IDENTITY#cognitoSub#<sub>` / `SK=MAP`, criado atomicamente (`ConditionExpression attribute_not_exists`) no primeiro login, apontando para `userId`/`tenantId` internos. Fora do agregado `TENANT#...` porque a busca ocorre *antes* de `tenantId` ser conhecido Ã¢â‚¬â€ ÃƒÂºnica exceÃƒÂ§ÃƒÂ£o adicional de particionamento alÃƒÂ©m do GSI3 (Ã‚Â§9.2), pelo mesmo motivo estrutural (chave nÃƒÂ£o pode depender de um dado ainda nÃƒÂ£o resolvido).
docs/architecture/implementation-blueprint.md:1797:10. **RatificaÃƒÂ§ÃƒÂ£o formal em `data-model.md`/`decisions-log.md` da chave global do GSI3**: a *decisÃƒÂ£o* jÃƒÂ¡ estÃƒÂ¡ fechada aqui (Ã‚Â§9.2, com justificativa completa e salvaguardas); o que resta ÃƒÂ© o registro mecÃƒÂ¢nico no documento normativo Ã¢â‚¬â€ nÃƒÂ£o uma lacuna de design, um passo de manutenÃƒÂ§ÃƒÂ£o documental (AGENTS.md Ã‚Â§5) a executar no fechamento desta sessÃƒÂ£o.
docs/architecture/implementation-blueprint.md:1824:| GSI3 global (scheduler) + shards versionados | NFR-002, NFR-011 | Pico extremo 16.667Ã¢â‚¬â€œ278 agendamentos/s (`capacity-model.md`), SLO de drenagem UNK-CAP-006 (`slo.md`) |
docs/architecture/implementation-blueprint.md:1843:| `ReminderDispatchWorker` | `ReminderDispatchQueue` (comando do producer) | Reminder/Notification | 10 | DLQ, `maxReceiveCount=5` |
docs/architecture/implementation-blueprint.md:1844:| `ReminderReconciliationWorker` | EventBridge diÃƒÂ¡rio | Reminder | 1 | sem DLQ (idempotente, roda diariamente) |
infra/lib\scoped-lambda-function.ts:2: * ScopedLambdaFunction Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§17.1. "O construct gera grants
infra/lib\scoped-lambda-function.ts:23:export interface AccessCapability {
infra/lib\scoped-lambda-function.ts:52:    /** GSI3 (scheduler) read-only - reserved EXCLUSIVELY for ReminderProducer
infra/lib\scoped-lambda-function.ts:59:        description: "GSI3 (scheduler) read-only - ReminderProducer only",
infra/lib\scoped-lambda-function.ts:113:export interface ScopedLambdaFunctionProps extends Omit<lambda.FunctionProps, "code"> {
infra/lib\scoped-lambda-function.ts:120: * Every Lambda in this system MUST be constructed via ScopedLambdaFunction, never
infra/lib\scoped-lambda-function.ts:124:export class ScopedLambdaFunction extends Construct {
infra/lib\scoped-lambda-function.ts:127:  constructor(scope: Construct, id: string, props: ScopedLambdaFunctionProps) {
infra/lib\scoped-lambda-function.ts:134:          `ScopedLambdaFunction(${id}): unknown access capability kind "${capability.kind}". ` +
infra/lib\scoped-lambda-function.ts:147:      code: code ?? lambda.Code.fromInline("exports.handler = async () => ({ statusCode: 501 });"),
infra/lib\expiration-tracker-stack.ts:2: * M1 stack: wires DynamoDB, Cognito, the identity test route (ScopedLambdaFunction) and
infra/lib\expiration-tracker-stack.ts:11:import { ScopedLambdaFunction, tableAccessFor } from "./scoped-lambda-function.js";
infra/lib\expiration-tracker-stack.ts:14:export interface ExpirationTrackerStackProps extends StackProps {
infra/lib\expiration-tracker-stack.ts:18:export class ExpirationTrackerStack extends Stack {
infra/lib\expiration-tracker-stack.ts:22:  readonly remindersHandler: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:23:  readonly reminderProducer: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:24:  readonly reminderDispatch: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:25:  readonly reminderReconciliation: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:35:    const testRouteHandler = new ScopedLambdaFunction(this, "TestPingHandler", {
infra/lib\expiration-tracker-stack.ts:43:    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
infra/lib\expiration-tracker-stack.ts:46:    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
infra/lib\expiration-tracker-stack.ts:58:    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
infra/lib\expiration-tracker-stack.ts:59:    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
infra/lib\expiration-tracker-stack.ts:69:    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
infra/lib\expiration-tracker-stack.ts:75:    // ReminderDispatch (Ã‚Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
infra/lib\expiration-tracker-stack.ts:77:    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
infra/lib\expiration-tracker-stack.ts:78:    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
infra/lib\expiration-tracker-stack.ts:92:    // ReminderReconciliation (Ã‚Â§9.5): single job for claim-expiry revert + DST
infra/lib\expiration-tracker-stack.ts:93:    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
infra/lib\expiration-tracker-stack.ts:95:    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
infra/lib\cognito.ts:12:export type MfaPolicy = "OFF" | "OPTIONAL" | "REQUIRED";
infra/lib\cognito.ts:14:export interface ExpirationTrackerAuthProps {
infra/lib\cognito.ts:26:export class ExpirationTrackerAuth extends Construct {
infra/lib\dynamo-table.ts:6: * GSI3 is the documented exception: its PK (GSI3PK) is deliberately NOT prefixed with
infra/lib\dynamo-table.ts:9: * `grantGsi3ReadTo` method instead of a general grantReadData-on-GSI3 path, so callers
infra/lib\dynamo-table.ts:18:export interface ExpirationTrackerTableProps {
infra/lib\dynamo-table.ts:23:export class ExpirationTrackerTable extends Construct {
infra/lib\dynamo-table.ts:53:    // GSI3 - scheduler. Global PK (no tenantId), IAM-restricted to ReminderProducer only
infra/lib\dynamo-table.ts:56:      indexName: "GSI3",
infra/lib\dynamo-table.ts:57:      partitionKey: { name: "GSI3PK", type: dynamodb.AttributeType.STRING },
infra/lib\dynamo-table.ts:58:      sortKey: { name: "GSI3SK", type: dynamodb.AttributeType.STRING },
infra/lib\dynamo-table.ts:85:   * Resource ARNs for every GSI EXCEPT GSI3 (data-model.md Ã‚Â§3's isolation safeguard).
infra/lib\dynamo-table.ts:90:   * for tenant-facing functions would silently grant `dynamodb:Query` on GSI3 to every
infra/lib\dynamo-table.ts:93:   * PolicyStatements with an explicit resource list (base table + the 5 non-GSI3 indexes)
infra/lib\dynamo-table.ts:105:    return `${this.table.tableArn}/index/GSI3`;
infra/lib\dynamo-table.ts:108:  /** General read/write grant on the base table + GSI1/GSI2/GSI4/GSI5/GSI6 (tenant-scoped indexes). Never includes GSI3 - see tenantFacingResources() above. */
infra/lib\dynamo-table.ts:144:  /** The ONLY sanctioned way to read GSI3 - resource is the GSI3 index ARN exclusively (never the table or any other index), intended solely for the M3 ReminderProducer. test/infra/stack.test.ts's isolation test asserts no other function's synthesized IAM policy references this index. */
infra/lib\api.ts:3: * (GET /test/ping) wired through a Cognito JWT authorizer + the ScopedLambdaFunction
infra/lib\api.ts:11:import type { ScopedLambdaFunction } from "./scoped-lambda-function.js";
infra/lib\api.ts:14:export interface ExpirationTrackerApiProps {
infra/lib\api.ts:16:  testRouteHandler: ScopedLambdaFunction;
infra/lib\api.ts:22:  itemsHandler?: ScopedLambdaFunction;
infra/lib\api.ts:25:export class ExpirationTrackerApi extends Construct {
src/workers\reminder-dispatch\dispatch.ts:32:export interface DispatchDeps {
src/workers\reminder-dispatch\dispatch.ts:43:export type DispatchOutcome =
src/workers\reminder-reconciliation\reconciliation.ts:28:export interface ReconciliationDeps {
src/workers\reminder-reconciliation\reconciliation.ts:39:export interface DstReconciliationCandidate {
src/workers\reminder-reconciliation\reconciliation.ts:47:export interface ReconciliationResult {
src/workers\reminder-producer\producer.ts:3: * injected `ReminderProducerStore` and clock, no AWS SDK/Lambda runtime dependency, so it
src/workers\reminder-producer\producer.ts:4: * is directly unit/integration-testable with a fake clock and fake GSI3 index (per the
src/workers\reminder-producer\producer.ts:9: *  2. query GSI3 across N shards of minute M AND a lookback window [M-lookback, M] (default
src/workers\reminder-producer\producer.ts:23:import type { ReminderProducerStore } from "../../modules/reminder/ports/reminder-store.js";
src/workers\reminder-producer\producer.ts:26:export interface DispatchCommand {
src/workers\reminder-producer\producer.ts:40:export interface ProducerDeps {
src/workers\reminder-producer\producer.ts:41:  store: ReminderProducerStore;
src/workers\reminder-producer\producer.ts:51:export interface ProducerTickResult {
src/workers\reminder-producer\producer.ts:89:        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
src/workers\reminder-producer\producer.ts:92:          const { tenantId, occurrenceId } = parseGsi3Sk(row.GSI3SK);
src/modules\expiration\ports\expiration-store.ts:15:export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
src/modules\expiration\ports\expiration-store.ts:18:export interface Gsi1QueryInput {
src/modules\expiration\ports\expiration-store.ts:24:export interface ExpirationStore {
src/modules\reminder\application\reminder-materializer.ts:3: * interface + Ã‚Â§9.2 (occurrence shape/GSI3 keys) + Ã‚Â§8.3 (reacting to ItemDueDateChanged:
src/modules\reminder\application\reminder-materializer.ts:32:export interface MaterializeInput {
src/modules\reminder\application\reminder-materializer.ts:41:export interface MaterializeResult {
src/modules\reminder\application\reminder-materializer.ts:83:export class ReminderMaterializer {
src/modules\reminder\application\reminder-materializer.ts:179:        GSI3PK: gsi3.GSI3PK,
src/modules\reminder\application\reminder-materializer.ts:180:        GSI3SK: gsi3.GSI3SK,
src/modules\reminder\application\reminder-materializer.ts:216:              // GSI3PK/GSI3SK are intentionally left in place (occ.ts's builder is
src/modules\reminder\ports\reminder-store.ts:11: * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
src/modules\reminder\ports\reminder-store.ts:13: * tem permissÃƒÂ£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
src/modules\reminder\ports\reminder-store.ts:15: * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
src/modules\reminder\ports\reminder-store.ts:23:export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\ports\reminder-store.ts:27:export interface ReminderStore {
src/modules\reminder\ports\reminder-store.ts:36:export interface Gsi3QueryInput {
src/modules\reminder\ports\reminder-store.ts:41:export interface ReminderProducerStore {
src/modules\reminder\application\id-generator.ts:2:export interface ReminderIdGenerator {
src/modules\expiration\http\item-handlers.ts:14:export interface HttpRequest<TBody = unknown> {
src/modules\expiration\http\item-handlers.ts:24:export interface HttpResponse {
src/modules\expiration\http\item-handlers.ts:29:export interface ExpirationHttpDeps {
src/modules\expiration\application\id-generator.ts:2:export interface ExpirationIdGenerator {
src/modules\reminder\http\policy-handlers.ts:8:export interface HttpRequest<TBody = unknown> {
src/modules\reminder\http\policy-handlers.ts:17:export interface HttpResponse {
src/modules\reminder\http\policy-handlers.ts:22:export interface ReminderHttpDeps {
src/modules\expiration\application\expiration-service.ts:42:export interface ExpirationServiceDeps {
src/modules\expiration\application\expiration-service.ts:49:export interface DashboardQuery {
src/modules\expiration\application\expiration-service.ts:55:export class ExpirationService {
src/modules\reminder\domain\recurrence.ts:19:export type ZonedResolutionKind = "NORMAL" | "AMBIGUOUS" | "NONEXISTENT";
src/modules\reminder\domain\recurrence.ts:21:export interface ZonedResolution {
src/modules\reminder\domain\recurrence.ts:26:export interface LocalDateTimeParts {
src/modules\identity\application\quota.ts:10:export type QuotaType = "API_REQUEST" | "UPLOAD_BYTES" | "UPLOAD_COUNT" | "AI_CALL";
src/modules\identity\application\quota.ts:12:export interface TenantQuotaRecord {
src/modules\identity\application\quota.ts:29:export interface QuotaCheckInput {
src/modules\identity\application\quota.ts:45:export class TenantQuotaService {
src/modules\reminder\domain\gsi3-parse.ts:1:/** Parses `GSI3SK = TENANT#<tenantId>#OCCURRENCE#<occurrenceId>` back into its parts - used by the ReminderProducer to reconstruct tenant context from a GSI3 query result before the strongly-consistent base-item read (data-model.md Ã‚Â§3: "tenantId preservado... para reconstruÃƒÂ§ÃƒÂ£o segura do contexto"). */
src/modules\reminder\domain\gsi3-parse.ts:5:    throw new Error(`Malformed GSI3SK: ${gsi3sk}`);
src/modules\reminder\domain\reminder-occurrence.ts:3: * co-located under the parent ExpirationItem's partition) and Ã‚Â§3's GSI3 scheduler index
src/modules\reminder\domain\reminder-occurrence.ts:9:export type ReminderOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED" | "ACKED";
src/modules\reminder\domain\reminder-occurrence.ts:11:export interface ReminderOccurrence extends EntityKey {
src/modules\reminder\domain\reminder-occurrence.ts:24:  shard: string; // zero-padded shard number, string form as used in GSI3PK
src/modules\reminder\domain\reminder-occurrence.ts:32:  GSI3PK?: string; // present only while status === SCHEDULED (or CLAIMED, see note in materializer) - removed once TRIGGERED/CANCELLED so the scheduler index only ever contains live work
src/modules\reminder\domain\reminder-occurrence.ts:33:  GSI3SK?: string;
src/modules\reminder\domain\reminder-occurrence.ts:68: * Builds the GSI3 keys for a materialized occurrence. `shardCount`/`shardFnVersion` are
src/modules\reminder\domain\reminder-occurrence.ts:77:}): { GSI3PK: string; GSI3SK: string; shard: string } {
src/modules\reminder\domain\reminder-occurrence.ts:81:    GSI3PK: `DUE#${minuteBucket(input.scheduledAt)}#${shard}`,
src/modules\reminder\domain\reminder-occurrence.ts:82:    GSI3SK: `TENANT#${input.tenantId}#OCCURRENCE#${input.occurrenceId}`,
src/modules\reminder\domain\reminder-occurrence.ts:87:/** Enumerates the GSI3PK values to query for a given UTC minute across all shards of a given generation - used by both the producer (current minute + lookback) and reconciliation. */
src/modules\identity\application\resolve-request-context.ts:18:export interface ValidatedClaims {
src/modules\identity\application\resolve-request-context.ts:26:export interface ResolveRequestContextInput {
src/modules\identity\application\resolve-request-context.ts:32:export interface IdGenerator {
src/modules\identity\application\resolve-request-context.ts:37:export class RequestContextResolver {
src/modules\identity\domain\authorization.ts:7:export type Action =
src/modules\identity\domain\authorization.ts:22:export interface AuthorizedResource {
src/modules\identity\domain\authorization.ts:29:export interface AuthorizationInput {
src/modules\identity\domain\authorization.ts:40:export type Role = "OWNER" | "MEMBER" | "VIEWER";
src/modules\identity\domain\authorization.ts:61:export class AuthorizationDeniedError extends Error {
src/modules\reminder\domain\reminder-policy.ts:8:export type ReminderPolicyScope = "TEMPLATE" | "ITEM";
src/modules\reminder\domain\reminder-policy.ts:9:export type NotificationChannelKind = "EMAIL" | "WHATSAPP";
src/modules\reminder\domain\reminder-policy.ts:13:export interface ReminderTrigger {
src/modules\reminder\domain\reminder-policy.ts:21:export interface QuietHours {
src/modules\reminder\domain\reminder-policy.ts:26:export interface ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:35:export interface ReminderPolicy extends EntityKey, ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:53:export interface PutPolicyInput {
src/modules\expiration\domain\audit-event.ts:13:export type AuditAction = "CREATE" | "UPDATE" | "ARCHIVE" | "RENEW" | "DELETE";
src/modules\expiration\domain\audit-event.ts:15:export interface AuditEvent extends EntityKey {
src/modules\expiration\domain\audit-event.ts:44:export interface BuildAuditEventInput {
src/modules\reminder\domain\notification-intent.ts:8:export type NotificationIntentStatus = "PENDING" | "CANCELLED" | "DISPATCHED" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:9:export type NotificationIntentKind = "EXPIRATION_REMINDER" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:11:export interface NotificationIntent extends EntityKey {
src/modules\reminder\domain\shard-config.ts:14:export interface ShardGeneration {
src/modules\reminder\domain\shard-config.ts:21:export interface ShardConfig {
src/modules\identity\domain\request-context.ts:7:export interface RequestContext {
src/modules\identity\ports\identity-store.ts:8:export interface EntityKey {
src/modules\identity\ports\identity-store.ts:13:export interface IdentityStore {
src/modules\reminder\application\reminder-policy-service.ts:20:export interface ReminderPolicyServiceDeps {
src/modules\reminder\application\reminder-policy-service.ts:27:export class ReminderPolicyService {
src/modules\expiration\domain\expiration-item.ts:11:export type ExpirationItemStatus = "ACTIVE" | "ARCHIVED" | "RENEWED" | "DELETED";
src/modules\expiration\domain\expiration-item.ts:13:export interface ExpirationItem extends EntityKey {
src/modules\expiration\domain\expiration-item.ts:65:export interface CreateItemInput {
src/modules\expiration\domain\expiration-item.ts:80:export interface UpdateItemInput {
src/modules\expiration\domain\expiration-item.ts:94:export interface RenewItemInput {
src/modules\identity\http\test-route-handler.ts:13:export interface TestRouteHttpRequest {
src/modules\identity\http\test-route-handler.ts:19:export interface TestRouteHttpResponse {
src/modules\identity\http\test-route-handler.ts:24:export interface TestRouteDeps {
src/modules\identity\persistence\identity-mapping-repository.ts:6: * (the other is GSI3) Ã¢â‚¬â€ the lookup happens *before* tenantId is known.
src/modules\identity\persistence\identity-mapping-repository.ts:11:export interface IdentityMapping {
src/modules\identity\persistence\identity-mapping-repository.ts:25:export class IdentityMappingRepository {
src/modules\identity\persistence\user-repository.ts:13:export interface UserProfile {
src/modules\identity\persistence\user-repository.ts:29:export interface DeviceSession {
src/modules\identity\persistence\user-repository.ts:53:export class UserRepository {

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/reminder/ports/reminder-store.ts; Get-Content src/modules/expiration/ports/expiration-store.ts; Get-Content src/modules/identity/ports/identity-store.ts; Get-Content infra/lib/scoped-lambda-function.ts; Get-Content infra/lib/expiration-tracker-stack.ts; Get-Content src/workers/reminder-producer/producer.ts; Get-Content src/workers/reminder-dispatch/dispatch.ts; Get-Content src/workers/reminder-reconciliation/reconciliation.ts; Get-Content package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 368ms:
/**
 * DynamoDB surface the Reminder module needs - same SDK-agnostic port pattern as
 * src/modules/expiration/ports/expiration-store.ts (AGENTS.md Ãƒâ€šÃ‚Â§6: "qualquer mÃƒÆ’Ã‚Â³dulo futuro
 * com a mesma necessidade deve reusar esse formato de port, nÃƒÆ’Ã‚Â£o inventar um novo"). This
 * IS that reuse: identical shape (get/putIfAbsent/update/transactWrite/queryGsi1-style),
 * plus one addition this module needs and Expiration didn't - `queryByItem`, to list an
 * item's existing ReminderOccurrence rows (co-located under the item's own PK per
 * data-model.md Ãƒâ€šÃ‚Â§2) for materialization diffing, due-date-change cancellation and DST
 * reconciliation.
 *
 * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
 * - implementation-blueprint.md Ãƒâ€šÃ‚Â§9.2's isolation safeguard ("nenhuma rota tenant-facing
 * tem permissÃƒÆ’Ã‚Â£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
 * the policy service, materializer and HTTP handlers - all tenant-facing) has NO method
 * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
 * ReminderProducer worker, exposes it. test/integration/gsi3-isolation.test.ts asserts
 * this structurally (no tenant-facing dependency graph can reach queryGsi3).
 */
// 2026-08-19 (Engineering Maturity Review): imports from shared/dynamodb/occ.ts directly
// rather than re-exporting through expiration/ports/expiration-store.js - the previous
// indirection was an accidental cross-module ports->ports dependency (this module doesn't
// actually need anything Expiration-specific here, just the generic DynamoDB shapes).
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface ReminderStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** All rows under an item's own partition whose SK begins with "OCC#" (ReminderOccurrence rows). Strongly consistent - base-partition read, not a GSI (data-model.md Ãƒâ€šÃ‚Â§5). */
  queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string): Promise<T[]>;
}

export interface Gsi3QueryInput {
  gsi3pk: string;
}

/** Narrow port, injected ONLY into the ReminderProducer worker (see file header). */
export interface ReminderProducerStore {
  queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]>;
  /** Strongly consistent read of the base item, used by the producer to reconstruct tenant context before claiming (data-model.md Ãƒâ€šÃ‚Â§3: "tenantId preservado... para reconstruÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o segura do contexto"). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** Conditional SCHEDULED -> CLAIMED transition (implementation-blueprint.md Ãƒâ€šÃ‚Â§9.3 point 3). */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
}
/**
 * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
 * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
 * inject an implementation, so domain/application logic is unit-testable without live
 * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
 * data-model.md Ãƒâ€šÃ‚Â§5's load-bearing sentence requires item update + outbox record (and,
 * for M2, the AuditEvent) to commit in a single `TransactWriteItems` ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â the aggregate
 * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
 * be individually visible without the others.
 */
import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

// Re-exported for backward compatibility with existing importers (application/http layers
// of this and other modules) - canonical definitions now live in shared/dynamodb/occ.ts.
export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";

export interface Gsi1QueryInput {
  gsi1pk: string;
  ascending?: boolean;
  limit?: number;
}

export interface ExpirationStore {
  /** Strongly consistent single-item read (data-model.md Ãƒâ€šÃ‚Â§5: authorization/edition/pre-send reads must be consistent). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  /** Unconditional overwrite of an existing item - used only for non-OCC bookkeeping records (e.g. idempotency status transitions), never for ExpirationItem/AuditEvent. */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * Commits every entry atomically. Throws an error recognized by
   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
   * assume partial application.
   */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** Eventually consistent GSI1 query (data-model.md Ãƒâ€šÃ‚Â§3: vencimentos/dashboard). */
  queryGsi1<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi1QueryInput): Promise<T[]>;
}
/**
 * Minimal DynamoDB surface the identity module needs, following the SDK-agnostic port
 * pattern established in src/shared/idempotency/idempotency.ts (M0): callers inject an
 * implementation of this port, so the module is unit-testable without live AWS. M1's
 * infra layer (infra/lib) provisions the real table; src/modules/identity/persistence
 * wires a DocumentClient-backed adapter to this same port for production use.
 */
export interface EntityKey {
  PK: string;
  SK: string;
}

export interface IdentityStore {
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  update<T extends EntityKey>(item: T): Promise<void>;
}
/**
 * ScopedLambdaFunction ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â implementation-blueprint.md Ãƒâ€šÃ‚Â§17.1. "O construct gera grants
 * especÃƒÆ’Ã‚Â­ficos e falha synth quando o recurso solicitado nÃƒÆ’Ã‚Â£o corresponde a uma capability
 * conhecida."
 *
 * Judgment call (blueprint is schematic, not exhaustive, about capability granularity):
 * DynamoDB IAM cannot restrict by entity type (SK prefix) the way the blueprint's example
 * syntax (`tableAccess.readWriteKeys("ReminderOccurrence", "ExpirationItem")`) visually
 * suggests - IAM conditions operate on dynamodb:LeadingKeys (PK) at best, not on SK
 * patterns. This implementation is honest about that limit: the `entities` list is kept
 * as construct metadata (visible in `cdk synth`, useful for audit/least-privilege review)
 * but the actual IAM grant is table-level read/write via the well-tested CDK
 * `Table.grantReadWriteData`. Tighter per-entity IAM (via a Query/Scan condition on SK
 * prefix, not currently exposed by CDK's grant helpers) is deferred; noted in the M1 report.
 */
import * as lambda from "aws-cdk-lib/aws-lambda";
import * as iam from "aws-cdk-lib/aws-iam";
import * as sqs from "aws-cdk-lib/aws-sqs";
import { Duration } from "aws-cdk-lib";
import { Construct } from "constructs";
import type { ExpirationTrackerTable } from "./dynamo-table.js";

export interface AccessCapability {
  readonly kind: string;
  readonly description: string;
  grant(fn: lambda.Function): void;
}

export function tableAccessFor(table: ExpirationTrackerTable) {
  return {
    readWriteKeys(...entities: string[]): AccessCapability {
      return {
        kind: "table:read-write",
        description: `read/write on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadWriteData(fn),
      };
    },
    readKeys(...entities: string[]): AccessCapability {
      return {
        kind: "table:read",
        description: `read-only on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadData(fn),
      };
    },
    create(...entities: string[]): AccessCapability {
      return {
        kind: "table:create",
        description: `create-only on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadWriteData(fn), // PutItem-only IAM condition not expressible via CDK grant helpers; documented limitation
      };
    },
    /** GSI3 (scheduler) read-only - reserved EXCLUSIVELY for ReminderProducer
     * (data-model.md Ãƒâ€šÃ‚Â§3 / implementation-blueprint.md Ãƒâ€šÃ‚Â§9.2's isolation safeguard). Routed
     * through ExpirationTrackerTable.grantGsi3ReadTo, never table.grantReadData, so no
     * other function can acquire this permission via this helper. */
    gsi3Read(): AccessCapability {
      return {
        kind: "table:gsi3-read",
        description: "GSI3 (scheduler) read-only - ReminderProducer only",
        grant: (fn) => table.grantGsi3ReadTo(fn),
      };
    },
  };
}

export function queueAccessFor() {
  return {
    consume(queue: sqs.IQueue): AccessCapability {
      return {
        kind: "queue:consume",
        description: `consume from ${queue.queueName}`,
        grant: (fn) => queue.grantConsumeMessages(fn),
      };
    },
    send(queue: sqs.IQueue): AccessCapability {
      return {
        kind: "queue:send",
        description: `send to ${queue.queueName}`,
        grant: (fn) => queue.grantSendMessages(fn),
      };
    },
  };
}

export function appConfigAccessFor() {
  return {
    read(profileName: string): AccessCapability {
      return {
        kind: "appconfig:read",
        description: `read AppConfig profile ${profileName}`,
        grant: (fn) =>
          fn.addToRolePolicy(
            new iam.PolicyStatement({
              actions: ["appconfig:GetLatestConfiguration", "appconfig:StartConfigurationSession"],
              resources: ["*"], // AppConfig resource ARNs are per-deploy; scoped once the AppConfig construct exists (M1+ follow-up)
            }),
          ),
      };
    },
  };
}

const KNOWN_CAPABILITY_KINDS = new Set([
  "table:read-write",
  "table:read",
  "table:create",
  "table:gsi3-read",
  "queue:consume",
  "queue:send",
  "appconfig:read",
]);

export interface ScopedLambdaFunctionProps extends Omit<lambda.FunctionProps, "code"> {
  access: AccessCapability[];
  /** Defaults to bundling entryFile via NodejsFunction-equivalent asset if provided; else code must be set via `code` prop override (tests use inline code). */
  code?: lambda.Code;
}

/**
 * Every Lambda in this system MUST be constructed via ScopedLambdaFunction, never
 * `new lambda.Function` directly (implementation-blueprint.md Ãƒâ€šÃ‚Â§17.1) - it is the single
 * enforcement point for least-privilege IAM per function.
 */
export class ScopedLambdaFunction extends Construct {
  readonly function: lambda.Function;

  constructor(scope: Construct, id: string, props: ScopedLambdaFunctionProps) {
    super(scope, id);

    for (const capability of props.access) {
      if (!KNOWN_CAPABILITY_KINDS.has(capability.kind)) {
        // "falha synth quando o recurso solicitado nÃƒÆ’Ã‚Â£o corresponde a uma capability conhecida"
        throw new Error(
          `ScopedLambdaFunction(${id}): unknown access capability kind "${capability.kind}". ` +
            `Known kinds: ${[...KNOWN_CAPABILITY_KINDS].join(", ")}.`,
        );
      }
    }

    const { access, code, ...rest } = props;

    this.function = new lambda.Function(this, "Function", {
      timeout: Duration.seconds(10),
      memorySize: 256,
      tracing: lambda.Tracing.ACTIVE,
      ...rest,
      code: code ?? lambda.Code.fromInline("exports.handler = async () => ({ statusCode: 501 });"),
    });

    for (const capability of access) {
      capability.grant(this.function);
    }
  }
}
/**
 * M1 stack: wires DynamoDB, Cognito, the identity test route (ScopedLambdaFunction) and
 * the API skeleton together. Later milestones (M2+) add stacks/constructs for their own
 * modules, importing this table/auth rather than re-declaring them.
 */
import { Stack, type StackProps } from "aws-cdk-lib";
import type { Construct } from "constructs";
import * as lambda from "aws-cdk-lib/aws-lambda";
import { ExpirationTrackerTable } from "./dynamo-table.js";
import { ExpirationTrackerAuth, type MfaPolicy } from "./cognito.js";
import { ScopedLambdaFunction, tableAccessFor } from "./scoped-lambda-function.js";
import { ExpirationTrackerApi } from "./api.js";

export interface ExpirationTrackerStackProps extends StackProps {
  mfaPolicy?: MfaPolicy;
}

export class ExpirationTrackerStack extends Stack {
  readonly table: ExpirationTrackerTable;
  readonly auth: ExpirationTrackerAuth;
  readonly api: ExpirationTrackerApi;
  readonly remindersHandler: ScopedLambdaFunction;
  readonly reminderProducer: ScopedLambdaFunction;
  readonly reminderDispatch: ScopedLambdaFunction;
  readonly reminderReconciliation: ScopedLambdaFunction;

  constructor(scope: Construct, id: string, props: ExpirationTrackerStackProps = {}) {
    super(scope, id, props);

    this.table = new ExpirationTrackerTable(this, "Table");
    this.auth = new ExpirationTrackerAuth(this, "Auth", { mfaPolicy: props.mfaPolicy });

    const tableAccess = tableAccessFor(this.table);

    const testRouteHandler = new ScopedLambdaFunction(this, "TestPingHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("IdentityMapping", "User", "TenantQuota")],
    });

    // M2: ExpirationItem/AuditEvent CRUD+renew+dashboard, plus outbox writes for
    // ItemDueDateChanged. GSI1 (dashboard) is table-level read/write per
    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
    // header comment) - never grantGsi3ReadTo, which stays reserved for M3's
    // ReminderProducer.
    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ExpirationItem", "AuditEvent", "OutboxEvent", "IdempotencyRecord")],
    });

    this.api = new ExpirationTrackerApi(this, "Api", {
      auth: this.auth,
      testRouteHandler,
      itemsHandler,
    });

    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ReminderPolicy", "ReminderOccurrence")],
    });

    // ReminderProducer (Ãƒâ€šÃ‚Â§9.3) is the ONLY function in the system granted gsi3Read() - the
    // narrow capability routed through ExpirationTrackerTable.grantGsi3ReadTo, which
    // itself never appears on any tenant-facing function (test/infra/reminder-engine.test.ts
    // asserts this at the synthesized-template level).
    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ReminderOccurrence"), tableAccess.gsi3Read()],
    });

    // ReminderDispatch (Ãƒâ€šÃ‚Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
    // one TransactWriteItems - needs write access to occurrences/intents/outbox/idempotency
    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [
        tableAccess.readWriteKeys(
          "ReminderOccurrence",
          "NotificationIntent",
          "OutboxEvent",
          "IdempotencyRecord",
        ),
        tableAccess.readKeys("ExpirationItem", "ReminderPolicy"),
      ],
    });

    // ReminderReconciliation (Ãƒâ€šÃ‚Â§9.5): single job for claim-expiry revert + DST
    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
    // the scheduler index).
    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [
        tableAccess.readWriteKeys("ReminderOccurrence"),
        tableAccess.readKeys("ExpirationItem", "ReminderPolicy"),
      ],
    });
  }
}
/**
 * ReminderProducer core logic - implementation-blueprint.md Ãƒâ€šÃ‚Â§9.3. Pure(ish) - takes an
 * injected `ReminderProducerStore` and clock, no AWS SDK/Lambda runtime dependency, so it
 * is directly unit/integration-testable with a fake clock and fake GSI3 index (per the
 * task's requirement to test time-sensitive logic without real wall-clock waiting).
 *
 * Steps (Ãƒâ€šÃ‚Â§9.3):
 *  1. read active shard generations (current + still-active legacy, Ãƒâ€šÃ‚Â§9.2's reshard runbook);
 *  2. query GSI3 across N shards of minute M AND a lookback window [M-lookback, M] (default
 *     5 minutes) - "sem lookback, 'o prÃƒÆ’Ã‚Â³ximo tick reprocessa' ÃƒÆ’Ã‚Â© falso";
 *  3. per eligible occurrence, conditional SCHEDULED -> CLAIMED with a short claim
 *     `claimExpiresAt`, and emit one `reminder.dispatch.v1` command;
 *  4. partial-batch-failure handling: only failed entries are surfaced in `failed`, callers
 *     retry just those (never the whole batch);
 *  5. returns counters for `scheduler_lag_seconds`/per-shard/lookback-depth metrics (left to
 *     the Lambda handler to emit via SecureLogger/EMF - this module stays observability-agnostic).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { gsi3PartitionsForMinute } from "../../modules/reminder/domain/reminder-occurrence.js";
import { parseGsi3Sk } from "../../modules/reminder/domain/gsi3-parse.js";
import { activeGenerations, type ShardConfig } from "../../modules/reminder/domain/shard-config.js";
import { isTransactionCanceled } from "../../modules/reminder/ports/reminder-store.js";
import type { ReminderProducerStore } from "../../modules/reminder/ports/reminder-store.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";

export interface DispatchCommand {
  commandType: "reminder.dispatch.v1";
  tenantId: string;
  deduplicationKey: string;
  data: {
    itemId: string;
    occurrenceId: string;
    occurrenceVersion: number;
    scheduledAt: string;
    itemVersion: number;
    policyVersion: number;
  };
}

export interface ProducerDeps {
  store: ReminderProducerStore;
  shardConfig: ShardConfig;
  tableName: string;
  now: () => string;
  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (Ãƒâ€šÃ‚Â§9.5). */
  claimTtlMs?: number;
  /** Lookback window in minutes, inclusive of the current minute (implementation-blueprint.md Ãƒâ€šÃ‚Â§9.3 example: [M-5min, M]). */
  lookbackMinutes?: number;
}

export interface ProducerTickResult {
  claimed: DispatchCommand[];
  failed: { occurrenceId: string; tenantId: string; error: unknown }[];
  scanned: number;
  minutesScanned: string[];
  shardPartitionsScanned: number;
}

function minuteFloor(d: Date): Date {
  return new Date(Math.floor(d.getTime() / 60_000) * 60_000);
}

/** Runs one producer tick for wall-clock minute `tickMinute` (already floored to the minute by the caller/Lambda trigger). */
export async function runProducerTick(deps: ProducerDeps, tickMinute: Date): Promise<ProducerTickResult> {
  const generations = activeGenerations(deps.shardConfig, deps.now());
  const lookback = deps.lookbackMinutes ?? 5;
  const claimTtlMs = deps.claimTtlMs ?? 2 * 60_000;

  const minutesToScan: Date[] = [];
  for (let i = lookback; i >= 0; i--) {
    minutesToScan.push(minuteFloor(new Date(tickMinute.getTime() - i * 60_000)));
  }

  const claimed: DispatchCommand[] = [];
  const failed: { occurrenceId: string; tenantId: string; error: unknown }[] = [];
  let scanned = 0;
  let partitions = 0;

  // Dedup guard: the same occurrence could show up in more than one scanned minute only if
  // materialization ever wrote it under a stale bucket, which shouldn't happen - but a
  // Set guards against double-claiming within a single tick regardless.
  const seen = new Set<string>();

  for (const minute of minutesToScan) {
    for (const generation of generations) {
      const partitionKeys = gsi3PartitionsForMinute(minute, generation.shardCount);
      partitions += partitionKeys.length;
      for (const gsi3pk of partitionKeys) {
        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
        for (const row of rows) {
          scanned += 1;
          const { tenantId, occurrenceId } = parseGsi3Sk(row.GSI3SK);
          if (seen.has(occurrenceId)) continue;
          seen.add(occurrenceId);

          try {
            const occurrence = await deps.store.get<ReminderOccurrence>({ PK: row.PK, SK: row.SK });
            if (!occurrence || occurrence.status !== "SCHEDULED") {
              // Already claimed/cancelled/triggered by a prior tick or race - not a failure.
              continue;
            }

            const claimExpiresAt = new Date(Date.parse(deps.now()) + claimTtlMs).toISOString();
            await deps.store.transactWrite([
              {
                Update: buildVersionedUpdate({
                  tableName: deps.tableName,
                  key: { PK: row.PK, SK: row.SK },
                  tenantId,
                  expectedVersion: occurrence.version,
                  set: { status: "CLAIMED", claimedAt: deps.now(), claimExpiresAt },
                }),
              },
            ]);

            claimed.push({
              commandType: "reminder.dispatch.v1",
              tenantId,
              deduplicationKey: `${tenantId}|${occurrenceId}|${occurrence.version + 1}`,
              data: {
                itemId: occurrence.itemId,
                occurrenceId,
                occurrenceVersion: occurrence.version + 1,
                scheduledAt: occurrence.scheduledAt,
                itemVersion: occurrence.itemVersion,
                policyVersion: occurrence.policyVersion,
              },
            });
          } catch (err) {
            if (isTransactionCanceled(err)) {
              // Lost the claim race to a concurrent producer tick - not a failure to retry.
              continue;
            }
            failed.push({ occurrenceId, tenantId, error: err });
          }
        }
      }
    }
  }

  return {
    claimed,
    failed,
    scanned,
    minutesScanned: minutesToScan.map((d) => d.toISOString()),
    shardPartitionsScanned: partitions,
  };
}
/**
 * Reminder dispatch worker core logic - implementation-blueprint.md Ãƒâ€šÃ‚Â§9.4. Consumes one
 * `reminder.dispatch.v1` command (produced by the ReminderProducer, Ãƒâ€šÃ‚Â§9.3) and performs the
 * CLAIMED -> TRIGGERED transition, creating exactly one `NotificationIntent` idempotently.
 *
 * Validation order per Ãƒâ€šÃ‚Â§9.4: occurrence must be CLAIMED (not SCHEDULED directly - the claim
 * prevents double-processing by two concurrent dispatch workers); item must be ACTIVE;
 * versions must match; schedule must be within tolerance; policy must still exist. If any
 * check fails, the occurrence is conditionally cancelled (CLAIMED -> CANCELLED) instead of
 * silently dropped, and repair is left to reconciliation (Ãƒâ€šÃ‚Â§9.5) - never a new ad-hoc
 * materialization from inside dispatch.
 *
 * On success, everything commits in ONE TransactWriteItems (Ãƒâ€šÃ‚Â§9.4: "corrige a lacuna de
 * durabilidade... antes o evento era tratado como reconstruÃƒÆ’Ã‚Â­vel sem outbox"):
 * NotificationIntent Put + occurrence CLAIMED->TRIGGERED Update + idempotency record Put
 * (`tenantId|occurrenceId`, Ãƒâ€šÃ‚Â§9.4's exact key for NotificationIntentCreated consumers) +
 * outbox `notification.intent-created.v1` Put.
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { appendToTransaction } from "../../shared/outbox/outbox.js";
import { buildIdempotencyKey } from "../../shared/idempotency/idempotency.js";
import type { DomainEvent } from "../../shared/contracts/events.js";
import { itemKey } from "../../modules/expiration/domain/expiration-item.js";
import { policyKey, type ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import { intentKey, type NotificationIntent } from "../../modules/reminder/domain/notification-intent.js";
import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../../modules/reminder/ports/reminder-store.js";
import type { DispatchCommand } from "../reminder-producer/producer.js";

const NOTIFICATION_INTENT_CREATED = "notification.intent-created.v1";

export interface DispatchDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  newIntentId: () => string;
  newEventId: () => string;
  correlationId: () => string;
  /** Max allowed lag between scheduledAt and now before a claimed occurrence is considered stale (default 30 minutes - generous vs. the claim TTL, covers legitimate SQS/Lambda retry delay). */
  toleranceMs?: number;
}

export type DispatchOutcome =
  | { kind: "TRIGGERED"; intent: NotificationIntent }
  | { kind: "ALREADY_TRIGGERED" }
  | { kind: "CANCELLED_STALE"; reason: string }
  | { kind: "SKIPPED_NOT_CLAIMED" };

/** Looks up the occurrence by (itemId, occurrenceId) - occurrences are co-located under the item's own partition (data-model.md Ãƒâ€šÃ‚Â§2), but the command doesn't carry the SK's scheduledAt segment, so the worker queries the item's occurrences and finds the match by occurrenceId. Kept here rather than in ReminderStore to avoid growing the port for a single caller. */
async function findOccurrence(store: ReminderStore, tenantId: string, itemId: string, occurrenceId: string): Promise<ReminderOccurrence | undefined> {
  const rows = await store.queryByItem<ReminderOccurrence>(tenantId, itemId);
  return rows.find((r) => r.occurrenceId === occurrenceId);
}

export async function dispatchOccurrence(deps: DispatchDeps, command: DispatchCommand): Promise<DispatchOutcome> {
  const { tenantId } = command;
  const { itemId, occurrenceId, itemVersion, policyVersion } = command.data;

  const occurrence = await findOccurrence(deps.store, tenantId, itemId, occurrenceId);
  if (!occurrence) {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }
  if (occurrence.status === "TRIGGERED") {
    return { kind: "ALREADY_TRIGGERED" };
  }
  if (occurrence.status !== "CLAIMED") {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }

  const item = await deps.store.get<{ PK: string; SK: string; status: string; version: number }>(itemKey(tenantId, itemId));
  const policy = await deps.store.get<ReminderPolicy>(policyKey(tenantId, occurrence.policyId));

  const toleranceMs = deps.toleranceMs ?? 30 * 60_000;
  const withinTolerance = Math.abs(Date.parse(deps.now()) - Date.parse(occurrence.scheduledAt)) <= toleranceMs;

  const stale =
    !item ||
    item.status !== "ACTIVE" ||
    item.version !== itemVersion ||
    occurrence.itemVersion !== itemVersion ||
    !policy ||
    policy.version !== policyVersion ||
    !policy.enabled ||
    !withinTolerance;

  if (stale) {
    const reason = !item
      ? "ITEM_NOT_FOUND"
      : item.status !== "ACTIVE"
        ? "ITEM_NOT_ACTIVE"
        : item.version !== itemVersion || occurrence.itemVersion !== itemVersion
          ? "ITEM_VERSION_MISMATCH"
          : !policy || !policy.enabled
            ? "POLICY_GONE_OR_DISABLED"
            : policy.version !== policyVersion
              ? "POLICY_VERSION_MISMATCH"
              : "OUT_OF_TOLERANCE";

    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId,
            expectedVersion: occurrence.version,
            set: { status: "CANCELLED" },
          }),
        },
      ]);
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
    return { kind: "CANCELLED_STALE", reason };
  }

  const now = deps.now();
  const intentId = deps.newIntentId();
  const intent: NotificationIntent = {
    ...intentKey(tenantId, intentId),
    entityType: "NotificationIntent",
    intentId,
    tenantId,
    kind: "EXPIRATION_REMINDER",
    itemId,
    occurrenceId,
    itemVersion,
    policyId: occurrence.policyId,
    policyVersion,
    scheduledAt: occurrence.scheduledAt,
    requestedChannels: policy.channels.filter((c) => !(policy.optOutChannels ?? []).includes(c)),
    status: "PENDING",
    supersedesIntentId: null,
    correctionReason: null,
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const entries: TransactWriteEntry[] = [
    { Put: { TableName: deps.tableName, Item: { ...intent }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
    {
      Update: buildVersionedUpdate({
        tableName: deps.tableName,
        key: { PK: occurrence.PK, SK: occurrence.SK },
        tenantId,
        expectedVersion: occurrence.version,
        set: { status: "TRIGGERED" },
      }),
    },
  ];

  // Idempotency record for NotificationIntentCreated consumers (Ãƒâ€šÃ‚Â§9.4: "tenantId|occurrenceId").
  const idem = buildIdempotencyKey(deps.tableName, tenantId, "reminder.dispatch", occurrenceId);
  entries.push({
    Put: {
      TableName: deps.tableName,
      Item: {
        PK: idem.PK,
        SK: idem.SK,
        entityType: "IdempotencyRecord",
        tenantId,
        operation: "reminder.dispatch",
        requestHash: occurrenceId,
        status: "COMPLETED",
        responseRef: intentId,
        expiresAt: new Date(Date.parse(now) + 7 * 24 * 60 * 60_000).toISOString(),
        createdAt: now,
        completedAt: now,
      },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });

  const event: DomainEvent = {
    specVersion: "1.0",
    eventId: deps.newEventId(),
    eventType: NOTIFICATION_INTENT_CREATED,
    source: "expiration-tracker.reminder",
    occurredAt: now,
    correlationId: deps.correlationId(),
    tenantId,
    actor: { type: "SYSTEM" },
    aggregate: { type: "NotificationIntent", id: intentId, version: 1 },
    data: {
      intentId,
      kind: "EXPIRATION_REMINDER",
      itemId,
      occurrenceId,
      itemVersion,
      policyId: occurrence.policyId,
      policyVersion,
      scheduledAt: occurrence.scheduledAt,
      requestedChannels: intent.requestedChannels,
      status: "PENDING",
      supersedesIntentId: null,
      correctionReason: null,
    },
  };
  appendToTransaction(entries as never, deps.tableName, event);

  try {
    await deps.store.transactWrite(entries);
  } catch (err) {
    if (isTransactionCanceled(err)) {
      // Duplicate delivery of the same command (SQS at-least-once) racing a prior success -
      // either the idempotency Put or the occurrence's CLAIMED condition already advanced.
      return { kind: "ALREADY_TRIGGERED" };
    }
    throw err;
  }

  return { kind: "TRIGGERED", intent };
}
/**
 * Reconciliation - implementation-blueprint.md Ãƒâ€šÃ‚Â§9.5, run as ONE job/Lambda serving TWO
 * purposes (per the task brief: "don't build two separate reconciliation paths"), both
 * documented in Ãƒâ€šÃ‚Â§9.3 point 4 ("claims expirados... via reconciliaÃƒÆ’Ã‚Â§ÃƒÆ’Ã‚Â£o, mesmo job da Ãƒâ€šÃ‚Â§9.5")
 * and Ãƒâ€šÃ‚Â§9.5 itself (DST):
 *
 *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
 *      `claimExpiresAt` (dispatch worker crashed/timed out before completing) reverts to
 *      SCHEDULED, conditionally, so the next producer tick's lookback window picks it up
 *      again;
 *  (b) DST-affected re-evaluation (Ãƒâ€šÃ‚Â§9.5 steps 1-6): for each (item, enabled policy) pair
 *      in the reconciliation batch, recompute the expected schedule from the policy's
 *      IANA timeZone + rule against a `[now, now+7d]` window, compare to what's actually
 *      materialized, cancel divergent SCHEDULED occurrences and materialize missing ones
 *      idempotently - TRIGGERED/ACKED history is never touched.
 *
 * Both passes reuse the same store/materializer/clock and are invoked together by
 * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
 * unblocks the producer sooner).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
import { ReminderMaterializer } from "../../modules/reminder/application/reminder-materializer.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import type { ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ShardConfig } from "../../modules/reminder/domain/shard-config.js";

export interface ReconciliationDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  shardConfig: ShardConfig;
}

/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
 * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
 * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
 * wiring); tests/callers supply the batch directly. */
export interface DstReconciliationCandidate {
  tenantId: string;
  itemId: string;
  itemVersion: number;
  itemDueDate: string;
  policy: ReminderPolicy;
}

export interface ReconciliationResult {
  claimsReverted: number;
  dstCancelled: number;
  dstCreated: number;
  dstDivergences: number;
}

/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
export async function reconcileExpiredClaims(
  deps: ReconciliationDeps,
  candidateOccurrences: ReminderOccurrence[],
): Promise<number> {
  let reverted = 0;
  const now = deps.now();
  for (const occurrence of candidateOccurrences) {
    if (occurrence.status !== "CLAIMED") continue;
    if (!occurrence.claimExpiresAt || occurrence.claimExpiresAt > now) continue;
    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId: occurrence.tenantId,
            expectedVersion: occurrence.version,
            set: { status: "SCHEDULED" },
          }),
        },
      ]);
      reverted += 1;
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
  }
  return reverted;
}

/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (Ãƒâ€šÃ‚Â§9.5 steps 1-6). */
export async function reconcileDst(
  deps: ReconciliationDeps,
  candidates: DstReconciliationCandidate[],
): Promise<{ cancelled: number; created: number; divergences: number }> {
  const materializer = new ReminderMaterializer(deps.store, deps.tableName, deps.now);
  const windowStart = Date.parse(deps.now());
  const windowEnd = windowStart + 7 * 24 * 60 * 60_000;

  let cancelled = 0;
  let created = 0;
  let divergences = 0;

  for (const candidate of candidates) {
    if (!candidate.policy.enabled) continue;

    const existing = await deps.store.queryByItem<ReminderOccurrence>(candidate.tenantId, candidate.itemId);
    const liveExisting = existing.filter(
      (o) =>
        o.policyId === candidate.policy.policyId &&
        o.itemVersion === candidate.itemVersion &&
        o.policyVersion === candidate.policy.version &&
        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
    );

    // Expected schedule per trigger, recomputed fresh from the IANA rule (never a frozen offset).
    const expectedByTrigger = new Map<string, { scheduledAtUtc: string }>();
    for (const trigger of candidate.policy.triggers) {
      const schedule = materializer.computeSchedule({
        itemDueDate: candidate.itemDueDate,
        offsetIso: trigger.offsetIso,
        localTime: trigger.localTime,
        timeZone: candidate.policy.timeZone,
        quietHours: candidate.policy.quietHours,
      });
      const t = Date.parse(schedule.scheduledAtUtc);
      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
      expectedByTrigger.set(trigger.triggerId, { scheduledAtUtc: schedule.scheduledAtUtc });
    }

    // Cancel occurrences whose triggerId is no longer expected, or whose scheduledAt now
    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
    // liveExisting already excludes those statuses).
    for (const occurrence of liveExisting) {
      const expected = expectedByTrigger.get(occurrence.triggerId);
      const diverges = !expected || expected.scheduledAtUtc !== occurrence.scheduledAt;
      if (!diverges) continue;
      divergences += 1;
      try {
        await deps.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: deps.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: candidate.tenantId,
              expectedVersion: occurrence.version,
              set: { status: "CANCELLED" },
            }),
          },
        ]);
        cancelled += 1;
      } catch (err) {
        if (!isTransactionCanceled(err)) throw err;
      }
    }

    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
    // materialize() itself is idempotent (putIfAbsent keyed by the deterministic
    // occurrenceId hash), so re-running it for triggers that already matched is a safe no-op.
    const stillMissing = candidate.policy.triggers.some((t) => {
      const expected = expectedByTrigger.get(t.triggerId);
      if (!expected) return false;
      return !liveExisting.some((o) => o.triggerId === t.triggerId && o.scheduledAt === expected.scheduledAtUtc);
    });
    if (stillMissing) {
      const result = await materializer.materialize({
        tenantId: candidate.tenantId,
        itemId: candidate.itemId,
        itemVersion: candidate.itemVersion,
        itemDueDate: candidate.itemDueDate,
        policy: candidate.policy,
        shardConfig: deps.shardConfig,
      });
      created += result.created.length;
    }
  }

  return { cancelled, created, divergences };
}

export async function runReconciliation(
  deps: ReconciliationDeps,
  input: { expiredClaimCandidates: ReminderOccurrence[]; dstCandidates: DstReconciliationCandidate[] },
): Promise<ReconciliationResult> {
  const claimsReverted = await reconcileExpiredClaims(deps, input.expiredClaimCandidates);
  const dst = await reconcileDst(deps, input.dstCandidates);
  return { claimsReverted, dstCancelled: dst.cancelled, dstCreated: dst.created, dstDivergences: dst.divergences };
}
{
  "name": "expiration-tracker",
  "version": "0.0.0",
  "private": true,
  "description": "Micro-SaaS de controle de vencimentos/renovacoes - guardrails e contratos (M0)",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "validate-schemas": "tsx scripts/validate-schemas.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "@vitest/coverage-v8": "^1.6.0",
    "aws-cdk-lib": "2.265.0",
    "aws-jwt-verify": "5.2.1",
    "constructs": "10.8.1",
    "dependency-cruiser": "17.4.3",
    "eslint": "^8.57.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "ajv": "^8.16.0",
    "ajv-formats": "^2.1.1",
    "ulid": "^2.3.0"
  }
}

codex
# Proposta tÃƒÂ©cnica Ã¢â‚¬â€ milestone Runtime Real / G8

## 1. DecisÃƒÂ£o central

O milestone deve transformar o fluxo atual em runtime implantÃƒÂ¡vel sem contaminar os ports com tipos do AWS SDK:

```text
EventBridge Rule
      Ã¢â€â€š
      Ã¢â€“Â¼
ReminderProducer Lambda
      Ã¢â€â€š
      Ã¢â€Å“Ã¢â€â‚¬ TransactWrite:
      Ã¢â€â€š    SCHEDULED Ã¢â€ â€™ CLAIMED
      Ã¢â€â€š    + registro durÃƒÂ¡vel DispatchOutbox
      Ã¢â€“Â¼
DynamoDB Stream / relay
      Ã¢â€â€š
      Ã¢â€“Â¼
ReminderDispatchQueue
      Ã¢â€â€š
      Ã¢â€“Â¼
ReminderDispatch Lambda
      Ã¢â€â€š
      Ã¢â€Å“Ã¢â€â‚¬ revalida estado/versionamento
      Ã¢â€â€Ã¢â€â‚¬ TransactWrite:
           CLAIMED Ã¢â€ â€™ TRIGGERED
           + NotificationIntent
           + OutboxEvent
           + IdempotencyRecord
```

A gravaÃƒÂ§ÃƒÂ£o durÃƒÂ¡vel intermediÃƒÂ¡ria ÃƒÂ© necessÃƒÂ¡ria. Fazer `TransactWrite(CLAIMED)` e depois `SendMessage` diretamente no handler mantÃƒÂ©m uma janela de perda:

1. DynamoDB confirma o claim.
2. Lambda cai antes de enviar ÃƒÂ  SQS.
3. A ocorrÃƒÂªncia fica `CLAIMED`, mas nÃƒÂ£o existe mensagem.

A reconciliaÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria nÃƒÂ£o fecha adequadamente essa janela: quando reverter o claim, a ocorrÃƒÂªncia poderÃƒÂ¡ estar fora do lookback de cinco minutos do producer. Portanto, fila e DLQ, isoladamente, nÃƒÂ£o bastam para fechar o P1 #1.

Minha recomendaÃƒÂ§ÃƒÂ£o ÃƒÂ© um registro `DispatchOutbox` gravado atomicamente com o claim e publicado por DynamoDB Streams. Uma alternativa aceitÃƒÂ¡vel ÃƒÂ© EventBridge Pipes entre o stream e a SQS, desde que seja demonstrado que filtragem, transformaÃƒÂ§ÃƒÂ£o do stream record e retries funcionam no ambiente AWS real. Eu prefiro um pequeno `DispatchOutboxRelay` Lambda porque oferece controle explÃƒÂ­cito de schema, observabilidade, partial batch failure e evoluÃƒÂ§ÃƒÂ£o do contrato.

## 2. DependÃƒÂªncias

Adicionar como dependÃƒÂªncias de runtime:

```json
{
  "@aws-sdk/client-dynamodb": "versÃƒÂ£o exata",
  "@aws-sdk/lib-dynamodb": "versÃƒÂ£o exata",
  "@aws-sdk/client-sqs": "versÃƒÂ£o exata"
}
```

E como dependÃƒÂªncias de desenvolvimento:

```json
{
  "esbuild": "versÃƒÂ£o exata",
  "@types/aws-lambda": "versÃƒÂ£o exata",
  "testcontainers": "versÃƒÂ£o exata"
}
```

Escolhas:

- `DynamoDBClient`: transporte, configuraÃƒÂ§ÃƒÂ£o de retry e endpoint.
- `DynamoDBDocumentClient`: objetos JavaScript, compatÃƒÂ­vel com os builders existentes.
- `SQSClient`: publicaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita pelo relay.
- `esbuild`: bundling determinÃƒÂ­stico dos entrypoints ESM para Node.js 20.
- VersÃƒÂµes exatas, nÃƒÂ£o ranges, mantendo lockfile e `npm ci`.

ConfiguraÃƒÂ§ÃƒÂ£o do `DynamoDBDocumentClient`:

```ts
DynamoDBDocumentClient.from(client, {
  marshallOptions: {
    removeUndefinedValues: true,
    convertClassInstanceToMap: false,
  },
})
```

NÃƒÂ£o habilitaria conversÃƒÂµes numÃƒÂ©ricas silenciosas. Campos de versÃƒÂ£o precisam continuar sendo nÃƒÂºmeros e valores grandes devem ser avaliados explicitamente antes de usar `wrapNumbers`.

O cliente deve usar retry adaptativo ou standard com nÃƒÂºmero pequeno e explÃƒÂ­cito de tentativas, por exemplo trÃƒÂªs. O timeout total precisa caber com folga no timeout da Lambda; nÃƒÂ£o se deve deixar o SDK consumir todo o orÃƒÂ§amento da invocaÃƒÂ§ÃƒÂ£o.

## 3. Estrutura dos adapters

```text
src/
  runtime/
    aws/
      dynamodb/
        client.ts
        errors.ts
        execute-transaction.ts
        identity-store-dynamodb.ts
        expiration-store-dynamodb.ts
        reminder-store-dynamodb.ts
        reminder-producer-store-dynamodb.ts
        reconciliation-candidate-source-dynamodb.ts
      sqs/
        dispatch-command-publisher.ts

    handlers/
      test-ping-handler.ts
      items-handler.ts
      reminders-handler.ts
      reminder-producer-handler.ts
      reminder-dispatch-handler.ts
      reminder-reconciliation-handler.ts
      dispatch-outbox-relay-handler.ts

    composition/
      identity.ts
      expiration.ts
      reminder.ts
```

Um adapter por port:

- `DynamoDbIdentityStore implements IdentityStore`
- `DynamoDbExpirationStore implements ExpirationStore`
- `DynamoDbReminderStore implements ReminderStore`
- `DynamoDbReminderProducerStore implements ReminderProducerStore`

NÃƒÂ£o criaria uma classe Ã¢â‚¬Å“universalÃ¢â‚¬Â pÃƒÂºblica implementando todos os ports. Isso permitiria injetar acidentalmente acesso ao GSI3 em cÃƒÂ³digo tenant-facing.

Pode haver helpers internos compartilhados para `GetCommand`, `PutCommand`, `UpdateCommand` e `TransactWriteCommand`, mas o mÃƒÂ©todo `queryGsi3` existe somente em `reminder-producer-store-dynamodb.ts`.

### Mapeamento das operaÃƒÂ§ÃƒÂµes

| Port | Comando AWS |
|---|---|
| `get` | `GetCommand`, `ConsistentRead: true` |
| `putIfAbsent` | `PutCommand` com condiÃƒÂ§ÃƒÂ£o existente; retorna `false` apenas para falha condicional |
| `update` | `PutCommand` de overwrite, restrito aos usos jÃƒÂ¡ documentados pelo port |
| `transactWrite` | `TransactWriteCommand` usando diretamente os builders existentes |
| `queryGsi1` | `QueryCommand`, `IndexName: GSI1`, eventual consistency |
| `queryByItem` | `QueryCommand` na tabela base, `PK = :pk AND begins_with(SK, :occ)` e leitura consistente |
| `queryGsi3` | `QueryCommand`, `IndexName: GSI3`, somente no adapter privilegiado |

PaginaÃƒÂ§ÃƒÂ£o deve ser completa por padrÃƒÂ£o. `limit` representa limite lÃƒÂ³gico total, nÃƒÂ£o apenas o `Limit` de uma ÃƒÂºnica pÃƒÂ¡gina.

### TraduÃƒÂ§ÃƒÂ£o de erros

O adapter deve traduzuzir, sem esconder a causa:

- `ConditionalCheckFailedException` em `putIfAbsent` Ã¢â€ â€™ `false`.
- `TransactionCanceledException` Ã¢â€ â€™ erro reconhecÃƒÂ­vel por `isTransactionCanceled`.
- throttling, timeout, falha 5xx e indisponibilidade Ã¢â€ â€™ `AppError` retryable.
- validation error, item/schema invÃƒÂ¡lido Ã¢â€ â€™ erro terminal.
- `AccessDeniedException` Ã¢â€ â€™ erro terminal operacional e alarme; nÃƒÂ£o deve virar retry infinito.

Preservar `$metadata.requestId`, nome da operaÃƒÂ§ÃƒÂ£o e causa no log, sem registrar item completo nem dados pessoais.

## 4. Lacuna da reconciliaÃƒÂ§ÃƒÂ£o

O handler de reconciliaÃƒÂ§ÃƒÂ£o atual nÃƒÂ£o consegue operar sozinho: `runReconciliation` recebe arrays de candidatos, mas `ReminderStore` nÃƒÂ£o oferece uma consulta que os produza.

Executar a Lambda diariamente com arrays vazios seria outro false-green.

O milestone precisa introduzir um port estreito, por exemplo:

```ts
interface ReconciliationCandidateSource {
  listExpiredClaims(input: PageInput): AsyncIterable<ReminderOccurrence>;
  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
}
```

O adapter consultaria o GSI6 ou o ÃƒÂ­ndice efetivamente normativo. Isso ÃƒÂ© integraÃƒÂ§ÃƒÂ£o mÃƒÂ­nima necessÃƒÂ¡ria, nÃƒÂ£o redesenho do domÃƒÂ­nio.

Se o GSI6 ainda nÃƒÂ£o representa claims expirados e polÃƒÂ­ticas ativas de maneira consultÃƒÂ¡vel, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo estÃƒÂ¡ apenas agendado, nÃƒÂ£o funcional.

AlÃƒÂ©m da execuÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria de DST, recuperaÃƒÂ§ÃƒÂ£o de claims precisa ter cadÃƒÂªncia curta. Recomendo:

- reconciliaÃƒÂ§ÃƒÂ£o de claims expirados: a cada minuto ou cinco minutos;
- reconciliaÃƒÂ§ÃƒÂ£o DST/materializaÃƒÂ§ÃƒÂ£o: diÃƒÂ¡ria.

Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durÃƒÂ¡vel, a reconciliaÃƒÂ§ÃƒÂ£o curta continua ÃƒÂºtil para reparar estados excepcionais.

## 5. Handlers Lambda

Cada arquivo em `src/runtime/handlers` ÃƒÂ© um composition root:

1. valida variÃƒÂ¡veis de ambiente;
2. instancia clientes e adapters fora do handler para reuso em warm starts;
3. converte o evento AWS para os tipos internos;
4. invoca a lÃƒÂ³gica existente;
5. converte resultado/erro para o protocolo da origem;
6. registra mÃƒÂ©tricas e logs via `SecureLogger`.

Nenhuma lÃƒÂ³gica de negÃƒÂ³cio deve morar no wrapper Lambda.

VariÃƒÂ¡veis mÃƒÂ­nimas:

```text
TABLE_NAME
DISPATCH_QUEUE_URL
SHARD_CONFIG
PRODUCER_LOOKBACK_MINUTES
CLAIM_TTL_SECONDS
LOG_LEVEL
```

Para HTTP, os handlers reais devem interpretar API Gateway HTTP API v2, extrair claims jÃƒÂ¡ validadas pelo authorizer, construir `RequestContext` e mapear `AppError` para status HTTP consistente.

Para schedules, validar `source`, `detail-type` e um campo explÃƒÂ­cito `mode`. Evento inesperado ÃƒÂ© terminal.

Para SQS, validar cada body contra `schemas/queues/reminder.dispatch.v1.json` antes de executar o domÃƒÂ­nio.

## 6. Bundling

Eu estenderia `ScopedLambdaFunction` com `entry` e `handlerExport`, mantendo-o como ÃƒÂºnico ponto de criaÃƒÂ§ÃƒÂ£o de Lambda. Internamente, usaria `lambda.Code.fromAsset` com bundling `esbuild`, ou incorporaria a semÃƒÂ¢ntica de `NodejsFunction` sem permitir que stacks criem Lambdas por fora do construct.

Exemplo conceitual:

```ts
new ScopedLambdaFunction(this, "ReminderDispatch", {
  entry: "src/runtime/handlers/reminder-dispatch-handler.ts",
  handlerExport: "handler",
  runtime: lambda.Runtime.NODEJS_20_X,
  access: [...],
});
```

ConfiguraÃƒÂ§ÃƒÂ£o:

- target `node20`;
- platform `node`;
- formato ESM compatÃƒÂ­vel com o projeto;
- sourcemap externo;
- minificaÃƒÂ§ÃƒÂ£o desabilitada inicialmente para stack traces auditÃƒÂ¡veis;
- arquivo de lock explÃƒÂ­cito;
- bundle independente por funÃƒÂ§ÃƒÂ£o;
- AWS SDK v3 incluÃƒÂ­do no bundle para versÃƒÂ£o reproduzÃƒÂ­vel.

NÃƒÂ£o confiaria na versÃƒÂ£o do SDK fornecida pela runtime Lambda.

Testes de infra devem falhar se qualquer funÃƒÂ§ÃƒÂ£o continuar usando inline code ou contiver o corpo 501.

## 7. SQS e DLQ

### Recursos

```text
ExpirationTracker-ReminderDispatch
ExpirationTracker-ReminderDispatch-DLQ
```

Os nomes fÃƒÂ­sicos podem receber stage, por exemplo:

```text
expiration-tracker-dev-reminder-dispatch
expiration-tracker-dev-reminder-dispatch-dlq
```

ConfiguraÃƒÂ§ÃƒÂ£o proposta:

| Propriedade | Queue | DLQ |
|---|---:|---:|
| Tipo | Standard | Standard |
| RetenÃƒÂ§ÃƒÂ£o | 4 dias | 14 dias |
| Long polling | 20 s | n/a |
| `maxReceiveCount` | 5 | n/a |
| Criptografia | SQS managed ou KMS do ambiente | igual |
| Visibility timeout | Ã¢â€°Â¥ 6 Ãƒâ€” timeout da Lambda | n/a |

A fila deve ser Standard, pois OCC e idempotÃƒÂªncia jÃƒÂ¡ sÃƒÂ£o necessÃƒÂ¡rios independentemente da ordenaÃƒÂ§ÃƒÂ£o; FIFO adicionaria throughput, group-id e dedup-window sem eliminar a necessidade de idempotÃƒÂªncia durÃƒÂ¡vel.

### Event source mapping

- batch size inicial: `10`;
- batching window: `0Ã¢â‚¬â€œ1 s`;
- `reportBatchItemFailures: true`;
- reserved concurrency da funÃƒÂ§ÃƒÂ£o: `10`;
- maximum concurrency do event source mapping: `10`;
- `bisectBatchOnError` nÃƒÂ£o se aplica a SQS;
- payload schema validado item a item.

Resposta:

```ts
return {
  batchItemFailures: failures.map(({ messageId }) => ({
    itemIdentifier: messageId,
  })),
};
```

ClassificaÃƒÂ§ÃƒÂ£o:

- sucesso, duplicata ou estado jÃƒÂ¡ avanÃƒÂ§ado Ã¢â€ â€™ ACK;
- erro retryable Ã¢â€ â€™ incluir em `batchItemFailures`;
- poison message/schema invÃƒÂ¡lido Ã¢â€ â€™ terminal, mas precisa chegar ÃƒÂ  DLQ.

Para poison message, hÃƒÂ¡ duas polÃƒÂ­ticas vÃƒÂ¡lidas. Eu recomendo retornar falha atÃƒÂ© `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observÃƒÂ¡vel. Para evitar custo inÃƒÂºtil, registrar a primeira detecÃƒÂ§ÃƒÂ£o e emitir mÃƒÂ©trica por receive count. NÃƒÂ£o deletar manualmente nem enviar diretamente ÃƒÂ  DLQ.

O relay do outbox tambÃƒÂ©m deve usar partial batch failure no DynamoDB Stream. Um registro sÃƒÂ³ ÃƒÂ© marcado publicado depois de `SendMessage` confirmado; duplicaÃƒÂ§ÃƒÂ£o entre envio e marcaÃƒÂ§ÃƒÂ£o ÃƒÂ© esperada e absorvida pelo dispatch idempotente.

## 8. EventBridge

Escolho **EventBridge Rule legado com `ScheduleExpression`**, nÃƒÂ£o EventBridge Scheduler, para estas cadÃƒÂªncias fixas.

Motivos:

- jÃƒÂ¡ ÃƒÂ© a tecnologia especificada no escopo;
- `rate(1 minute)` ÃƒÂ© simples e suficiente;
- integraÃƒÂ§ÃƒÂ£o direta com Lambda e retry policy madura;
- nÃƒÂ£o hÃƒÂ¡ milhÃƒÂµes de schedules por tenant;
- flexible time window nÃƒÂ£o existe em Scheduled Rule e nÃƒÂ£o ÃƒÂ© necessÃƒÂ¡ria para o producer.

Regras:

```text
ReminderProducerSchedule:
  rate(1 minute)

ReminderClaimReconciliationSchedule:
  rate(1 minute) ou rate(5 minutes)

ReminderDstReconciliationSchedule:
  cron diÃƒÂ¡rio em UTC
```

O evento deve carregar:

```json
{
  "mode": "PRODUCE"
}
```

ou:

```json
{
  "mode": "CLAIMS"
}
```

ou:

```json
{
  "mode": "DST"
}
```

Reserved concurrency:

- producer: `2`;
- reconciliation: `1`;
- dispatch: `10`.

Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contÃƒÂ­nuas precisam de alarme por invocaÃƒÂ§ÃƒÂµes com erro e por scheduler lag.

### Por que nÃƒÂ£o Scheduler

EventBridge Scheduler seria preferÃƒÂ­vel se houvesse schedules individuais, timezone por schedule, execuÃƒÂ§ÃƒÂ£o ÃƒÂºnica ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.

Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliaÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria, uma janela flexÃƒÂ­vel seria aceitÃƒÂ¡vel, mas nÃƒÂ£o justifica misturar dois mecanismos.

## 9. Testes

### Camada 1 Ã¢â‚¬â€ testes rÃƒÂ¡pidos

Continuam executando em todo PR:

- unitÃƒÂ¡rios do domÃƒÂ­nio;
- contratos de schema;
- CDK assertions;
- teste do bundle de cada entrypoint;
- teste que proÃƒÂ­be `fromInline` nos handlers de produÃƒÂ§ÃƒÂ£o;
- teste estrutural que assegura que somente producer tem ARN de GSI3.

### Camada 2 Ã¢â‚¬â€ integraÃƒÂ§ÃƒÂ£o em containers

Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade especÃƒÂ­fica.

DynamoDB Local, sozinho, nÃƒÂ£o testa:

- redrive SQS;
- DLQ;
- event source mapping;
- EventBridge;
- interaÃƒÂ§ÃƒÂ£o DynamoDB Ã¢â€ â€™ relay Ã¢â€ â€™ SQS.

LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suÃƒÂ­te, portas dinÃƒÂ¢micas e cleanup previsÃƒÂ­vel.

Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.

Casos obrigatÃƒÂ³rios:

1. CondiÃƒÂ§ÃƒÂ£o de OCC concorrente: apenas um writer vence.
2. Claim + dispatch outbox sÃƒÂ£o atÃƒÂ´micos.
3. Falha do relay preserva o registro para retry.
4. Duas entregas SQS criam um ÃƒÂºnico `NotificationIntent`.
5. Falha em um item do batch nÃƒÂ£o repete os itens bem-sucedidos.
6. Poison message chega ÃƒÂ  DLQ apÃƒÂ³s cinco receives.
7. Claim expirado ÃƒÂ© recuperado e volta a produzir trabalho.
8. Renew/cancel concorrente faz dispatch cancelar por versÃƒÂ£o obsoleta.
9. Query GSI3 encontra mÃƒÂºltiplos tenants, mas o producer nunca mistura o contexto reconstruÃƒÂ­do.
10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.

Para timeout real de dependÃƒÂªncia, usaria um proxy de rede controlÃƒÂ¡vel, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexÃƒÂ£o acima do timeout configurado e verifica:

- erro classificado como retryable;
- mensagem nÃƒÂ£o reconhecida como sucesso;
- retry/redrive;
- nenhuma transaÃƒÂ§ÃƒÂ£o parcial.

Isso testa falha de transporte real, nÃƒÂ£o um `mockRejectedValue`.

### Camada 3 Ã¢â‚¬â€ sandbox AWS efÃƒÂªmero

LocalStack nÃƒÂ£o prova semÃƒÂ¢ntica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 nÃƒÂ£o deve ser aprovado somente com containers.

Criaria uma suÃƒÂ­te curta contra uma stack AWS efÃƒÂªmera:

- deploy em conta/regiÃƒÂ£o de teste;
- TTL mÃƒÂ¡ximo da stack e tags de cleanup;
- seed de poucos itens;
- invocaÃƒÂ§ÃƒÂ£o do producer;
- observaÃƒÂ§ÃƒÂ£o da mensagem e do dispatch;
- poison message atÃƒÂ© DLQ;
- teste negativo assumindo a role de uma Lambda tenant-facing e tentando `Query` em GSI3;
- validaÃƒÂ§ÃƒÂ£o de schedule por invocaÃƒÂ§ÃƒÂ£o manual com o mesmo target, sem esperar o relÃƒÂ³gio;
- destruiÃƒÂ§ÃƒÂ£o da stack ao final.

CadÃƒÂªncia:

- containers em todo PR;
- sandbox AWS no merge para `develop` ou workflow manual obrigatÃƒÂ³rio do milestone;
- teste completo de schedule real em workflow noturno, pois esperar ticks torna PRs lentos e sujeitos a jitter.

O artefato da execuÃƒÂ§ÃƒÂ£o AWS Ã¢â‚¬â€ CloudWatch request IDs, mÃƒÂ©tricas, ARN da stack e resultados Ã¢â‚¬â€ deve ser preservado como evidÃƒÂªncia do gate.

## 10. Fault injection exigida

| Falha | InjeÃƒÂ§ÃƒÂ£o | EvidÃƒÂªncia esperada |
|---|---|---|
| Timeout DynamoDB/SQS | Toxiproxy interrompe conexÃƒÂ£o | erro retryable, retry, sem estado parcial |
| Queda apÃƒÂ³s claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
| Queda apÃƒÂ³s `SendMessage` | falhar antes de marcar outbox | possÃƒÂ­vel duplicata, um ÃƒÂºnico efeito lÃƒÂ³gico |
| Poison message | body invÃƒÂ¡lido real em SQS | cinco receives e mensagem na DLQ |
| Falha parcial de batch | uma mensagem invÃƒÂ¡lida entre vÃƒÂ¡lidas | somente invÃƒÂ¡lida retorna em `batchItemFailures` |
| Renew concorrente | atualizar versÃƒÂ£o apÃƒÂ³s claim | dispatch cancela stale, sem intent incorreto |
| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
| Duplo producer | duas invocaÃƒÂ§ÃƒÂµes simultÃƒÂ¢neas | um claim vence por condiÃƒÂ§ÃƒÂ£o |
| Tentativa GSI3 nÃƒÂ£o autorizada | role real tenant-facing | `AccessDenied`, nenhum dado retornado |

## 11. CritÃƒÂ©rios de saÃƒÂ­da do milestone

O milestone sÃƒÂ³ pode alegar fechamento operacional de G8 quando houver:

- zero handlers 501;
- bundles reais sintetizados;
- adapters reais cobrindo todos os ports;
- reconciliaÃƒÂ§ÃƒÂ£o alimentada por consultas reais, nÃƒÂ£o arrays vazios;
- claim e publicaÃƒÂ§ÃƒÂ£o conectados por mecanismo durÃƒÂ¡vel;
- fila, DLQ e redrive sintetizados e exercitados;
- partial batch failure comprovado;
- timeout, poison message e redrive exercitados;
- teste AWS negativo de GSI3 com role real;
- teste concorrente renew/cancel/dispatch;
- evidÃƒÂªncia registrada da execuÃƒÂ§ÃƒÂ£o em sandbox AWS.

## 12. O que este milestone nÃƒÂ£o resolve

Ã¢â‚¬Å“G8 fechadoÃ¢â‚¬Â nÃƒÂ£o significa que os cinco P1 desapareceram integralmente.

- **P1 #1 Ã¢â‚¬â€ recuperaÃƒÂ§ÃƒÂ£o assÃƒÂ­ncrona:** fecha o trecho producer Ã¢â€ â€™ dispatch se houver outbox/relay e reconciliaÃƒÂ§ÃƒÂ£o consultÃƒÂ¡vel. NÃƒÂ£o fecha pipelines futuros de Notification.
- **P1 #2 Ã¢â‚¬â€ idempotÃƒÂªncia no efeito externo:** nÃƒÂ£o pode ser provada ainda. `NotificationIntent` ÃƒÂ© apenas uma intenÃƒÂ§ÃƒÂ£o durÃƒÂ¡vel; M4 deverÃƒÂ¡ provar idempotÃƒÂªncia no limite do provider de e-mail/WhatsApp, inclusive timeout apÃƒÂ³s o provider aceitar a solicitaÃƒÂ§ÃƒÂ£o e antes de responder.
- **P1 #3 Ã¢â‚¬â€ false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. NÃƒÂ£o desaparece atÃƒÂ© testes AWS reais de M4 e operaÃƒÂ§ÃƒÂ£o continuada.
- **P1 #4 Ã¢â‚¬â€ concorrÃƒÂªncia/estado obsoleto:** o milestone pode provar OCC e revalidaÃƒÂ§ÃƒÂ£o entre renew/cancel/materializaÃƒÂ§ÃƒÂ£o/dispatch. NÃƒÂ£o prova todas as corridas com envio externo, callbacks e correÃƒÂ§ÃƒÂµes futuras de M4.
- **P1 #5 Ã¢â‚¬â€ blast radius do GSI3:** um teste IAM real prova que roles tenant-facing nÃƒÂ£o consultam o ÃƒÂ­ndice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa funÃƒÂ§ÃƒÂ£o ainda tÃƒÂªm blast radius global. SÃƒÂ£o necessÃƒÂ¡rios alarmes, logs sem PII, limites de batch e revisÃƒÂ£o especÃƒÂ­fica desse handler.

TambÃƒÂ©m nÃƒÂ£o resolve carga extrema, restore real, regiÃƒÂ£o/LGPD, provider de notificaÃƒÂ§ÃƒÂ£o, BFF de sessÃƒÂ£o nem aprovaÃƒÂ§ÃƒÂ£o global da arquitetura operacional.

## Nota e riscos

**ConfianÃƒÂ§a: 8,8/10.**

Principais riscos:

1. O port atual da reconciliaÃƒÂ§ÃƒÂ£o ÃƒÂ© insuficiente para um handler autÃƒÂ´nomo; o GSI6 precisa ser concretamente consultÃƒÂ¡vel.
2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS ÃƒÂ© indispensÃƒÂ¡vel.
4. O relay adiciona uma funÃƒÂ§ÃƒÂ£o e semÃƒÂ¢ntica de publicaÃƒÂ§ÃƒÂ£o que precisam de observabilidade e retenÃƒÂ§ÃƒÂ£o/cleanup do outbox.
5. A idempotÃƒÂªncia externa continuarÃƒÂ¡ nÃƒÂ£o comprovada atÃƒÂ© M4; qualquer declaraÃƒÂ§ÃƒÂ£o mais ampla de encerramento dos cinco P1 seria enganosa.
tokens used
38.889
# Proposta tÃƒÂ©cnica Ã¢â‚¬â€ milestone Runtime Real / G8

## 1. DecisÃƒÂ£o central

O milestone deve transformar o fluxo atual em runtime implantÃƒÂ¡vel sem contaminar os ports com tipos do AWS SDK:

```text
EventBridge Rule
      Ã¢â€â€š
      Ã¢â€“Â¼
ReminderProducer Lambda
      Ã¢â€â€š
      Ã¢â€Å“Ã¢â€â‚¬ TransactWrite:
      Ã¢â€â€š    SCHEDULED Ã¢â€ â€™ CLAIMED
      Ã¢â€â€š    + registro durÃƒÂ¡vel DispatchOutbox
      Ã¢â€“Â¼
DynamoDB Stream / relay
      Ã¢â€â€š
      Ã¢â€“Â¼
ReminderDispatchQueue
      Ã¢â€â€š
      Ã¢â€“Â¼
ReminderDispatch Lambda
      Ã¢â€â€š
      Ã¢â€Å“Ã¢â€â‚¬ revalida estado/versionamento
      Ã¢â€â€Ã¢â€â‚¬ TransactWrite:
           CLAIMED Ã¢â€ â€™ TRIGGERED
           + NotificationIntent
           + OutboxEvent
           + IdempotencyRecord
```

A gravaÃƒÂ§ÃƒÂ£o durÃƒÂ¡vel intermediÃƒÂ¡ria ÃƒÂ© necessÃƒÂ¡ria. Fazer `TransactWrite(CLAIMED)` e depois `SendMessage` diretamente no handler mantÃƒÂ©m uma janela de perda:

1. DynamoDB confirma o claim.
2. Lambda cai antes de enviar ÃƒÂ  SQS.
3. A ocorrÃƒÂªncia fica `CLAIMED`, mas nÃƒÂ£o existe mensagem.

A reconciliaÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria nÃƒÂ£o fecha adequadamente essa janela: quando reverter o claim, a ocorrÃƒÂªncia poderÃƒÂ¡ estar fora do lookback de cinco minutos do producer. Portanto, fila e DLQ, isoladamente, nÃƒÂ£o bastam para fechar o P1 #1.

Minha recomendaÃƒÂ§ÃƒÂ£o ÃƒÂ© um registro `DispatchOutbox` gravado atomicamente com o claim e publicado por DynamoDB Streams. Uma alternativa aceitÃƒÂ¡vel ÃƒÂ© EventBridge Pipes entre o stream e a SQS, desde que seja demonstrado que filtragem, transformaÃƒÂ§ÃƒÂ£o do stream record e retries funcionam no ambiente AWS real. Eu prefiro um pequeno `DispatchOutboxRelay` Lambda porque oferece controle explÃƒÂ­cito de schema, observabilidade, partial batch failure e evoluÃƒÂ§ÃƒÂ£o do contrato.

## 2. DependÃƒÂªncias

Adicionar como dependÃƒÂªncias de runtime:

```json
{
  "@aws-sdk/client-dynamodb": "versÃƒÂ£o exata",
  "@aws-sdk/lib-dynamodb": "versÃƒÂ£o exata",
  "@aws-sdk/client-sqs": "versÃƒÂ£o exata"
}
```

E como dependÃƒÂªncias de desenvolvimento:

```json
{
  "esbuild": "versÃƒÂ£o exata",
  "@types/aws-lambda": "versÃƒÂ£o exata",
  "testcontainers": "versÃƒÂ£o exata"
}
```

Escolhas:

- `DynamoDBClient`: transporte, configuraÃƒÂ§ÃƒÂ£o de retry e endpoint.
- `DynamoDBDocumentClient`: objetos JavaScript, compatÃƒÂ­vel com os builders existentes.
- `SQSClient`: publicaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita pelo relay.
- `esbuild`: bundling determinÃƒÂ­stico dos entrypoints ESM para Node.js 20.
- VersÃƒÂµes exatas, nÃƒÂ£o ranges, mantendo lockfile e `npm ci`.

ConfiguraÃƒÂ§ÃƒÂ£o do `DynamoDBDocumentClient`:

```ts
DynamoDBDocumentClient.from(client, {
  marshallOptions: {
    removeUndefinedValues: true,
    convertClassInstanceToMap: false,
  },
})
```

NÃƒÂ£o habilitaria conversÃƒÂµes numÃƒÂ©ricas silenciosas. Campos de versÃƒÂ£o precisam continuar sendo nÃƒÂºmeros e valores grandes devem ser avaliados explicitamente antes de usar `wrapNumbers`.

O cliente deve usar retry adaptativo ou standard com nÃƒÂºmero pequeno e explÃƒÂ­cito de tentativas, por exemplo trÃƒÂªs. O timeout total precisa caber com folga no timeout da Lambda; nÃƒÂ£o se deve deixar o SDK consumir todo o orÃƒÂ§amento da invocaÃƒÂ§ÃƒÂ£o.

## 3. Estrutura dos adapters

```text
src/
  runtime/
    aws/
      dynamodb/
        client.ts
        errors.ts
        execute-transaction.ts
        identity-store-dynamodb.ts
        expiration-store-dynamodb.ts
        reminder-store-dynamodb.ts
        reminder-producer-store-dynamodb.ts
        reconciliation-candidate-source-dynamodb.ts
      sqs/
        dispatch-command-publisher.ts

    handlers/
      test-ping-handler.ts
      items-handler.ts
      reminders-handler.ts
      reminder-producer-handler.ts
      reminder-dispatch-handler.ts
      reminder-reconciliation-handler.ts
      dispatch-outbox-relay-handler.ts

    composition/
      identity.ts
      expiration.ts
      reminder.ts
```

Um adapter por port:

- `DynamoDbIdentityStore implements IdentityStore`
- `DynamoDbExpirationStore implements ExpirationStore`
- `DynamoDbReminderStore implements ReminderStore`
- `DynamoDbReminderProducerStore implements ReminderProducerStore`

NÃƒÂ£o criaria uma classe Ã¢â‚¬Å“universalÃ¢â‚¬Â pÃƒÂºblica implementando todos os ports. Isso permitiria injetar acidentalmente acesso ao GSI3 em cÃƒÂ³digo tenant-facing.

Pode haver helpers internos compartilhados para `GetCommand`, `PutCommand`, `UpdateCommand` e `TransactWriteCommand`, mas o mÃƒÂ©todo `queryGsi3` existe somente em `reminder-producer-store-dynamodb.ts`.

### Mapeamento das operaÃƒÂ§ÃƒÂµes

| Port | Comando AWS |
|---|---|
| `get` | `GetCommand`, `ConsistentRead: true` |
| `putIfAbsent` | `PutCommand` com condiÃƒÂ§ÃƒÂ£o existente; retorna `false` apenas para falha condicional |
| `update` | `PutCommand` de overwrite, restrito aos usos jÃƒÂ¡ documentados pelo port |
| `transactWrite` | `TransactWriteCommand` usando diretamente os builders existentes |
| `queryGsi1` | `QueryCommand`, `IndexName: GSI1`, eventual consistency |
| `queryByItem` | `QueryCommand` na tabela base, `PK = :pk AND begins_with(SK, :occ)` e leitura consistente |
| `queryGsi3` | `QueryCommand`, `IndexName: GSI3`, somente no adapter privilegiado |

PaginaÃƒÂ§ÃƒÂ£o deve ser completa por padrÃƒÂ£o. `limit` representa limite lÃƒÂ³gico total, nÃƒÂ£o apenas o `Limit` de uma ÃƒÂºnica pÃƒÂ¡gina.

### TraduÃƒÂ§ÃƒÂ£o de erros

O adapter deve traduzuzir, sem esconder a causa:

- `ConditionalCheckFailedException` em `putIfAbsent` Ã¢â€ â€™ `false`.
- `TransactionCanceledException` Ã¢â€ â€™ erro reconhecÃƒÂ­vel por `isTransactionCanceled`.
- throttling, timeout, falha 5xx e indisponibilidade Ã¢â€ â€™ `AppError` retryable.
- validation error, item/schema invÃƒÂ¡lido Ã¢â€ â€™ erro terminal.
- `AccessDeniedException` Ã¢â€ â€™ erro terminal operacional e alarme; nÃƒÂ£o deve virar retry infinito.

Preservar `$metadata.requestId`, nome da operaÃƒÂ§ÃƒÂ£o e causa no log, sem registrar item completo nem dados pessoais.

## 4. Lacuna da reconciliaÃƒÂ§ÃƒÂ£o

O handler de reconciliaÃƒÂ§ÃƒÂ£o atual nÃƒÂ£o consegue operar sozinho: `runReconciliation` recebe arrays de candidatos, mas `ReminderStore` nÃƒÂ£o oferece uma consulta que os produza.

Executar a Lambda diariamente com arrays vazios seria outro false-green.

O milestone precisa introduzir um port estreito, por exemplo:

```ts
interface ReconciliationCandidateSource {
  listExpiredClaims(input: PageInput): AsyncIterable<ReminderOccurrence>;
  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
}
```

O adapter consultaria o GSI6 ou o ÃƒÂ­ndice efetivamente normativo. Isso ÃƒÂ© integraÃƒÂ§ÃƒÂ£o mÃƒÂ­nima necessÃƒÂ¡ria, nÃƒÂ£o redesenho do domÃƒÂ­nio.

Se o GSI6 ainda nÃƒÂ£o representa claims expirados e polÃƒÂ­ticas ativas de maneira consultÃƒÂ¡vel, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo estÃƒÂ¡ apenas agendado, nÃƒÂ£o funcional.

AlÃƒÂ©m da execuÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria de DST, recuperaÃƒÂ§ÃƒÂ£o de claims precisa ter cadÃƒÂªncia curta. Recomendo:

- reconciliaÃƒÂ§ÃƒÂ£o de claims expirados: a cada minuto ou cinco minutos;
- reconciliaÃƒÂ§ÃƒÂ£o DST/materializaÃƒÂ§ÃƒÂ£o: diÃƒÂ¡ria.

Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durÃƒÂ¡vel, a reconciliaÃƒÂ§ÃƒÂ£o curta continua ÃƒÂºtil para reparar estados excepcionais.

## 5. Handlers Lambda

Cada arquivo em `src/runtime/handlers` ÃƒÂ© um composition root:

1. valida variÃƒÂ¡veis de ambiente;
2. instancia clientes e adapters fora do handler para reuso em warm starts;
3. converte o evento AWS para os tipos internos;
4. invoca a lÃƒÂ³gica existente;
5. converte resultado/erro para o protocolo da origem;
6. registra mÃƒÂ©tricas e logs via `SecureLogger`.

Nenhuma lÃƒÂ³gica de negÃƒÂ³cio deve morar no wrapper Lambda.

VariÃƒÂ¡veis mÃƒÂ­nimas:

```text
TABLE_NAME
DISPATCH_QUEUE_URL
SHARD_CONFIG
PRODUCER_LOOKBACK_MINUTES
CLAIM_TTL_SECONDS
LOG_LEVEL
```

Para HTTP, os handlers reais devem interpretar API Gateway HTTP API v2, extrair claims jÃƒÂ¡ validadas pelo authorizer, construir `RequestContext` e mapear `AppError` para status HTTP consistente.

Para schedules, validar `source`, `detail-type` e um campo explÃƒÂ­cito `mode`. Evento inesperado ÃƒÂ© terminal.

Para SQS, validar cada body contra `schemas/queues/reminder.dispatch.v1.json` antes de executar o domÃƒÂ­nio.

## 6. Bundling

Eu estenderia `ScopedLambdaFunction` com `entry` e `handlerExport`, mantendo-o como ÃƒÂºnico ponto de criaÃƒÂ§ÃƒÂ£o de Lambda. Internamente, usaria `lambda.Code.fromAsset` com bundling `esbuild`, ou incorporaria a semÃƒÂ¢ntica de `NodejsFunction` sem permitir que stacks criem Lambdas por fora do construct.

Exemplo conceitual:

```ts
new ScopedLambdaFunction(this, "ReminderDispatch", {
  entry: "src/runtime/handlers/reminder-dispatch-handler.ts",
  handlerExport: "handler",
  runtime: lambda.Runtime.NODEJS_20_X,
  access: [...],
});
```

ConfiguraÃƒÂ§ÃƒÂ£o:

- target `node20`;
- platform `node`;
- formato ESM compatÃƒÂ­vel com o projeto;
- sourcemap externo;
- minificaÃƒÂ§ÃƒÂ£o desabilitada inicialmente para stack traces auditÃƒÂ¡veis;
- arquivo de lock explÃƒÂ­cito;
- bundle independente por funÃƒÂ§ÃƒÂ£o;
- AWS SDK v3 incluÃƒÂ­do no bundle para versÃƒÂ£o reproduzÃƒÂ­vel.

NÃƒÂ£o confiaria na versÃƒÂ£o do SDK fornecida pela runtime Lambda.

Testes de infra devem falhar se qualquer funÃƒÂ§ÃƒÂ£o continuar usando inline code ou contiver o corpo 501.

## 7. SQS e DLQ

### Recursos

```text
ExpirationTracker-ReminderDispatch
ExpirationTracker-ReminderDispatch-DLQ
```

Os nomes fÃƒÂ­sicos podem receber stage, por exemplo:

```text
expiration-tracker-dev-reminder-dispatch
expiration-tracker-dev-reminder-dispatch-dlq
```

ConfiguraÃƒÂ§ÃƒÂ£o proposta:

| Propriedade | Queue | DLQ |
|---|---:|---:|
| Tipo | Standard | Standard |
| RetenÃƒÂ§ÃƒÂ£o | 4 dias | 14 dias |
| Long polling | 20 s | n/a |
| `maxReceiveCount` | 5 | n/a |
| Criptografia | SQS managed ou KMS do ambiente | igual |
| Visibility timeout | Ã¢â€°Â¥ 6 Ãƒâ€” timeout da Lambda | n/a |

A fila deve ser Standard, pois OCC e idempotÃƒÂªncia jÃƒÂ¡ sÃƒÂ£o necessÃƒÂ¡rios independentemente da ordenaÃƒÂ§ÃƒÂ£o; FIFO adicionaria throughput, group-id e dedup-window sem eliminar a necessidade de idempotÃƒÂªncia durÃƒÂ¡vel.

### Event source mapping

- batch size inicial: `10`;
- batching window: `0Ã¢â‚¬â€œ1 s`;
- `reportBatchItemFailures: true`;
- reserved concurrency da funÃƒÂ§ÃƒÂ£o: `10`;
- maximum concurrency do event source mapping: `10`;
- `bisectBatchOnError` nÃƒÂ£o se aplica a SQS;
- payload schema validado item a item.

Resposta:

```ts
return {
  batchItemFailures: failures.map(({ messageId }) => ({
    itemIdentifier: messageId,
  })),
};
```

ClassificaÃƒÂ§ÃƒÂ£o:

- sucesso, duplicata ou estado jÃƒÂ¡ avanÃƒÂ§ado Ã¢â€ â€™ ACK;
- erro retryable Ã¢â€ â€™ incluir em `batchItemFailures`;
- poison message/schema invÃƒÂ¡lido Ã¢â€ â€™ terminal, mas precisa chegar ÃƒÂ  DLQ.

Para poison message, hÃƒÂ¡ duas polÃƒÂ­ticas vÃƒÂ¡lidas. Eu recomendo retornar falha atÃƒÂ© `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observÃƒÂ¡vel. Para evitar custo inÃƒÂºtil, registrar a primeira detecÃƒÂ§ÃƒÂ£o e emitir mÃƒÂ©trica por receive count. NÃƒÂ£o deletar manualmente nem enviar diretamente ÃƒÂ  DLQ.

O relay do outbox tambÃƒÂ©m deve usar partial batch failure no DynamoDB Stream. Um registro sÃƒÂ³ ÃƒÂ© marcado publicado depois de `SendMessage` confirmado; duplicaÃƒÂ§ÃƒÂ£o entre envio e marcaÃƒÂ§ÃƒÂ£o ÃƒÂ© esperada e absorvida pelo dispatch idempotente.

## 8. EventBridge

Escolho **EventBridge Rule legado com `ScheduleExpression`**, nÃƒÂ£o EventBridge Scheduler, para estas cadÃƒÂªncias fixas.

Motivos:

- jÃƒÂ¡ ÃƒÂ© a tecnologia especificada no escopo;
- `rate(1 minute)` ÃƒÂ© simples e suficiente;
- integraÃƒÂ§ÃƒÂ£o direta com Lambda e retry policy madura;
- nÃƒÂ£o hÃƒÂ¡ milhÃƒÂµes de schedules por tenant;
- flexible time window nÃƒÂ£o existe em Scheduled Rule e nÃƒÂ£o ÃƒÂ© necessÃƒÂ¡ria para o producer.

Regras:

```text
ReminderProducerSchedule:
  rate(1 minute)

ReminderClaimReconciliationSchedule:
  rate(1 minute) ou rate(5 minutes)

ReminderDstReconciliationSchedule:
  cron diÃƒÂ¡rio em UTC
```

O evento deve carregar:

```json
{
  "mode": "PRODUCE"
}
```

ou:

```json
{
  "mode": "CLAIMS"
}
```

ou:

```json
{
  "mode": "DST"
}
```

Reserved concurrency:

- producer: `2`;
- reconciliation: `1`;
- dispatch: `10`.

Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contÃƒÂ­nuas precisam de alarme por invocaÃƒÂ§ÃƒÂµes com erro e por scheduler lag.

### Por que nÃƒÂ£o Scheduler

EventBridge Scheduler seria preferÃƒÂ­vel se houvesse schedules individuais, timezone por schedule, execuÃƒÂ§ÃƒÂ£o ÃƒÂºnica ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.

Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliaÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria, uma janela flexÃƒÂ­vel seria aceitÃƒÂ¡vel, mas nÃƒÂ£o justifica misturar dois mecanismos.

## 9. Testes

### Camada 1 Ã¢â‚¬â€ testes rÃƒÂ¡pidos

Continuam executando em todo PR:

- unitÃƒÂ¡rios do domÃƒÂ­nio;
- contratos de schema;
- CDK assertions;
- teste do bundle de cada entrypoint;
- teste que proÃƒÂ­be `fromInline` nos handlers de produÃƒÂ§ÃƒÂ£o;
- teste estrutural que assegura que somente producer tem ARN de GSI3.

### Camada 2 Ã¢â‚¬â€ integraÃƒÂ§ÃƒÂ£o em containers

Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade especÃƒÂ­fica.

DynamoDB Local, sozinho, nÃƒÂ£o testa:

- redrive SQS;
- DLQ;
- event source mapping;
- EventBridge;
- interaÃƒÂ§ÃƒÂ£o DynamoDB Ã¢â€ â€™ relay Ã¢â€ â€™ SQS.

LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suÃƒÂ­te, portas dinÃƒÂ¢micas e cleanup previsÃƒÂ­vel.

Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.

Casos obrigatÃƒÂ³rios:

1. CondiÃƒÂ§ÃƒÂ£o de OCC concorrente: apenas um writer vence.
2. Claim + dispatch outbox sÃƒÂ£o atÃƒÂ´micos.
3. Falha do relay preserva o registro para retry.
4. Duas entregas SQS criam um ÃƒÂºnico `NotificationIntent`.
5. Falha em um item do batch nÃƒÂ£o repete os itens bem-sucedidos.
6. Poison message chega ÃƒÂ  DLQ apÃƒÂ³s cinco receives.
7. Claim expirado ÃƒÂ© recuperado e volta a produzir trabalho.
8. Renew/cancel concorrente faz dispatch cancelar por versÃƒÂ£o obsoleta.
9. Query GSI3 encontra mÃƒÂºltiplos tenants, mas o producer nunca mistura o contexto reconstruÃƒÂ­do.
10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.

Para timeout real de dependÃƒÂªncia, usaria um proxy de rede controlÃƒÂ¡vel, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexÃƒÂ£o acima do timeout configurado e verifica:

- erro classificado como retryable;
- mensagem nÃƒÂ£o reconhecida como sucesso;
- retry/redrive;
- nenhuma transaÃƒÂ§ÃƒÂ£o parcial.

Isso testa falha de transporte real, nÃƒÂ£o um `mockRejectedValue`.

### Camada 3 Ã¢â‚¬â€ sandbox AWS efÃƒÂªmero

LocalStack nÃƒÂ£o prova semÃƒÂ¢ntica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 nÃƒÂ£o deve ser aprovado somente com containers.

Criaria uma suÃƒÂ­te curta contra uma stack AWS efÃƒÂªmera:

- deploy em conta/regiÃƒÂ£o de teste;
- TTL mÃƒÂ¡ximo da stack e tags de cleanup;
- seed de poucos itens;
- invocaÃƒÂ§ÃƒÂ£o do producer;
- observaÃƒÂ§ÃƒÂ£o da mensagem e do dispatch;
- poison message atÃƒÂ© DLQ;
- teste negativo assumindo a role de uma Lambda tenant-facing e tentando `Query` em GSI3;
- validaÃƒÂ§ÃƒÂ£o de schedule por invocaÃƒÂ§ÃƒÂ£o manual com o mesmo target, sem esperar o relÃƒÂ³gio;
- destruiÃƒÂ§ÃƒÂ£o da stack ao final.

CadÃƒÂªncia:

- containers em todo PR;
- sandbox AWS no merge para `develop` ou workflow manual obrigatÃƒÂ³rio do milestone;
- teste completo de schedule real em workflow noturno, pois esperar ticks torna PRs lentos e sujeitos a jitter.

O artefato da execuÃƒÂ§ÃƒÂ£o AWS Ã¢â‚¬â€ CloudWatch request IDs, mÃƒÂ©tricas, ARN da stack e resultados Ã¢â‚¬â€ deve ser preservado como evidÃƒÂªncia do gate.

## 10. Fault injection exigida

| Falha | InjeÃƒÂ§ÃƒÂ£o | EvidÃƒÂªncia esperada |
|---|---|---|
| Timeout DynamoDB/SQS | Toxiproxy interrompe conexÃƒÂ£o | erro retryable, retry, sem estado parcial |
| Queda apÃƒÂ³s claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
| Queda apÃƒÂ³s `SendMessage` | falhar antes de marcar outbox | possÃƒÂ­vel duplicata, um ÃƒÂºnico efeito lÃƒÂ³gico |
| Poison message | body invÃƒÂ¡lido real em SQS | cinco receives e mensagem na DLQ |
| Falha parcial de batch | uma mensagem invÃƒÂ¡lida entre vÃƒÂ¡lidas | somente invÃƒÂ¡lida retorna em `batchItemFailures` |
| Renew concorrente | atualizar versÃƒÂ£o apÃƒÂ³s claim | dispatch cancela stale, sem intent incorreto |
| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
| Duplo producer | duas invocaÃƒÂ§ÃƒÂµes simultÃƒÂ¢neas | um claim vence por condiÃƒÂ§ÃƒÂ£o |
| Tentativa GSI3 nÃƒÂ£o autorizada | role real tenant-facing | `AccessDenied`, nenhum dado retornado |

## 11. CritÃƒÂ©rios de saÃƒÂ­da do milestone

O milestone sÃƒÂ³ pode alegar fechamento operacional de G8 quando houver:

- zero handlers 501;
- bundles reais sintetizados;
- adapters reais cobrindo todos os ports;
- reconciliaÃƒÂ§ÃƒÂ£o alimentada por consultas reais, nÃƒÂ£o arrays vazios;
- claim e publicaÃƒÂ§ÃƒÂ£o conectados por mecanismo durÃƒÂ¡vel;
- fila, DLQ e redrive sintetizados e exercitados;
- partial batch failure comprovado;
- timeout, poison message e redrive exercitados;
- teste AWS negativo de GSI3 com role real;
- teste concorrente renew/cancel/dispatch;
- evidÃƒÂªncia registrada da execuÃƒÂ§ÃƒÂ£o em sandbox AWS.

## 12. O que este milestone nÃƒÂ£o resolve

Ã¢â‚¬Å“G8 fechadoÃ¢â‚¬Â nÃƒÂ£o significa que os cinco P1 desapareceram integralmente.

- **P1 #1 Ã¢â‚¬â€ recuperaÃƒÂ§ÃƒÂ£o assÃƒÂ­ncrona:** fecha o trecho producer Ã¢â€ â€™ dispatch se houver outbox/relay e reconciliaÃƒÂ§ÃƒÂ£o consultÃƒÂ¡vel. NÃƒÂ£o fecha pipelines futuros de Notification.
- **P1 #2 Ã¢â‚¬â€ idempotÃƒÂªncia no efeito externo:** nÃƒÂ£o pode ser provada ainda. `NotificationIntent` ÃƒÂ© apenas uma intenÃƒÂ§ÃƒÂ£o durÃƒÂ¡vel; M4 deverÃƒÂ¡ provar idempotÃƒÂªncia no limite do provider de e-mail/WhatsApp, inclusive timeout apÃƒÂ³s o provider aceitar a solicitaÃƒÂ§ÃƒÂ£o e antes de responder.
- **P1 #3 Ã¢â‚¬â€ false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. NÃƒÂ£o desaparece atÃƒÂ© testes AWS reais de M4 e operaÃƒÂ§ÃƒÂ£o continuada.
- **P1 #4 Ã¢â‚¬â€ concorrÃƒÂªncia/estado obsoleto:** o milestone pode provar OCC e revalidaÃƒÂ§ÃƒÂ£o entre renew/cancel/materializaÃƒÂ§ÃƒÂ£o/dispatch. NÃƒÂ£o prova todas as corridas com envio externo, callbacks e correÃƒÂ§ÃƒÂµes futuras de M4.
- **P1 #5 Ã¢â‚¬â€ blast radius do GSI3:** um teste IAM real prova que roles tenant-facing nÃƒÂ£o consultam o ÃƒÂ­ndice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa funÃƒÂ§ÃƒÂ£o ainda tÃƒÂªm blast radius global. SÃƒÂ£o necessÃƒÂ¡rios alarmes, logs sem PII, limites de batch e revisÃƒÂ£o especÃƒÂ­fica desse handler.

TambÃƒÂ©m nÃƒÂ£o resolve carga extrema, restore real, regiÃƒÂ£o/LGPD, provider de notificaÃƒÂ§ÃƒÂ£o, BFF de sessÃƒÂ£o nem aprovaÃƒÂ§ÃƒÂ£o global da arquitetura operacional.

## Nota e riscos

**ConfianÃƒÂ§a: 8,8/10.**

Principais riscos:

1. O port atual da reconciliaÃƒÂ§ÃƒÂ£o ÃƒÂ© insuficiente para um handler autÃƒÂ´nomo; o GSI6 precisa ser concretamente consultÃƒÂ¡vel.
2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS ÃƒÂ© indispensÃƒÂ¡vel.
4. O relay adiciona uma funÃƒÂ§ÃƒÂ£o e semÃƒÂ¢ntica de publicaÃƒÂ§ÃƒÂ£o que precisam de observabilidade e retenÃƒÂ§ÃƒÂ£o/cleanup do outbox.
5. A idempotÃƒÂªncia externa continuarÃƒÂ¡ nÃƒÂ£o comprovada atÃƒÂ© M4; qualquer declaraÃƒÂ§ÃƒÂ£o mais ampla de encerramento dos cinco P1 seria enganosa.


 succeeded in 437ms:
# M3.5 Ã¢â‚¬â€ Runtime real do Reminder Engine Ã¢â‚¬â€ Proposta Claude (Rodada 1)

Escopo (fixado por `NEXT_SESSION_PROMPT.md`/`ENGINEERING.md` "DecisÃƒÂ£o pendente: escopo de G8"):
1. Adapters DynamoDB reais para `ReminderStore`, `ExpirationStore`, `IdentityStore` (hoje sÃƒÂ³ fakes em memÃƒÂ³ria).
2. Handlers Lambda reais substituindo os placeholders `501` em `infra/lib/expiration-tracker-stack.ts`.
3. Filas SQS + DLQ com redrive policy para o pipeline producerÃ¢â€ â€™dispatch.
4. EventBridge Scheduled Rule disparando `ReminderProducer` (1 min) e `ReminderReconciliation` (diÃƒÂ¡rio).
5. Testes de fault injection contra esse runtime real.

NÃƒÂ£o-escopo (explicitamente adiado, nÃƒÂ£o redescobrir): Notification/Document/Extraction (M4+), WhatsApp/e-mail providers, wiring real do GSI6 (item 4 da lista de pendÃƒÂªncias), backup/restore real.

## 1. Adapters DynamoDB reais

- Pacote: `@aws-sdk/client-dynamodb` + `@aws-sdk/lib-dynamodb` (DocumentClient v3), jÃƒÂ¡ implÃƒÂ­cito pelo blueprint (`implementation-blueprint.md` linha 6 lista DynamoDB na stack; M0-M3 deliberadamente adiaram o SDK real).
- Um adapter por port, em `src/modules/<mÃƒÂ³dulo>/persistence/dynamodb-<nome>.ts`, implementando a interface exata do port (`ReminderStore`, `ExpirationStore`, `IdentityStore`) Ã¢â‚¬â€ nenhuma mudanÃƒÂ§a de shape nos ports, sÃƒÂ³ nova implementaÃƒÂ§ÃƒÂ£o.
- `get`/`putIfAbsent`/`update`/`transactWrite`/`queryByItem`/`queryGsi1`/`queryGsi3` traduzem 1:1 para `GetCommand`/`PutCommand` com `ConditionExpression: attribute_not_exists(PK)`/`UpdateCommand`/`TransactWriteCommand`/`QueryCommand`. Os builders jÃƒÂ¡ existentes (`shared/dynamodb/occ.ts`, `shared/idempotency/idempotency.ts`, `shared/outbox/outbox.ts`) jÃƒÂ¡ produzem os parÃƒÂ¢metros nesse formato Ã¢â‚¬â€ os adapters sÃƒÂ£o uma camada fina de traduÃƒÂ§ÃƒÂ£o para os `*Command` do SDK, nÃƒÂ£o reimplementam lÃƒÂ³gica de OCC/idempotÃƒÂªncia.
- Erros do SDK (`ConditionalCheckFailedException`, `TransactionCanceledException`, `ProvisionedThroughputExceededException`, timeout/throttling) mapeados para a taxonomia de `shared/errors/app-error.ts` (`retryable` vs terminal) no prÃƒÂ³prio adapter Ã¢â‚¬â€ ÃƒÂ© o ÃƒÂºnico lugar que vÃƒÂª exceÃƒÂ§ÃƒÂµes nativas do SDK; o resto do sistema jÃƒÂ¡ ÃƒÂ© SDK-agnÃƒÂ³stico e deve continuar assim.
- Teste: suÃƒÂ­te de integraÃƒÂ§ÃƒÂ£o contra **DynamoDB Local** (`amazon/dynamodb-local` via Docker, ou `dynamodb-local` npm binÃƒÂ¡rio) Ã¢â‚¬â€ nÃƒÂ£o LocalStack (mais pesado, nÃƒÂ£o necessÃƒÂ¡rio sÃƒÂ³ para DynamoDB) e nÃƒÂ£o mocks de SDK (nÃƒÂ£o provaria nada sobre `ConditionExpression`/`TransactWriteItems` reais). As suÃƒÂ­tes de domÃƒÂ­nio existentes (`test/integration/*.test.ts`) rodam hoje contra as fakes em memÃƒÂ³ria; adiciona-se um novo alvo `test/integration-dynamodb/` que roda os MESMOS testes de comportamento (reusar os casos, nÃƒÂ£o duplicar lÃƒÂ³gica) contra o adapter real + DynamoDB Local, provando que a fake e o adapter real concordam. CI ganha um job novo que sobe o container antes da suÃƒÂ­te.

## 2. Handlers Lambda reais

- Bundling: `esbuild` via `NodejsFunction` (aws-cdk-lib) substitui `lambda.Code.fromInline` em `ScopedLambdaFunction` quando um `entryFile` ÃƒÂ© passado Ã¢â‚¬â€ mantÃƒÂ©m compatibilidade com os testes existentes que passam `code:` inline. Escolha de `esbuild`/`NodejsFunction` porque jÃƒÂ¡ ÃƒÂ© dependÃƒÂªncia transitiva do CDK e ÃƒÂ© o caminho documentado padrÃƒÂ£o, sem tooling adicional.
- Cada handler ÃƒÂ© uma funÃƒÂ§ÃƒÂ£o fina em `src/handlers/<nome>.ts` que: (a) resolve tenant/identity do evento (API Gateway JWT authorizer context para os HTTP; nenhuma resoluÃƒÂ§ÃƒÂ£o de tenant para os workers assÃƒÂ­ncronos Ã¢â‚¬â€ eles operam cross-tenant por design, GSI3-scoped), (b) constrÃƒÂ³i os adapters DynamoDB reais injetando-os nos services/workers jÃƒÂ¡ existentes e testados (`ExpirationService`, `ReminderPolicyService`, `reminder-producer.ts`, `reminder-dispatch.ts`, `reminder-reconciliation.ts`), (c) traduz o resultado do worker para o formato de evento (API Gateway response / SQS batch response / EventBridge void).
- SQS: handlers de fila usam **partial batch response** (`functionResponseType: ReportBatchItemFailures`) Ã¢â‚¬â€ item individual falho nÃƒÂ£o derruba o batch inteiro; mensagens nÃƒÂ£o reportadas como falha sÃƒÂ£o consideradas processadas.
- Testes: `test/handlers/*.test.ts` invocam o handler diretamente com eventos sintÃƒÂ©ticos (shape real de API Gateway/SQS/EventBridge) e adapters DynamoDB reais contra DynamoDB Local Ã¢â‚¬â€ prova a integraÃƒÂ§ÃƒÂ£o handlerÃ¢â€ â€SDKÃ¢â€ â€worker que as suÃƒÂ­tes de M0-M3 (lÃƒÂ³gica pura) nÃƒÂ£o cobriam. `test/infra/stack.test.ts` ganha asserts de que os handlers deixaram de ser `501`/inline.

## 3. SQS + DLQ

- Nova construct `infra/lib/reminder-queue.ts`: `ReminderDispatchQueue` (fila principal) + `ReminderDispatchDlq`, `maxReceiveCount=5` (valor jÃƒÂ¡ documentado em `implementation-blueprint.md` Ã‚Â§26 para `ReminderDispatchWorker`), redrive policy padrÃƒÂ£o SQS, criptografia SSE-SQS (chave gerenciada, sem KMS customer-managed Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ requisito de compliance que exija CMK ainda, `implementation-blueprint.md` Ã‚Â§16 nÃƒÂ£o especifica; revisar se LGPD/regiÃƒÂ£o decidir diferente).
- `ReminderProducer` ganha `queueAccessFor().send(queue)`; `ReminderDispatchWorker` ganha `.consume(queue)`, event source mapping com batch size pequeno (10, `implementation-blueprint.md` Ã‚Â§26 jÃƒÂ¡ fixa reserved concurrency 10) e `reportBatchItemFailures`.
- DLQ: alarme CloudWatch de idade (1h/4h jÃƒÂ¡ documentado em `implementation-blueprint.md` linha 879) Ã¢â‚¬â€ implementado como `cloudwatch.Alarm` na mesma construct; sem lambda de auto-redrive nesta fase (redrive manual via runbook, jÃƒÂ¡ existe `dlq-redrive/` referenciado na ÃƒÂ¡rvore do blueprint linha 114 Ã¢â‚¬â€ verificar se esse diretÃƒÂ³rio jÃƒÂ¡ existe ou ÃƒÂ© follow-up).
- Teste de infra: `test/infra/reminder-queue.test.ts` sintetiza e afirma DLQ+redrive policy+maxReceiveCount+encryption via `Template.fromStack`.

## 4. EventBridge Scheduled Rule

- `infra/lib/reminder-schedule.ts`: duas `scheduler.CfnSchedule` (EventBridge Scheduler, nÃƒÂ£o `events.Rule` legado Ã¢â‚¬â€ o blueprint linha 162 jÃƒÂ¡ nomeia "EventBridge Scheduler" especificamente para o producer, coerente com granularidade de minuto): `ReminderProducerSchedule` (`rate(1 minute)`) invocando `reminderProducer` via alvo Lambda; `ReminderReconciliationSchedule` (`rate(1 day)`, horÃƒÂ¡rio fixo off-peak) invocando `reminderReconciliation`.
- Ambos com `FlexibleTimeWindow: OFF` (precisÃƒÂ£o de minuto ÃƒÂ© o prÃƒÂ³prio mecanismo de correÃƒÂ§ÃƒÂ£o do producer Ã¢â‚¬â€ jitter quebraria a janela de lookback `[M-5min, M]`).
- Kill switch: os dois schedules nascem `ENABLED`, mas a stack aceita uma prop `schedulesEnabled` (default `true`) para permitir desligar em deploy/teste sem destruir a construct Ã¢â‚¬â€ reusa o padrÃƒÂ£o de flag booleana jÃƒÂ¡ visto em `ExpirationTrackerStackProps.mfaPolicy`.
- Teste de infra: assert de `ScheduleExpression`, alvo correto, e que NENHUM outro schedule invoca essas duas funÃƒÂ§ÃƒÂµes (evita duplicidade de tick).

## 5. Fault injection

Contra o runtime real (DynamoDB Local + handlers reais, sem mocks de SDK):
- **Timeout de dependÃƒÂªncia**: adapter com hook de injeÃƒÂ§ÃƒÂ£o de latÃƒÂªncia/erro (sÃƒÂ³ em modo de teste, nunca em produÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ flag de config, nÃƒÂ£o cÃƒÂ³digo condicional em produÃƒÂ§ÃƒÂ£o) simulando `ProvisionedThroughputExceededException`/timeout; prova que o handler retorna falha retryable (nÃƒÂ£o `reportBatchItemFailures` de item que na verdade nÃƒÂ£o falhou) e que o SQS reentrega.
- **Poison message**: mensagem SQS malformada (schema invÃƒÂ¡lido) injetada diretamente na fila (via SDK real, nÃƒÂ£o mock) Ã¢â€ â€™ prova que vai para DLQ apÃƒÂ³s `maxReceiveCount=5` sem derrubar o resto do batch, e que o erro ÃƒÂ© classificado terminal (nÃƒÂ£o retryable) em `app-error.ts` para nÃƒÂ£o desperdiÃƒÂ§ar as 5 tentativas em algo que nunca vai ter sucesso Ã¢â‚¬â€ ponto de risco identificado no P1 #2 do red team (idempotÃƒÂªncia no limite do efeito externo): mensagem poison nÃƒÂ£o deve reter lock/claim indefinidamente.
- **Redrive**: mensagem manualmente movida para DLQ Ã¢â€ â€™ redrive real (SQS `StartMessageMoveTask` ou CLI) Ã¢â€ â€™ reprocessada com sucesso, provando que o caminho de recuperaÃƒÂ§ÃƒÂ£o documentado no runbook funciona de fato, nÃƒÂ£o sÃƒÂ³ em teoria.
- Local: usar **LocalStack** aqui (diferente do item 1) porque fault injection de SQS real (redrive, `StartMessageMoveTask`) nÃƒÂ£o ÃƒÂ© bem suportado por simuladores mÃƒÂ­nimos Ã¢â‚¬â€ mas isolado num job de CI separado/opcional (mais pesado), nÃƒÂ£o bloqueante do `npm test` rÃƒÂ¡pido de todo dia.

## Itens que este milestone NÃƒÆ’O fecha (mas o red team apontou como P1 e merecem nota explÃƒÂ­cita)

- P1 #2 (idempotÃƒÂªncia no limite do efeito externo, "at-least-once clÃƒÂ¡ssico") e P1 #4 (fencing antes do efeito externo) sÃƒÂ£o sobre a LÃƒâ€œGICA de dispatch, nÃƒÂ£o sobre existir runtime Ã¢â‚¬â€ `reminder-dispatch.ts` jÃƒÂ¡ claim antes de disparar (M3), mas nÃƒÂ£o hÃƒÂ¡ "efeito externo" real ainda nesta stack (isso ÃƒÂ© Notification/M4). Este milestone prova que o pipeline producerÃ¢â€ â€™dispatch SOBREVIVE e SE RECUPERA de falha de infraestrutura real; nÃƒÂ£o resolve fencing contra um provedor de notificaÃƒÂ§ÃƒÂ£o externo, porque esse provedor ainda nÃƒÂ£o existe no sistema. Deve ficar registrado explicitamente para nÃƒÂ£o ser mal interpretado como "G8 fechado = idempotÃƒÂªncia ponta a ponta provada" quando K4 (Notification) ainda nÃƒÂ£o existe.
- P1 #5 (blast radius cross-tenant do GSI3): este milestone adiciona o primeiro teste negativo contra o ADAPTER real (nÃƒÂ£o sÃƒÂ³ a fake), mas nÃƒÂ£o muda o desenho de IAM jÃƒÂ¡ existente (M3 jÃƒÂ¡ corrigiu `grantGsi3ReadTo` explÃƒÂ­cito).

## Pergunta aberta para a rodada de crÃƒÂ­tica

Vale a pena, neste milestone, tambÃƒÂ©m escrever o adapter real de `ExpirationStore`/`IdentityStore` (que nÃƒÂ£o tÃƒÂªm worker assÃƒÂ­ncrono, sÃƒÂ³ HTTP), ou o escopo deveria focar SÃƒâ€œ no pipeline Reminder (que ÃƒÂ© o que tem G8/async recovery em jogo) e tratar os outros dois adapters como possÃƒÂ­vel milestone separado, jÃƒÂ¡ que `ENGINEERING.md` os lista mas o problema de G8 propriamente dito ÃƒÂ© sÃƒÂ³ o pipeline assÃƒÂ­ncrono? Minha leitura: o texto da decisÃƒÂ£o do usuÃƒÂ¡rio lista os trÃƒÂªs explicitamente, entÃƒÂ£o mantenho os trÃƒÂªs no escopo, mas a ORDEM de implementaÃƒÂ§ÃƒÂ£o deveria ser Reminder primeiro (ÃƒÂ© onde estÃƒÂ¡ o G8 real) e Expiration/Identity depois, jÃƒÂ¡ testados/estÃƒÂ¡veis (M1/M2), entÃƒÂ£o adapters para eles sÃƒÂ£o trabalho mais mecÃƒÂ¢nico.


 succeeded in 421ms:
# M3.5 Ã¢â‚¬â€ Rodada 2 Ã¢â‚¬â€ CrÃƒÂ­tica da Claude ÃƒÂ  proposta do Codex (Rodada 1)

Li `codex-output-round1.txt` na ÃƒÂ­ntegra apÃƒÂ³s submeter minha prÃƒÂ³pria proposta independente (`claude-proposal.md`), conforme protocolo de nota cega (AGENTS.md Ã‚Â§4).

## Pontos onde a proposta do Codex ÃƒÂ© claramente superior Ã¢â‚¬â€ adoto

1. **Gap real que minha proposta nÃƒÂ£o viu: `TransactWrite(claim)` seguido de `SendMessage` direto no handler nÃƒÂ£o ÃƒÂ© atÃƒÂ´mico.** Se a Lambda cai entre os dois passos, a ocorrÃƒÂªncia fica `CLAIMED` sem mensagem em fila, e a reconciliaÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria pode nÃƒÂ£o alcanÃƒÂ§ar essa ocorrÃƒÂªncia dentro da janela antes de virar um problema (ela reverte claim expirado, mas isso ÃƒÂ© *outro* ciclo, nÃƒÂ£o fecha a lacuna descrita). O Codex estÃƒÂ¡ certo: fila+DLQ sozinhas nÃƒÂ£o fecham P1 #1 sem uma etapa durÃƒÂ¡vel entre claim e publicaÃƒÂ§ÃƒÂ£o. Adoto **DynamoDB Streams Ã¢â€ â€™ `DispatchOutboxRelay` Lambda Ã¢â€ â€™ SQS**, reusando o padrÃƒÂ£o jÃƒÂ¡ existente de `shared/outbox/outbox.ts` (a mesma ideia de "outbox transacional" do M2/M3, agora com um relay real em vez de placeholder) em vez de inventar mecanismo novo.
2. **Gap real: a reconciliaÃƒÂ§ÃƒÂ£o nÃƒÂ£o tem fonte de candidatos.** Minha proposta tratou reconciliaÃƒÂ§ÃƒÂ£o como "jÃƒÂ¡ implementada, sÃƒÂ³ falta plugar" Ã¢â‚¬â€ errado. `runReconciliation` recebe arrays injetados; sem um port `ReconciliationCandidateSource` (ou equivalente) apoiado em GSI6 consultÃƒÂ¡vel, rodar a Lambda diariamente com array vazio ÃƒÂ© exatamente o false-green que o red team apontou (P1 #3). Adoto a proposta do Codex de um port estreito novo para isso, E que o wiring real do GSI6 (item 4 da lista de pendÃƒÂªncias em `NEXT_SESSION_PROMPT.md`) entra NO escopo deste milestone Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© mais um "follow-up" separado, porque sem ele o item 4 do escopo do milestone (EventBridge disparando reconciliation) ÃƒÂ© teatro.
3. **Teste em sandbox AWS efÃƒÂªmero como terceira camada, nÃƒÂ£o substituÃƒÂ­vel por LocalStack/DynamoDB Local.** Concordo Ã¢â‚¬â€ IAM real, event source mapping real e EventBridge real nÃƒÂ£o sÃƒÂ£o fielmente simulados. Isso tambÃƒÂ©m resolve minha "pergunta aberta": meu plano de fault injection via LocalStack sÃƒÂ³ para a camada de filas era necessÃƒÂ¡rio mas nÃƒÂ£o suficiente; preciso do sandbox real para o teste negativo de GSI3 (IAM real) e para provar redrive de verdade.
4. **CadÃƒÂªncia de reconciliaÃƒÂ§ÃƒÂ£o de claims expirados em minutos, separada da reconciliaÃƒÂ§ÃƒÂ£o DST diÃƒÂ¡ria**, com um campo `mode` no evento Ã¢â‚¬â€ minha proposta tratou reconciliaÃƒÂ§ÃƒÂ£o como um job diÃƒÂ¡rio monolÃƒÂ­tico sÃƒÂ³ porque assim jÃƒÂ¡ estÃƒÂ¡ implementado; o Codex estÃƒÂ¡ certo que a lacuna de claim expirado precisa de cadÃƒÂªncia muito mais curta que DST, e isso ÃƒÂ© uma correÃƒÂ§ÃƒÂ£o real ao meu desenho, nÃƒÂ£o sÃƒÂ³ estilo.

## Pontos onde discordo ou quero ajustar

1. **DynamoDB Local vs. LocalStack para a camada 2 (adapters puros).** O Codex descarta DynamoDB Local em favor de LocalStack via Testcontainers para tudo. Concordo que LocalStack ÃƒÂ© necessÃƒÂ¡rio para SQS/Streams/EventBridge Ã¢â‚¬â€ mas para a suÃƒÂ­te que sÃƒÂ³ prova que os adapters DynamoDB (`get`/`putIfAbsent`/`transactWrite`/`queryGsi1`/`queryGsi3`) concordam com as fakes em memÃƒÂ³ria existentes, DynamoDB Local ÃƒÂ© mais leve, mais determinÃƒÂ­stico e mais rÃƒÂ¡pido no dia a dia de CI (ÃƒÂ© o que M0-M3 jÃƒÂ¡ usam para tudo "rÃƒÂ¡pido"). Proposta convergida: DynamoDB Local para a suÃƒÂ­te de paridade adapter-vs-fake (roda em todo PR, rÃƒÂ¡pida); LocalStack/Testcontainers sÃƒÂ³ para a camada que precisa de SQS+Streams+EventBridge de verdade (fault injection, mais pesada, pode rodar em job separado). Isso ÃƒÂ© uma diferenÃƒÂ§a de organizaÃƒÂ§ÃƒÂ£o de teste, nÃƒÂ£o de arquitetura Ã¢â‚¬â€ baixo risco, mas vale registrar para nÃƒÂ£o misturar dois containers pesados onde um simples resolve.
2. **EventBridge Rule legado vs. Scheduler**: o Codex prefere Rule legado citando "jÃƒÂ¡ ÃƒÂ© a tecnologia especificada no escopo" Ã¢â‚¬â€ mas o texto do escopo diz "EventBridge Scheduled Rule", que ÃƒÂ© ambÃƒÂ­guo (pode significar "regra agendada" genericamente, nÃƒÂ£o necessariamente o serviÃƒÂ§o legado `events.Rule`), e `implementation-blueprint.md` linha 162 jÃƒÂ¡ nomeia explicitamente "**EventBridge Scheduler**" para o producer (nÃƒÂ£o Rule). Isso ÃƒÂ© uma decisÃƒÂ£o jÃƒÂ¡ tomada no blueprint aprovado (Claude 9.20/Codex 9.2), nÃƒÂ£o uma decisÃƒÂ£o em aberto deste milestone Ã¢â‚¬â€ nÃƒÂ£o deverÃƒÂ­amos reabri-la aqui sem necessidade. Argumento tÃƒÂ©cnico do Codex (nÃƒÂ£o hÃƒÂ¡ timezone-por-schedule, nÃƒÂ£o hÃƒÂ¡ milhÃƒÂµes de schedules) ÃƒÂ© vÃƒÂ¡lido em abstrato, mas o custo de manter consistÃƒÂªncia com uma decisÃƒÂ£o jÃƒÂ¡ convergida e aprovada pesa mais que a simplicidade marginal de usar Rule. Proposto: manter **EventBridge Scheduler**, `FlexibleTimeWindow: OFF` para producer e claim-reconciliation (precisÃƒÂ£o de minuto importa), janela flexÃƒÂ­vel pequena aceitÃƒÂ¡vel sÃƒÂ³ para o DST diÃƒÂ¡rio.
3. **Toxiproxy para timeout de dependÃƒÂªncia**: acho a escolha correta e mais rigorosa que a minha (eu nÃƒÂ£o tinha um mecanismo concreto de injeÃƒÂ§ÃƒÂ£o de latÃƒÂªncia de rede real). Adoto.

## Itens que nenhuma das duas propostas resolveu e precisam de decisÃƒÂ£o explÃƒÂ­cita nesta rodada

- Quem grava o outbox de dispatch: dentro do MESMO `TransactWriteItems` do claim (mesmo item `OutboxEvent` que M2/M3 jÃƒÂ¡ usam, com um `eventType: "ReminderClaimed"` novo) ou uma tabela/stream dedicada? Recomendo reusar `OutboxEvent`/`shared/outbox/outbox.ts` Ã¢â‚¬â€ nÃƒÂ£o introduzir um segundo mecanismo de outbox paralelo ao que jÃƒÂ¡ existe e jÃƒÂ¡ estÃƒÂ¡ testado; o relay ÃƒÂ© novo, o outbox nÃƒÂ£o precisa ser.
- Nome e forma exata do port de reconciliaÃƒÂ§ÃƒÂ£o (`ReconciliationCandidateSource` do Codex) Ã¢â‚¬â€ preciso ver se cabe como mÃƒÂ©todo adicional em `ReminderStore` (mantendo sÃƒÂ³ 2 interfaces, como hoje) ou se justifica uma terceira interface estreita, como `ReminderProducerStore`. Minha inclinaÃƒÂ§ÃƒÂ£o: terceira interface estreita, pelo mesmo motivo de isolamento que levou a `ReminderProducerStore` a ser separado (a query em GSI6 nÃƒÂ£o deveria ser alcanÃƒÂ§ÃƒÂ¡vel por cÃƒÂ³digo tenant-facing sem necessidade).

## Nota de confianÃƒÂ§a na proposta convergente resultante (apÃƒÂ³s incorporar os ajustes acima): 9.0/10. Risco residual: escopo cresceu (GSI6 real entrou), o que ÃƒÂ© correto tecnicamente mas deve ser comunicado explicitamente ao usuÃƒÂ¡rio como mudanÃƒÂ§a de escopo, nÃƒÂ£o escondido.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 5 "DynamoDB Local|LocalStack|Scheduler|Scheduled Rule|OutboxEvent|GSI6|reconcil|Nota" docs/architecture/reviews/m3.5-runtime-design/claude-proposal.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/m3.5-runtime-design/claude-critique-of-codex-round2.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 4 "DynamoDB Local|LocalStack|Scheduler|Rule legado|outbox|GSI6|candidate|Nota" docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 295ms:
2-
3-Escopo (fixado por `NEXT_SESSION_PROMPT.md`/`ENGINEERING.md` "DecisÃ£o pendente: escopo de G8"):
4-1. Adapters DynamoDB reais para `ReminderStore`, `ExpirationStore`, `IdentityStore` (hoje sÃ³ fakes em memÃ³ria).
5-2. Handlers Lambda reais substituindo os placeholders `501` em `infra/lib/expiration-tracker-stack.ts`.
6-3. Filas SQS + DLQ com redrive policy para o pipeline producerâ†’dispatch.
7:4. EventBridge Scheduled Rule disparando `ReminderProducer` (1 min) e `ReminderReconciliation` (diÃ¡rio).
8-5. Testes de fault injection contra esse runtime real.
9-
10:NÃ£o-escopo (explicitamente adiado, nÃ£o redescobrir): Notification/Document/Extraction (M4+), WhatsApp/e-mail providers, wiring real do GSI6 (item 4 da lista de pendÃªncias), backup/restore real.
11-
12-## 1. Adapters DynamoDB reais
13-
14-- Pacote: `@aws-sdk/client-dynamodb` + `@aws-sdk/lib-dynamodb` (DocumentClient v3), jÃ¡ implÃ­cito pelo blueprint (`implementation-blueprint.md` linha 6 lista DynamoDB na stack; M0-M3 deliberadamente adiaram o SDK real).
15-- Um adapter por port, em `src/modules/<mÃ³dulo>/persistence/dynamodb-<nome>.ts`, implementando a interface exata do port (`ReminderStore`, `ExpirationStore`, `IdentityStore`) â€” nenhuma mudanÃ§a de shape nos ports, sÃ³ nova implementaÃ§Ã£o.
16-- `get`/`putIfAbsent`/`update`/`transactWrite`/`queryByItem`/`queryGsi1`/`queryGsi3` traduzem 1:1 para `GetCommand`/`PutCommand` com `ConditionExpression: attribute_not_exists(PK)`/`UpdateCommand`/`TransactWriteCommand`/`QueryCommand`. Os builders jÃ¡ existentes (`shared/dynamodb/occ.ts`, `shared/idempotency/idempotency.ts`, `shared/outbox/outbox.ts`) jÃ¡ produzem os parÃ¢metros nesse formato â€” os adapters sÃ£o uma camada fina de traduÃ§Ã£o para os `*Command` do SDK, nÃ£o reimplementam lÃ³gica de OCC/idempotÃªncia.
17-- Erros do SDK (`ConditionalCheckFailedException`, `TransactionCanceledException`, `ProvisionedThroughputExceededException`, timeout/throttling) mapeados para a taxonomia de `shared/errors/app-error.ts` (`retryable` vs terminal) no prÃ³prio adapter â€” Ã© o Ãºnico lugar que vÃª exceÃ§Ãµes nativas do SDK; o resto do sistema jÃ¡ Ã© SDK-agnÃ³stico e deve continuar assim.
18:- Teste: suÃ­te de integraÃ§Ã£o contra **DynamoDB Local** (`amazon/dynamodb-local` via Docker, ou `dynamodb-local` npm binÃ¡rio) â€” nÃ£o LocalStack (mais pesado, nÃ£o necessÃ¡rio sÃ³ para DynamoDB) e nÃ£o mocks de SDK (nÃ£o provaria nada sobre `ConditionExpression`/`TransactWriteItems` reais). As suÃ­tes de domÃ­nio existentes (`test/integration/*.test.ts`) rodam hoje contra as fakes em memÃ³ria; adiciona-se um novo alvo `test/integration-dynamodb/` que roda os MESMOS testes de comportamento (reusar os casos, nÃ£o duplicar lÃ³gica) contra o adapter real + DynamoDB Local, provando que a fake e o adapter real concordam. CI ganha um job novo que sobe o container antes da suÃ­te.
19-
20-## 2. Handlers Lambda reais
21-
22-- Bundling: `esbuild` via `NodejsFunction` (aws-cdk-lib) substitui `lambda.Code.fromInline` em `ScopedLambdaFunction` quando um `entryFile` Ã© passado â€” mantÃ©m compatibilidade com os testes existentes que passam `code:` inline. Escolha de `esbuild`/`NodejsFunction` porque jÃ¡ Ã© dependÃªncia transitiva do CDK e Ã© o caminho documentado padrÃ£o, sem tooling adicional.
23:- Cada handler Ã© uma funÃ§Ã£o fina em `src/handlers/<nome>.ts` que: (a) resolve tenant/identity do evento (API Gateway JWT authorizer context para os HTTP; nenhuma resoluÃ§Ã£o de tenant para os workers assÃ­ncronos â€” eles operam cross-tenant por design, GSI3-scoped), (b) constrÃ³i os adapters DynamoDB reais injetando-os nos services/workers jÃ¡ existentes e testados (`ExpirationService`, `ReminderPolicyService`, `reminder-producer.ts`, `reminder-dispatch.ts`, `reminder-reconciliation.ts`), (c) traduz o resultado do worker para o formato de evento (API Gateway response / SQS batch response / EventBridge void).
24-- SQS: handlers de fila usam **partial batch response** (`functionResponseType: ReportBatchItemFailures`) â€” item individual falho nÃ£o derruba o batch inteiro; mensagens nÃ£o reportadas como falha sÃ£o consideradas processadas.
25:- Testes: `test/handlers/*.test.ts` invocam o handler diretamente com eventos sintÃ©ticos (shape real de API Gateway/SQS/EventBridge) e adapters DynamoDB reais contra DynamoDB Local â€” prova a integraÃ§Ã£o handlerâ†”SDKâ†”worker que as suÃ­tes de M0-M3 (lÃ³gica pura) nÃ£o cobriam. `test/infra/stack.test.ts` ganha asserts de que os handlers deixaram de ser `501`/inline.
26-
27-## 3. SQS + DLQ
28-
29-- Nova construct `infra/lib/reminder-queue.ts`: `ReminderDispatchQueue` (fila principal) + `ReminderDispatchDlq`, `maxReceiveCount=5` (valor jÃ¡ documentado em `implementation-blueprint.md` Â§26 para `ReminderDispatchWorker`), redrive policy padrÃ£o SQS, criptografia SSE-SQS (chave gerenciada, sem KMS customer-managed â€” nÃ£o hÃ¡ requisito de compliance que exija CMK ainda, `implementation-blueprint.md` Â§16 nÃ£o especifica; revisar se LGPD/regiÃ£o decidir diferente).
30-- `ReminderProducer` ganha `queueAccessFor().send(queue)`; `ReminderDispatchWorker` ganha `.consume(queue)`, event source mapping com batch size pequeno (10, `implementation-blueprint.md` Â§26 jÃ¡ fixa reserved concurrency 10) e `reportBatchItemFailures`.
31-- DLQ: alarme CloudWatch de idade (1h/4h jÃ¡ documentado em `implementation-blueprint.md` linha 879) â€” implementado como `cloudwatch.Alarm` na mesma construct; sem lambda de auto-redrive nesta fase (redrive manual via runbook, jÃ¡ existe `dlq-redrive/` referenciado na Ã¡rvore do blueprint linha 114 â€” verificar se esse diretÃ³rio jÃ¡ existe ou Ã© follow-up).
32-- Teste de infra: `test/infra/reminder-queue.test.ts` sintetiza e afirma DLQ+redrive policy+maxReceiveCount+encryption via `Template.fromStack`.
33-
34:## 4. EventBridge Scheduled Rule
35-
36:- `infra/lib/reminder-schedule.ts`: duas `scheduler.CfnSchedule` (EventBridge Scheduler, nÃ£o `events.Rule` legado â€” o blueprint linha 162 jÃ¡ nomeia "EventBridge Scheduler" especificamente para o producer, coerente com granularidade de minuto): `ReminderProducerSchedule` (`rate(1 minute)`) invocando `reminderProducer` via alvo Lambda; `ReminderReconciliationSchedule` (`rate(1 day)`, horÃ¡rio fixo off-peak) invocando `reminderReconciliation`.
37-- Ambos com `FlexibleTimeWindow: OFF` (precisÃ£o de minuto Ã© o prÃ³prio mecanismo de correÃ§Ã£o do producer â€” jitter quebraria a janela de lookback `[M-5min, M]`).
38-- Kill switch: os dois schedules nascem `ENABLED`, mas a stack aceita uma prop `schedulesEnabled` (default `true`) para permitir desligar em deploy/teste sem destruir a construct â€” reusa o padrÃ£o de flag booleana jÃ¡ visto em `ExpirationTrackerStackProps.mfaPolicy`.
39-- Teste de infra: assert de `ScheduleExpression`, alvo correto, e que NENHUM outro schedule invoca essas duas funÃ§Ãµes (evita duplicidade de tick).
40-
41-## 5. Fault injection
42-
43:Contra o runtime real (DynamoDB Local + handlers reais, sem mocks de SDK):
44-- **Timeout de dependÃªncia**: adapter com hook de injeÃ§Ã£o de latÃªncia/erro (sÃ³ em modo de teste, nunca em produÃ§Ã£o â€” flag de config, nÃ£o cÃ³digo condicional em produÃ§Ã£o) simulando `ProvisionedThroughputExceededException`/timeout; prova que o handler retorna falha retryable (nÃ£o `reportBatchItemFailures` de item que na verdade nÃ£o falhou) e que o SQS reentrega.
45-- **Poison message**: mensagem SQS malformada (schema invÃ¡lido) injetada diretamente na fila (via SDK real, nÃ£o mock) â†’ prova que vai para DLQ apÃ³s `maxReceiveCount=5` sem derrubar o resto do batch, e que o erro Ã© classificado terminal (nÃ£o retryable) em `app-error.ts` para nÃ£o desperdiÃ§ar as 5 tentativas em algo que nunca vai ter sucesso â€” ponto de risco identificado no P1 #2 do red team (idempotÃªncia no limite do efeito externo): mensagem poison nÃ£o deve reter lock/claim indefinidamente.
46-- **Redrive**: mensagem manualmente movida para DLQ â†’ redrive real (SQS `StartMessageMoveTask` ou CLI) â†’ reprocessada com sucesso, provando que o caminho de recuperaÃ§Ã£o documentado no runbook funciona de fato, nÃ£o sÃ³ em teoria.
47:- Local: usar **LocalStack** aqui (diferente do item 1) porque fault injection de SQS real (redrive, `StartMessageMoveTask`) nÃ£o Ã© bem suportado por simuladores mÃ­nimos â€” mas isolado num job de CI separado/opcional (mais pesado), nÃ£o bloqueante do `npm test` rÃ¡pido de todo dia.
48-
49-## Itens que este milestone NÃƒO fecha (mas o red team apontou como P1 e merecem nota explÃ­cita)
50-
51-- P1 #2 (idempotÃªncia no limite do efeito externo, "at-least-once clÃ¡ssico") e P1 #4 (fencing antes do efeito externo) sÃ£o sobre a LÃ“GICA de dispatch, nÃ£o sobre existir runtime â€” `reminder-dispatch.ts` jÃ¡ claim antes de disparar (M3), mas nÃ£o hÃ¡ "efeito externo" real ainda nesta stack (isso Ã© Notification/M4). Este milestone prova que o pipeline producerâ†’dispatch SOBREVIVE e SE RECUPERA de falha de infraestrutura real; nÃ£o resolve fencing contra um provedor de notificaÃ§Ã£o externo, porque esse provedor ainda nÃ£o existe no sistema. Deve ficar registrado explicitamente para nÃ£o ser mal interpretado como "G8 fechado = idempotÃªncia ponta a ponta provada" quando K4 (Notification) ainda nÃ£o existe.
52-- P1 #5 (blast radius cross-tenant do GSI3): este milestone adiciona o primeiro teste negativo contra o ADAPTER real (nÃ£o sÃ³ a fake), mas nÃ£o muda o desenho de IAM jÃ¡ existente (M3 jÃ¡ corrigiu `grantGsi3ReadTo` explÃ­cito).

 succeeded in 298ms:
# M3.5 Ã¢â‚¬â€ Rodada 2 Ã¢â‚¬â€ CrÃƒÂ­tica da Claude ÃƒÂ  proposta do Codex (Rodada 1)

Li `codex-output-round1.txt` na ÃƒÂ­ntegra apÃƒÂ³s submeter minha prÃƒÂ³pria proposta independente (`claude-proposal.md`), conforme protocolo de nota cega (AGENTS.md Ã‚Â§4).

## Pontos onde a proposta do Codex ÃƒÂ© claramente superior Ã¢â‚¬â€ adoto

1. **Gap real que minha proposta nÃƒÂ£o viu: `TransactWrite(claim)` seguido de `SendMessage` direto no handler nÃƒÂ£o ÃƒÂ© atÃƒÂ´mico.** Se a Lambda cai entre os dois passos, a ocorrÃƒÂªncia fica `CLAIMED` sem mensagem em fila, e a reconciliaÃƒÂ§ÃƒÂ£o diÃƒÂ¡ria pode nÃƒÂ£o alcanÃƒÂ§ar essa ocorrÃƒÂªncia dentro da janela antes de virar um problema (ela reverte claim expirado, mas isso ÃƒÂ© *outro* ciclo, nÃƒÂ£o fecha a lacuna descrita). O Codex estÃƒÂ¡ certo: fila+DLQ sozinhas nÃƒÂ£o fecham P1 #1 sem uma etapa durÃƒÂ¡vel entre claim e publicaÃƒÂ§ÃƒÂ£o. Adoto **DynamoDB Streams Ã¢â€ â€™ `DispatchOutboxRelay` Lambda Ã¢â€ â€™ SQS**, reusando o padrÃƒÂ£o jÃƒÂ¡ existente de `shared/outbox/outbox.ts` (a mesma ideia de "outbox transacional" do M2/M3, agora com um relay real em vez de placeholder) em vez de inventar mecanismo novo.
2. **Gap real: a reconciliaÃƒÂ§ÃƒÂ£o nÃƒÂ£o tem fonte de candidatos.** Minha proposta tratou reconciliaÃƒÂ§ÃƒÂ£o como "jÃƒÂ¡ implementada, sÃƒÂ³ falta plugar" Ã¢â‚¬â€ errado. `runReconciliation` recebe arrays injetados; sem um port `ReconciliationCandidateSource` (ou equivalente) apoiado em GSI6 consultÃƒÂ¡vel, rodar a Lambda diariamente com array vazio ÃƒÂ© exatamente o false-green que o red team apontou (P1 #3). Adoto a proposta do Codex de um port estreito novo para isso, E que o wiring real do GSI6 (item 4 da lista de pendÃƒÂªncias em `NEXT_SESSION_PROMPT.md`) entra NO escopo deste milestone Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© mais um "follow-up" separado, porque sem ele o item 4 do escopo do milestone (EventBridge disparando reconciliation) ÃƒÂ© teatro.
3. **Teste em sandbox AWS efÃƒÂªmero como terceira camada, nÃƒÂ£o substituÃƒÂ­vel por LocalStack/DynamoDB Local.** Concordo Ã¢â‚¬â€ IAM real, event source mapping real e EventBridge real nÃƒÂ£o sÃƒÂ£o fielmente simulados. Isso tambÃƒÂ©m resolve minha "pergunta aberta": meu plano de fault injection via LocalStack sÃƒÂ³ para a camada de filas era necessÃƒÂ¡rio mas nÃƒÂ£o suficiente; preciso do sandbox real para o teste negativo de GSI3 (IAM real) e para provar redrive de verdade.
4. **CadÃƒÂªncia de reconciliaÃƒÂ§ÃƒÂ£o de claims expirados em minutos, separada da reconciliaÃƒÂ§ÃƒÂ£o DST diÃƒÂ¡ria**, com um campo `mode` no evento Ã¢â‚¬â€ minha proposta tratou reconciliaÃƒÂ§ÃƒÂ£o como um job diÃƒÂ¡rio monolÃƒÂ­tico sÃƒÂ³ porque assim jÃƒÂ¡ estÃƒÂ¡ implementado; o Codex estÃƒÂ¡ certo que a lacuna de claim expirado precisa de cadÃƒÂªncia muito mais curta que DST, e isso ÃƒÂ© uma correÃƒÂ§ÃƒÂ£o real ao meu desenho, nÃƒÂ£o sÃƒÂ³ estilo.

## Pontos onde discordo ou quero ajustar

1. **DynamoDB Local vs. LocalStack para a camada 2 (adapters puros).** O Codex descarta DynamoDB Local em favor de LocalStack via Testcontainers para tudo. Concordo que LocalStack ÃƒÂ© necessÃƒÂ¡rio para SQS/Streams/EventBridge Ã¢â‚¬â€ mas para a suÃƒÂ­te que sÃƒÂ³ prova que os adapters DynamoDB (`get`/`putIfAbsent`/`transactWrite`/`queryGsi1`/`queryGsi3`) concordam com as fakes em memÃƒÂ³ria existentes, DynamoDB Local ÃƒÂ© mais leve, mais determinÃƒÂ­stico e mais rÃƒÂ¡pido no dia a dia de CI (ÃƒÂ© o que M0-M3 jÃƒÂ¡ usam para tudo "rÃƒÂ¡pido"). Proposta convergida: DynamoDB Local para a suÃƒÂ­te de paridade adapter-vs-fake (roda em todo PR, rÃƒÂ¡pida); LocalStack/Testcontainers sÃƒÂ³ para a camada que precisa de SQS+Streams+EventBridge de verdade (fault injection, mais pesada, pode rodar em job separado). Isso ÃƒÂ© uma diferenÃƒÂ§a de organizaÃƒÂ§ÃƒÂ£o de teste, nÃƒÂ£o de arquitetura Ã¢â‚¬â€ baixo risco, mas vale registrar para nÃƒÂ£o misturar dois containers pesados onde um simples resolve.
2. **EventBridge Rule legado vs. Scheduler**: o Codex prefere Rule legado citando "jÃƒÂ¡ ÃƒÂ© a tecnologia especificada no escopo" Ã¢â‚¬â€ mas o texto do escopo diz "EventBridge Scheduled Rule", que ÃƒÂ© ambÃƒÂ­guo (pode significar "regra agendada" genericamente, nÃƒÂ£o necessariamente o serviÃƒÂ§o legado `events.Rule`), e `implementation-blueprint.md` linha 162 jÃƒÂ¡ nomeia explicitamente "**EventBridge Scheduler**" para o producer (nÃƒÂ£o Rule). Isso ÃƒÂ© uma decisÃƒÂ£o jÃƒÂ¡ tomada no blueprint aprovado (Claude 9.20/Codex 9.2), nÃƒÂ£o uma decisÃƒÂ£o em aberto deste milestone Ã¢â‚¬â€ nÃƒÂ£o deverÃƒÂ­amos reabri-la aqui sem necessidade. Argumento tÃƒÂ©cnico do Codex (nÃƒÂ£o hÃƒÂ¡ timezone-por-schedule, nÃƒÂ£o hÃƒÂ¡ milhÃƒÂµes de schedules) ÃƒÂ© vÃƒÂ¡lido em abstrato, mas o custo de manter consistÃƒÂªncia com uma decisÃƒÂ£o jÃƒÂ¡ convergida e aprovada pesa mais que a simplicidade marginal de usar Rule. Proposto: manter **EventBridge Scheduler**, `FlexibleTimeWindow: OFF` para producer e claim-reconciliation (precisÃƒÂ£o de minuto importa), janela flexÃƒÂ­vel pequena aceitÃƒÂ¡vel sÃƒÂ³ para o DST diÃƒÂ¡rio.
3. **Toxiproxy para timeout de dependÃƒÂªncia**: acho a escolha correta e mais rigorosa que a minha (eu nÃƒÂ£o tinha um mecanismo concreto de injeÃƒÂ§ÃƒÂ£o de latÃƒÂªncia de rede real). Adoto.

## Itens que nenhuma das duas propostas resolveu e precisam de decisÃƒÂ£o explÃƒÂ­cita nesta rodada

- Quem grava o outbox de dispatch: dentro do MESMO `TransactWriteItems` do claim (mesmo item `OutboxEvent` que M2/M3 jÃƒÂ¡ usam, com um `eventType: "ReminderClaimed"` novo) ou uma tabela/stream dedicada? Recomendo reusar `OutboxEvent`/`shared/outbox/outbox.ts` Ã¢â‚¬â€ nÃƒÂ£o introduzir um segundo mecanismo de outbox paralelo ao que jÃƒÂ¡ existe e jÃƒÂ¡ estÃƒÂ¡ testado; o relay ÃƒÂ© novo, o outbox nÃƒÂ£o precisa ser.
- Nome e forma exata do port de reconciliaÃƒÂ§ÃƒÂ£o (`ReconciliationCandidateSource` do Codex) Ã¢â‚¬â€ preciso ver se cabe como mÃƒÂ©todo adicional em `ReminderStore` (mantendo sÃƒÂ³ 2 interfaces, como hoje) ou se justifica uma terceira interface estreita, como `ReminderProducerStore`. Minha inclinaÃƒÂ§ÃƒÂ£o: terceira interface estreita, pelo mesmo motivo de isolamento que levou a `ReminderProducerStore` a ser separado (a query em GSI6 nÃƒÂ£o deveria ser alcanÃƒÂ§ÃƒÂ¡vel por cÃƒÂ³digo tenant-facing sem necessidade).

## Nota de confianÃƒÂ§a na proposta convergente resultante (apÃƒÂ³s incorporar os ajustes acima): 9.0/10. Risco residual: escopo cresceu (GSI6 real entrou), o que ÃƒÂ© correto tecnicamente mas deve ser comunicado explicitamente ao usuÃƒÂ¡rio como mudanÃƒÂ§a de escopo, nÃƒÂ£o escondido.


 succeeded in 310ms:
23-
24-Arquitetura relevante ja existente e fixa (nao redesenhar, so integrar):
25-- Ports SDK-agnosticos ja definidos: ReminderStore (get/putIfAbsent/update/transactWrite/queryByItem), ReminderProducerStore (queryGsi3/get/transactWrite, isolado do resto por desenho), ExpirationStore, IdentityStore - todos em src/modules/*/ports/.
26-- shared/dynamodb/occ.ts ja produz os parametros de ConditionExpression/TransactWriteItems (builders puros, sem SDK ainda).
27:- shared/outbox/outbox.ts, shared/idempotency/idempotency.ts, shared/errors/app-error.ts (taxonomia retryable/terminal) ja existem.
28-- infra/lib/scoped-lambda-function.ts: todo Lambda deve nascer via ScopedLambdaFunction (unico ponto de enforcement de IAM least-privilege), hoje usa lambda.Code.fromInline para os placeholders.
29-- infra/lib/expiration-tracker-stack.ts ja declara os 5 ScopedLambdaFunction do Reminder Engine (RemindersHandler HTTP, ReminderProducer com gsi3Read exclusivo, ReminderDispatch, ReminderReconciliation) com as capabilities de tabela corretas - falta so trocar o codigo placeholder por bundle real e adicionar fila/schedule.
30-- GSI3 e deliberadamente isolado: so ReminderProducerStore/ReminderProducer pode fazer Query nele (protecao contra P1 #5), enforcement via IAM real (Table.grantReadWriteData do CDK inclui TODOS os indices por padrao - ja corrigido com PolicyStatement explicito).
31-- Reserved concurrency e DLQ ja documentados no blueprint (implementation-blueprint.md secao 26): ReminderDispatchWorker concurrency 10, DLQ maxReceiveCount=5; ReminderProducer concurrency 2, sem DLQ (lookback [M-5min,M] cobre falha de tick); ReminderReconciliation concurrency 1, sem DLQ (idempotente, diario).
32-
33:Produza uma proposta tecnica concreta cobrindo os 5 itens do escopo: escolhas de biblioteca/pacote AWS SDK v3, estrutura de arquivos dos adapters DynamoDB (um por port, mantendo os ports SDK-agnosticos como estao), estrategia de bundling dos handlers Lambda reais, desenho da fila+DLQ (nomes, redrive policy, batch size, partial batch failure), desenho do EventBridge Schedule (Scheduler vs Rule legado, flexible time window), e como voce testaria fault injection de forma real (nao mockada) sem tornar o CI lento/fragil demais - inclua sua escolha entre DynamoDB Local, LocalStack, ou testcontainers, e por que. Aponte explicitamente o que este milestone NAO resolve dos 5 P1 do red team (idempotencia ponta-a-ponta contra efeito externo real depende de Notification/M4, que nao existe ainda) para evitar falsa sensacao de "G8 fechado = tudo resolvido". Termine com uma nota de confianca 0-10 na sua propria proposta e os riscos que mais te preocupam.
34-
35-Responda em portugues, formato markdown, direto ao ponto (nao precisa de preambulo).
36-
37-codex
--
80-Todo o processo de design (Fases 0-3 do prompt mestre + os 14 entregÃƒÂ¡veis das seÃƒÂ§ÃƒÂµes 35-52 + threat model seÃƒÂ§ÃƒÂ£o 33 + Implementation Blueprint seÃƒÂ§ÃƒÂ£o 60) estÃƒÂ¡ completo e aprovado Ã¢â‚¬â€ ver `ARCHITECTURE.md` na raiz para o documento consolidado. `ARCHITECTURE STATUS: NOT APPROVED` ÃƒÂ© o estado normativo correto atÃƒÂ© haver implementaÃƒÂ§ÃƒÂ£o real testada sob falha/carga (rubrica B, `requirements.md` Ã‚Â§13.1) Ã¢â‚¬â€ **nÃƒÂ£o ÃƒÂ© reprovaÃƒÂ§ÃƒÂ£o de mÃƒÂ©rito, e nÃƒÂ£o muda com a conclusÃƒÂ£o do blueprint**: o blueprint ÃƒÂ© design detalhado, nÃƒÂ£o evidÃƒÂªncia operacional.
81-
82-## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ Implementation Blueprint (seÃƒÂ§ÃƒÂ£o 60) Ã¢â‚¬â€ APPROVED
83-
84:`docs/architecture/implementation-blueprint.md`. Componentes/mÃƒÂ³dulos (Identity, Expiration, Reminder, Notification, Document, Audit + workers assÃƒÂ­ncronos), interfaces concretas, eventos/schemas reais (grounded em `data-model.md`), ordem de deploy, milestones M0-M7, critÃƒÂ©rios de aceite tÃƒÂ©cnicos por componente, e as 7 lacunas do `threat-model.md` incorporadas como requisito desde o inÃƒÂ­cio (nÃƒÂ£o apÃƒÂªndice). Processo: 2 propostas independentes (Claude/Codex) Ã¢â€ â€™ crÃƒÂ­tica cruzada (17 problemas reais encontrados, incluindo um erro tÃƒÂ©cnico presente nas duas propostas Ã¢â‚¬â€ chave do GSI3 do scheduler nÃƒÂ£o era consultÃƒÂ¡vel) Ã¢â€ â€™ convergÃƒÂªncia Ã¢â€ â€™ 5 rodadas de nota cega com correÃƒÂ§ÃƒÂ£o pontual a cada achado (cabeÃƒÂ§alho prematuro, contradiÃƒÂ§ÃƒÂ£o de kill switch, tabela incompleta, decisÃƒÂµes Type 1 nÃƒÂ£o propagadas ÃƒÂ s seÃƒÂ§ÃƒÂµes operativas, mapeamento de estado da Step Functions incompleto). **Nota final: Claude 9.20 / Codex 9.2 (exato)** Ã¢â‚¬â€ ambos Ã¢â€°Â¥9.0 sem arredondar, 8 rodadas totais, nenhum gate violado. DecisÃƒÂ£o Type 1 nova registrada: chave global do GSI3 (`GSI3PK=DUE#yyyyMMddHHmm#NN`, exceÃƒÂ§ÃƒÂ£o documentada ÃƒÂ  regra de toda chave comeÃƒÂ§ar por `TENANT#tenantId`) Ã¢â‚¬â€ a decisÃƒÂ£o estÃƒÂ¡ fechada no blueprint; falta sÃƒÂ³ o registro mecÃƒÂ¢nico em `data-model.md` (nÃƒÂ£o bloqueia implementaÃƒÂ§ÃƒÂ£o).
85-
86-## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M0 "Guardrails e contratos" (implementation-blueprint.md Ã‚Â§19)
87-
88:Todas as entregas de M0 implementadas e testadas (53 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): estrutura TypeScript (`src/shared/*`, alinhada a `implementation-blueprint.md` Ã‚Â§2), schemas JSON (`schemas/{events,queues,api}` + `sensitive-fields.json`, validados via Ajv em `src/shared/contracts/schema-validator.ts`), `SecureLogger`+`Redactor` central (`src/shared/observability/`, corpus de teste com valores canÃƒÂ¡rio provando que nada vaza), configuraÃƒÂ§ÃƒÂ£o tipada fail-fast (`src/shared/config/config.ts`), taxonomia de erro normalizada retryable/terminal (`src/shared/errors/app-error.ts`), idempotÃƒÂªncia via `PutItem attribute_not_exists(PK)` (`src/shared/idempotency/idempotency.ts`, chaves por operaÃƒÂ§ÃƒÂ£o conforme `data-model.md` Ã‚Â§4), OCC com `ConditionExpression: version=:expected` (`src/shared/dynamodb/occ.ts`, `data-model.md` Ã‚Â§5), outbox transacional (`src/shared/outbox/outbox.ts`, shape de `implementation-blueprint.md` Ã‚Â§5.3), pipeline supply-chain (`.github/workflows/ci.yml`: npm ci imutÃƒÂ¡vel, actions pinadas por SHA, SBOM CycloneDX, audit, gate de schema). `AGENTS.md` Ã‚Â§6 atualizado com comandos/convenÃƒÂ§ÃƒÂµes. Nada foi commitado (working tree aberto para revisÃƒÂ£o).
89-
90:Judgment calls (blueprint estava silente): Ajv+ajv-formats para validaÃƒÂ§ÃƒÂ£o de JSON Schema; Vitest como test runner; ESLint `no-console` (com exceÃƒÂ§ÃƒÂ£o para `src/shared/observability/**`) como o mecanismo que faz "chamada direta a `console.*` falhar no lint" (`implementation-blueprint.md` Ã‚Â§14.1); idempotÃƒÂªncia/OCC/outbox construÃƒÂ­dos como builders puros de parÃƒÂ¢metros DynamoDB (sem `@aws-sdk` ainda) para ficarem testÃƒÂ¡veis sem tabelas reais, que sÃƒÂ³ existem a partir de M1; SLSA/assinatura de artefato adiada para M1+ (nÃƒÂ£o hÃƒÂ¡ alvo de deploy ainda); SHAs de actions no CI pinados no momento da escrita Ã¢â‚¬â€ revisar antes do primeiro run real.
91-
92-## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M1 "Foundation, Identity e isolamento" (implementation-blueprint.md Ã‚Â§19)
93-
94-Todas as entregas implementadas e testadas (89 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/identity/{domain,application,ports,persistence,http}` (resolver central, matriz de autorizaÃƒÂ§ÃƒÂ£o como cÃƒÂ³digo, `IdentityMapping`/`User`/`DeviceSession`/`TenantQuota` via portas SDK-agnÃƒÂ³sticas) e `infra/{lib,bin}` (`ExpirationTrackerTable`, `ExpirationTrackerAuth`, `ScopedLambdaFunction`, `ExpirationTrackerApi` com a rota `GET /test/ping`). SuÃƒÂ­te cross-tenant negativa em `test/integration/cross-tenant.test.ts` (9 casos, exit criterion do marco) e synth de infra em `test/infra/stack.test.ts` (6 casos, via `aws-cdk-lib/assertions`, sem AWS CLI). Detalhe completo em `docs/architecture/session-log.md`.
--
96-Judgment calls (blueprint silente ou pendÃƒÂªncia externa jÃƒÂ¡ conhecida): (1) MFA implementado como prop configurÃƒÂ¡vel (`OFF`/`OPTIONAL`/`REQUIRED`, default `OPTIONAL`) pois UNK-006 segue pendente de pesquisa externa Ã¢â‚¬â€ nÃƒÂ£o bloqueava M1; (2) CSP/CloudFront Response Headers Policy **nÃƒÂ£o implementado nesta sessÃƒÂ£o** Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ distribuiÃƒÂ§ÃƒÂ£o CloudFront/frontend ainda no repositÃƒÂ³rio, e o texto do blueprint (Ã‚Â§4.2) coloca CSP no contexto do SPA estÃƒÂ¡tico servido por CloudFront; tratado como pertencente ao milestone que introduzir o frontend, nÃƒÂ£o forÃƒÂ§ado em M1 (revisar se essa leitura estiver errada); (3) sessÃƒÂ£o/revogaÃƒÂ§ÃƒÂ£o modelada como campo `globalLogoutAfter` no item `User` + item filho `DeviceSession` (`TENANT#t#USER#u`/`SESSION#<deviceId>`) em vez de uma entidade `Session` de primeira classe Ã¢â‚¬â€ `data-model.md` nÃƒÂ£o define uma; (4) `ScopedLambdaFunction` mantÃƒÂ©m a lista de entidades por capability como metadado/documentaÃƒÂ§ÃƒÂ£o, mas o IAM real concedido ÃƒÂ© table-level (`grantReadWriteData`) Ã¢â‚¬â€ DynamoDB IAM nÃƒÂ£o expressa restriÃƒÂ§ÃƒÂ£o por SK/entidade da forma que a sintaxe do blueprint sugere visualmente; per-entity IAM mais fino fica como follow-up; (5) `TenantQuota` implementado como janela fixa (fixed-window), nÃƒÂ£o sliding-window/leaky-bucket Ã¢â‚¬â€ mais simples e ainda satisfaz "decremento atÃƒÂ´mico, sem race condition" do `data-model.md`; (6) BFF de sessÃƒÂ£o (`/session/refresh`, `/session/logout`) **nÃƒÂ£o implementado como rota HTTP nesta sessÃƒÂ£o** Ã¢â‚¬â€ o Cognito client jÃƒÂ¡ estÃƒÂ¡ configurado para o padrÃƒÂ£o (client secret, tokens de vida curta), mas os endpoints do BFF em si ficam para quando a Expiration/Notification API skeleton crescer alÃƒÂ©m da rota de teste; revisar se isso deveria ter sido parte do exit criterion. `cdk synth` real via CLI nÃƒÂ£o foi executado (o pacote `aws-cdk` CLI nÃƒÂ£o estÃƒÂ¡ instalado, sÃƒÂ³ a lib `aws-cdk-lib`); a sintetizaÃƒÂ§ÃƒÂ£o foi verificada programaticamente via `Template.fromStack` nos testes, que ÃƒÂ© equivalente para fins de validaÃƒÂ§ÃƒÂ£o estÃƒÂ¡tica.
97-
98-## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M2 "Expiration core e Audit" (implementation-blueprint.md Ã‚Â§19)
99-
100:Todas as entregas implementadas e testadas (134 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/expiration/{domain,application,ports,http}` Ã¢â‚¬â€ CRUD/renew (`ExpirationService`: createItem/getItem/updateItem/archiveItem/deleteItem/renewItem/listDashboard), OCC via `shared/dynamodb/occ.ts`, `ItemDueDateChanged` por outbox (`shared/outbox/outbox.ts`) na MESMA `TransactWriteItems` do item quando `dueDate` muda (updateItem e renewItem), `AuditEvent` append-only gravado em toda mutaÃƒÂ§ÃƒÂ£o na mesma transaÃƒÂ§ÃƒÂ£o, dashboard via GSI1. `renewItem` cria um item sucessor (`renewedFromId`) em vez de mutar `dueDate` no agregado de origem, que transiciona para `RENEWED`; ÃƒÂ© idempotente via `IdempotencyStore` de M0. Rotas HTTP CDK adicionadas em `infra/lib/api.ts`/`expiration-tracker-stack.ts` (`ItemsHandler`, mesmo authorizer JWT de M1). SuÃƒÂ­te de integraÃƒÂ§ÃƒÂ£o `test/integration/expiration-lifecycle.test.ts` prova o exit criterion do marco end-to-end via handlers HTTP reais. Detalhe completo em `docs/architecture/session-log.md`.
101-
102-Judgment calls (blueprint silente): (1) evento `ItemDueDateChanged` segue exatamente o schema jÃƒÂ¡ existente desde M0 (`schemas/events/item-due-date-changed.v1.json`: `itemId`/`previousDueDate`/`newDueDate`/`itemVersion`, `additionalProperties: false`) em vez do exemplo mais rico do texto do blueprint Ã‚Â§8.3 (que inclui `timeZone`/`reminderPolicyId`/`changeReason`) Ã¢â‚¬â€ o schema jÃƒÂ¡ testado/versionado ÃƒÂ© a fonte de verdade de contrato per `AGENTS.md` Ã‚Â§6, o exemplo em prosa nÃƒÂ£o; campos adicionais ficam para quando M3 (Reminder) precisar deles, exigindo `.v2` aditivo; (2) `renewItem` tambÃƒÂ©m dispara `ItemDueDateChanged` (para o item novo, `previousDueDate: null`) Ã¢â‚¬â€ o blueprint sÃƒÂ³ descreve esse evento no contexto de `updateItem`, mas renovaÃƒÂ§ÃƒÂ£o tambÃƒÂ©m ÃƒÂ© uma "mudanÃƒÂ§a de vencimento" do ponto de vista do futuro `ReminderProducer` (M3), e a alternativa (renovaÃƒÂ§ÃƒÂ£o silenciosa) deixaria o dashboard/scheduler sem sinal de que um novo item precisa de agendamento; (3) `archiveItem` usa a aÃƒÂ§ÃƒÂ£o de autorizaÃƒÂ§ÃƒÂ£o `item:update` (nÃƒÂ£o uma aÃƒÂ§ÃƒÂ£o dedicada Ã¢â‚¬â€ a matriz de M1 nÃƒÂ£o define uma para archive) enquanto `deleteItem` usa `item:delete` (ADMIN_ROLES), seguindo a granularidade que jÃƒÂ¡ existe; (4) idempotÃƒÂªncia de renovaÃƒÂ§ÃƒÂ£o aceita uma `idempotencyKey` explÃƒÂ­cita do chamador (header `Idempotency-Key`) com fallback determinÃƒÂ­stico `itemId|expectedVersion|cycle` quando ausente, jÃƒÂ¡ que o blueprint define a CHAVE de dedupe mas nÃƒÂ£o de onde ela vem por HTTP; (5) `ExpirationStore.transactWrite` foi adicionado ao port (nÃƒÂ£o existe em `IdentityStore`) porque nenhuma escrita de M1 precisava de `TransactWriteItems` multi-item Ã¢â‚¬â€ a fake em memÃƒÂ³ria (`test/unit/expiration/in-memory-store.ts`) sÃƒÂ³ entende as duas formas exatas de `ConditionExpression` que este cÃƒÂ³digo produz (mesma limitaÃƒÂ§ÃƒÂ£o documentada, mesmo espÃƒÂ­rito de `InMemoryIdentityStore`).
103-
104-## ConcluÃƒÂ­do nesta sessÃƒÂ£o Ã¢â‚¬â€ M3 "Reminder Engine" (implementation-blueprint.md Ã‚Â§19)
105-
106:Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversÃƒÂ£o localÃ¢â€ â€™UTC IANA-aware Ã¢â‚¬â€ DST tratado explicitamente, horÃƒÂ¡rio inexistente e ambÃƒÂ­guo Ã¢â‚¬â€ reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lÃƒÂ³gica pura testÃƒÂ¡vel com relÃƒÂ³gio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMEDÃ¢â€ â€™TRIGGERED`+`NotificationIntent`+outbox numa ÃƒÂºnica transaÃƒÂ§ÃƒÂ£o; reconciliaÃƒÂ§ÃƒÂ£o ÃƒÂ© um ÃƒÂºnico job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluÃƒÂ­am `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER funÃƒÂ§ÃƒÂ£o da tabela (inclusive as tenant-facing de M1/M2) Ã¢â‚¬â€ a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explÃƒÂ­citos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que sÃƒÂ³ `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.
107-
108:Judgment calls (blueprint silente): (1) `occurrenceId` derivado deterministicamente (hash da chave de idempotÃƒÂªncia de data-model.md Ã‚Â§4) em vez de UUID + registro `IdempotencyStore` separado Ã¢â‚¬â€ o prÃƒÂ³prio `putIfAbsent` condicional jÃƒÂ¡ garante a idempotÃƒÂªncia de materializaÃƒÂ§ÃƒÂ£o, mesmo padrÃƒÂ£o que data-model.md documenta para `WebhookInbox`; (2) cancelamento de ocorrÃƒÂªncia stale nÃƒÂ£o remove `GSI3PK`/`GSI3SK` (o builder de `occ.ts` ÃƒÂ© SET-only, sem REMOVE) Ã¢â‚¬â€ deixa um ponteiro ÃƒÂ³rfÃƒÂ£o no ÃƒÂ­ndice, mas o `Query` condicional `SCHEDULEDÃ¢â€ â€™CLAIMED` do producer falha sobre ele de forma inofensiva; (3) tolerÃƒÂ¢ncia de dispatch (`toleranceMs`, default 30min) e TTL de claim (default 2min) nÃƒÂ£o estÃƒÂ£o fixados no blueprint Ã¢â‚¬â€ valores razoÃƒÂ¡veis documentados no cÃƒÂ³digo, revisar contra dados reais de produÃƒÂ§ÃƒÂ£o; (4) reconciliaÃƒÂ§ÃƒÂ£o DST/claim-expiry recebe os candidatos como parÃƒÂ¢metro (batch) em vez de fazer sua prÃƒÂ³pria varredura via GSI6 Ã¢â‚¬â€ a wiring desse ÃƒÂ­ndice de "polÃƒÂ­ticas ativas"/"claims expirados" real fica como follow-up de infra, o mecanismo de reconciliaÃƒÂ§ÃƒÂ£o em si (o que a tarefa pediu) estÃƒÂ¡ implementado e testado; (5) Lambda handlers reais (bundling com `@aws-sdk`) nÃƒÂ£o foram escritos Ã¢â‚¬â€ mesmo estÃƒÂ¡gio que M0-M2, CDK usa cÃƒÂ³digo inline placeholder via `ScopedLambdaFunction`; a lÃƒÂ³gica testÃƒÂ¡vel ÃƒÂ© o entregÃƒÂ¡vel real desta fase, igual ÃƒÂ s anteriores.
109-
110-## PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o obrigatÃƒÂ³ria (histÃƒÂ³rico Ã¢â‚¬â€ superada pela seÃƒÂ§ÃƒÂ£o do topo)
111-
112-**Superada em 2026-08-19**: a Engineering Maturity Review identificou G8 (recuperaÃƒÂ§ÃƒÂ£o real de falha assÃƒÂ­ncrona) como bloqueador de engenharia Ã¢â‚¬â€ o milestone de runtime real (adapters/handlers/filas/EventBridge, ver seÃƒÂ§ÃƒÂ£o do topo) ÃƒÂ© prÃƒÂ©-requisito antes de M4 fazer sentido operacionalmente, ainda que M4 nÃƒÂ£o dependa dele estruturalmente. Lista original preservada abaixo como histÃƒÂ³rico, nÃƒÂ£o como prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o vigente.
113-
114-1. **M4 Ã¢â‚¬â€ Notification Engine** (`implementation-blueprint.md` Ã‚Â§19, depende de M3 Ã¢Å“â€¦): router de `NotificationIntent`Ã¢â€ â€™canal, delivery workers (email/WhatsApp stub-first), `NotificationAttempt`, resoluÃƒÂ§ÃƒÂ£o de destinatÃƒÂ¡rio/template.
115-2. Decidir e fechar os itens abaixo (BFF de sessÃƒÂ£o como rotas reais, CSP/CloudFront quando o frontend existir) antes de considerar a lacuna de "session theft" do threat model totalmente fechada Ã¢â‚¬â€ ainda nÃƒÂ£o resolvido, re-flagueado a cada sessÃƒÂ£o desde M1 para nÃƒÂ£o se perder: M1 fechou o mecanismo de revogaÃƒÂ§ÃƒÂ£o/matriz de autorizaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o o endpoint BFF completo; M2/M3 nÃƒÂ£o tinham escopo de frontend/sessÃƒÂ£o (confirmado no blueprint) entÃƒÂ£o tambÃƒÂ©m nÃƒÂ£o o resolveram.
116-3. **Ratificar formalmente em `data-model.md` a exceÃƒÂ§ÃƒÂ£o do GSI3** (chave global do scheduler, jÃƒÂ¡ decidida e justificada em `implementation-blueprint.md` Ã‚Â§9.2/Ã‚Â§23.3 item 10) Ã¢â‚¬â€ passo de manutenÃƒÂ§ÃƒÂ£o documental, nÃƒÂ£o nova rodada de debate. M3 jÃƒÂ¡ corrigiu o enforcement real do isolamento em IAM; falta sÃƒÂ³ o registro mecÃƒÂ¢nico no documento.
117:4. Wiring real do GSI6 (ou ÃƒÂ­ndice equivalente) para alimentar a reconciliaÃƒÂ§ÃƒÂ£o de M3 com o batch real de "polÃƒÂ­ticas ativas"/"claims a verificar" em produÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ hoje o mecanismo de reconciliaÃƒÂ§ÃƒÂ£o recebe esse batch como parÃƒÂ¢metro injetado (testÃƒÂ¡vel), a query real fica pendente.
118-5. Testes de carga real (cenÃƒÂ¡rios de drenagem do pico extremo, `capacity-model.md`, com SLO de drenagem UNK-CAP-006 a formalizar em `slo.md` antes do gate de M7), teste de restore real (gate jÃƒÂ¡ definido em `disaster-recovery.md` Ã‚Â§6), exercÃƒÂ­cio do runbook de credencial comprometida.
119-6. ReavaliaÃƒÂ§ÃƒÂ£o sob rubrica (B) Ã¢â‚¬â€ Operational Evidence Ã¢â‚¬â€ sÃƒÂ³ entÃƒÂ£o `ARCHITECTURE STATUS` pode legitimamente virar `APPROVED`. **NÃƒÂ£o declarar isso antes de haver evidÃƒÂªncia operacional real.** Implementar M0/M1/M2/M3 nÃƒÂ£o ÃƒÂ© evidÃƒÂªncia operacional.
120-7. DecisÃƒÂµes ainda pendentes de pesquisa externa (nÃƒÂ£o bloqueiam M4 estruturalmente, mas bloqueiam habilitar os canais/features especÃƒÂ­ficos, ver `implementation-blueprint.md` Ã‚Â§23.3): provider inicial de e-mail, BSP WhatsApp (pricing real, UNK-003), modelo Bedrock especÃƒÂ­fico, regiÃƒÂ£o AWS (bloqueante para LGPD/transferÃƒÂªncia internacional), MFA obrigatÃƒÂ³rio vs. opcional (UNK-006), ferramenta de backup S3 (RPOÃ¢â€°Â¤24h), ferramenta de assinatura/provenance do pipeline de CI.
121-# docs/architecture/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade
--
184-
185-exec
186-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^export interface|''^export type|''^export class|ReminderProducerStore|501|ScopedLambdaFunction|reservedConcurrent|GSI3|ReminderDispatch|ReminderReconciliation" src/modules src/workers infra/lib docs/architecture/implementation-blueprint.md package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
187- succeeded in 256ms:
188:docs/architecture/implementation-blueprint.md:11:1. **Scheduler GSI3 corrigido** â€” erro tÃ©cnico real presente nas duas propostas independentes: uma partition key `TENANT#t#DUE#yyyyMMddHHmm#NN` nÃ£o Ã© consultÃ¡vel pelo producer do minuto sem jÃ¡ saber quais tenants tÃªm ocorrÃªncia naquele minuto (`Query` exige PK exata). Corrigido em todo o documento para `GSI3PK=DUE#yyyyMMddHHmm#NN` / `GSI3SK=TENANT#tenantId#OCCURRENCE#occurrenceId` â€” exceÃ§Ã£o documentada e justificada Ã  regra "toda chave comeÃ§a com `TENANT#tenantId`" de `data-model.md` Â§3, com as salvaguardas da Â§9.2 abaixo. Esta Ã© uma mudanÃ§a Type 1 sobre `data-model.md` e deve ser ratificada lÃ¡ (ver `decisions-log.md`).
189:docs/architecture/implementation-blueprint.md:162:| `ReminderProducer` | EventBridge Scheduler, 1 minuto | Consultar GSI3 nos shards do minuto e enviar comandos para SQS |
190-docs/architecture/implementation-blueprint.md:163:| `ReminderDispatchWorker` | `ReminderDispatchQueue` | Revalidar ocorrÃªncia/item e produzir `NotificationIntent` |
191-docs/architecture/implementation-blueprint.md:164:| `ReminderReconciliationWorker` | EventBridge diÃ¡rio | Recalcular janela de sete dias e reparar ocorrÃªncias |
192-docs/architecture/implementation-blueprint.md:181:**Decidido (Â§23.1): Textract, parser determinÃ­stico e Bedrock sÃ£o Lambdas separadas desde o primeiro release**, nÃ£o um `ExtractionTaskHandler` Ãºnico consolidado depois. Justificativa: o sandbox de PDF (lacuna #2 do threat model, limites em Â§12.4/Â§23.1) exige isolamento de IAM/rede que uma funÃ§Ã£o combinada dilui â€” separar desde o dia 1 Ã© mais simples de auditar do que consolidar e depois desmembrar sob pressÃ£o de produÃ§Ã£o. Cada uma das trÃªs superfÃ­cies de risco isoladas (OCR gerenciado, parsing nÃ£o confiÃ¡vel em sandbox, chamada a LLM) tem sua prÃ³pria funÃ§Ã£o nomeada com `ScopedLambdaFunction` (Â§17.1) â€” nÃ£o Ã© "um estado, uma funÃ§Ã£o": estados de orquestraÃ§Ã£o pura (`NeedsBedrock?`) sÃ£o Choice states nativos sem Lambda, e estados que compartilham a mesma superfÃ­cie de IAM/dados (validaÃ§Ã£o de schema, comparaÃ§Ã£o entre extratores, persistÃªncia, fail-closed) compartilham `ExtractionValidationTaskHandler`. O mapeamento estadoâ†’handler completo estÃ¡ em Â§12.5.
193-docs/architecture/implementation-blueprint.md:201:export interface RequestContext {
--
229-infra/lib\expiration-tracker-stack.ts:46:    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
230-infra/lib\expiration-tracker-stack.ts:58:    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
231-infra/lib\expiration-tracker-stack.ts:59:    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
232-infra/lib\expiration-tracker-stack.ts:69:    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
233:infra/lib\expiration-tracker-stack.ts:75:    // ReminderDispatch (Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
234-infra/lib\expiration-tracker-stack.ts:77:    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
235-infra/lib\expiration-tracker-stack.ts:78:    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
236-infra/lib\expiration-tracker-stack.ts:92:    // ReminderReconciliation (Â§9.5): single job for claim-expiry revert + DST
237:infra/lib\expiration-tracker-stack.ts:93:    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
238-infra/lib\expiration-tracker-stack.ts:95:    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
239-infra/lib\cognito.ts:12:export type MfaPolicy = "OFF" | "OPTIONAL" | "REQUIRED";
240-infra/lib\cognito.ts:14:export interface ExpirationTrackerAuthProps {
241-infra/lib\cognito.ts:26:export class ExpirationTrackerAuth extends Construct {
--
250-infra/lib\dynamo-table.ts:85:   * Resource ARNs for every GSI EXCEPT GSI3 (data-model.md Â§3's isolation safeguard).
251-infra/lib\dynamo-table.ts:90:   * for tenant-facing functions would silently grant `dynamodb:Query` on GSI3 to every
252-infra/lib\dynamo-table.ts:93:   * PolicyStatements with an explicit resource list (base table + the 5 non-GSI3 indexes)
253-infra/lib\dynamo-table.ts:105:    return `${this.table.tableArn}/index/GSI3`;
254:infra/lib\dynamo-table.ts:108:  /** General read/write grant on the base table + GSI1/GSI2/GSI4/GSI5/GSI6 (tenant-scoped indexes). Never includes GSI3 - see tenantFacingResources() above. */
255-infra/lib\dynamo-table.ts:144:  /** The ONLY sanctioned way to read GSI3 - resource is the GSI3 index ARN exclusively (never the table or any other index), intended solely for the M3 ReminderProducer. test/infra/stack.test.ts's isolation test asserts no other function's synthesized IAM policy references this index. */
256-infra/lib\api.ts:3: * (GET /test/ping) wired through a Cognito JWT authorizer + the ScopedLambdaFunction
257-infra/lib\api.ts:11:import type { ScopedLambdaFunction } from "./scoped-lambda-function.js";
258-infra/lib\api.ts:14:export interface ExpirationTrackerApiProps {
--
420- * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
421- * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
422- * inject an implementation, so domain/application logic is unit-testable without live
423- * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
424: * data-model.md Ã‚Â§5's load-bearing sentence requires item update + outbox record (and,
425- * for M2, the AuditEvent) to commit in a single `TransactWriteItems` Ã¢â‚¬â€ the aggregate
426: * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
427- * be individually visible without the others.
428- */
429-import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
430-
--
666-      handler: "index.handler",
667-      access: [tableAccess.readWriteKeys("IdentityMapping", "User", "TenantQuota")],
668-    });
669-
670:    // M2: ExpirationItem/AuditEvent CRUD+renew+dashboard, plus outbox writes for
671-    // ItemDueDateChanged. GSI1 (dashboard) is table-level read/write per
672-    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
673-    // header comment) - never grantGsi3ReadTo, which stays reserved for M3's
674-    // ReminderProducer.
--
700-      handler: "index.handler",
701-      access: [tableAccess.readWriteKeys("ReminderOccurrence"), tableAccess.gsi3Read()],
702-    });
703-
704:    // ReminderDispatch (Ã‚Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
705:    // one TransactWriteItems - needs write access to occurrences/intents/outbox/idempotency
706-    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
707-    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
708-      runtime: lambda.Runtime.NODEJS_20_X,
709-      handler: "index.handler",
--
718-      ],
719-    });
720-
721-    // ReminderReconciliation (Ã‚Â§9.5): single job for claim-expiry revert + DST
722:    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
723-    // the scheduler index).
724-    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
725-      runtime: lambda.Runtime.NODEJS_20_X,
726-      handler: "index.handler",
--
891- * silently dropped, and repair is left to reconciliation (Ã‚Â§9.5) - never a new ad-hoc
892- * materialization from inside dispatch.
893- *
894- * On success, everything commits in ONE TransactWriteItems (Ã‚Â§9.4: "corrige a lacuna de
895: * durabilidade... antes o evento era tratado como reconstruÃƒÂ­vel sem outbox"):
896- * NotificationIntent Put + occurrence CLAIMED->TRIGGERED Update + idempotency record Put
897- * (`tenantId|occurrenceId`, Ã‚Â§9.4's exact key for NotificationIntentCreated consumers) +
898: * outbox `notification.intent-created.v1` Put.
899- */
900-import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
901:import { appendToTransaction } from "../../shared/outbox/outbox.js";
902-import { buildIdempotencyKey } from "../../shared/idempotency/idempotency.js";
903-import type { DomainEvent } from "../../shared/contracts/events.js";
904-import { itemKey } from "../../modules/expiration/domain/expiration-item.js";
905-import { policyKey, type ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
--
1127-  shardConfig: ShardConfig;
1128-}
1129-
1130-/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
1131: * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
1132- * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
1133- * wiring); tests/callers supply the batch directly. */
1134-export interface DstReconciliationCandidate {
1135-  tenantId: string;
--
1148-
1149-/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
1150-export async function reconcileExpiredClaims(
1151-  deps: ReconciliationDeps,
1152:  candidateOccurrences: ReminderOccurrence[],
1153-): Promise<number> {
1154-  let reverted = 0;
1155-  const now = deps.now();
1156:  for (const occurrence of candidateOccurrences) {
1157-    if (occurrence.status !== "CLAIMED") continue;
1158-    if (!occurrence.claimExpiresAt || occurrence.claimExpiresAt > now) continue;
1159-    try {
1160-      await deps.store.transactWrite([
--
1178-
1179-/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (Ã‚Â§9.5 steps 1-6). */
1180-export async function reconcileDst(
1181-  deps: ReconciliationDeps,
1182:  candidates: DstReconciliationCandidate[],
1183-): Promise<{ cancelled: number; created: number; divergences: number }> {
1184-  const materializer = new ReminderMaterializer(deps.store, deps.tableName, deps.now);
1185-  const windowStart = Date.parse(deps.now());
1186-  const windowEnd = windowStart + 7 * 24 * 60 * 60_000;
--
1188-  let cancelled = 0;
1189-  let created = 0;
1190-  let divergences = 0;
1191-
1192:  for (const candidate of candidates) {
1193:    if (!candidate.policy.enabled) continue;
1194-
1195:    const existing = await deps.store.queryByItem<ReminderOccurrence>(candidate.tenantId, candidate.itemId);
1196-    const liveExisting = existing.filter(
1197-      (o) =>
1198:        o.policyId === candidate.policy.policyId &&
1199:        o.itemVersion === candidate.itemVersion &&
1200:        o.policyVersion === candidate.policy.version &&
1201-        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
1202-    );
1203-
1204-    // Expected schedule per trigger, recomputed fresh from the IANA rule (never a frozen offset).
1205-    const expectedByTrigger = new Map<string, { scheduledAtUtc: string }>();
1206:    for (const trigger of candidate.policy.triggers) {
1207-      const schedule = materializer.computeSchedule({
1208:        itemDueDate: candidate.itemDueDate,
1209-        offsetIso: trigger.offsetIso,
1210-        localTime: trigger.localTime,
1211:        timeZone: candidate.policy.timeZone,
1212:        quietHours: candidate.policy.quietHours,
1213-      });
1214-      const t = Date.parse(schedule.scheduledAtUtc);
1215-      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
1216-      expectedByTrigger.set(trigger.triggerId, { scheduledAtUtc: schedule.scheduledAtUtc });
--
1229-          {
1230-            Update: buildVersionedUpdate({
1231-              tableName: deps.tableName,
1232-              key: { PK: occurrence.PK, SK: occurrence.SK },
1233:              tenantId: candidate.tenantId,
1234-              expectedVersion: occurrence.version,
1235-              set: { status: "CANCELLED" },
1236-            }),
1237-          },
--
1244-
1245-    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
1246-    // materialize() itself is idempotent (putIfAbsent keyed by the deterministic
1247-    // occurrenceId hash), so re-running it for triggers that already matched is a safe no-op.
1248:    const stillMissing = candidate.policy.triggers.some((t) => {
1249-      const expected = expectedByTrigger.get(t.triggerId);
1250-      if (!expected) return false;
1251-      return !liveExisting.some((o) => o.triggerId === t.triggerId && o.scheduledAt === expected.scheduledAtUtc);
1252-    });
1253-    if (stillMissing) {
1254-      const result = await materializer.materialize({
1255:        tenantId: candidate.tenantId,
1256:        itemId: candidate.itemId,
1257:        itemVersion: candidate.itemVersion,
1258:        itemDueDate: candidate.itemDueDate,
1259:        policy: candidate.policy,
1260-        shardConfig: deps.shardConfig,
1261-      });
1262-      created += result.created.length;
1263-    }
--
1415-        identity-store-dynamodb.ts
1416-        expiration-store-dynamodb.ts
1417-        reminder-store-dynamodb.ts
1418-        reminder-producer-store-dynamodb.ts
1419:        reconciliation-candidate-source-dynamodb.ts
1420-      sqs/
1421-        dispatch-command-publisher.ts
1422-
1423-    handlers/
--
1426-      reminders-handler.ts
1427-      reminder-producer-handler.ts
1428-      reminder-dispatch-handler.ts
1429-      reminder-reconciliation-handler.ts
1430:      dispatch-outbox-relay-handler.ts
1431-
1432-    composition/
1433-      identity.ts
1434-      expiration.ts
--
1486-  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
1487-}
1488-```
1489-
1490:O adapter consultaria o GSI6 ou o Ã­ndice efetivamente normativo. Isso Ã© integraÃ§Ã£o mÃ­nima necessÃ¡ria, nÃ£o redesenho do domÃ­nio.
1491-
1492:Se o GSI6 ainda nÃ£o representa claims expirados e polÃ­ticas ativas de maneira consultÃ¡vel, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo estÃ¡ apenas agendado, nÃ£o funcional.
1493-
1494-AlÃ©m da execuÃ§Ã£o diÃ¡ria de DST, recuperaÃ§Ã£o de claims precisa ter cadÃªncia curta. Recomendo:
1495-
1496-- reconciliaÃ§Ã£o de claims expirados: a cada minuto ou cinco minutos;
1497-- reconciliaÃ§Ã£o DST/materializaÃ§Ã£o: diÃ¡ria.
1498-
1499:Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durÃ¡vel, a reconciliaÃ§Ã£o curta continua Ãºtil para reparar estados excepcionais.
1500-
1501-## 5. Handlers Lambda
1502-
1503-Cada arquivo em `src/runtime/handlers` Ã© um composition root:
--
1614-- poison message/schema invÃ¡lido â†’ terminal, mas precisa chegar Ã  DLQ.
1615-
1616-Para poison message, hÃ¡ duas polÃ­ticas vÃ¡lidas. Eu recomendo retornar falha atÃ© `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observÃ¡vel. Para evitar custo inÃºtil, registrar a primeira detecÃ§Ã£o e emitir mÃ©trica por receive count. NÃ£o deletar manualmente nem enviar diretamente Ã  DLQ.
1617-
1618:O relay do outbox tambÃ©m deve usar partial batch failure no DynamoDB Stream. Um registro sÃ³ Ã© marcado publicado depois de `SendMessage` confirmado; duplicaÃ§Ã£o entre envio e marcaÃ§Ã£o Ã© esperada e absorvida pelo dispatch idempotente.
1619-
1620-## 8. EventBridge
1621-
1622:Escolho **EventBridge Rule legado com `ScheduleExpression`**, nÃ£o EventBridge Scheduler, para estas cadÃªncias fixas.
1623-
1624-Motivos:
1625-
1626-- jÃ¡ Ã© a tecnologia especificada no escopo;
--
1673-- dispatch: `10`.
1674-
1675-Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contÃ­nuas precisam de alarme por invocaÃ§Ãµes com erro e por scheduler lag.
1676-
1677:### Por que nÃ£o Scheduler
1678-
1679:EventBridge Scheduler seria preferÃ­vel se houvesse schedules individuais, timezone por schedule, execuÃ§Ã£o Ãºnica ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.
1680-
1681:Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliaÃ§Ã£o diÃ¡ria, uma janela flexÃ­vel seria aceitÃ¡vel, mas nÃ£o justifica misturar dois mecanismos.
1682-
1683-## 9. Testes
1684-
1685-### Camada 1 â€” testes rÃ¡pidos
--
1694-- teste estrutural que assegura que somente producer tem ARN de GSI3.
1695-
1696-### Camada 2 â€” integraÃ§Ã£o em containers
1697-
1698:Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade especÃ­fica.
1699-
1700:DynamoDB Local, sozinho, nÃ£o testa:
1701-
1702-- redrive SQS;
1703-- DLQ;
1704-- event source mapping;
1705-- EventBridge;
1706-- interaÃ§Ã£o DynamoDB â†’ relay â†’ SQS.
1707-
1708:LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suÃ­te, portas dinÃ¢micas e cleanup previsÃ­vel.
1709-
1710-Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.
1711-
1712-Casos obrigatÃ³rios:
1713-
1714-1. CondiÃ§Ã£o de OCC concorrente: apenas um writer vence.
1715:2. Claim + dispatch outbox sÃ£o atÃ´micos.
1716-3. Falha do relay preserva o registro para retry.
1717-4. Duas entregas SQS criam um Ãºnico `NotificationIntent`.
1718-5. Falha em um item do batch nÃ£o repete os itens bem-sucedidos.
1719-6. Poison message chega Ã  DLQ apÃ³s cinco receives.
--
1721-8. Renew/cancel concorrente faz dispatch cancelar por versÃ£o obsoleta.
1722-9. Query GSI3 encontra mÃºltiplos tenants, mas o producer nunca mistura o contexto reconstruÃ­do.
1723-10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.
1724-
1725:Para timeout real de dependÃªncia, usaria um proxy de rede controlÃ¡vel, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexÃ£o acima do timeout configurado e verifica:
1726-
1727-- erro classificado como retryable;
1728-- mensagem nÃ£o reconhecida como sucesso;
1729-- retry/redrive;
--
1732-Isso testa falha de transporte real, nÃ£o um `mockRejectedValue`.
1733-
1734-### Camada 3 â€” sandbox AWS efÃªmero
1735-
1736:LocalStack nÃ£o prova semÃ¢ntica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 nÃ£o deve ser aprovado somente com containers.
1737-
1738-Criaria uma suÃ­te curta contra uma stack AWS efÃªmera:
1739-
1740-- deploy em conta/regiÃ£o de teste;
--
1759-
1760-| Falha | InjeÃ§Ã£o | EvidÃªncia esperada |
1761-|---|---|---|
1762-| Timeout DynamoDB/SQS | Toxiproxy interrompe conexÃ£o | erro retryable, retry, sem estado parcial |
1763:| Queda apÃ³s claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
1764:| Queda apÃ³s `SendMessage` | falhar antes de marcar outbox | possÃ­vel duplicata, um Ãºnico efeito lÃ³gico |
1765-| Poison message | body invÃ¡lido real em SQS | cinco receives e mensagem na DLQ |
1766-| Falha parcial de batch | uma mensagem invÃ¡lida entre vÃ¡lidas | somente invÃ¡lida retorna em `batchItemFailures` |
1767-| Renew concorrente | atualizar versÃ£o apÃ³s claim | dispatch cancela stale, sem intent incorreto |
1768-| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
--
1788-## 12. O que este milestone nÃ£o resolve
1789-
1790-â€œG8 fechadoâ€ nÃ£o significa que os cinco P1 desapareceram integralmente.
1791-
1792:- **P1 #1 â€” recuperaÃ§Ã£o assÃ­ncrona:** fecha o trecho producer â†’ dispatch se houver outbox/relay e reconciliaÃ§Ã£o consultÃ¡vel. NÃ£o fecha pipelines futuros de Notification.
1793-- **P1 #2 â€” idempotÃªncia no efeito externo:** nÃ£o pode ser provada ainda. `NotificationIntent` Ã© apenas uma intenÃ§Ã£o durÃ¡vel; M4 deverÃ¡ provar idempotÃªncia no limite do provider de e-mail/WhatsApp, inclusive timeout apÃ³s o provider aceitar a solicitaÃ§Ã£o e antes de responder.
1794-- **P1 #3 â€” false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. NÃ£o desaparece atÃ© testes AWS reais de M4 e operaÃ§Ã£o continuada.
1795-- **P1 #4 â€” concorrÃªncia/estado obsoleto:** o milestone pode provar OCC e revalidaÃ§Ã£o entre renew/cancel/materializaÃ§Ã£o/dispatch. NÃ£o prova todas as corridas com envio externo, callbacks e correÃ§Ãµes futuras de M4.
1796-- **P1 #5 â€” blast radius do GSI3:** um teste IAM real prova que roles tenant-facing nÃ£o consultam o Ã­ndice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa funÃ§Ã£o ainda tÃªm blast radius global. SÃ£o necessÃ¡rios alarmes, logs sem PII, limites de batch e revisÃ£o especÃ­fica desse handler.
1797-
1798-TambÃ©m nÃ£o resolve carga extrema, restore real, regiÃ£o/LGPD, provider de notificaÃ§Ã£o, BFF de sessÃ£o nem aprovaÃ§Ã£o global da arquitetura operacional.
1799-
1800:## Nota e riscos
1801-
1802-**ConfianÃ§a: 8,8/10.**
1803-
1804-Principais riscos:
1805-
1806:1. O port atual da reconciliaÃ§Ã£o Ã© insuficiente para um handler autÃ´nomo; o GSI6 precisa ser concretamente consultÃ¡vel.
1807:2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
1808:3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS Ã© indispensÃ¡vel.
1809:4. O relay adiciona uma funÃ§Ã£o e semÃ¢ntica de publicaÃ§Ã£o que precisam de observabilidade e retenÃ§Ã£o/cleanup do outbox.
1810-5. A idempotÃªncia externa continuarÃ¡ nÃ£o comprovada atÃ© M4; qualquer declaraÃ§Ã£o mais ampla de encerramento dos cinco P1 seria enganosa.
1811-tokens used
1812-38.889
1813-# Proposta tÃ©cnica â€” milestone Runtime Real / G8
--
1910-        identity-store-dynamodb.ts
1911-        expiration-store-dynamodb.ts
1912-        reminder-store-dynamodb.ts
1913-        reminder-producer-store-dynamodb.ts
1914:        reconciliation-candidate-source-dynamodb.ts
1915-      sqs/
1916-        dispatch-command-publisher.ts
1917-
1918-    handlers/
--
1921-      reminders-handler.ts
1922-      reminder-producer-handler.ts
1923-      reminder-dispatch-handler.ts
1924-      reminder-reconciliation-handler.ts
1925:      dispatch-outbox-relay-handler.ts
1926-
1927-    composition/
1928-      identity.ts
1929-      expiration.ts
--
1981-  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
1982-}
1983-```
1984-
1985:O adapter consultaria o GSI6 ou o Ã­ndice efetivamente normativo. Isso Ã© integraÃ§Ã£o mÃ­nima necessÃ¡ria, nÃ£o redesenho do domÃ­nio.
1986-
1987:Se o GSI6 ainda nÃ£o representa claims expirados e polÃ­ticas ativas de maneira consultÃ¡vel, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo estÃ¡ apenas agendado, nÃ£o funcional.
1988-
1989-AlÃ©m da execuÃ§Ã£o diÃ¡ria de DST, recuperaÃ§Ã£o de claims precisa ter cadÃªncia curta. Recomendo:
1990-
1991-- reconciliaÃ§Ã£o de claims expirados: a cada minuto ou cinco minutos;
1992-- reconciliaÃ§Ã£o DST/materializaÃ§Ã£o: diÃ¡ria.
1993-
1994:Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durÃ¡vel, a reconciliaÃ§Ã£o curta continua Ãºtil para reparar estados excepcionais.
1995-
1996-## 5. Handlers Lambda
1997-
1998-Cada arquivo em `src/runtime/handlers` Ã© um composition root:
--
2109-- poison message/schema invÃ¡lido â†’ terminal, mas precisa chegar Ã  DLQ.
2110-
2111-Para poison message, hÃ¡ duas polÃ­ticas vÃ¡lidas. Eu recomendo retornar falha atÃ© `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observÃ¡vel. Para evitar custo inÃºtil, registrar a primeira detecÃ§Ã£o e emitir mÃ©trica por receive count. NÃ£o deletar manualmente nem enviar diretamente Ã  DLQ.
2112-
2113:O relay do outbox tambÃ©m deve usar partial batch failure no DynamoDB Stream. Um registro sÃ³ Ã© marcado publicado depois de `SendMessage` confirmado; duplicaÃ§Ã£o entre envio e marcaÃ§Ã£o Ã© esperada e absorvida pelo dispatch idempotente.
2114-
2115-## 8. EventBridge
2116-
2117:Escolho **EventBridge Rule legado com `ScheduleExpression`**, nÃ£o EventBridge Scheduler, para estas cadÃªncias fixas.
2118-
2119-Motivos:
2120-
2121-- jÃ¡ Ã© a tecnologia especificada no escopo;
--
2168-- dispatch: `10`.
2169-
2170-Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contÃ­nuas precisam de alarme por invocaÃ§Ãµes com erro e por scheduler lag.
2171-
2172:### Por que nÃ£o Scheduler
2173-
2174:EventBridge Scheduler seria preferÃ­vel se houvesse schedules individuais, timezone por schedule, execuÃ§Ã£o Ãºnica ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.
2175-
2176:Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliaÃ§Ã£o diÃ¡ria, uma janela flexÃ­vel seria aceitÃ¡vel, mas nÃ£o justifica misturar dois mecanismos.
2177-
2178-## 9. Testes
2179-
2180-### Camada 1 â€” testes rÃ¡pidos
--
2189-- teste estrutural que assegura que somente producer tem ARN de GSI3.
2190-
2191-### Camada 2 â€” integraÃ§Ã£o em containers
2192-
2193:Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade especÃ­fica.
2194-
2195:DynamoDB Local, sozinho, nÃ£o testa:
2196-
2197-- redrive SQS;
2198-- DLQ;
2199-- event source mapping;
2200-- EventBridge;
2201-- interaÃ§Ã£o DynamoDB â†’ relay â†’ SQS.
2202-
2203:LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suÃ­te, portas dinÃ¢micas e cleanup previsÃ­vel.
2204-
2205-Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.
2206-
2207-Casos obrigatÃ³rios:
2208-
2209-1. CondiÃ§Ã£o de OCC concorrente: apenas um writer vence.
2210:2. Claim + dispatch outbox sÃ£o atÃ´micos.
2211-3. Falha do relay preserva o registro para retry.
2212-4. Duas entregas SQS criam um Ãºnico `NotificationIntent`.
2213-5. Falha em um item do batch nÃ£o repete os itens bem-sucedidos.
2214-6. Poison message chega Ã  DLQ apÃ³s cinco receives.
--
2216-8. Renew/cancel concorrente faz dispatch cancelar por versÃ£o obsoleta.
2217-9. Query GSI3 encontra mÃºltiplos tenants, mas o producer nunca mistura o contexto reconstruÃ­do.
2218-10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.
2219-
2220:Para timeout real de dependÃªncia, usaria um proxy de rede controlÃ¡vel, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexÃ£o acima do timeout configurado e verifica:
2221-
2222-- erro classificado como retryable;
2223-- mensagem nÃ£o reconhecida como sucesso;
2224-- retry/redrive;
--
2227-Isso testa falha de transporte real, nÃ£o um `mockRejectedValue`.
2228-
2229-### Camada 3 â€” sandbox AWS efÃªmero
2230-
2231:LocalStack nÃ£o prova semÃ¢ntica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 nÃ£o deve ser aprovado somente com containers.
2232-
2233-Criaria uma suÃ­te curta contra uma stack AWS efÃªmera:
2234-
2235-- deploy em conta/regiÃ£o de teste;
--
2254-
2255-| Falha | InjeÃ§Ã£o | EvidÃªncia esperada |
2256-|---|---|---|
2257-| Timeout DynamoDB/SQS | Toxiproxy interrompe conexÃ£o | erro retryable, retry, sem estado parcial |
2258:| Queda apÃ³s claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
2259:| Queda apÃ³s `SendMessage` | falhar antes de marcar outbox | possÃ­vel duplicata, um Ãºnico efeito lÃ³gico |
2260-| Poison message | body invÃ¡lido real em SQS | cinco receives e mensagem na DLQ |
2261-| Falha parcial de batch | uma mensagem invÃ¡lida entre vÃ¡lidas | somente invÃ¡lida retorna em `batchItemFailures` |
2262-| Renew concorrente | atualizar versÃ£o apÃ³s claim | dispatch cancela stale, sem intent incorreto |
2263-| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
--
2283-## 12. O que este milestone nÃ£o resolve
2284-
2285-â€œG8 fechadoâ€ nÃ£o significa que os cinco P1 desapareceram integralmente.
2286-
2287:- **P1 #1 â€” recuperaÃ§Ã£o assÃ­ncrona:** fecha o trecho producer â†’ dispatch se houver outbox/relay e reconciliaÃ§Ã£o consultÃ¡vel. NÃ£o fecha pipelines futuros de Notification.
2288-- **P1 #2 â€” idempotÃªncia no efeito externo:** nÃ£o pode ser provada ainda. `NotificationIntent` Ã© apenas uma intenÃ§Ã£o durÃ¡vel; M4 deverÃ¡ provar idempotÃªncia no limite do provider de e-mail/WhatsApp, inclusive timeout apÃ³s o provider aceitar a solicitaÃ§Ã£o e antes de responder.
2289-- **P1 #3 â€” false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. NÃ£o desaparece atÃ© testes AWS reais de M4 e operaÃ§Ã£o continuada.
2290-- **P1 #4 â€” concorrÃªncia/estado obsoleto:** o milestone pode provar OCC e revalidaÃ§Ã£o entre renew/cancel/materializaÃ§Ã£o/dispatch. NÃ£o prova todas as corridas com envio externo, callbacks e correÃ§Ãµes futuras de M4.
2291-- **P1 #5 â€” blast radius do GSI3:** um teste IAM real prova que roles tenant-facing nÃ£o consultam o Ã­ndice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa funÃ§Ã£o ainda tÃªm blast radius global. SÃ£o necessÃ¡rios alarmes, logs sem PII, limites de batch e revisÃ£o especÃ­fica desse handler.
2292-
2293-TambÃ©m nÃ£o resolve carga extrema, restore real, regiÃ£o/LGPD, provider de notificaÃ§Ã£o, BFF de sessÃ£o nem aprovaÃ§Ã£o global da arquitetura operacional.
2294-
2295:## Nota e riscos
2296-
2297-**ConfianÃ§a: 8,8/10.**
2298-
2299-Principais riscos:
2300-
2301:1. O port atual da reconciliaÃ§Ã£o Ã© insuficiente para um handler autÃ´nomo; o GSI6 precisa ser concretamente consultÃ¡vel.
2302:2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
2303:3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS Ã© indispensÃ¡vel.
2304:4. O relay adiciona uma funÃ§Ã£o e semÃ¢ntica de publicaÃ§Ã£o que precisam de observabilidade e retenÃ§Ã£o/cleanup do outbox.
2305-5. A idempotÃªncia externa continuarÃ¡ nÃ£o comprovada atÃ© M4; qualquer declaraÃ§Ã£o mais ampla de encerramento dos cinco P1 seria enganosa.

codex
1. **DynamoDB Local vs. LocalStack/Testcontainers â€” concordo com a divisÃ£o em camadas.**  
   DynamoDB Local deve executar, em todo PR, a suÃ­te rÃ¡pida de paridade entre adapters reais e fakes: condiÃ§Ãµes, OCC, transaÃ§Ãµes e queries. LocalStack via Testcontainers fica restrito aos testes que exigem integraÃ§Ã£o entre Streams, Lambda, SQS, DLQ e EventBridge. O sandbox AWS efÃªmero permanece como gate para IAM, event source mappings e redrive gerenciado reais. Isso reduz custo de CI sem diminuir a evidÃªncia necessÃ¡ria para G8.

2. **EventBridge Scheduler vs. Rule legado â€” retiro minha preferÃªncia por Rule e concordo com Scheduler.**  
   Meu argumento tÃ©cnico pela Rule era defensÃ¡vel isoladamente, mas nÃ£o supera a precedÃªncia normativa: o blueprint aprovado especifica `EventBridge Scheduler` explicitamente. Reabrir essa decisÃ£o Type 1 sem necessidade nova seria incorreto. ConfiguraÃ§Ã£o convergente:

   - producer a cada minuto: `FlexibleTimeWindow: OFF`;
   - reconciliaÃ§Ã£o curta de claims expirados: `FlexibleTimeWindow: OFF`;
   - reconciliaÃ§Ã£o diÃ¡ria de DST/materializaÃ§Ã£o: Scheduler separado, podendo usar janela flexÃ­vel pequena;
   - mesmo handler pode receber `mode`, mantendo lÃ³gicas e mÃ©tricas separadas.

3. **Reuso do outbox existente â€” concordo, com uma precisÃ£o semÃ¢ntica.**  
   O claim e o `OutboxEvent` devem ser gravados no mesmo `TransactWriteItems`, usando `shared/outbox/outbox.ts` e o mesmo modelo de persistÃªncia. NÃ£o deve surgir tabela ou mecanismo de outbox paralelo.

   Eu usaria `eventType: "ReminderDispatchRequested"` em vez de `"ReminderClaimed"`: o registro durÃ¡vel representa o comando que precisa chegar Ã  fila, enquanto o claim Ã© apenas a transiÃ§Ã£o que o originou. O evento precisa ainda de roteamento inequÃ­voco â€” por `eventType` ou campo versionado de destino â€” para que somente o relay de dispatch o envie Ã  `ReminderDispatchQueue`, sem publicaÃ§Ã£o acidental pelo publisher genÃ©rico no EventBridge. O relay usa partial batch failure; duplicaÃ§Ã£o entre `SendMessage` e confirmaÃ§Ã£o do outbox continua esperada e Ã© absorvida pela idempotÃªncia do dispatch.

Os dois gaps continuam corretos apÃ³s ler a proposta original da Claude:

- **Durabilidade claim â†’ fila:** a proposta original fazia `TransactWrite(claim)` seguido de `SendMessage`, deixando uma janela real de perda. Outbox transacional + Streams/relay + sweeper fecha essa janela para o trecho producerâ†’dispatch.
- **Fonte de candidatos da reconciliaÃ§Ã£o:** o worker atual nÃ£o se torna real apenas por receber um schedule. Ã‰ necessÃ¡rio incluir no M3.5 o GSI6 consultÃ¡vel e um port estreito, separado de `ReminderStore`, como `ReminderReconciliationCandidateSource`. Ele deve expor explicitamente candidatos para claims expirados e para reconciliaÃ§Ã£o DST/materializaÃ§Ã£o, com namespaces de GSI6 que nÃ£o colidam com `RECON#OUTBOX#PENDING`.

A reconciliaÃ§Ã£o curta continua necessÃ¡ria mesmo com outbox: ela recupera leases/claims excepcionais; o sweeper do outbox recupera publicaÃ§Ã£o pendente. SÃ£o responsabilidades complementares.

**Nota de confianÃ§a na proposta convergente: 9,3/10.**

O risco residual principal Ã© de implementaÃ§Ã£o: definir sem ambiguidade os namespaces/projeÃ§Ãµes do GSI6 e o roteamento do `OutboxEvent`, evitando que publisher genÃ©rico e relay SQS disputem ou dupliquem o mesmo registro. Isso deve virar contrato e teste de integraÃ§Ã£o explÃ­citos. A nota tambÃ©m pressupÃµe sandbox AWS efÃªmero como gate; LocalStack sozinho nÃ£o fecha G8.
tokens used
40.219
1. **DynamoDB Local vs. LocalStack/Testcontainers â€” concordo com a divisÃ£o em camadas.**  
   DynamoDB Local deve executar, em todo PR, a suÃ­te rÃ¡pida de paridade entre adapters reais e fakes: condiÃ§Ãµes, OCC, transaÃ§Ãµes e queries. LocalStack via Testcontainers fica restrito aos testes que exigem integraÃ§Ã£o entre Streams, Lambda, SQS, DLQ e EventBridge. O sandbox AWS efÃªmero permanece como gate para IAM, event source mappings e redrive gerenciado reais. Isso reduz custo de CI sem diminuir a evidÃªncia necessÃ¡ria para G8.

2. **EventBridge Scheduler vs. Rule legado â€” retiro minha preferÃªncia por Rule e concordo com Scheduler.**  
   Meu argumento tÃ©cnico pela Rule era defensÃ¡vel isoladamente, mas nÃ£o supera a precedÃªncia normativa: o blueprint aprovado especifica `EventBridge Scheduler` explicitamente. Reabrir essa decisÃ£o Type 1 sem necessidade nova seria incorreto. ConfiguraÃ§Ã£o convergente:

   - producer a cada minuto: `FlexibleTimeWindow: OFF`;
   - reconciliaÃ§Ã£o curta de claims expirados: `FlexibleTimeWindow: OFF`;
   - reconciliaÃ§Ã£o diÃ¡ria de DST/materializaÃ§Ã£o: Scheduler separado, podendo usar janela flexÃ­vel pequena;
   - mesmo handler pode receber `mode`, mantendo lÃ³gicas e mÃ©tricas separadas.

3. **Reuso do outbox existente â€” concordo, com uma precisÃ£o semÃ¢ntica.**  
   O claim e o `OutboxEvent` devem ser gravados no mesmo `TransactWriteItems`, usando `shared/outbox/outbox.ts` e o mesmo modelo de persistÃªncia. NÃ£o deve surgir tabela ou mecanismo de outbox paralelo.

   Eu usaria `eventType: "ReminderDispatchRequested"` em vez de `"ReminderClaimed"`: o registro durÃ¡vel representa o comando que precisa chegar Ã  fila, enquanto o claim Ã© apenas a transiÃ§Ã£o que o originou. O evento precisa ainda de roteamento inequÃ­voco â€” por `eventType` ou campo versionado de destino â€” para que somente o relay de dispatch o envie Ã  `ReminderDispatchQueue`, sem publicaÃ§Ã£o acidental pelo publisher genÃ©rico no EventBridge. O relay usa partial batch failure; duplicaÃ§Ã£o entre `SendMessage` e confirmaÃ§Ã£o do outbox continua esperada e Ã© absorvida pela idempotÃªncia do dispatch.

Os dois gaps continuam corretos apÃ³s ler a proposta original da Claude:

- **Durabilidade claim â†’ fila:** a proposta original fazia `TransactWrite(claim)` seguido de `SendMessage`, deixando uma janela real de perda. Outbox transacional + Streams/relay + sweeper fecha essa janela para o trecho producerâ†’dispatch.
- **Fonte de candidatos da reconciliaÃ§Ã£o:** o worker atual nÃ£o se torna real apenas por receber um schedule. Ã‰ necessÃ¡rio incluir no M3.5 o GSI6 consultÃ¡vel e um port estreito, separado de `ReminderStore`, como `ReminderReconciliationCandidateSource`. Ele deve expor explicitamente candidatos para claims expirados e para reconciliaÃ§Ã£o DST/materializaÃ§Ã£o, com namespaces de GSI6 que nÃ£o colidam com `RECON#OUTBOX#PENDING`.

A reconciliaÃ§Ã£o curta continua necessÃ¡ria mesmo com outbox: ela recupera leases/claims excepcionais; o sweeper do outbox recupera publicaÃ§Ã£o pendente. SÃ£o responsabilidades complementares.

**Nota de confianÃ§a na proposta convergente: 9,3/10.**

O risco residual principal Ã© de implementaÃ§Ã£o: definir sem ambiguidade os namespaces/projeÃ§Ãµes do GSI6 e o roteamento do `OutboxEvent`, evitando que publisher genÃ©rico e relay SQS disputem ou dupliquem o mesmo registro. Isso deve virar contrato e teste de integraÃ§Ã£o explÃ­citos. A nota tambÃ©m pressupÃµe sandbox AWS efÃªmero como gate; LocalStack sozinho nÃ£o fecha G8.

# M3.5 â€” Runtime real do Reminder Engine (fechamento de G8) â€” Design convergido

Status: **APPROVED** (Claude 9.0/10, Codex 9.3/10, ambos â‰¥9.0 sem arredondar â€” protocolo `AGENTS.md` Â§4, 2 rodadas: propostas independentes Rodada 1 + crÃ­tica cruzada/trÃ©plica Rodada 2). Artefatos completos em `docs/architecture/reviews/m3.5-runtime-design/`.

Escopo fixado por `NEXT_SESSION_PROMPT.md`/`ENGINEERING.md` ("DecisÃ£o pendente: escopo de G8"), input do red team formal em `docs/engineering/reviews/checkpoint-12-redteam/summary.md`.

## DecisÃ£o central: outbox durÃ¡vel entre claim e publicaÃ§Ã£o

`TransactWrite(SCHEDULEDâ†’CLAIMED)` seguido de `SendMessage` direto no handler do producer **nÃ£o Ã© atÃ´mico** â€” se a Lambda cai entre os dois passos, a ocorrÃªncia fica `CLAIMED` sem mensagem em fila, e isso NÃƒO Ã© coberto pela janela de lookback do prÃ³prio producer (que consulta `M` outra chave a cada tick) nem necessariamente pela reconciliaÃ§Ã£o diÃ¡ria a tempo. Fila+DLQ sozinhas nÃ£o fecham essa lacuna (achado que nenhuma das duas propostas independentes tinha isoladamente â€” sÃ³ apareceu na crÃ­tica cruzada).

**DecisÃ£o**: o claim grava, na MESMA `TransactWriteItems`, um `OutboxEvent` (reusando `shared/outbox/outbox.ts` â€” nenhum mecanismo de outbox paralelo), `eventType: "ReminderDispatchRequested"`. Um `DispatchOutboxRelay` Lambda (gatilho: DynamoDB Streams) publica esse evento na `ReminderDispatchQueue` via SQS e marca o outbox como publicado; falha de publicaÃ§Ã£o (nÃ£o de persistÃªncia) Ã© recuperada por um sweeper periÃ³dico do prÃ³prio outbox, nunca reprocessada como se o claim nÃ£o tivesse acontecido. O roteamento precisa ser inequÃ­voco por `eventType`/campo versionado de destino, para que o relay de dispatch nunca dispute ou duplique o mesmo registro com o `OutboxPublisher` genÃ©rico jÃ¡ existente (M2/M3) â€” isso Ã© contrato explÃ­cito a testar, nÃ£o just "outro consumidor do outbox".

## ReconciliaÃ§Ã£o: precisa de fonte real de candidatos, entra no escopo

`runReconciliation` (M3) recebe candidatos como parÃ¢metro injetado â€” rodar a Lambda via EventBridge sem uma query real produzindo esses candidatos seria false-green (exatamente o P1 #3 do red team). **DecisÃ£o**: novo port estreito `ReminderReconciliationCandidateSource` (separado de `ReminderStore`, mesmo princÃ­pio de isolamento que levou a `ReminderProducerStore` a ser separado â€” nÃ£o dar a rotas tenant-facing acesso a uma query que elas nÃ£o precisam), implementado contra **GSI6 consultÃ¡vel de verdade**. Isso muda o escopo original: o "wiring real do GSI6" (item 4 da lista de pendÃªncias pÃ³s-M3) deixa de ser follow-up separado e entra neste milestone â€” sem ele, a EventBridge Rule/Scheduler de reconciliaÃ§Ã£o (item 4 do escopo de G8) dispara sobre nada.

CadÃªncia: reconciliaÃ§Ã£o de **claims expirados** roda em minutos (curta â€” recupera leases/claims excepcionais), separada da reconciliaÃ§Ã£o **DST/materializaÃ§Ã£o diÃ¡ria** (mesmo handler, campo `mode` no evento, concurrency reservada 1 preservada); sÃ£o responsabilidades complementares ao sweeper do outbox (que recupera publicaÃ§Ã£o pendente), nÃ£o redundantes.

## Componentes do milestone

1. **Adapters DynamoDB reais** (`@aws-sdk/client-dynamodb` + `@aws-sdk/lib-dynamodb`, `DynamoDBDocumentClient`, sem conversÃµes numÃ©ricas silenciosas): um por port â€” `DynamoDbIdentityStore`, `DynamoDbExpirationStore`, `DynamoDbReminderStore`, `DynamoDbReminderProducerStore`, `DynamoDbReminderReconciliationCandidateSource`. Nenhuma classe universal implementando todos os ports (evita injeÃ§Ã£o acidental de acesso a GSI3/GSI6 em cÃ³digo tenant-facing). `get`/`putIfAbsent`/`update`/`transactWrite`/`queryByItem`/`queryGsi1`/`queryGsi3` traduzem 1:1 para os builders jÃ¡ existentes em `shared/dynamodb/occ.ts` â€” os adapters sÃ£o traduÃ§Ã£o fina, nÃ£o reimplementam OCC/idempotÃªncia. Erros do SDK mapeados para `shared/errors/app-error.ts` (`ConditionalCheckFailedException`â†’`false`, `TransactionCanceledException`â†’`isTransactionCanceled`, throttling/timeoutâ†’retryable, validation/`AccessDeniedException`â†’terminal) sÃ³ neste adapter â€” o resto do sistema continua SDK-agnÃ³stico.
2. **Handlers Lambda reais**: bundling via `esbuild` (target `node20`, bundle independente por funÃ§Ã£o, SDK v3 incluÃ­do no bundle â€” nÃ£o confiar na versÃ£o fornecida pela runtime), estendendo `ScopedLambdaFunction` com `entry`/`handlerExport` (Ãºnico ponto de criaÃ§Ã£o de Lambda continua sendo esse construct). Cada handler Ã© composition root fino (env vars â†’ adapters â†’ lÃ³gica de domÃ­nio jÃ¡ testada â†’ traduÃ§Ã£o de protocolo â†’ `SecureLogger`), nenhuma lÃ³gica de negÃ³cio no wrapper. SQS: `reportBatchItemFailures: true`. Teste de infra novo: falha se qualquer funÃ§Ã£o continuar com `fromInline`/corpo `501`.
3. **SQS + DLQ**: `ReminderDispatchQueue` Standard (idempotÃªncia jÃ¡ Ã© necessÃ¡ria independente de ordenaÃ§Ã£o; FIFO nÃ£o eliminaria essa necessidade) + DLQ, `maxReceiveCount=5` (jÃ¡ documentado em `implementation-blueprint.md` Â§26), retenÃ§Ã£o 4d/14d, visibility timeout â‰¥6Ã— timeout da Lambda, batch size 10, `reportBatchItemFailures`. Poison message: deixar esgotar `maxReceiveCount` (redrive nativo observÃ¡vel), nÃ£o deletar/rotear manualmente.
4. **EventBridge Scheduler** (nÃ£o Rule legado â€” decisÃ£o jÃ¡ fixada em `implementation-blueprint.md` linha 162, Type 1 jÃ¡ aprovada, nÃ£o reaberta aqui sem necessidade tÃ©cnica nova): `ReminderProducerSchedule` (1 min, `FlexibleTimeWindow: OFF`), `ReminderClaimReconciliationSchedule` (1-5 min, `FlexibleTimeWindow: OFF`), `ReminderDstReconciliationSchedule` (diÃ¡rio, janela flexÃ­vel pequena aceitÃ¡vel). Reserved concurrency preservada do blueprint (producer 2, dispatch 10, reconciliation 1).
5. **Testes em 3 camadas**:
   - Camada 1 (todo PR, rÃ¡pida): unit/contract/CDK assertions + teste de bundle + proibiÃ§Ã£o de `fromInline` em produÃ§Ã£o + isolamento estrutural GSI3/GSI6.
   - Camada 2 (containers): **DynamoDB Local** para a suÃ­te de paridade adapter-real-vs-fake-em-memÃ³ria (rÃ¡pida, determinÃ­stica, Ã© o padrÃ£o que M0-M3 jÃ¡ usam); **LocalStack via Testcontainers** sÃ³ para o que precisa de Streams+SQS+DLQ+EventBridge de verdade â€” mais pesado, job separado. Toxiproxy para injeÃ§Ã£o real de timeout/latÃªncia de rede (nÃ£o `mockRejectedValue`).
   - Camada 3 (sandbox AWS efÃªmero, gate obrigatÃ³rio â€” LocalStack nÃ£o prova IAM real, event source mapping real nem redrive gerenciado real): deploy em conta/regiÃ£o de teste com TTL e cleanup, teste negativo de IAM real (role tenant-facing tentando `Query` em GSI3 â†’ `AccessDenied`), poison message atÃ© DLQ real, redrive real, invocaÃ§Ã£o manual do schedule. EvidÃªncia (CloudWatch request IDs, ARNs, resultados) preservada como prova do gate. CadÃªncia: containers em todo PR; sandbox no merge para `develop` ou workflow manual do milestone.

## Casos obrigatÃ³rios de fault injection

OCC concorrente (sÃ³ um writer vence), claim+outbox atÃ´micos, falha do relay preserva registro para retry (sweeper), duas entregas SQS criam um Ãºnico `NotificationIntent`, falha de item nÃ£o repete itens bem-sucedidos do batch, poison message chega Ã  DLQ apÃ³s 5 receives, claim expirado Ã© recuperado pela reconciliaÃ§Ã£o curta, renew/cancel concorrente faz dispatch cancelar por versÃ£o obsoleta, GSI3 encontrado por mÃºltiplos tenants sem misturar contexto, role tenant-facing recebe `AccessDenied` em GSI3/GSI6.

## O que este milestone explicitamente NÃƒO fecha

Registrar para nÃ£o ser mal interpretado como "G8 fechado = todos os 5 P1 resolvidos":
- **P1 #2 (idempotÃªncia no limite do efeito externo)**: `NotificationIntent` continua sendo sÃ³ uma intenÃ§Ã£o durÃ¡vel â€” idempotÃªncia contra um provedor externo real (e-mail/WhatsApp, timeout depois do provider aceitar e antes de responder) Ã© problema de M4 (Notification), que ainda nÃ£o existe.
- **P1 #3 (false-green)**: reduzido â€” adapters, handlers, filas e schedules passam a ser cobertos por teste real â€” mas nÃ£o desaparece atÃ© M4 rodar sob as mesmas condiÃ§Ãµes e atÃ© haver operaÃ§Ã£o continuada real.
- **P1 #4 (concorrÃªncia/estado obsoleto)**: provado entre renew/cancel/materializaÃ§Ã£o/dispatch/reconciliation. NÃ£o prova corridas envolvendo envio externo/callbacks de M4.
- **P1 #5 (blast radius do GSI3)**: teste IAM real prova que roles tenant-facing nÃ£o alcanÃ§am o Ã­ndice; o `ReminderProducer` continua sendo componente privilegiado por desenho â€” blast radius de um bug/comprometimento nele continua global. Alarmes, logs sem PII e revisÃ£o dedicada desse handler continuam necessÃ¡rios, fora do escopo mecÃ¢nico deste milestone.
- Carga extrema, restore real, regiÃ£o/LGPD, provider de notificaÃ§Ã£o, BFF de sessÃ£o, aprovaÃ§Ã£o global de `ARCHITECTURE STATUS` (rubrica B) â€” nenhum desses Ã© tocado aqui.

## MudanÃ§a de escopo a comunicar ao usuÃ¡rio

O escopo original (`NEXT_SESSION_PROMPT.md`) nÃ£o mencionava GSI6 explicitamente como parte deste milestone â€” a convergÃªncia de design mostrou que Ã© estruturalmente necessÃ¡rio para o item 4 (reconciliaÃ§Ã£o via EventBridge) nÃ£o ser teatro. Registrado aqui como decisÃ£o tÃ©cnica, nÃ£o escondido.


2026-08-19T18:07:27.983443Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new(); Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt; Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round2.txt; Get-Content -Raw -Encoding UTF8 docs/engineering/reviews/checkpoint-12-redteam/summary.md; Get-Content -Raw -Encoding UTF8 docs/architecture/m3.5-runtime-design.md'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new(); Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt; Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round2.txt; Get-Content -Raw -Encoding UTF8 docs/engineering/reviews/checkpoint-12-redteam/summary.md; Get-Content -Raw -Encoding UTF8 docs/architecture/m3.5-runtime-design.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new(); Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt; Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round2.txt; Get-Content -Raw -Encoding UTF8 docs/engineering/reviews/checkpoint-12-redteam/summary.md; Get-Content -Raw -Encoding UTF8 docs/architecture/m3.5-runtime-design.md'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw -Encoding UTF8 docs/architecture/reviews/m3.5-runtime-design/codex-output-round1.txt' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 338ms:
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a01b2d-0c11-74d1-b775-7005fd779b52
--------
user
Você é o segundo engenheiro independente num protocolo de nota cega Claude<->Codex (AGENTS.md secao 4 do repo expiration-tracker, c:/Users/Usuario/Desktop/projects/expiration-tracker). NAO leia claude-proposal.md neste diretorio - sua proposta precisa ser independente, sera comparada depois.

Contexto: micro-SaaS serverless AWS (TypeScript, CDK, DynamoDB, SQS, EventBridge). M0-M3 ja implementados e testados (123 testes) mas SEM runtime real: adapters DynamoDB sao fakes em memoria, handlers Lambda sao placeholders `exports.handler = async () => ({statusCode: 501})`, sem filas/EventBridge wired. Um red team formal (checkpoint-12) apontou isso como bloqueador de aprovacao de arquitetura (G8), com 5 P1: (1) pipeline assincrono sem recuperacao real, (2) idempotencia nao provada no limite do efeito externo (at-least-once classico), (3) false-green entre logica pura e comportamento implantado, (4) concorrencia/estado obsoleto entre renew/cancel/materializacao/dispatch/reconciliation, (5) blast radius cross-tenant do GSI3 (IAM ja limita, falta teste negativo contra adapter real).

O usuario decidiu tratar o fechamento de G8 como um milestone dedicado (mesma disciplina de M0-M3: design -> implementacao -> teste real -> revisao Claude+Codex), com este escopo fixo:
1. Adapters DynamoDB reais implementando os ports existentes (ReminderStore, ExpirationStore, IdentityStore) contra AWS real.
2. Handlers Lambda reais substituindo os placeholders 501.
3. Filas SQS + DLQ com redrive policy para o pipeline producer->dispatch.
4. EventBridge Scheduled Rule disparando o ReminderProducer periodicamente (a cada minuto) e a ReminderReconciliation periodicamente.
5. Testes de fault injection contra esse runtime real (timeout de dependencia, poison message, redrive).

Arquitetura relevante ja existente e fixa (nao redesenhar, so integrar):
- Ports SDK-agnosticos ja definidos: ReminderStore (get/putIfAbsent/update/transactWrite/queryByItem), ReminderProducerStore (queryGsi3/get/transactWrite, isolado do resto por desenho), ExpirationStore, IdentityStore - todos em src/modules/*/ports/.
- shared/dynamodb/occ.ts ja produz os parametros de ConditionExpression/TransactWriteItems (builders puros, sem SDK ainda).
- shared/outbox/outbox.ts, shared/idempotency/idempotency.ts, shared/errors/app-error.ts (taxonomia retryable/terminal) ja existem.
- infra/lib/scoped-lambda-function.ts: todo Lambda deve nascer via ScopedLambdaFunction (unico ponto de enforcement de IAM least-privilege), hoje usa lambda.Code.fromInline para os placeholders.
- infra/lib/expiration-tracker-stack.ts ja declara os 5 ScopedLambdaFunction do Reminder Engine (RemindersHandler HTTP, ReminderProducer com gsi3Read exclusivo, ReminderDispatch, ReminderReconciliation) com as capabilities de tabela corretas - falta so trocar o codigo placeholder por bundle real e adicionar fila/schedule.
- GSI3 e deliberadamente isolado: so ReminderProducerStore/ReminderProducer pode fazer Query nele (protecao contra P1 #5), enforcement via IAM real (Table.grantReadWriteData do CDK inclui TODOS os indices por padrao - ja corrigido com PolicyStatement explicito).
- Reserved concurrency e DLQ ja documentados no blueprint (implementation-blueprint.md secao 26): ReminderDispatchWorker concurrency 10, DLQ maxReceiveCount=5; ReminderProducer concurrency 2, sem DLQ (lookback [M-5min,M] cobre falha de tick); ReminderReconciliation concurrency 1, sem DLQ (idempotente, diario).

Produza uma proposta tecnica concreta cobrindo os 5 itens do escopo: escolhas de biblioteca/pacote AWS SDK v3, estrutura de arquivos dos adapters DynamoDB (um por port, mantendo os ports SDK-agnosticos como estao), estrategia de bundling dos handlers Lambda reais, desenho da fila+DLQ (nomes, redrive policy, batch size, partial batch failure), desenho do EventBridge Schedule (Scheduler vs Rule legado, flexible time window), e como voce testaria fault injection de forma real (nao mockada) sem tornar o CI lento/fragil demais - inclua sua escolha entre DynamoDB Local, LocalStack, ou testcontainers, e por que. Aponte explicitamente o que este milestone NAO resolve dos 5 P1 do red team (idempotencia ponta-a-ponta contra efeito externo real depende de Notification/M4, que nao existe ainda) para evitar falsa sensacao de "G8 fechado = tudo resolvido". Termine com uma nota de confianca 0-10 na sua propria proposta e os riscos que mais te preocupam.

Responda em portugues, formato markdown, direto ao ponto (nao precisa de preambulo).

codex
Vou inspecionar apenas as fontes normativas e o código existente necessários para ancorar a proposta; não abrirei `claude-proposal.md`. Como a tarefa é uma proposta independente, não farei alterações no repositório.
2026-08-19T17:59:02.703433Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'NEXT_SESSION_PROMPT.md','docs/architecture/README.md','docs/architecture/implementation-blueprint.md','src/modules/reminder/ports/reminder-store.ts','src/modules/expiration/ports/expiration-store.ts','src/modules/identity/ports/identity-store.ts','infra/lib/scoped-lambda-function.ts','infra/lib/expiration-tracker-stack.ts','package.json'); foreach ("'$f in $files) { if (Test-Path -LiteralPath $f) { Write-Output "FILE: $f"; Get-Content -LiteralPath $f } }; Write-Output '"'REMINDER FILES'; rg --files src/modules/reminder src/workers infra/lib | Where-Object { "'$_ -notmatch '"'claude-proposal' }; Write-Output 'SECTION 26'; rg -n -A 140 -B 10 '"'^## 26|''^### 26'"' docs/architecture/implementation-blueprint.md"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -LiteralPath NEXT_SESSION_PROMPT.md; Get-Content -LiteralPath docs/architecture/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 287ms:
# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o

## PrÃ³xima aÃ§Ã£o obrigatÃ³ria (2026-08-19, mais recente â€” leia isto primeiro)

Engineering Maturity Review concluÃ­da (checkpoints 0, 1, 2-9, 12; ver `ENGINEERING.md` na raiz para o relatÃ³rio completo). Veredito: `ENGINEERING FOUNDATION STATUS: NOT APPROVED`, bloqueador Ãºnico e conhecido: **G8 (recuperaÃ§Ã£o real de falha assÃ­ncrona)**.

**DecisÃ£o do usuÃ¡rio (2026-08-19)**: tratar o fechamento pleno de G8 como **novo milestone dedicado** (mesma disciplina de M0-M3: pesquisa/design â†’ implementaÃ§Ã£o â†’ teste real â†’ revisÃ£o Claude+Codex), nÃ£o como remediaÃ§Ã£o de sessÃ£o. Escopo desse milestone, per `ENGINEERING.md` (seÃ§Ã£o "DecisÃ£o pendente: escopo de G8"):

- Adapters DynamoDB reais implementando os ports (`ReminderStore`, `ExpirationStore`, `IdentityStore`) contra AWS real â€” hoje sÃ³ existem fakes em memÃ³ria para teste.
- Handlers Lambda reais substituindo os placeholders `exports.handler = async () => ({statusCode: 501})` em `infra/lib/expiration-tracker-stack.ts`.
- Filas SQS + DLQ com redrive policy para o pipeline producerâ†’dispatch.
- EventBridge Scheduled Rule disparando o `ReminderProducer` periodicamente (a cada minuto) e a `ReminderReconciliation` periodicamente.
- Testes de fault injection contra esse runtime real (timeout de dependÃªncia, poison message, redrive).

Antes de comeÃ§ar esse milestone: ler `docs/engineering/reviews/checkpoint-12-redteam/summary.md` (red team formal jÃ¡ identificou 5 P1 relacionados: pipeline sem recuperaÃ§Ã£o, idempotÃªncia nÃ£o provada no limite do efeito externo, false-green CI, concorrÃªncia/estado obsoleto entre renew/cancel/materializaÃ§Ã£o/dispatch/reconciliation, blast radius cross-tenant do GSI3 via workers privilegiados) â€” usar como input de design, nÃ£o redescobrir do zero.

Trabalho jÃ¡ feito nesta sessÃ£o de engenharia (nÃ£o repetir): CI real corrigido e confirmado verde em 5 execuÃ§Ãµes; branch `develop` estabelecida como padrÃ£o de trabalho (`AGENTS.md` Â§3), `main` protegida; `dependency-cruiser` como enforcement real de boundary (achou e corrigiu 2 violaÃ§Ãµes genuÃ­nas de arquitetura); testes de `reconciliation.ts`/`producer.ts` que nÃ£o existiam; tentativa de fechar a vulnerabilidade de devDependency (EX-001) via upgrade do Vitest foi revertida por quebrar CI real (bug upstream do npm, nÃ£o repetir sem verificar correÃ§Ã£o).

---

**CorreÃ§Ã£o de 2026-08-19 (Engineering Maturity Review, Checkpoint 2-9)**: as seÃ§Ãµes abaixo (escritas ao longo das sessÃµes de M0-M3) afirmam "nada foi commitado" e citam contagens de teste por marco que, somadas, nÃ£o batem com a realidade medida agora. Estado real verificado por execuÃ§Ã£o: M0-M3 **jÃ¡ estÃ¡ commitado** num Ãºnico commit (`154d6e0`), e `npm test` roda **123 testes** no total (19 arquivos), nÃ£o a soma das contagens individuais citadas abaixo. As seÃ§Ãµes nÃ£o foram reescritas (preservadas como histÃ³rico de sessÃ£o), mas nÃ£o devem ser lidas como estado de commit/contagem de teste vigente â€” confiar em `git log`/`npm test` reais, nÃ£o nesses nÃºmeros. Ver `docs/engineering/03-repository-baseline.md` e `docs/engineering/reviews/checkpoint-02-09-consolidated/` para a anÃ¡lise completa desta divergÃªncia.

Projeto: micro-SaaS de controle de vencimentos/renovaÃ§Ãµes. Pasta: `c:\Users\Usuario\Desktop\projects\expiration-tracker\`. Repo GitHub: `marcelo-jgoncalves/expiration-tracker` (privado).

Mapa completo de documentaÃ§Ã£o, status vigente e regra de precedÃªncia: `docs/architecture/README.md`. Regras de processo e ferramentas: `AGENTS.md`. Log cronolÃ³gico de sessÃµes: `docs/architecture/session-log.md`.

## Status atual

```text
DESIGN MATURITY STATUS: APPROVED (arquitetura conceitual + Implementation Blueprint)
ARCHITECTURE STATUS: NOT APPROVED
```

Todo o processo de design (Fases 0-3 do prompt mestre + os 14 entregÃ¡veis das seÃ§Ãµes 35-52 + threat model seÃ§Ã£o 33 + Implementation Blueprint seÃ§Ã£o 60) estÃ¡ completo e aprovado â€” ver `ARCHITECTURE.md` na raiz para o documento consolidado. `ARCHITECTURE STATUS: NOT APPROVED` Ã© o estado normativo correto atÃ© haver implementaÃ§Ã£o real testada sob falha/carga (rubrica B, `requirements.md` Â§13.1) â€” **nÃ£o Ã© reprovaÃ§Ã£o de mÃ©rito, e nÃ£o muda com a conclusÃ£o do blueprint**: o blueprint Ã© design detalhado, nÃ£o evidÃªncia operacional.

## ConcluÃ­do nesta sessÃ£o â€” Implementation Blueprint (seÃ§Ã£o 60) â€” APPROVED

`docs/architecture/implementation-blueprint.md`. Componentes/mÃ³dulos (Identity, Expiration, Reminder, Notification, Document, Audit + workers assÃ­ncronos), interfaces concretas, eventos/schemas reais (grounded em `data-model.md`), ordem de deploy, milestones M0-M7, critÃ©rios de aceite tÃ©cnicos por componente, e as 7 lacunas do `threat-model.md` incorporadas como requisito desde o inÃ­cio (nÃ£o apÃªndice). Processo: 2 propostas independentes (Claude/Codex) â†’ crÃ­tica cruzada (17 problemas reais encontrados, incluindo um erro tÃ©cnico presente nas duas propostas â€” chave do GSI3 do scheduler nÃ£o era consultÃ¡vel) â†’ convergÃªncia â†’ 5 rodadas de nota cega com correÃ§Ã£o pontual a cada achado (cabeÃ§alho prematuro, contradiÃ§Ã£o de kill switch, tabela incompleta, decisÃµes Type 1 nÃ£o propagadas Ã s seÃ§Ãµes operativas, mapeamento de estado da Step Functions incompleto). **Nota final: Claude 9.20 / Codex 9.2 (exato)** â€” ambos â‰¥9.0 sem arredondar, 8 rodadas totais, nenhum gate violado. DecisÃ£o Type 1 nova registrada: chave global do GSI3 (`GSI3PK=DUE#yyyyMMddHHmm#NN`, exceÃ§Ã£o documentada Ã  regra de toda chave comeÃ§ar por `TENANT#tenantId`) â€” a decisÃ£o estÃ¡ fechada no blueprint; falta sÃ³ o registro mecÃ¢nico em `data-model.md` (nÃ£o bloqueia implementaÃ§Ã£o).

## ConcluÃ­do nesta sessÃ£o â€” M0 "Guardrails e contratos" (implementation-blueprint.md Â§19)

Todas as entregas de M0 implementadas e testadas (53 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): estrutura TypeScript (`src/shared/*`, alinhada a `implementation-blueprint.md` Â§2), schemas JSON (`schemas/{events,queues,api}` + `sensitive-fields.json`, validados via Ajv em `src/shared/contracts/schema-validator.ts`), `SecureLogger`+`Redactor` central (`src/shared/observability/`, corpus de teste com valores canÃ¡rio provando que nada vaza), configuraÃ§Ã£o tipada fail-fast (`src/shared/config/config.ts`), taxonomia de erro normalizada retryable/terminal (`src/shared/errors/app-error.ts`), idempotÃªncia via `PutItem attribute_not_exists(PK)` (`src/shared/idempotency/idempotency.ts`, chaves por operaÃ§Ã£o conforme `data-model.md` Â§4), OCC com `ConditionExpression: version=:expected` (`src/shared/dynamodb/occ.ts`, `data-model.md` Â§5), outbox transacional (`src/shared/outbox/outbox.ts`, shape de `implementation-blueprint.md` Â§5.3), pipeline supply-chain (`.github/workflows/ci.yml`: npm ci imutÃ¡vel, actions pinadas por SHA, SBOM CycloneDX, audit, gate de schema). `AGENTS.md` Â§6 atualizado com comandos/convenÃ§Ãµes. Nada foi commitado (working tree aberto para revisÃ£o).

Judgment calls (blueprint estava silente): Ajv+ajv-formats para validaÃ§Ã£o de JSON Schema; Vitest como test runner; ESLint `no-console` (com exceÃ§Ã£o para `src/shared/observability/**`) como o mecanismo que faz "chamada direta a `console.*` falhar no lint" (`implementation-blueprint.md` Â§14.1); idempotÃªncia/OCC/outbox construÃ­dos como builders puros de parÃ¢metros DynamoDB (sem `@aws-sdk` ainda) para ficarem testÃ¡veis sem tabelas reais, que sÃ³ existem a partir de M1; SLSA/assinatura de artefato adiada para M1+ (nÃ£o hÃ¡ alvo de deploy ainda); SHAs de actions no CI pinados no momento da escrita â€” revisar antes do primeiro run real.

## ConcluÃ­do nesta sessÃ£o â€” M1 "Foundation, Identity e isolamento" (implementation-blueprint.md Â§19)

Todas as entregas implementadas e testadas (89 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/identity/{domain,application,ports,persistence,http}` (resolver central, matriz de autorizaÃ§Ã£o como cÃ³digo, `IdentityMapping`/`User`/`DeviceSession`/`TenantQuota` via portas SDK-agnÃ³sticas) e `infra/{lib,bin}` (`ExpirationTrackerTable`, `ExpirationTrackerAuth`, `ScopedLambdaFunction`, `ExpirationTrackerApi` com a rota `GET /test/ping`). SuÃ­te cross-tenant negativa em `test/integration/cross-tenant.test.ts` (9 casos, exit criterion do marco) e synth de infra em `test/infra/stack.test.ts` (6 casos, via `aws-cdk-lib/assertions`, sem AWS CLI). Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente ou pendÃªncia externa jÃ¡ conhecida): (1) MFA implementado como prop configurÃ¡vel (`OFF`/`OPTIONAL`/`REQUIRED`, default `OPTIONAL`) pois UNK-006 segue pendente de pesquisa externa â€” nÃ£o bloqueava M1; (2) CSP/CloudFront Response Headers Policy **nÃ£o implementado nesta sessÃ£o** â€” nÃ£o hÃ¡ distribuiÃ§Ã£o CloudFront/frontend ainda no repositÃ³rio, e o texto do blueprint (Â§4.2) coloca CSP no contexto do SPA estÃ¡tico servido por CloudFront; tratado como pertencente ao milestone que introduzir o frontend, nÃ£o forÃ§ado em M1 (revisar se essa leitura estiver errada); (3) sessÃ£o/revogaÃ§Ã£o modelada como campo `globalLogoutAfter` no item `User` + item filho `DeviceSession` (`TENANT#t#USER#u`/`SESSION#<deviceId>`) em vez de uma entidade `Session` de primeira classe â€” `data-model.md` nÃ£o define uma; (4) `ScopedLambdaFunction` mantÃ©m a lista de entidades por capability como metadado/documentaÃ§Ã£o, mas o IAM real concedido Ã© table-level (`grantReadWriteData`) â€” DynamoDB IAM nÃ£o expressa restriÃ§Ã£o por SK/entidade da forma que a sintaxe do blueprint sugere visualmente; per-entity IAM mais fino fica como follow-up; (5) `TenantQuota` implementado como janela fixa (fixed-window), nÃ£o sliding-window/leaky-bucket â€” mais simples e ainda satisfaz "decremento atÃ´mico, sem race condition" do `data-model.md`; (6) BFF de sessÃ£o (`/session/refresh`, `/session/logout`) **nÃ£o implementado como rota HTTP nesta sessÃ£o** â€” o Cognito client jÃ¡ estÃ¡ configurado para o padrÃ£o (client secret, tokens de vida curta), mas os endpoints do BFF em si ficam para quando a Expiration/Notification API skeleton crescer alÃ©m da rota de teste; revisar se isso deveria ter sido parte do exit criterion. `cdk synth` real via CLI nÃ£o foi executado (o pacote `aws-cdk` CLI nÃ£o estÃ¡ instalado, sÃ³ a lib `aws-cdk-lib`); a sintetizaÃ§Ã£o foi verificada programaticamente via `Template.fromStack` nos testes, que Ã© equivalente para fins de validaÃ§Ã£o estÃ¡tica.

## ConcluÃ­do nesta sessÃ£o â€” M2 "Expiration core e Audit" (implementation-blueprint.md Â§19)

Todas as entregas implementadas e testadas (134 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/expiration/{domain,application,ports,http}` â€” CRUD/renew (`ExpirationService`: createItem/getItem/updateItem/archiveItem/deleteItem/renewItem/listDashboard), OCC via `shared/dynamodb/occ.ts`, `ItemDueDateChanged` por outbox (`shared/outbox/outbox.ts`) na MESMA `TransactWriteItems` do item quando `dueDate` muda (updateItem e renewItem), `AuditEvent` append-only gravado em toda mutaÃ§Ã£o na mesma transaÃ§Ã£o, dashboard via GSI1. `renewItem` cria um item sucessor (`renewedFromId`) em vez de mutar `dueDate` no agregado de origem, que transiciona para `RENEWED`; Ã© idempotente via `IdempotencyStore` de M0. Rotas HTTP CDK adicionadas em `infra/lib/api.ts`/`expiration-tracker-stack.ts` (`ItemsHandler`, mesmo authorizer JWT de M1). SuÃ­te de integraÃ§Ã£o `test/integration/expiration-lifecycle.test.ts` prova o exit criterion do marco end-to-end via handlers HTTP reais. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) evento `ItemDueDateChanged` segue exatamente o schema jÃ¡ existente desde M0 (`schemas/events/item-due-date-changed.v1.json`: `itemId`/`previousDueDate`/`newDueDate`/`itemVersion`, `additionalProperties: false`) em vez do exemplo mais rico do texto do blueprint Â§8.3 (que inclui `timeZone`/`reminderPolicyId`/`changeReason`) â€” o schema jÃ¡ testado/versionado Ã© a fonte de verdade de contrato per `AGENTS.md` Â§6, o exemplo em prosa nÃ£o; campos adicionais ficam para quando M3 (Reminder) precisar deles, exigindo `.v2` aditivo; (2) `renewItem` tambÃ©m dispara `ItemDueDateChanged` (para o item novo, `previousDueDate: null`) â€” o blueprint sÃ³ descreve esse evento no contexto de `updateItem`, mas renovaÃ§Ã£o tambÃ©m Ã© uma "mudanÃ§a de vencimento" do ponto de vista do futuro `ReminderProducer` (M3), e a alternativa (renovaÃ§Ã£o silenciosa) deixaria o dashboard/scheduler sem sinal de que um novo item precisa de agendamento; (3) `archiveItem` usa a aÃ§Ã£o de autorizaÃ§Ã£o `item:update` (nÃ£o uma aÃ§Ã£o dedicada â€” a matriz de M1 nÃ£o define uma para archive) enquanto `deleteItem` usa `item:delete` (ADMIN_ROLES), seguindo a granularidade que jÃ¡ existe; (4) idempotÃªncia de renovaÃ§Ã£o aceita uma `idempotencyKey` explÃ­cita do chamador (header `Idempotency-Key`) com fallback determinÃ­stico `itemId|expectedVersion|cycle` quando ausente, jÃ¡ que o blueprint define a CHAVE de dedupe mas nÃ£o de onde ela vem por HTTP; (5) `ExpirationStore.transactWrite` foi adicionado ao port (nÃ£o existe em `IdentityStore`) porque nenhuma escrita de M1 precisava de `TransactWriteItems` multi-item â€” a fake em memÃ³ria (`test/unit/expiration/in-memory-store.ts`) sÃ³ entende as duas formas exatas de `ConditionExpression` que este cÃ³digo produz (mesma limitaÃ§Ã£o documentada, mesmo espÃ­rito de `InMemoryIdentityStore`).

## ConcluÃ­do nesta sessÃ£o â€” M3 "Reminder Engine" (implementation-blueprint.md Â§19)

Todas as entregas implementadas e testadas (123 testes, `npm test`/`typecheck`/`lint`/`validate-schemas` verdes): `src/modules/reminder/{domain,application,ports,http}` (policies CRUD, `ReminderMaterializer` com conversÃ£o localâ†’UTC IANA-aware â€” DST tratado explicitamente, horÃ¡rio inexistente e ambÃ­guo â€” reagindo a `ItemDueDateChanged` via `cancelStaleOccurrences`) e `src/workers/{reminder-producer,reminder-dispatch,reminder-reconciliation}` (lÃ³gica pura testÃ¡vel com relÃ³gio falso). GSI3 com shard versionado (`shardFnVersion`) e producer com janela de lookback `[M-5min, M]`; dispatch faz `CLAIMEDâ†’TRIGGERED`+`NotificationIntent`+outbox numa Ãºnica transaÃ§Ã£o; reconciliaÃ§Ã£o Ã© um Ãºnico job (claim-expiry + DST). Exit criterion provado end-to-end em `test/integration/reminder-engine.test.ts`. **Bug real de infraestrutura corrigido**: `Table.grantReadWriteData`/`grantReadData` do CDK sempre incluÃ­am `<tableArn>/index/*`, vazando `dynamodb:Query` no GSI3 para QUALQUER funÃ§Ã£o da tabela (inclusive as tenant-facing de M1/M2) â€” a salvaguarda de isolamento documentada desde M1 nunca foi de fato aplicada. Corrigido em `infra/lib/dynamo-table.ts` com `PolicyStatement`s explÃ­citos; teste de isolamento novo em `test/infra/stack.test.ts` prova via CDK synth que sÃ³ `ReminderProducer` referencia `/index/GSI3`. Detalhe completo em `docs/architecture/session-log.md`.

Judgment calls (blueprint silente): (1) `occurrenceId` derivado deterministicamente (hash da chave de idempotÃªncia de data-model.md Â§4) em vez de UUID + registro `IdempotencyStore` separado â€” o prÃ³prio `putIfAbsent` condicional jÃ¡ garante a idempotÃªncia de materializaÃ§Ã£o, mesmo padrÃ£o que data-model.md documenta para `WebhookInbox`; (2) cancelamento de ocorrÃªncia stale nÃ£o remove `GSI3PK`/`GSI3SK` (o builder de `occ.ts` Ã© SET-only, sem REMOVE) â€” deixa um ponteiro Ã³rfÃ£o no Ã­ndice, mas o `Query` condicional `SCHEDULEDâ†’CLAIMED` do producer falha sobre ele de forma inofensiva; (3) tolerÃ¢ncia de dispatch (`toleranceMs`, default 30min) e TTL de claim (default 2min) nÃ£o estÃ£o fixados no blueprint â€” valores razoÃ¡veis documentados no cÃ³digo, revisar contra dados reais de produÃ§Ã£o; (4) reconciliaÃ§Ã£o DST/claim-expiry recebe os candidatos como parÃ¢metro (batch) em vez de fazer sua prÃ³pria varredura via GSI6 â€” a wiring desse Ã­ndice de "polÃ­ticas ativas"/"claims expirados" real fica como follow-up de infra, o mecanismo de reconciliaÃ§Ã£o em si (o que a tarefa pediu) estÃ¡ implementado e testado; (5) Lambda handlers reais (bundling com `@aws-sdk`) nÃ£o foram escritos â€” mesmo estÃ¡gio que M0-M2, CDK usa cÃ³digo inline placeholder via `ScopedLambdaFunction`; a lÃ³gica testÃ¡vel Ã© o entregÃ¡vel real desta fase, igual Ã s anteriores.

## PrÃ³xima aÃ§Ã£o obrigatÃ³ria (histÃ³rico â€” superada pela seÃ§Ã£o do topo)

**Superada em 2026-08-19**: a Engineering Maturity Review identificou G8 (recuperaÃ§Ã£o real de falha assÃ­ncrona) como bloqueador de engenharia â€” o milestone de runtime real (adapters/handlers/filas/EventBridge, ver seÃ§Ã£o do topo) Ã© prÃ©-requisito antes de M4 fazer sentido operacionalmente, ainda que M4 nÃ£o dependa dele estruturalmente. Lista original preservada abaixo como histÃ³rico, nÃ£o como prÃ³xima aÃ§Ã£o vigente.

1. **M4 â€” Notification Engine** (`implementation-blueprint.md` Â§19, depende de M3 âœ…): router de `NotificationIntent`â†’canal, delivery workers (email/WhatsApp stub-first), `NotificationAttempt`, resoluÃ§Ã£o de destinatÃ¡rio/template.
2. Decidir e fechar os itens abaixo (BFF de sessÃ£o como rotas reais, CSP/CloudFront quando o frontend existir) antes de considerar a lacuna de "session theft" do threat model totalmente fechada â€” ainda nÃ£o resolvido, re-flagueado a cada sessÃ£o desde M1 para nÃ£o se perder: M1 fechou o mecanismo de revogaÃ§Ã£o/matriz de autorizaÃ§Ã£o, nÃ£o o endpoint BFF completo; M2/M3 nÃ£o tinham escopo de frontend/sessÃ£o (confirmado no blueprint) entÃ£o tambÃ©m nÃ£o o resolveram.
3. **Ratificar formalmente em `data-model.md` a exceÃ§Ã£o do GSI3** (chave global do scheduler, jÃ¡ decidida e justificada em `implementation-blueprint.md` Â§9.2/Â§23.3 item 10) â€” passo de manutenÃ§Ã£o documental, nÃ£o nova rodada de debate. M3 jÃ¡ corrigiu o enforcement real do isolamento em IAM; falta sÃ³ o registro mecÃ¢nico no documento.
4. Wiring real do GSI6 (ou Ã­ndice equivalente) para alimentar a reconciliaÃ§Ã£o de M3 com o batch real de "polÃ­ticas ativas"/"claims a verificar" em produÃ§Ã£o â€” hoje o mecanismo de reconciliaÃ§Ã£o recebe esse batch como parÃ¢metro injetado (testÃ¡vel), a query real fica pendente.
5. Testes de carga real (cenÃ¡rios de drenagem do pico extremo, `capacity-model.md`, com SLO de drenagem UNK-CAP-006 a formalizar em `slo.md` antes do gate de M7), teste de restore real (gate jÃ¡ definido em `disaster-recovery.md` Â§6), exercÃ­cio do runbook de credencial comprometida.
6. ReavaliaÃ§Ã£o sob rubrica (B) â€” Operational Evidence â€” sÃ³ entÃ£o `ARCHITECTURE STATUS` pode legitimamente virar `APPROVED`. **NÃ£o declarar isso antes de haver evidÃªncia operacional real.** Implementar M0/M1/M2/M3 nÃ£o Ã© evidÃªncia operacional.
7. DecisÃµes ainda pendentes de pesquisa externa (nÃ£o bloqueiam M4 estruturalmente, mas bloqueiam habilitar os canais/features especÃ­ficos, ver `implementation-blueprint.md` Â§23.3): provider inicial de e-mail, BSP WhatsApp (pricing real, UNK-003), modelo Bedrock especÃ­fico, regiÃ£o AWS (bloqueante para LGPD/transferÃªncia internacional), MFA obrigatÃ³rio vs. opcional (UNK-006), ferramenta de backup S3 (RPOâ‰¤24h), ferramenta de assinatura/provenance do pipeline de CI.
# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-19
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | 26 decisÃµes (D-000 a D-025), nota Claude/Codex, status |
| `adr/` | decisÃ£o/ADR | 8 ADRs formais para decisÃµes Type 1 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/status-projeto.html` | resumo/Ã­ndice (visual) | Painel visual do status do projeto |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^export interface|''^export type|''^export class|ReminderProducerStore|501|ScopedLambdaFunction|reservedConcurrent|GSI3|ReminderDispatch|ReminderReconciliation" src/modules src/workers infra/lib docs/architecture/implementation-blueprint.md package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 256ms:
docs/architecture/implementation-blueprint.md:11:1. **Scheduler GSI3 corrigido** — erro técnico real presente nas duas propostas independentes: uma partition key `TENANT#t#DUE#yyyyMMddHHmm#NN` não é consultável pelo producer do minuto sem já saber quais tenants têm ocorrência naquele minuto (`Query` exige PK exata). Corrigido em todo o documento para `GSI3PK=DUE#yyyyMMddHHmm#NN` / `GSI3SK=TENANT#tenantId#OCCURRENCE#occurrenceId` — exceção documentada e justificada à regra "toda chave começa com `TENANT#tenantId`" de `data-model.md` §3, com as salvaguardas da §9.2 abaixo. Esta é uma mudança Type 1 sobre `data-model.md` e deve ser ratificada lá (ver `decisions-log.md`).
docs/architecture/implementation-blueprint.md:162:| `ReminderProducer` | EventBridge Scheduler, 1 minuto | Consultar GSI3 nos shards do minuto e enviar comandos para SQS |
docs/architecture/implementation-blueprint.md:163:| `ReminderDispatchWorker` | `ReminderDispatchQueue` | Revalidar ocorrência/item e produzir `NotificationIntent` |
docs/architecture/implementation-blueprint.md:164:| `ReminderReconciliationWorker` | EventBridge diário | Recalcular janela de sete dias e reparar ocorrências |
docs/architecture/implementation-blueprint.md:181:**Decidido (§23.1): Textract, parser determinístico e Bedrock são Lambdas separadas desde o primeiro release**, não um `ExtractionTaskHandler` único consolidado depois. Justificativa: o sandbox de PDF (lacuna #2 do threat model, limites em §12.4/§23.1) exige isolamento de IAM/rede que uma função combinada dilui — separar desde o dia 1 é mais simples de auditar do que consolidar e depois desmembrar sob pressão de produção. Cada uma das três superfícies de risco isoladas (OCR gerenciado, parsing não confiável em sandbox, chamada a LLM) tem sua própria função nomeada com `ScopedLambdaFunction` (§17.1) — não é "um estado, uma função": estados de orquestração pura (`NeedsBedrock?`) são Choice states nativos sem Lambda, e estados que compartilham a mesma superfície de IAM/dados (validação de schema, comparação entre extratores, persistência, fail-closed) compartilham `ExtractionValidationTaskHandler`. O mapeamento estado→handler completo está em §12.5.
docs/architecture/implementation-blueprint.md:201:export interface RequestContext {
docs/architecture/implementation-blueprint.md:623:  "GSI3PK": "DUE#202609101200#02",
docs/architecture/implementation-blueprint.md:624:  "GSI3SK": "TENANT#t_01#OCCURRENCE#occ_01"
docs/architecture/implementation-blueprint.md:628:**Correção de convergência (Rodada 3)**: a chave original avaliada em ambas as propostas independentes (`GSI3PK=TENANT#t#DUE#...`) não é consultável pelo producer do minuto — `Query` no DynamoDB exige a partition key exata, e o producer não sabe a priori quais tenants têm ocorrência em `M`. Corrigido para uma **chave global** (`GSI3PK=DUE#yyyyMMddHHmm#NN`, sem `tenantId`), com `tenantId` preservado no `GSI3SK` e no item base para reconstrução segura do contexto. Esta é uma **exceção explícita e documentada** à regra "toda chave começa com `TENANT#tenantId`" de `data-model.md` §3 — Type 1, deve ser ratificada em `data-model.md`/`decisions-log.md`, não apenas aqui. Salvaguardas obrigatórias:
docs/architecture/implementation-blueprint.md:629:- IAM do índice restrito exclusivamente ao `ReminderProducer` — nenhuma rota tenant-facing tem permissão de `Query` no GSI3;
docs/architecture/implementation-blueprint.md:647:2. consulta GSI3 pelos `N` shards globais de `M` **e de uma janela de lookback** (ex. `[M-5min, M]`) para cobrir minutos cuja publicação falhou — sem lookback, "o próximo tick reprocessa" é falso, pois o próximo tick consulta uma chave diferente (`M+1`), deixando `M` definitivamente para trás em caso de falha;
docs/architecture/implementation-blueprint.md:1084:| # | Estado | Lambda (`ScopedLambdaFunction` próprio) |
docs/architecture/implementation-blueprint.md:1379:### 17.1 `ScopedLambdaFunction`
docs/architecture/implementation-blueprint.md:1384:new ScopedLambdaFunction(this, "ReminderDispatch", {
docs/architecture/implementation-blueprint.md:1529:- `ScopedLambdaFunction`;
docs/architecture/implementation-blueprint.md:1557:- GSI3;
docs/architecture/implementation-blueprint.md:1783:2. **Lookup `cognitoSub → userId`**: **decidido — item de mapeamento dedicado** `PK=IDENTITY#cognitoSub#<sub>` / `SK=MAP`, criado atomicamente (`ConditionExpression attribute_not_exists`) no primeiro login, apontando para `userId`/`tenantId` internos. Fora do agregado `TENANT#...` porque a busca ocorre *antes* de `tenantId` ser conhecido — única exceção adicional de particionamento além do GSI3 (§9.2), pelo mesmo motivo estrutural (chave não pode depender de um dado ainda não resolvido).
docs/architecture/implementation-blueprint.md:1797:10. **Ratificação formal em `data-model.md`/`decisions-log.md` da chave global do GSI3**: a *decisão* já está fechada aqui (§9.2, com justificativa completa e salvaguardas); o que resta é o registro mecânico no documento normativo — não uma lacuna de design, um passo de manutenção documental (AGENTS.md §5) a executar no fechamento desta sessão.
docs/architecture/implementation-blueprint.md:1824:| GSI3 global (scheduler) + shards versionados | NFR-002, NFR-011 | Pico extremo 16.667–278 agendamentos/s (`capacity-model.md`), SLO de drenagem UNK-CAP-006 (`slo.md`) |
docs/architecture/implementation-blueprint.md:1843:| `ReminderDispatchWorker` | `ReminderDispatchQueue` (comando do producer) | Reminder/Notification | 10 | DLQ, `maxReceiveCount=5` |
docs/architecture/implementation-blueprint.md:1844:| `ReminderReconciliationWorker` | EventBridge diário | Reminder | 1 | sem DLQ (idempotente, roda diariamente) |
infra/lib\scoped-lambda-function.ts:2: * ScopedLambdaFunction — implementation-blueprint.md §17.1. "O construct gera grants
infra/lib\scoped-lambda-function.ts:23:export interface AccessCapability {
infra/lib\scoped-lambda-function.ts:52:    /** GSI3 (scheduler) read-only - reserved EXCLUSIVELY for ReminderProducer
infra/lib\scoped-lambda-function.ts:59:        description: "GSI3 (scheduler) read-only - ReminderProducer only",
infra/lib\scoped-lambda-function.ts:113:export interface ScopedLambdaFunctionProps extends Omit<lambda.FunctionProps, "code"> {
infra/lib\scoped-lambda-function.ts:120: * Every Lambda in this system MUST be constructed via ScopedLambdaFunction, never
infra/lib\scoped-lambda-function.ts:124:export class ScopedLambdaFunction extends Construct {
infra/lib\scoped-lambda-function.ts:127:  constructor(scope: Construct, id: string, props: ScopedLambdaFunctionProps) {
infra/lib\scoped-lambda-function.ts:134:          `ScopedLambdaFunction(${id}): unknown access capability kind "${capability.kind}". ` +
infra/lib\scoped-lambda-function.ts:147:      code: code ?? lambda.Code.fromInline("exports.handler = async () => ({ statusCode: 501 });"),
infra/lib\expiration-tracker-stack.ts:2: * M1 stack: wires DynamoDB, Cognito, the identity test route (ScopedLambdaFunction) and
infra/lib\expiration-tracker-stack.ts:11:import { ScopedLambdaFunction, tableAccessFor } from "./scoped-lambda-function.js";
infra/lib\expiration-tracker-stack.ts:14:export interface ExpirationTrackerStackProps extends StackProps {
infra/lib\expiration-tracker-stack.ts:18:export class ExpirationTrackerStack extends Stack {
infra/lib\expiration-tracker-stack.ts:22:  readonly remindersHandler: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:23:  readonly reminderProducer: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:24:  readonly reminderDispatch: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:25:  readonly reminderReconciliation: ScopedLambdaFunction;
infra/lib\expiration-tracker-stack.ts:35:    const testRouteHandler = new ScopedLambdaFunction(this, "TestPingHandler", {
infra/lib\expiration-tracker-stack.ts:43:    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
infra/lib\expiration-tracker-stack.ts:46:    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
infra/lib\expiration-tracker-stack.ts:58:    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
infra/lib\expiration-tracker-stack.ts:59:    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
infra/lib\expiration-tracker-stack.ts:69:    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
infra/lib\expiration-tracker-stack.ts:75:    // ReminderDispatch (§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
infra/lib\expiration-tracker-stack.ts:77:    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
infra/lib\expiration-tracker-stack.ts:78:    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
infra/lib\expiration-tracker-stack.ts:92:    // ReminderReconciliation (§9.5): single job for claim-expiry revert + DST
infra/lib\expiration-tracker-stack.ts:93:    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
infra/lib\expiration-tracker-stack.ts:95:    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
infra/lib\cognito.ts:12:export type MfaPolicy = "OFF" | "OPTIONAL" | "REQUIRED";
infra/lib\cognito.ts:14:export interface ExpirationTrackerAuthProps {
infra/lib\cognito.ts:26:export class ExpirationTrackerAuth extends Construct {
infra/lib\dynamo-table.ts:6: * GSI3 is the documented exception: its PK (GSI3PK) is deliberately NOT prefixed with
infra/lib\dynamo-table.ts:9: * `grantGsi3ReadTo` method instead of a general grantReadData-on-GSI3 path, so callers
infra/lib\dynamo-table.ts:18:export interface ExpirationTrackerTableProps {
infra/lib\dynamo-table.ts:23:export class ExpirationTrackerTable extends Construct {
infra/lib\dynamo-table.ts:53:    // GSI3 - scheduler. Global PK (no tenantId), IAM-restricted to ReminderProducer only
infra/lib\dynamo-table.ts:56:      indexName: "GSI3",
infra/lib\dynamo-table.ts:57:      partitionKey: { name: "GSI3PK", type: dynamodb.AttributeType.STRING },
infra/lib\dynamo-table.ts:58:      sortKey: { name: "GSI3SK", type: dynamodb.AttributeType.STRING },
infra/lib\dynamo-table.ts:85:   * Resource ARNs for every GSI EXCEPT GSI3 (data-model.md §3's isolation safeguard).
infra/lib\dynamo-table.ts:90:   * for tenant-facing functions would silently grant `dynamodb:Query` on GSI3 to every
infra/lib\dynamo-table.ts:93:   * PolicyStatements with an explicit resource list (base table + the 5 non-GSI3 indexes)
infra/lib\dynamo-table.ts:105:    return `${this.table.tableArn}/index/GSI3`;
infra/lib\dynamo-table.ts:108:  /** General read/write grant on the base table + GSI1/GSI2/GSI4/GSI5/GSI6 (tenant-scoped indexes). Never includes GSI3 - see tenantFacingResources() above. */
infra/lib\dynamo-table.ts:144:  /** The ONLY sanctioned way to read GSI3 - resource is the GSI3 index ARN exclusively (never the table or any other index), intended solely for the M3 ReminderProducer. test/infra/stack.test.ts's isolation test asserts no other function's synthesized IAM policy references this index. */
infra/lib\api.ts:3: * (GET /test/ping) wired through a Cognito JWT authorizer + the ScopedLambdaFunction
infra/lib\api.ts:11:import type { ScopedLambdaFunction } from "./scoped-lambda-function.js";
infra/lib\api.ts:14:export interface ExpirationTrackerApiProps {
infra/lib\api.ts:16:  testRouteHandler: ScopedLambdaFunction;
infra/lib\api.ts:22:  itemsHandler?: ScopedLambdaFunction;
infra/lib\api.ts:25:export class ExpirationTrackerApi extends Construct {
src/workers\reminder-dispatch\dispatch.ts:32:export interface DispatchDeps {
src/workers\reminder-dispatch\dispatch.ts:43:export type DispatchOutcome =
src/workers\reminder-reconciliation\reconciliation.ts:28:export interface ReconciliationDeps {
src/workers\reminder-reconciliation\reconciliation.ts:39:export interface DstReconciliationCandidate {
src/workers\reminder-reconciliation\reconciliation.ts:47:export interface ReconciliationResult {
src/workers\reminder-producer\producer.ts:3: * injected `ReminderProducerStore` and clock, no AWS SDK/Lambda runtime dependency, so it
src/workers\reminder-producer\producer.ts:4: * is directly unit/integration-testable with a fake clock and fake GSI3 index (per the
src/workers\reminder-producer\producer.ts:9: *  2. query GSI3 across N shards of minute M AND a lookback window [M-lookback, M] (default
src/workers\reminder-producer\producer.ts:23:import type { ReminderProducerStore } from "../../modules/reminder/ports/reminder-store.js";
src/workers\reminder-producer\producer.ts:26:export interface DispatchCommand {
src/workers\reminder-producer\producer.ts:40:export interface ProducerDeps {
src/workers\reminder-producer\producer.ts:41:  store: ReminderProducerStore;
src/workers\reminder-producer\producer.ts:51:export interface ProducerTickResult {
src/workers\reminder-producer\producer.ts:89:        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
src/workers\reminder-producer\producer.ts:92:          const { tenantId, occurrenceId } = parseGsi3Sk(row.GSI3SK);
src/modules\expiration\ports\expiration-store.ts:15:export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
src/modules\expiration\ports\expiration-store.ts:18:export interface Gsi1QueryInput {
src/modules\expiration\ports\expiration-store.ts:24:export interface ExpirationStore {
src/modules\reminder\application\reminder-materializer.ts:3: * interface + §9.2 (occurrence shape/GSI3 keys) + §8.3 (reacting to ItemDueDateChanged:
src/modules\reminder\application\reminder-materializer.ts:32:export interface MaterializeInput {
src/modules\reminder\application\reminder-materializer.ts:41:export interface MaterializeResult {
src/modules\reminder\application\reminder-materializer.ts:83:export class ReminderMaterializer {
src/modules\reminder\application\reminder-materializer.ts:179:        GSI3PK: gsi3.GSI3PK,
src/modules\reminder\application\reminder-materializer.ts:180:        GSI3SK: gsi3.GSI3SK,
src/modules\reminder\application\reminder-materializer.ts:216:              // GSI3PK/GSI3SK are intentionally left in place (occ.ts's builder is
src/modules\reminder\ports\reminder-store.ts:11: * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
src/modules\reminder\ports\reminder-store.ts:13: * tem permissão de Query no GSI3") is enforced structurally here: ReminderStore (used by
src/modules\reminder\ports\reminder-store.ts:15: * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
src/modules\reminder\ports\reminder-store.ts:23:export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
src/modules\reminder\ports\reminder-store.ts:27:export interface ReminderStore {
src/modules\reminder\ports\reminder-store.ts:36:export interface Gsi3QueryInput {
src/modules\reminder\ports\reminder-store.ts:41:export interface ReminderProducerStore {
src/modules\reminder\application\id-generator.ts:2:export interface ReminderIdGenerator {
src/modules\expiration\http\item-handlers.ts:14:export interface HttpRequest<TBody = unknown> {
src/modules\expiration\http\item-handlers.ts:24:export interface HttpResponse {
src/modules\expiration\http\item-handlers.ts:29:export interface ExpirationHttpDeps {
src/modules\expiration\application\id-generator.ts:2:export interface ExpirationIdGenerator {
src/modules\reminder\http\policy-handlers.ts:8:export interface HttpRequest<TBody = unknown> {
src/modules\reminder\http\policy-handlers.ts:17:export interface HttpResponse {
src/modules\reminder\http\policy-handlers.ts:22:export interface ReminderHttpDeps {
src/modules\expiration\application\expiration-service.ts:42:export interface ExpirationServiceDeps {
src/modules\expiration\application\expiration-service.ts:49:export interface DashboardQuery {
src/modules\expiration\application\expiration-service.ts:55:export class ExpirationService {
src/modules\reminder\domain\recurrence.ts:19:export type ZonedResolutionKind = "NORMAL" | "AMBIGUOUS" | "NONEXISTENT";
src/modules\reminder\domain\recurrence.ts:21:export interface ZonedResolution {
src/modules\reminder\domain\recurrence.ts:26:export interface LocalDateTimeParts {
src/modules\identity\application\quota.ts:10:export type QuotaType = "API_REQUEST" | "UPLOAD_BYTES" | "UPLOAD_COUNT" | "AI_CALL";
src/modules\identity\application\quota.ts:12:export interface TenantQuotaRecord {
src/modules\identity\application\quota.ts:29:export interface QuotaCheckInput {
src/modules\identity\application\quota.ts:45:export class TenantQuotaService {
src/modules\reminder\domain\gsi3-parse.ts:1:/** Parses `GSI3SK = TENANT#<tenantId>#OCCURRENCE#<occurrenceId>` back into its parts - used by the ReminderProducer to reconstruct tenant context from a GSI3 query result before the strongly-consistent base-item read (data-model.md §3: "tenantId preservado... para reconstrução segura do contexto"). */
src/modules\reminder\domain\gsi3-parse.ts:5:    throw new Error(`Malformed GSI3SK: ${gsi3sk}`);
src/modules\reminder\domain\reminder-occurrence.ts:3: * co-located under the parent ExpirationItem's partition) and §3's GSI3 scheduler index
src/modules\reminder\domain\reminder-occurrence.ts:9:export type ReminderOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED" | "ACKED";
src/modules\reminder\domain\reminder-occurrence.ts:11:export interface ReminderOccurrence extends EntityKey {
src/modules\reminder\domain\reminder-occurrence.ts:24:  shard: string; // zero-padded shard number, string form as used in GSI3PK
src/modules\reminder\domain\reminder-occurrence.ts:32:  GSI3PK?: string; // present only while status === SCHEDULED (or CLAIMED, see note in materializer) - removed once TRIGGERED/CANCELLED so the scheduler index only ever contains live work
src/modules\reminder\domain\reminder-occurrence.ts:33:  GSI3SK?: string;
src/modules\reminder\domain\reminder-occurrence.ts:68: * Builds the GSI3 keys for a materialized occurrence. `shardCount`/`shardFnVersion` are
src/modules\reminder\domain\reminder-occurrence.ts:77:}): { GSI3PK: string; GSI3SK: string; shard: string } {
src/modules\reminder\domain\reminder-occurrence.ts:81:    GSI3PK: `DUE#${minuteBucket(input.scheduledAt)}#${shard}`,
src/modules\reminder\domain\reminder-occurrence.ts:82:    GSI3SK: `TENANT#${input.tenantId}#OCCURRENCE#${input.occurrenceId}`,
src/modules\reminder\domain\reminder-occurrence.ts:87:/** Enumerates the GSI3PK values to query for a given UTC minute across all shards of a given generation - used by both the producer (current minute + lookback) and reconciliation. */
src/modules\identity\application\resolve-request-context.ts:18:export interface ValidatedClaims {
src/modules\identity\application\resolve-request-context.ts:26:export interface ResolveRequestContextInput {
src/modules\identity\application\resolve-request-context.ts:32:export interface IdGenerator {
src/modules\identity\application\resolve-request-context.ts:37:export class RequestContextResolver {
src/modules\identity\domain\authorization.ts:7:export type Action =
src/modules\identity\domain\authorization.ts:22:export interface AuthorizedResource {
src/modules\identity\domain\authorization.ts:29:export interface AuthorizationInput {
src/modules\identity\domain\authorization.ts:40:export type Role = "OWNER" | "MEMBER" | "VIEWER";
src/modules\identity\domain\authorization.ts:61:export class AuthorizationDeniedError extends Error {
src/modules\reminder\domain\reminder-policy.ts:8:export type ReminderPolicyScope = "TEMPLATE" | "ITEM";
src/modules\reminder\domain\reminder-policy.ts:9:export type NotificationChannelKind = "EMAIL" | "WHATSAPP";
src/modules\reminder\domain\reminder-policy.ts:13:export interface ReminderTrigger {
src/modules\reminder\domain\reminder-policy.ts:21:export interface QuietHours {
src/modules\reminder\domain\reminder-policy.ts:26:export interface ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:35:export interface ReminderPolicy extends EntityKey, ReminderRule {
src/modules\reminder\domain\reminder-policy.ts:53:export interface PutPolicyInput {
src/modules\expiration\domain\audit-event.ts:13:export type AuditAction = "CREATE" | "UPDATE" | "ARCHIVE" | "RENEW" | "DELETE";
src/modules\expiration\domain\audit-event.ts:15:export interface AuditEvent extends EntityKey {
src/modules\expiration\domain\audit-event.ts:44:export interface BuildAuditEventInput {
src/modules\reminder\domain\notification-intent.ts:8:export type NotificationIntentStatus = "PENDING" | "CANCELLED" | "DISPATCHED" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:9:export type NotificationIntentKind = "EXPIRATION_REMINDER" | "CORRECTIVE";
src/modules\reminder\domain\notification-intent.ts:11:export interface NotificationIntent extends EntityKey {
src/modules\reminder\domain\shard-config.ts:14:export interface ShardGeneration {
src/modules\reminder\domain\shard-config.ts:21:export interface ShardConfig {
src/modules\identity\domain\request-context.ts:7:export interface RequestContext {
src/modules\identity\ports\identity-store.ts:8:export interface EntityKey {
src/modules\identity\ports\identity-store.ts:13:export interface IdentityStore {
src/modules\reminder\application\reminder-policy-service.ts:20:export interface ReminderPolicyServiceDeps {
src/modules\reminder\application\reminder-policy-service.ts:27:export class ReminderPolicyService {
src/modules\expiration\domain\expiration-item.ts:11:export type ExpirationItemStatus = "ACTIVE" | "ARCHIVED" | "RENEWED" | "DELETED";
src/modules\expiration\domain\expiration-item.ts:13:export interface ExpirationItem extends EntityKey {
src/modules\expiration\domain\expiration-item.ts:65:export interface CreateItemInput {
src/modules\expiration\domain\expiration-item.ts:80:export interface UpdateItemInput {
src/modules\expiration\domain\expiration-item.ts:94:export interface RenewItemInput {
src/modules\identity\http\test-route-handler.ts:13:export interface TestRouteHttpRequest {
src/modules\identity\http\test-route-handler.ts:19:export interface TestRouteHttpResponse {
src/modules\identity\http\test-route-handler.ts:24:export interface TestRouteDeps {
src/modules\identity\persistence\identity-mapping-repository.ts:6: * (the other is GSI3) — the lookup happens *before* tenantId is known.
src/modules\identity\persistence\identity-mapping-repository.ts:11:export interface IdentityMapping {
src/modules\identity\persistence\identity-mapping-repository.ts:25:export class IdentityMappingRepository {
src/modules\identity\persistence\user-repository.ts:13:export interface UserProfile {
src/modules\identity\persistence\user-repository.ts:29:export interface DeviceSession {
src/modules\identity\persistence\user-repository.ts:53:export class UserRepository {

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/reminder/ports/reminder-store.ts; Get-Content src/modules/expiration/ports/expiration-store.ts; Get-Content src/modules/identity/ports/identity-store.ts; Get-Content infra/lib/scoped-lambda-function.ts; Get-Content infra/lib/expiration-tracker-stack.ts; Get-Content src/workers/reminder-producer/producer.ts; Get-Content src/workers/reminder-dispatch/dispatch.ts; Get-Content src/workers/reminder-reconciliation/reconciliation.ts; Get-Content package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 368ms:
/**
 * DynamoDB surface the Reminder module needs - same SDK-agnostic port pattern as
 * src/modules/expiration/ports/expiration-store.ts (AGENTS.md Â§6: "qualquer mÃ³dulo futuro
 * com a mesma necessidade deve reusar esse formato de port, nÃ£o inventar um novo"). This
 * IS that reuse: identical shape (get/putIfAbsent/update/transactWrite/queryGsi1-style),
 * plus one addition this module needs and Expiration didn't - `queryByItem`, to list an
 * item's existing ReminderOccurrence rows (co-located under the item's own PK per
 * data-model.md Â§2) for materialization diffing, due-date-change cancellation and DST
 * reconciliation.
 *
 * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
 * - implementation-blueprint.md Â§9.2's isolation safeguard ("nenhuma rota tenant-facing
 * tem permissÃ£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
 * the policy service, materializer and HTTP handlers - all tenant-facing) has NO method
 * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
 * ReminderProducer worker, exposes it. test/integration/gsi3-isolation.test.ts asserts
 * this structurally (no tenant-facing dependency graph can reach queryGsi3).
 */
// 2026-08-19 (Engineering Maturity Review): imports from shared/dynamodb/occ.ts directly
// rather than re-exporting through expiration/ports/expiration-store.js - the previous
// indirection was an accidental cross-module ports->ports dependency (this module doesn't
// actually need anything Expiration-specific here, just the generic DynamoDB shapes).
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface ReminderStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** All rows under an item's own partition whose SK begins with "OCC#" (ReminderOccurrence rows). Strongly consistent - base-partition read, not a GSI (data-model.md Â§5). */
  queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string): Promise<T[]>;
}

export interface Gsi3QueryInput {
  gsi3pk: string;
}

/** Narrow port, injected ONLY into the ReminderProducer worker (see file header). */
export interface ReminderProducerStore {
  queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]>;
  /** Strongly consistent read of the base item, used by the producer to reconstruct tenant context before claiming (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** Conditional SCHEDULED -> CLAIMED transition (implementation-blueprint.md Â§9.3 point 3). */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
}
/**
 * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
 * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
 * inject an implementation, so domain/application logic is unit-testable without live
 * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
 * data-model.md Â§5's load-bearing sentence requires item update + outbox record (and,
 * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
 * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
 * be individually visible without the others.
 */
import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

// Re-exported for backward compatibility with existing importers (application/http layers
// of this and other modules) - canonical definitions now live in shared/dynamodb/occ.ts.
export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";

export interface Gsi1QueryInput {
  gsi1pk: string;
  ascending?: boolean;
  limit?: number;
}

export interface ExpirationStore {
  /** Strongly consistent single-item read (data-model.md Â§5: authorization/edition/pre-send reads must be consistent). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  /** Unconditional overwrite of an existing item - used only for non-OCC bookkeeping records (e.g. idempotency status transitions), never for ExpirationItem/AuditEvent. */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * Commits every entry atomically. Throws an error recognized by
   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
   * assume partial application.
   */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** Eventually consistent GSI1 query (data-model.md Â§3: vencimentos/dashboard). */
  queryGsi1<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi1QueryInput): Promise<T[]>;
}
/**
 * Minimal DynamoDB surface the identity module needs, following the SDK-agnostic port
 * pattern established in src/shared/idempotency/idempotency.ts (M0): callers inject an
 * implementation of this port, so the module is unit-testable without live AWS. M1's
 * infra layer (infra/lib) provisions the real table; src/modules/identity/persistence
 * wires a DocumentClient-backed adapter to this same port for production use.
 */
export interface EntityKey {
  PK: string;
  SK: string;
}

export interface IdentityStore {
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  update<T extends EntityKey>(item: T): Promise<void>;
}
/**
 * ScopedLambdaFunction â€” implementation-blueprint.md Â§17.1. "O construct gera grants
 * especÃ­ficos e falha synth quando o recurso solicitado nÃ£o corresponde a uma capability
 * conhecida."
 *
 * Judgment call (blueprint is schematic, not exhaustive, about capability granularity):
 * DynamoDB IAM cannot restrict by entity type (SK prefix) the way the blueprint's example
 * syntax (`tableAccess.readWriteKeys("ReminderOccurrence", "ExpirationItem")`) visually
 * suggests - IAM conditions operate on dynamodb:LeadingKeys (PK) at best, not on SK
 * patterns. This implementation is honest about that limit: the `entities` list is kept
 * as construct metadata (visible in `cdk synth`, useful for audit/least-privilege review)
 * but the actual IAM grant is table-level read/write via the well-tested CDK
 * `Table.grantReadWriteData`. Tighter per-entity IAM (via a Query/Scan condition on SK
 * prefix, not currently exposed by CDK's grant helpers) is deferred; noted in the M1 report.
 */
import * as lambda from "aws-cdk-lib/aws-lambda";
import * as iam from "aws-cdk-lib/aws-iam";
import * as sqs from "aws-cdk-lib/aws-sqs";
import { Duration } from "aws-cdk-lib";
import { Construct } from "constructs";
import type { ExpirationTrackerTable } from "./dynamo-table.js";

export interface AccessCapability {
  readonly kind: string;
  readonly description: string;
  grant(fn: lambda.Function): void;
}

export function tableAccessFor(table: ExpirationTrackerTable) {
  return {
    readWriteKeys(...entities: string[]): AccessCapability {
      return {
        kind: "table:read-write",
        description: `read/write on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadWriteData(fn),
      };
    },
    readKeys(...entities: string[]): AccessCapability {
      return {
        kind: "table:read",
        description: `read-only on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadData(fn),
      };
    },
    create(...entities: string[]): AccessCapability {
      return {
        kind: "table:create",
        description: `create-only on entities [${entities.join(", ")}]`,
        grant: (fn) => table.grantReadWriteData(fn), // PutItem-only IAM condition not expressible via CDK grant helpers; documented limitation
      };
    },
    /** GSI3 (scheduler) read-only - reserved EXCLUSIVELY for ReminderProducer
     * (data-model.md Â§3 / implementation-blueprint.md Â§9.2's isolation safeguard). Routed
     * through ExpirationTrackerTable.grantGsi3ReadTo, never table.grantReadData, so no
     * other function can acquire this permission via this helper. */
    gsi3Read(): AccessCapability {
      return {
        kind: "table:gsi3-read",
        description: "GSI3 (scheduler) read-only - ReminderProducer only",
        grant: (fn) => table.grantGsi3ReadTo(fn),
      };
    },
  };
}

export function queueAccessFor() {
  return {
    consume(queue: sqs.IQueue): AccessCapability {
      return {
        kind: "queue:consume",
        description: `consume from ${queue.queueName}`,
        grant: (fn) => queue.grantConsumeMessages(fn),
      };
    },
    send(queue: sqs.IQueue): AccessCapability {
      return {
        kind: "queue:send",
        description: `send to ${queue.queueName}`,
        grant: (fn) => queue.grantSendMessages(fn),
      };
    },
  };
}

export function appConfigAccessFor() {
  return {
    read(profileName: string): AccessCapability {
      return {
        kind: "appconfig:read",
        description: `read AppConfig profile ${profileName}`,
        grant: (fn) =>
          fn.addToRolePolicy(
            new iam.PolicyStatement({
              actions: ["appconfig:GetLatestConfiguration", "appconfig:StartConfigurationSession"],
              resources: ["*"], // AppConfig resource ARNs are per-deploy; scoped once the AppConfig construct exists (M1+ follow-up)
            }),
          ),
      };
    },
  };
}

const KNOWN_CAPABILITY_KINDS = new Set([
  "table:read-write",
  "table:read",
  "table:create",
  "table:gsi3-read",
  "queue:consume",
  "queue:send",
  "appconfig:read",
]);

export interface ScopedLambdaFunctionProps extends Omit<lambda.FunctionProps, "code"> {
  access: AccessCapability[];
  /** Defaults to bundling entryFile via NodejsFunction-equivalent asset if provided; else code must be set via `code` prop override (tests use inline code). */
  code?: lambda.Code;
}

/**
 * Every Lambda in this system MUST be constructed via ScopedLambdaFunction, never
 * `new lambda.Function` directly (implementation-blueprint.md Â§17.1) - it is the single
 * enforcement point for least-privilege IAM per function.
 */
export class ScopedLambdaFunction extends Construct {
  readonly function: lambda.Function;

  constructor(scope: Construct, id: string, props: ScopedLambdaFunctionProps) {
    super(scope, id);

    for (const capability of props.access) {
      if (!KNOWN_CAPABILITY_KINDS.has(capability.kind)) {
        // "falha synth quando o recurso solicitado nÃ£o corresponde a uma capability conhecida"
        throw new Error(
          `ScopedLambdaFunction(${id}): unknown access capability kind "${capability.kind}". ` +
            `Known kinds: ${[...KNOWN_CAPABILITY_KINDS].join(", ")}.`,
        );
      }
    }

    const { access, code, ...rest } = props;

    this.function = new lambda.Function(this, "Function", {
      timeout: Duration.seconds(10),
      memorySize: 256,
      tracing: lambda.Tracing.ACTIVE,
      ...rest,
      code: code ?? lambda.Code.fromInline("exports.handler = async () => ({ statusCode: 501 });"),
    });

    for (const capability of access) {
      capability.grant(this.function);
    }
  }
}
/**
 * M1 stack: wires DynamoDB, Cognito, the identity test route (ScopedLambdaFunction) and
 * the API skeleton together. Later milestones (M2+) add stacks/constructs for their own
 * modules, importing this table/auth rather than re-declaring them.
 */
import { Stack, type StackProps } from "aws-cdk-lib";
import type { Construct } from "constructs";
import * as lambda from "aws-cdk-lib/aws-lambda";
import { ExpirationTrackerTable } from "./dynamo-table.js";
import { ExpirationTrackerAuth, type MfaPolicy } from "./cognito.js";
import { ScopedLambdaFunction, tableAccessFor } from "./scoped-lambda-function.js";
import { ExpirationTrackerApi } from "./api.js";

export interface ExpirationTrackerStackProps extends StackProps {
  mfaPolicy?: MfaPolicy;
}

export class ExpirationTrackerStack extends Stack {
  readonly table: ExpirationTrackerTable;
  readonly auth: ExpirationTrackerAuth;
  readonly api: ExpirationTrackerApi;
  readonly remindersHandler: ScopedLambdaFunction;
  readonly reminderProducer: ScopedLambdaFunction;
  readonly reminderDispatch: ScopedLambdaFunction;
  readonly reminderReconciliation: ScopedLambdaFunction;

  constructor(scope: Construct, id: string, props: ExpirationTrackerStackProps = {}) {
    super(scope, id, props);

    this.table = new ExpirationTrackerTable(this, "Table");
    this.auth = new ExpirationTrackerAuth(this, "Auth", { mfaPolicy: props.mfaPolicy });

    const tableAccess = tableAccessFor(this.table);

    const testRouteHandler = new ScopedLambdaFunction(this, "TestPingHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("IdentityMapping", "User", "TenantQuota")],
    });

    // M2: ExpirationItem/AuditEvent CRUD+renew+dashboard, plus outbox writes for
    // ItemDueDateChanged. GSI1 (dashboard) is table-level read/write per
    // ScopedLambdaFunction's documented IAM granularity limit (see that construct's
    // header comment) - never grantGsi3ReadTo, which stays reserved for M3's
    // ReminderProducer.
    const itemsHandler = new ScopedLambdaFunction(this, "ItemsHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ExpirationItem", "AuditEvent", "OutboxEvent", "IdempotencyRecord")],
    });

    this.api = new ExpirationTrackerApi(this, "Api", {
      auth: this.auth,
      testRouteHandler,
      itemsHandler,
    });

    // M3: policy CRUD is tenant-facing (HTTP), like ItemsHandler - table-level RW, no GSI3.
    this.remindersHandler = new ScopedLambdaFunction(this, "RemindersHandler", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ReminderPolicy", "ReminderOccurrence")],
    });

    // ReminderProducer (Â§9.3) is the ONLY function in the system granted gsi3Read() - the
    // narrow capability routed through ExpirationTrackerTable.grantGsi3ReadTo, which
    // itself never appears on any tenant-facing function (test/infra/reminder-engine.test.ts
    // asserts this at the synthesized-template level).
    this.reminderProducer = new ScopedLambdaFunction(this, "ReminderProducer", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [tableAccess.readWriteKeys("ReminderOccurrence"), tableAccess.gsi3Read()],
    });

    // ReminderDispatch (Â§9.4): CLAIMED -> TRIGGERED + NotificationIntent + outbox, all in
    // one TransactWriteItems - needs write access to occurrences/intents/outbox/idempotency
    // and read access to ExpirationItem/ReminderPolicy for the staleness check, never GSI3.
    this.reminderDispatch = new ScopedLambdaFunction(this, "ReminderDispatch", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [
        tableAccess.readWriteKeys(
          "ReminderOccurrence",
          "NotificationIntent",
          "OutboxEvent",
          "IdempotencyRecord",
        ),
        tableAccess.readKeys("ExpirationItem", "ReminderPolicy"),
      ],
    });

    // ReminderReconciliation (Â§9.5): single job for claim-expiry revert + DST
    // re-evaluation - never GSI3 (it reasons over base-partition/GSI6-style batches, not
    // the scheduler index).
    this.reminderReconciliation = new ScopedLambdaFunction(this, "ReminderReconciliation", {
      runtime: lambda.Runtime.NODEJS_20_X,
      handler: "index.handler",
      access: [
        tableAccess.readWriteKeys("ReminderOccurrence"),
        tableAccess.readKeys("ExpirationItem", "ReminderPolicy"),
      ],
    });
  }
}
/**
 * ReminderProducer core logic - implementation-blueprint.md Â§9.3. Pure(ish) - takes an
 * injected `ReminderProducerStore` and clock, no AWS SDK/Lambda runtime dependency, so it
 * is directly unit/integration-testable with a fake clock and fake GSI3 index (per the
 * task's requirement to test time-sensitive logic without real wall-clock waiting).
 *
 * Steps (Â§9.3):
 *  1. read active shard generations (current + still-active legacy, Â§9.2's reshard runbook);
 *  2. query GSI3 across N shards of minute M AND a lookback window [M-lookback, M] (default
 *     5 minutes) - "sem lookback, 'o prÃ³ximo tick reprocessa' Ã© falso";
 *  3. per eligible occurrence, conditional SCHEDULED -> CLAIMED with a short claim
 *     `claimExpiresAt`, and emit one `reminder.dispatch.v1` command;
 *  4. partial-batch-failure handling: only failed entries are surfaced in `failed`, callers
 *     retry just those (never the whole batch);
 *  5. returns counters for `scheduler_lag_seconds`/per-shard/lookback-depth metrics (left to
 *     the Lambda handler to emit via SecureLogger/EMF - this module stays observability-agnostic).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { gsi3PartitionsForMinute } from "../../modules/reminder/domain/reminder-occurrence.js";
import { parseGsi3Sk } from "../../modules/reminder/domain/gsi3-parse.js";
import { activeGenerations, type ShardConfig } from "../../modules/reminder/domain/shard-config.js";
import { isTransactionCanceled } from "../../modules/reminder/ports/reminder-store.js";
import type { ReminderProducerStore } from "../../modules/reminder/ports/reminder-store.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";

export interface DispatchCommand {
  commandType: "reminder.dispatch.v1";
  tenantId: string;
  deduplicationKey: string;
  data: {
    itemId: string;
    occurrenceId: string;
    occurrenceVersion: number;
    scheduledAt: string;
    itemVersion: number;
    policyVersion: number;
  };
}

export interface ProducerDeps {
  store: ReminderProducerStore;
  shardConfig: ShardConfig;
  tableName: string;
  now: () => string;
  /** Short claim TTL - default 2 minutes, comfortably longer than one producer tick (1 minute) but short enough that a crashed dispatch worker's claim is reclaimable quickly by reconciliation (Â§9.5). */
  claimTtlMs?: number;
  /** Lookback window in minutes, inclusive of the current minute (implementation-blueprint.md Â§9.3 example: [M-5min, M]). */
  lookbackMinutes?: number;
}

export interface ProducerTickResult {
  claimed: DispatchCommand[];
  failed: { occurrenceId: string; tenantId: string; error: unknown }[];
  scanned: number;
  minutesScanned: string[];
  shardPartitionsScanned: number;
}

function minuteFloor(d: Date): Date {
  return new Date(Math.floor(d.getTime() / 60_000) * 60_000);
}

/** Runs one producer tick for wall-clock minute `tickMinute` (already floored to the minute by the caller/Lambda trigger). */
export async function runProducerTick(deps: ProducerDeps, tickMinute: Date): Promise<ProducerTickResult> {
  const generations = activeGenerations(deps.shardConfig, deps.now());
  const lookback = deps.lookbackMinutes ?? 5;
  const claimTtlMs = deps.claimTtlMs ?? 2 * 60_000;

  const minutesToScan: Date[] = [];
  for (let i = lookback; i >= 0; i--) {
    minutesToScan.push(minuteFloor(new Date(tickMinute.getTime() - i * 60_000)));
  }

  const claimed: DispatchCommand[] = [];
  const failed: { occurrenceId: string; tenantId: string; error: unknown }[] = [];
  let scanned = 0;
  let partitions = 0;

  // Dedup guard: the same occurrence could show up in more than one scanned minute only if
  // materialization ever wrote it under a stale bucket, which shouldn't happen - but a
  // Set guards against double-claiming within a single tick regardless.
  const seen = new Set<string>();

  for (const minute of minutesToScan) {
    for (const generation of generations) {
      const partitionKeys = gsi3PartitionsForMinute(minute, generation.shardCount);
      partitions += partitionKeys.length;
      for (const gsi3pk of partitionKeys) {
        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
        for (const row of rows) {
          scanned += 1;
          const { tenantId, occurrenceId } = parseGsi3Sk(row.GSI3SK);
          if (seen.has(occurrenceId)) continue;
          seen.add(occurrenceId);

          try {
            const occurrence = await deps.store.get<ReminderOccurrence>({ PK: row.PK, SK: row.SK });
            if (!occurrence || occurrence.status !== "SCHEDULED") {
              // Already claimed/cancelled/triggered by a prior tick or race - not a failure.
              continue;
            }

            const claimExpiresAt = new Date(Date.parse(deps.now()) + claimTtlMs).toISOString();
            await deps.store.transactWrite([
              {
                Update: buildVersionedUpdate({
                  tableName: deps.tableName,
                  key: { PK: row.PK, SK: row.SK },
                  tenantId,
                  expectedVersion: occurrence.version,
                  set: { status: "CLAIMED", claimedAt: deps.now(), claimExpiresAt },
                }),
              },
            ]);

            claimed.push({
              commandType: "reminder.dispatch.v1",
              tenantId,
              deduplicationKey: `${tenantId}|${occurrenceId}|${occurrence.version + 1}`,
              data: {
                itemId: occurrence.itemId,
                occurrenceId,
                occurrenceVersion: occurrence.version + 1,
                scheduledAt: occurrence.scheduledAt,
                itemVersion: occurrence.itemVersion,
                policyVersion: occurrence.policyVersion,
              },
            });
          } catch (err) {
            if (isTransactionCanceled(err)) {
              // Lost the claim race to a concurrent producer tick - not a failure to retry.
              continue;
            }
            failed.push({ occurrenceId, tenantId, error: err });
          }
        }
      }
    }
  }

  return {
    claimed,
    failed,
    scanned,
    minutesScanned: minutesToScan.map((d) => d.toISOString()),
    shardPartitionsScanned: partitions,
  };
}
/**
 * Reminder dispatch worker core logic - implementation-blueprint.md Â§9.4. Consumes one
 * `reminder.dispatch.v1` command (produced by the ReminderProducer, Â§9.3) and performs the
 * CLAIMED -> TRIGGERED transition, creating exactly one `NotificationIntent` idempotently.
 *
 * Validation order per Â§9.4: occurrence must be CLAIMED (not SCHEDULED directly - the claim
 * prevents double-processing by two concurrent dispatch workers); item must be ACTIVE;
 * versions must match; schedule must be within tolerance; policy must still exist. If any
 * check fails, the occurrence is conditionally cancelled (CLAIMED -> CANCELLED) instead of
 * silently dropped, and repair is left to reconciliation (Â§9.5) - never a new ad-hoc
 * materialization from inside dispatch.
 *
 * On success, everything commits in ONE TransactWriteItems (Â§9.4: "corrige a lacuna de
 * durabilidade... antes o evento era tratado como reconstruÃ­vel sem outbox"):
 * NotificationIntent Put + occurrence CLAIMED->TRIGGERED Update + idempotency record Put
 * (`tenantId|occurrenceId`, Â§9.4's exact key for NotificationIntentCreated consumers) +
 * outbox `notification.intent-created.v1` Put.
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { appendToTransaction } from "../../shared/outbox/outbox.js";
import { buildIdempotencyKey } from "../../shared/idempotency/idempotency.js";
import type { DomainEvent } from "../../shared/contracts/events.js";
import { itemKey } from "../../modules/expiration/domain/expiration-item.js";
import { policyKey, type ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import { intentKey, type NotificationIntent } from "../../modules/reminder/domain/notification-intent.js";
import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../../modules/reminder/ports/reminder-store.js";
import type { DispatchCommand } from "../reminder-producer/producer.js";

const NOTIFICATION_INTENT_CREATED = "notification.intent-created.v1";

export interface DispatchDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  newIntentId: () => string;
  newEventId: () => string;
  correlationId: () => string;
  /** Max allowed lag between scheduledAt and now before a claimed occurrence is considered stale (default 30 minutes - generous vs. the claim TTL, covers legitimate SQS/Lambda retry delay). */
  toleranceMs?: number;
}

export type DispatchOutcome =
  | { kind: "TRIGGERED"; intent: NotificationIntent }
  | { kind: "ALREADY_TRIGGERED" }
  | { kind: "CANCELLED_STALE"; reason: string }
  | { kind: "SKIPPED_NOT_CLAIMED" };

/** Looks up the occurrence by (itemId, occurrenceId) - occurrences are co-located under the item's own partition (data-model.md Â§2), but the command doesn't carry the SK's scheduledAt segment, so the worker queries the item's occurrences and finds the match by occurrenceId. Kept here rather than in ReminderStore to avoid growing the port for a single caller. */
async function findOccurrence(store: ReminderStore, tenantId: string, itemId: string, occurrenceId: string): Promise<ReminderOccurrence | undefined> {
  const rows = await store.queryByItem<ReminderOccurrence>(tenantId, itemId);
  return rows.find((r) => r.occurrenceId === occurrenceId);
}

export async function dispatchOccurrence(deps: DispatchDeps, command: DispatchCommand): Promise<DispatchOutcome> {
  const { tenantId } = command;
  const { itemId, occurrenceId, itemVersion, policyVersion } = command.data;

  const occurrence = await findOccurrence(deps.store, tenantId, itemId, occurrenceId);
  if (!occurrence) {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }
  if (occurrence.status === "TRIGGERED") {
    return { kind: "ALREADY_TRIGGERED" };
  }
  if (occurrence.status !== "CLAIMED") {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }

  const item = await deps.store.get<{ PK: string; SK: string; status: string; version: number }>(itemKey(tenantId, itemId));
  const policy = await deps.store.get<ReminderPolicy>(policyKey(tenantId, occurrence.policyId));

  const toleranceMs = deps.toleranceMs ?? 30 * 60_000;
  const withinTolerance = Math.abs(Date.parse(deps.now()) - Date.parse(occurrence.scheduledAt)) <= toleranceMs;

  const stale =
    !item ||
    item.status !== "ACTIVE" ||
    item.version !== itemVersion ||
    occurrence.itemVersion !== itemVersion ||
    !policy ||
    policy.version !== policyVersion ||
    !policy.enabled ||
    !withinTolerance;

  if (stale) {
    const reason = !item
      ? "ITEM_NOT_FOUND"
      : item.status !== "ACTIVE"
        ? "ITEM_NOT_ACTIVE"
        : item.version !== itemVersion || occurrence.itemVersion !== itemVersion
          ? "ITEM_VERSION_MISMATCH"
          : !policy || !policy.enabled
            ? "POLICY_GONE_OR_DISABLED"
            : policy.version !== policyVersion
              ? "POLICY_VERSION_MISMATCH"
              : "OUT_OF_TOLERANCE";

    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId,
            expectedVersion: occurrence.version,
            set: { status: "CANCELLED" },
          }),
        },
      ]);
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
    return { kind: "CANCELLED_STALE", reason };
  }

  const now = deps.now();
  const intentId = deps.newIntentId();
  const intent: NotificationIntent = {
    ...intentKey(tenantId, intentId),
    entityType: "NotificationIntent",
    intentId,
    tenantId,
    kind: "EXPIRATION_REMINDER",
    itemId,
    occurrenceId,
    itemVersion,
    policyId: occurrence.policyId,
    policyVersion,
    scheduledAt: occurrence.scheduledAt,
    requestedChannels: policy.channels.filter((c) => !(policy.optOutChannels ?? []).includes(c)),
    status: "PENDING",
    supersedesIntentId: null,
    correctionReason: null,
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const entries: TransactWriteEntry[] = [
    { Put: { TableName: deps.tableName, Item: { ...intent }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
    {
      Update: buildVersionedUpdate({
        tableName: deps.tableName,
        key: { PK: occurrence.PK, SK: occurrence.SK },
        tenantId,
        expectedVersion: occurrence.version,
        set: { status: "TRIGGERED" },
      }),
    },
  ];

  // Idempotency record for NotificationIntentCreated consumers (Â§9.4: "tenantId|occurrenceId").
  const idem = buildIdempotencyKey(deps.tableName, tenantId, "reminder.dispatch", occurrenceId);
  entries.push({
    Put: {
      TableName: deps.tableName,
      Item: {
        PK: idem.PK,
        SK: idem.SK,
        entityType: "IdempotencyRecord",
        tenantId,
        operation: "reminder.dispatch",
        requestHash: occurrenceId,
        status: "COMPLETED",
        responseRef: intentId,
        expiresAt: new Date(Date.parse(now) + 7 * 24 * 60 * 60_000).toISOString(),
        createdAt: now,
        completedAt: now,
      },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });

  const event: DomainEvent = {
    specVersion: "1.0",
    eventId: deps.newEventId(),
    eventType: NOTIFICATION_INTENT_CREATED,
    source: "expiration-tracker.reminder",
    occurredAt: now,
    correlationId: deps.correlationId(),
    tenantId,
    actor: { type: "SYSTEM" },
    aggregate: { type: "NotificationIntent", id: intentId, version: 1 },
    data: {
      intentId,
      kind: "EXPIRATION_REMINDER",
      itemId,
      occurrenceId,
      itemVersion,
      policyId: occurrence.policyId,
      policyVersion,
      scheduledAt: occurrence.scheduledAt,
      requestedChannels: intent.requestedChannels,
      status: "PENDING",
      supersedesIntentId: null,
      correctionReason: null,
    },
  };
  appendToTransaction(entries as never, deps.tableName, event);

  try {
    await deps.store.transactWrite(entries);
  } catch (err) {
    if (isTransactionCanceled(err)) {
      // Duplicate delivery of the same command (SQS at-least-once) racing a prior success -
      // either the idempotency Put or the occurrence's CLAIMED condition already advanced.
      return { kind: "ALREADY_TRIGGERED" };
    }
    throw err;
  }

  return { kind: "TRIGGERED", intent };
}
/**
 * Reconciliation - implementation-blueprint.md Â§9.5, run as ONE job/Lambda serving TWO
 * purposes (per the task brief: "don't build two separate reconciliation paths"), both
 * documented in Â§9.3 point 4 ("claims expirados... via reconciliaÃ§Ã£o, mesmo job da Â§9.5")
 * and Â§9.5 itself (DST):
 *
 *  (a) claim-expiry reconciliation: any occurrence still CLAIMED past its
 *      `claimExpiresAt` (dispatch worker crashed/timed out before completing) reverts to
 *      SCHEDULED, conditionally, so the next producer tick's lookback window picks it up
 *      again;
 *  (b) DST-affected re-evaluation (Â§9.5 steps 1-6): for each (item, enabled policy) pair
 *      in the reconciliation batch, recompute the expected schedule from the policy's
 *      IANA timeZone + rule against a `[now, now+7d]` window, compare to what's actually
 *      materialized, cancel divergent SCHEDULED occurrences and materialize missing ones
 *      idempotently - TRIGGERED/ACKED history is never touched.
 *
 * Both passes reuse the same store/materializer/clock and are invoked together by
 * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
 * unblocks the producer sooner).
 */
import { buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
import { ReminderMaterializer } from "../../modules/reminder/application/reminder-materializer.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import type { ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ShardConfig } from "../../modules/reminder/domain/shard-config.js";

export interface ReconciliationDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  shardConfig: ShardConfig;
}

/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
 * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
 * - M3's scope per the task brief is the reconciliation MECHANISM, not that index's
 * wiring); tests/callers supply the batch directly. */
export interface DstReconciliationCandidate {
  tenantId: string;
  itemId: string;
  itemVersion: number;
  itemDueDate: string;
  policy: ReminderPolicy;
}

export interface ReconciliationResult {
  claimsReverted: number;
  dstCancelled: number;
  dstCreated: number;
  dstDivergences: number;
}

/** Pass (a): any CLAIMED occurrence whose claimExpiresAt is in the past reverts to SCHEDULED. */
export async function reconcileExpiredClaims(
  deps: ReconciliationDeps,
  candidateOccurrences: ReminderOccurrence[],
): Promise<number> {
  let reverted = 0;
  const now = deps.now();
  for (const occurrence of candidateOccurrences) {
    if (occurrence.status !== "CLAIMED") continue;
    if (!occurrence.claimExpiresAt || occurrence.claimExpiresAt > now) continue;
    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId: occurrence.tenantId,
            expectedVersion: occurrence.version,
            set: { status: "SCHEDULED" },
          }),
        },
      ]);
      reverted += 1;
    } catch (err) {
      if (!isTransactionCanceled(err)) throw err;
    }
  }
  return reverted;
}

/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (Â§9.5 steps 1-6). */
export async function reconcileDst(
  deps: ReconciliationDeps,
  candidates: DstReconciliationCandidate[],
): Promise<{ cancelled: number; created: number; divergences: number }> {
  const materializer = new ReminderMaterializer(deps.store, deps.tableName, deps.now);
  const windowStart = Date.parse(deps.now());
  const windowEnd = windowStart + 7 * 24 * 60 * 60_000;

  let cancelled = 0;
  let created = 0;
  let divergences = 0;

  for (const candidate of candidates) {
    if (!candidate.policy.enabled) continue;

    const existing = await deps.store.queryByItem<ReminderOccurrence>(candidate.tenantId, candidate.itemId);
    const liveExisting = existing.filter(
      (o) =>
        o.policyId === candidate.policy.policyId &&
        o.itemVersion === candidate.itemVersion &&
        o.policyVersion === candidate.policy.version &&
        (o.status === "SCHEDULED" || o.status === "CLAIMED"),
    );

    // Expected schedule per trigger, recomputed fresh from the IANA rule (never a frozen offset).
    const expectedByTrigger = new Map<string, { scheduledAtUtc: string }>();
    for (const trigger of candidate.policy.triggers) {
      const schedule = materializer.computeSchedule({
        itemDueDate: candidate.itemDueDate,
        offsetIso: trigger.offsetIso,
        localTime: trigger.localTime,
        timeZone: candidate.policy.timeZone,
        quietHours: candidate.policy.quietHours,
      });
      const t = Date.parse(schedule.scheduledAtUtc);
      if (t < windowStart || t > windowEnd) continue; // out of the 7-day reconciliation window
      expectedByTrigger.set(trigger.triggerId, { scheduledAtUtc: schedule.scheduledAtUtc });
    }

    // Cancel occurrences whose triggerId is no longer expected, or whose scheduledAt now
    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
    // liveExisting already excludes those statuses).
    for (const occurrence of liveExisting) {
      const expected = expectedByTrigger.get(occurrence.triggerId);
      const diverges = !expected || expected.scheduledAtUtc !== occurrence.scheduledAt;
      if (!diverges) continue;
      divergences += 1;
      try {
        await deps.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: deps.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: candidate.tenantId,
              expectedVersion: occurrence.version,
              set: { status: "CANCELLED" },
            }),
          },
        ]);
        cancelled += 1;
      } catch (err) {
        if (!isTransactionCanceled(err)) throw err;
      }
    }

    // Materialize any expected trigger missing a live (SCHEDULED/CLAIMED) occurrence -
    // materialize() itself is idempotent (putIfAbsent keyed by the deterministic
    // occurrenceId hash), so re-running it for triggers that already matched is a safe no-op.
    const stillMissing = candidate.policy.triggers.some((t) => {
      const expected = expectedByTrigger.get(t.triggerId);
      if (!expected) return false;
      return !liveExisting.some((o) => o.triggerId === t.triggerId && o.scheduledAt === expected.scheduledAtUtc);
    });
    if (stillMissing) {
      const result = await materializer.materialize({
        tenantId: candidate.tenantId,
        itemId: candidate.itemId,
        itemVersion: candidate.itemVersion,
        itemDueDate: candidate.itemDueDate,
        policy: candidate.policy,
        shardConfig: deps.shardConfig,
      });
      created += result.created.length;
    }
  }

  return { cancelled, created, divergences };
}

export async function runReconciliation(
  deps: ReconciliationDeps,
  input: { expiredClaimCandidates: ReminderOccurrence[]; dstCandidates: DstReconciliationCandidate[] },
): Promise<ReconciliationResult> {
  const claimsReverted = await reconcileExpiredClaims(deps, input.expiredClaimCandidates);
  const dst = await reconcileDst(deps, input.dstCandidates);
  return { claimsReverted, dstCancelled: dst.cancelled, dstCreated: dst.created, dstDivergences: dst.divergences };
}
{
  "name": "expiration-tracker",
  "version": "0.0.0",
  "private": true,
  "description": "Micro-SaaS de controle de vencimentos/renovacoes - guardrails e contratos (M0)",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "validate-schemas": "tsx scripts/validate-schemas.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "@vitest/coverage-v8": "^1.6.0",
    "aws-cdk-lib": "2.265.0",
    "aws-jwt-verify": "5.2.1",
    "constructs": "10.8.1",
    "dependency-cruiser": "17.4.3",
    "eslint": "^8.57.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "ajv": "^8.16.0",
    "ajv-formats": "^2.1.1",
    "ulid": "^2.3.0"
  }
}

codex
# Proposta técnica — milestone Runtime Real / G8

## 1. Decisão central

O milestone deve transformar o fluxo atual em runtime implantável sem contaminar os ports com tipos do AWS SDK:

```text
EventBridge Rule
      │
      ▼
ReminderProducer Lambda
      │
      ├─ TransactWrite:
      │    SCHEDULED → CLAIMED
      │    + registro durável DispatchOutbox
      ▼
DynamoDB Stream / relay
      │
      ▼
ReminderDispatchQueue
      │
      ▼
ReminderDispatch Lambda
      │
      ├─ revalida estado/versionamento
      └─ TransactWrite:
           CLAIMED → TRIGGERED
           + NotificationIntent
           + OutboxEvent
           + IdempotencyRecord
```

A gravação durável intermediária é necessária. Fazer `TransactWrite(CLAIMED)` e depois `SendMessage` diretamente no handler mantém uma janela de perda:

1. DynamoDB confirma o claim.
2. Lambda cai antes de enviar à SQS.
3. A ocorrência fica `CLAIMED`, mas não existe mensagem.

A reconciliação diária não fecha adequadamente essa janela: quando reverter o claim, a ocorrência poderá estar fora do lookback de cinco minutos do producer. Portanto, fila e DLQ, isoladamente, não bastam para fechar o P1 #1.

Minha recomendação é um registro `DispatchOutbox` gravado atomicamente com o claim e publicado por DynamoDB Streams. Uma alternativa aceitável é EventBridge Pipes entre o stream e a SQS, desde que seja demonstrado que filtragem, transformação do stream record e retries funcionam no ambiente AWS real. Eu prefiro um pequeno `DispatchOutboxRelay` Lambda porque oferece controle explícito de schema, observabilidade, partial batch failure e evolução do contrato.

## 2. Dependências

Adicionar como dependências de runtime:

```json
{
  "@aws-sdk/client-dynamodb": "versão exata",
  "@aws-sdk/lib-dynamodb": "versão exata",
  "@aws-sdk/client-sqs": "versão exata"
}
```

E como dependências de desenvolvimento:

```json
{
  "esbuild": "versão exata",
  "@types/aws-lambda": "versão exata",
  "testcontainers": "versão exata"
}
```

Escolhas:

- `DynamoDBClient`: transporte, configuração de retry e endpoint.
- `DynamoDBDocumentClient`: objetos JavaScript, compatível com os builders existentes.
- `SQSClient`: publicação explícita pelo relay.
- `esbuild`: bundling determinístico dos entrypoints ESM para Node.js 20.
- Versões exatas, não ranges, mantendo lockfile e `npm ci`.

Configuração do `DynamoDBDocumentClient`:

```ts
DynamoDBDocumentClient.from(client, {
  marshallOptions: {
    removeUndefinedValues: true,
    convertClassInstanceToMap: false,
  },
})
```

Não habilitaria conversões numéricas silenciosas. Campos de versão precisam continuar sendo números e valores grandes devem ser avaliados explicitamente antes de usar `wrapNumbers`.

O cliente deve usar retry adaptativo ou standard com número pequeno e explícito de tentativas, por exemplo três. O timeout total precisa caber com folga no timeout da Lambda; não se deve deixar o SDK consumir todo o orçamento da invocação.

## 3. Estrutura dos adapters

```text
src/
  runtime/
    aws/
      dynamodb/
        client.ts
        errors.ts
        execute-transaction.ts
        identity-store-dynamodb.ts
        expiration-store-dynamodb.ts
        reminder-store-dynamodb.ts
        reminder-producer-store-dynamodb.ts
        reconciliation-candidate-source-dynamodb.ts
      sqs/
        dispatch-command-publisher.ts

    handlers/
      test-ping-handler.ts
      items-handler.ts
      reminders-handler.ts
      reminder-producer-handler.ts
      reminder-dispatch-handler.ts
      reminder-reconciliation-handler.ts
      dispatch-outbox-relay-handler.ts

    composition/
      identity.ts
      expiration.ts
      reminder.ts
```

Um adapter por port:

- `DynamoDbIdentityStore implements IdentityStore`
- `DynamoDbExpirationStore implements ExpirationStore`
- `DynamoDbReminderStore implements ReminderStore`
- `DynamoDbReminderProducerStore implements ReminderProducerStore`

Não criaria uma classe “universal” pública implementando todos os ports. Isso permitiria injetar acidentalmente acesso ao GSI3 em código tenant-facing.

Pode haver helpers internos compartilhados para `GetCommand`, `PutCommand`, `UpdateCommand` e `TransactWriteCommand`, mas o método `queryGsi3` existe somente em `reminder-producer-store-dynamodb.ts`.

### Mapeamento das operações

| Port | Comando AWS |
|---|---|
| `get` | `GetCommand`, `ConsistentRead: true` |
| `putIfAbsent` | `PutCommand` com condição existente; retorna `false` apenas para falha condicional |
| `update` | `PutCommand` de overwrite, restrito aos usos já documentados pelo port |
| `transactWrite` | `TransactWriteCommand` usando diretamente os builders existentes |
| `queryGsi1` | `QueryCommand`, `IndexName: GSI1`, eventual consistency |
| `queryByItem` | `QueryCommand` na tabela base, `PK = :pk AND begins_with(SK, :occ)` e leitura consistente |
| `queryGsi3` | `QueryCommand`, `IndexName: GSI3`, somente no adapter privilegiado |

Paginação deve ser completa por padrão. `limit` representa limite lógico total, não apenas o `Limit` de uma única página.

### Tradução de erros

O adapter deve traduzuzir, sem esconder a causa:

- `ConditionalCheckFailedException` em `putIfAbsent` → `false`.
- `TransactionCanceledException` → erro reconhecível por `isTransactionCanceled`.
- throttling, timeout, falha 5xx e indisponibilidade → `AppError` retryable.
- validation error, item/schema inválido → erro terminal.
- `AccessDeniedException` → erro terminal operacional e alarme; não deve virar retry infinito.

Preservar `$metadata.requestId`, nome da operação e causa no log, sem registrar item completo nem dados pessoais.

## 4. Lacuna da reconciliação

O handler de reconciliação atual não consegue operar sozinho: `runReconciliation` recebe arrays de candidatos, mas `ReminderStore` não oferece uma consulta que os produza.

Executar a Lambda diariamente com arrays vazios seria outro false-green.

O milestone precisa introduzir um port estreito, por exemplo:

```ts
interface ReconciliationCandidateSource {
  listExpiredClaims(input: PageInput): AsyncIterable<ReminderOccurrence>;
  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
}
```

O adapter consultaria o GSI6 ou o índice efetivamente normativo. Isso é integração mínima necessária, não redesenho do domínio.

Se o GSI6 ainda não representa claims expirados e políticas ativas de maneira consultável, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo está apenas agendado, não funcional.

Além da execução diária de DST, recuperação de claims precisa ter cadência curta. Recomendo:

- reconciliação de claims expirados: a cada minuto ou cinco minutos;
- reconciliação DST/materialização: diária.

Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durável, a reconciliação curta continua útil para reparar estados excepcionais.

## 5. Handlers Lambda

Cada arquivo em `src/runtime/handlers` é um composition root:

1. valida variáveis de ambiente;
2. instancia clientes e adapters fora do handler para reuso em warm starts;
3. converte o evento AWS para os tipos internos;
4. invoca a lógica existente;
5. converte resultado/erro para o protocolo da origem;
6. registra métricas e logs via `SecureLogger`.

Nenhuma lógica de negócio deve morar no wrapper Lambda.

Variáveis mínimas:

```text
TABLE_NAME
DISPATCH_QUEUE_URL
SHARD_CONFIG
PRODUCER_LOOKBACK_MINUTES
CLAIM_TTL_SECONDS
LOG_LEVEL
```

Para HTTP, os handlers reais devem interpretar API Gateway HTTP API v2, extrair claims já validadas pelo authorizer, construir `RequestContext` e mapear `AppError` para status HTTP consistente.

Para schedules, validar `source`, `detail-type` e um campo explícito `mode`. Evento inesperado é terminal.

Para SQS, validar cada body contra `schemas/queues/reminder.dispatch.v1.json` antes de executar o domínio.

## 6. Bundling

Eu estenderia `ScopedLambdaFunction` com `entry` e `handlerExport`, mantendo-o como único ponto de criação de Lambda. Internamente, usaria `lambda.Code.fromAsset` com bundling `esbuild`, ou incorporaria a semântica de `NodejsFunction` sem permitir que stacks criem Lambdas por fora do construct.

Exemplo conceitual:

```ts
new ScopedLambdaFunction(this, "ReminderDispatch", {
  entry: "src/runtime/handlers/reminder-dispatch-handler.ts",
  handlerExport: "handler",
  runtime: lambda.Runtime.NODEJS_20_X,
  access: [...],
});
```

Configuração:

- target `node20`;
- platform `node`;
- formato ESM compatível com o projeto;
- sourcemap externo;
- minificação desabilitada inicialmente para stack traces auditáveis;
- arquivo de lock explícito;
- bundle independente por função;
- AWS SDK v3 incluído no bundle para versão reproduzível.

Não confiaria na versão do SDK fornecida pela runtime Lambda.

Testes de infra devem falhar se qualquer função continuar usando inline code ou contiver o corpo 501.

## 7. SQS e DLQ

### Recursos

```text
ExpirationTracker-ReminderDispatch
ExpirationTracker-ReminderDispatch-DLQ
```

Os nomes físicos podem receber stage, por exemplo:

```text
expiration-tracker-dev-reminder-dispatch
expiration-tracker-dev-reminder-dispatch-dlq
```

Configuração proposta:

| Propriedade | Queue | DLQ |
|---|---:|---:|
| Tipo | Standard | Standard |
| Retenção | 4 dias | 14 dias |
| Long polling | 20 s | n/a |
| `maxReceiveCount` | 5 | n/a |
| Criptografia | SQS managed ou KMS do ambiente | igual |
| Visibility timeout | ≥ 6 × timeout da Lambda | n/a |

A fila deve ser Standard, pois OCC e idempotência já são necessários independentemente da ordenação; FIFO adicionaria throughput, group-id e dedup-window sem eliminar a necessidade de idempotência durável.

### Event source mapping

- batch size inicial: `10`;
- batching window: `0–1 s`;
- `reportBatchItemFailures: true`;
- reserved concurrency da função: `10`;
- maximum concurrency do event source mapping: `10`;
- `bisectBatchOnError` não se aplica a SQS;
- payload schema validado item a item.

Resposta:

```ts
return {
  batchItemFailures: failures.map(({ messageId }) => ({
    itemIdentifier: messageId,
  })),
};
```

Classificação:

- sucesso, duplicata ou estado já avançado → ACK;
- erro retryable → incluir em `batchItemFailures`;
- poison message/schema inválido → terminal, mas precisa chegar à DLQ.

Para poison message, há duas políticas válidas. Eu recomendo retornar falha até `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observável. Para evitar custo inútil, registrar a primeira detecção e emitir métrica por receive count. Não deletar manualmente nem enviar diretamente à DLQ.

O relay do outbox também deve usar partial batch failure no DynamoDB Stream. Um registro só é marcado publicado depois de `SendMessage` confirmado; duplicação entre envio e marcação é esperada e absorvida pelo dispatch idempotente.

## 8. EventBridge

Escolho **EventBridge Rule legado com `ScheduleExpression`**, não EventBridge Scheduler, para estas cadências fixas.

Motivos:

- já é a tecnologia especificada no escopo;
- `rate(1 minute)` é simples e suficiente;
- integração direta com Lambda e retry policy madura;
- não há milhões de schedules por tenant;
- flexible time window não existe em Scheduled Rule e não é necessária para o producer.

Regras:

```text
ReminderProducerSchedule:
  rate(1 minute)

ReminderClaimReconciliationSchedule:
  rate(1 minute) ou rate(5 minutes)

ReminderDstReconciliationSchedule:
  cron diário em UTC
```

O evento deve carregar:

```json
{
  "mode": "PRODUCE"
}
```

ou:

```json
{
  "mode": "CLAIMS"
}
```

ou:

```json
{
  "mode": "DST"
}
```

Reserved concurrency:

- producer: `2`;
- reconciliation: `1`;
- dispatch: `10`.

Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contínuas precisam de alarme por invocações com erro e por scheduler lag.

### Por que não Scheduler

EventBridge Scheduler seria preferível se houvesse schedules individuais, timezone por schedule, execução única ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.

Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliação diária, uma janela flexível seria aceitável, mas não justifica misturar dois mecanismos.

## 9. Testes

### Camada 1 — testes rápidos

Continuam executando em todo PR:

- unitários do domínio;
- contratos de schema;
- CDK assertions;
- teste do bundle de cada entrypoint;
- teste que proíbe `fromInline` nos handlers de produção;
- teste estrutural que assegura que somente producer tem ARN de GSI3.

### Camada 2 — integração em containers

Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade específica.

DynamoDB Local, sozinho, não testa:

- redrive SQS;
- DLQ;
- event source mapping;
- EventBridge;
- interação DynamoDB → relay → SQS.

LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suíte, portas dinâmicas e cleanup previsível.

Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.

Casos obrigatórios:

1. Condição de OCC concorrente: apenas um writer vence.
2. Claim + dispatch outbox são atômicos.
3. Falha do relay preserva o registro para retry.
4. Duas entregas SQS criam um único `NotificationIntent`.
5. Falha em um item do batch não repete os itens bem-sucedidos.
6. Poison message chega à DLQ após cinco receives.
7. Claim expirado é recuperado e volta a produzir trabalho.
8. Renew/cancel concorrente faz dispatch cancelar por versão obsoleta.
9. Query GSI3 encontra múltiplos tenants, mas o producer nunca mistura o contexto reconstruído.
10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.

Para timeout real de dependência, usaria um proxy de rede controlável, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexão acima do timeout configurado e verifica:

- erro classificado como retryable;
- mensagem não reconhecida como sucesso;
- retry/redrive;
- nenhuma transação parcial.

Isso testa falha de transporte real, não um `mockRejectedValue`.

### Camada 3 — sandbox AWS efêmero

LocalStack não prova semântica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 não deve ser aprovado somente com containers.

Criaria uma suíte curta contra uma stack AWS efêmera:

- deploy em conta/região de teste;
- TTL máximo da stack e tags de cleanup;
- seed de poucos itens;
- invocação do producer;
- observação da mensagem e do dispatch;
- poison message até DLQ;
- teste negativo assumindo a role de uma Lambda tenant-facing e tentando `Query` em GSI3;
- validação de schedule por invocação manual com o mesmo target, sem esperar o relógio;
- destruição da stack ao final.

Cadência:

- containers em todo PR;
- sandbox AWS no merge para `develop` ou workflow manual obrigatório do milestone;
- teste completo de schedule real em workflow noturno, pois esperar ticks torna PRs lentos e sujeitos a jitter.

O artefato da execução AWS — CloudWatch request IDs, métricas, ARN da stack e resultados — deve ser preservado como evidência do gate.

## 10. Fault injection exigida

| Falha | Injeção | Evidência esperada |
|---|---|---|
| Timeout DynamoDB/SQS | Toxiproxy interrompe conexão | erro retryable, retry, sem estado parcial |
| Queda após claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
| Queda após `SendMessage` | falhar antes de marcar outbox | possível duplicata, um único efeito lógico |
| Poison message | body inválido real em SQS | cinco receives e mensagem na DLQ |
| Falha parcial de batch | uma mensagem inválida entre válidas | somente inválida retorna em `batchItemFailures` |
| Renew concorrente | atualizar versão após claim | dispatch cancela stale, sem intent incorreto |
| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
| Duplo producer | duas invocações simultâneas | um claim vence por condição |
| Tentativa GSI3 não autorizada | role real tenant-facing | `AccessDenied`, nenhum dado retornado |

## 11. Critérios de saída do milestone

O milestone só pode alegar fechamento operacional de G8 quando houver:

- zero handlers 501;
- bundles reais sintetizados;
- adapters reais cobrindo todos os ports;
- reconciliação alimentada por consultas reais, não arrays vazios;
- claim e publicação conectados por mecanismo durável;
- fila, DLQ e redrive sintetizados e exercitados;
- partial batch failure comprovado;
- timeout, poison message e redrive exercitados;
- teste AWS negativo de GSI3 com role real;
- teste concorrente renew/cancel/dispatch;
- evidência registrada da execução em sandbox AWS.

## 12. O que este milestone não resolve

“G8 fechado” não significa que os cinco P1 desapareceram integralmente.

- **P1 #1 — recuperação assíncrona:** fecha o trecho producer → dispatch se houver outbox/relay e reconciliação consultável. Não fecha pipelines futuros de Notification.
- **P1 #2 — idempotência no efeito externo:** não pode ser provada ainda. `NotificationIntent` é apenas uma intenção durável; M4 deverá provar idempotência no limite do provider de e-mail/WhatsApp, inclusive timeout após o provider aceitar a solicitação e antes de responder.
- **P1 #3 — false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. Não desaparece até testes AWS reais de M4 e operação continuada.
- **P1 #4 — concorrência/estado obsoleto:** o milestone pode provar OCC e revalidação entre renew/cancel/materialização/dispatch. Não prova todas as corridas com envio externo, callbacks e correções futuras de M4.
- **P1 #5 — blast radius do GSI3:** um teste IAM real prova que roles tenant-facing não consultam o índice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa função ainda têm blast radius global. São necessários alarmes, logs sem PII, limites de batch e revisão específica desse handler.

Também não resolve carga extrema, restore real, região/LGPD, provider de notificação, BFF de sessão nem aprovação global da arquitetura operacional.

## Nota e riscos

**Confiança: 8,8/10.**

Principais riscos:

1. O port atual da reconciliação é insuficiente para um handler autônomo; o GSI6 precisa ser concretamente consultável.
2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS é indispensável.
4. O relay adiciona uma função e semântica de publicação que precisam de observabilidade e retenção/cleanup do outbox.
5. A idempotência externa continuará não comprovada até M4; qualquer declaração mais ampla de encerramento dos cinco P1 seria enganosa.
tokens used
38.889
# Proposta técnica — milestone Runtime Real / G8

## 1. Decisão central

O milestone deve transformar o fluxo atual em runtime implantável sem contaminar os ports com tipos do AWS SDK:

```text
EventBridge Rule
      │
      ▼
ReminderProducer Lambda
      │
      ├─ TransactWrite:
      │    SCHEDULED → CLAIMED
      │    + registro durável DispatchOutbox
      ▼
DynamoDB Stream / relay
      │
      ▼
ReminderDispatchQueue
      │
      ▼
ReminderDispatch Lambda
      │
      ├─ revalida estado/versionamento
      └─ TransactWrite:
           CLAIMED → TRIGGERED
           + NotificationIntent
           + OutboxEvent
           + IdempotencyRecord
```

A gravação durável intermediária é necessária. Fazer `TransactWrite(CLAIMED)` e depois `SendMessage` diretamente no handler mantém uma janela de perda:

1. DynamoDB confirma o claim.
2. Lambda cai antes de enviar à SQS.
3. A ocorrência fica `CLAIMED`, mas não existe mensagem.

A reconciliação diária não fecha adequadamente essa janela: quando reverter o claim, a ocorrência poderá estar fora do lookback de cinco minutos do producer. Portanto, fila e DLQ, isoladamente, não bastam para fechar o P1 #1.

Minha recomendação é um registro `DispatchOutbox` gravado atomicamente com o claim e publicado por DynamoDB Streams. Uma alternativa aceitável é EventBridge Pipes entre o stream e a SQS, desde que seja demonstrado que filtragem, transformação do stream record e retries funcionam no ambiente AWS real. Eu prefiro um pequeno `DispatchOutboxRelay` Lambda porque oferece controle explícito de schema, observabilidade, partial batch failure e evolução do contrato.

## 2. Dependências

Adicionar como dependências de runtime:

```json
{
  "@aws-sdk/client-dynamodb": "versão exata",
  "@aws-sdk/lib-dynamodb": "versão exata",
  "@aws-sdk/client-sqs": "versão exata"
}
```

E como dependências de desenvolvimento:

```json
{
  "esbuild": "versão exata",
  "@types/aws-lambda": "versão exata",
  "testcontainers": "versão exata"
}
```

Escolhas:

- `DynamoDBClient`: transporte, configuração de retry e endpoint.
- `DynamoDBDocumentClient`: objetos JavaScript, compatível com os builders existentes.
- `SQSClient`: publicação explícita pelo relay.
- `esbuild`: bundling determinístico dos entrypoints ESM para Node.js 20.
- Versões exatas, não ranges, mantendo lockfile e `npm ci`.

Configuração do `DynamoDBDocumentClient`:

```ts
DynamoDBDocumentClient.from(client, {
  marshallOptions: {
    removeUndefinedValues: true,
    convertClassInstanceToMap: false,
  },
})
```

Não habilitaria conversões numéricas silenciosas. Campos de versão precisam continuar sendo números e valores grandes devem ser avaliados explicitamente antes de usar `wrapNumbers`.

O cliente deve usar retry adaptativo ou standard com número pequeno e explícito de tentativas, por exemplo três. O timeout total precisa caber com folga no timeout da Lambda; não se deve deixar o SDK consumir todo o orçamento da invocação.

## 3. Estrutura dos adapters

```text
src/
  runtime/
    aws/
      dynamodb/
        client.ts
        errors.ts
        execute-transaction.ts
        identity-store-dynamodb.ts
        expiration-store-dynamodb.ts
        reminder-store-dynamodb.ts
        reminder-producer-store-dynamodb.ts
        reconciliation-candidate-source-dynamodb.ts
      sqs/
        dispatch-command-publisher.ts

    handlers/
      test-ping-handler.ts
      items-handler.ts
      reminders-handler.ts
      reminder-producer-handler.ts
      reminder-dispatch-handler.ts
      reminder-reconciliation-handler.ts
      dispatch-outbox-relay-handler.ts

    composition/
      identity.ts
      expiration.ts
      reminder.ts
```

Um adapter por port:

- `DynamoDbIdentityStore implements IdentityStore`
- `DynamoDbExpirationStore implements ExpirationStore`
- `DynamoDbReminderStore implements ReminderStore`
- `DynamoDbReminderProducerStore implements ReminderProducerStore`

Não criaria uma classe “universal” pública implementando todos os ports. Isso permitiria injetar acidentalmente acesso ao GSI3 em código tenant-facing.

Pode haver helpers internos compartilhados para `GetCommand`, `PutCommand`, `UpdateCommand` e `TransactWriteCommand`, mas o método `queryGsi3` existe somente em `reminder-producer-store-dynamodb.ts`.

### Mapeamento das operações

| Port | Comando AWS |
|---|---|
| `get` | `GetCommand`, `ConsistentRead: true` |
| `putIfAbsent` | `PutCommand` com condição existente; retorna `false` apenas para falha condicional |
| `update` | `PutCommand` de overwrite, restrito aos usos já documentados pelo port |
| `transactWrite` | `TransactWriteCommand` usando diretamente os builders existentes |
| `queryGsi1` | `QueryCommand`, `IndexName: GSI1`, eventual consistency |
| `queryByItem` | `QueryCommand` na tabela base, `PK = :pk AND begins_with(SK, :occ)` e leitura consistente |
| `queryGsi3` | `QueryCommand`, `IndexName: GSI3`, somente no adapter privilegiado |

Paginação deve ser completa por padrão. `limit` representa limite lógico total, não apenas o `Limit` de uma única página.

### Tradução de erros

O adapter deve traduzuzir, sem esconder a causa:

- `ConditionalCheckFailedException` em `putIfAbsent` → `false`.
- `TransactionCanceledException` → erro reconhecível por `isTransactionCanceled`.
- throttling, timeout, falha 5xx e indisponibilidade → `AppError` retryable.
- validation error, item/schema inválido → erro terminal.
- `AccessDeniedException` → erro terminal operacional e alarme; não deve virar retry infinito.

Preservar `$metadata.requestId`, nome da operação e causa no log, sem registrar item completo nem dados pessoais.

## 4. Lacuna da reconciliação

O handler de reconciliação atual não consegue operar sozinho: `runReconciliation` recebe arrays de candidatos, mas `ReminderStore` não oferece uma consulta que os produza.

Executar a Lambda diariamente com arrays vazios seria outro false-green.

O milestone precisa introduzir um port estreito, por exemplo:

```ts
interface ReconciliationCandidateSource {
  listExpiredClaims(input: PageInput): AsyncIterable<ReminderOccurrence>;
  listDstCandidates(input: ReconciliationWindow): AsyncIterable<DstReconciliationCandidate>;
}
```

O adapter consultaria o GSI6 ou o índice efetivamente normativo. Isso é integração mínima necessária, não redesenho do domínio.

Se o GSI6 ainda não representa claims expirados e políticas ativas de maneira consultável, essa modelagem precisa ser fechada antes de declarar o handler real. Sem isso, o item 4 do escopo está apenas agendado, não funcional.

Além da execução diária de DST, recuperação de claims precisa ter cadência curta. Recomendo:

- reconciliação de claims expirados: a cada minuto ou cinco minutos;
- reconciliação DST/materialização: diária.

Podem chamar o mesmo handler com um campo `mode`, preservando concurrency reservada 1. Mesmo com o outbox durável, a reconciliação curta continua útil para reparar estados excepcionais.

## 5. Handlers Lambda

Cada arquivo em `src/runtime/handlers` é um composition root:

1. valida variáveis de ambiente;
2. instancia clientes e adapters fora do handler para reuso em warm starts;
3. converte o evento AWS para os tipos internos;
4. invoca a lógica existente;
5. converte resultado/erro para o protocolo da origem;
6. registra métricas e logs via `SecureLogger`.

Nenhuma lógica de negócio deve morar no wrapper Lambda.

Variáveis mínimas:

```text
TABLE_NAME
DISPATCH_QUEUE_URL
SHARD_CONFIG
PRODUCER_LOOKBACK_MINUTES
CLAIM_TTL_SECONDS
LOG_LEVEL
```

Para HTTP, os handlers reais devem interpretar API Gateway HTTP API v2, extrair claims já validadas pelo authorizer, construir `RequestContext` e mapear `AppError` para status HTTP consistente.

Para schedules, validar `source`, `detail-type` e um campo explícito `mode`. Evento inesperado é terminal.

Para SQS, validar cada body contra `schemas/queues/reminder.dispatch.v1.json` antes de executar o domínio.

## 6. Bundling

Eu estenderia `ScopedLambdaFunction` com `entry` e `handlerExport`, mantendo-o como único ponto de criação de Lambda. Internamente, usaria `lambda.Code.fromAsset` com bundling `esbuild`, ou incorporaria a semântica de `NodejsFunction` sem permitir que stacks criem Lambdas por fora do construct.

Exemplo conceitual:

```ts
new ScopedLambdaFunction(this, "ReminderDispatch", {
  entry: "src/runtime/handlers/reminder-dispatch-handler.ts",
  handlerExport: "handler",
  runtime: lambda.Runtime.NODEJS_20_X,
  access: [...],
});
```

Configuração:

- target `node20`;
- platform `node`;
- formato ESM compatível com o projeto;
- sourcemap externo;
- minificação desabilitada inicialmente para stack traces auditáveis;
- arquivo de lock explícito;
- bundle independente por função;
- AWS SDK v3 incluído no bundle para versão reproduzível.

Não confiaria na versão do SDK fornecida pela runtime Lambda.

Testes de infra devem falhar se qualquer função continuar usando inline code ou contiver o corpo 501.

## 7. SQS e DLQ

### Recursos

```text
ExpirationTracker-ReminderDispatch
ExpirationTracker-ReminderDispatch-DLQ
```

Os nomes físicos podem receber stage, por exemplo:

```text
expiration-tracker-dev-reminder-dispatch
expiration-tracker-dev-reminder-dispatch-dlq
```

Configuração proposta:

| Propriedade | Queue | DLQ |
|---|---:|---:|
| Tipo | Standard | Standard |
| Retenção | 4 dias | 14 dias |
| Long polling | 20 s | n/a |
| `maxReceiveCount` | 5 | n/a |
| Criptografia | SQS managed ou KMS do ambiente | igual |
| Visibility timeout | ≥ 6 × timeout da Lambda | n/a |

A fila deve ser Standard, pois OCC e idempotência já são necessários independentemente da ordenação; FIFO adicionaria throughput, group-id e dedup-window sem eliminar a necessidade de idempotência durável.

### Event source mapping

- batch size inicial: `10`;
- batching window: `0–1 s`;
- `reportBatchItemFailures: true`;
- reserved concurrency da função: `10`;
- maximum concurrency do event source mapping: `10`;
- `bisectBatchOnError` não se aplica a SQS;
- payload schema validado item a item.

Resposta:

```ts
return {
  batchItemFailures: failures.map(({ messageId }) => ({
    itemIdentifier: messageId,
  })),
};
```

Classificação:

- sucesso, duplicata ou estado já avançado → ACK;
- erro retryable → incluir em `batchItemFailures`;
- poison message/schema inválido → terminal, mas precisa chegar à DLQ.

Para poison message, há duas políticas válidas. Eu recomendo retornar falha até `maxReceiveCount=5`, garantindo que o redrive nativo seja exercitado e observável. Para evitar custo inútil, registrar a primeira detecção e emitir métrica por receive count. Não deletar manualmente nem enviar diretamente à DLQ.

O relay do outbox também deve usar partial batch failure no DynamoDB Stream. Um registro só é marcado publicado depois de `SendMessage` confirmado; duplicação entre envio e marcação é esperada e absorvida pelo dispatch idempotente.

## 8. EventBridge

Escolho **EventBridge Rule legado com `ScheduleExpression`**, não EventBridge Scheduler, para estas cadências fixas.

Motivos:

- já é a tecnologia especificada no escopo;
- `rate(1 minute)` é simples e suficiente;
- integração direta com Lambda e retry policy madura;
- não há milhões de schedules por tenant;
- flexible time window não existe em Scheduled Rule e não é necessária para o producer.

Regras:

```text
ReminderProducerSchedule:
  rate(1 minute)

ReminderClaimReconciliationSchedule:
  rate(1 minute) ou rate(5 minutes)

ReminderDstReconciliationSchedule:
  cron diário em UTC
```

O evento deve carregar:

```json
{
  "mode": "PRODUCE"
}
```

ou:

```json
{
  "mode": "CLAIMS"
}
```

ou:

```json
{
  "mode": "DST"
}
```

Reserved concurrency:

- producer: `2`;
- reconciliation: `1`;
- dispatch: `10`.

Para o producer, configurar `retryAttempts` e `maxEventAge` pequenos, pois o lookback cobre ticks perdidos. Ainda assim, falhas contínuas precisam de alarme por invocações com erro e por scheduler lag.

### Por que não Scheduler

EventBridge Scheduler seria preferível se houvesse schedules individuais, timezone por schedule, execução única ou flexible time window. Aqui ele acrescenta IAM e um recurso adicional sem resolver um requisito real.

Se Scheduler fosse escolhido, o producer teria `FlexibleTimeWindow: OFF`: atrasar arbitrariamente um tick de minuto aumenta lag. Para a reconciliação diária, uma janela flexível seria aceitável, mas não justifica misturar dois mecanismos.

## 9. Testes

### Camada 1 — testes rápidos

Continuam executando em todo PR:

- unitários do domínio;
- contratos de schema;
- CDK assertions;
- teste do bundle de cada entrypoint;
- teste que proíbe `fromInline` nos handlers de produção;
- teste estrutural que assegura que somente producer tem ARN de GSI3.

### Camada 2 — integração em containers

Escolha: **Testcontainers com LocalStack fixado por digest**, complementado por DynamoDB Local somente se houver incompatibilidade específica.

DynamoDB Local, sozinho, não testa:

- redrive SQS;
- DLQ;
- event source mapping;
- EventBridge;
- interação DynamoDB → relay → SQS.

LocalStack cobre melhor o sistema deste milestone. Testcontainers fornece isolamento por suíte, portas dinâmicas e cleanup previsível.

Esses testes devem usar os adapters reais e clientes AWS SDK reais, nunca mocks de comandos.

Casos obrigatórios:

1. Condição de OCC concorrente: apenas um writer vence.
2. Claim + dispatch outbox são atômicos.
3. Falha do relay preserva o registro para retry.
4. Duas entregas SQS criam um único `NotificationIntent`.
5. Falha em um item do batch não repete os itens bem-sucedidos.
6. Poison message chega à DLQ após cinco receives.
7. Claim expirado é recuperado e volta a produzir trabalho.
8. Renew/cancel concorrente faz dispatch cancelar por versão obsoleta.
9. Query GSI3 encontra múltiplos tenants, mas o producer nunca mistura o contexto reconstruído.
10. Credencial/role tenant-facing recebe `AccessDenied` ao consultar GSI3.

Para timeout real de dependência, usaria um proxy de rede controlável, como Toxiproxy em container, entre o SDK e o endpoint LocalStack. O teste corta ou atrasa a conexão acima do timeout configurado e verifica:

- erro classificado como retryable;
- mensagem não reconhecida como sucesso;
- retry/redrive;
- nenhuma transação parcial.

Isso testa falha de transporte real, não um `mockRejectedValue`.

### Camada 3 — sandbox AWS efêmero

LocalStack não prova semântica completa de IAM, Lambda event source mapping, EventBridge nem redrive gerenciado. Portanto, G8 não deve ser aprovado somente com containers.

Criaria uma suíte curta contra uma stack AWS efêmera:

- deploy em conta/região de teste;
- TTL máximo da stack e tags de cleanup;
- seed de poucos itens;
- invocação do producer;
- observação da mensagem e do dispatch;
- poison message até DLQ;
- teste negativo assumindo a role de uma Lambda tenant-facing e tentando `Query` em GSI3;
- validação de schedule por invocação manual com o mesmo target, sem esperar o relógio;
- destruição da stack ao final.

Cadência:

- containers em todo PR;
- sandbox AWS no merge para `develop` ou workflow manual obrigatório do milestone;
- teste completo de schedule real em workflow noturno, pois esperar ticks torna PRs lentos e sujeitos a jitter.

O artefato da execução AWS — CloudWatch request IDs, métricas, ARN da stack e resultados — deve ser preservado como evidência do gate.

## 10. Fault injection exigida

| Falha | Injeção | Evidência esperada |
|---|---|---|
| Timeout DynamoDB/SQS | Toxiproxy interrompe conexão | erro retryable, retry, sem estado parcial |
| Queda após claim | falha do relay depois do commit | outbox reaparece e mensagem acaba publicada |
| Queda após `SendMessage` | falhar antes de marcar outbox | possível duplicata, um único efeito lógico |
| Poison message | body inválido real em SQS | cinco receives e mensagem na DLQ |
| Falha parcial de batch | uma mensagem inválida entre válidas | somente inválida retorna em `batchItemFailures` |
| Renew concorrente | atualizar versão após claim | dispatch cancela stale, sem intent incorreto |
| Cancel concorrente | cancelar antes do dispatch | nenhum intent ativo criado |
| Duplo producer | duas invocações simultâneas | um claim vence por condição |
| Tentativa GSI3 não autorizada | role real tenant-facing | `AccessDenied`, nenhum dado retornado |

## 11. Critérios de saída do milestone

O milestone só pode alegar fechamento operacional de G8 quando houver:

- zero handlers 501;
- bundles reais sintetizados;
- adapters reais cobrindo todos os ports;
- reconciliação alimentada por consultas reais, não arrays vazios;
- claim e publicação conectados por mecanismo durável;
- fila, DLQ e redrive sintetizados e exercitados;
- partial batch failure comprovado;
- timeout, poison message e redrive exercitados;
- teste AWS negativo de GSI3 com role real;
- teste concorrente renew/cancel/dispatch;
- evidência registrada da execução em sandbox AWS.

## 12. O que este milestone não resolve

“G8 fechado” não significa que os cinco P1 desapareceram integralmente.

- **P1 #1 — recuperação assíncrona:** fecha o trecho producer → dispatch se houver outbox/relay e reconciliação consultável. Não fecha pipelines futuros de Notification.
- **P1 #2 — idempotência no efeito externo:** não pode ser provada ainda. `NotificationIntent` é apenas uma intenção durável; M4 deverá provar idempotência no limite do provider de e-mail/WhatsApp, inclusive timeout após o provider aceitar a solicitação e antes de responder.
- **P1 #3 — false-green:** reduzido para adapters, handlers, SQS e schedules cobertos. Não desaparece até testes AWS reais de M4 e operação continuada.
- **P1 #4 — concorrência/estado obsoleto:** o milestone pode provar OCC e revalidação entre renew/cancel/materialização/dispatch. Não prova todas as corridas com envio externo, callbacks e correções futuras de M4.
- **P1 #5 — blast radius do GSI3:** um teste IAM real prova que roles tenant-facing não consultam o índice. O producer continua sendo um componente privilegiado com visibilidade cross-tenant por desenho; bugs ou comprometimento nessa função ainda têm blast radius global. São necessários alarmes, logs sem PII, limites de batch e revisão específica desse handler.

Também não resolve carga extrema, restore real, região/LGPD, provider de notificação, BFF de sessão nem aprovação global da arquitetura operacional.

## Nota e riscos

**Confiança: 8,8/10.**

Principais riscos:

1. O port atual da reconciliação é insuficiente para um handler autônomo; o GSI6 precisa ser concretamente consultável.
2. Implementar claim seguido de `SendMessage` sem outbox manteria exatamente a falha que o milestone pretende fechar.
3. LocalStack pode divergir de IAM, Streams, EventBridge e event source mappings; por isso o teste em sandbox AWS é indispensável.
4. O relay adiciona uma função e semântica de publicação que precisam de observabilidade e retenção/cleanup do outbox.
5. A idempotência externa continuará não comprovada até M4; qualquer declaração mais ampla de encerramento dos cinco P1 seria enganosa.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "interface (ReminderStore|ReminderProducerStore|ExpirationStore|IdentityStore)|GSI6|OutboxEvent|RECON#OUTBOX|outbox" src docs/architecture/data-model.md docs/architecture/implementation-blueprint.md schemas | Select-Object -First 240' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 422ms:
docs/architecture/implementation-blueprint.md:30:5. Operações críticas que alteram estado e exigem publicação usam outbox transacional.
docs/architecture/implementation-blueprint.md:93:    outbox/
docs/architecture/implementation-blueprint.md:99:    outbox-publisher/
docs/architecture/implementation-blueprint.md:161:| `OutboxPublisher` | DynamoDB Streams + agendamento | Publicar outbox no EventBridge e confirmar publicação |
docs/architecture/implementation-blueprint.md:178:| `RetentionReconciliationWorker` | EventBridge agendado | Processar GSI6 para retenção, purga e pendências |
docs/architecture/implementation-blueprint.md:354:  "entityType": "OutboxEvent",
docs/architecture/implementation-blueprint.md:366:  "GSI6PK": "RECON#OUTBOX#PENDING",
docs/architecture/implementation-blueprint.md:367:  "GSI6SK": "2026-08-19T14:03:22.481Z#evt_01"
docs/architecture/implementation-blueprint.md:373:1. recebe stream ou busca pendências em GSI6;
docs/architecture/implementation-blueprint.md:381:O sweeper periódico consulta GSI6 e cobre eventos perdidos após a retenção do Streams.
docs/architecture/implementation-blueprint.md:532:- renovação: transação entre item, idempotência, auditoria/outbox quando aplicável.
docs/architecture/implementation-blueprint.md:541:4. em `TransactWriteItems`, atualiza item e cria outbox `ItemDueDateChanged`;
docs/architecture/implementation-blueprint.md:681:Se válido, transação atômica (`TransactWriteItems`, corrige a lacuna de durabilidade apontada na crítica cruzada — antes o evento `ReminderTriggered` era tratado como "reconstruível" sem outbox, o que podia perder ou duplicar o disparo):
docs/architecture/implementation-blueprint.md:686:- cria outbox `NotificationIntentCreated` na mesma transação.
docs/architecture/implementation-blueprint.md:929:  "GSI6PK": "RECON#WEBHOOK#PENDING",
docs/architecture/implementation-blueprint.md:930:  "GSI6SK": "2026-09-10T12:01:02Z#provider_event_987"
docs/architecture/implementation-blueprint.md:1145:5. se alterar vencimento, cria outbox `ItemDueDateChanged`.
docs/architecture/implementation-blueprint.md:1154:- purga física posterior usa GSI6;
docs/architecture/implementation-blueprint.md:1213:**Correção de convergência (Rodada 3)**: "consumidor único, alarme direto, sem DLQ" não é estratégia de recuperação — alarmar não recupera eventos perdidos após o período de retry do EventBridge. O `audit-consumer` tem DLQ dedicada (mensagens de auditoria nunca são descartadas silenciosamente), com: retry documentado, redrive controlado (mesmo runbook de DLQ das demais filas, §22.2), e reconciliação periódica que compara a contagem/hash de eventos de domínio críticos publicados no outbox contra os `AuditEvent` correspondentes gravados, alertando divergência.
docs/architecture/implementation-blueprint.md:1390:    tableAccess.create("OutboxEvent"),
docs/architecture/implementation-blueprint.md:1456:(API monolith, outbox, reminder, notification, webhook)
docs/architecture/implementation-blueprint.md:1484:12. habilitação gradual: outbox → webhook → reminder → notification → documento.
docs/architecture/implementation-blueprint.md:1513:- outbox;
docs/architecture/implementation-blueprint.md:1545:- `ItemDueDateChanged` por outbox.
docs/architecture/implementation-blueprint.md:1563:Depende de M2 e outbox de M0.
docs/architecture/implementation-blueprint.md:1613:Depende de M5 e outbox/M2.
docs/architecture/implementation-blueprint.md:1655:- operações críticas usam outbox na mesma transação;
docs/architecture/implementation-blueprint.md:1656:- sweeper recupera outbox após simulação de perda do Stream.
docs/architecture/implementation-blueprint.md:1841:| `OutboxPublisher` | DynamoDB Streams + agendamento (sweeper) | transversal (outbox) | 5 | sem DLQ — falha mantém `PENDING`, sweeper reenfileira além da retenção de 24h do Streams |
docs/architecture/implementation-blueprint.md:1858:| `RetentionReconciliationWorker` | EventBridge agendado | transversal (via DAL) | 1 | sem DLQ (idempotente, consulta GSI6) |
src\workers\reminder-reconciliation\reconciliation.ts:36: * from a GSI6-style "active reminder policies" index (left as infra follow-up, see report
src\workers\reminder-dispatch\dispatch.ts:14: * durabilidade... antes o evento era tratado como reconstruível sem outbox"):
src\workers\reminder-dispatch\dispatch.ts:17: * outbox `notification.intent-created.v1` Put.
src\workers\reminder-dispatch\dispatch.ts:20:import { appendToTransaction } from "../../shared/outbox/outbox.js";
src\modules\reminder\ports\reminder-store.ts:27:export interface ReminderStore {
src\modules\reminder\ports\reminder-store.ts:41:export interface ReminderProducerStore {
schemas\events\item-due-date-changed.v1.json:5:  "description": "Critical domain event published via the transactional outbox (implementation-blueprint.md #5.3, #8.3) when an ExpirationItem due date changes.",
src\modules\reminder\application\reminder-policy-service.ts:7: * needed for plain CRUD (no critical outbox event is fired by a policy edit itself -
src\modules\reminder\application\reminder-policy-service.ts:9: * like the item aggregate's own dueDate-change -> outbox -> async materialize pipeline).
src\modules\reminder\application\reminder-materializer.ts:199:   * cancelled; callers (the outbox consumer / M3's due-date-change worker) then call
src\modules\expiration\ports\expiration-store.ts:6: * data-model.md §5's load-bearing sentence requires item update + outbox record (and,
src\modules\expiration\ports\expiration-store.ts:8: * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
src\modules\expiration\ports\expiration-store.ts:24:export interface ExpirationStore {
src\modules\expiration\domain\audit-event.ts:4: * item + its transactional Put entry (mirrors src/shared/outbox/outbox.ts's shape) so
src\modules\expiration\domain\audit-event.ts:80:/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction. */
src\modules\expiration\application\expiration-service.ts:3: * transaction, data-model.md §5's load-bearing OCC/outbox sentence. Every mutation:
src\modules\expiration\application\expiration-service.ts:6: *  3. builds its write(s) via shared/dynamodb/occ.ts + shared/outbox/outbox.ts builders;
src\modules\expiration\application\expiration-service.ts:7: *  4. commits the aggregate write + AuditEvent (+ outbox record, when the mutation
src\modules\expiration\application\expiration-service.ts:10: *     outbox crítico numa única transação."
src\modules\expiration\application\expiration-service.ts:19:import { appendToTransaction } from "../../../shared/outbox/outbox.js";
src\shared\outbox\outbox.ts:2: * Transactional outbox helper - implementation-blueprint.md #5.1 (OutboxPort) and #5.3
src\shared\outbox\outbox.ts:3: * (record shape + OutboxPublisher contract). Builds the outbox item + its
src\shared\outbox\outbox.ts:15:  entityType: "OutboxEvent";
src\shared\outbox\outbox.ts:27:  GSI6PK: string;
src\shared\outbox\outbox.ts:28:  GSI6SK: string;
src\shared\outbox\outbox.ts:31:/** Monthly shard for the outbox partition, matching #5.3's `TENANT#t#OUTBOX#202608` example. */
src\shared\outbox\outbox.ts:41:    entityType: "OutboxEvent",
src\shared\outbox\outbox.ts:53:    GSI6PK: "RECON#OUTBOX#PENDING",
src\shared\outbox\outbox.ts:54:    GSI6SK: `${event.occurredAt}#${event.eventId}`,
src\shared\outbox\outbox.ts:67: * OutboxPort.append from implementation-blueprint.md #5.1: appends the outbox Put to a
src\modules\identity\ports\identity-store.ts:13:export interface IdentityStore {
docs/architecture/data-model.md:11:- **Rodada 1** — propostas independentes. Convergência muito forte em entidades, idempotência, versionamento otimista e retenção. A proposta do Codex era estruturalmente mais refinada em 5 pontos, adotados aqui: (1) convenção de PK que co-localiza `ReminderOccurrence`/`Document` sob a partição do `ExpirationItem` pai (permite buscar item + suas ocorrências/documentos numa única query, em vez de exigir GSI); (2) `UploadSlot` como entidade de primeira classe, não atributo embutido; (3) `WebhookInbox` com PK `provider+account` e SK `providerEventId` (permite listar todos os eventos de um provider/conta, não só checar existência); (4) GSI consolidado de retenção/reconciliação (GSI6) cobrindo purge, upload slots vencidos e jobs pendentes num único índice em vez de vários; (5) distinção explícita entre leitura fortemente consistente (PK base, para autorização/edição/pré-envio) e eventualmente consistente (GSIs, aceitável para dashboards).
docs/architecture/data-model.md:51:- **GSI6 — retenção/reconciliação**: `PK=TENANT#t#PURGE#yyyyMM` ou `...#WORKSTATE#<status>`, `SK=<purgeAfter/expiresAt>#<type>#<id>`. Suporta purge, upload slots vencidos, inbox/outbox e jobs pendentes num índice único. TTL é limpeza auxiliar, nunca gatilho operacional (evita depender de timing assíncrono do TTL para correção — mesma lição do reconciliador de upload da Fase 3 Rodada 6).
docs/architecture/data-model.md:66:Toda entidade mutável usa `version`; updates exigem `ConditionExpression: version=:expected` e incrementam atomicamente (optimistic concurrency control, decisão Fase 3 Rodada 6). Alterar vencimento grava o item, cancela ocorrências antigas e cria o evento de outbox crítico (`ItemDueDateChanged`) numa única transação (`TransactWriteItems`) — fecha a corrida identificada no Red Team cenário 13. Aplicação de resultado de extração verifica simultaneamente as versões de `Document` e `ExpirationItem`; divergência ou tombstone descarta o resultado (Red Team cenário 14).
docs/architecture/data-model.md:70:Leitura da PK base é fortemente consistente quando necessário (autorização, edição, pré-envio de notificação — a revalidação de versão do Red Team cenário 13 depende disso). GSIs são eventualmente consistentes: dashboards podem atrasar brevemente; nenhuma decisão crítica (cancelamento, disparo, aplicação de extração) depende exclusivamente de um GSI. Scheduler, inbox, upload e outbox toleram duplicação por condições/idempotência e são cobertos por reconciliação periódica (mesmo padrão em todos os casos).

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/reminder/ports/reminder-store.ts; Get-Content -Raw src/modules/expiration/ports/expiration-store.ts; Get-Content -Raw src/modules/identity/ports/identity-store.ts; Get-Content -Raw src/shared/outbox/outbox.ts; Get-Content docs/architecture/implementation-blueprint.md | Select-Object -Skip 340 -First 50' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 462ms:
/**
 * DynamoDB surface the Reminder module needs - same SDK-agnostic port pattern as
 * src/modules/expiration/ports/expiration-store.ts (AGENTS.md Â§6: "qualquer mÃ³dulo futuro
 * com a mesma necessidade deve reusar esse formato de port, nÃ£o inventar um novo"). This
 * IS that reuse: identical shape (get/putIfAbsent/update/transactWrite/queryGsi1-style),
 * plus one addition this module needs and Expiration didn't - `queryByItem`, to list an
 * item's existing ReminderOccurrence rows (co-located under the item's own PK per
 * data-model.md Â§2) for materialization diffing, due-date-change cancellation and DST
 * reconciliation.
 *
 * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
 * - implementation-blueprint.md Â§9.2's isolation safeguard ("nenhuma rota tenant-facing
 * tem permissÃ£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
 * the policy service, materializer and HTTP handlers - all tenant-facing) has NO method
 * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
 * ReminderProducer worker, exposes it. test/integration/gsi3-isolation.test.ts asserts
 * this structurally (no tenant-facing dependency graph can reach queryGsi3).
 */
// 2026-08-19 (Engineering Maturity Review): imports from shared/dynamodb/occ.ts directly
// rather than re-exporting through expiration/ports/expiration-store.js - the previous
// indirection was an accidental cross-module ports->ports dependency (this module doesn't
// actually need anything Expiration-specific here, just the generic DynamoDB shapes).
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface ReminderStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** All rows under an item's own partition whose SK begins with "OCC#" (ReminderOccurrence rows). Strongly consistent - base-partition read, not a GSI (data-model.md Â§5). */
  queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string): Promise<T[]>;
}

export interface Gsi3QueryInput {
  gsi3pk: string;
}

/** Narrow port, injected ONLY into the ReminderProducer worker (see file header). */
export interface ReminderProducerStore {
  queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]>;
  /** Strongly consistent read of the base item, used by the producer to reconstruct tenant context before claiming (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** Conditional SCHEDULED -> CLAIMED transition (implementation-blueprint.md Â§9.3 point 3). */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
}

/**
 * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
 * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
 * inject an implementation, so domain/application logic is unit-testable without live
 * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
 * data-model.md Â§5's load-bearing sentence requires item update + outbox record (and,
 * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
 * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
 * be individually visible without the others.
 */
import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

// Re-exported for backward compatibility with existing importers (application/http layers
// of this and other modules) - canonical definitions now live in shared/dynamodb/occ.ts.
export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";

export interface Gsi1QueryInput {
  gsi1pk: string;
  ascending?: boolean;
  limit?: number;
}

export interface ExpirationStore {
  /** Strongly consistent single-item read (data-model.md Â§5: authorization/edition/pre-send reads must be consistent). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  /** Unconditional overwrite of an existing item - used only for non-OCC bookkeeping records (e.g. idempotency status transitions), never for ExpirationItem/AuditEvent. */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * Commits every entry atomically. Throws an error recognized by
   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
   * assume partial application.
   */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** Eventually consistent GSI1 query (data-model.md Â§3: vencimentos/dashboard). */
  queryGsi1<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi1QueryInput): Promise<T[]>;
}

/**
 * Minimal DynamoDB surface the identity module needs, following the SDK-agnostic port
 * pattern established in src/shared/idempotency/idempotency.ts (M0): callers inject an
 * implementation of this port, so the module is unit-testable without live AWS. M1's
 * infra layer (infra/lib) provisions the real table; src/modules/identity/persistence
 * wires a DocumentClient-backed adapter to this same port for production use.
 */
export interface EntityKey {
  PK: string;
  SK: string;
}

export interface IdentityStore {
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  update<T extends EntityKey>(item: T): Promise<void>;
}

/**
 * Transactional outbox helper - implementation-blueprint.md #5.1 (OutboxPort) and #5.3
 * (record shape + OutboxPublisher contract). Builds the outbox item + its
 * TransactWriteItems `Put` entry so callers can append it atomically alongside the
 * aggregate write (e.g. ExpirationItem update + ItemDueDateChanged in one transaction,
 * per data-model.md Â§5).
 */
import type { DomainEvent } from "../contracts/events.js";

export type OutboxStatus = "PENDING" | "PUBLISHED";

export interface OutboxRecord {
  PK: string;
  SK: string;
  entityType: "OutboxEvent";
  eventId: string;
  eventType: string;
  aggregateType: string;
  aggregateId: string;
  aggregateVersion: number;
  status: OutboxStatus;
  occurredAt: string;
  payload: Record<string, unknown>;
  publishAttempts: number;
  nextAttemptAt: string;
  createdAt: string;
  GSI6PK: string;
  GSI6SK: string;
}

/** Monthly shard for the outbox partition, matching #5.3's `TENANT#t#OUTBOX#202608` example. */
function monthShard(isoTimestamp: string): string {
  return isoTimestamp.slice(0, 7).replace("-", "");
}

export function buildOutboxRecord(event: DomainEvent): OutboxRecord {
  const shard = monthShard(event.occurredAt);
  return {
    PK: `TENANT#${event.tenantId}#OUTBOX#${shard}`,
    SK: `EVENT#${event.occurredAt}#${event.eventId}`,
    entityType: "OutboxEvent",
    eventId: event.eventId,
    eventType: event.eventType,
    aggregateType: event.aggregate.type,
    aggregateId: event.aggregate.id,
    aggregateVersion: event.aggregate.version,
    status: "PENDING",
    occurredAt: event.occurredAt,
    payload: event.data,
    publishAttempts: 0,
    nextAttemptAt: event.occurredAt,
    createdAt: event.occurredAt,
    GSI6PK: "RECON#OUTBOX#PENDING",
    GSI6SK: `${event.occurredAt}#${event.eventId}`,
  };
}

export interface DynamoTransactPutEntry {
  Put: {
    TableName: string;
    Item: Record<string, unknown>;
    ConditionExpression: string;
  };
}

/**
 * OutboxPort.append from implementation-blueprint.md #5.1: appends the outbox Put to a
 * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
 * owns the transaction and includes the aggregate's own conditional update alongside it.
 */
export function appendToTransaction(
  tx: DynamoTransactPutEntry[],
  tableName: string,
  event: DomainEvent,
): void {
  const record = buildOutboxRecord(event);
  tx.push({
    Put: {
      TableName: tableName,
      Item: { ...record },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });
}

/** Backoff schedule for OutboxPublisher retries (#5.3 step 6: "incrementa tentativas e
 * aplica backoff"). Exponential with a cap; judgment call - blueprint doesn't pin exact values. */
export function nextAttemptDelayMs(publishAttempts: number): number {
  const base = 1_000;
  const cap = 5 * 60_000;
  return Math.min(cap, base * 2 ** publishAttempts);
}

  attribute_not_exists(PK) AND attribute_not_exists(SK)
```

Soft delete preserva registro e incrementa `version`. ExclusÃ£o fÃ­sica ocorre apenas por polÃ­tica de retenÃ§Ã£o, com auditoria e verificaÃ§Ã£o de dependÃªncias.

### 5.3 Outbox

Registro:

```json
{
  "PK": "TENANT#t_01#OUTBOX#202608",
  "SK": "EVENT#2026-08-19T14:03:22.481Z#evt_01",
  "entityType": "OutboxEvent",
  "eventId": "evt_01",
  "eventType": "expiration.item-due-date-changed.v1",
  "aggregateType": "ExpirationItem",
  "aggregateId": "item_01",
  "aggregateVersion": 8,
  "status": "PENDING",
  "occurredAt": "2026-08-19T14:03:22.481Z",
  "payload": {},
  "publishAttempts": 0,
  "nextAttemptAt": "2026-08-19T14:03:22.481Z",
  "createdAt": "2026-08-19T14:03:22.481Z",
  "GSI6PK": "RECON#OUTBOX#PENDING",
  "GSI6SK": "2026-08-19T14:03:22.481Z#evt_01"
}
```

`OutboxPublisher`:

1. recebe stream ou busca pendÃªncias em GSI6;
2. adquire lease condicional por `eventId`;
3. publica uma entrada por `PutEvents`;
4. considera falha individual de `PutEvents`;
5. ao sucesso, muda para `PUBLISHED`, grava `publishedAt` e incrementa versÃ£o;
6. ao erro, mantÃ©m `PENDING`, incrementa tentativas e aplica backoff;
7. nÃ£o assume exactly-once; consumidores deduplicam por `eventId`.

O sweeper periÃ³dico consulta GSI6 e cobre eventos perdidos apÃ³s a retenÃ§Ã£o do Streams.

---

## 6. Envelope e compatibilidade de contratos

### 6.1 Envelope de evento de domÃ­nio

```json
{

codex
1. O documento é majoritariamente fiel à convergência, mas ainda não é uma transcrição normativa plenamente segura. Há quatro problemas:

- O cabeçalho atribui `Codex 9.3/10` ao documento final, mas essa nota foi dada à proposta convergente em prosa da Rodada 2, antes de este arquivo existir. Deve dizer que as notas aprovam o design convergente, enquanto este documento passou por validação posterior.
- A definição concreta do GSI6 — risco residual explicitamente apontado na Rodada 2 — não foi fechada. Faltam namespaces, chaves, projeções e paginação para claims expirados e candidatos DST/materialização.
- O roteamento do outbox continua descrito como “por `eventType`/campo versionado de destino”. Essa alternativa aberta não é um contrato implementável: é preciso escolher exatamente um discriminador, seu valor e qual consumidor possui cada evento.
- O `DispatchOutboxRelay` e o sweeper são decisão central, mas não aparecem como componente completo do milestone. Faltam event source mapping, partial batch failure do Stream, lease/condição de publicação, transição `PENDING→PUBLISHED`, retry/backoff e schedule do sweeper.

2. Das cinco seções, as subespecificadas são:

- **Adapters DynamoDB:** falta o contrato completo de `ReminderReconciliationCandidateSource` e seu mapeamento exato para o GSI6. A frase de que todas as operações traduzem “1:1 para builders de `occ.ts`” também é imprecisa: queries e reads não vêm desses builders.
- **Handlers Lambda:** implementável em linhas gerais, mas deve enumerar o relay e o sweeper como handlers obrigatórios e definir seus gatilhos.
- **SQS + DLQ:** a fila está suficientemente especificada; o trecho outbox→SQS não está.
- **Scheduler:** `1–5 min` e “janela flexível pequena” deixam valores para decisão durante a implementação. É preciso fixar cadência e janela.
- **Testes:** os casos são bons, mas falta exigir explicitamente o teste de exclusividade do roteamento: um `ReminderDispatchRequested` deve ser consumido somente pelo relay SQS, nunca pelo publisher genérico.

3. A seção de não fechamento não cobre precisamente os cinco P1 porque omite o **P1 #1**. O milestone fecha a recuperação do trecho producer→dispatch, mas não pipelines assíncronos futuros de Notification/M4. Isso estava explícito na Rodada 1 e deve permanecer explícito. P1 #2–#5 estão representados fielmente.

4. **Nota final: 8,7/10.**

Mudanças pontuais necessárias para chegar a 9,0:

- Corrigir o cabeçalho para não apresentar a nota 9,3 como validação deste arquivo.
- Adicionar P1 #1 à seção “explicitamente NÃO fecha”.
- Especificar no componente 1:
  - interface e métodos do `ReminderReconciliationCandidateSource`;
  - `GSI6PK/GSI6SK` exatos para claims expirados e DST/materialização;
  - atributos projetados, limites e paginação;
  - ausência de colisão com `RECON#OUTBOX#PENDING`.
- Escolher um único contrato de roteamento do outbox — por exemplo, `destination: "SQS_REMINDER_DISPATCH_V1"` — e declarar publisher responsável, schema e exclusão do publisher genérico.
- Adicionar relay e sweeper como componente explícito, com gatilhos, partial batch failure, lease condicional, confirmação, retry/backoff e cadência.
- Fixar os valores hoje abertos do Scheduler, por exemplo claims a cada 5 minutos e DST diário com janela flexível de 15 minutos.
- Corrigir “operações traduzem 1:1 para builders de `occ.ts`” para limitar essa afirmação às escritas condicionais/transacionais.
tokens used
39.223
1. O documento é majoritariamente fiel à convergência, mas ainda não é uma transcrição normativa plenamente segura. Há quatro problemas:

- O cabeçalho atribui `Codex 9.3/10` ao documento final, mas essa nota foi dada à proposta convergente em prosa da Rodada 2, antes de este arquivo existir. Deve dizer que as notas aprovam o design convergente, enquanto este documento passou por validação posterior.
- A definição concreta do GSI6 — risco residual explicitamente apontado na Rodada 2 — não foi fechada. Faltam namespaces, chaves, projeções e paginação para claims expirados e candidatos DST/materialização.
- O roteamento do outbox continua descrito como “por `eventType`/campo versionado de destino”. Essa alternativa aberta não é um contrato implementável: é preciso escolher exatamente um discriminador, seu valor e qual consumidor possui cada evento.
- O `DispatchOutboxRelay` e o sweeper são decisão central, mas não aparecem como componente completo do milestone. Faltam event source mapping, partial batch failure do Stream, lease/condição de publicação, transição `PENDING→PUBLISHED`, retry/backoff e schedule do sweeper.

2. Das cinco seções, as subespecificadas são:

- **Adapters DynamoDB:** falta o contrato completo de `ReminderReconciliationCandidateSource` e seu mapeamento exato para o GSI6. A frase de que todas as operações traduzem “1:1 para builders de `occ.ts`” também é imprecisa: queries e reads não vêm desses builders.
- **Handlers Lambda:** implementável em linhas gerais, mas deve enumerar o relay e o sweeper como handlers obrigatórios e definir seus gatilhos.
- **SQS + DLQ:** a fila está suficientemente especificada; o trecho outbox→SQS não está.
- **Scheduler:** `1–5 min` e “janela flexível pequena” deixam valores para decisão durante a implementação. É preciso fixar cadência e janela.
- **Testes:** os casos são bons, mas falta exigir explicitamente o teste de exclusividade do roteamento: um `ReminderDispatchRequested` deve ser consumido somente pelo relay SQS, nunca pelo publisher genérico.

3. A seção de não fechamento não cobre precisamente os cinco P1 porque omite o **P1 #1**. O milestone fecha a recuperação do trecho producer→dispatch, mas não pipelines assíncronos futuros de Notification/M4. Isso estava explícito na Rodada 1 e deve permanecer explícito. P1 #2–#5 estão representados fielmente.

4. **Nota final: 8,7/10.**

Mudanças pontuais necessárias para chegar a 9,0:

- Corrigir o cabeçalho para não apresentar a nota 9,3 como validação deste arquivo.
- Adicionar P1 #1 à seção “explicitamente NÃO fecha”.
- Especificar no componente 1:
  - interface e métodos do `ReminderReconciliationCandidateSource`;
  - `GSI6PK/GSI6SK` exatos para claims expirados e DST/materialização;
  - atributos projetados, limites e paginação;
  - ausência de colisão com `RECON#OUTBOX#PENDING`.
- Escolher um único contrato de roteamento do outbox — por exemplo, `destination: "SQS_REMINDER_DISPATCH_V1"` — e declarar publisher responsável, schema e exclusão do publisher genérico.
- Adicionar relay e sweeper como componente explícito, com gatilhos, partial batch failure, lease condicional, confirmação, retry/backoff e cadência.
- Fixar os valores hoje abertos do Scheduler, por exemplo claims a cada 5 minutos e DST diário com janela flexível de 15 minutos.
- Corrigir “operações traduzem 1:1 para builders de `occ.ts`” para limitar essa afirmação às escritas condicionais/transacionais.
