OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: none
reasoning summaries: none
session id: 01a0a2a3-1665-7c23-9e7e-1730391abf01
--------
user
Voce e o Codex, protocolo de debate Claude<->Codex (AGENTS.md secao 4), Rodada 2. Na Rodada 1 voce deu nota 5.2/10 a proposta do Claude, com achado bloqueante: o scan GSI3 continuava sendo uma unica invocacao monolitica obrigada a drenar o shard inteiro, alem de nao discriminar ReminderOccurrence/DocumentChasingOccurrence, nao tratar falha parcial de SendMessageBatch, e citar uma fonte nao-oficial (dev.to). Voce tambem sugeriu um hibrido concreto (scan paginado/retomavel via fila de scan, com LastEvaluatedKey como continuation token, cursor so avanca apos confirmar publish) e um checklist rebalanceado 40/25/20/15.

Abaixo esta a revisao do Claude (Round 2), que incorpora esse hibrido quase integralmente. Critique novamente as cegas (voce nao tem acesso a nota que o Claude deu a propria revisao) e de nota final 0-10 sem arredondar. Aponte especificamente: (1) o achado bloqueante da Rodada 1 foi de fato fechado pelo design paginado/retomavel? (2) a discriminacao de tipo GSI3 e o tratamento de falha parcial de publish estao corretos? (3) resta algum risco de arquitetura real nao endereçado? (4) o checklist reconciliado esta adequado agora? Termine com uma linha exata: "NOTA: X.X/10".

=== REVISAO ROUND 2 ===
# ReminderProducer structural fix — Round 2 (Claude revision)

## Resposta ao achado bloqueante do Codex (Round 1)

Aceito o achado central: a Round 1 descreveu a fila de claim, mas o **scan em si** (a query
GSI3 + `SendMessage`) continuava sendo uma única invocação obrigada a drenar o shard inteiro
antes do timeout — só deslocava o ponto de ruptura de "scan+claim" para "scan+publish". Correto,
e é exatamente a mesma classe de falha que a mitigação já tentada (timeout/concorrência maiores)
comete: adia, não elimina. Incorporo o híbrido proposto pelo Codex quase integralmente, com
ajustes pontuais justificados abaixo.

**Nota sobre o relatório PERF-12-10k "não estar no branch atual"**: correto e esperado — o prompt
da tarefa (fora desta proposta) instruiu explicitamente ler esse documento via
`git show perf/load-testing-10k-v1:docs/.../PERF-12-async-pipeline-10k.md`, sem trocar de branch,
porque ele vive num branch de performance separado, não em `develop`/`design/...`. Os números
citados foram lidos diretamente desse `git show`, não inventados — mas registro aqui, por
transparência, que um revisor futuro sem esse contexto de invocação legitimamente estranharia a
ausência do arquivo no checkout local.

## Design revisado — descoberta paginada e retomável + claim via consumer SQS

Duas filas SQS, dois papéis, mesma fronteira GSI3:

1. **Fila de scan** (`reminder-scan`): mensagens são unidades de trabalho pequenas —
   `{ shardGeneration, shardIndex, minute, continuationToken? }` — uma por (shard, minuto) a
   escanear, nunca "o shard inteiro". EventBridge (mesmo trigger de hoje, 1x/minuto) enfileira
   uma mensagem por partição elegível (mesmo cálculo de `gsi3PartitionsForMinute` × lookback já
   existente em `producer.ts`) em vez de fazer o loop inline.
2. **Scanner** (extensão do `reminder-producer` atual, agora acionado por SQS em vez de
   EventBridge direto — MESMA função/role, preservando a exclusividade de `gsi3_read`): consome
   uma mensagem de scan por vez, faz UMA página de `queryGsi3` (LIMIT explícito, não "ler tudo"),
   publica os candidatos dessa página na fila de claim via `SendMessageBatch` (chunks de 10,
   checando `Failed[]` da resposta — nunca tratando 200 HTTP como sucesso incondicional, achado
   do Codex incorporado), e SÓ DEPOIS de confirmar que 100% dos candidatos da página foram
   publicados (retry local dos `Failed[]` até esgotar tentativas; se ainda sobrar falha, a
   mensagem de scan inteira NÃO é confirmada — volta à fila via visibility timeout, reprocessando
   a MESMA página do zero, nunca avança cursor antes de publicar) enfileira uma mensagem de
   continuação com o `LastEvaluatedKey` daquela página, se houver. Cada mensagem de scan processa
   uma fatia limitada (ex. página de até 1000 itens do DynamoDB, tipicamente sub-segundo) — uma
   invocação nunca precisa mais terminar um shard inteiro, só uma página.
3. **Claim consumer** (novo, ou reaproveitamento do mesmo padrão SQS-consumer de
   `reminder-dispatch` — role separada, SEM `gsi3_read`): consome candidatos da fila de claim,
   faz `get`+`transactWrite` condicional por occurrence — corpo idêntico ao já existente em
   `producer.ts` linhas 230-315, só movido para um handler disparado por SQS em vez de por loop
   inline. `claimTtlMs`, outbox transacional, GSI6 pointer — tudo inalterado.

**Discriminação `ReminderOccurrence` vs. `DocumentChasingOccurrence` (achado do Codex
incorporado)**: a mensagem de candidato carrega o `GSI3SK` bruto (ou já pré-parseado com um campo
`entityType: "REMINDER" | "CHASING"`), preservando a mesma lógica de `parseGsi3Sk`/
`parseChasingGsi3Sk`/fail-closed `unknownEntityType` que já existe hoje — só movida do loop do
scanner para o claim consumer, que decide qual branch de claim (`claimChasingOccurrence` vs. o
claim de reminder) executar. Nenhuma regressão do M10 cluster 4: o mesmo fail-closed alarma via
`shouldAlarm()`-equivalente no novo consumer.

**Duplicação entre ticks/páginas (achado do Codex incorporado)**: uma occurrence pode aparecer
mais de uma vez na fila de claim (reprocessamento de página por falha parcial de publish, ou
scan de minutos sobrepostos do lookback, já um caso hoje coberto pelo `seen`-guard local). A
`transactWrite` condicional (`expectedVersion`, status `SCHEDULED`→`CLAIMED`) já torna isso seguro
por construção — a segunda tentativa de claim da mesma occurrence perde a corrida via
`isTransactionCanceled`, tratado como não-falha, EXATAMENTE como o código atual já trata hoje
duas partições/minutos sobrepostos do mesmo tick. Nenhum mecanismo de deduplicação novo
necessário — a OCC já existente é suficiente, e o Codex concorda que isso é o mecanismo correto
("OCC torna a duplicação segura").

**DLQ/observabilidade (achado do Codex incorporado, resolvido a nível arquitetural, detalhe fica
para implementação)**: ambas as filas (scan e claim) usam o módulo Terraform já existente
`modules/sqs-worker-queue` (mesmo padrão de `dispatch_queue`, `redrive_policy` com
`maxReceiveCount` padrão do projeto) — DLQ nativo herdado, não inventado. Um candidato preso em
DLQ da fila de claim é exatamente equivalente, em severidade, a um `failed[]` de hoje (já
alarma via `shouldAlarm()`); idade do candidato mais antigo na fila (`ApproximateAgeOfOldestMessage`,
métrica CloudWatch nativa de SQS, já usada por este projeto para `dispatch_queue` conforme
PERF-12 10k §17.3) supre a métrica de "quão velho é o candidato não reivindicado", substituindo
`scheduler_lag_seconds` como sinal primário de saúde do pipeline. Redrive automático/alerta é
decisão de implementação (nível 3-4), não desta rodada arquitetural.

## Checklist reconciliado (aceito o rebalanceamento do Codex, com uma discordância pontual justificada)

Aceito a crítica de que o peso original de 25% em IAM era alto demais frente a "correção" como
requisito primário — corrigir isso é o próprio objetivo da tarefa (PERF-12 10k existe porque a
mitigação anterior NÃO corrigiu). Adoto a distribuição do Codex quase integralmente:

1. **Correção estrutural / progresso durável e retomável** (peso 40%) — atende: nenhuma
   invocação (scan OU claim) precisa processar mais que uma unidade de trabalho limitada
   (uma página de scan, ou um candidato de claim) dentro do seu próprio timeout; cursor de
   continuação só avança após confirmação de publish; não atende: qualquer invocação cujo
   sucesso dependa de terminar de processar uma unidade de trabalho ilimitada (um shard/burst
   inteiro) antes de um timeout fixo.
2. **Recuperação operacional (DLQ, retenção, backpressure, observabilidade)** (peso 25%) —
   atende: DLQ nativo herdado do módulo já usado neste projeto, idade do candidato mais velho
   como métrica de saúde, falha parcial de `SendMessageBatch` tratada explicitamente (retry até
   esgotar, nunca 200 incondicional); não atende: qualquer falha de publish silenciosamente
   descartada, ou backlog sem sinal de alarme.
3. **Preserva a fronteira IAM/GSI3 já deliberada** (peso 20%) — reduzido de 25% para 20%
   (aceito o argumento do Codex de que segurança não deveria compensar perda de dado, mas
   discordo de reduzir mais que isso: é a condição que classifica esta decisão como Nível 5 e
   um requisito explícito do prompt da tarefa, não um "nice to have" descartável) — atende:
   só a MESMA função/role `reminder-producer` (agora scanner) lê GSI3, escopada por policy
   explícita (`gsi3_read` continua exclusiva dela, nunca concedida ao claim consumer), reforçado
   por teste de isolamento atualizado; não atende: qualquer novo componente com `gsi3_read`.
4. **Alinhamento com padrão de referência AWS + simplicidade relativa** (peso 15%, reduzido de
   20%) — atende: o padrão "scan paginado retomável → fila → consumer" é uma composição direta
   de dois padrões AWS documentados (paginação de query + decouple scan/process via SQS já
   citado na Round 1), sem inventar mecanismo de coordenação novo além do que DynamoDB
   (`LastEvaluatedKey`) e SQS (visibility timeout) já garantem nativamente; não atende: qualquer
   mecanismo de coordenação bespoke entre scan e claim.

## Correção da citação de fonte (achado do Codex incorporado)

Retiro `dev.to/aws-builders/...` da lista de fontes que fundamentam o padrão — mantenho só como
contexto de leitura, não como fonte que sustenta a decisão. A decisão fica apoiada em 3 fontes
oficiais AWS: (1) AWS Prescriptive Guidance (decouple scan/process via SQS, já citada), (2) AWS
Lambda Developer Guide "Using Lambda with Amazon SQS" (deduplicação/partial-batch-response/
idempotência — `docs.aws.amazon.com/lambda/latest/dg/with-sqs.html`, mesma página citada pelo
Codex), (3) AWS SQS Developer Guide, "Amazon SQS batch actions" (semântica de falha parcial em
`SendMessageBatch`, `Failed[]` mesmo com HTTP 200 —
`docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-batch-api-actions.html`),
ambas apontadas pelo próprio Codex e verificadas como documentação oficial.

## Decisão proposta (revisada)

**Opção B revisada — scan paginado/retomável via fila de scan + claim via fila de claim**,
incorporando o híbrido do Codex quase sem alteração. Diferença do Round 1: a etapa de DESCOBERTA
deixa de ser uma única invocação monolítica e passa a ser, ela mesma, unidades de trabalho
pequenas e retomáveis — fechando o achado bloqueante. `reminder-producer` continua sendo a única
função com `gsi3_read`; um novo claim consumer nunca precisa dela. `DocumentChasingOccurrence`
preservado sem regressão. Escopo de implementação (fora desta rodada): 2 filas SQS
(`reminder-scan`, `reminder-claim`), extensão do handler de `reminder-producer` para consumir SQS
em vez de EventBridge direto, novo claim-consumer handler, atualização de
`test/integration/gsi3-isolation.test.ts`-equivalente para cobrir a nova role do claim consumer.

codex
Vou revisar a proposta contra o estado real do repositório e as garantias oficiais de DynamoDB/Lambda/SQS. O foco será encontrar falhas residuais de correção — especialmente paginação concorrente, entrega duplicada e semântica de retry — antes de fechar a nota cega.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/engineering/change-risk-scale.md; Get-Content -Raw docs/engineering/research-protocol.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 323ms:
# Expiration Tracker â€” Estado Atual + PrÃ³xima AÃ§Ã£o

> Este arquivo Ã© estado atual + prÃ³xima aÃ§Ã£o (`AGENTS.md` Â§2), nunca fonte normativa e nunca histÃ³rico narrativo â€” histÃ³ria detalhada vive em `docs/architecture/{session-log,decisions-log}.md`, `docs/engineering/decisions-log.md` e nas pastas `reviews/`. Cada linha abaixo deve caber em 1-3 frases: o quÃª + status + referÃªncia D-xxx/E-xxx para detalhe completo. **Recompactado em 2026-09-08 (D-236, segunda reconciliaÃ§Ã£o de engenharia de contexto â€” a de 2026-08-29 jÃ¡ tinha feito isso uma vez, 1067â†’78 linhas; o arquivo tinha reacumulado ~134 KB/~15.600 palavras em sÃ³ ~299 linhas, achado E-022/full-audit-round2, nota 6,84/10).** Antes de adicionar uma entrada nova aqui: se o D-nÃºmero/E-nÃºmero jÃ¡ tem linha completa em `decisions-log.md` (deveria sempre ter), esta seÃ§Ã£o sÃ³ recebe 1-3 frases â€” nunca reconte a narrativa.

## Branch / as-of

**NÃ£o confie nesta seÃ§Ã£o sem confirmar.** `git branch --show-current` deve ser `develop`; `git log --oneline -5`, `git status` e `git pull` antes de assumir qualquer coisa abaixo como pendente ou concluÃ­do â€” mÃºltiplas sessÃµes/agentes trabalham neste repo em paralelo.

**PadrÃ£o de trabalho autÃ´nomo (Marcelo, 2026-09-01, vale para toda sessÃ£o futura)**: prosseguir continuamente enquanto houver trabalho de engenharia real a fazer â€” nunca parar para pedir "posso continuar?". SÃ³ pausar/perguntar quando o prÃ³ximo passo depender genuinamente de decisÃ£o exclusiva de Marcelo (produto/arquitetura, execuÃ§Ã£o destrutiva/irreversÃ­vel, gasto de infra nÃ£o trivial, ou algo que `AGENTS.md` Â§4 exige elevar a ele). Nesses casos: registrar o pendente aqui/`decisions-log.md`, seguir para outra frente independente, nunca ficar ocioso.

## Fase atual

`Consolidation + Pilot Readiness` com recomendaÃ§Ã£o **CONDITIONAL GO**. M0-M12 (exceto billing/D-052, bloqueado por fornecedor) e Multi-User B2B (15 waves, D-084 a D-120) implementados e deployados. DomÃ­nio Documental completo (D-143 a D-235, ver roadmap abaixo). Full-audit-round2 (auditoria cÃ­clica por eixo, `docs/engineering/joint-review-criteria.md`) em andamento desde 2026-09-07 â€” ver seÃ§Ã£o dedicada abaixo para o que estÃ¡ aberto por eixo.

## Roadmap de lanÃ§amento (`docs/project/roadmap-competitivo-2026-09-01.md`) â€” 11 itens P0

1. **Requirement Templates** â€” ðŸŸ¢ IMPLEMENTADO (D-191).
2. **Bulk import (Documents+Requirements+column mapping)** â€” ðŸŸ¢ IMPLEMENTADO (D-192).
3. **WhatsApp operacional** â€” ðŸŸ¢ **IMPLEMENTADO POR COMPLETO do lado de engenharia, D-197/ADR-0012, TODAS as 5/5 fatias + rota de opt-in (D-286, 2026-09-14) fechando a Ãºltima pendÃªncia nomeada.** `POST /notifications/whatsapp-opt-in` agora chama `WhatsAppOptInService.recordOptIn()` (antes sem nenhum caller HTTP). **Resta sÃ³**: o bloqueio de produto/jurÃ­dico E-019 (aviso de privacidade, DPA Meta formalmente aceito, residÃªncia de dados decidida â€” nenhum feito ainda, fora do controle de engenharia; as credenciais reais da Meta no secret tambÃ©m dependem disso). Nenhuma fatia de engenharia resta, nem pendÃªncia nomeada nÃ£o bloqueante.
4. **IA/OCR no Document Lifecycle** â€” ðŸŸ¢ IMPLEMENTADO por completo (D-193). Flags `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED`/`DOCUMENT_ARCHIVE_PROMOTION_ENABLED` deliberadamente OFF (ativaÃ§Ã£o Ã© decisÃ£o futura reversÃ­vel).
5. **Busca e filtros documentais** â€” ðŸŸ¢ IMPLEMENTADO fatias 1-3 (D-194/D-196). Fatias 4-5 (projeÃ§Ã£o materializada+GSI10, Ã­ndice por assignee) DEFERIDAS com gatilho quantitativo nomeado em D-194 â€” nÃ£o bloqueante.
6. **Dashboard operacional/compliance** â€” ðŸŸ¢ IMPLEMENTADO (D-196).
7. **RelatÃ³rios + exportaÃ§Ã£o + audit trail** â€” ðŸŸ¢ IMPLEMENTADO fatias 1-4 (D-195). Fora de escopo, nomeado: "solicitaÃ§Ãµes pendentes" (sem GSI tenant-wide por status) e audit trail legÃ­vel para negÃ³cio.
8. **Document Types configurÃ¡veis** â€” ðŸŸ¢ IMPLEMENTADO (D-173 a D-186, D-221, D-224, D-243, D-244): CRUD, RBAC, metadata configurÃ¡vel, leitura pÃºblica para guest, e `documentTypeId` agora OBRIGATÃ“RIO no schema HTTP do guest submit-evidence (corte Ãºnico, `documentType` livre removido por completo) â€” D-244 codou o desenho `APPROVED` de D-243 por inteiro (schema+serviÃ§o+9/9 testes do checklist), gate local completo verde.
9. **Consolidar Guest Upload + Requests + Review + Recurrence** â€” ðŸŸ¢ FECHADO POR INTEIRO (D-222/D-226 a D-230). Ciclo completo (criarâ†’emitir credencialâ†’entregarâ†’resolver) funciona nos dois caminhos (avulso e recorrÃªncia), provado por teste e2e real.
10. **Consolidar Storage + Versioning + Renewal** â€” ðŸŸ¢ avanÃ§ado; `DocumentFile` fechado por completo (D-163 a D-168).
11. **Frontend completo do P0** â€” ðŸŸ¢ **FECHADO POR COMPLETO, 2026-09-11.** 25 telas (Blocos 0-10, D-254 a D-270) implementadas/testadas/revisadas, 1 rodada Codex por bloco. AnÃ¡lise holÃ­stica do frontend + rodada Codex final: CONCLUÃDA (D-271). Detalhe bloco-a-bloco completo (achados de cada rodada Codex, arquivos tocados): `docs/architecture/decisions-log.md` D-254 a D-271, nunca recontado aqui. **A15 sem drill-down por linha â€” FECHADO (D-292, 2026-09-14)**: o dado (`ImportRowOutcome`) jÃ¡ existia persistido desde D-192 Â§6, sÃ³ faltava uma rota que o expusesse; backend+frontend fechados juntos. **A16 sem histÃ³rico de execuÃ§Ã£o â€” FECHADO (D-293, 2026-09-14)**: mesmo padrÃ£o â€” `ReportSubscriptionRun`/`ReportDeliveryAttempt` jÃ¡ existiam persistidos desde D-204 decisÃ£o 5, sÃ³ faltava a rota de listagem (a rota de download jÃ¡ existia mas era inalcanÃ§Ã¡vel sem saber o `runId`); backend+frontend fechados juntos. Nenhuma pendÃªncia nomeada resta neste item â€” **todas as 25 telas do plano estÃ£o completas sem nenhum gap conhecido restante**. (`ExternalShareLink` slice 2/3 â€” item 8 do backlog P1 â€” fechada por completo em D-273, 2026-09-12, nÃ£o Ã© mais pendÃªncia; `createSeries` sem fence de unicidade ACTIVE â€” P0.3 da auditoria externa â€” CORRIGIDO em D-278, 2026-09-12, nÃ£o Ã© mais pendÃªncia.)

**Armazenamento de arquivo do guest** â€” design (D-265/ADR-0013) e implementaÃ§Ã£o (D-266) CONCLUÃDOS 2026-09-10, protocolo Claudeâ†”Codex completo (7 rodadas design + 2 rodadas implementaÃ§Ã£o). PendÃªncia remanescente: CSP/CORS (ver item 11 acima) â€” rodada Claudeâ†”Codex dedicada ainda nÃ£o escopada por ninguÃ©m.

**Bloco 7** (A10 Rastreamento legado + A22 Entrega de solicitaÃ§Ã£o + G01 Upload convidado legado) â€” COMPLETO (D-267). 1 rodada Codex (4,6/10 â†’ achados corrigidos, incluindo gap real de infra: rota CloudFront nunca existiu para o caminho legado). ðŸŸ¢ **`DocumentChasingOccurrence` FECHADO POR COMPLETO (D-288 backend + D-289 frontend, 2026-09-14)** â€” `GET .../chasing-occurrences` implementada/testada, achado adjacente real corrigido (`listDocumentRequests` vazava linhas de chasing mistipadas), e `Tracking.tsx` consome a rota de verdade (evidÃªncia ponta-a-ponta satisfeita, D-285). Nenhuma pendÃªncia nomeada resta neste item.

## Nova capacidade fora do roadmap original: quota de armazenamento por tenant (D-249, 2026-09-09)

ðŸŸ¢ FECHADO POR COMPLETO â€” backend (tracking/enforcement/leitura, `TenantStorageQuota`) e UI real (`StorageQuotaCard`/`StorageSection`, Bloco 1/D-255-256) implementados e testados. Quota default 8GB, confirmada por Marcelo apÃ³s pesquisa de mercado. Detalhe completo: `docs/architecture/decisions-log.md` D-249, `docs/architecture/reviews/storage-quota-scoping/`.

## Backlog pÃ³s-lanÃ§amento P1 (autorizado 2026-09-04)

1. reminder sequences configurÃ¡veis â€” ðŸŸ¢ DONE (M3).
2. escalation/mÃºltiplos destinatÃ¡rios â€” ðŸŸ¢ FECHADO (D-199 a D-201).
3. busca OCR/full-text â€” ðŸ”´ **BLOQUEADO**, pendente decisÃ£o de Marcelo entre 3 caminhos nomeados em D-202 â€” nÃ£o Ã© o prÃ³ximo item executÃ¡vel sem essa decisÃ£o.
4. relatÃ³rios agendados â€” ðŸŸ¢ FECHADO POR COMPLETO (D-204/D-211 a D-215).
5. dossiÃª documental PDF/Excel â€” ðŸŸ¢ FECHADO POR COMPLETO (D-205/D-216/D-217). TTL de retenÃ§Ã£o do metadado fechado depois (D-235).
6. bulk actions â€” ðŸŸ¢ FECHADO POR COMPLETO (D-206/D-207/D-209/D-210).
7. metadata configurÃ¡vel por Document Type â€” ðŸŸ¢ FECHADO POR COMPLETO (D-218 a D-221).
8. compartilhamento externo seguro (`ExternalShareLink`) â€” ðŸŸ¢ **FECHADO POR COMPLETO (D-273), 2026-09-12** â€” design `APPROVED` (D-225), slice 1/3 (D-241) + slice 2/3 (rotas HTTP autenticadas+anÃ´nima, RBAC, schemas, terraform, BFF, testes) ambas implementadas. 2 achados reais corrigidos durante slice 2/3 (nÃ£o presentes na slice 1/3 original): pepper de auditoria de IP reusava o pepper do token (violaÃ§Ã£o da DecisÃ£o 11, corrigido com `ipAuditPepper` prÃ³prio); bug de forma do token no `handleCreateShareLink` (string composta `shareId.selector.secret` do serviÃ§o nunca dividida antes de devolver ao cliente â€” achado sÃ³ por teste de integraÃ§Ã£o real, nÃ£o pelos unit tests do serviÃ§o). Gate local completo verde (`npm test` 2926/2926), `terraform test` root 27/27 + mÃ³dulo `api-gateway` 1/1. Mergeado `developâ†’main` (PR #303) e **APLICADO em `dev` com sucesso** (CD run 34704323049, `Apply complete! Resources: 19 added, 96 changed, 4 destroyed`, exatamente o plano previsto, 0 erro). **Ãšltimo item do backlog P1 originalmente sem decisÃ£o pendente â€” todo item 1-2/4-8 do backlog P1 estÃ¡ fechado.** Item 3 (busca OCR/full-text) segue ðŸ”´ BLOQUEADO por decisÃ£o de Marcelo (nÃ£o Ã© engenharia) â€” o backlog P1 nÃ£o estÃ¡ 100% fechado por causa desse item isolado.
- **P2** (nÃ£o escopado): assinatura eletrÃ´nica; API pÃºblica; webhooks; integraÃ§Ãµes de calendÃ¡rio; compliance score avanÃ§ado; **assistente de IA conversacional no produto** (ex. "quais fornecedores vencem em breve"/explicar documento sinalizado, via API do Claude direto â€” nÃ£o MCP/Claude Code â€” item novo, 2026-09-11, ver `docs/project/integrations-and-tooling-research-2026-09-11.md` Â§5; validado por pesquisa de mercado como tendÃªncia real de diferenciaÃ§Ã£o em SaaS de compliance, mas nÃ£o escopado nem priorizado).
- **Futuro** (sem gatilho comercial): portal completo do cliente; SSO/SCIM/controles enterprise.
- **Identidade visual (workstream paralelo, nÃ£o bloqueia nem Ã© bloqueado por engenharia)**: Fase 1 (pesquisa/diagnÃ³stico de marca) CONCLUÃDA 2026-09-11 por Marcelo diretamente (fora desta sessÃ£o de engenharia) â€” `Relatorio_Fase_1_Pesquisa_e_Diagnostico_Expiration_Tracker.md` e `Proximas_Tarefas_Identidade_Visual.md` (ambos na raiz do repo, **deliberadamente FORA do `ROOT_MD_ALLOWLIST`** de `scripts/check-doc-drift.ts` â€” nunca commitar sem antes mover para `docs/project/` ou atualizar o allowlist). PrÃ³ximo passo nomeado: Fase 2 (3 territÃ³rios criativos), depois Fase 3 (escolha+busca de anterioridade jurÃ­dica+sistema de variantes), depois Fase 4 (aplicaÃ§Ã£o real no produto, aÃ­ sim uma sessÃ£o de engenharia).

## Full-audit round2 (`docs/engineering/joint-review-criteria.md`) â€” estado por eixo, 2026-09-07/08

Gate de fechamento Ã© â‰¥9,0/10 nos dois avaliadores, sem arredondar. Nenhum eixo abaixo atingiu o gate ainda, exceto os 3 achados HIGH/ALTA jÃ¡ corrigidos nominalmente (linhas seguintes). Detalhe completo de cada eixo: `docs/engineering/decisions-log.md` E-0xx + `docs/engineering/reviews/full-audit-round2-*-summary.md`.

- **E-020 (OperaÃ§Ãµes/SRE)** â€” achado ALTA (rollback quebrado, manifesto hardcodava 13 de 61 Lambdas) **CORRIGIDO (D-232)**: `scripts/generate-lambda-manifest.ts` gera o manifesto automaticamente a partir de `infra/*.tf`, `npm run check:lambda-manifest` CI-blocking.
- **E-018 (SeguranÃ§a)/E-021 (Arquitetura), achado convergente (SEC-R2-02)** â€” lease de entrega de credencial guest quebrado (marcador reivindicado antes do envio SES) **CORRIGIDO (D-233)**: mÃ¡quina `CLAIMED`â†’`DELIVERED`/`SEND_UNCERTAIN`. Achados menores de E-021 sobre fan-out/duplicaÃ§Ã£o de hidrataÃ§Ã£o GSI4 **CORRIGIDOS (D-274, 2026-09-12)**. Ainda pendente: EMF/dashboard operacional ausente.
- **E-018 (SeguranÃ§a) â€” IAM least-privilege** â€” `dynamodb:Scan` **CORRIGIDO PARCIALMENTE (D-234)**: removido das polÃ­ticas gerais tenant-facing, isolado aos 4 workers que fazem Scan cross-tenant. Risco residual documentado (LeadingKeys estÃ¡tico inviÃ¡vel para ~44 Lambdas HTTP) mitigado via `AuthorizedTenantId` + suÃ­te adversarial (143 casos). **PropagaÃ§Ã£o de `AuthorizedTenantId` aos key-builders de persistÃªncia: COMPLETA nos 4 mÃ³dulos** (document-archive D-237, expiration D-238, subject D-239, organization D-240) â€” todo key-builder tenant-scoped dos 4 mÃ³dulos agora exige o tipo branded, fechando o gap de compile-time que D-234 tinha deixado como follow-up.
- **E-015 (Privacidade)** â€” `DossierExportRun`/`ReportSubscriptionRun`/`ReportDeliveryAttempt` sem TTL **CORRIGIDO (D-235)**: `purgeAfterTtl` (30 dias) adicionado, TTL nativo DynamoDB. Pendente: critÃ©rios #1/#5/#6/#7 sem rodada de debate dedicada (retorno esperado baixo).
- **E-016 (GovernanÃ§a de Produto/Multi-tenant)** â€” nota 7,1/7,8, gate nÃ£o atingido. Nenhum vazamento cross-tenant confirmado. `reset-dev-data.ts`'s `QUEUE_BASE_NAMES` desatualizado **CORRIGIDO (D-274, 2026-09-12)**. Enforcement automÃ¡tico de que toda rota nova chama `authorize()` **CORRIGIDO (D-287, 2026-09-14)**: `test/architecture/authorize-coverage.test.ts` novo.
- **E-017 (GovernanÃ§a de IA)** â€” nota 7,2/7,1, gate nÃ£o atingido. 2 incidentes reais (delegaÃ§Ã£o recursiva de subagente, item de roadmap declarado fechado sem worker real) registrados retroativamente em `ai-governance.md` Â§5. Pendente: sem gate de evidÃªncia ponta-a-ponta para fechar item de ROADMAP (sÃ³ cobre todo list); sem limite de profundidade de subagente.
- **E-019 (JurÃ­dico/Contratual)** â€” nota 4,73â†’5,25/5,16â†’5,28, gate nÃ£o atingido (o mais baixo). Fixes factuais em `third-party-inventory.md`. **Bloqueante real antes de WhatsApp com usuÃ¡rio real**: aviso de privacidade, DPA Meta formalmente aceito, residÃªncia de dados decidida â€” nenhum feito.
- **E-022 (Engenharia de Contexto)** â€” nota 6,84/10, gate nÃ£o atingido. **Este prÃ³prio arquivo era o achado central** â€” reconciliado nesta sessÃ£o como D-236 (ver preÃ¢mbulo). Guardrail de `scripts/check-doc-drift.ts` tambÃ©m corrigido (checagem de bytes/palavras adicionada, ver `AGENTS.md` Â§6 e o prÃ³prio script). Achados factuais menores em `docs/architecture/README.md` jÃ¡ corrigidos.
- **E-023 (Qualidade de Engenharia)** â€” nota 8,17â†’8,32/8,84â†’8,34, gate nÃ£o atingido (mais perto de todos). **Corrida intermitente entre `test/architecture/system-mutation-allowlist.test.ts` e `tenant-fence-boundary.test.ts` RESOLVIDA (D-272, 2026-09-11)** â€” causa raiz real identificada (dois processos `tsc`/vitest concorrentes compilando a Ã¡rvore inteira, nÃ£o um bug de agendamento interno) e corrigida com lock cross-processo, reproduzida e verificada de verdade. Ãšnico pendente restante deste eixo: `coverage.thresholds` em `vitest.config.ts` (decisÃ£o de Marcelo) â€” sem isso, uma nova rodada formal de nota provavelmente ainda nÃ£o bate â‰¥9,0 neste critÃ©rio especÃ­fico.

## Auditoria crÃ­tica externa recebida 2026-09-12 (~8,2/10, nÃ£o Ã© rodada Claudeâ†”Codex)

Documento completo: `docs/engineering/reviews/external-audit-2026-09-11-critica-repositorio.md`. 3 achados novos verificados diretamente contra cÃ³digo/GitHub real antes de registrar aqui (nunca aceitos sÃ³ por alegaÃ§Ã£o):
- **CI de `main` exige sÃ³ o check `guardrails`** â€” **CORRIGIDO (D-277, 2026-09-12)**: `gh api` PATCH em `branches/main/protection/required_status_checks`, adicionados `frontend`/`dynamodb-integration`/`Validate Infra (Terraform)` (nenhum recurso Terraform equivalente neste repo para branch protection â€” configuraÃ§Ã£o sÃ³ de GitHub). Verificado pÃ³s-mudanÃ§a via leitura direta do estado, nÃ£o sÃ³ a resposta do PATCH.
- **`ReminderPolicy` permite N policies por item, frontend modela 1** â€” **CORRIGIDO (D-280, 2026-09-12)**: protocolo Claudeâ†”Codex completo (6 rodadas, 9,2/9,1/9,2), pointer fixo com uniqueness real, worker/renovaÃ§Ã£o corrigidos, script de migraÃ§Ã£o provado contra DynamoDB Local real.
- **IP bruto na PK do guest rate limiter** (`document-archive-guest-rate-limiter.ts`) â€” **CORRIGIDO (D-276, 2026-09-12)**: HMAC-SHA256 com pepper jÃ¡ existente na composiÃ§Ã£o (nunca um secret novo), aplica tanto ao `GuestDocumentAccessService` quanto ao `ExternalShareLinkService` (mesma classe compartilhada).

**Progresso 2026-09-12 (mesma sessÃ£o, itens do documento completo alÃ©m dos 3 achados acima)**: P0.3 (`createSeries` sem fence de existÃªncia/unicidade) **CORRIGIDO (D-278)**; P1.6 (`reset-dev-data.ts` filas desatualizadas) **CORRIGIDO (D-274)**; P2.1 (2 boundaries async sem schema runtime validation) **CORRIGIDO (D-279)**; **P0.4 (`ReminderPolicy` N-vs-1) CORRIGIDO E DEPLOYADO (D-280)** â€” protocolo Claudeâ†”Codex completo (6 rodadas, 9,2/9,1/9,2), mergeado `developâ†’main` (PR #308) e **APLICADO em `dev`** (CD run 34715940129, `success`, `Apply complete! 0 added, 16 changed, 0 destroyed`); **P2.3 (CVE js-yaml real no frontend) CORRIGIDO** e **P2.4 (`NEXT_SESSION_PROMPT.md` recompactado, 24,7KBâ†’22,6KB) feito**, ambos mergeados/deployados juntos (PR #309, CD run 34717531852 â€” precisou `terraform force-unlock` de um lock de state travado por corrida com uma das CI de Dependabot, prÃ©-autorizado por `AGENTS.md` Â§7, `success` depois). D-276 (P2.2)/D-277 (P0.6)/D-278 (P0.3) tambÃ©m jÃ¡ `APLICADOS` em `dev` (CD run 34710597154). **Todos os 8 achados/itens acima estÃ£o FECHADOS.**

**P0.2 (GuestSession nÃ£o vinculada ao token, bug de 2 abas) FECHADO (D-283), 2026-09-14** â€” protocolo Claudeâ†”Codex 1 rodada, Codex aprovou de primeira (9,4/10, 0 bloqueantes). **P0.1 (CSP/CORS bloqueando upload real browserâ†’S3) FECHADO (D-284), 2026-09-14** â€” escopo real mecÃ¢nico como a investigaÃ§Ã£o de uma sessÃ£o anterior jÃ¡ indicava, `terraform apply` real contra `dev` confirmado. Merge `developâ†’main`/verificaÃ§Ã£o pÃ³s-deploy de AMBOS ainda pendente (ver "PrÃ³xima aÃ§Ã£o recomendada"). Ainda abertos, em ordem do documento: P0.5 falta suÃ­te "Real System E2E" contra `dev` real (projeto de infra de teste novo, nÃ£o correÃ§Ã£o pontual); P0.7 WhatsApp usuÃ¡rio real (jurÃ­dico/produto, nÃ£o engenharia). P1: `RequestContext` caro (**deliberadamente movido para o FINAL da fila, Marcelo 2026-09-12** â€” tratar sÃ³ depois de todo o resto), Reminder Producer nÃ£o sustenta o SLO, Capacity Model desatualizado, sem load testing, frontend sem code-splitting, `coverage.thresholds` (decisÃ£o de Marcelo). P2 restante: supply chain **P2.3 corrigido, P2.4 feito** (ver acima) â€” o que resta de P2 Ã© sÃ³ o que jÃ¡ estÃ¡ registrado como decisÃ£o-dependente ou baixo retorno (ver full-audit round2 abaixo).

**Nota operacional (nÃ£o bloqueante, pode jÃ¡ ter se resolvido sozinho)**: `codex exec` bateu limite de uso repetidamente no fim desta sessÃ£o (5+ tentativas ao longo de ~45min com a mesma mensagem de erro, jÃ¡ vencida) â€” provavelmente esgotado pelas 6 rodadas do protocolo P0.4. Rodar um probe mÃ­nimo (`echo "Reply OK" | codex exec --skip-git-repo-check -`) antes de assumir que ainda estÃ¡ bloqueado.

## PendÃªncias reais que dependem de decisÃ£o de Marcelo (lista consolidada)

1. Item 3 do backlog P1 (busca OCR/full-text) â€” escolher entre 3 caminhos nomeados em D-202.
2. ExecuÃ§Ã£o destrutiva real de `scripts/reset-dev-data.ts --confirm`/`--include-cognito` contra `dev` â€” postergado, nÃ£o perguntar de novo atÃ© ele sinalizar.
3. `coverage.thresholds` em `vitest.config.ts` â€” ainda nÃ£o decidido (E-023).
4. WhatsApp com usuÃ¡rio real (item 3 P0, engenharia 100% fechada desde D-286 â€” rota de opt-in tambÃ©m jÃ¡ construÃ­da) â€” resta sÃ³ aviso de privacidade, DPA Meta, residÃªncia de dados (E-019).
5. Wave 1b (Design System) â€” quais componentes com overlay/focus-trap (`Combobox`/`DateInput`/`Tooltip`/`Popover`/`DropdownMenu`/`Modal`/`Drawer`/`Tabs`/`Pagination`/`Breadcrumb`/`Avatar`/`Card`) abordar primeiro â€” deliberadamente por Ãºltimo, por pedido de Marcelo.
6. User Validation (planejamento de interface) â€” aguarda sinal explÃ­cito dele.
7. Aplicar ao **Claude for Startups Program** (`claude.com/programs/startups`, atÃ© US$25.000 em crÃ©ditos de API, sem exigir VC) â€” projeto se encaixa no perfil, mas o cadastro exige dados da empresa/aÃ§Ã£o direta de Marcelo. Ver `docs/project/integrations-and-tooling-research-2026-09-11.md` Â§6.
8. P0.5 â€” suÃ­te "Real System E2E" contra `dev` real (browserâ†’CloudFrontâ†’API Gatewayâ†’S3 sem mocks) â€” projeto de infra de teste novo, nÃ£o correÃ§Ã£o pontual; precisa de decisÃ£o sobre credenciais/tenant de teste, cadÃªncia de execuÃ§Ã£o e estratÃ©gia de limpeza antes de comeÃ§ar. Deliberadamente adiado, Marcelo 2026-09-14.
9. **Import CSV em massa para Items** (proposta, ainda nÃ£o decidida) â€” hoje o import CSV (`src/modules/import/`) sÃ³ cobre `TrackedSubject`/`Document`/`Requirement`, nÃ£o `Item` (o vencimento em si, entidade mais central do produto). Identificado como lacuna real durante o Programa de Performance (PERF-12, 2026-09-14) ao precisar semear 10k Items para teste de carga do pipeline de lembretes â€” nÃ£o existe hoje nenhum caminho de criaÃ§Ã£o em massa para Items (nem CSV, nem bulk-create). NÃ£o Ã© correÃ§Ã£o pontual: decisÃµes de produto reais precisam ser tomadas antes de implementar â€” mapeamento de colunas, se a PolÃ­tica de Lembrete vem junto na mesma linha ou Ã© configurada depois, estratÃ©gia de deduplicaÃ§Ã£o, validaÃ§Ã£o linha a linha (mesmo padrÃ£o jÃ¡ usado para os outros 3 tipos). ProvÃ¡vel nÃ­vel 5-6 na escala de risco (`docs/engineering/change-risk-scale.md`) â€” protocolo Claudeâ†”Codex + possÃ­vel ADR antes de implementar. Aguardando sinal de Marcelo para virar iniciativa.

## PrÃ³xima aÃ§Ã£o recomendada

**P0 (11 itens) FECHADO POR COMPLETO, incluindo o item 11 (frontend) â€” 2026-09-11.** As 25 telas do plano de sequenciamento (Blocos 0-10, D-254 a D-270) estÃ£o todas implementadas, testadas e revisadas. HistÃ³rico de como o plano de 25 telas/auditoria/sequenciamento convergiu (D-247/D-248/D-250/D-251/D-252/D-253) preservado em `decisions-log.md`, nÃ£o recontado aqui.

**AnÃ¡lise geral do frontend + rodada Codex holÃ­stica: CONCLUÃDA (D-271)**. E-023 (eixo mais perto do gate de engenharia): achado mecÃ¢nico resolvido (D-272), sÃ³ falta decisÃ£o de Marcelo (`coverage.thresholds`).

**`ExternalShareLink` slice 2/3 FECHADA (D-273)** â€” todo item do backlog P1 exceto o item 3 (OCR/full-text, bloqueado por decisÃ£o de Marcelo) estÃ¡ fechado. Mergeado/deployado em `dev` com sucesso (CD run 34704323049). Achado incidental de drift de Terraform (4 `aws_lambda_permission` perpetuamente nÃ£o-convergentes) tambÃ©m corrigido na mesma sessÃ£o (D-275). E-016/E-021 (achados menores) **CORRIGIDOS (D-274)**.

**PrÃ³ximo passo imediato (2026-09-14) â€” nesta ordem, autonomamente, sem esperar comando**: P0.2 (D-283), P0.1 (D-284), E-017 (D-285) **e a rota de opt-in do WhatsApp (D-286)** fechados nesta sessÃ£o. PR #310/#311 mergeados e deployados com sucesso. D-286 commitado/pronto para PR â€” mergear e deixar a pipeline aplicar (nunca apply local, ver nota de processo abaixo).

**NOTA DE PROCESSO IMPORTANTE (2026-09-14)**: um `terraform apply` foi rodado diretamente desta mÃ¡quina contra `dev` antes de um merge â€” desvio real do fluxo normal (`main`â†’CD), corrigido no ato por instruÃ§Ã£o explÃ­cita do Marcelo ("use sempre a pipeline para apply do terraform" / "nunca faÃ§a o apply aqui diretamente"). **Regra permanente daqui em diante, todos os projetos**: `terraform apply` NUNCA roda localmente â€” sÃ³ via pipeline de CD. `plan`/`validate`/`fmt`/`test` locais continuam liberados para verificaÃ§Ã£o.

**E-016 (D-287) FECHADO, 2026-09-14** â€” `test/architecture/authorize-coverage.test.ts` novo, enforcement automÃ¡tico real (nÃ£o sÃ³ documentaÃ§Ã£o) de que toda rota HTTP chama `authorize()` (ela mesma ou o `application/` do mÃ³dulo), `PUBLIC_ROUTE_ALLOWLIST` nomeando as 5 exceÃ§Ãµes legÃ­timas. **Bloco 7/A10 FECHADO POR COMPLETO (D-288+D-289), 2026-09-14** â€” backend+frontend, achado adjacente real corrigido. **E-018/E-021 (D-290) FECHADO, 2026-09-14** â€” EMF metrics + dashboard operacional, protocolo Claudeâ†”Codex completo (3 rodadas, 6,8â†’7,2â†’9,2/10). Escopo parcial explÃ­cito (3 de 30 workers, entrega real e-mail/WhatsApp fora), nÃ£o uma pendÃªncia escondida. Nenhum item decisÃ£o-independente resta identificado do full-audit round2. P0.5 (suÃ­te "Real System E2E" contra `dev`) Ã© um projeto de infra novo, nÃ£o uma correÃ§Ã£o pontual â€” candidato a prÃ³xima sessÃ£o se mais trabalho autÃ´nomo for desejado.

**LiÃ§Ã£o de processo desta sessÃ£o (ver `AGENTS.md` Â§1)**: default agora Ã© fork serial (nÃ£o orquestraÃ§Ã£o paralela via Workflow) â€” mais barato em token, evita o "imposto" de recontextualizaÃ§Ã£o de agente fresco. Paralelizar sÃ³ se Marcelo pedir velocidade explicitamente.

## Programa de Performance (novo, 2026-09-14 â€” fora do roadmap P0/P1, iniciativa prÃ³pria de Marcelo)

Fonte: `expiration-tracker-plano-acao-performance-world-class-2026-09-14.md` (raiz do repo, documento do Marcelo â€” nunca commitar/mover sem pedir, Ã© dele). Rastreamento vivo criado: `docs/engineering/performance/TODO.md` + `README.md` (estrutura de diretÃ³rios `baseline/experiments/load-tests/screenshots/traces/results/` jÃ¡ criada localmente, ainda nÃ£o commitada). Ordem obrigatÃ³ria do plano (Â§4): PERF-00 a PERF-15, em 3 ciclos (A: 00-05, B: 06-10, C: 11-13, fechamento: 14-15) â€” nunca executar tudo de uma vez.

**PrÃ³ximo passo concreto, autÃ´nomo, sem esperar comando**: PERF-00 (criar branch `perf/performance-program-v1` a partir de `develop`, registrar SHA, commitar `docs/engineering/performance/`) â†’ PERF-01 (checar quota real de Lambda Concurrent Executions em `dev` via `aws --profile claude-dev`, hipÃ³tese do plano Ã© que ainda Ã© 10) â†’ PERF-02 (instrumentaÃ§Ã£o/spans). Regra do prÃ³prio plano: medir antes de alterar, nunca misturar experimentos, sempre separar cold/warm, sempre percentis. Nenhuma mudanÃ§a estrutural (memÃ³ria de Lambda, code splitting, staleTime, RequestContext fast path, PriceClass, Provisioned Concurrency) antes de haver baseline â€” ver `TODO.md` seÃ§Ã£o "NÃ£o fazer sem evidÃªncia".

## Status de evidÃªncia (nÃ£o presumir E2E sem checar)

A maioria dos mecanismos do roadmap P0/backlog P1 estÃ¡ `IMPLEMENTED`/`UNIT TESTED` e confirmada `Active` contra `dev` via `aws --profile claude-dev`, mas **nem todo mecanismo tem prova E2E de ponta a ponta disparando pelo gatilho real** (cron/SQS real, nÃ£o sÃ³ G-V3/unit) â€” isso Ã© nomeado individualmente nas linhas do roadmap acima onde relevante ("nunca testado ponta a ponta com... real"). NÃ£o assumir E2E PROVEN sem checar a linha especÃ­fica do item ou `decisions-log.md`.

## Links para histÃ³rico (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)

- `docs/architecture/session-log.md` â€” linha do tempo compacta, uma entrada por sessÃ£o.
- `docs/architecture/decisions-log.md` / `docs/engineering/decisions-log.md` â€” toda decisÃ£o com nota Claude/Codex e status (D-0xx / E-0xx).
- `docs/architecture/reviews/` â€” artefatos de cada rodada Claudeâ†”Codex por tema.
- `docs/project/handoffs/` â€” prompts de handoff de sessÃµes anteriores, superseded por este arquivo.
- `docs/frontend/` â€” planejamento de interface + `interface-quality-standard.md` + `prototype/`.
- `docs/architecture/multi-user-b2b-wave-tracker.md` â€” detalhe fatia-a-fatia do Multi-User B2B (15 waves, D-084 a D-120).

# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-29 (reconciliaÃ§Ã£o de engenharia de contexto â€” este bloco reescrito para corrigir drift real: dizia "auto-CONFIRMED nÃ£o propaga dueDate, aguardando decisÃ£o do Marcelo" e "8 de 9 classes de retenÃ§Ã£o sem purga" quando D-058/D-061 jÃ¡ tinham decidido e implementado ambos em sessÃµes anteriores). Consolidation + Pilot Readiness Program concluÃ­do, recomendaÃ§Ã£o **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`, backlog item-a-item em `pilot-readiness-program.md`): Wave 3 (tenant isolation/LGPD) `DONE` de ponta a ponta, zero vulnerabilidade cross-tenant real; `USER_DOCUMENT` e `EXTRACTION_TRANSIENT` tÃªm purga fÃ­sica real (D-061), as outras 7 de 9 classes de retenÃ§Ã£o LGPD nÃ£o tÃªm, decisÃ£o de escopo pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership; Wave 5 fechou GTR-01. M6/M7/M9/M10/M11 deployados em `main`/`dev`, M7 **E2E PROVEN** (verificaÃ§Ã£o real 2026-08-27, achados registrados em `decisions-log.md` D-057/D-058); M12 bloqueado por decisÃ£o de fornecedor (D-052). Full BFF + Frontend Production Foundation (D-053/D-054) **implementado e `APPROVED`**; planejamento de interface 8/9 etapas `APPROVED`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); BLOCKER-A/B/C **todos resolvidos**. W3-07 (exclusÃ£o fÃ­sica de tenant/DSR) tem design **`APPROVED WITH CONDITIONS`** (D-066/D-067, condiÃ§Ã£o de polÃ­tica SES jÃ¡ decidida) e implementaÃ§Ã£o incremental em andamento (fencing de writers D-068 a D-080, purge pipeline D-081 a D-083 com nota Codex 9,1/10, emenda de privacidade B2B-9/D-103/D-104); orquestrador real ganhou design `APROVADO` depois (D-121, ver mais adiante neste mesmo bloco) â€” nÃ£o deixar a frase acima ler como "ainda nÃ£o decidido" sem essa referÃªncia, achado de autocontradiÃ§Ã£o de `full-audit-round2-contexto` (2026-09-07). **Multi-User B2B (D-084 a D-108, 2026-08-30)**: design tÃ©cnico `APPROVED` via protocolo (Claude 9,2/Codex 9,2) e timing decidido diretamente pelo Marcelo (proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05`) â€” maior iniciativa de arquitetura autorizada no momento; **Waves B2B-0 a B2B-11 `DONE`** (inventÃ¡rio, physical model D-086, identidade global D-087/D-088, Organization+Membership+CreateOrganizationService D-089/D-090/D-091, `OnboardingStateResolver` puro D-092/D-094, `RequestContext Cutover` D-095/D-096 â€” login nÃ£o cria mais tenant sozinho, `POST /bff/organizations` fecha o loop de onboarding, cap transacional; `RBAC` D-097/D-098 â€” `Membership.role=ADMIN` real na matriz de autorizaÃ§Ã£o, primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014; `Invitations/Team` D-099/D-100 â€” `Invitation`/aceite/revogaÃ§Ã£o/gerÃªncia de membros com last-owner protection, segunda aplicaÃ§Ã£o de E-014, primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o; `BFF Organization Context` D-101/D-102 â€” `X-Organization-Id` transportado do BFF ao recurso, terceira aplicaÃ§Ã£o de E-014, fecha o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; `W3-07/Privacy Reconciliation` D-103/D-104 â€” quarta aplicaÃ§Ã£o de E-014, fecha o `InvitationTokenPointer` Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization, formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md`; `Tenant-aware Frontend` D-105/D-106 â€” primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend, corrige a regressÃ£o real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; `Responsibility + Notifications` D-107/D-108 â€” quinta aplicaÃ§Ã£o de E-014, migra `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch` de validaÃ§Ã£o vestigial para `Membership`+`GlobalUser` reais; NÃƒO inclui a supersessÃ£o de GTR-01, ver `multi-user-b2b-wave-tracker.md`; `Cutover de dev` D-110/D-111 â€” sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`), `scripts/reset-dev-data.ts` (reset/reseed, nÃ£o migraÃ§Ã£o, dado 100% sintÃ©tico confirmado por inventÃ¡rio real via `aws --profile claude-dev`) + remoÃ§Ã£o de `LEGACY_TENANT_ONLY`, Fase A verificada de fato contra `dev`; execuÃ§Ã£o real destrutiva (`--confirm`) pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo; `E2E/Adversarial Security` D-112/D-113 â€” sÃ©tima aplicaÃ§Ã£o de E-014, auditoria contra as 25 perguntas de `roadmap-evolution/17` Â§121 (20/25 jÃ¡ cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assÃ­ncrona de notificaÃ§Ã£o â€” `notification.ts`+`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` jÃ¡ testado, 2 ganham teste novo de revogaÃ§Ã£o/isolamento de role, 1 Ã© auditoria de fixture IDs sem achado). **Multi-User B2B â€” todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** **B2B-14 (Operational Evidence) `EM ANDAMENTO`, 2026-08-30**: 7 achados reais severos, cada um encontrado sÃ³ por exercitar o fluxo de verdade contra `dev` pela primeira vez â€” `app_origin` placeholder nunca trocado (D-114), `bff-handler` crashava em toda invocaÃ§Ã£o (D-115), GSI4 nunca autorizado aos 10 Lambdas reais que o consomem (D-116), 2 rotas do BFF nunca wireadas no API Gateway (D-117), nenhuma tela de onboarding existia (D-118), `ExpressionAttributeNames: {}` quebrava `TransactWriteItems` real em 3 arquivos incluindo o guard de Ãºltima-proteÃ§Ã£o-de-OWNER (D-119), e o convite/aceite de membro nunca foi alcanÃ§Ã¡vel de ponta a ponta â€” tela nunca existiu, rota nunca foi wireada, e-mail real nunca foi ativado, SES sandbox sem NENHUMA identity verificada no ambiente inteiro (D-120, 2 endereÃ§os reais verificados com o Marcelo). Pendente para fechar: Marcelo testar convite/aceite/troca-de-organizaÃ§Ã£o com uma segunda conta â€” **explicitamente adiado para uma sessÃ£o futura**, nÃ£o bloqueia o resto do trabalho. Em paralelo, **W3-07 â€” orquestrador de purga ganhou design `APROVADO` (D-121, protocolo Claudeâ†”Codex 3 rodadas, 9,1/9,2): Step Functions + EventBridge Scheduler, e a implementaÃ§Ã£o real estÃ¡ `IMPLEMENTED`/`DEPLOYED`** (D-124, PR #122, CD `success` 2026-08-31, state machine + sweeper `ENABLED` confirmados ao vivo contra `dev` â€” corrigido em 2026-09-07, esta frase ainda dizia "implementaÃ§Ã£o real ainda nÃ£o construÃ­da", achado de `full-audit-round2-contexto`); e 2 lacunas de produto levantadas pelo Marcelo (exportaÃ§Ã£o de dados CSV/planilha, reatribuiÃ§Ã£o de responsabilidade ao remover um Membership) tiveram rodadas de scoping dispatchadas via o mesmo protocolo â€” ver `decisions-log.md` para o D-number final de cada uma antes de assumir status. **`docs/engineering/definition-of-done.md` (E-012, emenda E-013)**: gate de processo permanente â€” nenhum item de todo list que produza cÃ³digo Ã© `completed` sem passar pelo gate correspondente ao seu nÃ­vel de risco, incluindo aplicaÃ§Ã£o concreta (nÃ£o sÃ³ referencial) de `test-engineering-standard.md`. Ver `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o exata â€” este bloco Ã© sÃ³ o resumo de fase, nÃ£o histÃ³rico item-a-item.
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `correlationid-xray-trace-join.md` | normativo atual (decisÃ£o + implementaÃ§Ã£o) | JunÃ§Ã£o `correlationId`â†”trace ADOT/X-Ray (E-011 pendÃªncia 1) â€” `APPROVED` via protocolo Claudeâ†”Codex (Claude 9,3/Codex 9,4), amplia D-022 sem reabri-la; implementado 2026-08-29 (`xray-trace-header.ts`, correÃ§Ã£o de precedÃªncia do `SecureLogger`), status `E2E PROVEN` (smoke test real contra `dev` confirmado 2026-08-29 â€” corrigido em 2026-09-07, esta linha ainda dizia "pendente", achado de `full-audit-round2-contexto`) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 em diante (D-231+ na revisÃ£o mais recente â€” nÃ£o referenciar um teto fixo aqui, ele desatualiza a cada sessÃ£o; a numeraÃ§Ã£o tambÃ©m nÃ£o Ã© sequencial na ordem das linhas â€” ex. D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/00-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt original (2026-08-22) que abriu as 3 fases de evoluÃ§Ã£o estratÃ©gica do roadmap abaixo; movido da raiz em 2026-08-29, mesmo tratamento de `blocker-b-mission-brief.md` |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `roadmap-evolution/16-document-lifecycle-strategic-analysis.md` | informativo (anÃ¡lise externa, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR) | EvoluÃ§Ã£o estratÃ©gica de "controle de vencimentos" para "gestÃ£o enxuta do ciclo de vida documental" â€” avaliaÃ§Ã£o de coerÃªncia/reaproveitamento/risco de overengineering; recomenda aprovar a direÃ§Ã£o mas nÃ£o antecipar o escopo, mantendo o foco em Consolidation + Pilot Readiness; identifica `Document` deixar de ser filho exclusivo de `ExpirationItem` como a mudanÃ§a arquitetural futura central, marcada nela mesma como Type 1 (ADR + protocolo Claudeâ†”Codex antes de qualquer implementaÃ§Ã£o) |
| `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` | **`APPROVED` â€” design tÃ©cnico (protocolo AGENTS.md Â§4) + timing (decisÃ£o direta do Marcelo, D-085)** | EspecificaÃ§Ã£o vigente de Multi-User B2B â€” `Organization` como tenant boundary permanente, `User` global, `Membership` N:N, RBAC por permissions. Protocolo Claudeâ†”Codex completo (Â§125): 3 rodadas reais, Claude 9,2/Codex 9,2, ambos â‰¥9,0 (mecanismo transacional de last-OWNER, unicidade de Membership/Invitation, tabela mantÃ©m/emenda/refaz do impacto no W3-07, supersessÃ£o explÃ­cita de D-060/GTR-01). **D-085 (2026-08-29)**: Marcelo decidiu diretamente proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05` (marcado `SUPERSEDED (timing)`, modelo de billing/entitlements desse cluster permanece vigente). **Waves B2B-0 a B2B-11 `DONE`** (D-108 conclui B2B-11, Responsibility + Notifications) â€” ver `multi-user-b2b-wave-tracker.md` para o detalhamento por subitem (inclui a nota de escopo sobre GTR-01/`Organization.displayName` ainda pendente). Wave B2B-12 (Cutover de dev) foi, na Ã©poca da escrita original desta linha, a prÃ³xima aÃ§Ã£o real do documento fonte â€” **hoje jÃ¡ `APPROVED`/`IMPLEMENTADO` (D-110/D-111, ver linha da tabela abaixo)**, corrigido em 2026-09-07 (achado de `full-audit-round2-contexto`: esta linha descrevia um estado histÃ³rico do documento-fonte como se fosse o estado atual) â€” ver `NEXT_SESSION_PROMPT.md` para o estado de execuÃ§Ã£o vigente |
| `roadmap-evolution/18-synthetic-persona-research-quality-baseline.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Fundamento metodolÃ³gico proposto para um futuro "Synthetic Persona Evaluation Framework" (agentes condicionados por persona exercitando a aplicaÃ§Ã£o real, em complemento â€” nunca substituiÃ§Ã£o â€” a testes humanos): modelo Persona+Contexto+Dataset+CenÃ¡rio+Objetivo="Trial", provenance de evidÃªncia (`hypothesisâ†’research-groundedâ†’human-observedâ†’production-observed`), hierarquia de oracle (estado determinÃ­stico > banco/API/evento > LLM grader > autoavaliaÃ§Ã£o do agente, esta Ãºltima nunca fonte de verdade), scorecard de 100 pontos + 18 Hard Gates (HG-01..18, ex. "agente nunca recebe oracle/caminho de navegaÃ§Ã£o", "actor nÃ£o pode ser o Ãºnico judge"). Puramente conceitual/genÃ©rico nesta etapa, ainda nÃ£o aplicado ao Expiration Tracker |
| `roadmap-evolution/19-synthetic-persona-application-analysis.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Aplica o baseline de `18` Ã  realidade atual do cÃ³digo: conclui que a aplicaÃ§Ã£o Ã© "muito favorÃ¡vel" a personas sintÃ©ticas (domÃ­nio modelado, OCC, idempotÃªncia, outbox, relÃ³gio injetÃ¡vel, `dev` resetÃ¡vel, Playwright, Test Engineering Standard jÃ¡ existentes), mas nomeia 4 restriÃ§Ãµes reais â€” zero usuÃ¡rio/produÃ§Ã£o real, frontend cobre sÃ³ parte das superfÃ­cies planejadas, modelo B2B ainda em transiÃ§Ã£o, e a principal: **o Playwright atual roda majoritariamente contra um BFF mockado (`page.route()`), nÃ£o contra o sistema real** â€” a mesma lacuna que B2B-14 (Operational Evidence) jÃ¡ vinha fechando manualmente. TambÃ©m documenta drift real entre `NEXT_SESSION_PROMPT.md`/comentÃ¡rios antigos e o cÃ³digo atual |
| `roadmap-evolution/20-synthetic-trial-identity-design.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Proposta de implementaÃ§Ã£o: identidade de execuÃ§Ã£o hierÃ¡rquica `evaluationRunIdâ†’trialIdâ†’correlationIdâ†’traceId/spanId`, deliberadamente **execution metadata, nunca atributo funcional do domÃ­nio** (nunca `ExpirationItem.trialId`), propagada via header Playwrightâ†’middleware BFFâ†’`RequestContext`â†’outbox `metadata` (nunca `payload`)â†’SQSâ†’workerâ†’audit event/log estruturado; gate de ambiente (`EVALUATION_CONTEXT_ENABLED`, `prod=false` por padrÃ£o), fail-closed para metadata invÃ¡lida, 12 Hard Gates prÃ³prios (HG-TID-01..12, ex. "trialId nunca altera autorizaÃ§Ã£o/validaÃ§Ã£o funcional"). O prÃ³prio documento recomenda revisÃ£o via protocolo Claudeâ†”Codex antes de entrar no roadmap â€” ainda nÃ£o submetida |
| `multi-user-b2b-wave-b2b4-scope.md` | **`APPROVED` (D-092, protocolo Claudeâ†”Codex 4 rodadas, Claude 9,3/Codex 9,4)** | Escopo final de Wave B2B-4 â€” `OnboardingStateResolver` puro (4 estados, procedimento sequencial estrito), sem wiring de login, sem exposiÃ§Ã£o HTTP; redesigna "remover auto-provision"/gate real para Wave B2B-5. EvidÃªncia das 4 rodadas em `reviews/multi-user-b2b-wave-b2b4-scoping/` |
| `multi-user-b2b-wave-b2b5-scope.md` | **`APPROVED` (D-095, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,1) â€” `IMPLEMENTADO` (D-096)** | Escopo final de Wave B2B-5 (RequestContext Cutover) â€” `bootstrapUser()` de 2 itens, `RequestContextResolver` reescrito com resoluÃ§Ã£o de Membership Ãºnica e assert explÃ­cito para `ADMIN` nÃ£o suportado, `POST /bff/organizations` com cap transacional (`GlobalUser.hasCreatedOrganization`), self-heal de sessÃ£o BFF; desvio faseado explÃ­cito de Â§11/Â§12 do physical model (seleÃ§Ã£o real de organizaÃ§Ã£o fica para B2B-6). EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b5-scoping/`; implementaÃ§Ã£o e 2 achados reais corrigidos (UserProfile mantido, gate de lifecycle da Organization adicionado) em `decisions-log.md` D-096 |
| `multi-user-b2b-wave-b2b6-scope.md` | **`APPROVED` (D-101, protocolo Claudeâ†”Codex 3 rodadas, checklist v2, Claude 9,1/Codex 9,2) â€” `IMPLEMENTADO` (D-102)** | Escopo final de Wave B2B-6 (BFF Organization Context) â€” terceira aplicaÃ§Ã£o real de E-014 (padrÃ£o header+revalidaÃ§Ã£o server-side, OWASP Multi Tenant Security). `X-Organization-Id` injetado sÃ³ pelo BFF (nunca do browser), `resolveWorkingOrganization()`/`OrganizationUnavailableError`(403)/`OrganizationSelectionRequiredError`(409) fecham o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; `POST /bff/organization/select` (CSRF)/`GET /bff/organizations` (filtrado por lifecycle) novos. EvidÃªncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b6-scoping/` |
| `multi-user-b2b-wave-b2b7-scope.md` | **`APPROVED` (D-097, protocolo Claudeâ†”Codex 3 rodadas, checklist v2, Claude 9,2/Codex 9,3) â€” `IMPLEMENTADO` (D-098)** | Escopo final de Wave B2B-7 (RBAC) â€” primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014 (pesquisa GitHub/Linear/Slack/Notion/NIST-ANSI-INCITS-359, checklist de critÃ©rios de nota contestado e reconciliado na Rodada 1â†’2). `Role` ganha `ADMIN`, paritÃ¡rio com `OWNER` nas 4 actions de deleÃ§Ã£o de recurso e no bypass de ownership; `notification:configure` reclassificada para `READ_ONLY_ROLES` (bug fix real, nÃ£o escolha ADMIN-vs-OWNER); `tenant:configure-document-request-delivery` isolada em novo tier `OWNER_ROLES`. EvidÃªncia das 3 rodadas (incl. a contestaÃ§Ã£o do checklist) em `reviews/multi-user-b2b-wave-b2b7-scoping/` |
| `multi-user-b2b-wave-b2b8-scope.md` | **`APPROVED` (D-099, protocolo Claudeâ†”Codex 3 rodadas, checklist v3, Claude 9,1/Codex 9,2) â€” `IMPLEMENTADO` (D-100)** | Escopo final de Wave B2B-8 (Invitations/Team) â€” segunda aplicaÃ§Ã£o real de E-014, declaraÃ§Ã£o `SIM PARCIAL` (mecanismo de dados jÃ¡ `APPROVED` em D-086; last-owner protection/hierarquia de gerÃªncia de membros pesquisados nesta wave, GitHub/Slack/Linear/Notion). `Invitation`+token pointer+dedup (D-086 Â§7), `AcceptInvitationService` (transaÃ§Ã£o de 6 itens, token consumido estruturalmente), `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService` com builder Ãºnico de `ownerCount` (`LastOwnerError`) â€” primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o. EvidÃªncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b8-scoping/` |
| `multi-user-b2b-wave-b2b9-scope.md` | **`APPROVED` (D-103, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,3/Codex 9,3) â€” `IMPLEMENTADO` (D-104)** | Escopo final de Wave B2B-9 (W3-07/Privacy Reconciliation) â€” quarta aplicaÃ§Ã£o real de E-014 (GitHub/Slack/Atlassian, "delete conta" vs. "delete organizaÃ§Ã£o"). Fix real: `InvitationTokenPointer` (declara `organizationId`, nÃ£o `tenantId`) ganha 3Âª clÃ¡usula OR no scan/`PURGE_DELETE` de purga, fechando Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization. `privacy-lgpd.md` Â§4.1/Â§4.2 novos (fronteira User-level vs. Organization-level erasure, invariante de Ãºltimo OWNER documentada) + 4 linhas de retenÃ§Ã£o. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b9-scoping/` |
| `multi-user-b2b-wave-b2b10-scope.md` | **`APPROVED` (D-105, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2) â€” `IMPLEMENTADO` (D-106)** | Escopo final de Wave B2B-10 (Tenant-aware Frontend) â€” primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend nesta sessÃ£o. Fix de regressÃ£o real (`AuthContext` tratava todo usuÃ¡rio autenticado como deslogado desde B2B-5); arquitetura de cache isolation (query keys por `organizationId` + `ActiveOrganizationProvider`/`switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta) fecha uma corrida real de vazamento de dado entre tenants na UI; switcher/members/settings novos. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b10-scoping/` |
| `multi-user-b2b-wave-b2b11-scope.md` | **`APPROVED` (D-107, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2) â€” `IMPLEMENTADO` (D-108)** | Escopo final de Wave B2B-11 (Responsibility + Notifications) â€” quinta aplicaÃ§Ã£o de E-014 (GitHub/Linear, "assignee deve ser membro real e ativo"). `NotificationRecipientResolver` migrado de `UserProfile` (vestigial) para `Membership`+`GlobalUser` (2 condiÃ§Ãµes); `assigneeUserId`/`ItemWatch.userId` agora validados contra `Membership` real via `MemberEligibilityChecker` novo. **Nota**: nÃ£o inclui a supersessÃ£o de GTR-01 (`UserProfile.requesterDisplayName`â†’`Organization.displayName`), prevista em B2B-5 mas nunca debatida nas 3 rodadas reais desta wave â€” ver `multi-user-b2b-wave-tracker.md`. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b11-scoping/` |
| `multi-user-b2b-wave-b2b12-scope.md` | **`APPROVED` (D-110, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,1) â€” `IMPLEMENTADO` (D-111)** | Escopo final de Wave B2B-12 (Cutover de dev) â€” sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`, polÃ­tica reset-vs-migraÃ§Ã£o jÃ¡ aprovada dentro do prÃ³prio `roadmap-evolution/17` Â§62-63). InventÃ¡rio real via `aws --profile claude-dev` confirma `dev` 100% sintÃ©tico/descartÃ¡vel; `scripts/reset-dev-data.ts` (Fase A inventÃ¡rio+snapshot sempre, Fase B delete real sÃ³ `--confirm`, allowlist de tabela/conta, verificaÃ§Ã£o final fail-loud) + remoÃ§Ã£o de `LEGACY_TENANT_ONLY` (backend+frontend+teste). Fase A verificada de fato contra `dev` (nÃ£o sÃ³ fakes). **ExecuÃ§Ã£o real destrutiva (`--confirm`/`--include-cognito`) pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo.** EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b12-scoping/` |
| `multi-user-b2b-wave-b2b13-scope.md` | **`APPROVED` (D-112, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,2, rÃ©gua E-014 9,3) â€” `IMPLEMENTADO` (D-113)** | Escopo final de Wave B2B-13 (E2E/Adversarial Security) â€” sÃ©tima aplicaÃ§Ã£o de E-014 (OWASP WSTG/Authorization Testing, SIM PARCIAL). Auditoria completa contra as 25 perguntas de `roadmap-evolution/17` Â§121 (matriz Qâ†’arquivo:linha completa): 20/25 jÃ¡ cobertas por testes reais de waves anteriores; 2 fecham com fix real (mesmo TOCTOU em `notification.ts`+`subject.ts` â€” entrega assÃ­ncrona nunca revalidava `Membership`, sÃ³ roteamento checava â€” corrigido reaproveitando `DynamoDbNotificationRecipientResolver` jÃ¡ testado, `ResolvedRecipient` ganha `email?: string`); 2 ganham teste novo (revogaÃ§Ã£o de Membership encadeada ponta-a-ponta; roles diferentes por Organization nunca vazam); 1 Ã© auditoria de fixture IDs (zero achado). A Rodada 1 do Codex achou um 6Âº achado real (mesmo TOCTOU em `document-chasing-dispatch`) que a proposta original nÃ£o tinha visto. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b13-scoping/` |
| `multi-user-b2b-wave-b2b0-inventory.md` | evidÃªncia de rodada (read-only, completo) | InventÃ¡rio verificado de Wave B2B-0 (Â§105 do documento acima): `tenantId=userId` em produÃ§Ã£o (3 pontos confirmados, 1 gap de fencing prÃ©-existente achado no login BFF), `IdentityMapping`/BFF session/`RequestContext`, stores tenant-scoped/S3 (3 prefixos reais, diverge de Â§68)/eventos/W3-07, frontend caches (zero isolamento tenant hoje) e contagem reproduzÃ­vel da suÃ­te de testes (122 arquivos/1092 casos, nÃ£o ~1104) |
| `multi-user-b2b-physical-model.md` | **`APPROVED` (D-086, protocolo Claudeâ†”Codex 5 rodadas, Claude 9,3/Codex 9,5)** | Physical model final de Wave B2B-1 â€” `User`/`IdentityMapping`/`Organization`/`Membership` (3 estados, GSI4 `MembershipByUser` reaproveitado)/`Invitation`/`ownerCount`/`bootstrapUser()`/`DeviceSession`+`logoutAll`/`RequestContext`/BFF session, respostas finais Ã s 25 perguntas de `roadmap-evolution/17` Â§121. EvidÃªncia das 5 rodadas em `reviews/multi-user-b2b-physical-model/` |
| `multi-user-b2b-wave-tracker.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Status DONE/IN PROGRESS/BLOCKED/NOT STARTED por wave (B2B-0 a B2B-15), mesmo papel de `docs/engineering/pilot-readiness-program.md` â€” ponto de entrada para saber o que jÃ¡ foi feito antes de continuar a iniciativa |
| `reviews/context-engineering-reconciliation/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 | ReconciliaÃ§Ã£o da arquitetura de contexto do repo (2026-08-29) â€” root cleanup (21â†’6 `.md`), `AGENTS.md`/`NEXT_SESSION_PROMPT.md` trimados (narrativa de milestone jÃ¡ duplicada em `decisions-log.md` removida), 2 guardrails novos em `check-doc-drift.ts` (root allowlist, size), READMEs stale corrigidos. 4 rodadas Codex (8,1â†’8,8â†’8,9â†’9,3/10, `APPROVED`), 3 achados reais de drift semÃ¢ntico prÃ©-existente corrigidos no processo |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/w3-07-purge-orchestrator-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-121) | Orquestrador real do purge pipeline W3-07 (Step Functions + EventBridge Scheduler) â€” decisÃ£o pendente desde D-083, fechada 3 rodadas (6,7â†’8,2â†’9,2/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o (novo mÃ³dulo Terraform, 2 Lambdas, `CloseOrganizationService`) fica para sessÃ£o dedicada futura |
| `reviews/responsibility-reassignment-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-122) | ReatribuiÃ§Ã£o de responsabilidade quando um Membership Ã© removido/sai â€” precondiÃ§Ã£o bloqueante best-effort via GSI1 jÃ¡ existente (zero GSI novo), 3 rodadas (7,0â†’8,1â†’9,1/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/data-export-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-123) | ExportaÃ§Ã£o de dados (CSV) de `ExpirationItem` â€” `item:export`/`ADMIN_ROLES`, handler sÃ­ncrono via `queryGsi1` com orÃ§amento decrescente + guard de 4 MB, `csv-export-writer.ts` (RFC4180+mitigaÃ§Ã£o de fÃ³rmula), 3 rodadas (6,2â†’8,1â†’9,1/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/search-and-filters-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-194) | Busca e filtros documentais (roadmap P0.5) â€” uniÃ£o discriminada Subject/Requirement/ExpirationItem via GSI7/GSI1 jÃ¡ tenant-facing (zero GSI novo), `Requirement.assigneeUserId` (mecanismo completo transportado de D-122/D-125), `UnifiedValidityState` derivado read-time. RÃ©gua reconciliada na Rodada 2 (9,6/10 estÃ¡vel), 5 rodadas (design 4,8â†’6,2â†’8,1â†’8,8â†’9,2/9,3 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO em 2026-08-20 (9/9 avaliados, sÃ³ Contexto bateu o gate â‰¥9.0 originalmente) â€” achados remediados/reavaliados em rodadas posteriores por eixo, ver decisions-log.md/pilot-readiness-program.md, nÃ£o re-enumerado aqui
Programas posteriores:    test-engineering-standard.md (E-010, APPROVED), logging-observability-standard.md (E-011, APPROVED), definition-of-done.md (E-012, APPROVED â€” gate por item de todo list; emenda E-013 tornando a aplicaÃ§Ã£o de test-engineering-standard.md operacional, nÃ£o sÃ³ referencial), research-protocol.md (E-014, APPROVED â€” pesquisa externa exigida/checklist de nota antes de decisÃµes Type 1 que dependem de padrÃ£o de mercado), Consolidation + Pilot Readiness Program (pilot-readiness-program.md/pilot-readiness-assessment.md, CONDITIONAL GO)
Last verified:            2026-09-08 (E-023 â€” full-audit round2, eixo Qualidade de Engenharia, reaberto (Round 1 fechou 2026-08-20 em 8,199/7,788); melhora real (Claude 8,199â†’8,32, Codex 7,788â†’8,341), gate â‰¥9,0 ainda nÃ£o atingido, convergido em 2 rodadas por acordo mÃºtuo explÃ­cito do Codex; achado real novo: suÃ­te completa rodou 2624/2625 nesta sessÃ£o por corrida intermitente entre 2 arquivos de teste de arquitetura (`system-mutation-allowlist.test.ts`/`tenant-fence-boundary.test.ts`) cuja mitigaÃ§Ã£o jÃ¡ tentada (fixtures movidas, `fileParallelism: false`) nÃ£o foi suficiente â€” causa raiz nÃ£o identificada, PENDENTE; achado do Round1 sobre logger sem `correlationId` automÃ¡tico confirmado corrigido de fato (`AsyncLocalStorage` real); 3 achados mecÃ¢nicos do Codex corrigidos nesta sessÃ£o (contagem desatualizada em `test-engineering-standard.md`, comentÃ¡rio do dependency-cruiser, CycloneDX sem versÃ£o pinada) â€” ver reviews/full-audit-round2-qualidade-summary.md. E-022 â€” full-audit round2, eixo Engenharia de Contexto, reaberto (Round 1 fechou 2026-08-20 em 9,08/9,09); gate â‰¥9,0 NÃƒO atingido, convergido em 3 rodadas, nota final 6,84/10 â€” achado central: `NEXT_SESSION_PROMPT.md` cumpre o guardrail de linhas mas nÃ£o sua intenÃ§Ã£o (~134 KB), guardrail mede sÃ³ linhas; 5 achados factuais mecÃ¢nicos corrigidos em `docs/architecture/README.md` no processo â€” ver reviews/full-audit-round2-contexto-summary.md. **Achado central CORRIGIDO, D-236, 2026-09-08**: `NEXT_SESSION_PROMPT.md` recompactado (299 linhas/~145.804 bytes â†’ 87 linhas/~12.304 bytes) e `scripts/check-doc-drift.ts` ganhou guardrail de densidade (bytes+palavras, independente de contagem de linhas) â€” ver `docs/engineering/decisions-log.md` E-022 e `docs/architecture/decisions-log.md` D-236; gate â‰¥9,0 nÃ£o reavaliado por rodada nova nesta correÃ§Ã£o. E-021 â€” full-audit round2, eixo Arquitetura, reaberto pelo volume de mudanÃ§a de escopo desde round1 (D-084 a D-231+: Multi-User B2B completo, Document Archive completo, ADR-0009 substitui CDK por Terraform); melhora real (Claude 7,966â†’8,55, Codex 8,743â†’8,782), gate â‰¥9,0 ainda nÃ£o atingido, convergido em 3 rodadas; achado real novo nÃ­vel 5 (nÃ£o corrigido, PENDENTE): `src/workers/guest-credential-delivery/deliver.ts` reivindica o marcador idempotente antes do envio SES â€” falha entre claim e send perde a entrega de credencial guest permanentemente e silenciosamente, sem alarme dedicado, redrive nativo neutralizado pelo prÃ³prio claim â€” ver reviews/full-audit-round2-arquitetura-summary.md. E-020 â€” full-audit round2, eixo OperaÃ§Ãµes/SRE e Continuidade de NegÃ³cio, reaberto por crescimento de escopo (32â†’61 Lambdas, rollback.yml novo); gate â‰¥9,0 nÃ£o atingido (Claude 5,18â†’4,36, Codex 4,36, convergido em 2 rodadas); achado real nÃ­vel 5: rollback.yml hardcoda "exatamente 13 funÃ§Ãµes" no manifesto mas o manifesto real tem 34 â€” rollback falha antes de reverter qualquer alias, bug estÃ¡tico nÃ£o dependente de incidente real â€” ver reviews/full-audit-round2-operacoes-summary.md. E-019 â€” full-audit round2, eixo GovernanÃ§a JurÃ­dica/Contratual/Terceiros, reaberto por D-231 (primeiro secret de vendor externo real, Meta Cloud API); gate â‰¥9.0 ainda nÃ£o atingido (Claude 4,73â†’5,247, Codex 5,163â†’5,275, convergido em 2 rodadas); E-018 â€” full-audit round2, eixo SeguranÃ§a da InformaÃ§Ã£o e AppSec, ver decisions-log.md)
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Marcar um item de todo list de cÃ³digo como `completed` | `definition-of-done.md` â€” gate por item (nÃ£o por PR/wave inteira): unidade de conclusÃ£o, gate por nÃ­vel de risco, registro mÃ­nimo de evidÃªncia |
| Decidir se uma proposta Type 1 precisa de pesquisa externa antes da Rodada 1, e como isso vira critÃ©rio de nota | `research-protocol.md` â€” critÃ©rio decidÃ­vel (nÃ­vel 5-6 + padrÃ£o externo estabelecido), declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO`, checklist de critÃ©rios derivado da pesquisa, fluxo de reconciliaÃ§Ã£o se o Codex contestar o checklist |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `definition-of-done.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,2, E-012; emenda E-013) | Definition of Done por item de todo list â€” granularidade ("unidade de conclusÃ£o"), gate por nÃ­vel de risco, classificaÃ§Ã£o de risco na prÃ¡tica, registro mÃ­nimo de evidÃªncia; complementa `quality-gate-tiers.md`/`change-risk-scale.md` sem substituÃ­-los. E-013 tornou a aplicaÃ§Ã£o de `test-engineering-standard.md` operacional (G-V3 mutaÃ§Ã£o nomeada por escrito), nÃ£o sÃ³ referencial |
| `research-protocol.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2, E-014) | Pesquisa externa antes de decisÃµes Type 1 â€” gatilho acoplado a `change-risk-scale.md` nÃ­vel 5-6 + Rodada 1 do protocolo (nunca uma disciplina paralela para lembrar); declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO` com fonte+data+representatividade; checklist de critÃ©rios de nota derivado da pesquisa, sub-rubrica subordinada a `joint-review-criteria.md`; fluxo de reconciliaÃ§Ã£o quando o Codex contesta o checklist em si |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `logging-observability-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, gate elevado 9,5/10) | RÃ©gua concreta de qualidade para logging/tracing/taxonomia de erro (`src/shared/observability/**`, `app-error.ts`, wiring Terraform de detecÃ§Ã£o) â€” 8 critÃ©rios ponderados, Ã¢ncoras de pontuaÃ§Ã£o, gate de auditoria 9,0/10, escrita em resposta a achados reais de uma rodada Codex de 2026-08-29 |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`docs/project/handoffs/expiration-tracker-next-days-master-plan-and-ai-prompt.md`, movido da raiz em 2026-08-29) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

4. **Reabertura de um eixo jÃ¡ fechado** (`full-audit-round2-<eixo>-*`, mesmo padrÃ£o de nomenclatura acima, "round2" substitui "round1" no prefixo, nÃ£o sufixo `-round2.md` isolado como o de uma nota Claude dentro da mesma rodada) â€” usada quando a recomendaÃ§Ã£o de fechamento de um `full-audit-round1-<eixo>-summary.md` cita uma condiÃ§Ã£o objetiva de reabertura (ex.: "reabrir quando X for implementado") e essa condiÃ§Ã£o se materializa. Casos reais: `full-audit-round2-privacidade-*` (E-015, 2026-09-07), reaberto porque a condiÃ§Ã£o nomeada no fechamento round1 (purge worker real) foi satisfeita por D-179/D-190; `full-audit-round2-produto-multitenant-*` (E-016, 2026-09-07), reaberto pelo volume de mudanÃ§a de escopo desde round1 (Document Archive D-143â€“D-226, WhatsApp, B2B-7 a B2B-14) â€” melhora real (4,65â†’7,1/7,8), gate ainda nÃ£o atingido, nenhum vazamento cross-tenant confirmado nesta rodada; `full-audit-round2-governanca-ia-*` (E-017, 2026-09-08); `full-audit-round2-seguranca-*` (E-018, 2026-09-08); `full-audit-round2-juridico-*` (E-019, 2026-09-08), reaberto por D-231 (primeiro secret de vendor externo real, Meta Cloud API) â€” gate ainda nÃ£o atingido (5,247/5,275), convergido em 2 rodadas; `full-audit-round2-arquitetura-*` (E-021, 2026-09-08), reaberto pelo volume de mudanÃ§a de escopo desde round1 (D-084 a D-231+) â€” melhora real (7,966/8,743â†’8,55/8,782), gate ainda nÃ£o atingido, convergido em 3 rodadas, achado novo real nÃ­vel 5 (claim-before-send em `guest-credential-delivery/deliver.ts`, perda silenciosa de entrega, PENDENTE para decisÃ£o de Marcelo).

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.

# Escala de Risco de MudanÃ§a â€” Expiration Tracker

> Formaliza (padrÃ£o adotado do `event-discovery-platform`, escala NÃ­vel 1-6 de lÃ¡) o que `AGENTS.md` Â§4 jÃ¡ distinguia de forma binÃ¡ria ("Type 1" vs "correÃ§Ã£o mecÃ¢nica"). A escala abaixo Ã© a rÃ©gua concreta para decidir isso sem depender de julgamento caso a caso repetido.

| NÃ­vel | Exemplo neste projeto | Exige |
|---|---|---|
| 1 â€” CosmÃ©tico | Typo em comentÃ¡rio, formataÃ§Ã£o, nome de variÃ¡vel local | Nada alÃ©m do lint normal |
| 2 â€” CorreÃ§Ã£o mecÃ¢nica | Bug de teste (nÃ£o de produto), ajuste de import, versÃ£o de dependÃªncia sem mudanÃ§a de comportamento | `AGENTS.md` Â§3 (commit direto em `develop`, sem debate) |
| 3 â€” ImplementaÃ§Ã£o de decisÃ£o jÃ¡ aprovada | Escrever o handler Lambda que implementa um design jÃ¡ fechado no protocolo Claudeâ†”Codex; adicionar teste cobrindo um caso jÃ¡ especificado | Julgamento de engenharia direto; documentar judgment calls nÃ£o previstos no design em `NEXT_SESSION_PROMPT.md`/`session-log.md` |
| 4 â€” Judgment call de baixo risco/alta reversibilidade | Escolha de biblioteca nÃ£o prescrita pelo blueprint (ex. Ajv, Vitest em M0); nome de variÃ¡vel de ambiente; heurÃ­stica interna sem contrato externo | Julgamento de engenharia direto + nota explÃ­cita do porquÃª no commit/relatÃ³rio de sessÃ£o |
| 5 â€” Muda contrato, chave de partiÃ§Ã£o, fronteira de mÃ³dulo, ou Ã© difÃ­cil de reverter | Novo GSI ou mudanÃ§a de chave existente; novo formato de evento/schema; nova exceÃ§Ã£o de particionamento (ex. GSI3/GSI6 globais); mudanÃ§a de regiÃ£o AWS de produÃ§Ã£o | **Protocolo Claudeâ†”Codex obrigatÃ³rio** (`AGENTS.md` Â§4), mÃ­nimo 3 rodadas, nota â‰¥9.0 de ambos |
| 6 â€” DecisÃ£o arquitetural formal | Nova stack tecnolÃ³gica, mudanÃ§a de modelo de dados fundamental, novo domÃ­nio de risco (ex. dado sensÃ­vel novo, novo terceiro com acesso a PII) | Protocolo Claudeâ†”Codex **+ ADR formal** em `docs/architecture/adr/` + atualizaÃ§Ã£o de `decisions-log.md` |

## Regra prÃ¡tica

Ao ficar em dÃºvida entre dois nÃ­veis adjacentes, tratar como o nÃ­vel mais alto â€” o custo de uma rodada de debate a mais Ã© sempre menor que o custo de uma decisÃ£o Type 1 tomada sem revisÃ£o (ver `docs/engineering/principles.md` #2).

Isso nÃ£o substitui o julgamento listado em `AGENTS.md` Â§4 ("aplicar bom senso de engenharia") â€” Ã© uma referÃªncia para calibrar esse julgamento de forma consistente entre sessÃµes.

# Pesquisa Externa antes de DecisÃµes Type 1 â€” Expiration Tracker

**Status: `APPROVED` via protocolo Claudeâ†”Codex (`AGENTS.md` Â§4), 3 rodadas, nota cega cada rodada: Rodada 1 Claude 7,9/Codex 7,7 â†’ Rodada 2 Claude 8,8/Codex 8,7 â†’ Rodada 3 Claude 9,2/Codex 9,2 (fechamento, ambos â‰¥9,0, sem arredondar).** Registrado como `docs/engineering/decisions-log.md` E-014. EvidÃªncia completa das 3 rodadas: `docs/engineering/reviews/research-protocol/`.

Pedido do Marcelo (2026-08-30, no fechamento da Wave B2B-5): formalizar quando pesquisa de padrÃµes/mercado deve informar uma decisÃ£o de arquitetura, do mesmo jeito que `definition-of-done.md` (E-012) formalizou o gate por item de todo list â€” inclusive submetendo a prÃ³pria formalizaÃ§Ã£o ao protocolo Claudeâ†”Codex atÃ© convergÃªncia, mesmo nÃ£o sendo decisÃ£o de arquitetura em si (mesmo precedente de E-012 Â§"Pedido explÃ­cito do Marcelo").

## Regra central

Uma decisÃ£o nÃ­vel 5-6 de `docs/engineering/change-risk-scale.md` (Type 1, jÃ¡ sob o protocolo obrigatÃ³rio de `AGENTS.md` Â§4) que **define um padrÃ£o ou contrato que sistemas fora deste projeto jÃ¡ resolveram de forma amplamente estabelecida** exige, antes da Rodada 1 do protocolo, uma declaraÃ§Ã£o explÃ­cita de pesquisa externa â€” nunca uma omissÃ£o silenciosa.

Este documento nÃ£o cria uma segunda rÃ©gua de risco: ele se acopla Ã  rÃ©gua que jÃ¡ existe (`change-risk-scale.md`) e ao ponto de entrada que jÃ¡ Ã© obrigatÃ³rio (a Rodada 1 do protocolo Claudeâ†”Codex, `AGENTS.md` Â§4) â€” nÃ£o Ã© uma disciplina nova e independente para lembrar, Ã© um campo obrigatÃ³rio dentro de uma disciplina que jÃ¡ Ã© enforced.

## Quando pesquisa Ã© exigida (critÃ©rio decidÃ­vel, nÃ£o julgamento livre)

Pesquisa externa Ã© exigida quando **ambas** as condiÃ§Ãµes valem:

1. A decisÃ£o Ã© nÃ­vel 5-6 de `change-risk-scale.md` (jÃ¡ aciona o protocolo de qualquer forma).
2. A decisÃ£o define, no todo ou em parte, um **padrÃ£o que existe fora deste projeto** â€” um problema que produtos/sistemas estabelecidos jÃ¡ resolveram de forma conhecida (ex.: modelo de permissÃµes RBAC, fluxo de convite/invite com prevenÃ§Ã£o de account-takeover, sessÃ£o/contexto multi-tenant, polÃ­tica de expiraÃ§Ã£o de token, fronteira de sessÃ£o OAuth/cookie/CSRF) â€” e nÃ£o apenas "como ESTE projeto especÃ­fico organiza seus prÃ³prios dados" (ex.: layout de chave DynamoDB, decisÃ£o de qual mÃ³dulo possui um valor default, nome de campo interno).

Regra prÃ¡tica para a linha divisÃ³ria: se a resposta certa depende de "o que a maioria dos produtos/padrÃµes estabelecidos faz aqui" (inclui RFCs/normas de seguranÃ§a, nÃ£o sÃ³ produtos comerciais), pesquisa Ã© exigida. Se a resposta certa depende sÃ³ de "como isso se encaixa no nosso prÃ³prio modelo de dados/mÃ³dulos jÃ¡ existentes", pesquisa nÃ£o ajuda e nÃ£o Ã© exigida â€” mesma lÃ³gica de proporcionalidade de `principles.md` #1, aplicada a research em vez de a mecanismo.

**DecisÃ£o composta (achado real da Rodada 1 do Codex, testado contra D-086/D-053-D-054)**: uma decisÃ£o Type 1 raramente Ã© 100% padrÃ£o-externo ou 100% interna â€” o physical model do Multi-User B2B (D-086) mistura layout de PK/SK (interno) com convite/last-OWNER/revogaÃ§Ã£o de Membership (padrÃ£o SaaS estabelecido); o Full BFF (D-053/D-054) Ã© majoritariamente padrÃ£o externo (OAuth/PKCE/cookie de sessÃ£o â€” RFCs e guias de seguranÃ§a de browser sÃ£o fonte direta do design), mas ainda toca decisÃµes internas (nome de tabela, TTL especÃ­fico). ForÃ§ar uma classificaÃ§Ã£o binÃ¡ria da decisÃ£o INTEIRA erra nos dois casos. A declaraÃ§Ã£o aceita um terceiro valor, `SIM PARCIAL`, com o escopo explÃ­cito de quais sub-decisÃµes sÃ£o informadas por pesquisa e quais sÃ£o puramente internas â€” o checklist da prÃ³xima seÃ§Ã£o cobre sÃ³ a parte `SIM`/`SIM PARCIAL`.

Pesquisa **factual isolada de implementaÃ§Ã£o** (qual Ã© a assinatura atual de uma chamada do AWS SDK, versÃ£o de uma lib) nunca aciona este documento. Mas pesquisa factual que **fundamenta o prÃ³prio padrÃ£o/postura de seguranÃ§a** de uma decisÃ£o Type 1 (ex.: RFC 6749/7636 informando o design de PKCE do Full BFF) conta como a pesquisa que este documento exige, mesmo vindo de fontes "tÃ©cnicas" â€” a distinÃ§Ã£o nÃ£o Ã© "a fonte parece uma RFC ou um blog de produto", Ã© "essa pesquisa decide um padrÃ£o, ou sÃ³ implementa um jÃ¡ decidido".

## DeclaraÃ§Ã£o obrigatÃ³ria na Rodada 1 (nunca omissÃ£o silenciosa)

Todo documento de proposta Rodada 1 de um protocolo Type 1 que se enquadre no critÃ©rio acima inclui, antes da seÃ§Ã£o de escopo, uma linha:

```text
Pesquisa externa considerada: SIM (fontes: <lista com data de acesso>)
                             | SIM PARCIAL (fontes: <lista>; escopo: <quais sub-decisÃµes
                               sÃ£o informadas por pesquisa, quais sÃ£o internas>)
                             | NÃƒO (motivo: <justificativa>)
```

`NÃƒO` Ã© uma resposta vÃ¡lida (ex.: "o padrÃ£o jÃ¡ estÃ¡ fechado internamente por uma decisÃ£o anterior, nÃ£o hÃ¡ ambiguidade que pesquisa resolveria") â€” o que nÃ£o Ã© vÃ¡lido Ã© a ausÃªncia da linha. Mesma disciplina do `DoD:` de `definition-of-done.md`: a linha em si Ã© a trilha auditÃ¡vel mÃ­nima, escrita no momento da proposta, nÃ£o reconstruÃ­da depois. Quando a resposta Ã© `SIM`/`SIM PARCIAL`, a mesma seÃ§Ã£o da proposta inclui logo em seguida o checklist de critÃ©rios de nota derivado da pesquisa (prÃ³xima seÃ§Ã£o), escopado sÃ³ Ã  parte informada por padrÃ£o externo â€” as duas coisas nascem juntas, nunca o checklist como um adendo tardio de uma rodada posterior.

**Pesquisa sem padrÃ£o convergente**: `SIM`/`SIM PARCIAL` nÃ£o pressupÃµe que o mercado concorda â€” Ã s vezes a pesquisa encontra abordagens legitimamente divergentes entre produtos estabelecidos. Nesse caso a declaraÃ§Ã£o registra isso explicitamente ("sem padrÃ£o convergente â€” fontes X/Y/Z discordam") e o checklist da prÃ³xima seÃ§Ã£o vira critÃ©rios de TRADE-OFF explÃ­citos (qual critÃ©rio favorece qual abordagem e por quÃª), nunca finge um consenso que a pesquisa nÃ£o encontrou.

## A pesquisa estabelece os critÃ©rios de nota da rodada â€” nunca julgamento livre depois

Achado do Marcelo (2026-08-30): a nota do protocolo Claudeâ†”Codex hoje Ã© dada por julgamento de cada lado, sem uma rÃ©gua comum declarada antes de avaliar â€” funciona quando a rodada Ã© sobre o prÃ³prio cÃ³digo/dados deste projeto (cada lado lÃª o mesmo diff), mas quando a decisÃ£o se apoia em padrÃ£o de mercado, julgamento livre reabre exatamente a subjetividade que a pesquisa deveria fechar.

Quando a declaraÃ§Ã£o Ã© `SIM`/`SIM PARCIAL`, a pesquisa nÃ£o produz sÃ³ fontes â€” produz um **checklist de critÃ©rios objetivos e pesados**, derivado do que ela encontrou como padrÃ£o estabelecido (ou, no caso de "sem padrÃ£o convergente", como critÃ©rios de trade-off explÃ­citos).

**Hierarquia com `joint-review-criteria.md` (achado real da Rodada 1 do Codex â€” a frase original "vira a rÃ©gua de nota da rodada inteira" contradizia a prÃ³pria declaraÃ§Ã£o de `joint-review-criteria.md` de ser "fonte Ãºnica dos critÃ©rios de avaliaÃ§Ã£o usados nas revisÃµes conjuntas")**: este checklist nÃ£o substitui nem compete com a rubrica fixa dos 9 eixos â€” Ã© uma **sub-rubrica subordinada**, especÃ­fica de UMA decisÃ£o, usada em conjunto com o(s) eixo(s) de `joint-review-criteria.md` que jÃ¡ se aplicariam a essa rodada de qualquer forma (ex.: uma proposta de RBAC continua sendo avaliada tambÃ©m contra o eixo de SeguranÃ§a/GovernanÃ§a de Produto jÃ¡ existente lÃ¡ â€” o checklist de pesquisa Ã© adicional, nÃ£o um substituto). Descartado depois que a decisÃ£o fecha; nunca vira uma seÃ§Ã£o nova permanente de `joint-review-criteria.md` por conta prÃ³pria (isso continua exigindo o procedimento de convergÃªncia prÃ³prio daquele documento, "NÃ£o criar a tabela de critÃ©rios dentro do primeiro doc de auditoria do eixo novo").

Regras de aplicaÃ§Ã£o:

- Os critÃ©rios sÃ£o declarados **na mesma proposta Rodada 1**, ao lado da linha "Pesquisa externa considerada: SIM/SIM PARCIAL" â€” antes de qualquer nota, nunca depois (evita o critÃ©rio ser moldado para justificar uma nota jÃ¡ formada).
- Formato: lista pesada, cada critÃ©rio com peso e uma Ã¢ncora curta do que "atende"/"nÃ£o atende" significa â€” combinando os dois precedentes jÃ¡ formais deste projeto: a tabela de critÃ©rios pesados por eixo de `joint-review-criteria.md` (formato) e os gates binÃ¡rios com Ã¢ncora de `test-engineering-standard.md` (G-V1..G-V6 sÃ£o validade binÃ¡ria, nÃ£o peso â€” a Ã¢ncora "atende/nÃ£o atende" Ã© o que se reaproveita de lÃ¡, nÃ£o o peso).
- Ambos os lados (Claude e Codex) graduam a proposta **contra esses critÃ©rios explÃ­citos** em toda rodada subsequente, nÃ£o contra impressÃ£o geral â€” a justificativa da nota cita qual critÃ©rio foi ou nÃ£o atendido, nÃ£o uma sensaÃ§Ã£o de qualidade.

**ReconciliaÃ§Ã£o quando o Codex contesta o checklist (achado central da Rodada 1 â€” o maior buraco identificado)**: se a crÃ­tica do Codex contesta critÃ©rios/pesos do checklist original (nÃ£o sÃ³ a nota dada contra ele), a proposta **nÃ£o pode ser aprovada contra o checklist contestado naquela mesma rodada** â€” a rÃ©gua em si estÃ¡ em disputa, nÃ£o sÃ³ o design. Fluxo:

1. A rodada que contesta registra a nota como **duas partes separadas, sÃ³ no artefato daquela rodada** (nunca como coluna do `decisions-log.md` final, ver abaixo): nota da rÃ©gua (o checklist estÃ¡ certo?) e nota do design (dado o checklist como estÃ¡, o design atende?) â€” nunca uma nota Ãºnica misturando as duas enquanto a rÃ©gua estÃ¡ disputada.
2. A rodada seguinte traz um checklist reconciliado (incorporando a contestaÃ§Ã£o) OU uma rejeiÃ§Ã£o ponto a ponto justificada de cada item contestado â€” nunca silÃªncio sobre a contestaÃ§Ã£o.
3. **Gate explÃ­cito de rÃ©gua estÃ¡vel** (achado real da Rodada 2 do Codex â€” "sem contestaÃ§Ã£o aberta" sozinho permite uma rÃ©gua ruim sobreviver por exaustÃ£o textual, nÃ£o por concordÃ¢ncia real): a rÃ©gua sÃ³ Ã© considerada estÃ¡vel quando **ambos os lados registram nota da rÃ©gua â‰¥9,0**, exatamente o mesmo padrÃ£o numÃ©rico jÃ¡ usado para a nota do design em `AGENTS.md` Â§4 â€” nunca "parou de ser contestada" como critÃ©rio informal por si sÃ³. A nota do design sÃ³ conta para o fechamento â‰¥9,0/â‰¥9,0 da decisÃ£o na rodada em que a rÃ©gua jÃ¡ estÃ¡ estÃ¡vel, ou em rodadas posteriores â€” nunca antes (inclui a possibilidade de rÃ©gua e design atingirem â‰¥9,0 na mesma rodada, sem exigir uma rodada extra sÃ³ por formalidade).
4. Se o checklist muda por um achado real de uma rodada (nÃ£o por capricho), a mudanÃ§a Ã© registrada explicitamente como correÃ§Ã£o â€” versÃ£o nova do checklist citada na rodada, nunca uma rÃ©gua trocada silenciosamente para justificar uma nota diferente da anterior.
- O registro final da decisÃ£o (`decisions-log.md`) cita **sÃ³ a nota final normal de Claude/Codex** (a mesma coluna que toda decisÃ£o jÃ¡ registra) **e a versÃ£o final do checklist usado** â€” nunca as notas intermediÃ¡rias de "rÃ©gua vs. design" de rodadas em disputa, que ficam sÃ³ no artefato de review daquela rodada (`docs/architecture/reviews/` ou `docs/engineering/reviews/`, conforme o tipo de decisÃ£o). Isso evita introduzir um formato de coluna novo num log que jÃ¡ tem convenÃ§Ã£o prÃ³pria (D-084/D-066 separam timing de qualidade tÃ©cnica ou usam `APPROVED WITH CONDITIONS`, mas nunca um par numÃ©rico rÃ©gua/design na mesma linha).

## Verificabilidade â€” fonte + data + representatividade, nunca "pesquisei e X Ã© assim"

Toda afirmaÃ§Ã£o vinda de pesquisa externa citada numa proposta cita a fonte real (nome/URL) e a data em que foi consultada, exatamente como o projeto jÃ¡ exige para cÃ³digo ("verificar por leitura direta antes de escrever, nÃ£o repetir uma alegaÃ§Ã£o nÃ£o verificada" â€” achado real da sessÃ£o que produziu D-086â†’D-088). Isto importa mais aqui do que numa alegaÃ§Ã£o sobre o prÃ³prio cÃ³digo: uma pesquisa externa erra por resumo impreciso, desatualizaÃ§Ã£o, ou viÃ©s de vendor de um jeito que ler o cÃ³digo deste repositÃ³rio nÃ£o erra â€” o revisor (Codex, ou uma sessÃ£o futura) precisa conseguir checar a fonte, nÃ£o sÃ³ confiar na sÃ­ntese.

**Fonte + data Ã© necessÃ¡rio, nÃ£o suficiente (achado real da Rodada 1 do Codex)**: a declaraÃ§Ã£o tambÃ©m justifica por que o conjunto de fontes Ã© representativo para o TIPO de decisÃ£o, nÃ£o sÃ³ cita-las. Duas classes concretas:

- **DecisÃµes com postura de seguranÃ§a/identidade** (sessÃ£o, autenticaÃ§Ã£o, convite, revogaÃ§Ã£o): preferir normas/RFCs/OWASP quando existirem, nunca sÃ³ documentaÃ§Ã£o de um vendor especÃ­fico â€” se sÃ³ documentaÃ§Ã£o de vendor estiver disponÃ­vel, a declaraÃ§Ã£o registra isso explicitamente como limitaÃ§Ã£o, nÃ£o esconde.
- **DecisÃµes de padrÃ£o de produto SaaS** (RBAC, onboarding, UX de troca de organizaÃ§Ã£o): 2-3 exemplos de produtos estabelecidos nÃ£o bastam por si sÃ³ se forem todos do mesmo nicho/porte â€” a declaraÃ§Ã£o nomeia por que a amostra escolhida Ã© representativa (ex.: "GitHub/Notion/Linear/Slack cobrem desde ferramentas dev-first atÃ© produtividade geral, reduzindo viÃ©s de nicho Ãºnico"), nÃ£o apenas lista os nomes.

Nunca inventar ou adivinhar uma URL/fonte â€” mesma regra que jÃ¡ vale para toda comunicaÃ§Ã£o com o Marcelo. Uma afirmaÃ§Ã£o de pesquisa sem fonte verificÃ¡vel Ã© tratada como nÃ£o verificada, nÃ£o como fato.

## Papel do Codex na rodada

Quando a Rodada 1 declara `SIM`/`SIM PARCIAL`, a crÃ­tica do Codex avalia trÃªs coisas, nÃ£o sÃ³ o design resultante: (1) a fonte sustenta a afirmaÃ§Ã£o citada? (2) hÃ¡ um padrÃ£o estabelecido relevante que a proposta nÃ£o considerou? (3) os critÃ©rios de nota propostos sÃ£o os certos, pesados corretamente, e verificÃ¡veis â€” o Codex pode contestar o prÃ³prio checklist antes de aplicÃ¡-lo, nÃ£o sÃ³ a nota dada contra ele. Quando a Rodada 1 declara `NÃƒO`, o Codex tem posiÃ§Ã£o aberta para discordar da declaraÃ§Ã£o â€” "esta decisÃ£o deveria ter se apoiado em pesquisa e nÃ£o se apoiou" Ã© um achado de rodada vÃ¡lido, mesma categoria dos achados jÃ¡ vistos em rodadas reais deste projeto (ex.: `UserProfile`/gate de lifecycle da Wave B2B-5, achados encontrados por revisÃ£o antes de fechar).

## Fluxo completo (nenhum mecanismo novo, sÃ³ as peÃ§as que faltavam)

```text
DecisÃ£o nÃ­vel 5-6 identificada
  â†’ pesquisa externa (se o critÃ©rio acima se aplicar) â€” fontes+data+representatividade
    registradas; SIM / SIM PARCIAL (com escopo) / NÃƒO / "sem padrÃ£o convergente"
  â†’ checklist de critÃ©rios de nota derivado da pesquisa, pesado e Ã¢ncorado, subordinado
    ao(s) eixo(s) de joint-review-criteria.md jÃ¡ aplicÃ¡veis
  â†’ proposta Rodada 1 com a linha "Pesquisa externa considerada: ..." + o checklist
  â†’ Codex pode contestar o checklist em si (nÃ£o sÃ³ a nota) â€” se contestar, nota da rÃ©gua
    e nota do design sÃ£o registradas separadas atÃ© o checklist reconciliar
  â†’ protocolo Claudeâ†”Codex atÃ© convergÃªncia, cada rodada graduada contra o checklist
    (mÃ­nimo 3 rodadas, â‰¥9,0 dos dois, sem arredondar â€” AGENTS.md Â§4)
  â†’ sÃ³ entÃ£o implementaÃ§Ã£o
  â†’ fechamento gated por definition-of-done.md (E-012/E-013) por item de todo list
```

Nenhuma etapa aqui Ã© nova â€” pesquisa+declaraÃ§Ã£o+checklist de critÃ©rios (com reconciliaÃ§Ã£o quando disputado) Ã© a peÃ§a que este documento adiciona ao fluxo que jÃ¡ existe.

## O que isso NÃƒO Ã©

- NÃ£o Ã© uma segunda rÃ©gua de risco â€” usa `change-risk-scale.md` como estÃ¡.
- NÃ£o transforma toda pesquisa em gate formal â€” sÃ³ a que informa uma decisÃ£o jÃ¡ nÃ­vel 5-6 (proporcionalidade, `principles.md` #1).
- NÃ£o substitui o protocolo Claudeâ†”Codex â€” Ã© um campo obrigatÃ³rio dentro da Rodada 1 dele, nÃ£o um protocolo paralelo.
- NÃ£o exige um formato de citaÃ§Ã£o acadÃªmico â€” sÃ³ fonte + data + justificativa de representatividade, suficiente para um revisor checar.
- NÃ£o substitui `joint-review-criteria.md` â€” aquele continua "fonte Ãºnica dos critÃ©rios de avaliaÃ§Ã£o usados nas revisÃµes conjuntas" para os 9 eixos fixos; o checklist deste documento Ã© uma sub-rubrica subordinada, especÃ­fica de UMA decisÃ£o, descartada depois que ela fecha â€” nunca uma seÃ§Ã£o nova permanente daquele documento por conta prÃ³pria.
- NÃ£o fica invisÃ­vel por estar indexado sÃ³ depois de aprovado â€” `AGENTS.md` Â§4 e `docs/engineering/README.md` sÃ£o atualizados no fechamento (mesma sequÃªncia de E-012), nÃ£o uma lacuna a mais para lembrar durante o debate em si.

## Exemplos concretos deste projeto (ilustrativos, nÃ£o decisÃµes jÃ¡ tomadas)

### Exemplo 1 â€” `SIM` completo

Wave B2B-7 (RBAC) precisa definir o que `Membership.role = ADMIN` autoriza alÃ©m de `OWNER`/`MEMBER`/`VIEWER` â€” um modelo de permissÃµes Ã© exatamente o tipo de padrÃ£o que produtos estabelecidos (GitHub, Notion, Linear, Slack) jÃ¡ resolveram de forma conhecida. Isso se enquadra no critÃ©rio: nÃ­vel 5-6 (muda a matriz de autorizaÃ§Ã£o real, `authorization.ts`) + padrÃ£o externo estabelecido, decisÃ£o inteira informada por pesquisa (nÃ£o hÃ¡ parte puramente interna significativa aqui). A Rodada 1 dessa proposta declararia `SIM`, citando as fontes reais consultadas (justificando por que a amostra de produtos Ã© representativa, nÃ£o sÃ³ listando nomes), e proporia um checklist como (ilustrativo â€” os pesos/critÃ©rios reais nascem da pesquisa de verdade, nÃ£o deste exemplo):

```text
1. (peso 30%) Hierarquia de papÃ©is sem explosÃ£o combinatÃ³ria â€” ADMIN Ã© um superconjunto
   claro de MEMBER, nÃ£o uma lista paralela de permissÃµes redundante.
2. (peso 25%) Default deny â€” uma aÃ§Ã£o nÃ£o listada explicitamente nunca Ã© permitida por
   omissÃ£o.
3. (peso 25%) NÃ£o quebra `authorize()`/`ACTION_ROLES` existente sem necessidade â€” extensÃ£o,
   nÃ£o reescrita, salvo achado real que exija o contrÃ¡rio.
4. (peso 20%) MudanÃ§a de permissÃ£o Ã© auditÃ¡vel (quem, quando, de que role para que role).
```

Cada rodada subsequente do protocolo Claudeâ†”Codex graduaria a proposta contra estes 4 itens especificamente, nÃ£o contra uma impressÃ£o geral de qualidade.

### Exemplo 2 â€” `SIM PARCIAL` (decisÃ£o composta, retrospectivo contra D-086)

O physical model do Multi-User B2B (D-086) Ã© o caso real que expÃ´s a necessidade deste valor intermediÃ¡rio. Nome/formato do GSI4 (`MembershipByUser`) e layout de `PK`/`SK` de `Membership` sÃ£o decisÃµes internas â€” nenhuma pesquisa de mercado ajudaria a escolher entre `GSI4PK=USER#<userId>` e uma alternativa equivalente, isso depende sÃ³ do resto do schema jÃ¡ existente deste projeto. A postura de isolamento IAM do Ã­ndice (achado real da Rodada 2 do Codex: "least-privilege para Ã­ndice cross-tenant" Ã‰ um padrÃ£o de seguranÃ§a que poderia depender de pesquisa externa, nÃ£o Ã© automaticamente interno sÃ³ por tocar um Ã­ndice) jÃ¡ estava decidida internamente ANTES de D-086, pelo precedente jÃ¡ estabelecido de GSI3/GSI6 (`AGENTS.md` Â§7 â€” "nunca conceder acesso de leitura a um GSI restrito via polÃ­tica geral de tabela") â€” Ã© interna aqui por decisÃ£o anterior jÃ¡ convergida, nÃ£o porque IAM/postura de seguranÃ§a seja categoricamente interna. Mas o mecanismo de `ownerCount`/last-OWNER, a unicidade de convite pendente, e a semÃ¢ntica de revogaÃ§Ã£o de Membership sÃ£o padrÃµes que produtos SaaS multi-tenant estabelecidos jÃ¡ resolveram e nÃ£o tinham precedente interno prÃ©vio. A declaraÃ§Ã£o correta nesse caso seria `SIM PARCIAL (fontes: ...; escopo: mecanismo de last-OWNER e revogaÃ§Ã£o de Membership sÃ£o informados por padrÃ£o externo; layout de chave/GSI e isolamento IAM do Ã­ndice sÃ£o decisÃ£o interna â€” o Ãºltimo por precedente jÃ¡ convergido em GSI3/GSI6, nÃ£o por serem categoricamente internos)` â€” e o checklist de critÃ©rios cobriria sÃ³ a parte de last-OWNER/revogaÃ§Ã£o, nÃ£o o schema inteiro.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "queryGsi3|parseGsi3Sk|parseChasingGsi3Sk|SendMessageBatch|isTransactionCanceled|gsi3PartitionsForMinute|seen|shouldAlarm|scheduler_lag" src test infra docs -g "*.ts" -g "*.tf" -g "*.md"' in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-09-15T01:16:47.958961Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.4 seconds
Total output lines: 426
Output:
docs\project\performance-audit-2026-09-02.md:226:Cada `queryGsi3()` drena todas as páginas daquela partition key antes de retornar.
src\workers\upload-slot-reconciliation\reconciliation.ts:4:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\upload-slot-reconciliation\reconciliation.ts:81:    if (isTransactionCanceled(err)) return; // concurrent sweep already claimed it.
src\workers\upload-slot-reconciliation\reconciliation.ts:96:    if (isTransactionCanceled(err)) return; // document already advanced concurrently - fine, don't clobber it.
src\workers\upload-finalizer\finalizer.ts:38: * used to give up on the FIRST OCC conflict (isTransactionCanceled -> "IGNORED_STALE"),
src\workers\transient-purge\purge.ts:42:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\transient-purge\purge.ts:162:          if (!isTransactionCanceled(err)) throw err;
src\workers\transient-purge\purge.ts:195:          if (!isTransactionCanceled(err)) throw err;
src\workers\transient-purge\purge.ts:230:        if (!isTransactionCanceled(err)) throw err;
src\workers\transient-purge\purge.ts:285:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\transient-purge\dynamodb-candidate-source.ts:13:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\transient-purge\dynamodb-candidate-source.ts:175:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\transient-purge\dynamodb-candidate-source.ts:176:      if (isTransactionCanceled(err)) throw err;
src\shared\tenant-lifecycle\tenant-business-mutation.ts:28:import { buildExistenceConditionCheck, isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";
src\shared\tenant-lifecycle\tenant-business-mutation.ts:193:    if (isTransactionCanceled(err)) {
src\shared\tenant-lifecycle\tenant-business-mutation.ts:259:    if (isTransactionCanceled(err)) return { ok: false, reason: "OCC_CONFLICT" };
src\shared\tenant-lifecycle\system-mutation.ts:56:  isTransactionCanceled,
src\shared\tenant-lifecycle\system-mutation.ts:369:    if (isTransactionCanceled(err)) {
test\unit\reminder\producer.test.ts:49:  async queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: { gsi3pk: string }) {
test\unit\reminder\producer.test.ts:50:    return this.inner.queryGsi3<T>(input);
test\unit\reminder\producer-chasing-branch.test.ts:14:import { runProducerTick, shouldAlarm, type ProducerTickResult } from "../../../src/workers/reminder-producer/producer.js";
test\unit\reminder\producer-chasing-branch.test.ts:193:describe("shouldAlarm - the exact regression a Codex adversarial review found (D-039/D-046/D-048)", () => {
test\unit\reminder\producer-chasing-branch.test.ts:208:    expect(shouldAlarm(baseResult())).toEqual({ alarm: false });
test\unit\reminder\producer-chasing-branch.test.ts:212:    const result = shouldAlarm(baseResult({ unknownEntityType: 2 }));
test\unit\reminder\producer-chasing-branch.test.ts:218:    const result = shouldAlarm(baseResult({ failed: [{ occurrenceId: "o1", tenantId: "t1", error: new Error("boom") }] }));
test\unit\reminder\producer-chasing-branch.test.ts:224:    const result = shouldAlarm(baseResult({ unknownEntityType: 1, failed: [{ occurrenceId: "o1", tenantId: "t1", error: new Error("boom") }] }));
test\unit\dispatch-outbox-relay\relay.test.ts:178:    const seenCorrelationIds: string[] = [];
test\unit\dispatch-outbox-relay\relay.test.ts:183:          seenCorrelationIds.push(correlationId);
test\unit\dispatch-outbox-relay\relay.test.ts:191:    expect(seenCorrelationIds).toEqual(["cor_original"]);
test\unit\dispatch-outbox-relay\relay.test.ts:196:    const seenCorrelationIds: string[] = [];
test\unit\dispatch-outbox-relay\relay.test.ts:201:          seenCorrelationIds.push(correlationId);
test\unit\dispatch-outbox-relay\relay.test.ts:209:    expect(seenCorrelationIds).toEqual(["evt-legacy"]);
test\unit\reminder\producer-burst.test.ts:106:    // racing the `seen` dedup guard or the DynamoDB conditional claim itself).
src\shared\observability\redactor.ts:103:  private redactValue(value: unknown, depth: number, seen: WeakSet<object>): unknown {
src\shared\observability\redactor.ts:120:      if (seen.has(value)) return "[CIRCULAR]";
src\shared\observability\redactor.ts:121:      seen.add(value);
src\shared\observability\redactor.ts:122:      return value.slice(0, this.limits.maxArrayLength).map((v) => this.redactValue(v, depth + 1, seen));
src\shared\observability\redactor.ts:125:      if (seen.has(value as object)) return "[CIRCULAR]";
src\shared\observability\redactor.ts:126:      seen.add(value as object);
src\shared\observability\redactor.ts:133:          out[key] = this.redactValue(v, depth + 1, seen);
src\workers\security-audit-purge\purge.ts:47:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\security-audit-purge\purge.ts:161:        if (!isTransactionCanceled(err)) throw err;
src\workers\security-audit-purge\purge.ts:218:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\security-audit-purge\dynamodb-candidate-source.ts:13:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\security-audit-purge\dynamodb-candidate-source.ts:139:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\security-audit-purge\dynamodb-candidate-source.ts:140:      if (isTransactionCanceled(err)) throw err;
test\unit\reminder\in-memory-store.ts:146:  async queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]> {
test\unit\context.test.ts:16:    const seen: Array<string | undefined> = [];
test\unit\context.test.ts:24:        seen.push(getContext()?.correlationId);
test\unit\context.test.ts:27:    expect(seen).toEqual(["cor_a", "cor_b", "cor_c"]);
test\unit\workers\document-purge.test.ts:2:import { buildVersionedUpdate, isTransactionCanceled } from "../../../src/shared/dynamodb/occ.js";
test\unit\workers\document-purge.test.ts:227:      rejected = isTransactionCanceled(err);
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:11:describe("DynamoDbReminderProducerStore.queryGsi3 — security audit trail", () => {
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:20:    const items = await store.queryGsi3({ gsi3pk: "DUE#..." });
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:35:    await expect(store.queryGsi3({ gsi3pk: "DUE#..." })).rejects.toThrow();
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:50:    await expect(store.queryGsi3({ gsi3pk: "DUE#..." })).rejects.toThrow();
src\workers\scheduled-reports\scheduler.ts:26:import { isTransactionCanceled, type ReportSubscriptionStore } from "../../modules/reports/ports/report-subscription-store.js";
src\workers\scheduled-reports\scheduler.ts:121:        if (isTransactionCanceled(err)) {
src\workers\scheduled-reports\scheduler.ts:136:/** Pure alarm decision, mirrors `reminder-producer/producer.ts`'s `shouldAlarm` - extracted so the
src\workers\scheduled-reports\scheduler.ts:139:export function shouldAlarmScheduledReports(result: ScheduledReportsTickResult): { alarm: boolean; reason?: string } {
src\shared\idempotency\idempotency.ts:11:import { isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";
src\shared\idempotency\idempotency.ts:123:      if (isTransactionCanceled(err)) return false;
src\workers\requirement-reindex\reindex.ts:24:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\requirement-reindex\reindex.ts:111:        if (isTransactionCanceled(err)) {
test\unit\bff\proxy-service.test.ts:48:    let seenHeaders: Record<string, string> = {};
test\unit\bff\proxy-service.test.ts:51:        seenHeaders = input.headers;
test\unit\bff\proxy-service.test.ts:57:    expect(seenHeaders["authorization"]).toBe("Bearer server-side-secret-token");
test\unit\bff\proxy-service.test.ts:61:    let seenHeaders: Record<string, string> = {};
test\unit\bff\proxy-service.test.ts:64:        seenHeaders = input.headers;
test\unit\bff\proxy-service.test.ts:74:    expect(seenHeaders["content-type"]).toBe("application/json");
test\unit\bff\proxy-service.test.ts:75:    expect(seenHeaders["if-match"]).toBe("3");
test\unit\bff\proxy-service.test.ts:76:    expect(seenHeaders["x-forwarded-for"]).toBeUndefined();
test\unit\bff\proxy-service.test.ts:77:    expect(seenHeaders["cookie"]).toBeUndefined();
test\unit\bff\proxy-service.test.ts:81:    let seenHeaders: Record<string, string> = {};
test\unit\bff\proxy-service.test.ts:84:        seenHeaders = input.headers;
test\unit\bff\proxy-service.test.ts:94:    expect(seenHeaders["idempotency-key"]).toBe("client-generated-key-1");
test\unit\bff\proxy-service.test.ts:126:    let seenUrl = "";
test\unit\bff\proxy-service.test.ts:129:        seenUrl = input.url;
test\unit\bff\proxy-service.test.ts:135:    expect(seenUrl).toBe("https://api.example.com/items/dashboard?status=overdue");
src\workers\requirement-evidence-refresh\refresh.ts:26:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\requirement-evidence-refresh\refresh.ts:118:      if (!isTransactionCanceled(err)) throw err;
src\workers\requirement-evidence-refresh\refresh.ts:181:  // unlinkEvidence, requirement-reindex). Throws (isTransactionCanceled) on a lost OCC race,
infra\modules\reminder-observability\main.tf:18:    # entityType (producer.ts's `shouldAlarm()`) - already covered by the ReminderProducer
src\shared\dynamodb\tenant-purge-scan.ts:140: * `TransactionCanceledException` must reach the caller intact so `isTransactionCanceled()` can
src\workers\report-subscription-delivery\delivery.ts:36:import { isTransactionCanceled, type ReportSubscriptionStore } from "../../modules/reports/ports/report-subscription-store.js";
src\workers\report-subscription-delivery\delivery.ts:106: * "first-writer-wins, everyone else reuses the frozen row" (`isTransactionCanceled` on a
src\workers\report-subscription-delivery\delivery.ts:139:    if (!isTransactionCanceled(err)) throw err;
src\workers\report-subscription-delivery\delivery.ts:174:      if (!isTransactionCanceled(err)) throw err;
src\workers\report-subscription-delivery\delivery.ts:229:    if (isTransactionCanceled(err)) return false;
src\workers\report-subscription-delivery\delivery.ts:257:    if (isTransactionCanceled(err)) return "LOST_RACE";
src\workers\report-subscription-delivery\delivery.ts:283:    if (!isTransactionCanceled(err)) throw err;
test\unit\subject\document-chasing.test.ts:2:import { chasingGsi3Keys, parseChasingGsi3Sk, buildChasingClaimGsi6Sk, documentChasingOccurrenceKey, documentChasingIntentKey } from "../../../src/modules/subject/domain/document-chasing.js";
test\unit\subject\document-chasing.test.ts:3:import { parseGsi3Sk } from "../../../src/modules/reminder/domain/gsi3-parse.js";
test\unit\subject\document-chasing.test.ts:12:    expect(() => parseGsi3Sk(keys.GSI3SK)).toThrow(/Malformed GSI3SK/);
test\unit\subject\document-chasing.test.ts:15:  it("parseChasingGsi3Sk round-trips a real chasing key and rejects a reminder-shaped one (never throws)", () => {
test\unit\subject\document-chasing.test.ts:17:    expect(parseChasingGsi3Sk(keys.GSI3SK)).toEqual({ tenantId: "t1", occurrenceId: "occ-1" });
test\unit\subject\document-chasing.test.ts:18:    expect(parseChasingGsi3Sk("TENANT#t1#OCCURRENCE#occ-1")).toBeUndefined();
test\unit\subject\document-chasing.test.ts:19:    expect(parseChasingGsi3Sk("garbage")).toBeUndefined();
src\shared\dynamodb\sdk-errors.ts:10:import { isConditionalCheckFailed, isTransactionCanceled } from "./occ.js";
src\shared\dynamodb\sdk-errors.ts:56:  // isTransactionCanceled()/isConditionalCheckFailed() themselves before reaching this
src\shared\dynamodb\sdk-errors.ts:62:export { isConditionalCheckFailed, isTransactionCanceled };
src\workers\reminder-reconciliation\reconciliation.ts:22:import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
src\workers\reminder-reconciliation\reconciliation.ts:103:      if (!isTransactionCanceled(err)) throw err;
src\workers\reminder-reconciliation\reconciliation.ts:183:            if (!isTransactionCanceled(err)) throw err; // lost a race - fine, not our job to force it
src\workers\reminder-reconciliation\reconciliation.ts:204:        if (!isTransactionCanceled(err)) throw err;
src\shared\dynamodb\occ.ts:422:export function isTransactionCanceled(err: unknown): boolean {
src\shared\dynamodb\occ.ts:433: * with `isTransactionCanceled` first) - the real AWS SDK error carries one entry per transact
test\unit\organization\update-organization-settings.test.ts:101:  // isTransactionCanceled) faria uma escrita concorrente stale sobrescrever silenciosamente, em
src\workers\reminder-producer\producer.ts:15: *  5. returns counters for `scheduler_lag_seconds`/per-shard/lookback-depth metrics (left to
src\workers\reminder-producer\producer.ts:21: * single `queryGsi3` call for a given shard/minute can return BOTH types mixed together, since
src\workers\reminder-producer\producer.ts:27: * (reminder-producer-handler.ts) throws whenever it's nonzero, via `shouldAlarm()` below, so a
src\workers\reminder-producer\producer.ts:31:import { gsi3PartitionsForMinute } from "../../modules/reminder/domain/reminder-occurrence.js";
src\workers\reminder-producer\producer.ts:32:import { parseGsi3Sk } from "../../modules/reminder/domain/gsi3-parse.js";
src\workers\reminder-producer\producer.ts:34:import { isTransactionCanceled } from "../../modules/reminder/ports/reminder-store.js";
src\workers\reminder-producer\producer.ts:40:import { parseChasingGsi3Sk } from "../../modules/subject/domain/document-chasing.js";
src\workers\reminder-producer\producer.ts:149:export function shouldAlarm(result: ProducerTickResult): { alarm: boolean; reason?: string } {
src\workers\reminder-producer\producer.ts:180:  const seen = new Set<string>();
src\workers\reminder-producer\producer.ts:184:      const partitionKeys = gsi3PartitionsForMinute(minute, generation.shardCount);
src\workers\reminder-producer\producer.ts:187:        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
src\workers\reminder-producer\producer.ts:190:        // why bounded concurrency replaces that here. `seen`/`claimed`/`failed`/
src\workers\reminder-producer\producer.ts:200:          const chasingParsed = parseChasingGsi3Sk(row.GSI3SK);
src\workers\reminder-producer\producer.ts:202:            if (seen.has(chasingParsed.occurrenceId)) return;
src\workers\reminder-producer\producer.ts:203:            seen.add(chasingParsed.occurrenceId);
src\workers\reminder-producer\producer.ts:219:            reminderParsed = parseGsi3Sk(row.GSI3SK);
src\workers\reminder-producer\producer.ts:227:          if (seen.has(occurrenceId)) return;
src\workers\reminder-producer\producer.ts:228:          seen.add(occurrenceId);
src\workers\reminder-producer\producer.ts:310:            if (isTransactionCanceled(err)) {
src\workers\reminder-dispatch\dispatch.ts:30:import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../../modules/reminder/ports/reminder-store.js";
src\workers\reminder-dispatch\dispatch.ts:248:      if (!isTransactionCanceled(err) || !occurrenceConditionFailed) throw err;
src\workers\reminder-dispatch\dispatch.ts:346:    if (isTransactionCanceled(err)) {
src\workers\quota-telemetry-purge\purge.ts:34:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\quota-telemetry-purge\purge.ts:152:        if (!isTransactionCanceled(err)) throw err;
src\workers\quota-telemetry-purge\purge.ts:207:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:12:import { isTransactionCanceled, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:94:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:95:      if (isTransactionCanceled(err)) throw err;
test\unit\reports\scheduler.test.ts:2:import { runScheduledReportsTick, shouldAlarmScheduledReports } from "../../../src/workers/scheduled-reports/scheduler.js";
test\unit\reports\scheduler.test.ts:164:describe("shouldAlarmScheduledReports", () => {
test\unit\reports\scheduler.test.ts:166:    expect(shouldAlarmScheduledReports({ scanned: 1, claimed: 0, skippedConcurrentlyModified: 0, skippedNotDue: 0, failed: [{ subscriptionId: "s", tenantId: "t", error: new Error("x") }], oldestCandidateAgeSeconds: undefined }).alarm).toBe(true);
test\unit\reports\scheduler.test.ts:170:    expect(shouldAlarmScheduledReports({ scanned: 0, claimed: 0, skippedConcurrentlyModified: 0, skippedNotDue: 0, failed: [], oldestCandidateAgeSeconds: undefined }).alarm).toBe(false);
src\workers\membership-purge\purge.ts:26:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\membership-purge\purge.ts:142:          if (!isTransactionCanceled(err)) throw err;
src\workers\membership-purge\purge.ts:181:        if (!isTransactionCanceled(err)) throw err;
src\workers\membership-purge\purge.ts:238:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\membership-purge\dynamodb-candidate-source.ts:12:import { isTransactionCanceled, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\membership-purge\dynamodb-candidate-source.ts:89:      // TransactWriteItems caller in this repo: purge.ts inspects isTransactionCanceled()/
src\workers\membership-purge\dynamodb-candidate-source.ts:92:      if (isTransactionCanceled(err)) throw err;
src\workers\membership-purge\candidate-source.ts:54:   * the poison-record backoff/DLQ move (a single conditioned Update). `isTransactionCanceled`/
src\runtime\aws\handlers\scheduled-reports-scheduler-handler.ts:8:import { runScheduledReportsTick, shouldAlarmScheduledReports } from "../../../workers/scheduled-reports/scheduler.js";
src\runtime\aws\handlers\scheduled-reports-scheduler-handler.ts:32:  const alarm = shouldAlarmScheduledReports(result);
src\runtime\aws\handlers\reminder-producer-handler.ts:10:import { runProducerTick, shouldAlarm } from "../../../workers/reminder-producer/producer.js";
src\runtime\aws\handlers\reminder-producer-handler.ts:54:    // `shouldAlarm()` (função pura, testada diretamente), separada de `failed` (uma linha
src\runtime\aws\handlers\reminder-producer-handler.ts:56:    const alarm = shouldAlarm(result);
src\modules\subject\ports\subject-store.ts:11:export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\workers\document-purge\purge.ts:42:  isTransactionCanceled,
src\workers\document-purge\purge.ts:144:    if (isTransactionCanceled(err)) return "SKIPPED";
src\workers\document-purge\purge.ts:208:    if (isTransactionCanceled(err)) return "SKIPPED";
src\workers\document-purge\purge.ts:247:    if (isTransactionCanceled(err)) return "SKIPPED";
src\workers\document-chasing-dispatch\dispatch.ts:27:import { buildVersionedUpdate, buildVersionedCreate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\document-chasing-dispatch\dispatch.ts:130:      if (!isTransactionCanceled(err)) throw err;
src\workers\document-ch…12436 tokens truncated…lê fresco o `DocumentType` do Document (mesmo padrão de `createDocument()`'s check `DocumentType.status=ACTIVE`, D-175), valida cada valor do corpo contra a definição de campo/opção ATIVA correspondente NAQUELE MOMENTO, e grava numa ÚNICA `TransactWriteItems`: `Update` em `Document` (`ConditionExpression: version = expectedDocumentVersion`) + `ConditionCheck` em `DocumentType` (`version = <versão que o serviço acabou de ler>`). Isso fecha a corrida real que o Codex apontou (cliente lê campo ATIVO, outro request arquiva o campo, primeiro request grava valor sob campo já arquivado) sem exigir que o cliente conheça a versão do DocumentType — o `ConditionCheck` falha a transação inteira (`TransactionCanceledException`, tratado com o `isTransactionCanceled()` já existente) se o `DocumentType` mudou entre a leitura e a escrita, e o handler HTTP responde 409 (mesmo padrão de conflito otimista já usado em toda escrita mutável do projeto).
docs\architecture\reviews\document-type-metadata-scoping\estado-final-consolidado.md:100:- `PATCH /document-archive/documents/{documentId}/metadata-values` (RBAC `docarchive:document-metadata-update`, corpo carrega `expectedDocumentVersion`). O serviço relê fresco o `DocumentType` do Document, valida cada valor contra a definição/opção ATIVA correspondente, e grava numa ÚNICA `TransactWriteItems`: `Update` em `Document` (`ConditionExpression: version = expectedDocumentVersion`) + `ConditionCheck` em `DocumentType` (`version = <versão recém-lida>`) — fecha a corrida (cliente lê campo ativo, outro request arquiva, primeiro grava sob campo já arquivado) sem exigir que o cliente conheça a versão do `DocumentType`. `TransactionCanceledException` tratada via `isTransactionCanceled()` já existente, handler responde 409.
docs\architecture\reviews\performance-audit-2026-09-02-reconciliation\estado-final-consolidado.md:19:| 3 | Reminder Producer: 6 min lookback × 4 shards × 24 queries GSI3 sequenciais, drena todas as páginas por partição antes de avançar, claim+outbox sequencial por ocorrência | **Confirmado** | `producer.ts:128-131` gera `lookback+1` minutos (default 5 → 6 minutos); `producer.ts:145-274` é um triplo loop `for (minute) for (generation) for (gsi3pk)` totalmente sequencial — `await deps.store.queryGsi3(...)` bloqueia antes do próximo `gsi3pk`; dentro de cada partição, o loop `for (const row of rows)` processa `Get` + `TransactWrite` por ocorrência um de cada vez, sem `Promise.all`/concorrência limitada. Shard count default (`shard-config.ts:31`, `DEFAULT_SHARD_COUNT`) confirma a topologia 4-shard citada. |
docs\architecture\reviews\ocr-extraction-document-lifecycle-reconciliation-scoping\round3-claude-final.md:20:  chegar transiciona `SCANNING → CLEAN`; o segundo recebe `isTransactionCanceled` e a chamada
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round2-reconciliation.md:943:.\docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round1-codex-blind-proposal.md:14:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round2-reconciliation.md:1016:.\docs\architecture\reviews\p0-frontend-screens-scoping\stage2-codex-prompt.txt:2:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round2-reconciliation.md:2231:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round1-codex-blind-proposal.md:14:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-selfgrade.md:6:de tipo GSI3, falha parcial de SendMessageBatch, correção de fonte) e aceitei o rebalanceamento
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:27:   uma mensagem por partição elegível (mesmo cálculo de `gsi3PartitionsForMinute` × lookback já
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:31:   uma mensagem de scan por vez, faz UMA página de `queryGsi3` (LIMIT explícito, não "ler tudo"),
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:32:   publica os candidatos dessa página na fila de claim via `SendMessageBatch` (chunks de 10,
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:49:`entityType: "REMINDER" | "CHASING"`), preservando a mesma lógica de `parseGsi3Sk`/
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:50:`parseChasingGsi3Sk`/fail-closed `unknownEntityType` que já existe hoje — só movida do loop do
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:53:`shouldAlarm()`-equivalente no novo consumer.
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:57:scan de minutos sobrepostos do lookback, já um caso hoje coberto pelo `seen`-guard local). A
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:60:`isTransactionCanceled`, tratado como não-falha, EXATAMENTE como o código atual já trata hoje
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:70:alarma via `shouldAlarm()`); idade do candidato mais antigo na fila (`ApproximateAgeOfOldestMessage`,
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:73:`scheduler_lag_seconds` como sinal primário de saúde do pipeline. Redrive automático/alerta é
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:90:   como métrica de saúde, falha parcial de `SendMessageBatch` tratada explicitamente (retry até
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:115:`SendMessageBatch`, `Failed[]` mesmo com HTTP 200 —
docs\architecture\reviews\p0-frontend-screens-scoping\stage1-round1-codex-blind-proposal.md:15:protocol (this repo's AGENTS.md paragraph 4). This is a BLIND round: you have not seen any
docs\architecture\reviews\p0-frontend-screens-scoping\stage1-round1-codex-blind-proposal.md:2475:docs/architecture\decisions-log.md:243:| D-212 | **D-204 fatia 2 (scheduler + claim transacional) — IMPLEMENTADA.** Roadmap P1 item 15 continua saindo do design-only. `runScheduledReportsTick` (`src/workers/scheduled-reports/scheduler.ts`) escaneia GSI8 (`WORK#REPORT_SUBSCRIPTION`) exatamente como `requirement-reindex/reindex.ts` (mesmo cap `MAX_PAGES=25`, mesma paginação), rele fresco cada `ReportSubscription` candidata (GSI8 nunca fonte de elegibilidade, defensivo contra query eventualmente consistente), e comita uma transação de 2 ações por assinatura due — `Update` avançando `nextRunAt`/ponteiro GSI8 (`ConditionCheck` de versão) + `Outbox` `Put` (destino `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`, já aditivo desde D-211) — na MESMA `TransactWriteItems`, mesma disciplina "claim + evento durável atômico" de `reminder-producer/producer.ts` (um claim seguido de SendMessage direto não é atômico; Lambda pode morrer no meio, deixando uma assinatura reivindicada sem entrega enfileirada). Payload do evento deliberadamente mínimo (`runId`/`subscriptionId`/`tenantId`/`scheduledFor`) — decisão 6 de D-204 exige que o worker de entrega (fatia 3, ainda não construído) reresolva `recipientUserIds` FRESCO no envio, então não há valor em embutir um snapshot de `reportTypes`/destinatários aqui; `runId` é gerado AGORA (não pela fatia 3) para que o futuro `ReportSubscriptionRun`/`ReportDeliveryAttempt` compartilhem um id estável entre reentregas SQS at-least-once, mesma postura do `deduplicationKey` de `reminder-producer`. Nova primitiva `nextWeeklyOccurrenceUtc` (`report-subscription-schedule.ts`) computa a próxima ocorrência de `dayOfWeek`(convenção ISO 1=segunda..7=domingo, documentada explicitamente em `ReportSubscription.dayOfWeek` desde D-211 para evitar off-by-one contra `Date.getUTCDay()`)/`localTime`/`timeZone`, reusando `zonedTimeToUtc`/`parseLocalTime`/`addCalendarDays` de `reminder/domain/recurrence.ts` (reuso cross-módulo já estabelecido por `notification/application/quiet-hours.ts` — nenhuma duplicação de lógica DST-aware) mais uma nova primitiva exportada `instantToLocalParts` (direção inversa de `zonedTimeToUtc`, necessária para achar "hoje" no calendário local do assinante antes de buscar a próxima ocorrência). Novo port mínimo `ReportSubscriptionStore` (`get`+`transactWrite` só — criação/gestão de assinatura via rota HTTP fica para uma fatia futura, ainda não decidida) + adapter real `DynamoDbReportSubscriptionStore` + `DynamoDbScheduledReportsCandidateSource` (GSI8 Query, mesmo formato de `requirement-reindex/dynamodb-candidate-source.ts`). Lambda real `scheduled-reports-scheduler-handler` (`timeout_seconds=300`, `gsi8_read_policy_json["report_subscription"]`/`worker_transact_write_policy_json["report_subscription"]` — chaves já provisionadas automaticamente por D-211's `local.gsi8_worker_types` addition, mecânico) + role/policy de `aws_scheduler_schedule` + log group + registrado em `security_audit_observability.global_index_function_names` (exigiu adicionar `"scheduled-reports"` à união fechada `GlobalIndexComponent`, `security-audit.ts`). **Decisão de implementação documentada, não uma nova decisão de arquitetura**: cron do `aws_scheduler_schedule` é DIÁRIO (04:15 UTC), não literalmente semanal apesar do texto da decisão 3 de D-204 dizer "aws_scheduler_schedule único semanal" — lido aqui como descrevendo a cadência da FEATURE (`ReportSubscriptionCadence="WEEKLY"`, decisão 1: cada assinatura dispara ~1x/semana), não a frequência de polling do schedule em si; "único" (1 Lambda/1 schedule/1 scan cross-tenant por execução, "mesmo padrão de requirement-reindex") é a parte vinculante da frase. Um poll diário é o que de fato honra `dayOfWeek`/`localTime`/`timeZone` por assinatura com precisão real (~24h de deriva máxima, igual a todo outro worker GSI8-esparso do projeto) — um poll literalmente semanal dessincronizaria o horário escolhido por cada assinante para o instante único em que o schedule disparasse. Aberto a correção se a leitura literal (cron semanal) era de fato a intenção. `terraform plan` real contra `dev` (`AWS_PROFILE=claude-dev`) confirmou os 12 recursos novos como `+ create` limpo (nenhum recurso pré-existente afetado; os 4 `-/+ destroy and then create replacement` de `aws_lambda_permission` no mesmo plano são débito técnico pré-existente já nomeado em sessão anterior, não relacionados a esta mudança) — `terraform fmt`/`validate` limpos. G-V3: 7 testes novos de `nextWeeklyOccurrenceUtc` (rollover mesma semana/semana seguinte, semântica estritamente-depois-do-instante, convenção ISO dayOfWeek=7≠`Date.getUTCDay()`, timezone-aware cross-fuso, busca a partir da data calendário LOCAL perto de fronteira de dia) + 7 testes de `runScheduledReportsTick` (claim avança nextRunAt/GSI8/escreve evento outbox, ignora não-due, multi-tenant, concorrência perdida no claim, candidato obsoleto nunca reclamado 2x) + 2 de `shouldAlarmScheduledReports`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) e a própria rota HTTP de criação/gestão de `ReportSubscription` (`ADMIN_ROLES`, decisão 1) — nenhuma delas implementada ainda; sem elas, nenhuma assinatura real pode ser criada nem entregue, feature continua não-usável ponta a ponta. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de cadência de cron documentado explicitamente acima) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 2 de D-204)** — pendente fatias 3-4 + rota HTTP de criação, PR/merge/CD | `src/workers/scheduled-reports/{scheduler.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/reports/domain/report-subscription-schedule.ts`, `src/modules/reports/ports/report-subscription-store.ts`, `src/modules/reports/persistence/dynamodb-report-subscription-store.ts`, `src/modules/reminder/domain/recurrence.ts`, `src/runtime/aws/handlers/scheduled-reports-scheduler-handler.ts`, `src/runtime/aws/composition/reports.ts`, `src/shared/observability/security-audit.ts`, `scripts/build-lambdas.ts`, `infra/main.tf` | 2026-09-05 |
docs\architecture\reviews\p0-frontend-screens-scoping\stage1-round1-codex-blind-proposal.md:2497:docs/architecture\decisions-log.md:270:| D-239 | **Follow-up de D-238 (fatia 3/4): propagação de `AuthorizedTenantId` para os key-builders de persistência do módulo subject (organization é a única fatia remanescente).** Réplica mecânica do padrão já aprovado em D-237/D-238 — protocolo Claude↔Codex dispensado (nenhuma forma de acesso worker/queue/guest-token nova que D-237/D-238 não tivessem coberto; a única forma genuinamente nova encontrada, `GuestSubmissionService`/o fluxo de convidado sem `RequestContext` algum, resolveu-se com o mesmo `authorizedTenantIdFromPersistedEntity()` sobre a entidade já lida do repositório — mesmo critério de confiança já estabelecido, não uma decisão de mecanismo nova). **Key-builders alterados** (`src/modules/subject/domain/*.ts`): `subjectKey`/`gsi7Keys`/`subjectExternalIdPointerKey` (tracked-subject.ts), `requirementAssignmentKey` (requirement-assignment.ts), `documentSubmissionKey` (document-submission.ts), `documentRequestKey` (document-request.ts), `documentRequestDeliveryPreferenceKey` (document-request-delivery-preference.ts), `entitlementKey`/`defaultEntitlement` (entitlement.ts), `subjectAuditKey`/`BuildSubjectAuditEventInput.tenantId` (audit-event.ts), `documentChasingOccurrenceKey`/`chasingGsi3Keys`/`buildChasingClaimGsi6Sk`/`documentChasingIntentKey` (document-chasing.ts) — 13 funções. `guestTokenPointerKey` (guest-token.ts) e `parseSubmissionQuarantineKey`/`parseChasingGsi3Sk` (parsers, não builders) deliberadamente NÃO tocados, mesma exceção tenantless já documentada no próprio código (lookup acontece antes de tenantId ser conhecido) e mesma disciplina de D-238 de nunca mudar um parser. **Call sites atualizados**: `subject/application/*.ts` (subject-service, requirement-service, document-request-service, guest-submission-service, advance-after-submission-evidence, document-chasing-materializer, document-chasing-producer — `authorizedTenantId(ctx)` derivado uma vez por método no lado HTTP, `authorizedTenantIdFromPersistedEntity()` no lado sem `RequestContext`: guest token/submission flow lendo `pointer`/`request` já persistidos, e o producer lendo `occurrence`), `workers/document-chasing-dispatch/dispatch.ts` e `workers/{submission-finalizer,submission-malware-result}/*.ts` (lado worker/SQS/S3-GuardDuty, `authorizedTenantIdFromPersistedEntity(command)` ou sobre o resultado do parser de quarantine-key, mesmo critério de proveniência que `upload-finalizer-handler.ts`/`malware-result-handler.ts` já usam para o branch document-archive), `runtime/aws/handlers/{upload-finalizer-handler,malware-result-handler}.ts` (branch subject dos dois handlers). **Achados cross-módulo que D-238 não previu, tratados com o mesmo padrão sem rodada nova**: `import/application/resolve-subject-references.ts` (chamado por `import-parse-service.ts`, que já usava `authorizedTenantIdFromPersistedEntity({tenantId})` para o resolver irmão de document-archive — mesmo tratamento aplicado aqui) e `organization/application/create-organization.ts` (`defaultEntitlement(organizationId, now)` na criação de tenant novo — `organizationId` é gerado pelo próprio servidor via `ids.newOrganizationId()`, nunca client-supplied, mesma proveniência de confiança que `authorizedTenantIdFromPersistedEntity()` normalmente exige de uma leitura de repositório, aplicada aqui a um valor recém-gerado em vez de relido). Nenhum terceiro construtor foi necessário. Cast `as AuthorizedTenantId` continua restrito a `authorization.ts` — grep confirma zero ocorrências novas. **Testes**: fixtures ajustadas (subject-service, requirement-service, subject-search, document-chasing/-dispatch/-reconciliation/-materializer, document-request-delivery-preference, submission-finalizer, submission-malware-result, security-audit-purge-pointer-creation, e os testes cross-módulo de import/organization) para construir `AuthorizedTenantId` via `authorizedTenantIdFromPersistedEntity({tenantId: "..."})`, nunca um cast local novo. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa **2786/2786** (239 arquivos, sem novos testes — só fixtures existentes ajustadas). `terraform`/infra não tocado. **Escopo remanescente, rastreado explicitamente**: os key-builders do módulo organization continuam aceitando `tenantId: string` puro — única fatia restante (fatia 4/4), mesma ordem de esforço. | Nível 3-4 (réplica mecânica de padrão já aprovado, mesmo módulo central de origem) | N/A (protocolo dispensado — decisão de design já registrada em D-237/D-238) | N/A | **IMPLEMENTADO (slice subject apenas — organization é o único follow-up rastreado remanescente, ver `NEXT_SESSION_PROMPT.md`)** | `src/modules/subject/domain/*.ts`, `src/modules/subject/application/*.ts`, `src/workers/{document-chasing-dispatch,submission-finalizer,submission-malware-result}/*.ts`, `src/runtime/aws/handlers/{upload-finalizer-handler,malware-result-handler}.ts`, `src/modules/import/application/resolve-subject-references.ts`, `src/modules/import/application/import-parse-service.ts`, `src/modules/organization/application/create-organization.ts` | 2026-09-08 |
docs\architecture\reviews\multi-user-b2b-wave-b2b12-scoping\round-3-claude-proposal.md:24:`isTransactionCanceled()`/`getCancellationReasonCodes()`/builders de update condicional,
rg: docs\architecture\history\cost-model\cost-model-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\slo\slo-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\claude-architecture-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\codex-architecture-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\slo\slo-codex-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\red-team-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\red-team-codex-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\round2-claude-critique.md: Acesso negado. (os error 5)
rg: docs\architecture\history\quality-criteria\claude-quality-criteria-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\round3-treplica.md: Acesso negado. (os error 5)
rg: docs\architecture\history\disaster-recovery\disaster-recovery-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\privacy-lgpd\privacy-lgpd-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\quality-criteria\codex-quality-criteria-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\disaster-recovery\disaster-recovery-codex-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\data-model\data-model-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\data-model\data-model-codex-round1.md: Acesso negado. (os error 5)

 exited 1 in 350ms:
docs\project\performance-audit-2026-09-02.md:226:Cada `queryGsi3()` drena todas as páginas daquela partition key antes de retornar.
src\workers\upload-slot-reconciliation\reconciliation.ts:4:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\upload-slot-reconciliation\reconciliation.ts:81:    if (isTransactionCanceled(err)) return; // concurrent sweep already claimed it.
src\workers\upload-slot-reconciliation\reconciliation.ts:96:    if (isTransactionCanceled(err)) return; // document already advanced concurrently - fine, don't clobber it.
src\workers\upload-finalizer\finalizer.ts:38: * used to give up on the FIRST OCC conflict (isTransactionCanceled -> "IGNORED_STALE"),
src\workers\transient-purge\purge.ts:42:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\transient-purge\purge.ts:162:          if (!isTransactionCanceled(err)) throw err;
src\workers\transient-purge\purge.ts:195:          if (!isTransactionCanceled(err)) throw err;
src\workers\transient-purge\purge.ts:230:        if (!isTransactionCanceled(err)) throw err;
src\workers\transient-purge\purge.ts:285:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\transient-purge\dynamodb-candidate-source.ts:13:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\transient-purge\dynamodb-candidate-source.ts:175:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\transient-purge\dynamodb-candidate-source.ts:176:      if (isTransactionCanceled(err)) throw err;
src\shared\tenant-lifecycle\tenant-business-mutation.ts:28:import { buildExistenceConditionCheck, isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";
src\shared\tenant-lifecycle\tenant-business-mutation.ts:193:    if (isTransactionCanceled(err)) {
src\shared\tenant-lifecycle\tenant-business-mutation.ts:259:    if (isTransactionCanceled(err)) return { ok: false, reason: "OCC_CONFLICT" };
src\shared\tenant-lifecycle\system-mutation.ts:56:  isTransactionCanceled,
src\shared\tenant-lifecycle\system-mutation.ts:369:    if (isTransactionCanceled(err)) {
test\unit\reminder\producer.test.ts:49:  async queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: { gsi3pk: string }) {
test\unit\reminder\producer.test.ts:50:    return this.inner.queryGsi3<T>(input);
test\unit\reminder\producer-chasing-branch.test.ts:14:import { runProducerTick, shouldAlarm, type ProducerTickResult } from "../../../src/workers/reminder-producer/producer.js";
test\unit\reminder\producer-chasing-branch.test.ts:193:describe("shouldAlarm - the exact regression a Codex adversarial review found (D-039/D-046/D-048)", () => {
test\unit\reminder\producer-chasing-branch.test.ts:208:    expect(shouldAlarm(baseResult())).toEqual({ alarm: false });
test\unit\reminder\producer-chasing-branch.test.ts:212:    const result = shouldAlarm(baseResult({ unknownEntityType: 2 }));
test\unit\reminder\producer-chasing-branch.test.ts:218:    const result = shouldAlarm(baseResult({ failed: [{ occurrenceId: "o1", tenantId: "t1", error: new Error("boom") }] }));
test\unit\reminder\producer-chasing-branch.test.ts:224:    const result = shouldAlarm(baseResult({ unknownEntityType: 1, failed: [{ occurrenceId: "o1", tenantId: "t1", error: new Error("boom") }] }));
test\unit\dispatch-outbox-relay\relay.test.ts:178:    const seenCorrelationIds: string[] = [];
test\unit\dispatch-outbox-relay\relay.test.ts:183:          seenCorrelationIds.push(correlationId);
test\unit\dispatch-outbox-relay\relay.test.ts:191:    expect(seenCorrelationIds).toEqual(["cor_original"]);
test\unit\dispatch-outbox-relay\relay.test.ts:196:    const seenCorrelationIds: string[] = [];
test\unit\dispatch-outbox-relay\relay.test.ts:201:          seenCorrelationIds.push(correlationId);
test\unit\dispatch-outbox-relay\relay.test.ts:209:    expect(seenCorrelationIds).toEqual(["evt-legacy"]);
test\unit\reminder\producer-burst.test.ts:106:    // racing the `seen` dedup guard or the DynamoDB conditional claim itself).
src\shared\observability\redactor.ts:103:  private redactValue(value: unknown, depth: number, seen: WeakSet<object>): unknown {
src\shared\observability\redactor.ts:120:      if (seen.has(value)) return "[CIRCULAR]";
src\shared\observability\redactor.ts:121:      seen.add(value);
src\shared\observability\redactor.ts:122:      return value.slice(0, this.limits.maxArrayLength).map((v) => this.redactValue(v, depth + 1, seen));
src\shared\observability\redactor.ts:125:      if (seen.has(value as object)) return "[CIRCULAR]";
src\shared\observability\redactor.ts:126:      seen.add(value as object);
src\shared\observability\redactor.ts:133:          out[key] = this.redactValue(v, depth + 1, seen);
src\workers\security-audit-purge\purge.ts:47:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\security-audit-purge\purge.ts:161:        if (!isTransactionCanceled(err)) throw err;
src\workers\security-audit-purge\purge.ts:218:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\security-audit-purge\dynamodb-candidate-source.ts:13:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\security-audit-purge\dynamodb-candidate-source.ts:139:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\security-audit-purge\dynamodb-candidate-source.ts:140:      if (isTransactionCanceled(err)) throw err;
test\unit\reminder\in-memory-store.ts:146:  async queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]> {
test\unit\context.test.ts:16:    const seen: Array<string | undefined> = [];
test\unit\context.test.ts:24:        seen.push(getContext()?.correlationId);
test\unit\context.test.ts:27:    expect(seen).toEqual(["cor_a", "cor_b", "cor_c"]);
test\unit\workers\document-purge.test.ts:2:import { buildVersionedUpdate, isTransactionCanceled } from "../../../src/shared/dynamodb/occ.js";
test\unit\workers\document-purge.test.ts:227:      rejected = isTransactionCanceled(err);
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:11:describe("DynamoDbReminderProducerStore.queryGsi3 — security audit trail", () => {
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:20:    const items = await store.queryGsi3({ gsi3pk: "DUE#..." });
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:35:    await expect(store.queryGsi3({ gsi3pk: "DUE#..." })).rejects.toThrow();
test\unit\reminder\dynamodb-reminder-producer-store.test.ts:50:    await expect(store.queryGsi3({ gsi3pk: "DUE#..." })).rejects.toThrow();
src\workers\scheduled-reports\scheduler.ts:26:import { isTransactionCanceled, type ReportSubscriptionStore } from "../../modules/reports/ports/report-subscription-store.js";
src\workers\scheduled-reports\scheduler.ts:121:        if (isTransactionCanceled(err)) {
src\workers\scheduled-reports\scheduler.ts:136:/** Pure alarm decision, mirrors `reminder-producer/producer.ts`'s `shouldAlarm` - extracted so the
src\workers\scheduled-reports\scheduler.ts:139:export function shouldAlarmScheduledReports(result: ScheduledReportsTickResult): { alarm: boolean; reason?: string } {
src\shared\idempotency\idempotency.ts:11:import { isTransactionCanceled, type TransactWriteEntry } from "../dynamodb/occ.js";
src\shared\idempotency\idempotency.ts:123:      if (isTransactionCanceled(err)) return false;
src\workers\requirement-reindex\reindex.ts:24:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\requirement-reindex\reindex.ts:111:        if (isTransactionCanceled(err)) {
test\unit\bff\proxy-service.test.ts:48:    let seenHeaders: Record<string, string> = {};
test\unit\bff\proxy-service.test.ts:51:        seenHeaders = input.headers;
test\unit\bff\proxy-service.test.ts:57:    expect(seenHeaders["authorization"]).toBe("Bearer server-side-secret-token");
test\unit\bff\proxy-service.test.ts:61:    let seenHeaders: Record<string, string> = {};
test\unit\bff\proxy-service.test.ts:64:        seenHeaders = input.headers;
test\unit\bff\proxy-service.test.ts:74:    expect(seenHeaders["content-type"]).toBe("application/json");
test\unit\bff\proxy-service.test.ts:75:    expect(seenHeaders["if-match"]).toBe("3");
test\unit\bff\proxy-service.test.ts:76:    expect(seenHeaders["x-forwarded-for"]).toBeUndefined();
test\unit\bff\proxy-service.test.ts:77:    expect(seenHeaders["cookie"]).toBeUndefined();
test\unit\bff\proxy-service.test.ts:81:    let seenHeaders: Record<string, string> = {};
test\unit\bff\proxy-service.test.ts:84:        seenHeaders = input.headers;
test\unit\bff\proxy-service.test.ts:94:    expect(seenHeaders["idempotency-key"]).toBe("client-generated-key-1");
test\unit\bff\proxy-service.test.ts:126:    let seenUrl = "";
test\unit\bff\proxy-service.test.ts:129:        seenUrl = input.url;
test\unit\bff\proxy-service.test.ts:135:    expect(seenUrl).toBe("https://api.example.com/items/dashboard?status=overdue");
src\workers\requirement-evidence-refresh\refresh.ts:26:import { buildVersionedUpdate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\requirement-evidence-refresh\refresh.ts:118:      if (!isTransactionCanceled(err)) throw err;
src\workers\requirement-evidence-refresh\refresh.ts:181:  // unlinkEvidence, requirement-reindex). Throws (isTransactionCanceled) on a lost OCC race,
infra\modules\reminder-observability\main.tf:18:    # entityType (producer.ts's `shouldAlarm()`) - already covered by the ReminderProducer
src\shared\dynamodb\tenant-purge-scan.ts:140: * `TransactionCanceledException` must reach the caller intact so `isTransactionCanceled()` can
src\workers\report-subscription-delivery\delivery.ts:36:import { isTransactionCanceled, type ReportSubscriptionStore } from "../../modules/reports/ports/report-subscription-store.js";
src\workers\report-subscription-delivery\delivery.ts:106: * "first-writer-wins, everyone else reuses the frozen row" (`isTransactionCanceled` on a
src\workers\report-subscription-delivery\delivery.ts:139:    if (!isTransactionCanceled(err)) throw err;
src\workers\report-subscription-delivery\delivery.ts:174:      if (!isTransactionCanceled(err)) throw err;
src\workers\report-subscription-delivery\delivery.ts:229:    if (isTransactionCanceled(err)) return false;
src\workers\report-subscription-delivery\delivery.ts:257:    if (isTransactionCanceled(err)) return "LOST_RACE";
src\workers\report-subscription-delivery\delivery.ts:283:    if (!isTransactionCanceled(err)) throw err;
test\unit\subject\document-chasing.test.ts:2:import { chasingGsi3Keys, parseChasingGsi3Sk, buildChasingClaimGsi6Sk, documentChasingOccurrenceKey, documentChasingIntentKey } from "../../../src/modules/subject/domain/document-chasing.js";
test\unit\subject\document-chasing.test.ts:3:import { parseGsi3Sk } from "../../../src/modules/reminder/domain/gsi3-parse.js";
test\unit\subject\document-chasing.test.ts:12:    expect(() => parseGsi3Sk(keys.GSI3SK)).toThrow(/Malformed GSI3SK/);
test\unit\subject\document-chasing.test.ts:15:  it("parseChasingGsi3Sk round-trips a real chasing key and rejects a reminder-shaped one (never throws)", () => {
test\unit\subject\document-chasing.test.ts:17:    expect(parseChasingGsi3Sk(keys.GSI3SK)).toEqual({ tenantId: "t1", occurrenceId: "occ-1" });
test\unit\subject\document-chasing.test.ts:18:    expect(parseChasingGsi3Sk("TENANT#t1#OCCURRENCE#occ-1")).toBeUndefined();
test\unit\subject\document-chasing.test.ts:19:    expect(parseChasingGsi3Sk("garbage")).toBeUndefined();
src\shared\dynamodb\sdk-errors.ts:10:import { isConditionalCheckFailed, isTransactionCanceled } from "./occ.js";
src\shared\dynamodb\sdk-errors.ts:56:  // isTransactionCanceled()/isConditionalCheckFailed() themselves before reaching this
src\shared\dynamodb\sdk-errors.ts:62:export { isConditionalCheckFailed, isTransactionCanceled };
src\workers\reminder-reconciliation\reconciliation.ts:22:import { isTransactionCanceled, type ReminderStore } from "../../modules/reminder/ports/reminder-store.js";
src\workers\reminder-reconciliation\reconciliation.ts:103:      if (!isTransactionCanceled(err)) throw err;
src\workers\reminder-reconciliation\reconciliation.ts:183:            if (!isTransactionCanceled(err)) throw err; // lost a race - fine, not our job to force it
src\workers\reminder-reconciliation\reconciliation.ts:204:        if (!isTransactionCanceled(err)) throw err;
src\shared\dynamodb\occ.ts:422:export function isTransactionCanceled(err: unknown): boolean {
src\shared\dynamodb\occ.ts:433: * with `isTransactionCanceled` first) - the real AWS SDK error carries one entry per transact
test\unit\organization\update-organization-settings.test.ts:101:  // isTransactionCanceled) faria uma escrita concorrente stale sobrescrever silenciosamente, em
src\workers\reminder-producer\producer.ts:15: *  5. returns counters for `scheduler_lag_seconds`/per-shard/lookback-depth metrics (left to
src\workers\reminder-producer\producer.ts:21: * single `queryGsi3` call for a given shard/minute can return BOTH types mixed together, since
src\workers\reminder-producer\producer.ts:27: * (reminder-producer-handler.ts) throws whenever it's nonzero, via `shouldAlarm()` below, so a
src\workers\reminder-producer\producer.ts:31:import { gsi3PartitionsForMinute } from "../../modules/reminder/domain/reminder-occurrence.js";
src\workers\reminder-producer\producer.ts:32:import { parseGsi3Sk } from "../../modules/reminder/domain/gsi3-parse.js";
src\workers\reminder-producer\producer.ts:34:import { isTransactionCanceled } from "../../modules/reminder/ports/reminder-store.js";
src\workers\reminder-producer\producer.ts:40:import { parseChasingGsi3Sk } from "../../modules/subject/domain/document-chasing.js";
src\workers\reminder-producer\producer.ts:149:export function shouldAlarm(result: ProducerTickResult): { alarm: boolean; reason?: string } {
src\workers\reminder-producer\producer.ts:180:  const seen = new Set<string>();
src\workers\reminder-producer\producer.ts:184:      const partitionKeys = gsi3PartitionsForMinute(minute, generation.shardCount);
src\workers\reminder-producer\producer.ts:187:        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
src\workers\reminder-producer\producer.ts:190:        // why bounded concurrency replaces that here. `seen`/`claimed`/`failed`/
src\workers\reminder-producer\producer.ts:200:          const chasingParsed = parseChasingGsi3Sk(row.GSI3SK);
src\workers\reminder-producer\producer.ts:202:            if (seen.has(chasingParsed.occurrenceId)) return;
src\workers\reminder-producer\producer.ts:203:            seen.add(chasingParsed.occurrenceId);
src\workers\reminder-producer\producer.ts:219:            reminderParsed = parseGsi3Sk(row.GSI3SK);
src\workers\reminder-producer\producer.ts:227:          if (seen.has(occurrenceId)) return;
src\workers\reminder-producer\producer.ts:228:          seen.add(occurrenceId);
src\workers\reminder-producer\producer.ts:310:            if (isTransactionCanceled(err)) {
src\workers\reminder-dispatch\dispatch.ts:30:import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../../modules/reminder/ports/reminder-store.js";
src\workers\reminder-dispatch\dispatch.ts:248:      if (!isTransactionCanceled(err) || !occurrenceConditionFailed) throw err;
src\workers\reminder-dispatch\dispatch.ts:346:    if (isTransactionCanceled(err)) {
src\workers\quota-telemetry-purge\purge.ts:34:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\quota-telemetry-purge\purge.ts:152:        if (!isTransactionCanceled(err)) throw err;
src\workers\quota-telemetry-purge\purge.ts:207:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:12:import { isTransactionCanceled, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:94:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\quota-telemetry-purge\dynamodb-candidate-source.ts:95:      if (isTransactionCanceled(err)) throw err;
test\unit\reports\scheduler.test.ts:2:import { runScheduledReportsTick, shouldAlarmScheduledReports } from "../../../src/workers/scheduled-reports/scheduler.js";
test\unit\reports\scheduler.test.ts:164:describe("shouldAlarmScheduledReports", () => {
test\unit\reports\scheduler.test.ts:166:    expect(shouldAlarmScheduledReports({ scanned: 1, claimed: 0, skippedConcurrentlyModified: 0, skippedNotDue: 0, failed: [{ subscriptionId: "s", tenantId: "t", error: new Error("x") }], oldestCandidateAgeSeconds: undefined }).alarm).toBe(true);
test\unit\reports\scheduler.test.ts:170:    expect(shouldAlarmScheduledReports({ scanned: 0, claimed: 0, skippedConcurrentlyModified: 0, skippedNotDue: 0, failed: [], oldestCandidateAgeSeconds: undefined }).alarm).toBe(false);
src\workers\membership-purge\purge.ts:26:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\membership-purge\purge.ts:142:          if (!isTransactionCanceled(err)) throw err;
src\workers\membership-purge\purge.ts:181:        if (!isTransactionCanceled(err)) throw err;
src\workers\membership-purge\purge.ts:238:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\membership-purge\dynamodb-candidate-source.ts:12:import { isTransactionCanceled, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\membership-purge\dynamodb-candidate-source.ts:89:      // TransactWriteItems caller in this repo: purge.ts inspects isTransactionCanceled()/
src\workers\membership-purge\dynamodb-candidate-source.ts:92:      if (isTransactionCanceled(err)) throw err;
src\workers\membership-purge\candidate-source.ts:54:   * the poison-record backoff/DLQ move (a single conditioned Update). `isTransactionCanceled`/
src\runtime\aws\handlers\scheduled-reports-scheduler-handler.ts:8:import { runScheduledReportsTick, shouldAlarmScheduledReports } from "../../../workers/scheduled-reports/scheduler.js";
src\runtime\aws\handlers\scheduled-reports-scheduler-handler.ts:32:  const alarm = shouldAlarmScheduledReports(result);
src\runtime\aws\handlers\reminder-producer-handler.ts:10:import { runProducerTick, shouldAlarm } from "../../../workers/reminder-producer/producer.js";
src\runtime\aws\handlers\reminder-producer-handler.ts:54:    // `shouldAlarm()` (função pura, testada diretamente), separada de `failed` (uma linha
src\runtime\aws\handlers\reminder-producer-handler.ts:56:    const alarm = shouldAlarm(result);
src\modules\subject\ports\subject-store.ts:11:export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\workers\document-purge\purge.ts:42:  isTransactionCanceled,
src\workers\document-purge\purge.ts:144:    if (isTransactionCanceled(err)) return "SKIPPED";
src\workers\document-purge\purge.ts:208:    if (isTransactionCanceled(err)) return "SKIPPED";
src\workers\document-purge\purge.ts:247:    if (isTransactionCanceled(err)) return "SKIPPED";
src\workers\document-chasing-dispatch\dispatch.ts:27:import { buildVersionedUpdate, buildVersionedCreate, isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\document-chasing-dispatch\dispatch.ts:130:      if (!isTransactionCanceled(err)) throw err;
src\workers\document-chasing-dispatch\dispatch.ts:178:      if (isTransactionCanceled(err)) return { kind: "ALREADY_TRIGGERED" };
src\workers\document-chasing-dispatch\dispatch.ts:271:    if (isTransactionCanceled(err)) return { kind: "ALREADY_TRIGGERED" };
src\workers\invitation-purge\purge.ts:27:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\invitation-purge\purge.ts:133:          if (!isTransactionCanceled(err)) throw err;
src\workers\invitation-purge\purge.ts:171:        if (!isTransactionCanceled(err)) throw err;
src\workers\invitation-purge\purge.ts:224:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\modules\subject\persistence\dynamodb-subject-store.ts:66:  // TransactionCanceledException precisa chegar intacta ao chamador (isTransactionCanceled())
src\workers\delivery-record-purge\purge.ts:41:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\delivery-record-purge\purge.ts:155:        if (!isTransactionCanceled(err)) throw err;
src\workers\delivery-record-purge\purge.ts:211:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\invitation-purge\dynamodb-candidate-source.ts:12:import { isTransactionCanceled, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\invitation-purge\dynamodb-candidate-source.ts:89:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\invitation-purge\dynamodb-candidate-source.ts:90:      if (isTransactionCanceled(err)) throw err;
src\workers\delivery-record-purge\dynamodb-candidate-source.ts:13:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\delivery-record-purge\dynamodb-candidate-source.ts:102:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\delivery-record-purge\dynamodb-candidate-source.ts:103:      if (isTransactionCanceled(err)) throw err;
src\modules\document\ports\document-store.ts:4:export { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src\workers\dossier-export\generate.ts:20:import { buildVersionedUpdate, isTransactionCanceled, type EntityKey } from "../../shared/dynamodb/occ.js";
src\workers\dossier-export\generate.ts:134:    if (isTransactionCanceled(err)) return undefined;
src\workers\dossier-export\generate.ts:162:    if (!isTransactionCanceled(err)) throw err;
src\workers\document-request-recurrence\materializer.ts:21:import { isTransactionCanceled } from "../../shared/dynamodb/occ.js";
src\workers\document-request-recurrence\materializer.ts:74:        if (isTransactionCanceled(err)) {
src\workers\core-user-data-purge\purge.ts:32:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\core-user-data-purge\purge.ts:149:        if (!isTransactionCanceled(err)) throw err;
src\workers\core-user-data-purge\purge.ts:205:          if (!isTransactionCanceled(backoffErr)) throw backoffErr;
src\workers\core-user-data-purge\dynamodb-candidate-source.ts:14:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../shared/dynamodb/occ.js";
src\workers\core-user-data-purge\dynamodb-candidate-source.ts:100:      // adapter: purge.ts inspects isTransactionCanceled()/getCancellationReasonCodes() itself.
src\workers\core-user-data-purge\dynamodb-candidate-source.ts:101:      if (isTransactionCanceled(err)) throw err;
src\modules\activity\domain\merge.ts:13: * unchanged). Accepted cost: a partition may re-fetch an already-seen-but-unconsumed item on
src\modules\bff\application\bff-auth-service.ts:12:import { getCancellationReasonCodes, isTransactionCanceled, buildAttributeOnceUpdate, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\bff\application\bff-auth-service.ts:653:      if (isTransactionCanceled(err) && getCancellationReasonCodes(err)?.[0] === "ConditionalCheckFailed") {
docs\engineering\reviews\external-audit-2026-09-11-critica-repositorio.md:821:      await queryGsi3
src\modules\subject\domain\document-chasing.ts:79:export function parseChasingGsi3Sk(gsi3sk: string): { tenantId: string; occurrenceId: string } | undefined {
src\modules\document\application\document-deletion-service.ts:10:import { buildVersionedUpdate, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\document\application\document-deletion-service.ts:67:      if (isTransactionCanceled(err)) {
src\modules\subject\application\subject-service.ts:28:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src\modules\subject\application\subject-service.ts:172:        if (isTransactionCanceled(err)) {
src\modules\subject\application\subject-service.ts:436:      if (isTransactionCanceled(err)) {
src\modules\identity\ports\identity-store.ts:39:   * M2): throws an error recognized by occ.ts's isTransactionCanceled() if ANY entry's
src\modules\subject\application\requirement-service.ts:23:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src\modules\subject\application\requirement-service.ts:330:      if (isTransactionCanceled(err)) {
docs\engineering\decisions-log.md:48:Ao testar a nova ferramenta contra o código real (não só contra um caso sintético), ela encontrou **2 violações genuínas já existentes**: `src/modules/expiration/domain/expiration-item.ts` e `audit-event.ts` importavam `EntityKey`/`TransactPutEntry` de `../ports/expiration-store.ts` (domain→ports, boundary real) em vez do `shared/dynamodb/occ.ts` já usado corretamente pelo módulo `reminder` para o mesmo tipo — `expiration-store.ts` tinha uma cópia própria e estruturalmente duplicada de `EntityKey`/`TransactPutEntry`/`TransactUpdateEntry`/`TransactWriteEntry`/`isTransactionCanceled`. Também descoberto no processo: `reminder/ports/reminder-store.ts` reexportava esses mesmos tipos de `expiration/ports/expiration-store.ts` — uma dependência acidental ports→ports entre módulos irmãos. Corrigido na origem: os tipos/função agora vivem só em `shared/dynamodb/occ.ts`; `expiration-store.ts` e `reminder-store.ts` importam de lá diretamente; `dependency-cruiser` confirma zero violações após a correção (rodado contra o código real, não um exemplo isolado).
src\modules\reminder\ports\reminder-store.ts:11: * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
src\modules\reminder\ports\reminder-store.ts:17: * this structurally (no tenant-facing dependency graph can reach queryGsi3).
src\modules\reminder\ports\reminder-store.ts:24:export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\reminder\ports\reminder-store.ts:48:  queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]>;
src\modules\subject\application\guest-submission-service.ts:16:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src\modules\subject\application\guest-submission-service.ts:203:      if (isTransactionCanceled(err) || err instanceof TenantNotActiveError) throw new GuestTokenInvalidError();
src\modules\subject\application\guest-submission-service.ts:282:      if (!isTransactionCanceled(err)) throw err;
src\modules\identity\application\quota.ts:9:import { buildConditionalPut, buildVersionedCreate, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\identity\application\quota.ts:292:      if (isTransactionCanceled(err)) return false;
src\modules\identity\application\bootstrap-identity.ts:23:import { isTransactionCanceled, buildVersionedCreate, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\identity\application\bootstrap-identity.ts:90:      if (!isTransactionCanceled(err)) throw err;
src\modules\identity\persistence\dynamodb-identity-store.ts:127:  // TransactionCanceledException must reach the caller as-is (isTransactionCanceled() is how
src\modules\subject\application\document-request-service.ts:21:import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
src\modules\subject\application\document-request-service.ts:187:      if (isTransactionCanceled(err)) throw new ConflictError("Failed to create document request under contention.", { subjectId, assignmentId });
src\modules\subject\application\document-request-service.ts:300:      if (isTransactionCanceled(err)) throw new ConflictError("Failed to update document request delivery preference under contention.", { tenantId: ctx.tenant.tenantId });
src\modules\subject\application\document-request-service.ts:404:      if (isTransactionCanceled(err)) throw new ConflictError("VERSION_CONFLICT", { cause: "transaction condition failed" });
docs\engineering\reviews\checkpoint-02-09-consolidated\claude-evaluation-round1.md:22:10. **Error handling não tem swallowing silencioso** nos 13 `catch` revisados — todos ou rethrow, ou mapeiam pra `AppError` tipado, ou tratam race condition nomeada explicitamente (`isTransactionCanceled`).
src\modules\subject\application\document-chasing-producer.ts:15:import { isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\subject\application\document-chasing-producer.ts:132:    if (isTransactionCanceled(err)) {
docs\architecture\decisions-log.md:243:| D-212 | **D-204 fatia 2 (scheduler + claim transacional) — IMPLEMENTADA.** Roadmap P1 item 15 continua saindo do design-only. `runScheduledReportsTick` (`src/workers/scheduled-reports/scheduler.ts`) escaneia GSI8 (`WORK#REPORT_SUBSCRIPTION`) exatamente como `requirement-reindex/reindex.ts` (mesmo cap `MAX_PAGES=25`, mesma paginação), rele fresco cada `ReportSubscription` candidata (GSI8 nunca fonte de elegibilidade, defensivo contra query eventualmente consistente), e comita uma transação de 2 ações por assinatura due — `Update` avançando `nextRunAt`/ponteiro GSI8 (`ConditionCheck` de versão) + `Outbox` `Put` (destino `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`, já aditivo desde D-211) — na MESMA `TransactWriteItems`, mesma disciplina "claim + evento durável atômico" de `reminder-producer/producer.ts` (um claim seguido de SendMessage direto não é atômico; Lambda pode morrer no meio, deixando uma assinatura reivindicada sem entrega enfileirada). Payload do evento deliberadamente mínimo (`runId`/`subscriptionId`/`tenantId`/`scheduledFor`) — decisão 6 de D-204 exige que o worker de entrega (fatia 3, ainda não construído) reresolva `recipientUserIds` FRESCO no envio, então não há valor em embutir um snapshot de `reportTypes`/destinatários aqui; `runId` é gerado AGORA (não pela fatia 3) para que o futuro `ReportSubscriptionRun`/`ReportDeliveryAttempt` compartilhem um id estável entre reentregas SQS at-least-once, mesma postura do `deduplicationKey` de `reminder-producer`. Nova primitiva `nextWeeklyOccurrenceUtc` (`report-subscription-schedule.ts`) computa a próxima ocorrência de `dayOfWeek`(convenção ISO 1=segunda..7=domingo, documentada explicitamente em `ReportSubscription.dayOfWeek` desde D-211 para evitar off-by-one contra `Date.getUTCDay()`)/`localTime`/`timeZone`, reusando `zonedTimeToUtc`/`parseLocalTime`/`addCalendarDays` de `reminder/domain/recurrence.ts` (reuso cross-módulo já estabelecido por `notification/application/quiet-hours.ts` — nenhuma duplicação de lógica DST-aware) mais uma nova primitiva exportada `instantToLocalParts` (direção inversa de `zonedTimeToUtc`, necessária para achar "hoje" no calendário local do assinante antes de buscar a próxima ocorrência). Novo port mínimo `ReportSubscriptionStore` (`get`+`transactWrite` só — criação/gestão de assinatura via rota HTTP fica para uma fatia futura, ainda não decidida) + adapter real `DynamoDbReportSubscriptionStore` + `DynamoDbScheduledReportsCandidateSource` (GSI8 Query, mesmo formato de `requirement-reindex/dynamodb-candidate-source.ts`). Lambda real `scheduled-reports-scheduler-handler` (`timeout_seconds=300`, `gsi8_read_policy_json["report_subscription"]`/`worker_transact_write_policy_json["report_subscription"]` — chaves já provisionadas automaticamente por D-211's `local.gsi8_worker_types` addition, mecânico) + role/policy de `aws_scheduler_schedule` + log group + registrado em `security_audit_observability.global_index_function_names` (exigiu adicionar `"scheduled-reports"` à união fechada `GlobalIndexComponent`, `security-audit.ts`). **Decisão de implementação documentada, não uma nova decisão de arquitetura**: cron do `aws_scheduler_schedule` é DIÁRIO (04:15 UTC), não literalmente semanal apesar do texto da decisão 3 de D-204 dizer "aws_scheduler_schedule único semanal" — lido aqui como descrevendo a cadência da FEATURE (`ReportSubscriptionCadence="WEEKLY"`, decisão 1: cada assinatura dispara ~1x/semana), não a frequência de polling do schedule em si; "único" (1 Lambda/1 schedule/1 scan cross-tenant por execução, "mesmo padrão de requirement-reindex") é a parte vinculante da frase. Um poll diário é o que de fato honra `dayOfWeek`/`localTime`/`timeZone` por assinatura com precisão real (~24h de deriva máxima, igual a todo outro worker GSI8-esparso do projeto) — um poll literalmente semanal dessincronizaria o horário escolhido por cada assinante para o instante único em que o schedule disparasse. Aberto a correção se a leitura literal (cron semanal) era de fato a intenção. `terraform plan` real contra `dev` (`AWS_PROFILE=claude-dev`) confirmou os 12 recursos novos como `+ create` limpo (nenhum recurso pré-existente afetado; os 4 `-/+ destroy and then create replacement` de `aws_lambda_permission` no mesmo plano são débito técnico pré-existente já nomeado em sessão anterior, não relacionados a esta mudança) — `terraform fmt`/`validate` limpos. G-V3: 7 testes novos de `nextWeeklyOccurrenceUtc` (rollover mesma semana/semana seguinte, semântica estritamente-depois-do-instante, convenção ISO dayOfWeek=7≠`Date.getUTCDay()`, timezone-aware cross-fuso, busca a partir da data calendário LOCAL perto de fronteira de dia) + 7 testes de `runScheduledReportsTick` (claim avança nextRunAt/GSI8/escreve evento outbox, ignora não-due, multi-tenant, concorrência perdida no claim, candidato obsoleto nunca reclamado 2x) + 2 de `shouldAlarmScheduledReports`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) e a própria rota HTTP de criação/gestão de `ReportSubscription` (`ADMIN_ROLES`, decisão 1) — nenhuma delas implementada ainda; sem elas, nenhuma assinatura real pode ser criada nem entregue, feature continua não-usável ponta a ponta. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de cadência de cron documentado explicitamente acima) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 2 de D-204)** — pendente fatias 3-4 + rota HTTP de criação, PR/merge/CD | `src/workers/scheduled-reports/{scheduler.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/reports/domain/report-subscription-schedule.ts`, `src/modules/reports/ports/report-subscription-store.ts`, `src/modules/reports/persistence/dynamodb-report-subscription-store.ts`, `src/modules/reminder/domain/recurrence.ts`, `src/runtime/aws/handlers/scheduled-reports-scheduler-handler.ts`, `src/runtime/aws/composition/reports.ts`, `src/shared/observability/security-audit.ts`, `scripts/build-lambdas.ts`, `infra/main.tf` | 2026-09-05 |
docs\architecture\decisions-log.md:270:| D-239 | **Follow-up de D-238 (fatia 3/4): propagação de `AuthorizedTenantId` para os key-builders de persistência do módulo subject (organization é a única fatia remanescente).** Réplica mecânica do padrão já aprovado em D-237/D-238 — protocolo Claude↔Codex dispensado (nenhuma forma de acesso worker/queue/guest-token nova que D-237/D-238 não tivessem coberto; a única forma genuinamente nova encontrada, `GuestSubmissionService`/o fluxo de convidado sem `RequestContext` algum, resolveu-se com o mesmo `authorizedTenantIdFromPersistedEntity()` sobre a entidade já lida do repositório — mesmo critério de confiança já estabelecido, não uma decisão de mecanismo nova). **Key-builders alterados** (`src/modules/subject/domain/*.ts`): `subjectKey`/`gsi7Keys`/`subjectExternalIdPointerKey` (tracked-subject.ts), `requirementAssignmentKey` (requirement-assignment.ts), `documentSubmissionKey` (document-submission.ts), `documentRequestKey` (document-request.ts), `documentRequestDeliveryPreferenceKey` (document-request-delivery-preference.ts), `entitlementKey`/`defaultEntitlement` (entitlement.ts), `subjectAuditKey`/`BuildSubjectAuditEventInput.tenantId` (audit-event.ts), `documentChasingOccurrenceKey`/`chasingGsi3Keys`/`buildChasingClaimGsi6Sk`/`documentChasingIntentKey` (document-chasing.ts) — 13 funções. `guestTokenPointerKey` (guest-token.ts) e `parseSubmissionQuarantineKey`/`parseChasingGsi3Sk` (parsers, não builders) deliberadamente NÃO tocados, mesma exceção tenantless já documentada no próprio código (lookup acontece antes de tenantId ser conhecido) e mesma disciplina de D-238 de nunca mudar um parser. **Call sites atualizados**: `subject/application/*.ts` (subject-service, requirement-service, document-request-service, guest-submission-service, advance-after-submission-evidence, document-chasing-materializer, document-chasing-producer — `authorizedTenantId(ctx)` derivado uma vez por método no lado HTTP, `authorizedTenantIdFromPersistedEntity()` no lado sem `RequestContext`: guest token/submission flow lendo `pointer`/`request` já persistidos, e o producer lendo `occurrence`), `workers/document-chasing-dispatch/dispatch.ts` e `workers/{submission-finalizer,submission-malware-result}/*.ts` (lado worker/SQS/S3-GuardDuty, `authorizedTenantIdFromPersistedEntity(command)` ou sobre o resultado do parser de quarantine-key, mesmo critério de proveniência que `upload-finalizer-handler.ts`/`malware-result-handler.ts` já usam para o branch document-archive), `runtime/aws/handlers/{upload-finalizer-handler,malware-result-handler}.ts` (branch subject dos dois handlers). **Achados cross-módulo que D-238 não previu, tratados com o mesmo padrão sem rodada nova**: `import/application/resolve-subject-references.ts` (chamado por `import-parse-service.ts`, que já usava `authorizedTenantIdFromPersistedEntity({tenantId})` para o resolver irmão de document-archive — mesmo tratamento aplicado aqui) e `organization/application/create-organization.ts` (`defaultEntitlement(organizationId, now)` na criação de tenant novo — `organizationId` é gerado pelo próprio servidor via `ids.newOrganizationId()`, nunca client-supplied, mesma proveniência de confiança que `authorizedTenantIdFromPersistedEntity()` normalmente exige de uma leitura de repositório, aplicada aqui a um valor recém-gerado em vez de relido). Nenhum terceiro construtor foi necessário. Cast `as AuthorizedTenantId` continua restrito a `authorization.ts` — grep confirma zero ocorrências novas. **Testes**: fixtures ajustadas (subject-service, requirement-service, subject-search, document-chasing/-dispatch/-reconciliation/-materializer, document-request-delivery-preference, submission-finalizer, submission-malware-result, security-audit-purge-pointer-creation, e os testes cross-módulo de import/organization) para construir `AuthorizedTenantId` via `authorizedTenantIdFromPersistedEntity({tenantId: "..."})`, nunca um cast local novo. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa **2786/2786** (239 arquivos, sem novos testes — só fixtures existentes ajustadas). `terraform`/infra não tocado. **Escopo remanescente, rastreado explicitamente**: os key-builders do módulo organization continuam aceitando `tenantId: string` puro — única fatia restante (fatia 4/4), mesma ordem de esforço. | Nível 3-4 (réplica mecânica de padrão já aprovado, mesmo módulo central de origem) | N/A (protocolo dispensado — decisão de design já registrada em D-237/D-238) | N/A | **IMPLEMENTADO (slice subject apenas — organization é o único follow-up rastreado remanescente, ver `NEXT_SESSION_PROMPT.md`)** | `src/modules/subject/domain/*.ts`, `src/modules/subject/application/*.ts`, `src/workers/{document-chasing-dispatch,submission-finalizer,submission-malware-result}/*.ts`, `src/runtime/aws/handlers/{upload-finalizer-handler,malware-result-handler}.ts`, `src/modules/import/application/resolve-subject-references.ts`, `src/modules/import/application/import-parse-service.ts`, `src/modules/organization/application/create-organization.ts` | 2026-09-08 |
src\modules\reminder\persistence\dynamodb-reminder-store.ts:49:  // TransactionCanceledException must reach the caller as-is (isTransactionCanceled() is
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:4: * `queryGsi3` exists, matching the port-level isolation documented in
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:20:  async queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]> {
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:48:      throw mapDynamoError(err, "ReminderProducerStore.queryGsi3");
src\modules\organization\ports\organization-store.ts:10:export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\reports\ports\report-subscription-store.ts:13:export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\reports\ports\report-subscription-store.ts:31:  /** Commits every entry atomically. Throws an error recognized by isTransactionCanceled() if
src\modules\reminder\domain\reminder-occurrence.ts:118:export function gsi3PartitionsForMinute(minuteUtc: Date, shardCount: number): string[] {
src\modules\organization\persistence\dynamodb-organization-store.ts:58:  // TransactionCanceledException precisa chegar intacta ao chamador (isTransactionCanceled())
src\modules\document-archive\ports\document-archive-store.ts:18:export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\ports\document-archive-store.ts:67:  /** Commits every entry atomically; throws an error recognized by `isTransactionCanceled()`
src\modules\reminder\domain\gsi3-parse.ts:2:export function parseGsi3Sk(gsi3sk: string): { tenantId: string; occurrenceId: string } {
docs\architecture\m3.5-runtime-design.md:48:1. **Adapters DynamoDB reais** (`@aws-sdk/client-dynamodb` + `@aws-sdk/lib-dynamodb`, `DynamoDBDocumentClient`, sem conversões numéricas silenciosas): um por port — `DynamoDbIdentityStore`, `DynamoDbExpirationStore`, `DynamoDbReminderStore`, `DynamoDbReminderProducerStore`, `DynamoDbReminderReconciliationCandidateSource` (contrato completo na seção de reconciliação acima). Nenhuma classe universal implementando todos os ports (evita injeção acidental de acesso a GSI3/GSI6 em código tenant-facing). As escritas condicionais/transacionais (`putIfAbsent`/`update`/`transactWrite`) traduzem 1:1 para os builders já existentes em `shared/dynamodb/occ.ts` — os adapters são tradução fina dessas, não reimplementam OCC/idempotência; os métodos de leitura/query (`get`, `queryByItem`, `queryGsi1`, `queryGsi3`, `listExpiredClaims`, `listDstCandidates`) são `GetCommand`/`QueryCommand` construídos diretamente no adapter (não vêm de `occ.ts`, que só cobre escrita). Erros do SDK mapeados para `shared/errors/app-error.ts` (`ConditionalCheckFailedException`→`false`, `TransactionCanceledException`→`isTransactionCanceled`, throttling/timeout→retryable, validation/`AccessDeniedException`→terminal) só neste adapter — o resto do sistema continua SDK-agnóstico.
src\modules\reports\persistence\dynamodb-report-subscription-store.ts:28:  // TransactionCanceledException must reach the caller as-is (isTransactionCanceled() is how
docs\architecture\implementation-blueprint.md:654:6. emite `scheduler_lag_seconds`, quantidade por shard e idade do lookback consumido.
src\modules\reminder\application\reminder-policy-service.ts:29:import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../ports/reminder-store.js";
src\modules\reminder\application\reminder-policy-service.ts:87:      if (isTransactionCanceled(err)) {
src\modules\reminder\application\reminder-policy-service.ts:229:      if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) {
src\modules\reminder\application\reminder-policy-service.ts:265:      if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) {
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:12:import { buildVersionedCreate, buildVersionedUpdate, buildVersionConditionCheck, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:131:      if (isTransactionCanceled(err)) return "DOCUMENT_DISCARDED";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:179:      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:208:      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:272:      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:302:      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
src\modules\document-archive\domain\requirement-template.ts:177:  const seen = new Map<string, number>();
src\modules\document-archive\domain\requirement-template.ts:180:    const previous = seen.get(normalized);
src\modules\document-archive\domain\requirement-template.ts:187:    seen.set(normalized, index);
src\modules\reminder\application\reminder-materializer.ts:31:import { isTransactionCanceled, type ReminderStore } from "../ports/reminder-store.js";
src\modules\reminder\application\reminder-materializer.ts:257:        if (isTransactionCanceled(err)) continue; // lost a race (already advanced) - fine, not our job to cancel it
src\modules\reminder\application\reminder-materializer.ts:317:        if (isTransactionCanceled(err) && occurrenceOrFenceFailed) continue;
src\modules\reminder\application\reminder-materializer.ts:377:        if (isTransactionCanceled(err) && reasons?.[0]?.Code === "ConditionalCheckFailed") continue;
src\modules\document-archive\persistence\dynamodb-document-archive-store.ts:72:  // TransactionCanceledException must reach the caller as-is (isTransactionCanceled() is how
src\modules\reports\domain\report-subscription-schedule.ts:30: * `afterIso`'s OWN calendar date as seen in `timeZone` (never UTC's calendar date - a subscriber
src\modules\expiration\ports\expiration-store.ts:16:export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\expiration\ports\expiration-store.ts:47:   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
src\modules\import\ports\import-store.ts:10:export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\reports\application\report-subscription-service.ts:19:import { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\reports\application\report-subscription-service.ts:119:      if (isTransactionCanceled(err)) throw new ConflictError("ReportSubscription already exists.", { subscriptionId });
src\modules\reports\application\report-subscription-service.ts:159:      if (isTransactionCanceled(err)) throw new ConflictError("ReportSubscription was concurrently modified or already deleted.", { subscriptionId, expectedVersion });
src\modules\notification\ports\notification-store.ts:6:export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
src\modules\expiration\persistence\dynamodb-expiration-store.ts:54:  // TransactionCanceledException must reach the caller as-is (isTransactionCanceled() is
src\modules\organization\application\update-organization-settings.ts:18:import { isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\organization\application\update-organization-settings.ts:94:      if (isTransactionCanceled(err)) {
src\modules\import\persistence\dynamodb-import-store.ts:46:  // TransactionCanceledException precisa chegar intacta ao chamador (isTransactionCanceled())
src\modules\notification\persistence\dynamodb-notification-store.ts:58:  // TransactionCanceledException must reach the caller as-is (isTransactionCanceled() is how
src\modules\expiration\application\item-watch-service.ts:14:import { isTransactionCanceled, type ExpirationStore, type TransactWriteEntry } from "../ports/expiration-store.js";
src\modules\expiration\application\item-watch-service.ts:90:        if (isTransactionCanceled(err)) continue;
src\modules\expiration\application\item-watch-service.ts:121:      if (isTransactionCanceled(err)) {
src\modules\organization\application\revoke-invitation.ts:9:import { isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\organization\application\revoke-invitation.ts:78:      if (isTransactionCanceled(err)) {
src\modules\expiration\application\expiration-service.ts:39:  isTransactionCanceled,
src\modules\expiration\application\expiration-service.ts:1109:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\guest-document-access-service.ts:29:import { buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\application\guest-document-access-service.ts:646:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\guest-document-access-service.ts:781:        if (!isTransactionCanceled(err)) throw err;
src\modules\document-archive\application\guest-document-access-service.ts:807:      if (!isTransactionCanceled(err)) throw err;
src\modules\organization\application\change-membership-role.ts:15:import { getCancellationReasonCodes, isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\organization\application\change-membership-role.ts:84:      if (isTransactionCanceled(err)) {
src\modules\organization\application\remove-membership.ts:21:import { getCancellationReasonCodes, isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\organization\application\remove-membership.ts:129:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\external-share-link-service.ts:24:  isTransactionCanceled,
src\modules\document-archive\application\external-share-link-service.ts:268:      if (isTransactionCanceled(err)) throw new ExternalShareLinkCapExceededError();
src\modules\document-archive\application\external-share-link-service.ts:318:      if (isTransactionCanceled(err)) throw new ConflictError("Share link is not ACTIVE or was modified concurrently.");
src\modules\organization\application\accept-invitation.ts:18:import { isTransactionCanceled, getCancellationReasonCodes, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\organization\application\accept-invitation.ts:178:      if (isTransactionCanceled(err)) {
src\modules\organization\application\leave-organization.ts:18:import { getCancellationReasonCodes, isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\organization\application\leave-organization.ts:112:      if (isTransactionCanceled(err)) {
docs\architecture\history\implementation-blueprint\implementation-blueprint-codex-round1.md:632:6. emite `scheduler_lag_seconds` e quantidade por shard.
src\modules\notification\application\email-delivery-workflow.ts:21:import { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\email-delivery-workflow.ts:184:    if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) return false;
src\modules\notification\application\email-delivery-workflow.ts:225:    if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) return "LOST_RACE";
src\modules\notification\application\email-delivery-workflow.ts:260:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\document-archive\application\apply-file-scan-result.ts:26:import { buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\application\apply-file-scan-result.ts:405:      if (isTransactionCanceled(err)) return "IGNORED_STALE"; // lost the race - a concurrent event/sweep already claimed this file.
src\modules\notification\application\notification-preferences-service.ts:30:import { isTransactionCanceled, type NotificationStore } from "../ports/notification-store.js";
src\modules\notification\application\notification-preferences-service.ts:110:      if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) {
src\modules\import\application\import-service.ts:30:import { isTransactionCanceled, type ImportStore, type TransactWriteEntry } from "../ports/import-store.js";
src\modules\import\application\import-service.ts:431:      if (isTransactionCanceled(err)) throw new ConflictError("Failed to submit import mapping under contention.", { jobId });
src\modules\import\application\import-service.ts:498:      if (isTransactionCanceled(err)) throw new ConflictError("Failed to request import commit under contention.", { jobId });
src\modules\document-archive\application\document-request-recurrence-service.ts:18:import { buildConditionalDelete, buildExistenceConditionCheck, buildVersionedCreate, buildVersionedUpdate, isTransactionCanceled, type EntityKey, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\document-archive\application\document-request-recurrence-service.ts:218:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-request-recurrence-service.ts:273:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentRequestSeries was concurrently modified.", { seriesId });
src\modules\document-archive\application\document-request-recurrence-service.ts:311:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentRequestSeries was concurrently modified.", { seriesId });
src\modules\document-archive\application\document-request-recurrence-service.ts:335:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentRequestSeries was concurrently modified (or the attempt id collided).", { seriesId });
src\modules\document-archive\application\document-request-recurrence-service.ts:370:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentRequestSeries was concurrently modified.", { seriesId });
src\modules\import\application\import-parse-service.ts:32:import { isTransactionCanceled, type ImportStore } from "../ports/import-store.js";
src\modules\import\application\import-parse-service.ts:89:      if (isTransactionCanceled(err)) return { kind: "SKIPPED_ALREADY_CLAIMED" };
src\modules\import\application\import-parse-service.ts:100:    if (isTransactionCanceled(err)) return { kind: "SKIPPED_ALREADY_CLAIMED" };
src\modules\import\application\import-parse-service.ts:143:      const seenExternalIdsInFile = new Set<string>();
src\modules\import\application\import-parse-service.ts:156:          if (seenExternalIdsInFile.has(row.externalId)) {
src\modules\import\application\import-parse-service.ts:161:          seenExternalIdsInFile.add(row.externalId);
src\modules\import\application\import-parse-service.ts:188:      const seenExternalIdsInFile = new Set<string>();
src\modules\import\application\import-parse-service.ts:202:          if (seenExternalIdsInFile.has(row.externalId)) {
src\modules\import\application\import-parse-service.ts:207:          seenExternalIdsInFile.add(row.externalId);
src\modules\import\application\import-parse-service.ts:245:      const seenExternalIdsInFile = new Set<string>();
src\modules\import\application\import-parse-service.ts:261:          if (seenExternalIdsInFile.has(row.externalId)) {
src\modules\import\application\import-parse-service.ts:266:          seenExternalIdsInFile.add(row.externalId);
src\modules\document-archive\application\document-request-credential-issuance-service.ts:30:  isTransactionCanceled,
src\modules\document-archive\application\document-request-credential-issuance-service.ts:204:      if (isTransactionCanceled(err)) {
src\modules\notification\application\notification-router-workflow.ts:22:import { isTransactionCanceled } from "../ports/notification-store.js";
src\modules\notification\application\notification-router-workflow.ts:151:      if (!isTransactionCanceled(err)) throw err;
src\modules\notification\application\notification-router-workflow.ts:291:    if (!isTransactionCanceled(err)) throw err;
src\modules\notification\application\notification-router-workflow.ts:398:    if (!isTransactionCanceled(err)) throw err;
src\modules\document-archive\application\document-archive-service.ts:24:  isTransactionCanceled,
src\modules\document-archive\application\document-archive-service.ts:737:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentVersion's file set was concurrently reserved, the Version is no longer DRAFT, or the tenant's storage quota was concurrently exhausted — retry.", { documentId, seq });
src\modules\document-archive\application\document-archive-service.ts:819:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentVersion was concurrently modified.", { documentId, seq });
src\modules\document-archive\application\document-archive-service.ts:877:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentVersion is no longer RECEIVED (already claimed or decided).", { documentId, seq });
src\modules\document-archive\application\document-archive-service.ts:1021:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:1123:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentVersion was concurrently modified.", { documentId, seq, reopenedDocumentRequest: reopenedRequest?.documentRequestId });
src\modules\document-archive\application\document-archive-service.ts:1230:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:1318:    if (!isTransactionCanceled(err)) throw err;
src\modules\document-archive\application\document-archive-service.ts:1490:      if (isTransactionCanceled(err)) throw new ConflictError("DossierExportRun was concurrently modified.", { subjectId, runId });
src\modules\document-archive\application\document-archive-service.ts:1739:      if (isTransactionCanceled(err)) throw new ConflictError("Requirement was concurrently modified.", { requirementId });
src\modules\document-archive\application\document-archive-service.ts:1774:      if (isTransactionCanceled(err)) throw new ConflictError("Requirement was concurrently modified.", { requirementId });
src\modules\document-archive\application\document-archive-service.ts:1884:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:1952:        if (isTransactionCanceled(err)) throw new ConflictError("DocumentType was concurrently modified.", { documentTypeId });
src\modules\document-archive\application\document-archive-service.ts:1990:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:2040:      if (isTransactionCanceled(err)) throw new ConflictError(`DocumentType is not ${fromStatus} (already ${toStatus}, or concurrently modified).`, { documentTypeId });
src\modules\document-archive\application\document-archive-service.ts:2114:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentType was concurrently modified.", { documentTypeId });
src\modules\document-archive\application\document-archive-service.ts:2168:      if (isTransactionCanceled(err)) throw new ConflictError("DocumentType was concurrently modified.", { documentTypeId });
src\modules\document-archive\application\document-archive-service.ts:2295:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:2415:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:2648:      if (isTransactionCanceled(err)) {
src\modules\document-archive\application\document-archive-service.ts:2686:      if (isTransactionCanceled(err)) {
src\modules\notification\application\ses-callback-workflow.ts:27:import { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\ses-callback-workflow.ts:135:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\ses-callback-workflow.ts:157:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\ses-callback-workflow.ts:169:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\ses-callback-workflow.ts:189:    if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) return false;
docs\architecture\reviews\bulk-import-documents-requirements-scoping\estado-final-consolidado.md:207:| `TrackedSubject` | `externalId` (agora campo 1ª classe + pointer) | `type+displayNameNormalized` | inalterado (`seenExternalIdsInFile`) |
docs\architecture\reviews\bulk-import-documents-requirements-scoping\estado-final-consolidado.md:211:"Primeiro vence" alinhado ao precedente REAL já existente (`seenExternalIdsInFile` — verificado
src\modules\import\application\import-commit-service.ts:59:import { buildVersionedUpdate, getCancellationReasonCodes, isTransactionCanceled, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
src\modules\import\application\import-commit-service.ts:366:    if (!isTransactionCanceled(err)) throw err;
src\modules\import\application\import-commit-service.ts:430:    if (!isTransactionCanceled(err)) throw err;
src\modules\notification\application\whatsapp-webhook-workflow.ts:28:import { isTransactionCanceled, isConditionalCheckFailed, buildVersionedUpdate, buildAccountScopedVersionedUpdate } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\whatsapp-webhook-workflow.ts:125:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\whatsapp-webhook-workflow.ts:159:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\whatsapp-webhook-workflow.ts:171:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\whatsapp-webhook-workflow.ts:183:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
src\modules\notification\application\whatsapp-delivery-workflow.ts:25:import { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
src\modules\notification\application\whatsapp-delivery-workflow.ts:206:    if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) return false;
src\modules\notification\application\whatsapp-delivery-workflow.ts:238:    if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) return "LOST_RACE";
src\modules\notification\application\whatsapp-delivery-workflow.ts:268:    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round2-claude-revision.md:264:Mesma disciplina de `seenExternalIdsInFile` (Subject, já existente) generalizada:
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round2-claude-revision.md:265:`seenDedupKeysInFile: Set<string>` por `(subjectId, dedupKey)` quando `externalId` está presente;
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round2-claude-revision.md:266:para `Requirement` SEM `externalId`, `seenNormalizedNamesInFile: Set<string>` por
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round3-claude-final.md:202:original)**: alinhado ao precedente REAL já existente (`seenExternalIdsInFile` em
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round3-codex-critique.md:52:- `seenExternalIdsInFile` implementa first-wins: a primeira ocorrência continua; as seguintes viram `REJECT`.
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round2-codex-critique.md:53:| 8 | Parcial | As estruturas `seen*` estão definidas, mas há contradição: “segunda ocorrência é rejeitada” e “as duas linhas são rejeitadas” não são o mesmo comportamento. Um algoritmo one-pass naturalmente aceita a primeira e rejeita a segunda; rejeitar ambas requer pré-contagem ou revisão retroativa do plano. |
docs\architecture\reviews\bulk-import-documents-requirements-scoping\round1-codex-critique.md:236:O parser atual mantém `seenExternalIdsInFile` para Subjects. A nova proposta fala somente em dedupe contra registros persistidos.
docs\architecture\reviews\m3.5-runtime-design\claude-proposal.md:16:- `get`/`putIfAbsent`/`update`/`transactWrite`/`queryByItem`/`queryGsi1`/`queryGsi3` traduzem 1:1 para `GetCommand`/`PutCommand` com `ConditionExpression: attribute_not_exists(PK)`/`UpdateCommand`/`TransactWriteCommand`/`QueryCommand`. Os builders já existentes (`shared/dynamodb/occ.ts`, `shared/idempotency/idempotency.ts`, `shared/outbox/outbox.ts`) já produzem os parâmetros nesse formato — os adapters são uma camada fina de tradução para os `*Command` do SDK, não reimplementam lógica de OCC/idempotência.
docs\architecture\reviews\m3.5-runtime-design\claude-critique-of-codex-round2.md:14:1. **DynamoDB Local vs. LocalStack para a camada 2 (adapters puros).** O Codex descarta DynamoDB Local em favor de LocalStack via Testcontainers para tudo. Concordo que LocalStack é necessário para SQS/Streams/EventBridge — mas para a suíte que só prova que os adapters DynamoDB (`get`/`putIfAbsent`/`transactWrite`/`queryGsi1`/`queryGsi3`) concordam com as fakes em memória existentes, DynamoDB Local é mais leve, mais determinístico e mais rápido no dia a dia de CI (é o que M0-M3 já usam para tudo "rápido"). Proposta convergida: DynamoDB Local para a suíte de paridade adapter-vs-fake (roda em todo PR, rápida); LocalStack/Testcontainers só para a camada que precisa de SQS+Streams+EventBridge de verdade (fault injection, mais pesada, pode rodar em job separado). Isso é uma diferença de organização de teste, não de arquitetura — baixo risco, mas vale registrar para não misturar dois containers pesados onde um simples resolve.
docs\architecture\reviews\guest-file-storage-scoping\round6-claude-reconciliation.md:4:`isTransactionCanceled` da Rodada 5 tratava TODO cancelamento como "extensão já venceu" ou "estado
docs\architecture\reviews\guest-file-storage-scoping\round6-claude-reconciliation.md:62:      if (!isTransactionCanceled(err)) throw err;
docs\architecture\reviews\guest-file-storage-scoping\round5-claude-reconciliation.md:24:Mesmo padrão de `isTransactionCanceled` + releitura que `submitEvidence()` já usa
docs\architecture\reviews\guest-file-storage-scoping\round5-claude-reconciliation.md:32:  if (!isTransactionCanceled(err)) throw err;
docs\architecture\reviews\frontend-visual-excellence-rubric-scoping\stage1-round1-codex-blind-proposal.md:240:- What is seen first, second, and third?
docs\architecture\reviews\document-type-scoping\round5-claude-revision.md:50:  if (isTransactionCanceled(err)) {
docs\architecture\reviews\document-type-scoping\round5-claude-revision.md:70:**não muda o `catch` de `submitEvidence()` em nada** — o `isTransactionCanceled(err)` já existente
docs\architecture\reviews\document-type-scoping\round3-claude-final.md:78:  if (isTransactionCanceled(err)) {
docs\architecture\reviews\guest-credential-issuance-scoping\round3-claude.md:58:`isTransactionCanceled` — precisa de uma nova função `cancellationReasonAt(err, index)` em
docs\architecture\reviews\document-type-metadata-scoping\round2-claude-revision.md:80:- `PATCH /document-archive/documents/{documentId}/metadata-values`: corpo carrega `expectedDocumentVersion` (OCC do próprio Document, não do DocumentType — o cliente nunca precisa saber/passar a versão do DocumentType). O SERVIÇO relê fresco o `DocumentType` do Document (mesmo padrão de `createDocument()`'s check `DocumentType.status=ACTIVE`, D-175), valida cada valor do corpo contra a definição de campo/opção ATIVA correspondente NAQUELE MOMENTO, e grava numa ÚNICA `TransactWriteItems`: `Update` em `Document` (`ConditionExpression: version = expectedDocumentVersion`) + `ConditionCheck` em `DocumentType` (`version = <versão que o serviço acabou de ler>`). Isso fecha a corrida real que o Codex apontou (cliente lê campo ATIVO, outro request arquiva o campo, primeiro request grava valor sob campo já arquivado) sem exigir que o cliente conheça a versão do DocumentType — o `ConditionCheck` falha a transação inteira (`TransactionCanceledException`, tratado com o `isTransactionCanceled()` já existente) se o `DocumentType` mudou entre a leitura e a escrita, e o handler HTTP responde 409 (mesmo padrão de conflito otimista já usado em toda escrita mutável do projeto).
docs\architecture\reviews\document-type-metadata-scoping\estado-final-consolidado.md:100:- `PATCH /document-archive/documents/{documentId}/metadata-values` (RBAC `docarchive:document-metadata-update`, corpo carrega `expectedDocumentVersion`). O serviço relê fresco o `DocumentType` do Document, valida cada valor contra a definição/opção ATIVA correspondente, e grava numa ÚNICA `TransactWriteItems`: `Update` em `Document` (`ConditionExpression: version = expectedDocumentVersion`) + `ConditionCheck` em `DocumentType` (`version = <versão recém-lida>`) — fecha a corrida (cliente lê campo ativo, outro request arquiva, primeiro grava sob campo já arquivado) sem exigir que o cliente conheça a versão do `DocumentType`. `TransactionCanceledException` tratada via `isTransactionCanceled()` já existente, handler responde 409.
docs\architecture\reviews\performance-audit-2026-09-02-reconciliation\estado-final-consolidado.md:19:| 3 | Reminder Producer: 6 min lookback × 4 shards × 24 queries GSI3 sequenciais, drena todas as páginas por partição antes de avançar, claim+outbox sequencial por ocorrência | **Confirmado** | `producer.ts:128-131` gera `lookback+1` minutos (default 5 → 6 minutos); `producer.ts:145-274` é um triplo loop `for (minute) for (generation) for (gsi3pk)` totalmente sequencial — `await deps.store.queryGsi3(...)` bloqueia antes do próximo `gsi3pk`; dentro de cada partição, o loop `for (const row of rows)` processa `Get` + `TransactWrite` por ocorrência um de cada vez, sem `Promise.all`/concorrência limitada. Shard count default (`shard-config.ts:31`, `DEFAULT_SHARD_COUNT`) confirma a topologia 4-shard citada. |
docs\architecture\reviews\ocr-extraction-document-lifecycle-reconciliation-scoping\round3-claude-final.md:20:  chegar transiciona `SCANNING → CLEAN`; o segundo recebe `isTransactionCanceled` e a chamada
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round2-reconciliation.md:943:.\docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round1-codex-blind-proposal.md:14:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round2-reconciliation.md:1016:.\docs\architecture\reviews\p0-frontend-screens-scoping\stage2-codex-prompt.txt:2:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round2-reconciliation.md:2231:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\p0-frontend-screens-scoping\stage2-round1-codex-blind-proposal.md:14:(AGENTS.md paragraph 4). BLIND round: you have not seen Claude's Stage 2 proposal.
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-selfgrade.md:6:de tipo GSI3, falha parcial de SendMessageBatch, correção de fonte) e aceitei o rebalanceamento
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:27:   uma mensagem por partição elegível (mesmo cálculo de `gsi3PartitionsForMinute` × lookback já
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:31:   uma mensagem de scan por vez, faz UMA página de `queryGsi3` (LIMIT explícito, não "ler tudo"),
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:32:   publica os candidatos dessa página na fila de claim via `SendMessageBatch` (chunks de 10,
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:49:`entityType: "REMINDER" | "CHASING"`), preservando a mesma lógica de `parseGsi3Sk`/
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:50:`parseChasingGsi3Sk`/fail-closed `unknownEntityType` que já existe hoje — só movida do loop do
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:53:`shouldAlarm()`-equivalente no novo consumer.
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:57:scan de minutos sobrepostos do lookback, já um caso hoje coberto pelo `seen`-guard local). A
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:60:`isTransactionCanceled`, tratado como não-falha, EXATAMENTE como o código atual já trata hoje
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:70:alarma via `shouldAlarm()`); idade do candidato mais antigo na fila (`ApproximateAgeOfOldestMessage`,
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:73:`scheduler_lag_seconds` como sinal primário de saúde do pipeline. Redrive automático/alerta é
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:90:   como métrica de saúde, falha parcial de `SendMessageBatch` tratada explicitamente (retry até
docs\architecture\reviews\reminder-producer-structural-fix-scoping\round-2-claude-revision.md:115:`SendMessageBatch`, `Failed[]` mesmo com HTTP 200 —
docs\architecture\reviews\p0-frontend-screens-scoping\stage1-round1-codex-blind-proposal.md:15:protocol (this repo's AGENTS.md paragraph 4). This is a BLIND round: you have not seen any
docs\architecture\reviews\p0-frontend-screens-scoping\stage1-round1-codex-blind-proposal.md:2475:docs/architecture\decisions-log.md:243:| D-212 | **D-204 fatia 2 (scheduler + claim transacional) — IMPLEMENTADA.** Roadmap P1 item 15 continua saindo do design-only. `runScheduledReportsTick` (`src/workers/scheduled-reports/scheduler.ts`) escaneia GSI8 (`WORK#REPORT_SUBSCRIPTION`) exatamente como `requirement-reindex/reindex.ts` (mesmo cap `MAX_PAGES=25`, mesma paginação), rele fresco cada `ReportSubscription` candidata (GSI8 nunca fonte de elegibilidade, defensivo contra query eventualmente consistente), e comita uma transação de 2 ações por assinatura due — `Update` avançando `nextRunAt`/ponteiro GSI8 (`ConditionCheck` de versão) + `Outbox` `Put` (destino `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`, já aditivo desde D-211) — na MESMA `TransactWriteItems`, mesma disciplina "claim + evento durável atômico" de `reminder-producer/producer.ts` (um claim seguido de SendMessage direto não é atômico; Lambda pode morrer no meio, deixando uma assinatura reivindicada sem entrega enfileirada). Payload do evento deliberadamente mínimo (`runId`/`subscriptionId`/`tenantId`/`scheduledFor`) — decisão 6 de D-204 exige que o worker de entrega (fatia 3, ainda não construído) reresolva `recipientUserIds` FRESCO no envio, então não há valor em embutir um snapshot de `reportTypes`/destinatários aqui; `runId` é gerado AGORA (não pela fatia 3) para que o futuro `ReportSubscriptionRun`/`ReportDeliveryAttempt` compartilhem um id estável entre reentregas SQS at-least-once, mesma postura do `deduplicationKey` de `reminder-producer`. Nova primitiva `nextWeeklyOccurrenceUtc` (`report-subscription-schedule.ts`) computa a próxima ocorrência de `dayOfWeek`(convenção ISO 1=segunda..7=domingo, documentada explicitamente em `ReportSubscription.dayOfWeek` desde D-211 para evitar off-by-one contra `Date.getUTCDay()`)/`localTime`/`timeZone`, reusando `zonedTimeToUtc`/`parseLocalTime`/`addCalendarDays` de `reminder/domain/recurrence.ts` (reuso cross-módulo já estabelecido por `notification/application/quiet-hours.ts` — nenhuma duplicação de lógica DST-aware) mais uma nova primitiva exportada `instantToLocalParts` (direção inversa de `zonedTimeToUtc`, necessária para achar "hoje" no calendário local do assinante antes de buscar a próxima ocorrência). Novo port mínimo `ReportSubscriptionStore` (`get`+`transactWrite` só — criação/gestão de assinatura via rota HTTP fica para uma fatia futura, ainda não decidida) + adapter real `DynamoDbReportSubscriptionStore` + `DynamoDbScheduledReportsCandidateSource` (GSI8 Query, mesmo formato de `requirement-reindex/dynamodb-candidate-source.ts`). Lambda real `scheduled-reports-scheduler-handler` (`timeout_seconds=300`, `gsi8_read_policy_json["report_subscription"]`/`worker_transact_write_policy_json["report_subscription"]` — chaves já provisionadas automaticamente por D-211's `local.gsi8_worker_types` addition, mecânico) + role/policy de `aws_scheduler_schedule` + log group + registrado em `security_audit_observability.global_index_function_names` (exigiu adicionar `"scheduled-reports"` à união fechada `GlobalIndexComponent`, `security-audit.ts`). **Decisão de implementação documentada, não uma nova decisão de arquitetura**: cron do `aws_scheduler_schedule` é DIÁRIO (04:15 UTC), não literalmente semanal apesar do texto da decisão 3 de D-204 dizer "aws_scheduler_schedule único semanal" — lido aqui como descrevendo a cadência da FEATURE (`ReportSubscriptionCadence="WEEKLY"`, decisão 1: cada assinatura dispara ~1x/semana), não a frequência de polling do schedule em si; "único" (1 Lambda/1 schedule/1 scan cross-tenant por execução, "mesmo padrão de requirement-reindex") é a parte vinculante da frase. Um poll diário é o que de fato honra `dayOfWeek`/`localTime`/`timeZone` por assinatura com precisão real (~24h de deriva máxima, igual a todo outro worker GSI8-esparso do projeto) — um poll literalmente semanal dessincronizaria o horário escolhido por cada assinante para o instante único em que o schedule disparasse. Aberto a correção se a leitura literal (cron semanal) era de fato a intenção. `terraform plan` real contra `dev` (`AWS_PROFILE=claude-dev`) confirmou os 12 recursos novos como `+ create` limpo (nenhum recurso pré-existente afetado; os 4 `-/+ destroy and then create replacement` de `aws_lambda_permission` no mesmo plano são débito técnico pré-existente já nomeado em sessão anterior, não relacionados a esta mudança) — `terraform fmt`/`validate` limpos. G-V3: 7 testes novos de `nextWeeklyOccurrenceUtc` (rollover mesma semana/semana seguinte, semântica estritamente-depois-do-instante, convenção ISO dayOfWeek=7≠`Date.getUTCDay()`, timezone-aware cross-fuso, busca a partir da data calendário LOCAL perto de fronteira de dia) + 7 testes de `runScheduledReportsTick` (claim avança nextRunAt/GSI8/escreve evento outbox, ignora não-due, multi-tenant, concorrência perdida no claim, candidato obsoleto nunca reclamado 2x) + 2 de `shouldAlarmScheduledReports`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) e a própria rota HTTP de criação/gestão de `ReportSubscription` (`ADMIN_ROLES`, decisão 1) — nenhuma delas implementada ainda; sem elas, nenhuma assinatura real pode ser criada nem entregue, feature continua não-usável ponta a ponta. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de cadência de cron documentado explicitamente acima) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 2 de D-204)** — pendente fatias 3-4 + rota HTTP de criação, PR/merge/CD | `src/workers/scheduled-reports/{scheduler.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/reports/domain/report-subscription-schedule.ts`, `src/modules/reports/ports/report-subscription-store.ts`, `src/modules/reports/persistence/dynamodb-report-subscription-store.ts`, `src/modules/reminder/domain/recurrence.ts`, `src/runtime/aws/handlers/scheduled-reports-scheduler-handler.ts`, `src/runtime/aws/composition/reports.ts`, `src/shared/observability/security-audit.ts`, `scripts/build-lambdas.ts`, `infra/main.tf` | 2026-09-05 |
docs\architecture\reviews\p0-frontend-screens-scoping\stage1-round1-codex-blind-proposal.md:2497:docs/architecture\decisions-log.md:270:| D-239 | **Follow-up de D-238 (fatia 3/4): propagação de `AuthorizedTenantId` para os key-builders de persistência do módulo subject (organization é a única fatia remanescente).** Réplica mecânica do padrão já aprovado em D-237/D-238 — protocolo Claude↔Codex dispensado (nenhuma forma de acesso worker/queue/guest-token nova que D-237/D-238 não tivessem coberto; a única forma genuinamente nova encontrada, `GuestSubmissionService`/o fluxo de convidado sem `RequestContext` algum, resolveu-se com o mesmo `authorizedTenantIdFromPersistedEntity()` sobre a entidade já lida do repositório — mesmo critério de confiança já estabelecido, não uma decisão de mecanismo nova). **Key-builders alterados** (`src/modules/subject/domain/*.ts`): `subjectKey`/`gsi7Keys`/`subjectExternalIdPointerKey` (tracked-subject.ts), `requirementAssignmentKey` (requirement-assignment.ts), `documentSubmissionKey` (document-submission.ts), `documentRequestKey` (document-request.ts), `documentRequestDeliveryPreferenceKey` (document-request-delivery-preference.ts), `entitlementKey`/`defaultEntitlement` (entitlement.ts), `subjectAuditKey`/`BuildSubjectAuditEventInput.tenantId` (audit-event.ts), `documentChasingOccurrenceKey`/`chasingGsi3Keys`/`buildChasingClaimGsi6Sk`/`documentChasingIntentKey` (document-chasing.ts) — 13 funções. `guestTokenPointerKey` (guest-token.ts) e `parseSubmissionQuarantineKey`/`parseChasingGsi3Sk` (parsers, não builders) deliberadamente NÃO tocados, mesma exceção tenantless já documentada no próprio código (lookup acontece antes de tenantId ser conhecido) e mesma disciplina de D-238 de nunca mudar um parser. **Call sites atualizados**: `subject/application/*.ts` (subject-service, requirement-service, document-request-service, guest-submission-service, advance-after-submission-evidence, document-chasing-materializer, document-chasing-producer — `authorizedTenantId(ctx)` derivado uma vez por método no lado HTTP, `authorizedTenantIdFromPersistedEntity()` no lado sem `RequestContext`: guest token/submission flow lendo `pointer`/`request` já persistidos, e o producer lendo `occurrence`), `workers/document-chasing-dispatch/dispatch.ts` e `workers/{submission-finalizer,submission-malware-result}/*.ts` (lado worker/SQS/S3-GuardDuty, `authorizedTenantIdFromPersistedEntity(command)` ou sobre o resultado do parser de quarantine-key, mesmo critério de proveniência que `upload-finalizer-handler.ts`/`malware-result-handler.ts` já usam para o branch document-archive), `runtime/aws/handlers/{upload-finalizer-handler,malware-result-handler}.ts` (branch subject dos dois handlers). **Achados cross-módulo que D-238 não previu, tratados com o mesmo padrão sem rodada nova**: `import/application/resolve-subject-references.ts` (chamado por `import-parse-service.ts`, que já usava `authorizedTenantIdFromPersistedEntity({tenantId})` para o resolver irmão de document-archive — mesmo tratamento aplicado aqui) e `organization/application/create-organization.ts` (`defaultEntitlement(organizationId, now)` na criação de tenant novo — `organizationId` é gerado pelo próprio servidor via `ids.newOrganizationId()`, nunca client-supplied, mesma proveniência de confiança que `authorizedTenantIdFromPersistedEntity()` normalmente exige de uma leitura de repositório, aplicada aqui a um valor recém-gerado em vez de relido). Nenhum terceiro construtor foi necessário. Cast `as AuthorizedTenantId` continua restrito a `authorization.ts` — grep confirma zero ocorrências novas. **Testes**: fixtures ajustadas (subject-service, requirement-service, subject-search, document-chasing/-dispatch/-reconciliation/-materializer, document-request-delivery-preference, submission-finalizer, submission-malware-result, security-audit-purge-pointer-creation, e os testes cross-módulo de import/organization) para construir `AuthorizedTenantId` via `authorizedTenantIdFromPersistedEntity({tenantId: "..."})`, nunca um cast local novo. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa **2786/2786** (239 arquivos, sem novos testes — só fixtures existentes ajustadas). `terraform`/infra não tocado. **Escopo remanescente, rastreado explicitamente**: os key-builders do módulo organization continuam aceitando `tenantId: string` puro — única fatia restante (fatia 4/4), mesma ordem de esforço. | Nível 3-4 (réplica mecânica de padrão já aprovado, mesmo módulo central de origem) | N/A (protocolo dispensado — decisão de design já registrada em D-237/D-238) | N/A | **IMPLEMENTADO (slice subject apenas — organization é o único follow-up rastreado remanescente, ver `NEXT_SESSION_PROMPT.md`)** | `src/modules/subject/domain/*.ts`, `src/modules/subject/application/*.ts`, `src/workers/{document-chasing-dispatch,submission-finalizer,submission-malware-result}/*.ts`, `src/runtime/aws/handlers/{upload-finalizer-handler,malware-result-handler}.ts`, `src/modules/import/application/resolve-subject-references.ts`, `src/modules/import/application/import-parse-service.ts`, `src/modules/organization/application/create-organization.ts` | 2026-09-08 |
docs\architecture\reviews\multi-user-b2b-wave-b2b12-scoping\round-3-claude-proposal.md:24:`isTransactionCanceled()`/`getCancellationReasonCodes()`/builders de update condicional,
rg: docs\architecture\history\cost-model\cost-model-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\slo\slo-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\claude-architecture-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\codex-architecture-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\slo\slo-codex-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\red-team-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\red-team-codex-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\round2-claude-critique.md: Acesso negado. (os error 5)
rg: docs\architecture\history\quality-criteria\claude-quality-criteria-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\architecture-fase3\round3-treplica.md: Acesso negado. (os error 5)
rg: docs\architecture\history\disaster-recovery\disaster-recovery-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\privacy-lgpd\privacy-lgpd-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\quality-criteria\codex-quality-criteria-proposal.md: Acesso negado. (os error 5)
rg: docs\architecture\history\disaster-recovery\disaster-recovery-codex-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\data-model\data-model-claude-round1.md: Acesso negado. (os error 5)
rg: docs\architecture\history\data-model\data-model-codex-round1.md: Acesso negado. (os error 5)

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'src/workers/reminder-producer/producer.ts'; "'$c=Get-Content $p; $c[170..330]; Get-Content -Raw src/modules/reminder/ports/reminder-store.ts; Get-Content -Raw src/modules/reminder/domain/gsi3-parse.ts; Get-Content -Raw src/modules/subject/domain/document-chasing.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 338ms:
  const chasingClaimed: ChasingDispatchCommand[] = [];
  const failed: { occurrenceId: string; tenantId: string; error: unknown }[] = [];
  let unknownEntityType = 0;
  let scanned = 0;
  let partitions = 0;

  // Dedup guard: the same occurrence could show up in more than one scanned minute only if
  // materialization ever wrote it under a stale bucket, which shouldn't happen - but a
  // Set guards against double-claiming within a single tick regardless.
  const seen = new Set<string>();

  for (const minute of minutesToScan) {
    for (const generation of generations) {
      const partitionKeys = gsi3PartitionsForMinute(minute, generation.shardCount);
      partitions += partitionKeys.length;
      for (const gsi3pk of partitionKeys) {
        const rows = await deps.store.queryGsi3<{ PK: string; SK: string; GSI3SK: string }>({ gsi3pk });
        // PERF-12: this used to be `for (const row of rows)`, one `get`+`transactWrite` pair
        // processed strictly sequentially - see PRODUCER_CLAIM_CONCURRENCY's doc comment for
        // why bounded concurrency replaces that here. `seen`/`claimed`/`failed`/
        // `chasingClaimed`/counters are shared mutable state across concurrent callbacks, but
        // every access happens either synchronously (no `await` in between, so the JS event
        // loop cannot interleave two callbacks' synchronous sections) or as an atomic
        // push/increment - never a race in practice under Node's single-threaded model.
        await mapWithConcurrency(rows, PRODUCER_CLAIM_CONCURRENCY, async (row) => {
          scanned += 1;

          // M10 cluster 4: try the chasing shape FIRST (never throws, `undefined` on no
          // match) - the reminder branch below is completely unchanged otherwise.
          const chasingParsed = parseChasingGsi3Sk(row.GSI3SK);
          if (chasingParsed) {
            if (seen.has(chasingParsed.occurrenceId)) return;
            seen.add(chasingParsed.occurrenceId);
            try {
              const outcome = await claimChasingOccurrence(
                { store: deps.store, tableName: deps.tableName, now: deps.now, claimTtlMs, newEventId: deps.newEventId, correlationId: deps.correlationId },
                { PK: row.PK, SK: row.SK },
              );
              if (outcome.kind === "CLAIMED") chasingClaimed.push(outcome.command);
              // SKIPPED_NOT_SCHEDULED / LOST_CLAIM_RACE: not failures, same as the reminder path below.
            } catch (err) {
              failed.push({ occurrenceId: chasingParsed.occurrenceId, tenantId: chasingParsed.tenantId, error: err });
            }
            return;
          }

          let reminderParsed: { tenantId: string; occurrenceId: string };
          try {
            reminderParsed = parseGsi3Sk(row.GSI3SK);
          } catch {
            // Fail-closed (D-039): neither pattern matched - never process a row we can't
            // identify, never skip it silently either. Alarm-worthy, surfaced via the tick result.
            unknownEntityType += 1;
            return;
          }
          const { tenantId, occurrenceId } = reminderParsed;
          if (seen.has(occurrenceId)) return;
          seen.add(occurrenceId);

          try {
            const occurrence = await deps.store.get<ReminderOccurrence>({ PK: row.PK, SK: row.SK });
            if (!occurrence || occurrence.status !== "SCHEDULED") {
              // Already claimed/cancelled/triggered by a prior tick or race - not a failure.
              return;
            }

            const claimExpiresAt = new Date(Date.parse(deps.now()) + claimTtlMs).toISOString();
            const newVersion = occurrence.version + 1;
            const now = deps.now();
            // Same value used for both the command's own envelope correlationId (read
            // directly from the SQS body per m5-observability-design.md #2's general SQS
            // rule) and the outbox DomainEvent's correlationId - one business correlation
            // per dispatch, not two independently-generated ids for the same operation.
            const correlationId = deps.correlationId();

            const command: DispatchCommand = {
              messageVersion: 1,
              messageId: deps.newEventId(),
              createdAt: now,
              correlationId,
              commandType: "reminder.dispatch.v1",
              tenantId,
              deduplicationKey: `${tenantId}|${occurrenceId}|${newVersion}`,
              data: {
                itemId: occurrence.itemId,
                occurrenceId,
                occurrenceVersion: newVersion,
                scheduledAt: occurrence.scheduledAt,
                itemVersion: occurrence.itemVersion,
                policyVersion: occurrence.policyVersion,
              },
            };

            // M3.5 "DecisÃ£o central: outbox durÃ¡vel": TransactWrite(claim) followed by a
            // direct SendMessage in the handler is NOT atomic (Lambda can die between the
            // two steps, leaving a CLAIMED occurrence with no queued command - neither the
            // producer's own lookback window nor the daily DST reconciliation reliably
            // covers that gap). The fix: write a durable OutboxEvent
            // (destination=SQS_REMINDER_DISPATCH_V1) in the SAME transaction as the claim;
            // DispatchOutboxRelay (DynamoDB Streams) publishes it to SQS, a sweeper recovers
            // publication failures. Also set the GSI6 WORKSTATE#CLAIMED pointer so the
            // reconciliation job can find this claim if it expires unrecovered - removed
            // when ReminderDispatch advances to TRIGGERED or reconciliation reverts it.
            const event: DomainEvent = {
              specVersion: "1.0",
              eventId: deps.newEventId(),
              eventType: "ReminderDispatchRequested",
              source: "expiration-tracker.reminder-producer",
              occurredAt: now,
              correlationId,
              tenantId,
              actor: { type: "SYSTEM" },
              aggregate: { type: "ReminderOccurrence", id: occurrenceId, version: newVersion },
              data: command as unknown as Record<string, unknown>,
            };
            const outboxEntries: DynamoTransactPutEntry[] = [];
            appendToTransaction(outboxEntries, deps.tableName, event, "SQS_REMINDER_DISPATCH_V1");

            await deps.store.transactWrite([
              {
                Update: buildVersionedUpdate({
                  tableName: deps.tableName,
                  key: { PK: row.PK, SK: row.SK },
                  tenantId,
                  expectedVersion: occurrence.version,
                  set: {
                    status: "CLAIMED",
                    claimedAt: deps.now(),
                    claimExpiresAt,
                    GSI6PK: GSI6PK_WORKSTATE_CLAIMED,
                    GSI6SK: buildExpiredClaimGsi6Sk(claimExpiresAt, tenantId, occurrenceId),
                  },
                }),
              },
              ...outboxEntries,
            ]);

            claimed.push(command);
          } catch (err) {
            if (isTransactionCanceled(err)) {
              // Lost the claim race to a concurrent producer tick - not a failure to retry.
              return;
            }
            failed.push({ occurrenceId, tenantId, error: err });
          }
        });
      }
    }
  }

  return {
    claimed,
    chasingClaimed,
    failed,
    unknownEntityType,
    scanned,
    minutesScanned: minutesToScan.map((d) => d.toISOString()),
    shardPartitionsScanned: partitions,
  };
}
/**
 * DynamoDB surface the Reminder module needs - same SDK-agnostic port pattern as
 * src/modules/expiration/ports/expiration-store.ts (AGENTS.md Â§6: "qualquer mÃ³dulo futuro
 * com a mesma necessidade deve reusar esse formato de port, nÃ£o inventar um novo"). This
 * IS that reuse: identical shape (get/putIfAbsent/update/transactWrite/queryGsi1-style),
 * plus one addition this module needs and Expiration didn't - `queryByItem`, to list an
 * item's existing ReminderOccurrence rows (co-located under the item's own PK per
 * data-model.md Â§2) for materialization diffing, due-date-change cancellation and DST
 * reconciliation.
 *
 * `ReminderProducerStore` is deliberately a SEPARATE, narrower interface (queryGsi3 only)
 * - implementation-blueprint.md Â§9.2's isolation safeguard ("nenhuma rota tenant-facing
 * tem permissÃ£o de Query no GSI3") is enforced structurally here: ReminderStore (used by
 * the policy service, materializer and HTTP handlers - all tenant-facing) has NO method
 * that can reach GSI3. Only ReminderProducerStore, injected exclusively into the
 * ReminderProducer worker, exposes it. test/integration/gsi3-isolation.test.ts asserts
 * this structurally (no tenant-facing dependency graph can reach queryGsi3).
 */
// 2026-08-19 (Engineering Maturity Review): imports from shared/dynamodb/occ.ts directly
// rather than re-exporting through expiration/ports/expiration-store.js - the previous
// indirection was an accidental cross-module ports->ports dependency (this module doesn't
// actually need anything Expiration-specific here, just the generic DynamoDB shapes).
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface ReminderStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /**
   * All rows under an item's own partition whose SK begins with `skPrefix` (default
   * `"OCC#"`, ReminderOccurrence rows). Strongly consistent - base-partition read, not a
   * GSI (data-model.md Â§5). BLOCKER-B reuses this exact query shape with `"POLICYREF#"` to
   * discover which ITEM-scoped policies point at an item (reminder-delivery-pipeline.md
   * Â§5) - deliberately generalized rather than adding a second near-identical method.
   */
  queryByItem<T extends EntityKey = Record<string, unknown> & EntityKey>(tenantId: string, itemId: string, skPrefix?: string): Promise<T[]>;
}

export interface Gsi3QueryInput {
  gsi3pk: string;
}

/** Narrow port, injected ONLY into the ReminderProducer worker (see file header). */
export interface ReminderProducerStore {
  queryGsi3<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi3QueryInput): Promise<T[]>;
  /** Strongly consistent read of the base item, used by the producer to reconstruct tenant context before claiming (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** Conditional SCHEDULED -> CLAIMED transition (implementation-blueprint.md Â§9.3 point 3). */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
}

/** Parses `GSI3SK = TENANT#<tenantId>#OCCURRENCE#<occurrenceId>` back into its parts - used by the ReminderProducer to reconstruct tenant context from a GSI3 query result before the strongly-consistent base-item read (data-model.md Â§3: "tenantId preservado... para reconstruÃ§Ã£o segura do contexto"). */
export function parseGsi3Sk(gsi3sk: string): { tenantId: string; occurrenceId: string } {
  const match = /^TENANT#(.+)#OCCURRENCE#(.+)$/.exec(gsi3sk);
  if (!match) {
    throw new Error(`Malformed GSI3SK: ${gsi3sk}`);
  }
  return { tenantId: match[1] as string, occurrenceId: match[2] as string };
}

/**
 * DocumentChasingOccurrence + DocumentChasingIntent â€” M10 cluster 4 (D-039/D-046/D-048).
 * Agregados-irmÃ£os de `ReminderOccurrence`/`NotificationIntent` (M3/M4, jÃ¡ em produÃ§Ã£o real)
 * â€” NUNCA generalizam esses agregados (mesmo princÃ­pio jÃ¡ aplicado ao `parser-sandbox` isolado
 * de M7): implementam interfaces mÃ­nimas compartilhadas sÃ³ para reusar a MECÃ‚NICA operacional
 * (GSI3 scheduler, claim OCC, outbox, reconciliaÃ§Ã£o), sem alterar o shape persistido do que jÃ¡
 * estÃ¡ verificado em produÃ§Ã£o.
 *
 * ColeÃ§Ã£o sob a MESMA partiÃ§Ã£o do `DocumentRequest` (`TENANT#t#SUBJECT#s`), nunca uma partiÃ§Ã£o
 * prÃ³pria â€” mesmo padrÃ£o de `ReminderOccurrence` co-localizado sob o item.
 *
 * GSI3: reaproveita o MESMO Ã­ndice/shards do scheduler de reminders (D-046, mini-revisÃ£o de
 * capacidade fechada â€” pico orgÃ¢nico combinado ~220Ã— abaixo do SLO de drenagem de pico extremo).
 * O discriminador entre os dois tipos vive na FORMA da GSI3SK (`...#CHASING#...` vs.
 * `...#OCCURRENCE#...`, ver `chasingGsi3SkPattern`/`gsi3-parse.ts`), nunca num atributo separado
 * â€” uma linha de GSI3 sÃ³ carrega PK/SK do Ã­ndice mais as chaves base da tabela (projeÃ§Ã£o
 * KEYS_ONLY), entÃ£o o discriminador precisa estar na prÃ³pria SK para o producer decidir o
 * caminho ANTES do get fortemente consistente.
 */
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import { stableHash } from "../../reminder/domain/reminder-occurrence.js";
import type { AuthorizedTenantId } from "../../identity/domain/authorization.js";

export type DocumentChasingTier = "T7" | "T3" | "EXPIRED";
export type DocumentChasingOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED";

export interface DocumentChasingOccurrence extends EntityKey {
  entityType: "DocumentChasingOccurrence";
  occurrenceId: string;
  tenantId: string;
  subjectId: string;
  assignmentId: string;
  documentRequestId: string;
  tier: DocumentChasingTier;
  scheduledAt: string; // UTC ISO-8601 instant
  documentRequestVersion: number; // versÃ£o esperada do DocumentRequest no momento da materializaÃ§Ã£o â€” mesma checagem de staleness que itemVersion faz para reminders
  shard: string;
  shardFnVersion: number;
  status: DocumentChasingOccurrenceStatus;
  claimedAt?: string;
  claimExpiresAt?: string;
  version: number;
  createdAt: string;
  updatedAt: string;
  GSI3PK?: string; // presente sÃ³ enquanto status === SCHEDULED (ou CLAIMED) â€” removido em TRIGGERED/CANCELLED, mesmo invariante de ReminderOccurrence
  GSI3SK?: string;
  GSI6PK?: string; // WORKSTATE#CLAIMED (constante compartilhada com reminders) enquanto CLAIMED
  GSI6SK?: string;
}

export function documentChasingOccurrenceKey(tenantId: AuthorizedTenantId, subjectId: string, assignmentId: string, documentRequestId: string, scheduledAt: string, occurrenceId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: `REQASSIGN#${assignmentId}#DOCREQ#${documentRequestId}#CHASING#${scheduledAt}#${occurrenceId}` };
}

function minuteBucket(scheduledAtUtcIso: string): string {
  const d = new Date(scheduledAtUtcIso);
  const pad = (n: number, len = 2) => String(n).padStart(len, "0");
  return `${d.getUTCFullYear()}${pad(d.getUTCMonth() + 1)}${pad(d.getUTCDate())}${pad(d.getUTCHours())}${pad(d.getUTCMinutes())}`;
}

/** Mesma fÃ³rmula de shard/minuto de `reminder-occurrence.ts#gsi3Keys` (reaproveita `stableHash`,
 * nunca reimplementa) â€” sÃ³ a forma da GSI3SK muda (`CHASING#` em vez de `OCCURRENCE#`), o
 * discriminador real que o producer usa para rotear cada linha lida do GSI3. */
export function chasingGsi3Keys(input: { tenantId: AuthorizedTenantId; occurrenceId: string; scheduledAt: string; shardCount: number }): { GSI3PK: string; GSI3SK: string; shard: string } {
  const shardNum = stableHash(input.occurrenceId) % input.shardCount;
  const shard = String(shardNum).padStart(2, "0");
  return {
    GSI3PK: `DUE#${minuteBucket(input.scheduledAt)}#${shard}`,
    GSI3SK: `TENANT#${input.tenantId}#CHASING#${input.occurrenceId}`,
    shard,
  };
}

const CHASING_GSI3SK_PATTERN = /^TENANT#(.+)#CHASING#(.+)$/;

/** Discriminador real usado pelo producer (`src/workers/reminder-producer/producer.ts`) para
 * decidir, ANTES de qualquer I/O, se uma linha do GSI3 Ã© chasing, reminder, ou nenhum dos dois
 * (fail-closed + alarme, D-039). Nunca lanÃ§a â€” parse estrutural puro. */
export function parseChasingGsi3Sk(gsi3sk: string): { tenantId: string; occurrenceId: string } | undefined {
  const match = CHASING_GSI3SK_PATTERN.exec(gsi3sk);
  if (!match) return undefined;
  return { tenantId: match[1] as string, occurrenceId: match[2] as string };
}

/** WORKSTATE#CLAIMED Ã© a MESMA constante global de `reconciliation-candidate-source.ts` â€”
 * reconciliaÃ§Ã£o de claim-expiry jÃ¡ lÃª esse workstate para qualquer entityType (D-048/D-046: sÃ³
 * o tipo TypeScript de `reconcileExpiredClaims` precisa alargar, o mecanismo Ã© idÃªntico). */
export function buildChasingClaimGsi6Sk(claimExpiresAt: string, tenantId: AuthorizedTenantId, occurrenceId: string): string {
  return `${claimExpiresAt}#TENANT#${tenantId}#CHASING#${occurrenceId}`;
}

export type DocumentChasingRecipientRef =
  | { kind: "EXTERNAL_EMAIL_SNAPSHOT"; email: string }
  | { kind: "INTERNAL_USER"; userId: string };

export type DocumentChasingIntentStatus = "PENDING" | "SENT" | "FAILED";

/** Agregado-irmÃ£o de `NotificationIntent` (D-039: "RecipientRef... escopado sÃ³ ao novo
 * DocumentChasingIntent â€” nÃ£o retrofitado no NotificationIntent existente"). Single-channel
 * (e-mail) em v1 â€” sem fan-out multi-canal, sem lease/retry (o prÃ³prio prÃ³ximo tier de chasing
 * Ã© o mecanismo de retry natural do produto). */
export interface DocumentChasingIntent extends EntityKey {
  entityType: "DocumentChasingIntent";
  intentId: string;
  tenantId: string;
  subjectId: string;
  assignmentId: string;
  documentRequestId: string;
  occurrenceId: string;
  tier: DocumentChasingTier;
  recipient: DocumentChasingRecipientRef;
  templateId: string;
  templateVersion: number;
  status: DocumentChasingIntentStatus;
  sentAt?: string;
  failureReason?: string;
  version: number;
  createdAt: string;
  updatedAt: string;
}

export function documentChasingIntentKey(tenantId: AuthorizedTenantId, subjectId: string, assignmentId: string, documentRequestId: string, intentId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#SUBJECT#${subjectId}`, SK: `REQASSIGN#${assignmentId}#DOCREQ#${documentRequestId}#CHASINGINTENT#${intentId}` };
}


web search: 
web search: site:docs.aws.amazon.com lambda SQS at least once duplicate processing partial batch response documentation ...
codex
A revisão fecha o bloqueante principal da Rodada 1, mas ainda não atinge o gate de 9,0. O desenho agora é estruturalmente retomável, porém a retomada ainda pode ser interrompida ou multiplicada pela semântica at-least-once da própria fila de scan.

1. Achado bloqueante da Rodada 1

Sim, foi fechado no aspecto essencial: nenhuma invocação precisa mais drenar um shard inteiro. Uma página tem limite explícito e também o teto de 1 MB do DynamoDB; `LastEvaluatedKey` usado como `ExclusiveStartKey` é o mecanismo oficial de paginação. Publicar a página antes de avançar o cursor transforma uma queda intermediária em duplicação, não perda. [AWS DynamoDB Query pagination](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Query.Pagination.html)

Há, contudo, uma imprecisão: uma página de até 1.000 itens, seguida de até 100 chamadas sequenciais de `SendMessageBatch` e retries, não é necessariamente “tipicamente sub-segundo”. Continua sendo trabalho limitado, portanto não reabre o bloqueante, mas timeout, concorrência, orçamento de retries e visibility timeout precisam ser dimensionados explicitamente.

2. Tipo GSI3 e falha parcial de publish

A discriminação está conceitualmente correta se o contrato final carregar `PK`, `SK` e `GSI3SK` bruto, e o consumer executar os dois parsers fail-closed. Eu não aceitaria a alternativa vaga “ou já pré-parseado”: um `entityType` desacoplado do `GSI3SK` cria duas fontes de verdade. O contrato deve escolher o valor bruto como autoridade, ter schema versionado e validar também que a entidade relida corresponde ao tenant, ID e tipo derivados da chave.

O tratamento de `SendMessageBatch.Failed[]` corrige diretamente a falha anterior: a AWS confirma que uma chamada pode retornar HTTP 200 com falhas por entrada. [AWS SendMessageBatch](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_SendMessageBatch.html)

Para ficar completo, deve constar expressamente:

- retry somente das entradas em `Failed[]`;
- exceção/timeout da chamada inteira tratado como resultado ambíguo, podendo duplicar entradas;
- continuação confirmada antes de reconhecer a mensagem de scan;
- `ReportBatchItemFailures` nos dois event-source mappings, salvo `batchSize=1` formalmente garantido.

A Lambda com SQS é at-least-once e pode entregar registros duplicados; partial batch response evita repetir registros bem-sucedidos do mesmo lote, mas não produz exactly-once. [AWS Lambda with SQS](https://docs.aws.amazon.com/lambda/latest/dg/with-sqs.html)

3. Riscos arquiteturais residuais

O principal risco restante está na própria cadeia de continuação. Uma entrega duplicada da mensagem de scan pode gerar duas mensagens de continuação para a mesma página. Cada uma pode gerar a página seguinte novamente, criando cadeias paralelas e amplificação recursiva de queries e publishes. OCC protege a transição final, mas não protege capacidade, custo nem o SLO do scanner. É necessário um `scanJobId/pageId` determinístico com checkpoint/fence idempotente, ou outra garantia equivalente que permita no máximo um avanço lógico por página.

Além disso:

- Uma continuação enviada à DLQ encerra aquela travessia antes das páginas restantes. “DLQ nativo” captura a falha, mas não assegura progresso. Política de alarme, replay/redrive e identidade idempotente do job fazem parte da arquitetura de recuperação, não são mero detalhe nível 3–4.
- `ApproximateAgeOfOldestMessage` apenas na fila de claim não detecta ocorrências ainda presas atrás de backlog na fila de scan. Ambas as filas precisam de idade/backlog, e deve permanecer uma métrica end-to-end baseada em `scheduledAt` ou minuto do scan.
- A própria AWS documenta que, em standard queues, poison pills podem ser excluídos da idade observada e que a idade reinicia ao mover para DLQ. Logo essa métrica não substitui sozinha `scheduler_lag_seconds`. [AWS SQS CloudWatch metrics](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-available-cloudwatch-metrics.html)
- Tratar qualquer `TransactionCanceledException` como simples corrida continua excessivamente amplo. O consumer deve distinguir a falha condicional esperada das demais cancellation reasons; throttling, conflito do outbox ou outra condição não podem ser reconhecidos silenciosamente como sucesso.
- A proposta não define controle de concorrência/backpressure do scanner e do claim consumer. Lambda/SQS pode escalar rapidamente; devem existir `maximumConcurrency`, reserved concurrency e capacidade DynamoDB consideradas para impedir que a solução do timeout vire throttling distribuído.

4. Checklist reconciliado

Os pesos 40/25/20/15 estão agora bem equilibrados e a fronteira IAM merece os 20% dados seu caráter deliberado e Type 1. A régua, entretanto, ainda está incompleta nas âncoras:

- “progresso durável” precisa exigir avanço idempotente, não apenas cursor após publish;
- “recuperação operacional” precisa exigir recuperação da continuação em DLQ e observar também a fila de scan;
- “simplicidade” não pode considerar atendido um encadeamento que ainda carece de checkpoint/fence contra duplicação at-least-once.

Minha avaliação da régua reconciliada é 8,7/10; portanto ela ainda não está estável pelo protocolo. O design avançou substancialmente e eliminou o bloqueante original, mas o risco de amplificação da cadeia de scan e de interrupção silenciosa após DLQ impede aprovação nesta rodada.

NOTA: 8.3/10
tokens used
51.658
A revisão fecha o bloqueante principal da Rodada 1, mas ainda não atinge o gate de 9,0. O desenho agora é estruturalmente retomável, porém a retomada ainda pode ser interrompida ou multiplicada pela semântica at-least-once da própria fila de scan.

1. Achado bloqueante da Rodada 1

Sim, foi fechado no aspecto essencial: nenhuma invocação precisa mais drenar um shard inteiro. Uma página tem limite explícito e também o teto de 1 MB do DynamoDB; `LastEvaluatedKey` usado como `ExclusiveStartKey` é o mecanismo oficial de paginação. Publicar a página antes de avançar o cursor transforma uma queda intermediária em duplicação, não perda. [AWS DynamoDB Query pagination](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Query.Pagination.html)

Há, contudo, uma imprecisão: uma página de até 1.000 itens, seguida de até 100 chamadas sequenciais de `SendMessageBatch` e retries, não é necessariamente “tipicamente sub-segundo”. Continua sendo trabalho limitado, portanto não reabre o bloqueante, mas timeout, concorrência, orçamento de retries e visibility timeout precisam ser dimensionados explicitamente.

2. Tipo GSI3 e falha parcial de publish

A discriminação está conceitualmente correta se o contrato final carregar `PK`, `SK` e `GSI3SK` bruto, e o consumer executar os dois parsers fail-closed. Eu não aceitaria a alternativa vaga “ou já pré-parseado”: um `entityType` desacoplado do `GSI3SK` cria duas fontes de verdade. O contrato deve escolher o valor bruto como autoridade, ter schema versionado e validar também que a entidade relida corresponde ao tenant, ID e tipo derivados da chave.

O tratamento de `SendMessageBatch.Failed[]` corrige diretamente a falha anterior: a AWS confirma que uma chamada pode retornar HTTP 200 com falhas por entrada. [AWS SendMessageBatch](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_SendMessageBatch.html)

Para ficar completo, deve constar expressamente:

- retry somente das entradas em `Failed[]`;
- exceção/timeout da chamada inteira tratado como resultado ambíguo, podendo duplicar entradas;
- continuação confirmada antes de reconhecer a mensagem de scan;
- `ReportBatchItemFailures` nos dois event-source mappings, salvo `batchSize=1` formalmente garantido.

A Lambda com SQS é at-least-once e pode entregar registros duplicados; partial batch response evita repetir registros bem-sucedidos do mesmo lote, mas não produz exactly-once. [AWS Lambda with SQS](https://docs.aws.amazon.com/lambda/latest/dg/with-sqs.html)

3. Riscos arquiteturais residuais

O principal risco restante está na própria cadeia de continuação. Uma entrega duplicada da mensagem de scan pode gerar duas mensagens de continuação para a mesma página. Cada uma pode gerar a página seguinte novamente, criando cadeias paralelas e amplificação recursiva de queries e publishes. OCC protege a transição final, mas não protege capacidade, custo nem o SLO do scanner. É necessário um `scanJobId/pageId` determinístico com checkpoint/fence idempotente, ou outra garantia equivalente que permita no máximo um avanço lógico por página.

Além disso:

- Uma continuação enviada à DLQ encerra aquela travessia antes das páginas restantes. “DLQ nativo” captura a falha, mas não assegura progresso. Política de alarme, replay/redrive e identidade idempotente do job fazem parte da arquitetura de recuperação, não são mero detalhe nível 3–4.
- `ApproximateAgeOfOldestMessage` apenas na fila de claim não detecta ocorrências ainda presas atrás de backlog na fila de scan. Ambas as filas precisam de idade/backlog, e deve permanecer uma métrica end-to-end baseada em `scheduledAt` ou minuto do scan.
- A própria AWS documenta que, em standard queues, poison pills podem ser excluídos da idade observada e que a idade reinicia ao mover para DLQ. Logo essa métrica não substitui sozinha `scheduler_lag_seconds`. [AWS SQS CloudWatch metrics](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-available-cloudwatch-metrics.html)
- Tratar qualquer `TransactionCanceledException` como simples corrida continua excessivamente amplo. O consumer deve distinguir a falha condicional esperada das demais cancellation reasons; throttling, conflito do outbox ou outra condição não podem ser reconhecidos silenciosamente como sucesso.
- A proposta não define controle de concorrência/backpressure do scanner e do claim consumer. Lambda/SQS pode escalar rapidamente; devem existir `maximumConcurrency`, reserved concurrency e capacidade DynamoDB consideradas para impedir que a solução do timeout vire throttling distribuído.

4. Checklist reconciliado

Os pesos 40/25/20/15 estão agora bem equilibrados e a fronteira IAM merece os 20% dados seu caráter deliberado e Type 1. A régua, entretanto, ainda está incompleta nas âncoras:

- “progresso durável” precisa exigir avanço idempotente, não apenas cursor após publish;
- “recuperação operacional” precisa exigir recuperação da continuação em DLQ e observar também a fila de scan;
- “simplicidade” não pode considerar atendido um encadeamento que ainda carece de checkpoint/fence contra duplicação at-least-once.

Minha avaliação da régua reconciliada é 8,7/10; portanto ela ainda não está estável pelo protocolo. O design avançou substancialmente e eliminou o bloqueante original, mas o risco de amplificação da cadeia de scan e de interrupção silenciosa após DLQ impede aprovação nesta rodada.

NOTA: 8.3/10
