OpenAI Codex v0.156.1
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-6-astra
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: medium
reasoning summaries: none
session id: 01a0d0ee-48cd-73a3-834c-e58d151ffda9
--------
user
Continuando o protocolo Claude-Codex (AGENTS.md secao 4) sobre D-321 (superficie de autenticacao
direta). Rodada 1 voce deu nota 6.8/10 com 8 achados. Nota cega desta rodada: voce ainda NAO viu a
nota do Claude para a Rodada 2 (existe, registrada em arquivo separado que voce NAO precisa ler).

IMPORTANTE sobre a Rodada 1: sua propria critica apontou, corretamente, que a rodada nao foi
estritamente cega porque a auto-nota do Claude estava dentro do MESMO arquivo que voce leu
(round-1-claude-proposal.md). Isso foi corrigido a partir de agora - a auto-nota do Claude vive
sempre num arquivo separado (round-N-claude-selfgrade.md) que nao esta na lista de leitura abaixo.

Leia, nesta ordem:

1. docs/architecture/reviews/d321-direct-auth-adversarial-review/round-2-claude-revision.md
   (a revisao do Claude respondendo a cada um dos seus 8 achados da Rodada 1 - correcao aplicada,
   pendencia registrada, decisao explicita mantida, ou treplica com evidencia contestando)

Depois, verifique o codigo REAL contra cada resposta da revisao (nao aceite a palavra do Claude
sem checar):

2. src/modules/bff/http/bff-handlers.ts (achados 1 e 8 - requireSameSiteFetch() e
   parseJsonObjectBody() novos, aplicados as 6 rotas)
3. src/modules/bff/persistence/cognito-idp-auth-client.ts (achados 2 e 3 - forgotPassword
   dobrando InvalidParameterException, confirmSignUp/resendConfirmationCode com
   looksAlreadyConfirmed())
4. src/modules/bff/application/bff-auth-service.ts (achado 4 - loginWithPassword mapeando
   TRANSIENT_FAILURE/UNKNOWN_OUTCOME para DependencyUnavailableError)
5. test/unit/bff/bff-handlers.test.ts (testes novos cobrindo os achados 1, 4, 8 - confira se
   realmente exercitam o comportamento, nao so existem)
6. docs/engineering/performance/traces/perf-04-auth.mjs (fix do smoke test k6 para nao quebrar
   com o novo guard de Sec-Fetch-Site)

Para o achado 5 (device-remembering) especificamente, a revisao contesta sua conclusao da Rodada 1
com evidencia (grep de zero ocorrencias de ConfirmDevice/UpdateDeviceStatus/DEVICE_SRP_AUTH/
NewDeviceMetadata em todo src/, historico do commit que introduziu device_configuration em
2026-08-20 - um mes antes de D-321, aplicando-se igualmente ao fluxo OIDC/PKCE original que usa o
MESMO endpoint /oauth2/token ha semanas sem incidente relatado). Avalie esse contra-argumento com
rigor - ele esta correto? A citacao da documentacao AWS sobre "remembered devices... active"
realmente exige confirmacao de dispositivo por sessao (ConfirmDevice chamado), ou a mera presenca
da configuracao no user pool basta para ativar o modo incompativel? Se a evidencia do Claude for
sólida, diga isso explicitamente - nao mantenha um achado so por inercia. Se ainda houver duvida
real, diga exatamente que evidencia adicional fecharia a questao.

Para o achado 6 (signup como oraculo de existencia), a revisao optou por MANTER o 409 com
justificativa (UX real do frontend, e sua propria Rodada 1 ja ofereceu isso como saida aceitavel
se documentado). Avalie se a documentacao esta adequada e se essa decisao e razoavel dado o
estagio do projeto (AGENTS.md secao 1: sem usuario real).

De sua nota final (0-10, uma casa decimal, nunca arredondada) para esta rodada, avaliando se os 8
achados foram genuinamente resolvidos (corrigido/pendencia registrada explicitamente/decisao
justificada/treplica com evidencia real - qualquer um desses conta como resolvido para efeito de
nota, uma pendencia REGISTRADA nao e a mesma coisa que um gap ignorado) ou se algum ainda esta mal
resolvido. Aponte qualquer achado NOVO que a mudanca de codigo desta rodada tenha introduzido.
Termine com uma linha no formato exato:

NOTA_CODEX: X.X

codex
Vou ler a revisão e conferir as respostas no código e nos testes, sem abrir a autoavaliação do Claude. Para device remembering, vou confrontar o argumento com a documentação oficial da AWS.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content docs/architecture/reviews/d321-direct-auth-adversarial-review/round-2-claude-revision.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 112ms:
# D-321 â€” Rodada 2 (revisÃ£o Claude, endereÃ§ando os 8 achados da Rodada 1)

Nota do Codex na Rodada 1: **6.8/10**. Nota cega do Claude (registrada antes de ler a crÃ­tica,
arquivo separado desta vez): ver `round-2-claude-selfgrade.md`.

## Achado 1 â€” Gap de seguranÃ§a, alta: login-CSRF viÃ¡vel â€” CORRIGIDO

`requireSameSiteFetch()` novo em `bff-handlers.ts`, reaproveitando `isSameSiteFetch()` (jÃ¡
existente em `domain/csrf.ts`, mesma primitiva que `checkCsrf()` usa como sua primeira camada) â€”
aplicado Ã s 6 rotas D-3xx (`handleLoginPassword`, `handleSignUp`, `handleConfirmSignUp`,
`handleResendConfirmationCode`, `handleForgotPassword`, `handleConfirmForgotPassword`), rejeitando
com 403 qualquer requisiÃ§Ã£o cujo `Sec-Fetch-Site` nÃ£o seja `same-origin`/`none` â€” inclusive
ausente (fail-closed, mesma disciplina de `isSameSiteFetch`'s prÃ³prio comentÃ¡rio). Isso fecha
login-CSRF por construÃ§Ã£o: um navegador sob controle de uma pÃ¡gina cross-site NUNCA consegue
forjar esse header (Ã© controlado pelo prÃ³prio navegador, nÃ£o pelo JS da pÃ¡gina) â€” sÃ³ um chamador
que Ã© genuinamente o app (ou um script/cliente de primeira parte fora de um navegador, que pode
declarar `Sec-Fetch-Site: none` livremente, categoria de confianÃ§a distinta de "navegador sob
controle de terceiro") passa. `docs/engineering/performance/traces/perf-04-auth.mjs` (script de
smoke k6, chamador de primeira parte) atualizado para declarar `Sec-Fetch-Site: none` explicitamente
â€” sem isso, o mesmo fix que fecha o buraco real tambÃ©m quebraria esse smoke test.

## Achado 7 â€” Gap de seguranÃ§a, rate-limiting â€” REGISTRADO como pendÃªncia explÃ­cita, nÃ£o bloqueia

O prÃ³prio Codex propÃ´s esta saÃ­da ("aceitaria postergar o limitador dedicado mediante pendÃªncia
explÃ­cita, obrigatÃ³ria antes de usuÃ¡rios reais"). Registrado em `NEXT_SESSION_PROMPT.md`/
`decisions-log.md` como pendÃªncia OBRIGATÃ“RIA antes de `WHATSAPP`/login com usuÃ¡rio real (mesma
lista que jÃ¡ tem E-019) â€” nunca silenciosamente esquecido. Rate 10/burst 20 (nÃºmero real
confirmado pelo Codex, corrigindo o "burst=50" que a proposta da Rodada 1 citou errado â€” a proposta
usava o nÃºmero de outro mÃ³dulo/rota por engano) na stage do BFF continua sendo a Ãºnica defesa de
vazÃ£o atÃ© essa pendÃªncia ser endereÃ§ada.

## Achado 8 â€” Bug de correÃ§Ã£o, baixa: entrada malformada vira 500 â€” CORRIGIDO

`parseJsonObjectBody()` novo, compartilhado pelas 6 rotas D-3xx â€” JSON invÃ¡lido ou nÃ£o-objeto
(array, `null`, string crua) agora lanÃ§a `ValidationError` (400), nunca deixa `JSON.parse(...)`
sem tratamento estourar como 500.

## Achado 4 â€” Bug de correÃ§Ã£o, mÃ©dia: indisponibilidade no login vira "credencial invÃ¡lida" â€” CORRIGIDO

`loginWithPassword` agora mapeia `TRANSIENT_FAILURE`/`UNKNOWN_OUTCOME` para
`DependencyUnavailableError` (503) â€” mesma disciplina que `signUp`/`confirmSignUp`/
`resendConfirmationCode`/`startForgotPassword`/`confirmForgotPassword` jÃ¡ tinham nesta mesma
classe. Apenas os outcomes genuinamente relacionados a credencial (`INVALID_CREDENTIALS`,
`USER_NOT_CONFIRMED`, `PASSWORD_RESET_REQUIRED`, `UNSUPPORTED_CHALLENGE`) continuam o
`AuthenticationError` genÃ©rico Ãºnico (anti-enumeraÃ§Ã£o preservada â€” 503 nÃ£o distingue nada sobre a
conta, sÃ³ que o sistema nÃ£o pÃ´de processar agora).

## Achado 3 â€” Bug de correÃ§Ã£o + gap de seguranÃ§a, mÃ©dia: confirmaÃ§Ã£o infere sucesso de exceÃ§Ã£o genÃ©rica â€” CORRIGIDO (parcialmente, ver nota)

`confirmSignUp`/`resendConfirmationCode` (adapter) agora sÃ³ mapeiam `NotAuthorizedException`/
`InvalidParameterException` para `ALREADY_CONFIRMED` quando a mensagem contÃ©m o texto documentado
("already confirmed"/"current status is confirmed", `looksAlreadyConfirmed()`, case-insensitive) â€”
qualquer outra causa cai em `UNKNOWN_OUTCOME` (503 no `BffAuthService`), nunca um falso sucesso
silencioso. **Nota honesta, nÃ£o escondida**: o texto exato da mensagem do Cognito NÃƒO Ã© parte do
contrato documentado da API (sÃ³ o TIPO da exceÃ§Ã£o Ã©) â€” este Ã© um narrowing best-effort, defesa em
profundidade, nÃ£o uma garantia formal. O orÃ¡culo residual que o Codex apontou (conta confirmada +
cÃ³digo arbitrÃ¡rio = 204, conta pendente + cÃ³digo errado = 400) continua existindo estruturalmente
â€” Ã© uma consequÃªncia inerente de `confirmSignUp` precisar ser idempotente para "jÃ¡ confirmado"
(UX real: usuÃ¡rio clica no link de confirmaÃ§Ã£o duas vezes), nÃ£o algo que dÃ¡ para eliminar sem
remover essa idempotÃªncia. Julgamento: o vazamento residual ("este e-mail tem uma conta
confirmada") Ã© estritamente mais fraco que o orÃ¡culo JÃ ACEITO do achado 6 (signup 409 jÃ¡ revela
"este e-mail tem conta", ponto final) â€” nÃ£o Ã© uma nova classe de informaÃ§Ã£o, Ã© um refinamento do
mesmo fato jÃ¡ exposto por design. NÃ£o fechado 100%, mas o bug de correÃ§Ã£o real (qualquer
`NotAuthorizedException` virando sucesso) estÃ¡.

## Achado 2 â€” Gap de seguranÃ§a, mÃ©dia: forgot-password sem anti-enumeraÃ§Ã£o completa â€” CORRIGIDO

`forgotPassword` (adapter) agora tambÃ©m dobra `InvalidParameterException` em `SUCCESS`, mesmo
tratamento que `UserNotFoundException` jÃ¡ tinha â€” fecha o orÃ¡culo de 3 vias (202 real, 202
nÃ£o-existe, 503 "existe mas sem atributo verificado para receber cÃ³digo") que a Rodada 1 apontou,
citando a documentaÃ§Ã£o oficial da AWS sobre supressÃ£o de existÃªncia.

## Achado 6 â€” Gap de seguranÃ§a, mÃ©dia, jÃ¡ admitido: signup Ã© orÃ¡culo de existÃªncia â€” MANTIDO, DECISÃƒO EXPLÃCITA REGISTRADA

DecisÃ£o consciente de MANTER o `409` (nÃ£o seguir a recomendaÃ§Ã£o do Codex de resposta uniforme).
RazÃµes: (1) `SignUp.tsx` (frontend) jÃ¡ tem UX real dependente dessa distinÃ§Ã£o ("JÃ¡ existe uma conta
com este e-mail.") â€” removÃª-la Ã© uma mudanÃ§a de produto/UX real, nÃ£o sÃ³ um endurecimento de
seguranÃ§a, e degradaria a experiÃªncia de um usuÃ¡rio legÃ­timo que esqueceu que jÃ¡ tem conta; (2) o
prÃ³prio Codex ofereceu esta saÃ­da como aceitÃ¡vel ("se o produto mantiver 409 deliberadamente,
registrar a aceitaÃ§Ã£o especÃ­fica"); (3) mesmo comportamento que a Hosted UI jÃ¡ tinha antes de
D-321 â€” nÃ£o Ã© uma regressÃ£o introduzida por esta mudanÃ§a. Registrado aqui, em `decisions-log.md` e
em `NEXT_SESSION_PROMPT.md` como decisÃ£o explÃ­cita e nomeada, nÃ£o como omissÃ£o silenciosa.

## Achado 5 â€” Bug de integraÃ§Ã£o, alta: tokens novos incompatÃ­veis com refresh existente â€” CONTESTADO (trÃ©plica com evidÃªncia)

Pesquisa externa feita (AGENTS.md Â§4, AWS oficial): a pÃ¡gina do endpoint `/oauth2/token`
confirma literalmente o texto que o Codex citou â€” "Users who sign in with the API operations
`InitiateAuth`... can refresh their tokens with the token endpoint when remembered devices is
*not* active in your user pool." Isto Ã© real e a citaÃ§Ã£o do Codex estÃ¡ correta quanto ao texto.

Mas a pÃ¡gina de device tracking (`amazon-cognito-user-pools-device-tracking.md`) esclarece o que
"ativo" significa na prÃ¡tica: dispositivos sÃ³ passam a ser "lembrados"/rastreados quando o app
efetivamente chama `ConfirmDevice`/`UpdateDeviceStatus` com o `DeviceKey`/`DeviceGroupKey`
retornados em `NewDeviceMetadata` â€” a simples presenÃ§a do bloco `device_configuration` no user
pool (Terraform) nÃ£o coloca nenhuma sessÃ£o real em estado "rastreado" por si sÃ³.

EvidÃªncia concreta, verificada nesta rodada:
1. `grep -rn "ConfirmDevice\|UpdateDeviceStatus\|DEVICE_SRP_AUTH\|NewDeviceMetadata" src/` â€” **zero
   ocorrÃªncias em todo o repositÃ³rio**. Nem o adapter novo (`CognitoIdpAuthClient`) nem o
   `CognitoOidcClient`/`fetch-cognito-oidc-client.ts` (fluxo OIDC/PKCE original) jamais chamam
   `ConfirmDevice`. `NewDeviceMetadata`, se retornado, Ã© descartado, nunca usado.
2. `device_configuration` (`challenge_required_on_new_device`/`device_only_remembered_on_user_prompt`)
   existe desde o commit `57942e11` (2026-08-20 â€” "Add Terraform cognito module"), **um mÃªs antes
   de D-321**, aplicando-se IGUALMENTE ao fluxo OIDC/PKCE original (`handleCallback`/`refresh()`
   via o MESMO endpoint `/oauth2/token`, nunca alterado por D-321).
3. Esse fluxo original processou logins reais contra `dev` por semanas (D-053 em diante, mÃºltiplas
   sessÃµes de teste real, k6, e2e) sem nenhum incidente relatado de "refresh falha silenciosamente"
   â€” se a mera presenÃ§a do bloco `device_configuration` bastasse para ativar o modo "rastreado"
   incompatÃ­vel com `/oauth2/token`, isso jÃ¡ teria quebrado o refresh do fluxo original hÃ¡ um mÃªs,
   e nunca quebrou.

**ConclusÃ£o**: o achado cita uma ressalva real e documentada da AWS, mas a aplica Ã  interpretaÃ§Ã£o
errada de "ativo" (config presente no pool vs. dispositivo efetivamente confirmado por sessÃ£o) â€”
neste cÃ³digo especÃ­fico, nenhuma sessÃ£o jamais confirma um dispositivo, entÃ£o "remembered devices"
nunca fica "ativo" para nenhum usuÃ¡rio, em nenhum dos dois fluxos. NÃ£o vejo motivo para classificar
como bloqueador. Aceito, porÃ©m, que a ambiguidade da prÃ³pria documentaÃ§Ã£o da AWS nÃ£o Ã© 100%
dirimida por esta inferÃªncia sozinha â€” fechamento total exigiria uma evidÃªncia empÃ­rica direta
(login via `POST /bff/login` real, esperar o access token expirar, confirmar refresh bem-sucedido
contra `dev`), que nÃ£o foi executada nesta rodada (fora do escopo de teste local, mesma disciplina
de nunca rodar `terraform apply`/testes reais contra `dev` sem necessidade clara). Registrado como
item de verificaÃ§Ã£o empÃ­rica pendente, nÃ£o como bug confirmado nem como fechamento sem ressalva.

## Auto-nota cega (Claude), Rodada 2

Ver `round-2-claude-selfgrade.md` (arquivo separado, nÃ£o incluÃ­do no que o Codex lÃª nesta rodada).

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content NEXT_SESSION_PROMPT.md; Get-Content docs/architecture/README.md; Get-Content docs/engineering/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 84ms:
# Expiration Tracker â€” Estado Atual + PrÃ³xima AÃ§Ã£o

> Este arquivo Ã© estado atual + prÃ³xima aÃ§Ã£o (`AGENTS.md` Â§2), nunca fonte normativa e nunca histÃ³rico narrativo â€” histÃ³ria detalhada vive em `docs/architecture/{session-log,decisions-log}.md`, `docs/engineering/decisions-log.md` e nas pastas `reviews/`. Cada linha abaixo deve caber em 1-3 frases: o quÃª + status + referÃªncia D-xxx/E-xxx para detalhe completo. **Recompactado em 2026-09-08 (D-236, segunda reconciliaÃ§Ã£o de engenharia de contexto â€” a de 2026-08-29 jÃ¡ tinha feito isso uma vez, 1067â†’78 linhas; o arquivo tinha reacumulado ~134 KB/~15.600 palavras em sÃ³ ~299 linhas, achado E-022/full-audit-round2, nota 6,84/10).** Antes de adicionar uma entrada nova aqui: se o D-nÃºmero/E-nÃºmero jÃ¡ tem linha completa em `decisions-log.md` (deveria sempre ter), esta seÃ§Ã£o sÃ³ recebe 1-3 frases â€” nunca reconte a narrativa.

## Branch / as-of

**NÃ£o confie nesta seÃ§Ã£o sem confirmar.** `git branch --show-current` deve ser `develop`; `git log --oneline -5`, `git status` e `git pull` antes de assumir qualquer coisa abaixo como pendente ou concluÃ­do â€” mÃºltiplas sessÃµes/agentes trabalham neste repo em paralelo.

**PadrÃ£o de trabalho autÃ´nomo (Marcelo, 2026-09-01, vale para toda sessÃ£o futura)**: prosseguir continuamente enquanto houver trabalho de engenharia real a fazer â€” nunca parar para pedir "posso continuar?". SÃ³ pausar/perguntar quando o prÃ³ximo passo depender genuinamente de decisÃ£o exclusiva de Marcelo (produto/arquitetura, execuÃ§Ã£o destrutiva/irreversÃ­vel, gasto de infra nÃ£o trivial, ou algo que `AGENTS.md` Â§4 exige elevar a ele). Nesses casos: registrar o pendente aqui/`decisions-log.md`, seguir para outra frente independente, nunca ficar ocioso.

## Fase atual

`Consolidation + Pilot Readiness` com recomendaÃ§Ã£o **CONDITIONAL GO**. M0-M12 (exceto billing/D-052, bloqueado por fornecedor) e Multi-User B2B (15 waves, D-084 a D-120) implementados e deployados. DomÃ­nio Documental completo (D-143 a D-235, ver roadmap abaixo). Full-audit-round2 (auditoria cÃ­clica por eixo, `docs/engineering/joint-review-criteria.md`) em andamento desde 2026-09-07 â€” ver seÃ§Ã£o dedicada abaixo para o que estÃ¡ aberto por eixo.

## Roadmap de lanÃ§amento (`docs/project/roadmap-competitivo-2026-09-01.md`) â€” 11 itens P0

**Todos os 11 itens ðŸŸ¢ IMPLEMENTADOS por completo** (Requirement Templates D-191; Bulk import D-192;
WhatsApp D-197/ADR-0012+D-286; IA/OCR D-193; Busca/filtros D-194/D-196; Dashboard D-196; RelatÃ³rios
D-195; Document Types D-173-D-186/D-221/D-224/D-243/D-244; Guest Upload+Requests+Review+Recurrence
D-222/D-226-D-230; Storage/Versioning/Renewal D-163-D-168; Frontend 25 telas D-254-D-271/D-292/D-293).
Detalhe item-a-item nunca recontado aqui: `docs/architecture/decisions-log.md`. Ãšnicas pendÃªncias
residuais, ambas fora de engenharia: **WhatsApp** aguarda E-019 (jurÃ­dico â€” aviso de privacidade,
DPA Meta, residÃªncia de dados); **Identidade visual** (workstream paralelo de Marcelo) â€” Fase 1
concluÃ­da 2026-09-11, Fase 2 avanÃ§ou nesta sessÃ£o (2026-09-20): design system v2 (violeta, Plus
Jakarta Sans, Ã­cones Lucide) auditado, corrigido e adotado como base oficial â€”
`docs/architecture/adr/ADR-0015-visual-identity-v2-violet.md`/D-307, artefato em
`docs/frontend/design-system-v2/`. **Porte para o cÃ³digo real: 31/31 telas FECHADAS** (as 13 do
protÃ³tipo original + as 18 do levantamento de rotas reais sem protÃ³tipo, ver abaixo) â€” todas
commitadas e pushadas em `develop` (2026-09-21, commits `637d1a0d`..`eebb3ebb`). Falta sÃ³ a
validaÃ§Ã£o visual de Marcelo (screenshots em `prototype/_tmp_validacao/01` a `25`).

**As 18 telas sem protÃ³tipo original** (Onboarding, Aceitar convite, Form de Fornecedor,
Requisitos/lista geral, SolicitaÃ§Ãµes e RecorrÃªncia, Rastreamento Legado, Exportar DossiÃª, Fila de
RevisÃ£o, Detalhe de Documento, CatÃ¡logo de Tipos de Documento, Editor de Tipo de Documento,
Templates de Requisito, PreferÃªncia de Entrega, PreferÃªncias de NotificaÃ§Ã£o, Membros, Log de
Atividade, RelatÃ³rios, Upload Legado de Convidado) tiveram protÃ³tipos gerados via Claude Design a
partir de prints reais (`prototype/telas-sem-prototipo/`) e foram portadas 01-06 numa sessÃ£o
anterior, 07-18 nesta sessÃ£o (2026-09-21). Achados reais do processo: barra lateral sticky +
rodapÃ© de identidade (`SidebarUserFooter`, estendeu `GET /bff/session` com `displayName`/`email`,
nunca `userId`/D-095-096); a maioria das 12 telas 07-18 jÃ¡ herdava os componentes v2 globalmente
e precisou sÃ³ de Ã­cones Lucide + agrupamento em `Panel` â€” nenhum componente novo foi inventado em
nenhuma das 18. Nomes/rÃ³tulos de papel e status de Membros passaram a usar `StatusBadge`/
`presentMembershipRole` em vez de enum cru (novas `presentMembershipStatus`/
`presentInvitationStatus` em `presentation.ts`).

**TambÃ©m fechados nesta sessÃ£o (2026-09-21)**: itens #15/#16 (resoluÃ§Ã£o de nome/e-mail do
responsÃ¡vel via claim OIDC, filtro de atividade por `resourceId`) e D-313 (download de documento).
D-314 (versionamento completo de Document) segue `PENDING_PROTOCOL_REVIEW`, sem cÃ³digo escrito.
(`Proximas_Tarefas_Identidade_Visual.md`, raiz do repo, deliberadamente fora do
`ROOT_MD_ALLOWLIST` â€” nunca commitar sem mover para `docs/` ou atualizar o allowlist.)

**PrÃ³xima aÃ§Ã£o real deste workstream**: aguardar validaÃ§Ã£o visual de Marcelo das 18 telas
(screenshots `prototype/_tmp_validacao/08` a `25`) antes de considerar o workstream de identidade
visual v2 encerrado. Nenhum protÃ³tipo novo pendente no momento.

## Nova capacidade fora do roadmap original: quota de armazenamento por tenant (D-249, 2026-09-09)

ðŸŸ¢ FECHADO POR COMPLETO â€” backend (tracking/enforcement/leitura, `TenantStorageQuota`) e UI real (`StorageQuotaCard`/`StorageSection`, Bloco 1/D-255-256) implementados e testados. Quota default 8GB, confirmada por Marcelo apÃ³s pesquisa de mercado. Detalhe completo: `docs/architecture/decisions-log.md` D-249, `docs/architecture/reviews/storage-quota-scoping/`.

## Backlog pÃ³s-lanÃ§amento P1 (autorizado 2026-09-04)

**Fechado por completo, exceto item 3.** Itens 1/2/4-8 ðŸŸ¢ DONE (reminder sequences M3; escalation
D-199-D-201; relatÃ³rios agendados D-204/D-211-D-215; dossiÃª PDF/Excel D-205/D-216/D-217/D-235;
bulk actions D-206/D-207/D-209/D-210; metadata por Document Type D-218-D-221; `ExternalShareLink`
D-273, aplicado em `dev` CD run 34704323049). Item 3 (busca OCR/full-text) ðŸ”´ **BLOQUEADO** â€”
decisÃ£o de Marcelo entre 3 caminhos nomeados em D-202. Detalhe item-a-item: `decisions-log.md`.
- **P2** (nÃ£o escopado): assinatura eletrÃ´nica; API pÃºblica; webhooks; integraÃ§Ãµes de calendÃ¡rio;
  compliance score avanÃ§ado; assistente de IA conversacional no produto (via API Claude direto,
  ver `docs/project/integrations-and-tooling-research-2026-09-11.md` Â§5).
- **Futuro** (sem gatilho comercial): portal completo do cliente; SSO/SCIM/controles enterprise.

## Full-audit round2 (`docs/engineering/joint-review-criteria.md`) e auditoria externa de 2026-09-12

Ambas substancialmente fechadas â€” todo achado HIGH/ALTA corrigido (E-015/E-016/E-018/E-020/E-021,
achados P0.1-P0.4/P0.6/P1.6/P2.1-P2.4 da auditoria externa: ver `decisions-log.md` D-232 a D-290
para detalhe item-a-item, nunca recontado aqui). **Ãšnico gate ainda nÃ£o atingido, decisÃ£o-dependente,
nÃ£o engenharia**: **E-019** (jurÃ­dico â€” aviso de privacidade/DPA Meta/residÃªncia de dados, bloqueia
WhatsApp com usuÃ¡rio real). **E-023** teve seu achado pendente de `coverage.thresholds` resolvido
2026-09-21 (D-317, `vitest.config.ts`). **E-017** teve seus 2 achados pendentes corrigidos
(`definition-of-done.md`), drift desta linha corrigido 2026-09-19.

## PendÃªncias reais que dependem de decisÃ£o de Marcelo (lista consolidada)

0. ~~CI quebrado (PR #380)~~ â€” **RESOLVIDO 2026-09-20**: Marcelo autorizou o reset de senha Cognito
   do "PERF Test Tenant" + atualizaÃ§Ã£o do secret `PERF_TEST_PASSWORD` do GitHub Actions; organizaÃ§Ã£o
   recriada via `POST /bff/organizations` (novo `organizationId=org_01M2ZS4523K9QBJ4DX223WGMFD`,
   substitui o antigo `org_01M2GE4F1SZPSJ47HCGRXH4XMN` apagado na limpeza de `dev`); todos os checks
   do PR #380 verdes; PR mergeado em `main` (`mergedAt=2026-09-20T16:08:53Z`).
1. Item 3 do backlog P1 (busca OCR/full-text) â€” escolher entre 3 caminhos nomeados em D-202.
2. `--include-cognito` de `scripts/reset-dev-data.ts` contra `dev` â€” nÃ£o executado (fora do escopo autorizado 2026-09-20, ver seÃ§Ã£o de limpeza abaixo); postergado, nÃ£o perguntar de novo atÃ© ele sinalizar.
3. ~~`coverage.thresholds` em `vitest.config.ts`~~ â€” **RESOLVIDO 2026-09-21 (D-317)**: medido primeiro
   (statements 86,55%/branches 84,84%/functions 86,9%/lines 86,55%, 270 arquivos/3148 testes),
   thresholds configurados ~1,5-2pts abaixo (85/83/85/85) como guard-rail sem quebrar CI.
4. WhatsApp com usuÃ¡rio real (item 3 P0, engenharia 100% fechada desde D-286 â€” rota de opt-in tambÃ©m jÃ¡ construÃ­da) â€” resta sÃ³ aviso de privacidade, DPA Meta, residÃªncia de dados (E-019).
5. Wave 1b (Design System) â€” quais componentes com overlay/focus-trap (`Combobox`/`DateInput`/`Tooltip`/`Popover`/`DropdownMenu`/`Modal`/`Drawer`/`Tabs`/`Pagination`/`Breadcrumb`/`Avatar`/`Card`) abordar primeiro â€” deliberadamente por Ãºltimo, por pedido de Marcelo.
6. User Validation (planejamento de interface) â€” aguarda sinal explÃ­cito dele.
7. Aplicar ao **Claude for Startups Program** (`claude.com/programs/startups`, atÃ© US$25.000 em crÃ©ditos de API, sem exigir VC) â€” projeto se encaixa no perfil, mas o cadastro exige dados da empresa/aÃ§Ã£o direta de Marcelo. Ver `docs/project/integrations-and-tooling-research-2026-09-11.md` Â§6.
8. P0.5 â€” suÃ­te "Real System E2E" contra `dev` real (browserâ†’CloudFrontâ†’API Gatewayâ†’S3 sem mocks) â€” projeto de infra de teste novo, nÃ£o correÃ§Ã£o pontual; precisa de decisÃ£o sobre credenciais/tenant de teste, cadÃªncia de execuÃ§Ã£o e estratÃ©gia de limpeza antes de comeÃ§ar. Deliberadamente adiado, Marcelo 2026-09-14.
9. ~~Import CSV em massa para Items~~ â€” **RESOLVIDO 2026-09-21 (D-319, `PENDING_PROTOCOL_REVIEW`)**:
   4Âº tipo em `src/modules/import/` (`ImportTargetEntityType: "Item"`), mesmo padrÃ£o de
   `TrackedSubject`/`Document`/`Requirement` (D-042/D-192). 4 decisÃµes de produto tomadas e
   documentadas em D-319 (mapeamento fixo v1 sem `assigneeUserId`; `ReminderPolicy` sempre
   configurada depois, nunca na mesma linha; dedupe via chave sintÃ©tica
   `categoryNormalized|nameNormalized|dueDate` em `ImportDedupRecord`; validaÃ§Ã£o linha-a-linha
   reaproveitando 100% a infraestrutura existente). Commit worker reaproveita o protocolo de
   2 chamadas do ramo `TrackedSubject` (claim de dedupe + `ExpirationService.createItem()` como
   caixa-preta), nÃ£o o TENTATIVA/FALLBACK de `Document`/`Requirement` (`Item` nÃ£o tem referÃªncia
   nenhuma a resolver). `reserveImport()` generalizado para aceitar `targetEntityType` opcional
   (fechou incidentalmente o mesmo gap para `Document`/`Requirement`, que nunca tiveram via HTTP
   real de criaÃ§Ã£o de job). Sem tela de frontend nova (`ImportWizard.tsx` jÃ¡ Ã© documentadamente
   `TrackedSubject`-only, mesmo gap que os outros 2 tipos jÃ¡ tinham). Backend 271/3179 verdes
   (+31 novos), cobertura 86,63%/84,85%/86,95%/86,63% (acima do threshold 85/83/85/85, D-317).
   RevisÃ£o adversarial Codex pendente quando o protocolo voltar (ver item 20 abaixo).
10. ~~`NotificationEntitlements` nunca provisionado~~ â€” **RESOLVIDO 2026-09-21 (D-315,
    `PENDING_PROTOCOL_REVIEW`)**: seedado atomicamente na criaÃ§Ã£o da Organization
    (`CreateOrganizationService`), `email.enabled: true`/`whatsapp.enabled: false` por padrÃ£o.
    Tenants `dev`/sintÃ©ticos jÃ¡ existentes ANTES desta mudanÃ§a continuam sem o registro (sem
    backfill, aceitÃ¡vel sem usuÃ¡rio real). RevisÃ£o adversarial Codex pendente quando o protocolo
    voltar.
11. **RevisÃ£o adversarial Codex pendente (voluntÃ¡ria, nÃ£o `PENDING_PROTOCOL_REVIEW`) â€” horÃ¡rio padrÃ£o de lembretes (`Organization.defaultReminderLocalTime`)**: jÃ¡ implementado/mergeado, nÃ­vel 4 (protocolo nÃ£o exigido normativamente), mas Marcelo pediu rodada extra assim que Codex voltar (2026-09-23). Contexto: `docs/architecture/reviews/reminder-default-local-time/PROPOSAL.md`.
12. **SeparaÃ§Ã£o de ambientes (`ADR-0014`, D-305) â€” protocolo Claudeâ†”Codex + autorizaÃ§Ã£o de Marcelo pendentes para as Fases 2-4**: decisÃ£o/pesquisa/Fase 1 completas (ver item 2 da ordem acima). Assim que o protocolo voltar (Codex 2026-09-23), rodar a revisÃ£o adversarial completa deste ADR (nÃ­vel 6 â€” nota cega, â‰¥9,0, mÃ­nimo 3 rodadas). Independente disso, Fases 2-4 (criar conta AWS `staging`/`production`, provisionar, pipeline de promoÃ§Ã£o `devâ†’stagingâ†’produÃ§Ã£o`) exigem autorizaÃ§Ã£o explÃ­cita de Marcelo antes de qualquer execuÃ§Ã£o â€” nÃ£o Ã© uma decisÃ£o que a rodada Claudeâ†”Codex sozinha desbloqueia, Ã© criaÃ§Ã£o de fronteira de conta/billing real.
13. ~~`ci.yml` sem fila global de lock do Terraform~~ â€” **RESOLVIDO 2026-09-21 (D-318)**: job
    `infra` ganhou `concurrency` de nÃ­vel-job prÃ³prio (`group: terraform-dev-lock`,
    `cancel-in-progress: false`), e `cd.yml` renomeado de `cd-develop` para o mesmo
    `terraform-dev-lock` â€” os dois agora disputam a mesma fila real em vez de duas filas
    com nomes diferentes. `ci-${{ github.ref }}` de nÃ­vel-workflow continua intacto para os
    outros jobs de `ci.yml`.
14. ~~Endpoint agregado de contagem por urgÃªncia para a VisÃ£o Geral~~ â€” **RESOLVIDO 2026-09-21
    (D-316, `PENDING_PROTOCOL_REVIEW`)**: `GET /dashboard/summary` jÃ¡ existia (Roadmap P0.6) e jÃ¡
    computava os 3 nÃºmeros internamente â€” sÃ³ faltava expor `itemsOverdueCount`/
    `itemsExpiringSoonCount`/`activeItemsCount` separadamente. `Overview.tsx` usa os 3 reais
    agora, com degradaÃ§Ã£o graciosa se a agregaÃ§Ã£o falhar. Link "Ver todos os vencimentos"
    continua removido por redundÃ¢ncia (detalhe: `decisions-log.md` D-308/D-316).
15. ~~ResoluÃ§Ã£o de nome de usuÃ¡rio (ResponsÃ¡vel)~~ â€” **RESOLVIDO 2026-09-21**: `GET /organizations/members` agora resolve `email`/`displayName` do `GlobalUser` (sÃ³ quando identidade ACTIVE, mesma regra do `recipient-resolver.ts`); nome capturado via claim OIDC `name` no login (escopo `profile` adicionado). Frontend (Membros, "ResponsÃ¡vel" no Detalhe) mostra nome/e-mail resolvido com fallback pro ID.
16. ~~Filtro de atividade por item/recurso~~ â€” **RESOLVIDO 2026-09-21**: `GET /activity` aceita `resourceId`, mesmo padrÃ£o jÃ¡ usado por `resourceType`. Card "HistÃ³rico de auditoria" do Detalhe agora mostra contagem real e leva a um log prÃ©-filtrado.
17. ~~PR #382 (`develop`â†’`main`) aberto, nÃ£o mergeado~~ â€” **RESOLVIDO 2026-09-21**: fechado sem
    merge (defasado, sÃ³ 7/18 telas) a pedido explÃ­cito de Marcelo; PR #383 novo aberto com o
    estado atual de `develop` (31/31 telas + D-313 a D-316) e mergeado em `main`. CI da PR pegou
    3 regressÃµes reais de e2e/a11y nunca detectadas localmente (vitest nÃ£o cobre Playwright):
    sidebar sticky quebrando o check "nada Ã© sticky/fixed" (SC 2.4.11) em 5 specs â€” corrigido
    allowlisting `.app-shell__nav` por nome, nÃ£o Ã© violaÃ§Ã£o real (prÃ³pria coluna flex, nunca
    sobrepÃµe conteÃºdo); label stale com "*" literal em 2 testes do guest wizard apÃ³s reskin pro
    `SelectField` real; back-link "â† Voltar" (`PageHeader`'s `above`) abaixo do mÃ­nimo de 24px do
    WCAG 2.5.8, corrigido na CSS compartilhada `.ui-page-header__back`. SuÃ­te e2e completa
    (158/158) verde apÃ³s as correÃ§Ãµes, commit `590d40a3`.
18. ~~Falha prÃ©-existente em `documents.test.ts` (`computeChecksumSha256`)~~ â€” **RESOLVIDA (achado
    2026-09-21)**: os 2 casos passam de forma estÃ¡vel e reproduzÃ­vel (3 reruns isolados + 2 rodadas
    completas da suÃ­te) â€” provavelmente resolvida por um bump de dependÃªncia jsdom/Node desde D-311,
    nÃ£o por nenhuma mudanÃ§a de cÃ³digo nesta sessÃ£o. Nenhuma aÃ§Ã£o necessÃ¡ria.
19. **Versionamento completo de `Document` (item-level) â€” `PENDING_PROTOCOL_REVIEW` (D-314, 2026-09-21)**: pedido de Marcelo apÃ³s comparar a tela "Documento" real com o protÃ³tipo, que assume um modelo de "substituir arquivo" inexistente hoje (`Document` Ã© 1 linha = 1 arquivo, sem histÃ³rico/versÃ£o). InvestigaÃ§Ã£o confirmou que Ã© mudanÃ§a nÃ­vel 5-6, estruturalmente equivalente ao D-143 (DomÃ­nio Documental, 6 rodadas de protocolo) â€” nÃ£o implementado, nenhum cÃ³digo escrito. Assim que o protocolo Claudeâ†”Codex voltar (Codex 2026-09-23), rodar a revisÃ£o adversarial completa, incluindo a alternativa de menor risco identificada (reaproveitar a mÃ¡quina de versionamento jÃ¡ existente em `document-archive`/D-143 em vez de duplicÃ¡-la). "Baixar documento" (a outra metade do mesmo pedido) jÃ¡ foi implementado nesta sessÃ£o sem precisar de protocolo (D-313, nÃ­vel 1-3, aditivo puro).
20. **`NotificationEntitlements` seedado no onboarding e endpoint agregado de urgÃªncia da VisÃ£o Geral â€” ambos `PENDING_PROTOCOL_REVIEW` (D-315/D-316, 2026-09-21)**: implementados a pedido explÃ­cito de Marcelo (nÃ­vel 3-4 cada, cÃ³digo real escrito e testado â€” ver itens #10/#14 acima e `decisions-log.md` para detalhe), mas envolveram decisÃ£o de produto (polÃ­tica de entitlement do plano free; quais 3 nÃºmeros mapeiam para os cards) tomada sem o protocolo Claudeâ†”Codex formal. Rodar revisÃ£o adversarial quando o protocolo voltar (Codex 2026-09-23).
21. ~~PÃ¡gina de login customizada~~ â€” **REVISTO 2026-09-22 (D-321, `PENDING_PROTOCOL_REVIEW`)**:
    D-320 (Managed Login) revertido a pedido direto de Marcelo (fidelidade visual/consistÃªncia
    com o design system v2); UI prÃ³pria implementada (`frontend/src/routes/auth/*`) com login
    direto via `InitiateAuth`/`USER_PASSWORD_AUTH` no BFF, signup/verificaÃ§Ã£o de e-mail,
    esqueci-senha/redefiniÃ§Ã£o. Hosted UI/rotas OIDC originais mantidas como fallback dormente,
    nunca removidas. Detalhe completo das 4 decisÃµes tÃ©cnicas: `decisions-log.md` D-321.
22. **Import CSV em massa para Items â€” `PENDING_PROTOCOL_REVIEW` (D-319, 2026-09-21)**: implementado a pedido explÃ­cito de Marcelo (nÃ­vel 5-6, cÃ³digo real escrito e testado â€” ver item #9 acima e `decisions-log.md` D-319 para as 4 decisÃµes de produto), mas decidido/implementado sem o protocolo Claudeâ†”Codex formal (suspenso). Rodar revisÃ£o adversarial quando o protocolo voltar (Codex 2026-09-23) â€” atenÃ§Ã£o especial ao trade-off de dedupe (decisÃ£o 3: sem proteÃ§Ã£o contra colisÃ£o com Item criado fora de import) e Ã  generalizaÃ§Ã£o de `reserveImport()`'s `targetEntityType` (efeito colateral sobre Document/Requirement).
23. **Login/signup/reset de senha via UI prÃ³pria (reversÃ£o de D-320) â€” `PENDING_PROTOCOL_REVIEW` (D-321, 2026-09-22)**: ver item #21 acima e `decisions-log.md` D-321. Rodar revisÃ£o adversarial quando o protocolo voltar (Codex 2026-09-23) â€” atenÃ§Ã£o especial Ã  escolha `USER_PASSWORD_AUTH` (vs. SRP) e ao SECRET_HASH server-side no BFF (nova superfÃ­cie de autenticaÃ§Ã£o).
24. ~~CI vermelho pÃ³s-D-321 + drift do Managed Login~~ â€” **RESOLVIDO 2026-09-22 (D-322/D-323)**: specs e2e/gate k6 assumiam o redirect antigo pra Hosted UI, corrigidos. Gap aberto sem impacto real: `dev` continua `ManagedLoginVersion=2` (Terraform de D-321 nÃ£o forÃ§a downgrade, optional+computed) â€” sÃ³ importa se `GET /bff/login` reativar como fallback. Detalhe: `decisions-log.md` D-322/D-323.
25. **Toggle real de canal (E-mail/WhatsApp) â€” adiado por Marcelo (D-327)**: E-mail Switch Ã© sÃ³ visual, WhatsApp sÃ³ tem opt-in. NÃ£o iniciar sem pedido explÃ­cito.
26. ~~ConfirmaÃ§Ã£o de posse do nÃºmero no opt-in de WhatsApp~~ â€” **IMPLEMENTADO 2026-09-23,
    `PENDING_PROTOCOL_REVIEW`** (nÃ­vel 4-5, decisÃ£o de Marcelo: implementar direto e marcar para
    revisÃ£o adversarial Codex quando o protocolo voltar, em vez de esperar). Novo par
    `WhatsAppPhoneConfirmation` (domain+service, `whatsapp-phone-confirmation.ts`/
    `-service.ts`) â€” entidade IRMÃƒ de `WhatsAppOptIn`, nunca um campo de status nela: um cÃ³digo de
    6 dÃ­gitos (HMAC+pepper reaproveitado `GUEST_TOKEN_PEPPER`, TTL 10min, 5 tentativas, cooldown de
    reenvio de 60s) Ã© enviado via `WhatsAppProviderAdapter.send()` direto (fora do pipeline de
    outbox/intent/attempt de lembretes) e sÃ³ DEPOIS de confirmado Ã© que `WhatsAppOptInService.
    recordOptIn()` Ã© chamado â€” ou seja, todo `WhatsAppOptIn` criado a partir de agora jÃ¡ nasce
    possession-confirmed, sem migraÃ§Ã£o/backfill nem mudar `notification-router.ts`/
    `resolveRecipientPhone`. 2 rotas novas (`POST .../whatsapp-opt-in/request-confirmation` e
    `.../confirm`), gated no mesmo flag `WHATSAPP` (`isWhatsAppChannelEnabled`) que jÃ¡ existia â€”
    todo ambiente hoje retorna 503 "ainda nÃ£o disponÃ­vel" atÃ© a E-019 (item 4) liberar, o que Ã©
    coerente com o "Em breve" que a tela jÃ¡ mostrava (nunca uma contradiÃ§Ã£o). `notifications-
    handler.ts` ganhou o mesmo padrÃ£o de carregamento assÃ­ncrono de Secrets Manager+AppConfig que
    `whatsapp-delivery-handler.ts` jÃ¡ usava (memoizado por cold start), com fail-closed se as envs
    nÃ£o estiverem configuradas â€” nunca derruba o Lambda inteiro (que tambÃ©m serve `/notifications/
    preferences`, sem relaÃ§Ã£o com WhatsApp). Terraform: `notifications_handler` ganhou os mesmos
    `WHATSAPP_SECRET_ID`/`APPCONFIG_*`/`GUEST_TOKEN_PEPPER` e policies que `whatsapp_delivery` jÃ¡
    tinha (mesmo secret, IAM sÃ³ ampliado, nada novo provisionado) â€” `terraform validate`/`fmt`/
    `test` locais rodados (nunca `apply`, regra permanente). Frontend: `NotificationPreferences.tsx`
    trocou "Ativar WhatsApp" (chamada direta a `recordOptIn`) por um fluxo de 2 passos espelhando
    `VerifyEmail.tsx` ("Enviar cÃ³digo" â†’ campo de cÃ³digo + "Confirmar"/"Reenviar cÃ³digo") â€” hook e
    funÃ§Ã£o de API antigos (`useWhatsAppOptIn`/`recordWhatsAppOptIn`) removidos por ficarem mortos no
    frontend (a rota HTTP `POST .../whatsapp-opt-in` em si continua existindo/testada, sÃ³ nÃ£o Ã© mais
    chamada por esta tela). Testes: 7 domain + 11 service + 4 handler (backend, todos novos) + 4
    e2e atualizados (`block8-notification-preferences.spec.ts`, rodados de verdade contra
    `npm run build`+`vite preview`, nÃ£o sÃ³ mockados) + testes unitÃ¡rios de frontend atualizados.
    3232 testes backend / 466 frontend verdes, lint/typecheck limpos nos dois. **Achado incidental,
    nÃ£o corrigido (fora de escopo, prÃ©-existente)**: `infra/modules/api-gateway/tests/
    api_gateway.tftest.hcl`'s asserÃ§Ã£o de `/items/{itemId}/documents*` jÃ¡ esperava 6 rotas mas a
    config real tem 7 â€” drift nÃ£o relacionado a esta mudanÃ§a, nÃ£o meu para corrigir aqui. RevisÃ£o
    adversarial Codex pendente quando o protocolo voltar (ver item 11 e seguintes acima para o
    mesmo padrÃ£o de espera).

## PrÃ³xima aÃ§Ã£o recomendada

**P0/P1/full-audit round2/auditoria externa/identidade visual v2 (31/31 telas) sÃ£o contexto
histÃ³rico jÃ¡ fechado, nÃ£o a prÃ³xima aÃ§Ã£o â€” ver seÃ§Ãµes acima.** Degrau de 100k do Programa de
Performance encerrado (achado real confirmado, ver seÃ§Ã£o prÃ³pria acima â€” nÃ£o Ã© mais pendÃªncia).

**MANDATO AUTÃ”NOMO de Marcelo 2026-09-21 (itens #3/#9/#13/#21) â€” 100% CONCLUÃDO 2026-09-22**: os 4
itens resolvidos (D-317/D-319/D-318/D-321, ver lista consolidada de pendÃªncias acima), mais o
achado incidental de CI vermelho pÃ³s-D-321 corrigido (D-322/D-323). **PR #384 (`develop`â†’`main`)
aberto e MERGEADO 2026-09-22** (`main` em `01cb15d4`), CI verde em todos os jobs antes do merge.

**ContinuaÃ§Ã£o 2026-09-22 (mesma sessÃ£o, depois do mandato): reestruturaÃ§Ã£o visual de telas por
protÃ³tipo real enviado por Marcelo, uma de cada vez, sempre confirmada por screenshot real antes
do commit** â€” Criar Vencimento, busca de Fornecedores, Novo/Editar Fornecedor, Entrega de
SolicitaÃ§Ã£o, Log de Atividade, Requisitos documentais (tiles de mÃ©trica coloridos permanentemente,
`3aab86ed`), ImportaÃ§Ã£o em massa. Bug real de login (D-324/D-325, 2 rodadas â€” reproduzido ao vivo
contra `dev` real, autorizado por Marcelo). G5 (opt-in de WhatsApp) fechado: backend jÃ¡ existia
(D-286), faltava sÃ³ a UI (D-326). NotificaÃ§Ãµes: E-mail vira `Switch` visual, "Idioma dos
lembretes" removido (D-327) â€” toggle REAL de canal (E-mail editÃ¡vel/WhatsApp opt-out) adiado por
Marcelo, ver item 25 abaixo. Tudo pushado em `develop`, CI+CD verdes. **PrÃ³xima aÃ§Ã£o real**:
aguardar Marcelo decidir o item 25, e/ou enviar o prÃ³ximo protÃ³tipo (padrÃ£o: ler HTML de
referÃªncia, replicar com componentes reais do design system v2 â€” nunca HTML bruto â€”, checklist
completo + screenshot real antes do commit).

**ContinuaÃ§Ã£o 2026-09-23 (sessÃ£o anterior, commitado em `d058becf`)**: achado real corrigido â€”
`InviteForm` (Membros) oferecia "Owner" no seletor de convite pra qualquer ADMIN, sem o mesmo
filtro que a troca de papel jÃ¡ tinha (`create-invitation.ts`'s `OwnerTierChangeRequiresOwnerError`)
â€” corrigido + 2 testes novos. Layout em 2 colunas aplicado a NotificaÃ§Ãµes, Membros e RelatÃ³rios
(protÃ³tipos enviados por Marcelo). Nome completo passou a obrigatÃ³rio no cadastro (`SignUp.tsx`) â€”
seta o atributo Cognito `name`, que jÃ¡ flui para `GlobalUser.displayName` no primeiro login via
`AwsJwtIdTokenVerifier`, sem nenhuma outra mudanÃ§a de backend. Texto de relatÃ³rio "Vencimentos a
vencer" â†’ "Vencimentos prÃ³ximos" (redundÃ¢ncia apontada por Marcelo).

**Nova sessÃ£o 2026-09-23 (mesmo dia)**: item 26 (confirmaÃ§Ã£o de posse do nÃºmero de WhatsApp)
implementado por completo, ver essa entrada na lista de pendÃªncias acima para o detalhe tÃ©cnico â€”
`PENDING_PROTOCOL_REVIEW`, ainda nÃ£o commitado nesta sessÃ£o (revisar `git status`/`git diff` antes
de commitar). **PrÃ³xima aÃ§Ã£o real**: commitar/pushar este trabalho, depois aguardar Marcelo decidir
o item 25 e/ou enviar o prÃ³ximo protÃ³tipo, e/ou revisÃ£o adversarial Codex do item 26 quando o
protocolo Claudeâ†”Codex voltar.

**Regra permanente (2026-09-14)**: `terraform apply` NUNCA roda localmente â€” sÃ³ via pipeline de CD. `plan`/`validate`/`fmt`/`test` locais continuam liberados.

**LiÃ§Ã£o de processo (reforÃ§ada 2026-09-22, D-322)**: antes de abrir PR `develop`â†’`main`, SEMPRE rodar a suÃ­te e2e completa sem filtro (`npx playwright test --project=chromium`) e conferir o CI real do prÃ³prio PR (nÃ£o sÃ³ de pushes individuais para `develop`) â€” specs desatualizadas por uma mudanÃ§a de comportamento anterior (ex. D-321) podem ficar vermelhas por mÃºltiplos commits sem ninguÃ©m perceber se cada sessÃ£o sÃ³ roda specs especÃ­ficas.

**LiÃ§Ã£o de processo**: default Ã© fork serial (nÃ£o orquestraÃ§Ã£o paralela via Workflow) â€” mais barato em token, evita o "imposto" de recontextualizaÃ§Ã£o de agente fresco. Paralelizar sÃ³ se Marcelo pedir velocidade explicitamente.

## ManutenÃ§Ã£o de CI/testes (2026-09-21, achados reais desta sessÃ£o, ver D-311/D-312)

CI de `develop` ficou vermelho por toda a Fase 2 de identidade visual v2 (desde `686d944`) sem
ninguÃ©m perceber â€” bug de `vite.config.ts` (HTTPS vazando pro `vite preview` do Playwright)
travava o job `frontend` inteiro antes de rodar qualquer teste real, mascarando de quebra uma
regressÃ£o real de contraste WCAG em ~15 telas. Ambos corrigidos e verificados (commit
`1ee7dcb`, D-311) â€” checar se o run de CI deste commit passou antes de confiar em qualquer run
anterior a ele como sinal de saÃºde do frontend. Separadamente, backend: `vitest.config.ts` forÃ§ava
TODOS os 274 arquivos de teste a rodar em sÃ©rie por causa de sÃ³ 7 (`test/architecture/**`)
precisarem disso â€” dividido em `vitest.workspace.ts` (D-312), sem perder a serializaÃ§Ã£o onde Ã©
realmente necessÃ¡ria.

## Programa de Performance (2026-09-14, iniciativa prÃ³pria de Marcelo â€” foco real da sessÃ£o)

Fonte: `expiration-tracker-plano-acao-performance-world-class-2026-09-14.md` (raiz do repo, do
Marcelo â€” nunca mover/commitar sem pedir). Rastreamento vivo: `docs/engineering/performance/TODO.md`.
Ciclos A/B/C, PERF-13/14 completos e implantados (2026-09-18). PERF-11/11-b: teto real Ã© o rate
limiter da prÃ³pria app (100 req/60s/tenant), nÃ£o infra AWS.

**PERF-12 (pipeline de lembretes)**: D-299 a D-304 (plano de controle dedicado por evento â€”
`DueWorkTable` autoritativa sem stream, outbox/stream/relay exclusivos para dispatch de reminders,
sub-shard da chave do outbox) implementados e implantados em `dev`. Narrativa completa de cada
decisÃ£o e dos incidentes reais pÃ³s-deploy (bug de checkpoint, gap de IAM em `PutItem` dentro de
`TransactWriteItems`, `reconcileDst` cancelando ocorrÃªncias atrasadas, sweeper travando por
partiÃ§Ã£o compartilhada no GSI6) jÃ¡ estÃ¡ em `decisions-log.md` (D-299 a D-304) e
`docs/engineering/performance/TODO.md` â€” nÃ£o recontar aqui. **Estado atual**: degrau de 10k padrÃ£o
revalidado 2026-09-20 (10.000/10.000, p100=230,26s, SLO 300s). D-304 continua
`PENDING_PROTOCOL_REVIEW` (protocolo suspenso). **Degrau de 100k â€” ENCERRADO 2026-09-21 (D-310):**
seed 100% completo, bug real de HARNESS achado e corrigido (buffer fixo de materializaÃ§Ã£o nÃ£o
escalava com o cohort), mas a prÃ³pria recuperaÃ§Ã£o LOCAL nunca terminou (`materialize` ficou 57min
fazendo polling sem produzir `cohort.json`, processo encerrado manualmente, lock removido â€” este
run nunca teve artefato oficial `cohort.json`/`result.json`). **O resultado real foi confirmado de
forma independente, direto na AWS** (DynamoDB `ConsistentRead` + CloudWatch, 2 investigaÃ§Ãµes
separadas): 100% das 100k ocorrÃªncias chegaram a `TRIGGERED`, zero erro/throttle, mas o disparo
real levou ~16-17min â€” **SLO de 300s NÃƒO atingido nesta escala**. NÃ£o Ã© bloqueador (sem usuÃ¡rio
real, `AGENTS.md` Â§1); mitigaÃ§Ã£o jÃ¡ existe via `defaultReminderLocalTime` (sorteio de horÃ¡rio).
DecisÃ£o de Marcelo: nÃ£o perseguir 500k nem otimizar `dispatch-outbox-relay-processor.ts` agora.

**Backlog registrado, nÃ£o implementado**: `dispatch-outbox-relay-processor.ts` processa lotes de
stream sequencialmente (~14 registros/s, `map-with-concurrency.ts` existe mas nÃ£o Ã© usado aqui) â€”
risco real se 100k/500k reproduzirem o gargalo do D-304. DecisÃ£o de Marcelo: nÃ£o implementar agora,
esperar o protocolo Claudeâ†”Codex voltar antes de mexer no caminho crÃ­tico de dispatch.

## Mandato autÃ´nomo da escada de performance â€” ENCERRADO (Marcelo, 2026-09-19; status 2026-09-21)

Escada 10kâ†’100kâ†’500k **ENCERRADA, nÃ£o retomar sem novo pedido explÃ­cito de Marcelo** â€” resultado
real (D-310) jÃ¡ registrado na seÃ§Ã£o "Programa de Performance" acima. Regras operacionais completas
para uma eventual retomada (cohort de tenants padrÃ£o vs. e-mail real, teto de
`PERF_REMINDER_BURST_SIZE`, cota SES, protocolo de re-run por degrau reprovado) ficam preservadas
em `docs/engineering/performance/TODO.md` e `decisions-log.md` (D-299 a D-310) â€” nÃ£o recontadas
aqui.

**Protocolo Claudeâ†”Codex SUSPENSO atÃ© novo aviso (Marcelo, 2026-09-19)** â€” Codex bloqueado atÃ©
2026-09-23, Antigravity sem cota atÃ© ~2026-09-26. Enquanto isso, decisÃµes de nÃ­vel 5-6 tomadas sem
o protocolo formal devem ser marcadas `PENDING_PROTOCOL_REVIEW` (nunca `APPROVED_BY_OWNER`) e
listadas aqui para retomar quando Codex/Antigravity voltarem.

**Checklist de conclusÃ£o de tarefa (2026-09-18)**: `docs/engineering/task-completion-checklist.md`
+ skill `.claude/skills/task-checklist/` â€” uso obrigatÃ³rio ao fim de toda tarefa, `AGENTS.md` Â§1.

**Achados incidentais registrados, nÃ£o bloqueantes**: gate `Authenticated k6 smoke` com flakiness
prÃ©-existente de cold start (`performance/TODO.md` Â§PERF-14); ADOT descarta lotes de trace sob
carga sustentada sem afetar requisiÃ§Ãµes reais (correÃ§Ã£o exigiria `collector.yaml` customizado em
~69 Lambdas, registrado como dÃ©bito tÃ©cnico); consolidaÃ§Ã£o de 23 PRs Dependabot do Terraform em
andamento.

## Itens de 2026-09-20 ainda nÃ£o decididos/iniciados (ordem de Marcelo, itens 1-2 jÃ¡ resolvidos acima)

- ~~HorÃ¡rio padrÃ£o de lembretes~~ â€” **CORREÃ‡ÃƒO 2026-09-21**: a linha anterior deste arquivo dizia
  "nÃ£o decidida nem iniciada" por engano (escrita por uma sessÃ£o sem contexto da implementaÃ§Ã£o jÃ¡
  feita ANTES da compactaÃ§Ã£o que a originou). JÃ¡ estÃ¡ 100% implementada e mergeada:
  `create-organization.ts`'s `pickReminderLocalTime()` sorteia um dos 15 horÃ¡rios BRT (10:00-17:00,
  meia/hora cheia) em `Organization.defaultReminderLocalTime` no onboarding; ajustÃ¡vel depois pelo
  Owner em ConfiguraÃ§Ãµes; `ItemReminderPolicy.tsx` jÃ¡ propÃµe esse valor como default de gatilhos
  NOVOS (nÃ£o retroativo). As 3 decisÃµes que estavam em aberto jÃ¡ foram resolvidas pela prÃ³pria
  implementaÃ§Ã£o (nÃ­vel=tenant, escopo=sÃ³ novos, local=ConfiguraÃ§Ãµes). Ãšnico resto: revisÃ£o
  adversarial Codex voluntÃ¡ria ainda pendente (item 11 da lista de pendÃªncias acima).
- **Subagentes de aprovaÃ§Ã£o por domÃ­nio** (Marcelo): pesquisa/planejamento (nÃ£o implementar ainda)
  de subagentes acionados ao fim de toda tarefa, um por eixo de `joint-review-criteria.md`. NÃ£o
  iniciado nesta sessÃ£o.

## Status de evidÃªncia (nÃ£o presumir E2E sem checar)

A maioria dos mecanismos do roadmap P0/backlog P1 estÃ¡ `IMPLEMENTED`/`UNIT TESTED` e confirmada `Active` contra `dev` via `aws --profile claude-dev`, mas **nem todo mecanismo tem prova E2E de ponta a ponta disparando pelo gatilho real** (cron/SQS real, nÃ£o sÃ³ G-V3/unit) â€” isso Ã© nomeado individualmente nas linhas do roadmap acima onde relevante ("nunca testado ponta a ponta com... real"). NÃ£o assumir E2E PROVEN sem checar a linha especÃ­fica do item ou `decisions-log.md`.

## Links para histÃ³rico (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)

- `docs/architecture/session-log.md` â€” linha do tempo compacta, uma entrada por sessÃ£o.
- `docs/architecture/decisions-log.md` / `docs/engineering/decisions-log.md` â€” toda decisÃ£o com nota Claude/Codex e status (D-0xx / E-0xx).
- `docs/architecture/reviews/` â€” artefatos de cada rodada Claudeâ†”Codex por tema.
- `docs/project/handoffs/` â€” prompts de handoff de sessÃµes anteriores, superseded por este arquivo.
- `docs/frontend/` â€” planejamento de interface + `interface-quality-standard.md` + `prototype/`.
- `docs/architecture/multi-user-b2b-wave-tracker.md` â€” detalhe fatia-a-fatia do Multi-User B2B (15 waves, D-084 a D-120).
# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-29 (reconciliaÃ§Ã£o de engenharia de contexto â€” este bloco reescrito para corrigir drift real: dizia "auto-CONFIRMED nÃ£o propaga dueDate, aguardando decisÃ£o do Marcelo" e "8 de 9 classes de retenÃ§Ã£o sem purga" quando D-058/D-061 jÃ¡ tinham decidido e implementado ambos em sessÃµes anteriores). Consolidation + Pilot Readiness Program concluÃ­do, recomendaÃ§Ã£o **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`, backlog item-a-item em `pilot-readiness-program.md`): Wave 3 (tenant isolation/LGPD) `DONE` de ponta a ponta, zero vulnerabilidade cross-tenant real; `USER_DOCUMENT` e `EXTRACTION_TRANSIENT` tÃªm purga fÃ­sica real (D-061), as outras 7 de 9 classes de retenÃ§Ã£o LGPD nÃ£o tÃªm, decisÃ£o de escopo pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership; Wave 5 fechou GTR-01. M6/M7/M9/M10/M11 deployados em `main`/`dev`, M7 **E2E PROVEN** (verificaÃ§Ã£o real 2026-08-27, achados registrados em `decisions-log.md` D-057/D-058); M12 bloqueado por decisÃ£o de fornecedor (D-052). Full BFF + Frontend Production Foundation (D-053/D-054) **implementado e `APPROVED`**; planejamento de interface 8/9 etapas `APPROVED`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); BLOCKER-A/B/C **todos resolvidos**. W3-07 (exclusÃ£o fÃ­sica de tenant/DSR) tem design **`APPROVED WITH CONDITIONS`** (D-066/D-067, condiÃ§Ã£o de polÃ­tica SES jÃ¡ decidida) e implementaÃ§Ã£o incremental em andamento (fencing de writers D-068 a D-080, purge pipeline D-081 a D-083 com nota Codex 9,1/10, emenda de privacidade B2B-9/D-103/D-104); orquestrador real ganhou design `APROVADO` depois (D-121, ver mais adiante neste mesmo bloco) â€” nÃ£o deixar a frase acima ler como "ainda nÃ£o decidido" sem essa referÃªncia, achado de autocontradiÃ§Ã£o de `full-audit-round2-contexto` (2026-09-07). **Multi-User B2B (D-084 a D-108, 2026-08-30)**: design tÃ©cnico `APPROVED` via protocolo (Claude 9,2/Codex 9,2) e timing decidido diretamente pelo Marcelo (proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05`) â€” maior iniciativa de arquitetura autorizada no momento; **Waves B2B-0 a B2B-11 `DONE`** (inventÃ¡rio, physical model D-086, identidade global D-087/D-088, Organization+Membership+CreateOrganizationService D-089/D-090/D-091, `OnboardingStateResolver` puro D-092/D-094, `RequestContext Cutover` D-095/D-096 â€” login nÃ£o cria mais tenant sozinho, `POST /bff/organizations` fecha o loop de onboarding, cap transacional; `RBAC` D-097/D-098 â€” `Membership.role=ADMIN` real na matriz de autorizaÃ§Ã£o, primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014; `Invitations/Team` D-099/D-100 â€” `Invitation`/aceite/revogaÃ§Ã£o/gerÃªncia de membros com last-owner protection, segunda aplicaÃ§Ã£o de E-014, primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o; `BFF Organization Context` D-101/D-102 â€” `X-Organization-Id` transportado do BFF ao recurso, terceira aplicaÃ§Ã£o de E-014, fecha o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; `W3-07/Privacy Reconciliation` D-103/D-104 â€” quarta aplicaÃ§Ã£o de E-014, fecha o `InvitationTokenPointer` Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization, formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md`; `Tenant-aware Frontend` D-105/D-106 â€” primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend, corrige a regressÃ£o real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; `Responsibility + Notifications` D-107/D-108 â€” quinta aplicaÃ§Ã£o de E-014, migra `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch` de validaÃ§Ã£o vestigial para `Membership`+`GlobalUser` reais; NÃƒO inclui a supersessÃ£o de GTR-01, ver `multi-user-b2b-wave-tracker.md`; `Cutover de dev` D-110/D-111 â€” sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`), `scripts/reset-dev-data.ts` (reset/reseed, nÃ£o migraÃ§Ã£o, dado 100% sintÃ©tico confirmado por inventÃ¡rio real via `aws --profile claude-dev`) + remoÃ§Ã£o de `LEGACY_TENANT_ONLY`, Fase A verificada de fato contra `dev`; execuÃ§Ã£o real destrutiva (`--confirm`) pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo; `E2E/Adversarial Security` D-112/D-113 â€” sÃ©tima aplicaÃ§Ã£o de E-014, auditoria contra as 25 perguntas de `roadmap-evolution/17` Â§121 (20/25 jÃ¡ cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assÃ­ncrona de notificaÃ§Ã£o â€” `notification.ts`+`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` jÃ¡ testado, 2 ganham teste novo de revogaÃ§Ã£o/isolamento de role, 1 Ã© auditoria de fixture IDs sem achado). **Multi-User B2B â€” todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** **B2B-14 (Operational Evidence) `EM ANDAMENTO`, 2026-08-30**: 7 achados reais severos, cada um encontrado sÃ³ por exercitar o fluxo de verdade contra `dev` pela primeira vez â€” `app_origin` placeholder nunca trocado (D-114), `bff-handler` crashava em toda invocaÃ§Ã£o (D-115), GSI4 nunca autorizado aos 10 Lambdas reais que o consomem (D-116), 2 rotas do BFF nunca wireadas no API Gateway (D-117), nenhuma tela de onboarding existia (D-118), `ExpressionAttributeNames: {}` quebrava `TransactWriteItems` real em 3 arquivos incluindo o guard de Ãºltima-proteÃ§Ã£o-de-OWNER (D-119), e o convite/aceite de membro nunca foi alcanÃ§Ã¡vel de ponta a ponta â€” tela nunca existiu, rota nunca foi wireada, e-mail real nunca foi ativado, SES sandbox sem NENHUMA identity verificada no ambiente inteiro (D-120, 2 endereÃ§os reais verificados com o Marcelo). Pendente para fechar: Marcelo testar convite/aceite/troca-de-organizaÃ§Ã£o com uma segunda conta â€” **explicitamente adiado para uma sessÃ£o futura**, nÃ£o bloqueia o resto do trabalho. Em paralelo, **W3-07 â€” orquestrador de purga ganhou design `APROVADO` (D-121, protocolo Claudeâ†”Codex 3 rodadas, 9,1/9,2): Step Functions + EventBridge Scheduler, e a implementaÃ§Ã£o real estÃ¡ `IMPLEMENTED`/`DEPLOYED`** (D-124, PR #122, CD `success` 2026-08-31, state machine + sweeper `ENABLED` confirmados ao vivo contra `dev` â€” corrigido em 2026-09-07, esta frase ainda dizia "implementaÃ§Ã£o real ainda nÃ£o construÃ­da", achado de `full-audit-round2-contexto`); e 2 lacunas de produto levantadas pelo Marcelo (exportaÃ§Ã£o de dados CSV/planilha, reatribuiÃ§Ã£o de responsabilidade ao remover um Membership) tiveram rodadas de scoping dispatchadas via o mesmo protocolo â€” ver `decisions-log.md` para o D-number final de cada uma antes de assumir status. **`docs/engineering/definition-of-done.md` (E-012, emenda E-013)**: gate de processo permanente â€” nenhum item de todo list que produza cÃ³digo Ã© `completed` sem passar pelo gate correspondente ao seu nÃ­vel de risco, incluindo aplicaÃ§Ã£o concreta (nÃ£o sÃ³ referencial) de `test-engineering-standard.md`. Ver `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o exata â€” este bloco Ã© sÃ³ o resumo de fase, nÃ£o histÃ³rico item-a-item.
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `correlationid-xray-trace-join.md` | normativo atual (decisÃ£o + implementaÃ§Ã£o) | JunÃ§Ã£o `correlationId`â†”trace ADOT/X-Ray (E-011 pendÃªncia 1) â€” `APPROVED` via protocolo Claudeâ†”Codex (Claude 9,3/Codex 9,4), amplia D-022 sem reabri-la; implementado 2026-08-29 (`xray-trace-header.ts`, correÃ§Ã£o de precedÃªncia do `SecureLogger`), status `E2E PROVEN` (smoke test real contra `dev` confirmado 2026-08-29 â€” corrigido em 2026-09-07, esta linha ainda dizia "pendente", achado de `full-audit-round2-contexto`) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 em diante (D-231+ na revisÃ£o mais recente â€” nÃ£o referenciar um teto fixo aqui, ele desatualiza a cada sessÃ£o; a numeraÃ§Ã£o tambÃ©m nÃ£o Ã© sequencial na ordem das linhas â€” ex. D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/00-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt original (2026-08-22) que abriu as 3 fases de evoluÃ§Ã£o estratÃ©gica do roadmap abaixo; movido da raiz em 2026-08-29, mesmo tratamento de `blocker-b-mission-brief.md` |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `roadmap-evolution/16-document-lifecycle-strategic-analysis.md` | informativo (anÃ¡lise externa, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR) | EvoluÃ§Ã£o estratÃ©gica de "controle de vencimentos" para "gestÃ£o enxuta do ciclo de vida documental" â€” avaliaÃ§Ã£o de coerÃªncia/reaproveitamento/risco de overengineering; recomenda aprovar a direÃ§Ã£o mas nÃ£o antecipar o escopo, mantendo o foco em Consolidation + Pilot Readiness; identifica `Document` deixar de ser filho exclusivo de `ExpirationItem` como a mudanÃ§a arquitetural futura central, marcada nela mesma como Type 1 (ADR + protocolo Claudeâ†”Codex antes de qualquer implementaÃ§Ã£o) |
| `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` | **`APPROVED` â€” design tÃ©cnico (protocolo AGENTS.md Â§4) + timing (decisÃ£o direta do Marcelo, D-085)** | EspecificaÃ§Ã£o vigente de Multi-User B2B â€” `Organization` como tenant boundary permanente, `User` global, `Membership` N:N, RBAC por permissions. Protocolo Claudeâ†”Codex completo (Â§125): 3 rodadas reais, Claude 9,2/Codex 9,2, ambos â‰¥9,0 (mecanismo transacional de last-OWNER, unicidade de Membership/Invitation, tabela mantÃ©m/emenda/refaz do impacto no W3-07, supersessÃ£o explÃ­cita de D-060/GTR-01). **D-085 (2026-08-29)**: Marcelo decidiu diretamente proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05` (marcado `SUPERSEDED (timing)`, modelo de billing/entitlements desse cluster permanece vigente). **Waves B2B-0 a B2B-11 `DONE`** (D-108 conclui B2B-11, Responsibility + Notifications) â€” ver `multi-user-b2b-wave-tracker.md` para o detalhamento por subitem (inclui a nota de escopo sobre GTR-01/`Organization.displayName` ainda pendente). Wave B2B-12 (Cutover de dev) foi, na Ã©poca da escrita original desta linha, a prÃ³xima aÃ§Ã£o real do documento fonte â€” **hoje jÃ¡ `APPROVED`/`IMPLEMENTADO` (D-110/D-111, ver linha da tabela abaixo)**, corrigido em 2026-09-07 (achado de `full-audit-round2-contexto`: esta linha descrevia um estado histÃ³rico do documento-fonte como se fosse o estado atual) â€” ver `NEXT_SESSION_PROMPT.md` para o estado de execuÃ§Ã£o vigente |
| `roadmap-evolution/18-synthetic-persona-research-quality-baseline.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Fundamento metodolÃ³gico proposto para um futuro "Synthetic Persona Evaluation Framework" (agentes condicionados por persona exercitando a aplicaÃ§Ã£o real, em complemento â€” nunca substituiÃ§Ã£o â€” a testes humanos): modelo Persona+Contexto+Dataset+CenÃ¡rio+Objetivo="Trial", provenance de evidÃªncia (`hypothesisâ†’research-groundedâ†’human-observedâ†’production-observed`), hierarquia de oracle (estado determinÃ­stico > banco/API/evento > LLM grader > autoavaliaÃ§Ã£o do agente, esta Ãºltima nunca fonte de verdade), scorecard de 100 pontos + 18 Hard Gates (HG-01..18, ex. "agente nunca recebe oracle/caminho de navegaÃ§Ã£o", "actor nÃ£o pode ser o Ãºnico judge"). Puramente conceitual/genÃ©rico nesta etapa, ainda nÃ£o aplicado ao Expiration Tracker |
| `roadmap-evolution/19-synthetic-persona-application-analysis.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Aplica o baseline de `18` Ã  realidade atual do cÃ³digo: conclui que a aplicaÃ§Ã£o Ã© "muito favorÃ¡vel" a personas sintÃ©ticas (domÃ­nio modelado, OCC, idempotÃªncia, outbox, relÃ³gio injetÃ¡vel, `dev` resetÃ¡vel, Playwright, Test Engineering Standard jÃ¡ existentes), mas nomeia 4 restriÃ§Ãµes reais â€” zero usuÃ¡rio/produÃ§Ã£o real, frontend cobre sÃ³ parte das superfÃ­cies planejadas, modelo B2B ainda em transiÃ§Ã£o, e a principal: **o Playwright atual roda majoritariamente contra um BFF mockado (`page.route()`), nÃ£o contra o sistema real** â€” a mesma lacuna que B2B-14 (Operational Evidence) jÃ¡ vinha fechando manualmente. TambÃ©m documenta drift real entre `NEXT_SESSION_PROMPT.md`/comentÃ¡rios antigos e o cÃ³digo atual |
| `roadmap-evolution/20-synthetic-trial-identity-design.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Proposta de implementaÃ§Ã£o: identidade de execuÃ§Ã£o hierÃ¡rquica `evaluationRunIdâ†’trialIdâ†’correlationIdâ†’traceId/spanId`, deliberadamente **execution metadata, nunca atributo funcional do domÃ­nio** (nunca `ExpirationItem.trialId`), propagada via header Playwrightâ†’middleware BFFâ†’`RequestContext`â†’outbox `metadata` (nunca `payload`)â†’SQSâ†’workerâ†’audit event/log estruturado; gate de ambiente (`EVALUATION_CONTEXT_ENABLED`, `prod=false` por padrÃ£o), fail-closed para metadata invÃ¡lida, 12 Hard Gates prÃ³prios (HG-TID-01..12, ex. "trialId nunca altera autorizaÃ§Ã£o/validaÃ§Ã£o funcional"). O prÃ³prio documento recomenda revisÃ£o via protocolo Claudeâ†”Codex antes de entrar no roadmap â€” ainda nÃ£o submetida |
| `multi-user-b2b-wave-b2b4-scope.md` | **`APPROVED` (D-092, protocolo Claudeâ†”Codex 4 rodadas, Claude 9,3/Codex 9,4)** | Escopo final de Wave B2B-4 â€” `OnboardingStateResolver` puro (4 estados, procedimento sequencial estrito), sem wiring de login, sem exposiÃ§Ã£o HTTP; redesigna "remover auto-provision"/gate real para Wave B2B-5. EvidÃªncia das 4 rodadas em `reviews/multi-user-b2b-wave-b2b4-scoping/` |
| `multi-user-b2b-wave-b2b5-scope.md` | **`APPROVED` (D-095, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,1) â€” `IMPLEMENTADO` (D-096)** | Escopo final de Wave B2B-5 (RequestContext Cutover) â€” `bootstrapUser()` de 2 itens, `RequestContextResolver` reescrito com resoluÃ§Ã£o de Membership Ãºnica e assert explÃ­cito para `ADMIN` nÃ£o suportado, `POST /bff/organizations` com cap transacional (`GlobalUser.hasCreatedOrganization`), self-heal de sessÃ£o BFF; desvio faseado explÃ­cito de Â§11/Â§12 do physical model (seleÃ§Ã£o real de organizaÃ§Ã£o fica para B2B-6). EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b5-scoping/`; implementaÃ§Ã£o e 2 achados reais corrigidos (UserProfile mantido, gate de lifecycle da Organization adicionado) em `decisions-log.md` D-096 |
| `multi-user-b2b-wave-b2b6-scope.md` | **`APPROVED` (D-101, protocolo Claudeâ†”Codex 3 rodadas, checklist v2, Claude 9,1/Codex 9,2) â€” `IMPLEMENTADO` (D-102)** | Escopo final de Wave B2B-6 (BFF Organization Context) â€” terceira aplicaÃ§Ã£o real de E-014 (padrÃ£o header+revalidaÃ§Ã£o server-side, OWASP Multi Tenant Security). `X-Organization-Id` injetado sÃ³ pelo BFF (nunca do browser), `resolveWorkingOrganization()`/`OrganizationUnavailableError`(403)/`OrganizationSelectionRequiredError`(409) fecham o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; `POST /bff/organization/select` (CSRF)/`GET /bff/organizations` (filtrado por lifecycle) novos. EvidÃªncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b6-scoping/` |
| `multi-user-b2b-wave-b2b7-scope.md` | **`APPROVED` (D-097, protocolo Claudeâ†”Codex 3 rodadas, checklist v2, Claude 9,2/Codex 9,3) â€” `IMPLEMENTADO` (D-098)** | Escopo final de Wave B2B-7 (RBAC) â€” primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014 (pesquisa GitHub/Linear/Slack/Notion/NIST-ANSI-INCITS-359, checklist de critÃ©rios de nota contestado e reconciliado na Rodada 1â†’2). `Role` ganha `ADMIN`, paritÃ¡rio com `OWNER` nas 4 actions de deleÃ§Ã£o de recurso e no bypass de ownership; `notification:configure` reclassificada para `READ_ONLY_ROLES` (bug fix real, nÃ£o escolha ADMIN-vs-OWNER); `tenant:configure-document-request-delivery` isolada em novo tier `OWNER_ROLES`. EvidÃªncia das 3 rodadas (incl. a contestaÃ§Ã£o do checklist) em `reviews/multi-user-b2b-wave-b2b7-scoping/` |
| `multi-user-b2b-wave-b2b8-scope.md` | **`APPROVED` (D-099, protocolo Claudeâ†”Codex 3 rodadas, checklist v3, Claude 9,1/Codex 9,2) â€” `IMPLEMENTADO` (D-100)** | Escopo final de Wave B2B-8 (Invitations/Team) â€” segunda aplicaÃ§Ã£o real de E-014, declaraÃ§Ã£o `SIM PARCIAL` (mecanismo de dados jÃ¡ `APPROVED` em D-086; last-owner protection/hierarquia de gerÃªncia de membros pesquisados nesta wave, GitHub/Slack/Linear/Notion). `Invitation`+token pointer+dedup (D-086 Â§7), `AcceptInvitationService` (transaÃ§Ã£o de 6 itens, token consumido estruturalmente), `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService` com builder Ãºnico de `ownerCount` (`LastOwnerError`) â€” primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o. EvidÃªncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b8-scoping/` |
| `multi-user-b2b-wave-b2b9-scope.md` | **`APPROVED` (D-103, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,3/Codex 9,3) â€” `IMPLEMENTADO` (D-104)** | Escopo final de Wave B2B-9 (W3-07/Privacy Reconciliation) â€” quarta aplicaÃ§Ã£o real de E-014 (GitHub/Slack/Atlassian, "delete conta" vs. "delete organizaÃ§Ã£o"). Fix real: `InvitationTokenPointer` (declara `organizationId`, nÃ£o `tenantId`) ganha 3Âª clÃ¡usula OR no scan/`PURGE_DELETE` de purga, fechando Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization. `privacy-lgpd.md` Â§4.1/Â§4.2 novos (fronteira User-level vs. Organization-level erasure, invariante de Ãºltimo OWNER documentada) + 4 linhas de retenÃ§Ã£o. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b9-scoping/` |
| `multi-user-b2b-wave-b2b10-scope.md` | **`APPROVED` (D-105, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2) â€” `IMPLEMENTADO` (D-106)** | Escopo final de Wave B2B-10 (Tenant-aware Frontend) â€” primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend nesta sessÃ£o. Fix de regressÃ£o real (`AuthContext` tratava todo usuÃ¡rio autenticado como deslogado desde B2B-5); arquitetura de cache isolation (query keys por `organizationId` + `ActiveOrganizationProvider`/`switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta) fecha uma corrida real de vazamento de dado entre tenants na UI; switcher/members/settings novos. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b10-scoping/` |
| `multi-user-b2b-wave-b2b11-scope.md` | **`APPROVED` (D-107, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2) â€” `IMPLEMENTADO` (D-108)** | Escopo final de Wave B2B-11 (Responsibility + Notifications) â€” quinta aplicaÃ§Ã£o de E-014 (GitHub/Linear, "assignee deve ser membro real e ativo"). `NotificationRecipientResolver` migrado de `UserProfile` (vestigial) para `Membership`+`GlobalUser` (2 condiÃ§Ãµes); `assigneeUserId`/`ItemWatch.userId` agora validados contra `Membership` real via `MemberEligibilityChecker` novo. **Nota**: nÃ£o inclui a supersessÃ£o de GTR-01 (`UserProfile.requesterDisplayName`â†’`Organization.displayName`), prevista em B2B-5 mas nunca debatida nas 3 rodadas reais desta wave â€” ver `multi-user-b2b-wave-tracker.md`. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b11-scoping/` |
| `multi-user-b2b-wave-b2b12-scope.md` | **`APPROVED` (D-110, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,1) â€” `IMPLEMENTADO` (D-111)** | Escopo final de Wave B2B-12 (Cutover de dev) â€” sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`, polÃ­tica reset-vs-migraÃ§Ã£o jÃ¡ aprovada dentro do prÃ³prio `roadmap-evolution/17` Â§62-63). InventÃ¡rio real via `aws --profile claude-dev` confirma `dev` 100% sintÃ©tico/descartÃ¡vel; `scripts/reset-dev-data.ts` (Fase A inventÃ¡rio+snapshot sempre, Fase B delete real sÃ³ `--confirm`, allowlist de tabela/conta, verificaÃ§Ã£o final fail-loud) + remoÃ§Ã£o de `LEGACY_TENANT_ONLY` (backend+frontend+teste). Fase A verificada de fato contra `dev` (nÃ£o sÃ³ fakes). **ExecuÃ§Ã£o real destrutiva (`--confirm`/`--include-cognito`) pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo.** EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b12-scoping/` |
| `multi-user-b2b-wave-b2b13-scope.md` | **`APPROVED` (D-112, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,2, rÃ©gua E-014 9,3) â€” `IMPLEMENTADO` (D-113)** | Escopo final de Wave B2B-13 (E2E/Adversarial Security) â€” sÃ©tima aplicaÃ§Ã£o de E-014 (OWASP WSTG/Authorization Testing, SIM PARCIAL). Auditoria completa contra as 25 perguntas de `roadmap-evolution/17` Â§121 (matriz Qâ†’arquivo:linha completa): 20/25 jÃ¡ cobertas por testes reais de waves anteriores; 2 fecham com fix real (mesmo TOCTOU em `notification.ts`+`subject.ts` â€” entrega assÃ­ncrona nunca revalidava `Membership`, sÃ³ roteamento checava â€” corrigido reaproveitando `DynamoDbNotificationRecipientResolver` jÃ¡ testado, `ResolvedRecipient` ganha `email?: string`); 2 ganham teste novo (revogaÃ§Ã£o de Membership encadeada ponta-a-ponta; roles diferentes por Organization nunca vazam); 1 Ã© auditoria de fixture IDs (zero achado). A Rodada 1 do Codex achou um 6Âº achado real (mesmo TOCTOU em `document-chasing-dispatch`) que a proposta original nÃ£o tinha visto. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b13-scoping/` |
| `multi-user-b2b-wave-b2b0-inventory.md` | evidÃªncia de rodada (read-only, completo) | InventÃ¡rio verificado de Wave B2B-0 (Â§105 do documento acima): `tenantId=userId` em produÃ§Ã£o (3 pontos confirmados, 1 gap de fencing prÃ©-existente achado no login BFF), `IdentityMapping`/BFF session/`RequestContext`, stores tenant-scoped/S3 (3 prefixos reais, diverge de Â§68)/eventos/W3-07, frontend caches (zero isolamento tenant hoje) e contagem reproduzÃ­vel da suÃ­te de testes (122 arquivos/1092 casos, nÃ£o ~1104) |
| `multi-user-b2b-physical-model.md` | **`APPROVED` (D-086, protocolo Claudeâ†”Codex 5 rodadas, Claude 9,3/Codex 9,5)** | Physical model final de Wave B2B-1 â€” `User`/`IdentityMapping`/`Organization`/`Membership` (3 estados, GSI4 `MembershipByUser` reaproveitado)/`Invitation`/`ownerCount`/`bootstrapUser()`/`DeviceSession`+`logoutAll`/`RequestContext`/BFF session, respostas finais Ã s 25 perguntas de `roadmap-evolution/17` Â§121. EvidÃªncia das 5 rodadas em `reviews/multi-user-b2b-physical-model/` |
| `multi-user-b2b-wave-tracker.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Status DONE/IN PROGRESS/BLOCKED/NOT STARTED por wave (B2B-0 a B2B-15), mesmo papel de `docs/engineering/pilot-readiness-program.md` â€” ponto de entrada para saber o que jÃ¡ foi feito antes de continuar a iniciativa |
| `reviews/context-engineering-reconciliation/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 | ReconciliaÃ§Ã£o da arquitetura de contexto do repo (2026-08-29) â€” root cleanup (21â†’6 `.md`), `AGENTS.md`/`NEXT_SESSION_PROMPT.md` trimados (narrativa de milestone jÃ¡ duplicada em `decisions-log.md` removida), 2 guardrails novos em `check-doc-drift.ts` (root allowlist, size), READMEs stale corrigidos. 4 rodadas Codex (8,1â†’8,8â†’8,9â†’9,3/10, `APPROVED`), 3 achados reais de drift semÃ¢ntico prÃ©-existente corrigidos no processo |
| `reviews/reminder-scan-control-plane/` | decisÃ£o Type 1 direta do owner (D-301, `APPROVED_BY_OWNER`) | EvoluÃ§Ã£o do ReminderScan apÃ³s o benchmark de 10k: plano de controle dedicado (tabela/stream/relay/SQS), sharding versionado, modelo de lease+outbox transacional, reconciliaÃ§Ã£o prÃ³pria, capacidade 10kâ†’1M e rollout/rollback por epoch. O protocolo Claudeâ†”Codex foi explicitamente dispensado pela escolha direta de Marcelo; alternativas e pesquisa oficial estÃ£o registradas na decisÃ£o. |
| `reviews/reminder-scan-control-plane/AMENDMENT-001.md` | emenda Type 1 direta do owner (D-302, `APPROVED_BY_OWNER`) | Substitui GSI3 como fonte autoritativa por `ReminderDueWorkTable` sem stream, escrita atomicamente com occurrences e lida com consistÃªncia forte; fecha claim antecipado no minuto corrente, perda por consistÃªncia eventual, parser fail-open e demais gates da revisÃ£o tÃ©cnica. |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/w3-07-purge-orchestrator-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-121) | Orquestrador real do purge pipeline W3-07 (Step Functions + EventBridge Scheduler) â€” decisÃ£o pendente desde D-083, fechada 3 rodadas (6,7â†’8,2â†’9,2/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o (novo mÃ³dulo Terraform, 2 Lambdas, `CloseOrganizationService`) fica para sessÃ£o dedicada futura |
| `reviews/responsibility-reassignment-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-122) | ReatribuiÃ§Ã£o de responsabilidade quando um Membership Ã© removido/sai â€” precondiÃ§Ã£o bloqueante best-effort via GSI1 jÃ¡ existente (zero GSI novo), 3 rodadas (7,0â†’8,1â†’9,1/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/data-export-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-123) | ExportaÃ§Ã£o de dados (CSV) de `ExpirationItem` â€” `item:export`/`ADMIN_ROLES`, handler sÃ­ncrono via `queryGsi1` com orÃ§amento decrescente + guard de 4 MB, `csv-export-writer.ts` (RFC4180+mitigaÃ§Ã£o de fÃ³rmula), 3 rodadas (6,2â†’8,1â†’9,1/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/search-and-filters-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-194) | Busca e filtros documentais (roadmap P0.5) â€” uniÃ£o discriminada Subject/Requirement/ExpirationItem via GSI7/GSI1 jÃ¡ tenant-facing (zero GSI novo), `Requirement.assigneeUserId` (mecanismo completo transportado de D-122/D-125), `UnifiedValidityState` derivado read-time. RÃ©gua reconciliada na Rodada 2 (9,6/10 estÃ¡vel), 5 rodadas (design 4,8â†’6,2â†’8,1â†’8,8â†’9,2/9,3 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |
# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO em 2026-08-20 (9/9 avaliados, sÃ³ Contexto bateu o gate â‰¥9.0 originalmente) â€” achados remediados/reavaliados em rodadas posteriores por eixo, ver decisions-log.md/pilot-readiness-program.md, nÃ£o re-enumerado aqui
Programas posteriores:    test-engineering-standard.md (E-010, APPROVED), logging-observability-standard.md (E-011, APPROVED), definition-of-done.md (E-012, APPROVED â€” gate por item de todo list; emenda E-013 tornando a aplicaÃ§Ã£o de test-engineering-standard.md operacional, nÃ£o sÃ³ referencial), research-protocol.md (E-014, APPROVED â€” pesquisa externa exigida/checklist de nota antes de decisÃµes Type 1 que dependem de padrÃ£o de mercado), Consolidation + Pilot Readiness Program (pilot-readiness-program.md/pilot-readiness-assessment.md, CONDITIONAL GO)
Last verified:            2026-09-08 (E-023 â€” full-audit round2, eixo Qualidade de Engenharia, reaberto (Round 1 fechou 2026-08-20 em 8,199/7,788); melhora real (Claude 8,199â†’8,32, Codex 7,788â†’8,341), gate â‰¥9,0 ainda nÃ£o atingido, convergido em 2 rodadas por acordo mÃºtuo explÃ­cito do Codex; achado real novo: suÃ­te completa rodou 2624/2625 nesta sessÃ£o por corrida intermitente entre 2 arquivos de teste de arquitetura (`system-mutation-allowlist.test.ts`/`tenant-fence-boundary.test.ts`) cuja mitigaÃ§Ã£o jÃ¡ tentada (fixtures movidas, `fileParallelism: false`) nÃ£o foi suficiente â€” causa raiz nÃ£o identificada, PENDENTE; achado do Round1 sobre logger sem `correlationId` automÃ¡tico confirmado corrigido de fato (`AsyncLocalStorage` real); 3 achados mecÃ¢nicos do Codex corrigidos nesta sessÃ£o (contagem desatualizada em `test-engineering-standard.md`, comentÃ¡rio do dependency-cruiser, CycloneDX sem versÃ£o pinada) â€” ver reviews/full-audit-round2-qualidade-summary.md. E-022 â€” full-audit round2, eixo Engenharia de Contexto, reaberto (Round 1 fechou 2026-08-20 em 9,08/9,09); gate â‰¥9,0 NÃƒO atingido, convergido em 3 rodadas, nota final 6,84/10 â€” achado central: `NEXT_SESSION_PROMPT.md` cumpre o guardrail de linhas mas nÃ£o sua intenÃ§Ã£o (~134 KB), guardrail mede sÃ³ linhas; 5 achados factuais mecÃ¢nicos corrigidos em `docs/architecture/README.md` no processo â€” ver reviews/full-audit-round2-contexto-summary.md. **Achado central CORRIGIDO, D-236, 2026-09-08**: `NEXT_SESSION_PROMPT.md` recompactado (299 linhas/~145.804 bytes â†’ 87 linhas/~12.304 bytes) e `scripts/check-doc-drift.ts` ganhou guardrail de densidade (bytes+palavras, independente de contagem de linhas) â€” ver `docs/engineering/decisions-log.md` E-022 e `docs/architecture/decisions-log.md` D-236; gate â‰¥9,0 nÃ£o reavaliado por rodada nova nesta correÃ§Ã£o. E-021 â€” full-audit round2, eixo Arquitetura, reaberto pelo volume de mudanÃ§a de escopo desde round1 (D-084 a D-231+: Multi-User B2B completo, Document Archive completo, ADR-0009 substitui CDK por Terraform); melhora real (Claude 7,966â†’8,55, Codex 8,743â†’8,782), gate â‰¥9,0 ainda nÃ£o atingido, convergido em 3 rodadas; achado real novo nÃ­vel 5 (nÃ£o corrigido, PENDENTE): `src/workers/guest-credential-delivery/deliver.ts` reivindica o marcador idempotente antes do envio SES â€” falha entre claim e send perde a entrega de credencial guest permanentemente e silenciosamente, sem alarme dedicado, redrive nativo neutralizado pelo prÃ³prio claim â€” ver reviews/full-audit-round2-arquitetura-summary.md. E-020 â€” full-audit round2, eixo OperaÃ§Ãµes/SRE e Continuidade de NegÃ³cio, reaberto por crescimento de escopo (32â†’61 Lambdas, rollback.yml novo); gate â‰¥9,0 nÃ£o atingido (Claude 5,18â†’4,36, Codex 4,36, convergido em 2 rodadas); achado real nÃ­vel 5: rollback.yml hardcoda "exatamente 13 funÃ§Ãµes" no manifesto mas o manifesto real tem 34 â€” rollback falha antes de reverter qualquer alias, bug estÃ¡tico nÃ£o dependente de incidente real â€” ver reviews/full-audit-round2-operacoes-summary.md. E-019 â€” full-audit round2, eixo GovernanÃ§a JurÃ­dica/Contratual/Terceiros, reaberto por D-231 (primeiro secret de vendor externo real, Meta Cloud API); gate â‰¥9.0 ainda nÃ£o atingido (Claude 4,73â†’5,247, Codex 5,163â†’5,275, convergido em 2 rodadas); E-018 â€” full-audit round2, eixo SeguranÃ§a da InformaÃ§Ã£o e AppSec, ver decisions-log.md)
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Marcar um item de todo list de cÃ³digo como `completed` | `definition-of-done.md` â€” gate por item (nÃ£o por PR/wave inteira): unidade de conclusÃ£o, gate por nÃ­vel de risco, registro mÃ­nimo de evidÃªncia |
| Fechar QUALQUER tarefa (checklist rÃ¡pida, nÃ£o substitui os documentos-fonte) | `task-completion-checklist.md` â€” versÃ£o checkbox de `definition-of-done.md`+`change-risk-scale.md`+`quality-gate-tiers.md`+`joint-review-criteria.md`; uso obrigatÃ³rio antes de marcar algo concluÃ­do |
| Decidir se uma proposta Type 1 precisa de pesquisa externa antes da Rodada 1, e como isso vira critÃ©rio de nota | `research-protocol.md` â€” critÃ©rio decidÃ­vel (nÃ­vel 5-6 + padrÃ£o externo estabelecido), declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO`, checklist de critÃ©rios derivado da pesquisa, fluxo de reconciliaÃ§Ã£o se o Codex contestar o checklist |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `definition-of-done.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,2, E-012; emenda E-013) | Definition of Done por item de todo list â€” granularidade ("unidade de conclusÃ£o"), gate por nÃ­vel de risco, classificaÃ§Ã£o de risco na prÃ¡tica, registro mÃ­nimo de evidÃªncia; complementa `quality-gate-tiers.md`/`change-risk-scale.md` sem substituÃ­-los. E-013 tornou a aplicaÃ§Ã£o de `test-engineering-standard.md` operacional (G-V3 mutaÃ§Ã£o nomeada por escrito), nÃ£o sÃ³ referencial |
| `task-completion-checklist.md` | normativo atual (2026-09-18, Marcelo pediu explicitamente que fosse persistido e indispensÃ¡vel daqui em diante) | Checklist em formato checkbox aplicada ao final de toda tarefa â€” deriva de `definition-of-done.md`/`change-risk-scale.md`/`quality-gate-tiers.md`/`joint-review-criteria.md` sem duplicar seu conteÃºdo; em divergÃªncia, o documento-fonte vence |
| `research-protocol.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2, E-014) | Pesquisa externa antes de decisÃµes Type 1 â€” gatilho acoplado a `change-risk-scale.md` nÃ­vel 5-6 + Rodada 1 do protocolo (nunca uma disciplina paralela para lembrar); declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO` com fonte+data+representatividade; checklist de critÃ©rios de nota derivado da pesquisa, sub-rubrica subordinada a `joint-review-criteria.md`; fluxo de reconciliaÃ§Ã£o quando o Codex contesta o checklist em si |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `logging-observability-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, gate elevado 9,5/10) | RÃ©gua concreta de qualidade para logging/tracing/taxonomia de erro (`src/shared/observability/**`, `app-error.ts`, wiring Terraform de detecÃ§Ã£o) â€” 8 critÃ©rios ponderados, Ã¢ncoras de pontuaÃ§Ã£o, gate de auditoria 9,0/10, escrita em resposta a achados reais de uma rodada Codex de 2026-08-29 |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`docs/project/handoffs/expiration-tracker-next-days-master-plan-and-ai-prompt.md`, movido da raiz em 2026-08-29) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

4. **Reabertura de um eixo jÃ¡ fechado** (`full-audit-round2-<eixo>-*`, mesmo padrÃ£o de nomenclatura acima, "round2" substitui "round1" no prefixo, nÃ£o sufixo `-round2.md` isolado como o de uma nota Claude dentro da mesma rodada) â€” usada quando a recomendaÃ§Ã£o de fechamento de um `full-audit-round1-<eixo>-summary.md` cita uma condiÃ§Ã£o objetiva de reabertura (ex.: "reabrir quando X for implementado") e essa condiÃ§Ã£o se materializa. Casos reais: `full-audit-round2-privacidade-*` (E-015, 2026-09-07), reaberto porque a condiÃ§Ã£o nomeada no fechamento round1 (purge worker real) foi satisfeita por D-179/D-190; `full-audit-round2-produto-multitenant-*` (E-016, 2026-09-07), reaberto pelo volume de mudanÃ§a de escopo desde round1 (Document Archive D-143â€“D-226, WhatsApp, B2B-7 a B2B-14) â€” melhora real (4,65â†’7,1/7,8), gate ainda nÃ£o atingido, nenhum vazamento cross-tenant confirmado nesta rodada; `full-audit-round2-governanca-ia-*` (E-017, 2026-09-08); `full-audit-round2-seguranca-*` (E-018, 2026-09-08); `full-audit-round2-juridico-*` (E-019, 2026-09-08), reaberto por D-231 (primeiro secret de vendor externo real, Meta Cloud API) â€” gate ainda nÃ£o atingido (5,247/5,275), convergido em 2 rodadas; `full-audit-round2-arquitetura-*` (E-021, 2026-09-08), reaberto pelo volume de mudanÃ§a de escopo desde round1 (D-084 a D-231+) â€” melhora real (7,966/8,743â†’8,55/8,782), gate ainda nÃ£o atingido, convergido em 3 rodadas, achado novo real nÃ­vel 5 (claim-before-send em `guest-credential-delivery/deliver.ts`, perda silenciosa de entrega, PENDENTE para decisÃ£o de Marcelo).

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.

web search: 
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 test/unit/bff/bff-handlers.test.ts; Get-Content -Encoding utf8 docs/engineering/performance/traces/perf-04-auth.mjs' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 src/modules/bff/http/bff-handlers.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 src/modules/bff/persistence/cognito-idp-auth-client.ts; Get-Content -Encoding utf8 src/modules/bff/application/bff-auth-service.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 252ms:
/**
 * HTTP-boundary tests for bff-handlers.ts - the layer that assembles cookies, enforces CSRF,
 * and decides how each internal outcome (AuthenticationError vs. something else) becomes an
 * HTTP response. Uses a real BffAuthService + real ProxyService (same fakes as
 * bff-auth-service.test.ts/proxy-service.test.ts) rather than a hand-rolled double, so these
 * tests exercise the actual integration, not a mock of it (gap found in review: this boundary
 * previously had no direct test at all, only its constituent services did).
 */
import { describe, expect, it } from "vitest";
import {
  handleLogin,
  handleCallback,
  handleLoginPassword,
  handleSignUp,
  handleConfirmSignUp,
  handleResendConfirmationCode,
  handleForgotPassword,
  handleConfirmForgotPassword,
  handleGetSession,
  handleLogout,
  handleProxy,
  handleCreateOrganization,
  type BffHttpDeps,
} from "../../../src/modules/bff/http/bff-handlers.js";
import type { BffHttpRequest } from "../../../src/modules/bff/http/http-types.js";
import { BffAuthService } from "../../../src/modules/bff/application/bff-auth-service.js";
import { ProxyService, type BackendFetcher } from "../../../src/modules/bff/application/proxy-service.js";
import { InMemorySessionStore } from "./in-memory-session-store.js";
import { FakeCognitoOidcClient, FakeCognitoAuthClient, FakeIdTokenVerifier, FakeTokenEncryptor } from "./fakes.js";
import { InMemoryIdentityStore } from "../identity/in-memory-store.js";
import { InMemoryOrganizationStore } from "../organization/in-memory-store.js";
import { IdentityBootstrapService } from "../../../src/modules/identity/application/bootstrap-identity.js";
import { GlobalUserRepository } from "../../../src/modules/identity/persistence/global-user-repository.js";
import { CreateOrganizationService } from "../../../src/modules/organization/application/create-organization.js";
import { AcceptInvitationService } from "../../../src/modules/organization/application/accept-invitation.js";
import { CSRF_COOKIE_NAME, LOGIN_COOKIE_NAME, SESSION_COOKIE_NAME } from "../../../src/modules/bff/domain/cookies.js";

const TABLE = "MainTable";

function buildDeps(backend: BackendFetcher = { fetch: async () => ({ statusCode: 200, headers: {}, body: "{}" }) }) {
  const sessionStore = new InMemorySessionStore();
  const identityStore = new InMemoryIdentityStore();
  const organizations = new InMemoryOrganizationStore();
  const bootstrap = new IdentityBootstrapService(identityStore, TABLE);
  const globalUsers = new GlobalUserRepository(identityStore);
  const createOrganization = new CreateOrganizationService(organizations, TABLE, { newOrganizationId: () => "org-1", newMembershipId: () => "membership-1", newInvitationId: () => "invitation-1", newAuditEventId: () => "audit-1" });
  const acceptInvitation = new AcceptInvitationService(organizations, TABLE, { newOrganizationId: () => "org-1", newMembershipId: () => "membership-1", newInvitationId: () => "invitation-1", newAuditEventId: () => "audit-1" }, "test-pepper");
  const cognitoClient = new FakeCognitoOidcClient();
  const cognitoAuthClient = new FakeCognitoAuthClient();
  const idTokenVerifier = new FakeIdTokenVerifier();
  const tokenEncryptor = new FakeTokenEncryptor();
  let userCounter = 0;
  let deviceCounter = 0;
  let clock = "2026-08-24T12:00:00.000Z";

  const auth = new BffAuthService({
    sessionStore,
    cognitoClient,
    cognitoAuthClient,
    idTokenVerifier,
    tokenEncryptor,
    bootstrap,
    globalUsers,
    organizations,
    mainTableName: TABLE,
    createOrganization,
    acceptInvitation,
    pepper: "test-pepper",
    redirectUri: "https://app.example.com/bff/callback",
    authorizeUrl: "https://auth.example.com/oauth2/authorize",
    clientId: "client-1",
    now: () => clock,
    newUserId: () => `user-${++userCounter}`,
    newDeviceId: () => `device-${++deviceCounter}`,
  });
  const proxy = new ProxyService(backend, "https://api.example.com");
  const deps: BffHttpDeps = { auth, proxy, appOrigin: "https://app.example.com" };
  return { deps, cognitoClient, cognitoAuthClient, idTokenVerifier, setClock: (iso: string) => { clock = iso; } };
}

function extractCookieValue(setCookieHeaders: string[] | undefined, name: string): string | undefined {
  const raw = setCookieHeaders?.find((c) => c.startsWith(`${name}=`));
  return raw?.split(";")[0]?.slice(name.length + 1);
}

/** Drives a full login through the HTTP handlers (not the service directly), returning the
 * real cookies a browser would end up holding. */
async function loginViaHttp(deps: BffHttpDeps) {
  const loginResponse = await handleLogin(deps, { method: "GET", path: "/bff/login", headers: {}, queryStringParameters: { returnTo: "/items/42" } });
  const loginCookie = extractCookieValue(loginResponse.cookies, LOGIN_COOKIE_NAME)!;
  const url = new URL(loginResponse.headers!["location"]!);
  const state = url.searchParams.get("state")!;

  const callbackResponse = await handleCallback(deps, {
    method: "GET",
    path: "/bff/callback",
    headers: { cookie: `${LOGIN_COOKIE_NAME}=${loginCookie}` },
    queryStringParameters: { code: "auth-code-1", state },
  });
  const sessionCookie = extractCookieValue(callbackResponse.cookies, SESSION_COOKIE_NAME)!;
  const csrfCookie = extractCookieValue(callbackResponse.cookies, CSRF_COOKIE_NAME)!;
  return { loginResponse, callbackResponse, sessionCookie, csrfCookie };
}

function authenticatedRequest(overrides: Partial<BffHttpRequest> & { sessionCookie: string; csrfCookie?: string }): BffHttpRequest {
  const { headers: overrideHeaders, sessionCookie, csrfCookie, ...rest } = overrides;
  const cookieParts = [`${SESSION_COOKIE_NAME}=${sessionCookie}`];
  if (csrfCookie) cookieParts.push(`${CSRF_COOKIE_NAME}=${csrfCookie}`);
  return {
    method: "GET",
    path: "/bff/session",
    ...rest,
    headers: { cookie: cookieParts.join("; "), ...overrideHeaders },
  };
}

describe("handleLogin", () => {
  it("redirects (302) to the Cognito authorize URL and sets an HttpOnly login cookie", async () => {
    const { deps } = buildDeps();
    const res = await handleLogin(deps, { method: "GET", path: "/bff/login", headers: {}, queryStringParameters: { returnTo: "/items/42" } });
    expect(res.statusCode).toBe(302);
    expect(res.headers?.["location"]).toContain("https://auth.example.com/oauth2/authorize");
    expect(extractCookieValue(res.cookies, LOGIN_COOKIE_NAME)).toBeTruthy();
  });
});

describe("handleCallback", () => {
  it("happy path: sets session + csrf cookies, clears the login cookie, redirects to appOrigin+returnTo", async () => {
    const { deps } = buildDeps();
    const { callbackResponse } = await loginViaHttp(deps);
    expect(callbackResponse.statusCode).toBe(302);
    expect(callbackResponse.headers?.["location"]).toBe("https://app.example.com/items/42");
    expect(extractCookieValue(callbackResponse.cookies, SESSION_COOKIE_NAME)).toBeTruthy();
    expect(extractCookieValue(callbackResponse.cookies, CSRF_COOKIE_NAME)).toBeTruthy();
    expect(callbackResponse.cookies?.some((c) => c.startsWith(`${LOGIN_COOKIE_NAME}=;`))).toBe(true);
  });
});

describe("handleGetSession", () => {
  // Wave B2B-5 (D-095): tenantId leaves the response - activeOrganizationId/onboardingState
  // take its place. A fresh login has no Organization yet, so onboardingState is reported.
  it("returns 200 {authenticated:true, onboardingState:NO_TENANT_NO_MEMBERSHIP} for a fresh session with no Organization yet", async () => {
    const { deps } = buildDeps();
    const { sessionCookie } = await loginViaHttp(deps);
    const res = await handleGetSession(deps, authenticatedRequest({ sessionCookie }));
    expect(res.statusCode).toBe(200);
    expect(res.body).toMatchObject({ authenticated: true, onboardingState: "NO_TENANT_NO_MEMBERSHIP" });
    expect((res.body as Record<string, unknown>)["activeOrganizationId"]).toBeUndefined();
  });

  // #15/sidebar identity card (2026-09-21) - displayName/email resolved from GlobalUser, never
  // session.userId itself (excluded by design, D-095/D-096).
  it("includes displayName/email resolved from the GlobalUser row created at login", async () => {
    const { deps, idTokenVerifier } = buildDeps();
    idTokenVerifier.nextResult = { subject: "cognito-sub-1", email: "ana@example.com", name: "Ana Exemplo" };
    const { sessionCookie } = await loginViaHttp(deps);
    const res = await handleGetSession(deps, authenticatedRequest({ sessionCookie }));
    expect(res.body).toMatchObject({ displayName: "Ana Exemplo", email: "ana@example.com" });
    expect((res.body as Record<string, unknown>)["userId"]).toBeUndefined();
  });

  it("returns 200 {authenticated:false} when there is definitively no session (missing cookie)", async () => {
    const { deps } = buildDeps();
    const res = await handleGetSession(deps, { method: "GET", path: "/bff/session", headers: {} });
    expect(res.statusCode).toBe(200);
    expect(res.body).toEqual({ authenticated: false });
  });

  it("does NOT collapse a genuinely uncertain failure into authenticated:false - a transient dependency failure surfaces as its real error, not a stronger 'logged out' claim (found in review: this used to catch every resolveSession failure the same way)", async () => {
    const { deps, cognitoClient, setClock } = buildDeps();
    const { sessionCookie } = await loginViaHttp(deps);

    // Force the access token to look expired and Cognito's refresh call to fail transiently -
    // this is a "we don't know" outcome, not "definitely not authenticated".
    setClock("2026-08-24T12:20:00.000Z");
    cognitoClient.refreshShouldThrow = true;

    const res = await handleGetSession(deps, authenticatedRequest({ sessionCookie }));
    expect(res.statusCode).toBe(503);
    expect(res.body).not.toEqual({ authenticated: false });
  });
});

describe("handleLogout CSRF enforcement", () => {
  it("rejects (403) a same-origin request with a missing/mismatched CSRF header even with a valid session cookie", async () => {
    const { deps } = buildDeps();
    const { sessionCookie, csrfCookie } = await loginViaHttp(deps);
    const res = await handleLogout(
      deps,
      authenticatedRequest({ sessionCookie, csrfCookie, method: "POST", path: "/bff/session/logout", headers: { "sec-fetch-site": "same-origin" } }),
    );
    expect(res.statusCode).toBe(403);
  });

  it("rejects (403) a cross-site request even with a correct CSRF header/cookie pair (Sec-Fetch-Site layer)", async () => {
    const { deps } = buildDeps();
    const { sessionCookie, csrfCookie } = await loginViaHttp(deps);
    const res = await handleLogout(
      deps,
      authenticatedRequest({
        sessionCookie,
        csrfCookie,
        method: "POST",
        path: "/bff/session/logout",
        headers: { "sec-fetch-site": "cross-site", "x-csrf-token": csrfCookie },
      }),
    );
    expect(res.statusCode).toBe(403);
  });

  it("succeeds (204) and clears cookies when Sec-Fetch-Site, header, and cookie all agree", async () => {
    const { deps } = buildDeps();
    const { sessionCookie, csrfCookie } = await loginViaHttp(deps);
    const res = await handleLogout(
      deps,
      authenticatedRequest({
        sessionCookie,
        csrfCookie,
        method: "POST",
        path: "/bff/session/logout",
        headers: { "sec-fetch-site": "same-origin", "x-csrf-token": csrfCookie },
      }),
    );
    expect(res.statusCode).toBe(204);
    expect(res.cookies?.some((c) => c.startsWith(`${SESSION_COOKIE_NAME}=;`))).toBe(true);

    const after = await handleGetSession(deps, authenticatedRequest({ sessionCookie }));
    expect(after.body).toEqual({ authenticated: false });
  });

  it("is a harmless no-op (204) for a request with no session cookie at all - nothing to protect, matches logout()'s own no-op", async () => {
    const { deps } = buildDeps();
    const res = await handleLogout(deps, { method: "POST", path: "/bff/session/logout", headers: {} });
    expect(res.statusCode).toBe(204);
  });
});

describe("handleProxy", () => {
  it("rejects (403) a proxied mutation missing CSRF, even to an allowlisted route", async () => {
    const { deps } = buildDeps();
    const { sessionCookie } = await loginViaHttp(deps);
    const res = await handleProxy(deps, authenticatedRequest({ sessionCookie, method: "POST", path: "/bff/api/items", headers: { "sec-fetch-site": "same-origin" } }), "/items", undefined);
    expect(res.statusCode).toBe(403);
  });

  it("forwards an allowlisted GET (safe method, no CSRF required) and returns the backend's response", async () => {
    const backend: BackendFetcher = { fetch: async () => ({ statusCode: 200, headers: { "content-type": "application/json" }, body: '{"items":[]}' }) };
    const { deps } = buildDeps(backend);
    const { sessionCookie } = await loginViaHttp(deps);
    const res = await handleProxy(deps, authenticatedRequest({ sessionCookie, method: "GET", path: "/bff/api/items/dashboard" }), "/items/dashboard", undefined);
    expect(res.statusCode).toBe(200);
    expect(res.body).toEqual({ items: [] });
  });

  // G3 follow-up bug: the 7 CSV report routes (reports-handler.ts) return text/csv, not JSON -
  // handleProxy used to JSON.parse() every backend body unconditionally, 500ing on any real CSV
  // response ("Unexpected token 'i', \"itemId,nam\"... is not valid JSON"). Confirms the fix:
  // a non-JSON content-type is passed through untouched, never parsed, with isRawBody set so
  // the API Gateway adapter also skips JSON.stringify() on the way back out.
  it("passes a CSV backend response through untouched instead of JSON.parse()ing it", async () => {
    const csvBody = 'itemId,name\n"item-1","Contract A"\n';
    const backend: BackendFetcher = {
      fetch: async () => ({
        statusCode: 200,
        headers: { "content-type": "text/csv; charset=utf-8", "content-disposition": 'attachment; filename="report.csv"' },
        body: csvBody,
      }),
    };
    const { deps } = buildDeps(backend);
    const { sessionCookie } = await loginViaHttp(deps);
    const res = await handleProxy(deps, authenticatedRequest({ sessionCookie, method: "GET", path: "/bff/api/reports/expiring-soon-items" }), "/reports/expiring-soon-items", undefined);
    expect(res.statusCode).toBe(200);
    expect(res.body).toBe(csvBody);
    expect(res.isRawBody).toBe(true);
    expect(res.headers?.["content-type"]).toBe("text/csv; charset=utf-8");
    expect(res.headers?.["content-disposition"]).toBe('attachment; filename="report.csv"');
  });
});

describe("handleCreateOrganization", () => {
  // D-129 (GTR-01 supersession): Organization.displayName is now the ONLY guest-facing
  // requester identity, so a whitespace-only name must never reach CreateOrganizationService.
  // Mutação: remover o `.trim()` de `displayName` no handler (ou a checagem `!displayName`
  // após ele) faria isto passar em vez de rejeitar com 400.
  it("D-129: rejects (400) a whitespace-only displayName", async () => {
    const { deps } = buildDeps();
    const { sessionCookie, csrfCookie } = await loginViaHttp(deps);
    const res = await handleCreateOrganization(
      deps,
      authenticatedRequest({
        sessionCookie,
        csrfCookie,
        method: "POST",
        path: "/bff/organizations",
        headers: { "sec-fetch-site": "same-origin", "x-csrf-token": csrfCookie },
        body: JSON.stringify({ displayName: "   ", timezone: "UTC" }),
      }),
    );
    expect(res.statusCode).toBe(400);
  });

  it("D-129: trims a padded valid displayName before creating the Organization", async () => {
    const { deps } = buildDeps();
    const { sessionCookie, csrfCookie } = await loginViaHttp(deps);
    const res = await handleCreateOrganization(
      deps,
      authenticatedRequest({
        sessionCookie,
        csrfCookie,
        method: "POST",
        path: "/bff/organizations",
        headers: { "sec-fetch-site": "same-origin", "x-csrf-token": csrfCookie },
        body: JSON.stringify({ displayName: "  Empresa Alfa  ", timezone: "UTC" }),
      }),
    );
    expect(res.statusCode).toBe(201);
  });
});

// Round-1 Codex finding (d321-direct-auth-adversarial-review): all 6 D-3xx routes below now
// require Fetch Metadata (`Sec-Fetch-Site: same-origin` or `none`) before anything else runs -
// `same-origin` is the real browser value these routes see in production; every other test in
// this section uses it so it never has to think about the guard while testing its own concern.
const SAME_ORIGIN_HEADERS = { "sec-fetch-site": "same-origin" };

// D-3xx (reversal of D-320): the app's own login/signup/reset-password screens now call these
// directly instead of redirecting to the Cognito Hosted UI - handleLogin/handleCallback above
// stay covered by their own existing tests (dormant fallback, never removed).
describe("handleLoginPassword", () => {
  it("403s a cross-site request before ever touching the body (login-CSRF guard)", async () => {
    const { deps } = buildDeps();
    const res = await handleLoginPassword(deps, {
      method: "POST",
      path: "/bff/login",
      headers: { "sec-fetch-site": "cross-site" },
      body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }),
    });
    expect(res.statusCode).toBe(403);
  });

  it("403s when Sec-Fetch-Site is absent - never fail-open for an older browser or unlabeled request", async () => {
    const { deps } = buildDeps();
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
    expect(res.statusCode).toBe(403);
  });

  it("400s when email or password is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com" }) });
    expect(res.statusCode).toBe(400);
  });

  it("400s on a malformed (non-JSON-object) body instead of a raw 500", async () => {
    const { deps } = buildDeps();
    const notJson = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: "not-json" });
    expect(notJson.statusCode).toBe(400);
    const arrayBody = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: "[]" });
    expect(arrayBody.statusCode).toBe(400);
  });

  it("sets session and CSRF cookies on a successful password login", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = {
      kind: "SUCCESS",
      tokens: { accessToken: "header." + Buffer.from(JSON.stringify({ sub: "s1" })).toString("base64url") + ".sig", idToken: "id-1", refreshToken: "refresh-1", expiresInSeconds: 900 },
    };
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
    expect(res.statusCode).toBe(200);
    expect(extractCookieValue(res.cookies, SESSION_COOKIE_NAME)).toBeTruthy();
    expect(extractCookieValue(res.cookies, CSRF_COOKIE_NAME)).toBeTruthy();
  });

  it("returns a generic 401 for invalid credentials - never distinguishes 'no such user' from 'wrong password' (anti-enumeration, D-3xx decision 3)", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = { kind: "INVALID_CREDENTIALS" };
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "nobody@example.com", password: "wrong" }) });
    expect(res.statusCode).toBe(401);
  });

  it("returns the same generic 401 for USER_NOT_CONFIRMED as for invalid credentials", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = { kind: "USER_NOT_CONFIRMED" };
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(res.statusCode).toBe(401);
  });

  // Round-1 Codex finding: TRANSIENT_FAILURE/UNKNOWN_OUTCOME used to fold into the SAME 401
  // "invalid credentials" as a real wrong password - a genuine Cognito outage/throttling told
  // the user they mistyped their password. Both now map to 503, never a false claim about the
  // credential itself.
  it("returns 503 (never 401) when authentication genuinely could not be completed", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = { kind: "TRANSIENT_FAILURE", cause: new Error("boom") };
    const transient = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(transient.statusCode).toBe(503);

    cognitoAuthClient.nextAuthenticateOutcome = { kind: "UNKNOWN_OUTCOME" };
    const unknown = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(unknown.statusCode).toBe(503);
  });
});

describe("handleSignUp", () => {
  it("403s a cross-site request", async () => {
    const { deps } = buildDeps();
    const res = await handleSignUp(deps, {
      method: "POST",
      path: "/bff/signup",
      headers: { "sec-fetch-site": "cross-site" },
      body: JSON.stringify({ email: "user@example.com", password: "x", name: "Ana" }),
    });
    expect(res.statusCode).toBe(403);
  });

  it("400s when email, password or name is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleSignUp(deps, { method: "POST", path: "/bff/signup", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(res.statusCode).toBe(400);
  });

  it("202s with CONFIRMATION_REQUIRED on a fresh signup", async () => {
    const { deps } = buildDeps();
    const res = await handleSignUp(deps, {
      method: "POST",
      path: "/bff/signup",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "new@example.com", password: "correct-horse-battery-1", name: "Ana Exemplo" }),
    });
    expect(res.statusCode).toBe(202);
    expect(res.body).toEqual({ status: "CONFIRMATION_REQUIRED" });
  });

  it("409s when the e-mail is already registered", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextSignUpOutcome = { kind: "EMAIL_ALREADY_REGISTERED" };
    const res = await handleSignUp(deps, {
      method: "POST",
      path: "/bff/signup",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "taken@example.com", password: "x", name: "Ana Exemplo" }),
    });
    expect(res.statusCode).toBe(409);
  });
});

describe("handleConfirmSignUp", () => {
  it("400s when email or confirmationCode is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com" }) });
    expect(res.statusCode).toBe(400);
  });

  it("204s on a valid confirmation code", async () => {
    const { deps } = buildDeps();
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
    expect(res.statusCode).toBe(204);
  });

  it("400s on an invalid/expired code", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "INVALID_CODE_OR_EXPIRED" };
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000" }) });
    expect(res.statusCode).toBe(400);
  });

  // Round-1 Codex finding: ConfirmSignUp's NotAuthorizedException used to be treated as
  // ALREADY_CONFIRMED unconditionally - ANY authorization failure (not just "already confirmed")
  // silently became a 204 success. Only the documented "already confirmed" message now does.
  it("does not report success for a NotAuthorizedException that isn't the documented 'already confirmed' case", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "UNKNOWN_OUTCOME" };
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
    expect(res.statusCode).toBe(503);
  });
});

describe("handleResendConfirmationCode", () => {
  it("204s regardless of whether the e-mail is registered (anti-enumeration)", async () => {
    const { deps } = buildDeps();
    const res = await handleResendConfirmationCode(deps, { method: "POST", path: "/bff/signup/resend", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "anyone@example.com" }) });
    expect(res.statusCode).toBe(204);
  });
});

describe("handleForgotPassword", () => {
  it("202s regardless of whether the e-mail is registered (anti-enumeration, D-3xx decision 3)", async () => {
    const { deps } = buildDeps();
    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "anyone@example.com" }) });
    expect(res.statusCode).toBe(202);
  });

  it("400s when email is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({}) });
    expect(res.statusCode).toBe(400);
  });
});

describe("handleConfirmForgotPassword", () => {
  it("204s on a valid code + new password", async () => {
    const { deps } = buildDeps();
    const res = await handleConfirmForgotPassword(deps, {
      method: "POST",
      path: "/bff/forgot-password/confirm",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456", newPassword: "Correct-Horse-1" }),
    });
    expect(res.statusCode).toBe(204);
  });

  it("400s on an invalid/expired code", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextConfirmForgotPasswordOutcome = { kind: "INVALID_CODE_OR_EXPIRED" };
    const res = await handleConfirmForgotPassword(deps, {
      method: "POST",
      path: "/bff/forgot-password/confirm",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000", newPassword: "x" }),
    });
    expect(res.statusCode).toBe(400);
  });
});
// PERF-04: obtain a real BFF session (cookies) for the synthetic perf-test tenant user.
//
// D-322 (2026-09-22): rewritten to call `POST /bff/login` (direct-auth entry point added by
// D-321) with a plain `fetch`, instead of driving a headless browser through the Cognito
// Hosted UI form (`#signInFormUsername` et al.). That selector-based flow broke the moment
// D-321 stopped being the app's primary login path - and turned out to be fragile even for the
// dormant `GET /bff/login` fallback itself, because Cognito's real `ManagedLoginVersion` stayed
// at 2 in the AWS account (a `managed_login_version` argument removed from Terraform config
// does not force a downgrade of an already-live domain - optional+computed, D-320/D-321 never
// actually reverted it in practice), which renders different markup than the classic Hosted UI
// this script's old selectors assumed. The direct-auth endpoint sidesteps all of that: it's the
// same JSON contract the app's own `/login` screen uses, no browser needed at all.
//
// Usage:
//   node docs/engineering/performance/traces/perf-04-auth.mjs
//
// Reads credentials from docs/engineering/performance/.local/perf-test-tenant-credentials.txt
// (gitignored - never commit). Prints the resulting cookies as JSON to stdout so they can be
// piped into curl/fetch-based follow-up scripts. Also writes them to
// docs/engineering/performance/.local/perf-04-session-cookies.json (gitignored) for reuse across
// multiple runs without re-authenticating (BFF session TTL is well beyond a single work session).

import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const CREDS_PATH = path.resolve(__dirname, "../.local/perf-test-tenant-credentials.txt");
const OUT_PATH = path.resolve(__dirname, "../.local/perf-04-session-cookies.json");
const APP_ORIGIN = "https://d1mbs2t047qo9d.cloudfront.net";
// This script runs via plain `node` (no ts-node/tsx, no build step first - ci.yml calls it
// directly), so the real constants in src/modules/bff/domain/cookies.ts can't be imported here;
// kept as literals in sync with that file, same convention the old Playwright-based version of
// this script already used.
const SESSION_COOKIE_NAME = "__Host-et_session";
const CSRF_COOKIE_NAME = "__Host-et_csrf";

function parseCreds(text) {
  const out = {};
  for (const line of text.split("\n")) {
    const m = line.match(/^(\w+)=(.*)$/);
    if (m) out[m[1]] = m[2].trim();
  }
  return out;
}

/** Splits a `Set-Cookie` value list into `{name, value}` pairs, keeping only the cookie's own
 * first `name=value` segment (drops `Path=`/`Secure`/`HttpOnly`/... attributes) - `getSetCookie()`
 * already separates multiple headers, this just parses each one's leading pair. */
function parseSetCookiePairs(setCookieValues) {
  return setCookieValues.map((raw) => {
    const firstSegment = raw.split(";")[0] ?? "";
    const eq = firstSegment.indexOf("=");
    return { name: firstSegment.slice(0, eq).trim(), value: firstSegment.slice(eq + 1).trim() };
  });
}

async function main() {
  const creds = process.env.PERF_TEST_EMAIL && process.env.PERF_TEST_PASSWORD
    ? { email: process.env.PERF_TEST_EMAIL, password: process.env.PERF_TEST_PASSWORD }
    : parseCreds(readFileSync(CREDS_PATH, "utf-8"));
  if (!creds.email || !creds.password) {
    throw new Error("Could not parse email/password from credentials file: " + CREDS_PATH);
  }

  const response = await fetch(`${APP_ORIGIN}/bff/login`, {
    method: "POST",
    // "none" is Fetch Metadata's own value for a request with no initiating browsing context
    // (typed URL, bookmark) - the correct declaration for a direct, first-party script calling
    // the API itself, not a browser page (round-1 Codex finding, d321-direct-auth-adversarial-
    // review: POST /bff/login now rejects any request that can't declare itself same-site/none,
    // closing a login-CSRF gap a cross-site browser page can never spoof this header to bypass).
    headers: { "content-type": "application/json", "sec-fetch-site": "none" },
    body: JSON.stringify({ email: creds.email, password: creds.password }),
  });

  if (!response.ok) {
    throw new Error(`POST /bff/login failed: ${response.status} ${await response.text()}`);
  }

  const cookies = parseSetCookiePairs(response.headers.getSetCookie());
  const sessionCookie = cookies.find((c) => c.name === SESSION_COOKIE_NAME);
  const csrfCookie = cookies.find((c) => c.name === CSRF_COOKIE_NAME);

  if (!sessionCookie) {
    console.error("All cookies seen:", cookies.map((c) => c.name));
    throw new Error("Did not find a session cookie after login - check cookie name constants in src/modules/bff/domain/cookies.ts");
  }

  const result = {
    cookieHeader: cookies.map((c) => `${c.name}=${c.value}`).join("; "),
    sessionCookieName: sessionCookie.name,
    csrfCookieName: csrfCookie ? csrfCookie.name : null,
    csrfTokenValue: csrfCookie ? csrfCookie.value : null,
    obtainedAt: new Date().toISOString(),
  };

  mkdirSync(path.dirname(OUT_PATH), { recursive: true });
  writeFileSync(OUT_PATH, JSON.stringify(result, null, 2));
  console.log(`[perf-auth] BFF session obtained at ${result.obtainedAt}`);
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});

 succeeded in 247ms:
/**
 * HTTP-shape handlers for /bff/* routes. These routes ARE the authentication boundary - no
 * JWT authorizer runs before them (unlike src/modules/expiration/http/item-handlers.ts),
 * so every handler here is responsible for its own cookie/CSRF verification.
 */
import { AppError, AuthenticationError, AuthorizationError, toAppError, ValidationError } from "../../../shared/errors/app-error.js";
import { logger } from "../../../shared/observability/logger.js";
import { emitMetric } from "../../../shared/observability/metrics.js";
import { BffAuthService } from "../application/bff-auth-service.js";
import { ProxyService } from "../application/proxy-service.js";
import { checkCsrf, isSameSiteFetch } from "../domain/csrf.js";
import {
  SESSION_COOKIE_NAME,
  LOGIN_COOKIE_NAME,
  CSRF_COOKIE_NAME,
  LOGIN_COOKIE_ATTRIBUTES,
  SESSION_COOKIE_ATTRIBUTES,
  CSRF_COOKIE_ATTRIBUTES,
  buildSetCookieHeader,
  buildClearCookieHeader,
  parseCookieHeader,
} from "../domain/cookies.js";
import type { BffHttpRequest, BffHttpResponse } from "./http-types.js";

const STATUS_BY_CATEGORY: Record<string, number> = {
  VALIDATION: 400,
  AUTH: 401,
  AUTHORIZATION: 403,
  NOT_FOUND: 404,
  CONFLICT: 409,
  QUOTA_EXCEEDED: 429,
  DEPENDENCY_UNAVAILABLE: 503,
  INTERNAL: 500,
};

// PERF-02: same per-service EMF namespace convention as the other handler files (e.g.
// "ExpirationTracker/ReminderDispatch" in reminder-dispatch-handler.ts, D-290).
const METRICS_NAMESPACE = "ExpirationTracker/BFF";

/** Low-cardinality status class ("2xx"/"4xx"/"5xx"/...) - never the raw status code as a metric
 * dimension (metrics.ts's own dimension-discipline rule). */
function statusClass(statusCode: number): string {
  return `${Math.floor(statusCode / 100)}xx`;
}

function toErrorResponse(err: unknown): BffHttpResponse {
  const appError = err instanceof AppError ? err : toAppError(err);
  return { statusCode: STATUS_BY_CATEGORY[appError.category] ?? 500, body: appError.toJSON() };
}

function cookiesOf(req: BffHttpRequest): Record<string, string> {
  return parseCookieHeader(req.headers["cookie"] ?? req.headers["Cookie"]);
}

/** Login-CSRF guard for the 6 D-3xx unauthenticated auth mutation routes (round-1 Codex finding,
 * `docs/architecture/reviews/d321-direct-auth-adversarial-review/`) - none of them has a session
 * yet to run the full `checkCsrf()` triple-layer check against, but Fetch Metadata
 * (`Sec-Fetch-Site`) needs no session/token to verify and is exactly the same primitive
 * `checkCsrf()` already uses as its first layer (`isSameSiteFetch`). Without this, a cross-site
 * page could POST the ATTACKER's own credentials to `handleLoginPassword` from the victim's
 * browser, establishing the attacker's session there (login-CSRF) - "no session cookie exists yet
 * to protect" was the wrong framing; a cross-site form submission to a JSON endpoint can still
 * succeed (`enctype="text/plain"` shaped to parse as valid JSON is a known bypass), so this must
 * be checked BEFORE the request is ever treated as legitimate, not skipped because there is "no
 * CSRF token" to compare against. */
function requireSameSiteFetch(req: BffHttpRequest): void {
  if (!isSameSiteFetch(req.headers["sec-fetch-site"])) {
    throw new AuthorizationError("Cross-site request rejected.", { reason: "CROSS_SITE_FETCH_REJECTED" });
  }
}

/** Shared safe JSON body parser for the 6 D-3xx handlers below (round-1 Codex finding) - a
 * malformed body (invalid JSON, or valid JSON that isn't an object - e.g. `null`, an array, a
 * bare string) used to reach `JSON.parse(...) as Record<string, unknown>` unchecked, then fail
 * with an uncaught TypeError on property access, surfacing as a 500 for what is really a 400
 * client error. */
function parseJsonObjectBody(body: string | undefined): Record<string, unknown> {
  if (!body) return {};
  let parsed: unknown;
  try {
    parsed = JSON.parse(body);
  } catch {
    throw new ValidationError("Request body is not valid JSON.");
  }
  if (typeof parsed !== "object" || parsed === null || Array.isArray(parsed)) {
    throw new ValidationError("Request body must be a JSON object.");
  }
  return parsed as Record<string, unknown>;
}

export interface BffHttpDeps {
  auth: BffAuthService;
  proxy: ProxyService;
  appOrigin: string; // for building same-origin redirect targets
}

export async function handleLogin(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const returnTo = req.queryStringParameters?.["returnTo"] ?? "/";
    const { loginToken, redirectUrl } = await deps.auth.startLogin(returnTo);
    return {
      statusCode: 302,
      headers: { location: redirectUrl },
      cookies: [buildSetCookieHeader(LOGIN_COOKIE_NAME, loginToken, LOGIN_COOKIE_ATTRIBUTES)],
      body: {},
    };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** D-3xx: `POST /bff/login` - direct-auth entry point replacing the Hosted UI redirect
 * (`handleLogin`/`handleCallback` above stay in place, dormant fallback - see decisions-log.md
 * D-3xx). `requireSameSiteFetch()` (round-1 Codex finding, `docs/architecture/reviews/
 * d321-direct-auth-adversarial-review/`) replaces this handler's earlier "no CSRF check needed,
 * no session exists yet" reasoning - that reasoning was correct for the double-submit-token half
 * of CSRF protection (there is genuinely no session/token to compare against yet), but wrong for
 * login-CSRF specifically (an attacker's OWN credentials, submitted from the VICTIM's browser,
 * establishing the ATTACKER's session there) - Fetch Metadata needs no session to check. */
export async function handleLoginPassword(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    requireSameSiteFetch(req);
    const body = parseJsonObjectBody(req.body);
    const email = typeof body["email"] === "string" ? body["email"] : undefined;
    const password = typeof body["password"] === "string" ? body["password"] : undefined;
    if (!email || !password) throw new ValidationError("email and password are required.");

    const { sessionToken, csrfToken } = await deps.auth.loginWithPassword({ email, password });
    return {
      statusCode: 200,
      body: {},
      cookies: [
        buildSetCookieHeader(SESSION_COOKIE_NAME, sessionToken, SESSION_COOKIE_ATTRIBUTES),
        buildSetCookieHeader(CSRF_COOKIE_NAME, csrfToken, CSRF_COOKIE_ATTRIBUTES),
      ],
    };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** D-3xx: `POST /bff/signup` - self-service registration (parity with the Hosted UI's own
 * "Sign up" link, which was live by default - see BffAuthService.signUp's doc comment).
 * `requireSameSiteFetch()` applied for defense-in-depth consistency across all 6 D-3xx routes
 * (round-1 Codex finding) even though this route sets no session cookie itself. */
export async function handleSignUp(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    requireSameSiteFetch(req);
    const body = parseJsonObjectBody(req.body);
    const email = typeof body["email"] === "string" ? body["email"] : undefined;
    const password = typeof body["password"] === "string" ? body["password"] : undefined;
    const name = typeof body["name"] === "string" ? body["name"] : undefined;
    if (!email || !password || !name) throw new ValidationError("email, password and name are required.");

    const result = await deps.auth.signUp({ email, password, name });
    return { statusCode: 202, body: result };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** D-3xx: `POST /bff/signup/confirm` - the e-mail verification step. */
export async function handleConfirmSignUp(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    requireSameSiteFetch(req);
    const body = parseJsonObjectBody(req.body);
    const email = typeof body["email"] === "string" ? body["email"] : undefined;
    const confirmationCode = typeof body["confirmationCode"] === "string" ? body["confirmationCode"] : undefined;
    if (!email || !confirmationCode) throw new ValidationError("email and confirmationCode are required.");

    await deps.auth.confirmSignUp({ email, confirmationCode });
    return { statusCode: 204, body: {} };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** D-3xx: `POST /bff/signup/resend` - reissues the confirmation code. */
export async function handleResendConfirmationCode(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    requireSameSiteFetch(req);
    const body = parseJsonObjectBody(req.body);
    const email = typeof body["email"] === "string" ? body["email"] : undefined;
    if (!email) throw new ValidationError("email is required.");

    await deps.auth.resendConfirmationCode({ email });
    return { statusCode: 204, body: {} };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** D-3xx: `POST /bff/forgot-password` - always 202, anti-enumeration (decision 3): the caller
 * can never tell from this response alone whether the e-mail is actually registered. */
export async function handleForgotPassword(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    requireSameSiteFetch(req);
    const body = parseJsonObjectBody(req.body);
    const email = typeof body["email"] === "string" ? body["email"] : undefined;
    if (!email) throw new ValidationError("email is required.");

    await deps.auth.startForgotPassword({ email });
    return { statusCode: 202, body: {} };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** D-3xx: `POST /bff/forgot-password/confirm` - code + new password. */
export async function handleConfirmForgotPassword(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    requireSameSiteFetch(req);
    const body = parseJsonObjectBody(req.body);
    const email = typeof body["email"] === "string" ? body["email"] : undefined;
    const confirmationCode = typeof body["confirmationCode"] === "string" ? body["confirmationCode"] : undefined;
    const newPassword = typeof body["newPassword"] === "string" ? body["newPassword"] : undefined;
    if (!email || !confirmationCode || !newPassword) throw new ValidationError("email, confirmationCode and newPassword are required.");

    await deps.auth.confirmForgotPassword({ email, confirmationCode, newPassword });
    return { statusCode: 204, body: {} };
  } catch (err) {
    return toErrorResponse(err);
  }
}

export async function handleCallback(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const code = req.queryStringParameters?.["code"];
    const state = req.queryStringParameters?.["state"];
    if (!code || !state) throw new ValidationError("Missing code or state on OIDC callback.");

    const loginCookie = cookiesOf(req)[LOGIN_COOKIE_NAME];
    const { sessionToken, csrfToken, returnTo } = await deps.auth.handleCallback({ loginCookie, code, state });

    return {
      statusCode: 302,
      headers: { location: `${deps.appOrigin}${returnTo}` },
      cookies: [
        buildSetCookieHeader(SESSION_COOKIE_NAME, sessionToken, SESSION_COOKIE_ATTRIBUTES),
        buildSetCookieHeader(CSRF_COOKIE_NAME, csrfToken, CSRF_COOKIE_ATTRIBUTES),
        buildClearCookieHeader(LOGIN_COOKIE_NAME, LOGIN_COOKIE_ATTRIBUTES),
      ],
      body: {},
    };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** GET /bff/session - lets the frontend learn its own auth state on boot without needing to
 * probe a proxied resource route first (AUTHENTICATED/SESSION_MISSING/SESSION_EXPIRED per the
 * Frontend Production Foundation mission's required auth-state contract).
 *
 * Wave B2B-5 (D-095): `tenantId` leaves the response - `activeOrganizationId`/`onboardingState`
 * take its place (`resolveSessionWithOnboarding`, self-heals a stale/missing
 * `activeOrganizationId` against the real Membership data before responding). Exactly one of
 * the two is present: an established organization, or the reason there isn't one yet. */
export async function handleGetSession(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const { activeOrganizationId, onboardingState, organizationSelectionRequired, displayName, email } = await deps.auth.resolveSessionWithOnboarding(
      cookiesOf(req)[SESSION_COOKIE_NAME],
    );
    return { statusCode: 200, body: { authenticated: true, activeOrganizationId, onboardingState, organizationSelectionRequired, displayName, email } };
  } catch (err) {
    if (err instanceof AuthenticationError) {
      // Definitive: no session, or one that is genuinely gone (expired/revoked/malformed) -
      // never leak WHICH of those to the browser, they all look identical from here.
      return { statusCode: 200, body: { authenticated: false } };
    }
    // Anything else (e.g. a DependencyUnavailableError from a transient/unknown refresh
    // outcome) means resolution genuinely could not be completed - collapsing this into
    // "authenticated: false" would tell the frontend a stronger claim than we can support
    // (AuthContext's SESSION_MISSING vs. REFRESH_FAILED distinction exists precisely so this
    // case surfaces as "we don't know", not "definitely logged out" - found in review).
    return toErrorResponse(err);
  }
}

async function checkCsrfForSession(deps: BffHttpDeps, req: BffHttpRequest, sessionCookie: string | undefined): Promise<boolean> {
  const cookies = cookiesOf(req);
  let session;
  try {
    session = await deps.auth.resolveSession(sessionCookie);
  } catch (err) {
    if (err instanceof AuthenticationError) {
      // No resolvable session at all (missing/malformed/expired/revoked) - nothing to
      // protect against forging, logout of a nonexistent session is a harmless no-op either
      // way (see logout()'s own early-return on a missing/invalid cookie).
      return true;
    }
    // Any other failure (e.g. a transient dependency issue while resolving) means we cannot
    // rule out that a real, valid session exists to protect - fail closed rather than assume
    // there is nothing there (found in review: the previous blanket catch treated every
    // failure the same as "definitely no session").
    return false;
  }
  return checkCsrf({
    method: req.method,
    secFetchSite: req.headers["sec-fetch-site"],
    headerToken: req.headers["x-csrf-token"],
    cookieToken: cookies[CSRF_COOKIE_NAME],
    sessionCsrfSecret: session.csrfSecret,
  });
}

export async function handleLogout(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  const cookies = cookiesOf(req);
  if (!(await checkCsrfForSession(deps, req, cookies[SESSION_COOKIE_NAME]))) {
    return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
  }
  await deps.auth.logout(cookies[SESSION_COOKIE_NAME]);
  return {
    statusCode: 204,
    cookies: [
      buildClearCookieHeader(SESSION_COOKIE_NAME, SESSION_COOKIE_ATTRIBUTES),
      buildClearCookieHeader(CSRF_COOKIE_NAME, CSRF_COOKIE_ATTRIBUTES),
    ],
    body: {},
  };
}

export async function handleLogoutAll(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  const cookies = cookiesOf(req);
  if (!(await checkCsrfForSession(deps, req, cookies[SESSION_COOKIE_NAME]))) {
    return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
  }
  await deps.auth.logoutAll(cookies[SESSION_COOKIE_NAME]);
  return {
    statusCode: 204,
    cookies: [
      buildClearCookieHeader(SESSION_COOKIE_NAME, SESSION_COOKIE_ATTRIBUTES),
      buildClearCookieHeader(CSRF_COOKIE_NAME, CSRF_COOKIE_ATTRIBUTES),
    ],
    body: {},
  };
}

/** POST /bff/organizations (Wave B2B-5, D-095, achado 2.3): the first real HTTP consumer of
 * `CreateOrganizationService` (B2B-3/D-091) - without it, `bootstrapUser()`'s 2-item transact
 * would strand every fresh login in `NO_TENANT_NO_MEMBERSHIP` with no way out. Authorized by
 * identity alone (a valid session), never by `authorize()`'s tenant-scoped matrix - there is no
 * Organization to be a member of yet at the moment this call is made, by definition. */
export async function handleCreateOrganization(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const cookies = cookiesOf(req);
    const session = await deps.auth.resolveSession(cookies[SESSION_COOKIE_NAME]);

    if (
      !checkCsrf({
        method: req.method,
        secFetchSite: req.headers["sec-fetch-site"],
        headerToken: req.headers["x-csrf-token"],
        cookieToken: cookies[CSRF_COOKIE_NAME],
        sessionCsrfSecret: session.csrfSecret,
      })
    ) {
      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
    }

    const body = (req.body ? JSON.parse(req.body) : {}) as Record<string, unknown>;
    // D-129 (GTR-01 supersession): trimmed here too (defense in depth at the HTTP boundary,
    // consistent with other BFF handlers) - CreateOrganizationService.buildCreateEntries() is
    // still the real authority and independently rejects blank input.
    const displayName = typeof body["displayName"] === "string" ? body["displayName"].trim() : undefined;
    const timezone = typeof body["timezone"] === "string" ? body["timezone"] : undefined;
    if (!displayName || !timezone) {
      throw new ValidationError("displayName and timezone are required.");
    }

    const { organizationId } = await deps.auth.createOrganization(session, { displayName, timezone });
    return { statusCode: 201, body: { organizationId } };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** GET /bff/organizations (Wave B2B-6, D-101) - lists only effectively usable organizations
 * (Membership ACTIVE + TenantLifecycleRecord ACTIVE). Read-only, no CSRF needed (same posture as
 * `handleGetSession`). */
export async function handleListOrganizations(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const session = await deps.auth.resolveSession(cookiesOf(req)[SESSION_COOKIE_NAME]);
    const organizations = await deps.auth.listOrganizations(session.userId);
    return { statusCode: 200, body: { organizations } };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** POST /bff/organization/select (Wave B2B-6, D-101) - same CSRF/mutation discipline as
 * `handleCreateOrganization` above. Validates via `resolveWorkingOrganization()` (never trusts
 * the client-supplied `organizationId` without a real Membership + lifecycle check). */
export async function handleSelectOrganization(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const cookies = cookiesOf(req);
    const session = await deps.auth.resolveSession(cookies[SESSION_COOKIE_NAME]);

    if (
      !checkCsrf({
        method: req.method,
        secFetchSite: req.headers["sec-fetch-site"],
        headerToken: req.headers["x-csrf-token"],
        cookieToken: cookies[CSRF_COOKIE_NAME],
        sessionCsrfSecret: session.csrfSecret,
      })
    ) {
      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
    }

    const body = (req.body ? JSON.parse(req.body) : {}) as Record<string, unknown>;
    const organizationId = typeof body["organizationId"] === "string" ? body["organizationId"] : undefined;
    if (!organizationId) {
      throw new ValidationError("organizationId is required.");
    }

    const result = await deps.auth.selectOrganization(session, organizationId);
    if (!result.ok) {
      return { statusCode: 403, body: { code: "ORGANIZATION_UNAVAILABLE", category: "AUTHORIZATION", message: "This organization is not available in your current context.", retryable: false } };
    }
    return { statusCode: 204, body: {} };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** POST /bff/invitations/accept (Wave B2B-8, D-099) - identity-only, same class as
 * `handleCreateOrganization` above (no Organization to be a member of yet, by definition, when
 * the caller has no prior Membership anywhere). */
export async function handleAcceptInvitation(deps: BffHttpDeps, req: BffHttpRequest): Promise<BffHttpResponse> {
  try {
    const cookies = cookiesOf(req);
    const session = await deps.auth.resolveSession(cookies[SESSION_COOKIE_NAME]);

    if (
      !checkCsrf({
        method: req.method,
        secFetchSite: req.headers["sec-fetch-site"],
        headerToken: req.headers["x-csrf-token"],
        cookieToken: cookies[CSRF_COOKIE_NAME],
        sessionCsrfSecret: session.csrfSecret,
      })
    ) {
      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
    }

    const body = (req.body ? JSON.parse(req.body) : {}) as Record<string, unknown>;
    const token = typeof body["token"] === "string" ? body["token"] : undefined;
    if (!token) {
      throw new ValidationError("token is required.");
    }

    const { organizationId } = await deps.auth.acceptInvitation(session, { token });
    return { statusCode: 200, body: { organizationId } };
  } catch (err) {
    return toErrorResponse(err);
  }
}

/** POST/PUT/PATCH/DELETE/GET /bff/api/{proxy+} - the allowlisted forward to the real API. */
export async function handleProxy(deps: BffHttpDeps, req: BffHttpRequest, backendPath: string, queryString: string | undefined): Promise<BffHttpResponse> {
  try {
    const cookies = cookiesOf(req);

    const sessionResolveStart = Date.now();
    const session = await deps.auth.resolveSession(cookies[SESSION_COOKIE_NAME]);
    const sessionResolveMs = Date.now() - sessionResolveStart;

    if (!checkCsrf({
      method: req.method,
      secFetchSite: req.headers["sec-fetch-site"],
      headerToken: req.headers["x-csrf-token"],
      cookieToken: cookies[CSRF_COOKIE_NAME],
      sessionCsrfSecret: session.csrfSecret,
    })) {
      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
    }

    const proxyStart = Date.now();
    const result = await deps.proxy.forward(session, { method: req.method, path: backendPath, queryString, headers: req.headers, body: req.body });
    const proxyMs = Date.now() - proxyStart;

    // PERF-02: BFF-side timing for the two steps handleProxy actually performs, plus the
    // backend's response status class. Emitted right here (rather than only in
    // src/runtime/aws/handlers/*.ts, metrics.ts's usual convention) because handleProxy IS the
    // HTTP boundary for /bff/api/* - there is no intervening service layer to push this into.
    logger.info("bff proxy timing", { sessionResolveMs, proxyMs, backendStatus: result.statusCode });
    emitMetric(METRICS_NAMESPACE, { name: "bff.session_resolve_ms", value: sessionResolveMs, unit: "Milliseconds" });
    emitMetric(METRICS_NAMESPACE, { name: "bff.proxy_ms", value: proxyMs, unit: "Milliseconds" });
    emitMetric(METRICS_NAMESPACE, { name: "bff.backend_status", value: 1, unit: "Count", dimensions: { StatusClass: statusClass(result.statusCode) } });

    // G3 follow-up: not every proxied route returns JSON (the 7 CSV report routes return
    // text/csv - see proxy-allowlist.ts's own comment on those entries). JSON.parse()ing every
    // response unconditionally corrupted/500'd those - only parse when the backend actually
    // says it sent JSON; otherwise pass the body through untouched (isRawBody: true tells the
    // API Gateway adapter to skip JSON.stringify() on the way back out).
    const contentType = result.headers["content-type"];
    const isJson = contentType === undefined || contentType.startsWith("application/json");
    if (!result.body) {
      return { statusCode: result.statusCode, headers: result.headers, body: {} };
    }
    if (isJson) {
      return { statusCode: result.statusCode, headers: result.headers, body: JSON.parse(result.body) };
    }
    return { statusCode: result.statusCode, headers: result.headers, body: result.body, isRawBody: true };
  } catch (err) {
    return toErrorResponse(err);
  }
}

 succeeded in 292ms:
/**
 * Real adapter for CognitoAuthClient (D-3xx) - `@aws-sdk/client-cognito-identity-provider`
 * (already a project dependency), never a hand-rolled HTTP call: these are stateful,
 * exception-shaped Cognito APIs (unlike the plain OAuth2 REST endpoints
 * fetch-cognito-oidc-client.ts calls), so the official SDK's typed commands/exceptions are the
 * right tool, not a reason to reimplement anything cryptographic.
 */
import { createHmac } from "node:crypto";
import {
  CognitoIdentityProviderClient,
  InitiateAuthCommand,
  SignUpCommand,
  ConfirmSignUpCommand,
  ResendConfirmationCodeCommand,
  ForgotPasswordCommand,
  ConfirmForgotPasswordCommand,
  NotAuthorizedException,
  UserNotFoundException,
  UserNotConfirmedException,
  PasswordResetRequiredException,
  UsernameExistsException,
  InvalidPasswordException,
  InvalidParameterException,
  CodeMismatchException,
  ExpiredCodeException,
} from "@aws-sdk/client-cognito-identity-provider";
import type {
  CognitoAuthClient,
  CognitoAuthenticateOutcome,
  CognitoConfirmForgotPasswordOutcome,
  CognitoConfirmSignUpOutcome,
  CognitoForgotPasswordOutcome,
  CognitoResendConfirmationOutcome,
  CognitoSignUpOutcome,
} from "../ports/cognito-auth-client.js";

/** Cognito's own documented algorithm for a confidential (secret-bearing) app client's
 * SECRET_HASH parameter - HMAC-SHA256(key = client secret, message = username + client id),
 * base64-encoded. Not a custom scheme: this is the exact, publicly documented computation
 * every Cognito SDK/CLI caller with a client secret must perform; `node:crypto`'s HMAC is the
 * same primitive `kms-token-encryptor.ts`/`opaque-token.ts` already lean on elsewhere in this
 * module for HMAC-based work. */
function secretHash(username: string, clientId: string, clientSecret: string): string {
  return createHmac("sha256", clientSecret).update(username + clientId).digest("base64");
}

/**
 * Best-effort match on Cognito's own (undocumented-as-contract) exception message text for "this
 * account is already confirmed" - round-1 Codex finding (`docs/architecture/reviews/
 * d321-direct-auth-adversarial-review/`): `confirmSignUp`/`resendConfirmationCode` used to treat
 * ANY `NotAuthorizedException`/`InvalidParameterException` as ALREADY_CONFIRMED, which could
 * silently report success for an unrelated authorization/parameter failure. Message text is not
 * part of Cognito's documented API contract (only the exception TYPE is), so this is deliberately
 * a narrowing, defense-in-depth check, never the only thing standing between a real failure and a
 * false success - a message that doesn't match falls through to `UNKNOWN_OUTCOME` (mapped to a
 * 503 by `BffAuthService`), never a silently assumed success.
 */
function looksAlreadyConfirmed(message: string | undefined): boolean {
  return /already confirmed|current status is confirmed/i.test(message ?? "");
}

function isTransient(err: unknown): boolean {
  // Anything that isn't one of the specific, expected Cognito exceptions below is treated as
  // transient/unknown rather than silently swallowed - a thrown network error, throttling, or
  // an AWS-side 5xx all fall here.
  return !(
    err instanceof NotAuthorizedException ||
    err instanceof UserNotFoundException ||
    err instanceof UserNotConfirmedException ||
    err instanceof PasswordResetRequiredException ||
    err instanceof UsernameExistsException ||
    err instanceof InvalidPasswordException ||
    err instanceof InvalidParameterException ||
    err instanceof CodeMismatchException ||
    err instanceof ExpiredCodeException
  );
}

export class CognitoIdpAuthClient implements CognitoAuthClient {
  constructor(
    private readonly client: CognitoIdentityProviderClient,
    private readonly clientId: string,
    private readonly clientSecret: string,
  ) {}

  private hash(username: string): string {
    return secretHash(username, this.clientId, this.clientSecret);
  }

  async authenticateWithPassword(input: { username: string; password: string }): Promise<CognitoAuthenticateOutcome> {
    try {
      const result = await this.client.send(
        new InitiateAuthCommand({
          AuthFlow: "USER_PASSWORD_AUTH",
          ClientId: this.clientId,
          AuthParameters: { USERNAME: input.username, PASSWORD: input.password, SECRET_HASH: this.hash(input.username) },
        }),
      );
      if (result.ChallengeName) {
        return { kind: "UNSUPPORTED_CHALLENGE", challengeName: result.ChallengeName };
      }
      const auth = result.AuthenticationResult;
      if (!auth?.AccessToken || !auth.IdToken || !auth.RefreshToken || auth.ExpiresIn === undefined) {
        return { kind: "UNKNOWN_OUTCOME" };
      }
      return {
        kind: "SUCCESS",
        tokens: { accessToken: auth.AccessToken, idToken: auth.IdToken, refreshToken: auth.RefreshToken, expiresInSeconds: auth.ExpiresIn },
      };
    } catch (err) {
      // NotAuthorizedException and UserNotFoundException are folded into the SAME outcome
      // (INVALID_CREDENTIALS) - Cognito's `prevent_user_existence_errors = "ENABLED"` already
      // makes this the effective behavior in practice (UserNotFoundException should rarely
      // surface at all for InitiateAuth under that setting), but the fold happens here too so
      // this adapter can never accidentally leak the distinction even if that ever changes.
      if (err instanceof NotAuthorizedException || err instanceof UserNotFoundException) return { kind: "INVALID_CREDENTIALS" };
      if (err instanceof UserNotConfirmedException) return { kind: "USER_NOT_CONFIRMED" };
      if (err instanceof PasswordResetRequiredException) return { kind: "PASSWORD_RESET_REQUIRED" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async signUp(input: { username: string; password: string; name: string }): Promise<CognitoSignUpOutcome> {
    try {
      await this.client.send(
        new SignUpCommand({
          ClientId: this.clientId,
          Username: input.username,
          Password: input.password,
          SecretHash: this.hash(input.username),
          // `name` (standard Cognito attribute, no schema/read_attributes change needed - see
          // infra/modules/cognito/main.tf) rides the ID token's own `name` claim on first login,
          // which `AwsJwtIdTokenVerifier.verify()` already reads generically for every auth path
          // (direct password login included, not just OIDC) - `BootstrapIdentityService` then
          // just works, no other backend change required for `GlobalUser.displayName`.
          UserAttributes: [
            { Name: "email", Value: input.username },
            { Name: "name", Value: input.name },
          ],
        }),
      );
      return { kind: "CONFIRMATION_REQUIRED" };
    } catch (err) {
      // UsernameExistsException is NOT anti-enumeration-collapsed (unlike every other outcome
      // in this file) - SignUp itself has always had to tell a real user "this e-mail is
      // already registered" for basic usability (the Hosted UI's own signup form did too); this
      // is not a new information leak introduced by this adapter.
      if (err instanceof UsernameExistsException) return { kind: "EMAIL_ALREADY_REGISTERED" };
      if (err instanceof InvalidPasswordException) return { kind: "INVALID_PASSWORD" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async confirmSignUp(input: { username: string; confirmationCode: string }): Promise<CognitoConfirmSignUpOutcome> {
    try {
      await this.client.send(
        new ConfirmSignUpCommand({ ClientId: this.clientId, Username: input.username, ConfirmationCode: input.confirmationCode, SecretHash: this.hash(input.username) }),
      );
      return { kind: "SUCCESS" };
    } catch (err) {
      if (err instanceof CodeMismatchException || err instanceof ExpiredCodeException) return { kind: "INVALID_CODE_OR_EXPIRED" };
      // Only the documented "already confirmed" message maps to that outcome - ANY other
      // NotAuthorizedException (app client misconfiguration, unexpected auth failure) used to
      // be folded into the same false success (round-1 Codex finding).
      if (err instanceof NotAuthorizedException && looksAlreadyConfirmed(err.message)) return { kind: "ALREADY_CONFIRMED" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async resendConfirmationCode(input: { username: string }): Promise<CognitoResendConfirmationOutcome> {
    try {
      await this.client.send(new ResendConfirmationCodeCommand({ ClientId: this.clientId, Username: input.username, SecretHash: this.hash(input.username) }));
      return { kind: "SENT" };
    } catch (err) {
      // Same narrowing as confirmSignUp above (round-1 Codex finding) - only the documented
      // "already confirmed" message maps to that outcome, never every InvalidParameterException.
      if (err instanceof InvalidParameterException && looksAlreadyConfirmed(err.message)) return { kind: "ALREADY_CONFIRMED" };
      // Anti-enumeration (decision 3), same fold as forgotPassword() below - a nonexistent
      // e-mail must resolve identically to a real, unconfirmed one from the caller's point of
      // view. Without this, UserNotFoundException would fall through to UNKNOWN_OUTCOME below
      // and BffAuthService.resendConfirmationCode would surface it as a distinguishable error.
      if (err instanceof UserNotFoundException) return { kind: "SENT" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async forgotPassword(input: { username: string }): Promise<CognitoForgotPasswordOutcome> {
    try {
      await this.client.send(new ForgotPasswordCommand({ ClientId: this.clientId, Username: input.username, SecretHash: this.hash(input.username) }));
      return { kind: "SUCCESS" };
    } catch (err) {
      // UserNotFoundException is swallowed into SUCCESS on purpose (D-3xx decision 3) - under
      // `prevent_user_existence_errors = "ENABLED"` Cognito itself normally never throws this
      // for ForgotPassword in the first place, but this adapter does not rely on that alone.
      // InvalidParameterException is folded the same way (round-1 Codex finding, AWS's own docs
      // on error suppression) - a user with no verified recovery attribute (email/phone) also
      // throws this, which would otherwise create a 3-way oracle (real send=202, no-such-user=
      // 202, "exists but nothing to send to"=503) distinguishable without any timing measurement.
      if (err instanceof UserNotFoundException || err instanceof InvalidParameterException) return { kind: "SUCCESS" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async confirmForgotPassword(input: { username: string; confirmationCode: string; newPassword: string }): Promise<CognitoConfirmForgotPasswordOutcome> {
    try {
      await this.client.send(
        new ConfirmForgotPasswordCommand({
          ClientId: this.clientId,
          Username: input.username,
          ConfirmationCode: input.confirmationCode,
          Password: input.newPassword,
          SecretHash: this.hash(input.username),
        }),
      );
      return { kind: "SUCCESS" };
    } catch (err) {
      if (err instanceof CodeMismatchException || err instanceof ExpiredCodeException) return { kind: "INVALID_CODE_OR_EXPIRED" };
      if (err instanceof UserNotFoundException) return { kind: "INVALID_CODE_OR_EXPIRED" }; // never distinguish from a bad code (anti-enumeration)
      if (err instanceof InvalidPasswordException) return { kind: "INVALID_PASSWORD" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }
}

export function createCognitoIdpClient(): CognitoIdentityProviderClient {
  return new CognitoIdentityProviderClient({});
}
/**
 * BffAuthService — the Full BFF's core (D-053/D-054, docs/architecture/reviews/
 * bff-full-vs-session-design/). The browser only ever holds opaque cookies; every Cognito
 * token (access/ID/refresh) lives here, server-side, in the dedicated session table.
 *
 * Reuses src/modules/identity as a library (RequestContextResolver's constituent repos), the
 * same way src/workers/reminder-producer imports src/modules/reminder in-process - no new
 * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
 */
import { randomUUID } from "node:crypto";
import { AuthenticationError, ConflictError, DependencyUnavailableError, ValidationError } from "../../../shared/errors/app-error.js";
import { getCancellationReasonCodes, isTransactionCanceled, buildAttributeOnceUpdate, type TransactWriteEntry } from "../../../shared/dynamodb/occ.js";
import { IdentityBootstrapService } from "../../identity/application/bootstrap-identity.js";
import { GlobalUserRepository, deviceSessionKey, globalUserKey } from "../../identity/persistence/global-user-repository.js";
import { OnboardingStateResolver, type OnboardingState } from "../../organization/application/onboarding-state.js";
import { listUsableOrganizations, type UsableOrganization } from "../../organization/application/resolve-active-membership.js";
import { resolveWorkingOrganization } from "../../organization/application/resolve-working-organization.js";
import { CreateOrganizationService } from "../../organization/application/create-organization.js";
import { AcceptInvitationService } from "../../organization/application/accept-invitation.js";
import type { OrganizationStore } from "../../organization/ports/organization-store.js";
import type { SessionStore } from "../ports/session-store.js";
import type { CognitoOidcClient, IdTokenVerifier } from "../ports/cognito-oidc-client.js";
import type { CognitoAuthClient, CognitoAuthTokens } from "../ports/cognito-auth-client.js";
import type { TokenEncryptor } from "../ports/token-encryptor.js";
import { issueOpaqueToken, parseOpaqueToken, opaqueTokenSecretMatches, hmacTokenCrypto } from "../domain/opaque-token.js";
import { issueCsrfSecret } from "../domain/csrf.js";
import {
  sessionKey,
  loginAttemptKey,
  type Session,
  type LoginAttempt,
} from "../domain/session.js";
import { SESSION_ABSOLUTE_TTL_SECONDS, SESSION_IDLE_TTL_SECONDS, SESSION_IDLE_RENEWAL_THRESHOLD_SECONDS, LOGIN_ATTEMPT_TTL_SECONDS } from "../domain/cookies.js";
import type { RefreshOutcome } from "../domain/refresh-outcome.js";

export interface BffAuthServiceDeps {
  sessionStore: SessionStore;
  cognitoClient: CognitoOidcClient;
  /** D-3xx (reversal of D-320's Managed Login decision): direct-auth port (InitiateAuth/
   * SignUp/ForgotPassword family) backing the app's own login/signup/reset-password screens.
   * `cognitoClient` above stays untouched - it still serves refresh/revoke (and the dormant
   * OIDC/PKCE fallback, handleCallback below). */
  cognitoAuthClient: CognitoAuthClient;
  idTokenVerifier: IdTokenVerifier;
  tokenEncryptor: TokenEncryptor;
  /** Wave B2B-5 (D-095): renamed from `TenantBootstrapService` — no longer creates any
   * tenant/Organization at login, just the global identity (2-item transact). Both login paths
   * still bootstrap through this single atomic service (D-087/B2B-2). */
  bootstrap: IdentityBootstrapService;
  /** Wave B2B-5 (D-095): device sessions and global-logout revocation moved here from
   * `UserRepository`/`UserProfile` (removed entirely, D-160 — zero real reader) — both are
   * properties of the tenant-independent identity (physical model §10). */
  globalUsers: GlobalUserRepository;
  organizations: OrganizationStore;
  mainTableName: string;
  createOrganization: CreateOrganizationService;
  /** Wave B2B-8 (D-099): identity-only route, same class as `createOrganization` above - no
   * tenant exists yet at the moment `POST /bff/invitations/accept` is called. */
  acceptInvitation: AcceptInvitationService;
  pepper: string;
  redirectUri: string;
  authorizeUrl: string; // Cognito Hosted UI /oauth2/authorize base URL (from Terraform output/env, not hardcoded)
  clientId: string;
  now: () => string;
  newUserId: () => string;
  newDeviceId: () => string;
}

export interface SessionWithOnboarding {
  session: Session;
  activeOrganizationId?: string;
  onboardingState?: OnboardingState;
  /** Wave B2B-6 (D-101): mutually exclusive with the 2 fields above - the user has more than
   * one usable Membership (ACTIVE + its Organization's own TenantLifecycleRecord ACTIVE) and no
   * valid selection is on the session yet. Explicit, typed contract (not a reused/overloaded
   * meaning of `onboardingState`, which `OnboardingStateResolver` never evaluates lifecycle
   * for - achado da Rodada 1 do Codex). */
  organizationSelectionRequired?: { organizations: UsableOrganization[] };
  /** #15/sidebar identity card (2026-09-21): resolved from `GlobalUser`, same "absent means no
   * name/email on file" rule as everywhere else this data surfaces - never `session.userId`
   * itself, which stays excluded from every session-shaped response by design (D-095/D-096). */
  displayName?: string;
  email?: string;
}

export interface StartedLogin {
  loginToken: string;
  redirectUrl: string;
}

/** Allowlisted app paths only - never an open redirect (D-053/D-054 never proposed a
 * `returnTo` mechanism; this validation is new and mandatory precisely because it wasn't
 * already decided - an unvalidated returnTo would be a classic open-redirect vector). */
function isValidReturnPath(path: string): boolean {
  return path.startsWith("/") && !path.startsWith("//") && !path.includes("://");
}

export class BffAuthService {
  private readonly onboarding: OnboardingStateResolver;

  constructor(private readonly deps: BffAuthServiceDeps) {
    this.onboarding = new OnboardingStateResolver(deps.organizations);
  }

  /** Step 1 of the OIDC flow: mint a LoginAttempt (state/nonce/PKCE verifier), return the
   * Cognito Hosted UI URL to redirect the browser to, and the opaque login-cookie value. */
  async startLogin(returnTo: string): Promise<StartedLogin> {
    const safeReturnTo = isValidReturnPath(returnTo) ? returnTo : "/";
    const issued = issueOpaqueToken(this.deps.pepper);
    const state = randomUUID();
    const nonce = randomUUID();
    const codeVerifier = randomUUID() + randomUUID(); // >=43 chars required by PKCE spec (RFC 7636)
    const codeChallenge = await pkceChallenge(codeVerifier);

    const now = this.deps.now();
    const attempt: LoginAttempt = {
      ...loginAttemptKey(issued.selectorHash),
      SK: "POINTER",
      entityType: "LoginAttempt",
      selectorHash: issued.selectorHash,
      secretHash: issued.secretHash,
      state,
      nonce,
      codeVerifier,
      returnTo: safeReturnTo,
      purgeAfterTtl: epochSeconds(now) + LOGIN_ATTEMPT_TTL_SECONDS,
      createdAt: now,
      version: 1,
    };
    const created = await this.deps.sessionStore.putIfAbsent(attempt);
    if (!created) {
      // selectorHash collision is astronomically unlikely (128 bits of entropy) - if it
      // ever happens, fail closed rather than silently overwrite another attempt in flight.
      throw new ConflictError("Could not start a new login attempt.");
    }

    const params = new URLSearchParams({
      client_id: this.deps.clientId,
      response_type: "code",
      redirect_uri: this.deps.redirectUri,
      state,
      nonce,
      code_challenge: codeChallenge,
      code_challenge_method: "S256",
      // matches infra/modules/cognito's allowed_oauth_scopes - "email"/"profile" are what
      // populate idClaims.email/idClaims.name below (#15, 2026-09-21).
      scope: "openid email profile",
    });

    return { loginToken: issued.token, redirectUrl: `${this.deps.authorizeUrl}?${params.toString()}` };
  }

  /** Step 2: the Cognito redirect back to our callback route. Validates state (against the
   * LoginAttempt this exact login cookie points to - not just "some" attempt), exchanges the
   * code via PKCE, verifies the ID token's signature/issuer/audience/nonce, resolves the
   * tenant/user (reusing identity module first-login logic), and mints the real session. */
  async handleCallback(input: { loginCookie: string | undefined; code: string; state: string }): Promise<{ sessionToken: string; csrfToken: string; returnTo: string }> {
    if (!input.loginCookie) throw new AuthenticationError("Missing login session.");
    const parsed = parseOpaqueToken(input.loginCookie);
    if (!parsed) throw new AuthenticationError("Malformed login session.");

    const attempt = await this.deps.sessionStore.get<LoginAttempt>(loginAttemptKey(hmacTokenCrypto.hash(this.deps.pepper, parsed.selector)));
    if (!attempt || attempt.entityType !== "LoginAttempt" || attempt.consumedAt) {
      throw new AuthenticationError("Login session not found or already used.");
    }
    if (!opaqueTokenSecretMatches(this.deps.pepper, parsed.secret, attempt.secretHash)) {
      throw new AuthenticationError("Invalid login session.");
    }
    // DynamoDB TTL deletion is best-effort and can lag well past the item's TTL timestamp
    // (found in Round D re-verification) - a short-lived, single-use auth object like this
    // must never rely on that alone to enforce its own window; check the timestamp directly.
    if (epochSeconds(this.deps.now()) > attempt.purgeAfterTtl) {
      throw new AuthenticationError("Login session expired.");
    }
    // Single-use: consume immediately, before the Cognito round trip, so a duplicate/retried
    // callback (double-click, browser back button) can never replay the same code twice
    // against a still-valid LoginAttempt. Conditional on `version` (not a plain update) -
    // two concurrent callbacks could otherwise both read consumedAt=undefined and both
    // proceed before either write landed (found in review); only one wins this race.
    const consumed = await this.deps.sessionStore.updateConditional<LoginAttempt>(
      { ...attempt, consumedAt: this.deps.now(), version: attempt.version + 1 },
      { version: attempt.version },
    );
    if (!consumed) {
      throw new AuthenticationError("Login session not found or already used.");
    }

    if (input.state !== attempt.state) {
      throw new AuthenticationError("State mismatch - possible CSRF on the OIDC callback itself.");
    }

    const tokens = await this.deps.cognitoClient.exchangeAuthorizationCode({
      code: input.code,
      codeVerifier: attempt.codeVerifier,
      redirectUri: this.deps.redirectUri,
    });

    const idClaims = await this.deps.idTokenVerifier.verify(tokens.idToken, attempt.nonce);
    const { sessionToken, csrfToken } = await this.establishSession(tokens, idClaims);
    return { sessionToken, csrfToken, returnTo: attempt.returnTo };
  }

  /** D-3xx: direct-auth login (`POST /bff/login`), replacing the Hosted UI redirect as the
   * frontend's primary entry point (D-320 reversed - see decisions-log.md). Cognito's own
   * `prevent_user_existence_errors = "ENABLED"` already collapses "no such user" and "wrong
   * password" into one indistinguishable outcome (`INVALID_CREDENTIALS`) - this method preserves
   * that by throwing the exact same generic `AuthenticationError` for it, `USER_NOT_CONFIRMED`,
   * `PASSWORD_RESET_REQUIRED` and `UNSUPPORTED_CHALLENGE` alike, never a message that would let a
   * caller distinguish "this account doesn't exist" from "this account exists but locked". */
  async loginWithPassword(input: { email: string; password: string }): Promise<{ sessionToken: string; csrfToken: string }> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.authenticateWithPassword({ username, password: input.password });
    // Round-1 Codex finding (d321-direct-auth-adversarial-review): TRANSIENT_FAILURE/
    // UNKNOWN_OUTCOME used to fold into the SAME "invalid credentials" as a real wrong password -
    // a genuine Cognito outage/throttling would falsely tell the user they mistyped their
    // password. Mapped to DependencyUnavailableError instead, same posture every other method in
    // this file already uses - this reveals nothing about account existence, only "the system
    // could not complete this request right now", which is not an anti-enumeration regression.
    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
      throw new DependencyUnavailableError("Não foi possível autenticar agora - tente novamente.");
    }
    if (outcome.kind !== "SUCCESS") {
      throw new AuthenticationError("E-mail ou senha inválidos.");
    }

    // Direct-auth ID token was never minted against an `/oauth2/authorize` nonce (undefined is
    // the documented "skip nonce check" signal - see IdTokenVerifier.verify's own doc comment).
    const idClaims = await this.deps.idTokenVerifier.verify(outcome.tokens.idToken, undefined);
    return this.establishSession(outcome.tokens, idClaims);
  }

  /** D-3xx: self-service signup (`POST /bff/signup`) - the same path the Hosted UI's own "Sign
   * up" link exercised (the pool has no `admin_create_user_config` restricting registration to
   * admins, `infra/modules/cognito/main.tf` - self-service signup was already live, this is
   * parity, not a new capability). `auto_verified_attributes = ["email"]` means Cognito always
   * requires the confirmation-code step below before the account can log in. */
  async signUp(input: { email: string; password: string; name: string }): Promise<{ status: "CONFIRMATION_REQUIRED" }> {
    const username = input.email.trim().toLowerCase();
    const name = input.name.trim();
    if (!name) {
      throw new ValidationError("O nome é obrigatório.");
    }
    const outcome = await this.deps.cognitoAuthClient.signUp({ username, password: input.password, name });
    if (outcome.kind === "EMAIL_ALREADY_REGISTERED") {
      throw new ConflictError("Já existe uma conta com este e-mail.");
    }
    if (outcome.kind === "INVALID_PASSWORD") {
      throw new ValidationError("A senha não atende aos requisitos mínimos de segurança.");
    }
    if (outcome.kind !== "CONFIRMATION_REQUIRED") {
      throw new DependencyUnavailableError("Não foi possível criar a conta - tente novamente.");
    }
    return { status: "CONFIRMATION_REQUIRED" };
  }

  /** D-3xx: `POST /bff/signup/confirm` - the e-mail verification step Cognito's
   * `auto_verified_attributes` requires before a freshly self-registered account can log in. */
  async confirmSignUp(input: { email: string; confirmationCode: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.confirmSignUp({ username, confirmationCode: input.confirmationCode });
    if (outcome.kind === "INVALID_CODE_OR_EXPIRED") {
      throw new ValidationError("Código inválido ou expirado.");
    }
    if (outcome.kind === "ALREADY_CONFIRMED") {
      // Idempotent from the caller's point of view - the account is confirmed either way, which
      // is exactly what the caller wanted, so this is not surfaced as an error.
      return;
    }
    if (outcome.kind !== "SUCCESS") {
      throw new DependencyUnavailableError("Não foi possível confirmar o e-mail - tente novamente.");
    }
  }

  /** D-3xx: `POST /bff/signup/resend` - lets a user whose confirmation code expired get a new
   * one without restarting the whole signup form. */
  async resendConfirmationCode(input: { email: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.resendConfirmationCode({ username });
    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
      throw new DependencyUnavailableError("Não foi possível reenviar o código - tente novamente.");
    }
    // SENT or ALREADY_CONFIRMED: both are a benign no-op from the caller's perspective (a
    // confirmed account never needs the code anyway) - anti-enumeration posture (decision 3):
    // never let the response distinguish "this e-mail isn't registered" from either of those.
  }

  /** D-3xx: `POST /bff/forgot-password` - always resolves the same way regardless of whether
   * the e-mail is registered (Cognito's own anti-enumeration posture under
   * `prevent_user_existence_errors = "ENABLED"`, preserved by CognitoIdpAuthClient.
   * forgotPassword folding UserNotFoundException into SUCCESS too - belt and suspenders). Only a
   * genuine transient/unknown failure surfaces differently, and even then never says why. */
  async startForgotPassword(input: { email: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.forgotPassword({ username });
    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
      throw new DependencyUnavailableError("Não foi possível iniciar a redefinição de senha - tente novamente.");
    }
  }

  /** D-3xx: `POST /bff/forgot-password/confirm` - the code+new-password step. */
  async confirmForgotPassword(input: { email: string; confirmationCode: string; newPassword: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.confirmForgotPassword({ username, confirmationCode: input.confirmationCode, newPassword: input.newPassword });
    if (outcome.kind === "INVALID_CODE_OR_EXPIRED") {
      throw new ValidationError("Código inválido ou expirado.");
    }
    if (outcome.kind === "INVALID_PASSWORD") {
      throw new ValidationError("A nova senha não atende aos requisitos mínimos de segurança.");
    }
    if (outcome.kind !== "SUCCESS") {
      throw new DependencyUnavailableError("Não foi possível redefinir a senha - tente novamente.");
    }
  }

  /** Shared by `handleCallback` (OIDC/PKCE, dormant fallback) and `loginWithPassword` (D-3xx,
   * the real entry point now) - both end with an already-obtained token set and verified ID
   * claims, and both need the exact same bootstrap/device-session/session-creation sequence.
   * Extracted here (was duplicated inline in handleCallback before D-3xx) so the two callers can
   * never drift apart on how a session actually gets created. */
  private async establishSession(
    tokens: CognitoAuthTokens,
    idClaims: { subject: string; email?: string; name?: string },
  ): Promise<{ sessionToken: string; csrfToken: string }> {
    // Access token claims are decoded (not independently re-verified) - it was obtained via a
    // direct server-to-server call to Cognito's token endpoint over TLS with client
    // credentials, the same trusted backchannel that just produced a signature-verified ID
    // token for the identical login; a second JWKS round trip would add latency without a
    // corresponding security gain. Never do this for a token that arrived via the browser.
    const accessClaims = decodeJwtPayloadUnverified(tokens.accessToken);

    // Wave B2B-5 (D-095): newUserId() only matters on first-ever login for this cognitoSub
    // (IdentityBootstrapService ignores it on repeat logins) - authentication no longer creates
    // any tenant, just the global identity (2-item transact: GlobalUser + IdentityMapping).
    const newUserId = this.deps.newUserId();
    const { mapping, user } = await this.deps.bootstrap.bootstrapUser(idClaims.subject, newUserId, (idClaims.email ?? "").toLowerCase(), idClaims.name);
    if (user.identityStatus !== "ACTIVE") {
      throw new AuthenticationError("User is not active.");
    }

    const deviceId = this.deps.newDeviceId();
    await this.deps.globalUsers.upsertDeviceSession({
      ...deviceSessionKey(mapping.userId, deviceId),
      entityType: "DeviceSession",
      userId: mapping.userId,
      deviceId,
      sessionId: randomUUID(),
      refreshFamilyId: randomUUID(),
      createdAt: this.deps.now(),
      lastSeenAt: this.deps.now(),
      expiresAt: new Date(Date.parse(this.deps.now()) + SESSION_ABSOLUTE_TTL_SECONDS * 1000).toISOString(),
      status: "ACTIVE",
    });

    // Sessão sempre criada aqui, nunca falha por falta de Organization (D-095 mudança E) -
    // activeOrganizationId fica ausente se este usuário ainda não tem Membership utilizável;
    // GET /bff/session reporta onboardingState nesse caso (resolveSessionWithOnboarding).
    const activeOrganizationId = await this.deriveActiveOrganizationId(mapping.userId);

    const issuedSession = issueOpaqueToken(this.deps.pepper);
    const csrfSecret = issueCsrfSecret();
    const now = this.deps.now();
    const absoluteExpiresAt = new Date(Date.parse(now) + SESSION_ABSOLUTE_TTL_SECONDS * 1000).toISOString();
    const session: Session = {
      ...sessionKey(issuedSession.selectorHash),
      SK: "POINTER",
      entityType: "Session",
      selectorHash: issuedSession.selectorHash,
      secretHash: issuedSession.secretHash,
      activeOrganizationId,
      userId: mapping.userId,
      cognitoSubject: idClaims.subject,
      deviceId,
      csrfSecret,
      encryptedRefreshToken: await this.deps.tokenEncryptor.encrypt(tokens.refreshToken),
      accessToken: tokens.accessToken,
      accessTokenExpiresAt: new Date(Date.parse(now) + tokens.expiresInSeconds * 1000).toISOString(),
      absoluteExpiresAt,
      purgeAfterTtl: epochSeconds(now) + SESSION_IDLE_TTL_SECONDS,
      refreshState: "IDLE",
      createdAt: now,
      updatedAt: now,
      version: 1,
    };
    const created = await this.deps.sessionStore.putIfAbsent(session);
    if (!created) {
      throw new ConflictError("Could not create session.");
    }

    void accessClaims; // decoded for future use (e.g. audit logging tokenId); not persisted raw
    return { sessionToken: issuedSession.token, csrfToken: csrfSecret };
  }

  /** Resolves a session cookie to its record, refreshing the cached access token first if it
   * is close to expiry. Never resolves a session past its absolute or idle expiry. */
  async resolveSession(sessionCookie: string | undefined): Promise<Session> {
    if (!sessionCookie) throw new AuthenticationError("No session.");
    const parsed = parseOpaqueToken(sessionCookie);
    if (!parsed) throw new AuthenticationError("Malformed session.");
    const selectorHash = hmacTokenCrypto.hash(this.deps.pepper, parsed.selector);

    let session = await this.deps.sessionStore.get<Session>(sessionKey(selectorHash));
    if (!session || session.entityType !== "Session" || session.revokedAt) {
      throw new AuthenticationError("Session not found.");
    }
    if (!opaqueTokenSecretMatches(this.deps.pepper, parsed.secret, session.secretHash)) {
      throw new AuthenticationError("Invalid session.");
    }
    const now = this.deps.now();    // trigger a global logout of someone else's account.   * `>1` usable organizations both leave it `undefined`, benignly - unlike the resource-side
   * resolver's own fail-closed errors, a BFF session hint that stays unset just means the next
   * `GET /bff/session` reports `onboardingState`/`organizationSelectionRequired` instead. */
  private async deriveActiveOrganizationId(userId: string): Promise<string | undefined> {
    const usable = await listUsableOrganizations(this.deps.organizations, userId);
    if (usable.length !== 1) return undefined;
    return usable[0]?.organizationId;
  }
}

function epochSeconds(iso: string): number {
  return Math.floor(Date.parse(iso) / 1000);
}

function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

/** RFC 7636 S256 code_challenge = BASE64URL(SHA256(code_verifier)). */
async function pkceChallenge(verifier: string): Promise<string> {
  const { createHash } = await import("node:crypto");
  return createHash("sha256").update(verifier).digest("base64url");
}

/** Decodes (does NOT verify) a JWT's payload segment - only ever used on a token obtained
 * directly from Cognito's token endpoint over a trusted backchannel, never on a token that
 * arrived via the browser. */
function decodeJwtPayloadUnverified(jwt: string): Record<string, unknown> {
  const parts = jwt.split(".");
  if (parts.length !== 3 || !parts[1]) {
    throw new ValidationError("Malformed JWT.");
  }
  return JSON.parse(Buffer.from(parts[1], "base64url").toString("utf-8")) as Record<string, unknown>;
}

web search: site.docs.aws.amazon.com cognito token endpoint remembered devices InitiateAuth ...
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 src/modules/bff/persistence/cognito-idp-auth-client.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 87ms:
/**
 * Real adapter for CognitoAuthClient (D-3xx) - `@aws-sdk/client-cognito-identity-provider`
 * (already a project dependency), never a hand-rolled HTTP call: these are stateful,
 * exception-shaped Cognito APIs (unlike the plain OAuth2 REST endpoints
 * fetch-cognito-oidc-client.ts calls), so the official SDK's typed commands/exceptions are the
 * right tool, not a reason to reimplement anything cryptographic.
 */
import { createHmac } from "node:crypto";
import {
  CognitoIdentityProviderClient,
  InitiateAuthCommand,
  SignUpCommand,
  ConfirmSignUpCommand,
  ResendConfirmationCodeCommand,
  ForgotPasswordCommand,
  ConfirmForgotPasswordCommand,
  NotAuthorizedException,
  UserNotFoundException,
  UserNotConfirmedException,
  PasswordResetRequiredException,
  UsernameExistsException,
  InvalidPasswordException,
  InvalidParameterException,
  CodeMismatchException,
  ExpiredCodeException,
} from "@aws-sdk/client-cognito-identity-provider";
import type {
  CognitoAuthClient,
  CognitoAuthenticateOutcome,
  CognitoConfirmForgotPasswordOutcome,
  CognitoConfirmSignUpOutcome,
  CognitoForgotPasswordOutcome,
  CognitoResendConfirmationOutcome,
  CognitoSignUpOutcome,
} from "../ports/cognito-auth-client.js";

/** Cognito's own documented algorithm for a confidential (secret-bearing) app client's
 * SECRET_HASH parameter - HMAC-SHA256(key = client secret, message = username + client id),
 * base64-encoded. Not a custom scheme: this is the exact, publicly documented computation
 * every Cognito SDK/CLI caller with a client secret must perform; `node:crypto`'s HMAC is the
 * same primitive `kms-token-encryptor.ts`/`opaque-token.ts` already lean on elsewhere in this
 * module for HMAC-based work. */
function secretHash(username: string, clientId: string, clientSecret: string): string {
  return createHmac("sha256", clientSecret).update(username + clientId).digest("base64");
}

/**
 * Best-effort match on Cognito's own (undocumented-as-contract) exception message text for "this
 * account is already confirmed" - round-1 Codex finding (`docs/architecture/reviews/
 * d321-direct-auth-adversarial-review/`): `confirmSignUp`/`resendConfirmationCode` used to treat
 * ANY `NotAuthorizedException`/`InvalidParameterException` as ALREADY_CONFIRMED, which could
 * silently report success for an unrelated authorization/parameter failure. Message text is not
 * part of Cognito's documented API contract (only the exception TYPE is), so this is deliberately
 * a narrowing, defense-in-depth check, never the only thing standing between a real failure and a
 * false success - a message that doesn't match falls through to `UNKNOWN_OUTCOME` (mapped to a
 * 503 by `BffAuthService`), never a silently assumed success.
 */
function looksAlreadyConfirmed(message: string | undefined): boolean {
  return /already confirmed|current status is confirmed/i.test(message ?? "");
}

function isTransient(err: unknown): boolean {
  // Anything that isn't one of the specific, expected Cognito exceptions below is treated as
  // transient/unknown rather than silently swallowed - a thrown network error, throttling, or
  // an AWS-side 5xx all fall here.
  return !(
    err instanceof NotAuthorizedException ||
    err instanceof UserNotFoundException ||
    err instanceof UserNotConfirmedException ||
    err instanceof PasswordResetRequiredException ||
    err instanceof UsernameExistsException ||
    err instanceof InvalidPasswordException ||
    err instanceof InvalidParameterException ||
    err instanceof CodeMismatchException ||
    err instanceof ExpiredCodeException
  );
}

export class CognitoIdpAuthClient implements CognitoAuthClient {
  constructor(
    private readonly client: CognitoIdentityProviderClient,
    private readonly clientId: string,
    private readonly clientSecret: string,
  ) {}

  private hash(username: string): string {
    return secretHash(username, this.clientId, this.clientSecret);
  }

  async authenticateWithPassword(input: { username: string; password: string }): Promise<CognitoAuthenticateOutcome> {
    try {
      const result = await this.client.send(
        new InitiateAuthCommand({
          AuthFlow: "USER_PASSWORD_AUTH",
          ClientId: this.clientId,
          AuthParameters: { USERNAME: input.username, PASSWORD: input.password, SECRET_HASH: this.hash(input.username) },
        }),
      );
      if (result.ChallengeName) {
        return { kind: "UNSUPPORTED_CHALLENGE", challengeName: result.ChallengeName };
      }
      const auth = result.AuthenticationResult;
      if (!auth?.AccessToken || !auth.IdToken || !auth.RefreshToken || auth.ExpiresIn === undefined) {
        return { kind: "UNKNOWN_OUTCOME" };
      }
      return {
        kind: "SUCCESS",
        tokens: { accessToken: auth.AccessToken, idToken: auth.IdToken, refreshToken: auth.RefreshToken, expiresInSeconds: auth.ExpiresIn },
      };
    } catch (err) {
      // NotAuthorizedException and UserNotFoundException are folded into the SAME outcome
      // (INVALID_CREDENTIALS) - Cognito's `prevent_user_existence_errors = "ENABLED"` already
      // makes this the effective behavior in practice (UserNotFoundException should rarely
      // surface at all for InitiateAuth under that setting), but the fold happens here too so
      // this adapter can never accidentally leak the distinction even if that ever changes.
      if (err instanceof NotAuthorizedException || err instanceof UserNotFoundException) return { kind: "INVALID_CREDENTIALS" };
      if (err instanceof UserNotConfirmedException) return { kind: "USER_NOT_CONFIRMED" };
      if (err instanceof PasswordResetRequiredException) return { kind: "PASSWORD_RESET_REQUIRED" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async signUp(input: { username: string; password: string; name: string }): Promise<CognitoSignUpOutcome> {
    try {
      await this.client.send(
        new SignUpCommand({
          ClientId: this.clientId,
          Username: input.username,
          Password: input.password,
          SecretHash: this.hash(input.username),
          // `name` (standard Cognito attribute, no schema/read_attributes change needed - see
          // infra/modules/cognito/main.tf) rides the ID token's own `name` claim on first login,
          // which `AwsJwtIdTokenVerifier.verify()` already reads generically for every auth path
          // (direct password login included, not just OIDC) - `BootstrapIdentityService` then
          // just works, no other backend change required for `GlobalUser.displayName`.
          UserAttributes: [
            { Name: "email", Value: input.username },
            { Name: "name", Value: input.name },
          ],
        }),
      );
      return { kind: "CONFIRMATION_REQUIRED" };
    } catch (err) {
      // UsernameExistsException is NOT anti-enumeration-collapsed (unlike every other outcome
      // in this file) - SignUp itself has always had to tell a real user "this e-mail is
      // already registered" for basic usability (the Hosted UI's own signup form did too); this
      // is not a new information leak introduced by this adapter.
      if (err instanceof UsernameExistsException) return { kind: "EMAIL_ALREADY_REGISTERED" };
      if (err instanceof InvalidPasswordException) return { kind: "INVALID_PASSWORD" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async confirmSignUp(input: { username: string; confirmationCode: string }): Promise<CognitoConfirmSignUpOutcome> {
    try {
      await this.client.send(
        new ConfirmSignUpCommand({ ClientId: this.clientId, Username: input.username, ConfirmationCode: input.confirmationCode, SecretHash: this.hash(input.username) }),
      );
      return { kind: "SUCCESS" };
    } catch (err) {
      if (err instanceof CodeMismatchException || err instanceof ExpiredCodeException) return { kind: "INVALID_CODE_OR_EXPIRED" };
      // Only the documented "already confirmed" message maps to that outcome - ANY other
      // NotAuthorizedException (app client misconfiguration, unexpected auth failure) used to
      // be folded into the same false success (round-1 Codex finding).
      if (err instanceof NotAuthorizedException && looksAlreadyConfirmed(err.message)) return { kind: "ALREADY_CONFIRMED" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async resendConfirmationCode(input: { username: string }): Promise<CognitoResendConfirmationOutcome> {
    try {
      await this.client.send(new ResendConfirmationCodeCommand({ ClientId: this.clientId, Username: input.username, SecretHash: this.hash(input.username) }));
      return { kind: "SENT" };
    } catch (err) {
      // Same narrowing as confirmSignUp above (round-1 Codex finding) - only the documented
      // "already confirmed" message maps to that outcome, never every InvalidParameterException.
      if (err instanceof InvalidParameterException && looksAlreadyConfirmed(err.message)) return { kind: "ALREADY_CONFIRMED" };
      // Anti-enumeration (decision 3), same fold as forgotPassword() below - a nonexistent
      // e-mail must resolve identically to a real, unconfirmed one from the caller's point of
      // view. Without this, UserNotFoundException would fall through to UNKNOWN_OUTCOME below
      // and BffAuthService.resendConfirmationCode would surface it as a distinguishable error.
      if (err instanceof UserNotFoundException) return { kind: "SENT" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async forgotPassword(input: { username: string }): Promise<CognitoForgotPasswordOutcome> {
    try {
      await this.client.send(new ForgotPasswordCommand({ ClientId: this.clientId, Username: input.username, SecretHash: this.hash(input.username) }));
      return { kind: "SUCCESS" };
    } catch (err) {
      // UserNotFoundException is swallowed into SUCCESS on purpose (D-3xx decision 3) - under
      // `prevent_user_existence_errors = "ENABLED"` Cognito itself normally never throws this
      // for ForgotPassword in the first place, but this adapter does not rely on that alone.
      // InvalidParameterException is folded the same way (round-1 Codex finding, AWS's own docs
      // on error suppression) - a user with no verified recovery attribute (email/phone) also
      // throws this, which would otherwise create a 3-way oracle (real send=202, no-such-user=
      // 202, "exists but nothing to send to"=503) distinguishable without any timing measurement.
      if (err instanceof UserNotFoundException || err instanceof InvalidParameterException) return { kind: "SUCCESS" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }

  async confirmForgotPassword(input: { username: string; confirmationCode: string; newPassword: string }): Promise<CognitoConfirmForgotPasswordOutcome> {
    try {
      await this.client.send(
        new ConfirmForgotPasswordCommand({
          ClientId: this.clientId,
          Username: input.username,
          ConfirmationCode: input.confirmationCode,
          Password: input.newPassword,
          SecretHash: this.hash(input.username),
        }),
      );
      return { kind: "SUCCESS" };
    } catch (err) {
      if (err instanceof CodeMismatchException || err instanceof ExpiredCodeException) return { kind: "INVALID_CODE_OR_EXPIRED" };
      if (err instanceof UserNotFoundException) return { kind: "INVALID_CODE_OR_EXPIRED" }; // never distinguish from a bad code (anti-enumeration)
      if (err instanceof InvalidPasswordException) return { kind: "INVALID_PASSWORD" };
      if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
      return { kind: "UNKNOWN_OUTCOME" };
    }
  }
}

export function createCognitoIdpClient(): CognitoIdentityProviderClient {
  return new CognitoIdentityProviderClient({});
}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'loginWithPassword|TRANSIENT_FAILURE|UNKNOWN_OUTCOME|async signUp|async confirmSignUp|async resend|async startForgot' src/modules/bff/application/bff-auth-service.ts; rg -n 'cross-site|same-site|malformed|TRANSIENT|UNKNOWN|Sec-Fetch|sec-fetch|D-3|InvalidParameter|already confirmed' test/unit/bff/bff-handlers.test.ts docs/engineering/performance/traces/perf-04-auth.mjs; git status --short; git branch --show-current" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 120ms:
210:  async loginWithPassword(input: { email: string; password: string }): Promise<{ sessionToken: string; csrfToken: string }> {
213:    // Round-1 Codex finding (d321-direct-auth-adversarial-review): TRANSIENT_FAILURE/
214:    // UNKNOWN_OUTCOME used to fold into the SAME "invalid credentials" as a real wrong password -
219:    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
237:  async signUp(input: { email: string; password: string; name: string }): Promise<{ status: "CONFIRMATION_REQUIRED" }> {
258:  async confirmSignUp(input: { email: string; confirmationCode: string }): Promise<void> {
276:  async resendConfirmationCode(input: { email: string }): Promise<void> {
279:    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
292:  async startForgotPassword(input: { email: string }): Promise<void> {
295:    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
315:  /** Shared by `handleCallback` (OIDC/PKCE, dormant fallback) and `loginWithPassword` (D-3xx,
430:      if (outcome.kind === "UNKNOWN_OUTCOME") {
431:        // Mirrors this codebase's other UNKNOWN_OUTCOME handling (import/renew): never
537:      return { kind: "UNKNOWN_OUTCOME" }; // response lost after Cognito may have already rotated
544:    if (cognitoOutcome.kind === "TRANSIENT_FAILURE") {
548:    if (cognitoOutcome.kind === "UNKNOWN_OUTCOME") {
550:      return { kind: "UNKNOWN_OUTCOME" };
582:      return { kind: "UNKNOWN_OUTCOME" };
docs/engineering/performance/traces/perf-04-auth.mjs:3:// D-322 (2026-09-22): rewritten to call `POST /bff/login` (direct-auth entry point added by
docs/engineering/performance/traces/perf-04-auth.mjs:4:// D-321) with a plain `fetch`, instead of driving a headless browser through the Cognito
docs/engineering/performance/traces/perf-04-auth.mjs:6:// D-321 stopped being the app's primary login path - and turned out to be fragile even for the
docs/engineering/performance/traces/perf-04-auth.mjs:9:// does not force a downgrade of an already-live domain - optional+computed, D-320/D-321 never
docs/engineering/performance/traces/perf-04-auth.mjs:71:    // review: POST /bff/login now rejects any request that can't declare itself same-site/none,
docs/engineering/performance/traces/perf-04-auth.mjs:72:    // closing a login-CSRF gap a cross-site browser page can never spoof this header to bypass).
docs/engineering/performance/traces/perf-04-auth.mjs:73:    headers: { "content-type": "application/json", "sec-fetch-site": "none" },
test/unit/bff/bff-handlers.test.ts:190:      authenticatedRequest({ sessionCookie, csrfCookie, method: "POST", path: "/bff/session/logout", headers: { "sec-fetch-site": "same-origin" } }),
test/unit/bff/bff-handlers.test.ts:195:  it("rejects (403) a cross-site request even with a correct CSRF header/cookie pair (Sec-Fetch-Site layer)", async () => {
test/unit/bff/bff-handlers.test.ts:205:        headers: { "sec-fetch-site": "cross-site", "x-csrf-token": csrfCookie },
test/unit/bff/bff-handlers.test.ts:211:  it("succeeds (204) and clears cookies when Sec-Fetch-Site, header, and cookie all agree", async () => {
test/unit/bff/bff-handlers.test.ts:221:        headers: { "sec-fetch-site": "same-origin", "x-csrf-token": csrfCookie },
test/unit/bff/bff-handlers.test.ts:242:    const res = await handleProxy(deps, authenticatedRequest({ sessionCookie, method: "POST", path: "/bff/api/items", headers: { "sec-fetch-site": "same-origin" } }), "/items", undefined);
test/unit/bff/bff-handlers.test.ts:295:        headers: { "sec-fetch-site": "same-origin", "x-csrf-token": csrfCookie },
test/unit/bff/bff-handlers.test.ts:312:        headers: { "sec-fetch-site": "same-origin", "x-csrf-token": csrfCookie },
test/unit/bff/bff-handlers.test.ts:320:// Round-1 Codex finding (d321-direct-auth-adversarial-review): all 6 D-3xx routes below now
test/unit/bff/bff-handlers.test.ts:321:// require Fetch Metadata (`Sec-Fetch-Site: same-origin` or `none`) before anything else runs -
test/unit/bff/bff-handlers.test.ts:324:const SAME_ORIGIN_HEADERS = { "sec-fetch-site": "same-origin" };
test/unit/bff/bff-handlers.test.ts:326:// D-3xx (reversal of D-320): the app's own login/signup/reset-password screens now call these
test/unit/bff/bff-handlers.test.ts:330:  it("403s a cross-site request before ever touching the body (login-CSRF guard)", async () => {
test/unit/bff/bff-handlers.test.ts:335:      headers: { "sec-fetch-site": "cross-site" },
test/unit/bff/bff-handlers.test.ts:341:  it("403s when Sec-Fetch-Site is absent - never fail-open for an older browser or unlabeled request", async () => {
test/unit/bff/bff-handlers.test.ts:353:  it("400s on a malformed (non-JSON-object) body instead of a raw 500", async () => {
test/unit/bff/bff-handlers.test.ts:373:  it("returns a generic 401 for invalid credentials - never distinguishes 'no such user' from 'wrong password' (anti-enumeration, D-3xx decision 3)", async () => {
test/unit/bff/bff-handlers.test.ts:387:  // Round-1 Codex finding: TRANSIENT_FAILURE/UNKNOWN_OUTCOME used to fold into the SAME 401
test/unit/bff/bff-handlers.test.ts:393:    cognitoAuthClient.nextAuthenticateOutcome = { kind: "TRANSIENT_FAILURE", cause: new Error("boom") };
test/unit/bff/bff-handlers.test.ts:397:    cognitoAuthClient.nextAuthenticateOutcome = { kind: "UNKNOWN_OUTCOME" };
test/unit/bff/bff-handlers.test.ts:404:  it("403s a cross-site request", async () => {
test/unit/bff/bff-handlers.test.ts:409:      headers: { "sec-fetch-site": "cross-site" },
test/unit/bff/bff-handlers.test.ts:467:  // ALREADY_CONFIRMED unconditionally - ANY authorization failure (not just "already confirmed")
test/unit/bff/bff-handlers.test.ts:468:  // silently became a 204 success. Only the documented "already confirmed" message now does.
test/unit/bff/bff-handlers.test.ts:469:  it("does not report success for a NotAuthorizedException that isn't the documented 'already confirmed' case", async () => {
test/unit/bff/bff-handlers.test.ts:471:    cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "UNKNOWN_OUTCOME" };
test/unit/bff/bff-handlers.test.ts:486:  it("202s regardless of whether the e-mail is registered (anti-enumeration, D-3xx decision 3)", async () => {
warning: unable to access 'C:\Users\Usuario/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\Usuario/.config/git/ignore': Permission denied
 M docs/engineering/performance/traces/perf-04-auth.mjs
 M src/modules/bff/application/bff-auth-service.ts
 M src/modules/bff/http/bff-handlers.ts
 M src/modules/bff/persistence/cognito-idp-auth-client.ts
 M test/unit/bff/bff-handlers.test.ts
?? docs/architecture/reviews/d321-direct-auth-adversarial-review/
?? "prototype/ET - SystemDesign.zip"
develop

web search: 
web search: https://docs.aws.amazon.com/cognito-user-identity-pools/latest/APIReference/API_DeviceConfigurationType.html
codex
As correções de parsing, CSRF e erro transitório estão no código. No achado 5, encontrei documentação oficial que distingue a ativação no pool da confirmação de um dispositivo; isso enfraquece a tréplica. Vou verificar essa distinção junto com os testes e os registros de pendência.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 test/unit/bff/bff-handlers.test.ts | Select-Object -Skip 319; Get-Content -Encoding utf8 src/modules/bff/application/bff-auth-service.ts | Select-Object -Skip 209 -First 106; Get-Content -Encoding utf8 .claude/skills/task-checklist/SKILL.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 164ms:
// Round-1 Codex finding (d321-direct-auth-adversarial-review): all 6 D-3xx routes below now
// require Fetch Metadata (`Sec-Fetch-Site: same-origin` or `none`) before anything else runs -
// `same-origin` is the real browser value these routes see in production; every other test in
// this section uses it so it never has to think about the guard while testing its own concern.
const SAME_ORIGIN_HEADERS = { "sec-fetch-site": "same-origin" };

// D-3xx (reversal of D-320): the app's own login/signup/reset-password screens now call these
// directly instead of redirecting to the Cognito Hosted UI - handleLogin/handleCallback above
// stay covered by their own existing tests (dormant fallback, never removed).
describe("handleLoginPassword", () => {
  it("403s a cross-site request before ever touching the body (login-CSRF guard)", async () => {
    const { deps } = buildDeps();
    const res = await handleLoginPassword(deps, {
      method: "POST",
      path: "/bff/login",
      headers: { "sec-fetch-site": "cross-site" },
      body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }),
    });
    expect(res.statusCode).toBe(403);
  });

  it("403s when Sec-Fetch-Site is absent - never fail-open for an older browser or unlabeled request", async () => {
    const { deps } = buildDeps();
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
    expect(res.statusCode).toBe(403);
  });

  it("400s when email or password is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com" }) });
    expect(res.statusCode).toBe(400);
  });

  it("400s on a malformed (non-JSON-object) body instead of a raw 500", async () => {
    const { deps } = buildDeps();
    const notJson = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: "not-json" });
    expect(notJson.statusCode).toBe(400);
    const arrayBody = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: "[]" });
    expect(arrayBody.statusCode).toBe(400);
  });

  it("sets session and CSRF cookies on a successful password login", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = {
      kind: "SUCCESS",
      tokens: { accessToken: "header." + Buffer.from(JSON.stringify({ sub: "s1" })).toString("base64url") + ".sig", idToken: "id-1", refreshToken: "refresh-1", expiresInSeconds: 900 },
    };
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
    expect(res.statusCode).toBe(200);
    expect(extractCookieValue(res.cookies, SESSION_COOKIE_NAME)).toBeTruthy();
    expect(extractCookieValue(res.cookies, CSRF_COOKIE_NAME)).toBeTruthy();
  });

  it("returns a generic 401 for invalid credentials - never distinguishes 'no such user' from 'wrong password' (anti-enumeration, D-3xx decision 3)", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = { kind: "INVALID_CREDENTIALS" };
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "nobody@example.com", password: "wrong" }) });
    expect(res.statusCode).toBe(401);
  });

  it("returns the same generic 401 for USER_NOT_CONFIRMED as for invalid credentials", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = { kind: "USER_NOT_CONFIRMED" };
    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(res.statusCode).toBe(401);
  });

  // Round-1 Codex finding: TRANSIENT_FAILURE/UNKNOWN_OUTCOME used to fold into the SAME 401
  // "invalid credentials" as a real wrong password - a genuine Cognito outage/throttling told
  // the user they mistyped their password. Both now map to 503, never a false claim about the
  // credential itself.
  it("returns 503 (never 401) when authentication genuinely could not be completed", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextAuthenticateOutcome = { kind: "TRANSIENT_FAILURE", cause: new Error("boom") };
    const transient = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(transient.statusCode).toBe(503);

    cognitoAuthClient.nextAuthenticateOutcome = { kind: "UNKNOWN_OUTCOME" };
    const unknown = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(unknown.statusCode).toBe(503);
  });
});

describe("handleSignUp", () => {
  it("403s a cross-site request", async () => {
    const { deps } = buildDeps();
    const res = await handleSignUp(deps, {
      method: "POST",
      path: "/bff/signup",
      headers: { "sec-fetch-site": "cross-site" },
      body: JSON.stringify({ email: "user@example.com", password: "x", name: "Ana" }),
    });
    expect(res.statusCode).toBe(403);
  });

  it("400s when email, password or name is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleSignUp(deps, { method: "POST", path: "/bff/signup", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
    expect(res.statusCode).toBe(400);
  });

  it("202s with CONFIRMATION_REQUIRED on a fresh signup", async () => {
    const { deps } = buildDeps();
    const res = await handleSignUp(deps, {
      method: "POST",
      path: "/bff/signup",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "new@example.com", password: "correct-horse-battery-1", name: "Ana Exemplo" }),
    });
    expect(res.statusCode).toBe(202);
    expect(res.body).toEqual({ status: "CONFIRMATION_REQUIRED" });
  });

  it("409s when the e-mail is already registered", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextSignUpOutcome = { kind: "EMAIL_ALREADY_REGISTERED" };
    const res = await handleSignUp(deps, {
      method: "POST",
      path: "/bff/signup",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "taken@example.com", password: "x", name: "Ana Exemplo" }),
    });
    expect(res.statusCode).toBe(409);
  });
});

describe("handleConfirmSignUp", () => {
  it("400s when email or confirmationCode is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com" }) });
    expect(res.statusCode).toBe(400);
  });

  it("204s on a valid confirmation code", async () => {
    const { deps } = buildDeps();
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
    expect(res.statusCode).toBe(204);
  });

  it("400s on an invalid/expired code", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "INVALID_CODE_OR_EXPIRED" };
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000" }) });
    expect(res.statusCode).toBe(400);
  });

  // Round-1 Codex finding: ConfirmSignUp's NotAuthorizedException used to be treated as
  // ALREADY_CONFIRMED unconditionally - ANY authorization failure (not just "already confirmed")
  // silently became a 204 success. Only the documented "already confirmed" message now does.
  it("does not report success for a NotAuthorizedException that isn't the documented 'already confirmed' case", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "UNKNOWN_OUTCOME" };
    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
    expect(res.statusCode).toBe(503);
  });
});

describe("handleResendConfirmationCode", () => {
  it("204s regardless of whether the e-mail is registered (anti-enumeration)", async () => {
    const { deps } = buildDeps();
    const res = await handleResendConfirmationCode(deps, { method: "POST", path: "/bff/signup/resend", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "anyone@example.com" }) });
    expect(res.statusCode).toBe(204);
  });
});

describe("handleForgotPassword", () => {
  it("202s regardless of whether the e-mail is registered (anti-enumeration, D-3xx decision 3)", async () => {
    const { deps } = buildDeps();
    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "anyone@example.com" }) });
    expect(res.statusCode).toBe(202);
  });

  it("400s when email is missing", async () => {
    const { deps } = buildDeps();
    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({}) });
    expect(res.statusCode).toBe(400);
  });
});

describe("handleConfirmForgotPassword", () => {
  it("204s on a valid code + new password", async () => {
    const { deps } = buildDeps();
    const res = await handleConfirmForgotPassword(deps, {
      method: "POST",
      path: "/bff/forgot-password/confirm",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456", newPassword: "Correct-Horse-1" }),
    });
    expect(res.statusCode).toBe(204);
  });

  it("400s on an invalid/expired code", async () => {
    const { deps, cognitoAuthClient } = buildDeps();
    cognitoAuthClient.nextConfirmForgotPasswordOutcome = { kind: "INVALID_CODE_OR_EXPIRED" };
    const res = await handleConfirmForgotPassword(deps, {
      method: "POST",
      path: "/bff/forgot-password/confirm",
      headers: SAME_ORIGIN_HEADERS,
      body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000", newPassword: "x" }),
    });
    expect(res.statusCode).toBe(400);
  });
});
  async loginWithPassword(input: { email: string; password: string }): Promise<{ sessionToken: string; csrfToken: string }> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.authenticateWithPassword({ username, password: input.password });
    // Round-1 Codex finding (d321-direct-auth-adversarial-review): TRANSIENT_FAILURE/
    // UNKNOWN_OUTCOME used to fold into the SAME "invalid credentials" as a real wrong password -
    // a genuine Cognito outage/throttling would falsely tell the user they mistyped their
    // password. Mapped to DependencyUnavailableError instead, same posture every other method in
    // this file already uses - this reveals nothing about account existence, only "the system
    // could not complete this request right now", which is not an anti-enumeration regression.
    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
      throw new DependencyUnavailableError("Não foi possível autenticar agora - tente novamente.");
    }
    if (outcome.kind !== "SUCCESS") {
      throw new AuthenticationError("E-mail ou senha inválidos.");
    }

    // Direct-auth ID token was never minted against an `/oauth2/authorize` nonce (undefined is
    // the documented "skip nonce check" signal - see IdTokenVerifier.verify's own doc comment).
    const idClaims = await this.deps.idTokenVerifier.verify(outcome.tokens.idToken, undefined);
    return this.establishSession(outcome.tokens, idClaims);
  }

  /** D-3xx: self-service signup (`POST /bff/signup`) - the same path the Hosted UI's own "Sign
   * up" link exercised (the pool has no `admin_create_user_config` restricting registration to
   * admins, `infra/modules/cognito/main.tf` - self-service signup was already live, this is
   * parity, not a new capability). `auto_verified_attributes = ["email"]` means Cognito always
   * requires the confirmation-code step below before the account can log in. */
  async signUp(input: { email: string; password: string; name: string }): Promise<{ status: "CONFIRMATION_REQUIRED" }> {
    const username = input.email.trim().toLowerCase();
    const name = input.name.trim();
    if (!name) {
      throw new ValidationError("O nome é obrigatório.");
    }
    const outcome = await this.deps.cognitoAuthClient.signUp({ username, password: input.password, name });
    if (outcome.kind === "EMAIL_ALREADY_REGISTERED") {
      throw new ConflictError("Já existe uma conta com este e-mail.");
    }
    if (outcome.kind === "INVALID_PASSWORD") {
      throw new ValidationError("A senha não atende aos requisitos mínimos de segurança.");
    }
    if (outcome.kind !== "CONFIRMATION_REQUIRED") {
      throw new DependencyUnavailableError("Não foi possível criar a conta - tente novamente.");
    }
    return { status: "CONFIRMATION_REQUIRED" };
  }

  /** D-3xx: `POST /bff/signup/confirm` - the e-mail verification step Cognito's
   * `auto_verified_attributes` requires before a freshly self-registered account can log in. */
  async confirmSignUp(input: { email: string; confirmationCode: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.confirmSignUp({ username, confirmationCode: input.confirmationCode });
    if (outcome.kind === "INVALID_CODE_OR_EXPIRED") {
      throw new ValidationError("Código inválido ou expirado.");
    }
    if (outcome.kind === "ALREADY_CONFIRMED") {
      // Idempotent from the caller's point of view - the account is confirmed either way, which
      // is exactly what the caller wanted, so this is not surfaced as an error.
      return;
    }
    if (outcome.kind !== "SUCCESS") {
      throw new DependencyUnavailableError("Não foi possível confirmar o e-mail - tente novamente.");
    }
  }

  /** D-3xx: `POST /bff/signup/resend` - lets a user whose confirmation code expired get a new
   * one without restarting the whole signup form. */
  async resendConfirmationCode(input: { email: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.resendConfirmationCode({ username });
    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
      throw new DependencyUnavailableError("Não foi possível reenviar o código - tente novamente.");
    }
    // SENT or ALREADY_CONFIRMED: both are a benign no-op from the caller's perspective (a
    // confirmed account never needs the code anyway) - anti-enumeration posture (decision 3):
    // never let the response distinguish "this e-mail isn't registered" from either of those.
  }

  /** D-3xx: `POST /bff/forgot-password` - always resolves the same way regardless of whether
   * the e-mail is registered (Cognito's own anti-enumeration posture under
   * `prevent_user_existence_errors = "ENABLED"`, preserved by CognitoIdpAuthClient.
   * forgotPassword folding UserNotFoundException into SUCCESS too - belt and suspenders). Only a
   * genuine transient/unknown failure surfaces differently, and even then never says why. */
  async startForgotPassword(input: { email: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.forgotPassword({ username });
    if (outcome.kind === "TRANSIENT_FAILURE" || outcome.kind === "UNKNOWN_OUTCOME") {
      throw new DependencyUnavailableError("Não foi possível iniciar a redefinição de senha - tente novamente.");
    }
  }

  /** D-3xx: `POST /bff/forgot-password/confirm` - the code+new-password step. */
  async confirmForgotPassword(input: { email: string; confirmationCode: string; newPassword: string }): Promise<void> {
    const username = input.email.trim().toLowerCase();
    const outcome = await this.deps.cognitoAuthClient.confirmForgotPassword({ username, confirmationCode: input.confirmationCode, newPassword: input.newPassword });
    if (outcome.kind === "INVALID_CODE_OR_EXPIRED") {
      throw new ValidationError("Código inválido ou expirado.");
    }
    if (outcome.kind === "INVALID_PASSWORD") {
      throw new ValidationError("A nova senha não atende aos requisitos mínimos de segurança.");
    }
    if (outcome.kind !== "SUCCESS") {
      throw new DependencyUnavailableError("Não foi possível redefinir a senha - tente novamente.");
    }
  }

  /** Shared by `handleCallback` (OIDC/PKCE, dormant fallback) and `loginWithPassword` (D-3xx,
---
name: task-checklist
description: Run the Expiration Tracker task-completion checklist (docs/engineering/task-completion-checklist.md) against the work just done in this session, before marking a task/todo item complete. Use at the end of any task that produced or changed real code, a recorded decision, or a normative document in this repo — not for read-only tasks (research, planning, pure Q&A). Invoke explicitly as /task-checklist, or reach for it proactively whenever you are about to report a task as finished.
---

# Task-completion checklist runner

This project decided (Marcelo, 2026-09-18) to keep this checklist an agent-invoked skill, not a
blocking hook — `docs/engineering/definition-of-done.md` §"O que isso NÃO é" explains why (it stays
proportional: lowers the friction of applying the gate, without forcing automation the project
hasn't yet seen evidence it needs). Running this skill is still something you choose to do, exactly
like reading `definition-of-done.md` directly — this file just makes that fast and consistent.

## What to do

1. Read `docs/engineering/task-completion-checklist.md` in full — it is the actual checklist; this
   skill does not duplicate its content, only drives applying it.
2. Identify the real diff/decision this task produced (`git diff`/`git status`, the artifact
   written, or the decision registered) — never evaluate against the task's original intent, only
   against what was actually produced.
3. Walk the checklist section by section, in order, against that real diff:
   - §0: confirm this is a valid, coherent unit of completion (decompose first if it isn't).
   - §1: classify the risk level, citing the concrete trigger from `change-risk-scale.md`.
   - §2: run the gate commands the classified level requires — actually run them, read the real
     output; never assume green.
   - §3: for each of the 9 axes, mark N/A or evaluated — do not force axes the diff didn't touch,
     do not skip evaluating ones it did.
   - §4: only if this task closes a roadmap/backlog item — confirm end-to-end evidence is cited, not
     just "each slice was green in isolation."
   - §5: write the `DoD:` evidence line in the required format, in the best available location
     (commit message > review artifact > response body).
   - §6: only when concluding a phase/milestone, not every item.
   - §7: confirm the task isn't ending because of an unstated blocker — if the next step needs
     Marcelo's decision, that pending item is named and another independent thread continues.
4. If any required gate is missing, failed, or wasn't actually run: do **not** report the task as
   done. State plainly what's missing and leave the todo item `in_progress`/`pending` with the
   finding recorded — never mark something `completed` "to fix later."
5. Report the result concisely: the risk level, which gates ran (with real outcomes), which axes
   were touched, and the final `DoD:` line. Don't restate the whole checklist back to the user —
   only the parts that were actually evaluated and anything that failed.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'D-321|rate.limit|409|empírica|device|WHATSAPP' NEXT_SESSION_PROMPT.md docs/architecture/decisions-log.md | Select-Object -Last 24; rg -n 'ConfirmDevice|UpdateDeviceStatus|DEVICE_SRP_AUTH|NewDeviceMetadata' src; rg -n 'device_configuration|challenge_required|device_only' infra/modules/cognito/main.tf; rg --files test | rg 'cognito.*auth|bff-auth'; Get-Content -Encoding utf8 src/modules/bff/domain/csrf.ts; Get-Content -Encoding utf8 docs/engineering/performance/traces/perf-04-auth.mjs | Select-Object -Skip 52 -First 50" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 233ms:
docs/architecture/decisions-log.md:238:| D-191 | **RequirementTemplate — item 1 (P0.1) do roadmap de lançamento, design `APPROVED` E implementado ponta a ponta na mesma sessão.** Protocolo Claude↔Codex completo, 4 rodadas reais (`codex exec`, prompt por arquivo em primeiro plano), régua contestada na Rodada 1 e reconciliada em 4 versões (`research-protocol.md`): Rodada 1 Claude 8,2 / Codex régua 6,4 design 6,6 → R2 9,2/9,1 vs 8,3/8,1 → R3 9,4/9,3 vs 8,8/8,7 → **R4 9,4/9,3 vs 9,1/9,0** (ambos ≥9,0 nas duas dimensões, sem arredondar). E-014 `SIM PARCIAL` — 5 fontes externas com data de acesso (Drata "will never modify your policies without your involvement", Vanta, Asana, Atlassian/Jira `Clone`, HighLevel Snapshots), amostra justificada como representativa por cobrir o nicho funcional exato (compliance/GRC) e o de maior volume de uso (PM genérico); **padrão convergente**: materialização por CÓPIA no apply, nunca live-link com propagação. Divergência registrada: prevenção de duplicidade no apply não tem padrão externo (o Asana explicitamente não a oferece) — desenhada internamente. **Decisões**: (1) a entidade-alvo é `Requirement` (document-archive, D-143/D-145), não a legada `RequirementAssignment`, que fica intocada; (2) snapshot no apply, com `sourceTemplateId`/`sourceTemplateItemId`/`sourceTemplateAppliedVersion` como rastro que NENHUM caminho de leitura/derivação/worker consulta; (3) **decisão de produto declarada explicitamente** (não inferida do precedente de `DocumentType`, correção epistêmica do Codex): o nome de um `Requirement` é único por Subject, com as 4 consequências respondidas e gatilho de reversão nomeado (campo `period` entra na chave do ponteiro), enforced transacionalmente por `RequirementNamePointer` em TODOS os caminhos de escrita — `createRequirement`/`updateRequirement`/`deleteRequirement`/`applyTemplate`, não só no novo; (4) itens embutidos, cap 30; (5) `planTemplateApplication` puro com uma implementação e dois call sites (preview e apply). **5 achados reais fechados, cada um levantado por uma rodada do Codex e verificado por leitura direta do código, não por argumento**: `createRequirement` nunca verificava o Subject (criava `Requirement` sob Subject inexistente/ARCHIVED — lacuna pré-existente); `status <> DELETED` deixaria `ARCHIVED` passar (`TrackedSubjectStatus` é `ACTIVE\|ARCHIVED\|DELETED`, fence passa a enumerar `= ACTIVE`); dois itens de template normalizando para o mesmo nome virariam dois `Put` na MESMA chave de ponteiro numa transação (`ValidationException`/500 opaco em vez de 409 — fechado por `assertTemplateItemNamesUnique` em create/update/duplicate + planejador); fence do template passa a ser `status = ACTIVE AND version = <lida>`, não só o status (custo zero em ações, fecha o TOCTOU de aplicar itens de uma versão já editada); classificação de cancelamento estrutural por `labels` por entrada, nunca índice literal, e **nenhum erro afirma causa que o DynamoDB não revelou** (removida a releitura que escolhia entre 404 e 409 — `SubjectPreconditionFailedError` e `TemplatePreconditionFailedError` únicos). **Erro real meu, corrigido na Rodada 4**: a reserva do cap 30 estava justificada por aritmética impossível (com `C+S<=30`, proteger skips nunca chega a 93); a justificativa correta é o pior caso previsto `3N+3` (um evento de auditoria por `Requirement`, lacuna conhecida deste módulo), que impõe teto `N<=32` — 30 é escolha operacional abaixo do teto, não valor derivado. Contrato transacional do apply: `2N+3` ações. Limites em BYTES UTF-8 (não caracteres) + `estimateDynamoItemBytesUpperBound()` documentado como limite superior (lança em tipo não coberto, condição explícita do Codex). Contrato de honestidade declarado: preview e apply não podem divergir algoritmicamente mas podem divergir temporalmente; `skip` é observação de planejamento e NÃO é protegido transacionalmente (só `create` é); a leitura do plano é completa (`queryByPk` esgota `LastEvaluatedKey`, verificado) porém eventually consistent, e reaplicar é seguro e idempotente. **Achado pré-existente registrado, deliberadamente fora do escopo, com identificador e próximo passo concreto (`RT-LANE-FALLBACK-01`)**: `tenant-business-mutation.ts` linhas 213-223 classifica `CancellationReasons` ausente/malformado como `TenantNotActiveError`, afirmando causa não revelada — fatia própria nível 3-4, mesma disciplina de D-177→D-178 e D-183. | Nível 5-6 (modelo de dados novo + operação de fan-out transacional + regra de domínio nova sobre uma entidade existente) | Claude 9,3 | Codex 9,0 | **IMPLEMENTADO — gate local completo verde**: `typecheck`, `lint`, 1931/1931 (`test/unit`+`integration`+`contract`, 22 testes adversariais G-V3 novos + 11 de contrato + 2 de allowlist), `check-docs`, `validate-schemas`, `build:lambdas`, `dependency-cruiser` 0 violações, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). Os 2 arquivos de `test/architecture/` que falham neste worktree falham IDENTICAMENTE na árvore sem a mudança (confirmado por `git stash`) — artefato de o worktree não ter `node_modules` para os processos filhos (`tsc`/`dependency-cruiser`), não regressão. **VERIFICADO AO VIVO (2026-09-03)**: PR #219 mergeado (`72ce4bb`→`4c90d44`), CI (`33713015703`/`33713051982`/`33713271496`) e CD (`33713455836`) reais `success` via `gh run view`; contra `dev`, `exptrk-dev-document-archive-handler` `Active`/`Successful` e **as 9 rotas `requirement-templates` confirmadas presentes** (`aws apigatewayv2 get-routes --api-id uav7id1muh --profile claude-dev`). O CI verde também confirmou que os 2 arquivos de `test/architecture/` passam com `node_modules` resolvível — as falhas locais eram artefato do worktree, não regressão. | `docs/architecture/reviews/requirement-template-scoping/` (9 arquivos), `src/modules/document-archive/domain/requirement-template.ts` (novo), `src/modules/document-archive/domain/requirement.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/http/document-archive-handlers.ts`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/errors/app-error.ts`, `src/runtime/aws/{ids.ts,handlers/document-archive-handler.ts}`, `infra/modules/api-gateway/main.tf`, 7 schemas em `schemas/api/`, testes em `test/{unit,integration,contract}/` | 2026-09-03 |
docs/architecture/decisions-log.md:245:| D-193 | Item 4/9 do design (`planDocumentVersionValidityEffect` + `confirmedBy`/`confirmedAt` + transações re-chaveadas). Novo planner puro `extraction/domain/document-version-validity-effect.ts` — único ponto que decide o efeito em `DocumentVersion.validUntil` a partir de `{fieldName, confirmedValue, documentVersion.{state,validUntil}}`: só `expirationDate` produz efeito; só estados elegíveis (`RECEIVED`/`UNDER_REVIEW`/`ACCEPTED`, mesmo conjunto do Starter) aceitam mudança; `validUntil` byte-idêntico ao já gravado planeja `NO_CHANGE` (é isso que torna o outbox condicional de verdade, não sempre-disparado). Chamado IDENTICAMENTE pelos dois caminhos: novo `confirm-reject-field-document-archive.ts` (manual, `confirmFieldForDocumentArchive`/`rejectFieldForDocumentArchive` — mecanismo novo, sem rota HTTP ainda, deliberadamente não wireado nesta fatia) e `run-extraction-validation.ts`'s `persistExtractedFieldsStage` (auto-confirm, `buildAutoConfirmDocumentVersionUpdate`, fechando o gap que a slice 3 deixou aberto explicitamente). `ExtractedField` ganha `confirmedBy`/`confirmedAt` (proveniência, critério 6 do checklist do design) — setado em AMBOS os caminhos, inclusive o `document`-module ANTIGO (`confirm-reject-field.ts`'s `doConfirmField`, mudança aditiva, cardinalidade da transação antiga inalterada). Transação nova `confirmFieldForDocumentArchive`: 3 agregados/4 ações (`DocumentVersion` Update sempre presente — mesmo em `NO_CHANGE` só bump de version/updatedAt —, `ExtractionRun` ConditionCheck, `ExtractedField` Update, `Outbox` Put só quando `effect.kind==="SET"`). Transação nova `rejectFieldForDocumentArchive`: 2 agregados/2 ações (`ExtractedField` Update, `ExtractionRun` ConditionCheck) — `DocumentVersion` NUNCA referenciada, nem por `ConditionCheck`. `Requirement` confirmado por leitura do texto do design como fora de QUALQUER uma das duas transações (convergência assíncrona é item 5/9, não tocado). Novo `OutboxDestination = "SQS_REQUIREMENT_EVIDENCE_REFRESH_V1"` adicionado ao union (valor existe desde já, sem fila/rota/worker reais — isso é item 5/9/6/9). **Escopo deliberadamente reduzido, nomeado explicitamente**: a unificação do caminho `document` ANTIGO com `document-archive` numa única transação NÃO foi feita — o módulo antigo não tem conceito de `DocumentVersion`/`Requirement`, então a transação antiga (`confirmField`/`rejectField` em `confirm-reject-field.ts`, 4-way incluindo `ExpirationItem`/`Document`) permanece byte-idêntica em cardinalidade, só ganhando `confirmedBy`/`confirmedAt` como campo adicional no mesmo Update já existente. O planner único é o real ponto de unificação (critério 5 do checklist — mesmo efeito por construção nos dois caminhos), não uma transação física compartilhada entre os dois módulos. G-V3: 8 testes do planner (SET/overwrite/NO_CHANGE por-valor-idêntico/NO_CHANGE por-estado-inelegível×4/campo-não-mapeado); 13 testes do novo `confirm-reject-field-document-archive.ts` (overwrite real, outbox condicional provado via o `effect` computado passado à store — não só o efeito colateral —, reject nunca toca `DocumentVersion`, `Requirement` semeado e provado BYTE-IDÊNTICO após confirm E após reject via `toEqual` no fixture original, idempotência, 409s nos 3 agregados, 404, cross-tenant); testes novos em `run-extraction-validation.test.ts` provando o auto-confirm agora fecha o gap da slice 3 (SET real, NO_CHANGE por valor idêntico, NO_CHANGE por estado inelegível, proveniência `SYSTEM_AUTO_CONFIRM`). | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 4/9, escolha de engenharia real registrada: unificação de transação física adiada, planner é o ponto de unificação) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint limpos, suíte 2097/2097 — 23 testes novos —, check-docs/validate-schemas/build:lambdas limpos; sem mudança de infra/Terraform, `terraform test` não rodado). D-193 item 4/9 do design FECHADO (planner + proveniência + as duas transações re-chaveadas), com a ressalva de escopo acima registrada. Próxima fatia: item 5/9 — convergência assíncrona de `Requirement`: novo índice reverso esparso `GSI_EVIDENCE` + `linkEvidence`/`unlinkEvidence` passam a escrevê-lo (item 5), fila+DLQ+rota no `DispatchOutboxRelay`+worker `requirement-evidence-refresh-handler.ts` que NUNCA aplica o payload do evento, sempre relê `DocumentVersion`+`Requirement` frescos (item 6). | `src/modules/extraction/{domain/{extracted-field.ts,document-version-validity-effect.ts},application/{confirm-reject-field.ts,confirm-reject-field-document-archive.ts,run-extraction-validation.ts},ports/extracted-field-store.ts,persistence/dynamodb-extracted-field-store.ts}`, `src/shared/outbox/outbox.ts`, testes em `test/unit/extraction/{confirm-reject-field,confirm-reject-field-document-archive,document-version-validity-effect,run-extraction-validation}.test.ts` | 2026-09-03 |
docs/architecture/decisions-log.md:249:| D-193 | Item 8/9 do design ("Sequenciamento") — dois feature flags AppConfig em ordem obrigatoria, fechando por construcao a janela "CLEAN sem consumidor": `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED` (STARTER, novos em `FeatureFlags`/`AppConfigFeatureFlagsReader`, mesmo `=== true` fail-closed parsing de AI_EXTRACTION/OCR/WHATSAPP) e `DOCUMENT_ARCHIVE_PROMOTION_ENABLED` (PROMOTER), ambos default `false`. STARTER gates `startExtractionRunForDocumentArchive()` (nova precondicao 0, checada ANTES de qualquer leitura DynamoDB, novo motivo de refuso `STARTER_DISABLED`); PROMOTER gates o terceiro branch (`document-archive`) de `upload-finalizer-handler.ts`/`malware-result-handler.ts` (`finalizeDocumentArchiveUpload()`/`processDocumentArchiveMalwareResult()`, novo outcome `IGNORED_PROMOTION_DISABLED`, checado ANTES de qualquer acesso a store/S3 — enquanto OFF o comportamento e byte-identico ao de antes da slice 1: chave reconhecida mas descartada, sem retry). Novo `src/modules/extraction/application/document-archive-activation.ts` — `isDocumentArchivePromotionEnabled()` exige AMBOS os flags (nunca so PROMOTER), o mecanismo real de ordem-por-construcao: ligar PROMOTER sem STARTER (a ordem proibida) nunca promove um arquivo a CLEAN, entao a janela perigosa nunca existe em nenhum caminho de codigo, nao so por disciplina de runbook. **Achado de fronteira real durante a fatia**: a primeira versao do arquivo vivia em `extraction/domain/` mas importava o tipo `FeatureFlags` de `extraction/ports/` — violava `domain-must-not-reach-application-layers` (pego por `test/architecture/tenant-fence-boundary.test.ts` na primeira rodada de testes), movido para `extraction/application/` (mesma classe de correcao de fronteira que a slice do D-179 para `transient-purge` ja tinha achado para `shared-must-not-reach-modules`). Terraform: duas novas `variable` (default `false`) em `infra/modules/feature-flags`, plumbadas no `jsonencode` do AppConfig e nos env vars/IAM (`feature_flags_read_policy_json`) dos 3 Lambdas afetados (`upload_finalizer_handler`, `malware_result_handler`, `extraction_starter_handler`). G-V3: com ambos OFF (default) prova o mecanismo COMPLETAMENTE inerte para documentos `document-archive` — nenhum acesso a store/S3, nao so um refuso no passo final (`DocumentFile`/`ExtractionRun` nunca tocados); com SO PROMOTER ON (ordem proibida deliberada) prova que o gate de ordem bloqueia a ativacao mesmo assim; com ambos ON (ordem correta) prova o pipeline real slices 1->3 rodando ponta a ponta contra o MESMO `InMemoryDocumentArchiveStore` compartilhado entre as duas funcoes (`test/unit/extraction/document-archive-pipeline-activation.test.ts`, novo — nao e so 2 testes isolados do gate, e uma prova de integracao real do mecanismo das slices 1-7 agora gateado/ativado). Suite **2151/2151** (195 arquivos), `typecheck`/`lint`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` **23/23** (`AWS_PROFILE=claude-dev`). | Nivel 3-4 (implementacao direta de design ja `APPROVED`, D-193 slice 8/9 — unico ponto real de engenharia fora do design: o achado de fronteira domain-vs-application do novo arquivo, achado e corrigido no caminho) | — | — | **IMPLEMENTADO** (gate local completo, ver detalhe acima). D-193 item 8/9 do design FECHADO — restante apenas item 9/9 (rename mecanico `document-classifier.ts`->`document-format-classifier.ts`, nivel 1-2, sem protocolo), depois do qual D-193 fica 9/9 COMPLETO e pronto para o PR-final-para-`main`+verificacao-ao-vivo (espelhando o fechamento de D-192). Commit `51fd70b` em `develop` (push direto, sem PR — `AGENTS.md` §3). | `src/modules/extraction/{ports/feature-flags-reader.ts,persistence/appconfig-feature-flags-reader.ts,application/{document-archive-activation.ts,start-extraction-run-for-document-archive.ts}}`, `src/workers/{upload-finalizer/document-archive-finalizer.ts,malware-result/document-archive-result-processor.ts}`, `src/runtime/aws/{composition/{document-archive.ts,extraction.ts},handlers/{upload-finalizer-handler.ts,malware-result-handler.ts,extraction-starter-handler.ts}}`, `infra/{main.tf,modules/feature-flags/{main.tf,variables.tf,outputs.tf}}`, testes em `test/unit/{extraction/{document-archive-activation.test.ts,document-archive-pipeline-activation.test.ts,start-extraction-run-for-document-archive.test.ts,appconfig-feature-flags-reader.test.ts},document/{document-archive-finalizer.test.ts,document-archive-result-processor.test.ts}}` | 2026-09-03 |
docs/architecture/decisions-log.md:254:| D-197 | **WhatsApp Operacional (Roadmap P0.3) — DESIGN, protocolo Claude↔Codex completo, 3 rodadas** (`docs/architecture/reviews/whatsapp-channel-scoping/`). **Marcelo decidiu o fornecedor nesta sessão: Meta Cloud API direta, não BSP** — desbloqueia o item; tudo que decorre dessa escolha passou pelo protocolo. **Declaração E-014: SIM** (fontes primárias Meta for Developers — Messaging Limits/Webhooks getting-started/Get opt-in, todas consultadas 2026-09-04, mais 1 fonte de prática de mercado para `biz_opaque_callback_data`/pricing 2026, com limitação de fonte registrada explicitamente onde a Meta não tinha página oficial citável). **Régua contestada na Rodada 1** (Codex achou o checklist da Rodada 1 incompleto — faltavam webhook inbox/replay como critério obrigatório, contrato SQS por canal do ADR-0008, quota por destinatário único vs. consumo agregado genérico, opt-in vinculado ao telefone) — reconciliada em v2 (Rodada 2, régua 8,7/10, ainda 2 pontos de ambiguidade) e fechada em v3 (Rodada 3, régua 9,3/10, estável). **3 achados reais do Codex, todos verificados por leitura de código, fecharam a Rodada 3**: (1) `TenantQuotaService` (fixed-window, por tenant, conta chamadas) não serve para o limite real da Meta (destinatário único, janela móvel, cross-tenant/portfólio) — mecanismo novo: item de tabela BASE (não GSI) `PK=WHATSAPP#PORTFOLIO`/`SK=SENT#<timestamp>#<telefone>` (tempo primeiro na SK, torna a Query por range válida — a v2 tinha a ordem invertida, achado real do Codex), IAM dedicada por `LeadingKeys`, fail-closed, hot-partition nomeado com gatilho de sharding; (2) `WebhookInbox` precisa de um caminho tenantless — achado incidental real: `ses-callback-workflow.ts` (precedente já em produção) na verdade retorna `UNMATCHED` ANTES de gravar o inbox quando as tags faltam, contradizendo o próprio cenário 15 de `architecture-fase3-consolidada.md` — corrigido para WhatsApp usando a cláusula "tenant/ACCOUNT" já prevista naquele cenário (`wabaId`, disponível antes de qualquer correlação); (3) correlação de webhook via `biz_opaque_callback_data` (mesmo papel das tags de SES) confirmada por pesquisa dedicada, formalizada como gate de implementação (confirmar contra sandbox real antes do primeiro envio de produção). **10 decisões finais** (D-1 a D-10, detalhe completo em `estado-final-consolidado.md`): widening de tipo sem migração; `WhatsAppProviderAdapter` espelhando `EmailProviderAdapter`; templates pré-provisionados (Meta Business Manager, nunca criados via API); categoria `Utility` sempre (produto é business-initiated); `WhatsAppOptIn` vinculado ao telefone exato; `GlobalUser.phoneE164` novo; `WebhookInbox` account-scoped; quota de destinatário único (acima); fila/schema `SQS_NOTIFICATION_WHATSAPP_V1` dedicados (ADR-0008); secrets via Secrets Manager (5 valores, incluindo `verify_token`) + kill switch de 2 flags em ordem (mesmo mecanismo de D-193 slice 8). ADR formal (nível 6 exige): `ADR-0012-whatsapp-cloud-api-channel.md`. DESIGN-ONLY — nenhum código/schema/infra alterado. | Type 1 (nível 6 — novo terceiro com acesso a PII) | 9,2/10 (Rodada 3, autoavaliação às cegas) | 9,1/10 (Rodada 3, final, régua 9,3/10 estável) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194.** Próximo: fatia 1 (widening de tipo + entidades novas, sem HTTP exposto) — ver `estado-final-consolidado.md` para o programa completo de 5 fatias. | `docs/architecture/reviews/whatsapp-channel-scoping/`, `docs/architecture/adr/ADR-0012-whatsapp-cloud-api-channel.md` | 2026-09-04 |
docs/architecture/decisions-log.md:262:| D-205 | **Roadmap P1 item 16 ("Dossiê documental PDF/Excel") — DESIGN, protocolo Claude↔Codex completo, 4 rodadas** (`docs/architecture/reviews/document-dossier-scoping/`). Fecha o próximo item do backlog P1 depois de D-204 (item 15). **Declaração E-014: SIM PARCIAL** (Drata "Pre-Audit package" e Vanta "evidence list/policy packet", fontes primárias datadas 2026-09-05 — nenhum dos dois mescla bytes de documento num PDF único, mas oferecem pacotes/ZIP de evidência, correção real da Rodada 1 que superestimava "nunca bytes"). **5 achados reais do Codex, todos verificados por leitura de código, fecharam as 4 rodadas**: (1) RBAC "ADMIN_ROLES ou assignee" não funcionaria — `authorization.ts:323` só aplica o gate de assignee quando `ownerUserId` E `assigneeUserId` existem juntos, `Requirement` não tem `ownerUserId` — fechado com **ADMIN_ROLES exclusivamente**, removendo disclosure real (assignee de 1 Requirement exportando o Subject inteiro); (2) histórico de relink de evidência não é reconstruível (`linkEvidence`/`unlinkEvidence` só sobrescrevem ponteiros atuais, sem evento próprio) — fechado como limitação explícita nomeada do v1; (3) PDF+XLSX "mesmo conteúdo" conflitava com truncamento visual — fechado declarando papéis distintos (XLSX exaustivo, PDF narrativo, nunca omite linha); (4) `POST` disparar geração imediatamente não cumpria a Journey J22 ("mostra escopo antes de exportar") — fechado com fluxo preview (`PREVIEW_READY`, sem worker)/confirm (`scopeHash` obrigatório, idempotente, 409 se divergir); (5) semântica "as of" ambígua — fechado declarando conjunto de linhas congelado no preview, valores relidos frescos na geração (`generatedAt` explícito no documento). **11 decisões finais** (detalhe completo em `estado-final-consolidado.md`): manifesto metadata-first v1 (bytes de documento explicitamente fora de escopo); `DossierExportRun` com preview/confirm/`scopeHash` (fingerprint inspirado no padrão de D-194); releitura fresca de valores na geração (convenção de D-193); sem truncamento silencioso (`DossierTooLargeError` declarado); XLSX exaustivo/PDF narrativo; sanitização Excel estendendo o precedente de `csv-export-writer.ts` (D-195); PDF sem lib de layout nova (`pdf-lib` builder, tabela determinística); download autenticado com presign sob demanda (mesmo padrão de D-204); RBAC `ADMIN_ROLES` exclusivo. **Fora de escopo, nomeado**: bundle evidenciário completo (bytes embutidos); pacote ZIP único (v1 gera 2 downloads separados); log evento-a-evento de decisões. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (nova capacidade de geração de documento + novo fluxo preview/confirm + nova rota autenticada) | 9,3/10 (Rodada 4, autoavaliação às cegas) | 9,2/10 (Rodada 4, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197/D-204.** Fatias sugeridas (não decididas): (1) `DossierExportRun`+rotas preview/confirm+`scopeHash`; (2) worker de geração (PDF+XLSX)+S3+`DossierTooLargeError`; (3) rota de download autenticada com presign sob demanda. | `docs/architecture/reviews/document-dossier-scoping/` | 2026-09-05 |
docs/architecture/decisions-log.md:263:| D-206 | **Roadmap P1 item 17 ("Bulk actions") — DESIGN, protocolo Claude↔Codex completo, 4 rodadas** (`docs/architecture/reviews/bulk-actions-scoping/`). Fecha o próximo item do backlog P1 depois de D-205 (item 16). Bulk archive foi anteriormente classificado como "product creep" por falta de base em roadmap (`docs/frontend/interface-context-and-critical-tasks.md` §30) — condição que não vale mais, roadmap agora nomeia o item formalmente. **Declaração E-014: SIM PARCIAL** (Atlassian Jira bulk operations — job assíncrono/`taskId`/polling até 1000 itens — e Asana multi-select síncrono, fontes primárias 2026-09-05; padrão de mercado é split síncrono-para-pequeno/assíncrono-para-grande, este design fica no lado síncrono por proporcionalidade de escala, teto 100 não 1000). **6 achados reais do Codex, todos verificados por leitura de código, fecharam as 4 rodadas**: (1) bulk archive é mais destrutivo (`ItemDeactivated`+cancelamento de reminders, `expiration-service.ts:795`) — fechado com confirmação UX explícita obrigatória; (2) faltava contrato `{itemId, expectedVersion}` por item — sem ele perderia a garantia OCC — fechado, obrigatório; (3) teto 100 síncrono sobre o timeout real de 10s de `items_handler` (`lambda-function/variables.tf:43`) — fechado com Lambda DEDICADA (mesmo precedente de `export-handler`, D-123/D-126), `timeout_seconds=25`, dentro da cota real de 30s do HTTP API v2 (`api-gateway/main.tf:6`, correção de "REST 29s"); (4) justificativa "export processa 2000 itens" era analogia fraca (leitura vs. transação) — fechado reclassificando o teto como hipótese de engenharia condicionada a smoke test antes de produção; (5) ambiguidade de retry pós-timeout (itens já aplicados voltariam como `VERSION_CONFLICT`) — fechado com regra de reconciliação restrita (`TARGET_ALREADY_APPLIED` só quando version avançou exatamente 1 E o estado bate EXATAMENTE com o alvo, nunca afirmando autoria da tentativa); (6) `archiveItem()` atual não bloqueia rearquivar item já `ARCHIVED` com versão válida — fechado com precondição `status === "ACTIVE"` no bulk archive (`INELIGIBLE_STATE` se não elegível), comportamento single-item existente intocado. **9 decisões finais** (detalhe completo em `estado-final-consolidado.md`): lista v1 restrita a bulk reassign+archive; confirmação UX+precondição ACTIVE-only para archive; contrato por item com `expectedVersion`; fan-out via transações pequenas independentes (padrão de `import-commit-service.ts`, D-192); reconciliação de retry sem ledger; resposta síncrona com taxonomia completa de outcome; Lambda dedicada 25s com teto 100 condicionado a validação empírica; RBAC `WRITE_ROLES` sem elevação artificial. **Fora de escopo, nomeado**: bulk delete/renew/Requirement/Document; job assíncrono/ledger (só se autoria de tentativa virar requisito); teto acima de 100. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (novo mecanismo de fan-out transacional + nova Lambda dedicada + nova rota HTTP) | 9,1/10 (Rodada 4, autoavaliação às cegas) | 9,2/10 (Rodada 4, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197/D-204/D-205.** Fatias sugeridas (não decididas): (1) Lambda dedicada+rotas HTTP+RBAC; (2) fan-out+reconciliação+taxonomia de outcome; (3) smoke test de integração medindo custo real de 100 itens antes de expor em produção. | `docs/architecture/reviews/bulk-actions-scoping/` | 2026-09-05 |
docs/architecture/decisions-log.md:266:| D-209 | **D-206 fatia 1/3 (Lambda dedicada+rotas HTTP+RBAC) — IMPLEMENTADA. Item 17 do backlog P1 agora reachable via HTTP, fecha D-206 por completo (fatias 1+2, só falta a 3 — smoke test de custo real).** Novo Lambda `bulk-actions-handler` (`timeout_seconds=25`, mesmo padrão de `export-handler`/D-123-D-126, `policy_documents_json=[tenant_facing_read_write_policy_json, gsi4_read_policy_json]` — a segunda por resolver `RequestContext` via `resolver.resolve()`, mesma regra de D-116). 2 rotas novas (`POST /items/bulk-reassign`, `POST /items/bulk-archive`) numa única integração compartilhada (padrão `reports_handler`'s "N rotas : 1 integração"), wireadas em `infra/modules/api-gateway/main.tf` (`variables.tf`+`main.tf` do módulo, `infra/main.tf`'s bloco `module.api`) e no BFF (`proxy-allowlist.ts` — mesma disciplina D-117/D-120/D-178, já que bulk actions é JSON puro, nunca esbarra no gap de `content-disposition` que mantém `/items/export`/`/reports/*` fora do BFF). 2 schemas novos (`bulk-reassign-items-request.v1.json`/`bulk-archive-items-request.v1.json`, registrados em `schema-validator.ts`) — `assigneeUserId` vazio limpa o responsável (mesma convenção de `UpdateItemRequest`), `confirm` do archive usa `const: true` (rejeita `false` e ausência, nunca infere confirmação). Novo módulo HTTP `bulk-action-handlers.ts` reusa os helpers genéricos de `item-handlers.ts` (`withErrorMapping`/`validateAgainstSchema`/`consumeApiRequestQuota`, promovidos de privados para exportados) em vez de duplicá-los — diferente de `export-handler.ts`, que precisou de um pipeline próprio só por causa do corpo CSV; bulk actions é JSON puro, então reusa o pipeline compartilhado. **Achados reais durante a implementação (nenhum exigiu rodada nova de protocolo — implementação direta de design já `APPROVED`)**: (1) `infra/modules/api-gateway/tests/api_gateway.tftest.hcl` estava com um `run` block genuinamente quebrado — faltavam `reports_invoke_arn`/`document_archive_invoke_arn`/`document_archive_guest_invoke_arn` (nunca atualizado desde D-143/D-195), preenchido com valores mock para o teste voltar a rodar (não corrigido: as contagens de rota pré-existentes `/items* == 10` vs. real 13, `/subjects* == 21` vs. 22, `/imports* == 3` vs. 5 continuam quebradas, mesma classe de drift que D-208 achou para o output `lambda_function_names`, 32 vs. 52 — nomeado, não corrigido, dívida técnica separada); confirmado que NENHUMA dessas falhas pré-existentes é bloqueante em CI (D-138: testes em `modules/*/tests/` não rodam na CI, só `infra/tests/stack.tftest.hcl` na raiz). Verificação empírica: `terraform plan` REAL contra `dev` (`AWS_PROFILE=claude-dev`) mostra exatamente **11 recursos novos** (Lambda+role+2 policies+2 attachments+alias, +integração+2 rotas+permissão) e as MESMAS 4 substituições pré-existentes já isoladas por D-208 — nada inesperado. G-V3: 8 testes novos de HTTP (`bulk-action-handlers.test.ts` — 400 em corpo ausente/inválido/campo extra/campo faltante, 200 com outcomes por item, tanto reassign quanto archive), 16 testes de `proxy-allowlist.test.ts` (2 novos + 14 preexistentes, todos verdes — achado de teste próprio: `GET /items/bulk-reassign` casa legitimamente com `{itemId}` por ambiguidade já documentada no arquivo, teste ajustado para usar `PATCH`), 10 testes de contrato novos (`schemas.test.ts`), 6 asserts novos em `api_gateway.tftest.hcl`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, suíte completa a confirmar. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO, mergeado em `main` (PR #243, `f80ace7`), CI/CD 100% verde** | `src/modules/expiration/http/{item-handlers.ts,bulk-action-handlers.ts}`, `src/runtime/aws/handlers/bulk-actions-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `schemas/api/bulk-{reassign,archive}-items-request.v1.json`, `src/shared/contracts/schema-validator.ts`, `scripts/build-lambdas.ts`, `infra/{main.tf,modules/api-gateway/{main.tf,variables.tf,tests/api_gateway.tftest.hcl}}`, `test/{unit/expiration/bulk-action-handlers.test.ts,unit/bff/proxy-allowlist.test.ts,contract/schemas.test.ts}` | 2026-09-05 |
docs/architecture/decisions-log.md:267:| D-210 | **D-206 fatia 3/3 (smoke test de custo real) — CONCLUÍDA. Item 17 do backlog P1 (bulk actions) FECHADO POR COMPLETO — as 3 fatias de D-206 (design+fan-out/reconciliação+HTTP/RBAC+validação empírica de custo) estão todas implementadas, deployadas e verificadas ao vivo.** Medição real contra o Lambda `bulk-actions-handler` JÁ DEPLOYADO em `dev` (nunca contra `InMemoryExpirationStore`/local): tenant sintético (`smoke-test-bulk-tenant`) + identidade real (`GlobalUser`/`IdentityMapping`/`Membership` ACTIVE OWNER, seedados diretamente via DynamoDB real, mesmo formato físico que `IdentityBootstrapService`/onboarding produzem) + 100 `ExpirationItem`s reais criados via `ExpirationService.createItem()` contra `dev`. **Achado real durante a primeira tentativa de medição**: chamar o SERVIÇO diretamente da máquina local (sem passar pelo Lambda) mediu 35.479ms para 100 itens — ACIMA do orçamento de 25s — mas essa medição inclui overhead de rede pública (máquina local→AWS us-east-1), não reflete o custo real dentro do Lambda (mesma região que o DynamoDB). Corrigido invocando o Lambda REAL via `aws lambda invoke` (`--qualifier live`) com um evento HTTP API v2 sintético (claims JWT construídos à mão, nunca passam pelo API Gateway/Cognito de verdade — o Lambda não reverifica assinatura, só o API Gateway faz isso antes de invocar), medindo o tempo de invocação real de ponta a ponta. **Resultado final, definitivo**: `bulkReassignItems` para 100 itens contra o Lambda real levou **8.982ms (35,9% do orçamento de 25.000ms)** — 100/100 outcomes `SUCCEEDED`, confirmando o teto `BULK_ACTION_ITEM_CAP=100` é seguro com margem saudável (~64% de folga) no timeout atual de 25s. Achado colateral real, não um bug: uma primeira tentativa de medição com um `assigneeUserId` sintético não-membro corretamente retornou `INELIGIBLE_ASSIGNEE` para os 100 itens (RBAC/elegibilidade real funcionando, não um erro de teste) — corrigido reusando o próprio usuário seedado (membro real elegível) como assignee, produzindo a medição válida acima. Dados sintéticos limpos ao final (606 linhas deletadas via scan+delete direto, confirmado 0 linhas restantes) — scripts de smoke test eram throwaway, nunca commitados (`scripts/_smoke-*.ts`, removidos após o uso, `AGENTS.md` §1: dev 100% sintético/resetável). | Nível 1-2 (validação empírica de hipótese de engenharia já nomeada por D-206, sem decisão nova) | — | — | **CONCLUÍDO — item 17 do backlog P1 FECHADO POR COMPLETO (D-206/D-207/D-209/D-210)** | nenhum arquivo do repo alterado (só medição ao vivo contra `dev`, scripts throwaway nunca commitados) | 2026-09-05 |
docs/architecture/decisions-log.md:270:| D-213 | **ReportSubscription CRUD (create/get/list/delete) — IMPLEMENTADA.** Fecha a lacuna nomeada por D-212: sem uma rota HTTP de criação, nenhuma assinatura real podia existir para o scheduler/worker de entrega processarem. D-204 decisão 1 previa "criação/gestão via rota HTTP ADMIN_ROLES", mas a quebra em fatias sugerida (§"Próxima ação" do estado-final-consolidado) não incluía essa rota como fatia própria — decisão de sequenciamento tomada aqui, não uma decisão de arquitetura nova. **Superfície deliberadamente estreita para v1**: create/get/list/delete apenas, SEM update — um assinante que queira mudar reportTypes/horário/destinatários apaga e recria, mesma disciplina "não inventar superfície que o design aprovado não pediu" de `bulk-actions-scoping`'s v1 (só reassign+archive). `ReportSubscriptionService` nova (`authorize()`+`executeTenantBusinessMutation` no create/delete, mesmo pipeline de `DocumentArchiveService.createDocumentType`/`getDocumentType`) — nova action RBAC única `reports:subscription-manage` (`ADMIN_ROLES`, mesmo tier de `docarchive:requirement-export`/`item:export` — uma assinatura nomeia OUTROS membros como destinatários e produz os mesmos CSVs bulk cross-membro que essas actions já protegem, não o tier mais permissivo de "ler o próprio recurso"). **Achado real durante a fatia**: `ReportSubscription` (D-211) não tinha nenhum índice de listagem — cada assinatura é sua própria partição (`PK=TENANT#<t>#REPORTSUB#<subId>`), sem GSI nenhum "lista todas do tenant" seria impossível; corrigido adicionando `GSI1PK=TENANT#<t>#REPORTSUB`/`GSI1SK=<createdAt>#REPORTSUB#<id>` ao GSI1 compartilhado (mesmo índice físico que Document/ExpirationItem/Requirement/DocumentType/RequirementTemplate já discriminam por prefixo — nenhum índice novo), reusando exatamente o padrão `documentTypeGsi1Keys` já estabeleceu para o catálogo de DocumentType. `createSubscription` computa `nextRunAt` inicial via `nextWeeklyOccurrenceUtc(now, ...)` (D-212) — a mesma primitiva que o scheduler usa para avançar a cada claim, então uma assinatura recém-criada já é descobrível pelo GSI8 sem caso especial de "primeira execução". `delete` é físico (não soft-archive) — `buildVersionedDelete` + fence do tenant, releitura fresca ANTES do delete para distinguir `NotFoundError` (nunca existiu) de `ConflictError` (`expectedVersion` obsoleto), nunca assumindo causa que o DynamoDB não revelou (mesma postura RT-LANE-FALLBACK-01). Rotas `POST /reports/subscriptions`, `GET /reports/subscriptions`, `GET /reports/subscriptions/{subscriptionId}`, `POST /reports/subscriptions/{subscriptionId}/delete` — MESMO Lambda/integration `reports-handler` que já serve os 7 relatórios CSV (D-195), discriminado por `routeKey` no entrypoint (nunca confundido com a resposta CSV — `isCsvResponse()` já existia para esse propósito); 4 entradas novas em `proxy-allowlist.ts` (disciplina D-117/D-120/D-178/D-209 — diferente das 7 rotas CSV, que são deliberadamente NÃO proxyadas pela BFF por causa do `Content-Disposition`, estas são JSON puro e não têm esse problema). 2 schemas JSON novos (`report-subscription-create-request.v1.json`/`-delete-request.v1.json`) registrados em `schema-validator.ts`. G-V3: 12 testes de `ReportSubscriptionService` (create com nextRunAt correto/RBAC/validação de domínio+dayOfWeek+localTime+timeZone, get/list via GSI1 ordenado, delete com NotFoundError/ConflictError), 10 de HTTP handlers (400 em corpo ausente/schema inválido/param ausente, 404/409 reais), 2 novos de `proxy-allowlist`, 10 de contrato para os 2 schemas — **80 testes novos** nesta fatia. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `terraform fmt`/`validate` limpos; `terraform plan` real contra `dev` confirmou as 4 rotas novas como `+ create` limpo (os mesmos 4 `-/+ replace` de `aws_lambda_permission` pré-existentes de D-212 reaparecem, não relacionados); suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS consumindo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`+rota de download; wiring completo do outbox) seguem não implementadas — uma assinatura agora pode ser criada e será reivindicada pelo scheduler (D-212) no próximo tick devido, mas nenhum e-mail real é enviado até a fatia 3 existir. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de sequenciamento — rota HTTP como fatia própria, não prevista nas 4 fatias sugeridas — e 1 de modelagem de índice — GSI1 novo para ReportSubscription, mecânico/reuso de padrão já estabelecido) | — | — | **IMPLEMENTADO (create/get/list/delete)** — pendente fatias 3-4 de D-204, PR/merge/CD | `src/modules/reports/{domain/report-subscription.ts,application/report-subscription-service.ts,application/id-generator.ts,http/reports-handler.ts,ports/report-subscription-store.ts,persistence/dynamodb-report-subscription-store.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{ids.ts,composition/reports.ts,handlers/reports-handler.ts}`, `src/shared/contracts/schema-validator.ts`, `schemas/api/report-subscription-{create,delete}-request.v1.json`, `infra/modules/api-gateway/main.tf` | 2026-09-05 |
docs/architecture/decisions-log.md:273:| D-216 | **D-205 fatia 1 (`DossierExportRun` + preview/confirm HTTP + `scopeHash`) — IMPLEMENTADA.** Começa a tirar o item 16 do backlog P1 (dossiê documental PDF/Excel) do design-only. `DossierExportRun` (`src/modules/document-archive/domain/dossier-export-run.ts`) co-localizado na MESMA partição do Subject (`PK=TENANT#<t>#SUBJECT#<subjectId>`, `SK=DOSSIER#<runId>`) — mesma convenção que `requirementKey()` já estabelece, mesmo racional (o ciclo de vida do export pertence ao Subject). `POST /document-archive/subjects/{subjectId}/dossier` cria o run em `PREVIEW_READY`, congelando os `requirementIds` ATUAIS do Subject (decisão 4 — nunca redderivados depois) e devolvendo as linhas de preview na MESMA resposta. `POST .../dossier/{runId}/confirm` é idempotente dado um `scopeHash` batendo (fingerprint canônico JSON+SHA-256 sobre `subjectId`+`requirementIds` ordenados) — confirmar um run JÁ confirmado com o MESMO hash é no-op de sucesso, nunca um segundo dispatch; um hash divergente é SEMPRE 409, independente do status atual (decisão 3). Um confirm bem-sucedido dispara um destino de outbox novo (`SQS_DOSSIER_EXPORT_V1`, aditivo, sem consumidor ainda — mesmo padrão "valor existe antes do consumidor" de `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`/D-211). RBAC: action nova `docarchive:dossier-export`, `ADMIN_ROLES` EXCLUSIVAMENTE (decisão 10 — a Rodada 1 do Codex já tinha achado real que "ADMIN_ROLES ou assignee" não seria expressável estruturalmente, já que `Requirement` não tem `ownerUserId`). Achado de reuso fechado no caminho: o mecanismo de fingerprint canônico JSON+SHA-256 que `search-cursor.ts` (D-194 Fatia 3) já usa para o cursor de busca foi extraído para `shared/domain/fingerprint.ts` novo, reusado por `scopeHash` em vez de duplicar a canonicalização. Métodos novos (`previewDossierExport`/`confirmDossierExport`) adicionados à MESMA `DocumentArchiveService` já gigante (não um serviço novo — a lógica reusa diretamente `listRequirements`/`trackedSubjectKeyForFence` já existentes na classe, mesmo racional de `getSubjectCompliance`). G-V3: 10 testes de serviço (`dossier-export.test.ts` — congelamento de requirementIds, scopeHash determinístico, 404 de Subject inexistente, idempotência do confirm, 409 de hash divergente mesmo após confirmado, RBAC) + 5 de HTTP (`document-archive-dossier-routes.test.ts` — preview→confirm ponta a ponta via handlers reais, 400 de scopeHash ausente, 409, 404, 403 de VIEWER) + 3 de contrato do schema novo + 2 de `proxy-allowlist`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2392/2392** (217 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate` limpos; `terraform plan` real contra `dev` mostrou exatamente as 2 rotas novas esperadas (mais o mesmo baseline pré-existente de 4-replace/110-change já nomeado em sessões anteriores). PR #250 mergeado (`866a762`), CD (`34046861084`) `completed`/`success` confirmado por polling síncrono do `databaseId` capturado ANTES do merge. **Verificação ao vivo contra `dev` feita**: `exptrk-dev-document-archive-handler` `Active`/`Successful` (`LastModified` batendo com o horário deste deploy); as 2 rotas `POST /document-archive/subjects/{subjectId}/dossier`/`.../dossier/{runId}/confirm` confirmadas presentes na API `uav7id1muh`. **Pendente real, nomeado, não bloqueante**: fatia 2 (worker de geração PDF+XLSX via `pdf-lib`+nova dependência de Excel, `DossierTooLargeError`, S3) e fatia 3 (rota de download autenticada com presign sob demanda, mesmo padrão de D-204) seguem não implementadas — hoje dá pra fazer preview/confirm, mas nenhum documento é gerado nem baixável ainda. **Próxima ação real**: fatia 2 de D-205, ou escopar itens 18-19 do backlog P1, a critério de quem retomar. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 1 de D-205)** — pendente fatias 2-3 | `src/modules/document-archive/{domain/dossier-export-run.ts,application/document-archive-service.ts,application/id-generator.ts,http/document-archive-handlers.ts}`, `src/shared/domain/{fingerprint.ts,search-cursor.ts}`, `src/shared/outbox/outbox.ts`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{ids.ts,handlers/document-archive-handler.ts}`, `src/shared/contracts/schema-validator.ts`, `schemas/api/docarchive-dossier-confirm-request.v1.json`, `infra/modules/api-gateway/main.tf` | 2026-09-06 |
docs/architecture/decisions-log.md:274:| D-217 | **D-205 fatia 2-3 (worker de geração PDF+XLSX + rota de download autenticada) — IMPLEMENTADA. Item 16 do backlog P1 (dossiê documental) FECHADO POR INTEIRO (D-205 completo, fatias 1-3).** `processDossierExportGeneration` (`src/workers/dossier-export/generate.ts`) consome `SQS_DOSSIER_EXPORT_V1` (dispatch já existia desde D-216, sem consumidor): reclama `CONFIRMED→GENERATING` via lease (`generatingLeaseExpiresAt`, mesmo padrão de `NotificationAttempt`'s SUBMITTING — reclama um run travado cuja lease expirou, pula silenciosamente um redelivery ainda dentro da lease), busca os dados via `documentArchive.getDossierExportData` (chamada ÚNICA — achado próprio corrigido antes do commit: rascunho inicial chamava duas vezes, uma só para `subjectDisplayName`), declara `TOO_LARGE` (nunca um artefato truncado silenciosamente) quando o escopo CONGELADO excede `MAX_DOSSIER_REQUIREMENTS=200`, omite silenciosamente (nunca fabrica) um `Requirement` apagado desde o confirm, gera PDF (`dossier-pdf-builder.ts`, `pdf-lib` — primeiro uso do projeto para CRIAR, não só parsear, um PDF; paginação real via `font.widthOfTextAtSize`, nunca descarta uma linha) e XLSX (`dossier-xlsx-builder.ts`, dependência nova `exceljs@4.4.0` exact-pinned; 2 abas, nunca trunca célula) EM PARALELO, sobe ambos ao S3 (bucket `report_exports` REUSADO sob prefixo próprio, nenhum bucket novo — achado de reuso explícito), marca `READY`+`generatedAt`, limpa a lease; erro no meio marca `FAILED`+`failureReason` e RELANÇA (nunca engole). Mitigação de injeção de fórmula (`sanitizeFormulaInjection`, apóstrofo líder em `=+-@`) extraída para `shared/spreadsheet/formula-injection.ts` e aplicada tanto no XLSX novo quanto retroativamente ao CSV existente (`csv-export-writer.ts`) — achado de segurança fechado no caminho, não só no código novo. Rota nova `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download?format=pdf|xlsx` — mesmo padrão de presign sob demanda de 5 min de D-204/D-215 (nunca embute link de S3 de TTL longo), 409 se o run não está `READY`, 400 para `format` fora de `pdf`/`xlsx`, RBAC `docarchive:dossier-export` `ADMIN_ROLES` EXCLUSIVAMENTE (mesma decisão 10 de D-216, sem fallback de destinatário — sensibilidade de divulgação diferente da rota de relatórios). Infra nova: fila+DLQ `dossier-export` (`sqs-worker-queue`, `consumer_timeout_seconds=120`); Lambda `dossier-export-generation-handler` (`timeout=120s`, env `QUARANTINE_BUCKET_NAME`+`REPORT_EXPORTS_BUCKET_NAME`); `document_archive_handler` ganha `REPORT_EXPORTS_BUCKET_NAME`+policy de leitura S3. **Achado mecânico fechado no caminho**: `workbook.xlsx.load(buffer)` falhava o typecheck do arquivo de teste (`Buffer<ArrayBufferLike>` do `@types/node` aninhado do `exceljs` estruturalmente diferente do `Buffer` do próprio repo) — contornado com um helper `loadWorkbook` que passa pelo cast `unknown`, escopado só ao arquivo de teste. G-V3: 4 testes de `dossier-xlsx-builder.test.ts` + 5 de `dossier-pdf-builder.test.ts` + 8 de `dossier-export-generation.test.ts` (happy path, `RUN_NOT_FOUND`, idempotência `ALREADY_RESOLVED`, lease não expirada `SKIPPED_IN_PROGRESS`, reclamação de lease expirada, `TOO_LARGE`, requirement apagado omitido, `FAILED`+rethrow) + 5 novos de download em `document-archive-dossier-routes.test.ts` (total 10 no arquivo) + extensão de `proxy-allowlist.test.ts`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2415/2415** (220 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate`/`plan` limpos contra `dev`. PR #252 mergeado (`bc35727`); CD (`34050910874`) falhou uma vez por contenção real de lock do state Terraform contra `dev` (4 runs de CI concorrentes de branches Dependabot rodando `terraform plan` simultaneamente no mesmo backend S3/DynamoDB — não um defeito de código; diagnosticado via `gh run view --log-failed`, resolvido aguardando os 4 runs concorrentes concluírem e rodando `gh run rerun --failed`, sem precisar do `force-unlock` pré-autorizado por §7) — rerun `completed`/`success` confirmado. **Verificação ao vivo contra `dev`, todos os pontos confirmados**: `exptrk-dev-document-archive-handler`/`exptrk-dev-dossier-export-generation-handler` `Active`/`Successful` (`LastModified` batendo com o horário deste deploy); fila+DLQ `exptrk-dev-dossier-export`/`-dlq` existem; event source mapping `Enabled` (via `--event-source-arn`, mesmo achado de ferramenta já nomeado em D-215); rota `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download` presente na API `uav7id1muh`. **Item 16 do backlog P1 agora COMPLETO por inteiro** (fatias 1-3 de D-205 fechadas) — ainda não testado ponta a ponta com um preview→confirm→geração real disparando via SQS real em `dev` (só G-V3/unit + smoke de infra), próxima sessão pode considerar esse smoke test antes de considerar a feature 100% validada, ou seguir para os itens 18-19 do backlog P1 (metadata configurável por Document Type; compartilhamento externo seguro). | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO POR COMPLETO (D-205 FECHADO, item 16 do backlog P1 fechado por inteiro)** | `src/workers/dossier-export/generate.ts`, `src/modules/document-archive/{application/document-archive-service.ts,application/dossier-pdf-builder.ts,application/dossier-xlsx-builder.ts,http/document-archive-handlers.ts,ports/dossier-export-store.ts,persistence/s3-dossier-export-store.ts,domain/dossier-export-run.ts}`, `src/shared/{spreadsheet/formula-injection.ts,csv/csv-export-writer.ts,errors/app-error.ts}`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition/document-archive.ts,composition/reminder.ts,handlers/document-archive-handler.ts,handlers/dossier-export-generation-handler.ts,handlers/dispatch-outbox-relay-handler.ts,handlers/outbox-sweeper-handler.ts}`, `infra/main.tf`, `infra/modules/api-gateway/main.tf`, `scripts/build-lambdas.ts`, `package.json` | 2026-09-06 |
docs/architecture/decisions-log.md:279:| D-223 | **WhatsApp Operacional (roadmap P0.3) — fatia 1/5 do programa de implementação: widening de tipo (D-1) + entidades novas (D-5/D-6), SEM HTTP exposto. Design já `APPROVED` desde D-197 (protocolo Claude↔Codex completo, 3 rodadas, `docs/architecture/reviews/whatsapp-channel-scoping/estado-final-consolidado.md`) — implementação direta, sem nova rodada de protocolo (nível 3-4).** `NotificationAttempt.channel`/`.provider` e `NotificationAttemptLookup.provider` alargados de literal (`"EMAIL"`/`"SES"`) para união (`NotificationChannel` de `notification-intent.ts`, reusado em vez de redefinido; `NotificationProvider = "SES"\|"META_CLOUD_API"` novo) — aditivo, toda linha existente permanece um subconjunto válido do tipo novo, `SUPPORTED_CHANNELS` em `notification-router.ts` continua `["EMAIL"]` (roteamento real do WhatsApp é fatia 2+, esta fatia só alarga o TYPE). `WhatsAppOptIn` novo (`src/modules/notification/domain/whatsapp-opt-in.ts`): `PK=TENANT#<t>#USER#<userId>`, `SK=WHATSAPP_OPTIN#<phoneE164>` — a chave inclui o telefone por construção (D-5), então uma troca de número cria uma linha NOVA em vez de mutar a antiga, tornando "consentimento é escopado ao telefone ATUAL" verdadeiro sem invalidação explícita nenhuma; `buildWhatsAppOptIn()` valida formato E.164 e lança `ValidationError` ANTES de qualquer I/O. `WhatsAppOptInService.recordOptIn()` novo (`putIfAbsent` create-once, autorização `notification:configure` — mesma ação/READ_ONLY_ROLES de `NotificationPreferencesService`, usuário gerenciando o PRÓPRIO consentimento) — deliberadamente sem rota HTTP ainda (fatia 1 é domínio+entidades, a rota vem numa fatia futura sem mudar a forma desta classe). `GlobalUser.phoneE164?: string` novo (D-6) + `GlobalUserRepository.setPhoneNumber()` novo, valida E.164 antes de ler/escrever, lança `ValidationError` (formato) ou `NotFoundError` (usuário inexistente) — mesmo padrão read-then-`update()` sem OCC que `logoutAll()` já usa nesta classe (propriedade preexistente, não introduzida agora). Validador `isValidE164()` promovido a `src/shared/text/phone-e164.ts` (mesma convenção `shared/text/` de `normalizeDisplayName`, reusado por `WhatsAppOptIn` e `GlobalUser` sem violar fronteira de módulo — `check-boundaries` confirmou 0 violações). **Achado de processo real desta sessão, não de produto**: outro agente trabalhava em paralelo na mesma árvore de trabalho (sem isolamento de worktree, item 9 do roadmap P0, commit não relacionado `D-222`) com 2 arquivos temporariamente quebrando `npm run typecheck` (`export` duplicado em `document-archive-guest-handlers.ts`, módulo `exceljs` não resolvido) no momento desta sessão — confirmado por `git stash` seletivo (só os arquivos desta fatia) que os 2 erros são pré-existentes/alheios, não introduzidos aqui; gate desta fatia verificado com typecheck isolado (stash) limpo, além de `lint`/`check-boundaries`/`check-docs`/`validate-schemas` rodados no estado real da árvore (limpos) e a suíte completa de `test/unit/notification`+`test/unit/identity` (201/201) mais os 18 testes novos desta fatia, todos verdes; `npm test` completo NÃO pôde ser confirmado 100% verde nesta sessão por causa do WIP alheio ainda em andamento no momento do commit — não é uma falha desta fatia, é um estado transitório da árvore compartilhada. G-V3 real: chave de `WhatsAppOptIn` muda com o telefone (prova por teste que duas chaves com telefones diferentes nunca colidem); opt-in idempotente preserva `optedInAt` original mesmo numa segunda chamada com timestamp posterior; `setPhoneNumber`/`buildWhatsAppOptIn` rejeitam formato inválido ANTES de qualquer leitura/escrita (nenhuma linha parcial persistida). **Nenhuma rota HTTP, worker, fila, secret ou infra Terraform tocados nesta fatia** — nada disso é aplicável/verificável ao vivo contra `dev` ainda, por design (fatia domain-only). **Próxima ação real: fatia 2/5** — `WhatsAppProviderAdapter` (D-2, mesma forma de porta de `EmailProviderAdapter`) + `WhatsAppDeliveryWorker` + fila/DLQ dedicada `whatsapp-deliver-queue`/schema `notification.whatsapp-deliver.v1` (D-9) — mapeamento exato dos códigos de erro reais da Cloud API fica para essa fatia, é uma pendência nomeada no design, não desta. | Nível 3-4 (implementação de design já `APPROVED` nível 5-6, sem decisão nova) | — | — | **IMPLEMENTADO (fatia 1/5, domínio+tipos apenas — fatias 2-5 pendentes)** | `src/modules/notification/domain/{notification-attempt.ts,whatsapp-opt-in.ts}`, `src/modules/notification/application/whatsapp-opt-in-service.ts`, `src/modules/identity/persistence/global-user-repository.ts`, `src/shared/text/phone-e164.ts`, `test/unit/{shared/phone-e164.test.ts,notification/whatsapp-opt-in{,-service}.test.ts,identity/global-user-repository.test.ts}` | 2026-09-07 |
docs/architecture/decisions-log.md:281:| D-224 | **Rota pública de descoberta de DocumentTypes para o guest flow (item 6 de D-173's `estado-final-consolidado.md`, fatia de engenharia isolada da pendência de produto) — IMPLEMENTADA.** Contradição real encontrada e registrada aqui: o texto do item 6 (D-173) pedia tornar `documentTypeId` OBRIGATÓRIO no schema HTTP do guest submit e remover `documentType`; mas D-184 (protocolo completo, 3 rodadas, `APPROVED`, posterior a D-173) já havia REJEITADO essa obrigatoriedade, com o achado real de que nem `DocumentRequest` nem `Requirement` referenciam um `DocumentType` esperado — não existia nenhum mecanismo de descoberta para um guest saber qual `documentTypeId` é válido, então tornar o campo obrigatório sem esse mecanismo quebraria o fluxo sem alternativa. D-184 implementou em vez disso validação CONDICIONAL (`documentTypeSupplied = input.documentType !== undefined`): quando fornecido, valida via `ConditionCheck` transacional contra `DocumentType.status=ACTIVE`; quando ausente, fallback antigo para `requirementId` sem validação — postura preservada integralmente por esta sessão, nenhuma linha de `submitEvidence()` foi tocada. Esta sessão implementa exatamente o mecanismo de descoberta que faltava (a peça que o texto do item 6 não previa como pré-requisito): `GET /document-archive/guest/document-requests/{token}/document-types`, novo método `GuestDocumentAccessService.listActiveDocumentTypesForGuest()` (mesma disciplina de `resolveCredential` — parse→rate-limit-antes-de-lookup→dummy-compare anti-timing→expiry/revocação→liveness do `DocumentRequest` — reescrita inline em vez de reusar `resolveCredential` porque este método não pode chamar `markOpened()`, efeito colateral que só `resolveCredential` deve ter), lendo o catálogo via `store.queryIndexPage` direto na mesma namespace GSI1 DOCTYPESTATUS de `DocumentArchiveService.listDocumentTypes()` — SEM `authorize()`/`RequestContext`, mesma postura de todo o arquivo (confirmado lendo o cabeçalho antes de escrever). Handler novo `handleListGuestDocumentTypes` em `document-archive-guest-handlers.ts`, mesmo padrão anti-enumeração dos outros 3 handlers guest (token inválido/expirado/revogado/rate-limitado colapsa no mesmo `GuestAccessInvalidError` genérico). Rota wireada no mesmo Lambda `document_archive_guest` (`authorization_type=NONE`) em `infra/modules/api-gateway/main.tf` — `source_arn` do `aws_lambda_permission.document_archive_guest` já cobre via wildcard `.../document-archive/guest*`, nenhuma permission nova necessária. `proxy-allowlist.ts` DELIBERADAMENTE não tocado — todo `/guest/*` é excluído de lá por design (confirmado no comentário do cabeçalho do arquivo antes de pular o passo). Nenhum schema de request registrado (rota GET sem corpo, mesmo padrão de toda rota GET existente em `schema-validator.ts`). **Pendência de produto registrada, NÃO decidida nesta sessão**: tornar `documentTypeId` obrigatório em `schemas/api/docarchive-guest-submit-evidence-request.v1.json` (removendo o `documentType` livre antigo) agora se torna estruturalmente viável — o guest pode listar tipos ATIVOS antes de submeter — mas continua sendo uma mudança de contrato HTTP público que pode quebrar integrações guest existentes funcionando hoje sem o campo; decisão de produto de Marcelo, não decidida unilateralmente por engenharia nesta sessão. Schema do guest submit permanece INTOCADO, fluxo guest continua aceitando `documentType` opcional exatamente como D-184 deixou. G-V3: 5 testes novos em `guest-document-access-service.test.ts` (g1: token válido retorna só tipos ACTIVE do tenant certo; g2: nunca vaza DocumentType de outro tenant mesmo com ids colidentes entre tenants; g3: DEPRECATED nunca aparece na lista, incluindo a transição real de GSI1PK/GSI1SK que `flipDocumentTypeStatus()` faz — não só a flag `status`; g4: token malformado/expirado rejeita com o mesmo `GuestAccessInvalidError` genérico; g5: `DocumentRequest` já CANCELLED rejeita mesmo com credencial bem-formada). Gate local: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; `terraform fmt`/`validate` limpos (`AWS_PROFILE=claude-dev`, `terraform init -backend=false` local, sem plan contra `dev` real nesta sessão); suíte completa `npm test` rodada 2x — a primeira leva mostrou 3 falhas causadas por WIP alheio já presente na árvore de trabalho ANTES desta sessão começar (D-223, WhatsApp item 3, commit ainda não feito no momento desta leitura: erro de typecheck pré-existente em `test/unit/identity/global-user-repository.test.ts`, confirmado por `git stash` seletivo dos arquivos desta tarefa que o erro persiste sem eles), não relacionado a este trabalho; os testes desta tarefa especificamente (`guest-document-access-service.test.ts`, 23/23) passam limpos isoladamente. | Nível 3 (implementação de mecanismo já previsto/necessário por D-184, rota nova aditiva, nenhum contrato público existente alterado) | — | — | **IMPLEMENTADO (fatia de engenharia) — pendência de produto explícita não decidida (documentTypeId obrigatório no schema guest)** | `src/modules/document-archive/application/guest-document-access-service.ts`, `src/modules/document-archive/http/document-archive-guest-handlers.ts`, `src/runtime/aws/handlers/document-archive-guest-handler.ts`, `infra/modules/api-gateway/main.tf`, `test/unit/document-archive/guest-document-access-service.test.ts` | 2026-09-07 |
docs/architecture/decisions-log.md:282:| D-225 | **Compartilhamento Externo Seguro (`ExternalShareLink`, item 19/último item do backlog P1) — design `APPROVED` via protocolo Claude↔Codex, 4 rodadas reais (`codex exec`), régua recalibrada 3 vezes (v1→v2→v3). Fechamento na Rodada 4: Codex nota cega pesquisa 9,3/10, design 9,2/10 (ambos ≥9,0, sem arredondar).** DESIGN-ONLY, nenhum código/schema/infra tocado. E-014 `SIM` (OWASP Forgot Password/Authentication Cheat Sheets para entropia/anti-enumeration; Dropbox/Google para divergência real de postura de expiração de link; AWS Prescriptive Guidance/S3 User Guide, fonte primária, para a limitação estrutural de não-revogabilidade de URLs presigned antes do TTL — confirma que a janela residual de revogação não é falha de design, é propriedade documentada do mecanismo AWS já usado em D-215/216/217). Limitação de pesquisa registrada explicitamente: sem fonte oficial equivalente de Box/Notion/Figma localizada dentro do orçamento das 4 rodadas. **Achado real de leitura direta que mudou o design nas Rodadas 3-4**: existe um precedente MAIS específico que `guest-token.ts` (D-037) — `guest-document-access-service.ts`/`request-access-credential.ts` (D-143, document-archive), já com a ordem anti-enumeração correta (parse→rate-limit ANTES do lookup→lookup→comparação dummy-safe) e `DocumentArchiveGuestRateLimiter.consumeBoth` (seletor+IP) — reaproveitado literalmente em vez do precedente genérico. 3 rodadas de achados reais fechados progressivamente: Rodada 1→2 (17 achados: revogação vs. presign já emitido, ausência de regra determinística de qual `DocumentFile` servir, vínculo referencial não protegido transacionalmente, token em query string, `shareId` vs. selector sem reconciliação, criação não atômica, rate limiter mal citado, comparação dummy ausente, estado `EXPIRED` sem transição real, contradição de default de TTL, semântica "CURRENT" ampliando acesso sem novo consentimento, pendência de senha tratada inconsistentemente, auditoria incompleta com contadores sem writer, IP hash subespecificado, tenant lifecycle não revalidado na leitura anônima, metadados podem vazar dado tenant-controlled, sem cap/listagem de governança); Rodada 2→3 (nomes de atributo errados confirmados por leitura — `DocumentFile.scanStatus` não `status`, `DocumentVersion.state` não `status` —, IP cru persistido no rate limiter como dívida pré-existente herdada, `security-audit.ts` é CloudWatch estruturado não um stream consultável); Rodada 3→4 (contador atômico de cap sem reconciliação de expiração natural — corrigido com reconciliação lazy limitada a 5 itens na própria tentativa de criação —, semântica de revalidação incompleta após `DocumentVersion`/`Document` mudarem de estado — resolvida declarando explicitamente que o link é uma "cópia congelada" que NUNCA revalida esses dois campos, só `scanStatus`/`cleanObject`/tenant/status/expiração —, transporte do token corrigido de query string para PATH). **Decisão final sobre a pendência de senha (não é mais pendência de produto aberta)**: v1 é bearer-token puro, por consistência com uma decisão de produto já convergida no mesmo módulo (`guest-document-access-service.ts` já aceita acesso anônimo bearer-only para a mesma categoria de dado sensível, sentido inverso — upload em vez de leitura); senha/PIN opcional nomeada como fatia futura não bloqueante. Evidência completa das 4 rodadas em `docs/architecture/reviews/external-sharing-scoping/` (9 arquivos + `estado-final-consolidado.md`). | Nível 5-6 (`change-risk-scale.md` — acesso anônimo/não-autenticado a recurso do tenant, postura de segurança nova) | 9,4 (autoavaliação implícita na convergência da Rodada 4, nunca contestada pelo Codex) | 9,2 | **APPROVED (design)** — item 19 do backlog P1 (último item pendente) escopado por inteiro; nenhuma implementação de código feita | `docs/architecture/reviews/external-sharing-scoping/{round1-claude-proposal,round1-codex-critique,round2-claude-revision,round2-codex-critique,round3-claude-final,round3-codex-critique,round4-claude-revision,round4-codex-critique,estado-final-consolidado}.md` | 2026-09-07 |
docs/architecture/decisions-log.md:286:| D-229 | **WhatsApp Operacional fatia 2/5 (D-197, `whatsapp-channel-scoping/estado-final-consolidado.md` D-2/D-9) — IMPLEMENTADA.** Implementação direta de design já `APPROVED` (nível 3-4, sem decisão nova). `WhatsAppProviderAdapter` (`src/modules/notification/ports/whatsapp-provider.ts`) espelha `EmailProviderAdapter` campo a campo (`send()`→`SendResult`/`SendError{kind: CONCLUSIVE_RETRYABLE\|CONCLUSIVE_TERMINAL\|AMBIGUOUS}`). `WhatsAppCloudApiAdapter` (`providers/whatsapp-cloud-api-adapter.ts`) implementa a chamada HTTP real (Node 24's `fetch` global, sem dependência nova) contra a Graph API do Meta, com mapeamento real dos códigos de erro documentados (190/OAuthException→TERMINAL, 131056/131057/429→RETRYABLE, 131026/131047/132000/132001→TERMINAL, 5xx→RETRYABLE, resposta não-JSON/network error/timeout→AMBIGUOUS nunca assumido seguro). **Nota de credenciais (fronteira explícita desta fatia)**: `WhatsAppCloudApiConfig` já é a forma final que vai receber a config de Secrets Manager na fatia 3/5 (D-10) — hoje a fonte é só uma env var placeholder (`WHATSAPP_ACCESS_TOKEN`, default `""`), documentado no cabeçalho do adapter e em `buildWhatsAppDeliveryDeps`; a MUDANÇA de fatia 3/5 é só a fonte da credencial, nunca a forma do adapter. `WhatsAppDeliveryWorker`/`processWhatsAppDelivery` (`application/whatsapp-delivery-workflow.ts`) reaproveita literalmente `decideSendAction`/`nextStatusAfterSendAttempt` de `email-delivery.ts` (já eram genéricos, nunca específicos de SES, apesar do nome do arquivo) via `whatsapp-delivery.ts` — mesma disciplina de lease/OCC/fence W3-07 (D-067) do e-mail, mesmo tratamento AMBIGUOUS→UNKNOWN nunca retry automático. Resolução de destinatário: `ResolvedRecipient.phoneE164` novo (mesma leitura de `GlobalUser` que já produz `email`) + `resolveRecipientPhone` (composition) que SÓ retorna o telefone quando um `WhatsAppOptIn` existe para o telefone ATUAL (D-5 — troca de número invalida opt-in anterior por construção, nunca checagem explícita). Fila+DLQ dedicada `whatsapp-deliver-queue` (módulo genérico `sqs-worker-queue`, `maxReceiveCount=5` fixo do módulo) — NUNCA reaproveita `email-deliver-queue` (ADR-0008). Destino de outbox novo `SQS_NOTIFICATION_WHATSAPP_V1`, schema `notification.whatsapp-deliver.v1` (mesma forma de `notification-email-deliver.v1.json`, registrado em `schema-validator.ts`), `buildWhatsAppOutboxRecord` (`application/whatsapp-outbox.ts`) análogo a `buildEmailOutboxRecord` mas em arquivo próprio (o router NÃO o chama ainda — fatia 5/5). `notification-whatsapp-outbox-relay-handler.ts` reaproveita literalmente `processStreamRecords`/`relayStreamRecord` (já genéricos por `destination`, sem duplicação). `outbox-sweeper-handler.ts` ganha o destino novo (11º) na mesma role privilegiada compartilhada, mesmo padrão de toda extensão anterior. **DELIBERADAMENTE NÃO TOCADO**: `notification-router.ts`'s `SUPPORTED_CHANNELS` (continua só `EMAIL` — fatia 5/5); webhook handler/Secrets Manager (D-7/D-10, fatia 3/5); quota de 24h (D-8, fatia 4/5); kill switch/RBAC final (fatia 5/5). Mecanismo alcançável hoje só por chamada direta/teste, nunca pelo fluxo real de notificação. **Achado de processo (colisão de numeração)**: D-228 já estava em uso concorrente (não isolado por worktree) por outra sessão implementando o worker de entrega de credencial de guest (item 9 do roadmap P0) no mesmo instante — confirmado contra `decisions-log.md` committed antes de escrever esta linha; toda referência interna desta fatia usa D-229, nunca D-228. G-V3: mapeamento adversarial completo do adapter (13 testes — 2xx sem message id, cada família de código de erro Graph API, HTTP 429/500 sem corpo parseável, corpo não-JSON, `fetch()` lançando, forma exata do payload HTTP incluindo `biz_opaque_callback_data`); idempotência/estrutura do worker (12 testes, mesmo roteiro adversarial de `email-delivery-workflow.test.ts` — lease ativo/expirado, staleness→REPLACEMENT, fence W3-07 DELETING, falha conclusiva vs. ambígua); `buildWhatsAppOutboxRecord` validado contra o próprio schema registrado. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2548/2548** (231 arquivos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. | Nível 3-4 (implementação de design já `APPROVED` D-197, sem decisão nova) | — | — | **IMPLEMENTADO (fatia 2/5) — fatia 3/5 (webhook D-7 + Secrets Manager D-10) é a próxima ação real** | `src/modules/notification/{ports/whatsapp-provider.ts,providers/whatsapp-cloud-api-adapter.ts,application/whatsapp-delivery.ts,application/whatsapp-delivery-workflow.ts,application/whatsapp-outbox.ts,ports/recipient-resolver.ts,persistence/dynamodb-recipient-resolver.ts}`, `src/runtime/aws/handlers/{whatsapp-delivery-handler.ts,whatsapp-outbox-relay-handler.ts,outbox-sweeper-handler.ts}`, `src/runtime/aws/composition/notification.ts`, `src/shared/outbox/outbox.ts`, `src/shared/contracts/schema-validator.ts`, `schemas/queues/notification-whatsapp-deliver.v1.json`, `infra/main.tf`, `infra/variables.tf`, `infra/outputs.tf`, `infra/tests/stack.tftest.hcl`, `scripts/build-lambdas.ts`, `test/unit/notification/{whatsapp-cloud-api-adapter.test.ts,whatsapp-delivery.test.ts,whatsapp-delivery-workflow.test.ts,whatsapp-outbox.test.ts}` | 2026-09-07 |
docs/architecture/decisions-log.md:288:| D-231 | **WhatsApp Operacional fatia 3/5 (D-197, D-7 webhook handler + D-10 Secrets Manager/kill switch) — IMPLEMENTADA.** Base era implementação direta de design já `APPROVED` (nível 3-4), mas dois gaps reais de modelagem foram achados durante a implementação e resolvidos via protocolo Claude<->Codex completo (`AGENTS.md` §4, nota cega, `codex exec`) ANTES de codificar, por instrução explícita do dono do projeto para esta frente: **(1) escopo de purge do `WebhookInbox` WhatsApp** (4 rodadas, notas finais 9,2 Claude / 9,2 Codex - `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/estado-final-consolidado.md`): `WebhookInbox` WhatsApp e account-scoped (`PK=WEBHOOK#WHATSAPP#<wabaId>`), nunca tenant-scoped, mas `transient-purge/purge.ts` fenceia todo delete com `TenantLifecycleRecord.status=ACTIVE` - sem um tenant real (o caso normal para qualquer wamid nao correlacionado), a linha nunca purgaria, quebrando a retencao de 7 dias (`privacy-lgpd.md` §4) silenciosamente. Resolvido com `purgeScope: "TENANT"|"ACCOUNT"` imutavel escrito na criacao (nunca inferido do GSI8, que e discovery-only/eventualmente consistente), `accountScopedTransientPurgeGsi8Keys()` novo (mesmo `GSI8PK`, discriminador `#ACCOUNT#<accountId>#` em vez de `#TENANT#<tenantId>#`), uniao discriminada em `candidate-source.ts`/`dynamodb-candidate-source.ts`, e dois ramos de transacao em `purge.ts` (`TENANT`: `ConditionCheck` de tenant ativo + `Delete`, backoff/DLQ por tenant; `ACCOUNT`: so `Delete` condicionado por `version`, `CancellationReasons[0]` != `ConditionalCheckFailed` sempre relancado, nunca backoff/quarentena por tenant). **(2) `buildVersionedUpdate()` nao serve a uma entidade sem tenant** (3 rodadas, notas finais 9,3 Claude / 9,2 Codex): o unico builder de update condicional versionado do repositorio (`shared/dynamodb/occ.ts`, usado por ~40 call sites) embute incondicionalmente `#tenantId = :tenantId` no `ConditionExpression` - `WebhookInbox` WhatsApp nao tem (nem pode fabricar) esse atributo sem reabrir a ambiguidade ja fechada no gap (1). Resolvido com um nucleo privado `buildScopedVersionedUpdate()` compartilhado (unica implementacao de incremento de version/updatedAt/SET/REMOVE/extraConditions/deteccao de colisao) e dois builders publicos: `buildVersionedUpdate()` inalterado byte-a-byte (mesma assinatura/expressao para todo call site existente) e `buildAccountScopedVersionedUpdate()` novo, fence `#accountId = :accountId`. **Decisao final da fatia** (D-7/D-10): `whatsapp-webhook-processor.ts` (puro) implementa `verifyMetaSignature()` (HMAC-SHA256 sobre o raw body, `X-Hub-Signature-256`, `timingSafeEqual`, mesma disciplina de `bff/domain/csrf.ts`), `verifyMetaWebhookChallenge()` (handshake `GET` de registro do webhook, `hub.mode`/`hub.verify_token`/`hub.challenge`, conforme documentacao real da Meta Cloud API verificada 2026-09-07), `parseBizOpaqueCallbackData()`/`parseCloudApiWebhookEvents()` (parsing do payload real de status), e `decideWhatsAppCallbackApplication()` (precedencia monotonica SENT->ACCEPTED/DELIVERED/READ->DELIVERED conflacionado/FAILED->FAILED_TERMINAL, mesma disciplina de `ses-callback-processor.ts`). `whatsapp-webhook-workflow.ts` espelha `ses-callback-workflow.ts` (grava a linha create-once IMEDIATAMENTE apos assinatura valida, ANTES de correlacionar; `markInboxUnmatched`-equivalente na MESMA linha se a correlacao falhar) com `purgeScope: "ACCOUNT"` sempre, `accountId`/`providerAccountId` = `wabaId`, correlacao bem-sucedida so anota `correlatedTenantId`/`attemptId`/`intentId` como metadado (nunca muda o dono do purge). `whatsapp-webhook-handler.ts` (novo, API Gateway HTTP API, rota PUBLICA `authorization_type=NONE` - segunda rota publica do projeto depois de `guest_documents`, D-037): `GET` responde o handshake; `POST` verifica assinatura sobre o raw body ANTES de qualquer `JSON.parse`/persistencia (G-V3), sempre retorna 200 a Meta apos a verificacao (nunca deixa uma falha de processamento parecer uma falha de endpoint que dispararia retry de Meta), checa o kill switch so para decidir se persiste/age. `secrets-manager-whatsapp-config.ts` novo (`loadWhatsAppSecrets`) - um secret JSON so (accessToken/appSecret/phoneNumberId/wabaId/verifyToken, mesma razao de nao fragmentar em 5: emitidos juntos pela Meta, nada rotaciona independente), nunca logado, cacheado por cold start. `whatsapp-activation.ts` novo - kill switch de 2 flags em ordem obrigatoria (`WHATSAPP` + `WHATSAPP_DELIVERY_WORKER_ENABLED`, mesmo mecanismo de D-193 slice 8/9/`document-archive-activation.ts`), `isWhatsAppDeliveryWorkerEnabled()` exige AMBOS. `whatsapp-delivery-handler.ts` (fatia 2/5) migrado de env vars placeholder para Secrets Manager + kill switch (batch inteiro dropado sem side-effect, nunca marcado como falha de item, quando o kill switch esta off). Terraform: `aws_secretsmanager_secret`/`_version` (placeholder JSON, `lifecycle.ignore_changes` protege um valor real setado fora de banda) + politica IAM `secretsmanager:GetSecretValue` escopada SO ao ARN do secret, anexada so aos 2 Lambdas (`whatsapp_delivery`, `whatsapp_webhook_handler` novo); rota publica nova `modules/api-gateway` (`whatsapp_webhook` integration/route/lambda-permission, mesmo throttling restritivo de `guest_documents`); `whatsapp_access_token`/`whatsapp_phone_number_id` (placeholders de fatia 2/5) removidos de `variables.tf`. **Nao wireado nesta fatia** (fatia 4-5): `notification-router.ts`'s `SUPPORTED_CHANNELS`, quota de 24h (D-8). G-V3 real: assinatura invalida sempre rejeita antes de qualquer gravacao (7 testes adversariais incl. header ausente/malformado/tamanho errado); replay do mesmo wamid e idempotente (`DUPLICATE_INBOX`); correlacao falha vira `UNMATCHED` na MESMA linha; secrets nunca logados/persistidos fora do `SecretsManager`; purge de conta nunca precisa de tenant. 30+9+4 testes novos (`whatsapp-webhook-processor.test.ts`/`whatsapp-webhook-workflow.test.ts`/`whatsapp-activation.test.ts`), 5 novos em `test/unit/workers/transient-purge.test.ts` (ramo ACCOUNT: purge sem tenant, sem cross-talk com fence de tenant, colisao de versao, cancelamento nao reconhecido relancado), 3 novos em `test/unit/occ.test.ts` (`buildAccountScopedVersionedUpdate`). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suite completa **2625/2625** (236 arquivos); `terraform fmt`/`validate` limpos, `terraform test` do stack principal 23/23 (`AWS_PROFILE=claude-dev`) - modulo `api-gateway` isolado tem 3 assercoes de contagem de rota pre-existentes e nao relacionadas falhando (10->13 items, 21->22 subjects, 3->5 imports; achado incidental de outra sessao concorrente sem isolamento de worktree, confirmado via `git diff --stat` que esta fatia so ADICIONA linhas a esse arquivo - nao investigado/corrigido aqui, fora de escopo). **PR/merge/CI/verificacao ao vivo em `dev` NAO executados nesta sessao** (orcamento de tempo insuficiente para um `terraform plan`/`apply` real supervisionado depois de tocar infra que ja esta deployada - `module.whatsapp_delivery` muda de shape de env var - dado que e seguro adiar, ver `NEXT_SESSION_PROMPT.md`). | Nivel 5-6 para os 2 gaps achados (modelagem de dado/seguranca que atravessa um invariante transversal do repositorio - GSI8/tenant-fencing e o unico builder OCC condicional do sistema); Nivel 3-4 para o resto (implementacao direta de D-197/D-7/D-10 ja `APPROVED`) | 9,0 (gap 1) / 9,3 (gap 2) | 9,2 (gap 1) / 9,2 (gap 2) | **IMPLEMENTADO (fatia 3/5) - fatia 4/5 (quota D-8 de 24h + IAM dedicada) e a proxima acao real; terraform desta fatia validado (`fmt`/`validate`/`test` do stack principal) mas nunca `plan`/`apply`'d contra AWS real nem mergeado a `main`** | `src/shared/dynamodb/occ.ts`, `src/shared/transient-purge-gsi8.ts`, `src/workers/transient-purge/{purge.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/notification/application/{whatsapp-webhook-processor.ts,whatsapp-webhook-workflow.ts,whatsapp-activation.ts,ses-callback-workflow.ts}`, `src/modules/notification/persistence/secrets-manager-whatsapp-config.ts`, `src/modules/extraction/{ports/feature-flags-reader.ts,persistence/appconfig-feature-flags-reader.ts}`, `src/runtime/aws/handlers/{whatsapp-webhook-handler.ts,whatsapp-delivery-handler.ts}`, `src/runtime/aws/composition/notification.ts`, `schemas/api/webhook-inbox.v1.json`, `infra/main.tf`, `infra/variables.tf`, `infra/modules/api-gateway/{main.tf,variables.tf,tests/api_gateway.tftest.hcl}`, `infra/modules/feature-flags/{main.tf,variables.tf}`, `scripts/build-lambdas.ts`, `package.json`, `test/unit/{occ.test.ts,workers/transient-purge.test.ts,workers/transient-purge-fakes.ts,notification/whatsapp-webhook-processor.test.ts,notification/whatsapp-webhook-workflow.test.ts,notification/whatsapp-activation.test.ts,notification/in-memory-store.ts}`, `test/contract/schemas.test.ts`, `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/` | 2026-09-08 |
docs/architecture/decisions-log.md:292:| D-235 | **E-015 (full-audit-round2, eixo Privacidade, achado HIGH convergente com E-021/Arquitetura) — `DossierExportRun` (D-205, D-216/D-217)/`ReportSubscriptionRun`+`ReportDeliveryAttempt` (D-204, D-211 a D-215) geravam dado pessoal denso por Subject (PDF/Excel/CSV) sem NENHUM TTL/purga, CORRIGIDO por implementação direta — investigado antes de aplicar o protocolo `AGENTS.md` §4 e classificado como aplicação mecânica de um padrão já convergido no mesmo módulo, não decisão de mecanismo em aberto (nível 2-3, protocolo dispensável).** Investigação encontrou que o achado já estava PARCIALMENTE fechado sem que o texto da auditoria refletisse isso: `aws_s3_bucket_lifecycle_configuration.report_exports` (`infra/main.tf`, D-215 decisão 6) já expira o OBJETO S3 (o dado pessoal denso de fato — PDF/Excel/CSV) em 30 dias desde antes desta sessão — nenhuma mudança de infra foi necessária para essa parte. O gap real e remanescente era só nos TRÊS REGISTROS DynamoDB de metadado sobre cada export (Subject id, Requirement id set, scope hash, lista de destinatários, resultado de entrega) — nenhum carregava `purgeAfterTtl`, ficando indefinidamente na tabela principal mesmo depois do objeto S3 que descrevem já ter expirado. Confirmado por leitura de `docs/architecture/data-model.md` §1 (nota de status 2026-08-20) que `retentionClass`/`purgeAfter` (o par de campos genérico do design-target) NUNCA foi materializado em nenhuma entidade do projeto — mas `purgeAfterTtl` (o atributo físico de TTL do DynamoDB, `infra/modules/dynamo-table/main.tf`) é um padrão amplamente estabelecido e já usado em >15 entidades de >8 módulos diferentes (sessões, rate limiters, tokens de convite/guest, `ReminderOccurrence`, `EphemeralTelemetryMutation` etc.) — a mesma mecânica, não uma nova invenção. **Correção**: campo `purgeAfterTtl: number` (epoch seconds) adicionado às 3 interfaces (`dossier-export-run.ts`, `report-subscription-run.ts`, `report-delivery-attempt.ts`), calculado uma única vez no momento de criação de cada registro (`previewDossierExport`/`getOrCreateRun`/criação do `ReportDeliveryAttempt` em `delivery.ts`) como `createdAt + 30 dias` — o MESMO número de 30 dias já decidido para o bucket S3 (D-215 decisão 6), nunca uma retenção nova inventada, por proporcionalidade: a linha de metadado não deveria sobreviver ao objeto S3 que ela descreve. Nunca re-setado em updates parciais (`buildVersionedUpdate` com `SET` específico nunca toca o atributo, então o valor de criação persiste). Nenhum worker de purga customizado necessário — exclusão física é nativa do TTL do DynamoDB, mesma mecânica de `GuestCredentialDelivery`/`InvitationToken`/etc. Avaliação de proporcionalidade explícita: NÃO se estendeu a mesma correção aos outros ~230 tipos de entidade do projeto sem TTL — escopo é estritamente os 3 tipos nomeados pelo achado E-015/E-021 (dado pessoal denso por Subject sem contrapartida de retenção), não uma generalização de `retentionClass`/`purgeAfter` para o resto do modelo de dados (isso continua M4+ pendente, D-179 a D-190/`docs/architecture/data-model.md` §1 nota de status inalterada). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2783/2783** (239 arquivos, sem novos — 4 fixtures de teste existentes atualizadas com o campo novo); `terraform fmt`/`validate`/`test` (27/27, `AWS_PROFILE=claude-dev`, sem mudança de infra — nenhum teste novo necessário, a lifecycle rule do S3 já era coberta). | Nível 2-3 (implementação mecânica de padrão já convergido no mesmo módulo — TTL nativo DynamoDB, sem worker/mecanismo novo) | N/A (protocolo dispensado — decisão avaliada e justificada nesta linha) | N/A | **IMPLEMENTADO** | `src/modules/document-archive/domain/dossier-export-run.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/reports/domain/{report-subscription-run.ts,report-delivery-attempt.ts}`, `src/workers/report-subscription-delivery/delivery.ts`, `test/unit/document-archive/dossier-export-generation.test.ts`, `test/integration/document-archive-dossier-routes.test.ts`, `test/unit/reports/{delivery.test.ts,report-subscription-handlers.test.ts}` | 2026-09-08 |
docs/architecture/decisions-log.md:298:| D-241 | **Implementação de `ExternalShareLink` (D-225, backlog P1 item 8/19) — SLICE 1/3 (domínio + persistência + serviço de aplicação COMPLETO e testado); slices 2/3 (rotas HTTP anônima/autenticada, RBAC, schemas, terraform) EXPLICITAMENTE NÃO INICIADAS nesta sessão.** Pausa deliberada, não interrupção por bloqueio: prioridade do projeto mudou para fechar o P0 (roadmap de lançamento) por inteiro antes de qualquer item novo do backlog P1 — `ExternalShareLink` é item 8/19 do P1, então este slice para aqui em ponto limpo (árvore committada, suíte verde) até o P0 fechar. Implementa exatamente o design `APPROVED` em D-225/`estado-final-consolidado.md`, sem reabrir o protocolo Claude↔Codex (já aprovado, ambos ≥9,0). **Entregue nesta sessão**: `src/modules/document-archive/domain/external-share-link.ts` (entidade `ExternalShareLink` + ponteiro tenantless `ExternalShareLinkPointer`, key-builders — todos com `AuthorizedTenantId` desde o dia 1, zero cast fora de `authorization.ts` — token `selector.secret` 128/256-bit reaproveitando literalmente a mecânica de `request-access-credential.ts`/D-143 com pepper PRÓPRIO); `Document.activeExternalShareLinkCount` (contador atômico, sparse); `id-generator.ts`/`runtime/aws/ids.ts` (`newShareId`); `src/shared/observability/security-audit.ts` (4 eventos fechados: `_created`/`_revoked`/`_presign_issued`/`_tenant_inactive_blocked`); `src/modules/document-archive/ports/external-share-link-file-store.ts` + adapter real `persistence/s3-external-share-link-file-store.ts` (presign de GET direto contra o `DocumentObjectReference` congelado, `ResponseContentDisposition` sanitizado); `src/modules/document-archive/application/external-share-link-service.ts` — `createShareLink` (uma `TransactWriteItems`: ConditionChecks de integridade referencial com os nomes reais `DocumentFile.scanStatus`/`DocumentVersion.state`, cap atômico `MAX_ACTIVE_SHARE_LINKS_PER_DOCUMENT=5` com reconciliação lazy de links expirados — no máximo 5 por tentativa, deltas do contador consolidados num único `Update` do `Document` na MESMA transação, achado do Codex Rodada 4 fechado), `resolveForAnonymousAccess` (parse→rate-limit ANTES do lookup→pointer lookup→compare dummy-safe→status/expiração→tenant ACTIVE→`DocumentFile.scanStatus=CLEAN`+`cleanObject` revalidados via `documentFileSeq`/`documentFileId` CONGELADOS na criação — NUNCA relê `DocumentVersion`/`Document` de novo, contrato "cópia congelada" da Decisão 6 — nunca `authorize()`/`RequestContext`), `revokeShareLink` (OCC `ACTIVE→REVOKED` + decremento atômico do contador), `listShareLinks` (GSI1 paginado). Token no PATH (Decisão 3), nunca query string. **Testes**: `test/unit/document-archive/external-share-link.test.ts` (4, mecânica de token/anti-enumeration) + `external-share-link-service.test.ts` (17: happy path, sem versão ACCEPTED, arquivo não CLEAN, cap sem nada a reconciliar, cap+reconciliação bem-sucedida, token malformado/selector inexistente/secret errado/expirado/revogado/tenant inativo/rate-limit exaustão, revoke+decremento, revoke duplicado, listagem). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos, suíte completa **2807/2807** (241 arquivos — 21 testes novos + ~25 arquivos de teste pré-existentes com fixture `DocumentArchiveIdGenerator` ajustada para a nova propriedade `newShareId`, mecânico). **Explicitamente NÃO feito** (fatias 2/3 do fatiamento sugerido pelo próprio design doc, retomar quando o P1 voltar a ser prioridade): rota HTTP `GET /external-share/{shareId}/{token}` (`Cache-Control: no-store`/`Referrer-Policy: no-referrer`, handler dedicado, confirmar redaction do path bruto — pendência nomeada 4 do design doc); rotas autenticadas `POST .../share-links` / `PATCH .../revoke` / `GET .../share-links` com RBAC `docarchive:share-link-{create,revoke,list}` (ADMIN_ROLES) — as 3 actions NÃO foram adicionadas a `authorization.ts` ainda; schemas JSON em `schemas/api/`; nenhum Terraform tocado (D-225 não exige nova infra — sem novo GSI, reaproveita GSI1 — mas a rota HTTP anônima em si ainda precisa de wiring de API Gateway/Lambda quando essa fatia for retomada). Nenhuma pendência de produto/arquitetura nova gerada — só trabalho de implementação restante do próprio D-225. | Nível 5-6 (acesso anônimo/não-autenticado a recurso de tenant — mesma classificação do design original) | N/A (implementação de design já aprovado — protocolo dispensado por `AGENTS.md` §4, "implementação direta de decisão já aprovada") | N/A | **PARCIALMENTE IMPLEMENTADO — slice 1/3 (domínio+persistência+aplicação) completo e verde; slices 2/3 (HTTP+RBAC+schemas+terraform) pendentes, retomar após P0 fechar** | `src/modules/document-archive/domain/external-share-link.ts`, `src/modules/document-archive/domain/document.ts`, `src/modules/document-archive/application/external-share-link-service.ts`, `src/modules/document-archive/application/id-generator.ts`, `src/modules/document-archive/ports/external-share-link-file-store.ts`, `src/modules/document-archive/persistence/s3-external-share-link-file-store.ts`, `src/shared/observability/security-audit.ts`, `src/runtime/aws/ids.ts`, `test/unit/document-archive/external-share-link*.test.ts` | 2026-09-08 |
docs/architecture/decisions-log.md:302:| D-245 | **WhatsApp Operacional fatia 4/5 (D-197, D-8 quota de 24h + IAM dedicada) — IMPLEMENTADA.** Implementação direta de design já `APPROVED` (`whatsapp-channel-scoping/estado-final-consolidado.md`, nível 3-4, protocolo Claude↔Codex dispensado — o design já fixava a forma física exata: item base-table `PK=WHATSAPP#PORTFOLIO`/`SK=SENT#<timestampIso>#<phoneE164>`, TTL 25h, IAM `dynamodb:LeadingKeys=["WHATSAPP#PORTFOLIO"]` só no `WhatsAppDeliveryWorker`, fail-closed em falha de leitura — nenhuma lacuna genuína de modelagem restava para o protocolo resolver). **Mecanismo**: `whatsapp-portfolio-quota.ts` (domínio, sem I/O) — `whatsAppPortfolioQuotaKey`/`buildWhatsAppPortfolioQuotaEntry` (TTL = sentAt+25h, 1h de folga sobre a janela de 24h para nunca competir com o sweep nativo de TTL no limite exato da janela); `whatsAppPortfolioQuotaWindowStartSk`/`WindowEndSk` (bounds reais de range, não só a promessa qualitativa — atende ao critério 3 explicitamente contestado na Rodada 3 do design); `evaluateWhatsAppPortfolioQuota()` pura — um telefone JÁ presente na janela sempre é admitido (já contou contra o tier, reenviar não consome um slot NOVO de destinatário único, espelhando a métrica real da Meta "unique numbers reached"); um telefone NOVO só é admitido enquanto a contagem de distintos na janela estiver abaixo do tier. `NotificationStore.queryWhatsAppPortfolioQuotaWindow()` novo (porta + implementação DynamoDB) — `Query` real com paginação completa (`ExclusiveStartKey` até esgotar, custo/paginação explícitos, não uma página limitada única, per o mesmo critério 3). `whatsapp-portfolio-quota-service.ts` (`checkAndRecordWhatsAppPortfolioQuota`) — lê a janela, decide, grava a entrada nova via `putIfAbsent` só quando admitido; qualquer erro na leitura vira `READ_FAILURE` (bloqueado), nunca um "allow" silencioso — postura deliberadamente diferente do fail-open de `TenantQuotaService.consumeEphemeralTelemetry` (telemetria pura vs. um gate real de admissão contra teto externo). **Wiring em `whatsapp-delivery-workflow.ts`**: checagem/gravação inserida IMEDIATAMENTE após resolver o telefone do destinatário e ANTES da chamada real à Cloud API — mesma disciplina "admitir antes da chamada externa" que a claim `SUBMITTING`/lease já usa; uma recusa (tier atingido OU falha de leitura) vira `FAILURE{failureKind: CONCLUSIVE_RETRYABLE}` reaproveitando a state machine já existente de `email-delivery.ts`/`whatsapp-delivery.ts` (nenhum status novo, nenhuma branch nova na state machine — nenhuma chamada externa foi tentada, logo não há ambiguidade, e a janela rolante torna uma retentativa futura genuinamente capaz de suceder). `WhatsAppDeliveryWorkflowDeps.portfolioQuotaTierLimit: number` novo, threadeado por `buildWhatsAppDeliveryDeps()`→`whatsapp-delivery-handler.ts` a partir de `WHATSAPP_PORTFOLIO_QUOTA_TIER_LIMIT` (env var, default 250 — o tier REAL mais baixo da Meta, então uma env var esquecida falha para o teto mais restritivo, nunca para "ilimitado"). **Chave física deliberadamente tenantless** (`PK=WHATSAPP#PORTFOLIO`, sem `TENANT#`) — não usa `AuthorizedTenantId` neste key-builder por construção (mesma razão de `WebhookInbox` WhatsApp ser account-scoped, D-231/D-7): esta entidade mede consumo agregado de um recurso de portfólio compartilhado por TODOS os tenants, não um dado de um tenant — não é uma exceção à disciplina D-237-240, é um caso fora do domínio dessa disciplina (nenhum outro key-builder novo desta fatia é tenant-scoped; grep confirma zero `as AuthorizedTenantId` fora de `authorization.ts` no diff). **IAM**: `whatsapp_portfolio_quota_policy_json` novo em `infra/modules/dynamo-table/` (`Query`+`PutItem` na tabela base, condição `dynamodb:LeadingKeys=["WHATSAPP#PORTFOLIO"]`, mesma disciplina de isolamento de `gsi8_read` aplicada à tabela base em vez de um GSI) — anexado SÓ ao módulo `whatsapp_delivery` em `infra/main.tf`, nunca a `whatsapp_webhook_handler` (design: "só no WhatsAppDeliveryWorker"). Variável nova `whatsapp_portfolio_quota_tier_limit` (default 250) em `infra/variables.tf`. Risco de hot-partition nomeado explicitamente no design (toda mensagem de todo tenant grava na MESMA partição) — aceito e não mitigado agora, gatilho de sharding por hora já registrado no design, não reaberto aqui. **Achado de processo (colisão de numeração, mesmo padrão de D-223/D-229)**: `D-244` já estava em uso concorrente (implementação de D-243, guest submit-evidence, mesma árvore sem isolamento de worktree) no momento desta sessão — esta entrada usa D-245. `npm run typecheck` isolado (via `git stash` seletivo dos 3 arquivos alheios de D-243/D-244 em WIP) confirmou que os arquivos desta fatia não introduzem nenhum erro novo; com a árvore completa (WIP alheio incluído) o typecheck também passou limpo depois que aquele WIP foi commitado por sua própria sessão. G-V3 real: 7 testes de domínio (`whatsapp-portfolio-quota.test.ts` — chave/SK time-first, TTL exato, bounds de janela, decisão pura para novo/já-presente/tier-zero); 3 de serviço (`whatsapp-portfolio-quota-service.test.ts` — admite+grava, bloqueia sem gravar, fail-closed em erro de leitura); 5 adversariais no worker (`whatsapp-delivery-workflow.test.ts` — sob o tier grava e envia, tier atingido por OUTRO destinatário bloqueia sem chamar a Cloud API, destinatário JÁ contado na janela ainda é admitido mesmo no tier, entrada fora da janela de 24h não conta, falha de leitura do quota é fail-closed). Gate local completo verde: `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `npm test` **2826/2826** (243 arquivos); `terraform fmt`/`validate` limpos, `terraform test` **27/27** (`AWS_PROFILE=claude-dev`, stack principal). **`terraform apply` NÃO executado** (fora do escopo desta sessão por instrução explícita — fica para o pipeline CI/CD ao mergear `develop`→`main`, mesmo padrão que D-242 confirmou funcionar). | Nível 3-4 (implementação de design já `APPROVED` D-197/D-8, sem decisão nova) | N/A | N/A | **IMPLEMENTADO (fatia 4/5) — fecha o item 3 do roadmap P0 (WhatsApp Operacional) do lado de engenharia pura, restando só fatia 5/5 (router wiring + kill switch final + RBAC + terraform completo) e o bloqueio de produto/jurídico E-019 (fora do controle de engenharia), nenhum dos dois tocados aqui** | `src/modules/notification/{domain/whatsapp-portfolio-quota.ts,application/whatsapp-portfolio-quota-service.ts,application/whatsapp-delivery-workflow.ts,ports/notification-store.ts,persistence/dynamodb-notification-store.ts}`, `src/runtime/aws/{composition/notification.ts,handlers/whatsapp-delivery-handler.ts}`, `infra/{main.tf,variables.tf,modules/dynamo-table/{main.tf,outputs.tf}}`, `test/unit/notification/{whatsapp-portfolio-quota.test.ts,whatsapp-portfolio-quota-service.test.ts,whatsapp-delivery-workflow.test.ts,in-memory-store.ts}` | 2026-09-09 |
docs/architecture/decisions-log.md:303:| D-246 | **WhatsApp Operacional fatia 5/5 (D-197, router wiring + kill switch + RBAC + terraform completo) — IMPLEMENTADA. Fecha o item 3 do roadmap P0 do lado de engenharia por completo.** Implementação direta de design já `APPROVED` (nível 3-4, protocolo Claude↔Codex dispensado — `estado-final-consolidado.md` já nomeava esta fatia como "router wiring + kill switch + RBAC + terraform completo", sem lacuna de modelagem restante; D-231 e D-245 já haviam deixado o ponto de extensão explicitamente marcado no código). **Mecanismo**: `notification-router.ts`'s `SUPPORTED_CHANNELS` (const estática `["EMAIL"]`) substituída por `isChannelRoutable(channel, input)` — EMAIL continua incondicionalmente roteável uma vez que os passos 2-6 (item/policy/recipient/entitlement/opt-out, todos EMAIL-específicos desde M4) já passaram; WHATSAPP exige AMBOS `input.whatsappChannelEnabled` (o kill switch `WHATSAPP`, D-197/D-10) E `input.entitlement.whatsappEnabled` (`NotificationEntitlements.whatsapp.enabled` — campo que existia na entidade desde a Rodada 1 do design mas era "campo morto", sem leitor, achado real registrado no próprio `round1-claude-proposal.md`) — nega com `CHANNEL_UNAVAILABLE` (motivo já existente na união fechada, nenhum motivo novo inventado) quando qualquer um falha, fail-closed inclusive quando o registro de entitlements está ausente (`whatsappEnabled` undefined nunca é tratado como permitido). `RouterEntitlementState.whatsappEnabled`/`RouterInput.whatsappChannelEnabled` novos, ambos opcionais (default `false`/`undefined` tratado como não-permitido) — decisão deliberada para não exigir alteração de nenhuma fixture EMAIL-only pré-existente. `notification-router-workflow.ts`'s `routeNotificationIntent()` agora lê `entitlements.whatsapp.enabled` (antes só `entitlements.email.enabled` era lido) e passa `deps.whatsappChannelEnabled` adiante; `applyRoutedDecision()` generaliza o loop de construção de `NotificationAttempt`+outbox de "só EMAIL" para EMAIL/WHATSAPP — `channel`/`provider` (`META_CLOUD_API`) derivados do canal roteado, outbox delegado a `buildWhatsAppOutboxRecord` (D-9, já existia standalone desde a fatia 2/5, nunca antes chamado por código de produção) que aponta para `SQS_NOTIFICATION_WHATSAPP_V1` — nunca a fila de e-mail (ADR-0008). `NotificationRouterWorkflowDeps.whatsappChannelEnabled?: boolean` novo (opcional, mesma razão de compatibilidade de fixture). **Kill switch no handler**: `notification-router-handler.ts` (DynamoDB Streams) ganha a MESMA leitura de `AppConfigFeatureFlagsReader`/`isWhatsAppChannelEnabled()` que `whatsapp-delivery-handler.ts` já usa — lida UMA VEZ por batch (não por registro), fail-closed em erro de leitura (nunca "desconhecido, prosseguir"); diferente do delivery worker, uma falha aqui NUNCA derruba o lote inteiro — intents EMAIL-only roteiam exatamente como antes, só intents que pedem WHATSAPP caem em `CHANNEL_UNAVAILABLE` via o próprio gate per-channel do router. `buildNotificationRouterDeps()` ganha o parâmetro `whatsappChannelEnabled` (default `false`). **RBAC**: nenhuma `Action`/rota nova necessária — o router é um worker assíncrono (Streams), nunca invocado via HTTP; a única superfície de usuário do programa WhatsApp já coberta por RBAC é `WhatsAppOptInService.recordOptIn()` (`notification:configure`, já registrado em `authorization.ts` desde a fatia 1/5, self-service — usuário gerencia o PRÓPRIO consentimento). **Achado nomeado, fora de escopo desta fatia**: `WhatsAppOptInService` ainda não tem nenhuma rota HTTP que a invoque (grep confirma) — sem ela, nenhum usuário real consegue de fato registrar opt-in hoje; não estava listado como entregável de nenhuma das 5 fatias do design (`estado-final-consolidado.md` nomeia fatia 5 como "router wiring + kill switch + RBAC + terraform completo", não "rota de opt-in") e por isso não foi construído aqui — registrado como pendência nomeada para uma sessão futura, não um gap escondido. **Terraform**: `module.notification_router` em `infra/main.tf` ganha `APPCONFIG_APPLICATION_ID`/`APPCONFIG_ENVIRONMENT_ID`/`APPCONFIG_CONFIGURATION_PROFILE_ID` (env vars) + `module.feature_flags.feature_flags_read_policy_json` (IAM) — mesmo trio que `whatsapp_delivery`/`whatsapp_webhook_handler`/`extraction_starter_handler` já têm; nenhum recurso novo (fila/GSI/tabela) — fatia 5/5 não introduz nenhum, só habilita um Lambda já existente a ler AppConfig. G-V3 real: 5 novos testes em `notification-router.test.ts` (WHATSAPP com kill switch off → `CHANNEL_UNAVAILABLE`; kill switch ON mas entitlement nega → `CHANNEL_UNAVAILABLE`; kill switch ON mas entitlement ausente/undefined → `CHANNEL_UNAVAILABLE`, fail-closed nunca fail-open; kill switch ON + entitled → `ROUTED`; EMAIL+WHATSAPP simultâneos ambos `ROUTED`); 2 novos em `notification-router-workflow.test.ts` (happy path WHATSAPP cria `NotificationAttempt{channel:WHATSAPP,provider:META_CLOUD_API}`+`OutboxEvent{destination:SQS_NOTIFICATION_WHATSAPP_V1}`; kill switch off com entitlement true ainda cancela `CHANNEL_UNAVAILABLE`, sem attempt/outbox criado — prova que o kill switch é um gate independente da entitlement, não substituível por ela). Gate local completo verde: `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; `npm test` **2832/2832** (243 arquivos, 6 testes novos); `terraform fmt`/`validate` limpos, `terraform test` **27/27** (`AWS_PROFILE=claude-dev`, stack principal). **`terraform apply` NÃO executado** (fora do escopo desta sessão por instrução explícita, mesmo padrão de D-242/D-245 — fica para uma varredura coordenada futura junto com o terraform pendente de D-245, nenhum dos dois ainda mergeado/aplicado em `main`/`dev`). | Nível 3-4 (implementação de design já `APPROVED` D-197, sem decisão nova) | N/A | N/A | **IMPLEMENTADO (fatia 5/5) — FECHA o item 3 do roadmap P0 (WhatsApp Operacional) por completo do lado de engenharia; nenhuma fatia resta.** Restam apenas: (1) o bloqueio de produto/jurídico E-019 (privacy notice, DPA da Meta, residência de dados — fora do controle de engenharia, já rastreado na lista "Pendências reais que dependem de decisão de Marcelo"); (2) o terraform desta fatia + o de D-245 (fatia 4/5) ainda não mergeados/`apply`'d em `main`/`dev` — varredura coordenada futura; (3) pendência nomeada, não bloqueante: rota HTTP para `WhatsAppOptInService.recordOptIn()` (nunca fez parte de nenhuma das 5 fatias do design, mas sem ela nenhum usuário real consegue opt-in) | `src/modules/notification/application/{notification-router.ts,notification-router-workflow.ts,whatsapp-activation.ts,whatsapp-delivery-workflow.ts,whatsapp-outbox.ts}`, `src/runtime/aws/{composition/notification.ts,handlers/{notification-router-handler.ts,whatsapp-delivery-handler.ts}}`, `infra/main.tf`, `test/unit/notification/{notification-router.test.ts,notification-router-workflow.test.ts}` | 2026-09-08 |
docs/architecture/decisions-log.md:320:| D-267 | **Bloco 7 do plano de sequenciamento (D-253) — COMPLETO. A10 (Rastreamento legado), A22 (Entrega de solicitação) e G01 (Upload de convidado, legado) implementados, testados e revisados (1 rodada Codex, achados reais corrigidos).** A10 (`Tracking.tsx`): CRUD completo de `RequirementAssignment` (assign/editar/excluir), vincular/desvincular um `ExpirationItem`, solicitar/revogar um `DocumentRequest` legado; detalhe em página própria (Snapshot congelado + Timeline de eventos vivos, per spec V7). Dois desvios reais e documentados no próprio código: nenhuma rota HTTP lê `DocumentChasingOccurrence` (timeline omite lembretes automáticos, nunca fabrica dado) e a coluna "Atividade recente" da lista foi simplificada para `updatedAt` (evita um N+1 de histórico por vínculo que nenhuma outra tela deste código faz). A22 (`RequestDeliverySettings.tsx`): preferência de entrega tenant-wide (EMAIL/MANUAL), OWNER_ROLES exclusivo, com guarda de rota própria. G01 (`LegacyGuestUpload.tsx`): upload de convidado em etapa única (sem seleção de tipo, sem confirm explícito — ao contrário de G02), reaproveitando `GuestLinkUnavailable`. **Gap de infra fechado**: não existia NENHUMA rota CloudFront para `/guest/document-requests/*` — resolvido com um alias `GET .../info` (mesmo handler, `guest-documents-handler.ts`) + 2 novos `ordered_cache_behavior`, espelhando o padrão já usado por G02. **Rodada única de revisão Claude↔Codex sobre o diff completo (2 commits, ~2,6 mil linhas, colado inline): nota cega 4,6/10 NEEDS FIXES — 3 achados BLOQUEANTES, 3 ALTOS, 3 MÉDIOS, todos verificados como reais contra o código antes de corrigir (nenhum aceito só por alegação), todos corrigidos na mesma sessão.** Bloqueantes: (1) "Solicitar documento" descartava o `guestToken` inteiro no sucesso — para entrega MANUAL (ou EMAIL falho/kill-switched) o operador nunca teria como obter o link, já que ele só é retornado uma vez; corrigido mostrando o link copiável. (2) `document-request-service.ts`/`dispatch.ts` construíam o link do convidado como `?token=...` (query string) — nunca bateu com a rota real da SPA (`/guest/document-requests/:token`), bug pré-existente que só esta sessão expôs de ponta a ponta pela primeira vez; corrigido para formato de path em ambos os call sites, e `GUEST_UPLOAD_BASE_URL` (deliberadamente nunca wireado desde D-047, "sem frontend real ainda") finalmente ligado em Terraform para as duas Lambdas, condição agora obsoleta. (3) G01 afirmava "link de uso único", mas o backend genuinamente permite reenvio por design (`submissionCount`) — corrigida a cópia em vez de mudar comportamento de backend já estabelecido. Altos: falha ao carregar submissões era mostrada silenciosamente como lista vazia (agora aviso+retry); conflitos OCC (editar/vincular/desvincular/revogar/excluir) nunca invalidavam o cache, podendo repetir o mesmo 409 indefinidamente (corrigido nos 5 pontos); "Revogar" não tinha diálogo de confirmação apesar de invalidar o link imediatamente (adicionado, nomeando destinatário+consequência). Médios: rádios de A22 editáveis durante o save (agora desabilitados); falha transitória de rede/5xx em G01 colapsava no mesmo estado permanente "link indisponível" de um token realmente inválido (agora um estado distinto, com retry); seletor de arquivo sem anel de foco visível por teclado (corrigido de forma genérica em `Button.css`, beneficia G02 também). **Gate local completo verde antes e depois da rodada**: backend typecheck/lint/check-boundaries/check-docs/validate-schemas/build:lambdas (61 Lambdas)/suíte completa (unit+contract+architecture+integration, ~2900 testes); terraform fmt/validate/test (27/27, `--profile claude-dev`); frontend typecheck/lint/vite build/suíte vitest completa (329/332 — as 3 falhas restantes são o gap pré-existente de `crypto.subtle`/jsdom em `documents.test.ts` mais uma flake intermitente já documentada em `Settings.test.tsx` sob carga pesada de suíte completa, ambas confirmadas não relacionadas)/Playwright chromium completo (138/138, incluindo os 25 testes novos do bloco). 4 commits reais: `7242b55` (backend/infra G01), `82111f9` (frontend A10/A22/G01), `89016ec` (correções da rodada Codex). | Nível 5-6/6 (implementação de 3 telas do plano `APPROVED` + um gap de infra pré-existente descoberto e fechado + achados de segurança/UX genuínos corrigidos, protocolo Claude↔Codex completo aplicado) | Autoavaliação: cada achado do Codex foi verificado contra o código real antes de corrigir (nenhum aceito por alegação, nenhum descartado sem checar) — a mesma disciplina de honestidade que D-265/D-266 já registraram para seus próprios retrocessos | 4,6/10 NEEDS FIXES (3 BLOQUEANTE + 3 ALTO + 3 MÉDIO) → todos os 9 achados corrigidos e verificados por teste dedicado (5 testes novos) → nota pós-correção não re-solicitada ao Codex (achados eram objetivamente fechados: fix + teste + gate local completo verde) | **BLOCO 7 COMPLETO — A10/A22/G01 implementados, testados, revisados e corrigidos; pendência de CSP/CORS (D-266) permanece a única barreira restante para upload real browser→S3 em qualquer caminho guest/autenticado. Frontend completo do P0 (item 11 do roadmap) avança para o Bloco 8 (A18, Preferências de notificação).** | `frontend/src/routes/{subjects/Tracking.tsx,subjects/RequestDeliverySettings.tsx,guest/LegacyGuestUpload.tsx,subjects/SubjectHub.tsx}`, `frontend/src/{api/{subjects,types,presentation,queryKeys,guestLegacyUpload}.ts,hooks/use{AssignRequirement,UpdateRequirementAssignment,DeleteRequirementAssignment,CreateLegacyDocumentRequest,RevokeLegacyDocumentRequest,LegacyDocumentRequests,RequirementAssignment,DocumentRequestDeliveryPreference,UpdateDocumentRequestDeliveryPreference}.ts,components/ui/{RadioGroup.css,Button.css},shell/navigation.ts,App.tsx}`, `src/{modules/subject/application/document-request-service.ts,workers/document-chasing-dispatch/dispatch.ts,runtime/aws/handlers/guest-documents-handler.ts}`, `infra/{main.tf,modules/api-gateway/main.tf,modules/spa-hosting/main.tf}`, `test/unit/subject/guest-handlers.test.ts`, `frontend/{e2e/block7-tracking-delivery-guest.spec.ts,test/routes/{subjects/Tracking.test.tsx,subjects/RequestDeliverySettings.test.tsx,guest/LegacyGuestUpload.test.tsx,subjects/SubjectHub.test.tsx},test/shell/navigation.test.ts}` | 2026-09-11 |
docs/architecture/decisions-log.md:335:| D-276 | **P2.2 (auditoria externa 2026-09-11)/SEC-R2-04 (full-audit round2, E-018) — achado real, decisão-independente: IP bruto do guest era persistido em texto claro na PK do rate limiter (`document-archive-guest-rate-limiter.ts`).** `DOCARCHIVEGUESTIP#<ip>#RATE` gravava o endereço IP do chamador literalmente, sem hash, na dimensão "por IP" do rate limit multidimensional (D-143 Decisão 4) — mitigado por `purgeAfterTtl`, mas ainda PII em texto claro sentada na tabela até a TTL expirar. Corrigido: `DocumentArchiveGuestRateLimiter` agora exige um `ipHashPepper` no construtor e faz `createHmac("sha256", pepper).update(ip).digest("hex")` antes de montar a chave — mesmo primitivo já usado por `external-share-link.ts`/`request-access-credential.ts`. Deliberadamente reusa um pepper JÁ existente em cada composição (não introduz um novo secret): a Lambda de `document-archive-guest-handler.ts` reusa `guestAccessPepper` (mesma superfície guest, não um domínio novo); a Lambda `external-share-handler.ts` reusa `ipAuditPepper` (já a pepper dedicada a pseudonimizar IP de auditoria nessa mesma classe de serviço, D-273) — nenhuma decisão de rotação/gestão de segredo nova. Este classe é usada por AMBOS `GuestDocumentAccessService` (D-146) e `ExternalShareLinkService` (D-225/D-241/D-273), então a correção teve que atualizar as 2 composições reais + 9 pontos de instanciação em arquivos de teste. 2 testes novos (`document-archive-guest-rate-limiter.test.ts`): a chave real gravada nunca é o IP puro (é o hash), e peppers diferentes produzem hashes diferentes para o mesmo IP (prova de que é HMAC de verdade, não um pass-through disfarçado). | Nível 2 (correção mecânica de pseudonimização, reusa pepper já existente, nenhum secret novo, nenhuma mudança de comportamento observável do rate limit em si — mesmo limite/janela, só a REPRESENTAÇÃO da chave muda; protocolo Claude↔Codex não exigido por `AGENTS.md` §4) | N/A — correção mecânica, não decisão nova | N/A | **IMPLEMENTADO E VERIFICADO** — `typecheck`/`lint --max-warnings=0`/`check-boundaries` (0 violações, 643 módulos) limpos, testes-alvo (6 arquivos afetados, 70/70 verdes incl. os 2 novos) sem regressão | `src/modules/document-archive/application/document-archive-guest-rate-limiter.ts`, `src/runtime/aws/composition/document-archive.ts`, `test/unit/document-archive/{document-archive-guest-rate-limiter,document-archive-guest-handlers,external-share-link-service,guest-credential-issuance-to-delivery-e2e,guest-document-access-service}.test.ts`, `test/integration/document-archive-share-link-routes.test.ts` | 2026-09-12 |
docs/architecture/decisions-log.md:342:| D-283 | **P0.2 (auditoria externa 2026-09-11) — `GuestSession` agora vinculada ao credential/token do path da mutação, fechando o bug de colisão de 2 abas nomeado em D-264.** Protocolo Claude↔Codex 1 rodada (`docs/architecture/reviews/guest-session-binding-scoping/`): Rodada 1 (proposta escrita numa sessão anterior, bloqueada por rate-limit do Codex, nunca enviada) enviada nesta sessão após probe mínimo confirmar disponibilidade — Codex aprovou de primeira, **NOTA 9,4/10, VEREDITO APPROVED, 0 achados bloqueantes**, 3 menores (comentário desatualizado em `guest-session.ts`, cobertura de teste adversarial faltando, imprecisão de descrição sobre ordem do rate-limiter) — todos incorporados na implementação sem nova rodada (Rodada 2, `round2-claude-reconciliation.md`). **Mecanismo**: novo `assertPathTokenBoundToSession()` (`guest-document-access-service.ts`) — reusa `parseRequestAccessToken`/`hmacRequestAccessCrypto.hash` já existentes para derivar só o SELECTOR hash do token do path (nunca reautentica o secret — a sessão já provou posse na emissão, Decision 4) e compara com `===` (não `timingSafeEqual`, comparação de identificador opaco não-secreto) contra `resolved.session.credentialSelectorHash` (campo já existente desde D-146, até então só "audit trail"). Chamado logo após `resolveSession()` e antes de `assertCsrf()`/qualquer efeito colateral em `submitEvidence()`/`confirmUploadInFlight()`; mismatch colapsa no mesmo `GuestAccessInvalidError` genérico de qualquer outra falha de guest-auth (nunca um erro diferenciado). HTTP: `handleSubmitEvidence`/`handleConfirmUpload` (`document-archive-guest-handlers.ts`) agora repassam o `token` já extraído por `requireToken()` (antes descartado, só chamado "for route symmetry") como novo parâmetro `pathToken` das duas chamadas de serviço. 5 testes novos (4 unit no serviço + 1 HTTP): token de OUTRO credential rejeitado sem nenhum efeito colateral (0 `DocumentVersion` criada), mesmo credential aceito, token malformado rejeitado genericamente, `confirmUploadInFlight` com token trocado rejeitado sem estender o deadline, e o equivalente HTTP-level confirmando 401/`GUEST_ACCESS_INVALID`. | Nível 5-6 (redefine um padrão de segurança já resolvido fora do projeto — vínculo sessão↔recurso, pesquisa externa OWASP citada na Rodada 1 — protocolo Claude↔Codex completo) | Aceito o veredito Codex sem contraproposta — 3 achados menores, todos incorporados, nenhum desacordo a reconciliar | R1 9,4/10 — **APROVADO de primeira** | **IMPLEMENTADO E VERIFICADO** — `typecheck`/`lint --max-warnings=0`/`check-boundaries` (0 violações, 645 módulos)/`check-docs` limpos; suíte completa `npm test` **2947/2947** (249 arquivos, +5 vs. D-280's 2942) | `src/modules/document-archive/{domain/guest-session.ts,application/guest-document-access-service.ts,http/document-archive-guest-handlers.ts}`, `test/unit/document-archive/{guest-document-access-service,document-archive-guest-handlers}.test.ts` | 2026-09-14 |
docs/architecture/decisions-log.md:355:| D-285 | **E-017 (full-audit round2, Governança de IA) — os 2 gaps estruturais decisão-independentes nomeados na rodada (`docs/engineering/decisions-log.md` E-017) formalizados por escrito em `definition-of-done.md`.** (1) **Gate de fechamento de item de ROADMAP**: nova seção exige que a linha que declara um item 🟢 cite explicitamente a evidência de fluxo PONTA A PONTA (teste de integração real exercitando os handlers em sequência, ou verificação ao vivo pós-deploy citando o comando/consulta que confirma o efeito fim-a-fim) — fecha a causa raiz nomeada em AI-INC-004 (D-227 declarou o item 9 do roadmap fechado com "suíte verde + CI verde + deploy" quando na verdade um componente inteiro do fluxo, o worker de entrega, nunca tinha sido construído; só descoberto por verificação ao vivo pós-merge, D-228). Quando a evidência ainda não existir (bloqueio externo, ex. E-019/WhatsApp), a regra exige 🟡 explícito, nunca 🟢 com a lacuna só em prosa. (2) **Limite de profundidade de redelegação de subagente**: máximo 1 nível de subagente sem produzir progresso real verificável (diff/resultado de comando/achado concreto) antes de escalar para execução direta — fecha a causa raiz comum de AI-INC-001 (2026-08-20) e sua recorrência AI-INC-003 (2026-09-07, 3 níveis de subagentes redelegando a mesma tarefa sem progresso), ambos registrados em `ai-governance.md` §5 com a mesma causa raiz nunca corrigida até agora. Os 2 incidentes e o próprio E-017 tiveram seus status atualizados em `ai-governance.md`/`decisions-log.md` de engenharia para refletir a correção — deliberadamente classificados como **"mecanismo existe, verificação empírica ainda pendente"**, nunca "incidente fechado", já que nenhum evento real testou a regra nova ainda (honestidade de processo, mesmo padrão que o próprio E-017 já cobrava de si mesmo). | Nível 3-4 (auditoria/documentação de processo, mesma classificação que o próprio E-017 já tinha — sem código de produto tocado, protocolo Claude↔Codex dispensado pelo mesmo racional já usado para D-236/D-274/D-282, correção de registro/processo não é decisão de arquitetura) | N/A — correção de processo/documentação, não decisão de arquitetura nova | N/A | **FEITO E VERIFICADO** — `npm run check-docs` limpo | `docs/engineering/{definition-of-done.md,ai-governance.md,decisions-log.md}` | 2026-09-14 |
53:  device_configuration {
54:    challenge_required_on_new_device      = true
55:    device_only_remembered_on_user_prompt = true
test\unit\bff\bff-auth-service.test.ts
/**
 * CSRF — triple-layer verification per D-053/D-054: (1) Sec-Fetch-Site, checked first and
 * never fail-open when absent (older browsers that omit it fall through to the double-submit
 * check instead of being waved through); (2) X-CSRF-Token request header must equal (3) the
 * non-HttpOnly __Host-et_csrf cookie value, AND both must equal the secret stored server-side
 * on the Session record - an attacker who could somehow set the cookie cross-site (which
 * SameSite=Strict already prevents) would still fail the server-side comparison.
 */
import { randomBytes, timingSafeEqual } from "node:crypto";

export function issueCsrfSecret(): string {
  return randomBytes(32).toString("hex");
}

const SAFE_METHODS = new Set(["GET", "HEAD", "OPTIONS"]);

export function requiresCsrfCheck(method: string): boolean {
  return !SAFE_METHODS.has(method.toUpperCase());
}

/** `undefined` Sec-Fetch-Site (older browser) is treated as "not same-origin" - never
 * fail-open. Only "same-origin" and "none" (user-typed URL / bookmark, i.e. not a
 * cross-site page making the request) pass this layer. */
export function isSameSiteFetch(secFetchSite: string | undefined): boolean {
  return secFetchSite === "same-origin" || secFetchSite === "none";
}

function safeEqual(a: string, b: string): boolean {
  const bufA = Buffer.from(a, "utf-8");
  const bufB = Buffer.from(b, "utf-8");
  if (bufA.length !== bufB.length) return false;
  return timingSafeEqual(bufA, bufB);
}

export interface CsrfCheckInput {
  method: string;
  secFetchSite: string | undefined;
  headerToken: string | undefined;
  cookieToken: string | undefined;
  sessionCsrfSecret: string;
}

export function checkCsrf(input: CsrfCheckInput): boolean {
  if (!requiresCsrfCheck(input.method)) return true;
  if (!isSameSiteFetch(input.secFetchSite)) return false;
  if (!input.headerToken || !input.cookieToken) return false;
  return safeEqual(input.headerToken, input.cookieToken) && safeEqual(input.headerToken, input.sessionCsrfSecret);
}
    const eq = firstSegment.indexOf("=");
    return { name: firstSegment.slice(0, eq).trim(), value: firstSegment.slice(eq + 1).trim() };
  });
}

async function main() {
  const creds = process.env.PERF_TEST_EMAIL && process.env.PERF_TEST_PASSWORD
    ? { email: process.env.PERF_TEST_EMAIL, password: process.env.PERF_TEST_PASSWORD }
    : parseCreds(readFileSync(CREDS_PATH, "utf-8"));
  if (!creds.email || !creds.password) {
    throw new Error("Could not parse email/password from credentials file: " + CREDS_PATH);
  }

  const response = await fetch(`${APP_ORIGIN}/bff/login`, {
    method: "POST",
    // "none" is Fetch Metadata's own value for a request with no initiating browsing context
    // (typed URL, bookmark) - the correct declaration for a direct, first-party script calling
    // the API itself, not a browser page (round-1 Codex finding, d321-direct-auth-adversarial-
    // review: POST /bff/login now rejects any request that can't declare itself same-site/none,
    // closing a login-CSRF gap a cross-site browser page can never spoof this header to bypass).
    headers: { "content-type": "application/json", "sec-fetch-site": "none" },
    body: JSON.stringify({ email: creds.email, password: creds.password }),
  });

  if (!response.ok) {
    throw new Error(`POST /bff/login failed: ${response.status} ${await response.text()}`);
  }

  const cookies = parseSetCookiePairs(response.headers.getSetCookie());
  const sessionCookie = cookies.find((c) => c.name === SESSION_COOKIE_NAME);
  const csrfCookie = cookies.find((c) => c.name === CSRF_COOKIE_NAME);

  if (!sessionCookie) {
    console.error("All cookies seen:", cookies.map((c) => c.name));
    throw new Error("Did not find a session cookie after login - check cookie name constants in src/modules/bff/domain/cookies.ts");
  }

  const result = {
    cookieHeader: cookies.map((c) => `${c.name}=${c.value}`).join("; "),
    sessionCookieName: sessionCookie.name,
    csrfCookieName: csrfCookie ? csrfCookie.name : null,
    csrfTokenValue: csrfCookie ? csrfCookie.value : null,
    obtainedAt: new Date().toISOString(),
  };

  mkdirSync(path.dirname(OUT_PATH), { recursive: true });
  writeFileSync(OUT_PATH, JSON.stringify(result, null, 2));
  console.log(`[perf-auth] BFF session obtained at ${result.obtainedAt}`);
}


web search: 
web search: https://docs.aws.amazon.com/cognito-user-identity-pools/latest/APIReference/API_DeviceConfigurationType.html
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'D-321|rate.limit|empírica|409' NEXT_SESSION_PROMPT.md; rg -n 'D-321|OBRIGATÓRIA|oráculo|empírica' docs/architecture/decisions-log.md | Select-Object -Last 12; rg -n 'device_configuration|challenge_required|device_only' infra/modules/cognito/main.tf; rg --files test | rg 'cognito.*auth|bff-auth'; git log -1 --format='%h %ad %s' --date=short 57942e11; rg -n 'Já existe|409' frontend/src/pages/SignUp.tsx" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 exited 1 in 187ms:
152:21. ~~Página de login customizada~~ — **REVISTO 2026-09-22 (D-321, `PENDING_PROTOCOL_REVIEW`)**:
157:    nunca removidas. Detalhe completo das 4 decisões técnicas: `decisions-log.md` D-321.
159:23. **Login/signup/reset de senha via UI própria (reversão de D-320) — `PENDING_PROTOCOL_REVIEW` (D-321, 2026-09-22)**: ver item #21 acima e `decisions-log.md` D-321. Rodar revisão adversarial quando o protocolo voltar (Codex 2026-09-23) — atenção especial à escolha `USER_PASSWORD_AUTH` (vs. SRP) e ao SECRET_HASH server-side no BFF (nova superfície de autenticação).
160:24. ~~CI vermelho pós-D-321 + drift do Managed Login~~ — **RESOLVIDO 2026-09-22 (D-322/D-323)**: specs e2e/gate k6 assumiam o redirect antigo pra Hosted UI, corrigidos. Gap aberto sem impacto real: `dev` continua `ManagedLoginVersion=2` (Terraform de D-321 não força downgrade, optional+computed) — só importa se `GET /bff/login` reativar como fallback. Detalhe: `decisions-log.md` D-322/D-323.
204:itens resolvidos (D-317/D-319/D-318/D-321, ver lista consolidada de pendências acima), mais o
205:achado incidental de CI vermelho pós-D-321 corrigido (D-322/D-323). **PR #384 (`develop`→`main`)
239:**Lição de processo (reforçada 2026-09-22, D-322)**: antes de abrir PR `develop`→`main`, SEMPRE rodar a suíte e2e completa sem filtro (`npx playwright test --project=chromium`) e conferir o CI real do próprio PR (não só de pushes individuais para `develop`) — specs desatualizadas por uma mudança de comportamento anterior (ex. D-321) podem ficar vermelhas por múltiplos commits sem ninguém perceber se cada sessão só roda specs específicas.
9:| D-325 | **D-324 não resolveu por completo — Marcelo reportou o mesmo travamento de login de novo, após o deploy de D-324. Segunda causa raiz real, mais profunda, encontrada por reprodução ao vivo (sessão real de teste, `perf-04-auth.mjs`) + reprodução determinística local (browser real, BFF mockado).** A primeira reprodução (contexto de browser totalmente limpo, nunca teve um 401 antes) funcionou perfeitamente com o fix de D-324 — o que mascarou a causa real: `AuthContext.tsx`'s `reportedUnauthorized` (um `useState` que vira `true` assim que QUALQUER 401 real acontece, latching `state` em `SESSION_EXPIRED`) só era resetado para `false` dentro de `logout()`/`logoutEverywhere()` — nunca depois de um NOVO login bem-sucedido. O comentário original do latch dizia "a única saída deste estado é a navegação de página inteira de `reauthenticate()`, que remonta este provider do zero" — verdade antes de D-321, falso desde então (`reauthenticate()` virou um `navigate()` client-side, nunca remonta `AuthProvider`). Resultado: uma vez que QUALQUER 401 acontecesse na aba (sessão expirada, não é preciso ser um erro grave), o latch travava `state` em `SESSION_EXPIRED` PARA SEMPRE dentro daquela aba — mesmo depois de um login novo 100% bem-sucedido (cookie setado certo, confirmado por reprodução: abrir só a URL do CloudFront de novo, forçando um reload de página inteira, sempre "curava" o problema, porque isso remonta `AuthProvider` do zero). O fix de D-324 (remover o efeito redundante em `login.isSuccess`) dependia inteiramente de `state.status === "AUTHENTICATED"` disparar — mas com o latch travado, `state` nunca chega lá, então D-324 sozinho não resolvia o caso real (que sempre envolve uma sessão que já expirou uma vez antes). Corrigido expondo `clearReauthLatch()` em `AuthContextValue`, chamado por `useLogin`'s `onSuccess` ANTES de invalidar a query de sessão — um login bem-sucedido é, junto de `logout()` explícito, o único sinal legítimo que supera a memória de "estava não-autorizado". Reproduzido ao vivo contra a aplicação real implantada (usuário sintético de teste, autorizado por Marcelo) — cenário de browser limpo funcionou mesmo ANTES deste fix (por isso a reprodução anterior não pegou o bug); reprodução determinística local (401 real em `/bff/api/items/dashboard` -> bounce -> login de novo) confirmou o travamento SEM este fix e a recuperação COM ele. | Nível 2-3 (correção de bug real de produção, expõe uma função nova no contexto de auth, nenhuma mudança de contrato externo) | N/A — dispensado por `AGENTS.md` §4 (correção mecânica de bug reproduzido) | N/A | `npm run typecheck`/`lint` limpos; testes novos em `AuthContext.test.tsx` (reproduz o travamento SEM o fix dentro do próprio teste, depois confirma a recuperação chamando `clearReauthLatch()`) e `Login.test.tsx` (confirma que `useLogin` chama `clearReauthLatch` no sucesso) + suíte unitária completa 57/57 arquivos 459/459 verde; suíte e2e completa com build fresco e `CI=true` 158/158 verde; reprodução end-to-end dedicada (browser real, BFF mockado, 401 real -> bounce -> login -> recupera) confirmando o comportamento antes/depois; `check-docs` limpo. | `frontend/src/auth/AuthContext.tsx`, `frontend/src/hooks/useAuthActions.ts`, `frontend/test/auth/{AuthContext,ProtectedRoute}.test.tsx`, `frontend/test/routes/auth/Login.test.tsx` | 2026-09-22 |
10:| D-324 | **Bug real de login pós-D-321 — "entro com as credenciais e não acontece nada, fica na tela" (achado de Marcelo, 2026-09-22).** Causa raiz: `Login.tsx` tinha DOIS efeitos navegando para `returnTo` — um em `state.status === "AUTHENTICATED"` (fonte de verdade real do `AuthContext`) e outro, redundante, em `login.isSuccess`. `useLogin`'s `onSuccess` só dispara `invalidateQueries(sessionQueryKey)`, sem esperar o refetch resolver — `login.isSuccess` liga bem antes do `sessionQuery` do `AuthContext` refletir a sessão nova. O efeito redundante navegava com o `state` do `AuthContext` ainda `SESSION_MISSING` (dado velho em cache), `ProtectedRoute` via isso na rota de destino e chamava `reauthenticate()` de volta pra `/login` — um mount novo do componente, sem as credenciais digitadas, parecendo travado. Confirma a segunda parte do relato (tirar o final da URL e reabrir sem precisar logar de novo): o cookie de sessão já tinha sido setado corretamente pelo login original, só o estado client-side do SPA é que ainda não tinha convergido no momento do bounce. Corrigido removendo o efeito redundante — único gatilho de navegação agora é `state.status === "AUTHENTICATED"`, nunca o retorno otimista da mutation. Investigação confirmou que o mesmo padrão NÃO existe em SignUp/ForgotPassword/ResetPassword (SignUp navega pra `/verify-email`, rota pública; ForgotPassword só troca de estado na mesma página; ResetPassword manda pra `/login`, nunca pra rota autenticada — nenhum dos três tem a corrida real de `ProtectedRoute` contra sessão ainda não confirmada). | Nível 2 (correção de bug real de producão, remove código redundante, nenhuma mudança de contrato/dado) | N/A — dispensado por `AGENTS.md` §4 (correção mecânica de bug reproduzido, mesmo naipe de D-311/D-322) | N/A | `npm run typecheck`/`lint` limpos; 2 testes novos em `Login.test.tsx` (um simula a race real e confirma que NÃO navega enquanto `state` não confirma AUTHENTICATED, outro confirma que navega uma vez que confirma) + suíte unitária completa 57/57 arquivos 457/457 verde; suíte e2e completa com build fresco e `CI=true` (evita servidor de preview obsoleto, achado de processo de D-322) 158/158 verde; `check-docs` limpo. | `frontend/src/routes/auth/Login.tsx`, `frontend/test/routes/auth/Login.test.tsx` | 2026-09-22 |
11:| D-323 | **Gate "Authenticated k6 smoke" quebrado no PR #384 (`develop`→`main`) — `perf-04-auth.mjs` dirigia um browser Playwright pelo formulário clássico da Cognito Hosted UI (`#signInFormUsername`), que parou de renderizar assim que D-321 substituiu o login principal do app por uma tela própria.** Achado adicional real ao investigar: o `ManagedLoginVersion` do domínio Cognito real de `dev` (`exptrk-dev-bff`, confirmado via `aws cognito-idp describe-user-pool-domain`) continua **2**, mesmo depois do Terraform de D-321 remover `managed_login_version` do bloco `aws_cognito_user_pool_domain` — esse argumento é optional+computed no provider AWS, então removê-lo do config nunca força um downgrade de um domínio já ativo (`terraform apply` não tem diff para aplicar). Ou seja, D-321 nunca reverteu de fato o Managed Login no mundo real, só na declaração — a rota dormente `GET /bff/login` hoje serve a UI nova (Managed Login), não a clássica que o script assumia. Sem impacto funcional no login real de usuário (o app não depende mais da Hosted UI para o fluxo principal), mas quebrava este script. Corrigido reescrevendo `perf-04-auth.mjs` para chamar `POST /bff/login` (endpoint direct-auth do próprio D-321) via `fetch` puro — mesmo contrato JSON que a tela `/login` real do app usa, sem browser/Playwright/Chromium nenhum. `ci.yml` perdeu os passos agora mortos ("Install frontend tooling"/"Install Chromium") desse job. **Gap registrado, não corrigido nesta rodada** (fora do escopo de destravar o PR): o drift real entre Terraform e o `ManagedLoginVersion` do domínio `dev` — se algum dia importar de novo (ex. reativar a rota dormente como fallback real), precisa de um `managed_login_version` explícito (não omitido) para forçar o downgrade. | Nível 2 (correção de script de infra de teste após mudança de comportamento já revisada em D-321/D-322, nenhuma mudança de código de produção ou infra real) | N/A — dispensado por `AGENTS.md` §4 (correção mecânica) | N/A | `node --check` no script limpo; `POST /bff/login` é o mesmo endpoint já coberto por `test/unit/bff/bff-handlers.test.ts` (D-321); YAML de `ci.yml` validado (`yaml.safe_load`); execução real do job só se confirma no próprio CI do PR #384 (`gh run view`, job "Authenticated k6 smoke"), não localmente (credenciais de `PERF_TEST_EMAIL`/`PASSWORD` são secret do GitHub Actions). | `docs/engineering/performance/traces/perf-04-auth.mjs`, `.github/workflows/ci.yml` | 2026-09-22 |
12:| D-322 | **CI de `develop` vermelho desde D-321 (achado real ao preparar o PR para `main`, 2026-09-22) — 4 e2e/smoke assumiam o redirect antigo para a Hosted UI (`GET /bff/login`), nunca atualizados quando D-321 trocou `reauthenticate()` para um `navigate()` client-side até `/login`.** `e2e/smoke.spec.ts` (3 testes: visita não-autenticada, 401 mid-sessão, logout) e `e2e/expiration-vertical-slice.spec.ts` (E2E-06, interrupção de sessão durante criação) esperavam `page.waitForRequest(url contendo "/bff/login")`, que nunca mais dispara — o app não faz mais nenhuma chamada de rede para essa rota ao reautenticar, só troca de URL no próprio SPA. Gap de processo real: cada sessão que implementou D-321/os ajustes seguintes só rodou specs e2e específicas (`--grep`/arquivo único), nunca `npx playwright test` sem filtro, então os 4 testes ficaram vermelhos por 5 commits (`7ad0d049`..`e47eb837`) sem ninguém perceber. Corrigido substituindo `waitForRequest("/bff/login")` por `page.waitForURL(/\/login\?/)` + asserção de `returnTo` via `new URL(page.url())` — mesma coisa que os testes já verificavam, só que contra a navegação real (client-side), não uma chamada de rede que não existe mais. `mockLoginRedirect()` (helper morto, só usado pelo E2E-06) removido. | Nível 2 (correção de teste desatualizado após mudança de comportamento já revisada em D-321, nenhuma mudança de código de produção) | N/A — dispensado por `AGENTS.md` §4 (correção mecânica de teste, mesmo naipe de D-311/D-312) | N/A | Suíte e2e completa (`npx playwright test --project=chromium`, sem filtro — nunca rodada assim desde D-321): 158/158 verdes, incluindo os 4 antes vermelhos. `npm run typecheck`/`lint` limpos. `npm run check-docs` limpo. | `frontend/e2e/{smoke.spec.ts,expiration-vertical-slice.spec.ts}` | 2026-09-22 |
13:| D-321 | **Reversão explícita de D-320 (item #21) — Marcelo, consultado diretamente, escolheu UI própria em vez de Managed Login, citando fidelidade visual e consistência com o resto do design system v2 como prioridade sobre o menor esforço/risco do caminho anterior; pediu explicitamente para implementar já, mesmo com o protocolo Claude↔Codex suspenso.** Substitui o redirect para a Hosted UI/Managed Login (D-053/D-054/D-320) por telas próprias (`frontend/src/routes/auth/{Login,SignUp,VerifyEmail,ForgotPassword,ResetPassword}.tsx`) usando os componentes reais do design system v2 (`Panel`/`TextField`/`Button`/`InlineNotice`, violeta, Plus Jakarta Sans, ícones Lucide) — os 3 pilares da ADR-0015 que D-320 documentou como inatingíveis via Managed Login. 4 decisões técnicas: **(1) Mecanismo de autenticação**: `InitiateAuth`/`AuthFlow=USER_PASSWORD_AUTH` via `@aws-sdk/client-cognito-identity-provider` (já dependência), chamado só no BFF (`src/modules/bff/persistence/cognito-idp-auth-client.ts`) — nunca `USER_SRP_AUTH`/`amazon-cognito-identity-js`: o app client já tem `generate_secret=true` (BFF pattern, D-053), então qualquer chamada precisa de `SECRET_HASH` (HMAC-SHA256 documentado da própria Cognito, não criptografia reinventada) calculado server-side; USER_PASSWORD_AUTH evita reimplementar o protocolo SRP client-side, e o SDK oficial cobre exatamente essa chamada. Credencial viaja sobre TLS direto para a API do Cognito, mesmo nível de confiança que o POST do formulário da Hosted UI já tinha. `explicit_auth_flows` trocado de `ALLOW_USER_SRP_AUTH` (nunca usado por nenhum código real) para `ALLOW_USER_PASSWORD_AUTH` (least privilege). Refresh (`/oauth2/token`) e revoke (`/oauth2/revoke`) continuam intactos (`fetch-cognito-oidc-client.ts`, nunca tocado) — só a obtenção do token inicial mudou; `BffAuthService.establishSession()` foi extraído de `handleCallback` para ser compartilhado com `loginWithPassword`, garantindo que as duas entradas (OIDC dormente e direct-auth) nunca divirjam no bootstrap de identidade/sessão. Sessão continua o cookie opaco `__Host-et_session` já existente (D-053/D-054) — nenhum formato novo. **(2) Escopo v1**: login (obrigatório) + esqueci-senha/redefinição (`ForgotPassword`/`ConfirmForgotPassword`, paridade obrigatória) + **signup + verificação de e-mail** (`SignUp`/`ConfirmSignUp`/`ResendConfirmationCode`) — investigação confirmou que o User Pool NÃO tem `admin_create_user_config` restringindo criação a admin, então o self-signup da Hosted UI já estava ativo por padrão (o próprio `Onboarding.tsx` já lida com "identidade nova sem nenhuma Organization" como fluxo real, não hipotético) — omitir signup seria uma regressão real, não só um gap aceito. MFA fica fora do escopo v1: `mfa_configuration=OPTIONAL` está configurado no pool, mas nenhuma UI de matrícula jamais existiu e nenhum usuário real tem TOTP configurado (confirmado por não haver nenhum call site de matrícula MFA em todo `src/`/`frontend/src/`) — excluir não é regressão, é o mesmo estado de hoje; `InitiateAuth` retornando um `ChallengeName` inesperado (ex. `SOFTWARE_TOKEN_MFA`) falha fechado como `UNSUPPORTED_CHALLENGE`, gap nomeado, não mascarado. **(3) Anti-enumeração**: reaproveita 100% o que o Cognito já faz — `prevent_user_existence_errors=ENABLED` (já setado desde D-053) já colapsa "usuário não existe" e "senha errada" na mesma `NotAuthorizedException` para `InitiateAuth`, e `ForgotPassword`/`ConfirmForgotPassword`/`ResendConfirmationCode` para um usuário inexistente sob esse mesmo flag não vazam a distinção — `CognitoIdpAuthClient` preserva essa postura explicitamente em código (nunca confiando só no comportamento do lado do Cognito): `UserNotFoundException` dobrado em `INVALID_CREDENTIALS`/`SUCCESS`/`SENT` conforme o caso, nunca um outcome distinto. Única exceção deliberada: `SignUp` continua revelando e-mail já cadastrado (`UsernameExistsException`/409) — mesmo comportamento que o próprio formulário de signup da Hosted UI já tinha, não uma regressão nova. **(4) Managed Login (D-320)**: revertido por completo — `user_pool_tier=ESSENTIALS` removido (nenhum recurso do plano Essentials é necessário para `InitiateAuth`/`SignUp`/`ForgotPassword`, APIs core disponíveis em todo tier, confirmado via doc oficial AWS), `managed_login_version=2` removido do domínio (volta ao hosted UI clássico v1, implícito), `aws_cognito_managed_login_branding.web_client` removido por inteiro (config morta, nenhuma página a aplicar). O domínio Cognito (`aws_cognito_user_pool_domain.this`) e as rotas `GET /bff/login`/`GET /bff/callback` (fluxo OIDC/PKCE original, D-053/D-054) foram deliberadamente MANTIDOS, nunca removidos — ainda servem `/oauth2/token`/`/oauth2/revoke` (refresh/revoke) e ficam como fallback de rollback dormente, testado, documentado, sem custo de manutenção adicional (não foi preciso alterar nenhum teste existente desses handlers). Terraform: `explicit_auth_flows`, `user_pool_tier`, `managed_login_version`, `aws_cognito_managed_login_branding` ajustados em `infra/modules/cognito/main.tf`; 6 rotas novas em `infra/modules/bff-api-gateway/main.tf` (`POST /bff/login`, `/bff/signup`, `/bff/signup/confirm`, `/bff/signup/resend`, `/bff/forgot-password`, `/bff/forgot-password/confirm`) — mesmo gap de wiring já achado 2x antes (D-117/D-120), desta vez coberto por teste `terraform test` dedicado desde o início. Achado incidental corrigido: `infra/modules/cognito/tests/cognito.tftest.hcl` tinha `domain_prefix` (variável obrigatória desde D-053/D-054) faltando em todo `run` block, fazendo a suíte inteira falhar/pular silenciosamente há meses — corrigido agora porque bloqueava a verificação das próprias asserções novas deste D-321. | Nível 5-6 (nova superfície de autenticação — troca direta de credenciais, criação de conta, redefinição de senha — reversão de uma decisão arquitetural anterior formalmente registrada) | N/A — protocolo Claude↔Codex suspenso (Codex bloqueado até 2026-09-23); Marcelo autorizou explicitamente (2026-09-21) implementar agora sem esperar o protocolo voltar | PENDING_PROTOCOL_REVIEW — revisão adversarial Codex completa (incluindo a nova superfície de segurança de autenticação) fica pendente para quando o protocolo voltar (2026-09-23), mesmo padrão de D-315/D-316/D-319 | Backend: `npm run typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas` limpos; suíte completa 271 arquivos/3208 testes verdes (+~30 novos: `bff-auth-service.test.ts`/`bff-handlers.test.ts` cobrindo `loginWithPassword`/`signUp`/`confirmSignUp`/`resendConfirmationCode`/`startForgotPassword`/`confirmForgotPassword`); cobertura 86,26%/84,66%/87,01%/86,26% (acima do threshold 85/83/85/85, D-317). Frontend: `npm run typecheck`/`lint --max-warnings=0` limpos; suíte completa 57 arquivos/452 testes verdes (+16 novos, `test/routes/auth/*.test.tsx`); `AuthContext.test.tsx` ajustado para o novo `useNavigate()` interno (precisa de `MemoryRouter` ancestor). Terraform: `terraform fmt -recursive` limpo; `terraform validate` (`AWS_PROFILE=claude-dev`, `-backend=false`) `Success!`; `terraform test` dos módulos `cognito` (4/4, incluindo a correção do `domain_prefix`) e `bff-api-gateway` (8/8, incluindo a nova asserção de 16 rotas) verdes; `terraform plan` real contra o state `dev` NÃO executado (evita concorrência com o lock global de CD, D-318) — fica para a pipeline confirmar antes do apply, nunca rodado localmente. `npm run check-docs` limpo. | `src/modules/bff/{ports/cognito-auth-client.ts,persistence/cognito-idp-auth-client.ts,application/bff-auth-service.ts,http/bff-handlers.ts,ports/cognito-oidc-client.ts,persistence/aws-jwt-id-token-verifier.ts}`, `src/runtime/aws/{composition/bff.ts,handlers/bff-handler.ts}`, `infra/modules/{cognito/main.tf,bff-api-gateway/main.tf}`, `frontend/src/{routes/auth/*,api/auth.ts,hooks/useAuthActions.ts,auth/AuthContext.tsx,auth/ProtectedRoute.tsx,api/session.ts,components/forms/TextField.tsx,App.tsx}`, `test/unit/bff/*`, `infra/modules/{cognito,bff-api-gateway}/tests/*.tftest.hcl`, `frontend/test/{routes/auth/*,auth/AuthContext.test.tsx}` | 2026-09-22 |
29:| D-306 | **Emenda de escopo de ADR-0014, mesma sessão: `cd.yml` passa a disparar em `develop`, não mais `main` — sem criar conta AWS nova.** Marcelo redirecionou explicitamente o próximo passo imediato ("não vamos usar outras contas ainda... quero apenas que o deploy na conta atual de dev seja feito por branch correspondente"): corrige a inconsistência de nomenclatura (branch de trabalho real é `develop`, mas quem disparava deploy de `dev` era `main`) dentro da ÚNICA conta AWS já existente, sem depender das Fases 2-4 do ADR-0014 (contas novas). `main` fica sem gatilho de deploy até staging/produção existirem de fato. Achado real da AWS/GitHub verificado antes de implementar (`AGENTS.md` §4): `workflow_run` sempre executa a cópia do arquivo de workflow do branch PADRÃO do repositório (`main`, confirmado via `gh repo view`), nunca a do branch que disparou o CI — a mudança só passa a valer de fato depois deste PR mergear em `main`; o próprio merge não dispara deploy (CI roda em `main` nesse push, mas `cd.yml` já teria o filtro novo `branches: [develop]`, que não casa contra um evento de `main`) — validação real só no próximo push a `develop` pós-merge. | Nível 3-4 (mudança mecânica de configuração CI/CD, sem novo domínio de risco) | N/A — correção de configuração, não decisão de arquitetura nova | N/A | `terraform`/testes não se aplicam (mudança é só `.yml`); YAML validado (`python3 -c "import yaml..."`); comentários do arquivo corrigidos para não referenciar mais proteção de branch de `main` (que nunca existiu em `develop`) como a garantia real — a garantia real é `workflow_run.conclusion == 'success'` para o `head_sha` exato, independente de proteção de branch. **Validação empírica pendente**: só confirmável observando um push real a `develop` pós-merge disparar o deploy. | `.github/workflows/cd.yml`, `docs/architecture/adr/ADR-0014-environment-separation.md` §Emenda | 2026-09-20 |
91:| D-045 | M10, fatia de guest upload (`DocumentRequest`/`DocumentSubmission`/`GuestTokenPointer`) implementada — continuação direta da mesma autorização de D-044, seguindo o design já fechado em D-037. Token opaco (`selector.secret`, só hash HMAC-SHA256+pepper persistido), `GuestSubmissionService` fora de `RequestContext`/`authorize()` por design. Antes do commit, revisão adversarial dedicada (Codex, justificada pela impossibilidade de testar contra AWS real nesta sessão) achou 2 ALTO + 3 MÉDIO reais, todos corrigidos: `GUEST_TOKEN_PEPPER` não estava wireado na infra (quebraria cold start); rota `/guest/*`+WAF não estavam provisionadas; oráculo de enumeração (rate limit consumido só após checagem de existência do pointer, `QuotaExceededError` distinguível de `GuestTokenInvalidError`); ausência de caminho dummy contra timing attack; `deadline` do `DocumentRequest` decidido em design mas nunca aplicado ao TTL do token. 4 novos testes unitários + workers `submission-finalizer`/`submission-malware-result` espelhando M6; 405 testes totais, zero regressão. Infra nova: módulo `waf` (WAFv2 Web ACL regional + rate-based rule escopada a `/guest/*`), módulo Lambda `guest_documents_handler`, `random_password` para o pepper. `terraform plan` real contra `dev` verificado (38 a criar, 54 a atualizar, 0 a destruir) — nunca aplicado | Type 1 (nível 5, implementação de decisão já aprovada em D-037) | N/A — implementação direta de design já aprovado, não decisão nova | N/A | **IMPLEMENTADO EM `develop`, NÃO DEPLOYADO** (mesma pendência de D-044: merge `develop→main` exige decisão explícita do Marcelo) | `src/modules/subject/{domain,application,http}/*guest*`, `src/modules/subject/*document-request*`, `infra/modules/waf/`, commits `c7ecf77`..`f5b87dc` | 2026-08-23 |
263:| D-206 | **Roadmap P1 item 17 ("Bulk actions") — DESIGN, protocolo Claude↔Codex completo, 4 rodadas** (`docs/architecture/reviews/bulk-actions-scoping/`). Fecha o próximo item do backlog P1 depois de D-205 (item 16). Bulk archive foi anteriormente classificado como "product creep" por falta de base em roadmap (`docs/frontend/interface-context-and-critical-tasks.md` §30) — condição que não vale mais, roadmap agora nomeia o item formalmente. **Declaração E-014: SIM PARCIAL** (Atlassian Jira bulk operations — job assíncrono/`taskId`/polling até 1000 itens — e Asana multi-select síncrono, fontes primárias 2026-09-05; padrão de mercado é split síncrono-para-pequeno/assíncrono-para-grande, este design fica no lado síncrono por proporcionalidade de escala, teto 100 não 1000). **6 achados reais do Codex, todos verificados por leitura de código, fecharam as 4 rodadas**: (1) bulk archive é mais destrutivo (`ItemDeactivated`+cancelamento de reminders, `expiration-service.ts:795`) — fechado com confirmação UX explícita obrigatória; (2) faltava contrato `{itemId, expectedVersion}` por item — sem ele perderia a garantia OCC — fechado, obrigatório; (3) teto 100 síncrono sobre o timeout real de 10s de `items_handler` (`lambda-function/variables.tf:43`) — fechado com Lambda DEDICADA (mesmo precedente de `export-handler`, D-123/D-126), `timeout_seconds=25`, dentro da cota real de 30s do HTTP API v2 (`api-gateway/main.tf:6`, correção de "REST 29s"); (4) justificativa "export processa 2000 itens" era analogia fraca (leitura vs. transação) — fechado reclassificando o teto como hipótese de engenharia condicionada a smoke test antes de produção; (5) ambiguidade de retry pós-timeout (itens já aplicados voltariam como `VERSION_CONFLICT`) — fechado com regra de reconciliação restrita (`TARGET_ALREADY_APPLIED` só quando version avançou exatamente 1 E o estado bate EXATAMENTE com o alvo, nunca afirmando autoria da tentativa); (6) `archiveItem()` atual não bloqueia rearquivar item já `ARCHIVED` com versão válida — fechado com precondição `status === "ACTIVE"` no bulk archive (`INELIGIBLE_STATE` se não elegível), comportamento single-item existente intocado. **9 decisões finais** (detalhe completo em `estado-final-consolidado.md`): lista v1 restrita a bulk reassign+archive; confirmação UX+precondição ACTIVE-only para archive; contrato por item com `expectedVersion`; fan-out via transações pequenas independentes (padrão de `import-commit-service.ts`, D-192); reconciliação de retry sem ledger; resposta síncrona com taxonomia completa de outcome; Lambda dedicada 25s com teto 100 condicionado a validação empírica; RBAC `WRITE_ROLES` sem elevação artificial. **Fora de escopo, nomeado**: bulk delete/renew/Requirement/Document; job assíncrono/ledger (só se autoria de tentativa virar requisito); teto acima de 100. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (novo mecanismo de fan-out transacional + nova Lambda dedicada + nova rota HTTP) | 9,1/10 (Rodada 4, autoavaliação às cegas) | 9,2/10 (Rodada 4, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197/D-204/D-205.** Fatias sugeridas (não decididas): (1) Lambda dedicada+rotas HTTP+RBAC; (2) fan-out+reconciliação+taxonomia de outcome; (3) smoke test de integração medindo custo real de 100 itens antes de expor em produção. | `docs/architecture/reviews/bulk-actions-scoping/` | 2026-09-05 |
266:| D-209 | **D-206 fatia 1/3 (Lambda dedicada+rotas HTTP+RBAC) — IMPLEMENTADA. Item 17 do backlog P1 agora reachable via HTTP, fecha D-206 por completo (fatias 1+2, só falta a 3 — smoke test de custo real).** Novo Lambda `bulk-actions-handler` (`timeout_seconds=25`, mesmo padrão de `export-handler`/D-123-D-126, `policy_documents_json=[tenant_facing_read_write_policy_json, gsi4_read_policy_json]` — a segunda por resolver `RequestContext` via `resolver.resolve()`, mesma regra de D-116). 2 rotas novas (`POST /items/bulk-reassign`, `POST /items/bulk-archive`) numa única integração compartilhada (padrão `reports_handler`'s "N rotas : 1 integração"), wireadas em `infra/modules/api-gateway/main.tf` (`variables.tf`+`main.tf` do módulo, `infra/main.tf`'s bloco `module.api`) e no BFF (`proxy-allowlist.ts` — mesma disciplina D-117/D-120/D-178, já que bulk actions é JSON puro, nunca esbarra no gap de `content-disposition` que mantém `/items/export`/`/reports/*` fora do BFF). 2 schemas novos (`bulk-reassign-items-request.v1.json`/`bulk-archive-items-request.v1.json`, registrados em `schema-validator.ts`) — `assigneeUserId` vazio limpa o responsável (mesma convenção de `UpdateItemRequest`), `confirm` do archive usa `const: true` (rejeita `false` e ausência, nunca infere confirmação). Novo módulo HTTP `bulk-action-handlers.ts` reusa os helpers genéricos de `item-handlers.ts` (`withErrorMapping`/`validateAgainstSchema`/`consumeApiRequestQuota`, promovidos de privados para exportados) em vez de duplicá-los — diferente de `export-handler.ts`, que precisou de um pipeline próprio só por causa do corpo CSV; bulk actions é JSON puro, então reusa o pipeline compartilhado. **Achados reais durante a implementação (nenhum exigiu rodada nova de protocolo — implementação direta de design já `APPROVED`)**: (1) `infra/modules/api-gateway/tests/api_gateway.tftest.hcl` estava com um `run` block genuinamente quebrado — faltavam `reports_invoke_arn`/`document_archive_invoke_arn`/`document_archive_guest_invoke_arn` (nunca atualizado desde D-143/D-195), preenchido com valores mock para o teste voltar a rodar (não corrigido: as contagens de rota pré-existentes `/items* == 10` vs. real 13, `/subjects* == 21` vs. 22, `/imports* == 3` vs. 5 continuam quebradas, mesma classe de drift que D-208 achou para o output `lambda_function_names`, 32 vs. 52 — nomeado, não corrigido, dívida técnica separada); confirmado que NENHUMA dessas falhas pré-existentes é bloqueante em CI (D-138: testes em `modules/*/tests/` não rodam na CI, só `infra/tests/stack.tftest.hcl` na raiz). Verificação empírica: `terraform plan` REAL contra `dev` (`AWS_PROFILE=claude-dev`) mostra exatamente **11 recursos novos** (Lambda+role+2 policies+2 attachments+alias, +integração+2 rotas+permissão) e as MESMAS 4 substituições pré-existentes já isoladas por D-208 — nada inesperado. G-V3: 8 testes novos de HTTP (`bulk-action-handlers.test.ts` — 400 em corpo ausente/inválido/campo extra/campo faltante, 200 com outcomes por item, tanto reassign quanto archive), 16 testes de `proxy-allowlist.test.ts` (2 novos + 14 preexistentes, todos verdes — achado de teste próprio: `GET /items/bulk-reassign` casa legitimamente com `{itemId}` por ambiguidade já documentada no arquivo, teste ajustado para usar `PATCH`), 10 testes de contrato novos (`schemas.test.ts`), 6 asserts novos em `api_gateway.tftest.hcl`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, suíte completa a confirmar. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO, mergeado em `main` (PR #243, `f80ace7`), CI/CD 100% verde** | `src/modules/expiration/http/{item-handlers.ts,bulk-action-handlers.ts}`, `src/runtime/aws/handlers/bulk-actions-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `schemas/api/bulk-{reassign,archive}-items-request.v1.json`, `src/shared/contracts/schema-validator.ts`, `scripts/build-lambdas.ts`, `infra/{main.tf,modules/api-gateway/{main.tf,variables.tf,tests/api_gateway.tftest.hcl}}`, `test/{unit/expiration/bulk-action-handlers.test.ts,unit/bff/proxy-allowlist.test.ts,contract/schemas.test.ts}` | 2026-09-05 |
267:| D-210 | **D-206 fatia 3/3 (smoke test de custo real) — CONCLUÍDA. Item 17 do backlog P1 (bulk actions) FECHADO POR COMPLETO — as 3 fatias de D-206 (design+fan-out/reconciliação+HTTP/RBAC+validação empírica de custo) estão todas implementadas, deployadas e verificadas ao vivo.** Medição real contra o Lambda `bulk-actions-handler` JÁ DEPLOYADO em `dev` (nunca contra `InMemoryExpirationStore`/local): tenant sintético (`smoke-test-bulk-tenant`) + identidade real (`GlobalUser`/`IdentityMapping`/`Membership` ACTIVE OWNER, seedados diretamente via DynamoDB real, mesmo formato físico que `IdentityBootstrapService`/onboarding produzem) + 100 `ExpirationItem`s reais criados via `ExpirationService.createItem()` contra `dev`. **Achado real durante a primeira tentativa de medição**: chamar o SERVIÇO diretamente da máquina local (sem passar pelo Lambda) mediu 35.479ms para 100 itens — ACIMA do orçamento de 25s — mas essa medição inclui overhead de rede pública (máquina local→AWS us-east-1), não reflete o custo real dentro do Lambda (mesma região que o DynamoDB). Corrigido invocando o Lambda REAL via `aws lambda invoke` (`--qualifier live`) com um evento HTTP API v2 sintético (claims JWT construídos à mão, nunca passam pelo API Gateway/Cognito de verdade — o Lambda não reverifica assinatura, só o API Gateway faz isso antes de invocar), medindo o tempo de invocação real de ponta a ponta. **Resultado final, definitivo**: `bulkReassignItems` para 100 itens contra o Lambda real levou **8.982ms (35,9% do orçamento de 25.000ms)** — 100/100 outcomes `SUCCEEDED`, confirmando o teto `BULK_ACTION_ITEM_CAP=100` é seguro com margem saudável (~64% de folga) no timeout atual de 25s. Achado colateral real, não um bug: uma primeira tentativa de medição com um `assigneeUserId` sintético não-membro corretamente retornou `INELIGIBLE_ASSIGNEE` para os 100 itens (RBAC/elegibilidade real funcionando, não um erro de teste) — corrigido reusando o próprio usuário seedado (membro real elegível) como assignee, produzindo a medição válida acima. Dados sintéticos limpos ao final (606 linhas deletadas via scan+delete direto, confirmado 0 linhas restantes) — scripts de smoke test eram throwaway, nunca commitados (`scripts/_smoke-*.ts`, removidos após o uso, `AGENTS.md` §1: dev 100% sintético/resetável). | Nível 1-2 (validação empírica de hipótese de engenharia já nomeada por D-206, sem decisão nova) | — | — | **CONCLUÍDO — item 17 do backlog P1 FECHADO POR COMPLETO (D-206/D-207/D-209/D-210)** | nenhum arquivo do repo alterado (só medição ao vivo contra `dev`, scripts throwaway nunca commitados) | 2026-09-05 |
289:| D-232 | **E-020 (full-audit-round2, Operações/SRE) — mecanismo de emergency rollback (.github/workflows/rollback.yml) estava completamente quebrado, CORRIGIDO.** `infra/outputs.tf`'s `lambda_function_names`/`lambda_published_versions` eram listas mantidas à mão que pararam de crescer com `infra/main.tf` (61 módulos Lambda reais hoje, só 33-34 cobertos, 44% de defasagem), e `rollback.yml` ainda hardcodava "manifesto deve ter exatamente 13 funções" — todo rollback real falhava na validação do manifesto antes de tocar qualquer alias. Decisão de mecanismo (nível 5-6, protocolo Claude<->Codex completo — `AGENTS.md` §4, `codex exec`, nota cega, 3 rodadas: Claude 8,7→8,9→9,2, Codex 8,7→8,9→9,4, ambos ≥9,0 sem arredondar na rodada 3): **codegen determinístico + verificação de drift no CI**, não migração das 61 declarações `module "*_handler"` para um único `for_each` nativo (descartado por ora — heterogeneidade real de IAM/env/triggers entre elas tornaria esse refactor um risco/escopo muito maior que o bug, sem ganho arquitetural comprovado agora; reconsiderar só se/quando existir uma abstração de config realmente estável). **Mecanismo**: `scripts/generate-lambda-manifest.ts` (+ `scripts/lib/hcl-lex.ts`, sanitizador de comentários/strings/heredocs) varre todo `infra/*.tf` por blocos `module "<nome>" { source = "./modules/lambda-function" }` (brace-depth tracking, falha explícita em bloco malformado/nome duplicado) e gera `infra/lambda-manifest.generated.tf` (formatado via `terraform fmt`) — os dois outputs saíram de `outputs.tf` na mão, nunca mais editados por humano/agente. `npm run check:lambda-manifest` (CI-blocking, `.github/workflows/ci.yml`, mesmo tier de `check-docs`) falha a build no instante em que o arquivo gerado diverge de `infra/*.tf`. `test/unit/scripts/lambda-manifest.test.ts` prova a cobertura com um oráculo estruturalmente independente (achado real da Rodada 2 do Codex: duas regex sobre a mesma hipótese não são um oráculo de verdade) — scan sequencial linha-a-linha (não brace-counting) sobre o mesmo texto sanitizado, mais uma varredura por `resource "aws_lambda_function"` fora de `infra/modules/lambda-function/main.tf` (segunda forma de criação de Lambda que o gerador não veria); `test/unit/scripts/hcl-lex.test.ts` cobre o sanitizador com fixtures adversariais (comentário de linha/bloco, string com chave escapada, heredoc). `rollback.yml`: removido o `fn_count != "13"`, substituído por checagem estrutural (`.functions` objeto não-vazio) — a igualdade de SET de nomes já existente entre manifesto-alvo e `current-healthy` já implica igualdade de contagem, então o literal não protegia nada que a checagem estrutural não cubra, só dava um segundo lugar para ficar obsoleto. `infra/tests/stack.tftest.hcl`: as duas asserções de contagem literal (`== 34`, `== 33`) viraram invariantes estruturais sem número mágico (distinção, não-vazio, `toset(keys(lambda_published_versions)) == toset(lambda_function_names)`). **Achado relacionado registrado, não corrigido nesta tarefa** (aceito explicitamente pelo Codex na Rodada 3 como fora de escopo razoável): na janela entre o deploy que introduz esta correção e o SEGUNDO deploy saudável subsequente, o rollback de Lambda fica temporariamente INDISPONÍVEL — não há manifesto anterior com conjunto de 61 nomes compatível para a igualdade de conjunto já exigida por design original (entrega 1, anterior a este bug). Aceitável (AGENTS.md §1: sem usuário real, `dev` resetável) — não é bug a contornar com `deployment_id` explícito (falharia pelo mesmo motivo); rollback volta ao normal a partir do segundo deploy saudável completo pós-correção. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2635/2635** (238 arquivos, +10 novos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos, incluindo as 3 asserções reescritas. | Nível 5-6 (mecanismo de rollback de produção-futura/deploy — decisão de como a lista de Lambdas cobertas é derivada, difícil de reverter sem reabrir o mesmo bug) | 9,2 | 9,4 | **IMPLEMENTADO** | `scripts/generate-lambda-manifest.ts`, `scripts/lib/hcl-lex.ts`, `infra/lambda-manifest.generated.tf`, `infra/outputs.tf`, `infra/tests/stack.tftest.hcl`, `.github/workflows/{rollback.yml,ci.yml}`, `package.json`, `test/unit/scripts/{lambda-manifest.test.ts,hcl-lex.test.ts}` | 2026-09-08 |
355:| D-285 | **E-017 (full-audit round2, Governança de IA) — os 2 gaps estruturais decisão-independentes nomeados na rodada (`docs/engineering/decisions-log.md` E-017) formalizados por escrito em `definition-of-done.md`.** (1) **Gate de fechamento de item de ROADMAP**: nova seção exige que a linha que declara um item 🟢 cite explicitamente a evidência de fluxo PONTA A PONTA (teste de integração real exercitando os handlers em sequência, ou verificação ao vivo pós-deploy citando o comando/consulta que confirma o efeito fim-a-fim) — fecha a causa raiz nomeada em AI-INC-004 (D-227 declarou o item 9 do roadmap fechado com "suíte verde + CI verde + deploy" quando na verdade um componente inteiro do fluxo, o worker de entrega, nunca tinha sido construído; só descoberto por verificação ao vivo pós-merge, D-228). Quando a evidência ainda não existir (bloqueio externo, ex. E-019/WhatsApp), a regra exige 🟡 explícito, nunca 🟢 com a lacuna só em prosa. (2) **Limite de profundidade de redelegação de subagente**: máximo 1 nível de subagente sem produzir progresso real verificável (diff/resultado de comando/achado concreto) antes de escalar para execução direta — fecha a causa raiz comum de AI-INC-001 (2026-08-20) e sua recorrência AI-INC-003 (2026-09-07, 3 níveis de subagentes redelegando a mesma tarefa sem progresso), ambos registrados em `ai-governance.md` §5 com a mesma causa raiz nunca corrigida até agora. Os 2 incidentes e o próprio E-017 tiveram seus status atualizados em `ai-governance.md`/`decisions-log.md` de engenharia para refletir a correção — deliberadamente classificados como **"mecanismo existe, verificação empírica ainda pendente"**, nunca "incidente fechado", já que nenhum evento real testou a regra nova ainda (honestidade de processo, mesmo padrão que o próprio E-017 já cobrava de si mesmo). | Nível 3-4 (auditoria/documentação de processo, mesma classificação que o próprio E-017 já tinha — sem código de produto tocado, protocolo Claude↔Codex dispensado pelo mesmo racional já usado para D-236/D-274/D-282, correção de registro/processo não é decisão de arquitetura) | N/A — correção de processo/documentação, não decisão de arquitetura nova | N/A | **FEITO E VERIFICADO** — `npm run check-docs` limpo | `docs/engineering/{definition-of-done.md,ai-governance.md,decisions-log.md}` | 2026-09-14 |
53:  device_configuration {
54:    challenge_required_on_new_device      = true
55:    device_only_remembered_on_user_prompt = true
test\unit\bff\bff-auth-service.test.ts
57942e11 2026-08-20 Add Terraform cognito module (ADR-0009 migration)
rg: frontend/src/pages/SignUp.tsx: IO error for operation on frontend/src/pages/SignUp.tsx: O sistema não pode encontrar o caminho especificado. (os error 3)

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'npx vitest run test/unit/bff/bff-handlers.test.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 exited 1 in 1117ms:
X [ERROR] Cannot read directory "../../..": Access is denied.

X [ERROR] Could not resolve "C:\\Users\\Usuario\\Desktop\\projects\\expiration-tracker\\vitest.config.ts"

failed to load config from C:\Users\Usuario\Desktop\projects\expiration-tracker\vitest.config.ts

⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
Error: Build failed with 2 errors:
error: Cannot read directory "../../..": Access is denied.
error: Could not resolve "C:\\Users\\Usuario\\Desktop\\projects\\expiration-tracker\\vitest.config.ts"
    at failureErrorWithLog (C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:1472:15)
    at C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:945:25
    at runOnEndCallbacks (C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:1315:45)
    at buildResponseToResult (C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:943:7)
    at C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:970:16
    at responseCallbacks.<computed> (C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:622:9)
    at handleIncomingPacket (C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:677:12)
    at Socket.readFromStdout (C:\Users\Usuario\Desktop\projects\expiration-tracker\node_modules\vite\node_modules\esbuild\lib\main.js:600:7)
    at Socket.emit (node:events:509:28)
    at addChunk (node:internal/streams/readable:563:12) {
  errors: [Getter/Setter],
  warnings: [Getter/Setter]
}




exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 .claude/skills/task-checklist/SKILL.md; Get-Content -Encoding utf8 docs/engineering/task-completion-checklist.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 98ms:
---
name: task-checklist
description: Run the Expiration Tracker task-completion checklist (docs/engineering/task-completion-checklist.md) against the work just done in this session, before marking a task/todo item complete. Use at the end of any task that produced or changed real code, a recorded decision, or a normative document in this repo — not for read-only tasks (research, planning, pure Q&A). Invoke explicitly as /task-checklist, or reach for it proactively whenever you are about to report a task as finished.
---

# Task-completion checklist runner

This project decided (Marcelo, 2026-09-18) to keep this checklist an agent-invoked skill, not a
blocking hook — `docs/engineering/definition-of-done.md` §"O que isso NÃO é" explains why (it stays
proportional: lowers the friction of applying the gate, without forcing automation the project
hasn't yet seen evidence it needs). Running this skill is still something you choose to do, exactly
like reading `definition-of-done.md` directly — this file just makes that fast and consistent.

## What to do

1. Read `docs/engineering/task-completion-checklist.md` in full — it is the actual checklist; this
   skill does not duplicate its content, only drives applying it.
2. Identify the real diff/decision this task produced (`git diff`/`git status`, the artifact
   written, or the decision registered) — never evaluate against the task's original intent, only
   against what was actually produced.
3. Walk the checklist section by section, in order, against that real diff:
   - §0: confirm this is a valid, coherent unit of completion (decompose first if it isn't).
   - §1: classify the risk level, citing the concrete trigger from `change-risk-scale.md`.
   - §2: run the gate commands the classified level requires — actually run them, read the real
     output; never assume green.
   - §3: for each of the 9 axes, mark N/A or evaluated — do not force axes the diff didn't touch,
     do not skip evaluating ones it did.
   - §4: only if this task closes a roadmap/backlog item — confirm end-to-end evidence is cited, not
     just "each slice was green in isolation."
   - §5: write the `DoD:` evidence line in the required format, in the best available location
     (commit message > review artifact > response body).
   - §6: only when concluding a phase/milestone, not every item.
   - §7: confirm the task isn't ending because of an unstated blocker — if the next step needs
     Marcelo's decision, that pending item is named and another independent thread continues.
4. If any required gate is missing, failed, or wasn't actually run: do **not** report the task as
   done. State plainly what's missing and leave the todo item `in_progress`/`pending` with the
   finding recorded — never mark something `completed` "to fix later."
5. Report the result concisely: the risk level, which gates ran (with real outcomes), which axes
   were touched, and the final `DoD:` line. Don't restate the whole checklist back to the user —
   only the parts that were actually evaluated and anything that failed.
# Checklist de conclusão de tarefa

> Uso obrigatório ao final de qualquer tarefa/item de todo list que produza ou altere código real,
> decisão registrada, ou documento normativo. Uma tarefa não está concluída até passar por esta
> checklist inteira — não é um resumo do que já foi feito, é o próprio gate. Operacionaliza em
> formato de checkbox o que já é normativo em `definition-of-done.md` + `change-risk-scale.md` +
> `quality-gate-tiers.md` + `joint-review-criteria.md` (nossos "pilares") — não os substitui, não
> duplica o texto deles, só torna a aplicação rápida de verificar item a item. Em qualquer
> divergência entre esta checklist e um dos quatro documentos-fonte, o documento-fonte vence.
> Existe uma Skill (`/task-checklist`, `.claude/skills/task-checklist/SKILL.md`) que aplica este
> arquivo de forma consistente — invocação do próprio agente, não hook bloqueante (`definition-of-
> done.md` §"O que isso NÃO é").

## 0. Isto é uma unidade válida de conclusão?

- [ ] O diff tem escopo coeso e um único nível máximo de risco identificável (`definition-of-done.md`
      §"Unidade de conclusão"). Se cruza mais de uma camada com gates diferentes (schema + handler +
      teste) ou mais de um gatilho de risco — decompor antes de continuar, não aplicar esta checklist
      a um item guarda-chuva.
- [ ] Se for um item read-only (inventário, pesquisa, leitura de código, planejamento) — esta
      checklist não se aplica; não há diff para avaliar.

## 1. Classificar o risco real (`change-risk-scale.md`)

- [ ] Nível classificado **pelo diff real produzido**, não pela intenção original do item, citando o
      gatilho concreto (ex. "nível 5 porque criou GSI novo", nunca só "parece arriscado").
- [ ] Em dúvida entre dois níveis adjacentes → usar o mais alto.
- [ ] Se a implementação revelou um contrato/fronteira de módulo novo não visível no início →
      reclassificar para 5-6 ANTES de fechar, não depois.

Nível classificado: **___** (1 Cosmético / 2 Correção mecânica / 3 Implementação já aprovada /
4 Judgment call reversível / 5 Contrato/chave/fronteira nova / 6 Decisão arquitetural formal)

## 2. Gate mínimo pelo nível (`definition-of-done.md` + `quality-gate-tiers.md`)

- [ ] **1-2**: `npm run typecheck` + `npm run lint` rodados e verdes.
- [ ] Se o diff toca `frontend/src/**` (qualquer nível) → gates `FE-G1..FE-G5`
      (`docs/frontend/frontend-engineering-quality-standard.md` §63) aplicados de fato, não só
      citados — FAIL em qualquer um bloqueia independente do nível classificado em §1.
- [ ] **3-4** (tudo do 1-2, mais): teste do módulo tocado roda e passa; `npm run check-boundaries` se
      import/fronteira mudou; `npm run validate-schemas` se schema/contrato mudou; `npm run
      build:lambdas` se handler/runtime mudou.
- [ ] Se o diff toca `src/shared/observability/**`/`app-error.ts`/log novo →
      `logging-observability-standard.md` aplicado, não só citado.
- [ ] Se o diff adiciona/altera teste automatizado → **G-V3 aplicado de fato**: cada `it()`
      novo/alterado tem comentário imediatamente acima nomeando uma mutação concreta que faria a
      asserção falhar (não basta a suíte estar verde — verde prova que passa, não que detectaria um
      bug real). G-V4 (nome/comentário declara a intenção) confirmado.
- [ ] Se o diff adiciona/altera comentário de código ou anotação de doc (decisions-log, roadmap,
      handoff) → revisão crítica de concisão aplicada (`AGENTS.md` §7): descreve só a realidade
      atual, sem meta-narrativa de correção/processo de verificação, informação extra só se
      indispensável para evitar repetir um erro real — nunca por completude/auditoria.
- [ ] **5-6** (tudo do 3-4, mais): protocolo Claude↔Codex (`AGENTS.md` §4) completo, nota ≥9,0 de
      ambos sem arredondar, artefato salvo em `docs/engineering/reviews/` ou
      `docs/architecture/reviews/`. Nível 6 adicionalmente exige ADR formal em
      `docs/architecture/adr/` + entrada em `decisions-log.md`.
- [ ] Se nível 5-6 depende de um padrão que sistemas externos já resolveram (RBAC, invite, sessão
      multi-tenant, etc.) → declaração `SIM`/`SIM PARCIAL`/`NÃO` de pesquisa externa registrada na
      Rodada 1 (`research-protocol.md`), não omitida.
- [ ] Se a decisão depende de comportamento específico de um serviço AWS → documentação oficial
      consultada antes de propor solução (`AGENTS.md` §4), não só memória interna.

## 3. Pilares/eixos de revisão — aplicar só os genuinamente tocados (`joint-review-criteria.md`)

Não forçar os 9 eixos em toda tarefa (proporcionalidade, `principles.md` #1) — marcar cada um
como **N/A** (não tocado) ou **avaliado** (com achado ou "sem achado"):

- [ ] **Arquitetura** — mudou topologia, contrato entre componentes, ou reaproveitou padrão
      existente em vez de generalizar prematuramente?
- [ ] **Qualidade de engenharia** — cobertura de teste real (não só verde), duplicação, dívida
      técnica introduzida vs. removida?
- [ ] **Engenharia de contexto** — documentos de estado (`NEXT_SESSION_PROMPT.md`,
      `docs/architecture/README.md`, decisions-log) continuam concordantes após esta mudança? Novo
      `.md` (se houver) está indexado e dentro do allowlist/guardrails de `check-doc-drift.ts`?
- [ ] **Segurança da informação/AppSec** — toca autenticação, autorização, segredo, superfície de
      ataque nova?
- [ ] **Privacidade/governança de dados** — toca PII, retenção, TTL, direito do titular?
- [ ] **Operações/SRE** — toca alarme, runbook, capacidade, rollback, DLQ?
- [ ] **Governança de IA** — envolveu delegação de subagente (checar limite de profundidade,
      `definition-of-done.md` §"Limite de profundidade de redelegação"), ou decisão que dispensou o
      protocolo Claude↔Codex (checar as 3 condições de `ai-governance.md` §2)?
- [ ] **Governança jurídica/terceiros** — introduziu dependência de terceiro, secret de vendor
      externo, ou termo contratual novo?
- [ ] **Governança de produto/multi-tenant** — toca isolamento de tenant, RBAC, ou rota HTTP nova
      (checar `authorize()` coverage, D-287)?

## 4. Evidência de ponta a ponta, se este item fecha um item de ROADMAP

- [ ] Se esta tarefa declara um item de `roadmap-competitivo-2026-09-01.md`/backlog P1/P2 como 🟢
      concluído: a linha de fechamento cita explicitamente qual evidência prova o fluxo PONTA A
      PONTA (teste de integração real, ou verificação ao vivo pós-deploy com comando/consulta
      citado) — nunca só "cada fatia estava verde isoladamente" (achado real, D-227/D-228).
- [ ] Se não há evidência ponta a ponta ainda disponível → o item fica 🟡, nomeando explicitamente o
      que falta, nunca 🟢 com a lacuna só em prosa.

## 5. Registro mínimo de evidência (obrigatório a partir do nível 3)

- [ ] Linha registrada no formato:
      `DoD: item=<nome curto>; risco=<nível + gatilho>; evidência=<comandos/rubricas e resultado>; lacunas=<nenhuma | pendência explícita>`
- [ ] Registrada no lugar certo, em ordem de preferência: (1) mensagem de commit real; (2) artefato
      de revisão em `reviews/` citando o arquivo real; (3) corpo da resposta da sessão, só se nenhum
      dos dois anteriores existir para este item.
- [ ] Gate falhou ou não rodou → item permanece `in_progress`/`pending` com o achado registrado,
      nunca marcado `completed` "para corrigir depois".

## 6. Manutenção de contexto, só ao concluir uma fase/marco (não todo item — `AGENTS.md` §6)

- [ ] Estado e próxima ação concordam entre `ARCHITECTURE.md`, `docs/architecture/README.md` e
      `NEXT_SESSION_PROMPT.md`.
- [ ] Nenhum `.md` novo foi deixado solto na raiz do repo fora do allowlist.
- [ ] `npm run check-docs` verde.

## 7. Padrão de trabalho autônomo — antes de considerar a sessão "parada"

- [ ] Se o próximo passo depende de decisão exclusiva do Marcelo (produto/arquitetura genuína,
      credencial, ação física, custo não trivial) → pendência registrada em `NEXT_SESSION_PROMPT.md`/
      `decisions-log.md`, e a sessão segue para outra frente independente em vez de ficar ociosa.
- [ ] Nenhuma ação genuinamente destrutiva/irreversível foi tomada sem confirmação (force-push,
      `terraform apply` local, deletar branch/dado real).

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n -o '.{0,100}(rate.limit|409|oráculo|empírica|antes de.*usuário|limita.{0,15}IP).{0,160}' NEXT_SESSION_PROMPT.md docs/architecture/decisions-log.md | Select-Object -Last 20; rg --files frontend | rg 'SignUp'; rg -n 'CognitoIdpAuthClient|looksAlreadyConfirmed|InvalidParameterException' test/unit/bff; git diff --stat; Get-Content -Encoding utf8 src/modules/bff/domain/csrf.ts | Select-Object -First 25; rg -n 'oauth2/token|async refresh' src/modules/bff/persistence/fetch-cognito-oidc-client.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 288ms:
docs/architecture/decisions-log.md:281:ers.ts`, mesmo padrão anti-enumeração dos outros 3 handlers guest (token inválido/expirado/revogado/rate-limitado colapsa no mesmo `GuestAccessInvalidError` genérico). Rota wireada no mesmo Lambda `document_archive_guest` (`authorization_type=NONE`) em `infra/modules/ap
docs/architecture/decisions-log.md:282:uest-access-credential.ts` (D-143, document-archive), já com a ordem anti-enumeração correta (parse→rate-limit ANTES do lookup→lookup→comparação dummy-safe) e `DocumentArchiveGuestRateLimiter.consumeBoth` (seletor+IP) — reaproveitado literalmente em vez do precedente ge
docs/architecture/decisions-log.md:282:acionalmente, token em query string, `shareId` vs. selector sem reconciliação, criação não atômica, rate limiter mal citado, comparação dummy ausente, estado `EXPIRED` sem transição real, contradição de default de TTL, semântica "CURRENT" ampliando acesso sem novo conse
docs/architecture/decisions-log.md:282:DocumentFile.scanStatus` não `status`, `DocumentVersion.state` não `status` —, IP cru persistido no rate limiter como dívida pré-existente herdada, `security-audit.ts` é CloudWatch estruturado não um stream consultável); Rodada 3→4 (contador atômico de cap sem reconcili
docs/architecture/decisions-log.md:289:a "manifesto deve ter exatamente 13 funções" — todo rollback real falhava na validação do manifesto antes de tocar qualquer alias. Decisão de mecanismo (nível 5-6, protocolo Claude<->Codex completo — `AGENTS.md` §4, `codex exec`, nota cega, 3 rodadas: Claude 8,7→8,9→9,2, Codex 8,7→8,9→9,4, ambos ≥9,0 sem arredondar na rodada 3): **codegen determinístico + verificação de drift no CI**, não migração das 61 declarações `module "*_handler"` para um único `for_each` nativo (descartado por ora — heterogeneidade real de IAM/env/triggers entre elas tornaria esse refactor um risco/escopo muito maior que o bug, sem ganho arquitetural comprovado agora; reconsiderar só se/quando existir uma abstração de config realmente estável). **Mecanismo**: `scripts/generate-lambda-manifest.ts` (+ `scripts/lib/hcl-lex.ts`, sanitizador de comentários/strings/heredocs) varre todo `infra/*.tf` por blocos `module "<nome>" { source = "./modules/lambda-function" }` (brace-depth tracking, falha explícita em bloco malformado/nome duplicado) e gera `infra/lambda-manifest.generated.tf` (formatado via `terraform fmt`) — os dois outputs saíram de `outputs.tf` na mão, nunca mais editados por humano/agente. `npm run check:lambda-manifest` (CI-blocking, `.github/workflows/ci.yml`, mesmo tier de `check-docs`) falha a build no instante em que o arquivo gerado diverge de `infra/*.tf`. `test/unit/scripts/lambda-manifest.test.ts` prova a cobertura com um oráculo estruturalmente independente (achado real da Rodada 2 do Codex: duas regex sobre a mesma hipótese não são um oráculo de verdade) — scan sequencial linha-a-linha (não brace-counting) sobre o mesmo texto sanitizado, mais uma varredura por `resource "aws_lambda_function"` fora de `infra/modules/lambda-function/main.tf` (segunda forma de criação de Lambda que o gerador não veria); `test/unit/scripts/hcl-lex.test.ts` cobre o sanitizador com fixtures adversariais (comentário de linha/bloco, string com chave escapada, heredoc). `rollback.yml`: removido o `fn_count != "13"`, substituído por checagem estrutural (`.functions` objeto não-vazio) — a igualdade de SET de nomes já existente entre manifesto-alvo e `current-healthy` já implica igualdade de contagem, então o literal não protegia nada que a checagem estrutural não cubra, só dava um segundo lugar para ficar obsoleto. `infra/tests/stack.tftest.hcl`: as duas asserções de contagem literal (`== 34`, `== 33`) viraram invariantes estruturais sem número mágico (distinção, não-vazio, `toset(keys(lambda_published_versions)) == toset(lambda_function_names)`). **Achado relacionado registrado, não corrigido nesta tarefa** (aceito explicitamente pelo Codex na Rodada 3 como fora de escopo razoável): na janela entre o deploy que introduz esta correção e o SEGUNDO deploy saudável subsequente, o rollback de Lambda fica temporariamente INDISPONÍVEL — não há manifesto anterior com conjunto de 61 nomes compatível para a igualdade de conjunto já exigida por design original (entrega 1, anterior a este bug). Aceitável (AGENTS.md §1: sem usuário real, `dev` resetável) — não é bug a contornar com `deployment_id` explícito (falharia pelo mesmo motivo); rollback volta ao normal a partir do segundo deploy 
docs/architecture/decisions-log.md:291:ty/domain/authorization.ts`, chokepoint fail-closed já existente e testado, TENANT_MISMATCH checado antes de role) + `AuthorizedTenantId` (branded type novo, `authorizedTenantId(context)`, prova de proveniência — vem de `RequestContext`, nunca de input do cliente) + suíte adversarial exaustiva NOVA (`test/integration/tenant-isolation-matrix.test.ts`, 143 casos = todas as 71 `Action`s reais × 2, derivada de `ALL_ACTIONS` que é o próprio `Object.keys(ACTION_ROLES)` — nunca uma lista paralela mantida à mão, então uma `Action` nova é coberta automaticamente, sem manutenção). Escopo consciente: propagação de `AuthorizedTenantId` para os key-builders de persistência de TODOS os módulos (document-archive/expiration/subject/organization) discutida nas Rodadas 2-3 como ideal, mas NÃO implementada nesta sessão — o tipo existe e está documentado para uso futuro; tratado como follow-up explícito, não silenciosamente abandonado, dado o tamanho real do refactor (dezenas de arquivos de domínio) frente à mitigação primária já real (matriz adversarial) que Codex confirmou ser "a maior parte do trabalho útil". **Gatilho de reavaliação explícito**: reavaliar isolamento via STS AssumeRole por tenant (session tags) ou particionamento físico de tabela ANTES de qualquer usuário real pagante em produção, ou se o produto adotar um tier enterprise com requisito contratual de isolamento por infraestrutura (não só por aplicação). Gate loca
docs/architecture/decisions-log.md:292: é um padrão amplamente estabelecido e já usado em >15 entidades de >8 módulos diferentes (sessões, rate limiters, tokens de convite/guest, `ReminderOccurrence`, `EphemeralTelemetryMutation` etc.) — a mesma mecânica, não uma nova invenção. **Correção**: campo `purgeAfte
docs/architecture/decisions-log.md:298:Document` na MESMA transação, achado do Codex Rodada 4 fechado), `resolveForAnonymousAccess` (parse→rate-limit ANTES do lookup→pointer lookup→compare dummy-safe→status/expiração→tenant ACTIVE→`DocumentFile.scanStatus=CLEAN`+`cleanObject` revalidados via `documentFileSeq
docs/architecture/decisions-log.md:298: bem-sucedida, token malformado/selector inexistente/secret errado/expirado/revogado/tenant inativo/rate-limit exaustão, revoke+decremento, revoke duplicado, listagem). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limp
docs/architecture/decisions-log.md:316:rigidos), fork serial.** Backend real (`src/modules/import/`, D-042/D-192) verificado campo-a-campo antes de tipar o frontend — dois desvios reais confirmados e documentados no código: (1) `POST /imports` não tem `targetEntityType` — todo job criado por esta tela é hardcoded `TrackedSubject` no backend, então importação de Document/Requirement (mencionada na spec prosa) não tem ponto de entrada real ainda; (2) dedupe é automático-skip apenas (`SKIP_DUPLICATE` contado, nunca criado nem atualizado) — não existe escolha "Atualizar existente/Criar como novo" em lugar algum do backend, nem rota que exponha resultado por linha, então as etapas de preview/resultado só mostram contadores agregados, nunca uma tabela linha-a-linha. Wizard único cujo passo deriva puramente do `status` real do job (nunca estado de cliente independente), então reabrir a URL persistente de um job sempre resume corretamente. **Rodada única de revisão Claude↔Codex (nota cega 5,4/10 NEEDS FIXES)**: achado CRÍTICO confirmado — mostrar o formulário de mapeamento editável durante `UPLOADED` corria contra a própria reivindicação `UPLOADED→PARSING` do worker de parse disparado por S3; se a escrita do mapeamento do cliente vencesse, escrevia `UPLOADED→UPLOADED` (nenhum novo parse disparado) e a reivindicação do worker perdia OCC e desistia para sempre — o job ficava órfão permanentemente; corrigido restringindo o formulário editável só a `AWAITING_MAPPING`. 3 achados ALTOS: valor de fallback forçado de campo obrigatório podia ser enviado sem confirmação do usuário (corrigido, bloqueia "Pré-visualizar" até todo campo obrigatório ser detectado/escolhido); chave de idempotência de upload só era renovada após sucesso, reutil
docs/architecture/decisions-log.md:320:CC (editar/vincular/desvincular/revogar/excluir) nunca invalidavam o cache, podendo repetir o mesmo 409 indefinidamente (corrigido nos 5 pontos); "Revogar" não tinha diálogo de confirmação apesar de invalidar o link imediatamente (adicionado, nomeando destinatári
docs/architecture/decisions-log.md:327: mesmo item) e um ponteiro obsoleto nunca era revalidado contra o `scope`/`itemId` real da política antes de ser devolvido; (4) conflito OCC não recarregava a versão atual, então um novo salvamento reenviava o mesmo `If-Match` obsoleto indefinidamente; (5) `invalidateQueries` sozinho deixava uma janela real entre salvar com sucesso e o refetch assentar, onde um segundo salvamento imediato ainda veria o `existingPolicyId`/`expectedVersion` pré-salvamento. **Todos os 5 corrigidos na mesma sessão** (commit `6809023`): nova action `reminder:read` (READ_ONLY_ROLES) na matriz de autorização, usada só pela rota de descoberta; `getPolicyForItem` agora desreferencia TODOS os ponteiros, descarta qualquer um cujo `tenantId`/`scope`/`itemId` da política não bate mais (mesma disciplina de `onItemDueDateChanged` do worker), e escolhe deterministicamente o candidato válido mais recentemente atualizado (nunca um `refs[0]` não validado) — cardinalidade múltipla pré-existente registrada como lacuna de domínio conhecida, não introduzida aqui, não silenciosamente resolvida por uma migração de schema fora do escopo desta sessão; `handleSave` chama `refetchPolicy()` em conflito OCC (a edição do usuário sobrevive porque o efeito só rebaseline enquanto `!dirty`); `useSaveReminderPolicy.onSuccess` escreve a resposta da mutação no cache via `setQueryData` sincron
docs/architecture/decisions-log.md:335:-018) — achado real, decisão-independente: IP bruto do guest era persistido em texto claro na PK do rate limiter (`document-archive-guest-rate-limiter.ts`).** `DOCARCHIVEGUESTIP#<ip>#RATE` gravava o endereço IP do chamador literalmente, sem hash, na dimensão "por IP" do
docs/architecture/decisions-log.md:335: rate limit multidimensional (D-143 Decisão 4) — mitigado por `purgeAfterTtl`, mas ainda PII em texto claro sentada na tabela até a TTL expirar. Corrigido: `DocumentArchiv
docs/architecture/decisions-log.md:335:ções reais + 9 pontos de instanciação em arquivos de teste. 2 testes novos (`document-archive-guest-rate-limiter.test.ts`): a chave real gravada nunca é o IP puro (é o hash), e peppers diferentes produzem hashes diferentes para o mesmo IP (prova de que é HMAC de verdade
docs/architecture/decisions-log.md:335:ação, reusa pepper já existente, nenhum secret novo, nenhuma mudança de comportamento observável do rate limit em si — mesmo limite/janela, só a REPRESENTAÇÃO da chave muda; protocolo Claude↔Codex não exigido por `AGENTS.md` §4) | N/A — correção mecânica, não decisão no
docs/architecture/decisions-log.md:335: incl. os 2 novos) sem regressão | `src/modules/document-archive/application/document-archive-guest-rate-limiter.ts`, `src/runtime/aws/composition/document-archive.ts`, `test/unit/document-archive/{document-archive-guest-rate-limiter,document-archive-guest-handlers,exte
docs/architecture/decisions-log.md:342:ws/guest-session-binding-scoping/`): Rodada 1 (proposta escrita numa sessão anterior, bloqueada por rate-limit do Codex, nunca enviada) enviada nesta sessão após probe mínimo confirmar disponibilidade — Codex aprovou de primeira, **NOTA 9,4/10, VEREDITO APPROVED, 0 acha
docs/architecture/decisions-log.md:342:`guest-session.ts`, cobertura de teste adversarial faltando, imprecisão de descrição sobre ordem do rate-limiter) — todos incorporados na implementação sem nova rodada (Rodada 2, `round2-claude-reconciliation.md`). **Mecanismo**: novo `assertPathTokenBoundToSession()` (
docs/architecture/decisions-log.md:355:aria para refletir a correção — deliberadamente classificados como **"mecanismo existe, verificação empírica ainda pendente"**, nunca "incidente fechado", já que nenhum evento real testou a regra nova ainda (honestidade de processo, mesmo padrão que o próprio E-017 já
frontend\test\routes\auth\SignUp.test.tsx
frontend\src\routes\auth\SignUp.tsx
warning: in the working copy of 'docs/engineering/performance/traces/perf-04-auth.mjs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/modules/bff/persistence/cognito-idp-auth-client.ts', LF will be replaced by CRLF the next time Git touches it
 .../performance/traces/perf-04-auth.mjs            |  7 +-
 src/modules/bff/application/bff-auth-service.ts    |  9 ++
 src/modules/bff/http/bff-handlers.ts               | 73 ++++++++++++----
 .../bff/persistence/cognito-idp-auth-client.ts     | 30 ++++++-
 test/unit/bff/bff-handlers.test.ts                 | 97 ++++++++++++++++++----
 5 files changed, 183 insertions(+), 33 deletions(-)
/**
 * CSRF — triple-layer verification per D-053/D-054: (1) Sec-Fetch-Site, checked first and
 * never fail-open when absent (older browsers that omit it fall through to the double-submit
 * check instead of being waved through); (2) X-CSRF-Token request header must equal (3) the
 * non-HttpOnly __Host-et_csrf cookie value, AND both must equal the secret stored server-side
 * on the Session record - an attacker who could somehow set the cookie cross-site (which
 * SameSite=Strict already prevents) would still fail the server-side comparison.
 */
import { randomBytes, timingSafeEqual } from "node:crypto";

export function issueCsrfSecret(): string {
  return randomBytes(32).toString("hex");
}

const SAFE_METHODS = new Set(["GET", "HEAD", "OPTIONS"]);

export function requiresCsrfCheck(method: string): boolean {
  return !SAFE_METHODS.has(method.toUpperCase());
}

/** `undefined` Sec-Fetch-Site (older browser) is treated as "not same-origin" - never
 * fail-open. Only "same-origin" and "none" (user-typed URL / bookmark, i.e. not a
 * cross-site page making the request) pass this layer. */
export function isSameSiteFetch(secFetchSite: string | undefined): boolean {
  return secFetchSite === "same-origin" || secFetchSite === "none";
2: * Real Cognito OAuth2/OIDC adapter - plain `fetch` against the Hosted UI's `/oauth2/token`
28:    const res = await fetch(`${this.domain}/oauth2/token`, {
40:  async refreshAccessToken(input: { refreshToken: string }): Promise<CognitoRefreshOutcome> {
44:      res = await fetch(`${this.domain}/oauth2/token`, {

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git diff -- test/unit/bff/bff-handlers.test.ts src/modules/bff/persistence/cognito-idp-auth-client.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 69ms:
warning: in the working copy of 'src/modules/bff/persistence/cognito-idp-auth-client.ts', LF will be replaced by CRLF the next time Git touches it
diff --git a/src/modules/bff/persistence/cognito-idp-auth-client.ts b/src/modules/bff/persistence/cognito-idp-auth-client.ts
index a884d9a8..d231b411 100644
--- a/src/modules/bff/persistence/cognito-idp-auth-client.ts
+++ b/src/modules/bff/persistence/cognito-idp-auth-client.ts
@@ -44,6 +44,21 @@ function secretHash(username: string, clientId: string, clientSecret: string): s
   return createHmac("sha256", clientSecret).update(username + clientId).digest("base64");
 }
 
+/**
+ * Best-effort match on Cognito's own (undocumented-as-contract) exception message text for "this
+ * account is already confirmed" - round-1 Codex finding (`docs/architecture/reviews/
+ * d321-direct-auth-adversarial-review/`): `confirmSignUp`/`resendConfirmationCode` used to treat
+ * ANY `NotAuthorizedException`/`InvalidParameterException` as ALREADY_CONFIRMED, which could
+ * silently report success for an unrelated authorization/parameter failure. Message text is not
+ * part of Cognito's documented API contract (only the exception TYPE is), so this is deliberately
+ * a narrowing, defense-in-depth check, never the only thing standing between a real failure and a
+ * false success - a message that doesn't match falls through to `UNKNOWN_OUTCOME` (mapped to a
+ * 503 by `BffAuthService`), never a silently assumed success.
+ */
+function looksAlreadyConfirmed(message: string | undefined): boolean {
+  return /already confirmed|current status is confirmed/i.test(message ?? "");
+}
+
 function isTransient(err: unknown): boolean {
   // Anything that isn't one of the specific, expected Cognito exceptions below is treated as
   // transient/unknown rather than silently swallowed - a thrown network error, throttling, or
@@ -146,7 +161,10 @@ export class CognitoIdpAuthClient implements CognitoAuthClient {
       return { kind: "SUCCESS" };
     } catch (err) {
       if (err instanceof CodeMismatchException || err instanceof ExpiredCodeException) return { kind: "INVALID_CODE_OR_EXPIRED" };
-      if (err instanceof NotAuthorizedException) return { kind: "ALREADY_CONFIRMED" }; // Cognito's own message for this case, when ENABLED
+      // Only the documented "already confirmed" message maps to that outcome - ANY other
+      // NotAuthorizedException (app client misconfiguration, unexpected auth failure) used to
+      // be folded into the same false success (round-1 Codex finding).
+      if (err instanceof NotAuthorizedException && looksAlreadyConfirmed(err.message)) return { kind: "ALREADY_CONFIRMED" };
       if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
       return { kind: "UNKNOWN_OUTCOME" };
     }
@@ -157,7 +175,9 @@ export class CognitoIdpAuthClient implements CognitoAuthClient {
       await this.client.send(new ResendConfirmationCodeCommand({ ClientId: this.clientId, Username: input.username, SecretHash: this.hash(input.username) }));
       return { kind: "SENT" };
     } catch (err) {
-      if (err instanceof InvalidParameterException) return { kind: "ALREADY_CONFIRMED" }; // Cognito's message when the user is already CONFIRMED
+      // Same narrowing as confirmSignUp above (round-1 Codex finding) - only the documented
+      // "already confirmed" message maps to that outcome, never every InvalidParameterException.
+      if (err instanceof InvalidParameterException && looksAlreadyConfirmed(err.message)) return { kind: "ALREADY_CONFIRMED" };
       // Anti-enumeration (decision 3), same fold as forgotPassword() below - a nonexistent
       // e-mail must resolve identically to a real, unconfirmed one from the caller's point of
       // view. Without this, UserNotFoundException would fall through to UNKNOWN_OUTCOME below
@@ -176,7 +196,11 @@ export class CognitoIdpAuthClient implements CognitoAuthClient {
       // UserNotFoundException is swallowed into SUCCESS on purpose (D-3xx decision 3) - under
       // `prevent_user_existence_errors = "ENABLED"` Cognito itself normally never throws this
       // for ForgotPassword in the first place, but this adapter does not rely on that alone.
-      if (err instanceof UserNotFoundException) return { kind: "SUCCESS" };
+      // InvalidParameterException is folded the same way (round-1 Codex finding, AWS's own docs
+      // on error suppression) - a user with no verified recovery attribute (email/phone) also
+      // throws this, which would otherwise create a 3-way oracle (real send=202, no-such-user=
+      // 202, "exists but nothing to send to"=503) distinguishable without any timing measurement.
+      if (err instanceof UserNotFoundException || err instanceof InvalidParameterException) return { kind: "SUCCESS" };
       if (isTransient(err)) return { kind: "TRANSIENT_FAILURE", cause: err };
       return { kind: "UNKNOWN_OUTCOME" };
     }
diff --git a/test/unit/bff/bff-handlers.test.ts b/test/unit/bff/bff-handlers.test.ts
index 8f4c12a1..695e93c4 100644
--- a/test/unit/bff/bff-handlers.test.ts
+++ b/test/unit/bff/bff-handlers.test.ts
@@ -317,23 +317,54 @@ describe("handleCreateOrganization", () => {
   });
 });
 
+// Round-1 Codex finding (d321-direct-auth-adversarial-review): all 6 D-3xx routes below now
+// require Fetch Metadata (`Sec-Fetch-Site: same-origin` or `none`) before anything else runs -
+// `same-origin` is the real browser value these routes see in production; every other test in
+// this section uses it so it never has to think about the guard while testing its own concern.
+const SAME_ORIGIN_HEADERS = { "sec-fetch-site": "same-origin" };
+
 // D-3xx (reversal of D-320): the app's own login/signup/reset-password screens now call these
 // directly instead of redirecting to the Cognito Hosted UI - handleLogin/handleCallback above
 // stay covered by their own existing tests (dormant fallback, never removed).
 describe("handleLoginPassword", () => {
+  it("403s a cross-site request before ever touching the body (login-CSRF guard)", async () => {
+    const { deps } = buildDeps();
+    const res = await handleLoginPassword(deps, {
+      method: "POST",
+      path: "/bff/login",
+      headers: { "sec-fetch-site": "cross-site" },
+      body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }),
+    });
+    expect(res.statusCode).toBe(403);
+  });
+
+  it("403s when Sec-Fetch-Site is absent - never fail-open for an older browser or unlabeled request", async () => {
+    const { deps } = buildDeps();
+    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
+    expect(res.statusCode).toBe(403);
+  });
+
   it("400s when email or password is missing", async () => {
     const { deps } = buildDeps();
-    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "user@example.com" }) });
+    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com" }) });
     expect(res.statusCode).toBe(400);
   });
 
+  it("400s on a malformed (non-JSON-object) body instead of a raw 500", async () => {
+    const { deps } = buildDeps();
+    const notJson = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: "not-json" });
+    expect(notJson.statusCode).toBe(400);
+    const arrayBody = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: "[]" });
+    expect(arrayBody.statusCode).toBe(400);
+  });
+
   it("sets session and CSRF cookies on a successful password login", async () => {
     const { deps, cognitoAuthClient } = buildDeps();
     cognitoAuthClient.nextAuthenticateOutcome = {
       kind: "SUCCESS",
       tokens: { accessToken: "header." + Buffer.from(JSON.stringify({ sub: "s1" })).toString("base64url") + ".sig", idToken: "id-1", refreshToken: "refresh-1", expiresInSeconds: 900 },
     };
-    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
+    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "correct-horse" }) });
     expect(res.statusCode).toBe(200);
     expect(extractCookieValue(res.cookies, SESSION_COOKIE_NAME)).toBeTruthy();
     expect(extractCookieValue(res.cookies, CSRF_COOKIE_NAME)).toBeTruthy();
@@ -342,22 +373,48 @@ describe("handleLoginPassword", () => {
   it("returns a generic 401 for invalid credentials - never distinguishes 'no such user' from 'wrong password' (anti-enumeration, D-3xx decision 3)", async () => {
     const { deps, cognitoAuthClient } = buildDeps();
     cognitoAuthClient.nextAuthenticateOutcome = { kind: "INVALID_CREDENTIALS" };
-    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "nobody@example.com", password: "wrong" }) });
+    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "nobody@example.com", password: "wrong" }) });
     expect(res.statusCode).toBe(401);
   });
 
   it("returns the same generic 401 for USER_NOT_CONFIRMED as for invalid credentials", async () => {
     const { deps, cognitoAuthClient } = buildDeps();
     cognitoAuthClient.nextAuthenticateOutcome = { kind: "USER_NOT_CONFIRMED" };
-    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: {}, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
+    const res = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
     expect(res.statusCode).toBe(401);
   });
+
+  // Round-1 Codex finding: TRANSIENT_FAILURE/UNKNOWN_OUTCOME used to fold into the SAME 401
+  // "invalid credentials" as a real wrong password - a genuine Cognito outage/throttling told
+  // the user they mistyped their password. Both now map to 503, never a false claim about the
+  // credential itself.
+  it("returns 503 (never 401) when authentication genuinely could not be completed", async () => {
+    const { deps, cognitoAuthClient } = buildDeps();
+    cognitoAuthClient.nextAuthenticateOutcome = { kind: "TRANSIENT_FAILURE", cause: new Error("boom") };
+    const transient = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
+    expect(transient.statusCode).toBe(503);
+
+    cognitoAuthClient.nextAuthenticateOutcome = { kind: "UNKNOWN_OUTCOME" };
+    const unknown = await handleLoginPassword(deps, { method: "POST", path: "/bff/login", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
+    expect(unknown.statusCode).toBe(503);
+  });
 });
 
 describe("handleSignUp", () => {
+  it("403s a cross-site request", async () => {
+    const { deps } = buildDeps();
+    const res = await handleSignUp(deps, {
+      method: "POST",
+      path: "/bff/signup",
+      headers: { "sec-fetch-site": "cross-site" },
+      body: JSON.stringify({ email: "user@example.com", password: "x", name: "Ana" }),
+    });
+    expect(res.statusCode).toBe(403);
+  });
+
   it("400s when email, password or name is missing", async () => {
     const { deps } = buildDeps();
-    const res = await handleSignUp(deps, { method: "POST", path: "/bff/signup", headers: {}, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
+    const res = await handleSignUp(deps, { method: "POST", path: "/bff/signup", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", password: "x" }) });
     expect(res.statusCode).toBe(400);
   });
 
@@ -366,7 +423,7 @@ describe("handleSignUp", () => {
     const res = await handleSignUp(deps, {
       method: "POST",
       path: "/bff/signup",
-      headers: {},
+      headers: SAME_ORIGIN_HEADERS,
       body: JSON.stringify({ email: "new@example.com", password: "correct-horse-battery-1", name: "Ana Exemplo" }),
     });
     expect(res.statusCode).toBe(202);
@@ -379,7 +436,7 @@ describe("handleSignUp", () => {
     const res = await handleSignUp(deps, {
       method: "POST",
       path: "/bff/signup",
-      headers: {},
+      headers: SAME_ORIGIN_HEADERS,
       body: JSON.stringify({ email: "taken@example.com", password: "x", name: "Ana Exemplo" }),
     });
     expect(res.statusCode).toBe(409);
@@ -389,28 +446,38 @@ describe("handleSignUp", () => {
 describe("handleConfirmSignUp", () => {
   it("400s when email or confirmationCode is missing", async () => {
     const { deps } = buildDeps();
-    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: {}, body: JSON.stringify({ email: "user@example.com" }) });
+    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com" }) });
     expect(res.statusCode).toBe(400);
   });
 
   it("204s on a valid confirmation code", async () => {
     const { deps } = buildDeps();
-    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: {}, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
+    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
     expect(res.statusCode).toBe(204);
   });
 
   it("400s on an invalid/expired code", async () => {
     const { deps, cognitoAuthClient } = buildDeps();
     cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "INVALID_CODE_OR_EXPIRED" };
-    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: {}, body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000" }) });
+    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000" }) });
     expect(res.statusCode).toBe(400);
   });
+
+  // Round-1 Codex finding: ConfirmSignUp's NotAuthorizedException used to be treated as
+  // ALREADY_CONFIRMED unconditionally - ANY authorization failure (not just "already confirmed")
+  // silently became a 204 success. Only the documented "already confirmed" message now does.
+  it("does not report success for a NotAuthorizedException that isn't the documented 'already confirmed' case", async () => {
+    const { deps, cognitoAuthClient } = buildDeps();
+    cognitoAuthClient.nextConfirmSignUpOutcome = { kind: "UNKNOWN_OUTCOME" };
+    const res = await handleConfirmSignUp(deps, { method: "POST", path: "/bff/signup/confirm", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456" }) });
+    expect(res.statusCode).toBe(503);
+  });
 });
 
 describe("handleResendConfirmationCode", () => {
   it("204s regardless of whether the e-mail is registered (anti-enumeration)", async () => {
     const { deps } = buildDeps();
-    const res = await handleResendConfirmationCode(deps, { method: "POST", path: "/bff/signup/resend", headers: {}, body: JSON.stringify({ email: "anyone@example.com" }) });
+    const res = await handleResendConfirmationCode(deps, { method: "POST", path: "/bff/signup/resend", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "anyone@example.com" }) });
     expect(res.statusCode).toBe(204);
   });
 });
@@ -418,13 +485,13 @@ describe("handleResendConfirmationCode", () => {
 describe("handleForgotPassword", () => {
   it("202s regardless of whether the e-mail is registered (anti-enumeration, D-3xx decision 3)", async () => {
     const { deps } = buildDeps();
-    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: {}, body: JSON.stringify({ email: "anyone@example.com" }) });
+    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({ email: "anyone@example.com" }) });
     expect(res.statusCode).toBe(202);
   });
 
   it("400s when email is missing", async () => {
     const { deps } = buildDeps();
-    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: {}, body: JSON.stringify({}) });
+    const res = await handleForgotPassword(deps, { method: "POST", path: "/bff/forgot-password", headers: SAME_ORIGIN_HEADERS, body: JSON.stringify({}) });
     expect(res.statusCode).toBe(400);
   });
 });
@@ -435,7 +502,7 @@ describe("handleConfirmForgotPassword", () => {
     const res = await handleConfirmForgotPassword(deps, {
       method: "POST",
       path: "/bff/forgot-password/confirm",
-      headers: {},
+      headers: SAME_ORIGIN_HEADERS,
       body: JSON.stringify({ email: "user@example.com", confirmationCode: "123456", newPassword: "Correct-Horse-1" }),
     });
     expect(res.statusCode).toBe(204);
@@ -447,7 +514,7 @@ describe("handleConfirmForgotPassword", () => {
     const res = await handleConfirmForgotPassword(deps, {
       method: "POST",
       path: "/bff/forgot-password/confirm",
-      headers: {},
+      headers: SAME_ORIGIN_HEADERS,
       body: JSON.stringify({ email: "user@example.com", confirmationCode: "000000", newPassword: "x" }),
     });
     expect(res.statusCode).toBe(400);

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Encoding utf8 docs/architecture/decisions-log.md | Where-Object { $_ -match '"'"'^'"\\| D-321 ' }; rg -n 'rate|409|oráculo|limita|empírica' NEXT_SESSION_PROMPT.md; rg -n '409|Já existe' frontend/src/routes/auth/SignUp.tsx; rg -n 'CognitoIdpAuthClient' test; git show 57942e11:infra/modules/cognito/main.tf | Select-String -Pattern 'device_configuration' -Context 0,4" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 exited 1 in 225ms:
| D-321 | **Reversão explícita de D-320 (item #21) — Marcelo, consultado diretamente, escolheu UI própria em vez de Managed Login, citando fidelidade visual e consistência com o resto do design system v2 como prioridade sobre o menor esforço/risco do caminho anterior; pediu explicitamente para implementar já, mesmo com o protocolo Claude↔Codex suspenso.** Substitui o redirect para a Hosted UI/Managed Login (D-053/D-054/D-320) por telas próprias (`frontend/src/routes/auth/{Login,SignUp,VerifyEmail,ForgotPassword,ResetPassword}.tsx`) usando os componentes reais do design system v2 (`Panel`/`TextField`/`Button`/`InlineNotice`, violeta, Plus Jakarta Sans, ícones Lucide) — os 3 pilares da ADR-0015 que D-320 documentou como inatingíveis via Managed Login. 4 decisões técnicas: **(1) Mecanismo de autenticação**: `InitiateAuth`/`AuthFlow=USER_PASSWORD_AUTH` via `@aws-sdk/client-cognito-identity-provider` (já dependência), chamado só no BFF (`src/modules/bff/persistence/cognito-idp-auth-client.ts`) — nunca `USER_SRP_AUTH`/`amazon-cognito-identity-js`: o app client já tem `generate_secret=true` (BFF pattern, D-053), então qualquer chamada precisa de `SECRET_HASH` (HMAC-SHA256 documentado da própria Cognito, não criptografia reinventada) calculado server-side; USER_PASSWORD_AUTH evita reimplementar o protocolo SRP client-side, e o SDK oficial cobre exatamente essa chamada. Credencial viaja sobre TLS direto para a API do Cognito, mesmo nível de confiança que o POST do formulário da Hosted UI já tinha. `explicit_auth_flows` trocado de `ALLOW_USER_SRP_AUTH` (nunca usado por nenhum código real) para `ALLOW_USER_PASSWORD_AUTH` (least privilege). Refresh (`/oauth2/token`) e revoke (`/oauth2/revoke`) continuam intactos (`fetch-cognito-oidc-client.ts`, nunca tocado) — só a obtenção do token inicial mudou; `BffAuthService.establishSession()` foi extraído de `handleCallback` para ser compartilhado com `loginWithPassword`, garantindo que as duas entradas (OIDC dormente e direct-auth) nunca divirjam no bootstrap de identidade/sessão. Sessão continua o cookie opaco `__Host-et_session` já existente (D-053/D-054) — nenhum formato novo. **(2) Escopo v1**: login (obrigatório) + esqueci-senha/redefinição (`ForgotPassword`/`ConfirmForgotPassword`, paridade obrigatória) + **signup + verificação de e-mail** (`SignUp`/`ConfirmSignUp`/`ResendConfirmationCode`) — investigação confirmou que o User Pool NÃO tem `admin_create_user_config` restringindo criação a admin, então o self-signup da Hosted UI já estava ativo por padrão (o próprio `Onboarding.tsx` já lida com "identidade nova sem nenhuma Organization" como fluxo real, não hipotético) — omitir signup seria uma regressão real, não só um gap aceito. MFA fica fora do escopo v1: `mfa_configuration=OPTIONAL` está configurado no pool, mas nenhuma UI de matrícula jamais existiu e nenhum usuário real tem TOTP configurado (confirmado por não haver nenhum call site de matrícula MFA em todo `src/`/`frontend/src/`) — excluir não é regressão, é o mesmo estado de hoje; `InitiateAuth` retornando um `ChallengeName` inesperado (ex. `SOFTWARE_TOKEN_MFA`) falha fechado como `UNSUPPORTED_CHALLENGE`, gap nomeado, não mascarado. **(3) Anti-enumeração**: reaproveita 100% o que o Cognito já faz — `prevent_user_existence_errors=ENABLED` (já setado desde D-053) já colapsa "usuário não existe" e "senha errada" na mesma `NotAuthorizedException` para `InitiateAuth`, e `ForgotPassword`/`ConfirmForgotPassword`/`ResendConfirmationCode` para um usuário inexistente sob esse mesmo flag não vazam a distinção — `CognitoIdpAuthClient` preserva essa postura explicitamente em código (nunca confiando só no comportamento do lado do Cognito): `UserNotFoundException` dobrado em `INVALID_CREDENTIALS`/`SUCCESS`/`SENT` conforme o caso, nunca um outcome distinto. Única exceção deliberada: `SignUp` continua revelando e-mail já cadastrado (`UsernameExistsException`/409) — mesmo comportamento que o próprio formulário de signup da Hosted UI já tinha, não uma regressão nova. **(4) Managed Login (D-320)**: revertido por completo — `user_pool_tier=ESSENTIALS` removido (nenhum recurso do plano Essentials é necessário para `InitiateAuth`/`SignUp`/`ForgotPassword`, APIs core disponíveis em todo tier, confirmado via doc oficial AWS), `managed_login_version=2` removido do domínio (volta ao hosted UI clássico v1, implícito), `aws_cognito_managed_login_branding.web_client` removido por inteiro (config morta, nenhuma página a aplicar). O domínio Cognito (`aws_cognito_user_pool_domain.this`) e as rotas `GET /bff/login`/`GET /bff/callback` (fluxo OIDC/PKCE original, D-053/D-054) foram deliberadamente MANTIDOS, nunca removidos — ainda servem `/oauth2/token`/`/oauth2/revoke` (refresh/revoke) e ficam como fallback de rollback dormente, testado, documentado, sem custo de manutenção adicional (não foi preciso alterar nenhum teste existente desses handlers). Terraform: `explicit_auth_flows`, `user_pool_tier`, `managed_login_version`, `aws_cognito_managed_login_branding` ajustados em `infra/modules/cognito/main.tf`; 6 rotas novas em `infra/modules/bff-api-gateway/main.tf` (`POST /bff/login`, `/bff/signup`, `/bff/signup/confirm`, `/bff/signup/resend`, `/bff/forgot-password`, `/bff/forgot-password/confirm`) — mesmo gap de wiring já achado 2x antes (D-117/D-120), desta vez coberto por teste `terraform test` dedicado desde o início. Achado incidental corrigido: `infra/modules/cognito/tests/cognito.tftest.hcl` tinha `domain_prefix` (variável obrigatória desde D-053/D-054) faltando em todo `run` block, fazendo a suíte inteira falhar/pular silenciosamente há meses — corrigido agora porque bloqueava a verificação das próprias asserções novas deste D-321. | Nível 5-6 (nova superfície de autenticação — troca direta de credenciais, criação de conta, redefinição de senha — reversão de uma decisão arquitetural anterior formalmente registrada) | N/A — protocolo Claude↔Codex suspenso (Codex bloqueado até 2026-09-23); Marcelo autorizou explicitamente (2026-09-21) implementar agora sem esperar o protocolo voltar | PENDING_PROTOCOL_REVIEW — revisão adversarial Codex completa (incluindo a nova superfície de segurança de autenticação) fica pendente para quando o protocolo voltar (2026-09-23), mesmo padrão de D-315/D-316/D-319 | Backend: `npm run typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas` limpos; suíte completa 271 arquivos/3208 testes verdes (+~30 novos: `bff-auth-service.test.ts`/`bff-handlers.test.ts` cobrindo `loginWithPassword`/`signUp`/`confirmSignUp`/`resendConfirmationCode`/`startForgotPassword`/`confirmForgotPassword`); cobertura 86,26%/84,66%/87,01%/86,26% (acima do threshold 85/83/85/85, D-317). Frontend: `npm run typecheck`/`lint --max-warnings=0` limpos; suíte completa 57 arquivos/452 testes verdes (+16 novos, `test/routes/auth/*.test.tsx`); `AuthContext.test.tsx` ajustado para o novo `useNavigate()` interno (precisa de `MemoryRouter` ancestor). Terraform: `terraform fmt -recursive` limpo; `terraform validate` (`AWS_PROFILE=claude-dev`, `-backend=false`) `Success!`; `terraform test` dos módulos `cognito` (4/4, incluindo a correção do `domain_prefix`) e `bff-api-gateway` (8/8, incluindo a nova asserção de 16 rotas) verdes; `terraform plan` real contra o state `dev` NÃO executado (evita concorrência com o lock global de CD, D-318) — fica para a pipeline confirmar antes do apply, nunca rodado localmente. `npm run check-docs` limpo. | `src/modules/bff/{ports/cognito-auth-client.ts,persistence/cognito-idp-auth-client.ts,application/bff-auth-service.ts,http/bff-handlers.ts,ports/cognito-oidc-client.ts,persistence/aws-jwt-id-token-verifier.ts}`, `src/runtime/aws/{composition/bff.ts,handlers/bff-handler.ts}`, `infra/modules/{cognito/main.tf,bff-api-gateway/main.tf}`, `frontend/src/{routes/auth/*,api/auth.ts,hooks/useAuthActions.ts,auth/AuthContext.tsx,auth/ProtectedRoute.tsx,api/session.ts,components/forms/TextField.tsx,App.tsx}`, `test/unit/bff/*`, `infra/modules/{cognito,bff-api-gateway}/tests/*.tftest.hcl`, `frontend/test/{routes/auth/*,auth/AuthContext.test.tsx}` | 2026-09-22 |
259:Ciclos A/B/C, PERF-13/14 completos e implantados (2026-09-18). PERF-11/11-b: teto real é o rate
66:        ? "Já existe uma conta com este e-mail."
fatal: path 'infra/modules/cognito/main.tf' exists on disk, but not in '57942e11'

codex
**Nota desta rodada: 8.4/10.** As correções são reais, mas a tréplica do achado 5 contradiz uma definição explícita da AWS. Também há diferença entre os registros prometidos e os arquivos presentes. Não li a autoavaliação do Claude.

1. **Login-CSRF — corrigido.** `requireSameSiteFetch()` precede parsing e chamada de serviço nas seis rotas. A primitiva aceita somente `same-origin`/`none`; ausência, `same-site` e `cross-site` são rejeitados. Os testes de login e signup exercitam efetivamente o 403. Entretanto, o teste denominado “before ever touching the body” não comprova essa ordem: usa JSON válido e não verifica ausência de chamadas ao serviço. A ordem está correta por inspeção.

2. **Forgot-password — corrigido para o caso apontado.** O adapter realmente transforma `InvalidParameterException` e `UserNotFoundException` em `SUCCESS`, eliminando a distinção por status identificada anteriormente. Falta teste do adapter lançando essas exceções reais; o teste HTTP com cliente falso não comprova esse mapeamento.

3. **Confirmação — correção funcional aceita, com ressalvas.** As exceções genéricas deixaram de implicar sucesso automaticamente. O reconhecimento textual é limitado e sua fragilidade está declarada. Porém:
   - O teste novo injeta `UNKNOWN_OUTCOME` no cliente falso; **não lança `NotAuthorizedException` nem executa `looksAlreadyConfirmed()`**. Ele passaria mesmo revertendo a correção do adapter.
   - Saber que uma conta está **confirmada** acrescenta informação à existência da conta. Portanto, “estritamente mais fraco” não é uma justificativa correta. Aceito o residual como explicitamente reconhecido nesta rodada, mas sua aceitação deve ser descrita nesses termos.

4. **Indisponibilidade no login — corrigido e bem exercitado.** `TRANSIENT_FAILURE` e `UNKNOWN_OUTCOME` passam pelo serviço real até o HTTP 503 nos testes. A distinção em relação ao 401 está efetivamente verificada.

5. **Device remembering — a tréplica não procede na conclusão central.** Confirmei a ausência das chamadas de dispositivo em `src/` e a presença das duas propriedades em `device_configuration`. Esses fatos não demonstram desativação da funcionalidade no pool.

   A AWS é explícita: “When you provide a value for any property of `DeviceConfiguration`, you activate the device remembering for the user pool.” **Ativar a funcionalidade no pool e confirmar um dispositivo são operações distintas.** `ConfirmDevice` registra o dispositivo; não é condição para considerar a configuração do pool ativa. [AWS — DeviceConfigurationType](https://docs.aws.amazon.com/cognito-user-identity-pools/latest/APIReference/API_DeviceConfigurationType.html)

   O argumento do OIDC antigo também não fecha a questão: a própria documentação distingue os caminhos. Tokens obtidos por authorization code podem ser renovados pelo endpoint; tokens obtidos por `InitiateAuth` têm a restrição quando remembered devices está ativo. Logo, semanas de sucesso do fluxo OIDC são compatíveis com uma incompatibilidade no novo fluxo. [AWS — Token endpoint](https://docs.aws.amazon.com/cognito/latest/developerguide/token-endpoint.html)

   **Mantenho o achado por essa evidência, não por inércia.** Reconheço que a verificação empírica foi registrada como pendente; não a trato como gap ignorado. O problema é classificá-la como simples ambiguidade documental quando existe uma restrição explícita aplicável.

   Para fechar empiricamente: registrar a configuração efetiva do pool/app client, obter um refresh token por `USER_PASSWORD_AUTH` e executar a renovação pelo adapter real, preservando status/erro sem expor tokens. Não é necessário esperar o access token expirar para testar a troca diretamente. Ainda falta essa evidência; não afirmo ter reproduzido a falha em `dev`.

6. **Signup 409 — decisão razoável e aceita.** Confirmei a mensagem no frontend e a exceção deliberada na entrada D-321 de `decisions-log.md`. A revisão explica o benefício de UX e reconhece a exposição. Isso é suficiente para aceitar a decisão neste estágio. Não encontrei a anotação específica prometida em `NEXT_SESSION_PROMPT.md`, mas isso não invalida o registro existente.

7. **Rate limiting — adiamento aceito, registro parcialmente divergente.** A própria revisão registra explicitamente a obrigatoriedade antes de usuários reais; conto isso como encaminhamento válido, conforme solicitado. Entretanto, **não encontrei essa pendência em `NEXT_SESSION_PROMPT.md` nem na entrada D-321**, apesar da afirmação de que foi registrada nesses locais. Falta reconciliar o acompanhamento operacional com a revisão.

8. **Entrada malformada — corrigida.** O parser compartilhado trata JSON inválido e valores não objeto nas seis rotas. O teste de `not-json` detecta a regressão anterior. Já `[]` retornaria 400 anteriormente por ausência dos campos obrigatórios, portanto não comprova sozinho a nova validação estrutural. `null` seria um caso de regressão mais discriminante.

O helper `perf-04-auth.mjs` envia corretamente `sec-fetch-site: none` no `fetch` de login. Não executei o smoke contra `dev`.

**Não identifiquei regressão funcional nova no diff desta rodada.** Identifiquei uma nova deficiência de teste: o caso nomeado como verificação de `NotAuthorizedException` não alcança o adapter que pretende proteger.

Tentei executar `npx vitest run test/unit/bff/bff-handlers.test.ts`, mas o carregamento da configuração falhou por acesso negado no ambiente. Assim, minha validação dos testes foi estática; não declaro suíte verde. A principal razão para permanecer abaixo de 9,0 é a conclusão incorreta do achado 5, seguida pelas limitações de evidência e registro acima.

NOTA_CODEX: 8.4
tokens used
86.074
**Nota desta rodada: 8.4/10.** As correções são reais, mas a tréplica do achado 5 contradiz uma definição explícita da AWS. Também há diferença entre os registros prometidos e os arquivos presentes. Não li a autoavaliação do Claude.

1. **Login-CSRF — corrigido.** `requireSameSiteFetch()` precede parsing e chamada de serviço nas seis rotas. A primitiva aceita somente `same-origin`/`none`; ausência, `same-site` e `cross-site` são rejeitados. Os testes de login e signup exercitam efetivamente o 403. Entretanto, o teste denominado “before ever touching the body” não comprova essa ordem: usa JSON válido e não verifica ausência de chamadas ao serviço. A ordem está correta por inspeção.

2. **Forgot-password — corrigido para o caso apontado.** O adapter realmente transforma `InvalidParameterException` e `UserNotFoundException` em `SUCCESS`, eliminando a distinção por status identificada anteriormente. Falta teste do adapter lançando essas exceções reais; o teste HTTP com cliente falso não comprova esse mapeamento.

3. **Confirmação — correção funcional aceita, com ressalvas.** As exceções genéricas deixaram de implicar sucesso automaticamente. O reconhecimento textual é limitado e sua fragilidade está declarada. Porém:
   - O teste novo injeta `UNKNOWN_OUTCOME` no cliente falso; **não lança `NotAuthorizedException` nem executa `looksAlreadyConfirmed()`**. Ele passaria mesmo revertendo a correção do adapter.
   - Saber que uma conta está **confirmada** acrescenta informação à existência da conta. Portanto, “estritamente mais fraco” não é uma justificativa correta. Aceito o residual como explicitamente reconhecido nesta rodada, mas sua aceitação deve ser descrita nesses termos.

4. **Indisponibilidade no login — corrigido e bem exercitado.** `TRANSIENT_FAILURE` e `UNKNOWN_OUTCOME` passam pelo serviço real até o HTTP 503 nos testes. A distinção em relação ao 401 está efetivamente verificada.

5. **Device remembering — a tréplica não procede na conclusão central.** Confirmei a ausência das chamadas de dispositivo em `src/` e a presença das duas propriedades em `device_configuration`. Esses fatos não demonstram desativação da funcionalidade no pool.

   A AWS é explícita: “When you provide a value for any property of `DeviceConfiguration`, you activate the device remembering for the user pool.” **Ativar a funcionalidade no pool e confirmar um dispositivo são operações distintas.** `ConfirmDevice` registra o dispositivo; não é condição para considerar a configuração do pool ativa. [AWS — DeviceConfigurationType](https://docs.aws.amazon.com/cognito-user-identity-pools/latest/APIReference/API_DeviceConfigurationType.html)

   O argumento do OIDC antigo também não fecha a questão: a própria documentação distingue os caminhos. Tokens obtidos por authorization code podem ser renovados pelo endpoint; tokens obtidos por `InitiateAuth` têm a restrição quando remembered devices está ativo. Logo, semanas de sucesso do fluxo OIDC são compatíveis com uma incompatibilidade no novo fluxo. [AWS — Token endpoint](https://docs.aws.amazon.com/cognito/latest/developerguide/token-endpoint.html)

   **Mantenho o achado por essa evidência, não por inércia.** Reconheço que a verificação empírica foi registrada como pendente; não a trato como gap ignorado. O problema é classificá-la como simples ambiguidade documental quando existe uma restrição explícita aplicável.

   Para fechar empiricamente: registrar a configuração efetiva do pool/app client, obter um refresh token por `USER_PASSWORD_AUTH` e executar a renovação pelo adapter real, preservando status/erro sem expor tokens. Não é necessário esperar o access token expirar para testar a troca diretamente. Ainda falta essa evidência; não afirmo ter reproduzido a falha em `dev`.

6. **Signup 409 — decisão razoável e aceita.** Confirmei a mensagem no frontend e a exceção deliberada na entrada D-321 de `decisions-log.md`. A revisão explica o benefício de UX e reconhece a exposição. Isso é suficiente para aceitar a decisão neste estágio. Não encontrei a anotação específica prometida em `NEXT_SESSION_PROMPT.md`, mas isso não invalida o registro existente.

7. **Rate limiting — adiamento aceito, registro parcialmente divergente.** A própria revisão registra explicitamente a obrigatoriedade antes de usuários reais; conto isso como encaminhamento válido, conforme solicitado. Entretanto, **não encontrei essa pendência em `NEXT_SESSION_PROMPT.md` nem na entrada D-321**, apesar da afirmação de que foi registrada nesses locais. Falta reconciliar o acompanhamento operacional com a revisão.

8. **Entrada malformada — corrigida.** O parser compartilhado trata JSON inválido e valores não objeto nas seis rotas. O teste de `not-json` detecta a regressão anterior. Já `[]` retornaria 400 anteriormente por ausência dos campos obrigatórios, portanto não comprova sozinho a nova validação estrutural. `null` seria um caso de regressão mais discriminante.

O helper `perf-04-auth.mjs` envia corretamente `sec-fetch-site: none` no `fetch` de login. Não executei o smoke contra `dev`.

**Não identifiquei regressão funcional nova no diff desta rodada.** Identifiquei uma nova deficiência de teste: o caso nomeado como verificação de `NotAuthorizedException` não alcança o adapter que pretende proteger.

Tentei executar `npx vitest run test/unit/bff/bff-handlers.test.ts`, mas o carregamento da configuração falhou por acesso negado no ambiente. Assim, minha validação dos testes foi estática; não declaro suíte verde. A principal razão para permanecer abaixo de 9,0 é a conclusão incorreta do achado 5, seguida pelas limitações de evidência e registro acima.

NOTA_CODEX: 8.4
