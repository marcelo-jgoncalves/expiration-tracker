# Rodada 1 — auto-nota cega do Claude

**8.2/10** (registrada antes de rodar o Codex).

**Nota de processo (achado real, corrigido a partir da Rodada 2 em diante)**: esta auto-nota foi
escrita DENTRO do próprio `round-1-claude-proposal.md`, o mesmo arquivo que o Codex leu — o Codex
corretamente apontou que isso quebra o protocolo de nota cega ("esta avaliação não pode ser
registrada como estritamente cega"). A partir da Rodada 2, a auto-nota do Claude vive sempre num
arquivo separado (`round-N-claude-selfgrade.md`, como este), nunca incluído na lista de arquivos
que o prompt do Codex pede para ler.
