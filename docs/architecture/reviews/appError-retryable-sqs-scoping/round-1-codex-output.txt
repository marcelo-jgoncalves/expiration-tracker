2026-08-31T17:51:45.802271Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a058f2-de46-7b73-8580-371582278bf4
--------
user
# AppError.retryable — SQS consumer behavior: Round 1 (Claude proposal)

## Contexto herdado (E-011, `docs/engineering/decisions-log.md`)

Auditoria 2026-08-29 achou `AppError.retryable`/`isRetryable()` documentados de forma enganosa
("drives SQS consumer behavior") quando, na prática, nenhum handler SQS real ramifica sobre o
valor — todos reportam `batchItemFailure` incondicionalmente, deixando `maxReceiveCount`+DLQ
nativos do SQS serem o árbitro real de retry-vs-terminal. Marcelo decidiu na época: manter o
comportamento, só corrigir a documentação para ser honesta sobre o escopo atual (diagnóstico/log,
não roteamento). Ficou registrado como pendência explícita: **deveria um handler SQS real passar
a ramificar sobre `retryable`?** Se sim, o que acontece com uma mensagem `retryable:false`
(poison message) — dropar com log de auditoria, mandar direto pro DLQ via chamada SQS explícita,
outra coisa? Quais os riscos de modo de falha (uma classificação errada agora causaria perda real
de dado/processamento pulado, não só retry desperdiçado)?

Autoridade ampliada (`docs/engineering/ai-governance.md` §1, 2026-08-31): este resíduo de produto
está explicitamente dentro do escopo que Marcelo autorizou Claude+Codex a decidir via protocolo
completo.

## Ground truth verificado (não assumido)

- `src/shared/errors/app-error.ts`: taxonomia consistente na prática — todo erro de validação/
  regra de negócio/autorização é `retryable:false` (determinístico, mesma entrada falha do mesmo
  jeito); `DependencyUnavailableError` default `true` (falha de dependência externa, correto:
  throttle/timeout/5xx valem retry); a única exceção real é `ExtractionCommitFailedError`
  (`DEPENDENCY_UNAVALABLE`, `retryable:true`) — correta por documentação própria: falha de commit
  no DynamoDB após sucesso do Textract é candidata legítima a retry, não um erro de payload.
  **Nenhuma inconsistência real encontrada** — a classificação existente já é uma base sã para
  uma política de branching, ao contrário do que a pergunta do prompt sugeriu verificar.
- 3 handlers SQS lidos (`reminder-dispatch-handler.ts`, `email-delivery-handler.ts`,
  `textract-task-handler.ts` por grep) confirmam o padrão idêntico: schema-inválido já É tratado
  como "poison message" NO COMENTÁRIO, mas ainda reporta `batchItemFailure` (retry incondicional
  até o DLQ nativo agir).
- `infra/modules/sqs-worker-queue/main.tf`: `redrive_policy` com `maxReceiveCount` fixo em 5
  (provado por `infra/tests/stack.tftest.hcl:318` e `sqs_worker_queue.tftest.hcl:20`), uniforme
  em todas as filas deste módulo. DLQ real já wireado, sem mecanismo paralelo a inventar.

## Pesquisa externa — declaração (`research-protocol.md`, E-014)

**SIM** — poison-message handling em Lambda+SQS é um padrão bem estabelecido, documentado pela
própria AWS com exemplos de referência.

Fonte 1: AWS Lambda Developer Guide, "Handling errors for an SQS event source in Lambda"
(`docs.aws.amazon.com/lambda/latest/dg/services-sqs-errorhandling.md`, conteúdo obtido via
WebFetch 2026-08-31). Achado central: **todos** os exemplos de referência oficiais da AWS (10
linguagens, incluindo TypeScript) capturam `catch (error)` genérico e reportam
`batchItemFailure` incondicionalmente — nenhum exemplo oficial ramifica por tipo/classe de erro.
A doc não menciona nenhum padrão de "enviar direto pro DLQ via API" como recomendação central;
todo o mecanismo de retry-vs-terminal descrito ali é `maxReceiveCount`+redrive policy nativo.
Trecho literal: "When an invocation fails, Lambda attempts to retry the invocation while
implementing a backoff strategy... If your function code caused the error, Lambda stops
processing and retrying the invocation [current batch]... After your queue's visibility timeout
runs out, the message reappears in the queue" — quem decide releitura vs. DLQ é a fila (SQS),
nunca o handler.

Fonte 2: mesma página, seção "Implementing partial batch responses" — confirma que
`ReportBatchItemFailures` existe para não reprocessar itens BEM-SUCEDIDOS do mesmo batch, não
para dar ao handler um mecanismo de terminação antecipada por classificação de erro.

**Conclusão da pesquisa**: o padrão canônico da própria AWS é exatamente o que este codebase já
faz (branching zero, redrive nativo). Um branching explícito no handler para mandar
`retryable:false` direto ao DLQ seria um desvio do padrão de referência, não uma correção de
gap — precisa justificar-se por um ganho concreto (menos ruído/latência de retry inútil) que supere
o risco novo que introduz (uma chamada SQS direta ao DLQ, e o modo de falha de classificação
errada virar perda real em vez de desperdício).

## Checklist ponderado (derivado da pesquisa)

1. **Alinhamento com o padrão de referência AWS** (30%) — manter o mecanismo nativo
   (`maxReceiveCount`+redrive) como única fonte de verdade de retry-vs-terminal pontua alto;
   inventar um caminho paralelo pontua baixo a menos que o ganho justifique o desvio.
2. **Blast radius de uma classificação errada** (25%) — uma mensagem transitória mal classificada
   como `retryable:false` sob um mecanismo de branch-and-drop-early causa perda real; sob o
   mecanismo atual (sempre retry até `maxReceiveCount`), o pior caso de má classificação é
   inofensivo (mensagem que já teria ido pro DLQ de qualquer forma, só via caminho nativo).
3. **Ganho real mensurável** (20%) — quantas retentativas são efetivamente desperdiçadas hoje?
   Com `maxReceiveCount=5` e backoff da própria Lambda (não configurável por mensagem), o custo é
   pequeno (minutos, não horas) e nenhuma fila deste sistema é sensível a latência de poison
   message (nenhuma é sync-user-facing).
4. **Simplicidade/reversibilidade** (15%) — zero código novo pontua alto; qualquer chamada SQS
   direta (`SendMessage` ao DLQ + delete manual da fonte) introduz um segundo caminho de escrita
   à fila DLQ, exigindo IAM extra, coordenação de exclusão dupla (a mensagem original ainda existe
   até `batchItemFailure` decidir; mandar cópia ao DLQ sem tirar da fonte duplica).
5. **Consistência entre as 10+ filas do sistema** (10%) — qualquer política deve valer
   uniformemente (mesmo módulo Terraform, mesmo `maxReceiveCount=5`), não caso a caso.

## Proposta Round 1

**Não implementar branching ativo sobre `retryable` em nenhum handler SQS.** Fechar a pendência de
E-011 como decisão explícita (não mais "pendente"), com o seguinte raciocínio registrado:

1. O padrão de referência da própria AWS confirma (Fonte 1/2 acima) que retry-vs-terminal deve
   ser decidido pelo mecanismo nativo da fila, não por um `if` no handler — o codebase já segue
   esse padrão.
2. `maxReceiveCount=5` já limita o blast radius de uma mensagem verdadeiramente poison a 5
   tentativas (minutos, não horas) antes do DLQ nativo assumir — o "desperdício" que uma
   ramificação ativa eliminaria é pequeno e não observado como problema real hoje (nenhum
   incidente de fila saturada por poison message neste projeto).
3. O risco novo que o branching introduziria (uma classificação `retryable:false` errada agora
   causando perda real de mensagem, em vez de apenas retries desperdiçados) é assimétrico e pior
   do que o problema que resolveria — especialmente porque a superfície de erro `retryable:false`
   inclui categorias amplas (`VALIDATION`, `BUSINESS_RULE`, `AUTHORIZATION`) cuja classificação é
   feita para contexto HTTP síncrono (M1), nunca auditada pensando em "é seguro derrubar esta
   mensagem de fila permanentemente".
4. Nenhuma fila deste sistema hoje é sensível a latência de poison message a ponto de justificar a
   complexidade/IAM extra de um roteamento explícito ao DLQ.

**Ação concreta**: fechar E-011 com uma entrada nova no decisions-log (`D-128`,
`docs/architecture/decisions-log.md`, mesmo padrão de D-124/D-125/D-127), reescrever o comentário
de `AppError`/`isRetryable()` em `app-error.ts` para declarar a decisão como fechada (não mais
"pendência", mas "decidido: não branch, ver D-128"), sem nenhuma mudança de comportamento de
runtime. Nenhuma mudança nos handlers SQS além do comentário/doc.

## Fora de escopo (explícito)

- Qualquer mudança em `maxReceiveCount`/redrive policy (permanece 5, decisão anterior não
  revisitada aqui).
- Powertools for AWS Lambda batch processor (mencionado pela doc oficial como utilitário) — fora
  de escopo, mudança de dependência não solicitada.
- Revisitar a classificação `retryable` de qualquer subclasse individual de `AppError` — auditada
  e considerada consistente (ver Ground truth acima), nenhuma mudança proposta.

codex
Vou avaliar a proposta como a parte Codex da Rodada 1, com nota cega e foco no checklist ponderado. Primeiro vou conferir as fontes normativas e o estado real do repositório; nesta rodada não farei alterações.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 191ms:
# Expiration Tracker â€” Estado Atual + PrÃ³xima AÃ§Ã£o

> Este arquivo Ã© estado atual + prÃ³xima aÃ§Ã£o (`AGENTS.md` Â§2), nunca fonte normativa e nunca histÃ³rico narrativo â€” histÃ³ria detalhada vive em `docs/architecture/{session-log,decisions-log}.md` e nas pastas `reviews/`. Recompactado em 2026-08-29 (reconciliaÃ§Ã£o de engenharia de contexto, ver `docs/architecture/reviews/context-engineering-reconciliation/`) para parar de acumular narrativa sessÃ£o-a-sessÃ£o jÃ¡ duplicada nesses dois arquivos â€” a versÃ£o anterior tinha 1067 linhas, a maior parte delas o histÃ³rico completo de D-058 a D-083, palavra por palavra jÃ¡ presente em `decisions-log.md`.

## Branch / as-of

**NÃ£o confie nesta seÃ§Ã£o sem confirmar.** `git branch --show-current` deve ser `develop`; `git log --oneline -5`, `git status` e `git pull` antes de assumir qualquer coisa abaixo como pendente ou concluÃ­do â€” mÃºltiplas sessÃµes/agentes trabalham neste repo em paralelo.

**Estado em 2026-08-31 17:xx**: as 3 lacunas de produto (D-121/122/123) estÃ£o **100% completas â€” design â†’ implementaÃ§Ã£o â†’ deploy â†’ verificaÃ§Ã£o ao vivo**: D-124 (orquestrador W3-07, PR #122), D-125 (reatribuiÃ§Ã£o de responsabilidade, PR #123), D-126 (exportaÃ§Ã£o CSV, PR #124, PR #125 corrigiu um bug real de `depends_on` faltante que quebrou o primeiro CD) â€” todos com CD `success` confirmado, `exptrk-dev-export-handler` confirmado `Active`/`Successful` via `aws lambda get-function --profile claude-dev`. Multi-User B2B (15 waves) `DONE`. Wave B2B-14 (roteiro manual convite/aceite 2Âª conta) segue adiada pelo Marcelo, nÃ£o bloqueante. **2 bugs reais de CD encontrados e corrigidos nesta sessÃ£o** (PR #125: `depends_on` faltante; rerun do CD por race condition de lock com a prÃ³pria CI) â€” nenhum recorrente, apenas registrar como classe de achado real desta rodada de implementaÃ§Ã£o.

**AmpliaÃ§Ã£o de autoridade registrada nesta sessÃ£o (Marcelo, 2026-08-31, `docs/engineering/ai-governance.md` Â§1)**: para os itens da lista abaixo, resÃ­duos de decisÃ£o de produto (nÃ£o sÃ³ o mecanismo tÃ©cnico) tambÃ©m podem ser decididos por Claude+Codex via protocolo completo (pesquisa externa + rodada adversarial + nota cega â‰¥9,0) â€” revertÃ­vel por ele depois, nÃ£o dispensa o protocolo em si, NÃƒO se estende a execuÃ§Ã£o real/destrutiva.

**PrÃ³ximos itens autorizados a tratar autonomamente, NESTA ORDEM (Marcelo confirmou a ordem e a autoridade ampliada, 2026-08-31)**:
1. ~~EstratÃ©gia de quarentena antes de exclusÃ£o fÃ­sica de dado persistido + as 7 classes de retenÃ§Ã£o LGPD restantes~~ â€” **`APPROVED` (design), D-127, 2026-08-31**, protocolo completo 11 rodadas (Claude 9,4/Codex 9,3). Ver `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`. Mecanismo: novo estado `HELD_FOR_RECOVERY` (30 dias, cancelÃ¡vel) no grafo de `TenantLifecycleRecord`, reusa o Step Functions de W3-07/D-124. PriorizaÃ§Ã£o LGPD das 7 classes sem purga real dentro de tenant `ACTIVE`: `CORE_USER_DATA`â†’`DELIVERY_RECORD`â†’`SECURITY_AUDIT`â†’`QUOTA_TELEMETRY`â†’`ACCOUNT_ACTIVE`(nÃ£o-fechamento)â†’`TRANSIENT` remanescente, com `LEGAL_EVIDENCE` em lane bloqueada separada (trava jurÃ­dica). **Design-only, deliberado** â€” implementaÃ§Ã£o real fica para sessÃ£o(Ãµes) futura(s) dedicada(s), tamanho comparÃ¡vel a uma wave, nÃ£o a um dia.
2. `AppError.retryable` (comportamento de retry/DLQ do SQS) â€” `docs/engineering/decisions-log.md` E-011.
3. SupersessÃ£o de GTR-01 (`UserProfile.requesterDisplayName`â†’`Organization.displayName`) â€” `roadmap-evolution/17` Â§121 Q21.
4. Wave 1 â€” Design System reconciliation (revisar/adotar as 2 propostas do Marcelo em `docs/frontend/`).
5. **Tela de atividade/audit log para ADMIN/OWNER** (Marcelo, 2026-08-31) â€” ver visÃ£o do responsÃ¡vel de renovaÃ§Ã£o/criaÃ§Ã£o de vencimento, exportaÃ§Ã£o de dados, e demais aÃ§Ãµes relevantes de outros usuÃ¡rios da organizaÃ§Ã£o. Base jÃ¡ existe (`AuditAction`/taxonomia em `src/shared/observability/security-audit.ts`, eventos jÃ¡ emitidos por vÃ¡rias mutaÃ§Ãµes) â€” provavelmente Ã© majoritariamente expor dado jÃ¡ coletado, nÃ£o uma pipeline nova. Rodada Claudeâ†”Codex prÃ³pria vai decidir: feed leve vs. audit log completo/pesquisÃ¡vel, retenÃ§Ã£o, RBAC (provavelmente `ADMIN_ROLES`, seguir o precedente jÃ¡ usado para `item:export`), e a relaÃ§Ã£o com o item 1 (os mesmos eventos de auditoria jÃ¡ sÃ£o citados como mecanismo residual de observabilidade no D-122, e o item 1 acima decidiu `SECURITY_AUDIT` como prioridade 3 de purga LGPD).
6. **ImplementaÃ§Ã£o real de D-127** (item 1 acima) â€” nomeado explicitamente como trabalho futuro, nÃ£o urgente: novo Lambda de leitura para o ASL, extensÃ£o do sweeper de reconciliaÃ§Ã£o, migraÃ§Ã£o dos `ReminderOccurrence` `CANCELLED`-writers para `cancelOccurrenceUpdate()`, regra ESLint de enforcement, e os 7 workers de purga LGPD um de cada vez na ordem decidida.

**Item 7 (execuÃ§Ã£o destrutiva `reset-dev-data.ts --confirm`) explicitamente POSTERGADO pelo Marcelo â€” nÃ£o tratar, nÃ£o perguntar de novo atÃ© ele sinalizar.**

Cada um dos 4 itens acima segue o mesmo padrÃ£o desta sessÃ£o: rodada Claudeâ†”Codex completa (pesquisa externa quando aplicÃ¡vel, 3+ rodadas, nota cega, gate 9,0) â†’ se pequeno o bastante, implementaÃ§Ã£o direta na mesma sessÃ£o (D-124/125/126 pattern) â†’ `decisions-log.md` novo D-number (prÃ³ximo Ã© **D-127**, confirmar lendo o arquivo) â†’ atualizar este arquivo â†’ commit/push `develop` autÃ´nomo â†’ PRâ†’`main` se suite verde. **Cuidado com conflito**: todos tocam `decisions-log.md`/este arquivo â€” rodar SEQUENCIALMENTE, nunca em paralelo (liÃ§Ã£o aprendida nesta sessÃ£o com D-121, evitar delegaÃ§Ã£o recursiva de agentes â€” instruir explicitamente "nÃ£o spawnar sub-agente, fazer o trabalho vocÃª mesmo").

## Fase atual

`Consolidation + Pilot Readiness` concluÃ­do com recomendaÃ§Ã£o **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`). M0-M11 implementados e deployados; M7 (extraÃ§Ã£o/OCR) code-complete e **E2E PROVEN** em `dev`; Full BFF + Frontend Production Foundation `APPROVED` e implementados; planejamento de interface 8/9 etapas `APPROVED` (sÃ³ falta User Validation, em suspenso a pedido do Marcelo). W3-07 (exclusÃ£o fÃ­sica de tenant/DSR/LGPD) tem design `APPROVED WITH CONDITIONS` (D-066/D-067) e estÃ¡ em implementaÃ§Ã£o incremental por chunks (D-068 a D-104, Ãºltima emenda de privacidade B2B-9). M12 (billing) bloqueado por decisÃ£o de fornecedor (D-052). **Multi-User B2B (D-084 a D-108): design tÃ©cnico `APPROVED` via protocolo Claudeâ†”Codex, physical model formalizado (Wave B2B-1, D-086) e timing decidido diretamente pelo Marcelo â€” proceder agora, supersedendo o gatilho comercial que antes gated M13.** Waves B2B-0 a B2B-11 `DONE` â€” **B2B-5 (RequestContext Cutover) implementada (D-096): login nÃ£o cria mais tenant sozinho, `RequestContextResolver` resolve organizaÃ§Ã£o real via `Membership`, `POST /bff/organizations` fecha o loop de onboarding com cap transacional. B2B-7 (RBAC) implementada (D-097/D-098): `Membership.role=ADMIN` real na matriz de autorizaÃ§Ã£o, primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014. B2B-8 (Invitations/Team) implementada (D-099/D-100): `Invitation`/aceite/revogaÃ§Ã£o/gerÃªncia de membros (role-change/remove/leave) com last-owner protection transacional, segunda aplicaÃ§Ã£o de E-014, primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o â€” novo Lambda `memberships-handler`, 7 rotas API Gateway novas. B2B-6 (BFF Organization Context) implementada (D-101/D-102): `X-Organization-Id` (BFF-derived, nunca do browser) transportado atÃ© o `RequestContextResolver`, fecha o `InternalError` 500 que B2B-8 tornou explorÃ¡vel, `POST /bff/organization/select`/`GET /bff/organizations` novos, terceira aplicaÃ§Ã£o de E-014. B2B-9 (W3-07/Privacy Reconciliation) implementada (D-103/D-104): fecha o `InvitationTokenPointer` Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization (3Âª clÃ¡usula OR no scan/`PURGE_DELETE`), formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md` Â§4.1, quarta aplicaÃ§Ã£o de E-014. B2B-10 (Tenant-aware Frontend) implementada (D-105/D-106): corrige uma regressÃ£o real (frontend tratava todo usuÃ¡rio autenticado como deslogado desde B2B-5), isolamento de cache real entre tenants (`ActiveOrganizationProvider`, gate `switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta), switcher/members/settings novos, primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend. B2B-11 (Responsibility + Notifications) implementada (D-107/D-108): `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch.userId` migrados de validaÃ§Ã£o vestigial (`UserProfile`) para `Membership`+`GlobalUser` reais, quinta aplicaÃ§Ã£o de E-014 â€” NÃƒO inclui a supersessÃ£o de GTR-01 (`Organization.displayName`), ainda pendente. B2B-12 (Cutover de dev) implementada (D-110/D-111): `scripts/reset-dev-data.ts` novo (Fase A inventÃ¡rio+snapshot sempre, Fase B delete real sÃ³ `--confirm`, allowlist de tabela/conta, verificaÃ§Ã£o final fail-loud), `LEGACY_TENANT_ONLY` removido (backend+frontend+teste), sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`) â€” Fase A (leitura) jÃ¡ verificada de fato contra `dev` real; **execuÃ§Ã£o real destrutiva (`--confirm`/`--include-cognito`) permanece pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo, registrada como o Ãºnico item aberto dessa wave.** B2B-13 (E2E/Adversarial Security) implementada (D-112/D-113): auditoria completa contra as 25 perguntas de `roadmap-evolution/17` Â§121 (20/25 jÃ¡ cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assÃ­ncrona de notificaÃ§Ã£o â€” `notification.ts`/`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` jÃ¡ testado, 2 ganham teste novo de revogaÃ§Ã£o/isolamento de role, 1 Ã© auditoria de fixture IDs sem achado), sÃ©tima aplicaÃ§Ã£o de E-014. **Multi-User B2B â€” todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** B2B-14 (Operational Evidence) `EM ANDAMENTO` â€” 7 achados reais corrigidos (D-114 a D-120), teste de convite/aceite com segunda conta explicitamente adiado pelo Marcelo. B2B-15 (Documentation Reconciliation) `EM ANDAMENTO` em paralelo. 3 decisÃµes de produto novas (purga W3-07/orquestrador, exportaÃ§Ã£o de dados, reatribuiÃ§Ã£o de responsabilidade) fecharam design `APROVADO` via protocolo (D-121/D-122/D-123); a primeira (orquestrador de purga) jÃ¡ foi implementada em D-124, as outras duas seguem sÃ³ com design. Ver "O que estÃ¡ em andamento" abaixo. **Lembrete de calibraÃ§Ã£o (D-093, `AGENTS.md` Â§1)**: projeto sem usuÃ¡rio real nem produÃ§Ã£o â€” nÃ£o pesar "quebraria conta/sessÃ£o existente em `dev`" como risco bloqueador.

Ver `docs/architecture/README.md` (linha `Design maturity`/bloco de status no topo) para o resumo executivo mais completo â€” este arquivo nÃ£o duplica aquele bloco, sÃ³ aponta para ele.

## O que jÃ¡ estÃ¡ implementado (referÃªncia, nÃ£o repetir aqui)

- **Backend por mÃ³dulo/milestone**: `AGENTS.md` Â§7 (invariantes estÃ¡veis) + `ARCHITECTURE.md` (visÃ£o consolidada).
- **Frontend/planejamento de interface**: `docs/frontend/README.md` (Ã­ndice completo, blockers tÃ©cnicos BLOCKER-A/B/C todos resolvidos, GTR-01 com decisÃ£o de produto pendente W5-01 jÃ¡ fechada).
- **Engenharia/qualidade**: `docs/engineering/README.md` (padrÃµes, gates, backlog do programa de pilot readiness).
- **Infra**: Terraform (`infra/`, ADR-0009), CI/CD via GitHub Actions OIDC.
- **ReconciliaÃ§Ã£o de engenharia de contexto (2026-08-29)**: `DONE`, mergeada em `main` (PR #84) â€” root cleanup + `AGENTS.md`/este arquivo reconciliados + 2 guardrails novos em `check-doc-drift.ts`, revisÃ£o Claudeâ†”Codex 9,3/10. Registro completo: `docs/architecture/reviews/context-engineering-reconciliation/`.
- **E-011 (logging/tracing) â€” junÃ§Ã£o `correlationId`â†”X-Ray**: `E2E PROVEN` (2026-08-29) â€” smoke test real em `dev` via `aws --profile claude-dev` confirmou `xrayTraceId` no log idÃªntico ao trace real do X-Ray. Detalhe: `docs/architecture/correlationid-xray-trace-join.md`.
- **E-012 (2026-08-29) â€” Definition of Done por item de todo list**: `APPROVED` via protocolo Claudeâ†”Codex (3 rodadas, Claude 9,1/Codex 9,2). Regra de processo permanente, vigente a partir de agora em toda sessÃ£o: nenhum item de todo list que produza/altere cÃ³digo real Ã© `completed` sem passar pelo gate do seu nÃ­vel de risco â€” ver `docs/engineering/definition-of-done.md` (granularidade, gate por nÃ­vel, registro de evidÃªncia `DoD:`). **Emenda E-013 (2026-08-30)**: aplicaÃ§Ã£o de `test-engineering-standard.md` tornada operacional (nÃ£o sÃ³ referencial) â€” G-V3 (mutaÃ§Ã£o nomeada por escrito) exigido explicitamente por teste, nÃ£o sÃ³ "suÃ­te verde". Achado do Marcelo, corrigido retroativamente nos testes de B2B-2.
- **D-093 (2026-08-30) â€” sem usuÃ¡rio real nem produÃ§Ã£o, nÃ£o pesar esse risco**: `AGENTS.md` Â§1 ganhou parÃ¡grafo permanente â€” projeto em fase de construÃ§Ã£o, sem usuÃ¡rio real, sem produÃ§Ã£o (deploy sÃ³ em `dev`, dado sintÃ©tico resetÃ¡vel), sem prazo. Nunca tratar "quebraria conta/sessÃ£o existente em `dev`" como risco bloqueador por si sÃ³; nÃ£o dispensa rigor de engenharia/seguranÃ§a/protocolo Â§4. Achado do Marcelo, motivado por D-092 ter invocado essa categoria de risco indevidamente (a conclusÃ£o tÃ©cnica de D-092 continua vÃ¡lida por outras razÃµes).
- **D-094 (2026-08-30) â€” Wave B2B-4 (Onboarding) implementada, `DONE`**: `OnboardingStateResolver` (`src/modules/organization/application/onboarding-state.ts`) exatamente per escopo D-092 â€” classificador puro, hidrata Membership contra a partiÃ§Ã£o base em vez de confiar na projeÃ§Ã£o do GSI4 (physical model Â§6), 7 testes com G-V3 aplicado desde a escrita. Detalhe completo em `docs/architecture/multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-094.
- **D-095/D-096 (2026-08-30) â€” Wave B2B-5 (RequestContext Cutover), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-095) e implementado (D-096) no mesmo dia, com sinal do Marcelo entre as duas etapas. Login nÃ£o cria mais tenant sozinho (`bootstrapUser()` 2 itens); `RequestContextResolver` reescrito resolve a Organization real via `Membership`, com gate de lifecycle da prÃ³pria Organization (achado real, corrigido antes de fechar â€” faltava no design original) e assert nomeado para `Membership.role` nÃ£o suportado (`ADMIN`); `POST /bff/organizations` (novo) fecha o loop de onboarding com cap transacional (`GlobalUser.hasCreatedOrganization`, mesma `TransactWriteItems` da criaÃ§Ã£o, nunca check-then-act); sessÃ£o BFF com self-heal. Achado real corrigido: `UserProfile` **nÃ£o** foi removido como o escopo original prescrevia â€” ainda serve `profile-service.ts`/guest flows; provisionamento sÃ³ mudou de lugar (lazy, por Organization). SuÃ­te completa 1134/1134, zero regressÃ£o, `terraform test`/`build:lambdas` verdes. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b5-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-095/D-096.
- **E-014 (2026-08-30) â€” Pesquisa externa antes de decisÃµes Type 1**: `APPROVED` via protocolo Claudeâ†”Codex (3 rodadas, Claude 9,2/Codex 9,2). Regra de processo permanente: uma decisÃ£o nÃ­vel 5-6 que define um padrÃ£o que sistemas fora deste projeto jÃ¡ resolveram (RBAC, convite, sessÃ£o multi-tenant) exige declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO` na Rodada 1 e, se aplicÃ¡vel, um checklist de critÃ©rios pesados derivado da pesquisa que vira a rÃ©gua de nota da rodada (nunca julgamento livre depois) â€” ver `docs/engineering/research-protocol.md`. Pedido do Marcelo, mesmo padrÃ£o de E-012. **Aplicada 6 vezes: Wave B2B-7 (RBAC, D-097), Wave B2B-8 (Invitations/Team, D-099), Wave B2B-6 (BFF Organization Context, D-101), Wave B2B-9 (W3-07/Privacy Reconciliation, D-103), Wave B2B-10 (Tenant-aware Frontend, D-105, primeira aplicaÃ§Ã£o a uma decisÃ£o de frontend â€” query keys escopadas por tenant, padrÃ£o documentado pela prÃ³pria TanStack Query v5) e Wave B2B-11 (Responsibility + Notifications, D-107, GitHub/Linear "assignee deve ser membro real e ativo") â€” nas 6 a Rodada 1 do Codex contestou o prÃ³prio checklist ou achou bloqueante real, reconciliado nas rodadas seguintes.**
- **D-097/D-098 (2026-08-30) â€” Wave B2B-7 (RBAC), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-097, primeira aplicaÃ§Ã£o real de E-014) e implementado no mesmo dia (D-098). Pesquisa real (GitHub/Linear/Slack/Notion/NIST-ANSI-INCITS-359) nÃ£o convergiu para uma regra Ãºnica "ADMIN=OWNER" â€” a Rodada 1 do Codex contestou o prÃ³prio checklist de critÃ©rios de nota (nÃ£o sÃ³ o design), reconciliado na Rodada 2 (rÃ©gua 9,1/9,1) antes da nota do design voltar a contar. `Role` ganha `ADMIN`, paritÃ¡rio com `OWNER` nas 4 actions de deleÃ§Ã£o de recurso e no bypass de ownership de `authorize()`; `notification:configure` reclassificada de `ADMIN_ROLES` para `READ_ONLY_ROLES` (achado real, bug fix â€” bloquearia `MEMBER`/`VIEWER` reais de configurar a prÃ³pria preferÃªncia pessoal, contradizendo o prÃ³prio comentÃ¡rio do cÃ³digo citando essa action como precedente de self-service); `tenant:configure-document-request-delivery` isolada em novo tier `OWNER_ROLES` (config. externa/reputacional do tenant). Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b7-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-097/D-098.
- **D-099/D-100 (2026-08-30) â€” Wave B2B-8 (Invitations/Team), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-099, segunda aplicaÃ§Ã£o de E-014, declaraÃ§Ã£o `SIM PARCIAL`) e implementado no mesmo dia (D-100). Pesquisa real confirmou (nÃ£o redesenhou) o mecanismo `ownerCount > :one` jÃ¡ `APPROVED` em D-086 Â§8 (convergÃªncia 4/4 GitHub/Slack/Notion/Linear em last-owner protection) e informou a hierarquia de gerÃªncia de membros (ADMIN-tier-e-acima gerencia membros; sÃ³ OWNER promove/demove OWNER). A Rodada 1 do Codex achou 3 bugs de design reais (convite pendente com a mesma tier de privacidade que lista de membros; token de convite nunca consumido dentro da transaÃ§Ã£o de aceite; colisÃ£o de chave de rate-limit com `initial-invite-rate-limiter.ts`) â€” todos corrigidos nas Rodadas 2/3. `Invitation`+token pointer+dedup (D-086 Â§7); `AcceptInvitationService` (transaÃ§Ã£o de 6 itens); `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService` com builder Ãºnico de `ownerCount`; novo Lambda `memberships-handler` + 7 rotas API Gateway. Gap registrado, nÃ£o escondido: envio real de e-mail de convite via SES nÃ£o conectado nesta wave (mecanismo pronto, ativaÃ§Ã£o requer decisÃ£o de kill-switch). Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b8-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-099/D-100.
- **D-101/D-102 (2026-08-30) â€” Wave B2B-6 (BFF Organization Context), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-101, terceira aplicaÃ§Ã£o de E-014) e implementado no mesmo dia (D-102). Achado real que motivou a wave: `RequestContextResolver.resolveActiveMembership()` lanÃ§ava `InternalError` (500) para >1 `Membership` `ACTIVE` â€” inatingÃ­vel atÃ© B2B-8, agora explorÃ¡vel de verdade. Pesquisa (padrÃ£o header+revalidaÃ§Ã£o server-side, OWASP Multi Tenant Security) confirma a postura jÃ¡ em uso neste projeto (revalidaÃ§Ã£o via banco, nÃ£o cache de claims). A Rodada 1 do Codex contestou o checklist (faltava OWASP; faltava boundary BFF/browser; `select` sem CSRF; lista sem filtro de lifecycle; estado ambÃ­guo sem contrato tipado) â€” reconciliado nas Rodadas 2/3. `X-Organization-Id` injetado sÃ³ pelo BFF (nunca lido do browser, `ProxyService.forward()`); `resolveWorkingOrganization()`/`OrganizationUnavailableError`(403)/`OrganizationSelectionRequiredError`(409) substituem o `InternalError` antigo e um `AuthenticationError` prÃ©-existente inconsistente; `organizationIdHint` obrigatÃ³rio (nÃ£o opcional) threadeado por 56 call sites reais/13 arquivos; `POST /bff/organization/select`/`GET /bff/organizations` novos. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b6-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-101/D-102.
- **D-103/D-104 (2026-08-30) â€” Wave B2B-9 (W3-07/Privacy Reconciliation), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-103, quarta aplicaÃ§Ã£o de E-014) e implementado no mesmo dia (D-104). Achado real de cÃ³digo: `InvitationTokenPointer` (`organization/domain/invitation-token.ts`) declara `organizationId`, nÃ£o `tenantId` â€” o scan/`PURGE_DELETE` de purga (ampliados em D-082/B1 sÃ³ para o nome `tenantId`) nunca alcanÃ§avam esse pointer, deixando-o Ã³rfÃ£o para sempre apÃ³s exclusÃ£o de uma Organization. Pesquisa real (GitHub/Slack/Atlassian, convergÃªncia 3/3) fundamentou a nova seÃ§Ã£o `privacy-lgpd.md` Â§4.1 (User-level vs. Organization-level erasure â€” titular individual nunca cascateia para dado de negÃ³cio organization-owned; invariante de Ãºltimo OWNER documentada, sem guard de cÃ³digo, sem call site real hoje). A Rodada 1 do Codex achou 3 bloqueantes reais (fronteira User/Organization incompleta â€” faltava `DeviceSession`/`Session`/`IdentityMapping`/`UserProfile`; `MembershipInviteRateLimitRecord` ausente da retenÃ§Ã£o; plano de teste nÃ£o provava o adapter real) â€” todos corrigidos na Rodada 2, Rodada 3 foi trÃ©plica de polimento sem achado novo. Fix: 3Âª clÃ¡usula `OR organizationId = :tenantId` no scan (`tenant-purge-scan.ts`) e no `PURGE_DELETE` (`system-mutation.ts`). Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b9-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-103/D-104.
- **D-105/D-106 (2026-08-30) â€” Wave B2B-10 (Tenant-aware Frontend), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-105, primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend nesta sessÃ£o) e implementado no mesmo dia (D-106). Achado real #1: `frontend/src/auth/AuthContext.tsx`'s `probe()` exigia campos (`tenantId`/`userId`) que `GET /bff/session` nunca retorna desde B2B-5/D-095 â€” todo usuÃ¡rio autenticado era tratado como `SESSION_MISSING`, regressÃ£o real em `develop`/`main`, corrigida. Achado real #2: nenhum serviÃ§o de B2B-8 atualiza `Organization.displayName`/`timezone` â€” `UpdateOrganizationSettingsService` novo (`organization:update-settings`/`OWNER_ROLES`). A Rodada 1 do Codex achou a proposta original subestimando uma corrida real de isolamento de cache (`organizationId` nÃ£o Ã© uma variÃ¡vel que o `queryFn` real usa â€” o BFF injeta `X-Organization-Id` a partir da sessÃ£o server-side, nÃ£o do cliente); Rodada 2 fechou a corrida com um gate `switching`+`cancelQueries`, mas achou que `useActiveOrganization()` precisava ser Context Ãºnico (nÃ£o hook reimplementado por chamador); Rodada 3 exigiu `AbortSignal` propagado ponta-a-ponta para o `cancelQueries()` funcionar de verdade. `api/queryKeys.ts` (factory central), `auth/ActiveOrganizationContext.tsx` (`ActiveOrganizationProvider`), `components/OrganizationSwitcher.tsx`, `routes/{Members,Settings}.tsx` novos. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b10-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-105/D-106.
- **D-107/D-108 (2026-08-30) â€” Wave B2B-11 (Responsibility + Notifications), `DONE`**: escopo aprovado via protocolo Claudeâ†”Codex 3 rodadas (D-107, sexta aplicaÃ§Ã£o de E-014) e implementado no mesmo dia (D-108). 4 achados reais: `NotificationRecipientResolver` validava contra `UserProfile` (vestigial desde B2B-5); `resolveCandidateUserId()`'s fallback `?? tenantId` estruturalmente quebrado pÃ³s-B2B; `ItemWatchService.addWatcher`/`assigneeUserId` sem validaÃ§Ã£o nenhuma contra `Membership` real. A Rodada 1 do Codex achou 3 bloqueantes (mudanÃ§a de assinatura do fallback quebraria o caller real + um teste existente; migraÃ§Ã£o `UserProfileâ†’Membership` colapsava indevidamente a distinÃ§Ã£o `RECIPIENT_NOT_FOUND`/`RECIPIENT_NOT_ELIGIBLE`; e-mail de entrega ainda lia `UserProfile`, quebrando para um membro recÃ©m-aceito que nunca logou naquela Organization) â€” corrigidos na Rodada 2 (fallback vira `""` mantendo tipo `string`; distinÃ§Ã£o de estados preservada; e-mail migrado para `GlobalUser`). Rodada 2â†’3: elegibilidade precisa exigir `Membership ACTIVE` **e** `GlobalUser.identityStatus ACTIVE` (mesma regra dupla de `resolve-request-context.ts`). Port novo `MemberEligibilityChecker` (`expiration/ports/`). **Nota de escopo real**: nÃ£o inclui a supersessÃ£o de GTR-01 (`UserProfile.requesterDisplayName`â†’`Organization.displayName`), prevista em B2B-5 mas nunca debatida nas 3 rodadas â€” continua pendente. Detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b11-scope.md`, `multi-user-b2b-wave-tracker.md` e `decisions-log.md` D-107/D-108.

## O que estÃ¡ em andamento

1. **Multi-User B2B â€” autorizado a prosseguir, Waves B2B-0 a B2B-13 `DONE` (D-084 a D-113).** Design tÃ©cnico `APPROVED` (protocolo completo em `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` Â§125) e timing decidido diretamente pelo Marcelo. Estado detalhado por subitem (evidÃªncia `DoD:`, judgment calls de sequenciamento documentados) vive em `docs/architecture/multi-user-b2b-wave-tracker.md` â€” nÃ£o duplicado aqui. Resumo: **B2B-0** inventÃ¡rio read-only; **B2B-1** physical model formalizado via protocolo Claudeâ†”Codex 5 rodadas (D-086, `docs/architecture/multi-user-b2b-physical-model.md`); **B2B-2** fundaÃ§Ã£o de identidade global aditiva (D-087/D-088); **B2B-3** `Organization`/`Membership`/`CreateOrganizationService` (D-089/D-090/D-091); **B2B-4** `OnboardingStateResolver` (D-092/D-094); **B2B-5** `RequestContext Cutover` (D-095/D-096) â€” login nÃ£o cria mais tenant sozinho, `RequestContextResolver` resolve a Organization real, `POST /bff/organizations` fecha o loop de onboarding; **B2B-7** `RBAC` (D-097/D-098) â€” `Membership.role=ADMIN` real na matriz de autorizaÃ§Ã£o; **B2B-8** `Invitations/Team` (D-099/D-100) â€” `Invitation`/aceite/revogaÃ§Ã£o/gerÃªncia de membros com last-owner protection, primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o, `ADMIN` finalmente alcanÃ§Ã¡vel em produÃ§Ã£o via convite; **B2B-6** `BFF Organization Context` (D-101/D-102) â€” `X-Organization-Id` transportado do BFF ao recurso, multi-org selecionÃ¡vel de verdade, fecha o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; **B2B-9** `W3-07/Privacy Reconciliation` (D-103/D-104) â€” fecha o `InvitationTokenPointer` Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization, formaliza User-level vs. Organization-level erasure em `privacy-lgpd.md` Â§4.1; **B2B-10** `Tenant-aware Frontend` (D-105/D-106) â€” corrige a regressÃ£o real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; **B2B-11** `Responsibility + Notifications` (D-107/D-108) â€” `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch.userId` migrados para `Membership`+`GlobalUser` reais (NÃƒO inclui a supersessÃ£o de GTR-01, ainda pendente); **B2B-12** `Cutover de dev` (D-110/D-111) â€” `scripts/reset-dev-data.ts` (Fase A inventÃ¡rio+snapshot sempre, Fase B delete real sÃ³ `--confirm`, allowlist de tabela/conta, verificaÃ§Ã£o final fail-loud), `LEGACY_TENANT_ONLY` removido (backend+frontend+teste), Fase A (leitura) jÃ¡ verificada de fato contra `dev` real; execuÃ§Ã£o real destrutiva permanece pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo; **B2B-13** `E2E/Adversarial Security` (D-112/D-113) â€” auditoria contra as 25 perguntas de Â§121 (20/25 jÃ¡ cobertas por waves anteriores, 2 fecham com fix real de TOCTOU em entrega assÃ­ncrona de notificaÃ§Ã£o, 2 ganham teste novo de revogaÃ§Ã£o/isolamento de role, 1 Ã© auditoria de fixture IDs sem achado).

   **Multi-User B2B â€” todas as 14 waves originais (B2B-0 a B2B-13) `DONE`. B2B-14 (Operational
   Evidence) `EM ANDAMENTO`, 7 achados reais severos jÃ¡ corrigidos (D-114 a D-120), todos sÃ³
   descobertos por exercitar o fluxo de verdade contra `dev` pela primeira vez** â€” exatamente o
   job desta wave (mesmo espÃ­rito de E-011/M7's E2E PROVEN): (1) `app_origin` placeholder nunca
   trocado, OAuth nunca funcionou (D-114); (2) `bff-handler` crashava em TODA invocaÃ§Ã£o desde
   B2B-8, `GUEST_TOKEN_PEPPER` nunca wireado (D-115); (3) `gsi4_read_policy_json` nunca anexado
   aos 10 Lambdas reais que o consomem (D-116); (4) `GET /bff/organizations`/`POST
   /bff/organization/select` nunca tiveram rota real no API Gateway (D-117); (5) nenhuma tela de
   "criar organizaÃ§Ã£o" existia no frontend, todo usuÃ¡rio sem Organization ficava preso em
   "Carregandoâ€¦" para sempre (D-118, feature nova `Onboarding.tsx`+`OnboardingGate.tsx`); (6)
   `ExpressionAttributeNames: {}` explÃ­cito quebrava `TransactWriteItems` real com
   `ValidationException` em 3 arquivos, o mais severo sendo o guard de Ãºltima-proteÃ§Ã£o-de-OWNER
   (D-119); (7) aceitar convite nunca teve tela nem rota real no API Gateway (mesma classe do
   D-117) e o e-mail real de convite nunca foi ativado â€” SES sandbox sem NENHUM endereÃ§o
   verificado no ambiente inteiro (D-120, PR #121, CD `success` confirmado â€” 2 endereÃ§os reais
   verificados com o Marcelo, `LeaveOrganizationSection` novo, `handleListMembers` parou de
   vazar o item bruto do DynamoDB). **Testado ao vivo** com o cookie de sessÃ£o real do Marcelo:
   save de Settings, `GET /bff/organizations`, `GET /bff/api/organizations/members` todos
   confirmados OK; `POST /bff/organizations` corretamente bloqueado por `CONFLICT` (cap de uma
   organizaÃ§Ã£o por `GlobalUser`, confirma que troca de organizaÃ§Ã£o sÃ³ Ã© testÃ¡vel depois do
   convite/aceite fechar o ciclo). **Pendente para fechar B2B-14**: teste de convite/aceite com
   uma segunda conta e o resto do roteiro manual (trocar role, trocar de organizaÃ§Ã£o, revogar
   membership, excluir organizaÃ§Ã£o) â€” **Marcelo confirmou explicitamente que isso fica para uma
   sessÃ£o futura, nÃ£o bloqueia nada agora**. B2B-15 (Documentation Reconciliation, checklist
   `AGENTS.md` Â§6) jÃ¡ comeÃ§ou em paralelo (nÃ£o depende de B2B-14 fechar) â€” ver item da tabela de
   gates abaixo para o que jÃ¡ foi feito/falta.

   **3 lacunas reais de produto, fora do escopo original de B2B-0..15, levantadas pelo Marcelo em
   2026-08-30** (detalhe completo nas memÃ³rias de sessÃ£o â€” ver rodapÃ© deste arquivo): (a) o
   pipeline de purga de tenant (W3-07) estÃ¡ implementado e testado, mas nada o aciona
   automaticamente quando uma conta/organizaÃ§Ã£o era fechada â€” **`APROVADO` em D-121,
   `IMPLEMENTADO` em D-124, mergeado em `main` (PR #122) e CD `success` em 2026-08-31 â€” state
   machine `exptrk-dev-tenant-purge` e sweeper `exptrk-dev-tenant-purge-sweeper` (`ENABLED`)
   confirmados ao vivo contra `dev` via `aws --profile claude-dev`. Totalmente resolvido; falta sÃ³
   uma purga real de ponta a ponta para virar `E2E PROVEN`, sem urgÃªncia**; (b) exportaÃ§Ã£o de dados (CSV/planilha)
   nunca foi implementada, sÃ³ importaÃ§Ã£o (M10) â€” lacuna real dado que LGPD exige portabilidade
   como direito do titular, escopo ainda nÃ£o debatido; (c) quando um `Membership` Ã© removido,
   `ExpirationItem.assigneeUserId`/`ItemWatch` nÃ£o sÃ£o reatribuÃ­dos nem limpos â€” o campo fica
   Ã³rfÃ£o indefinidamente e a notificaÃ§Ã£o sÃ³ para de disparar silenciosamente, sem fallback nem
   fluxo de reatribuiÃ§Ã£o â€” escopo ainda nÃ£o debatido.
2. **W3-07 â€” purge pipeline durÃ¡vel + orquestrador**: pipeline implementado/revisado (D-081/D-082/D-083, Codex 9,1/10) e emendado para as entidades B2B (B2B-9/D-104). **Orquestrador `IMPLEMENTADO` (D-124, 2026-08-31)** â€” exatamente o mecanismo `APROVADO` em D-121, implementaÃ§Ã£o direta sem rodada nova: mÃ³dulo `infra/modules/tenant-purge-workflow/` + `infra/state-machines/tenant-purge.asl.json` (Wait 1800s â†’ transiÃ§Ãµes â†’ `RunPurge` â†’ `Choice` com bound `PURGE_RETRY_LIMIT=20` â†’ `VERIFIED`/`DELETED`, ramos `MarkBlocked*` â†’ `Fail`), 3 Lambdas novos (transiÃ§Ã£o Ãºnica parametrizada por `from`/`to`, wrapper de `purgeTenant()` devolvendo o envelope compacto, sweeper), 2 alarmes `AWS/States` novos (`ExecutionsFailed`+`ExecutionsTimedOut`), EventBridge Scheduler `rate(1 day)`, `CloseOrganizationService`+action `organization:close` (`OWNER_ROLES`)+rota `POST /organizations/close`+UI type-to-confirm, composition root real fechando o KNOWN LIMIT de prefixoâ†”bucket de `purge-tenant.ts` (tabela FECHADA bucketâ†’raiz). `transitionTenantLifecycle()`/`purgeTenant()` deixam de ser cÃ³digo sem chamador. **Mergeado em `main` (PR #122, 2026-08-31) e CD `success`** â€” confirmado ao vivo contra `dev` (`aws stepfunctions list-state-machines`/`aws scheduler list-schedules` via `--profile claude-dev`): `exptrk-dev-tenant-purge` existe, `exptrk-dev-tenant-purge-sweeper` estÃ¡ `ENABLED` e rodando `rate(1 day)`. **Downstream ainda pendente**: nenhuma purga real de tenant executou ainda de ponta a ponta (nÃ£o hÃ¡ tenant genuinamente `DELETING` em `dev` hoje) â€” fica para quando `CloseOrganizationService` for exercitado de verdade, mesmo padrÃ£o de "cÃ³digo deployado, sem E2E PROVEN ainda" que outras waves jÃ¡ registraram.
3. **W3-07 â€” fencing dos writers de negÃ³cio** (`TenantBusinessMutation`): a maioria dos writers reais jÃ¡ fenced (chunks D-068 a D-080, ver `decisions-log.md`). Gap residual documentado, nÃ£o explorÃ¡vel hoje: entradas sem PK `TENANT#`-prefixed E sem `tenantId` declarado (`LoginAttempt`/`GuestRateLimitRecord`) passam sem verificaÃ§Ã£o â€” nenhum call site real produz isso.

## Gates / bloqueios abertos

| Item | Precisa de | Onde estÃ¡ o detalhe |
|---|---|---|
| Wave B2B-14 â€” fechar o roteiro manual (convite/aceite com 2Âª conta, troca de role, switch de organizaÃ§Ã£o, revogaÃ§Ã£o, exclusÃ£o de organizaÃ§Ã£o) | **Adiado pelo prÃ³prio Marcelo para uma sessÃ£o futura** â€” nÃ£o bloqueia nada, nÃ£o perguntar de novo atÃ© ele sinalizar | `decisions-log.md` D-120, `multi-user-b2b-wave-tracker.md` linha B2B-14 |
| ExecuÃ§Ã£o real destrutiva de `scripts/reset-dev-data.ts` (`--confirm`/`--include-cognito` contra `dev`) | ConfirmaÃ§Ã£o explÃ­cita do Marcelo antes de rodar â€” design/implementaÃ§Ã£o/verificaÃ§Ã£o de leitura jÃ¡ `DONE` (D-110/D-111) | `docs/architecture/multi-user-b2b-wave-b2b12-scope.md`, `decisions-log.md` D-111 |
| SupersessÃ£o de GTR-01 (`UserProfile.requesterDisplayName`â†’`Organization.displayName`) â€” prevista em B2B-5, nunca debatida | Rodada de design/scoping prÃ³pria via protocolo Claudeâ†”Codex (fora do escopo real de B2B-11) | `docs/architecture/multi-user-b2b-wave-tracker.md` (achados/pendÃªncias), `roadmap-evolution/17` Â§121 Q21 |
| `AppError.retryable` â€” deveria decidir comportamento real de SQS retry/DLQ? | DecisÃ£o de produto do Marcelo | `docs/engineering/decisions-log.md` E-011 |
| 7 de 9 classes de retenÃ§Ã£o LGPD sem purga fÃ­sica real dentro de tenant `ACTIVE` (`privacy-lgpd.md` Â§4) | **`APPROVED` (design + priorizaÃ§Ã£o), D-127, 2026-08-31** â€” implementaÃ§Ã£o real fica para sessÃ£o futura, uma classe por vez, ordem jÃ¡ decidida | `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md` |
| User Validation (planejamento de interface) | Sinal explÃ­cito do Marcelo para retomar | `docs/frontend/README.md` |
| Wave 1 (Design System reconciliation) â€” **dependÃªncia resolvida (2026-08-30)**: Marcelo trouxe `docs/frontend/design-system-v1-proposal.md` + `docs/frontend/frontend-engineering-quality-standard-v1-proposal.md` (movidos da raiz para `docs/frontend/`) | Revisar/adotar as 2 propostas via protocolo Claudeâ†”Codex antes de rodar a Wave 1 de verdade â€” nenhuma foi lida em profundidade ainda | `docs/frontend/README.md`, `docs/engineering/pilot-readiness-program.md` Wave 1 |
| **EstratÃ©gia comum de quarentena antes de exclusÃ£o fÃ­sica de dado persistido** | **`APPROVED` (design), D-127, 2026-08-31** â€” novo estado `HELD_FOR_RECOVERY` (30 dias, cancelÃ¡vel) inserido entre `ACTIVE` e `DELETING` no grafo do W3-07; cross-cutting por design, reusa o Step Functions de D-124 sem novo serviÃ§o AWS. ImplementaÃ§Ã£o real (Lambda de leitura, sweeper estendido, migraÃ§Ã£o de writers, regra ESLint) fica para sessÃ£o futura dedicada | `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`, `decisions-log.md` D-127 |

## DecisÃµes deliberadamente adiadas (jÃ¡ decididas como "nÃ£o agora", nÃ£o esquecidas)

- M12 (Billing) â€” bloqueado por escolha de fornecedor de pagamento (D-052).
- OpÃ§Ã£o B (atributo de span OpenTelemetry para correlationIdâ†”trace) â€” candidata futura, 3 prÃ©-requisitos nomeados, nÃ£o perseguida agora (`correlationid-xray-trace-join.md` Â§2).
- Document Lifecycle Management â€” mesma classe, arquivado como informativo (`docs/architecture/roadmap-evolution/16-document-lifecycle-strategic-analysis.md`).
- BFF/frontend quality standard (rubrica mais ampla que `interface-quality-standard.md`) â€” proposta nÃ£o adotada (`docs/frontend/bff-frontend-quality-standard-proposal.md`).
- Itens explicitamente fora de escopo do prÃ³prio Multi-User B2B (`roadmap-evolution/17` Â§98): custom roles, ABAC, SAML/SCIM, seat billing, platform admin/impersonation, department hierarchy.
- **Synthetic Persona Evaluation Framework** (`docs/architecture/roadmap-evolution/18-20`, trazido pelo Marcelo 2026-08-31) â€” **decisÃ£o explÃ­cita dele (2026-08-31): nÃ£o Ã© prioridade agora, mas precisa estar pronto quando chegar a hora de testes mais robustos antes de testes com humanos de verdade.** Gatilho de retomada: quando a sessÃ£o decidir avanÃ§ar para essa fase de testes prÃ©-User-Validation (nÃ£o antes â€” `User Validation` em si tambÃ©m estÃ¡ em suspenso, ver linha acima). Nessa hora, retomar nesta ordem: (1) confirmar com o Marcelo se ainda Ã© a prioridade certa naquele momento; (2) fechar primeiro a lacuna mais barata que o doc 19 jÃ¡ nomeia â€” Playwright hoje roda majoritariamente contra BFF mockado (`page.route()`), nÃ£o contra o sistema real; (3) submeter o doc 20 (trial identity, tecnicamente sÃ³lido nesta leitura â€” metadata nunca em entidade de domÃ­nio, propagaÃ§Ã£o via outbox `metadata`/nunca `payload`, gate de ambiente, fail-closed) Ã  Rodada 1 do protocolo Claudeâ†”Codex antes de qualquer cÃ³digo.

## PrÃ³xima aÃ§Ã£o, em ordem de valor esperado

1. **Confirmar que o CD de D-120 (PR #121, `main`) concluiu com sucesso** antes de assumir Wave B2B-14 aplicada em `dev` â€” se ainda nÃ£o confirmado, checar `gh run list --branch main --workflow "Deploy (CD)" --limit 3` primeiro (jÃ¡ confirmado `success` ao final da sessÃ£o que fechou D-120, mas confirme de novo, mÃºltiplas sessÃµes trabalham neste repo).
2. **Wave B2B-14 permanece aberta sÃ³ pelo roteiro manual de convite/aceite/2Âª conta â€” Marcelo adiou para uma sessÃ£o futura, explicitamente.** NÃ£o perguntar de novo, nÃ£o tratar como bloqueante; se ele trouxer o assunto, o passo a passo jÃ¡ estÃ¡ registrado em `decisions-log.md` D-120.
3. **Wave B2B-15 (Documentation Reconciliation) `DONE` (2026-08-31)** â€” checklist completo de `AGENTS.md` Â§6 verificado (`docs/frontend/README.md`/`docs/engineering/README.md` sem drift, ADRs consistentes com `decisions-log.md`, `npm run check-docs` limpo). **Multi-User B2B (todas as 15 waves originais, B2B-0 a B2B-15) estÃ¡ agora `DONE`** â€” sÃ³ resta o roteiro manual adiado (item 2 acima) e as 3 lacunas de produto com design aprovado (itens 5-7 abaixo).
4. **Das 3 lacunas de produto levantadas em 2026-08-30, as 3 jÃ¡ foram implementadas**: orquestrador do purge (D-121â†’D-124), reatribuiÃ§Ã£o de responsabilidade (D-122â†’D-125), exportaÃ§Ã£o de dados CSV (D-123â†’D-126). Nenhuma decisÃ£o pendente restante nessa frente â€” decisÃ£o de produto residual do Marcelo sobre D-126: grÃ¡tis/imediato vs. limitado por plano de export (default v1 jÃ¡ ativo: grÃ¡tis, sem gate, M12 bloqueado por D-052).
5. **Quarentena/recuperaÃ§Ã£o + priorizaÃ§Ã£o LGPD (item 1 da lista de "PrÃ³ximos itens" acima) â€” `APPROVED` (design), D-127, 2026-08-31.** ImplementaÃ§Ã£o real fica para sessÃ£o futura dedicada (item 6 da mesma lista), nÃ£o urgente.
6. `AppError.retryable` â€” decisÃ£o de produto pendente, nÃ£o implementar sem sinal explÃ­cito do Marcelo.
7. ExecuÃ§Ã£o real destrutiva de `scripts/reset-dev-data.ts` (`--confirm`) â€” aguarda confirmaÃ§Ã£o explÃ­cita do Marcelo, nÃ£o bloqueia nenhuma wave seguinte.

## Leitura obrigatÃ³ria antes da prÃ³xima aÃ§Ã£o

`AGENTS.md` Â§2 (inÃ­cio de sessÃ£o) â†’ `docs/architecture/README.md` (mapa vigente) â†’ a linha da tabela "Gates / bloqueios abertos" relevante Ã  tarefa escolhida, e sÃ³ entÃ£o o documento/pasta de review que ela referencia.

## Status de evidÃªncia (nÃ£o presumir E2E sem checar)

| Item | Status |
|---|---|
| M7 extraÃ§Ã£o/OCR | `E2E PROVEN` (verificaÃ§Ã£o real 2026-08-27 contra `dev`) |
| Full BFF + Frontend Production Foundation | `APPROVED` + implementado, `E2E PROVEN` |
| W3-06 (`USER_DOCUMENT` purge) | `IMPLEMENTED`/`E2E PROVEN` (`terraform plan`/`test` reais contra `dev`) |
| W3-07 purge pipeline (D-081-083, emendado B2B-9/D-104) | `IMPLEMENTED`/`UNIT TESTED` â€” entidades B2B incluÃ­das, sem orquestrador wireado, sem teste de integraÃ§Ã£o AWS real |
| E-011 correlationIdâ†”X-Ray | `E2E PROVEN` (smoke test real 2026-08-29 contra `dev`) |
| Visual Language + Design System | `APPROVED ... PROVISIONAL PENDING USER VALIDATION` |
| B2B-10 Tenant-aware Frontend (D-105/D-106) | `IMPLEMENTED`/`UNIT+COMPONENT+E2E TESTED` (139/139 vitest, 24/24 Playwright, mutaÃ§Ã£o verificada em 3 pontos) â€” os 5 specs E2E mockavam `GET /bff/session` com a forma antiga (`tenantId`/`userId`), achado sÃ³ pelo CI apÃ³s o push inicial, corrigido; sem E2E dedicado a multi-organizaÃ§Ã£o/switcher ainda (fica para B2B-13) |
| B2B-11 Responsibility + Notifications (D-107/D-108) | `IMPLEMENTED`/`UNIT TESTED` (1210/1210 backend, mutaÃ§Ã£o verificada em 2 pontos) â€” sem verificaÃ§Ã£o E2E real contra `dev` (fica para B2B-14, Operational Evidence) |
| B2B-12 Cutover de dev (D-110/D-111) | `IMPLEMENTED`/`UNIT TESTED` (1232/1232 backend + 139/139 vitest + 24/24 Playwright frontend, mutaÃ§Ã£o verificada em 3 pontos) â€” Fase A (read-only) do script **`E2E PROVEN`** (rodada de fato contra `dev` real via `aws --profile claude-dev`, manifest gravado com sucesso); Fase B (`--confirm` real, destrutiva) nunca executada â€” aguarda confirmaÃ§Ã£o explÃ­cita do Marcelo |
| B2B-13 E2E/Adversarial Security (D-112/D-113) | `IMPLEMENTED`/`UNIT TESTED` (1235/1235 backend, mutaÃ§Ã£o verificada em 4 pontos) â€” auditoria contra as 25 perguntas de Â§121 completa (matriz Qâ†’arquivo:linha em `multi-user-b2b-wave-b2b13-scope.md`); sem verificaÃ§Ã£o E2E real contra `dev` (fica para B2B-14, Operational Evidence) |
| B2B-14 Operational Evidence (D-114 a D-120) | `EM ANDAMENTO` â€” 7 achados reais corrigidos e mergeados (D-120 Ã© o mais recente, PR #121, CD `success` confirmado); login OAuth, onboarding, GSI4/rotas de organizaÃ§Ã£o, save de Settings, listagem de organizaÃ§Ãµes/membros â€” todos exercitados de verdade em `dev` (Settings/organizaÃ§Ãµes/membros via cookie de sessÃ£o real do Marcelo). Roteiro manual de convite/aceite com 2Âª conta/troca de role/switch/revogaÃ§Ã£o/exclusÃ£o de organizaÃ§Ã£o **adiado pelo Marcelo para uma sessÃ£o futura**, nÃ£o bloqueante |
| W3-07 orquestrador de purga (D-121 design, D-124 implementaÃ§Ã£o) | `IMPLEMENTED`/`DEPLOYED` (1267/1267 backend, 156/156 vitest frontend, `terraform test` 19/19 raiz + 9/9 mÃ³dulo novo + 1/1 api-gateway + 7/7 bff-api-gateway; mutaÃ§Ã£o verificada e revertida em 3 pontos; mergeado em `main` PR #122, CD `success` 2026-08-31, state machine + sweeper `ENABLED` confirmados ao vivo contra `dev`) â€” **ainda nÃ£o `E2E PROVEN`**: nenhuma purga real de tenant executou de ponta a ponta ainda |
| ReatribuiÃ§Ã£o de responsabilidade (D-122 design, D-125 implementaÃ§Ã£o) | `IMPLEMENTED`/`UNIT TESTED` (1249/1249 backend, 156/156 vitest frontend, mutaÃ§Ã£o verificada e revertida em 3 pontos) â€” precondiÃ§Ã£o best-effort real em `RemoveMembershipService`/`LeaveOrganizationService`, porta `AssignedActiveItemsLookup` paginada a exaustÃ£o via GSI1; sem verificaÃ§Ã£o E2E real contra `dev` ainda |
| ExportaÃ§Ã£o de dados CSV (D-123 design, D-126 implementaÃ§Ã£o) | `IMPLEMENTED`/`UNIT TESTED` (1277/1277 backend testes coletados, 1275 passando â€” 2 falhas prÃ©-existentes nÃ£o relacionadas, ver D-125/D-126; mutaÃ§Ã£o verificada em 4 pontos) â€” `item:export`/`ADMIN_ROLES`, `ExpirationService.exportItems()` com orÃ§amento decrescente de 2.000 itens entre 3 queries GSI1, guard de 4 MB em `buildExportCsv()`, `csv-export-writer.ts` (RFC4180+mitigaÃ§Ã£o de fÃ³rmula), Lambda dedicada `export-handler`/rota `GET /items/export` real (`terraform test` limpo); sem verificaÃ§Ã£o E2E real contra `dev` ainda |

## Links para histÃ³rico (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)

- `docs/architecture/session-log.md` â€” linha do tempo compacta, uma entrada por sessÃ£o.
- `docs/architecture/decisions-log.md` / `docs/engineering/decisions-log.md` â€” toda decisÃ£o com nota Claude/Codex e status (numeraÃ§Ã£o D-0xx / E-0xx, nÃ£o sequencial na ordem das linhas).
- `docs/architecture/reviews/` â€” artefatos de cada rodada Claudeâ†”Codex por tema.
- `docs/project/handoffs/` â€” prompts de handoff de sessÃµes anteriores, superseded por este arquivo, preservados como evidÃªncia.
- `docs/frontend/` â€” os 8 documentos de planejamento de interface + `interface-quality-standard.md` + `prototype/`.

# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-29 (reconciliaÃ§Ã£o de engenharia de contexto â€” este bloco reescrito para corrigir drift real: dizia "auto-CONFIRMED nÃ£o propaga dueDate, aguardando decisÃ£o do Marcelo" e "8 de 9 classes de retenÃ§Ã£o sem purga" quando D-058/D-061 jÃ¡ tinham decidido e implementado ambos em sessÃµes anteriores). Consolidation + Pilot Readiness Program concluÃ­do, recomendaÃ§Ã£o **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`, backlog item-a-item em `pilot-readiness-program.md`): Wave 3 (tenant isolation/LGPD) `DONE` de ponta a ponta, zero vulnerabilidade cross-tenant real; `USER_DOCUMENT` e `EXTRACTION_TRANSIENT` tÃªm purga fÃ­sica real (D-061), as outras 7 de 9 classes de retenÃ§Ã£o LGPD nÃ£o tÃªm, decisÃ£o de escopo pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership; Wave 5 fechou GTR-01. M6/M7/M9/M10/M11 deployados em `main`/`dev`, M7 **E2E PROVEN** (verificaÃ§Ã£o real 2026-08-27, achados registrados em `decisions-log.md` D-057/D-058); M12 bloqueado por decisÃ£o de fornecedor (D-052). Full BFF + Frontend Production Foundation (D-053/D-054) **implementado e `APPROVED`**; planejamento de interface 8/9 etapas `APPROVED`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); BLOCKER-A/B/C **todos resolvidos**. W3-07 (exclusÃ£o fÃ­sica de tenant/DSR) tem design **`APPROVED WITH CONDITIONS`** (D-066/D-067, condiÃ§Ã£o de polÃ­tica SES jÃ¡ decidida) e implementaÃ§Ã£o incremental em andamento (fencing de writers D-068 a D-080, purge pipeline D-081 a D-083 com nota Codex 9,1/10, emenda de privacidade B2B-9/D-103/D-104) â€” orquestrador real (Step Functions vs. Lambda+EventBridge) ainda nÃ£o decidido, ortogonal ao escopo de retenÃ§Ã£o jÃ¡ fechado. **Multi-User B2B (D-084 a D-108, 2026-08-30)**: design tÃ©cnico `APPROVED` via protocolo (Claude 9,2/Codex 9,2) e timing decidido diretamente pelo Marcelo (proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05`) â€” maior iniciativa de arquitetura autorizada no momento; **Waves B2B-0 a B2B-11 `DONE`** (inventÃ¡rio, physical model D-086, identidade global D-087/D-088, Organization+Membership+CreateOrganizationService D-089/D-090/D-091, `OnboardingStateResolver` puro D-092/D-094, `RequestContext Cutover` D-095/D-096 â€” login nÃ£o cria mais tenant sozinho, `POST /bff/organizations` fecha o loop de onboarding, cap transacional; `RBAC` D-097/D-098 â€” `Membership.role=ADMIN` real na matriz de autorizaÃ§Ã£o, primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014; `Invitations/Team` D-099/D-100 â€” `Invitation`/aceite/revogaÃ§Ã£o/gerÃªncia de membros com last-owner protection, segunda aplicaÃ§Ã£o de E-014, primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o; `BFF Organization Context` D-101/D-102 â€” `X-Organization-Id` transportado do BFF ao recurso, terceira aplicaÃ§Ã£o de E-014, fecha o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; `W3-07/Privacy Reconciliation` D-103/D-104 â€” quarta aplicaÃ§Ã£o de E-014, fecha o `InvitationTokenPointer` Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization, formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md`; `Tenant-aware Frontend` D-105/D-106 â€” primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend, corrige a regressÃ£o real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; `Responsibility + Notifications` D-107/D-108 â€” quinta aplicaÃ§Ã£o de E-014, migra `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch` de validaÃ§Ã£o vestigial para `Membership`+`GlobalUser` reais; NÃƒO inclui a supersessÃ£o de GTR-01, ver `multi-user-b2b-wave-tracker.md`; `Cutover de dev` D-110/D-111 â€” sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`), `scripts/reset-dev-data.ts` (reset/reseed, nÃ£o migraÃ§Ã£o, dado 100% sintÃ©tico confirmado por inventÃ¡rio real via `aws --profile claude-dev`) + remoÃ§Ã£o de `LEGACY_TENANT_ONLY`, Fase A verificada de fato contra `dev`; execuÃ§Ã£o real destrutiva (`--confirm`) pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo; `E2E/Adversarial Security` D-112/D-113 â€” sÃ©tima aplicaÃ§Ã£o de E-014, auditoria contra as 25 perguntas de `roadmap-evolution/17` Â§121 (20/25 jÃ¡ cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assÃ­ncrona de notificaÃ§Ã£o â€” `notification.ts`+`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` jÃ¡ testado, 2 ganham teste novo de revogaÃ§Ã£o/isolamento de role, 1 Ã© auditoria de fixture IDs sem achado). **Multi-User B2B â€” todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** **B2B-14 (Operational Evidence) `EM ANDAMENTO`, 2026-08-30**: 7 achados reais severos, cada um encontrado sÃ³ por exercitar o fluxo de verdade contra `dev` pela primeira vez â€” `app_origin` placeholder nunca trocado (D-114), `bff-handler` crashava em toda invocaÃ§Ã£o (D-115), GSI4 nunca autorizado aos 10 Lambdas reais que o consomem (D-116), 2 rotas do BFF nunca wireadas no API Gateway (D-117), nenhuma tela de onboarding existia (D-118), `ExpressionAttributeNames: {}` quebrava `TransactWriteItems` real em 3 arquivos incluindo o guard de Ãºltima-proteÃ§Ã£o-de-OWNER (D-119), e o convite/aceite de membro nunca foi alcanÃ§Ã¡vel de ponta a ponta â€” tela nunca existiu, rota nunca foi wireada, e-mail real nunca foi ativado, SES sandbox sem NENHUMA identity verificada no ambiente inteiro (D-120, 2 endereÃ§os reais verificados com o Marcelo). Pendente para fechar: Marcelo testar convite/aceite/troca-de-organizaÃ§Ã£o com uma segunda conta â€” **explicitamente adiado para uma sessÃ£o futura**, nÃ£o bloqueia o resto do trabalho. Em paralelo, **W3-07 â€” orquestrador de purga ganhou design `APROVADO` (D-121, protocolo Claudeâ†”Codex 3 rodadas, 9,1/9,2): Step Functions + EventBridge Scheduler** â€” implementaÃ§Ã£o real ainda nÃ£o construÃ­da, fica para sessÃ£o dedicada futura (mesmo padrÃ£o de D-081); e 2 lacunas de produto levantadas pelo Marcelo (exportaÃ§Ã£o de dados CSV/planilha, reatribuiÃ§Ã£o de responsabilidade ao remover um Membership) tiveram rodadas de scoping dispatchadas via o mesmo protocolo â€” ver `decisions-log.md` para o D-number final de cada uma antes de assumir status. **`docs/engineering/definition-of-done.md` (E-012, emenda E-013)**: gate de processo permanente â€” nenhum item de todo list que produza cÃ³digo Ã© `completed` sem passar pelo gate correspondente ao seu nÃ­vel de risco, incluindo aplicaÃ§Ã£o concreta (nÃ£o sÃ³ referencial) de `test-engineering-standard.md`. Ver `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o exata â€” este bloco Ã© sÃ³ o resumo de fase, nÃ£o histÃ³rico item-a-item.
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `correlationid-xray-trace-join.md` | normativo atual (decisÃ£o + implementaÃ§Ã£o) | JunÃ§Ã£o `correlationId`â†”trace ADOT/X-Ray (E-011 pendÃªncia 1) â€” `APPROVED` via protocolo Claudeâ†”Codex (Claude 9,3/Codex 9,4), amplia D-022 sem reabri-la; implementado 2026-08-29 (`xray-trace-header.ts`, correÃ§Ã£o de precedÃªncia do `SecureLogger`), status `IMPLEMENTED`/`UNIT TESTED`, smoke test real em `dev` ainda pendente |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃ§Ã£o nÃ£o Ã© sequencial na ordem das linhas â€” D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/00-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt original (2026-08-22) que abriu as 3 fases de evoluÃ§Ã£o estratÃ©gica do roadmap abaixo; movido da raiz em 2026-08-29, mesmo tratamento de `blocker-b-mission-brief.md` |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `roadmap-evolution/16-document-lifecycle-strategic-analysis.md` | informativo (anÃ¡lise externa, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR) | EvoluÃ§Ã£o estratÃ©gica de "controle de vencimentos" para "gestÃ£o enxuta do ciclo de vida documental" â€” avaliaÃ§Ã£o de coerÃªncia/reaproveitamento/risco de overengineering; recomenda aprovar a direÃ§Ã£o mas nÃ£o antecipar o escopo, mantendo o foco em Consolidation + Pilot Readiness; identifica `Document` deixar de ser filho exclusivo de `ExpirationItem` como a mudanÃ§a arquitetural futura central, marcada nela mesma como Type 1 (ADR + protocolo Claudeâ†”Codex antes de qualquer implementaÃ§Ã£o) |
| `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` | **`APPROVED` â€” design tÃ©cnico (protocolo AGENTS.md Â§4) + timing (decisÃ£o direta do Marcelo, D-085)** | EspecificaÃ§Ã£o vigente de Multi-User B2B â€” `Organization` como tenant boundary permanente, `User` global, `Membership` N:N, RBAC por permissions. Protocolo Claudeâ†”Codex completo (Â§125): 3 rodadas reais, Claude 9,2/Codex 9,2, ambos â‰¥9,0 (mecanismo transacional de last-OWNER, unicidade de Membership/Invitation, tabela mantÃ©m/emenda/refaz do impacto no W3-07, supersessÃ£o explÃ­cita de D-060/GTR-01). **D-085 (2026-08-29)**: Marcelo decidiu diretamente proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05` (marcado `SUPERSEDED (timing)`, modelo de billing/entitlements desse cluster permanece vigente). **Waves B2B-0 a B2B-11 `DONE`** (D-108 conclui B2B-11, Responsibility + Notifications) â€” ver `multi-user-b2b-wave-tracker.md` para o detalhamento por subitem (inclui a nota de escopo sobre GTR-01/`Organization.displayName` ainda pendente). Wave B2B-12 (Cutover de dev) Ã© a prÃ³xima aÃ§Ã£o real, escopo ainda nÃ£o debatido â€” ver `NEXT_SESSION_PROMPT.md` para o estado de execuÃ§Ã£o |
| `roadmap-evolution/18-synthetic-persona-research-quality-baseline.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Fundamento metodolÃ³gico proposto para um futuro "Synthetic Persona Evaluation Framework" (agentes condicionados por persona exercitando a aplicaÃ§Ã£o real, em complemento â€” nunca substituiÃ§Ã£o â€” a testes humanos): modelo Persona+Contexto+Dataset+CenÃ¡rio+Objetivo="Trial", provenance de evidÃªncia (`hypothesisâ†’research-groundedâ†’human-observedâ†’production-observed`), hierarquia de oracle (estado determinÃ­stico > banco/API/evento > LLM grader > autoavaliaÃ§Ã£o do agente, esta Ãºltima nunca fonte de verdade), scorecard de 100 pontos + 18 Hard Gates (HG-01..18, ex. "agente nunca recebe oracle/caminho de navegaÃ§Ã£o", "actor nÃ£o pode ser o Ãºnico judge"). Puramente conceitual/genÃ©rico nesta etapa, ainda nÃ£o aplicado ao Expiration Tracker |
| `roadmap-evolution/19-synthetic-persona-application-analysis.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Aplica o baseline de `18` Ã  realidade atual do cÃ³digo: conclui que a aplicaÃ§Ã£o Ã© "muito favorÃ¡vel" a personas sintÃ©ticas (domÃ­nio modelado, OCC, idempotÃªncia, outbox, relÃ³gio injetÃ¡vel, `dev` resetÃ¡vel, Playwright, Test Engineering Standard jÃ¡ existentes), mas nomeia 4 restriÃ§Ãµes reais â€” zero usuÃ¡rio/produÃ§Ã£o real, frontend cobre sÃ³ parte das superfÃ­cies planejadas, modelo B2B ainda em transiÃ§Ã£o, e a principal: **o Playwright atual roda majoritariamente contra um BFF mockado (`page.route()`), nÃ£o contra o sistema real** â€” a mesma lacuna que B2B-14 (Operational Evidence) jÃ¡ vinha fechando manualmente. TambÃ©m documenta drift real entre `NEXT_SESSION_PROMPT.md`/comentÃ¡rios antigos e o cÃ³digo atual |
| `roadmap-evolution/20-synthetic-trial-identity-design.md` | informativo (anÃ¡lise externa trazida pelo Marcelo em 2026-08-31, nÃ£o passou pelo protocolo AGENTS.md Â§4, sem ADR, sem decisÃ£o de proceder) | Proposta de implementaÃ§Ã£o: identidade de execuÃ§Ã£o hierÃ¡rquica `evaluationRunIdâ†’trialIdâ†’correlationIdâ†’traceId/spanId`, deliberadamente **execution metadata, nunca atributo funcional do domÃ­nio** (nunca `ExpirationItem.trialId`), propagada via header Playwrightâ†’middleware BFFâ†’`RequestContext`â†’outbox `metadata` (nunca `payload`)â†’SQSâ†’workerâ†’audit event/log estruturado; gate de ambiente (`EVALUATION_CONTEXT_ENABLED`, `prod=false` por padrÃ£o), fail-closed para metadata invÃ¡lida, 12 Hard Gates prÃ³prios (HG-TID-01..12, ex. "trialId nunca altera autorizaÃ§Ã£o/validaÃ§Ã£o funcional"). O prÃ³prio documento recomenda revisÃ£o via protocolo Claudeâ†”Codex antes de entrar no roadmap â€” ainda nÃ£o submetida |
| `multi-user-b2b-wave-b2b4-scope.md` | **`APPROVED` (D-092, protocolo Claudeâ†”Codex 4 rodadas, Claude 9,3/Codex 9,4)** | Escopo final de Wave B2B-4 â€” `OnboardingStateResolver` puro (4 estados, procedimento sequencial estrito), sem wiring de login, sem exposiÃ§Ã£o HTTP; redesigna "remover auto-provision"/gate real para Wave B2B-5. EvidÃªncia das 4 rodadas em `reviews/multi-user-b2b-wave-b2b4-scoping/` |
| `multi-user-b2b-wave-b2b5-scope.md` | **`APPROVED` (D-095, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,1) â€” `IMPLEMENTADO` (D-096)** | Escopo final de Wave B2B-5 (RequestContext Cutover) â€” `bootstrapUser()` de 2 itens, `RequestContextResolver` reescrito com resoluÃ§Ã£o de Membership Ãºnica e assert explÃ­cito para `ADMIN` nÃ£o suportado, `POST /bff/organizations` com cap transacional (`GlobalUser.hasCreatedOrganization`), self-heal de sessÃ£o BFF; desvio faseado explÃ­cito de Â§11/Â§12 do physical model (seleÃ§Ã£o real de organizaÃ§Ã£o fica para B2B-6). EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b5-scoping/`; implementaÃ§Ã£o e 2 achados reais corrigidos (UserProfile mantido, gate de lifecycle da Organization adicionado) em `decisions-log.md` D-096 |
| `multi-user-b2b-wave-b2b6-scope.md` | **`APPROVED` (D-101, protocolo Claudeâ†”Codex 3 rodadas, checklist v2, Claude 9,1/Codex 9,2) â€” `IMPLEMENTADO` (D-102)** | Escopo final de Wave B2B-6 (BFF Organization Context) â€” terceira aplicaÃ§Ã£o real de E-014 (padrÃ£o header+revalidaÃ§Ã£o server-side, OWASP Multi Tenant Security). `X-Organization-Id` injetado sÃ³ pelo BFF (nunca do browser), `resolveWorkingOrganization()`/`OrganizationUnavailableError`(403)/`OrganizationSelectionRequiredError`(409) fecham o `InternalError` 500 que B2B-8 tornou explorÃ¡vel; `POST /bff/organization/select` (CSRF)/`GET /bff/organizations` (filtrado por lifecycle) novos. EvidÃªncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b6-scoping/` |
| `multi-user-b2b-wave-b2b7-scope.md` | **`APPROVED` (D-097, protocolo Claudeâ†”Codex 3 rodadas, checklist v2, Claude 9,2/Codex 9,3) â€” `IMPLEMENTADO` (D-098)** | Escopo final de Wave B2B-7 (RBAC) â€” primeira aplicaÃ§Ã£o real de `docs/engineering/research-protocol.md`/E-014 (pesquisa GitHub/Linear/Slack/Notion/NIST-ANSI-INCITS-359, checklist de critÃ©rios de nota contestado e reconciliado na Rodada 1â†’2). `Role` ganha `ADMIN`, paritÃ¡rio com `OWNER` nas 4 actions de deleÃ§Ã£o de recurso e no bypass de ownership; `notification:configure` reclassificada para `READ_ONLY_ROLES` (bug fix real, nÃ£o escolha ADMIN-vs-OWNER); `tenant:configure-document-request-delivery` isolada em novo tier `OWNER_ROLES`. EvidÃªncia das 3 rodadas (incl. a contestaÃ§Ã£o do checklist) em `reviews/multi-user-b2b-wave-b2b7-scoping/` |
| `multi-user-b2b-wave-b2b8-scope.md` | **`APPROVED` (D-099, protocolo Claudeâ†”Codex 3 rodadas, checklist v3, Claude 9,1/Codex 9,2) â€” `IMPLEMENTADO` (D-100)** | Escopo final de Wave B2B-8 (Invitations/Team) â€” segunda aplicaÃ§Ã£o real de E-014, declaraÃ§Ã£o `SIM PARCIAL` (mecanismo de dados jÃ¡ `APPROVED` em D-086; last-owner protection/hierarquia de gerÃªncia de membros pesquisados nesta wave, GitHub/Slack/Linear/Notion). `Invitation`+token pointer+dedup (D-086 Â§7), `AcceptInvitationService` (transaÃ§Ã£o de 6 itens, token consumido estruturalmente), `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService` com builder Ãºnico de `ownerCount` (`LastOwnerError`) â€” primeiro writer real de `Membership` alÃ©m da criaÃ§Ã£o. EvidÃªncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b8-scoping/` |
| `multi-user-b2b-wave-b2b9-scope.md` | **`APPROVED` (D-103, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,3/Codex 9,3) â€” `IMPLEMENTADO` (D-104)** | Escopo final de Wave B2B-9 (W3-07/Privacy Reconciliation) â€” quarta aplicaÃ§Ã£o real de E-014 (GitHub/Slack/Atlassian, "delete conta" vs. "delete organizaÃ§Ã£o"). Fix real: `InvitationTokenPointer` (declara `organizationId`, nÃ£o `tenantId`) ganha 3Âª clÃ¡usula OR no scan/`PURGE_DELETE` de purga, fechando Ã³rfÃ£o pÃ³s-exclusÃ£o de Organization. `privacy-lgpd.md` Â§4.1/Â§4.2 novos (fronteira User-level vs. Organization-level erasure, invariante de Ãºltimo OWNER documentada) + 4 linhas de retenÃ§Ã£o. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b9-scoping/` |
| `multi-user-b2b-wave-b2b10-scope.md` | **`APPROVED` (D-105, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2) â€” `IMPLEMENTADO` (D-106)** | Escopo final de Wave B2B-10 (Tenant-aware Frontend) â€” primeira aplicaÃ§Ã£o do protocolo a uma decisÃ£o de frontend nesta sessÃ£o. Fix de regressÃ£o real (`AuthContext` tratava todo usuÃ¡rio autenticado como deslogado desde B2B-5); arquitetura de cache isolation (query keys por `organizationId` + `ActiveOrganizationProvider`/`switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta) fecha uma corrida real de vazamento de dado entre tenants na UI; switcher/members/settings novos. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b10-scoping/` |
| `multi-user-b2b-wave-b2b11-scope.md` | **`APPROVED` (D-107, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2) â€” `IMPLEMENTADO` (D-108)** | Escopo final de Wave B2B-11 (Responsibility + Notifications) â€” quinta aplicaÃ§Ã£o de E-014 (GitHub/Linear, "assignee deve ser membro real e ativo"). `NotificationRecipientResolver` migrado de `UserProfile` (vestigial) para `Membership`+`GlobalUser` (2 condiÃ§Ãµes); `assigneeUserId`/`ItemWatch.userId` agora validados contra `Membership` real via `MemberEligibilityChecker` novo. **Nota**: nÃ£o inclui a supersessÃ£o de GTR-01 (`UserProfile.requesterDisplayName`â†’`Organization.displayName`), prevista em B2B-5 mas nunca debatida nas 3 rodadas reais desta wave â€” ver `multi-user-b2b-wave-tracker.md`. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b11-scoping/` |
| `multi-user-b2b-wave-b2b12-scope.md` | **`APPROVED` (D-110, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,1) â€” `IMPLEMENTADO` (D-111)** | Escopo final de Wave B2B-12 (Cutover de dev) â€” sexta aplicaÃ§Ã£o de E-014 (declaraÃ§Ã£o `NÃƒO`, polÃ­tica reset-vs-migraÃ§Ã£o jÃ¡ aprovada dentro do prÃ³prio `roadmap-evolution/17` Â§62-63). InventÃ¡rio real via `aws --profile claude-dev` confirma `dev` 100% sintÃ©tico/descartÃ¡vel; `scripts/reset-dev-data.ts` (Fase A inventÃ¡rio+snapshot sempre, Fase B delete real sÃ³ `--confirm`, allowlist de tabela/conta, verificaÃ§Ã£o final fail-loud) + remoÃ§Ã£o de `LEGACY_TENANT_ONLY` (backend+frontend+teste). Fase A verificada de fato contra `dev` (nÃ£o sÃ³ fakes). **ExecuÃ§Ã£o real destrutiva (`--confirm`/`--include-cognito`) pendente de confirmaÃ§Ã£o explÃ­cita do Marcelo.** EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b12-scoping/` |
| `multi-user-b2b-wave-b2b13-scope.md` | **`APPROVED` (D-112, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,2, rÃ©gua E-014 9,3) â€” `IMPLEMENTADO` (D-113)** | Escopo final de Wave B2B-13 (E2E/Adversarial Security) â€” sÃ©tima aplicaÃ§Ã£o de E-014 (OWASP WSTG/Authorization Testing, SIM PARCIAL). Auditoria completa contra as 25 perguntas de `roadmap-evolution/17` Â§121 (matriz Qâ†’arquivo:linha completa): 20/25 jÃ¡ cobertas por testes reais de waves anteriores; 2 fecham com fix real (mesmo TOCTOU em `notification.ts`+`subject.ts` â€” entrega assÃ­ncrona nunca revalidava `Membership`, sÃ³ roteamento checava â€” corrigido reaproveitando `DynamoDbNotificationRecipientResolver` jÃ¡ testado, `ResolvedRecipient` ganha `email?: string`); 2 ganham teste novo (revogaÃ§Ã£o de Membership encadeada ponta-a-ponta; roles diferentes por Organization nunca vazam); 1 Ã© auditoria de fixture IDs (zero achado). A Rodada 1 do Codex achou um 6Âº achado real (mesmo TOCTOU em `document-chasing-dispatch`) que a proposta original nÃ£o tinha visto. EvidÃªncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b13-scoping/` |
| `multi-user-b2b-wave-b2b0-inventory.md` | evidÃªncia de rodada (read-only, completo) | InventÃ¡rio verificado de Wave B2B-0 (Â§105 do documento acima): `tenantId=userId` em produÃ§Ã£o (3 pontos confirmados, 1 gap de fencing prÃ©-existente achado no login BFF), `IdentityMapping`/BFF session/`RequestContext`, stores tenant-scoped/S3 (3 prefixos reais, diverge de Â§68)/eventos/W3-07, frontend caches (zero isolamento tenant hoje) e contagem reproduzÃ­vel da suÃ­te de testes (122 arquivos/1092 casos, nÃ£o ~1104) |
| `multi-user-b2b-physical-model.md` | **`APPROVED` (D-086, protocolo Claudeâ†”Codex 5 rodadas, Claude 9,3/Codex 9,5)** | Physical model final de Wave B2B-1 â€” `User`/`IdentityMapping`/`Organization`/`Membership` (3 estados, GSI4 `MembershipByUser` reaproveitado)/`Invitation`/`ownerCount`/`bootstrapUser()`/`DeviceSession`+`logoutAll`/`RequestContext`/BFF session, respostas finais Ã s 25 perguntas de `roadmap-evolution/17` Â§121. EvidÃªncia das 5 rodadas em `reviews/multi-user-b2b-physical-model/` |
| `multi-user-b2b-wave-tracker.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Status DONE/IN PROGRESS/BLOCKED/NOT STARTED por wave (B2B-0 a B2B-15), mesmo papel de `docs/engineering/pilot-readiness-program.md` â€” ponto de entrada para saber o que jÃ¡ foi feito antes de continuar a iniciativa |
| `reviews/context-engineering-reconciliation/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 | ReconciliaÃ§Ã£o da arquitetura de contexto do repo (2026-08-29) â€” root cleanup (21â†’6 `.md`), `AGENTS.md`/`NEXT_SESSION_PROMPT.md` trimados (narrativa de milestone jÃ¡ duplicada em `decisions-log.md` removida), 2 guardrails novos em `check-doc-drift.ts` (root allowlist, size), READMEs stale corrigidos. 4 rodadas Codex (8,1â†’8,8â†’8,9â†’9,3/10, `APPROVED`), 3 achados reais de drift semÃ¢ntico prÃ©-existente corrigidos no processo |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/w3-07-purge-orchestrator-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-121) | Orquestrador real do purge pipeline W3-07 (Step Functions + EventBridge Scheduler) â€” decisÃ£o pendente desde D-083, fechada 3 rodadas (6,7â†’8,2â†’9,2/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o (novo mÃ³dulo Terraform, 2 Lambdas, `CloseOrganizationService`) fica para sessÃ£o dedicada futura |
| `reviews/responsibility-reassignment-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-122) | ReatribuiÃ§Ã£o de responsabilidade quando um Membership Ã© removido/sai â€” precondiÃ§Ã£o bloqueante best-effort via GSI1 jÃ¡ existente (zero GSI novo), 3 rodadas (7,0â†’8,1â†’9,1/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/data-export-scoping/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-123) | ExportaÃ§Ã£o de dados (CSV) de `ExpirationItem` â€” `item:export`/`ADMIN_ROLES`, handler sÃ­ncrono via `queryGsi1` com orÃ§amento decrescente + guard de 4 MB, `csv-export-writer.ts` (RFC4180+mitigaÃ§Ã£o de fÃ³rmula), 3 rodadas (6,2â†’8,1â†’9,1/9,1 `APPROVED`). Design apenas â€” implementaÃ§Ã£o fica para sessÃ£o dedicada futura |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO em 2026-08-20 (9/9 avaliados, sÃ³ Contexto bateu o gate â‰¥9.0 originalmente) â€” achados remediados/reavaliados em rodadas posteriores por eixo, ver decisions-log.md/pilot-readiness-program.md, nÃ£o re-enumerado aqui
Programas posteriores:    test-engineering-standard.md (E-010, APPROVED), logging-observability-standard.md (E-011, APPROVED), definition-of-done.md (E-012, APPROVED â€” gate por item de todo list; emenda E-013 tornando a aplicaÃ§Ã£o de test-engineering-standard.md operacional, nÃ£o sÃ³ referencial), research-protocol.md (E-014, APPROVED â€” pesquisa externa exigida/checklist de nota antes de decisÃµes Type 1 que dependem de padrÃ£o de mercado), Consolidation + Pilot Readiness Program (pilot-readiness-program.md/pilot-readiness-assessment.md, CONDITIONAL GO)
Last verified:            2026-08-30 (E-014 aprovado â€” ver docs/architecture/README.md para o estado vigente completo, nÃ£o duplicado aqui)
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Marcar um item de todo list de cÃ³digo como `completed` | `definition-of-done.md` â€” gate por item (nÃ£o por PR/wave inteira): unidade de conclusÃ£o, gate por nÃ­vel de risco, registro mÃ­nimo de evidÃªncia |
| Decidir se uma proposta Type 1 precisa de pesquisa externa antes da Rodada 1, e como isso vira critÃ©rio de nota | `research-protocol.md` â€” critÃ©rio decidÃ­vel (nÃ­vel 5-6 + padrÃ£o externo estabelecido), declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO`, checklist de critÃ©rios derivado da pesquisa, fluxo de reconciliaÃ§Ã£o se o Codex contestar o checklist |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `definition-of-done.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,1/Codex 9,2, E-012; emenda E-013) | Definition of Done por item de todo list â€” granularidade ("unidade de conclusÃ£o"), gate por nÃ­vel de risco, classificaÃ§Ã£o de risco na prÃ¡tica, registro mÃ­nimo de evidÃªncia; complementa `quality-gate-tiers.md`/`change-risk-scale.md` sem substituÃ­-los. E-013 tornou a aplicaÃ§Ã£o de `test-engineering-standard.md` operacional (G-V3 mutaÃ§Ã£o nomeada por escrito), nÃ£o sÃ³ referencial |
| `research-protocol.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, Claude 9,2/Codex 9,2, E-014) | Pesquisa externa antes de decisÃµes Type 1 â€” gatilho acoplado a `change-risk-scale.md` nÃ­vel 5-6 + Rodada 1 do protocolo (nunca uma disciplina paralela para lembrar); declaraÃ§Ã£o `SIM`/`SIM PARCIAL`/`NÃƒO` com fonte+data+representatividade; checklist de critÃ©rios de nota derivado da pesquisa, sub-rubrica subordinada a `joint-review-criteria.md`; fluxo de reconciliaÃ§Ã£o quando o Codex contesta o checklist em si |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `logging-observability-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 3 rodadas, gate elevado 9,5/10) | RÃ©gua concreta de qualidade para logging/tracing/taxonomia de erro (`src/shared/observability/**`, `app-error.ts`, wiring Terraform de detecÃ§Ã£o) â€” 8 critÃ©rios ponderados, Ã¢ncoras de pontuaÃ§Ã£o, gate de auditoria 9,0/10, escrita em resposta a achados reais de uma rodada Codex de 2026-08-29 |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`docs/project/handoffs/expiration-tracker-next-days-master-plan-and-ai-prompt.md`, movido da raiz em 2026-08-29) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.


codex
O estado do repositório confirma que este é o próximo item autorizado e uma decisão Type 1. Vou agora testar os pontos que mais podem alterar a conclusão: semântica real de partial batch, destino de mensagens “suprimidas” e precisão da afirmação sobre custo/tempo.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "retryable|isRetryable|batchItemFailures|maxReceiveCount|D-12[457]|E-011" src infra docs/architecture/decisions-log.md docs/engineering/decisions-log.md; Get-Content -Raw docs/engineering/research-protocol.md; Get-Content -Raw docs/engineering/joint-review-criteria.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 235ms:
docs/engineering/decisions-log.md:55:2. **`reminder-dispatch-handler.ts` fazia cast não validado de `JSON.parse(record.body)`** apesar de já existir um schema (`schemas/queues/reminder-dispatch.v1.json`) nunca usado por nenhum handler real. Corrigido para validar via `defaultSchemaRegistry` antes do cast, tratando payload schema-inválido como poison message (não retryable).
docs/engineering/decisions-log.md:81:## E-011 — `logging-observability-standard.md` APPROVED (3 rodadas, gate 9,5/10) + auditoria real da implementação (7,0 → 9,0 → 8,7/10) + 1 decisão de produto registrada, pendente
docs/engineering/decisions-log.md:89:**Auditoria formal da implementação real contra os critérios `APPROVED`**: primeira passada, **7,9/10** — abaixo do gate. 4 achados BLOQUEANTES: (1) `correlationId` não atravessava a fronteira Step Functions do pipeline de extração/OCR (cada um dos 4 task handlers gerava um `randomUUID()` novo, tornando os logs de uma mesma execução impossíveis de correlacionar entre si); (2) invocação síncrona Lambda→Lambda para o parser-sandbox sem nenhuma propagação/logging; (3) campo `AppError.retryable` documentado como "drives SQS consumer behavior" mas na prática nunca consultado por handler real (confirmado por grep: só um log, zero branching); (4) `email-delivery-handler.ts` logava schema-inválido/JSON malformado fora de qualquer `runWithContext`, sem correlationId nenhum.
docs/engineering/decisions-log.md:91:**Corrigido nesta sessão (não a decisão de produto pendente, ver abaixo)**: propagação real de `correlationId` de ponta a ponta pelo pipeline de extração inteiro (`ExtractionExecutionInput`/`StartOcrInput`/`TextractJob`/`RunDeterministicParserInput-Output`/`RunBedrockExtractionInput-Output`/`ValidationContext`, todos ganharam o campo, sempre re-ecoado nos 2 pontos de retorno de `run-bedrock-extraction.ts`; os 4 task handlers passaram a ler o valor em vez de gerar um novo); `LambdaPdfParser`/`parser-sandbox-handler.ts` (que tinha ZERO logging antes) ganharam propagação real + `SecureLogger`; `email-delivery-handler.ts` ganhou o mesmo padrão de fallback-correlationId-antes-do-parse que `reminder-dispatch-handler.ts` já tinha; `reminder-dispatch-handler.ts`/`email-delivery-handler.ts` passaram a usar `toAppError`+logar `errorCode`/`retryable` (antes logavam `err.message` cru); comentário desatualizado em `textract-task-handler.ts` sobre `ErrorEquals` casar `.code` (deveria ser `errorType`, já corrigido de fato desde W2-02) corrigido; descrição de alarme em `security-audit-observability/main.tf` não cita mais "4 HTTP handlers" (número já obsoleto de novo). 8 testes novos/estendidos provam a propagação real, não só que compila. Rodada de reconfirmação: **8,7/10** — 2 dos 4 bloqueantes fechados de verdade (achados 3/4 acima), 2 permanecem.
docs/engineering/decisions-log.md:95:2. **`AppError.retryable` não decide comportamento real de nenhum handler SQS** — Marcelo decidiu explicitamente: manter como está, só documentação corrigida (não mais afirma falsamente que "drives SQS behavior" — `app-error.ts`'s comentários em `AppError`/`isRetryable()` agora são honestos sobre o escopo atual real: só diagnóstico/logging). **Decisão de produto pendente, registrada aqui, não implementada**: se/quando SQS handlers devem realmente parar de retentar um erro `retryable:false` (hoje todo handler reporta `batchItemFailure` incondicionalmente, deixando `maxReceiveCount`+DLQ nativos do SQS serem o árbitro real) é uma mudança de comportamento de produção genuína (o que aconteceria com uma mensagem não-retryable? dropar? rotear para um estado terminal explícito?) — não decidida, não implementada, mesma classe de `W2-01-DECISION`/`pilot-readiness-program.md`.
docs/architecture/decisions-log.md:42:- **Rodada 6 (Architecture Red Team, seção 58)**: 20 cenários avaliados independentemente por Claude e Codex (`docs/architecture/history/architecture-fase3/red-team-claude-round1.md`, `docs/architecture/history/architecture-fase3/red-team-codex-round1.md`). Convergência forte, 6 lacunas críticas fechadas (upload presigned contornando quota, maxReceiveCount+SLA de DLQ, inbox de webhook idempotente, optimistic concurrency control para alteração de data/exclusão de documento, falha de região como risco aceito documentado, expand/contract para rollback de schema/eventos). Codex revisou os fechamentos e apontou 2 refinamentos não-bloqueantes (restituição de slot de upload via reconciliador, não TTL direto; chave composta de webhook `provider+tenant+providerEventId`), ambos incorporados. **Reavaliação final: Claude 9.13 / Codex 9.20 (exato) — ambos ≥9.0, nenhum gate violado.**
docs/architecture/decisions-log.md:139:| D-122 | Reatribuição de responsabilidade ao remover/sair de uma organização — pergunta real do Marcelo (2026-08-30): quando um membro responsável por um vencimento é removido, alguém herda a responsabilidade? Proposta dele: o próprio membro reatribui antes de sair; se o item já tem outro responsável, não precisa. Confirmado por leitura de código (não hipotético) que `RemoveMembershipService`/`LeaveOrganizationService` nunca tocam `ExpirationItem.assigneeUserId`/`ItemWatch` — o campo fica órfão indefinidamente, a notificação só para de disparar silenciosamente no dispatch (B2B-11/D-108). Pergunta aberta desde `roadmap-evolution/17` §121 itens 22-23, nunca respondida com mecanismo. **Declaração E-014: SIM** — pesquisa real (Jira/GitHub/Linear, fetch direto 2026-08-30) sobre reatribuição de assignee na desativação/remoção de um membro de workspace. **Achado real que corrigiu uma alegação informal anterior desta mesma sessão** (que citava Linear como exemplo de "unassign on deactivation"): as 3 fontes reais **convergem em NÃO bloquear nem reatribuir automaticamente na operação equivalente a `Remove`/`Leave` (soft/deactivate)** — Jira preserva a atribuição ("assignee/watcher/reporter... will still appear"), GitHub faz auto-unassign best-effort inconsistente, Linear preserva indefinidamente para referência histórica. O único precedente real de bloqueio (Jira) se aplica a hard DELETE de usuário, operação mais destrutiva sem equivalente direto neste projeto. **Decisão diverge deliberadamente do mercado** (não por convergência externa, registrado explicitamente como tal) — justificada pela natureza do domínio (rastreamento de prazo/obrigação legal tem custo de item órfão mais alto que um board de tarefas genérico). **Achado interno decisivo**: `assigneeUserId` não tem GSI próprio, mas `GSI1` (`GSI1PK=TENANT#t#ITEMSTATUS#ACTIVE`, `projection_type=ALL`, confirmado por leitura direta de `infra/modules/dynamo-table/main.tf`) já é o índice que o dashboard usa para listar itens ACTIVE de um tenant — encontrar "itens ACTIVE atribuídos ao usuário X" é uma `Query` nesse índice já aprovado + `FilterExpression`, zero GSI novo, zero decisão D-026 nova. `ItemWatch` (sem índice viável, precedente B2B-11 de "remover watcher é sempre seguro") e `RequirementAssignment`/`TrackedSubject` (confirmado por leitura que nenhum dos dois tem um segundo campo de responsável) ficam fora de escopo. **Rodada 1 (Codex 7,0/10 design, 7,2/10 régua contestada, não aprovado)**: síntese da pesquisa Jira imprecisa (citava só o ticket antigo, não a distinção real deactivate-preserva vs. delete-bloqueia); TOCTOU real e não resolvido entre o check via GSI1 (eventualmente consistente, nunca `ConsistentRead` em GSI) e a transação de remoção; checklist original subpesava consistência sob concorrência; capacidade por tenant não fechada. **Rodada 2 (Codex 8,1/10 design, 8,4/10 régua, não aprovado)**: pesquisa Jira corrigida (deactivate preserva, delete bloqueia — fontes `support.atlassian.com/user-management/docs/remove-or-suspend-a-user/` + thread da comunidade, ambas fetch direto); TOCTOU aceito como best-effort explicitamente *bounded* pelo status quo já `APPROVED` em B2B-11 (pior caso nunca pior que o bug que esta proposta corrige) — atomicidade perfeita confirmada inatingível (GSI nunca fortemente consistente, `TransactWriteItems` exige chaves conhecidas antecipadamente, máx. 100 itens); checklist reancorado com critério novo dedicado a consistência (peso 25%, contestado de novo por premiar demais uma garantia fraca); payload do erro limitado a 20 `itemId`s + `totalKnown`/`truncated`; decidido "sem bypass de emergência nesta versão" como default normativo (não mais pendência aberta). **Rodada 3 (Codex 9,2/10 régua, 9,1/10 design, Claude 9,1/10, APROVADO)**: critério de consistência repesado (25%→15%, âncora exige declaração explícita de semântica best-effort + observabilidade via mecanismos JÁ existentes — audit event de remoção + log de cancelamento de notificação de B2B-11 — nunca telemetria nova inventada); contrato de paginação explícito fechando risco real de falso negativo (`Query` pagina a partição `GSI1` `ACTIVE` inteira via `LastEvaluatedKey` até esgotar, nunca usa `Limit` do DynamoDB como truncamento — o cap de 20 vale só para a lista retornada, depois de já contar o total real); precisão de citação (conclusão vem da combinação de 2 fontes, não de uma isolada). **Mecanismo decidido**: precondição bloqueante *best-effort* (nunca atômica) em `RemoveMembershipService.remove()`/`LeaveOrganizationService.leave()`, via porta nova `AssignedActiveItemsLookup` no módulo `organization` (implementação real no composition root, mesma classe de decisão já `APPROVED` de `MemberEligibilityChecker`/B2B-11) — nota de implementação do Codex: o `queryGsi1` já existente em `dynamodb-expiration-store.ts` tem `Limit`+break por `items.length`, não deve ser reaproveitado ingenuamente para este lookup. Erro novo `ResponsibilityReassignmentRequiredError` (`BUSINESS_RULE`, `retryable: false`, mesmo padrão de `LastOwnerError`) com `{targetUserId, itemIds (até 20), totalKnown, truncated}`. `ItemWatch` fora de escopo. Sem bypass de emergência nesta versão. **Fora de escopo desta rodada**: implementação real (nenhum código escrito) — só o mecanismo/design, mesmo padrão de D-121. | Type 1 (nível 5, `AGENTS.md` §4 — nova fronteira de módulo cruzando organization↔expiration, nova precondição bloqueante numa mutação já em produção) | 9,1/10 (Rodada 3, auto-avaliação às cegas) | 9,1/10 (Rodada 3, final) | **DESIGN APROVADO — implementação real (nova porta, novo erro, wiring do composition root, suíte de testes) fica para uma sessão dedicada futura, mesmo padrão de D-121** | `docs/architecture/reviews/responsibility-reassignment-scoping/` | 2026-08-30 |
docs/architecture/decisions-log.md:144:| D-125 | Responsibility Reassignment on Member Removal — implementação real do mecanismo APROVADO em D-122 (Claude 9,1/Codex 9,1, 3 rodadas, `docs/architecture/reviews/responsibility-reassignment-scoping/`), mesmo padrão de D-096/D-102/D-108/D-113/D-124 (implementação direta de design já aprovado, nenhuma rodada Claude↔Codex nova para o mecanismo). **Precondição best-effort, explicitamente NÃO atômica**, em `RemoveMembershipService.remove()`/`LeaveOrganizationService.leave()`: antes da transação de remoção, checa se `targetUserId` ainda é `assigneeUserId` de algum `ExpirationItem` `ACTIVE` na organização e lança `ResponsibilityReassignmentRequiredError` em vez de prosseguir — uma leitura síncrona de decisão, nunca parte do `TransactWriteItems` (Round-3 "Estado final consolidado"). **Nova porta `AssignedActiveItemsLookup`** (`organization/ports/assigned-active-items-lookup.ts`, nome/forma do Round-3 — supersede o `SoleResponsibilityChecker` do Round-1) na CONSUMING module, implementada no composition root (`runtime/aws/composition/organization.ts`'s `buildAssignedActiveItemsLookup`, exportada para teste direto) — mesmo padrão estrutural de `expiration/ports/member-eligibility.ts`, mas na direção OPOSTA (primeira vez que `organization` lê dado de `expiration`). **Contrato de paginação da Correção 2 do Round-3, implementado literalmente**: pagina o GSI1 `TENANT#t#ITEMSTATUS#ACTIVE` até esgotar (`LastEvaluatedKey` até `undefined`), aplica `FilterExpression: assigneeUserId = :userId` a cada página, NUNCA usa `Limit` do DynamoDB como proxy de truncamento — `totalKnown` é a contagem real de todas as páginas, o teto de 20 aplica só à lista `itemIds` retornada, `truncated` é `totalKnown > itemIds.length`. GSI1 não está na taxonomia restrita de `security-audit.ts` (só GSI3/GSI6 exigem `security.global_index_access`) — nenhum evento novo de auditoria necessário. **Erro novo** `ResponsibilityReassignmentRequiredError` (`app-error.ts`, `BUSINESS_RULE`/`RESPONSIBILITY_REASSIGNMENT_REQUIRED`, `retryable: false`, `details: {targetUserId, itemIds (até 20), totalKnown, truncated}`), mapeado para 422 pelo `STATUS_BY_CATEGORY` genérico já existente (nenhuma mudança de handler necessária). **Ordem de checagem** em `remove-membership.ts`: autorização → tier OWNER (`OwnerTierChangeRequiresOwnerError`) → reatribuição de responsabilidade (novo) → transação (que ainda aplica `LastOwnerError` atomicamente) — a checagem de tier vence porque decide SE o caller pode agir, antes de qualquer pergunta sobre o estado do alvo; documentado inline com teste dedicado provando a ordem quando ambos os erros se aplicariam. **Escopo respeitado integralmente**: `ItemWatch`/watchers intocados (precedente B2B-11 mantido), nenhum bypass de emergência inventado (pergunta de produto real, deferida a Marcelo, não decidida aqui), nenhuma UI nova de reatribuição (a mutação `updateItem` já existente, já validada por `MemberEligibilityChecker`, é o mecanismo) — só paridade mínima de mensagem de erro no frontend (`isResponsibilityReassignmentRequiredError` em `api/errors.ts` + `Settings.tsx`'s `LeaveOrganizationSection`, mesma classe de polimento pequeno que D-120 fez para `LastOwnerError`). **G-V3**: 3 mutações reais verificadas e revertidas — (1) neutralizar a checagem `if (assigned.itemIds.length > 0)` em `remove-membership.ts` (1 teste falha); (2) trocar `truncated: totalKnown > itemIds.length` por `truncated: false` derivado de `itemIds.length` no adapter (1 teste falha — prova que `totalKnown` não é derivado do array capado); (3) remover o `while (exclusiveStartKey)` (travar em uma página só) no mesmo adapter (1 teste falha — prova paginação real até exaustão). Nenhuma mutação mascarada. **Sem infra/rota nova**: `remove-membership.ts`/`leave-organization.ts` já wireados a handlers HTTP existentes desde B2B-8 — confirmado por leitura, não assumido; `build:lambdas`/`terraform test` não exigidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos (as 2 falhas de `npm test` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes em `scripts/reset-dev-data.ts`, sem relação com esta mudança); backend 1249 testes passando / 137 arquivos (2 novos: `assigned-active-items-lookup.test.ts` com 4 testes, +6 testes novos em `membership-management.test.ts`), frontend 156 testes / 24 arquivos inalterado (só paridade de mensagem, sem teste novo de UI). Zero regressão real. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-122, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo de precondição best-effort real, porta + adapter reais, erro novo mapeado, paridade mínima de frontend | `src/modules/organization/ports/assigned-active-items-lookup.ts`, `src/modules/organization/application/{remove-membership,leave-organization}.ts`, `src/runtime/aws/composition/organization.ts`, `src/shared/errors/app-error.ts`, `frontend/src/api/errors.ts`, `frontend/src/routes/Settings.tsx`, `test/unit/organization/{assigned-active-items-lookup,membership-management}.test.ts`, `test/unit/identity/resolver.test.ts` | 2026-08-31 |
docs/architecture/decisions-log.md:145:| D-124 | W3-07 — implementação real do orquestrador de purga de tenant, exatamente o mecanismo APROVADO em D-121 (implementação direta de design já aprovado, mesmo padrão de D-096/D-102/D-108/D-113 — nenhuma rodada Claude↔Codex nova para o mecanismo). Fecha a lacuna que o próprio D-121 nomeou: `transitionTenantLifecycle()` (D-068) e `purgeTenant()` (W3-07) eram código real, funcional e testado, **sem nenhum chamador em produção** — nada no sistema jamais disparou uma purga. **Nova máquina de estados Step Functions** (`infra/state-machines/tenant-purge.asl.json` + `infra/modules/tenant-purge-workflow/`, gêmeo estrutural de `extraction-workflow`): `Wait(1800s)` literal (cutoff D-066 Rodada H, nunca redecidido) → `AdvanceToQuiescing` → `AdvanceToPurging` → `RunPurge` → `EvaluatePurgeResult` (`Choice`, nunca `Retry`/`Catch` — Fix 2, porque `purgeTenant()` devolve `PARTIAL`/`SUCCESS`/`FAILED` como valor resolvido normal) → `AdvanceToVerified` → `AdvanceToDeleted` → `Succeed`; ramos `MarkBlocked*` → `Fail`. **Handler único de transição** (Fix 3) parametrizado por `from`/`to` no `Parameters` de cada estado, com no-op idempotente quando o registro já está em `to` e re-leitura única em `SystemMutationConflictError`. **Envelope compacto** (Fix 6) verbatim: `{status, checkpoint, counters:{s3UnresolvedCount,dynamoRejectedCount,sessionRejectedCount}, failure}`; `TenantPurgeResult` completo continua só em log estruturado (`SecureLogger`). **`PURGE_RETRY_LIMIT = 20`** como `local` Terraform nomeado, substituído no ASL via `replace()` (Fix 8). **Alarme CloudWatch novo e real** (Fix 7): `AWS/States` `ExecutionsFailed` **e** `ExecutionsTimedOut`, dimensionados por `StateMachineArn`, no mesmo tópico SNS de alerta; `evaluation_periods=1` (não 3 como os alarmes de reminder) porque uma purga falha deixa dado de tenant fisicamente presente após um pedido de exclusão. **Sweeper** via EventBridge Scheduler `rate(1 day)` com as 2 responsabilidades aprovadas: reparo durável de `StartExecution` para tenants `DELETING`/`QUIESCING`/`PURGING`/`VERIFIED` parados há >1h (nunca `BLOCKED`/`HELD` — remediação humana em curso), e verificação residual pós-`DELETED` na janela de 90 dias reusando `verifyTenant*Empty()` sem inventar lógica nova. **`CloseOrganizationService`** novo com a ordenação corrigida da Rodada 3 Fix 8 (checagem de estado terminal ANTES do `StartExecution` incondicional), erro novo `OrganizationClosureUnavailableError` (`CONFLICT`/409, mesma categoria de `TenantNotActiveError`), action RBAC nova `organization:close` em `OWNER_ROLES`. **Composition root real** (`src/runtime/aws/composition/tenant-purge.ts`) que fecha o KNOWN LIMIT documentado no próprio `purge-tenant.ts` (Codex rodada 5): tabela FECHADA bucket→raiz (`clean/`, `tenant/`, `tenant/`, `ocr/`, cada uma verificada lendo o key builder real), nunca um par `{bucket, prefix}` arbitrário. **IAM mínimo** conforme Fix 8, verificado contra os adapters reais (sem `s3:GetObject` em lugar nenhum — a purga apaga, nunca lê conteúdo). **3 achados reais corrigidos durante a implementação, nenhum deles mudança de forma do design aprovado**: (1) o design nomeava `Wait` como cobrindo `DELETING→QUIESCING`, mas um estado `Wait` não escreve no DynamoDB — foi necessário um `AdvanceToQuiescing` explícito, realização fiel do mesmo handler único, não desvio; (2) `checkpoint`/`failure` do envelope precisam ser `null` explícito e nunca ausentes — o `ResultSelector` do ASL lê por JSONPath e um caminho AUSENTE é falha dura `States.Runtime`, não `null`; (3) a role de execução da máquina de estados precisava de `logs:*LogDelivery`+`xray:Put*` além de `lambda:InvokeFunction` — sem elas o `apply` falha (não o runtime), achado por comparação direta com `extraction_workflow_invoke_lambdas`, não assumido. **Rota HTTP**: `POST /organizations/close` no módulo **`api-gateway`** (não `bff-api-gateway`) + `proxy-allowlist.ts` + dispatch no `memberships-handler`, todos no MESMO commit, com assert dedicado no `tftest` — correção de roteamento apurada por leitura: toda mutação de organização (`leave`, `settings`) roda por `api-gateway`+proxy do BFF, nunca por uma rota `/bff/*` própria; seguir a instrução literal do prompt teria reproduzido a classe de bug de D-117/D-120. **UX de confirmação** (latitude explícita da Rodada 1): `CloseOrganizationSection` em `Settings.tsx`, type-to-confirm do `organizationId` (não diálogo de 2 passos — o segundo clique vira memória muscular; digitar o identificador obriga a ler QUAL organização está sendo destruída, o modo de falha que importa agora que um usuário pertence a várias), visível só a OWNER no cliente, com o mesmo token revalidado no servidor contra o tenant RESOLVIDO. **G-V3**: mutação real verificada e revertida em 3 pontos — remover o no-op idempotente do handler de transição (2 testes falham), remover o filtro de 1h do sweeper (1 teste falha), e a ordenação Rodada-2 do `CloseOrganizationService` (4 testes falham). A primeira tentativa de mutação da ordenação foi MASCARADA pelo próprio guard de exaustividade do serviço e não falhou — registrado honestamente e refeito com a mutação fiel (ambos os guards removidos). O bound do `Choice` é coberto no `tftest` do módulo (`"NumericLessThan": 20` como número JSON real, nunca string). **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos; backend 1267 testes / 136 arquivos (32 novos), frontend 156 testes / 24 arquivos (4 novos); `terraform test` 19/19 raiz (3 runs novos), 9/9 `tenant-purge-workflow` (módulo novo), 1/1 `api-gateway`, 7/7 `bff-api-gateway`; zero regressão. Nenhuma operação AWS destrutiva — só `validate`/`test`/`plan`. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-121, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — orquestrador real de ponta a ponta; `transitionTenantLifecycle()`/`purgeTenant()` deixam de ser código sem chamador | `infra/state-machines/tenant-purge.asl.json`, `infra/modules/tenant-purge-workflow/`, `infra/{main,outputs}.tf`, `infra/modules/api-gateway/`, `infra/tests/stack.tftest.hcl`, `src/workers/tenant-purge/{lifecycle-transition,purge-result-envelope,tenant-purge-sweep}.ts`, `src/shared/tenant-lifecycle/{tenant-purge-execution-starter,sfn-tenant-purge-execution-starter}.ts`, `src/shared/dynamodb/tenant-purge-scan.ts`, `src/shared/errors/app-error.ts`, `src/modules/organization/{application/close-organization.ts,http/organization-lifecycle-handlers.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition,handlers}/**`, `frontend/src/**` | 2026-08-31 |
docs/architecture/decisions-log.md:146:| D-126 | CSV data export — implementação real do mecanismo APROVADO em D-123 (implementação direta de design já aprovado, mesmo padrão de D-096/D-102/D-108/D-113/D-124/D-125 — nenhuma rodada Claude↔Codex nova para o mecanismo). **Nova action RBAC `item:export`** (`identity/domain/authorization.ts`) em `ADMIN_ROLES`, justificativa de assimetria de disclosure verbatim do design. **Orçamento decrescente literal** (round-3 Achado #1) em `ExpirationService.exportItems()` — 3 chamadas `queryGsi1` (`ACTIVE`/`ARCHIVED`/`RENEWED`, ordem fixa, `DELETED` nunca consultado), `limit: budget+1` para detectar estouro sem query de contagem separada, `EXPORT_ITEM_CAP=2000` exportado como constante nomeada; confirmado por leitura de `dynamodb-expiration-store.ts` que `queryGsi1` pagina internamente até acumular `limit` itens reais sem `FilterExpression`, tornando `limit`-como-contagem seguro aqui (diferente do achado de D-125 sobre GSI1+filter). **Guard de 4 MB** (round-3 Achado #2) em `buildExportCsv()` (`expiration/http/export-handler.ts`), acumulando `Buffer.byteLength` do CSV final serializado (header+CRLF+linhas pós-quoting RFC4180), nunca a soma de valores brutos — qualquer um dos dois limites (item ou byte) que disparar primeiro rejeita com o mesmo `ValidationError`. **`src/shared/csv/csv-export-writer.ts`** novo, `serializeCsvField()`/`serializeCsvRow()` únicos fazendo mitigação de fórmula (apóstrofo, extensão do precedente `APROVADO` de `roadmap-evolution/09`) ANTES da quotação RFC4180 no mesmo passe — nunca dois mecanismos separados, provado por teste dedicado do caso que precisa dos dois. **`toApiGatewayCsvResult()`** novo e aditivo em `http-adapter.ts` (round-3 Achado #3) — único call site é o novo handler; `toApiGatewayResult()` existente inalterado. **Colunas CSV** (15, ordem fixa) reverificadas contra `expiration-item.ts` real: `itemId,name,category,description,dueDate,issueDate,periodicity,issuer,number,assigneeUserId,tags,priority,status,createdAt,updatedAt`. **Filename** gerado só de `tenantId` (ULID server-side) + timestamp — nunca `Organization.displayName` nem string do cliente (achado não-bloqueante do Codex round-3, risco de header injection). **Decisão de wiring HTTP, não coberta literalmente pelo design aprovado**: `items_handler` (Terraform) não declara `timeout_seconds` (default 10s, `lambda-function/variables.tf`), insuficiente para o `timeout_seconds=25` exigido pelo round-3 Achado #6 sem afetar toda rota `/items*` existente — optado por **Lambda dedicada `export_handler`** (`infra/main.tf`, `timeout_seconds=25`, mesma política `tenant_facing_read_write_policy_json` de `items_handler`, sem `gsi4_read_policy_json` — GSI1 não é restrito) + rota própria `GET /items/export` no módulo `api-gateway` (não `bff-api-gateway`, mesmo precedente de D-124 — leitura/escrita de item já roteia por `api-gateway`), integração/permissão/assert de rota dedicados no MESMO commit (classe de bug D-117/D-120), coexistindo com `GET /items/{itemId}` pelo mesmo precedente de segmento literal que `GET /items/dashboard` já usa. **G-V3**: verificado por teste dedicado que defeataria uma implementação mais fraca — (1) `serializeCsvField` prova que mitigar-depois-de-quotar produziria CSV sintaticamente quebrado (comentário no teste mostra a saída incorreta), a implementação real mitiga primeiro; (2) teste com `ExpirationStore` stub prova que `queryGsi1` pedido com `limit: 2001` retornando exatamente 2001 linhas de `ACTIVE` sozinho dispara o `ValidationError` com `statusWhereExceeded: "ACTIVE"` — prova o `budget+1` na fronteira exata; (3) teste de `buildExportCsv` com descrição pesada em vírgulas (que quase dobra de tamanho após quoting) prova que o guard mede bytes PÓS-serialização, não os brutos; (4) teste RBAC prova `MEMBER`/`VIEWER` negados e `ADMIN`/`OWNER` permitidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos (as 2 falhas de `npm test`/`system-mutation-allowlist.test.ts` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — mesmas `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes de `scripts/reset-dev-data.ts` já documentadas em D-125, sem relação com esta mudança); backend 1277 testes / 139 arquivos (3 novos: `csv-export-writer.test.ts` 16 testes, `export-handler.test.ts` 5 testes, +7 testes novos em `expiration-service.test.ts`), 1275 passando (2 falhas pré-existentes não relacionadas); `terraform validate`/`terraform test` limpos na raiz (`AWS_PROFILE=claude-dev`) e no módulo `api-gateway` (1/1 run, 3 asserts novos); zero regressão real. Nenhuma operação AWS destrutiva — só `validate`/`test`/`build`. Escopo respeitado integralmente: só `ExpirationItem`, sem gate de billing (M12 bloqueado por D-052), `TrackedSubject`/documentos fora de escopo (v2 futura). | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-123, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124/D-125) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo real de ponta a ponta (RBAC → orçamento GSI1 → guard de bytes → serialização CSV → resposta HTTP → Lambda dedicada → rota API Gateway); nunca antes tinha chamador HTTP real | `src/modules/identity/domain/authorization.ts`, `src/modules/expiration/application/expiration-service.ts`, `src/modules/expiration/http/export-handler.ts`, `src/shared/csv/csv-export-writer.ts`, `src/runtime/aws/http-adapter.ts`, `src/runtime/aws/handlers/export-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `infra/modules/api-gateway/{main,variables}.tf`, `infra/modules/api-gateway/tests/api_gateway.tftest.hcl`, `test/unit/expiration/{expiration-service,export-handler}.test.ts`, `test/unit/shared/csv-export-writer.test.ts` | 2026-08-31 |
docs/architecture/decisions-log.md:147:| D-127 | Estratégia de quarentena/recuperação antes de exclusão física irreversível (cross-cutting, motivada por W3-07/`CloseOrganizationService` mas desenhada para qualquer feature futura da mesma classe, ex. armazenamento de arquivos ainda não escopado) + priorização das 7 classes `retentionClass` de `privacy-lgpd.md` §4 ainda sem purga física real dentro de um tenant `ACTIVE` — protocolo Claude↔Codex completo, **11 rodadas** (Rodada 1: 5,5/10 — 11 achados reais, cancelamento inalcançável pelo `RequestContext` normal, sem coordenação `StopExecution`, nome de execução fixo quebra reabertura, LGPD com erro semântico vencimento≠exclusão; Rodadas 2-9 fecharam achados sucessivos incluindo 2 erros factuais próprios corrigidos ao vivo — `src/shared/ids.ts` inexistente (real: `src/runtime/aws/ids.ts`), `RecurrenceOccurrence` inexistente (real: `ReminderOccurrence`) — e um padrão real de 3 rodadas seguidas achando mais um `CANCELLED`-writer manualmente enumerado (3→4→5 sites); Rodada 10 trocou enumeração manual por um helper central (`cancelOccurrenceUpdate()`), Codex apontou que um helper sozinho não é garantia mecânica; Rodada 11 fechou com regra ESLint (`no-restricted-syntax`, mesma classe de enforcement de `no-console`/`AGENTS.md` §7) tornando o bypass um erro de `npm run lint`, não uma convenção — Claude 9,4/Codex 9,3, gate atingido). **Pesquisa externa (SIM)**: GitHub (repositório deletado restaurável 90 dias, `docs.github.com`, 2026-08-31), AWS (conta fechada reabrível 90 dias, `docs.aws.amazon.com/accounts`, 2026-08-31), Google Workspace (usuário deletado restaurável 20-25 dias, `workspace.google.com`, 2026-08-31), Slack (arquivo sem compartilhamento, grace period 30 dias, `slack.com/help`, 2026-08-31) — convergência real 20-90 dias, nenhuma fonte usa quarentena medida em segundos/minutos para dado persistido. **Mecanismo aprovado**: novo estado `HELD_FOR_RECOVERY` inserido entre `ACTIVE` e `DELETING` no grafo de `TenantLifecycleRecord` (`ACTIVE ⇄ HELD_FOR_RECOVERY → DELETING → QUIESCING → PURGING → VERIFIED → DELETED`, única aresta nova de retorno a `ACTIVE`), duração **30 dias** (piso defensável mais curto da faixa convergente, não "ponto médio" — erro de aritmética da Rodada 1 corrigido explicitamente na Rodada 2), reusa o Step Functions já existente do W3-07 (D-124) sem novo serviço AWS — `recoveryDeadline`/`closureAttemptId` persistidos na mesma transação da transição (nome de execução `${tenantId}-${closureAttemptId}`, ARN determinístico derivado, nunca lido de volta, fechando a race de persistência de ARN); ação real de cancelamento existe (`CancelOrganizationClosureService`, caminho de resolução de identidade dedicado via `IdentityMapping` — nunca `RequestContextResolver` normal, que exige `ACTIVE` — e primitive de autorização dedicado `authorizeCancelClosure()`/`ClosureCancellationDeniedError`, não o union `Action` genérico); `StopExecution` sempre roda antes de qualquer restauração de `ACTIVE`; sweeper diário (extensão do `tenant-purge-sweeper` de D-124) reconcilia falhas parciais só sob conjunção estrita (execução `ABORTED` + `closureAttemptId` correspondente + registro ainda `HELD_FOR_RECOVERY` — qualquer outra combinação alarma, nunca restaura otimisticamente); legal hold usa `HELD_FOR_RECOVERY→HELD` (não `BLOCKED`, reservado a falha operacional real), retomada preserva o `recoveryDeadline` original (hold suspende, nunca reseta o relógio). **Priorização LGPD** (2 pistas): executável — `CORE_USER_DATA` (`ExpirationItem`/policy soft-deletados `deletedAt+30d`, mais `ReminderOccurrence` como worker irmão independente via `purgeAfterTtl` TTL nativo do DynamoDB, mesmo mecanismo de `InvitationTokenPointer`/`invitation-token.ts`, nunca dependente do pai sobreviver) → `DELIVERY_RECORD` (180d) → `SECURITY_AUDIT` (365d) → `QUOTA_TELEMETRY` (janela+30d) → `ACCOUNT_ACTIVE` não-fechamento (`Invitation`→`Membership`→`Channel`) → `TRANSIENT` remanescente (`WebhookInbox`/`UploadSlot`); bloqueada — `LEGAL_EVIDENCE` (trava jurídica/KMS/Object Lock já `APPROVED`, fora da ordem linear); já resolvidas, zero backlog — `USER_DOCUMENT`, `EXTRACTION_TRANSIENT`, `InvitationTokenPointer`/`TRANSIENT`. **Escopo desta rodada: design-only**, deliberado e justificado (Rodada 1 subestimou como "1 estado+1 Wait+1 service", Rodada 2 reclassificou corretamente após o tamanho real aparecer — comparável a uma wave dedicada, não a D-125) — implementação real (novo Lambda `ReadTenantLifecycleRecord`, extensão do sweeper, migração dos 5 `CANCELLED`-writers para `cancelOccurrenceUpdate()`, regra ESLint, 7 workers de purga LGPD um por vez na ordem acima) fica para sessão(ões) futura(s) dedicada(s), mesmo padrão D-121→D-124. | Type 1 (nível 5-6, política de retenção/exclusão de dado, LGPD-relevante — `change-risk-scale.md`) | 9,4 | 9,3 | **APPROVED** (design, 11 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/quarantine-retention-scoping/` (round-1 a round-11, proposta+self-grade+critique Codex cada) | 2026-08-31 |
src\shared\tenant-lifecycle\tenant-purge-execution-starter.ts:2: * W3-07 purge orchestrator (D-124, implementing D-121's approved design).
infra\tests\stack.tftest.hcl:319:    error_message = "ReminderDispatchQueue's redrive policy must have maxReceiveCount=5"
infra\tests\stack.tftest.hcl:692:# --- W3-07 tenant purge orchestrator (D-124, implementing D-121) ---------------------------
src\shared\errors\app-error.ts:5: * (per #6.2) that every consumer "classifica erro como retryable ou terminal" and
src\shared\errors\app-error.ts:32:  retryable: boolean;
src\shared\errors\app-error.ts:41: * HONEST SCOPE of `retryable` (logging-observability-standard.md audit finding, 2026-08-29,
src\shared\errors\app-error.ts:43: * every consumer to "classifica erro como retryable ou terminal", and this field IS that
src\shared\errors\app-error.ts:47: * letting SQS's own `maxReceiveCount`+DLQ redelivery mechanics be the actual retry/terminal
src\shared\errors\app-error.ts:48: * decision - `retryable` only reaches a log line (diagnosis), never an `if`. Separately, the M7
src\shared\errors\app-error.ts:52: * consumers SHOULD start branching on `retryable` (e.g. route a known-terminal error straight
src\shared\errors\app-error.ts:53: * to DLQ instead of spending `maxReceiveCount` retries on it first) is a real, undecided
src\shared\errors\app-error.ts:63:  readonly retryable: boolean;
src\shared\errors\app-error.ts:71:    this.retryable = options.retryable;
src\shared\errors\app-error.ts:84:      retryable: this.retryable,
src\shared\errors\app-error.ts:92:    super({ code: "VALIDATION_FAILED", category: "VALIDATION", message, retryable: false, details });
src\shared\errors\app-error.ts:99:    super({ code: "AUTH_REQUIRED", category: "AUTH", message, retryable: false, details });
src\shared\errors\app-error.ts:106:    super({ code: "AUTHORIZATION_DENIED", category: "AUTHORIZATION", message, retryable: false, details });
src\shared\errors\app-error.ts:113:    super({ code: "NOT_FOUND", category: "NOT_FOUND", message, retryable: false, details });
src\shared\errors\app-error.ts:127:    super({ code: "INELIGIBLE_ASSIGNEE", category: "NOT_FOUND", message, retryable: false, details });
src\shared\errors\app-error.ts:135:    super({ code: "CONFLICT", category: "CONFLICT", message, retryable: false, details });
src\shared\errors\app-error.ts:142:    super({ code: "QUOTA_EXCEEDED", category: "QUOTA_EXCEEDED", message, retryable: false, details });
src\shared\errors\app-error.ts:147:/** Downstream/provider failure - `retryable` defaults to true (throttling, timeout, 5xx are
src\shared\errors\app-error.ts:153:  constructor(message: string, details?: Record<string, unknown>, cause?: unknown, retryable = true) {
src\shared\errors\app-error.ts:154:    super({ code: "DEPENDENCY_UNAVAILABLE", category: "DEPENDENCY_UNAVAILABLE", message, retryable, details, cause });
src\shared\errors\app-error.ts:161:    super({ code: "INTERNAL", category: "INTERNAL", message, retryable: false, details, cause });
src\shared\errors\app-error.ts:175: * Textract input format. Never retryable — the same bytes will classify the same way again. */
src\shared\errors\app-error.ts:178:    super({ code: "UnsupportedDocumentType", category: "VALIDATION", message, retryable: false, details });
src\shared\errors\app-error.ts:187:    super({ code: "OcrDisabled", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:196:    super({ code: "TextractUnsupportedDocument", category: "VALIDATION", message, retryable: false, details });
src\shared\errors\app-error.ts:206:    super({ code: "TextractPartialFailure", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:218:    super({ code: "TextractJobPersistenceFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:233:    super({ code: "DeterministicParserFailed", category: "INTERNAL", message, retryable: false, details });
src\shared\errors\app-error.ts:245:    super({ code: "AiExtractionDisabled", category: "VALIDATION", message, retryable: false, details });
src\shared\errors\app-error.ts:257:    super({ code: "BedrockExtractionFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:269:    super({ code: "ExtractionCommitFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: true, details, cause });
src\shared\errors\app-error.ts:275: * the operation violates a business invariant — HTTP 422. `retryable: false` always: retrying
src\shared\errors\app-error.ts:279:    super({ code: "BUSINESS_RULE_VIOLATION", category: "BUSINESS_RULE", message, retryable: false, details });
src\shared\errors\app-error.ts:287: * this codebase never expects once bootstrap always creates one). `retryable: false`: the
src\shared\errors\app-error.ts:291:    super({ code: "TENANT_NOT_ACTIVE", category: "CONFLICT", message, retryable: false, details });
src\shared\errors\app-error.ts:311:      retryable: false,
src\shared\errors\app-error.ts:331:      retryable: false,
src\shared\errors\app-error.ts:343: * D-086 §8, never exercised by a real writer until this wave. `retryable: false`: the caller
src\shared\errors\app-error.ts:347:    super({ code: "LAST_OWNER", category: "BUSINESS_RULE", message, retryable: false, details });
src\shared\errors\app-error.ts:360:    super({ code: "OWNER_TIER_CHANGE_REQUIRES_OWNER", category: "AUTHORIZATION", message, retryable: false, details });
src\shared\errors\app-error.ts:374:    super({ code: "INVITATION_TOKEN_UNAVAILABLE", category: "CONFLICT", message, retryable: false, details });
src\shared\errors\app-error.ts:391:    super({ code: "ORGANIZATION_UNAVAILABLE", category: "AUTHORIZATION", message, retryable: false, details });
src\shared\errors\app-error.ts:405:    super({ code: "ORGANIZATION_SELECTION_REQUIRED", category: "CONFLICT", message, retryable: false, details });
src\shared\errors\app-error.ts:410:/** W3-07 purge orchestrator (D-124, implementing D-121). `CloseOrganizationService` refuses to
src\shared\errors\app-error.ts:416: * it just is not in a closable state). `retryable: false`: no amount of retrying moves the record
src\shared\errors\app-error.ts:422:    super({ code: "ORGANIZATION_CLOSURE_UNAVAILABLE", category: "CONFLICT", message, retryable: false, details });
src\shared\errors\app-error.ts:427:/** D-122/D-125 (Responsibility Reassignment on Member Removal): thrown by
src\shared\errors\app-error.ts:437: * true count. `retryable: false`: the caller must reassign the flagged items first (via the
src\shared\errors\app-error.ts:449:      retryable: false,
src\shared\errors\app-error.ts:467:/** Classifies any thrown value's `retryable` flag (defaults to `false`/terminal for an
src\shared\errors\app-error.ts:473:export function isRetryable(err: unknown): boolean {
src\shared\errors\app-error.ts:474:  return toAppError(err).retryable;
src\modules\identity\domain\authorization.ts:89:  // W3-07 purge orchestrator (D-124, implementing D-121): closing the organization starts the
src\modules\identity\domain\authorization.ts:125: * `organization:update-settings` (B2B-10), `organization:close` (W3-07/D-124). */
infra\state-machines\tenant-purge.asl.json:2:  "Comment": "W3-07 tenant purge orchestrator (D-124, implementing the design APPROVED as D-121, docs/architecture/reviews/w3-07-purge-orchestrator-scoping/). Drives the real ACTIVE->DELETING->QUIESCING->PURGING->VERIFIED->DELETED lifecycle for one tenant. Started by CloseOrganizationService (and repaired by the sweeper) with input {tenantId} and execution name = tenantId, which is the whole idempotency mechanism. Like ../modules/extraction-workflow/'s document-extraction.asl.json, this checked-in file is a TEMPLATE: infra/modules/tenant-purge-workflow/main.tf substitutes the two Lambda FunctionName placeholders and the retry-limit placeholder via replace() at plan/apply time, so the tokens below are never what actually reaches AWS - keeping the file human-readable for review is the same tradeoff that module already makes. The module's own tftest asserts every placeholder was substituted, which is why this comment deliberately does not quote them verbatim.",
src\workers\upload-finalizer\finalizer.ts:42: * retryable race, not real staleness. Giving up there permanently dropped this worker's
src\shared\tenant-lifecycle\system-mutation.ts:345: * never an expected/retryable outcome.
infra\outputs.tf:128:  description = "Plan-time-known name of the W3-07 tenant-purge state machine (D-124). Must match local.tenant_purge_state_machine_arn's name segment - that deterministic ARN is what the memberships handler (CloseOrganizationService) and the sweeper both receive as an env var and call StartExecution against."
src\shared\tenant-lifecycle\sfn-tenant-purge-execution-starter.ts:2: * Real AWS Step Functions adapter for `TenantPurgeExecutionStarter` (W3-07/D-124). Structural twin
src\workers\tenant-purge\tenant-purge-sweep.ts:2: * W3-07 purge orchestrator (D-124, implementing D-121's approved design — Rodada 2 Fix 5 plus
src\modules\subject\application\guest-submission-service.ts:31:    super({ code: "GUEST_TOKEN_INVALID", category: "AUTH", message: "Invalid or expired link.", retryable: false });
src\shared\dynamodb\tenant-purge-scan.ts:127: * W3-07 purge orchestrator (D-124): the `SystemMutationStore` the purge and the lifecycle
src\shared\dynamodb\tenant-purge-scan.ts:152: * W3-07 purge orchestrator (D-124): consistent point read of one tenant's `TenantLifecycleRecord`.
src\shared\dynamodb\tenant-purge-scan.ts:173: * W3-07 purge orchestrator (D-124): discovery source for the recurring sweeper — every
src\shared\dynamodb\sdk-errors.ts:57:  // fallback) - treat as a retryable dependency failure rather than swallowing detail,
src\runtime\aws\handlers\email-delivery-handler.ts:3: * reminder-dispatch-handler.ts (schema-invalid payload is a poison message, not retryable). */
src\runtime\aws\handlers\email-delivery-handler.ts:41:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\email-delivery-handler.ts:57:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\email-delivery-handler.ts:89:            // errorCode/retryable logged for consistency with every other handler's pattern
src\runtime\aws\handlers\email-delivery-handler.ts:90:            // (e.g. textract-task-handler.ts) - see app-error.ts's isRetryable() doc comment
src\runtime\aws\handlers\email-delivery-handler.ts:91:            // for the honest, current scope of what `retryable` actually drives today (this
src\runtime\aws\handlers\email-delivery-handler.ts:93:            // batch item failure regardless of the value - SQS's own maxReceiveCount+DLQ is
src\runtime\aws\handlers\email-delivery-handler.ts:97:            logger.error("email-delivery failed", { messageId: record.messageId, errorCode: appErr.code, retryable: appErr.retryable });
src\runtime\aws\handlers\email-delivery-handler.ts:98:            batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\email-delivery-handler.ts:106:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\email-delivery-handler.ts:111:  return { batchItemFailures };
src\workers\tenant-purge\purge-result-envelope.ts:2: * W3-07 purge orchestrator (D-124, implementing D-121's approved design — Rodada 3 Fix 6).
src\workers\tenant-purge\lifecycle-transition.ts:2: * W3-07 purge orchestrator (D-124, implementing D-121's approved design — Rodada 2 Fix 3).
src\workers\tenant-purge\lifecycle-transition.ts:55: * (something else moved the tenant), never a retryable race. Surfaces to Step Functions as this
src\runtime\aws\handlers\bff-handler.ts:89:  return toApiGatewayResult({ statusCode: 404, body: { code: "NOT_FOUND", category: "NOT_FOUND", message: "Unknown BFF route.", retryable: false } });
src\runtime\aws\handlers\document-chasing-dispatch-handler.ts:4: * poison message, not retryable). */
src\runtime\aws\handlers\document-chasing-dispatch-handler.ts:30:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\document-chasing-dispatch-handler.ts:40:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\document-chasing-dispatch-handler.ts:50:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\document-chasing-dispatch-handler.ts:55:  return { batchItemFailures };
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:20:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:45:            batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:49:          batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:55:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:59:  return batchItemFailures;
src\runtime\aws\composition\tenant-purge.ts:2: * Composition root for the W3-07 tenant purge orchestrator (D-124, implementing D-121). This is
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:35:  const batchItemFailures = await processStreamRecords(deps, logger, event.Records);
src\runtime\aws\handlers\dispatch-outbox-relay-handler.ts:36:  return { batchItemFailures };
src\runtime\aws\composition\organization.ts:33:/** D-122/D-125 (Responsibility Reassignment on Member Removal): a THIN adapter directly against
src\runtime\aws\composition\organization.ts:87:  /** W3-07 (D-124): the tenant-purge state machine ARN. Optional so every existing caller and test
src\runtime\aws\handlers\upload-finalizer-handler.ts:38:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\upload-finalizer-handler.ts:47:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\upload-finalizer-handler.ts:70:          // is not a retryable failure - it can never resolve on retry. Log and drop, never DLQ-loop.
src\runtime\aws\handlers\upload-finalizer-handler.ts:98:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\upload-finalizer-handler.ts:103:  return { batchItemFailures };
infra\modules\tenant-purge-workflow\tests\tenant_purge_workflow.tftest.hcl:1:# W3-07 tenant purge orchestrator (D-124, implementing D-121). Same shape and same `apply`
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:31:  const batchItemFailures = await processStreamRecords(deps, logger, event.Records);
src\runtime\aws\handlers\notification-email-outbox-relay-handler.ts:32:  return { batchItemFailures };
infra\modules\tenant-purge-workflow\main.tf:1:# W3-07 tenant purge orchestrator (D-124, implementing the design APPROVED as D-121).
src\runtime\aws\handlers\textract-task-handler.ts:11: *   Textract's own JSON string carrying `JobId`). Same `batchItemFailures` pattern as
src\runtime\aws\handlers\textract-task-handler.ts:101:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\textract-task-handler.ts:121:          return; // not retryable - will never parse differently.
src\runtime\aws\handlers\textract-task-handler.ts:132:        logger.error("textract-task COMPLETE_OCR failed", { messageId: record.messageId, errorCode: appErr.code, retryable: appErr.retryable });
src\runtime\aws\handlers\textract-task-handler.ts:133:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\textract-task-handler.ts:138:  return { batchItemFailures };
src\runtime\aws\handlers\memberships-handler.ts:40:// W3-07 (D-124): required for POST /organizations/close. Absent means the closure route returns a
src\runtime\aws\handlers\malware-result-handler.ts:39:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\malware-result-handler.ts:48:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\malware-result-handler.ts:55:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\malware-result-handler.ts:106:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\malware-result-handler.ts:111:  return { batchItemFailures };
src\runtime\aws\handlers\notification-router-handler.ts:3: * only genuinely retryable outcomes (RouterWorkflowOutcome "RETRY") are reported back as
src\runtime\aws\handlers\notification-router-handler.ts:24:  batchItemFailures: { itemIdentifier: string }[],
src\runtime\aws\handlers\notification-router-handler.ts:37:        batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\notification-router-handler.ts:41:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\notification-router-handler.ts:47:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\notification-router-handler.ts:57:      await processRecord(record, item, batchItemFailures);
src\runtime\aws\handlers\notification-router-handler.ts:60:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\notification-router-handler.ts:64:  return { batchItemFailures };
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:17:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:41:            batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:45:          batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:50:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:54:  return batchItemFailures;
src\runtime\aws\handlers\tenant-purge-worker-handler.ts:2: * Real handler behind the RunPurge Task (W3-07/D-124). A deliberately thin wrapper around the
src\runtime\aws\handlers\tenant-purge-sweeper-handler.ts:1:/** Real handler for the tenant-purge sweeper (EventBridge Scheduler, W3-07/D-124). Two
src\runtime\aws\handlers\reminder-materialization-trigger-handler.ts:5: * maxReceiveCount+DLQ (not this handler) is what stops retries of a message that can never
src\runtime\aws\handlers\reminder-materialization-trigger-handler.ts:24:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\reminder-materialization-trigger-handler.ts:34:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\reminder-materialization-trigger-handler.ts:45:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\reminder-materialization-trigger-handler.ts:50:  return { batchItemFailures };
src\runtime\aws\handlers\import-parse-handler.ts:32:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\import-parse-handler.ts:41:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\import-parse-handler.ts:48:          // retryable, never DLQ-loop. Same fail-closed posture as upload-finalizer-handler.ts.
src\runtime\aws\handlers\import-parse-handler.ts:66:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\import-parse-handler.ts:71:  return { batchItemFailures };
src\runtime\aws\handlers\ses-callback-handler.ts:65:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\ses-callback-handler.ts:85:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\ses-callback-handler.ts:90:  return { batchItemFailures };
src\modules\organization\http\organization-lifecycle-handlers.ts:2: * HTTP handler for W3-07's organization closure (D-124, implementing D-121) — the single real
src\runtime\aws\handlers\tenant-lifecycle-transition-handler.ts:1:/** Real handler behind every transition Task of the tenant-purge state machine (W3-07/D-124,
infra\modules\sqs-worker-queue\variables.tf:3:# FIFO would add throughput limits without removing that need), maxReceiveCount=5 per
src\runtime\aws\handlers\extraction-starter-handler.ts:29:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\extraction-starter-handler.ts:38:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\extraction-starter-handler.ts:45:          // promotion copy) is not a retryable failure - it can never resolve on retry.
src\runtime\aws\handlers\extraction-starter-handler.ts:68:        // it's retryable, same batch-item-failure path as any other transient error.
src\runtime\aws\handlers\extraction-starter-handler.ts:74:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\extraction-starter-handler.ts:79:  return { batchItemFailures };
src\runtime\aws\handlers\reminder-dispatch-handler.ts:27:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\reminder-dispatch-handler.ts:46:          // Schema-invalid payload is treated as a poison message, not a retryable failure -
src\runtime\aws\handlers\reminder-dispatch-handler.ts:48:          // SQS's own maxReceiveCount+DLQ (not this handler) is what stops the retries
src\runtime\aws\handlers\reminder-dispatch-handler.ts:51:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\reminder-dispatch-handler.ts:63:        // errorCode/retryable logged for consistency with every other handler's pattern (e.g.
src\runtime\aws\handlers\reminder-dispatch-handler.ts:64:        // textract-task-handler.ts) - see app-error.ts's isRetryable() doc comment for the
src\runtime\aws\handlers\reminder-dispatch-handler.ts:65:        // honest, current scope of what `retryable` actually drives today (this handler still
src\runtime\aws\handlers\reminder-dispatch-handler.ts:67:        // maxReceiveCount+DLQ is the real terminal decision, not this field; logged here for
src\runtime\aws\handlers\reminder-dispatch-handler.ts:70:        logger.error("reminder-dispatch failed", { messageId: record.messageId, errorCode: appErr.code, retryable: appErr.retryable });
src\runtime\aws\handlers\reminder-dispatch-handler.ts:71:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\reminder-dispatch-handler.ts:76:  return { batchItemFailures };
src\runtime\aws\handlers\import-commit-handler.ts:3: * reminder-dispatch-handler.ts (schema-invalid payload is a poison message, not retryable). */
src\runtime\aws\handlers\import-commit-handler.ts:40:  const batchItemFailures: { itemIdentifier: string }[] = [];
src\runtime\aws\handlers\import-commit-handler.ts:50:          batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\import-commit-handler.ts:60:        batchItemFailures.push({ itemIdentifier: record.messageId });
src\runtime\aws\handlers\import-commit-handler.ts:65:  return { batchItemFailures };
infra\main.tf:198:    # W3-07 (D-124): POST /organizations/close. Without this env var the composition root never
infra\main.tf:210:    # W3-07 (D-124): states:StartExecution on the tenant-purge state machine ARN only.
infra\main.tf:2471:# W3-07 tenant purge orchestrator (D-124, implementing the design APPROVED as D-121).
infra\modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:20:    condition     = strcontains(aws_sqs_queue.this.redrive_policy, "\"maxReceiveCount\":5")
infra\modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:21:    error_message = "Main queue's redrive policy must set maxReceiveCount to 5"
infra\modules\sqs-worker-queue\main.tf:6:# (maxReceiveCount=5, visibility timeout = 6x consumer timeout, long polling, DLQ age alarm
infra\modules\sqs-worker-queue\main.tf:26:    maxReceiveCount     = 5
infra\modules\api-gateway\main.tf:322:    # W3-07 (D-124): CloseOrganizationService's trigger - starts the real tenant purge. Same
src\workers\malware-result\result-processor.ts:38: * the evidence at the same moment - retryable, not real staleness. Fixed with the same bounded
infra\modules\api-gateway\tests\api_gateway.tftest.hcl:325:  # W3-07 (D-124): the organization-closure route. This assertion exists specifically because
src\modules\bff\application\bff-auth-service.ts:294:        // silently treated as a definitive failure, but also never marked retryable - Cognito
src\modules\bff\application\bff-auth-service.ts:296:        // an already-consumed credential. `retryable: false` here matches the frontend's own
infra\modules\reminder-observability\main.tf:44:# alarm: this one catches slow/stuck consumption BEFORE messages exhaust maxReceiveCount and
src\modules\organization\application\remove-membership.ts:8: * D-122/D-125 (Responsibility Reassignment on Member Removal): before the removal transaction, a
src\modules\bff\domain\proxy-allowlist.ts:79:  // W3-07 (D-124): the organization-closure trigger. Must be allowlisted here AND routed in
src\modules\extraction\application\start-extraction-run.ts:37: * observes CLEAN yet. Distinct from DOCUMENT_NOT_FOUND (never retryable - the document genuinely
src\modules\extraction\application\start-extraction-run.ts:38: * doesn't exist), this IS retryable: thrown, not returned, so the runtime handler's normal SQS
src\modules\document\domain\malware-scan-result.ts:6: * (retryable within the adapter, per the adapter's own retry/DLQ policy) and are never
src\modules\bff\domain\refresh-outcome.ts:13:  /** Timeout/5xx from Cognito - session stays intact, caller should surface a retryable
src\modules\bff\http\bff-handlers.ts:145:    return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
src\modules\bff\http\bff-handlers.ts:161:    return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
src\modules\bff\http\bff-handlers.ts:193:      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
src\modules\bff\http\bff-handlers.ts:240:      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
src\modules\bff\http\bff-handlers.ts:251:      return { statusCode: 403, body: { code: "ORGANIZATION_UNAVAILABLE", category: "AUTHORIZATION", message: "This organization is not available in your current context.", retryable: false } };
src\modules\bff\http\bff-handlers.ts:276:      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
src\modules\bff\http\bff-handlers.ts:305:      return { statusCode: 403, body: { code: "CSRF_CHECK_FAILED", category: "AUTHORIZATION", message: "CSRF check failed.", retryable: false } };
src\modules\organization\application\leave-organization.ts:10: * D-122/D-125 (Responsibility Reassignment on Member Removal): same best-effort, non-atomic
src\modules\organization\application\close-organization.ts:2: * CloseOrganizationService — W3-07 purge orchestrator (D-124, implementing D-121's approved
src\modules\notification\providers\ses-email-adapter.ts:9: * treat as retryable or terminal. Anything that looks like a network/timeout/unknown error
# Pesquisa Externa antes de DecisÃµes Type 1 â€” Expiration Tracker

**Status: `APPROVED` via protocolo Claudeâ†”Codex (`AGENTS.md` Â§4), 3 rodadas, nota cega cada rodada: Rodada 1 Claude 7,9/Codex 7,7 â†’ Rodada 2 Claude 8,8/Codex 8,7 â†’ Rodada 3 Claude 9,2/Codex 9,2 (fechamento, ambos â‰¥9,0, sem arredondar).** Registrado como `docs/engineering/decisions-log.md` E-014. EvidÃªncia completa das 3 rodadas: `docs/engineering/reviews/research-protocol/`.

Pedido do Marcelo (2026-08-30, no fechamento da Wave B2B-5): formalizar quando pesquisa de padrÃµes/mercado deve informar uma decisÃ£o de arquitetura, do mesmo jeito que `definition-of-done.md` (E-012) formalizou o gate por item de todo list â€” inclusive submetendo a prÃ³pria formalizaÃ§Ã£o ao protocolo Claudeâ†”Codex atÃ© convergÃªncia, mesmo nÃ£o sendo decisÃ£o de arquitetura em si (mesmo precedente de E-012 Â§"Pedido explÃ­cito do Marcelo").

## Regra central

Uma decisÃ£o nÃ­vel 5-6 de `docs/engineering/change-risk-scale.md` (Type 1, jÃ¡ sob o protocolo obrigatÃ³rio de `AGENTS.md` Â§4) que **define um padrÃ£o ou contrato que sistemas fora deste projeto jÃ¡ resolveram de forma amplamente estabelecida** exige, antes da Rodada 1 do protocolo, uma declaraÃ§Ã£o explÃ­cita de pesquisa externa â€” nunca uma omissÃ£o silenciosa.

Este documento nÃ£o cria uma segunda rÃ©gua de risco: ele se acopla Ã  rÃ©gua que jÃ¡ existe (`change-risk-scale.md`) e ao ponto de entrada que jÃ¡ Ã© obrigatÃ³rio (a Rodada 1 do protocolo Claudeâ†”Codex, `AGENTS.md` Â§4) â€” nÃ£o Ã© uma disciplina nova e independente para lembrar, Ã© um campo obrigatÃ³rio dentro de uma disciplina que jÃ¡ Ã© enforced.

## Quando pesquisa Ã© exigida (critÃ©rio decidÃ­vel, nÃ£o julgamento livre)

Pesquisa externa Ã© exigida quando **ambas** as condiÃ§Ãµes valem:

1. A decisÃ£o Ã© nÃ­vel 5-6 de `change-risk-scale.md` (jÃ¡ aciona o protocolo de qualquer forma).
2. A decisÃ£o define, no todo ou em parte, um **padrÃ£o que existe fora deste projeto** â€” um problema que produtos/sistemas estabelecidos jÃ¡ resolveram de forma conhecida (ex.: modelo de permissÃµes RBAC, fluxo de convite/invite com prevenÃ§Ã£o de account-takeover, sessÃ£o/contexto multi-tenant, polÃ­tica de expiraÃ§Ã£o de token, fronteira de sessÃ£o OAuth/cookie/CSRF) â€” e nÃ£o apenas "como ESTE projeto especÃ­fico organiza seus prÃ³prios dados" (ex.: layout de chave DynamoDB, decisÃ£o de qual mÃ³dulo possui um valor default, nome de campo interno).

Regra prÃ¡tica para a linha divisÃ³ria: se a resposta certa depende de "o que a maioria dos produtos/padrÃµes estabelecidos faz aqui" (inclui RFCs/normas de seguranÃ§a, nÃ£o sÃ³ produtos comerciais), pesquisa Ã© exigida. Se a resposta certa depende sÃ³ de "como isso se encaixa no nosso prÃ³prio modelo de dados/mÃ³dulos jÃ¡ existentes", pesquisa nÃ£o ajuda e nÃ£o Ã© exigida â€” mesma lÃ³gica de proporcionalidade de `principles.md` #1, aplicada a research em vez de a mecanismo.

**DecisÃ£o composta (achado real da Rodada 1 do Codex, testado contra D-086/D-053-D-054)**: uma decisÃ£o Type 1 raramente Ã© 100% padrÃ£o-externo ou 100% interna â€” o physical model do Multi-User B2B (D-086) mistura layout de PK/SK (interno) com convite/last-OWNER/revogaÃ§Ã£o de Membership (padrÃ£o SaaS estabelecido); o Full BFF (D-053/D-054) Ã© majoritariamente padrÃ£o externo (OAuth/PKCE/cookie de sessÃ£o â€” RFCs e guias de seguranÃ§a de browser sÃ£o fonte direta do design), mas ainda toca decisÃµes internas (nome de tabela, TTL especÃ­fico). ForÃ§ar uma classificaÃ§Ã£o binÃ¡ria da decisÃ£o INTEIRA erra nos dois casos. A declaraÃ§Ã£o aceita um terceiro valor, `SIM PARCIAL`, com o escopo explÃ­cito de quais sub-decisÃµes sÃ£o informadas por pesquisa e quais sÃ£o puramente internas â€” o checklist da prÃ³xima seÃ§Ã£o cobre sÃ³ a parte `SIM`/`SIM PARCIAL`.

Pesquisa **factual isolada de implementaÃ§Ã£o** (qual Ã© a assinatura atual de uma chamada do AWS SDK, versÃ£o de uma lib) nunca aciona este documento. Mas pesquisa factual que **fundamenta o prÃ³prio padrÃ£o/postura de seguranÃ§a** de uma decisÃ£o Type 1 (ex.: RFC 6749/7636 informando o design de PKCE do Full BFF) conta como a pesquisa que este documento exige, mesmo vindo de fontes "tÃ©cnicas" â€” a distinÃ§Ã£o nÃ£o Ã© "a fonte parece uma RFC ou um blog de produto", Ã© "essa pesquisa decide um padrÃ£o, ou sÃ³ implementa um jÃ¡ decidido".

## DeclaraÃ§Ã£o obrigatÃ³ria na Rodada 1 (nunca omissÃ£o silenciosa)

Todo documento de proposta Rodada 1 de um protocolo Type 1 que se enquadre no critÃ©rio acima inclui, antes da seÃ§Ã£o de escopo, uma linha:

```text
Pesquisa externa considerada: SIM (fontes: <lista com data de acesso>)
                             | SIM PARCIAL (fontes: <lista>; escopo: <quais sub-decisÃµes
                               sÃ£o informadas por pesquisa, quais sÃ£o internas>)
                             | NÃƒO (motivo: <justificativa>)
```

`NÃƒO` Ã© uma resposta vÃ¡lida (ex.: "o padrÃ£o jÃ¡ estÃ¡ fechado internamente por uma decisÃ£o anterior, nÃ£o hÃ¡ ambiguidade que pesquisa resolveria") â€” o que nÃ£o Ã© vÃ¡lido Ã© a ausÃªncia da linha. Mesma disciplina do `DoD:` de `definition-of-done.md`: a linha em si Ã© a trilha auditÃ¡vel mÃ­nima, escrita no momento da proposta, nÃ£o reconstruÃ­da depois. Quando a resposta Ã© `SIM`/`SIM PARCIAL`, a mesma seÃ§Ã£o da proposta inclui logo em seguida o checklist de critÃ©rios de nota derivado da pesquisa (prÃ³xima seÃ§Ã£o), escopado sÃ³ Ã  parte informada por padrÃ£o externo â€” as duas coisas nascem juntas, nunca o checklist como um adendo tardio de uma rodada posterior.

**Pesquisa sem padrÃ£o convergente**: `SIM`/`SIM PARCIAL` nÃ£o pressupÃµe que o mercado concorda â€” Ã s vezes a pesquisa encontra abordagens legitimamente divergentes entre produtos estabelecidos. Nesse caso a declaraÃ§Ã£o registra isso explicitamente ("sem padrÃ£o convergente â€” fontes X/Y/Z discordam") e o checklist da prÃ³xima seÃ§Ã£o vira critÃ©rios de TRADE-OFF explÃ­citos (qual critÃ©rio favorece qual abordagem e por quÃª), nunca finge um consenso que a pesquisa nÃ£o encontrou.

## A pesquisa estabelece os critÃ©rios de nota da rodada â€” nunca julgamento livre depois

Achado do Marcelo (2026-08-30): a nota do protocolo Claudeâ†”Codex hoje Ã© dada por julgamento de cada lado, sem uma rÃ©gua comum declarada antes de avaliar â€” funciona quando a rodada Ã© sobre o prÃ³prio cÃ³digo/dados deste projeto (cada lado lÃª o mesmo diff), mas quando a decisÃ£o se apoia em padrÃ£o de mercado, julgamento livre reabre exatamente a subjetividade que a pesquisa deveria fechar.

Quando a declaraÃ§Ã£o Ã© `SIM`/`SIM PARCIAL`, a pesquisa nÃ£o produz sÃ³ fontes â€” produz um **checklist de critÃ©rios objetivos e pesados**, derivado do que ela encontrou como padrÃ£o estabelecido (ou, no caso de "sem padrÃ£o convergente", como critÃ©rios de trade-off explÃ­citos).

**Hierarquia com `joint-review-criteria.md` (achado real da Rodada 1 do Codex â€” a frase original "vira a rÃ©gua de nota da rodada inteira" contradizia a prÃ³pria declaraÃ§Ã£o de `joint-review-criteria.md` de ser "fonte Ãºnica dos critÃ©rios de avaliaÃ§Ã£o usados nas revisÃµes conjuntas")**: este checklist nÃ£o substitui nem compete com a rubrica fixa dos 9 eixos â€” Ã© uma **sub-rubrica subordinada**, especÃ­fica de UMA decisÃ£o, usada em conjunto com o(s) eixo(s) de `joint-review-criteria.md` que jÃ¡ se aplicariam a essa rodada de qualquer forma (ex.: uma proposta de RBAC continua sendo avaliada tambÃ©m contra o eixo de SeguranÃ§a/GovernanÃ§a de Produto jÃ¡ existente lÃ¡ â€” o checklist de pesquisa Ã© adicional, nÃ£o um substituto). Descartado depois que a decisÃ£o fecha; nunca vira uma seÃ§Ã£o nova permanente de `joint-review-criteria.md` por conta prÃ³pria (isso continua exigindo o procedimento de convergÃªncia prÃ³prio daquele documento, "NÃ£o criar a tabela de critÃ©rios dentro do primeiro doc de auditoria do eixo novo").

Regras de aplicaÃ§Ã£o:

- Os critÃ©rios sÃ£o declarados **na mesma proposta Rodada 1**, ao lado da linha "Pesquisa externa considerada: SIM/SIM PARCIAL" â€” antes de qualquer nota, nunca depois (evita o critÃ©rio ser moldado para justificar uma nota jÃ¡ formada).
- Formato: lista pesada, cada critÃ©rio com peso e uma Ã¢ncora curta do que "atende"/"nÃ£o atende" significa â€” combinando os dois precedentes jÃ¡ formais deste projeto: a tabela de critÃ©rios pesados por eixo de `joint-review-criteria.md` (formato) e os gates binÃ¡rios com Ã¢ncora de `test-engineering-standard.md` (G-V1..G-V6 sÃ£o validade binÃ¡ria, nÃ£o peso â€” a Ã¢ncora "atende/nÃ£o atende" Ã© o que se reaproveita de lÃ¡, nÃ£o o peso).
- Ambos os lados (Claude e Codex) graduam a proposta **contra esses critÃ©rios explÃ­citos** em toda rodada subsequente, nÃ£o contra impressÃ£o geral â€” a justificativa da nota cita qual critÃ©rio foi ou nÃ£o atendido, nÃ£o uma sensaÃ§Ã£o de qualidade.

**ReconciliaÃ§Ã£o quando o Codex contesta o checklist (achado central da Rodada 1 â€” o maior buraco identificado)**: se a crÃ­tica do Codex contesta critÃ©rios/pesos do checklist original (nÃ£o sÃ³ a nota dada contra ele), a proposta **nÃ£o pode ser aprovada contra o checklist contestado naquela mesma rodada** â€” a rÃ©gua em si estÃ¡ em disputa, nÃ£o sÃ³ o design. Fluxo:

1. A rodada que contesta registra a nota como **duas partes separadas, sÃ³ no artefato daquela rodada** (nunca como coluna do `decisions-log.md` final, ver abaixo): nota da rÃ©gua (o checklist estÃ¡ certo?) e nota do design (dado o checklist como estÃ¡, o design atende?) â€” nunca uma nota Ãºnica misturando as duas enquanto a rÃ©gua estÃ¡ disputada.
2. A rodada seguinte traz um checklist reconciliado (incorporando a contestaÃ§Ã£o) OU uma rejeiÃ§Ã£o ponto a ponto justificada de cada item contestado â€” nunca silÃªncio sobre a contestaÃ§Ã£o.
3. **Gate explÃ­cito de rÃ©gua estÃ¡vel** (achado real da Rodada 2 do Codex â€” "sem contestaÃ§Ã£o aberta" sozinho permite uma rÃ©gua ruim sobreviver por exaustÃ£o textual, nÃ£o por concordÃ¢ncia real): a rÃ©gua sÃ³ Ã© considerada estÃ¡vel quando **ambos os lados registram nota da rÃ©gua â‰¥9,0**, exatamente o mesmo padrÃ£o numÃ©rico jÃ¡ usado para a nota do design em `AGENTS.md` Â§4 â€” nunca "parou de ser contestada" como critÃ©rio informal por si sÃ³. A nota do design sÃ³ conta para o fechamento â‰¥9,0/â‰¥9,0 da decisÃ£o na rodada em que a rÃ©gua jÃ¡ estÃ¡ estÃ¡vel, ou em rodadas posteriores â€” nunca antes (inclui a possibilidade de rÃ©gua e design atingirem â‰¥9,0 na mesma rodada, sem exigir uma rodada extra sÃ³ por formalidade).
4. Se o checklist muda por um achado real de uma rodada (nÃ£o por capricho), a mudanÃ§a Ã© registrada explicitamente como correÃ§Ã£o â€” versÃ£o nova do checklist citada na rodada, nunca uma rÃ©gua trocada silenciosamente para justificar uma nota diferente da anterior.
- O registro final da decisÃ£o (`decisions-log.md`) cita **sÃ³ a nota final normal de Claude/Codex** (a mesma coluna que toda decisÃ£o jÃ¡ registra) **e a versÃ£o final do checklist usado** â€” nunca as notas intermediÃ¡rias de "rÃ©gua vs. design" de rodadas em disputa, que ficam sÃ³ no artefato de review daquela rodada (`docs/architecture/reviews/` ou `docs/engineering/reviews/`, conforme o tipo de decisÃ£o). Isso evita introduzir um formato de coluna novo num log que jÃ¡ tem convenÃ§Ã£o prÃ³pria (D-084/D-066 separam timing de qualidade tÃ©cnica ou usam `APPROVED WITH CONDITIONS`, mas nunca um par numÃ©rico rÃ©gua/design na mesma linha).

## Verificabilidade â€” fonte + data + representatividade, nunca "pesquisei e X Ã© assim"

Toda afirmaÃ§Ã£o vinda de pesquisa externa citada numa proposta cita a fonte real (nome/URL) e a data em que foi consultada, exatamente como o projeto jÃ¡ exige para cÃ³digo ("verificar por leitura direta antes de escrever, nÃ£o repetir uma alegaÃ§Ã£o nÃ£o verificada" â€” achado real da sessÃ£o que produziu D-086â†’D-088). Isto importa mais aqui do que numa alegaÃ§Ã£o sobre o prÃ³prio cÃ³digo: uma pesquisa externa erra por resumo impreciso, desatualizaÃ§Ã£o, ou viÃ©s de vendor de um jeito que ler o cÃ³digo deste repositÃ³rio nÃ£o erra â€” o revisor (Codex, ou uma sessÃ£o futura) precisa conseguir checar a fonte, nÃ£o sÃ³ confiar na sÃ­ntese.

**Fonte + data Ã© necessÃ¡rio, nÃ£o suficiente (achado real da Rodada 1 do Codex)**: a declaraÃ§Ã£o tambÃ©m justifica por que o conjunto de fontes Ã© representativo para o TIPO de decisÃ£o, nÃ£o sÃ³ cita-las. Duas classes concretas:

- **DecisÃµes com postura de seguranÃ§a/identidade** (sessÃ£o, autenticaÃ§Ã£o, convite, revogaÃ§Ã£o): preferir normas/RFCs/OWASP quando existirem, nunca sÃ³ documentaÃ§Ã£o de um vendor especÃ­fico â€” se sÃ³ documentaÃ§Ã£o de vendor estiver disponÃ­vel, a declaraÃ§Ã£o registra isso explicitamente como limitaÃ§Ã£o, nÃ£o esconde.
- **DecisÃµes de padrÃ£o de produto SaaS** (RBAC, onboarding, UX de troca de organizaÃ§Ã£o): 2-3 exemplos de produtos estabelecidos nÃ£o bastam por si sÃ³ se forem todos do mesmo nicho/porte â€” a declaraÃ§Ã£o nomeia por que a amostra escolhida Ã© representativa (ex.: "GitHub/Notion/Linear/Slack cobrem desde ferramentas dev-first atÃ© produtividade geral, reduzindo viÃ©s de nicho Ãºnico"), nÃ£o apenas lista os nomes.

Nunca inventar ou adivinhar uma URL/fonte â€” mesma regra que jÃ¡ vale para toda comunicaÃ§Ã£o com o Marcelo. Uma afirmaÃ§Ã£o de pesquisa sem fonte verificÃ¡vel Ã© tratada como nÃ£o verificada, nÃ£o como fato.

## Papel do Codex na rodada

Quando a Rodada 1 declara `SIM`/`SIM PARCIAL`, a crÃ­tica do Codex avalia trÃªs coisas, nÃ£o sÃ³ o design resultante: (1) a fonte sustenta a afirmaÃ§Ã£o citada? (2) hÃ¡ um padrÃ£o estabelecido relevante que a proposta nÃ£o considerou? (3) os critÃ©rios de nota propostos sÃ£o os certos, pesados corretamente, e verificÃ¡veis â€” o Codex pode contestar o prÃ³prio checklist antes de aplicÃ¡-lo, nÃ£o sÃ³ a nota dada contra ele. Quando a Rodada 1 declara `NÃƒO`, o Codex tem posiÃ§Ã£o aberta para discordar da declaraÃ§Ã£o â€” "esta decisÃ£o deveria ter se apoiado em pesquisa e nÃ£o se apoiou" Ã© um achado de rodada vÃ¡lido, mesma categoria dos achados jÃ¡ vistos em rodadas reais deste projeto (ex.: `UserProfile`/gate de lifecycle da Wave B2B-5, achados encontrados por revisÃ£o antes de fechar).

## Fluxo completo (nenhum mecanismo novo, sÃ³ as peÃ§as que faltavam)

```text
DecisÃ£o nÃ­vel 5-6 identificada
  â†’ pesquisa externa (se o critÃ©rio acima se aplicar) â€” fontes+data+representatividade
    registradas; SIM / SIM PARCIAL (com escopo) / NÃƒO / "sem padrÃ£o convergente"
  â†’ checklist de critÃ©rios de nota derivado da pesquisa, pesado e Ã¢ncorado, subordinado
    ao(s) eixo(s) de joint-review-criteria.md jÃ¡ aplicÃ¡veis
  â†’ proposta Rodada 1 com a linha "Pesquisa externa considerada: ..." + o checklist
  â†’ Codex pode contestar o checklist em si (nÃ£o sÃ³ a nota) â€” se contestar, nota da rÃ©gua
    e nota do design sÃ£o registradas separadas atÃ© o checklist reconciliar
  â†’ protocolo Claudeâ†”Codex atÃ© convergÃªncia, cada rodada graduada contra o checklist
    (mÃ­nimo 3 rodadas, â‰¥9,0 dos dois, sem arredondar â€” AGENTS.md Â§4)
  â†’ sÃ³ entÃ£o implementaÃ§Ã£o
  â†’ fechamento gated por definition-of-done.md (E-012/E-013) por item de todo list
```

Nenhuma etapa aqui Ã© nova â€” pesquisa+declaraÃ§Ã£o+checklist de critÃ©rios (com reconciliaÃ§Ã£o quando disputado) Ã© a peÃ§a que este documento adiciona ao fluxo que jÃ¡ existe.

## O que isso NÃƒO Ã©

- NÃ£o Ã© uma segunda rÃ©gua de risco â€” usa `change-risk-scale.md` como estÃ¡.
- NÃ£o transforma toda pesquisa em gate formal â€” sÃ³ a que informa uma decisÃ£o jÃ¡ nÃ­vel 5-6 (proporcionalidade, `principles.md` #1).
- NÃ£o substitui o protocolo Claudeâ†”Codex â€” Ã© um campo obrigatÃ³rio dentro da Rodada 1 dele, nÃ£o um protocolo paralelo.
- NÃ£o exige um formato de citaÃ§Ã£o acadÃªmico â€” sÃ³ fonte + data + justificativa de representatividade, suficiente para um revisor checar.
- NÃ£o substitui `joint-review-criteria.md` â€” aquele continua "fonte Ãºnica dos critÃ©rios de avaliaÃ§Ã£o usados nas revisÃµes conjuntas" para os 9 eixos fixos; o checklist deste documento Ã© uma sub-rubrica subordinada, especÃ­fica de UMA decisÃ£o, descartada depois que ela fecha â€” nunca uma seÃ§Ã£o nova permanente daquele documento por conta prÃ³pria.
- NÃ£o fica invisÃ­vel por estar indexado sÃ³ depois de aprovado â€” `AGENTS.md` Â§4 e `docs/engineering/README.md` sÃ£o atualizados no fechamento (mesma sequÃªncia de E-012), nÃ£o uma lacuna a mais para lembrar durante o debate em si.

## Exemplos concretos deste projeto (ilustrativos, nÃ£o decisÃµes jÃ¡ tomadas)

### Exemplo 1 â€” `SIM` completo

Wave B2B-7 (RBAC) precisa definir o que `Membership.role = ADMIN` autoriza alÃ©m de `OWNER`/`MEMBER`/`VIEWER` â€” um modelo de permissÃµes Ã© exatamente o tipo de padrÃ£o que produtos estabelecidos (GitHub, Notion, Linear, Slack) jÃ¡ resolveram de forma conhecida. Isso se enquadra no critÃ©rio: nÃ­vel 5-6 (muda a matriz de autorizaÃ§Ã£o real, `authorization.ts`) + padrÃ£o externo estabelecido, decisÃ£o inteira informada por pesquisa (nÃ£o hÃ¡ parte puramente interna significativa aqui). A Rodada 1 dessa proposta declararia `SIM`, citando as fontes reais consultadas (justificando por que a amostra de produtos Ã© representativa, nÃ£o sÃ³ listando nomes), e proporia um checklist como (ilustrativo â€” os pesos/critÃ©rios reais nascem da pesquisa de verdade, nÃ£o deste exemplo):

```text
1. (peso 30%) Hierarquia de papÃ©is sem explosÃ£o combinatÃ³ria â€” ADMIN Ã© um superconjunto
   claro de MEMBER, nÃ£o uma lista paralela de permissÃµes redundante.
2. (peso 25%) Default deny â€” uma aÃ§Ã£o nÃ£o listada explicitamente nunca Ã© permitida por
   omissÃ£o.
3. (peso 25%) NÃ£o quebra `authorize()`/`ACTION_ROLES` existente sem necessidade â€” extensÃ£o,
   nÃ£o reescrita, salvo achado real que exija o contrÃ¡rio.
4. (peso 20%) MudanÃ§a de permissÃ£o Ã© auditÃ¡vel (quem, quando, de que role para que role).
```

Cada rodada subsequente do protocolo Claudeâ†”Codex graduaria a proposta contra estes 4 itens especificamente, nÃ£o contra uma impressÃ£o geral de qualidade.

### Exemplo 2 â€” `SIM PARCIAL` (decisÃ£o composta, retrospectivo contra D-086)

O physical model do Multi-User B2B (D-086) Ã© o caso real que expÃ´s a necessidade deste valor intermediÃ¡rio. Nome/formato do GSI4 (`MembershipByUser`) e layout de `PK`/`SK` de `Membership` sÃ£o decisÃµes internas â€” nenhuma pesquisa de mercado ajudaria a escolher entre `GSI4PK=USER#<userId>` e uma alternativa equivalente, isso depende sÃ³ do resto do schema jÃ¡ existente deste projeto. A postura de isolamento IAM do Ã­ndice (achado real da Rodada 2 do Codex: "least-privilege para Ã­ndice cross-tenant" Ã‰ um padrÃ£o de seguranÃ§a que poderia depender de pesquisa externa, nÃ£o Ã© automaticamente interno sÃ³ por tocar um Ã­ndice) jÃ¡ estava decidida internamente ANTES de D-086, pelo precedente jÃ¡ estabelecido de GSI3/GSI6 (`AGENTS.md` Â§7 â€” "nunca conceder acesso de leitura a um GSI restrito via polÃ­tica geral de tabela") â€” Ã© interna aqui por decisÃ£o anterior jÃ¡ convergida, nÃ£o porque IAM/postura de seguranÃ§a seja categoricamente interna. Mas o mecanismo de `ownerCount`/last-OWNER, a unicidade de convite pendente, e a semÃ¢ntica de revogaÃ§Ã£o de Membership sÃ£o padrÃµes que produtos SaaS multi-tenant estabelecidos jÃ¡ resolveram e nÃ£o tinham precedente interno prÃ©vio. A declaraÃ§Ã£o correta nesse caso seria `SIM PARCIAL (fontes: ...; escopo: mecanismo de last-OWNER e revogaÃ§Ã£o de Membership sÃ£o informados por padrÃ£o externo; layout de chave/GSI e isolamento IAM do Ã­ndice sÃ£o decisÃ£o interna â€” o Ãºltimo por precedente jÃ¡ convergido em GSI3/GSI6, nÃ£o por serem categoricamente internos)` â€” e o checklist de critÃ©rios cobriria sÃ³ a parte de last-OWNER/revogaÃ§Ã£o, nÃ£o o schema inteiro.

---
status: active
owner: engineering
authority: normative
---

# CritÃ©rios de revisÃ£o conjunta Claudeâ†”Codex, por eixo

Fonte Ãºnica dos critÃ©rios de avaliaÃ§Ã£o (nome, peso, definiÃ§Ã£o) usados nas revisÃµes conjuntas Claudeâ†”Codex (`AGENTS.md` Â§4). Achados/rodadas de uma execuÃ§Ã£o especÃ­fica do protocolo (ex. `docs/architecture/reviews/m3.5-runtime-design/`) registram evidÃªncia de uma revisÃ£o pontual â€” nunca redefinem ou duplicam a tabela de pesos; apenas linkam para a seÃ§Ã£o correspondente aqui.

Adaptado (nÃ£o copiado) do padrÃ£o equivalente do projeto irmÃ£o `event-discovery-platform` (mesmo usuÃ¡rio) â€” mesmas fontes normativas e mesmos nomes/definiÃ§Ãµes de critÃ©rio (sÃ£o padrÃµes de indÃºstria: ISO/IEC 25010, AWS Well-Architected, ATAM, DORA/Core-4, mais a literatura de context engineering para dev assistido por IA), mas com **pesos recalibrados por convergÃªncia independente Claudeâ†”Codex especificamente para este repositÃ³rio** â€” nunca herdados sem revisÃ£o. Rodada de convergÃªncia completa (2 propostas independentes + reconciliaÃ§Ã£o) em `docs/engineering/reviews/joint-review-criteria-round1-{claude,codex-prompt,codex-output}.*`; ambas as partes â‰¥9.0 de confianÃ§a na proposta prÃ³pria, sem necessidade de rodada formal de desacordo (mesma condiÃ§Ã£o de convergÃªncia rÃ¡pida que o projeto irmÃ£o documenta para o eixo de contexto).

Cada eixo evolui apenas se o prÃ³prio critÃ©rio se mostrar mal calibrado em uso real â€” registrar por que, junto com a mudanÃ§a, e nÃ£o reabrir a cada nova revisÃ£o sobre o mesmo eixo.

**Eixos formalizados (9)**: Arquitetura, Qualidade de Engenharia, Engenharia de Contexto, SeguranÃ§a da InformaÃ§Ã£o e AppSec, Privacidade e GovernanÃ§a de Dados, OperaÃ§Ãµes/SRE e Continuidade de NegÃ³cio, GovernanÃ§a de IA e Controles Internos, GovernanÃ§a JurÃ­dica/Contratual/Terceiros, GovernanÃ§a de Produto e ServiÃ§o Multi-tenant. **FinOps fica como 10Âº eixo, aprovado por decisÃ£o do usuÃ¡rio mas ainda sem critÃ©rios definidos** â€” nÃ£o usar atÃ© ganhar seÃ§Ã£o prÃ³pria aqui; economia unitÃ¡ria hoje Ã© sÃ³ um critÃ©rio dentro de Produto/ServiÃ§o Multi-tenant, nÃ£o um substituto do eixo.

---

## Eixo: Arquitetura

Convergido em 2026-08-19. Pesos puxados para cima em Reliability & Fault Recovery e Data Model & Consistency â€” refletem que G8 (recuperaÃ§Ã£o real de falha assÃ­ncrona) Ã© o gate de engenharia mais caro e mais recentemente testado deste projeto, e que o single-table DynamoDB com 6 GSIs e duas exceÃ§Ãµes de particionamento (GSI3/GSI6, chaves globais) jÃ¡ produziu bugs reais (inconsultabilidade de PK, ponteiro removido incorretamente) â€” risco concreto, nÃ£o hipotÃ©tico.

| # | CritÃ©rio | Peso |
|---:|---|---:|
| 1 | Domain Fit & Simplicity | 8% |
| 2 | Reliability & Fault Recovery | 16% |
| 3 | Event & Integration Correctness | 11% |
| 4 | Data Model & Consistency | 13% |
| 5 | Security & Privacy | 13% |
| 6 | Modifiability & Evolvability | 7% |
| 7 | Observability & Operability | 8% |
| 8 | Testability & Delivery Safety | 8% |
| 9 | Cost & Resource Governance | 5% |
| 10 | Performance & Scalability Fitness | 4% |
| 11 | Architecture Governance & Traceability | 7% |

## Eixo: Qualidade de engenharia

Convergido em 2026-08-19. Eixo distinto do de Arquitetura â€” craft de cÃ³digo, disciplina de testes, rigor de CI, tooling, disciplina de documentaÃ§Ã£o/processo, gestÃ£o de dÃ­vida tÃ©cnica; nÃ£o redecide design de sistema. Ajuste principal: **Delivery, Release & Recovery Discipline** subiu de forma expressiva (padrÃ£o de origem tinha 8%) â€” nÃ£o porque a entrega jÃ¡ seja madura, mas justamente o oposto: nenhum deploy real foi validado ainda neste projeto, e o Ãºnico bloqueador restante para fechar G8 Ã© exatamente executar deploy real + IAM negativo + DLQ/redrive + EventBridge Scheduler em sandbox AWS. Uma lacuna real de evidÃªncia pesa mais, nÃ£o menos â€” mesmo raciocÃ­nio aplicado ao eixo de contexto abaixo.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Code Correctness & Defensive Design | 11% | PreservaÃ§Ã£o de invariantes via validaÃ§Ã£o explÃ­cita, tratamento de falha, transiÃ§Ãµes de estado seguras, ausÃªncia de corrupÃ§Ã£o silenciosa. |
| 2 | Test Effectiveness & Coverage Discipline | 15% | EvidÃªncia crÃ­vel de unit/integration/contract/negative/failure/regression focada em risco real, nÃ£o apenas contagem/cobertura de linha. SuÃ­te extensa baseada sÃ³ em fakes nÃ£o pontua alto â€” precisa provar propriedades reais (DynamoDB/IAM/SQS/EventBridge) quando o risco Ã© real. |
| 3 | CI Quality Gates & Merge Safety | 11% | Enforcement determinÃ­stico de checks obrigatÃ³rios em toda mudanÃ§a, sem bypass informal, exceÃ§Ãµes formalmente governadas. |
| 4 | Type Safety, Static Analysis & Automated Enforcement | 9% | Uso efetivo de tipagem estrita, lint, dependency-cruiser (grafo real de imports) e fitness functions customizadas para prevenir classes de defeito automaticamente. |
| 5 | Readability, Consistency & Implementation Maintainability | 8% | CÃ³digo coeso, nomeado claramente, estilo consistente, DRY/KISS/YAGNI aplicado com julgamento, barato de modificar. |
| 6 | Delivery, Release & Recovery Discipline | 11% | Artefatos/ambientes reproduzÃ­veis, promoÃ§Ã£o segura, evidÃªncia de deploy, rollback, validaÃ§Ã£o de recuperaÃ§Ã£o â€” critÃ©rio de saÃ­da real de G8, nÃ£o secundÃ¡rio neste estÃ¡gio. |
| 7 | Dependency & Supply-Chain Hygiene | 7% | DependÃªncias controladas, lockfiles, actions/tools pinados, triagem de vulnerabilidade com `expiresAt` formal (`docs/engineering/exceptions.md`), SBOM. |
| 8 | Debuggability & Operational Feedback | 7% | DiagnÃ³sticos estruturados e privacy-safe (`SecureLogger`/`Redactor`), correlation context, erros acionÃ¡veis, suficientes para investigar falhas assÃ­ncronas. |
| 9 | Developer Experience & Reproducibility | 5% | Instalar/validar/testar/rodar o repositÃ³rio a partir de checkout limpo, com comandos pinados e documentados, de baixo atrito. |
| 10 | Documentation Quality & Process Discipline | 5% | Specs, standards, decisÃµes e registros de mudanÃ§a claros e autoritativos, com rigor de processo proporcional a projeto solo. |
| 11 | Documentationâ€“Implementation Drift Control | 6% | Checks determinÃ­sticos ou executados regularmente que detectam divergÃªncia entre documentaÃ§Ã£o, cÃ³digo, infra, config de CI e realidade implantada. |
| 12 | Technical-Debt & Continuous-Improvement Practice | 5% | Registro honesto de atalhos e controles falhos, com dono, trigger, expiry quando aplicÃ¡vel, e follow-through baseado em evidÃªncia. |

## Eixo: Engenharia de contexto

Convergido em 2026-08-19. Avalia a qualidade do prÃ³prio sistema de documentaÃ§Ã£o/contexto do projeto â€” nÃ£o cÃ³digo, nÃ£o arquitetura de sistema, nÃ£o craft de engenharia (eixos distintos, jÃ¡ cobertos acima). Ajuste principal: **Context Routing & Progressive Disclosure** subiu de forma expressiva (padrÃ£o de origem tinha 13%) pelo mesmo raciocÃ­nio do eixo anterior â€” este projeto nÃ£o tem um "context router" dedicado equivalente ao `system-overview.md` do projeto irmÃ£o (`AGENTS.md` Â§2 cumpre parcialmente esse papel, mas Ã© mais fraco), e o volume de documentos normativos/histÃ³ricos/handoffs jÃ¡ torna caro escolher o read-set mÃ­nimo correto. Uma lacuna estrutural real pesa mais no critÃ©rio que ela mede, nunca menos.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Canonicalidade, Autoridade & NÃ£o-DuplicaÃ§Ã£o | 15% | Cada fato normativo tem exatamente uma fonte de verdade; derivados (Ã­ndices, mapas, resumos) sÃ£o explicitamente nÃ£o autoritativos e referenciam a fonte em vez de copiÃ¡-la. Ver `docs/architecture/README.md` Â§"PrecedÃªncia de fontes". |
| 2 | Clareza de PapÃ©is & Proporcionalidade | 9% | Cada documento tem propÃ³sito, escopo e autoridade inequÃ­vocos, sem sobreposiÃ§Ã£o semÃ¢ntica relevante. Estrutura proporcional ao tamanho real do projeto â€” sem sofisticaÃ§Ã£o antecipada (`docs/engineering/principles.md` #1). |
| 3 | Context Routing & Progressive Disclosure | 15% | Humanos e agentes conseguem identificar e carregar o menor conjunto suficiente de contexto para cada tipo de tarefa. Lacuna reconhecida: sem context router dedicado hoje â€” `AGENTS.md` Â§2 Ã© o candidato mais prÃ³ximo, mais fraco que o padrÃ£o do projeto irmÃ£o. |
| 4 | CorrespondÃªncia com a Realidade & Controle de Drift | 16% | O sistema distingue intenÃ§Ã£o documentada, implementaÃ§Ã£o declarada e estado efetivamente observado, reconciliando divergÃªncias explicitamente. `ARCHITECTURE STATUS`/gates G1-G8 sÃ³ mudam com evidÃªncia operacional real (`docs/engineering/principles.md` #6). |
| 5 | Lifecycle, ProveniÃªncia & EvoluÃ§Ã£o do Conhecimento | 12% | Arquitetura vigente vs. histÃ³rica, ADR aceito imutÃ¡vel e supersedido por ADR novo, metadata de status/autoridade, lifecycle de evidÃªncias (`docs/architecture/history/`). |
| 6 | Rastreabilidade de DecisÃµes, Trabalho & Triggers | 10% | DecisÃµes caras, trabalho adiado, dÃ­vida tÃ©cnica e evidÃªncias permanecem conectados; gatilhos de reavaliaÃ§Ã£o concretos e verificÃ¡veis (`docs/engineering/exceptions.md`, `decisions-log.md`). |
| 7 | Higiene de Contexto & Sinal-RuÃ­do | 8% | SÃ³ conhecimento durÃ¡vel Ã© promovido a destinos canÃ´nicos; ausÃªncia de versÃµes concorrentes ou documentos Ã³rfÃ£os. |
| 8 | Portabilidade AgnÃ³stica entre Agentes de IA | 6% | Regras essenciais utilizÃ¡veis por diferentes agentes/ferramentas â€” `AGENTS.md`/`CLAUDE.md` jÃ¡ seguem essa separaÃ§Ã£o. |
| 9 | Auditabilidade & Enforcement do Sistema de Contexto | 9% | AfirmaÃ§Ãµes sobre a saÃºde do contexto verificÃ¡veis por evidÃªncia datada e revisÃµes reproduzÃ­veis; achados com lifecycle claro (aberto/corrigido/adiado/arquivado). |

## Eixo: SeguranÃ§a da InformaÃ§Ã£o e AppSec

Convergido em 2026-08-19 (fontes: OWASP ASVS 5.0, OWASP Top 10:2025, OWASP SAMM, prÃ¡ticas de least-privilege IAM AWS Lambda/DynamoDB). Rodada de convergÃªncia completa em `docs/engineering/reviews/security-axis-criteria-round1-{claude,codex-prompt,codex-output}.*` â€” convergÃªncia forte: os dois critÃ©rios de maior peso (isolamento multi-tenant, least-privilege IAM) saÃ­ram com pesos **idÃªnticos** nas duas propostas independentes, sem precisar de reconciliaÃ§Ã£o. Eixo avalia confidencialidade/integridade/disponibilidade/isolamento diante de abuso intencional e falhas de fronteira de confianÃ§a â€” nÃ£o reavalia craft de cÃ³digo, arquitetura funcional ou disciplina de teste geral (eixos distintos); sÃ³ quando esses elementos sÃ£o controle ou evidÃªncia especificamente de seguranÃ§a.

Pesos calibrados pelo risco real jÃ¡ observado neste projeto: GSI3/GSI6 sÃ£o exceÃ§Ãµes de particionamento com blast radius cross-tenant deliberado que jÃ¡ produziram bug real nesta sessÃ£o (nÃ£o risco hipotÃ©tico); o pipeline assÃ­ncrono (M3.5/G8) passou por 3 rodadas reais de revisÃ£o que encontraram bugs de contrato, race condition e limpeza de Ã­ndice.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Isolamento Multi-Tenant & AutorizaÃ§Ã£o por Objeto | 18% | `tenantId` derivado exclusivamente da identidade validada e propagado por chaves, queries, eventos, idempotÃªncia e auditoria; proteÃ§Ã£o contra IDOR e acesso cross-tenant em toda rota/worker. Maior peso do eixo â€” risco mais concreto e jÃ¡ testado deste projeto (GSI3/GSI6). |
| 2 | Least-Privilege IAM & ContenÃ§Ã£o de Blast Radius | 14% | Capability/role especÃ­fica por Lambda (`ScopedLambdaFunction`), sem grants implÃ­citos/curinga. `gsi3Read()`/`gsi6Read()` como capabilities exclusivas de lista fechada de workers â€” permissÃµes tenant-facing devem produzir `AccessDenied` real (Camada 3, ainda pendente). |
| 3 | AutenticaÃ§Ã£o, SessÃ£o & GestÃ£o de Identidade | 11% | ValidaÃ§Ã£o completa de JWT Cognito (assinatura/issuer/audience/expiraÃ§Ã£o/claims), revogaÃ§Ã£o global/por dispositivo, TTL/rotaÃ§Ã£o, MFA proporcional. Nenhuma autorizaÃ§Ã£o confia em identidade/tenant fornecido pelo cliente. |
| 4 | Integridade do Pipeline AssÃ­ncrono & Fronteiras de Mensagem | 14% | Eventos EventBridge/SQS/Streams/outbox autenticados pela infra, validados por schema, vinculados ao tenant, protegidos contra replay/duplicaÃ§Ã£o/roteamento cruzado. Claim/estado/outbox preservam atomicidade; retry/DLQ/redrive/sweeper nÃ£o contornam autorizaÃ§Ã£o. Peso alto: Ã© onde G8 jÃ¡ encontrou bugs reais. |
| 5 | ValidaÃ§Ã£o de Entrada, Injection & Fail-Closed | 9% | ValidaÃ§Ã£o server-side por contrato (Ajv) em toda borda â€” HTTP, filas, dados persistidos. Entrada invÃ¡lida/estado impossÃ­vel/erro de autorizaÃ§Ã£o falha de modo fechado, sem mutaÃ§Ã£o parcial nem detalhe interno vazado. |
| 6 | ProteÃ§Ã£o de Dados SensÃ­veis, Segredos & Criptografia | 9% | MinimizaÃ§Ã£o/classificaÃ§Ã£o de PII e tokens, criptografia em trÃ¢nsito/repouso, segredos fora de cÃ³digo/log/artefato. `SecureLogger`/`Redactor` deve valer tambÃ©m em exceÃ§Ãµes, traces, eventos e DLQ â€” nÃ£o sÃ³ no caminho feliz. |
| 7 | Logging Seguro, DetecÃ§Ã£o & Resposta a Incidentes | 8% | Eventos de autenticaÃ§Ã£o/autorizaÃ§Ã£o negada/alteraÃ§Ã£o privilegiada/acesso a GSI global produzem trilha Ã­ntegra, correlacionÃ¡vel, privacy-safe (OWASP A09:2025). Alarmes com threshold acionÃ¡vel e runbook de contenÃ§Ã£o/revogaÃ§Ã£o/investigaÃ§Ã£o. |
| 8 | ConfiguraÃ§Ã£o Segura da Plataforma & SuperfÃ­cie Exposta | 6% | API Gateway/Cognito/Lambda/DynamoDB/SQS/EventBridge/Streams com defaults seguros, exposiÃ§Ã£o mÃ­nima, retenÃ§Ã£o adequada. MudanÃ§as de CDK devem ser capazes de detectar regressÃ£o (wildcard de Ã­ndice, grant excessivo, handler placeholder â€” jÃ¡ aconteceu nesta sessÃ£o). |
| 9 | ResistÃªncia a Abuso, DoS & ExaustÃ£o de Custo | 5% | Rate limits, quotas por tenant, concorrÃªncia reservada, limites de batch/paginaÃ§Ã£o impedem que um tenant, poison message ou evento amplificado degrade outros tenants ou gere custo descontrolado; retries/reconciliaÃ§Ã£o nÃ£o podem virar multiplicador de ataque. |
| 10 | VerificaÃ§Ã£o Adversarial & GestÃ£o ContÃ­nua de Risco | 6% | EvidÃªncia especÃ­fica de seguranÃ§a (threat model vivo, testes de abuso/cross-tenant/replay, assertions de IAM/IaC) â€” inclusive teste em AWS real quando emulaÃ§Ã£o nÃ£o prova o controle (Camada 3). Achados com severidade/dono/prazo; nova superfÃ­cie (upload, provedor externo, frontend, webhook) reabre a anÃ¡lise antes da implementaÃ§Ã£o. |

## Eixo: Privacidade e GovernanÃ§a de Dados

Convergido em 2026-08-19 (fontes: LGPD/orientaÃ§Ãµes ANPD sobre RIPD/direitos/incidentes/transferÃªncias, DAMA-DMBOK). Rodada completa em `docs/engineering/reviews/remaining-axes-round1-{claude,codex-prompt,codex-output}.*`. Avalia licitude, finalidade, governanÃ§a e ciclo de vida dos dados â€” nÃ£o controles tÃ©cnicos de acesso (isolamento multi-tenant/IAM jÃ¡ pertencem ao eixo de SeguranÃ§a, nÃ£o duplicado aqui). Maior peso do eixo em RetenÃ§Ã£o/ExclusÃ£o porque Ã© o workflow mais greenfield e envolve mÃºltiplos stores (DynamoDB, S3, filas, backups, providers).

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | InventÃ¡rio, ClassificaÃ§Ã£o, Ownership & Linhagem de Dados | 15% | InventÃ¡rio versionado de dados pessoais, sensÃ­veis, documentos, metadados, logs, eventos e backups; finalidade, origem, destino, responsÃ¡vel e `retentionClass` rastreÃ¡veis. Nova entidade/campo/integraÃ§Ã£o nÃ£o entra sem classificaÃ§Ã£o. |
| 2 | Base Legal, Finalidade & MinimizaÃ§Ã£o | 16% | Base legal e finalidade definidas por tratamento, com teste de necessidade/proporcionalidade. Uso secundÃ¡rio exige reavaliaÃ§Ã£o, nÃ£o reutilizaÃ§Ã£o implÃ­cita. |
| 3 | Direitos do Titular & Portabilidade Efetiva | 16% | ConfirmaÃ§Ã£o/acesso/correÃ§Ã£o/exportaÃ§Ã£o/oposiÃ§Ã£o/exclusÃ£o com fluxo autenticado, prazos, estados e evidÃªncia de conclusÃ£o. |
| 4 | RetenÃ§Ã£o, Legal Hold, ExclusÃ£o VerificÃ¡vel & Backups | 17% | Prazo por classe com evento inicial, prazo final e mecanismo executÃ¡vel. ExclusÃ£o alcanÃ§a DynamoDB/S3/Ã­ndices/filas/providers/restores; testes provam que dado excluÃ­do nÃ£o ressurge apÃ³s restore. |
| 5 | LocalizaÃ§Ã£o, TransferÃªncia Internacional & Subprocessamento | 14% | RegiÃ£o de produÃ§Ã£o, paÃ­ses de tratamento, subprocessadores e mecanismos jurÃ­dicos de transferÃªncia decididos antes de produÃ§Ã£o â€” decisÃ£o hoje pendente por LGPD, nÃ£o drift silencioso. |
| 6 | RIPD, Risco aos Titulares & Privacy by Design | 10% | CritÃ©rios objetivos de quando elaborar/atualizar RIPD; tratamento de alto risco exige decisÃ£o humana registrada. |
| 7 | Qualidade, CorreÃ§Ã£o & ProveniÃªncia dos Dados | 7% | Dados exatos/completos/atuais vinculados Ã  origem/versÃ£o; inferÃªncia de IA distinguÃ­vel de dado confirmado pelo usuÃ¡rio. |
| 8 | Accountability, EvidÃªncia & Monitoramento de Privacidade | 5% | ResponsÃ¡veis, aprovaÃ§Ãµes, DSRs, holds e exceÃ§Ãµes produzem evidÃªncia auditÃ¡vel, sem virar telemetria excessiva de dado pessoal. |

## Eixo: OperaÃ§Ãµes, SRE e Continuidade de NegÃ³cio

Convergido em 2026-08-19 (fontes: Google SRE Book â€” SLO/error budget dirige decisÃ£o operacional, nÃ£o sÃ³ relatÃ³rio â€”, AWS Well-Architected SaaS Lens Â§Operate). Avalia capacidade de operar/detectar/responder/recuperar o serviÃ§o real â€” o desenho de SLO/DR jÃ¡ existe (`slo.md`/`disaster-recovery.md`) mas nunca foi operacionalizado com dado de produÃ§Ã£o real. Maior peso em Backup/Restore porque "documentado mas nunca testado" Ã© o padrÃ£o de risco mais repetido observado neste projeto (G8 sÃ³ fechou depois de testar de verdade).

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | SLIs, SLOs & Error Budgets AcionÃ¡veis | 16% | SLIs representam resultado percebido (API, reminder freshness, backlog, reconciliaÃ§Ã£o); consumo de error budget condiciona release/priorizaÃ§Ã£o, nÃ£o sÃ³ relatÃ³rio. |
| 2 | Observabilidade Operacional & VisÃ£o por Tenant | 11% | Detecta impacto agregado e noisy neighbor entre tenants â€” `SecureLogger` maduro nÃ£o basta sem dashboard/sinal real. |
| 3 | DetecÃ§Ã£o, Resposta & ComunicaÃ§Ã£o de Incidentes | 15% | Alertas com threshold acionÃ¡vel/dono/escalonamento/runbook; comunicaÃ§Ã£o a tenants/titulares/ANPD quando aplicÃ¡vel; exercÃ­cios comprovam funcionamento sob pressÃ£o. |
| 4 | SaÃºde do Pipeline AssÃ­ncrono & RecuperaÃ§Ã£o de Backlog | 14% | Idade/profundidade de fila, DLQ, outbox, claims, sweeper monitorados; redrive/replay idempotentes e limitados. Peso alto pela criticidade do produto + Camada 3 ainda pendente. |
| 5 | Backup, Restore, RTO/RPO & Continuidade | 18% | PITR/backup configurado nÃ£o basta â€” nota depende de restore real periÃ³dico com RTO/RPO observado. Falha regional Ã© risco aceito explicitamente (regiÃ£o Ãºnica, Stage 0-2), nÃ£o ignorado. |
| 6 | ProntidÃ£o de Deploy, Rollback & MudanÃ§a Operacional | 10% | Deploy com artefato identificÃ¡vel, checks prÃ©/pÃ³s, rollback/roll-forward; mudanÃ§a de schema/GSI/KMS/provider aciona validaÃ§Ã£o proporcional. |
| 7 | Capacidade, DependÃªncias & DegradaÃ§Ã£o Controlada | 9% | Limites AWS/providers/concorrÃªncia/custo conhecidos e testados; kill switches preservam trabalho recuperÃ¡vel. |
| 8 | Post-mortem, ExercÃ­cios & Melhoria ContÃ­nua | 7% | Post-mortem sem culpa com aÃ§Ã£o/dono/prazo; falha recorrente ou aÃ§Ã£o vencida reduz nota. |

## Eixo: GovernanÃ§a de IA e Controles Internos

Convergido em 2026-08-19 (fontes: NIST AI RMF â€” ciclo Govern/Map/Measure/Manage â€”, ISO/IEC 42001). Abrange Claude/Codex construindo e operando o projeto, e futuros componentes de IA/OCR do produto. Avalia autoridade, supervisÃ£o, independÃªncia, proveniÃªncia e gestÃ£o de risco â€” nÃ£o a qualidade genÃ©rica do cÃ³digo gerado (eixo de Engenharia). Maior peso em IndependÃªncia da RevisÃ£o porque Ã© o controle interno central deste projeto (protocolo `AGENTS.md` Â§4).

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Limites de Autoridade, PermissÃµes & SupervisÃ£o Humana | 18% | AÃ§Ãµes permitidas/proibidas/sujeitas a aprovaÃ§Ã£o humana por agente; deploy/merge/exclusÃ£o/produÃ§Ã£o/Type-1/comunicaÃ§Ã£o externa seguem least authority e fail-closed. |
| 2 | AtribuiÃ§Ã£o, ProveniÃªncia & Reprodutibilidade das AÃ§Ãµes | 15% | Toda mudanÃ§a relevante atribuÃ­vel a agente/modelo/sessÃ£o/diff/teste/aprovador â€” reconstruÃ­vel sem exigir armazenamento indiscriminado de prompt sensÃ­vel. |
| 3 | IndependÃªncia da RevisÃ£o & SegregaÃ§Ã£o de FunÃ§Ãµes | 15% | Nota cega, mÃ­nimo de rodadas, mesmo agente nunca simula aprovaÃ§Ã£o independente de si mesmo. Controle interno central do projeto â€” sem ele, "revisÃ£o Claudeâ†”Codex" viraria teatro. |
| 4 | InventÃ¡rio de Casos de Uso & GestÃ£o do Risco de IA | 13% | Usos de IA inventariados por finalidade/impacto/dado/autonomia/reversibilidade; mudanÃ§a relevante reabre avaliaÃ§Ã£o (Govern-Map-Measure-Manage). |
| 5 | AvaliaÃ§Ã£o de CorreÃ§Ã£o, LimitaÃ§Ãµes & Impacto | 12% | Nota alta sem evidÃªncia de arquivo:linha concreta nÃ£o fecha revisÃ£o (jÃ¡ Ã© a prÃ¡tica deste projeto â€” formalizado aqui como critÃ©rio). |
| 6 | ProteÃ§Ã£o de Contexto, Dados & Segredos no Uso de IA | 10% | Apenas contexto necessÃ¡rio enviado a modelo/ferramenta; segredos/dado pessoal/documento de tenant obedecem classificaÃ§Ã£o e retenÃ§Ã£o. |
| 7 | GestÃ£o de Modelos, Ferramentas, Fornecedores & MudanÃ§as | 9% | VersÃ£o/capacidade de modelo/CLI conhecida; upgrade tem avaliaÃ§Ã£o de regressÃ£o e impacto â€” comportamento de fornecedor nÃ£o Ã© controle interno garantido. |
| 8 | Incidentes de IA, ExceÃ§Ãµes & Melhoria ContÃ­nua | 8% | Mecanismo de suspender uso/reverter/registrar falha; bypass/exceÃ§Ã£o com fundamento, prazo e aprovaÃ§Ã£o. |

## Eixo: GovernanÃ§a JurÃ­dica, Contratual e de Terceiros

Convergido em 2026-08-19 (fontes: SOC 2 Trust Services Criteria â€” gestÃ£o de fornecedor, nÃ£o certificaÃ§Ã£o prematura â€”, licenciamento OSS). Avalia obrigaÃ§Ã£o/licenÃ§a/contrato/responsabilidade/risco de dependÃªncia externa â€” SBOM, pinning e integridade tÃ©cnica de build continuam nos eixos de Engenharia/SeguranÃ§a, nÃ£o duplicados aqui.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | PapÃ©is JurÃ­dicos, Responsabilidades & Modelo Contratual | 16% | Define quando o serviÃ§o Ã© controlador/operador/ambos; decisÃ£o pendente recebe dono e gate antes do lanÃ§amento. |
| 2 | InventÃ¡rio, Due Diligence & Monitoramento de Terceiros | 16% | Registro de AWS/modelos de IA/OCR/mensageria/CI com serviÃ§o, dados, criticidade, regiÃ£o, certificaÃ§Ã£o, lock-in e responsÃ¡vel. |
| 3 | Licenciamento OSS, Propriedade Intelectual & Uso de ConteÃºdo | 14% | DependÃªncias/cÃ³digo gerado com licenÃ§a/proveniÃªncia compatÃ­vel com operaÃ§Ã£o comercial; autoria por IA nÃ£o presume ausÃªncia de risco de licenÃ§a. |
| 4 | Termos de Uso, Aviso de Privacidade & Consentimentos | 14% | Termos/avisos compatÃ­veis com comportamento real do produto â€” nÃ£o existem ainda, prÃ©-requisito antes do primeiro usuÃ¡rio real. |
| 5 | DPAs, TransferÃªncias, Incidentes & ObrigaÃ§Ãµes de Fornecedor | 13% | Contrato com operador/suboperador cobre instruÃ§Ãµes, seguranÃ§a, localizaÃ§Ã£o, exclusÃ£o e comunicaÃ§Ã£o tempestiva de incidente (LGPD art. 39). |
| 6 | Compromissos Comerciais, SLA & ProteÃ§Ã£o do Consumidor | 13% | NÃ£o prometer entrega de mensagem quando sÃ³ hÃ¡ tentativa do provider, nem disponibilidade sem arquitetura correspondente. |
| 7 | AprovaÃ§Ã£o, EvidÃªncia, ExceÃ§Ãµes & MudanÃ§a RegulatÃ³ria | 7% | Gatilho de reavaliaÃ§Ã£o em mudanÃ§a regulatÃ³ria/produto/regiÃ£o/provider; aprovaÃ§Ã£o sem dono nÃ£o pontua como controle. |
| 8 | Continuidade, Portabilidade & SaÃ­da de Fornecedor | 7% | DependÃªncia crÃ­tica tem estratÃ©gia proporcional de substituiÃ§Ã£o/exportaÃ§Ã£o â€” dado nÃ£o fica irrecuperavelmente preso sem risco aceito e documentado. |

## Eixo: GovernanÃ§a de Produto e ServiÃ§o Multi-tenant

Convergido em 2026-08-19 (fontes: AWS Well-Architected SaaS Lens â€” onboarding automatizado no control plane, crypto-shredding no offboarding, tenant como conceito de primeira classe). Avalia o serviÃ§o percebido/administrado pelo tenant ao longo do ciclo de vida â€” nÃ£o reavalia isolamento/IAM tÃ©cnico (eixo de SeguranÃ§a). **FinOps fica como eixo prÃ³prio por decisÃ£o do usuÃ¡rio, nÃ£o formalizado ainda** â€” economia unitÃ¡ria aqui Ã© sÃ³ um critÃ©rio de produto, nÃ£o compete em peso com esse eixo futuro.

| # | CritÃ©rio | Peso | DefiniÃ§Ã£o |
|---:|---|---:|---|
| 1 | Lifecycle Automatizado de Tenant | 18% | Onboarding/ativaÃ§Ã£o/convite/mudanÃ§a de plano/suspensÃ£o/offboarding como estados explÃ­citos, idempotentes, auditÃ¡veis â€” hoje o tenant nasce implicitamente no primeiro login (MVP `tenantId=userId`), sem control plane dedicado. |
| 2 | Offboarding, ExportaÃ§Ã£o & DestruiÃ§Ã£o CriptogrÃ¡fica | 16% | "NÃ£o conseguimos deletar um tenant" Ã© falha de compliance, nÃ£o sÃ³ tÃ©cnica; crypto-shredding coordenado com retenÃ§Ã£o/backup/legal hold. |
| 3 | Planos, Entitlements, Quotas & Fairness | 13% | Capacidades/limites como polÃ­tica centralizada e versionada; `TenantQuota` jÃ¡ existe (M1), falta plano de billing real. NÃ£o duplica rate-limiting de seguranÃ§a â€” mede correÃ§Ã£o da oferta do produto. |
| 4 | CorreÃ§Ã£o do ServiÃ§o de Lembretes & ProteÃ§Ã£o do UsuÃ¡rio | 15% | Timezone/DST/opt-out/quiet hours/renovaÃ§Ã£o corrigÃ­veis; lembrete perdido/duplicado/obsoleto tem detecÃ§Ã£o e reparo â€” Ã© a falha mais grave possÃ­vel do produto. |
| 5 | TransparÃªncia, Usabilidade & Acessibilidade | 10% | Estado/erro/atraso comunicado em linguagem clara; ainda nÃ£o se aplica plenamente (sem frontend), mas entra no radar em M4+. |
| 6 | AdministraÃ§Ã£o, Suporte & OperaÃ§Ã£o sob a Ã“tica do Tenant | 10% | Suporte autorizado diagnostica saÃºde/uso/config por tenant com trilha de auditoria, sem acesso informal a conteÃºdo. |
| 7 | MÃ©tricas de Valor, Consumo & Economia UnitÃ¡ria | 10% | AdoÃ§Ã£o/sucesso de lembrete/consumo atribuÃ­vel por tenant de forma privacy-safe â€” germe do futuro eixo FinOps, nÃ£o o eixo em si. |
| 8 | EvoluÃ§Ã£o Unificada & Controle de CustomizaÃ§Ã£o | 8% | Nenhuma feature cria "fork" por tenant; variaÃ§Ã£o por plano usa mecanismo governado (mesmo princÃ­pio jÃ¡ aplicado ao reshard versionado do GSI3). |

## Como adicionar um novo eixo

NÃ£o criar a tabela de critÃ©rios dentro do primeiro doc de auditoria do eixo novo. Seguir o mesmo procedimento de convergÃªncia independente (`AGENTS.md` Â§4) e, ao final, adicionar aqui uma seÃ§Ã£o nova nesse mesmo formato â€” o registro de auditoria referencia a seÃ§Ã£o, nÃ£o a duplica.


web search: 
web search: https://docs.aws.amazon.com/lambda/latest/dg/services-sqs-errorhandling.html
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/shared/errors/app-error.ts; Get-Content infra/modules/sqs-worker-queue/main.tf; rg -n "visibility_timeout|sqs-worker-queue|batchItemFailures|schema-invalid|poison" infra src/runtime/aws/handlers test | Select-Object -First 240; git branch --show-current; git status --short' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 361ms:
/**
 * Normalized error taxonomy used across API handlers and async workers.
 *
 * implementation-blueprint.md doesn't name a concrete class hierarchy, but requires
 * (per #6.2) that every consumer "classifica erro como retryable ou terminal" and
 * (per #10.4/NotificationAttempt) that failures carry a normalized error code+category
 * instead of raw SDK exceptions leaking into logs/DLQ metadata. This module is the
 * single place that decision is implemented, so error handling is consistent between
 * the API error-mapper (middleware/error-mapper.ts, M1) and async worker DLQ paths.
 */

export type ErrorCategory =
  | "VALIDATION"
  | "AUTH"
  | "AUTHORIZATION"
  | "NOT_FOUND"
  | "CONFLICT"
  | "QUOTA_EXCEEDED"
  | "DEPENDENCY_UNAVAILABLE"
  | "INTERNAL"
  // M7 item 8 (`claude-reconciliation-final-design.md` Â§1.7): the first 422 in this codebase â€”
  // a request that is well-formed and passes every OCC/idempotency/authorization check, but
  // violates a business invariant the domain enforces (e.g. confirming an `ExtractedField` that
  // isn't `PENDING_CONFIRMATION`, or a `confirmedValue` that fails its `valueType` validation).
  // Deliberately distinct from VALIDATION (schema/shape) and CONFLICT (OCC/version/idempotency).
  | "BUSINESS_RULE";

export interface AppErrorOptions {
  code: string;
  category: ErrorCategory;
  message: string;
  retryable: boolean;
  /** Arbitrary structured context. MUST be redacted before logging - callers pass raw values here. */
  details?: Record<string, unknown>;
  cause?: unknown;
}

/**
 * Base class for all normalized application errors.
 *
 * HONEST SCOPE of `retryable` (logging-observability-standard.md audit finding, 2026-08-29,
 * correcting an overclaim this comment used to make): implementation-blueprint.md #6.2 asks
 * every consumer to "classifica erro como retryable ou terminal", and this field IS that
 * classification - but no real SQS handler in this codebase branches on it today. Every SQS
 * consumer (reminder-dispatch-handler.ts, email-delivery-handler.ts, textract-task-handler.ts's
 * COMPLETE_OCR, ...) reports EVERY caught error as a batch item failure unconditionally,
 * letting SQS's own `maxReceiveCount`+DLQ redelivery mechanics be the actual retry/terminal
 * decision - `retryable` only reaches a log line (diagnosis), never an `if`. Separately, the M7
 * Step Functions ASL's `Retry`/`Catch` blocks (document-extraction.asl.json) do real
 * conditional retry/terminal routing, but keyed on `errorType` (the thrown class's name)
 * matched statically at deploy time - never on this runtime boolean either. Whether SQS
 * consumers SHOULD start branching on `retryable` (e.g. route a known-terminal error straight
 * to DLQ instead of spending `maxReceiveCount` retries on it first) is a real, undecided
 * product/architecture question - not implemented here, not this session's call to make
 * unilaterally (Type 1, `AGENTS.md` Â§4/Â§1). This field is not useless: it is real,
 * meaningful classification metadata surfaced consistently in structured logs today, and the
 * substrate a future decision would build on - just not, as of this comment, itself deciding
 * SQS behavior.
 */
export class AppError extends Error {
  readonly code: string;
  readonly category: ErrorCategory;
  readonly retryable: boolean;
  readonly details: Record<string, unknown> | undefined;

  constructor(options: AppErrorOptions) {
    super(options.message);
    this.name = "AppError";
    this.code = options.code;
    this.category = options.category;
    this.retryable = options.retryable;
    this.details = options.details;
    if (options.cause !== undefined) {
      this.cause = options.cause;
    }
    Error.captureStackTrace?.(this, AppError);
  }

  toJSON(): Record<string, unknown> {
    return {
      code: this.code,
      category: this.category,
      message: this.message,
      retryable: this.retryable,
      details: this.details,
    };
  }
}

export class ValidationError extends AppError {
  constructor(message: string, details?: Record<string, unknown>) {
    super({ code: "VALIDATION_FAILED", category: "VALIDATION", message, retryable: false, details });
    this.name = "ValidationError";
  }
}

export class AuthenticationError extends AppError {
  constructor(message = "Authentication required or invalid.", details?: Record<string, unknown>) {
    super({ code: "AUTH_REQUIRED", category: "AUTH", message, retryable: false, details });
    this.name = "AuthenticationError";
  }
}

export class AuthorizationError extends AppError {
  constructor(message = "Not authorized for this resource.", details?: Record<string, unknown>) {
    super({ code: "AUTHORIZATION_DENIED", category: "AUTHORIZATION", message, retryable: false, details });
    this.name = "AuthorizationError";
  }
}

export class NotFoundError extends AppError {
  constructor(message = "Resource not found.", details?: Record<string, unknown>) {
    super({ code: "NOT_FOUND", category: "NOT_FOUND", message, retryable: false, details });
    this.name = "NotFoundError";
  }
}

/** Wave B2B-11 (Responsibility + Notifications) - the target userId for an assignee/watcher
 * write is not an eligible member of the Organization (no Membership at all, or a Membership
 * that exists but is not ACTIVE, or a globally-suspended identity - see
 * `expiration/ports/member-eligibility.ts`). Mapped to the same NOT_FOUND category/404 as
 * `NotFoundError` deliberately (Codex round 1: never let the HTTP status distinguish "this
 * person exists but isn't eligible" from "no such person", which would let a caller enumerate
 * real userIds) - the distinction is a NAMED error class for internal logs/metrics only. */
export class IneligibleAssigneeError extends AppError {
  constructor(message = "Target user is not an eligible member of this organization.", details?: Record<string, unknown>) {
    super({ code: "INELIGIBLE_ASSIGNEE", category: "NOT_FOUND", message, retryable: false, details });
    this.name = "IneligibleAssigneeError";
  }
}

/** Version/state conflicts - OCC failures, duplicate creation, stale aggregate. */
export class ConflictError extends AppError {
  constructor(message: string, details?: Record<string, unknown>) {
    super({ code: "CONFLICT", category: "CONFLICT", message, retryable: false, details });
    this.name = "ConflictError";
  }
}

export class QuotaExceededError extends AppError {
  constructor(message = "Quota exceeded.", details?: Record<string, unknown>) {
    super({ code: "QUOTA_EXCEEDED", category: "QUOTA_EXCEEDED", message, retryable: false, details });
    this.name = "QuotaExceededError";
  }
}

/** Downstream/provider failure - `retryable` defaults to true (throttling, timeout, 5xx are
 * worth retrying), but pass `false` for a genuinely ambiguous outcome (e.g. the BFF's own
 * UNKNOWN_OUTCOME refresh result, src/modules/bff/application/bff-auth-service.ts) where a
 * blind retry could race an already-rotated credential rather than just re-hit a flaky
 * dependency. */
export class DependencyUnavailableError extends AppError {
  constructor(message: string, details?: Record<string, unknown>, cause?: unknown, retryable = true) {
    super({ code: "DEPENDENCY_UNAVAILABLE", category: "DEPENDENCY_UNAVAILABLE", message, retryable, details, cause });
    this.name = "DependencyUnavailableError";
  }
}

export class InternalError extends AppError {
  constructor(message = "Internal error.", details?: Record<string, unknown>, cause?: unknown) {
    super({ code: "INTERNAL", category: "INTERNAL", message, retryable: false, details, cause });
    this.name = "InternalError";
  }
}

/**
 * M7 extraction/OCR taxonomy (`claude-reconciliation-final-design.md` Â§2 â€” the ASL's
 * `RunTextract` Catch block). Each `code` string below is NOT decorative â€” it MUST literally
 * match the corresponding `ErrorEquals` entry in `document-extraction.asl.json`, because
 * `TextractTaskHandler` reports failures via `SendTaskFailure({ error: <code> })` and Step
 * Functions matches Catch blocks on that exact string. Never rename one without the other.
 */

/** Heuristic classification (extension/magic-bytes/Document metadata) found no supported
 * Textract input format. Never retryable â€” the same bytes will classify the same way again. */
export class UnsupportedDocumentTypeError extends AppError {
  constructor(message = "Document type is not supported by the extraction pipeline.", details?: Record<string, unknown>) {
    super({ code: "UnsupportedDocumentType", category: "VALIDATION", message, retryable: false, details });
    this.name = "UnsupportedDocumentTypeError";
  }
}

/** The `OCR` AppConfig kill switch was off at the moment `RunTextract` executed. Per design
 * Â§1.5.1 this is a definitive "no OCR evidence for this pass", never a suspend-and-resume. */
export class OcrDisabledError extends AppError {
  constructor(message = "OCR is disabled by the feature-flags kill switch.", details?: Record<string, unknown>) {
    super({ code: "OcrDisabled", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
    this.name = "OcrDisabledError";
  }
}

/** Textract itself rejected the document as an unsupported format/corrupt file at
 * `StartDocumentTextDetection` time (distinct from the pre-call heuristic classifier). */
export class TextractUnsupportedDocumentError extends AppError {
  constructor(message = "Textract rejected the document as unsupported.", details?: Record<string, unknown>) {
    super({ code: "TextractUnsupportedDocument", category: "VALIDATION", message, retryable: false, details });
    this.name = "TextractUnsupportedDocumentError";
  }
}

/** Textract job ended (or was read back) in a degraded state the handler cannot use as a
 * reliable OCR artifact â€” not `PARTIAL_SUCCESS` (which IS usable, per design Â§3), a harder
 * failure than that (e.g. `FAILED` job status). */
export class TextractPartialFailureError extends AppError {
  constructor(message = "Textract job did not complete usably.", details?: Record<string, unknown>) {
    super({ code: "TextractPartialFailure", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
    this.name = "TextractPartialFailureError";
  }
}

/** `StartDocumentTextDetection` succeeded but persisting the `TextractJob` correlation record
 * failed even after the handler's own local retry (design Â§2, "RecuperaÃ§Ã£o do intervalo") â€” the
 * Textract job is now orphaned (no waiting callback can ever resolve it), but the run itself
 * must not block on that: it falls through to the deterministic parser like any other
 * `RunTextract` failure. */
export class TextractJobPersistenceFailedError extends AppError {
  constructor(message = "Failed to persist TextractJob correlation record after StartDocumentTextDetection succeeded.", details?: Record<string, unknown>) {
    super({ code: "TextractJobPersistenceFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
    this.name = "TextractJobPersistenceFailedError";
  }
}

/** `PdfParserTaskHandler`'s `RunDeterministicParser` state (M7 item 5, D-035 Â§1.3) failed to
 * even attempt parsing (e.g. the OCR artifact was declared available but could not be read/
 * parsed as valid Textract block JSON). This is the ONE failure the ASL routes straight to
 * `MarkPendingConfirmation` without trying Bedrock (design Â§1.2) - the ASL's `Catch` for this
 * state is the generic `States.ALL`, so this `code` does not need to match a specific
 * `ErrorEquals` entry the way the Textract errors above do, but it still follows the same
 * normalized-taxonomy discipline (never a bare `Error`/`throw`). Never thrown just because no
 * candidate value was found - "no candidate" is a normal, successful parser outcome. */
export class DeterministicParserFailedError extends AppError {
  constructor(message = "Deterministic parser failed to process the OCR artifact.", details?: Record<string, unknown>) {
    super({ code: "DeterministicParserFailed", category: "INTERNAL", message, retryable: false, details });
    this.name = "DeterministicParserFailedError";
  }
}

/** `BedrockExtractionTaskHandler`'s `RunBedrock` state (M7 item 6, D-035 Â§1.9/Â§1.11) - the
 * `AI_EXTRACTION` kill switch was off (or unreadable, fail-closed) when the handler itself
 * checked it, even though the ASL's `CheckAiKillSwitch` Choice state already gates this path -
 * this is a defense-in-depth re-check, never the only gate. The ASL's `Catch` for `RunBedrock`
 * is the generic `States.ALL`, so this `code` does not need to match a specific `ErrorEquals`. */
export class AiExtractionDisabledError extends AppError {
  constructor(message = "AI_EXTRACTION kill switch is off; failing closed.", details?: Record<string, unknown>) {
    super({ code: "AiExtractionDisabled", category: "VALIDATION", message, retryable: false, details });
    this.name = "AiExtractionDisabledError";
  }
}

/** Bedrock Converse call failed, or the model's tool-call response could not be parsed/
 * validated against the closed `submit_extraction` schema (malformed/missing tool call, wrong
 * tool name, extra fields, token-limit truncation, etc. - design Â§1.11's adversarial corpus).
 * Never thrown just because the model reported low/no confidence for a field - that is a
 * normal, successful outcome (an empty or low-confidence candidate), not an error. */
export class BedrockExtractionFailedError extends AppError {
  constructor(message = "Bedrock extraction call failed or returned an unusable response.", details?: Record<string, unknown>) {
    super({ code: "BedrockExtractionFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
    this.name = "BedrockExtractionFailedError";
  }
}

/** `ExtractionValidationTaskHandler`'s `PERSIST_EXTRACTED_FIELDS`/`MARK_PENDING_CONFIRMATION`
 * operations (M7 item 7, D-035 Â§2/Â§3) failed to commit â€” a genuine DynamoDB error, not the
 * expected `DOCUMENT_DISCARDED` outcome (which is not an error at all, see
 * `run-extraction-validation.ts`). The ASL's `Catch` for these states is the generic
 * `States.ALL`, so this `code` does not need to match a specific `ErrorEquals` entry. */
export class ExtractionCommitFailedError extends AppError {
  constructor(message = "Failed to commit extraction run outcome.", details?: Record<string, unknown>, cause?: unknown) {
    super({ code: "ExtractionCommitFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: true, details, cause });
    this.name = "ExtractionCommitFailedError";
  }
}

/** M7 item 8 (Â§1.7): request is well-formed, authorized, and every OCC version matched, but
 * the operation violates a business invariant â€” HTTP 422. `retryable: false` always: retrying
 * the identical request without changing anything about the world will fail identically. */
export class BusinessRuleError extends AppError {
  constructor(message: string, details?: Record<string, unknown>) {
    super({ code: "BUSINESS_RULE_VIOLATION", category: "BUSINESS_RULE", message, retryable: false, details });
    this.name = "BusinessRuleError";
  }
}

/** W3-07 (D-067, `TenantBusinessMutation` lane, `src/shared/tenant-lifecycle/`): thrown when
 * a tenant-scoped business mutation's ConditionCheck against `TenantLifecycleRecord.status =
 * ACTIVE` fails â€” the tenant has entered DELETING (or has no lifecycle record at all, which
 * this codebase never expects once bootstrap always creates one). `retryable: false`: the
 * tenant is not coming back to ACTIVE, so retrying identically will fail identically. */
export class TenantNotActiveError extends AppError {
  constructor(message = "Tenant is not ACTIVE; mutation rejected.", details?: Record<string, unknown>) {
    super({ code: "TENANT_NOT_ACTIVE", category: "CONFLICT", message, retryable: false, details });
    this.name = "TenantNotActiveError";
  }
}

/** Wave B2B-5 (D-095, RequestContext Cutover): the identity itself is valid/active, but no
 * `ACTIVE` `Membership` exists yet to build a working `RequestContext` from â€” the caller must
 * be routed through onboarding, never treated as a hard authentication failure. Carries the
 * exact `OnboardingState` (`OnboardingStateResolver`, Wave B2B-4/D-094) so the HTTP/BFF layer
 * can distinguish "create an organization" from "you were suspended" instead of collapsing all
 * non-working states into one generic denial. */
export class OnboardingRequiredError extends AppError {
  constructor(
    readonly onboardingState: string,
    details?: Record<string, unknown>,
  ) {
    super({
      code: "ONBOARDING_REQUIRED",
      category: "AUTH",
      message: `No usable Membership for this identity (onboardingState=${onboardingState}).`,
      retryable: false,
      details: { ...details, onboardingState },
    });
    this.name = "OnboardingRequiredError";
  }
}

/** Wave B2B-5 (D-095, Codex Rodada 1 achado 2.2): `Membership.role` supports 4 values
 * (`OWNER|ADMIN|MEMBER|VIEWER`, `organization/domain/membership.ts`) but the real authorization
 * matrix (`identity/domain/authorization.ts`) only knows 3 until Wave B2B-7 ships the real
 * permission derivation. Thrown explicitly at the `RequestContext`-building boundary instead of
 * letting an unsupported role silently fail every `authorize()` check via the matrix's own
 * unsafe cast â€” loud, not silent, until the real policy exists. Unreachable today: the only
 * `Membership` writer (`CreateOrganizationService`) always grants `OWNER`. */
export class UnsupportedMembershipRoleError extends AppError {
  constructor(role: string, details?: Record<string, unknown>) {
    super({
      code: "UNSUPPORTED_MEMBERSHIP_ROLE",
      category: "INTERNAL",
      message: `Membership role "${role}" is not supported by the authorization matrix yet (Wave B2B-7).`,
      retryable: false,
      details: { ...details, role },
    });
    this.name = "UnsupportedMembershipRoleError";
  }
}

/** Wave B2B-8 (D-099, physical model Â§8): thrown when a transaction's `ownerCount > :one`
 * guard fails â€” the target `Membership` is the organization's last `ACTIVE` `OWNER`, and the
 * requested role-change/remove/leave would zero out `Organization.ownerCount`. Research
 * (GitHub/Slack/Notion, 2026-08-30) found this exact protection convergent across all 4
 * products consulted â€” this error is the UX-facing name for a mechanism already `APPROVED` in
 * D-086 Â§8, never exercised by a real writer until this wave. `retryable: false`: the caller
 * must promote a second `OWNER` first, not just retry. */
export class LastOwnerError extends AppError {
  constructor(message = "Cannot complete this action - the organization would be left with no OWNER. Promote another member to OWNER first.", details?: Record<string, unknown>) {
    super({ code: "LAST_OWNER", category: "BUSINESS_RULE", message, retryable: false, details });
    this.name = "LastOwnerError";
  }
}

/** Wave B2B-8 (D-099): a `membership:invite`/`membership:role-change`/`membership:remove` call
 * targets (or would create/promote to) the `OWNER` tier, but the caller's own role is not
 * `OWNER`. Distinct from the generic `AuthorizationDeniedError` (identity/domain/
 * authorization.ts) - the matrix baseline (`ADMIN_ROLES`) already let the caller attempt the
 * action; this is a named service-level check on top of it (research: Slack - only Owners
 * assign/demote Owners, no source shows an Admin touching the Owner tier). */
export class OwnerTierChangeRequiresOwnerError extends AppError {
  constructor(message = "Only an OWNER can invite, promote, or change the role of another OWNER.", details?: Record<string, unknown>) {
    super({ code: "OWNER_TIER_CHANGE_REQUIRES_OWNER", category: "AUTHORIZATION", message, retryable: false, details });
    this.name = "OwnerTierChangeRequiresOwnerError";
  }
}

/** Wave B2B-8 (D-099, physical model Â§7/Â§121 Q14): the `InvitationTokenPointer` resolved for an
 * `AcceptInvitation` call failed its transactional guard (`attribute_not_exists(consumedAt) AND
 * expiresAt > :now`) - deliberately generic naming (not `...AlreadyConsumed`): the failed
 * condition cannot distinguish replay from a token that expired in the narrow window between
 * the pre-transaction resolution and the transaction commit (Codex Rodada 2/3 achado,
 * docs/architecture/reviews/multi-user-b2b-wave-b2b8-scoping/), and claiming a specific cause
 * the check cannot prove would be a false diagnostic. */
export class InvitationTokenUnavailableError extends AppError {
  constructor(message = "This invitation link is no longer valid.", details?: Record<string, unknown>) {
    super({ code: "INVITATION_TOKEN_UNAVAILABLE", category: "CONFLICT", message, retryable: false, details });
    this.name = "InvitationTokenUnavailableError";
  }
}

/** Wave B2B-6 (D-101, physical model Â§11/Â§12): the caller's selected organization (via
 * `X-Organization-Id`, BFF-derived, or the BFF session's own `activeOrganizationId`) does not
 * resolve to a usable working context - the `Membership` isn't `ACTIVE`, or the `Organization`'s
 * own `TenantLifecycleRecord` isn't `ACTIVE`. `category: "AUTHORIZATION"` (403), not `CONFLICT`:
 * this is an access/context check at the RequestContext-building boundary, not a concurrent-
 * write conflict (research: DEV's x-tenant-id pattern returns 403 on membership mismatch, OWASP
 * Multi Tenant Security treats this as the "Tenant Context Injection" access-control class).
 * Replaces (does not coexist with) the pre-existing `AuthenticationError` this same lifecycle
 * check used to throw - a deliberate, small observable-contract fix (D-093: acceptable, no real
 * users/production). */
export class OrganizationUnavailableError extends AppError {
  constructor(message = "This organization is not available in your current context.", details?: Record<string, unknown>) {
    super({ code: "ORGANIZATION_UNAVAILABLE", category: "AUTHORIZATION", message, retryable: false, details });
    this.name = "OrganizationUnavailableError";
  }
}

/** Wave B2B-6 (D-101): the caller has more than one usable `Membership` (`ACTIVE` + its
 * Organization's `TenantLifecycleRecord` `ACTIVE`) and supplied no `X-Organization-Id` hint -
 * ambiguous, never "pick the first" (same fail-closed discipline as the rest of this codebase).
 * Replaces the `InternalError` this case used to throw before Wave B2B-8 made it reachable. */
export class OrganizationSelectionRequiredError extends AppError {
  constructor(message = "Multiple organizations are available; select one via X-Organization-Id.", details?: Record<string, unknown>) {
    // CONFLICT (409), not AUTHORIZATION - the caller IS allowed, the request is just
    // ambiguous (more than one valid Membership) without a disambiguating hint, closer to "the
    // request can't be completed as given" than to "access denied".
    super({ code: "ORGANIZATION_SELECTION_REQUIRED", category: "CONFLICT", message, retryable: false, details });
    this.name = "OrganizationSelectionRequiredError";
  }
}

/** W3-07 purge orchestrator (D-124, implementing D-121). `CloseOrganizationService` refuses to
 * act when the tenant's `TenantLifecycleRecord` already sits in VERIFIED/DELETED/BLOCKED/HELD â€”
 * the purge is either past the point of no return or stuck awaiting an operator, and re-launching
 * an execution for it would be nonsensical rather than merely redundant. `category:
 * "CONFLICT"` (409), same as `TenantNotActiveError`: this is a state-of-the-resource refusal, not
 * an authorization decision (the caller genuinely is an OWNER and genuinely may close the org â€”
 * it just is not in a closable state). `retryable: false`: no amount of retrying moves the record
 * back, only operator remediation of a BLOCKED/HELD tenant does. Deliberately does NOT cover
 * DELETING/QUIESCING/PURGING â€” those fall through to the unconditional idempotent
 * `StartExecution` retry (Rodada 3 Fix 8's corrected ordering). */
export class OrganizationClosureUnavailableError extends AppError {
  constructor(message = "This organization is already being closed, closed, or on hold - contact support.", details?: Record<string, unknown>) {
    super({ code: "ORGANIZATION_CLOSURE_UNAVAILABLE", category: "CONFLICT", message, retryable: false, details });
    this.name = "OrganizationClosureUnavailableError";
  }
}

/** D-122/D-125 (Responsibility Reassignment on Member Removal): thrown by
 * `RemoveMembershipService.remove()`/`LeaveOrganizationService.leave()` when `targetUserId` is
 * still `assigneeUserId` of at least one `ACTIVE` `ExpirationItem` in the organization
 * (`AssignedActiveItemsLookup`, `organization/ports/assigned-active-items-lookup.ts`). Explicitly
 * best-effort, NOT atomic (Round-3 "Estado final consolidado") - a synchronous read-then-decide
 * precondition before the removal transaction, not part of it; a rare concurrent reassignment
 * between the check and the transaction remains a residual, observed only via the existing
 * `MEMBER_REMOVED`/`MEMBER_LEFT` audit trail and B2B-11's notification-cancellation log, never a
 * new telemetry mechanism invented just for this case. `itemIds` is capped at 20 per
 * `AssignedActiveItemsLookup`'s pagination contract; `totalKnown`/`truncated` always reflect the
 * true count. `retryable: false`: the caller must reassign the flagged items first (via the
 * existing `updateItem` mutation, already validated by `MemberEligibilityChecker`), not just
 * retry the identical removal. */
export class ResponsibilityReassignmentRequiredError extends AppError {
  constructor(
    input: { targetUserId: string; itemIds: string[]; totalKnown: number; truncated: boolean },
    message = "Target user is still the assignee of active expiration items - reassign them before removing this member.",
  ) {
    super({
      code: "RESPONSIBILITY_REASSIGNMENT_REQUIRED",
      category: "BUSINESS_RULE",
      message,
      retryable: false,
      details: { ...input },
    });
    this.name = "ResponsibilityReassignmentRequiredError";
  }
}

/** Normalizes any thrown value into an AppError, for boundaries (handlers, workers). */
export function toAppError(err: unknown): AppError {
  if (err instanceof AppError) {
    return err;
  }
  if (err instanceof Error) {
    return new InternalError(err.message, undefined, err);
  }
  return new InternalError("Non-Error value thrown.", { thrown: String(err) });
}

/** Classifies any thrown value's `retryable` flag (defaults to `false`/terminal for an
 * unclassified error, never `true`, to avoid treating an unknown failure as safe-to-retry
 * indefinitely per implementation-blueprint.md #6.2). See `AppError`'s own doc comment for the
 * honest current scope: this is NOT today's real SQS/DLQ routing decision (no real consumer
 * branches on it - see that comment for why), it is the classification itself, consulted for
 * logging/diagnosis and available for a future decision to actually drive routing on. */
export function isRetryable(err: unknown): boolean {
  return toAppError(err).retryable;
}
# Generic SQS worker queue + DLQ, reused across milestones (M3.5 reminder dispatch, M4
# notification router/email-deliver/ses-callback) - extracted from the reminder-specific
# module of the same shape (round1 cross-critique of M4's design: "o mÃ³dulo atual
# reminder-queue nÃ£o deve ser reutilizado nominalmente porque contÃ©m nomes, SIDs e
# descriÃ§Ãµes especÃ­ficos de reminder"). See variables.tf for the sizing rationale
# (maxReceiveCount=5, visibility timeout = 6x consumer timeout, long polling, DLQ age alarm
# at 1h per implementation-blueprint.md line 879) - unchanged from the original module,
# only the naming is generic now.

resource "aws_sqs_queue" "dlq" {
  name                      = "${var.queue_name}-dlq"
  message_retention_seconds = 14 * 24 * 60 * 60 # 14 days
  sqs_managed_sse_enabled   = true
  tags                      = var.tags
}

resource "aws_sqs_queue" "this" {
  name                       = var.queue_name
  message_retention_seconds  = 4 * 24 * 60 * 60 # 4 days
  visibility_timeout_seconds = var.consumer_timeout_seconds * 6
  receive_wait_time_seconds  = 20 # long polling
  sqs_managed_sse_enabled    = true

  redrive_policy = jsonencode({
    deadLetterTargetArn = aws_sqs_queue.dlq.arn
    maxReceiveCount     = 5
  })

  tags = var.tags
}

# implementation-blueprint.md line 879: DLQ age alarm at 1h/4h.
resource "aws_cloudwatch_metric_alarm" "dlq_age" {
  alarm_name          = "${var.queue_name}-dlq-age"
  namespace           = "AWS/SQS"
  metric_name         = "ApproximateAgeOfOldestMessage"
  dimensions          = { QueueName = aws_sqs_queue.dlq.name }
  statistic           = "Maximum"
  period              = 300 # 5 minutes
  evaluation_periods  = 1
  threshold           = 3600 # 1 hour, in seconds
  comparison_operator = "GreaterThanThreshold"
  alarm_description   = "ReminderDispatchQueue DLQ has messages older than 1 hour - investigate and redrive per runbook (never blind redrive)."
  treat_missing_data  = "notBreaching"
  alarm_actions       = [var.alert_topic_arn]
  tags                = var.tags
}

# Queue ARN constructed deterministically (not read from aws_sqs_queue.this.arn) so the IAM
# policy documents below are plan-time-known and don't depend on the queue resource actually
# existing yet â€” same pattern as the dynamo-table module. SQS ARN format is stable/documented:
# arn:aws:sqs:<region>:<account>:<queue-name>.
locals {
  queue_arn = "arn:aws:sqs:${var.aws_region}:${var.aws_account_id}:${var.queue_name}"
}

# IAM policy documents mirroring scoped-lambda-function.ts's queueAccessFor() capabilities.
data "aws_iam_policy_document" "consume" {
  statement {
    sid = "WorkerQueueConsume"
    actions = [
      "sqs:ReceiveMessage",
      "sqs:DeleteMessage",
      "sqs:GetQueueAttributes",
    ]
    resources = [local.queue_arn]
  }
}

data "aws_iam_policy_document" "send" {
  statement {
    sid       = "WorkerQueueSend"
    actions   = ["sqs:SendMessage"]
    resources = [local.queue_arn]
  }
}
test\unit\dispatch-outbox-relay-processor.test.ts:73:  it("3 records, the 2nd send fails: batchItemFailures reports only the 2nd, the 3rd is processed normally, and the 3rd's log never carries the 2nd's correlationId", async () => {
test\unit\dispatch-outbox-relay-processor.test.ts:98:    const batchItemFailures = await processStreamRecords(deps, logger, records);
test\unit\dispatch-outbox-relay-processor.test.ts:101:    expect(batchItemFailures).toEqual([{ itemIdentifier: "evt-2" }]);
infra\main.tf:3:# environment: dynamo-table, cognito, api-gateway, lambda-function (x8), sqs-worker-queue,
infra\main.tf:505:  source = "./modules/sqs-worker-queue"
infra\main.tf:530:  source = "./modules/sqs-worker-queue"
infra\main.tf:575:  source = "./modules/sqs-worker-queue"
infra\main.tf:586:  source = "./modules/sqs-worker-queue"
infra\main.tf:597:  source = "./modules/sqs-worker-queue"
infra\main.tf:944:  source = "./modules/sqs-worker-queue"
infra\main.tf:955:  source = "./modules/sqs-worker-queue"
infra\main.tf:1104:  source = "./modules/sqs-worker-queue"
infra\main.tf:1491:# de cada fila (já embutida em sqs-worker-queue) continua sendo a rede mínima herdada, agora
infra\main.tf:1502:  source = "./modules/sqs-worker-queue"
infra\main.tf:1603:  source = "./modules/sqs-worker-queue"
infra\main.tf:1742:  source = "./modules/sqs-worker-queue"
infra\main.tf:1985:  source = "./modules/sqs-worker-queue"
infra\tests\stack.tftest.hcl:331:  # 1 DLQ age alarm (dispatch_queue, sqs-worker-queue module) + 5 per-function error alarms
infra\tests\stack.tftest.hcl:335:  # alarms (one per new sqs-worker-queue instance: router/email-deliver/ses-callback) via
infra\tests\stack.tftest.hcl:585:  # reminder-observability's/sqs-worker-queue's own module tests; the alert topic's ARN
test\unit\expiration\expiration-service.test.ts:339:    // simply never retried) must not leave "another-client-key" poisoned for unrelated future
test\unit\reminder\producer.test.ts:21:/** Wraps a real InMemoryReminderStore but injects one poison failure: the first
test\unit\reminder\producer.test.ts:22: * transactWrite whose Update targets `poisonSk` throws a plain Error (not a
test\unit\reminder\producer.test.ts:30:    private readonly poisonSk: string,
test\unit\reminder\producer.test.ts:39:      const hitsPoisonKey = entries.some((e) => "Update" in e && e.Update.Key.SK === this.poisonSk);
test\unit\reminder\producer.test.ts:111:    const poisonStore = new PoisonOnceStore(store, occurrence.SK);
test\unit\reminder\producer.test.ts:114:      { store: poisonStore, shardConfig: defaultShardConfig(), tableName: TABLE, now, newEventId: () => `evt-${Math.random()}`, correlationId: () => "c" },
test\unit\reminder\reminder-materialization-trigger.test.ts:373:  it("throws UnrecognizedTriggerEventTypeError for an unknown eventType (poison-message signal, not a silent no-op)", async () => {
src/runtime/aws/handlers\import-commit-handler.ts:3: * reminder-dispatch-handler.ts (schema-invalid payload is a poison message, not retryable). */
src/runtime/aws/handlers\import-commit-handler.ts:40:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\import-commit-handler.ts:49:          logger.error("import-commit schema-invalid payload", { messageId: record.messageId, errors });
src/runtime/aws/handlers\import-commit-handler.ts:50:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\import-commit-handler.ts:60:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\import-commit-handler.ts:65:  return { batchItemFailures };
src/runtime/aws/handlers\import-parse-handler.ts:32:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\import-parse-handler.ts:41:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\import-parse-handler.ts:66:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\import-parse-handler.ts:71:  return { batchItemFailures };
src/runtime/aws/handlers\malware-result-handler.ts:39:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\malware-result-handler.ts:48:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\malware-result-handler.ts:55:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\malware-result-handler.ts:106:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\malware-result-handler.ts:111:  return { batchItemFailures };
src/runtime/aws/handlers\email-delivery-handler.ts:3: * reminder-dispatch-handler.ts (schema-invalid payload is a poison message, not retryable). */
src/runtime/aws/handlers\email-delivery-handler.ts:41:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\email-delivery-handler.ts:48:    // body - a malformed/schema-invalid message's own failure log was unjoinable to anything.
src/runtime/aws/handlers\email-delivery-handler.ts:56:          logger.error("email-delivery schema-invalid payload", { messageId: record.messageId, errors });
src/runtime/aws/handlers\email-delivery-handler.ts:57:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\email-delivery-handler.ts:98:            batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\email-delivery-handler.ts:106:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\email-delivery-handler.ts:111:  return { batchItemFailures };
src/runtime/aws/handlers\dispatch-outbox-relay-processor.ts:20:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\dispatch-outbox-relay-processor.ts:45:            batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\dispatch-outbox-relay-processor.ts:49:          batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\dispatch-outbox-relay-processor.ts:55:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\dispatch-outbox-relay-processor.ts:59:  return batchItemFailures;
src/runtime/aws/handlers\notification-email-outbox-relay-processor.ts:17:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\notification-email-outbox-relay-processor.ts:41:            batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\notification-email-outbox-relay-processor.ts:45:          batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\notification-email-outbox-relay-processor.ts:50:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\notification-email-outbox-relay-processor.ts:54:  return batchItemFailures;
src/runtime/aws/handlers\notification-email-outbox-relay-handler.ts:31:  const batchItemFailures = await processStreamRecords(deps, logger, event.Records);
src/runtime/aws/handlers\notification-email-outbox-relay-handler.ts:32:  return { batchItemFailures };
src/runtime/aws/handlers\extraction-starter-handler.ts:29:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\extraction-starter-handler.ts:38:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\extraction-starter-handler.ts:74:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\extraction-starter-handler.ts:79:  return { batchItemFailures };
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:3: * processing - same discipline as reminder-dispatch-handler.ts (schema-invalid payload is a
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:4: * poison message, not retryable). */
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:30:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:39:          logger.error("document-chasing-dispatch schema-invalid payload", { messageId: record.messageId, errors });
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:40:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:50:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\document-chasing-dispatch-handler.ts:55:  return { batchItemFailures };
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:3: * partial-batch-failure/poison-message discipline as reminder-dispatch-handler.ts: only
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:4: * schema-invalid or genuinely failed records are reported back, so SQS's own
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:24:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:33:          logger.error("reminder-materialization-trigger schema-invalid payload", { messageId: record.messageId, errors });
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:34:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:45:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\reminder-materialization-trigger-handler.ts:50:  return { batchItemFailures };
src/runtime/aws/handlers\notification-router-handler.ts:24:  batchItemFailures: { itemIdentifier: string }[],
src/runtime/aws/handlers\notification-router-handler.ts:37:        batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\notification-router-handler.ts:41:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\notification-router-handler.ts:47:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\notification-router-handler.ts:57:      await processRecord(record, item, batchItemFailures);
src/runtime/aws/handlers\notification-router-handler.ts:60:      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
src/runtime/aws/handlers\notification-router-handler.ts:64:  return { batchItemFailures };
src/runtime/aws/handlers\reminder-dispatch-handler.ts:27:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\reminder-dispatch-handler.ts:34:    // never whether a message is processed: a schema-invalid message (including any
src/runtime/aws/handlers\reminder-dispatch-handler.ts:36:    // always rejected as a poison message below, regardless of which correlationId source
src/runtime/aws/handlers\reminder-dispatch-handler.ts:46:          // Schema-invalid payload is treated as a poison message, not a retryable failure -
src/runtime/aws/handlers\reminder-dispatch-handler.ts:50:          logger.error("reminder-dispatch schema-invalid payload", { messageId: record.messageId, errors });
src/runtime/aws/handlers\reminder-dispatch-handler.ts:51:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\reminder-dispatch-handler.ts:71:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\reminder-dispatch-handler.ts:76:  return { batchItemFailures };
src/runtime/aws/handlers\textract-task-handler.ts:11: *   Textract's own JSON string carrying `JobId`). Same `batchItemFailures` pattern as
src/runtime/aws/handlers\textract-task-handler.ts:101:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\textract-task-handler.ts:133:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\textract-task-handler.ts:138:  return { batchItemFailures };
src/runtime/aws/handlers\dispatch-outbox-relay-handler.ts:2: * placeholder. Partial batch failure so a poison record doesn't block the rest of the
src/runtime/aws/handlers\dispatch-outbox-relay-handler.ts:35:  const batchItemFailures = await processStreamRecords(deps, logger, event.Records);
src/runtime/aws/handlers\dispatch-outbox-relay-handler.ts:36:  return { batchItemFailures };
src/runtime/aws/handlers\upload-finalizer-handler.ts:38:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\upload-finalizer-handler.ts:47:          batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\upload-finalizer-handler.ts:98:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\upload-finalizer-handler.ts:103:  return { batchItemFailures };
src/runtime/aws/handlers\ses-callback-handler.ts:65:  const batchItemFailures: { itemIdentifier: string }[] = [];
src/runtime/aws/handlers\ses-callback-handler.ts:85:        batchItemFailures.push({ itemIdentifier: record.messageId });
src/runtime/aws/handlers\ses-callback-handler.ts:90:  return { batchItemFailures };
infra\modules\import-observability\variables.tf:7:# on top of the DLQ-age alarm each queue already has via sqs-worker-queue.
infra\modules\import-observability\main.tf:74:# --- ImportCommitWorker: any real failure (unhandled exception, schema-invalid payload, or a
infra\modules\import-observability\main.tf:81:  pattern        = "{ $.event = \"import-commit failed\" || $.event = \"import-commit schema-invalid payload\" }"
infra\modules\import-observability\main.tf:115:  alarm_description   = "ImportCommitWorker threw an unhandled exception, received a schema-invalid SQS payload, or found a plan whose SHA-256 no longer matches what ImportParseWorker wrote (FAILED_INTEGRITY_MISMATCH - possible tampering or corruption between parse and commit). FAILED_ENTITLEMENT_EXCEEDED is deliberately not alarmed here (expected tenant-caused outcome, not a system health signal)."
infra\modules\document-malware-protection\variables.tf:48:  description = "ARN of the SQS queue (sqs-worker-queue module instance) that MalwareResultWorker consumes. The EventBridge rule created here targets it."
infra\modules\ses-notifications\outputs.tf:10:# this module doesn't own that queue resource (it's created by the sqs-worker-queue module
infra\modules\ses-notifications\main.tf:31:# the same plan-time-known-policy-document rationale as dynamo-table/sqs-worker-queue.
infra\modules\ses-notifications\variables.tf:19:  description = "AWS region - used only to construct the SNS topic ARN deterministically for the subscription/queue policy documents, same rationale as the dynamo-table/sqs-worker-queue modules (keeps policy documents plan-time-known)."
infra\modules\sqs-worker-queue\main.tf:20:  visibility_timeout_seconds = var.consumer_timeout_seconds * 6
infra\modules\spa-hosting\tests\spa_hosting.tftest.hcl:13:  # rendered JSON (same root cause documented in sqs-worker-queue/tests: content assertions on
infra\modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:46:    condition     = aws_sqs_queue.this.visibility_timeout_seconds == 60
infra\modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:51:run "visibility_timeout_scales_with_consumer_timeout" {
infra\modules\sqs-worker-queue\tests\sqs_worker_queue.tftest.hcl:60:    condition     = aws_sqs_queue.this.visibility_timeout_seconds == 180
develop
?? .claude/worktrees/
?? docs/architecture/reviews/appError-retryable-sqs-scoping/

codex
## Codex — Rodada 1

**Nota cega da régua: 8,5/10**  
**Nota cega do design: 8,7/10**  
**Status: NÃO APROVADO nesta rodada**

A conclusão técnica parece correta: **não implementar branching por `retryable` e manter `maxReceiveCount` + redrive nativo como mecanismo único**. Porém, a fundamentação contém três imprecisões que precisam ser corrigidas antes do gate 9,0.

### Achados

1. **A pesquisa atribui à AWS uma recomendação mais forte do que a fonte realmente faz.**

A documentação explica retry, redrive e partial batch responses, e os exemplos reportam qualquer exceção como falha. Isso demonstra o padrão básico, mas não estabelece normativamente que retry-vs-terminal “deve” ser decidido exclusivamente pela fila.

A própria documentação também reconhece `DeleteMessage` como forma de impedir novo processamento. Portanto:

- correto: o mecanismo existente é diretamente suportado e exemplificado pela AWS;
- excessivo: dizer que branching seria contrário ao padrão canônico ou que a AWS determina que nunca seja feito;
- formulação recomendada: **a AWS não oferece, nos exemplos consultados, um padrão de classificação por erro que justifique adicionar esse mecanismo aqui**.

[Handling errors for an SQS event source](https://docs.aws.amazon.com/lambda/latest/dg/services-sqs-errorhandling.html) · [Using Lambda with Amazon SQS](https://docs.aws.amazon.com/lambda/latest/dg/with-sqs.html)

2. **“Cinco tentativas, minutos, não horas” não está demonstrado.**

O módulo define visibilidade como `consumer_timeout_seconds * 6`, e os consumidores podem usar timeouts diferentes. Além disso, o backoff e o tempo efetivo até a quinta recepção não são garantidos por essa configuração isoladamente.

D-128 deve dizer apenas:

> O número de recepções é limitado a cinco antes do redrive; o tempo até o DLQ depende da configuração e do comportamento operacional da fila/Lambda.

O argumento econômico continua válido sem inventar uma duração.

3. **A classificação atual não é uma base segura para branching — mas por motivo ainda mais forte que o apresentado.**

`toAppError()` converte qualquer exceção desconhecida em `InternalError`, cujo `retryable` é `false`. Logo, um timeout, erro inesperado do SDK ou defeito de integração que escape da normalização específica pode parecer terminal. O mesmo vale para categorias reutilizadas originalmente em fluxos HTTP/Step Functions.

Isso reforça decisivamente a proposta de não branch, mas invalida a afirmação ampla de que a taxonomia já foi auditada como “base sã para uma política de branching”. Ela é coerente como **metadado diagnóstico contextual**, não comprovadamente segura como política de acknowledgement/descarte de SQS.

### Ajuste necessário na régua

O critério 1 deve ser renomeado para algo como:

> **Aderência aos mecanismos documentados e ausência de necessidade comprovada para um caminho adicional.**

A âncora alta não pode depender da alegação de que a AWS prescreve exclusividade do redrive nativo.

Também sugiro que o critério 2 explicite os dois erros de classificação:

- falso terminal: perda antecipada se a mensagem for reconhecida como sucesso;
- falso retryable: até cinco processamentos e posterior DLQ, comportamento limitado e recuperável.

### Escopo documental

Há mais consumidores SQS no repositório do que os três citados, incluindo importação, malware, extração, upload e document chasing. Não é necessário ler cada um manualmente se houver inventário reproduzível, mas D-128 não deve alegar cobertura de “todas as 10+ filas” com evidência baseada apenas em três handlers.

Também vale corrigir comentários existentes que dizem simultaneamente “poison message, not retryable” e adicionam o item a `batchItemFailures`. A formulação honesta seria:

> deterministic poison message, still retried and redriven under the uniform native SQS policy.

### Veredito

Com essas correções, a decisão recomendada permanece:

- nenhum branching sobre `retryable`;
- nenhuma chamada direta à DLQ;
- nenhuma mudança de runtime;
- `retryable` permanece somente como metadado diagnóstico;
- reavaliação apenas diante de evidência operacional concreta, como saturação, custo material ou atraso relevante causado por poison messages.

A proposta está próxima do gate, mas a alegação normativa sobre a AWS, a duração não demonstrada e a caracterização excessivamente otimista da taxonomia impedem nota ≥9 nesta rodada.
tokens used
56.757
## Codex — Rodada 1

**Nota cega da régua: 8,5/10**  
**Nota cega do design: 8,7/10**  
**Status: NÃO APROVADO nesta rodada**

A conclusão técnica parece correta: **não implementar branching por `retryable` e manter `maxReceiveCount` + redrive nativo como mecanismo único**. Porém, a fundamentação contém três imprecisões que precisam ser corrigidas antes do gate 9,0.

### Achados

1. **A pesquisa atribui à AWS uma recomendação mais forte do que a fonte realmente faz.**

A documentação explica retry, redrive e partial batch responses, e os exemplos reportam qualquer exceção como falha. Isso demonstra o padrão básico, mas não estabelece normativamente que retry-vs-terminal “deve” ser decidido exclusivamente pela fila.

A própria documentação também reconhece `DeleteMessage` como forma de impedir novo processamento. Portanto:

- correto: o mecanismo existente é diretamente suportado e exemplificado pela AWS;
- excessivo: dizer que branching seria contrário ao padrão canônico ou que a AWS determina que nunca seja feito;
- formulação recomendada: **a AWS não oferece, nos exemplos consultados, um padrão de classificação por erro que justifique adicionar esse mecanismo aqui**.

[Handling errors for an SQS event source](https://docs.aws.amazon.com/lambda/latest/dg/services-sqs-errorhandling.html) · [Using Lambda with Amazon SQS](https://docs.aws.amazon.com/lambda/latest/dg/with-sqs.html)

2. **“Cinco tentativas, minutos, não horas” não está demonstrado.**

O módulo define visibilidade como `consumer_timeout_seconds * 6`, e os consumidores podem usar timeouts diferentes. Além disso, o backoff e o tempo efetivo até a quinta recepção não são garantidos por essa configuração isoladamente.

D-128 deve dizer apenas:

> O número de recepções é limitado a cinco antes do redrive; o tempo até o DLQ depende da configuração e do comportamento operacional da fila/Lambda.

O argumento econômico continua válido sem inventar uma duração.

3. **A classificação atual não é uma base segura para branching — mas por motivo ainda mais forte que o apresentado.**

`toAppError()` converte qualquer exceção desconhecida em `InternalError`, cujo `retryable` é `false`. Logo, um timeout, erro inesperado do SDK ou defeito de integração que escape da normalização específica pode parecer terminal. O mesmo vale para categorias reutilizadas originalmente em fluxos HTTP/Step Functions.

Isso reforça decisivamente a proposta de não branch, mas invalida a afirmação ampla de que a taxonomia já foi auditada como “base sã para uma política de branching”. Ela é coerente como **metadado diagnóstico contextual**, não comprovadamente segura como política de acknowledgement/descarte de SQS.

### Ajuste necessário na régua

O critério 1 deve ser renomeado para algo como:

> **Aderência aos mecanismos documentados e ausência de necessidade comprovada para um caminho adicional.**

A âncora alta não pode depender da alegação de que a AWS prescreve exclusividade do redrive nativo.

Também sugiro que o critério 2 explicite os dois erros de classificação:

- falso terminal: perda antecipada se a mensagem for reconhecida como sucesso;
- falso retryable: até cinco processamentos e posterior DLQ, comportamento limitado e recuperável.

### Escopo documental

Há mais consumidores SQS no repositório do que os três citados, incluindo importação, malware, extração, upload e document chasing. Não é necessário ler cada um manualmente se houver inventário reproduzível, mas D-128 não deve alegar cobertura de “todas as 10+ filas” com evidência baseada apenas em três handlers.

Também vale corrigir comentários existentes que dizem simultaneamente “poison message, not retryable” e adicionam o item a `batchItemFailures`. A formulação honesta seria:

> deterministic poison message, still retried and redriven under the uniform native SQS policy.

### Veredito

Com essas correções, a decisão recomendada permanece:

- nenhum branching sobre `retryable`;
- nenhuma chamada direta à DLQ;
- nenhuma mudança de runtime;
- `retryable` permanece somente como metadado diagnóstico;
- reavaliação apenas diante de evidência operacional concreta, como saturação, custo material ou atraso relevante causado por poison messages.

A proposta está próxima do gate, mas a alegação normativa sobre a AWS, a duração não demonstrada e a caracterização excessivamente otimista da taxonomia impedem nota ≥9 nesta rodada.
