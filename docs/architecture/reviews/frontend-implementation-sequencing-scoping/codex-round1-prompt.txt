You are acting as an independent reviewer/proposer in the Claude<->Codex protocol (AGENTS.md section 4) for this repository (expiration-tracker). This is a BLIND independent proposal round: you have not seen any Claude proposal on this question. Answer from your own reading of the repo.

CONTEXT: This is a B2B SaaS (Expiration Tracker) tracking compliance documents. Backend for the P0 roadmap is fully implemented. Frontend: a 25-screen plan exists (docs/frontend/p0-screen-inventory-plan.md, A01-A23 authenticated + G01/G02 guest), and all 25 screens now have an audited/revised spec (docs/architecture/reviews/screen-spec-audit-2026-09-09/estado-final-consolidado.md) - 24 were revised after CRITICAL/Major findings during audit (6 CRITICAL RBAC/security findings, all fixed in spec text), only A10 has a genuine post-revision score of 92.4/100 (WORLD-CLASS-READY). The other 24 scored well below the 90 pass floor pre-revision and were NOT re-scored after revision (an honest gap in the process). System-level findings from that audit (system-level-findings.md): SLF-01 (no risk-prioritized metric-card pattern exists yet, a system constraint affecting A03/A09's dashboard-style cards), SLF-02 (motion tokens already exist in design-system.md but specs didn't reference them - a spec-authoring gap, not a missing token), SLF-03 (routes must include :orgId - spec-authoring checklist gap), SLF-05 (guest screens G01/G02 need a shared anti-enumeration "link unavailable" UI pattern, not yet built as a real component).

Read these files directly before answering:
- AGENTS.md (repo protocol, section 4 is the Claude<->Codex debate protocol, section 7 is code conventions)
- docs/frontend/p0-screen-inventory-plan.md (full 25-screen plan, section 6 has a 12-journey table, section 3 lists 7 persistent nav items)
- docs/architecture/reviews/screen-spec-audit-2026-09-09/estado-final-consolidado.md (audit results)
- docs/frontend/core-expiration-vertical-slice.md (the flagship precedent: 96 unit/component + 12 E2E tests for Expiration Collection/Detail/Create/Renew = A04/A05)
- docs/frontend/frontend-production-foundation.md (the BFF + frontend architecture: Vite+React+TS+React Router v7+TanStack Query v5)
- Actually inspect the real code: frontend/src/routes/ (list the directory), frontend/src/components/ui/ (list the directory), and run `git log --oneline -20 -- frontend/` to see what has actually shipped. Do NOT assume the plan doc's claim that "only A04/A05 exist" is accurate - verify directly, since other routes (Subjects, Members, Settings, ActivityLog, Onboarding, AcceptInvitation) may already have real (even if partial) implementations that should change the build-order recommendation.

QUESTIONS TO ANSWER (this is an execution-planning/sequencing decision, not a security-critical architecture decision - proportion your rigor accordingly):

1. Build order for the remaining screens: journey-vertical vs plan-ID order vs shared-component-DNA grouping vs some other approach. Ground this in the plan's own 12 journeys and in what you find already built in the real code.
2. How new screens integrate with the existing AppShell/BFF/design-system foundation - does the shell need restructuring to add the ~7 nav items, and is there real regression risk to whatever is already shipped and tested?
3. Testing strategy per screen: the flagship precedent (A04/A05) used ~96 unit + 12 E2E tests for 4 screens. Is that rigor realistic for all 24 remaining screens? Propose a tiered approach if not, with justification for which screens land in which tier.
4. "Block" granularity for a recurring Claude<->Codex review that runs after each block of screens is implemented (not just once at the start): what should a block be (one screen? one journey? a fixed batch size?), and what should the recurring review actually check?
5. Should the named system-level design-system gaps (SLF-01 metric-card pattern, SLF-02 motion guidance, SLF-05 anti-enumeration guest pattern) be fixed once up front as a "block 0" foundation piece, or incrementally as each affected screen is built? Justify.

Give your own concrete, opinionated answer to all 5 questions - a specific ordered list of blocks with named screens, not just principles. Self-score your own proposal 0-10 on soundness/practicality at the end, with one sentence justifying the score. Be honest about uncertainty or claims you could not verify from the repo.
