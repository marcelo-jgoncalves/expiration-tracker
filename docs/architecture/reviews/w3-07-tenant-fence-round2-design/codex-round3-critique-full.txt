2026-08-28T21:20:20.255427Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a04a3e-bd92-79e1-b5c6-53b843f9ede0
--------
user
Voce e o revisor adversarial independente (protocolo Claude-Codex de AGENTS.md secao 4) do repositorio
expiration-tracker (c:/Users/Usuario/Desktop/projects/expiration-tracker). Continuacao da Rodada 2 do W3-07
(fence de exclusao de tenant), que voce mesmo reprovou com nota 4,1/10 em
docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt.

Leia, na integra, antes de avaliar:
1. codex-round2-critique-full.txt (sua propria critica - checklist de 8 achados + N1-N4 achados novos que a
   proposta nova abaixo alega corrigir)
2. claude-proposal-round3.md (a avaliar agora)
3. src/modules/identity/ports/identity-store.ts e identity-mapping-repository.ts (port real a estender)
4. src/workers/reminder-dispatch/dispatch.ts (linhas ~126/244) e
   src/modules/reminder/application/reminder-materializer.ts (linhas ~312/373) - confirme se anexar o fence
   AO FIM (nao ao inicio) de fato preserva os indices que esse codigo assume, ou se ha alguma suposicao de
   "ultimo indice" que o append quebraria
5. src/modules/extraction/application/start-extraction-run.ts, complete-ocr.ts, start-ocr.ts,
   run-bedrock-extraction.ts (confirme as correcoes propostas contra o codigo real)
6. .eslintrc.cjs real deste repo (confirme se a sintaxe overrides/no-restricted-imports/no-restricted-syntax
   proposta e valida para a versao instalada e se realmente fecha os dois bypasses que voce demonstrou na
   Rodada 2)
7. Grep as keys S3 reais usadas por Document/artefato OCR (s3-document-object-store.ts,
   s3-ocr-artifact-store.ts) para confirmar ou refutar a alegacao da proposta de que ja seguem (ou nao)
   convencao tenant/<tenantId>/... - e se os buckets relevantes tem versionamento habilitado (grep
   infra/modules/document-buckets, infra/modules/import-bucket por versioning)

Trate cada um dos 8 achados da Rodada 1 + N1-N4 da Rodada 2 como item de checklist, confirmando RESOLVIDO /
PARCIAL / NAO RESOLVIDO contra codigo real, nao contra a alegacao da proposta. Preste atencao especial a:
- Se a varredura S3 por prefixo ate convergencia (secao 7/8) e de fato implementavel e fecha a garantia, ou
  introduz problemas novos (ex.: objetos em buckets sem essa convencao de key, custo/tempo de ListObjectsV2
  recursivo, DeleteObjects em lote com falhas parciais).
- Se o novo contrato de TenantQuotaService.consume() retornando "RESERVED"|"ALREADY_RESERVED" e coerente com
  os demais call sites reais (grep todos os usos de consume()), sem quebrar nenhum.
- Se a mudanca de IdentityStore para incluir transactWrite segue o mesmo padrao real que outros stores
  (dynamodb-document-store.ts etc.) usam, ou se ha uma diferenca estrutural que a proposta nao reconheceu.

Responda com:
1. Nota de 0 a 10 (protocolo de nota cega desta rodada).
2. Checklist explicito de cada achado (8 da Rodada 1 + N1-N4 da Rodada 2): RESOLVIDO / PARCIAL / NAO
   RESOLVIDO, com evidencia contra codigo real.
3. Achados NOVOS desta rodada, se houver.
4. Recomendacao: aprovar (>=9.0), pedir revisao, ou reprovar.

Continue rigoroso - nao suavize por ja terem sido duas rodadas de correcao.

codex
Vou conduzir a revisão como auditoria somente leitura e aplicar o protocolo/rubrica do repositório. Primeiro lerei integralmente as fontes obrigatórias e o índice de engenharia; depois cruzarei cada alegação com código, ESLint, infraestrutura e todos os call sites relevantes.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 183ms:
# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o

> Este arquivo Ã© estado atual + prÃ³xima aÃ§Ã£o (`AGENTS.md` Â§2), nÃ£o histÃ³rico. Para a linha do tempo completa por sessÃ£o, ver `docs/architecture/session-log.md`; para toda decisÃ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) â€” confirmar `git log`/branch atual antes de assumir que o PR jÃ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program â€” Waves 0/2/3/5 concluÃ­das, Wave 4 parcial (gated), Wave 1 nÃ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃ£o de consolidaÃ§Ã£o/pilot-readiness. **Backlog canÃ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃ­ntese final/recomendaÃ§Ã£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃ¡vel do prompt mestre Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) â€” nÃ£o duplicar detalhe aqui, sÃ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃ§Ã£o) `DONE`. **Wave 2 (evidÃªncia operacional) `DONE`** (2026-08-28) â€” os 6 drills (W2-03 a W2-08) executados com evidÃªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO â‰ˆ3min44s, RPO nÃ£o medido â€” item aberto), load test real (977 invocaÃ§Ãµes, cota `API_REQUEST` segurando sob carga), alarme forÃ§ado com notificaÃ§Ã£o SNSâ†’e-mail real confirmada. **RPO (W2-06), dedupe pÃ³s-commit (W2-05) e o pipeline de alarme do W2-08 fechados na mesma sessÃ£o (2026-08-28)** â€” ver `pilot-readiness-program.md` seÃ§Ã£o Wave 2 para o detalhe completo (inclui o achado real de que "credential-compromise via HTTP" Ã© estruturalmente inalcanÃ§Ã¡vel hoje, nÃ£o uma lacuna). Claims especÃ­ficas por drill: ver `docs/engineering/test-engineering-standard.md` Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada. **`W3-06-DECISION` fechada e implementada nesta sessÃ£o (2026-08-28)**: desenho do mecanismo de purga real de `USER_DOCUMENT` aprovado via protocolo Claudeâ†”Codex completo (6 rodadas, Codex 9,2/Claude 9,1, D-061 em `decisions-log.md`) e implementado de ponta a ponta â€” `DocumentPurgeWorker` real (claim/lease sobre GSI6, apaga o objeto S3 real via `cleanObject`/evidÃªncia â€” nunca `quarantineObject` â€” e a linha `Document`, grava `DocumentPurgeReceipt` nÃ£o sensÃ­vel), agendado a cada 6h, verificado com `terraform plan`/`terraform test` reais contra `dev` (ver `docs/architecture/reviews/w3-06-user-document-purge-design/`). Wave 4 (Identity/RBAC) `PARTIAL` â€” confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃ£o de negÃ³cio jÃ¡ registrada (`AGENTS.md` Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) â€” `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃ£o (2026-08-28) â€” auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard â€” novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados com fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa precisa da Wave 2. Protocolo Claudeâ†”Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃ­cito do Marcelo, acima do 9,0 padrÃ£o) â€” trajetÃ³ria Codex 6.35â†’7.85â†’8.70â†’9.18â†’9.34â†’9.46â†’9.48â†’**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃ§Ã£o D-0xx). EvidÃªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃ£o vale para qualquer teste/drill novo a partir de agora** â€” antes de declarar um teste ou drill "concluÃ­do", checar contra os gates de Â§3 (nunca reversÃ£o silenciosa, nunca claim mais ampla que a evidÃªncia).

**RecomendaÃ§Ã£o da sÃ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃªs gates reais nomeados na sÃ­ntese original â€” guest trust (`GTR-01`/`W5-01`), evidÃªncia operacional (Wave 2), e retenÃ§Ã£o/purga real (`W3-06`) â€” **fecharam todos nesta sessÃ£o**, e todos os itens secundÃ¡rios de Wave 2 (RPO, dedupe pÃ³s-commit, pipeline de alarme de credential-compromise) tambÃ©m fecharam na mesma sessÃ£o. Ãšnico item nÃ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).

954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃ§Ã£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃ§Ã£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃ£o** (W2-06 RPO, W2-05 dedupe pÃ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃ£o, nenhuma aprovada**: (1) D-062, 4 rodadas Claudeâ†”Codex, notas 3,4â†’5,1â†’4,7/10 â€” achado central: garantir "nÃ£o ressurreiÃ§Ã£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃ­ncronos); Claude e Codex recomendaram convergentemente nÃ£o implementar sem esse fence, e a sessÃ£o parou por decisÃ£o do Marcelo. (2) O Marcelo entÃ£o decidiu NÃƒO esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃ­cio â€” D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃ¡ existente e jÃ¡ checado em `resolveRequestContext`) seria apagado pela prÃ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃ³ximo login â€” o mecanismo de autenticaÃ§Ã£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃ­cito do Marcelo para planejamento minucioso numa sessÃ£o dedicada** (nÃ£o abandonado). **PrÃ³xima aÃ§Ã£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃ§Ã£o de levantar superfÃ­cies de escrita por ponto de entrada de runtime â€” API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions â€” nunca por pasta de cÃ³digo, que jÃ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso â€” os 3 gates reais (W3-06/Wave 2/GTR-01) jÃ¡ fecharam.

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃ³rio e passou a ter a primeira linguagem visual real do produto â€” arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃ§Ã£o **Operational Calm** aplicada Ã s 5 superfÃ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION â€” PROVISIONAL PENDING USER VALIDATION` â€” a palavra PROVISIONAL Ã© literal: nenhuma sessÃ£o com usuÃ¡rio aconteceu, e as **15** hipÃ³teses de design (hierarquia de navegaÃ§Ã£o, densidade final, rÃ³tulos, colocaÃ§Ã£o de aÃ§Ãµes secundÃ¡rias, accent, e as duas afirmaÃ§Ãµes subjetivas sobre a prÃ³pria direÃ§Ã£o) estÃ£o registradas como adiamentos explÃ­citos no Â§35 do documento, nÃ£o como fatos. **Zero dependÃªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de Ã­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃ§a estrutural Ãºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃ¢ntica com urgÃªncia e situaÃ§Ã£o em colunas separadas â€” mesmos dados/ordenaÃ§Ã£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃªnticos, 3 grupos de urgÃªncia) foi verificada contra o cÃ³digo real do frontend **pela primeira vez** (antes sÃ³ contra o protÃ³tipo), com asserÃ§Ã£o automatizada. Protocolo Claudeâ†”Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 â€” `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃ§Ã£o normal, entÃ£o o botÃ£o de submit continuava clicÃ¡vel durante a mutaÃ§Ã£o exibindo "Renovandoâ€¦"; e os cabeÃ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃ§am linhas â€” ambos corrigidos com teste de regressÃ£o. O protocolo **nÃ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Â§4) â€” o protocolo mais longo jÃ¡ executado neste repositÃ³rio. TrÃªs rodadas acharam defeitos **criados pela rodada de correÃ§Ã£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃ£o foi cÃ³digo errado e sim **documentaÃ§Ã£o afirmando prova mais ampla que a evidÃªncia nomeada**, que apareceu seis vezes: a alegaÃ§Ã£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃ§Ã£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃµes futuras deste repositÃ³rio. Acessibilidade virou executÃ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃ§Ãµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃ§Ãµes declaradas e registro de decisÃµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃªncia real registrada, nÃ£o bloqueante**: as baselines de regressÃ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃ£o por plataforma, entÃ£o o projeto Playwright `visual` Ã© deliberadamente separado de `npm run test:e2e` e **nÃ£o** estÃ¡ gatilhado no CI hoje (plugÃ¡-lo falharia por baseline ausente, nÃ£o por regressÃ£o real). O caminho de adoÃ§Ã£o estÃ¡ escrito no Â§31 do documento: gravar as baselines num runner Linux, commitÃ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃ© lÃ¡ o CI cobre as mesmas superfÃ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃ©m nÃ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃ­veis no ambiente) â€” `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos â€” Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration â€” sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real â†’ BFF real â†’ API real â†’ persistÃªncia real). Nenhuma rota nova de BFF foi necessÃ¡ria (todas as 6 rotas de item jÃ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo Claudeâ†”Codex (Round B adversarial achou 4 bugs reais â€” 1 S1: corrida TOCTOU real na reaquisiÃ§Ã£o de um registro de idempotÃªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃ§Ã£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃ³ `idempotency.complete()` falhasse depois], hash de renovaÃ§Ã£o ambÃ­guo quando `cycle` Ã© enviado independente de `newDueDate`, bug de fuso horÃ¡rio na formataÃ§Ã£o de data da `Overview` â€” todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃ§Ã£o tambÃ©m foi corrigido um bug real prÃ©-existente de liveness de idempotÃªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) â€” a correÃ§Ã£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃ§ado pela correÃ§Ã£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃ§Ã£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃ§Ã£o), 621 testes de backend â€” nenhuma regressÃ£o. Pendente ao fim desta sessÃ£o: push, PR para `develop`, CI, merge (ver "PrÃ³xima aÃ§Ã£o" abaixo se ainda nÃ£o concluÃ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) â€” todos deployados em `main`/`dev`. **M7 (extraÃ§Ã£o/OCR) teve implementaÃ§Ã£o iniciada em 2026-08-25** (D-057, decisÃ£o direta do Marcelo â€” "prossiga entÃ£o") â€” ver seÃ§Ã£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃ£o disparou.

**Full BFF (autenticaÃ§Ã£o de browser)**: design fechado via protocolo Claudeâ†”Codex em duas rodadas â€” D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` â€” 76 testes unitÃ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃ§Ã£o de frontend de produÃ§Ã£o real (`frontend/` â€” Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃ¡rio/componente + 6 smoke E2E via Playwright). Protocolo Claudeâ†”Codex completo (Rodada D levou **6 passagens** atÃ© convergir â€” 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃ³digo prÃ©-correÃ§Ã£o: consumo nÃ£o-atÃ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃ£o revogada concorrentemente; releituras pÃ³s-refresh nÃ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃ£o checando expiraÃ§Ã£o/idle-TTL antes de agir; idle-TTL da prÃ³pria `Session` nunca validado contra o relÃ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Âº-8Âº), cada um fechado via protocolo Claudeâ†”Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` â€” papÃ©is, JTBD, inventÃ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` â€” modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` â€” 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` â€” 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes Surfaceâ†”Journey/Concept/Transition. RevisÃ£o Codex achou 3 furos reais de mesma causa raiz â€” `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃ§a em `SCANNING`, nÃ£o sÃ³ em `CLEAN`) â€” todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` â€” wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃ£o Codex achou 5 furos reais + 1 divergÃªncia factual â€” aÃ§Ã£o primÃ¡ria ambÃ­gua em 4 coleÃ§Ãµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas â€” todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃ³digo executÃ¡vel real, HTML/CSS/JS sem dependÃªncias) â€” protÃ³tipo navegÃ¡vel cobrindo as 17 superfÃ­cies e J-01â€“J-08 com 34 Prototype Scenario IDs determinÃ­sticos, verificado com testes automatizados de navegador headless. RevisÃ£o Codex achou 6 furos reais de 4 causas raiz â€” compressÃ£o de estados no guest upload (faltava validaÃ§Ã£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃ§Ã£o de coleta externa violando epistemic integrity (anunciava verificaÃ§Ã£o de seguranÃ§a concluÃ­da ao operador, que nÃ£o pode observar isso), dois campos sem `<label>`, e reincidÃªncia de menÃ§Ã£o a WhatsApp â€” todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` â€” avaliaÃ§Ã£o do protÃ³tipo **executÃ¡vel** (nÃ£o sÃ³ a documentaÃ§Ã£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃ¢ntica/forms testados em navegador headless real, nÃ£o sÃ³ lidos), `axe-core` (0 violaÃ§Ãµes em todos os estados testados, nas duas rodadas), re-execuÃ§Ã£o de J-01â€“J-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** Claudeâ†”Codex (nÃ£o as 2-3 rodadas tÃ­picas de etapas anteriores): Rodada A (autoavaliaÃ§Ã£o, 9 achados corrigidos) concluiu aprovaÃ§Ã£o â€” **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃ£o vistos pela autoavaliaÃ§Ã£o: o mais grave, a prÃ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃ§Ã£o em 3 formulÃ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃ§Ã£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃ¡-los no fake-backend â€” uma violaÃ§Ã£o epistÃªmica real, nÃ£o cosmÃ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃ³prio ao manter um texto "SIMULATED FOR UX VALIDATION â€” NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃ­cie pÃºblica de guest (jargÃ£o tÃ©cnico visÃ­vel a um usuÃ¡rio externo nÃ£o-tÃ©cnico). Rodada D (nova adversarial sobre o cÃ³digo jÃ¡ corrigido) achou essa lacuna de conteÃºdo e uma correÃ§Ã£o incompleta nova (`submitAlert` reabilitava o botÃ£o sÃ³ na branch de erro, nÃ£o na de sucesso) â€” ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃ£o arredondado; calculado refletindo esse histÃ³rico de 4 rodadas, nÃ£o sÃ³ o estado final do cÃ³digo) â€” `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` â€” "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃ£o) no protÃ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃ§Ã£o, nunca marcado como backend-resolvido; cenÃ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃ§Ãµes) que achou e corrigiu uma falta real de ordenaÃ§Ã£o por urgÃªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃ£o sÃ³ no protÃ³tipo) â€” `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃ©tricas de validaÃ§Ã£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃ¡ em uso desde a 1Âª etapa, agora formal, nenhum critÃ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃ§Ã£o tÃ©cnica em Participant Mode, teste de idempotÃªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃµes de documentaÃ§Ã£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃ©-existentes desde a Rodada A) â€” corrigidos no fechamento, zero contaminaÃ§Ã£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** â€” **fechado nesta sessÃ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` â€” `DocumentService.listDocuments`/`getDocument`, autorizaÃ§Ã£o `document:read` (aÃ§Ã£o jÃ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃ§Ã£o do item jÃ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` â€” `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃ§Ã£o `requirement:read` jÃ¡ existente (sem action nova reservada de antemÃ£o, diferente de `document:read`), mesma leitura por partiÃ§Ã£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform â€” nÃ£o Ã© desta sessÃ£o, jÃ¡ estava corrigido antes (comentÃ¡rio no prÃ³prio `main.tf`), sÃ³ confirmado que segue correto.

  Ambas as metades: mudanÃ§a mecÃ¢nica sobre um padrÃ£o de acesso jÃ¡ aprovado no data model â€” nÃ£o passaram pelo protocolo `AGENTS.md` Â§4 completo (nÃ£o Ã© decisÃ£o de arquitetura nova, Ã© fechamento de gap de leitura jÃ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃ­cio da sessÃ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃ£o. **Achado real corrigido depois, na mesma sessÃ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` â€” mesma classe de gap que as rotas de `DocumentRequest` jÃ¡ tiveram antes (comentÃ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** â€” **implementaÃ§Ã£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃ§Ã£o abaixo). Antes desta branch, a materializaÃ§Ã£o automÃ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃ§Ã£o/ediÃ§Ã£o de item. PendÃªncia real restante: decisÃ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) â€” nÃ£o bloqueia mais nada alÃ©m dessa decisÃ£o em si.
- **BLOCKER-C** â€” **decisÃ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃ£o humana explÃ­cita)**, confirmando a hipÃ³tese lÃ­der jÃ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Â§22, `interface-conceptual-model-and-information-architecture.md` Â§37, `interface-critical-user-journeys.md` Â§37 â€” esses documentos continuam como registro histÃ³rico do debate, nÃ£o reescritos, per `AGENTS.md` regra de precedÃªncia de `history/`/artefatos aprovados). InvestigaÃ§Ã£o confirmou que Variante A (fechamento automÃ¡tico) nÃ£o Ã© viÃ¡vel hoje: `DocumentSubmission` nÃ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃ§a a qual `ExpirationItem` uma submissÃ£o pertence â€” decidir automaticamente seria adivinhar, nÃ£o resolver. **Mecanismo de backend para a Variante B jÃ¡ existe, nenhum cÃ³digo novo necessÃ¡rio**: visibilidade do operador sobre submissÃµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃ§Ã£o jÃ¡ existente (`POST .../link`, prÃ©-existente). **Frontend implementado na mesma sessÃ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject â€” `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores â†’ detalhe com requisitos â†’ "Revisar" expande evidÃªncia enviada (`DocumentSubmission`) â†’ operador informa o `itemId` de um vencimento jÃ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃ§Ã£o de subject nem sugestÃ£o automÃ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃ³tulos seguem a mesma disciplina de Epistemic Integrity jÃ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃ§a) â€” conteÃºdo nÃ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta Ãºltima etapa: **`GTR-01`** (identidade do solicitante nÃ£o exposta ao guest) agora **simulada** no protÃ³tipo para User Validation, mas ainda nÃ£o resolvida no backend â€” `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃªncia) foi **resolvido no backend real** â€” `createItem` ganhou `idempotencyKey` opcional â€” e, na sessÃ£o do Full BFF/frontend, o `requestHash` de detecÃ§Ã£o de payload divergente foi migrado de concatenaÃ§Ã£o por delimitador (colisÃ£o real possÃ­vel) para JSON canÃ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` â€” a lacuna "nenhum frontend/BFF real envia o header" estÃ¡ fechada para o caminho de criaÃ§Ã£o de item.

**PrÃ³xima etapa natural do design** (ainda nÃ£o iniciada): **User Validation** â€” usando o protÃ³tipo jÃ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode Ã© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Â§15-18/Â§23-24 (tese de produto, mÃ©tricas de validaÃ§Ã£o, tarefas candidatas derivadas de outcomes, limitaÃ§Ãµes conhecidas do protÃ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃ£o produzido ainda. NÃ£o obriga inÃ­cio imediato de Visual Design/Design System.

## BLOCKER-B â€” implementaÃ§Ã£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃ£o de 2026-08-24/25 (continuaÃ§Ã£o apÃ³s a sessÃ£o anterior ter sido abortada por limite de tokens sÃ³ com reconhecimento parcial â€” handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo Claudeâ†”Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` â€” esse documento Ã© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃ§Ã£o real (nÃ£o sÃ³ design) tambÃ©m revisada via protocolo Claudeâ†”Codex (2 rodadas, nota final 9,2/10, `APPROVED` â€” achados reais corrigidos: reclassificaÃ§Ã£o de exceÃ§Ã£o em dois mÃ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃ§Ã£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃ§Ã£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃªs novos mÃ©todos de reconciliaÃ§Ã£o em `ReminderMaterializer` (fenced por versÃ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃ§Ã£o: o "caminho genÃ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃ³digo â€” `router_queue` nÃ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Â§8). **Implementado nesta sessÃ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃ§Ã£o da criaÃ§Ã£o do item (sem `ConditionCheck` de existÃªncia â€” o item novo Ã© garantido pela prÃ³pria transaÃ§Ã£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃ³ `ExpirationItem` (mudanÃ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" â€” reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃ£o estava instalado no inÃ­cio desta sessÃ£o (`npm ci` rodado para poder validar) â€” ambiente, nÃ£o cÃ³digo.

**PrÃ³xima aÃ§Ã£o**: **M7 (extraÃ§Ã£o/OCR) â€” todos os 9 itens numerados originais da lista estÃ£o CONCLUÃDOS (2026-08-26)**, cÃ³digo+infra reais para itens 1-9, nÃ£o sÃ³ camada pura â€” ver as seÃ§Ãµes dedicadas "Item N â€” CONCLUÃDO" na seÃ§Ã£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) â€” causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃ­ficos com `States.ALL` num Ãºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃ£o pega esse erro, sÃ³ a chamada real de criaÃ§Ã£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** â€” o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃ§Ã£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** â€” a nota de uma sessÃ£o anterior dizendo "nenhum profile corresponde Ã  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃµes de escrita necessÃ¡rias e a **verificaÃ§Ã£o E2E real foi executada** (ver `## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃ§a de Terraform foi cogitada para isso â€” a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃ³ o gatilho automÃ¡tico via EventBridge Ã© que Ã© gated). **RecorrÃªncia fechada na mesma sessÃ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` â€” a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃ³ descoberto no meio de um `terraform apply` real. NÃ£o requer credencial de escrita (Ã© uma chamada de validaÃ§Ã£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃ£o retomar sem sinal explÃ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA â€” etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA â€” etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃ¡ aprovada em nÃ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃ¡vel por hash; `implementation-blueprint.md` Â§12/Â§23: CSP com hashes estÃ¡ticos calculados no build, sem nonce dinÃ¢mico â€” Day 0 nÃ£o tem compute de borda). O que falta Ã© o desenho de implementaÃ§Ã£o real e o cÃ³digo â€” nenhuma decisÃ£o de produto pendente, mas hÃ¡ **uma decisÃ£o de arquitetura real ainda em aberto** que a prÃ³xima sessÃ£o precisa resolver antes de implementar (Type 1, nÃ­vel 5-6 da escala de risco â€” protocolo `AGENTS.md` Â§4 aplicÃ¡vel):

1. **DecisÃ£o de arquitetura a fechar primeiro â€” FECHADA nesta sessÃ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo Claudeâ†”Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** â€” `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` Ã© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma Ãºnica distribution, behaviors `/bff` e `/bff/*` â†’ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior â†’ S3 com OAC + CloudFront Function de SPA routing (sÃ³ nesse behavior, nunca no do BFF). Zero mudanÃ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃ©-existentes descobertos pela revisÃ£o, a aplicar na implementaÃ§Ã£o: `proxy-service.ts` nÃ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃ£o implementado): acesso direto ao `execute-api` pÃºblico precisa de mitigaÃ§Ã£o (header estÃ¡tico + WAF) antes de produÃ§Ã£o pÃºblica real.
2. **MÃ³dulo Terraform `infra/modules/spa-hosting` â€” IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃ­citos â†’ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default â†’ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃ©todo GET/HEAD, associada SÃ“ ao default behavior â€” nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃ©-existentes do ADR-0011 tambÃ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃ§Ã£o errada do prÃ³prio ADR-0011): `var.spa_content_security_policy` nÃ£o precisa de hashes calculados por build â€” verificado contra o `frontend/dist/index.html` real, o build do Vite nÃ£o emite nenhum `<script>`/`<style>` inline, todo asset Ã© carregado por URL externa jÃ¡ hasheada, entÃ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃ¡ existe em `frontend/index.html`) bastam sem hash nenhum â€” a variÃ¡vel ganhou um default determinÃ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃ£o consegue expressar). **MÃ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) â€” corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃ£o bloqueia o primeiro apply, sÃ³ o domÃ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro â€” confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃ£o sÃ³ contra mocks.
3. **Deploy imutÃ¡vel por hash â€” IMPLEMENTADO (2026-08-25)**: Vite jÃ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra Ã© necessÃ¡rio (o hash no nome do arquivo jÃ¡ Ã© o mecanismo de imutabilidade). `index.html` Ã© tratado Ã  parte (nÃ£o hasheado, `Cache-Control: no-cache`, upload por Ãºltimo).
4. **IntegraÃ§Ã£o com `cd.yml` â€” IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃ£o um job novo) â€” `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** â€” assets antigos nunca sÃ£o removidos, Ã© isso que mantÃ©m um rollback de `index.html` funcional, jÃ¡ que as referÃªncias antigas continuam servÃ­veis), upload de `index.html` por Ãºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃ¡ existente. **PendÃªncia real registrada, nÃ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃ³ cobre Lambdas â€” nÃ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃ£o `rollback.yml` (mecanismo existente) nÃ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃ£o bloqueia o deploy normal.
5. **VerificaÃ§Ã£o real (Camada 3) â€” CONCLUÃDA (2026-08-25), achou e corrigiu um bug real de produÃ§Ã£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** â€” isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃ­tica fixa nÃ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃ§Ã£o, tudo confirmado contra a distribution real:
   - `GET /` â†’ 200, `text/html`.
   - `GET /assets/index-*.js` â†’ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃ£o, V6 do ADR-0011) â†’ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) â†’ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) â€” nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) â†’ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃ£o (V4) â†’ 401 `application/json` (`AUTH_REQUIRED`) â€” nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃ£o (V5-404) â†’ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado â€” o caso 404-por-allowlist com sessÃ£o vÃ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃ£o foram exercitados por nÃ£o serem automatizÃ¡veis via `curl` sozinho; a propriedade crÃ­tica de seguranÃ§a â€” nenhum erro do BFF vira HTML da SPA â€” jÃ¡ estÃ¡ confirmada pelos casos acima).
   - Headers de seguranÃ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy â€” exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃ£o sÃ³ via `terraform plan`.
   **PendÃªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃ£o lidos via CloudFront/OAC (sÃ³ por IAM de role de Lambda), entÃ£o nÃ£o tÃªm este bug â€” mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃ£o corrigido preventivamente nesta sessÃ£o (fora do escopo desta decisÃ£o, nenhum uso real hoje), sÃ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃ§Ã£o/OCR) â€” implementaÃ§Ã£o iniciada (2026-08-25, D-057)

Design jÃ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) â€” esta seÃ§Ã£o Ã© sobre a IMPLEMENTAÃ‡ÃƒO, o design nÃ£o muda. **Escopo Ã© grande** (Step Functions, Textract assÃ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃ§Ã£o) â€” vai continuar por mÃºltiplas sessÃµes, nÃ£o Ã© razoÃ¡vel fechar num Ãºnico slice. Estado exato ao fim desta sessÃ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Â§4 ganhou a classe `EXTRACTION_TRANSIENT` â€” prÃ©-requisito de design que o prÃ³prio D-035 exige estar registrado antes de qualquer implementaÃ§Ã£o (nÃ£o Ã© pendÃªncia pÃ³s-produÃ§Ã£o).
2. MÃ³dulo novo `src/modules/extraction/domain/` (sÃ³ domÃ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` â€” `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃ³pria chave de idempotÃªncia da execuÃ§Ã£o).
   - `extracted-field.ts` â€” `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Â§12.5.
   - `field-schema.ts` â€” schema v1 com sÃ³ `expirationDate` (Ãºnico campo que o design aprovado nomeia concretamente â€” deliberadamente nÃ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` â€” `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` â€” helper de safety net de 24h para o artefato transitÃ³rio (a exclusÃ£o real Ã© explÃ­cita por `ExtractionValidationTaskHandler`, este helper Ã© sÃ³ a rede de seguranÃ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` â€” **ainda sem PR/merge para `main`**, dado que nÃ£o hÃ¡ nenhuma infra/runtime ativÃ¡vel ainda, nada a fazer deploy).

**Item 1 â€” CONCLUÃDO nesta sessÃ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃ§Ã£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃ³dulo corrigido pelo design â€” transversal, nÃ£o `extraction-appconfig`, jÃ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃªs defaults `false` (fail-closed, prÃ©-condiÃ§Ãµes externas de M7 nÃ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Â§1.6, default `false`) â€” gate Terraform separado do pipeline inteiro, ainda nÃ£o referenciado por nenhum recurso (sÃ³ os workers futuros vÃ£o condicionar-se a ele). 2 `terraform test` do mÃ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) â€” CAMADA PURA CONCLUÃDA nesta sessÃ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃ£o que o item 0 seguiu: domÃ­nio+aplicaÃ§Ã£o primeiro, depois handler Lambda/infra numa fatia posterior â€” deliberado, nÃ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` â€” cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` â€” entidade `TextractJob` (chave prÃ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃ£o tenant-scoped â€” `COMPLETE_OCR` sÃ³ tem `jobId` disponÃ­vel da notificaÃ§Ã£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃ§Ã£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` â€” classificador heurÃ­stico puro (magic bytes > extensÃ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃ§a) quando nenhum sinal Ã© conclusivo â€” quem decide que isso Ã© `UnsupportedDocumentTypeError` Ã© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed Ã© responsabilidade do caller, nÃ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃ©todo de delete â€” a exclusÃ£o do artefato Ã© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) â€” orquestra classificaÃ§Ã£o â†’ kill switch `OCR` (fail-closed) â†’ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` â€” um retry do mesmo run encontra `QuotaExceededError` contra a prÃ³pria reserva anterior e trata como "jÃ¡ reservado", nÃ£o como falha real) â†’ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃ­stico) â†’ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` â€” retorna void, a execuÃ§Ã£o do Step Functions fica parada no `taskToken` atÃ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) â€” busca `TextractJob` por `jobId`; caso Ã³rfÃ£o (nenhum job encontrado) faz UMA chamada de confirmaÃ§Ã£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃµe um mÃ©todo de delete); `taskTokenCiphertext` Ã© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃ§ado, sem tocar no registro) em qualquer outro erro â€” a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto â€” `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃ£o Ã© revalidado como JSON aninhado, mesma convenÃ§Ã£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` â€” `RunTextract` Ã© o Ãºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃ£o existem.
- `infra/modules/extraction-workflow/` â€” mÃ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃ§Ã£o + role de execuÃ§Ã£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒO chamado em `infra/main.tf`** â€” um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃ£o sÃ³ em runtime), e os 3 handlers dos itens 5-7 nÃ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃ³prio diretÃ³rio do mÃ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃ³dulo especificamente â€” pendÃªncia explÃ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos Ã  mÃ£o (sem `vi.mock`), cobrindo: classificaÃ§Ã£o por magic-bytes/extensÃ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃ§Ã£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃªncia da reserva de quota sob retry do mesmo run; retry local de persistÃªncia do `TextractJob` (sucesso apÃ³s 1 falha, esgotamento apÃ³s N); `completeOcr`: job Ã³rfÃ£o (com e sem falha na confirmaÃ§Ã£o), jÃ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃ§Ã£o multi-pÃ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃ³rio de `SendTask*` relanÃ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃ£o expÃµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na Ã¡rvore inteira de `infra/`.

**Item 4 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃªs decisÃµes pendentes acima foram todas tomadas e implementadas (nÃ£o deixadas como pendÃªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃ£o final** (nÃ£o a extensÃ£o via layer) â€” `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/Ãºltimos flags conhecidos em memÃ³ria do processo. Motivo: a extensÃ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` â€” nÃ£o existe nenhum precedente disso no repo (sÃ³ `adot_layer_arn`, uma layer diferente), e este handler sÃ³ lÃª os flags uma vez por invocaÃ§Ã£o de `START_OCR` (nÃ£o por pÃ¡gina/bloco), entÃ£o o custo/latÃªncia de uma chamada de API direta por invocaÃ§Ã£o Ã© equivalente ao que a extensÃ£o teria numa invocaÃ§Ã£o fria de qualquer forma. Revisitar sÃ³ se o volume tornar isso um custo real (a extensÃ£o amortiza entre invocaÃ§Ãµes warm via cache local, este adapter sÃ³ amortiza dentro de uma sessÃ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃ£o reuso da CMK do BFF â€” mesma disciplina D-054: um token de callback do Step Functions Ã© uma credencial viva tÃ£o sensÃ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃ³dulos independentemente deployÃ¡veis acoplaria rotaÃ§Ã£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃ³dulo, mesma razÃ£o do port).
- **TÃ³pico SNS de conclusÃ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar â€” distinta da role de execuÃ§Ã£o do prÃ³prio Lambda.

Real (cÃ³digo + infra, nÃ£o sÃ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃ§Ã£o real `TaskTimedOut`/`TaskDoesNotExist`â†’`TERMINAL_QUIET`, `InvalidToken`â†’`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃ§ado), `AppConfigFeatureFlagsReader` â€” todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` â€” um Ãºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃ§Ã£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃ£o SSE-KMS â€” evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃ³pico SNS, permissÃ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃ¡ escopada Ã  ARN determinÃ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** â€” mesma lÃ³gica do item 2: todo recurso aqui Ã© inerte sozinho, porque `StartDocumentTextDetection` (a Ãºnica chamada paga) sÃ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27â†’28 funÃ§Ãµes (2 asserts).
- 11 testes novos (794 totais no backend) â€” sÃ³ para os dois adapters com lÃ³gica real de branching (`SfnTaskTokenSender`'s classificaÃ§Ã£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃ£o ganharam teste unitÃ¡rio dedicado, seguindo o MESMO precedente jÃ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃ©m nÃ£o tÃªm) â€” verificados via `terraform plan` real contra `dev`, nÃ£o via mock unitÃ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target â€” 0 erros, 0 destroys inesperados (sÃ³ o prÃ³prio slice + o ruÃ­do de republish de versÃ£o jÃ¡ prÃ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) â€” **ainda sem PR/merge para `main`** no momento em que este parÃ¡grafo foi escrito (abrir o PR Ã© o prÃ³ximo passo imediato desta mesma sessÃ£o, ver "PrÃ³xima aÃ§Ã£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 â€” a ASL's `RunDeterministicParser` Ã© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃ£o hÃ¡ task token nenhum neste handler), entÃ£o Ã© uma Ãºnica invocaÃ§Ã£o sÃ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` â€” `extractExpirationDateCandidates()`, regex determinÃ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃ¡ a atÃ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃ§a fraca (0.5, abaixo do threshold) quando nÃ£o hÃ¡ palavra-chave prÃ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃ©rico que trunca o texto processado â€” a Ãºnica reutilizaÃ§Ã£o real de M6 possÃ­vel aqui, jÃ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃ£o sobre texto OCR, e nunca extrai campos/datas (sÃ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃ³digo `DeterministicParserFailed`) â€” nÃ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃ¡ Ã© o genÃ©rico `States.ALL`.
- AplicaÃ§Ã£o nova `src/modules/extraction/application/run-deterministic-parser.ts` â€” `runDeterministicParser()`: lÃª o artefato OCR (quando presente) â†’ extrai candidatos de `expirationDate` via `getFieldSchema()` â†’ chama `needsBedrock()` (item 0) â†’ lÃª o kill switch `AI_EXTRACTION` (fail-closed) â†’ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** â€” retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃ¡ implica `true`) e a decisÃ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃ£o duplicada) `OcrArtifactStore` ganhou `get()` â€” o item 4 sÃ³ tinha `put()` (quem escreve o artefato); o item 5 Ã© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada â€” sÃ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃ§Ã£o, nÃ£o uma decisÃ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃ£o tinha `ResultPath` na transiÃ§Ã£o de sucesso do ASL, entÃ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃ­a inteiramente** o `$` do Step Functions â€” todo o contexto original da execuÃ§Ã£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃ£o sÃ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃ³prio `job`) ao payload de sucesso. MudanÃ§a aditiva/reversÃ­vel sem migraÃ§Ã£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃ³rico real).
- **DECISÃƒO PENDENTE (registrada aqui, default conservador aplicado)**: o design (Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃ£o implementado â€” nenhum campo de metadado de PDF (data de criaÃ§Ã£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃ£o de produto nÃ£o revisada, nÃ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃ§Ã£o na prÃ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` â€” nÃ£o muda nenhum `Next`/fluxo, sÃ³ corrige o path de leitura para bater com a semÃ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃ§Ã£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` â€” sÃ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` â€” invocaÃ§Ã£o sÃ­ncrona Ãºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` â€” sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃ¡ escopado Ã  ARN determinÃ­stica da state machine futura (mesmo padrÃ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28â†’29 funÃ§Ãµes (3 asserts: nomes, distinÃ§Ã£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃ§Ãµes dia/mÃªs impossÃ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar â€” sÃ³ republish de versÃ£o prÃ©-existente em todas as Lambdas, mesmo ruÃ­do que o item 4 jÃ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` â€” drift **prÃ©-existente do item 4**, nÃ£o introduzido nesta sessÃ£o, causado por uma normalizaÃ§Ã£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃ¡ detectava antes desta sessÃ£o comeÃ§ar; nÃ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 â€” ambos concluÃ­dos em sessÃµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ â€” **CONCLUÃDO 2026-08-26**, ver "Item 3 â€” CONCLUÃDO nesta sessÃ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ â€” **CONCLUÃDO 2026-08-26**, ver seÃ§Ã£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` â€” sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃ§Ã£o, na prÃ³pria ASL** (nÃ£o uma decisÃ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` â€” `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta â€” sem isso, `BedrockExtractionTaskHandler` nÃ£o teria como saber onde ler o texto OCR (o design Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃ§Ã£o de contexto que `completeOcr` (item 4/5) jÃ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃ³digo + infra, nÃ£o sÃ³ camada pura)**:
- DomÃ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` â€” `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃ¡vel, nunca concatenada com input do usuÃ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` â€” nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃ³ `textArtifact` â€” tipo local `BedrockTextArtifactRef`, estruturalmente idÃªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃ­be domÃ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) â€” nenhum import do SDK Bedrock fora do adapter.
- AplicaÃ§Ã£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) â€” re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃ¡ filtra isso mas a chamada paga nunca confia sÃ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃ£o hÃ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz â€” nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) â€” Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃªncia de tool-call, tool errado) â€” qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃ§Ã£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` â€” DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃ£o configurÃ¡vel via `bedrock_region`, distinta da regiÃ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` â€” invocaÃ§Ã£o sÃ­ncrona Ãºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃ£o Bedrock: configurÃ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) â€” design Â§4 bloqueia sÃ³ a escolha/validaÃ§Ã£o real para produÃ§Ã£o, nunca a testabilidade em `dev`; a permissÃ£o IAM jÃ¡ Ã© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota â€” mesmo padrÃ£o do item 4 â€”, `feature_flags_read_policy_json`, `s3:GetObject` sÃ³ leitura no bucket `EXTRACTION_TRANSIENT` â€” nunca escrita, mesma disciplina de blast-radius do item 5 â€”, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃ¡ escopado Ã  ARN determinÃ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29â†’30 funÃ§Ãµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 â€” caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃ§Ã£o de quota em falha, **14Âº caso adversarial de custo-abuso**: execuÃ§Ã£o retentada/duplicada do mesmo `runId` reserva contra a prÃ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 â€” corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Â§1.11) nÃ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) â€” o corpus construÃ­do cobre as mesmas classes de ameaÃ§a descritas ali (injeÃ§Ã£o via conteÃºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Âº caso de custo-abuso) a partir de princÃ­pios, documentado no comentÃ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar â€” os do item 5 ainda nÃ£o aplicado + os do item 6 â€”, 56 a mudar â€” mesmo ruÃ­do de republish de versÃ£o jÃ¡ documentado nos itens 4/5 â€”, 1 destroy-and-recreate â€” o MESMO drift prÃ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃ£o introduzido nesta sessÃ£o). **Nunca `terraform apply` local**.
- **DecisÃ£o pendente registrada, nÃ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃ©m de `expirationDate`, entÃ£o o schema fechado do tool `submit_extraction` sÃ³ tem essa uma propriedade â€” `buildSubmitExtractionToolSchema()` jÃ¡ Ã© genÃ©rico sobre `FIELD_SCHEMA_V1`, entÃ£o um campo novo no schema nÃ£o precisa de mudanÃ§a de cÃ³digo aqui, sÃ³ uma nova versÃ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) â€” todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃ¡gio, design Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃ£o mais importante desta sessÃ£o â€” a limpeza do artefato transitÃ³rio (design Â§3, "achado mais sÃ©rio")**: `OcrArtifactStore.delete()` Ã© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃ³fica do parser determinÃ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` â€” design cita esse estado explicitamente como ponto de deleÃ§Ã£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` â€” rodam antes do run alcanÃ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado â€” zero atÃ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` â€” `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃ£o-vazio. Um candidato invÃ¡lido nunca Ã© descartado silenciosamente â€” Ã© marcado `valid: false` e tratado como "essa fonte nÃ£o produziu nada" na comparaÃ§Ã£o.
- `compare-extractors.ts` â€” `compareExtractors(candidates)`: 0 candidatos usÃ¡veis â†’ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` â€” jÃ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` â€” nÃ£o representa separadamente; decisÃ£o registrada aqui em vez de alargar o tipo jÃ¡ commitado). 1 candidato â†’ `SINGLE_SOURCE` com valor. 2+ concordando â†’ `MATCH` (confianÃ§a = a maior das duas). 2+ discordando â†’ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" â€” `candidateValue` carrega sÃ³ o de maior confianÃ§a, para exibiÃ§Ã£o, mas Ã© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` â€” `decideFieldOutcome(comparison)`: sem `candidateValue` â†’ `PENDING_CONFIRMATION`; `MISMATCH` â†’ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) â†’ `PENDING_CONFIRMATION`; senÃ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) â†’ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** â€” esse estado sÃ³ Ã© alcanÃ§Ã¡vel pela rota HTTP de rejeiÃ§Ã£o humana (item 8, ainda nÃ£o implementada).

**DecisÃ£o de arquitetura tomada nesta sessÃ£o (nÃ£o estava no design, resolvida por inferÃªncia direta do enum jÃ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃ£o tem um estado "aguardando confirmaÃ§Ã£o" separado â€” entÃ£o a distinÃ§Ã£o "run completa vs. precisa de revisÃ£o humana" vive inteiramente no nÃ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃ¡lido â†’ comparaÃ§Ã£o â†’ persistÃªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃ­da, hoje sÃ³ para log/observabilidade â€” nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` Ã© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho â€” o prÃ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa Ãºnica operaÃ§Ã£o, porque Ã© o Ãºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, Ã© uma decisÃ£o de produto pequena e reversÃ­vel (mudar sÃ³ a decisÃ£o de estado, nÃ£o a mecÃ¢nica de comparaÃ§Ã£o/persistÃªncia) â€” registrado aqui para reabrir se necessÃ¡rio.

**Descarte por exclusÃ£o concorrente (design Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃ£o do `Document` lida nÃ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃ£o sÃ³ um "ler status DELETED e torcer"). Se o `Document` jÃ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃ§Ã£o â€” chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃ§Ã£o falhar dentro da transaÃ§Ã£o (mudanÃ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃ§Ã£o como descarte â€” decisÃ£o conservadora documentada no comentÃ¡rio do adapter, jÃ¡ que distinguir a causa exata do cancelamento por cÃ³digo de erro do SDK seria frÃ¡gil entre versÃµes).

**Ports/persistÃªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) â€” expÃµe SÃ“ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num Ãºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` Ã© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` â€” mÃ©todo standalone, usado SÃ“ no caminho de descarte (onde nÃ£o hÃ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` â€” **mudanÃ§a de contrato deliberada**: o item 4 tinha deixado esse mÃ©todo deliberadamente FORA do port ("a exclusÃ£o do artefato Ã© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" â€” tornava a exclusÃ£o acidental do chamador errado estruturalmente impossÃ­vel). Agora adicionado porque este Ã‰ o chamador certo â€” `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃ³pria API do S3, deletar uma key ausente nÃ£o lanÃ§a). Os testes que antes afirmavam "o port nem expÃµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃ¡ que o mÃ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃ¡-lo).

**AplicaÃ§Ã£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃ§Ãµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um Ãºnico `ValidationContext` que evolui atravÃ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃ£o exportado) que encapsula a leitura do `Document`+decisÃ£o discard-ou-commit. Um erro genuÃ­no de commit (nÃ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` â€” o `Catch`/`Retry` da ASL trata como transitÃ³rio).

**Achado real corrigido nesta sessÃ£o, em cÃ³digo do item 6 jÃ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` â€” a MESMA classe de bug que o item 5 jÃ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Â§3's "achado mais sÃ©rio" Ã© sobre limpeza de artefato, mas essa categoria de "output nÃ£o reanexa o contexto anterior" jÃ¡ apareceu 3 vezes agora: item 4â†’5, item 5â†’6, e este item 6â†’7). Corrigido de forma aditiva (campos novos sÃ³ ecoam o que jÃ¡ vinha em `input`, sem recomputar nada) â€” os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃ§Ã£o (nÃ£o fazem `toEqual` no objeto inteiro). **PadrÃ£o a vigiar em qualquer implementaÃ§Ã£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃ³ximo estado vai precisar, nunca assumir que "sÃ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃ©m ganhou `ResultSelector`/`OutputPath` (nÃ£o tinha nenhum antes â€” outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` â€” o footprint mais estreito dos quatro Lambdas de extraÃ§Ã£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` â€” este Ã© o Ãºnico Lambda do pipeline inteiro com permissÃ£o de deletar lÃ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma Ãºnica `aws_lambda_permission` cobre as cinco invocaÃ§Ãµes (todas vÃªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30â†’31 funÃ§Ãµes (3 asserts).

**VerificaÃ§Ã£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃ­cio da sessÃ£o â€” nÃ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃ£o, nÃºmero incorreto de digitaÃ§Ã£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃ§Ã£o do artefato descrito acima), mais 16 testes prÃ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir â€” o Ãºnico destroy Ã© o MESMO drift prÃ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃµes dos itens 5/6, nÃ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃ£o de produto pendente registrada, nÃ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` â€” isso Ã© o item 8 (rotas de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7), ainda nÃ£o implementado. AtÃ© lÃ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃ£o hÃ¡ nenhuma forma de um operador vÃª-lo ou agir sobre ele pela API.

**Item 3 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora Ã© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃ¡ existentes (items 4-7) e uma role de execuÃ§Ã£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` â€” o mesmo nome que `local.extraction_state_machine_arn` jÃ¡ esperava desde o item 2, verificado por `terraform test` (nÃ£o confiado sÃ³ Ã  leitura do cÃ³digo).

- **IAM da role de execuÃ§Ã£o do prÃ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado Ã s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃªncia documentada da prÃ³pria AWS para `logging_configuration` de state machine â€” opera no subsistema de log delivery a nÃ­vel de conta, nÃ£o num log group especÃ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃ§Ãµes que `AWSXRayDaemonWriteAccess`, que o mÃ³dulo `lambda-function` jÃ¡ anexa a toda funÃ§Ã£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃ¡-lo, escopado Ã  ARN determinÃ­stica do state machine) jÃ¡ estava pronto desde os itens 4-7 â€” nÃ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃ§Ã£o 30 dias, mesmo padrÃ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃ­vel `ERROR` (nÃ£o `ALL`) com `include_execution_data = false` â€” decisÃ£o deliberada espelhando a disciplina de privacidade do prÃ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃ§Ã£o pode carregar texto OCR/valores de campo extraÃ­dos; nunca devem cair num log group com retenÃ§Ã£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` â€” todo Lambda do repo jÃ¡ roda com ADOT/X-Ray ativo (AGENTS.md Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` â€” o state machine, sua role e as 4 permissÃµes de invocaÃ§Ã£o sempre existem (inspecionÃ¡veis/deployÃ¡veis). RaciocÃ­nio registrado no comentÃ¡rio do prÃ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃ¡ gateia o ÃšNICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que Ã© quem chama `StartExecution`) â€” com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃ© ser iniciado manualmente para uma execuÃ§Ã£o de teste, mas nunca recebe trÃ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) â€” 13â†’15 passando. MÃ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃªncia explÃ­cita registrada pelo item 7) â€” 5 `run` via `mock_provider`+`apply` (substituiÃ§Ã£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃ¡s; `type = STANDARD`; role passada atravÃ©s sem modificaÃ§Ã£o; tracing/logging configurados) â€” 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) â€” **a verificaÃ§Ã£o crÃ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` â†’ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" â€” a definiÃ§Ã£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** â€” o Ãºnico destroy Ã© o MESMO drift prÃ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃ§Ã£o de `function_name` com/sem qualificador `:live`), jÃ¡ documentado nas sessÃµes dos itens 5/6/7, nÃ£o introduzido aqui. Nenhuma issue de integraÃ§Ã£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃ­ram corretamente â€” nÃ£o foi necessÃ¡rio nenhum fix no `replace()` do mÃ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃ£o â€” sessÃ£o nÃ£o tocou cÃ³digo de aplicaÃ§Ã£o, sÃ³ infra). `terraform fmt -check -recursive` limpo em toda a Ã¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃ£o precisar de `command = apply` contra o provider real). Novo output no mÃ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃ£o (branch `develop`, PR #60 `developâ†’main` ainda aberto â€” push deve atualizÃ¡-lo automaticamente): instanciaÃ§Ã£o do mÃ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: `ExtractionStarterWorker` completo â€” domÃ­nio/aplicaÃ§Ã£o/persistÃªncia/handler real, nÃ£o sÃ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` â€” portas prÃ³prias, nÃ£o reusa `DocumentStore` do mÃ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃ¡ existia â€” gated sÃ³ no registro criaria um run Ã³rfÃ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real Ã© a idempotÃªncia nativa do `StartExecution` por nome de execuÃ§Ã£o determinÃ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃ£o S3â†’EventBridgeâ†’SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃ§Ã£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` â€” insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) â€” mudanÃ§a na M6 (jÃ¡ em produÃ§Ã£o), mas segura: `cleanObject` Ã© sempre um valor opaco armazenado na entidade, nunca reconstruÃ­do/reparsed em nenhum outro lugar do cÃ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis), mas a regra EventBridge que liga o bucket limpo Ã  fila Ã© o ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` â€” com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) â€” jÃ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃªncias) â€” comportamento condicional verificado, nÃ£o sÃ³ lido no cÃ³digo. **Ainda sem PR/merge** (mesma lÃ³gica do item 1: mergeÃ¡vel isoladamente, mas ainda aguardando decisÃ£o de "quando" â€” ver abaixo).
3. ASL â€” camada pura (o arquivo `.asl.json` e o mÃ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** (cÃ³digo+infra, nÃ£o sÃ³ camada pura) â€” ver a seÃ§Ã£o dedicada acima ("Item 4 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, arquivos tocados, decisÃµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 5 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, arquivos tocados, decisÃµes tomadas (reanexaÃ§Ã£o de contexto ao payload de sucesso de `completeOcr`, correÃ§Ã£o do path `$.Payload.*` nas Choice states) e a decisÃ£o pendente registrada (sem heurÃ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 6 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, decisÃµes tomadas (modelo/regiÃ£o placeholder configurÃ¡veis, correÃ§Ã£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃ§Ã£o do `artifact` ao output do item 5) e o corpus adversarial construÃ­do.
7. `ExtractionValidationTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 7 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, decisÃµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃ¡ commitado, invariante de deleÃ§Ã£o do artefato) e o que falta (item 8 ainda nÃ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃ§Ã£o â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta**: ver seÃ§Ã£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃ§Ã£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: ver "Item 3 â€” CONCLUÃDO nesta sessÃ£o" na seÃ§Ã£o M7 acima (topo do arquivo) para o detalhe completo â€” `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃ§Ã£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃ§Ã£o "pendÃªncias explÃ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real â€” nÃ£o bloqueia implementaÃ§Ã£o, sÃ³ produÃ§Ã£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` â€” o Ãºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) â€” idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃª o `ExtractedField` jÃ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` Ã© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) â€” lÃª os quatro, valida as 4 versÃµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃ§Ã£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` â€” primeiro 422 do projeto). `reject` Ã© OCC de 3 vias (nunca lÃª/toca `ExpirationItem` â€” nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoâ†’atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃ³ `expirationDate` -> `dueDate` no schema v1) â€” quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃ§Ã£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃ§Ã£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` â€” `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` â€” um plain read eventualmente consistente, nunca usado pelos OCC writes jÃ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃ¡ existente) reaproveitado por `documents`/`items` na composition root â€” `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` â€” `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) â€” mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` â†’ `authorize()` internamente via a application function â†’ `TenantQuotaService` `API_REQUEST` 100/60s â†’ schema Ajv â†’ chamada). AutorizaÃ§Ã£o: `extraction:confirm` (aÃ§Ã£o **jÃ¡ existia** na matriz desde uma sessÃ£o anterior â€” cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃ³pias desse mapa nos outros mÃ³dulos nÃ£o precisam, deixadas como estÃ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃ¡ tem `tenant_facing_read_write_policy_json` completo na tabela â€” zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃ£o de adapter `DynamoLike` que `ExpirationService` jÃ¡ usa para sua prÃ³pria `IdempotencyStore`, ao invÃ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` â€” o `source_arn` do `aws_lambda_permission.documents` jÃ¡ Ã© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃ­do de republish de `source_code_hash` jÃ¡ documentado em toda sessÃ£o anterior deste milestone â€” o prÃ³prio `documents-handler` bundle mudou porque o cÃ³digo mudou, os outros 15 sÃ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas â€” nÃ£o repetido o gap histÃ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` â€” `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` â€” reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃ¡lido/invÃ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos Ã  mÃ£o sobre uma tabela DynamoDB em memÃ³ria com OCC real â€” `InMemoryTable.write()` sÃ³ aplica se a versÃ£o bate) â€” feliz confirm (item atualizado + versÃµes incrementadas), feliz reject (item intocado), idempotÃªncia de replay dos dois (sem re-executar a transaÃ§Ã£o), 409 individual para cada uma das 4/3 versÃµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃ¡lido, 404 para cada uma das 4 entidades ausente, negaÃ§Ã£o de autorizaÃ§Ã£o sem membership. 2 fakes prÃ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃ§Ã£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃ­cio da sessÃ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃ aconteceu, sÃ³ a persistÃªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒO libera a quota** â€” investigado nesta sessÃ£o e confirmado correto, nÃ£o uma lacuna: como a reserva Ã© por-execuÃ§Ã£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃ³pria reserva anterior, que `startOcr` jÃ¡ trata genericamente como "jÃ¡ reservado, nÃ£o Ã© exaustÃ£o real" (linhas 86-90) â€” **independente de a reserva ter sido liberada ou nÃ£o**. Liberar aqui nÃ£o destravaria nada que jÃ¡ nÃ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃ£o exato â€” falha da chamada Bedrock em si (apÃ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃ¡ existente, confirmado). NÃ£o hÃ¡ nenhum caminho de falha PÃ“S-chamada-bem-sucedida neste handler (ao contrÃ¡rio do Textract, o resultado do Bedrock Ã© sÃ³ re-anexado ao payload de saÃ­da, nunca persistido localmente por este handler) â€” nÃ£o hÃ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃ¡ completa e correta â€” nenhum cÃ³digo novo necessÃ¡rio, nenhuma lacuna real encontrada. NÃ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador â€” fora de escopo do design aprovado, nÃ£o registrado como pendÃªncia.

## M7 â€” estado final desta sessÃ£o: cÃ³digo-completo para `dev`

Com os itens 1-9 concluÃ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃ¡ code-complete para testar em `dev`** â€” nÃ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃ£o: nÃ£o hÃ¡ nenhum mÃ³dulo/recurso do pipeline de extraÃ§Ã£o pendente de instanciaÃ§Ã£o). O Ãºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃ§Ã£o end-to-end real~~ â€” **FEITA em 2026-08-27**, ver a seÃ§Ã£o `## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃ£o, 1 documentado como decisÃ£o pendente).
- Runbook de calibraÃ§Ã£o para job Textract "preso" (mencionado no item 3) â€” nÃ£o bloqueia, Ã© operacional.

## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃ£o `StartExecution` na mÃ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃ§Ã£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` â†’ `exptrk-dev-extraction-starter-handler:live` â†’ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O Ãºnico elo originado Ã  mÃ£o foi a mensagem SQS, porque a regra EventBridge que a produziria Ã© o Ãºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃ¡-la exigiria `terraform apply` â€” proibido pelo Â§3 do `AGENTS.md`. Nada alÃ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃ£o Ã© automatizÃ¡vel por CLI. **Nenhum resultado de extraÃ§Ã£o foi falsificado** â€” todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃ§Ã£o foi produzido pelo pipeline real.

**As 3 execuÃ§Ãµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃ§Ã£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,â€¦}}`) | RunTextract(falhou) â†’ RunDeterministicParser â†’ NeedsBedrock â†’ CheckAiKillSwitch â†’ ValidateSchema â†’ CompareExtractors â†’ PersistExtractedFields â†’ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃ¡ria, envelope canÃ´nico com `OCR:true` | idÃªnticos ao anterior | SUCCEEDED, **ainda degradado** â€” evidÃªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃ¡ria, envelope canÃ´nico **+ cÃ³pia top-level** dos 3 booleanos | RunTextract â†’ RunDeterministicParser â†’ NeedsBedrock â†’ ValidateSchema â†’ CompareExtractors â†’ PersistExtractedFields â†’ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃªncia do caminho feliz (`output` real da execuÃ§Ã£o `run_c1aefâ€¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefâ€¦/42d064cf-â€¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃ­stico extraiu a data correta**. `RunBedrock` NÃƒO foi exercitado (`needsBedrock:false` â€” o candidato determinÃ­stico jÃ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃ³sito). `CompleteRun` apagou o artefato transitÃ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃ§Ã£o/quota/idempotÃªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcâ€¦` â†’ **200**, campo `PENDING_CONFIRMATION` â†’ `CONFIRMED` (`version` 1â†’2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) â†’ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃ§Ã£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` â†’ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃ£o reaplicou).
- `reject` no campo de `run_a22dfâ€¦` â†’ **200**, `PENDING_CONFIRMATION` â†’ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefâ€¦` â†’ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) â€” ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado â€” pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Â§17.3 especifica) `{"features":{"AI_EXTRACTION":â€¦,"OCR":â€¦,"WHATSAPP":â€¦}}`, mas o adapter lia `parsed.OCR` no nÃ­vel raiz â€” logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃªncia isolada: a execuÃ§Ã£o `run_a22dfâ€¦` rodou com a versÃ£o AppConfig 2 (`OCR:true` dentro do envelope canÃ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefâ€¦`, cuja Ãºnica diferenÃ§a era a cÃ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃ­vel raiz) + teste de regressÃ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados Ã  lista de imports estÃ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃ³pria doc-string manda "add both the file AND a static import line". Toda chamada Ã s duas rotas retornava 500 antes de qualquer lÃ³gica de negÃ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` â€” o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃ³ria dos testes unitÃ¡rios ignoram atributos extras na chave, por isso a suÃ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃ³ depois dessa correÃ§Ã£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` â€” GAP REAL, NÃƒO CORRIGIDO (decisÃ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃ§a â€” foi exatamente o que aconteceu com `run_c1aefâ€¦`, o caso de sucesso do pipeline. Mas o **Ãºnico** cÃ³digo que escreve `ExpirationItem.dueDate` Ã© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃ¡ `CONFIRMED` (verificado, ver acima). ConsequÃªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** Ã© atualizado e nÃ£o existe rota que o atualize. NÃ£o corrigido de propÃ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃ§Ã£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" Ã© decisÃ£o de produto/design (Type 1, Â§4 do `AGENTS.md`), nÃ£o conserto mecÃ¢nico.

### Achado menor (nÃ£o corrigido, sem impacto observado nesta verificaÃ§Ã£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, â€¦), mas o Step Functions casa contra o `errorType` do Lambda, que Ã© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, â€¦). Confirmado no histÃ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃ£o** casou â€” quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃ­ **hÃ¡** perda de comportamento (uma falha transitÃ³ria de commit nÃ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD â€” deixado para uma sessÃ£o de infra.

### Estado do ambiente apÃ³s a sessÃ£o

Flags AppConfig **revertidas**: versÃ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃµes temporÃ¡rias 2/3 deletadas â€” `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃ³ o ruÃ­do jÃ¡ conhecido de republicaÃ§Ã£o de versÃ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃµes deletadas), `ocr/` no bucket transitÃ³rio volta 0. Os histÃ³ricos das 3 execuÃ§Ãµes do Step Functions ficam (artefato normal da AWS, nÃ£o Ã© dado de teste a limpar).

**As correÃ§Ãµes dos bugs 1 e 2 estÃ£o apenas em `develop`, ainda NÃƒO implantadas em `dev`** (o CD sÃ³ aplica em push para `main`). A verificaÃ§Ã£o do `confirm` real rodou contra o bundle esbuild jÃ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃ© o prÃ³ximo merge para `main`.

**Bloqueado sÃ³ para produÃ§Ã£o real** (nÃ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃ§Ã£o de modelo Bedrock + regiÃ£o, RIPD formal (`privacy-lgpd.md` Â§6, gatilho jÃ¡ registrado).

## DecisÃµes pendentes do Marcelo

1. **M7 (extraÃ§Ã£o/OCR)**: design jÃ¡ aprovado (D-035), zero decisÃ£o de produto pendente â€” sÃ³ falta decidir *quando* comeÃ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) â€” nÃ£o retomar sem sinal explÃ­cito dele.
3. **Estado real de `git`/deploy no inÃ­cio da prÃ³xima sessÃ£o nÃ£o deve ser presumido** â€” confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃªncias residuais nÃ£o bloqueantes (registradas, nÃ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃ©-existente nÃ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ â€” **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃºmeros (9 vulnerabilidades reais, nÃ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃ£o documentada (`testcontainers`â†’`dockerode`â†’`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃ£o reverificado nesta sessÃ£o).
- Observabilidade por funÃ§Ã£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃ©rica jÃ¡ cobre "estÃ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃ§Ã£o de upload slot expirado) nunca exercitada contra AWS real~~ â€” **executada em 2026-08-25, achou um bug real de produÃ§Ã£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado â€” confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃ³s a transiÃ§Ã£o (ponteiro Ã³rfÃ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃ§Ã£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** â€” commit `2fab80f`, jÃ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃ£o deployado" estava desatualizada (verificado nesta sessÃ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso â€” sÃ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` Ã© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃ³ Ã© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃ§Ã£o defensiva do status real da occurrence antes de agir â€” um ponteiro Ã³rfÃ£o nunca Ã© revisitado fora dessa janela, e mesmo se fosse, Ã© ignorado com seguranÃ§a. Diferente do bug do `upload-slot` (falha total â€” nada funcionava), este Ã© um descompasso comentÃ¡rio-vs-cÃ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃ¡ revisado 9,2/10 pelo protocolo Claudeâ†”Codex e ativo em produÃ§Ã£o real â€” nÃ£o corrigido nesta sessÃ£o deliberadamente, para nÃ£o mexer num trecho crÃ­tico jÃ¡ extensivamente revisado sem uma rodada de revisÃ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃ§Ã£o real, Â§13-30) que, se adotada como padrÃ£o oficial, deveria passar pela mesma convergÃªncia independente Claudeâ†”Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃ¡ usaram â€” nÃ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃ£o de 12 eixos especÃ­fico do planejamento de interface, jÃ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃªncias (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)

- `docs/architecture/session-log.md` â€” linha do tempo compacta, uma entrada por sessÃ£o.
- `docs/architecture/decisions-log.md` â€” todas as decisÃµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` â€” os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` â€” o protÃ³tipo interativo executÃ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` â€” debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` â€” implementaÃ§Ã£o real do Full BFF + fundaÃ§Ã£o de frontend de produÃ§Ã£o, achados do protocolo Claudeâ†”Codex sobre o cÃ³digo, Final Status.

# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃ§Ã£o LGPD sem purga fÃ­sica real e DSR 100% design-only, ambos registrados como decisÃ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃ­ntese final com recomendaÃ§Ã£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` â€” ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃ§Ã£o/OCR) code-complete e deployado em `dev`** â€” todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃ­stico/Bedrock/validaÃ§Ã£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃ§Ã£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3â†’SQSâ†’Step Functionsâ†’Textractâ†’parserâ†’persistÃªncia, ver `NEXT_SESSION_PROMPT.md`) â€” achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃ£o corrigido e aguardando decisÃ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` â€” nenhuma rota escreve isso hoje); M12 bloqueado por decisÃ£o de produto (D-052); Full BFF â€” D-053/D-054 â€” **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo Claudeâ†”Codex completo em `docs/frontend/frontend-production-foundation.md` â€” Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃ§a de sessÃ£o atÃ© convergir); planejamento de interface concluÃ­do â€” 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃ£o **todos resolvidos** (backend + frontend onde aplicÃ¡vel â€” BLOCKER-C via Variante B, revisÃ£o humana explÃ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada â€” ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃ³xima aÃ§Ã£o concreta, este bloco pode ficar temporariamente atrÃ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃ§Ã£o nÃ£o Ã© sequencial na ordem das linhas â€” D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO â€” 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃ³ Contexto bateu o gate â‰¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round3.md; Get-Content -Raw docs/engineering/joint-review-criteria.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 192ms:
2026-08-28T21:14:32.835776Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a04a39-6fe5-78d1-9691-d2b1557d861e
--------
user
Voce e o revisor adversarial independente (protocolo Claude-Codex de AGENTS.md secao 4) do repositorio
expiration-tracker (c:/Users/Usuario/Desktop/projects/expiration-tracker). Continuacao da Rodada 1 do W3-07
(fence de exclusao de tenant), que voce mesmo reprovou com nota 2,8/10 em
docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt.

Leia, na integra, antes de avaliar:
1. docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt (sua propria
   critica da Rodada 1 - os 8 achados bloqueantes que a proposta nova abaixo alega corrigir)
2. docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md (proposta original)
3. docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md (proposta corrigida -
   a avaliar agora)
4. src/modules/identity/application/resolve-request-context.ts e
   src/modules/identity/persistence/identity-mapping-repository.ts (codigo real do resolver/mapping)
5. src/shared/dynamodb/occ.ts (primitivos reais ja existentes que a proposta reusa)
6. src/modules/extraction/application/run-bedrock-extraction.ts e
   src/modules/extraction/application/start-ocr.ts (achados #5/#6 da Rodada 1 - confirme se a correcao
   proposta e coerente com o codigo real)
7. package.json / eslint.config.js reais deste repo (confirme se a sintaxe de regra ESLint proposta na
   secao 3 do Round 2 e sintaticamente valida para a versao instalada, nao apenas plausivel)

Nao aceite nenhuma alegacao da Rodada 2 de que um achado da Rodada 1 foi corrigido sem verificar contra o
codigo real. Trate cada um dos 8 achados da sua propria critica anterior como um item de checklist: para
cada um, confirme explicitamente se a correcao proposta (a) resolve o achado de fato, (b) e implementavel
como descrita, (c) nao introduz um achado novo. Preste atencao especial a:
- O bootstrap atomico de 3 Puts (IdentityMapping+TenantLifecycleRecord+User) na mesma TransactWriteItems -
  isso e realmente implementavel com os tipos existentes, ou ha algum problema de chave/particao/limite?
- O ConditionCheck injetado como "primeiro item" de fencedTransactWrite - existe algum caso real onde isso
  quebra uma transacao existente (ex.: limite de itens, chave duplicada, ordem que importa para outro
  motivo)?
- A secao 6 (residual de URL presignada) argumenta que a garantia de nao-ressurreicao de DADO continua de
  pe mesmo com a janela de emissao de capability. Essa distincao e valida tecnicamente, ou e uma forma de
  reclassificar um achado real como "fora de escopo"?
- DRAIN_WINDOW_SECONDS=900 - o numero e justificado pelos leases/TTLs reais do sistema (grep os TTLs reais
  usados, nao confie no numero da proposta), ou e arbitrario?
- Existe algum caminho de escrita de dado de tenant que nem apos a Rodada 2 passaria por
  fencedTransactWrite (procure de novo, exaustivamente, nao confie na tabela da secao 4)?

Responda com:
1. Nota de 0 a 10 (protocolo de nota cega desta rodada).
2. Para cada um dos 8 achados da Rodada 1: RESOLVIDO / PARCIALMENTE RESOLVIDO / NAO RESOLVIDO, com evidencia.
3. Achados NOVOS introduzidos pela Rodada 2 (se houver), confirmados contra codigo real.
4. Recomendacao: aprovar (>=9.0), pedir revisao, ou reprovar.

Seja tao rigoroso quanto na Rodada 1 - nao suavize por ja ter havido uma correcao. O historico deste
repositorio mostra que aceitar "parece corrigido" sem reverificar levou a reprovacoes tardias.

codex
Vou fazer a revisÃ£o como Type 1 sob o protocolo, comeÃ§ando pelos documentos de entrada obrigatÃ³rios e depois cruzando cada alegaÃ§Ã£o com o cÃ³digo e a configuraÃ§Ã£o reais. TambÃ©m vou refazer a busca exaustiva de escritas e TTLs, sem tomar a tabela da proposta como inventÃ¡rio confiÃ¡vel.
2026-08-28T21:14:43.113605Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Item NEXT_SESSION_PROMPT.md, docs/architecture/README.md, docs/engineering/README.md, docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt, docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md, docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md, src/modules/identity/application/resolve-request-context.ts, src/modules/identity/persistence/identity-mapping-repository.ts, src/shared/dynamodb/occ.ts, src/modules/extraction/application/run-bedrock-extraction.ts, src/modules/extraction/application/start-ocr.ts, package.json, eslint.config.js | Select-Object FullName,Length'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Item NEXT_SESSION_PROMPT.md, docs/architecture/README.md, docs/engineering/README.md, docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt, docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md, docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md, src/modules/identity/application/resolve-request-context.ts, src/modules/identity/persistence/identity-mapping-repository.ts, src/shared/dynamodb/occ.ts, src/modules/extraction/application/run-bedrock-extraction.ts, src/modules/extraction/application/start-ocr.ts, package.json, eslint.config.js | Select-Object FullName,Length' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Item NEXT_SESSION_PROMPT.md, docs/architecture/README.md, docs/engineering/README.md, docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt, docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md, docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md, src/modules/identity/application/resolve-request-context.ts, src/modules/identity/persistence/identity-mapping-repository.ts, src/shared/dynamodb/occ.ts, src/modules/extraction/application/run-bedrock-extraction.ts, src/modules/extraction/application/start-ocr.ts, package.json, eslint.config.js | Select-Object FullName,Length'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 167ms:
# Expiration Tracker Ã¢â‚¬â€ Status e PrÃƒÂ³xima SessÃƒÂ£o

> Este arquivo ÃƒÂ© estado atual + prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o (`AGENTS.md` Ã‚Â§2), nÃƒÂ£o histÃƒÂ³rico. Para a linha do tempo completa por sessÃƒÂ£o, ver `docs/architecture/session-log.md`; para toda decisÃƒÂ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃƒÂ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Ã‚Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) Ã¢â‚¬â€ confirmar `git log`/branch atual antes de assumir que o PR jÃƒÂ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃƒÂ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program Ã¢â‚¬â€ Waves 0/2/3/5 concluÃƒÂ­das, Wave 4 parcial (gated), Wave 1 nÃƒÂ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃƒÂ£o de consolidaÃƒÂ§ÃƒÂ£o/pilot-readiness. **Backlog canÃƒÂ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃƒÂ­ntese final/recomendaÃƒÂ§ÃƒÂ£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃƒÂ¡vel do prompt mestre Ã‚Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) Ã¢â‚¬â€ nÃƒÂ£o duplicar detalhe aqui, sÃƒÂ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃƒÂ§ÃƒÂ£o) `DONE`. **Wave 2 (evidÃƒÂªncia operacional) `DONE`** (2026-08-28) Ã¢â‚¬â€ os 6 drills (W2-03 a W2-08) executados com evidÃƒÂªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO Ã¢â€°Ë†3min44s, RPO nÃƒÂ£o medido Ã¢â‚¬â€ item aberto), load test real (977 invocaÃƒÂ§ÃƒÂµes, cota `API_REQUEST` segurando sob carga), alarme forÃƒÂ§ado com notificaÃƒÂ§ÃƒÂ£o SNSÃ¢â€ â€™e-mail real confirmada. **RPO (W2-06), dedupe pÃƒÂ³s-commit (W2-05) e o pipeline de alarme do W2-08 fechados na mesma sessÃƒÂ£o (2026-08-28)** Ã¢â‚¬â€ ver `pilot-readiness-program.md` seÃƒÂ§ÃƒÂ£o Wave 2 para o detalhe completo (inclui o achado real de que "credential-compromise via HTTP" ÃƒÂ© estruturalmente inalcanÃƒÂ§ÃƒÂ¡vel hoje, nÃƒÂ£o uma lacuna). Claims especÃƒÂ­ficas por drill: ver `docs/engineering/test-engineering-standard.md` Ã‚Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada. **`W3-06-DECISION` fechada e implementada nesta sessÃƒÂ£o (2026-08-28)**: desenho do mecanismo de purga real de `USER_DOCUMENT` aprovado via protocolo ClaudeÃ¢â€ â€Codex completo (6 rodadas, Codex 9,2/Claude 9,1, D-061 em `decisions-log.md`) e implementado de ponta a ponta Ã¢â‚¬â€ `DocumentPurgeWorker` real (claim/lease sobre GSI6, apaga o objeto S3 real via `cleanObject`/evidÃƒÂªncia Ã¢â‚¬â€ nunca `quarantineObject` Ã¢â‚¬â€ e a linha `Document`, grava `DocumentPurgeReceipt` nÃƒÂ£o sensÃƒÂ­vel), agendado a cada 6h, verificado com `terraform plan`/`terraform test` reais contra `dev` (ver `docs/architecture/reviews/w3-06-user-document-purge-design/`). Wave 4 (Identity/RBAC) `PARTIAL` Ã¢â‚¬â€ confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃƒÂ£o de negÃƒÂ³cio jÃƒÂ¡ registrada (`AGENTS.md` Ã‚Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) Ã¢â‚¬â€ `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃƒÂ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃƒÂ£o (2026-08-28) Ã¢â‚¬â€ auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard Ã¢â‚¬â€ novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃƒÂ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados com fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa precisa da Wave 2. Protocolo ClaudeÃ¢â€ â€Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃƒÂ­cito do Marcelo, acima do 9,0 padrÃƒÂ£o) Ã¢â‚¬â€ trajetÃƒÂ³ria Codex 6.35Ã¢â€ â€™7.85Ã¢â€ â€™8.70Ã¢â€ â€™9.18Ã¢â€ â€™9.34Ã¢â€ â€™9.46Ã¢â€ â€™9.48Ã¢â€ â€™**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃƒÂ§ÃƒÂ£o D-0xx). EvidÃƒÂªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃƒÂ£o vale para qualquer teste/drill novo a partir de agora** Ã¢â‚¬â€ antes de declarar um teste ou drill "concluÃƒÂ­do", checar contra os gates de Ã‚Â§3 (nunca reversÃƒÂ£o silenciosa, nunca claim mais ampla que a evidÃƒÂªncia).

**RecomendaÃƒÂ§ÃƒÂ£o da sÃƒÂ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃƒÂªs gates reais nomeados na sÃƒÂ­ntese original Ã¢â‚¬â€ guest trust (`GTR-01`/`W5-01`), evidÃƒÂªncia operacional (Wave 2), e retenÃƒÂ§ÃƒÂ£o/purga real (`W3-06`) Ã¢â‚¬â€ **fecharam todos nesta sessÃƒÂ£o**, e todos os itens secundÃƒÂ¡rios de Wave 2 (RPO, dedupe pÃƒÂ³s-commit, pipeline de alarme de credential-compromise) tambÃƒÂ©m fecharam na mesma sessÃƒÂ£o. ÃƒÅ¡nico item nÃƒÂ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).

954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃƒÂ§ÃƒÂ£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃƒÂ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃƒÂ§ÃƒÂ£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃƒÂ£o** (W2-06 RPO, W2-05 dedupe pÃƒÂ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃƒÂ£o, nenhuma aprovada**: (1) D-062, 4 rodadas ClaudeÃ¢â€ â€Codex, notas 3,4Ã¢â€ â€™5,1Ã¢â€ â€™4,7/10 Ã¢â‚¬â€ achado central: garantir "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃƒÂ­ncronos); Claude e Codex recomendaram convergentemente nÃƒÂ£o implementar sem esse fence, e a sessÃƒÂ£o parou por decisÃƒÂ£o do Marcelo. (2) O Marcelo entÃƒÂ£o decidiu NÃƒÆ’O esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃƒÂ­cio Ã¢â‚¬â€ D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃƒÂ¡ existente e jÃƒÂ¡ checado em `resolveRequestContext`) seria apagado pela prÃƒÂ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃƒÂ³ximo login Ã¢â‚¬â€ o mecanismo de autenticaÃƒÂ§ÃƒÂ£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada** (nÃƒÂ£o abandonado). **PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃƒÂ§ÃƒÂ£o de levantar superfÃƒÂ­cies de escrita por ponto de entrada de runtime Ã¢â‚¬â€ API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions Ã¢â‚¬â€ nunca por pasta de cÃƒÂ³digo, que jÃƒÂ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso Ã¢â‚¬â€ os 3 gates reais (W3-06/Wave 2/GTR-01) jÃƒÂ¡ fecharam.

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃƒÂ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃƒÂ³rio e passou a ter a primeira linguagem visual real do produto Ã¢â‚¬â€ arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃƒÂ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃƒÂ§ÃƒÂ£o **Operational Calm** aplicada ÃƒÂ s 5 superfÃƒÂ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION Ã¢â‚¬â€ PROVISIONAL PENDING USER VALIDATION` Ã¢â‚¬â€ a palavra PROVISIONAL ÃƒÂ© literal: nenhuma sessÃƒÂ£o com usuÃƒÂ¡rio aconteceu, e as **15** hipÃƒÂ³teses de design (hierarquia de navegaÃƒÂ§ÃƒÂ£o, densidade final, rÃƒÂ³tulos, colocaÃƒÂ§ÃƒÂ£o de aÃƒÂ§ÃƒÂµes secundÃƒÂ¡rias, accent, e as duas afirmaÃƒÂ§ÃƒÂµes subjetivas sobre a prÃƒÂ³pria direÃƒÂ§ÃƒÂ£o) estÃƒÂ£o registradas como adiamentos explÃƒÂ­citos no Ã‚Â§35 do documento, nÃƒÂ£o como fatos. **Zero dependÃƒÂªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de ÃƒÂ­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃƒÂ§a estrutural ÃƒÂºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃƒÂ¢ntica com urgÃƒÂªncia e situaÃƒÂ§ÃƒÂ£o em colunas separadas Ã¢â‚¬â€ mesmos dados/ordenaÃƒÂ§ÃƒÂ£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃƒÂªnticos, 3 grupos de urgÃƒÂªncia) foi verificada contra o cÃƒÂ³digo real do frontend **pela primeira vez** (antes sÃƒÂ³ contra o protÃƒÂ³tipo), com asserÃƒÂ§ÃƒÂ£o automatizada. Protocolo ClaudeÃ¢â€ â€Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 Ã¢â‚¬â€ `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃƒÂ§ÃƒÂ£o normal, entÃƒÂ£o o botÃƒÂ£o de submit continuava clicÃƒÂ¡vel durante a mutaÃƒÂ§ÃƒÂ£o exibindo "RenovandoÃ¢â‚¬Â¦"; e os cabeÃƒÂ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃƒÂ§am linhas Ã¢â‚¬â€ ambos corrigidos com teste de regressÃƒÂ£o. O protocolo **nÃƒÂ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃƒÂ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Ã‚Â§4) Ã¢â‚¬â€ o protocolo mais longo jÃƒÂ¡ executado neste repositÃƒÂ³rio. TrÃƒÂªs rodadas acharam defeitos **criados pela rodada de correÃƒÂ§ÃƒÂ£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃƒÂ£o foi cÃƒÂ³digo errado e sim **documentaÃƒÂ§ÃƒÂ£o afirmando prova mais ampla que a evidÃƒÂªncia nomeada**, que apareceu seis vezes: a alegaÃƒÂ§ÃƒÂ£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃƒÂ§ÃƒÂ£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃƒÂµes futuras deste repositÃƒÂ³rio. Acessibilidade virou executÃƒÂ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃƒÂ§ÃƒÂµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃƒÂ§ÃƒÂµes declaradas e registro de decisÃƒÂµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃƒÂ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃƒÂ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃƒÂªncia real registrada, nÃƒÂ£o bloqueante**: as baselines de regressÃƒÂ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃƒÂ£o por plataforma, entÃƒÂ£o o projeto Playwright `visual` ÃƒÂ© deliberadamente separado de `npm run test:e2e` e **nÃƒÂ£o** estÃƒÂ¡ gatilhado no CI hoje (plugÃƒÂ¡-lo falharia por baseline ausente, nÃƒÂ£o por regressÃƒÂ£o real). O caminho de adoÃƒÂ§ÃƒÂ£o estÃƒÂ¡ escrito no Ã‚Â§31 do documento: gravar as baselines num runner Linux, commitÃƒÂ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃƒÂ© lÃƒÂ¡ o CI cobre as mesmas superfÃƒÂ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃƒÂ©m nÃƒÂ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃƒÂ­veis no ambiente) Ã¢â‚¬â€ `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos Ã¢â‚¬â€ Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration Ã¢â‚¬â€ sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real Ã¢â€ â€™ BFF real Ã¢â€ â€™ API real Ã¢â€ â€™ persistÃƒÂªncia real). Nenhuma rota nova de BFF foi necessÃƒÂ¡ria (todas as 6 rotas de item jÃƒÂ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo ClaudeÃ¢â€ â€Codex (Round B adversarial achou 4 bugs reais Ã¢â‚¬â€ 1 S1: corrida TOCTOU real na reaquisiÃƒÂ§ÃƒÂ£o de um registro de idempotÃƒÂªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃƒÂ§ÃƒÂ£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃƒÂ³ `idempotency.complete()` falhasse depois], hash de renovaÃƒÂ§ÃƒÂ£o ambÃƒÂ­guo quando `cycle` ÃƒÂ© enviado independente de `newDueDate`, bug de fuso horÃƒÂ¡rio na formataÃƒÂ§ÃƒÂ£o de data da `Overview` Ã¢â‚¬â€ todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃƒÂ§ÃƒÂ£o tambÃƒÂ©m foi corrigido um bug real prÃƒÂ©-existente de liveness de idempotÃƒÂªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃƒÂªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) Ã¢â‚¬â€ a correÃƒÂ§ÃƒÂ£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃƒÂ§ado pela correÃƒÂ§ÃƒÂ£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃƒÂ§ÃƒÂ£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃƒÂ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃƒÂ§ÃƒÂ£o), 621 testes de backend Ã¢â‚¬â€ nenhuma regressÃƒÂ£o. Pendente ao fim desta sessÃƒÂ£o: push, PR para `develop`, CI, merge (ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" abaixo se ainda nÃƒÂ£o concluÃƒÂ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) Ã¢â‚¬â€ todos deployados em `main`/`dev`. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) teve implementaÃƒÂ§ÃƒÂ£o iniciada em 2026-08-25** (D-057, decisÃƒÂ£o direta do Marcelo Ã¢â‚¬â€ "prossiga entÃƒÂ£o") Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃƒÂ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃƒÂ£o disparou.

**Full BFF (autenticaÃƒÂ§ÃƒÂ£o de browser)**: design fechado via protocolo ClaudeÃ¢â€ â€Codex em duas rodadas Ã¢â‚¬â€ D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃƒÂ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` Ã¢â‚¬â€ 76 testes unitÃƒÂ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o real (`frontend/` Ã¢â‚¬â€ Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃƒÂ¡rio/componente + 6 smoke E2E via Playwright). Protocolo ClaudeÃ¢â€ â€Codex completo (Rodada D levou **6 passagens** atÃƒÂ© convergir Ã¢â‚¬â€ 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃƒÂ³digo prÃƒÂ©-correÃƒÂ§ÃƒÂ£o: consumo nÃƒÂ£o-atÃƒÂ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃƒÂ£o revogada concorrentemente; releituras pÃƒÂ³s-refresh nÃƒÂ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃƒÂ£o checando expiraÃƒÂ§ÃƒÂ£o/idle-TTL antes de agir; idle-TTL da prÃƒÂ³pria `Session` nunca validado contra o relÃƒÂ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃƒÂªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Ã‚Âº-8Ã‚Âº), cada um fechado via protocolo ClaudeÃ¢â€ â€Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` Ã¢â‚¬â€ papÃƒÂ©is, JTBD, inventÃƒÂ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` Ã¢â‚¬â€ modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` Ã¢â‚¬â€ 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` Ã¢â‚¬â€ 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes SurfaceÃ¢â€ â€Journey/Concept/Transition. RevisÃƒÂ£o Codex achou 3 furos reais de mesma causa raiz Ã¢â‚¬â€ `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃƒÂ§a em `SCANNING`, nÃƒÂ£o sÃƒÂ³ em `CLEAN`) Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` Ã¢â‚¬â€ wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃƒÂ£o Codex achou 5 furos reais + 1 divergÃƒÂªncia factual Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o primÃƒÂ¡ria ambÃƒÂ­gua em 4 coleÃƒÂ§ÃƒÂµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃƒÂ³digo executÃƒÂ¡vel real, HTML/CSS/JS sem dependÃƒÂªncias) Ã¢â‚¬â€ protÃƒÂ³tipo navegÃƒÂ¡vel cobrindo as 17 superfÃƒÂ­cies e J-01Ã¢â‚¬â€œJ-08 com 34 Prototype Scenario IDs determinÃƒÂ­sticos, verificado com testes automatizados de navegador headless. RevisÃƒÂ£o Codex achou 6 furos reais de 4 causas raiz Ã¢â‚¬â€ compressÃƒÂ£o de estados no guest upload (faltava validaÃƒÂ§ÃƒÂ£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃƒÂ§ÃƒÂ£o de coleta externa violando epistemic integrity (anunciava verificaÃƒÂ§ÃƒÂ£o de seguranÃƒÂ§a concluÃƒÂ­da ao operador, que nÃƒÂ£o pode observar isso), dois campos sem `<label>`, e reincidÃƒÂªncia de menÃƒÂ§ÃƒÂ£o a WhatsApp Ã¢â‚¬â€ todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` Ã¢â‚¬â€ avaliaÃƒÂ§ÃƒÂ£o do protÃƒÂ³tipo **executÃƒÂ¡vel** (nÃƒÂ£o sÃƒÂ³ a documentaÃƒÂ§ÃƒÂ£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃƒÂ¢ntica/forms testados em navegador headless real, nÃƒÂ£o sÃƒÂ³ lidos), `axe-core` (0 violaÃƒÂ§ÃƒÂµes em todos os estados testados, nas duas rodadas), re-execuÃƒÂ§ÃƒÂ£o de J-01Ã¢â‚¬â€œJ-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** ClaudeÃ¢â€ â€Codex (nÃƒÂ£o as 2-3 rodadas tÃƒÂ­picas de etapas anteriores): Rodada A (autoavaliaÃƒÂ§ÃƒÂ£o, 9 achados corrigidos) concluiu aprovaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃƒÂ£o vistos pela autoavaliaÃƒÂ§ÃƒÂ£o: o mais grave, a prÃƒÂ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃƒÂ§ÃƒÂ£o em 3 formulÃƒÂ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃƒÂ§ÃƒÂ£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃƒÂ¡-los no fake-backend Ã¢â‚¬â€ uma violaÃƒÂ§ÃƒÂ£o epistÃƒÂªmica real, nÃƒÂ£o cosmÃƒÂ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃƒÂ³prio ao manter um texto "SIMULATED FOR UX VALIDATION Ã¢â‚¬â€ NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃƒÂ­cie pÃƒÂºblica de guest (jargÃƒÂ£o tÃƒÂ©cnico visÃƒÂ­vel a um usuÃƒÂ¡rio externo nÃƒÂ£o-tÃƒÂ©cnico). Rodada D (nova adversarial sobre o cÃƒÂ³digo jÃƒÂ¡ corrigido) achou essa lacuna de conteÃƒÂºdo e uma correÃƒÂ§ÃƒÂ£o incompleta nova (`submitAlert` reabilitava o botÃƒÂ£o sÃƒÂ³ na branch de erro, nÃƒÂ£o na de sucesso) Ã¢â‚¬â€ ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃƒÂ£o arredondado; calculado refletindo esse histÃƒÂ³rico de 4 rodadas, nÃƒÂ£o sÃƒÂ³ o estado final do cÃƒÂ³digo) Ã¢â‚¬â€ `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` Ã¢â‚¬â€ "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃƒÂ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃƒÂ£o) no protÃƒÂ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃƒÂ§ÃƒÂ£o, nunca marcado como backend-resolvido; cenÃƒÂ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃƒÂ§ÃƒÂµes) que achou e corrigiu uma falta real de ordenaÃƒÂ§ÃƒÂ£o por urgÃƒÂªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃƒÂ£o sÃƒÂ³ no protÃƒÂ³tipo) Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃƒÂ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃƒÂ¡ em uso desde a 1Ã‚Âª etapa, agora formal, nenhum critÃƒÂ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃƒÂ§ÃƒÂ£o tÃƒÂ©cnica em Participant Mode, teste de idempotÃƒÂªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃƒÂµes de documentaÃƒÂ§ÃƒÂ£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃƒÂ©-existentes desde a Rodada A) Ã¢â‚¬â€ corrigidos no fechamento, zero contaminaÃƒÂ§ÃƒÂ£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃƒÂ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** Ã¢â‚¬â€ **fechado nesta sessÃƒÂ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` Ã¢â‚¬â€ `DocumentService.listDocuments`/`getDocument`, autorizaÃƒÂ§ÃƒÂ£o `document:read` (aÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃƒÂ§ÃƒÂ£o do item jÃƒÂ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃƒÂ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` Ã¢â‚¬â€ `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃƒÂ§ÃƒÂ£o `requirement:read` jÃƒÂ¡ existente (sem action nova reservada de antemÃƒÂ£o, diferente de `document:read`), mesma leitura por partiÃƒÂ§ÃƒÂ£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃƒÂ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© desta sessÃƒÂ£o, jÃƒÂ¡ estava corrigido antes (comentÃƒÂ¡rio no prÃƒÂ³prio `main.tf`), sÃƒÂ³ confirmado que segue correto.

  Ambas as metades: mudanÃƒÂ§a mecÃƒÂ¢nica sobre um padrÃƒÂ£o de acesso jÃƒÂ¡ aprovado no data model Ã¢â‚¬â€ nÃƒÂ£o passaram pelo protocolo `AGENTS.md` Ã‚Â§4 completo (nÃƒÂ£o ÃƒÂ© decisÃƒÂ£o de arquitetura nova, ÃƒÂ© fechamento de gap de leitura jÃƒÂ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃƒÂ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃƒÂ­cio da sessÃƒÂ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃƒÂ£o. **Achado real corrigido depois, na mesma sessÃƒÂ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` Ã¢â‚¬â€ mesma classe de gap que as rotas de `DocumentRequest` jÃƒÂ¡ tiveram antes (comentÃƒÂ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** Ã¢â‚¬â€ **implementaÃƒÂ§ÃƒÂ£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃƒÂ§ÃƒÂ£o abaixo). Antes desta branch, a materializaÃƒÂ§ÃƒÂ£o automÃƒÂ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃƒÂ§ÃƒÂ£o/ediÃƒÂ§ÃƒÂ£o de item. PendÃƒÂªncia real restante: decisÃƒÂ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) Ã¢â‚¬â€ nÃƒÂ£o bloqueia mais nada alÃƒÂ©m dessa decisÃƒÂ£o em si.
- **BLOCKER-C** Ã¢â‚¬â€ **decisÃƒÂ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃƒÂ£o humana explÃƒÂ­cita)**, confirmando a hipÃƒÂ³tese lÃƒÂ­der jÃƒÂ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Ã‚Â§22, `interface-conceptual-model-and-information-architecture.md` Ã‚Â§37, `interface-critical-user-journeys.md` Ã‚Â§37 Ã¢â‚¬â€ esses documentos continuam como registro histÃƒÂ³rico do debate, nÃƒÂ£o reescritos, per `AGENTS.md` regra de precedÃƒÂªncia de `history/`/artefatos aprovados). InvestigaÃƒÂ§ÃƒÂ£o confirmou que Variante A (fechamento automÃƒÂ¡tico) nÃƒÂ£o ÃƒÂ© viÃƒÂ¡vel hoje: `DocumentSubmission` nÃƒÂ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃƒÂ§a a qual `ExpirationItem` uma submissÃƒÂ£o pertence Ã¢â‚¬â€ decidir automaticamente seria adivinhar, nÃƒÂ£o resolver. **Mecanismo de backend para a Variante B jÃƒÂ¡ existe, nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio**: visibilidade do operador sobre submissÃƒÂµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃƒÂ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existente (`POST .../link`, prÃƒÂ©-existente). **Frontend implementado na mesma sessÃƒÂ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject Ã¢â‚¬â€ `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃƒÂ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores Ã¢â€ â€™ detalhe com requisitos Ã¢â€ â€™ "Revisar" expande evidÃƒÂªncia enviada (`DocumentSubmission`) Ã¢â€ â€™ operador informa o `itemId` de um vencimento jÃƒÂ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃƒÂ§ÃƒÂ£o de subject nem sugestÃƒÂ£o automÃƒÂ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃƒÂ³tulos seguem a mesma disciplina de Epistemic Integrity jÃƒÂ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃƒÂ§a) Ã¢â‚¬â€ conteÃƒÂºdo nÃƒÂ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta ÃƒÂºltima etapa: **`GTR-01`** (identidade do solicitante nÃƒÂ£o exposta ao guest) agora **simulada** no protÃƒÂ³tipo para User Validation, mas ainda nÃƒÂ£o resolvida no backend Ã¢â‚¬â€ `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃƒÂªncia) foi **resolvido no backend real** Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional Ã¢â‚¬â€ e, na sessÃƒÂ£o do Full BFF/frontend, o `requestHash` de detecÃƒÂ§ÃƒÂ£o de payload divergente foi migrado de concatenaÃƒÂ§ÃƒÂ£o por delimitador (colisÃƒÂ£o real possÃƒÂ­vel) para JSON canÃƒÂ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` Ã¢â‚¬â€ a lacuna "nenhum frontend/BFF real envia o header" estÃƒÂ¡ fechada para o caminho de criaÃƒÂ§ÃƒÂ£o de item.

**PrÃƒÂ³xima etapa natural do design** (ainda nÃƒÂ£o iniciada): **User Validation** Ã¢â‚¬â€ usando o protÃƒÂ³tipo jÃƒÂ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode ÃƒÂ© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Ã‚Â§15-18/Ã‚Â§23-24 (tese de produto, mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o, tarefas candidatas derivadas de outcomes, limitaÃƒÂ§ÃƒÂµes conhecidas do protÃƒÂ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃƒÂ£o produzido ainda. NÃƒÂ£o obriga inÃƒÂ­cio imediato de Visual Design/Design System.

## BLOCKER-B Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃƒÂ£o de 2026-08-24/25 (continuaÃƒÂ§ÃƒÂ£o apÃƒÂ³s a sessÃƒÂ£o anterior ter sido abortada por limite de tokens sÃƒÂ³ com reconhecimento parcial Ã¢â‚¬â€ handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃƒÂ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo ClaudeÃ¢â€ â€Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` Ã¢â‚¬â€ esse documento ÃƒÂ© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃƒÂ§ÃƒÂ£o real (nÃƒÂ£o sÃƒÂ³ design) tambÃƒÂ©m revisada via protocolo ClaudeÃ¢â€ â€Codex (2 rodadas, nota final 9,2/10, `APPROVED` Ã¢â‚¬â€ achados reais corrigidos: reclassificaÃƒÂ§ÃƒÂ£o de exceÃƒÂ§ÃƒÂ£o em dois mÃƒÂ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃƒÂ§ÃƒÂ£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃƒÂ§ÃƒÂ£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃƒÂªs novos mÃƒÂ©todos de reconciliaÃƒÂ§ÃƒÂ£o em `ReminderMaterializer` (fenced por versÃƒÂ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃƒÂ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃƒÂ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃƒÂ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃƒÂ§ÃƒÂ£o: o "caminho genÃƒÂ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃƒÂ³digo Ã¢â‚¬â€ `router_queue` nÃƒÂ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃƒÂ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃƒÂ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃƒÂ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Ã‚Â§8). **Implementado nesta sessÃƒÂ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃƒÂ§ÃƒÂ£o da criaÃƒÂ§ÃƒÂ£o do item (sem `ConditionCheck` de existÃƒÂªncia Ã¢â‚¬â€ o item novo ÃƒÂ© garantido pela prÃƒÂ³pria transaÃƒÂ§ÃƒÂ£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃƒÂ³ `ExpirationItem` (mudanÃƒÂ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" Ã¢â‚¬â€ reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃƒÂ£o estava instalado no inÃƒÂ­cio desta sessÃƒÂ£o (`npm ci` rodado para poder validar) Ã¢â‚¬â€ ambiente, nÃƒÂ£o cÃƒÂ³digo.

**PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o**: **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ todos os 9 itens numerados originais da lista estÃƒÂ£o CONCLUÃƒÂDOS (2026-08-26)**, cÃƒÂ³digo+infra reais para itens 1-9, nÃƒÂ£o sÃƒÂ³ camada pura Ã¢â‚¬â€ ver as seÃƒÂ§ÃƒÂµes dedicadas "Item N Ã¢â‚¬â€ CONCLUÃƒÂDO" na seÃƒÂ§ÃƒÂ£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Ã‚Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃƒÂ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) Ã¢â‚¬â€ causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃƒÂ­ficos com `States.ALL` num ÃƒÂºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃƒÂ£o pega esse erro, sÃƒÂ³ a chamada real de criaÃƒÂ§ÃƒÂ£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** Ã¢â‚¬â€ o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃƒÂ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃƒÂ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃƒÂ§ÃƒÂ£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃƒÂ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** Ã¢â‚¬â€ a nota de uma sessÃƒÂ£o anterior dizendo "nenhum profile corresponde ÃƒÂ  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃƒÂµes de escrita necessÃƒÂ¡rias e a **verificaÃƒÂ§ÃƒÂ£o E2E real foi executada** (ver `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃƒÂ§a de Terraform foi cogitada para isso Ã¢â‚¬â€ a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃƒÂ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃƒÂ³ o gatilho automÃƒÂ¡tico via EventBridge ÃƒÂ© que ÃƒÂ© gated). **RecorrÃƒÂªncia fechada na mesma sessÃƒÂ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃƒÂ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` Ã¢â‚¬â€ a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃƒÂ³ descoberto no meio de um `terraform apply` real. NÃƒÂ£o requer credencial de escrita (ÃƒÂ© uma chamada de validaÃƒÂ§ÃƒÂ£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃƒÂ£o retomar sem sinal explÃƒÂ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃƒÂ¡ aprovada em nÃƒÂ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Ã‚Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃƒÂ¡vel por hash; `implementation-blueprint.md` Ã‚Â§12/Ã‚Â§23: CSP com hashes estÃƒÂ¡ticos calculados no build, sem nonce dinÃƒÂ¢mico Ã¢â‚¬â€ Day 0 nÃƒÂ£o tem compute de borda). O que falta ÃƒÂ© o desenho de implementaÃƒÂ§ÃƒÂ£o real e o cÃƒÂ³digo Ã¢â‚¬â€ nenhuma decisÃƒÂ£o de produto pendente, mas hÃƒÂ¡ **uma decisÃƒÂ£o de arquitetura real ainda em aberto** que a prÃƒÂ³xima sessÃƒÂ£o precisa resolver antes de implementar (Type 1, nÃƒÂ­vel 5-6 da escala de risco Ã¢â‚¬â€ protocolo `AGENTS.md` Ã‚Â§4 aplicÃƒÂ¡vel):

1. **DecisÃƒÂ£o de arquitetura a fechar primeiro Ã¢â‚¬â€ FECHADA nesta sessÃƒÂ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo ClaudeÃ¢â€ â€Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** Ã¢â‚¬â€ `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` ÃƒÂ© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃƒÂªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma ÃƒÂºnica distribution, behaviors `/bff` e `/bff/*` Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior Ã¢â€ â€™ S3 com OAC + CloudFront Function de SPA routing (sÃƒÂ³ nesse behavior, nunca no do BFF). Zero mudanÃƒÂ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃƒÂ©-existentes descobertos pela revisÃƒÂ£o, a aplicar na implementaÃƒÂ§ÃƒÂ£o: `proxy-service.ts` nÃƒÂ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃƒÂ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃƒÂ£o implementado): acesso direto ao `execute-api` pÃƒÂºblico precisa de mitigaÃƒÂ§ÃƒÂ£o (header estÃƒÂ¡tico + WAF) antes de produÃƒÂ§ÃƒÂ£o pÃƒÂºblica real.
2. **MÃƒÂ³dulo Terraform `infra/modules/spa-hosting` Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃƒÂºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃƒÂ­citos Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default Ã¢â€ â€™ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃƒÂ©todo GET/HEAD, associada SÃƒâ€œ ao default behavior Ã¢â‚¬â€ nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃƒÂ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃƒÂ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃƒÂ©-existentes do ADR-0011 tambÃƒÂ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃƒÂ§ÃƒÂ£o errada do prÃƒÂ³prio ADR-0011): `var.spa_content_security_policy` nÃƒÂ£o precisa de hashes calculados por build Ã¢â‚¬â€ verificado contra o `frontend/dist/index.html` real, o build do Vite nÃƒÂ£o emite nenhum `<script>`/`<style>` inline, todo asset ÃƒÂ© carregado por URL externa jÃƒÂ¡ hasheada, entÃƒÂ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃƒÂ¡ existe em `frontend/index.html`) bastam sem hash nenhum Ã¢â‚¬â€ a variÃƒÂ¡vel ganhou um default determinÃƒÂ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃƒÂ£o consegue expressar). **MÃƒÂ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) Ã¢â‚¬â€ corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃƒÂ£o bloqueia o primeiro apply, sÃƒÂ³ o domÃƒÂ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro Ã¢â‚¬â€ confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃƒÂ£o sÃƒÂ³ contra mocks.
3. **Deploy imutÃƒÂ¡vel por hash Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: Vite jÃƒÂ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra ÃƒÂ© necessÃƒÂ¡rio (o hash no nome do arquivo jÃƒÂ¡ ÃƒÂ© o mecanismo de imutabilidade). `index.html` ÃƒÂ© tratado ÃƒÂ  parte (nÃƒÂ£o hasheado, `Cache-Control: no-cache`, upload por ÃƒÂºltimo).
4. **IntegraÃƒÂ§ÃƒÂ£o com `cd.yml` Ã¢â‚¬â€ IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃƒÂ£o um job novo) Ã¢â‚¬â€ `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** Ã¢â‚¬â€ assets antigos nunca sÃƒÂ£o removidos, ÃƒÂ© isso que mantÃƒÂ©m um rollback de `index.html` funcional, jÃƒÂ¡ que as referÃƒÂªncias antigas continuam servÃƒÂ­veis), upload de `index.html` por ÃƒÂºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃƒÂ¡ existente. **PendÃƒÂªncia real registrada, nÃƒÂ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃƒÂ³ cobre Lambdas Ã¢â‚¬â€ nÃƒÂ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃƒÂ£o `rollback.yml` (mecanismo existente) nÃƒÂ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃƒÂ£o bloqueia o deploy normal.
5. **VerificaÃƒÂ§ÃƒÂ£o real (Camada 3) Ã¢â‚¬â€ CONCLUÃƒÂDA (2026-08-25), achou e corrigiu um bug real de produÃƒÂ§ÃƒÂ£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃƒÂ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** Ã¢â‚¬â€ isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃƒÂ­tica fixa nÃƒÂ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃƒÂ§ÃƒÂ£o, tudo confirmado contra a distribution real:
   - `GET /` Ã¢â€ â€™ 200, `text/html`.
   - `GET /assets/index-*.js` Ã¢â€ â€™ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃƒÂ£o, V6 do ADR-0011) Ã¢â€ â€™ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) Ã¢â€ â€™ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) Ã¢â‚¬â€ nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) Ã¢â€ â€™ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃƒÂ£o (V4) Ã¢â€ â€™ 401 `application/json` (`AUTH_REQUIRED`) Ã¢â‚¬â€ nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃƒÂ£o (V5-404) Ã¢â€ â€™ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado Ã¢â‚¬â€ o caso 404-por-allowlist com sessÃƒÂ£o vÃƒÂ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃƒÂ£o foram exercitados por nÃƒÂ£o serem automatizÃƒÂ¡veis via `curl` sozinho; a propriedade crÃƒÂ­tica de seguranÃƒÂ§a Ã¢â‚¬â€ nenhum erro do BFF vira HTML da SPA Ã¢â‚¬â€ jÃƒÂ¡ estÃƒÂ¡ confirmada pelos casos acima).
   - Headers de seguranÃƒÂ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃƒÂ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy Ã¢â‚¬â€ exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃƒÂ£o sÃƒÂ³ via `terraform plan`.
   **PendÃƒÂªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃƒÂ£o lidos via CloudFront/OAC (sÃƒÂ³ por IAM de role de Lambda), entÃƒÂ£o nÃƒÂ£o tÃƒÂªm este bug Ã¢â‚¬â€ mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃƒÂ£o corrigido preventivamente nesta sessÃƒÂ£o (fora do escopo desta decisÃƒÂ£o, nenhum uso real hoje), sÃƒÂ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃƒÂ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o iniciada (2026-08-25, D-057)

Design jÃƒÂ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) Ã¢â‚¬â€ esta seÃƒÂ§ÃƒÂ£o ÃƒÂ© sobre a IMPLEMENTAÃƒâ€¡ÃƒÆ’O, o design nÃƒÂ£o muda. **Escopo ÃƒÂ© grande** (Step Functions, Textract assÃƒÂ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ vai continuar por mÃƒÂºltiplas sessÃƒÂµes, nÃƒÂ£o ÃƒÂ© razoÃƒÂ¡vel fechar num ÃƒÂºnico slice. Estado exato ao fim desta sessÃƒÂ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Ã‚Â§4 ganhou a classe `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ prÃƒÂ©-requisito de design que o prÃƒÂ³prio D-035 exige estar registrado antes de qualquer implementaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o ÃƒÂ© pendÃƒÂªncia pÃƒÂ³s-produÃƒÂ§ÃƒÂ£o).
2. MÃƒÂ³dulo novo `src/modules/extraction/domain/` (sÃƒÂ³ domÃƒÂ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` Ã¢â‚¬â€ `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃƒÂ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃƒÂ³pria chave de idempotÃƒÂªncia da execuÃƒÂ§ÃƒÂ£o).
   - `extracted-field.ts` Ã¢â‚¬â€ `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Ã‚Â§12.5.
   - `field-schema.ts` Ã¢â‚¬â€ schema v1 com sÃƒÂ³ `expirationDate` (ÃƒÂºnico campo que o design aprovado nomeia concretamente Ã¢â‚¬â€ deliberadamente nÃƒÂ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` Ã¢â‚¬â€ `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` Ã¢â‚¬â€ helper de safety net de 24h para o artefato transitÃƒÂ³rio (a exclusÃƒÂ£o real ÃƒÂ© explÃƒÂ­cita por `ExtractionValidationTaskHandler`, este helper ÃƒÂ© sÃƒÂ³ a rede de seguranÃƒÂ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃƒÂ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` Ã¢â‚¬â€ **ainda sem PR/merge para `main`**, dado que nÃƒÂ£o hÃƒÂ¡ nenhuma infra/runtime ativÃƒÂ¡vel ainda, nada a fazer deploy).

**Item 1 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃƒÂ§ÃƒÂ£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃƒÂ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃƒÂ³dulo corrigido pelo design Ã¢â‚¬â€ transversal, nÃƒÂ£o `extraction-appconfig`, jÃƒÂ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃƒÂªs defaults `false` (fail-closed, prÃƒÂ©-condiÃƒÂ§ÃƒÂµes externas de M7 nÃƒÂ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Ã‚Â§1.6, default `false`) Ã¢â‚¬â€ gate Terraform separado do pipeline inteiro, ainda nÃƒÂ£o referenciado por nenhum recurso (sÃƒÂ³ os workers futuros vÃƒÂ£o condicionar-se a ele). 2 `terraform test` do mÃƒÂ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) Ã¢â‚¬â€ CAMADA PURA CONCLUÃƒÂDA nesta sessÃƒÂ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃƒÂ£o que o item 0 seguiu: domÃƒÂ­nio+aplicaÃƒÂ§ÃƒÂ£o primeiro, depois handler Lambda/infra numa fatia posterior Ã¢â‚¬â€ deliberado, nÃƒÂ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` Ã¢â‚¬â€ cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` Ã¢â‚¬â€ entidade `TextractJob` (chave prÃƒÂ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃƒÂ£o tenant-scoped Ã¢â‚¬â€ `COMPLETE_OCR` sÃƒÂ³ tem `jobId` disponÃƒÂ­vel da notificaÃƒÂ§ÃƒÂ£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃƒÂ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃƒÂ§ÃƒÂ£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` Ã¢â‚¬â€ classificador heurÃƒÂ­stico puro (magic bytes > extensÃƒÂ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃƒÂ§a) quando nenhum sinal ÃƒÂ© conclusivo Ã¢â‚¬â€ quem decide que isso ÃƒÂ© `UnsupportedDocumentTypeError` ÃƒÂ© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed ÃƒÂ© responsabilidade do caller, nÃƒÂ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃƒÂ©todo de delete Ã¢â‚¬â€ a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃƒÂ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃƒÂ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) Ã¢â‚¬â€ orquestra classificaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ kill switch `OCR` (fail-closed) Ã¢â€ â€™ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` Ã¢â‚¬â€ um retry do mesmo run encontra `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior e trata como "jÃƒÂ¡ reservado", nÃƒÂ£o como falha real) Ã¢â€ â€™ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃƒÂ­stico) Ã¢â€ â€™ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` Ã¢â‚¬â€ retorna void, a execuÃƒÂ§ÃƒÂ£o do Step Functions fica parada no `taskToken` atÃƒÂ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃƒÂ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) Ã¢â‚¬â€ busca `TextractJob` por `jobId`; caso ÃƒÂ³rfÃƒÂ£o (nenhum job encontrado) faz UMA chamada de confirmaÃƒÂ§ÃƒÂ£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃƒÂ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃƒÂµe um mÃƒÂ©todo de delete); `taskTokenCiphertext` ÃƒÂ© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃƒÂ§ado, sem tocar no registro) em qualquer outro erro Ã¢â‚¬â€ a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto Ã¢â‚¬â€ `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃƒÂ£o ÃƒÂ© revalidado como JSON aninhado, mesma convenÃƒÂ§ÃƒÂ£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` Ã¢â‚¬â€ `RunTextract` ÃƒÂ© o ÃƒÂºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃƒÂ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃƒÂ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃƒÂ£o existem.
- `infra/modules/extraction-workflow/` Ã¢â‚¬â€ mÃƒÂ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃƒÂ§ÃƒÂ£o + role de execuÃƒÂ§ÃƒÂ£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒÆ’O chamado em `infra/main.tf`** Ã¢â‚¬â€ um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃƒÂ£o sÃƒÂ³ em runtime), e os 3 handlers dos itens 5-7 nÃƒÂ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃƒÂ³prio diretÃƒÂ³rio do mÃƒÂ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃƒÂ³dulo especificamente Ã¢â‚¬â€ pendÃƒÂªncia explÃƒÂ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos ÃƒÂ  mÃƒÂ£o (sem `vi.mock`), cobrindo: classificaÃƒÂ§ÃƒÂ£o por magic-bytes/extensÃƒÂ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃƒÂ§ÃƒÂ£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃƒÂªncia da reserva de quota sob retry do mesmo run; retry local de persistÃƒÂªncia do `TextractJob` (sucesso apÃƒÂ³s 1 falha, esgotamento apÃƒÂ³s N); `completeOcr`: job ÃƒÂ³rfÃƒÂ£o (com e sem falha na confirmaÃƒÂ§ÃƒÂ£o), jÃƒÂ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃƒÂ§ÃƒÂ£o multi-pÃƒÂ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃƒÂ³rio de `SendTask*` relanÃƒÂ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃƒÂ£o expÃƒÂµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na ÃƒÂ¡rvore inteira de `infra/`.

**Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃƒÂªs decisÃƒÂµes pendentes acima foram todas tomadas e implementadas (nÃƒÂ£o deixadas como pendÃƒÂªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃƒÂ£o final** (nÃƒÂ£o a extensÃƒÂ£o via layer) Ã¢â‚¬â€ `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/ÃƒÂºltimos flags conhecidos em memÃƒÂ³ria do processo. Motivo: a extensÃƒÂ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` Ã¢â‚¬â€ nÃƒÂ£o existe nenhum precedente disso no repo (sÃƒÂ³ `adot_layer_arn`, uma layer diferente), e este handler sÃƒÂ³ lÃƒÂª os flags uma vez por invocaÃƒÂ§ÃƒÂ£o de `START_OCR` (nÃƒÂ£o por pÃƒÂ¡gina/bloco), entÃƒÂ£o o custo/latÃƒÂªncia de uma chamada de API direta por invocaÃƒÂ§ÃƒÂ£o ÃƒÂ© equivalente ao que a extensÃƒÂ£o teria numa invocaÃƒÂ§ÃƒÂ£o fria de qualquer forma. Revisitar sÃƒÂ³ se o volume tornar isso um custo real (a extensÃƒÂ£o amortiza entre invocaÃƒÂ§ÃƒÂµes warm via cache local, este adapter sÃƒÂ³ amortiza dentro de uma sessÃƒÂ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃƒÂ£o reuso da CMK do BFF Ã¢â‚¬â€ mesma disciplina D-054: um token de callback do Step Functions ÃƒÂ© uma credencial viva tÃƒÂ£o sensÃƒÂ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃƒÂ³dulos independentemente deployÃƒÂ¡veis acoplaria rotaÃƒÂ§ÃƒÂ£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃƒÂªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃƒÂ³dulo, mesma razÃƒÂ£o do port).
- **TÃƒÂ³pico SNS de conclusÃƒÂ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar Ã¢â‚¬â€ distinta da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio Lambda.

Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃƒÂ§ÃƒÂ£o real `TaskTimedOut`/`TaskDoesNotExist`Ã¢â€ â€™`TERMINAL_QUIET`, `InvalidToken`Ã¢â€ â€™`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃƒÂ§ado), `AppConfigFeatureFlagsReader` Ã¢â‚¬â€ todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` Ã¢â‚¬â€ um ÃƒÂºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃƒÂ§ÃƒÂ£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃƒÂ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃƒÂ£o SSE-KMS Ã¢â‚¬â€ evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃƒÂ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃƒÂ³pico SNS, permissÃƒÂ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃƒÂ¡ escopada ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃƒÂ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** Ã¢â‚¬â€ mesma lÃƒÂ³gica do item 2: todo recurso aqui ÃƒÂ© inerte sozinho, porque `StartDocumentTextDetection` (a ÃƒÂºnica chamada paga) sÃƒÂ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃƒÂ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃƒÂ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27Ã¢â€ â€™28 funÃƒÂ§ÃƒÂµes (2 asserts).
- 11 testes novos (794 totais no backend) Ã¢â‚¬â€ sÃƒÂ³ para os dois adapters com lÃƒÂ³gica real de branching (`SfnTaskTokenSender`'s classificaÃƒÂ§ÃƒÂ£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃƒÂ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃƒÂ£o ganharam teste unitÃƒÂ¡rio dedicado, seguindo o MESMO precedente jÃƒÂ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃƒÂ©m nÃƒÂ£o tÃƒÂªm) Ã¢â‚¬â€ verificados via `terraform plan` real contra `dev`, nÃƒÂ£o via mock unitÃƒÂ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target Ã¢â‚¬â€ 0 erros, 0 destroys inesperados (sÃƒÂ³ o prÃƒÂ³prio slice + o ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ prÃƒÂ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Ã‚Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) Ã¢â‚¬â€ **ainda sem PR/merge para `main`** no momento em que este parÃƒÂ¡grafo foi escrito (abrir o PR ÃƒÂ© o prÃƒÂ³ximo passo imediato desta mesma sessÃƒÂ£o, ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 Ã¢â‚¬â€ a ASL's `RunDeterministicParser` ÃƒÂ© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃƒÂ£o hÃƒÂ¡ task token nenhum neste handler), entÃƒÂ£o ÃƒÂ© uma ÃƒÂºnica invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃƒÂ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` Ã¢â‚¬â€ `extractExpirationDateCandidates()`, regex determinÃƒÂ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃƒÂ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃƒÂ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃƒÂ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃƒÂ¡ a atÃƒÂ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃƒÂ§a fraca (0.5, abaixo do threshold) quando nÃƒÂ£o hÃƒÂ¡ palavra-chave prÃƒÂ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃƒÂ©rico que trunca o texto processado Ã¢â‚¬â€ a ÃƒÂºnica reutilizaÃƒÂ§ÃƒÂ£o real de M6 possÃƒÂ­vel aqui, jÃƒÂ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃƒÂ£o sobre texto OCR, e nunca extrai campos/datas (sÃƒÂ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃƒÂ³digo `DeterministicParserFailed`) Ã¢â‚¬â€ nÃƒÂ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃƒÂ¡ ÃƒÂ© o genÃƒÂ©rico `States.ALL`.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-deterministic-parser.ts` Ã¢â‚¬â€ `runDeterministicParser()`: lÃƒÂª o artefato OCR (quando presente) Ã¢â€ â€™ extrai candidatos de `expirationDate` via `getFieldSchema()` Ã¢â€ â€™ chama `needsBedrock()` (item 0) Ã¢â€ â€™ lÃƒÂª o kill switch `AI_EXTRACTION` (fail-closed) Ã¢â€ â€™ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** Ã¢â‚¬â€ retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃƒÂ¡ implica `true`) e a decisÃƒÂ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃƒÂ£o duplicada) `OcrArtifactStore` ganhou `get()` Ã¢â‚¬â€ o item 4 sÃƒÂ³ tinha `put()` (quem escreve o artefato); o item 5 ÃƒÂ© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada Ã¢â‚¬â€ sÃƒÂ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o uma decisÃƒÂ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃƒÂ£o tinha `ResultPath` na transiÃƒÂ§ÃƒÂ£o de sucesso do ASL, entÃƒÂ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃƒÂ­a inteiramente** o `$` do Step Functions Ã¢â‚¬â€ todo o contexto original da execuÃƒÂ§ÃƒÂ£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃƒÂ£o sÃƒÂ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃƒÂ³prio `job`) ao payload de sucesso. MudanÃƒÂ§a aditiva/reversÃƒÂ­vel sem migraÃƒÂ§ÃƒÂ£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃƒÂ³rico real).
- **DECISÃƒÆ’O PENDENTE (registrada aqui, default conservador aplicado)**: o design (Ã‚Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃƒÂ£o implementado Ã¢â‚¬â€ nenhum campo de metadado de PDF (data de criaÃƒÂ§ÃƒÂ£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃƒÂ£o de produto nÃƒÂ£o revisada, nÃƒÂ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃƒÂ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃƒÂ§ÃƒÂ£o na prÃƒÂ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃƒÂ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` Ã¢â‚¬â€ nÃƒÂ£o muda nenhum `Next`/fluxo, sÃƒÂ³ corrige o path de leitura para bater com a semÃƒÂ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃƒÂ§ÃƒÂ£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` Ã¢â‚¬â€ sÃƒÂ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃƒÂ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` Ã¢â‚¬â€ sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (mesmo padrÃƒÂ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28Ã¢â€ â€™29 funÃƒÂ§ÃƒÂµes (3 asserts: nomes, distinÃƒÂ§ÃƒÂ£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃƒÂ§ÃƒÂµes dia/mÃƒÂªs impossÃƒÂ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar Ã¢â‚¬â€ sÃƒÂ³ republish de versÃƒÂ£o prÃƒÂ©-existente em todas as Lambdas, mesmo ruÃƒÂ­do que o item 4 jÃƒÂ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` Ã¢â‚¬â€ drift **prÃƒÂ©-existente do item 4**, nÃƒÂ£o introduzido nesta sessÃƒÂ£o, causado por uma normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃƒÂ¡ detectava antes desta sessÃƒÂ£o comeÃƒÂ§ar; nÃƒÂ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 Ã¢â‚¬â€ ambos concluÃƒÂ­dos em sessÃƒÂµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver seÃƒÂ§ÃƒÂ£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃƒÂ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` Ã¢â‚¬â€ sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, na prÃƒÂ³pria ASL** (nÃƒÂ£o uma decisÃƒÂ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃƒÂ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃƒÂ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃƒÂ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃƒÂ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃƒÂ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` Ã¢â‚¬â€ `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃƒÂ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta Ã¢â‚¬â€ sem isso, `BedrockExtractionTaskHandler` nÃƒÂ£o teria como saber onde ler o texto OCR (o design Ã‚Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃƒÂ§ÃƒÂ£o de contexto que `completeOcr` (item 4/5) jÃƒÂ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura)**:
- DomÃƒÂ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` Ã¢â‚¬â€ `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃƒÂ¡vel, nunca concatenada com input do usuÃƒÂ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` Ã¢â‚¬â€ nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃƒÂ³ `textArtifact` Ã¢â‚¬â€ tipo local `BedrockTextArtifactRef`, estruturalmente idÃƒÂªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃƒÂ­be domÃƒÂ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) Ã¢â‚¬â€ nenhum import do SDK Bedrock fora do adapter.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) Ã¢â‚¬â€ re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃƒÂ¡ filtra isso mas a chamada paga nunca confia sÃƒÂ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃƒÂ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃƒÂ£o hÃƒÂ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz Ã¢â‚¬â€ nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) Ã¢â‚¬â€ Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃƒÂ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃƒÂªncia de tool-call, tool errado) Ã¢â‚¬â€ qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃƒÂ§ÃƒÂ£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` Ã¢â‚¬â€ DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃƒÂ£o configurÃƒÂ¡vel via `bedrock_region`, distinta da regiÃƒÂ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃƒÂ£o Bedrock: configurÃƒÂ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) Ã¢â‚¬â€ design Ã‚Â§4 bloqueia sÃƒÂ³ a escolha/validaÃƒÂ§ÃƒÂ£o real para produÃƒÂ§ÃƒÂ£o, nunca a testabilidade em `dev`; a permissÃƒÂ£o IAM jÃƒÂ¡ ÃƒÂ© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota Ã¢â‚¬â€ mesmo padrÃƒÂ£o do item 4 Ã¢â‚¬â€, `feature_flags_read_policy_json`, `s3:GetObject` sÃƒÂ³ leitura no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ nunca escrita, mesma disciplina de blast-radius do item 5 Ã¢â‚¬â€, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃƒÂ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29Ã¢â€ â€™30 funÃƒÂ§ÃƒÂµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 Ã¢â‚¬â€ caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃƒÂ§ÃƒÂ£o de quota em falha, **14Ã‚Âº caso adversarial de custo-abuso**: execuÃƒÂ§ÃƒÂ£o retentada/duplicada do mesmo `runId` reserva contra a prÃƒÂ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 Ã¢â‚¬â€ corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃƒÂ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃƒÂ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃƒÂ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Ã‚Â§1.11) nÃƒÂ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) Ã¢â‚¬â€ o corpus construÃƒÂ­do cobre as mesmas classes de ameaÃƒÂ§a descritas ali (injeÃƒÂ§ÃƒÂ£o via conteÃƒÂºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Ã‚Âº caso de custo-abuso) a partir de princÃƒÂ­pios, documentado no comentÃƒÂ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar Ã¢â‚¬â€ os do item 5 ainda nÃƒÂ£o aplicado + os do item 6 Ã¢â‚¬â€, 56 a mudar Ã¢â‚¬â€ mesmo ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ documentado nos itens 4/5 Ã¢â‚¬â€, 1 destroy-and-recreate Ã¢â‚¬â€ o MESMO drift prÃƒÂ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃƒÂ£o introduzido nesta sessÃƒÂ£o). **Nunca `terraform apply` local**.
- **DecisÃƒÂ£o pendente registrada, nÃƒÂ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃƒÂ©m de `expirationDate`, entÃƒÂ£o o schema fechado do tool `submit_extraction` sÃƒÂ³ tem essa uma propriedade Ã¢â‚¬â€ `buildSubmitExtractionToolSchema()` jÃƒÂ¡ ÃƒÂ© genÃƒÂ©rico sobre `FIELD_SCHEMA_V1`, entÃƒÂ£o um campo novo no schema nÃƒÂ£o precisa de mudanÃƒÂ§a de cÃƒÂ³digo aqui, sÃƒÂ³ uma nova versÃƒÂ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) Ã¢â‚¬â€ todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃƒÂ¡gio, design Ã‚Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃƒÂ£o mais importante desta sessÃƒÂ£o Ã¢â‚¬â€ a limpeza do artefato transitÃƒÂ³rio (design Ã‚Â§3, "achado mais sÃƒÂ©rio")**: `OcrArtifactStore.delete()` ÃƒÂ© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃƒÂ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃƒÂ³fica do parser determinÃƒÂ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` Ã¢â‚¬â€ design cita esse estado explicitamente como ponto de deleÃƒÂ§ÃƒÂ£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` Ã¢â‚¬â€ rodam antes do run alcanÃƒÂ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃƒÂªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado Ã¢â‚¬â€ zero atÃƒÂ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃƒÂ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` Ã¢â‚¬â€ `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃƒÂ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃƒÂ£o-vazio. Um candidato invÃƒÂ¡lido nunca ÃƒÂ© descartado silenciosamente Ã¢â‚¬â€ ÃƒÂ© marcado `valid: false` e tratado como "essa fonte nÃƒÂ£o produziu nada" na comparaÃƒÂ§ÃƒÂ£o.
- `compare-extractors.ts` Ã¢â‚¬â€ `compareExtractors(candidates)`: 0 candidatos usÃƒÂ¡veis Ã¢â€ â€™ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` Ã¢â‚¬â€ jÃƒÂ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` Ã¢â‚¬â€ nÃƒÂ£o representa separadamente; decisÃƒÂ£o registrada aqui em vez de alargar o tipo jÃƒÂ¡ commitado). 1 candidato Ã¢â€ â€™ `SINGLE_SOURCE` com valor. 2+ concordando Ã¢â€ â€™ `MATCH` (confianÃƒÂ§a = a maior das duas). 2+ discordando Ã¢â€ â€™ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" Ã¢â‚¬â€ `candidateValue` carrega sÃƒÂ³ o de maior confianÃƒÂ§a, para exibiÃƒÂ§ÃƒÂ£o, mas ÃƒÂ© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` Ã¢â‚¬â€ `decideFieldOutcome(comparison)`: sem `candidateValue` Ã¢â€ â€™ `PENDING_CONFIRMATION`; `MISMATCH` Ã¢â€ â€™ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃƒÂ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) Ã¢â€ â€™ `PENDING_CONFIRMATION`; senÃƒÂ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) Ã¢â€ â€™ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** Ã¢â‚¬â€ esse estado sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ÃƒÂ¡vel pela rota HTTP de rejeiÃƒÂ§ÃƒÂ£o humana (item 8, ainda nÃƒÂ£o implementada).

**DecisÃƒÂ£o de arquitetura tomada nesta sessÃƒÂ£o (nÃƒÂ£o estava no design, resolvida por inferÃƒÂªncia direta do enum jÃƒÂ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃƒÂ£o tem um estado "aguardando confirmaÃƒÂ§ÃƒÂ£o" separado Ã¢â‚¬â€ entÃƒÂ£o a distinÃƒÂ§ÃƒÂ£o "run completa vs. precisa de revisÃƒÂ£o humana" vive inteiramente no nÃƒÂ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃƒÂ¡lido Ã¢â€ â€™ comparaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ persistÃƒÂªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃƒÂ­da, hoje sÃƒÂ³ para log/observabilidade Ã¢â‚¬â€ nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` ÃƒÂ© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho Ã¢â‚¬â€ o prÃƒÂ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa ÃƒÂºnica operaÃƒÂ§ÃƒÂ£o, porque ÃƒÂ© o ÃƒÂºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, ÃƒÂ© uma decisÃƒÂ£o de produto pequena e reversÃƒÂ­vel (mudar sÃƒÂ³ a decisÃƒÂ£o de estado, nÃƒÂ£o a mecÃƒÂ¢nica de comparaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia) Ã¢â‚¬â€ registrado aqui para reabrir se necessÃƒÂ¡rio.

**Descarte por exclusÃƒÂ£o concorrente (design Ã‚Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃƒÂ£o do `Document` lida nÃƒÂ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃƒÂ£o sÃƒÂ³ um "ler status DELETED e torcer"). Se o `Document` jÃƒÂ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃƒÂ§ÃƒÂ£o falhar dentro da transaÃƒÂ§ÃƒÂ£o (mudanÃƒÂ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃƒÂ§ÃƒÂ£o como descarte Ã¢â‚¬â€ decisÃƒÂ£o conservadora documentada no comentÃƒÂ¡rio do adapter, jÃƒÂ¡ que distinguir a causa exata do cancelamento por cÃƒÂ³digo de erro do SDK seria frÃƒÂ¡gil entre versÃƒÂµes).

**Ports/persistÃƒÂªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) Ã¢â‚¬â€ expÃƒÂµe SÃƒâ€œ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num ÃƒÂºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` ÃƒÂ© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` Ã¢â‚¬â€ mÃƒÂ©todo standalone, usado SÃƒâ€œ no caminho de descarte (onde nÃƒÂ£o hÃƒÂ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` Ã¢â‚¬â€ **mudanÃƒÂ§a de contrato deliberada**: o item 4 tinha deixado esse mÃƒÂ©todo deliberadamente FORA do port ("a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" Ã¢â‚¬â€ tornava a exclusÃƒÂ£o acidental do chamador errado estruturalmente impossÃƒÂ­vel). Agora adicionado porque este Ãƒâ€° o chamador certo Ã¢â‚¬â€ `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃƒÂ³pria API do S3, deletar uma key ausente nÃƒÂ£o lanÃƒÂ§a). Os testes que antes afirmavam "o port nem expÃƒÂµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃƒÂ¡ que o mÃƒÂ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃƒÂ¡-lo).

**AplicaÃƒÂ§ÃƒÂ£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃƒÂ§ÃƒÂµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um ÃƒÂºnico `ValidationContext` que evolui atravÃƒÂ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃƒÂ£o exportado) que encapsula a leitura do `Document`+decisÃƒÂ£o discard-ou-commit. Um erro genuÃƒÂ­no de commit (nÃƒÂ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` Ã¢â‚¬â€ o `Catch`/`Retry` da ASL trata como transitÃƒÂ³rio).

**Achado real corrigido nesta sessÃƒÂ£o, em cÃƒÂ³digo do item 6 jÃƒÂ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` Ã¢â‚¬â€ a MESMA classe de bug que o item 5 jÃƒÂ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Ã‚Â§3's "achado mais sÃƒÂ©rio" ÃƒÂ© sobre limpeza de artefato, mas essa categoria de "output nÃƒÂ£o reanexa o contexto anterior" jÃƒÂ¡ apareceu 3 vezes agora: item 4Ã¢â€ â€™5, item 5Ã¢â€ â€™6, e este item 6Ã¢â€ â€™7). Corrigido de forma aditiva (campos novos sÃƒÂ³ ecoam o que jÃƒÂ¡ vinha em `input`, sem recomputar nada) Ã¢â‚¬â€ os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃƒÂ§ÃƒÂ£o (nÃƒÂ£o fazem `toEqual` no objeto inteiro). **PadrÃƒÂ£o a vigiar em qualquer implementaÃƒÂ§ÃƒÂ£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃƒÂ³ximo estado vai precisar, nunca assumir que "sÃƒÂ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃƒÂ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃƒÂ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃƒÂ©m ganhou `ResultSelector`/`OutputPath` (nÃƒÂ£o tinha nenhum antes Ã¢â‚¬â€ outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` Ã¢â‚¬â€ o footprint mais estreito dos quatro Lambdas de extraÃƒÂ§ÃƒÂ£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ este ÃƒÂ© o ÃƒÂºnico Lambda do pipeline inteiro com permissÃƒÂ£o de deletar lÃƒÂ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma ÃƒÂºnica `aws_lambda_permission` cobre as cinco invocaÃƒÂ§ÃƒÂµes (todas vÃƒÂªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30Ã¢â€ â€™31 funÃƒÂ§ÃƒÂµes (3 asserts).

**VerificaÃƒÂ§ÃƒÂ£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃƒÂ­cio da sessÃƒÂ£o Ã¢â‚¬â€ nÃƒÂ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃƒÂ£o, nÃƒÂºmero incorreto de digitaÃƒÂ§ÃƒÂ£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃƒÂ§ÃƒÂ£o do artefato descrito acima), mais 16 testes prÃƒÂ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃƒÂµes dos itens 5/6, nÃƒÂ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃƒÂ£o de produto pendente registrada, nÃƒÂ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` Ã¢â‚¬â€ isso ÃƒÂ© o item 8 (rotas de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7), ainda nÃƒÂ£o implementado. AtÃƒÂ© lÃƒÂ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃƒÂ£o hÃƒÂ¡ nenhuma forma de um operador vÃƒÂª-lo ou agir sobre ele pela API.

**Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora ÃƒÂ© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃƒÂ¡ existentes (items 4-7) e uma role de execuÃƒÂ§ÃƒÂ£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` Ã¢â‚¬â€ o mesmo nome que `local.extraction_state_machine_arn` jÃƒÂ¡ esperava desde o item 2, verificado por `terraform test` (nÃƒÂ£o confiado sÃƒÂ³ ÃƒÂ  leitura do cÃƒÂ³digo).

- **IAM da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado ÃƒÂ s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃƒÂªncia documentada da prÃƒÂ³pria AWS para `logging_configuration` de state machine Ã¢â‚¬â€ opera no subsistema de log delivery a nÃƒÂ­vel de conta, nÃƒÂ£o num log group especÃƒÂ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃƒÂ§ÃƒÂµes que `AWSXRayDaemonWriteAccess`, que o mÃƒÂ³dulo `lambda-function` jÃƒÂ¡ anexa a toda funÃƒÂ§ÃƒÂ£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃƒÂ¡-lo, escopado ÃƒÂ  ARN determinÃƒÂ­stica do state machine) jÃƒÂ¡ estava pronto desde os itens 4-7 Ã¢â‚¬â€ nÃƒÂ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃƒÂ§ÃƒÂ£o 30 dias, mesmo padrÃƒÂ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃƒÂ­vel `ERROR` (nÃƒÂ£o `ALL`) com `include_execution_data = false` Ã¢â‚¬â€ decisÃƒÂ£o deliberada espelhando a disciplina de privacidade do prÃƒÂ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃƒÂ§ÃƒÂ£o pode carregar texto OCR/valores de campo extraÃƒÂ­dos; nunca devem cair num log group com retenÃƒÂ§ÃƒÂ£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` Ã¢â‚¬â€ todo Lambda do repo jÃƒÂ¡ roda com ADOT/X-Ray ativo (AGENTS.md Ã‚Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Ã‚Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ o state machine, sua role e as 4 permissÃƒÂµes de invocaÃƒÂ§ÃƒÂ£o sempre existem (inspecionÃƒÂ¡veis/deployÃƒÂ¡veis). RaciocÃƒÂ­nio registrado no comentÃƒÂ¡rio do prÃƒÂ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃƒÂ¡ gateia o ÃƒÅ¡NICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que ÃƒÂ© quem chama `StartExecution`) Ã¢â‚¬â€ com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃƒÂ© ser iniciado manualmente para uma execuÃƒÂ§ÃƒÂ£o de teste, mas nunca recebe trÃƒÂ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) Ã¢â‚¬â€ 13Ã¢â€ â€™15 passando. MÃƒÂ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃƒÂªncia explÃƒÂ­cita registrada pelo item 7) Ã¢â‚¬â€ 5 `run` via `mock_provider`+`apply` (substituiÃƒÂ§ÃƒÂ£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃƒÂ¡s; `type = STANDARD`; role passada atravÃƒÂ©s sem modificaÃƒÂ§ÃƒÂ£o; tracing/logging configurados) Ã¢â‚¬â€ 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) Ã¢â‚¬â€ **a verificaÃƒÂ§ÃƒÂ£o crÃƒÂ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` Ã¢â€ â€™ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" Ã¢â‚¬â€ a definiÃƒÂ§ÃƒÂ£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃƒÂ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live`), jÃƒÂ¡ documentado nas sessÃƒÂµes dos itens 5/6/7, nÃƒÂ£o introduzido aqui. Nenhuma issue de integraÃƒÂ§ÃƒÂ£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃƒÂ­ram corretamente Ã¢â‚¬â€ nÃƒÂ£o foi necessÃƒÂ¡rio nenhum fix no `replace()` do mÃƒÂ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃƒÂ£o Ã¢â‚¬â€ sessÃƒÂ£o nÃƒÂ£o tocou cÃƒÂ³digo de aplicaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ infra). `terraform fmt -check -recursive` limpo em toda a ÃƒÂ¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃƒÂ£o precisar de `command = apply` contra o provider real). Novo output no mÃƒÂ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃƒÂ£o (branch `develop`, PR #60 `developÃ¢â€ â€™main` ainda aberto Ã¢â‚¬â€ push deve atualizÃƒÂ¡-lo automaticamente): instanciaÃƒÂ§ÃƒÂ£o do mÃƒÂ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: `ExtractionStarterWorker` completo Ã¢â‚¬â€ domÃƒÂ­nio/aplicaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia/handler real, nÃƒÂ£o sÃƒÂ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` Ã¢â‚¬â€ portas prÃƒÂ³prias, nÃƒÂ£o reusa `DocumentStore` do mÃƒÂ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃƒÂ¡ existia Ã¢â‚¬â€ gated sÃƒÂ³ no registro criaria um run ÃƒÂ³rfÃƒÂ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real ÃƒÂ© a idempotÃƒÂªncia nativa do `StartExecution` por nome de execuÃƒÂ§ÃƒÂ£o determinÃƒÂ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃƒÂ£o S3Ã¢â€ â€™EventBridgeÃ¢â€ â€™SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃƒÂ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` Ã¢â‚¬â€ insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃƒÂ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) Ã¢â‚¬â€ mudanÃƒÂ§a na M6 (jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o), mas segura: `cleanObject` ÃƒÂ© sempre um valor opaco armazenado na entidade, nunca reconstruÃƒÂ­do/reparsed em nenhum outro lugar do cÃƒÂ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃƒÂ¡veis/inspecionÃƒÂ¡veis), mas a regra EventBridge que liga o bucket limpo ÃƒÂ  fila ÃƒÂ© o ÃƒÅ¡NICO recurso condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃƒÂ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` ÃƒÂ© determinÃƒÂ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) Ã¢â‚¬â€ jÃƒÂ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃƒÂªncias) Ã¢â‚¬â€ comportamento condicional verificado, nÃƒÂ£o sÃƒÂ³ lido no cÃƒÂ³digo. **Ainda sem PR/merge** (mesma lÃƒÂ³gica do item 1: mergeÃƒÂ¡vel isoladamente, mas ainda aguardando decisÃƒÂ£o de "quando" Ã¢â‚¬â€ ver abaixo).
3. ASL Ã¢â‚¬â€ camada pura (o arquivo `.asl.json` e o mÃƒÂ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** (cÃƒÂ³digo+infra, nÃƒÂ£o sÃƒÂ³ camada pura) Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃƒÂ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 5 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (reanexaÃƒÂ§ÃƒÂ£o de contexto ao payload de sucesso de `completeOcr`, correÃƒÂ§ÃƒÂ£o do path `$.Payload.*` nas Choice states) e a decisÃƒÂ£o pendente registrada (sem heurÃƒÂ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 6 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (modelo/regiÃƒÂ£o placeholder configurÃƒÂ¡veis, correÃƒÂ§ÃƒÂ£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃƒÂ§ÃƒÂ£o do `artifact` ao output do item 5) e o corpus adversarial construÃƒÂ­do.
7. `ExtractionValidationTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 7 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃƒÂ¡ commitado, invariante de deleÃƒÂ§ÃƒÂ£o do artefato) e o que falta (item 8 ainda nÃƒÂ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" na seÃƒÂ§ÃƒÂ£o M7 acima (topo do arquivo) para o detalhe completo Ã¢â‚¬â€ `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃƒÂ§ÃƒÂ£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃƒÂ§ÃƒÂ£o "pendÃƒÂªncias explÃƒÂ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real Ã¢â‚¬â€ nÃƒÂ£o bloqueia implementaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ produÃƒÂ§ÃƒÂ£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` Ã¢â‚¬â€ o ÃƒÂºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃƒÂ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) Ã¢â‚¬â€ idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃƒÂª o `ExtractedField` jÃƒÂ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` ÃƒÂ© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) Ã¢â‚¬â€ lÃƒÂª os quatro, valida as 4 versÃƒÂµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃƒÂ§ÃƒÂ£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` Ã¢â‚¬â€ primeiro 422 do projeto). `reject` ÃƒÂ© OCC de 3 vias (nunca lÃƒÂª/toca `ExpirationItem` Ã¢â‚¬â€ nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoÃ¢â€ â€™atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃƒÂ³ `expirationDate` -> `dueDate` no schema v1) Ã¢â‚¬â€ quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃƒÂ§ÃƒÂ£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃƒÂ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃƒÂ§ÃƒÂ£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃƒÂ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` Ã¢â‚¬â€ `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃƒÂ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃƒÂ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` Ã¢â‚¬â€ um plain read eventualmente consistente, nunca usado pelos OCC writes jÃƒÂ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃƒÂ¡ existente) reaproveitado por `documents`/`items` na composition root Ã¢â‚¬â€ `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃƒÂ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` Ã¢â‚¬â€ `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) Ã¢â‚¬â€ mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` Ã¢â€ â€™ `authorize()` internamente via a application function Ã¢â€ â€™ `TenantQuotaService` `API_REQUEST` 100/60s Ã¢â€ â€™ schema Ajv Ã¢â€ â€™ chamada). AutorizaÃƒÂ§ÃƒÂ£o: `extraction:confirm` (aÃƒÂ§ÃƒÂ£o **jÃƒÂ¡ existia** na matriz desde uma sessÃƒÂ£o anterior Ã¢â‚¬â€ cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃƒÂ³pias desse mapa nos outros mÃƒÂ³dulos nÃƒÂ£o precisam, deixadas como estÃƒÂ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃƒÂ¡ tem `tenant_facing_read_write_policy_json` completo na tabela Ã¢â‚¬â€ zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃƒÂ£o de adapter `DynamoLike` que `ExpirationService` jÃƒÂ¡ usa para sua prÃƒÂ³pria `IdempotencyStore`, ao invÃƒÂ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` Ã¢â‚¬â€ o `source_arn` do `aws_lambda_permission.documents` jÃƒÂ¡ ÃƒÂ© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃƒÂ­do de republish de `source_code_hash` jÃƒÂ¡ documentado em toda sessÃƒÂ£o anterior deste milestone Ã¢â‚¬â€ o prÃƒÂ³prio `documents-handler` bundle mudou porque o cÃƒÂ³digo mudou, os outros 15 sÃƒÂ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas Ã¢â‚¬â€ nÃƒÂ£o repetido o gap histÃƒÂ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` Ã¢â‚¬â€ `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃƒÂ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` Ã¢â‚¬â€ reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃƒÂ¡lido/invÃƒÂ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos ÃƒÂ  mÃƒÂ£o sobre uma tabela DynamoDB em memÃƒÂ³ria com OCC real Ã¢â‚¬â€ `InMemoryTable.write()` sÃƒÂ³ aplica se a versÃƒÂ£o bate) Ã¢â‚¬â€ feliz confirm (item atualizado + versÃƒÂµes incrementadas), feliz reject (item intocado), idempotÃƒÂªncia de replay dos dois (sem re-executar a transaÃƒÂ§ÃƒÂ£o), 409 individual para cada uma das 4/3 versÃƒÂµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃƒÂ¡lido, 404 para cada uma das 4 entidades ausente, negaÃƒÂ§ÃƒÂ£o de autorizaÃƒÂ§ÃƒÂ£o sem membership. 2 fakes prÃƒÂ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃƒÂ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃƒÂ§ÃƒÂ£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃƒÂ­cio da sessÃƒÂ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃƒÂ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃƒÂ aconteceu, sÃƒÂ³ a persistÃƒÂªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒÆ’O libera a quota** Ã¢â‚¬â€ investigado nesta sessÃƒÂ£o e confirmado correto, nÃƒÂ£o uma lacuna: como a reserva ÃƒÂ© por-execuÃƒÂ§ÃƒÂ£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior, que `startOcr` jÃƒÂ¡ trata genericamente como "jÃƒÂ¡ reservado, nÃƒÂ£o ÃƒÂ© exaustÃƒÂ£o real" (linhas 86-90) Ã¢â‚¬â€ **independente de a reserva ter sido liberada ou nÃƒÂ£o**. Liberar aqui nÃƒÂ£o destravaria nada que jÃƒÂ¡ nÃƒÂ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃƒÂ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃƒÂ£o exato Ã¢â‚¬â€ falha da chamada Bedrock em si (apÃƒÂ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃƒÂ¡ existente, confirmado). NÃƒÂ£o hÃƒÂ¡ nenhum caminho de falha PÃƒâ€œS-chamada-bem-sucedida neste handler (ao contrÃƒÂ¡rio do Textract, o resultado do Bedrock ÃƒÂ© sÃƒÂ³ re-anexado ao payload de saÃƒÂ­da, nunca persistido localmente por este handler) Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃƒÂ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃƒÂ¡ completa e correta Ã¢â‚¬â€ nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio, nenhuma lacuna real encontrada. NÃƒÂ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador Ã¢â‚¬â€ fora de escopo do design aprovado, nÃƒÂ£o registrado como pendÃƒÂªncia.

## M7 Ã¢â‚¬â€ estado final desta sessÃƒÂ£o: cÃƒÂ³digo-completo para `dev`

Com os itens 1-9 concluÃƒÂ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃƒÂ¡ code-complete para testar em `dev`** Ã¢â‚¬â€ nÃƒÂ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃƒÂ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃƒÂ£o: nÃƒÂ£o hÃƒÂ¡ nenhum mÃƒÂ³dulo/recurso do pipeline de extraÃƒÂ§ÃƒÂ£o pendente de instanciaÃƒÂ§ÃƒÂ£o). O ÃƒÂºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃƒÂ§ÃƒÂ£o end-to-end real~~ Ã¢â‚¬â€ **FEITA em 2026-08-27**, ver a seÃƒÂ§ÃƒÂ£o `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃƒÂ£o, 1 documentado como decisÃƒÂ£o pendente).
- Runbook de calibraÃƒÂ§ÃƒÂ£o para job Textract "preso" (mencionado no item 3) Ã¢â‚¬â€ nÃƒÂ£o bloqueia, ÃƒÂ© operacional.

## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃƒÂ£o `StartExecution` na mÃƒÂ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃƒÂ§ÃƒÂ£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃƒÂ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` Ã¢â€ â€™ `exptrk-dev-extraction-starter-handler:live` Ã¢â€ â€™ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O ÃƒÂºnico elo originado ÃƒÂ  mÃƒÂ£o foi a mensagem SQS, porque a regra EventBridge que a produziria ÃƒÂ© o ÃƒÂºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃƒÂ¡-la exigiria `terraform apply` Ã¢â‚¬â€ proibido pelo Ã‚Â§3 do `AGENTS.md`. Nada alÃƒÂ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃƒÂ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃƒÂ£o ÃƒÂ© automatizÃƒÂ¡vel por CLI. **Nenhum resultado de extraÃƒÂ§ÃƒÂ£o foi falsificado** Ã¢â‚¬â€ todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃƒÂ§ÃƒÂ£o foi produzido pelo pipeline real.

**As 3 execuÃƒÂ§ÃƒÂµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃƒÂ§ÃƒÂ£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,Ã¢â‚¬Â¦}}`) | RunTextract(falhou) Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ CheckAiKillSwitch Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃƒÂ¡ria, envelope canÃƒÂ´nico com `OCR:true` | idÃƒÂªnticos ao anterior | SUCCEEDED, **ainda degradado** Ã¢â‚¬â€ evidÃƒÂªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃƒÂ¡ria, envelope canÃƒÂ´nico **+ cÃƒÂ³pia top-level** dos 3 booleanos | RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃƒÂªncia do caminho feliz (`output` real da execuÃƒÂ§ÃƒÂ£o `run_c1aefÃ¢â‚¬Â¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefÃ¢â‚¬Â¦/42d064cf-Ã¢â‚¬Â¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃƒÂ­stico extraiu a data correta**. `RunBedrock` NÃƒÆ’O foi exercitado (`needsBedrock:false` Ã¢â‚¬â€ o candidato determinÃƒÂ­stico jÃƒÂ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃƒÂ³sito). `CompleteRun` apagou o artefato transitÃƒÂ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃƒÂ§ÃƒÂ£o/quota/idempotÃƒÂªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, campo `PENDING_CONFIRMATION` Ã¢â€ â€™ `CONFIRMED` (`version` 1Ã¢â€ â€™2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) Ã¢â€ â€™ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃƒÂ§ÃƒÂ£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` Ã¢â€ â€™ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃƒÂ£o reaplicou).
- `reject` no campo de `run_a22dfÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, `PENDING_CONFIRMATION` Ã¢â€ â€™ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefÃ¢â‚¬Â¦` Ã¢â€ â€™ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) Ã¢â‚¬â€ ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado Ã¢â‚¬â€ pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃƒÂ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Ã‚Â§17.3 especifica) `{"features":{"AI_EXTRACTION":Ã¢â‚¬Â¦,"OCR":Ã¢â‚¬Â¦,"WHATSAPP":Ã¢â‚¬Â¦}}`, mas o adapter lia `parsed.OCR` no nÃƒÂ­vel raiz Ã¢â‚¬â€ logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃƒÂªncia isolada: a execuÃƒÂ§ÃƒÂ£o `run_a22dfÃ¢â‚¬Â¦` rodou com a versÃƒÂ£o AppConfig 2 (`OCR:true` dentro do envelope canÃƒÂ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefÃ¢â‚¬Â¦`, cuja ÃƒÂºnica diferenÃƒÂ§a era a cÃƒÂ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃƒÂ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃƒÂ­vel raiz) + teste de regressÃƒÂ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃƒÂ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados ÃƒÂ  lista de imports estÃƒÂ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃƒÂ³pria doc-string manda "add both the file AND a static import line". Toda chamada ÃƒÂ s duas rotas retornava 500 antes de qualquer lÃƒÂ³gica de negÃƒÂ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` Ã¢â‚¬â€ o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃƒÂ³ria dos testes unitÃƒÂ¡rios ignoram atributos extras na chave, por isso a suÃƒÂ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃƒÂ³ depois dessa correÃƒÂ§ÃƒÂ£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` Ã¢â‚¬â€ GAP REAL, NÃƒÆ’O CORRIGIDO (decisÃƒÂ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃƒÂ§a Ã¢â‚¬â€ foi exatamente o que aconteceu com `run_c1aefÃ¢â‚¬Â¦`, o caso de sucesso do pipeline. Mas o **ÃƒÂºnico** cÃƒÂ³digo que escreve `ExpirationItem.dueDate` ÃƒÂ© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃƒÂ¡ `CONFIRMED` (verificado, ver acima). ConsequÃƒÂªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** ÃƒÂ© atualizado e nÃƒÂ£o existe rota que o atualize. NÃƒÂ£o corrigido de propÃƒÂ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃƒÂ§ÃƒÂ£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" ÃƒÂ© decisÃƒÂ£o de produto/design (Type 1, Ã‚Â§4 do `AGENTS.md`), nÃƒÂ£o conserto mecÃƒÂ¢nico.

### Achado menor (nÃƒÂ£o corrigido, sem impacto observado nesta verificaÃƒÂ§ÃƒÂ£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, Ã¢â‚¬Â¦), mas o Step Functions casa contra o `errorType` do Lambda, que ÃƒÂ© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, Ã¢â‚¬Â¦). Confirmado no histÃƒÂ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃƒÂ£o** casou Ã¢â‚¬â€ quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃƒÂ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃƒÂ­ **hÃƒÂ¡** perda de comportamento (uma falha transitÃƒÂ³ria de commit nÃƒÂ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD Ã¢â‚¬â€ deixado para uma sessÃƒÂ£o de infra.

### Estado do ambiente apÃƒÂ³s a sessÃƒÂ£o

Flags AppConfig **revertidas**: versÃƒÂ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃƒÂµes temporÃƒÂ¡rias 2/3 deletadas Ã¢â‚¬â€ `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃƒÂ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃƒÂ³ o ruÃƒÂ­do jÃƒÂ¡ conhecido de republicaÃƒÂ§ÃƒÂ£o de versÃƒÂ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃƒÂµes deletadas), `ocr/` no bucket transitÃƒÂ³rio volta 0. Os histÃƒÂ³ricos das 3 execuÃƒÂ§ÃƒÂµes do Step Functions ficam (artefato normal da AWS, nÃƒÂ£o ÃƒÂ© dado de teste a limpar).

**As correÃƒÂ§ÃƒÂµes dos bugs 1 e 2 estÃƒÂ£o apenas em `develop`, ainda NÃƒÆ’O implantadas em `dev`** (o CD sÃƒÂ³ aplica em push para `main`). A verificaÃƒÂ§ÃƒÂ£o do `confirm` real rodou contra o bundle esbuild jÃƒÂ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃƒÂ© o prÃƒÂ³ximo merge para `main`.

**Bloqueado sÃƒÂ³ para produÃƒÂ§ÃƒÂ£o real** (nÃƒÂ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃƒÂ§ÃƒÂ£o de modelo Bedrock + regiÃƒÂ£o, RIPD formal (`privacy-lgpd.md` Ã‚Â§6, gatilho jÃƒÂ¡ registrado).

## DecisÃƒÂµes pendentes do Marcelo

1. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR)**: design jÃƒÂ¡ aprovado (D-035), zero decisÃƒÂ£o de produto pendente Ã¢â‚¬â€ sÃƒÂ³ falta decidir *quando* comeÃƒÂ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) Ã¢â‚¬â€ nÃƒÂ£o retomar sem sinal explÃƒÂ­cito dele.
3. **Estado real de `git`/deploy no inÃƒÂ­cio da prÃƒÂ³xima sessÃƒÂ£o nÃƒÂ£o deve ser presumido** Ã¢â‚¬â€ confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃƒÂªncias residuais nÃƒÂ£o bloqueantes (registradas, nÃƒÂ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃƒÂ©-existente nÃƒÂ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ Ã¢â‚¬â€ **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃƒÂºmeros (9 vulnerabilidades reais, nÃƒÂ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃƒÂ£o documentada (`testcontainers`Ã¢â€ â€™`dockerode`Ã¢â€ â€™`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃƒÂ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃƒÂ£o reverificado nesta sessÃƒÂ£o).
- Observabilidade por funÃƒÂ§ÃƒÂ£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃƒÂ©rica jÃƒÂ¡ cobre "estÃƒÂ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃƒÂ§ÃƒÂ£o de upload slot expirado) nunca exercitada contra AWS real~~ Ã¢â‚¬â€ **executada em 2026-08-25, achou um bug real de produÃƒÂ§ÃƒÂ£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado Ã¢â‚¬â€ confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃƒÂ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃƒÂ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃƒÂ³s a transiÃƒÂ§ÃƒÂ£o (ponteiro ÃƒÂ³rfÃƒÂ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃƒÂ§ÃƒÂ£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃƒÂ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃƒÂ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** Ã¢â‚¬â€ commit `2fab80f`, jÃƒÂ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃƒÂ£o deployado" estava desatualizada (verificado nesta sessÃƒÂ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃƒÂ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso Ã¢â‚¬â€ sÃƒÂ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` ÃƒÂ© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃƒÂ³ ÃƒÂ© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃƒÂ§ÃƒÂ£o defensiva do status real da occurrence antes de agir Ã¢â‚¬â€ um ponteiro ÃƒÂ³rfÃƒÂ£o nunca ÃƒÂ© revisitado fora dessa janela, e mesmo se fosse, ÃƒÂ© ignorado com seguranÃƒÂ§a. Diferente do bug do `upload-slot` (falha total Ã¢â‚¬â€ nada funcionava), este ÃƒÂ© um descompasso comentÃƒÂ¡rio-vs-cÃƒÂ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃƒÂ¡ revisado 9,2/10 pelo protocolo ClaudeÃ¢â€ â€Codex e ativo em produÃƒÂ§ÃƒÂ£o real Ã¢â‚¬â€ nÃƒÂ£o corrigido nesta sessÃƒÂ£o deliberadamente, para nÃƒÂ£o mexer num trecho crÃƒÂ­tico jÃƒÂ¡ extensivamente revisado sem uma rodada de revisÃƒÂ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃƒÂ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃƒÂ§ÃƒÂ£o real, Ã‚Â§13-30) que, se adotada como padrÃƒÂ£o oficial, deveria passar pela mesma convergÃƒÂªncia independente ClaudeÃ¢â€ â€Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃƒÂ¡ usaram Ã¢â‚¬â€ nÃƒÂ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃƒÂ£o de 12 eixos especÃƒÂ­fico do planejamento de interface, jÃƒÂ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃƒÂªncias (nÃƒÂ£o reler por padrÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ sob demanda)

- `docs/architecture/session-log.md` Ã¢â‚¬â€ linha do tempo compacta, uma entrada por sessÃƒÂ£o.
- `docs/architecture/decisions-log.md` Ã¢â‚¬â€ todas as decisÃƒÂµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` Ã¢â‚¬â€ os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` Ã¢â‚¬â€ o protÃƒÂ³tipo interativo executÃƒÂ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` Ã¢â‚¬â€ debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o real do Full BFF + fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o, achados do protocolo ClaudeÃ¢â€ â€Codex sobre o cÃƒÂ³digo, Final Status.


2026-08-28T21:14:52.636493Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'docs/architecture/README.md','docs/engineering/README.md','docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt','docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md','docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md','src/modules/identity/application/resolve-request-context.ts','src/modules/identity/persistence/identity-mapping-repository.ts','src/shared/dynamodb/occ.ts','src/modules/extraction/application/run-bedrock-extraction.ts','src/modules/extraction/application/start-ocr.ts','package.json','eslint.config.js'); foreach ("'$f in $files) { $n=(Get-Content -Encoding utf8 $f).Count; Write-Output "$n`t$f" }'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'docs/architecture/README.md','docs/engineering/README.md','docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt','docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md','docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md','src/modules/identity/application/resolve-request-context.ts','src/modules/identity/persistence/identity-mapping-repository.ts','src/shared/dynamodb/occ.ts','src/modules/extraction/application/run-bedrock-extraction.ts','src/modules/extraction/application/start-ocr.ts','package.json','eslint.config.js'); foreach ("'$f in $files) { $n=(Get-Content -Encoding utf8 $f).Count; Write-Output "$n`t$f" }' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'docs/architecture/README.md','docs/engineering/README.md','docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt','docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md','docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round2.md','src/modules/identity/application/resolve-request-context.ts','src/modules/identity/persistence/identity-mapping-repository.ts','src/shared/dynamodb/occ.ts','src/modules/extraction/application/run-bedrock-extraction.ts','src/modules/extraction/application/start-ocr.ts','package.json','eslint.config.js'); foreach ("'$f in $files) { $n=(Get-Content -Encoding utf8 $f).Count; Write-Output "$n`t$f" }'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw -Encoding utf8 "docs/architecture/README.md"' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 183ms:
# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃ§Ã£o LGPD sem purga fÃ­sica real e DSR 100% design-only, ambos registrados como decisÃ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃ­ntese final com recomendaÃ§Ã£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` â€” ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃ§Ã£o/OCR) code-complete e deployado em `dev`** â€” todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃ­stico/Bedrock/validaÃ§Ã£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃ§Ã£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3â†’SQSâ†’Step Functionsâ†’Textractâ†’parserâ†’persistÃªncia, ver `NEXT_SESSION_PROMPT.md`) â€” achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃ£o corrigido e aguardando decisÃ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` â€” nenhuma rota escreve isso hoje); M12 bloqueado por decisÃ£o de produto (D-052); Full BFF â€” D-053/D-054 â€” **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo Claudeâ†”Codex completo em `docs/frontend/frontend-production-foundation.md` â€” Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃ§a de sessÃ£o atÃ© convergir); planejamento de interface concluÃ­do â€” 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃ£o **todos resolvidos** (backend + frontend onde aplicÃ¡vel â€” BLOCKER-C via Variante B, revisÃ£o humana explÃ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada â€” ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃ³xima aÃ§Ã£o concreta, este bloco pode ficar temporariamente atrÃ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃ§Ã£o nÃ£o Ã© sequencial na ordem das linhas â€” D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw -Encoding utf8 "docs/engineering/README.md"' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 159ms:
# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO â€” 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃ³ Contexto bateu o gate â‰¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw -Encoding utf8 "docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt"' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 172ms:
2026-08-28T21:07:00.618902Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a04a32-89d5-7b80-9392-0b11cace87ff
--------
user
VocÃª Ã© o revisor adversarial independente (protocolo Claude-Codex de AGENTS.md secao 4) do repositorio
expiration-tracker (c:/Users/Usuario/Desktop/projects/expiration-tracker). Tarefa: W3-07, fence de exclusao
fisica de tenant (DSR), retomada apos duas tentativas reprovadas (D-062: 3,4-5,1-4,7/10; D-063 Rodada 1:
3,2/10 - pior que a anterior).

Leia primeiro, na integra, ANTES de avaliar a proposta nova:
1. docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md (D-062)
2. docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md (D-063)
3. docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md (a proposta nova a avaliar)
4. src/modules/identity/application/resolve-request-context.ts (resolver real)
5. src/shared/dynamodb/occ.ts (builders/primitivos reais ja existentes que a proposta reusa)
6. src/modules/subject/application/guest-submission-service.ts (superficie guest, se existir - confirme o caminho real)

Contexto necessario: a proposta nova muda a estrategia dos dois desenhos reprovados. Em vez de enumerar e
corrigir cada um dos 36 handlers de src/runtime/aws/handlers/ individualmente (a causa raiz do fracasso das
duas tentativas anteriores foi auditoria incompleta por essa via), ela propoe interceptar o fence na camada
de escrita compartilhada: um tombstone TenantLifecycleRecord fora do universo apagavel pela cascata, mais
dois wrappers novos (fencedTransactWrite/fencedSingleWrite) que toda escrita DynamoDB de dado de tenant
passaria a usar, reforcados por uma regra de dependency-cruiser que proibe construir TransactWriteCommand/
UpdateCommand/PutCommand/DeleteCommand fora desses wrappers. Efeitos externos irreversiveis (SES, Bedrock,
Textract) usam um protocolo de "decisao nasce de escrita fenced antes do efeito", nao uma checagem simples.
URLs S3 presignadas ja emitidas sao fechadas no ponto de finalizacao (persistencia), nao na emissao.

Sua tarefa: critique adversarialmente, do zero, sem aceitar nenhuma alegacao da proposta sem verificar contra
o codigo real do repositorio (grep, leitura de arquivo). Responda com:
1. Nota de 0 a 10 (protocolo de nota cega - nao existe nota anterior a comparar, esta e a primeira desta
   rodada).
2. Achados REAIS, cada um confirmado contra codigo/arquitetura real (nome de arquivo/linha quando aplicavel),
   nao hipoteticos. De atencao especial a: (a) os 19 arquivos de persistencia realmente cobrem TODA escrita
   de dado de tenant, ou existe algum call site fora desses 19 (grep exaustivo, nao confie na lista da
   proposta); (b) a regra de dependency-cruiser proposta e tecnicamente viavel para banir construcao direta
   desses comandos AWS SDK sem falsos positivos (ex.: os proprios wrappers fencedTransactWrite/
   fencedSingleWrite PRECISAM construir esses comandos - a regra precisa ter excecao correta); (c) o
   ConditionCheck contra TenantLifecycleRecord adicionado como PRIMEIRO item de um TransactWriteItems ja
   proximo do limite de 100 itens quebra algum caso real hoje; (d) a materializacao do RequestContextResolver
   (createProfileIfAbsent) realmente passa pelo wrapper fenced way descrito, ou tem um caminho proprio que
   escaparia; (e) o achado mais grave de D-063 (fence apagavel pela propria cascata) esta genuinamente
   resolvido, nao so deslocado; (f) o protocolo de "decisao nasce de escrita fenced" para SES/Bedrock tem
   alguma janela de corrida nao reconhecida pela propria proposta; (g) qualquer worker/handler que escreva
   dado de tenant SEM passar por nenhum dos 19 arquivos de persistencia listados (o achado exaustivo anterior
   levantou 36 handlers - confirme se algum deles bypassa os stores).
3. Recomendacao: aprovar (>=9.0), pedir revisao (com o que precisa mudar), ou reprovar.

Nao segure achados por educacao - o historico deste repositorio mostra que apontar "parece bom" sem verificar
levou a duas reprovacoes tardias e caras. Seja tao rigoroso quanto (ou mais que) as rodadas anteriores.

codex
Vou conduzir a revisÃ£o adversarial pelo protocolo do repositÃ³rio. Primeiro lerei integralmente os seis artefatos obrigatÃ³rios e os Ã­ndices de sessÃ£o/arquitetura; depois farei buscas exaustivas independentes por construÃ§Ãµes e execuÃ§Ãµes de comandos DynamoDB, limites transacionais e efeitos externos, sem tomar a lista da proposta como fonte de verdade.
2026-08-28T21:07:11.202530Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md; Get-Content -Raw src/modules/identity/application/resolve-request-context.ts; Get-Content -Raw src/shared/dynamodb/occ.ts; if (Test-Path src/modules/subject/application/guest-submission-service.ts) { Get-Content -Raw src/modules/subject/application/guest-submission-service.ts } else { Write-Output 'MISSING_GUEST_PATH'; rg --files src | rg 'guest|submission' }"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md; Get-Content -Raw src/modules/identity/application/resolve-request-context.ts; Get-Content -Raw src/shared/dynamodb/occ.ts; if (Test-Path src/modules/subject/application/guest-submission-service.ts) { Get-Content -Raw src/modules/subject/application/guest-submission-service.ts } else { Write-Output 'MISSING_GUEST_PATH'; rg --files src | rg 'guest|submission' }" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md; Get-Content -Raw src/modules/identity/application/resolve-request-context.ts; Get-Content -Raw src/shared/dynamodb/occ.ts; if (Test-Path src/modules/subject/application/guest-submission-service.ts) { Get-Content -Raw src/modules/subject/application/guest-submission-service.ts } else { Write-Output 'MISSING_GUEST_PATH'; rg --files src | rg 'guest|submission' }"` rejected: blocked by policy
codex
A leitura agrupada foi bloqueada pela polÃ­tica do terminal; vou fazer chamadas isoladas, reafirmando o diretÃ³rio em cada uma, como exige o AGENTS.md. Isso nÃ£o altera o escopo nem o mÃ©todo da revisÃ£o.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'NEXT_SESSION_PROMPT.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 180ms:
# Expiration Tracker Ã¢â‚¬â€ Status e PrÃƒÂ³xima SessÃƒÂ£o

> Este arquivo ÃƒÂ© estado atual + prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o (`AGENTS.md` Ã‚Â§2), nÃƒÂ£o histÃƒÂ³rico. Para a linha do tempo completa por sessÃƒÂ£o, ver `docs/architecture/session-log.md`; para toda decisÃƒÂ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃƒÂ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Ã‚Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) Ã¢â‚¬â€ confirmar `git log`/branch atual antes de assumir que o PR jÃƒÂ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃƒÂ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program Ã¢â‚¬â€ Waves 0/2/3/5 concluÃƒÂ­das, Wave 4 parcial (gated), Wave 1 nÃƒÂ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃƒÂ£o de consolidaÃƒÂ§ÃƒÂ£o/pilot-readiness. **Backlog canÃƒÂ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃƒÂ­ntese final/recomendaÃƒÂ§ÃƒÂ£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃƒÂ¡vel do prompt mestre Ã‚Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) Ã¢â‚¬â€ nÃƒÂ£o duplicar detalhe aqui, sÃƒÂ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃƒÂ§ÃƒÂ£o) `DONE`. **Wave 2 (evidÃƒÂªncia operacional) `DONE`** (2026-08-28) Ã¢â‚¬â€ os 6 drills (W2-03 a W2-08) executados com evidÃƒÂªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO Ã¢â€°Ë†3min44s, RPO nÃƒÂ£o medido Ã¢â‚¬â€ item aberto), load test real (977 invocaÃƒÂ§ÃƒÂµes, cota `API_REQUEST` segurando sob carga), alarme forÃƒÂ§ado com notificaÃƒÂ§ÃƒÂ£o SNSÃ¢â€ â€™e-mail real confirmada. **RPO (W2-06), dedupe pÃƒÂ³s-commit (W2-05) e o pipeline de alarme do W2-08 fechados na mesma sessÃƒÂ£o (2026-08-28)** Ã¢â‚¬â€ ver `pilot-readiness-program.md` seÃƒÂ§ÃƒÂ£o Wave 2 para o detalhe completo (inclui o achado real de que "credential-compromise via HTTP" ÃƒÂ© estruturalmente inalcanÃƒÂ§ÃƒÂ¡vel hoje, nÃƒÂ£o uma lacuna). Claims especÃƒÂ­ficas por drill: ver `docs/engineering/test-engineering-standard.md` Ã‚Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada. **`W3-06-DECISION` fechada e implementada nesta sessÃƒÂ£o (2026-08-28)**: desenho do mecanismo de purga real de `USER_DOCUMENT` aprovado via protocolo ClaudeÃ¢â€ â€Codex completo (6 rodadas, Codex 9,2/Claude 9,1, D-061 em `decisions-log.md`) e implementado de ponta a ponta Ã¢â‚¬â€ `DocumentPurgeWorker` real (claim/lease sobre GSI6, apaga o objeto S3 real via `cleanObject`/evidÃƒÂªncia Ã¢â‚¬â€ nunca `quarantineObject` Ã¢â‚¬â€ e a linha `Document`, grava `DocumentPurgeReceipt` nÃƒÂ£o sensÃƒÂ­vel), agendado a cada 6h, verificado com `terraform plan`/`terraform test` reais contra `dev` (ver `docs/architecture/reviews/w3-06-user-document-purge-design/`). Wave 4 (Identity/RBAC) `PARTIAL` Ã¢â‚¬â€ confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃƒÂ£o de negÃƒÂ³cio jÃƒÂ¡ registrada (`AGENTS.md` Ã‚Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) Ã¢â‚¬â€ `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃƒÂ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃƒÂ£o (2026-08-28) Ã¢â‚¬â€ auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard Ã¢â‚¬â€ novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃƒÂ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados com fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa precisa da Wave 2. Protocolo ClaudeÃ¢â€ â€Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃƒÂ­cito do Marcelo, acima do 9,0 padrÃƒÂ£o) Ã¢â‚¬â€ trajetÃƒÂ³ria Codex 6.35Ã¢â€ â€™7.85Ã¢â€ â€™8.70Ã¢â€ â€™9.18Ã¢â€ â€™9.34Ã¢â€ â€™9.46Ã¢â€ â€™9.48Ã¢â€ â€™**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃƒÂ§ÃƒÂ£o D-0xx). EvidÃƒÂªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃƒÂ£o vale para qualquer teste/drill novo a partir de agora** Ã¢â‚¬â€ antes de declarar um teste ou drill "concluÃƒÂ­do", checar contra os gates de Ã‚Â§3 (nunca reversÃƒÂ£o silenciosa, nunca claim mais ampla que a evidÃƒÂªncia).

**RecomendaÃƒÂ§ÃƒÂ£o da sÃƒÂ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃƒÂªs gates reais nomeados na sÃƒÂ­ntese original Ã¢â‚¬â€ guest trust (`GTR-01`/`W5-01`), evidÃƒÂªncia operacional (Wave 2), e retenÃƒÂ§ÃƒÂ£o/purga real (`W3-06`) Ã¢â‚¬â€ **fecharam todos nesta sessÃƒÂ£o**, e todos os itens secundÃƒÂ¡rios de Wave 2 (RPO, dedupe pÃƒÂ³s-commit, pipeline de alarme de credential-compromise) tambÃƒÂ©m fecharam na mesma sessÃƒÂ£o. ÃƒÅ¡nico item nÃƒÂ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).

954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃƒÂ§ÃƒÂ£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃƒÂ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃƒÂ§ÃƒÂ£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃƒÂ£o** (W2-06 RPO, W2-05 dedupe pÃƒÂ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃƒÂ£o, nenhuma aprovada**: (1) D-062, 4 rodadas ClaudeÃ¢â€ â€Codex, notas 3,4Ã¢â€ â€™5,1Ã¢â€ â€™4,7/10 Ã¢â‚¬â€ achado central: garantir "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃƒÂ­ncronos); Claude e Codex recomendaram convergentemente nÃƒÂ£o implementar sem esse fence, e a sessÃƒÂ£o parou por decisÃƒÂ£o do Marcelo. (2) O Marcelo entÃƒÂ£o decidiu NÃƒÆ’O esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃƒÂ­cio Ã¢â‚¬â€ D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃƒÂ¡ existente e jÃƒÂ¡ checado em `resolveRequestContext`) seria apagado pela prÃƒÂ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃƒÂ³ximo login Ã¢â‚¬â€ o mecanismo de autenticaÃƒÂ§ÃƒÂ£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada** (nÃƒÂ£o abandonado). **PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃƒÂ§ÃƒÂ£o de levantar superfÃƒÂ­cies de escrita por ponto de entrada de runtime Ã¢â‚¬â€ API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions Ã¢â‚¬â€ nunca por pasta de cÃƒÂ³digo, que jÃƒÂ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso Ã¢â‚¬â€ os 3 gates reais (W3-06/Wave 2/GTR-01) jÃƒÂ¡ fecharam.

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃƒÂ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃƒÂ³rio e passou a ter a primeira linguagem visual real do produto Ã¢â‚¬â€ arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃƒÂ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃƒÂ§ÃƒÂ£o **Operational Calm** aplicada ÃƒÂ s 5 superfÃƒÂ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION Ã¢â‚¬â€ PROVISIONAL PENDING USER VALIDATION` Ã¢â‚¬â€ a palavra PROVISIONAL ÃƒÂ© literal: nenhuma sessÃƒÂ£o com usuÃƒÂ¡rio aconteceu, e as **15** hipÃƒÂ³teses de design (hierarquia de navegaÃƒÂ§ÃƒÂ£o, densidade final, rÃƒÂ³tulos, colocaÃƒÂ§ÃƒÂ£o de aÃƒÂ§ÃƒÂµes secundÃƒÂ¡rias, accent, e as duas afirmaÃƒÂ§ÃƒÂµes subjetivas sobre a prÃƒÂ³pria direÃƒÂ§ÃƒÂ£o) estÃƒÂ£o registradas como adiamentos explÃƒÂ­citos no Ã‚Â§35 do documento, nÃƒÂ£o como fatos. **Zero dependÃƒÂªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de ÃƒÂ­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃƒÂ§a estrutural ÃƒÂºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃƒÂ¢ntica com urgÃƒÂªncia e situaÃƒÂ§ÃƒÂ£o em colunas separadas Ã¢â‚¬â€ mesmos dados/ordenaÃƒÂ§ÃƒÂ£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃƒÂªnticos, 3 grupos de urgÃƒÂªncia) foi verificada contra o cÃƒÂ³digo real do frontend **pela primeira vez** (antes sÃƒÂ³ contra o protÃƒÂ³tipo), com asserÃƒÂ§ÃƒÂ£o automatizada. Protocolo ClaudeÃ¢â€ â€Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 Ã¢â‚¬â€ `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃƒÂ§ÃƒÂ£o normal, entÃƒÂ£o o botÃƒÂ£o de submit continuava clicÃƒÂ¡vel durante a mutaÃƒÂ§ÃƒÂ£o exibindo "RenovandoÃ¢â‚¬Â¦"; e os cabeÃƒÂ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃƒÂ§am linhas Ã¢â‚¬â€ ambos corrigidos com teste de regressÃƒÂ£o. O protocolo **nÃƒÂ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃƒÂ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Ã‚Â§4) Ã¢â‚¬â€ o protocolo mais longo jÃƒÂ¡ executado neste repositÃƒÂ³rio. TrÃƒÂªs rodadas acharam defeitos **criados pela rodada de correÃƒÂ§ÃƒÂ£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃƒÂ£o foi cÃƒÂ³digo errado e sim **documentaÃƒÂ§ÃƒÂ£o afirmando prova mais ampla que a evidÃƒÂªncia nomeada**, que apareceu seis vezes: a alegaÃƒÂ§ÃƒÂ£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃƒÂ§ÃƒÂ£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃƒÂµes futuras deste repositÃƒÂ³rio. Acessibilidade virou executÃƒÂ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃƒÂ§ÃƒÂµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃƒÂ§ÃƒÂµes declaradas e registro de decisÃƒÂµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃƒÂ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃƒÂ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃƒÂªncia real registrada, nÃƒÂ£o bloqueante**: as baselines de regressÃƒÂ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃƒÂ£o por plataforma, entÃƒÂ£o o projeto Playwright `visual` ÃƒÂ© deliberadamente separado de `npm run test:e2e` e **nÃƒÂ£o** estÃƒÂ¡ gatilhado no CI hoje (plugÃƒÂ¡-lo falharia por baseline ausente, nÃƒÂ£o por regressÃƒÂ£o real). O caminho de adoÃƒÂ§ÃƒÂ£o estÃƒÂ¡ escrito no Ã‚Â§31 do documento: gravar as baselines num runner Linux, commitÃƒÂ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃƒÂ© lÃƒÂ¡ o CI cobre as mesmas superfÃƒÂ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃƒÂ©m nÃƒÂ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃƒÂ­veis no ambiente) Ã¢â‚¬â€ `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos Ã¢â‚¬â€ Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration Ã¢â‚¬â€ sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real Ã¢â€ â€™ BFF real Ã¢â€ â€™ API real Ã¢â€ â€™ persistÃƒÂªncia real). Nenhuma rota nova de BFF foi necessÃƒÂ¡ria (todas as 6 rotas de item jÃƒÂ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo ClaudeÃ¢â€ â€Codex (Round B adversarial achou 4 bugs reais Ã¢â‚¬â€ 1 S1: corrida TOCTOU real na reaquisiÃƒÂ§ÃƒÂ£o de um registro de idempotÃƒÂªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃƒÂ§ÃƒÂ£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃƒÂ³ `idempotency.complete()` falhasse depois], hash de renovaÃƒÂ§ÃƒÂ£o ambÃƒÂ­guo quando `cycle` ÃƒÂ© enviado independente de `newDueDate`, bug de fuso horÃƒÂ¡rio na formataÃƒÂ§ÃƒÂ£o de data da `Overview` Ã¢â‚¬â€ todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃƒÂ§ÃƒÂ£o tambÃƒÂ©m foi corrigido um bug real prÃƒÂ©-existente de liveness de idempotÃƒÂªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃƒÂªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) Ã¢â‚¬â€ a correÃƒÂ§ÃƒÂ£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃƒÂ§ado pela correÃƒÂ§ÃƒÂ£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃƒÂ§ÃƒÂ£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃƒÂ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃƒÂ§ÃƒÂ£o), 621 testes de backend Ã¢â‚¬â€ nenhuma regressÃƒÂ£o. Pendente ao fim desta sessÃƒÂ£o: push, PR para `develop`, CI, merge (ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" abaixo se ainda nÃƒÂ£o concluÃƒÂ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) Ã¢â‚¬â€ todos deployados em `main`/`dev`. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) teve implementaÃƒÂ§ÃƒÂ£o iniciada em 2026-08-25** (D-057, decisÃƒÂ£o direta do Marcelo Ã¢â‚¬â€ "prossiga entÃƒÂ£o") Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃƒÂ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃƒÂ£o disparou.

**Full BFF (autenticaÃƒÂ§ÃƒÂ£o de browser)**: design fechado via protocolo ClaudeÃ¢â€ â€Codex em duas rodadas Ã¢â‚¬â€ D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃƒÂ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` Ã¢â‚¬â€ 76 testes unitÃƒÂ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o real (`frontend/` Ã¢â‚¬â€ Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃƒÂ¡rio/componente + 6 smoke E2E via Playwright). Protocolo ClaudeÃ¢â€ â€Codex completo (Rodada D levou **6 passagens** atÃƒÂ© convergir Ã¢â‚¬â€ 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃƒÂ³digo prÃƒÂ©-correÃƒÂ§ÃƒÂ£o: consumo nÃƒÂ£o-atÃƒÂ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃƒÂ£o revogada concorrentemente; releituras pÃƒÂ³s-refresh nÃƒÂ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃƒÂ£o checando expiraÃƒÂ§ÃƒÂ£o/idle-TTL antes de agir; idle-TTL da prÃƒÂ³pria `Session` nunca validado contra o relÃƒÂ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃƒÂªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Ã‚Âº-8Ã‚Âº), cada um fechado via protocolo ClaudeÃ¢â€ â€Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` Ã¢â‚¬â€ papÃƒÂ©is, JTBD, inventÃƒÂ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` Ã¢â‚¬â€ modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` Ã¢â‚¬â€ 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` Ã¢â‚¬â€ 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes SurfaceÃ¢â€ â€Journey/Concept/Transition. RevisÃƒÂ£o Codex achou 3 furos reais de mesma causa raiz Ã¢â‚¬â€ `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃƒÂ§a em `SCANNING`, nÃƒÂ£o sÃƒÂ³ em `CLEAN`) Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` Ã¢â‚¬â€ wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃƒÂ£o Codex achou 5 furos reais + 1 divergÃƒÂªncia factual Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o primÃƒÂ¡ria ambÃƒÂ­gua em 4 coleÃƒÂ§ÃƒÂµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃƒÂ³digo executÃƒÂ¡vel real, HTML/CSS/JS sem dependÃƒÂªncias) Ã¢â‚¬â€ protÃƒÂ³tipo navegÃƒÂ¡vel cobrindo as 17 superfÃƒÂ­cies e J-01Ã¢â‚¬â€œJ-08 com 34 Prototype Scenario IDs determinÃƒÂ­sticos, verificado com testes automatizados de navegador headless. RevisÃƒÂ£o Codex achou 6 furos reais de 4 causas raiz Ã¢â‚¬â€ compressÃƒÂ£o de estados no guest upload (faltava validaÃƒÂ§ÃƒÂ£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃƒÂ§ÃƒÂ£o de coleta externa violando epistemic integrity (anunciava verificaÃƒÂ§ÃƒÂ£o de seguranÃƒÂ§a concluÃƒÂ­da ao operador, que nÃƒÂ£o pode observar isso), dois campos sem `<label>`, e reincidÃƒÂªncia de menÃƒÂ§ÃƒÂ£o a WhatsApp Ã¢â‚¬â€ todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` Ã¢â‚¬â€ avaliaÃƒÂ§ÃƒÂ£o do protÃƒÂ³tipo **executÃƒÂ¡vel** (nÃƒÂ£o sÃƒÂ³ a documentaÃƒÂ§ÃƒÂ£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃƒÂ¢ntica/forms testados em navegador headless real, nÃƒÂ£o sÃƒÂ³ lidos), `axe-core` (0 violaÃƒÂ§ÃƒÂµes em todos os estados testados, nas duas rodadas), re-execuÃƒÂ§ÃƒÂ£o de J-01Ã¢â‚¬â€œJ-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** ClaudeÃ¢â€ â€Codex (nÃƒÂ£o as 2-3 rodadas tÃƒÂ­picas de etapas anteriores): Rodada A (autoavaliaÃƒÂ§ÃƒÂ£o, 9 achados corrigidos) concluiu aprovaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃƒÂ£o vistos pela autoavaliaÃƒÂ§ÃƒÂ£o: o mais grave, a prÃƒÂ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃƒÂ§ÃƒÂ£o em 3 formulÃƒÂ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃƒÂ§ÃƒÂ£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃƒÂ¡-los no fake-backend Ã¢â‚¬â€ uma violaÃƒÂ§ÃƒÂ£o epistÃƒÂªmica real, nÃƒÂ£o cosmÃƒÂ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃƒÂ³prio ao manter um texto "SIMULATED FOR UX VALIDATION Ã¢â‚¬â€ NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃƒÂ­cie pÃƒÂºblica de guest (jargÃƒÂ£o tÃƒÂ©cnico visÃƒÂ­vel a um usuÃƒÂ¡rio externo nÃƒÂ£o-tÃƒÂ©cnico). Rodada D (nova adversarial sobre o cÃƒÂ³digo jÃƒÂ¡ corrigido) achou essa lacuna de conteÃƒÂºdo e uma correÃƒÂ§ÃƒÂ£o incompleta nova (`submitAlert` reabilitava o botÃƒÂ£o sÃƒÂ³ na branch de erro, nÃƒÂ£o na de sucesso) Ã¢â‚¬â€ ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃƒÂ£o arredondado; calculado refletindo esse histÃƒÂ³rico de 4 rodadas, nÃƒÂ£o sÃƒÂ³ o estado final do cÃƒÂ³digo) Ã¢â‚¬â€ `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` Ã¢â‚¬â€ "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃƒÂ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃƒÂ£o) no protÃƒÂ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃƒÂ§ÃƒÂ£o, nunca marcado como backend-resolvido; cenÃƒÂ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃƒÂ§ÃƒÂµes) que achou e corrigiu uma falta real de ordenaÃƒÂ§ÃƒÂ£o por urgÃƒÂªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃƒÂ£o sÃƒÂ³ no protÃƒÂ³tipo) Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃƒÂ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃƒÂ¡ em uso desde a 1Ã‚Âª etapa, agora formal, nenhum critÃƒÂ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃƒÂ§ÃƒÂ£o tÃƒÂ©cnica em Participant Mode, teste de idempotÃƒÂªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃƒÂµes de documentaÃƒÂ§ÃƒÂ£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃƒÂ©-existentes desde a Rodada A) Ã¢â‚¬â€ corrigidos no fechamento, zero contaminaÃƒÂ§ÃƒÂ£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃƒÂ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** Ã¢â‚¬â€ **fechado nesta sessÃƒÂ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` Ã¢â‚¬â€ `DocumentService.listDocuments`/`getDocument`, autorizaÃƒÂ§ÃƒÂ£o `document:read` (aÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃƒÂ§ÃƒÂ£o do item jÃƒÂ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃƒÂ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` Ã¢â‚¬â€ `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃƒÂ§ÃƒÂ£o `requirement:read` jÃƒÂ¡ existente (sem action nova reservada de antemÃƒÂ£o, diferente de `document:read`), mesma leitura por partiÃƒÂ§ÃƒÂ£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃƒÂ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© desta sessÃƒÂ£o, jÃƒÂ¡ estava corrigido antes (comentÃƒÂ¡rio no prÃƒÂ³prio `main.tf`), sÃƒÂ³ confirmado que segue correto.

  Ambas as metades: mudanÃƒÂ§a mecÃƒÂ¢nica sobre um padrÃƒÂ£o de acesso jÃƒÂ¡ aprovado no data model Ã¢â‚¬â€ nÃƒÂ£o passaram pelo protocolo `AGENTS.md` Ã‚Â§4 completo (nÃƒÂ£o ÃƒÂ© decisÃƒÂ£o de arquitetura nova, ÃƒÂ© fechamento de gap de leitura jÃƒÂ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃƒÂ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃƒÂ­cio da sessÃƒÂ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃƒÂ£o. **Achado real corrigido depois, na mesma sessÃƒÂ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` Ã¢â‚¬â€ mesma classe de gap que as rotas de `DocumentRequest` jÃƒÂ¡ tiveram antes (comentÃƒÂ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** Ã¢â‚¬â€ **implementaÃƒÂ§ÃƒÂ£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃƒÂ§ÃƒÂ£o abaixo). Antes desta branch, a materializaÃƒÂ§ÃƒÂ£o automÃƒÂ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃƒÂ§ÃƒÂ£o/ediÃƒÂ§ÃƒÂ£o de item. PendÃƒÂªncia real restante: decisÃƒÂ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) Ã¢â‚¬â€ nÃƒÂ£o bloqueia mais nada alÃƒÂ©m dessa decisÃƒÂ£o em si.
- **BLOCKER-C** Ã¢â‚¬â€ **decisÃƒÂ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃƒÂ£o humana explÃƒÂ­cita)**, confirmando a hipÃƒÂ³tese lÃƒÂ­der jÃƒÂ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Ã‚Â§22, `interface-conceptual-model-and-information-architecture.md` Ã‚Â§37, `interface-critical-user-journeys.md` Ã‚Â§37 Ã¢â‚¬â€ esses documentos continuam como registro histÃƒÂ³rico do debate, nÃƒÂ£o reescritos, per `AGENTS.md` regra de precedÃƒÂªncia de `history/`/artefatos aprovados). InvestigaÃƒÂ§ÃƒÂ£o confirmou que Variante A (fechamento automÃƒÂ¡tico) nÃƒÂ£o ÃƒÂ© viÃƒÂ¡vel hoje: `DocumentSubmission` nÃƒÂ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃƒÂ§a a qual `ExpirationItem` uma submissÃƒÂ£o pertence Ã¢â‚¬â€ decidir automaticamente seria adivinhar, nÃƒÂ£o resolver. **Mecanismo de backend para a Variante B jÃƒÂ¡ existe, nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio**: visibilidade do operador sobre submissÃƒÂµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃƒÂ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existente (`POST .../link`, prÃƒÂ©-existente). **Frontend implementado na mesma sessÃƒÂ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject Ã¢â‚¬â€ `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃƒÂ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores Ã¢â€ â€™ detalhe com requisitos Ã¢â€ â€™ "Revisar" expande evidÃƒÂªncia enviada (`DocumentSubmission`) Ã¢â€ â€™ operador informa o `itemId` de um vencimento jÃƒÂ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃƒÂ§ÃƒÂ£o de subject nem sugestÃƒÂ£o automÃƒÂ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃƒÂ³tulos seguem a mesma disciplina de Epistemic Integrity jÃƒÂ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃƒÂ§a) Ã¢â‚¬â€ conteÃƒÂºdo nÃƒÂ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta ÃƒÂºltima etapa: **`GTR-01`** (identidade do solicitante nÃƒÂ£o exposta ao guest) agora **simulada** no protÃƒÂ³tipo para User Validation, mas ainda nÃƒÂ£o resolvida no backend Ã¢â‚¬â€ `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃƒÂªncia) foi **resolvido no backend real** Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional Ã¢â‚¬â€ e, na sessÃƒÂ£o do Full BFF/frontend, o `requestHash` de detecÃƒÂ§ÃƒÂ£o de payload divergente foi migrado de concatenaÃƒÂ§ÃƒÂ£o por delimitador (colisÃƒÂ£o real possÃƒÂ­vel) para JSON canÃƒÂ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` Ã¢â‚¬â€ a lacuna "nenhum frontend/BFF real envia o header" estÃƒÂ¡ fechada para o caminho de criaÃƒÂ§ÃƒÂ£o de item.

**PrÃƒÂ³xima etapa natural do design** (ainda nÃƒÂ£o iniciada): **User Validation** Ã¢â‚¬â€ usando o protÃƒÂ³tipo jÃƒÂ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode ÃƒÂ© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Ã‚Â§15-18/Ã‚Â§23-24 (tese de produto, mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o, tarefas candidatas derivadas de outcomes, limitaÃƒÂ§ÃƒÂµes conhecidas do protÃƒÂ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃƒÂ£o produzido ainda. NÃƒÂ£o obriga inÃƒÂ­cio imediato de Visual Design/Design System.

## BLOCKER-B Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃƒÂ£o de 2026-08-24/25 (continuaÃƒÂ§ÃƒÂ£o apÃƒÂ³s a sessÃƒÂ£o anterior ter sido abortada por limite de tokens sÃƒÂ³ com reconhecimento parcial Ã¢â‚¬â€ handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃƒÂ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo ClaudeÃ¢â€ â€Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` Ã¢â‚¬â€ esse documento ÃƒÂ© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃƒÂ§ÃƒÂ£o real (nÃƒÂ£o sÃƒÂ³ design) tambÃƒÂ©m revisada via protocolo ClaudeÃ¢â€ â€Codex (2 rodadas, nota final 9,2/10, `APPROVED` Ã¢â‚¬â€ achados reais corrigidos: reclassificaÃƒÂ§ÃƒÂ£o de exceÃƒÂ§ÃƒÂ£o em dois mÃƒÂ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃƒÂ§ÃƒÂ£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃƒÂ§ÃƒÂ£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃƒÂªs novos mÃƒÂ©todos de reconciliaÃƒÂ§ÃƒÂ£o em `ReminderMaterializer` (fenced por versÃƒÂ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃƒÂ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃƒÂ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃƒÂ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃƒÂ§ÃƒÂ£o: o "caminho genÃƒÂ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃƒÂ³digo Ã¢â‚¬â€ `router_queue` nÃƒÂ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃƒÂ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃƒÂ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃƒÂ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Ã‚Â§8). **Implementado nesta sessÃƒÂ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃƒÂ§ÃƒÂ£o da criaÃƒÂ§ÃƒÂ£o do item (sem `ConditionCheck` de existÃƒÂªncia Ã¢â‚¬â€ o item novo ÃƒÂ© garantido pela prÃƒÂ³pria transaÃƒÂ§ÃƒÂ£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃƒÂ³ `ExpirationItem` (mudanÃƒÂ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" Ã¢â‚¬â€ reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃƒÂ£o estava instalado no inÃƒÂ­cio desta sessÃƒÂ£o (`npm ci` rodado para poder validar) Ã¢â‚¬â€ ambiente, nÃƒÂ£o cÃƒÂ³digo.

**PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o**: **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ todos os 9 itens numerados originais da lista estÃƒÂ£o CONCLUÃƒÂDOS (2026-08-26)**, cÃƒÂ³digo+infra reais para itens 1-9, nÃƒÂ£o sÃƒÂ³ camada pura Ã¢â‚¬â€ ver as seÃƒÂ§ÃƒÂµes dedicadas "Item N Ã¢â‚¬â€ CONCLUÃƒÂDO" na seÃƒÂ§ÃƒÂ£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Ã‚Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃƒÂ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) Ã¢â‚¬â€ causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃƒÂ­ficos com `States.ALL` num ÃƒÂºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃƒÂ£o pega esse erro, sÃƒÂ³ a chamada real de criaÃƒÂ§ÃƒÂ£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** Ã¢â‚¬â€ o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃƒÂ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃƒÂ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃƒÂ§ÃƒÂ£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃƒÂ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** Ã¢â‚¬â€ a nota de uma sessÃƒÂ£o anterior dizendo "nenhum profile corresponde ÃƒÂ  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃƒÂµes de escrita necessÃƒÂ¡rias e a **verificaÃƒÂ§ÃƒÂ£o E2E real foi executada** (ver `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃƒÂ§a de Terraform foi cogitada para isso Ã¢â‚¬â€ a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃƒÂ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃƒÂ³ o gatilho automÃƒÂ¡tico via EventBridge ÃƒÂ© que ÃƒÂ© gated). **RecorrÃƒÂªncia fechada na mesma sessÃƒÂ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃƒÂ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` Ã¢â‚¬â€ a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃƒÂ³ descoberto no meio de um `terraform apply` real. NÃƒÂ£o requer credencial de escrita (ÃƒÂ© uma chamada de validaÃƒÂ§ÃƒÂ£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃƒÂ£o retomar sem sinal explÃƒÂ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃƒÂ¡ aprovada em nÃƒÂ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Ã‚Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃƒÂ¡vel por hash; `implementation-blueprint.md` Ã‚Â§12/Ã‚Â§23: CSP com hashes estÃƒÂ¡ticos calculados no build, sem nonce dinÃƒÂ¢mico Ã¢â‚¬â€ Day 0 nÃƒÂ£o tem compute de borda). O que falta ÃƒÂ© o desenho de implementaÃƒÂ§ÃƒÂ£o real e o cÃƒÂ³digo Ã¢â‚¬â€ nenhuma decisÃƒÂ£o de produto pendente, mas hÃƒÂ¡ **uma decisÃƒÂ£o de arquitetura real ainda em aberto** que a prÃƒÂ³xima sessÃƒÂ£o precisa resolver antes de implementar (Type 1, nÃƒÂ­vel 5-6 da escala de risco Ã¢â‚¬â€ protocolo `AGENTS.md` Ã‚Â§4 aplicÃƒÂ¡vel):

1. **DecisÃƒÂ£o de arquitetura a fechar primeiro Ã¢â‚¬â€ FECHADA nesta sessÃƒÂ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo ClaudeÃ¢â€ â€Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** Ã¢â‚¬â€ `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` ÃƒÂ© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃƒÂªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma ÃƒÂºnica distribution, behaviors `/bff` e `/bff/*` Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior Ã¢â€ â€™ S3 com OAC + CloudFront Function de SPA routing (sÃƒÂ³ nesse behavior, nunca no do BFF). Zero mudanÃƒÂ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃƒÂ©-existentes descobertos pela revisÃƒÂ£o, a aplicar na implementaÃƒÂ§ÃƒÂ£o: `proxy-service.ts` nÃƒÂ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃƒÂ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃƒÂ£o implementado): acesso direto ao `execute-api` pÃƒÂºblico precisa de mitigaÃƒÂ§ÃƒÂ£o (header estÃƒÂ¡tico + WAF) antes de produÃƒÂ§ÃƒÂ£o pÃƒÂºblica real.
2. **MÃƒÂ³dulo Terraform `infra/modules/spa-hosting` Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃƒÂºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃƒÂ­citos Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default Ã¢â€ â€™ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃƒÂ©todo GET/HEAD, associada SÃƒâ€œ ao default behavior Ã¢â‚¬â€ nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃƒÂ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃƒÂ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃƒÂ©-existentes do ADR-0011 tambÃƒÂ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃƒÂ§ÃƒÂ£o errada do prÃƒÂ³prio ADR-0011): `var.spa_content_security_policy` nÃƒÂ£o precisa de hashes calculados por build Ã¢â‚¬â€ verificado contra o `frontend/dist/index.html` real, o build do Vite nÃƒÂ£o emite nenhum `<script>`/`<style>` inline, todo asset ÃƒÂ© carregado por URL externa jÃƒÂ¡ hasheada, entÃƒÂ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃƒÂ¡ existe em `frontend/index.html`) bastam sem hash nenhum Ã¢â‚¬â€ a variÃƒÂ¡vel ganhou um default determinÃƒÂ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃƒÂ£o consegue expressar). **MÃƒÂ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) Ã¢â‚¬â€ corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃƒÂ£o bloqueia o primeiro apply, sÃƒÂ³ o domÃƒÂ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro Ã¢â‚¬â€ confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃƒÂ£o sÃƒÂ³ contra mocks.
3. **Deploy imutÃƒÂ¡vel por hash Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: Vite jÃƒÂ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra ÃƒÂ© necessÃƒÂ¡rio (o hash no nome do arquivo jÃƒÂ¡ ÃƒÂ© o mecanismo de imutabilidade). `index.html` ÃƒÂ© tratado ÃƒÂ  parte (nÃƒÂ£o hasheado, `Cache-Control: no-cache`, upload por ÃƒÂºltimo).
4. **IntegraÃƒÂ§ÃƒÂ£o com `cd.yml` Ã¢â‚¬â€ IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃƒÂ£o um job novo) Ã¢â‚¬â€ `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** Ã¢â‚¬â€ assets antigos nunca sÃƒÂ£o removidos, ÃƒÂ© isso que mantÃƒÂ©m um rollback de `index.html` funcional, jÃƒÂ¡ que as referÃƒÂªncias antigas continuam servÃƒÂ­veis), upload de `index.html` por ÃƒÂºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃƒÂ¡ existente. **PendÃƒÂªncia real registrada, nÃƒÂ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃƒÂ³ cobre Lambdas Ã¢â‚¬â€ nÃƒÂ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃƒÂ£o `rollback.yml` (mecanismo existente) nÃƒÂ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃƒÂ£o bloqueia o deploy normal.
5. **VerificaÃƒÂ§ÃƒÂ£o real (Camada 3) Ã¢â‚¬â€ CONCLUÃƒÂDA (2026-08-25), achou e corrigiu um bug real de produÃƒÂ§ÃƒÂ£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃƒÂ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** Ã¢â‚¬â€ isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃƒÂ­tica fixa nÃƒÂ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃƒÂ§ÃƒÂ£o, tudo confirmado contra a distribution real:
   - `GET /` Ã¢â€ â€™ 200, `text/html`.
   - `GET /assets/index-*.js` Ã¢â€ â€™ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃƒÂ£o, V6 do ADR-0011) Ã¢â€ â€™ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) Ã¢â€ â€™ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) Ã¢â‚¬â€ nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) Ã¢â€ â€™ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃƒÂ£o (V4) Ã¢â€ â€™ 401 `application/json` (`AUTH_REQUIRED`) Ã¢â‚¬â€ nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃƒÂ£o (V5-404) Ã¢â€ â€™ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado Ã¢â‚¬â€ o caso 404-por-allowlist com sessÃƒÂ£o vÃƒÂ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃƒÂ£o foram exercitados por nÃƒÂ£o serem automatizÃƒÂ¡veis via `curl` sozinho; a propriedade crÃƒÂ­tica de seguranÃƒÂ§a Ã¢â‚¬â€ nenhum erro do BFF vira HTML da SPA Ã¢â‚¬â€ jÃƒÂ¡ estÃƒÂ¡ confirmada pelos casos acima).
   - Headers de seguranÃƒÂ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃƒÂ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy Ã¢â‚¬â€ exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃƒÂ£o sÃƒÂ³ via `terraform plan`.
   **PendÃƒÂªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃƒÂ£o lidos via CloudFront/OAC (sÃƒÂ³ por IAM de role de Lambda), entÃƒÂ£o nÃƒÂ£o tÃƒÂªm este bug Ã¢â‚¬â€ mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃƒÂ£o corrigido preventivamente nesta sessÃƒÂ£o (fora do escopo desta decisÃƒÂ£o, nenhum uso real hoje), sÃƒÂ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃƒÂ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o iniciada (2026-08-25, D-057)

Design jÃƒÂ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) Ã¢â‚¬â€ esta seÃƒÂ§ÃƒÂ£o ÃƒÂ© sobre a IMPLEMENTAÃƒâ€¡ÃƒÆ’O, o design nÃƒÂ£o muda. **Escopo ÃƒÂ© grande** (Step Functions, Textract assÃƒÂ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ vai continuar por mÃƒÂºltiplas sessÃƒÂµes, nÃƒÂ£o ÃƒÂ© razoÃƒÂ¡vel fechar num ÃƒÂºnico slice. Estado exato ao fim desta sessÃƒÂ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Ã‚Â§4 ganhou a classe `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ prÃƒÂ©-requisito de design que o prÃƒÂ³prio D-035 exige estar registrado antes de qualquer implementaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o ÃƒÂ© pendÃƒÂªncia pÃƒÂ³s-produÃƒÂ§ÃƒÂ£o).
2. MÃƒÂ³dulo novo `src/modules/extraction/domain/` (sÃƒÂ³ domÃƒÂ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` Ã¢â‚¬â€ `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃƒÂ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃƒÂ³pria chave de idempotÃƒÂªncia da execuÃƒÂ§ÃƒÂ£o).
   - `extracted-field.ts` Ã¢â‚¬â€ `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Ã‚Â§12.5.
   - `field-schema.ts` Ã¢â‚¬â€ schema v1 com sÃƒÂ³ `expirationDate` (ÃƒÂºnico campo que o design aprovado nomeia concretamente Ã¢â‚¬â€ deliberadamente nÃƒÂ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` Ã¢â‚¬â€ `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` Ã¢â‚¬â€ helper de safety net de 24h para o artefato transitÃƒÂ³rio (a exclusÃƒÂ£o real ÃƒÂ© explÃƒÂ­cita por `ExtractionValidationTaskHandler`, este helper ÃƒÂ© sÃƒÂ³ a rede de seguranÃƒÂ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃƒÂ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` Ã¢â‚¬â€ **ainda sem PR/merge para `main`**, dado que nÃƒÂ£o hÃƒÂ¡ nenhuma infra/runtime ativÃƒÂ¡vel ainda, nada a fazer deploy).

**Item 1 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃƒÂ§ÃƒÂ£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃƒÂ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃƒÂ³dulo corrigido pelo design Ã¢â‚¬â€ transversal, nÃƒÂ£o `extraction-appconfig`, jÃƒÂ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃƒÂªs defaults `false` (fail-closed, prÃƒÂ©-condiÃƒÂ§ÃƒÂµes externas de M7 nÃƒÂ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Ã‚Â§1.6, default `false`) Ã¢â‚¬â€ gate Terraform separado do pipeline inteiro, ainda nÃƒÂ£o referenciado por nenhum recurso (sÃƒÂ³ os workers futuros vÃƒÂ£o condicionar-se a ele). 2 `terraform test` do mÃƒÂ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) Ã¢â‚¬â€ CAMADA PURA CONCLUÃƒÂDA nesta sessÃƒÂ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃƒÂ£o que o item 0 seguiu: domÃƒÂ­nio+aplicaÃƒÂ§ÃƒÂ£o primeiro, depois handler Lambda/infra numa fatia posterior Ã¢â‚¬â€ deliberado, nÃƒÂ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` Ã¢â‚¬â€ cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` Ã¢â‚¬â€ entidade `TextractJob` (chave prÃƒÂ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃƒÂ£o tenant-scoped Ã¢â‚¬â€ `COMPLETE_OCR` sÃƒÂ³ tem `jobId` disponÃƒÂ­vel da notificaÃƒÂ§ÃƒÂ£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃƒÂ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃƒÂ§ÃƒÂ£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` Ã¢â‚¬â€ classificador heurÃƒÂ­stico puro (magic bytes > extensÃƒÂ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃƒÂ§a) quando nenhum sinal ÃƒÂ© conclusivo Ã¢â‚¬â€ quem decide que isso ÃƒÂ© `UnsupportedDocumentTypeError` ÃƒÂ© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed ÃƒÂ© responsabilidade do caller, nÃƒÂ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃƒÂ©todo de delete Ã¢â‚¬â€ a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃƒÂ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃƒÂ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) Ã¢â‚¬â€ orquestra classificaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ kill switch `OCR` (fail-closed) Ã¢â€ â€™ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` Ã¢â‚¬â€ um retry do mesmo run encontra `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior e trata como "jÃƒÂ¡ reservado", nÃƒÂ£o como falha real) Ã¢â€ â€™ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃƒÂ­stico) Ã¢â€ â€™ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` Ã¢â‚¬â€ retorna void, a execuÃƒÂ§ÃƒÂ£o do Step Functions fica parada no `taskToken` atÃƒÂ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃƒÂ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) Ã¢â‚¬â€ busca `TextractJob` por `jobId`; caso ÃƒÂ³rfÃƒÂ£o (nenhum job encontrado) faz UMA chamada de confirmaÃƒÂ§ÃƒÂ£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃƒÂ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃƒÂµe um mÃƒÂ©todo de delete); `taskTokenCiphertext` ÃƒÂ© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃƒÂ§ado, sem tocar no registro) em qualquer outro erro Ã¢â‚¬â€ a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto Ã¢â‚¬â€ `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃƒÂ£o ÃƒÂ© revalidado como JSON aninhado, mesma convenÃƒÂ§ÃƒÂ£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` Ã¢â‚¬â€ `RunTextract` ÃƒÂ© o ÃƒÂºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃƒÂ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃƒÂ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃƒÂ£o existem.
- `infra/modules/extraction-workflow/` Ã¢â‚¬â€ mÃƒÂ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃƒÂ§ÃƒÂ£o + role de execuÃƒÂ§ÃƒÂ£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒÆ’O chamado em `infra/main.tf`** Ã¢â‚¬â€ um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃƒÂ£o sÃƒÂ³ em runtime), e os 3 handlers dos itens 5-7 nÃƒÂ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃƒÂ³prio diretÃƒÂ³rio do mÃƒÂ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃƒÂ³dulo especificamente Ã¢â‚¬â€ pendÃƒÂªncia explÃƒÂ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos ÃƒÂ  mÃƒÂ£o (sem `vi.mock`), cobrindo: classificaÃƒÂ§ÃƒÂ£o por magic-bytes/extensÃƒÂ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃƒÂ§ÃƒÂ£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃƒÂªncia da reserva de quota sob retry do mesmo run; retry local de persistÃƒÂªncia do `TextractJob` (sucesso apÃƒÂ³s 1 falha, esgotamento apÃƒÂ³s N); `completeOcr`: job ÃƒÂ³rfÃƒÂ£o (com e sem falha na confirmaÃƒÂ§ÃƒÂ£o), jÃƒÂ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃƒÂ§ÃƒÂ£o multi-pÃƒÂ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃƒÂ³rio de `SendTask*` relanÃƒÂ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃƒÂ£o expÃƒÂµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na ÃƒÂ¡rvore inteira de `infra/`.

**Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃƒÂªs decisÃƒÂµes pendentes acima foram todas tomadas e implementadas (nÃƒÂ£o deixadas como pendÃƒÂªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃƒÂ£o final** (nÃƒÂ£o a extensÃƒÂ£o via layer) Ã¢â‚¬â€ `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/ÃƒÂºltimos flags conhecidos em memÃƒÂ³ria do processo. Motivo: a extensÃƒÂ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` Ã¢â‚¬â€ nÃƒÂ£o existe nenhum precedente disso no repo (sÃƒÂ³ `adot_layer_arn`, uma layer diferente), e este handler sÃƒÂ³ lÃƒÂª os flags uma vez por invocaÃƒÂ§ÃƒÂ£o de `START_OCR` (nÃƒÂ£o por pÃƒÂ¡gina/bloco), entÃƒÂ£o o custo/latÃƒÂªncia de uma chamada de API direta por invocaÃƒÂ§ÃƒÂ£o ÃƒÂ© equivalente ao que a extensÃƒÂ£o teria numa invocaÃƒÂ§ÃƒÂ£o fria de qualquer forma. Revisitar sÃƒÂ³ se o volume tornar isso um custo real (a extensÃƒÂ£o amortiza entre invocaÃƒÂ§ÃƒÂµes warm via cache local, este adapter sÃƒÂ³ amortiza dentro de uma sessÃƒÂ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃƒÂ£o reuso da CMK do BFF Ã¢â‚¬â€ mesma disciplina D-054: um token de callback do Step Functions ÃƒÂ© uma credencial viva tÃƒÂ£o sensÃƒÂ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃƒÂ³dulos independentemente deployÃƒÂ¡veis acoplaria rotaÃƒÂ§ÃƒÂ£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃƒÂªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃƒÂ³dulo, mesma razÃƒÂ£o do port).
- **TÃƒÂ³pico SNS de conclusÃƒÂ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar Ã¢â‚¬â€ distinta da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio Lambda.

Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃƒÂ§ÃƒÂ£o real `TaskTimedOut`/`TaskDoesNotExist`Ã¢â€ â€™`TERMINAL_QUIET`, `InvalidToken`Ã¢â€ â€™`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃƒÂ§ado), `AppConfigFeatureFlagsReader` Ã¢â‚¬â€ todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` Ã¢â‚¬â€ um ÃƒÂºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃƒÂ§ÃƒÂ£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃƒÂ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃƒÂ£o SSE-KMS Ã¢â‚¬â€ evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃƒÂ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃƒÂ³pico SNS, permissÃƒÂ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃƒÂ¡ escopada ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃƒÂ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** Ã¢â‚¬â€ mesma lÃƒÂ³gica do item 2: todo recurso aqui ÃƒÂ© inerte sozinho, porque `StartDocumentTextDetection` (a ÃƒÂºnica chamada paga) sÃƒÂ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃƒÂ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃƒÂ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27Ã¢â€ â€™28 funÃƒÂ§ÃƒÂµes (2 asserts).
- 11 testes novos (794 totais no backend) Ã¢â‚¬â€ sÃƒÂ³ para os dois adapters com lÃƒÂ³gica real de branching (`SfnTaskTokenSender`'s classificaÃƒÂ§ÃƒÂ£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃƒÂ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃƒÂ£o ganharam teste unitÃƒÂ¡rio dedicado, seguindo o MESMO precedente jÃƒÂ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃƒÂ©m nÃƒÂ£o tÃƒÂªm) Ã¢â‚¬â€ verificados via `terraform plan` real contra `dev`, nÃƒÂ£o via mock unitÃƒÂ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target Ã¢â‚¬â€ 0 erros, 0 destroys inesperados (sÃƒÂ³ o prÃƒÂ³prio slice + o ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ prÃƒÂ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Ã‚Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) Ã¢â‚¬â€ **ainda sem PR/merge para `main`** no momento em que este parÃƒÂ¡grafo foi escrito (abrir o PR ÃƒÂ© o prÃƒÂ³ximo passo imediato desta mesma sessÃƒÂ£o, ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 Ã¢â‚¬â€ a ASL's `RunDeterministicParser` ÃƒÂ© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃƒÂ£o hÃƒÂ¡ task token nenhum neste handler), entÃƒÂ£o ÃƒÂ© uma ÃƒÂºnica invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃƒÂ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` Ã¢â‚¬â€ `extractExpirationDateCandidates()`, regex determinÃƒÂ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃƒÂ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃƒÂ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃƒÂ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃƒÂ¡ a atÃƒÂ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃƒÂ§a fraca (0.5, abaixo do threshold) quando nÃƒÂ£o hÃƒÂ¡ palavra-chave prÃƒÂ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃƒÂ©rico que trunca o texto processado Ã¢â‚¬â€ a ÃƒÂºnica reutilizaÃƒÂ§ÃƒÂ£o real de M6 possÃƒÂ­vel aqui, jÃƒÂ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃƒÂ£o sobre texto OCR, e nunca extrai campos/datas (sÃƒÂ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃƒÂ³digo `DeterministicParserFailed`) Ã¢â‚¬â€ nÃƒÂ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃƒÂ¡ ÃƒÂ© o genÃƒÂ©rico `States.ALL`.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-deterministic-parser.ts` Ã¢â‚¬â€ `runDeterministicParser()`: lÃƒÂª o artefato OCR (quando presente) Ã¢â€ â€™ extrai candidatos de `expirationDate` via `getFieldSchema()` Ã¢â€ â€™ chama `needsBedrock()` (item 0) Ã¢â€ â€™ lÃƒÂª o kill switch `AI_EXTRACTION` (fail-closed) Ã¢â€ â€™ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** Ã¢â‚¬â€ retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃƒÂ¡ implica `true`) e a decisÃƒÂ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃƒÂ£o duplicada) `OcrArtifactStore` ganhou `get()` Ã¢â‚¬â€ o item 4 sÃƒÂ³ tinha `put()` (quem escreve o artefato); o item 5 ÃƒÂ© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada Ã¢â‚¬â€ sÃƒÂ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o uma decisÃƒÂ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃƒÂ£o tinha `ResultPath` na transiÃƒÂ§ÃƒÂ£o de sucesso do ASL, entÃƒÂ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃƒÂ­a inteiramente** o `$` do Step Functions Ã¢â‚¬â€ todo o contexto original da execuÃƒÂ§ÃƒÂ£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃƒÂ£o sÃƒÂ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃƒÂ³prio `job`) ao payload de sucesso. MudanÃƒÂ§a aditiva/reversÃƒÂ­vel sem migraÃƒÂ§ÃƒÂ£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃƒÂ³rico real).
- **DECISÃƒÆ’O PENDENTE (registrada aqui, default conservador aplicado)**: o design (Ã‚Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃƒÂ£o implementado Ã¢â‚¬â€ nenhum campo de metadado de PDF (data de criaÃƒÂ§ÃƒÂ£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃƒÂ£o de produto nÃƒÂ£o revisada, nÃƒÂ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃƒÂ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃƒÂ§ÃƒÂ£o na prÃƒÂ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃƒÂ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` Ã¢â‚¬â€ nÃƒÂ£o muda nenhum `Next`/fluxo, sÃƒÂ³ corrige o path de leitura para bater com a semÃƒÂ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃƒÂ§ÃƒÂ£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` Ã¢â‚¬â€ sÃƒÂ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃƒÂ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` Ã¢â‚¬â€ sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (mesmo padrÃƒÂ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28Ã¢â€ â€™29 funÃƒÂ§ÃƒÂµes (3 asserts: nomes, distinÃƒÂ§ÃƒÂ£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃƒÂ§ÃƒÂµes dia/mÃƒÂªs impossÃƒÂ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar Ã¢â‚¬â€ sÃƒÂ³ republish de versÃƒÂ£o prÃƒÂ©-existente em todas as Lambdas, mesmo ruÃƒÂ­do que o item 4 jÃƒÂ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` Ã¢â‚¬â€ drift **prÃƒÂ©-existente do item 4**, nÃƒÂ£o introduzido nesta sessÃƒÂ£o, causado por uma normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃƒÂ¡ detectava antes desta sessÃƒÂ£o comeÃƒÂ§ar; nÃƒÂ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 Ã¢â‚¬â€ ambos concluÃƒÂ­dos em sessÃƒÂµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver seÃƒÂ§ÃƒÂ£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃƒÂ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` Ã¢â‚¬â€ sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, na prÃƒÂ³pria ASL** (nÃƒÂ£o uma decisÃƒÂ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃƒÂ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃƒÂ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃƒÂ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃƒÂ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃƒÂ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` Ã¢â‚¬â€ `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃƒÂ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta Ã¢â‚¬â€ sem isso, `BedrockExtractionTaskHandler` nÃƒÂ£o teria como saber onde ler o texto OCR (o design Ã‚Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃƒÂ§ÃƒÂ£o de contexto que `completeOcr` (item 4/5) jÃƒÂ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura)**:
- DomÃƒÂ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` Ã¢â‚¬â€ `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃƒÂ¡vel, nunca concatenada com input do usuÃƒÂ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` Ã¢â‚¬â€ nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃƒÂ³ `textArtifact` Ã¢â‚¬â€ tipo local `BedrockTextArtifactRef`, estruturalmente idÃƒÂªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃƒÂ­be domÃƒÂ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) Ã¢â‚¬â€ nenhum import do SDK Bedrock fora do adapter.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) Ã¢â‚¬â€ re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃƒÂ¡ filtra isso mas a chamada paga nunca confia sÃƒÂ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃƒÂ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃƒÂ£o hÃƒÂ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz Ã¢â‚¬â€ nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) Ã¢â‚¬â€ Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃƒÂ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃƒÂªncia de tool-call, tool errado) Ã¢â‚¬â€ qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃƒÂ§ÃƒÂ£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` Ã¢â‚¬â€ DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃƒÂ£o configurÃƒÂ¡vel via `bedrock_region`, distinta da regiÃƒÂ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃƒÂ£o Bedrock: configurÃƒÂ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) Ã¢â‚¬â€ design Ã‚Â§4 bloqueia sÃƒÂ³ a escolha/validaÃƒÂ§ÃƒÂ£o real para produÃƒÂ§ÃƒÂ£o, nunca a testabilidade em `dev`; a permissÃƒÂ£o IAM jÃƒÂ¡ ÃƒÂ© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota Ã¢â‚¬â€ mesmo padrÃƒÂ£o do item 4 Ã¢â‚¬â€, `feature_flags_read_policy_json`, `s3:GetObject` sÃƒÂ³ leitura no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ nunca escrita, mesma disciplina de blast-radius do item 5 Ã¢â‚¬â€, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃƒÂ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29Ã¢â€ â€™30 funÃƒÂ§ÃƒÂµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 Ã¢â‚¬â€ caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃƒÂ§ÃƒÂ£o de quota em falha, **14Ã‚Âº caso adversarial de custo-abuso**: execuÃƒÂ§ÃƒÂ£o retentada/duplicada do mesmo `runId` reserva contra a prÃƒÂ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 Ã¢â‚¬â€ corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃƒÂ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃƒÂ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃƒÂ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Ã‚Â§1.11) nÃƒÂ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) Ã¢â‚¬â€ o corpus construÃƒÂ­do cobre as mesmas classes de ameaÃƒÂ§a descritas ali (injeÃƒÂ§ÃƒÂ£o via conteÃƒÂºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Ã‚Âº caso de custo-abuso) a partir de princÃƒÂ­pios, documentado no comentÃƒÂ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar Ã¢â‚¬â€ os do item 5 ainda nÃƒÂ£o aplicado + os do item 6 Ã¢â‚¬â€, 56 a mudar Ã¢â‚¬â€ mesmo ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ documentado nos itens 4/5 Ã¢â‚¬â€, 1 destroy-and-recreate Ã¢â‚¬â€ o MESMO drift prÃƒÂ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃƒÂ£o introduzido nesta sessÃƒÂ£o). **Nunca `terraform apply` local**.
- **DecisÃƒÂ£o pendente registrada, nÃƒÂ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃƒÂ©m de `expirationDate`, entÃƒÂ£o o schema fechado do tool `submit_extraction` sÃƒÂ³ tem essa uma propriedade Ã¢â‚¬â€ `buildSubmitExtractionToolSchema()` jÃƒÂ¡ ÃƒÂ© genÃƒÂ©rico sobre `FIELD_SCHEMA_V1`, entÃƒÂ£o um campo novo no schema nÃƒÂ£o precisa de mudanÃƒÂ§a de cÃƒÂ³digo aqui, sÃƒÂ³ uma nova versÃƒÂ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) Ã¢â‚¬â€ todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃƒÂ¡gio, design Ã‚Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃƒÂ£o mais importante desta sessÃƒÂ£o Ã¢â‚¬â€ a limpeza do artefato transitÃƒÂ³rio (design Ã‚Â§3, "achado mais sÃƒÂ©rio")**: `OcrArtifactStore.delete()` ÃƒÂ© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃƒÂ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃƒÂ³fica do parser determinÃƒÂ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` Ã¢â‚¬â€ design cita esse estado explicitamente como ponto de deleÃƒÂ§ÃƒÂ£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` Ã¢â‚¬â€ rodam antes do run alcanÃƒÂ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃƒÂªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado Ã¢â‚¬â€ zero atÃƒÂ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃƒÂ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` Ã¢â‚¬â€ `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃƒÂ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃƒÂ£o-vazio. Um candidato invÃƒÂ¡lido nunca ÃƒÂ© descartado silenciosamente Ã¢â‚¬â€ ÃƒÂ© marcado `valid: false` e tratado como "essa fonte nÃƒÂ£o produziu nada" na comparaÃƒÂ§ÃƒÂ£o.
- `compare-extractors.ts` Ã¢â‚¬â€ `compareExtractors(candidates)`: 0 candidatos usÃƒÂ¡veis Ã¢â€ â€™ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` Ã¢â‚¬â€ jÃƒÂ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` Ã¢â‚¬â€ nÃƒÂ£o representa separadamente; decisÃƒÂ£o registrada aqui em vez de alargar o tipo jÃƒÂ¡ commitado). 1 candidato Ã¢â€ â€™ `SINGLE_SOURCE` com valor. 2+ concordando Ã¢â€ â€™ `MATCH` (confianÃƒÂ§a = a maior das duas). 2+ discordando Ã¢â€ â€™ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" Ã¢â‚¬â€ `candidateValue` carrega sÃƒÂ³ o de maior confianÃƒÂ§a, para exibiÃƒÂ§ÃƒÂ£o, mas ÃƒÂ© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` Ã¢â‚¬â€ `decideFieldOutcome(comparison)`: sem `candidateValue` Ã¢â€ â€™ `PENDING_CONFIRMATION`; `MISMATCH` Ã¢â€ â€™ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃƒÂ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) Ã¢â€ â€™ `PENDING_CONFIRMATION`; senÃƒÂ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) Ã¢â€ â€™ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** Ã¢â‚¬â€ esse estado sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ÃƒÂ¡vel pela rota HTTP de rejeiÃƒÂ§ÃƒÂ£o humana (item 8, ainda nÃƒÂ£o implementada).

**DecisÃƒÂ£o de arquitetura tomada nesta sessÃƒÂ£o (nÃƒÂ£o estava no design, resolvida por inferÃƒÂªncia direta do enum jÃƒÂ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃƒÂ£o tem um estado "aguardando confirmaÃƒÂ§ÃƒÂ£o" separado Ã¢â‚¬â€ entÃƒÂ£o a distinÃƒÂ§ÃƒÂ£o "run completa vs. precisa de revisÃƒÂ£o humana" vive inteiramente no nÃƒÂ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃƒÂ¡lido Ã¢â€ â€™ comparaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ persistÃƒÂªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃƒÂ­da, hoje sÃƒÂ³ para log/observabilidade Ã¢â‚¬â€ nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` ÃƒÂ© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho Ã¢â‚¬â€ o prÃƒÂ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa ÃƒÂºnica operaÃƒÂ§ÃƒÂ£o, porque ÃƒÂ© o ÃƒÂºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, ÃƒÂ© uma decisÃƒÂ£o de produto pequena e reversÃƒÂ­vel (mudar sÃƒÂ³ a decisÃƒÂ£o de estado, nÃƒÂ£o a mecÃƒÂ¢nica de comparaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia) Ã¢â‚¬â€ registrado aqui para reabrir se necessÃƒÂ¡rio.

**Descarte por exclusÃƒÂ£o concorrente (design Ã‚Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃƒÂ£o do `Document` lida nÃƒÂ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃƒÂ£o sÃƒÂ³ um "ler status DELETED e torcer"). Se o `Document` jÃƒÂ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃƒÂ§ÃƒÂ£o falhar dentro da transaÃƒÂ§ÃƒÂ£o (mudanÃƒÂ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃƒÂ§ÃƒÂ£o como descarte Ã¢â‚¬â€ decisÃƒÂ£o conservadora documentada no comentÃƒÂ¡rio do adapter, jÃƒÂ¡ que distinguir a causa exata do cancelamento por cÃƒÂ³digo de erro do SDK seria frÃƒÂ¡gil entre versÃƒÂµes).

**Ports/persistÃƒÂªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) Ã¢â‚¬â€ expÃƒÂµe SÃƒâ€œ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num ÃƒÂºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` ÃƒÂ© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` Ã¢â‚¬â€ mÃƒÂ©todo standalone, usado SÃƒâ€œ no caminho de descarte (onde nÃƒÂ£o hÃƒÂ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` Ã¢â‚¬â€ **mudanÃƒÂ§a de contrato deliberada**: o item 4 tinha deixado esse mÃƒÂ©todo deliberadamente FORA do port ("a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" Ã¢â‚¬â€ tornava a exclusÃƒÂ£o acidental do chamador errado estruturalmente impossÃƒÂ­vel). Agora adicionado porque este Ãƒâ€° o chamador certo Ã¢â‚¬â€ `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃƒÂ³pria API do S3, deletar uma key ausente nÃƒÂ£o lanÃƒÂ§a). Os testes que antes afirmavam "o port nem expÃƒÂµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃƒÂ¡ que o mÃƒÂ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃƒÂ¡-lo).

**AplicaÃƒÂ§ÃƒÂ£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃƒÂ§ÃƒÂµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um ÃƒÂºnico `ValidationContext` que evolui atravÃƒÂ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃƒÂ£o exportado) que encapsula a leitura do `Document`+decisÃƒÂ£o discard-ou-commit. Um erro genuÃƒÂ­no de commit (nÃƒÂ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` Ã¢â‚¬â€ o `Catch`/`Retry` da ASL trata como transitÃƒÂ³rio).

**Achado real corrigido nesta sessÃƒÂ£o, em cÃƒÂ³digo do item 6 jÃƒÂ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` Ã¢â‚¬â€ a MESMA classe de bug que o item 5 jÃƒÂ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Ã‚Â§3's "achado mais sÃƒÂ©rio" ÃƒÂ© sobre limpeza de artefato, mas essa categoria de "output nÃƒÂ£o reanexa o contexto anterior" jÃƒÂ¡ apareceu 3 vezes agora: item 4Ã¢â€ â€™5, item 5Ã¢â€ â€™6, e este item 6Ã¢â€ â€™7). Corrigido de forma aditiva (campos novos sÃƒÂ³ ecoam o que jÃƒÂ¡ vinha em `input`, sem recomputar nada) Ã¢â‚¬â€ os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃƒÂ§ÃƒÂ£o (nÃƒÂ£o fazem `toEqual` no objeto inteiro). **PadrÃƒÂ£o a vigiar em qualquer implementaÃƒÂ§ÃƒÂ£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃƒÂ³ximo estado vai precisar, nunca assumir que "sÃƒÂ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃƒÂ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃƒÂ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃƒÂ©m ganhou `ResultSelector`/`OutputPath` (nÃƒÂ£o tinha nenhum antes Ã¢â‚¬â€ outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` Ã¢â‚¬â€ o footprint mais estreito dos quatro Lambdas de extraÃƒÂ§ÃƒÂ£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ este ÃƒÂ© o ÃƒÂºnico Lambda do pipeline inteiro com permissÃƒÂ£o de deletar lÃƒÂ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma ÃƒÂºnica `aws_lambda_permission` cobre as cinco invocaÃƒÂ§ÃƒÂµes (todas vÃƒÂªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30Ã¢â€ â€™31 funÃƒÂ§ÃƒÂµes (3 asserts).

**VerificaÃƒÂ§ÃƒÂ£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃƒÂ­cio da sessÃƒÂ£o Ã¢â‚¬â€ nÃƒÂ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃƒÂ£o, nÃƒÂºmero incorreto de digitaÃƒÂ§ÃƒÂ£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃƒÂ§ÃƒÂ£o do artefato descrito acima), mais 16 testes prÃƒÂ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃƒÂµes dos itens 5/6, nÃƒÂ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃƒÂ£o de produto pendente registrada, nÃƒÂ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` Ã¢â‚¬â€ isso ÃƒÂ© o item 8 (rotas de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7), ainda nÃƒÂ£o implementado. AtÃƒÂ© lÃƒÂ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃƒÂ£o hÃƒÂ¡ nenhuma forma de um operador vÃƒÂª-lo ou agir sobre ele pela API.

**Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora ÃƒÂ© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃƒÂ¡ existentes (items 4-7) e uma role de execuÃƒÂ§ÃƒÂ£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` Ã¢â‚¬â€ o mesmo nome que `local.extraction_state_machine_arn` jÃƒÂ¡ esperava desde o item 2, verificado por `terraform test` (nÃƒÂ£o confiado sÃƒÂ³ ÃƒÂ  leitura do cÃƒÂ³digo).

- **IAM da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado ÃƒÂ s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃƒÂªncia documentada da prÃƒÂ³pria AWS para `logging_configuration` de state machine Ã¢â‚¬â€ opera no subsistema de log delivery a nÃƒÂ­vel de conta, nÃƒÂ£o num log group especÃƒÂ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃƒÂ§ÃƒÂµes que `AWSXRayDaemonWriteAccess`, que o mÃƒÂ³dulo `lambda-function` jÃƒÂ¡ anexa a toda funÃƒÂ§ÃƒÂ£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃƒÂ¡-lo, escopado ÃƒÂ  ARN determinÃƒÂ­stica do state machine) jÃƒÂ¡ estava pronto desde os itens 4-7 Ã¢â‚¬â€ nÃƒÂ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃƒÂ§ÃƒÂ£o 30 dias, mesmo padrÃƒÂ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃƒÂ­vel `ERROR` (nÃƒÂ£o `ALL`) com `include_execution_data = false` Ã¢â‚¬â€ decisÃƒÂ£o deliberada espelhando a disciplina de privacidade do prÃƒÂ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃƒÂ§ÃƒÂ£o pode carregar texto OCR/valores de campo extraÃƒÂ­dos; nunca devem cair num log group com retenÃƒÂ§ÃƒÂ£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` Ã¢â‚¬â€ todo Lambda do repo jÃƒÂ¡ roda com ADOT/X-Ray ativo (AGENTS.md Ã‚Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Ã‚Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ o state machine, sua role e as 4 permissÃƒÂµes de invocaÃƒÂ§ÃƒÂ£o sempre existem (inspecionÃƒÂ¡veis/deployÃƒÂ¡veis). RaciocÃƒÂ­nio registrado no comentÃƒÂ¡rio do prÃƒÂ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃƒÂ¡ gateia o ÃƒÅ¡NICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que ÃƒÂ© quem chama `StartExecution`) Ã¢â‚¬â€ com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃƒÂ© ser iniciado manualmente para uma execuÃƒÂ§ÃƒÂ£o de teste, mas nunca recebe trÃƒÂ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) Ã¢â‚¬â€ 13Ã¢â€ â€™15 passando. MÃƒÂ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃƒÂªncia explÃƒÂ­cita registrada pelo item 7) Ã¢â‚¬â€ 5 `run` via `mock_provider`+`apply` (substituiÃƒÂ§ÃƒÂ£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃƒÂ¡s; `type = STANDARD`; role passada atravÃƒÂ©s sem modificaÃƒÂ§ÃƒÂ£o; tracing/logging configurados) Ã¢â‚¬â€ 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) Ã¢â‚¬â€ **a verificaÃƒÂ§ÃƒÂ£o crÃƒÂ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` Ã¢â€ â€™ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" Ã¢â‚¬â€ a definiÃƒÂ§ÃƒÂ£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃƒÂ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live`), jÃƒÂ¡ documentado nas sessÃƒÂµes dos itens 5/6/7, nÃƒÂ£o introduzido aqui. Nenhuma issue de integraÃƒÂ§ÃƒÂ£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃƒÂ­ram corretamente Ã¢â‚¬â€ nÃƒÂ£o foi necessÃƒÂ¡rio nenhum fix no `replace()` do mÃƒÂ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃƒÂ£o Ã¢â‚¬â€ sessÃƒÂ£o nÃƒÂ£o tocou cÃƒÂ³digo de aplicaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ infra). `terraform fmt -check -recursive` limpo em toda a ÃƒÂ¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃƒÂ£o precisar de `command = apply` contra o provider real). Novo output no mÃƒÂ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃƒÂ£o (branch `develop`, PR #60 `developÃ¢â€ â€™main` ainda aberto Ã¢â‚¬â€ push deve atualizÃƒÂ¡-lo automaticamente): instanciaÃƒÂ§ÃƒÂ£o do mÃƒÂ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: `ExtractionStarterWorker` completo Ã¢â‚¬â€ domÃƒÂ­nio/aplicaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia/handler real, nÃƒÂ£o sÃƒÂ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` Ã¢â‚¬â€ portas prÃƒÂ³prias, nÃƒÂ£o reusa `DocumentStore` do mÃƒÂ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃƒÂ¡ existia Ã¢â‚¬â€ gated sÃƒÂ³ no registro criaria um run ÃƒÂ³rfÃƒÂ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real ÃƒÂ© a idempotÃƒÂªncia nativa do `StartExecution` por nome de execuÃƒÂ§ÃƒÂ£o determinÃƒÂ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃƒÂ£o S3Ã¢â€ â€™EventBridgeÃ¢â€ â€™SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃƒÂ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` Ã¢â‚¬â€ insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃƒÂ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) Ã¢â‚¬â€ mudanÃƒÂ§a na M6 (jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o), mas segura: `cleanObject` ÃƒÂ© sempre um valor opaco armazenado na entidade, nunca reconstruÃƒÂ­do/reparsed em nenhum outro lugar do cÃƒÂ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃƒÂ¡veis/inspecionÃƒÂ¡veis), mas a regra EventBridge que liga o bucket limpo ÃƒÂ  fila ÃƒÂ© o ÃƒÅ¡NICO recurso condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃƒÂ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` ÃƒÂ© determinÃƒÂ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) Ã¢â‚¬â€ jÃƒÂ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃƒÂªncias) Ã¢â‚¬â€ comportamento condicional verificado, nÃƒÂ£o sÃƒÂ³ lido no cÃƒÂ³digo. **Ainda sem PR/merge** (mesma lÃƒÂ³gica do item 1: mergeÃƒÂ¡vel isoladamente, mas ainda aguardando decisÃƒÂ£o de "quando" Ã¢â‚¬â€ ver abaixo).
3. ASL Ã¢â‚¬â€ camada pura (o arquivo `.asl.json` e o mÃƒÂ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** (cÃƒÂ³digo+infra, nÃƒÂ£o sÃƒÂ³ camada pura) Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃƒÂ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 5 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (reanexaÃƒÂ§ÃƒÂ£o de contexto ao payload de sucesso de `completeOcr`, correÃƒÂ§ÃƒÂ£o do path `$.Payload.*` nas Choice states) e a decisÃƒÂ£o pendente registrada (sem heurÃƒÂ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 6 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (modelo/regiÃƒÂ£o placeholder configurÃƒÂ¡veis, correÃƒÂ§ÃƒÂ£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃƒÂ§ÃƒÂ£o do `artifact` ao output do item 5) e o corpus adversarial construÃƒÂ­do.
7. `ExtractionValidationTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 7 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃƒÂ¡ commitado, invariante de deleÃƒÂ§ÃƒÂ£o do artefato) e o que falta (item 8 ainda nÃƒÂ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" na seÃƒÂ§ÃƒÂ£o M7 acima (topo do arquivo) para o detalhe completo Ã¢â‚¬â€ `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃƒÂ§ÃƒÂ£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃƒÂ§ÃƒÂ£o "pendÃƒÂªncias explÃƒÂ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real Ã¢â‚¬â€ nÃƒÂ£o bloqueia implementaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ produÃƒÂ§ÃƒÂ£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` Ã¢â‚¬â€ o ÃƒÂºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃƒÂ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) Ã¢â‚¬â€ idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃƒÂª o `ExtractedField` jÃƒÂ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` ÃƒÂ© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) Ã¢â‚¬â€ lÃƒÂª os quatro, valida as 4 versÃƒÂµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃƒÂ§ÃƒÂ£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` Ã¢â‚¬â€ primeiro 422 do projeto). `reject` ÃƒÂ© OCC de 3 vias (nunca lÃƒÂª/toca `ExpirationItem` Ã¢â‚¬â€ nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoÃ¢â€ â€™atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃƒÂ³ `expirationDate` -> `dueDate` no schema v1) Ã¢â‚¬â€ quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃƒÂ§ÃƒÂ£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃƒÂ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃƒÂ§ÃƒÂ£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃƒÂ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` Ã¢â‚¬â€ `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃƒÂ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃƒÂ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` Ã¢â‚¬â€ um plain read eventualmente consistente, nunca usado pelos OCC writes jÃƒÂ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃƒÂ¡ existente) reaproveitado por `documents`/`items` na composition root Ã¢â‚¬â€ `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃƒÂ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` Ã¢â‚¬â€ `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) Ã¢â‚¬â€ mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` Ã¢â€ â€™ `authorize()` internamente via a application function Ã¢â€ â€™ `TenantQuotaService` `API_REQUEST` 100/60s Ã¢â€ â€™ schema Ajv Ã¢â€ â€™ chamada). AutorizaÃƒÂ§ÃƒÂ£o: `extraction:confirm` (aÃƒÂ§ÃƒÂ£o **jÃƒÂ¡ existia** na matriz desde uma sessÃƒÂ£o anterior Ã¢â‚¬â€ cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃƒÂ³pias desse mapa nos outros mÃƒÂ³dulos nÃƒÂ£o precisam, deixadas como estÃƒÂ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃƒÂ¡ tem `tenant_facing_read_write_policy_json` completo na tabela Ã¢â‚¬â€ zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃƒÂ£o de adapter `DynamoLike` que `ExpirationService` jÃƒÂ¡ usa para sua prÃƒÂ³pria `IdempotencyStore`, ao invÃƒÂ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` Ã¢â‚¬â€ o `source_arn` do `aws_lambda_permission.documents` jÃƒÂ¡ ÃƒÂ© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃƒÂ­do de republish de `source_code_hash` jÃƒÂ¡ documentado em toda sessÃƒÂ£o anterior deste milestone Ã¢â‚¬â€ o prÃƒÂ³prio `documents-handler` bundle mudou porque o cÃƒÂ³digo mudou, os outros 15 sÃƒÂ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas Ã¢â‚¬â€ nÃƒÂ£o repetido o gap histÃƒÂ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` Ã¢â‚¬â€ `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃƒÂ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` Ã¢â‚¬â€ reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃƒÂ¡lido/invÃƒÂ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos ÃƒÂ  mÃƒÂ£o sobre uma tabela DynamoDB em memÃƒÂ³ria com OCC real Ã¢â‚¬â€ `InMemoryTable.write()` sÃƒÂ³ aplica se a versÃƒÂ£o bate) Ã¢â‚¬â€ feliz confirm (item atualizado + versÃƒÂµes incrementadas), feliz reject (item intocado), idempotÃƒÂªncia de replay dos dois (sem re-executar a transaÃƒÂ§ÃƒÂ£o), 409 individual para cada uma das 4/3 versÃƒÂµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃƒÂ¡lido, 404 para cada uma das 4 entidades ausente, negaÃƒÂ§ÃƒÂ£o de autorizaÃƒÂ§ÃƒÂ£o sem membership. 2 fakes prÃƒÂ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃƒÂ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃƒÂ§ÃƒÂ£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃƒÂ­cio da sessÃƒÂ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃƒÂ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃƒÂ aconteceu, sÃƒÂ³ a persistÃƒÂªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒÆ’O libera a quota** Ã¢â‚¬â€ investigado nesta sessÃƒÂ£o e confirmado correto, nÃƒÂ£o uma lacuna: como a reserva ÃƒÂ© por-execuÃƒÂ§ÃƒÂ£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior, que `startOcr` jÃƒÂ¡ trata genericamente como "jÃƒÂ¡ reservado, nÃƒÂ£o ÃƒÂ© exaustÃƒÂ£o real" (linhas 86-90) Ã¢â‚¬â€ **independente de a reserva ter sido liberada ou nÃƒÂ£o**. Liberar aqui nÃƒÂ£o destravaria nada que jÃƒÂ¡ nÃƒÂ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃƒÂ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃƒÂ£o exato Ã¢â‚¬â€ falha da chamada Bedrock em si (apÃƒÂ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃƒÂ¡ existente, confirmado). NÃƒÂ£o hÃƒÂ¡ nenhum caminho de falha PÃƒâ€œS-chamada-bem-sucedida neste handler (ao contrÃƒÂ¡rio do Textract, o resultado do Bedrock ÃƒÂ© sÃƒÂ³ re-anexado ao payload de saÃƒÂ­da, nunca persistido localmente por este handler) Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃƒÂ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃƒÂ¡ completa e correta Ã¢â‚¬â€ nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio, nenhuma lacuna real encontrada. NÃƒÂ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador Ã¢â‚¬â€ fora de escopo do design aprovado, nÃƒÂ£o registrado como pendÃƒÂªncia.

## M7 Ã¢â‚¬â€ estado final desta sessÃƒÂ£o: cÃƒÂ³digo-completo para `dev`

Com os itens 1-9 concluÃƒÂ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃƒÂ¡ code-complete para testar em `dev`** Ã¢â‚¬â€ nÃƒÂ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃƒÂ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃƒÂ£o: nÃƒÂ£o hÃƒÂ¡ nenhum mÃƒÂ³dulo/recurso do pipeline de extraÃƒÂ§ÃƒÂ£o pendente de instanciaÃƒÂ§ÃƒÂ£o). O ÃƒÂºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃƒÂ§ÃƒÂ£o end-to-end real~~ Ã¢â‚¬â€ **FEITA em 2026-08-27**, ver a seÃƒÂ§ÃƒÂ£o `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃƒÂ£o, 1 documentado como decisÃƒÂ£o pendente).
- Runbook de calibraÃƒÂ§ÃƒÂ£o para job Textract "preso" (mencionado no item 3) Ã¢â‚¬â€ nÃƒÂ£o bloqueia, ÃƒÂ© operacional.

## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃƒÂ£o `StartExecution` na mÃƒÂ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃƒÂ§ÃƒÂ£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃƒÂ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` Ã¢â€ â€™ `exptrk-dev-extraction-starter-handler:live` Ã¢â€ â€™ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O ÃƒÂºnico elo originado ÃƒÂ  mÃƒÂ£o foi a mensagem SQS, porque a regra EventBridge que a produziria ÃƒÂ© o ÃƒÂºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃƒÂ¡-la exigiria `terraform apply` Ã¢â‚¬â€ proibido pelo Ã‚Â§3 do `AGENTS.md`. Nada alÃƒÂ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃƒÂ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃƒÂ£o ÃƒÂ© automatizÃƒÂ¡vel por CLI. **Nenhum resultado de extraÃƒÂ§ÃƒÂ£o foi falsificado** Ã¢â‚¬â€ todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃƒÂ§ÃƒÂ£o foi produzido pelo pipeline real.

**As 3 execuÃƒÂ§ÃƒÂµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃƒÂ§ÃƒÂ£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,Ã¢â‚¬Â¦}}`) | RunTextract(falhou) Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ CheckAiKillSwitch Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃƒÂ¡ria, envelope canÃƒÂ´nico com `OCR:true` | idÃƒÂªnticos ao anterior | SUCCEEDED, **ainda degradado** Ã¢â‚¬â€ evidÃƒÂªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃƒÂ¡ria, envelope canÃƒÂ´nico **+ cÃƒÂ³pia top-level** dos 3 booleanos | RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃƒÂªncia do caminho feliz (`output` real da execuÃƒÂ§ÃƒÂ£o `run_c1aefÃ¢â‚¬Â¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefÃ¢â‚¬Â¦/42d064cf-Ã¢â‚¬Â¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃƒÂ­stico extraiu a data correta**. `RunBedrock` NÃƒÆ’O foi exercitado (`needsBedrock:false` Ã¢â‚¬â€ o candidato determinÃƒÂ­stico jÃƒÂ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃƒÂ³sito). `CompleteRun` apagou o artefato transitÃƒÂ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃƒÂ§ÃƒÂ£o/quota/idempotÃƒÂªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, campo `PENDING_CONFIRMATION` Ã¢â€ â€™ `CONFIRMED` (`version` 1Ã¢â€ â€™2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) Ã¢â€ â€™ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃƒÂ§ÃƒÂ£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` Ã¢â€ â€™ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃƒÂ£o reaplicou).
- `reject` no campo de `run_a22dfÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, `PENDING_CONFIRMATION` Ã¢â€ â€™ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefÃ¢â‚¬Â¦` Ã¢â€ â€™ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) Ã¢â‚¬â€ ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado Ã¢â‚¬â€ pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃƒÂ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Ã‚Â§17.3 especifica) `{"features":{"AI_EXTRACTION":Ã¢â‚¬Â¦,"OCR":Ã¢â‚¬Â¦,"WHATSAPP":Ã¢â‚¬Â¦}}`, mas o adapter lia `parsed.OCR` no nÃƒÂ­vel raiz Ã¢â‚¬â€ logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃƒÂªncia isolada: a execuÃƒÂ§ÃƒÂ£o `run_a22dfÃ¢â‚¬Â¦` rodou com a versÃƒÂ£o AppConfig 2 (`OCR:true` dentro do envelope canÃƒÂ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefÃ¢â‚¬Â¦`, cuja ÃƒÂºnica diferenÃƒÂ§a era a cÃƒÂ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃƒÂ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃƒÂ­vel raiz) + teste de regressÃƒÂ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃƒÂ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados ÃƒÂ  lista de imports estÃƒÂ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃƒÂ³pria doc-string manda "add both the file AND a static import line". Toda chamada ÃƒÂ s duas rotas retornava 500 antes de qualquer lÃƒÂ³gica de negÃƒÂ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` Ã¢â‚¬â€ o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃƒÂ³ria dos testes unitÃƒÂ¡rios ignoram atributos extras na chave, por isso a suÃƒÂ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃƒÂ³ depois dessa correÃƒÂ§ÃƒÂ£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` Ã¢â‚¬â€ GAP REAL, NÃƒÆ’O CORRIGIDO (decisÃƒÂ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃƒÂ§a Ã¢â‚¬â€ foi exatamente o que aconteceu com `run_c1aefÃ¢â‚¬Â¦`, o caso de sucesso do pipeline. Mas o **ÃƒÂºnico** cÃƒÂ³digo que escreve `ExpirationItem.dueDate` ÃƒÂ© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃƒÂ¡ `CONFIRMED` (verificado, ver acima). ConsequÃƒÂªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** ÃƒÂ© atualizado e nÃƒÂ£o existe rota que o atualize. NÃƒÂ£o corrigido de propÃƒÂ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃƒÂ§ÃƒÂ£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" ÃƒÂ© decisÃƒÂ£o de produto/design (Type 1, Ã‚Â§4 do `AGENTS.md`), nÃƒÂ£o conserto mecÃƒÂ¢nico.

### Achado menor (nÃƒÂ£o corrigido, sem impacto observado nesta verificaÃƒÂ§ÃƒÂ£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, Ã¢â‚¬Â¦), mas o Step Functions casa contra o `errorType` do Lambda, que ÃƒÂ© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, Ã¢â‚¬Â¦). Confirmado no histÃƒÂ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃƒÂ£o** casou Ã¢â‚¬â€ quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃƒÂ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃƒÂ­ **hÃƒÂ¡** perda de comportamento (uma falha transitÃƒÂ³ria de commit nÃƒÂ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD Ã¢â‚¬â€ deixado para uma sessÃƒÂ£o de infra.

### Estado do ambiente apÃƒÂ³s a sessÃƒÂ£o

Flags AppConfig **revertidas**: versÃƒÂ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃƒÂµes temporÃƒÂ¡rias 2/3 deletadas Ã¢â‚¬â€ `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃƒÂ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃƒÂ³ o ruÃƒÂ­do jÃƒÂ¡ conhecido de republicaÃƒÂ§ÃƒÂ£o de versÃƒÂ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃƒÂµes deletadas), `ocr/` no bucket transitÃƒÂ³rio volta 0. Os histÃƒÂ³ricos das 3 execuÃƒÂ§ÃƒÂµes do Step Functions ficam (artefato normal da AWS, nÃƒÂ£o ÃƒÂ© dado de teste a limpar).

**As correÃƒÂ§ÃƒÂµes dos bugs 1 e 2 estÃƒÂ£o apenas em `develop`, ainda NÃƒÆ’O implantadas em `dev`** (o CD sÃƒÂ³ aplica em push para `main`). A verificaÃƒÂ§ÃƒÂ£o do `confirm` real rodou contra o bundle esbuild jÃƒÂ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃƒÂ© o prÃƒÂ³ximo merge para `main`.

**Bloqueado sÃƒÂ³ para produÃƒÂ§ÃƒÂ£o real** (nÃƒÂ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃƒÂ§ÃƒÂ£o de modelo Bedrock + regiÃƒÂ£o, RIPD formal (`privacy-lgpd.md` Ã‚Â§6, gatilho jÃƒÂ¡ registrado).

## DecisÃƒÂµes pendentes do Marcelo

1. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR)**: design jÃƒÂ¡ aprovado (D-035), zero decisÃƒÂ£o de produto pendente Ã¢â‚¬â€ sÃƒÂ³ falta decidir *quando* comeÃƒÂ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) Ã¢â‚¬â€ nÃƒÂ£o retomar sem sinal explÃƒÂ­cito dele.
3. **Estado real de `git`/deploy no inÃƒÂ­cio da prÃƒÂ³xima sessÃƒÂ£o nÃƒÂ£o deve ser presumido** Ã¢â‚¬â€ confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃƒÂªncias residuais nÃƒÂ£o bloqueantes (registradas, nÃƒÂ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃƒÂ©-existente nÃƒÂ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ Ã¢â‚¬â€ **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃƒÂºmeros (9 vulnerabilidades reais, nÃƒÂ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃƒÂ£o documentada (`testcontainers`Ã¢â€ â€™`dockerode`Ã¢â€ â€™`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃƒÂ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃƒÂ£o reverificado nesta sessÃƒÂ£o).
- Observabilidade por funÃƒÂ§ÃƒÂ£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃƒÂ©rica jÃƒÂ¡ cobre "estÃƒÂ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃƒÂ§ÃƒÂ£o de upload slot expirado) nunca exercitada contra AWS real~~ Ã¢â‚¬â€ **executada em 2026-08-25, achou um bug real de produÃƒÂ§ÃƒÂ£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado Ã¢â‚¬â€ confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃƒÂ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃƒÂ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃƒÂ³s a transiÃƒÂ§ÃƒÂ£o (ponteiro ÃƒÂ³rfÃƒÂ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃƒÂ§ÃƒÂ£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃƒÂ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃƒÂ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** Ã¢â‚¬â€ commit `2fab80f`, jÃƒÂ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃƒÂ£o deployado" estava desatualizada (verificado nesta sessÃƒÂ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃƒÂ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso Ã¢â‚¬â€ sÃƒÂ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` ÃƒÂ© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃƒÂ³ ÃƒÂ© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃƒÂ§ÃƒÂ£o defensiva do status real da occurrence antes de agir Ã¢â‚¬â€ um ponteiro ÃƒÂ³rfÃƒÂ£o nunca ÃƒÂ© revisitado fora dessa janela, e mesmo se fosse, ÃƒÂ© ignorado com seguranÃƒÂ§a. Diferente do bug do `upload-slot` (falha total Ã¢â‚¬â€ nada funcionava), este ÃƒÂ© um descompasso comentÃƒÂ¡rio-vs-cÃƒÂ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃƒÂ¡ revisado 9,2/10 pelo protocolo ClaudeÃ¢â€ â€Codex e ativo em produÃƒÂ§ÃƒÂ£o real Ã¢â‚¬â€ nÃƒÂ£o corrigido nesta sessÃƒÂ£o deliberadamente, para nÃƒÂ£o mexer num trecho crÃƒÂ­tico jÃƒÂ¡ extensivamente revisado sem uma rodada de revisÃƒÂ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃƒÂ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃƒÂ§ÃƒÂ£o real, Ã‚Â§13-30) que, se adotada como padrÃƒÂ£o oficial, deveria passar pela mesma convergÃƒÂªncia independente ClaudeÃ¢â€ â€Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃƒÂ¡ usaram Ã¢â‚¬â€ nÃƒÂ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃƒÂ£o de 12 eixos especÃƒÂ­fico do planejamento de interface, jÃƒÂ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃƒÂªncias (nÃƒÂ£o reler por padrÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ sob demanda)

- `docs/architecture/session-log.md` Ã¢â‚¬â€ linha do tempo compacta, uma entrada por sessÃƒÂ£o.
- `docs/architecture/decisions-log.md` Ã¢â‚¬â€ todas as decisÃƒÂµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` Ã¢â‚¬â€ os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` Ã¢â‚¬â€ o protÃƒÂ³tipo interativo executÃƒÂ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` Ã¢â‚¬â€ debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o real do Full BFF + fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o, achados do protocolo ClaudeÃ¢â€ â€Codex sobre o cÃƒÂ³digo, Final Status.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/README.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 179ms:
# docs/architecture/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃƒÂ§ÃƒÂ£o real (cÃƒÂ³digo/infra/testes) Ã¢â‚¬â€ Implementation Blueprint concluÃƒÂ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃƒÂ§ÃƒÂ£o LGPD sem purga fÃƒÂ­sica real e DSR 100% design-only, ambos registrados como decisÃƒÂ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃƒÂ­ntese final com recomendaÃƒÂ§ÃƒÂ£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` Ã¢â‚¬â€ ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) code-complete e deployado em `dev`** Ã¢â‚¬â€ todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃƒÂ­stico/Bedrock/validaÃƒÂ§ÃƒÂ£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃƒÂ§ÃƒÂ£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3Ã¢â€ â€™SQSÃ¢â€ â€™Step FunctionsÃ¢â€ â€™TextractÃ¢â€ â€™parserÃ¢â€ â€™persistÃƒÂªncia, ver `NEXT_SESSION_PROMPT.md`) Ã¢â‚¬â€ achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃƒÂ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃƒÂ£o corrigido e aguardando decisÃƒÂ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` Ã¢â‚¬â€ nenhuma rota escreve isso hoje); M12 bloqueado por decisÃƒÂ£o de produto (D-052); Full BFF Ã¢â‚¬â€ D-053/D-054 Ã¢â‚¬â€ **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo ClaudeÃ¢â€ â€Codex completo em `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o atÃƒÂ© convergir); planejamento de interface concluÃƒÂ­do Ã¢â‚¬â€ 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃƒÂ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃƒÂ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃƒÂ£o **todos resolvidos** (backend + frontend onde aplicÃƒÂ¡vel Ã¢â‚¬â€ BLOCKER-C via Variante B, revisÃƒÂ£o humana explÃƒÂ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada Ã¢â‚¬â€ ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta, este bloco pode ficar temporariamente atrÃƒÂ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

1. `AGENTS.md` (raiz) Ã¢â‚¬â€ processo de trabalho dos agentes, sempre vence sobre conteÃƒÂºdo de arquitetura.
2. ADR aceito em `adr/` Ã¢â‚¬â€ decisÃƒÂ£o arquitetural especÃƒÂ­fica e formal.
3. Documento temÃƒÂ¡tico corrente (tabela abaixo, coluna "normativo atual") Ã¢â‚¬â€ especificaÃƒÂ§ÃƒÂ£o detalhada do domÃƒÂ­nio.
4. `ARCHITECTURE.md` Ã¢â‚¬â€ visÃƒÂ£o consolidada e ÃƒÂ­ndice executivo; nÃƒÂ£o sobrescreve silenciosamente um ADR ou documento temÃƒÂ¡tico divergente Ã¢â‚¬â€ divergÃƒÂªncia entre eles ÃƒÂ© defeito a corrigir, nÃƒÂ£o licenÃƒÂ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` Ã¢â‚¬â€ estado de execuÃƒÂ§ÃƒÂ£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` Ã¢â‚¬â€ evidÃƒÂªncia histÃƒÂ³rica de como se chegou a uma decisÃƒÂ£o, nunca normativo.

## ÃƒÂndice por documento

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/ÃƒÂ­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃƒÂ³rico/processo (ciclo concluÃƒÂ­do) | Processo ClaudeÃ¢â€ â€Codex que produziu o design; nÃƒÂ£o ÃƒÂ© ponto de entrada de sessÃƒÂ£o |
| `quality-criteria.md` | normativo atual | 12 critÃƒÂ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃƒÂ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃƒÂ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃƒÂµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃƒÂ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃƒÂ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃƒÂ§ÃƒÂ£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃƒÂ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃƒÂ£o de domÃƒÂ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃƒÂ§ÃƒÂµes de estÃƒÂ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃƒÂ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃƒÂ§ÃƒÂ£o 33 Ã¢â‚¬â€ APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃƒÂ§ÃƒÂ£o 60 Ã¢â‚¬â€ componentes, interfaces, eventos/schemas, ordem de deploy, milestones Ã¢â‚¬â€ APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) Ã¢â‚¬â€ adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas Ã¢â‚¬â€ APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B Ã¢â‚¬â€ pipeline real de materializaÃƒÂ§ÃƒÂ£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃƒÂªncia (dispatch + reconciliaÃƒÂ§ÃƒÂ£o), backfill Ã¢â‚¬â€ APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃƒÂ§ÃƒÂ£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃƒÂ³rico/evidÃƒÂªncia (citado por `reminder-delivery-pipeline.md` Ã‚Â§3/Ã‚Â§3.2) | Recon prÃƒÂ©-implementaÃƒÂ§ÃƒÂ£o de BLOCKER-B (materializer/trigger de materializaÃƒÂ§ÃƒÂ£o, infra Terraform) confirmado contra o cÃƒÂ³digo real antes do design ser escrito; BLOCKER-B estÃƒÂ¡ implementado e mergeado (PR #50) Ã¢â‚¬â€ este documento sÃƒÂ³ registra a evidÃƒÂªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃƒÂ³rico/evidÃƒÂªncia | Texto verbatim do prompt de missÃƒÂ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃƒÂ³ existia em histÃƒÂ³rico de conversa de uma sessÃƒÂ£o especÃƒÂ­fica |
| `decisions-log.md` | decisÃƒÂ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃƒÂ§ÃƒÂ£o nÃƒÂ£o ÃƒÂ© sequencial na ordem das linhas Ã¢â‚¬â€ D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada | Artefatos do protocolo ClaudeÃ¢â€ â€Codex de M7 (proposta Claude, proposta Codex, crÃƒÂ­tica, reconciliaÃƒÂ§ÃƒÂ£o final aprovada Ã¢â‚¬â€ D-035) |
| `adr/` | decisÃƒÂ£o/ADR | 11 ADRs formais para decisÃƒÂµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada (protocolo `AGENTS.md` Ã‚Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃƒÂªncia CloudFront + Full BFF) Ã¢â‚¬â€ nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/ÃƒÂ­ndice (visual) | **O documento de status do projeto** Ã¢â‚¬â€ painel visual (timeline de marcos, achados reais, pendÃƒÂªncias); abrir no navegador para uma visÃƒÂ£o executiva rÃƒÂ¡pida, mais legÃƒÂ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃƒÂ£o) |
| `session-log.md` | histÃƒÂ³rico | Log cronolÃƒÂ³gico compacto por sessÃƒÂ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃƒÂ£o normativo) | Fase 1 da evoluÃƒÂ§ÃƒÂ£o estratÃƒÂ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃƒÂ§ÃƒÂ£o de cada capacidade comercial proposta contra o cÃƒÂ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃƒÂ­nio + protocolo ClaudeÃ¢â€ â€Codex por tema), nunca decisÃƒÂ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃƒÂ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 Ã¢â‚¬â€ achado central: billing por sujeito rastreado ÃƒÂ© padrÃƒÂ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃƒÂ­nio (`TrackedSubject`+`RequirementAssignment`) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃƒÂ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃƒÂ§ÃƒÂ£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃƒÂ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃƒÂ§ÃƒÂ£o de mini-revisÃƒÂ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃƒÂ£o direta de padrÃƒÂ£o jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o (mesma partiÃƒÂ§ÃƒÂ£o de `Document`/M6); digest registrado como questÃƒÂ£o aberta, nÃƒÂ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, lista de rejeitados formal sÃƒÂ³ na Fase 3) | Fase 2b, sexto cluster Ã¢â‚¬â€ custom fields genÃƒÂ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃƒÂ£o (nota 9,1/9,0), valor jÃƒÂ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, sÃƒÂ©timo e ÃƒÂºltimo cluster (CSV import/export) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o na entrada); plano linha-a-linha em S3, nÃƒÂ£o DynamoDB; **Fase 2b concluÃƒÂ­da, 7/7 clusters Ã¢â€°Â¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃƒÂ­ntese proposta, nÃƒÂ£o autorizaÃƒÂ§ÃƒÂ£o de implementaÃƒÂ§ÃƒÂ£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph Ã¢â‚¬â€ consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃƒÂ­ntese proposta, ADRs formais sÃƒÂ³ com decisÃƒÂ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃƒÂ§a/persistÃƒÂªncia/custo, lista de 10 ADRs candidatos, estratÃƒÂ©gia de teste/migraÃƒÂ§ÃƒÂ£o, perguntas abertas reais, capacidades rejeitadas Ã¢â‚¬â€ inclui revisÃƒÂ£o adversarial final de coerÃƒÂªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃƒÂ§ÃƒÂ£o de prÃƒÂ©-requisito (D-046) | Mini-revisÃƒÂ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) Ã¢â‚¬â€ pico orgÃƒÂ¢nico ~220Ãƒâ€” abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/ÃƒÂ­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-047/D-048) | Fecha D-047 Ã¢â‚¬â€ entrega/reenvio do link de guest upload: rotaÃƒÂ§ÃƒÂ£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-049) | Fecha "DecisÃƒÂ£o B" de D-048 Ã¢â‚¬â€ automatiza o convite inicial de guest upload (hoje manual) atrÃƒÂ¡s de preferÃƒÂªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-052) | PÃƒÂ³s-M11: M12 (Billing) fica bloqueado por decisÃƒÂ£o de produto (fornecedor de pagamento) Ã¢â‚¬â€ zero cÃƒÂ³digo novo; achado real de que o projeto nÃƒÂ£o tem conceito de "platform staff" cross-tenant, descartando atÃƒÂ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃƒÂ£o do browser Ã¢â‚¬â€ decide o mecanismo de autenticaÃƒÂ§ÃƒÂ£o das chamadas de recurso que o "BFF de sessÃƒÂ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃƒÂ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃƒÂ§ÃƒÂ£o nativa do Cognito no refresh (nÃƒÂ£o contador local Ã¢â‚¬â€ evita falso-positivo de invalidaÃƒÂ§ÃƒÂ£o), tabela de sessÃƒÂ£o dedicada IAM-isolada, cookies login/sessÃƒÂ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) Ã¢â‚¬â€ ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃƒÂ§ÃƒÂ£o e do protocolo ClaudeÃ¢â€ â€Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o corrigidos) |
| `reviews/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada (protocolo `AGENTS.md` Ã‚Â§4, pÃƒÂ³s-M0) | Artefatos de revisÃƒÂ£o ClaudeÃ¢â€ â€Codex de implementaÃƒÂ§ÃƒÂ£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) Ã¢â‚¬â€ mesmo papel de `history/` (evidÃƒÂªncia, nunca normativo), mas para rodadas ocorridas depois que cÃƒÂ³digo passou a existir, em vez das rodadas de design conceitual prÃƒÂ©-implementaÃƒÂ§ÃƒÂ£o |
| `history/` | histÃƒÂ³rico/supersedido | Artefatos de rodada (propostas, crÃƒÂ­ticas, red team) que produziram os documentos normativos acima Ã¢â‚¬â€ ver subseÃƒÂ§ÃƒÂ£o |

## `history/` Ã¢â‚¬â€ evidÃƒÂªncia de rodada, por tema

Cada subpasta contÃƒÂ©m os artefatos de proposta/crÃƒÂ­tica/trÃƒÂ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃƒÂ£o vigente Ã¢â‚¬â€ sÃƒÂ³ como prova de como o consenso foi alcanÃƒÂ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/engineering/README.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 174ms:
# docs/engineering/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃƒÂDO Ã¢â‚¬â€ 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃƒÂ³ Contexto bateu o gate Ã¢â€°Â¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃƒÂ³rio trata de **como o trabalho de engenharia ÃƒÂ© medido e revisado** (rubrica, critÃƒÂ©rios por eixo, protocolo de debate, achados de auditoria) Ã¢â‚¬â€ nÃƒÂ£o confundir com `docs/architecture/`, que trata do que o sistema Ãƒâ€° (design, modelo de dados, decisÃƒÂµes de arquitetura). Se a dÃƒÂºvida for "o GSI3 ÃƒÂ© consultÃƒÂ¡vel por quem", vÃƒÂ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃƒÂ³rio ÃƒÂ© o certo.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

Mesma regra de `docs/architecture/README.md` Ã¢â‚¬â€ `AGENTS.md` (raiz) sempre vence sobre conteÃƒÂºdo deste diretÃƒÂ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) ÃƒÂ© evidÃƒÂªncia de uma rodada especÃƒÂ­fica, nunca redefine os pesos/critÃƒÂ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃƒÂ¡ em vez de duplicar, ver seu prÃƒÂ³prio rodapÃƒÂ© "Como adicionar um novo eixo").

## Quando carregar o quÃƒÂª (roteamento por tipo de tarefa)

| Sua tarefa ÃƒÂ©... | Carregue |
|---|---|
| Rodar o protocolo ClaudeÃ¢â€ â€Codex num eixo especÃƒÂ­fico (`AGENTS.md` Ã‚Â§4) | `joint-review-criteria.md` (seÃƒÂ§ÃƒÂ£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃƒÂ­fico do cÃƒÂ³digo tira / que critÃƒÂ©rio ele afeta | `joint-review-criteria.md` (nÃƒÂ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional ÃƒÂ© vÃƒÂ¡lido/de qualidade suficiente (gate binÃƒÂ¡rio, critÃƒÂ©rio ponderado, nota mÃƒÂ­nima) | `test-engineering-standard.md` Ã¢â‚¬â€ rÃƒÂ©gua concreta que `joint-review-criteria.md`'s critÃƒÂ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃƒÂ§a precisa de ADR/protocolo formal ou ÃƒÂ© correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica | `change-risk-scale.md` (a rÃƒÂ©gua concreta) Ã¢â‚¬â€ `AGENTS.md` Ã‚Â§4 sÃƒÂ³ distingue os dois extremos de forma binÃƒÂ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` Ã¢â‚¬â€ mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃƒÂ§ÃƒÂ£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃƒÂ£o | `exceptions.md` |
| Ver o histÃƒÂ³rico completo de decisÃƒÂµes de engenharia (nÃƒÂ£o de arquitetura) com motivo | `decisions-log.md` Ã¢â‚¬â€ E-000 em diante, mesmo padrÃƒÂ£o de `docs/architecture/decisions-log.md` mas para decisÃƒÂµes de processo/qualidade, nÃƒÂ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃƒÂ©rios/gates G1-G6 (histÃƒÂ³rico, prÃƒÂ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA Ã¢â‚¬â€ mudanÃƒÂ§a exige nova rodada formal, nÃƒÂ£o ediÃƒÂ§ÃƒÂ£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃƒÂ³rio no inÃƒÂ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` Ã¢â‚¬â€ histÃƒÂ³rico, nÃƒÂ£o estado atual (ver aviso no prÃƒÂ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃƒÂ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃƒÂ§ÃƒÂ£o de nomenclatura abaixo) |
| Consultar evidÃƒÂªncia bruta de uma rodada especÃƒÂ­fica (nota cega, prompt do Codex, saÃƒÂ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` Ã¢â‚¬â€ evidÃƒÂªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃƒÂ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) Ã¢â‚¬â€ link relativo quebrado + referÃƒÂªncia `AGENTS.md Ã‚Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## ÃƒÂndice por documento (nÃƒÂ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃƒÂ­pios de engenharia adotados (proporcionalidade, evidÃƒÂªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃƒÂ§a NÃƒÂ­vel 1-6, rÃƒÂ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃƒÂ©rios/pesos por eixo das revisÃƒÂµes conjuntas ClaudeÃ¢â€ â€Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo ClaudeÃ¢â€ â€Codex 8 rodadas, gate elevado 9,5/10) | PadrÃƒÂ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados, fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃƒÂ§ÃƒÂµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃƒÂ£o (log vivo) | DecisÃƒÂµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃƒÂ³rico/registro vivo | DivergÃƒÂªncias materiais ClaudeÃ¢â€ â€Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Ã‚Â§4 pode ser dispensado, inventÃƒÂ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃƒÂ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃƒÂ³rico (CONGELADA) | Rubrica original de 12 critÃƒÂ©rios/gates G1-G6 da Engineering Maturity Review Ã¢â‚¬â€ antecessora conceitual de `joint-review-criteria.md`, nÃƒÂ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃƒÂ§ÃƒÂµes executÃƒÂ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃƒÂ³rico/fundamentaÃƒÂ§ÃƒÂ£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃƒÂ³rico | Estado do repositÃƒÂ³rio no inÃƒÂ­cio da Engineering Maturity Review Ã¢â‚¬â€ nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃƒÂ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) Ã¢â‚¬â€ DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃƒÂ­ntese/recomendaÃƒÂ§ÃƒÂ£o (entregÃƒÂ¡vel final do programa, prompt mestre Ã‚Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃƒÂªncia de `pilot-readiness-program.md` Ã¢â‚¬â€ nÃƒÂ£o repete achado, sÃƒÂ³ aponta; nÃƒÂ£o ÃƒÂ© aprovaÃƒÂ§ÃƒÂ£o final, ÃƒÂ© insumo para a decisÃƒÂ£o do Marcelo |
| `reviews/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada | Artefatos de toda rodada ClaudeÃ¢â€ â€Codex de processo/qualidade Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o prÃƒÂ³pria abaixo |

## `reviews/` Ã¢â‚¬â€ convenÃƒÂ§ÃƒÂ£o de nomenclatura e classificaÃƒÂ§ÃƒÂ£o

EvidÃƒÂªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃƒÂµes de arquitetura: nunca normativo, sÃƒÂ³ prova de como uma nota/decisÃƒÂ£o foi alcanÃƒÂ§ada. TrÃƒÂªs geraÃƒÂ§ÃƒÂµes de conteÃƒÂºdo coexistem aqui, nesta ordem cronolÃƒÂ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃƒÂ­vel raiz de `reviews/`) Ã¢â‚¬â€ produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos ÃƒÂ© intencional (agrupa antes de outros nomes em ordem alfabÃƒÂ©tica, sinaliza "transcript bruto, nÃƒÂ£o leia direto").
2. **ConvergÃƒÂªncia dos critÃƒÂ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) Ã¢â‚¬â€ produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) Ã¢â‚¬â€ a rodada em andamento nesta sessÃƒÂ£o. PadrÃƒÂ£o de nomenclatura por eixo (`<eixo>` Ã¢Ë†Ë† `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... Ã¢â‚¬â€ nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... Ã¢â‚¬â€ prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... Ã¢â‚¬â€ saÃƒÂ­da bruta do Codex CLI. Alguns trechos que ecoam saÃƒÂ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃƒÂ§ÃƒÂµes via PowerShell) Ã¢â‚¬â€ revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃƒÂ£o sÃƒÂ³ mitigado); a saÃƒÂ­da de uma rodada em andamento pode ainda conter mojibake atÃƒÂ© a rodada seguinte revisÃƒÂ¡-la, por ser evidÃƒÂªncia datada do que o Codex realmente viu naquele momento Ã¢â‚¬â€ nunca editada retroativamente. Se precisar do conteÃƒÂºdo, prefira o `-summary.md` correspondente, que jÃƒÂ¡ extrai a informaÃƒÂ§ÃƒÂ£o sem o ruÃƒÂ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` Ã¢â‚¬â€ **o ÃƒÂºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃƒÂ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃƒÂ§ÃƒÂ£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃƒÂ£o fonte de verdade por si sÃƒÂ³ Ã¢â‚¬â€ sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃƒÂ­tica de proveniÃƒÂªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃƒÂ£o Ã¢â‚¬â€ exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃƒÂ¡ que sua proveniÃƒÂªncia ÃƒÂ© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃƒÂ§ÃƒÂ£o cronolÃƒÂ³gica descrita nesta seÃƒÂ§ÃƒÂ£o. Isso ÃƒÂ© uma decisÃƒÂ£o de design, nÃƒÂ£o uma lacuna: um transcript nunca ÃƒÂ© lido isoladamente por convenÃƒÂ§ÃƒÂ£o (ver acima), entÃƒÂ£o nÃƒÂ£o precisa carregar sua prÃƒÂ³pria metadata.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 176ms:
# W3-07 Ã¢â‚¬â€ Status final: REPROVADO, NÃƒÆ’O IMPLEMENTADO (2026-08-28)

> D-062 em `docs/architecture/decisions-log.md`. Este documento resume o resultado das 4
> rodadas para quem retomar este trabalho no futuro Ã¢â‚¬â€ nÃƒÂ£o reabrir o protocolo do zero sem ler
> isto primeiro.

## Resultado

4 rodadas ClaudeÃ¢â€ â€Codex, notas **3,4 Ã¢â€ â€™ 5,1 Ã¢â€ â€™ 4,7 / 10** Ã¢â‚¬â€ nunca atingiu o gate de 9,0. Reprovado
por decisÃƒÂ£o do Marcelo, seguindo recomendaÃƒÂ§ÃƒÂ£o convergente e independente de Claude e Codex
(pedida explicitamente apÃƒÂ³s a Rodada 3): **nÃƒÂ£o implementar agora**, registrar os achados como
pesquisa.

## Por que nÃƒÂ£o convergiu (o achado central)

Toda tentativa de fechar "COMPLETED" com garantia real de nÃƒÂ£o-ressurreiÃƒÂ§ÃƒÂ£o (um dado do tenant
sendo recriado depois de uma exclusÃƒÂ£o jÃƒÂ¡ declarada concluÃƒÂ­da) esbarrou na mesma parede: o
sistema tem **trÃƒÂªs classes de escritor** que uma prÃƒÂ©-condiÃƒÂ§ÃƒÂ£o simples (ex.: desativar login no
Cognito) nÃƒÂ£o fecha:

1. HTTP autenticado (fechÃƒÂ¡vel via Cognito).
2. **SuperfÃƒÂ­cie pÃƒÂºblica de guest upload** (`GuestSubmissionService` Ã¢â‚¬â€ nunca passa por
   `RequestContext`/Cognito, autentica sÃƒÂ³ por token opaco).
3. **Workers assÃƒÂ­ncronos** jÃƒÂ¡ enfileirados ou agendados independentemente de qualquer sessÃƒÂ£o de
   usuÃƒÂ¡rio (`reminder-materialization-trigger`, `reminder-reconciliation`,
   `document-chasing-dispatch`, outbox relay/sweeper, callback SES, reconciliaÃƒÂ§ÃƒÂ£o de
   upload/malware, import, extraÃƒÂ§ÃƒÂ£o/OCR via Step Functions).

Fechar as trÃƒÂªs de verdade exige um fence de `TenantStatus` consultado por **todo** esses
caminhos Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© mais "o mecanismo de descoberta+exclusÃƒÂ£o", ÃƒÂ© a mesma feature de "bloqueio
imediato" que a decisÃƒÂ£o de escopo original desta sessÃƒÂ£o jÃƒÂ¡ tinha deixado de fora
deliberadamente. As duas decisÃƒÂµes de escopo (mecanismo pequeno vs. sem bloqueio imediato) sÃƒÂ£o
mutuamente incompatÃƒÂ­veis com uma garantia forte Ã¢â‚¬â€ nÃƒÂ£o dÃƒÂ¡ para ter as duas.

## Por que nÃƒÂ£o foi aceito um "best-effort documentado" como meio-termo

RecomendaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita do Codex, aceita: implementar um mecanismo que alega apagar
fisicamente mas nÃƒÂ£o sustenta essa garantia ÃƒÂ© uma capacidade **pior que nÃƒÂ£o ter nenhuma** Ã¢â‚¬â€ pode
ser lido operacionalmente como "DSR atendido" mesmo com uma ressalva no `decisions-log.md`, e
ainda geraria cÃƒÂ³digo que precisaria ser redesenhado quando a garantia real for exigida de
verdade.

## O que sobrevive desta pesquisa (reusar, nÃƒÂ£o redescobrir)

- **InventÃƒÂ¡rio verificado dos 40 `entityType` reais** do sistema (`src/modules/**`), com
  classificaÃƒÂ§ÃƒÂ£o por presenÃƒÂ§a de `version`/`tenantId` Ã¢â‚¬â€ ver Rodada 3/4
  (`claude-proposal-round3.md`/`round4.md`) para a tabela completa e as correÃƒÂ§ÃƒÂµes feitas em
  cima da prÃƒÂ³pria Rodada 2 (que tambÃƒÂ©m errou o inventÃƒÂ¡rio na primeira tentativa Ã¢â‚¬â€ dois erros
  reais de levantamento em rodadas diferentes, liÃƒÂ§ÃƒÂ£o: sempre grep exaustivo, nunca amostra).
- **Bug real descoberto, nÃƒÂ£o corrigido ainda**: `tenantId` (MVP `tenantId=userId`) nÃƒÂ£o ÃƒÂ© o
  Cognito `sub`/`Username` Ã¢â‚¬â€ `IdentityMapping.cognitoSub`/`User.identitySubject` sÃƒÂ£o os campos
  certos. Qualquer aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant precisa resolver isso
  primeiro.
- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
  `bff-session-table`, uma tabela DynamoDB fÃƒÂ­sica separada Ã¢â‚¬â€ nenhum mecanismo de descoberta
  tenant-wide pode assumir uma ÃƒÂºnica tabela.
- **CorreÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica pendente, independente**: `PolicyRef` (`reminder-policy.ts:69-72`) nÃƒÂ£o
  carrega `tenantId` Ã¢â‚¬â€ pequena, real, trivial de corrigir numa sessÃƒÂ£o futura sem depender de
  W3-07.
- **Reuso jÃƒÂ¡ decidido para quando retomar**: `Document` continua a ÃƒÂºnica categoria com efeito
  colateral S3 real Ã¢â‚¬â€ reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061), nunca duplicar.

## Gatilho para retomar

Requisito real de DSR (idealmente decidido antes do primeiro cliente B2B que dependa disso).
Quando retomado, o fence de `TenantStatus` em todo write path (HTTP + guest + workers) deve
entrar no escopo do **primeiro** desenho, nÃƒÂ£o ser descoberto de novo rodada a rodada como
aconteceu aqui.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 171ms:
# W3-07 (retomada, D-063) Ã¢â‚¬â€ status: PAUSADO apÃƒÂ³s Rodada 1, para planejamento minucioso na prÃƒÂ³xima sessÃƒÂ£o

> DecisÃƒÂ£o do Marcelo (2026-08-28): nÃƒÂ£o adiar por falta de usuÃƒÂ¡rio real hoje Ã¢â‚¬â€ construir a
> garantia completa de exclusÃƒÂ£o de tenant algum dia, mas com anÃƒÂ¡lise minuciosa antes de
> implementar. Esta pasta ÃƒÂ© a continuaÃƒÂ§ÃƒÂ£o de
> `docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/` (D-062, reprovada em 4
> rodadas) Ã¢â‚¬â€ leia aquela primeiro (especialmente `claude-final-status-not-approved.md`) antes
> desta.

## O que esta rodada tentou

Reabrir o desenho com o fence de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" dentro do escopo desde a Rodada 1 (em vez
de descoberto reativamente, como na tentativa D-062). Achado de partida:
`User.status: "ACTIVE" | "SUSPENDED"` (`user-repository.ts:22`) jÃƒÂ¡ existe e jÃƒÂ¡ ÃƒÂ© checado em toda
resoluÃƒÂ§ÃƒÂ£o de contexto HTTP autenticada (`resolve-request-context.ts:67-69`), mas `"SUSPENDED"`
nunca ÃƒÂ© escrito por nenhum cÃƒÂ³digo hoje. A proposta usava isso como fence primÃƒÂ¡rio (zero cÃƒÂ³digo
novo para a superfÃƒÂ­cie HTTP autenticada) + checagem nova no fluxo de convidado + auditoria dos
workers em `src/workers/`.

## Resultado: Rodada 1, nota 3,2/10, REPROVADA Ã¢â‚¬â€ pior que a rodada equivalente da tentativa anterior

Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
**precisa** resolver desde o desenho inicial, nÃƒÂ£o descobrir depois:

### Achado mais grave Ã¢â‚¬â€ o prÃƒÂ³prio fence ÃƒÂ© apagÃƒÂ¡vel pela cascata que ele deveria proteger

`RequestContextResolver.resolve()` (`resolve-request-context.ts:49-65`) provisiona
automaticamente um `User` novo com `status: "ACTIVE"` no primeiro login de um `cognitoSub`
sem perfil existente. Se a cascata apaga o `User` (ele ÃƒÂ© sÃƒÂ³ mais uma linha do tenant, como
qualquer outra), um token ainda vÃƒÂ¡lido reautentica e o sistema **recria o tenant sozinho** Ã¢â‚¬â€
a prÃƒÂ³pria lÃƒÂ³gica de "provisionar no primeiro login" resssucita o que acabou de ser apagado.
**ConsequÃƒÂªncia de design**: o tombstone/fence do tenant nÃƒÂ£o pode ser a mesma linha que a
cascata elimina Ã¢â‚¬â€ precisa ser um registro que sobrevive ÃƒÂ  exclusÃƒÂ£o completa (ex.: um registro
de "tenant encerrado" fora do universo apagÃƒÂ¡vel, no mesmo espÃƒÂ­rito do `TenantDeletionRequest`
jÃƒÂ¡ proposto em D-062, mas agora como fonte de verdade que o RESOLVER tambÃƒÂ©m consulta, nÃƒÂ£o sÃƒÂ³
um tombstone passivo).

### Checagens desacopladas (leitura solta antes de agir) tÃƒÂªm corrida TOCTOU real

Qualquer "leia `User.status`, depois aja" (proposto para `GuestSubmissionService` e para os
workers) tem uma janela real entre a leitura e o efeito. Para efeitos DynamoDB, o fence precisa
participar da MESMA `TransactWriteItems` da mutaÃƒÂ§ÃƒÂ£o (`ConditionCheck`), nÃƒÂ£o uma leitura solta
antes. Para efeitos externos (SES, S3, SQS, Step Functions) nÃƒÂ£o existe uma condiÃƒÂ§ÃƒÂ£o atÃƒÂ´mica
equivalente Ã¢â‚¬â€ precisa de um protocolo de claim/drenagem dedicado, nÃƒÂ£o uma checagem simples.

### Auditoria de superfÃƒÂ­cies de escrita estava errada e incompleta

Confirmado incorreto (releem entidade de origem, mas ainda produzem efeito novo sem fence real):
- `dispatch-outbox-relay` Ã¢â‚¬â€ produz mensagem SQS nova antes de marcar o outbox como publicado.
- `upload-slot-reconciliation` Ã¢â‚¬â€ escreve `Document.status = TIMEOUT` sem reler fence de tenant.
- `document-chasing-dispatch` Ã¢â‚¬â€ pode criar `GuestTokenPointer` novo e mandar e-mail via SES
  depois de o tenant jÃƒÂ¡ estar suspenso.

SuperfÃƒÂ­cies inteiras que a auditoria por pasta (`src/workers/`) nÃƒÂ£o capturou:
- `import-commit-handler` Ã¢â‚¬â€ fabrica um `RequestContext` de sistema com papel `OWNER`, nunca
  resolve usuÃƒÂ¡rio nem checa status.
- `start-extraction-run`/Step Functions do mÃƒÂ³dulo `extraction` Ã¢â‚¬â€ inicia execuÃƒÂ§ÃƒÂ£o sem fence,
  tarefas subsequentes continuam escrevendo artefato S3/estado depois da suspensÃƒÂ£o.
- `ses-callback-workflow` Ã¢â‚¬â€ cria/atualiza `WebhookInbox`/`NotificationAttempt`/
  `NotificationPreferences` sem checar fence.
- URLs presignadas de S3 jÃƒÂ¡ entregues a um convidado continuam vÃƒÂ¡lidas mesmo apÃƒÂ³s o fence Ã¢â‚¬â€
  um upload feito depois ainda dispara toda a cadeia de finalizaÃƒÂ§ÃƒÂ£o/malware/extraÃƒÂ§ÃƒÂ£o.

**LiÃƒÂ§ÃƒÂ£o estrutural para a prÃƒÂ³xima tentativa**: organizar o levantamento por **ponto de entrada
de runtime** (API Gateway, fila SQS, DynamoDB Streams, evento S3/EventBridge, EventBridge
Scheduler, callback SNS/SES, Step Functions), nunca por pasta de cÃƒÂ³digo (`src/workers/`) Ã¢â‚¬â€
essa organizaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ produziu falsos negativos duas vezes (D-062 e esta rodada).

## O que jÃƒÂ¡ estÃƒÂ¡ validado e deve ser reaproveitado, nÃƒÂ£o redescoberto

- InventÃƒÂ¡rio verificado dos ~40 `entityType` reais do sistema, por presenÃƒÂ§a de `version`/
  `tenantId` (D-062, Rodadas 3-4).
- Mecanismo de descoberta+exclusÃƒÂ£o (Scan + taxonomia de 3 categorias + convergÃƒÂªncia por
  re-Scan) Ã¢â‚¬â€ a parte de "como apagar o que jÃƒÂ¡ nÃƒÂ£o tem mais escritor" continua vÃƒÂ¡lida; o que
  falta ÃƒÂ© sÃƒÂ³ a garantia de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" antes de declarar concluÃƒÂ­do.
- `Document` deve reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061) Ã¢â‚¬â€ nunca duplicar.
- `Session`/`LoginAttempt`/`DeviceSession` vivem em `bff-session-table`, tabela fÃƒÂ­sica separada.
- Usar `User.status` (ou um registro de tenant equivalente) como sinal central ÃƒÂ© uma boa
  direÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ nÃƒÂ£o pode ser o prÃƒÂ³prio dado que a cascata apaga, e nÃƒÂ£o pode ser uma leitura
  desacoplada da escrita protegida.

## PrÃƒÂ³xima sessÃƒÂ£o Ã¢â‚¬â€ como retomar com rigor

1. Ler esta pasta inteira + `w3-07-tenant-cascade-deletion-design/` (D-062) antes de propor
   qualquer coisa nova Ã¢â‚¬â€ nÃƒÂ£o repetir o levantamento do zero.
2. Levantar TODOS os pontos de entrada de runtime reais (nÃƒÂ£o sÃƒÂ³ `src/workers/`) que podem
   escrever dado de um tenant: grep por `SQSEvent`, `handler(): Promise<void>` (schedule),
   `S3Event`/`EventBridgeEvent`, Step Functions (`infra/state-machines/*.asl.json`), callbacks
   SNS/SES Ã¢â‚¬â€ e para cada um, confirmar se e como ele valida que o tenant/recurso de origem
   ainda ÃƒÂ© vÃƒÂ¡lido.
3. Desenhar o tombstone do tenant como um registro que **sobrevive** ÃƒÂ  prÃƒÂ³pria cascata e que
   `RequestContextResolver`/toda superfÃƒÂ­cie de entrada consulta ANTES de qualquer
   provisionamento automÃƒÂ¡tico (fechando o achado mais grave desta rodada).
4. Decidir, para cada superfÃƒÂ­cie de escrita, se o fence entra via `ConditionCheck` na mesma
   transaÃƒÂ§ÃƒÂ£o (caminhos DynamoDB) ou via um protocolo de claim/drenagem dedicado (efeitos
   externos: SES, S3, Step Functions, filas).
5. SÃƒÂ³ depois disso desenhar novamente a descoberta+exclusÃƒÂ£o em si (que jÃƒÂ¡ estÃƒÂ¡ bem
   encaminhada) Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© o gargalo, o fence ÃƒÂ©.

## Registro de decisÃƒÂ£o

D-063 em `docs/architecture/decisions-log.md`: reprovada na Rodada 1 (3,2/10), pausada por
pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada futura, nÃƒÂ£o uma
decisÃƒÂ£o de abandonar o objetivo.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 163ms:
# W3-07 Ã¢â‚¬â€ Tenant Deletion Fence, Proposta Round 1 (retomada pÃƒÂ³s-D-063)

> Contexto obrigatÃƒÂ³rio jÃƒÂ¡ lido: `w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md` (D-062)
> e `w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (D-063). Este documento
> assume os achados de ambos como verdadeiros e nÃƒÂ£o os redescobre.

## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃƒÂ£o por pasta)

Auditoria completa dos 36 handlers reais em `src/runtime/aws/handlers/` contra o Terraform (gatilho declarado,
nÃƒÂ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:

1. **NÃƒÂ£o existe hoje nenhum cÃƒÂ³digo de cascata/fence** Ã¢â‚¬â€ greenfield real, nenhuma verificaÃƒÂ§ÃƒÂ£o de "tenant
   excluÃƒÂ­do" em lugar nenhum.
2. `RequestContextResolver.resolve()` ÃƒÂ© o ÃƒÂºnico ponto do sistema que checa qualquer status hoje
   (`UserProfile.status !== "ACTIVE"`), e sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ado por rotas HTTP autenticadas. A checagem ÃƒÂ©
   **desacoplada** (lida uma vez no inÃƒÂ­cio do handler, fora de qualquer transaÃƒÂ§ÃƒÂ£o).
3. `GuestSubmissionService.resolveToken()` **nunca** consulta tenant/usuÃƒÂ¡rio Ã¢â‚¬â€ sÃƒÂ³ token/`DocumentRequest`
   (comentÃƒÂ¡rio explÃƒÂ­cito no cÃƒÂ³digo confirmando que ÃƒÂ© deliberado). Sem fence hoje.
4. `startSubmission()`/`handleReserveUpload`/`handleReserveImport` emitem **URLs S3 presignadas** (PUT) que o
   S3 aceita sem passar por nenhum handler nosso atÃƒÂ© o evento "Object Created" disparar a finalizaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€
   minutos depois, com `tenantId` derivado da **key S3 jÃƒÂ¡ gravada**, nÃƒÂ£o de contexto revalidado.
5. Toda a cadeia `import-parse Ã¢â€ â€™ import-commit`, `reminder-producer Ã¢â€ â€™ reminder-dispatch Ã¢â€ â€™
   reminder-materialization-trigger`, `dispatch-outbox-relay`/`outbox-sweeper`, `notification-router Ã¢â€ â€™
   email-delivery Ã¢â€ â€™ ses-callback`, `document-chasing-dispatch`, `malware-result`/`upload-finalizer`,
   `upload-slot-reconciliation` Ã¢â‚¬â€ **nenhuma** checa tenant hoje; `tenantId` vem sempre de um payload jÃƒÂ¡
   enfileirado ou de uma key S3 jÃƒÂ¡ materializada antes.
6. A Step Functions `document-extraction` (`extraction-starter Ã¢â€ â€™ RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™
   [RunBedrock] Ã¢â€ â€™ ValidateSchema/CompareExtractors Ã¢â€ â€™ PersistExtractedFields/MarkPendingConfirmation Ã¢â€ â€™
   CompleteRun`) propaga `tenantId` pelo payload de state para state, nunca revalida. As escritas reais
   (`PersistExtractedFields`/`MarkPendingConfirmation`) sÃƒÂ³ acontecem nos dois ÃƒÂºltimos states Ã¢â‚¬â€ minutos depois
   do inÃƒÂ­cio, depois de uma chamada Bedrock paga real e de um `waitForTaskToken` do Textract (atÃƒÂ© 600s).
7. **Achado que muda o desenho**: apesar da superfÃƒÂ­cie parecer enorme (36 handlers, 3 classes de gatilho,
   Step Functions incluÃƒÂ­da), praticamente toda escrita de dado de tenant termina em um de dois primitivos
   jÃƒÂ¡ existentes e centralizados:
   - `TransactWriteCommand` (DynamoDB), construÃƒÂ­do em **8 arquivos de persistÃƒÂªncia**
     (`dynamodb-{extracted-field,document,reminder,subject,import,expiration,reminder-producer,notification}-store.ts`),
     usando os builders de `src/shared/dynamodb/occ.ts` (`buildVersionedUpdate`/`buildVersionedCreate`/
     `buildVersionConditionCheck`/`buildExistenceConditionCheck`) Ã¢â‚¬â€ regra jÃƒÂ¡ normativa em `AGENTS.md` Ã‚Â§7.
   - `UpdateCommand`/`PutCommand`/`DeleteCommand` single-item, em **11 arquivos de persistÃƒÂªncia** (mesma lista
     + `dynamodb-extraction-run-store`, `dynamodb-textract-job-store`, `dynamodb-session-store` [tabela
     separada], `dynamodb-outbox-relay-store`, `dynamodb-identity-store`).
   Isso ÃƒÂ© uma superfÃƒÂ­cie **enumerÃƒÂ¡vel e jÃƒÂ¡ disciplinada**, nÃƒÂ£o 36 pontos ad-hoc Ã¢â‚¬â€ o oposto do que quebrou
   D-062/D-063.

## 1. O tombstone (sobrevive ÃƒÂ  cascata)

Entidade nova `TenantLifecycleRecord`, **fora do universo apagÃƒÂ¡vel pela cascata** (a rotina de
descoberta+exclusÃƒÂ£o por Scan de D-062/D-063 exclui explicitamente este `entityType` Ã¢â‚¬â€ mesma disciplina jÃƒÂ¡
usada para excluir a prÃƒÂ³pria tabela `bff-session-table` do Scan).

```
PK = "TENANT#<tenantId>#LIFECYCLE"
SK = "LIFECYCLE"
entityType = "TenantLifecycleRecord"
status = "ACTIVE" | "DELETING" | "DELETED"
deletionStartedAt / deletionCompletedAt
version   (OCC padrÃƒÂ£o)
```

Vive na tabela principal (nÃƒÂ£o numa tabela separada Ã¢â‚¬â€ precisa ser lido pelo mesmo `TransactWriteItems` que
grava dado de tenant, e `TransactWriteItems` nÃƒÂ£o atravessa tabelas). Nasce `ACTIVE` no primeiro
`createProfileIfAbsent` (mesma transaÃƒÂ§ÃƒÂ£o, custo zero de coordenaÃƒÂ§ÃƒÂ£o adicional).

## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃƒÂ£o handler por handler)

Dois wrappers novos em `src/shared/dynamodb/`, substituindo a construÃƒÂ§ÃƒÂ£o direta de comandos nos 8+11 arquivos
de persistÃƒÂªncia:

- `fencedTransactWrite(client, { tenantId, items })` Ã¢â‚¬â€ adiciona automaticamente, como **primeiro** item da
  lista, um `ConditionCheck` (via `buildExistenceConditionCheck` jÃƒÂ¡ existente) contra
  `TENANT#<tenantId>#LIFECYCLE`/`LIFECYCLE` asserindo `status = "ACTIVE"`. Todo `TransactWriteItems` que
  escreve dado de tenant passa a incluir esse ConditionCheck sem que cada service precise lembrar.
- `fencedSingleWrite(client, { tenantId, command })` Ã¢â‚¬â€ para os `UpdateCommand`/`PutCommand`/`DeleteCommand`
  isolados, reescreve o comando como um `TransactWriteCommand` de 2 itens (a escrita real + o ConditionCheck
  do fence) Ã¢â‚¬â€ DynamoDB nÃƒÂ£o tem `ConditionExpression` cross-item para comando single-item, entÃƒÂ£o a ÃƒÂºnica forma
  atÃƒÂ´mica de compor "escreva X E confirme que o tenant estÃƒÂ¡ ACTIVE" ÃƒÂ© uma transaÃƒÂ§ÃƒÂ£o de 2 itens. Aceitar o
  custo de WCU/latÃƒÂªncia adicional (jÃƒÂ¡ pago hoje pelos outros usos de `TransactWriteItems` no mesmo cÃƒÂ³digo).

**Enforcement estrutural, nÃƒÂ£o convenÃƒÂ§ÃƒÂ£o**: regra nova de `dependency-cruiser` (mesmo mecanismo que jÃƒÂ¡ proÃƒÂ­be
`shared` de importar `modules/**`) proibindo `new TransactWriteCommand(`/`new UpdateCommand(`/`new
PutCommand(`/`new DeleteCommand(` fora de `src/shared/dynamodb/fenced-write.ts` Ã¢â‚¬â€ fecha a classe de erro
"um call site novo esqueceu o fence", que foi exatamente a causa raiz do achado mais grave de D-063
(auditoria incompleta por pasta).

Isso fecha, sem tocar em nenhum dos 36 handlers individualmente: `items-handler`, `documents-handler` (a
persistÃƒÂªncia do `Document`, nÃƒÂ£o a emissÃƒÂ£o da URL presignada Ã¢â‚¬â€ ver Ã‚Â§4), `guest-documents-handler` (via
`DocumentSubmission`), `import-commit-handler` (mesmo com `systemContextFor()` sintÃƒÂ©tico Ã¢â‚¬â€ o fence nÃƒÂ£o
depende de `RequestContext`, ÃƒÂ© checado na escrita, nÃƒÂ£o na autorizaÃƒÂ§ÃƒÂ£o), toda a cadeia de lembretes/outbox,
`notification-router`/`email-delivery` (a gravaÃƒÂ§ÃƒÂ£o da tentativa, nÃƒÂ£o o `ses:SendEmail` em si Ã¢â‚¬â€ ver Ã‚Â§5),
`malware-result`/`upload-finalizer`, `upload-slot-reconciliation`, e as duas tasks de escrita da Step
Functions (`PersistExtractedFields`/`MarkPendingConfirmation`).

## 3. Fechando a ressurreiÃƒÂ§ÃƒÂ£o no resolver (defesa em profundidade, nÃƒÂ£o a defesa primÃƒÂ¡ria)

`RequestContextResolver.resolve()` ganha uma leitura do `TenantLifecycleRecord` **antes** de
`createProfileIfAbsent` (nÃƒÂ£o substitui a checagem de `UserProfile.status`, ÃƒÂ© adicional e anterior). Se
`status !== "ACTIVE"`, rejeita com 403 sem provisionar nada. Isso ÃƒÂ© defesa em profundidade Ã¢â‚¬â€ mesmo que
faltasse, o Ã‚Â§2 jÃƒÂ¡ impediria a escrita real do `User` recriado, porque `createProfileIfAbsent` tambÃƒÂ©m passa
pelo `fencedSingleWrite`. A checagem no resolver existe para dar um erro claro (403) em vez de deixar o
usuÃƒÂ¡rio achar que logou e sÃƒÂ³ descobrir a rejeiÃƒÂ§ÃƒÂ£o no primeiro POST.

## 4. URLs presignadas jÃƒÂ¡ emitidas (S3)

NÃƒÂ£o sÃƒÂ£o revogÃƒÂ¡veis diretamente. MitigaÃƒÂ§ÃƒÂ£o real, nÃƒÂ£o best-effort: a finalizaÃƒÂ§ÃƒÂ£o (`upload-finalizer-handler`/
`malware-result-handler`/equivalente guest) ÃƒÂ© exatamente o ponto que persiste `Document`/`DocumentSubmission`
via `fencedTransactWrite` (Ã‚Â§2) Ã¢â‚¬â€ se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a transaÃƒÂ§ÃƒÂ£o de finalizaÃƒÂ§ÃƒÂ£o falha
(`ConditionalCheckFailedException`), o objeto S3 fica ÃƒÂ³rfÃƒÂ£o na quarantine (limpo depois por
`document-purge-handler`/lifecycle, mecanismo jÃƒÂ¡ aprovado em D-061), mas **nenhuma linha DynamoDB nova
aparece**. Resultado: nenhuma ressurreiÃƒÂ§ÃƒÂ£o de dado consultÃƒÂ¡vel, sÃƒÂ³ lixo de storage jÃƒÂ¡ coberto por mecanismo
existente. TTL curto (600s) jÃƒÂ¡ limita a janela de exposiÃƒÂ§ÃƒÂ£o.

## 5. Efeitos externos irreversÃƒÂ­veis (SES, Bedrock, Textract) Ã¢â‚¬â€ protocolo de duas fases, nÃƒÂ£o checagem simples

Nenhum destes tem `ConditionExpression` atÃƒÂ´mica nativa. Desenho: **a decisÃƒÂ£o de disparar o efeito precisa
nascer de uma escrita fenced**, nÃƒÂ£o de uma leitura solta:

- `email-delivery-handler`: antes de `ses:SendEmail`, marca `NotificationAttempt.status = "SENDING"` via
  `fencedTransactWrite`. Se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a marcaÃƒÂ§ÃƒÂ£o falha e o handler nunca chama SES. Um
  e-mail cuja marcaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ commitou antes do `DELETING` ser escrito ainda sai (janela de milissegundos,
  inerente a qualquer sistema com efeito externo assÃƒÂ­ncrono Ã¢â‚¬â€ documentado como risco residual aceito, nÃƒÂ£o
  escondido).
- `document-chasing-dispatch-handler`: mesmo padrÃƒÂ£o Ã¢â‚¬â€ o novo `GuestTokenPointer` sÃƒÂ³ ÃƒÂ© criado via
  `fencedTransactWrite`; se falhar, nenhum token novo existe, nenhum e-mail ÃƒÂ© enviado.
- Bedrock/Textract (custo, nÃƒÂ£o dado): `BedrockExtractionTaskHandler`/`TextractTaskHandler` jÃƒÂ¡ gravam
  `TenantQuotaService`/`TextractJob` via os stores fenced do Ã‚Â§2 Ã¢â‚¬â€ se o tenant foi excluÃƒÂ­do entre o
  `RunTextract` e o `RunBedrock`, a chamada Bedrock ainda ocorre (custo real, nÃƒÂ£o dado), mas
  `PersistExtractedFields` falha depois. Custo residual pequeno e limitado, nÃƒÂ£o uma garantia de dado quebrada.

## 6. SequÃƒÂªncia da cascata (drenagem antes de apagar)

1. `fencedSingleWrite` grava `TenantLifecycleRecord.status = "DELETING"` (create condicional
   `attribute_not_exists(PK)` Ã¢â€ â€™ falha se uma cascata jÃƒÂ¡ estÃƒÂ¡ em curso, evita corrida de duplo-start).
2. Best-effort: `StopExecution` em toda execuÃƒÂ§ÃƒÂ£o Step Functions ativa cujo payload referencia o tenant
   (via tag/GSI jÃƒÂ¡ existente do desenho de extraÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ reduz custo Bedrock/Textract desperdiÃƒÂ§ado, nÃƒÂ£o ÃƒÂ©
   requisito de correÃƒÂ§ÃƒÂ£o (o fence do Ã‚Â§2/Ã‚Â§5 jÃƒÂ¡ garante correÃƒÂ§ÃƒÂ£o sem isto).
3. Roda a descoberta+exclusÃƒÂ£o jÃƒÂ¡ aprovada (Scan + taxonomia de ~40 `entityType`, convergÃƒÂªncia por re-Scan),
   reusando `DocumentPurgeWorker`/GSI6 para `Document` (D-061) Ã¢â‚¬â€ mecanismo inalterado desta rodada.
4. `TenantLifecycleRecord` **nunca** ÃƒÂ© alvo do Scan (exclusÃƒÂ£o explÃƒÂ­cita por `entityType`, mesma disciplina jÃƒÂ¡
   usada para `bff-session-table`).
5. ConvergÃƒÂªncia confirmada (zero itens restantes) Ã¢â€ â€™ `fencedSingleWrite` flip para `status = "DELETED"`
   (permanente; nenhum cÃƒÂ³digo reabre um tenant `DELETED` Ã¢â‚¬â€ fora de escopo desta feature).

## 7. O que este desenho NÃƒÆ’O resolve (fora de escopo explÃƒÂ­cito, registrar e nÃƒÂ£o mascarar)

- Rotas HTTP/confirmaÃƒÂ§ÃƒÂ£o/exportaÃƒÂ§ÃƒÂ£o de DSR completo Ã¢â‚¬â€ jÃƒÂ¡ fora de escopo desde D-062, inalterado.
- `PolicyRef` sem `tenantId` (achado independente de D-062) Ã¢â‚¬â€ correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica separada.
- `tenantId` (MVP `tenantId=userId`) Ã¢â€°Â  Cognito `sub` Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant
  precisa resolver isso ÃƒÂ  parte; nÃƒÂ£o bloqueia este desenho porque a cascata nunca chama a API do Cognito.

## 8. Por que isto nÃƒÂ£o repete os erros de D-062/D-063

- O fence nÃƒÂ£o ÃƒÂ© a mesma linha que a cascata apaga (Ã‚Â§1, `TenantLifecycleRecord` explicitamente excluÃƒÂ­do do
  Scan) Ã¢â‚¬â€ resolve o achado mais grave de D-063.
- A checagem ÃƒÂ© acoplada (`ConditionCheck` na mesma transaÃƒÂ§ÃƒÂ£o), nunca uma leitura solta antes de agir Ã¢â‚¬â€
  resolve o segundo achado de D-063 (TOCTOU).
- A cobertura nÃƒÂ£o depende de enumerar 36 handlers corretamente Ã¢â‚¬â€ depende de 19 arquivos de persistÃƒÂªncia jÃƒÂ¡
  conhecidos + uma regra de lint estrutural que impede um call site futuro de escapar do fence. Reduz a
  classe de erro "auditoria incompleta" de "confiar em uma lista manual" para "confiar numa regra estÃƒÂ¡tica
  verificÃƒÂ¡vel por `dependency-cruiser`" Ã¢â‚¬â€ resolve a causa raiz comum ÃƒÂ s duas rodadas reprovadas.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/identity/application/resolve-request-context.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 152ms:
/**
 * resolveRequestContext Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§4.1/Ã‚Â§7.2, the central resolver.
 * Steps (verbatim from the blueprint):
 *  1. validation already performed by the API Gateway authorizer (JWT signature/exp) Ã¢â‚¬â€
 *     this function receives already-validated claims, it does not verify JWT signatures;
 *  2. consistent lookup of cognitoSub -> userId (IdentityMapping, atomic first-login create);
 *  3. session/device resolution;
 *  4. tenant/membership resolution;
 *  5. reject tokens issued before globalLogoutAfter/deviceLogoutAfter;
 *  6. build the immutable RequestContext.
 */
import { AuthenticationError } from "../../../shared/errors/app-error.js";
import { IdentityMappingRepository } from "../persistence/identity-mapping-repository.js";
import { UserRepository } from "../persistence/user-repository.js";
import type { RequestContext } from "../domain/request-context.js";

/** Claims already validated (signature + expiry) by the API Gateway JWT authorizer. */
export interface ValidatedClaims {
  sub: string;
  tokenId: string; // jti
  issuedAt: string; // iat, ISO-8601
  expiresAt: string; // exp, ISO-8601
  deviceId?: string;
}

export interface ResolveRequestContextInput {
  claims: ValidatedClaims;
  requestId: string;
  correlationId: string;
}

export interface IdGenerator {
  newUserId(): string;
  newSessionId(): string;
}

export class RequestContextResolver {
  constructor(
    private readonly identityMappings: IdentityMappingRepository,
    private readonly users: UserRepository,
    private readonly ids: IdGenerator,
  ) {}

  async resolve(input: ResolveRequestContextInput): Promise<RequestContext> {
    const { claims } = input;

    // Step 2: cognitoSub -> userId/tenantId. MVP tenantId=userId (data-model.md Ã‚Â§7.3),
    // decided here and nowhere else so a future Organization model changes one call site.
    const newUserId = this.ids.newUserId();
    const mapping = await this.identityMappings.findOrCreate(claims.sub, newUserId, newUserId);

    // Step 3/4: profile + membership. First-login also provisions the User profile
    // (OWNER role, MVP single-tenant-per-user) so resolveContext never returns a
    // context for a mapping with no backing profile.
    let profile = await this.users.getProfile(mapping.tenantId, mapping.userId);
    if (!profile) {
      profile = await this.users.createProfileIfAbsent({
        userId: mapping.userId,
        tenantId: mapping.tenantId,
        identitySubject: claims.sub,
        emailNormalized: "",
        roles: ["OWNER"],
        status: "ACTIVE",
      });
    }

    if (profile.status !== "ACTIVE") {
      throw new AuthenticationError("User is not active.", { userId: profile.userId });
    }

    // Step 5: reject tokens issued before global logout watermark.
    if (profile.globalLogoutAfter && claims.issuedAt < profile.globalLogoutAfter) {
      throw new AuthenticationError("Session revoked (global logout).", { userId: profile.userId });
    }

    let deviceSession;
    if (claims.deviceId) {
      deviceSession = await this.users.getDeviceSession(mapping.tenantId, mapping.userId, claims.deviceId);
      if (deviceSession) {
        if (deviceSession.status === "REVOKED") {
          throw new AuthenticationError("Session revoked (device logout).", { deviceId: claims.deviceId });
        }
        if (deviceSession.deviceLogoutAfter && claims.issuedAt < deviceSession.deviceLogoutAfter) {
          throw new AuthenticationError("Session revoked (device logout).", { deviceId: claims.deviceId });
        }
      }
    }

    const sessionId = deviceSession?.sessionId ?? this.ids.newSessionId();

    return {
      requestId: input.requestId,
      correlationId: input.correlationId,
      principal: {
        userId: profile.userId,
        cognitoSubject: claims.sub,
        sessionId,
        deviceId: claims.deviceId,
      },
      tenant: {
        tenantId: profile.tenantId,
        roles: profile.roles,
      },
      auth: {
        issuedAt: claims.issuedAt,
        expiresAt: claims.expiresAt,
        tokenId: claims.tokenId,
      },
    };
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/shared/dynamodb/occ.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 164ms:
/**
 * Optimistic concurrency control helper - implementation-blueprint.md #5.2 / M0 deliverable.
 *
 * M0 ships no tables/Lambdas yet (that's M1+), so this module only builds the DynamoDB
 * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
 * so it stays testable without AWS credentials/mocks. M1 wires these builders into the
 * actual DocumentClient calls once tables exist.
 */

export interface EntityKey {
  PK: string;
  SK: string;
}

export interface VersionedUpdateInput {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  /** Additional SET clauses beyond version/updatedAt, e.g. { status: "CANCELLED" }. */
  set: Record<string, unknown>;
  /** Attribute names to REMOVE atomically in the same conditional update - e.g. dropping
   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
   * since M3 (occ.ts was SET-only, cancelled GSI3 pointers were left orphaned because
   * nothing actively queried them); M3.5 needs REMOVE for real because GSI6 pointers ARE
   * actively queried by reconciliation. */
  remove?: string[];
  now?: string;
  /** Extra ConditionExpression clauses ANDed to the base condition, each with its own
   * caller-supplied names/values (W3-06 purge worker's claim/fence conditions - e.g. checking
   * `legalHold`/`GSI6PK`/`purgeAfter` atomically alongside the version check). Each entry's
   * `expression` is wrapped in its own parentheses before being ANDed in, so callers never need
   * to hand-balance parens against the base condition. Placeholder names/values MUST NOT collide
   * with the base condition's (#version/#tenantId/#updatedAt/:expectedVersion/:tenantId/:one/:now)
   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
   * throws before building an invalid ConditionExpression rather than silently overwriting one
   * caller's placeholder with another's. */
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}

export interface DynamoUpdateCommandInput {
  TableName: string;
  Key: EntityKey;
  UpdateExpression: string;
  ConditionExpression: string;
  ExpressionAttributeNames: Record<string, string>;
  ExpressionAttributeValues: Record<string, unknown>;
}

/**
 * Builds an UpdateItem input enforcing implementation-blueprint.md #5.2's exact
 * ConditionExpression: attribute_exists(PK) AND attribute_exists(SK) AND
 * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
 */
export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
  const now = input.now ?? new Date().toISOString();

  const names: Record<string, string> = {
    "#version": "version",
    "#tenantId": "tenantId",
    "#updatedAt": "updatedAt",
  };
  const values: Record<string, unknown> = {
    ":expectedVersion": input.expectedVersion,
    ":tenantId": input.tenantId,
    ":one": 1,
    ":now": now,
  };

  const setClauses = ["#version = #version + :one", "#updatedAt = :now"];
  let i = 0;
  for (const [field, value] of Object.entries(input.set)) {
    const nameKey = `#set${i}`;
    const valueKey = `:set${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    setClauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  const removeClauses: string[] = [];
  let j = 0;
  for (const field of input.remove ?? []) {
    const nameKey = `#rem${j}`;
    names[nameKey] = field;
    removeClauses.push(nameKey);
    j += 1;
  }

  const expression =
    removeClauses.length > 0
      ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}`
      : `SET ${setClauses.join(", ")}`;

  const baseConditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved/generated key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved/generated key: ${valueKey}`);
      values[valueKey] = value;
    }
    baseConditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: expression,
    ConditionExpression: baseConditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export interface DynamoPutCommandInput {
  TableName: string;
  Item: Record<string, unknown>;
  ConditionExpression: string;
}

/**
 * Builds a PutItem input for first-time creation of a versioned entity, per
 * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
 * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
 */
export function buildVersionedCreate(tableName: string, item: Record<string, unknown> & EntityKey): DynamoPutCommandInput {
  return {
    TableName: tableName,
    Item: item,
    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
  };
}

/**
 * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
 * (2026-08-19, Engineering Maturity Review): that was the only place they were declared,
 * but reminder/ports/reminder-store.ts already re-exported them from there (a cross-module
 * ports->ports dependency), and expiration/domain/audit-event.ts imported them directly,
 * which is a domain->ports boundary violation dependency-cruiser catches but ESLint's
 * text-matching no-restricted-imports rule cannot (it only matches literal specifier
 * substrings, and this codebase's relative imports never contain "modules/"). Reuses the
 * inner Put/Update shapes already defined above instead of re-declaring them a third time.
 */
export interface TransactPutEntry {
  Put: DynamoPutCommandInput;
}

export interface TransactUpdateEntry {
  Update: DynamoUpdateCommandInput;
}

export interface DynamoConditionCheckCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactConditionCheckEntry {
  ConditionCheck: DynamoConditionCheckCommandInput;
}

export interface DynamoDeleteCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression?: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactDeleteEntry {
  Delete: DynamoDeleteCommandInput;
}

/**
 * Builds a version-conditioned Delete input (W3-06 purge worker's terminal step - a Document
 * row can only be physically removed if it is still at the exact version/state the claim
 * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
 * `UpdateItem`). `extraConditions` uses the same collision-checked merge as
 * `buildVersionedUpdate` - see that function's docs.
 */
export function buildVersionedDelete(input: {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}): DynamoDeleteCommandInput {
  const names: Record<string, string> = { "#version": "version", "#tenantId": "tenantId" };
  const values: Record<string, unknown> = { ":expectedVersion": input.expectedVersion, ":tenantId": input.tenantId };
  const conditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];

  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved key: ${valueKey}`);
      values[valueKey] = value;
    }
    conditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    ConditionExpression: conditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;

/**
 * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
 * expected version (and, optionally, still holds a given attribute value) - used to fence
 * a commit against a fact read earlier in the same operation but not itself written by
 * this transaction (e.g. reminder dispatch asserting the ReminderPolicy it read is still
 * enabled at the version it read, per the BLOCKER-B freshness-fencing fix: reading a row
 * and later writing elsewhere based on that read is a TOCTOU gap unless the read fact is
 * re-asserted atomically inside the same TransactWriteItems as the write it gates).
 */
export function buildVersionConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  expectedVersion: number;
  /** Extra attribute=value equality conditions beyond version, e.g. { enabled: true, status: "ACTIVE" }. */
  extra?: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = { "#version": "version" };
  const values: Record<string, unknown> = { ":version": input.expectedVersion };
  const clauses = ["#version = :version"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra ?? {})) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/**
 * Sibling of `buildVersionConditionCheck` for facts that have no version to pin - e.g.
 * BLOCKER-B's ITEM-scoped-policy integrity check (a policy references an item's
 * existence/status/tenant, not a specific item version - referencing an item doesn't pin
 * it to a version the way an occurrence's materialization does). `extra` must include at
 * least one condition (an existence-only check with no `extra` would degenerate to
 * `attribute_exists(PK)`, which every real row satisfies trivially - not useful on its own).
 */
export function buildExistenceConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  extra: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = {};
  const values: Record<string, unknown> = {};
  const clauses = ["attribute_exists(PK)"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra)) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
 * ConditionExpression - used by callers to distinguish OCC/idempotency conflicts from other
 * DynamoDB errors without importing the SDK type. */
export const TRANSACTION_CANCELED = "TransactionCanceledException";

export function isTransactionCanceled(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === TRANSACTION_CANCELED
  );
}

/** Name of the AWS SDK error thrown when a ConditionExpression fails - used by callers
 * to distinguish OCC conflicts from other DynamoDB errors without importing the SDK type. */
export const CONDITIONAL_CHECK_FAILED = "ConditionalCheckFailedException";

export function isConditionalCheckFailed(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === CONDITIONAL_CHECK_FAILED
  );
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 144ms:
# W3-07 Ã¢â‚¬â€ Status final: REPROVADO, NÃƒÆ’O IMPLEMENTADO (2026-08-28)

> D-062 em `docs/architecture/decisions-log.md`. Este documento resume o resultado das 4
> rodadas para quem retomar este trabalho no futuro Ã¢â‚¬â€ nÃƒÂ£o reabrir o protocolo do zero sem ler
> isto primeiro.

## Resultado

4 rodadas ClaudeÃ¢â€ â€Codex, notas **3,4 Ã¢â€ â€™ 5,1 Ã¢â€ â€™ 4,7 / 10** Ã¢â‚¬â€ nunca atingiu o gate de 9,0. Reprovado
por decisÃƒÂ£o do Marcelo, seguindo recomendaÃƒÂ§ÃƒÂ£o convergente e independente de Claude e Codex
(pedida explicitamente apÃƒÂ³s a Rodada 3): **nÃƒÂ£o implementar agora**, registrar os achados como
pesquisa.

## Por que nÃƒÂ£o convergiu (o achado central)

Toda tentativa de fechar "COMPLETED" com garantia real de nÃƒÂ£o-ressurreiÃƒÂ§ÃƒÂ£o (um dado do tenant
sendo recriado depois de uma exclusÃƒÂ£o jÃƒÂ¡ declarada concluÃƒÂ­da) esbarrou na mesma parede: o
sistema tem **trÃƒÂªs classes de escritor** que uma prÃƒÂ©-condiÃƒÂ§ÃƒÂ£o simples (ex.: desativar login no
Cognito) nÃƒÂ£o fecha:

1. HTTP autenticado (fechÃƒÂ¡vel via Cognito).
2. **SuperfÃƒÂ­cie pÃƒÂºblica de guest upload** (`GuestSubmissionService` Ã¢â‚¬â€ nunca passa por
   `RequestContext`/Cognito, autentica sÃƒÂ³ por token opaco).
3. **Workers assÃƒÂ­ncronos** jÃƒÂ¡ enfileirados ou agendados independentemente de qualquer sessÃƒÂ£o de
   usuÃƒÂ¡rio (`reminder-materialization-trigger`, `reminder-reconciliation`,
   `document-chasing-dispatch`, outbox relay/sweeper, callback SES, reconciliaÃƒÂ§ÃƒÂ£o de
   upload/malware, import, extraÃƒÂ§ÃƒÂ£o/OCR via Step Functions).

Fechar as trÃƒÂªs de verdade exige um fence de `TenantStatus` consultado por **todo** esses
caminhos Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© mais "o mecanismo de descoberta+exclusÃƒÂ£o", ÃƒÂ© a mesma feature de "bloqueio
imediato" que a decisÃƒÂ£o de escopo original desta sessÃƒÂ£o jÃƒÂ¡ tinha deixado de fora
deliberadamente. As duas decisÃƒÂµes de escopo (mecanismo pequeno vs. sem bloqueio imediato) sÃƒÂ£o
mutuamente incompatÃƒÂ­veis com uma garantia forte Ã¢â‚¬â€ nÃƒÂ£o dÃƒÂ¡ para ter as duas.

## Por que nÃƒÂ£o foi aceito um "best-effort documentado" como meio-termo

RecomendaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita do Codex, aceita: implementar um mecanismo que alega apagar
fisicamente mas nÃƒÂ£o sustenta essa garantia ÃƒÂ© uma capacidade **pior que nÃƒÂ£o ter nenhuma** Ã¢â‚¬â€ pode
ser lido operacionalmente como "DSR atendido" mesmo com uma ressalva no `decisions-log.md`, e
ainda geraria cÃƒÂ³digo que precisaria ser redesenhado quando a garantia real for exigida de
verdade.

## O que sobrevive desta pesquisa (reusar, nÃƒÂ£o redescobrir)

- **InventÃƒÂ¡rio verificado dos 40 `entityType` reais** do sistema (`src/modules/**`), com
  classificaÃƒÂ§ÃƒÂ£o por presenÃƒÂ§a de `version`/`tenantId` Ã¢â‚¬â€ ver Rodada 3/4
  (`claude-proposal-round3.md`/`round4.md`) para a tabela completa e as correÃƒÂ§ÃƒÂµes feitas em
  cima da prÃƒÂ³pria Rodada 2 (que tambÃƒÂ©m errou o inventÃƒÂ¡rio na primeira tentativa Ã¢â‚¬â€ dois erros
  reais de levantamento em rodadas diferentes, liÃƒÂ§ÃƒÂ£o: sempre grep exaustivo, nunca amostra).
- **Bug real descoberto, nÃƒÂ£o corrigido ainda**: `tenantId` (MVP `tenantId=userId`) nÃƒÂ£o ÃƒÂ© o
  Cognito `sub`/`Username` Ã¢â‚¬â€ `IdentityMapping.cognitoSub`/`User.identitySubject` sÃƒÂ£o os campos
  certos. Qualquer aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant precisa resolver isso
  primeiro.
- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
  `bff-session-table`, uma tabela DynamoDB fÃƒÂ­sica separada Ã¢â‚¬â€ nenhum mecanismo de descoberta
  tenant-wide pode assumir uma ÃƒÂºnica tabela.
- **CorreÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica pendente, independente**: `PolicyRef` (`reminder-policy.ts:69-72`) nÃƒÂ£o
  carrega `tenantId` Ã¢â‚¬â€ pequena, real, trivial de corrigir numa sessÃƒÂ£o futura sem depender de
  W3-07.
- **Reuso jÃƒÂ¡ decidido para quando retomar**: `Document` continua a ÃƒÂºnica categoria com efeito
  colateral S3 real Ã¢â‚¬â€ reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061), nunca duplicar.

## Gatilho para retomar

Requisito real de DSR (idealmente decidido antes do primeiro cliente B2B que dependa disso).
Quando retomado, o fence de `TenantStatus` em todo write path (HTTP + guest + workers) deve
entrar no escopo do **primeiro** desenho, nÃƒÂ£o ser descoberto de novo rodada a rodada como
aconteceu aqui.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 182ms:
# W3-07 (retomada, D-063) Ã¢â‚¬â€ status: PAUSADO apÃƒÂ³s Rodada 1, para planejamento minucioso na prÃƒÂ³xima sessÃƒÂ£o

> DecisÃƒÂ£o do Marcelo (2026-08-28): nÃƒÂ£o adiar por falta de usuÃƒÂ¡rio real hoje Ã¢â‚¬â€ construir a
> garantia completa de exclusÃƒÂ£o de tenant algum dia, mas com anÃƒÂ¡lise minuciosa antes de
> implementar. Esta pasta ÃƒÂ© a continuaÃƒÂ§ÃƒÂ£o de
> `docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/` (D-062, reprovada em 4
> rodadas) Ã¢â‚¬â€ leia aquela primeiro (especialmente `claude-final-status-not-approved.md`) antes
> desta.

## O que esta rodada tentou

Reabrir o desenho com o fence de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" dentro do escopo desde a Rodada 1 (em vez
de descoberto reativamente, como na tentativa D-062). Achado de partida:
`User.status: "ACTIVE" | "SUSPENDED"` (`user-repository.ts:22`) jÃƒÂ¡ existe e jÃƒÂ¡ ÃƒÂ© checado em toda
resoluÃƒÂ§ÃƒÂ£o de contexto HTTP autenticada (`resolve-request-context.ts:67-69`), mas `"SUSPENDED"`
nunca ÃƒÂ© escrito por nenhum cÃƒÂ³digo hoje. A proposta usava isso como fence primÃƒÂ¡rio (zero cÃƒÂ³digo
novo para a superfÃƒÂ­cie HTTP autenticada) + checagem nova no fluxo de convidado + auditoria dos
workers em `src/workers/`.

## Resultado: Rodada 1, nota 3,2/10, REPROVADA Ã¢â‚¬â€ pior que a rodada equivalente da tentativa anterior

Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
**precisa** resolver desde o desenho inicial, nÃƒÂ£o descobrir depois:

### Achado mais grave Ã¢â‚¬â€ o prÃƒÂ³prio fence ÃƒÂ© apagÃƒÂ¡vel pela cascata que ele deveria proteger

`RequestContextResolver.resolve()` (`resolve-request-context.ts:49-65`) provisiona
automaticamente um `User` novo com `status: "ACTIVE"` no primeiro login de um `cognitoSub`
sem perfil existente. Se a cascata apaga o `User` (ele ÃƒÂ© sÃƒÂ³ mais uma linha do tenant, como
qualquer outra), um token ainda vÃƒÂ¡lido reautentica e o sistema **recria o tenant sozinho** Ã¢â‚¬â€
a prÃƒÂ³pria lÃƒÂ³gica de "provisionar no primeiro login" resssucita o que acabou de ser apagado.
**ConsequÃƒÂªncia de design**: o tombstone/fence do tenant nÃƒÂ£o pode ser a mesma linha que a
cascata elimina Ã¢â‚¬â€ precisa ser um registro que sobrevive ÃƒÂ  exclusÃƒÂ£o completa (ex.: um registro
de "tenant encerrado" fora do universo apagÃƒÂ¡vel, no mesmo espÃƒÂ­rito do `TenantDeletionRequest`
jÃƒÂ¡ proposto em D-062, mas agora como fonte de verdade que o RESOLVER tambÃƒÂ©m consulta, nÃƒÂ£o sÃƒÂ³
um tombstone passivo).

### Checagens desacopladas (leitura solta antes de agir) tÃƒÂªm corrida TOCTOU real

Qualquer "leia `User.status`, depois aja" (proposto para `GuestSubmissionService` e para os
workers) tem uma janela real entre a leitura e o efeito. Para efeitos DynamoDB, o fence precisa
participar da MESMA `TransactWriteItems` da mutaÃƒÂ§ÃƒÂ£o (`ConditionCheck`), nÃƒÂ£o uma leitura solta
antes. Para efeitos externos (SES, S3, SQS, Step Functions) nÃƒÂ£o existe uma condiÃƒÂ§ÃƒÂ£o atÃƒÂ´mica
equivalente Ã¢â‚¬â€ precisa de um protocolo de claim/drenagem dedicado, nÃƒÂ£o uma checagem simples.

### Auditoria de superfÃƒÂ­cies de escrita estava errada e incompleta

Confirmado incorreto (releem entidade de origem, mas ainda produzem efeito novo sem fence real):
- `dispatch-outbox-relay` Ã¢â‚¬â€ produz mensagem SQS nova antes de marcar o outbox como publicado.
- `upload-slot-reconciliation` Ã¢â‚¬â€ escreve `Document.status = TIMEOUT` sem reler fence de tenant.
- `document-chasing-dispatch` Ã¢â‚¬â€ pode criar `GuestTokenPointer` novo e mandar e-mail via SES
  depois de o tenant jÃƒÂ¡ estar suspenso.

SuperfÃƒÂ­cies inteiras que a auditoria por pasta (`src/workers/`) nÃƒÂ£o capturou:
- `import-commit-handler` Ã¢â‚¬â€ fabrica um `RequestContext` de sistema com papel `OWNER`, nunca
  resolve usuÃƒÂ¡rio nem checa status.
- `start-extraction-run`/Step Functions do mÃƒÂ³dulo `extraction` Ã¢â‚¬â€ inicia execuÃƒÂ§ÃƒÂ£o sem fence,
  tarefas subsequentes continuam escrevendo artefato S3/estado depois da suspensÃƒÂ£o.
- `ses-callback-workflow` Ã¢â‚¬â€ cria/atualiza `WebhookInbox`/`NotificationAttempt`/
  `NotificationPreferences` sem checar fence.
- URLs presignadas de S3 jÃƒÂ¡ entregues a um convidado continuam vÃƒÂ¡lidas mesmo apÃƒÂ³s o fence Ã¢â‚¬â€
  um upload feito depois ainda dispara toda a cadeia de finalizaÃƒÂ§ÃƒÂ£o/malware/extraÃƒÂ§ÃƒÂ£o.

**LiÃƒÂ§ÃƒÂ£o estrutural para a prÃƒÂ³xima tentativa**: organizar o levantamento por **ponto de entrada
de runtime** (API Gateway, fila SQS, DynamoDB Streams, evento S3/EventBridge, EventBridge
Scheduler, callback SNS/SES, Step Functions), nunca por pasta de cÃƒÂ³digo (`src/workers/`) Ã¢â‚¬â€
essa organizaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ produziu falsos negativos duas vezes (D-062 e esta rodada).

## O que jÃƒÂ¡ estÃƒÂ¡ validado e deve ser reaproveitado, nÃƒÂ£o redescoberto

- InventÃƒÂ¡rio verificado dos ~40 `entityType` reais do sistema, por presenÃƒÂ§a de `version`/
  `tenantId` (D-062, Rodadas 3-4).
- Mecanismo de descoberta+exclusÃƒÂ£o (Scan + taxonomia de 3 categorias + convergÃƒÂªncia por
  re-Scan) Ã¢â‚¬â€ a parte de "como apagar o que jÃƒÂ¡ nÃƒÂ£o tem mais escritor" continua vÃƒÂ¡lida; o que
  falta ÃƒÂ© sÃƒÂ³ a garantia de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" antes de declarar concluÃƒÂ­do.
- `Document` deve reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061) Ã¢â‚¬â€ nunca duplicar.
- `Session`/`LoginAttempt`/`DeviceSession` vivem em `bff-session-table`, tabela fÃƒÂ­sica separada.
- Usar `User.status` (ou um registro de tenant equivalente) como sinal central ÃƒÂ© uma boa
  direÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ nÃƒÂ£o pode ser o prÃƒÂ³prio dado que a cascata apaga, e nÃƒÂ£o pode ser uma leitura
  desacoplada da escrita protegida.

## PrÃƒÂ³xima sessÃƒÂ£o Ã¢â‚¬â€ como retomar com rigor

1. Ler esta pasta inteira + `w3-07-tenant-cascade-deletion-design/` (D-062) antes de propor
   qualquer coisa nova Ã¢â‚¬â€ nÃƒÂ£o repetir o levantamento do zero.
2. Levantar TODOS os pontos de entrada de runtime reais (nÃƒÂ£o sÃƒÂ³ `src/workers/`) que podem
   escrever dado de um tenant: grep por `SQSEvent`, `handler(): Promise<void>` (schedule),
   `S3Event`/`EventBridgeEvent`, Step Functions (`infra/state-machines/*.asl.json`), callbacks
   SNS/SES Ã¢â‚¬â€ e para cada um, confirmar se e como ele valida que o tenant/recurso de origem
   ainda ÃƒÂ© vÃƒÂ¡lido.
3. Desenhar o tombstone do tenant como um registro que **sobrevive** ÃƒÂ  prÃƒÂ³pria cascata e que
   `RequestContextResolver`/toda superfÃƒÂ­cie de entrada consulta ANTES de qualquer
   provisionamento automÃƒÂ¡tico (fechando o achado mais grave desta rodada).
4. Decidir, para cada superfÃƒÂ­cie de escrita, se o fence entra via `ConditionCheck` na mesma
   transaÃƒÂ§ÃƒÂ£o (caminhos DynamoDB) ou via um protocolo de claim/drenagem dedicado (efeitos
   externos: SES, S3, Step Functions, filas).
5. SÃƒÂ³ depois disso desenhar novamente a descoberta+exclusÃƒÂ£o em si (que jÃƒÂ¡ estÃƒÂ¡ bem
   encaminhada) Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© o gargalo, o fence ÃƒÂ©.

## Registro de decisÃƒÂ£o

D-063 em `docs/architecture/decisions-log.md`: reprovada na Rodada 1 (3,2/10), pausada por
pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada futura, nÃƒÂ£o uma
decisÃƒÂ£o de abandonar o objetivo.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 146ms:
# W3-07 Ã¢â‚¬â€ Tenant Deletion Fence, Proposta Round 1 (retomada pÃƒÂ³s-D-063)

> Contexto obrigatÃƒÂ³rio jÃƒÂ¡ lido: `w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md` (D-062)
> e `w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (D-063). Este documento
> assume os achados de ambos como verdadeiros e nÃƒÂ£o os redescobre.

## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃƒÂ£o por pasta)

Auditoria completa dos 36 handlers reais em `src/runtime/aws/handlers/` contra o Terraform (gatilho declarado,
nÃƒÂ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:

1. **NÃƒÂ£o existe hoje nenhum cÃƒÂ³digo de cascata/fence** Ã¢â‚¬â€ greenfield real, nenhuma verificaÃƒÂ§ÃƒÂ£o de "tenant
   excluÃƒÂ­do" em lugar nenhum.
2. `RequestContextResolver.resolve()` ÃƒÂ© o ÃƒÂºnico ponto do sistema que checa qualquer status hoje
   (`UserProfile.status !== "ACTIVE"`), e sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ado por rotas HTTP autenticadas. A checagem ÃƒÂ©
   **desacoplada** (lida uma vez no inÃƒÂ­cio do handler, fora de qualquer transaÃƒÂ§ÃƒÂ£o).
3. `GuestSubmissionService.resolveToken()` **nunca** consulta tenant/usuÃƒÂ¡rio Ã¢â‚¬â€ sÃƒÂ³ token/`DocumentRequest`
   (comentÃƒÂ¡rio explÃƒÂ­cito no cÃƒÂ³digo confirmando que ÃƒÂ© deliberado). Sem fence hoje.
4. `startSubmission()`/`handleReserveUpload`/`handleReserveImport` emitem **URLs S3 presignadas** (PUT) que o
   S3 aceita sem passar por nenhum handler nosso atÃƒÂ© o evento "Object Created" disparar a finalizaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€
   minutos depois, com `tenantId` derivado da **key S3 jÃƒÂ¡ gravada**, nÃƒÂ£o de contexto revalidado.
5. Toda a cadeia `import-parse Ã¢â€ â€™ import-commit`, `reminder-producer Ã¢â€ â€™ reminder-dispatch Ã¢â€ â€™
   reminder-materialization-trigger`, `dispatch-outbox-relay`/`outbox-sweeper`, `notification-router Ã¢â€ â€™
   email-delivery Ã¢â€ â€™ ses-callback`, `document-chasing-dispatch`, `malware-result`/`upload-finalizer`,
   `upload-slot-reconciliation` Ã¢â‚¬â€ **nenhuma** checa tenant hoje; `tenantId` vem sempre de um payload jÃƒÂ¡
   enfileirado ou de uma key S3 jÃƒÂ¡ materializada antes.
6. A Step Functions `document-extraction` (`extraction-starter Ã¢â€ â€™ RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™
   [RunBedrock] Ã¢â€ â€™ ValidateSchema/CompareExtractors Ã¢â€ â€™ PersistExtractedFields/MarkPendingConfirmation Ã¢â€ â€™
   CompleteRun`) propaga `tenantId` pelo payload de state para state, nunca revalida. As escritas reais
   (`PersistExtractedFields`/`MarkPendingConfirmation`) sÃƒÂ³ acontecem nos dois ÃƒÂºltimos states Ã¢â‚¬â€ minutos depois
   do inÃƒÂ­cio, depois de uma chamada Bedrock paga real e de um `waitForTaskToken` do Textract (atÃƒÂ© 600s).
7. **Achado que muda o desenho**: apesar da superfÃƒÂ­cie parecer enorme (36 handlers, 3 classes de gatilho,
   Step Functions incluÃƒÂ­da), praticamente toda escrita de dado de tenant termina em um de dois primitivos
   jÃƒÂ¡ existentes e centralizados:
   - `TransactWriteCommand` (DynamoDB), construÃƒÂ­do em **8 arquivos de persistÃƒÂªncia**
     (`dynamodb-{extracted-field,document,reminder,subject,import,expiration,reminder-producer,notification}-store.ts`),
     usando os builders de `src/shared/dynamodb/occ.ts` (`buildVersionedUpdate`/`buildVersionedCreate`/
     `buildVersionConditionCheck`/`buildExistenceConditionCheck`) Ã¢â‚¬â€ regra jÃƒÂ¡ normativa em `AGENTS.md` Ã‚Â§7.
   - `UpdateCommand`/`PutCommand`/`DeleteCommand` single-item, em **11 arquivos de persistÃƒÂªncia** (mesma lista
     + `dynamodb-extraction-run-store`, `dynamodb-textract-job-store`, `dynamodb-session-store` [tabela
     separada], `dynamodb-outbox-relay-store`, `dynamodb-identity-store`).
   Isso ÃƒÂ© uma superfÃƒÂ­cie **enumerÃƒÂ¡vel e jÃƒÂ¡ disciplinada**, nÃƒÂ£o 36 pontos ad-hoc Ã¢â‚¬â€ o oposto do que quebrou
   D-062/D-063.

## 1. O tombstone (sobrevive ÃƒÂ  cascata)

Entidade nova `TenantLifecycleRecord`, **fora do universo apagÃƒÂ¡vel pela cascata** (a rotina de
descoberta+exclusÃƒÂ£o por Scan de D-062/D-063 exclui explicitamente este `entityType` Ã¢â‚¬â€ mesma disciplina jÃƒÂ¡
usada para excluir a prÃƒÂ³pria tabela `bff-session-table` do Scan).

```
PK = "TENANT#<tenantId>#LIFECYCLE"
SK = "LIFECYCLE"
entityType = "TenantLifecycleRecord"
status = "ACTIVE" | "DELETING" | "DELETED"
deletionStartedAt / deletionCompletedAt
version   (OCC padrÃƒÂ£o)
```

Vive na tabela principal (nÃƒÂ£o numa tabela separada Ã¢â‚¬â€ precisa ser lido pelo mesmo `TransactWriteItems` que
grava dado de tenant, e `TransactWriteItems` nÃƒÂ£o atravessa tabelas). Nasce `ACTIVE` no primeiro
`createProfileIfAbsent` (mesma transaÃƒÂ§ÃƒÂ£o, custo zero de coordenaÃƒÂ§ÃƒÂ£o adicional).

## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃƒÂ£o handler por handler)

Dois wrappers novos em `src/shared/dynamodb/`, substituindo a construÃƒÂ§ÃƒÂ£o direta de comandos nos 8+11 arquivos
de persistÃƒÂªncia:

- `fencedTransactWrite(client, { tenantId, items })` Ã¢â‚¬â€ adiciona automaticamente, como **primeiro** item da
  lista, um `ConditionCheck` (via `buildExistenceConditionCheck` jÃƒÂ¡ existente) contra
  `TENANT#<tenantId>#LIFECYCLE`/`LIFECYCLE` asserindo `status = "ACTIVE"`. Todo `TransactWriteItems` que
  escreve dado de tenant passa a incluir esse ConditionCheck sem que cada service precise lembrar.
- `fencedSingleWrite(client, { tenantId, command })` Ã¢â‚¬â€ para os `UpdateCommand`/`PutCommand`/`DeleteCommand`
  isolados, reescreve o comando como um `TransactWriteCommand` de 2 itens (a escrita real + o ConditionCheck
  do fence) Ã¢â‚¬â€ DynamoDB nÃƒÂ£o tem `ConditionExpression` cross-item para comando single-item, entÃƒÂ£o a ÃƒÂºnica forma
  atÃƒÂ´mica de compor "escreva X E confirme que o tenant estÃƒÂ¡ ACTIVE" ÃƒÂ© uma transaÃƒÂ§ÃƒÂ£o de 2 itens. Aceitar o
  custo de WCU/latÃƒÂªncia adicional (jÃƒÂ¡ pago hoje pelos outros usos de `TransactWriteItems` no mesmo cÃƒÂ³digo).

**Enforcement estrutural, nÃƒÂ£o convenÃƒÂ§ÃƒÂ£o**: regra nova de `dependency-cruiser` (mesmo mecanismo que jÃƒÂ¡ proÃƒÂ­be
`shared` de importar `modules/**`) proibindo `new TransactWriteCommand(`/`new UpdateCommand(`/`new
PutCommand(`/`new DeleteCommand(` fora de `src/shared/dynamodb/fenced-write.ts` Ã¢â‚¬â€ fecha a classe de erro
"um call site novo esqueceu o fence", que foi exatamente a causa raiz do achado mais grave de D-063
(auditoria incompleta por pasta).

Isso fecha, sem tocar em nenhum dos 36 handlers individualmente: `items-handler`, `documents-handler` (a
persistÃƒÂªncia do `Document`, nÃƒÂ£o a emissÃƒÂ£o da URL presignada Ã¢â‚¬â€ ver Ã‚Â§4), `guest-documents-handler` (via
`DocumentSubmission`), `import-commit-handler` (mesmo com `systemContextFor()` sintÃƒÂ©tico Ã¢â‚¬â€ o fence nÃƒÂ£o
depende de `RequestContext`, ÃƒÂ© checado na escrita, nÃƒÂ£o na autorizaÃƒÂ§ÃƒÂ£o), toda a cadeia de lembretes/outbox,
`notification-router`/`email-delivery` (a gravaÃƒÂ§ÃƒÂ£o da tentativa, nÃƒÂ£o o `ses:SendEmail` em si Ã¢â‚¬â€ ver Ã‚Â§5),
`malware-result`/`upload-finalizer`, `upload-slot-reconciliation`, e as duas tasks de escrita da Step
Functions (`PersistExtractedFields`/`MarkPendingConfirmation`).

## 3. Fechando a ressurreiÃƒÂ§ÃƒÂ£o no resolver (defesa em profundidade, nÃƒÂ£o a defesa primÃƒÂ¡ria)

`RequestContextResolver.resolve()` ganha uma leitura do `TenantLifecycleRecord` **antes** de
`createProfileIfAbsent` (nÃƒÂ£o substitui a checagem de `UserProfile.status`, ÃƒÂ© adicional e anterior). Se
`status !== "ACTIVE"`, rejeita com 403 sem provisionar nada. Isso ÃƒÂ© defesa em profundidade Ã¢â‚¬â€ mesmo que
faltasse, o Ã‚Â§2 jÃƒÂ¡ impediria a escrita real do `User` recriado, porque `createProfileIfAbsent` tambÃƒÂ©m passa
pelo `fencedSingleWrite`. A checagem no resolver existe para dar um erro claro (403) em vez de deixar o
usuÃƒÂ¡rio achar que logou e sÃƒÂ³ descobrir a rejeiÃƒÂ§ÃƒÂ£o no primeiro POST.

## 4. URLs presignadas jÃƒÂ¡ emitidas (S3)

NÃƒÂ£o sÃƒÂ£o revogÃƒÂ¡veis diretamente. MitigaÃƒÂ§ÃƒÂ£o real, nÃƒÂ£o best-effort: a finalizaÃƒÂ§ÃƒÂ£o (`upload-finalizer-handler`/
`malware-result-handler`/equivalente guest) ÃƒÂ© exatamente o ponto que persiste `Document`/`DocumentSubmission`
via `fencedTransactWrite` (Ã‚Â§2) Ã¢â‚¬â€ se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a transaÃƒÂ§ÃƒÂ£o de finalizaÃƒÂ§ÃƒÂ£o falha
(`ConditionalCheckFailedException`), o objeto S3 fica ÃƒÂ³rfÃƒÂ£o na quarantine (limpo depois por
`document-purge-handler`/lifecycle, mecanismo jÃƒÂ¡ aprovado em D-061), mas **nenhuma linha DynamoDB nova
aparece**. Resultado: nenhuma ressurreiÃƒÂ§ÃƒÂ£o de dado consultÃƒÂ¡vel, sÃƒÂ³ lixo de storage jÃƒÂ¡ coberto por mecanismo
existente. TTL curto (600s) jÃƒÂ¡ limita a janela de exposiÃƒÂ§ÃƒÂ£o.

## 5. Efeitos externos irreversÃƒÂ­veis (SES, Bedrock, Textract) Ã¢â‚¬â€ protocolo de duas fases, nÃƒÂ£o checagem simples

Nenhum destes tem `ConditionExpression` atÃƒÂ´mica nativa. Desenho: **a decisÃƒÂ£o de disparar o efeito precisa
nascer de uma escrita fenced**, nÃƒÂ£o de uma leitura solta:

- `email-delivery-handler`: antes de `ses:SendEmail`, marca `NotificationAttempt.status = "SENDING"` via
  `fencedTransactWrite`. Se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a marcaÃƒÂ§ÃƒÂ£o falha e o handler nunca chama SES. Um
  e-mail cuja marcaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ commitou antes do `DELETING` ser escrito ainda sai (janela de milissegundos,
  inerente a qualquer sistema com efeito externo assÃƒÂ­ncrono Ã¢â‚¬â€ documentado como risco residual aceito, nÃƒÂ£o
  escondido).
- `document-chasing-dispatch-handler`: mesmo padrÃƒÂ£o Ã¢â‚¬â€ o novo `GuestTokenPointer` sÃƒÂ³ ÃƒÂ© criado via
  `fencedTransactWrite`; se falhar, nenhum token novo existe, nenhum e-mail ÃƒÂ© enviado.
- Bedrock/Textract (custo, nÃƒÂ£o dado): `BedrockExtractionTaskHandler`/`TextractTaskHandler` jÃƒÂ¡ gravam
  `TenantQuotaService`/`TextractJob` via os stores fenced do Ã‚Â§2 Ã¢â‚¬â€ se o tenant foi excluÃƒÂ­do entre o
  `RunTextract` e o `RunBedrock`, a chamada Bedrock ainda ocorre (custo real, nÃƒÂ£o dado), mas
  `PersistExtractedFields` falha depois. Custo residual pequeno e limitado, nÃƒÂ£o uma garantia de dado quebrada.

## 6. SequÃƒÂªncia da cascata (drenagem antes de apagar)

1. `fencedSingleWrite` grava `TenantLifecycleRecord.status = "DELETING"` (create condicional
   `attribute_not_exists(PK)` Ã¢â€ â€™ falha se uma cascata jÃƒÂ¡ estÃƒÂ¡ em curso, evita corrida de duplo-start).
2. Best-effort: `StopExecution` em toda execuÃƒÂ§ÃƒÂ£o Step Functions ativa cujo payload referencia o tenant
   (via tag/GSI jÃƒÂ¡ existente do desenho de extraÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ reduz custo Bedrock/Textract desperdiÃƒÂ§ado, nÃƒÂ£o ÃƒÂ©
   requisito de correÃƒÂ§ÃƒÂ£o (o fence do Ã‚Â§2/Ã‚Â§5 jÃƒÂ¡ garante correÃƒÂ§ÃƒÂ£o sem isto).
3. Roda a descoberta+exclusÃƒÂ£o jÃƒÂ¡ aprovada (Scan + taxonomia de ~40 `entityType`, convergÃƒÂªncia por re-Scan),
   reusando `DocumentPurgeWorker`/GSI6 para `Document` (D-061) Ã¢â‚¬â€ mecanismo inalterado desta rodada.
4. `TenantLifecycleRecord` **nunca** ÃƒÂ© alvo do Scan (exclusÃƒÂ£o explÃƒÂ­cita por `entityType`, mesma disciplina jÃƒÂ¡
   usada para `bff-session-table`).
5. ConvergÃƒÂªncia confirmada (zero itens restantes) Ã¢â€ â€™ `fencedSingleWrite` flip para `status = "DELETED"`
   (permanente; nenhum cÃƒÂ³digo reabre um tenant `DELETED` Ã¢â‚¬â€ fora de escopo desta feature).

## 7. O que este desenho NÃƒÆ’O resolve (fora de escopo explÃƒÂ­cito, registrar e nÃƒÂ£o mascarar)

- Rotas HTTP/confirmaÃƒÂ§ÃƒÂ£o/exportaÃƒÂ§ÃƒÂ£o de DSR completo Ã¢â‚¬â€ jÃƒÂ¡ fora de escopo desde D-062, inalterado.
- `PolicyRef` sem `tenantId` (achado independente de D-062) Ã¢â‚¬â€ correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica separada.
- `tenantId` (MVP `tenantId=userId`) Ã¢â€°Â  Cognito `sub` Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant
  precisa resolver isso ÃƒÂ  parte; nÃƒÂ£o bloqueia este desenho porque a cascata nunca chama a API do Cognito.

## 8. Por que isto nÃƒÂ£o repete os erros de D-062/D-063

- O fence nÃƒÂ£o ÃƒÂ© a mesma linha que a cascata apaga (Ã‚Â§1, `TenantLifecycleRecord` explicitamente excluÃƒÂ­do do
  Scan) Ã¢â‚¬â€ resolve o achado mais grave de D-063.
- A checagem ÃƒÂ© acoplada (`ConditionCheck` na mesma transaÃƒÂ§ÃƒÂ£o), nunca uma leitura solta antes de agir Ã¢â‚¬â€
  resolve o segundo achado de D-063 (TOCTOU).
- A cobertura nÃƒÂ£o depende de enumerar 36 handlers corretamente Ã¢â‚¬â€ depende de 19 arquivos de persistÃƒÂªncia jÃƒÂ¡
  conhecidos + uma regra de lint estrutural que impede um call site futuro de escapar do fence. Reduz a
  classe de erro "auditoria incompleta" de "confiar em uma lista manual" para "confiar numa regra estÃƒÂ¡tica
  verificÃƒÂ¡vel por `dependency-cruiser`" Ã¢â‚¬â€ resolve a causa raiz comum ÃƒÂ s duas rodadas reprovadas.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg --files src | rg 'guest.*submission|submission.*service'; rg -n --glob '*.ts' 'new\\s+(TransactWriteCommand|UpdateCommand|PutCommand|DeleteCommand)|\\.send\\s*\\(\\s*new\\s+(TransactWriteCommand|UpdateCommand|PutCommand|DeleteCommand)' src; rg -n --glob '*.ts' 'TransactWriteItems|TransactItems\\s*:' src test; rg -n 'dependency-cruiser|forbidden|no-restricted' .dependency-cruiser* package.json" in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-28T21:07:34.023633Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.3 seconds
Output:
src\modules\subject\application\guest-submission-service.ts
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\persistence\dynamodb-session-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\bff\persistence\dynamodb-session-store.ts:49:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:33:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:49:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\identity\persistence\dynamodb-identity-store.ts:61:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:33:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:48:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:21:        new UpdateCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:39:        new UpdateCommand({
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\document\persistence\dynamodb-document-store.ts:35:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\subject\persistence\dynamodb-subject-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\subject\persistence\dynamodb-subject-store.ts:49:        new PutCommand({
src\modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\persistence\dynamodb-import-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:24:        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:50:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:45:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:28:        new PutCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:43:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:37:        new PutCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:52:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
test\unit\outbox.test.ts:49:  it("appends a conditional Put entry so the caller can commit it in the same TransactWriteItems as the aggregate write", () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:6: * TransactWriteItems/Query semantics that the fakes only approximate.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:65:  it("materialize -> producer claim (real GSI3 query + real TransactWriteItems) -> durable outbox -> relay -> dispatch -> exactly one NotificationIntent", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:92:    // Real GSI3 Query against DynamoDB Local, real conditional TransactWriteItems claim,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:152:    // Real dispatch: CLAIMED -> TRIGGERED + NotificationIntent, real TransactWriteItems.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:175:  it("real conditional Query on GSI6 finds an expired claim and reconciliation reverts it via real TransactWriteItems", async () => {
src\workers\reminder-dispatch\dispatch.ts:13: * On success, everything commits in ONE TransactWriteItems (Â§9.4: "corrige a lacuna de
src\workers\document-purge\purge.ts:23: *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
src\modules\subject\ports\subject-store.ts:5: * agregado + contador + audit numa Ãºnica TransactWriteItems (nunca separado, ver
src\shared\outbox\outbox.ts:4: * TransactWriteItems `Put` entry so callers can append it atomically alongside the
src\shared\outbox\outbox.ts:116: * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
src\modules\subject\persistence\dynamodb-subject-store.ts:71:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\bff\application\bff-auth-service.ts:8: * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src\shared\idempotency\idempotency.ts:79: * already has one for its own aggregate writes) - a single-entry conditional TransactWriteItems,
src\modules\subject\application\subject-service.ts:7: * mutaÃ§Ã£o muda o nÃºmero de subjects ACTIVE) em uma Ãºnica TransactWriteItems.
src\shared\dynamodb\occ.ts:138: * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
src\shared\dynamodb\occ.ts:221: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src\shared\dynamodb\occ.ts:227: * re-asserted atomically inside the same TransactWriteItems as the write it gates).
src\shared\dynamodb\occ.ts:299:/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
src\modules\expiration\domain\audit-event.ts:5: * every mutation appends its audit record inside the SAME TransactWriteItems as the
src\modules\expiration\domain\audit-event.ts:80:/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction (accepts the full Put|Update union so callers don't need an unsafe cast for a mixed transaction). */
src\modules\expiration\ports\expiration-store.ts:7: * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
src\modules\expiration\application\expiration-service.ts:8: *     changes dueDate) in a single TransactWriteItems - implementing data-model.md Â§5:
src\modules\extraction\application\run-extraction-validation.ts:212: * all-or-nothing `TransactWriteItems` as the field rows, whose per-row `attribute_not_exists`
src\modules\import\persistence\dynamodb-import-store.ts:51:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-store.ts:55:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:67:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\notification\persistence\dynamodb-notification-store.ts:64:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\expiration\persistence\dynamodb-expiration-store.ts:3: * TransactWriteItems parameter shapes already produced by shared/dynamodb/occ.ts's
src\modules\expiration\persistence\dynamodb-expiration-store.ts:60:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\extraction\application\confirm-reject-field.ts:5: * `TransactWriteItems`) â€” `confirm` is a 4-way OCC (`ExpirationItem`/`Document`/`ExtractionRun`/
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\extraction\ports\extracted-field-store.ts:9: * terminal value must happen atomically, in the SAME `TransactWriteItems` that re-asserts the
src\modules\extraction\ports\extracted-field-store.ts:33:   * item is updated with OCC in the SAME `TransactWriteItems` as the field rows and the run
src\modules\extraction\ports\extracted-field-store.ts:52:   * `TransactWriteItems` is all-or-nothing), transitions the run to a terminal status, and
src\modules\extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (Â§1.7): one `TransactWriteItems` â€”
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:2: * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\application\reminder-policy-service.ts:9: * the SAME TransactWriteItems as the policy row itself:
src\modules\reminder\application\reminder-materializer.ts:265:   * `shouldCancel`, each in its own TransactWriteItems ALSO containing a ConditionCheck on
src\modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
test\unit\extraction\dynamodb-extracted-field-store.test.ts:27:    async send(command: { input: { TransactItems: CapturedTransactItem[] } }) {
test\unit\extraction\run-extraction-validation.test.ts:282:      // TransactWriteItems, so the dueDate write can never be applied a second time.
src\modules\notification\application\notification-router-workflow.ts:4: * into a SINGLE TransactWriteItems - the router never issues more than one transactional
src\modules\notification\application\notification-router.ts:4: * decide what happens, then translates the result into a single TransactWriteItems.
package.json:15:    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
package.json:32:    "dependency-cruiser": "17.4.3",
rg: .dependency-cruiser*: A sintaxe do nome do arquivo, do nome do diretÃ³rio ou do rÃ³tulo do volume estÃ¡ incorreta. (os error 123)

 exited 1 in 331ms:
src\modules\subject\application\guest-submission-service.ts
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\persistence\dynamodb-session-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\bff\persistence\dynamodb-session-store.ts:49:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:33:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:49:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\identity\persistence\dynamodb-identity-store.ts:61:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:33:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:48:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:21:        new UpdateCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:39:        new UpdateCommand({
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\document\persistence\dynamodb-document-store.ts:35:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\subject\persistence\dynamodb-subject-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\subject\persistence\dynamodb-subject-store.ts:49:        new PutCommand({
src\modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\persistence\dynamodb-import-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:24:        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:50:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:45:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:28:        new PutCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:43:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:37:        new PutCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:52:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
test\unit\outbox.test.ts:49:  it("appends a conditional Put entry so the caller can commit it in the same TransactWriteItems as the aggregate write", () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:6: * TransactWriteItems/Query semantics that the fakes only approximate.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:65:  it("materialize -> producer claim (real GSI3 query + real TransactWriteItems) -> durable outbox -> relay -> dispatch -> exactly one NotificationIntent", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:92:    // Real GSI3 Query against DynamoDB Local, real conditional TransactWriteItems claim,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:152:    // Real dispatch: CLAIMED -> TRIGGERED + NotificationIntent, real TransactWriteItems.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:175:  it("real conditional Query on GSI6 finds an expired claim and reconciliation reverts it via real TransactWriteItems", async () => {
src\workers\reminder-dispatch\dispatch.ts:13: * On success, everything commits in ONE TransactWriteItems (Â§9.4: "corrige a lacuna de
src\workers\document-purge\purge.ts:23: *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
src\modules\subject\ports\subject-store.ts:5: * agregado + contador + audit numa Ãºnica TransactWriteItems (nunca separado, ver
src\shared\outbox\outbox.ts:4: * TransactWriteItems `Put` entry so callers can append it atomically alongside the
src\shared\outbox\outbox.ts:116: * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
src\modules\subject\persistence\dynamodb-subject-store.ts:71:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\bff\application\bff-auth-service.ts:8: * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src\shared\idempotency\idempotency.ts:79: * already has one for its own aggregate writes) - a single-entry conditional TransactWriteItems,
src\modules\subject\application\subject-service.ts:7: * mutaÃ§Ã£o muda o nÃºmero de subjects ACTIVE) em uma Ãºnica TransactWriteItems.
src\shared\dynamodb\occ.ts:138: * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
src\shared\dynamodb\occ.ts:221: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src\shared\dynamodb\occ.ts:227: * re-asserted atomically inside the same TransactWriteItems as the write it gates).
src\shared\dynamodb\occ.ts:299:/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
src\modules\expiration\domain\audit-event.ts:5: * every mutation appends its audit record inside the SAME TransactWriteItems as the
src\modules\expiration\domain\audit-event.ts:80:/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction (accepts the full Put|Update union so callers don't need an unsafe cast for a mixed transaction). */
src\modules\expiration\ports\expiration-store.ts:7: * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
src\modules\expiration\application\expiration-service.ts:8: *     changes dueDate) in a single TransactWriteItems - implementing data-model.md Â§5:
src\modules\extraction\application\run-extraction-validation.ts:212: * all-or-nothing `TransactWriteItems` as the field rows, whose per-row `attribute_not_exists`
src\modules\import\persistence\dynamodb-import-store.ts:51:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-store.ts:55:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:67:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\notification\persistence\dynamodb-notification-store.ts:64:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\expiration\persistence\dynamodb-expiration-store.ts:3: * TransactWriteItems parameter shapes already produced by shared/dynamodb/occ.ts's
src\modules\expiration\persistence\dynamodb-expiration-store.ts:60:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\extraction\application\confirm-reject-field.ts:5: * `TransactWriteItems`) â€” `confirm` is a 4-way OCC (`ExpirationItem`/`Document`/`ExtractionRun`/
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\extraction\ports\extracted-field-store.ts:9: * terminal value must happen atomically, in the SAME `TransactWriteItems` that re-asserts the
src\modules\extraction\ports\extracted-field-store.ts:33:   * item is updated with OCC in the SAME `TransactWriteItems` as the field rows and the run
src\modules\extraction\ports\extracted-field-store.ts:52:   * `TransactWriteItems` is all-or-nothing), transitions the run to a terminal status, and
src\modules\extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (Â§1.7): one `TransactWriteItems` â€”
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:2: * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\application\reminder-policy-service.ts:9: * the SAME TransactWriteItems as the policy row itself:
src\modules\reminder\application\reminder-materializer.ts:265:   * `shouldCancel`, each in its own TransactWriteItems ALSO containing a ConditionCheck on
src\modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
test\unit\extraction\dynamodb-extracted-field-store.test.ts:27:    async send(command: { input: { TransactItems: CapturedTransactItem[] } }) {
test\unit\extraction\run-extraction-validation.test.ts:282:      // TransactWriteItems, so the dueDate write can never be applied a second time.
src\modules\notification\application\notification-router-workflow.ts:4: * into a SINGLE TransactWriteItems - the router never issues more than one transactional
src\modules\notification\application\notification-router.ts:4: * decide what happens, then translates the result into a single TransactWriteItems.
package.json:15:    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
package.json:32:    "dependency-cruiser": "17.4.3",
rg: .dependency-cruiser*: A sintaxe do nome do arquivo, do nome do diretÃ³rio ou do rÃ³tulo do volume estÃ¡ incorreta. (os error 123)

codex
O primeiro grep jÃ¡ contradiz um ponto factual: as construÃ§Ãµes diretas encontradas estÃ£o em 14 arquivos, nÃ£o numa uniÃ£o claramente definida de â€œ8+11 = 19â€; e hÃ¡ stores em `shared/outbox` e `bff` que a redaÃ§Ã£o mistura com â€œdado de tenantâ€ sem explicitar exceÃ§Ãµes. Agora vou ampliar para todos os comandos mutÃ¡veis do SDK (`BatchWrite`, low-level client, transaÃ§Ãµes montadas indiretamente), ler cada store relevante e conferir a capacidade real do dependency-cruiser.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/subject/application/guest-submission-service.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 164ms:
/**
 * GuestSubmissionService Ã¢â‚¬â€ lado do convidado (sem conta) de 04-domain-model-guest-upload.md
 * (D-037). NUNCA passa por `RequestContext`/`authorize()` Ã¢â‚¬â€ o convidado ÃƒÂ© validado sÃƒÂ³ pelo
 * token. Toda falha de validaÃƒÂ§ÃƒÂ£o retorna o MESMO erro genÃƒÂ©rico (`GuestTokenInvalidError`),
 * SEM detalhe algum no corpo da resposta (anti-enumeration Ã¢â‚¬â€ `AppError.toJSON()` inclui
 * `details` na resposta HTTP, entÃƒÂ£o este mÃƒÂ³dulo nunca populates `details` com o motivo real).
 */
import { randomUUID } from "node:crypto";
import { AppError, ValidationError } from "../../../shared/errors/app-error.js";
import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
import { parseGuestToken, secretMatches, guestTokenPointerKey, hmacGuestTokenCrypto, type GuestTokenPointer } from "../domain/guest-token.js";
import { documentRequestKey, type DocumentRequest } from "../domain/document-request.js";
import { requirementAssignmentKey, type RequirementAssignment } from "../domain/requirement-assignment.js";
import { documentSubmissionKey, type DocumentSubmission } from "../domain/document-submission.js";
import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
import type { SubjectIdGenerator } from "./id-generator.js";
import type { GuestRateLimiter } from "./guest-rate-limiter.js";
import { MAX_UPLOAD_BYTES } from "../../document/application/upload-validation.js";
import type { UploadUrlSigner } from "../../document/ports/upload-url-signer.js";
import { sanitizeTenantText } from "../../notification/providers/email-templates.js";

const ALLOWED_MEDIA_TYPES: ReadonlySet<string> = new Set(["application/pdf", "image/jpeg", "image/png"]);
const PRESIGN_TTL_SECONDS = 600;

/** Erro ÃƒÂºnico e genÃƒÂ©rico para toda falha de validaÃƒÂ§ÃƒÂ£o de guest token Ã¢â‚¬â€ NUNCA carrega `details`
 * (categoria AUTH -> 401, mesmo em caminhos completamente distintos: token malformado,
 * inexistente, secret errado, expirado, revogado). */
export class GuestTokenInvalidError extends AppError {
  constructor() {
    super({ code: "GUEST_TOKEN_INVALID", category: "AUTH", message: "Invalid or expired link.", retryable: false });
    this.name = "GuestTokenInvalidError";
  }
}

export interface GuestRequestInfo {
  requirementName: string;
  deadline?: string;
  allowedMediaTypes: string[];
  maxUploadBytes: number;
  /** W5-01/GTR-01 (`decisions-log.md` D-060): identity of whoever created the request, shown
   * to the guest so an unauthenticated document upload no longer arrives from an anonymous
   * source. Always present (never `undefined`) - falls back to a generic, honest placeholder
   * when the tenant never captured `UserProfile.requesterDisplayName` (never inferred, e.g.
   * from an e-mail domain). */
  requesterDisplayName: string;
}

const REQUESTER_NAME_FALLBACK = "Solicitante nÃƒÂ£o identificado";

export interface StartGuestSubmissionInput {
  fileName: string;
  mediaType: string;
  contentLength: number;
  checksumSha256: string;
}

export interface StartGuestSubmissionResult {
  submissionId: string;
  uploadUrl: string;
  requiredHeaders: Record<string, string>;
  expiresAt: string;
}

export interface GuestSubmissionServiceDeps {
  store: SubjectStore;
  tableName: string;
  quarantineBucket: string;
  ids: SubjectIdGenerator;
  signer: UploadUrlSigner;
  rateLimiter: GuestRateLimiter;
  guestTokenPepper: string;
  /** W5-01/GTR-01 (D-060): resolves `UserProfile.requesterDisplayName` for the request's
   * creator (`DocumentRequest.requestedByUserId`) - same injected-resolver pattern as
   * `resolveInternalUserEmail` in `document-chasing-dispatch`'s dispatch.ts, deliberately NOT a
   * direct identity-module import (composition-root wiring, not a cross-module dependency). */
  resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
  now?: () => string;
}

interface ResolvedToken {
  pointer: GuestTokenPointer;
  request: DocumentRequest;
}

export class GuestSubmissionService {
  private readonly store: SubjectStore;
  private readonly tableName: string;
  private readonly quarantineBucket: string;
  private readonly ids: SubjectIdGenerator;
  private readonly signer: UploadUrlSigner;
  private readonly rateLimiter: GuestRateLimiter;
  private readonly pepper: string;
  private readonly resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
  private readonly now: () => string;

  constructor(deps: GuestSubmissionServiceDeps) {
    this.store = deps.store;
    this.tableName = deps.tableName;
    this.quarantineBucket = deps.quarantineBucket;
    this.ids = deps.ids;
    this.signer = deps.signer;
    this.rateLimiter = deps.rateLimiter;
    this.pepper = deps.guestTokenPepper;
    this.resolveRequesterDisplayName = deps.resolveRequesterDisplayName;
    this.now = deps.now ?? (() => new Date().toISOString());
  }

  async getRequestInfo(rawToken: string): Promise<GuestRequestInfo> {
    const resolved = await this.resolveToken(rawToken);
    const assignment = await this.store.get<RequirementAssignment>(
      requirementAssignmentKey(resolved.request.tenantId, resolved.request.subjectId, resolved.request.assignmentId),
    );
    if (!assignment || assignment.deletedAt) throw new GuestTokenInvalidError();

    if (resolved.request.status === "REQUESTED") {
      await this.markOpened(resolved.request);
    }

    const resolvedRequesterName = await this.resolveRequesterDisplayName?.({
      tenantId: resolved.request.tenantId,
      userId: resolved.request.requestedByUserId,
    });

    return {
      requirementName: assignment.requirementName,
      deadline: resolved.request.deadline,
      allowedMediaTypes: [...ALLOWED_MEDIA_TYPES],
      maxUploadBytes: MAX_UPLOAD_BYTES,
      requesterDisplayName: sanitizeTenantText(resolvedRequesterName, REQUESTER_NAME_FALLBACK),
    };
  }

  async startSubmission(rawToken: string, input: StartGuestSubmissionInput): Promise<StartGuestSubmissionResult> {
    const resolved = await this.resolveToken(rawToken);

    if (!ALLOWED_MEDIA_TYPES.has(input.mediaType)) {
      throw new ValidationError("Unsupported media type.", { mediaType: input.mediaType });
    }
    if (input.contentLength <= 0 || input.contentLength > MAX_UPLOAD_BYTES) {
      throw new ValidationError("contentLength must be between 1 and 10MiB.");
    }
    if (!/^[a-f0-9]{64}$/i.test(input.checksumSha256)) {
      throw new ValidationError("checksumSha256 must be a 64-character hex SHA-256 digest.");
    }

    const { tenantId, subjectId, assignmentId, documentRequestId } = resolved.pointer;
    const submissionId = this.ids.newSubmissionId();
    const now = this.now();
    const expiresAt = new Date(Date.parse(now) + PRESIGN_TTL_SECONDS * 1000).toISOString();
    // Formato canÃƒÂ´nico de submission-anchored quarantine key (submission-quarantine-key.ts) -
    // nunca colide com o formato item-anchored existente de M6.
    const quarantineKey = `tenant/${tenantId}/subject/${subjectId}/assignment/${assignmentId}/submission/${submissionId}/document/${submissionId}/slot/${submissionId}/${randomUUID()}`;

    const submission: DocumentSubmission = {
      ...documentSubmissionKey(tenantId, subjectId, assignmentId, submissionId),
      entityType: "DocumentSubmission",
      submissionId,
      tenantId,
      subjectId,
      assignmentId,
      documentRequestId,
      fileName: input.fileName,
      mediaType: input.mediaType,
      contentLength: input.contentLength,
      checksumSha256: input.checksumSha256,
      status: "PENDING_UPLOAD",
      quarantineObject: { bucket: this.quarantineBucket, key: quarantineKey, versionId: "" },
      createdAt: now,
      updatedAt: now,
      version: 1,
    };

    const entries: TransactWriteEntry[] = [
      { Put: buildVersionedCreate(this.tableName, submission as unknown as Record<string, unknown> & { PK: string; SK: string }) },
      {
        Update: buildVersionedUpdate({
          tableName: this.tableName,
          key: documentRequestKey(tenantId, subjectId, assignmentId, documentRequestId),
          tenantId,
          expectedVersion: resolved.request.version,
          set: {
            status: "SUBMITTED",
            lastSubmissionId: submissionId,
            submissionCount: resolved.request.submissionCount + 1,
          },
        }),
      },
    ];

    try {
      await this.store.transactWrite(entries);
    } catch (err) {
      // Corrida real (ex. dois envios quase simultÃƒÂ¢neos do mesmo link) - convidado sÃƒÂ³ vÃƒÂª o
      // erro genÃƒÂ©rico, nunca detalhe de qual condiÃƒÂ§ÃƒÂ£o falhou.
      if (isTransactionCanceled(err)) throw new GuestTokenInvalidError();
      throw err;
    }

    const presigned = await this.signer.presignUpload({
      bucket: this.quarantineBucket,
      key: quarantineKey,
      mediaType: input.mediaType,
      contentLength: input.contentLength,
      checksumSha256: input.checksumSha256,
      metadata: { submissionId, tenantId },
      expiresInSeconds: PRESIGN_TTL_SECONDS,
    });

    return { submissionId, uploadUrl: presigned.uploadUrl, requiredHeaders: presigned.requiredHeaders, expiresAt };
  }

  /** Valida o token de ponta a ponta: parse estrutural -> lookup por selectorHash -> comparaÃƒÂ§ÃƒÂ£o
   * `timingSafeEqual` do secret -> expiraÃƒÂ§ÃƒÂ£o/revogaÃƒÂ§ÃƒÂ£o -> rate limit por token. Qualquer falha
   * em qualquer etapa produz o MESMO erro genÃƒÂ©rico. */
  private async resolveToken(rawToken: string): Promise<ResolvedToken> {
    const parsed = parseGuestToken(rawToken);
    if (!parsed) throw new GuestTokenInvalidError();

    const selectorHash = hmacGuestTokenCrypto.hash(this.pepper, parsed.selector);

    // Rate limit consumido ANTES do lookup do ponteiro, por selectorHash (calculÃƒÂ¡vel mesmo
    // que o ponteiro nÃƒÂ£o exista) Ã¢â‚¬â€ achado real de revisÃƒÂ£o adversarial (Codex): consumir sÃƒÂ³
    // depois de confirmar que o ponteiro existe criava um oracle de enumeraÃƒÂ§ÃƒÂ£o (selector
    // real esgota o limite e vira 429 QuotaExceededError; selector inexistente nunca chega
    // lÃƒÂ¡ e sempre dÃƒÂ¡ 401). Qualquer estouro de rate limit tambÃƒÂ©m vira o MESMO erro genÃƒÂ©rico
    // abaixo, nunca 429 diferenciado nesta superfÃƒÂ­cie pÃƒÂºblica.
    try {
      await this.rateLimiter.consume({ selectorHash, limit: 30, windowSeconds: 60 });
    } catch {
      throw new GuestTokenInvalidError();
    }

    const pointer = await this.store.get<GuestTokenPointer>(guestTokenPointerKey(selectorHash));

    // Caminho dummy anti-timing (achado real de revisÃƒÂ£o adversarial): mesmo quando o ponteiro
    // nÃƒÂ£o existe, ainda computamos um hash e uma comparaÃƒÂ§ÃƒÂ£o timingSafeEqual equivalente, para
    // que o tempo de resposta nÃƒÂ£o dependa de o selector existir ou nÃƒÂ£o. O hash dummy nunca
    // pode colidir com um secretHash real (prefixo fixo que issueGuestToken() nunca produz).
    const targetSecretHash = pointer?.secretHash ?? hmacGuestTokenCrypto.hash(this.pepper, `dummy:${selectorHash}`);
    const secretOk = secretMatches(this.pepper, parsed.secret, targetSecretHash);
    if (!pointer || !secretOk) throw new GuestTokenInvalidError();

    if (pointer.revokedAt) throw new GuestTokenInvalidError();
    if (pointer.expiresAt < this.now()) throw new GuestTokenInvalidError();

    const request = await this.store.get<DocumentRequest>(documentRequestKey(pointer.tenantId, pointer.subjectId, pointer.assignmentId, pointer.documentRequestId));
    if (!request || request.status === "CANCELLED" || request.status === "REVOKED" || request.status === "EXPIRED") {
      throw new GuestTokenInvalidError();
    }
    // D-037: TTL ÃƒÂ© "14 dias OU deadline, o que vier primeiro" Ã¢â‚¬â€ expiresAt do ponteiro jÃƒÂ¡
    // reflete isso na criaÃƒÂ§ÃƒÂ£o (document-request-service.ts), mas revalida aqui tambÃƒÂ©m caso
    // um deadline seja editado no futuro sem reemitir o token.
    if (request.deadline && request.deadline < this.now()) throw new GuestTokenInvalidError();

    return { pointer, request };
  }

  private async markOpened(request: DocumentRequest): Promise<void> {
    try {
      await this.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: this.tableName,
            key: documentRequestKey(request.tenantId, request.subjectId, request.assignmentId, request.documentRequestId),
            tenantId: request.tenantId,
            expectedVersion: request.version,
            set: { status: "OPENED", lastOpenedAt: this.now() },
          }),
        },
      ]);
    } catch (err) {
      // Best-effort - corrida de leitura concorrente (2 aberturas quase simultÃƒÂ¢neas do mesmo
      // link) nunca deve impedir o convidado de ver as informaÃƒÂ§ÃƒÂµes do pedido.
      if (!isTransactionCanceled(err)) throw err;
    }
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n --glob '*.ts' '@aws-sdk/(lib-dynamodb|client-dynamodb)' src; rg -n --glob '*.ts' 'new\\s+[A-Za-z0-9_]*(Write|Update|Put|Delete|Transact|Execute|Batch)[A-Za-z0-9_]*Command\\s*\\(' src; Get-Content -Raw '.dependency-cruiser.cjs'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 229ms:
src\modules\subject\persistence\dynamodb-subject-store.ts:6:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\subject\persistence\dynamodb-subject-store.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\import\persistence\dynamodb-import-store.ts:6:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\import\persistence\dynamodb-import-store.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\extraction.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\expiration.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:14:import { DynamoDBClient } from "@aws-sdk/client-dynamodb";
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:15:import { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\document.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\expiration\persistence\dynamodb-expiration-store.ts:8:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\expiration\persistence\dynamodb-expiration-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\bff.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\import.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\subject.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\subject.ts:22:import { GetCommand } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\identity.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\notification.ts:3:import { GetCommand, type DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\bff\persistence\dynamodb-session-store.ts:6:import { GetCommand, PutCommand } from "@aws-sdk/lib-dynamodb";
src\modules\bff\persistence\dynamodb-session-store.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\reminder.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\workers\document-purge\dynamodb-document-purge-candidate-source.ts:9:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
src\workers\document-purge\dynamodb-document-purge-candidate-source.ts:10:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-candidate-source.ts:5:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-candidate-source.ts:6:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-store.ts:3:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-store.ts:4:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-recipient-resolver.ts:16:import { GetCommand } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-recipient-resolver.ts:17:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-notification-store.ts:12:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-notification-store.ts:13:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reminder-store.ts:3:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reminder-store.ts:4:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\shared\dynamodb\occ.ts:5: * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:8:import { GetCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\shared\dynamodb\client.ts:7:import { DynamoDBClient } from "@aws-sdk/client-dynamodb";
src\shared\dynamodb\client.ts:8:import { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:9:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:10:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:4:import { QueryCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:5:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\composition\extraction.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\identity\persistence\dynamodb-identity-store.ts:8:import { GetCommand, PutCommand } from "@aws-sdk/lib-dynamodb";
src\modules\identity\persistence\dynamodb-identity-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:8:import { GetCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:10:import type { TransactWriteCommandInput } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:3:import { GetCommand, PutCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:4:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:4:import { PutCommand, GetCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:5:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\subject\persistence\dynamodb-subject-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\subject\persistence\dynamodb-subject-store.ts:49:        new PutCommand({
src\modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:33:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:48:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src\modules\import\persistence\s3-import-object-store.ts:23:    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\persistence\dynamodb-import-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:28:        new PutCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:43:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:21:        new UpdateCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:39:        new UpdateCommand({
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:37:        new PutCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:52:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
src\modules\document\persistence\s3-upload-url-signer.ts:15:    const command = new PutObjectCommand({
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\document\persistence\dynamodb-document-store.ts:35:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\document\persistence\s3-document-object-store.ts:49:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\persistence\dynamodb-session-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\bff\persistence\dynamodb-session-store.ts:49:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:33:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:49:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\identity\persistence\dynamodb-identity-store.ts:61:        new PutCommand({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:24:        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:50:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:45:        new UpdateCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:17:      new PutObjectCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:38:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
/**
 * Real dependency-graph boundary enforcement Ã¢â‚¬â€ Engineering Maturity Review red team finding
 * (2026-08-19, checkpoint-12-redteam): the ESLint `no-restricted-imports` rule in
 * .eslintrc.cjs only catches a DIRECT forbidden import; it cannot see a transitive path
 * (e.g. domain/a.ts -> domain/bridge.ts -> application/service.ts), dynamic import(), or a
 * barrel file re-exporting a forbidden layer. dependency-cruiser resolves the actual module
 * graph (including transitively), so it closes that gap instead of just restating it.
 *
 * This is deliberately narrow Ã¢â‚¬â€ three rules, matching exactly the three boundaries already
 * enforced (imperfectly) by ESLint. Not a general-purpose architecture linter; adding more
 * rules here should require the same justification as any other engineering decision
 * (docs/engineering/decisions-log.md), not accumulate by default.
 */
module.exports = {
  forbidden: [
    {
      name: "domain-must-not-reach-infra",
      severity: "error",
      comment: "src/modules/*/domain/** must not depend on infra/, directly or transitively.",
      from: { path: "^src/modules/[^/]+/domain/" },
      to: { path: "^infra/" },
    },
    {
      name: "domain-must-not-reach-aws-sdk",
      severity: "error",
      comment: "src/modules/*/domain/** must stay AWS-SDK-agnostic, directly or transitively.",
      from: { path: "^src/modules/[^/]+/domain/" },
      to: { path: "^(aws-sdk|@aws-sdk/)" },
    },
    {
      name: "domain-must-not-reach-application-layers",
      severity: "error",
      comment:
        "src/modules/*/domain/** must not depend on ANY module's application/ports/http/persistence layers - not a sibling module's, and not even its own (domain is the innermost layer; application depends on domain, never the reverse). Matches .eslintrc.cjs's no-restricted-imports pattern exactly (that rule's glob is also module-name-agnostic) - direct or transitive.",
      from: { path: "^src/modules/[^/]+/domain/" },
      to: { path: "^src/modules/[^/]+/(application|ports|http|persistence)/" },
    },
    {
      name: "shared-must-not-reach-modules",
      severity: "error",
      comment:
        "src/shared/** is the lower-level layer every module's domain ultimately depends on (transitively) - it must never depend back on src/modules/**, or the dependency graph inverts. Added when security-audit.ts (src/shared/observability/) was tempted to import AuthorizationDeniedError's closed union types from modules/identity/domain/authorization.ts for a stricter signature - decoupled instead (reason/action typed as string there; callers already hold the closed-union value). This rule prevents that specific mistake from being reintroduced silently.",
      from: { path: "^src/shared/" },
      to: { path: "^src/modules/" },
    },
  ],
  options: {
    tsPreCompilationDeps: true,
    tsConfig: { fileName: "tsconfig.json" },
    enhancedResolveOptions: {
      exportsFields: ["exports"],
      conditionNames: ["import", "require", "node", "default"],
    },
    reporterOptions: {
      text: { highlightFocused: true },
    },
  },
};


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/identity/persistence/identity-mapping-repository.ts'; Get-Content -Raw 'src/modules/identity/persistence/user-repository.ts'; Get-Content -Raw 'src/modules/identity/persistence/dynamodb-identity-store.ts'; Get-Content -Raw 'src/modules/extraction/persistence/dynamodb-extracted-field-store.ts'; rg -n 'MAX_|maxItems|max.*field|fields\\.length|TransactItems|transactWrite\\(' src/modules src/workers | rg '100|99|field|Transact|transact'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 260ms:
/**
 * IdentityMapping repository Ã¢â‚¬â€ data-model.md Ã‚Â§2/Ã‚Â§"IdentityMapping" +
 * implementation-blueprint.md Ã‚Â§23.1/Ã‚Â§7.3: PK=IDENTITY#cognitoSub#<sub>, SK=MAP, created
 * atomically (ConditionExpression attribute_not_exists(PK)) on first login. This is one
 * of the two documented exceptions to the "every key starts with TENANT#tenantId" rule
 * (the other is GSI3) Ã¢â‚¬â€ the lookup happens *before* tenantId is known.
 */
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
import { InternalError } from "../../../shared/errors/app-error.js";

export interface IdentityMapping {
  PK: string;
  SK: "MAP";
  entityType: "IdentityMapping";
  cognitoSub: string;
  userId: string;
  tenantId: string;
  createdAt: string;
}

export function identityMappingKey(cognitoSub: string): EntityKey {
  return { PK: `IDENTITY#cognitoSub#${cognitoSub}`, SK: "MAP" };
}

export class IdentityMappingRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async find(cognitoSub: string): Promise<IdentityMapping | undefined> {
    return this.store.get<IdentityMapping>(identityMappingKey(cognitoSub));
  }

  /**
   * Atomically creates the mapping on first login. MVP: tenantId=userId (data-model.md
   * Ã‚Â§7.3 "No MVP, tenantId=userId, mas o cÃƒÂ³digo recebe ambos como conceitos separados"),
   * so newUserId is used for both Ã¢â‚¬â€ callers must never derive one from the other outside
   * this function once Organizations (FUT-001) exist.
   *
   * Idempotent under races: if two concurrent first-logins for the same sub both call
   * this, exactly one PutItem wins (attribute_not_exists(PK)); the loser re-reads and
   * returns the winner's mapping instead of erroring, so first-login is safe to retry.
   */
  async findOrCreate(cognitoSub: string, newUserId: string, newTenantId: string): Promise<IdentityMapping> {
    const existing = await this.find(cognitoSub);
    if (existing) {
      return existing;
    }

    const key = identityMappingKey(cognitoSub);
    const candidate: IdentityMapping = {
      ...key,
      SK: "MAP",
      entityType: "IdentityMapping",
      cognitoSub,
      userId: newUserId,
      tenantId: newTenantId,
      createdAt: this.now(),
    };

    const created = await this.store.putIfAbsent(candidate);
    if (created) {
      return candidate;
    }

    // Lost the race: someone else created it first-login-atomically between our
    // find() and putIfAbsent(). Re-read; if it's still missing something is badly
    // wrong (a delete of an IdentityMapping is not a supported operation).
    const winner = await this.find(cognitoSub);
    if (!winner) {
      throw new InternalError("IdentityMapping vanished after losing creation race.", { cognitoSub });
    }
    return winner;
  }
}

/**
 * User repository Ã¢â‚¬â€ data-model.md Ã‚Â§2 (`TENANT#t#USER#u` / `PROFILE`) plus the session-
 * revocation fields required by implementation-blueprint.md Ã‚Â§4.2 ("Logout por dispositivo
 * atualiza deviceLogoutAfter/revoga a famÃƒÂ­lia de refresh. Logout global atualiza
 * globalLogoutAfter"). data-model.md doesn't enumerate a standalone Session entity, so
 * this module keeps revocation watermarks on the User item itself (globalLogoutAfter) and
 * per-device entries as child items under the same tenant partition
 * (`TENANT#t#USER#u` / `SESSION#<deviceId>`) rather than inventing a new top-level
 * aggregate Ã¢â‚¬â€ see the M1 report for this judgment call.
 */
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";

export interface UserProfile {
  PK: string;
  SK: "PROFILE";
  entityType: "User";
  userId: string;
  tenantId: string;
  identitySubject: string;
  emailNormalized: string;
  roles: string[];
  status: "ACTIVE" | "SUSPENDED";
  globalLogoutAfter?: string;
  /** W5-01/GTR-01 (`decisions-log.md` D-060): name shown to a guest as "who is requesting this
   * document" (`GuestRequestInfo.requesterDisplayName`) and interpolated into the guest-facing
   * email templates. Optional and user-editable (`PUT /profile`) Ã¢â‚¬â€ never inferred from the
   * user's e-mail domain (this codebase's epistemic-integrity discipline: never guess data that
   * was never actually captured). Absent until the user sets it once. */
  requesterDisplayName?: string;
  createdAt: string;
  updatedAt: string;
  version: number;
}

export interface DeviceSession {
  PK: string;
  SK: string; // SESSION#<deviceId>
  entityType: "DeviceSession";
  tenantId: string;
  userId: string;
  deviceId: string;
  sessionId: string;
  refreshFamilyId: string;
  deviceLogoutAfter?: string;
  createdAt: string;
  lastSeenAt: string;
  expiresAt: string;
  status: "ACTIVE" | "REVOKED";
}

export function userProfileKey(tenantId: string, userId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: "PROFILE" };
}

export function deviceSessionKey(tenantId: string, userId: string, deviceId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: `SESSION#${deviceId}` };
}

export class UserRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async getProfile(tenantId: string, userId: string): Promise<UserProfile | undefined> {
    return this.store.get<UserProfile>(userProfileKey(tenantId, userId));
  }

  async createProfileIfAbsent(profile: Omit<UserProfile, "PK" | "SK" | "entityType" | "createdAt" | "updatedAt" | "version">): Promise<UserProfile> {
    const key = userProfileKey(profile.tenantId, profile.userId);
    const existing = await this.getProfile(profile.tenantId, profile.userId);
    if (existing) {
      return existing;
    }
    const now = this.now();
    const item: UserProfile = {
      ...key,
      SK: "PROFILE",
      entityType: "User",
      ...profile,
      createdAt: now,
      updatedAt: now,
      version: 1,
    };
    await this.store.putIfAbsent(item);
    return item;
  }

  async getDeviceSession(tenantId: string, userId: string, deviceId: string): Promise<DeviceSession | undefined> {
    return this.store.get<DeviceSession>(deviceSessionKey(tenantId, userId, deviceId));
  }

  async upsertDeviceSession(session: DeviceSession): Promise<void> {
    await this.store.update(session);
  }

  /** Logout global Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§4.2: revokes every token issued before now. */
  async logoutAll(tenantId: string, userId: string): Promise<void> {
    const profile = await this.getProfile(tenantId, userId);
    if (!profile) return;
    await this.store.update({ ...profile, globalLogoutAfter: this.now(), updatedAt: this.now() });
  }

  /** Logout by device Ã¢â‚¬â€ revokes only this device's refresh family. */
  async logoutDevice(tenantId: string, userId: string, deviceId: string): Promise<void> {
    const session = await this.getDeviceSession(tenantId, userId, deviceId);
    if (!session) return;
    await this.store.update({ ...session, status: "REVOKED", deviceLogoutAfter: this.now() });
  }

  /** W5-01/GTR-01: sets or clears (`undefined`) the name shown to guests as the requester's
   * identity. Same unconditional-overwrite pattern as `logoutAll`/`logoutDevice` above Ã¢â‚¬â€ this
   * port has no per-item OCC beyond `updateConditional`'s counter-only use (quota.ts), and a
   * lost update on this single self-editable text field carries the same negligible,
   * already-accepted risk as those two fields. Caller (`ProfileService`) is responsible for
   * confirming the profile exists first. */
  async setRequesterDisplayName(profile: UserProfile, requesterDisplayName: string | undefined): Promise<UserProfile> {
    const updated: UserProfile = { ...profile, requesterDisplayName, updatedAt: this.now() };
    await this.store.update(updated);
    return updated;
  }
}

/**
 * Real DynamoDB adapter for IdentityStore (M3.5). Thin translation to
 * DynamoDBDocumentClient commands - no OCC/idempotency logic lives here, that's already
 * built by shared/dynamodb/occ.ts and shared/idempotency/idempotency.ts; this class only
 * executes the commands those builders produce (for `update`) and issues plain
 * Get/Put for the rest.
 */
import { GetCommand, PutCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";

export class DynamoDbIdentityStore implements IdentityStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
    try {
      const result = await this.client.send(
        new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: true }),
      );
      return result.Item as T | undefined;
    } catch (err) {
      throw mapDynamoError(err, "IdentityStore.get");
    }
  }

  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
    try {
      await this.client.send(
        new PutCommand({
          TableName: this.tableName,
          Item: item,
          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "IdentityStore.putIfAbsent");
    }
  }

  /** Unconditional overwrite - matches the fake's semantics (non-OCC bookkeeping writes only). */
  async update<T extends EntityKey>(item: T): Promise<void> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
    } catch (err) {
      throw mapDynamoError(err, "IdentityStore.update");
    }
  }

  async updateConditional<T extends EntityKey>(
    item: T,
    expected: { count: number; resetAt: string },
  ): Promise<boolean> {
    try {
      await this.client.send(
        new PutCommand({
          TableName: this.tableName,
          Item: item,
          // Real production bug found via a live smoke test against
          // exptrk-dev-notifications-handler (2026-08-21): `count` is a DynamoDB reserved
          // word (https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
          // - using it as a bare attribute name in a ConditionExpression, instead of through
          // an ExpressionAttributeNames placeholder, makes every real DynamoDB call fail with
          // ValidationException. Never caught by any test because the in-memory
          // IdentityStore fake (test/unit/identity/in-memory-store.ts) doesn't parse
          // condition expressions the way real DynamoDB does - this path was never actually
          // exercised against real DynamoDB before this session's real deploy.
          ConditionExpression: "#count = :expectedCount AND resetAt = :expectedResetAt",
          ExpressionAttributeNames: { "#count": "count" },
          ExpressionAttributeValues: {
            ":expectedCount": expected.count,
            ":expectedResetAt": expected.resetAt,
          },
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "IdentityStore.updateConditional");
    }
  }
}

/** Real DynamoDB adapter for `ExtractedFieldStore` (M7 item 7) Ã¢â‚¬â€ the first real writer of
 * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
 * `attribute_not_exists` Put per field row (first-time create, `buildVersionedCreate`), one
 * versioned Update on the parent `ExtractionRun` (`buildVersionedUpdate`), and one
 * `ConditionCheck` re-asserting the `Document` row is still at the version the caller read
 * moments earlier (`buildVersionConditionCheck`) Ã¢â‚¬â€ the TOCTOU close for the concurrent-discard
 * race (design Ã‚Â§3). All three succeed or none do. */
import { GetCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { TransactWriteCommandInput } from "@aws-sdk/lib-dynamodb";
import { buildVersionedCreate, buildVersionedUpdate, buildVersionConditionCheck, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import { mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
import type {
  CommitRunOutcomeInput,
  CommitRunOutcomeResult,
  ConfirmFieldInput,
  ExtractedFieldStore,
  FieldTransitionResult,
  RejectFieldInput,
} from "../ports/extracted-field-store.js";
import type { ExtractedField } from "../domain/extracted-field.js";

export class DynamoDbExtractedFieldStore implements ExtractedFieldStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async commitRunOutcome(input: CommitRunOutcomeInput): Promise<CommitRunOutcomeResult> {
    const fieldPuts = input.fields.map((field) => ({ Put: buildVersionedCreate(this.tableName, field as unknown as Record<string, unknown> & { PK: string; SK: string }) }));

    const runUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.runKey,
        tenantId: input.runTenantId,
        expectedVersion: input.runExpectedVersion,
        set: { status: input.runStatus, completedAt: input.completedAt },
      }),
    };

    const documentGuard = buildVersionConditionCheck({
      tableName: this.tableName,
      key: input.documentKey,
      expectedVersion: input.documentExpectedVersion,
    });

    // W2-01-DECISION: when the run auto-confirmed a field that maps to an `ExpirationItem`
    // attribute, that item write joins THIS transaction Ã¢â‚¬â€ never a follow-up write Ã¢â‚¬â€ so the
    // auto-confirm outcome is as atomic as the manual `confirmField` one.
    const itemUpdate = input.itemUpdate
      ? [
          {
            Update: buildVersionedUpdate({
              tableName: this.tableName,
              key: input.itemUpdate.key,
              tenantId: input.itemUpdate.tenantId,
              expectedVersion: input.itemUpdate.expectedVersion,
              set: input.itemUpdate.set,
              now: input.completedAt,
            }),
          },
        ]
      : [];

    const transactItems = [...fieldPuts, runUpdate, documentGuard, ...itemUpdate] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      // A cancellation here can only come from one of: a field row already existing (should
      // never happen - runId makes each field's SK unique per run, so this would indicate a
      // genuine retry-after-partial-commit bug, not a real race), the run's own version having
      // moved (nothing else in the system updates ExtractionRun.status before this handler
      // runs), the Document guard (the actual race this method exists to detect), or the
      // optional `ExpirationItem` update's own version having moved. Rather
      // than parsing per-item cancellation reasons (fragile across SDK versions), any
      // cancellation here is treated uniformly as DOCUMENT_DISCARDED - the safe, conservative
      // reading is "something about the world this commit assumed is no longer true", and the
      // caller's fallback (mark the run DISCARDED, persist zero fields) is safe even in the
      // (extremely unlikely) case the true cause was one of the other two conditions.
      if (isTransactionCanceled(err)) return "DOCUMENT_DISCARDED";
      throw mapDynamoError(err, "ExtractedFieldStore.commitRunOutcome");
    }
  }

  async get(key: EntityKey): Promise<ExtractedField | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
      return result.Item as ExtractedField | undefined;
    } catch (err) {
      throw mapDynamoError(err, "ExtractedFieldStore.get");
    }
  }

  async confirmField(input: ConfirmFieldInput): Promise<FieldTransitionResult> {
    const fieldUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.fieldKey,
        tenantId: input.fieldTenantId,
        expectedVersion: input.fieldExpectedVersion,
        set: { state: "CONFIRMED", confirmedValue: input.confirmedValue },
        now: input.now,
      }),
    };

    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });

    const itemEntry = input.itemUpdate
      ? {
          Update: buildVersionedUpdate({
            tableName: this.tableName,
            key: input.itemKey,
            tenantId: input.itemTenantId,
            expectedVersion: input.itemExpectedVersion,
            set: input.itemUpdate,
            now: input.now,
          }),
        }
      : buildVersionConditionCheck({ tableName: this.tableName, key: input.itemKey, expectedVersion: input.itemExpectedVersion });

    const transactItems = [fieldUpdate, runGuard, documentGuard, itemEntry] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
      throw mapDynamoError(err, "ExtractedFieldStore.confirmField");
    }
  }

  async rejectField(input: RejectFieldInput): Promise<FieldTransitionResult> {
    const set: Record<string, unknown> = { state: "REJECTED" };
    if (input.correctionReason !== undefined) set["correctionReason"] = input.correctionReason;

    const fieldUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.fieldKey,
        tenantId: input.fieldTenantId,
        expectedVersion: input.fieldExpectedVersion,
        set,
        now: input.now,
      }),
    };

    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });

    const transactItems = [fieldUpdate, runGuard, documentGuard] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
      throw mapDynamoError(err, "ExtractedFieldStore.rejectField");
    }
  }
}

src/workers\document-chasing-dispatch\dispatch.ts:113:      await deps.store.transactWrite([
src/workers\document-chasing-dispatch\dispatch.ts:172:      await deps.store.transactWrite(entries);
src/workers\document-chasing-dispatch\dispatch.ts:265:    await deps.store.transactWrite(entries);
src/workers\reminder-dispatch\dispatch.ts:101:      await deps.store.transactWrite([
src/workers\reminder-dispatch\dispatch.ts:234:    await deps.store.transactWrite(entries);
src/workers\malware-result\result-processor.ts:61:      await deps.store.transactWrite([
src/workers\reminder-producer\producer.ts:245:            await deps.store.transactWrite([
src/workers\document-purge\purge.ts:122:    await deps.store.transactWrite([
src/workers\document-purge\purge.ts:173:  await deps.store.transactWrite([
src/workers\document-purge\purge.ts:192:    await deps.store.transactWrite([
src/workers\document-purge\purge.ts:218:      await deps.store.transactWrite([
src/workers\document-purge\purge.ts:233:    await deps.store.transactWrite([
src/workers\upload-slot-reconciliation\reconciliation.ts:57:    await deps.store.transactWrite([
src/workers\upload-slot-reconciliation\reconciliation.ts:85:    await deps.store.transactWrite([
src/workers\reminder-reconciliation\reconciliation.ts:87:      await deps.store.transactWrite([
src/workers\reminder-reconciliation\reconciliation.ts:170:            await deps.store.transactWrite([
src/workers\reminder-reconciliation\reconciliation.ts:190:        await deps.store.transactWrite([
src/workers\submission-finalizer\finalizer.ts:65:      await deps.store.transactWrite([
src/modules\subject\ports\subject-store.ts:30:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/workers\submission-malware-result\result-processor.ts:49:      await deps.store.transactWrite([
src/modules\import\ports\import-store.ts:16:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\document\application\advance-after-evidence.ts:90:        await deps.store.transactWrite(rejectEntries);
src/modules\document\application\advance-after-evidence.ts:134:      await deps.store.transactWrite(entries);
src/workers\upload-finalizer\finalizer.ts:78:      await deps.store.transactWrite([
src/modules\document\application\document-deletion-service.ts:45:      await this.store.transactWrite([
src/modules\subject\persistence\dynamodb-subject-store.ts:68:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\subject\persistence\dynamodb-subject-store.ts:71:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\document\persistence\dynamodb-document-store.ts:42:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src/modules\document\application\document-service.ts:185:        await this.store.transactWrite(entries);
src/modules\document\ports\document-store.ts:11:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\persistence\dynamodb-import-store.ts:48:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\import\persistence\dynamodb-import-store.ts:51:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\reminder\ports\reminder-store.ts:31:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\reminder\ports\reminder-store.ts:52:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\domain\import-row.ts:72:  if (displayName.length > MAX_DISPLAY_NAME_LENGTH) return { rejection: { reason: "DISPLAY_NAME_TOO_LONG", field: "displayName" } };
src/modules\import\domain\import-row.ts:89:  if (tags.length > MAX_TAGS) return { rejection: { reason: "TOO_MANY_TAGS", field: "tags" } };
src/modules\import\domain\import-row.ts:91:    if (tag.length > MAX_TAG_LENGTH) return { rejection: { reason: "TAG_TOO_LONG", field: "tags" } };
src/modules\reminder\persistence\dynamodb-reminder-store.ts:52:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\persistence\dynamodb-reminder-store.ts:55:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:64:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:67:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\notification\ports\notification-store.ts:13:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\extraction\persistence\dynamodb-extracted-field-store.ts:67:    const transactItems = [...fieldPuts, runUpdate, documentGuard, ...itemUpdate] as unknown as TransactWriteCommandInput["TransactItems"];
src/modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src/modules\extraction\persistence\dynamodb-extracted-field-store.ts:126:    const transactItems = [fieldUpdate, runGuard, documentGuard, itemEntry] as unknown as TransactWriteCommandInput["TransactItems"];
src/modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src/modules\extraction\persistence\dynamodb-extracted-field-store.ts:155:    const transactItems = [fieldUpdate, runGuard, documentGuard] as unknown as TransactWriteCommandInput["TransactItems"];
src/modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src/modules\notification\persistence\dynamodb-notification-store.ts:61:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\notification\persistence\dynamodb-notification-store.ts:64:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\extraction\domain\deterministic-field-parser.ts:8: * limits (`workers/parser-sandbox/parser.ts`'s `MAX_DECOMPRESSED_BYTES`) rather than a new,
src/modules\extraction\domain\deterministic-field-parser.ts:13:import { MAX_DECOMPRESSED_BYTES } from "../../../workers/parser-sandbox/parser.js";
src/modules\extraction\domain\deterministic-field-parser.ts:98: * Only ever processes up to `MAX_DECOMPRESSED_BYTES` characters of input (M6 numeric limit,
src/modules\extraction\domain\deterministic-field-parser.ts:102:  const bounded = ocrText.length > MAX_DECOMPRESSED_BYTES ? ocrText.slice(0, MAX_DECOMPRESSED_BYTES) : ocrText;
src/modules\import\application\import-service.ts:212:      await this.store.transactWrite(entries);
src/modules\notification\application\email-delivery-workflow.ts:158:    await deps.store.transactWrite([
src/modules\notification\application\email-delivery-workflow.ts:190:    await deps.store.transactWrite([
src/modules\subject\application\guest-submission-service.ts:191:      await this.store.transactWrite(entries);
src/modules\subject\application\guest-submission-service.ts:260:      await this.store.transactWrite([
src/modules\subject\application\document-request-service.ts:183:      await this.store.transactWrite(entries);
src/modules\subject\application\document-request-service.ts:290:      await this.store.transactWrite(entries);
src/modules\subject\application\document-request-service.ts:318:    await this.store.transactWrite(entries);
src/modules\subject\application\document-request-service.ts:363:      await this.store.transactWrite(entries);
src/modules\notification\application\notification-preferences-service.ts:96:      await this.store.transactWrite([
src/modules\notification\application\notification-router-workflow.ts:104:      await deps.store.transactWrite([
src/modules\notification\application\notification-router-workflow.ts:240:    await deps.store.transactWrite(entries);
src/modules\notification\application\notification-router-workflow.ts:314:    await deps.store.transactWrite(entries);
src/modules\reminder\application\reminder-materializer.ts:229:        await this.store.transactWrite([
src/modules\reminder\application\reminder-materializer.ts:285:        await this.store.transactWrite([
src/modules\reminder\application\reminder-materializer.ts:357:        await this.store.transactWrite([
src/modules\subject\application\document-chasing-producer.ts:22:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\subject\application\document-chasing-producer.ts:111:    await deps.store.transactWrite([
src/modules\reminder\application\reminder-policy-service.ts:85:      await this.store.transactWrite(entries);
src/modules\reminder\application\reminder-policy-service.ts:169:      await this.store.transactWrite(entries);
src/modules\reminder\application\reminder-policy-service.ts:205:      await this.store.transactWrite(entries);
src/modules\subject\application\advance-after-submission-evidence.ts:54:        await deps.store.transactWrite([
src/modules\subject\application\advance-after-submission-evidence.ts:76:      await deps.store.transactWrite(entries);
src/modules\notification\application\ses-callback-workflow.ts:110:    await deps.store.transactWrite([
src/modules\notification\application\ses-callback-workflow.ts:141:    await deps.store.transactWrite([
src/modules\notification\application\ses-callback-workflow.ts:153:    await deps.store.transactWrite([
src/modules\notification\application\ses-callback-workflow.ts:172:    await deps.store.transactWrite([
src/modules\expiration\application\expiration-service.ts:687:      await this.store.transactWrite(entries);
src/modules\expiration\ports\expiration-store.ts:36:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\expiration\application\item-watch-service.ts:66:        await this.store.transactWrite(entries);
src/modules\expiration\application\item-watch-service.ts:88:      await this.store.transactWrite(entries);
src/modules\subject\application\requirement-service.ts:320:      await this.store.transactWrite(entries);
src/modules\expiration\persistence\dynamodb-expiration-store.ts:57:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\expiration\persistence\dynamodb-expiration-store.ts:60:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src/modules\subject\application\subject-service.ts:114:        await this.store.transactWrite(entries);
src/modules\subject\application\subject-service.ts:313:      await this.store.transactWrite(entries);

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'fields|ExtractedField' schemas src/modules/extraction | rg 'maxItems|slice|length|field'; rg -n 'entries\\.push|const entries|TransactWriteEntry\\[\\]' src/modules src/workers | Select-Object -First 250; rg -n 'SendEmailCommand|StartExecutionCommand|StartDocument|InvokeModel|Textract|Bedrock|PutObjectCommand|CopyObjectCommand|DeleteObjectCommand|SendMessageCommand' src" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 277ms:
schemas\sensitive-fields.json:3:  "$id": "https://expiration-tracker/schemas/sensitive-fields.json",
schemas\sensitive-fields.json:50:      "description": "Authorization header values that leak into free-text fields (e.g. sanitized SDK error messages)."
src/modules/extraction\ports\extracted-field-store.ts:2: * `ExtractedField` persistence port ? `ExtractionValidationTaskHandler` (M7 item 7) is the
src/modules/extraction\ports\extracted-field-store.ts:8: * side too: writing the `ExtractedField` rows and transitioning `ExtractionRun.status` to a
src/modules/extraction\ports\extracted-field-store.ts:12: * commit must abort the whole write, never leave `ExtractedField` rows for an item that no
src/modules/extraction\ports\extracted-field-store.ts:16:import type { ExtractedField } from "../domain/extracted-field.js";
src/modules/extraction\ports\extracted-field-store.ts:21:  fields: readonly ExtractedField[];
src/modules/extraction\ports\extracted-field-store.ts:49:export interface ExtractedFieldStore {
src/modules/extraction\ports\extracted-field-store.ts:50:  /** Atomically writes every `fields` row (first-time create, `attribute_not_exists` per row ?
src/modules/extraction\ports\extracted-field-store.ts:55:   * `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` instead, with zero fields.
src/modules/extraction\ports\extracted-field-store.ts:64:  /** M7 item 8 (?1.7): plain, eventually-consistent read of one `ExtractedField` row ? the
src/modules/extraction\ports\extracted-field-store.ts:67:  get(key: EntityKey): Promise<ExtractedField | undefined>;
src/modules/extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (?1.7): one `TransactWriteItems` ?
src/modules/extraction\ports\extracted-field-store.ts:70:   * `ExtractedField.state` -> `CONFIRMED` (+ `confirmedValue`), `ConditionCheck`s pinning
src/modules/extraction\ports\extracted-field-store.ts:75:   * design still requires `expectedItemVersion` on every confirm, even for fields with no
src/modules/extraction\ports\extracted-field-store.ts:81:  /** `POST .../fields/{fieldName}/reject` (?1.7): `ExtractedField.state` -> `REJECTED` (+
src/modules/extraction\ports\bedrock-client.ts:12:   * arguments don't validate against the closed schema (extra/missing/wrong-typed fields). */
src/modules/extraction\ports\extraction-run-store.ts:21:   * updates this same run's status atomically together with the fields it writes for the
schemas\queues\reminder-materialization-trigger.v1.json:5:  "description": "BLOCKER-B (reminder-delivery-pipeline.md #4, Codex Round H APPROVED 9.2/10): the SQS message body the reminder-materialization-trigger worker consumes. Deliberately NOT a full SqsCommandEnvelope (schemas/queues/command-envelope.v1.json) - this destination (SQS_REMINDER_MATERIALIZATION_TRIGGER_V1) forwards the bare outbox domain-event payload, so the sender (composition/reminder.ts) folds in only the two envelope fields (tenantId, eventType) the worker actually needs for routing, that its payload doesn't already self-describe. `data` is conditionally validated below against the SAME shape as the corresponding domain event schema's own `data` (item-due-date-changed.v1.json, item-deactivated.v1.json, reminder-policy-changed.v1.json), by $ref into each - never duplicated by hand, so the two can never drift apart silently.",
src/modules/extraction\persistence\bedrock-runtime-client.ts:48:    throw new BedrockExtractionFailedError("submit_extraction tool input has unexpected fields.", { extraKeys });
src/modules/extraction\persistence\bedrock-runtime-client.ts:51:  const fields: BedrockFieldCandidate[] = [];
src/modules/extraction\persistence\bedrock-runtime-client.ts:67:    fields.push({ fieldName: field.fieldName, value: entry["value"], confidence });
src/modules/extraction\persistence\bedrock-runtime-client.ts:69:  return fields;
src/modules/extraction\persistence\bedrock-runtime-client.ts:129:    const fields = parseSubmitExtractionInput(toolUseBlock.toolUse.input, request.pipelineVersion);
src/modules/extraction\persistence\bedrock-runtime-client.ts:130:    return { fields };
src/modules/extraction\application\complete-ocr.ts:90:    // without these fields RunDeterministicParser would have no tenantId/documentId/runId/
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:1:/** Real DynamoDB adapter for `ExtractedFieldStore` (M7 item 7) ? the first real writer of
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:2: * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:18:  ExtractedFieldStore,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:22:import type { ExtractedField } from "../domain/extracted-field.js";
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:24:export class DynamoDbExtractedFieldStore implements ExtractedFieldStore {
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:31:    const fieldPuts = input.fields.map((field) => ({ Put: buildVersionedCreate(this.tableName, field as unknown as Record<string, unknown> & { PK: string; SK: string }) }));
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:82:      // caller's fallback (mark the run DISCARDED, persist zero fields) is safe even in the
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:85:      throw mapDynamoError(err, "ExtractedFieldStore.commitRunOutcome");
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:89:  async get(key: EntityKey): Promise<ExtractedField | undefined> {
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:92:      return result.Item as ExtractedField | undefined;
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:94:      throw mapDynamoError(err, "ExtractedFieldStore.get");
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:133:      throw mapDynamoError(err, "ExtractedFieldStore.confirmField");
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:162:      throw mapDynamoError(err, "ExtractedFieldStore.rejectField");
schemas\api\confirm-extracted-field-request.v1.json:4:  "title": "ConfirmExtractedFieldRequest v1",
schemas\api\confirm-extracted-field-request.v1.json:5:  "description": "POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm request body (m7-extraction-design/claude-reconciliation-final-design.md ?1.7).",
src/modules/extraction\http\extraction-handlers.ts:45:  fields: ConfirmRejectFieldDeps;
src/modules/extraction\http\extraction-handlers.ts:115:    const field = await confirmField(deps.fields, context, {
src/modules/extraction\http\extraction-handlers.ts:142:    const field = await rejectField(deps.fields, context, {
src/modules/extraction\domain\bedrock-extraction.ts:52:Your only job is to read the text inside the <untrusted_document_text> block in the user message and call the submit_extraction tool exactly once with your best-effort extraction of the requested fields.
src/modules/extraction\domain\bedrock-extraction.ts:57:3. Only extract the fields explicitly listed in the tool schema. Never invent additional fields.
src/modules/extraction\domain\bedrock-extraction.ts:77:export function buildSubmitExtractionToolSchema(fields: readonly FieldDefinition[] = FIELD_SCHEMA_V1): {
src/modules/extraction\domain\bedrock-extraction.ts:83:  for (const field of fields) {
src/modules/extraction\domain\bedrock-extraction.ts:94:        required: fields.filter((f) => f.required).map((f) => f.fieldName),
src/modules/extraction\domain\bedrock-extraction.ts:103: * fields can never be starved, but the response also can never grow unbounded. */
src/modules/extraction\domain\bedrock-extraction.ts:120:    "Extract the requested fields from the document text below by calling submit_extraction.",
src/modules/extraction\domain\bedrock-extraction.ts:146:  readonly fields: readonly BedrockFieldCandidate[];
src/modules/extraction\application\run-bedrock-extraction.ts:20:import type { ExtractedFieldValueType } from "../domain/extracted-field.js";
src/modules/extraction\application\run-bedrock-extraction.ts:169:      const bedrockFields: BedrockFieldCandidateOutput[] = result.fields.map((f) => ({
src/modules/extraction\composition\extraction.ts:30:import { DynamoDbExtractedFieldStore } from "../persistence/dynamodb-extracted-field-store.js";
src/modules/extraction\composition\extraction.ts:179:  const fields = new DynamoDbExtractedFieldStore(clients.dynamo, config.tableName);
src/modules/extraction\composition\extraction.ts:187:    runExtractionValidation: { documents, items, runs, fields, artifacts },
src/modules/extraction\application\confirm-reject-field.ts:3: * `POST .../fields/{fieldName}/confirm` and `POST .../fields/{fieldName}/reject`. Same shape as
src/modules/extraction\application\confirm-reject-field.ts:6: * `ExtractedField`), `reject` is 3-way (never touches `ExpirationItem`).
src/modules/extraction\application\confirm-reject-field.ts:19:import { extractedFieldKey, type ExtractedField } from "../domain/extracted-field.js";
src/modules/extraction\application\confirm-reject-field.ts:24:import type { ExtractedFieldStore } from "../ports/extracted-field-store.js";
src/modules/extraction\application\confirm-reject-field.ts:31:  fields: ExtractedFieldStore;
src/modules/extraction\application\confirm-reject-field.ts:61:async function readField(deps: ConfirmRejectFieldDeps, tenantId: string, documentId: string, fieldName: string, runId: string): Promise<ExtractedField> {
src/modules/extraction\application\confirm-reject-field.ts:62:  const field = await deps.fields.get(extractedFieldKey(tenantId, documentId, fieldName, runId));
src/modules/extraction\application\confirm-reject-field.ts:64:    throw new NotFoundError("ExtractedField not found.", { documentId, fieldName, runId });
src/modules/extraction\application\confirm-reject-field.ts:96:export async function confirmField(deps: ConfirmRejectFieldDeps, ctx: RequestContext, params: ConfirmFieldParams): Promise<ExtractedField> {
src/modules/extraction\application\confirm-reject-field.ts:134:async function doConfirmField(deps: ConfirmRejectFieldDeps, tenantId: string, params: ConfirmFieldParams): Promise<ExtractedField> {
src/modules/extraction\application\confirm-reject-field.ts:140:  assertVersion("ExtractedField", params.expectedFieldVersion, field.version, { fieldName: params.fieldName });
src/modules/extraction\application\confirm-reject-field.ts:146:    throw new BusinessRuleError(`ExtractedField is not pending confirmation (state=${field.state}).`, { fieldName: params.fieldName, state: field.state });
src/modules/extraction\application\confirm-reject-field.ts:169:  const outcome = await deps.fields.confirmField({
src/modules/extraction\application\confirm-reject-field.ts:203:export async function rejectField(deps: ConfirmRejectFieldDeps, ctx: RequestContext, params: RejectFieldParams): Promise<ExtractedField> {
src/modules/extraction\application\confirm-reject-field.ts:240:async function doRejectField(deps: ConfirmRejectFieldDeps, tenantId: string, params: RejectFieldParams): Promise<ExtractedField> {
src/modules/extraction\application\confirm-reject-field.ts:245:  assertVersion("ExtractedField", params.expectedFieldVersion, field.version, { fieldName: params.fieldName });
src/modules/extraction\application\confirm-reject-field.ts:250:    throw new BusinessRuleError(`ExtractedField is not pending confirmation (state=${field.state}).`, { fieldName: params.fieldName, state: field.state });
src/modules/extraction\application\confirm-reject-field.ts:254:  const outcome = await deps.fields.rejectField({
src/modules/extraction\application\confirm-reject-field.ts:276:  const result: ExtractedField = { ...field, state: "REJECTED", version: field.version + 1, updatedAt: now };
src/modules/extraction\domain\decide-bedrock.ts:14:import type { ExtractedFieldValueType } from "./extracted-field.js";
src/modules/extraction\domain\decide-bedrock.ts:36:  readonly fields: readonly FieldExtractionAssessment[];
src/modules/extraction\domain\decide-bedrock.ts:51:  return input.fields.some((field) => fieldNeedsBedrock(field, input.ocrAvailable));
src/modules/extraction\application\run-deterministic-parser.ts:13: *    `artifact`, plus the run identity fields it re-attaches - see complete-ocr.ts).
src/modules/extraction\application\run-deterministic-parser.ts:29:import type { ExtractedFieldValueType } from "../domain/extracted-field.js";
src/modules/extraction\application\run-deterministic-parser.ts:140:  const decisionInput: BedrockDecisionInput = { fields: assessments, ocrAvailable, thresholdVersion: THRESHOLD_VERSION_V1 };
src/modules/extraction\application\run-extraction-validation.ts:26:import { extractedFieldKey, type ExtractedField, type ExtractedFieldValueType, type ExtractionSource } from "../domain/extracted-field.js";
src/modules/extraction\application\run-extraction-validation.ts:34:import type { ExtractedFieldStore } from "../ports/extracted-field-store.js";
src/modules/extraction\application\run-extraction-validation.ts:46:  fields: ExtractedFieldStore;
src/modules/extraction\application\run-extraction-validation.ts:149:  fields: ExtractedField[],
src/modules/extraction\application\run-extraction-validation.ts:169:    result = await deps.fields.commitRunOutcome({
src/modules/extraction\application\run-extraction-validation.ts:170:      fields,
src/modules/extraction\application\run-extraction-validation.ts:205: *  - only auto-`CONFIRMED` fields count; `PENDING_CONFIRMATION` still waits for a human;
src/modules/extraction\application\run-extraction-validation.ts:206: *  - only fields the schema actually maps to an item attribute (`expirationDate` -> `dueDate`)
src/modules/extraction\application\run-extraction-validation.ts:207: *    ? no invented handling for hypothetical future fields;
src/modules/extraction\application\run-extraction-validation.ts:208: *  - a missing or `DELETED` item is NOT an error here: the run still commits its fields (same
src/modules/extraction\application\run-extraction-validation.ts:221:  fields: readonly ExtractedField[],
src/modules/extraction\application\run-extraction-validation.ts:223:  const autoConfirmed = fields.filter((f) => f.state === "CONFIRMED" && f.confirmedValue !== undefined && ITEM_ATTRIBUTE_BY_FIELD_NAME[f.fieldName] !== undefined);
src/modules/extraction\application\run-extraction-validation.ts:252:  const fields: ExtractedField[] = compared.map((cf) => {
src/modules/extraction\application\run-extraction-validation.ts:276:  const itemUpdate = await buildAutoConfirmItemUpdate(deps, input, fields);
src/modules/extraction\application\run-extraction-validation.ts:282:  const outcome = await commitOrDiscard(deps, input, fields, "COMPLETED", itemUpdate);
src/modules/extraction\application\run-extraction-validation.ts:283:  const requiresReview = outcome === "COMPLETED" && fields.some((f) => f.state === "PENDING_CONFIRMATION");
src/modules/extraction\application\run-extraction-validation.ts:301:  const fields: ExtractedField[] = schema.map((field) => ({
src/modules/extraction\application\run-extraction-validation.ts:319:  const outcome = await commitOrDiscard(deps, input, fields, "FAILED");
src/modules/extraction\domain\decide-field-outcome.ts:6: * `ExtractedFieldState` enum - PENDING_CONFIRMATION/CONFIRMED/REJECTED, no separate "needs
src/modules/extraction\domain\decide-field-outcome.ts:7: * review" run-level state - means individual fields carry review status, not the run itself).
src/modules/extraction\domain\decide-field-outcome.ts:13:import type { ExtractedFieldState } from "./extracted-field.js";
src/modules/extraction\domain\decide-field-outcome.ts:16:export function decideFieldOutcome(comparison: ComparisonResult): { state: ExtractedFieldState; confirmedValue?: string } {
src/modules/extraction\domain\extracted-field.ts:2: * ExtractedField ? um campo candidato produzido por um `ExtractionRun` (M7).
src/modules/extraction\domain\extracted-field.ts:14:export type ExtractedFieldValueType = "DATE" | "STRING" | "NUMBER";
src/modules/extraction\domain\extracted-field.ts:25:export type ExtractedFieldState = "PENDING_CONFIRMATION" | "CONFIRMED" | "REJECTED";
src/modules/extraction\domain\extracted-field.ts:27:export interface ExtractedField extends EntityKey {
src/modules/extraction\domain\extracted-field.ts:28:  entityType: "ExtractedField";
src/modules/extraction\domain\extracted-field.ts:33:  valueType: ExtractedFieldValueType;
src/modules/extraction\domain\extracted-field.ts:38:  state: ExtractedFieldState;
src/modules/extraction\domain\field-schema.ts:2: * Field schema for the extraction pipeline (M7) ? the set of fields a run tries to extract,
src/modules/extraction\domain\field-schema.ts:9: * narrow rather than inventing additional product fields no design document specified.
src/modules/extraction\domain\field-schema.ts:11:import type { ExtractedFieldValueType } from "./extracted-field.js";
src/modules/extraction\domain\field-schema.ts:16:  readonly valueType: ExtractedFieldValueType;
schemas\api\reject-extracted-field-request.v1.json:4:  "title": "RejectExtractedFieldRequest v1",
schemas\api\reject-extracted-field-request.v1.json:5:  "description": "POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/reject request body (m7-extraction-design/claude-reconciliation-final-design.md ?1.7) ? never accepts item attributes, only touches ExtractedField.",
schemas\api\update-notification-preferences-request.v1.json:5:  "description": "PUT /notifications/preferences request body (src/modules/notification/domain/notification-preferences.ts NotificationPreferences' user-editable fields).",
src/modules/extraction\domain\validate-field-value.ts:8:import type { ExtractedFieldValueType } from "./extracted-field.js";
src/modules/extraction\domain\validate-field-value.ts:12:export function isValidFieldValue(valueType: ExtractedFieldValueType, value: string): boolean {
src/modules\subject\ports\subject-store.ts:30:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/workers\reminder-dispatch\dispatch.ts:156:  const entries: TransactWriteEntry[] = [
src/workers\reminder-dispatch\dispatch.ts:186:  entries.push({
src/modules\reminder\ports\reminder-store.ts:31:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\reminder\ports\reminder-store.ts:52:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/workers\document-chasing-dispatch\dispatch.ts:157:    const entries: TransactWriteEntry[] = [
src/workers\document-chasing-dispatch\dispatch.ts:240:  const entries: TransactWriteEntry[] = [
src/modules\document\application\advance-after-evidence.ts:34:async function appendSlotConsumption(deps: AdvanceAfterEvidenceDeps, entries: TransactWriteEntry[], tenantId: string, uploadSlotId: string): Promise<void> {
src/modules\document\application\advance-after-evidence.ts:37:  entries.push({
src/modules\document\application\advance-after-evidence.ts:85:      const rejectEntries: TransactWriteEntry[] = [
src/modules\document\application\advance-after-evidence.ts:121:    const entries: TransactWriteEntry[] = [
src/modules\expiration\ports\expiration-store.ts:36:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\document\application\document-service.ts:180:      const entries: TransactWriteEntry[] = [
src/modules\reminder\persistence\dynamodb-reminder-store.ts:52:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\persistence\dynamodb-reminder-producer-store.ts:64:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\expiration\persistence\dynamodb-expiration-store.ts:57:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\expiration\domain\audit-event.ts:81:export function appendAuditToTransaction(tx: TransactWriteEntry[], tableName: string, event: AuditEvent): void {
src/modules\subject\persistence\dynamodb-subject-store.ts:68:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\document\persistence\dynamodb-document-store.ts:42:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\expiration\application\item-watch-service.ts:62:      const entries: TransactWriteEntry[] = [
src/modules\expiration\application\item-watch-service.ts:84:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:176:    const entries: TransactWriteEntry[] = [{ Put: buildVersionedCreate(this.tableName, item as unknown as Record<string, unknown> & { PK: string; SK: string }) }];
src/modules\expiration\application\expiration-service.ts:279:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:481:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:560:      entries.push({ Put: buildVersionedCreate(this.tableName, newPolicy as unknown as Record<string, unknown> & { PK: string; SK: string }) });
src/modules\expiration\application\expiration-service.ts:561:      entries.push({
src/modules\expiration\application\expiration-service.ts:600:    const entries: TransactWriteEntry[] = [
src/modules\expiration\application\expiration-service.ts:631:    entries: TransactWriteEntry[],
src/modules\expiration\application\expiration-service.ts:652:    entries: TransactWriteEntry[],
src/modules\expiration\application\expiration-service.ts:685:  private async commit(entries: TransactWriteEntry[]): Promise<void> {
src/modules\subject\domain\audit-event.ts:90:export function appendSubjectAuditToTransaction(tx: TransactWriteEntry[], tableName: string, event: SubjectAuditEvent): void {
src/modules\import\ports\import-store.ts:16:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\document\ports\document-store.ts:11:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\import\persistence\dynamodb-import-store.ts:48:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\reminder\application\reminder-policy-service.ts:80:    const entries: TransactWriteEntry[] = [{ Put: buildVersionedCreate(this.tableName, policy as unknown as Record<string, unknown> & { PK: string; SK: string }) }];
src/modules\reminder\application\reminder-policy-service.ts:124:    const entries: TransactWriteEntry[] = [
src/modules\reminder\application\reminder-policy-service.ts:141:      entries.push({ Delete: { TableName: this.tableName, Key: policyRefKey(tenantId, policy.itemId!, policyId) } });
src/modules\reminder\application\reminder-policy-service.ts:184:    const entries: TransactWriteEntry[] = [
src/modules\reminder\application\reminder-policy-service.ts:224:    entries: TransactWriteEntry[],
src/modules\reminder\application\reminder-policy-service.ts:228:    entries.push(
src/modules\reminder\application\reminder-policy-service.ts:236:      entries.push({
src/modules\reminder\application\reminder-policy-service.ts:247:    entries: TransactWriteEntry[],
src/modules\import\application\import-service.ts:171:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\document-request-service.ts:168:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\document-request-service.ts:253:    const entries: TransactWriteEntry[] = existing
src/modules\subject\application\document-request-service.ts:308:    const entries: TransactWriteEntry[] = [];
src/modules\subject\application\document-request-service.ts:342:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\document-request-service.ts:377:    entries: TransactWriteEntry[],
src/modules\subject\application\advance-after-submission-evidence.ts:72:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:91:      const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:159:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:222:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\subject-service.ts:236:      entries.push({
src/modules\subject\application\subject-service.ts:260:    entries: TransactWriteEntry[],
src/modules\subject\application\subject-service.ts:311:  private async commit(entries: TransactWriteEntry[]): Promise<void> {
src/modules\subject\application\guest-submission-service.ts:173:    const entries: TransactWriteEntry[] = [
src/modules\import\application\import-commit-service.ts:71:  const entries: ImportRowPlanEntry[] = planContent
src/modules\subject\application\document-chasing-producer.ts:22:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\subject\application\requirement-service.ts:71:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:125:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:162:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:192:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:249:    const entries: TransactWriteEntry[] = [
src/modules\subject\application\requirement-service.ts:273:    entries: TransactWriteEntry[],
src/modules\subject\application\requirement-service.ts:318:  private async commit(entries: TransactWriteEntry[]): Promise<void> {
src/modules\notification\persistence\dynamodb-notification-store.ts:61:  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
src/modules\notification\ports\notification-store.ts:13:  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
src/modules\notification\application\notification-router-workflow.ts:206:  const entries: TransactWriteEntry[] = [
src/modules\notification\application\notification-router-workflow.ts:256:  const entries: TransactWriteEntry[] = [
src/modules\notification\application\notification-router-workflow.ts:299:    entries.push({ Put: { TableName: deps.tableName, Item: { ...attempt }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } });
src/modules\notification\application\notification-router-workflow.ts:302:    entries.push({ Put: { TableName: deps.tableName, Item: { ...lookup }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } });
src/modules\notification\application\notification-router-workflow.ts:304:    entries.push({
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:2: * Real Lambda for `BedrockExtractionTaskHandler` (M7 item 6, D-035 Â§1.9/Â§1.11). Same shape as
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:3: * item 5's `PdfParserTaskHandler` - the ASL's `RunBedrock` state is `arn:aws:states:::lambda:
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:8: * `SecureLogger` only - never logs document/OCR text, the Bedrock system/user prompt content, or
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:17:import { buildBedrockExtractionTaskWorkerDeps, createRealBedrockExtractionTaskWorkerClients } from "../../../modules/extraction/composition/extraction.js";
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:18:import { runBedrockExtraction, type RunBedrockExtractionInput, type RunBedrockExtractionOutput } from "../../../modules/extraction/application/run-bedrock-extraction.js";
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:33:// model ID beyond what Bedrock itself rejects at call time.
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:45:const realClients = createRealBedrockExtractionTaskWorkerClients(bedrockRegion);
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:47:const deps = buildBedrockExtractionTaskWorkerDeps(
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:59:export async function handler(event: RunBedrockExtractionInput): Promise<RunBedrockExtractionOutput> {
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:62:      const output = await runBedrockExtraction(deps.runBedrockExtraction, event);
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:63:      logger.info("bedrock-extraction-task RunBedrock succeeded", {
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:73:      logger.error("bedrock-extraction-task RunBedrock failed", { documentId: event.documentId, runId: event.runId, errorCode: appErr.code });
src\shared\errors\app-error.ts:136: * `RunTextract` Catch block). Each `code` string below is NOT decorative â€” it MUST literally
src\shared\errors\app-error.ts:138: * `TextractTaskHandler` reports failures via `SendTaskFailure({ error: <code> })` and Step
src\shared\errors\app-error.ts:143: * Textract input format. Never retryable â€” the same bytes will classify the same way again. */
src\shared\errors\app-error.ts:151:/** The `OCR` AppConfig kill switch was off at the moment `RunTextract` executed. Per design
src\shared\errors\app-error.ts:160:/** Textract itself rejected the document as an unsupported format/corrupt file at
src\shared\errors\app-error.ts:161: * `StartDocumentTextDetection` time (distinct from the pre-call heuristic classifier). */
src\shared\errors\app-error.ts:162:export class TextractUnsupportedDocumentError extends AppError {
src\shared\errors\app-error.ts:163:  constructor(message = "Textract rejected the document as unsupported.", details?: Record<string, unknown>) {
src\shared\errors\app-error.ts:164:    super({ code: "TextractUnsupportedDocument", category: "VALIDATION", message, retryable: false, details });
src\shared\errors\app-error.ts:165:    this.name = "TextractUnsupportedDocumentError";
src\shared\errors\app-error.ts:169:/** Textract job ended (or was read back) in a degraded state the handler cannot use as a
src\shared\errors\app-error.ts:172:export class TextractPartialFailureError extends AppError {
src\shared\errors\app-error.ts:173:  constructor(message = "Textract job did not complete usably.", details?: Record<string, unknown>) {
src\shared\errors\app-error.ts:174:    super({ code: "TextractPartialFailure", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:175:    this.name = "TextractPartialFailureError";
src\shared\errors\app-error.ts:179:/** `StartDocumentTextDetection` succeeded but persisting the `TextractJob` correlation record
src\shared\errors\app-error.ts:181: * Textract job is now orphaned (no waiting callback can ever resolve it), but the run itself
src\shared\errors\app-error.ts:183: * `RunTextract` failure. */
src\shared\errors\app-error.ts:184:export class TextractJobPersistenceFailedError extends AppError {
src\shared\errors\app-error.ts:185:  constructor(message = "Failed to persist TextractJob correlation record after StartDocumentTextDetection succeeded.", details?: Record<string, unknown>) {
src\shared\errors\app-error.ts:186:    super({ code: "TextractJobPersistenceFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:187:    this.name = "TextractJobPersistenceFailedError";
src\shared\errors\app-error.ts:193: * parsed as valid Textract block JSON). This is the ONE failure the ASL routes straight to
src\shared\errors\app-error.ts:194: * `MarkPendingConfirmation` without trying Bedrock (design Â§1.2) - the ASL's `Catch` for this
src\shared\errors\app-error.ts:196: * `ErrorEquals` entry the way the Textract errors above do, but it still follows the same
src\shared\errors\app-error.ts:206:/** `BedrockExtractionTaskHandler`'s `RunBedrock` state (M7 item 6, D-035 Â§1.9/Â§1.11) - the
src\shared\errors\app-error.ts:209: * this is a defense-in-depth re-check, never the only gate. The ASL's `Catch` for `RunBedrock`
src\shared\errors\app-error.ts:218:/** Bedrock Converse call failed, or the model's tool-call response could not be parsed/
src\shared\errors\app-error.ts:223:export class BedrockExtractionFailedError extends AppError {
src\shared\errors\app-error.ts:224:  constructor(message = "Bedrock extraction call failed or returned an unusable response.", details?: Record<string, unknown>) {
src\shared\errors\app-error.ts:225:    super({ code: "BedrockExtractionFailed", category: "DEPENDENCY_UNAVAILABLE", message, retryable: false, details });
src\shared\errors\app-error.ts:226:    this.name = "BedrockExtractionFailedError";
src\runtime\aws\composition\reminder.ts:3:import { SQSClient, SendMessageCommand } from "@aws-sdk/client-sqs";
src\runtime\aws\composition\reminder.ts:77:      new SendMessageCommand({
src\runtime\aws\composition\reminder.ts:92:      new SendMessageCommand({
src\runtime\aws\composition\notification.ts:4:import { SQSClient, SendMessageCommand } from "@aws-sdk/client-sqs";
src\runtime\aws\composition\notification.ts:40:          new SendMessageCommand({
src\modules\import\persistence\s3-import-object-store.ts:3:import { GetObjectCommand, PutObjectCommand, type S3Client } from "@aws-sdk/client-s3";
src\modules\import\persistence\s3-import-object-store.ts:23:    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));
src\runtime\aws\handlers\outbox-sweeper-handler.ts:9:import { SQSClient, SendMessageCommand } from "@aws-sdk/client-sqs";
src\runtime\aws\handlers\outbox-sweeper-handler.ts:39:    new SendMessageCommand({
src\runtime\aws\handlers\outbox-sweeper-handler.ts:52:    new SendMessageCommand({
src\runtime\aws\handlers\pdf-parser-task-handler.ts:2: * Real Lambda for `PdfParserTaskHandler` (M7 item 5, D-035 Â§1.3). Unlike `TextractTaskHandler`,
src\runtime\aws\handlers\pdf-parser-task-handler.ts:8: * No AI provider, no Textract, no DynamoDB access - purely deterministic regex/heuristic field
src\runtime\aws\handlers\pdf-parser-task-handler.ts:9: * extraction over the OCR artifact (when present) plus the `needsBedrock()`/`AI_EXTRACTION`
src\runtime\aws\handlers\pdf-parser-task-handler.ts:51:        needsBedrock: output.needsBedrock,
src\runtime\aws\handlers\pdf-parser-task-handler.ts:57:      // straight to MarkPendingConfirmation, never to Bedrock, per design Â§1.2.
src\modules\extraction\persistence\bedrock-runtime-client.ts:1:/** Real `BedrockClient` adapter over `@aws-sdk/client-bedrock-runtime`'s Converse API (M7 item
src\modules\extraction\persistence\bedrock-runtime-client.ts:5: * caller; only the parsed, schema-validated `BedrockExtractionResult`.
src\modules\extraction\persistence\bedrock-runtime-client.ts:8: * (`BedrockRuntimeClient`'s own region comes from the client passed in at composition time, same
src\modules\extraction\persistence\bedrock-runtime-client.ts:10: * de modelo Bedrock" to a pre-production decision, so this adapter must never hardcode a real
src\modules\extraction\persistence\bedrock-runtime-client.ts:15:import { BedrockRuntimeClient, ConverseCommand, type Tool } from "@aws-sdk/client-bedrock-runtime";
src\modules\extraction\persistence\bedrock-runtime-client.ts:16:import type { BedrockClient } from "../ports/bedrock-client.js";
src\modules\extraction\persistence\bedrock-runtime-client.ts:18:import type { BedrockExtractionRequest, BedrockExtractionResult, BedrockFieldCandidate } from "../domain/bedrock-extraction.js";
src\modules\extraction\persistence\bedrock-runtime-client.ts:27:import { BedrockExtractionFailedError } from "../../../shared/errors/app-error.js";
src\modules\extraction\persistence\bedrock-runtime-client.ts:40:function parseSubmitExtractionInput(raw: unknown, pipelineVersion: string): BedrockFieldCandidate[] {
src\modules\extraction\persistence\bedrock-runtime-client.ts:42:    throw new BedrockExtractionFailedError("submit_extraction tool input is not an object.");
src\modules\extraction\persistence\bedrock-runtime-client.ts:48:    throw new BedrockExtractionFailedError("submit_extraction tool input has unexpected fields.", { extraKeys });
src\modules\extraction\persistence\bedrock-runtime-client.ts:51:  const fields: BedrockFieldCandidate[] = [];
src\modules\extraction\persistence\bedrock-runtime-client.ts:56:        throw new BedrockExtractionFailedError(`submit_extraction tool input is missing required field ${field.fieldName}.`);
src\modules\extraction\persistence\bedrock-runtime-client.ts:61:      throw new BedrockExtractionFailedError(`submit_extraction tool input for ${field.fieldName} has the wrong shape.`);
src\modules\extraction\persistence\bedrock-runtime-client.ts:65:      throw new BedrockExtractionFailedError(`submit_extraction tool input for ${field.fieldName} has an out-of-range confidence.`);
src\modules\extraction\persistence\bedrock-runtime-client.ts:72:export class BedrockRuntimeConverseClient implements BedrockClient {
src\modules\extraction\persistence\bedrock-runtime-client.ts:74:    private readonly client: BedrockRuntimeClient,
src\modules\extraction\persistence\bedrock-runtime-client.ts:79:  async extract(request: BedrockExtractionRequest): Promise<BedrockExtractionResult> {
src\modules\extraction\persistence\bedrock-runtime-client.ts:91:      throw new BedrockExtractionFailedError("Failed to read/parse the OCR artifact for Bedrock extraction.", {
src\modules\extraction\persistence\bedrock-runtime-client.ts:118:      throw new BedrockExtractionFailedError("Bedrock Converse call failed.", { cause: err instanceof Error ? err.message : String(err) });
src\modules\extraction\persistence\bedrock-runtime-client.ts:124:      throw new BedrockExtractionFailedError("Model response did not include a submit_extraction tool call.", {
src\modules\extraction\composition\extraction.ts:1:/** Composition root for `TextractTaskHandler` (M7 items 3/4) against real AWS adapters â€”
src\modules\extraction\composition\extraction.ts:2: * DynamoDB, S3, Textract, Step Functions, KMS, AppConfigData. Mirrors
src\modules\extraction\composition\extraction.ts:12:import { TextractClient as AwsTextractClient } from "@aws-sdk/client-textract";
src\modules\extraction\composition\extraction.ts:15:import { DynamoDbTextractJobStore } from "../persistence/dynamodb-textract-job-store.js";
src\modules\extraction\composition\extraction.ts:16:import { TextractSdkClient } from "../persistence/textract-sdk-client.js";
src\modules\extraction\composition\extraction.ts:21:import { BedrockRuntimeClient } from "@aws-sdk/client-bedrock-runtime";
src\modules\extraction\composition\extraction.ts:25:import type { RunBedrockExtractionDeps } from "../application/run-bedrock-extraction.js";
src\modules\extraction\composition\extraction.ts:26:import { BedrockRuntimeConverseClient } from "../persistence/bedrock-runtime-client.js";
src\modules\extraction\composition\extraction.ts:33:export interface TextractTaskWorkerConfig {
src\modules\extraction\composition\extraction.ts:42:export interface TextractTaskWorkerDeps {
src\modules\extraction\composition\extraction.ts:47:export function buildTextractTaskWorkerDeps(
src\modules\extraction\composition\extraction.ts:54:    textract: AwsTextractClient;
src\modules\extraction\composition\extraction.ts:56:  config: TextractTaskWorkerConfig,
src\modules\extraction\composition\extraction.ts:57:): TextractTaskWorkerDeps {
src\modules\extraction\composition\extraction.ts:60:  const jobs = new DynamoDbTextractJobStore(clients.dynamo, config.tableName);
src\modules\extraction\composition\extraction.ts:61:  const textract = new TextractSdkClient(clients.textract);
src\modules\extraction\composition\extraction.ts:87:export function createRealTextractTaskWorkerClients() {
src\modules\extraction\composition\extraction.ts:91:    textract: new AwsTextractClient({}),
src\modules\extraction\composition\extraction.ts:96: * dependency set than `TextractTaskHandler`'s: no DynamoDB, no Textract, no KMS, no Step
src\modules\extraction\composition\extraction.ts:127:/** Composition root for `BedrockExtractionTaskHandler` (M7 item 6, D-035 Â§1.9/Â§1.11) - like
src\modules\extraction\composition\extraction.ts:129: * via `TenantQuotaService`/`DynamoDbIdentityStore`, same as `TextractTaskHandler`), S3 (read the
src\modules\extraction\composition\extraction.ts:131: * `BedrockRuntimeClient`. No KMS, no Step Functions client, no Textract client. */
src\modules\extraction\composition\extraction.ts:132:export interface BedrockExtractionTaskWorkerConfig {
src\modules\extraction\composition\extraction.ts:139:export interface BedrockExtractionTaskWorkerDeps {
src\modules\extraction\composition\extraction.ts:140:  runBedrockExtraction: RunBedrockExtractionDeps;
src\modules\extraction\composition\extraction.ts:143:export function buildBedrockExtractionTaskWorkerDeps(
src\modules\extraction\composition\extraction.ts:144:  clients: { dynamo: DynamoDBDocumentClient; s3: S3Client; appConfigData: AppConfigDataClient; bedrockRuntime: BedrockRuntimeClient },
src\modules\extraction\composition\extraction.ts:145:  config: BedrockExtractionTaskWorkerConfig,
src\modules\extraction\composition\extraction.ts:146:): BedrockExtractionTaskWorkerDeps {
src\modules\extraction\composition\extraction.ts:151:  const bedrock = new BedrockRuntimeConverseClient(clients.bedrockRuntime, artifacts, config.bedrockModelId);
src\modules\extraction\composition\extraction.ts:154:    runBedrockExtraction: { featureFlags, quota, bedrock },
src\modules\extraction\composition\extraction.ts:161: * artifact at the run's terminal step). No Textract, no Bedrock, no KMS, no Step Functions
src\modules\extraction\composition\extraction.ts:191:export function createRealBedrockExtractionTaskWorkerClients(region: string) {
src\modules\extraction\composition\extraction.ts:198:    bedrockRuntime: new BedrockRuntimeClient({ region }),
src\runtime\aws\handlers\textract-task-handler.ts:2: * Real Lambda for `TextractTaskHandler` (M7 items 3/4). Single deployable function dispatching
src\runtime\aws\handlers\textract-task-handler.ts:6: *   waitForTaskToken` (`infra/state-machines/document-extraction.asl.json`'s `RunTextract`
src\runtime\aws\handlers\textract-task-handler.ts:9: * - `COMPLETE_OCR`: SQS batch, fed by a queue subscribed to Textract's own SNS job-completion
src\runtime\aws\handlers\textract-task-handler.ts:11: *   Textract's own JSON string carrying `JobId`). Same `batchItemFailures` pattern as
src\runtime\aws\handlers\textract-task-handler.ts:22:import { buildTextractTaskWorkerDeps, createRealTextractTaskWorkerClients } from "../../../modules/extraction/composition/extraction.js";
src\runtime\aws\handlers\textract-task-handler.ts:50:const realClients = createRealTextractTaskWorkerClients();
src\runtime\aws\handlers\textract-task-handler.ts:52:const deps = buildTextractTaskWorkerDeps(
src\runtime\aws\handlers\textract-task-handler.ts:72:interface TextractSnsMessage {
src\runtime\aws\handlers\textract-task-handler.ts:107:        const message = JSON.parse(envelope.Message) as TextractSnsMessage;
src\modules\extraction\ports\textract-job-store.ts:1:/** DynamoDB surface for `TextractJob` â€” narrow, matching the ExtractionRunStore/DocumentReader
src\modules\extraction\ports\textract-job-store.ts:4:import type { TextractJob } from "../domain/textract-job.js";
src\modules\extraction\ports\textract-job-store.ts:6:export interface TextractJobStore {
src\modules\extraction\ports\textract-job-store.ts:7:  /** Creates a brand-new TextractJob record. Called only AFTER StartDocumentTextDetection has
src\modules\extraction\ports\textract-job-store.ts:9:   * must surface as `TextractJobPersistenceFailedError`, never be swallowed. */
src\modules\extraction\ports\textract-job-store.ts:10:  create(job: TextractJob): Promise<void>;
src\modules\extraction\ports\textract-job-store.ts:14:  getByJobId(jobId: string): Promise<TextractJob | null>;
src\modules\extraction\ports\textract-job-store.ts:23:  updateConditional(job: TextractJob, expected: { version: number }): Promise<boolean>;
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:1:/** Real DynamoDB adapter for `TextractJobStore`. Uses the shared single table (main
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:2: * `TABLE_NAME`) â€” `TextractJob` is keyed by `jobId` alone (see domain/textract-job.ts's own
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:7:import type { TextractJobStore } from "../ports/textract-job-store.js";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:8:import { textractJobKey, type TextractJob } from "../domain/textract-job.js";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:11:export class DynamoDbTextractJobStore implements TextractJobStore {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:21:  async create(job: TextractJob): Promise<void> {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:31:      throw mapDynamoError(err, "TextractJobStore.create");
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:35:  async getByJobId(jobId: string): Promise<TextractJob | null> {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:38:      return (result.Item as TextractJob | undefined) ?? null;
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:40:      throw mapDynamoError(err, "TextractJobStore.getByJobId");
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:45:   * convention) â€” `TextractJob` is deliberately not tenant-scoped (see domain doc comment), so
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:47:  async updateConditional(job: TextractJob, expected: { version: number }): Promise<boolean> {
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:73:      throw mapDynamoError(err, "TextractJobStore.updateConditional");
src\modules\extraction\ports\textract-client.ts:1:/** Textract surface `TextractTaskHandler` needs â€” narrow port, no `@aws-sdk/client-textract`
src\modules\extraction\ports\textract-client.ts:4:export interface StartDocumentTextDetectionInput {
src\modules\extraction\ports\textract-client.ts:15:export interface StartDocumentTextDetectionResult {
src\modules\extraction\ports\textract-client.ts:19:export type TextractBlock = Record<string, unknown>;
src\modules\extraction\ports\textract-client.ts:21:export type TextractJobStatusResult = "SUCCEEDED" | "FAILED" | "PARTIAL_SUCCESS" | "IN_PROGRESS";
src\modules\extraction\ports\textract-client.ts:24:  status: TextractJobStatusResult;
src\modules\extraction\ports\textract-client.ts:25:  blocks: readonly TextractBlock[];
src\modules\extraction\ports\textract-client.ts:30:export interface TextractClient {
src\modules\extraction\ports\textract-client.ts:31:  startDocumentTextDetection(input: StartDocumentTextDetectionInput): Promise<StartDocumentTextDetectionResult>;
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:4:import { S3Client, PutObjectCommand, GetObjectCommand, DeleteObjectCommand } from "@aws-sdk/client-s3";
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:17:      new PutObjectCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:36:    // DeleteObjectCommand is already idempotent at the S3 API level (deleting a missing key
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:38:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
src\modules\extraction\domain\bedrock-extraction.ts:2: * `BedrockExtractionRequest`/prompt isolation contract for `BedrockExtractionTaskHandler` (M7
src\modules\extraction\domain\bedrock-extraction.ts:4: * - no AWS SDK import here (the `BedrockClient` port + its real adapter own the Converse API
src\modules\extraction\domain\bedrock-extraction.ts:13: *  - Converse API (never legacy InvokeModel's free-form body).
src\modules\extraction\domain\bedrock-extraction.ts:36:export interface BedrockTextArtifactRef {
src\modules\extraction\domain\bedrock-extraction.ts:62: * the field list it is supposed to mirror (`fieldNeedsBedrock` in `decide-bedrock.ts` is
src\modules\extraction\domain\bedrock-extraction.ts:128:/** Â§1.9: the request the application layer builds for the `BedrockClient` port - only an
src\modules\extraction\domain\bedrock-extraction.ts:130:export interface BedrockExtractionRequest {
src\modules\extraction\domain\bedrock-extraction.ts:131:  readonly textArtifact: BedrockTextArtifactRef;
src\modules\extraction\domain\bedrock-extraction.ts:138: * `BedrockExtractionFailedError`, never a partially-populated result here). */
src\modules\extraction\domain\bedrock-extraction.ts:139:export interface BedrockFieldCandidate {
src\modules\extraction\domain\bedrock-extraction.ts:145:export interface BedrockExtractionResult {
src\modules\extraction\domain\bedrock-extraction.ts:146:  readonly fields: readonly BedrockFieldCandidate[];
src\modules\document\persistence\s3-upload-url-signer.ts:7:import { PutObjectCommand, S3Client } from "@aws-sdk/client-s3";
src\modules\document\persistence\s3-upload-url-signer.ts:15:    const command = new PutObjectCommand({
src\modules\extraction\application\complete-ocr.ts:2: * `TextractTaskHandler`'s `COMPLETE_OCR` operation â€” triggered by the SQS queue subscribed to
src\modules\extraction\application\complete-ocr.ts:3: * Textract's SNS job-completion topic, NOT by Step Functions directly (`claude-reconciliation-
src\modules\extraction\application\complete-ocr.ts:4: * final-design.md` Â§1.2/Â§2/Â§3). Looks up the `TextractJob` by `jobId`, pages
src\modules\extraction\application\complete-ocr.ts:14:import { textractJobKey, type TextractJob } from "../domain/textract-job.js";
src\modules\extraction\application\complete-ocr.ts:15:import { TextractPartialFailureError } from "../../../shared/errors/app-error.js";
src\modules\extraction\application\complete-ocr.ts:16:import type { TextractClient, TextractJobStatusResult } from "../ports/textract-client.js";
src\modules\extraction\application\complete-ocr.ts:17:import type { TextractJobStore } from "../ports/textract-job-store.js";
src\modules\extraction\application\complete-ocr.ts:23:  textract: TextractClient;
src\modules\extraction\application\complete-ocr.ts:24:  jobs: TextractJobStore;
src\modules\extraction\application\complete-ocr.ts:42:async function collectPages(textract: TextractClient, jobId: string): Promise<{ status: TextractJobStatusResult; blocks: unknown[]; warnings: string[] }> {
src\modules\extraction\application\complete-ocr.ts:45:  let status: TextractJobStatusResult = "IN_PROGRESS";
src\modules\extraction\application\complete-ocr.ts:89:    // `RunTextract` on the happy path (no ResultPath on that Task's success transition), so
src\modules\extraction\application\complete-ocr.ts:108:    sendErrorCode = new TextractPartialFailureError(`Textract job ${input.jobId} did not complete usably (status=${status}).`, { jobId: input.jobId }).code;
src\modules\extraction\application\complete-ocr.ts:115:    : await deps.sender.sendTaskFailure(taskToken, sendErrorCode!, `Textract jobId=${input.jobId}`);
src\modules\extraction\application\complete-ocr.ts:124:  const updated: TextractJob = {
src\modules\extraction\domain\decide-bedrock.ts:2: * needsBedrock() â€” decides whether a run must call Bedrock, given only the candidates already
src\modules\extraction\domain\decide-bedrock.ts:3: * produced by the deterministic parser and a lightweight OCR-text scan (never a second Bedrock
src\modules\extraction\domain\decide-bedrock.ts:10: * OCR candidates) while OCR is actually available. (c) is deliberately never based on Textract
src\modules\extraction\domain\decide-bedrock.ts:28:   * Textract artifact, NEVER a second Bedrock call) - 0 means OCR suggested nothing for this
src\modules\extraction\domain\decide-bedrock.ts:34:export interface BedrockDecisionInput {
src\modules\extraction\domain\decide-bedrock.ts:37:  /** false when OCR_SKIPPED_KILL_SWITCH or the Textract job ended FAILED. */
src\modules\extraction\domain\decide-bedrock.ts:42:function fieldNeedsBedrock(field: FieldExtractionAssessment, ocrAvailable: boolean): boolean {
src\modules\extraction\domain\decide-bedrock.ts:50:export function needsBedrock(input: BedrockDecisionInput): boolean {
src\modules\extraction\domain\decide-bedrock.ts:51:  return input.fields.some((field) => fieldNeedsBedrock(field, input.ocrAvailable));
src\modules\extraction\persistence\kms-task-token-encryptor.ts:2: * Real KMS adapter for the Step Functions task-token ciphertext (`TextractJob.taskTokenCiphertext`).
src\modules\document\persistence\s3-document-object-store.ts:4:import { CopyObjectCommand, DeleteObjectCommand, HeadObjectCommand, S3Client } from "@aws-sdk/client-s3";
src\modules\document\persistence\s3-document-object-store.ts:33:    // promotion attempt - confirmed via a real CopyObjectCommand failing with an opaque
src\modules\document\persistence\s3-document-object-store.ts:38:      new CopyObjectCommand({
src\modules\document\persistence\s3-document-object-store.ts:49:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src\modules\extraction\domain\decide-field-outcome.ts:29:  // parser alone would have needed to clear to skip Bedrock in the first place (field-schema.ts
src\modules\extraction\persistence\sfn-extraction-execution-starter.ts:5:import { SFNClient, StartExecutionCommand, ExecutionAlreadyExists } from "@aws-sdk/client-sfn";
src\modules\extraction\persistence\sfn-extraction-execution-starter.ts:17:        new StartExecutionCommand({
src\modules\extraction\persistence\textract-sdk-client.ts:1:/** Real adapter for the `TextractClient` port over `@aws-sdk/client-textract`. Only this file
src\modules\extraction\persistence\textract-sdk-client.ts:2: * imports the Textract SDK â€” every caller above it (start-ocr.ts/complete-ocr.ts) only sees the
src\modules\extraction\persistence\textract-sdk-client.ts:5:  TextractClient as AwsTextractClient,
src\modules\extraction\persistence\textract-sdk-client.ts:6:  StartDocumentTextDetectionCommand,
src\modules\extraction\persistence\textract-sdk-client.ts:10:  TextractClient,
src\modules\extraction\persistence\textract-sdk-client.ts:11:  StartDocumentTextDetectionInput,
src\modules\extraction\persistence\textract-sdk-client.ts:12:  StartDocumentTextDetectionResult,
src\modules\extraction\persistence\textract-sdk-client.ts:14:  TextractJobStatusResult,
src\modules\extraction\persistence\textract-sdk-client.ts:17:function mapStatus(status: string | undefined): TextractJobStatusResult {
src\modules\extraction\persistence\textract-sdk-client.ts:22:export class TextractSdkClient implements TextractClient {
src\modules\extraction\persistence\textract-sdk-client.ts:23:  constructor(private readonly client: AwsTextractClient) {}
src\modules\extraction\persistence\textract-sdk-client.ts:25:  async startDocumentTextDetection(input: StartDocumentTextDetectionInput): Promise<StartDocumentTextDetectionResult> {
src\modules\extraction\persistence\textract-sdk-client.ts:27:      new StartDocumentTextDetectionCommand({
src\modules\extraction\persistence\textract-sdk-client.ts:35:      throw new Error("Textract StartDocumentTextDetection returned no JobId.");
src\modules\extraction\persistence\textract-sdk-client.ts:51:export function createTextractClient(): AwsTextractClient {
src\modules\extraction\persistence\textract-sdk-client.ts:52:  return new AwsTextractClient({});
src\modules\extraction\domain\deterministic-field-parser.ts:3: * Â§1.3). Pure function over already-OCR'd text (Textract LINE blocks joined), never over raw
src\modules\extraction\domain\deterministic-field-parser.ts:5: * plain `arn:aws:states:::lambda:invoke`, no Bedrock/Textract/DynamoDB access at all.
src\modules\extraction\domain\deterministic-field-parser.ts:9: * unbounded parser â€” the OCR text itself already went through Textract (trusted), but the
src\modules\extraction\domain\deterministic-field-parser.ts:18: * so it alone never resolves the field without Bedrock corroboration. */
src\modules\extraction\ports\extraction-execution-starter.ts:15:  /** RunTextract's START_OCR operation (`start-ocr.ts`) requires these to classify the document
src\modules\extraction\ports\extraction-execution-starter.ts:16:   * type before calling Textract - real bug found 2026-08-27 verifying the pipeline end-to-end
src\modules\extraction\ports\bedrock-client.ts:1:/** Narrow port over Bedrock's Converse API - no `@aws-sdk/client-bedrock-runtime` import
src\modules\extraction\ports\bedrock-client.ts:2: * outside the adapter (same discipline as `TextractClient`/`OcrArtifactStore`). The adapter
src\modules\extraction\ports\bedrock-client.ts:6:import type { BedrockExtractionRequest, BedrockExtractionResult } from "../domain/bedrock-extraction.js";
src\modules\extraction\ports\bedrock-client.ts:8:export interface BedrockClient {
src\modules\extraction\ports\bedrock-client.ts:9:  /** Throws `BedrockExtractionFailedError` (never returns a partial/best-effort result) when:
src\modules\extraction\ports\bedrock-client.ts:13:  extract(request: BedrockExtractionRequest): Promise<BedrockExtractionResult>;
src\modules\extraction\ports\feature-flags-reader.ts:3: * WHATSAPP). `TextractTaskHandler` is the first real consumer (D-035 Â§1.5 â€” "aplicaÃ§Ã£o
src\modules\extraction\domain\document-classifier.ts:2: * Heuristic document-type classifier â€” the first internal step of `RunTextract`'s `START_OCR`
src\modules\extraction\domain\document-classifier.ts:5: * Textract call). Textract itself never classifies document type â€” this is pure heuristic
src\modules\extraction\domain\document-classifier.ts:13:/** Textract's own supported input formats for StartDocumentTextDetection. */
src\modules\extraction\application\run-bedrock-extraction.ts:2: * `BedrockExtractionTaskHandler`'s single operation (M7 item 6, D-035 Â§1.9/Â§1.11) - the ASL's
src\modules\extraction\application\run-bedrock-extraction.ts:3: * `RunBedrock` state (`arn:aws:states:::lambda:invoke`, plain synchronous invocation, same
src\modules\extraction\application\run-bedrock-extraction.ts:5: * `RunDeterministicParser`'s output said `needsBedrock: true` AND `aiExtractionEnabled: true`
src\modules\extraction\application\run-bedrock-extraction.ts:6: * (the ASL's `NeedsBedrock`/`CheckAiKillSwitch` Choice states), but this function re-checks the
src\modules\extraction\application\run-bedrock-extraction.ts:12: * - never reimplements `needsBedrock()`/`decide-bedrock.ts`'s contract, and never re-reads the
src\modules\extraction\application\run-bedrock-extraction.ts:15:import { AiExtractionDisabledError, BedrockExtractionFailedError, QuotaExceededError } from "../../../shared/errors/app-error.js";
src\modules\extraction\application\run-bedrock-extraction.ts:17:import type { BedrockClient } from "../ports/bedrock-client.js";
src\modules\extraction\application\run-bedrock-extraction.ts:23:export interface RunBedrockExtractionDeps {
src\modules\extraction\application\run-bedrock-extraction.ts:26:  bedrock: BedrockClient;
src\modules\extraction\application\run-bedrock-extraction.ts:27:  /** Bounded local retry before propagating `BedrockExtractionFailedError` - same discipline as
src\modules\extraction\application\run-bedrock-extraction.ts:34:export interface RunBedrockExtractionFieldInput {
src\modules\extraction\application\run-bedrock-extraction.ts:45:export interface RunBedrockExtractionInput {
src\modules\extraction\application\run-bedrock-extraction.ts:53:  extractedFields: RunBedrockExtractionFieldInput[];
src\modules\extraction\application\run-bedrock-extraction.ts:54:  needsBedrock: boolean;
src\modules\extraction\application\run-bedrock-extraction.ts:59:export interface BedrockFieldCandidateOutput {
src\modules\extraction\application\run-bedrock-extraction.ts:71: * SINGLE_SOURCE) is item 7's job, this function only reports what Bedrock itself produced. */
src\modules\extraction\application\run-bedrock-extraction.ts:72:export interface RunBedrockExtractionOutput {
src\modules\extraction\application\run-bedrock-extraction.ts:79:  bedrockFields: BedrockFieldCandidateOutput[];
src\modules\extraction\application\run-bedrock-extraction.ts:82:  // RunBedrock -> ValidateSchema transition has no separate merge step, so whatever this
src\modules\extraction\application\run-bedrock-extraction.ts:84:  // extractedFields/artifact/needsBedrock/aiExtractionEnabled would be silently lost on every
src\modules\extraction\application\run-bedrock-extraction.ts:85:  // run that actually reaches Bedrock, the exact class of ResultPath bug item 5 already found
src\modules\extraction\application\run-bedrock-extraction.ts:89:  extractedFields: RunBedrockExtractionFieldInput[];
src\modules\extraction\application\run-bedrock-extraction.ts:90:  needsBedrock: boolean;
src\modules\extraction\application\run-bedrock-extraction.ts:97:export async function runBedrockExtraction(deps: RunBedrockExtractionDeps, input: RunBedrockExtractionInput): Promise<RunBedrockExtractionOutput> {
src\modules\extraction\application\run-bedrock-extraction.ts:111:    // No OCR text artifact was ever produced for this run (RunTextract failed, or OCR was
src\modules\extraction\application\run-bedrock-extraction.ts:112:    // disabled) - there is nothing for Bedrock to read. This is not itself an error: the ASL
src\modules\extraction\application\run-bedrock-extraction.ts:113:    // only reaches RunBedrock when needsBedrock was true, and needsBedrock's rule (a) treats
src\modules\extraction\application\run-bedrock-extraction.ts:114:    // "no deterministic candidate" as needing Bedrock even with zero OCR text available, so a
src\modules\extraction\application\run-bedrock-extraction.ts:115:    // caller CAN legitimately reach here with no artifact. Bedrock cannot help without any
src\modules\extraction\application\run-bedrock-extraction.ts:130:      needsBedrock: input.needsBedrock,
src\modules\extraction\application\run-bedrock-extraction.ts:141:  // QuotaExceededError on its own prior reservation - never a second paid Bedrock call for an
src\modules\extraction\application\run-bedrock-extraction.ts:151:    // proceed to call Bedrock exactly once for this attempt - the guarantee this quota key
src\modules\extraction\application\run-bedrock-extraction.ts:152:    // provides is "at most one concurrent/duplicate EXECUTION reserves this run's Bedrock call
src\modules\extraction\application\run-bedrock-extraction.ts:154:    // genuinely already-answered run never reaches RunBedrock again because CompleteRun (item 7)
src\modules\extraction\application\run-bedrock-extraction.ts:169:      const bedrockFields: BedrockFieldCandidateOutput[] = result.fields.map((f) => ({
src\modules\extraction\application\run-bedrock-extraction.ts:188:        needsBedrock: input.needsBedrock,
src\modules\extraction\application\run-bedrock-extraction.ts:199:  // failed call does not permanently burn this run's one Bedrock attempt.
src\modules\extraction\application\run-bedrock-extraction.ts:201:  throw new BedrockExtractionFailedError(`Bedrock extraction failed for run ${input.runId} after ${attempts} attempt(s).`, {
src\modules\extraction\ports\ocr-artifact-store.ts:4: * (never by `TextractTaskHandler` itself, design Â§3), 24h S3 lifecycle as safety net only. */
src\modules\extraction\ports\ocr-artifact-store.ts:11:  /** `blocksJson` is the serialized Textract block array â€” never logged, never placed on an
src\modules\extraction\ports\ocr-artifact-store.ts:15:  /** Reads back the serialized Textract block array written by `put()` â€” the two real
src\modules\extraction\ports\ocr-artifact-store.ts:16:   * consumers are `PdfParserTaskHandler` (item 5) and `BedrockExtractionTaskHandler` (item 6),
src\modules\extraction\ports\ocr-artifact-store.ts:26:   * path / the concurrent-discard path â€” never from `TextractTaskHandler`'s `COMPLETE_OCR`
src\modules\extraction\application\run-extraction-validation.ts:77:  needsBedrock?: boolean;
src\modules\extraction\application\run-extraction-validation.ts:322:  // never have been created at all, if RunTextract itself failed before the parser even ran) is
src\modules\extraction\domain\extracted-field.ts:16:/** Which extractor(s) produced a candidate for this field. Bedrock only appears here when
src\modules\extraction\domain\extracted-field.ts:17: * `decide-bedrock.ts`'s needsBedrock() returned true for this field. */
src\modules\extraction\application\start-ocr.ts:2: * `TextractTaskHandler`'s `START_OCR` operation â€” the single `RunTextract` invocation the ASL
src\modules\extraction\application\start-ocr.ts:11:  deriveTextractClientRequestToken,
src\modules\extraction\application\start-ocr.ts:12:  computeTextractJobTtl,
src\modules\extraction\application\start-ocr.ts:14:  type TextractJob,
src\modules\extraction\application\start-ocr.ts:19:  TextractJobPersistenceFailedError,
src\modules\extraction\application\start-ocr.ts:20:  TextractUnsupportedDocumentError,
src\modules\extraction\application\start-ocr.ts:24:import type { TextractClient } from "../ports/textract-client.js";
src\modules\extraction\application\start-ocr.ts:25:import type { TextractJobStore } from "../ports/textract-job-store.js";
src\modules\extraction\application\start-ocr.ts:32:  textract: TextractClient;
src\modules\extraction\application\start-ocr.ts:33:  jobs: TextractJobStore;
src\modules\extraction\application\start-ocr.ts:37:  /** Bounded local retry before propagating `TextractJobPersistenceFailedError`, per design Â§2
src\modules\extraction\application\start-ocr.ts:38:   * ("RecuperaÃ§Ã£o do intervalo StartDocumentTextDetection -> persistÃªncia"). */
src\modules\extraction\application\start-ocr.ts:92:  const clientRequestToken = deriveTextractClientRequestToken(input.tenantId, input.documentId, input.documentVersion, input.pipelineVersion, input.runId);
src\modules\extraction\application\start-ocr.ts:106:    // Reservation was never consumed by Textract (the call itself failed) â€” compensate per
src\modules\extraction\application\start-ocr.ts:109:    throw new TextractUnsupportedDocumentError(`StartDocumentTextDetection failed for document ${input.documentId}.`, { documentId: input.documentId, cause: err instanceof Error ? err.message : String(err) });
src\modules\extraction\application\start-ocr.ts:114:  const job: TextractJob = {
src\modules\extraction\application\start-ocr.ts:116:    entityType: "TextractJob",
src\modules\extraction\application\start-ocr.ts:127:    ttl: computeTextractJobTtl(now),
src\modules\extraction\application\start-ocr.ts:143:  throw new TextractJobPersistenceFailedError(`Failed to persist TextractJob ${jobId} after ${attempts} attempt(s).`, {
src\modules\extraction\domain\textract-job.ts:2: * TextractJob â€” transient correlation record between a Step Functions `waitForTaskToken`
src\modules\extraction\domain\textract-job.ts:7: * `COMPLETE_OCR` is triggered by a Textract SNS notification that only carries `JobId`/`JobTag`,
src\modules\extraction\domain\textract-job.ts:23:export type TextractJobStatus = "STARTED" | "COMPLETED" | "FAILED";
src\modules\extraction\domain\textract-job.ts:25:export interface TextractJob extends EntityKey {
src\modules\extraction\domain\textract-job.ts:26:  entityType: "TextractJob";
src\modules\extraction\domain\textract-job.ts:34:   * REPLACES the whole Step Functions `$` (no `ResultPath`, per the ASL's `RunTextract`
src\modules\extraction\domain\textract-job.ts:36:   * pipelineVersion/...) would otherwise be lost to every state after `RunTextract` on the
src\modules\extraction\domain\textract-job.ts:43:  status: TextractJobStatus;
src\modules\extraction\domain\textract-job.ts:59:/** `ClientRequestToken` for `StartDocumentTextDetection` â€” deterministic so a retried
src\modules\extraction\domain\textract-job.ts:61: * the SAME Textract job instead of starting a second paid OCR pass, per design Â§1.2/Â§2. */
src\modules\extraction\domain\textract-job.ts:62:export function deriveTextractClientRequestToken(
src\modules\extraction\domain\textract-job.ts:76:export function computeTextractJobTtl(nowIso: string): number {
src\modules\extraction\application\run-deterministic-parser.ts:5: * `RunTextract`). Runs entirely deterministic, non-AI logic: read the OCR artifact (if any),
src\modules\extraction\application\run-deterministic-parser.ts:6: * scan it for `expirationDate` candidates, decide whether Bedrock is needed
src\modules\extraction\application\run-deterministic-parser.ts:7: * (`decide-bedrock.ts`'s `needsBedrock()`), and read the `AI_EXTRACTION` kill switch so the
src\modules\extraction\application\run-deterministic-parser.ts:10: * Reached from the ASL in TWO distinct shapes (design Â§1.2, "RunTextract failure above...
src\modules\extraction\application\run-deterministic-parser.ts:22: * required field, which `needsBedrock()` rule (a) already treats correctly (no candidate =
src\modules\extraction\application\run-deterministic-parser.ts:23: * needs Bedrock, or PENDING_CONFIRMATION if Bedrock is off/unavailable) - never fabricates a
src\modules\extraction\application\run-deterministic-parser.ts:27:import { needsBedrock, type BedrockDecisionInput, type FieldExtractionAssessment } from "../domain/decide-bedrock.js";
src\modules\extraction\application\run-deterministic-parser.ts:46:  /** Present (`true`) only on the happy path (`RunTextract` succeeded). Absent/false on the
src\modules\extraction\application\run-deterministic-parser.ts:47:   * degraded path (`RunTextract` failed - `$.ocrFailure` carries the Catch details, not
src\modules\extraction\application\run-deterministic-parser.ts:70:  needsBedrock: boolean;
src\modules\extraction\application\run-deterministic-parser.ts:72:  /** Re-attached only when `ocrAvailable` - the SAME artifact ref `BedrockExtractionTaskHandler`
src\modules\extraction\application\run-deterministic-parser.ts:73:   * (item 6) reads via `OcrArtifactStore.get()` when `needsBedrock`/`aiExtractionEnabled` route
src\modules\extraction\application\run-deterministic-parser.ts:74:   * the ASL to `RunBedrock`. Never a second Textract read - this is the identical S3 object
src\modules\extraction\application\run-deterministic-parser.ts:79:interface TextractLineBlock {
src\modules\extraction\application\run-deterministic-parser.ts:92:    throw new DeterministicParserFailedError("OCR artifact JSON is not an array of Textract blocks.");
src\modules\extraction\application\run-deterministic-parser.ts:94:  return (blocks as TextractLineBlock[])
src\modules\extraction\application\run-deterministic-parser.ts:140:  const decisionInput: BedrockDecisionInput = { fields: assessments, ocrAvailable, thresholdVersion: THRESHOLD_VERSION_V1 };
src\modules\extraction\application\run-deterministic-parser.ts:141:  const bedrockNeeded = needsBedrock(decisionInput);
src\modules\extraction\application\run-deterministic-parser.ts:162:    needsBedrock: bedrockNeeded,
src\modules\extraction\application\start-extraction-run.ts:90:      // RunTextract's classifier needs these (see ExtractionExecutionInput's doc comment) -
src\modules\extraction\domain\field-schema.ts:23: * deterministic candidate alone is not trusted enough - Textract confidence is about
src\modules\notification\providers\ses-email-adapter.ts:13:import { SESv2Client, SendEmailCommand } from "@aws-sdk/client-sesv2";
src\modules\notification\providers\ses-email-adapter.ts:47:        new SendEmailCommand({

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/notification/application/email-delivery-workflow.ts'; Get-Content -Raw 'src/modules/extraction/application/run-bedrock-extraction.ts'; Get-Content -Raw 'src/modules/extraction/application/start-ocr.ts'; Get-Content -Raw 'src/modules/import/persistence/s3-import-object-store.ts'; rg -n 'putObject\\(|S3ImportObjectStore|ImportObjectStore|presign' src/modules/import src/runtime/aws/handlers" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 205ms:
/**
 * EmailDeliveryWorker composition-root logic (M4). Same role as
 * notification-router-workflow.ts: loads entities with consistent reads, calls the pure
 * decision functions (email-delivery.ts, corrective-intent-service.ts), translates the
 * result into real DynamoDB writes + (only on the SEND path) one external SES call.
 *
 * The SQS command (`notification.email-deliver.v1`) carries `attemptId` but not the
 * attempt's full SK (which embeds `attemptNumber`) - this worker resolves it via the SAME
 * NotificationAttemptLookup pointer the SesCallbackWorker uses (round3-fixes.md item 1),
 * reused here rather than inventing a second correlation mechanism for the same problem.
 */
import { itemKey, type ExpirationItem } from "../../expiration/domain/expiration-item.js";
import type { NotificationIntent } from "../../reminder/domain/notification-intent.js";
import {
  notificationAttemptLookupKey,
  type NotificationAttempt,
  type NotificationAttemptLookup,
} from "../domain/notification-attempt.js";
import type { NotificationStore } from "../ports/notification-store.js";
import { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
import type { EmailProviderAdapter, EmailSendResult } from "../ports/email-provider.js";
import { EmailSendError } from "../ports/email-provider.js";
import { decideSendAction, nextStatusAfterSendAttempt } from "./email-delivery.js";
import { applyStaleDeliveryDecision } from "./notification-router-workflow.js";

export interface EmailDeliverCommandData {
  tenantId: string;
  intentId: string;
  attemptId: string;
  itemId: string;
  expectedItemVersion: number;
  templateId: string;
  templateVersion: number;
  locale: string;
  deliverNotBefore: string;
  correlationId: string;
}

export interface EmailDeliveryWorkflowDeps {
  store: NotificationStore;
  tableName: string;
  emailProvider: EmailProviderAdapter;
  resolveRecipientEmail: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
  renderTemplate: (input: { templateId: string; templateVersion: number; locale: string; item: ExpirationItem }) => Record<string, unknown>;
  now: () => string;
  newIntentId: () => string;
  leaseDurationMs?: number;
}

export type EmailDeliveryOutcome =
  | { kind: "DEFERRED" } // deliverNotBefore not reached yet - caller reschedules, never discards
  | { kind: "SKIPPED_NO_ATTEMPT" } // lookup/attempt missing - logged upstream, not a crash
  | { kind: "SKIPPED_IN_PROGRESS" }
  | { kind: "SKIPPED_RESOLVED" }
  | { kind: "RECONCILED_UNKNOWN" }
  | { kind: "SKIPPED_LOST_LEASE_RACE" } // another invocation already claimed this SUBMITTING transition
  | { kind: "NOT_SENT_STALE"; correctiveKind: "REPLACEMENT" | "CORRECTIVE" }
  | { kind: "SENT"; providerMessageId: string }
  | { kind: "SEND_FAILED"; nextStatus: string };

export async function processEmailDelivery(deps: EmailDeliveryWorkflowDeps, command: EmailDeliverCommandData): Promise<EmailDeliveryOutcome> {
  const now = deps.now();
  if (Date.parse(command.deliverNotBefore) > Date.parse(now)) {
    return { kind: "DEFERRED" };
  }

  const lookup = await deps.store.get<NotificationAttemptLookup>(notificationAttemptLookupKey(command.tenantId, command.attemptId), true);
  if (!lookup) return { kind: "SKIPPED_NO_ATTEMPT" };

  const intentPk = `TENANT#${lookup.tenantId}#INTENT#${lookup.intentId}`;
  const attempt = await deps.store.get<NotificationAttempt>({ PK: intentPk, SK: lookup.attemptSk }, true);
  if (!attempt) return { kind: "SKIPPED_NO_ATTEMPT" };

  const leaseDurationMs = deps.leaseDurationMs ?? 5 * 60_000;
  const action = decideSendAction({ status: attempt.status, leaseExpiresAt: attempt.leaseExpiresAt }, now);

  if (action.action === "SKIP_IN_PROGRESS") return { kind: "SKIPPED_IN_PROGRESS" };
  if (action.action === "SKIP_RESOLVED") return { kind: "SKIPPED_RESOLVED" };

  if (action.action === "RECONCILE_UNKNOWN") {
    try {
      await deps.store.update({
        ...attempt,
        status: "UNKNOWN",
        version: attempt.version + 1,
        updatedAt: now,
      });
    } catch {
      // Best-effort - a concurrent writer already resolved this attempt out of SUBMITTING.
    }
    return { kind: "RECONCILED_UNKNOWN" };
  }

  // action.action === "SEND" from here.
  const item = await deps.store.get<ExpirationItem>(itemKey(command.tenantId, command.itemId), true);
  const intent = await deps.store.get<NotificationIntent>({ PK: intentPk, SK: "META" }, true);

  const isStale = !item || item.status !== "ACTIVE" || item.version !== command.expectedItemVersion;
  if (isStale) {
    const marked = await tryConditionalUpdate(deps, attempt, { status: "NOT_SENT_STALE" }, now);
    if (!marked) return { kind: "SKIPPED_LOST_LEASE_RACE" };

    let correctiveKind: "REPLACEMENT" | "CORRECTIVE" = "REPLACEMENT"; // attempt never left PREPARED/FAILED_RETRYABLE before this point
    if (intent && item) {
      const outcome = await applyStaleDeliveryDecision(deps, intent, correctiveKind, now, item.version, intent.policyVersion);
      correctiveKind = outcome.correctiveKind;
    }
    return { kind: "NOT_SENT_STALE", correctiveKind };
  }

  const leaseExpiresAt = new Date(Date.parse(now) + leaseDurationMs).toISOString();
  const claimedSubmitting = await tryConditionalUpdate(deps, attempt, { status: "SUBMITTING", leaseExpiresAt, submitStartedAt: now }, now);
  if (!claimedSubmitting) return { kind: "SKIPPED_LOST_LEASE_RACE" };

  const recipientUserId = intent?.recipientUserId;
  const to = recipientUserId ? await deps.resolveRecipientEmail({ tenantId: command.tenantId, userId: recipientUserId }) : undefined;

  if (!to || !item) {
    // Can't send without a resolved address - conclusive terminal failure, not ambiguous.
    const nextStatus = nextStatusAfterSendAttempt({ kind: "FAILURE", failureKind: "CONCLUSIVE_TERMINAL" });
    await forceUpdateAttemptStatus(deps, { ...attempt, status: "SUBMITTING", version: attempt.version + 1 }, nextStatus, now);
    return { kind: "SEND_FAILED", nextStatus };
  }

  let sendResult: EmailSendResult;
  try {
    sendResult = await deps.emailProvider.send({
      to,
      templateId: command.templateId,
      templateVersion: command.templateVersion,
      locale: command.locale,
      renderContext: deps.renderTemplate({ templateId: command.templateId, templateVersion: command.templateVersion, locale: command.locale, item }),
      tags: { attemptId: attempt.attemptId, intentId: command.intentId, tenantId: command.tenantId, correlationId: command.correlationId },
    });
  } catch (err) {
    const failureKind = err instanceof EmailSendError ? err.kind : "AMBIGUOUS";
    const nextStatus = nextStatusAfterSendAttempt({ kind: "FAILURE", failureKind });
    await forceUpdateAttemptStatus(deps, { ...attempt, status: "SUBMITTING", version: attempt.version + 1 }, nextStatus, now);
    return { kind: "SEND_FAILED", nextStatus };
  }

  const nextStatus = nextStatusAfterSendAttempt({ kind: "ACCEPTED", providerMessageId: sendResult.providerMessageId });
  await forceUpdateAttemptStatus(deps, { ...attempt, status: "SUBMITTING", version: attempt.version + 1 }, nextStatus, now, sendResult.providerMessageId);
  return { kind: "SENT", providerMessageId: sendResult.providerMessageId };
}

/** OCC-conditional update FROM the attempt's currently-known status - if another invocation
 * already moved it (lease race), this returns false rather than overwriting concurrent
 * progress. */
async function tryConditionalUpdate(
  deps: EmailDeliveryWorkflowDeps,
  attempt: NotificationAttempt,
  set: Record<string, unknown>,
  now: string,
): Promise<boolean> {
  try {
    await deps.store.transactWrite([
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: { PK: attempt.PK, SK: attempt.SK },
          tenantId: attempt.tenantId,
          expectedVersion: attempt.version,
          now,
          set,
        }),
      },
    ]);
    return true;
  } catch (err) {
    if (isTransactionCanceled(err) || isConditionalCheckFailed(err)) return false;
    throw err;
  }
}

/** Post-SEND status transition - the attempt was already moved to SUBMITTING (with a
 * bumped version) immediately before the external call, so this update's expected version
 * is that bumped value. Failure here (e.g. concurrent redrive) is logged upstream; the
 * attempt may be reconciled to UNKNOWN by a later invocation instead, which is acceptable
 * per the at-most-once policy. */
async function forceUpdateAttemptStatus(
  deps: EmailDeliveryWorkflowDeps,
  submittingAttempt: NotificationAttempt,
  nextStatus: string,
  now: string,
  providerMessageId?: string,
): Promise<void> {
  try {
    await deps.store.transactWrite([
      {
        Update: buildVersionedUpdate({
          tableName: deps.tableName,
          key: { PK: submittingAttempt.PK, SK: submittingAttempt.SK },
          tenantId: submittingAttempt.tenantId,
          expectedVersion: submittingAttempt.version,
          now,
          set: {
            status: nextStatus,
            ...(providerMessageId ? { providerMessageId, acceptedAt: now } : {}),
            completedAt: now,
          },
        }),
      },
    ]);
  } catch (err) {
    if (!isTransactionCanceled(err) && !isConditionalCheckFailed(err)) throw err;
  }
}

/**
 * `BedrockExtractionTaskHandler`'s single operation (M7 item 6, D-035 Ã‚Â§1.9/Ã‚Â§1.11) - the ASL's
 * `RunBedrock` state (`arn:aws:states:::lambda:invoke`, plain synchronous invocation, same
 * shape as item 5's `RunDeterministicParser` - no task token). Reached only when
 * `RunDeterministicParser`'s output said `needsBedrock: true` AND `aiExtractionEnabled: true`
 * (the ASL's `NeedsBedrock`/`CheckAiKillSwitch` Choice states), but this function re-checks the
 * kill switch itself (defense in depth, never the only gate - the ASL Choice can be bypassed by
 * a hand-crafted `StartExecution` input in theory, so the paid call must never trust its own
 * caller blindly).
 *
 * Consumes exactly what item 5's `runDeterministicParser()` produces (`RunDeterministicParserOutput`)
 * - never reimplements `needsBedrock()`/`decide-bedrock.ts`'s contract, and never re-reads the
 * OCR artifact through a different path than the one `PdfParserTaskHandler` already resolved.
 */
import { AiExtractionDisabledError, BedrockExtractionFailedError, QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { FeatureFlagsReader } from "../ports/feature-flags-reader.js";
import type { BedrockClient } from "../ports/bedrock-client.js";
import type { ExtractionArtifactRef } from "../ports/ocr-artifact-store.js";
import { TenantQuotaService } from "../../identity/application/quota.js";
import type { ExtractedFieldValueType } from "../domain/extracted-field.js";
import { BEDROCK_SYSTEM_PROMPT_VERSION } from "../domain/bedrock-extraction.js";

export interface RunBedrockExtractionDeps {
  featureFlags: FeatureFlagsReader;
  quota: TenantQuotaService;
  bedrock: BedrockClient;
  /** Bounded local retry before propagating `BedrockExtractionFailedError` - same discipline as
   * `start-ocr.ts`'s `jobPersistAttempts`, but here the "operation" being retried is the model
   * call itself, so this defaults low (a single retry) to avoid amplifying the cost-abuse
   * surface a flaky retry loop would create. */
  callAttempts?: number;
}

export interface RunBedrockExtractionFieldInput {
  fieldName: string;
  valueType: ExtractedFieldValueType;
  candidateValue?: string;
  confidence?: number;
  source: "DETERMINISTIC_PARSER";
}

/** Mirrors `RunDeterministicParserOutput` exactly - the ASL passes item 5's output straight
 * through as this handler's input (`Parameters.Payload.$: "$"` after the `RunDeterministicParser`
 * task's `OutputPath` strips the `lambda:invoke` wrapper - see `document-extraction.asl.json`). */
export interface RunBedrockExtractionInput {
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  ocrAvailable: boolean;
  extractedFields: RunBedrockExtractionFieldInput[];
  needsBedrock: boolean;
  aiExtractionEnabled: boolean;
  artifact?: ExtractionArtifactRef;
}

export interface BedrockFieldCandidateOutput {
  fieldName: string;
  valueType: ExtractedFieldValueType;
  candidateValue?: string;
  confidence?: number;
  source: "BEDROCK";
}

/** Shaped for item 7 (`ExtractionValidationTaskHandler`'s `CompareExtractors`) to consume
 * alongside item 5's `extractedFields` (deterministic) - same discipline items 4/5 already
 * followed ("assemble what the next real consumer needs"). Deliberately does NOT merge/compare
 * against the deterministic candidates itself - `ExtractionAgreement` (MATCH/MISMATCH/
 * SINGLE_SOURCE) is item 7's job, this function only reports what Bedrock itself produced. */
export interface RunBedrockExtractionOutput {
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  bedrockFields: BedrockFieldCandidateOutput[];
  bedrockSystemPromptVersion: string;
  // Passthrough of RunDeterministicParser's own output (item 7 finding, D-035 Ã‚Â§2: the ASL's
  // RunBedrock -> ValidateSchema transition has no separate merge step, so whatever this
  // function returns IS $ for ValidateSchema - without echoing these back, item 5's
  // extractedFields/artifact/needsBedrock/aiExtractionEnabled would be silently lost on every
  // run that actually reaches Bedrock, the exact class of ResultPath bug item 5 already found
  // and fixed once for completeOcr's own success payload). Never recomputed - these are the
  // SAME values `input` already carried in, just carried back out.
  ocrAvailable: boolean;
  extractedFields: RunBedrockExtractionFieldInput[];
  needsBedrock: boolean;
  aiExtractionEnabled: boolean;
  artifact?: ExtractionArtifactRef;
}

const AI_CALL_RESERVATION_WINDOW_SECONDS = 7 * 24 * 60 * 60;

export async function runBedrockExtraction(deps: RunBedrockExtractionDeps, input: RunBedrockExtractionInput): Promise<RunBedrockExtractionOutput> {
  let flags;
  try {
    flags = await deps.featureFlags.getFlags();
  } catch {
    // Fail-closed (mandatory, per feature-flags-reader.ts's own contract) - any read error is
    // treated identically to AI_EXTRACTION=false, never as "unknown, proceed".
    throw new AiExtractionDisabledError("Feature flags could not be read; failing closed.", { documentId: input.documentId, runId: input.runId });
  }
  if (!flags.AI_EXTRACTION) {
    throw new AiExtractionDisabledError("AI_EXTRACTION kill switch is off.", { documentId: input.documentId, runId: input.runId });
  }

  if (!input.artifact) {
    // No OCR text artifact was ever produced for this run (RunTextract failed, or OCR was
    // disabled) - there is nothing for Bedrock to read. This is not itself an error: the ASL
    // only reaches RunBedrock when needsBedrock was true, and needsBedrock's rule (a) treats
    // "no deterministic candidate" as needing Bedrock even with zero OCR text available, so a
    // caller CAN legitimately reach here with no artifact. Bedrock cannot help without any
    // document text either, so this returns zero candidates rather than calling the model with
    // an empty document (which would waste a paid call and a quota reservation for a
    // guaranteed-empty answer).
    return {
      tenantId: input.tenantId,
      itemId: input.itemId,
      documentId: input.documentId,
      documentVersion: input.documentVersion,
      runId: input.runId,
      pipelineVersion: input.pipelineVersion,
      bedrockFields: [],
      bedrockSystemPromptVersion: BEDROCK_SYSTEM_PROMPT_VERSION,
      ocrAvailable: input.ocrAvailable,
      extractedFields: input.extractedFields,
      needsBedrock: input.needsBedrock,
      aiExtractionEnabled: input.aiExtractionEnabled,
      artifact: input.artifact,
    };
  }

  // Idempotency key mirrors start-ocr.ts's exact pattern (design Ã‚Â§1.11's 14th adversarial case
  // - cost-abuse via repeated reprocessing of an unchanged document): `runId` is itself
  // deterministic from tenantId|documentId|documentVersion|pipelineVersion
  // (deriveExtractionRunId(), extraction-run.ts), so a retried/duplicate Step Functions
  // execution for the SAME document version reserves against the SAME quota window and hits
  // QuotaExceededError on its own prior reservation - never a second paid Bedrock call for an
  // unchanged document.
  const quotaWindow = `${input.runId}|BEDROCK`;
  try {
    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
  } catch (err) {
    if (!(err instanceof QuotaExceededError)) throw err;
    // Same run already reserved AI_CALL/BEDROCK on a prior attempt. Idempotent replay: since the
    // actual model call has not happened yet at this point in a genuinely fresh attempt, and a
    // reservation here can only pre-exist from an earlier attempt of the SAME run, we still
    // proceed to call Bedrock exactly once for this attempt - the guarantee this quota key
    // provides is "at most one concurrent/duplicate EXECUTION reserves this run's Bedrock call
    // slot", not a second layer of dedup on top of the ASL's own single-path execution. A
    // genuinely already-answered run never reaches RunBedrock again because CompleteRun (item 7)
    // is terminal.
  }

  const attempts = Math.max(1, deps.callAttempts ?? 1);
  let lastErr: unknown;
  for (let i = 0; i < attempts; i++) {
    try {
      const result = await deps.bedrock.extract({
        textArtifact: input.artifact,
        pipelineVersion: input.pipelineVersion,
        systemPromptVersion: BEDROCK_SYSTEM_PROMPT_VERSION,
      });

      const schemaByName = new Map(input.extractedFields.map((f) => [f.fieldName, f.valueType]));
      const bedrockFields: BedrockFieldCandidateOutput[] = result.fields.map((f) => ({
        fieldName: f.fieldName,
        valueType: schemaByName.get(f.fieldName) ?? "STRING",
        candidateValue: f.value === "" ? undefined : f.value,
        confidence: f.confidence,
        source: "BEDROCK",
      }));

      return {
        tenantId: input.tenantId,
        itemId: input.itemId,
        documentId: input.documentId,
        documentVersion: input.documentVersion,
        runId: input.runId,
        pipelineVersion: input.pipelineVersion,
        bedrockFields,
        bedrockSystemPromptVersion: BEDROCK_SYSTEM_PROMPT_VERSION,
        ocrAvailable: input.ocrAvailable,
        extractedFields: input.extractedFields,
        needsBedrock: input.needsBedrock,
        aiExtractionEnabled: input.aiExtractionEnabled,
        artifact: input.artifact,
      };
    } catch (err) {
      lastErr = err;
    }
  }

  // The call never succeeded even after local retry - compensate the quota reservation (design
  // Ã‚Â§1.8's exact compensation pattern from start-ocr.ts) before propagating, so a genuinely
  // failed call does not permanently burn this run's one Bedrock attempt.
  await deps.quota.release({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
  throw new BedrockExtractionFailedError(`Bedrock extraction failed for run ${input.runId} after ${attempts} attempt(s).`, {
    runId: input.runId,
    documentId: input.documentId,
    cause: lastErr instanceof Error ? lastErr.message : String(lastErr),
  });
}

/**
 * `TextractTaskHandler`'s `START_OCR` operation Ã¢â‚¬â€ the single `RunTextract` invocation the ASL
 * calls via `arn:aws:states:::lambda:invoke.waitForTaskToken` (`claude-reconciliation-final-
 * design.md` Ã‚Â§1.2/Ã‚Â§2). Pure orchestration over ports, same testing convention as
 * `startExtractionRun` (hand-written fakes, no `vi.mock`). Deliberately never calls
 * `SendTaskSuccess` Ã¢â‚¬â€ on a clean return the Step Functions execution stays parked on the task
 * token until `completeOcr` (a LATER, separate SQS-triggered invocation) resolves it.
 */
import { classifyDocumentType } from "../domain/document-classifier.js";
import {
  deriveTextractClientRequestToken,
  computeTextractJobTtl,
  textractJobKey,
  type TextractJob,
} from "../domain/textract-job.js";
import {
  OcrDisabledError,
  QuotaExceededError,
  TextractJobPersistenceFailedError,
  TextractUnsupportedDocumentError,
  UnsupportedDocumentTypeError,
} from "../../../shared/errors/app-error.js";
import type { FeatureFlagsReader } from "../ports/feature-flags-reader.js";
import type { TextractClient } from "../ports/textract-client.js";
import type { TextractJobStore } from "../ports/textract-job-store.js";
import type { TaskTokenEncryptor } from "../ports/task-token-encryptor.js";
import { TenantQuotaService } from "../../identity/application/quota.js";

export interface StartOcrDeps {
  featureFlags: FeatureFlagsReader;
  quota: TenantQuotaService;
  textract: TextractClient;
  jobs: TextractJobStore;
  tokenEncryptor: TaskTokenEncryptor;
  snsTopicArn: string;
  snsRoleArn: string;
  /** Bounded local retry before propagating `TextractJobPersistenceFailedError`, per design Ã‚Â§2
   * ("RecuperaÃƒÂ§ÃƒÂ£o do intervalo StartDocumentTextDetection -> persistÃƒÂªncia"). */
  jobPersistAttempts?: number;
  now?: () => string;
}

export interface StartOcrInput {
  taskToken: string;
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  cleanObject: { bucket: string; key: string; versionId: string };
  fileName: string;
  contentType?: string;
  magicBytes?: Uint8Array;
}

/** Quota reservation window covers this run's TEXTRACT call only Ã¢â‚¬â€ `limit: 1` over a
 * long-lived window turns `TenantQuotaService.consume` into an idempotency lock keyed by
 * `runId|TEXTRACT` (design Ã‚Â§1.8's exact idempotency key) rather than a real rate cap: a retried
 * `START_OCR` invocation for the SAME run hits `QuotaExceededError` against its OWN prior
 * reservation, which this function treats as "already reserved, proceed" rather than a real
 * quota failure Ã¢â‚¬â€ see the catch block below. */
const AI_CALL_RESERVATION_WINDOW_SECONDS = 7 * 24 * 60 * 60;

export async function startOcr(deps: StartOcrDeps, input: StartOcrInput): Promise<void> {
  const documentType = classifyDocumentType({ fileName: input.fileName, contentType: input.contentType, magicBytes: input.magicBytes });
  if (!documentType) {
    throw new UnsupportedDocumentTypeError(`Cannot classify document ${input.documentId} for OCR.`, { documentId: input.documentId, fileName: input.fileName });
  }

  let flags;
  try {
    flags = await deps.featureFlags.getFlags();
  } catch {
    // Fail-closed (mandatory, per feature-flags-reader.js's own contract): any read error is
    // treated identically to OCR=false, never as "unknown, proceed".
    throw new OcrDisabledError("Feature flags could not be read; failing closed.", { documentId: input.documentId });
  }
  if (!flags.OCR) {
    throw new OcrDisabledError("OCR kill switch is off.", { documentId: input.documentId });
  }

  const quotaWindow = `${input.runId}|TEXTRACT`;
  try {
    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
  } catch (err) {
    if (!(err instanceof QuotaExceededError)) throw err;
    // Same run already reserved AI_CALL/TEXTRACT on a prior attempt Ã¢â‚¬â€ idempotent, not a real
    // quota exhaustion (a genuine cap would use a per-tenant/per-period window, not per-run).
  }

  const clientRequestToken = deriveTextractClientRequestToken(input.tenantId, input.documentId, input.documentVersion, input.pipelineVersion, input.runId);

  let jobId: string;
  try {
    const started = await deps.textract.startDocumentTextDetection({
      bucket: input.cleanObject.bucket,
      key: input.cleanObject.key,
      clientRequestToken,
      jobTag: input.runId,
      snsTopicArn: deps.snsTopicArn,
      snsRoleArn: deps.snsRoleArn,
    });
    jobId = started.jobId;
  } catch (err) {
    // Reservation was never consumed by Textract (the call itself failed) Ã¢â‚¬â€ compensate per
    // design Ã‚Â§1.8 before propagating.
    await deps.quota.release({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
    throw new TextractUnsupportedDocumentError(`StartDocumentTextDetection failed for document ${input.documentId}.`, { documentId: input.documentId, cause: err instanceof Error ? err.message : String(err) });
  }

  const now = deps.now?.() ?? new Date().toISOString();
  const taskTokenCiphertext = await deps.tokenEncryptor.encrypt(input.taskToken);
  const job: TextractJob = {
    ...textractJobKey(jobId),
    entityType: "TextractJob",
    jobId,
    tenantId: input.tenantId,
    itemId: input.itemId,
    documentId: input.documentId,
    documentVersion: input.documentVersion,
    runId: input.runId,
    pipelineVersion: input.pipelineVersion,
    clientRequestToken,
    status: "STARTED",
    taskTokenCiphertext,
    ttl: computeTextractJobTtl(now),
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const attempts = Math.max(1, deps.jobPersistAttempts ?? 2);
  let lastErr: unknown;
  for (let i = 0; i < attempts; i++) {
    try {
      await deps.jobs.create(job);
      return; // deliberately no SendTaskSuccess here Ã¢â‚¬â€ see module doc comment.
    } catch (err) {
      lastErr = err;
    }
  }
  throw new TextractJobPersistenceFailedError(`Failed to persist TextractJob ${jobId} after ${attempts} attempt(s).`, {
    jobId,
    runId: input.runId,
    cause: lastErr instanceof Error ? lastErr.message : String(lastErr),
  });
}

/** Real S3 adapter for ImportObjectStore (M11). Get/put simples - sem versionId/malware
 * scanning envolvidos (ver ports/import-object-store.ts para o porquÃƒÂª). */
import { GetObjectCommand, PutObjectCommand, type S3Client } from "@aws-sdk/client-s3";
import type { ImportObjectStore } from "../ports/import-object-store.js";

async function streamToBuffer(body: unknown): Promise<Buffer> {
  const chunks: Buffer[] = [];
  for await (const chunk of body as AsyncIterable<Uint8Array>) {
    chunks.push(Buffer.from(chunk));
  }
  return Buffer.concat(chunks);
}

export class S3ImportObjectStore implements ImportObjectStore {
  constructor(private readonly client: S3Client) {}

  async getObject(bucket: string, key: string): Promise<Buffer> {
    const result = await this.client.send(new GetObjectCommand({ Bucket: bucket, Key: key }));
    return streamToBuffer(result.Body);
  }

  async putObject(bucket: string, key: string, body: string, contentType: string): Promise<void> {
    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));
  }
}

src/modules/import\ports\import-object-store.ts:10:export interface ImportObjectStore {
src/modules/import\ports\import-object-store.ts:12:  putObject(bucket: string, key: string, body: string, contentType: string): Promise<void>;
src/modules/import\persistence\s3-import-object-store.ts:1:/** Real S3 adapter for ImportObjectStore (M11). Get/put simples - sem versionId/malware
src/modules/import\persistence\s3-import-object-store.ts:4:import type { ImportObjectStore } from "../ports/import-object-store.js";
src/modules/import\persistence\s3-import-object-store.ts:14:export class S3ImportObjectStore implements ImportObjectStore {
src/modules/import\persistence\s3-import-object-store.ts:22:  async putObject(bucket: string, key: string, body: string, contentType: string): Promise<void> {
src/modules/import\domain\import-job.ts:22:  | "UPLOADED" // presigned PUT concluÃ­do (assumido, S3 nÃ£o confirma para o backend sÃ­ncronamente) - aguardando ObjectCreated
src/modules/import\application\import-parse-service.ts:21:import type { ImportObjectStore } from "../ports/import-object-store.js";
src/modules/import\application\import-parse-service.ts:28:  objectStore: ImportObjectStore;
src/modules/import\application\import-parse-service.ts:129:    await deps.objectStore.putObject(deps.planBucket, key, planContent, "application/x-ndjson");
src/modules/import\application\import-commit-service.ts:29:import type { ImportObjectStore } from "../ports/import-object-store.js";
src/modules/import\application\import-commit-service.ts:35:  objectStore: ImportObjectStore;
src/modules/import\application\import-service.ts:2: * ImportService â€” lado HTTP de M11 (D-042): reservar upload (presigned PUT, mesmo padrÃ£o de
src/modules/import\application\import-service.ts:142:    const { uploadUrl, requiredHeaders } = await this.signer.presignUpload({

2026-08-28T21:08:15.585250Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md','src/modules/identity/application/resolve-request-context.ts','src/modules/identity/persistence/dynamodb-identity-store.ts','src/modules/extraction/persistence/dynamodb-extracted-field-store.ts','src/modules/extraction/domain/field-schema.ts','src/modules/import/application/import-parse-service.ts','src/modules/notification/application/email-delivery-workflow.ts','src/modules/extraction/application/run-bedrock-extraction.ts','src/modules/extraction/application/start-ocr.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=1; Get-Content $f | ForEach-Object { "${n}:$_"; $n++ } | Select-String -Pattern '"'fencedSingleWrite|createProfileIfAbsent|status "'!==|findOrCreate|new PutCommand|fieldPuts|transactItems|FIELD_SCHEMA|putObject|SUBMITTING|emailProvider.send|quota.consume|bedrock.extract|startDocumentTextDetection|jobs.create|dependency-cruiser|ConditionCheck|DELETED|DELETING'"' }"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md','src/modules/identity/application/resolve-request-context.ts','src/modules/identity/persistence/dynamodb-identity-store.ts','src/modules/extraction/persistence/dynamodb-extracted-field-store.ts','src/modules/extraction/domain/field-schema.ts','src/modules/import/application/import-parse-service.ts','src/modules/notification/application/email-delivery-workflow.ts','src/modules/extraction/application/run-bedrock-extraction.ts','src/modules/extraction/application/start-ocr.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=1; Get-Content $f | ForEach-Object { "${n}:$_"; $n++ } | Select-String -Pattern '"'fencedSingleWrite|createProfileIfAbsent|status "'!==|findOrCreate|new PutCommand|fieldPuts|transactItems|FIELD_SCHEMA|putObject|SUBMITTING|emailProvider.send|quota.consume|bedrock.extract|startDocumentTextDetection|jobs.create|dependency-cruiser|ConditionCheck|DELETED|DELETING'"' }" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md','src/modules/identity/application/resolve-request-context.ts','src/modules/identity/persistence/dynamodb-identity-store.ts','src/modules/extraction/persistence/dynamodb-extracted-field-store.ts','src/modules/extraction/domain/field-schema.ts','src/modules/import/application/import-parse-service.ts','src/modules/notification/application/email-delivery-workflow.ts','src/modules/extraction/application/run-bedrock-extraction.ts','src/modules/extraction/application/start-ocr.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=1; Get-Content $f | ForEach-Object { "${n}:$_"; $n++ } | Select-String -Pattern '"'fencedSingleWrite|createProfileIfAbsent|status "'!==|findOrCreate|new PutCommand|fieldPuts|transactItems|FIELD_SCHEMA|putObject|SUBMITTING|emailProvider.send|quota.consume|bedrock.extract|startDocumentTextDetection|jobs.create|dependency-cruiser|ConditionCheck|DELETED|DELETING'"' }"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'fencedSingleWrite|createProfileIfAbsent|status "'!==|findOrCreate|dependency-cruiser|DELETING|DELETED'"' docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md src/modules/identity/application/resolve-request-context.ts; rg -n 'fieldPuts|transactItems|FIELD_SCHEMA' src/modules/extraction/persistence/dynamodb-extracted-field-store.ts src/modules/extraction/domain/field-schema.ts; rg -n 'putObject|SUBMITTING|emailProvider.send|quota.consume|bedrock.extract|startDocumentTextDetection|jobs.create' src/modules/import/application/import-parse-service.ts src/modules/notification/application/email-delivery-workflow.ts src/modules/extraction/application/run-bedrock-extraction.ts src/modules/extraction/application/start-ocr.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 237ms:
src/modules/identity/application/resolve-request-context.ts:50:    const mapping = await this.identityMappings.findOrCreate(claims.sub, newUserId, newUserId);
src/modules/identity/application/resolve-request-context.ts:57:      profile = await this.users.createProfileIfAbsent({
src/modules/identity/application/resolve-request-context.ts:67:    if (profile.status !== "ACTIVE") {
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:15:   (`UserProfile.status !== "ACTIVE"`), e sÃ³ Ã© alcanÃ§ado por rotas HTTP autenticadas. A checagem Ã©
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:55:status = "ACTIVE" | "DELETING" | "DELETED"
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:62:`createProfileIfAbsent` (mesma transaÃ§Ã£o, custo zero de coordenaÃ§Ã£o adicional).
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:73:- `fencedSingleWrite(client, { tenantId, command })` â€” para os `UpdateCommand`/`PutCommand`/`DeleteCommand`
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:79:**Enforcement estrutural, nÃ£o convenÃ§Ã£o**: regra nova de `dependency-cruiser` (mesmo mecanismo que jÃ¡ proÃ­be
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:96:`createProfileIfAbsent` (nÃ£o substitui a checagem de `UserProfile.status`, Ã© adicional e anterior). Se
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:97:`status !== "ACTIVE"`, rejeita com 403 sem provisionar nada. Isso Ã© defesa em profundidade â€” mesmo que
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:98:faltasse, o Â§2 jÃ¡ impediria a escrita real do `User` recriado, porque `createProfileIfAbsent` tambÃ©m passa
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:99:pelo `fencedSingleWrite`. A checagem no resolver existe para dar um erro claro (403) em vez de deixar o
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:106:via `fencedTransactWrite` (Â§2) â€” se o tenant jÃ¡ estÃ¡ `DELETING`, a transaÃ§Ã£o de finalizaÃ§Ã£o falha
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:118:  `fencedTransactWrite`. Se o tenant jÃ¡ estÃ¡ `DELETING`, a marcaÃ§Ã£o falha e o handler nunca chama SES. Um
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:119:  e-mail cuja marcaÃ§Ã£o jÃ¡ commitou antes do `DELETING` ser escrito ainda sai (janela de milissegundos,
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:131:1. `fencedSingleWrite` grava `TenantLifecycleRecord.status = "DELETING"` (create condicional
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:140:5. ConvergÃªncia confirmada (zero itens restantes) â†’ `fencedSingleWrite` flip para `status = "DELETED"`
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:141:   (permanente; nenhum cÃ³digo reabre um tenant `DELETED` â€” fora de escopo desta feature).
docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:159:  verificÃ¡vel por `dependency-cruiser`" â€” resolve a causa raiz comum Ã s duas rodadas reprovadas.
src/modules/extraction/domain/field-schema.ts:28:export const FIELD_SCHEMA_V1: readonly FieldDefinition[] = [{ fieldName: "expirationDate", required: true, valueType: "DATE" }];
src/modules/extraction/domain/field-schema.ts:31:  if (pipelineVersion === PIPELINE_VERSION_V1) return FIELD_SCHEMA_V1;
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:31:    const fieldPuts = input.fields.map((field) => ({ Put: buildVersionedCreate(this.tableName, field as unknown as Record<string, unknown> & { PK: string; SK: string }) }));
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:67:    const transactItems = [...fieldPuts, runUpdate, documentGuard, ...itemUpdate] as unknown as TransactWriteCommandInput["TransactItems"];
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:126:    const transactItems = [fieldUpdate, runGuard, documentGuard, itemEntry] as unknown as TransactWriteCommandInput["TransactItems"];
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:155:    const transactItems = [fieldUpdate, runGuard, documentGuard] as unknown as TransactWriteCommandInput["TransactItems"];
src/modules/extraction/persistence/dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src/modules/extraction/application/run-bedrock-extraction.ts:21:import { BEDROCK_SYSTEM_PROMPT_VERSION } from "../domain/bedrock-extraction.js";
src/modules/extraction/application/run-bedrock-extraction.ts:145:    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
src/modules/extraction/application/run-bedrock-extraction.ts:162:      const result = await deps.bedrock.extract({
src/modules/extraction/application/start-ocr.ts:85:    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
src/modules/extraction/application/start-ocr.ts:96:    const started = await deps.textract.startDocumentTextDetection({
src/modules/extraction/application/start-ocr.ts:137:      await deps.jobs.create(job);
src/modules/notification/application/email-delivery-workflow.ts:57:  | { kind: "SKIPPED_LOST_LEASE_RACE" } // another invocation already claimed this SUBMITTING transition
src/modules/notification/application/email-delivery-workflow.ts:90:      // Best-effort - a concurrent writer already resolved this attempt out of SUBMITTING.
src/modules/notification/application/email-delivery-workflow.ts:113:  const claimedSubmitting = await tryConditionalUpdate(deps, attempt, { status: "SUBMITTING", leaseExpiresAt, submitStartedAt: now }, now);
src/modules/notification/application/email-delivery-workflow.ts:122:    await forceUpdateAttemptStatus(deps, { ...attempt, status: "SUBMITTING", version: attempt.version + 1 }, nextStatus, now);
src/modules/notification/application/email-delivery-workflow.ts:128:    sendResult = await deps.emailProvider.send({
src/modules/notification/application/email-delivery-workflow.ts:139:    await forceUpdateAttemptStatus(deps, { ...attempt, status: "SUBMITTING", version: attempt.version + 1 }, nextStatus, now);
src/modules/notification/application/email-delivery-workflow.ts:144:  await forceUpdateAttemptStatus(deps, { ...attempt, status: "SUBMITTING", version: attempt.version + 1 }, nextStatus, now, sendResult.providerMessageId);
src/modules/notification/application/email-delivery-workflow.ts:177:/** Post-SEND status transition - the attempt was already moved to SUBMITTING (with a
src/modules/import/application/import-parse-service.ts:124:    await deps.quota.consume({ tenantId, quotaType: "IMPORT_ROWS", window: "current", limit: 20000, windowSeconds: 60 * 60 });
src/modules/import/application/import-parse-service.ts:129:    await deps.objectStore.putObject(deps.planBucket, key, planContent, "application/x-ndjson");

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'slice\\(|BATCH|batch|entries\\.length|MAX_.*(ROW|ITEM)|20000|100' src/modules/import/application/import-service.ts src/modules/import/application/import-commit-service.ts src/modules/import/application/import-service.ts src/modules/import/application/import-parse-service.ts; rg -n 'new (PutObjectCommand|CopyObjectCommand|DeleteObjectCommand|SendMessageCommand|StartExecutionCommand)' src --glob '*.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 206ms:
src/modules/import/application/import-parse-service.ts:19:import { importJobKey, MAX_IMPORT_FILE_BYTES, MAX_IMPORT_ROWS, type ImportJob } from "../domain/import-job.js";
src/modules/import/application/import-parse-service.ts:62:    if (rows.length > MAX_IMPORT_ROWS) {
src/modules/import/application/import-parse-service.ts:124:    await deps.quota.consume({ tenantId, quotaType: "IMPORT_ROWS", window: "current", limit: 20000, windowSeconds: 60 * 60 });
src/modules/import/application/import-service.ts:6: * aqui, mesmo para preview pequeno (design: "sync sÃ³ atÃ© 128 KiB/100 linhas" fica registrado
src/modules/import/application/import-service.ts:109:    const expiresAt = new Date(Date.parse(now) + PRESIGN_TTL_SECONDS * 1000).toISOString();
src/modules/import/application/import-service.ts:121:      const jobExpiresAt = new Date(Date.parse(now) + IMPORT_JOB_TTL_SECONDS * 1000).toISOString();
src/modules/import/application/import-service.ts:6: * aqui, mesmo para preview pequeno (design: "sync sÃ³ atÃ© 128 KiB/100 linhas" fica registrado
src/modules/import/application/import-service.ts:109:    const expiresAt = new Date(Date.parse(now) + PRESIGN_TTL_SECONDS * 1000).toISOString();
src/modules/import/application/import-service.ts:121:      const jobExpiresAt = new Date(Date.parse(now) + IMPORT_JOB_TTL_SECONDS * 1000).toISOString();
src\runtime\aws\composition\reminder.ts:77:      new SendMessageCommand({
src\runtime\aws\composition\reminder.ts:92:      new SendMessageCommand({
src\runtime\aws\composition\notification.ts:40:          new SendMessageCommand({
src\runtime\aws\handlers\outbox-sweeper-handler.ts:39:    new SendMessageCommand({
src\runtime\aws\handlers\outbox-sweeper-handler.ts:52:    new SendMessageCommand({
src\modules\extraction\persistence\sfn-extraction-execution-starter.ts:17:        new StartExecutionCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:17:      new PutObjectCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:38:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
src\modules\document\persistence\s3-document-object-store.ts:38:      new CopyObjectCommand({
src\modules\document\persistence\s3-document-object-store.ts:49:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src\modules\document\persistence\s3-upload-url-signer.ts:15:    const command = new PutObjectCommand({
src\modules\import\persistence\s3-import-object-store.ts:23:    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/import/application/import-commit-service.ts'; Get-Content -Raw 'src/modules/import/application/import-service.ts'; Get-Content -Raw 'src/modules/identity/application/quota.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 172ms:
/**
 * ImportCommitWorker Ã¢â‚¬â€ M11 (D-042). LÃƒÂª o plano JÃƒÂ VALIDADO do S3 (nunca reparsa o CSV
 * original - design "validar uma vez sÃƒÂ³"), valida `planSha256` contra o `ImportJob`, cria um
 * `TrackedSubject` por linha `CREATE_SUBJECT` reaproveitando `SubjectService.createSubject()`
 * inalterado (mesmo entitlement check/transaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ testados de M9 - nunca duplicado aqui).
 *
 * IdempotÃƒÂªncia de retry (SQS at-least-once, "Residuais nÃƒÂ£o resolvidos" do design - polÃƒÂ­tica
 * de commit parcial decidida aqui): cursor `lastCommittedRowNumber` na prÃƒÂ³pria `ImportJob`,
 * avanÃƒÂ§ado sÃƒÂ³ DEPOIS de cada linha confirmada - um retry retoma exatamente de onde parou,
 * nunca reprocessa uma linha jÃƒÂ¡ committada. TODA linha CREATE_SUBJECT, com ou sem
 * `externalId`, primeiro CLAIMA um registro de dedup (`ImportDedupRecord`) antes de chamar
 * `createSubject()` - linhas com `externalId` usam a chave real (serve dedupe entre imports E
 * idempotÃƒÂªncia de commit ao mesmo tempo); linhas sem `externalId` usam uma chave sintÃƒÂ©tica
 * `job:<jobId>:row:<rowNumber>` (sÃƒÂ³ serve idempotÃƒÂªncia de commit, nunca aparece fora deste
 * job). Um retry que encontra o registro jÃƒÂ¡ claimado sabe que a linha jÃƒÂ¡ foi committada e
 * pula direto para o avanÃƒÂ§o do cursor.
 *
 * PolÃƒÂ­tica de commit parcial (decisÃƒÂ£o de implementaÃƒÂ§ÃƒÂ£o registrada aqui, design silente):
 * se o limite de `TenantEntitlement` for atingido no meio do commit, o job PARA
 * imediatamente (fail-fast) em vez de pular linhas restantes - continuar nÃƒÂ£o teria efeito
 * (o limite nÃƒÂ£o muda linha a linha) e sÃƒÂ³ acumularia mais registros de dedup ÃƒÂ³rfÃƒÂ£os.
 */
import { createHash } from "node:crypto";
import { importJobKey, type ImportJob } from "../domain/import-job.js";
import { importDedupKey, type ImportDedupRecord } from "../domain/import-dedup.js";
import type { ImportRowPlanEntry } from "../domain/import-row.js";
import { QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { ImportStore } from "../ports/import-store.js";
import type { ImportObjectStore } from "../ports/import-object-store.js";
import type { SubjectService } from "../../subject/application/subject-service.js";
import type { RequestContext } from "../../identity/domain/request-context.js";

export interface ImportCommitDeps {
  store: ImportStore;
  objectStore: ImportObjectStore;
  planBucket: string;
  subjects: SubjectService;
  now: () => string;
}

export type ImportCommitOutcome =
  | { kind: "COMMITTED"; createdCount: number }
  | { kind: "FAILED_ENTITLEMENT_EXCEEDED"; createdCount: number }
  | { kind: "FAILED_INTEGRITY_MISMATCH" }
  | { kind: "SKIPPED_NOT_COMMITTING" };

function syntheticRowDedupKey(jobId: string, rowNumber: number): string {
  return `job:${jobId}:row:${rowNumber}`;
}

export async function commitImportJob(deps: ImportCommitDeps, ctx: RequestContext, jobId: string): Promise<ImportCommitOutcome> {
  const job = await deps.store.get<ImportJob>(importJobKey(ctx.tenant.tenantId, jobId));
  if (!job || job.status !== "COMMITTING") {
    return { kind: "SKIPPED_NOT_COMMITTING" };
  }
  if (!job.planObjectKey || !job.planSha256) {
    await failJob(deps, job, "MISSING_PLAN_REFERENCE");
    return { kind: "FAILED_INTEGRITY_MISMATCH" };
  }

  const planBytes = await deps.objectStore.getObject(deps.planBucket, job.planObjectKey);
  const planContent = planBytes.toString("utf-8");
  const actualSha256 = createHash("sha256").update(planContent, "utf-8").digest("hex");
  if (actualSha256 !== job.planSha256) {
    // Integridade comprometida (plano mudou depois do preview, ou corrupÃƒÂ§ÃƒÂ£o) - nunca commita
    // sobre um plano que o usuÃƒÂ¡rio nÃƒÂ£o viu no preview.
    await failJob(deps, job, "PLAN_INTEGRITY_MISMATCH");
    return { kind: "FAILED_INTEGRITY_MISMATCH" };
  }

  const entries: ImportRowPlanEntry[] = planContent
    .split("\n")
    .filter((line) => line.trim().length > 0)
    .map((line) => JSON.parse(line) as ImportRowPlanEntry);

  const cursor = job.lastCommittedRowNumber ?? 0;
  const pending = entries.filter((e): e is Extract<ImportRowPlanEntry, { action: "CREATE_SUBJECT" }> => e.action === "CREATE_SUBJECT" && e.rowNumber > cursor);

  // `current` acompanha o estado persistido mais recente (versÃƒÂ£o incluÃƒÂ­da) - nunca reusa o
  // `job` original atravÃƒÂ©s do loop, ou toda escrita repetiria a MESMA versÃƒÂ£o (achado real
  // corrigido antes do commit: cada update() abaixo precisa do version+1 do estado anterior,
  // nÃƒÂ£o do version original lido no inÃƒÂ­cio da funÃƒÂ§ÃƒÂ£o).
  let current: ImportJob = job;
  let createdCount = 0;

  for (const entry of pending) {
    const dedupExternalId = entry.row.externalId ?? syntheticRowDedupKey(jobId, entry.rowNumber);
    const claimed = await deps.store.putIfAbsent<ImportDedupRecord>({
      ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
      entityType: "ImportDedupRecord",
      tenantId: ctx.tenant.tenantId,
      externalId: dedupExternalId,
      subjectId: "", // preenchido abaixo, apÃƒÂ³s createSubject() confirmar - placeholder aceitÃƒÂ¡vel aqui pois a claim em si (nÃƒÂ£o o valor) ÃƒÂ© o que garante idempotÃƒÂªncia.
      createdAt: deps.now(),
    });

    if (claimed) {
      try {
        const subject = await deps.subjects.createSubject(ctx, {
          type: entry.row.type,
          displayName: entry.row.displayName,
          notes: entry.row.notes,
          tags: entry.row.tags,
        });
        await deps.store.update<ImportDedupRecord>({
          ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
          entityType: "ImportDedupRecord",
          tenantId: ctx.tenant.tenantId,
          externalId: dedupExternalId,
          subjectId: subject.subjectId,
          createdAt: deps.now(),
        });
        createdCount += 1;
      } catch (err) {
        if (err instanceof QuotaExceededError) {
          // Fail-fast (polÃƒÂ­tica registrada acima) - a claim de dedup desta UMA linha fica
          // ÃƒÂ³rfÃƒÂ£ (limitaÃƒÂ§ÃƒÂ£o documentada de v1, nÃƒÂ£o uma segunda claim por linha restante).
          await deps.store.update<ImportJob>({ ...current, status: "FAILED", failureReason: "ENTITLEMENT_EXCEEDED", updatedAt: deps.now(), version: current.version + 1 });
          return { kind: "FAILED_ENTITLEMENT_EXCEEDED", createdCount };
        }
        throw err;
      }
    }
    // claimed === false: linha jÃƒÂ¡ committada por uma tentativa anterior (retry seguro) -
    // avanÃƒÂ§a o cursor sem recriar nada.

    current = { ...current, lastCommittedRowNumber: entry.rowNumber, updatedAt: deps.now(), version: current.version + 1 };
    await deps.store.update<ImportJob>(current);
  }

  current = { ...current, status: "COMMITTED", updatedAt: deps.now(), version: current.version + 1 };
  await deps.store.update<ImportJob>(current);
  return { kind: "COMMITTED", createdCount };
}

async function failJob(deps: ImportCommitDeps, job: ImportJob, reason: string): Promise<void> {
  await deps.store.update<ImportJob>({ ...job, status: "FAILED", failureReason: reason, updatedAt: deps.now(), version: job.version + 1 });
}

/**
 * ImportService Ã¢â‚¬â€ lado HTTP de M11 (D-042): reservar upload (presigned PUT, mesmo padrÃƒÂ£o de
 * document-service.ts's reserveUpload), consultar status, solicitar commit. O parse/commit
 * em si roda em workers assÃƒÂ­ncronos (import-parse-service.ts/import-commit-service.ts),
 * disparados por evento S3 (parse) e por outbox (commit) respectivamente Ã¢â‚¬â€ nunca sÃƒÂ­ncronos
 * aqui, mesmo para preview pequeno (design: "sync sÃƒÂ³ atÃƒÂ© 128 KiB/100 linhas" fica registrado
 * como possÃƒÂ­vel otimizaÃƒÂ§ÃƒÂ£o futura, nÃƒÂ£o implementado em v1 Ã¢â‚¬â€ simplicidade > latÃƒÂªncia agora).
 */
import { randomUUID } from "node:crypto";
import type { RequestContext } from "../../identity/domain/request-context.js";
import { authorize } from "../../identity/domain/authorization.js";
import { ConflictError, NotFoundError, ValidationError } from "../../../shared/errors/app-error.js";
import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
import { IdempotencyStore, transitionIdempotencyStatus, type DynamoLike } from "../../../shared/idempotency/idempotency.js";
import { appendToTransaction } from "../../../shared/outbox/outbox.js";
import type { DomainEvent } from "../../../shared/contracts/events.js";
import { importJobKey, IMPORT_JOB_TTL_SECONDS, MAX_IMPORT_FILE_BYTES, type ImportJob } from "../domain/import-job.js";
import { isTransactionCanceled, type ImportStore, type TransactWriteEntry } from "../ports/import-store.js";
import type { UploadUrlSigner } from "../../document/ports/upload-url-signer.js";
import type { ImportIdGenerator } from "./id-generator.js";
import type { TenantQuotaService } from "../../identity/application/quota.js";

const PRESIGN_TTL_SECONDS = 15 * 60;
const OPERATION = "import.reserve";

export interface ReserveImportInput {
  contentLength: number;
  checksumSha256: string;
}

export interface ReserveImportResult {
  jobId: string;
  uploadUrl: string;
  requiredHeaders: Record<string, string>;
  expiresAt: string;
}

/** Envelope da mensagem SQS_IMPORT_COMMIT_V1 - schemas/queues/import-commit.v1.json. Mesmo
 * padrÃƒÂ£o do `DispatchCommand` de reminder-producer/producer.ts: como o relay/sweeper sÃƒÂ³
 * reenvia `OutboxRecord.payload` (== `DomainEvent.data`), este tipo precisa carregar sozinho
 * tudo que command-envelope.v1.json exige no nÃƒÂ­vel raiz (tenantId incluso). */
export interface ImportCommitCommand {
  messageVersion: 1;
  messageId: string;
  createdAt: string;
  correlationId: string;
  commandType: "import.commit.v1";
  tenantId: string;
  deduplicationKey: string;
  data: { jobId: string };
}

export interface ImportServiceDeps {
  store: ImportStore;
  tableName: string;
  rawBucket: string;
  ids: ImportIdGenerator;
  signer: UploadUrlSigner;
  quota: TenantQuotaService;
  now?: () => string;
}

export class ImportService {
  private readonly store: ImportStore;
  private readonly tableName: string;
  private readonly rawBucket: string;
  private readonly ids: ImportIdGenerator;
  private readonly signer: UploadUrlSigner;
  private readonly quota: TenantQuotaService;
  private readonly now: () => string;
  private readonly idempotency: IdempotencyStore;

  constructor(deps: ImportServiceDeps) {
    this.store = deps.store;
    this.tableName = deps.tableName;
    this.rawBucket = deps.rawBucket;
    this.ids = deps.ids;
    this.signer = deps.signer;
    this.quota = deps.quota;
    this.now = deps.now ?? (() => new Date().toISOString());
    const adapter: DynamoLike = {
      putIfAbsent: async (item) => ((await this.store.putIfAbsent(item)) ? "PUT" : "ALREADY_EXISTS"),
      get: (key) => this.store.get(key),
      update: (item) => this.store.update(item),
      transitionIfStatus: (item, expectedStatus) => transitionIdempotencyStatus(this.store, this.tableName, item, expectedStatus),
    };
    this.idempotency = new IdempotencyStore(adapter, this.tableName, this.now);
  }

  async reserveImport(ctx: RequestContext, input: ReserveImportInput, idempotencyKey: string): Promise<ReserveImportResult> {
    authorize({ context: ctx, action: "import:create", resource: { tenantId: ctx.tenant.tenantId } });

    if (input.contentLength <= 0 || input.contentLength > MAX_IMPORT_FILE_BYTES) {
      throw new ValidationError("contentLength must be between 1 byte and 5 MiB.", { contentLength: input.contentLength, maxBytes: MAX_IMPORT_FILE_BYTES });
    }
    if (!/^[a-f0-9]{64}$/i.test(input.checksumSha256)) {
      throw new ValidationError("checksumSha256 must be a 64-character hex SHA-256 digest.");
    }

    // Novos tipos de quota (design): IMPORT_COUNT (jobs por janela) e IMPORT_BYTES (soma de
    // bytes reservados por janela) - separados de UPLOAD_* (M6), mesmo mecanismo
    // TenantQuotaService jÃƒÂ¡ testado. IMPORT_ROWS ÃƒÂ© verificado no parse worker (sÃƒÂ³ ali o
    // total real de linhas ÃƒÂ© conhecido).
    await this.quota.consume({ tenantId: ctx.tenant.tenantId, quotaType: "IMPORT_COUNT", window: "current", limit: 10, windowSeconds: 60 * 60 });
    await this.quota.consume({ tenantId: ctx.tenant.tenantId, quotaType: "IMPORT_BYTES", window: "current", limit: 50 * MAX_IMPORT_FILE_BYTES, windowSeconds: 60 * 60 });

    const requestHash = `${input.contentLength}|${input.checksumSha256}`;
    const now = this.now();
    const expiresAt = new Date(Date.parse(now) + PRESIGN_TTL_SECONDS * 1000).toISOString();

    const begin = await this.idempotency.begin({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, requestHash, expiresAt });

    let jobId: string;
    if (begin === "COMPLETED_SAME_REQUEST") {
      const record = await this.store.get({ PK: `TENANT#${ctx.tenant.tenantId}#IDEMPOTENCY#${OPERATION}`, SK: `KEY#${idempotencyKey}` });
      const existingJobId = (record as { responseRef?: string } | undefined)?.responseRef;
      if (!existingJobId) throw new ConflictError("reserveImport idempotency record missing responseRef.");
      jobId = existingJobId;
    } else {
      jobId = this.ids.newImportJobId();
      const jobExpiresAt = new Date(Date.parse(now) + IMPORT_JOB_TTL_SECONDS * 1000).toISOString();
      const job: ImportJob = {
        ...importJobKey(ctx.tenant.tenantId, jobId),
        entityType: "ImportJob",
        jobId,
        tenantId: ctx.tenant.tenantId,
        targetEntityType: "TrackedSubject",
        status: "UPLOADED",
        createdByUserId: ctx.principal.userId,
        checksumSha256: input.checksumSha256,
        mappingVersion: 1,
        expiresAt: jobExpiresAt,
        createdAt: now,
        updatedAt: now,
        version: 1,
      };
      await this.store.putIfAbsent(job);
      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: jobId });
    }

    const key = this.rawObjectKey(ctx.tenant.tenantId, jobId);
    const { uploadUrl, requiredHeaders } = await this.signer.presignUpload({
      bucket: this.rawBucket,
      key,
      mediaType: "text/csv",
      contentLength: input.contentLength,
      checksumSha256: input.checksumSha256,
      metadata: { tenantId: ctx.tenant.tenantId, jobId },
      expiresInSeconds: PRESIGN_TTL_SECONDS,
    });

    return { jobId, uploadUrl, requiredHeaders, expiresAt };
  }

  async getImportJob(ctx: RequestContext, jobId: string): Promise<ImportJob> {
    authorize({ context: ctx, action: "import:read", resource: { tenantId: ctx.tenant.tenantId } });
    const job = await this.store.get<ImportJob>(importJobKey(ctx.tenant.tenantId, jobId));
    if (!job) throw new NotFoundError("ImportJob not found.", { jobId });
    return job;
  }

  async requestCommit(ctx: RequestContext, jobId: string, expectedVersion: number): Promise<void> {
    authorize({ context: ctx, action: "import:commit", resource: { tenantId: ctx.tenant.tenantId } });
    const job = await this.store.get<ImportJob>(importJobKey(ctx.tenant.tenantId, jobId));
    if (!job) throw new NotFoundError("ImportJob not found.", { jobId });
    if (job.status !== "PREVIEW_READY") {
      throw new ConflictError(`ImportJob must be PREVIEW_READY to commit (current status: ${job.status}).`, { jobId, status: job.status });
    }

    const now = this.now();
    const entries: TransactWriteEntry[] = [
      {
        Update: buildVersionedUpdate({
          tableName: this.tableName,
          key: importJobKey(ctx.tenant.tenantId, jobId),
          tenantId: ctx.tenant.tenantId,
          expectedVersion,
          set: { status: "COMMITTING" },
        }),
      },
    ];
    // O relay/sweeper sÃƒÂ³ reenvia `event.data` para a fila (nunca o DomainEvent completo) -
    // por isso `data` aqui precisa jÃƒÂ¡ SER o envelope inteiro exigido por command-envelope.v1.json
    // (messageVersion/messageId/tenantId/deduplicationKey no nÃƒÂ­vel raiz), mesmo padrÃƒÂ£o do
    // DispatchCommand de reminder-producer/producer.ts. Sem isso o commit worker nunca teria
    // tenantId disponÃƒÂ­vel na mensagem SQS.
    const command: ImportCommitCommand = {
      messageVersion: 1,
      messageId: randomUUID(),
      createdAt: now,
      correlationId: ctx.correlationId,
      commandType: "import.commit.v1",
      tenantId: ctx.tenant.tenantId,
      deduplicationKey: `${ctx.tenant.tenantId}|${jobId}|${expectedVersion + 1}`,
      data: { jobId },
    };
    const event: DomainEvent = {
      specVersion: "1.0",
      eventId: randomUUID(),
      eventType: "ImportCommitRequested",
      source: "expiration-tracker.import-service",
      occurredAt: now,
      correlationId: ctx.correlationId,
      tenantId: ctx.tenant.tenantId,
      actor: { type: "USER", userId: ctx.principal.userId },
      aggregate: { type: "ImportJob", id: jobId, version: expectedVersion + 1 },
      data: command as unknown as Record<string, unknown>,
    };
    appendToTransaction(entries, this.tableName, event, "SQS_IMPORT_COMMIT_V1");

    try {
      await this.store.transactWrite(entries);
    } catch (err) {
      if (isTransactionCanceled(err)) throw new ConflictError("Failed to request import commit under contention.", { jobId });
      throw err;
    }
  }

  private rawObjectKey(tenantId: string, jobId: string): string {
    // Nunca o nome de arquivo original (mesmo motivo de M6's quarantineKey) - sÃƒÂ³ IDs internos.
    return `tenant/${tenantId}/imports/${jobId}/raw.csv`;
  }
}

/**
 * TenantQuota token bucket Ã¢â‚¬â€ data-model.md Ã‚Â§"Rastreabilidade a gates de abuso/custo":
 * PK=TENANT#t#QUOTA, SK=TYPE#<quotaType>#<window>, decremented via ConditionExpression
 * "no race condition between concurrent requests of the same tenant" (same pattern as
 * UploadSlot). Enforced at the API/Lambda boundary per M1 deliverable list.
 */
import { QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";

export type QuotaType =
  | "API_REQUEST"
  | "UPLOAD_BYTES"
  | "UPLOAD_COUNT"
  | "AI_CALL"
  | "NOTIFICATION_EMAIL"
  // M11 (CSV import/export, D-042, 09-domain-model-csv-import.md): separados de UPLOAD_* (M6) -
  // import ÃƒÂ© uma superfÃƒÂ­cie de processamento em massa com um perfil de abuso diferente.
  | "IMPORT_COUNT"
  | "IMPORT_BYTES"
  | "IMPORT_ROWS";

export interface TenantQuotaRecord {
  PK: string;
  SK: string;
  entityType: "TenantQuota";
  tenantId: string;
  quotaType: QuotaType;
  limit: number;
  windowSeconds: number;
  count: number;
  resetAt: string;
  killSwitchOverride?: boolean;
}

export function tenantQuotaKey(tenantId: string, quotaType: QuotaType, window: string): EntityKey {
  return { PK: `TENANT#${tenantId}#QUOTA`, SK: `TYPE#${quotaType}#${window}` };
}

export interface QuotaCheckInput {
  tenantId: string;
  quotaType: QuotaType;
  window: string; // e.g. rolling window identifier/bucket, caller-defined granularity
  limit: number;
  windowSeconds: number;
}

/**
 * Fixed-window token bucket (simplest correct primitive satisfying "atomic decrement,
 * no race condition" from data-model.md; a true sliding-window/leaky-bucket is a
 * documented possible refinement, not required by the blueprint text). Consumes 1 unit
 * per call; throws QuotaExceededError (mapped to 429 by the http layer) when exhausted.
 * killSwitchOverride, when true, denies regardless of remaining count (AppConfig-driven
 * emergency block, per data-model.md).
 */
export class TenantQuotaService {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  /**
   * Bounded retry count for the read-check-conditionalWrite loop below (full audit round1,
   * eixo GovernanÃƒÂ§a de Produto, critÃƒÂ©rio 3 Ã¢â‚¬â€ the previous unconditional `store.update` allowed
   * lost updates between two concurrent `consume` calls for the same tenant/quotaType: both
   * could read the same `count`, both compute `count+1`, and the second overwrite would
   * silently discard the first increment, letting a tenant exceed its quota). 20 attempts
   * covers a burst of simultaneous requests against the same partition key without needing
   * backoff/jitter for this fixed-window primitive's expected contention level; exhausting it
   * means genuine sustained hot contention, not a normal path.
   */
  private static readonly MAX_CONTENTION_RETRIES = 20;

  async consume(input: QuotaCheckInput): Promise<void> {
    for (let attempt = 0; attempt < TenantQuotaService.MAX_CONTENTION_RETRIES; attempt++) {
      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
      const nowIso = this.now();
      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();

      const existing = await this.store.get<TenantQuotaRecord>(key);
      if (!existing) {
        const created = await this.store.putIfAbsent({
          ...key,
          entityType: "TenantQuota",
          tenantId: input.tenantId,
          quotaType: input.quotaType,
          limit: input.limit,
          windowSeconds: input.windowSeconds,
          count: 1,
          resetAt,
        });
        if (created) return;
        // Lost the create race; loop to re-read the record another caller just created.
        continue;
      }

      if (existing.killSwitchOverride) {
        throw new QuotaExceededError("Quota blocked by kill switch.", { tenantId: input.tenantId, quotaType: input.quotaType });
      }

      const windowExpired = existing.resetAt < nowIso;
      const effectiveCount = windowExpired ? 0 : existing.count;

      if (effectiveCount >= existing.limit) {
        throw new QuotaExceededError("Quota exceeded.", {
          tenantId: input.tenantId,
          quotaType: input.quotaType,
          limit: existing.limit,
        });
      }

      const nextResetAt = windowExpired ? resetAt : existing.resetAt;
      const wrote = await this.store.updateConditional(
        { ...existing, count: effectiveCount + 1, resetAt: nextResetAt },
        { count: existing.count, resetAt: existing.resetAt },
      );
      if (wrote) return;
      // Another concurrent consume() won the write race; re-read and retry against fresh state.
    }

    throw new QuotaExceededError("Quota check could not complete under contention.", {
      tenantId: input.tenantId,
      quotaType: input.quotaType,
    });
  }

  /**
   * Releases 1 previously-consumed unit Ã¢â‚¬â€ M6 design Ã‚Â§3.5 (UploadSlotReconciliationWorker
   * "libera quota idempotentemente" for a slot that expired unconfirmed). Idempotent: never
   * decrements below 0, and calling this twice for the same already-released slot floors at 0
   * rather than double-crediting. A window that has already reset naturally (count implicitly
   * 0) is a no-op, not an error - the quota already recovered on its own.
   */
  async release(input: Omit<QuotaCheckInput, "limit"> & { limit?: number }): Promise<void> {
    for (let attempt = 0; attempt < TenantQuotaService.MAX_CONTENTION_RETRIES; attempt++) {
      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
      const nowIso = this.now();
      const existing = await this.store.get<TenantQuotaRecord>(key);
      if (!existing) return; // nothing consumed yet for this window - nothing to release.

      const windowExpired = existing.resetAt < nowIso;
      if (windowExpired) return; // window already reset naturally - already recovered.

      const nextCount = Math.max(0, existing.count - 1);
      const wrote = await this.store.updateConditional({ ...existing, count: nextCount }, { count: existing.count, resetAt: existing.resetAt });
      if (wrote) return;
      // Lost a concurrent write race; re-read and retry against fresh state.
    }
    throw new QuotaExceededError("Quota release could not complete under contention.", { tenantId: input.tenantId, quotaType: input.quotaType });
  }
}


codex
Nota: 2,8/10

RecomendaÃ§Ã£o: REPROVAR esta rodada. A direÃ§Ã£o de centralizar o fence Ã© melhor que enumerar handlers, e o tombstone permanente corrige conceitualmente o principal erro de D-063. PorÃ©m, o desenho apresentado nÃ£o Ã© implementÃ¡vel como escrito e ainda deixa superfÃ­cies reais fora da garantia anunciada.

## Achados bloqueantes

### 1. A prÃ³pria transiÃ§Ã£o do lifecycle Ã© impossÃ­vel pelo wrapper proposto

A proposta diz que `fencedSingleWrite` sempre adiciona um `ConditionCheck` exigindo lifecycle `ACTIVE` ([claude-proposal-round1.md:73](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:73)). Depois usa esse wrapper para:

- escrever `DELETING` ([linha 131](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:131));
- escrever `DELETED` ([linha 140](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:140)).

Isso falha de duas maneiras:

1. Uma transaÃ§Ã£o DynamoDB nÃ£o pode conter `ConditionCheck` e `Update`/`Put` para a mesma chave do lifecycle.
2. O flip `DELETING â†’ DELETED` nunca satisfaria o guard `status = ACTIVE`.

AlÃ©m disso, a linha 131 chama a operaÃ§Ã£o de â€œcreate condicional `attribute_not_exists(PK)`â€. Como o lifecycle jÃ¡ deve existir como `ACTIVE`, esse create falharia sempre, nÃ£o apenas numa corrida de duplo-start.

Ã‰ necessÃ¡rio um primitive administrativo separado, por exemplo `transitionTenantLifecycle(expectedStatus, nextStatus)`, com uma Ãºnica atualizaÃ§Ã£o condicional sobre a prÃ³pria linha e inacessÃ­vel a writers normais.

### 2. O bootstrap do primeiro tenant tambÃ©m Ã© impossÃ­vel

A proposta afirma simultaneamente que:

- o lifecycle nasce `ACTIVE` na mesma transaÃ§Ã£o de `createProfileIfAbsent` ([linha 62](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:62));
- `createProfileIfAbsent` usa `fencedSingleWrite` ([linhas 96â€“99](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:96));
- o resolver rejeita quando lifecycle `status !== ACTIVE`.

No cÃ³digo real, porÃ©m, o resolver primeiro cria ou lÃª `IdentityMapping` ([resolve-request-context.ts:50](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/application/resolve-request-context.ts:50)) e sÃ³ depois chama `createProfileIfAbsent` ([linha 57](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/application/resolve-request-context.ts:57)).

Para um tenant novo nÃ£o existe lifecycle `ACTIVE`. Portanto:

- a leitura anterior rejeitaria o primeiro login; ou
- o wrapper exigiria a existÃªncia do lifecycle e impediria sua criaÃ§Ã£o; ou
- tentar colocar `ConditionCheck` e `Put` da mesma linha na transaÃ§Ã£o seria invÃ¡lido.

`IdentityMapping` tambÃ©m jÃ¡ teria sido criado antes da suposta verificaÃ§Ã£o. O desenho precisa de um protocolo explÃ­cito e atÃ´mico de bootstrap `IdentityMapping + Lifecycle + User`, ou de uma separaÃ§Ã£o formal entre â€œtenant nunca provisionadoâ€ e â€œtenant permanentemente apagadoâ€.

### 3. A regra proposta nÃ£o pode ser implementada pelo dependency-cruiser

A proposta promete proibir expressÃµes como `new TransactWriteCommand(...)` fora do wrapper ([linhas 79â€“83](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:79)).

O `.dependency-cruiser.cjs` real sÃ³ analisa dependÃªncias/importaÃ§Ãµes e caminhos no grafo. Ele nÃ£o analisa construÃ§Ãµes AST nem sabe distinguir:

- `new GetCommand`;
- `new PutCommand`;
- import de tipo;
- construÃ§Ã£o versus mera importaÃ§Ã£o.

Proibir `@aws-sdk/lib-dynamodb` fora do wrapper geraria falsos positivos em todos os leitores que importam `GetCommand`/`QueryCommand`. Permitir o pacote deixa construÃ§Ãµes mutÃ¡veis escaparem.

O enforcement precisa ser ESLint AST (`no-restricted-imports` com mÃ³dulos especializados ou regra customizada/no-restricted-syntax), possivelmente acompanhado por dependency-cruiser para restringir quais mÃ³dulos podem importar o adapter fenced. A garantia estrutural central da proposta, hoje, Ã© tecnicamente falsa.

### 4. O inventÃ¡rio â€œ19 arquivos cobrem toda escrita de tenantâ€ nÃ£o Ã© verdadeiro

O grep exaustivo encontrou construÃ§Ãµes DynamoDB mutÃ¡veis diretas em 14 arquivos, nÃ£o uma uniÃ£o demonstrada de 19. Todos os comandos DynamoDB mutÃ¡veis diretos atuais estÃ£o nesses adapters; nÃ£o encontrei handler construindo diretamente `PutCommand`, `UpdateCommand`, `DeleteCommand` ou `TransactWriteCommand`.

Mas isso nÃ£o cobre toda escrita de dado de tenant. Existem writers externos reais fora dessa superfÃ­cie:

- Plano de importaÃ§Ã£o em S3: `putObject` em [import-parse-service.ts:129](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/import/application/import-parse-service.ts:129), executado pelo adapter `s3-import-object-store.ts`.
- Artefato OCR em S3: `PutObjectCommand` em `src/modules/extraction/persistence/s3-ocr-artifact-store.ts:17`.
- PromoÃ§Ã£o/cÃ³pia de documento: `CopyObjectCommand` em `src/modules/document/persistence/s3-document-object-store.ts:38`.
- Uploads presignados de documentos e imports.
- Mensagens SQS construÃ­das em compositions e no `outbox-sweeper-handler`.
- InÃ­cio de Step Functions em `sfn-extraction-execution-starter.ts`.

Logo, os wrappers fechariam as linhas DynamoDB, nÃ£o â€œtoda escrita de dado de tenantâ€ nem todos os 36 handlers.

### 5. O protocolo de efeitos externos tem uma janela real, mas nÃ£o de â€œmilissegundosâ€

No fluxo de e-mail, a claim `SUBMITTING` ocorre em [email-delivery-workflow.ts:113](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/email-delivery-workflow.ts:113), e o SES sÃ³ Ã© chamado na [linha 128](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/email-delivery-workflow.ts:128).

ApÃ³s a claim fenced, a execuÃ§Ã£o ainda:

- resolve destinatÃ¡rio;
- renderiza template;
- pode ser pausada, sofrer throttling ou ficar congelada;
- mantÃ©m lease padrÃ£o de cinco minutos.

Assim, um fence `DELETING` pode ser instalado e a cascata pode avanÃ§ar enquanto uma execuÃ§Ã£o previamente claimed ainda envia o e-mail. A janela nÃ£o Ã© justificadamente â€œde milissegundosâ€; pode ser de minutos ou atÃ© do limite de execuÃ§Ã£o/retry adotado. Drenagem ou um estado operacional que aguarde claims externas precisa fazer parte da condiÃ§Ã£o de conclusÃ£o.

### 6. Bedrock e Textract nÃ£o seguem integralmente o protocolo anunciado

Bedrock reserva quota em [run-bedrock-extraction.ts:145](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-bedrock-extraction.ts:145) e chama o modelo na [linha 162](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-bedrock-extraction.ts:162). HÃ¡ a mesma janela claimâ†’efeito do SES.

Pior: se `quota.consume` encontra uma reserva anterior e lanÃ§a `QuotaExceededError`, o cÃ³digo deliberadamente ignora o erro e continua para Bedrock. Uma reserva antiga nÃ£o prova que a execuÃ§Ã£o atual nasceu de uma escrita fenced posterior; portanto uma retomada pode chamar Bedrock depois de `DELETING`.

Textract reserva quota na [start-ocr.ts:85](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:85), chama o serviÃ§o na [linha 96](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:96) e sÃ³ cria `TextractJob` na [linha 137](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:137). A chamada externa jÃ¡ aconteceu antes da persistÃªncia do job. O desenho nÃ£o define drenagem nem prova que uma reserva preexistente continua autorizando o efeito.

### 7. As URLs presignadas nÃ£o estÃ£o fechadas apenas no â€œponto de finalizaÃ§Ã£oâ€

O problema nÃ£o se limita a URLs jÃ¡ emitidas. Em `GuestSubmissionService.startSubmission`, a transaÃ§Ã£o ocorre antes de `signer.presignUpload`. Portanto:

1. a escrita fenced pode commitar enquanto o tenant estÃ¡ `ACTIVE`;
2. a exclusÃ£o muda o tenant para `DELETING`;
3. a mesma invocation ainda emite e devolve uma URL nova.

O mesmo padrÃ£o existe em `ImportService.reserveImport`: as escritas precedem a emissÃ£o da URL.

A persistÃªncia posterior fenced pode impedir linhas DynamoDB, mas nÃ£o impede emissÃ£o de nova capability apÃ³s o fence nem a criaÃ§Ã£o fÃ­sica do objeto S3. Isso precisa ser incluÃ­do no risco/garantia, com drenagem, epoch/token verificÃ¡vel na finalizaÃ§Ã£o ou polÃ­tica de expiraÃ§Ã£o e varredura que seja parte da conclusÃ£o DSR.

### 8. Um upload tardio pode surgir depois da â€œconvergÃªncia zeroâ€

A cascata verifica zero itens DynamoDB e marca `DELETED`, mas uma URL ainda vÃ¡lida pode receber um objeto S3 depois desse instante. O desenho diz que o objeto ficarÃ¡ Ã³rfÃ£o e serÃ¡ limpo â€œdepoisâ€, porÃ©m nÃ£o demonstra:

- que todos os buckets envolvidos possuem lifecycle apropriado;
- que o objeto tardio Ã© descoberto pelo mecanismo DSR;
- que `DELETED` significa exclusÃ£o fÃ­sica concluÃ­da apesar desse objeto;
- qual Ã© o prazo mÃ¡ximo de eliminaÃ§Ã£o.

Isso conflita com a claim forte de fence de exclusÃ£o fÃ­sica. Um lifecycle eventual pode ser uma polÃ­tica aceitÃ¡vel, mas deve ser especificado como parte verificÃ¡vel do contrato de conclusÃ£o, nÃ£o assumido.

## VerificaÃ§Ãµes sem achado bloqueante

- O tombstone separado e explicitamente excluÃ­do da cascata resolve conceitualmente o achado central de D-063: ele nÃ£o seria apagado junto com `User`. O problema foi resolvido, nÃ£o apenas deslocado â€” desde que a exclusÃ£o seja enforceada por cÃ³digo/teste e que o primitive administrativo seja corrigido.
- NÃ£o encontrei hoje transaÃ§Ã£o prÃ³xima de 100 itens. O maior candidato estrutural Ã© `DynamoDbExtractedFieldStore.commitRunOutcome`, mas o schema atual possui somente um campo em `FIELD_SCHEMA_V1`; a transaÃ§Ã£o atual chega a quatro itens antes do fence, cinco depois. Ainda assim, o wrapper deve rejeitar explicitamente `items.length > 99`, para nÃ£o introduzir falha futura silenciosa.
- NÃ£o encontrei handler construindo diretamente comandos DynamoDB mutÃ¡veis fora dos adapters identificados. O bypass real estÃ¡ nos efeitos e stores S3/SQS/SFN, nÃ£o em um `PutCommand` escondido dentro de handler.

## O que a prÃ³xima proposta precisa mudar

1. Criar primitives separados para bootstrap, writes normais fenced e transiÃ§Ãµes administrativas do lifecycle.
2. Definir atomicamente o tratamento de â€œlifecycle ausenteâ€: novo tenant versus tenant apagado.
3. Substituir a alegaÃ§Ã£o de dependency-cruiser por enforcement AST tecnicamente executÃ¡vel e testado com fixtures positivas/negativas.
4. Publicar inventÃ¡rio gerado por grep de todos os writers: DynamoDB, S3, SES, SQS, SFN, Bedrock e Textract.
5. Definir drenagem/epoch para claims jÃ¡ commitadas antes de permitir `DELETED`.
6. Cobrir emissÃ£o tardia de presigned URLs e objetos S3 criados depois da convergÃªncia.
7. Especificar e testar o limite de 99 itens antes de inserir o ConditionCheck.

A ideia central merece continuaÃ§Ã£o, mas esta proposta ainda nÃ£o sustenta a garantia de nÃ£o-ressurreiÃ§Ã£o nem uma cascata executÃ¡vel.
tokens used
107.510
Nota: 2,8/10

RecomendaÃ§Ã£o: REPROVAR esta rodada. A direÃ§Ã£o de centralizar o fence Ã© melhor que enumerar handlers, e o tombstone permanente corrige conceitualmente o principal erro de D-063. PorÃ©m, o desenho apresentado nÃ£o Ã© implementÃ¡vel como escrito e ainda deixa superfÃ­cies reais fora da garantia anunciada.

## Achados bloqueantes

### 1. A prÃ³pria transiÃ§Ã£o do lifecycle Ã© impossÃ­vel pelo wrapper proposto

A proposta diz que `fencedSingleWrite` sempre adiciona um `ConditionCheck` exigindo lifecycle `ACTIVE` ([claude-proposal-round1.md:73](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:73)). Depois usa esse wrapper para:

- escrever `DELETING` ([linha 131](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:131));
- escrever `DELETED` ([linha 140](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:140)).

Isso falha de duas maneiras:

1. Uma transaÃ§Ã£o DynamoDB nÃ£o pode conter `ConditionCheck` e `Update`/`Put` para a mesma chave do lifecycle.
2. O flip `DELETING â†’ DELETED` nunca satisfaria o guard `status = ACTIVE`.

AlÃ©m disso, a linha 131 chama a operaÃ§Ã£o de â€œcreate condicional `attribute_not_exists(PK)`â€. Como o lifecycle jÃ¡ deve existir como `ACTIVE`, esse create falharia sempre, nÃ£o apenas numa corrida de duplo-start.

Ã‰ necessÃ¡rio um primitive administrativo separado, por exemplo `transitionTenantLifecycle(expectedStatus, nextStatus)`, com uma Ãºnica atualizaÃ§Ã£o condicional sobre a prÃ³pria linha e inacessÃ­vel a writers normais.

### 2. O bootstrap do primeiro tenant tambÃ©m Ã© impossÃ­vel

A proposta afirma simultaneamente que:

- o lifecycle nasce `ACTIVE` na mesma transaÃ§Ã£o de `createProfileIfAbsent` ([linha 62](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:62));
- `createProfileIfAbsent` usa `fencedSingleWrite` ([linhas 96â€“99](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:96));
- o resolver rejeita quando lifecycle `status !== ACTIVE`.

No cÃ³digo real, porÃ©m, o resolver primeiro cria ou lÃª `IdentityMapping` ([resolve-request-context.ts:50](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/application/resolve-request-context.ts:50)) e sÃ³ depois chama `createProfileIfAbsent` ([linha 57](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/application/resolve-request-context.ts:57)).

Para um tenant novo nÃ£o existe lifecycle `ACTIVE`. Portanto:

- a leitura anterior rejeitaria o primeiro login; ou
- o wrapper exigiria a existÃªncia do lifecycle e impediria sua criaÃ§Ã£o; ou
- tentar colocar `ConditionCheck` e `Put` da mesma linha na transaÃ§Ã£o seria invÃ¡lido.

`IdentityMapping` tambÃ©m jÃ¡ teria sido criado antes da suposta verificaÃ§Ã£o. O desenho precisa de um protocolo explÃ­cito e atÃ´mico de bootstrap `IdentityMapping + Lifecycle + User`, ou de uma separaÃ§Ã£o formal entre â€œtenant nunca provisionadoâ€ e â€œtenant permanentemente apagadoâ€.

### 3. A regra proposta nÃ£o pode ser implementada pelo dependency-cruiser

A proposta promete proibir expressÃµes como `new TransactWriteCommand(...)` fora do wrapper ([linhas 79â€“83](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md:79)).

O `.dependency-cruiser.cjs` real sÃ³ analisa dependÃªncias/importaÃ§Ãµes e caminhos no grafo. Ele nÃ£o analisa construÃ§Ãµes AST nem sabe distinguir:

- `new GetCommand`;
- `new PutCommand`;
- import de tipo;
- construÃ§Ã£o versus mera importaÃ§Ã£o.

Proibir `@aws-sdk/lib-dynamodb` fora do wrapper geraria falsos positivos em todos os leitores que importam `GetCommand`/`QueryCommand`. Permitir o pacote deixa construÃ§Ãµes mutÃ¡veis escaparem.

O enforcement precisa ser ESLint AST (`no-restricted-imports` com mÃ³dulos especializados ou regra customizada/no-restricted-syntax), possivelmente acompanhado por dependency-cruiser para restringir quais mÃ³dulos podem importar o adapter fenced. A garantia estrutural central da proposta, hoje, Ã© tecnicamente falsa.

### 4. O inventÃ¡rio â€œ19 arquivos cobrem toda escrita de tenantâ€ nÃ£o Ã© verdadeiro

O grep exaustivo encontrou construÃ§Ãµes DynamoDB mutÃ¡veis diretas em 14 arquivos, nÃ£o uma uniÃ£o demonstrada de 19. Todos os comandos DynamoDB mutÃ¡veis diretos atuais estÃ£o nesses adapters; nÃ£o encontrei handler construindo diretamente `PutCommand`, `UpdateCommand`, `DeleteCommand` ou `TransactWriteCommand`.

Mas isso nÃ£o cobre toda escrita de dado de tenant. Existem writers externos reais fora dessa superfÃ­cie:

- Plano de importaÃ§Ã£o em S3: `putObject` em [import-parse-service.ts:129](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/import/application/import-parse-service.ts:129), executado pelo adapter `s3-import-object-store.ts`.
- Artefato OCR em S3: `PutObjectCommand` em `src/modules/extraction/persistence/s3-ocr-artifact-store.ts:17`.
- PromoÃ§Ã£o/cÃ³pia de documento: `CopyObjectCommand` em `src/modules/document/persistence/s3-document-object-store.ts:38`.
- Uploads presignados de documentos e imports.
- Mensagens SQS construÃ­das em compositions e no `outbox-sweeper-handler`.
- InÃ­cio de Step Functions em `sfn-extraction-execution-starter.ts`.

Logo, os wrappers fechariam as linhas DynamoDB, nÃ£o â€œtoda escrita de dado de tenantâ€ nem todos os 36 handlers.

### 5. O protocolo de efeitos externos tem uma janela real, mas nÃ£o de â€œmilissegundosâ€

No fluxo de e-mail, a claim `SUBMITTING` ocorre em [email-delivery-workflow.ts:113](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/email-delivery-workflow.ts:113), e o SES sÃ³ Ã© chamado na [linha 128](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/email-delivery-workflow.ts:128).

ApÃ³s a claim fenced, a execuÃ§Ã£o ainda:

- resolve destinatÃ¡rio;
- renderiza template;
- pode ser pausada, sofrer throttling ou ficar congelada;
- mantÃ©m lease padrÃ£o de cinco minutos.

Assim, um fence `DELETING` pode ser instalado e a cascata pode avanÃ§ar enquanto uma execuÃ§Ã£o previamente claimed ainda envia o e-mail. A janela nÃ£o Ã© justificadamente â€œde milissegundosâ€; pode ser de minutos ou atÃ© do limite de execuÃ§Ã£o/retry adotado. Drenagem ou um estado operacional que aguarde claims externas precisa fazer parte da condiÃ§Ã£o de conclusÃ£o.

### 6. Bedrock e Textract nÃ£o seguem integralmente o protocolo anunciado

Bedrock reserva quota em [run-bedrock-extraction.ts:145](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-bedrock-extraction.ts:145) e chama o modelo na [linha 162](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-bedrock-extraction.ts:162). HÃ¡ a mesma janela claimâ†’efeito do SES.

Pior: se `quota.consume` encontra uma reserva anterior e lanÃ§a `QuotaExceededError`, o cÃ³digo deliberadamente ignora o erro e continua para Bedrock. Uma reserva antiga nÃ£o prova que a execuÃ§Ã£o atual nasceu de uma escrita fenced posterior; portanto uma retomada pode chamar Bedrock depois de `DELETING`.

Textract reserva quota na [start-ocr.ts:85](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:85), chama o serviÃ§o na [linha 96](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:96) e sÃ³ cria `TextractJob` na [linha 137](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:137). A chamada externa jÃ¡ aconteceu antes da persistÃªncia do job. O desenho nÃ£o define drenagem nem prova que uma reserva preexistente continua autorizando o efeito.

### 7. As URLs presignadas nÃ£o estÃ£o fechadas apenas no â€œponto de finalizaÃ§Ã£oâ€

O problema nÃ£o se limita a URLs jÃ¡ emitidas. Em `GuestSubmissionService.startSubmission`, a transaÃ§Ã£o ocorre antes de `signer.presignUpload`. Portanto:

1. a escrita fenced pode commitar enquanto o tenant estÃ¡ `ACTIVE`;
2. a exclusÃ£o muda o tenant para `DELETING`;
3. a mesma invocation ainda emite e devolve uma URL nova.

O mesmo padrÃ£o existe em `ImportService.reserveImport`: as escritas precedem a emissÃ£o da URL.

A persistÃªncia posterior fenced pode impedir linhas DynamoDB, mas nÃ£o impede emissÃ£o de nova capability apÃ³s o fence nem a criaÃ§Ã£o fÃ­sica do objeto S3. Isso precisa ser incluÃ­do no risco/garantia, com drenagem, epoch/token verificÃ¡vel na finalizaÃ§Ã£o ou polÃ­tica de expiraÃ§Ã£o e varredura que seja parte da conclusÃ£o DSR.

### 8. Um upload tardio pode surgir depois da â€œconvergÃªncia zeroâ€

A cascata verifica zero itens DynamoDB e marca `DELETED`, mas uma URL ainda vÃ¡lida pode receber um objeto S3 depois desse instante. O desenho diz que o objeto ficarÃ¡ Ã³rfÃ£o e serÃ¡ limpo â€œdepoisâ€, porÃ©m nÃ£o demonstra:

- que todos os buckets envolvidos possuem lifecycle apropriado;
- que o objeto tardio Ã© descoberto pelo mecanismo DSR;
- que `DELETED` significa exclusÃ£o fÃ­sica concluÃ­da apesar desse objeto;
- qual Ã© o prazo mÃ¡ximo de eliminaÃ§Ã£o.

Isso conflita com a claim forte de fence de exclusÃ£o fÃ­sica. Um lifecycle eventual pode ser uma polÃ­tica aceitÃ¡vel, mas deve ser especificado como parte verificÃ¡vel do contrato de conclusÃ£o, nÃ£o assumido.

## VerificaÃ§Ãµes sem achado bloqueante

- O tombstone separado e explicitamente excluÃ­do da cascata resolve conceitualmente o achado central de D-063: ele nÃ£o seria apagado junto com `User`. O problema foi resolvido, nÃ£o apenas deslocado â€” desde que a exclusÃ£o seja enforceada por cÃ³digo/teste e que o primitive administrativo seja corrigido.
- NÃ£o encontrei hoje transaÃ§Ã£o prÃ³xima de 100 itens. O maior candidato estrutural Ã© `DynamoDbExtractedFieldStore.commitRunOutcome`, mas o schema atual possui somente um campo em `FIELD_SCHEMA_V1`; a transaÃ§Ã£o atual chega a quatro itens antes do fence, cinco depois. Ainda assim, o wrapper deve rejeitar explicitamente `items.length > 99`, para nÃ£o introduzir falha futura silenciosa.
- NÃ£o encontrei handler construindo diretamente comandos DynamoDB mutÃ¡veis fora dos adapters identificados. O bypass real estÃ¡ nos efeitos e stores S3/SQS/SFN, nÃ£o em um `PutCommand` escondido dentro de handler.

## O que a prÃ³xima proposta precisa mudar

1. Criar primitives separados para bootstrap, writes normais fenced e transiÃ§Ãµes administrativas do lifecycle.
2. Definir atomicamente o tratamento de â€œlifecycle ausenteâ€: novo tenant versus tenant apagado.
3. Substituir a alegaÃ§Ã£o de dependency-cruiser por enforcement AST tecnicamente executÃ¡vel e testado com fixtures positivas/negativas.
4. Publicar inventÃ¡rio gerado por grep de todos os writers: DynamoDB, S3, SES, SQS, SFN, Bedrock e Textract.
5. Definir drenagem/epoch para claims jÃ¡ commitadas antes de permitir `DELETED`.
6. Cobrir emissÃ£o tardia de presigned URLs e objetos S3 criados depois da convergÃªncia.
7. Especificar e testar o limite de 99 itens antes de inserir o ConditionCheck.

A ideia central merece continuaÃ§Ã£o, mas esta proposta ainda nÃ£o sustenta a garantia de nÃ£o-ressurreiÃ§Ã£o nem uma cascata executÃ¡vel.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw -Encoding utf8 "docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md"' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 163ms:
# W3-07 â€” Tenant Deletion Fence, Proposta Round 1 (retomada pÃ³s-D-063)

> Contexto obrigatÃ³rio jÃ¡ lido: `w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md` (D-062)
> e `w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (D-063). Este documento
> assume os achados de ambos como verdadeiros e nÃ£o os redescobre.

## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃ£o por pasta)

Auditoria completa dos 36 handlers reais em `src/runtime/aws/handlers/` contra o Terraform (gatilho declarado,
nÃ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:

1. **NÃ£o existe hoje nenhum cÃ³digo de cascata/fence** â€” greenfield real, nenhuma verificaÃ§Ã£o de "tenant
   excluÃ­do" em lugar nenhum.
2. `RequestContextResolver.resolve()` Ã© o Ãºnico ponto do sistema que checa qualquer status hoje
   (`UserProfile.status !== "ACTIVE"`), e sÃ³ Ã© alcanÃ§ado por rotas HTTP autenticadas. A checagem Ã©
   **desacoplada** (lida uma vez no inÃ­cio do handler, fora de qualquer transaÃ§Ã£o).
3. `GuestSubmissionService.resolveToken()` **nunca** consulta tenant/usuÃ¡rio â€” sÃ³ token/`DocumentRequest`
   (comentÃ¡rio explÃ­cito no cÃ³digo confirmando que Ã© deliberado). Sem fence hoje.
4. `startSubmission()`/`handleReserveUpload`/`handleReserveImport` emitem **URLs S3 presignadas** (PUT) que o
   S3 aceita sem passar por nenhum handler nosso atÃ© o evento "Object Created" disparar a finalizaÃ§Ã£o â€”
   minutos depois, com `tenantId` derivado da **key S3 jÃ¡ gravada**, nÃ£o de contexto revalidado.
5. Toda a cadeia `import-parse â†’ import-commit`, `reminder-producer â†’ reminder-dispatch â†’
   reminder-materialization-trigger`, `dispatch-outbox-relay`/`outbox-sweeper`, `notification-router â†’
   email-delivery â†’ ses-callback`, `document-chasing-dispatch`, `malware-result`/`upload-finalizer`,
   `upload-slot-reconciliation` â€” **nenhuma** checa tenant hoje; `tenantId` vem sempre de um payload jÃ¡
   enfileirado ou de uma key S3 jÃ¡ materializada antes.
6. A Step Functions `document-extraction` (`extraction-starter â†’ RunTextract â†’ RunDeterministicParser â†’
   [RunBedrock] â†’ ValidateSchema/CompareExtractors â†’ PersistExtractedFields/MarkPendingConfirmation â†’
   CompleteRun`) propaga `tenantId` pelo payload de state para state, nunca revalida. As escritas reais
   (`PersistExtractedFields`/`MarkPendingConfirmation`) sÃ³ acontecem nos dois Ãºltimos states â€” minutos depois
   do inÃ­cio, depois de uma chamada Bedrock paga real e de um `waitForTaskToken` do Textract (atÃ© 600s).
7. **Achado que muda o desenho**: apesar da superfÃ­cie parecer enorme (36 handlers, 3 classes de gatilho,
   Step Functions incluÃ­da), praticamente toda escrita de dado de tenant termina em um de dois primitivos
   jÃ¡ existentes e centralizados:
   - `TransactWriteCommand` (DynamoDB), construÃ­do em **8 arquivos de persistÃªncia**
     (`dynamodb-{extracted-field,document,reminder,subject,import,expiration,reminder-producer,notification}-store.ts`),
     usando os builders de `src/shared/dynamodb/occ.ts` (`buildVersionedUpdate`/`buildVersionedCreate`/
     `buildVersionConditionCheck`/`buildExistenceConditionCheck`) â€” regra jÃ¡ normativa em `AGENTS.md` Â§7.
   - `UpdateCommand`/`PutCommand`/`DeleteCommand` single-item, em **11 arquivos de persistÃªncia** (mesma lista
     + `dynamodb-extraction-run-store`, `dynamodb-textract-job-store`, `dynamodb-session-store` [tabela
     separada], `dynamodb-outbox-relay-store`, `dynamodb-identity-store`).
   Isso Ã© uma superfÃ­cie **enumerÃ¡vel e jÃ¡ disciplinada**, nÃ£o 36 pontos ad-hoc â€” o oposto do que quebrou
   D-062/D-063.

## 1. O tombstone (sobrevive Ã  cascata)

Entidade nova `TenantLifecycleRecord`, **fora do universo apagÃ¡vel pela cascata** (a rotina de
descoberta+exclusÃ£o por Scan de D-062/D-063 exclui explicitamente este `entityType` â€” mesma disciplina jÃ¡
usada para excluir a prÃ³pria tabela `bff-session-table` do Scan).

```
PK = "TENANT#<tenantId>#LIFECYCLE"
SK = "LIFECYCLE"
entityType = "TenantLifecycleRecord"
status = "ACTIVE" | "DELETING" | "DELETED"
deletionStartedAt / deletionCompletedAt
version   (OCC padrÃ£o)
```

Vive na tabela principal (nÃ£o numa tabela separada â€” precisa ser lido pelo mesmo `TransactWriteItems` que
grava dado de tenant, e `TransactWriteItems` nÃ£o atravessa tabelas). Nasce `ACTIVE` no primeiro
`createProfileIfAbsent` (mesma transaÃ§Ã£o, custo zero de coordenaÃ§Ã£o adicional).

## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃ£o handler por handler)

Dois wrappers novos em `src/shared/dynamodb/`, substituindo a construÃ§Ã£o direta de comandos nos 8+11 arquivos
de persistÃªncia:

- `fencedTransactWrite(client, { tenantId, items })` â€” adiciona automaticamente, como **primeiro** item da
  lista, um `ConditionCheck` (via `buildExistenceConditionCheck` jÃ¡ existente) contra
  `TENANT#<tenantId>#LIFECYCLE`/`LIFECYCLE` asserindo `status = "ACTIVE"`. Todo `TransactWriteItems` que
  escreve dado de tenant passa a incluir esse ConditionCheck sem que cada service precise lembrar.
- `fencedSingleWrite(client, { tenantId, command })` â€” para os `UpdateCommand`/`PutCommand`/`DeleteCommand`
  isolados, reescreve o comando como um `TransactWriteCommand` de 2 itens (a escrita real + o ConditionCheck
  do fence) â€” DynamoDB nÃ£o tem `ConditionExpression` cross-item para comando single-item, entÃ£o a Ãºnica forma
  atÃ´mica de compor "escreva X E confirme que o tenant estÃ¡ ACTIVE" Ã© uma transaÃ§Ã£o de 2 itens. Aceitar o
  custo de WCU/latÃªncia adicional (jÃ¡ pago hoje pelos outros usos de `TransactWriteItems` no mesmo cÃ³digo).

**Enforcement estrutural, nÃ£o convenÃ§Ã£o**: regra nova de `dependency-cruiser` (mesmo mecanismo que jÃ¡ proÃ­be
`shared` de importar `modules/**`) proibindo `new TransactWriteCommand(`/`new UpdateCommand(`/`new
PutCommand(`/`new DeleteCommand(` fora de `src/shared/dynamodb/fenced-write.ts` â€” fecha a classe de erro
"um call site novo esqueceu o fence", que foi exatamente a causa raiz do achado mais grave de D-063
(auditoria incompleta por pasta).

Isso fecha, sem tocar em nenhum dos 36 handlers individualmente: `items-handler`, `documents-handler` (a
persistÃªncia do `Document`, nÃ£o a emissÃ£o da URL presignada â€” ver Â§4), `guest-documents-handler` (via
`DocumentSubmission`), `import-commit-handler` (mesmo com `systemContextFor()` sintÃ©tico â€” o fence nÃ£o
depende de `RequestContext`, Ã© checado na escrita, nÃ£o na autorizaÃ§Ã£o), toda a cadeia de lembretes/outbox,
`notification-router`/`email-delivery` (a gravaÃ§Ã£o da tentativa, nÃ£o o `ses:SendEmail` em si â€” ver Â§5),
`malware-result`/`upload-finalizer`, `upload-slot-reconciliation`, e as duas tasks de escrita da Step
Functions (`PersistExtractedFields`/`MarkPendingConfirmation`).

## 3. Fechando a ressurreiÃ§Ã£o no resolver (defesa em profundidade, nÃ£o a defesa primÃ¡ria)

`RequestContextResolver.resolve()` ganha uma leitura do `TenantLifecycleRecord` **antes** de
`createProfileIfAbsent` (nÃ£o substitui a checagem de `UserProfile.status`, Ã© adicional e anterior). Se
`status !== "ACTIVE"`, rejeita com 403 sem provisionar nada. Isso Ã© defesa em profundidade â€” mesmo que
faltasse, o Â§2 jÃ¡ impediria a escrita real do `User` recriado, porque `createProfileIfAbsent` tambÃ©m passa
pelo `fencedSingleWrite`. A checagem no resolver existe para dar um erro claro (403) em vez de deixar o
usuÃ¡rio achar que logou e sÃ³ descobrir a rejeiÃ§Ã£o no primeiro POST.

## 4. URLs presignadas jÃ¡ emitidas (S3)

NÃ£o sÃ£o revogÃ¡veis diretamente. MitigaÃ§Ã£o real, nÃ£o best-effort: a finalizaÃ§Ã£o (`upload-finalizer-handler`/
`malware-result-handler`/equivalente guest) Ã© exatamente o ponto que persiste `Document`/`DocumentSubmission`
via `fencedTransactWrite` (Â§2) â€” se o tenant jÃ¡ estÃ¡ `DELETING`, a transaÃ§Ã£o de finalizaÃ§Ã£o falha
(`ConditionalCheckFailedException`), o objeto S3 fica Ã³rfÃ£o na quarantine (limpo depois por
`document-purge-handler`/lifecycle, mecanismo jÃ¡ aprovado em D-061), mas **nenhuma linha DynamoDB nova
aparece**. Resultado: nenhuma ressurreiÃ§Ã£o de dado consultÃ¡vel, sÃ³ lixo de storage jÃ¡ coberto por mecanismo
existente. TTL curto (600s) jÃ¡ limita a janela de exposiÃ§Ã£o.

## 5. Efeitos externos irreversÃ­veis (SES, Bedrock, Textract) â€” protocolo de duas fases, nÃ£o checagem simples

Nenhum destes tem `ConditionExpression` atÃ´mica nativa. Desenho: **a decisÃ£o de disparar o efeito precisa
nascer de uma escrita fenced**, nÃ£o de uma leitura solta:

- `email-delivery-handler`: antes de `ses:SendEmail`, marca `NotificationAttempt.status = "SENDING"` via
  `fencedTransactWrite`. Se o tenant jÃ¡ estÃ¡ `DELETING`, a marcaÃ§Ã£o falha e o handler nunca chama SES. Um
  e-mail cuja marcaÃ§Ã£o jÃ¡ commitou antes do `DELETING` ser escrito ainda sai (janela de milissegundos,
  inerente a qualquer sistema com efeito externo assÃ­ncrono â€” documentado como risco residual aceito, nÃ£o
  escondido).
- `document-chasing-dispatch-handler`: mesmo padrÃ£o â€” o novo `GuestTokenPointer` sÃ³ Ã© criado via
  `fencedTransactWrite`; se falhar, nenhum token novo existe, nenhum e-mail Ã© enviado.
- Bedrock/Textract (custo, nÃ£o dado): `BedrockExtractionTaskHandler`/`TextractTaskHandler` jÃ¡ gravam
  `TenantQuotaService`/`TextractJob` via os stores fenced do Â§2 â€” se o tenant foi excluÃ­do entre o
  `RunTextract` e o `RunBedrock`, a chamada Bedrock ainda ocorre (custo real, nÃ£o dado), mas
  `PersistExtractedFields` falha depois. Custo residual pequeno e limitado, nÃ£o uma garantia de dado quebrada.

## 6. SequÃªncia da cascata (drenagem antes de apagar)

1. `fencedSingleWrite` grava `TenantLifecycleRecord.status = "DELETING"` (create condicional
   `attribute_not_exists(PK)` â†’ falha se uma cascata jÃ¡ estÃ¡ em curso, evita corrida de duplo-start).
2. Best-effort: `StopExecution` em toda execuÃ§Ã£o Step Functions ativa cujo payload referencia o tenant
   (via tag/GSI jÃ¡ existente do desenho de extraÃ§Ã£o) â€” reduz custo Bedrock/Textract desperdiÃ§ado, nÃ£o Ã©
   requisito de correÃ§Ã£o (o fence do Â§2/Â§5 jÃ¡ garante correÃ§Ã£o sem isto).
3. Roda a descoberta+exclusÃ£o jÃ¡ aprovada (Scan + taxonomia de ~40 `entityType`, convergÃªncia por re-Scan),
   reusando `DocumentPurgeWorker`/GSI6 para `Document` (D-061) â€” mecanismo inalterado desta rodada.
4. `TenantLifecycleRecord` **nunca** Ã© alvo do Scan (exclusÃ£o explÃ­cita por `entityType`, mesma disciplina jÃ¡
   usada para `bff-session-table`).
5. ConvergÃªncia confirmada (zero itens restantes) â†’ `fencedSingleWrite` flip para `status = "DELETED"`
   (permanente; nenhum cÃ³digo reabre um tenant `DELETED` â€” fora de escopo desta feature).

## 7. O que este desenho NÃƒO resolve (fora de escopo explÃ­cito, registrar e nÃ£o mascarar)

- Rotas HTTP/confirmaÃ§Ã£o/exportaÃ§Ã£o de DSR completo â€” jÃ¡ fora de escopo desde D-062, inalterado.
- `PolicyRef` sem `tenantId` (achado independente de D-062) â€” correÃ§Ã£o mecÃ¢nica separada.
- `tenantId` (MVP `tenantId=userId`) â‰  Cognito `sub` â€” aÃ§Ã£o administrativa futura no Cognito por tenant
  precisa resolver isso Ã  parte; nÃ£o bloqueia este desenho porque a cascata nunca chama a API do Cognito.

## 8. Por que isto nÃ£o repete os erros de D-062/D-063

- O fence nÃ£o Ã© a mesma linha que a cascata apaga (Â§1, `TenantLifecycleRecord` explicitamente excluÃ­do do
  Scan) â€” resolve o achado mais grave de D-063.
- A checagem Ã© acoplada (`ConditionCheck` na mesma transaÃ§Ã£o), nunca uma leitura solta antes de agir â€”
  resolve o segundo achado de D-063 (TOCTOU).
- A cobertura nÃ£o depende de enumerar 36 handlers corretamente â€” depende de 19 arquivos de persistÃªncia jÃ¡
  conhecidos + uma regra de lint estrutural que impede um call site futuro de escapar do fence. Reduz a
  classe de erro "auditoria incompleta" de "confiar em uma lista manual" para "confiar numa regra estÃ¡tica
  verificÃ¡vel por `dependency-cruiser`" â€” resolve a causa raiz comum Ã s duas rodadas reprovadas.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw -Encoding utf8 docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round1-critique-full.txt' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 176ms:
2026-08-28T21:07:00.618902Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a04a32-89d5-7b80-9392-0b11cace87ff
--------
user
VocÃª Ã© o revisor adversarial independente (protocolo Claude-Codex de AGENTS.md secao 4) do repositorio
expiration-tracker (c:/Users/Usuario/Desktop/projects/expiration-tracker). Tarefa: W3-07, fence de exclusao
fisica de tenant (DSR), retomada apos duas tentativas reprovadas (D-062: 3,4-5,1-4,7/10; D-063 Rodada 1:
3,2/10 - pior que a anterior).

Leia primeiro, na integra, ANTES de avaliar a proposta nova:
1. docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md (D-062)
2. docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md (D-063)
3. docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md (a proposta nova a avaliar)
4. src/modules/identity/application/resolve-request-context.ts (resolver real)
5. src/shared/dynamodb/occ.ts (builders/primitivos reais ja existentes que a proposta reusa)
6. src/modules/subject/application/guest-submission-service.ts (superficie guest, se existir - confirme o caminho real)

Contexto necessario: a proposta nova muda a estrategia dos dois desenhos reprovados. Em vez de enumerar e
corrigir cada um dos 36 handlers de src/runtime/aws/handlers/ individualmente (a causa raiz do fracasso das
duas tentativas anteriores foi auditoria incompleta por essa via), ela propoe interceptar o fence na camada
de escrita compartilhada: um tombstone TenantLifecycleRecord fora do universo apagavel pela cascata, mais
dois wrappers novos (fencedTransactWrite/fencedSingleWrite) que toda escrita DynamoDB de dado de tenant
passaria a usar, reforcados por uma regra de dependency-cruiser que proibe construir TransactWriteCommand/
UpdateCommand/PutCommand/DeleteCommand fora desses wrappers. Efeitos externos irreversiveis (SES, Bedrock,
Textract) usam um protocolo de "decisao nasce de escrita fenced antes do efeito", nao uma checagem simples.
URLs S3 presignadas ja emitidas sao fechadas no ponto de finalizacao (persistencia), nao na emissao.

Sua tarefa: critique adversarialmente, do zero, sem aceitar nenhuma alegacao da proposta sem verificar contra
o codigo real do repositorio (grep, leitura de arquivo). Responda com:
1. Nota de 0 a 10 (protocolo de nota cega - nao existe nota anterior a comparar, esta e a primeira desta
   rodada).
2. Achados REAIS, cada um confirmado contra codigo/arquitetura real (nome de arquivo/linha quando aplicavel),
   nao hipoteticos. De atencao especial a: (a) os 19 arquivos de persistencia realmente cobrem TODA escrita
   de dado de tenant, ou existe algum call site fora desses 19 (grep exaustivo, nao confie na lista da
   proposta); (b) a regra de dependency-cruiser proposta e tecnicamente viavel para banir construcao direta
   desses comandos AWS SDK sem falsos positivos (ex.: os proprios wrappers fencedTransactWrite/
   fencedSingleWrite PRECISAM construir esses comandos - a regra precisa ter excecao correta); (c) o
   ConditionCheck contra TenantLifecycleRecord adicionado como PRIMEIRO item de um TransactWriteItems ja
   proximo do limite de 100 itens quebra algum caso real hoje; (d) a materializacao do RequestContextResolver
   (createProfileIfAbsent) realmente passa pelo wrapper fenced way descrito, ou tem um caminho proprio que
   escaparia; (e) o achado mais grave de D-063 (fence apagavel pela propria cascata) esta genuinamente
   resolvido, nao so deslocado; (f) o protocolo de "decisao nasce de escrita fenced" para SES/Bedrock tem
   alguma janela de corrida nao reconhecida pela propria proposta; (g) qualquer worker/handler que escreva
   dado de tenant SEM passar por nenhum dos 19 arquivos de persistencia listados (o achado exaustivo anterior
   levantou 36 handlers - confirme se algum deles bypassa os stores).
3. Recomendacao: aprovar (>=9.0), pedir revisao (com o que precisa mudar), ou reprovar.

Nao segure achados por educacao - o historico deste repositorio mostra que apontar "parece bom" sem verificar
levou a duas reprovacoes tardias e caras. Seja tao rigoroso quanto (ou mais que) as rodadas anteriores.

codex
Vou conduzir a revisÃ£o adversarial pelo protocolo do repositÃ³rio. Primeiro lerei integralmente os seis artefatos obrigatÃ³rios e os Ã­ndices de sessÃ£o/arquitetura; depois farei buscas exaustivas independentes por construÃ§Ãµes e execuÃ§Ãµes de comandos DynamoDB, limites transacionais e efeitos externos, sem tomar a lista da proposta como fonte de verdade.
2026-08-28T21:07:11.202530Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md; Get-Content -Raw src/modules/identity/application/resolve-request-context.ts; Get-Content -Raw src/shared/dynamodb/occ.ts; if (Test-Path src/modules/subject/application/guest-submission-service.ts) { Get-Content -Raw src/modules/subject/application/guest-submission-service.ts } else { Write-Output 'MISSING_GUEST_PATH'; rg --files src | rg 'guest|submission' }"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md; Get-Content -Raw src/modules/identity/application/resolve-request-context.ts; Get-Content -Raw src/shared/dynamodb/occ.ts; if (Test-Path src/modules/subject/application/guest-submission-service.ts) { Get-Content -Raw src/modules/subject/application/guest-submission-service.ts } else { Write-Output 'MISSING_GUEST_PATH'; rg --files src | rg 'guest|submission' }" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md; Get-Content -Raw src/modules/identity/application/resolve-request-context.ts; Get-Content -Raw src/shared/dynamodb/occ.ts; if (Test-Path src/modules/subject/application/guest-submission-service.ts) { Get-Content -Raw src/modules/subject/application/guest-submission-service.ts } else { Write-Output 'MISSING_GUEST_PATH'; rg --files src | rg 'guest|submission' }"` rejected: blocked by policy
codex
A leitura agrupada foi bloqueada pela polÃ­tica do terminal; vou fazer chamadas isoladas, reafirmando o diretÃ³rio em cada uma, como exige o AGENTS.md. Isso nÃ£o altera o escopo nem o mÃ©todo da revisÃ£o.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'NEXT_SESSION_PROMPT.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 180ms:
# Expiration Tracker Ã¢â‚¬â€ Status e PrÃƒÂ³xima SessÃƒÂ£o

> Este arquivo ÃƒÂ© estado atual + prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o (`AGENTS.md` Ã‚Â§2), nÃƒÂ£o histÃƒÂ³rico. Para a linha do tempo completa por sessÃƒÂ£o, ver `docs/architecture/session-log.md`; para toda decisÃƒÂ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃƒÂ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Ã‚Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) Ã¢â‚¬â€ confirmar `git log`/branch atual antes de assumir que o PR jÃƒÂ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃƒÂ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program Ã¢â‚¬â€ Waves 0/2/3/5 concluÃƒÂ­das, Wave 4 parcial (gated), Wave 1 nÃƒÂ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃƒÂ£o de consolidaÃƒÂ§ÃƒÂ£o/pilot-readiness. **Backlog canÃƒÂ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃƒÂ­ntese final/recomendaÃƒÂ§ÃƒÂ£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃƒÂ¡vel do prompt mestre Ã‚Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) Ã¢â‚¬â€ nÃƒÂ£o duplicar detalhe aqui, sÃƒÂ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃƒÂ§ÃƒÂ£o) `DONE`. **Wave 2 (evidÃƒÂªncia operacional) `DONE`** (2026-08-28) Ã¢â‚¬â€ os 6 drills (W2-03 a W2-08) executados com evidÃƒÂªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO Ã¢â€°Ë†3min44s, RPO nÃƒÂ£o medido Ã¢â‚¬â€ item aberto), load test real (977 invocaÃƒÂ§ÃƒÂµes, cota `API_REQUEST` segurando sob carga), alarme forÃƒÂ§ado com notificaÃƒÂ§ÃƒÂ£o SNSÃ¢â€ â€™e-mail real confirmada. **RPO (W2-06), dedupe pÃƒÂ³s-commit (W2-05) e o pipeline de alarme do W2-08 fechados na mesma sessÃƒÂ£o (2026-08-28)** Ã¢â‚¬â€ ver `pilot-readiness-program.md` seÃƒÂ§ÃƒÂ£o Wave 2 para o detalhe completo (inclui o achado real de que "credential-compromise via HTTP" ÃƒÂ© estruturalmente inalcanÃƒÂ§ÃƒÂ¡vel hoje, nÃƒÂ£o uma lacuna). Claims especÃƒÂ­ficas por drill: ver `docs/engineering/test-engineering-standard.md` Ã‚Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada. **`W3-06-DECISION` fechada e implementada nesta sessÃƒÂ£o (2026-08-28)**: desenho do mecanismo de purga real de `USER_DOCUMENT` aprovado via protocolo ClaudeÃ¢â€ â€Codex completo (6 rodadas, Codex 9,2/Claude 9,1, D-061 em `decisions-log.md`) e implementado de ponta a ponta Ã¢â‚¬â€ `DocumentPurgeWorker` real (claim/lease sobre GSI6, apaga o objeto S3 real via `cleanObject`/evidÃƒÂªncia Ã¢â‚¬â€ nunca `quarantineObject` Ã¢â‚¬â€ e a linha `Document`, grava `DocumentPurgeReceipt` nÃƒÂ£o sensÃƒÂ­vel), agendado a cada 6h, verificado com `terraform plan`/`terraform test` reais contra `dev` (ver `docs/architecture/reviews/w3-06-user-document-purge-design/`). Wave 4 (Identity/RBAC) `PARTIAL` Ã¢â‚¬â€ confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃƒÂ£o de negÃƒÂ³cio jÃƒÂ¡ registrada (`AGENTS.md` Ã‚Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) Ã¢â‚¬â€ `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃƒÂ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃƒÂ£o (2026-08-28) Ã¢â‚¬â€ auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard Ã¢â‚¬â€ novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃƒÂ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados com fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa precisa da Wave 2. Protocolo ClaudeÃ¢â€ â€Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃƒÂ­cito do Marcelo, acima do 9,0 padrÃƒÂ£o) Ã¢â‚¬â€ trajetÃƒÂ³ria Codex 6.35Ã¢â€ â€™7.85Ã¢â€ â€™8.70Ã¢â€ â€™9.18Ã¢â€ â€™9.34Ã¢â€ â€™9.46Ã¢â€ â€™9.48Ã¢â€ â€™**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃƒÂ§ÃƒÂ£o D-0xx). EvidÃƒÂªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃƒÂ£o vale para qualquer teste/drill novo a partir de agora** Ã¢â‚¬â€ antes de declarar um teste ou drill "concluÃƒÂ­do", checar contra os gates de Ã‚Â§3 (nunca reversÃƒÂ£o silenciosa, nunca claim mais ampla que a evidÃƒÂªncia).

**RecomendaÃƒÂ§ÃƒÂ£o da sÃƒÂ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃƒÂªs gates reais nomeados na sÃƒÂ­ntese original Ã¢â‚¬â€ guest trust (`GTR-01`/`W5-01`), evidÃƒÂªncia operacional (Wave 2), e retenÃƒÂ§ÃƒÂ£o/purga real (`W3-06`) Ã¢â‚¬â€ **fecharam todos nesta sessÃƒÂ£o**, e todos os itens secundÃƒÂ¡rios de Wave 2 (RPO, dedupe pÃƒÂ³s-commit, pipeline de alarme de credential-compromise) tambÃƒÂ©m fecharam na mesma sessÃƒÂ£o. ÃƒÅ¡nico item nÃƒÂ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).

954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃƒÂ§ÃƒÂ£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃƒÂ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃƒÂ§ÃƒÂ£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃƒÂ£o** (W2-06 RPO, W2-05 dedupe pÃƒÂ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃƒÂ£o, nenhuma aprovada**: (1) D-062, 4 rodadas ClaudeÃ¢â€ â€Codex, notas 3,4Ã¢â€ â€™5,1Ã¢â€ â€™4,7/10 Ã¢â‚¬â€ achado central: garantir "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃƒÂ­ncronos); Claude e Codex recomendaram convergentemente nÃƒÂ£o implementar sem esse fence, e a sessÃƒÂ£o parou por decisÃƒÂ£o do Marcelo. (2) O Marcelo entÃƒÂ£o decidiu NÃƒÆ’O esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃƒÂ­cio Ã¢â‚¬â€ D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃƒÂ¡ existente e jÃƒÂ¡ checado em `resolveRequestContext`) seria apagado pela prÃƒÂ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃƒÂ³ximo login Ã¢â‚¬â€ o mecanismo de autenticaÃƒÂ§ÃƒÂ£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada** (nÃƒÂ£o abandonado). **PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃƒÂ§ÃƒÂ£o de levantar superfÃƒÂ­cies de escrita por ponto de entrada de runtime Ã¢â‚¬â€ API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions Ã¢â‚¬â€ nunca por pasta de cÃƒÂ³digo, que jÃƒÂ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso Ã¢â‚¬â€ os 3 gates reais (W3-06/Wave 2/GTR-01) jÃƒÂ¡ fecharam.

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃƒÂ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃƒÂ³rio e passou a ter a primeira linguagem visual real do produto Ã¢â‚¬â€ arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃƒÂ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃƒÂ§ÃƒÂ£o **Operational Calm** aplicada ÃƒÂ s 5 superfÃƒÂ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION Ã¢â‚¬â€ PROVISIONAL PENDING USER VALIDATION` Ã¢â‚¬â€ a palavra PROVISIONAL ÃƒÂ© literal: nenhuma sessÃƒÂ£o com usuÃƒÂ¡rio aconteceu, e as **15** hipÃƒÂ³teses de design (hierarquia de navegaÃƒÂ§ÃƒÂ£o, densidade final, rÃƒÂ³tulos, colocaÃƒÂ§ÃƒÂ£o de aÃƒÂ§ÃƒÂµes secundÃƒÂ¡rias, accent, e as duas afirmaÃƒÂ§ÃƒÂµes subjetivas sobre a prÃƒÂ³pria direÃƒÂ§ÃƒÂ£o) estÃƒÂ£o registradas como adiamentos explÃƒÂ­citos no Ã‚Â§35 do documento, nÃƒÂ£o como fatos. **Zero dependÃƒÂªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de ÃƒÂ­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃƒÂ§a estrutural ÃƒÂºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃƒÂ¢ntica com urgÃƒÂªncia e situaÃƒÂ§ÃƒÂ£o em colunas separadas Ã¢â‚¬â€ mesmos dados/ordenaÃƒÂ§ÃƒÂ£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃƒÂªnticos, 3 grupos de urgÃƒÂªncia) foi verificada contra o cÃƒÂ³digo real do frontend **pela primeira vez** (antes sÃƒÂ³ contra o protÃƒÂ³tipo), com asserÃƒÂ§ÃƒÂ£o automatizada. Protocolo ClaudeÃ¢â€ â€Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 Ã¢â‚¬â€ `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃƒÂ§ÃƒÂ£o normal, entÃƒÂ£o o botÃƒÂ£o de submit continuava clicÃƒÂ¡vel durante a mutaÃƒÂ§ÃƒÂ£o exibindo "RenovandoÃ¢â‚¬Â¦"; e os cabeÃƒÂ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃƒÂ§am linhas Ã¢â‚¬â€ ambos corrigidos com teste de regressÃƒÂ£o. O protocolo **nÃƒÂ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃƒÂ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Ã‚Â§4) Ã¢â‚¬â€ o protocolo mais longo jÃƒÂ¡ executado neste repositÃƒÂ³rio. TrÃƒÂªs rodadas acharam defeitos **criados pela rodada de correÃƒÂ§ÃƒÂ£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃƒÂ£o foi cÃƒÂ³digo errado e sim **documentaÃƒÂ§ÃƒÂ£o afirmando prova mais ampla que a evidÃƒÂªncia nomeada**, que apareceu seis vezes: a alegaÃƒÂ§ÃƒÂ£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃƒÂ§ÃƒÂ£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃƒÂµes futuras deste repositÃƒÂ³rio. Acessibilidade virou executÃƒÂ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃƒÂ§ÃƒÂµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃƒÂ§ÃƒÂµes declaradas e registro de decisÃƒÂµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃƒÂ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃƒÂ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃƒÂªncia real registrada, nÃƒÂ£o bloqueante**: as baselines de regressÃƒÂ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃƒÂ£o por plataforma, entÃƒÂ£o o projeto Playwright `visual` ÃƒÂ© deliberadamente separado de `npm run test:e2e` e **nÃƒÂ£o** estÃƒÂ¡ gatilhado no CI hoje (plugÃƒÂ¡-lo falharia por baseline ausente, nÃƒÂ£o por regressÃƒÂ£o real). O caminho de adoÃƒÂ§ÃƒÂ£o estÃƒÂ¡ escrito no Ã‚Â§31 do documento: gravar as baselines num runner Linux, commitÃƒÂ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃƒÂ© lÃƒÂ¡ o CI cobre as mesmas superfÃƒÂ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃƒÂ©m nÃƒÂ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃƒÂ­veis no ambiente) Ã¢â‚¬â€ `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos Ã¢â‚¬â€ Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration Ã¢â‚¬â€ sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real Ã¢â€ â€™ BFF real Ã¢â€ â€™ API real Ã¢â€ â€™ persistÃƒÂªncia real). Nenhuma rota nova de BFF foi necessÃƒÂ¡ria (todas as 6 rotas de item jÃƒÂ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo ClaudeÃ¢â€ â€Codex (Round B adversarial achou 4 bugs reais Ã¢â‚¬â€ 1 S1: corrida TOCTOU real na reaquisiÃƒÂ§ÃƒÂ£o de um registro de idempotÃƒÂªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃƒÂ§ÃƒÂ£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃƒÂ³ `idempotency.complete()` falhasse depois], hash de renovaÃƒÂ§ÃƒÂ£o ambÃƒÂ­guo quando `cycle` ÃƒÂ© enviado independente de `newDueDate`, bug de fuso horÃƒÂ¡rio na formataÃƒÂ§ÃƒÂ£o de data da `Overview` Ã¢â‚¬â€ todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃƒÂ§ÃƒÂ£o tambÃƒÂ©m foi corrigido um bug real prÃƒÂ©-existente de liveness de idempotÃƒÂªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃƒÂªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) Ã¢â‚¬â€ a correÃƒÂ§ÃƒÂ£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃƒÂ§ado pela correÃƒÂ§ÃƒÂ£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃƒÂ§ÃƒÂ£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃƒÂ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃƒÂ§ÃƒÂ£o), 621 testes de backend Ã¢â‚¬â€ nenhuma regressÃƒÂ£o. Pendente ao fim desta sessÃƒÂ£o: push, PR para `develop`, CI, merge (ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" abaixo se ainda nÃƒÂ£o concluÃƒÂ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) Ã¢â‚¬â€ todos deployados em `main`/`dev`. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) teve implementaÃƒÂ§ÃƒÂ£o iniciada em 2026-08-25** (D-057, decisÃƒÂ£o direta do Marcelo Ã¢â‚¬â€ "prossiga entÃƒÂ£o") Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃƒÂ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃƒÂ£o disparou.

**Full BFF (autenticaÃƒÂ§ÃƒÂ£o de browser)**: design fechado via protocolo ClaudeÃ¢â€ â€Codex em duas rodadas Ã¢â‚¬â€ D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃƒÂ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` Ã¢â‚¬â€ 76 testes unitÃƒÂ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o real (`frontend/` Ã¢â‚¬â€ Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃƒÂ¡rio/componente + 6 smoke E2E via Playwright). Protocolo ClaudeÃ¢â€ â€Codex completo (Rodada D levou **6 passagens** atÃƒÂ© convergir Ã¢â‚¬â€ 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃƒÂ³digo prÃƒÂ©-correÃƒÂ§ÃƒÂ£o: consumo nÃƒÂ£o-atÃƒÂ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃƒÂ£o revogada concorrentemente; releituras pÃƒÂ³s-refresh nÃƒÂ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃƒÂ£o checando expiraÃƒÂ§ÃƒÂ£o/idle-TTL antes de agir; idle-TTL da prÃƒÂ³pria `Session` nunca validado contra o relÃƒÂ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃƒÂªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Ã‚Âº-8Ã‚Âº), cada um fechado via protocolo ClaudeÃ¢â€ â€Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` Ã¢â‚¬â€ papÃƒÂ©is, JTBD, inventÃƒÂ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` Ã¢â‚¬â€ modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` Ã¢â‚¬â€ 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` Ã¢â‚¬â€ 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes SurfaceÃ¢â€ â€Journey/Concept/Transition. RevisÃƒÂ£o Codex achou 3 furos reais de mesma causa raiz Ã¢â‚¬â€ `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃƒÂ§a em `SCANNING`, nÃƒÂ£o sÃƒÂ³ em `CLEAN`) Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` Ã¢â‚¬â€ wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃƒÂ£o Codex achou 5 furos reais + 1 divergÃƒÂªncia factual Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o primÃƒÂ¡ria ambÃƒÂ­gua em 4 coleÃƒÂ§ÃƒÂµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas Ã¢â‚¬â€ todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃƒÂ³digo executÃƒÂ¡vel real, HTML/CSS/JS sem dependÃƒÂªncias) Ã¢â‚¬â€ protÃƒÂ³tipo navegÃƒÂ¡vel cobrindo as 17 superfÃƒÂ­cies e J-01Ã¢â‚¬â€œJ-08 com 34 Prototype Scenario IDs determinÃƒÂ­sticos, verificado com testes automatizados de navegador headless. RevisÃƒÂ£o Codex achou 6 furos reais de 4 causas raiz Ã¢â‚¬â€ compressÃƒÂ£o de estados no guest upload (faltava validaÃƒÂ§ÃƒÂ£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃƒÂ§ÃƒÂ£o de coleta externa violando epistemic integrity (anunciava verificaÃƒÂ§ÃƒÂ£o de seguranÃƒÂ§a concluÃƒÂ­da ao operador, que nÃƒÂ£o pode observar isso), dois campos sem `<label>`, e reincidÃƒÂªncia de menÃƒÂ§ÃƒÂ£o a WhatsApp Ã¢â‚¬â€ todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` Ã¢â‚¬â€ avaliaÃƒÂ§ÃƒÂ£o do protÃƒÂ³tipo **executÃƒÂ¡vel** (nÃƒÂ£o sÃƒÂ³ a documentaÃƒÂ§ÃƒÂ£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃƒÂ¢ntica/forms testados em navegador headless real, nÃƒÂ£o sÃƒÂ³ lidos), `axe-core` (0 violaÃƒÂ§ÃƒÂµes em todos os estados testados, nas duas rodadas), re-execuÃƒÂ§ÃƒÂ£o de J-01Ã¢â‚¬â€œJ-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** ClaudeÃ¢â€ â€Codex (nÃƒÂ£o as 2-3 rodadas tÃƒÂ­picas de etapas anteriores): Rodada A (autoavaliaÃƒÂ§ÃƒÂ£o, 9 achados corrigidos) concluiu aprovaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃƒÂ£o vistos pela autoavaliaÃƒÂ§ÃƒÂ£o: o mais grave, a prÃƒÂ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃƒÂ§ÃƒÂ£o em 3 formulÃƒÂ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃƒÂ§ÃƒÂ£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃƒÂ¡-los no fake-backend Ã¢â‚¬â€ uma violaÃƒÂ§ÃƒÂ£o epistÃƒÂªmica real, nÃƒÂ£o cosmÃƒÂ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃƒÂ³prio ao manter um texto "SIMULATED FOR UX VALIDATION Ã¢â‚¬â€ NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃƒÂ­cie pÃƒÂºblica de guest (jargÃƒÂ£o tÃƒÂ©cnico visÃƒÂ­vel a um usuÃƒÂ¡rio externo nÃƒÂ£o-tÃƒÂ©cnico). Rodada D (nova adversarial sobre o cÃƒÂ³digo jÃƒÂ¡ corrigido) achou essa lacuna de conteÃƒÂºdo e uma correÃƒÂ§ÃƒÂ£o incompleta nova (`submitAlert` reabilitava o botÃƒÂ£o sÃƒÂ³ na branch de erro, nÃƒÂ£o na de sucesso) Ã¢â‚¬â€ ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃƒÂ£o arredondado; calculado refletindo esse histÃƒÂ³rico de 4 rodadas, nÃƒÂ£o sÃƒÂ³ o estado final do cÃƒÂ³digo) Ã¢â‚¬â€ `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` Ã¢â‚¬â€ "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃƒÂ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃƒÂ£o) no protÃƒÂ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃƒÂ§ÃƒÂ£o, nunca marcado como backend-resolvido; cenÃƒÂ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃƒÂ§ÃƒÂµes) que achou e corrigiu uma falta real de ordenaÃƒÂ§ÃƒÂ£o por urgÃƒÂªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃƒÂ£o sÃƒÂ³ no protÃƒÂ³tipo) Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃƒÂ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃƒÂ¡ em uso desde a 1Ã‚Âª etapa, agora formal, nenhum critÃƒÂ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃƒÂ§ÃƒÂ£o tÃƒÂ©cnica em Participant Mode, teste de idempotÃƒÂªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃƒÂµes de documentaÃƒÂ§ÃƒÂ£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃƒÂ©-existentes desde a Rodada A) Ã¢â‚¬â€ corrigidos no fechamento, zero contaminaÃƒÂ§ÃƒÂ£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃƒÂ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** Ã¢â‚¬â€ **fechado nesta sessÃƒÂ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` Ã¢â‚¬â€ `DocumentService.listDocuments`/`getDocument`, autorizaÃƒÂ§ÃƒÂ£o `document:read` (aÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃƒÂ§ÃƒÂ£o do item jÃƒÂ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃƒÂ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` Ã¢â‚¬â€ `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃƒÂ§ÃƒÂ£o `requirement:read` jÃƒÂ¡ existente (sem action nova reservada de antemÃƒÂ£o, diferente de `document:read`), mesma leitura por partiÃƒÂ§ÃƒÂ£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃƒÂ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© desta sessÃƒÂ£o, jÃƒÂ¡ estava corrigido antes (comentÃƒÂ¡rio no prÃƒÂ³prio `main.tf`), sÃƒÂ³ confirmado que segue correto.

  Ambas as metades: mudanÃƒÂ§a mecÃƒÂ¢nica sobre um padrÃƒÂ£o de acesso jÃƒÂ¡ aprovado no data model Ã¢â‚¬â€ nÃƒÂ£o passaram pelo protocolo `AGENTS.md` Ã‚Â§4 completo (nÃƒÂ£o ÃƒÂ© decisÃƒÂ£o de arquitetura nova, ÃƒÂ© fechamento de gap de leitura jÃƒÂ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃƒÂ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃƒÂ­cio da sessÃƒÂ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃƒÂ£o. **Achado real corrigido depois, na mesma sessÃƒÂ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` Ã¢â‚¬â€ mesma classe de gap que as rotas de `DocumentRequest` jÃƒÂ¡ tiveram antes (comentÃƒÂ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** Ã¢â‚¬â€ **implementaÃƒÂ§ÃƒÂ£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃƒÂ§ÃƒÂ£o abaixo). Antes desta branch, a materializaÃƒÂ§ÃƒÂ£o automÃƒÂ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃƒÂ§ÃƒÂ£o/ediÃƒÂ§ÃƒÂ£o de item. PendÃƒÂªncia real restante: decisÃƒÂ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) Ã¢â‚¬â€ nÃƒÂ£o bloqueia mais nada alÃƒÂ©m dessa decisÃƒÂ£o em si.
- **BLOCKER-C** Ã¢â‚¬â€ **decisÃƒÂ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃƒÂ£o humana explÃƒÂ­cita)**, confirmando a hipÃƒÂ³tese lÃƒÂ­der jÃƒÂ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Ã‚Â§22, `interface-conceptual-model-and-information-architecture.md` Ã‚Â§37, `interface-critical-user-journeys.md` Ã‚Â§37 Ã¢â‚¬â€ esses documentos continuam como registro histÃƒÂ³rico do debate, nÃƒÂ£o reescritos, per `AGENTS.md` regra de precedÃƒÂªncia de `history/`/artefatos aprovados). InvestigaÃƒÂ§ÃƒÂ£o confirmou que Variante A (fechamento automÃƒÂ¡tico) nÃƒÂ£o ÃƒÂ© viÃƒÂ¡vel hoje: `DocumentSubmission` nÃƒÂ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃƒÂ§a a qual `ExpirationItem` uma submissÃƒÂ£o pertence Ã¢â‚¬â€ decidir automaticamente seria adivinhar, nÃƒÂ£o resolver. **Mecanismo de backend para a Variante B jÃƒÂ¡ existe, nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio**: visibilidade do operador sobre submissÃƒÂµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃƒÂ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ existente (`POST .../link`, prÃƒÂ©-existente). **Frontend implementado na mesma sessÃƒÂ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject Ã¢â‚¬â€ `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃƒÂ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores Ã¢â€ â€™ detalhe com requisitos Ã¢â€ â€™ "Revisar" expande evidÃƒÂªncia enviada (`DocumentSubmission`) Ã¢â€ â€™ operador informa o `itemId` de um vencimento jÃƒÂ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃƒÂ§ÃƒÂ£o de subject nem sugestÃƒÂ£o automÃƒÂ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃƒÂ³tulos seguem a mesma disciplina de Epistemic Integrity jÃƒÂ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃƒÂ§a) Ã¢â‚¬â€ conteÃƒÂºdo nÃƒÂ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta ÃƒÂºltima etapa: **`GTR-01`** (identidade do solicitante nÃƒÂ£o exposta ao guest) agora **simulada** no protÃƒÂ³tipo para User Validation, mas ainda nÃƒÂ£o resolvida no backend Ã¢â‚¬â€ `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃƒÂªncia) foi **resolvido no backend real** Ã¢â‚¬â€ `createItem` ganhou `idempotencyKey` opcional Ã¢â‚¬â€ e, na sessÃƒÂ£o do Full BFF/frontend, o `requestHash` de detecÃƒÂ§ÃƒÂ£o de payload divergente foi migrado de concatenaÃƒÂ§ÃƒÂ£o por delimitador (colisÃƒÂ£o real possÃƒÂ­vel) para JSON canÃƒÂ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` Ã¢â‚¬â€ a lacuna "nenhum frontend/BFF real envia o header" estÃƒÂ¡ fechada para o caminho de criaÃƒÂ§ÃƒÂ£o de item.

**PrÃƒÂ³xima etapa natural do design** (ainda nÃƒÂ£o iniciada): **User Validation** Ã¢â‚¬â€ usando o protÃƒÂ³tipo jÃƒÂ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode ÃƒÂ© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Ã‚Â§15-18/Ã‚Â§23-24 (tese de produto, mÃƒÂ©tricas de validaÃƒÂ§ÃƒÂ£o, tarefas candidatas derivadas de outcomes, limitaÃƒÂ§ÃƒÂµes conhecidas do protÃƒÂ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃƒÂ£o produzido ainda. NÃƒÂ£o obriga inÃƒÂ­cio imediato de Visual Design/Design System.

## BLOCKER-B Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃƒÂ£o de 2026-08-24/25 (continuaÃƒÂ§ÃƒÂ£o apÃƒÂ³s a sessÃƒÂ£o anterior ter sido abortada por limite de tokens sÃƒÂ³ com reconhecimento parcial Ã¢â‚¬â€ handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃƒÂ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo ClaudeÃ¢â€ â€Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` Ã¢â‚¬â€ esse documento ÃƒÂ© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃƒÂ§ÃƒÂ£o real (nÃƒÂ£o sÃƒÂ³ design) tambÃƒÂ©m revisada via protocolo ClaudeÃ¢â€ â€Codex (2 rodadas, nota final 9,2/10, `APPROVED` Ã¢â‚¬â€ achados reais corrigidos: reclassificaÃƒÂ§ÃƒÂ£o de exceÃƒÂ§ÃƒÂ£o em dois mÃƒÂ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃƒÂ§ÃƒÂ£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃƒÂ§ÃƒÂ£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃƒÂªs novos mÃƒÂ©todos de reconciliaÃƒÂ§ÃƒÂ£o em `ReminderMaterializer` (fenced por versÃƒÂ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃƒÂ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃƒÂ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃƒÂ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃƒÂ§ÃƒÂ£o: o "caminho genÃƒÂ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃƒÂ³digo Ã¢â‚¬â€ `router_queue` nÃƒÂ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃƒÂ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃƒÂ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃƒÂ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Ã‚Â§8). **Implementado nesta sessÃƒÂ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃƒÂ§ÃƒÂ£o da criaÃƒÂ§ÃƒÂ£o do item (sem `ConditionCheck` de existÃƒÂªncia Ã¢â‚¬â€ o item novo ÃƒÂ© garantido pela prÃƒÂ³pria transaÃƒÂ§ÃƒÂ£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃƒÂ³ `ExpirationItem` (mudanÃƒÂ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" Ã¢â‚¬â€ reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃƒÂ£o estava instalado no inÃƒÂ­cio desta sessÃƒÂ£o (`npm ci` rodado para poder validar) Ã¢â‚¬â€ ambiente, nÃƒÂ£o cÃƒÂ³digo.

**PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o**: **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ todos os 9 itens numerados originais da lista estÃƒÂ£o CONCLUÃƒÂDOS (2026-08-26)**, cÃƒÂ³digo+infra reais para itens 1-9, nÃƒÂ£o sÃƒÂ³ camada pura Ã¢â‚¬â€ ver as seÃƒÂ§ÃƒÂµes dedicadas "Item N Ã¢â‚¬â€ CONCLUÃƒÂDO" na seÃƒÂ§ÃƒÂ£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Ã‚Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃƒÂ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) Ã¢â‚¬â€ causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃƒÂ­ficos com `States.ALL` num ÃƒÂºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃƒÂ£o pega esse erro, sÃƒÂ³ a chamada real de criaÃƒÂ§ÃƒÂ£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** Ã¢â‚¬â€ o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃƒÂ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃƒÂ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃƒÂ§ÃƒÂ£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃƒÂ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** Ã¢â‚¬â€ a nota de uma sessÃƒÂ£o anterior dizendo "nenhum profile corresponde ÃƒÂ  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃƒÂµes de escrita necessÃƒÂ¡rias e a **verificaÃƒÂ§ÃƒÂ£o E2E real foi executada** (ver `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃƒÂ§a de Terraform foi cogitada para isso Ã¢â‚¬â€ a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃƒÂ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃƒÂ³ o gatilho automÃƒÂ¡tico via EventBridge ÃƒÂ© que ÃƒÂ© gated). **RecorrÃƒÂªncia fechada na mesma sessÃƒÂ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃƒÂ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` Ã¢â‚¬â€ a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃƒÂ³ descoberto no meio de um `terraform apply` real. NÃƒÂ£o requer credencial de escrita (ÃƒÂ© uma chamada de validaÃƒÂ§ÃƒÂ£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃƒÂ£o retomar sem sinal explÃƒÂ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA Ã¢â‚¬â€ etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃƒÂ¡ aprovada em nÃƒÂ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Ã‚Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃƒÂ¡vel por hash; `implementation-blueprint.md` Ã‚Â§12/Ã‚Â§23: CSP com hashes estÃƒÂ¡ticos calculados no build, sem nonce dinÃƒÂ¢mico Ã¢â‚¬â€ Day 0 nÃƒÂ£o tem compute de borda). O que falta ÃƒÂ© o desenho de implementaÃƒÂ§ÃƒÂ£o real e o cÃƒÂ³digo Ã¢â‚¬â€ nenhuma decisÃƒÂ£o de produto pendente, mas hÃƒÂ¡ **uma decisÃƒÂ£o de arquitetura real ainda em aberto** que a prÃƒÂ³xima sessÃƒÂ£o precisa resolver antes de implementar (Type 1, nÃƒÂ­vel 5-6 da escala de risco Ã¢â‚¬â€ protocolo `AGENTS.md` Ã‚Â§4 aplicÃƒÂ¡vel):

1. **DecisÃƒÂ£o de arquitetura a fechar primeiro Ã¢â‚¬â€ FECHADA nesta sessÃƒÂ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo ClaudeÃ¢â€ â€Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** Ã¢â‚¬â€ `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` ÃƒÂ© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃƒÂªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma ÃƒÂºnica distribution, behaviors `/bff` e `/bff/*` Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior Ã¢â€ â€™ S3 com OAC + CloudFront Function de SPA routing (sÃƒÂ³ nesse behavior, nunca no do BFF). Zero mudanÃƒÂ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃƒÂ©-existentes descobertos pela revisÃƒÂ£o, a aplicar na implementaÃƒÂ§ÃƒÂ£o: `proxy-service.ts` nÃƒÂ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃƒÂ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃƒÂ£o implementado): acesso direto ao `execute-api` pÃƒÂºblico precisa de mitigaÃƒÂ§ÃƒÂ£o (header estÃƒÂ¡tico + WAF) antes de produÃƒÂ§ÃƒÂ£o pÃƒÂºblica real.
2. **MÃƒÂ³dulo Terraform `infra/modules/spa-hosting` Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃƒÂºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃƒÂ­citos Ã¢â€ â€™ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default Ã¢â€ â€™ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃƒÂ©todo GET/HEAD, associada SÃƒâ€œ ao default behavior Ã¢â‚¬â€ nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃƒÂ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃƒÂ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃƒÂ©-existentes do ADR-0011 tambÃƒÂ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃƒÂ§ÃƒÂ£o errada do prÃƒÂ³prio ADR-0011): `var.spa_content_security_policy` nÃƒÂ£o precisa de hashes calculados por build Ã¢â‚¬â€ verificado contra o `frontend/dist/index.html` real, o build do Vite nÃƒÂ£o emite nenhum `<script>`/`<style>` inline, todo asset ÃƒÂ© carregado por URL externa jÃƒÂ¡ hasheada, entÃƒÂ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃƒÂ¡ existe em `frontend/index.html`) bastam sem hash nenhum Ã¢â‚¬â€ a variÃƒÂ¡vel ganhou um default determinÃƒÂ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃƒÂ£o consegue expressar). **MÃƒÂ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) Ã¢â‚¬â€ corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃƒÂ£o bloqueia o primeiro apply, sÃƒÂ³ o domÃƒÂ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro Ã¢â‚¬â€ confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃƒÂ£o sÃƒÂ³ contra mocks.
3. **Deploy imutÃƒÂ¡vel por hash Ã¢â‚¬â€ IMPLEMENTADO (2026-08-25)**: Vite jÃƒÂ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra ÃƒÂ© necessÃƒÂ¡rio (o hash no nome do arquivo jÃƒÂ¡ ÃƒÂ© o mecanismo de imutabilidade). `index.html` ÃƒÂ© tratado ÃƒÂ  parte (nÃƒÂ£o hasheado, `Cache-Control: no-cache`, upload por ÃƒÂºltimo).
4. **IntegraÃƒÂ§ÃƒÂ£o com `cd.yml` Ã¢â‚¬â€ IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃƒÂ£o um job novo) Ã¢â‚¬â€ `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** Ã¢â‚¬â€ assets antigos nunca sÃƒÂ£o removidos, ÃƒÂ© isso que mantÃƒÂ©m um rollback de `index.html` funcional, jÃƒÂ¡ que as referÃƒÂªncias antigas continuam servÃƒÂ­veis), upload de `index.html` por ÃƒÂºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃƒÂ¡ existente. **PendÃƒÂªncia real registrada, nÃƒÂ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃƒÂ³ cobre Lambdas Ã¢â‚¬â€ nÃƒÂ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃƒÂ£o `rollback.yml` (mecanismo existente) nÃƒÂ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃƒÂ£o bloqueia o deploy normal.
5. **VerificaÃƒÂ§ÃƒÂ£o real (Camada 3) Ã¢â‚¬â€ CONCLUÃƒÂDA (2026-08-25), achou e corrigiu um bug real de produÃƒÂ§ÃƒÂ£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃƒÂ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** Ã¢â‚¬â€ isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃƒÂ­tica fixa nÃƒÂ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃƒÂ§ÃƒÂ£o, tudo confirmado contra a distribution real:
   - `GET /` Ã¢â€ â€™ 200, `text/html`.
   - `GET /assets/index-*.js` Ã¢â€ â€™ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃƒÂ£o, V6 do ADR-0011) Ã¢â€ â€™ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) Ã¢â€ â€™ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) Ã¢â‚¬â€ nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) Ã¢â€ â€™ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃƒÂ£o (V4) Ã¢â€ â€™ 401 `application/json` (`AUTH_REQUIRED`) Ã¢â‚¬â€ nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃƒÂ£o (V5-404) Ã¢â€ â€™ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado Ã¢â‚¬â€ o caso 404-por-allowlist com sessÃƒÂ£o vÃƒÂ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃƒÂ£o foram exercitados por nÃƒÂ£o serem automatizÃƒÂ¡veis via `curl` sozinho; a propriedade crÃƒÂ­tica de seguranÃƒÂ§a Ã¢â‚¬â€ nenhum erro do BFF vira HTML da SPA Ã¢â‚¬â€ jÃƒÂ¡ estÃƒÂ¡ confirmada pelos casos acima).
   - Headers de seguranÃƒÂ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃƒÂ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy Ã¢â‚¬â€ exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃƒÂ£o sÃƒÂ³ via `terraform plan`.
   **PendÃƒÂªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃƒÂ£o lidos via CloudFront/OAC (sÃƒÂ³ por IAM de role de Lambda), entÃƒÂ£o nÃƒÂ£o tÃƒÂªm este bug Ã¢â‚¬â€ mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃƒÂ£o corrigido preventivamente nesta sessÃƒÂ£o (fora do escopo desta decisÃƒÂ£o, nenhum uso real hoje), sÃƒÂ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃƒÂ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃƒÂ§ÃƒÂ£o/OCR) Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o iniciada (2026-08-25, D-057)

Design jÃƒÂ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) Ã¢â‚¬â€ esta seÃƒÂ§ÃƒÂ£o ÃƒÂ© sobre a IMPLEMENTAÃƒâ€¡ÃƒÆ’O, o design nÃƒÂ£o muda. **Escopo ÃƒÂ© grande** (Step Functions, Textract assÃƒÂ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ vai continuar por mÃƒÂºltiplas sessÃƒÂµes, nÃƒÂ£o ÃƒÂ© razoÃƒÂ¡vel fechar num ÃƒÂºnico slice. Estado exato ao fim desta sessÃƒÂ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Ã‚Â§4 ganhou a classe `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ prÃƒÂ©-requisito de design que o prÃƒÂ³prio D-035 exige estar registrado antes de qualquer implementaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o ÃƒÂ© pendÃƒÂªncia pÃƒÂ³s-produÃƒÂ§ÃƒÂ£o).
2. MÃƒÂ³dulo novo `src/modules/extraction/domain/` (sÃƒÂ³ domÃƒÂ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` Ã¢â‚¬â€ `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃƒÂ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃƒÂ³pria chave de idempotÃƒÂªncia da execuÃƒÂ§ÃƒÂ£o).
   - `extracted-field.ts` Ã¢â‚¬â€ `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Ã‚Â§12.5.
   - `field-schema.ts` Ã¢â‚¬â€ schema v1 com sÃƒÂ³ `expirationDate` (ÃƒÂºnico campo que o design aprovado nomeia concretamente Ã¢â‚¬â€ deliberadamente nÃƒÂ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` Ã¢â‚¬â€ `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` Ã¢â‚¬â€ helper de safety net de 24h para o artefato transitÃƒÂ³rio (a exclusÃƒÂ£o real ÃƒÂ© explÃƒÂ­cita por `ExtractionValidationTaskHandler`, este helper ÃƒÂ© sÃƒÂ³ a rede de seguranÃƒÂ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃƒÂ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` Ã¢â‚¬â€ **ainda sem PR/merge para `main`**, dado que nÃƒÂ£o hÃƒÂ¡ nenhuma infra/runtime ativÃƒÂ¡vel ainda, nada a fazer deploy).

**Item 1 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃƒÂ§ÃƒÂ£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃƒÂ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃƒÂ³dulo corrigido pelo design Ã¢â‚¬â€ transversal, nÃƒÂ£o `extraction-appconfig`, jÃƒÂ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃƒÂªs defaults `false` (fail-closed, prÃƒÂ©-condiÃƒÂ§ÃƒÂµes externas de M7 nÃƒÂ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Ã‚Â§1.6, default `false`) Ã¢â‚¬â€ gate Terraform separado do pipeline inteiro, ainda nÃƒÂ£o referenciado por nenhum recurso (sÃƒÂ³ os workers futuros vÃƒÂ£o condicionar-se a ele). 2 `terraform test` do mÃƒÂ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) Ã¢â‚¬â€ CAMADA PURA CONCLUÃƒÂDA nesta sessÃƒÂ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃƒÂ£o que o item 0 seguiu: domÃƒÂ­nio+aplicaÃƒÂ§ÃƒÂ£o primeiro, depois handler Lambda/infra numa fatia posterior Ã¢â‚¬â€ deliberado, nÃƒÂ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` Ã¢â‚¬â€ cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` Ã¢â‚¬â€ entidade `TextractJob` (chave prÃƒÂ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃƒÂ£o tenant-scoped Ã¢â‚¬â€ `COMPLETE_OCR` sÃƒÂ³ tem `jobId` disponÃƒÂ­vel da notificaÃƒÂ§ÃƒÂ£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃƒÂ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃƒÂ§ÃƒÂ£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` Ã¢â‚¬â€ classificador heurÃƒÂ­stico puro (magic bytes > extensÃƒÂ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃƒÂ§a) quando nenhum sinal ÃƒÂ© conclusivo Ã¢â‚¬â€ quem decide que isso ÃƒÂ© `UnsupportedDocumentTypeError` ÃƒÂ© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed ÃƒÂ© responsabilidade do caller, nÃƒÂ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃƒÂ©todo de delete Ã¢â‚¬â€ a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃƒÂ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃƒÂ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) Ã¢â‚¬â€ orquestra classificaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ kill switch `OCR` (fail-closed) Ã¢â€ â€™ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` Ã¢â‚¬â€ um retry do mesmo run encontra `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior e trata como "jÃƒÂ¡ reservado", nÃƒÂ£o como falha real) Ã¢â€ â€™ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃƒÂ­stico) Ã¢â€ â€™ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` Ã¢â‚¬â€ retorna void, a execuÃƒÂ§ÃƒÂ£o do Step Functions fica parada no `taskToken` atÃƒÂ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃƒÂ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) Ã¢â‚¬â€ busca `TextractJob` por `jobId`; caso ÃƒÂ³rfÃƒÂ£o (nenhum job encontrado) faz UMA chamada de confirmaÃƒÂ§ÃƒÂ£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃƒÂ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃƒÂµe um mÃƒÂ©todo de delete); `taskTokenCiphertext` ÃƒÂ© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃƒÂ§ado, sem tocar no registro) em qualquer outro erro Ã¢â‚¬â€ a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto Ã¢â‚¬â€ `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃƒÂ£o ÃƒÂ© revalidado como JSON aninhado, mesma convenÃƒÂ§ÃƒÂ£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` Ã¢â‚¬â€ `RunTextract` ÃƒÂ© o ÃƒÂºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃƒÂ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃƒÂ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃƒÂ£o existem.
- `infra/modules/extraction-workflow/` Ã¢â‚¬â€ mÃƒÂ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃƒÂ§ÃƒÂ£o + role de execuÃƒÂ§ÃƒÂ£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒÆ’O chamado em `infra/main.tf`** Ã¢â‚¬â€ um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃƒÂ£o sÃƒÂ³ em runtime), e os 3 handlers dos itens 5-7 nÃƒÂ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃƒÂ³prio diretÃƒÂ³rio do mÃƒÂ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃƒÂ³dulo especificamente Ã¢â‚¬â€ pendÃƒÂªncia explÃƒÂ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos ÃƒÂ  mÃƒÂ£o (sem `vi.mock`), cobrindo: classificaÃƒÂ§ÃƒÂ£o por magic-bytes/extensÃƒÂ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃƒÂ§ÃƒÂ£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃƒÂªncia da reserva de quota sob retry do mesmo run; retry local de persistÃƒÂªncia do `TextractJob` (sucesso apÃƒÂ³s 1 falha, esgotamento apÃƒÂ³s N); `completeOcr`: job ÃƒÂ³rfÃƒÂ£o (com e sem falha na confirmaÃƒÂ§ÃƒÂ£o), jÃƒÂ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃƒÂ§ÃƒÂ£o multi-pÃƒÂ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃƒÂ³rio de `SendTask*` relanÃƒÂ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃƒÂ£o expÃƒÂµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na ÃƒÂ¡rvore inteira de `infra/`.

**Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃƒÂªs decisÃƒÂµes pendentes acima foram todas tomadas e implementadas (nÃƒÂ£o deixadas como pendÃƒÂªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃƒÂ£o final** (nÃƒÂ£o a extensÃƒÂ£o via layer) Ã¢â‚¬â€ `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/ÃƒÂºltimos flags conhecidos em memÃƒÂ³ria do processo. Motivo: a extensÃƒÂ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` Ã¢â‚¬â€ nÃƒÂ£o existe nenhum precedente disso no repo (sÃƒÂ³ `adot_layer_arn`, uma layer diferente), e este handler sÃƒÂ³ lÃƒÂª os flags uma vez por invocaÃƒÂ§ÃƒÂ£o de `START_OCR` (nÃƒÂ£o por pÃƒÂ¡gina/bloco), entÃƒÂ£o o custo/latÃƒÂªncia de uma chamada de API direta por invocaÃƒÂ§ÃƒÂ£o ÃƒÂ© equivalente ao que a extensÃƒÂ£o teria numa invocaÃƒÂ§ÃƒÂ£o fria de qualquer forma. Revisitar sÃƒÂ³ se o volume tornar isso um custo real (a extensÃƒÂ£o amortiza entre invocaÃƒÂ§ÃƒÂµes warm via cache local, este adapter sÃƒÂ³ amortiza dentro de uma sessÃƒÂ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃƒÂ£o reuso da CMK do BFF Ã¢â‚¬â€ mesma disciplina D-054: um token de callback do Step Functions ÃƒÂ© uma credencial viva tÃƒÂ£o sensÃƒÂ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃƒÂ³dulos independentemente deployÃƒÂ¡veis acoplaria rotaÃƒÂ§ÃƒÂ£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃƒÂªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃƒÂ³dulo, mesma razÃƒÂ£o do port).
- **TÃƒÂ³pico SNS de conclusÃƒÂ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar Ã¢â‚¬â€ distinta da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio Lambda.

Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃƒÂ§ÃƒÂ£o real `TaskTimedOut`/`TaskDoesNotExist`Ã¢â€ â€™`TERMINAL_QUIET`, `InvalidToken`Ã¢â€ â€™`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃƒÂ§ado), `AppConfigFeatureFlagsReader` Ã¢â‚¬â€ todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` Ã¢â‚¬â€ um ÃƒÂºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃƒÂ§ÃƒÂ£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃƒÂ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃƒÂ£o SSE-KMS Ã¢â‚¬â€ evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃƒÂ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃƒÂ³pico SNS, permissÃƒÂ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃƒÂ¡ escopada ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃƒÂ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** Ã¢â‚¬â€ mesma lÃƒÂ³gica do item 2: todo recurso aqui ÃƒÂ© inerte sozinho, porque `StartDocumentTextDetection` (a ÃƒÂºnica chamada paga) sÃƒÂ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃƒÂ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃƒÂ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27Ã¢â€ â€™28 funÃƒÂ§ÃƒÂµes (2 asserts).
- 11 testes novos (794 totais no backend) Ã¢â‚¬â€ sÃƒÂ³ para os dois adapters com lÃƒÂ³gica real de branching (`SfnTaskTokenSender`'s classificaÃƒÂ§ÃƒÂ£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃƒÂ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃƒÂ£o ganharam teste unitÃƒÂ¡rio dedicado, seguindo o MESMO precedente jÃƒÂ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃƒÂ©m nÃƒÂ£o tÃƒÂªm) Ã¢â‚¬â€ verificados via `terraform plan` real contra `dev`, nÃƒÂ£o via mock unitÃƒÂ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target Ã¢â‚¬â€ 0 erros, 0 destroys inesperados (sÃƒÂ³ o prÃƒÂ³prio slice + o ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ prÃƒÂ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Ã‚Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) Ã¢â‚¬â€ **ainda sem PR/merge para `main`** no momento em que este parÃƒÂ¡grafo foi escrito (abrir o PR ÃƒÂ© o prÃƒÂ³ximo passo imediato desta mesma sessÃƒÂ£o, ver "PrÃƒÂ³xima aÃƒÂ§ÃƒÂ£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 Ã¢â‚¬â€ a ASL's `RunDeterministicParser` ÃƒÂ© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃƒÂ£o hÃƒÂ¡ task token nenhum neste handler), entÃƒÂ£o ÃƒÂ© uma ÃƒÂºnica invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃƒÂ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` Ã¢â‚¬â€ `extractExpirationDateCandidates()`, regex determinÃƒÂ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃƒÂ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃƒÂ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃƒÂ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃƒÂ¡ a atÃƒÂ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃƒÂ§a fraca (0.5, abaixo do threshold) quando nÃƒÂ£o hÃƒÂ¡ palavra-chave prÃƒÂ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃƒÂ©rico que trunca o texto processado Ã¢â‚¬â€ a ÃƒÂºnica reutilizaÃƒÂ§ÃƒÂ£o real de M6 possÃƒÂ­vel aqui, jÃƒÂ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃƒÂ£o sobre texto OCR, e nunca extrai campos/datas (sÃƒÂ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃƒÂ³digo `DeterministicParserFailed`) Ã¢â‚¬â€ nÃƒÂ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃƒÂ¡ ÃƒÂ© o genÃƒÂ©rico `States.ALL`.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-deterministic-parser.ts` Ã¢â‚¬â€ `runDeterministicParser()`: lÃƒÂª o artefato OCR (quando presente) Ã¢â€ â€™ extrai candidatos de `expirationDate` via `getFieldSchema()` Ã¢â€ â€™ chama `needsBedrock()` (item 0) Ã¢â€ â€™ lÃƒÂª o kill switch `AI_EXTRACTION` (fail-closed) Ã¢â€ â€™ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** Ã¢â‚¬â€ retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃƒÂ¡ implica `true`) e a decisÃƒÂ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃƒÂ£o duplicada) `OcrArtifactStore` ganhou `get()` Ã¢â‚¬â€ o item 4 sÃƒÂ³ tinha `put()` (quem escreve o artefato); o item 5 ÃƒÂ© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada Ã¢â‚¬â€ sÃƒÂ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, nÃƒÂ£o uma decisÃƒÂ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃƒÂ£o tinha `ResultPath` na transiÃƒÂ§ÃƒÂ£o de sucesso do ASL, entÃƒÂ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃƒÂ­a inteiramente** o `$` do Step Functions Ã¢â‚¬â€ todo o contexto original da execuÃƒÂ§ÃƒÂ£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃƒÂ£o sÃƒÂ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃƒÂ³prio `job`) ao payload de sucesso. MudanÃƒÂ§a aditiva/reversÃƒÂ­vel sem migraÃƒÂ§ÃƒÂ£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃƒÂ³rico real).
- **DECISÃƒÆ’O PENDENTE (registrada aqui, default conservador aplicado)**: o design (Ã‚Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃƒÂ£o implementado Ã¢â‚¬â€ nenhum campo de metadado de PDF (data de criaÃƒÂ§ÃƒÂ£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃƒÂ£o de produto nÃƒÂ£o revisada, nÃƒÂ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃƒÂ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃƒÂ§ÃƒÂ£o na prÃƒÂ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃƒÂ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` Ã¢â‚¬â€ nÃƒÂ£o muda nenhum `Next`/fluxo, sÃƒÂ³ corrige o path de leitura para bater com a semÃƒÂ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃƒÂ§ÃƒÂ£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` Ã¢â‚¬â€ sÃƒÂ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃƒÂ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` Ã¢â‚¬â€ sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura (mesmo padrÃƒÂ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28Ã¢â€ â€™29 funÃƒÂ§ÃƒÂµes (3 asserts: nomes, distinÃƒÂ§ÃƒÂ£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃƒÂ§ÃƒÂµes dia/mÃƒÂªs impossÃƒÂ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar Ã¢â‚¬â€ sÃƒÂ³ republish de versÃƒÂ£o prÃƒÂ©-existente em todas as Lambdas, mesmo ruÃƒÂ­do que o item 4 jÃƒÂ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` Ã¢â‚¬â€ drift **prÃƒÂ©-existente do item 4**, nÃƒÂ£o introduzido nesta sessÃƒÂ£o, causado por uma normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃƒÂ¡ detectava antes desta sessÃƒÂ£o comeÃƒÂ§ar; nÃƒÂ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 Ã¢â‚¬â€ ambos concluÃƒÂ­dos em sessÃƒÂµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ Ã¢â‚¬â€ **CONCLUÃƒÂDO 2026-08-26**, ver seÃƒÂ§ÃƒÂ£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃƒÂ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` Ã¢â‚¬â€ sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o, na prÃƒÂ³pria ASL** (nÃƒÂ£o uma decisÃƒÂ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃƒÂ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃƒÂ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` Ã¢â‚¬â€ os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃƒÂ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃƒÂ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃƒÂ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` Ã¢â‚¬â€ `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃƒÂ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta Ã¢â‚¬â€ sem isso, `BedrockExtractionTaskHandler` nÃƒÂ£o teria como saber onde ler o texto OCR (o design Ã‚Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃƒÂ§ÃƒÂ£o de contexto que `completeOcr` (item 4/5) jÃƒÂ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃƒÂ³digo + infra, nÃƒÂ£o sÃƒÂ³ camada pura)**:
- DomÃƒÂ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` Ã¢â‚¬â€ `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃƒÂ¡vel, nunca concatenada com input do usuÃƒÂ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` Ã¢â‚¬â€ nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃƒÂ³ `textArtifact` Ã¢â‚¬â€ tipo local `BedrockTextArtifactRef`, estruturalmente idÃƒÂªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃƒÂ­be domÃƒÂ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) Ã¢â‚¬â€ nenhum import do SDK Bedrock fora do adapter.
- AplicaÃƒÂ§ÃƒÂ£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) Ã¢â‚¬â€ re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃƒÂ¡ filtra isso mas a chamada paga nunca confia sÃƒÂ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃƒÂ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃƒÂ£o hÃƒÂ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz Ã¢â‚¬â€ nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) Ã¢â‚¬â€ Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃƒÂ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃƒÂªncia de tool-call, tool errado) Ã¢â‚¬â€ qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃƒÂ§ÃƒÂ£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` Ã¢â‚¬â€ DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃƒÂ£o configurÃƒÂ¡vel via `bedrock_region`, distinta da regiÃƒÂ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` Ã¢â‚¬â€ invocaÃƒÂ§ÃƒÂ£o sÃƒÂ­ncrona ÃƒÂºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃƒÂ£o Bedrock: configurÃƒÂ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) Ã¢â‚¬â€ design Ã‚Â§4 bloqueia sÃƒÂ³ a escolha/validaÃƒÂ§ÃƒÂ£o real para produÃƒÂ§ÃƒÂ£o, nunca a testabilidade em `dev`; a permissÃƒÂ£o IAM jÃƒÂ¡ ÃƒÂ© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota Ã¢â‚¬â€ mesmo padrÃƒÂ£o do item 4 Ã¢â‚¬â€, `feature_flags_read_policy_json`, `s3:GetObject` sÃƒÂ³ leitura no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ nunca escrita, mesma disciplina de blast-radius do item 5 Ã¢â‚¬â€, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃƒÂ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃƒÂ¡ escopado ÃƒÂ  ARN determinÃƒÂ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29Ã¢â€ â€™30 funÃƒÂ§ÃƒÂµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 Ã¢â‚¬â€ caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃƒÂ§ÃƒÂ£o de quota em falha, **14Ã‚Âº caso adversarial de custo-abuso**: execuÃƒÂ§ÃƒÂ£o retentada/duplicada do mesmo `runId` reserva contra a prÃƒÂ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 Ã¢â‚¬â€ corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃƒÂ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃƒÂ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃƒÂ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃƒÂ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Ã‚Â§1.11) nÃƒÂ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) Ã¢â‚¬â€ o corpus construÃƒÂ­do cobre as mesmas classes de ameaÃƒÂ§a descritas ali (injeÃƒÂ§ÃƒÂ£o via conteÃƒÂºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Ã‚Âº caso de custo-abuso) a partir de princÃƒÂ­pios, documentado no comentÃƒÂ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar Ã¢â‚¬â€ os do item 5 ainda nÃƒÂ£o aplicado + os do item 6 Ã¢â‚¬â€, 56 a mudar Ã¢â‚¬â€ mesmo ruÃƒÂ­do de republish de versÃƒÂ£o jÃƒÂ¡ documentado nos itens 4/5 Ã¢â‚¬â€, 1 destroy-and-recreate Ã¢â‚¬â€ o MESMO drift prÃƒÂ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃƒÂ£o introduzido nesta sessÃƒÂ£o). **Nunca `terraform apply` local**.
- **DecisÃƒÂ£o pendente registrada, nÃƒÂ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃƒÂ©m de `expirationDate`, entÃƒÂ£o o schema fechado do tool `submit_extraction` sÃƒÂ³ tem essa uma propriedade Ã¢â‚¬â€ `buildSubmitExtractionToolSchema()` jÃƒÂ¡ ÃƒÂ© genÃƒÂ©rico sobre `FIELD_SCHEMA_V1`, entÃƒÂ£o um campo novo no schema nÃƒÂ£o precisa de mudanÃƒÂ§a de cÃƒÂ³digo aqui, sÃƒÂ³ uma nova versÃƒÂ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) Ã¢â‚¬â€ todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃƒÂ¡gio, design Ã‚Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃƒÂ£o mais importante desta sessÃƒÂ£o Ã¢â‚¬â€ a limpeza do artefato transitÃƒÂ³rio (design Ã‚Â§3, "achado mais sÃƒÂ©rio")**: `OcrArtifactStore.delete()` ÃƒÂ© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃƒÂ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃƒÂ³fica do parser determinÃƒÂ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` Ã¢â‚¬â€ design cita esse estado explicitamente como ponto de deleÃƒÂ§ÃƒÂ£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` Ã¢â‚¬â€ rodam antes do run alcanÃƒÂ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃƒÂªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado Ã¢â‚¬â€ zero atÃƒÂ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃƒÂ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` Ã¢â‚¬â€ `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃƒÂ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃƒÂ£o-vazio. Um candidato invÃƒÂ¡lido nunca ÃƒÂ© descartado silenciosamente Ã¢â‚¬â€ ÃƒÂ© marcado `valid: false` e tratado como "essa fonte nÃƒÂ£o produziu nada" na comparaÃƒÂ§ÃƒÂ£o.
- `compare-extractors.ts` Ã¢â‚¬â€ `compareExtractors(candidates)`: 0 candidatos usÃƒÂ¡veis Ã¢â€ â€™ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` Ã¢â‚¬â€ jÃƒÂ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` Ã¢â‚¬â€ nÃƒÂ£o representa separadamente; decisÃƒÂ£o registrada aqui em vez de alargar o tipo jÃƒÂ¡ commitado). 1 candidato Ã¢â€ â€™ `SINGLE_SOURCE` com valor. 2+ concordando Ã¢â€ â€™ `MATCH` (confianÃƒÂ§a = a maior das duas). 2+ discordando Ã¢â€ â€™ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" Ã¢â‚¬â€ `candidateValue` carrega sÃƒÂ³ o de maior confianÃƒÂ§a, para exibiÃƒÂ§ÃƒÂ£o, mas ÃƒÂ© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` Ã¢â‚¬â€ `decideFieldOutcome(comparison)`: sem `candidateValue` Ã¢â€ â€™ `PENDING_CONFIRMATION`; `MISMATCH` Ã¢â€ â€™ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃƒÂ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) Ã¢â€ â€™ `PENDING_CONFIRMATION`; senÃƒÂ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) Ã¢â€ â€™ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** Ã¢â‚¬â€ esse estado sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ÃƒÂ¡vel pela rota HTTP de rejeiÃƒÂ§ÃƒÂ£o humana (item 8, ainda nÃƒÂ£o implementada).

**DecisÃƒÂ£o de arquitetura tomada nesta sessÃƒÂ£o (nÃƒÂ£o estava no design, resolvida por inferÃƒÂªncia direta do enum jÃƒÂ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃƒÂ£o tem um estado "aguardando confirmaÃƒÂ§ÃƒÂ£o" separado Ã¢â‚¬â€ entÃƒÂ£o a distinÃƒÂ§ÃƒÂ£o "run completa vs. precisa de revisÃƒÂ£o humana" vive inteiramente no nÃƒÂ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃƒÂ¡lido Ã¢â€ â€™ comparaÃƒÂ§ÃƒÂ£o Ã¢â€ â€™ persistÃƒÂªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃƒÂ­da, hoje sÃƒÂ³ para log/observabilidade Ã¢â‚¬â€ nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` ÃƒÂ© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho Ã¢â‚¬â€ o prÃƒÂ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa ÃƒÂºnica operaÃƒÂ§ÃƒÂ£o, porque ÃƒÂ© o ÃƒÂºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, ÃƒÂ© uma decisÃƒÂ£o de produto pequena e reversÃƒÂ­vel (mudar sÃƒÂ³ a decisÃƒÂ£o de estado, nÃƒÂ£o a mecÃƒÂ¢nica de comparaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia) Ã¢â‚¬â€ registrado aqui para reabrir se necessÃƒÂ¡rio.

**Descarte por exclusÃƒÂ£o concorrente (design Ã‚Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃƒÂ£o do `Document` lida nÃƒÂ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃƒÂ£o sÃƒÂ³ um "ler status DELETED e torcer"). Se o `Document` jÃƒÂ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃƒÂ§ÃƒÂ£o falhar dentro da transaÃƒÂ§ÃƒÂ£o (mudanÃƒÂ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃƒÂ§ÃƒÂ£o como descarte Ã¢â‚¬â€ decisÃƒÂ£o conservadora documentada no comentÃƒÂ¡rio do adapter, jÃƒÂ¡ que distinguir a causa exata do cancelamento por cÃƒÂ³digo de erro do SDK seria frÃƒÂ¡gil entre versÃƒÂµes).

**Ports/persistÃƒÂªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) Ã¢â‚¬â€ expÃƒÂµe SÃƒâ€œ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num ÃƒÂºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` ÃƒÂ© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` Ã¢â‚¬â€ mÃƒÂ©todo standalone, usado SÃƒâ€œ no caminho de descarte (onde nÃƒÂ£o hÃƒÂ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` Ã¢â‚¬â€ **mudanÃƒÂ§a de contrato deliberada**: o item 4 tinha deixado esse mÃƒÂ©todo deliberadamente FORA do port ("a exclusÃƒÂ£o do artefato ÃƒÂ© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" Ã¢â‚¬â€ tornava a exclusÃƒÂ£o acidental do chamador errado estruturalmente impossÃƒÂ­vel). Agora adicionado porque este Ãƒâ€° o chamador certo Ã¢â‚¬â€ `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃƒÂ³pria API do S3, deletar uma key ausente nÃƒÂ£o lanÃƒÂ§a). Os testes que antes afirmavam "o port nem expÃƒÂµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃƒÂ¡ que o mÃƒÂ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃƒÂ¡-lo).

**AplicaÃƒÂ§ÃƒÂ£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃƒÂ§ÃƒÂµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um ÃƒÂºnico `ValidationContext` que evolui atravÃƒÂ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃƒÂ£o exportado) que encapsula a leitura do `Document`+decisÃƒÂ£o discard-ou-commit. Um erro genuÃƒÂ­no de commit (nÃƒÂ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` Ã¢â‚¬â€ o `Catch`/`Retry` da ASL trata como transitÃƒÂ³rio).

**Achado real corrigido nesta sessÃƒÂ£o, em cÃƒÂ³digo do item 6 jÃƒÂ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` Ã¢â‚¬â€ a MESMA classe de bug que o item 5 jÃƒÂ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Ã‚Â§3's "achado mais sÃƒÂ©rio" ÃƒÂ© sobre limpeza de artefato, mas essa categoria de "output nÃƒÂ£o reanexa o contexto anterior" jÃƒÂ¡ apareceu 3 vezes agora: item 4Ã¢â€ â€™5, item 5Ã¢â€ â€™6, e este item 6Ã¢â€ â€™7). Corrigido de forma aditiva (campos novos sÃƒÂ³ ecoam o que jÃƒÂ¡ vinha em `input`, sem recomputar nada) Ã¢â‚¬â€ os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃƒÂ§ÃƒÂ£o (nÃƒÂ£o fazem `toEqual` no objeto inteiro). **PadrÃƒÂ£o a vigiar em qualquer implementaÃƒÂ§ÃƒÂ£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃƒÂ³ximo estado vai precisar, nunca assumir que "sÃƒÂ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃƒÂ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃƒÂ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃƒÂ©m ganhou `ResultSelector`/`OutputPath` (nÃƒÂ£o tinha nenhum antes Ã¢â‚¬â€ outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` Ã¢â‚¬â€ o footprint mais estreito dos quatro Lambdas de extraÃƒÂ§ÃƒÂ£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` Ã¢â‚¬â€ este ÃƒÂ© o ÃƒÂºnico Lambda do pipeline inteiro com permissÃƒÂ£o de deletar lÃƒÂ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma ÃƒÂºnica `aws_lambda_permission` cobre as cinco invocaÃƒÂ§ÃƒÂµes (todas vÃƒÂªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30Ã¢â€ â€™31 funÃƒÂ§ÃƒÂµes (3 asserts).

**VerificaÃƒÂ§ÃƒÂ£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃƒÂ­cio da sessÃƒÂ£o Ã¢â‚¬â€ nÃƒÂ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃƒÂ£o, nÃƒÂºmero incorreto de digitaÃƒÂ§ÃƒÂ£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃƒÂ§ÃƒÂ£o do artefato descrito acima), mais 16 testes prÃƒÂ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃƒÂµes dos itens 5/6, nÃƒÂ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃƒÂ£o de produto pendente registrada, nÃƒÂ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` Ã¢â‚¬â€ isso ÃƒÂ© o item 8 (rotas de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7), ainda nÃƒÂ£o implementado. AtÃƒÂ© lÃƒÂ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃƒÂ£o hÃƒÂ¡ nenhuma forma de um operador vÃƒÂª-lo ou agir sobre ele pela API.

**Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora ÃƒÂ© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃƒÂ¡ existentes (items 4-7) e uma role de execuÃƒÂ§ÃƒÂ£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` Ã¢â‚¬â€ o mesmo nome que `local.extraction_state_machine_arn` jÃƒÂ¡ esperava desde o item 2, verificado por `terraform test` (nÃƒÂ£o confiado sÃƒÂ³ ÃƒÂ  leitura do cÃƒÂ³digo).

- **IAM da role de execuÃƒÂ§ÃƒÂ£o do prÃƒÂ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado ÃƒÂ s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃƒÂªncia documentada da prÃƒÂ³pria AWS para `logging_configuration` de state machine Ã¢â‚¬â€ opera no subsistema de log delivery a nÃƒÂ­vel de conta, nÃƒÂ£o num log group especÃƒÂ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃƒÂ§ÃƒÂµes que `AWSXRayDaemonWriteAccess`, que o mÃƒÂ³dulo `lambda-function` jÃƒÂ¡ anexa a toda funÃƒÂ§ÃƒÂ£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃƒÂ¡-lo, escopado ÃƒÂ  ARN determinÃƒÂ­stica do state machine) jÃƒÂ¡ estava pronto desde os itens 4-7 Ã¢â‚¬â€ nÃƒÂ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃƒÂ§ÃƒÂ£o 30 dias, mesmo padrÃƒÂ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃƒÂ­vel `ERROR` (nÃƒÂ£o `ALL`) com `include_execution_data = false` Ã¢â‚¬â€ decisÃƒÂ£o deliberada espelhando a disciplina de privacidade do prÃƒÂ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃƒÂ§ÃƒÂ£o pode carregar texto OCR/valores de campo extraÃƒÂ­dos; nunca devem cair num log group com retenÃƒÂ§ÃƒÂ£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` Ã¢â‚¬â€ todo Lambda do repo jÃƒÂ¡ roda com ADOT/X-Ray ativo (AGENTS.md Ã‚Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Ã‚Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ o state machine, sua role e as 4 permissÃƒÂµes de invocaÃƒÂ§ÃƒÂ£o sempre existem (inspecionÃƒÂ¡veis/deployÃƒÂ¡veis). RaciocÃƒÂ­nio registrado no comentÃƒÂ¡rio do prÃƒÂ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃƒÂ¡ gateia o ÃƒÅ¡NICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que ÃƒÂ© quem chama `StartExecution`) Ã¢â‚¬â€ com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃƒÂ© ser iniciado manualmente para uma execuÃƒÂ§ÃƒÂ£o de teste, mas nunca recebe trÃƒÂ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) Ã¢â‚¬â€ 13Ã¢â€ â€™15 passando. MÃƒÂ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃƒÂªncia explÃƒÂ­cita registrada pelo item 7) Ã¢â‚¬â€ 5 `run` via `mock_provider`+`apply` (substituiÃƒÂ§ÃƒÂ£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃƒÂ¡s; `type = STANDARD`; role passada atravÃƒÂ©s sem modificaÃƒÂ§ÃƒÂ£o; tracing/logging configurados) Ã¢â‚¬â€ 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) Ã¢â‚¬â€ **a verificaÃƒÂ§ÃƒÂ£o crÃƒÂ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` Ã¢â€ â€™ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" Ã¢â‚¬â€ a definiÃƒÂ§ÃƒÂ£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃƒÂ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** Ã¢â‚¬â€ o ÃƒÂºnico destroy ÃƒÂ© o MESMO drift prÃƒÂ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃƒÂ§ÃƒÂ£o de `function_name` com/sem qualificador `:live`), jÃƒÂ¡ documentado nas sessÃƒÂµes dos itens 5/6/7, nÃƒÂ£o introduzido aqui. Nenhuma issue de integraÃƒÂ§ÃƒÂ£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃƒÂ­ram corretamente Ã¢â‚¬â€ nÃƒÂ£o foi necessÃƒÂ¡rio nenhum fix no `replace()` do mÃƒÂ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃƒÂ£o Ã¢â‚¬â€ sessÃƒÂ£o nÃƒÂ£o tocou cÃƒÂ³digo de aplicaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ infra). `terraform fmt -check -recursive` limpo em toda a ÃƒÂ¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃƒÂ£o precisar de `command = apply` contra o provider real). Novo output no mÃƒÂ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃƒÂ£o (branch `develop`, PR #60 `developÃ¢â€ â€™main` ainda aberto Ã¢â‚¬â€ push deve atualizÃƒÂ¡-lo automaticamente): instanciaÃƒÂ§ÃƒÂ£o do mÃƒÂ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: `ExtractionStarterWorker` completo Ã¢â‚¬â€ domÃƒÂ­nio/aplicaÃƒÂ§ÃƒÂ£o/persistÃƒÂªncia/handler real, nÃƒÂ£o sÃƒÂ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` Ã¢â‚¬â€ portas prÃƒÂ³prias, nÃƒÂ£o reusa `DocumentStore` do mÃƒÂ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃƒÂ¡ existia Ã¢â‚¬â€ gated sÃƒÂ³ no registro criaria um run ÃƒÂ³rfÃƒÂ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real ÃƒÂ© a idempotÃƒÂªncia nativa do `StartExecution` por nome de execuÃƒÂ§ÃƒÂ£o determinÃƒÂ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃƒÂ£o S3Ã¢â€ â€™EventBridgeÃ¢â€ â€™SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃƒÂ§ÃƒÂ£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃƒÂ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` Ã¢â‚¬â€ insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃƒÂ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) Ã¢â‚¬â€ mudanÃƒÂ§a na M6 (jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o), mas segura: `cleanObject` ÃƒÂ© sempre um valor opaco armazenado na entidade, nunca reconstruÃƒÂ­do/reparsed em nenhum outro lugar do cÃƒÂ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃƒÂ¡veis/inspecionÃƒÂ¡veis), mas a regra EventBridge que liga o bucket limpo ÃƒÂ  fila ÃƒÂ© o ÃƒÅ¡NICO recurso condicionado a `var.extraction_pipeline_enabled` Ã¢â‚¬â€ com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃƒÂ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` ÃƒÂ© determinÃƒÂ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) Ã¢â‚¬â€ jÃƒÂ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃƒÂªncias) Ã¢â‚¬â€ comportamento condicional verificado, nÃƒÂ£o sÃƒÂ³ lido no cÃƒÂ³digo. **Ainda sem PR/merge** (mesma lÃƒÂ³gica do item 1: mergeÃƒÂ¡vel isoladamente, mas ainda aguardando decisÃƒÂ£o de "quando" Ã¢â‚¬â€ ver abaixo).
3. ASL Ã¢â‚¬â€ camada pura (o arquivo `.asl.json` e o mÃƒÂ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** (cÃƒÂ³digo+infra, nÃƒÂ£o sÃƒÂ³ camada pura) Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 4 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃƒÂ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 5 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, arquivos tocados, decisÃƒÂµes tomadas (reanexaÃƒÂ§ÃƒÂ£o de contexto ao payload de sucesso de `completeOcr`, correÃƒÂ§ÃƒÂ£o do path `$.Payload.*` nas Choice states) e a decisÃƒÂ£o pendente registrada (sem heurÃƒÂ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 6 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (modelo/regiÃƒÂ£o placeholder configurÃƒÂ¡veis, correÃƒÂ§ÃƒÂ£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃƒÂ§ÃƒÂ£o do `artifact` ao output do item 5) e o corpus adversarial construÃƒÂ­do.
7. `ExtractionValidationTaskHandler` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta** Ã¢â‚¬â€ ver a seÃƒÂ§ÃƒÂ£o dedicada acima ("Item 7 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o") para o estado exato, decisÃƒÂµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃƒÂ¡ commitado, invariante de deleÃƒÂ§ÃƒÂ£o do artefato) e o que falta (item 8 ainda nÃƒÂ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` Ã¢â‚¬â€ **CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃƒÂ§ÃƒÂ£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26)**: ver "Item 3 Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o" na seÃƒÂ§ÃƒÂ£o M7 acima (topo do arquivo) para o detalhe completo Ã¢â‚¬â€ `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃƒÂ§ÃƒÂ£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃƒÂ§ÃƒÂ£o "pendÃƒÂªncias explÃƒÂ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real Ã¢â‚¬â€ nÃƒÂ£o bloqueia implementaÃƒÂ§ÃƒÂ£o, sÃƒÂ³ produÃƒÂ§ÃƒÂ£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃƒÂ§ÃƒÂ£o/rejeiÃƒÂ§ÃƒÂ£o, Ã‚Â§1.7) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` Ã¢â‚¬â€ o ÃƒÂºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃƒÂ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) Ã¢â‚¬â€ idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃƒÂª o `ExtractedField` jÃƒÂ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` ÃƒÂ© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) Ã¢â‚¬â€ lÃƒÂª os quatro, valida as 4 versÃƒÂµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃƒÂ§ÃƒÂ£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` Ã¢â‚¬â€ primeiro 422 do projeto). `reject` ÃƒÂ© OCC de 3 vias (nunca lÃƒÂª/toca `ExpirationItem` Ã¢â‚¬â€ nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoÃ¢â€ â€™atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃƒÂ³ `expirationDate` -> `dueDate` no schema v1) Ã¢â‚¬â€ quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃƒÂ§ÃƒÂ£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃƒÂ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃƒÂ§ÃƒÂ£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃƒÂ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` Ã¢â‚¬â€ `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃƒÂ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃƒÂ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` Ã¢â‚¬â€ um plain read eventualmente consistente, nunca usado pelos OCC writes jÃƒÂ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃƒÂ¡ existente) reaproveitado por `documents`/`items` na composition root Ã¢â‚¬â€ `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃƒÂ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` Ã¢â‚¬â€ `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) Ã¢â‚¬â€ mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` Ã¢â€ â€™ `authorize()` internamente via a application function Ã¢â€ â€™ `TenantQuotaService` `API_REQUEST` 100/60s Ã¢â€ â€™ schema Ajv Ã¢â€ â€™ chamada). AutorizaÃƒÂ§ÃƒÂ£o: `extraction:confirm` (aÃƒÂ§ÃƒÂ£o **jÃƒÂ¡ existia** na matriz desde uma sessÃƒÂ£o anterior Ã¢â‚¬â€ cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃƒÂ³pias desse mapa nos outros mÃƒÂ³dulos nÃƒÂ£o precisam, deixadas como estÃƒÂ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃƒÂ¡ tem `tenant_facing_read_write_policy_json` completo na tabela Ã¢â‚¬â€ zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃƒÂ£o de adapter `DynamoLike` que `ExpirationService` jÃƒÂ¡ usa para sua prÃƒÂ³pria `IdempotencyStore`, ao invÃƒÂ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` Ã¢â‚¬â€ o `source_arn` do `aws_lambda_permission.documents` jÃƒÂ¡ ÃƒÂ© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃƒÂ­do de republish de `source_code_hash` jÃƒÂ¡ documentado em toda sessÃƒÂ£o anterior deste milestone Ã¢â‚¬â€ o prÃƒÂ³prio `documents-handler` bundle mudou porque o cÃƒÂ³digo mudou, os outros 15 sÃƒÂ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas Ã¢â‚¬â€ nÃƒÂ£o repetido o gap histÃƒÂ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` Ã¢â‚¬â€ `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃƒÂ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` Ã¢â‚¬â€ reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃƒÂ¡lido/invÃƒÂ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos ÃƒÂ  mÃƒÂ£o sobre uma tabela DynamoDB em memÃƒÂ³ria com OCC real Ã¢â‚¬â€ `InMemoryTable.write()` sÃƒÂ³ aplica se a versÃƒÂ£o bate) Ã¢â‚¬â€ feliz confirm (item atualizado + versÃƒÂµes incrementadas), feliz reject (item intocado), idempotÃƒÂªncia de replay dos dois (sem re-executar a transaÃƒÂ§ÃƒÂ£o), 409 individual para cada uma das 4/3 versÃƒÂµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃƒÂ¡lido, 404 para cada uma das 4 entidades ausente, negaÃƒÂ§ÃƒÂ£o de autorizaÃƒÂ§ÃƒÂ£o sem membership. 2 fakes prÃƒÂ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃƒÂ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃƒÂ§ÃƒÂ£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃƒÂ­cio da sessÃƒÂ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) Ã¢â‚¬â€ CONCLUÃƒÂDO nesta sessÃƒÂ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃƒÂ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃƒÂ aconteceu, sÃƒÂ³ a persistÃƒÂªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒÆ’O libera a quota** Ã¢â‚¬â€ investigado nesta sessÃƒÂ£o e confirmado correto, nÃƒÂ£o uma lacuna: como a reserva ÃƒÂ© por-execuÃƒÂ§ÃƒÂ£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃƒÂ³pria reserva anterior, que `startOcr` jÃƒÂ¡ trata genericamente como "jÃƒÂ¡ reservado, nÃƒÂ£o ÃƒÂ© exaustÃƒÂ£o real" (linhas 86-90) Ã¢â‚¬â€ **independente de a reserva ter sido liberada ou nÃƒÂ£o**. Liberar aqui nÃƒÂ£o destravaria nada que jÃƒÂ¡ nÃƒÂ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃƒÂ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃƒÂ£o exato Ã¢â‚¬â€ falha da chamada Bedrock em si (apÃƒÂ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃƒÂ¡ existente, confirmado). NÃƒÂ£o hÃƒÂ¡ nenhum caminho de falha PÃƒâ€œS-chamada-bem-sucedida neste handler (ao contrÃƒÂ¡rio do Textract, o resultado do Bedrock ÃƒÂ© sÃƒÂ³ re-anexado ao payload de saÃƒÂ­da, nunca persistido localmente por este handler) Ã¢â‚¬â€ nÃƒÂ£o hÃƒÂ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃƒÂ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃƒÂ¡ completa e correta Ã¢â‚¬â€ nenhum cÃƒÂ³digo novo necessÃƒÂ¡rio, nenhuma lacuna real encontrada. NÃƒÂ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador Ã¢â‚¬â€ fora de escopo do design aprovado, nÃƒÂ£o registrado como pendÃƒÂªncia.

## M7 Ã¢â‚¬â€ estado final desta sessÃƒÂ£o: cÃƒÂ³digo-completo para `dev`

Com os itens 1-9 concluÃƒÂ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃƒÂ¡ code-complete para testar em `dev`** Ã¢â‚¬â€ nÃƒÂ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃƒÂ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃƒÂ£o: nÃƒÂ£o hÃƒÂ¡ nenhum mÃƒÂ³dulo/recurso do pipeline de extraÃƒÂ§ÃƒÂ£o pendente de instanciaÃƒÂ§ÃƒÂ£o). O ÃƒÂºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃƒÂ§ÃƒÂ£o end-to-end real~~ Ã¢â‚¬â€ **FEITA em 2026-08-27**, ver a seÃƒÂ§ÃƒÂ£o `## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃƒÂ£o, 1 documentado como decisÃƒÂ£o pendente).
- Runbook de calibraÃƒÂ§ÃƒÂ£o para job Textract "preso" (mencionado no item 3) Ã¢â‚¬â€ nÃƒÂ£o bloqueia, ÃƒÂ© operacional.

## M7 Ã¢â‚¬â€ verificaÃƒÂ§ÃƒÂ£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃƒÂ£o `StartExecution` na mÃƒÂ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃƒÂ§ÃƒÂ£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃƒÂ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` Ã¢â€ â€™ `exptrk-dev-extraction-starter-handler:live` Ã¢â€ â€™ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O ÃƒÂºnico elo originado ÃƒÂ  mÃƒÂ£o foi a mensagem SQS, porque a regra EventBridge que a produziria ÃƒÂ© o ÃƒÂºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃƒÂ¡-la exigiria `terraform apply` Ã¢â‚¬â€ proibido pelo Ã‚Â§3 do `AGENTS.md`. Nada alÃƒÂ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃƒÂ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃƒÂ£o ÃƒÂ© automatizÃƒÂ¡vel por CLI. **Nenhum resultado de extraÃƒÂ§ÃƒÂ£o foi falsificado** Ã¢â‚¬â€ todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃƒÂ§ÃƒÂ£o foi produzido pelo pipeline real.

**As 3 execuÃƒÂ§ÃƒÂµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃƒÂ§ÃƒÂ£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,Ã¢â‚¬Â¦}}`) | RunTextract(falhou) Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ CheckAiKillSwitch Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃƒÂ¡ria, envelope canÃƒÂ´nico com `OCR:true` | idÃƒÂªnticos ao anterior | SUCCEEDED, **ainda degradado** Ã¢â‚¬â€ evidÃƒÂªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃƒÂ¡ria, envelope canÃƒÂ´nico **+ cÃƒÂ³pia top-level** dos 3 booleanos | RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™ NeedsBedrock Ã¢â€ â€™ ValidateSchema Ã¢â€ â€™ CompareExtractors Ã¢â€ â€™ PersistExtractedFields Ã¢â€ â€™ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃƒÂªncia do caminho feliz (`output` real da execuÃƒÂ§ÃƒÂ£o `run_c1aefÃ¢â‚¬Â¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefÃ¢â‚¬Â¦/42d064cf-Ã¢â‚¬Â¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃƒÂ­stico extraiu a data correta**. `RunBedrock` NÃƒÆ’O foi exercitado (`needsBedrock:false` Ã¢â‚¬â€ o candidato determinÃƒÂ­stico jÃƒÂ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃƒÂ³sito). `CompleteRun` apagou o artefato transitÃƒÂ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃƒÂ§ÃƒÂ£o/quota/idempotÃƒÂªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, campo `PENDING_CONFIRMATION` Ã¢â€ â€™ `CONFIRMED` (`version` 1Ã¢â€ â€™2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) Ã¢â€ â€™ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃƒÂ§ÃƒÂ£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` Ã¢â€ â€™ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃƒÂ£o reaplicou).
- `reject` no campo de `run_a22dfÃ¢â‚¬Â¦` Ã¢â€ â€™ **200**, `PENDING_CONFIRMATION` Ã¢â€ â€™ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefÃ¢â‚¬Â¦` Ã¢â€ â€™ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) Ã¢â‚¬â€ ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado Ã¢â‚¬â€ pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃƒÂ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Ã‚Â§17.3 especifica) `{"features":{"AI_EXTRACTION":Ã¢â‚¬Â¦,"OCR":Ã¢â‚¬Â¦,"WHATSAPP":Ã¢â‚¬Â¦}}`, mas o adapter lia `parsed.OCR` no nÃƒÂ­vel raiz Ã¢â‚¬â€ logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃƒÂªncia isolada: a execuÃƒÂ§ÃƒÂ£o `run_a22dfÃ¢â‚¬Â¦` rodou com a versÃƒÂ£o AppConfig 2 (`OCR:true` dentro do envelope canÃƒÂ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefÃ¢â‚¬Â¦`, cuja ÃƒÂºnica diferenÃƒÂ§a era a cÃƒÂ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃƒÂ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃƒÂ­vel raiz) + teste de regressÃƒÂ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃƒÂ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados ÃƒÂ  lista de imports estÃƒÂ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃƒÂ³pria doc-string manda "add both the file AND a static import line". Toda chamada ÃƒÂ s duas rotas retornava 500 antes de qualquer lÃƒÂ³gica de negÃƒÂ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` Ã¢â‚¬â€ o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃƒÂ³ria dos testes unitÃƒÂ¡rios ignoram atributos extras na chave, por isso a suÃƒÂ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃƒÂ³ depois dessa correÃƒÂ§ÃƒÂ£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` Ã¢â‚¬â€ GAP REAL, NÃƒÆ’O CORRIGIDO (decisÃƒÂ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃƒÂ§a Ã¢â‚¬â€ foi exatamente o que aconteceu com `run_c1aefÃ¢â‚¬Â¦`, o caso de sucesso do pipeline. Mas o **ÃƒÂºnico** cÃƒÂ³digo que escreve `ExpirationItem.dueDate` ÃƒÂ© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃƒÂ¡ `CONFIRMED` (verificado, ver acima). ConsequÃƒÂªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** ÃƒÂ© atualizado e nÃƒÂ£o existe rota que o atualize. NÃƒÂ£o corrigido de propÃƒÂ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃƒÂ§ÃƒÂ£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" ÃƒÂ© decisÃƒÂ£o de produto/design (Type 1, Ã‚Â§4 do `AGENTS.md`), nÃƒÂ£o conserto mecÃƒÂ¢nico.

### Achado menor (nÃƒÂ£o corrigido, sem impacto observado nesta verificaÃƒÂ§ÃƒÂ£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, Ã¢â‚¬Â¦), mas o Step Functions casa contra o `errorType` do Lambda, que ÃƒÂ© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, Ã¢â‚¬Â¦). Confirmado no histÃƒÂ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃƒÂ£o** casou Ã¢â‚¬â€ quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃƒÂ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃƒÂ­ **hÃƒÂ¡** perda de comportamento (uma falha transitÃƒÂ³ria de commit nÃƒÂ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD Ã¢â‚¬â€ deixado para uma sessÃƒÂ£o de infra.

### Estado do ambiente apÃƒÂ³s a sessÃƒÂ£o

Flags AppConfig **revertidas**: versÃƒÂ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃƒÂµes temporÃƒÂ¡rias 2/3 deletadas Ã¢â‚¬â€ `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃƒÂ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃƒÂ³ o ruÃƒÂ­do jÃƒÂ¡ conhecido de republicaÃƒÂ§ÃƒÂ£o de versÃƒÂ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃƒÂµes deletadas), `ocr/` no bucket transitÃƒÂ³rio volta 0. Os histÃƒÂ³ricos das 3 execuÃƒÂ§ÃƒÂµes do Step Functions ficam (artefato normal da AWS, nÃƒÂ£o ÃƒÂ© dado de teste a limpar).

**As correÃƒÂ§ÃƒÂµes dos bugs 1 e 2 estÃƒÂ£o apenas em `develop`, ainda NÃƒÆ’O implantadas em `dev`** (o CD sÃƒÂ³ aplica em push para `main`). A verificaÃƒÂ§ÃƒÂ£o do `confirm` real rodou contra o bundle esbuild jÃƒÂ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃƒÂ© o prÃƒÂ³ximo merge para `main`.

**Bloqueado sÃƒÂ³ para produÃƒÂ§ÃƒÂ£o real** (nÃƒÂ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃƒÂ§ÃƒÂ£o de modelo Bedrock + regiÃƒÂ£o, RIPD formal (`privacy-lgpd.md` Ã‚Â§6, gatilho jÃƒÂ¡ registrado).

## DecisÃƒÂµes pendentes do Marcelo

1. **M7 (extraÃƒÂ§ÃƒÂ£o/OCR)**: design jÃƒÂ¡ aprovado (D-035), zero decisÃƒÂ£o de produto pendente Ã¢â‚¬â€ sÃƒÂ³ falta decidir *quando* comeÃƒÂ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) Ã¢â‚¬â€ nÃƒÂ£o retomar sem sinal explÃƒÂ­cito dele.
3. **Estado real de `git`/deploy no inÃƒÂ­cio da prÃƒÂ³xima sessÃƒÂ£o nÃƒÂ£o deve ser presumido** Ã¢â‚¬â€ confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃƒÂªncias residuais nÃƒÂ£o bloqueantes (registradas, nÃƒÂ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃƒÂ©-existente nÃƒÂ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ Ã¢â‚¬â€ **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃƒÂºmeros (9 vulnerabilidades reais, nÃƒÂ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃƒÂ£o documentada (`testcontainers`Ã¢â€ â€™`dockerode`Ã¢â€ â€™`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃƒÂ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃƒÂ£o reverificado nesta sessÃƒÂ£o).
- Observabilidade por funÃƒÂ§ÃƒÂ£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃƒÂ©rica jÃƒÂ¡ cobre "estÃƒÂ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃƒÂ§ÃƒÂ£o de upload slot expirado) nunca exercitada contra AWS real~~ Ã¢â‚¬â€ **executada em 2026-08-25, achou um bug real de produÃƒÂ§ÃƒÂ£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado Ã¢â‚¬â€ confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃƒÂ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃƒÂ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃƒÂ³s a transiÃƒÂ§ÃƒÂ£o (ponteiro ÃƒÂ³rfÃƒÂ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃƒÂ§ÃƒÂ£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃƒÂ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃƒÂ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** Ã¢â‚¬â€ commit `2fab80f`, jÃƒÂ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃƒÂ£o deployado" estava desatualizada (verificado nesta sessÃƒÂ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃƒÂ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso Ã¢â‚¬â€ sÃƒÂ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` ÃƒÂ© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃƒÂ³ ÃƒÂ© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃƒÂ§ÃƒÂ£o defensiva do status real da occurrence antes de agir Ã¢â‚¬â€ um ponteiro ÃƒÂ³rfÃƒÂ£o nunca ÃƒÂ© revisitado fora dessa janela, e mesmo se fosse, ÃƒÂ© ignorado com seguranÃƒÂ§a. Diferente do bug do `upload-slot` (falha total Ã¢â‚¬â€ nada funcionava), este ÃƒÂ© um descompasso comentÃƒÂ¡rio-vs-cÃƒÂ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃƒÂ¡ revisado 9,2/10 pelo protocolo ClaudeÃ¢â€ â€Codex e ativo em produÃƒÂ§ÃƒÂ£o real Ã¢â‚¬â€ nÃƒÂ£o corrigido nesta sessÃƒÂ£o deliberadamente, para nÃƒÂ£o mexer num trecho crÃƒÂ­tico jÃƒÂ¡ extensivamente revisado sem uma rodada de revisÃƒÂ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃƒÂ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃƒÂ§ÃƒÂ£o real, Ã‚Â§13-30) que, se adotada como padrÃƒÂ£o oficial, deveria passar pela mesma convergÃƒÂªncia independente ClaudeÃ¢â€ â€Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃƒÂ¡ usaram Ã¢â‚¬â€ nÃƒÂ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃƒÂ£o de 12 eixos especÃƒÂ­fico do planejamento de interface, jÃƒÂ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃƒÂªncias (nÃƒÂ£o reler por padrÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ sob demanda)

- `docs/architecture/session-log.md` Ã¢â‚¬â€ linha do tempo compacta, uma entrada por sessÃƒÂ£o.
- `docs/architecture/decisions-log.md` Ã¢â‚¬â€ todas as decisÃƒÂµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` Ã¢â‚¬â€ os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` Ã¢â‚¬â€ o protÃƒÂ³tipo interativo executÃƒÂ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` Ã¢â‚¬â€ debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ implementaÃƒÂ§ÃƒÂ£o real do Full BFF + fundaÃƒÂ§ÃƒÂ£o de frontend de produÃƒÂ§ÃƒÂ£o, achados do protocolo ClaudeÃ¢â€ â€Codex sobre o cÃƒÂ³digo, Final Status.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/README.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 179ms:
# docs/architecture/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃƒÂ§ÃƒÂ£o real (cÃƒÂ³digo/infra/testes) Ã¢â‚¬â€ Implementation Blueprint concluÃƒÂ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta Ã¢â‚¬â€ zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃƒÂ§ÃƒÂ£o LGPD sem purga fÃƒÂ­sica real e DSR 100% design-only, ambos registrados como decisÃƒÂ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃƒÂ­ntese final com recomendaÃƒÂ§ÃƒÂ£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` Ã¢â‚¬â€ ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃƒÂ§ÃƒÂ£o/OCR) code-complete e deployado em `dev`** Ã¢â‚¬â€ todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃƒÂ­stico/Bedrock/validaÃƒÂ§ÃƒÂ£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃƒÂ§ÃƒÂ£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3Ã¢â€ â€™SQSÃ¢â€ â€™Step FunctionsÃ¢â€ â€™TextractÃ¢â€ â€™parserÃ¢â€ â€™persistÃƒÂªncia, ver `NEXT_SESSION_PROMPT.md`) Ã¢â‚¬â€ achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃƒÂ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃƒÂ£o corrigido e aguardando decisÃƒÂ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` Ã¢â‚¬â€ nenhuma rota escreve isso hoje); M12 bloqueado por decisÃƒÂ£o de produto (D-052); Full BFF Ã¢â‚¬â€ D-053/D-054 Ã¢â‚¬â€ **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo ClaudeÃ¢â€ â€Codex completo em `docs/frontend/frontend-production-foundation.md` Ã¢â‚¬â€ Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o atÃƒÂ© convergir); planejamento de interface concluÃƒÂ­do Ã¢â‚¬â€ 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃƒÂ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃƒÂ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃƒÂ£o **todos resolvidos** (backend + frontend onde aplicÃƒÂ¡vel Ã¢â‚¬â€ BLOCKER-C via Variante B, revisÃƒÂ£o humana explÃƒÂ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada Ã¢â‚¬â€ ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta, este bloco pode ficar temporariamente atrÃƒÂ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃƒÂ³xima aÃƒÂ§ÃƒÂ£o concreta.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

1. `AGENTS.md` (raiz) Ã¢â‚¬â€ processo de trabalho dos agentes, sempre vence sobre conteÃƒÂºdo de arquitetura.
2. ADR aceito em `adr/` Ã¢â‚¬â€ decisÃƒÂ£o arquitetural especÃƒÂ­fica e formal.
3. Documento temÃƒÂ¡tico corrente (tabela abaixo, coluna "normativo atual") Ã¢â‚¬â€ especificaÃƒÂ§ÃƒÂ£o detalhada do domÃƒÂ­nio.
4. `ARCHITECTURE.md` Ã¢â‚¬â€ visÃƒÂ£o consolidada e ÃƒÂ­ndice executivo; nÃƒÂ£o sobrescreve silenciosamente um ADR ou documento temÃƒÂ¡tico divergente Ã¢â‚¬â€ divergÃƒÂªncia entre eles ÃƒÂ© defeito a corrigir, nÃƒÂ£o licenÃƒÂ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` Ã¢â‚¬â€ estado de execuÃƒÂ§ÃƒÂ£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` Ã¢â‚¬â€ evidÃƒÂªncia histÃƒÂ³rica de como se chegou a uma decisÃƒÂ£o, nunca normativo.

## ÃƒÂndice por documento

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/ÃƒÂ­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃƒÂ³rico/processo (ciclo concluÃƒÂ­do) | Processo ClaudeÃ¢â€ â€Codex que produziu o design; nÃƒÂ£o ÃƒÂ© ponto de entrada de sessÃƒÂ£o |
| `quality-criteria.md` | normativo atual | 12 critÃƒÂ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃƒÂ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃƒÂ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃƒÂµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃƒÂ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃƒÂ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃƒÂ§ÃƒÂ£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃƒÂ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃƒÂ£o de domÃƒÂ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃƒÂ§ÃƒÂµes de estÃƒÂ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃƒÂ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃƒÂ§ÃƒÂ£o 33 Ã¢â‚¬â€ APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃƒÂ§ÃƒÂ£o 60 Ã¢â‚¬â€ componentes, interfaces, eventos/schemas, ordem de deploy, milestones Ã¢â‚¬â€ APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) Ã¢â‚¬â€ adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas Ã¢â‚¬â€ APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B Ã¢â‚¬â€ pipeline real de materializaÃƒÂ§ÃƒÂ£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃƒÂªncia (dispatch + reconciliaÃƒÂ§ÃƒÂ£o), backfill Ã¢â‚¬â€ APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃƒÂ§ÃƒÂ£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃƒÂ³rico/evidÃƒÂªncia (citado por `reminder-delivery-pipeline.md` Ã‚Â§3/Ã‚Â§3.2) | Recon prÃƒÂ©-implementaÃƒÂ§ÃƒÂ£o de BLOCKER-B (materializer/trigger de materializaÃƒÂ§ÃƒÂ£o, infra Terraform) confirmado contra o cÃƒÂ³digo real antes do design ser escrito; BLOCKER-B estÃƒÂ¡ implementado e mergeado (PR #50) Ã¢â‚¬â€ este documento sÃƒÂ³ registra a evidÃƒÂªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃƒÂ³rico/evidÃƒÂªncia | Texto verbatim do prompt de missÃƒÂ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃƒÂ³ existia em histÃƒÂ³rico de conversa de uma sessÃƒÂ£o especÃƒÂ­fica |
| `decisions-log.md` | decisÃƒÂ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃƒÂ§ÃƒÂ£o nÃƒÂ£o ÃƒÂ© sequencial na ordem das linhas Ã¢â‚¬â€ D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada | Artefatos do protocolo ClaudeÃ¢â€ â€Codex de M7 (proposta Claude, proposta Codex, crÃƒÂ­tica, reconciliaÃƒÂ§ÃƒÂ£o final aprovada Ã¢â‚¬â€ D-035) |
| `adr/` | decisÃƒÂ£o/ADR | 11 ADRs formais para decisÃƒÂµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada (protocolo `AGENTS.md` Ã‚Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃƒÂªncia CloudFront + Full BFF) Ã¢â‚¬â€ nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/ÃƒÂ­ndice (visual) | **O documento de status do projeto** Ã¢â‚¬â€ painel visual (timeline de marcos, achados reais, pendÃƒÂªncias); abrir no navegador para uma visÃƒÂ£o executiva rÃƒÂ¡pida, mais legÃƒÂ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃƒÂ£o) |
| `session-log.md` | histÃƒÂ³rico | Log cronolÃƒÂ³gico compacto por sessÃƒÂ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃƒÂ£o normativo) | Fase 1 da evoluÃƒÂ§ÃƒÂ£o estratÃƒÂ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃƒÂ§ÃƒÂ£o de cada capacidade comercial proposta contra o cÃƒÂ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃƒÂ­nio + protocolo ClaudeÃ¢â€ â€Codex por tema), nunca decisÃƒÂ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃƒÂ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 Ã¢â‚¬â€ achado central: billing por sujeito rastreado ÃƒÂ© padrÃƒÂ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃƒÂ­nio (`TrackedSubject`+`RequirementAssignment`) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃƒÂ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃƒÂ§ÃƒÂ£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃƒÂ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃƒÂ§ÃƒÂ£o de mini-revisÃƒÂ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃƒÂ£o direta de padrÃƒÂ£o jÃƒÂ¡ em produÃƒÂ§ÃƒÂ£o (mesma partiÃƒÂ§ÃƒÂ£o de `Document`/M6); digest registrado como questÃƒÂ£o aberta, nÃƒÂ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, lista de rejeitados formal sÃƒÂ³ na Fase 3) | Fase 2b, sexto cluster Ã¢â‚¬â€ custom fields genÃƒÂ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃƒÂ£o (nota 9,1/9,0), valor jÃƒÂ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4, ADR formal sÃƒÂ³ na Fase 3) | Fase 2b, sÃƒÂ©timo e ÃƒÂºltimo cluster (CSV import/export) Ã¢â‚¬â€ protocolo ClaudeÃ¢â€ â€Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃƒÂ§ÃƒÂ£o (nÃƒÂ£o na entrada); plano linha-a-linha em S3, nÃƒÂ£o DynamoDB; **Fase 2b concluÃƒÂ­da, 7/7 clusters Ã¢â€°Â¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃƒÂ­ntese proposta, nÃƒÂ£o autorizaÃƒÂ§ÃƒÂ£o de implementaÃƒÂ§ÃƒÂ£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph Ã¢â‚¬â€ consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃƒÂ­ntese proposta, ADRs formais sÃƒÂ³ com decisÃƒÂ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃƒÂ§a/persistÃƒÂªncia/custo, lista de 10 ADRs candidatos, estratÃƒÂ©gia de teste/migraÃƒÂ§ÃƒÂ£o, perguntas abertas reais, capacidades rejeitadas Ã¢â‚¬â€ inclui revisÃƒÂ£o adversarial final de coerÃƒÂªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃƒÂ§ÃƒÂ£o de prÃƒÂ©-requisito (D-046) | Mini-revisÃƒÂ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) Ã¢â‚¬â€ pico orgÃƒÂ¢nico ~220Ãƒâ€” abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/ÃƒÂ­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-047/D-048) | Fecha D-047 Ã¢â‚¬â€ entrega/reenvio do link de guest upload: rotaÃƒÂ§ÃƒÂ£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-049) | Fecha "DecisÃƒÂ£o B" de D-048 Ã¢â‚¬â€ automatiza o convite inicial de guest upload (hoje manual) atrÃƒÂ¡s de preferÃƒÂªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-052) | PÃƒÂ³s-M11: M12 (Billing) fica bloqueado por decisÃƒÂ£o de produto (fornecedor de pagamento) Ã¢â‚¬â€ zero cÃƒÂ³digo novo; achado real de que o projeto nÃƒÂ£o tem conceito de "platform staff" cross-tenant, descartando atÃƒÂ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃƒÂ£o reconciliada via protocolo AGENTS.md Ã‚Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃƒÂ£o do browser Ã¢â‚¬â€ decide o mecanismo de autenticaÃƒÂ§ÃƒÂ£o das chamadas de recurso que o "BFF de sessÃƒÂ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃƒÂ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃƒÂ§ÃƒÂ£o nativa do Cognito no refresh (nÃƒÂ£o contador local Ã¢â‚¬â€ evita falso-positivo de invalidaÃƒÂ§ÃƒÂ£o), tabela de sessÃƒÂ£o dedicada IAM-isolada, cookies login/sessÃƒÂ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) Ã¢â‚¬â€ ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃƒÂ§ÃƒÂ£o e do protocolo ClaudeÃ¢â€ â€Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃƒÂ§a de sessÃƒÂ£o corrigidos) |
| `reviews/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada (protocolo `AGENTS.md` Ã‚Â§4, pÃƒÂ³s-M0) | Artefatos de revisÃƒÂ£o ClaudeÃ¢â€ â€Codex de implementaÃƒÂ§ÃƒÂ£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) Ã¢â‚¬â€ mesmo papel de `history/` (evidÃƒÂªncia, nunca normativo), mas para rodadas ocorridas depois que cÃƒÂ³digo passou a existir, em vez das rodadas de design conceitual prÃƒÂ©-implementaÃƒÂ§ÃƒÂ£o |
| `history/` | histÃƒÂ³rico/supersedido | Artefatos de rodada (propostas, crÃƒÂ­ticas, red team) que produziram os documentos normativos acima Ã¢â‚¬â€ ver subseÃƒÂ§ÃƒÂ£o |

## `history/` Ã¢â‚¬â€ evidÃƒÂªncia de rodada, por tema

Cada subpasta contÃƒÂ©m os artefatos de proposta/crÃƒÂ­tica/trÃƒÂ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃƒÂ£o vigente Ã¢â‚¬â€ sÃƒÂ³ como prova de como o consenso foi alcanÃƒÂ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/engineering/README.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 174ms:
# docs/engineering/ Ã¢â‚¬â€ ÃƒÂndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃƒÂDO Ã¢â‚¬â€ 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃƒÂ³ Contexto bateu o gate Ã¢â€°Â¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃƒÂ³rio trata de **como o trabalho de engenharia ÃƒÂ© medido e revisado** (rubrica, critÃƒÂ©rios por eixo, protocolo de debate, achados de auditoria) Ã¢â‚¬â€ nÃƒÂ£o confundir com `docs/architecture/`, que trata do que o sistema Ãƒâ€° (design, modelo de dados, decisÃƒÂµes de arquitetura). Se a dÃƒÂºvida for "o GSI3 ÃƒÂ© consultÃƒÂ¡vel por quem", vÃƒÂ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃƒÂ³rio ÃƒÂ© o certo.

## PrecedÃƒÂªncia de fontes (quando houver divergÃƒÂªncia)

Mesma regra de `docs/architecture/README.md` Ã¢â‚¬â€ `AGENTS.md` (raiz) sempre vence sobre conteÃƒÂºdo deste diretÃƒÂ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) ÃƒÂ© evidÃƒÂªncia de uma rodada especÃƒÂ­fica, nunca redefine os pesos/critÃƒÂ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃƒÂ¡ em vez de duplicar, ver seu prÃƒÂ³prio rodapÃƒÂ© "Como adicionar um novo eixo").

## Quando carregar o quÃƒÂª (roteamento por tipo de tarefa)

| Sua tarefa ÃƒÂ©... | Carregue |
|---|---|
| Rodar o protocolo ClaudeÃ¢â€ â€Codex num eixo especÃƒÂ­fico (`AGENTS.md` Ã‚Â§4) | `joint-review-criteria.md` (seÃƒÂ§ÃƒÂ£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃƒÂ­fico do cÃƒÂ³digo tira / que critÃƒÂ©rio ele afeta | `joint-review-criteria.md` (nÃƒÂ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional ÃƒÂ© vÃƒÂ¡lido/de qualidade suficiente (gate binÃƒÂ¡rio, critÃƒÂ©rio ponderado, nota mÃƒÂ­nima) | `test-engineering-standard.md` Ã¢â‚¬â€ rÃƒÂ©gua concreta que `joint-review-criteria.md`'s critÃƒÂ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃƒÂ§a precisa de ADR/protocolo formal ou ÃƒÂ© correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica | `change-risk-scale.md` (a rÃƒÂ©gua concreta) Ã¢â‚¬â€ `AGENTS.md` Ã‚Â§4 sÃƒÂ³ distingue os dois extremos de forma binÃƒÂ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` Ã¢â‚¬â€ mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃƒÂ§ÃƒÂ£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃƒÂ£o | `exceptions.md` |
| Ver o histÃƒÂ³rico completo de decisÃƒÂµes de engenharia (nÃƒÂ£o de arquitetura) com motivo | `decisions-log.md` Ã¢â‚¬â€ E-000 em diante, mesmo padrÃƒÂ£o de `docs/architecture/decisions-log.md` mas para decisÃƒÂµes de processo/qualidade, nÃƒÂ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃƒÂ©rios/gates G1-G6 (histÃƒÂ³rico, prÃƒÂ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA Ã¢â‚¬â€ mudanÃƒÂ§a exige nova rodada formal, nÃƒÂ£o ediÃƒÂ§ÃƒÂ£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃƒÂ³rio no inÃƒÂ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` Ã¢â‚¬â€ histÃƒÂ³rico, nÃƒÂ£o estado atual (ver aviso no prÃƒÂ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃƒÂ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃƒÂ§ÃƒÂ£o de nomenclatura abaixo) |
| Consultar evidÃƒÂªncia bruta de uma rodada especÃƒÂ­fica (nota cega, prompt do Codex, saÃƒÂ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` Ã¢â‚¬â€ evidÃƒÂªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃƒÂ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) Ã¢â‚¬â€ link relativo quebrado + referÃƒÂªncia `AGENTS.md Ã‚Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## ÃƒÂndice por documento (nÃƒÂ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃƒÂ§ÃƒÂ£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃƒÂ­pios de engenharia adotados (proporcionalidade, evidÃƒÂªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃƒÂ§a NÃƒÂ­vel 1-6, rÃƒÂ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃƒÂ©rios/pesos por eixo das revisÃƒÂµes conjuntas ClaudeÃ¢â€ â€Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo ClaudeÃ¢â€ â€Codex 8 rodadas, gate elevado 9,5/10) | PadrÃƒÂ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) Ã¢â‚¬â€ gates binÃƒÂ¡rios (G-V1..G-V6, G-C1), critÃƒÂ©rios ponderados, fÃƒÂ³rmula de agregaÃƒÂ§ÃƒÂ£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃƒÂ§ÃƒÂµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃƒÂ£o (log vivo) | DecisÃƒÂµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃƒÂ³rico/registro vivo | DivergÃƒÂªncias materiais ClaudeÃ¢â€ â€Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Ã‚Â§4 pode ser dispensado, inventÃƒÂ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃƒÂ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃƒÂ³rico (CONGELADA) | Rubrica original de 12 critÃƒÂ©rios/gates G1-G6 da Engineering Maturity Review Ã¢â‚¬â€ antecessora conceitual de `joint-review-criteria.md`, nÃƒÂ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃƒÂ§ÃƒÂµes executÃƒÂ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃƒÂ³rico/fundamentaÃƒÂ§ÃƒÂ£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃƒÂ³rico | Estado do repositÃƒÂ³rio no inÃƒÂ­cio da Engineering Maturity Review Ã¢â‚¬â€ nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃƒÂ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) Ã¢â‚¬â€ DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃƒÂ­ntese/recomendaÃƒÂ§ÃƒÂ£o (entregÃƒÂ¡vel final do programa, prompt mestre Ã‚Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃƒÂªncia de `pilot-readiness-program.md` Ã¢â‚¬â€ nÃƒÂ£o repete achado, sÃƒÂ³ aponta; nÃƒÂ£o ÃƒÂ© aprovaÃƒÂ§ÃƒÂ£o final, ÃƒÂ© insumo para a decisÃƒÂ£o do Marcelo |
| `reviews/` | histÃƒÂ³rico/evidÃƒÂªncia de rodada | Artefatos de toda rodada ClaudeÃ¢â€ â€Codex de processo/qualidade Ã¢â‚¬â€ ver seÃƒÂ§ÃƒÂ£o prÃƒÂ³pria abaixo |

## `reviews/` Ã¢â‚¬â€ convenÃƒÂ§ÃƒÂ£o de nomenclatura e classificaÃƒÂ§ÃƒÂ£o

EvidÃƒÂªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃƒÂµes de arquitetura: nunca normativo, sÃƒÂ³ prova de como uma nota/decisÃƒÂ£o foi alcanÃƒÂ§ada. TrÃƒÂªs geraÃƒÂ§ÃƒÂµes de conteÃƒÂºdo coexistem aqui, nesta ordem cronolÃƒÂ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃƒÂ­vel raiz de `reviews/`) Ã¢â‚¬â€ produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos ÃƒÂ© intencional (agrupa antes de outros nomes em ordem alfabÃƒÂ©tica, sinaliza "transcript bruto, nÃƒÂ£o leia direto").
2. **ConvergÃƒÂªncia dos critÃƒÂ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) Ã¢â‚¬â€ produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) Ã¢â‚¬â€ a rodada em andamento nesta sessÃƒÂ£o. PadrÃƒÂ£o de nomenclatura por eixo (`<eixo>` Ã¢Ë†Ë† `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... Ã¢â‚¬â€ nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... Ã¢â‚¬â€ prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... Ã¢â‚¬â€ saÃƒÂ­da bruta do Codex CLI. Alguns trechos que ecoam saÃƒÂ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃƒÂ§ÃƒÂµes via PowerShell) Ã¢â‚¬â€ revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃƒÂ£o sÃƒÂ³ mitigado); a saÃƒÂ­da de uma rodada em andamento pode ainda conter mojibake atÃƒÂ© a rodada seguinte revisÃƒÂ¡-la, por ser evidÃƒÂªncia datada do que o Codex realmente viu naquele momento Ã¢â‚¬â€ nunca editada retroativamente. Se precisar do conteÃƒÂºdo, prefira o `-summary.md` correspondente, que jÃƒÂ¡ extrai a informaÃƒÂ§ÃƒÂ£o sem o ruÃƒÂ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` Ã¢â‚¬â€ **o ÃƒÂºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃƒÂ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃƒÂ§ÃƒÂ£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃƒÂ£o fonte de verdade por si sÃƒÂ³ Ã¢â‚¬â€ sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃƒÂ­tica de proveniÃƒÂªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃƒÂ£o Ã¢â‚¬â€ exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃƒÂ¡ que sua proveniÃƒÂªncia ÃƒÂ© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃƒÂ§ÃƒÂ£o cronolÃƒÂ³gica descrita nesta seÃƒÂ§ÃƒÂ£o. Isso ÃƒÂ© uma decisÃƒÂ£o de design, nÃƒÂ£o uma lacuna: um transcript nunca ÃƒÂ© lido isoladamente por convenÃƒÂ§ÃƒÂ£o (ver acima), entÃƒÂ£o nÃƒÂ£o precisa carregar sua prÃƒÂ³pria metadata.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 176ms:
# W3-07 Ã¢â‚¬â€ Status final: REPROVADO, NÃƒÆ’O IMPLEMENTADO (2026-08-28)

> D-062 em `docs/architecture/decisions-log.md`. Este documento resume o resultado das 4
> rodadas para quem retomar este trabalho no futuro Ã¢â‚¬â€ nÃƒÂ£o reabrir o protocolo do zero sem ler
> isto primeiro.

## Resultado

4 rodadas ClaudeÃ¢â€ â€Codex, notas **3,4 Ã¢â€ â€™ 5,1 Ã¢â€ â€™ 4,7 / 10** Ã¢â‚¬â€ nunca atingiu o gate de 9,0. Reprovado
por decisÃƒÂ£o do Marcelo, seguindo recomendaÃƒÂ§ÃƒÂ£o convergente e independente de Claude e Codex
(pedida explicitamente apÃƒÂ³s a Rodada 3): **nÃƒÂ£o implementar agora**, registrar os achados como
pesquisa.

## Por que nÃƒÂ£o convergiu (o achado central)

Toda tentativa de fechar "COMPLETED" com garantia real de nÃƒÂ£o-ressurreiÃƒÂ§ÃƒÂ£o (um dado do tenant
sendo recriado depois de uma exclusÃƒÂ£o jÃƒÂ¡ declarada concluÃƒÂ­da) esbarrou na mesma parede: o
sistema tem **trÃƒÂªs classes de escritor** que uma prÃƒÂ©-condiÃƒÂ§ÃƒÂ£o simples (ex.: desativar login no
Cognito) nÃƒÂ£o fecha:

1. HTTP autenticado (fechÃƒÂ¡vel via Cognito).
2. **SuperfÃƒÂ­cie pÃƒÂºblica de guest upload** (`GuestSubmissionService` Ã¢â‚¬â€ nunca passa por
   `RequestContext`/Cognito, autentica sÃƒÂ³ por token opaco).
3. **Workers assÃƒÂ­ncronos** jÃƒÂ¡ enfileirados ou agendados independentemente de qualquer sessÃƒÂ£o de
   usuÃƒÂ¡rio (`reminder-materialization-trigger`, `reminder-reconciliation`,
   `document-chasing-dispatch`, outbox relay/sweeper, callback SES, reconciliaÃƒÂ§ÃƒÂ£o de
   upload/malware, import, extraÃƒÂ§ÃƒÂ£o/OCR via Step Functions).

Fechar as trÃƒÂªs de verdade exige um fence de `TenantStatus` consultado por **todo** esses
caminhos Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© mais "o mecanismo de descoberta+exclusÃƒÂ£o", ÃƒÂ© a mesma feature de "bloqueio
imediato" que a decisÃƒÂ£o de escopo original desta sessÃƒÂ£o jÃƒÂ¡ tinha deixado de fora
deliberadamente. As duas decisÃƒÂµes de escopo (mecanismo pequeno vs. sem bloqueio imediato) sÃƒÂ£o
mutuamente incompatÃƒÂ­veis com uma garantia forte Ã¢â‚¬â€ nÃƒÂ£o dÃƒÂ¡ para ter as duas.

## Por que nÃƒÂ£o foi aceito um "best-effort documentado" como meio-termo

RecomendaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita do Codex, aceita: implementar um mecanismo que alega apagar
fisicamente mas nÃƒÂ£o sustenta essa garantia ÃƒÂ© uma capacidade **pior que nÃƒÂ£o ter nenhuma** Ã¢â‚¬â€ pode
ser lido operacionalmente como "DSR atendido" mesmo com uma ressalva no `decisions-log.md`, e
ainda geraria cÃƒÂ³digo que precisaria ser redesenhado quando a garantia real for exigida de
verdade.

## O que sobrevive desta pesquisa (reusar, nÃƒÂ£o redescobrir)

- **InventÃƒÂ¡rio verificado dos 40 `entityType` reais** do sistema (`src/modules/**`), com
  classificaÃƒÂ§ÃƒÂ£o por presenÃƒÂ§a de `version`/`tenantId` Ã¢â‚¬â€ ver Rodada 3/4
  (`claude-proposal-round3.md`/`round4.md`) para a tabela completa e as correÃƒÂ§ÃƒÂµes feitas em
  cima da prÃƒÂ³pria Rodada 2 (que tambÃƒÂ©m errou o inventÃƒÂ¡rio na primeira tentativa Ã¢â‚¬â€ dois erros
  reais de levantamento em rodadas diferentes, liÃƒÂ§ÃƒÂ£o: sempre grep exaustivo, nunca amostra).
- **Bug real descoberto, nÃƒÂ£o corrigido ainda**: `tenantId` (MVP `tenantId=userId`) nÃƒÂ£o ÃƒÂ© o
  Cognito `sub`/`Username` Ã¢â‚¬â€ `IdentityMapping.cognitoSub`/`User.identitySubject` sÃƒÂ£o os campos
  certos. Qualquer aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant precisa resolver isso
  primeiro.
- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
  `bff-session-table`, uma tabela DynamoDB fÃƒÂ­sica separada Ã¢â‚¬â€ nenhum mecanismo de descoberta
  tenant-wide pode assumir uma ÃƒÂºnica tabela.
- **CorreÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica pendente, independente**: `PolicyRef` (`reminder-policy.ts:69-72`) nÃƒÂ£o
  carrega `tenantId` Ã¢â‚¬â€ pequena, real, trivial de corrigir numa sessÃƒÂ£o futura sem depender de
  W3-07.
- **Reuso jÃƒÂ¡ decidido para quando retomar**: `Document` continua a ÃƒÂºnica categoria com efeito
  colateral S3 real Ã¢â‚¬â€ reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061), nunca duplicar.

## Gatilho para retomar

Requisito real de DSR (idealmente decidido antes do primeiro cliente B2B que dependa disso).
Quando retomado, o fence de `TenantStatus` em todo write path (HTTP + guest + workers) deve
entrar no escopo do **primeiro** desenho, nÃƒÂ£o ser descoberto de novo rodada a rodada como
aconteceu aqui.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 171ms:
# W3-07 (retomada, D-063) Ã¢â‚¬â€ status: PAUSADO apÃƒÂ³s Rodada 1, para planejamento minucioso na prÃƒÂ³xima sessÃƒÂ£o

> DecisÃƒÂ£o do Marcelo (2026-08-28): nÃƒÂ£o adiar por falta de usuÃƒÂ¡rio real hoje Ã¢â‚¬â€ construir a
> garantia completa de exclusÃƒÂ£o de tenant algum dia, mas com anÃƒÂ¡lise minuciosa antes de
> implementar. Esta pasta ÃƒÂ© a continuaÃƒÂ§ÃƒÂ£o de
> `docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/` (D-062, reprovada em 4
> rodadas) Ã¢â‚¬â€ leia aquela primeiro (especialmente `claude-final-status-not-approved.md`) antes
> desta.

## O que esta rodada tentou

Reabrir o desenho com o fence de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" dentro do escopo desde a Rodada 1 (em vez
de descoberto reativamente, como na tentativa D-062). Achado de partida:
`User.status: "ACTIVE" | "SUSPENDED"` (`user-repository.ts:22`) jÃƒÂ¡ existe e jÃƒÂ¡ ÃƒÂ© checado em toda
resoluÃƒÂ§ÃƒÂ£o de contexto HTTP autenticada (`resolve-request-context.ts:67-69`), mas `"SUSPENDED"`
nunca ÃƒÂ© escrito por nenhum cÃƒÂ³digo hoje. A proposta usava isso como fence primÃƒÂ¡rio (zero cÃƒÂ³digo
novo para a superfÃƒÂ­cie HTTP autenticada) + checagem nova no fluxo de convidado + auditoria dos
workers em `src/workers/`.

## Resultado: Rodada 1, nota 3,2/10, REPROVADA Ã¢â‚¬â€ pior que a rodada equivalente da tentativa anterior

Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
**precisa** resolver desde o desenho inicial, nÃƒÂ£o descobrir depois:

### Achado mais grave Ã¢â‚¬â€ o prÃƒÂ³prio fence ÃƒÂ© apagÃƒÂ¡vel pela cascata que ele deveria proteger

`RequestContextResolver.resolve()` (`resolve-request-context.ts:49-65`) provisiona
automaticamente um `User` novo com `status: "ACTIVE"` no primeiro login de um `cognitoSub`
sem perfil existente. Se a cascata apaga o `User` (ele ÃƒÂ© sÃƒÂ³ mais uma linha do tenant, como
qualquer outra), um token ainda vÃƒÂ¡lido reautentica e o sistema **recria o tenant sozinho** Ã¢â‚¬â€
a prÃƒÂ³pria lÃƒÂ³gica de "provisionar no primeiro login" resssucita o que acabou de ser apagado.
**ConsequÃƒÂªncia de design**: o tombstone/fence do tenant nÃƒÂ£o pode ser a mesma linha que a
cascata elimina Ã¢â‚¬â€ precisa ser um registro que sobrevive ÃƒÂ  exclusÃƒÂ£o completa (ex.: um registro
de "tenant encerrado" fora do universo apagÃƒÂ¡vel, no mesmo espÃƒÂ­rito do `TenantDeletionRequest`
jÃƒÂ¡ proposto em D-062, mas agora como fonte de verdade que o RESOLVER tambÃƒÂ©m consulta, nÃƒÂ£o sÃƒÂ³
um tombstone passivo).

### Checagens desacopladas (leitura solta antes de agir) tÃƒÂªm corrida TOCTOU real

Qualquer "leia `User.status`, depois aja" (proposto para `GuestSubmissionService` e para os
workers) tem uma janela real entre a leitura e o efeito. Para efeitos DynamoDB, o fence precisa
participar da MESMA `TransactWriteItems` da mutaÃƒÂ§ÃƒÂ£o (`ConditionCheck`), nÃƒÂ£o uma leitura solta
antes. Para efeitos externos (SES, S3, SQS, Step Functions) nÃƒÂ£o existe uma condiÃƒÂ§ÃƒÂ£o atÃƒÂ´mica
equivalente Ã¢â‚¬â€ precisa de um protocolo de claim/drenagem dedicado, nÃƒÂ£o uma checagem simples.

### Auditoria de superfÃƒÂ­cies de escrita estava errada e incompleta

Confirmado incorreto (releem entidade de origem, mas ainda produzem efeito novo sem fence real):
- `dispatch-outbox-relay` Ã¢â‚¬â€ produz mensagem SQS nova antes de marcar o outbox como publicado.
- `upload-slot-reconciliation` Ã¢â‚¬â€ escreve `Document.status = TIMEOUT` sem reler fence de tenant.
- `document-chasing-dispatch` Ã¢â‚¬â€ pode criar `GuestTokenPointer` novo e mandar e-mail via SES
  depois de o tenant jÃƒÂ¡ estar suspenso.

SuperfÃƒÂ­cies inteiras que a auditoria por pasta (`src/workers/`) nÃƒÂ£o capturou:
- `import-commit-handler` Ã¢â‚¬â€ fabrica um `RequestContext` de sistema com papel `OWNER`, nunca
  resolve usuÃƒÂ¡rio nem checa status.
- `start-extraction-run`/Step Functions do mÃƒÂ³dulo `extraction` Ã¢â‚¬â€ inicia execuÃƒÂ§ÃƒÂ£o sem fence,
  tarefas subsequentes continuam escrevendo artefato S3/estado depois da suspensÃƒÂ£o.
- `ses-callback-workflow` Ã¢â‚¬â€ cria/atualiza `WebhookInbox`/`NotificationAttempt`/
  `NotificationPreferences` sem checar fence.
- URLs presignadas de S3 jÃƒÂ¡ entregues a um convidado continuam vÃƒÂ¡lidas mesmo apÃƒÂ³s o fence Ã¢â‚¬â€
  um upload feito depois ainda dispara toda a cadeia de finalizaÃƒÂ§ÃƒÂ£o/malware/extraÃƒÂ§ÃƒÂ£o.

**LiÃƒÂ§ÃƒÂ£o estrutural para a prÃƒÂ³xima tentativa**: organizar o levantamento por **ponto de entrada
de runtime** (API Gateway, fila SQS, DynamoDB Streams, evento S3/EventBridge, EventBridge
Scheduler, callback SNS/SES, Step Functions), nunca por pasta de cÃƒÂ³digo (`src/workers/`) Ã¢â‚¬â€
essa organizaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ produziu falsos negativos duas vezes (D-062 e esta rodada).

## O que jÃƒÂ¡ estÃƒÂ¡ validado e deve ser reaproveitado, nÃƒÂ£o redescoberto

- InventÃƒÂ¡rio verificado dos ~40 `entityType` reais do sistema, por presenÃƒÂ§a de `version`/
  `tenantId` (D-062, Rodadas 3-4).
- Mecanismo de descoberta+exclusÃƒÂ£o (Scan + taxonomia de 3 categorias + convergÃƒÂªncia por
  re-Scan) Ã¢â‚¬â€ a parte de "como apagar o que jÃƒÂ¡ nÃƒÂ£o tem mais escritor" continua vÃƒÂ¡lida; o que
  falta ÃƒÂ© sÃƒÂ³ a garantia de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" antes de declarar concluÃƒÂ­do.
- `Document` deve reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061) Ã¢â‚¬â€ nunca duplicar.
- `Session`/`LoginAttempt`/`DeviceSession` vivem em `bff-session-table`, tabela fÃƒÂ­sica separada.
- Usar `User.status` (ou um registro de tenant equivalente) como sinal central ÃƒÂ© uma boa
  direÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ nÃƒÂ£o pode ser o prÃƒÂ³prio dado que a cascata apaga, e nÃƒÂ£o pode ser uma leitura
  desacoplada da escrita protegida.

## PrÃƒÂ³xima sessÃƒÂ£o Ã¢â‚¬â€ como retomar com rigor

1. Ler esta pasta inteira + `w3-07-tenant-cascade-deletion-design/` (D-062) antes de propor
   qualquer coisa nova Ã¢â‚¬â€ nÃƒÂ£o repetir o levantamento do zero.
2. Levantar TODOS os pontos de entrada de runtime reais (nÃƒÂ£o sÃƒÂ³ `src/workers/`) que podem
   escrever dado de um tenant: grep por `SQSEvent`, `handler(): Promise<void>` (schedule),
   `S3Event`/`EventBridgeEvent`, Step Functions (`infra/state-machines/*.asl.json`), callbacks
   SNS/SES Ã¢â‚¬â€ e para cada um, confirmar se e como ele valida que o tenant/recurso de origem
   ainda ÃƒÂ© vÃƒÂ¡lido.
3. Desenhar o tombstone do tenant como um registro que **sobrevive** ÃƒÂ  prÃƒÂ³pria cascata e que
   `RequestContextResolver`/toda superfÃƒÂ­cie de entrada consulta ANTES de qualquer
   provisionamento automÃƒÂ¡tico (fechando o achado mais grave desta rodada).
4. Decidir, para cada superfÃƒÂ­cie de escrita, se o fence entra via `ConditionCheck` na mesma
   transaÃƒÂ§ÃƒÂ£o (caminhos DynamoDB) ou via um protocolo de claim/drenagem dedicado (efeitos
   externos: SES, S3, Step Functions, filas).
5. SÃƒÂ³ depois disso desenhar novamente a descoberta+exclusÃƒÂ£o em si (que jÃƒÂ¡ estÃƒÂ¡ bem
   encaminhada) Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© o gargalo, o fence ÃƒÂ©.

## Registro de decisÃƒÂ£o

D-063 em `docs/architecture/decisions-log.md`: reprovada na Rodada 1 (3,2/10), pausada por
pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada futura, nÃƒÂ£o uma
decisÃƒÂ£o de abandonar o objetivo.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 163ms:
# W3-07 Ã¢â‚¬â€ Tenant Deletion Fence, Proposta Round 1 (retomada pÃƒÂ³s-D-063)

> Contexto obrigatÃƒÂ³rio jÃƒÂ¡ lido: `w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md` (D-062)
> e `w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (D-063). Este documento
> assume os achados de ambos como verdadeiros e nÃƒÂ£o os redescobre.

## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃƒÂ£o por pasta)

Auditoria completa dos 36 handlers reais em `src/runtime/aws/handlers/` contra o Terraform (gatilho declarado,
nÃƒÂ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:

1. **NÃƒÂ£o existe hoje nenhum cÃƒÂ³digo de cascata/fence** Ã¢â‚¬â€ greenfield real, nenhuma verificaÃƒÂ§ÃƒÂ£o de "tenant
   excluÃƒÂ­do" em lugar nenhum.
2. `RequestContextResolver.resolve()` ÃƒÂ© o ÃƒÂºnico ponto do sistema que checa qualquer status hoje
   (`UserProfile.status !== "ACTIVE"`), e sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ado por rotas HTTP autenticadas. A checagem ÃƒÂ©
   **desacoplada** (lida uma vez no inÃƒÂ­cio do handler, fora de qualquer transaÃƒÂ§ÃƒÂ£o).
3. `GuestSubmissionService.resolveToken()` **nunca** consulta tenant/usuÃƒÂ¡rio Ã¢â‚¬â€ sÃƒÂ³ token/`DocumentRequest`
   (comentÃƒÂ¡rio explÃƒÂ­cito no cÃƒÂ³digo confirmando que ÃƒÂ© deliberado). Sem fence hoje.
4. `startSubmission()`/`handleReserveUpload`/`handleReserveImport` emitem **URLs S3 presignadas** (PUT) que o
   S3 aceita sem passar por nenhum handler nosso atÃƒÂ© o evento "Object Created" disparar a finalizaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€
   minutos depois, com `tenantId` derivado da **key S3 jÃƒÂ¡ gravada**, nÃƒÂ£o de contexto revalidado.
5. Toda a cadeia `import-parse Ã¢â€ â€™ import-commit`, `reminder-producer Ã¢â€ â€™ reminder-dispatch Ã¢â€ â€™
   reminder-materialization-trigger`, `dispatch-outbox-relay`/`outbox-sweeper`, `notification-router Ã¢â€ â€™
   email-delivery Ã¢â€ â€™ ses-callback`, `document-chasing-dispatch`, `malware-result`/`upload-finalizer`,
   `upload-slot-reconciliation` Ã¢â‚¬â€ **nenhuma** checa tenant hoje; `tenantId` vem sempre de um payload jÃƒÂ¡
   enfileirado ou de uma key S3 jÃƒÂ¡ materializada antes.
6. A Step Functions `document-extraction` (`extraction-starter Ã¢â€ â€™ RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™
   [RunBedrock] Ã¢â€ â€™ ValidateSchema/CompareExtractors Ã¢â€ â€™ PersistExtractedFields/MarkPendingConfirmation Ã¢â€ â€™
   CompleteRun`) propaga `tenantId` pelo payload de state para state, nunca revalida. As escritas reais
   (`PersistExtractedFields`/`MarkPendingConfirmation`) sÃƒÂ³ acontecem nos dois ÃƒÂºltimos states Ã¢â‚¬â€ minutos depois
   do inÃƒÂ­cio, depois de uma chamada Bedrock paga real e de um `waitForTaskToken` do Textract (atÃƒÂ© 600s).
7. **Achado que muda o desenho**: apesar da superfÃƒÂ­cie parecer enorme (36 handlers, 3 classes de gatilho,
   Step Functions incluÃƒÂ­da), praticamente toda escrita de dado de tenant termina em um de dois primitivos
   jÃƒÂ¡ existentes e centralizados:
   - `TransactWriteCommand` (DynamoDB), construÃƒÂ­do em **8 arquivos de persistÃƒÂªncia**
     (`dynamodb-{extracted-field,document,reminder,subject,import,expiration,reminder-producer,notification}-store.ts`),
     usando os builders de `src/shared/dynamodb/occ.ts` (`buildVersionedUpdate`/`buildVersionedCreate`/
     `buildVersionConditionCheck`/`buildExistenceConditionCheck`) Ã¢â‚¬â€ regra jÃƒÂ¡ normativa em `AGENTS.md` Ã‚Â§7.
   - `UpdateCommand`/`PutCommand`/`DeleteCommand` single-item, em **11 arquivos de persistÃƒÂªncia** (mesma lista
     + `dynamodb-extraction-run-store`, `dynamodb-textract-job-store`, `dynamodb-session-store` [tabela
     separada], `dynamodb-outbox-relay-store`, `dynamodb-identity-store`).
   Isso ÃƒÂ© uma superfÃƒÂ­cie **enumerÃƒÂ¡vel e jÃƒÂ¡ disciplinada**, nÃƒÂ£o 36 pontos ad-hoc Ã¢â‚¬â€ o oposto do que quebrou
   D-062/D-063.

## 1. O tombstone (sobrevive ÃƒÂ  cascata)

Entidade nova `TenantLifecycleRecord`, **fora do universo apagÃƒÂ¡vel pela cascata** (a rotina de
descoberta+exclusÃƒÂ£o por Scan de D-062/D-063 exclui explicitamente este `entityType` Ã¢â‚¬â€ mesma disciplina jÃƒÂ¡
usada para excluir a prÃƒÂ³pria tabela `bff-session-table` do Scan).

```
PK = "TENANT#<tenantId>#LIFECYCLE"
SK = "LIFECYCLE"
entityType = "TenantLifecycleRecord"
status = "ACTIVE" | "DELETING" | "DELETED"
deletionStartedAt / deletionCompletedAt
version   (OCC padrÃƒÂ£o)
```

Vive na tabela principal (nÃƒÂ£o numa tabela separada Ã¢â‚¬â€ precisa ser lido pelo mesmo `TransactWriteItems` que
grava dado de tenant, e `TransactWriteItems` nÃƒÂ£o atravessa tabelas). Nasce `ACTIVE` no primeiro
`createProfileIfAbsent` (mesma transaÃƒÂ§ÃƒÂ£o, custo zero de coordenaÃƒÂ§ÃƒÂ£o adicional).

## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃƒÂ£o handler por handler)

Dois wrappers novos em `src/shared/dynamodb/`, substituindo a construÃƒÂ§ÃƒÂ£o direta de comandos nos 8+11 arquivos
de persistÃƒÂªncia:

- `fencedTransactWrite(client, { tenantId, items })` Ã¢â‚¬â€ adiciona automaticamente, como **primeiro** item da
  lista, um `ConditionCheck` (via `buildExistenceConditionCheck` jÃƒÂ¡ existente) contra
  `TENANT#<tenantId>#LIFECYCLE`/`LIFECYCLE` asserindo `status = "ACTIVE"`. Todo `TransactWriteItems` que
  escreve dado de tenant passa a incluir esse ConditionCheck sem que cada service precise lembrar.
- `fencedSingleWrite(client, { tenantId, command })` Ã¢â‚¬â€ para os `UpdateCommand`/`PutCommand`/`DeleteCommand`
  isolados, reescreve o comando como um `TransactWriteCommand` de 2 itens (a escrita real + o ConditionCheck
  do fence) Ã¢â‚¬â€ DynamoDB nÃƒÂ£o tem `ConditionExpression` cross-item para comando single-item, entÃƒÂ£o a ÃƒÂºnica forma
  atÃƒÂ´mica de compor "escreva X E confirme que o tenant estÃƒÂ¡ ACTIVE" ÃƒÂ© uma transaÃƒÂ§ÃƒÂ£o de 2 itens. Aceitar o
  custo de WCU/latÃƒÂªncia adicional (jÃƒÂ¡ pago hoje pelos outros usos de `TransactWriteItems` no mesmo cÃƒÂ³digo).

**Enforcement estrutural, nÃƒÂ£o convenÃƒÂ§ÃƒÂ£o**: regra nova de `dependency-cruiser` (mesmo mecanismo que jÃƒÂ¡ proÃƒÂ­be
`shared` de importar `modules/**`) proibindo `new TransactWriteCommand(`/`new UpdateCommand(`/`new
PutCommand(`/`new DeleteCommand(` fora de `src/shared/dynamodb/fenced-write.ts` Ã¢â‚¬â€ fecha a classe de erro
"um call site novo esqueceu o fence", que foi exatamente a causa raiz do achado mais grave de D-063
(auditoria incompleta por pasta).

Isso fecha, sem tocar em nenhum dos 36 handlers individualmente: `items-handler`, `documents-handler` (a
persistÃƒÂªncia do `Document`, nÃƒÂ£o a emissÃƒÂ£o da URL presignada Ã¢â‚¬â€ ver Ã‚Â§4), `guest-documents-handler` (via
`DocumentSubmission`), `import-commit-handler` (mesmo com `systemContextFor()` sintÃƒÂ©tico Ã¢â‚¬â€ o fence nÃƒÂ£o
depende de `RequestContext`, ÃƒÂ© checado na escrita, nÃƒÂ£o na autorizaÃƒÂ§ÃƒÂ£o), toda a cadeia de lembretes/outbox,
`notification-router`/`email-delivery` (a gravaÃƒÂ§ÃƒÂ£o da tentativa, nÃƒÂ£o o `ses:SendEmail` em si Ã¢â‚¬â€ ver Ã‚Â§5),
`malware-result`/`upload-finalizer`, `upload-slot-reconciliation`, e as duas tasks de escrita da Step
Functions (`PersistExtractedFields`/`MarkPendingConfirmation`).

## 3. Fechando a ressurreiÃƒÂ§ÃƒÂ£o no resolver (defesa em profundidade, nÃƒÂ£o a defesa primÃƒÂ¡ria)

`RequestContextResolver.resolve()` ganha uma leitura do `TenantLifecycleRecord` **antes** de
`createProfileIfAbsent` (nÃƒÂ£o substitui a checagem de `UserProfile.status`, ÃƒÂ© adicional e anterior). Se
`status !== "ACTIVE"`, rejeita com 403 sem provisionar nada. Isso ÃƒÂ© defesa em profundidade Ã¢â‚¬â€ mesmo que
faltasse, o Ã‚Â§2 jÃƒÂ¡ impediria a escrita real do `User` recriado, porque `createProfileIfAbsent` tambÃƒÂ©m passa
pelo `fencedSingleWrite`. A checagem no resolver existe para dar um erro claro (403) em vez de deixar o
usuÃƒÂ¡rio achar que logou e sÃƒÂ³ descobrir a rejeiÃƒÂ§ÃƒÂ£o no primeiro POST.

## 4. URLs presignadas jÃƒÂ¡ emitidas (S3)

NÃƒÂ£o sÃƒÂ£o revogÃƒÂ¡veis diretamente. MitigaÃƒÂ§ÃƒÂ£o real, nÃƒÂ£o best-effort: a finalizaÃƒÂ§ÃƒÂ£o (`upload-finalizer-handler`/
`malware-result-handler`/equivalente guest) ÃƒÂ© exatamente o ponto que persiste `Document`/`DocumentSubmission`
via `fencedTransactWrite` (Ã‚Â§2) Ã¢â‚¬â€ se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a transaÃƒÂ§ÃƒÂ£o de finalizaÃƒÂ§ÃƒÂ£o falha
(`ConditionalCheckFailedException`), o objeto S3 fica ÃƒÂ³rfÃƒÂ£o na quarantine (limpo depois por
`document-purge-handler`/lifecycle, mecanismo jÃƒÂ¡ aprovado em D-061), mas **nenhuma linha DynamoDB nova
aparece**. Resultado: nenhuma ressurreiÃƒÂ§ÃƒÂ£o de dado consultÃƒÂ¡vel, sÃƒÂ³ lixo de storage jÃƒÂ¡ coberto por mecanismo
existente. TTL curto (600s) jÃƒÂ¡ limita a janela de exposiÃƒÂ§ÃƒÂ£o.

## 5. Efeitos externos irreversÃƒÂ­veis (SES, Bedrock, Textract) Ã¢â‚¬â€ protocolo de duas fases, nÃƒÂ£o checagem simples

Nenhum destes tem `ConditionExpression` atÃƒÂ´mica nativa. Desenho: **a decisÃƒÂ£o de disparar o efeito precisa
nascer de uma escrita fenced**, nÃƒÂ£o de uma leitura solta:

- `email-delivery-handler`: antes de `ses:SendEmail`, marca `NotificationAttempt.status = "SENDING"` via
  `fencedTransactWrite`. Se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a marcaÃƒÂ§ÃƒÂ£o falha e o handler nunca chama SES. Um
  e-mail cuja marcaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ commitou antes do `DELETING` ser escrito ainda sai (janela de milissegundos,
  inerente a qualquer sistema com efeito externo assÃƒÂ­ncrono Ã¢â‚¬â€ documentado como risco residual aceito, nÃƒÂ£o
  escondido).
- `document-chasing-dispatch-handler`: mesmo padrÃƒÂ£o Ã¢â‚¬â€ o novo `GuestTokenPointer` sÃƒÂ³ ÃƒÂ© criado via
  `fencedTransactWrite`; se falhar, nenhum token novo existe, nenhum e-mail ÃƒÂ© enviado.
- Bedrock/Textract (custo, nÃƒÂ£o dado): `BedrockExtractionTaskHandler`/`TextractTaskHandler` jÃƒÂ¡ gravam
  `TenantQuotaService`/`TextractJob` via os stores fenced do Ã‚Â§2 Ã¢â‚¬â€ se o tenant foi excluÃƒÂ­do entre o
  `RunTextract` e o `RunBedrock`, a chamada Bedrock ainda ocorre (custo real, nÃƒÂ£o dado), mas
  `PersistExtractedFields` falha depois. Custo residual pequeno e limitado, nÃƒÂ£o uma garantia de dado quebrada.

## 6. SequÃƒÂªncia da cascata (drenagem antes de apagar)

1. `fencedSingleWrite` grava `TenantLifecycleRecord.status = "DELETING"` (create condicional
   `attribute_not_exists(PK)` Ã¢â€ â€™ falha se uma cascata jÃƒÂ¡ estÃƒÂ¡ em curso, evita corrida de duplo-start).
2. Best-effort: `StopExecution` em toda execuÃƒÂ§ÃƒÂ£o Step Functions ativa cujo payload referencia o tenant
   (via tag/GSI jÃƒÂ¡ existente do desenho de extraÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ reduz custo Bedrock/Textract desperdiÃƒÂ§ado, nÃƒÂ£o ÃƒÂ©
   requisito de correÃƒÂ§ÃƒÂ£o (o fence do Ã‚Â§2/Ã‚Â§5 jÃƒÂ¡ garante correÃƒÂ§ÃƒÂ£o sem isto).
3. Roda a descoberta+exclusÃƒÂ£o jÃƒÂ¡ aprovada (Scan + taxonomia de ~40 `entityType`, convergÃƒÂªncia por re-Scan),
   reusando `DocumentPurgeWorker`/GSI6 para `Document` (D-061) Ã¢â‚¬â€ mecanismo inalterado desta rodada.
4. `TenantLifecycleRecord` **nunca** ÃƒÂ© alvo do Scan (exclusÃƒÂ£o explÃƒÂ­cita por `entityType`, mesma disciplina jÃƒÂ¡
   usada para `bff-session-table`).
5. ConvergÃƒÂªncia confirmada (zero itens restantes) Ã¢â€ â€™ `fencedSingleWrite` flip para `status = "DELETED"`
   (permanente; nenhum cÃƒÂ³digo reabre um tenant `DELETED` Ã¢â‚¬â€ fora de escopo desta feature).

## 7. O que este desenho NÃƒÆ’O resolve (fora de escopo explÃƒÂ­cito, registrar e nÃƒÂ£o mascarar)

- Rotas HTTP/confirmaÃƒÂ§ÃƒÂ£o/exportaÃƒÂ§ÃƒÂ£o de DSR completo Ã¢â‚¬â€ jÃƒÂ¡ fora de escopo desde D-062, inalterado.
- `PolicyRef` sem `tenantId` (achado independente de D-062) Ã¢â‚¬â€ correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica separada.
- `tenantId` (MVP `tenantId=userId`) Ã¢â€°Â  Cognito `sub` Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant
  precisa resolver isso ÃƒÂ  parte; nÃƒÂ£o bloqueia este desenho porque a cascata nunca chama a API do Cognito.

## 8. Por que isto nÃƒÂ£o repete os erros de D-062/D-063

- O fence nÃƒÂ£o ÃƒÂ© a mesma linha que a cascata apaga (Ã‚Â§1, `TenantLifecycleRecord` explicitamente excluÃƒÂ­do do
  Scan) Ã¢â‚¬â€ resolve o achado mais grave de D-063.
- A checagem ÃƒÂ© acoplada (`ConditionCheck` na mesma transaÃƒÂ§ÃƒÂ£o), nunca uma leitura solta antes de agir Ã¢â‚¬â€
  resolve o segundo achado de D-063 (TOCTOU).
- A cobertura nÃƒÂ£o depende de enumerar 36 handlers corretamente Ã¢â‚¬â€ depende de 19 arquivos de persistÃƒÂªncia jÃƒÂ¡
  conhecidos + uma regra de lint estrutural que impede um call site futuro de escapar do fence. Reduz a
  classe de erro "auditoria incompleta" de "confiar em uma lista manual" para "confiar numa regra estÃƒÂ¡tica
  verificÃƒÂ¡vel por `dependency-cruiser`" Ã¢â‚¬â€ resolve a causa raiz comum ÃƒÂ s duas rodadas reprovadas.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/identity/application/resolve-request-context.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 152ms:
/**
 * resolveRequestContext Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§4.1/Ã‚Â§7.2, the central resolver.
 * Steps (verbatim from the blueprint):
 *  1. validation already performed by the API Gateway authorizer (JWT signature/exp) Ã¢â‚¬â€
 *     this function receives already-validated claims, it does not verify JWT signatures;
 *  2. consistent lookup of cognitoSub -> userId (IdentityMapping, atomic first-login create);
 *  3. session/device resolution;
 *  4. tenant/membership resolution;
 *  5. reject tokens issued before globalLogoutAfter/deviceLogoutAfter;
 *  6. build the immutable RequestContext.
 */
import { AuthenticationError } from "../../../shared/errors/app-error.js";
import { IdentityMappingRepository } from "../persistence/identity-mapping-repository.js";
import { UserRepository } from "../persistence/user-repository.js";
import type { RequestContext } from "../domain/request-context.js";

/** Claims already validated (signature + expiry) by the API Gateway JWT authorizer. */
export interface ValidatedClaims {
  sub: string;
  tokenId: string; // jti
  issuedAt: string; // iat, ISO-8601
  expiresAt: string; // exp, ISO-8601
  deviceId?: string;
}

export interface ResolveRequestContextInput {
  claims: ValidatedClaims;
  requestId: string;
  correlationId: string;
}

export interface IdGenerator {
  newUserId(): string;
  newSessionId(): string;
}

export class RequestContextResolver {
  constructor(
    private readonly identityMappings: IdentityMappingRepository,
    private readonly users: UserRepository,
    private readonly ids: IdGenerator,
  ) {}

  async resolve(input: ResolveRequestContextInput): Promise<RequestContext> {
    const { claims } = input;

    // Step 2: cognitoSub -> userId/tenantId. MVP tenantId=userId (data-model.md Ã‚Â§7.3),
    // decided here and nowhere else so a future Organization model changes one call site.
    const newUserId = this.ids.newUserId();
    const mapping = await this.identityMappings.findOrCreate(claims.sub, newUserId, newUserId);

    // Step 3/4: profile + membership. First-login also provisions the User profile
    // (OWNER role, MVP single-tenant-per-user) so resolveContext never returns a
    // context for a mapping with no backing profile.
    let profile = await this.users.getProfile(mapping.tenantId, mapping.userId);
    if (!profile) {
      profile = await this.users.createProfileIfAbsent({
        userId: mapping.userId,
        tenantId: mapping.tenantId,
        identitySubject: claims.sub,
        emailNormalized: "",
        roles: ["OWNER"],
        status: "ACTIVE",
      });
    }

    if (profile.status !== "ACTIVE") {
      throw new AuthenticationError("User is not active.", { userId: profile.userId });
    }

    // Step 5: reject tokens issued before global logout watermark.
    if (profile.globalLogoutAfter && claims.issuedAt < profile.globalLogoutAfter) {
      throw new AuthenticationError("Session revoked (global logout).", { userId: profile.userId });
    }

    let deviceSession;
    if (claims.deviceId) {
      deviceSession = await this.users.getDeviceSession(mapping.tenantId, mapping.userId, claims.deviceId);
      if (deviceSession) {
        if (deviceSession.status === "REVOKED") {
          throw new AuthenticationError("Session revoked (device logout).", { deviceId: claims.deviceId });
        }
        if (deviceSession.deviceLogoutAfter && claims.issuedAt < deviceSession.deviceLogoutAfter) {
          throw new AuthenticationError("Session revoked (device logout).", { deviceId: claims.deviceId });
        }
      }
    }

    const sessionId = deviceSession?.sessionId ?? this.ids.newSessionId();

    return {
      requestId: input.requestId,
      correlationId: input.correlationId,
      principal: {
        userId: profile.userId,
        cognitoSubject: claims.sub,
        sessionId,
        deviceId: claims.deviceId,
      },
      tenant: {
        tenantId: profile.tenantId,
        roles: profile.roles,
      },
      auth: {
        issuedAt: claims.issuedAt,
        expiresAt: claims.expiresAt,
        tokenId: claims.tokenId,
      },
    };
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/shared/dynamodb/occ.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 164ms:
/**
 * Optimistic concurrency control helper - implementation-blueprint.md #5.2 / M0 deliverable.
 *
 * M0 ships no tables/Lambdas yet (that's M1+), so this module only builds the DynamoDB
 * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
 * so it stays testable without AWS credentials/mocks. M1 wires these builders into the
 * actual DocumentClient calls once tables exist.
 */

export interface EntityKey {
  PK: string;
  SK: string;
}

export interface VersionedUpdateInput {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  /** Additional SET clauses beyond version/updatedAt, e.g. { status: "CANCELLED" }. */
  set: Record<string, unknown>;
  /** Attribute names to REMOVE atomically in the same conditional update - e.g. dropping
   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
   * since M3 (occ.ts was SET-only, cancelled GSI3 pointers were left orphaned because
   * nothing actively queried them); M3.5 needs REMOVE for real because GSI6 pointers ARE
   * actively queried by reconciliation. */
  remove?: string[];
  now?: string;
  /** Extra ConditionExpression clauses ANDed to the base condition, each with its own
   * caller-supplied names/values (W3-06 purge worker's claim/fence conditions - e.g. checking
   * `legalHold`/`GSI6PK`/`purgeAfter` atomically alongside the version check). Each entry's
   * `expression` is wrapped in its own parentheses before being ANDed in, so callers never need
   * to hand-balance parens against the base condition. Placeholder names/values MUST NOT collide
   * with the base condition's (#version/#tenantId/#updatedAt/:expectedVersion/:tenantId/:one/:now)
   * or with any `set`/`remove` generated key (#set<i>/:set<i>/#rem<j>) - buildVersionedUpdate
   * throws before building an invalid ConditionExpression rather than silently overwriting one
   * caller's placeholder with another's. */
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}

export interface DynamoUpdateCommandInput {
  TableName: string;
  Key: EntityKey;
  UpdateExpression: string;
  ConditionExpression: string;
  ExpressionAttributeNames: Record<string, string>;
  ExpressionAttributeValues: Record<string, unknown>;
}

/**
 * Builds an UpdateItem input enforcing implementation-blueprint.md #5.2's exact
 * ConditionExpression: attribute_exists(PK) AND attribute_exists(SK) AND
 * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
 */
export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
  const now = input.now ?? new Date().toISOString();

  const names: Record<string, string> = {
    "#version": "version",
    "#tenantId": "tenantId",
    "#updatedAt": "updatedAt",
  };
  const values: Record<string, unknown> = {
    ":expectedVersion": input.expectedVersion,
    ":tenantId": input.tenantId,
    ":one": 1,
    ":now": now,
  };

  const setClauses = ["#version = #version + :one", "#updatedAt = :now"];
  let i = 0;
  for (const [field, value] of Object.entries(input.set)) {
    const nameKey = `#set${i}`;
    const valueKey = `:set${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    setClauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  const removeClauses: string[] = [];
  let j = 0;
  for (const field of input.remove ?? []) {
    const nameKey = `#rem${j}`;
    names[nameKey] = field;
    removeClauses.push(nameKey);
    j += 1;
  }

  const expression =
    removeClauses.length > 0
      ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}`
      : `SET ${setClauses.join(", ")}`;

  const baseConditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];
  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved/generated key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved/generated key: ${valueKey}`);
      values[valueKey] = value;
    }
    baseConditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: expression,
    ConditionExpression: baseConditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export interface DynamoPutCommandInput {
  TableName: string;
  Item: Record<string, unknown>;
  ConditionExpression: string;
}

/**
 * Builds a PutItem input for first-time creation of a versioned entity, per
 * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
 * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
 */
export function buildVersionedCreate(tableName: string, item: Record<string, unknown> & EntityKey): DynamoPutCommandInput {
  return {
    TableName: tableName,
    Item: item,
    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
  };
}

/**
 * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
 * (2026-08-19, Engineering Maturity Review): that was the only place they were declared,
 * but reminder/ports/reminder-store.ts already re-exported them from there (a cross-module
 * ports->ports dependency), and expiration/domain/audit-event.ts imported them directly,
 * which is a domain->ports boundary violation dependency-cruiser catches but ESLint's
 * text-matching no-restricted-imports rule cannot (it only matches literal specifier
 * substrings, and this codebase's relative imports never contain "modules/"). Reuses the
 * inner Put/Update shapes already defined above instead of re-declaring them a third time.
 */
export interface TransactPutEntry {
  Put: DynamoPutCommandInput;
}

export interface TransactUpdateEntry {
  Update: DynamoUpdateCommandInput;
}

export interface DynamoConditionCheckCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactConditionCheckEntry {
  ConditionCheck: DynamoConditionCheckCommandInput;
}

export interface DynamoDeleteCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression?: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactDeleteEntry {
  Delete: DynamoDeleteCommandInput;
}

/**
 * Builds a version-conditioned Delete input (W3-06 purge worker's terminal step - a Document
 * row can only be physically removed if it is still at the exact version/state the claim
 * observed, same OCC discipline as `buildVersionedUpdate`, just for `DeleteItem` instead of
 * `UpdateItem`). `extraConditions` uses the same collision-checked merge as
 * `buildVersionedUpdate` - see that function's docs.
 */
export function buildVersionedDelete(input: {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}): DynamoDeleteCommandInput {
  const names: Record<string, string> = { "#version": "version", "#tenantId": "tenantId" };
  const values: Record<string, unknown> = { ":expectedVersion": input.expectedVersion, ":tenantId": input.tenantId };
  const conditionParts = ["attribute_exists(PK)", "attribute_exists(SK)", "#version = :expectedVersion", "#tenantId = :tenantId"];

  for (const extra of input.extraConditions ?? []) {
    for (const [nameKey, name] of Object.entries(extra.names ?? {})) {
      if (nameKey in names) throw new Error(`extraConditions name placeholder collides with a reserved key: ${nameKey}`);
      names[nameKey] = name;
    }
    for (const [valueKey, value] of Object.entries(extra.values ?? {})) {
      if (valueKey in values) throw new Error(`extraConditions value placeholder collides with a reserved key: ${valueKey}`);
      values[valueKey] = value;
    }
    conditionParts.push(`(${extra.expression})`);
  }

  return {
    TableName: input.tableName,
    Key: input.key,
    ConditionExpression: conditionParts.join(" AND "),
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;

/**
 * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
 * expected version (and, optionally, still holds a given attribute value) - used to fence
 * a commit against a fact read earlier in the same operation but not itself written by
 * this transaction (e.g. reminder dispatch asserting the ReminderPolicy it read is still
 * enabled at the version it read, per the BLOCKER-B freshness-fencing fix: reading a row
 * and later writing elsewhere based on that read is a TOCTOU gap unless the read fact is
 * re-asserted atomically inside the same TransactWriteItems as the write it gates).
 */
export function buildVersionConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  expectedVersion: number;
  /** Extra attribute=value equality conditions beyond version, e.g. { enabled: true, status: "ACTIVE" }. */
  extra?: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = { "#version": "version" };
  const values: Record<string, unknown> = { ":version": input.expectedVersion };
  const clauses = ["#version = :version"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra ?? {})) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/**
 * Sibling of `buildVersionConditionCheck` for facts that have no version to pin - e.g.
 * BLOCKER-B's ITEM-scoped-policy integrity check (a policy references an item's
 * existence/status/tenant, not a specific item version - referencing an item doesn't pin
 * it to a version the way an occurrence's materialization does). `extra` must include at
 * least one condition (an existence-only check with no `extra` would degenerate to
 * `attribute_exists(PK)`, which every real row satisfies trivially - not useful on its own).
 */
export function buildExistenceConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  extra: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = {};
  const values: Record<string, unknown> = {};
  const clauses = ["attribute_exists(PK)"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra)) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
 * ConditionExpression - used by callers to distinguish OCC/idempotency conflicts from other
 * DynamoDB errors without importing the SDK type. */
export const TRANSACTION_CANCELED = "TransactionCanceledException";

export function isTransactionCanceled(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === TRANSACTION_CANCELED
  );
}

/** Name of the AWS SDK error thrown when a ConditionExpression fails - used by callers
 * to distinguish OCC conflicts from other DynamoDB errors without importing the SDK type. */
export const CONDITIONAL_CHECK_FAILED = "ConditionalCheckFailedException";

export function isConditionalCheckFailed(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === CONDITIONAL_CHECK_FAILED
  );
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 144ms:
# W3-07 Ã¢â‚¬â€ Status final: REPROVADO, NÃƒÆ’O IMPLEMENTADO (2026-08-28)

> D-062 em `docs/architecture/decisions-log.md`. Este documento resume o resultado das 4
> rodadas para quem retomar este trabalho no futuro Ã¢â‚¬â€ nÃƒÂ£o reabrir o protocolo do zero sem ler
> isto primeiro.

## Resultado

4 rodadas ClaudeÃ¢â€ â€Codex, notas **3,4 Ã¢â€ â€™ 5,1 Ã¢â€ â€™ 4,7 / 10** Ã¢â‚¬â€ nunca atingiu o gate de 9,0. Reprovado
por decisÃƒÂ£o do Marcelo, seguindo recomendaÃƒÂ§ÃƒÂ£o convergente e independente de Claude e Codex
(pedida explicitamente apÃƒÂ³s a Rodada 3): **nÃƒÂ£o implementar agora**, registrar os achados como
pesquisa.

## Por que nÃƒÂ£o convergiu (o achado central)

Toda tentativa de fechar "COMPLETED" com garantia real de nÃƒÂ£o-ressurreiÃƒÂ§ÃƒÂ£o (um dado do tenant
sendo recriado depois de uma exclusÃƒÂ£o jÃƒÂ¡ declarada concluÃƒÂ­da) esbarrou na mesma parede: o
sistema tem **trÃƒÂªs classes de escritor** que uma prÃƒÂ©-condiÃƒÂ§ÃƒÂ£o simples (ex.: desativar login no
Cognito) nÃƒÂ£o fecha:

1. HTTP autenticado (fechÃƒÂ¡vel via Cognito).
2. **SuperfÃƒÂ­cie pÃƒÂºblica de guest upload** (`GuestSubmissionService` Ã¢â‚¬â€ nunca passa por
   `RequestContext`/Cognito, autentica sÃƒÂ³ por token opaco).
3. **Workers assÃƒÂ­ncronos** jÃƒÂ¡ enfileirados ou agendados independentemente de qualquer sessÃƒÂ£o de
   usuÃƒÂ¡rio (`reminder-materialization-trigger`, `reminder-reconciliation`,
   `document-chasing-dispatch`, outbox relay/sweeper, callback SES, reconciliaÃƒÂ§ÃƒÂ£o de
   upload/malware, import, extraÃƒÂ§ÃƒÂ£o/OCR via Step Functions).

Fechar as trÃƒÂªs de verdade exige um fence de `TenantStatus` consultado por **todo** esses
caminhos Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© mais "o mecanismo de descoberta+exclusÃƒÂ£o", ÃƒÂ© a mesma feature de "bloqueio
imediato" que a decisÃƒÂ£o de escopo original desta sessÃƒÂ£o jÃƒÂ¡ tinha deixado de fora
deliberadamente. As duas decisÃƒÂµes de escopo (mecanismo pequeno vs. sem bloqueio imediato) sÃƒÂ£o
mutuamente incompatÃƒÂ­veis com uma garantia forte Ã¢â‚¬â€ nÃƒÂ£o dÃƒÂ¡ para ter as duas.

## Por que nÃƒÂ£o foi aceito um "best-effort documentado" como meio-termo

RecomendaÃƒÂ§ÃƒÂ£o explÃƒÂ­cita do Codex, aceita: implementar um mecanismo que alega apagar
fisicamente mas nÃƒÂ£o sustenta essa garantia ÃƒÂ© uma capacidade **pior que nÃƒÂ£o ter nenhuma** Ã¢â‚¬â€ pode
ser lido operacionalmente como "DSR atendido" mesmo com uma ressalva no `decisions-log.md`, e
ainda geraria cÃƒÂ³digo que precisaria ser redesenhado quando a garantia real for exigida de
verdade.

## O que sobrevive desta pesquisa (reusar, nÃƒÂ£o redescobrir)

- **InventÃƒÂ¡rio verificado dos 40 `entityType` reais** do sistema (`src/modules/**`), com
  classificaÃƒÂ§ÃƒÂ£o por presenÃƒÂ§a de `version`/`tenantId` Ã¢â‚¬â€ ver Rodada 3/4
  (`claude-proposal-round3.md`/`round4.md`) para a tabela completa e as correÃƒÂ§ÃƒÂµes feitas em
  cima da prÃƒÂ³pria Rodada 2 (que tambÃƒÂ©m errou o inventÃƒÂ¡rio na primeira tentativa Ã¢â‚¬â€ dois erros
  reais de levantamento em rodadas diferentes, liÃƒÂ§ÃƒÂ£o: sempre grep exaustivo, nunca amostra).
- **Bug real descoberto, nÃƒÂ£o corrigido ainda**: `tenantId` (MVP `tenantId=userId`) nÃƒÂ£o ÃƒÂ© o
  Cognito `sub`/`Username` Ã¢â‚¬â€ `IdentityMapping.cognitoSub`/`User.identitySubject` sÃƒÂ£o os campos
  certos. Qualquer aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant precisa resolver isso
  primeiro.
- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
  `bff-session-table`, uma tabela DynamoDB fÃƒÂ­sica separada Ã¢â‚¬â€ nenhum mecanismo de descoberta
  tenant-wide pode assumir uma ÃƒÂºnica tabela.
- **CorreÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica pendente, independente**: `PolicyRef` (`reminder-policy.ts:69-72`) nÃƒÂ£o
  carrega `tenantId` Ã¢â‚¬â€ pequena, real, trivial de corrigir numa sessÃƒÂ£o futura sem depender de
  W3-07.
- **Reuso jÃƒÂ¡ decidido para quando retomar**: `Document` continua a ÃƒÂºnica categoria com efeito
  colateral S3 real Ã¢â‚¬â€ reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061), nunca duplicar.

## Gatilho para retomar

Requisito real de DSR (idealmente decidido antes do primeiro cliente B2B que dependa disso).
Quando retomado, o fence de `TenantStatus` em todo write path (HTTP + guest + workers) deve
entrar no escopo do **primeiro** desenho, nÃƒÂ£o ser descoberto de novo rodada a rodada como
aconteceu aqui.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 182ms:
# W3-07 (retomada, D-063) Ã¢â‚¬â€ status: PAUSADO apÃƒÂ³s Rodada 1, para planejamento minucioso na prÃƒÂ³xima sessÃƒÂ£o

> DecisÃƒÂ£o do Marcelo (2026-08-28): nÃƒÂ£o adiar por falta de usuÃƒÂ¡rio real hoje Ã¢â‚¬â€ construir a
> garantia completa de exclusÃƒÂ£o de tenant algum dia, mas com anÃƒÂ¡lise minuciosa antes de
> implementar. Esta pasta ÃƒÂ© a continuaÃƒÂ§ÃƒÂ£o de
> `docs/architecture/reviews/w3-07-tenant-cascade-deletion-design/` (D-062, reprovada em 4
> rodadas) Ã¢â‚¬â€ leia aquela primeiro (especialmente `claude-final-status-not-approved.md`) antes
> desta.

## O que esta rodada tentou

Reabrir o desenho com o fence de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" dentro do escopo desde a Rodada 1 (em vez
de descoberto reativamente, como na tentativa D-062). Achado de partida:
`User.status: "ACTIVE" | "SUSPENDED"` (`user-repository.ts:22`) jÃƒÂ¡ existe e jÃƒÂ¡ ÃƒÂ© checado em toda
resoluÃƒÂ§ÃƒÂ£o de contexto HTTP autenticada (`resolve-request-context.ts:67-69`), mas `"SUSPENDED"`
nunca ÃƒÂ© escrito por nenhum cÃƒÂ³digo hoje. A proposta usava isso como fence primÃƒÂ¡rio (zero cÃƒÂ³digo
novo para a superfÃƒÂ­cie HTTP autenticada) + checagem nova no fluxo de convidado + auditoria dos
workers em `src/workers/`.

## Resultado: Rodada 1, nota 3,2/10, REPROVADA Ã¢â‚¬â€ pior que a rodada equivalente da tentativa anterior

Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
**precisa** resolver desde o desenho inicial, nÃƒÂ£o descobrir depois:

### Achado mais grave Ã¢â‚¬â€ o prÃƒÂ³prio fence ÃƒÂ© apagÃƒÂ¡vel pela cascata que ele deveria proteger

`RequestContextResolver.resolve()` (`resolve-request-context.ts:49-65`) provisiona
automaticamente um `User` novo com `status: "ACTIVE"` no primeiro login de um `cognitoSub`
sem perfil existente. Se a cascata apaga o `User` (ele ÃƒÂ© sÃƒÂ³ mais uma linha do tenant, como
qualquer outra), um token ainda vÃƒÂ¡lido reautentica e o sistema **recria o tenant sozinho** Ã¢â‚¬â€
a prÃƒÂ³pria lÃƒÂ³gica de "provisionar no primeiro login" resssucita o que acabou de ser apagado.
**ConsequÃƒÂªncia de design**: o tombstone/fence do tenant nÃƒÂ£o pode ser a mesma linha que a
cascata elimina Ã¢â‚¬â€ precisa ser um registro que sobrevive ÃƒÂ  exclusÃƒÂ£o completa (ex.: um registro
de "tenant encerrado" fora do universo apagÃƒÂ¡vel, no mesmo espÃƒÂ­rito do `TenantDeletionRequest`
jÃƒÂ¡ proposto em D-062, mas agora como fonte de verdade que o RESOLVER tambÃƒÂ©m consulta, nÃƒÂ£o sÃƒÂ³
um tombstone passivo).

### Checagens desacopladas (leitura solta antes de agir) tÃƒÂªm corrida TOCTOU real

Qualquer "leia `User.status`, depois aja" (proposto para `GuestSubmissionService` e para os
workers) tem uma janela real entre a leitura e o efeito. Para efeitos DynamoDB, o fence precisa
participar da MESMA `TransactWriteItems` da mutaÃƒÂ§ÃƒÂ£o (`ConditionCheck`), nÃƒÂ£o uma leitura solta
antes. Para efeitos externos (SES, S3, SQS, Step Functions) nÃƒÂ£o existe uma condiÃƒÂ§ÃƒÂ£o atÃƒÂ´mica
equivalente Ã¢â‚¬â€ precisa de um protocolo de claim/drenagem dedicado, nÃƒÂ£o uma checagem simples.

### Auditoria de superfÃƒÂ­cies de escrita estava errada e incompleta

Confirmado incorreto (releem entidade de origem, mas ainda produzem efeito novo sem fence real):
- `dispatch-outbox-relay` Ã¢â‚¬â€ produz mensagem SQS nova antes de marcar o outbox como publicado.
- `upload-slot-reconciliation` Ã¢â‚¬â€ escreve `Document.status = TIMEOUT` sem reler fence de tenant.
- `document-chasing-dispatch` Ã¢â‚¬â€ pode criar `GuestTokenPointer` novo e mandar e-mail via SES
  depois de o tenant jÃƒÂ¡ estar suspenso.

SuperfÃƒÂ­cies inteiras que a auditoria por pasta (`src/workers/`) nÃƒÂ£o capturou:
- `import-commit-handler` Ã¢â‚¬â€ fabrica um `RequestContext` de sistema com papel `OWNER`, nunca
  resolve usuÃƒÂ¡rio nem checa status.
- `start-extraction-run`/Step Functions do mÃƒÂ³dulo `extraction` Ã¢â‚¬â€ inicia execuÃƒÂ§ÃƒÂ£o sem fence,
  tarefas subsequentes continuam escrevendo artefato S3/estado depois da suspensÃƒÂ£o.
- `ses-callback-workflow` Ã¢â‚¬â€ cria/atualiza `WebhookInbox`/`NotificationAttempt`/
  `NotificationPreferences` sem checar fence.
- URLs presignadas de S3 jÃƒÂ¡ entregues a um convidado continuam vÃƒÂ¡lidas mesmo apÃƒÂ³s o fence Ã¢â‚¬â€
  um upload feito depois ainda dispara toda a cadeia de finalizaÃƒÂ§ÃƒÂ£o/malware/extraÃƒÂ§ÃƒÂ£o.

**LiÃƒÂ§ÃƒÂ£o estrutural para a prÃƒÂ³xima tentativa**: organizar o levantamento por **ponto de entrada
de runtime** (API Gateway, fila SQS, DynamoDB Streams, evento S3/EventBridge, EventBridge
Scheduler, callback SNS/SES, Step Functions), nunca por pasta de cÃƒÂ³digo (`src/workers/`) Ã¢â‚¬â€
essa organizaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ produziu falsos negativos duas vezes (D-062 e esta rodada).

## O que jÃƒÂ¡ estÃƒÂ¡ validado e deve ser reaproveitado, nÃƒÂ£o redescoberto

- InventÃƒÂ¡rio verificado dos ~40 `entityType` reais do sistema, por presenÃƒÂ§a de `version`/
  `tenantId` (D-062, Rodadas 3-4).
- Mecanismo de descoberta+exclusÃƒÂ£o (Scan + taxonomia de 3 categorias + convergÃƒÂªncia por
  re-Scan) Ã¢â‚¬â€ a parte de "como apagar o que jÃƒÂ¡ nÃƒÂ£o tem mais escritor" continua vÃƒÂ¡lida; o que
  falta ÃƒÂ© sÃƒÂ³ a garantia de "nÃƒÂ£o ressurreiÃƒÂ§ÃƒÂ£o" antes de declarar concluÃƒÂ­do.
- `Document` deve reusar o `DocumentPurgeWorker`/GSI6 jÃƒÂ¡ aprovado (D-061) Ã¢â‚¬â€ nunca duplicar.
- `Session`/`LoginAttempt`/`DeviceSession` vivem em `bff-session-table`, tabela fÃƒÂ­sica separada.
- Usar `User.status` (ou um registro de tenant equivalente) como sinal central ÃƒÂ© uma boa
  direÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€ sÃƒÂ³ nÃƒÂ£o pode ser o prÃƒÂ³prio dado que a cascata apaga, e nÃƒÂ£o pode ser uma leitura
  desacoplada da escrita protegida.

## PrÃƒÂ³xima sessÃƒÂ£o Ã¢â‚¬â€ como retomar com rigor

1. Ler esta pasta inteira + `w3-07-tenant-cascade-deletion-design/` (D-062) antes de propor
   qualquer coisa nova Ã¢â‚¬â€ nÃƒÂ£o repetir o levantamento do zero.
2. Levantar TODOS os pontos de entrada de runtime reais (nÃƒÂ£o sÃƒÂ³ `src/workers/`) que podem
   escrever dado de um tenant: grep por `SQSEvent`, `handler(): Promise<void>` (schedule),
   `S3Event`/`EventBridgeEvent`, Step Functions (`infra/state-machines/*.asl.json`), callbacks
   SNS/SES Ã¢â‚¬â€ e para cada um, confirmar se e como ele valida que o tenant/recurso de origem
   ainda ÃƒÂ© vÃƒÂ¡lido.
3. Desenhar o tombstone do tenant como um registro que **sobrevive** ÃƒÂ  prÃƒÂ³pria cascata e que
   `RequestContextResolver`/toda superfÃƒÂ­cie de entrada consulta ANTES de qualquer
   provisionamento automÃƒÂ¡tico (fechando o achado mais grave desta rodada).
4. Decidir, para cada superfÃƒÂ­cie de escrita, se o fence entra via `ConditionCheck` na mesma
   transaÃƒÂ§ÃƒÂ£o (caminhos DynamoDB) ou via um protocolo de claim/drenagem dedicado (efeitos
   externos: SES, S3, Step Functions, filas).
5. SÃƒÂ³ depois disso desenhar novamente a descoberta+exclusÃƒÂ£o em si (que jÃƒÂ¡ estÃƒÂ¡ bem
   encaminhada) Ã¢â‚¬â€ nÃƒÂ£o ÃƒÂ© o gargalo, o fence ÃƒÂ©.

## Registro de decisÃƒÂ£o

D-063 em `docs/architecture/decisions-log.md`: reprovada na Rodada 1 (3,2/10), pausada por
pedido explÃƒÂ­cito do Marcelo para planejamento minucioso numa sessÃƒÂ£o dedicada futura, nÃƒÂ£o uma
decisÃƒÂ£o de abandonar o objetivo.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round1.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 146ms:
# W3-07 Ã¢â‚¬â€ Tenant Deletion Fence, Proposta Round 1 (retomada pÃƒÂ³s-D-063)

> Contexto obrigatÃƒÂ³rio jÃƒÂ¡ lido: `w3-07-tenant-cascade-deletion-design/claude-final-status-not-approved.md` (D-062)
> e `w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (D-063). Este documento
> assume os achados de ambos como verdadeiros e nÃƒÂ£o os redescobre.

## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃƒÂ£o por pasta)

Auditoria completa dos 36 handlers reais em `src/runtime/aws/handlers/` contra o Terraform (gatilho declarado,
nÃƒÂ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:

1. **NÃƒÂ£o existe hoje nenhum cÃƒÂ³digo de cascata/fence** Ã¢â‚¬â€ greenfield real, nenhuma verificaÃƒÂ§ÃƒÂ£o de "tenant
   excluÃƒÂ­do" em lugar nenhum.
2. `RequestContextResolver.resolve()` ÃƒÂ© o ÃƒÂºnico ponto do sistema que checa qualquer status hoje
   (`UserProfile.status !== "ACTIVE"`), e sÃƒÂ³ ÃƒÂ© alcanÃƒÂ§ado por rotas HTTP autenticadas. A checagem ÃƒÂ©
   **desacoplada** (lida uma vez no inÃƒÂ­cio do handler, fora de qualquer transaÃƒÂ§ÃƒÂ£o).
3. `GuestSubmissionService.resolveToken()` **nunca** consulta tenant/usuÃƒÂ¡rio Ã¢â‚¬â€ sÃƒÂ³ token/`DocumentRequest`
   (comentÃƒÂ¡rio explÃƒÂ­cito no cÃƒÂ³digo confirmando que ÃƒÂ© deliberado). Sem fence hoje.
4. `startSubmission()`/`handleReserveUpload`/`handleReserveImport` emitem **URLs S3 presignadas** (PUT) que o
   S3 aceita sem passar por nenhum handler nosso atÃƒÂ© o evento "Object Created" disparar a finalizaÃƒÂ§ÃƒÂ£o Ã¢â‚¬â€
   minutos depois, com `tenantId` derivado da **key S3 jÃƒÂ¡ gravada**, nÃƒÂ£o de contexto revalidado.
5. Toda a cadeia `import-parse Ã¢â€ â€™ import-commit`, `reminder-producer Ã¢â€ â€™ reminder-dispatch Ã¢â€ â€™
   reminder-materialization-trigger`, `dispatch-outbox-relay`/`outbox-sweeper`, `notification-router Ã¢â€ â€™
   email-delivery Ã¢â€ â€™ ses-callback`, `document-chasing-dispatch`, `malware-result`/`upload-finalizer`,
   `upload-slot-reconciliation` Ã¢â‚¬â€ **nenhuma** checa tenant hoje; `tenantId` vem sempre de um payload jÃƒÂ¡
   enfileirado ou de uma key S3 jÃƒÂ¡ materializada antes.
6. A Step Functions `document-extraction` (`extraction-starter Ã¢â€ â€™ RunTextract Ã¢â€ â€™ RunDeterministicParser Ã¢â€ â€™
   [RunBedrock] Ã¢â€ â€™ ValidateSchema/CompareExtractors Ã¢â€ â€™ PersistExtractedFields/MarkPendingConfirmation Ã¢â€ â€™
   CompleteRun`) propaga `tenantId` pelo payload de state para state, nunca revalida. As escritas reais
   (`PersistExtractedFields`/`MarkPendingConfirmation`) sÃƒÂ³ acontecem nos dois ÃƒÂºltimos states Ã¢â‚¬â€ minutos depois
   do inÃƒÂ­cio, depois de uma chamada Bedrock paga real e de um `waitForTaskToken` do Textract (atÃƒÂ© 600s).
7. **Achado que muda o desenho**: apesar da superfÃƒÂ­cie parecer enorme (36 handlers, 3 classes de gatilho,
   Step Functions incluÃƒÂ­da), praticamente toda escrita de dado de tenant termina em um de dois primitivos
   jÃƒÂ¡ existentes e centralizados:
   - `TransactWriteCommand` (DynamoDB), construÃƒÂ­do em **8 arquivos de persistÃƒÂªncia**
     (`dynamodb-{extracted-field,document,reminder,subject,import,expiration,reminder-producer,notification}-store.ts`),
     usando os builders de `src/shared/dynamodb/occ.ts` (`buildVersionedUpdate`/`buildVersionedCreate`/
     `buildVersionConditionCheck`/`buildExistenceConditionCheck`) Ã¢â‚¬â€ regra jÃƒÂ¡ normativa em `AGENTS.md` Ã‚Â§7.
   - `UpdateCommand`/`PutCommand`/`DeleteCommand` single-item, em **11 arquivos de persistÃƒÂªncia** (mesma lista
     + `dynamodb-extraction-run-store`, `dynamodb-textract-job-store`, `dynamodb-session-store` [tabela
     separada], `dynamodb-outbox-relay-store`, `dynamodb-identity-store`).
   Isso ÃƒÂ© uma superfÃƒÂ­cie **enumerÃƒÂ¡vel e jÃƒÂ¡ disciplinada**, nÃƒÂ£o 36 pontos ad-hoc Ã¢â‚¬â€ o oposto do que quebrou
   D-062/D-063.

## 1. O tombstone (sobrevive ÃƒÂ  cascata)

Entidade nova `TenantLifecycleRecord`, **fora do universo apagÃƒÂ¡vel pela cascata** (a rotina de
descoberta+exclusÃƒÂ£o por Scan de D-062/D-063 exclui explicitamente este `entityType` Ã¢â‚¬â€ mesma disciplina jÃƒÂ¡
usada para excluir a prÃƒÂ³pria tabela `bff-session-table` do Scan).

```
PK = "TENANT#<tenantId>#LIFECYCLE"
SK = "LIFECYCLE"
entityType = "TenantLifecycleRecord"
status = "ACTIVE" | "DELETING" | "DELETED"
deletionStartedAt / deletionCompletedAt
version   (OCC padrÃƒÂ£o)
```

Vive na tabela principal (nÃƒÂ£o numa tabela separada Ã¢â‚¬â€ precisa ser lido pelo mesmo `TransactWriteItems` que
grava dado de tenant, e `TransactWriteItems` nÃƒÂ£o atravessa tabelas). Nasce `ACTIVE` no primeiro
`createProfileIfAbsent` (mesma transaÃƒÂ§ÃƒÂ£o, custo zero de coordenaÃƒÂ§ÃƒÂ£o adicional).

## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃƒÂ£o handler por handler)

Dois wrappers novos em `src/shared/dynamodb/`, substituindo a construÃƒÂ§ÃƒÂ£o direta de comandos nos 8+11 arquivos
de persistÃƒÂªncia:

- `fencedTransactWrite(client, { tenantId, items })` Ã¢â‚¬â€ adiciona automaticamente, como **primeiro** item da
  lista, um `ConditionCheck` (via `buildExistenceConditionCheck` jÃƒÂ¡ existente) contra
  `TENANT#<tenantId>#LIFECYCLE`/`LIFECYCLE` asserindo `status = "ACTIVE"`. Todo `TransactWriteItems` que
  escreve dado de tenant passa a incluir esse ConditionCheck sem que cada service precise lembrar.
- `fencedSingleWrite(client, { tenantId, command })` Ã¢â‚¬â€ para os `UpdateCommand`/`PutCommand`/`DeleteCommand`
  isolados, reescreve o comando como um `TransactWriteCommand` de 2 itens (a escrita real + o ConditionCheck
  do fence) Ã¢â‚¬â€ DynamoDB nÃƒÂ£o tem `ConditionExpression` cross-item para comando single-item, entÃƒÂ£o a ÃƒÂºnica forma
  atÃƒÂ´mica de compor "escreva X E confirme que o tenant estÃƒÂ¡ ACTIVE" ÃƒÂ© uma transaÃƒÂ§ÃƒÂ£o de 2 itens. Aceitar o
  custo de WCU/latÃƒÂªncia adicional (jÃƒÂ¡ pago hoje pelos outros usos de `TransactWriteItems` no mesmo cÃƒÂ³digo).

**Enforcement estrutural, nÃƒÂ£o convenÃƒÂ§ÃƒÂ£o**: regra nova de `dependency-cruiser` (mesmo mecanismo que jÃƒÂ¡ proÃƒÂ­be
`shared` de importar `modules/**`) proibindo `new TransactWriteCommand(`/`new UpdateCommand(`/`new
PutCommand(`/`new DeleteCommand(` fora de `src/shared/dynamodb/fenced-write.ts` Ã¢â‚¬â€ fecha a classe de erro
"um call site novo esqueceu o fence", que foi exatamente a causa raiz do achado mais grave de D-063
(auditoria incompleta por pasta).

Isso fecha, sem tocar em nenhum dos 36 handlers individualmente: `items-handler`, `documents-handler` (a
persistÃƒÂªncia do `Document`, nÃƒÂ£o a emissÃƒÂ£o da URL presignada Ã¢â‚¬â€ ver Ã‚Â§4), `guest-documents-handler` (via
`DocumentSubmission`), `import-commit-handler` (mesmo com `systemContextFor()` sintÃƒÂ©tico Ã¢â‚¬â€ o fence nÃƒÂ£o
depende de `RequestContext`, ÃƒÂ© checado na escrita, nÃƒÂ£o na autorizaÃƒÂ§ÃƒÂ£o), toda a cadeia de lembretes/outbox,
`notification-router`/`email-delivery` (a gravaÃƒÂ§ÃƒÂ£o da tentativa, nÃƒÂ£o o `ses:SendEmail` em si Ã¢â‚¬â€ ver Ã‚Â§5),
`malware-result`/`upload-finalizer`, `upload-slot-reconciliation`, e as duas tasks de escrita da Step
Functions (`PersistExtractedFields`/`MarkPendingConfirmation`).

## 3. Fechando a ressurreiÃƒÂ§ÃƒÂ£o no resolver (defesa em profundidade, nÃƒÂ£o a defesa primÃƒÂ¡ria)

`RequestContextResolver.resolve()` ganha uma leitura do `TenantLifecycleRecord` **antes** de
`createProfileIfAbsent` (nÃƒÂ£o substitui a checagem de `UserProfile.status`, ÃƒÂ© adicional e anterior). Se
`status !== "ACTIVE"`, rejeita com 403 sem provisionar nada. Isso ÃƒÂ© defesa em profundidade Ã¢â‚¬â€ mesmo que
faltasse, o Ã‚Â§2 jÃƒÂ¡ impediria a escrita real do `User` recriado, porque `createProfileIfAbsent` tambÃƒÂ©m passa
pelo `fencedSingleWrite`. A checagem no resolver existe para dar um erro claro (403) em vez de deixar o
usuÃƒÂ¡rio achar que logou e sÃƒÂ³ descobrir a rejeiÃƒÂ§ÃƒÂ£o no primeiro POST.

## 4. URLs presignadas jÃƒÂ¡ emitidas (S3)

NÃƒÂ£o sÃƒÂ£o revogÃƒÂ¡veis diretamente. MitigaÃƒÂ§ÃƒÂ£o real, nÃƒÂ£o best-effort: a finalizaÃƒÂ§ÃƒÂ£o (`upload-finalizer-handler`/
`malware-result-handler`/equivalente guest) ÃƒÂ© exatamente o ponto que persiste `Document`/`DocumentSubmission`
via `fencedTransactWrite` (Ã‚Â§2) Ã¢â‚¬â€ se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a transaÃƒÂ§ÃƒÂ£o de finalizaÃƒÂ§ÃƒÂ£o falha
(`ConditionalCheckFailedException`), o objeto S3 fica ÃƒÂ³rfÃƒÂ£o na quarantine (limpo depois por
`document-purge-handler`/lifecycle, mecanismo jÃƒÂ¡ aprovado em D-061), mas **nenhuma linha DynamoDB nova
aparece**. Resultado: nenhuma ressurreiÃƒÂ§ÃƒÂ£o de dado consultÃƒÂ¡vel, sÃƒÂ³ lixo de storage jÃƒÂ¡ coberto por mecanismo
existente. TTL curto (600s) jÃƒÂ¡ limita a janela de exposiÃƒÂ§ÃƒÂ£o.

## 5. Efeitos externos irreversÃƒÂ­veis (SES, Bedrock, Textract) Ã¢â‚¬â€ protocolo de duas fases, nÃƒÂ£o checagem simples

Nenhum destes tem `ConditionExpression` atÃƒÂ´mica nativa. Desenho: **a decisÃƒÂ£o de disparar o efeito precisa
nascer de uma escrita fenced**, nÃƒÂ£o de uma leitura solta:

- `email-delivery-handler`: antes de `ses:SendEmail`, marca `NotificationAttempt.status = "SENDING"` via
  `fencedTransactWrite`. Se o tenant jÃƒÂ¡ estÃƒÂ¡ `DELETING`, a marcaÃƒÂ§ÃƒÂ£o falha e o handler nunca chama SES. Um
  e-mail cuja marcaÃƒÂ§ÃƒÂ£o jÃƒÂ¡ commitou antes do `DELETING` ser escrito ainda sai (janela de milissegundos,
  inerente a qualquer sistema com efeito externo assÃƒÂ­ncrono Ã¢â‚¬â€ documentado como risco residual aceito, nÃƒÂ£o
  escondido).
- `document-chasing-dispatch-handler`: mesmo padrÃƒÂ£o Ã¢â‚¬â€ o novo `GuestTokenPointer` sÃƒÂ³ ÃƒÂ© criado via
  `fencedTransactWrite`; se falhar, nenhum token novo existe, nenhum e-mail ÃƒÂ© enviado.
- Bedrock/Textract (custo, nÃƒÂ£o dado): `BedrockExtractionTaskHandler`/`TextractTaskHandler` jÃƒÂ¡ gravam
  `TenantQuotaService`/`TextractJob` via os stores fenced do Ã‚Â§2 Ã¢â‚¬â€ se o tenant foi excluÃƒÂ­do entre o
  `RunTextract` e o `RunBedrock`, a chamada Bedrock ainda ocorre (custo real, nÃƒÂ£o dado), mas
  `PersistExtractedFields` falha depois. Custo residual pequeno e limitado, nÃƒÂ£o uma garantia de dado quebrada.

## 6. SequÃƒÂªncia da cascata (drenagem antes de apagar)

1. `fencedSingleWrite` grava `TenantLifecycleRecord.status = "DELETING"` (create condicional
   `attribute_not_exists(PK)` Ã¢â€ â€™ falha se uma cascata jÃƒÂ¡ estÃƒÂ¡ em curso, evita corrida de duplo-start).
2. Best-effort: `StopExecution` em toda execuÃƒÂ§ÃƒÂ£o Step Functions ativa cujo payload referencia o tenant
   (via tag/GSI jÃƒÂ¡ existente do desenho de extraÃƒÂ§ÃƒÂ£o) Ã¢â‚¬â€ reduz custo Bedrock/Textract desperdiÃƒÂ§ado, nÃƒÂ£o ÃƒÂ©
   requisito de correÃƒÂ§ÃƒÂ£o (o fence do Ã‚Â§2/Ã‚Â§5 jÃƒÂ¡ garante correÃƒÂ§ÃƒÂ£o sem isto).
3. Roda a descoberta+exclusÃƒÂ£o jÃƒÂ¡ aprovada (Scan + taxonomia de ~40 `entityType`, convergÃƒÂªncia por re-Scan),
   reusando `DocumentPurgeWorker`/GSI6 para `Document` (D-061) Ã¢â‚¬â€ mecanismo inalterado desta rodada.
4. `TenantLifecycleRecord` **nunca** ÃƒÂ© alvo do Scan (exclusÃƒÂ£o explÃƒÂ­cita por `entityType`, mesma disciplina jÃƒÂ¡
   usada para `bff-session-table`).
5. ConvergÃƒÂªncia confirmada (zero itens restantes) Ã¢â€ â€™ `fencedSingleWrite` flip para `status = "DELETED"`
   (permanente; nenhum cÃƒÂ³digo reabre um tenant `DELETED` Ã¢â‚¬â€ fora de escopo desta feature).

## 7. O que este desenho NÃƒÆ’O resolve (fora de escopo explÃƒÂ­cito, registrar e nÃƒÂ£o mascarar)

- Rotas HTTP/confirmaÃƒÂ§ÃƒÂ£o/exportaÃƒÂ§ÃƒÂ£o de DSR completo Ã¢â‚¬â€ jÃƒÂ¡ fora de escopo desde D-062, inalterado.
- `PolicyRef` sem `tenantId` (achado independente de D-062) Ã¢â‚¬â€ correÃƒÂ§ÃƒÂ£o mecÃƒÂ¢nica separada.
- `tenantId` (MVP `tenantId=userId`) Ã¢â€°Â  Cognito `sub` Ã¢â‚¬â€ aÃƒÂ§ÃƒÂ£o administrativa futura no Cognito por tenant
  precisa resolver isso ÃƒÂ  parte; nÃƒÂ£o bloqueia este desenho porque a cascata nunca chama a API do Cognito.

## 8. Por que isto nÃƒÂ£o repete os erros de D-062/D-063

- O fence nÃƒÂ£o ÃƒÂ© a mesma linha que a cascata apaga (Ã‚Â§1, `TenantLifecycleRecord` explicitamente excluÃƒÂ­do do
  Scan) Ã¢â‚¬â€ resolve o achado mais grave de D-063.
- A checagem ÃƒÂ© acoplada (`ConditionCheck` na mesma transaÃƒÂ§ÃƒÂ£o), nunca uma leitura solta antes de agir Ã¢â‚¬â€
  resolve o segundo achado de D-063 (TOCTOU).
- A cobertura nÃƒÂ£o depende de enumerar 36 handlers corretamente Ã¢â‚¬â€ depende de 19 arquivos de persistÃƒÂªncia jÃƒÂ¡
  conhecidos + uma regra de lint estrutural que impede um call site futuro de escapar do fence. Reduz a
  classe de erro "auditoria incompleta" de "confiar em uma lista manual" para "confiar numa regra estÃƒÂ¡tica
  verificÃƒÂ¡vel por `dependency-cruiser`" Ã¢â‚¬â€ resolve a causa raiz comum ÃƒÂ s duas rodadas reprovadas.


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg --files src | rg 'guest.*submission|submission.*service'; rg -n --glob '*.ts' 'new\\s+(TransactWriteCommand|UpdateCommand|PutCommand|DeleteCommand)|\\.send\\s*\\(\\s*new\\s+(TransactWriteCommand|UpdateCommand|PutCommand|DeleteCommand)' src; rg -n --glob '*.ts' 'TransactWriteItems|TransactItems\\s*:' src test; rg -n 'dependency-cruiser|forbidden|no-restricted' .dependency-cruiser* package.json" in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-28T21:07:34.023633Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.3 seconds
Output:
src\modules\subject\application\guest-submission-service.ts
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\persistence\dynamodb-session-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\bff\persistence\dynamodb-session-store.ts:49:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:33:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:49:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\identity\persistence\dynamodb-identity-store.ts:61:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:33:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:48:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:21:        new UpdateCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:39:        new UpdateCommand({
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\document\persistence\dynamodb-document-store.ts:35:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\subject\persistence\dynamodb-subject-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\subject\persistence\dynamodb-subject-store.ts:49:        new PutCommand({
src\modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\persistence\dynamodb-import-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:24:        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:50:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:45:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:28:        new PutCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:43:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:37:        new PutCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:52:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
test\unit\outbox.test.ts:49:  it("appends a conditional Put entry so the caller can commit it in the same TransactWriteItems as the aggregate write", () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:6: * TransactWriteItems/Query semantics that the fakes only approximate.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:65:  it("materialize -> producer claim (real GSI3 query + real TransactWriteItems) -> durable outbox -> relay -> dispatch -> exactly one NotificationIntent", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:92:    // Real GSI3 Query against DynamoDB Local, real conditional TransactWriteItems claim,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:152:    // Real dispatch: CLAIMED -> TRIGGERED + NotificationIntent, real TransactWriteItems.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:175:  it("real conditional Query on GSI6 finds an expired claim and reconciliation reverts it via real TransactWriteItems", async () => {
src\workers\reminder-dispatch\dispatch.ts:13: * On success, everything commits in ONE TransactWriteItems (Â§9.4: "corrige a lacuna de
src\workers\document-purge\purge.ts:23: *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
src\modules\subject\ports\subject-store.ts:5: * agregado + contador + audit numa Ãºnica TransactWriteItems (nunca separado, ver
src\shared\outbox\outbox.ts:4: * TransactWriteItems `Put` entry so callers can append it atomically alongside the
src\shared\outbox\outbox.ts:116: * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
src\modules\subject\persistence\dynamodb-subject-store.ts:71:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\bff\application\bff-auth-service.ts:8: * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src\shared\idempotency\idempotency.ts:79: * already has one for its own aggregate writes) - a single-entry conditional TransactWriteItems,
src\modules\subject\application\subject-service.ts:7: * mutaÃ§Ã£o muda o nÃºmero de subjects ACTIVE) em uma Ãºnica TransactWriteItems.
src\shared\dynamodb\occ.ts:138: * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
src\shared\dynamodb\occ.ts:221: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src\shared\dynamodb\occ.ts:227: * re-asserted atomically inside the same TransactWriteItems as the write it gates).
src\shared\dynamodb\occ.ts:299:/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
src\modules\expiration\domain\audit-event.ts:5: * every mutation appends its audit record inside the SAME TransactWriteItems as the
src\modules\expiration\domain\audit-event.ts:80:/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction (accepts the full Put|Update union so callers don't need an unsafe cast for a mixed transaction). */
src\modules\expiration\ports\expiration-store.ts:7: * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
src\modules\expiration\application\expiration-service.ts:8: *     changes dueDate) in a single TransactWriteItems - implementing data-model.md Â§5:
src\modules\extraction\application\run-extraction-validation.ts:212: * all-or-nothing `TransactWriteItems` as the field rows, whose per-row `attribute_not_exists`
src\modules\import\persistence\dynamodb-import-store.ts:51:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-store.ts:55:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:67:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\notification\persistence\dynamodb-notification-store.ts:64:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\expiration\persistence\dynamodb-expiration-store.ts:3: * TransactWriteItems parameter shapes already produced by shared/dynamodb/occ.ts's
src\modules\expiration\persistence\dynamodb-expiration-store.ts:60:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\extraction\application\confirm-reject-field.ts:5: * `TransactWriteItems`) â€” `confirm` is a 4-way OCC (`ExpirationItem`/`Document`/`ExtractionRun`/
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\extraction\ports\extracted-field-store.ts:9: * terminal value must happen atomically, in the SAME `TransactWriteItems` that re-asserts the
src\modules\extraction\ports\extracted-field-store.ts:33:   * item is updated with OCC in the SAME `TransactWriteItems` as the field rows and the run
src\modules\extraction\ports\extracted-field-store.ts:52:   * `TransactWriteItems` is all-or-nothing), transitions the run to a terminal status, and
src\modules\extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (Â§1.7): one `TransactWriteItems` â€”
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:2: * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\application\reminder-policy-service.ts:9: * the SAME TransactWriteItems as the policy row itself:
src\modules\reminder\application\reminder-materializer.ts:265:   * `shouldCancel`, each in its own TransactWriteItems ALSO containing a ConditionCheck on
src\modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
test\unit\extraction\dynamodb-extracted-field-store.test.ts:27:    async send(command: { input: { TransactItems: CapturedTransactItem[] } }) {
test\unit\extraction\run-extraction-validation.test.ts:282:      // TransactWriteItems, so the dueDate write can never be applied a second time.
src\modules\notification\application\notification-router-workflow.ts:4: * into a SINGLE TransactWriteItems - the router never issues more than one transactional
src\modules\notification\application\notification-router.ts:4: * decide what happens, then translates the result into a single TransactWriteItems.
package.json:15:    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
package.json:32:    "dependency-cruiser": "17.4.3",
rg: .dependency-cruiser*: A sintaxe do nome do arquivo, do nome do diretÃ³rio ou do rÃ³tulo do volume estÃ¡ incorreta. (os error 123)

 exited 1 in 331ms:
src\modules\subject\application\guest-submission-service.ts
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\persistence\dynamodb-session-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\bff\persistence\dynamodb-session-store.ts:49:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:33:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:49:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\identity\persistence\dynamodb-identity-store.ts:61:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:33:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:48:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:21:        new UpdateCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:39:        new UpdateCommand({
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\document\persistence\dynamodb-document-store.ts:35:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\subject\persistence\dynamodb-subject-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\subject\persistence\dynamodb-subject-store.ts:49:        new PutCommand({
src\modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\persistence\dynamodb-import-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:24:        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:50:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:45:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:28:        new PutCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:43:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:37:        new PutCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:52:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
test\unit\outbox.test.ts:49:  it("appends a conditional Put entry so the caller can commit it in the same TransactWriteItems as the aggregate write", () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:6: * TransactWriteItems/Query semantics that the fakes only approximate.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:65:  it("materialize -> producer claim (real GSI3 query + real TransactWriteItems) -> durable outbox -> relay -> dispatch -> exactly one NotificationIntent", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:92:    // Real GSI3 Query against DynamoDB Local, real conditional TransactWriteItems claim,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:152:    // Real dispatch: CLAIMED -> TRIGGERED + NotificationIntent, real TransactWriteItems.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:175:  it("real conditional Query on GSI6 finds an expired claim and reconciliation reverts it via real TransactWriteItems", async () => {
src\workers\reminder-dispatch\dispatch.ts:13: * On success, everything commits in ONE TransactWriteItems (Â§9.4: "corrige a lacuna de
src\workers\document-purge\purge.ts:23: *  - The terminal step is one `TransactWriteItems`: `Delete` the `Document` row (conditioned on
src\modules\subject\ports\subject-store.ts:5: * agregado + contador + audit numa Ãºnica TransactWriteItems (nunca separado, ver
src\shared\outbox\outbox.ts:4: * TransactWriteItems `Put` entry so callers can append it atomically alongside the
src\shared\outbox\outbox.ts:116: * caller-supplied TransactWriteItems array (`tx`) rather than writing directly - the caller
src\modules\subject\persistence\dynamodb-subject-store.ts:71:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\bff\application\bff-auth-service.ts:8: * internal HTTP endpoint, no TransactWriteItems across the main table and this module's table.
src\shared\idempotency\idempotency.ts:79: * already has one for its own aggregate writes) - a single-entry conditional TransactWriteItems,
src\modules\subject\application\subject-service.ts:7: * mutaÃ§Ã£o muda o nÃºmero de subjects ACTIVE) em uma Ãºnica TransactWriteItems.
src\shared\dynamodb\occ.ts:138: * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
src\shared\dynamodb\occ.ts:221: * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
src\shared\dynamodb\occ.ts:227: * re-asserted atomically inside the same TransactWriteItems as the write it gates).
src\shared\dynamodb\occ.ts:299:/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
src\modules\expiration\domain\audit-event.ts:5: * every mutation appends its audit record inside the SAME TransactWriteItems as the
src\modules\expiration\domain\audit-event.ts:80:/** Appends the AuditEvent's conditional Put entry to a caller-supplied TransactWriteItems array, same convention as outbox.ts's appendToTransaction (accepts the full Put|Update union so callers don't need an unsafe cast for a mixed transaction). */
src\modules\expiration\ports\expiration-store.ts:7: * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
src\modules\expiration\application\expiration-service.ts:8: *     changes dueDate) in a single TransactWriteItems - implementing data-model.md Â§5:
src\modules\extraction\application\run-extraction-validation.ts:212: * all-or-nothing `TransactWriteItems` as the field rows, whose per-row `attribute_not_exists`
src\modules\import\persistence\dynamodb-import-store.ts:51:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-store.ts:55:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:67:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\notification\persistence\dynamodb-notification-store.ts:64:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\expiration\persistence\dynamodb-expiration-store.ts:3: * TransactWriteItems parameter shapes already produced by shared/dynamodb/occ.ts's
src\modules\expiration\persistence\dynamodb-expiration-store.ts:60:        TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"],
src\modules\extraction\application\confirm-reject-field.ts:5: * `TransactWriteItems`) â€” `confirm` is a 4-way OCC (`ExpirationItem`/`Document`/`ExtractionRun`/
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\extraction\ports\extracted-field-store.ts:9: * terminal value must happen atomically, in the SAME `TransactWriteItems` that re-asserts the
src\modules\extraction\ports\extracted-field-store.ts:33:   * item is updated with OCC in the SAME `TransactWriteItems` as the field rows and the run
src\modules\extraction\ports\extracted-field-store.ts:52:   * `TransactWriteItems` is all-or-nothing), transitions the run to a terminal status, and
src\modules\extraction\ports\extracted-field-store.ts:69:  /** `POST .../fields/{fieldName}/confirm` (Â§1.7): one `TransactWriteItems` â€”
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:2: * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\reminder\application\reminder-policy-service.ts:9: * the SAME TransactWriteItems as the policy row itself:
src\modules\reminder\application\reminder-materializer.ts:265:   * `shouldCancel`, each in its own TransactWriteItems ALSO containing a ConditionCheck on
src\modules\notification\domain\notification-attempt.ts:85: * (same TransactWriteItems, ConditionExpression attribute_not_exists(PK) - never overwrites
test\unit\extraction\dynamodb-extracted-field-store.test.ts:27:    async send(command: { input: { TransactItems: CapturedTransactItem[] } }) {
test\unit\extraction\run-extraction-validation.test.ts:282:      // TransactWriteItems, so the dueDate write can never be applied a second time.
src\modules\notification\application\notification-router-workflow.ts:4: * into a SINGLE TransactWriteItems - the router never issues more than one transactional
src\modules\notification\application\notification-router.ts:4: * decide what happens, then translates the result into a single TransactWriteItems.
package.json:15:    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
package.json:32:    "dependency-cruiser": "17.4.3",
rg: .dependency-cruiser*: A sintaxe do nome do arquivo, do nome do diretÃ³rio ou do rÃ³tulo do volume estÃ¡ incorreta. (os error 123)

codex
O primeiro grep jÃ¡ contradiz um ponto factual: as construÃ§Ãµes diretas encontradas estÃ£o em 14 arquivos, nÃ£o numa uniÃ£o claramente definida de â€œ8+11 = 19â€; e hÃ¡ stores em `shared/outbox` e `bff` que a redaÃ§Ã£o mistura com â€œdado de tenantâ€ sem explicitar exceÃ§Ãµes. Agora vou ampliar para todos os comandos mutÃ¡veis do SDK (`BatchWrite`, low-level client, transaÃ§Ãµes montadas indiretamente), ler cada store relevante e conferir a capacidade real do dependency-cruiser.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/subject/application/guest-submission-service.ts'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 164ms:
/**
 * GuestSubmissionService Ã¢â‚¬â€ lado do convidado (sem conta) de 04-domain-model-guest-upload.md
 * (D-037). NUNCA passa por `RequestContext`/`authorize()` Ã¢â‚¬â€ o convidado ÃƒÂ© validado sÃƒÂ³ pelo
 * token. Toda falha de validaÃƒÂ§ÃƒÂ£o retorna o MESMO erro genÃƒÂ©rico (`GuestTokenInvalidError`),
 * SEM detalhe algum no corpo da resposta (anti-enumeration Ã¢â‚¬â€ `AppError.toJSON()` inclui
 * `details` na resposta HTTP, entÃƒÂ£o este mÃƒÂ³dulo nunca populates `details` com o motivo real).
 */
import { randomUUID } from "node:crypto";
import { AppError, ValidationError } from "../../../shared/errors/app-error.js";
import { buildVersionedCreate, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
import { parseGuestToken, secretMatches, guestTokenPointerKey, hmacGuestTokenCrypto, type GuestTokenPointer } from "../domain/guest-token.js";
import { documentRequestKey, type DocumentRequest } from "../domain/document-request.js";
import { requirementAssignmentKey, type RequirementAssignment } from "../domain/requirement-assignment.js";
import { documentSubmissionKey, type DocumentSubmission } from "../domain/document-submission.js";
import { isTransactionCanceled, type SubjectStore, type TransactWriteEntry } from "../ports/subject-store.js";
import type { SubjectIdGenerator } from "./id-generator.js";
import type { GuestRateLimiter } from "./guest-rate-limiter.js";
import { MAX_UPLOAD_BYTES } from "../../document/application/upload-validation.js";
import type { UploadUrlSigner } from "../../document/ports/upload-url-signer.js";
import { sanitizeTenantText } from "../../notification/providers/email-templates.js";

const ALLOWED_MEDIA_TYPES: ReadonlySet<string> = new Set(["application/pdf", "image/jpeg", "image/png"]);
const PRESIGN_TTL_SECONDS = 600;

/** Erro ÃƒÂºnico e genÃƒÂ©rico para toda falha de validaÃƒÂ§ÃƒÂ£o de guest token Ã¢â‚¬â€ NUNCA carrega `details`
 * (categoria AUTH -> 401, mesmo em caminhos completamente distintos: token malformado,
 * inexistente, secret errado, expirado, revogado). */
export class GuestTokenInvalidError extends AppError {
  constructor() {
    super({ code: "GUEST_TOKEN_INVALID", category: "AUTH", message: "Invalid or expired link.", retryable: false });
    this.name = "GuestTokenInvalidError";
  }
}

export interface GuestRequestInfo {
  requirementName: string;
  deadline?: string;
  allowedMediaTypes: string[];
  maxUploadBytes: number;
  /** W5-01/GTR-01 (`decisions-log.md` D-060): identity of whoever created the request, shown
   * to the guest so an unauthenticated document upload no longer arrives from an anonymous
   * source. Always present (never `undefined`) - falls back to a generic, honest placeholder
   * when the tenant never captured `UserProfile.requesterDisplayName` (never inferred, e.g.
   * from an e-mail domain). */
  requesterDisplayName: string;
}

const REQUESTER_NAME_FALLBACK = "Solicitante nÃƒÂ£o identificado";

export interface StartGuestSubmissionInput {
  fileName: string;
  mediaType: string;
  contentLength: number;
  checksumSha256: string;
}

export interface StartGuestSubmissionResult {
  submissionId: string;
  uploadUrl: string;
  requiredHeaders: Record<string, string>;
  expiresAt: string;
}

export interface GuestSubmissionServiceDeps {
  store: SubjectStore;
  tableName: string;
  quarantineBucket: string;
  ids: SubjectIdGenerator;
  signer: UploadUrlSigner;
  rateLimiter: GuestRateLimiter;
  guestTokenPepper: string;
  /** W5-01/GTR-01 (D-060): resolves `UserProfile.requesterDisplayName` for the request's
   * creator (`DocumentRequest.requestedByUserId`) - same injected-resolver pattern as
   * `resolveInternalUserEmail` in `document-chasing-dispatch`'s dispatch.ts, deliberately NOT a
   * direct identity-module import (composition-root wiring, not a cross-module dependency). */
  resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
  now?: () => string;
}

interface ResolvedToken {
  pointer: GuestTokenPointer;
  request: DocumentRequest;
}

export class GuestSubmissionService {
  private readonly store: SubjectStore;
  private readonly tableName: string;
  private readonly quarantineBucket: string;
  private readonly ids: SubjectIdGenerator;
  private readonly signer: UploadUrlSigner;
  private readonly rateLimiter: GuestRateLimiter;
  private readonly pepper: string;
  private readonly resolveRequesterDisplayName?: (input: { tenantId: string; userId: string }) => Promise<string | undefined>;
  private readonly now: () => string;

  constructor(deps: GuestSubmissionServiceDeps) {
    this.store = deps.store;
    this.tableName = deps.tableName;
    this.quarantineBucket = deps.quarantineBucket;
    this.ids = deps.ids;
    this.signer = deps.signer;
    this.rateLimiter = deps.rateLimiter;
    this.pepper = deps.guestTokenPepper;
    this.resolveRequesterDisplayName = deps.resolveRequesterDisplayName;
    this.now = deps.now ?? (() => new Date().toISOString());
  }

  async getRequestInfo(rawToken: string): Promise<GuestRequestInfo> {
    const resolved = await this.resolveToken(rawToken);
    const assignment = await this.store.get<RequirementAssignment>(
      requirementAssignmentKey(resolved.request.tenantId, resolved.request.subjectId, resolved.request.assignmentId),
    );
    if (!assignment || assignment.deletedAt) throw new GuestTokenInvalidError();

    if (resolved.request.status === "REQUESTED") {
      await this.markOpened(resolved.request);
    }

    const resolvedRequesterName = await this.resolveRequesterDisplayName?.({
      tenantId: resolved.request.tenantId,
      userId: resolved.request.requestedByUserId,
    });

    return {
      requirementName: assignment.requirementName,
      deadline: resolved.request.deadline,
      allowedMediaTypes: [...ALLOWED_MEDIA_TYPES],
      maxUploadBytes: MAX_UPLOAD_BYTES,
      requesterDisplayName: sanitizeTenantText(resolvedRequesterName, REQUESTER_NAME_FALLBACK),
    };
  }

  async startSubmission(rawToken: string, input: StartGuestSubmissionInput): Promise<StartGuestSubmissionResult> {
    const resolved = await this.resolveToken(rawToken);

    if (!ALLOWED_MEDIA_TYPES.has(input.mediaType)) {
      throw new ValidationError("Unsupported media type.", { mediaType: input.mediaType });
    }
    if (input.contentLength <= 0 || input.contentLength > MAX_UPLOAD_BYTES) {
      throw new ValidationError("contentLength must be between 1 and 10MiB.");
    }
    if (!/^[a-f0-9]{64}$/i.test(input.checksumSha256)) {
      throw new ValidationError("checksumSha256 must be a 64-character hex SHA-256 digest.");
    }

    const { tenantId, subjectId, assignmentId, documentRequestId } = resolved.pointer;
    const submissionId = this.ids.newSubmissionId();
    const now = this.now();
    const expiresAt = new Date(Date.parse(now) + PRESIGN_TTL_SECONDS * 1000).toISOString();
    // Formato canÃƒÂ´nico de submission-anchored quarantine key (submission-quarantine-key.ts) -
    // nunca colide com o formato item-anchored existente de M6.
    const quarantineKey = `tenant/${tenantId}/subject/${subjectId}/assignment/${assignmentId}/submission/${submissionId}/document/${submissionId}/slot/${submissionId}/${randomUUID()}`;

    const submission: DocumentSubmission = {
      ...documentSubmissionKey(tenantId, subjectId, assignmentId, submissionId),
      entityType: "DocumentSubmission",
      submissionId,
      tenantId,
      subjectId,
      assignmentId,
      documentRequestId,
      fileName: input.fileName,
      mediaType: input.mediaType,
      contentLength: input.contentLength,
      checksumSha256: input.checksumSha256,
      status: "PENDING_UPLOAD",
      quarantineObject: { bucket: this.quarantineBucket, key: quarantineKey, versionId: "" },
      createdAt: now,
      updatedAt: now,
      version: 1,
    };

    const entries: TransactWriteEntry[] = [
      { Put: buildVersionedCreate(this.tableName, submission as unknown as Record<string, unknown> & { PK: string; SK: string }) },
      {
        Update: buildVersionedUpdate({
          tableName: this.tableName,
          key: documentRequestKey(tenantId, subjectId, assignmentId, documentRequestId),
          tenantId,
          expectedVersion: resolved.request.version,
          set: {
            status: "SUBMITTED",
            lastSubmissionId: submissionId,
            submissionCount: resolved.request.submissionCount + 1,
          },
        }),
      },
    ];

    try {
      await this.store.transactWrite(entries);
    } catch (err) {
      // Corrida real (ex. dois envios quase simultÃƒÂ¢neos do mesmo link) - convidado sÃƒÂ³ vÃƒÂª o
      // erro genÃƒÂ©rico, nunca detalhe de qual condiÃƒÂ§ÃƒÂ£o falhou.
      if (isTransactionCanceled(err)) throw new GuestTokenInvalidError();
      throw err;
    }

    const presigned = await this.signer.presignUpload({
      bucket: this.quarantineBucket,
      key: quarantineKey,
      mediaType: input.mediaType,
      contentLength: input.contentLength,
      checksumSha256: input.checksumSha256,
      metadata: { submissionId, tenantId },
      expiresInSeconds: PRESIGN_TTL_SECONDS,
    });

    return { submissionId, uploadUrl: presigned.uploadUrl, requiredHeaders: presigned.requiredHeaders, expiresAt };
  }

  /** Valida o token de ponta a ponta: parse estrutural -> lookup por selectorHash -> comparaÃƒÂ§ÃƒÂ£o
   * `timingSafeEqual` do secret -> expiraÃƒÂ§ÃƒÂ£o/revogaÃƒÂ§ÃƒÂ£o -> rate limit por token. Qualquer falha
   * em qualquer etapa produz o MESMO erro genÃƒÂ©rico. */
  private async resolveToken(rawToken: string): Promise<ResolvedToken> {
    const parsed = parseGuestToken(rawToken);
    if (!parsed) throw new GuestTokenInvalidError();

    const selectorHash = hmacGuestTokenCrypto.hash(this.pepper, parsed.selector);

    // Rate limit consumido ANTES do lookup do ponteiro, por selectorHash (calculÃƒÂ¡vel mesmo
    // que o ponteiro nÃƒÂ£o exista) Ã¢â‚¬â€ achado real de revisÃƒÂ£o adversarial (Codex): consumir sÃƒÂ³
    // depois de confirmar que o ponteiro existe criava um oracle de enumeraÃƒÂ§ÃƒÂ£o (selector
    // real esgota o limite e vira 429 QuotaExceededError; selector inexistente nunca chega
    // lÃƒÂ¡ e sempre dÃƒÂ¡ 401). Qualquer estouro de rate limit tambÃƒÂ©m vira o MESMO erro genÃƒÂ©rico
    // abaixo, nunca 429 diferenciado nesta superfÃƒÂ­cie pÃƒÂºblica.
    try {
      await this.rateLimiter.consume({ selectorHash, limit: 30, windowSeconds: 60 });
    } catch {
      throw new GuestTokenInvalidError();
    }

    const pointer = await this.store.get<GuestTokenPointer>(guestTokenPointerKey(selectorHash));

    // Caminho dummy anti-timing (achado real de revisÃƒÂ£o adversarial): mesmo quando o ponteiro
    // nÃƒÂ£o existe, ainda computamos um hash e uma comparaÃƒÂ§ÃƒÂ£o timingSafeEqual equivalente, para
    // que o tempo de resposta nÃƒÂ£o dependa de o selector existir ou nÃƒÂ£o. O hash dummy nunca
    // pode colidir com um secretHash real (prefixo fixo que issueGuestToken() nunca produz).
    const targetSecretHash = pointer?.secretHash ?? hmacGuestTokenCrypto.hash(this.pepper, `dummy:${selectorHash}`);
    const secretOk = secretMatches(this.pepper, parsed.secret, targetSecretHash);
    if (!pointer || !secretOk) throw new GuestTokenInvalidError();

    if (pointer.revokedAt) throw new GuestTokenInvalidError();
    if (pointer.expiresAt < this.now()) throw new GuestTokenInvalidError();

    const request = await this.store.get<DocumentRequest>(documentRequestKey(pointer.tenantId, pointer.subjectId, pointer.assignmentId, pointer.documentRequestId));
    if (!request || request.status === "CANCELLED" || request.status === "REVOKED" || request.status === "EXPIRED") {
      throw new GuestTokenInvalidError();
    }
    // D-037: TTL ÃƒÂ© "14 dias OU deadline, o que vier primeiro" Ã¢â‚¬â€ expiresAt do ponteiro jÃƒÂ¡
    // reflete isso na criaÃƒÂ§ÃƒÂ£o (document-request-service.ts), mas revalida aqui tambÃƒÂ©m caso
    // um deadline seja editado no futuro sem reemitir o token.
    if (request.deadline && request.deadline < this.now()) throw new GuestTokenInvalidError();

    return { pointer, request };
  }

  private async markOpened(request: DocumentRequest): Promise<void> {
    try {
      await this.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: this.tableName,
            key: documentRequestKey(request.tenantId, request.subjectId, request.assignmentId, request.documentRequestId),
            tenantId: request.tenantId,
            expectedVersion: request.version,
            set: { status: "OPENED", lastOpenedAt: this.now() },
          }),
        },
      ]);
    } catch (err) {
      // Best-effort - corrida de leitura concorrente (2 aberturas quase simultÃƒÂ¢neas do mesmo
      // link) nunca deve impedir o convidado de ver as informaÃƒÂ§ÃƒÂµes do pedido.
      if (!isTransactionCanceled(err)) throw err;
    }
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n --glob '*.ts' '@aws-sdk/(lib-dynamodb|client-dynamodb)' src; rg -n --glob '*.ts' 'new\\s+[A-Za-z0-9_]*(Write|Update|Put|Delete|Transact|Execute|Batch)[A-Za-z0-9_]*Command\\s*\\(' src; Get-Content -Raw '.dependency-cruiser.cjs'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 229ms:
src\modules\subject\persistence\dynamodb-subject-store.ts:6:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\subject\persistence\dynamodb-subject-store.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\import\persistence\dynamodb-import-store.ts:6:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\import\persistence\dynamodb-import-store.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\extraction.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\expiration.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:14:import { DynamoDBClient } from "@aws-sdk/client-dynamodb";
src\runtime\aws\handlers\bedrock-extraction-task-handler.ts:15:import { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\document.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\expiration\persistence\dynamodb-expiration-store.ts:8:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\expiration\persistence\dynamodb-expiration-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\bff.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\import.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\subject.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\subject.ts:22:import { GetCommand } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\identity.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\notification.ts:3:import { GetCommand, type DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\bff\persistence\dynamodb-session-store.ts:6:import { GetCommand, PutCommand } from "@aws-sdk/lib-dynamodb";
src\modules\bff\persistence\dynamodb-session-store.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\runtime\aws\composition\reminder.ts:2:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\workers\document-purge\dynamodb-document-purge-candidate-source.ts:9:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
src\workers\document-purge\dynamodb-document-purge-candidate-source.ts:10:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-candidate-source.ts:5:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-candidate-source.ts:6:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-store.ts:3:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\document\persistence\dynamodb-document-store.ts:4:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-recipient-resolver.ts:16:import { GetCommand } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-recipient-resolver.ts:17:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-notification-store.ts:12:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\notification\persistence\dynamodb-notification-store.ts:13:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reminder-store.ts:3:import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reminder-store.ts:4:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\shared\dynamodb\occ.ts:5: * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:8:import { GetCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\shared\dynamodb\client.ts:7:import { DynamoDBClient } from "@aws-sdk/client-dynamodb";
src\shared\dynamodb\client.ts:8:import { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:9:import { QueryCommand } from "@aws-sdk/lib-dynamodb";
src\modules\reminder\persistence\dynamodb-reconciliation-candidate-source.ts:10:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:4:import { QueryCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:5:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\composition\extraction.ts:7:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\identity\persistence\dynamodb-identity-store.ts:8:import { GetCommand, PutCommand } from "@aws-sdk/lib-dynamodb";
src\modules\identity\persistence\dynamodb-identity-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:8:import { GetCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:9:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:10:import type { TransactWriteCommandInput } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:3:import { GetCommand, PutCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:4:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:4:import { PutCommand, GetCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:5:import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
src\modules\subject\persistence\dynamodb-subject-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\subject\persistence\dynamodb-subject-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\subject\persistence\dynamodb-subject-store.ts:49:        new PutCommand({
src\modules\subject\persistence\dynamodb-subject-store.ts:70:      new TransactWriteCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:33:        new PutCommand({
src\modules\expiration\persistence\dynamodb-expiration-store.ts:48:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\expiration\persistence\dynamodb-expiration-store.ts:59:      new TransactWriteCommand({
src\modules\import\persistence\s3-import-object-store.ts:23:    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));
src\modules\import\persistence\dynamodb-import-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\import\persistence\dynamodb-import-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\import\persistence\dynamodb-import-store.ts:50:      new TransactWriteCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:28:        new PutCommand({
src\modules\reminder\persistence\dynamodb-reminder-store.ts:43:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\reminder\persistence\dynamodb-reminder-store.ts:54:      new TransactWriteCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:21:        new UpdateCommand({
src\shared\outbox\persistence\dynamodb-outbox-relay-store.ts:39:        new UpdateCommand({
src\modules\reminder\persistence\dynamodb-reminder-producer-store.ts:66:      new TransactWriteCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:37:        new PutCommand({
src\modules\notification\persistence\dynamodb-notification-store.ts:52:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\notification\persistence\dynamodb-notification-store.ts:63:      new TransactWriteCommand({
src\modules\document\persistence\s3-upload-url-signer.ts:15:    const command = new PutObjectCommand({
src\modules\document\persistence\dynamodb-document-store.ts:25:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\document\persistence\dynamodb-document-store.ts:35:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\document\persistence\dynamodb-document-store.ts:44:      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
src\modules\document\persistence\s3-document-object-store.ts:49:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src\modules\bff\persistence\dynamodb-session-store.ts:29:        new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }),
src\modules\bff\persistence\dynamodb-session-store.ts:40:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\bff\persistence\dynamodb-session-store.ts:49:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:33:        new PutCommand({
src\modules\identity\persistence\dynamodb-identity-store.ts:49:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
src\modules\identity\persistence\dynamodb-identity-store.ts:61:        new PutCommand({
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:70:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:129:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-extracted-field-store.ts:158:      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:24:        new PutCommand({
src\modules\extraction\persistence\dynamodb-textract-job-store.ts:50:        new UpdateCommand({
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:27:      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
src\modules\extraction\persistence\dynamodb-extraction-run-store.ts:45:        new UpdateCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:17:      new PutObjectCommand({
src\modules\extraction\persistence\s3-ocr-artifact-store.ts:38:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
/**
 * Real dependency-graph boundary enforcement Ã¢â‚¬â€ Engineering Maturity Review red team finding
 * (2026-08-19, checkpoint-12-redteam): the ESLint `no-restricted-imports` rule in
 * .eslintrc.cjs only catches a DIRECT forbidden import; it cannot see a transitive path
 * (e.g. domain/a.ts -> domain/bridge.ts -> application/service.ts), dynamic import(), or a
 * barrel file re-exporting a forbidden layer. dependency-cruiser resolves the actual module
 * graph (including transitively), so it closes that gap instead of just restating it.
 *
 * This is deliberately narrow Ã¢â‚¬â€ three rules, matching exactly the three boundaries already
 * enforced (imperfectly) by ESLint. Not a general-purpose architecture linter; adding more
 * rules here should require the same justification as any other engineering decision
 * (docs/engineering/decisions-log.md), not accumulate by default.
 */
module.exports = {
  forbidden: [
    {
      name: "domain-must-not-reach-infra",
      severity: "error",
      comment: "src/modules/*/domain/** must not depend on infra/, directly or transitively.",
      from: { path: "^src/modules/[^/]+/domain/" },
      to: { path: "^infra/" },
    },
    {
      name: "domain-must-not-reach-aws-sdk",
      severity: "error",
      comment: "src/modules/*/domain/** must stay AWS-SDK-agnostic, directly or transitively.",
      from: { path: "^src/modules/[^/]+/domain/" },
      to: { path: "^(aws-sdk|@aws-sdk/)" },
    },
    {
      name: "domain-must-not-reach-application-layers",
      severity: "error",
      comment:
        "src/modules/*/domain/** must not depend on ANY module's application/ports/http/persistence layers - not a sibling module's, and not even its own (domain is the innermost layer; application depends on domain, never the reverse). Matches .eslintrc.cjs's no-restricted-imports pattern exactly (that rule's glob is also module-name-agnostic) - direct or transitive.",
      from: { path: "^src/modules/[^/]+/domain/" },
      to: { path: "^src/modules/[^/]+/(application|ports|http|persistence)/" },
    },
    {
      name: "shared-must-not-reach-modules",
      severity: "error",
      comment:
        "src/shared/** is the lower-level layer every module's domain ultimately depends on (transitively) - it must never depend back on src/modules/**, or the dependency graph inverts. Added when security-audit.ts (src/shared/observability/) was tempted to import AuthorizationDeniedError's closed union types from modules/identity/domain/authorization.ts for a stricter signature - decoupled instead (reason/action typed as string there; callers already hold the closed-union value). This rule prevents that specific mistake from being reintroduced silently.",
      from: { path: "^src/shared/" },
      to: { path: "^src/modules/" },
    },
  ],
  options: {
    tsPreCompilationDeps: true,
    tsConfig: { fileName: "tsconfig.json" },
    enhancedResolveOptions: {
      exportsFields: ["exports"],
      conditionNames: ["import", "require", "node", "default"],
    },
    reporterOptions: {
      text: { highlightFocused: true },
    },
  },
};


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'src/modules/identity/persistence/identity-mapping-repository.ts'; Get-Content -Raw 'src/modules/identity/persistence/user-repository.ts'; Get-Content -Raw 'src/modules/identity/persistence/dynamodb-identity-store.ts'; Get-Content -Raw 'src/modules/extraction/persistence/dynamodb-extracted-field-store.ts'; rg -n 'MAX_|maxItems|max.*field|fields\\.length|TransactItems|transactWrite\\(' src/modules src/workers | rg '100|99|field|Transact|transact'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 260ms:
/**
 * IdentityMapping repository Ã¢â‚¬â€ data-model.md Ã‚Â§2/Ã‚Â§"IdentityMapping" +
 * implementation-blueprint.md Ã‚Â§23.1/Ã‚Â§7.3: PK=IDENTITY#cognitoSub#<sub>, SK=MAP, created
 * atomically (ConditionExpression attribute_not_exists(PK)) on first login. This is one
 * of the two documented exceptions to the "every key starts with TENANT#tenantId" rule
 * (the other is GSI3) Ã¢â‚¬â€ the lookup happens *before* tenantId is known.
 */
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
import { InternalError } from "../../../shared/errors/app-error.js";

export interface IdentityMapping {
  PK: string;
  SK: "MAP";
  entityType: "IdentityMapping";
  cognitoSub: string;
  userId: string;
  tenantId: string;
  createdAt: string;
}

export function identityMappingKey(cognitoSub: string): EntityKey {
  return { PK: `IDENTITY#cognitoSub#${cognitoSub}`, SK: "MAP" };
}

export class IdentityMappingRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async find(cognitoSub: string): Promise<IdentityMapping | undefined> {
    return this.store.get<IdentityMapping>(identityMappingKey(cognitoSub));
  }

  /**
   * Atomically creates the mapping on first login. MVP: tenantId=userId (data-model.md
   * Ã‚Â§7.3 "No MVP, tenantId=userId, mas o cÃƒÂ³digo recebe ambos como conceitos separados"),
   * so newUserId is used for both Ã¢â‚¬â€ callers must never derive one from the other outside
   * this function once Organizations (FUT-001) exist.
   *
   * Idempotent under races: if two concurrent first-logins for the same sub both call
   * this, exactly one PutItem wins (attribute_not_exists(PK)); the loser re-reads and
   * returns the winner's mapping instead of erroring, so first-login is safe to retry.
   */
  async findOrCreate(cognitoSub: string, newUserId: string, newTenantId: string): Promise<IdentityMapping> {
    const existing = await this.find(cognitoSub);
    if (existing) {
      return existing;
    }

    const key = identityMappingKey(cognitoSub);
    const candidate: IdentityMapping = {
      ...key,
      SK: "MAP",
      entityType: "IdentityMapping",
      cognitoSub,
      userId: newUserId,
      tenantId: newTenantId,
      createdAt: this.now(),
    };

    const created = await this.store.putIfAbsent(candidate);
    if (created) {
      return candidate;
    }

    // Lost the race: someone else created it first-login-atomically between our
    // find() and putIfAbsent(). Re-read; if it's still missing something is badly
    // wrong (a delete of an IdentityMapping is not a supported operation).
    const winner = await this.find(cognitoSub);
    if (!winner) {
      throw new InternalError("IdentityMapping vanished after losing creation race.", { cognitoSub });
    }
    return winner;
  }
}

/**
 * User repository Ã¢â‚¬â€ data-model.md Ã‚Â§2 (`TENANT#t#USER#u` / `PROFILE`) plus the session-
 * revocation fields required by implementation-blueprint.md Ã‚Â§4.2 ("Logout por dispositivo
 * atualiza deviceLogoutAfter/revoga a famÃƒÂ­lia de refresh. Logout global atualiza
 * globalLogoutAfter"). data-model.md doesn't enumerate a standalone Session entity, so
 * this module keeps revocation watermarks on the User item itself (globalLogoutAfter) and
 * per-device entries as child items under the same tenant partition
 * (`TENANT#t#USER#u` / `SESSION#<deviceId>`) rather than inventing a new top-level
 * aggregate Ã¢â‚¬â€ see the M1 report for this judgment call.
 */
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";

export interface UserProfile {
  PK: string;
  SK: "PROFILE";
  entityType: "User";
  userId: string;
  tenantId: string;
  identitySubject: string;
  emailNormalized: string;
  roles: string[];
  status: "ACTIVE" | "SUSPENDED";
  globalLogoutAfter?: string;
  /** W5-01/GTR-01 (`decisions-log.md` D-060): name shown to a guest as "who is requesting this
   * document" (`GuestRequestInfo.requesterDisplayName`) and interpolated into the guest-facing
   * email templates. Optional and user-editable (`PUT /profile`) Ã¢â‚¬â€ never inferred from the
   * user's e-mail domain (this codebase's epistemic-integrity discipline: never guess data that
   * was never actually captured). Absent until the user sets it once. */
  requesterDisplayName?: string;
  createdAt: string;
  updatedAt: string;
  version: number;
}

export interface DeviceSession {
  PK: string;
  SK: string; // SESSION#<deviceId>
  entityType: "DeviceSession";
  tenantId: string;
  userId: string;
  deviceId: string;
  sessionId: string;
  refreshFamilyId: string;
  deviceLogoutAfter?: string;
  createdAt: string;
  lastSeenAt: string;
  expiresAt: string;
  status: "ACTIVE" | "REVOKED";
}

export function userProfileKey(tenantId: string, userId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: "PROFILE" };
}

export function deviceSessionKey(tenantId: string, userId: string, deviceId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#USER#${userId}`, SK: `SESSION#${deviceId}` };
}

export class UserRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async getProfile(tenantId: string, userId: string): Promise<UserProfile | undefined> {
    return this.store.get<UserProfile>(userProfileKey(tenantId, userId));
  }

  async createProfileIfAbsent(profile: Omit<UserProfile, "PK" | "SK" | "entityType" | "createdAt" | "updatedAt" | "version">): Promise<UserProfile> {
    const key = userProfileKey(profile.tenantId, profile.userId);
    const existing = await this.getProfile(profile.tenantId, profile.userId);
    if (existing) {
      return existing;
    }
    const now = this.now();
    const item: UserProfile = {
      ...key,
      SK: "PROFILE",
      entityType: "User",
      ...profile,
      createdAt: now,
      updatedAt: now,
      version: 1,
    };
    await this.store.putIfAbsent(item);
    return item;
  }

  async getDeviceSession(tenantId: string, userId: string, deviceId: string): Promise<DeviceSession | undefined> {
    return this.store.get<DeviceSession>(deviceSessionKey(tenantId, userId, deviceId));
  }

  async upsertDeviceSession(session: DeviceSession): Promise<void> {
    await this.store.update(session);
  }

  /** Logout global Ã¢â‚¬â€ implementation-blueprint.md Ã‚Â§4.2: revokes every token issued before now. */
  async logoutAll(tenantId: string, userId: string): Promise<void> {
    const profile = await this.getProfile(tenantId, userId);
    if (!profile) return;
    await this.store.update({ ...profile, globalLogoutAfter: this.now(), updatedAt: this.now() });
  }

  /** Logout by device Ã¢â‚¬â€ revokes only this device's refresh family. */
  async logoutDevice(tenantId: string, userId: string, deviceId: string): Promise<void> {
    const session = await this.getDeviceSession(tenantId, userId, deviceId);
    if (!session) return;
    await this.store.update({ ...session, status: "REVOKED", deviceLogoutAfter: this.now() });
  }

  /** W5-01/GTR-01: sets or clears (`undefined`) the name shown to guests as the requester's
   * identity. Same unconditional-overwrite pattern as `logoutAll`/`logoutDevice` above Ã¢â‚¬â€ this
   * port has no per-item OCC beyond `updateConditional`'s counter-only use (quota.ts), and a
   * lost update on this single self-editable text field carries the same negligible,
   * already-accepted risk as those two fields. Caller (`ProfileService`) is responsible for
   * confirming the profile exists first. */
  async setRequesterDisplayName(profile: UserProfile, requesterDisplayName: string | undefined): Promise<UserProfile> {
    const updated: UserProfile = { ...profile, requesterDisplayName, updatedAt: this.now() };
    await this.store.update(updated);
    return updated;
  }
}

/**
 * Real DynamoDB adapter for IdentityStore (M3.5). Thin translation to
 * DynamoDBDocumentClient commands - no OCC/idempotency logic lives here, that's already
 * built by shared/dynamodb/occ.ts and shared/idempotency/idempotency.ts; this class only
 * executes the commands those builders produce (for `update`) and issues plain
 * Get/Put for the rest.
 */
import { GetCommand, PutCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";

export class DynamoDbIdentityStore implements IdentityStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
    try {
      const result = await this.client.send(
        new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: true }),
      );
      return result.Item as T | undefined;
    } catch (err) {
      throw mapDynamoError(err, "IdentityStore.get");
    }
  }

  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
    try {
      await this.client.send(
        new PutCommand({
          TableName: this.tableName,
          Item: item,
          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "IdentityStore.putIfAbsent");
    }
  }

  /** Unconditional overwrite - matches the fake's semantics (non-OCC bookkeeping writes only). */
  async update<T extends EntityKey>(item: T): Promise<void> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
    } catch (err) {
      throw mapDynamoError(err, "IdentityStore.update");
    }
  }

  async updateConditional<T extends EntityKey>(
    item: T,
    expected: { count: number; resetAt: string },
  ): Promise<boolean> {
    try {
      await this.client.send(
        new PutCommand({
          TableName: this.tableName,
          Item: item,
          // Real production bug found via a live smoke test against
          // exptrk-dev-notifications-handler (2026-08-21): `count` is a DynamoDB reserved
          // word (https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
          // - using it as a bare attribute name in a ConditionExpression, instead of through
          // an ExpressionAttributeNames placeholder, makes every real DynamoDB call fail with
          // ValidationException. Never caught by any test because the in-memory
          // IdentityStore fake (test/unit/identity/in-memory-store.ts) doesn't parse
          // condition expressions the way real DynamoDB does - this path was never actually
          // exercised against real DynamoDB before this session's real deploy.
          ConditionExpression: "#count = :expectedCount AND resetAt = :expectedResetAt",
          ExpressionAttributeNames: { "#count": "count" },
          ExpressionAttributeValues: {
            ":expectedCount": expected.count,
            ":expectedResetAt": expected.resetAt,
          },
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "IdentityStore.updateConditional");
    }
  }
}

/** Real DynamoDB adapter for `ExtractedFieldStore` (M7 item 7) Ã¢â‚¬â€ the first real writer of
 * `ExtractedField` rows. `commitRunOutcome` is a single `TransactWriteItems`: one
 * `attribute_not_exists` Put per field row (first-time create, `buildVersionedCreate`), one
 * versioned Update on the parent `ExtractionRun` (`buildVersionedUpdate`), and one
 * `ConditionCheck` re-asserting the `Document` row is still at the version the caller read
 * moments earlier (`buildVersionConditionCheck`) Ã¢â‚¬â€ the TOCTOU close for the concurrent-discard
 * race (design Ã‚Â§3). All three succeed or none do. */
import { GetCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { TransactWriteCommandInput } from "@aws-sdk/lib-dynamodb";
import { buildVersionedCreate, buildVersionedUpdate, buildVersionConditionCheck, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import { mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
import type {
  CommitRunOutcomeInput,
  CommitRunOutcomeResult,
  ConfirmFieldInput,
  ExtractedFieldStore,
  FieldTransitionResult,
  RejectFieldInput,
} from "../ports/extracted-field-store.js";
import type { ExtractedField } from "../domain/extracted-field.js";

export class DynamoDbExtractedFieldStore implements ExtractedFieldStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async commitRunOutcome(input: CommitRunOutcomeInput): Promise<CommitRunOutcomeResult> {
    const fieldPuts = input.fields.map((field) => ({ Put: buildVersionedCreate(this.tableName, field as unknown as Record<string, unknown> & { PK: string; SK: string }) }));

    const runUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.runKey,
        tenantId: input.runTenantId,
        expectedVersion: input.runExpectedVersion,
        set: { status: input.runStatus, completedAt: input.completedAt },
      }),
    };

    const documentGuard = buildVersionConditionCheck({
      tableName: this.tableName,
      key: input.documentKey,
      expectedVersion: input.documentExpectedVersion,
    });

    // W2-01-DECISION: when the run auto-confirmed a field that maps to an `ExpirationItem`
    // attribute, that item write joins THIS transaction Ã¢â‚¬â€ never a follow-up write Ã¢â‚¬â€ so the
    // auto-confirm outcome is as atomic as the manual `confirmField` one.
    const itemUpdate = input.itemUpdate
      ? [
          {
            Update: buildVersionedUpdate({
              tableName: this.tableName,
              key: input.itemUpdate.key,
              tenantId: input.itemUpdate.tenantId,
              expectedVersion: input.itemUpdate.expectedVersion,
              set: input.itemUpdate.set,
              now: input.completedAt,
            }),
          },
        ]
      : [];

    const transactItems = [...fieldPuts, runUpdate, documentGuard, ...itemUpdate] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      // A cancellation here can only come from one of: a field row already existing (should
      // never happen - runId makes each field's SK unique per run, so this would indicate a
      // genuine retry-after-partial-commit bug, not a real race), the run's own version having
      // moved (nothing else in the system updates ExtractionRun.status before this handler
      // runs), the Document guard (the actual race this method exists to detect), or the
      // optional `ExpirationItem` update's own version having moved. Rather
      // than parsing per-item cancellation reasons (fragile across SDK versions), any
      // cancellation here is treated uniformly as DOCUMENT_DISCARDED - the safe, conservative
      // reading is "something about the world this commit assumed is no longer true", and the
      // caller's fallback (mark the run DISCARDED, persist zero fields) is safe even in the
      // (extremely unlikely) case the true cause was one of the other two conditions.
      if (isTransactionCanceled(err)) return "DOCUMENT_DISCARDED";
      throw mapDynamoError(err, "ExtractedFieldStore.commitRunOutcome");
    }
  }

  async get(key: EntityKey): Promise<ExtractedField | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
      return result.Item as ExtractedField | undefined;
    } catch (err) {
      throw mapDynamoError(err, "ExtractedFieldStore.get");
    }
  }

  async confirmField(input: ConfirmFieldInput): Promise<FieldTransitionResult> {
    const fieldUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.fieldKey,
        tenantId: input.fieldTenantId,
        expectedVersion: input.fieldExpectedVersion,
        set: { state: "CONFIRMED", confirmedValue: input.confirmedValue },
        now: input.now,
      }),
    };

    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });

    const itemEntry = input.itemUpdate
      ? {
          Update: buildVersionedUpdate({
            tableName: this.tableName,
            key: input.itemKey,
            tenantId: input.itemTenantId,
            expectedVersion: input.itemExpectedVersion,
            set: input.itemUpdate,
            now: input.now,
          }),
        }
      : buildVersionConditionCheck({ tableName: this.tableName, key: input.itemKey, expectedVersion: input.itemExpectedVersion });

    const transactItems = [fieldUpdate, runGuard, documentGuard, itemEntry] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
      throw mapDynamoError(err, "ExtractedFieldStore.confirmField");
    }
  }

  async rejectField(input: RejectFieldInput): Promise<FieldTransitionResult> {
    const set: Record<string, unknown> = { state: "REJECTED" };
    if (input.correctionReason !== undefined) set["correctionReason"] = input.correctionReason;

    const fieldUpdate = {
      Update: buildVersionedUpdate({
        tableName: this.tableName,
        key: input.fieldKey,
        tenantId: input.fieldTenantId,
        expectedVersion: input.fieldExpectedVersion,
        set,
        now: input.now,
      }),
    };

    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });

    const transactItems = [fieldUpdate, runGuard, documentGuard] as unknown as TransactWriteCommandInput["TransactItems"];

    try {
      await this.client.send(new TransactWriteCommand({ TransactItems: transactItems }));
      return "COMMITTED";
    } catch (err) {
      if (isTransactionCanceled(err)) return "VERSION_CONFLICT";
      throw mapDynamoError(err, "ExtractedFieldStore.rejectField");
    }
  }
}

src/workers\document-chasing-dispatch\dispatch.ts:113:      await deps.store.transactWrite([
src/workers\document-chasing-dispatch\dispatch.ts:172:      await
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "''^(#|##|###)|Achado|N[1-4]|Nota|RESOLVIDO|PARCIAL|NÃO RESOLVIDO|NAO RESOLVIDO|Recomend" docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt; rg -n "''^(#|##|###)" docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round3.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 194ms:
52:1. Nota de 0 a 10 (protocolo de nota cega desta rodada).
53:2. Para cada um dos 8 achados da Rodada 1: RESOLVIDO / PARCIALMENTE RESOLVIDO / NAO RESOLVIDO, com evidencia.
54:3. Achados NOVOS introduzidos pela Rodada 2 (se houver), confirmados contra codigo real.
55:4. Recomendacao: aprovar (>=9.0), pedir revisao, ou reprovar.
70:# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o
74:## Estado atual
82:**RecomendaÃ§Ã£o da sÃ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃªs gates reais nomeados na sÃ­ntese original â€” guest trust (`GTR-01`/`W5-01`), evidÃªncia operacional (Wave 2), e retenÃ§Ã£o/purga real (`W3-06`) â€” **fecharam todos nesta sessÃ£o**, e todos os itens secundÃ¡rios de Wave 2 (RPO, dedupe pÃ³s-commit, pipeline de alarme de credential-compromise) tambÃ©m fecharam na mesma sessÃ£o. Ãšnico item nÃ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).
84:954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃ§Ã£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃ§Ã£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃ£o** (W2-06 RPO, W2-05 dedupe pÃ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃ£o, nenhuma aprovada**: (1) D-062, 4 rodadas Claudeâ†”Codex, notas 3,4â†’5,1â†’4,7/10 â€” achado central: garantir "nÃ£o ressurreiÃ§Ã£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃ­ncronos); Claude e Codex recomendaram convergentemente nÃ£o implementar sem esse fence, e a sessÃ£o parou por decisÃ£o do Marcelo. (2) O Marcelo entÃ£o decidiu NÃƒO esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃ­cio â€” D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃ¡ existente e jÃ¡ checado em `resolveRequestContext`) seria apagado pela prÃ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃ³ximo login â€” o mecanismo de autenticaÃ§Ã£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃ­cito do Marcelo para planejamento minucioso numa sessÃ£o dedicada** (nÃ£o abandonado). **PrÃ³xima aÃ§Ã£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃ§Ã£o de levantar superfÃ­cies de escrita por ponto de entrada de runtime â€” API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions â€” nunca por pasta de cÃ³digo, que jÃ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso â€” os 3 gates reais (W3-06/Wave 2/GTR-01) jÃ¡ fecharam.
109:  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` â€” `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃ§Ã£o `requirement:read` jÃ¡ existente (sem action nova reservada de antemÃ£o, diferente de `document:read`), mesma leitura por partiÃ§Ã£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform â€” nÃ£o Ã© desta sessÃ£o, jÃ¡ estava corrigido antes (comentÃ¡rio no prÃ³prio `main.tf`), sÃ³ confirmado que segue correto.
111:  Ambas as metades: mudanÃ§a mecÃ¢nica sobre um padrÃ£o de acesso jÃ¡ aprovado no data model â€” nÃ£o passaram pelo protocolo `AGENTS.md` Â§4 completo (nÃ£o Ã© decisÃ£o de arquitetura nova, Ã© fechamento de gap de leitura jÃ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃ­cio da sessÃ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃ£o. **Achado real corrigido depois, na mesma sessÃ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` â€” mesma classe de gap que as rotas de `DocumentRequest` jÃ¡ tiveram antes (comentÃ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
119:## BLOCKER-B â€” implementaÃ§Ã£o completa e mergeada em `develop` (PR #50, commit `8f00160`)
129:## Infra de hospedagem do SPA â€” etapas 1-5 fechadas (2026-08-25)
134:2. **MÃ³dulo Terraform `infra/modules/spa-hosting` â€” IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃ­citos â†’ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default â†’ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃ©todo GET/HEAD, associada SÃ“ ao default behavior â€” nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃ©-existentes do ADR-0011 tambÃ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃ§Ã£o errada do prÃ³prio ADR-0011): `var.spa_content_security_policy` nÃ£o precisa de hashes calculados por build â€” verificado contra o `frontend/dist/index.html` real, o build do Vite nÃ£o emite nenhum `<script>`/`<style>` inline, todo asset Ã© carregado por URL externa jÃ¡ hasheada, entÃ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃ¡ existe em `frontend/index.html`) bastam sem hash nenhum â€” a variÃ¡vel ganhou um default determinÃ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃ£o consegue expressar). **MÃ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) â€” corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃ£o bloqueia o primeiro apply, sÃ³ o domÃ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro â€” confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃ£o sÃ³ contra mocks.
148:## M7 (extraÃ§Ã£o/OCR) â€” implementaÃ§Ã£o iniciada (2026-08-25, D-057)
197:- **Achado real corrigido durante a implementaÃ§Ã£o, nÃ£o uma decisÃ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃ£o tinha `ResultPath` na transiÃ§Ã£o de sucesso do ASL, entÃ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃ­a inteiramente** o `$` do Step Functions â€” todo o contexto original da execuÃ§Ã£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃ£o sÃ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃ³prio `job`) ao payload de sucesso. MudanÃ§a aditiva/reversÃ­vel sem migraÃ§Ã£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃ³rico real).
199:- **Achado de correÃ§Ã£o na prÃ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` â€” nÃ£o muda nenhum `Next`/fluxo, sÃ³ corrige o path de leitura para bater com a semÃ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
210:## Item 6 (`BedrockExtractionTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
214:**Achado real corrigido durante a implementaÃ§Ã£o, na prÃ³pria ASL** (nÃ£o uma decisÃ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` â€” `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.
232:## Item 7 (`ExtractionValidationTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
254:**Achado real corrigido nesta sessÃ£o, em cÃ³digo do item 6 jÃ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` â€” a MESMA classe de bug que o item 5 jÃ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Â§3's "achado mais sÃ©rio" Ã© sobre limpeza de artefato, mas essa categoria de "output nÃ£o reanexa o contexto anterior" jÃ¡ apareceu 3 vezes agora: item 4â†’5, item 5â†’6, e este item 6â†’7). Corrigido de forma aditiva (campos novos sÃ³ ecoam o que jÃ¡ vinha em `input`, sem recomputar nada) â€” os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃ§Ã£o (nÃ£o fazem `toEqual` no objeto inteiro). **PadrÃ£o a vigiar em qualquer implementaÃ§Ã£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃ³ximo estado vai precisar, nunca assumir que "sÃ³ preciso retornar o que eu calculei".
275:**Item 2 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: `ExtractionStarterWorker` completo â€” domÃ­nio/aplicaÃ§Ã£o/persistÃªncia/handler real, nÃ£o sÃ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` â€” portas prÃ³prias, nÃ£o reusa `DocumentStore` do mÃ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃ¡ existia â€” gated sÃ³ no registro criaria um run Ã³rfÃ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real Ã© a idempotÃªncia nativa do `StartExecution` por nome de execuÃ§Ã£o determinÃ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃ£o S3â†’EventBridgeâ†’SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃ§Ã£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` â€” insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) â€” mudanÃ§a na M6 (jÃ¡ em produÃ§Ã£o), mas segura: `cleanObject` Ã© sempre um valor opaco armazenado na entidade, nunca reconstruÃ­do/reparsed em nenhum outro lugar do cÃ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis), mas a regra EventBridge que liga o bucket limpo Ã  fila Ã© o ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` â€” com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) â€” jÃ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃªncias) â€” comportamento condicional verificado, nÃ£o sÃ³ lido no cÃ³digo. **Ainda sem PR/merge** (mesma lÃ³gica do item 1: mergeÃ¡vel isoladamente, mas ainda aguardando decisÃ£o de "quando" â€” ver abaixo).
286:## Item 8 (rotas HTTP de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
302:## Item 9 (quota `AI_CALL`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo
311:## M7 â€” estado final desta sessÃ£o: cÃ³digo-completo para `dev`
318:## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)
342:### Bugs reais encontrados
350:### Achado menor (nÃ£o corrigido, sem impacto observado nesta verificaÃ§Ã£o)
354:### Estado do ambiente apÃ³s a sessÃ£o
362:## DecisÃµes pendentes do Marcelo
368:## PendÃªncias residuais nÃ£o bloqueantes (registradas, nÃ£o bloqueiam nada)
373:- **Achado real, severidade baixa, nÃ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso â€” sÃ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` Ã© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃ³ Ã© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃ§Ã£o defensiva do status real da occurrence antes de agir â€” um ponteiro Ã³rfÃ£o nunca Ã© revisitado fora dessa janela, e mesmo se fosse, Ã© ignorado com seguranÃ§a. Diferente do bug do `upload-slot` (falha total â€” nada funcionava), este Ã© um descompasso comentÃ¡rio-vs-cÃ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃ¡ revisado 9,2/10 pelo protocolo Claudeâ†”Codex e ativo em produÃ§Ã£o real â€” nÃ£o corrigido nesta sessÃ£o deliberadamente, para nÃ£o mexer num trecho crÃ­tico jÃ¡ extensivamente revisado sem uma rodada de revisÃ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
376:## ReferÃªncias (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)
393:# docs/architecture/ — Índice e Mapa de Autoridade
404:## Precedência de fontes (quando houver divergência)
413:## Índice por documento
465:## `history/` — evidência de rodada, por tema
485:# docs/engineering/ — Índice e Mapa de Autoridade
496:## Precedência de fontes (quando houver divergência)
500:## Quando carregar o quê (roteamento por tipo de tarefa)
518:## Índice por documento (nível raiz de `docs/engineering/`)
539:## `reviews/` — convenção de nomenclatura e classificação
549:   - `full-audit-round1-<eixo>-summary.md` — **o único arquivo desta trinca que deveria ser lido diretamente.** Nota final por critério (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correção pontual".
595:1. Nota de 0 a 10 (protocolo de nota cega - nao existe nota anterior a comparar, esta e a primeira desta
597:2. Achados REAIS, cada um confirmado contra codigo/arquitetura real (nome de arquivo/linha quando aplicavel),
611:3. Recomendacao: aprovar (>=9.0), pedir revisao (com o que precisa mudar), ou reprovar.
628:# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o
632:## Estado atual
640:**RecomendaÃ§Ã£o da sÃ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃªs gates reais nomeados na sÃ­ntese original â€” guest trust (`GTR-01`/`W5-01`), evidÃªncia operacional (Wave 2), e retenÃ§Ã£o/purga real (`W3-06`) â€” **fecharam todos nesta sessÃ£o**, e todos os itens secundÃ¡rios de Wave 2 (RPO, dedupe pÃ³s-commit, pipeline de alarme de credential-compromise) tambÃ©m fecharam na mesma sessÃ£o. Ãšnico item nÃ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).
642:954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃ§Ã£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃ§Ã£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃ£o** (W2-06 RPO, W2-05 dedupe pÃ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃ£o, nenhuma aprovada**: (1) D-062, 4 rodadas Claudeâ†”Codex, notas 3,4â†’5,1â†’4,7/10 â€” achado central: garantir "nÃ£o ressurreiÃ§Ã£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃ­ncronos); Claude e Codex recomendaram convergentemente nÃ£o implementar sem esse fence, e a sessÃ£o parou por decisÃ£o do Marcelo. (2) O Marcelo entÃ£o decidiu NÃƒO esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃ­cio â€” D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃ¡ existente e jÃ¡ checado em `resolveRequestContext`) seria apagado pela prÃ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃ³ximo login â€” o mecanismo de autenticaÃ§Ã£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃ­cito do Marcelo para planejamento minucioso numa sessÃ£o dedicada** (nÃ£o abandonado). **PrÃ³xima aÃ§Ã£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃ§Ã£o de levantar superfÃ­cies de escrita por ponto de entrada de runtime â€” API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions â€” nunca por pasta de cÃ³digo, que jÃ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso â€” os 3 gates reais (W3-06/Wave 2/GTR-01) jÃ¡ fecharam.
667:  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` â€” `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃ§Ã£o `requirement:read` jÃ¡ existente (sem action nova reservada de antemÃ£o, diferente de `document:read`), mesma leitura por partiÃ§Ã£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform â€” nÃ£o Ã© desta sessÃ£o, jÃ¡ estava corrigido antes (comentÃ¡rio no prÃ³prio `main.tf`), sÃ³ confirmado que segue correto.
669:  Ambas as metades: mudanÃ§a mecÃ¢nica sobre um padrÃ£o de acesso jÃ¡ aprovado no data model â€” nÃ£o passaram pelo protocolo `AGENTS.md` Â§4 completo (nÃ£o Ã© decisÃ£o de arquitetura nova, Ã© fechamento de gap de leitura jÃ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃ­cio da sessÃ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃ£o. **Achado real corrigido depois, na mesma sessÃ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` â€” mesma classe de gap que as rotas de `DocumentRequest` jÃ¡ tiveram antes (comentÃ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
677:## BLOCKER-B â€” implementaÃ§Ã£o completa e mergeada em `develop` (PR #50, commit `8f00160`)
687:## Infra de hospedagem do SPA â€” etapas 1-5 fechadas (2026-08-25)
692:2. **MÃ³dulo Terraform `infra/modules/spa-hosting` â€” IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃ­citos â†’ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default â†’ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃ©todo GET/HEAD, associada SÃ“ ao default behavior â€” nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃ©-existentes do ADR-0011 tambÃ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃ§Ã£o errada do prÃ³prio ADR-0011): `var.spa_content_security_policy` nÃ£o precisa de hashes calculados por build â€” verificado contra o `frontend/dist/index.html` real, o build do Vite nÃ£o emite nenhum `<script>`/`<style>` inline, todo asset Ã© carregado por URL externa jÃ¡ hasheada, entÃ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃ¡ existe em `frontend/index.html`) bastam sem hash nenhum â€” a variÃ¡vel ganhou um default determinÃ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃ£o consegue expressar). **MÃ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) â€” corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃ£o bloqueia o primeiro apply, sÃ³ o domÃ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro â€” confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃ£o sÃ³ contra mocks.
706:## M7 (extraÃ§Ã£o/OCR) â€” implementaÃ§Ã£o iniciada (2026-08-25, D-057)
755:- **Achado real corrigido durante a implementaÃ§Ã£o, nÃ£o uma decisÃ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃ£o tinha `ResultPath` na transiÃ§Ã£o de sucesso do ASL, entÃ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃ­a inteiramente** o `$` do Step Functions â€” todo o contexto original da execuÃ§Ã£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃ£o sÃ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃ³prio `job`) ao payload de sucesso. MudanÃ§a aditiva/reversÃ­vel sem migraÃ§Ã£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃ³rico real).
757:- **Achado de correÃ§Ã£o na prÃ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` â€” nÃ£o muda nenhum `Next`/fluxo, sÃ³ corrige o path de leitura para bater com a semÃ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
768:## Item 6 (`BedrockExtractionTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
772:**Achado real corrigido durante a implementaÃ§Ã£o, na prÃ³pria ASL** (nÃ£o uma decisÃ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` â€” `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.
790:## Item 7 (`ExtractionValidationTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
812:**Achado real corrigido nesta sessÃ£o, em cÃ³digo do item 6 jÃ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` â€” a MESMA classe de bug que o item 5 jÃ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Â§3's "achado mais sÃ©rio" Ã© sobre limpeza de artefato, mas essa categoria de "output nÃ£o reanexa o contexto anterior" jÃ¡ apareceu 3 vezes agora: item 4â†’5, item 5â†’6, e este item 6â†’7). Corrigido de forma aditiva (campos novos sÃ³ ecoam o que jÃ¡ vinha em `input`, sem recomputar nada) â€” os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃ§Ã£o (nÃ£o fazem `toEqual` no objeto inteiro). **PadrÃ£o a vigiar em qualquer implementaÃ§Ã£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃ³ximo estado vai precisar, nunca assumir que "sÃ³ preciso retornar o que eu calculei".
833:**Item 2 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: `ExtractionStarterWorker` completo â€” domÃ­nio/aplicaÃ§Ã£o/persistÃªncia/handler real, nÃ£o sÃ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` â€” portas prÃ³prias, nÃ£o reusa `DocumentStore` do mÃ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃ¡ existia â€” gated sÃ³ no registro criaria um run Ã³rfÃ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real Ã© a idempotÃªncia nativa do `StartExecution` por nome de execuÃ§Ã£o determinÃ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃ£o S3â†’EventBridgeâ†’SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃ§Ã£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` â€” insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) â€” mudanÃ§a na M6 (jÃ¡ em produÃ§Ã£o), mas segura: `cleanObject` Ã© sempre um valor opaco armazenado na entidade, nunca reconstruÃ­do/reparsed em nenhum outro lugar do cÃ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis), mas a regra EventBridge que liga o bucket limpo Ã  fila Ã© o ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` â€” com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) â€” jÃ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃªncias) â€” comportamento condicional verificado, nÃ£o sÃ³ lido no cÃ³digo. **Ainda sem PR/merge** (mesma lÃ³gica do item 1: mergeÃ¡vel isoladamente, mas ainda aguardando decisÃ£o de "quando" â€” ver abaixo).
844:## Item 8 (rotas HTTP de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
860:## Item 9 (quota `AI_CALL`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo
869:## M7 â€” estado final desta sessÃ£o: cÃ³digo-completo para `dev`
876:## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)
900:### Bugs reais encontrados
908:### Achado menor (nÃ£o corrigido, sem impacto observado nesta verificaÃ§Ã£o)
912:### Estado do ambiente apÃ³s a sessÃ£o
920:## DecisÃµes pendentes do Marcelo
926:## PendÃªncias residuais nÃ£o bloqueantes (registradas, nÃ£o bloqueiam nada)
931:- **Achado real, severidade baixa, nÃ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso â€” sÃ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` Ã© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃ³ Ã© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃ§Ã£o defensiva do status real da occurrence antes de agir â€” um ponteiro Ã³rfÃ£o nunca Ã© revisitado fora dessa janela, e mesmo se fosse, Ã© ignorado com seguranÃ§a. Diferente do bug do `upload-slot` (falha total â€” nada funcionava), este Ã© um descompasso comentÃ¡rio-vs-cÃ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃ¡ revisado 9,2/10 pelo protocolo Claudeâ†”Codex e ativo em produÃ§Ã£o real â€” nÃ£o corrigido nesta sessÃ£o deliberadamente, para nÃ£o mexer num trecho crÃ­tico jÃ¡ extensivamente revisado sem uma rodada de revisÃ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
934:## ReferÃªncias (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)
946:# docs/architecture/ â€” Ãndice e Mapa de Autoridade
957:## PrecedÃªncia de fontes (quando houver divergÃªncia)
966:## Ãndice por documento
1018:## `history/` â€” evidÃªncia de rodada, por tema
1038:# docs/engineering/ â€” Ãndice e Mapa de Autoridade
1049:## PrecedÃªncia de fontes (quando houver divergÃªncia)
1053:## Quando carregar o quÃª (roteamento por tipo de tarefa)
1071:## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)
1092:## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o
1102:   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".
1110:# W3-07 â€” Status final: REPROVADO, NÃƒO IMPLEMENTADO (2026-08-28)
1116:## Resultado
1123:## Por que nÃ£o convergiu (o achado central)
1144:## Por que nÃ£o foi aceito um "best-effort documentado" como meio-termo
1146:RecomendaÃ§Ã£o explÃ­cita do Codex, aceita: implementar um mecanismo que alega apagar
1152:## O que sobrevive desta pesquisa (reusar, nÃ£o redescobrir)
1163:- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
1172:## Gatilho para retomar
1183:# W3-07 (retomada, D-063) â€” status: PAUSADO apÃ³s Rodada 1, para planejamento minucioso na prÃ³xima sessÃ£o
1192:## O que esta rodada tentou
1195:de descoberto reativamente, como na tentativa D-062). Achado de partida:
1202:## Resultado: Rodada 1, nota 3,2/10, REPROVADA â€” pior que a rodada equivalente da tentativa anterior
1204:Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
1207:### Achado mais grave â€” o prÃ³prio fence Ã© apagÃ¡vel pela cascata que ele deveria proteger
1220:### Checagens desacopladas (leitura solta antes de agir) tÃªm corrida TOCTOU real
1228:### Auditoria de superfÃ­cies de escrita estava errada e incompleta
1251:## O que jÃ¡ estÃ¡ validado e deve ser reaproveitado, nÃ£o redescoberto
1264:## PrÃ³xima sessÃ£o â€” como retomar com rigor
1282:## Registro de decisÃ£o
1292:# W3-07 â€” Tenant Deletion Fence, Proposta Round 1 (retomada pÃ³s-D-063)
1298:## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃ£o por pasta)
1301:nÃ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:
1323:7. **Achado que muda o desenho**: apesar da superfÃ­cie parecer enorme (36 handlers, 3 classes de gatilho,
1336:## 1. O tombstone (sobrevive Ã  cascata)
1355:## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃ£o handler por handler)
1384:## 3. Fechando a ressurreiÃ§Ã£o no resolver (defesa em profundidade, nÃ£o a defesa primÃ¡ria)
1393:## 4. URLs presignadas jÃ¡ emitidas (S3)
1403:## 5. Efeitos externos irreversÃ­veis (SES, Bedrock, Textract) â€” protocolo de duas fases, nÃ£o checagem simples
1420:## 6. SequÃªncia da cascata (drenagem antes de apagar)
1434:## 7. O que este desenho NÃƒO resolve (fora de escopo explÃ­cito, registrar e nÃ£o mascarar)
1441:## 8. Por que isto nÃ£o repete os erros de D-062/D-063
1901:# W3-07 â€” Status final: REPROVADO, NÃƒO IMPLEMENTADO (2026-08-28)
1907:## Resultado
1914:## Por que nÃ£o convergiu (o achado central)
1935:## Por que nÃ£o foi aceito um "best-effort documentado" como meio-termo
1937:RecomendaÃ§Ã£o explÃ­cita do Codex, aceita: implementar um mecanismo que alega apagar
1943:## O que sobrevive desta pesquisa (reusar, nÃ£o redescobrir)
1954:- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
1963:## Gatilho para retomar
1974:# W3-07 (retomada, D-063) â€” status: PAUSADO apÃ³s Rodada 1, para planejamento minucioso na prÃ³xima sessÃ£o
1983:## O que esta rodada tentou
1986:de descoberto reativamente, como na tentativa D-062). Achado de partida:
1993:## Resultado: Rodada 1, nota 3,2/10, REPROVADA â€” pior que a rodada equivalente da tentativa anterior
1995:Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
1998:### Achado mais grave â€” o prÃ³prio fence Ã© apagÃ¡vel pela cascata que ele deveria proteger
2011:### Checagens desacopladas (leitura solta antes de agir) tÃªm corrida TOCTOU real
2019:### Auditoria de superfÃ­cies de escrita estava errada e incompleta
2042:## O que jÃ¡ estÃ¡ validado e deve ser reaproveitado, nÃ£o redescoberto
2055:## PrÃ³xima sessÃ£o â€” como retomar com rigor
2073:## Registro de decisÃ£o
2083:# W3-07 â€” Tenant Deletion Fence, Proposta Round 1 (retomada pÃ³s-D-063)
2089:## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃ£o por pasta)
2092:nÃ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:
2114:7. **Achado que muda o desenho**: apesar da superfÃ­cie parecer enorme (36 handlers, 3 classes de gatilho,
2127:## 1. O tombstone (sobrevive Ã  cascata)
2146:## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃ£o handler por handler)
2175:## 3. Fechando a ressurreiÃ§Ã£o no resolver (defesa em profundidade, nÃ£o a defesa primÃ¡ria)
2184:## 4. URLs presignadas jÃ¡ emitidas (S3)
2194:## 5. Efeitos externos irreversÃ­veis (SES, Bedrock, Textract) â€” protocolo de duas fases, nÃ£o checagem simples
2211:## 6. SequÃªncia da cascata (drenagem antes de apagar)
2225:## 7. O que este desenho NÃƒO resolve (fora de escopo explÃ­cito, registrar e nÃ£o mascarar)
2232:## 8. Por que isto nÃ£o repete os erros de D-062/D-063
5150:Nota: 2,8/10
5152:Recomendação: REPROVAR esta rodada. A direção de centralizar o fence é melhor que enumerar handlers, e o tombstone permanente corrige conceitualmente o principal erro de D-063. Porém, o desenho apresentado não é implementável como escrito e ainda deixa superfícies reais fora da garantia anunciada.
5154:## Achados bloqueantes
5156:### 1. A própria transição do lifecycle é impossível pelo wrapper proposto
5172:### 2. O bootstrap do primeiro tenant também é impossível
5190:### 3. A regra proposta não pode ser implementada pelo dependency-cruiser
5205:### 4. O inventário “19 arquivos cobrem toda escrita de tenant” não é verdadeiro
5220:### 5. O protocolo de efeitos externos tem uma janela real, mas não de “milissegundos”
5233:### 6. Bedrock e Textract não seguem integralmente o protocolo anunciado
5241:### 7. As URLs presignadas não estão fechadas apenas no “ponto de finalização”
5253:### 8. Um upload tardio pode surgir depois da “convergência zero”
5264:## Verificações sem achado bloqueante
5270:## O que a próxima proposta precisa mudar
5283:Nota: 2,8/10
5285:Recomendação: REPROVAR esta rodada. A direção de centralizar o fence é melhor que enumerar handlers, e o tombstone permanente corrige conceitualmente o principal erro de D-063. Porém, o desenho apresentado não é implementável como escrito e ainda deixa superfícies reais fora da garantia anunciada.
5287:## Achados bloqueantes
5289:### 1. A própria transição do lifecycle é impossível pelo wrapper proposto
5305:### 2. O bootstrap do primeiro tenant também é impossível
5323:### 3. A regra proposta não pode ser implementada pelo dependency-cruiser
5338:### 4. O inventário “19 arquivos cobrem toda escrita de tenant” não é verdadeiro
5353:### 5. O protocolo de efeitos externos tem uma janela real, mas não de “milissegundos”
5366:### 6. Bedrock e Textract não seguem integralmente o protocolo anunciado
5374:### 7. As URLs presignadas não estão fechadas apenas no “ponto de finalização”
5386:### 8. Um upload tardio pode surgir depois da “convergência zero”
5397:## Verificações sem achado bloqueante
5403:## O que a próxima proposta precisa mudar
5419:# W3-07 — Tenant Deletion Fence, Proposta Round 1 (retomada pós-D-063)
5425:## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, não por pasta)
5428:não suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:
5450:7. **Achado que muda o desenho**: apesar da superfície parecer enorme (36 handlers, 3 classes de gatilho,
5463:## 1. O tombstone (sobrevive à cascata)
5482:## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, não handler por handler)
5511:## 3. Fechando a ressurreição no resolver (defesa em profundidade, não a defesa primária)
5520:## 4. URLs presignadas já emitidas (S3)
5530:## 5. Efeitos externos irreversíveis (SES, Bedrock, Textract) — protocolo de duas fases, não checagem simples
5547:## 6. Sequência da cascata (drenagem antes de apagar)
5561:## 7. O que este desenho NÃO resolve (fora de escopo explícito, registrar e não mascarar)
5568:## 8. Por que isto não repete os erros de D-062/D-063
5621:1. Nota de 0 a 10 (protocolo de nota cega - nao existe nota anterior a comparar, esta e a primeira desta
5623:2. Achados REAIS, cada um confirmado contra codigo/arquitetura real (nome de arquivo/linha quando aplicavel),
5637:3. Recomendacao: aprovar (>=9.0), pedir revisao (com o que precisa mudar), ou reprovar.
5654:# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o
5658:## Estado atual
5666:**RecomendaÃ§Ã£o da sÃ­ntese final** (addendum 3/2026-08-28 em `pilot-readiness-assessment.md`): os trÃªs gates reais nomeados na sÃ­ntese original â€” guest trust (`GTR-01`/`W5-01`), evidÃªncia operacional (Wave 2), e retenÃ§Ã£o/purga real (`W3-06`) â€” **fecharam todos nesta sessÃ£o**, e todos os itens secundÃ¡rios de Wave 2 (RPO, dedupe pÃ³s-commit, pipeline de alarme de credential-compromise) tambÃ©m fecharam na mesma sessÃ£o. Ãšnico item nÃ£o-bloqueante restante: DSR completo (W3-07, pode reusar o mesmo mecanismo GSI6 claim/lease do W3-06).
5668:954 testes de backend (era 928, +12 do worker de purga +14 de `occ.ts`/deleÃ§Ã£o), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real (15/15) e `terraform plan` real contra `dev` verdes, incluindo o mÃ³dulo `document_purge_handler` novo. **`W3-06` fechado** (desenho + implementaÃ§Ã£o real, D-061), **todos os drills remanescentes da Wave 2 fechados na mesma sessÃ£o** (W2-06 RPO, W2-05 dedupe pÃ³s-commit, W2-08 pipeline de alarme). **W3-07 (cascata DSR) tentado duas vezes nesta sessÃ£o, nenhuma aprovada**: (1) D-062, 4 rodadas Claudeâ†”Codex, notas 3,4â†’5,1â†’4,7/10 â€” achado central: garantir "nÃ£o ressurreiÃ§Ã£o" exige um fence de `TenantStatus` em todo write path (HTTP+guest+workers assÃ­ncronos); Claude e Codex recomendaram convergentemente nÃ£o implementar sem esse fence, e a sessÃ£o parou por decisÃ£o do Marcelo. (2) O Marcelo entÃ£o decidiu NÃƒO esperar gatilho comercial ("se precisamos, temos que desenvolver") e pediu para retomar com o fence dentro do escopo desde o inÃ­cio â€” D-063, Rodada 1: nota 3,2/10, **pior** que a tentativa anterior. Achado mais grave desta rodada: o fence proposto (reusar `User.status`, jÃ¡ existente e jÃ¡ checado em `resolveRequestContext`) seria apagado pela prÃ³pria cascata, e o resolver re-provisiona automaticamente um `User ACTIVE` no prÃ³ximo login â€” o mecanismo de autenticaÃ§Ã£o ressuscitaria o tenant sozinho. **Pausado por pedido explÃ­cito do Marcelo para planejamento minucioso numa sessÃ£o dedicada** (nÃ£o abandonado). **PrÃ³xima aÃ§Ã£o real desta feature**: ler `docs/architecture/reviews/w3-07-tenant-deletion-with-fence-design/claude-status-paused-for-next-session.md` (roteiro completo de retomada, inclui a liÃ§Ã£o de levantar superfÃ­cies de escrita por ponto de entrada de runtime â€” API Gateway/SQS/Streams/S3/EventBridge/Scheduler/SNS/SES/Step Functions â€” nunca por pasta de cÃ³digo, que jÃ¡ falhou duas vezes) antes de propor qualquer desenho novo. Nenhum gate de pilot readiness depende disso â€” os 3 gates reais (W3-06/Wave 2/GTR-01) jÃ¡ fecharam.
5693:  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` â€” `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃ§Ã£o `requirement:read` jÃ¡ existente (sem action nova reservada de antemÃ£o, diferente de `document:read`), mesma leitura por partiÃ§Ã£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform â€” nÃ£o Ã© desta sessÃ£o, jÃ¡ estava corrigido antes (comentÃ¡rio no prÃ³prio `main.tf`), sÃ³ confirmado que segue correto.
5695:  Ambas as metades: mudanÃ§a mecÃ¢nica sobre um padrÃ£o de acesso jÃ¡ aprovado no data model â€” nÃ£o passaram pelo protocolo `AGENTS.md` Â§4 completo (nÃ£o Ã© decisÃ£o de arquitetura nova, Ã© fechamento de gap de leitura jÃ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃ­cio da sessÃ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃ£o. **Achado real corrigido depois, na mesma sessÃ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` â€” mesma classe de gap que as rotas de `DocumentRequest` jÃ¡ tiveram antes (comentÃ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
5703:## BLOCKER-B â€” implementaÃ§Ã£o completa e mergeada em `develop` (PR #50, commit `8f00160`)
5713:## Infra de hospedagem do SPA â€” etapas 1-5 fechadas (2026-08-25)
5718:2. **MÃ³dulo Terraform `infra/modules/spa-hosting` â€” IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃ­citos â†’ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default â†’ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃ©todo GET/HEAD, associada SÃ“ ao default behavior â€” nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃ©-existentes do ADR-0011 tambÃ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃ§Ã£o errada do prÃ³prio ADR-0011): `var.spa_content_security_policy` nÃ£o precisa de hashes calculados por build â€” verificado contra o `frontend/dist/index.html` real, o build do Vite nÃ£o emite nenhum `<script>`/`<style>` inline, todo asset Ã© carregado por URL externa jÃ¡ hasheada, entÃ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃ¡ existe em `frontend/index.html`) bastam sem hash nenhum â€” a variÃ¡vel ganhou um default determinÃ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃ£o consegue expressar). **MÃ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) â€” corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃ£o bloqueia o primeiro apply, sÃ³ o domÃ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro â€” confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃ£o sÃ³ contra mocks.
5732:## M7 (extraÃ§Ã£o/OCR) â€” implementaÃ§Ã£o iniciada (2026-08-25, D-057)
5781:- **Achado real corrigido durante a implementaÃ§Ã£o, nÃ£o uma decisÃ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃ£o tinha `ResultPath` na transiÃ§Ã£o de sucesso do ASL, entÃ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃ­a inteiramente** o `$` do Step Functions â€” todo o contexto original da execuÃ§Ã£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃ£o sÃ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃ³prio `job`) ao payload de sucesso. MudanÃ§a aditiva/reversÃ­vel sem migraÃ§Ã£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃ³rico real).
5783:- **Achado de correÃ§Ã£o na prÃ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` â€” nÃ£o muda nenhum `Next`/fluxo, sÃ³ corrige o path de leitura para bater com a semÃ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
5794:## Item 6 (`BedrockExtractionTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
5798:**Achado real corrigido durante a implementaÃ§Ã£o, na prÃ³pria ASL** (nÃ£o uma decisÃ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` â€” `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.
5816:## Item 7 (`ExtractionValidationTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
5838:**Achado real corrigido nesta sessÃ£o, em cÃ³digo do item 6 jÃ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` â€” a MESMA classe de bug que o item 5 jÃ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Â§3's "achado mais sÃ©rio" Ã© sobre limpeza de artefato, mas essa categoria de "output nÃ£o reanexa o contexto anterior" jÃ¡ apareceu 3 vezes agora: item 4â†’5, item 5â†’6, e este item 6â†’7). Corrigido de forma aditiva (campos novos sÃ³ ecoam o que jÃ¡ vinha em `input`, sem recomputar nada) â€” os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃ§Ã£o (nÃ£o fazem `toEqual` no objeto inteiro). **PadrÃ£o a vigiar em qualquer implementaÃ§Ã£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃ³ximo estado vai precisar, nunca assumir que "sÃ³ preciso retornar o que eu calculei".
5859:**Item 2 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: `ExtractionStarterWorker` completo â€” domÃ­nio/aplicaÃ§Ã£o/persistÃªncia/handler real, nÃ£o sÃ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` â€” portas prÃ³prias, nÃ£o reusa `DocumentStore` do mÃ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃ¡ existia â€” gated sÃ³ no registro criaria um run Ã³rfÃ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real Ã© a idempotÃªncia nativa do `StartExecution` por nome de execuÃ§Ã£o determinÃ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃ£o S3â†’EventBridgeâ†’SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃ§Ã£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` â€” insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) â€” mudanÃ§a na M6 (jÃ¡ em produÃ§Ã£o), mas segura: `cleanObject` Ã© sempre um valor opaco armazenado na entidade, nunca reconstruÃ­do/reparsed em nenhum outro lugar do cÃ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis), mas a regra EventBridge que liga o bucket limpo Ã  fila Ã© o ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` â€” com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) â€” jÃ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃªncias) â€” comportamento condicional verificado, nÃ£o sÃ³ lido no cÃ³digo. **Ainda sem PR/merge** (mesma lÃ³gica do item 1: mergeÃ¡vel isoladamente, mas ainda aguardando decisÃ£o de "quando" â€” ver abaixo).
5870:## Item 8 (rotas HTTP de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta
5886:## Item 9 (quota `AI_CALL`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo
5895:## M7 â€” estado final desta sessÃ£o: cÃ³digo-completo para `dev`
5902:## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)
5926:### Bugs reais encontrados
5934:### Achado menor (nÃ£o corrigido, sem impacto observado nesta verificaÃ§Ã£o)
5938:### Estado do ambiente apÃ³s a sessÃ£o
5946:## DecisÃµes pendentes do Marcelo
5952:## PendÃªncias residuais nÃ£o bloqueantes (registradas, nÃ£o bloqueiam nada)
5957:- **Achado real, severidade baixa, nÃ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso â€” sÃ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` Ã© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃ³ Ã© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃ§Ã£o defensiva do status real da occurrence antes de agir â€” um ponteiro Ã³rfÃ£o nunca Ã© revisitado fora dessa janela, e mesmo se fosse, Ã© ignorado com seguranÃ§a. Diferente do bug do `upload-slot` (falha total â€” nada funcionava), este Ã© um descompasso comentÃ¡rio-vs-cÃ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃ¡ revisado 9,2/10 pelo protocolo Claudeâ†”Codex e ativo em produÃ§Ã£o real â€” nÃ£o corrigido nesta sessÃ£o deliberadamente, para nÃ£o mexer num trecho crÃ­tico jÃ¡ extensivamente revisado sem uma rodada de revisÃ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
5960:## ReferÃªncias (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)
5972:# docs/architecture/ â€” Ãndice e Mapa de Autoridade
5983:## PrecedÃªncia de fontes (quando houver divergÃªncia)
5992:## Ãndice por documento
6044:## `history/` â€” evidÃªncia de rodada, por tema
6064:# docs/engineering/ â€” Ãndice e Mapa de Autoridade
6075:## PrecedÃªncia de fontes (quando houver divergÃªncia)
6079:## Quando carregar o quÃª (roteamento por tipo de tarefa)
6097:## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)
6118:## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o
6128:   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".
6136:# W3-07 â€” Status final: REPROVADO, NÃƒO IMPLEMENTADO (2026-08-28)
6142:## Resultado
6149:## Por que nÃ£o convergiu (o achado central)
6170:## Por que nÃ£o foi aceito um "best-effort documentado" como meio-termo
6172:RecomendaÃ§Ã£o explÃ­cita do Codex, aceita: implementar um mecanismo que alega apagar
6178:## O que sobrevive desta pesquisa (reusar, nÃ£o redescobrir)
6189:- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
6198:## Gatilho para retomar
6209:# W3-07 (retomada, D-063) â€” status: PAUSADO apÃ³s Rodada 1, para planejamento minucioso na prÃ³xima sessÃ£o
6218:## O que esta rodada tentou
6221:de descoberto reativamente, como na tentativa D-062). Achado de partida:
6228:## Resultado: Rodada 1, nota 3,2/10, REPROVADA â€” pior que a rodada equivalente da tentativa anterior
6230:Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
6233:### Achado mais grave â€” o prÃ³prio fence Ã© apagÃ¡vel pela cascata que ele deveria proteger
6246:### Checagens desacopladas (leitura solta antes de agir) tÃªm corrida TOCTOU real
6254:### Auditoria de superfÃ­cies de escrita estava errada e incompleta
6277:## O que jÃ¡ estÃ¡ validado e deve ser reaproveitado, nÃ£o redescoberto
6290:## PrÃ³xima sessÃ£o â€” como retomar com rigor
6308:## Registro de decisÃ£o
6318:# W3-07 â€” Tenant Deletion Fence, Proposta Round 1 (retomada pÃ³s-D-063)
6324:## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃ£o por pasta)
6327:nÃ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:
6349:7. **Achado que muda o desenho**: apesar da superfÃ­cie parecer enorme (36 handlers, 3 classes de gatilho,
6362:## 1. O tombstone (sobrevive Ã  cascata)
6381:## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃ£o handler por handler)
6410:## 3. Fechando a ressurreiÃ§Ã£o no resolver (defesa em profundidade, nÃ£o a defesa primÃ¡ria)
6419:## 4. URLs presignadas jÃ¡ emitidas (S3)
6429:## 5. Efeitos externos irreversÃ­veis (SES, Bedrock, Textract) â€” protocolo de duas fases, nÃ£o checagem simples
6446:## 6. SequÃªncia da cascata (drenagem antes de apagar)
6460:## 7. O que este desenho NÃƒO resolve (fora de escopo explÃ­cito, registrar e nÃ£o mascarar)
6467:## 8. Por que isto nÃ£o repete os erros de D-062/D-063
6927:# W3-07 â€” Status final: REPROVADO, NÃƒO IMPLEMENTADO (2026-08-28)
6933:## Resultado
6940:## Por que nÃ£o convergiu (o achado central)
6961:## Por que nÃ£o foi aceito um "best-effort documentado" como meio-termo
6963:RecomendaÃ§Ã£o explÃ­cita do Codex, aceita: implementar um mecanismo que alega apagar
6969:## O que sobrevive desta pesquisa (reusar, nÃ£o redescobrir)
6980:- **Achado estrutural**: `Session`/`LoginAttempt`/`DeviceSession` vivem em
6989:## Gatilho para retomar
7000:# W3-07 (retomada, D-063) â€” status: PAUSADO apÃ³s Rodada 1, para planejamento minucioso na prÃ³xima sessÃ£o
7009:## O que esta rodada tentou
7012:de descoberto reativamente, como na tentativa D-062). Achado de partida:
7019:## Resultado: Rodada 1, nota 3,2/10, REPROVADA â€” pior que a rodada equivalente da tentativa anterior
7021:Ver `codex-round1-critique-full.txt` para o texto completo. Achados que qualquer retomada futura
7024:### Achado mais grave â€” o prÃ³prio fence Ã© apagÃ¡vel pela cascata que ele deveria proteger
7037:### Checagens desacopladas (leitura solta antes de agir) tÃªm corrida TOCTOU real
7045:### Auditoria de superfÃ­cies de escrita estava errada e incompleta
7068:## O que jÃ¡ estÃ¡ validado e deve ser reaproveitado, nÃ£o redescoberto
7081:## PrÃ³xima sessÃ£o â€” como retomar com rigor
7099:## Registro de decisÃ£o
7109:# W3-07 â€” Tenant Deletion Fence, Proposta Round 1 (retomada pÃ³s-D-063)
7115:## 0. Levantamento exaustivo desta rodada (por ponto de entrada de runtime, nÃ£o por pasta)
7118:nÃ£o suposto pelo nome). Achados estruturais, na ordem em que mudam o desenho:
7140:7. **Achado que muda o desenho**: apesar da superfÃ­cie parecer enorme (36 handlers, 3 classes de gatilho,
7153:## 1. O tombstone (sobrevive Ã  cascata)
7172:## 2. Fence estrutural na camada de escrita (fecha os 36 pontos de uma vez, nÃ£o handler por handler)
7201:## 3. Fechando a ressurreiÃ§Ã£o no resolver (defesa em profundidade, nÃ£o a defesa primÃ¡ria)
7210:## 4. URLs presignadas jÃ¡ emitidas (S3)
7220:## 5. Efeitos externos irreversÃ­veis (SES, Bedrock, Textract) â€” protocolo de duas fases, nÃ£o checagem simples
7237:## 6. SequÃªncia da cascata (drenagem antes de apagar)
7251:## 7. O que este desenho NÃƒO resolve (fora de escopo explÃ­cito, registrar e nÃ£o mascarar)
7258:## 8. Por que isto nÃ£o repete os erros de D-062/D-063
10176:Nota: 2,8/10
10178:Recomendação: REPROVAR esta rodada. A direção de centralizar o fence é melhor que enumerar handlers, e o tombstone permanente corrige conceitualmente o principal erro de D-063. Porém, o desenho apresentado não é implementável como escrito e ainda deixa superfícies reais fora da garantia anunciada.
10180:## Achados bloqueantes
10182:### 1. A própria transição do lifecycle é impossível pelo wrapper proposto
10198:### 2. O bootstrap do primeiro tenant também é impossível
10216:### 3. A regra proposta não pode ser implementada pelo dependency-cruiser
10231:### 4. O inventário “19 arquivos cobrem toda escrita de tenant” não é verdadeiro
10246:### 5. O protocolo de efeitos externos tem uma janela real, mas não de “milissegundos”
10259:### 6. Bedrock e Textract não seguem integralmente o protocolo anunciado
10267:### 7. As URLs presignadas não estão fechadas apenas no “ponto de finalização”
10279:### 8. Um upload tardio pode surgir depois da “convergência zero”
10290:## Verificações sem achado bloqueante
10296:## O que a próxima proposta precisa mudar
10309:Nota: 2,8/10
10311:Recomendação: REPROVAR esta rodada. A direção de centralizar o fence é melhor que enumerar handlers, e o tombstone permanente corrige conceitualmente o principal erro de D-063. Porém, o desenho apresentado não é implementável como escrito e ainda deixa superfícies reais fora da garantia anunciada.
10313:## Achados bloqueantes
10315:### 1. A própria transição do lifecycle é impossível pelo wrapper proposto
10331:### 2. O bootstrap do primeiro tenant também é impossível
10349:### 3. A regra proposta não pode ser implementada pelo dependency-cruiser
10364:### 4. O inventário “19 arquivos cobrem toda escrita de tenant” não é verdadeiro
10379:### 5. O protocolo de efeitos externos tem uma janela real, mas não de “milissegundos”
10392:### 6. Bedrock e Textract não seguem integralmente o protocolo anunciado
10400:### 7. As URLs presignadas não estão fechadas apenas no “ponto de finalização”
10412:### 8. Um upload tardio pode surgir depois da “convergência zero”
10423:## Verificações sem achado bloqueante
10429:## O que a próxima proposta precisa mudar
10445:# W3-07 — Tenant Deletion Fence, Proposta Round 2
10452:## Achado adicional que muda a Rodada 2: `IdentityMapping` já é permanente por invariante existente
10460:## 1. Três primitivos distintos (corrige achado #1)
10487:## 2. Resolver corrigido (corrige achado #2 — bootstrap do primeiro tenant)
10526:## 3. Enforcement via ESLint, não dependency-cruiser (corrige achado #3)
10552:## 4. Inventário expandido além de DynamoDB (corrige achado #4)
10568:## 5. Janela de claim→efeito: bounded, não "milissegundos" (corrige achados #5 e #6)
10595:## 6. URLs presignadas — residual aceito e delimitado, não reaberto como falha de dado (refina achado #7)
10610:## 7. O que muda na sequência de descoberta+exclusão (D-062/D-061, inalterado em essência)
10615:## 8. Checklist de rastreamento dos achados da Rodada 1
10617:| # | Achado Rodada 1 | Resolução nesta rodada |
14272:# --- KMS: the AWS-managed S3 key (alias/aws/s3), NOT a customer-managed key -----------------
14273:# Real cost decision (2026-08-22, Marcelo direct call - dispensa o protocolo Claude<->Codex
14274:# por ser simplificação por custo, não reabertura de arquitetura, ver decisions-log.md):
14275:# a D-016 original (chaves separadas por bucket) previa 2 CMKs (~US$1/mês cada, ~US$2/mês fixo
14276:# só por existirem, mesmo com zero uploads em dev). AWS-managed keys não têm a cobrança mensal
14277:# de CMK - ambos os buckets agora compartilham a MESMA chave gerenciada pela AWS (só existe uma
14278:# "aws/s3" por conta, não é possível ter uma por bucket sem voltar a ser uma CMK). A separação
14279:# de segurança entre quarantine/clean continua vindo dos 2 buckets físicos distintos + IAM
14280:# least-privilege (cada função só recebe permissão explícita no bucket que precisa) - o
14281:# controle principal já era esse, a chave própria por bucket era uma camada extra, não a única.
14286:# --- Quarantine bucket -----------------------------------------------------------------------
14287:# Real invokers: DocumentService signs a presigned PutObject scoped to a fresh key here;
14288:# UploadFinalizerWorker/parser-sandbox read only the exact key/version they're told about;
14289:# MalwareResultWorker reads + deletes on confirmed promotion. No business-facing Lambda role
14290:# (items-handler etc.) ever receives any permission on this bucket.
14380:# --- Clean bucket ------------------------------------------------------------------------
14381:# Real invokers: only MalwareResultWorker writes (the promotion copy). No M6 handler reads it
14382:# (extraction/M7 will be the first real reader) - deliberately not wired to anything else yet.
14448:# --- EventBridge notifications on the quarantine bucket (S3 "Object Created" -> EventBridge) --
14449:# Required so document-malware-protection's rule (and the finalizer's own S3-event rule, wired
14450:# at root) can match on this bucket's real events.
14461:# Document upload/malware boundary buckets — M6 runtime design
14462:# (docs/architecture/reviews/m6-document-upload-design/codex-reconciliation-round2-final-design.md).
14463:# Two physically separate buckets (quarantine/clean). D-016 originally approved a dedicated
14464:# CMK per bucket; superseded 2026-08-22 (Marcelo direct cost decision, dev has near-zero real
14465:# upload volume and CMKs bill ~US$1/mo/key just for existing) - both buckets now share the
14466:# AWS-managed "aws/s3" key (see main.tf's data.aws_kms_key.s3_managed), which has no per-key
14467:# monthly charge. The security boundary between quarantine/clean is the 2 physical buckets +
14468:# IAM least-privilege (each Lambda's own capability policy), never a shared table-wide grant -
14469:# that property is unaffected by which KMS key backs the encryption.
14492:# --- KMS: same AWS-managed S3 key as document-buckets (2026-08-22 cost decision, no per-key
14493:# monthly charge, security boundary is the bucket + IAM least-privilege, not the key). ---------
14586:# EventBridge notifications (S3 "Object Created") - required for the parse-trigger rule at
14587:# root to match real events on this bucket (same pattern as document-buckets' quarantine).
14598:# Import raw CSV + validated plan JSONL bucket - M11 runtime design (D-042). A single bucket
14599:# (not a quarantine/clean pair like document-buckets) because the design explicitly scoped CSV
14600:# import OUT of malware scanning for v1 (DynamoDB never interprets a formula/script inside a
14601:# CSV cell - the injection surface belongs to a future export boundary, not import). The
14602:# `tenant/*/imports/*/raw.csv` and `tenant/*/imports/*/plan/*.jsonl` prefixes never collide, so
14603:# one bucket safely serves both the parse worker's input and its output.
14682:Nota cega: **4,1/10**
14684:Recomendação: **REPROVAR e pedir nova revisão**.
14688:## Checklist dos 8 achados da Rodada 1
14690:### 1. Primitive administrativo autocontraditório — RESOLVIDO
14702:### 2. Bootstrap do primeiro tenant — PARCIALMENTE RESOLVIDO
14722:### 3. Enforcement ESLint — NÃO RESOLVIDO
14747:### 4. Inventário completo dos writers — PARCIALMENTE RESOLVIDO
14769:### 5. Janela claim→efeito e drenagem — PARCIALMENTE RESOLVIDO
14786:### 6. Bedrock e Textract — NÃO RESOLVIDO
14799:### 7. URLs presignadas e emissão tardia — PARCIALMENTE RESOLVIDO
14817:### 8. Upload tardio depois da convergência e prazo máximo — NÃO RESOLVIDO
14836:## Achados novos introduzidos pela Rodada 2
14838:### N1 — Inserir o fence como primeiro item quebra tratamento real de `CancellationReasons`
14859:### N2 — Limite de 99 itens continua sem enforcement
14865:### N3 — O fluxo de `StartExecution` invalida a regra “efeito sempre precedido por escrita fenced nesta invocação”
14869:### N4 — A proposta não fecha a criação de artefato OCR
14877:## Síntese
14893:Nota cega: **4,1/10**
14895:Recomendação: **REPROVAR e pedir nova revisão**.
14899:## Checklist dos 8 achados da Rodada 1
14901:### 1. Primitive administrativo autocontraditório — RESOLVIDO
14913:### 2. Bootstrap do primeiro tenant — PARCIALMENTE RESOLVIDO
14933:### 3. Enforcement ESLint — NÃO RESOLVIDO
14958:### 4. Inventário completo dos writers — PARCIALMENTE RESOLVIDO
14980:### 5. Janela claim→efeito e drenagem — PARCIALMENTE RESOLVIDO
14997:### 6. Bedrock e Textract — NÃO RESOLVIDO
15010:### 7. URLs presignadas e emissão tardia — PARCIALMENTE RESOLVIDO
15028:### 8. Upload tardio depois da convergência e prazo máximo — NÃO RESOLVIDO
15047:## Achados novos introduzidos pela Rodada 2
15049:### N1 — Inserir o fence como primeiro item quebra tratamento real de `CancellationReasons`
15070:### N2 — Limite de 99 itens continua sem enforcement
15076:### N3 — O fluxo de `StartExecution` invalida a regra “efeito sempre precedido por escrita fenced nesta invocação”
15080:### N4 — A proposta não fecha a criação de artefato OCR
15088:## Síntese
1:# W3-07 — Tenant Deletion Fence, Proposta Round 3
7:## N1 — Fence anexado ao FIM da transação, não como primeiro item (corrige o achado mais grave da Rodada 2)
17:## N2 — Limite de 99 itens, enforcement explícito
24:## 2 — Bootstrap: porta estendida, não criado do zero em `shared/dynamodb` (corrige achado #2)
45:## 3 — Enforcement ESLint real, cobrindo alias e namespace import (corrige achado #3)
82:## 4/N3 — `StartExecution`/Textract: fence em toda invocação, não só na criação (corrige achado #4 e N3)
94:## 6 — Textract corrigido com o mesmo padrão do Bedrock (corrige achado #6)
104:## N4 — Artefato OCR: fence imediatamente antes do `PutObject` (corrige achado novo)
112:## 7/8 — Exclusão física de S3 verificável, substituindo a suposição de lifecycle passivo (corrige achados #7 e #8)
143:## Checklist atualizado
157:## Pendências explícitas para a próxima rodada verificar (não fechadas aqui, sinalizadas para não repetir o
158:## erro de afirmar sem checar)

2026-08-28T21:20:42.167400Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$c=Get-Content docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt; $c[14680..15090]; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round3.md'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$c=Get-Content docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt; $c[14680..15090]; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round3.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$c=Get-Content docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt; $c[14680..15090]; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round3.md'` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content docs/architecture/reviews/w3-07-tenant-fence-round2-design/codex-round2-critique-full.txt | Select-Object -Skip 14680 -First 412; Get-Content -Raw docs/architecture/reviews/w3-07-tenant-fence-round2-design/claude-proposal-round3.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 317ms:
codex
Nota cega: **4,1/10**

RecomendaÃ§Ã£o: **REPROVAR e pedir nova revisÃ£o**.

A Rodada 2 melhora substancialmente a decomposiÃ§Ã£o dos primitives, mas ainda nÃ£o sustenta a garantia de exclusÃ£o fÃ­sica/nÃ£o ressurreiÃ§Ã£o. Dos oito achados anteriores, apenas um foi integralmente resolvido; quatro ficaram parciais e trÃªs continuam nÃ£o resolvidos. AlÃ©m disso, a inserÃ§Ã£o do fence como primeiro item quebra lÃ³gica real baseada nos Ã­ndices de `CancellationReasons`.

## Checklist dos 8 achados da Rodada 1

### 1. Primitive administrativo autocontraditÃ³rio â€” RESOLVIDO

A separaÃ§Ã£o entre:

- `bootstrapTenant`;
- `fencedTransactWrite`;
- `transitionTenantLifecycle`;

remove a contradiÃ§Ã£o da Rodada 1. `ACTIVE â†’ DELETING` e `DELETING â†’ DELETED` podem ser implementados como um Ãºnico `Update` condicional da prÃ³pria linha, sem `ConditionCheck` duplicado para a mesma chave.

NÃ£o hÃ¡ impedimento do DynamoDB para essa transiÃ§Ã£o. O primitive administrativo deve permanecer inacessÃ­vel aos writers normais e usar OCC/status esperado na prÃ³pria `Update`.

### 2. Bootstrap do primeiro tenant â€” PARCIALMENTE RESOLVIDO

O bootstrap de trÃªs `Put`s Ã© vÃ¡lido no DynamoDB:

- `IdentityMapping`: `PK=IDENTITY#cognitoSub#...`, `SK=MAP`;
- lifecycle: `PK=TENANT#<tenantId>#LIFECYCLE`, `SK=LIFECYCLE`;
- User: `PK=TENANT#<tenantId>#USER#<userId>`, `SK=PROFILE`.

As trÃªs chaves sÃ£o distintas, podem estar na mesma tabela e somam somente trÃªs itens. NÃ£o hÃ¡ problema de partiÃ§Ã£o nem de limite transacional.

PorÃ©m, ele **nÃ£o Ã© implementÃ¡vel com os tipos existentes como descrito**:

- `IdentityStore` nÃ£o possui `transactWrite`; oferece somente `putIfAbsent`, `get`, `update` e `updateConditional`: [identity-store.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/ports/identity-store.ts).
- O adapter real tambÃ©m sÃ³ implementa essas operaÃ§Ãµes: [dynamodb-identity-store.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/persistence/dynamodb-identity-store.ts).
- Um primitive em `src/shared/dynamodb/tenant-lifecycle.ts` nÃ£o pode importar os tipos/construtores de `IdentityMapping` e `User` dos mÃ³dulos sem violar a direÃ§Ã£o `shared â†’ modules`. Construir os itens manualmente no shared duplicaria contratos de domÃ­nio.

Ã‰ necessÃ¡rio especificar onde o bootstrap vive e ampliar explicitamente o port de identidade â€” ou receber os trÃªs itens jÃ¡ construÃ­dos em um primitive DynamoDB genÃ©rico. A proposta atual pula essa integraÃ§Ã£o arquitetural.

HÃ¡ ainda um detalhe obrigatÃ³rio: apÃ³s `LOST_RACE`, a releitura pode retornar `undefined`; o pseudocÃ³digo atribui diretamente a `mapping`, embora o repositÃ³rio atual trate esse caso como `InternalError`.

### 3. Enforcement ESLint â€” NÃƒO RESOLVIDO

A alegaÃ§Ã£o factual da proposta estÃ¡ errada:

- NÃ£o existe `eslint.config.js`.
- O repositÃ³rio nÃ£o usa Flat Config.
- A configuraÃ§Ã£o real Ã© [`.eslintrc.cjs`](/C:/Users/Usuario/Desktop/projects/expiration-tracker/.eslintrc.cjs), no formato legado do ESLint 8.57.
- `package.json` confirma `"eslint": "^8.57.0"`: [package.json](/C:/Users/Usuario/Desktop/projects/expiration-tracker/package.json).

Portanto, o bloco apresentado na seÃ§Ã£o 3 nÃ£o pode ser inserido como escrito. Teria de ser convertido em um `override` de `.eslintrc.cjs`, com `excludedFiles` ou override posterior para os arquivos-Ã¢ncora.

O seletor tambÃ©m nÃ£o fornece a garantia estrutural anunciada. Ele detecta apenas identificadores com aqueles nomes. Estes escapes continuam vÃ¡lidos:

```ts
import { PutCommand as DynamoPut } from "@aws-sdk/lib-dynamodb";
new DynamoPut(...);
```

```ts
import * as ddb from "@aws-sdk/lib-dynamodb";
new ddb.PutCommand(...);
```

Para fechar a classe de bypass, o desenho precisa restringir tambÃ©m os imports mutÃ¡veis â€” por exemplo, `no-restricted-imports` com exceÃ§Ã£o nos anchors â€” e testar aliases e namespace imports, nÃ£o somente o nome canÃ´nico.

### 4. InventÃ¡rio completo dos writers â€” PARCIALMENTE RESOLVIDO

A tabela ficou melhor, mas ainda contÃ©m alegaÃ§Ãµes incorretas.

O caso mais claro Ã© `StartExecution`. A proposta diz que, se a criaÃ§Ã£o fenced de `ExtractionRun` falhar, a execuÃ§Ã£o nÃ£o inicia. O cÃ³digo real deliberadamente chama `startExecution` mesmo quando `putIfAbsent` retorna `false`:

- criaÃ§Ã£o/releitura: [start-extraction-run.ts:68](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts:68);
- `StartExecution` incondicional: [start-extraction-run.ts:80](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts:80).

Uma redelivery pode encontrar um `ExtractionRun` criado antes de `DELETING` e iniciar/reiniciar a execuÃ§Ã£o depois do fence sem uma escrita fenced bem-sucedida naquela invocaÃ§Ã£o. Isso contradiz a regra geral da prÃ³pria seÃ§Ã£o 4.

A busca exaustiva confirmou ainda writers externos em:

- S3: import raw/plan, OCR artifact, quarantineâ†’clean e presigned PUT;
- SES;
- SQS;
- Step Functions;
- Textract;
- `SendTaskSuccess`/`SendTaskFailure`.

A exceÃ§Ã£o do BFF tambÃ©m precisa estar formalizada: `dynamodb-session-store.ts` grava uma tabela separada e nÃ£o pode receber automaticamente um `ConditionCheck` apontando para a tabela principal.

### 5. Janela claimâ†’efeito e drenagem â€” PARCIALMENTE RESOLVIDO

Reconhecer que a janela nÃ£o Ã© de milissegundos foi correto. O lease real do e-mail Ã© cinco minutos: [email-delivery-workflow.ts:71](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/email-delivery-workflow.ts:71).

Mas `DRAIN_WINDOW_SECONDS=900` nÃ£o foi derivado de todos os tempos reais:

- guest presign: 600 s;
- import presign: **900 s exatos**, sem margem: [import-service.ts:23](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/import/application/import-service.ts:23);
- e-mail: lease de 300 s;
- SQS: mensagens permanecem atÃ© 14 dias na fila principal e quatro dias na DLQ: [sqs-worker-queue/main.tf](/C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/modules/sqs-worker-queue/main.tf);
- Textract/Bedrock usam reserva de sete dias;
- `TextractJob` tem TTL de 24 horas.

Fila/TTL longo nÃ£o exige por si sÃ³ esperar todo esse perÃ­odo, desde que toda redelivery volte a adquirir um fence. Mas isso ainda nÃ£o Ã© verdade para Textract e `StartExecution`. Logo, os 900 segundos nÃ£o dominam todos os caminhos que podem produzir efeito depois do fence.

TambÃ©m falta dizer como a espera de 15 minutos serÃ¡ executada de modo durÃ¡vel. Um `sleep` numa Lambda consumiria todo o limite mÃ¡ximo possÃ­vel e nÃ£o Ã© um mecanismo de cascata confiÃ¡vel.

### 6. Bedrock e Textract â€” NÃƒO RESOLVIDO

A proposta corrige somente Bedrock.

No cÃ³digo real, ambos aceitam uma reserva anterior como autorizaÃ§Ã£o:

- Bedrock ignora `QuotaExceededError` e segue para `bedrock.extract`: [run-bedrock-extraction.ts:144](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-bedrock-extraction.ts:144).
- Textract faz exatamente o mesmo antes de `StartDocumentTextDetection`: [start-ocr.ts:84](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:84).

A seÃ§Ã£o 5(a) manda alterar apenas `run-bedrock-extraction.ts`. Assim, uma redelivery tardia de `START_OCR`, usando uma reserva anterior ao fence, ainda pode chamar Textract depois de `DELETING`.

AlÃ©m disso, â€œa reserva desta invocaÃ§Ã£o sucedeuâ€ exige mudanÃ§a explÃ­cita no contrato de `TenantQuotaService`: hoje `consume()` retorna `void`, e o caso preexistente sÃ³ Ã© distinguido por `QuotaExceededError`. A proposta nÃ£o define o novo contrato nem como preservar retries seguros.

### 7. URLs presignadas e emissÃ£o tardia â€” PARCIALMENTE RESOLVIDO

A releitura imediatamente antes de `presignUpload` reduz a janela, mas nÃ£o a elimina. A sequÃªncia real hoje Ã© efetivamente:

1. transaÃ§Ã£o DynamoDB;
2. exclusÃ£o pode instalar `DELETING`;
3. `presignUpload`.

Isso estÃ¡ visÃ­vel em [guest-submission-service.ts:191](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/guest-submission-service.ts:191) e [guest-submission-service.ts:199](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/guest-submission-service.ts:199).

A distinÃ§Ã£o â€œcapability, nÃ£o dadoâ€ Ã© vÃ¡lida somente atÃ© alguÃ©m usar a URL. Uma URL emitida antes/depois do fence pode criar **dado fÃ­sico de tenant no S3 depois de `DELETING` ou atÃ© depois da convergÃªncia DynamoDB**. Portanto:

- Ã© correto dizer que isso nÃ£o ressuscita uma linha DynamoDB consultÃ¡vel;
- Ã© incorreto concluir que nÃ£o ressuscita dado;
- para uma feature de exclusÃ£o fÃ­sica/DSR, um objeto contendo documento ou CSV continua sendo dado pessoal, mesmo sem linha DynamoDB.

A releitura Ã© defesa em profundidade, nÃ£o fechamento da garantia.

### 8. Upload tardio depois da convergÃªncia e prazo mÃ¡ximo â€” NÃƒO RESOLVIDO

`DRAIN_WINDOW_SECONDS` nÃ£o Ã© prazo mÃ¡ximo de eliminaÃ§Ã£o. Ã‰ apenas um atraso antes de iniciar a cascata.

Depois dele ainda existem:

- Scan e re-Scans sem duraÃ§Ã£o mÃ¡xima;
- retries;
- purga de documentos;
- processamento assÃ­ncrono;
- possÃ­veis objetos S3 tardios.

Os lifecycles reais sÃ£o:

- quarantine de documentos: um dia: [document-buckets/main.tf:60](/C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/modules/document-buckets/main.tf:60);
- import raw/plan: 14 dias: [import-bucket/variables.tf](/C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/modules/import-bucket/variables.tf).

Logo, um import tardio pode sobreviver por atÃ© 14 dias. A proposta nÃ£o inclui varredura por prefixo de tenant, version IDs, delete markers ou comprovaÃ§Ã£o de zero objetos antes de `DELETED`. O lifecycle eventual nÃ£o permite declarar exclusÃ£o fÃ­sica concluÃ­da apÃ³s 900 segundos.

## Achados novos introduzidos pela Rodada 2

### N1 â€” Inserir o fence como primeiro item quebra tratamento real de `CancellationReasons`

A ordem dos itens nÃ£o importa para o commit atÃ´mico do DynamoDB, mas importa para o cÃ³digo do repositÃ³rio, que interpreta falhas por Ã­ndice.

Exemplos reais:

- `reminder-dispatch` assume que o update da occurrence Ã© Ã­ndice 0 na transaÃ§Ã£o de cancelamento e Ã­ndices 1/2/3 na transaÃ§Ã£o principal: [dispatch.ts:126](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:126), [dispatch.ts:244](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:244).
- `reminder-materializer` assume occurrence no Ã­ndice 0 e policy fence no Ã­ndice 1: [reminder-materializer.ts:312](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:312).
- Outro caminho do mesmo materializer assume a occurrence no Ã­ndice 0: [reminder-materializer.ts:373](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:373).

Ao prependar o tenant fence:

- o Ã­ndice 0 passa a ser o lifecycle;
- as condiÃ§Ãµes existentes deslocam uma posiÃ§Ã£o;
- uma falha do tenant fence pode ser classificada como corrida segura da occurrence;
- uma corrida legÃ­tima da occurrence pode deixar de ser reconhecida e virar retry.

Isso pode fazer workers reconhecerem `DELETING` como `ALREADY_TRIGGERED`, `CANCELLED_STALE` ou corrida segura. Ã‰ um defeito funcional e de seguranÃ§a real.

O wrapper deve anexar o fence no fim ou, preferivelmente, eliminar dependÃªncia de Ã­ndices rÃ­gidos por metadados/mapeamento explÃ­cito. Mesmo anexar no fim exige ajustar os casos que assumem contagem fixa.

### N2 â€” Limite de 99 itens continua sem enforcement

NÃ£o existe hoje transaÃ§Ã£o prÃ³xima de 100 itens, entÃ£o adicionar um item nÃ£o quebra um caso atual por capacidade. PorÃ©m, a exigÃªncia da Rodada 1 de rejeitar `items.length > 99` nÃ£o apareceu no Round 2.

O wrapper precisa falhar localmente antes de chamar o SDK, com teste para 99â†’100 permitido e 100â†’101 rejeitado. Sem isso, a abstraÃ§Ã£o cria uma falha futura distante do call site.

### N3 â€” O fluxo de `StartExecution` invalida a regra â€œefeito sempre precedido por escrita fenced nesta invocaÃ§Ã£oâ€

Como explicado no achado 4, `startExtractionRun` chama Step Functions mesmo quando `ExtractionRun` jÃ¡ existia. A tabela da seÃ§Ã£o 4 afirma o contrÃ¡rio. Isso Ã© uma alegaÃ§Ã£o nova e factualmente falsa contra o cÃ³digo real.

### N4 â€” A proposta nÃ£o fecha a criaÃ§Ã£o de artefato OCR

A tabela diz apenas que uma escrita precedente â€œdeve ser fencedâ€. O artefato Ã© criado em `completeOcr`, depois que o job Textract foi iniciado e concluÃ­do, por:

- `deps.artifacts.put(...)`: [complete-ocr.ts:84](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/complete-ocr.ts:84).

NÃ£o foi especificada uma aquisiÃ§Ã£o fenced na prÃ³pria invocaÃ§Ã£o imediatamente antes desse `PutObject`. Uma escrita antiga de `TextractJob` nÃ£o satisfaz a regra formulada pela Rodada 2.

## SÃ­ntese

A direÃ§Ã£o agora Ã© recuperÃ¡vel, mas a proposta ainda precisa:

1. adaptar o bootstrap Ã  arquitetura real de ports/boundaries;
2. escrever enforcement vÃ¡lido para `.eslintrc.cjs`, cobrindo aliases e namespace imports;
3. nÃ£o deslocar silenciosamente os Ã­ndices de `CancellationReasons`;
4. corrigir Textract, nÃ£o sÃ³ Bedrock;
5. fechar `StartExecution` em retries com record preexistente;
6. definir uma estratÃ©gia de S3 por prefixo/versionamento para exclusÃ£o fÃ­sica;
7. derivar a drenagem de todos os caminhos reais e executÃ¡-la de forma durÃ¡vel;
8. distinguir formalmente â€œzero linhas consultÃ¡veisâ€ de â€œzero dado fÃ­sico do titularâ€.

No estado atual, **nÃ£o aprovar**.
tokens used
118.879
Nota cega: **4,1/10**

RecomendaÃ§Ã£o: **REPROVAR e pedir nova revisÃ£o**.

A Rodada 2 melhora substancialmente a decomposiÃ§Ã£o dos primitives, mas ainda nÃ£o sustenta a garantia de exclusÃ£o fÃ­sica/nÃ£o ressurreiÃ§Ã£o. Dos oito achados anteriores, apenas um foi integralmente resolvido; quatro ficaram parciais e trÃªs continuam nÃ£o resolvidos. AlÃ©m disso, a inserÃ§Ã£o do fence como primeiro item quebra lÃ³gica real baseada nos Ã­ndices de `CancellationReasons`.

## Checklist dos 8 achados da Rodada 1

### 1. Primitive administrativo autocontraditÃ³rio â€” RESOLVIDO

A separaÃ§Ã£o entre:

- `bootstrapTenant`;
- `fencedTransactWrite`;
- `transitionTenantLifecycle`;

remove a contradiÃ§Ã£o da Rodada 1. `ACTIVE â†’ DELETING` e `DELETING â†’ DELETED` podem ser implementados como um Ãºnico `Update` condicional da prÃ³pria linha, sem `ConditionCheck` duplicado para a mesma chave.

NÃ£o hÃ¡ impedimento do DynamoDB para essa transiÃ§Ã£o. O primitive administrativo deve permanecer inacessÃ­vel aos writers normais e usar OCC/status esperado na prÃ³pria `Update`.

### 2. Bootstrap do primeiro tenant â€” PARCIALMENTE RESOLVIDO

O bootstrap de trÃªs `Put`s Ã© vÃ¡lido no DynamoDB:

- `IdentityMapping`: `PK=IDENTITY#cognitoSub#...`, `SK=MAP`;
- lifecycle: `PK=TENANT#<tenantId>#LIFECYCLE`, `SK=LIFECYCLE`;
- User: `PK=TENANT#<tenantId>#USER#<userId>`, `SK=PROFILE`.

As trÃªs chaves sÃ£o distintas, podem estar na mesma tabela e somam somente trÃªs itens. NÃ£o hÃ¡ problema de partiÃ§Ã£o nem de limite transacional.

PorÃ©m, ele **nÃ£o Ã© implementÃ¡vel com os tipos existentes como descrito**:

- `IdentityStore` nÃ£o possui `transactWrite`; oferece somente `putIfAbsent`, `get`, `update` e `updateConditional`: [identity-store.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/ports/identity-store.ts).
- O adapter real tambÃ©m sÃ³ implementa essas operaÃ§Ãµes: [dynamodb-identity-store.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/persistence/dynamodb-identity-store.ts).
- Um primitive em `src/shared/dynamodb/tenant-lifecycle.ts` nÃ£o pode importar os tipos/construtores de `IdentityMapping` e `User` dos mÃ³dulos sem violar a direÃ§Ã£o `shared â†’ modules`. Construir os itens manualmente no shared duplicaria contratos de domÃ­nio.

Ã‰ necessÃ¡rio especificar onde o bootstrap vive e ampliar explicitamente o port de identidade â€” ou receber os trÃªs itens jÃ¡ construÃ­dos em um primitive DynamoDB genÃ©rico. A proposta atual pula essa integraÃ§Ã£o arquitetural.

HÃ¡ ainda um detalhe obrigatÃ³rio: apÃ³s `LOST_RACE`, a releitura pode retornar `undefined`; o pseudocÃ³digo atribui diretamente a `mapping`, embora o repositÃ³rio atual trate esse caso como `InternalError`.

### 3. Enforcement ESLint â€” NÃƒO RESOLVIDO

A alegaÃ§Ã£o factual da proposta estÃ¡ errada:

- NÃ£o existe `eslint.config.js`.
- O repositÃ³rio nÃ£o usa Flat Config.
- A configuraÃ§Ã£o real Ã© [`.eslintrc.cjs`](/C:/Users/Usuario/Desktop/projects/expiration-tracker/.eslintrc.cjs), no formato legado do ESLint 8.57.
- `package.json` confirma `"eslint": "^8.57.0"`: [package.json](/C:/Users/Usuario/Desktop/projects/expiration-tracker/package.json).

Portanto, o bloco apresentado na seÃ§Ã£o 3 nÃ£o pode ser inserido como escrito. Teria de ser convertido em um `override` de `.eslintrc.cjs`, com `excludedFiles` ou override posterior para os arquivos-Ã¢ncora.

O seletor tambÃ©m nÃ£o fornece a garantia estrutural anunciada. Ele detecta apenas identificadores com aqueles nomes. Estes escapes continuam vÃ¡lidos:

```ts
import { PutCommand as DynamoPut } from "@aws-sdk/lib-dynamodb";
new DynamoPut(...);
```

```ts
import * as ddb from "@aws-sdk/lib-dynamodb";
new ddb.PutCommand(...);
```

Para fechar a classe de bypass, o desenho precisa restringir tambÃ©m os imports mutÃ¡veis â€” por exemplo, `no-restricted-imports` com exceÃ§Ã£o nos anchors â€” e testar aliases e namespace imports, nÃ£o somente o nome canÃ´nico.

### 4. InventÃ¡rio completo dos writers â€” PARCIALMENTE RESOLVIDO

A tabela ficou melhor, mas ainda contÃ©m alegaÃ§Ãµes incorretas.

O caso mais claro Ã© `StartExecution`. A proposta diz que, se a criaÃ§Ã£o fenced de `ExtractionRun` falhar, a execuÃ§Ã£o nÃ£o inicia. O cÃ³digo real deliberadamente chama `startExecution` mesmo quando `putIfAbsent` retorna `false`:

- criaÃ§Ã£o/releitura: [start-extraction-run.ts:68](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts:68);
- `StartExecution` incondicional: [start-extraction-run.ts:80](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts:80).

Uma redelivery pode encontrar um `ExtractionRun` criado antes de `DELETING` e iniciar/reiniciar a execuÃ§Ã£o depois do fence sem uma escrita fenced bem-sucedida naquela invocaÃ§Ã£o. Isso contradiz a regra geral da prÃ³pria seÃ§Ã£o 4.

A busca exaustiva confirmou ainda writers externos em:

- S3: import raw/plan, OCR artifact, quarantineâ†’clean e presigned PUT;
- SES;
- SQS;
- Step Functions;
- Textract;
- `SendTaskSuccess`/`SendTaskFailure`.

A exceÃ§Ã£o do BFF tambÃ©m precisa estar formalizada: `dynamodb-session-store.ts` grava uma tabela separada e nÃ£o pode receber automaticamente um `ConditionCheck` apontando para a tabela principal.

### 5. Janela claimâ†’efeito e drenagem â€” PARCIALMENTE RESOLVIDO

Reconhecer que a janela nÃ£o Ã© de milissegundos foi correto. O lease real do e-mail Ã© cinco minutos: [email-delivery-workflow.ts:71](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/email-delivery-workflow.ts:71).

Mas `DRAIN_WINDOW_SECONDS=900` nÃ£o foi derivado de todos os tempos reais:

- guest presign: 600 s;
- import presign: **900 s exatos**, sem margem: [import-service.ts:23](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/import/application/import-service.ts:23);
- e-mail: lease de 300 s;
- SQS: mensagens permanecem atÃ© 14 dias na fila principal e quatro dias na DLQ: [sqs-worker-queue/main.tf](/C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/modules/sqs-worker-queue/main.tf);
- Textract/Bedrock usam reserva de sete dias;
- `TextractJob` tem TTL de 24 horas.

Fila/TTL longo nÃ£o exige por si sÃ³ esperar todo esse perÃ­odo, desde que toda redelivery volte a adquirir um fence. Mas isso ainda nÃ£o Ã© verdade para Textract e `StartExecution`. Logo, os 900 segundos nÃ£o dominam todos os caminhos que podem produzir efeito depois do fence.

TambÃ©m falta dizer como a espera de 15 minutos serÃ¡ executada de modo durÃ¡vel. Um `sleep` numa Lambda consumiria todo o limite mÃ¡ximo possÃ­vel e nÃ£o Ã© um mecanismo de cascata confiÃ¡vel.

### 6. Bedrock e Textract â€” NÃƒO RESOLVIDO

A proposta corrige somente Bedrock.

No cÃ³digo real, ambos aceitam uma reserva anterior como autorizaÃ§Ã£o:

- Bedrock ignora `QuotaExceededError` e segue para `bedrock.extract`: [run-bedrock-extraction.ts:144](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/run-bedrock-extraction.ts:144).
- Textract faz exatamente o mesmo antes de `StartDocumentTextDetection`: [start-ocr.ts:84](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts:84).

A seÃ§Ã£o 5(a) manda alterar apenas `run-bedrock-extraction.ts`. Assim, uma redelivery tardia de `START_OCR`, usando uma reserva anterior ao fence, ainda pode chamar Textract depois de `DELETING`.

AlÃ©m disso, â€œa reserva desta invocaÃ§Ã£o sucedeuâ€ exige mudanÃ§a explÃ­cita no contrato de `TenantQuotaService`: hoje `consume()` retorna `void`, e o caso preexistente sÃ³ Ã© distinguido por `QuotaExceededError`. A proposta nÃ£o define o novo contrato nem como preservar retries seguros.

### 7. URLs presignadas e emissÃ£o tardia â€” PARCIALMENTE RESOLVIDO

A releitura imediatamente antes de `presignUpload` reduz a janela, mas nÃ£o a elimina. A sequÃªncia real hoje Ã© efetivamente:

1. transaÃ§Ã£o DynamoDB;
2. exclusÃ£o pode instalar `DELETING`;
3. `presignUpload`.

Isso estÃ¡ visÃ­vel em [guest-submission-service.ts:191](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/guest-submission-service.ts:191) e [guest-submission-service.ts:199](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/subject/application/guest-submission-service.ts:199).

A distinÃ§Ã£o â€œcapability, nÃ£o dadoâ€ Ã© vÃ¡lida somente atÃ© alguÃ©m usar a URL. Uma URL emitida antes/depois do fence pode criar **dado fÃ­sico de tenant no S3 depois de `DELETING` ou atÃ© depois da convergÃªncia DynamoDB**. Portanto:

- Ã© correto dizer que isso nÃ£o ressuscita uma linha DynamoDB consultÃ¡vel;
- Ã© incorreto concluir que nÃ£o ressuscita dado;
- para uma feature de exclusÃ£o fÃ­sica/DSR, um objeto contendo documento ou CSV continua sendo dado pessoal, mesmo sem linha DynamoDB.

A releitura Ã© defesa em profundidade, nÃ£o fechamento da garantia.

### 8. Upload tardio depois da convergÃªncia e prazo mÃ¡ximo â€” NÃƒO RESOLVIDO

`DRAIN_WINDOW_SECONDS` nÃ£o Ã© prazo mÃ¡ximo de eliminaÃ§Ã£o. Ã‰ apenas um atraso antes de iniciar a cascata.

Depois dele ainda existem:

- Scan e re-Scans sem duraÃ§Ã£o mÃ¡xima;
- retries;
- purga de documentos;
- processamento assÃ­ncrono;
- possÃ­veis objetos S3 tardios.

Os lifecycles reais sÃ£o:

- quarantine de documentos: um dia: [document-buckets/main.tf:60](/C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/modules/document-buckets/main.tf:60);
- import raw/plan: 14 dias: [import-bucket/variables.tf](/C:/Users/Usuario/Desktop/projects/expiration-tracker/infra/modules/import-bucket/variables.tf).

Logo, um import tardio pode sobreviver por atÃ© 14 dias. A proposta nÃ£o inclui varredura por prefixo de tenant, version IDs, delete markers ou comprovaÃ§Ã£o de zero objetos antes de `DELETED`. O lifecycle eventual nÃ£o permite declarar exclusÃ£o fÃ­sica concluÃ­da apÃ³s 900 segundos.

## Achados novos introduzidos pela Rodada 2

### N1 â€” Inserir o fence como primeiro item quebra tratamento real de `CancellationReasons`

A ordem dos itens nÃ£o importa para o commit atÃ´mico do DynamoDB, mas importa para o cÃ³digo do repositÃ³rio, que interpreta falhas por Ã­ndice.

Exemplos reais:

- `reminder-dispatch` assume que o update da occurrence Ã© Ã­ndice 0 na transaÃ§Ã£o de cancelamento e Ã­ndices 1/2/3 na transaÃ§Ã£o principal: [dispatch.ts:126](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:126), [dispatch.ts:244](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts:244).
- `reminder-materializer` assume occurrence no Ã­ndice 0 e policy fence no Ã­ndice 1: [reminder-materializer.ts:312](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:312).
- Outro caminho do mesmo materializer assume a occurrence no Ã­ndice 0: [reminder-materializer.ts:373](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:373).

Ao prependar o tenant fence:

- o Ã­ndice 0 passa a ser o lifecycle;
- as condiÃ§Ãµes existentes deslocam uma posiÃ§Ã£o;
- uma falha do tenant fence pode ser classificada como corrida segura da occurrence;
- uma corrida legÃ­tima da occurrence pode deixar de ser reconhecida e virar retry.

Isso pode fazer workers reconhecerem `DELETING` como `ALREADY_TRIGGERED`, `CANCELLED_STALE` ou corrida segura. Ã‰ um defeito funcional e de seguranÃ§a real.

O wrapper deve anexar o fence no fim ou, preferivelmente, eliminar dependÃªncia de Ã­ndices rÃ­gidos por metadados/mapeamento explÃ­cito. Mesmo anexar no fim exige ajustar os casos que assumem contagem fixa.

### N2 â€” Limite de 99 itens continua sem enforcement

NÃ£o existe hoje transaÃ§Ã£o prÃ³xima de 100 itens, entÃ£o adicionar um item nÃ£o quebra um caso atual por capacidade. PorÃ©m, a exigÃªncia da Rodada 1 de rejeitar `items.length > 99` nÃ£o apareceu no Round 2.

O wrapper precisa falhar localmente antes de chamar o SDK, com teste para 99â†’100 permitido e 100â†’101 rejeitado. Sem isso, a abstraÃ§Ã£o cria uma falha futura distante do call site.

### N3 â€” O fluxo de `StartExecution` invalida a regra â€œefeito sempre precedido por escrita fenced nesta invocaÃ§Ã£oâ€

Como explicado no achado 4, `startExtractionRun` chama Step Functions mesmo quando `ExtractionRun` jÃ¡ existia. A tabela da seÃ§Ã£o 4 afirma o contrÃ¡rio. Isso Ã© uma alegaÃ§Ã£o nova e factualmente falsa contra o cÃ³digo real.

### N4 â€” A proposta nÃ£o fecha a criaÃ§Ã£o de artefato OCR

A tabela diz apenas que uma escrita precedente â€œdeve ser fencedâ€. O artefato Ã© criado em `completeOcr`, depois que o job Textract foi iniciado e concluÃ­do, por:

- `deps.artifacts.put(...)`: [complete-ocr.ts:84](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/complete-ocr.ts:84).

NÃ£o foi especificada uma aquisiÃ§Ã£o fenced na prÃ³pria invocaÃ§Ã£o imediatamente antes desse `PutObject`. Uma escrita antiga de `TextractJob` nÃ£o satisfaz a regra formulada pela Rodada 2.

## SÃ­ntese

A direÃ§Ã£o agora Ã© recuperÃ¡vel, mas a proposta ainda precisa:

1. adaptar o bootstrap Ã  arquitetura real de ports/boundaries;
# W3-07 â€” Tenant Deletion Fence, Proposta Round 3

> Corrige o checklist de 8 achados da Rodada 1 (agora com status real apÃ³s Round 2: 1 resolvido, 4 parciais,
> 3 nÃ£o resolvidos) e os 4 achados novos da Rodada 2 (N1-N4), todos confirmados contra cÃ³digo real em
> `codex-round2-critique-full.txt`. Nota Round 2: 4,1/10.

## N1 â€” Fence anexado ao FIM da transaÃ§Ã£o, nÃ£o como primeiro item (corrige o achado mais grave da Rodada 2)

CÃ³digo real confirmado pelo Codex: `dispatch.ts:126/244` e `reminder-materializer.ts:312/373` assumem Ã­ndice
fixo (`occurrence` no Ã­ndice 0, `policy fence` no Ã­ndice 1) para interpretar `CancellationReasons` de uma
`TransactWriteItems` cancelada. `fencedTransactWrite` deixa de inserir o `ConditionCheck` do tenant como
primeiro item â€” passa a fazer `items.push(tenantFenceCheck)` (append), preservando todos os Ã­ndices que o
cÃ³digo de interpretaÃ§Ã£o de falha jÃ¡ depende. VerificaÃ§Ã£o exigida na implementaÃ§Ã£o real: reler cada um dos 4
call sites citados e confirmar que nenhum tambÃ©m assume "o Ãºltimo Ã­ndice Ã© sempre X" antes de fechar isto
como resolvido â€” nÃ£o presumir aqui, sinalizado explicitamente para a prÃ³xima rodada verificar contra cÃ³digo.

## N2 â€” Limite de 99 itens, enforcement explÃ­cito

`fencedTransactWrite` lanÃ§a erro sÃ­ncrono se `items.length >= 99` **antes** de anexar o item do fence (i.e.
rejeita a partir de 99 itens de entrada, nÃ£o de 100), com teste cobrindo 98â†’permitido, 99â†’rejeitado. NÃ£o
existe hoje nenhuma transaÃ§Ã£o real perto disso (maior Ã© 5 itens, `DynamoDbExtractedFieldStore`), mas o guard
Ã© barato e fecha a lacuna apontada.

## 2 â€” Bootstrap: porta estendida, nÃ£o criado do zero em `shared/dynamodb` (corrige achado #2)

Confirmado: `IdentityStore` (`src/modules/identity/ports/identity-store.ts`) nÃ£o tem `transactWrite`, e um
primitivo em `shared/dynamodb` nÃ£o pode importar tipos de `IdentityMapping`/`User` (regra de fronteira
`shared` â†’ `modules` jÃ¡ existente, `AGENTS.md` Â§7). CorreÃ§Ã£o: o bootstrap nÃ£o vive em `shared/dynamodb` â€”
vive em `src/modules/identity/application/bootstrap-tenant.ts`, mesmo mÃ³dulo que jÃ¡ possui os tipos de
domÃ­nio, e usa um `transactWrite(entries: TransactWriteEntry[]): Promise<void>` **novo mÃ©todo no port
`IdentityStore`** (mesmo padrÃ£o que `dynamodb-document-store.ts`/`dynamodb-expiration-store.ts` jÃ¡
implementam para seus prÃ³prios mÃ³dulos â€” nÃ£o Ã© um padrÃ£o novo, Ã© estender o port de identidade ao mesmo nÃ­vel
dos outros). `TransactWriteEntry`/builders de `occ.ts` continuam genÃ©ricos em `shared/dynamodb` (sem mudanÃ§a
ali) â€” sÃ³ o *chamador* concreto do bootstrap fica no mÃ³dulo de identidade, que Ã© o Ãºnico que precisa conhecer
a forma real de `IdentityMapping`/`TenantLifecycleRecord`/`User`.

`TenantLifecycleRecord` (chave/status, sem lÃ³gica de domÃ­nio) fica declarado em `shared/dynamodb/` como um
tipo estrutural puro (`{ PK, SK, status }`), sem depender de nenhum mÃ³dulo â€” todo mÃ³dulo que precisa do
`ConditionCheck` do fence (nÃ£o sÃ³ identity) usa sÃ³ essa forma estrutural, nunca importa o mÃ³dulo identity.

CorreÃ§Ã£o do bug de tratamento do `LOST_RACE` apontado pelo Codex: `bootstrapTenant` propaga o
`InternalError` que `IdentityMappingRepository.findOrCreate`/`find` jÃ¡ lanÃ§a quando o mapeamento
"desaparece" apÃ³s perder a corrida (nÃ£o silencia, nÃ£o presume `mapping` definido sem checar).

## 3 â€” Enforcement ESLint real, cobrindo alias e namespace import (corrige achado #3)

Confirmado: o repo usa `.eslintrc.cjs` (ESLint 8.57, config legado, nÃ£o Flat Config) â€” a Rodada 2 propÃ´s
sintaxe errada. CorreÃ§Ã£o, como `overrides` em `.eslintrc.cjs`:

```js
overrides: [
  {
    files: ["src/**/*.ts"],
    excludedFiles: [
      "src/shared/dynamodb/tenant-lifecycle.ts",
      "src/modules/identity/application/bootstrap-tenant.ts",
    ],
    rules: {
      "no-restricted-imports": ["error", {
        paths: [{
          name: "@aws-sdk/lib-dynamodb",
          importNames: ["PutCommand", "UpdateCommand", "DeleteCommand", "TransactWriteCommand"],
          message: "Construa comandos DynamoDB mutÃ¡veis sÃ³ via tenant-lifecycle.ts (W3-07 fence).",
        }],
      }],
      "no-restricted-syntax": ["error", {
        // cobre `new ddb.PutCommand(...)` (namespace import), que no-restricted-imports nÃ£o pega
        selector: "NewExpression[callee.type='MemberExpression'][callee.property.name=/^(Put|Update|Delete|TransactWrite)Command$/]",
        message: "Construa comandos DynamoDB mutÃ¡veis sÃ³ via tenant-lifecycle.ts (W3-07 fence).",
      }],
    },
  },
],
```

`no-restricted-imports` com `importNames` casa pelo nome **exportado original**, nÃ£o pelo alias local â€” cobre
`import { PutCommand as X }`. A regra `no-restricted-syntax` cobre o caso de namespace (`import * as ddb`).
Combinadas, fecham as duas classes de bypass que o Codex demonstrou. Fixtures de teste obrigatÃ³rias na
implementaÃ§Ã£o real: os dois exemplos de bypass do Codex, mais o caso direto `new PutCommand(...)`, todos
devem falhar fora dos anchors e passar dentro.

## 4/N3 â€” `StartExecution`/Textract: fence em toda invocaÃ§Ã£o, nÃ£o sÃ³ na criaÃ§Ã£o (corrige achado #4 e N3)

Achado confirmado: `start-extraction-run.ts:68-80` chama `StartExecution` **incondicionalmente**, mesmo
quando `putIfAbsent` retorna `false` (redelivery de uma mensagem jÃ¡ processada). A tabela da Rodada 2 estava
errada. CorreÃ§Ã£o real: toda invocaÃ§Ã£o de `startExtractionRun` â€” criaÃ§Ã£o nova OU redelivery de uma jÃ¡
existente â€” precisa de uma escrita fenced fresca **nesta invocaÃ§Ã£o**, nÃ£o apenas na primeira vez. Trocar o
`putIfAbsent` condicional simples por uma transiÃ§Ã£o de status fenced (`ExtractionRun.status: "CREATED" â†’
"STARTING"`, via `fencedTransactWrite`) que roda sempre, inclusive em redelivery â€” se a transiÃ§Ã£o falhar
(porque jÃ¡ estÃ¡ `STARTING`/`COMPLETED`, ou porque o fence de tenant falhou), `StartExecution` nÃ£o Ã© chamado.
Isso tambÃ©m fecha a classe geral: "efeito externo sÃ³ depois de escrita fenced desta invocaÃ§Ã£o", sem exceÃ§Ã£o
de "jÃ¡ existia".

## 6 â€” Textract corrigido com o mesmo padrÃ£o do Bedrock (corrige achado #6)

`start-ocr.ts:84` tem o mesmo bug do Bedrock (ignora `QuotaExceededError` de reserva prÃ©-existente e chama
`StartDocumentTextDetection` mesmo assim). CorreÃ§Ã£o idÃªntica Ã  do Bedrock: sÃ³ prossegue se a reserva desta
invocaÃ§Ã£o suceder. Contrato de `TenantQuotaService.consume()` muda de `Promise<void>` para
`Promise<"RESERVED" | "ALREADY_RESERVED">` â€” call sites que hoje toleram uma reserva prÃ©-existente como
autorizaÃ§Ã£o (Bedrock e Textract) passam a exigir `"RESERVED"` especificamente antes do efeito externo; outros
call sites do `consume()` que nÃ£o tÃªm esse requisito (ex.: `API_REQUEST` do middleware HTTP) continuam
aceitando qualquer um dos dois valores sem mudanÃ§a de comportamento.

## N4 â€” Artefato OCR: fence imediatamente antes do `PutObject` (corrige achado novo)

`complete-ocr.ts:84` escreve o artefato OCR via `deps.artifacts.put(...)` depois que o job Textract jÃ¡
concluiu â€” a escrita de `TextractJob` que precede isso na mesma funÃ§Ã£o Ã© antiga (feita em `startOcr`, minutos
antes), nÃ£o satisfaz a regra de "fenced nesta invocaÃ§Ã£o". CorreÃ§Ã£o: `completeOcr` faz uma transiÃ§Ã£o fenced
prÃ³pria (`TextractJob.status: "IN_PROGRESS" â†’ "COMPLETING"`, via `fencedTransactWrite`) imediatamente antes
da chamada a `deps.artifacts.put(...)` â€” se falhar, o artefato nunca Ã© escrito.

## 7/8 â€” ExclusÃ£o fÃ­sica de S3 verificÃ¡vel, substituindo a suposiÃ§Ã£o de lifecycle passivo (corrige achados #7 e #8)

Achado aceito integralmente: a distinÃ§Ã£o "zero linha DynamoDB consultÃ¡vel" â‰  "zero dado fÃ­sico do titular" Ã©
real, e um documento/CSV em S3 Ã© dado pessoal mesmo sem linha DynamoDB. CorreÃ§Ã£o estrutural, nÃ£o paliativa:

**Toda key S3 de dado de tenant jÃ¡ segue (ou passa a seguir) o prefixo `tenant/<tenantId>/`** â€” confirmado jÃ¡
verdadeiro para `ImportService.rawObjectKey` (`tenant/${tenantId}/imports/${jobId}/raw.csv`); a implementaÃ§Ã£o
real precisa confirmar/padronizar o mesmo para as keys de `Document` (quarantine/clean) e artefato OCR, que
hoje podem nÃ£o seguir essa convenÃ§Ã£o â€” item de verificaÃ§Ã£o explÃ­cito para a prÃ³xima rodada, nÃ£o presumido
aqui.

O passo final da cascata deixa de ser "espera 900s e assume": passa a ser uma **varredura ativa e
verificÃ¡vel por bucket**, rodando depois do `DRAIN_WINDOW_SECONDS` (ainda necessÃ¡rio para efeitos SES/quota/
outbox jÃ¡ em voo, mas nÃ£o mais a garantia de exclusÃ£o fÃ­sica):

1. Para cada bucket relevante (quarantine, clean, import, OCR-transient): `ListObjectsV2({ Prefix:
   "tenant/<tenantId>/" })`, incluindo `ListObjectVersions` nos buckets versionados (confirmar quais
   buckets tÃªm versionamento â€” `spa-hosting`/`document-buckets` jÃ¡ usam versionamento por outro motivo,
   verificar na implementaÃ§Ã£o real quais dos buckets de dado de tenant tambÃ©m tÃªm).
2. `DeleteObjects` em lote (atÃ© 1000 por chamada) de tudo que a listagem retornar, incluindo todas as
   versÃµes e delete markers.
3. Repetir (1)+(2) atÃ© a listagem retornar vazia (convergÃªncia, mesmo padrÃ£o jÃ¡ usado para o Scan DynamoDB).
4. SÃ³ entÃ£o `transitionTenantLifecycle(tenantId, "DELETING", "DELETED")`.

Isso responde diretamente ao achado #8 ("prazo mÃ¡ximo de eliminaÃ§Ã£o"): nÃ£o Ã© mais um nÃºmero fixo assumido â€”
Ã© "atÃ© a varredura por prefixo convergir em zero", igual ao mecanismo jÃ¡ aprovado para DynamoDB (D-062),
aplicado agora a S3. Uma URL presignada usada depois do passo (1)-(3) jÃ¡ ter rodado uma vez cria um objeto
novo que a prÃ³xima iteraÃ§Ã£o do loop (1)-(3) ainda descobre â€” sÃ³ fecha de vez quando a listagem vem vazia
duas vezes seguidas (mesma disciplina de "convergÃªncia" que o Scan DynamoDB jÃ¡ usa), nÃ£o numa Ãºnica
passagem.

## Checklist atualizado

| # | Achado | Status apÃ³s Round 3 |
|---|---|---|
| N1 | Fence quebra Ã­ndices de `CancellationReasons` | Corrigido â€” append, nÃ£o prepend |
| N2 | Limite de 99 itens | Corrigido â€” guard explÃ­cito + teste |
| 2 | Bootstrap nÃ£o implementÃ¡vel nos ports reais | Corrigido â€” vive em `modules/identity`, port `IdentityStore` estendido |
| 3 | ESLint real (nÃ£o dependency-cruiser, nÃ£o Flat Config) | Corrigido â€” `.eslintrc.cjs` overrides, cobre alias+namespace |
| 4/N3 | `StartExecution` incondicional em redelivery | Corrigido â€” fence em toda invocaÃ§Ã£o via transiÃ§Ã£o de status |
| 6 | Textract com o mesmo bug do Bedrock | Corrigido â€” mesmo padrÃ£o, contrato de `consume()` muda |
| N4 | Artefato OCR sem fence na invocaÃ§Ã£o | Corrigido â€” transiÃ§Ã£o fenced imediatamente antes do `PutObject` |
| 7/8 | "Zero linha" â‰  "zero dado fÃ­sico"; sem prazo real | Corrigido â€” varredura S3 por prefixo atÃ© convergÃªncia, substitui a espera fixa |
| 5 | `DRAIN_WINDOW_SECONDS` nÃ£o derivado dos tempos reais | Mantido sÃ³ para efeitos SES/outbox em voo (300s de lease); a garantia de exclusÃ£o fÃ­sica agora vem da convergÃªncia S3 (item 7/8), nÃ£o do prazo fixo |

## PendÃªncias explÃ­citas para a prÃ³xima rodada verificar (nÃ£o fechadas aqui, sinalizadas para nÃ£o repetir o
## erro de afirmar sem checar)

- Confirmar se as keys de `Document`/artefato OCR jÃ¡ seguem `tenant/<tenantId>/...` ou precisam de migraÃ§Ã£o.
- Confirmar quais buckets de dado de tenant tÃªm versionamento habilitado (afeta se `ListObjectVersions` Ã©
  necessÃ¡rio alÃ©m de `ListObjectsV2`).
- Reverificar os 4 call sites de `CancellationReasons` por Ã­ndice depois do append, incluindo qualquer
  suposiÃ§Ã£o de "Ãºltimo Ã­ndice" que o append possa quebrar.


codex
A proposta corrige várias descrições anteriores, mas isso ainda não basta: estou verificando agora três pontos de alto risco que podem invalidá-la mesmo no papel — semântica das transições idempotentes de extração/OCR, compatibilidade do novo retorno de quota com todos os consumidores e se a suposta convergência S3 possui um prefixo realmente enumerável em cada bucket.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/identity/ports/identity-store.ts; Get-Content -Raw src/modules/identity/persistence/identity-mapping-repository.ts; Get-Content -Raw src/workers/reminder-dispatch/dispatch.ts; Get-Content -Raw src/modules/reminder/application/reminder-materializer.ts; Get-Content -Raw .eslintrc.cjs; Get-Content -Raw package.json' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 174ms:
/**
 * Minimal DynamoDB surface the identity module needs, following the SDK-agnostic port
 * pattern established in src/shared/idempotency/idempotency.ts (M0): callers inject an
 * implementation of this port, so the module is unit-testable without live AWS. M1's
 * infra layer (infra/lib) provisions the real table; src/modules/identity/persistence
 * wires a DocumentClient-backed adapter to this same port for production use.
 */
export interface EntityKey {
  PK: string;
  SK: string;
}

export interface IdentityStore {
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** Unconditional overwrite - only for bookkeeping writes with no concurrent-writer risk (see quota.ts for the counter case, which needs updateConditional instead). */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * PutItem gated on the counter field(s) still matching `expected` at write time (full audit
   * round1, eixo GovernanÃ§a de Produto, criterio 3 - TenantQuota.consume() did a
   * read-modify-write over the unconditional `update`, allowing lost updates under concurrent
   * requests for the same tenant/quotaType). Returns false on conditional-check failure so the
   * caller can re-read and retry instead of silently overwriting a concurrent writer's count.
   */
  updateConditional<T extends EntityKey>(item: T, expected: { count: number; resetAt: string }): Promise<boolean>;
}

/**
 * IdentityMapping repository â€” data-model.md Â§2/Â§"IdentityMapping" +
 * implementation-blueprint.md Â§23.1/Â§7.3: PK=IDENTITY#cognitoSub#<sub>, SK=MAP, created
 * atomically (ConditionExpression attribute_not_exists(PK)) on first login. This is one
 * of the two documented exceptions to the "every key starts with TENANT#tenantId" rule
 * (the other is GSI3) â€” the lookup happens *before* tenantId is known.
 */
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";
import { InternalError } from "../../../shared/errors/app-error.js";

export interface IdentityMapping {
  PK: string;
  SK: "MAP";
  entityType: "IdentityMapping";
  cognitoSub: string;
  userId: string;
  tenantId: string;
  createdAt: string;
}

export function identityMappingKey(cognitoSub: string): EntityKey {
  return { PK: `IDENTITY#cognitoSub#${cognitoSub}`, SK: "MAP" };
}

export class IdentityMappingRepository {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  async find(cognitoSub: string): Promise<IdentityMapping | undefined> {
    return this.store.get<IdentityMapping>(identityMappingKey(cognitoSub));
  }

  /**
   * Atomically creates the mapping on first login. MVP: tenantId=userId (data-model.md
   * Â§7.3 "No MVP, tenantId=userId, mas o cÃ³digo recebe ambos como conceitos separados"),
   * so newUserId is used for both â€” callers must never derive one from the other outside
   * this function once Organizations (FUT-001) exist.
   *
   * Idempotent under races: if two concurrent first-logins for the same sub both call
   * this, exactly one PutItem wins (attribute_not_exists(PK)); the loser re-reads and
   * returns the winner's mapping instead of erroring, so first-login is safe to retry.
   */
  async findOrCreate(cognitoSub: string, newUserId: string, newTenantId: string): Promise<IdentityMapping> {
    const existing = await this.find(cognitoSub);
    if (existing) {
      return existing;
    }

    const key = identityMappingKey(cognitoSub);
    const candidate: IdentityMapping = {
      ...key,
      SK: "MAP",
      entityType: "IdentityMapping",
      cognitoSub,
      userId: newUserId,
      tenantId: newTenantId,
      createdAt: this.now(),
    };

    const created = await this.store.putIfAbsent(candidate);
    if (created) {
      return candidate;
    }

    // Lost the race: someone else created it first-login-atomically between our
    // find() and putIfAbsent(). Re-read; if it's still missing something is badly
    // wrong (a delete of an IdentityMapping is not a supported operation).
    const winner = await this.find(cognitoSub);
    if (!winner) {
      throw new InternalError("IdentityMapping vanished after losing creation race.", { cognitoSub });
    }
    return winner;
  }
}

/**
 * Reminder dispatch worker core logic - implementation-blueprint.md Â§9.4. Consumes one
 * `reminder.dispatch.v1` command (produced by the ReminderProducer, Â§9.3) and performs the
 * CLAIMED -> TRIGGERED transition, creating exactly one `NotificationIntent` idempotently.
 *
 * Validation order per Â§9.4: occurrence must be CLAIMED (not SCHEDULED directly - the claim
 * prevents double-processing by two concurrent dispatch workers); item must be ACTIVE;
 * versions must match; schedule must be within tolerance; policy must still exist. If any
 * check fails, the occurrence is conditionally cancelled (CLAIMED -> CANCELLED) instead of
 * silently dropped, and repair is left to reconciliation (Â§9.5) - never a new ad-hoc
 * materialization from inside dispatch.
 *
 * On success, everything commits in ONE TransactWriteItems (Â§9.4: "corrige a lacuna de
 * durabilidade... antes o evento era tratado como reconstruÃ­vel sem outbox"):
 * NotificationIntent Put + occurrence CLAIMED->TRIGGERED Update + idempotency record Put
 * (`tenantId|occurrenceId`, Â§9.4's exact key for NotificationIntentCreated consumers) +
 * outbox `notification.intent-created.v1` Put.
 */
import { buildVersionConditionCheck, buildVersionedUpdate } from "../../shared/dynamodb/occ.js";
import { appendToTransaction } from "../../shared/outbox/outbox.js";
import { buildIdempotencyKey } from "../../shared/idempotency/idempotency.js";
import type { DomainEvent } from "../../shared/contracts/events.js";
import { itemKey } from "../../modules/expiration/domain/expiration-item.js";
import { policyKey, type ReminderPolicy } from "../../modules/reminder/domain/reminder-policy.js";
import type { ReminderOccurrence } from "../../modules/reminder/domain/reminder-occurrence.js";
import { intentKey, type NotificationIntent } from "../../modules/reminder/domain/notification-intent.js";
import { isTransactionCanceled, type ReminderStore, type TransactWriteEntry } from "../../modules/reminder/ports/reminder-store.js";
import type { DispatchCommand } from "../reminder-producer/producer.js";

const NOTIFICATION_INTENT_CREATED = "notification.intent-created.v1";

export interface DispatchDeps {
  store: ReminderStore;
  tableName: string;
  now: () => string;
  newIntentId: () => string;
  newEventId: () => string;
  correlationId: () => string;
  /** Max allowed lag between scheduledAt and now before a claimed occurrence is considered stale (default 30 minutes - generous vs. the claim TTL, covers legitimate SQS/Lambda retry delay). */
  toleranceMs?: number;
}

export type DispatchOutcome =
  | { kind: "TRIGGERED"; intent: NotificationIntent }
  | { kind: "ALREADY_TRIGGERED" }
  | { kind: "CANCELLED_STALE"; reason: string }
  | { kind: "SKIPPED_NOT_CLAIMED" }
  | { kind: "ABORTED_FRESHNESS_RACE" };

/** Looks up the occurrence by (itemId, occurrenceId) - occurrences are co-located under the item's own partition (data-model.md Â§2), but the command doesn't carry the SK's scheduledAt segment, so the worker queries the item's occurrences and finds the match by occurrenceId. Kept here rather than in ReminderStore to avoid growing the port for a single caller. */
async function findOccurrence(store: ReminderStore, tenantId: string, itemId: string, occurrenceId: string): Promise<ReminderOccurrence | undefined> {
  const rows = await store.queryByItem<ReminderOccurrence>(tenantId, itemId);
  return rows.find((r) => r.occurrenceId === occurrenceId);
}

export async function dispatchOccurrence(deps: DispatchDeps, command: DispatchCommand): Promise<DispatchOutcome> {
  const { tenantId } = command;
  const { itemId, occurrenceId, itemVersion, policyVersion } = command.data;

  const occurrence = await findOccurrence(deps.store, tenantId, itemId, occurrenceId);
  if (!occurrence) {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }
  if (occurrence.status === "TRIGGERED") {
    return { kind: "ALREADY_TRIGGERED" };
  }
  if (occurrence.status !== "CLAIMED") {
    return { kind: "SKIPPED_NOT_CLAIMED" };
  }

  const item = await deps.store.get<{ PK: string; SK: string; status: string; version: number }>(itemKey(tenantId, itemId));
  const policy = await deps.store.get<ReminderPolicy>(policyKey(tenantId, occurrence.policyId));

  const toleranceMs = deps.toleranceMs ?? 30 * 60_000;
  const withinTolerance = Math.abs(Date.parse(deps.now()) - Date.parse(occurrence.scheduledAt)) <= toleranceMs;

  const stale =
    !item ||
    item.status !== "ACTIVE" ||
    item.version !== itemVersion ||
    occurrence.itemVersion !== itemVersion ||
    !policy ||
    policy.version !== policyVersion ||
    !policy.enabled ||
    !withinTolerance;

  if (stale) {
    const reason = !item
      ? "ITEM_NOT_FOUND"
      : item.status !== "ACTIVE"
        ? "ITEM_NOT_ACTIVE"
        : item.version !== itemVersion || occurrence.itemVersion !== itemVersion
          ? "ITEM_VERSION_MISMATCH"
          : !policy || !policy.enabled
            ? "POLICY_GONE_OR_DISABLED"
            : policy.version !== policyVersion
              ? "POLICY_VERSION_MISMATCH"
              : "OUT_OF_TOLERANCE";

    try {
      await deps.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: deps.tableName,
            key: { PK: occurrence.PK, SK: occurrence.SK },
            tenantId,
            expectedVersion: occurrence.version,
            set: { status: "CANCELLED" },
            // M3.5: the claim's WORKSTATE#CLAIMED GSI6 pointer (written by the producer)
            // stops applying the moment this claim is resolved - leaving it would make the
            // reconciliation job find a claim that's already been handled.
            remove: ["GSI6PK", "GSI6SK"],
          }),
        },
      ]);
    } catch (err) {
      // Codex Round E/F finding: mirrors D2's exact defect on this second, older
      // transaction - TransactionCanceledException is not synonymous with "the occurrence's
      // own condition lost" (throttling/other cancellation reasons must be retried, not
      // silently treated as an already-resolved stale occurrence). Only entry 0 (this
      // transaction's sole Update) failing its own ConditionalCheckFailed is provably safe
      // to swallow - Round F caught that the first fix still fell through to "swallow" when
      // `CancellationReasons` was absent entirely (`reasons && !occurrenceConditionFailed`
      // is falsy when `reasons` is undefined); this must rethrow whenever it cannot prove
      // the specific reason, exactly like the success-path catch below already does.
      const reasons = (err as { CancellationReasons?: Array<{ Code?: string }> }).CancellationReasons;
      const occurrenceConditionFailed = reasons?.[0]?.Code === "ConditionalCheckFailed";
      if (!isTransactionCanceled(err) || !occurrenceConditionFailed) throw err;
    }
    return { kind: "CANCELLED_STALE", reason };
  }

  const now = deps.now();
  const intentId = deps.newIntentId();
  const intent: NotificationIntent = {
    ...intentKey(tenantId, intentId),
    entityType: "NotificationIntent",
    intentId,
    tenantId,
    kind: "EXPIRATION_REMINDER",
    itemId,
    occurrenceId,
    itemVersion,
    policyId: occurrence.policyId,
    policyVersion,
    scheduledAt: occurrence.scheduledAt,
    requestedChannels: policy.channels.filter((c) => !(policy.optOutChannels ?? []).includes(c)),
    status: "PENDING",
    supersedesIntentId: null,
    correctionReason: null,
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const entries: TransactWriteEntry[] = [
    { Put: { TableName: deps.tableName, Item: { ...intent }, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" } },
    {
      Update: buildVersionedUpdate({
        tableName: deps.tableName,
        key: { PK: occurrence.PK, SK: occurrence.SK },
        tenantId,
        expectedVersion: occurrence.version,
        set: { status: "TRIGGERED" },
        remove: ["GSI6PK", "GSI6SK"],
      }),
    },
    // Freshness fence (BLOCKER-B, Codex Round B CRITICAL finding): the `stale` check above
    // reads item/policy moments before this transaction commits - without re-asserting those
    // exact facts atomically here, a policy disable/update or item archive/delete landing in
    // that gap would still produce a real NotificationIntent for an item/policy that is no
    // longer current by the time this transaction is durable. Re-asserted as ConditionChecks
    // (not re-reads) so the whole transaction, including the NotificationIntent Put, is
    // atomically gated on both still holding.
    buildVersionConditionCheck({ tableName: deps.tableName, key: itemKey(tenantId, itemId), expectedVersion: itemVersion, extra: { status: "ACTIVE" } }),
    buildVersionConditionCheck({
      tableName: deps.tableName,
      key: policyKey(tenantId, occurrence.policyId),
      expectedVersion: policyVersion,
      extra: { enabled: true },
    }),
  ];

  // Idempotency record for NotificationIntentCreated consumers (Â§9.4: "tenantId|occurrenceId").
  const idem = buildIdempotencyKey(deps.tableName, tenantId, "reminder.dispatch", occurrenceId);
  entries.push({
    Put: {
      TableName: deps.tableName,
      Item: {
        PK: idem.PK,
        SK: idem.SK,
        entityType: "IdempotencyRecord",
        tenantId,
        operation: "reminder.dispatch",
        requestHash: occurrenceId,
        status: "COMPLETED",
        responseRef: intentId,
        expiresAt: new Date(Date.parse(now) + 7 * 24 * 60 * 60_000).toISOString(),
        createdAt: now,
        completedAt: now,
      },
      ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
    },
  });

  const event: DomainEvent = {
    specVersion: "1.0",
    eventId: deps.newEventId(),
    eventType: NOTIFICATION_INTENT_CREATED,
    source: "expiration-tracker.reminder",
    occurredAt: now,
    correlationId: deps.correlationId(),
    tenantId,
    actor: { type: "SYSTEM" },
    aggregate: { type: "NotificationIntent", id: intentId, version: 1 },
    data: {
      intentId,
      kind: "EXPIRATION_REMINDER",
      itemId,
      occurrenceId,
      itemVersion,
      policyId: occurrence.policyId,
      policyVersion,
      scheduledAt: occurrence.scheduledAt,
      requestedChannels: intent.requestedChannels,
      status: "PENDING",
      supersedesIntentId: null,
      correctionReason: null,
    },
  };
  appendToTransaction(entries, deps.tableName, event);

  try {
    await deps.store.transactWrite(entries);
  } catch (err) {
    if (isTransactionCanceled(err)) {
      // Codex Round D real defect fix: entry index alone doesn't prove which race occurred -
      // ANY entry can fail (throttling, a genuine idempotency collision at index 4, an
      // unrelated outbox condition at index 5), and treating every cancellation as one of
      // this handler's two known-safe outcomes silently swallowed failures the SQS handler
      // needs to retry (it acks every returned outcome, never just an exception - see
      // reminder-dispatch-handler.ts). Only the SPECIFIC entries whose failure is provably
      // safe to swallow are recognized here; everything else rethrows so the caller retries.
      const reasons = (err as { CancellationReasons?: Array<{ Code?: string }> }).CancellationReasons;
      const failed = (i: number) => reasons?.[i]?.Code === "ConditionalCheckFailed";

      // Entry 1: the occurrence's own CLAIMED->TRIGGERED condition already advanced -
      // duplicate delivery of the same command racing a prior success on THIS occurrence.
      if (failed(1)) {
        return { kind: "ALREADY_TRIGGERED" };
      }
      // Entries 2/3: the freshness fence itself lost a race against a concurrent
      // policy/item change - the occurrence is untouched (still CLAIMED), correctly
      // re-evaluated by a later dispatch attempt or reconciliation, never a duplicate send.
      if (failed(2) || failed(3)) {
        return { kind: "ABORTED_FRESHNESS_RACE" };
      }
      // Any other cancellation (idempotency/outbox condition, throttling, missing
      // CancellationReasons from a non-conforming store) is not provably safe to treat as a
      // known no-op outcome - rethrow so the handler reports it as a batch item failure and
      // SQS retries, rather than silently acknowledging an unexplained failure.
      throw err;
    }
    throw err;
  }

  return { kind: "TRIGGERED", intent };
}

/**
 * ReminderMaterializer â€” implementation-blueprint.md Â§9.1's `ReminderMaterializer`
 * interface + Â§9.2 (occurrence shape/GSI3 keys) + Â§8.3 (reacting to ItemDueDateChanged:
 * "Consumer do Reminder cancela ocorrÃªncias antigas por condiÃ§Ã£o de versÃ£o... e
 * materializa as novas em lotes idempotentes. Evento repetido produz o mesmo resultado.").
 * Closes Red Team scenario 13 (stale reminders after a due-date edit).
 *
 * Idempotency (data-model.md Â§4): "tenantId|itemId|itemVersion|policyId|policyVersion|
 * triggerId|scheduledAtUtc". Judgment call: rather than a separate IdempotencyStore
 * record (M0's IdempotencyStore, used elsewhere for operations whose result isn't itself
 * a uniquely-keyed row), the occurrenceId is DERIVED deterministically from this exact
 * key (stableHash) and creation goes through ReminderStore.putIfAbsent's
 * attribute_not_exists(PK) condition - the occurrence row IS the idempotency record, same
 * pattern data-model.md Â§4 documents for WebhookInbox ("PK+SK jÃ¡ Ã© a chave de
 * idempotÃªncia"). A retried/duplicated materialize() call computes the same occurrenceId
 * and its putIfAbsent no-ops.
 */
import {
  zonedTimeToUtc,
  toCalendarDate,
  addCalendarDays,
  parseDayOffset,
  parseLocalTime,
  timeZoneObservesDst,
} from "../domain/recurrence.js";
import { gsi3Keys, occurrenceKey, stableHash, type ReminderOccurrence } from "../domain/reminder-occurrence.js";
import { policyKey, type ReminderPolicy, type QuietHours } from "../domain/reminder-policy.js";
import type { ShardConfig } from "../domain/shard-config.js";
import { activeGenerations } from "../domain/shard-config.js";
import { buildVersionConditionCheck, buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";
import { isTransactionCanceled, type ReminderStore } from "../ports/reminder-store.js";
import { GSI6PK_WORKSTATE_DST_PENDING, buildDstCandidateGsi6Sk } from "../ports/reconciliation-candidate-source.js";

export interface MaterializeInput {
  tenantId: string;
  itemId: string;
  itemVersion: number;
  itemDueDate: string; // ISO date or date-time
  policy: ReminderPolicy;
  shardConfig: ShardConfig;
}

export interface MaterializeResult {
  created: ReminderOccurrence[];
  skippedExisting: number;
}

function applyQuietHours(hour: number, minute: number, quietHours: QuietHours | undefined): { hour: number; minute: number } {
  if (!quietHours) return { hour, minute };
  const start = parseLocalTime(quietHours.startLocalTime);
  const end = parseLocalTime(quietHours.endLocalTime);
  const t = hour * 60 + minute;
  const s = start.hour * 60 + start.minute;
  const e = end.hour * 60 + end.minute;
  const inWindow = s <= e ? t >= s && t < e : t >= s || t < e; // handles windows crossing midnight
  if (!inWindow) return { hour, minute };
  return { hour: end.hour, minute: end.minute };
}

function toLocalIsoString(parts: { year: number; month: number; day: number; hour: number; minute: number }): string {
  const pad = (n: number, len = 2) => String(n).padStart(len, "0");
  return `${parts.year}-${pad(parts.month)}-${pad(parts.day)}T${pad(parts.hour)}:${pad(parts.minute)}:00`;
}

function idempotencyKey(input: {
  tenantId: string;
  itemId: string;
  itemVersion: number;
  policyId: string;
  policyVersion: number;
  triggerId: string;
  scheduledAtUtc: string;
}): string {
  return [
    input.tenantId,
    input.itemId,
    input.itemVersion,
    input.policyId,
    input.policyVersion,
    input.triggerId,
    input.scheduledAtUtc,
  ].join("|");
}

export class ReminderMaterializer {
  constructor(
    private readonly store: ReminderStore,
    private readonly tableName: string,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  /** Computes the scheduledAt (UTC) + local wall-clock string for one trigger, given the item's dueDate. Pure - no I/O - exposed for unit testing DST edge cases directly. */
  computeSchedule(input: {
    itemDueDate: string;
    offsetIso: string;
    localTime: string;
    timeZone: string;
    quietHours?: QuietHours;
  }): { scheduledAtUtc: string; localScheduledAt: string; dstKind: "NORMAL" | "AMBIGUOUS" | "NONEXISTENT" } {
    const baseDate = toCalendarDate(input.itemDueDate);
    const offsetDays = parseDayOffset(input.offsetIso);
    const targetDate = addCalendarDays(baseDate, offsetDays);
    const { hour: rawHour, minute: rawMinute } = parseLocalTime(input.localTime);
    const { hour, minute } = applyQuietHours(rawHour, rawMinute, input.quietHours);

    const resolution = zonedTimeToUtc(
      { year: targetDate.year, month: targetDate.month, day: targetDate.day, hour, minute },
      input.timeZone,
    );

    return {
      scheduledAtUtc: new Date(resolution.utcMillis).toISOString(),
      localScheduledAt: toLocalIsoString({ ...targetDate, hour, minute }),
      dstKind: resolution.kind,
    };
  }

  /**
   * Materializes every trigger of `policy` for the given item/version. Idempotent: safe
   * to call repeatedly for the same (itemVersion, policyVersion) - existing occurrences
   * are left untouched (skippedExisting), only missing ones are created.
   */
  async materialize(input: MaterializeInput): Promise<MaterializeResult> {
    const created: ReminderOccurrence[] = [];
    let skippedExisting = 0;

    if (!input.policy.enabled) {
      return { created, skippedExisting };
    }

    const generation = activeGenerations(input.shardConfig, this.now())[0]!; // current generation

    for (const trigger of input.policy.triggers) {
      const schedule = this.computeSchedule({
        itemDueDate: input.itemDueDate,
        offsetIso: trigger.offsetIso,
        localTime: trigger.localTime,
        timeZone: input.policy.timeZone,
        quietHours: input.policy.quietHours,
      });

      const key = idempotencyKey({
        tenantId: input.tenantId,
        itemId: input.itemId,
        itemVersion: input.itemVersion,
        policyId: input.policy.policyId,
        policyVersion: input.policy.version,
        triggerId: trigger.triggerId,
        scheduledAtUtc: schedule.scheduledAtUtc,
      });
      const occurrenceId = `occ_${stableHash(key).toString(16)}`;

      const now = this.now();
      const gsi3 = gsi3Keys({
        tenantId: input.tenantId,
        occurrenceId,
        scheduledAt: schedule.scheduledAtUtc,
        shardCount: generation.shardCount,
      });

      // M3.5 (docs/architecture/m3.5-runtime-design.md Â§"ReconciliaÃ§Ã£o"; broadened after
      // Codex's implementation review found the original AMBIGUOUS/NONEXISTENT-only trigger
      // too narrow): a GSI6 WORKSTATE#DST_PENDING pointer is set whenever EITHER the
      // schedule was computed at an ambiguous/nonexistent local time (dstKind !== "NORMAL")
      // OR the policy's timeZone observes DST at all in the occurrence's target year - a
      // plain 09:00 reminder in a DST-observing zone can still need re-evaluation after an
      // offset change even though its own computed instant was never ambiguous. Occurrences
      // in a fixed-offset timeZone (e.g. America/Sao_Paulo since 2019) never get the pointer.
      const dstRelevant =
        schedule.dstKind !== "NORMAL" || timeZoneObservesDst(input.policy.timeZone, toCalendarDate(schedule.scheduledAtUtc).year);
      const dstPending = dstRelevant
        ? {
            GSI6PK: GSI6PK_WORKSTATE_DST_PENDING,
            GSI6SK: buildDstCandidateGsi6Sk(schedule.scheduledAtUtc, input.tenantId, occurrenceId),
          }
        : {};

      const occurrence: ReminderOccurrence = {
        ...occurrenceKey(input.tenantId, input.itemId, schedule.scheduledAtUtc, occurrenceId),
        entityType: "ReminderOccurrence",
        occurrenceId,
        tenantId: input.tenantId,
        itemId: input.itemId,
        policyId: input.policy.policyId,
        triggerId: trigger.triggerId,
        scheduledAt: schedule.scheduledAtUtc,
        localScheduledAt: schedule.localScheduledAt,
        timeZone: input.policy.timeZone,
        originalRule: { offset: trigger.offsetIso, localTime: trigger.localTime },
        itemVersion: input.itemVersion,
        policyVersion: input.policy.version,
        shard: gsi3.shard,
        shardFnVersion: generation.shardFnVersion,
        status: "SCHEDULED",
        version: 1,
        createdAt: now,
        updatedAt: now,
        GSI3PK: gsi3.GSI3PK,
        GSI3SK: gsi3.GSI3SK,
        ...dstPending,
      };

      const wasCreated = await this.store.putIfAbsent(occurrence);
      if (wasCreated) {
        created.push(occurrence);
      } else {
        skippedExisting += 1;
      }
    }

    return { created, skippedExisting };
  }

  /**
   * Reacts to ItemDueDateChanged (implementation-blueprint.md Â§8.3): cancels occurrences
   * materialized under a stale itemVersion (still SCHEDULED/CLAIMED), one conditional
   * update per occurrence - NOT a single unbounded transaction (Â§8.3: "nenhuma transaÃ§Ã£o
   * tenta cancelar um nÃºmero ilimitado de ocorrÃªncias de uma sÃ³ vez"). Returns the count
   * cancelled; callers (the outbox consumer / M3's due-date-change worker) then call
   * materialize() per active policy to create the fresh occurrences.
   */
  async cancelStaleOccurrences(input: { tenantId: string; itemId: string; currentItemVersion: number }): Promise<number> {
    const occurrences = await this.store.queryByItem<ReminderOccurrence>(input.tenantId, input.itemId);
    let cancelled = 0;
    for (const occurrence of occurrences) {
      if (occurrence.itemVersion >= input.currentItemVersion) continue;
      if (occurrence.status !== "SCHEDULED" && occurrence.status !== "CLAIMED") continue;
      try {
        await this.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: this.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: input.tenantId,
              expectedVersion: occurrence.version,
              // Full-audit round1 (Arquitetura, Data Model & Consistency) fix: both GSI3 and
              // GSI6 pointers are now removed on cancellation. Previously only GSI6 was
              // removed (occ.ts's builder is SET-only; REMOVE support was added in M3.5 for
              // GSI6 but never extended to GSI3) - the scheduler index kept a stale pointer.
              // It was a harmless no-op in practice (the producer's SCHEDULED->CLAIMED
              // condition simply fails for a CANCELLED row), but it left a real, unbounded
              // data-consistency residue in the single-table's most sensitive index (GSI3 is
              // the one index with a deliberate cross-tenant-shaped key, per data-model.md
              // Â§3's isolation safeguard) with no cleanup mechanism ever proposed. Removing it
              // here keeps GSI3 containing only live SCHEDULED/CLAIMED work, matching the
              // invariant already enforced for GSI6.
              set: { status: "CANCELLED" },
              remove: ["GSI3PK", "GSI3SK", "GSI6PK", "GSI6SK"],
            }),
          },
        ]);
        cancelled += 1;
      } catch (err) {
        if (isTransactionCanceled(err)) continue; // lost a race (already advanced) - fine, not our job to cancel it
        throw err;
      }
    }
    return cancelled;
  }

  /**
   * Shared implementation for the two policy-fenced reconcile methods below
   * (reminder-delivery-pipeline.md Â§6/Â§4's unified policy-changed loop). Cancels
   * SCHEDULED/CLAIMED occurrences belonging to `policy.policyId` under `itemId` that match
   * `shouldCancel`, each in its own TransactWriteItems ALSO containing a ConditionCheck on
   * the policy row asserting it is still at `policy.version` (Codex Round F/G finding -
   * `buildVersionConditionCheck`, the same mechanism as the dispatch fence, commit
   * `3eeda33`). If the policy has moved on by commit time, that specific cancellation is
   * safely skipped rather than wrongly applied - the event that moved the policy again
   * always fires its own reconcile, which sees fresh state.
   */
  private async cancelOccurrencesFencedByPolicy(input: {
    tenantId: string;
    itemId: string;
    policy: ReminderPolicy;
    shouldCancel: (occurrence: ReminderOccurrence) => boolean;
  }): Promise<number> {
    const occurrences = await this.store.queryByItem<ReminderOccurrence>(input.tenantId, input.itemId);
    let cancelled = 0;
    for (const occurrence of occurrences) {
      if (occurrence.policyId !== input.policy.policyId) continue;
      if (occurrence.status !== "SCHEDULED" && occurrence.status !== "CLAIMED") continue;
      if (!input.shouldCancel(occurrence)) continue;
      try {
        await this.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: this.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: input.tenantId,
              expectedVersion: occurrence.version,
              set: { status: "CANCELLED" },
              remove: ["GSI3PK", "GSI3SK", "GSI6PK", "GSI6SK"],
            }),
          },
          buildVersionConditionCheck({
            tableName: this.tableName,
            key: policyKey(input.tenantId, input.policy.policyId),
            expectedVersion: input.policy.version,
          }),
        ]);
        cancelled += 1;
      } catch (err) {
        // Codex implementation-review finding (real defect, same class D/E/F already fixed
        // in dispatch.ts): TransactionCanceledException is not synonymous with "the occurrence
        // advanced or the policy moved on" - throttling/an unrelated cancellation reason must
        // be retried, not silently treated as an already-resolved race. Only entry 0 (the
        // occurrence's own condition) or entry 1 (the policy fence) failing with
        // ConditionalCheckFailed is provably one of the two expected/safe races; anything else
        // rethrows so the caller (the trigger worker's SQS handler) reports a batch item
        // failure and SQS retries.
        const reasons = (err as { CancellationReasons?: Array<{ Code?: string }> }).CancellationReasons;
        const occurrenceOrFenceFailed = reasons?.[0]?.Code === "ConditionalCheckFailed" || reasons?.[1]?.Code === "ConditionalCheckFailed";
        if (isTransactionCanceled(err) && occurrenceOrFenceFailed) continue;
        throw err;
      }
    }
    return cancelled;
  }

  /**
   * For `itemId` as `policy`'s CURRENT target (reminder-delivery-pipeline.md Â§6): cancels
   * occurrences that are stale relative to the policy's current version, or whose policy is
   * now disabled. Leaves occurrences already at the current version/enabled state alone.
   */
  async reconcilePolicyOccurrences(input: { tenantId: string; itemId: string; policy: ReminderPolicy }): Promise<number> {
    return this.cancelOccurrencesFencedByPolicy({
      ...input,
      shouldCancel: (occurrence) => occurrence.policyVersion !== input.policy.version || !input.policy.enabled,
    });
  }

  /**
   * For `itemId` as a partition `policy` no longer targets (reminder-delivery-pipeline.md
   * Â§4/Â§5's unified policy-changed loop, `previousItemId` side): cancels EVERY live
   * occurrence for this policy under this item, regardless of version - there is no
   * "current version" to compare against, since the policy doesn't target this item at all
   * anymore. Never materializes here.
   */
  async reconcilePolicyOccurrencesUnconditionally(input: { tenantId: string; itemId: string; policy: ReminderPolicy }): Promise<number> {
    return this.cancelOccurrencesFencedByPolicy({ ...input, shouldCancel: () => true });
  }

  /**
   * For a terminal item transition (archive/delete/renewal-old-side,
   * reminder-delivery-pipeline.md Â§4/Â§8's `item-deactivated` event): cancels every live
   * occurrence under the item regardless of which policy owns it - no policy fence, since
   * this isn't gated on any one policy's state, only on the item itself being terminal
   * (already committed by the caller's own transaction before this event fires).
   */
  async cancelAllOccurrences(input: { tenantId: string; itemId: string }): Promise<number> {
    const occurrences = await this.store.queryByItem<ReminderOccurrence>(input.tenantId, input.itemId);
    let cancelled = 0;
    for (const occurrence of occurrences) {
      if (occurrence.status !== "SCHEDULED" && occurrence.status !== "CLAIMED") continue;
      try {
        await this.store.transactWrite([
          {
            Update: buildVersionedUpdate({
              tableName: this.tableName,
              key: { PK: occurrence.PK, SK: occurrence.SK },
              tenantId: input.tenantId,
              expectedVersion: occurrence.version,
              set: { status: "CANCELLED" },
              remove: ["GSI3PK", "GSI3SK", "GSI6PK", "GSI6SK"],
            }),
          },
        ]);
        cancelled += 1;
      } catch (err) {
        // Same fix as cancelOccurrencesFencedByPolicy above - only the occurrence's own
        // (sole) condition failing is provably safe to swallow.
        const reasons = (err as { CancellationReasons?: Array<{ Code?: string }> }).CancellationReasons;
        if (isTransactionCanceled(err) && reasons?.[0]?.Code === "ConditionalCheckFailed") continue;
        throw err;
      }
    }
    return cancelled;
  }
}

/**
 * Lint conventions per implementation-blueprint.md #14.1: "Todos os handlers usam
 * SecureLogger. Chamadas diretas a console.* falham no lint." src/shared/observability
 * is the only place allowed to touch console.* (it's the sink the logger writes to).
 */
module.exports = {
  root: true,
  parser: "@typescript-eslint/parser",
  parserOptions: {
    ecmaVersion: 2022,
    sourceType: "module",
  },
  plugins: ["@typescript-eslint"],
  extends: [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
  ],
  env: {
    node: true,
    es2022: true,
  },
  // prototype/ is plain browser JS (window/document/location globals), deliberately isolated
  // from production code (see prototype/README.md) â€” it is not part of this Node/TS ruleset.
  // frontend/ is a separate npm project (own package.json/lockfile/.eslintrc.cjs, its own CI
  // job) â€” its plugins live in frontend/node_modules, unreachable from this root install.
  // infra/modules/spa-hosting/spa-routing.js is CloudFront Function runtime JS (ADR-0011) -
  // its `handler` export is invoked by the CloudFront runtime itself, never imported by any
  // TS module here, so no-unused-vars flags it incorrectly; it has its own unit test coverage
  // (test/unit/infra/spa-routing.test.ts) instead of lint coverage.
  ignorePatterns: ["dist/", "cdk.out/", "coverage/", "node_modules/", "prototype/", "frontend/", "infra/modules/spa-hosting/spa-routing.js"],
  rules: {
    "no-console": "error",
    "@typescript-eslint/no-unused-vars": ["error", { argsIgnorePattern: "^_" }],
    "@typescript-eslint/no-explicit-any": "warn",
  },
  overrides: [
    {
      files: ["src/shared/observability/**/*.ts"],
      rules: {
        "no-console": "off",
      },
    },
    {
      files: ["test/**/*.ts", "**/*.test.ts"],
      rules: {
        "@typescript-eslint/no-explicit-any": "off",
      },
    },
    {
      files: ["scripts/**/*.ts"],
      rules: {
        "no-console": "off",
      },
    },
    {
      // Architecture boundary, IDE-speed layer only (Engineering Maturity Review G10,
      // 2026-08-19). IMPORTANT: no-restricted-imports matches the literal import-specifier
      // TEXT, not the resolved module graph - it cannot see a transitive re-export (domain
      // file A imports domain file B, which imports application/), and this codebase's
      // relative imports (e.g. "../ports/foo.js") often don't even literally contain
      // "modules/" for the cross-module patterns below to match. The AUTHORITATIVE boundary
      // check is `npm run check-boundaries` (.dependency-cruiser.cjs, wired into CI) - it
      // resolves the real graph. This ESLint rule stays only for fast in-editor feedback on
      // the direct-import case; do not treat it as sufficient on its own.
      files: ["src/modules/*/domain/**/*.ts"],
      rules: {
        "no-restricted-imports": [
          "error",
          {
            patterns: [
              { group: ["**/infra/**"], message: "domain/ must not depend on infra/ (architecture boundary)." },
              { group: ["aws-sdk", "@aws-sdk/*"], message: "domain/ must be AWS-SDK-agnostic (architecture boundary)." },
              { group: ["**/modules/*/application/**", "**/modules/*/ports/**", "**/modules/*/http/**", "**/modules/*/persistence/**"], message: "domain/ must not import another module's application/ports/http/persistence layers (architecture boundary)." },
            ],
          },
        ],
      },
    },
  ],
};

{
  "name": "expiration-tracker",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Micro-SaaS de controle de vencimentos/renovacoes - guardrails e contratos (M0)",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "check-boundaries": "depcruise src --config .dependency-cruiser.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:dynamodb": "vitest run --config vitest.dynamodb.config.ts",
    "validate-schemas": "tsx scripts/validate-schemas.ts",
    "check-docs": "tsx scripts/check-doc-drift.ts",
    "check-spa-build-artifacts": "node scripts/check-spa-build-artifacts.mjs",
    "build:lambdas": "tsx scripts/build-lambdas.ts"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.145",
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "@vitest/coverage-v8": "^1.6.0",
    "aws-jwt-verify": "5.2.1",
    "dependency-cruiser": "17.4.3",
    "esbuild": "0.24.0",
    "eslint": "^8.57.0",
    "testcontainers": "10.13.2",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@aws-sdk/client-appconfigdata": "3.1114.0",
    "@aws-sdk/client-bedrock-runtime": "3.1114.0",
    "@aws-sdk/client-dynamodb": "3.1114.0",
    "@aws-sdk/client-kms": "3.1114.0",
    "@aws-sdk/client-lambda": "3.1114.0",
    "@aws-sdk/client-s3": "3.1114.0",
    "@aws-sdk/client-sesv2": "3.1114.0",
    "@aws-sdk/client-sfn": "3.1114.0",
    "@aws-sdk/client-sqs": "3.1114.0",
    "@aws-sdk/client-textract": "3.1114.0",
    "@aws-sdk/lib-dynamodb": "3.1114.0",
    "@aws-sdk/s3-request-presigner": "3.1114.0",
    "@aws-sdk/util-dynamodb": "3.996.9",
    "ajv": "^8.16.0",
    "ajv-formats": "^2.1.1",
    "pdf-lib": "1.17.1",
    "ulid": "^2.3.0"
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/extraction/application/start-extraction-run.ts; Get-Content -Raw src/modules/extraction/application/complete-ocr.ts; Get-Content -Raw src/modules/extraction/application/start-ocr.ts; Get-Content -Raw src/modules/extraction/application/run-bedrock-extraction.ts; rg -n \"\\.consume\\(|consume\\(\" src test | Select-Object -First 300; rg -n \"class TenantQuota|interface TenantQuota|async consume|consume\\(\" src/modules/identity" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 254ms:
/**
 * ExtractionStarterWorker's pure orchestration (`implementation-blueprint.md` Â§12.5,
 * `claude-reconciliation-final-design.md` Â§1.1/Â§2 "LoadMetadata"). Triggered by the S3
 * "Object Created" event on the clean bucket; creates the idempotent `ExtractionRun` and, only
 * on the FIRST successful creation, starts the Step Functions Standard execution.
 */
import { documentKey, type Document } from "../../document/domain/document.js";
import { extractionRunKey, deriveExtractionRunId, type ExtractionRun } from "../domain/extraction-run.js";
import { PIPELINE_VERSION_V1 } from "../domain/field-schema.js";
import type { DocumentReader } from "../ports/document-reader.js";
import type { ExtractionRunStore } from "../ports/extraction-run-store.js";
import type { ExtractionExecutionStarter } from "../ports/extraction-execution-starter.js";

export interface StartExtractionRunDeps {
  documents: DocumentReader;
  runs: ExtractionRunStore;
  executions: ExtractionExecutionStarter;
  now?: () => string;
}

export interface StartExtractionRunInput {
  tenantId: string;
  itemId: string;
  documentId: string;
  cleanObject: { bucket: string; key: string; versionId: string };
}

export type StartExtractionRunOutcome = "STARTED" | "ALREADY_RUNNING" | "DOCUMENT_NOT_FOUND";

/** The clean-bucket promotion copy and the Document's own `status: "CLEAN"` transition are two
 * separate writes (advanceAfterEvidence() copies the object BEFORE the transactional status
 * update) - a real, expected race where the S3 event can arrive before the Document read below
 * observes CLEAN yet. Distinct from DOCUMENT_NOT_FOUND (never retryable - the document genuinely
 * doesn't exist), this IS retryable: thrown, not returned, so the runtime handler's normal SQS
 * batch-item-failure path redelivers it instead of silently dropping a real, in-flight upload. */
export class DocumentNotCleanYetError extends Error {
  constructor(documentId: string, status: string) {
    super(`Document ${documentId} is not CLEAN yet (status=${status}) - retrying.`);
    this.name = "DocumentNotCleanYetError";
  }
}

export async function startExtractionRun(deps: StartExtractionRunDeps, input: StartExtractionRunInput): Promise<StartExtractionRunOutcome> {
  const doc = await deps.documents.get<Document>(documentKey(input.tenantId, input.itemId, input.documentId), true);
  if (!doc) return "DOCUMENT_NOT_FOUND";
  if (doc.status !== "CLEAN") throw new DocumentNotCleanYetError(input.documentId, doc.status);

  const documentVersion = doc.version;
  const pipelineVersion = PIPELINE_VERSION_V1;
  const runId = deriveExtractionRunId(input.tenantId, input.documentId, documentVersion, pipelineVersion);
  const now = deps.now?.() ?? new Date().toISOString();

  const run: ExtractionRun = {
    ...extractionRunKey(input.tenantId, input.documentId, runId),
    entityType: "ExtractionRun",
    tenantId: input.tenantId,
    itemId: input.itemId,
    documentId: input.documentId,
    documentVersion,
    runId,
    pipelineVersion,
    status: "RUNNING",
    startedAt: now,
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const created = await deps.runs.putIfAbsent(run);

  // startExecution is called EVERY time, whether the DynamoDB record was just created or
  // already existed - deliberately not gated on `created`. Gating it would orphan a run
  // whenever putIfAbsent succeeds but the subsequent startExecution call fails transiently
  // (network blip, throttling): the record would sit in RUNNING forever with no real Step
  // Functions execution behind it, and a retry would find the record already there and skip
  // starting one. Instead, the real dedup mechanism is Step Functions' own execution-name
  // uniqueness (`runId` as `name`, see ExtractionExecutionStarter's doc comment) - calling
  // StartExecution again with the same name and the same input is itself idempotent at the
  // AWS API level, so this stays safe to call unconditionally on every retry.
  await deps.executions.startExecution({
    name: runId,
    input: {
      tenantId: input.tenantId,
      itemId: input.itemId,
      documentId: input.documentId,
      documentVersion,
      runId,
      pipelineVersion,
      cleanObject: input.cleanObject,
      // RunTextract's classifier needs these (see ExtractionExecutionInput's doc comment) -
      // `doc` was already fetched above, no extra read.
      fileName: doc.fileName,
      contentType: doc.mediaType,
    },
  });

  return created ? "STARTED" : "ALREADY_RUNNING";
}

/**
 * `TextractTaskHandler`'s `COMPLETE_OCR` operation â€” triggered by the SQS queue subscribed to
 * Textract's SNS job-completion topic, NOT by Step Functions directly (`claude-reconciliation-
 * final-design.md` Â§1.2/Â§2/Â§3). Looks up the `TextractJob` by `jobId`, pages
 * `GetDocumentTextDetection`, persists the OCR artifact, and resolves the parked
 * `waitForTaskToken` task via `SendTaskSuccess`/`SendTaskFailure`.
 *
 * Governing invariant (design Â§3, closed after 3 correction rounds): this function must NEVER
 * delete the transient OCR artifact, in any outcome â€” only `ExtractionValidationTaskHandler`
 * (a later handler, not yet implemented) does that, at run closure. `taskTokenCiphertext` is
 * cleared only on a TERMINAL SendTask* outcome (success, or the three terminal errors); any
 * other SendTask* error is rethrown so the SQS message redelivers with the token still intact.
 */
import { textractJobKey, type TextractJob } from "../domain/textract-job.js";
import { TextractPartialFailureError } from "../../../shared/errors/app-error.js";
import type { TextractClient, TextractJobStatusResult } from "../ports/textract-client.js";
import type { TextractJobStore } from "../ports/textract-job-store.js";
import type { OcrArtifactStore, ExtractionArtifactRef } from "../ports/ocr-artifact-store.js";
import type { TaskTokenEncryptor } from "../ports/task-token-encryptor.js";
import type { TaskTokenSender } from "../ports/task-token-sender.js";

export interface CompleteOcrDeps {
  textract: TextractClient;
  jobs: TextractJobStore;
  artifacts: OcrArtifactStore;
  tokenEncryptor: TaskTokenEncryptor;
  sender: TaskTokenSender;
  now?: () => string;
}

export interface CompleteOcrInput {
  jobId: string;
}

export type CompleteOcrOutcome =
  | "SUCCEEDED"
  | "PARTIAL_SUCCEEDED"
  | "FAILED_REPORTED"
  | "ORPHAN_JOB"
  | "ALREADY_FINALIZED";

async function collectPages(textract: TextractClient, jobId: string): Promise<{ status: TextractJobStatusResult; blocks: unknown[]; warnings: string[] }> {
  const blocks: unknown[] = [];
  const warnings = new Set<string>();
  let status: TextractJobStatusResult = "IN_PROGRESS";
  let nextToken: string | undefined;
  do {
    const page = await textract.getDocumentTextDetectionPage(jobId, nextToken);
    status = page.status;
    blocks.push(...page.blocks);
    for (const w of page.warnings ?? []) warnings.add(w);
    nextToken = page.nextToken;
  } while (nextToken);
  return { status, blocks, warnings: [...warnings] };
}

export async function completeOcr(deps: CompleteOcrDeps, input: CompleteOcrInput): Promise<CompleteOcrOutcome> {
  const job = await deps.jobs.getByJobId(input.jobId);
  if (!job) {
    // Reconciliation per design Â§2: confirm the job is real, discard the result, never error.
    try {
      await deps.textract.getDocumentTextDetectionPage(input.jobId);
    } catch {
      // Confirmation is best-effort only â€” no run is waiting on this job either way.
    }
    return "ORPHAN_JOB";
  }

  if (!job.taskTokenCiphertext) {
    // Already finalized by a previous (possibly concurrent) delivery of the same completion
    // notification â€” idempotent no-op, never a duplicate SendTask* call.
    return "ALREADY_FINALIZED";
  }

  const { status, blocks, warnings } = await collectPages(deps.textract, input.jobId);

  let artifact: ExtractionArtifactRef | undefined;
  let outcome: CompleteOcrOutcome;
  let sendSuccess: boolean;
  let sendPayload: unknown;
  let sendErrorCode: string | undefined;

  if (status === "SUCCEEDED" || status === "PARTIAL_SUCCESS") {
    artifact = await deps.artifacts.put(job.runId, JSON.stringify(blocks));
    const effectiveWarnings = status === "PARTIAL_SUCCESS" ? [...new Set([...warnings, "PARTIAL_OCR"])] : warnings;
    sendSuccess = true;
    // Re-attaches the original execution context (item 5, D-057 pending decision) - this
    // SendTaskSuccess payload becomes the ENTIRE Step Functions `$` for every state after
    // `RunTextract` on the happy path (no ResultPath on that Task's success transition), so
    // without these fields RunDeterministicParser would have no tenantId/documentId/runId/
    // pipelineVersion to work with.
    sendPayload = {
      ocrAvailable: true,
      artifact,
      warnings: effectiveWarnings,
      tenantId: job.tenantId,
      itemId: job.itemId,
      documentId: job.documentId,
      documentVersion: job.documentVersion,
      runId: job.runId,
      pipelineVersion: job.pipelineVersion,
    };
    outcome = status === "PARTIAL_SUCCESS" ? "PARTIAL_SUCCEEDED" : "SUCCEEDED";
  } else {
    // FAILED (or an unexpected IN_PROGRESS on a completion notification, treated the same way -
    // this handler only runs off a completion event, so IN_PROGRESS here is itself anomalous).
    sendSuccess = false;
    sendErrorCode = new TextractPartialFailureError(`Textract job ${input.jobId} did not complete usably (status=${status}).`, { jobId: input.jobId }).code;
    outcome = "FAILED_REPORTED";
  }

  const taskToken = await deps.tokenEncryptor.decrypt(job.taskTokenCiphertext);
  const sendResult = sendSuccess
    ? await deps.sender.sendTaskSuccess(taskToken, sendPayload)
    : await deps.sender.sendTaskFailure(taskToken, sendErrorCode!, `Textract jobId=${input.jobId}`);
  // Any other SendTask* error (throttling, transient network) throws out of the calls above and
  // is NOT caught here â€” it propagates to the SQS consumer for redelivery, taskTokenCiphertext
  // left untouched, exactly per design Â§3's rodada-7 correction.

  // Every outcome reachable here (SENT, TERMINAL_QUIET, TERMINAL_WARN_INVALID_TOKEN) is terminal
  // for this token â€” clear it. A lost OCC race on the update means a concurrent delivery already
  // did this; treated as success, not retried.
  const now = deps.now?.() ?? new Date().toISOString();
  const updated: TextractJob = {
    ...job,
    ...textractJobKey(job.jobId),
    status: sendSuccess ? "COMPLETED" : "FAILED",
    taskTokenCiphertext: undefined,
    version: job.version + 1,
    updatedAt: now,
  };
  await deps.jobs.updateConditional(updated, { version: job.version });

  void sendResult; // outcome informs logging at the handler boundary, not branching here.
  return outcome;
}

/**
 * `TextractTaskHandler`'s `START_OCR` operation â€” the single `RunTextract` invocation the ASL
 * calls via `arn:aws:states:::lambda:invoke.waitForTaskToken` (`claude-reconciliation-final-
 * design.md` Â§1.2/Â§2). Pure orchestration over ports, same testing convention as
 * `startExtractionRun` (hand-written fakes, no `vi.mock`). Deliberately never calls
 * `SendTaskSuccess` â€” on a clean return the Step Functions execution stays parked on the task
 * token until `completeOcr` (a LATER, separate SQS-triggered invocation) resolves it.
 */
import { classifyDocumentType } from "../domain/document-classifier.js";
import {
  deriveTextractClientRequestToken,
  computeTextractJobTtl,
  textractJobKey,
  type TextractJob,
} from "../domain/textract-job.js";
import {
  OcrDisabledError,
  QuotaExceededError,
  TextractJobPersistenceFailedError,
  TextractUnsupportedDocumentError,
  UnsupportedDocumentTypeError,
} from "../../../shared/errors/app-error.js";
import type { FeatureFlagsReader } from "../ports/feature-flags-reader.js";
import type { TextractClient } from "../ports/textract-client.js";
import type { TextractJobStore } from "../ports/textract-job-store.js";
import type { TaskTokenEncryptor } from "../ports/task-token-encryptor.js";
import { TenantQuotaService } from "../../identity/application/quota.js";

export interface StartOcrDeps {
  featureFlags: FeatureFlagsReader;
  quota: TenantQuotaService;
  textract: TextractClient;
  jobs: TextractJobStore;
  tokenEncryptor: TaskTokenEncryptor;
  snsTopicArn: string;
  snsRoleArn: string;
  /** Bounded local retry before propagating `TextractJobPersistenceFailedError`, per design Â§2
   * ("RecuperaÃ§Ã£o do intervalo StartDocumentTextDetection -> persistÃªncia"). */
  jobPersistAttempts?: number;
  now?: () => string;
}

export interface StartOcrInput {
  taskToken: string;
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  cleanObject: { bucket: string; key: string; versionId: string };
  fileName: string;
  contentType?: string;
  magicBytes?: Uint8Array;
}

/** Quota reservation window covers this run's TEXTRACT call only â€” `limit: 1` over a
 * long-lived window turns `TenantQuotaService.consume` into an idempotency lock keyed by
 * `runId|TEXTRACT` (design Â§1.8's exact idempotency key) rather than a real rate cap: a retried
 * `START_OCR` invocation for the SAME run hits `QuotaExceededError` against its OWN prior
 * reservation, which this function treats as "already reserved, proceed" rather than a real
 * quota failure â€” see the catch block below. */
const AI_CALL_RESERVATION_WINDOW_SECONDS = 7 * 24 * 60 * 60;

export async function startOcr(deps: StartOcrDeps, input: StartOcrInput): Promise<void> {
  const documentType = classifyDocumentType({ fileName: input.fileName, contentType: input.contentType, magicBytes: input.magicBytes });
  if (!documentType) {
    throw new UnsupportedDocumentTypeError(`Cannot classify document ${input.documentId} for OCR.`, { documentId: input.documentId, fileName: input.fileName });
  }

  let flags;
  try {
    flags = await deps.featureFlags.getFlags();
  } catch {
    // Fail-closed (mandatory, per feature-flags-reader.js's own contract): any read error is
    // treated identically to OCR=false, never as "unknown, proceed".
    throw new OcrDisabledError("Feature flags could not be read; failing closed.", { documentId: input.documentId });
  }
  if (!flags.OCR) {
    throw new OcrDisabledError("OCR kill switch is off.", { documentId: input.documentId });
  }

  const quotaWindow = `${input.runId}|TEXTRACT`;
  try {
    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
  } catch (err) {
    if (!(err instanceof QuotaExceededError)) throw err;
    // Same run already reserved AI_CALL/TEXTRACT on a prior attempt â€” idempotent, not a real
    // quota exhaustion (a genuine cap would use a per-tenant/per-period window, not per-run).
  }

  const clientRequestToken = deriveTextractClientRequestToken(input.tenantId, input.documentId, input.documentVersion, input.pipelineVersion, input.runId);

  let jobId: string;
  try {
    const started = await deps.textract.startDocumentTextDetection({
      bucket: input.cleanObject.bucket,
      key: input.cleanObject.key,
      clientRequestToken,
      jobTag: input.runId,
      snsTopicArn: deps.snsTopicArn,
      snsRoleArn: deps.snsRoleArn,
    });
    jobId = started.jobId;
  } catch (err) {
    // Reservation was never consumed by Textract (the call itself failed) â€” compensate per
    // design Â§1.8 before propagating.
    await deps.quota.release({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
    throw new TextractUnsupportedDocumentError(`StartDocumentTextDetection failed for document ${input.documentId}.`, { documentId: input.documentId, cause: err instanceof Error ? err.message : String(err) });
  }

  const now = deps.now?.() ?? new Date().toISOString();
  const taskTokenCiphertext = await deps.tokenEncryptor.encrypt(input.taskToken);
  const job: TextractJob = {
    ...textractJobKey(jobId),
    entityType: "TextractJob",
    jobId,
    tenantId: input.tenantId,
    itemId: input.itemId,
    documentId: input.documentId,
    documentVersion: input.documentVersion,
    runId: input.runId,
    pipelineVersion: input.pipelineVersion,
    clientRequestToken,
    status: "STARTED",
    taskTokenCiphertext,
    ttl: computeTextractJobTtl(now),
    version: 1,
    createdAt: now,
    updatedAt: now,
  };

  const attempts = Math.max(1, deps.jobPersistAttempts ?? 2);
  let lastErr: unknown;
  for (let i = 0; i < attempts; i++) {
    try {
      await deps.jobs.create(job);
      return; // deliberately no SendTaskSuccess here â€” see module doc comment.
    } catch (err) {
      lastErr = err;
    }
  }
  throw new TextractJobPersistenceFailedError(`Failed to persist TextractJob ${jobId} after ${attempts} attempt(s).`, {
    jobId,
    runId: input.runId,
    cause: lastErr instanceof Error ? lastErr.message : String(lastErr),
  });
}

/**
 * `BedrockExtractionTaskHandler`'s single operation (M7 item 6, D-035 Â§1.9/Â§1.11) - the ASL's
 * `RunBedrock` state (`arn:aws:states:::lambda:invoke`, plain synchronous invocation, same
 * shape as item 5's `RunDeterministicParser` - no task token). Reached only when
 * `RunDeterministicParser`'s output said `needsBedrock: true` AND `aiExtractionEnabled: true`
 * (the ASL's `NeedsBedrock`/`CheckAiKillSwitch` Choice states), but this function re-checks the
 * kill switch itself (defense in depth, never the only gate - the ASL Choice can be bypassed by
 * a hand-crafted `StartExecution` input in theory, so the paid call must never trust its own
 * caller blindly).
 *
 * Consumes exactly what item 5's `runDeterministicParser()` produces (`RunDeterministicParserOutput`)
 * - never reimplements `needsBedrock()`/`decide-bedrock.ts`'s contract, and never re-reads the
 * OCR artifact through a different path than the one `PdfParserTaskHandler` already resolved.
 */
import { AiExtractionDisabledError, BedrockExtractionFailedError, QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { FeatureFlagsReader } from "../ports/feature-flags-reader.js";
import type { BedrockClient } from "../ports/bedrock-client.js";
import type { ExtractionArtifactRef } from "../ports/ocr-artifact-store.js";
import { TenantQuotaService } from "../../identity/application/quota.js";
import type { ExtractedFieldValueType } from "../domain/extracted-field.js";
import { BEDROCK_SYSTEM_PROMPT_VERSION } from "../domain/bedrock-extraction.js";

export interface RunBedrockExtractionDeps {
  featureFlags: FeatureFlagsReader;
  quota: TenantQuotaService;
  bedrock: BedrockClient;
  /** Bounded local retry before propagating `BedrockExtractionFailedError` - same discipline as
   * `start-ocr.ts`'s `jobPersistAttempts`, but here the "operation" being retried is the model
   * call itself, so this defaults low (a single retry) to avoid amplifying the cost-abuse
   * surface a flaky retry loop would create. */
  callAttempts?: number;
}

export interface RunBedrockExtractionFieldInput {
  fieldName: string;
  valueType: ExtractedFieldValueType;
  candidateValue?: string;
  confidence?: number;
  source: "DETERMINISTIC_PARSER";
}

/** Mirrors `RunDeterministicParserOutput` exactly - the ASL passes item 5's output straight
 * through as this handler's input (`Parameters.Payload.$: "$"` after the `RunDeterministicParser`
 * task's `OutputPath` strips the `lambda:invoke` wrapper - see `document-extraction.asl.json`). */
export interface RunBedrockExtractionInput {
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  ocrAvailable: boolean;
  extractedFields: RunBedrockExtractionFieldInput[];
  needsBedrock: boolean;
  aiExtractionEnabled: boolean;
  artifact?: ExtractionArtifactRef;
}

export interface BedrockFieldCandidateOutput {
  fieldName: string;
  valueType: ExtractedFieldValueType;
  candidateValue?: string;
  confidence?: number;
  source: "BEDROCK";
}

/** Shaped for item 7 (`ExtractionValidationTaskHandler`'s `CompareExtractors`) to consume
 * alongside item 5's `extractedFields` (deterministic) - same discipline items 4/5 already
 * followed ("assemble what the next real consumer needs"). Deliberately does NOT merge/compare
 * against the deterministic candidates itself - `ExtractionAgreement` (MATCH/MISMATCH/
 * SINGLE_SOURCE) is item 7's job, this function only reports what Bedrock itself produced. */
export interface RunBedrockExtractionOutput {
  tenantId: string;
  itemId: string;
  documentId: string;
  documentVersion: number;
  runId: string;
  pipelineVersion: string;
  bedrockFields: BedrockFieldCandidateOutput[];
  bedrockSystemPromptVersion: string;
  // Passthrough of RunDeterministicParser's own output (item 7 finding, D-035 Â§2: the ASL's
  // RunBedrock -> ValidateSchema transition has no separate merge step, so whatever this
  // function returns IS $ for ValidateSchema - without echoing these back, item 5's
  // extractedFields/artifact/needsBedrock/aiExtractionEnabled would be silently lost on every
  // run that actually reaches Bedrock, the exact class of ResultPath bug item 5 already found
  // and fixed once for completeOcr's own success payload). Never recomputed - these are the
  // SAME values `input` already carried in, just carried back out.
  ocrAvailable: boolean;
  extractedFields: RunBedrockExtractionFieldInput[];
  needsBedrock: boolean;
  aiExtractionEnabled: boolean;
  artifact?: ExtractionArtifactRef;
}

const AI_CALL_RESERVATION_WINDOW_SECONDS = 7 * 24 * 60 * 60;

export async function runBedrockExtraction(deps: RunBedrockExtractionDeps, input: RunBedrockExtractionInput): Promise<RunBedrockExtractionOutput> {
  let flags;
  try {
    flags = await deps.featureFlags.getFlags();
  } catch {
    // Fail-closed (mandatory, per feature-flags-reader.ts's own contract) - any read error is
    // treated identically to AI_EXTRACTION=false, never as "unknown, proceed".
    throw new AiExtractionDisabledError("Feature flags could not be read; failing closed.", { documentId: input.documentId, runId: input.runId });
  }
  if (!flags.AI_EXTRACTION) {
    throw new AiExtractionDisabledError("AI_EXTRACTION kill switch is off.", { documentId: input.documentId, runId: input.runId });
  }

  if (!input.artifact) {
    // No OCR text artifact was ever produced for this run (RunTextract failed, or OCR was
    // disabled) - there is nothing for Bedrock to read. This is not itself an error: the ASL
    // only reaches RunBedrock when needsBedrock was true, and needsBedrock's rule (a) treats
    // "no deterministic candidate" as needing Bedrock even with zero OCR text available, so a
    // caller CAN legitimately reach here with no artifact. Bedrock cannot help without any
    // document text either, so this returns zero candidates rather than calling the model with
    // an empty document (which would waste a paid call and a quota reservation for a
    // guaranteed-empty answer).
    return {
      tenantId: input.tenantId,
      itemId: input.itemId,
      documentId: input.documentId,
      documentVersion: input.documentVersion,
      runId: input.runId,
      pipelineVersion: input.pipelineVersion,
      bedrockFields: [],
      bedrockSystemPromptVersion: BEDROCK_SYSTEM_PROMPT_VERSION,
      ocrAvailable: input.ocrAvailable,
      extractedFields: input.extractedFields,
      needsBedrock: input.needsBedrock,
      aiExtractionEnabled: input.aiExtractionEnabled,
      artifact: input.artifact,
    };
  }

  // Idempotency key mirrors start-ocr.ts's exact pattern (design Â§1.11's 14th adversarial case
  // - cost-abuse via repeated reprocessing of an unchanged document): `runId` is itself
  // deterministic from tenantId|documentId|documentVersion|pipelineVersion
  // (deriveExtractionRunId(), extraction-run.ts), so a retried/duplicate Step Functions
  // execution for the SAME document version reserves against the SAME quota window and hits
  // QuotaExceededError on its own prior reservation - never a second paid Bedrock call for an
  // unchanged document.
  const quotaWindow = `${input.runId}|BEDROCK`;
  try {
    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
  } catch (err) {
    if (!(err instanceof QuotaExceededError)) throw err;
    // Same run already reserved AI_CALL/BEDROCK on a prior attempt. Idempotent replay: since the
    // actual model call has not happened yet at this point in a genuinely fresh attempt, and a
    // reservation here can only pre-exist from an earlier attempt of the SAME run, we still
    // proceed to call Bedrock exactly once for this attempt - the guarantee this quota key
    // provides is "at most one concurrent/duplicate EXECUTION reserves this run's Bedrock call
    // slot", not a second layer of dedup on top of the ASL's own single-path execution. A
    // genuinely already-answered run never reaches RunBedrock again because CompleteRun (item 7)
    // is terminal.
  }

  const attempts = Math.max(1, deps.callAttempts ?? 1);
  let lastErr: unknown;
  for (let i = 0; i < attempts; i++) {
    try {
      const result = await deps.bedrock.extract({
        textArtifact: input.artifact,
        pipelineVersion: input.pipelineVersion,
        systemPromptVersion: BEDROCK_SYSTEM_PROMPT_VERSION,
      });

      const schemaByName = new Map(input.extractedFields.map((f) => [f.fieldName, f.valueType]));
      const bedrockFields: BedrockFieldCandidateOutput[] = result.fields.map((f) => ({
        fieldName: f.fieldName,
        valueType: schemaByName.get(f.fieldName) ?? "STRING",
        candidateValue: f.value === "" ? undefined : f.value,
        confidence: f.confidence,
        source: "BEDROCK",
      }));

      return {
        tenantId: input.tenantId,
        itemId: input.itemId,
        documentId: input.documentId,
        documentVersion: input.documentVersion,
        runId: input.runId,
        pipelineVersion: input.pipelineVersion,
        bedrockFields,
        bedrockSystemPromptVersion: BEDROCK_SYSTEM_PROMPT_VERSION,
        ocrAvailable: input.ocrAvailable,
        extractedFields: input.extractedFields,
        needsBedrock: input.needsBedrock,
        aiExtractionEnabled: input.aiExtractionEnabled,
        artifact: input.artifact,
      };
    } catch (err) {
      lastErr = err;
    }
  }

  // The call never succeeded even after local retry - compensate the quota reservation (design
  // Â§1.8's exact compensation pattern from start-ocr.ts) before propagating, so a genuinely
  // failed call does not permanently burn this run's one Bedrock attempt.
  await deps.quota.release({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
  throw new BedrockExtractionFailedError(`Bedrock extraction failed for run ${input.runId} after ${attempts} attempt(s).`, {
    runId: input.runId,
    documentId: input.documentId,
    cause: lastErr instanceof Error ? lastErr.message : String(lastErr),
  });
}

test\integration-dynamodb\quota.dynamodb.test.ts:11: * Reproduction: `TenantQuotaService.consume()`'s FIRST call for a given bucket uses
test\integration-dynamodb\quota.dynamodb.test.ts:35:  it("a second consume() call within the same window hits updateConditional against real DynamoDB without a reserved-word ValidationException", async () => {
test\integration-dynamodb\quota.dynamodb.test.ts:40:    await quota.consume(input);
test\integration-dynamodb\quota.dynamodb.test.ts:42:    await quota.consume(input);
test\integration-dynamodb\quota.dynamodb.test.ts:43:    await quota.consume(input);
test\integration-dynamodb\quota.dynamodb.test.ts:56:    await quota.consume(input);
test\integration-dynamodb\quota.dynamodb.test.ts:57:    await quota.consume(input);
test\integration-dynamodb\quota.dynamodb.test.ts:58:    await expect(quota.consume(input)).rejects.toBeInstanceOf(QuotaExceededError);
test\unit\identity\quota.test.ts:12:    await quota.consume(input);
test\unit\identity\quota.test.ts:13:    await quota.consume(input);
test\unit\identity\quota.test.ts:14:    await quota.consume(input);
test\unit\identity\quota.test.ts:15:    await expect(quota.consume(input)).rejects.toBeInstanceOf(QuotaExceededError);
test\unit\identity\quota.test.ts:24:    await quota.consume(a);
test\unit\identity\quota.test.ts:25:    await expect(quota.consume(a)).rejects.toBeInstanceOf(QuotaExceededError);
test\unit\identity\quota.test.ts:27:    await expect(quota.consume(b)).resolves.toBeUndefined();
test\unit\identity\quota.test.ts:36:    await quota.consume(input);
test\unit\identity\quota.test.ts:37:    await expect(quota.consume(input)).rejects.toBeInstanceOf(QuotaExceededError);
test\unit\identity\quota.test.ts:40:    await expect(quota.consume(input)).resolves.toBeUndefined();
test\unit\identity\quota.test.ts:60:      quota.consume({ tenantId: "tenant-a", quotaType: "AI_CALL", window: "w1", limit: 100, windowSeconds: 60 }),
test\unit\identity\quota.test.ts:64:  it("does not lose updates under concurrent consume() for the same tenant/quotaType (full-audit round1, eixo Produto, critério 3)", async () => {
test\unit\identity\quota.test.ts:69:    const results = await Promise.allSettled(Array.from({ length: 25 }, () => quota.consume(input)));
test\unit\identity\quota.test.ts:80:  it("release() decrements a previously-consumed unit, freeing capacity for a new consume() in the same window (M6 UploadSlotReconciliationWorker)", async () => {
test\unit\identity\quota.test.ts:85:    await quota.consume(input);
test\unit\identity\quota.test.ts:86:    await expect(quota.consume(input)).rejects.toBeInstanceOf(QuotaExceededError);
test\unit\identity\quota.test.ts:89:    await expect(quota.consume(input)).resolves.toBeUndefined();
test\unit\identity\quota.test.ts:97:    await quota.consume(input);
test\unit\document\upload-slot-reconciliation.test.ts:75:    await quota.consume({ tenantId: "t1", quotaType: "UPLOAD_COUNT", window: "current", limit: 1, windowSeconds: 60 });
test\unit\document\upload-slot-reconciliation.test.ts:95:    // Quota was released - a new consume() in the same window should succeed again.
test\unit\document\upload-slot-reconciliation.test.ts:96:    await expect(quota.consume({ tenantId: "t1", quotaType: "UPLOAD_COUNT", window: "current", limit: 1, windowSeconds: 60 })).resolves.toBeUndefined();
src\modules\subject\http\subject-handlers.ts:18:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:43:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:53:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:64:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:81:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:93:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:107:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:119:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:137:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\http\requirement-handlers.ts:150:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
test\unit\extraction\start-ocr.test.ts:182:    // Quota was released - a fresh consume() for the same window should succeed again, not
test\unit\extraction\start-ocr.test.ts:185:      quota.consume({ tenantId: "t1", quotaType: "AI_CALL", window: "run_x|TEXTRACT", limit: 1, windowSeconds: 1000 }),
src\modules\subject\http\document-request-handlers.ts:41:  await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\subject\application\initial-invite-rate-limiter.ts:48:    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS`, SK: "RATE" }, 20, 60 * 60);
src\modules\subject\application\initial-invite-rate-limiter.ts:49:    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS`, SK: "RATE_DAILY" }, 100, 24 * 60 * 60);
src\modules\subject\application\initial-invite-rate-limiter.ts:50:    await this.consume({ PK: `TENANT#${tenantId}#SETTINGS#RECIPIENT#${recipientHash(recipientEmail)}`, SK: "RATE" }, 3, 24 * 60 * 60);
src\modules\subject\application\initial-invite-rate-limiter.ts:53:  private async consume(key: { PK: string; SK: "RATE" | "RATE_DAILY" }, limit: number, windowSeconds: number): Promise<void> {
src\modules\identity\ports\identity-store.ts:21:   * round1, eixo Governança de Produto, criterio 3 - TenantQuota.consume() did a
src\modules\subject\application\guest-submission-service.ts:228:      await this.rateLimiter.consume({ selectorHash, limit: 30, windowSeconds: 60 });
src\modules\subject\application\guest-rate-limiter.ts:38:  async consume(input: { selectorHash: string; limit: number; windowSeconds: number }): Promise<void> {
src\modules\import\http\import-handlers.ts:15:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\notification\http\preferences-handlers.ts:13:  await quota.consume({
src\modules\identity\http\test-route-handler.ts:53:    await deps.quota.consume({
src\modules\identity\http\profile-handlers.ts:13:  await quota.consume({
src\modules\import\application\import-service.ts:104:    await this.quota.consume({ tenantId: ctx.tenant.tenantId, quotaType: "IMPORT_COUNT", window: "current", limit: 10, windowSeconds: 60 * 60 });
src\modules\import\application\import-service.ts:105:    await this.quota.consume({ tenantId: ctx.tenant.tenantId, quotaType: "IMPORT_BYTES", window: "current", limit: 50 * MAX_IMPORT_FILE_BYTES, windowSeconds: 60 * 60 });
src\modules\import\application\import-parse-service.ts:124:    await deps.quota.consume({ tenantId, quotaType: "IMPORT_ROWS", window: "current", limit: 20000, windowSeconds: 60 * 60 });
src\modules\reminder\http\policy-handlers.ts:15:  await quota.consume({
src\modules\document\http\document-handlers.ts:16:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "UPLOAD_COUNT", window: "current", limit: 20, windowSeconds: 60 });
src\modules\identity\application\quota.ts:73:  async consume(input: QuotaCheckInput): Promise<void> {
src\modules\identity\application\quota.ts:117:      // Another concurrent consume() won the write race; re-read and retry against fresh state.
src\modules\expiration\http\item-watch-handlers.ts:72:  await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\expiration\http\item-handlers.ts:26:  await quota.consume({
src\modules\extraction\http\extraction-handlers.ts:20:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src\modules\extraction\application\start-ocr.ts:85:    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
src\modules\extraction\application\run-bedrock-extraction.ts:145:    await deps.quota.consume({ tenantId: input.tenantId, quotaType: "AI_CALL", window: quotaWindow, limit: 1, windowSeconds: AI_CALL_RESERVATION_WINDOW_SECONDS });
src/modules/identity\ports\identity-store.ts:21:   * round1, eixo Governança de Produto, criterio 3 - TenantQuota.consume() did a
src/modules/identity\http\test-route-handler.ts:53:    await deps.quota.consume({
src/modules/identity\http\profile-handlers.ts:13:  await quota.consume({
src/modules/identity\application\quota.ts:22:export interface TenantQuotaRecord {
src/modules/identity\application\quota.ts:55:export class TenantQuotaService {
src/modules/identity\application\quota.ts:73:  async consume(input: QuotaCheckInput): Promise<void> {
src/modules/identity\application\quota.ts:117:      // Another concurrent consume() won the write race; re-read and retry against fresh state.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw src/modules/identity/application/quota.ts; Get-Content -Raw src/modules/extraction/ports/extraction-run-store.ts; Get-Content -Raw src/modules/extraction/ports/textract-job-store.ts; Get-Content -Raw src/modules/extraction/persistence/dynamodb-extraction-run-store.ts; Get-Content -Raw src/modules/extraction/persistence/dynamodb-textract-job-store.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 167ms:
/**
 * TenantQuota token bucket â€” data-model.md Â§"Rastreabilidade a gates de abuso/custo":
 * PK=TENANT#t#QUOTA, SK=TYPE#<quotaType>#<window>, decremented via ConditionExpression
 * "no race condition between concurrent requests of the same tenant" (same pattern as
 * UploadSlot). Enforced at the API/Lambda boundary per M1 deliverable list.
 */
import { QuotaExceededError } from "../../../shared/errors/app-error.js";
import type { EntityKey, IdentityStore } from "../ports/identity-store.js";

export type QuotaType =
  | "API_REQUEST"
  | "UPLOAD_BYTES"
  | "UPLOAD_COUNT"
  | "AI_CALL"
  | "NOTIFICATION_EMAIL"
  // M11 (CSV import/export, D-042, 09-domain-model-csv-import.md): separados de UPLOAD_* (M6) -
  // import Ã© uma superfÃ­cie de processamento em massa com um perfil de abuso diferente.
  | "IMPORT_COUNT"
  | "IMPORT_BYTES"
  | "IMPORT_ROWS";

export interface TenantQuotaRecord {
  PK: string;
  SK: string;
  entityType: "TenantQuota";
  tenantId: string;
  quotaType: QuotaType;
  limit: number;
  windowSeconds: number;
  count: number;
  resetAt: string;
  killSwitchOverride?: boolean;
}

export function tenantQuotaKey(tenantId: string, quotaType: QuotaType, window: string): EntityKey {
  return { PK: `TENANT#${tenantId}#QUOTA`, SK: `TYPE#${quotaType}#${window}` };
}

export interface QuotaCheckInput {
  tenantId: string;
  quotaType: QuotaType;
  window: string; // e.g. rolling window identifier/bucket, caller-defined granularity
  limit: number;
  windowSeconds: number;
}

/**
 * Fixed-window token bucket (simplest correct primitive satisfying "atomic decrement,
 * no race condition" from data-model.md; a true sliding-window/leaky-bucket is a
 * documented possible refinement, not required by the blueprint text). Consumes 1 unit
 * per call; throws QuotaExceededError (mapped to 429 by the http layer) when exhausted.
 * killSwitchOverride, when true, denies regardless of remaining count (AppConfig-driven
 * emergency block, per data-model.md).
 */
export class TenantQuotaService {
  constructor(
    private readonly store: IdentityStore,
    private readonly now: () => string = () => new Date().toISOString(),
  ) {}

  /**
   * Bounded retry count for the read-check-conditionalWrite loop below (full audit round1,
   * eixo GovernanÃ§a de Produto, critÃ©rio 3 â€” the previous unconditional `store.update` allowed
   * lost updates between two concurrent `consume` calls for the same tenant/quotaType: both
   * could read the same `count`, both compute `count+1`, and the second overwrite would
   * silently discard the first increment, letting a tenant exceed its quota). 20 attempts
   * covers a burst of simultaneous requests against the same partition key without needing
   * backoff/jitter for this fixed-window primitive's expected contention level; exhausting it
   * means genuine sustained hot contention, not a normal path.
   */
  private static readonly MAX_CONTENTION_RETRIES = 20;

  async consume(input: QuotaCheckInput): Promise<void> {
    for (let attempt = 0; attempt < TenantQuotaService.MAX_CONTENTION_RETRIES; attempt++) {
      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
      const nowIso = this.now();
      const resetAt = new Date(Date.parse(nowIso) + input.windowSeconds * 1000).toISOString();

      const existing = await this.store.get<TenantQuotaRecord>(key);
      if (!existing) {
        const created = await this.store.putIfAbsent({
          ...key,
          entityType: "TenantQuota",
          tenantId: input.tenantId,
          quotaType: input.quotaType,
          limit: input.limit,
          windowSeconds: input.windowSeconds,
          count: 1,
          resetAt,
        });
        if (created) return;
        // Lost the create race; loop to re-read the record another caller just created.
        continue;
      }

      if (existing.killSwitchOverride) {
        throw new QuotaExceededError("Quota blocked by kill switch.", { tenantId: input.tenantId, quotaType: input.quotaType });
      }

      const windowExpired = existing.resetAt < nowIso;
      const effectiveCount = windowExpired ? 0 : existing.count;

      if (effectiveCount >= existing.limit) {
        throw new QuotaExceededError("Quota exceeded.", {
          tenantId: input.tenantId,
          quotaType: input.quotaType,
          limit: existing.limit,
        });
      }

      const nextResetAt = windowExpired ? resetAt : existing.resetAt;
      const wrote = await this.store.updateConditional(
        { ...existing, count: effectiveCount + 1, resetAt: nextResetAt },
        { count: existing.count, resetAt: existing.resetAt },
      );
      if (wrote) return;
      // Another concurrent consume() won the write race; re-read and retry against fresh state.
    }

    throw new QuotaExceededError("Quota check could not complete under contention.", {
      tenantId: input.tenantId,
      quotaType: input.quotaType,
    });
  }

  /**
   * Releases 1 previously-consumed unit â€” M6 design Â§3.5 (UploadSlotReconciliationWorker
   * "libera quota idempotentemente" for a slot that expired unconfirmed). Idempotent: never
   * decrements below 0, and calling this twice for the same already-released slot floors at 0
   * rather than double-crediting. A window that has already reset naturally (count implicitly
   * 0) is a no-op, not an error - the quota already recovered on its own.
   */
  async release(input: Omit<QuotaCheckInput, "limit"> & { limit?: number }): Promise<void> {
    for (let attempt = 0; attempt < TenantQuotaService.MAX_CONTENTION_RETRIES; attempt++) {
      const key = tenantQuotaKey(input.tenantId, input.quotaType, input.window);
      const nowIso = this.now();
      const existing = await this.store.get<TenantQuotaRecord>(key);
      if (!existing) return; // nothing consumed yet for this window - nothing to release.

      const windowExpired = existing.resetAt < nowIso;
      if (windowExpired) return; // window already reset naturally - already recovered.

      const nextCount = Math.max(0, existing.count - 1);
      const wrote = await this.store.updateConditional({ ...existing, count: nextCount }, { count: existing.count, resetAt: existing.resetAt });
      if (wrote) return;
      // Lost a concurrent write race; re-read and retry against fresh state.
    }
    throw new QuotaExceededError("Quota release could not complete under contention.", { tenantId: input.tenantId, quotaType: input.quotaType });
  }
}

/** DynamoDB surface the Extraction module needs â€” same port pattern as DocumentStore/
 * ReminderStore (AGENTS.md Â§6). */
export type { EntityKey } from "../../../shared/dynamodb/occ.js";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";

export interface ExtractionRunStore {
  /** M7 item 8 (confirm/reject field routes, Â§1.7): a plain, eventually-consistent read of the
   * `ExtractionRun` row â€” the OCC-guarded write paths above never needed this, but the HTTP
   * routes need to read the run's current `version` before asserting `expectedRunVersion`. */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;

  /** Idempotent create â€” the actual idempotency mechanism for the whole run
   * (deriveExtractionRunId() already makes the key deterministic; this is what turns a
   * duplicate S3 event / SQS redelivery into a no-op instead of a second run). */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;

  /** Standalone OCC status transition, used ONLY for the DISCARDED outcome (M7 item 7,
   * `ExtractionValidationTaskHandler`) â€” the concurrent-discard race is detected too late for
   * any `ExtractedField` row to still be meaningful, so there is nothing to commit
   * transactionally alongside it (contrast `ExtractedFieldStore.commitRunOutcome`, which
   * updates this same run's status atomically together with the fields it writes for the
   * COMPLETED/FAILED outcomes). Returns `false` on an OCC conflict (stale `expectedVersion`) â€”
   * the caller treats that as "someone else already finalized this run", never retries blindly. */
  updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean>;
}

/** DynamoDB surface for `TextractJob` â€” narrow, matching the ExtractionRunStore/DocumentReader
 * pattern already established in this module (AGENTS.md Â§7). */
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import type { TextractJob } from "../domain/textract-job.js";

export interface TextractJobStore {
  /** Creates a brand-new TextractJob record. Called only AFTER StartDocumentTextDetection has
   * already succeeded (design Â§2) â€” a failure here (even after the caller's own local retry)
   * must surface as `TextractJobPersistenceFailedError`, never be swallowed. */
  create(job: TextractJob): Promise<void>;

  /** Lookup by `jobId` alone â€” the only correlation `COMPLETE_OCR` has available from the SNS
   * notification. `null` when no matching job exists (the orphan-job case, design Â§2). */
  getByJobId(jobId: string): Promise<TextractJob | null>;

  /** Conditional update used by `COMPLETE_OCR` to record the terminal outcome and clear
   * `taskTokenCiphertext` (design Â§3) â€” `expectedVersion` guards against concurrent SQS
   * redelivery of the same completion message racing itself. Returns false (not throw) on a
   * lost OCC race, matching `ExtractionRunStore`/`TenantQuotaService`'s own convention, so the
   * caller can decide idempotently (a lost race here means another delivery already finished
   * the same work â€” safe to treat as success, never as a hard failure).
   */
  updateConditional(job: TextractJob, expected: { version: number }): Promise<boolean>;

  key(jobId: string): EntityKey;
}

/** Real DynamoDB adapter for ExtractionRunStore. Same translation pattern as
 * DynamoDbDocumentStore/DynamoDbReminderStore. */
import { GetCommand, PutCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey } from "../ports/extraction-run-store.js";
import type { ExtractionRunStore } from "../ports/extraction-run-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";
import { buildVersionedUpdate } from "../../../shared/dynamodb/occ.js";

export class DynamoDbExtractionRunStore implements ExtractionRunStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
      return result.Item as T | undefined;
    } catch (err) {
      throw mapDynamoError(err, "ExtractionRunStore.get");
    }
  }

  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "ExtractionRunStore.putIfAbsent");
    }
  }

  async updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean> {
    const command = buildVersionedUpdate({
      tableName: this.tableName,
      key,
      tenantId,
      expectedVersion,
      set: { status, completedAt },
    });
    try {
      await this.client.send(
        new UpdateCommand({
          TableName: command.TableName,
          Key: command.Key,
          UpdateExpression: command.UpdateExpression,
          ConditionExpression: command.ConditionExpression,
          ExpressionAttributeNames: command.ExpressionAttributeNames,
          ExpressionAttributeValues: command.ExpressionAttributeValues,
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "ExtractionRunStore.updateStatus");
    }
  }
}

/** Real DynamoDB adapter for `TextractJobStore`. Uses the shared single table (main
 * `TABLE_NAME`) â€” `TextractJob` is keyed by `jobId` alone (see domain/textract-job.ts's own
 * doc comment on why), never tenant-scoped, so this adapter never needs a tenant argument. */
import { PutCommand, GetCommand, UpdateCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import type { TextractJobStore } from "../ports/textract-job-store.js";
import { textractJobKey, type TextractJob } from "../domain/textract-job.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";

export class DynamoDbTextractJobStore implements TextractJobStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  key(jobId: string): EntityKey {
    return textractJobKey(jobId);
  }

  async create(job: TextractJob): Promise<void> {
    try {
      await this.client.send(
        new PutCommand({
          TableName: this.tableName,
          Item: job,
          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
        }),
      );
    } catch (err) {
      throw mapDynamoError(err, "TextractJobStore.create");
    }
  }

  async getByJobId(jobId: string): Promise<TextractJob | null> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: this.key(jobId) }));
      return (result.Item as TextractJob | undefined) ?? null;
    } catch (err) {
      throw mapDynamoError(err, "TextractJobStore.getByJobId");
    }
  }

  /** No `tenantId` equality check here (unlike `buildVersionedUpdate`'s usual tenant-scoped
   * convention) â€” `TextractJob` is deliberately not tenant-scoped (see domain doc comment), so
   * the only fencing fact available is `version` itself. */
  async updateConditional(job: TextractJob, expected: { version: number }): Promise<boolean> {
    try {
      await this.client.send(
        new UpdateCommand({
          TableName: this.tableName,
          Key: this.key(job.jobId),
          ConditionExpression: "attribute_exists(PK) AND attribute_exists(SK) AND #version = :expectedVersion",
          UpdateExpression: job.taskTokenCiphertext === undefined ? "SET #status = :status, #updatedAt = :updatedAt, #version = :newVersion REMOVE #taskTokenCiphertext" : "SET #status = :status, #updatedAt = :updatedAt, #version = :newVersion, #taskTokenCiphertext = :taskTokenCiphertext",
          ExpressionAttributeNames: {
            "#version": "version",
            "#status": "status",
            "#updatedAt": "updatedAt",
            "#taskTokenCiphertext": "taskTokenCiphertext",
          },
          ExpressionAttributeValues: {
            ":expectedVersion": expected.version,
            ":status": job.status,
            ":updatedAt": job.updatedAt,
            ":newVersion": job.version,
            ...(job.taskTokenCiphertext === undefined ? {} : { ":taskTokenCiphertext": job.taskTokenCiphertext }),
          },
        }),
      );
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "TextractJobStore.updateConditional");
    }
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/document/persistence/s3-document-object-store.ts; Get-Content -Raw src/modules/extraction/persistence/s3-ocr-artifact-store.ts; rg -n \"key:|Key:|objectKey|rawObjectKey|clean/|quarantine/|tenant/\" src/modules/document src/modules/extraction src/modules/import src/workers | Select-Object -First 400; rg -n \"versioning|versioning_configuration|status\\s*=\\s*\\\"Enabled\\\"|aws_s3_bucket_versioning\" infra/modules/document-buckets infra/modules/import-bucket infra | Select-Object -First 300" in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-28T21:21:14.905755Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.3 seconds
Output:
/** Real S3 adapter for DocumentObjectStore (M6). Every operation is scoped to an exact
 * bucket/key/versionId - never a bare key - per M6 design Â§"DecisÃµes-chave" (identidade
 * imutÃ¡vel de objeto). */
import { CopyObjectCommand, DeleteObjectCommand, HeadObjectCommand, S3Client } from "@aws-sdk/client-s3";
import type { DocumentObjectReference } from "../domain/document-object-reference.js";
import type { DocumentObjectStore, ObjectMetadata } from "../ports/document-object-store.js";

export class S3DocumentObjectStore implements DocumentObjectStore {
  constructor(private readonly client: S3Client) {}

  async headObject(ref: DocumentObjectReference): Promise<ObjectMetadata | undefined> {
    try {
      const result = await this.client.send(new HeadObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
      return {
        contentLength: result.ContentLength ?? 0,
        mediaType: result.ContentType ?? "application/octet-stream",
        checksumSha256: result.ChecksumSHA256,
      };
    } catch (err) {
      if (typeof err === "object" && err !== null && "name" in err && (err as { name?: unknown }).name === "NotFound") {
        return undefined;
      }
      throw err;
    }
  }

  async copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference> {
    // Real bug found via Camada 3 (2026-08-22): encodeURIComponent() on the FULL key also
    // encodes its "/" path separators as "%2F", producing a CopySource that doesn't resolve to
    // any real object (S3 keys use literal "/" as the segment delimiter; only the characters
    // WITHIN each segment need escaping). Every quarantine key has multiple "/" segments
    // (tenant/<id>/item/<id>/document/<id>/slot/<id>/<uuid>), so this affected every real
    // promotion attempt - confirmed via a real CopyObjectCommand failing with an opaque
    // "UnknownError" (S3's error response for a CopySource that doesn't parse as a real
    // bucket/key/versionId triple). Fixed by escaping each path segment independently.
    const encodedKey = source.key.split("/").map(encodeURIComponent).join("/");
    const result = await this.client.send(
      new CopyObjectCommand({
        Bucket: destinationBucket,
        Key: destinationKey,
        CopySource: `${source.bucket}/${encodedKey}?versionId=${source.versionId}`,
        ServerSideEncryption: "aws:kms",
      }),
    );
    return { bucket: destinationBucket, key: destinationKey, versionId: result.VersionId ?? "" };
  }

  async deleteObjectVersion(ref: DocumentObjectReference): Promise<void> {
    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
  }
}

export function createS3Client(): S3Client {
  return new S3Client({});
}

/** Real S3 adapter for `OcrArtifactStore`, writing to the `EXTRACTION_TRANSIENT` bucket
 * (`privacy-lgpd.md` Â§4). `delete()` is used ONLY by `ExtractionValidationTaskHandler` (M7 item
 * 7) â€” see the port's doc comment for why this method exists at all. */
import { S3Client, PutObjectCommand, GetObjectCommand, DeleteObjectCommand } from "@aws-sdk/client-s3";
import { randomUUID } from "node:crypto";
import type { OcrArtifactStore, ExtractionArtifactRef } from "../ports/ocr-artifact-store.js";

export class S3OcrArtifactStore implements OcrArtifactStore {
  constructor(
    private readonly client: S3Client,
    private readonly bucket: string,
  ) {}

  async put(runId: string, blocksJson: string): Promise<ExtractionArtifactRef> {
    const key = `ocr/${runId}/${randomUUID()}.json`;
    await this.client.send(
      new PutObjectCommand({
        Bucket: this.bucket,
        Key: key,
        Body: blocksJson,
        ContentType: "application/json",
      }),
    );
    return { bucket: this.bucket, key };
  }

  async get(ref: ExtractionArtifactRef): Promise<string> {
    const result = await this.client.send(new GetObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
    if (!result.Body) {
      throw new Error(`OCR artifact ${ref.bucket}/${ref.key} returned no body.`);
    }
    return result.Body.transformToString("utf-8");
  }

  async delete(ref: ExtractionArtifactRef): Promise<void> {
    // DeleteObjectCommand is already idempotent at the S3 API level (deleting a missing key
    // returns success, never a 404) - no extra existence check needed here.
    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
  }
}

export function createS3Client(): S3Client {
  return new S3Client({});
}

src/workers\upload-slot-reconciliation\reconciliation.ts:61:          key: uploadSlotKey(slot.tenantId, slot.uploadSlotId),
src/modules/document\ports\upload-url-signer.ts:6:  key: string;
src/modules/document\ports\pdf-parser.ts:14:  parse(ref: { bucket: string; key: string; versionId: string }): Promise<PdfParseResult>;
src/modules/document\ports\document-store.ts:8:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
src/modules/document\ports\document-object-store.ts:17:  copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference>;
src/workers\malware-result\result-processor.ts:15:  object: { bucket: string; key: string; versionId: string };
src/workers\reminder-dispatch\dispatch.ts:105:            key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-dispatch\dispatch.ts:161:        key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-dispatch\dispatch.ts:175:    buildVersionConditionCheck({ tableName: deps.tableName, key: itemKey(tenantId, itemId), expectedVersion: itemVersion, extra: { status: "ACTIVE" } }),
src/workers\reminder-dispatch\dispatch.ts:178:      key: policyKey(tenantId, occurrence.policyId),
src/modules/import\ports\import-store.ts:13:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
src/modules/document\persistence\s3-upload-url-signer.ts:17:      Key: input.key,
src/workers\upload-finalizer\finalizer.ts:15:  object: { bucket: string; key: string; versionId: string };
src/modules/extraction\domain\bedrock-extraction.ts:38:  key: string;
src/modules/document\persistence\s3-document-object-store.ts:13:      const result = await this.client.send(new HeadObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src/modules/document\persistence\s3-document-object-store.ts:27:  async copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference> {
src/modules/document\persistence\s3-document-object-store.ts:32:    // (tenant/<id>/item/<id>/document/<id>/slot/<id>/<uuid>), so this affected every real
src/modules/document\persistence\s3-document-object-store.ts:40:        Key: destinationKey,
src/modules/document\persistence\s3-document-object-store.ts:45:    return { bucket: destinationBucket, key: destinationKey, versionId: result.VersionId ?? "" };
src/modules/document\persistence\s3-document-object-store.ts:49:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src/workers\document-chasing-dispatch\dispatch.ts:117:            key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\document-chasing-dispatch\dispatch.ts:162:          key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\document-chasing-dispatch\dispatch.ts:245:        key: documentRequestKey(tenantId, subjectId, assignmentId, documentRequestId),
src/workers\document-chasing-dispatch\dispatch.ts:255:        key: { PK: occurrence.PK, SK: occurrence.SK },
src/modules/import\ports\import-object-store.ts:11:  getObject(bucket: string, key: string): Promise<Buffer>;
src/modules/import\ports\import-object-store.ts:12:  putObject(bucket: string, key: string, body: string, contentType: string): Promise<void>;
src/workers\document-purge\purge.ts:126:          key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:177:        key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:196:          key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:222:            key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:237:          key: { PK: candidate.PK, SK: candidate.SK },
src/workers\reminder-reconciliation\reconciliation.ts:91:            key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-reconciliation\reconciliation.ts:174:                  key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-reconciliation\reconciliation.ts:194:              key: { PK: occurrence.PK, SK: occurrence.SK },
src/modules/extraction\application\start-ocr.ts:51:  cleanObject: { bucket: string; key: string; versionId: string };
src/modules/extraction\application\start-ocr.ts:89:    // quota exhaustion (a genuine cap would use a per-tenant/per-period window, not per-run).
src/modules/extraction\application\start-ocr.ts:98:      key: input.cleanObject.key,
src/modules/document\persistence\lambda-pdf-parser.ts:13:  async parse(ref: { bucket: string; key: string; versionId: string }): Promise<PdfParseResult> {
src/modules/extraction\application\start-extraction-run.ts:25:  cleanObject: { bucket: string; key: string; versionId: string };
src/modules/document\persistence\dynamodb-document-store.ts:14:  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead = true): Promise<T | undefined> {
src/modules/document\persistence\dynamodb-document-store.ts:16:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: consistentRead }));
src/modules/document\persistence\dynamodb-document-store.ts:51:      let exclusiveStartKey: Record<string, unknown> | undefined;
src/modules/document\persistence\dynamodb-document-store.ts:58:            ExclusiveStartKey: exclusiveStartKey,
src/modules/document\persistence\dynamodb-document-candidate-source.ts:28:          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
src/modules/extraction\application\run-extraction-validation.ts:176:      documentKey: docKey,
src/workers\submission-malware-result\result-processor.ts:15:  object: { bucket: string; key: string; versionId: string };
src/modules/import\persistence\s3-import-object-store.ts:17:  async getObject(bucket: string, key: string): Promise<Buffer> {
src/modules/import\persistence\s3-import-object-store.ts:18:    const result = await this.client.send(new GetObjectCommand({ Bucket: bucket, Key: key }));
src/modules/import\persistence\s3-import-object-store.ts:22:  async putObject(bucket: string, key: string, body: string, contentType: string): Promise<void> {
src/modules/import\persistence\s3-import-object-store.ts:23:    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));
src/modules/import\domain\import-raw-key.ts:3: * ImportService.reserveImport() (`tenant/<tenantId>/imports/<jobId>/raw.csv`). Used only at
src/modules/import\domain\import-raw-key.ts:17:export function parseImportRawKey(key: string): ParsedImportRawKey | undefined {
src/modules/extraction\ports\textract-client.ts:6:  key: string;
src/modules/import\application\import-service.ts:49:  deduplicationKey: string;
src/modules/import\application\import-service.ts:90:  async reserveImport(ctx: RequestContext, input: ReserveImportInput, idempotencyKey: string): Promise<ReserveImportResult> {
src/modules/import\application\import-service.ts:111:    const begin = await this.idempotency.begin({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, requestHash, expiresAt });
src/modules/import\application\import-service.ts:138:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: jobId });
src/modules/import\application\import-service.ts:141:    const key = this.rawObjectKey(ctx.tenant.tenantId, jobId);
src/modules/import\application\import-service.ts:175:          key: importJobKey(ctx.tenant.tenantId, jobId),
src/modules/import\application\import-service.ts:194:      deduplicationKey: `${ctx.tenant.tenantId}|${jobId}|${expectedVersion + 1}`,
src/modules/import\application\import-service.ts:219:  private rawObjectKey(tenantId: string, jobId: string): string {
src/modules/import\application\import-service.ts:221:    return `tenant/${tenantId}/imports/${jobId}/raw.csv`;
src/modules/extraction\ports\extraction-run-store.ts:10:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
src/modules/extraction\ports\extraction-run-store.ts:24:  updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean>;
src/modules/import\persistence\dynamodb-import-store.ts:17:  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
src/modules/import\persistence\dynamodb-import-store.ts:19:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: true }));
src/modules/import\persistence\dynamodb-import-store.ts:59:      let exclusiveStartKey: Record<string, unknown> | undefined;
src/modules/import\persistence\dynamodb-import-store.ts:66:            ExclusiveStartKey: exclusiveStartKey,
src/workers\submission-finalizer\finalizer.ts:18:  object: { bucket: string; key: string; versionId: string };
src/modules/import\application\import-parse-service.ts:42:  return `tenant/${tenantId}/imports/${jobId}/plan/page-0.jsonl`;
src/modules/import\application\import-parse-service.ts:55:    const rawKey = `tenant/${tenantId}/imports/${jobId}/raw.csv`;
src/modules/import\application\import-parse-service.ts:138:      planObjectKey: key,
src/modules/extraction\application\confirm-reject-field.ts:46:  idempotencyKey: string;
src/modules/extraction\application\confirm-reject-field.ts:58:  idempotencyKey: string;
src/modules/extraction\application\confirm-reject-field.ts:136:  const { key: runKey, run } = await readRun(deps, tenantId, params.documentId, params.runId);
src/modules/extraction\application\confirm-reject-field.ts:137:  const { key: documentKeyResolved, document } = await readDocument(deps, tenantId, params.itemId, params.documentId);
src/modules/extraction\application\confirm-reject-field.ts:138:  const { key: itemKeyResolved, item } = await readItem(deps, tenantId, params.itemId);
src/modules/extraction\application\confirm-reject-field.ts:176:    fieldKey: extractedFieldKey(tenantId, params.documentId, params.fieldName, params.runId),
src/modules/extraction\application\confirm-reject-field.ts:182:    documentKey: documentKeyResolved,
src/modules/extraction\application\confirm-reject-field.ts:184:    itemKey: itemKeyResolved,
src/modules/extraction\application\confirm-reject-field.ts:242:  const { key: runKey, run } = await readRun(deps, tenantId, params.documentId, params.runId);
src/modules/extraction\application\confirm-reject-field.ts:243:  const { key: documentKeyResolved, document } = await readDocument(deps, tenantId, params.itemId, params.documentId);
src/modules/extraction\application\confirm-reject-field.ts:256:    fieldKey: extractedFieldKey(tenantId, params.documentId, params.fieldName, params.runId),
src/modules/extraction\application\confirm-reject-field.ts:262:    documentKey: documentKeyResolved,
src/modules/extraction\ports\ocr-artifact-store.ts:7:  key: string;
src/workers\reminder-producer\producer.ts:59:  deduplicationKey: string;
src/workers\reminder-producer\producer.ts:209:              deduplicationKey: `${tenantId}|${occurrenceId}|${newVersion}`,
src/workers\reminder-producer\producer.ts:249:                  key: { PK: row.PK, SK: row.SK },
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:36:        key: input.runKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:45:      key: input.documentKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:57:              key: input.itemUpdate.key,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:89:  async get(key: EntityKey): Promise<ExtractedField | undefined> {
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:91:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:102:        key: input.fieldKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:110:    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:111:    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:117:            key: input.itemKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:124:      : buildVersionConditionCheck({ tableName: this.tableName, key: input.itemKey, expectedVersion: input.itemExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:144:        key: input.fieldKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:152:    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:153:    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });
src/modules/extraction\persistence\dynamodb-textract-job-store.ts:37:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: this.key(jobId) }));
src/modules/extraction\persistence\dynamodb-textract-job-store.ts:52:          Key: this.key(job.jobId),
src/modules/extraction\ports\document-reader.ts:10:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
src/modules/extraction\persistence\s3-ocr-artifact-store.ts:19:        Key: key,
src/modules/extraction\persistence\s3-ocr-artifact-store.ts:28:    const result = await this.client.send(new GetObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
src/modules/extraction\persistence\s3-ocr-artifact-store.ts:38:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
src/modules/extraction\ports\entity-reader.ts:11:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
src/modules/document\application\advance-after-evidence.ts:40:      key: uploadSlotKey(tenantId, uploadSlotId),
src/modules/document\application\advance-after-evidence.ts:56:  input: { tenantId: string; itemId: string; documentId: string; expectedObject: { bucket: string; key: string; versionId: string } },
src/modules/document\application\advance-after-evidence.ts:110:    const cleanKey = `clean/${doc.tenantId}/${doc.itemId}/${doc.documentId}`;
src/modules/extraction\ports\extracted-field-store.ts:22:  runKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:29:  documentKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:43:  key: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:67:  get(key: EntityKey): Promise<ExtractedField | undefined>;
src/modules/extraction\ports\extracted-field-store.ts:90:  fieldKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:94:  runKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:96:  documentKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:98:  itemKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:108:  fieldKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:112:  runKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:114:  documentKey: EntityKey;
src/modules/extraction\domain\textract-job.ts:8: * with no tenant/document context available before this record is read. A tenant-scoped key
src/modules/document\domain\clean-key.ts:3: * advanceAfterEvidence()'s promotion copy (`clean/<tenantId>/<itemId>/<documentId>`). Used
src/modules/document\domain\clean-key.ts:17:export function parseCleanKey(key: string): ParsedCleanKey | undefined {
src/modules/document\application\document-service.ts:75:  async reserveUpload(ctx: RequestContext, itemId: string, input: ReserveUploadInput, idempotencyKey: string): Promise<ReserveUploadResult> {
src/modules/document\application\document-service.ts:95:      key: idempotencyKey,
src/modules/document\application\document-service.ts:102:    let quarantineKey: string;
src/modules/document\application\document-service.ts:133:      quarantineKey = `tenant/${ctx.tenant.tenantId}/item/${itemId}/document/${documentId}/slot/${uploadSlotId}/${randomUUID()}`;
src/modules/document\application\document-service.ts:149:        quarantineObject: { bucket: this.quarantineBucket, key: quarantineKey, versionId: "" },
src/modules/document\application\document-service.ts:193:      key: quarantineKey,
src/modules/document\application\document-service.ts:202:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: documentId });
src/modules/extraction\ports\extraction-execution-starter.ts:14:  cleanObject: { bucket: string; key: string; versionId: string };
src/modules/document\domain\document-object-reference.ts:12:  key: string;
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:16:  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:18:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:35:  async updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean> {
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:47:          Key: command.Key,
src/modules/document\domain\upload-slot.ts:19:  quarantineKey: string;
src/modules/document\domain\quarantine-key.ts:3: * DocumentService.reserveUpload() (`tenant/<tenantId>/item/<itemId>/document/<documentId>/
src/modules/document\domain\quarantine-key.ts:17:export function parseQuarantineKey(key: string): ParsedQuarantineKey | undefined {
rg: regex parse error:
    (?:versioning|versioning_configuration|status\s*=\s*\)
    ^
error: unclosed group

 exited 1 in 267ms:
/** Real S3 adapter for DocumentObjectStore (M6). Every operation is scoped to an exact
 * bucket/key/versionId - never a bare key - per M6 design Â§"DecisÃµes-chave" (identidade
 * imutÃ¡vel de objeto). */
import { CopyObjectCommand, DeleteObjectCommand, HeadObjectCommand, S3Client } from "@aws-sdk/client-s3";
import type { DocumentObjectReference } from "../domain/document-object-reference.js";
import type { DocumentObjectStore, ObjectMetadata } from "../ports/document-object-store.js";

export class S3DocumentObjectStore implements DocumentObjectStore {
  constructor(private readonly client: S3Client) {}

  async headObject(ref: DocumentObjectReference): Promise<ObjectMetadata | undefined> {
    try {
      const result = await this.client.send(new HeadObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
      return {
        contentLength: result.ContentLength ?? 0,
        mediaType: result.ContentType ?? "application/octet-stream",
        checksumSha256: result.ChecksumSHA256,
      };
    } catch (err) {
      if (typeof err === "object" && err !== null && "name" in err && (err as { name?: unknown }).name === "NotFound") {
        return undefined;
      }
      throw err;
    }
  }

  async copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference> {
    // Real bug found via Camada 3 (2026-08-22): encodeURIComponent() on the FULL key also
    // encodes its "/" path separators as "%2F", producing a CopySource that doesn't resolve to
    // any real object (S3 keys use literal "/" as the segment delimiter; only the characters
    // WITHIN each segment need escaping). Every quarantine key has multiple "/" segments
    // (tenant/<id>/item/<id>/document/<id>/slot/<id>/<uuid>), so this affected every real
    // promotion attempt - confirmed via a real CopyObjectCommand failing with an opaque
    // "UnknownError" (S3's error response for a CopySource that doesn't parse as a real
    // bucket/key/versionId triple). Fixed by escaping each path segment independently.
    const encodedKey = source.key.split("/").map(encodeURIComponent).join("/");
    const result = await this.client.send(
      new CopyObjectCommand({
        Bucket: destinationBucket,
        Key: destinationKey,
        CopySource: `${source.bucket}/${encodedKey}?versionId=${source.versionId}`,
        ServerSideEncryption: "aws:kms",
      }),
    );
    return { bucket: destinationBucket, key: destinationKey, versionId: result.VersionId ?? "" };
  }

  async deleteObjectVersion(ref: DocumentObjectReference): Promise<void> {
    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
  }
}

export function createS3Client(): S3Client {
  return new S3Client({});
}

/** Real S3 adapter for `OcrArtifactStore`, writing to the `EXTRACTION_TRANSIENT` bucket
 * (`privacy-lgpd.md` Â§4). `delete()` is used ONLY by `ExtractionValidationTaskHandler` (M7 item
 * 7) â€” see the port's doc comment for why this method exists at all. */
import { S3Client, PutObjectCommand, GetObjectCommand, DeleteObjectCommand } from "@aws-sdk/client-s3";
import { randomUUID } from "node:crypto";
import type { OcrArtifactStore, ExtractionArtifactRef } from "../ports/ocr-artifact-store.js";

export class S3OcrArtifactStore implements OcrArtifactStore {
  constructor(
    private readonly client: S3Client,
    private readonly bucket: string,
  ) {}

  async put(runId: string, blocksJson: string): Promise<ExtractionArtifactRef> {
    const key = `ocr/${runId}/${randomUUID()}.json`;
    await this.client.send(
      new PutObjectCommand({
        Bucket: this.bucket,
        Key: key,
        Body: blocksJson,
        ContentType: "application/json",
      }),
    );
    return { bucket: this.bucket, key };
  }

  async get(ref: ExtractionArtifactRef): Promise<string> {
    const result = await this.client.send(new GetObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
    if (!result.Body) {
      throw new Error(`OCR artifact ${ref.bucket}/${ref.key} returned no body.`);
    }
    return result.Body.transformToString("utf-8");
  }

  async delete(ref: ExtractionArtifactRef): Promise<void> {
    // DeleteObjectCommand is already idempotent at the S3 API level (deleting a missing key
    // returns success, never a 404) - no extra existence check needed here.
    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
  }
}

export function createS3Client(): S3Client {
  return new S3Client({});
}

src/workers\upload-slot-reconciliation\reconciliation.ts:61:          key: uploadSlotKey(slot.tenantId, slot.uploadSlotId),
src/modules/document\ports\upload-url-signer.ts:6:  key: string;
src/modules/document\ports\pdf-parser.ts:14:  parse(ref: { bucket: string; key: string; versionId: string }): Promise<PdfParseResult>;
src/modules/document\ports\document-store.ts:8:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
src/modules/document\ports\document-object-store.ts:17:  copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference>;
src/workers\malware-result\result-processor.ts:15:  object: { bucket: string; key: string; versionId: string };
src/workers\reminder-dispatch\dispatch.ts:105:            key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-dispatch\dispatch.ts:161:        key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-dispatch\dispatch.ts:175:    buildVersionConditionCheck({ tableName: deps.tableName, key: itemKey(tenantId, itemId), expectedVersion: itemVersion, extra: { status: "ACTIVE" } }),
src/workers\reminder-dispatch\dispatch.ts:178:      key: policyKey(tenantId, occurrence.policyId),
src/modules/import\ports\import-store.ts:13:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
src/modules/document\persistence\s3-upload-url-signer.ts:17:      Key: input.key,
src/workers\upload-finalizer\finalizer.ts:15:  object: { bucket: string; key: string; versionId: string };
src/modules/extraction\domain\bedrock-extraction.ts:38:  key: string;
src/modules/document\persistence\s3-document-object-store.ts:13:      const result = await this.client.send(new HeadObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src/modules/document\persistence\s3-document-object-store.ts:27:  async copyObject(source: DocumentObjectReference, destinationBucket: string, destinationKey: string): Promise<DocumentObjectReference> {
src/modules/document\persistence\s3-document-object-store.ts:32:    // (tenant/<id>/item/<id>/document/<id>/slot/<id>/<uuid>), so this affected every real
src/modules/document\persistence\s3-document-object-store.ts:40:        Key: destinationKey,
src/modules/document\persistence\s3-document-object-store.ts:45:    return { bucket: destinationBucket, key: destinationKey, versionId: result.VersionId ?? "" };
src/modules/document\persistence\s3-document-object-store.ts:49:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key, VersionId: ref.versionId }));
src/workers\document-chasing-dispatch\dispatch.ts:117:            key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\document-chasing-dispatch\dispatch.ts:162:          key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\document-chasing-dispatch\dispatch.ts:245:        key: documentRequestKey(tenantId, subjectId, assignmentId, documentRequestId),
src/workers\document-chasing-dispatch\dispatch.ts:255:        key: { PK: occurrence.PK, SK: occurrence.SK },
src/modules/import\ports\import-object-store.ts:11:  getObject(bucket: string, key: string): Promise<Buffer>;
src/modules/import\ports\import-object-store.ts:12:  putObject(bucket: string, key: string, body: string, contentType: string): Promise<void>;
src/workers\document-purge\purge.ts:126:          key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:177:        key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:196:          key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:222:            key: { PK: candidate.PK, SK: candidate.SK },
src/workers\document-purge\purge.ts:237:          key: { PK: candidate.PK, SK: candidate.SK },
src/workers\reminder-reconciliation\reconciliation.ts:91:            key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-reconciliation\reconciliation.ts:174:                  key: { PK: occurrence.PK, SK: occurrence.SK },
src/workers\reminder-reconciliation\reconciliation.ts:194:              key: { PK: occurrence.PK, SK: occurrence.SK },
src/modules/extraction\application\start-ocr.ts:51:  cleanObject: { bucket: string; key: string; versionId: string };
src/modules/extraction\application\start-ocr.ts:89:    // quota exhaustion (a genuine cap would use a per-tenant/per-period window, not per-run).
src/modules/extraction\application\start-ocr.ts:98:      key: input.cleanObject.key,
src/modules/document\persistence\lambda-pdf-parser.ts:13:  async parse(ref: { bucket: string; key: string; versionId: string }): Promise<PdfParseResult> {
src/modules/extraction\application\start-extraction-run.ts:25:  cleanObject: { bucket: string; key: string; versionId: string };
src/modules/document\persistence\dynamodb-document-store.ts:14:  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead = true): Promise<T | undefined> {
src/modules/document\persistence\dynamodb-document-store.ts:16:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: consistentRead }));
src/modules/document\persistence\dynamodb-document-store.ts:51:      let exclusiveStartKey: Record<string, unknown> | undefined;
src/modules/document\persistence\dynamodb-document-store.ts:58:            ExclusiveStartKey: exclusiveStartKey,
src/modules/document\persistence\dynamodb-document-candidate-source.ts:28:          ExclusiveStartKey: input.cursor ? JSON.parse(input.cursor) : undefined,
src/modules/extraction\application\run-extraction-validation.ts:176:      documentKey: docKey,
src/workers\submission-malware-result\result-processor.ts:15:  object: { bucket: string; key: string; versionId: string };
src/modules/import\persistence\s3-import-object-store.ts:17:  async getObject(bucket: string, key: string): Promise<Buffer> {
src/modules/import\persistence\s3-import-object-store.ts:18:    const result = await this.client.send(new GetObjectCommand({ Bucket: bucket, Key: key }));
src/modules/import\persistence\s3-import-object-store.ts:22:  async putObject(bucket: string, key: string, body: string, contentType: string): Promise<void> {
src/modules/import\persistence\s3-import-object-store.ts:23:    await this.client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType }));
src/modules/import\domain\import-raw-key.ts:3: * ImportService.reserveImport() (`tenant/<tenantId>/imports/<jobId>/raw.csv`). Used only at
src/modules/import\domain\import-raw-key.ts:17:export function parseImportRawKey(key: string): ParsedImportRawKey | undefined {
src/modules/extraction\ports\textract-client.ts:6:  key: string;
src/modules/import\application\import-service.ts:49:  deduplicationKey: string;
src/modules/import\application\import-service.ts:90:  async reserveImport(ctx: RequestContext, input: ReserveImportInput, idempotencyKey: string): Promise<ReserveImportResult> {
src/modules/import\application\import-service.ts:111:    const begin = await this.idempotency.begin({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, requestHash, expiresAt });
src/modules/import\application\import-service.ts:138:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: jobId });
src/modules/import\application\import-service.ts:141:    const key = this.rawObjectKey(ctx.tenant.tenantId, jobId);
src/modules/import\application\import-service.ts:175:          key: importJobKey(ctx.tenant.tenantId, jobId),
src/modules/import\application\import-service.ts:194:      deduplicationKey: `${ctx.tenant.tenantId}|${jobId}|${expectedVersion + 1}`,
src/modules/import\application\import-service.ts:219:  private rawObjectKey(tenantId: string, jobId: string): string {
src/modules/import\application\import-service.ts:221:    return `tenant/${tenantId}/imports/${jobId}/raw.csv`;
src/modules/extraction\ports\extraction-run-store.ts:10:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
src/modules/extraction\ports\extraction-run-store.ts:24:  updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean>;
src/modules/import\persistence\dynamodb-import-store.ts:17:  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
src/modules/import\persistence\dynamodb-import-store.ts:19:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: true }));
src/modules/import\persistence\dynamodb-import-store.ts:59:      let exclusiveStartKey: Record<string, unknown> | undefined;
src/modules/import\persistence\dynamodb-import-store.ts:66:            ExclusiveStartKey: exclusiveStartKey,
src/workers\submission-finalizer\finalizer.ts:18:  object: { bucket: string; key: string; versionId: string };
src/modules/import\application\import-parse-service.ts:42:  return `tenant/${tenantId}/imports/${jobId}/plan/page-0.jsonl`;
src/modules/import\application\import-parse-service.ts:55:    const rawKey = `tenant/${tenantId}/imports/${jobId}/raw.csv`;
src/modules/import\application\import-parse-service.ts:138:      planObjectKey: key,
src/modules/extraction\application\confirm-reject-field.ts:46:  idempotencyKey: string;
src/modules/extraction\application\confirm-reject-field.ts:58:  idempotencyKey: string;
src/modules/extraction\application\confirm-reject-field.ts:136:  const { key: runKey, run } = await readRun(deps, tenantId, params.documentId, params.runId);
src/modules/extraction\application\confirm-reject-field.ts:137:  const { key: documentKeyResolved, document } = await readDocument(deps, tenantId, params.itemId, params.documentId);
src/modules/extraction\application\confirm-reject-field.ts:138:  const { key: itemKeyResolved, item } = await readItem(deps, tenantId, params.itemId);
src/modules/extraction\application\confirm-reject-field.ts:176:    fieldKey: extractedFieldKey(tenantId, params.documentId, params.fieldName, params.runId),
src/modules/extraction\application\confirm-reject-field.ts:182:    documentKey: documentKeyResolved,
src/modules/extraction\application\confirm-reject-field.ts:184:    itemKey: itemKeyResolved,
src/modules/extraction\application\confirm-reject-field.ts:242:  const { key: runKey, run } = await readRun(deps, tenantId, params.documentId, params.runId);
src/modules/extraction\application\confirm-reject-field.ts:243:  const { key: documentKeyResolved, document } = await readDocument(deps, tenantId, params.itemId, params.documentId);
src/modules/extraction\application\confirm-reject-field.ts:256:    fieldKey: extractedFieldKey(tenantId, params.documentId, params.fieldName, params.runId),
src/modules/extraction\application\confirm-reject-field.ts:262:    documentKey: documentKeyResolved,
src/modules/extraction\ports\ocr-artifact-store.ts:7:  key: string;
src/workers\reminder-producer\producer.ts:59:  deduplicationKey: string;
src/workers\reminder-producer\producer.ts:209:              deduplicationKey: `${tenantId}|${occurrenceId}|${newVersion}`,
src/workers\reminder-producer\producer.ts:249:                  key: { PK: row.PK, SK: row.SK },
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:36:        key: input.runKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:45:      key: input.documentKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:57:              key: input.itemUpdate.key,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:89:  async get(key: EntityKey): Promise<ExtractedField | undefined> {
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:91:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:102:        key: input.fieldKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:110:    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:111:    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:117:            key: input.itemKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:124:      : buildVersionConditionCheck({ tableName: this.tableName, key: input.itemKey, expectedVersion: input.itemExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:144:        key: input.fieldKey,
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:152:    const runGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.runKey, expectedVersion: input.runExpectedVersion });
src/modules/extraction\persistence\dynamodb-extracted-field-store.ts:153:    const documentGuard = buildVersionConditionCheck({ tableName: this.tableName, key: input.documentKey, expectedVersion: input.documentExpectedVersion });
src/modules/extraction\persistence\dynamodb-textract-job-store.ts:37:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: this.key(jobId) }));
src/modules/extraction\persistence\dynamodb-textract-job-store.ts:52:          Key: this.key(job.jobId),
src/modules/extraction\ports\document-reader.ts:10:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
src/modules/extraction\persistence\s3-ocr-artifact-store.ts:19:        Key: key,
src/modules/extraction\persistence\s3-ocr-artifact-store.ts:28:    const result = await this.client.send(new GetObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
src/modules/extraction\persistence\s3-ocr-artifact-store.ts:38:    await this.client.send(new DeleteObjectCommand({ Bucket: ref.bucket, Key: ref.key }));
src/modules/extraction\ports\entity-reader.ts:11:  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
src/modules/document\application\advance-after-evidence.ts:40:      key: uploadSlotKey(tenantId, uploadSlotId),
src/modules/document\application\advance-after-evidence.ts:56:  input: { tenantId: string; itemId: string; documentId: string; expectedObject: { bucket: string; key: string; versionId: string } },
src/modules/document\application\advance-after-evidence.ts:110:    const cleanKey = `clean/${doc.tenantId}/${doc.itemId}/${doc.documentId}`;
src/modules/extraction\ports\extracted-field-store.ts:22:  runKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:29:  documentKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:43:  key: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:67:  get(key: EntityKey): Promise<ExtractedField | undefined>;
src/modules/extraction\ports\extracted-field-store.ts:90:  fieldKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:94:  runKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:96:  documentKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:98:  itemKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:108:  fieldKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:112:  runKey: EntityKey;
src/modules/extraction\ports\extracted-field-store.ts:114:  documentKey: EntityKey;
src/modules/extraction\domain\textract-job.ts:8: * with no tenant/document context available before this record is read. A tenant-scoped key
src/modules/document\domain\clean-key.ts:3: * advanceAfterEvidence()'s promotion copy (`clean/<tenantId>/<itemId>/<documentId>`). Used
src/modules/document\domain\clean-key.ts:17:export function parseCleanKey(key: string): ParsedCleanKey | undefined {
src/modules/document\application\document-service.ts:75:  async reserveUpload(ctx: RequestContext, itemId: string, input: ReserveUploadInput, idempotencyKey: string): Promise<ReserveUploadResult> {
src/modules/document\application\document-service.ts:95:      key: idempotencyKey,
src/modules/document\application\document-service.ts:102:    let quarantineKey: string;
src/modules/document\application\document-service.ts:133:      quarantineKey = `tenant/${ctx.tenant.tenantId}/item/${itemId}/document/${documentId}/slot/${uploadSlotId}/${randomUUID()}`;
src/modules/document\application\document-service.ts:149:        quarantineObject: { bucket: this.quarantineBucket, key: quarantineKey, versionId: "" },
src/modules/document\application\document-service.ts:193:      key: quarantineKey,
src/modules/document\application\document-service.ts:202:      await this.idempotency.complete({ tenantId: ctx.tenant.tenantId, operation: OPERATION, key: idempotencyKey, responseRef: documentId });
src/modules/extraction\ports\extraction-execution-starter.ts:14:  cleanObject: { bucket: string; key: string; versionId: string };
src/modules/document\domain\document-object-reference.ts:12:  key: string;
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:16:  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined> {
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:18:      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key }));
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:35:  async updateStatus(key: EntityKey, tenantId: string, expectedVersion: number, status: "DISCARDED", completedAt: string): Promise<boolean> {
src/modules/extraction\persistence\dynamodb-extraction-run-store.ts:47:          Key: command.Key,
src/modules/document\domain\upload-slot.ts:19:  quarantineKey: string;
src/modules/document\domain\quarantine-key.ts:3: * DocumentService.reserveUpload() (`tenant/<tenantId>/item/<itemId>/document/<documentId>/
src/modules/document\domain\quarantine-key.ts:17:export function parseQuarantineKey(key: string): ParsedQuarantineKey | undefined {
rg: regex parse error:
    (?:versioning|versioning_configuration|status\s*=\s*\)
    ^
error: unclosed group

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"aws_s3_bucket_versioning|versioning_configuration|versioning|status = \\\"Enabled\\\"\" infra/modules/document-buckets infra/modules/import-bucket infra/modules | Select-Object -First 300; Get-Content -Raw infra/modules/document-buckets/main.tf; Get-Content -Raw infra/modules/import-bucket/main.tf" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 321ms:
Prompt Mestre — Engineering Maturity Review do Expiration Tracker.md:698:- semantic versioning quando aplicável;
docs\00-prompt-mestre.md:853:- API versioning;
docs\00-prompt-mestre.md:1075:- versioning;
docs\00-prompt-mestre.md:1107:- versioning;
docs\00-prompt-mestre.md:1629:- versioning;
infra\main.tf:1702:# `EXTRACTION_TRANSIENT` S3 class (privacy-lgpd.md §4) - dedicated bucket, no versioning/
infra\modules\document-buckets\tests\document_buckets.tftest.hcl:53:    condition     = aws_s3_bucket_versioning.quarantine.versioning_configuration[0].status == "Enabled"
infra\modules\document-buckets\tests\document_buckets.tftest.hcl:54:    error_message = "Quarantine bucket must have versioning enabled"
infra\modules\document-buckets\tests\document_buckets.tftest.hcl:58:    condition     = aws_s3_bucket_versioning.clean.versioning_configuration[0].status == "Enabled"
infra\modules\document-buckets\tests\document_buckets.tftest.hcl:59:    error_message = "Clean bucket must have versioning enabled"
infra\modules\document-buckets\main.tf:53:resource "aws_s3_bucket_versioning" "quarantine" {
infra\modules\document-buckets\main.tf:55:  versioning_configuration {
infra\modules\document-buckets\main.tf:61:  depends_on = [aws_s3_bucket_versioning.quarantine]
infra\modules\document-buckets\main.tf:145:resource "aws_s3_bucket_versioning" "clean" {
infra\modules\document-buckets\main.tf:147:  versioning_configuration {
infra\modules\spa-hosting\main.tf:52:resource "aws_s3_bucket_versioning" "spa" {
infra\modules\spa-hosting\main.tf:54:  versioning_configuration {
infra\modules\deploy-manifest-bucket\tests\deploy_manifest_bucket.tftest.hcl:40:    condition     = aws_s3_bucket_versioning.this.versioning_configuration[0].status == "Enabled"
infra\modules\deploy-manifest-bucket\tests\deploy_manifest_bucket.tftest.hcl:41:    error_message = "Bucket must have versioning enabled"
infra\modules\deploy-manifest-bucket\main.tf:45:resource "aws_s3_bucket_versioning" "this" {
infra\modules\deploy-manifest-bucket\main.tf:47:  versioning_configuration {
infra\modules\deploy-manifest-bucket\main.tf:53:  # Requires versioning to be configured on the bucket first (aws provider ordering).
infra\modules\deploy-manifest-bucket\main.tf:54:  depends_on = [aws_s3_bucket_versioning.this]
docs\architecture\reviews\m7-extraction-design\codex-proposal-round1.md:1071:- sem versioning;
infra\modules\import-bucket\tests\import_bucket.tftest.hcl:30:    condition     = aws_s3_bucket_versioning.import.versioning_configuration[0].status == "Enabled"
infra\modules\import-bucket\tests\import_bucket.tftest.hcl:31:    error_message = "Import bucket must have versioning enabled"
infra\modules\import-bucket\main.tf:39:resource "aws_s3_bucket_versioning" "import" {
infra\modules\import-bucket\main.tf:41:  versioning_configuration {
infra\modules\import-bucket\main.tf:47:  depends_on = [aws_s3_bucket_versioning.import]
docs\engineering\reviews\_codex-output-checkpoint1-round1.txt:859:- semantic versioning quando aplicável;
docs\engineering\reviews\_codex-output-checkpoint1-round1.txt:3093:- semantic versioning quando aplicável;
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-prompt-round3.txt:23:   infra/modules/document-buckets, infra/modules/import-bucket por versioning)
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round3-critique-full.txt:1073:resource "aws_s3_bucket_versioning" "quarantine" {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round3-critique-full.txt:1075:  versioning_configuration {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round3-critique-full.txt:1081:  depends_on = [aws_s3_bucket_versioning.quarantine]
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round3-critique-full.txt:1165:resource "aws_s3_bucket_versioning" "clean" {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round3-critique-full.txt:1167:  versioning_configuration {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14244:infra\modules\document-buckets\main.tf:53:resource "aws_s3_bucket_versioning" "quarantine" {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14247:infra\modules\document-buckets\main.tf:61:  depends_on = [aws_s3_bucket_versioning.quarantine]
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14324:resource "aws_s3_bucket_versioning" "quarantine" {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14326:  versioning_configuration {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14332:  depends_on = [aws_s3_bucket_versioning.quarantine]
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14416:resource "aws_s3_bucket_versioning" "clean" {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14418:  versioning_configuration {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14530:resource "aws_s3_bucket_versioning" "import" {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14532:  versioning_configuration {
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round2-critique-full.txt:14538:  depends_on = [aws_s3_bucket_versioning.import]
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:778:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "GSI6PK|CLAIM|PENDING|claim|lease|legalHold|DocumentStatus|deleteObjectVersion|quarantineObject|cleanObject|purgeAfter|TransactWrite|LastEvaluatedKey|gsi6_read|EXACTLY|lifecycle|versioning|alarm|metric" src/modules/document src/modules/reminder/application/reminder-materializer.ts src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts infra/main.tf infra/modules/dynamo-table/main.tf infra/modules/document-buckets/main.tf; Get-Content -Raw src/modules/reminder/application/reminder-materializer.ts; Get-Content -Raw src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:780:infra/modules/document-buckets/main.tf:53:resource "aws_s3_bucket_versioning" "quarantine" {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:781:infra/modules/document-buckets/main.tf:55:  versioning_configuration {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:783:infra/modules/document-buckets/main.tf:61:  depends_on = [aws_s3_bucket_versioning.quarantine]
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:786:infra/modules/document-buckets/main.tf:145:resource "aws_s3_bucket_versioning" "clean" {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:787:infra/modules/document-buckets/main.tf:147:  versioning_configuration {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:800:infra/main.tf:1605:# `EXTRACTION_TRANSIENT` S3 class (privacy-lgpd.md §4) - dedicated bucket, no versioning/
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round2-critique-full.txt:873:src/modules/document\domain\document-object-reference.ts:5: * alone. With versioning enabled on both document buckets, a bare key can refer to multiple
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round5-critique-full.txt:2264:docs/architecture/reviews/w3-06-user-document-purge-design\codex-round2-critique-full.txt:778:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "GSI6PK|CLAIM|PENDING|claim|lease|legalHold|DocumentStatus|deleteObjectVersion|quarantineObject|cleanObject|purgeAfter|TransactWrite|LastEvaluatedKey|gsi6_read|EXACTLY|lifecycle|versioning|alarm|metric" src/modules/document src/modules/reminder/application/reminder-materializer.ts src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts infra/main.tf infra/modules/dynamo-table/main.tf infra/modules/document-buckets/main.tf; Get-Content -Raw src/modules/reminder/application/reminder-materializer.ts; Get-Content -Raw src/modules/reminder/persistence/dynamodb-reconciliation-candidate-source.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round5-critique-full.txt:2266:docs/architecture/reviews/w3-06-user-document-purge-design\codex-round2-critique-full.txt-780-infra/modules/document-buckets/main.tf:53:resource "aws_s3_bucket_versioning" "quarantine" {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round5-critique-full.txt:2267:docs/architecture/reviews/w3-06-user-document-purge-design\codex-round2-critique-full.txt-781-infra/modules/document-buckets/main.tf:55:  versioning_configuration {
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round5-critique-full.txt:2269:docs/architecture/reviews/w3-06-user-document-purge-design\codex-round2-critique-full.txt-783-infra/modules/document-buckets/main.tf:61:  depends_on = [aws_s3_bucket_versioning.quarantine]
docs\architecture\reviews\w3-06-user-document-purge-design\codex-round5-critique-full.txt:2276:docs/architecture/reviews/w3-06-user-document-purge-design\codex-round2-critique-full.txt:800:infra/main.tf:1605:# `EXTRACTION_TRANSIENT` S3 class (privacy-lgpd.md §4) - dedicated bucket, no versioning/
docs\architecture\reviews\w3-07-tenant-cascade-deletion-design\codex-round2-critique-full.txt:8296:# `EXTRACTION_TRANSIENT` S3 class (privacy-lgpd.md Â§4) - dedicated bucket, no versioning/
src\modules\extraction\ports\ocr-artifact-store.ts:2: * `claude-reconciliation-final-design.md` §1.4) — dedicated bucket/prefix, no versioning/
src\modules\document\domain\document-object-reference.ts:5: * alone. With versioning enabled on both document buckets, a bare key can refer to multiple
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round3-critique-full.txt:36:   infra/modules/document-buckets, infra/modules/import-bucket por versioning)
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round3-critique-full.txt:11798:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/document/persistence/s3-document-object-store.ts; Get-Content -Raw src/modules/extraction/persistence/s3-ocr-artifact-store.ts; rg -n \"key:|Key:|objectKey|rawObjectKey|clean/|quarantine/|tenant/\" src/modules/document src/modules/extraction src/modules/import src/workers | Select-Object -First 400; rg -n \"versioning|versioning_configuration|status\\s*=\\s*\\\"Enabled\\\"|aws_s3_bucket_versioning\" infra/modules/document-buckets infra/modules/import-bucket infra | Select-Object -First 300" in C:\Users\Usuario\Desktop\projects\expiration-tracker
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round3-critique-full.txt:12045:    (?:versioning|versioning_configuration|status\s*=\s*\)
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round3-critique-full.txt:12293:    (?:versioning|versioning_configuration|status\s*=\s*\)
docs\architecture\reviews\w3-07-tenant-fence-round2-design\codex-round3-critique-full.txt:12298:"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"aws_s3_bucket_versioning|versioning_configuration|versioning|status = \\\"Enabled\\\"\" infra/modules/document-buckets infra/modules/import-bucket infra/modules | Select-Object -First 300; Get-Content -Raw infra/modules/document-buckets/main.tf; Get-Content -Raw infra/modules/import-bucket/main.tf" in C:\Users\Usuario\Desktop\projects\expiration-tracker
# --- KMS: the AWS-managed S3 key (alias/aws/s3), NOT a customer-managed key -----------------
# Real cost decision (2026-08-22, Marcelo direct call - dispensa o protocolo Claude<->Codex
# por ser simplificaÃ§Ã£o por custo, nÃ£o reabertura de arquitetura, ver decisions-log.md):
# a D-016 original (chaves separadas por bucket) previa 2 CMKs (~US$1/mÃªs cada, ~US$2/mÃªs fixo
# sÃ³ por existirem, mesmo com zero uploads em dev). AWS-managed keys nÃ£o tÃªm a cobranÃ§a mensal
# de CMK - ambos os buckets agora compartilham a MESMA chave gerenciada pela AWS (sÃ³ existe uma
# "aws/s3" por conta, nÃ£o Ã© possÃ­vel ter uma por bucket sem voltar a ser uma CMK). A separaÃ§Ã£o
# de seguranÃ§a entre quarantine/clean continua vindo dos 2 buckets fÃ­sicos distintos + IAM
# least-privilege (cada funÃ§Ã£o sÃ³ recebe permissÃ£o explÃ­cita no bucket que precisa) - o
# controle principal jÃ¡ era esse, a chave prÃ³pria por bucket era uma camada extra, nÃ£o a Ãºnica.
data "aws_kms_key" "s3_managed" {
  key_id = "alias/aws/s3"
}

# --- Quarantine bucket -----------------------------------------------------------------------
# Real invokers: DocumentService signs a presigned PutObject scoped to a fresh key here;
# UploadFinalizerWorker/parser-sandbox read only the exact key/version they're told about;
# MalwareResultWorker reads + deletes on confirmed promotion. No business-facing Lambda role
# (items-handler etc.) ever receives any permission on this bucket.

resource "aws_s3_bucket" "quarantine" {
  bucket        = "${var.name_prefix}-documents-quarantine"
  force_destroy = false
  tags          = var.tags
}

resource "aws_s3_bucket_public_access_block" "quarantine" {
  bucket                  = aws_s3_bucket.quarantine.id
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

resource "aws_s3_bucket_ownership_controls" "quarantine" {
  bucket = aws_s3_bucket.quarantine.id
  rule {
    object_ownership = "BucketOwnerEnforced"
  }
}

resource "aws_s3_bucket_server_side_encryption_configuration" "quarantine" {
  bucket = aws_s3_bucket.quarantine.id
  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm     = "aws:kms"
      kms_master_key_id = data.aws_kms_key.s3_managed.arn
    }
    bucket_key_enabled = true
  }
}

resource "aws_s3_bucket_versioning" "quarantine" {
  bucket = aws_s3_bucket.quarantine.id
  versioning_configuration {
    status = "Enabled"
  }
}

resource "aws_s3_bucket_lifecycle_configuration" "quarantine" {
  depends_on = [aws_s3_bucket_versioning.quarantine]
  bucket     = aws_s3_bucket.quarantine.id

  rule {
    id     = "expire-transient-quarantine-content"
    status = "Enabled"

    filter {} # applies to every object - the whole bucket is transient by design.

    expiration {
      days = var.quarantine_lifecycle_days
    }

    noncurrent_version_expiration {
      noncurrent_days = var.quarantine_lifecycle_days
    }

    abort_incomplete_multipart_upload {
      days_after_initiation = 1
    }
  }
}

resource "aws_s3_bucket_policy" "quarantine" {
  bucket = aws_s3_bucket.quarantine.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyInsecureTransport"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource  = [aws_s3_bucket.quarantine.arn, "${aws_s3_bucket.quarantine.arn}/*"]
        Condition = { Bool = { "aws:SecureTransport" = "false" } }
      },
      {
        Sid       = "DenyWrongEncryption"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:PutObject"
        Resource  = "${aws_s3_bucket.quarantine.arn}/*"
        Condition = { StringNotEquals = { "s3:x-amz-server-side-encryption" = "aws:kms" } }
      }
    ]
  })
}

# --- Clean bucket ------------------------------------------------------------------------
# Real invokers: only MalwareResultWorker writes (the promotion copy). No M6 handler reads it
# (extraction/M7 will be the first real reader) - deliberately not wired to anything else yet.

resource "aws_s3_bucket" "clean" {
  bucket        = "${var.name_prefix}-documents-clean"
  force_destroy = false
  tags          = var.tags
}

resource "aws_s3_bucket_public_access_block" "clean" {
  bucket                  = aws_s3_bucket.clean.id
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

resource "aws_s3_bucket_ownership_controls" "clean" {
  bucket = aws_s3_bucket.clean.id
  rule {
    object_ownership = "BucketOwnerEnforced"
  }
}

resource "aws_s3_bucket_server_side_encryption_configuration" "clean" {
  bucket = aws_s3_bucket.clean.id
  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm     = "aws:kms"
      kms_master_key_id = data.aws_kms_key.s3_managed.arn
    }
    bucket_key_enabled = true
  }
}

resource "aws_s3_bucket_versioning" "clean" {
  bucket = aws_s3_bucket.clean.id
  versioning_configuration {
    status = "Enabled"
  }
}

resource "aws_s3_bucket_policy" "clean" {
  bucket = aws_s3_bucket.clean.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyInsecureTransport"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource  = [aws_s3_bucket.clean.arn, "${aws_s3_bucket.clean.arn}/*"]
        Condition = { Bool = { "aws:SecureTransport" = "false" } }
      },
      {
        Sid       = "DenyWrongEncryption"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:PutObject"
        Resource  = "${aws_s3_bucket.clean.arn}/*"
        Condition = { StringNotEquals = { "s3:x-amz-server-side-encryption" = "aws:kms" } }
      }
    ]
  })
}

# --- EventBridge notifications on the quarantine bucket (S3 "Object Created" -> EventBridge) --
# Required so document-malware-protection's rule (and the finalizer's own S3-event rule, wired
# at root) can match on this bucket's real events.

resource "aws_s3_bucket_notification" "quarantine" {
  bucket      = aws_s3_bucket.quarantine.id
  eventbridge = true
}

# --- KMS: same AWS-managed S3 key as document-buckets (2026-08-22 cost decision, no per-key
# monthly charge, security boundary is the bucket + IAM least-privilege, not the key). ---------
data "aws_kms_key" "s3_managed" {
  key_id = "alias/aws/s3"
}

resource "aws_s3_bucket" "import" {
  bucket        = "${var.name_prefix}-imports"
  force_destroy = false
  tags          = var.tags
}

resource "aws_s3_bucket_public_access_block" "import" {
  bucket                  = aws_s3_bucket.import.id
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

resource "aws_s3_bucket_ownership_controls" "import" {
  bucket = aws_s3_bucket.import.id
  rule {
    object_ownership = "BucketOwnerEnforced"
  }
}

resource "aws_s3_bucket_server_side_encryption_configuration" "import" {
  bucket = aws_s3_bucket.import.id
  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm     = "aws:kms"
      kms_master_key_id = data.aws_kms_key.s3_managed.arn
    }
    bucket_key_enabled = true
  }
}

resource "aws_s3_bucket_versioning" "import" {
  bucket = aws_s3_bucket.import.id
  versioning_configuration {
    status = "Enabled"
  }
}

resource "aws_s3_bucket_lifecycle_configuration" "import" {
  depends_on = [aws_s3_bucket_versioning.import]
  bucket     = aws_s3_bucket.import.id

  rule {
    id     = "expire-import-artifacts"
    status = "Enabled"

    filter {} # whole bucket is transient (raw CSV + plan JSONL, never a long-term store).

    expiration {
      days = var.lifecycle_days
    }

    noncurrent_version_expiration {
      noncurrent_days = var.lifecycle_days
    }

    abort_incomplete_multipart_upload {
      days_after_initiation = 1
    }
  }
}

resource "aws_s3_bucket_policy" "import" {
  bucket = aws_s3_bucket.import.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyInsecureTransport"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource  = [aws_s3_bucket.import.arn, "${aws_s3_bucket.import.arn}/*"]
        Condition = { Bool = { "aws:SecureTransport" = "false" } }
      },
      {
        Sid       = "DenyWrongEncryption"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:PutObject"
        Resource  = "${aws_s3_bucket.import.arn}/*"
        Condition = { StringNotEquals = { "s3:x-amz-server-side-encryption" = "aws:kms" } }
      }
    ]
  })
}

# EventBridge notifications (S3 "Object Created") - required for the parse-trigger rule at
# root to match real events on this bucket (same pattern as document-buckets' quarantine).
resource "aws_s3_bucket_notification" "import" {
  bucket      = aws_s3_bucket.import.id
  eventbridge = true
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src/modules/document/ports/document-store.ts; Get-Content -Raw src/modules/document/persistence/dynamodb-document-store.ts; Get-Content -Raw src/modules/expiration/ports/expiration-store.ts; rg -n \"BatchWriteCommand|PutCommand|UpdateCommand|DeleteCommand|TransactWriteCommand|import \\* as .*@aws-sdk/lib-dynamodb\" src --glob \"*.ts\" | Measure-Object; rg -n \"BatchWriteCommand|BatchWriteItemCommand\" src --glob \"*.ts\"" in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-28T21:21:28.436025Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.3 seconds
Output:
/** DynamoDB surface the Document module needs â€” same port pattern as reminder-store.ts/
 * expiration-store.ts (AGENTS.md Â§6). */
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface DocumentStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** BLOCKER-A: lists a Document's item partition (`TENANT#t#ITEM#i`) filtered by SK prefix
   * (`DOC#`) â€” same shape as ExpirationStore.queryByPk/ReminderStore's item-partition query,
   * no new GSI needed since Document is already keyed under the item's own partition
   * (data-model.md line 34). */
  queryByPk<T extends EntityKey = Record<string, unknown> & EntityKey>(pk: string, skPrefix?: string): Promise<T[]>;
}

/** Global (non-tenant-prefixed) GSI6 key for slots pending reconciliation â€” same convention
 * already used by reminder-reconciliation/outbox-sweeper (WORKSTATE#CLAIMED,
 * RECON#OUTBOX#PENDING). Documented explicitly here because adding a THIRD real consumer of
 * GSI6 is itself a real structural change (M6 design Â§"riscos") to an isolation boundary that
 * used to be closed to exactly 2 roles â€” see infra/tests/stack.tftest.hcl's
 * gsi6_access_granted_only_to_reconciliation_and_sweeper test, renamed as part of this
 * milestone to include upload-slot-reconciliation. */
export const GSI6PK_RECON_UPLOAD_PENDING = "RECON#UPLOAD#PENDING";

/** Real bug found via Camada 3 verification against AWS real (2026-08-25, dev account):
 * `reserveUpload` never actually wrote `GSI6PK`/`GSI6SK` onto the `UploadSlot` it created -
 * confirmed empirically (a fabricated expired RESERVED slot with no GSI6 attributes was
 * invisible to the real deployed worker, `resultCount: 0`; the SAME item with these two
 * attributes added manually was found and correctly reconciled on the next invocation).
 * Every expired upload reservation in production was silently never cleaned up. Same
 * `<sortKey>#TENANT#<tenantId>#<ENTITY>#<id>` uniqueness-suffix convention as
 * `buildExpiredClaimGsi6Sk`/`buildDstCandidateGsi6Sk` (reminder module) - required because
 * GSI6SK must be unique per GSI6PK partition, and two slots can share the same expiresAt. */
export function buildUploadSlotGsi6Sk(expiresAt: string, tenantId: string, uploadSlotId: string): string {
  return `${expiresAt}#TENANT#${tenantId}#SLOT#${uploadSlotId}`;
}

/** W3-06 (D-061): purge worklist pointers, same global WORKSTATE convention. `PENDING` is the
 * candidate queue (written by `DocumentDeletionService` in the same transaction as the
 * soft-delete); `CLAIMED` is the lease state `DocumentPurgeWorker` moves a `Document` into while
 * it performs the (non-transactional) S3 deletion. `DocumentPurgeReceipt` never needs `CLAIMED`
 * - it has no external side effect to protect with a lease, so it is deleted directly from
 * `PENDING` in one conditioned transaction. */
export const GSI6PK_PURGE_PENDING = "WORKSTATE#PURGE_PENDING";
export const GSI6PK_PURGE_CLAIMED = "WORKSTATE#PURGE_CLAIMED";

export function buildDocumentPurgeGsi6Sk(purgeAfter: string, tenantId: string, documentId: string): string {
  return `${purgeAfter}#TENANT#${tenantId}#DOCUMENT#${documentId}`;
}

export function buildDocumentPurgeClaimGsi6Sk(claimExpiresAt: string, tenantId: string, documentId: string): string {
  return `${claimExpiresAt}#TENANT#${tenantId}#DOCUMENT#${documentId}`;
}

export function buildPurgeReceiptGsi6Sk(purgeAfter: string, tenantId: string, documentId: string): string {
  return `${purgeAfter}#TENANT#${tenantId}#PURGERECEIPT#${documentId}`;
}

export interface Gsi6QueryInput {
  gsi6pk: string;
  before: string;
  pageSize?: number;
  cursor?: string;
}

export interface Page<T> {
  items: T[];
  cursor?: string;
}

/** Narrow port, injected ONLY into the UploadSlotReconciliationWorker â€” same isolation
 * safeguard pattern as ReminderProducerStore/ReminderReconciliationCandidateSource. */
export interface UploadSlotReconciliationSource {
  queryExpiredSlots<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi6QueryInput): Promise<Page<T>>;
}

/** Real DynamoDB adapter for DocumentStore (M6). Same translation pattern as
 * DynamoDbReminderStore/DynamoDbExpirationStore. */
import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey, DocumentStore, TransactWriteEntry } from "../ports/document-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";

export class DynamoDbDocumentStore implements DocumentStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead = true): Promise<T | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: consistentRead }));
      return result.Item as T | undefined;
    } catch (err) {
      throw mapDynamoError(err, "DocumentStore.get");
    }
  }

  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "DocumentStore.putIfAbsent");
    }
  }

  async update<T extends EntityKey>(item: T): Promise<void> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
    } catch (err) {
      throw mapDynamoError(err, "DocumentStore.update");
    }
  }

  // TransactionCanceledException must reach the caller as-is - never wrapped by mapDynamoError.
  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
    await this.client.send(
      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
    );
  }

  async queryByPk<T extends EntityKey = Record<string, unknown> & EntityKey>(pk: string, skPrefix?: string): Promise<T[]> {
    try {
      const items: T[] = [];
      let exclusiveStartKey: Record<string, unknown> | undefined;
      do {
        const result = await this.client.send(
          new QueryCommand({
            TableName: this.tableName,
            KeyConditionExpression: skPrefix ? "PK = :pk AND begins_with(SK, :prefix)" : "PK = :pk",
            ExpressionAttributeValues: skPrefix ? { ":pk": pk, ":prefix": skPrefix } : { ":pk": pk },
            ExclusiveStartKey: exclusiveStartKey,
          }),
        );
        items.push(...((result.Items ?? []) as T[]));
        exclusiveStartKey = result.LastEvaluatedKey;
      } while (exclusiveStartKey);
      return items;
    } catch (err) {
      throw mapDynamoError(err, "DocumentStore.queryByPk");
    }
  }
}

/**
 * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
 * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
 * inject an implementation, so domain/application logic is unit-testable without live
 * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
 * data-model.md Â§5's load-bearing sentence requires item update + outbox record (and,
 * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
 * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
 * be individually visible without the others.
 */
import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

// Re-exported for backward compatibility with existing importers (application/http layers
// of this and other modules) - canonical definitions now live in shared/dynamodb/occ.ts.
export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";

export interface Gsi1QueryInput {
  gsi1pk: string;
  ascending?: boolean;
  limit?: number;
}

export interface ExpirationStore {
  /** Strongly consistent single-item read (data-model.md Â§5: authorization/edition/pre-send reads must be consistent). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  /** Unconditional overwrite of an existing item - used only for non-OCC bookkeeping records (e.g. idempotency status transitions), never for ExpirationItem/AuditEvent. */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * Commits every entry atomically. Throws an error recognized by
   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
   * assume partial application.
   */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** Eventually consistent GSI1 query (data-model.md Â§3: vencimentos/dashboard). */
  queryGsi1<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi1QueryInput): Promise<T[]>;
  /**
   * Query pela partiÃ§Ã£o do item com prefixo opcional de SK â€” adiÃ§Ã£o puramente aditiva
   * (07-domain-model-escalation-watchers-digest.md, D-040) para listar `ItemWatch` sob a
   * mesma partiÃ§Ã£o de `ExpirationItem`, mesmo padrÃ£o de coleÃ§Ã£o que `Document` (M6) jÃ¡ usa.
   * NÃ£o muda nenhum mÃ©todo/comportamento existente â€” zero risco de regressÃ£o ao agregado
   * ExpirationItem jÃ¡ em produÃ§Ã£o.
   */
  queryByPk<T extends EntityKey = Record<string, unknown> & EntityKey>(pk: string, skPrefix?: string): Promise<T[]>;
}



Count    : 67
Average  : 
Sum      : 
Maximum  : 
Minimum  : 
Property : 




 exited 1 in 295ms:
/** DynamoDB surface the Document module needs â€” same port pattern as reminder-store.ts/
 * expiration-store.ts (AGENTS.md Â§6). */
export type { EntityKey, TransactWriteEntry, TransactPutEntry, TransactUpdateEntry } from "../../../shared/dynamodb/occ.js";
export { isTransactionCanceled, isConditionalCheckFailed } from "../../../shared/dynamodb/occ.js";
import type { EntityKey, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

export interface DocumentStore {
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead?: boolean): Promise<T | undefined>;
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  update<T extends EntityKey>(item: T): Promise<void>;
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** BLOCKER-A: lists a Document's item partition (`TENANT#t#ITEM#i`) filtered by SK prefix
   * (`DOC#`) â€” same shape as ExpirationStore.queryByPk/ReminderStore's item-partition query,
   * no new GSI needed since Document is already keyed under the item's own partition
   * (data-model.md line 34). */
  queryByPk<T extends EntityKey = Record<string, unknown> & EntityKey>(pk: string, skPrefix?: string): Promise<T[]>;
}

/** Global (non-tenant-prefixed) GSI6 key for slots pending reconciliation â€” same convention
 * already used by reminder-reconciliation/outbox-sweeper (WORKSTATE#CLAIMED,
 * RECON#OUTBOX#PENDING). Documented explicitly here because adding a THIRD real consumer of
 * GSI6 is itself a real structural change (M6 design Â§"riscos") to an isolation boundary that
 * used to be closed to exactly 2 roles â€” see infra/tests/stack.tftest.hcl's
 * gsi6_access_granted_only_to_reconciliation_and_sweeper test, renamed as part of this
 * milestone to include upload-slot-reconciliation. */
export const GSI6PK_RECON_UPLOAD_PENDING = "RECON#UPLOAD#PENDING";

/** Real bug found via Camada 3 verification against AWS real (2026-08-25, dev account):
 * `reserveUpload` never actually wrote `GSI6PK`/`GSI6SK` onto the `UploadSlot` it created -
 * confirmed empirically (a fabricated expired RESERVED slot with no GSI6 attributes was
 * invisible to the real deployed worker, `resultCount: 0`; the SAME item with these two
 * attributes added manually was found and correctly reconciled on the next invocation).
 * Every expired upload reservation in production was silently never cleaned up. Same
 * `<sortKey>#TENANT#<tenantId>#<ENTITY>#<id>` uniqueness-suffix convention as
 * `buildExpiredClaimGsi6Sk`/`buildDstCandidateGsi6Sk` (reminder module) - required because
 * GSI6SK must be unique per GSI6PK partition, and two slots can share the same expiresAt. */
export function buildUploadSlotGsi6Sk(expiresAt: string, tenantId: string, uploadSlotId: string): string {
  return `${expiresAt}#TENANT#${tenantId}#SLOT#${uploadSlotId}`;
}

/** W3-06 (D-061): purge worklist pointers, same global WORKSTATE convention. `PENDING` is the
 * candidate queue (written by `DocumentDeletionService` in the same transaction as the
 * soft-delete); `CLAIMED` is the lease state `DocumentPurgeWorker` moves a `Document` into while
 * it performs the (non-transactional) S3 deletion. `DocumentPurgeReceipt` never needs `CLAIMED`
 * - it has no external side effect to protect with a lease, so it is deleted directly from
 * `PENDING` in one conditioned transaction. */
export const GSI6PK_PURGE_PENDING = "WORKSTATE#PURGE_PENDING";
export const GSI6PK_PURGE_CLAIMED = "WORKSTATE#PURGE_CLAIMED";

export function buildDocumentPurgeGsi6Sk(purgeAfter: string, tenantId: string, documentId: string): string {
  return `${purgeAfter}#TENANT#${tenantId}#DOCUMENT#${documentId}`;
}

export function buildDocumentPurgeClaimGsi6Sk(claimExpiresAt: string, tenantId: string, documentId: string): string {
  return `${claimExpiresAt}#TENANT#${tenantId}#DOCUMENT#${documentId}`;
}

export function buildPurgeReceiptGsi6Sk(purgeAfter: string, tenantId: string, documentId: string): string {
  return `${purgeAfter}#TENANT#${tenantId}#PURGERECEIPT#${documentId}`;
}

export interface Gsi6QueryInput {
  gsi6pk: string;
  before: string;
  pageSize?: number;
  cursor?: string;
}

export interface Page<T> {
  items: T[];
  cursor?: string;
}

/** Narrow port, injected ONLY into the UploadSlotReconciliationWorker â€” same isolation
 * safeguard pattern as ReminderProducerStore/ReminderReconciliationCandidateSource. */
export interface UploadSlotReconciliationSource {
  queryExpiredSlots<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi6QueryInput): Promise<Page<T>>;
}

/** Real DynamoDB adapter for DocumentStore (M6). Same translation pattern as
 * DynamoDbReminderStore/DynamoDbExpirationStore. */
import { GetCommand, PutCommand, QueryCommand, TransactWriteCommand } from "@aws-sdk/lib-dynamodb";
import type { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import type { EntityKey, DocumentStore, TransactWriteEntry } from "../ports/document-store.js";
import { isConditionalCheckFailed, mapDynamoError } from "../../../shared/dynamodb/sdk-errors.js";

export class DynamoDbDocumentStore implements DocumentStore {
  constructor(
    private readonly client: DynamoDBDocumentClient,
    private readonly tableName: string,
  ) {}

  async get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey, consistentRead = true): Promise<T | undefined> {
    try {
      const result = await this.client.send(new GetCommand({ TableName: this.tableName, Key: key, ConsistentRead: consistentRead }));
      return result.Item as T | undefined;
    } catch (err) {
      throw mapDynamoError(err, "DocumentStore.get");
    }
  }

  async putIfAbsent<T extends EntityKey>(item: T): Promise<boolean> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item, ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)" }));
      return true;
    } catch (err) {
      if (isConditionalCheckFailed(err)) return false;
      throw mapDynamoError(err, "DocumentStore.putIfAbsent");
    }
  }

  async update<T extends EntityKey>(item: T): Promise<void> {
    try {
      await this.client.send(new PutCommand({ TableName: this.tableName, Item: item }));
    } catch (err) {
      throw mapDynamoError(err, "DocumentStore.update");
    }
  }

  // TransactionCanceledException must reach the caller as-is - never wrapped by mapDynamoError.
  async transactWrite(entries: TransactWriteEntry[]): Promise<void> {
    await this.client.send(
      new TransactWriteCommand({ TransactItems: entries as unknown as ConstructorParameters<typeof TransactWriteCommand>[0]["TransactItems"] }),
    );
  }

  async queryByPk<T extends EntityKey = Record<string, unknown> & EntityKey>(pk: string, skPrefix?: string): Promise<T[]> {
    try {
      const items: T[] = [];
      let exclusiveStartKey: Record<string, unknown> | undefined;
      do {
        const result = await this.client.send(
          new QueryCommand({
            TableName: this.tableName,
            KeyConditionExpression: skPrefix ? "PK = :pk AND begins_with(SK, :prefix)" : "PK = :pk",
            ExpressionAttributeValues: skPrefix ? { ":pk": pk, ":prefix": skPrefix } : { ":pk": pk },
            ExclusiveStartKey: exclusiveStartKey,
          }),
        );
        items.push(...((result.Items ?? []) as T[]));
        exclusiveStartKey = result.LastEvaluatedKey;
      } while (exclusiveStartKey);
      return items;
    } catch (err) {
      throw mapDynamoError(err, "DocumentStore.queryByPk");
    }
  }
}

/**
 * DynamoDB surface the expiration module needs. Follows the same SDK-agnostic port
 * pattern established by src/modules/identity/ports/identity-store.ts (M1): callers
 * inject an implementation, so domain/application logic is unit-testable without live
 * AWS. Unlike IdentityStore, this port also exposes `transactWrite` because
 * data-model.md Â§5's load-bearing sentence requires item update + outbox record (and,
 * for M2, the AuditEvent) to commit in a single `TransactWriteItems` â€” the aggregate
 * write, the critical `ItemDueDateChanged` outbox record and the audit trail must never
 * be individually visible without the others.
 */
import type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry } from "../../../shared/dynamodb/occ.js";

// Re-exported for backward compatibility with existing importers (application/http layers
// of this and other modules) - canonical definitions now live in shared/dynamodb/occ.ts.
export type { EntityKey, TransactPutEntry, TransactUpdateEntry, TransactWriteEntry };
export { TRANSACTION_CANCELED, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";

export interface Gsi1QueryInput {
  gsi1pk: string;
  ascending?: boolean;
  limit?: number;
}

export interface ExpirationStore {
  /** Strongly consistent single-item read (data-model.md Â§5: authorization/edition/pre-send reads must be consistent). */
  get<T extends EntityKey = Record<string, unknown> & EntityKey>(key: EntityKey): Promise<T | undefined>;
  /** PutItem with ConditionExpression attribute_not_exists(PK) - true if this call created the item. */
  putIfAbsent<T extends EntityKey>(item: T): Promise<boolean>;
  /** Unconditional overwrite of an existing item - used only for non-OCC bookkeeping records (e.g. idempotency status transitions), never for ExpirationItem/AuditEvent. */
  update<T extends EntityKey>(item: T): Promise<void>;
  /**
   * Commits every entry atomically. Throws an error recognized by
   * isTransactionCanceled() if ANY entry's ConditionExpression fails - callers must not
   * assume partial application.
   */
  transactWrite(entries: TransactWriteEntry[]): Promise<void>;
  /** Eventually consistent GSI1 query (data-model.md Â§3: vencimentos/dashboard). */
  queryGsi1<T extends EntityKey = Record<string, unknown> & EntityKey>(input: Gsi1QueryInput): Promise<T[]>;
  /**
   * Query pela partiÃ§Ã£o do item com prefixo opcional de SK â€” adiÃ§Ã£o puramente aditiva
   * (07-domain-model-escalation-watchers-digest.md, D-040) para listar `ItemWatch` sob a
   * mesma partiÃ§Ã£o de `ExpirationItem`, mesmo padrÃ£o de coleÃ§Ã£o que `Document` (M6) jÃ¡ usa.
   * NÃ£o muda nenhum mÃ©todo/comportamento existente â€” zero risco de regressÃ£o ao agregado
   * ExpirationItem jÃ¡ em produÃ§Ã£o.
   */
  queryByPk<T extends EntityKey = Record<string, unknown> & EntityKey>(pk: string, skPrefix?: string): Promise<T[]>;
}



Count    : 67
Average  : 
Sum      : 
Maximum  : 
Minimum  : 
Property : 




2026-08-28T21:21:28.523683Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Nota cega: **4,8/10**

Recomendação: **REPROVAR e pedir nova revisão**.

A Rodada 3 resolve N1 e N2, além de tornar o bootstrap arquiteturalmente plausível. Porém, as correções propostas para `StartExecution`, quota/Textract, `completeOcr` e exclusão S3 quebram idempotência/liveness ou dependem de convenções de key que não existem no código real. A garantia de exclusão física ainda não está fechada.

## Checklist dos 8 achados da Rodada 1

### 1. Primitive administrativo autocontraditório — RESOLVIDO

A separação entre bootstrap, escrita fenced e transição administrativa permanece coerente. Uma transição `ACTIVE → DELETING → DELETED` pode ser feita por `Update` condicional sem duplicar `ConditionCheck` sobre a mesma chave.

Não surgiu regressão neste ponto.

### 2. Bootstrap do primeiro tenant — PARCIAL

Mover o bootstrap para `modules/identity/application` e estender `IdentityStore` é compatível com a arquitetura de ports. Outros stores realmente expõem `transactWrite(entries)`, como [document-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/ports/document-store.ts) e [expiration-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/expiration/ports/expiration-store.ts).

Entretanto, a proposta ainda não fecha a integração real:

- O port atual não possui `transactWrite`: [identity-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/ports/identity-store.ts).
- O adapter de identidade precisará receber e executar `TransactWriteEntry[]`, além de preservar `TransactionCanceledException`.
- O bootstrap precisa construir os três itens e suas condições no módulo de identidade. Isso exige acesso ao `tableName` ou uma abstração equivalente, detalhe omitido.
- O fluxo existente está encapsulado em `IdentityMappingRepository.findOrCreate()`, que hoje executa `putIfAbsent` isolado: [identity-mapping-repository.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/persistence/identity-mapping-repository.ts). Não basta adicionar um arquivo paralelo; o resolver precisa deixar de usar o caminho não atômico.
- Dizer que `bootstrapTenant` “propaga o `InternalError` que `findOrCreate` já lança” é impreciso: o novo bootstrap transacional substituirá justamente o trecho de criação de `findOrCreate`. O tratamento de corrida e releitura precisa ser reespecificado no novo fluxo.

É implementável, mas não está especificado no nível necessário para considerar resolvido.

### 3. Enforcement ESLint — PARCIAL

A forma geral é válida para `.eslintrc.cjs` e ESLint 8.57:

- `overrides`, `files` e `excludedFiles` são compatíveis com a configuração legada real.
- `no-restricted-imports.importNames` identifica o nome importado original e alcança `PutCommand as X`.
- O seletor de `NewExpression` alcança o namespace comum `new ddb.PutCommand()`.

Isso fecha os dois exemplos exatos demonstrados na Rodada 2.

Não fecha, porém, a garantia estrutural mais ampla anunciada:

- `new ddb["PutCommand"]()` escapa do seletor baseado em `callee.property.name`.
- `BatchWriteCommand` não está proibido.
- Imports dinâmicos e reexports intermediários continuam fora da regra.
- Os únicos `excludedFiles` são os dois anchors novos, mas hoje há dezenas de adapters legítimos importando comandos mutáveis. A proposta pressupõe uma migração completa desses adapters para o wrapper sem especificá-la.

Portanto, a sintaxe é plausível e fecha alias/namespace simples, mas o enforcement total ainda é parcial. Os fixtures mencionados são necessários, porém insuficientes para provar a invariância geral.

### 4. Inventário completo dos writers — NÃO RESOLVIDO

A correção proposta para `startExtractionRun` contradiz o modelo real e recria exatamente o problema de liveness que o código atual evita.

Hoje:

- `ExtractionRun` nasce diretamente em `status: "RUNNING"`.
- `startExecution()` é chamado em toda redelivery.
- Isso é deliberado para recuperar a falha entre persistir o run e iniciar Step Functions: [start-extraction-run.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts).

A proposta introduz `CREATED → STARTING`, estados que não existem no fluxo atual, e diz que uma redelivery encontrando `STARTING` não chama `StartExecution`. Sequência problemática:

1. A transição fenced grava `STARTING`.
2. `StartExecution` falha transitoriamente antes de chegar à AWS.
3. A redelivery encontra `STARTING`.
4. A transição falha e `StartExecution` não é repetido.
5. O run fica órfão permanentemente.

Além disso, a proposta não explica como distinguir:

- `STARTING` antes de qualquer chamada bem-sucedida;
- `STARTING` depois de uma chamada cujo resultado foi perdido;
- execução já existente no Step Functions.

A correção precisa preservar a chamada idempotente por `execution name`, fazendo uma aquisição fenced que possa ser repetida sem bloquear recovery.

### 5. Janela claim→efeito e drenagem — PARCIAL

A proposta melhora corretamente a separação entre:

- drenagem de capabilities/efeitos em voo;
- comprovação de eliminação física por varredura.

Mas os 900 segundos continuam insuficientemente especificados:

- O presign de importação dura exatamente 900 segundos, sem margem para skew ou tempo entre emissão e instalação de `DELETING`.
- Não há mecanismo durável descrito para esperar, paginar e retomar a cascata.
- Uma única Lambda não pode assumir que listagem, exclusão de versões e repetição até convergência caberão em uma invocação.
- Filas e redeliveries longas só são aceitáveis se todos os caminhos revalidarem o fence; isso ainda não está garantido pelos fluxos de extração/OCR propostos.

A varredura S3 reduz a dependência do prazo fixo, mas não resolve o orchestration/liveness do processo completo.

### 6. Bedrock e Textract — NÃO RESOLVIDO

A mudança de `consume()` para `Promise<"RESERVED" | "ALREADY_RESERVED">` não é coerente com a semântica real de `TenantQuotaService`.

Hoje o mesmo primitive serve dois usos diferentes:

- quota agregada normal, como `API_REQUEST`, `IMPORT_COUNT` e `UPLOAD_COUNT`;
- reserva idempotente por run, usando `limit: 1` e uma window específica.

O registro não contém um identificador de quem fez a reserva. Quando `effectiveCount >= limit`, o serviço não consegue distinguir genericamente:

- replay do mesmo consumidor;
- outro consumidor que realmente excedeu a quota;
- kill switch;
- contenção sustentada.

Se `ALREADY_RESERVED` substituir `QuotaExceededError` genericamente, handlers HTTP que ignoram o retorno deixam de aplicar quota. Se o retorno só existir para determinados inputs, o contrato precisa expressar essa modalidade explicitamente, e não apenas mudar o retorno global.

Também há falhas de recovery específicas:

- Textract: depois de `StartDocumentTextDetection` suceder e a persistência de `TextractJob` falhar, a redelivery encontra a reserva anterior. Se `"ALREADY_RESERVED"` proíbe chamar Textract, ela nunca recupera o `jobId`. O código atual repete a chamada com `clientRequestToken` idempotente exatamente para permitir essa recuperação: [start-ocr.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts).
- Bedrock: a reserva sozinha não registra se a chamada ocorreu, falhou ou teve resposta perdida. Bloquear todo `"ALREADY_RESERVED"` pode abandonar uma chamada nunca feita; permitir prosseguir mantém o risco original de duplicação.

É necessário um registro explícito de claim/outcome do efeito, ou um protocolo diferente por provider. O retorno binário proposto não carrega informação suficiente.

### 7. URLs presignadas e emissão tardia — PARCIAL

A varredura ativa é a direção correta, mas não fecha o sistema real porque as keys não compartilham atualmente o prefixo proposto:

- Quarantine: usa `tenant/<tenantId>/...`, portanto é enumerável.
- Import raw/plan: usa `tenant/<tenantId>/imports/...`, portanto é enumerável.
- Clean: usa `clean/<tenantId>/<itemId>/<documentId>`, não `tenant/<tenantId>/...`: [advance-after-evidence.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/application/advance-after-evidence.ts).
- OCR: usa `ocr/<runId>/<uuid>.json`, sem `tenantId` enumerável: [s3-ocr-artifact-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/s3-ocr-artifact-store.ts).

Logo, uma varredura exclusiva por `tenant/<tenantId>/` deixaria objetos clean e OCR intactos.

Padronizar apenas novas keys também não apaga objetos já existentes. É necessária uma estratégia explícita para legado:

- listar pelos prefixos antigos;
- derivar as keys a partir dos registros antes de apagá-los;
- ou migrar objetos e provar que não restou dado no formato anterior.

### 8. Upload tardio, convergência e prazo máximo — NÃO RESOLVIDO

O algoritmo apresentado ainda não sustenta a conclusão `DELETED`.

Nos buckets reais:

- Quarantine: versionado.
- Clean: versionado.
- Import: versionado.
- OCR transient: não versionado.

Para buckets versionados, é necessário paginar `ListObjectVersions` com `KeyMarker` e `VersionIdMarker`, apagar versões e delete markers e verificar a resposta de cada `DeleteObjects`. O serviço pode retornar HTTP bem-sucedido com erros individuais em `Errors[]`; esses itens precisam ser retidos e repetidos.

Também faltam:

- cursor durável entre invocações;
- política para throttling e falhas parciais;
- limite de custo/trabalho por execução;
- comprovação de que todos os prefixes antigos e novos foram cobertos;
- tratamento explícito de objeto criado depois da segunda listagem vazia.

“Duas listagens vazias consecutivas” não constitui por si só uma barreira contra uma URL ainda válida. Isso só funciona se, antes da primeira listagem final, estiver provado que todas as URLs emitidas expiraram e que nenhum emissor pode criar novas URLs após `DELETING`.

## Checklist N1–N4 da Rodada 2

### N1. Append do fence e índices de `CancellationReasons` — RESOLVIDO

Os call sites examinados dependem dos índices iniciais, não do último índice:

- `reminder-dispatch` verifica 0 no cancelamento e 1/2/3 no commit principal: [dispatch.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts).
- `reminder-materializer` verifica 0 e, em um fluxo, 1: [reminder-materializer.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts).

Nenhum desses caminhos assume comprimento fixo ou que determinada condição esteja no último índice. Anexar o tenant fence preserva os índices existentes.

A falha do novo item final também não seria confundida com as corridas reconhecidas, desde que o código continue relançando cancelamentos não associados aos índices conhecidos.

### N2. Limite de 99 itens — RESOLVIDO

Rejeitar `items.length >= 99` antes do append produz:

- 98 entradas + fence = 99: permitido;
- 99 entradas + fence = 100: rejeitado.

É uma política conservadora, válida e verificável com teste unitário. O limite atual do DynamoDB comporta 100 ações por transação.

### N3. `StartExecution` em redelivery — NÃO RESOLVIDO

A proposta reconhece corretamente o diagnóstico anterior, mas a transição `CREATED → STARTING` não preserva o recovery existente. Ela impede justamente a repetição de `StartExecution` depois de falha transitória entre a escrita e o efeito.

O fence precisa ser revalidável em toda tentativa sem transformar `STARTING` num estado terminal que bloqueia a recuperação.

### N4. Artefato OCR sem fence na própria invocação — NÃO RESOLVIDO

A correção proposta não corresponde ao estado real:

- O `TextractJob` é criado como `status: "STARTED"`, não `"IN_PROGRESS"`: [start-ocr.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts).
- `completeOcr` não faz transição para `IN_PROGRESS` antes de coletar páginas: [complete-ocr.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/complete-ocr.ts).

Portanto, `IN_PROGRESS → COMPLETING` falharia para todo job real.

Mesmo corrigindo o nome do estado, há uma falha de liveness:

1. Transição para `COMPLETING`.
2. `PutObject` sucede.
3. `SendTaskSuccess` falha transitoriamente.
4. A mensagem é redeliverada.
5. A transição original já não pode ser repetida.
6. O token não é enviado novamente e a execução pode ficar estacionada.

É necessário tornar `COMPLETING` reentrante e persistir a referência do artefato, ou separar claim, criação idempotente do artefato e finalização do token em uma máquina de estados recuperável.

## Achados novos da Rodada 3

### R3-N1 — A transição de `ExtractionRun` cria run órfão após falha de `StartExecution`

A correção de N3 remove o mecanismo de recuperação documentado no próprio código. É um bloqueante de liveness.

### R3-N2 — O novo contrato de quota não distingue replay de quota realmente excedida

`TenantQuotaRecord` só armazena contador por window. Não existe reservation owner/idempotency key separada que permita produzir `"ALREADY_RESERVED"` de maneira genericamente correta.

### R3-N3 — A transição OCR usa estado inexistente e impede retry após efeito parcial

O estado real é `STARTED`. Além disso, `COMPLETING` precisa aceitar retries depois de `PutObject` ou `SendTask*` parcial.

### R3-N4 — A varredura S3 não alcança duas classes reais de objeto

Clean usa `clean/<tenantId>/...`; OCR usa `ocr/<runId>/...`. O prefixo único alegado não existe hoje.

### R3-N5 — A exclusão S3 não especifica paginação, falhas parciais nem checkpoint durável

Sem esses elementos, “repetir até convergir” é uma intenção, não um algoritmo operacional seguro. Em especial, ignorar `DeleteObjects.Errors[]` permite declarar vazio lógico enquanto versões que falharam continuam armazenadas.

## Recomendação

**REPROVAR e pedir Rodada 4.**

Para a próxima rodada atingir condição de aprovação, ela precisa no mínimo:

1. preservar recovery idempotente de `StartExecution`;
2. separar quota agregada de reserva idempotente por efeito;
3. tornar o fluxo `completeOcr` reentrante após qualquer falha parcial;
4. definir prefixes reais e estratégia para objetos legados clean/OCR;
5. especificar paginação, `DeleteObjects.Errors[]`, checkpoints e retomada durável da varredura S3;
6. provar a barreira temporal entre expiração da última URL presignada e a verificação final de zero objetos.
tokens used
99.083
Nota cega: **4,8/10**

Recomendação: **REPROVAR e pedir nova revisão**.

A Rodada 3 resolve N1 e N2, além de tornar o bootstrap arquiteturalmente plausível. Porém, as correções propostas para `StartExecution`, quota/Textract, `completeOcr` e exclusão S3 quebram idempotência/liveness ou dependem de convenções de key que não existem no código real. A garantia de exclusão física ainda não está fechada.

## Checklist dos 8 achados da Rodada 1

### 1. Primitive administrativo autocontraditório — RESOLVIDO

A separação entre bootstrap, escrita fenced e transição administrativa permanece coerente. Uma transição `ACTIVE → DELETING → DELETED` pode ser feita por `Update` condicional sem duplicar `ConditionCheck` sobre a mesma chave.

Não surgiu regressão neste ponto.

### 2. Bootstrap do primeiro tenant — PARCIAL

Mover o bootstrap para `modules/identity/application` e estender `IdentityStore` é compatível com a arquitetura de ports. Outros stores realmente expõem `transactWrite(entries)`, como [document-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/ports/document-store.ts) e [expiration-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/expiration/ports/expiration-store.ts).

Entretanto, a proposta ainda não fecha a integração real:

- O port atual não possui `transactWrite`: [identity-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/ports/identity-store.ts).
- O adapter de identidade precisará receber e executar `TransactWriteEntry[]`, além de preservar `TransactionCanceledException`.
- O bootstrap precisa construir os três itens e suas condições no módulo de identidade. Isso exige acesso ao `tableName` ou uma abstração equivalente, detalhe omitido.
- O fluxo existente está encapsulado em `IdentityMappingRepository.findOrCreate()`, que hoje executa `putIfAbsent` isolado: [identity-mapping-repository.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/persistence/identity-mapping-repository.ts). Não basta adicionar um arquivo paralelo; o resolver precisa deixar de usar o caminho não atômico.
- Dizer que `bootstrapTenant` “propaga o `InternalError` que `findOrCreate` já lança” é impreciso: o novo bootstrap transacional substituirá justamente o trecho de criação de `findOrCreate`. O tratamento de corrida e releitura precisa ser reespecificado no novo fluxo.

É implementável, mas não está especificado no nível necessário para considerar resolvido.

### 3. Enforcement ESLint — PARCIAL

A forma geral é válida para `.eslintrc.cjs` e ESLint 8.57:

- `overrides`, `files` e `excludedFiles` são compatíveis com a configuração legada real.
- `no-restricted-imports.importNames` identifica o nome importado original e alcança `PutCommand as X`.
- O seletor de `NewExpression` alcança o namespace comum `new ddb.PutCommand()`.

Isso fecha os dois exemplos exatos demonstrados na Rodada 2.

Não fecha, porém, a garantia estrutural mais ampla anunciada:

- `new ddb["PutCommand"]()` escapa do seletor baseado em `callee.property.name`.
- `BatchWriteCommand` não está proibido.
- Imports dinâmicos e reexports intermediários continuam fora da regra.
- Os únicos `excludedFiles` são os dois anchors novos, mas hoje há dezenas de adapters legítimos importando comandos mutáveis. A proposta pressupõe uma migração completa desses adapters para o wrapper sem especificá-la.

Portanto, a sintaxe é plausível e fecha alias/namespace simples, mas o enforcement total ainda é parcial. Os fixtures mencionados são necessários, porém insuficientes para provar a invariância geral.

### 4. Inventário completo dos writers — NÃO RESOLVIDO

A correção proposta para `startExtractionRun` contradiz o modelo real e recria exatamente o problema de liveness que o código atual evita.

Hoje:

- `ExtractionRun` nasce diretamente em `status: "RUNNING"`.
- `startExecution()` é chamado em toda redelivery.
- Isso é deliberado para recuperar a falha entre persistir o run e iniciar Step Functions: [start-extraction-run.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-extraction-run.ts).

A proposta introduz `CREATED → STARTING`, estados que não existem no fluxo atual, e diz que uma redelivery encontrando `STARTING` não chama `StartExecution`. Sequência problemática:

1. A transição fenced grava `STARTING`.
2. `StartExecution` falha transitoriamente antes de chegar à AWS.
3. A redelivery encontra `STARTING`.
4. A transição falha e `StartExecution` não é repetido.
5. O run fica órfão permanentemente.

Além disso, a proposta não explica como distinguir:

- `STARTING` antes de qualquer chamada bem-sucedida;
- `STARTING` depois de uma chamada cujo resultado foi perdido;
- execução já existente no Step Functions.

A correção precisa preservar a chamada idempotente por `execution name`, fazendo uma aquisição fenced que possa ser repetida sem bloquear recovery.

### 5. Janela claim→efeito e drenagem — PARCIAL

A proposta melhora corretamente a separação entre:

- drenagem de capabilities/efeitos em voo;
- comprovação de eliminação física por varredura.

Mas os 900 segundos continuam insuficientemente especificados:

- O presign de importação dura exatamente 900 segundos, sem margem para skew ou tempo entre emissão e instalação de `DELETING`.
- Não há mecanismo durável descrito para esperar, paginar e retomar a cascata.
- Uma única Lambda não pode assumir que listagem, exclusão de versões e repetição até convergência caberão em uma invocação.
- Filas e redeliveries longas só são aceitáveis se todos os caminhos revalidarem o fence; isso ainda não está garantido pelos fluxos de extração/OCR propostos.

A varredura S3 reduz a dependência do prazo fixo, mas não resolve o orchestration/liveness do processo completo.

### 6. Bedrock e Textract — NÃO RESOLVIDO

A mudança de `consume()` para `Promise<"RESERVED" | "ALREADY_RESERVED">` não é coerente com a semântica real de `TenantQuotaService`.

Hoje o mesmo primitive serve dois usos diferentes:

- quota agregada normal, como `API_REQUEST`, `IMPORT_COUNT` e `UPLOAD_COUNT`;
- reserva idempotente por run, usando `limit: 1` e uma window específica.

O registro não contém um identificador de quem fez a reserva. Quando `effectiveCount >= limit`, o serviço não consegue distinguir genericamente:

- replay do mesmo consumidor;
- outro consumidor que realmente excedeu a quota;
- kill switch;
- contenção sustentada.

Se `ALREADY_RESERVED` substituir `QuotaExceededError` genericamente, handlers HTTP que ignoram o retorno deixam de aplicar quota. Se o retorno só existir para determinados inputs, o contrato precisa expressar essa modalidade explicitamente, e não apenas mudar o retorno global.

Também há falhas de recovery específicas:

- Textract: depois de `StartDocumentTextDetection` suceder e a persistência de `TextractJob` falhar, a redelivery encontra a reserva anterior. Se `"ALREADY_RESERVED"` proíbe chamar Textract, ela nunca recupera o `jobId`. O código atual repete a chamada com `clientRequestToken` idempotente exatamente para permitir essa recuperação: [start-ocr.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts).
- Bedrock: a reserva sozinha não registra se a chamada ocorreu, falhou ou teve resposta perdida. Bloquear todo `"ALREADY_RESERVED"` pode abandonar uma chamada nunca feita; permitir prosseguir mantém o risco original de duplicação.

É necessário um registro explícito de claim/outcome do efeito, ou um protocolo diferente por provider. O retorno binário proposto não carrega informação suficiente.

### 7. URLs presignadas e emissão tardia — PARCIAL

A varredura ativa é a direção correta, mas não fecha o sistema real porque as keys não compartilham atualmente o prefixo proposto:

- Quarantine: usa `tenant/<tenantId>/...`, portanto é enumerável.
- Import raw/plan: usa `tenant/<tenantId>/imports/...`, portanto é enumerável.
- Clean: usa `clean/<tenantId>/<itemId>/<documentId>`, não `tenant/<tenantId>/...`: [advance-after-evidence.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/application/advance-after-evidence.ts).
- OCR: usa `ocr/<runId>/<uuid>.json`, sem `tenantId` enumerável: [s3-ocr-artifact-store.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/persistence/s3-ocr-artifact-store.ts).

Logo, uma varredura exclusiva por `tenant/<tenantId>/` deixaria objetos clean e OCR intactos.

Padronizar apenas novas keys também não apaga objetos já existentes. É necessária uma estratégia explícita para legado:

- listar pelos prefixos antigos;
- derivar as keys a partir dos registros antes de apagá-los;
- ou migrar objetos e provar que não restou dado no formato anterior.

### 8. Upload tardio, convergência e prazo máximo — NÃO RESOLVIDO

O algoritmo apresentado ainda não sustenta a conclusão `DELETED`.

Nos buckets reais:

- Quarantine: versionado.
- Clean: versionado.
- Import: versionado.
- OCR transient: não versionado.

Para buckets versionados, é necessário paginar `ListObjectVersions` com `KeyMarker` e `VersionIdMarker`, apagar versões e delete markers e verificar a resposta de cada `DeleteObjects`. O serviço pode retornar HTTP bem-sucedido com erros individuais em `Errors[]`; esses itens precisam ser retidos e repetidos.

Também faltam:

- cursor durável entre invocações;
- política para throttling e falhas parciais;
- limite de custo/trabalho por execução;
- comprovação de que todos os prefixes antigos e novos foram cobertos;
- tratamento explícito de objeto criado depois da segunda listagem vazia.

“Duas listagens vazias consecutivas” não constitui por si só uma barreira contra uma URL ainda válida. Isso só funciona se, antes da primeira listagem final, estiver provado que todas as URLs emitidas expiraram e que nenhum emissor pode criar novas URLs após `DELETING`.

## Checklist N1–N4 da Rodada 2

### N1. Append do fence e índices de `CancellationReasons` — RESOLVIDO

Os call sites examinados dependem dos índices iniciais, não do último índice:

- `reminder-dispatch` verifica 0 no cancelamento e 1/2/3 no commit principal: [dispatch.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/workers/reminder-dispatch/dispatch.ts).
- `reminder-materializer` verifica 0 e, em um fluxo, 1: [reminder-materializer.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts).

Nenhum desses caminhos assume comprimento fixo ou que determinada condição esteja no último índice. Anexar o tenant fence preserva os índices existentes.

A falha do novo item final também não seria confundida com as corridas reconhecidas, desde que o código continue relançando cancelamentos não associados aos índices conhecidos.

### N2. Limite de 99 itens — RESOLVIDO

Rejeitar `items.length >= 99` antes do append produz:

- 98 entradas + fence = 99: permitido;
- 99 entradas + fence = 100: rejeitado.

É uma política conservadora, válida e verificável com teste unitário. O limite atual do DynamoDB comporta 100 ações por transação.

### N3. `StartExecution` em redelivery — NÃO RESOLVIDO

A proposta reconhece corretamente o diagnóstico anterior, mas a transição `CREATED → STARTING` não preserva o recovery existente. Ela impede justamente a repetição de `StartExecution` depois de falha transitória entre a escrita e o efeito.

O fence precisa ser revalidável em toda tentativa sem transformar `STARTING` num estado terminal que bloqueia a recuperação.

### N4. Artefato OCR sem fence na própria invocação — NÃO RESOLVIDO

A correção proposta não corresponde ao estado real:

- O `TextractJob` é criado como `status: "STARTED"`, não `"IN_PROGRESS"`: [start-ocr.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/start-ocr.ts).
- `completeOcr` não faz transição para `IN_PROGRESS` antes de coletar páginas: [complete-ocr.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/extraction/application/complete-ocr.ts).

Portanto, `IN_PROGRESS → COMPLETING` falharia para todo job real.

Mesmo corrigindo o nome do estado, há uma falha de liveness:

1. Transição para `COMPLETING`.
2. `PutObject` sucede.
3. `SendTaskSuccess` falha transitoriamente.
4. A mensagem é redeliverada.
5. A transição original já não pode ser repetida.
6. O token não é enviado novamente e a execução pode ficar estacionada.

É necessário tornar `COMPLETING` reentrante e persistir a referência do artefato, ou separar claim, criação idempotente do artefato e finalização do token em uma máquina de estados recuperável.

## Achados novos da Rodada 3

### R3-N1 — A transição de `ExtractionRun` cria run órfão após falha de `StartExecution`

A correção de N3 remove o mecanismo de recuperação documentado no próprio código. É um bloqueante de liveness.

### R3-N2 — O novo contrato de quota não distingue replay de quota realmente excedida

`TenantQuotaRecord` só armazena contador por window. Não existe reservation owner/idempotency key separada que permita produzir `"ALREADY_RESERVED"` de maneira genericamente correta.

### R3-N3 — A transição OCR usa estado inexistente e impede retry após efeito parcial

O estado real é `STARTED`. Além disso, `COMPLETING` precisa aceitar retries depois de `PutObject` ou `SendTask*` parcial.

### R3-N4 — A varredura S3 não alcança duas classes reais de objeto

Clean usa `clean/<tenantId>/...`; OCR usa `ocr/<runId>/...`. O prefixo único alegado não existe hoje.

### R3-N5 — A exclusão S3 não especifica paginação, falhas parciais nem checkpoint durável

Sem esses elementos, “repetir até convergir” é uma intenção, não um algoritmo operacional seguro. Em especial, ignorar `DeleteObjects.Errors[]` permite declarar vazio lógico enquanto versões que falharam continuam armazenadas.

## Recomendação

**REPROVAR e pedir Rodada 4.**

Para a próxima rodada atingir condição de aprovação, ela precisa no mínimo:

1. preservar recovery idempotente de `StartExecution`;
2. separar quota agregada de reserva idempotente por efeito;
3. tornar o fluxo `completeOcr` reentrante após qualquer falha parcial;
4. definir prefixes reais e estratégia para objetos legados clean/OCR;
5. especificar paginação, `DeleteObjects.Errors[]`, checkpoints e retomada durável da varredura S3;
6. provar a barreira temporal entre expiração da última URL presignada e a verificação final de zero objetos.
