Codex, Round H, same W3-07 review, 5th real round. Round G scored CODEX SCORE: 8.8/10 and gave an exact, closed-form list: close 3 items (VERIFYING->DELETED extinction condition, discoverable failed compensation, two-phase ExtractionRun rollout) and you said "APPROVED WITH CONDITIONS (sole condition = SES human decision) would be warranted for the architecture direction" afterward. Closing all 3 now. Fresh blind score - do not anchor to 8.8.

1. VERIFYING -> DELETED EXTINCTION CONDITION - adopting your own suggested closure, combining two of your proposed options rather than picking one: the completion claim is explicitly weakened and made honest (Epistemic Integrity - this project's own governing principle in AGENTS.md is to never claim something is proven when only designed) as follows:
   - Finalization uses a conservative bound covering all admitted external effects and bearer capabilities: max(900s presign TTL, 60s Lambda timeout, 600s ASL TimeoutSeconds) + safety margin = same 1800s number as before, but its role is now explicitly re-scoped to ONLY: "time after which any operation that was atomically admitted while ACTIVE has either completed its external effect+compensation-or-commit, or is presumed abandoned." It is NOT used as a claim about SQS/DLQ/scheduler timing (that class of resurrection is already fully closed indefinitely by the DynamoDB fence itself, no bound needed there, per Round G item 1's accepted reframing).
   - VERIFYING requires: (a) the structural fence has been active since before the finalization cutoff (indefinite, structural - already true), AND (b) two consecutive empty re-scans of DynamoDB + all inventoried S3 prefixes, at or after the finalization cutoff.
   - DELETED's completion claim is stated precisely and weaker than "proven zero forever": "no DynamoDB business state reachable, and no S3 residue found by two consecutive empty scans performed at or after the finalization cutoff." This is a DESIGNED-and-VERIFIED-at-a-point-in-time claim, not a mathematical permanent-zero claim - consistent with this project's own DESIGNED/IMPLEMENTED/TESTED/DEPLOYED/E2E PROVEN/OPERATIONALLY PROVEN distinction (never overclaim).
   - A PERMANENT post-DELETED residue sweeper (reusing the exact existing pattern of DocumentPurgeWorker/W3-06/D-061, already implemented in this codebase for a different entity) continues scanning the tenant's known former prefixes/keys on a recurring schedule (proposing daily, matching the existing scheduled-worker cadence pattern in the codebase) for a bounded retention window matching privacy-lgpd.md's existing 90-day cap on any residual copy - not indefinitely, bounded by the SAME ceiling the project has already normatively adopted elsewhere for exactly this kind of residual-copy concern. Anything the sweeper finds is auto-purged again AND raises an alarm/finding (never silently absorbed) - this is what actually provides the ongoing correctness guarantee your race scenario (compensation fails, admitted writer resumes after the final scan) needs: not a stronger one-time proof, but continued, alarmed, bounded-duration monitoring after the point-in-time claim.

2. DISCOVERABLE FAILED COMPENSATION: closed as a direct consequence of item 1 - the permanent post-DELETED sweeper's scope explicitly includes the clean-bucket and quarantine-bucket prefixes (not just the tables), so a compensating S3 delete that failed (network blip, process died before attempting it) is caught by the same recurring sweep that covers any other late residue, within the same 90-day bounded window, with the same alarm-on-find behavior. No separate mechanism is introduced - explicitly reusing the item 1 answer rather than inventing a second one.

3. TWO-PHASE ExtractionRun MARKER ROLLOUT: adopting your exact proposed sequencing verbatim - Phase A deploys code that writes `admittedWhileActive: true` on every newly created run WITHOUT yet requiring it on retries; backfill all older qualifying runs idempotently; repeat a strongly consistent verification scan until zero eligible markerless rows are found (not a single pre/post count assertion - a repeat-until-zero loop, closing your "scan doesn't snapshot across pages" objection); only then does Phase B activate the lifecycle fence and marker-required retry behavior; the markerless-reconciliation/alert path is retained permanently as defense in depth (never removed after cutover, in case a bug or bypass ever creates a markerless row again). Additionally recording your 3 named implementation obligations verbatim into the design doc as normative requirements, not just accepted commentary: the transaction builder must guarantee the lifecycle ConditionCheck entry's stable identity/index (callers never infer it from a mutable array assembled elsewhere - the fenced executor itself owns array construction, consistent with the Round E/F typed-lane model where callers cannot construct a TenantBusinessMutation without going through the one function that appends the check); the DynamoDB error mapper must preserve typed CancellationReasons through to callers (not collapse every cancellation into a generic OCC-contention error) so compensation logic can distinguish the lifecycle entry's specific ConditionalCheckFailed from an ordinary version conflict; the backfill verifier asserts zero eligible markerless rows via the repeat-scan-until-zero loop, never a raw aggregate row-count comparison.

YOUR TASK:
1. Verify each of 1-3 for real closure: CLOSED / PARTIALLY CLOSED / NOT CLOSED. For item 1 specifically: does combining "point-in-time weaker completion claim" + "permanent bounded sweeper with alarming" actually resolve your race scenario (admitted operation resumes and creates an S3 object after the final scan, compensation also fails) in a way that is honest and doesn't just relabel the same gap? Is a 90-day sweeper window justified by reusing privacy-lgpd.md's existing cap, or does it need independent justification for this specific residual-copy scenario?
2. Any remaining architectural gap after these 3 closures, or has this review-only track reached what it can close?
3. Fresh independent score: "CODEX SCORE: X.X/10". If this reaches 9.0 - or if it doesn't but you believe no further review-only round would move it further (i.e., remaining gaps, if any, are now genuinely implementation/infra/human-decision work, not open design questions) - say so explicitly and state the decision you believe is warranted: APPROVED / APPROVED WITH CONDITIONS (name the condition(s) precisely) / NEEDS ANOTHER ROUND (say exactly what a next round would need to do that this one couldn't). Do not inflate the number to close the process - if it's still below 9.0 and a real design gap remains, say exactly what it is.

Be concise and verdict-first, 70-120 lines.
