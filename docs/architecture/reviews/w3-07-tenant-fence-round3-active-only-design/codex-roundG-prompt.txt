Codex, Round G, same W3-07 review. Round F scored CODEX SCORE: 8.9/10 and named exactly 5 remaining items, explicitly stating 4 of them are "closable through further design work in this session, no live AWS account required" and only item 5 (SES policy) requires a genuine human decision outside this review. I am closing items 1-4 now. Fresh blind score again - do not anchor to 8.9.

CLOSURE OF THE 5 NAMED ITEMS:

1. REPLACE THE QUIESCENCE-AS-SAFETY-BOUND CLAIM (your point: SQS retains messages 14 days, DLQ 4 days, so max(900s,60s,600s)+margin is not a valid system-wide bound). Adopting your own stated preferred interpretation explicitly and normatively: SAFETY (non-resurrection) does NOT depend on any time bound at all - it holds indefinitely because every TenantBusinessMutation is transactionally fenced against ACTIVE, forever, regardless of how late a delayed SQS message, DLQ replay, or scheduled reconciliation runs. A redelivery arriving 13 days later still hits the same ConditionCheck and still fails to persist business state if the tenant is DELETING/DELETED. Therefore quiescence/the 900s+margin number is RENAMED and RESCOPED: it is not a "non-resurrection bound," it is a "purge start-time heuristic" - an operational convenience for when it's PROBABLY safe to begin the purge scan without wasting cycles re-scanning against still-plausible in-flight writes, not a correctness requirement. The purge process itself is idempotent and re-runnable (already true of the design - re-scan-until-empty), so starting it "a bit early" relative to the true tail of SQS/DLQ retention costs nothing but a possible extra purge pass; the state machine's VERIFYING phase's empty-rescan-after-last-delete requirement (already in the design, Round A) is what actually proves convergence, not the timing heuristic. This removes the "invalid universal bound" problem by removing the load-bearing role time was playing: time picks WHEN to first attempt purge (cheap to get wrong in either direction), the fence + re-verification proves WHETHER it's actually done (this is what's load-bearing for correctness).

2. FENCE EVERY INTERMEDIATE EVIDENCE MUTATION: confirmed and accepted - uploadEvidence/malwareEvidence/SCANNING-status writes in advance-after-evidence.ts and advance-after-submission-evidence.ts are each their own TenantBusinessMutation (not just the final CLEAN transition), each requiring the ACTIVE ConditionCheck in their own transaction. No exceptions carved out.

3. ORPHAN CLEAN-BUCKET COPY (S3 copy happens before the fenced DynamoDB commit; if DELETING wins the race, a clean-bucket object exists with no committed Document row pointing at it yet): propose a compensating-delete pattern, reusing a pattern ALREADY PRESENT in the codebase today - start-ocr.ts already compensates a Textract quota reservation with an explicit deps.quota.release() call when the subsequent step fails (see the "Reservation was never consumed... compensate per design 1.8" comment in that file). Same shape applied here: when the fenced CLEAN-transition transaction fails SPECIFICALLY because the ACTIVE ConditionCheck failed (a lifecycle rejection, distinguishable in code from an ordinary OCC version conflict - TransactWriteItems' CancellationReasons array reports which entry failed, and this fence entry can be positioned/identified for exactly this purpose), the calling code synchronously deletes the just-copied clean-bucket object as compensation (S3 DeleteObjectCommand is already idempotent per s3-ocr-artifact-store.ts's existing comment about DeleteObjectCommand success-on-missing-key). This is a same-invocation compensating action, not a background reconciler - if the compensating delete itself fails (network blip), the object becomes purge-phase residue, already covered by the existing clean-bucket purge scan (clean bucket is already enumerable by tenantId per clean/<tenantId>/<itemId>/<documentId>, confirmed in D-065's findings) - so worst case degrades to "found by purge scan," never "permanently invisible."

4. LEGACY ExtractionRun CUTOVER: the "legacy row" concern is actually MOOT for this specific system, for a reason I should have stated in Round F - TenantLifecycleRecord and the entire ACTIVE-fence mechanism DO NOT EXIST IN CODE YET (confirmed by `grep -ril "TenantLifecycle" src/` returning nothing - this whole W3-07 mechanism is pre-implementation, greenfield). Therefore when this design eventually ships, there is a well-defined migration moment: every ExtractionRun row that exists in the table AT THE MOMENT OF DEPLOYMENT was, by definition, created under a regime where the concept of DELETING did not exist at all (the whole tenant universe today is implicitly "ACTIVE" because there is no other state) - so a ONE-TIME backfill migration writing `admittedWhileActive: true` onto every pre-existing RUNNING/STARTED ExtractionRun row at deploy time is both correct and complete (not a soft "audit," a mechanical, verifiable backfill script with a row-count assertion pre/post). From that point forward, the marker is written ONLY by the new fenced creation transaction, and start-extraction-run.ts's retry path is changed to check for the marker's presence before calling startExecution() again - a row missing the marker (which after the backfill should be impossible, but is checked anyway, defense in depth) is refused and routed to a reconciliation/alert path instead of silently retried. This gives runtime a real, machine-checkable discriminator, not a timestamp heuristic.

5. SES: unchanged from Round F, explicitly retained as an open human decision, phrased exactly as you required: "Option 1 (admission-point blocking, already-admitted sends may complete) is engineering's recommended default pending Marcelo/legal's decision; it is not the effective default if nobody answers - implementation of the SES send path does not proceed until this is recorded as an explicit decision in decisions-log.md, OR proceeds behind a flag defaulting to the SAFER Option 2 behavior until answered." This is now stated as a hard implementation gate, not a soft caveat.

YOUR TASK:
1. Verify each of 1-4 for real closure (CLOSED/PARTIALLY CLOSED/NOT CLOSED), specifically:
   - Is reframing "quiescence" from a safety-bound into a "purge start-time heuristic" (with re-verification, not timing, doing the correctness work) actually sound, or does it hide a real problem? Is there ANY scenario where starting purge "too early" relative to in-flight work could cause an actual safety violation (not just an extra harmless purge pass)?
   - Does the compensating-delete-on-lifecycle-rejection pattern for orphan clean copies actually work given TransactWriteItems' CancellationReasons mechanics (confirm this is real AWS behavior - can a caller actually distinguish "this specific ConditionCheck entry failed" from other transaction failure reasons)?
   - Is the ExtractionRun backfill-marker migration argument (moot legacy problem because the whole mechanism is pre-implementation/greenfield) actually valid, or is there still a gap (e.g., a race during the deploy window itself, rows created between backfill-script-read and the code deploy that adds the fence)?
2. Is item 5's phrasing now an acceptable, honest gate (implementation blocked pending human decision) rather than a dodge?
3. Any NEW gap introduced by these 4 closures (e.g., the CancellationReasons-based branching logic itself, the backfill script's own correctness, the reframed quiescence definition's interaction with the state-machine states from Round A)?
4. Fresh independent score: "CODEX SCORE: X.X/10". This is Round G after 4 prior real rounds (7.8->8.4->8.8->8.9). If this closes the gate, say so plainly and explicitly confirm APPROVED / APPROVED WITH CONDITIONS (naming the condition = SES human decision) is now warranted for the architecture DIRECTION (not full implementation, which doesn't exist). If not, say exactly what's left and whether it's still closable by discussion or genuinely requires something else.

Be concise and verdict-first, 80-140 lines.
