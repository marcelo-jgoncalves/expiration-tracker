Codex, this is Round E, continuing the same W3-07 review (Expiration Tracker, C:\Users\Usuario\Desktop\projects\expiration-tracker). You gave CODEX SCORE: 8.4/10 in Round D with 5 prioritized blockers. I am addressing blockers #1 and #4 concretely this round (the two that are genuine architecture-direction questions), and explicitly NOT trying to fully close #2/#3/#5 in this same session - argue with me if you think that's wrong, but evaluate on the merits of what IS being closed.

BLOCKER #1 FIX - eliminating the "check ACTIVE, pause, call provider" TOCTOU via a real code mechanism, not just words:

Verified in code: TenantQuotaService.consume() (src/modules/identity/application/quota.ts) is the actual admission gate used before every external paid effect (Textract in start-ocr.ts, Bedrock in run-bedrock-extraction.ts) - both call quota.consume() with a per-run reservation window BEFORE calling the provider. Today consume() does a single-item conditional write via IdentityStore.updateConditional/putIfAbsent (read-check-conditionalWrite retry loop, NOT a multi-item TransactWriteItems, NOT going through occ.ts's builders - it uses a separate IdentityStore port).

Proposed fix: convert consume()'s write step from a single-item conditional update to a 2-item TransactWriteItems: item 1 = the existing quota Update (same logic, same ConditionExpression on count/resetAt), item 2 = a ConditionCheck asserting TenantLifecycleRecord.status = ACTIVE (using occ.ts's existing buildVersionConditionCheck/buildExistenceConditionCheck helpers, no new DynamoDB primitive needed - the retry loop structure is unchanged, only the final write call changes from updateConditional to a transactWrite call, which requires extending the IdentityStore port with a transactWrite method, the same port-extension direction D-065 already identified as needed for bootstrap). This makes the quota reservation itself the durable, fenced admission capability for that specific Textract/Bedrock call: the ACTIVE check and the reservation commit ATOMICALLY in the same transaction, so there is no window between "read ACTIVE" and "call provider" - if the transaction commits, ACTIVE was true at that exact commit, and the code is then authorized to make the provider call as the direct synchronous continuation of that commit (no further pause is introduced between the transaction and the call - it's the next line of code in the same function).

Same pattern applies to S3 presigned uploads: GuestSubmissionService.startSubmission() and DocumentService's upload flow already create a DynamoDB row (DocumentSubmission / UploadSlot) via transactWrite BEFORE presigning - proposed fix: add the same ACTIVE ConditionCheck as the last entry in that existing transactWrite call (this one is EASIER than quota.consume() because it already IS a TransactWriteItems, not a single-item update - no port extension needed here, just one more entry). The presign call happens only after that transaction succeeds - same "next line of code, no pause" reasoning.

For Step Functions: start-extraction-run.ts's putIfAbsent(run) is currently a single-item conditional create (ExtractionRunStore port) - needs the same treatment as quota.consume(): convert to a 2-item TransactWriteItems (ExtractionRun create + ACTIVE ConditionCheck) via a port extension, then call startExecution() as the direct continuation.

For SES/notification sends: verified this was NOT covered - the notification module's two-phase design (persist NotificationAttempt/Intent row, then a separate worker sends via SES) means the ACTUAL send can happen well after the fenced admission (this is NOT "next line of code", there's a real worker/queue hop in between). Proposed explicit policy (not previously stated): the SES-sending worker itself must re-check TenantLifecycleRecord.status (a plain read, not requiring a transaction, since it's not persisting new business state on the happy path - just calling an external API) immediately before calling SES, and skip/no-op if DELETING. This does NOT close the same TOCTOU class (a read-then-call gap remains, bounded by the worker's own execution time, which is short and already timeout-bounded) - propose accepting this as a documented, bounded residual risk consistent with privacy-lgpd.md's "immediate blocking" being a best-effort target bounded by worker latency, not an instantaneous guarantee - explicitly flagged as different in kind from the DynamoDB-transactional cases above, not silently equated with them.

For completeOcr (the TOCTOU you flagged specifically): agree with your Round D verdict - S3 PutObject cannot participate in a DynamoDB transaction, so this one is NOT closable via the transaction trick. Adopting your proposed resolution verbatim: never gate on a lifecycle check immediately before the PutObject; instead accept that a write started while ACTIVE may complete after DELETING, let it happen, and rely on the purge+reverification phase (not a pre-write check) to guarantee eventual non-resurrection at the DynamoDB level (completeOcr's SendTaskSuccess payload does NOT get persisted into ExtractedField/business state unless a LATER fenced transaction accepts it - so the actual non-resurrection guarantee still holds at the point where the OCR text would turn into business state, several steps later in the pipeline, which DOES go through a fenced write).

BLOCKER #4 FIX - typed lanes instead of a textual carve-out:

Propose two explicit port-level categories instead of one undifferentiated mutation path:
- `TenantBusinessMutation`: any TransactWriteItems that creates/mutates tenant-owned state visible to the tenant or usable to reconstruct their business data (ExpirationItem, Document, ReminderPolicy, DocumentSubmission, TextractJob/ExtractionRun, UserProfile, quota reservations, BFF session rows tied to a login). MUST include the ACTIVE ConditionCheck as the last entry - enforced by a wrapper type that only the fenced executor can construct (the executor takes a tenantId + entries and appends the check itself, so callers cannot forget it or omit it - not a lint rule, a type that cannot be constructed without going through the one function that adds the check).
- `SystemMutation`: a narrow, explicitly enumerated allowlist (not organically growing) covering ONLY: the lifecycle transition itself (ACTIVE->DELETING->...->DELETED, writing TenantLifecycleRecord), the purge Step Functions' own deletes, and the outbox relay's bookkeeping fields (status/leaseOwner/GSI6PK on an OutboxRecord that was ALREADY created by a fenced TenantBusinessMutation earlier - the relay only transitions metadata of an already-admitted record, never creates new business-visible content). Each SystemMutation call site is enumerated by name in the design doc (not "the outbox is exempt" as a blanket statement) and reviewed individually - today that's exactly 3 call sites (tryAcquireLease, markPublished, and the future purge worker's deletes), not an open set.

New consumers (e.g. a future 4th outbox consumer) default to being a `TenantBusinessMutation` unless explicitly reviewed and added to the enumerated `SystemMutation` allowlist - closing your objection that "a new consumer without a gate reopens the vulnerability" (the type system route makes the SAFE path the default, not an opt-in).

YOUR TASK:
1. Does the quota.consume()/ExtractionRun/presign transactification actually close the TOCTOU for Textract/Bedrock/S3-presign/Step-Functions admission, or is there still a gap? Be specific.
2. Is the SES residual-risk framing ("bounded by worker latency, not instantaneous, explicitly different in kind") an honest and acceptable resolution, or does it not actually satisfy privacy-lgpd.md's "immediate blocking" requirement closely enough to pass? What would make it acceptable if not?
3. Is the completeOcr resolution (accept write-after-DELETING at the S3 layer, rely on purge+reverification, non-resurrection holds only because business-state persistence is a separate later fenced write) actually sound, or does it hide a real problem?
4. Does the typed-lane (TenantBusinessMutation vs enumerated SystemMutation) proposal actually close blocker #4, or is it still hand-wavy?
5. Given blockers #2/#3/#5 (full writer/presigner/timeout inventory, full S3 multipart/legacy-key/re-scan detail, full concurrent test plan) are being explicitly deferred as implementation-detailing work for a dedicated follow-up session rather than closed now - is that a legitimate scope boundary for an architecture-direction decision, or does the gate genuinely require them closed first? Your honest opinion, not what you think I want to hear.
6. Fresh score: "CODEX SCORE: X.X/10". If still below 9.0, say plainly whether you think 9.0 is reachable through further design-only reconciliation in this same line of work, or whether it structurally requires an implementation-adjacent session (real infra inventory, real code) to close - i.e., tell me if we've hit the ceiling of what a review-only round can accomplish.

Be concise, 80-150 lines, verdict-first.
