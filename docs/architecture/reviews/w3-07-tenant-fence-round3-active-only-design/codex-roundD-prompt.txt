You are Codex, continuing as independent adversarial reviewer on the Expiration Tracker repo (C:\Users\Usuario\Desktop\projects\expiration-tracker, branch develop). This is Round D of the Claude<->Codex protocol in AGENTS.md section 4 (fresh verification round, blind on score again - do not anchor to any score mentioned below, form your own).

BACKGROUND: In Round A/B you reviewed the "ACTIVE-only fence" direction for W3-07 (tenant deletion fence for LGPD/DSR) versus the abandoned "claim+outcome" protocol, and gave CODEX SCORE: 7.8/10, listing 10 blocking gaps in docs/architecture/reviews/w3-07-tenant-fence-round3-active-only-design/codex-roundA-critique-full.txt (read it now for full context - do not re-derive it from scratch). I have now written a Round C reconciliation attempting to close all 10 gaps with concrete design decisions verified against real code. Your job: verify whether each gap is actually closed, find anything still wrong or newly introduced, and give a fresh independent score.

ROUND C RECONCILIATION (my proposed resolution of your 10 gaps - evaluate each critically, do not rubber-stamp):

1. FORMAL PROPERTY: "No TransactWriteItems that creates/mutates tenant-owned business state in the main table commits unless it includes, as its LAST entry, a ConditionCheck on TenantLifecycleRecord's key asserting status=ACTIVE. 'Admitted' = the moment this transaction is submitted to DynamoDB, not the moment any external provider is called - so a Lambda that read ACTIVE, paused, then called an external effect after DELETING is still safe AS LONG AS the actual persistence of that effect's result also goes through a fenced transaction (the persistence is what's gated, not the provider call itself)." Physical-purge claim is scoped explicitly to: main table + bff-session-table + the inventoried S3 buckets (quarantine/import/clean/document/EXTRACTION_TRANSIENT-ocr), with documented exceptions: TenantLifecycleRecord tombstone, SECURITY_AUDIT/AuditEvent, DynamoDB PITR backups (already governed by privacy-lgpd.md's 35-day/90-day-cap rules), SQS/DLQ residual, Step Functions execution history, CloudWatch Logs/traces, Textract/Bedrock/SES provider-side retention.

2. WRITER/STORAGE INVENTORY (verified against real code this session): confirmed GuestSubmissionService (src/modules/subject/application/guest-submission-service.ts) NEVER goes through RequestContext/resolve-request-context.ts - it calls store.transactWrite() directly with its own TransactWriteEntry array (buildVersionedCreate for DocumentSubmission + buildVersionedUpdate for DocumentRequest). Proposed fix: append a ConditionCheck against TenantLifecycleRecord as the LAST entry in that same entries array (using the existing buildVersionConditionCheck/buildExistenceConditionCheck helpers already in src/shared/dynamodb/occ.ts - zero new primitives needed, they already accept arbitrary extra key/condition). Also confirmed: DynamoDbOutboxRelayStore (src/shared/outbox/persistence/dynamodb-outbox-relay-store.ts) uses raw UpdateCommand/QueryCommand, NOT the occ.ts builders at all, for tryAcquireLease/markPublished/listPendingReminderDispatch. Proposed classification: outbox relay only republishes ALREADY-COMMITTED events (it does not create new tenant business state, only transitions an existing OutboxRecord's own bookkeeping fields status/leaseOwner/GSI6PK) - proposed as an explicitly authorized "system housekeeping" lane, not required to check ACTIVE, but MUST stop being able to enqueue a NEW externally-visible side effect (e.g. must not re-publish a reminder dispatch to a DELETING tenant) - requires the CONSUMER of the outbox event (not the relay itself) to check lifecycle before acting. bff-session-table: proposed to be included via a MULTI-TABLE TransactWriteItems (same AWS account/region, confirmed technically supported) whenever a session mutation must be fenced. Full async producer inventory reused from Round A's P8 answer (SQS/Streams/S3-events/scheduler list) - not re-enumerated here for brevity, treated as already closed.

3. STRUCTURAL ENFORCEMENT BEYOND ESLINT: reuse occ.ts's ALREADY-EXISTING `extraConditions` parameter on buildVersionedUpdate/buildVersionedDelete (verified in code: it does collision-checked placeholder merging today, built for W3-06's legalHold check) - add a new small helper `buildTenantActiveConditionCheck(tenantId)` that any TransactWriteItems caller appends. Enforcement layers proposed: (a) a mandatory service-layer wrapper that ALL application services must call instead of raw store methods for tenant-scoped mutations (single choke point, not scattered), (b) an architecture-level test (same category as infra/tests/stack.tftest.hcl for Terraform) asserting every persistence adapter's transactWrite/update call site includes the fence entry, (c) ESLint as SECONDARY guard only, specifically banning BatchWriteCommand/dynamic imports/computed command properties for any file under a tenant-scoped persistence path.

4. PRIVILEGED BYPASS MODEL: ACTIVE->DELETING transition and the purge Step Functions are separate IAM roles from all tenant-facing Lambda roles (least privilege, no shared role). tenantId for a deletion request is taken ONLY from (a) the authenticated principal's own tenant for self-service deletion, or (b) a verified DataSubjectRequest row (already designed in privacy-lgpd.md section 3's RECEIVED->VERIFIED->DISCOVERED->HELD/PURGING->COMPLETED state machine) - never from an unauthenticated/unverified payload field. Step-up confirmation required before the irreversible PURGING phase (reusing the existing DataSubjectRequest verification flow, not inventing a new one). Immutable audit event at every lifecycle transition (reusing existing AuditEvent/SecureLogger pattern).

5. PER-EFFECT PROTOCOL: Step Functions - no new StartExecution once DELETING (checked in the SAME fenced transaction that creates ExtractionRun in start-extraction-run.ts); in-flight executions rely on existing ASL TimeoutSeconds/HeartbeatSeconds (confirmed real values: 120s/600s on the waitForTaskToken task) - completeOcr checks TenantLifecycleRecord before persisting the artifact/ExtractedField state, and if DELETING, calls SendTaskFailure with a distinguishable reason code and skips the PutObject entirely (never writes the artifact for a DELETING tenant). Textract - no new StartDocumentTextDetection once DELETING (gate before quota.consume in start-ocr.ts); Bedrock - no new call once DELETING (gate before quota.consume in run-bedrock-extraction.ts) - Bedrock's pre-existing lack of idempotency key is explicitly out of scope, not worsened. S3 - deterministic keys (see item below) + block issuance of new presigned URLs once DELETING + wait out the EXISTING observable TTL of already-issued URLs (confirmed real value: PRESIGN_TTL_SECONDS = 600 in guest-submission-service.ts; other presign call sites, e.g. ImportService.reserveImport per D-064's finding, need the same treatment - not yet individually verified this round). SQS/outbox consumers check lifecycle before persisting any result; discard is a terminal ack with an audited reason, never silent drop, never infinite redelivery.

6. BOUNDED QUIESCENCE: adopts Round A's "incapacity to write" definition. Deadline = max(all real Lambda function timeouts actually configured in Terraform, all real Step Functions per-task/per-execution TimeoutSeconds in the ASL, all real presigned-URL TTL constants in code) + a fixed safety margin, after which purge begins. This requires an actual inventory pass over infra/ and every presign call site to compute the real max - NOT yet done this round, proposed as the deliverable of the next detailing pass, not hand-waved as infinite/unbounded.

7. S3 PURGE PLAN: buckets/prefixes confirmed this round and in D-065's findings: quarantine (tenant/<tenantId>/...), import (tenant/<tenantId>/imports/...), clean (clean/<tenantId>/<itemId>/<documentId> - NOT prefixed with "tenant/"), EXTRACTION_TRANSIENT/OCR (currently ocr/<runId>/<uuid>.json, confirmed NOT versioned per D-065's finding "OCR transient não é versionado, confirmado" - only quarantine/clean/import are versioned buckets). Purge design: ListObjectVersions with KeyMarker/VersionIdMarker pagination for the 3 versioned buckets; DeleteObjects batched (<=1000 keys/versions per call); inspect and retry Errors[]; delete markers handled explicitly; re-list after the last delete batch to confirm zero versions (an explicit empty confirmation pass, not assumed). OCR bucket, being unversioned, only needs a single DeleteObjects pass per key (no version history to chase) - but its keys must first become tenant-enumerable (see item 11 below) or be resolved via ExtractionRun/runId->tenantId lookup before the DynamoDB rows are purged.

8. ANTI-STUCK STATE MACHINE: ACTIVE -> DELETING -> QUIESCING -> PURGING -> VERIFYING -> DELETED, plus BLOCKED/HELD entered on: DeleteObjects persistent errors after N retries, legal hold flagged mid-purge, or a quiescence deadline computation failing (missing/unknown timeout config). BLOCKED never auto-transitions to DELETED - requires manual operator action with its own audit event. A CloudWatch alarm fires if any tenant remains in DELETING/QUIESCING/PURGING longer than the computed bound from item 6 plus a purge SLA margin.

9. STRONG AUTH ON TRANSITION: reuses the existing DataSubjectRequest verification flow already designed (not new) in privacy-lgpd.md, OWNER-role-only authorization (existing identity matrix), rate-limited initiation endpoint, immutable audit event, and a distinct IAM role for the purge Step Functions execution (already covered in item 4).

10. ATOMIC BOOTSTRAP: confirmed real bug location this round - src/modules/identity/application/resolve-request-context.ts lines ~49-64: `this.identityMappings.findOrCreate(...)` followed unconditionally by `this.users.createProfileIfAbsent(...)` on first login, with ZERO lifecycle check today (TenantLifecycleRecord does not exist in code yet - this whole area is pre-implementation). Proposed fix: fold IdentityMapping-create + TenantLifecycleRecord-create-if-absent-as-ACTIVE (or assert-not-DELETING/DELETED-if-already-present) + User-profile-create into ONE TransactWriteItems, replacing the current two sequential non-transactional calls.

ADDITIONAL FIX (independent bug, also being proposed for the same design doc, not yet implemented per the task's no-code rule): S3OcrArtifactStore.put() (src/modules/extraction/persistence/s3-ocr-artifact-store.ts:17) currently does `ocr/${runId}/${randomUUID()}.json`. Proposed fix: deterministic key `ocr/<tenantId>/<runId>.json` (single key per run, tenantId-prefixed for enumerability). Since this bucket is confirmed NOT versioned, a redelivery's PutObject to the SAME key simply overwrites the prior object - eliminating both the orphan-artifact bug (PutObject success + SendTaskSuccess timeout no longer creates a second physical object) and the enumerability gap (no tenantId in the old key) in one change. Requires tenantId to be threaded into S3OcrArtifactStore.put()'s signature (currently only takes runId) - a real interface change, not just a constant.

YOUR TASK:

1. For EACH of the 10 original gaps, verify: is it now CLOSED (concrete, verifiable, no more hand-waving), PARTIALLY CLOSED (still has a real hole - name it precisely), or NOT CLOSED (reconciliation didn't actually address it)? Be skeptical - this is exactly the point where a review is tempted to accept "we thought about it" as "it's solved."
2. Check the proposed OCR key fix (ocr/<tenantId>/<runId>.json, unversioned bucket, overwrite-on-redelivery) for correctness - does it actually eliminate the orphan-artifact scenario? Any new problem it introduces (e.g. a genuine legitimate re-run of the same runId that SHOULD produce a different artifact - does that ever happen in this pipeline? check extraction-run.ts's deriveExtractionRunId if useful)?
3. Identify any NEW gap introduced by the reconciliation itself (e.g., the multi-table TransactWriteItems for bff-session-table, the outbox-relay "system housekeeping lane" carve-out, the "provider call vs persistence" distinction in item 1).
4. Give a fresh independent score out of 10 for this reconciled design DIRECTION (still not full implementation-ready detail on every item - some, like item 6's real timeout inventory, are explicitly deferred as next-round work). State it as "CODEX SCORE: X.X/10". Do not anchor to 7.8 - form your own judgment; it is legitimate for it to still be below 9.0, at 9.0+, or even lower if the reconciliation introduced new problems.
5. If not yet at 9.0: give a prioritized, SHORT list (aim for <=5 items) of what remains blocking versus what is now acceptable as "detailed in a follow-up implementation session, not a design gap."

Be concise - aim for 100-180 lines, prioritize the verdict-per-gap and the score over restating the reconciliation back to me.
