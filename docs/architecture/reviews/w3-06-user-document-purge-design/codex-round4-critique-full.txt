2026-08-28T17:49:46.199962Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a0497d-f6ad-7e42-9bed-df36ae703dc6
--------
user
Rodada 4 do protocolo Claude<->Codex, mesmo design W3-06 (purga fisica de USER_DOCUMENT/Document).
Rodada 3: nota 7,8/10, 5 achados bloqueantes (status sobrescrito por DELETED impede a tabela por
status; hold pos-claim ainda nao fechado estruturalmente; extensao do builder occ.ts inexequivel
como especificada; estado terminal apos falhas repetidas ausente; discriminador de
DocumentPurgeReceipt invalido).

Claude respondeu na Rodada 4, arquivada em
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md. Leia esse
arquivo e confirme contra o codigo real: src/modules/document/application/document-deletion-service.ts
(confirme que cleanObject/uploadEvidence/malwareEvidence realmente sobrevivem ao soft-delete sem
serem limpos), src/shared/dynamodb/occ.ts (a extensao proposta de buildVersionedUpdate e
estruturalmente correta e implementavel como especificada?), docs/architecture/data-model.md §1
(confirme que entityType e realmente atributo comum a toda entidade), e o argumento de que o OCC
obrigatorio do projeto (AGENTS.md §7) ja fecha estruturalmente a corrida de legalHold pos-claim
sem mecanismo novo - esse argumento e valido ou tem uma falha logica?

De uma nota de 0 a 10 (sem arredondar), gate de aprovacao 9.0. Se aprovar, diga explicitamente
"APROVADO" no veredito. Formato: ## Achados bloqueantes / ## Achados nao-bloqueantes / ## Nota:
X/10 / ## Veredito.

codex
Vou revisar a proposta da Rodada 4 contra os três artefatos indicados e rastrear os call sites do OCC suficientes para testar o argumento da corrida de legal hold. Como é uma revisão explícita do protocolo, também vou carregar o índice de engenharia exigido pelo repositório.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md; Get-Content -Raw src/modules/document/application/document-deletion-service.ts; Get-Content -Raw src/shared/dynamodb/occ.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 312ms:
# Expiration Tracker â€” Status e PrÃ³xima SessÃ£o

> Este arquivo Ã© estado atual + prÃ³xima aÃ§Ã£o (`AGENTS.md` Â§2), nÃ£o histÃ³rico. Para a linha do tempo completa por sessÃ£o, ver `docs/architecture/session-log.md`; para toda decisÃ£o com nota Claude/Codex, ver `docs/architecture/decisions-log.md`. Reescrito em 2026-08-23 para remover narrativa jÃ¡ duplicada nesses dois arquivos (checklist `AGENTS.md` Â§6). Atualizado em 2026-08-24 com o Core Expiration Vertical Slice (ver abaixo) â€” confirmar `git log`/branch atual antes de assumir que o PR jÃ¡ foi mergeado, este arquivo pode ficar temporariamente atrÃ¡s do estado real de `develop`.

## Estado atual

**Consolidation + Pilot Readiness Program â€” Waves 0/2/3/5 concluÃ­das, Wave 4 parcial (gated), Wave 1 nÃ£o iniciada (2026-08-28)**: Marcelo trouxe `expiration-tracker-next-days-master-plan-and-ai-prompt.md` (raiz, commit `4b547ab`) como handoff para um programa multi-sessÃ£o de consolidaÃ§Ã£o/pilot-readiness. **Backlog canÃ´nico item-a-item: `docs/engineering/pilot-readiness-program.md`. SÃ­ntese final/recomendaÃ§Ã£o GO-CONDITIONAL-NOGO: `docs/engineering/pilot-readiness-assessment.md`** (entregÃ¡vel do prompt mestre Â§42, com addendum de 2026-08-28 refletindo o fechamento da Wave 2/5) â€” nÃ£o duplicar detalhe aqui, sÃ³ apontar para os dois.

Resumo ultra-compacto (ver os dois documentos acima para tudo): Wave 0 (reconciliaÃ§Ã£o) `DONE`. **Wave 2 (evidÃªncia operacional) `DONE`** (2026-08-28) â€” os 6 drills (W2-03 a W2-08) executados com evidÃªncia real contra `dev`: kill switch M7, pipeline de lembretes fim a fim, DLQ/replay, restore PITR real (RTO â‰ˆ3min44s, RPO nÃ£o medido â€” item aberto), load test real (977 invocaÃ§Ãµes, cota `API_REQUEST` segurando sob carga), alarme forÃ§ado com notificaÃ§Ã£o SNSâ†’e-mail real confirmada â€” sÃ³ a metade credential-compromise do W2-08 nÃ£o foi exercitada. Claims especÃ­ficas por drill (algumas mais estreitas que os tÃ­tulos originais do backlog): ver `pilot-readiness-program.md` seÃ§Ã£o Wave 2 e `docs/engineering/test-engineering-standard.md` Â§5. Wave 3 (tenant isolation + LGPD) `DONE` de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada, mas achado sÃ©rio nÃ£o corrigido: `W3-06-DECISION` (produto decidiu priorizar, mas o **desenho do mecanismo** de purga real ainda nÃ£o passou pelo protocolo Claudeâ†”Codex â€” Type 1, `AGENTS.md` Â§4, **prÃ³xima aÃ§Ã£o real desta sessÃ£o**). Wave 4 (Identity/RBAC) `PARTIAL` â€” confirmado que `tenantId=userId` hoje, RBAC real gated por decisÃ£o de negÃ³cio jÃ¡ registrada (`AGENTS.md` Â§1). **Wave 5 (GTR-01) `DONE`** (2026-08-28) â€” `UserProfile.requesterDisplayName` + `GET/PUT /profile` + guest-facing surfaces (`GuestSubmissionService.getRequestInfo`, 2 templates de e-mail) implementados de ponta a ponta, commit `7dacbac`, D-060 em `decisions-log.md`. Wave 1 (Design System) deliberadamente nÃ£o iniciada, aguardando o Marcelo atualizar o Design System formal. **W2-01-DECISION**: implementado nesta sessÃ£o (2026-08-28) â€” auto-confirm agora escreve `dueDate` automaticamente, commit `e9f2439`, D-058.

**Test Engineering Standard â€” novo, APPROVED (2026-08-28)**: `docs/engineering/test-engineering-standard.md`, padrÃ£o normativo world-class para validade/qualidade de teste automatizado E drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados com fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa precisa da Wave 2. Protocolo Claudeâ†”Codex de **8 rodadas** contra gate elevado a 9,5/10 (pedido explÃ­cito do Marcelo, acima do 9,0 padrÃ£o) â€” trajetÃ³ria Codex 6.35â†’7.85â†’8.70â†’9.18â†’9.34â†’9.46â†’9.48â†’**9.62** (aprovado), Claude final 9.9/10. E-010 em `docs/engineering/decisions-log.md` (distinto de `docs/architecture/decisions-log.md`, que usa numeraÃ§Ã£o D-0xx). EvidÃªncia completa em `docs/engineering/reviews/test-engineering-standard/`. **Este padrÃ£o vale para qualquer teste/drill novo a partir de agora** â€” antes de declarar um teste ou drill "concluÃ­do", checar contra os gates de Â§3 (nunca reversÃ£o silenciosa, nunca claim mais ampla que a evidÃªncia).

**RecomendaÃ§Ã£o da sÃ­ntese final** (addendum 2026-08-28 em `pilot-readiness-assessment.md`): CONDITIONAL GO para um piloto controlado e estreito â€” dos trÃªs gates nomeados na sÃ­ntese original, guest trust (`GTR-01`/`W5-01`) e evidÃªncia operacional (Wave 2) fecharam nesta sessÃ£o; sÃ³ `W3-06` (purga real) segue como gate real aberto.

928 testes de backend, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, `terraform test` real dos mÃ³dulos `api-gateway`+raiz verde. **PrÃ³xima aÃ§Ã£o**: `W3-06` â€” o *desenho do mecanismo* de purga real de `USER_DOCUMENT` ainda precisa do protocolo Claudeâ†”Codex completo (`AGENTS.md` Â§4) antes de implementar, mesmo com a decisÃ£o de produto ("fazer agora") jÃ¡ tomada; W3-07 (cascata DSR) usa o mesmo mecanismo, escopar junto. Ã‰ o Ãºnico gate real de pilot readiness ainda sem nenhum progresso desde a sÃ­ntese original. Item secundÃ¡rio: a metade credential-compromise do W2-08 e o RPO real do W2-06 (drills futuros, nÃ£o bloqueantes).

**Visual Language + Design System Foundation** (2026-08-26, PR #61, merge `1a0d5f1` em `develop`, branch jÃ¡ deletada): o frontend deixou de ser propositalmente grayscale/provisÃ³rio e passou a ter a primeira linguagem visual real do produto â€” arquitetura de tokens em duas camadas (`frontend/src/styles/tokens.css` + `base.css`, substituindo as 71 linhas de `foundation.css`), ~9 primitives acessÃ­veis em `frontend/src/components/ui/` (Button/ButtonLink, StatusBadge, UrgencyIndicator, DataTable, InlineNotice, PageHeader/Section/Toolbar/Panel), e a direÃ§Ã£o **Operational Calm** aplicada Ã s 5 superfÃ­cies do Core Expiration slice (Overview, Collection, Detail, Create, Renew). `APPROVED AS VISUAL LANGUAGE + DESIGN SYSTEM FOUNDATION â€” PROVISIONAL PENDING USER VALIDATION` â€” a palavra PROVISIONAL Ã© literal: nenhuma sessÃ£o com usuÃ¡rio aconteceu, e as **15** hipÃ³teses de design (hierarquia de navegaÃ§Ã£o, densidade final, rÃ³tulos, colocaÃ§Ã£o de aÃ§Ãµes secundÃ¡rias, accent, e as duas afirmaÃ§Ãµes subjetivas sobre a prÃ³pria direÃ§Ã£o) estÃ£o registradas como adiamentos explÃ­citos no Â§35 do documento, nÃ£o como fatos. **Zero dependÃªncias novas** (nenhum framework de UI, nenhum Storybook, nenhuma biblioteca de Ã­cones); JS de bundle inalterado, CSS 23,04 kB (4,46 kB gzip). MudanÃ§a estrutural Ãºnica: a Expiration Collection deixou de ser `<ul>/<li>` e virou `<table>` semÃ¢ntica com urgÃªncia e situaÃ§Ã£o em colunas separadas â€” mesmos dados/ordenaÃ§Ã£o/agrupamento/filtro/rotas. A densidade real (140 itens, nomes longos e quase-idÃªnticos, 3 grupos de urgÃªncia) foi verificada contra o cÃ³digo real do frontend **pela primeira vez** (antes sÃ³ contra o protÃ³tipo), com asserÃ§Ã£o automatizada. Protocolo Claudeâ†”Codex completo: Rodada B adversarial achou 5 achados reais, 2 S2 â€” `Button` usava `disabled ?? pending` e `RenewItem` passa `disabled={conflict}`=`false` numa renovaÃ§Ã£o normal, entÃ£o o botÃ£o de submit continuava clicÃ¡vel durante a mutaÃ§Ã£o exibindo "Renovandoâ€¦"; e os cabeÃ§alhos de grupo da tabela usavam `scope="colgroup"` onde encabeÃ§am linhas â€” ambos corrigidos com teste de regressÃ£o. O protocolo **nÃ£o** parou na Rodada D: foram **16 rodadas e 11 reaberturas** atÃ© convergir em Codex **9,04** e Claude **9,2** (sem arredondamento, `AGENTS.md` Â§4) â€” o protocolo mais longo jÃ¡ executado neste repositÃ³rio. TrÃªs rodadas acharam defeitos **criados pela rodada de correÃ§Ã£o anterior** (D-01, F-01, G-01), e a classe dominante de achado nÃ£o foi cÃ³digo errado e sim **documentaÃ§Ã£o afirmando prova mais ampla que a evidÃªncia nomeada**, que apareceu seis vezes: a alegaÃ§Ã£o "sem armadilha de teclado" sobreviveu dez rodadas antes de ganhar uma asserÃ§Ã£o de cobertura que realmente a prova. Vale como precedente de processo para revisÃµes futuras deste repositÃ³rio. Acessibilidade virou executÃ¡vel em `frontend/e2e/accessibility.spec.ts` (**9 testes no projeto `chromium`, logo no CI em todo PR**), e duas falhas reais foram achadas por essas asserÃ§Ãµes e corrigidas (contraste 4,48:1 e alvo de 19px). Detalhe completo, contrastes medidos, gates `VL-G1..VL-G17`, limitaÃ§Ãµes declaradas e registro de decisÃµes: `docs/frontend/visual-language-and-design-system.md`. 124 testes unitÃ¡rio/componente + 24 E2E funcionais (9 deles de acessibilidade) + 10 baselines de regressÃ£o visual (era 110 + 12 + 0). CI do PR #61: 4/4 verde.

**PendÃªncia real registrada, nÃ£o bloqueante**: as baselines de regressÃ£o visual foram gravadas em `win32` e o CI roda em `ubuntu-latest`; baselines de screenshot sÃ£o por plataforma, entÃ£o o projeto Playwright `visual` Ã© deliberadamente separado de `npm run test:e2e` e **nÃ£o** estÃ¡ gatilhado no CI hoje (plugÃ¡-lo falharia por baseline ausente, nÃ£o por regressÃ£o real). O caminho de adoÃ§Ã£o estÃ¡ escrito no Â§31 do documento: gravar as baselines num runner Linux, commitÃ¡-las, e adicionar `npm run test:visual` ao job `frontend` de `.github/workflows/ci.yml`. AtÃ© lÃ¡ o CI cobre as mesmas superfÃ­cies funcionalmente via `frontend/e2e/expiration-density.spec.ts`. TambÃ©m nÃ£o executado e declarado como tal: teste com leitor de tela real (NVDA/VoiceOver indisponÃ­veis no ambiente) â€” `REQUIRED` antes de Pilot.

**Core Expiration Vertical Slice** (2026-08-24, branch `feat/core-expiration-vertical-slice`): primeiro fluxo real e completo do anchor Vencimentos â€” Expiration Collection, Expiration Detail, Create Expiration, Renew Expiration â€” sobre a Frontend Production Foundation, usando contratos reais de ponta a ponta (frontend real â†’ BFF real â†’ API real â†’ persistÃªncia real). Nenhuma rota nova de BFF foi necessÃ¡ria (todas as 6 rotas de item jÃ¡ allowlisted). `APPROVED AS CORE EXPIRATION PRODUCTION VERTICAL SLICE` via protocolo Claudeâ†”Codex (Round B adversarial achou 4 bugs reais â€” 1 S1: corrida TOCTOU real na reaquisiÃ§Ã£o de um registro de idempotÃªncia `ABORTED` [`IdempotencyStore.begin()` fazia `get()`+`update()` incondicional, permitindo duas retentativas concorrentes "vencerem" e executarem a operaÃ§Ã£o guardada duas vezes]; 3 S2: `renewItem`'s `abort()` podendo disparar depois de um commit bem-sucedido [se sÃ³ `idempotency.complete()` falhasse depois], hash de renovaÃ§Ã£o ambÃ­guo quando `cycle` Ã© enviado independente de `newDueDate`, bug de fuso horÃ¡rio na formataÃ§Ã£o de data da `Overview` â€” todos corrigidos na Rodada C e reverificados sem achados novos na Rodada D). Durante a implementaÃ§Ã£o tambÃ©m foi corrigido um bug real prÃ©-existente de liveness de idempotÃªncia (`createItem`/`renewItem` nunca liberavam o lock de idempotÃªncia quando a escrita protegida falhava, deixando o registro `IN_PROGRESS` para sempre) â€” a correÃ§Ã£o adicionou um novo estado terminal `ABORTED` em `IdempotencyStore` (`src/shared/idempotency/idempotency.ts`), depois reforÃ§ado pela correÃ§Ã£o de corrida da Rodada C. Detalhe completo, achados, reconciliaÃ§Ã£o e Final Status: `docs/frontend/core-expiration-vertical-slice.md`. 96 testes unitÃ¡rio/componente de frontend + 12 E2E Playwright (era 42+6 na fundaÃ§Ã£o), 621 testes de backend â€” nenhuma regressÃ£o. Pendente ao fim desta sessÃ£o: push, PR para `develop`, CI, merge (ver "PrÃ³xima aÃ§Ã£o" abaixo se ainda nÃ£o concluÃ­do).

**Backend**: M0-M11 implementados. M6 (documentos/malware), M9 (Subject/Requirement), M10 (guest upload + automated chasing + convite inicial), M11 (CSV import) â€” todos deployados em `main`/`dev`. **M7 (extraÃ§Ã£o/OCR) teve implementaÃ§Ã£o iniciada em 2026-08-25** (D-057, decisÃ£o direta do Marcelo â€” "prossiga entÃ£o") â€” ver seÃ§Ã£o dedicada abaixo para o estado exato e o que falta. M12 (billing) **bloqueado por decisÃ£o de produto** (D-052, escolha de fornecedor de pagamento). M13 (Organization/Membership/RBAC) **gated** por gatilho comercial real (primeira venda B2B) que nÃ£o disparou.

**Full BFF (autenticaÃ§Ã£o de browser)**: design fechado via protocolo Claudeâ†”Codex em duas rodadas â€” D-053 (Full BFF, Claude 9,2/Codex 9,3) e D-054 (amendment de uma auditoria adversarial de 16 pontos, Claude 9,2/Codex 9,4). **Implementado nesta sessÃ£o e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/` â€” 76 testes unitÃ¡rios; infra Terraform real em `infra/modules/bff-session-table/` e `infra/modules/bff-api-gateway/`), junto de uma fundaÃ§Ã£o de frontend de produÃ§Ã£o real (`frontend/` â€” Vite+React+TS+React Router v7+TanStack Query v5, 42 testes unitÃ¡rio/componente + 6 smoke E2E via Playwright). Protocolo Claudeâ†”Codex completo (Rodada D levou **6 passagens** atÃ© convergir â€” 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o encontrados e corrigidos, todos verificados experimentalmente contra o cÃ³digo prÃ©-correÃ§Ã£o: consumo nÃ£o-atÃ´mico de `LoginAttempt`; commit incondicional do refresh podendo ressuscitar sessÃ£o revogada concorrentemente; releituras pÃ³s-refresh nÃ£o checando `revokedAt`; `logout`/`logoutAll` aceitando um `selector` correto com `secret` errado; `logoutAll` e `handleCallback` nÃ£o checando expiraÃ§Ã£o/idle-TTL antes de agir; idle-TTL da prÃ³pria `Session` nunca validado contra o relÃ³gio). Registro completo e Final Status em `docs/frontend/frontend-production-foundation.md`. Debate de design original em `docs/architecture/reviews/bff-full-vs-session-design/`.

**Planejamento de interface**: 8 documentos/artefatos produzidos em sequÃªncia, todos `docs/frontend/*.md` (mais `prototype/` para o 6Âº-8Âº), cada um fechado via protocolo Claudeâ†”Codex e `APPROVED`:
1. `interface-context-and-critical-tasks.md` â€” papÃ©is, JTBD, inventÃ¡rio de tarefas, criticidade/readiness.
2. `interface-conceptual-model-and-information-architecture.md` â€” modelo conceitual, IA recomendada (dual-anchor: Vencimentos + Fornecedor/Subject).
3. `interface-critical-user-journeys.md` â€” 8 journeys outcome-a-outcome.
4. `interface-screen-and-state-inventory.md` â€” 17 Interaction Surfaces (`SURF-001` a `SURF-017`) derivadas das 8 journeys, com taxonomia de estado (loading/empty/error/persistence/visibility), Epistemic Integrity Matrix e matrizes Surfaceâ†”Journey/Concept/Transition. RevisÃ£o Codex achou 3 furos reais de mesma causa raiz â€” `Document.SCANNING` classificado `PERSISTED` como se fosse `REMOTE_ASYNC`/`USER_KNOWN`; corrigido para `NOT_CURRENTLY_OBSERVABLE` (o gap de leitura de `BLOCKER-A` comeÃ§a em `SCANNING`, nÃ£o sÃ³ em `CLEAN`) â€” todos corrigidos, nenhum estrutural.
5. `interface-low-fidelity-wireframes.md` â€” wireframe ASCII das 17 `SURF-xxx` (grayscale conceitual, sem identidade visual), 8 journey walkthroughs, State Coverage Matrix. RevisÃ£o Codex achou 5 furos reais + 1 divergÃªncia factual â€” aÃ§Ã£o primÃ¡ria ambÃ­gua em 4 coleÃ§Ãµes, `BLOCKER-A` mascarado no estado inicial de Document Context, `SATISFIED` reaproximado de "compliance atual" numa variante do branch de `BLOCKER-C`, canal WhatsApp sem lastro (product creep), e `Dependencies` de Full BFF incompletas â€” todos corrigidos, nenhum estrutural.
6. `interface-interaction-prototype.md` + `prototype/` (cÃ³digo executÃ¡vel real, HTML/CSS/JS sem dependÃªncias) â€” protÃ³tipo navegÃ¡vel cobrindo as 17 superfÃ­cies e J-01â€“J-08 com 34 Prototype Scenario IDs determinÃ­sticos, verificado com testes automatizados de navegador headless. RevisÃ£o Codex achou 6 furos reais de 4 causas raiz â€” compressÃ£o de estados no guest upload (faltava validaÃ§Ã£o de arquivo e "reserva aceita" distinto de "enviado"), uma simulaÃ§Ã£o de coleta externa violando epistemic integrity (anunciava verificaÃ§Ã£o de seguranÃ§a concluÃ­da ao operador, que nÃ£o pode observar isso), dois campos sem `<label>`, e reincidÃªncia de menÃ§Ã£o a WhatsApp â€” todos corrigidos e reverificados funcionalmente, nenhum estrutural.
7. `interface-heuristic-accessibility-evaluation.md` â€” avaliaÃ§Ã£o do protÃ³tipo **executÃ¡vel** (nÃ£o sÃ³ a documentaÃ§Ã£o): Nielsen H1-H10, WCAG 2.2 AA (teclado/foco/semÃ¢ntica/forms testados em navegador headless real, nÃ£o sÃ³ lidos), `axe-core` (0 violaÃ§Ãµes em todos os estados testados, nas duas rodadas), re-execuÃ§Ã£o de J-01â€“J-08, Epistemic Integrity, `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`. Protocolo de **4 rodadas** Claudeâ†”Codex (nÃ£o as 2-3 rodadas tÃ­picas de etapas anteriores): Rodada A (autoavaliaÃ§Ã£o, 9 achados corrigidos) concluiu aprovaÃ§Ã£o â€” **prematuramente**, como a Rodada B mostrou. Rodada B (adversarial) achou 6 problemas-raiz reais nÃ£o vistos pela autoavaliaÃ§Ã£o: o mais grave, a prÃ³pria guarda anti-duplo-submit adicionada na Rodada A quebrava o recovery de erro de validaÃ§Ã£o em 3 formulÃ¡rios (S3); o segundo mais grave, `reconcileImport()` (caminho de reconciliaÃ§Ã£o manual de `UNKNOWN_OUTCOME` de import) afirmava ter criado registros sem realmente materializÃ¡-los no fake-backend â€” uma violaÃ§Ã£o epistÃªmica real, nÃ£o cosmÃ©tica. Rodada C corrigiu os 6, mas cometeu um erro de julgamento prÃ³prio ao manter um texto "SIMULATED FOR UX VALIDATION â€” NOT CURRENTLY SUPPORTED BY BACKEND" numa superfÃ­cie pÃºblica de guest (jargÃ£o tÃ©cnico visÃ­vel a um usuÃ¡rio externo nÃ£o-tÃ©cnico). Rodada D (nova adversarial sobre o cÃ³digo jÃ¡ corrigido) achou essa lacuna de conteÃºdo e uma correÃ§Ã£o incompleta nova (`submitAlert` reabilitava o botÃ£o sÃ³ na branch de erro, nÃ£o na de sucesso) â€” ambos corrigidos e reverificados individualmente em navegador no fechamento desta etapa. Quality Score final **9.04/10** (nÃ£o arredondado; calculado refletindo esse histÃ³rico de 4 rodadas, nÃ£o sÃ³ o estado final do cÃ³digo) â€” `APPROVED AS INPUT FOR USER VALIDATION`.
8. `interface-validation-readiness.md` â€” "Validation Readiness + Product Focus Hardening", 8 workstreams antes de User Validation: Participant Mode (default, sem Scenario IDs/barra de controle/tags tÃ©cnicas) vs. Evaluator Mode (`?mode=evaluator`, preserva tudo, sem regressÃ£o) no protÃ³tipo; `GTR-01` simulado no guest flow ("Solicitado por: Empresa Alfa Ltda."), documentado como simulaÃ§Ã£o, nunca marcado como backend-resolvido; cenÃ¡rio de densidade `PROTO-STRESS-DENSITY-01` (155 vencimentos/38 fornecedores/95 requisitos/21 solicitaÃ§Ãµes) que achou e corrigiu uma falta real de ordenaÃ§Ã£o por urgÃªncia na Overview/Collection; `CREATE-IDEMPOTENCY-01` **resolvido no backend real** (nÃ£o sÃ³ no protÃ³tipo) â€” `createItem` ganhou `idempotencyKey` opcional reusando o `IdempotencyStore` jÃ¡ usado por `renewItem`, 6 testes novos; tese de produto (compliance documental leve de terceiros) e mÃ©tricas de validaÃ§Ã£o formalizadas; `docs/frontend/interface-quality-standard.md` criado (12 eixos jÃ¡ em uso desde a 1Âª etapa, agora formal, nenhum critÃ©rio novo); matriz de gates User Validation/Pilot/Paid Pilot/Public Production para `BLOCKER-A/B/C`/`GTR-01`/`CREATE-IDEMPOTENCY-01`/Full BFF/Operational Architecture. Protocolo de 4 rodadas + 1 fechamento: Rodada B achou 4 furos reais (2 vazamentos de anotaÃ§Ã£o tÃ©cnica em Participant Mode, teste de idempotÃªncia sem cobrir o crash entre `commit()`/`complete()`, 2 imprecisÃµes de documentaÃ§Ã£o); Rodada C corrigiu os 4; Rodada D, ao reler o arquivo inteiro mais uma vez, achou **mais 6 vazamentos** (nenhum introduzido pela Rodada C, todos prÃ©-existentes desde a Rodada A) â€” corrigidos no fechamento, zero contaminaÃ§Ã£o residual confirmada por varredura completa. `APPROVED FOR USER VALIDATION PLANNING`.

Esses documentos descobriram **3 blockers tÃ©cnicos reais de backend** (nenhum resolvido tecnicamente, todos citados por ID em todo lugar relevante, nunca mascarados):
- **BLOCKER-A** â€” **fechado nesta sessÃ£o (2026-08-25), as duas metades**:
  1. `Document` (item-attached, M6): `GET /items/{itemId}/documents` e `GET /items/{itemId}/documents/{documentId}` â€” `DocumentService.listDocuments`/`getDocument`, autorizaÃ§Ã£o `document:read` (aÃ§Ã£o jÃ¡ existia na matriz, nunca usada), reusa `queryByPk` na partiÃ§Ã£o do item jÃ¡ existente (`data-model.md` linha 34), sem GSI novo.
  2. `DocumentSubmission` (guest-upload/subject module, agregado-irmÃ£o de `Document`, ancorado no `RequirementAssignment`): `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions` e `.../submissions/{submissionId}` â€” `RequirementService.listDocumentSubmissions`/`getDocumentSubmission`, reusando a aÃ§Ã£o `requirement:read` jÃ¡ existente (sem action nova reservada de antemÃ£o, diferente de `document:read`), mesma leitura por partiÃ§Ã£o sem GSI novo (`REQASSIGN#assignmentId#SUBMISSION#` como prefixo de SK). Achado incidental ao mexer nas rotas de `/subjects*`: as 4 rotas de `DocumentRequest` (D-037/D-049) jÃ¡ tinham handler pronto mas nunca tinham sido registradas no Terraform â€” nÃ£o Ã© desta sessÃ£o, jÃ¡ estava corrigido antes (comentÃ¡rio no prÃ³prio `main.tf`), sÃ³ confirmado que segue correto.

  Ambas as metades: mudanÃ§a mecÃ¢nica sobre um padrÃ£o de acesso jÃ¡ aprovado no data model â€” nÃ£o passaram pelo protocolo `AGENTS.md` Â§4 completo (nÃ£o Ã© decisÃ£o de arquitetura nova, Ã© fechamento de gap de leitura jÃ¡ modelado). 14 testes novos de unit (service+handlers+domain), rotas Terraform + `terraform test` dos mÃ³dulos `api-gateway` atualizados e passando, 709 testes de backend (era 695 no inÃ­cio da sessÃ£o), lint/typecheck/check-boundaries/validate-schemas/check-docs limpos, nenhuma regressÃ£o. **Achado real corrigido depois, na mesma sessÃ£o**: as 4 rotas novas nunca tinham sido allowlisted em `src/modules/bff/domain/proxy-allowlist.ts` â€” mesma classe de gap que as rotas de `DocumentRequest` jÃ¡ tiveram antes (comentÃ¡rio em `infra/modules/api-gateway/main.tf`). Corrigido, 4 testes novos, 711 testes de backend.
- **BLOCKER-B** â€” **implementaÃ§Ã£o completa, mergeada em `develop`** (PR #50, commit `8f00160`, 2026-08-25; ver seÃ§Ã£o abaixo). Antes desta branch, a materializaÃ§Ã£o automÃ¡tica de `ReminderOccurrence` estava desconectada do caminho normal de criaÃ§Ã£o/ediÃ§Ã£o de item. PendÃªncia real restante: decisÃ£o de produto sobre `renewItem` copiar `ReminderPolicy` (ver abaixo) â€” nÃ£o bloqueia mais nada alÃ©m dessa decisÃ£o em si.
- **BLOCKER-C** â€” **decisÃ£o tomada pelo Marcelo em 2026-08-25: Variante B (revisÃ£o humana explÃ­cita)**, confirmando a hipÃ³tese lÃ­der jÃ¡ registrada nos documentos de planejamento (`interface-validation-readiness.md` Â§22, `interface-conceptual-model-and-information-architecture.md` Â§37, `interface-critical-user-journeys.md` Â§37 â€” esses documentos continuam como registro histÃ³rico do debate, nÃ£o reescritos, per `AGENTS.md` regra de precedÃªncia de `history/`/artefatos aprovados). InvestigaÃ§Ã£o confirmou que Variante A (fechamento automÃ¡tico) nÃ£o Ã© viÃ¡vel hoje: `DocumentSubmission` nÃ£o guarda nenhum dado estruturado (data de validade, tipo) que permitiria inferir com seguranÃ§a a qual `ExpirationItem` uma submissÃ£o pertence â€” decidir automaticamente seria adivinhar, nÃ£o resolver. **Mecanismo de backend para a Variante B jÃ¡ existe, nenhum cÃ³digo novo necessÃ¡rio**: visibilidade do operador sobre submissÃµes pendentes vem das rotas `GET /subjects/{subjectId}/requirements/{assignmentId}/submissions`/`.../submissions/{submissionId}` (fechadas nesta mesma sessÃ£o como segunda metade de BLOCKER-A) combinadas com `GET /subjects/{subjectId}/requirements` (lista assignments `MISSING`) e o mecanismo de satisfaÃ§Ã£o jÃ¡ existente (`POST .../link`, prÃ©-existente). **Frontend implementado na mesma sessÃ£o (2026-08-25)**: primeira fatia real do anchor Fornecedor/Subject â€” `frontend/src/routes/subjects/{SubjectsCollection,SubjectDetail}.tsx` (rotas `/subjects`, `/subjects/:subjectId`, jÃ¡ ligadas no nav do `AppShell`), API layer (`api/subjects.ts`), 6 hooks novos (`useSubjectsDashboard`, `useSubject`, `useRequirementAssignments`, `useDocumentSubmissions`, `useLinkExpirationItem`/`useUnlinkExpirationItem` via `useOccMutation`, primeiro consumidor real desse hook). Fluxo: lista de fornecedores â†’ detalhe com requisitos â†’ "Revisar" expande evidÃªncia enviada (`DocumentSubmission`) â†’ operador informa o `itemId` de um vencimento jÃ¡ existente e vincula (`POST .../link`), OCC-protegido (`If-Match`), com `Desvincular` para reverter. Deliberadamente sem criaÃ§Ã£o de subject nem sugestÃ£o automÃ¡tica de item (a mesma lacuna de dado estruturado que descartou a Variante A). RÃ³tulos seguem a mesma disciplina de Epistemic Integrity jÃ¡ estabelecida no planejamento de interface (`SATISFIED`="Vinculado", nunca "Em dia"; `CLEAN`="Verificado (seguranÃ§a) â€” conteÃºdo nÃ£o conferido", nunca "Aprovado"). 11 testes novos, 110 testes de frontend (era 99), typecheck/lint/build limpos. **BLOCKER-C agora fechado de ponta a ponta** (backend + frontend).

Dois achados menores viraram gates formais nesta Ãºltima etapa: **`GTR-01`** (identidade do solicitante nÃ£o exposta ao guest) agora **simulada** no protÃ³tipo para User Validation, mas ainda nÃ£o resolvida no backend â€” `REQUIRED` a partir de Pilot. **`CREATE-IDEMPOTENCY-01`** (`POST /items` sem idempotÃªncia) foi **resolvido no backend real** â€” `createItem` ganhou `idempotencyKey` opcional â€” e, na sessÃ£o do Full BFF/frontend, o `requestHash` de detecÃ§Ã£o de payload divergente foi migrado de concatenaÃ§Ã£o por delimitador (colisÃ£o real possÃ­vel) para JSON canÃ´nico + SHA-256, e o frontend real (`frontend/src/hooks/useIdempotentMutation.ts`) passou a efetivamente enviar o header `Idempotency-Key` â€” a lacuna "nenhum frontend/BFF real envia o header" estÃ¡ fechada para o caminho de criaÃ§Ã£o de item.

**PrÃ³xima etapa natural do design** (ainda nÃ£o iniciada): **User Validation** â€” usando o protÃ³tipo jÃ¡ com Participant Mode isolado em `prototype/` (ver `prototype/README.md` para como rodar; Participant Mode Ã© o default, `?mode=evaluator` ativa o modo de engenharia) e `interface-validation-readiness.md` Â§15-18/Â§23-24 (tese de produto, mÃ©tricas de validaÃ§Ã£o, tarefas candidatas derivadas de outcomes, limitaÃ§Ãµes conhecidas do protÃ³tipo a comunicar ao facilitador) como input; o roteiro formal de entrevista fica para `User Validation Planning`, deliberadamente nÃ£o produzido ainda. NÃ£o obriga inÃ­cio imediato de Visual Design/Design System.

## BLOCKER-B â€” implementaÃ§Ã£o completa e mergeada em `develop` (PR #50, commit `8f00160`)

SessÃ£o de 2026-08-24/25 (continuaÃ§Ã£o apÃ³s a sessÃ£o anterior ter sido abortada por limite de tokens sÃ³ com reconhecimento parcial â€” handoff original em `docs/architecture/blocker-b-recon-handoff.md`, agora histÃ³rico) completou o pipeline real de entrega de lembretes de ponta a ponta. Arquitetura decidida e aprovada via protocolo Claudeâ†”Codex completo (rodadas B a H, nota final 9,2/10) em `docs/architecture/reminder-delivery-pipeline.md` â€” esse documento Ã© a fonte de verdade do desenho (event taxonomy, pointer lifecycle, fencing, backfill). ImplementaÃ§Ã£o real (nÃ£o sÃ³ design) tambÃ©m revisada via protocolo Claudeâ†”Codex (2 rodadas, nota final 9,2/10, `APPROVED` â€” achados reais corrigidos: reclassificaÃ§Ã£o de exceÃ§Ã£o em dois mÃ©todos novos do materializer, ConditionCheck de integridade de item pulado numa ediÃ§Ã£o same-item, schema da fila nunca registrado no `defaultSchemaRegistry` de produÃ§Ã£o).

O que existe agora, real e testado (695 testes, lint/typecheck/check-boundaries/validate-schemas limpos): trÃªs novos mÃ©todos de reconciliaÃ§Ã£o em `ReminderMaterializer` (fenced por versÃ£o da policy); lifecycle do ponteiro `POLICYREF#` em `ReminderPolicyService` (create/move/remove atÃ´mico); dois eventos novos (`expiration.item-deactivated.v1`, `reminder.policy-changed.v1`) mais `createItem` passando a emitir `expiration.item-due-date-changed.v1` (antes nÃ£o emitia nada); o worker `reminder-materialization-trigger` (a peÃ§a que realmente fecha o BLOCKER-B); mecanismo de entrega real via `DispatchOutboxRelay`/`OutboxSweeper` (achado real durante a implementaÃ§Ã£o: o "caminho genÃ©rico via EventBridge" que o desenho original presumia nunca foi de fato implementado neste cÃ³digo â€” `router_queue` nÃ£o tem consumidor Lambda nenhum); Terraform completo (fila+DLQ+Lambda+IAM, 13 `terraform test` passando); script de backfill (`scripts/backfill-reminder-policies.ts`, nunca roda automaticamente no deploy).

**DecisÃ£o do Marcelo (2026-08-25)**: `renewItem` deve copiar automaticamente a `ReminderPolicy` do item de origem para o novo, com um aviso ao usuÃ¡rio (resposta/UI) de que a policy copiada pode precisar de ajuste. Substitui o default de engenharia "nÃ£o copiar" antes proposto (`docs/architecture/reminder-delivery-pipeline.md` Â§8). **Implementado nesta sessÃ£o (2026-08-25), backend e frontend**: `completeRenewal` copia a(s) `ReminderPolicy` ITEM-scoped do item de origem para o novo, dentro da MESMA transaÃ§Ã£o da criaÃ§Ã£o do item (sem `ConditionCheck` de existÃªncia â€” o item novo Ã© garantido pela prÃ³pria transaÃ§Ã£o); `renewItem` passou a retornar `{ item, copiedReminderPolicyIds }` em vez de sÃ³ `ExpirationItem` (mudanÃ§a de contrato, ~13 call sites de teste atualizados); `handleRenewItem` inclui `copiedReminderPolicyIds` no corpo da resposta HTTP. No frontend, `RenewItemResponse` (novo tipo), `renewItem()`/`useRenewItem` propagam o campo, `RenewItem.tsx` passa `copiedReminderPolicyIds` via `navigate(...).state`, e `ItemDetail.tsx` exibe um aviso ("Os lembretes do ciclo anterior foram copiados... Revise...") quando `justRenewed && copiedReminderPolicyIds.length > 0`. 4 testes novos/reescritos no backend (incluindo `reminder-materialization-trigger.test.ts`, que antes afirmava explicitamente "policies are NOT auto-copied" â€” reescrito para o comportamento novo) + 3 novos no frontend. 710 testes de backend + 99 de frontend (era 96), lint/typecheck/check-boundaries/validate-schemas/build limpos nos dois projetos. `frontend/node_modules` nÃ£o estava instalado no inÃ­cio desta sessÃ£o (`npm ci` rodado para poder validar) â€” ambiente, nÃ£o cÃ³digo.

**PrÃ³xima aÃ§Ã£o**: **M7 (extraÃ§Ã£o/OCR) â€” todos os 9 itens numerados originais da lista estÃ£o CONCLUÃDOS (2026-08-26)**, cÃ³digo+infra reais para itens 1-9, nÃ£o sÃ³ camada pura â€” ver as seÃ§Ãµes dedicadas "Item N â€” CONCLUÃDO" na seÃ§Ã£o M7 abaixo para o detalhe de cada um. Resumo: itens 1-2 aplicados/gated em `dev`; itens 4-7 (`TextractTaskHandler`/`PdfParserTaskHandler`/`BedrockExtractionTaskHandler`/`ExtractionValidationTaskHandler`) reais de ponta a ponta; item 3 (`aws_sfn_state_machine` real via `infra/modules/extraction-workflow/`, wireado em `infra/main.tf`); item 8 (rotas HTTP `confirm`/`reject`, Â§1.7) implementado; item 9 (quota `AI_CALL`) verificado a fundo, nada faltando. **PR #60 (itens 5-8) mergeado em `main` em 2026-08-27** (commit `aeed7b8`). **O CD (`Deploy (CD)`) que rodou automaticamente apÃ³s esse merge FALHOU** no step `Terraform apply (dev)` (run `33036794831`) â€” causa raiz real: `infra/state-machines/document-extraction.asl.json`'s `RunTextract` Catch misturava nomes de erro especÃ­ficos com `States.ALL` num Ãºnico `ErrorEquals`, o que o Step Functions rejeita (`SCHEMA_VALIDATION_FAILED: States.ALL must appear alone and at end of list`; `terraform plan` nÃ£o pega esse erro, sÃ³ a chamada real de criaÃ§Ã£o do recurso em `apply`). **Corrigido, mergeado (PR #62, commit `a6faac6`) e CD reverificado verde (run `33041060474`)** â€” o Catch foi dividido em dois Catchers com o mesmo `Next`, sem mudanÃ§a de comportamento. Verificado com `npx asl-validator` localmente e, pÃ³s-deploy, com leitura real via `aws stepfunctions describe-state-machine --profile claude-dev` contra `dev`: a definiÃ§Ã£o publicada tem o fix (dois Catchers, `States.ALL` sozinho no segundo) e as 4 `FunctionName` jÃ¡ resolvidas para ARNs reais (`:live`). **`claude-dev` (perfil AWS CLI local) resolve para a conta `dev` (`975707451904`) certa** â€” a nota de uma sessÃ£o anterior dizendo "nenhum profile corresponde Ã  conta dev" estava desatualizada/errada. Em 2026-08-27 o Marcelo concedeu a esse perfil as permissÃµes de escrita necessÃ¡rias e a **verificaÃ§Ã£o E2E real foi executada** (ver `## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)` abaixo). Nenhuma mudanÃ§a de Terraform foi cogitada para isso â€” a flag `extraction_pipeline_enabled` nem precisa ser tocada, jÃ¡ que o state machine sempre existe e pode ser iniciado manualmente independente dela (sÃ³ o gatilho automÃ¡tico via EventBridge Ã© que Ã© gated). **RecorrÃªncia fechada na mesma sessÃ£o**: `ci.yml`/`cd.yml` ganharam um passo novo (`Validate Step Functions ASL definitions`, logo apÃ³s "Configure AWS credentials" nos dois) que chama `aws stepfunctions validate-state-machine-definition` contra todo `infra/state-machines/*.asl.json` â€” a mesma API que rejeitou o `States.ALL` antes, agora rodando em todo PR (`ci.yml`, antes do merge) e de novo no deploy (`cd.yml`, belt-and-suspenders), nunca mais sÃ³ descoberto no meio de um `terraform apply` real. NÃ£o requer credencial de escrita (Ã© uma chamada de validaÃ§Ã£o sem efeito colateral, testada com sucesso via o profile `claude-dev` read-only). SLSA/provenance de artefato **explicitamente adiado a pedido do Marcelo**. User Validation **em suspenso, aguardando segunda ordem do Marcelo** (nÃ£o retomar sem sinal explÃ­cito). Infra de hospedagem do SPA (etapas 1-5) fechada de ponta a ponta desde 2026-08-25, ver `## Infra de hospedagem do SPA â€” etapas 1-5 fechadas` abaixo se precisar do detalhe.

## Infra de hospedagem do SPA â€” etapas 1-5 fechadas (2026-08-25)

Arquitetura jÃ¡ aprovada em nÃ­vel conceitual (`docs/architecture/architecture-fase3-consolidada.md` Â§3: S3 privado + CloudFront, Origin Access Control, deploy imutÃ¡vel por hash; `implementation-blueprint.md` Â§12/Â§23: CSP com hashes estÃ¡ticos calculados no build, sem nonce dinÃ¢mico â€” Day 0 nÃ£o tem compute de borda). O que falta Ã© o desenho de implementaÃ§Ã£o real e o cÃ³digo â€” nenhuma decisÃ£o de produto pendente, mas hÃ¡ **uma decisÃ£o de arquitetura real ainda em aberto** que a prÃ³xima sessÃ£o precisa resolver antes de implementar (Type 1, nÃ­vel 5-6 da escala de risco â€” protocolo `AGENTS.md` Â§4 aplicÃ¡vel):

1. **DecisÃ£o de arquitetura a fechar primeiro â€” FECHADA nesta sessÃ£o (2026-08-25)**: como CloudFront e o BFF coexistem. **Alternativa (a) aprovada via protocolo Claudeâ†”Codex completo, 6 rodadas, nota final 9,2 (Claude)/9,3 (Codex) sobre 10** â€” `docs/architecture/adr/ADR-0011-cloudfront-bff-coexistence.md` Ã© a fonte de verdade do desenho; debate completo com cada achado real (erros de origin request policy, precedÃªncia de behaviors, mascaramento de erros do BFF via `custom_error_response`, header `Idempotency-Key` nunca encaminhado pelo proxy, etc.) em `docs/architecture/reviews/spa-hosting-cloudfront-bff/`. Resumo: uma Ãºnica distribution, behaviors `/bff` e `/bff/*` â†’ origem custom HTTPS do API Gateway do BFF (`AllViewerExceptHostHeader`, `CachingDisabled`), default behavior â†’ S3 com OAC + CloudFront Function de SPA routing (sÃ³ nesse behavior, nunca no do BFF). Zero mudanÃ§a em `client.ts`/`cookies.ts`/`csrf.ts`. Inclui dois fixes prÃ©-existentes descobertos pela revisÃ£o, a aplicar na implementaÃ§Ã£o: `proxy-service.ts` nÃ£o encaminha `Idempotency-Key` ao backend (`FORWARDED_REQUEST_HEADERS`), e o CORS de fallback de `bff-api-gateway` nÃ£o inclui `idempotency-key`/`if-match`/`PATCH`. Gate registrado (nÃ£o implementado): acesso direto ao `execute-api` pÃºblico precisa de mitigaÃ§Ã£o (header estÃ¡tico + WAF) antes de produÃ§Ã£o pÃºblica real.
2. **MÃ³dulo Terraform `infra/modules/spa-hosting` â€” IMPLEMENTADO (2026-08-25)**: bucket S3 privado (OAC, bloqueio de acesso pÃºblico total, versionado), CloudFront com 3 `ordered_cache_behavior`/`default_cache_behavior` (`/bff` e `/bff/*` explÃ­citos â†’ origem custom HTTPS do API Gateway do BFF, `AllViewerExceptHostHeader`, `CachingDisabled`; default â†’ S3+OAC), CloudFront Function de SPA routing (`spa-routing.js`, `RESERVED_PREFIXES = ["/bff", "/.well-known/"]`, guarda de mÃ©todo GET/HEAD, associada SÃ“ ao default behavior â€” nunca aos behaviors do BFF), duas Response Headers Policy distintas (`spa` com CSP prÃ³pria, `bff_edge_floor` com `override=false` e os mesmos valores literais de `bff-handler.ts`). 13 `terraform test` (mock_provider + `override_data` para o bucket policy) + 8 testes unitÃ¡rios Vitest da function (`test/unit/infra/spa-routing.test.ts`, via `vm`). Os 2 fixes prÃ©-existentes do ADR-0011 tambÃ©m aplicados: `proxy-service.ts` agora encaminha `idempotency-key` (1 teste novo), CORS de `bff-api-gateway` ganhou `Idempotency-Key`/`If-Match`/`PATCH` (1 `terraform test` novo). **Achado real ao implementar a etapa 3** (corrige uma suposiÃ§Ã£o errada do prÃ³prio ADR-0011): `var.spa_content_security_policy` nÃ£o precisa de hashes calculados por build â€” verificado contra o `frontend/dist/index.html` real, o build do Vite nÃ£o emite nenhum `<script>`/`<style>` inline, todo asset Ã© carregado por URL externa jÃ¡ hasheada, entÃ£o `script-src 'self'`/`style-src 'self'` (mesmos valores do `<meta>` CSP interino que jÃ¡ existe em `frontend/index.html`) bastam sem hash nenhum â€” a variÃ¡vel ganhou um default determinÃ­stico (mesma CSP do meta tag + `frame-ancestors 'none'`, que um `<meta>` nÃ£o consegue expressar). **MÃ³dulo wireado no `infra/main.tf` root** (`module "spa_hosting"`, usa `module.bff_api.api_endpoint`) e outputs novos em `infra/outputs.tf` (`spa_bucket_name`/`spa_distribution_id`/`spa_distribution_domain_name`). `var.app_origin` continua placeholder por enquanto (mesma disciplina de `ses_from_address`/`bff_cognito_domain_prefix`) â€” corrigir via `-var app_origin=https://<spa_distribution_domain_name>` depois que a distribution real existir (nÃ£o bloqueia o primeiro apply, sÃ³ o domÃ­nio final do Cognito/CORS). **Verificado com `terraform plan` real contra a conta `dev`** (perfil `claude-dev`, `-target=module.spa_hosting`): 11 recursos a criar, zero erro â€” confirma que os nomes de managed policy (`Managed-CachingDisabled`/`Managed-CachingOptimized`/`Managed-AllViewerExceptHostHeader`) resolvem de verdade contra a API real do CloudFront, nÃ£o sÃ³ contra mocks.
3. **Deploy imutÃ¡vel por hash â€” IMPLEMENTADO (2026-08-25)**: Vite jÃ¡ hasheia todo asset (`index-<hash>.js`/`.css`); nenhum prefixo versionado extra Ã© necessÃ¡rio (o hash no nome do arquivo jÃ¡ Ã© o mecanismo de imutabilidade). `index.html` Ã© tratado Ã  parte (nÃ£o hasheado, `Cache-Control: no-cache`, upload por Ãºltimo).
4. **IntegraÃ§Ã£o com `cd.yml` â€” IMPLEMENTADA (2026-08-25)**: novos passos no job `deploy-dev` existente (nÃ£o um job novo) â€” `npm ci`+`npm run build` em `frontend/`, `scripts/check-spa-build-artifacts.mjs` (segundo call site, junto com o de `ci.yml`), `aws s3 sync` dos assets hasheados (`--exclude index.html`, `Cache-Control: public, max-age=31536000, immutable`, **sem `--delete`** â€” assets antigos nunca sÃ£o removidos, Ã© isso que mantÃ©m um rollback de `index.html` funcional, jÃ¡ que as referÃªncias antigas continuam servÃ­veis), upload de `index.html` por Ãºltimo, `aws cloudfront create-invalidation` para `/index.html` e `/`. Outputs novos (`spa_bucket`/`spa_distribution_id`/`spa_distribution_domain`) lidos do mesmo passo `Get Terraform outputs` jÃ¡ existente. **PendÃªncia real registrada, nÃ£o implementada**: o manifesto de deploy (`pointers/current-healthy.json`) ainda sÃ³ cobre Lambdas â€” nÃ£o guarda qual `index.html`/conjunto de assets estava ativo, entÃ£o `rollback.yml` (mecanismo existente) nÃ£o cobre a SPA ainda; um rollback do SPA hoje seria manual (reenviar um `index.html` antigo, cujos assets referenciados continuam em S3 por causa do `--delete` nunca usado). Candidato a follow-up, nÃ£o bloqueia o deploy normal.
5. **VerificaÃ§Ã£o real (Camada 3) â€” CONCLUÃDA (2026-08-25), achou e corrigiu um bug real de produÃ§Ã£o**: primeiro `apply` real via CD (PR #54, 11 recursos criados: bucket `exptrk-dev-spa`, distribution `E2XPYCT6NSP8R1`, domÃ­nio `d1mbs2t047qo9d.cloudfront.net`). Primeira rodada de `curl` devolveu **403 AccessDenied em tudo** â€” isolado (via `aws s3api get-bucket-encryption` depois de confirmar bucket policy/OAC corretos) como SSE-KMS com a chave gerenciada `alias/aws/s3`, cuja polÃ­tica fixa nÃ£o pode conceder `kms:Decrypt` a `cloudfront.amazonaws.com`. **Corrigido** (PR #55, SSE-S3/AES256) e **reaplicado com sucesso via CD**. Segunda rodada de verificaÃ§Ã£o, tudo confirmado contra a distribution real:
   - `GET /` â†’ 200, `text/html`.
   - `GET /assets/index-*.js` â†’ 200, `Cache-Control: public, max-age=31536000, immutable`.
   - `GET /items` (rota SPA sem extensÃ£o, V6 do ADR-0011) â†’ 200, corpo = `index.html` (fallback da CloudFront Function funcionando).
   - `GET /bff` e `GET /bff/` (V1/V2) â†’ 404 `application/json` (`{"message":"Not Found"}`, resposta real do API Gateway) â€” nunca HTML, confirma que o namespace `/bff` nunca cai no fallback da SPA.
   - `GET /bff/session` (V3, rota real do BFF) â†’ 200 `application/json` (`{"authenticated":false}`).
   - `GET /bff/api/items` sem sessÃ£o (V4) â†’ 401 `application/json` (`AUTH_REQUIRED`) â€” nunca HTML.
   - `GET /bff/api/rota-inexistente` sem sessÃ£o (V5-404) â†’ 401 `AUTH_REQUIRED` (a checagem de auth roda antes do allowlist match, esperado â€” o caso 404-por-allowlist com sessÃ£o vÃ¡lida e o caso 403-por-CSRF, que exigem um login real via Cognito Hosted UI, nÃ£o foram exercitados por nÃ£o serem automatizÃ¡veis via `curl` sozinho; a propriedade crÃ­tica de seguranÃ§a â€” nenhum erro do BFF vira HTML da SPA â€” jÃ¡ estÃ¡ confirmada pelos casos acima).
   - Headers de seguranÃ§a confirmados chegando ao browser real e DIFERENTES por behavior: SPA (`/`) tem a CSP prÃ³pria (`script-src 'self'` etc.) + HSTS + `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy`; BFF (`/bff/session`) tem `default-src 'none'; frame-ancestors 'none'` + os mesmos HSTS/nosniff/Referrer-Policy â€” exatamente o desenho de duas Response Headers Policy distintas do ADR-0011, verificado real, nÃ£o sÃ³ via `terraform plan`.
   **PendÃªncia real registrada**: `document-buckets`/`deploy-manifest-bucket` usam a mesma chave `alias/aws/s3` mas nunca sÃ£o lidos via CloudFront/OAC (sÃ³ por IAM de role de Lambda), entÃ£o nÃ£o tÃªm este bug â€” mas se algum desses buckets algum dia ganhar uma origem CloudFront, o mesmo problema se repetiria. NÃ£o corrigido preventivamente nesta sessÃ£o (fora do escopo desta decisÃ£o, nenhum uso real hoje), sÃ³ registrado como conhecimento para o futuro. **A infra de hospedagem do SPA (etapas 1-5) estÃ¡ fechada de ponta a ponta e ativa em `dev` real.**

## M7 (extraÃ§Ã£o/OCR) â€” implementaÃ§Ã£o iniciada (2026-08-25, D-057)

Design jÃ¡ aprovado em `docs/architecture/reviews/m7-extraction-design/claude-reconciliation-final-design.md` (D-035, 7 rodadas, 9,2/9,3) â€” esta seÃ§Ã£o Ã© sobre a IMPLEMENTAÃ‡ÃƒO, o design nÃ£o muda. **Escopo Ã© grande** (Step Functions, Textract assÃ­ncrono, sandbox de parser isolado, Bedrock condicional, AppConfig, rotas HTTP de confirmaÃ§Ã£o) â€” vai continuar por mÃºltiplas sessÃµes, nÃ£o Ã© razoÃ¡vel fechar num Ãºnico slice. Estado exato ao fim desta sessÃ£o:

**Feito**:
1. `docs/architecture/privacy-lgpd.md` Â§4 ganhou a classe `EXTRACTION_TRANSIENT` â€” prÃ©-requisito de design que o prÃ³prio D-035 exige estar registrado antes de qualquer implementaÃ§Ã£o (nÃ£o Ã© pendÃªncia pÃ³s-produÃ§Ã£o).
2. MÃ³dulo novo `src/modules/extraction/domain/` (sÃ³ domÃ­nio puro, zero infra/Lambda/HTTP ainda):
   - `extraction-run.ts` â€” `ExtractionRun` (chave `TENANT#t#DOC#d`/`RUN#<runId>`, `data-model.md` linha 105) + `deriveExtractionRunId()` determinÃ­stico (SHA-256 de `tenantId|documentId|documentVersion|pipelineVersion`, a prÃ³pria chave de idempotÃªncia da execuÃ§Ã£o).
   - `extracted-field.ts` â€” `ExtractedField` (mesma PK do run, SK `FIELD#<fieldName>#<runId>`, `data-model.md` linha 107), com `ExtractionSource`/`ExtractionAgreement`/`ExtractedFieldState` fechados conforme o exemplo de `implementation-blueprint.md` Â§12.5.
   - `field-schema.ts` â€” schema v1 com sÃ³ `expirationDate` (Ãºnico campo que o design aprovado nomeia concretamente â€” deliberadamente nÃ£o inventei outros campos de produto), `DETERMINISTIC_CONFIDENCE_THRESHOLD = 0.75`, `pipelineVersion`/`thresholdVersion` versionados juntos.
   - `decide-bedrock.ts` â€” `needsBedrock()` implementando o contrato exato da rodada 4 do design (regras a/b/c, incluindo a regra (c) de ambiguidade real de OCR nunca baseada em confidence isolada de reconhecimento de caractere).
   - `retention.ts` â€” helper de safety net de 24h para o artefato transitÃ³rio (a exclusÃ£o real Ã© explÃ­cita por `ExtractionValidationTaskHandler`, este helper Ã© sÃ³ a rede de seguranÃ§a do lifecycle S3).
3. 15 testes novos (740 totais), zero regressÃ£o. Commitado direto em `develop` (`docs: ...`/`feat(extraction): ...` â€” **ainda sem PR/merge para `main`**, dado que nÃ£o hÃ¡ nenhuma infra/runtime ativÃ¡vel ainda, nada a fazer deploy).

**Item 1 â€” CONCLUÃDO nesta sessÃ£o (2026-08-25/26)**: `infra/modules/feature-flags` (AppConfig real, aplicaÃ§Ã£o/environment/configuration-profile `AWS.Freeform`/deployment instantÃ¢neo) entregando os kill switches `AI_EXTRACTION`/`OCR`/`WHATSAPP` (nome do mÃ³dulo corrigido pelo design â€” transversal, nÃ£o `extraction-appconfig`, jÃ¡ que o schema inclui `WHATSAPP`, feature de Notification). Os trÃªs defaults `false` (fail-closed, prÃ©-condiÃ§Ãµes externas de M7 nÃ£o fechadas). Output `feature_flags_read_policy_json` pronto para os workers futuros (item 2+) anexarem. Root `variables.tf` ganhou `extraction_pipeline_enabled` (D-035 Â§1.6, default `false`) â€” gate Terraform separado do pipeline inteiro, ainda nÃ£o referenciado por nenhum recurso (sÃ³ os workers futuros vÃ£o condicionar-se a ele). 2 `terraform test` do mÃ³dulo + as 13 do `stack.tftest.hcl` passando, `terraform fmt`/`validate` limpos, `terraform plan -target=module.feature_flags` real contra `dev` (perfil `claude-dev`) confirmou 6 recursos a criar sem erro. **PR #57 mergeado, CI verde, CD aplicou com sucesso em `dev` real** (AppConfig application/environment/configuration-profile/deployment reais existem agora na conta `dev`).

**Item 3 (ASL) e item 4 (`TextractTaskHandler`) â€” CAMADA PURA CONCLUÃDA nesta sessÃ£o (2026-08-26), sem runtime/infra real ainda** (mesmo padrÃ£o que o item 0 seguiu: domÃ­nio+aplicaÃ§Ã£o primeiro, depois handler Lambda/infra numa fatia posterior â€” deliberado, nÃ£o um corte de escopo silencioso):

- Taxonomia de erro nova em `src/shared/errors/app-error.ts`: `UnsupportedDocumentTypeError`/`OcrDisabledError`/`TextractUnsupportedDocumentError`/`TextractPartialFailureError`/`TextractJobPersistenceFailedError` â€” cada `code` casa literalmente com o `ErrorEquals` correspondente no ASL (ex.: `code: "UnsupportedDocumentType"`), nunca renomear um sem o outro.
- `src/modules/extraction/domain/textract-job.ts` â€” entidade `TextractJob` (chave prÃ³pria `TEXTRACTJOB#<jobId>`/`TEXTRACTJOB#<jobId>`, deliberadamente nÃ£o tenant-scoped â€” `COMPLETE_OCR` sÃ³ tem `jobId` disponÃ­vel da notificaÃ§Ã£o SNS/SQS), `deriveTextractClientRequestToken()` determinÃ­stico, `computeTextractJobTtl()`. Documentado em `docs/architecture/data-model.md` (nova seÃ§Ã£o "TextractJob").
- `src/modules/extraction/domain/document-classifier.ts` â€” classificador heurÃ­stico puro (magic bytes > extensÃ£o > contentType), `classifyDocumentType()` retorna `null` (nunca lanÃ§a) quando nenhum sinal Ã© conclusivo â€” quem decide que isso Ã© `UnsupportedDocumentTypeError` Ã© o caller.
- Portas novas em `src/modules/extraction/ports/`: `feature-flags-reader.ts` (fail-closed Ã© responsabilidade do caller, nÃ£o do adapter), `textract-job-store.ts`, `textract-client.ts`, `ocr-artifact-store.ts` (sem mÃ©todo de delete â€” a exclusÃ£o do artefato Ã© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler), `task-token-encryptor.ts` (mesmo contrato de `src/modules/bff/ports/token-encryptor.ts`, declarado localmente por disciplina de fronteira de mÃ³dulo), `task-token-sender.ts` (`SendTaskOutcome` distingue `SENT`/`TERMINAL_QUIET`/`TERMINAL_WARN_INVALID_TOKEN`; qualquer outro erro de `SendTask*` deve ser relanÃ§ado pelo adapter, nunca capturado).
- `src/modules/extraction/application/start-ocr.ts` (`startOcr()`) â€” orquestra classificaÃ§Ã£o â†’ kill switch `OCR` (fail-closed) â†’ reserva `AI_CALL` idempotente (`TenantQuotaService.consume` com `window: "<runId>|TEXTRACT"`, `limit: 1` â€” um retry do mesmo run encontra `QuotaExceededError` contra a prÃ³pria reserva anterior e trata como "jÃ¡ reservado", nÃ£o como falha real) â†’ `StartDocumentTextDetection` (com `ClientRequestToken` determinÃ­stico) â†’ persiste `TextractJob` com retry local (`jobPersistAttempts`, default 2) antes de propagar `TextractJobPersistenceFailedError`. Nunca chama `SendTaskSuccess` â€” retorna void, a execuÃ§Ã£o do Step Functions fica parada no `taskToken` atÃ© `completeOcr` resolver depois. Compensa a quota via `TenantQuotaService.release()` quando o prÃ³prio `StartDocumentTextDetection` falha.
- `src/modules/extraction/application/complete-ocr.ts` (`completeOcr()`) â€” busca `TextractJob` por `jobId`; caso Ã³rfÃ£o (nenhum job encontrado) faz UMA chamada de confirmaÃ§Ã£o a `GetDocumentTextDetection`, descarta o resultado, nunca trata como erro; pagina os resultados; `SUCCEEDED`/`PARTIAL_SUCCESS` persistem o artefato e chamam `SendTaskSuccess` (com warning `PARTIAL_OCR` no caso parcial); `FAILED` chama `SendTaskFailure(TextractPartialFailure)`; **nunca apaga o artefato transitÃ³rio em nenhum caminho** (o port `OcrArtifactStore` nem expÃµe um mÃ©todo de delete); `taskTokenCiphertext` Ã© limpo em qualquer desfecho terminal do `SendTask*` (sucesso, `TERMINAL_QUIET`, `TERMINAL_WARN_INVALID_TOKEN`) e preservado (erro relanÃ§ado, sem tocar no registro) em qualquer outro erro â€” a mensagem SQS reentrega normalmente.
- Schema novo `schemas/queues/textract-completion.v1.json` (envelope SNS bruto â€” `Type`/`MessageId`/`TopicArn`/`Message`, o `Message` interno nÃ£o Ã© revalidado como JSON aninhado, mesma convenÃ§Ã£o de `notification-ses-callback.v1.json`) + 4 casos novos em `test/contract/schemas.test.ts`.
- `infra/state-machines/document-extraction.asl.json` â€” `RunTextract` Ã© o Ãºnico Task state real (aponta para `textract-task:live`, `waitForTaskToken`, `Catch` cobrindo os 5 cÃ³digos de erro novos + timeouts, tudo indo para `RunDeterministicParser`, nunca direto para `MarkPendingConfirmation`). Os demais ~9 estados (`RunDeterministicParser`/`NeedsBedrock`/`CheckAiKillSwitch`/`RunBedrock`/`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) sÃ£o stubs documentados referenciando Lambdas dos itens 5-7 (`pdf-parser-task:live`/`bedrock-extraction-task:live`/`extraction-validation-task:live`), que ainda nÃ£o existem.
- `infra/modules/extraction-workflow/` â€” mÃ³dulo Terraform novo (`variables.tf`/`main.tf`/`outputs.tf`/`versions.tf`), parametrizado pelas 4 ARNs de funÃ§Ã£o + role de execuÃ§Ã£o, usa `templatefile`-like `replace()` encadeado para substituir os placeholders `"textract-task:live"` etc. pelas ARNs reais no `definition` do `aws_sfn_state_machine`. **Deliberadamente NÃƒO chamado em `infra/main.tf`** â€” um `aws_sfn_state_machine` cujo `definition` referencia uma Lambda inexistente falha o `apply` real (nÃ£o sÃ³ em runtime), e os 3 handlers dos itens 5-7 nÃ£o existem. Verificado standalone: `terraform fmt`/`init -backend=false`/`validate` limpos dentro do prÃ³prio diretÃ³rio do mÃ³dulo. Nenhum `terraform test` (`.tftest.hcl`) escrito ainda para este mÃ³dulo especificamente â€” pendÃªncia explÃ­cita abaixo.
- 35 testes novos (783 totais no backend, era 748): `test/unit/extraction/{document-classifier,textract-job,start-ocr,complete-ocr}.test.ts`, todos com fakes escritos Ã  mÃ£o (sem `vi.mock`), cobrindo: classificaÃ§Ã£o por magic-bytes/extensÃ£o/contentType/prioridade; determinismo do `ClientRequestToken`; caminho feliz de `startOcr`; `UnsupportedDocumentTypeError` antes de tocar flags/quota/Textract; `OcrDisabledError` (kill switch off e leitura de flags falhando, fail-closed); compensaÃ§Ã£o de quota + `TextractUnsupportedDocumentError` quando o Textract falha; idempotÃªncia da reserva de quota sob retry do mesmo run; retry local de persistÃªncia do `TextractJob` (sucesso apÃ³s 1 falha, esgotamento apÃ³s N); `completeOcr`: job Ã³rfÃ£o (com e sem falha na confirmaÃ§Ã£o), jÃ¡-finalizado (ciphertext ausente), `SUCCEEDED`/`PARTIAL_SUCCESS`/`FAILED`, paginaÃ§Ã£o multi-pÃ¡gina, os 3 desfechos terminais limpando o ciphertext, erro transitÃ³rio de `SendTask*` relanÃ§ado sem tocar no ciphertext, e a invariante "nunca apaga o artefato" (o port nÃ£o expÃµe delete). `npm run typecheck`/`lint`/`test`/`validate-schemas`/`check-docs` limpos; `terraform fmt -check -recursive` limpo na Ã¡rvore inteira de `infra/`.

**Item 4 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real runtime + infra, ainda sem PR/merge**: as trÃªs decisÃµes pendentes acima foram todas tomadas e implementadas (nÃ£o deixadas como pendÃªncia):
- **AppConfig no Lambda: `@aws-sdk/client-appconfigdata` direto, decisÃ£o final** (nÃ£o a extensÃ£o via layer) â€” `src/modules/extraction/persistence/appconfig-feature-flags-reader.ts`, `StartConfigurationSession`+`GetLatestConfiguration` com cache de `nextToken`/Ãºltimos flags conhecidos em memÃ³ria do processo. Motivo: a extensÃ£o via Lambda Layer precisaria de uma ARN de layer gerenciada pela AWS wireada em `infra/modules/lambda-function` â€” nÃ£o existe nenhum precedente disso no repo (sÃ³ `adot_layer_arn`, uma layer diferente), e este handler sÃ³ lÃª os flags uma vez por invocaÃ§Ã£o de `START_OCR` (nÃ£o por pÃ¡gina/bloco), entÃ£o o custo/latÃªncia de uma chamada de API direta por invocaÃ§Ã£o Ã© equivalente ao que a extensÃ£o teria numa invocaÃ§Ã£o fria de qualquer forma. Revisitar sÃ³ se o volume tornar isso um custo real (a extensÃ£o amortiza entre invocaÃ§Ãµes warm via cache local, este adapter sÃ³ amortiza dentro de uma sessÃ£o de token de um processo longo).
- **`TaskTokenEncryptor` real: CMK nova dedicada** (`aws_kms_key.task_token` em `infra/main.tf`), nÃ£o reuso da CMK do BFF â€” mesma disciplina D-054: um token de callback do Step Functions Ã© uma credencial viva tÃ£o sensÃ­vel quanto o refresh token do BFF, e compartilhar uma CMK entre dois mÃ³dulos independentemente deployÃ¡veis acoplaria rotaÃ§Ã£o/key-policy dos dois. `src/modules/extraction/persistence/kms-task-token-encryptor.ts` (`KmsTaskTokenEncryptor`, estruturalmente idÃªntico a `bff`'s `KmsTokenEncryptor`, mas declarado localmente por disciplina de fronteira de mÃ³dulo, mesma razÃ£o do port).
- **TÃ³pico SNS de conclusÃ£o do Textract: criado** (`aws_sns_topic.textract_completion`) + role IAM dedicada (`aws_iam_role.textract_sns_publisher`, `sts:AssumeRole` escopado a `textract.amazonaws.com`) que o Textract assume para publicar â€” distinta da role de execuÃ§Ã£o do prÃ³prio Lambda.

Real (cÃ³digo + infra, nÃ£o sÃ³ camada pura):
- Adapters: `DynamoDbTextractJobStore`, `TextractSdkClient` (`@aws-sdk/client-textract`), `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`, `SfnTaskTokenSender` (`@aws-sdk/client-sfn`, classificaÃ§Ã£o real `TaskTimedOut`/`TaskDoesNotExist`â†’`TERMINAL_QUIET`, `InvalidToken`â†’`TERMINAL_WARN_INVALID_TOKEN`, qualquer outro erro relanÃ§ado), `AppConfigFeatureFlagsReader` â€” todos em `src/modules/extraction/persistence/`.
- Composition root `src/modules/extraction/composition/extraction.ts` (`buildTextractTaskWorkerDeps`).
- Handler real `src/runtime/aws/handlers/textract-task-handler.ts` â€” um Ãºnico deployable dispatando por shape de evento: `START_OCR` (invocaÃ§Ã£o direta do Step Functions via `lambda:invoke.waitForTaskToken`, evento `{ operation, taskToken, input }`) vs `COMPLETE_OCR` (`SQSEvent`, mesmo padrÃ£o `batchItemFailures` de `extraction-starter-handler.ts`, envelope SNS bruto per `schemas/queues/textract-completion.v1.json`). `SecureLogger`/`runWithContext` em todo lugar, nunca loga task token/texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (Textract Start/Get, `states:SendTaskSuccess/Failure/Heartbeat`, KMS na CMK nova, S3 leitura no bucket clean + leitura/escrita no bucket `EXTRACTION_TRANSIENT` novo, `feature_flags_read_policy_json`), bucket `EXTRACTION_TRANSIENT` (SSE-S3 deliberadamente, nÃ£o SSE-KMS â€” evita a MESMA classe de bug do incidente OAC/KMS do spa-hosting, aqui sem necessidade real porque sÃ³ a role deste Lambda toca o bucket; lifecycle 24h = `EXTRACTION_TRANSIENT_LIFECYCLE_HOURS`), fila+DLQ (`sqs-worker-queue`) assinando o tÃ³pico SNS, permissÃ£o `lambda:InvokeFunction` para `states.amazonaws.com` jÃ¡ escopada Ã  ARN determinÃ­stica da state machine futura (`local.extraction_state_machine_arn`, jÃ¡ existia desde o item 2). **Nenhum recurso condicionado a `var.extraction_pipeline_enabled`** â€” mesma lÃ³gica do item 2: todo recurso aqui Ã© inerte sozinho, porque `StartDocumentTextDetection` (a Ãºnica chamada paga) sÃ³ roda se o Step Functions invocar este Lambda via `waitForTaskToken`, e a state machine (item 3) ainda nÃ£o existe.
- Novo output `textract_task_handler_function_arn` (ponto de wiring para o mÃ³dulo `extraction-workflow` do item 3, ainda uninstantiated). `scripts/build-lambdas.ts` ganhou `"textract-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 27â†’28 funÃ§Ãµes (2 asserts).
- 11 testes novos (794 totais no backend) â€” sÃ³ para os dois adapters com lÃ³gica real de branching (`SfnTaskTokenSender`'s classificaÃ§Ã£o de erro, `AppConfigFeatureFlagsReader`'s cache de sessÃ£o); os wrappers puramente pass-through (`DynamoDbTextractJobStore`, `TextractSdkClient`, `S3OcrArtifactStore`, `KmsTaskTokenEncryptor`) nÃ£o ganharam teste unitÃ¡rio dedicado, seguindo o MESMO precedente jÃ¡ existente no repo (`DynamoDbExtractionRunStore`, `KmsTokenEncryptor`, `SfnExtractionExecutionStarter` tambÃ©m nÃ£o tÃªm) â€” verificados via `terraform plan` real contra `dev`, nÃ£o via mock unitÃ¡rio.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 30 recursos novos quanto full-stack sem target â€” 0 erros, 0 destroys inesperados (sÃ³ o prÃ³prio slice + o ruÃ­do de republish de versÃ£o jÃ¡ prÃ©-existente em todas as outras Lambdas). **Nunca `terraform apply` local** (`AGENTS.md` Â§3).
- Commits em `develop`: `853ac01` (camada de runtime real) e `fc20dd5` (infra real) â€” **ainda sem PR/merge para `main`** no momento em que este parÃ¡grafo foi escrito (abrir o PR Ã© o prÃ³ximo passo imediato desta mesma sessÃ£o, ver "PrÃ³xima aÃ§Ã£o" no topo do arquivo).

**Item 5 (`PdfParserTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta**: bem mais estreito que o item 4 â€” a ASL's `RunDeterministicParser` Ã© `arn:aws:states:::lambda:invoke` **sem** `waitForTaskToken` (nÃ£o hÃ¡ task token nenhum neste handler), entÃ£o Ã© uma Ãºnica invocaÃ§Ã£o sÃ­ncrona, sem SQS, sem `SendTaskSuccess/Failure`, sem DynamoDB/Textract/KMS/Step Functions client.

- DomÃ­nio novo `src/modules/extraction/domain/deterministic-field-parser.ts` â€” `extractExpirationDateCandidates()`, regex determinÃ­stico (dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd, pivot de ano de 2 dÃ­gitos 80/00) sobre o texto OCR (linhas `LINE` do Textract jÃ¡ concatenadas), nunca sobre bytes de PDF crus e nunca chamando IA. ConfianÃ§a alta (0.9, acima do `DETERMINISTIC_CONFIDENCE_THRESHOLD`) quando a data estÃ¡ a atÃ© 40 caracteres de uma palavra-chave de vencimento (pt-BR/en); confianÃ§a fraca (0.5, abaixo do threshold) quando nÃ£o hÃ¡ palavra-chave prÃ³xima. Reaproveita `MAX_DECOMPRESSED_BYTES` de `workers/parser-sandbox/parser.ts` (M6) como o limite numÃ©rico que trunca o texto processado â€” a Ãºnica reutilizaÃ§Ã£o real de M6 possÃ­vel aqui, jÃ¡ que o parser estrutural de M6 (`parsePdfStructure`) opera sobre bytes de PDF, nÃ£o sobre texto OCR, e nunca extrai campos/datas (sÃ³ valida estrutura). `DeterministicParserFailedError` novo em `app-error.ts` (cÃ³digo `DeterministicParserFailed`) â€” nÃ£o precisa casar literalmente com nenhum `ErrorEquals` porque o `Catch` deste estado no ASL jÃ¡ Ã© o genÃ©rico `States.ALL`.
- AplicaÃ§Ã£o nova `src/modules/extraction/application/run-deterministic-parser.ts` â€” `runDeterministicParser()`: lÃª o artefato OCR (quando presente) â†’ extrai candidatos de `expirationDate` via `getFieldSchema()` â†’ chama `needsBedrock()` (item 0) â†’ lÃª o kill switch `AI_EXTRACTION` (fail-closed) â†’ retorna `{..., extractedFields, needsBedrock, aiExtractionEnabled}`. Caminho degradado (Textract falhou, sem artefato): **nunca fabrica um candidato** â€” retorna `extractedFields` vazio, deixando `needsBedrock()` decidir (regra (a), "sem candidato" jÃ¡ implica `true`) e a decisÃ£o real cair pra Bedrock/`PENDING_CONFIRMATION` a jusante, nunca inventando aqui.
- Porta estendida (nÃ£o duplicada) `OcrArtifactStore` ganhou `get()` â€” o item 4 sÃ³ tinha `put()` (quem escreve o artefato); o item 5 Ã© o primeiro leitor real. `S3OcrArtifactStore.get()` implementado. Continua sem `delete()` (invariante do item 4 preservada â€” sÃ³ `ExtractionValidationTaskHandler`, item 7, apaga).
- **Achado real corrigido durante a implementaÃ§Ã£o, nÃ£o uma decisÃ£o de produto nova**: `completeOcr`'s `SendTaskSuccess` (item 4) nÃ£o tinha `ResultPath` na transiÃ§Ã£o de sucesso do ASL, entÃ£o seu payload (`{ocrAvailable, artifact, warnings}`) **substituÃ­a inteiramente** o `$` do Step Functions â€” todo o contexto original da execuÃ§Ã£o (`tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion`) seria perdido para `RunDeterministicParser` no caminho feliz (no caminho degradado, o `Catch`'s `ResultPath: "$.ocrFailure"` preserva o `$` original normalmente, entÃ£o sÃ³ o caminho de sucesso tinha o problema). Corrigido: `TextractJob` ganhou o campo `pipelineVersion` (gravado por `startOcr`), e `completeOcr` reanexa `tenantId`/`itemId`/`documentId`/`documentVersion`/`runId`/`pipelineVersion` (lidos do prÃ³prio `job`) ao payload de sucesso. MudanÃ§a aditiva/reversÃ­vel sem migraÃ§Ã£o de dado real (o registro tem TTL de 24h, nunca houve dado histÃ³rico real).
- **DECISÃƒO PENDENTE (registrada aqui, default conservador aplicado)**: o design (Â§1.3) menciona "parser tries file-metadata-only heuristics" para o caminho degradado (sem OCR). NÃ£o implementado â€” nenhum campo de metadado de PDF (data de criaÃ§Ã£o, autor, etc.) corresponde semanticamente a `expirationDate`; inventar esse mapeamento seria uma decisÃ£o de produto nÃ£o revisada, nÃ£o um default de engenharia. Ficou como fallback conservador (zero candidatos, nunca um valor fabricado). Revisitar sÃ³ se um campo futuro do schema realmente mapear para metadado real de arquivo.
- **Achado de correÃ§Ã£o na prÃ³pria ASL, sem alterar topologia/Next states**: o retorno real de uma `arn:aws:states:::lambda:invoke` (sem `waitForTaskToken`) vem embrulhado em `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` (stubs dos itens 6/7, ainda nÃ£o implementados) referenciavam `$.needsBedrock`/`$.aiExtractionEnabled` diretamente, o que nunca teria batido com o payload real do handler. Corrigido para `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` â€” nÃ£o muda nenhum `Next`/fluxo, sÃ³ corrige o path de leitura para bater com a semÃ¢ntica real do `lambda:invoke`. Um `ResultSelector` mais limpo fica para quando os itens 6/7 forem implementados de verdade e o formato completo puder ser redesenhado junto.
- ComposiÃ§Ã£o `buildPdfParserTaskWorkerDeps`/`createRealPdfParserTaskWorkerClients` em `src/modules/extraction/composition/extraction.ts` â€” sÃ³ S3 + AppConfig como clientes reais (nada de DynamoDB/Textract/KMS/SFN).
- Handler real `src/runtime/aws/handlers/pdf-parser-task-handler.ts` â€” invocaÃ§Ã£o sÃ­ncrona Ãºnica (`handler(event) -> output`), `SecureLogger`/`runWithContext`, nunca loga texto OCR.
- Infra real em `infra/main.tf`: Lambda+role IAM (sÃ³ `s3:GetObject` no bucket `EXTRACTION_TRANSIENT` + `feature_flags_read_policy_json` â€” sem tabela DynamoDB, sem KMS, sem SNS/SQS), `aws_lambda_permission` para `states.amazonaws.com` jÃ¡ escopado Ã  ARN determinÃ­stica da state machine futura (mesmo padrÃ£o do item 4). Novo output `pdf_parser_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"pdf-parser-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 28â†’29 funÃ§Ãµes (3 asserts: nomes, distinÃ§Ã£o, `lambda_published_versions`).
- 18 testes novos (812 totais no backend, era 794): `test/unit/extraction/deterministic-field-parser.test.ts` (10, incluindo dedup por valor, ambiguidade de 2+ candidatos, truncamento no limite de M6, combinaÃ§Ãµes dia/mÃªs impossÃ­veis) e `test/unit/extraction/run-deterministic-parser.test.ts` (8, incluindo caminho feliz, caminho degradado nunca fabricando candidato, fail-closed do `AI_EXTRACTION`, falha de leitura/parse do artefato levantando `DeterministicParserFailedError`).
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 8 recursos novos (0 erros) quanto full-stack sem target (9 a criar, 56 a mudar â€” sÃ³ republish de versÃ£o prÃ©-existente em todas as Lambdas, mesmo ruÃ­do que o item 4 jÃ¡ documentou; 1 destroy-and-recreate em `aws_lambda_permission.textract_task_from_state_machine` â€” drift **prÃ©-existente do item 4**, nÃ£o introduzido nesta sessÃ£o, causado por uma normalizaÃ§Ã£o de `function_name` com/sem qualificador `:live` que o provider AWS jÃ¡ detectava antes desta sessÃ£o comeÃ§ar; nÃ£o corrigido aqui por estar fora do escopo do item 5, registrado para quem tocar o item 3/ASL wiring depois). **Nunca `terraform apply` local**.

**O que faltava depois do item 6 â€” ambos concluÃ­dos em sessÃµes subsequentes**:
1. ~~Item 3 (ASL) precisa ser instantiado de verdade~~ â€” **CONCLUÃDO 2026-08-26**, ver "Item 3 â€” CONCLUÃDO nesta sessÃ£o" abaixo.
2. ~~Item 7 (`ExtractionValidationTaskHandler`)~~ â€” **CONCLUÃDO 2026-08-26**, ver seÃ§Ã£o dedicada abaixo.

## Item 6 (`BedrockExtractionTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

Mesmo padrÃ£o do item 5 (`arn:aws:states:::lambda:invoke` plano, sem `waitForTaskToken` â€” sem task token, sem SQS, sem `SendTaskSuccess/Failure`), mas com DynamoDB (quota) e o cliente Bedrock Runtime a mais.

**Achado real corrigido durante a implementaÃ§Ã£o, na prÃ³pria ASL** (nÃ£o uma decisÃ£o de produto nova): o retorno de `RunDeterministicParser` (item 5, tambÃ©m um `lambda:invoke` simples) nunca tinha `OutputPath`/`ResultSelector`, entÃ£o `$` depois desse Task virava o wrapper bruto `{ExecutedVersion, Payload, StatusCode}` â€” os `Choice` states `NeedsBedrock`/`CheckAiKillSwitch` jÃ¡ liam `$.Payload.needsBedrock`/`$.Payload.aiExtractionEnabled` corretamente (fix do item 5), mas o prÃ³prio `RunBedrock` (`Payload.$: "$"`) enviaria esse wrapper INTEIRO para `BedrockExtractionTaskHandler`, nÃ£o o `RunDeterministicParserOutput` limpo. Corrigido: `RunDeterministicParser` ganhou `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"`, que descarta o wrapper `lambda:invoke` â€” `NeedsBedrock`/`CheckAiKillSwitch` foram atualizados para ler `$.needsBedrock`/`$.aiExtractionEnabled` diretamente (nÃ£o mais `$.Payload.*`), e `RunBedrock`'s `Payload.$: "$"` agora envia exatamente `RunBedrockExtractionInput` (que espelha `RunDeterministicParserOutput`). `ValidateSchema` (item 7, ainda stub) vai precisar do mesmo tratamento quando for implementado de verdade.

**Segundo achado real, mesma causa raiz**: `RunDeterministicParserOutput` nunca carregava o `artifact: ExtractionArtifactRef` de volta â€” sem isso, `BedrockExtractionTaskHandler` nÃ£o teria como saber onde ler o texto OCR (o design Â§1.9 exige `textArtifact` no request, nunca uma segunda leitura via Textract). Corrigido: `runDeterministicParser()` agora reanexa `input.artifact` no output quando `ocrAvailable`, mesma disciplina de reanexaÃ§Ã£o de contexto que `completeOcr` (item 4/5) jÃ¡ tinha estabelecido para `tenantId`/`itemId`/etc.

**Real (cÃ³digo + infra, nÃ£o sÃ³ camada pura)**:
- DomÃ­nio novo `src/modules/extraction/domain/bedrock-extraction.ts` â€” `BEDROCK_SYSTEM_PROMPT_V1`/`BEDROCK_SYSTEM_PROMPT_VERSION` (constante versionada e imutÃ¡vel, nunca concatenada com input do usuÃ¡rio), `buildSubmitExtractionToolSchema()` (schema fechado, `additionalProperties: false`, gerado a partir de `FIELD_SCHEMA_V1` â€” nunca hardcoded separadamente do schema real), `buildUserMessageText()` (bloco `<untrusted_document_text>` com truncamento em `BEDROCK_MAX_ARTIFACT_CHARS`), `BedrockExtractionRequest` (sÃ³ `textArtifact` â€” tipo local `BedrockTextArtifactRef`, estruturalmente idÃªntico a `ExtractionArtifactRef` do port mas declarado localmente porque `dependency-cruiser` proÃ­be domÃ­nio importar de `ports/`).
- Porta nova `src/modules/extraction/ports/bedrock-client.ts` (`BedrockClient.extract()`) â€” nenhum import do SDK Bedrock fora do adapter.
- AplicaÃ§Ã£o nova `src/modules/extraction/application/run-bedrock-extraction.ts` (`runBedrockExtraction()`) â€” re-checa `AI_EXTRACTION` fail-closed (defesa em profundidade, o `Choice` da ASL jÃ¡ filtra isso mas a chamada paga nunca confia sÃ³ nisso), reserva `AI_CALL` idempotente (`runId|BEDROCK`, mesmo padrÃ£o de `runId|TEXTRACT` do item 4), chama `BedrockClient.extract()`, compensa a quota (`release()`) se a chamada falhar, nunca fabrica candidato quando nÃ£o hÃ¡ artefato (caminho totalmente degradado retorna `bedrockFields: []`). Consome exatamente o que `runDeterministicParser()` produz â€” nunca reimplementa `needsBedrock()`.
- Adapter real `src/modules/extraction/persistence/bedrock-runtime-client.ts` (`BedrockRuntimeConverseClient`) â€” Converse API real (`@aws-sdk/client-bedrock-runtime@3.1114.0`, adicionado ao `package.json`), `toolChoice` forÃ§ado a `submit_extraction`, `temperature: 0`, `maxTokens: BEDROCK_MAX_OUTPUT_TOKENS`, valida a resposta do tool-call contra o schema fechado derivado de `getFieldSchema()` (rejeita chave extra, campo obrigatÃ³rio ausente, tipo errado, confidence fora de `[0,1]`, ausÃªncia de tool-call, tool errado) â€” qualquer falha vira `BedrockExtractionFailedError`, nunca um resultado parcial silencioso.
- Taxonomia de erro nova em `app-error.ts`: `AiExtractionDisabledError`/`BedrockExtractionFailedError`.
- ComposiÃ§Ã£o `buildBedrockExtractionTaskWorkerDeps`/`createRealBedrockExtractionTaskWorkerClients` em `extraction.ts` â€” DynamoDB (quota) + S3 (artefato) + AppConfig + `BedrockRuntimeClient` (regiÃ£o configurÃ¡vel via `bedrock_region`, distinta da regiÃ£o do stack).
- Handler real `src/runtime/aws/handlers/bedrock-extraction-task-handler.ts` â€” invocaÃ§Ã£o sÃ­ncrona Ãºnica, `SecureLogger`/`runWithContext`, nunca loga texto de documento/prompt/resposta do modelo.
- **Modelo/regiÃ£o Bedrock: configurÃ¡veis, com default obviamente placeholder** (`var.bedrock_model_id = "PLACEHOLDER_BEDROCK_MODEL_ID_NOT_SELECTED"`, `var.bedrock_region = "us-east-1"`) â€” design Â§4 bloqueia sÃ³ a escolha/validaÃ§Ã£o real para produÃ§Ã£o, nunca a testabilidade em `dev`; a permissÃ£o IAM jÃ¡ Ã© escopada ao ARN pattern do placeholder (`bedrock:InvokeModel`/`bedrock:Converse` em `arn:aws:bedrock:<region>::foundation-model/<model_id>`), nunca `"*"`.
- Infra real em `infra/main.tf`: Lambda+role IAM (`module.table.tenant_facing_read_write_policy_json` para a quota â€” mesmo padrÃ£o do item 4 â€”, `feature_flags_read_policy_json`, `s3:GetObject` sÃ³ leitura no bucket `EXTRACTION_TRANSIENT` â€” nunca escrita, mesma disciplina de blast-radius do item 5 â€”, `bedrock:InvokeModel`/`bedrock:Converse` escopados), sem VPC, sem acesso a Textract/outros serviÃ§os. `aws_lambda_permission` para `states.amazonaws.com` jÃ¡ escopado Ã  ARN determinÃ­stica da state machine futura. Novo output `bedrock_extraction_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"bedrock-extraction-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 29â†’30 funÃ§Ãµes (3 asserts) + `infra/outputs.tf`.
- 18 testes novos (830 totais no backend, era 812): `test/unit/extraction/run-bedrock-extraction.test.ts` (6 â€” caminho feliz, fail-closed do kill switch em duas variantes, caminho degradado sem artefato, compensaÃ§Ã£o de quota em falha, **14Âº caso adversarial de custo-abuso**: execuÃ§Ã£o retentada/duplicada do mesmo `runId` reserva contra a prÃ³pria janela `runId|BEDROCK` anterior, nunca dobra chamadas reais por retry) e `test/unit/extraction/bedrock-runtime-client.test.ts` (12 â€” corpus adversarial do adapter: sem tool-call, tool errado, chave extra no schema fechado, campo obrigatÃ³rio ausente, tipo errado, confidence fora de faixa, falha da prÃ³pria chamada Converse, falha de leitura do artefato antes de gastar a chamada, texto do documento (incluindo uma tag de fechamento falsificada tentando "escapar" do bloco untrusted) nunca aparece fora do bloco `user`/nunca influencia o parsing alÃ©m do que o tool-call estruturado diz, `system` sempre a constante fixa, `toolChoice` sempre forÃ§ado, `temperature`/`maxTokens` sempre as constantes de design). Um corpus adversarial "de 13 casos do Codex" citado no design (Â§1.11) nÃ£o foi encontrado verbatim em nenhum arquivo sob `docs/architecture/reviews/m7-extraction-design/` (procurado antes de escrever os testes) â€” o corpus construÃ­do cobre as mesmas classes de ameaÃ§a descritas ali (injeÃ§Ã£o via conteÃºdo do documento, tool errado/nenhum tool, estouro de schema/token, resposta malformada, mais o 14Âº caso de custo-abuso) a partir de princÃ­pios, documentado no comentÃ¡rio de topo de cada arquivo de teste.
- `typecheck`/`lint`/`check-boundaries`/`test`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan` real contra `dev` (perfil `claude-dev`) tanto `-target=` nos 10 recursos novos do handler (0 erros) quanto full-stack sem target (19 a criar â€” os do item 5 ainda nÃ£o aplicado + os do item 6 â€”, 56 a mudar â€” mesmo ruÃ­do de republish de versÃ£o jÃ¡ documentado nos itens 4/5 â€”, 1 destroy-and-recreate â€” o MESMO drift prÃ©-existente do item 4/5 em `aws_lambda_permission.textract_task_from_state_machine`, nÃ£o introduzido nesta sessÃ£o). **Nunca `terraform apply` local**.
- **DecisÃ£o pendente registrada, nÃ£o bloqueante**: nenhum campo do design nomeia hoje um segundo campo alÃ©m de `expirationDate`, entÃ£o o schema fechado do tool `submit_extraction` sÃ³ tem essa uma propriedade â€” `buildSubmitExtractionToolSchema()` jÃ¡ Ã© genÃ©rico sobre `FIELD_SCHEMA_V1`, entÃ£o um campo novo no schema nÃ£o precisa de mudanÃ§a de cÃ³digo aqui, sÃ³ uma nova versÃ£o de `pipelineVersion`/`thresholdVersion` (mesma disciplina de `field-schema.ts`).

## Item 7 (`ExtractionValidationTaskHandler`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

Fecha as ASL states 8-12 (`ValidateSchema`/`CompareExtractors`/`PersistExtractedFields`/`MarkPendingConfirmation`/`CompleteRun`) â€” todas as cinco invocam o MESMO Lambda com um `operation` distinto no payload (mantidas como Task states separados no ASL para auditoria/`Catch` por estÃ¡gio, design Â§2, nunca colapsadas). Consumidor real dos outputs dos itens 5/6 (`RunDeterministicParserOutput`/`RunBedrockExtractionOutput`).

**A decisÃ£o mais importante desta sessÃ£o â€” a limpeza do artefato transitÃ³rio (design Â§3, "achado mais sÃ©rio")**: `OcrArtifactStore.delete()` Ã© chamado em EXATAMENTE dois lugares: `completeRunStage()` (caminho normal, depois que `PersistExtractedFields` jÃ¡ commitou o resultado no banco) e `markPendingConfirmationStage()` (caminho de falha catastrÃ³fica do parser determinÃ­stico, que nunca passa por `PersistExtractedFields`/`CompleteRun` â€” design cita esse estado explicitamente como ponto de deleÃ§Ã£o "em caminho FAILED"). `validateSchema()`/`compareExtractorsStage()`/`persistExtractedFieldsStage()` NUNCA tocam `delete()` â€” rodam antes do run alcanÃ§ar um estado terminal, e uma retentativa de qualquer uma delas precisa poder reler o artefato. Teste dedicado (`run-extraction-validation.test.ts`, describe "governing invariant") prova isso rodando a sequÃªncia completa via `runExtractionValidation()` e contando chamadas de `delete()` depois de cada estado â€” zero atÃ© `CompleteRun`/`MarkPendingConfirmation`, exatamente uma ali.

**DomÃ­nio novo** (`src/modules/extraction/domain/`):
- `validate-field-value.ts` â€” `isValidFieldValue(valueType, value)`: `DATE` exige `yyyy-mm-dd` E que a data seja calendarmente vÃ¡lida (rejeita `2027-13-40`), `NUMBER` exige `Number.isFinite`, `STRING` exige nÃ£o-vazio. Um candidato invÃ¡lido nunca Ã© descartado silenciosamente â€” Ã© marcado `valid: false` e tratado como "essa fonte nÃ£o produziu nada" na comparaÃ§Ã£o.
- `compare-extractors.ts` â€” `compareExtractors(candidates)`: 0 candidatos usÃ¡veis â†’ `SINGLE_SOURCE` sem `candidateValue` (caso degenerado que `ExtractionAgreement` â€” jÃ¡ fechado no item 0 como `SINGLE_SOURCE\|MATCH\|MISMATCH` â€” nÃ£o representa separadamente; decisÃ£o registrada aqui em vez de alargar o tipo jÃ¡ commitado). 1 candidato â†’ `SINGLE_SOURCE` com valor. 2+ concordando â†’ `MATCH` (confianÃ§a = a maior das duas). 2+ discordando â†’ `MISMATCH` (nunca resolve automaticamente escolhendo um "vencedor" â€” `candidateValue` carrega sÃ³ o de maior confianÃ§a, para exibiÃ§Ã£o, mas Ã© `agreement` que decide o roteamento).
- `decide-field-outcome.ts` â€” `decideFieldOutcome(comparison)`: sem `candidateValue` â†’ `PENDING_CONFIRMATION`; `MISMATCH` â†’ `PENDING_CONFIRMATION` sempre; `SINGLE_SOURCE` com confianÃ§a indefinida ou abaixo de `DETERMINISTIC_CONFIDENCE_THRESHOLD` (0.75) â†’ `PENDING_CONFIRMATION`; senÃ£o (`MATCH`, ou `SINGLE_SOURCE` confiante) â†’ `CONFIRMED` com `confirmedValue` = o candidato. **Nunca retorna `REJECTED`** â€” esse estado sÃ³ Ã© alcanÃ§Ã¡vel pela rota HTTP de rejeiÃ§Ã£o humana (item 8, ainda nÃ£o implementada).

**DecisÃ£o de arquitetura tomada nesta sessÃ£o (nÃ£o estava no design, resolvida por inferÃªncia direta do enum jÃ¡ commitado)**: `ExtractionRunStatus` (`RUNNING\|COMPLETED\|FAILED\|DISCARDED`) nÃ£o tem um estado "aguardando confirmaÃ§Ã£o" separado â€” entÃ£o a distinÃ§Ã£o "run completa vs. precisa de revisÃ£o humana" vive inteiramente no nÃ­vel de CADA `ExtractedField` (campo `state`), nunca no `ExtractionRun` em si. Isso resolve a pergunta "o run chega a `COMPLETED` imediatamente, ou vai para `MarkPendingConfirmation`" da seguinte forma: no caminho normal (schema vÃ¡lido â†’ comparaÃ§Ã£o â†’ persistÃªncia), o run SEMPRE termina em `CompleteRun`/`status: COMPLETED`, mesmo que um ou mais campos individuais fiquem `PENDING_CONFIRMATION` (sinalizado por `requiresReview: true` no contexto de saÃ­da, hoje sÃ³ para log/observabilidade â€” nenhuma rota HTTP consome esse campo ainda, ver item 8). `MarkPendingConfirmation`/`status: FAILED` Ã© reservado exclusivamente para quando `RunDeterministicParser` falha catastroficamente ANTES de qualquer candidato existir (nenhum `ValidateSchema`/`CompareExtractors`/`PersistExtractedFields` chega a rodar nesse caminho â€” o prÃ³prio `MarkPendingConfirmation` monta e persiste os `ExtractedField` do schema, todos `PENDING_CONFIRMATION` sem candidato, E apaga o artefato, tudo numa Ãºnica operaÃ§Ã£o, porque Ã© o Ãºnico passo que esse ramo do ASL executa). Se essa leitura divergir do que o Marcelo pretendia ao aprovar o design original, Ã© uma decisÃ£o de produto pequena e reversÃ­vel (mudar sÃ³ a decisÃ£o de estado, nÃ£o a mecÃ¢nica de comparaÃ§Ã£o/persistÃªncia) â€” registrado aqui para reabrir se necessÃ¡rio.

**Descarte por exclusÃ£o concorrente (design Â§3)**: implementado via `DocumentReader.get()` (leitura fresca do `Document` logo antes de persistir) + um `ConditionCheck` (`buildVersionConditionCheck`) dentro do MESMO `TransactWriteItems` que grava os `ExtractedField`/atualiza o `ExtractionRun`, reasserindo que a versÃ£o do `Document` lida nÃ£o mudou entre a leitura e o commit (fecha o TOCTOU real, nÃ£o sÃ³ um "ler status DELETED e torcer"). Se o `Document` jÃ¡ estava `DELETED` no momento da leitura, nem chega a montar a transaÃ§Ã£o â€” chama `ExtractionRunStore.updateStatus(..., "DISCARDED", ...)` direto, sem persistir nenhum `ExtractedField`. Se a condiÃ§Ã£o falhar dentro da transaÃ§Ã£o (mudanÃ§a concorrente entre a leitura e o commit), `commitRunOutcome()` retorna `"DOCUMENT_DISCARDED"` (tratamento uniforme de qualquer cancelamento da transaÃ§Ã£o como descarte â€” decisÃ£o conservadora documentada no comentÃ¡rio do adapter, jÃ¡ que distinguir a causa exata do cancelamento por cÃ³digo de erro do SDK seria frÃ¡gil entre versÃµes).

**Ports/persistÃªncia**:
- `ports/extracted-field-store.ts` (`ExtractedFieldStore`, novo) â€” expÃµe SÃ“ `commitRunOutcome()`, nunca um `put`/`batchPut` avulso: grava todos os `ExtractedField` do run + atualiza `ExtractionRun.status` + o `ConditionCheck` do `Document` num Ãºnico `TransactWriteItems` (tudo ou nada). `DynamoDbExtractedFieldStore` Ã© o adapter real, usando `buildVersionedCreate`/`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts` (nunca `PutItem`/`UpdateItem` cru).
- `ports/extraction-run-store.ts` ganhou `updateStatus(key, tenantId, expectedVersion, "DISCARDED", completedAt)` â€” mÃ©todo standalone, usado SÃ“ no caminho de descarte (onde nÃ£o hÃ¡ nenhum `ExtractedField` para persistir junto). `DynamoDbExtractionRunStore` implementado com `UpdateCommand`+`buildVersionedUpdate`.
- `ports/ocr-artifact-store.ts` ganhou `delete()` â€” **mudanÃ§a de contrato deliberada**: o item 4 tinha deixado esse mÃ©todo deliberadamente FORA do port ("a exclusÃ£o do artefato Ã© exclusivamente de `ExtractionValidationTaskHandler`, item 7, nunca deste handler" â€” tornava a exclusÃ£o acidental do chamador errado estruturalmente impossÃ­vel). Agora adicionado porque este Ã‰ o chamador certo â€” `S3OcrArtifactStore.delete()` usa `DeleteObjectCommand` (idempotente na prÃ³pria API do S3, deletar uma key ausente nÃ£o lanÃ§a). Os testes que antes afirmavam "o port nem expÃµe delete" (`complete-ocr.test.ts`) foram reescritos para provar a invariante de outra forma (espiona `artifacts.delete` e conta chamadas, jÃ¡ que o mÃ©todo agora existe no tipo mas `completeOcr` nunca deve chamÃ¡-lo).

**AplicaÃ§Ã£o** (`src/modules/extraction/application/run-extraction-validation.ts`): um dispatcher `runExtractionValidation(deps, operation, input)` sobre cinco funÃ§Ãµes (`validateSchema`/`compareExtractorsStage`/`persistExtractedFieldsStage`/`markPendingConfirmationStage`/`completeRunStage`), todas operando sobre um Ãºnico `ValidationContext` que evolui atravÃ©s dos cinco `operation`s. `persistExtractedFieldsStage`/`markPendingConfirmationStage` compartilham um helper `commitOrDiscard()` (nÃ£o exportado) que encapsula a leitura do `Document`+decisÃ£o discard-ou-commit. Um erro genuÃ­no de commit (nÃ£o um descarte) vira `ExtractionCommitFailedError` (novo em `app-error.ts`, `retryable: true` â€” o `Catch`/`Retry` da ASL trata como transitÃ³rio).

**Achado real corrigido nesta sessÃ£o, em cÃ³digo do item 6 jÃ¡ commitado**: `RunBedrockExtractionOutput` nunca reanexava `extractedFields`/`artifact`/`needsBedrock`/`aiExtractionEnabled` â€” a MESMA classe de bug que o item 5 jÃ¡ tinha corrigido uma vez para `completeOcr`'s success payload (design Â§3's "achado mais sÃ©rio" Ã© sobre limpeza de artefato, mas essa categoria de "output nÃ£o reanexa o contexto anterior" jÃ¡ apareceu 3 vezes agora: item 4â†’5, item 5â†’6, e este item 6â†’7). Corrigido de forma aditiva (campos novos sÃ³ ecoam o que jÃ¡ vinha em `input`, sem recomputar nada) â€” os testes existentes de `run-bedrock-extraction.test.ts` continuam passando sem alteraÃ§Ã£o (nÃ£o fazem `toEqual` no objeto inteiro). **PadrÃ£o a vigiar em qualquer implementaÃ§Ã£o futura de um novo Task state que consome o output de outro**: sempre conferir se o output reanexa TUDO que o prÃ³ximo estado vai precisar, nunca assumir que "sÃ³ preciso retornar o que eu calculei".

**ASL atualizada** (`infra/state-machines/document-extraction.asl.json`): os cinco estados-stub agora sÃ£o Task states reais com `ResultSelector: {"Payload.$": "$.Payload"}` + `OutputPath: "$.Payload"` (mesmo padrÃ£o de desembrulho de `RunDeterministicParser`/`RunBedrock`) e blocos `Retry` (`Lambda.ServiceException`/`Lambda.AWSLambdaException`/`Lambda.SdkClientException`, mais `ExtractionCommitFailed` em `PersistExtractedFields`/`MarkPendingConfirmation`). `RunBedrock` tambÃ©m ganhou `ResultSelector`/`OutputPath` (nÃ£o tinha nenhum antes â€” outro sintoma do mesmo achado acima).

**Infra real** (`infra/main.tf`): `module.extraction_validation_task_handler` â€” o footprint mais estreito dos quatro Lambdas de extraÃ§Ã£o: `module.table.tenant_facing_read_write_policy_json` (leitura de `Document`, escrita de `ExtractedField`/`ExtractionRun`) + uma policy NOVA e estritamente `s3:DeleteObject` (nunca `GetObject`/`PutObject`) no bucket `EXTRACTION_TRANSIENT` â€” este Ã© o Ãºnico Lambda do pipeline inteiro com permissÃ£o de deletar lÃ¡. Sem Textract/Bedrock/VPC/KMS/SNS/SQS/Step Functions client/AppConfig. Uma Ãºnica `aws_lambda_permission` cobre as cinco invocaÃ§Ãµes (todas vÃªm do mesmo `states.amazonaws.com`/mesma state machine ARN). Novo output `extraction_validation_task_handler_function_arn`. `scripts/build-lambdas.ts` ganhou `"extraction-validation-task-handler"`. `infra/tests/stack.tftest.hcl` atualizado de 30â†’31 funÃ§Ãµes (3 asserts).

**VerificaÃ§Ã£o real**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. 53 testes novos (867 totais no backend, era 830 no inÃ­cio da sessÃ£o â€” nÃ£o os "920" mencionados por engano na mensagem do primeiro commit desta sessÃ£o, nÃºmero incorreto de digitaÃ§Ã£o, corrigido aqui): `validate-field-value.test.ts` (8), `compare-extractors.test.ts` (6), `decide-field-outcome.test.ts` (7), `run-extraction-validation.test.ts` (16, incluindo o teste de invariante de deleÃ§Ã£o do artefato descrito acima), mais 16 testes prÃ©-existentes ajustados (fakes que precisaram ganhar `delete()`/`updateStatus()` para continuar implementando as interfaces estendidas). `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 13/13 passando, `terraform plan -target=` real contra `dev` (perfil `claude-dev`) para os 8 recursos novos: 8 a criar, 0 a mudar, 0 a destruir. Plano full-stack sem target: 27 a criar/56 a mudar/1 a destruir â€” o Ãºnico destroy Ã© o MESMO drift prÃ©-existente do item 4 em `aws_lambda_permission.textract_task_from_state_machine` (documentado nas sessÃµes dos itens 5/6, nÃ£o introduzido aqui). **Nunca `terraform apply` local**.

**DecisÃ£o de produto pendente registrada, nÃ£o bloqueante**: nenhuma rota HTTP hoje consome `requiresReview`/`ExtractedField.state === PENDING_CONFIRMATION` â€” isso Ã© o item 8 (rotas de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7), ainda nÃ£o implementado. AtÃ© lÃ¡, um `ExtractedField` `PENDING_CONFIRMATION` existe no banco mas nÃ£o hÃ¡ nenhuma forma de um operador vÃª-lo ou agir sobre ele pela API.

**Item 3 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26): a state machine real foi instanciada, plan-verificada contra `dev`**: `module "extraction_workflow"` agora Ã© chamado de `infra/main.tf`, passando as 4 ARNs `local.*_function_arn` jÃ¡ existentes (items 4-7) e uma role de execuÃ§Ã£o dedicada (`aws_iam_role.extraction_workflow_state_machine`). O nome do state machine resolve exatamente para `exptrk-dev-document-extraction` â€” o mesmo nome que `local.extraction_state_machine_arn` jÃ¡ esperava desde o item 2, verificado por `terraform test` (nÃ£o confiado sÃ³ Ã  leitura do cÃ³digo).

- **IAM da role de execuÃ§Ã£o do prÃ³prio state machine** (`infra/main.tf`, `data.aws_iam_policy_document.extraction_workflow_invoke_lambdas`, 3 statements): `lambda:InvokeFunction` escopado Ã s 4 ARNs exatas (nunca wildcard); `logs:CreateLogDelivery`/`GetLogDelivery`/`UpdateLogDelivery`/`DeleteLogDelivery`/`ListLogDeliveries`/`PutResourcePolicy`/`DescribeResourcePolicies`/`DescribeLogGroups` em `"*"` (exigÃªncia documentada da prÃ³pria AWS para `logging_configuration` de state machine â€” opera no subsistema de log delivery a nÃ­vel de conta, nÃ£o num log group especÃ­fico); `xray:PutTraceSegments`/`PutTelemetryRecords`/`GetSamplingRules`/`GetSamplingTargets` em `"*"` (mesmas aÃ§Ãµes que `AWSXRayDaemonWriteAccess`, que o mÃ³dulo `lambda-function` jÃ¡ anexa a toda funÃ§Ã£o com tracing ativo). O lado inverso (cada handler autorizando `states.amazonaws.com` a invocÃ¡-lo, escopado Ã  ARN determinÃ­stica do state machine) jÃ¡ estava pronto desde os itens 4-7 â€” nÃ£o precisou de nenhum ajuste.
- **CloudWatch Logs + X-Ray** (`infra/modules/extraction-workflow/main.tf`): `aws_cloudwatch_log_group.document_extraction` (`/aws/vendedlogs/states/exptrk-dev-document-extraction`, retenÃ§Ã£o 30 dias, mesmo padrÃ£o de `document-observability`/`import-observability`) + `logging_configuration` no nÃ­vel `ERROR` (nÃ£o `ALL`) com `include_execution_data = false` â€” decisÃ£o deliberada espelhando a disciplina de privacidade do prÃ³prio `EXTRACTION_TRANSIENT` (o input/output de uma execuÃ§Ã£o pode carregar texto OCR/valores de campo extraÃ­dos; nunca devem cair num log group com retenÃ§Ã£o/acesso mais frouxos que o bucket do artefato). `tracing_configuration { enabled = true }` â€” todo Lambda do repo jÃ¡ roda com ADOT/X-Ray ativo (AGENTS.md Â§7, M5); o state machine que os orquestra agora participa do mesmo trace.
- **Disciplina de gate (D-035 Â§1.6)**: NADA nesta camada foi condicionado a `var.extraction_pipeline_enabled` â€” o state machine, sua role e as 4 permissÃµes de invocaÃ§Ã£o sempre existem (inspecionÃ¡veis/deployÃ¡veis). RaciocÃ­nio registrado no comentÃ¡rio do prÃ³prio `main.tf`: um segundo gate aqui seria redundante, porque o item 2 jÃ¡ gateia o ÃšNICO ponto de entrada real (a regra EventBridge que liga o bucket limpo do M6 a `ExtractionStarterWorker`, que Ã© quem chama `StartExecution`) â€” com esse gate em `false` (default), o state machine pode existir/ser inspecionado/atÃ© ser iniciado manualmente para uma execuÃ§Ã£o de teste, mas nunca recebe trÃ¡fego real.
- **`terraform test`**: `infra/tests/stack.tftest.hcl` ganhou 2 `run` novos (nomes plan-time-known do state machine batendo com `local.extraction_state_machine_arn`, IAM da role com exatamente 3 statements nunca wildcard) â€” 13â†’15 passando. MÃ³dulo `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` **criado do zero** (pendÃªncia explÃ­cita registrada pelo item 7) â€” 5 `run` via `mock_provider`+`apply` (substituiÃ§Ã£o dos 4 placeholders ASL pelas ARNs reais, nunca deixando um placeholder para trÃ¡s; `type = STANDARD`; role passada atravÃ©s sem modificaÃ§Ã£o; tracing/logging configurados) â€” 5/5 passando.
- **`terraform plan` real contra `dev`** (perfil `claude-dev`, conta `975707451904`) â€” **a verificaÃ§Ã£o crÃ­tica**: `-target=module.extraction_workflow -target=aws_iam_role.extraction_workflow_state_machine -target=aws_iam_role_policy.extraction_workflow_state_machine` â†’ **27 a criar, 2 a mudar, 0 erros** (nenhum "invalid ARN"/"resource not found" â€” a definiÃ§Ã£o do state machine, que embute as 4 ARNs Lambda reais via `replace()` encadeado do mÃ³dulo, resolveu sem erro contra a API real da AWS, prova de que o item 3 finalmente funciona). Plano full-stack sem target: **31 a criar / 56 a mudar / 1 a destruir** â€” o Ãºnico destroy Ã© o MESMO drift prÃ©-existente do item 4 (`aws_lambda_permission.textract_task_from_state_machine`, normalizaÃ§Ã£o de `function_name` com/sem qualificador `:live`), jÃ¡ documentado nas sessÃµes dos itens 5/6/7, nÃ£o introduzido aqui. Nenhuma issue de integraÃ§Ã£o nova apareceu (os placeholders `"textract-task:live"` etc. substituÃ­ram corretamente â€” nÃ£o foi necessÃ¡rio nenhum fix no `replace()` do mÃ³dulo). **Nunca `terraform apply` local.**
- **Backend**: `typecheck`/`lint`/`validate-schemas`/`check-docs` limpos, 867/867 testes de backend passando (nenhuma regressÃ£o â€” sessÃ£o nÃ£o tocou cÃ³digo de aplicaÃ§Ã£o, sÃ³ infra). `terraform fmt -check -recursive` limpo em toda a Ã¡rvore `infra/`.
- Novos outputs em `infra/outputs.tf`: `extraction_state_machine_arn` (deve ser igual a `local.extraction_state_machine_arn`) e `extraction_state_machine_name` (plan-time-known, usado pelos testes para nÃ£o precisar de `command = apply` contra o provider real). Novo output no mÃ³dulo: `log_group_arn`, `state_machine_name`.
- Commits nesta sessÃ£o (branch `develop`, PR #60 `developâ†’main` ainda aberto â€” push deve atualizÃ¡-lo automaticamente): instanciaÃ§Ã£o do mÃ³dulo + IAM + logging/tracing + testes + docs.

**Item 2 â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: `ExtractionStarterWorker` completo â€” domÃ­nio/aplicaÃ§Ã£o/persistÃªncia/handler real, nÃ£o sÃ³ camada pura. `src/modules/extraction/{ports,application,persistence}/` (`DocumentReader`/`ExtractionRunStore`/`ExtractionExecutionStarter` â€” portas prÃ³prias, nÃ£o reusa `DocumentStore` do mÃ³dulo document; `startExtractionRun()` idempotente: cria `ExtractionRun` via `putIfAbsent`, mas chama `startExecution` do Step Functions SEMPRE, mesmo quando o registro jÃ¡ existia â€” gated sÃ³ no registro criaria um run Ã³rfÃ£o se `startExecution` falhasse depois de `putIfAbsent` suceder, a dedup real Ã© a idempotÃªncia nativa do `StartExecution` por nome de execuÃ§Ã£o determinÃ­stico = `runId`), `src/runtime/aws/handlers/extraction-starter-handler.ts` (SQS, mesmo padrÃ£o S3â†’EventBridgeâ†’SQS do `upload-finalizer-handler`). **Achado real corrigido durante a implementaÃ§Ã£o**: a key do bucket limpo (`advance-after-evidence.ts`) sÃ³ tinha `clean/<tenantId>/<documentId>`, sem `itemId` â€” insuficiente para montar a PK de `Document` (`TENANT#t#ITEM#i`) a partir sÃ³ do evento S3. Corrigido estendendo o formato para `clean/<tenantId>/<itemId>/<documentId>` (mesma forma item-anchored da key de quarentena, `parseCleanKey()` novo espelhando `parseQuarantineKey()`) â€” mudanÃ§a na M6 (jÃ¡ em produÃ§Ã£o), mas segura: `cleanObject` Ã© sempre um valor opaco armazenado na entidade, nunca reconstruÃ­do/reparsed em nenhum outro lugar do cÃ³digo. Infra real em `infra/main.tf`: fila+DLQ+Lambda+IAM sempre existem (deployÃ¡veis/inspecionÃ¡veis), mas a regra EventBridge que liga o bucket limpo Ã  fila Ã© o ÃšNICO recurso condicionado a `var.extraction_pipeline_enabled` â€” com o gate `false` (default), nenhum evento real do fluxo M6 (que jÃ¡ roda continuamente em `dev`) chega ao worker. `local.extraction_state_machine_arn` Ã© determinÃ­stico a partir do nome que o item 3 DEVE usar (`${local.name_prefix}-document-extraction`) â€” jÃ¡ correto quando a state machine real for criada, sem placeholder para trocar depois. 8 testes novos (748 totais no backend), `terraform test` (13 do stack) passando, `terraform plan` real contra `dev` confirmou 12 recursos a criar com o gate `false` (zero regra EventBridge) e mais 5 com o gate `true` (a regra + dependÃªncias) â€” comportamento condicional verificado, nÃ£o sÃ³ lido no cÃ³digo. **Ainda sem PR/merge** (mesma lÃ³gica do item 1: mergeÃ¡vel isoladamente, mas ainda aguardando decisÃ£o de "quando" â€” ver abaixo).
3. ASL â€” camada pura (o arquivo `.asl.json` e o mÃ³dulo `extraction-workflow` uninstantiated) segue como estava, ver acima.
4. `TextractTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** (cÃ³digo+infra, nÃ£o sÃ³ camada pura) â€” ver a seÃ§Ã£o dedicada acima ("Item 4 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, arquivos tocados, decisÃµes tomadas (AppConfig direto via SDK, CMK nova dedicada, tÃ³pico SNS novo) e o que falta (item 3 instantiation + itens 5-7).
5. `PdfParserTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 5 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, arquivos tocados, decisÃµes tomadas (reanexaÃ§Ã£o de contexto ao payload de sucesso de `completeOcr`, correÃ§Ã£o do path `$.Payload.*` nas Choice states) e a decisÃ£o pendente registrada (sem heurÃ­stica de metadado de arquivo no caminho degradado).
6. `BedrockExtractionTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 6 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, decisÃµes tomadas (modelo/regiÃ£o placeholder configurÃ¡veis, correÃ§Ã£o de `ResultSelector`/`OutputPath` na ASL, reanexaÃ§Ã£o do `artifact` ao output do item 5) e o corpus adversarial construÃ­do.
7. `ExtractionValidationTaskHandler` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta** â€” ver a seÃ§Ã£o dedicada acima ("Item 7 â€” CONCLUÃDO nesta sessÃ£o") para o estado exato, decisÃµes tomadas (mapeamento COMPLETED/FAILED/DISCARDED do `ExtractionRunStatus` jÃ¡ commitado, invariante de deleÃ§Ã£o do artefato) e o que falta (item 8 ainda nÃ£o consome `PENDING_CONFIRMATION`).
8. **Rotas HTTP de confirmaÃ§Ã£o â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta**: ver seÃ§Ã£o dedicada "Item 8" logo abaixo.
9. Quota `AI_CALL` â€” **CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo, nada faltando**: ver seÃ§Ã£o dedicada "Item 9" logo abaixo.

**Item 3 (instanciar o `aws_sfn_state_machine` real) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26)**: ver "Item 3 â€” CONCLUÃDO nesta sessÃ£o" na seÃ§Ã£o M7 acima (topo do arquivo) para o detalhe completo â€” `infra/main.tf` wireando `module "extraction_workflow"` com as 4 ARNs reais + role de execuÃ§Ã£o dedicada (`lambda:InvokeFunction` escopado + CloudWatch Logs delivery + X-Ray), `infra/modules/extraction-workflow/tests/extraction_workflow.tftest.hcl` novo (5 testes), `infra/tests/stack.tftest.hcl` +2 testes, `terraform plan` real contra `dev` limpo. Item 3 da seÃ§Ã£o "pendÃªncias explÃ­citas" do design (job Textract "preso") fica como runbook a calibrar com volume real â€” nÃ£o bloqueia implementaÃ§Ã£o, sÃ³ produÃ§Ã£o real fora de `dev`.

## Item 8 (rotas HTTP de confirmaÃ§Ã£o/rejeiÃ§Ã£o, Â§1.7) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), real de ponta a ponta

`POST /items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm`\|`reject` â€” o Ãºnico trecho do M7 original que ainda faltava para o pipeline ficar code-complete em `dev`. Mesmo padrÃ£o idempotente+transacional de `ExpirationService.renewItem` (item-handlers.ts), adaptado para um caso de 4 entidades.

- **Application** `src/modules/extraction/application/confirm-reject-field.ts` (`confirmField`/`rejectField`) â€” idempotente via `IdempotencyStore` (mesmo `begin`/`complete`/`abort` de sempre, replay lÃª o `ExtractedField` jÃ¡ commitado em vez de guardar um `responseRef` estruturado). `confirm` Ã© um OCC de 4 vias (`ExtractedField`/`ExtractionRun`/`Document`/`ExpirationItem`) â€” lÃª os quatro, valida as 4 versÃµes (`ConflictError`/409 em qualquer uma), valida `field.state === "PENDING_CONFIRMATION"` e `isValidFieldValue()` (item 7's funÃ§Ã£o, reaproveitada) contra o `confirmedValue` recebido, ambos `BusinessRuleError`/422 (categoria `BUSINESS_RULE` **nova** em `app-error.ts` â€” primeiro 422 do projeto). `reject` Ã© OCC de 3 vias (nunca lÃª/toca `ExpirationItem` â€” nem o path o pede). 404 (`NotFoundError`) em qualquer uma das 4 entidades ausente.
- **Mapeamento campoâ†’atributo do item**: `ITEM_ATTRIBUTE_BY_FIELD_NAME` (sÃ³ `expirationDate` -> `dueDate` no schema v1) â€” quando o campo confirmado mapeia para um atributo conhecido de `ExpirationItem`, a transaÃ§Ã£o faz um `Update` real (recalculando `gsi1Keys()` com o `dueDate` novo); quando nÃ£o mapeia (nenhum campo do schema v1 cai nesse caso hoje, mas o design exige `expectedItemVersion` em TODO confirm, mesmo sem efeito colateral no item), a transaÃ§Ã£o faz um `ConditionCheck`-only no item em vez de inventar um `Update` sem `set`.
- **Ports estendidos** (nÃ£o recriados): `ExtractedFieldStore` (item 7) ganhou `get()`/`confirmField()`/`rejectField()` â€” `confirmField`/`rejectField` retornam `"VERSION_CONFLICT"` (nunca lanÃ§am) quando qualquer `ConditionCheck`/`Update` condicional falha dentro do `TransactWriteItems`, igual ao `commitRunOutcome` jÃ¡ existente trata `TransactionCanceledException` de forma uniforme. `ExtractionRunStore` (item 2) ganhou `get()` â€” um plain read eventualmente consistente, nunca usado pelos OCC writes jÃ¡ existentes. Port novo `ports/entity-reader.ts` (`EntityReader`, mesma forma exata de `DocumentReader` jÃ¡ existente) reaproveitado por `documents`/`items` na composition root â€” `DynamoDbDocumentStore`/`DynamoDbExpirationStore` jÃ¡ satisfazem a interface estruturalmente, nenhum adapter novo.
- **Adapter real** `DynamoDbExtractedFieldStore.confirmField`/`.rejectField` â€” `TransactWriteCommand` usando os builders de sempre (`buildVersionedUpdate`/`buildVersionConditionCheck` de `occ.ts`), nunca `UpdateItem`/`ConditionCheck` cru.
- **HTTP** `src/modules/extraction/http/extraction-handlers.ts` (`handleConfirmField`/`handleRejectField`) â€” mesma pipeline exata de `document-handlers.ts`/`item-handlers.ts` (`resolver.resolve` â†’ `authorize()` internamente via a application function â†’ `TenantQuotaService` `API_REQUEST` 100/60s â†’ schema Ajv â†’ chamada). AutorizaÃ§Ã£o: `extraction:confirm` (aÃ§Ã£o **jÃ¡ existia** na matriz desde uma sessÃ£o anterior â€” cobre os dois verbos HTTP, exatamente como o design pede). `STATUS_BY_CATEGORY` local ganhou `BUSINESS_RULE: 422` (as outras cÃ³pias desse mapa nos outros mÃ³dulos nÃ£o precisam, deixadas como estÃ£o).
- **Sem Lambda nova**: as duas rotas ficam sob o MESMO grupo `/items/{itemId}/documents*` e o MESMO Lambda `documents_handler` de sempre (que jÃ¡ tem `tenant_facing_read_write_policy_json` completo na tabela â€” zero IAM novo). `src/runtime/aws/handlers/documents-handler.ts` ganhou 2 `case` novos no dispatcher + `buildFieldConfirmationDeps()` (composition root novo em `src/runtime/aws/composition/extraction.ts`, reaproveitando o mesmo padrÃ£o de adapter `DynamoLike` que `ExpirationService` jÃ¡ usa para sua prÃ³pria `IdempotencyStore`, ao invÃ©s de reinventar).
- **Terraform**: `infra/modules/api-gateway/main.tf`'s `documents_routes` ganhou `confirm_field`/`reject_field` â€” o `source_arn` do `aws_lambda_permission.documents` jÃ¡ Ã© um wildcard (`/items/*/documents*`), cobre as rotas novas automaticamente. `terraform plan -target=module.api` real contra `dev` (perfil `claude-dev`, conta `975707451904`, `-var-file=env/dev.tfvars -var="aws_account_id=975707451904"`): **2 a criar** (exatamente as 2 rotas novas, confirmado por nome no plan) **16 a mudar** (mesmo ruÃ­do de republish de `source_code_hash` jÃ¡ documentado em toda sessÃ£o anterior deste milestone â€” o prÃ³prio `documents-handler` bundle mudou porque o cÃ³digo mudou, os outros 15 sÃ£o o mesmo drift de sempre), **0 a destruir**.
- **BFF allowlist**: `src/modules/bff/domain/proxy-allowlist.ts` ganhou as 2 rotas â€” nÃ£o repetido o gap histÃ³rico de `DocumentRequest`/BLOCKER-A.
- **Schemas** `schemas/api/{confirm,reject}-extracted-field-request.v1.json` â€” `confirm` exige as 4 `expected*Version` + `confirmedValue`, `additionalProperties: false` (nunca aceita atributo de item nem JSON Patch, por design). `reject` exige sÃ³ `expectedDocumentVersion`/`expectedRunVersion`/`expectedFieldVersion` (sem `expectedItemVersion` â€” reject nunca toca o item), `correctionReason` opcional. 7 casos novos em `test/contract/schemas.test.ts` (vÃ¡lido/invÃ¡lido dos dois, incluindo rejeitar `expectedItemVersion` no corpo de reject).
- **Testes**: `test/unit/extraction/confirm-reject-field.test.ts` (19 testes, fakes escritos Ã  mÃ£o sobre uma tabela DynamoDB em memÃ³ria com OCC real â€” `InMemoryTable.write()` sÃ³ aplica se a versÃ£o bate) â€” feliz confirm (item atualizado + versÃµes incrementadas), feliz reject (item intocado), idempotÃªncia de replay dos dois (sem re-executar a transaÃ§Ã£o), 409 individual para cada uma das 4/3 versÃµes esperadas, 422 de estado errado (confirm e reject) sem tocar o item, 422 de `confirmedValue` invÃ¡lido, 404 para cada uma das 4 entidades ausente, negaÃ§Ã£o de autorizaÃ§Ã£o sem membership. 2 fakes prÃ©-existentes (`run-extraction-validation.test.ts`, `start-extraction-run.test.ts`) precisaram sÃ³ de um `get()` stub para continuar implementando as interfaces estendidas.
- **VerificaÃ§Ã£o real**: 25 testes novos (**892 totais no backend**, era 867 no inÃ­cio da sessÃ£o). `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos. `terraform fmt -check -recursive` limpo, `terraform validate` limpo, `terraform test` 15/15 passando, `terraform plan -target=module.api` real contra `dev` confirmado (2 criar/16 mudar/0 destruir, ver acima). **Nunca `terraform apply` local.**

## Item 9 (quota `AI_CALL`) â€” CONCLUÃDO nesta sessÃ£o (2026-08-26), verificado a fundo

Auditados TODOS os caminhos de falha de `startOcr()` (item 4) e `runBedrockExtraction()` (item 6) depois de `quota.consume({ quotaType: "AI_CALL", ... })`:

- `startOcr`: `StartDocumentTextDetection` falhando (a chamada paga nunca aconteceu) -> `quota.release()` chamado antes de propagar `TextractUnsupportedDocumentError` (linha jÃ¡ existente, confirmado). `TextractJobPersistenceFailedError` (a chamada paga JÃ aconteceu, sÃ³ a persistÃªncia local do `TextractJob` falhou depois) -> **deliberadamente NÃƒO libera a quota** â€” investigado nesta sessÃ£o e confirmado correto, nÃ£o uma lacuna: como a reserva Ã© por-execuÃ§Ã£o (`window: "${runId}|TEXTRACT"`, `limit: 1`), uma retentativa da ASL para o MESMO run sempre bate em `QuotaExceededError` contra a prÃ³pria reserva anterior, que `startOcr` jÃ¡ trata genericamente como "jÃ¡ reservado, nÃ£o Ã© exaustÃ£o real" (linhas 86-90) â€” **independente de a reserva ter sido liberada ou nÃ£o**. Liberar aqui nÃ£o destravaria nada que jÃ¡ nÃ£o estivesse destravado, e liberar incorretamente sinalizaria "nenhuma chamada paga aconteceu ainda" quando uma jÃ¡ aconteceu.
- `runBedrockExtraction`: mesmo padrÃ£o exato â€” falha da chamada Bedrock em si (apÃ³s esgotar `callAttempts` locais) -> `quota.release()` chamado antes de propagar `BedrockExtractionFailedError` (linha jÃ¡ existente, confirmado). NÃ£o hÃ¡ nenhum caminho de falha PÃ“S-chamada-bem-sucedida neste handler (ao contrÃ¡rio do Textract, o resultado do Bedrock Ã© sÃ³ re-anexado ao payload de saÃ­da, nunca persistido localmente por este handler) â€” nÃ£o hÃ¡ uma segunda categoria de falha a auditar aqui.

**ConclusÃ£o**: a cobertura de `AI_CALL` via `TenantQuotaService.consume()`/`.release()` nos itens 4/6 estÃ¡ completa e correta â€” nenhum cÃ³digo novo necessÃ¡rio, nenhuma lacuna real encontrada. NÃ£o existe (nem o design exige) uma rota HTTP de consulta de uso/limite de `AI_CALL` para o operador â€” fora de escopo do design aprovado, nÃ£o registrado como pendÃªncia.

## M7 â€” estado final desta sessÃ£o: cÃ³digo-completo para `dev`

Com os itens 1-9 concluÃ­dos (1/2/4/5/6/7/8/9 de ponta a ponta real, item 3 instanciando a state machine real), **M7 estÃ¡ code-complete para testar em `dev`** â€” nÃ£o falta nenhum item da lista original de 10 (o item 10, "Infra Terraform completa", jÃ¡ estava dobrado dentro dos itens 4-7/3, confirmado nesta sessÃ£o: nÃ£o hÃ¡ nenhum mÃ³dulo/recurso do pipeline de extraÃ§Ã£o pendente de instanciaÃ§Ã£o). O Ãºnico gate real que falta antes de considerar M7 encerrado:

- ~~Uma verificaÃ§Ã£o end-to-end real~~ â€” **FEITA em 2026-08-27**, ver a seÃ§Ã£o `## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)` logo abaixo. Encontrou **3 bugs reais bloqueantes** (2 corrigidos nesta sessÃ£o, 1 documentado como decisÃ£o pendente).
- Runbook de calibraÃ§Ã£o para job Textract "preso" (mencionado no item 3) â€” nÃ£o bloqueia, Ã© operacional.

## M7 â€” verificaÃ§Ã£o end-to-end real em `dev` (2026-08-27)

**O que foi exercitado, e por qual caminho.** Cadeia de eventos real, nÃ£o `StartExecution` na mÃ£o: PDF real (1075 bytes, texto `Validade: 31/12/2027`, gerado com `pdf-lib`) enviado ao bucket real `exptrk-dev-documents-clean` sob a chave de produÃ§Ã£o `clean/<tenantId>/<itemId>/<documentId>` (SSE-KMS obrigatÃ³ria pela bucket policy), e uma mensagem no formato EventBridge "Object Created" publicada na fila real `exptrk-dev-extraction-starter` â†’ `exptrk-dev-extraction-starter-handler:live` â†’ `startExtractionRun` real (leu o `Document` CLEAN, criou o `ExtractionRun` idempotente, chamou `StartExecution` com `name = runId`). O Ãºnico elo originado Ã  mÃ£o foi a mensagem SQS, porque a regra EventBridge que a produziria Ã© o Ãºnico recurso condicionado a `var.extraction_pipeline_enabled` (hoje `false`) e criÃ¡-la exigiria `terraform apply` â€” proibido pelo Â§3 do `AGENTS.md`. Nada alÃ©m disso foi simulado: Lambdas, state machine, Textract, S3, DynamoDB, KMS e AppConfig foram todos os recursos reais de `dev`.

Escafalho mÃ­nimo escrito direto no DynamoDB real (`IdentityMapping`, `User` OWNER, `ExpirationItem`, 3 `Document` CLEAN) porque login via Cognito Hosted UI nÃ£o Ã© automatizÃ¡vel por CLI. **Nenhum resultado de extraÃ§Ã£o foi falsificado** â€” todo `ExtractionRun`/`ExtractedField`/`TextractJob` desta seÃ§Ã£o foi produzido pelo pipeline real.

**As 3 execuÃ§Ãµes reais** (state machine `exptrk-dev-document-extraction`):

| ExecuÃ§Ã£o | Flags AppConfig servidas | Estados que rodaram | Resultado |
|---|---|---|---|
| `run_9a0adc8271f936cdfd93163a7bcfd21c` | v1 Terraform (`{"features":{OCR:false,â€¦}}`) | RunTextract(falhou) â†’ RunDeterministicParser â†’ NeedsBedrock â†’ CheckAiKillSwitch â†’ ValidateSchema â†’ CompareExtractors â†’ PersistExtractedFields â†’ CompleteRun | SUCCEEDED, caminho degradado (`ocrAvailable:false`), campo `PENDING_CONFIRMATION` sem candidato |
| `run_a22df0565a3c9f96e80e69e8138e97e7` | v2 temporÃ¡ria, envelope canÃ´nico com `OCR:true` | idÃªnticos ao anterior | SUCCEEDED, **ainda degradado** â€” evidÃªncia do bug 1 abaixo |
| `run_c1aef1a7c3f1272b9f9ff78cc963ab98` | v3 temporÃ¡ria, envelope canÃ´nico **+ cÃ³pia top-level** dos 3 booleanos | RunTextract â†’ RunDeterministicParser â†’ NeedsBedrock â†’ ValidateSchema â†’ CompareExtractors â†’ PersistExtractedFields â†’ CompleteRun | SUCCEEDED, **caminho feliz completo com Textract real** |

EvidÃªncia do caminho feliz (`output` real da execuÃ§Ã£o `run_c1aefâ€¦`): `ocrAvailable:true`, artefato OCR real em `s3://exptrk-dev-extraction-transient/ocr/run_c1aefâ€¦/42d064cf-â€¦.json`, `extractedFields[0] = {fieldName:"expirationDate", candidateValue:"2027-12-31", confidence:0.9, source:"DETERMINISTIC_PARSER", valid:true}`, `needsBedrock:false`, `agreement:"SINGLE_SOURCE"`, `runOutcome:"COMPLETED"`. Ou seja: **Textract real leu o PDF real e o parser determinÃ­stico extraiu a data correta**. `RunBedrock` NÃƒO foi exercitado (`needsBedrock:false` â€” o candidato determinÃ­stico jÃ¡ passou do limiar 0,75; `AI_EXTRACTION` foi mantido `false` de propÃ³sito). `CompleteRun` apagou o artefato transitÃ³rio (verificado: `ocr/` ficou com 0 objetos).

**As 2 rotas HTTP novas, exercitadas de verdade** (bundle esbuild real de `documents-handler`, evento API Gateway HTTP API v2 forjado apenas para pular o login Cognito; resolver/matriz de autorizaÃ§Ã£o/quota/idempotÃªncia/`TransactWriteItems` todos reais contra `exptrk-dev-table`):

- `confirm` no campo `PENDING_CONFIRMATION` de `run_9a0adcâ€¦` â†’ **200**, campo `PENDING_CONFIRMATION` â†’ `CONFIRMED` (`version` 1â†’2, `confirmedValue:"2027-12-31"`).
- **`ExpirationItem.dueDate` antes: `2030-01-01` (version 1) â†’ depois: `2027-12-31` (version 2)**, com `GSI1SK` re-chaveado para `DUE#2027-12-31#ITEM#itm_m7verify` na mesma transaÃ§Ã£o. Este era o objetivo final do gate.
- Replay do mesmo `Idempotency-Key` â†’ 200 com o mesmo campo e **`ExpirationItem.version` continuou 2** (nÃ£o reaplicou).
- `reject` no campo de `run_a22dfâ€¦` â†’ **200**, `PENDING_CONFIRMATION` â†’ `REJECTED` com `correctionReason` gravado, sem tocar no `ExpirationItem`.
- `confirm` no campo auto-`CONFIRMED` de `run_c1aefâ€¦` â†’ **422 BUSINESS_RULE** (`"ExtractedField is not pending confirmation (state=CONFIRMED)"`) â€” ver bug 3.

### Bugs reais encontrados

1. **`AppConfigFeatureFlagsReader` lia os kill switches do lugar errado â€” pipeline permanentemente fail-closed em `dev`. CORRIGIDO nesta sessÃ£o.** `infra/modules/feature-flags/main.tf` publica (e `implementation-blueprint.md` Â§17.3 especifica) `{"features":{"AI_EXTRACTION":â€¦,"OCR":â€¦,"WHATSAPP":â€¦}}`, mas o adapter lia `parsed.OCR` no nÃ­vel raiz â€” logo toda flag resolvia `false` independentemente do valor implantado, e todo `START_OCR` morria em `OcrDisabledError`. EvidÃªncia isolada: a execuÃ§Ã£o `run_a22dfâ€¦` rodou com a versÃ£o AppConfig 2 (`OCR:true` dentro do envelope canÃ´nico) e **ainda assim** falhou com `OcrDisabledError`; a `run_c1aefâ€¦`, cuja Ãºnica diferenÃ§a era a cÃ³pia top-level dos mesmos booleanos, chamou Textract normalmente. Os testes unitÃ¡rios do adapter afirmavam a forma errada, e por isso nunca pegaram. Corrigido lendo o envelope `features` (com fallback tolerante para o nÃ­vel raiz) + teste de regressÃ£o.
2. **As duas rotas de `confirm`/`reject` estavam 100% quebradas contra o DynamoDB real (dois defeitos independentes). CORRIGIDOS nesta sessÃ£o.**
   - **500 `Unknown schema $id`**: `schemas/api/confirm-extracted-field-request.v1.json` e `reject-extracted-field-request.v1.json` nunca foram adicionados Ã  lista de imports estÃ¡ticos de `defaultSchemaRegistry` (`src/shared/contracts/schema-validator.ts`), cuja prÃ³pria doc-string manda "add both the file AND a static import line". Toda chamada Ã s duas rotas retornava 500 antes de qualquer lÃ³gica de negÃ³cio.
   - **409 permanente**: `confirm-reject-field.ts` passava a **entidade `ExtractedField` inteira** como `fieldKey` para `confirmField`/`rejectField`; `buildVersionedUpdate` repassa isso como `Key` do `Update`, e o DynamoDB real rejeita com `ValidationError: The provided key element does not match the schema` dentro do `TransactWriteItems` â€” o que a store interpreta como `VERSION_CONFLICT` e o HTTP mapeia para 409. Capturado com `CancellationReasons` reais: `[{"Code":"ValidationError","Message":"The provided key element does not match the schema"},{"Code":"None"},{"Code":"None"},{"Code":"None"}]`. Os fakes em memÃ³ria dos testes unitÃ¡rios ignoram atributos extras na chave, por isso a suÃ­te passava. Corrigido passando `extractedFieldKey(...)` nas duas rotas. SÃ³ depois dessa correÃ§Ã£o o `confirm` real devolveu 200 e atualizou o `dueDate`.
3. **Campo auto-`CONFIRMED` pelo pipeline nunca chega ao `ExpirationItem` â€” GAP REAL, NÃƒO CORRIGIDO (decisÃ£o do Marcelo).** `decideFieldOutcome` auto-aceita (`state: "CONFIRMED"`) um candidato `SINGLE_SOURCE` acima do limiar de confianÃ§a â€” foi exatamente o que aconteceu com `run_c1aefâ€¦`, o caso de sucesso do pipeline. Mas o **Ãºnico** cÃ³digo que escreve `ExpirationItem.dueDate` Ã© a rota HTTP de `confirm`, que exige `state === "PENDING_CONFIRMATION"` e devolve 422 para um campo jÃ¡ `CONFIRMED` (verificado, ver acima). ConsequÃªncia: no caminho de maior sucesso do pipeline, o `dueDate` do item **nunca** Ã© atualizado e nÃ£o existe rota que o atualize. NÃ£o corrigido de propÃ³sito: escolher entre "o pipeline passa a escrever o `dueDate` na transaÃ§Ã£o de `PersistExtractedFields`" e "`decideFieldOutcome` nunca auto-confirma, sempre exige humano" Ã© decisÃ£o de produto/design (Type 1, Â§4 do `AGENTS.md`), nÃ£o conserto mecÃ¢nico.

### Achado menor (nÃ£o corrigido, sem impacto observado nesta verificaÃ§Ã£o)

Os `ErrorEquals` do ASL usam os valores de `code` das `AppError` (`OcrDisabled`, `ExtractionCommitFailed`, â€¦), mas o Step Functions casa contra o `errorType` do Lambda, que Ã© o `name` da classe (`OcrDisabledError`, `ExtractionCommitFailedError`, â€¦). Confirmado no histÃ³rico real: o `TaskFailed` de `RunTextract` trouxe `"error": "OcrDisabledError"` e o Catcher nomeado **nÃ£o** casou â€” quem pegou foi o `States.ALL` seguinte, que tem o mesmo `Next`, entÃ£o o comportamento observado ficou correto por acidente. O mesmo descasamento torna morto o `Retry` de `PersistExtractedFields`/`MarkPendingConfirmation` em `ExtractionCommitFailed`, e aÃ­ **hÃ¡** perda de comportamento (uma falha transitÃ³ria de commit nÃ£o seria retentada como desenhado). Corrigir exige mexer no ASL e portanto um `terraform apply` via CD â€” deixado para uma sessÃ£o de infra.

### Estado do ambiente apÃ³s a sessÃ£o

Flags AppConfig **revertidas**: versÃ£o hospedada 1 (a do Terraform) reimplantada (deployment 4), e as versÃµes temporÃ¡rias 2/3 deletadas â€” `list-hosted-configuration-versions` volta `[1]`, exatamente como antes. Nenhum `terraform apply`. `terraform plan` posterior contra `dev` nÃ£o mostra **nenhum** recurso AppConfig/S3/DynamoDB no diff (sÃ³ o ruÃ­do jÃ¡ conhecido de republicaÃ§Ã£o de versÃ£o de Lambda, ampliado por um `npm run build:lambdas` local). Todo dado de teste removido: `scan` filtrando `m7verify` volta `Count: 0`, o prefixo S3 `clean/usr_m7verify20260827/` volta 0 objetos (todas as versÃµes deletadas), `ocr/` no bucket transitÃ³rio volta 0. Os histÃ³ricos das 3 execuÃ§Ãµes do Step Functions ficam (artefato normal da AWS, nÃ£o Ã© dado de teste a limpar).

**As correÃ§Ãµes dos bugs 1 e 2 estÃ£o apenas em `develop`, ainda NÃƒO implantadas em `dev`** (o CD sÃ³ aplica em push para `main`). A verificaÃ§Ã£o do `confirm` real rodou contra o bundle esbuild jÃ¡ corrigido, executado localmente contra o DynamoDB real; o `documents-handler` implantado em `dev` continua com os dois defeitos atÃ© o prÃ³ximo merge para `main`.

**Bloqueado sÃ³ para produÃ§Ã£o real** (nÃ£o bloqueia implementar/testar em `dev`, exatamente como o toggle foi desenhado): escolha/validaÃ§Ã£o de modelo Bedrock + regiÃ£o, RIPD formal (`privacy-lgpd.md` Â§6, gatilho jÃ¡ registrado).

## DecisÃµes pendentes do Marcelo

1. **M7 (extraÃ§Ã£o/OCR)**: design jÃ¡ aprovado (D-035), zero decisÃ£o de produto pendente â€” sÃ³ falta decidir *quando* comeÃ§ar, depois da infra de hospedagem ou em paralelo.
2. **User Validation**: **em suspenso a pedido do Marcelo** (2026-08-25) â€” nÃ£o retomar sem sinal explÃ­cito dele.
3. **Estado real de `git`/deploy no inÃ­cio da prÃ³xima sessÃ£o nÃ£o deve ser presumido** â€” confirmar `git status`/`git log`/branch atual antes de qualquer trabalho novo, em vez de reconstruir a partir deste resumo.

## PendÃªncias residuais nÃ£o bloqueantes (registradas, nÃ£o bloqueiam nada)

- ~~`npm audit --omit=dev` no job `guardrails` segue com achado prÃ©-existente nÃ£o batendo exatamente com `docs/engineering/exceptions.md` EX-001~~ â€” **reavaliado em 2026-08-25**: EX-001 estava desatualizado em nÃºmeros (9 vulnerabilidades reais, nÃ£o 5) e prazo (expirado desde M4, nunca reavaliado); corrigido com uma segunda cadeia transitiva real e nÃ£o documentada (`testcontainers`â†’`dockerode`â†’`tar-fs`/`undici`, usada por `test:dynamodb` em CI real). `npm audit --omit=dev` continua em 0 nas duas raÃ­zes (`/` e `frontend/`). Novo prazo: 2026-09-24. Upgrade do Vitest ainda bloqueado pelo bug upstream `npm/cli#4828` (status nÃ£o reverificado nesta sessÃ£o).
- Observabilidade por funÃ§Ã£o (alarmes CloudWatch) para `ImportParseWorker`/`ImportCommitWorker`: deixada como residual documentado em D-050 (a DLQ-age alarm genÃ©rica jÃ¡ cobre "estÃ¡ falhando").
- ~~Camada 3 de M6 (teste real de reconciliaÃ§Ã£o de upload slot expirado) nunca exercitada contra AWS real~~ â€” **executada em 2026-08-25, achou um bug real de produÃ§Ã£o**: `reserveUpload` nunca escrevia `GSI6PK`/`GSI6SK` no `UploadSlot` criado â€” confirmado empiricamente contra a conta `dev` real (perfil `claude-dev`, mesma conta do `AWS_ROLE_ARN_DEV`): um slot expirado fabricado sem esses campos ficou invisÃ­vel ao worker real (`resultCount: 0`); o MESMO item, com os campos adicionados manualmente, foi corretamente descoberto e reconciliado (`slotsExpired: 1`, documento associado passou a `TIMEOUT`). Confirmou tambÃ©m que `GSI6PK`/`GSI6SK` nunca eram removidos apÃ³s a transiÃ§Ã£o (ponteiro Ã³rfÃ£o) e que nenhum caminho jamais marcava o slot como `CONSUMED` num upload bem-sucedido (a mesma lacuna estrutural, do lado oposto). **Corrigido**: `reserveUpload` agora escreve os campos GSI6 (`document-store.ts`'s `buildUploadSlotGsi6Sk`, mesma convenÃ§Ã£o `<timestamp>#TENANT#<t>#<ENTIDADE>#<id>` do mÃ³dulo `reminder`); `reconciliation.ts` remove os campos ao marcar `EXPIRED`; `advance-after-evidence.ts` marca o slot `CONSUMED` + remove os campos quando o Document atinge um resultado terminal (`CLEAN` ou `REJECTED`/`UNSUPPORTED`). 5 testes novos, 716 testes de backend, nenhuma regressÃ£o. Itens de teste fabricados na tabela `dev` real foram limpos ao final (confirmado `Count: 0`). **Deployado** â€” commit `2fab80f`, jÃ¡ mergeado em `main` (PR #55) e presente em `develop`; nota anterior dizendo "ainda nÃ£o deployado" estava desatualizada (verificado nesta sessÃ£o via `git merge-base --is-ancestor`).
- **Achado real, severidade baixa, nÃ£o corrigido** (2026-08-25, mesma auditoria que achou o bug do `upload-slot` acima): `reminder-occurrence.ts`/`document-chasing.ts` documentam que `GSI3PK`/`GSI3SK` devem ser removidos em `TRIGGERED`/`CANCELLED` ("mesmo invariante" entre os dois tipos de occurrence), mas nem `reminder-dispatch/dispatch.ts` nem `document-chasing-dispatch/dispatch.ts` fazem isso â€” sÃ³ removem `GSI6PK`/`GSI6SK`. Investigado antes de decidir: `GSI3` Ã© particionada por bucket de minuto (`DUE#<minuto>#<shard>`) e sÃ³ Ã© lida pelo producer dentro de uma janela de lookback de 5 minutos, sempre com re-validaÃ§Ã£o defensiva do status real da occurrence antes de agir â€” um ponteiro Ã³rfÃ£o nunca Ã© revisitado fora dessa janela, e mesmo se fosse, Ã© ignorado com seguranÃ§a. Diferente do bug do `upload-slot` (falha total â€” nada funcionava), este Ã© um descompasso comentÃ¡rio-vs-cÃ³digo de baixa severidade (linhas extras nunca lidas de novo) num caminho jÃ¡ revisado 9,2/10 pelo protocolo Claudeâ†”Codex e ativo em produÃ§Ã£o real â€” nÃ£o corrigido nesta sessÃ£o deliberadamente, para nÃ£o mexer num trecho crÃ­tico jÃ¡ extensivamente revisado sem uma rodada de revisÃ£o dedicada. Candidato a um follow-up de baixa prioridade, nunca urgente.
- `expiration-tracker-bff-frontend-quality-standard.md` (raiz) contÃ©m uma rubrica candidata mais ampla (BFF/performance/testes de frontend de produÃ§Ã£o real, Â§13-30) que, se adotada como padrÃ£o oficial, deveria passar pela mesma convergÃªncia independente Claudeâ†”Codex que os 9 eixos de `docs/engineering/joint-review-criteria.md` jÃ¡ usaram â€” nÃ£o decidido ainda. (`docs/frontend/interface-quality-standard.md`, o padrÃ£o de 12 eixos especÃ­fico do planejamento de interface, jÃ¡ existe como arquivo formal desde `interface-validation-readiness.md`.)

## ReferÃªncias (nÃ£o reler por padrÃ£o â€” sÃ³ sob demanda)

- `docs/architecture/session-log.md` â€” linha do tempo compacta, uma entrada por sessÃ£o.
- `docs/architecture/decisions-log.md` â€” todas as decisÃµes (D-000 a D-054) com nota Claude/Codex e status.
- `docs/frontend/` â€” os 8 documentos de planejamento de interface aprovados (mais `interface-quality-standard.md`); `prototype/` â€” o protÃ³tipo interativo executÃ¡vel (ver `prototype/README.md`).
- `docs/architecture/reviews/bff-full-vs-session-design/` â€” debate completo do Full BFF (design).
- `docs/frontend/frontend-production-foundation.md` â€” implementaÃ§Ã£o real do Full BFF + fundaÃ§Ã£o de frontend de produÃ§Ã£o, achados do protocolo Claudeâ†”Codex sobre o cÃ³digo, Final Status.

# docs/architecture/ â€” Ãndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÃ§Ã£o real (cÃ³digo/infra/testes) â€” Implementation Blueprint concluÃ­do
Last verified:           2026-08-28 (Consolidation + Pilot Readiness Program, Wave 3 tenant isolation/LGPD auditada de ponta a ponta â€” zero vulnerabilidade cross-tenant real encontrada, mas 8 de 9 classes de retenÃ§Ã£o LGPD sem purga fÃ­sica real e DSR 100% design-only, ambos registrados como decisÃ£o pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership implementada; Wave 5 escopou o gap de identidade do guest (GTR-01); sÃ­ntese final com recomendaÃ§Ã£o CONDITIONAL GO em `docs/engineering/pilot-readiness-assessment.md` â€” ver `docs/engineering/pilot-readiness-program.md` para o backlog completo item-a-item. M6/M9/M10/M11 deployados em `main`/`dev`; **M7 (extraÃ§Ã£o/OCR) code-complete e deployado em `dev`** â€” todos os 9 itens da lista original reais de ponta a ponta (Textract/parser determinÃ­stico/Bedrock/validaÃ§Ã£o, state machine real, rotas HTTP confirm/reject, quota AI_CALL), PR #60 mergeado em `main`; **verificaÃ§Ã£o E2E real executada em 2026-08-27** contra `dev` (cadeia real S3â†’SQSâ†’Step Functionsâ†’Textractâ†’parserâ†’persistÃªncia, ver `NEXT_SESSION_PROMPT.md`) â€” achou 3 bugs reais, 2 corrigidos e mergeados em `main` via PR #67 (envelope de feature flags do AppConfig lido no nÃ­vel errado, deixando o pipeline permanentemente fail-closed; rotas HTTP `confirm`/`reject` 100% quebradas por schema nunca registrado + chave OCC malformada), 1 gap real deliberadamente nÃ£o corrigido e aguardando decisÃ£o de produto do Marcelo (campo auto-`CONFIRMED` pelo pipeline nunca propaga para `ExpirationItem.dueDate` â€” nenhuma rota escreve isso hoje); M12 bloqueado por decisÃ£o de produto (D-052); Full BFF â€” D-053/D-054 â€” **implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, infra Terraform, frontend real em `frontend/`, protocolo Claudeâ†”Codex completo em `docs/frontend/frontend-production-foundation.md` â€” Rodada D levou 6 passagens/5 achados bloqueantes reais de seguranÃ§a de sessÃ£o atÃ© convergir); planejamento de interface concluÃ­do â€” 8 de 9 etapas `APPROVED` em `docs/frontend/`, sÃ³ falta User Validation (em suspenso a pedido do Marcelo); os 3 blockers tÃ©cnicos de backend descobertos no planejamento de interface (BLOCKER-A/B/C) estÃ£o **todos resolvidos** (backend + frontend onde aplicÃ¡vel â€” BLOCKER-C via Variante B, revisÃ£o humana explÃ­cita, primeira fatia real do anchor Fornecedor/Subject); infra de hospedagem do SPA (CloudFront+S3, ADR-0011) fechada de ponta a ponta e deployada â€” ver `NEXT_SESSION_PROMPT.md` para o estado exato e a prÃ³xima aÃ§Ã£o concreta, este bloco pode ficar temporariamente atrÃ¡s dele)
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÃ³xima aÃ§Ã£o concreta.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

1. `AGENTS.md` (raiz) â€” processo de trabalho dos agentes, sempre vence sobre conteÃºdo de arquitetura.
2. ADR aceito em `adr/` â€” decisÃ£o arquitetural especÃ­fica e formal.
3. Documento temÃ¡tico corrente (tabela abaixo, coluna "normativo atual") â€” especificaÃ§Ã£o detalhada do domÃ­nio.
4. `ARCHITECTURE.md` â€” visÃ£o consolidada e Ã­ndice executivo; nÃ£o sobrescreve silenciosamente um ADR ou documento temÃ¡tico divergente â€” divergÃªncia entre eles Ã© defeito a corrigir, nÃ£o licenÃ§a para escolher.
5. `NEXT_SESSION_PROMPT.md` â€” estado de execuÃ§Ã£o, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` â€” evidÃªncia histÃ³rica de como se chegou a uma decisÃ£o, nunca normativo.

## Ãndice por documento

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Ã­ndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÃ³rico/processo (ciclo concluÃ­do) | Processo Claudeâ†”Codex que produziu o design; nÃ£o Ã© ponto de entrada de sessÃ£o |
| `quality-criteria.md` | normativo atual | 12 critÃ©rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÃ©rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÃ£o-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÃµes numeradas |
| `data-model.md` | normativo atual | Modelo de domÃ­nio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÃ­cios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÃ§Ã£o, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÃ¡gio |
| `mcp-readiness.md` | normativo atual | ProntidÃ£o de domÃ­nio para MCP futuro |
| `evolution.md` | normativo atual | TransiÃ§Ãµes de estÃ¡gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÃ£o pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÃ§Ã£o 33 â€” APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÃ§Ã£o 60 â€” componentes, interfaces, eventos/schemas, ordem de deploy, milestones â€” APPROVED (Claude 9.20 / Codex 9.2) |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) â€” adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas â€” APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B â€” pipeline real de materializaÃ§Ã£o/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÃªncia (dispatch + reconciliaÃ§Ã£o), backfill â€” APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÃ§Ã£o, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÃ³rico/evidÃªncia (citado por `reminder-delivery-pipeline.md` Â§3/Â§3.2) | Recon prÃ©-implementaÃ§Ã£o de BLOCKER-B (materializer/trigger de materializaÃ§Ã£o, infra Terraform) confirmado contra o cÃ³digo real antes do design ser escrito; BLOCKER-B estÃ¡ implementado e mergeado (PR #50) â€” este documento sÃ³ registra a evidÃªncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÃ³rico/evidÃªncia | Texto verbatim do prompt de missÃ£o original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÃ³ existia em histÃ³rico de conversa de uma sessÃ£o especÃ­fica |
| `decisions-log.md` | decisÃ£o/ADR (log vivo) | D-000 a D-043 (a numeraÃ§Ã£o nÃ£o Ã© sequencial na ordem das linhas â€” D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÃ³rico/evidÃªncia de rodada | Artefatos do protocolo Claudeâ†”Codex de M7 (proposta Claude, proposta Codex, crÃ­tica, reconciliaÃ§Ã£o final aprovada â€” D-035) |
| `adr/` | decisÃ£o/ADR | 11 ADRs formais para decisÃµes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÃªncia CloudFront + Full BFF) â€” nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Ã­ndice (visual) | **O documento de status do projeto** â€” painel visual (timeline de marcos, achados reais, pendÃªncias); abrir no navegador para uma visÃ£o executiva rÃ¡pida, mais legÃ­vel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÃ£o) |
| `session-log.md` | histÃ³rico | Log cronolÃ³gico compacto por sessÃ£o |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÃ£o normativo) | Fase 1 da evoluÃ§Ã£o estratÃ©gica do roadmap (2026-08-23): estado real dos milestones + classificaÃ§Ã£o de cada capacidade comercial proposta contra o cÃ³digo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÃ­nio + protocolo Claudeâ†”Codex por tema), nunca decisÃ£o fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÃ£o normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 â€” achado central: billing por sujeito rastreado Ã© padrÃ£o de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÃ­nio (`TrackedSubject`+`RequirementAssignment`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÃ£o de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÃ§Ã£o pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,1/9,2; agregados-irmÃ£os em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÃ¡ em produÃ§Ã£o, aplicando o precedente de M7; GSI3 reaproveitado sob condiÃ§Ã£o de mini-revisÃ£o de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÃ£o direta de padrÃ£o jÃ¡ em produÃ§Ã£o (mesma partiÃ§Ã£o de `Document`/M6); digest registrado como questÃ£o aberta, nÃ£o decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, lista de rejeitados formal sÃ³ na Fase 3) | Fase 2b, sexto cluster â€” custom fields genÃ©rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÃ£o (nota 9,1/9,0), valor jÃ¡ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÃ£o reconciliada via protocolo AGENTS.md Â§4, ADR formal sÃ³ na Fase 3) | Fase 2b, sÃ©timo e Ãºltimo cluster (CSV import/export) â€” protocolo Claudeâ†”Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÃ§Ã£o (nÃ£o na entrada); plano linha-a-linha em S3, nÃ£o DynamoDB; **Fase 2b concluÃ­da, 7/7 clusters â‰¥9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÃ­ntese proposta, nÃ£o autorizaÃ§Ã£o de implementaÃ§Ã£o) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph â€” consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÃ­ntese proposta, ADRs formais sÃ³ com decisÃ£o do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÃ§a/persistÃªncia/custo, lista de 10 ADRs candidatos, estratÃ©gia de teste/migraÃ§Ã£o, perguntas abertas reais, capacidades rejeitadas â€” inclui revisÃ£o adversarial final de coerÃªncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÃ§Ã£o de prÃ©-requisito (D-046) | Mini-revisÃ£o de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) â€” pico orgÃ¢nico ~220Ã— abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Ã­ndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-047/D-048) | Fecha D-047 â€” entrega/reenvio do link de guest upload: rotaÃ§Ã£o de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-049) | Fecha "DecisÃ£o B" de D-048 â€” automatiza o convite inicial de guest upload (hoje manual) atrÃ¡s de preferÃªncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-052) | PÃ³s-M11: M12 (Billing) fica bloqueado por decisÃ£o de produto (fornecedor de pagamento) â€” zero cÃ³digo novo; achado real de que o projeto nÃ£o tem conceito de "platform staff" cross-tenant, descartando atÃ© a fatia manual de entitlement; nota final 9,3/9,4 |
| `reviews/bff-full-vs-session-design/` | decisÃ£o reconciliada via protocolo AGENTS.md Â§4 (D-053/D-054) | Full BFF como fronteira de sessÃ£o do browser â€” decide o mecanismo de autenticaÃ§Ã£o das chamadas de recurso que o "BFF de sessÃ£o" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÃ£o opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÃ§Ã£o nativa do Cognito no refresh (nÃ£o contador local â€” evita falso-positivo de invalidaÃ§Ã£o), tabela de sessÃ£o dedicada IAM-isolada, cookies login/sessÃ£o com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) â€” ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÃ§Ã£o e do protocolo Claudeâ†”Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÃ§a de sessÃ£o corrigidos) |
| `reviews/` | histÃ³rico/evidÃªncia de rodada (protocolo `AGENTS.md` Â§4, pÃ³s-M0) | Artefatos de revisÃ£o Claudeâ†”Codex de implementaÃ§Ã£o real, por milestone (ex. `reviews/m3.5-runtime-design/`) â€” mesmo papel de `history/` (evidÃªncia, nunca normativo), mas para rodadas ocorridas depois que cÃ³digo passou a existir, em vez das rodadas de design conceitual prÃ©-implementaÃ§Ã£o |
| `history/` | histÃ³rico/supersedido | Artefatos de rodada (propostas, crÃ­ticas, red team) que produziram os documentos normativos acima â€” ver subseÃ§Ã£o |

## `history/` â€” evidÃªncia de rodada, por tema

Cada subpasta contÃ©m os artefatos de proposta/crÃ­tica/trÃ©plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÃ£o vigente â€” sÃ³ como prova de como o consenso foi alcanÃ§ado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ â€” Ãndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÃDO â€” 9/9 avaliados (ver NEXT_SESSION_PROMPT.md, sÃ³ Contexto bateu o gate â‰¥9.0; os outros 8 ficaram honestamente abaixo, classificados)
Last verified:            2026-08-20
```

Este diretÃ³rio trata de **como o trabalho de engenharia Ã© medido e revisado** (rubrica, critÃ©rios por eixo, protocolo de debate, achados de auditoria) â€” nÃ£o confundir com `docs/architecture/`, que trata do que o sistema Ã‰ (design, modelo de dados, decisÃµes de arquitetura). Se a dÃºvida for "o GSI3 Ã© consultÃ¡vel por quem", vÃ¡ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÃ³rio Ã© o certo.

## PrecedÃªncia de fontes (quando houver divergÃªncia)

Mesma regra de `docs/architecture/README.md` â€” `AGENTS.md` (raiz) sempre vence sobre conteÃºdo deste diretÃ³rio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ã© evidÃªncia de uma rodada especÃ­fica, nunca redefine os pesos/critÃ©rios de `joint-review-criteria.md` (ele mesmo linka para lÃ¡ em vez de duplicar, ver seu prÃ³prio rodapÃ© "Como adicionar um novo eixo").

## Quando carregar o quÃª (roteamento por tipo de tarefa)

| Sua tarefa Ã©... | Carregue |
|---|---|
| Rodar o protocolo Claudeâ†”Codex num eixo especÃ­fico (`AGENTS.md` Â§4) | `joint-review-criteria.md` (seÃ§Ã£o do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÃ­fico do cÃ³digo tira / que critÃ©rio ele afeta | `joint-review-criteria.md` (nÃ£o precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ã© vÃ¡lido/de qualidade suficiente (gate binÃ¡rio, critÃ©rio ponderado, nota mÃ­nima) | `test-engineering-standard.md` â€” rÃ©gua concreta que `joint-review-criteria.md`'s critÃ©rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÃ§a precisa de ADR/protocolo formal ou Ã© correÃ§Ã£o mecÃ¢nica | `change-risk-scale.md` (a rÃ©gua concreta) â€” `AGENTS.md` Â§4 sÃ³ distingue os dois extremos de forma binÃ¡ria |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` â€” mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÃ§Ã£o/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÃ£o | `exceptions.md` |
| Ver o histÃ³rico completo de decisÃµes de engenharia (nÃ£o de arquitetura) com motivo | `decisions-log.md` â€” E-000 em diante, mesmo padrÃ£o de `docs/architecture/decisions-log.md` mas para decisÃµes de processo/qualidade, nÃ£o de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÃ©rios/gates G1-G6 (histÃ³rico, prÃ© full-audit) | `01-engineering-quality-criteria.md` (CONGELADA â€” mudanÃ§a exige nova rodada formal, nÃ£o ediÃ§Ã£o direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÃ³rio no inÃ­cio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` â€” histÃ³rico, nÃ£o estado atual (ver aviso no prÃ³prio arquivo) |
| Consultar o resultado de uma auditoria especÃ­fica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÃ§Ã£o de nomenclatura abaixo) |
| Consultar evidÃªncia bruta de uma rodada especÃ­fica (nota cega, prompt do Codex, saÃ­da bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` â€” evidÃªncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÃ­stico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) â€” link relativo quebrado + referÃªncia `AGENTS.md Â§N` desatualizada, bloqueante no CI (`guardrails`) |

## Ãndice por documento (nÃ­vel raiz de `docs/engineering/`)

| Documento | ClassificaÃ§Ã£o | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÃ­pios de engenharia adotados (proporcionalidade, evidÃªncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÃ§a NÃ­vel 1-6, rÃ©gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `joint-review-criteria.md` | normativo atual | CritÃ©rios/pesos por eixo das revisÃµes conjuntas Claudeâ†”Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo Claudeâ†”Codex 8 rodadas, gate elevado 9,5/10) | PadrÃ£o de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) â€” gates binÃ¡rios (G-V1..G-V6, G-C1), critÃ©rios ponderados, fÃ³rmula de agregaÃ§Ã£o, auditoria retroativa da Wave 2 (2026-08-28) |
| `exceptions.md` | normativo atual (registro vivo) | ExceÃ§Ãµes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÃ£o (log vivo) | DecisÃµes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÃ³rico/registro vivo | DivergÃªncias materiais Claudeâ†”Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` Â§4 pode ser dispensado, inventÃ¡rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÃ³prio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÃ³rico (CONGELADA) | Rubrica original de 12 critÃ©rios/gates G1-G6 da Engineering Maturity Review â€” antecessora conceitual de `joint-review-criteria.md`, nÃ£o superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÃ§Ãµes executÃ¡veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÃ³rico/fundamentaÃ§Ã£o | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÃ³rico | Estado do repositÃ³rio no inÃ­cio da Engineering Maturity Review â€” nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÃ£o normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`expiration-tracker-next-days-master-plan-and-ai-prompt.md`, raiz) â€” DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÃ­ntese/recomendaÃ§Ã£o (entregÃ¡vel final do programa, prompt mestre Â§42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÃªncia de `pilot-readiness-program.md` â€” nÃ£o repete achado, sÃ³ aponta; nÃ£o Ã© aprovaÃ§Ã£o final, Ã© insumo para a decisÃ£o do Marcelo |
| `reviews/` | histÃ³rico/evidÃªncia de rodada | Artefatos de toda rodada Claudeâ†”Codex de processo/qualidade â€” ver seÃ§Ã£o prÃ³pria abaixo |

## `reviews/` â€” convenÃ§Ã£o de nomenclatura e classificaÃ§Ã£o

EvidÃªncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÃµes de arquitetura: nunca normativo, sÃ³ prova de como uma nota/decisÃ£o foi alcanÃ§ada. TrÃªs geraÃ§Ãµes de conteÃºdo coexistem aqui, nesta ordem cronolÃ³gica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÃ­vel raiz de `reviews/`) â€” produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ã© intencional (agrupa antes de outros nomes em ordem alfabÃ©tica, sinaliza "transcript bruto, nÃ£o leia direto").
2. **ConvergÃªncia dos critÃ©rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) â€” produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) â€” a rodada em andamento nesta sessÃ£o. PadrÃ£o de nomenclatura por eixo (`<eixo>` âˆˆ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... â€” nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... â€” prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... â€” saÃ­da bruta do Codex CLI. Alguns trechos que ecoam saÃ­da de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÃ§Ãµes via PowerShell) â€” revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÃ£o sÃ³ mitigado); a saÃ­da de uma rodada em andamento pode ainda conter mojibake atÃ© a rodada seguinte revisÃ¡-la, por ser evidÃªncia datada do que o Codex realmente viu naquele momento â€” nunca editada retroativamente. Se precisar do conteÃºdo, prefira o `-summary.md` correspondente, que jÃ¡ extrai a informaÃ§Ã£o sem o ruÃ­do de encoding.
   - `full-audit-round1-<eixo>-summary.md` â€” **o Ãºnico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÃ©rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÃ§Ã£o pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÃ£o fonte de verdade por si sÃ³ â€” sempre prefira o `.md` de resumo/nota que os acompanha. **PolÃ­tica de proveniÃªncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÃ£o â€” exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÃ¡ que sua proveniÃªncia Ã© herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÃ§Ã£o cronolÃ³gica descrita nesta seÃ§Ã£o. Isso Ã© uma decisÃ£o de design, nÃ£o uma lacuna: um transcript nunca Ã© lido isoladamente por convenÃ§Ã£o (ver acima), entÃ£o nÃ£o precisa carregar sua prÃ³pria metadata.

# W3-06 â€” Round 4 (Claude trÃ©plica, respondendo Ã  Rodada 3 do Codex, nota 7,8/10, 5 achados bloqueantes)

## ResoluÃ§Ã£o achado "novo" (status apagado pela exclusÃ£o lÃ³gica)

Correto â€” `document-deletion-service.ts:51` sobrescreve `status` para `DELETED`, entÃ£o a tabela
por-status da Rodada 3 Ã© inexequÃ­vel como escrita. **CorreÃ§Ã£o: nÃ£o depender de `status` anterior
nenhum.** `cleanObject`/`uploadEvidence`/`malwareEvidence` jÃ¡ sobrevivem Ã  exclusÃ£o lÃ³gica sem
qualquer mudanÃ§a (nenhum cÃ³digo os limpa em `deleteDocument`), entÃ£o sÃ£o discriminadores
suficientes por si:

```
if (doc.cleanObject) â†’ deleteObjectVersion(doc.cleanObject)               // foi promovido a CLEAN
else if (uploadEvidence?.object ?? malwareEvidence?.object) â†’ deleteObjectVersion(esse objeto)  // teve evidÃªncia real, nunca promovido (REJECTED/UNSUPPORTED)
else â†’ nada a apagar por versÃ£o explÃ­cita (nunca chegou evidÃªncia; lifecycle 24h de quarantine jÃ¡ cobre)
```

Nenhum campo novo (`deletedFromStatus` ou similar) Ã© necessÃ¡rio â€” mais simples que a tabela da
Rodada 3, nÃ£o mais complexo. Cobre exatamente os mesmos trÃªs casos, sÃ³ que por presenÃ§a de dado
em vez de status categÃ³rico (mais robusto a estados futuros que a Rodada 3 nÃ£o listou, ex.: um
`TIMEOUT` que teve evidÃªncia parcial de malware mas nunca de upload cai automaticamente no
segundo ramo, sem precisar de uma linha nova na tabela).

## ResoluÃ§Ã£o achado 1 (hold pÃ³s-claim) â€” fechamento estrutural, nÃ£o reduÃ§Ã£o de risco

Concordo que "reduzir a janela" nÃ£o Ã© fechar. Fechamento real: **toda escrita de `legalHold`
neste sistema, presente ou futura, Ã© obrigada pela convenÃ§Ã£o jÃ¡ existente e vinculante do prÃ³prio
repositÃ³rio (`AGENTS.md` Â§7: "toda escrita mutÃ¡vel usa os builders de `occ.ts` ... nunca
`UpdateItem`/`PutItem` cru") a passar por `buildVersionedUpdate`/OCC com `expectedVersion`** â€”
isso nÃ£o Ã© um mecanismo novo proposto por este design, Ã© a Ãºnica forma sancionada de escrever
qualquer campo em qualquer entidade deste projeto, jÃ¡ enforced por convenÃ§Ã£o de longa data (nenhum
outro campo de nenhuma entidade tem uma exceÃ§Ã£o a essa regra hoje).

ConsequÃªncia: o `claim` (achado 3 abaixo) jÃ¡ condiciona em `expectedVersion`. Um setter de hold
futuro que respeite a mesma convenÃ§Ã£o OCC obrigatÃ³ria tambÃ©m condiciona sua escrita em
`expectedVersion`. **As duas transaÃ§Ãµes nÃ£o podem ambas suceder sobre a mesma versÃ£o** â€” Ã©
exclusÃ£o mÃºtua por construÃ§Ã£o do OCC do projeto, nÃ£o por um handshake ad-hoc deste design:
- Se o claim commita primeiro (`version â†’ N+1`, `GSI6PK â†’ CLAIMED`), o setter de hold que leu a
  versÃ£o `N` falha seu prÃ³prio `TransactWriteItems` (`expectedVersion = N` nÃ£o bate mais),
  reforÃ§ando a mesma re-leitura que qualquer conflito de OCC jÃ¡ forÃ§a em todo o resto do sistema
  â€” cabe Ã  feature de hold (fora de escopo aqui) decidir o que fazer ao reler e achar
  `GSI6PK = WORKSTATE#PURGE_CLAIMED` (ex.: recusar o hold com erro "documento em purga", ou
  aceitar e emitir um alerta operacional) â€” **essa decisÃ£o de produto Ã© de W3-07/feature de hold,
  nÃ£o desta decisÃ£o**, mas o desenho aqui jÃ¡ torna a condiÃ§Ã£o de corrida estruturalmente
  impossÃ­vel, nÃ£o apenas provÃ¡vel de ser pega a tempo.
- Se o hold commita primeiro, o claim (`expectedVersion` desatualizado) falha e nunca toca S3.

Nenhuma leitura extra prÃ©-delete Ã© necessÃ¡ria (removida â€” era defesa supÃ©rflua uma vez que a
janela real jÃ¡ nÃ£o existe). **PendÃªncia textual explÃ­cita mantida**: a feature de hold futura deve
tratar o caso "hold perdeu a corrida de OCC contra um claim jÃ¡ commitado" â€” comportamento
especÃ­fico dela, fora do escopo Type 1 desta decisÃ£o.

## ResoluÃ§Ã£o achado 3 (API do builder inexequÃ­vel)

`buildVersionedUpdate` (`occ.ts`) ganha uma extensÃ£o aditiva e tipada (nÃ£o um fragmento de string
solto):

```typescript
export interface VersionedUpdateInput {
  // ...campos existentes inalterados...
  /** Extra ConditionExpression clauses ANDed to the base condition, each with its own
   * caller-supplied names/values. Caller is responsible for unique attribute name/value
   * placeholders that do not collide with the base ("#version"/":expectedVersion"/etc.) or with
   * `set`/`remove` keys - buildVersionedUpdate throws on any detected collision. */
  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
}
```

`buildVersionedUpdate` funde `names`/`values` de cada `extraConditions[i]` nos mapas jÃ¡ existentes
(lanÃ§ando erro descritivo se alguma chave colidir com `#set${i}`/`:set${i}`/`#rem${j}`/
`#version`/`:expectedVersion`/`#tenantId`/`:tenantId`/`#updatedAt`/`:now`/`:one` â€” checagem
mecÃ¢nica simples, mesma disciplina de "falha-fechado" do resto do projeto), e concatena
`AND (${expression})` para cada entrada Ã  `ConditionExpression` final. Uso concreto do claim:

```typescript
buildVersionedUpdate({
  tableName, key, tenantId, expectedVersion: doc.version,
  set: { GSI6PK: "WORKSTATE#PURGE_CLAIMED", GSI6SK: claimedGsi6Sk, purgeAttempts: (doc.purgeAttempts ?? 0) + 1 },
  extraConditions: [
    { expression: "attribute_not_exists(#legalHold) OR #legalHold = :false", names: { "#legalHold": "legalHold" }, values: { ":false": false } },
    { expression: "#status = :deleted", names: { "#status": "status" }, values: { ":deleted": "DELETED" } },
    { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": "WORKSTATE#PURGE_PENDING", ":expectedSk": doc.GSI6SK } },
    { expression: "purgeAfter <= :now", values: { ":now": nowIso } },
  ],
})
```

Cobre exatamente as invariantes que a Rodada 3 pediu, de forma mecanicamente segura (sem colisÃ£o
de placeholder), reusando o helper existente em vez de bifurcar a lÃ³gica de OCC do projeto.

## ResoluÃ§Ã£o achado 4 (estado terminal ausente)

Novo campo `purgeStatus?: "STUCK"` materializado no prÃ³prio item `Document` (nÃ£o sÃ³ mÃ©trica).
TransaÃ§Ã£o de reconciliaÃ§Ã£o de lease expirado passa a ter dois desfechos, nÃ£o um:
- `purgeAttempts < 5`: devolve a `WORKSTATE#PURGE_PENDING` (comportamento jÃ¡ descrito), incrementa
  nada aqui (o incremento Ã© sÃ³ no claim).
- `purgeAttempts >= 5`: `TransactWriteItems` condicionado (mesmo `expectedVersion`) que **remove**
  `GSI6PK`/`GSI6SK` (sai de ambas as filas, nunca mais reclaimado automaticamente) e seta
  `purgeStatus: "STUCK"`. Estado **reprocessÃ¡vel, nÃ£o descartado**: reverter exige uma aÃ§Ã£o
  administrativa explÃ­cita (fora de escopo implementar uma rota HTTP para isso agora â€” registrar
  como pendÃªncia textual, mesma disciplina de `principles.md` #1; a reversÃ£o manual imediata, se
  necessÃ¡ria antes de existir essa rota, Ã© um `UpdateItem` operacional Ãºnico feito por um humano
  com acesso real Ã  tabela, igual a qualquer break-glass hoje) que zera `purgeAttempts` e
  reescreve o ponteiro `PENDING`. Alarme dedicado (`document_purge_stuck_total > 0`) jÃ¡ proposto na
  Rodada 3 continua como o sinal que aciona essa aÃ§Ã£o manual â€” a diferenÃ§a agora Ã© que o estado
  em si sobrevive de forma consultÃ¡vel (`Scan`/relatÃ³rio futuro por `purgeStatus = STUCK`), nÃ£o sÃ³
  como uma mÃ©trica que pode se perder.
- Fencing da reconciliaÃ§Ã£o: a query de lease expirado (`WORKSTATE#PURGE_CLAIMED`, `GSI6SK < now`)
  sÃ³ devolve linhas cujo `GSI6SK` (que embute `claimExpiresAt`) jÃ¡ passou â€” a transaÃ§Ã£o de
  devoluÃ§Ã£o usa o mesmÃ­ssimo `expectedVersion`/`GSI6PK`/`GSI6SK` lidos nessa query como
  `extraConditions`, entÃ£o uma execuÃ§Ã£o antiga que "acorda" tarde e tenta finalizar depois de jÃ¡
  ter perdido a lease falha essa condiÃ§Ã£o (a reconciliaÃ§Ã£o jÃ¡ rodou e mudou a versÃ£o) â€” nunca
  duas transaÃ§Ãµes confirmam sobre o mesmo lease.

## ResoluÃ§Ã£o achado 5 (discriminador de recibo)

Descartado "ausÃªncia de `cleanObject`/`quarantineObject`" como discriminador (correto, Codex â€”
nÃ£o Ã© seguro). Usar `entityType` (atributo comum a toda entidade,
`data-model.md` Â§1: "Atributos comuns... `entityType`") â€” jÃ¡ presente em toda linha, incluindo
`DocumentPurgeReceipt`. O worker lÃª o item completo (GSI6 Ã© `ALL`-projected) e despacha por
`entityType` **antes** de montar a condiÃ§Ã£o de claim:

- `entityType === "Document"`: claim usa as `extraConditions` do achado 3 (`legalHold`,
  `status = DELETED`, GSI6 esperado, `purgeAfter`), depois S3 pela lÃ³gica do achado
  "status apagado" acima, depois `Delete Document` + `Put DocumentPurgeReceipt`.
- `entityType === "DocumentPurgeReceipt"`: claim usa `extraConditions` prÃ³pria (sem `status`,
  que este tipo nÃ£o tem â€” sÃ³ GSI6 esperado + `purgeAfter <= now`), sem nenhuma chamada S3, sÃ³
  `Delete` da prÃ³pria linha.

Dois branches tipados dentro do mesmo `DocumentPurgeWorker`/mesma invocaÃ§Ã£o agendada â€” "mesmo
worker" quer dizer mesmo deployable/mesma Lambda/mesmo agendamento, nunca "mesma funÃ§Ã£o de claim
sem diferenciaÃ§Ã£o de tipo" (a ambiguidade que a Rodada 3 apontou corretamente).

## Estado do design apÃ³s Rodada 4

Todos os 5 achados bloqueantes da Rodada 3 endereÃ§ados com mudanÃ§a concreta e verificÃ¡vel contra
o cÃ³digo real. PeÃ§o reavaliaÃ§Ã£o â€” se algo ainda nÃ£o fechar para â‰¥9.0, preciso do apontamento
exato, idealmente jÃ¡ indicando se Ã© um achado genuinamente novo ou uma reabertura.

/**
 * Logical deletion â€” M6 design Â§3.6 / blueprint Â§12.6. Version-conditioned, idempotent,
 * never physically deletes the object here (physical purge is a separate, later step driven
 * by `retentionClass`/`purgeAfter` â€” out of scope for M6's acceptance criteria beyond marking
 * the document DELETED and scheduling the clock).
 */
import type { RequestContext } from "../../identity/domain/request-context.js";
import { authorize } from "../../identity/domain/authorization.js";
import { NotFoundError, ConflictError } from "../../../shared/errors/app-error.js";
import { buildVersionedUpdate, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
import { documentKey, type Document } from "../domain/document.js";
import { computeUserDocumentPurgeAfter } from "../domain/retention.js";
import type { DocumentStore } from "../ports/document-store.js";

export interface DocumentDeletionServiceDeps {
  store: DocumentStore;
  tableName: string;
  now?: () => string;
}

const TERMINAL_ALREADY = new Set(["DELETED"]);

export class DocumentDeletionService {
  private readonly store: DocumentStore;
  private readonly tableName: string;
  private readonly now: () => string;

  constructor(deps: DocumentDeletionServiceDeps) {
    this.store = deps.store;
    this.tableName = deps.tableName;
    this.now = deps.now ?? (() => new Date().toISOString());
  }

  async deleteDocument(ctx: RequestContext, itemId: string, documentId: string): Promise<void> {
    authorize({ context: ctx, action: "document:delete", resource: { tenantId: ctx.tenant.tenantId } });

    const key = documentKey(ctx.tenant.tenantId, itemId, documentId);
    const doc = await this.store.get<Document>(key, true);
    if (!doc) throw new NotFoundError("Document not found.", { itemId, documentId });
    if (TERMINAL_ALREADY.has(doc.status)) return; // idempotent: already deleted.

    const now = this.now();
    try {
      await this.store.transactWrite([
        {
          Update: buildVersionedUpdate({
            tableName: this.tableName,
            key,
            tenantId: ctx.tenant.tenantId,
            expectedVersion: doc.version,
            set: { status: "DELETED", deletedAt: now, purgeAfter: computeUserDocumentPurgeAfter(now) },
          }),
        },
      ]);
    } catch (err) {
      if (isTransactionCanceled(err)) {
        throw new ConflictError("Document was modified concurrently; retry with the current version.", { itemId, documentId });
      }
      throw err;
    }
  }
}

/**
 * Optimistic concurrency control helper - implementation-blueprint.md #5.2 / M0 deliverable.
 *
 * M0 ships no tables/Lambdas yet (that's M1+), so this module only builds the DynamoDB
 * command *parameters* for OCC-safe writes; it does not depend on @aws-sdk/client-dynamodb
 * so it stays testable without AWS credentials/mocks. M1 wires these builders into the
 * actual DocumentClient calls once tables exist.
 */

export interface EntityKey {
  PK: string;
  SK: string;
}

export interface VersionedUpdateInput {
  tableName: string;
  key: EntityKey;
  tenantId: string;
  expectedVersion: number;
  /** Additional SET clauses beyond version/updatedAt, e.g. { status: "CANCELLED" }. */
  set: Record<string, unknown>;
  /** Attribute names to REMOVE atomically in the same conditional update - e.g. dropping
   * GSI6PK/GSI6SK when a reconciliation-candidate pointer (M3.5) stops applying. Gap noted
   * since M3 (occ.ts was SET-only, cancelled GSI3 pointers were left orphaned because
   * nothing actively queried them); M3.5 needs REMOVE for real because GSI6 pointers ARE
   * actively queried by reconciliation. */
  remove?: string[];
  now?: string;
}

export interface DynamoUpdateCommandInput {
  TableName: string;
  Key: EntityKey;
  UpdateExpression: string;
  ConditionExpression: string;
  ExpressionAttributeNames: Record<string, string>;
  ExpressionAttributeValues: Record<string, unknown>;
}

/**
 * Builds an UpdateItem input enforcing implementation-blueprint.md #5.2's exact
 * ConditionExpression: attribute_exists(PK) AND attribute_exists(SK) AND
 * #version = :expectedVersion AND #tenantId = :tenantId, and increments version atomically.
 */
export function buildVersionedUpdate(input: VersionedUpdateInput): DynamoUpdateCommandInput {
  const now = input.now ?? new Date().toISOString();

  const names: Record<string, string> = {
    "#version": "version",
    "#tenantId": "tenantId",
    "#updatedAt": "updatedAt",
  };
  const values: Record<string, unknown> = {
    ":expectedVersion": input.expectedVersion,
    ":tenantId": input.tenantId,
    ":one": 1,
    ":now": now,
  };

  const setClauses = ["#version = #version + :one", "#updatedAt = :now"];
  let i = 0;
  for (const [field, value] of Object.entries(input.set)) {
    const nameKey = `#set${i}`;
    const valueKey = `:set${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    setClauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  const removeClauses: string[] = [];
  let j = 0;
  for (const field of input.remove ?? []) {
    const nameKey = `#rem${j}`;
    names[nameKey] = field;
    removeClauses.push(nameKey);
    j += 1;
  }

  const expression =
    removeClauses.length > 0
      ? `SET ${setClauses.join(", ")} REMOVE ${removeClauses.join(", ")}`
      : `SET ${setClauses.join(", ")}`;

  return {
    TableName: input.tableName,
    Key: input.key,
    UpdateExpression: expression,
    ConditionExpression:
      "attribute_exists(PK) AND attribute_exists(SK) AND #version = :expectedVersion AND #tenantId = :tenantId",
    ExpressionAttributeNames: names,
    ExpressionAttributeValues: values,
  };
}

export interface DynamoPutCommandInput {
  TableName: string;
  Item: Record<string, unknown>;
  ConditionExpression: string;
}

/**
 * Builds a PutItem input for first-time creation of a versioned entity, per
 * implementation-blueprint.md #5.2: ConditionExpression attribute_not_exists(PK) AND
 * attribute_not_exists(SK). Caller supplies `item` already containing PK/SK/tenantId/version=1.
 */
export function buildVersionedCreate(tableName: string, item: Record<string, unknown> & EntityKey): DynamoPutCommandInput {
  return {
    TableName: tableName,
    Item: item,
    ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
  };
}

/**
 * TransactWriteItems entry shapes - moved here from expiration/ports/expiration-store.ts
 * (2026-08-19, Engineering Maturity Review): that was the only place they were declared,
 * but reminder/ports/reminder-store.ts already re-exported them from there (a cross-module
 * ports->ports dependency), and expiration/domain/audit-event.ts imported them directly,
 * which is a domain->ports boundary violation dependency-cruiser catches but ESLint's
 * text-matching no-restricted-imports rule cannot (it only matches literal specifier
 * substrings, and this codebase's relative imports never contain "modules/"). Reuses the
 * inner Put/Update shapes already defined above instead of re-declaring them a third time.
 */
export interface TransactPutEntry {
  Put: DynamoPutCommandInput;
}

export interface TransactUpdateEntry {
  Update: DynamoUpdateCommandInput;
}

export interface DynamoConditionCheckCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression: string;
  ExpressionAttributeNames?: Record<string, string>;
  ExpressionAttributeValues?: Record<string, unknown>;
}

export interface TransactConditionCheckEntry {
  ConditionCheck: DynamoConditionCheckCommandInput;
}

export interface DynamoDeleteCommandInput {
  TableName: string;
  Key: EntityKey;
  ConditionExpression?: string;
}

export interface TransactDeleteEntry {
  Delete: DynamoDeleteCommandInput;
}

export type TransactWriteEntry = TransactPutEntry | TransactUpdateEntry | TransactConditionCheckEntry | TransactDeleteEntry;

/**
 * Builds a TransactWriteItems `ConditionCheck` entry asserting a row is still at an
 * expected version (and, optionally, still holds a given attribute value) - used to fence
 * a commit against a fact read earlier in the same operation but not itself written by
 * this transaction (e.g. reminder dispatch asserting the ReminderPolicy it read is still
 * enabled at the version it read, per the BLOCKER-B freshness-fencing fix: reading a row
 * and later writing elsewhere based on that read is a TOCTOU gap unless the read fact is
 * re-asserted atomically inside the same TransactWriteItems as the write it gates).
 */
export function buildVersionConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  expectedVersion: number;
  /** Extra attribute=value equality conditions beyond version, e.g. { enabled: true, status: "ACTIVE" }. */
  extra?: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = { "#version": "version" };
  const values: Record<string, unknown> = { ":version": input.expectedVersion };
  const clauses = ["#version = :version"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra ?? {})) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/**
 * Sibling of `buildVersionConditionCheck` for facts that have no version to pin - e.g.
 * BLOCKER-B's ITEM-scoped-policy integrity check (a policy references an item's
 * existence/status/tenant, not a specific item version - referencing an item doesn't pin
 * it to a version the way an occurrence's materialization does). `extra` must include at
 * least one condition (an existence-only check with no `extra` would degenerate to
 * `attribute_exists(PK)`, which every real row satisfies trivially - not useful on its own).
 */
export function buildExistenceConditionCheck(input: {
  tableName: string;
  key: EntityKey;
  extra: Record<string, unknown>;
}): TransactConditionCheckEntry {
  const names: Record<string, string> = {};
  const values: Record<string, unknown> = {};
  const clauses = ["attribute_exists(PK)"];

  let i = 0;
  for (const [field, value] of Object.entries(input.extra)) {
    const nameKey = `#c${i}`;
    const valueKey = `:c${i}`;
    names[nameKey] = field;
    values[valueKey] = value;
    clauses.push(`${nameKey} = ${valueKey}`);
    i += 1;
  }

  return {
    ConditionCheck: {
      TableName: input.tableName,
      Key: input.key,
      ConditionExpression: clauses.join(" AND "),
      ExpressionAttributeNames: names,
      ExpressionAttributeValues: values,
    },
  };
}

/** Name of the AWS SDK error thrown when any entry in a TransactWriteItems call fails its
 * ConditionExpression - used by callers to distinguish OCC/idempotency conflicts from other
 * DynamoDB errors without importing the SDK type. */
export const TRANSACTION_CANCELED = "TransactionCanceledException";

export function isTransactionCanceled(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === TRANSACTION_CANCELED
  );
}

/** Name of the AWS SDK error thrown when a ConditionExpression fails - used by callers
 * to distinguish OCC conflicts from other DynamoDB errors without importing the SDK type. */
export const CONDITIONAL_CHECK_FAILED = "ConditionalCheckFailedException";

export function isConditionalCheckFailed(err: unknown): boolean {
  return (
    typeof err === "object" &&
    err !== null &&
    "name" in err &&
    (err as { name?: unknown }).name === CONDITIONAL_CHECK_FAILED
  );
}


codex
A primeira leitura confirma que o soft-delete só faz `SET` de `status`, `deletedAt` e `purgeAfter`; ele não remove os campos de evidência. Agora estou verificando duas questões mais sutis: se a API proposta compõe expressões DynamoDB válidas em todos os casos e se “OCC obrigatório” protege contra um legal hold iniciado depois do claim — que é diferente de impedir dois commits sobre a mesma versão.
2026-08-28T17:50:02.623700Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md'; Select-String -Path "'$p -Pattern '"'"'^## |''^### |legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt'"' -Context 2,5; Select-String -Path docs/architecture/data-model.md -Pattern 'Atributos comuns|entityType' -Context 2,8; Get-Content -Raw src/modules/document/domain/document.ts; rg -n \"legalHold|buildVersionedUpdate\\(|entityType.*Document|DocumentPurgeReceipt|cleanObject|uploadEvidence|malwareEvidence\" src test docs/architecture -g \"*.ts\" -g \"*.md\""` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md'; Select-String -Path "'$p -Pattern '"'"'^## |''^### |legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt'"' -Context 2,5; Select-String -Path docs/architecture/data-model.md -Pattern 'Atributos comuns|entityType' -Context 2,8; Get-Content -Raw src/modules/document/domain/document.ts; rg -n \"legalHold|buildVersionedUpdate\\(|entityType.*Document|DocumentPurgeReceipt|cleanObject|uploadEvidence|malwareEvidence\" src test docs/architecture -g \"*.ts\" -g \"*.md\"" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p='"'docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md'; Select-String -Path "'$p -Pattern '"'"'^## |''^### |legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt'"' -Context 2,5; Select-String -Path docs/architecture/data-model.md -Pattern 'Atributos comuns|entityType' -Context 2,8; Get-Content -Raw src/modules/document/domain/document.ts; rg -n \"legalHold|buildVersionedUpdate\\(|entityType.*Document|DocumentPurgeReceipt|cleanObject|uploadEvidence|malwareEvidence\" src test docs/architecture -g \"*.ts\" -g \"*.md\""` rejected: blocked by policy
2026-08-28T17:50:09.457510Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md -Pattern 'legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt' -Context 2,5"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md -Pattern 'legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt' -Context 2,5" in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/data-model.md -Pattern 'Atributos comuns|entityType' -Context 2,8; Get-Content -Raw src/modules/document/domain/document.ts" in C:\Users\Usuario\Desktop\projects\expiration-tracker
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'legalHold|buildVersionedUpdate\\(|entityType.*Document|DocumentPurgeReceipt|cleanObject|uploadEvidence|malwareEvidence' src test docs/architecture -g '*.ts' -g '*.md'" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md -Pattern 'legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt' -Context 2,5"` rejected: blocked by policy
2026-08-28T17:50:09.481606Z ERROR codex_core::tools::router: error=`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/data-model.md -Pattern 'Atributos comuns|entityType' -Context 2,8; Get-Content -Raw src/modules/document/domain/document.ts"` rejected: blocked by policy
 declined in 0ms:
`"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path docs/architecture/data-model.md -Pattern 'Atributos comuns|entityType' -Context 2,8; Get-Content -Raw src/modules/document/domain/document.ts"` rejected: blocked by policy
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -C 4 "legalHold|cleanObject|uploadEvidence|malwareEvidence|entityType|extraConditions|DocumentPurgeReceipt" docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md docs/architecture/data-model.md src/modules/document/domain/document.ts src test' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 242ms:
src/modules/document/domain/document.ts-2- * Document — data-model.md line 34: PK=`TENANT#t#ITEM#i`, SK=`DOC#d`. State set matches the
src/modules/document/domain/document.ts-3- * approved data model exactly: PENDING_UPLOAD/SCANNING/CLEAN/REJECTED/UNSUPPORTED/TIMEOUT/
src/modules/document/domain/document.ts-4- * DELETED (implementation-blueprint.md §12.2-12.3; TIMEOUT is driven by
src/modules/document/domain/document.ts-5- * UploadSlotReconciliationWorker for scans that never resolve, not by the malware/finalizer
src/modules/document/domain/document.ts:6: * workers directly). M6 design §2.1 adds uploadEvidence/malwareEvidence (independent evidence
src/modules/document/domain/document.ts-7- * fields, never presuming which arrives first - see document-state-machine.ts) and
src/modules/document/domain/document.ts-8- * retentionClass/purgeAfter (privacy-lgpd.md §4 USER_DOCUMENT class materialized on the entity
src/modules/document/domain/document.ts-9- * from day one, not bolted on later).
src/modules/document/domain/document.ts-10- */
--
src/modules/document/domain/document.ts-28-  observedAt: string;
src/modules/document/domain/document.ts-29-}
src/modules/document/domain/document.ts-30-
src/modules/document/domain/document.ts-31-export interface Document extends EntityKey {
src/modules/document/domain/document.ts:32:  entityType: "Document";
src/modules/document/domain/document.ts-33-  tenantId: string;
src/modules/document/domain/document.ts-34-  itemId: string;
src/modules/document/domain/document.ts-35-  documentId: string;
src/modules/document/domain/document.ts-36-  uploadSlotId: string;
--
src/modules/document/domain/document.ts-40-  checksumSha256: string;
src/modules/document/domain/document.ts-41-  status: DocumentStatus;
src/modules/document/domain/document.ts-42-  quarantineObject: DocumentObjectReference;
src/modules/document/domain/document.ts-43-  /** Only present once promoted - never populated before status === "CLEAN". */
src/modules/document/domain/document.ts:44:  cleanObject?: DocumentObjectReference;
src/modules/document/domain/document.ts:45:  uploadEvidence?: UploadEvidence;
src/modules/document/domain/document.ts:46:  malwareEvidence?: MalwareEvidence;
src/modules/document/domain/document.ts-47-  extractionRunId?: string; // M7, unused by M6, kept for schema forward-compatibility only.
src/modules/document/domain/document.ts-48-  retentionClass: Extract<DocumentRetentionClass, "USER_DOCUMENT">;
src/modules/document/domain/document.ts-49-  purgeAfter?: string; // set only once a terminal/deletion event establishes the clock.
src/modules/document/domain/document.ts-50-  version: number;
--
docs/architecture/data-model.md-17-`tenantId` obrigatório em toda chave DynamoDB, idempotency key, objeto S3, evento e mensagem (SCALE-004). MVP: `tenantId=userId`; futuro: `tenantId=organizationId`. Identidade autenticável global no IdP (Cognito); `User` representa o perfil dentro do tenant, com `identitySubject` desacoplado do `sub` do Cognito (decisão §4 da arquitetura).
docs/architecture/data-model.md-18-
docs/architecture/data-model.md-19-Agregados: **Tenant/Access** (Organization, User, Membership) · **Expiration** (ExpirationItem raiz; ReminderPolicy, ReminderOccurrence, Document, ExtractedField referenciam item e sua versão) · **Notification** (NotificationIntent como efeito lógico único; NotificationAttempt por interação) · **Integration** (Channel, Provider, WebhookInbox, UploadSlot) · **Compliance** (AuditEvent, append-only).
docs/architecture/data-model.md-20-
docs/architecture/data-model.md:21:Convenção física: `PK = TENANT#<tenantId>#<aggregate>`, `SK = <entityType>#<id>[#...]`. IDs UUIDv7/ULID (ordenáveis por tempo, evitam hot-key sequencial de auto-incremento). Datas ISO-8601 UTC; timezones IANA. Atributos comuns a toda entidade (design-target, ver nota de status abaixo): `entityType`, `schemaVersion`, `createdAt`, `updatedAt`, `version`, `deletedAt?`, `retentionClass`, `purgeAfter?`.
docs/architecture/data-model.md-22-
docs/architecture/data-model.md-23-**Status de implementação (adicionado full-audit round1/eixo Privacidade, 2026-08-20):** `retentionClass`/`purgeAfter` são compromisso de design (`docs/architecture/privacy-lgpd.md` §4), não implementação atual — nenhuma entidade em `src/` carrega esses dois campos ainda (M0-M3 não os materializou), e o GSI6 físico (`infra/lib/dynamo-table.ts`) está hoje restrito por IAM exclusivamente a `ReminderReconciliation`/`OutboxSweeperReminderDispatch`, sem papel de purge sancionado. Registrar aqui explicitamente para não ler este parágrafo como "já implementado" — trabalho de M4+ (worker de purge, migração de escrita para popular os campos, ampliação da policy de GSI6) fica pendente e rastreado por este parágrafo, não por drift silencioso.
docs/architecture/data-model.md-24-
docs/architecture/data-model.md-25-## 2. Entidades e armazenamento
--
docs/architecture/data-model.md-60-
docs/architecture/data-model.md-61-## 3. GSIs (todos com chave iniciando por `TENANT#tenantId`, exceto GSI3 — ver exceção documentada abaixo)
docs/architecture/data-model.md-62-- **GSI1 — vencimentos/dashboard**: `PK=TENANT#t#ITEMSTATUS#<status>`, `SK=DUE#<dueDate>#ITEM#i`. Lista próximos/vencidos/arquivados.
docs/architecture/data-model.md-63-- **GSI2 — responsável/categoria**: `PK=TENANT#t#ASSIGNEE#u` ou `TENANT#t#CATEGORY#c`, `SK=DUE#<dueDate>#ITEM#i`. Índices separados se ambos forem padrões frequentes de acesso.
docs/architecture/data-model.md:64:- **GSI3 — scheduler global de ocorrências agendadas** *(entendimento atualizado em M10/D-039/D-046, `roadmap-evolution/12-automated-chasing-capacity-review.md` — mudança de documentação, não de mecanismo)*: `PK=GSI3PK=DUE#yyyyMMddHHmm#NN`, `SK=GSI3SK` discriminado por `entityType` na própria forma da chave — `TENANT#t#OCCURRENCE#<occurrenceId>` para `ReminderOccurrence` (formato original, zero migração das linhas já em produção) e `TENANT#t#CHASING#<occurrenceId>` para `DocumentChasingOccurrence` (M10 cluster 4, agregado-irmão — nunca generaliza `ReminderOccurrence`). 4 shards/minuto inicial (decisão Fase 3 Rodada 4); produtor condiciona por status/versão, branca por `entityType` ao processar cada linha lida, e falha-fechado com alarme se a forma da SK não corresponder a nenhum discriminador conhecido. **Correção Type 1 (ratificada em `implementation-blueprint.md` §9.2/§9.3, decisions-log.md D-028)**: a chave original (`PK=TENANT#t#DUE#...`) não é consultável pelo `ReminderProducer` — `Query` do DynamoDB exige PK exata, e o producer do minuto `M` não sabe a priori quais tenants têm ocorrência em `M`. GSI3 é portanto a **única exceção** à regra "toda chave começa com `TENANT#tenantId`": a PK é global (sem `tenantId`), com `tenantId` preservado na SK e no item base para reconstrução segura do contexto. Salvaguardas obrigatórias (detalhadas em `implementation-blueprint.md` §9.2, mantidas para os dois `entityType`): IAM do índice restrito exclusivamente ao producer (nenhuma rota tenant-facing tem `Query` neste GSI); projeção mínima, sem conteúdo de negócio sensível; teste de isolamento automatizado provando que nenhuma API tenant-facing o alcança, direta ou indiretamente; dimensionamento de shards por pico global, não por tenant; alarmes segmentados por `entityType` (D-046) para que um pico anômalo de um tipo não se esconda no agregado do outro.
docs/architecture/data-model.md-65-- **GSI4 — membership por usuário**: `PK=TENANT#t#USER#u`, `SK=ORG#o#MEMBERSHIP#m`.
docs/architecture/data-model.md-66-- **GSI5 — provider callback**: `PK=TENANT#t#PROVIDER#p`, `SK=MSG#<providerMessageId>`. Localiza a tentativa correta a partir de webhook de delivery/bounce.
docs/architecture/data-model.md-67-- **GSI6 — retenção/reconciliação**: `PK=TENANT#t#PURGE#yyyyMM` ou `...#WORKSTATE#<status>`, `SK=<purgeAfter/expiresAt>#<type>#<id>`. Suporta purge, upload slots vencidos, inbox/outbox e jobs pendentes num índice único. TTL é limpeza auxiliar, nunca gatilho operacional (evita depender de timing assíncrono do TTL para correção — mesma lição do reconciliador de upload da Fase 3 Rodada 6).
docs/architecture/data-model.md-68-- **GSI7 — listagem de `TrackedSubject`** *(M9, D-036)*: `PK=TENANT#t#SUBJECTSTATUS#<status>`, `SK=TYPE#<type>#NAME#<displayNameNormalized>#SUBJECT#<subjectId>`. Escopo único (só listagem de subject por status/tipo/nome — decisão explícita de não misturar com outro access pattern no mesmo índice, `roadmap-evolution/03-...md` rodada 2). Tenant-scoped, entra na política geral `tenant_facing_read_write`/`tenant_facing_read`, nunca isolado como GSI3/GSI6. `RequirementAssignment` não usa GSI novo — é coleção sob a partição do subject (ver §2).
--
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-3-## Resolução achado "novo" (status apagado pela exclusão lógica)
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-4-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-5-Correto — `document-deletion-service.ts:51` sobrescreve `status` para `DELETED`, então a tabela
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-6-por-status da Rodada 3 é inexequível como escrita. **Correção: não depender de `status` anterior
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:7:nenhum.** `cleanObject`/`uploadEvidence`/`malwareEvidence` já sobrevivem à exclusão lógica sem
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-8-qualquer mudança (nenhum código os limpa em `deleteDocument`), então são discriminadores
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-9-suficientes por si:
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-10-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-11-```
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:12:if (doc.cleanObject) → deleteObjectVersion(doc.cleanObject)               // foi promovido a CLEAN
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:13:else if (uploadEvidence?.object ?? malwareEvidence?.object) → deleteObjectVersion(esse objeto)  // teve evidência real, nunca promovido (REJECTED/UNSUPPORTED)
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-14-else → nada a apagar por versão explícita (nunca chegou evidência; lifecycle 24h de quarantine já cobre)
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-15-```
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-16-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-17-Nenhum campo novo (`deletedFromStatus` ou similar) é necessário — mais simples que a tabela da
--
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-21-segundo ramo, sem precisar de uma linha nova na tabela).
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-22-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-23-## Resolução achado 1 (hold pós-claim) — fechamento estrutural, não redução de risco
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-24-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:25:Concordo que "reduzir a janela" não é fechar. Fechamento real: **toda escrita de `legalHold`
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-26-neste sistema, presente ou futura, é obrigada pela convenção já existente e vinculante do próprio
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-27-repositório (`AGENTS.md` §7: "toda escrita mutável usa os builders de `occ.ts` ... nunca
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-28-`UpdateItem`/`PutItem` cru") a passar por `buildVersionedUpdate`/OCC com `expectedVersion`** —
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-29-isso não é um mecanismo novo proposto por este design, é a única forma sancionada de escrever
--
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-60-  /** Extra ConditionExpression clauses ANDed to the base condition, each with its own
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-61-   * caller-supplied names/values. Caller is responsible for unique attribute name/value
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-62-   * placeholders that do not collide with the base ("#version"/":expectedVersion"/etc.) or with
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-63-   * `set`/`remove` keys - buildVersionedUpdate throws on any detected collision. */
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:64:  extraConditions?: Array<{ expression: string; names?: Record<string, string>; values?: Record<string, unknown> }>;
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-65-}
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-66-```
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-67-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:68:`buildVersionedUpdate` funde `names`/`values` de cada `extraConditions[i]` nos mapas já existentes
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-69-(lançando erro descritivo se alguma chave colidir com `#set${i}`/`:set${i}`/`#rem${j}`/
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-70-`#version`/`:expectedVersion`/`#tenantId`/`:tenantId`/`#updatedAt`/`:now`/`:one` — checagem
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-71-mecânica simples, mesma disciplina de "falha-fechado" do resto do projeto), e concatena
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-72-`AND (${expression})` para cada entrada à `ConditionExpression` final. Uso concreto do claim:
--
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-74-```typescript
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-75-buildVersionedUpdate({
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-76-  tableName, key, tenantId, expectedVersion: doc.version,
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-77-  set: { GSI6PK: "WORKSTATE#PURGE_CLAIMED", GSI6SK: claimedGsi6Sk, purgeAttempts: (doc.purgeAttempts ?? 0) + 1 },
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:78:  extraConditions: [
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:79:    { expression: "attribute_not_exists(#legalHold) OR #legalHold = :false", names: { "#legalHold": "legalHold" }, values: { ":false": false } },
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-80-    { expression: "#status = :deleted", names: { "#status": "status" }, values: { ":deleted": "DELETED" } },
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-81-    { expression: "GSI6PK = :expectedPk AND GSI6SK = :expectedSk", values: { ":expectedPk": "WORKSTATE#PURGE_PENDING", ":expectedSk": doc.GSI6SK } },
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-82-    { expression: "purgeAfter <= :now", values: { ":now": nowIso } },
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-83-  ],
--
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-106-  como uma métrica que pode se perder.
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-107-- Fencing da reconciliação: a query de lease expirado (`WORKSTATE#PURGE_CLAIMED`, `GSI6SK < now`)
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-108-  só devolve linhas cujo `GSI6SK` (que embute `claimExpiresAt`) já passou — a transação de
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-109-  devolução usa o mesmíssimo `expectedVersion`/`GSI6PK`/`GSI6SK` lidos nessa query como
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:110:  `extraConditions`, então uma execução antiga que "acorda" tarde e tenta finalizar depois de já
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-111-  ter perdido a lease falha essa condição (a reconciliação já rodou e mudou a versão) — nunca
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-112-  duas transações confirmam sobre o mesmo lease.
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-113-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-114-## Resolução achado 5 (discriminador de recibo)
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-115-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:116:Descartado "ausência de `cleanObject`/`quarantineObject`" como discriminador (correto, Codex —
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:117:não é seguro). Usar `entityType` (atributo comum a toda entidade,
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:118:`data-model.md` §1: "Atributos comuns... `entityType`") — já presente em toda linha, incluindo
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:119:`DocumentPurgeReceipt`. O worker lê o item completo (GSI6 é `ALL`-projected) e despacha por
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:120:`entityType` **antes** de montar a condição de claim:
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-121-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:122:- `entityType === "Document"`: claim usa as `extraConditions` do achado 3 (`legalHold`,
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-123-  `status = DELETED`, GSI6 esperado, `purgeAfter`), depois S3 pela lógica do achado
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:124:  "status apagado" acima, depois `Delete Document` + `Put DocumentPurgeReceipt`.
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md:125:- `entityType === "DocumentPurgeReceipt"`: claim usa `extraConditions` própria (sem `status`,
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-126-  que este tipo não tem — só GSI6 esperado + `purgeAfter <= now`), sem nenhuma chamada S3, só
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-127-  `Delete` da própria linha.
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-128-
docs/architecture/reviews/w3-06-user-document-purge-design/claude-proposal-round4.md-129-Dois branches tipados dentro do mesmo `DocumentPurgeWorker`/mesma invocação agendada — "mesmo
--
test\integration\reminder-engine.test.ts-56-    // Seed the ExpirationItem directly (M2 already proves the item lifecycle end-to-end;
test\integration\reminder-engine.test.ts-57-    // M3's scope is the Reminder Engine reacting to an ACTIVE item + policy).
test\integration\reminder-engine.test.ts-58-    await store.putIfAbsent({
test\integration\reminder-engine.test.ts-59-      ...itemKey(TENANT, ITEM_ID),
test\integration\reminder-engine.test.ts:60:      entityType: "ExpirationItem",
test\integration\reminder-engine.test.ts-61-      itemId: ITEM_ID,
test\integration\reminder-engine.test.ts-62-      tenantId: TENANT,
test\integration\reminder-engine.test.ts-63-      status: "ACTIVE",
test\integration\reminder-engine.test.ts-64-      dueDate: "2026-09-10T00:00:00.000Z",
--
test\integration\reminder-engine.test.ts-144-    // must not create a second intent.
test\integration\reminder-engine.test.ts-145-    const duplicateOutcome = await dispatchOccurrence(dispatchDeps, command);
test\integration\reminder-engine.test.ts-146-    expect(duplicateOutcome.kind).toBe("ALREADY_TRIGGERED");
test\integration\reminder-engine.test.ts-147-
test\integration\reminder-engine.test.ts:148:    const allIntents = store.allItems().filter((i) => i["entityType"] === "NotificationIntent");
test\integration\reminder-engine.test.ts-149-    expect(allIntents).toHaveLength(1);
test\integration\reminder-engine.test.ts-150-
test\integration\reminder-engine.test.ts-151-    // Outbox: exactly one reminder.policy-changed.v1 (BLOCKER-B, written by createPolicy),
test\integration\reminder-engine.test.ts-152-    // one durable ReminderDispatchRequested (M3.5, written by the producer's claim), and
test\integration\reminder-engine.test.ts-153-    // one notification.intent-created.v1 (M3, written by dispatch).
test\integration\reminder-engine.test.ts:154:    const outboxEvents = store.allItems().filter((i) => i["entityType"] === "OutboxEvent");
test\integration\reminder-engine.test.ts-155-    expect(outboxEvents).toHaveLength(3);
test\integration\reminder-engine.test.ts-156-    const policyChanged = outboxEvents.filter((e) => e["eventType"] === "reminder.policy-changed.v1");
test\integration\reminder-engine.test.ts-157-    expect(policyChanged).toHaveLength(1);
test\integration\reminder-engine.test.ts-158-    const dispatchRequested = outboxEvents.filter((e) => e["eventType"] === "ReminderDispatchRequested");
--
test\integration\reminder-engine.test.ts-286-    expect(outcome.kind).toBe("CANCELLED_STALE");
test\integration\reminder-engine.test.ts-287-
test\integration\reminder-engine.test.ts-288-    const row = await store.get<ReminderOccurrence>({ PK: occurrence.PK, SK: occurrence.SK });
test\integration\reminder-engine.test.ts-289-    expect(row?.status).toBe("CANCELLED");
test\integration\reminder-engine.test.ts:290:    const intents = store.allItems().filter((i) => i["entityType"] === "NotificationIntent");
test\integration\reminder-engine.test.ts-291-    expect(intents).toHaveLength(0);
test\integration\reminder-engine.test.ts-292-  });
test\integration\reminder-engine.test.ts-293-});
--
test\contract\schemas.test.ts-260-  it("accepts a valid WebhookInbox record", () => {
test\contract\schemas.test.ts-261-    const { valid, errors } = registry.validate(
test\contract\schemas.test.ts-262-      "https://expiration-tracker/schemas/api/webhook-inbox.v1.json",
test\contract\schemas.test.ts-263-      {
test\contract\schemas.test.ts:264:        entityType: "WebhookInbox",
test\contract\schemas.test.ts-265-        provider: "provider_a",
test\contract\schemas.test.ts-266-        providerAccountId: "acct_01",
test\contract\schemas.test.ts-267-        providerEventId: "provider_event_987",
test\contract\schemas.test.ts-268-        eventKind: "DELIVERY_STATUS",
--
test\contract\schemas.test.ts-530-  it("rejects a WebhookInbox record with signatureVerified=false (must never be persisted unverified)", () => {
test\contract\schemas.test.ts-531-    const { valid } = registry.validate(
test\contract\schemas.test.ts-532-      "https://expiration-tracker/schemas/api/webhook-inbox.v1.json",
test\contract\schemas.test.ts-533-      {
test\contract\schemas.test.ts:534:        entityType: "WebhookInbox",
test\contract\schemas.test.ts-535-        provider: "provider_a",
test\contract\schemas.test.ts-536-        providerAccountId: "acct_01",
test\contract\schemas.test.ts-537-        providerEventId: "provider_event_987",
test\contract\schemas.test.ts-538-        eventKind: "DELIVERY_STATUS",
--
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-45-  function policy(): ReminderPolicy {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-46-    return {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-47-      PK: `TENANT#${TENANT}#POLICY#p1`,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-48-      SK: "META",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:49:      entityType: "ReminderPolicy",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-50-      policyId: "p1",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-51-      tenantId: TENANT,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-52-      scope: "ITEM",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-53-      itemId: ITEM_ID,
--
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-67-
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-68-    await store.putIfAbsent({
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-69-      PK: `TENANT#${TENANT}#ITEM#${ITEM_ID}`,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-70-      SK: "META",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:71:      entityType: "ExpirationItem",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-72-      itemId: ITEM_ID,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-73-      tenantId: TENANT,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-74-      status: "ACTIVE",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-75-      dueDate: "2026-09-10T00:00:00.000Z",
--
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-178-
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-179-    await store.putIfAbsent({
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-180-      PK: `TENANT#${TENANT}#ITEM#${itemId}`,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-181-      SK: "META",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:182:      entityType: "ExpirationItem",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-183-      itemId,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-184-      tenantId: TENANT,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-185-      status: "ACTIVE",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-186-      dueDate: "2026-09-10T00:00:00.000Z",
--
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-190-    const occurrenceId = "occ_expired";
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-191-    await store.putIfAbsent({
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-192-      PK: `TENANT#${TENANT}#ITEM#${itemId}`,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-193-      SK: `OCC#2026-09-10T12:00:00.000Z#${occurrenceId}`,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:194:      entityType: "ReminderOccurrence",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-195-      occurrenceId,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-196-      tenantId: TENANT,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-197-      itemId,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts-198-      policyId: "p1",
--
test\integration\expiration-lifecycle.test.ts-113-
test\integration\expiration-lifecycle.test.ts-114-    // Audit trail: append-only, one record per mutation (create, due-date update, renew's
test\integration\expiration-lifecycle.test.ts-115-    // two-sided audit, archive, delete). No reminder/notification entity is ever written.
test\integration\expiration-lifecycle.test.ts-116-    const allRecords = expirationStore.allItems();
test\integration\expiration-lifecycle.test.ts:117:    const auditRecords = allRecords.filter((r) => r["entityType"] === "AuditEvent");
test\integration\expiration-lifecycle.test.ts-118-    expect(auditRecords.length).toBeGreaterThanOrEqual(5);
test\integration\expiration-lifecycle.test.ts-119-    const reminderOrNotificationRecords = allRecords.filter(
test\integration\expiration-lifecycle.test.ts:120:      (r) => r["entityType"] === "ReminderOccurrence" || r["entityType"] === "NotificationIntent",
test\integration\expiration-lifecycle.test.ts-121-    );
test\integration\expiration-lifecycle.test.ts-122-    expect(reminderOrNotificationRecords).toHaveLength(0);
test\integration\expiration-lifecycle.test.ts-123-
test\integration\expiration-lifecycle.test.ts-124-    // BLOCKER-B: outbox events now cover the full item lifecycle, not just manual due-date
test\integration\expiration-lifecycle.test.ts-125-    // edits - item-due-date-changed.v1 fires on create (x2, source+renewed), the manual
test\integration\expiration-lifecycle.test.ts-126-    // edit (x1), and the renewed item's own creation (x1) = 3; item-deactivated.v1 fires on
test\integration\expiration-lifecycle.test.ts-127-    // the source item's RENEWED transition, the archive, and the delete = 3.
test\integration\expiration-lifecycle.test.ts:128:    const outboxEvents = allRecords.filter((r) => r["entityType"] === "OutboxEvent");
test\integration\expiration-lifecycle.test.ts-129-    expect(outboxEvents).toHaveLength(6);
test\integration\expiration-lifecycle.test.ts-130-    const dueDateChanged = outboxEvents.filter((e) => e["eventType"] === "expiration.item-due-date-changed.v1");
test\integration\expiration-lifecycle.test.ts-131-    const deactivated = outboxEvents.filter((e) => e["eventType"] === "expiration.item-deactivated.v1");
test\integration\expiration-lifecycle.test.ts-132-    expect(dueDateChanged).toHaveLength(3);
--
src\shared\outbox\outbox.ts-34-
src\shared\outbox\outbox.ts-35-export interface OutboxRecord {
src\shared\outbox\outbox.ts-36-  PK: string;
src\shared\outbox\outbox.ts-37-  SK: string;
src\shared\outbox\outbox.ts:38:  entityType: "OutboxEvent";
src\shared\outbox\outbox.ts-39-  /** BLOCKER-B addition: previously only embedded in `PK` (`TENANT#<t>#OUTBOX#<shard>`),
src\shared\outbox\outbox.ts-40-   * never its own field - a sender whose payload doesn't already self-describe its tenant
src\shared\outbox\outbox.ts-41-   * (unlike DispatchCommand) needs this explicitly, without every caller re-parsing `PK`. */
src\shared\outbox\outbox.ts-42-  tenantId: string;
--
src\shared\outbox\outbox.ts-73-  const shard = monthShard(event.occurredAt);
src\shared\outbox\outbox.ts-74-  return {
src\shared\outbox\outbox.ts-75-    PK: `TENANT#${event.tenantId}#OUTBOX#${shard}`,
src\shared\outbox\outbox.ts-76-    SK: `EVENT#${event.occurredAt}#${event.eventId}`,
src\shared\outbox\outbox.ts:77:    entityType: "OutboxEvent",
src\shared\outbox\outbox.ts-78-    tenantId: event.tenantId,
src\shared\outbox\outbox.ts-79-    eventId: event.eventId,
src\shared\outbox\outbox.ts-80-    eventType: event.eventType,
src\shared\outbox\outbox.ts-81-    aggregateType: event.aggregate.type,
--
test\unit\bff\proxy-service.test.ts-6-function fakeSession(overrides: Partial<Session> = {}): Session {
test\unit\bff\proxy-service.test.ts-7-  return {
test\unit\bff\proxy-service.test.ts-8-    PK: "SESSION#x",
test\unit\bff\proxy-service.test.ts-9-    SK: "POINTER",
test\unit\bff\proxy-service.test.ts:10:    entityType: "Session",
test\unit\bff\proxy-service.test.ts-11-    selectorHash: "x",
test\unit\bff\proxy-service.test.ts-12-    secretHash: "y",
test\unit\bff\proxy-service.test.ts-13-    tenantId: "tenant-1",
test\unit\bff\proxy-service.test.ts-14-    userId: "user-1",
--
test\unit\dispatch-outbox-relay-processor.test.ts-10-function outboxRecord(overrides: Partial<OutboxRecord> = {}): OutboxRecord {
test\unit\dispatch-outbox-relay-processor.test.ts-11-  return {
test\unit\dispatch-outbox-relay-processor.test.ts-12-    PK: "TENANT#t1#OUTBOX#202608",
test\unit\dispatch-outbox-relay-processor.test.ts-13-    SK: "EVENT#2026-08-19T10:00:00.000Z#evt-1",
test\unit\dispatch-outbox-relay-processor.test.ts:14:    entityType: "OutboxEvent",
test\unit\dispatch-outbox-relay-processor.test.ts-15-    tenantId: "t1",
test\unit\dispatch-outbox-relay-processor.test.ts-16-    eventId: "evt-1",
test\unit\dispatch-outbox-relay-processor.test.ts-17-    eventType: "ReminderDispatchRequested",
test\unit\dispatch-outbox-relay-processor.test.ts-18-    aggregateType: "ReminderOccurrence",
--
src\workers\upload-finalizer\finalizer.ts-64-      const parseResult = await deps.parser.parse(input.object);
src\workers\upload-finalizer\finalizer.ts-65-      uploadValid = parseResult.outcome === "VALID";
src\workers\upload-finalizer\finalizer.ts-66-    }
src\workers\upload-finalizer\finalizer.ts-67-
src\workers\upload-finalizer\finalizer.ts:68:    const uploadEvidence = {
src\workers\upload-finalizer\finalizer.ts-69-      object: input.object,
src\workers\upload-finalizer\finalizer.ts-70-      contentLength: observed.contentLength,
src\workers\upload-finalizer\finalizer.ts-71-      mediaType: observed.mediaType,
src\workers\upload-finalizer\finalizer.ts-72-      checksumSha256: observed.checksumSha256 ?? doc.checksumSha256,
--
src\workers\upload-finalizer\finalizer.ts-81-            tableName: deps.tableName,
src\workers\upload-finalizer\finalizer.ts-82-            key,
src\workers\upload-finalizer\finalizer.ts-83-            tenantId: input.tenantId,
src\workers\upload-finalizer\finalizer.ts-84-            expectedVersion: doc.version,
src\workers\upload-finalizer\finalizer.ts:85:            set: { status: "SCANNING", uploadEvidence },
src\workers\upload-finalizer\finalizer.ts-86-          }),
src\workers\upload-finalizer\finalizer.ts-87-        },
src\workers\upload-finalizer\finalizer.ts-88-      ]);
src\workers\upload-finalizer\finalizer.ts-89-    } catch (err) {
--
test\unit\dispatch-outbox-relay\relay.test.ts-7-function makeRecord(overrides: Partial<OutboxRecord> = {}): OutboxRecord {
test\unit\dispatch-outbox-relay\relay.test.ts-8-  return {
test\unit\dispatch-outbox-relay\relay.test.ts-9-    PK: "TENANT#t1#OUTBOX#202608",
test\unit\dispatch-outbox-relay\relay.test.ts-10-    SK: "EVENT#2026-08-19T10:00:00.000Z#evt-1",
test\unit\dispatch-outbox-relay\relay.test.ts:11:    entityType: "OutboxEvent",
test\unit\dispatch-outbox-relay\relay.test.ts-12-    tenantId: "t1",
test\unit\dispatch-outbox-relay\relay.test.ts-13-    eventId: "evt-1",
test\unit\dispatch-outbox-relay\relay.test.ts-14-    eventType: "ReminderDispatchRequested",
test\unit\dispatch-outbox-relay\relay.test.ts-15-    aggregateType: "ReminderOccurrence",
--
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-25-    if (!image) continue;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-26-
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-27-    try {
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-28-      const item = unmarshall(image as Record<string, never>) as OutboxRecord;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts:29:      if (item.entityType !== "OutboxEvent") continue;
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-30-
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-31-      // m5-observability-design.md #2: DynamoDB Streams fallback is the record's own
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-32-      // SequenceNumber, not eventId (that fallback is the sweeper's, via
src\runtime\aws\handlers\dispatch-outbox-relay-processor.ts-33-      // outboxRecordCorrelationId - a different source per the design's table).
--
src\workers\submission-malware-result\result-processor.ts-39-
src\workers\submission-malware-result\result-processor.ts-40-    if (submission.quarantineObject.bucket !== input.object.bucket || submission.quarantineObject.key !== input.object.key) {
src\workers\submission-malware-result\result-processor.ts-41-      return "IGNORED_WRONG_OBJECT";
src\workers\submission-malware-result\result-processor.ts-42-    }
src\workers\submission-malware-result\result-processor.ts:43:    if (submission.malwareEvidence?.scanResultId === input.scanResultId) return "IGNORED_DUPLICATE_SCAN";
src\workers\submission-malware-result\result-processor.ts-44-    if (submission.status !== "PENDING_UPLOAD" && submission.status !== "SCANNING") return "IGNORED_STALE";
src\workers\submission-malware-result\result-processor.ts-45-
src\workers\submission-malware-result\result-processor.ts:46:    const malwareEvidence = { object: input.object, status: input.status, scanResultId: input.scanResultId, observedAt: now() };
src\workers\submission-malware-result\result-processor.ts-47-
src\workers\submission-malware-result\result-processor.ts-48-    try {
src\workers\submission-malware-result\result-processor.ts-49-      await deps.store.transactWrite([
src\workers\submission-malware-result\result-processor.ts:50:        { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { malwareEvidence } }) },
src\workers\submission-malware-result\result-processor.ts-51-      ]);
src\workers\submission-malware-result\result-processor.ts-52-    } catch (err) {
src\workers\submission-malware-result\result-processor.ts-53-      if (isTransactionCanceled(err)) continue;
src\workers\submission-malware-result\result-processor.ts-54-      throw err;
--
test\unit\identity\resolver.test.ts-81-    });
test\unit\identity\resolver.test.ts-82-    await users.upsertDeviceSession({
test\unit\identity\resolver.test.ts-83-      PK: `TENANT#${ctx.tenant.tenantId}#USER#${ctx.principal.userId}`,
test\unit\identity\resolver.test.ts-84-      SK: "SESSION#device-1",
test\unit\identity\resolver.test.ts:85:      entityType: "DeviceSession",
test\unit\identity\resolver.test.ts-86-      tenantId: ctx.tenant.tenantId,
test\unit\identity\resolver.test.ts-87-      userId: ctx.principal.userId,
test\unit\identity\resolver.test.ts-88-      deviceId: "device-1",
test\unit\identity\resolver.test.ts-89-      sessionId: ctx.principal.sessionId,
--
src\workers\submission-finalizer\finalizer.ts-51-      const parseResult = await deps.parser.parse(input.object);
src\workers\submission-finalizer\finalizer.ts-52-      uploadValid = parseResult.outcome === "VALID";
src\workers\submission-finalizer\finalizer.ts-53-    }
src\workers\submission-finalizer\finalizer.ts-54-
src\workers\submission-finalizer\finalizer.ts:55:    const uploadEvidence = {
src\workers\submission-finalizer\finalizer.ts-56-      object: input.object,
src\workers\submission-finalizer\finalizer.ts-57-      contentLength: observed.contentLength,
src\workers\submission-finalizer\finalizer.ts-58-      mediaType: observed.mediaType,
src\workers\submission-finalizer\finalizer.ts-59-      checksumSha256: observed.checksumSha256 ?? submission.checksumSha256,
--
src\workers\submission-finalizer\finalizer.ts-62-    };
src\workers\submission-finalizer\finalizer.ts-63-
src\workers\submission-finalizer\finalizer.ts-64-    try {
src\workers\submission-finalizer\finalizer.ts-65-      await deps.store.transactWrite([
src\workers\submission-finalizer\finalizer.ts:66:        { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: "SCANNING", uploadEvidence } }) },
src\workers\submission-finalizer\finalizer.ts-67-      ]);
src\workers\submission-finalizer\finalizer.ts-68-    } catch (err) {
src\workers\submission-finalizer\finalizer.ts-69-      if (isTransactionCanceled(err)) continue;
src\workers\submission-finalizer\finalizer.ts-70-      throw err;
--
src\shared\idempotency\idempotency.ts-29-
src\shared\idempotency\idempotency.ts-30-interface IdempotencyRecord {
src\shared\idempotency\idempotency.ts-31-  PK: string;
src\shared\idempotency\idempotency.ts-32-  SK: string;
src\shared\idempotency\idempotency.ts:33:  entityType: "IdempotencyRecord";
src\shared\idempotency\idempotency.ts-34-  tenantId: string;
src\shared\idempotency\idempotency.ts-35-  operation: string;
src\shared\idempotency\idempotency.ts-36-  requestHash: string;
src\shared\idempotency\idempotency.ts-37-  status: "IN_PROGRESS" | "COMPLETED" | "ABORTED";
--
src\shared\idempotency\idempotency.ts-163-    const { PK, SK } = buildIdempotencyKey(this.tableName, input.tenantId, input.operation, input.key);
src\shared\idempotency\idempotency.ts-164-    const record: IdempotencyRecord = {
src\shared\idempotency\idempotency.ts-165-      PK,
src\shared\idempotency\idempotency.ts-166-      SK,
src\shared\idempotency\idempotency.ts:167:      entityType: "IdempotencyRecord",
src\shared\idempotency\idempotency.ts-168-      tenantId: input.tenantId,
src\shared\idempotency\idempotency.ts-169-      operation: input.operation,
src\shared\idempotency\idempotency.ts-170-      requestHash: input.requestHash,
src\shared\idempotency\idempotency.ts-171-      status: "IN_PROGRESS",
--
test\unit\expiration\expiration-service.test.ts-34-    expect(item.categoryNormalized).toBe("licencas");
test\unit\expiration\expiration-service.test.ts-35-    expect(item.GSI1PK).toBe("TENANT#tenant-1#ITEMSTATUS#ACTIVE");
test\unit\expiration\expiration-service.test.ts-36-    expect(item.GSI1SK).toBe("DUE#2026-09-10T00:00:00.000Z#ITEM#item-1");
test\unit\expiration\expiration-service.test.ts-37-
test\unit\expiration\expiration-service.test.ts:38:    const audits = store.allItems().filter((i) => i["entityType"] === "AuditEvent");
test\unit\expiration\expiration-service.test.ts-39-    expect(audits).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-40-    expect(audits[0]?.["action"]).toBe("CREATE");
test\unit\expiration\expiration-service.test.ts-41-  });
test\unit\expiration\expiration-service.test.ts-42-
--
test\unit\expiration\expiration-service.test.ts-45-    const first = await service.createItem(ctx(), input, "fixed-create-key");
test\unit\expiration\expiration-service.test.ts-46-    const second = await service.createItem(ctx(), input, "fixed-create-key");
test\unit\expiration\expiration-service.test.ts-47-
test\unit\expiration\expiration-service.test.ts-48-    expect(second.itemId).toBe(first.itemId);
test\unit\expiration\expiration-service.test.ts:49:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-50-  });
test\unit\expiration\expiration-service.test.ts-51-
test\unit\expiration\expiration-service.test.ts-52-  it("createItem without an idempotency key never dedupes (unprotected, backward-compatible default) - two calls with identical payload create two distinct items", async () => {
test\unit\expiration\expiration-service.test.ts-53-    const input = { name: "Alvará", category: "Licenças", dueDate: "2026-09-10T00:00:00.000Z" };
test\unit\expiration\expiration-service.test.ts-54-    const first = await service.createItem(ctx(), input);
test\unit\expiration\expiration-service.test.ts-55-    const second = await service.createItem(ctx(), input);
test\unit\expiration\expiration-service.test.ts-56-
test\unit\expiration\expiration-service.test.ts-57-    expect(second.itemId).not.toBe(first.itemId);
test\unit\expiration\expiration-service.test.ts:58:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(2);
test\unit\expiration\expiration-service.test.ts-59-  });
test\unit\expiration\expiration-service.test.ts-60-
test\unit\expiration\expiration-service.test.ts-61-  it("createItem: a different idempotency key creates a distinct item even with the same payload", async () => {
test\unit\expiration\expiration-service.test.ts-62-    const input = { name: "Alvará", category: "Licenças", dueDate: "2026-09-10T00:00:00.000Z" };
--
test\unit\expiration\expiration-service.test.ts-100-    const input = { name: "Alvará", category: "Licenças", dueDate: "2026-09-10T00:00:00.000Z" };
test\unit\expiration\expiration-service.test.ts-101-    vi.spyOn(store, "update").mockRejectedValueOnce(new Error("simulated crash before idempotency.complete()"));
test\unit\expiration\expiration-service.test.ts-102-
test\unit\expiration\expiration-service.test.ts-103-    await expect(service.createItem(ctx(), input, "crash-key")).rejects.toThrow("simulated crash");
test\unit\expiration\expiration-service.test.ts:104:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(1); // the item WAS created - commit() already succeeded before the simulated crash
test\unit\expiration\expiration-service.test.ts-105-
test\unit\expiration\expiration-service.test.ts-106-    await expect(service.createItem(ctx(), input, "crash-key")).rejects.toBeInstanceOf(ConcurrentOperationError); // retry fails safe (no duplicate), it does not silently succeed nor duplicate
test\unit\expiration\expiration-service.test.ts:107:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(1); // still exactly one item - the original defect (duplicate creation) does not resurface
test\unit\expiration\expiration-service.test.ts-108-  });
test\unit\expiration\expiration-service.test.ts-109-
test\unit\expiration\expiration-service.test.ts-110-  it("createItem denies a VIEWER role", async () => {
test\unit\expiration\expiration-service.test.ts-111-    await expect(
--
test\unit\expiration\expiration-service.test.ts-138-    await service.updateItem(ctx(), item.itemId, { dueDate: "2026-10-01T00:00:00.000Z" }, item.version);
test\unit\expiration\expiration-service.test.ts-139-
test\unit\expiration\expiration-service.test.ts-140-    // BLOCKER-B: createItem itself now also emits expiration.item-due-date-changed.v1
test\unit\expiration\expiration-service.test.ts-141-    // (previousDueDate: null) - filter to the UPDATE's own event specifically.
test\unit\expiration\expiration-service.test.ts:142:    const outboxRecords = store.allItems().filter((i) => i["entityType"] === "OutboxEvent" && i["eventType"] === "expiration.item-due-date-changed.v1");
test\unit\expiration\expiration-service.test.ts-143-    expect(outboxRecords).toHaveLength(2);
test\unit\expiration\expiration-service.test.ts-144-    const updateEvent = outboxRecords.find((r) => (r["payload"] as Record<string, unknown>)["previousDueDate"] !== null)!;
test\unit\expiration\expiration-service.test.ts-145-    const payload = updateEvent["payload"] as Record<string, unknown>;
test\unit\expiration\expiration-service.test.ts-146-    expect(payload["previousDueDate"]).toBe("2026-09-10T00:00:00.000Z");
--
test\unit\expiration\expiration-service.test.ts-150-
test\unit\expiration\expiration-service.test.ts-151-  it("createItem emits ItemDueDateChanged (BLOCKER-B: a new item's due date is 'the due date changing' from nonexistent, so a policy attached at creation time can materialize immediately)", async () => {
test\unit\expiration\expiration-service.test.ts-152-    const item = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\expiration\expiration-service.test.ts-153-
test\unit\expiration\expiration-service.test.ts:154:    const outboxRecords = store.allItems().filter((i) => i["entityType"] === "OutboxEvent");
test\unit\expiration\expiration-service.test.ts-155-    expect(outboxRecords).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-156-    expect(outboxRecords[0]?.["eventType"]).toBe("expiration.item-due-date-changed.v1");
test\unit\expiration\expiration-service.test.ts-157-    const payload = outboxRecords[0]?.["payload"] as Record<string, unknown>;
test\unit\expiration\expiration-service.test.ts-158-    expect(payload["previousDueDate"]).toBeNull();
--
test\unit\expiration\expiration-service.test.ts-164-  it("updateItem without a dueDate change writes no ADDITIONAL outbox record beyond createItem's own", async () => {
test\unit\expiration\expiration-service.test.ts-165-    const item = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\expiration\expiration-service.test.ts-166-    await service.updateItem(ctx(), item.itemId, { name: "renamed" }, item.version);
test\unit\expiration\expiration-service.test.ts-167-
test\unit\expiration\expiration-service.test.ts:168:    const outboxRecords = store.allItems().filter((i) => i["entityType"] === "OutboxEvent");
test\unit\expiration\expiration-service.test.ts-169-    expect(outboxRecords).toHaveLength(1); // only createItem's - updateItem itself added none
test\unit\expiration\expiration-service.test.ts-170-  });
test\unit\expiration\expiration-service.test.ts-171-
test\unit\expiration\expiration-service.test.ts-172-  it("archiveItem emits expiration.item-deactivated.v1", async () => {
test\unit\expiration\expiration-service.test.ts-173-    const item = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\expiration\expiration-service.test.ts-174-    await service.archiveItem(ctx(), item.itemId, item.version);
test\unit\expiration\expiration-service.test.ts-175-
test\unit\expiration\expiration-service.test.ts:176:    const deactivated = store.allItems().filter((i) => i["entityType"] === "OutboxEvent" && i["eventType"] === "expiration.item-deactivated.v1");
test\unit\expiration\expiration-service.test.ts-177-    expect(deactivated).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-178-    expect((deactivated[0]?.["payload"] as Record<string, unknown>)["itemId"]).toBe(item.itemId);
test\unit\expiration\expiration-service.test.ts-179-  });
test\unit\expiration\expiration-service.test.ts-180-
test\unit\expiration\expiration-service.test.ts-181-  it("deleteItem emits expiration.item-deactivated.v1", async () => {
test\unit\expiration\expiration-service.test.ts-182-    const item = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\expiration\expiration-service.test.ts-183-    await service.deleteItem(ctx(), item.itemId, item.version);
test\unit\expiration\expiration-service.test.ts-184-
test\unit\expiration\expiration-service.test.ts:185:    const deactivated = store.allItems().filter((i) => i["entityType"] === "OutboxEvent" && i["eventType"] === "expiration.item-deactivated.v1");
test\unit\expiration\expiration-service.test.ts-186-    expect(deactivated).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-187-  });
test\unit\expiration\expiration-service.test.ts-188-
test\unit\expiration\expiration-service.test.ts-189-  it("renewItem emits expiration.item-deactivated.v1 for the OLD item alongside item-due-date-changed.v1 for the NEW item", async () => {
test\unit\expiration\expiration-service.test.ts-190-    const source = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\expiration\expiration-service.test.ts-191-    const renewed = await service.renewItem(ctx(), source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version);
test\unit\expiration\expiration-service.test.ts-192-
test\unit\expiration\expiration-service.test.ts:193:    const deactivated = store.allItems().filter((i) => i["entityType"] === "OutboxEvent" && i["eventType"] === "expiration.item-deactivated.v1");
test\unit\expiration\expiration-service.test.ts-194-    expect(deactivated).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-195-    expect((deactivated[0]?.["payload"] as Record<string, unknown>)["itemId"]).toBe(source.itemId);
test\unit\expiration\expiration-service.test.ts-196-
test\unit\expiration\expiration-service.test.ts:197:    const dueDateChanged = store.allItems().filter((i) => i["entityType"] === "OutboxEvent" && i["eventType"] === "expiration.item-due-date-changed.v1");
test\unit\expiration\expiration-service.test.ts-198-    // source's own createItem event + the renewed item's own creation event
test\unit\expiration\expiration-service.test.ts-199-    expect(dueDateChanged).toHaveLength(2);
test\unit\expiration\expiration-service.test.ts-200-    expect(dueDateChanged.some((r) => r["aggregateId"] === renewed.item.itemId)).toBe(true);
test\unit\expiration\expiration-service.test.ts-201-  });
--
test\unit\expiration\expiration-service.test.ts-204-    const item = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\expiration\expiration-service.test.ts-205-    await service.updateItem(ctx(), item.itemId, { name: "b" }, item.version);
test\unit\expiration\expiration-service.test.ts-206-    await service.archiveItem(ctx(), item.itemId, item.version + 1);
test\unit\expiration\expiration-service.test.ts-207-
test\unit\expiration\expiration-service.test.ts:208:    const audits = store.allItems().filter((i) => i["entityType"] === "AuditEvent");
test\unit\expiration\expiration-service.test.ts-209-    expect(audits.map((a) => a["action"])).toEqual(["CREATE", "UPDATE", "ARCHIVE"]);
test\unit\expiration\expiration-service.test.ts-210-    for (const audit of audits) {
test\unit\expiration\expiration-service.test.ts-211-      expect(audit["PK"]).toMatch(/^TENANT#tenant-1#AUDIT#\d{6}$/);
test\unit\expiration\expiration-service.test.ts-212-    }
--
test\unit\expiration\expiration-service.test.ts-235-    const first = await service.renewItem(ctx(), source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version, "fixed-key");
test\unit\expiration\expiration-service.test.ts-236-    const second = await service.renewItem(ctx(), source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version, "fixed-key");
test\unit\expiration\expiration-service.test.ts-237-
test\unit\expiration\expiration-service.test.ts-238-    expect(second.item.itemId).toBe(first.item.itemId);
test\unit\expiration\expiration-service.test.ts:239:    const allItems = store.allItems().filter((i) => i["entityType"] === "ExpirationItem");
test\unit\expiration\expiration-service.test.ts-240-    expect(allItems).toHaveLength(2); // source + one renewed successor, not two
test\unit\expiration\expiration-service.test.ts-241-  });
test\unit\expiration\expiration-service.test.ts-242-
test\unit\expiration\expiration-service.test.ts-243-  it("renewItem: an OCC conflict (stale expectedVersion) releases the idempotency key, so a retry with a freshly-fetched version succeeds - it does not permanently fail with ConcurrentOperationError", async () => {
--
test\unit\expiration\expiration-service.test.ts-260-      service.renewItem(ctx(), source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version + 1, "same-client-key"),
test\unit\expiration\expiration-service.test.ts-261-    ).rejects.toBeInstanceOf(ConflictError);
test\unit\expiration\expiration-service.test.ts-262-
test\unit\expiration\expiration-service.test.ts-263-    // The failed attempt must not have created a successor or transitioned the source.
test\unit\expiration\expiration-service.test.ts:264:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(1);
test\unit\expiration\expiration-service.test.ts-265-
test\unit\expiration\expiration-service.test.ts-266-    // Retry: the caller re-fetched the item (same version as before, nothing actually changed
test\unit\expiration\expiration-service.test.ts-267-    // in this test - a real concurrent writer isn't needed to prove the key is unblocked) and
test\unit\expiration\expiration-service.test.ts-268-    // resubmits with the SAME idempotency key, per mission §29.
test\unit\expiration\expiration-service.test.ts-269-    const renewed = await service.renewItem(ctx(), source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version, "same-client-key");
test\unit\expiration\expiration-service.test.ts-270-
test\unit\expiration\expiration-service.test.ts-271-    expect(renewed.item.renewedFromId).toBe(source.itemId);
test\unit\expiration\expiration-service.test.ts:272:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(2);
test\unit\expiration\expiration-service.test.ts-273-  });
test\unit\expiration\expiration-service.test.ts-274-
test\unit\expiration\expiration-service.test.ts-275-  it("renewItem: retrying under the same key after a wrong-status conflict (already renewed) also releases the lock rather than blocking forever", async () => {
test\unit\expiration\expiration-service.test.ts-276-    const source = await service.createItem(ctx(), { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
--
test\unit\expiration\expiration-service.test.ts-318-
test\unit\expiration\expiration-service.test.ts-319-    // The transactional write itself DID succeed (source RENEWED, one successor created) -
test\unit\expiration\expiration-service.test.ts-320-    // only idempotency bookkeeping failed afterward, and must not have been silently discarded
test\unit\expiration\expiration-service.test.ts-321-    // by an incorrect abort().
test\unit\expiration\expiration-service.test.ts:322:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(2);
test\unit\expiration\expiration-service.test.ts-323-
test\unit\expiration\expiration-service.test.ts-324-    // A retry under the same key - even with the now-stale original expectedVersion - must
test\unit\expiration\expiration-service.test.ts-325-    // never create a second successor. The record is left IN_PROGRESS (the pre-existing,
test\unit\expiration\expiration-service.test.ts-326-    // documented residual: mission §32/docs/frontend/core-expiration-vertical-slice.md §16),
test\unit\expiration\expiration-service.test.ts-327-    // so this surfaces as ConcurrentOperationError, not data loss or duplication.
test\unit\expiration\expiration-service.test.ts-328-    await expect(
test\unit\expiration\expiration-service.test.ts-329-      service.renewItem(ctx(), source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version, "same-key"),
test\unit\expiration\expiration-service.test.ts-330-    ).rejects.toBeInstanceOf(ConcurrentOperationError);
test\unit\expiration\expiration-service.test.ts:331:    expect(store.allItems().filter((i) => i["entityType"] === "ExpirationItem")).toHaveLength(2); // still exactly 2, never 3
test\unit\expiration\expiration-service.test.ts-332-  });
test\unit\expiration\expiration-service.test.ts-333-
test\unit\expiration\expiration-service.test.ts-334-  it("listDashboard queries GSI1 by tenant+status and returns items ordered by dueDate", async () => {
test\unit\expiration\expiration-service.test.ts-335-    await service.createItem(ctx(), { name: "later", category: "b", dueDate: "2026-12-01T00:00:00.000Z" });
--
test\unit\identity\quota.test.ts-45-    const quota = new TenantQuotaService(store);
test\unit\identity\quota.test.ts-46-    const key = { PK: "TENANT#tenant-a#QUOTA", SK: "TYPE#AI_CALL#w1" };
test\unit\identity\quota.test.ts-47-    await store.putIfAbsent({
test\unit\identity\quota.test.ts-48-      ...key,
test\unit\identity\quota.test.ts:49:      entityType: "TenantQuota",
test\unit\identity\quota.test.ts-50-      tenantId: "tenant-a",
test\unit\identity\quota.test.ts-51-      quotaType: "AI_CALL",
test\unit\identity\quota.test.ts-52-      limit: 100,
test\unit\identity\quota.test.ts-53-      windowSeconds: 60,
--
src\runtime\aws\handlers\extraction-starter-handler.ts-51-          const outcome = await startExtractionRun(deps, {
src\runtime\aws\handlers\extraction-starter-handler.ts-52-            tenantId: parsed.tenantId,
src\runtime\aws\handlers\extraction-starter-handler.ts-53-            itemId: parsed.itemId,
src\runtime\aws\handlers\extraction-starter-handler.ts-54-            documentId: parsed.documentId,
src\runtime\aws\handlers\extraction-starter-handler.ts:55:            cleanObject: { bucket: detail.bucket.name, key: detail.object.key, versionId: detail.object["version-id"]! },
src\runtime\aws\handlers\extraction-starter-handler.ts-56-          });
src\runtime\aws\handlers\extraction-starter-handler.ts-57-          logger.info("extraction-starter outcome", { documentId: parsed.documentId, outcome });
src\runtime\aws\handlers\extraction-starter-handler.ts-58-        });
src\runtime\aws\handlers\extraction-starter-handler.ts-59-      } catch (err) {
--
test\unit\extraction\confirm-reject-field.test.ts-148-
test\unit\extraction\confirm-reject-field.test.ts-149-function seedFixture(table: InMemoryTable) {
test\unit\extraction\confirm-reject-field.test.ts-150-  const item: ExpirationItem = {
test\unit\extraction\confirm-reject-field.test.ts-151-    ...itemKey("t1", "item1"),
test\unit\extraction\confirm-reject-field.test.ts:152:    entityType: "ExpirationItem",
test\unit\extraction\confirm-reject-field.test.ts-153-    itemId: "item1",
test\unit\extraction\confirm-reject-field.test.ts-154-    tenantId: "t1",
test\unit\extraction\confirm-reject-field.test.ts-155-    name: "Alvará",
test\unit\extraction\confirm-reject-field.test.ts-156-    category: "Licenças",
--
test\unit\extraction\confirm-reject-field.test.ts-165-    GSI1SK: "DUE#2026-01-01T00:00:00.000Z#ITEM#item1",
test\unit\extraction\confirm-reject-field.test.ts-166-  };
test\unit\extraction\confirm-reject-field.test.ts-167-  const document: Document = {
test\unit\extraction\confirm-reject-field.test.ts-168-    ...documentKey("t1", "item1", "doc1"),
test\unit\extraction\confirm-reject-field.test.ts:169:    entityType: "Document",
test\unit\extraction\confirm-reject-field.test.ts-170-    tenantId: "t1",
test\unit\extraction\confirm-reject-field.test.ts-171-    itemId: "item1",
test\unit\extraction\confirm-reject-field.test.ts-172-    documentId: "doc1",
test\unit\extraction\confirm-reject-field.test.ts-173-    uploadSlotId: "slot1",
--
test\unit\extraction\confirm-reject-field.test.ts-183-    updatedAt: "2026-01-01T00:00:00.000Z",
test\unit\extraction\confirm-reject-field.test.ts-184-  };
test\unit\extraction\confirm-reject-field.test.ts-185-  const run: ExtractionRun = {
test\unit\extraction\confirm-reject-field.test.ts-186-    ...extractionRunKey("t1", "doc1", "run1"),
test\unit\extraction\confirm-reject-field.test.ts:187:    entityType: "ExtractionRun",
test\unit\extraction\confirm-reject-field.test.ts-188-    tenantId: "t1",
test\unit\extraction\confirm-reject-field.test.ts-189-    itemId: "item1",
test\unit\extraction\confirm-reject-field.test.ts-190-    documentId: "doc1",
test\unit\extraction\confirm-reject-field.test.ts-191-    documentVersion: 3,
--
test\unit\extraction\confirm-reject-field.test.ts-199-    updatedAt: "2026-01-01T00:05:00.000Z",
test\unit\extraction\confirm-reject-field.test.ts-200-  };
test\unit\extraction\confirm-reject-field.test.ts-201-  const field: ExtractedField = {
test\unit\extraction\confirm-reject-field.test.ts-202-    ...extractedFieldKey("t1", "doc1", "expirationDate", "run1"),
test\unit\extraction\confirm-reject-field.test.ts:203:    entityType: "ExtractedField",
test\unit\extraction\confirm-reject-field.test.ts-204-    tenantId: "t1",
test\unit\extraction\confirm-reject-field.test.ts-205-    documentId: "doc1",
test\unit\extraction\confirm-reject-field.test.ts-206-    runId: "run1",
test\unit\extraction\confirm-reject-field.test.ts-207-    fieldName: "expirationDate",
--
src\workers\reminder-producer\producer.ts-110- * claim failures.
src\workers\reminder-producer\producer.ts-111- */
src\workers\reminder-producer\producer.ts-112-export function shouldAlarm(result: ProducerTickResult): { alarm: boolean; reason?: string } {
src\workers\reminder-producer\producer.ts-113-  if (result.unknownEntityType > 0) {
src\workers\reminder-producer\producer.ts:114:    return { alarm: true, reason: `reminder-producer: ${result.unknownEntityType} GSI3 row(s) matched neither the reminder nor the chasing entityType - fail-closed` };
src\workers\reminder-producer\producer.ts-115-  }
src\workers\reminder-producer\producer.ts-116-  if (result.failed.length > 0) {
src\workers\reminder-producer\producer.ts-117-    return { alarm: true, reason: `reminder-producer: ${result.failed.length} occurrence(s) failed to claim` };
src\workers\reminder-producer\producer.ts-118-  }
--
test\unit\document\upload-slot-reconciliation.test.ts-11-
test\unit\document\upload-slot-reconciliation.test.ts-12-function baseSlot(overrides: Partial<UploadSlot> = {}): UploadSlot {
test\unit\document\upload-slot-reconciliation.test.ts-13-  return {
test\unit\document\upload-slot-reconciliation.test.ts-14-    ...uploadSlotKey("t1", "slot1"),
test\unit\document\upload-slot-reconciliation.test.ts:15:    entityType: "UploadSlot",
test\unit\document\upload-slot-reconciliation.test.ts-16-    tenantId: "t1",
test\unit\document\upload-slot-reconciliation.test.ts-17-    uploadSlotId: "slot1",
test\unit\document\upload-slot-reconciliation.test.ts-18-    documentId: "doc1",
test\unit\document\upload-slot-reconciliation.test.ts-19-    itemId: "item1",
--
test\unit\document\upload-slot-reconciliation.test.ts-36-
test\unit\document\upload-slot-reconciliation.test.ts-37-function baseDocument(overrides: Partial<Document> = {}): Document {
test\unit\document\upload-slot-reconciliation.test.ts-38-  return {
test\unit\document\upload-slot-reconciliation.test.ts-39-    ...documentKey("t1", "item1", "doc1"),
test\unit\document\upload-slot-reconciliation.test.ts:40:    entityType: "Document",
test\unit\document\upload-slot-reconciliation.test.ts-41-    tenantId: "t1",
test\unit\document\upload-slot-reconciliation.test.ts-42-    itemId: "item1",
test\unit\document\upload-slot-reconciliation.test.ts-43-    documentId: "doc1",
test\unit\document\upload-slot-reconciliation.test.ts-44-    uploadSlotId: "slot1",
--
test\unit\document\upload-slot-reconciliation.test.ts-98-
test\unit\document\upload-slot-reconciliation.test.ts-99-  it("never times out a document that already reached a terminal state (CLEAN) before the sweep ran", async () => {
test\unit\document\upload-slot-reconciliation.test.ts-100-    const store = new InMemoryDocumentStore();
test\unit\document\upload-slot-reconciliation.test.ts-101-    await store.putIfAbsent(baseSlot());
test\unit\document\upload-slot-reconciliation.test.ts:102:    await store.putIfAbsent(baseDocument({ status: "CLEAN", cleanObject: { bucket: "clean", key: "k", versionId: "v" } }));
test\unit\document\upload-slot-reconciliation.test.ts-103-    const identityStore = new InMemoryIdentityStore();
test\unit\document\upload-slot-reconciliation.test.ts-104-    const quota = new TenantQuotaService(identityStore, () => "2026-08-22T00:15:00.000Z");
test\unit\document\upload-slot-reconciliation.test.ts-105-
test\unit\document\upload-slot-reconciliation.test.ts-106-    await reconcileExpiredUploadSlots({ store, candidates: fakeCandidateSource([baseSlot()]), quota, tableName: TABLE, now: () => "2026-08-22T00:15:00.000Z" });
--
test\unit\document\upload-finalizer.test.ts-12-
test\unit\document\upload-finalizer.test.ts-13-function baseDocument(overrides: Partial<Document> = {}): Document {
test\unit\document\upload-finalizer.test.ts-14-  return {
test\unit\document\upload-finalizer.test.ts-15-    ...documentKey("t1", "item1", "doc1"),
test\unit\document\upload-finalizer.test.ts:16:    entityType: "Document",
test\unit\document\upload-finalizer.test.ts-17-    tenantId: "t1",
test\unit\document\upload-finalizer.test.ts-18-    itemId: "item1",
test\unit\document\upload-finalizer.test.ts-19-    documentId: "doc1",
test\unit\document\upload-finalizer.test.ts-20-    uploadSlotId: "slot1",
--
test\unit\document\upload-finalizer.test.ts-55-    );
test\unit\document\upload-finalizer.test.ts-56-    expect(outcome).toBe("CONFIRMED");
test\unit\document\upload-finalizer.test.ts-57-    const doc = (await store.get(documentKey("t1", "item1", "doc1"))) as Document;
test\unit\document\upload-finalizer.test.ts-58-    expect(doc.status).toBe("SCANNING");
test\unit\document\upload-finalizer.test.ts:59:    expect(doc.uploadEvidence?.valid).toBe(true);
test\unit\document\upload-finalizer.test.ts-60-  });
test\unit\document\upload-finalizer.test.ts-61-
test\unit\document\upload-finalizer.test.ts-62-  it("rejects when the observed size doesn't match what was declared at reservation", async () => {
test\unit\document\upload-finalizer.test.ts-63-    const store = new InMemoryDocumentStore();
--
test\unit\document\upload-finalizer.test.ts-69-    );
test\unit\document\upload-finalizer.test.ts-70-    expect(outcome).toBe("REJECTED_INVALID");
test\unit\document\upload-finalizer.test.ts-71-    const doc = (await store.get(documentKey("t1", "item1", "doc1"))) as Document;
test\unit\document\upload-finalizer.test.ts-72-    expect(doc.status).toBe("REJECTED");
test\unit\document\upload-finalizer.test.ts:73:    expect(doc.uploadEvidence?.valid).toBe(false);
test\unit\document\upload-finalizer.test.ts-74-  });
test\unit\document\upload-finalizer.test.ts-75-
test\unit\document\upload-finalizer.test.ts-76-  it("rejects when the PDF sandbox parser reports invalid structure, even though size/checksum matched", async () => {
test\unit\document\upload-finalizer.test.ts-77-    const store = new InMemoryDocumentStore();
--
test\unit\document\upload-finalizer.test.ts-133-        if (getCalls === 1 && doc) {
test\unit\document\upload-finalizer.test.ts-134-          // Simulate MalwareResultWorker concurrently persisting its own evidence between our
test\unit\document\upload-finalizer.test.ts-135-          // read and our write - bumps version, so our transactWrite below (still holding the
test\unit\document\upload-finalizer.test.ts-136-          // stale version from THIS read) must fail once and retry.
test\unit\document\upload-finalizer.test.ts:137:          await store.update({ ...doc, malwareEvidence: { object: OBJECT, status: "NO_THREATS_FOUND", scanResultId: "scan-1", observedAt: "2026-08-22T00:00:01.000Z" }, version: 2 });
test\unit\document\upload-finalizer.test.ts-138-        }
test\unit\document\upload-finalizer.test.ts-139-        return doc as unknown as T;
test\unit\document\upload-finalizer.test.ts-140-      },
test\unit\document\upload-finalizer.test.ts-141-      transactWrite: store.transactWrite.bind(store),
--
test\unit\document\upload-finalizer.test.ts-150-    );
test\unit\document\upload-finalizer.test.ts-151-
test\unit\document\upload-finalizer.test.ts-152-    expect(outcome).toBe("CONFIRMED");
test\unit\document\upload-finalizer.test.ts-153-    expect(getCalls).toBeGreaterThanOrEqual(2); // proves the retry actually happened, not a lucky first pass.
test\unit\document\upload-finalizer.test.ts:154:    const doc = store.allItems().find((i) => i["entityType"] === "Document") as unknown as Document | undefined;
test\unit\document\upload-finalizer.test.ts-155-    expect(doc?.status).toBe("CLEAN"); // both evidences ended up applied: upload valid + malware clean -> PROMOTE.
test\unit\document\upload-finalizer.test.ts:156:    expect(doc?.uploadEvidence?.valid).toBe(true);
test\unit\document\upload-finalizer.test.ts:157:    expect(doc?.malwareEvidence?.status).toBe("NO_THREATS_FOUND");
test\unit\document\upload-finalizer.test.ts-158-  });
test\unit\document\upload-finalizer.test.ts-159-});
--
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-22-    if (!image) continue;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-23-
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-24-    try {
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-25-      const item = unmarshall(image as Record<string, never>) as OutboxRecord;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts:26:      if (item.entityType !== "OutboxEvent") continue;
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-27-
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-28-      // m5-observability-design.md #2: DynamoDB Streams fallback is the record's own
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-29-      // SequenceNumber, not eventId (that fallback is the sweeper's, via
src\runtime\aws\handlers\notification-email-outbox-relay-processor.ts-30-      // outboxRecordCorrelationId - a different source per the design's table).
--
test\unit\import\import-service.test.ts-60-    const first = await service.reserveImport(ctx(), { contentLength: 1024, checksumSha256: VALID_SHA256 }, "idem-1");
test\unit\import\import-service.test.ts-61-    const second = await service.reserveImport(ctx(), { contentLength: 1024, checksumSha256: VALID_SHA256 }, "idem-1");
test\unit\import\import-service.test.ts-62-    expect(second.jobId).toBe(first.jobId);
test\unit\import\import-service.test.ts-63-
test\unit\import\import-service.test.ts:64:    const jobs = store.allItems().filter((i) => i["entityType"] === "ImportJob");
test\unit\import\import-service.test.ts-65-    expect(jobs).toHaveLength(1);
test\unit\import\import-service.test.ts-66-  });
test\unit\import\import-service.test.ts-67-
test\unit\import\import-service.test.ts-68-  it("getImportJob throws NotFoundError for an unknown jobId", async () => {
--
test\unit\import\import-service.test.ts-90-    await store.update<ImportJob>({ ...job!, status: "PREVIEW_READY" });
test\unit\import\import-service.test.ts-91-
test\unit\import\import-service.test.ts-92-    await service.requestCommit(ctx(), jobId, job!.version);
test\unit\import\import-service.test.ts-93-
test\unit\import\import-service.test.ts:94:    const outboxRecords = store.allItems().filter((i) => i["entityType"] === "OutboxEvent");
test\unit\import\import-service.test.ts-95-    expect(outboxRecords).toHaveLength(1);
test\unit\import\import-service.test.ts-96-    expect(outboxRecords[0]?.["destination"]).toBe("SQS_IMPORT_COMMIT_V1");
test\unit\import\import-service.test.ts-97-
test\unit\import\import-service.test.ts-98-    const payload = outboxRecords[0]?.["payload"] as ImportCommitCommand;
--
test\unit\subject\submission-malware-result.test.ts-11-
test\unit\subject\submission-malware-result.test.ts-12-function baseSubmission(overrides: Partial<DocumentSubmission> = {}): DocumentSubmission {
test\unit\subject\submission-malware-result.test.ts-13-  return {
test\unit\subject\submission-malware-result.test.ts-14-    ...documentSubmissionKey("t1", "subject1", "assign1", "sub1"),
test\unit\subject\submission-malware-result.test.ts:15:    entityType: "DocumentSubmission",
test\unit\subject\submission-malware-result.test.ts-16-    submissionId: "sub1",
test\unit\subject\submission-malware-result.test.ts-17-    tenantId: "t1",
test\unit\subject\submission-malware-result.test.ts-18-    subjectId: "subject1",
test\unit\subject\submission-malware-result.test.ts-19-    assignmentId: "assign1",
--
test\unit\subject\submission-malware-result.test.ts-53-
test\unit\subject\submission-malware-result.test.ts-54-  it("promotes to CLEAN when upload was already valid and result is NO_THREATS_FOUND", async () => {
test\unit\subject\submission-malware-result.test.ts-55-    const store = new InMemorySubjectStore();
test\unit\subject\submission-malware-result.test.ts-56-    await store.putIfAbsent(
test\unit\subject\submission-malware-result.test.ts:57:      baseSubmission({ status: "SCANNING", uploadEvidence: { object: OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-23T00:01:00.000Z" } }),
test\unit\subject\submission-malware-result.test.ts-58-    );
test\unit\subject\submission-malware-result.test.ts-59-    const outcome = await processSubmissionMalwareResult({ store, objects: fakeObjects(), tableName: TABLE, cleanBucket: CLEAN_BUCKET }, { ...INPUT_BASE, status: "NO_THREATS_FOUND", scanResultId: "scan-1" });
test\unit\subject\submission-malware-result.test.ts-60-    expect(outcome).toBe("APPLIED");
test\unit\subject\submission-malware-result.test.ts-61-    const submission = (await store.get(documentSubmissionKey("t1", "subject1", "assign1", "sub1"))) as DocumentSubmission;
--
test\unit\subject\submission-malware-result.test.ts-73-  });
test\unit\subject\submission-malware-result.test.ts-74-
test\unit\subject\submission-malware-result.test.ts-75-  it("deduplicates a redelivered scan result by scanResultId", async () => {
test\unit\subject\submission-malware-result.test.ts-76-    const store = new InMemorySubjectStore();
test\unit\subject\submission-malware-result.test.ts:77:    await store.putIfAbsent(baseSubmission({ malwareEvidence: { object: OBJECT, status: "NO_THREATS_FOUND", scanResultId: "scan-1", observedAt: "2026-08-23T00:00:00.000Z" } }));
test\unit\subject\submission-malware-result.test.ts-78-    const outcome = await processSubmissionMalwareResult({ store, objects: fakeObjects(), tableName: TABLE, cleanBucket: CLEAN_BUCKET }, { ...INPUT_BASE, status: "NO_THREATS_FOUND", scanResultId: "scan-1" });
test\unit\subject\submission-malware-result.test.ts-79-    expect(outcome).toBe("IGNORED_DUPLICATE_SCAN");
test\unit\subject\submission-malware-result.test.ts-80-  });
test\unit\subject\submission-malware-result.test.ts-81-
--
test\unit\subject\submission-malware-result.test.ts-100-      get: async <T,>(key: Parameters<SubjectStore["get"]>[0]) => {
test\unit\subject\submission-malware-result.test.ts-101-        getCalls += 1;
test\unit\subject\submission-malware-result.test.ts-102-        const submission = await store.get<DocumentSubmission>(key);
test\unit\subject\submission-malware-result.test.ts-103-        if (getCalls === 1 && submission) {
test\unit\subject\submission-malware-result.test.ts:104:          await store.update({ ...submission, status: "SCANNING", uploadEvidence: { object: OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-23T00:00:01.000Z" }, version: 2 });
test\unit\subject\submission-malware-result.test.ts-105-        }
test\unit\subject\submission-malware-result.test.ts-106-        return submission as unknown as T;
test\unit\subject\submission-malware-result.test.ts-107-      },
test\unit\subject\submission-malware-result.test.ts-108-      transactWrite: store.transactWrite.bind(store),
--
test\unit\subject\submission-malware-result.test.ts-116-    const outcome = await processSubmissionMalwareResult({ store: racingStore, objects: fakeObjects(), tableName: TABLE, cleanBucket: CLEAN_BUCKET }, { ...INPUT_BASE, status: "NO_THREATS_FOUND", scanResultId: "scan-1" });
test\unit\subject\submission-malware-result.test.ts-117-
test\unit\subject\submission-malware-result.test.ts-118-    expect(outcome).toBe("APPLIED");
test\unit\subject\submission-malware-result.test.ts-119-    expect(getCalls).toBeGreaterThanOrEqual(2);
test\unit\subject\submission-malware-result.test.ts:120:    const submission = store.allItems().find((i) => i["entityType"] === "DocumentSubmission") as unknown as DocumentSubmission | undefined;
test\unit\subject\submission-malware-result.test.ts-121-    expect(submission?.status).toBe("CLEAN");
test\unit\subject\submission-malware-result.test.ts-122-  });
test\unit\subject\submission-malware-result.test.ts-123-});
--
test\unit\extraction\start-ocr.test.ts-87-    documentId: "doc1",
test\unit\extraction\start-ocr.test.ts-88-    documentVersion: 3,
test\unit\extraction\start-ocr.test.ts-89-    runId: "run_x",
test\unit\extraction\start-ocr.test.ts-90-    pipelineVersion: "2026-08-01",
test\unit\extraction\start-ocr.test.ts:91:    cleanObject: { bucket: "clean-bucket", key: "clean/t1/item1/doc1", versionId: "v1" },
test\unit\extraction\start-ocr.test.ts-92-    fileName: "cert.pdf",
test\unit\extraction\start-ocr.test.ts-93-    ...overrides,
test\unit\extraction\start-ocr.test.ts-94-  };
test\unit\extraction\start-ocr.test.ts-95-}
--
test\unit\subject\submission-finalizer.test.ts-12-
test\unit\subject\submission-finalizer.test.ts-13-function baseSubmission(overrides: Partial<DocumentSubmission> = {}): DocumentSubmission {
test\unit\subject\submission-finalizer.test.ts-14-  return {
test\unit\subject\submission-finalizer.test.ts-15-    ...documentSubmissionKey("t1", "subject1", "assign1", "sub1"),
test\unit\subject\submission-finalizer.test.ts:16:    entityType: "DocumentSubmission",
test\unit\subject\submission-finalizer.test.ts-17-    submissionId: "sub1",
test\unit\subject\submission-finalizer.test.ts-18-    tenantId: "t1",
test\unit\subject\submission-finalizer.test.ts-19-    subjectId: "subject1",
test\unit\subject\submission-finalizer.test.ts-20-    assignmentId: "assign1",
--
test\unit\subject\submission-finalizer.test.ts-57-    );
test\unit\subject\submission-finalizer.test.ts-58-    expect(outcome).toBe("CONFIRMED");
test\unit\subject\submission-finalizer.test.ts-59-    const submission = (await store.get(documentSubmissionKey("t1", "subject1", "assign1", "sub1"))) as DocumentSubmission;
test\unit\subject\submission-finalizer.test.ts-60-    expect(submission.status).toBe("SCANNING");
test\unit\subject\submission-finalizer.test.ts:61:    expect(submission.uploadEvidence?.valid).toBe(true);
test\unit\subject\submission-finalizer.test.ts-62-  });
test\unit\subject\submission-finalizer.test.ts-63-
test\unit\subject\submission-finalizer.test.ts-64-  it("rejects when the observed size doesn't match what was declared", async () => {
test\unit\subject\submission-finalizer.test.ts-65-    const store = new InMemorySubjectStore();
--
test\unit\subject\submission-finalizer.test.ts-105-      get: async <T,>(key: Parameters<SubjectStore["get"]>[0]) => {
test\unit\subject\submission-finalizer.test.ts-106-        getCalls += 1;
test\unit\subject\submission-finalizer.test.ts-107-        const submission = await store.get<DocumentSubmission>(key);
test\unit\subject\submission-finalizer.test.ts-108-        if (getCalls === 1 && submission) {
test\unit\subject\submission-finalizer.test.ts:109:          await store.update({ ...submission, malwareEvidence: { object: OBJECT, status: "NO_THREATS_FOUND", scanResultId: "scan-1", observedAt: "2026-08-23T00:00:01.000Z" }, version: 2 });
test\unit\subject\submission-finalizer.test.ts-110-        }
test\unit\subject\submission-finalizer.test.ts-111-        return submission as unknown as T;
test\unit\subject\submission-finalizer.test.ts-112-      },
test\unit\subject\submission-finalizer.test.ts-113-      transactWrite: store.transactWrite.bind(store),
--
test\unit\subject\submission-finalizer.test.ts-121-    const outcome = await finalizeSubmissionUpload({ store: racingStore, objects: fakeObjects(), parser: fakeParser(), tableName: TABLE, cleanBucket: CLEAN_BUCKET }, INPUT_BASE);
test\unit\subject\submission-finalizer.test.ts-122-
test\unit\subject\submission-finalizer.test.ts-123-    expect(outcome).toBe("CONFIRMED");
test\unit\subject\submission-finalizer.test.ts-124-    expect(getCalls).toBeGreaterThanOrEqual(2);
test\unit\subject\submission-finalizer.test.ts:125:    const submission = store.allItems().find((i) => i["entityType"] === "DocumentSubmission") as unknown as DocumentSubmission | undefined;
test\unit\subject\submission-finalizer.test.ts-126-    expect(submission?.status).toBe("CLEAN");
test\unit\subject\submission-finalizer.test.ts:127:    expect(submission?.uploadEvidence?.valid).toBe(true);
test\unit\subject\submission-finalizer.test.ts:128:    expect(submission?.malwareEvidence?.status).toBe("NO_THREATS_FOUND");
test\unit\subject\submission-finalizer.test.ts-129-  });
test\unit\subject\submission-finalizer.test.ts-130-});
--
test\unit\document\malware-result-processor.test.ts-11-
test\unit\document\malware-result-processor.test.ts-12-function baseDocument(overrides: Partial<Document> = {}): Document {
test\unit\document\malware-result-processor.test.ts-13-  return {
test\unit\document\malware-result-processor.test.ts-14-    ...documentKey("t1", "item1", "doc1"),
test\unit\document\malware-result-processor.test.ts:15:    entityType: "Document",
test\unit\document\malware-result-processor.test.ts-16-    tenantId: "t1",
test\unit\document\malware-result-processor.test.ts-17-    itemId: "item1",
test\unit\document\malware-result-processor.test.ts-18-    documentId: "doc1",
test\unit\document\malware-result-processor.test.ts-19-    uploadSlotId: "slot1",
--
test\unit\document\malware-result-processor.test.ts-54-
test\unit\document\malware-result-processor.test.ts-55-  it("promotes to CLEAN when upload was already valid and result is NO_THREATS_FOUND", async () => {
test\unit\document\malware-result-processor.test.ts-56-    const store = new InMemoryDocumentStore();
test\unit\document\malware-result-processor.test.ts-57-    await store.putIfAbsent(
test\unit\document\malware-result-processor.test.ts:58:      baseDocument({ status: "SCANNING", uploadEvidence: { object: OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" } }),
test\unit\document\malware-result-processor.test.ts-59-    );
test\unit\document\malware-result-processor.test.ts-60-    const outcome = await processMalwareResult(
test\unit\document\malware-result-processor.test.ts-61-      { store, objects: fakeObjects(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\malware-result-processor.test.ts-62-      { tenantId: "t1", itemId: "item1", documentId: "doc1", object: OBJECT, status: "NO_THREATS_FOUND", scanResultId: "scan-1" },
--
test\unit\document\malware-result-processor.test.ts-77-  });
test\unit\document\malware-result-processor.test.ts-78-
test\unit\document\malware-result-processor.test.ts-79-  it("deduplicates a redelivered scan result by scanResultId, never reprocessing it", async () => {
test\unit\document\malware-result-processor.test.ts-80-    const store = new InMemoryDocumentStore();
test\unit\document\malware-result-processor.test.ts:81:    await store.putIfAbsent(baseDocument({ malwareEvidence: { object: OBJECT, status: "NO_THREATS_FOUND", scanResultId: "scan-1", observedAt: "2026-08-22T00:00:00.000Z" } }));
test\unit\document\malware-result-processor.test.ts-82-    const outcome = await processMalwareResult(
test\unit\document\malware-result-processor.test.ts-83-      { store, objects: fakeObjects(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\malware-result-processor.test.ts-84-      { tenantId: "t1", itemId: "item1", documentId: "doc1", object: OBJECT, status: "NO_THREATS_FOUND", scanResultId: "scan-1" },
test\unit\document\malware-result-processor.test.ts-85-    );
--
test\unit\document\malware-result-processor.test.ts-116-        if (getCalls === 1 && doc) {
test\unit\document\malware-result-processor.test.ts-117-          // Simulate UploadFinalizerWorker concurrently persisting its own evidence between our
test\unit\document\malware-result-processor.test.ts-118-          // read and our write - bumps version, so our transactWrite below (still holding the
test\unit\document\malware-result-processor.test.ts-119-          // stale version from THIS read) must fail once and retry.
test\unit\document\malware-result-processor.test.ts:120:          await store.update({ ...doc, status: "SCANNING", uploadEvidence: { object: OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:00:01.000Z" }, version: 2 });
test\unit\document\malware-result-processor.test.ts-121-        }
test\unit\document\malware-result-processor.test.ts-122-        return doc as unknown as T;
test\unit\document\malware-result-processor.test.ts-123-      },
test\unit\document\malware-result-processor.test.ts-124-      transactWrite: store.transactWrite.bind(store),
--
test\unit\document\malware-result-processor.test.ts-133-    );
test\unit\document\malware-result-processor.test.ts-134-
test\unit\document\malware-result-processor.test.ts-135-    expect(outcome).toBe("APPLIED");
test\unit\document\malware-result-processor.test.ts-136-    expect(getCalls).toBeGreaterThanOrEqual(2); // proves the retry actually happened, not a lucky first pass.
test\unit\document\malware-result-processor.test.ts:137:    const doc = store.allItems().find((i) => i["entityType"] === "Document") as unknown as Document | undefined;
test\unit\document\malware-result-processor.test.ts-138-    expect(doc?.status).toBe("CLEAN"); // both evidences ended up applied: upload valid + malware clean -> PROMOTE.
test\unit\document\malware-result-processor.test.ts:139:    expect(doc?.uploadEvidence?.valid).toBe(true);
test\unit\document\malware-result-processor.test.ts:140:    expect(doc?.malwareEvidence?.status).toBe("NO_THREATS_FOUND");
test\unit\document\malware-result-processor.test.ts-141-  });
test\unit\document\malware-result-processor.test.ts-142-});
--
src\workers\reminder-dispatch\dispatch.ts-133-  const now = deps.now();
src\workers\reminder-dispatch\dispatch.ts-134-  const intentId = deps.newIntentId();
src\workers\reminder-dispatch\dispatch.ts-135-  const intent: NotificationIntent = {
src\workers\reminder-dispatch\dispatch.ts-136-    ...intentKey(tenantId, intentId),
src\workers\reminder-dispatch\dispatch.ts:137:    entityType: "NotificationIntent",
src\workers\reminder-dispatch\dispatch.ts-138-    intentId,
src\workers\reminder-dispatch\dispatch.ts-139-    tenantId,
src\workers\reminder-dispatch\dispatch.ts-140-    kind: "EXPIRATION_REMINDER",
src\workers\reminder-dispatch\dispatch.ts-141-    itemId,
--
src\workers\reminder-dispatch\dispatch.ts-188-      TableName: deps.tableName,
src\workers\reminder-dispatch\dispatch.ts-189-      Item: {
src\workers\reminder-dispatch\dispatch.ts-190-        PK: idem.PK,
src\workers\reminder-dispatch\dispatch.ts-191-        SK: idem.SK,
src\workers\reminder-dispatch\dispatch.ts:192:        entityType: "IdempotencyRecord",
src\workers\reminder-dispatch\dispatch.ts-193-        tenantId,
src\workers\reminder-dispatch\dispatch.ts-194-        operation: "reminder.dispatch",
src\workers\reminder-dispatch\dispatch.ts-195-        requestHash: occurrenceId,
src\workers\reminder-dispatch\dispatch.ts-196-        status: "COMPLETED",
--
test\unit\extraction\start-extraction-run.test.ts-9-
test\unit\extraction\start-extraction-run.test.ts-10-function cleanDocument(overrides: Partial<Document> = {}): Document {
test\unit\extraction\start-extraction-run.test.ts-11-  return {
test\unit\extraction\start-extraction-run.test.ts-12-    ...documentKey("t1", "item1", "doc1"),
test\unit\extraction\start-extraction-run.test.ts:13:    entityType: "Document",
test\unit\extraction\start-extraction-run.test.ts-14-    tenantId: "t1",
test\unit\extraction\start-extraction-run.test.ts-15-    itemId: "item1",
test\unit\extraction\start-extraction-run.test.ts-16-    documentId: "doc1",
test\unit\extraction\start-extraction-run.test.ts-17-    uploadSlotId: "slot1",
--
test\unit\extraction\start-extraction-run.test.ts-20-    contentLength: 100,
test\unit\extraction\start-extraction-run.test.ts-21-    checksumSha256: "a".repeat(64),
test\unit\extraction\start-extraction-run.test.ts-22-    status: "CLEAN",
test\unit\extraction\start-extraction-run.test.ts-23-    quarantineObject: { bucket: "q", key: "quarantine/t1/item1/doc1/slot1/x", versionId: "v0" },
test\unit\extraction\start-extraction-run.test.ts:24:    cleanObject: { bucket: "clean-bucket", key: "clean/t1/item1/doc1", versionId: "v1" },
test\unit\extraction\start-extraction-run.test.ts-25-    retentionClass: "USER_DOCUMENT",
test\unit\extraction\start-extraction-run.test.ts-26-    version: 3,
test\unit\extraction\start-extraction-run.test.ts-27-    createdAt: "2026-08-25T00:00:00.000Z",
test\unit\extraction\start-extraction-run.test.ts-28-    updatedAt: "2026-08-25T00:00:00.000Z",
--
test\unit\extraction\start-extraction-run.test.ts-67-    const runs = new FakeExtractionRunStore();
test\unit\extraction\start-extraction-run.test.ts-68-    const executions = new FakeExecutionStarter();
test\unit\extraction\start-extraction-run.test.ts-69-    const outcome = await startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts-70-      { documents: new FakeDocumentReader(undefined), runs, executions },
test\unit\extraction\start-extraction-run.test.ts:71:      { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT },
test\unit\extraction\start-extraction-run.test.ts-72-    );
test\unit\extraction\start-extraction-run.test.ts-73-    expect(outcome).toBe("DOCUMENT_NOT_FOUND");
test\unit\extraction\start-extraction-run.test.ts-74-    expect(executions.calls).toHaveLength(0);
test\unit\extraction\start-extraction-run.test.ts-75-  });
--
test\unit\extraction\start-extraction-run.test.ts-79-    const executions = new FakeExecutionStarter();
test\unit\extraction\start-extraction-run.test.ts-80-    await expect(
test\unit\extraction\start-extraction-run.test.ts-81-      startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts-82-        { documents: new FakeDocumentReader(cleanDocument({ status: "SCANNING" })), runs, executions },
test\unit\extraction\start-extraction-run.test.ts:83:        { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT },
test\unit\extraction\start-extraction-run.test.ts-84-      ),
test\unit\extraction\start-extraction-run.test.ts-85-    ).rejects.toBeInstanceOf(DocumentNotCleanYetError);
test\unit\extraction\start-extraction-run.test.ts-86-    expect(executions.calls).toHaveLength(0);
test\unit\extraction\start-extraction-run.test.ts-87-  });
--
test\unit\extraction\start-extraction-run.test.ts-90-    const runs = new FakeExtractionRunStore();
test\unit\extraction\start-extraction-run.test.ts-91-    const executions = new FakeExecutionStarter();
test\unit\extraction\start-extraction-run.test.ts-92-    const outcome = await startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts-93-      { documents: new FakeDocumentReader(cleanDocument()), runs, executions, now: () => "2026-08-26T00:00:00.000Z" },
test\unit\extraction\start-extraction-run.test.ts:94:      { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT },
test\unit\extraction\start-extraction-run.test.ts-95-    );
test\unit\extraction\start-extraction-run.test.ts-96-
test\unit\extraction\start-extraction-run.test.ts-97-    expect(outcome).toBe("STARTED");
test\unit\extraction\start-extraction-run.test.ts-98-    expect(executions.calls).toHaveLength(1);
--
test\unit\extraction\start-extraction-run.test.ts-105-      documentId: "doc1",
test\unit\extraction\start-extraction-run.test.ts-106-      documentVersion: 3,
test\unit\extraction\start-extraction-run.test.ts-107-      runId: expectedRunId,
test\unit\extraction\start-extraction-run.test.ts-108-      pipelineVersion: PIPELINE_VERSION_V1,
test\unit\extraction\start-extraction-run.test.ts:109:      cleanObject: CLEAN_OBJECT,
test\unit\extraction\start-extraction-run.test.ts-110-      fileName: "cert.pdf",
test\unit\extraction\start-extraction-run.test.ts-111-      contentType: "application/pdf",
test\unit\extraction\start-extraction-run.test.ts-112-    });
test\unit\extraction\start-extraction-run.test.ts-113-
--
test\unit\extraction\start-extraction-run.test.ts-120-    const executions = new FakeExecutionStarter();
test\unit\extraction\start-extraction-run.test.ts-121-    const doc = cleanDocument({ fileName: "invoice-2026.PNG", mediaType: "image/png" });
test\unit\extraction\start-extraction-run.test.ts-122-    await startExtractionRun(
test\unit\extraction\start-extraction-run.test.ts-123-      { documents: new FakeDocumentReader(doc), runs, executions },
test\unit\extraction\start-extraction-run.test.ts:124:      { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT },
test\unit\extraction\start-extraction-run.test.ts-125-    );
test\unit\extraction\start-extraction-run.test.ts-126-
test\unit\extraction\start-extraction-run.test.ts-127-    expect(executions.calls[0]?.input.fileName).toBe("invoice-2026.PNG");
test\unit\extraction\start-extraction-run.test.ts-128-    expect(executions.calls[0]?.input.contentType).toBe("image/png");
--
test\unit\extraction\start-extraction-run.test.ts-131-  it("is idempotent: a second invocation for the same document/version finds the run already there but still calls startExecution (AWS-side idempotency is the real dedup, see the port's doc comment)", async () => {
test\unit\extraction\start-extraction-run.test.ts-132-    const runs = new FakeExtractionRunStore();
test\unit\extraction\start-extraction-run.test.ts-133-    const executions = new FakeExecutionStarter();
test\unit\extraction\start-extraction-run.test.ts-134-    const deps = { documents: new FakeDocumentReader(cleanDocument()), runs, executions };
test\unit\extraction\start-extraction-run.test.ts:135:    const input = { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT };
test\unit\extraction\start-extraction-run.test.ts-136-
test\unit\extraction\start-extraction-run.test.ts-137-    const first = await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts-138-    const second = await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts-139-
--
test\unit\extraction\start-extraction-run.test.ts-152-        if (attempt === 1) throw new Error("transient SFN throttling");
test\unit\extraction\start-extraction-run.test.ts-153-      },
test\unit\extraction\start-extraction-run.test.ts-154-    };
test\unit\extraction\start-extraction-run.test.ts-155-    const deps = { documents: new FakeDocumentReader(cleanDocument()), runs, executions: flaky };
test\unit\extraction\start-extraction-run.test.ts:156:    const input = { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT };
test\unit\extraction\start-extraction-run.test.ts-157-
test\unit\extraction\start-extraction-run.test.ts-158-    await expect(startExtractionRun(deps, input)).rejects.toThrow("transient SFN throttling");
test\unit\extraction\start-extraction-run.test.ts-159-    // Retry: the ExtractionRun record already exists (putIfAbsent -> false), but
test\unit\extraction\start-extraction-run.test.ts-160-    // startExecution must still be attempted - not skipped just because the record is there.
--
test\unit\extraction\start-extraction-run.test.ts-166-  it("derives a distinct run for a new document version - a re-uploaded/re-scanned document starts a fresh run", async () => {
test\unit\extraction\start-extraction-run.test.ts-167-    const runs = new FakeExtractionRunStore();
test\unit\extraction\start-extraction-run.test.ts-168-    const executions = new FakeExecutionStarter();
test\unit\extraction\start-extraction-run.test.ts-169-    const deps = { documents: new FakeDocumentReader(cleanDocument({ version: 3 })), runs, executions };
test\unit\extraction\start-extraction-run.test.ts:170:    const input = { tenantId: "t1", itemId: "item1", documentId: "doc1", cleanObject: CLEAN_OBJECT };
test\unit\extraction\start-extraction-run.test.ts-171-    await startExtractionRun(deps, input);
test\unit\extraction\start-extraction-run.test.ts-172-
test\unit\extraction\start-extraction-run.test.ts-173-    const deps2 = { documents: new FakeDocumentReader(cleanDocument({ version: 4 })), runs, executions };
test\unit\extraction\start-extraction-run.test.ts-174-    const outcome = await startExtractionRun(deps2, input);
--
test\unit\subject\subject-service.test.ts-39-    );
test\unit\subject\subject-service.test.ts-40-    expect(entitlement?.activeTrackedSubjectsCount).toBe(1);
test\unit\subject\subject-service.test.ts-41-    expect(entitlement?.activeTrackedSubjectsLimit).toBe(DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT);
test\unit\subject\subject-service.test.ts-42-
test\unit\subject\subject-service.test.ts:43:    const audits = store.allItems().filter((i) => i["entityType"] === "SubjectAuditEvent");
test\unit\subject\subject-service.test.ts-44-    expect(audits).toHaveLength(1);
test\unit\subject\subject-service.test.ts-45-    expect(audits[0]?.["action"]).toBe("CREATE");
test\unit\subject\subject-service.test.ts-46-  });
test\unit\subject\subject-service.test.ts-47-
--
test\unit\subject\subject-service.test.ts-56-      await service.createSubject(ctx(), { type: "VENDOR", displayName: `Subject ${i}` });
test\unit\subject\subject-service.test.ts-57-    }
test\unit\subject\subject-service.test.ts-58-    await expect(service.createSubject(ctx(), { type: "VENDOR", displayName: "one too many" })).rejects.toBeInstanceOf(QuotaExceededError);
test\unit\subject\subject-service.test.ts-59-
test\unit\subject\subject-service.test.ts:60:    const subjects = store.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\subject\subject-service.test.ts-61-    expect(subjects).toHaveLength(DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT);
test\unit\subject\subject-service.test.ts-62-  });
test\unit\subject\subject-service.test.ts-63-
test\unit\subject\subject-service.test.ts-64-  it("getSubject 404s on a soft-deleted subject", async () => {
--
test\unit\subject\subject-service.test.ts-93-  it("archiving a subject frees a slot that a new subject can then use", async () => {
test\unit\subject\subject-service.test.ts-94-    for (let i = 0; i < DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT; i++) {
test\unit\subject\subject-service.test.ts-95-      await service.createSubject(ctx(), { type: "VENDOR", displayName: `Subject ${i}` });
test\unit\subject\subject-service.test.ts-96-    }
test\unit\subject\subject-service.test.ts:97:    const all = store.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\subject\subject-service.test.ts-98-    const first = all[0] as unknown as { subjectId: string; version: number };
test\unit\subject\subject-service.test.ts-99-    await service.archiveSubject(ctx(), first.subjectId, first.version);
test\unit\subject\subject-service.test.ts-100-
test\unit\subject\subject-service.test.ts-101-    await expect(service.createSubject(ctx(), { type: "VENDOR", displayName: "fits now" })).resolves.toBeDefined();
--
test\unit\subject\document-chasing-materializer.test.ts-27-
test\unit\subject\document-chasing-materializer.test.ts-28-    expect(result.skippedPast).toEqual([]);
test\unit\subject\document-chasing-materializer.test.ts-29-    expect(result.created.map((o) => o.tier).sort()).toEqual(["EXPIRED", "T3", "T7"]);
test\unit\subject\document-chasing-materializer.test.ts-30-    expect(result.created.every((o) => o.status === "SCHEDULED")).toBe(true);
test\unit\subject\document-chasing-materializer.test.ts:31:    expect(result.created.every((o) => o.entityType === "DocumentChasingOccurrence")).toBe(true);
test\unit\subject\document-chasing-materializer.test.ts-32-
test\unit\subject\document-chasing-materializer.test.ts-33-    const t7 = result.created.find((o) => o.tier === "T7")!;
test\unit\subject\document-chasing-materializer.test.ts-34-    const t3 = result.created.find((o) => o.tier === "T3")!;
test\unit\subject\document-chasing-materializer.test.ts-35-    const expired = result.created.find((o) => o.tier === "EXPIRED")!;
--
test\unit\document\document-state-machine.test.ts-8-  it("rejects on THREATS_FOUND regardless of upload validity", () => {
test\unit\document\document-state-machine.test.ts-9-    const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-10-      currentStatus: "SCANNING",
test\unit\document\document-state-machine.test.ts-11-      uploadValid: true,
test\unit\document\document-state-machine.test.ts:12:      malwareEvidence: { object, status: "THREATS_FOUND", scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-13-    });
test\unit\document\document-state-machine.test.ts-14-    expect(decision).toEqual({ action: "REJECT", status: "REJECTED" });
test\unit\document\document-state-machine.test.ts-15-  });
test\unit\document\document-state-machine.test.ts-16-
test\unit\document\document-state-machine.test.ts-17-  it("rejects as UNSUPPORTED when GuardDuty reports UNSUPPORTED", () => {
test\unit\document\document-state-machine.test.ts-18-    const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-19-      currentStatus: "SCANNING",
test\unit\document\document-state-machine.test.ts-20-      uploadValid: true,
test\unit\document\document-state-machine.test.ts:21:      malwareEvidence: { object, status: "UNSUPPORTED", scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-22-    });
test\unit\document\document-state-machine.test.ts-23-    expect(decision).toEqual({ action: "REJECT", status: "UNSUPPORTED" });
test\unit\document\document-state-machine.test.ts-24-  });
test\unit\document\document-state-machine.test.ts-25-
--
test\unit\document\document-state-machine.test.ts-35-
test\unit\document\document-state-machine.test.ts-36-  it("awaits more evidence when malware is clean but upload evidence hasn't arrived yet (malware arrived first)", () => {
test\unit\document\document-state-machine.test.ts-37-    const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-38-      currentStatus: "PENDING_UPLOAD",
test\unit\document\document-state-machine.test.ts:39:      malwareEvidence: { object, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-40-    });
test\unit\document\document-state-machine.test.ts-41-    expect(decision).toEqual({ action: "AWAIT_MORE_EVIDENCE" });
test\unit\document\document-state-machine.test.ts-42-  });
test\unit\document\document-state-machine.test.ts-43-
test\unit\document\document-state-machine.test.ts-44-  it("promotes only when upload is confirmed valid AND malware is clean (upload-then-malware order)", () => {
test\unit\document\document-state-machine.test.ts-45-    const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-46-      currentStatus: "SCANNING",
test\unit\document\document-state-machine.test.ts-47-      uploadValid: true,
test\unit\document\document-state-machine.test.ts:48:      malwareEvidence: { object, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-49-    });
test\unit\document\document-state-machine.test.ts-50-    expect(decision).toEqual({ action: "PROMOTE" });
test\unit\document\document-state-machine.test.ts-51-  });
test\unit\document\document-state-machine.test.ts-52-
--
test\unit\document\document-state-machine.test.ts-55-    // the same action once both evidences are known, per M6 design's own acceptance criterion.
test\unit\document\document-state-machine.test.ts-56-    const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-57-      currentStatus: "SCANNING",
test\unit\document\document-state-machine.test.ts-58-      uploadValid: true,
test\unit\document\document-state-machine.test.ts:59:      malwareEvidence: { object, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-60-    });
test\unit\document\document-state-machine.test.ts-61-    expect(decision).toEqual({ action: "PROMOTE" });
test\unit\document\document-state-machine.test.ts-62-  });
test\unit\document\document-state-machine.test.ts-63-
--
test\unit\document\document-state-machine.test.ts-65-    for (const status of ["CLEAN", "REJECTED", "UNSUPPORTED", "TIMEOUT", "DELETED"] as const) {
test\unit\document\document-state-machine.test.ts-66-      const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-67-        currentStatus: status,
test\unit\document\document-state-machine.test.ts-68-        uploadValid: true,
test\unit\document\document-state-machine.test.ts:69:        malwareEvidence: { object, status: "THREATS_FOUND", scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-70-      });
test\unit\document\document-state-machine.test.ts-71-      expect(decision).toEqual({ action: "IGNORE_STALE_EVENT" });
test\unit\document\document-state-machine.test.ts-72-    }
test\unit\document\document-state-machine.test.ts-73-  });
--
test\unit\document\document-state-machine.test.ts-76-    for (const status of ["ACCESS_DENIED", "FAILED"] as const) {
test\unit\document\document-state-machine.test.ts-77-      const decision = decideNextAction({
test\unit\document\document-state-machine.test.ts-78-        currentStatus: "SCANNING",
test\unit\document\document-state-machine.test.ts-79-        uploadValid: true,
test\unit\document\document-state-machine.test.ts:80:        malwareEvidence: { object, status, scanResultId: "s1", observedAt: "2026-01-01T00:00:00.000Z" },
test\unit\document\document-state-machine.test.ts-81-      });
test\unit\document\document-state-machine.test.ts-82-      expect(decision).toEqual({ action: "AWAIT_MORE_EVIDENCE" });
test\unit\document\document-state-machine.test.ts-83-    }
test\unit\document\document-state-machine.test.ts-84-  });
--
test\unit\subject\document-chasing-dispatch.test.ts-57-
test\unit\subject\document-chasing-dispatch.test.ts-58-  async function seedOccurrence(tier: DocumentChasingTier, status: "CLAIMED" | "SCHEDULED" | "TRIGGERED" = "CLAIMED"): Promise<DocumentChasingOccurrence> {
test\unit\subject\document-chasing-dispatch.test.ts-59-    const occurrence: DocumentChasingOccurrence = {
test\unit\subject\document-chasing-dispatch.test.ts-60-      ...documentChasingOccurrenceKey(TENANT, SUBJECT, ASSIGNMENT, DOCREQ, SCHEDULED_AT, OCC_ID),
test\unit\subject\document-chasing-dispatch.test.ts:61:      entityType: "DocumentChasingOccurrence",
test\unit\subject\document-chasing-dispatch.test.ts-62-      occurrenceId: OCC_ID,
test\unit\subject\document-chasing-dispatch.test.ts-63-      tenantId: TENANT,
test\unit\subject\document-chasing-dispatch.test.ts-64-      subjectId: SUBJECT,
test\unit\subject\document-chasing-dispatch.test.ts-65-      assignmentId: ASSIGNMENT,
--
test\unit\subject\document-chasing-dispatch.test.ts-81-  async function seedRequest(status: DocumentRequestStatus = "REQUESTED", version = 1): Promise<DocumentRequest> {
test\unit\subject\document-chasing-dispatch.test.ts-82-    const issued = issueGuestToken(PEPPER);
test\unit\subject\document-chasing-dispatch.test.ts-83-    const request: DocumentRequest = {
test\unit\subject\document-chasing-dispatch.test.ts-84-      ...documentRequestKey(TENANT, SUBJECT, ASSIGNMENT, DOCREQ),
test\unit\subject\document-chasing-dispatch.test.ts:85:      entityType: "DocumentRequest",
test\unit\subject\document-chasing-dispatch.test.ts-86-      documentRequestId: DOCREQ,
test\unit\subject\document-chasing-dispatch.test.ts-87-      tenantId: TENANT,
test\unit\subject\document-chasing-dispatch.test.ts-88-      subjectId: SUBJECT,
test\unit\subject\document-chasing-dispatch.test.ts-89-      assignmentId: ASSIGNMENT,
--
test\unit\subject\document-chasing-dispatch.test.ts-103-    };
test\unit\subject\document-chasing-dispatch.test.ts-104-    await store.putIfAbsent(request);
test\unit\subject\document-chasing-dispatch.test.ts-105-    await store.putIfAbsent({
test\unit\subject\document-chasing-dispatch.test.ts-106-      ...guestTokenPointerKey(issued.selectorHash),
test\unit\subject\document-chasing-dispatch.test.ts:107:      entityType: "GuestTokenPointer",
test\unit\subject\document-chasing-dispatch.test.ts-108-      selectorHash: issued.selectorHash,
test\unit\subject\document-chasing-dispatch.test.ts-109-      secretHash: issued.secretHash,
test\unit\subject\document-chasing-dispatch.test.ts-110-      tenantId: TENANT,
test\unit\subject\document-chasing-dispatch.test.ts-111-      subjectId: SUBJECT,
--
test\unit\subject\document-chasing-dispatch.test.ts-123-
test\unit\subject\document-chasing-dispatch.test.ts-124-  async function seedAssignment(): Promise<RequirementAssignment> {
test\unit\subject\document-chasing-dispatch.test.ts-125-    const assignment: RequirementAssignment = {
test\unit\subject\document-chasing-dispatch.test.ts-126-      ...requirementAssignmentKey(TENANT, SUBJECT, ASSIGNMENT),
test\unit\subject\document-chasing-dispatch.test.ts:127:      entityType: "RequirementAssignment",
test\unit\subject\document-chasing-dispatch.test.ts-128-      assignmentId: ASSIGNMENT,
test\unit\subject\document-chasing-dispatch.test.ts-129-      tenantId: TENANT,
test\unit\subject\document-chasing-dispatch.test.ts-130-      subjectId: SUBJECT,
test\unit\subject\document-chasing-dispatch.test.ts-131-      requirementName: "Certidão negativa",
--
test\unit\subject\requirement-service.test.ts-10-
test\unit\subject\requirement-service.test.ts-11-function makeSubmission(overrides: Partial<DocumentSubmission> & { subjectId: string; assignmentId: string; submissionId: string }): DocumentSubmission {
test\unit\subject\requirement-service.test.ts-12-  return {
test\unit\subject\requirement-service.test.ts-13-    ...documentSubmissionKey("tenant-1", overrides.subjectId, overrides.assignmentId, overrides.submissionId),
test\unit\subject\requirement-service.test.ts:14:    entityType: "DocumentSubmission",
test\unit\subject\requirement-service.test.ts-15-    tenantId: "tenant-1",
test\unit\subject\requirement-service.test.ts-16-    documentRequestId: "request-1",
test\unit\subject\requirement-service.test.ts-17-    fileName: "a.pdf",
test\unit\subject\requirement-service.test.ts-18-    mediaType: "application/pdf",
--
test\unit\extraction\run-extraction-validation.test.ts-22-
test\unit\extraction\run-extraction-validation.test.ts-23-function makeDocument(overrides: Partial<Document> = {}): Document {
test\unit\extraction\run-extraction-validation.test.ts-24-  return {
test\unit\extraction\run-extraction-validation.test.ts-25-    ...documentKey("t1", "item1", "doc1"),
test\unit\extraction\run-extraction-validation.test.ts:26:    entityType: "Document",
test\unit\extraction\run-extraction-validation.test.ts-27-    tenantId: "t1",
test\unit\extraction\run-extraction-validation.test.ts-28-    itemId: "item1",
test\unit\extraction\run-extraction-validation.test.ts-29-    documentId: "doc1",
test\unit\extraction\run-extraction-validation.test.ts-30-    uploadSlotId: "slot1",
--
test\unit\extraction\run-extraction-validation.test.ts-52-function makeItem(overrides: Partial<ExpirationItem> = {}): ExpirationItem {
test\unit\extraction\run-extraction-validation.test.ts-53-  return {
test\unit\extraction\run-extraction-validation.test.ts-54-    ...itemKey("t1", "item1"),
test\unit\extraction\run-extraction-validation.test.ts-55-    SK: "META",
test\unit\extraction\run-extraction-validation.test.ts:56:    entityType: "ExpirationItem",
test\unit\extraction\run-extraction-validation.test.ts-57-    itemId: "item1",
test\unit\extraction\run-extraction-validation.test.ts-58-    tenantId: "t1",
test\unit\extraction\run-extraction-validation.test.ts-59-    name: "Alvará",
test\unit\extraction\run-extraction-validation.test.ts-60-    category: "licenca",
--
test\unit\document\document-service.test.ts-56-    expect(result.uploadSlotId).toBeTruthy();
test\unit\document\document-service.test.ts-57-    expect(result.uploadUrl).toContain(BUCKET);
test\unit\document\document-service.test.ts-58-
test\unit\document\document-service.test.ts-59-    const items = store.allItems();
test\unit\document\document-service.test.ts:60:    const doc = items.find((i) => i["entityType"] === "Document");
test\unit\document\document-service.test.ts:61:    const slot = items.find((i) => i["entityType"] === "UploadSlot");
test\unit\document\document-service.test.ts-62-    expect(doc?.["status"]).toBe("PENDING_UPLOAD");
test\unit\document\document-service.test.ts-63-    expect(slot?.["status"]).toBe("RESERVED");
test\unit\document\document-service.test.ts-64-    expect(doc?.["quarantineObject"]).toBeDefined();
test\unit\document\document-service.test.ts-65-  });
test\unit\document\document-service.test.ts-66-
test\unit\document\document-service.test.ts-67-  it("real bug found via Camada 3 verification against AWS real (2026-08-25): the created UploadSlot must carry GSI6PK/GSI6SK, or UploadSlotReconciliationWorker's sweep never discovers it - confirmed empirically against the deployed dev Lambda (0 results without these fields, correctly reconciled once added)", async () => {
test\unit\document\document-service.test.ts-68-    const { store, service } = buildService();
test\unit\document\document-service.test.ts-69-    const result = await service.reserveUpload(ctx(), "item1", { fileName: "a.pdf", mediaType: "application/pdf", contentLength: 100, checksumSha256: VALID_SHA256 }, "idem-gsi6-1");
test\unit\document\document-service.test.ts:70:    const slot = store.allItems().find((i) => i["entityType"] === "UploadSlot") as Record<string, unknown> | undefined;
test\unit\document\document-service.test.ts-71-    expect(slot?.["GSI6PK"]).toBe("RECON#UPLOAD#PENDING");
test\unit\document\document-service.test.ts-72-    expect(slot?.["GSI6SK"]).toBe(`${result.expiresAt}#TENANT#t1#SLOT#${result.uploadSlotId}`);
test\unit\document\document-service.test.ts-73-  });
test\unit\document\document-service.test.ts-74-
test\unit\document\document-service.test.ts-75-  it("never embeds the original file name in the quarantine object key (tenantId/itemId/documentId ARE embedded deliberately - see quarantine-key.ts)", async () => {
test\unit\document\document-service.test.ts-76-    const { store, service } = buildService();
test\unit\document\document-service.test.ts-77-    const result = await service.reserveUpload(ctx(), "item1", { fileName: "sensitive-name-joão.pdf", mediaType: "application/pdf", contentLength: 1000, checksumSha256: VALID_SHA256 }, "idem-2");
test\unit\document\document-service.test.ts:78:    const doc = store.allItems().find((i) => i["entityType"] === "Document") as { quarantineObject: { key: string } } | undefined;
test\unit\document\document-service.test.ts-79-    expect(doc?.quarantineObject.key).not.toContain("joão");
test\unit\document\document-service.test.ts-80-    expect(doc?.quarantineObject.key).not.toContain("sensitive-name");
test\unit\document\document-service.test.ts-81-    // tenantId/itemId/documentId are deliberately embedded - the S3 event that later reaches
test\unit\document\document-service.test.ts-82-    // UploadFinalizerWorker/MalwareResultWorker carries no application context beyond
--
test\unit\document\document-service.test.ts-139-
test\unit\document\document-service.test.ts-140-  it("getDocument throws NotFoundError for a DELETED document (soft-deleted rows never surface, same convention as ExpirationService.readActiveItem)", async () => {
test\unit\document\document-service.test.ts-141-    const { store, service } = buildService();
test\unit\document\document-service.test.ts-142-    const reserved = await service.reserveUpload(ctx(), "item1", { fileName: "a.pdf", mediaType: "application/pdf", contentLength: 100, checksumSha256: VALID_SHA256 }, "idem-get-2");
test\unit\document\document-service.test.ts:143:    const doc = store.allItems().find((i) => i["entityType"] === "Document") as Record<string, unknown> & { PK: string; SK: string };
test\unit\document\document-service.test.ts-144-    await store.update({ ...doc, status: "DELETED" });
test\unit\document\document-service.test.ts-145-    await expect(service.getDocument(ctx(), "item1", reserved.documentId)).rejects.toThrow(/not found/i);
test\unit\document\document-service.test.ts-146-  });
test\unit\document\document-service.test.ts-147-
test\unit\document\document-service.test.ts-148-  it("listDocuments returns every non-deleted document under the item, excludes DELETED", async () => {
test\unit\document\document-service.test.ts-149-    const { store, service } = buildService();
test\unit\document\document-service.test.ts-150-    const first = await service.reserveUpload(ctx(), "item1", { fileName: "a.pdf", mediaType: "application/pdf", contentLength: 100, checksumSha256: VALID_SHA256 }, "idem-list-1");
test\unit\document\document-service.test.ts-151-    const second = await service.reserveUpload(ctx(), "item1", { fileName: "b.pdf", mediaType: "application/pdf", contentLength: 100, checksumSha256: VALID_SHA256 }, "idem-list-2");
test\unit\document\document-service.test.ts:152:    const secondDoc = store.allItems().find((i) => i["entityType"] === "Document" && i["documentId"] === second.documentId) as Record<string, unknown> & { PK: string; SK: string };
test\unit\document\document-service.test.ts-153-    await store.update({ ...secondDoc, status: "DELETED" });
test\unit\document\document-service.test.ts-154-
test\unit\document\document-service.test.ts-155-    const documents = await service.listDocuments(ctx(), "item1");
test\unit\document\document-service.test.ts-156-    expect(documents.map((d) => d.documentId)).toEqual([first.documentId]);
--
test\unit\document\document-service.test.ts-183-    const tenantADoc = await service.reserveUpload(ctx(), "item1", { fileName: "a.pdf", mediaType: "application/pdf", contentLength: 100, checksumSha256: VALID_SHA256 }, "idem-cross-3");
test\unit\document\document-service.test.ts-184-    const tenantBDoc = await service.reserveUpload(ctxFor("t2"), "item1", { fileName: "a.pdf", mediaType: "application/pdf", contentLength: 100, checksumSha256: VALID_SHA256 }, "idem-cross-4");
test\unit\document\document-service.test.ts-185-
test\unit\document\document-service.test.ts-186-    expect(tenantADoc.documentId).not.toBe(tenantBDoc.documentId);
test\unit\document\document-service.test.ts:187:    const rows = store.allItems().filter((i) => i["entityType"] === "Document") as Array<Record<string, unknown>>;
test\unit\document\document-service.test.ts-188-    expect(rows.find((r) => r["documentId"] === tenantADoc.documentId)?.["tenantId"]).toBe("t1");
test\unit\document\document-service.test.ts-189-    expect(rows.find((r) => r["documentId"] === tenantBDoc.documentId)?.["tenantId"]).toBe("t2");
test\unit\document\document-service.test.ts-190-    await expect(service.getDocument(ctxFor("t2"), "item1", tenantADoc.documentId)).rejects.toThrow(/not found/i);
test\unit\document\document-service.test.ts-191-    await expect(service.getDocument(ctx(), "item1", tenantBDoc.documentId)).rejects.toThrow(/not found/i);
--
src\modules\subject\domain\tracked-subject.ts-12-export type TrackedSubjectStatus = "ACTIVE" | "ARCHIVED" | "DELETED";
src\modules\subject\domain\tracked-subject.ts-13-
src\modules\subject\domain\tracked-subject.ts-14-export interface TrackedSubject extends EntityKey {
src\modules\subject\domain\tracked-subject.ts-15-  SK: "META";
src\modules\subject\domain\tracked-subject.ts:16:  entityType: "TrackedSubject";
src\modules\subject\domain\tracked-subject.ts-17-  subjectId: string;
src\modules\subject\domain\tracked-subject.ts-18-  tenantId: string;
src\modules\subject\domain\tracked-subject.ts-19-  type: TrackedSubjectType;
src\modules\subject\domain\tracked-subject.ts-20-  displayName: string;
--
test\unit\extraction\dynamodb-extracted-field-store.test.ts-33-}
test\unit\extraction\dynamodb-extracted-field-store.test.ts-34-
test\unit\extraction\dynamodb-extracted-field-store.test.ts-35-const field: ExtractedField = {
test\unit\extraction\dynamodb-extracted-field-store.test.ts-36-  ...extractedFieldKey("t1", "doc1", "expirationDate", "run1"),
test\unit\extraction\dynamodb-extracted-field-store.test.ts:37:  entityType: "ExtractedField",
test\unit\extraction\dynamodb-extracted-field-store.test.ts-38-  tenantId: "t1",
test\unit\extraction\dynamodb-extracted-field-store.test.ts-39-  documentId: "doc1",
test\unit\extraction\dynamodb-extracted-field-store.test.ts-40-  runId: "run1",
test\unit\extraction\dynamodb-extracted-field-store.test.ts-41-  fieldName: "expirationDate",
--
test\unit\document\document-deletion-service.test.ts-19-
test\unit\document\document-deletion-service.test.ts-20-function baseDocument(overrides: Partial<Document> = {}): Document {
test\unit\document\document-deletion-service.test.ts-21-  return {
test\unit\document\document-deletion-service.test.ts-22-    ...documentKey("t1", "item1", "doc1"),
test\unit\document\document-deletion-service.test.ts:23:    entityType: "Document",
test\unit\document\document-deletion-service.test.ts-24-    tenantId: "t1",
test\unit\document\document-deletion-service.test.ts-25-    itemId: "item1",
test\unit\document\document-deletion-service.test.ts-26-    documentId: "doc1",
test\unit\document\document-deletion-service.test.ts-27-    uploadSlotId: "slot1",
--
src\workers\malware-result\result-processor.ts-51-    if (doc.quarantineObject.bucket !== input.object.bucket || doc.quarantineObject.key !== input.object.key) {
src\workers\malware-result\result-processor.ts-52-      return "IGNORED_WRONG_OBJECT";
src\workers\malware-result\result-processor.ts-53-    }
src\workers\malware-result\result-processor.ts-54-
src\workers\malware-result\result-processor.ts:55:    if (doc.malwareEvidence?.scanResultId === input.scanResultId) return "IGNORED_DUPLICATE_SCAN";
src\workers\malware-result\result-processor.ts-56-    if (doc.status !== "PENDING_UPLOAD" && doc.status !== "SCANNING") return "IGNORED_STALE";
src\workers\malware-result\result-processor.ts-57-
src\workers\malware-result\result-processor.ts:58:    const malwareEvidence = { object: input.object, status: input.status, scanResultId: input.scanResultId, observedAt: now() };
src\workers\malware-result\result-processor.ts-59-
src\workers\malware-result\result-processor.ts-60-    try {
src\workers\malware-result\result-processor.ts-61-      await deps.store.transactWrite([
src\workers\malware-result\result-processor.ts:62:        { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: doc.version, set: { malwareEvidence } }) },
src\workers\malware-result\result-processor.ts-63-      ]);
src\workers\malware-result\result-processor.ts-64-    } catch (err) {
src\workers\malware-result\result-processor.ts-65-      if (isTransactionCanceled(err)) continue; // concurrent evidence write raced us - re-read fresh and retry.
src\workers\malware-result\result-processor.ts-66-      throw err;
--
test\unit\extraction\complete-ocr.test.ts-83-
test\unit\extraction\complete-ocr.test.ts-84-function baseJob(overrides: Partial<TextractJob> = {}): TextractJob {
test\unit\extraction\complete-ocr.test.ts-85-  return {
test\unit\extraction\complete-ocr.test.ts-86-    ...textractJobKey("job_1"),
test\unit\extraction\complete-ocr.test.ts:87:    entityType: "TextractJob",
test\unit\extraction\complete-ocr.test.ts-88-    jobId: "job_1",
test\unit\extraction\complete-ocr.test.ts-89-    tenantId: "t1",
test\unit\extraction\complete-ocr.test.ts-90-    itemId: "item1",
test\unit\extraction\complete-ocr.test.ts-91-    documentId: "doc1",
--
src\modules\subject\domain\requirement-assignment.ts-25-  | "REJECTED"
src\modules\subject\domain\requirement-assignment.ts-26-  | "SATISFIED";
src\modules\subject\domain\requirement-assignment.ts-27-
src\modules\subject\domain\requirement-assignment.ts-28-export interface RequirementAssignment extends EntityKey {
src\modules\subject\domain\requirement-assignment.ts:29:  entityType: "RequirementAssignment";
src\modules\subject\domain\requirement-assignment.ts-30-  assignmentId: string;
src\modules\subject\domain\requirement-assignment.ts-31-  subjectId: string;
src\modules\subject\domain\requirement-assignment.ts-32-  tenantId: string;
src\modules\subject\domain\requirement-assignment.ts-33-  requirementName: string;
--
test\unit\document\advance-after-evidence.test.ts-11-
test\unit\document\advance-after-evidence.test.ts-12-function baseDocument(overrides: Partial<Document> = {}): Document {
test\unit\document\advance-after-evidence.test.ts-13-  return {
test\unit\document\advance-after-evidence.test.ts-14-    ...documentKey("t1", "item1", "doc1"),
test\unit\document\advance-after-evidence.test.ts:15:    entityType: "Document",
test\unit\document\advance-after-evidence.test.ts-16-    tenantId: "t1",
test\unit\document\advance-after-evidence.test.ts-17-    itemId: "item1",
test\unit\document\advance-after-evidence.test.ts-18-    documentId: "doc1",
test\unit\document\advance-after-evidence.test.ts-19-    uploadSlotId: "slot1",
--
test\unit\document\advance-after-evidence.test.ts-33-
test\unit\document\advance-after-evidence.test.ts-34-function baseSlot(overrides: Partial<UploadSlot> = {}): UploadSlot {
test\unit\document\advance-after-evidence.test.ts-35-  return {
test\unit\document\advance-after-evidence.test.ts-36-    ...uploadSlotKey("t1", "slot1"),
test\unit\document\advance-after-evidence.test.ts:37:    entityType: "UploadSlot",
test\unit\document\advance-after-evidence.test.ts-38-    tenantId: "t1",
test\unit\document\advance-after-evidence.test.ts-39-    uploadSlotId: "slot1",
test\unit\document\advance-after-evidence.test.ts-40-    documentId: "doc1",
test\unit\document\advance-after-evidence.test.ts-41-    itemId: "item1",
--
test\unit\document\advance-after-evidence.test.ts-65-describe("advanceAfterEvidence — corrida real entre upload e malware", () => {
test\unit\document\advance-after-evidence.test.ts-66-  it("stays AWAITING when only upload evidence exists (upload arrived first)", async () => {
test\unit\document\advance-after-evidence.test.ts-67-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-68-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts:69:      baseDocument({ status: "SCANNING", uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" } }),
test\unit\document\advance-after-evidence.test.ts-70-    );
test\unit\document\advance-after-evidence.test.ts-71-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-72-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\advance-after-evidence.test.ts-73-      { tenantId: "t1", itemId: "item1", documentId: "doc1", expectedObject: QUARANTINE_OBJECT },
--
test\unit\document\advance-after-evidence.test.ts-77-
test\unit\document\advance-after-evidence.test.ts-78-  it("stays AWAITING when only malware-clean evidence exists (malware arrived first, before upload confirmation)", async () => {
test\unit\document\advance-after-evidence.test.ts-79-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-80-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts:81:      baseDocument({ status: "PENDING_UPLOAD", malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:01:00.000Z" } }),
test\unit\document\advance-after-evidence.test.ts-82-    );
test\unit\document\advance-after-evidence.test.ts-83-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-84-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\advance-after-evidence.test.ts-85-      { tenantId: "t1", itemId: "item1", documentId: "doc1", expectedObject: QUARANTINE_OBJECT },
--
test\unit\document\advance-after-evidence.test.ts-91-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-92-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-93-      baseDocument({
test\unit\document\advance-after-evidence.test.ts-94-        status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:95:        uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:96:        malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-97-      }),
test\unit\document\advance-after-evidence.test.ts-98-    );
test\unit\document\advance-after-evidence.test.ts-99-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-100-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
--
test\unit\document\advance-after-evidence.test.ts-102-    );
test\unit\document\advance-after-evidence.test.ts-103-    expect(outcome).toBe("PROMOTED");
test\unit\document\advance-after-evidence.test.ts-104-    const doc = (await store.get(documentKey("t1", "item1", "doc1"))) as Document;
test\unit\document\advance-after-evidence.test.ts-105-    expect(doc.status).toBe("CLEAN");
test\unit\document\advance-after-evidence.test.ts:106:    expect(doc.cleanObject?.bucket).toBe(CLEAN_BUCKET);
test\unit\document\advance-after-evidence.test.ts-107-  });
test\unit\document\advance-after-evidence.test.ts-108-
test\unit\document\advance-after-evidence.test.ts-109-  it("real bug found via Camada 3 (2026-08-22): promotion copies from the evidence's real object reference, never doc.quarantineObject (whose versionId is always \"\" - reserveUpload sets it before the real object exists, and copying with an empty versionId crashes S3 with 'Version id cannot be the empty string')", async () => {
test\unit\document\advance-after-evidence.test.ts-110-    const store = new InMemoryDocumentStore();
--
test\unit\document\advance-after-evidence.test.ts-113-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-114-      baseDocument({
test\unit\document\advance-after-evidence.test.ts-115-        status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts-116-        quarantineObject: emptyVersionPlaceholder,
test\unit\document\advance-after-evidence.test.ts:117:        uploadEvidence: { object: realObject, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:118:        malwareEvidence: { object: realObject, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-119-      }),
test\unit\document\advance-after-evidence.test.ts-120-    );
test\unit\document\advance-after-evidence.test.ts-121-    let copiedFrom: { bucket: string; key: string; versionId: string } | undefined;
test\unit\document\advance-after-evidence.test.ts-122-    const outcome = await advanceAfterEvidence(
--
test\unit\document\advance-after-evidence.test.ts-131-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-132-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-133-      baseDocument({
test\unit\document\advance-after-evidence.test.ts-134-        status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:135:        malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:136:        uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-137-      }),
test\unit\document\advance-after-evidence.test.ts-138-    );
test\unit\document\advance-after-evidence.test.ts-139-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-140-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
--
test\unit\document\advance-after-evidence.test.ts-144-  });
test\unit\document\advance-after-evidence.test.ts-145-
test\unit\document\advance-after-evidence.test.ts-146-  it("rejects immediately on THREATS_FOUND even without upload evidence yet", async () => {
test\unit\document\advance-after-evidence.test.ts-147-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts:148:    await store.putIfAbsent(baseDocument({ status: "PENDING_UPLOAD", malwareEvidence: { object: QUARANTINE_OBJECT, status: "THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:01:00.000Z" } }));
test\unit\document\advance-after-evidence.test.ts-149-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-150-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\advance-after-evidence.test.ts-151-      { tenantId: "t1", itemId: "item1", documentId: "doc1", expectedObject: QUARANTINE_OBJECT },
test\unit\document\advance-after-evidence.test.ts-152-    );
test\unit\document\advance-after-evidence.test.ts-153-    expect(outcome).toBe("REJECTED");
test\unit\document\advance-after-evidence.test.ts-154-    const doc = (await store.get(documentKey("t1", "item1", "doc1"))) as Document;
test\unit\document\advance-after-evidence.test.ts-155-    expect(doc.status).toBe("REJECTED");
test\unit\document\advance-after-evidence.test.ts:156:    expect(doc.cleanObject).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts-157-  });
test\unit\document\advance-after-evidence.test.ts-158-
test\unit\document\advance-after-evidence.test.ts-159-  it("rejects when upload evidence exists but is marked invalid, even if malware comes back clean (real bug found during implementation: presence of evidence was wrongly treated as validity)", async () => {
test\unit\document\advance-after-evidence.test.ts-160-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-161-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-162-      baseDocument({
test\unit\document\advance-after-evidence.test.ts-163-        status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:164:        uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 999, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: false, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:165:        malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-166-      }),
test\unit\document\advance-after-evidence.test.ts-167-    );
test\unit\document\advance-after-evidence.test.ts-168-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-169-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
--
test\unit\document\advance-after-evidence.test.ts-171-    );
test\unit\document\advance-after-evidence.test.ts-172-    expect(outcome).toBe("REJECTED");
test\unit\document\advance-after-evidence.test.ts-173-    const doc = (await store.get(documentKey("t1", "item1", "doc1"))) as Document;
test\unit\document\advance-after-evidence.test.ts-174-    expect(doc.status).toBe("REJECTED");
test\unit\document\advance-after-evidence.test.ts:175:    expect(doc.cleanObject).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts-176-  });
test\unit\document\advance-after-evidence.test.ts-177-
test\unit\document\advance-after-evidence.test.ts-178-  it("never promotes when copy verification fails (size mismatch on the clean object)", async () => {
test\unit\document\advance-after-evidence.test.ts-179-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-180-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-181-      baseDocument({
test\unit\document\advance-after-evidence.test.ts-182-        status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:183:        uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:184:        malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-185-      }),
test\unit\document\advance-after-evidence.test.ts-186-    );
test\unit\document\advance-after-evidence.test.ts-187-    const brokenObjects = fakeObjectStore({ headObject: async () => ({ contentLength: 50, mediaType: "application/pdf" }) });
test\unit\document\advance-after-evidence.test.ts-188-    await expect(
--
test\unit\document\advance-after-evidence.test.ts-193-  });
test\unit\document\advance-after-evidence.test.ts-194-
test\unit\document\advance-after-evidence.test.ts-195-  it("ignores late/duplicate evidence once the document is already terminal (CLEAN)", async () => {
test\unit\document\advance-after-evidence.test.ts-196-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts:197:    await store.putIfAbsent(baseDocument({ status: "CLEAN", cleanObject: { bucket: CLEAN_BUCKET, key: "clean/t1/doc1", versionId: "v1" } }));
test\unit\document\advance-after-evidence.test.ts-198-    const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-199-      { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\advance-after-evidence.test.ts-200-      { tenantId: "t1", itemId: "item1", documentId: "doc1", expectedObject: QUARANTINE_OBJECT },
test\unit\document\advance-after-evidence.test.ts-201-    );
--
test\unit\document\advance-after-evidence.test.ts-206-    const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-207-    await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-208-      baseDocument({
test\unit\document\advance-after-evidence.test.ts-209-        status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:210:        uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:211:        malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-212-      }),
test\unit\document\advance-after-evidence.test.ts-213-    );
test\unit\document\advance-after-evidence.test.ts-214-    const flakyObjects = fakeObjectStore({
test\unit\document\advance-after-evidence.test.ts-215-      deleteObjectVersion: async () => {
--
test\unit\document\advance-after-evidence.test.ts-226-      await store.putIfAbsent(baseSlot());
test\unit\document\advance-after-evidence.test.ts-227-      await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-228-        baseDocument({
test\unit\document\advance-after-evidence.test.ts-229-          status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:230:          uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:231:          malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-232-        }),
test\unit\document\advance-after-evidence.test.ts-233-      );
test\unit\document\advance-after-evidence.test.ts-234-      const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-235-        { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
--
test\unit\document\advance-after-evidence.test.ts-244-
test\unit\document\advance-after-evidence.test.ts-245-    it("marks the associated RESERVED slot CONSUMED and strips its GSI6 pointer on REJECT", async () => {
test\unit\document\advance-after-evidence.test.ts-246-      const store = new InMemoryDocumentStore();
test\unit\document\advance-after-evidence.test.ts-247-      await store.putIfAbsent(baseSlot());
test\unit\document\advance-after-evidence.test.ts:248:      await store.putIfAbsent(baseDocument({ status: "PENDING_UPLOAD", malwareEvidence: { object: QUARANTINE_OBJECT, status: "THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:01:00.000Z" } }));
test\unit\document\advance-after-evidence.test.ts-249-      const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-250-        { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
test\unit\document\advance-after-evidence.test.ts-251-        { tenantId: "t1", itemId: "item1", documentId: "doc1", expectedObject: QUARANTINE_OBJECT },
test\unit\document\advance-after-evidence.test.ts-252-      );
--
test\unit\document\advance-after-evidence.test.ts-262-      await store.putIfAbsent(baseSlot({ status: "EXPIRED", GSI6PK: undefined, GSI6SK: undefined }));
test\unit\document\advance-after-evidence.test.ts-263-      await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-264-        baseDocument({
test\unit\document\advance-after-evidence.test.ts-265-          status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts:266:          uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:267:          malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-268-        }),
test\unit\document\advance-after-evidence.test.ts-269-      );
test\unit\document\advance-after-evidence.test.ts-270-      const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-271-        { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
--
test\unit\document\advance-after-evidence.test.ts-281-      await store.putIfAbsent(
test\unit\document\advance-after-evidence.test.ts-282-        baseDocument({
test\unit\document\advance-after-evidence.test.ts-283-          status: "SCANNING",
test\unit\document\advance-after-evidence.test.ts-284-          uploadSlotId: "slot-never-existed",
test\unit\document\advance-after-evidence.test.ts:285:          uploadEvidence: { object: QUARANTINE_OBJECT, contentLength: 100, mediaType: "application/pdf", checksumSha256: "a".repeat(64), valid: true, observedAt: "2026-08-22T00:01:00.000Z" },
test\unit\document\advance-after-evidence.test.ts:286:          malwareEvidence: { object: QUARANTINE_OBJECT, status: "NO_THREATS_FOUND", scanResultId: "s1", observedAt: "2026-08-22T00:02:00.000Z" },
test\unit\document\advance-after-evidence.test.ts-287-        }),
test\unit\document\advance-after-evidence.test.ts-288-      );
test\unit\document\advance-after-evidence.test.ts-289-      const outcome = await advanceAfterEvidence(
test\unit\document\advance-after-evidence.test.ts-290-        { store, objects: fakeObjectStore(), tableName: TABLE, cleanBucket: CLEAN_BUCKET },
--
src\modules\subject\domain\guest-token.ts-16-export const GUEST_TOKEN_TTL_SECONDS = 14 * 24 * 60 * 60; // 14 dias (04-domain-model-guest-upload.md).
src\modules\subject\domain\guest-token.ts-17-
src\modules\subject\domain\guest-token.ts-18-export interface GuestTokenPointer extends EntityKey {
src\modules\subject\domain\guest-token.ts-19-  SK: "POINTER";
src\modules\subject\domain\guest-token.ts:20:  entityType: "GuestTokenPointer";
src\modules\subject\domain\guest-token.ts-21-  selectorHash: string;
src\modules\subject\domain\guest-token.ts-22-  secretHash: string;
src\modules\subject\domain\guest-token.ts-23-  tenantId: string;
src\modules\subject\domain\guest-token.ts-24-  subjectId: string;
--
test\unit\import\import-commit-service.test.ts-45-    subjects = new SubjectService({ store: subjectStore, tableName: "MainTable", ids: makeSubjectIdGenerator(), now: () => NOW });
test\unit\import\import-commit-service.test.ts-46-
test\unit\import\import-commit-service.test.ts-47-    const job: ImportJob = {
test\unit\import\import-commit-service.test.ts-48-      ...importJobKey(TENANT, JOB_ID),
test\unit\import\import-commit-service.test.ts:49:      entityType: "ImportJob",
test\unit\import\import-commit-service.test.ts-50-      jobId: JOB_ID,
test\unit\import\import-commit-service.test.ts-51-      tenantId: TENANT,
test\unit\import\import-commit-service.test.ts-52-      targetEntityType: "TrackedSubject",
test\unit\import\import-commit-service.test.ts-53-      status: "COMMITTING",
--
test\unit\import\import-commit-service.test.ts-85-    const job = await store.get<ImportJob>(importJobKey(TENANT, JOB_ID));
test\unit\import\import-commit-service.test.ts-86-    expect(job?.status).toBe("COMMITTED");
test\unit\import\import-commit-service.test.ts-87-    expect(job?.lastCommittedRowNumber).toBe(2);
test\unit\import\import-commit-service.test.ts-88-
test\unit\import\import-commit-service.test.ts:89:    const created = subjectStore.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\import\import-commit-service.test.ts-90-    expect(created).toHaveLength(2);
test\unit\import\import-commit-service.test.ts-91-  });
test\unit\import\import-commit-service.test.ts-92-
test\unit\import\import-commit-service.test.ts-93-  it("fails the job with FAILED_INTEGRITY_MISMATCH when the plan content no longer matches planSha256", async () => {
--
test\unit\import\import-commit-service.test.ts-125-
test\unit\import\import-commit-service.test.ts-126-    const second = await commitImportJob(deps(), ctx(), JOB_ID);
test\unit\import\import-commit-service.test.ts-127-    expect(second).toEqual({ kind: "COMMITTED", createdCount: 0 }); // nothing NEW created
test\unit\import\import-commit-service.test.ts-128-
test\unit\import\import-commit-service.test.ts:129:    const created = subjectStore.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\import\import-commit-service.test.ts-130-    expect(created).toHaveLength(3); // still exactly 3, never duplicated
test\unit\import\import-commit-service.test.ts-131-  });
test\unit\import\import-commit-service.test.ts-132-
test\unit\import\import-commit-service.test.ts-133-  it("retry safety: a row without externalId is never duplicated on retry either (synthetic per-row dedup key)", async () => {
--
test\unit\import\import-commit-service.test.ts-138-    await store.update<ImportJob>({ ...job!, status: "COMMITTING", lastCommittedRowNumber: 0 }); // simulate a retry that lost its cursor progress
test\unit\import\import-commit-service.test.ts-139-
test\unit\import\import-commit-service.test.ts-140-    await commitImportJob(deps(), ctx(), JOB_ID);
test\unit\import\import-commit-service.test.ts-141-
test\unit\import\import-commit-service.test.ts:142:    const created = subjectStore.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\import\import-commit-service.test.ts-143-    expect(created).toHaveLength(1); // the dedup claim (not the cursor) is what actually prevented the duplicate
test\unit\import\import-commit-service.test.ts-144-  });
test\unit\import\import-commit-service.test.ts-145-
test\unit\import\import-commit-service.test.ts-146-  it("stops fail-fast on entitlement exceeded, without processing the remaining rows", async () => {
--
test\unit\import\import-commit-service.test.ts-152-    expect(outcome).toEqual({ kind: "FAILED_ENTITLEMENT_EXCEEDED", createdCount: 1 });
test\unit\import\import-commit-service.test.ts-153-    const job = await store.get<ImportJob>(importJobKey(TENANT, JOB_ID));
test\unit\import\import-commit-service.test.ts-154-    expect(job?.status).toBe("FAILED");
test\unit\import\import-commit-service.test.ts-155-    expect(job?.failureReason).toBe("ENTITLEMENT_EXCEEDED");
test\unit\import\import-commit-service.test.ts:156:    const created = subjectStore.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\import\import-commit-service.test.ts-157-    expect(created).toHaveLength(1); // only the first row committed before the limit hit
test\unit\import\import-commit-service.test.ts-158-  });
test\unit\import\import-commit-service.test.ts-159-
test\unit\import\import-commit-service.test.ts-160-  it("resumes from lastCommittedRowNumber on a real restart, never reprocessing rows already past the cursor", async () => {
--
test\unit\import\import-commit-service.test.ts-164-
test\unit\import\import-commit-service.test.ts-165-    await commitImportJob(deps(), ctx(), JOB_ID);
test\unit\import\import-commit-service.test.ts-166-
test\unit\import\import-commit-service.test.ts-167-    // Row 1 was never (re)created here - only rows 2 and 3 should exist.
test\unit\import\import-commit-service.test.ts:168:    const created = subjectStore.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\import\import-commit-service.test.ts-169-    expect(created).toHaveLength(2);
test\unit\import\import-commit-service.test.ts-170-    expect(created.map((c) => (c as unknown as { displayName: string }).displayName).sort()).toEqual(["Subject 2", "Subject 3"]);
test\unit\import\import-commit-service.test.ts-171-  });
test\unit\import\import-commit-service.test.ts-172-
--
test\unit\import\import-commit-service.test.ts-179-
test\unit\import\import-commit-service.test.ts-180-    const outcome = await commitImportJob(deps(), ctx(), JOB_ID);
test\unit\import\import-commit-service.test.ts-181-
test\unit\import\import-commit-service.test.ts-182-    expect(outcome).toEqual({ kind: "COMMITTED", createdCount: 1 });
test\unit\import\import-commit-service.test.ts:183:    const created = subjectStore.allItems().filter((i) => i["entityType"] === "TrackedSubject");
test\unit\import\import-commit-service.test.ts-184-    expect(created).toHaveLength(1);
test\unit\import\import-commit-service.test.ts-185-  });
test\unit\import\import-commit-service.test.ts-186-
test\unit\import\import-commit-service.test.ts-187-  it("writes an ImportDedupRecord for every committed row, including synthetic keys for rows without externalId", async () => {
--
src\modules\subject\domain\entitlement.ts-19-export const DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT = 25;
src\modules\subject\domain\entitlement.ts-20-
src\modules\subject\domain\entitlement.ts-21-export interface TenantEntitlement extends EntityKey {
src\modules\subject\domain\entitlement.ts-22-  SK: "PLAN";
src\modules\subject\domain\entitlement.ts:23:  entityType: "TenantEntitlement";
src\modules\subject\domain\entitlement.ts-24-  tenantId: string;
src\modules\subject\domain\entitlement.ts-25-  planId: string;
src\modules\subject\domain\entitlement.ts-26-  activeTrackedSubjectsLimit: number;
src\modules\subject\domain\entitlement.ts-27-  activeTrackedSubjectsCount: number;
--
src\modules\subject\domain\entitlement.ts-36-
src\modules\subject\domain\entitlement.ts-37-export function defaultEntitlement(tenantId: string, now: string): TenantEntitlement {
src\modules\subject\domain\entitlement.ts-38-  return {
src\modules\subject\domain\entitlement.ts-39-    ...entitlementKey(tenantId),
src\modules\subject\domain\entitlement.ts:40:    entityType: "TenantEntitlement",
src\modules\subject\domain\entitlement.ts-41-    tenantId,
src\modules\subject\domain\entitlement.ts-42-    planId: DEFAULT_PLAN_ID,
src\modules\subject\domain\entitlement.ts-43-    activeTrackedSubjectsLimit: DEFAULT_ACTIVE_TRACKED_SUBJECTS_LIMIT,
src\modules\subject\domain\entitlement.ts-44-    activeTrackedSubjectsCount: 0,
--
test\unit\import\import-parse-service.test.ts-27-    quota = new TenantQuotaService(new InMemoryIdentityStore(), () => NOW);
test\unit\import\import-parse-service.test.ts-28-
test\unit\import\import-parse-service.test.ts-29-    const job: ImportJob = {
test\unit\import\import-parse-service.test.ts-30-      ...importJobKey(TENANT, JOB_ID),
test\unit\import\import-parse-service.test.ts:31:      entityType: "ImportJob",
test\unit\import\import-parse-service.test.ts-32-      jobId: JOB_ID,
test\unit\import\import-parse-service.test.ts-33-      tenantId: TENANT,
test\unit\import\import-parse-service.test.ts-34-      targetEntityType: "TrackedSubject",
test\unit\import\import-parse-service.test.ts-35-      status: "UPLOADED",
--
test\unit\import\import-parse-service.test.ts-82-
test\unit\import\import-parse-service.test.ts-83-  it("skips a row whose externalId already exists from a PRIOR import (strong dedup)", async () => {
test\unit\import\import-parse-service.test.ts-84-    await store.putIfAbsent<ImportDedupRecord>({
test\unit\import\import-parse-service.test.ts-85-      ...importDedupKey(TENANT, "ext-1"),
test\unit\import\import-parse-service.test.ts:86:      entityType: "ImportDedupRecord",
test\unit\import\import-parse-service.test.ts-87-      tenantId: TENANT,
test\unit\import\import-parse-service.test.ts-88-      externalId: "ext-1",
test\unit\import\import-parse-service.test.ts-89-      subjectId: "subject-existing",
test\unit\import\import-parse-service.test.ts-90-      createdAt: NOW,
--
test\unit\import\import-parse-service.test.ts-99-  it("skips a row with no externalId whose normalized displayName+type already exists among ACTIVE subjects (weak fallback dedup)", async () => {
test\unit\import\import-parse-service.test.ts-100-    await subjectStore.putIfAbsent({
test\unit\import\import-parse-service.test.ts-101-      PK: `TENANT#${TENANT}#SUBJECT#existing-1`,
test\unit\import\import-parse-service.test.ts-102-      SK: "META",
test\unit\import\import-parse-service.test.ts:103:      entityType: "TrackedSubject",
test\unit\import\import-parse-service.test.ts-104-      subjectId: "existing-1",
test\unit\import\import-parse-service.test.ts-105-      tenantId: TENANT,
test\unit\import\import-parse-service.test.ts-106-      type: "VENDOR",
test\unit\import\import-parse-service.test.ts-107-      displayName: "ACME Ltda",
--
test\unit\reminder\backfill-reminder-policies.test.ts-50-describe("backfill-reminder-policies: processPage", () => {
test\unit\reminder\backfill-reminder-policies.test.ts-51-  it("creates the pointer and materializes for a pre-existing ITEM-scoped enabled policy", async () => {
test\unit\reminder\backfill-reminder-policies.test.ts-52-    const store = new InMemoryReminderStore();
test\unit\reminder\backfill-reminder-policies.test.ts-53-    const materializer = new ReminderMaterializer(store, TABLE, () => NOW);
test\unit\reminder\backfill-reminder-policies.test.ts:54:    await store.putIfAbsent({ ...itemKey(TENANT, "item1"), entityType: "ExpirationItem", itemId: "item1", tenantId: TENANT, status: "ACTIVE", dueDate: "2026-09-10T00:00:00.000Z", version: 1 });
test\unit\reminder\backfill-reminder-policies.test.ts-55-    // Simulates a policy saved BEFORE BLOCKER-B deployed: no pointer exists for it.
test\unit\reminder\backfill-reminder-policies.test.ts-56-    const policy = {
test\unit\reminder\backfill-reminder-policies.test.ts-57-      PK: "TENANT#t1#POLICY#p1",
test\unit\reminder\backfill-reminder-policies.test.ts-58-      SK: "META" as const,
test\unit\reminder\backfill-reminder-policies.test.ts:59:      entityType: "ReminderPolicy" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-60-      policyId: "p1",
test\unit\reminder\backfill-reminder-policies.test.ts-61-      tenantId: TENANT,
test\unit\reminder\backfill-reminder-policies.test.ts-62-      scope: "ITEM" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-63-      itemId: "item1",
--
test\unit\reminder\backfill-reminder-policies.test.ts-82-
test\unit\reminder\backfill-reminder-policies.test.ts-83-  it("is idempotent: running the same page twice does not duplicate the pointer or the occurrence", async () => {
test\unit\reminder\backfill-reminder-policies.test.ts-84-    const store = new InMemoryReminderStore();
test\unit\reminder\backfill-reminder-policies.test.ts-85-    const materializer = new ReminderMaterializer(store, TABLE, () => NOW);
test\unit\reminder\backfill-reminder-policies.test.ts:86:    await store.putIfAbsent({ ...itemKey(TENANT, "item1"), entityType: "ExpirationItem", itemId: "item1", tenantId: TENANT, status: "ACTIVE", dueDate: "2026-09-10T00:00:00.000Z", version: 1 });
test\unit\reminder\backfill-reminder-policies.test.ts-87-    const policy = {
test\unit\reminder\backfill-reminder-policies.test.ts-88-      PK: "TENANT#t1#POLICY#p1",
test\unit\reminder\backfill-reminder-policies.test.ts-89-      SK: "META" as const,
test\unit\reminder\backfill-reminder-policies.test.ts:90:      entityType: "ReminderPolicy" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-91-      policyId: "p1",
test\unit\reminder\backfill-reminder-policies.test.ts-92-      tenantId: TENANT,
test\unit\reminder\backfill-reminder-policies.test.ts-93-      scope: "ITEM" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-94-      itemId: "item1",
--
test\unit\reminder\backfill-reminder-policies.test.ts-121-    // No item seeded at all.
test\unit\reminder\backfill-reminder-policies.test.ts-122-    const policy = {
test\unit\reminder\backfill-reminder-policies.test.ts-123-      PK: "TENANT#t1#POLICY#p1",
test\unit\reminder\backfill-reminder-policies.test.ts-124-      SK: "META" as const,
test\unit\reminder\backfill-reminder-policies.test.ts:125:      entityType: "ReminderPolicy" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-126-      policyId: "p1",
test\unit\reminder\backfill-reminder-policies.test.ts-127-      tenantId: TENANT,
test\unit\reminder\backfill-reminder-policies.test.ts-128-      scope: "ITEM" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-129-      itemId: "ghost-item",
--
test\unit\reminder\backfill-reminder-policies.test.ts-146-
test\unit\reminder\backfill-reminder-policies.test.ts-147-  it("skips a disabled policy's materialization but still creates its pointer (discoverable for future re-enable)", async () => {
test\unit\reminder\backfill-reminder-policies.test.ts-148-    const store = new InMemoryReminderStore();
test\unit\reminder\backfill-reminder-policies.test.ts-149-    const materializer = new ReminderMaterializer(store, TABLE, () => NOW);
test\unit\reminder\backfill-reminder-policies.test.ts:150:    await store.putIfAbsent({ ...itemKey(TENANT, "item1"), entityType: "ExpirationItem", itemId: "item1", tenantId: TENANT, status: "ACTIVE", dueDate: "2026-09-10T00:00:00.000Z", version: 1 });
test\unit\reminder\backfill-reminder-policies.test.ts-151-    const policy = {
test\unit\reminder\backfill-reminder-policies.test.ts-152-      PK: "TENANT#t1#POLICY#p1",
test\unit\reminder\backfill-reminder-policies.test.ts-153-      SK: "META" as const,
test\unit\reminder\backfill-reminder-policies.test.ts:154:      entityType: "ReminderPolicy" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-155-      policyId: "p1",
test\unit\reminder\backfill-reminder-policies.test.ts-156-      tenantId: TENANT,
test\unit\reminder\backfill-reminder-policies.test.ts-157-      scope: "ITEM" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-158-      itemId: "item1",
--
test\unit\reminder\backfill-reminder-policies.test.ts-177-    const materializer = new ReminderMaterializer(store, TABLE, () => NOW);
test\unit\reminder\backfill-reminder-policies.test.ts-178-    const policy = {
test\unit\reminder\backfill-reminder-policies.test.ts-179-      PK: "TENANT#t1#POLICY#p1",
test\unit\reminder\backfill-reminder-policies.test.ts-180-      SK: "META" as const,
test\unit\reminder\backfill-reminder-policies.test.ts:181:      entityType: "ReminderPolicy" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-182-      policyId: "p1",
test\unit\reminder\backfill-reminder-policies.test.ts-183-      tenantId: TENANT,
test\unit\reminder\backfill-reminder-policies.test.ts-184-      scope: "TEMPLATE" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-185-      name: "r",
--
test\unit\reminder\backfill-reminder-policies.test.ts-200-
test\unit\reminder\backfill-reminder-policies.test.ts-201-  it("dry-run mode reports what it found but writes nothing", async () => {
test\unit\reminder\backfill-reminder-policies.test.ts-202-    const store = new InMemoryReminderStore();
test\unit\reminder\backfill-reminder-policies.test.ts-203-    const materializer = new ReminderMaterializer(store, TABLE, () => NOW);
test\unit\reminder\backfill-reminder-policies.test.ts:204:    await store.putIfAbsent({ ...itemKey(TENANT, "item1"), entityType: "ExpirationItem", itemId: "item1", tenantId: TENANT, status: "ACTIVE", dueDate: "2026-09-10T00:00:00.000Z", version: 1 });
test\unit\reminder\backfill-reminder-policies.test.ts-205-    const policy = {
test\unit\reminder\backfill-reminder-policies.test.ts-206-      PK: "TENANT#t1#POLICY#p1",
test\unit\reminder\backfill-reminder-policies.test.ts-207-      SK: "META" as const,
test\unit\reminder\backfill-reminder-policies.test.ts:208:      entityType: "ReminderPolicy" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-209-      policyId: "p1",
test\unit\reminder\backfill-reminder-policies.test.ts-210-      tenantId: TENANT,
test\unit\reminder\backfill-reminder-policies.test.ts-211-      scope: "ITEM" as const,
test\unit\reminder\backfill-reminder-policies.test.ts-212-      itemId: "item1",
--
test\unit\reminder\dispatch.test.ts-61-  store: InMemoryReminderStore,
test\unit\reminder\dispatch.test.ts-62-): Promise<{ command: DispatchCommand; dispatchDeps: DispatchDeps; policyPk: string }> {
test\unit\reminder\dispatch.test.ts-63-  await store.putIfAbsent({
test\unit\reminder\dispatch.test.ts-64-    ...itemKey(TENANT, ITEM_ID),
test\unit\reminder\dispatch.test.ts:65:    entityType: "ExpirationItem",
test\unit\reminder\dispatch.test.ts-66-    itemId: ITEM_ID,
test\unit\reminder\dispatch.test.ts-67-    tenantId: TENANT,
test\unit\reminder\dispatch.test.ts-68-    status: "ACTIVE",
test\unit\reminder\dispatch.test.ts-69-    dueDate: "2026-09-10T00:00:00.000Z",
--
test\unit\reminder\dispatch.test.ts-155-    const outcome = await dispatchOccurrence(setup.dispatchDeps, setup.command);
test\unit\reminder\dispatch.test.ts-156-
test\unit\reminder\dispatch.test.ts-157-    expect(outcome.kind).toBe("ABORTED_FRESHNESS_RACE");
test\unit\reminder\dispatch.test.ts-158-    const allRows = store.allItems();
test\unit\reminder\dispatch.test.ts:159:    expect(allRows.some((r) => r["entityType"] === "NotificationIntent")).toBe(false);
test\unit\reminder\dispatch.test.ts:160:    const occurrenceRow = allRows.find((r) => r["entityType"] === "ReminderOccurrence");
test\unit\reminder\dispatch.test.ts-161-    expect(occurrenceRow?.["status"]).toBe("CLAIMED"); // untouched - transaction rolled back
test\unit\reminder\dispatch.test.ts-162-  });
test\unit\reminder\dispatch.test.ts-163-
test\unit\reminder\dispatch.test.ts-164-  it("aborts (ABORTED_FRESHNESS_RACE) and creates no NotificationIntent when the item is archived in the window between dispatch's read and its commit", async () => {
--
test\unit\reminder\dispatch.test.ts-172-    const outcome = await dispatchOccurrence(dispatchDeps, command);
test\unit\reminder\dispatch.test.ts-173-
test\unit\reminder\dispatch.test.ts-174-    expect(outcome.kind).toBe("ABORTED_FRESHNESS_RACE");
test\unit\reminder\dispatch.test.ts-175-    const allRows = store.allItems();
test\unit\reminder\dispatch.test.ts:176:    expect(allRows.some((r) => r["entityType"] === "NotificationIntent")).toBe(false);
test\unit\reminder\dispatch.test.ts-177-  });
test\unit\reminder\dispatch.test.ts-178-
test\unit\reminder\dispatch.test.ts-179-  it("still succeeds (TRIGGERED) when nothing races - the fence never fires a false positive on the happy path", async () => {
test\unit\reminder\dispatch.test.ts-180-    const store = new InMemoryReminderStore();
--
test\unit\reminder\dispatch.test.ts-183-    const outcome = await dispatchOccurrence(dispatchDeps, command);
test\unit\reminder\dispatch.test.ts-184-
test\unit\reminder\dispatch.test.ts-185-    expect(outcome.kind).toBe("TRIGGERED");
test\unit\reminder\dispatch.test.ts-186-    const allRows = store.allItems();
test\unit\reminder\dispatch.test.ts:187:    expect(allRows.some((r) => r["entityType"] === "NotificationIntent")).toBe(true);
test\unit\reminder\dispatch.test.ts-188-  });
test\unit\reminder\dispatch.test.ts-189-});
--
src\workers\document-chasing-dispatch\dispatch.ts-136-  if (tier === "EXPIRED") {
src\workers\document-chasing-dispatch\dispatch.ts-137-    const recipient: DocumentChasingRecipientRef = { kind: "INTERNAL_USER", userId: request.requestedByUserId };
src\workers\document-chasing-dispatch\dispatch.ts-138-    const intent: DocumentChasingIntent = {
src\workers\document-chasing-dispatch\dispatch.ts-139-      ...documentChasingIntentKey(tenantId, subjectId, assignmentId, documentRequestId, intentId),
src\workers\document-chasing-dispatch\dispatch.ts:140:      entityType: "DocumentChasingIntent",
src\workers\document-chasing-dispatch\dispatch.ts-141-      intentId,
src\workers\document-chasing-dispatch\dispatch.ts-142-      tenantId,
src\workers\document-chasing-dispatch\dispatch.ts-143-      subjectId,
src\workers\document-chasing-dispatch\dispatch.ts-144-      assignmentId,
--
src\workers\document-chasing-dispatch\dispatch.ts-202-  const issued = issueGuestToken(deps.guestTokenPepper);
src\workers\document-chasing-dispatch\dispatch.ts-203-  const newTokenVersion = request.tokenVersion + 1;
src\workers\document-chasing-dispatch\dispatch.ts-204-  const pointer: GuestTokenPointer = {
src\workers\document-chasing-dispatch\dispatch.ts-205-    ...guestTokenPointerKey(issued.selectorHash),
src\workers\document-chasing-dispatch\dispatch.ts:206:    entityType: "GuestTokenPointer",
src\workers\document-chasing-dispatch\dispatch.ts-207-    selectorHash: issued.selectorHash,
src\workers\document-chasing-dispatch\dispatch.ts-208-    secretHash: issued.secretHash,
src\workers\document-chasing-dispatch\dispatch.ts-209-    tenantId,
src\workers\document-chasing-dispatch\dispatch.ts-210-    subjectId,
--
src\workers\document-chasing-dispatch\dispatch.ts-219-  };
src\workers\document-chasing-dispatch\dispatch.ts-220-  const recipient: DocumentChasingRecipientRef = { kind: "EXTERNAL_EMAIL_SNAPSHOT", email: request.recipientEmail };
src\workers\document-chasing-dispatch\dispatch.ts-221-  const intent: DocumentChasingIntent = {
src\workers\document-chasing-dispatch\dispatch.ts-222-    ...documentChasingIntentKey(tenantId, subjectId, assignmentId, documentRequestId, intentId),
src\workers\document-chasing-dispatch\dispatch.ts:223:    entityType: "DocumentChasingIntent",
src\workers\document-chasing-dispatch\dispatch.ts-224-    intentId,
src\workers\document-chasing-dispatch\dispatch.ts-225-    tenantId,
src\workers\document-chasing-dispatch\dispatch.ts-226-    subjectId,
src\workers\document-chasing-dispatch\dispatch.ts-227-    assignmentId,
--
test\unit\subject\document-request-initial-invite.test.ts-132-    const service = await makeService();
test\unit\subject\document-request-initial-invite.test.ts-133-    for (let i = 0; i < 3; i++) {
test\unit\subject\document-request-initial-invite.test.ts-134-      await requirements.assignRequirement(ctx(), subjectId, { requirementName: `Req ${i}` });
test\unit\subject\document-request-initial-invite.test.ts-135-    }
test\unit\subject\document-request-initial-invite.test.ts:136:    const assignments = await store.queryByPk<{ PK: string; SK: string; assignmentId: string; entityType: string }>(`TENANT#${TENANT}#SUBJECT#${subjectId}`, "REQASSIGN#");
test\unit\subject\document-request-initial-invite.test.ts-137-    // Excludes the original `assignmentId` from `beforeEach` - it must stay untouched so the
test\unit\subject\document-request-initial-invite.test.ts-138-    // final assertion below can prove no request was created under it.
test\unit\subject\document-request-initial-invite.test.ts:139:    const freshAssignments = assignments.filter((a) => a.entityType === "RequirementAssignment" && a.assignmentId !== assignmentId);
test\unit\subject\document-request-initial-invite.test.ts-140-    expect(freshAssignments).toHaveLength(3);
test\unit\subject\document-request-initial-invite.test.ts-141-
test\unit\subject\document-request-initial-invite.test.ts-142-    // 3 successful EMAIL requests to the SAME recipient (per-recipient limit).
test\unit\subject\document-request-initial-invite.test.ts-143-    for (let i = 0; i < 3; i++) {
--
test\unit\subject\document-request-initial-invite.test.ts-153-    // Exact match on `assignmentId` (not just the SK prefix) - the fake ID generator's shared
test\unit\subject\document-request-initial-invite.test.ts-154-    // module-level counter can make one assignmentId a literal string-prefix of another
test\unit\subject\document-request-initial-invite.test.ts-155-    // (e.g. "assignment-1" vs "assignment-10"), which would make prefix-only matching a false
test\unit\subject\document-request-initial-invite.test.ts-156-    // positive here.
test\unit\subject\document-request-initial-invite.test.ts:157:    const requests = await store.queryByPk<{ PK: string; SK: string; entityType: string; assignmentId: string }>(`TENANT#${TENANT}#SUBJECT#${subjectId}`, "REQASSIGN#");
test\unit\subject\document-request-initial-invite.test.ts:158:    expect(requests.filter((r) => r.entityType === "DocumentRequest" && r.assignmentId === assignmentId)).toHaveLength(0);
test\unit\subject\document-request-initial-invite.test.ts-159-  });
test\unit\subject\document-request-initial-invite.test.ts-160-
test\unit\subject\document-request-initial-invite.test.ts-161-  it("getDocumentRequestDeliveryPreference/setDocumentRequestDeliveryPreference require ADMIN_ROLES (tenant:configure-document-request-delivery)", async () => {
test\unit\subject\document-request-initial-invite.test.ts-162-    const service = await makeService();
--
src\modules\subject\domain\document-submission.ts-16-
src\modules\subject\domain\document-submission.ts-17-export type { DocumentStatus, UploadEvidence, MalwareEvidence, DocumentObjectReference };
src\modules\subject\domain\document-submission.ts-18-
src\modules\subject\domain\document-submission.ts-19-export interface DocumentSubmission extends EntityKey {
src\modules\subject\domain\document-submission.ts:20:  entityType: "DocumentSubmission";
src\modules\subject\domain\document-submission.ts-21-  submissionId: string;
src\modules\subject\domain\document-submission.ts-22-  tenantId: string;
src\modules\subject\domain\document-submission.ts-23-  subjectId: string;
src\modules\subject\domain\document-submission.ts-24-  assignmentId: string;
--
src\modules\subject\domain\document-submission.ts-28-  contentLength: number;
src\modules\subject\domain\document-submission.ts-29-  checksumSha256: string;
src\modules\subject\domain\document-submission.ts-30-  status: DocumentStatus;
src\modules\subject\domain\document-submission.ts-31-  quarantineObject: DocumentObjectReference;
src\modules\subject\domain\document-submission.ts:32:  cleanObject?: DocumentObjectReference;
src\modules\subject\domain\document-submission.ts:33:  uploadEvidence?: UploadEvidence;
src\modules\subject\domain\document-submission.ts:34:  malwareEvidence?: MalwareEvidence;
src\modules\subject\domain\document-submission.ts-35-  createdAt: string;
src\modules\subject\domain\document-submission.ts-36-  updatedAt: string;
src\modules\subject\domain\document-submission.ts-37-  deletedAt?: string;
src\modules\subject\domain\document-submission.ts-38-  version: number;
--
test\unit\reminder\producer.test.ts-66-    clock = { current: "2026-08-01T00:00:00.000Z" };
test\unit\reminder\producer.test.ts-67-
test\unit\reminder\producer.test.ts-68-    await store.putIfAbsent({
test\unit\reminder\producer.test.ts-69-      ...itemKey(TENANT, ITEM_ID),
test\unit\reminder\producer.test.ts:70:      entityType: "ExpirationItem",
test\unit\reminder\producer.test.ts-71-      itemId: ITEM_ID,
test\unit\reminder\producer.test.ts-72-      tenantId: TENANT,
test\unit\reminder\producer.test.ts-73-      status: "ACTIVE",
test\unit\reminder\producer.test.ts-74-      dueDate: "2026-09-10T00:00:00.000Z",
--
test\unit\reminder\producer-chasing-branch.test.ts-1-/**
test\unit\reminder\producer-chasing-branch.test.ts:2: * M10 cluster 4 (D-039/D-046/D-048): dedicated coverage for producer.ts's new entityType
test\unit\reminder\producer-chasing-branch.test.ts-3- * discriminator branch - the highest-risk touch of this session, since GSI3 is now a SHARED
test\unit\reminder\producer-chasing-branch.test.ts-4- * index. Proves: (1) a DocumentChasingOccurrence claims correctly through the exact same
test\unit\reminder\producer-chasing-branch.test.ts-5- * scan/claim/outbox mechanics as reminders; (2) a MIXED tick (one reminder + one chasing
test\unit\reminder\producer-chasing-branch.test.ts-6- * occurrence in the SAME GSI3 partition/minute, exactly what a real shared-index scan returns)
--
test\unit\reminder\producer-chasing-branch.test.ts-48-  // its scheduledAt equals tokenExpiresAt exactly, easiest to target with a tick.
test\unit\reminder\producer-chasing-branch.test.ts-49-  return result.created.find((o) => o.tier === "EXPIRED")!;
test\unit\reminder\producer-chasing-branch.test.ts-50-}
test\unit\reminder\producer-chasing-branch.test.ts-51-
test\unit\reminder\producer-chasing-branch.test.ts:52:describe("producer.ts - M10 cluster 4 entityType discriminator branch", () => {
test\unit\reminder\producer-chasing-branch.test.ts-53-  it("claims a DocumentChasingOccurrence through the same GSI3 scan mechanics as reminders", async () => {
test\unit\reminder\producer-chasing-branch.test.ts-54-    const store = new InMemoryReminderStore();
test\unit\reminder\producer-chasing-branch.test.ts-55-    const clock = { current: "2026-08-20T00:00:00.000Z" };
test\unit\reminder\producer-chasing-branch.test.ts-56-    const now = () => clock.current;
--
test\unit\reminder\producer-chasing-branch.test.ts-78-    expect(row?.GSI6PK).toBe("WORKSTATE#CLAIMED");
test\unit\reminder\producer-chasing-branch.test.ts-79-
test\unit\reminder\producer-chasing-branch.test.ts-80-    // The outbox event was written with the chasing-specific destination, in the SAME
test\unit\reminder\producer-chasing-branch.test.ts-81-    // transaction as the claim - never a separate, non-atomic write.
test\unit\reminder\producer-chasing-branch.test.ts:82:    const outboxRecords = store.allItems().filter((i) => i["entityType"] === "OutboxEvent");
test\unit\reminder\producer-chasing-branch.test.ts-83-    expect(outboxRecords).toHaveLength(1);
test\unit\reminder\producer-chasing-branch.test.ts-84-    expect(outboxRecords[0]?.["destination"]).toBe("SQS_DOCUMENT_CHASING_DISPATCH_V1");
test\unit\reminder\producer-chasing-branch.test.ts-85-  });
test\unit\reminder\producer-chasing-branch.test.ts-86-
--
test\unit\reminder\producer-chasing-branch.test.ts-90-    const now = () => clock.current;
test\unit\reminder\producer-chasing-branch.test.ts-91-
test\unit\reminder\producer-chasing-branch.test.ts-92-    await store.putIfAbsent({
test\unit\reminder\producer-chasing-branch.test.ts-93-      ...itemKey(TENANT, "item1"),
test\unit\reminder\producer-chasing-branch.test.ts:94:      entityType: "ExpirationItem",
test\unit\reminder\producer-chasing-branch.test.ts-95-      itemId: "item1",
test\unit\reminder\producer-chasing-branch.test.ts-96-      tenantId: TENANT,
test\unit\reminder\producer-chasing-branch.test.ts-97-      status: "ACTIVE",
test\unit\reminder\producer-chasing-branch.test.ts-98-      dueDate: "2026-08-23T12:00:00.000Z",
--
test\unit\reminder\producer-chasing-branch.test.ts-137-  it("fails closed on an unrecognized GSI3SK shape - never processed, never crashes the tick, counted for alarming", async () => {
test\unit\reminder\producer-chasing-branch.test.ts-138-    const store = new InMemoryReminderStore();
test\unit\reminder\producer-chasing-branch.test.ts-139-    const now = () => "2026-08-23T12:00:30.000Z";
test\unit\reminder\producer-chasing-branch.test.ts-140-
test\unit\reminder\producer-chasing-branch.test.ts:141:    // Simulates a future/foreign entityType writing to the shared GSI3 with neither the
test\unit\reminder\producer-chasing-branch.test.ts-142-    // reminder nor the chasing SK shape - a real bug (or a third entity type added later
test\unit\reminder\producer-chasing-branch.test.ts-143-    // without updating the discriminator) must never be silently processed or dropped.
test\unit\reminder\producer-chasing-branch.test.ts-144-    await store.putIfAbsent({
test\unit\reminder\producer-chasing-branch.test.ts-145-      PK: "TENANT#t1#SOMETHING#weird",
test\unit\reminder\producer-chasing-branch.test.ts-146-      SK: "META",
test\unit\reminder\producer-chasing-branch.test.ts:147:      entityType: "SomeFutureThing",
test\unit\reminder\producer-chasing-branch.test.ts-148-      GSI3PK: "DUE#202608231200#00",
test\unit\reminder\producer-chasing-branch.test.ts-149-      GSI3SK: "TENANT#t1#UNKNOWNTYPE#foo",
test\unit\reminder\producer-chasing-branch.test.ts-150-    });
test\unit\reminder\producer-chasing-branch.test.ts-151-
--
src\runtime\aws\handlers\notification-router-handler.ts-19-const logger = new SecureLogger({ baseContext: { service: "notification-router" } });
src\runtime\aws\handlers\notification-router-handler.ts-20-
src\runtime\aws\handlers\notification-router-handler.ts-21-async function processRecord(
src\runtime\aws\handlers\notification-router-handler.ts-22-  record: import("aws-lambda").DynamoDBRecord,
src\runtime\aws\handlers\notification-router-handler.ts:23:  item: Record<string, unknown> & { entityType?: string; tenantId?: string },
src\runtime\aws\handlers\notification-router-handler.ts-24-  batchItemFailures: { itemIdentifier: string }[],
src\runtime\aws\handlers\notification-router-handler.ts-25-): Promise<void> {
src\runtime\aws\handlers\notification-router-handler.ts-26-  // m5-observability-design.md #2: NotificationIntent doesn't carry a correlationId
src\runtime\aws\handlers\notification-router-handler.ts-27-  // field - fall back to the Streams record's own SequenceNumber, same fallback the
--
src\runtime\aws\handlers\notification-router-handler.ts-51-    const image = record.dynamodb?.NewImage;
src\runtime\aws\handlers\notification-router-handler.ts-52-    if (!image) continue;
src\runtime\aws\handlers\notification-router-handler.ts-53-
src\runtime\aws\handlers\notification-router-handler.ts-54-    try {
src\runtime\aws\handlers\notification-router-handler.ts:55:      const item = unmarshall(image as Record<string, never>) as Record<string, unknown> & { entityType?: string; tenantId?: string };
src\runtime\aws\handlers\notification-router-handler.ts:56:      if (item["entityType"] !== "NotificationIntent") continue;
src\runtime\aws\handlers\notification-router-handler.ts-57-      await processRecord(record, item, batchItemFailures);
src\runtime\aws\handlers\notification-router-handler.ts-58-    } catch (err) {
src\runtime\aws\handlers\notification-router-handler.ts-59-      logger.error("notification-router failed to parse Streams image", { eventID: record.eventID, error: err instanceof Error ? err.message : String(err) });
src\runtime\aws\handlers\notification-router-handler.ts-60-      batchItemFailures.push({ itemIdentifier: record.eventID ?? "" });
--
src\modules\subject\domain\document-request.ts-9-
src\modules\subject\domain\document-request.ts-10-export type DocumentRequestStatus = "REQUESTED" | "OPENED" | "SUBMITTED" | "COMPLETED" | "CANCELLED" | "EXPIRED" | "REVOKED";
src\modules\subject\domain\document-request.ts-11-
src\modules\subject\domain\document-request.ts-12-export interface DocumentRequest extends EntityKey {
src\modules\subject\domain\document-request.ts:13:  entityType: "DocumentRequest";
src\modules\subject\domain\document-request.ts-14-  documentRequestId: string;
src\modules\subject\domain\document-request.ts-15-  tenantId: string;
src\modules\subject\domain\document-request.ts-16-  subjectId: string;
src\modules\subject\domain\document-request.ts-17-  assignmentId: string;
--
test\unit\notification\email-delivery-workflow.test.ts-15-
test\unit\notification\email-delivery-workflow.test.ts-16-function makeItem(overrides: Partial<ExpirationItem> = {}): ExpirationItem {
test\unit\notification\email-delivery-workflow.test.ts-17-  return {
test\unit\notification\email-delivery-workflow.test.ts-18-    ...itemKey(TENANT, ITEM_ID),
test\unit\notification\email-delivery-workflow.test.ts:19:    entityType: "ExpirationItem",
test\unit\notification\email-delivery-workflow.test.ts-20-    itemId: ITEM_ID,
test\unit\notification\email-delivery-workflow.test.ts-21-    tenantId: TENANT,
test\unit\notification\email-delivery-workflow.test.ts-22-    name: "Passport",
test\unit\notification\email-delivery-workflow.test.ts-23-    category: "document",
--
test\unit\notification\email-delivery-workflow.test.ts-37-function makeIntent(overrides: Partial<NotificationIntent> = {}): NotificationIntent {
test\unit\notification\email-delivery-workflow.test.ts-38-  return {
test\unit\notification\email-delivery-workflow.test.ts-39-    PK: `TENANT#${TENANT}#INTENT#${INTENT_ID}`,
test\unit\notification\email-delivery-workflow.test.ts-40-    SK: "META",
test\unit\notification\email-delivery-workflow.test.ts:41:    entityType: "NotificationIntent",
test\unit\notification\email-delivery-workflow.test.ts-42-    intentId: INTENT_ID,
test\unit\notification\email-delivery-workflow.test.ts-43-    tenantId: TENANT,
test\unit\notification\email-delivery-workflow.test.ts-44-    kind: "EXPIRATION_REMINDER",
test\unit\notification\email-delivery-workflow.test.ts-45-    itemId: ITEM_ID,
--
test\unit\notification\email-delivery-workflow.test.ts-62-
test\unit\notification\email-delivery-workflow.test.ts-63-function makeAttempt(overrides: Partial<NotificationAttempt> = {}): NotificationAttempt {
test\unit\notification\email-delivery-workflow.test.ts-64-  return {
test\unit\notification\email-delivery-workflow.test.ts-65-    ...notificationAttemptKey(TENANT, INTENT_ID, 1, ATTEMPT_ID),
test\unit\notification\email-delivery-workflow.test.ts:66:    entityType: "NotificationAttempt",
test\unit\notification\email-delivery-workflow.test.ts-67-    tenantId: TENANT,
test\unit\notification\email-delivery-workflow.test.ts-68-    intentId: INTENT_ID,
test\unit\notification\email-delivery-workflow.test.ts-69-    attemptId: ATTEMPT_ID,
test\unit\notification\email-delivery-workflow.test.ts-70-    attemptNumber: 1,
--
test\unit\notification\email-delivery-workflow.test.ts-191-    const attempt = await store.get<NotificationAttempt>(notificationAttemptKey(TENANT, INTENT_ID, 1, ATTEMPT_ID));
test\unit\notification\email-delivery-workflow.test.ts-192-    expect(attempt?.status).toBe("NOT_SENT_STALE");
test\unit\notification\email-delivery-workflow.test.ts-193-
test\unit\notification\email-delivery-workflow.test.ts-194-    const all = store.allItems();
test\unit\notification\email-delivery-workflow.test.ts:195:    const newIntent = all.find((i) => i["entityType"] === "NotificationIntent" && i["intentId"] === "newintent-1");
test\unit\notification\email-delivery-workflow.test.ts-196-    expect(newIntent).toBeDefined();
test\unit\notification\email-delivery-workflow.test.ts-197-    expect((newIntent as unknown as NotificationIntent).kind).toBe("REPLACEMENT");
test\unit\notification\email-delivery-workflow.test.ts-198-    expect((newIntent as unknown as NotificationIntent).itemVersion).toBe(4);
test\unit\notification\email-delivery-workflow.test.ts-199-  });
--
test\unit\reminder\reconciliation.test.ts-47-    policies = new ReminderPolicyService({ store, tableName: TABLE, ids: makeReminderIdGenerator(), now });
test\unit\reminder\reconciliation.test.ts-48-
test\unit\reminder\reconciliation.test.ts-49-    await store.putIfAbsent({
test\unit\reminder\reconciliation.test.ts-50-      ...itemKey(TENANT, ITEM_ID),
test\unit\reminder\reconciliation.test.ts:51:      entityType: "ExpirationItem",
test\unit\reminder\reconciliation.test.ts-52-      itemId: ITEM_ID,
test\unit\reminder\reconciliation.test.ts-53-      tenantId: TENANT,
test\unit\reminder\reconciliation.test.ts-54-      status: "ACTIVE",
test\unit\reminder\reconciliation.test.ts-55-      dueDate: "2026-09-10T00:00:00.000Z",
--
test\unit\reminder\reconciliation.test.ts-61-    it("does not touch a CLAIMED occurrence whose claimExpiresAt has not passed yet", async () => {
test\unit\reminder\reconciliation.test.ts-62-      const occurrence: ReminderOccurrence = {
test\unit\reminder\reconciliation.test.ts-63-        PK: `TENANT#${TENANT}#ITEM#${ITEM_ID}`,
test\unit\reminder\reconciliation.test.ts-64-        SK: "OCC#2026-09-10T08:00:00.000Z#occ1",
test\unit\reminder\reconciliation.test.ts:65:        entityType: "ReminderOccurrence",
test\unit\reminder\reconciliation.test.ts-66-        occurrenceId: "occ1",
test\unit\reminder\reconciliation.test.ts-67-        tenantId: TENANT,
test\unit\reminder\reconciliation.test.ts-68-        itemId: ITEM_ID,
test\unit\reminder\reconciliation.test.ts-69-        policyId: "policy1",
--
test\unit\reminder\reconciliation.test.ts-96-    it("ignores an occurrence that is not CLAIMED (e.g. already TRIGGERED)", async () => {
test\unit\reminder\reconciliation.test.ts-97-      const occurrence: ReminderOccurrence = {
test\unit\reminder\reconciliation.test.ts-98-        PK: `TENANT#${TENANT}#ITEM#${ITEM_ID}`,
test\unit\reminder\reconciliation.test.ts-99-        SK: "OCC#2026-09-10T08:00:00.000Z#occ2",
test\unit\reminder\reconciliation.test.ts:100:        entityType: "ReminderOccurrence",
test\unit\reminder\reconciliation.test.ts-101-        occurrenceId: "occ2",
test\unit\reminder\reconciliation.test.ts-102-        tenantId: TENANT,
test\unit\reminder\reconciliation.test.ts-103-        itemId: ITEM_ID,
test\unit\reminder\reconciliation.test.ts-104-        policyId: "policy1",
--
test\unit\reminder\reconciliation.test.ts-151-      const staleOccurrenceId = "occ_stale_legacy";
test\unit\reminder\reconciliation.test.ts-152-      const gsi3 = gsi3Keys({ tenantId: TENANT, occurrenceId: staleOccurrenceId, scheduledAt: staleScheduledAt, shardCount: defaultShardConfig().current.shardCount });
test\unit\reminder\reconciliation.test.ts-153-      const stale: ReminderOccurrence = {
test\unit\reminder\reconciliation.test.ts-154-        ...occurrenceKey(TENANT, ITEM_ID, staleScheduledAt, staleOccurrenceId),
test\unit\reminder\reconciliation.test.ts:155:        entityType: "ReminderOccurrence",
test\unit\reminder\reconciliation.test.ts-156-        occurrenceId: staleOccurrenceId,
test\unit\reminder\reconciliation.test.ts-157-        tenantId: TENANT,
test\unit\reminder\reconciliation.test.ts-158-        itemId: ITEM_ID,
test\unit\reminder\reconciliation.test.ts-159-        policyId: policy.policyId,
--
test\unit\reminder\reminder-materialization-trigger.test.ts-161-
test\unit\reminder\reminder-materialization-trigger.test.ts-162-    it("skips (does not crash on) an orphaned pointer whose policy no longer exists/matches", async () => {
test\unit\reminder\reminder-materialization-trigger.test.ts-163-      const item = await expirationService.createItem(ctx, { name: "a", category: "b", dueDate: "2026-09-10T00:00:00.000Z" });
test\unit\reminder\reminder-materialization-trigger.test.ts-164-      // Write an orphaned pointer directly - no real policy behind it.
test\unit\reminder\reminder-materialization-trigger.test.ts:165:      await store.putIfAbsent({ PK: itemKey(TENANT, item.itemId).PK, SK: "POLICYREF#ghost-policy", entityType: "ReminderPolicyRef", policyId: "ghost-policy" });
test\unit\reminder\reminder-materialization-trigger.test.ts-166-
test\unit\reminder\reminder-materialization-trigger.test.ts-167-      const result = await handleTriggerEvent(deps, { kind: "ITEM_DUE_DATE_CHANGED", tenantId: TENANT, itemId: item.itemId });
test\unit\reminder\reminder-materialization-trigger.test.ts-168-
test\unit\reminder\reminder-materialization-trigger.test.ts-169-      expect(result.skippedOrphanedPointers).toBe(1);
--
test\unit\reminder\reminder-materialization-trigger.test.ts-233-      await policyService.createPolicy(ctx, { scope: "ITEM", itemId: other.itemId, rule: RULE });
test\unit\reminder\reminder-materialization-trigger.test.ts-234-      // An orphaned pointer under `source`'s own partition (never a real production shape,
test\unit\reminder\reminder-materialization-trigger.test.ts-235-      // same defensive scenario "orphaned pointers" above exercises for the due-date-changed
test\unit\reminder\reminder-materialization-trigger.test.ts-236-      // path) - must be skipped by the copy, not dereferenced blindly.
test\unit\reminder\reminder-materialization-trigger.test.ts:237:      await store.putIfAbsent({ PK: itemKey(TENANT, source.itemId).PK, SK: "POLICYREF#ghost-policy", entityType: "ReminderPolicyRef", policyId: "ghost-policy" });
test\unit\reminder\reminder-materialization-trigger.test.ts-238-
test\unit\reminder\reminder-materialization-trigger.test.ts-239-      const renewed = await expirationService.renewItem(ctx, source.itemId, { newDueDate: "2027-09-10T00:00:00.000Z" }, source.version);
test\unit\reminder\reminder-materialization-trigger.test.ts-240-
test\unit\reminder\reminder-materialization-trigger.test.ts-241-      expect(renewed.copiedReminderPolicyIds).toEqual([]);
--
test\unit\notification\ses-callback-workflow.test.ts-13-
test\unit\notification\ses-callback-workflow.test.ts-14-function makeAttempt(overrides: Partial<NotificationAttempt> = {}): NotificationAttempt {
test\unit\notification\ses-callback-workflow.test.ts-15-  return {
test\unit\notification\ses-callback-workflow.test.ts-16-    ...notificationAttemptKey(TENANT, INTENT_ID, 1, ATTEMPT_ID),
test\unit\notification\ses-callback-workflow.test.ts:17:    entityType: "NotificationAttempt",
test\unit\notification\ses-callback-workflow.test.ts-18-    tenantId: TENANT,
test\unit\notification\ses-callback-workflow.test.ts-19-    intentId: INTENT_ID,
test\unit\notification\ses-callback-workflow.test.ts-20-    attemptId: ATTEMPT_ID,
test\unit\notification\ses-callback-workflow.test.ts-21-    attemptNumber: 1,
--
test\unit\notification\ses-callback-workflow.test.ts-39-function makeIntent(overrides: Partial<NotificationIntent> = {}): NotificationIntent {
test\unit\notification\ses-callback-workflow.test.ts-40-  return {
test\unit\notification\ses-callback-workflow.test.ts-41-    PK: `TENANT#${TENANT}#INTENT#${INTENT_ID}`,
test\unit\notification\ses-callback-workflow.test.ts-42-    SK: "META",
test\unit\notification\ses-callback-workflow.test.ts:43:    entityType: "NotificationIntent",
test\unit\notification\ses-callback-workflow.test.ts-44-    intentId: INTENT_ID,
test\unit\notification\ses-callback-workflow.test.ts-45-    tenantId: TENANT,
test\unit\notification\ses-callback-workflow.test.ts-46-    kind: "EXPIRATION_REMINDER",
test\unit\notification\ses-callback-workflow.test.ts-47-    itemId: "item1",
--
test\unit\notification\ses-callback-workflow.test.ts-128-    await seed(makeAttempt({ status: "DELIVERED" }));
test\unit\notification\ses-callback-workflow.test.ts-129-    await store.putIfAbsent(makeIntent());
test\unit\notification\ses-callback-workflow.test.ts-130-    const preferences: NotificationPreferences = {
test\unit\notification\ses-callback-workflow.test.ts-131-      ...notificationPreferencesKey(TENANT, "user1"),
test\unit\notification\ses-callback-workflow.test.ts:132:      entityType: "NotificationPreferences",
test\unit\notification\ses-callback-workflow.test.ts-133-      tenantId: TENANT,
test\unit\notification\ses-callback-workflow.test.ts-134-      userId: "user1",
test\unit\notification\ses-callback-workflow.test.ts-135-      emailEnabled: true,
test\unit\notification\ses-callback-workflow.test.ts-136-      locale: "pt-BR",
--
test\unit\reminder\reminder-policy-service.test.ts-28-
test\unit\reminder\reminder-policy-service.test.ts-29-async function seedActiveItem(store: InMemoryReminderStore, itemId: string, tenantId = TENANT): Promise<void> {
test\unit\reminder\reminder-policy-service.test.ts-30-  await store.putIfAbsent({
test\unit\reminder\reminder-policy-service.test.ts-31-    ...itemKey(tenantId, itemId),
test\unit\reminder\reminder-policy-service.test.ts:32:    entityType: "ExpirationItem",
test\unit\reminder\reminder-policy-service.test.ts-33-    itemId,
test\unit\reminder\reminder-policy-service.test.ts-34-    tenantId,
test\unit\reminder\reminder-policy-service.test.ts-35-    status: "ACTIVE",
test\unit\reminder\reminder-policy-service.test.ts-36-    dueDate: "2026-09-10T00:00:00.000Z",
--
test\unit\reminder\reminder-policy-service.test.ts-38-  });
test\unit\reminder\reminder-policy-service.test.ts-39-}
test\unit\reminder\reminder-policy-service.test.ts-40-
test\unit\reminder\reminder-policy-service.test.ts-41-function outboxEvents(store: InMemoryReminderStore) {
test\unit\reminder\reminder-policy-service.test.ts:42:  return store.allItems().filter((i) => i["entityType"] === "OutboxEvent");
test\unit\reminder\reminder-policy-service.test.ts-43-}
test\unit\reminder\reminder-policy-service.test.ts-44-
test\unit\reminder\reminder-policy-service.test.ts-45-describe("ReminderPolicyService - domain validation", () => {
test\unit\reminder\reminder-policy-service.test.ts-46-  it("throws when scope ITEM is missing itemId", () => {
--
test\unit\reminder\reminder-policy-service.test.ts-76-    ).rejects.toBeInstanceOf(ConflictError);
test\unit\reminder\reminder-policy-service.test.ts-77-  });
test\unit\reminder\reminder-policy-service.test.ts-78-
test\unit\reminder\reminder-policy-service.test.ts-79-  it("rejects an ITEM-scoped policy whose item is not ACTIVE", async () => {
test\unit\reminder\reminder-policy-service.test.ts:80:    await store.putIfAbsent({ ...itemKey(TENANT, "item1"), entityType: "ExpirationItem", itemId: "item1", tenantId: TENANT, status: "ARCHIVED", dueDate: NOW, version: 1 });
test\unit\reminder\reminder-policy-service.test.ts-81-    await expect(
test\unit\reminder\reminder-policy-service.test.ts-82-      service.createPolicy(ctx, {
test\unit\reminder\reminder-policy-service.test.ts-83-        scope: "ITEM",
test\unit\reminder\reminder-policy-service.test.ts-84-        itemId: "item1",
--
src\modules\subject\domain\document-request-delivery-preference.ts-17-export type InitialInviteDeliveryOverride = "DEFAULT" | DocumentRequestDeliveryMode;
src\modules\subject\domain\document-request-delivery-preference.ts-18-
src\modules\subject\domain\document-request-delivery-preference.ts-19-export interface DocumentRequestDeliveryPreference extends EntityKey {
src\modules\subject\domain\document-request-delivery-preference.ts-20-  SK: "DOCUMENT_REQUEST_DELIVERY";
src\modules\subject\domain\document-request-delivery-preference.ts:21:  entityType: "DocumentRequestDeliveryPreference";
src\modules\subject\domain\document-request-delivery-preference.ts-22-  tenantId: string;
src\modules\subject\domain\document-request-delivery-preference.ts-23-  initialInviteDeliveryDefault: DocumentRequestDeliveryMode;
src\modules\subject\domain\document-request-delivery-preference.ts-24-  updatedByUserId: string;
src\modules\subject\domain\document-request-delivery-preference.ts-25-  createdAt: string;
--
test\unit\subject\document-chasing-reconciliation.test.ts-18-    const store = new InMemoryReminderStore();
test\unit\subject\document-chasing-reconciliation.test.ts-19-    const claimExpiresAt = "2026-08-23T12:02:00.000Z";
test\unit\subject\document-chasing-reconciliation.test.ts-20-    const occurrence: DocumentChasingOccurrence = {
test\unit\subject\document-chasing-reconciliation.test.ts-21-      ...documentChasingOccurrenceKey(TENANT, "s1", "a1", "d1", "2026-08-23T12:00:00.000Z", "occ-1"),
test\unit\subject\document-chasing-reconciliation.test.ts:22:      entityType: "DocumentChasingOccurrence",
test\unit\subject\document-chasing-reconciliation.test.ts-23-      occurrenceId: "occ-1",
test\unit\subject\document-chasing-reconciliation.test.ts-24-      tenantId: TENANT,
test\unit\subject\document-chasing-reconciliation.test.ts-25-      subjectId: "s1",
test\unit\subject\document-chasing-reconciliation.test.ts-26-      assignmentId: "a1",
--
test\unit\subject\document-chasing-reconciliation.test.ts-57-    const store = new InMemoryReminderStore();
test\unit\subject\document-chasing-reconciliation.test.ts-58-    const claimExpiresAt = "2026-08-23T12:10:00.000Z"; // still in the future relative to now()
test\unit\subject\document-chasing-reconciliation.test.ts-59-    const occurrence: DocumentChasingOccurrence = {
test\unit\subject\document-chasing-reconciliation.test.ts-60-      ...documentChasingOccurrenceKey(TENANT, "s1", "a1", "d1", "2026-08-23T12:00:00.000Z", "occ-1"),
test\unit\subject\document-chasing-reconciliation.test.ts:61:      entityType: "DocumentChasingOccurrence",
test\unit\subject\document-chasing-reconciliation.test.ts-62-      occurrenceId: "occ-1",
test\unit\subject\document-chasing-reconciliation.test.ts-63-      tenantId: TENANT,
test\unit\subject\document-chasing-reconciliation.test.ts-64-      subjectId: "s1",
test\unit\subject\document-chasing-reconciliation.test.ts-65-      assignmentId: "a1",
--
test\unit\reminder\reminder-materializer.test.ts-11-  return {
test\unit\reminder\reminder-materializer.test.ts-12-    ...policyKey(tenantId, policyId), // derives PK from the final tenantId/policyId, not hardcoded -
test\unit\reminder\reminder-materializer.test.ts-13-    // a fixed PK here would make two policy() calls with different policyId overrides
test\unit\reminder\reminder-materializer.test.ts-14-    // silently collide on the same store row (real bug this test file hit once).
test\unit\reminder\reminder-materializer.test.ts:15:    entityType: "ReminderPolicy",
test\unit\reminder\reminder-materializer.test.ts-16-    policyId,
test\unit\reminder\reminder-materializer.test.ts-17-    tenantId,
test\unit\reminder\reminder-materializer.test.ts-18-    scope: "ITEM",
test\unit\reminder\reminder-materializer.test.ts-19-    itemId: "item1",
--
test\unit\notification\notification-router-workflow.test.ts-16-
test\unit\notification\notification-router-workflow.test.ts-17-function makeItem(overrides: Partial<ExpirationItem> = {}): ExpirationItem {
test\unit\notification\notification-router-workflow.test.ts-18-  return {
test\unit\notification\notification-router-workflow.test.ts-19-    ...itemKey(TENANT, ITEM_ID),
test\unit\notification\notification-router-workflow.test.ts:20:    entityType: "ExpirationItem",
test\unit\notification\notification-router-workflow.test.ts-21-    itemId: ITEM_ID,
test\unit\notification\notification-router-workflow.test.ts-22-    tenantId: TENANT,
test\unit\notification\notification-router-workflow.test.ts-23-    name: "Passport",
test\unit\notification\notification-router-workflow.test.ts-24-    category: "document",
--
test\unit\notification\notification-router-workflow.test.ts-37-
test\unit\notification\notification-router-workflow.test.ts-38-function makePolicy(overrides: Partial<ReminderPolicy> = {}): ReminderPolicy {
test\unit\notification\notification-router-workflow.test.ts-39-  return {
test\unit\notification\notification-router-workflow.test.ts-40-    ...policyKey(TENANT, POLICY_ID),
test\unit\notification\notification-router-workflow.test.ts:41:    entityType: "ReminderPolicy",
test\unit\notification\notification-router-workflow.test.ts-42-    policyId: POLICY_ID,
test\unit\notification\notification-router-workflow.test.ts-43-    tenantId: TENANT,
test\unit\notification\notification-router-workflow.test.ts-44-    scope: "ITEM",
test\unit\notification\notification-router-workflow.test.ts-45-    itemId: ITEM_ID,
--
test\unit\notification\notification-router-workflow.test.ts-58-function makeIntent(overrides: Partial<NotificationIntent> = {}): NotificationIntent {
test\unit\notification\notification-router-workflow.test.ts-59-  return {
test\unit\notification\notification-router-workflow.test.ts-60-    PK: `TENANT#${TENANT}#INTENT#intent1`,
test\unit\notification\notification-router-workflow.test.ts-61-    SK: "META",
test\unit\notification\notification-router-workflow.test.ts:62:    entityType: "NotificationIntent",
test\unit\notification\notification-router-workflow.test.ts-63-    intentId: "intent1",
test\unit\notification\notification-router-workflow.test.ts-64-    tenantId: TENANT,
test\unit\notification\notification-router-workflow.test.ts-65-    kind: "EXPIRATION_REMINDER",
test\unit\notification\notification-router-workflow.test.ts-66-    itemId: ITEM_ID,
--
test\unit\notification\notification-router-workflow.test.ts-118-
test\unit\notification\notification-router-workflow.test.ts-119-  function defaultEntitlements(): NotificationEntitlements {
test\unit\notification\notification-router-workflow.test.ts-120-    return {
test\unit\notification\notification-router-workflow.test.ts-121-      ...notificationEntitlementsKey(TENANT),
test\unit\notification\notification-router-workflow.test.ts:122:      entityType: "NotificationEntitlements",
test\unit\notification\notification-router-workflow.test.ts-123-      tenantId: TENANT,
test\unit\notification\notification-router-workflow.test.ts-124-      email: { enabled: true },
test\unit\notification\notification-router-workflow.test.ts-125-      whatsapp: { enabled: false },
test\unit\notification\notification-router-workflow.test.ts-126-      planVersion: 1,
--
test\unit\notification\notification-router-workflow.test.ts-132-
test\unit\notification\notification-router-workflow.test.ts-133-  function defaultPreferences(userId: string): NotificationPreferences {
test\unit\notification\notification-router-workflow.test.ts-134-    return {
test\unit\notification\notification-router-workflow.test.ts-135-      ...notificationPreferencesKey(TENANT, userId),
test\unit\notification\notification-router-workflow.test.ts:136:      entityType: "NotificationPreferences",
test\unit\notification\notification-router-workflow.test.ts-137-      tenantId: TENANT,
test\unit\notification\notification-router-workflow.test.ts-138-      userId,
test\unit\notification\notification-router-workflow.test.ts-139-      emailEnabled: true,
test\unit\notification\notification-router-workflow.test.ts-140-      locale: "pt-BR",
--
test\unit\notification\notification-router-workflow.test.ts-158-    expect(updatedIntent?.status).toBe("DISPATCHED");
test\unit\notification\notification-router-workflow.test.ts-159-    expect(updatedIntent?.routedChannels).toEqual(["EMAIL"]);
test\unit\notification\notification-router-workflow.test.ts-160-
test\unit\notification\notification-router-workflow.test.ts-161-    const all = store.allItems();
test\unit\notification\notification-router-workflow.test.ts:162:    const attempt = all.find((i) => i["entityType"] === "NotificationAttempt") as unknown as NotificationAttempt;
test\unit\notification\notification-router-workflow.test.ts-163-    expect(attempt).toBeDefined();
test\unit\notification\notification-router-workflow.test.ts-164-    expect(attempt.status).toBe("PREPARED");
test\unit\notification\notification-router-workflow.test.ts-165-
test\unit\notification\notification-router-workflow.test.ts:166:    const lookup = all.find((i) => i["entityType"] === "NotificationAttemptLookup");
test\unit\notification\notification-router-workflow.test.ts-167-    expect(lookup).toBeDefined();
test\unit\notification\notification-router-workflow.test.ts-168-
test\unit\notification\notification-router-workflow.test.ts:169:    const outboxEvent = all.find((i) => i["entityType"] === "OutboxEvent");
test\unit\notification\notification-router-workflow.test.ts-170-    expect(outboxEvent).toBeDefined();
test\unit\notification\notification-router-workflow.test.ts-171-    expect(outboxEvent?.["destination"]).toBe("SQS_NOTIFICATION_EMAIL_V1");
test\unit\notification\notification-router-workflow.test.ts-172-  });
test\unit\notification\notification-router-workflow.test.ts-173-
--
test\unit\notification\notification-router-workflow.test.ts-204-    const oldIntent = await store.get<NotificationIntent>({ PK: intent.PK, SK: intent.SK });
test\unit\notification\notification-router-workflow.test.ts-205-    expect(oldIntent?.status).toBe("CANCELLED");
test\unit\notification\notification-router-workflow.test.ts-206-
test\unit\notification\notification-router-workflow.test.ts-207-    const all = store.allItems();
test\unit\notification\notification-router-workflow.test.ts:208:    const newIntent = all.find((i) => i["entityType"] === "NotificationIntent" && i["intentId"] !== intent.intentId) as unknown as NotificationIntent;
test\unit\notification\notification-router-workflow.test.ts-209-    expect(newIntent).toBeDefined();
test\unit\notification\notification-router-workflow.test.ts-210-    expect(newIntent.kind).toBe("REPLACEMENT");
test\unit\notification\notification-router-workflow.test.ts-211-    expect(newIntent.supersedesIntentId).toBe(intent.intentId);
test\unit\notification\notification-router-workflow.test.ts-212-    expect(newIntent.itemVersion).toBe(4);
--
test\unit\notification\notification-router-workflow.test.ts-221-    const outcome = await routeNotificationIntent(deps, intent);
test\unit\notification\notification-router-workflow.test.ts-222-    expect(outcome).toEqual({ kind: "CANCELLED", reason: "RECIPIENT_NOT_FOUND" });
test\unit\notification\notification-router-workflow.test.ts-223-
test\unit\notification\notification-router-workflow.test.ts-224-    const all = store.allItems();
test\unit\notification\notification-router-workflow.test.ts:225:    expect(all.some((i) => i["entityType"] === "NotificationAttempt")).toBe(false);
test\unit\notification\notification-router-workflow.test.ts:226:    expect(all.some((i) => i["entityType"] === "OutboxEvent")).toBe(false);
test\unit\notification\notification-router-workflow.test.ts-227-  });
test\unit\notification\notification-router-workflow.test.ts-228-
test\unit\notification\notification-router-workflow.test.ts-229-  it("entitlement record missing (technical gap) -> RETRY, no write at all", async () => {
test\unit\notification\notification-router-workflow.test.ts-230-    await seed({ preferences: defaultPreferences(TENANT) }); // no entitlements record
--
src\modules\subject\domain\document-chasing.ts-23-export type DocumentChasingTier = "T7" | "T3" | "EXPIRED";
src\modules\subject\domain\document-chasing.ts-24-export type DocumentChasingOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED";
src\modules\subject\domain\document-chasing.ts-25-
src\modules\subject\domain\document-chasing.ts-26-export interface DocumentChasingOccurrence extends EntityKey {
src\modules\subject\domain\document-chasing.ts:27:  entityType: "DocumentChasingOccurrence";
src\modules\subject\domain\document-chasing.ts-28-  occurrenceId: string;
src\modules\subject\domain\document-chasing.ts-29-  tenantId: string;
src\modules\subject\domain\document-chasing.ts-30-  subjectId: string;
src\modules\subject\domain\document-chasing.ts-31-  assignmentId: string;
--
src\modules\subject\domain\document-chasing.ts-81-  return { tenantId: match[1] as string, occurrenceId: match[2] as string };
src\modules\subject\domain\document-chasing.ts-82-}
src\modules\subject\domain\document-chasing.ts-83-
src\modules\subject\domain\document-chasing.ts-84-/** WORKSTATE#CLAIMED é a MESMA constante global de `reconciliation-candidate-source.ts` —
src\modules\subject\domain\document-chasing.ts:85: * reconciliação de claim-expiry já lê esse workstate para qualquer entityType (D-048/D-046: só
src\modules\subject\domain\document-chasing.ts-86- * o tipo TypeScript de `reconcileExpiredClaims` precisa alargar, o mecanismo é idêntico). */
src\modules\subject\domain\document-chasing.ts-87-export function buildChasingClaimGsi6Sk(claimExpiresAt: string, tenantId: string, occurrenceId: string): string {
src\modules\subject\domain\document-chasing.ts-88-  return `${claimExpiresAt}#TENANT#${tenantId}#CHASING#${occurrenceId}`;
src\modules\subject\domain\document-chasing.ts-89-}
--
src\modules\subject\domain\document-chasing.ts-98- * DocumentChasingIntent — não retrofitado no NotificationIntent existente"). Single-channel
src\modules\subject\domain\document-chasing.ts-99- * (e-mail) em v1 — sem fan-out multi-canal, sem lease/retry (o próprio próximo tier de chasing
src\modules\subject\domain\document-chasing.ts-100- * é o mecanismo de retry natural do produto). */
src\modules\subject\domain\document-chasing.ts-101-export interface DocumentChasingIntent extends EntityKey {
src\modules\subject\domain\document-chasing.ts:102:  entityType: "DocumentChasingIntent";
src\modules\subject\domain\document-chasing.ts-103-  intentId: string;
src\modules\subject\domain\document-chasing.ts-104-  tenantId: string;
src\modules\subject\domain\document-chasing.ts-105-  subjectId: string;
src\modules\subject\domain\document-chasing.ts-106-  assignmentId: string;
--
src\modules\subject\domain\audit-event.ts-28-  | "CONFIGURE_DOCUMENT_REQUEST_DELIVERY";
src\modules\subject\domain\audit-event.ts-29-export type SubjectAuditResourceType = "TrackedSubject" | "RequirementAssignment" | "DocumentRequest" | "DocumentRequestDeliveryPreference";
src\modules\subject\domain\audit-event.ts-30-
src\modules\subject\domain\audit-event.ts-31-export interface SubjectAuditEvent extends EntityKey {
src\modules\subject\domain\audit-event.ts:32:  entityType: "SubjectAuditEvent";
src\modules\subject\domain\audit-event.ts-33-  auditEventId: string;
src\modules\subject\domain\audit-event.ts-34-  tenantId: string;
src\modules\subject\domain\audit-event.ts-35-  resourceType: SubjectAuditResourceType;
src\modules\subject\domain\audit-event.ts-36-  resourceId: string;
--
src\modules\subject\domain\audit-event.ts-70-export function buildSubjectAuditEvent(input: BuildSubjectAuditEventInput): SubjectAuditEvent {
src\modules\subject\domain\audit-event.ts-71-  const key = subjectAuditKey(input.tenantId, input.occurredAt, input.auditEventId);
src\modules\subject\domain\audit-event.ts-72-  return {
src\modules\subject\domain\audit-event.ts-73-    ...key,
src\modules\subject\domain\audit-event.ts:74:    entityType: "SubjectAuditEvent",
src\modules\subject\domain\audit-event.ts-75-    auditEventId: input.auditEventId,
src\modules\subject\domain\audit-event.ts-76-    tenantId: input.tenantId,
src\modules\subject\domain\audit-event.ts-77-    resourceType: input.resourceType,
src\modules\subject\domain\audit-event.ts-78-    resourceId: input.resourceId,
--
src\modules\import\application\import-service.ts-120-      jobId = this.ids.newImportJobId();
src\modules\import\application\import-service.ts-121-      const jobExpiresAt = new Date(Date.parse(now) + IMPORT_JOB_TTL_SECONDS * 1000).toISOString();
src\modules\import\application\import-service.ts-122-      const job: ImportJob = {
src\modules\import\application\import-service.ts-123-        ...importJobKey(ctx.tenant.tenantId, jobId),
src\modules\import\application\import-service.ts:124:        entityType: "ImportJob",
src\modules\import\application\import-service.ts-125-        jobId,
src\modules\import\application\import-service.ts-126-        tenantId: ctx.tenant.tenantId,
src\modules\import\application\import-service.ts-127-        targetEntityType: "TrackedSubject",
src\modules\import\application\import-service.ts-128-        status: "UPLOADED",
--
src\modules\import\application\import-commit-service.ts-86-  for (const entry of pending) {
src\modules\import\application\import-commit-service.ts-87-    const dedupExternalId = entry.row.externalId ?? syntheticRowDedupKey(jobId, entry.rowNumber);
src\modules\import\application\import-commit-service.ts-88-    const claimed = await deps.store.putIfAbsent<ImportDedupRecord>({
src\modules\import\application\import-commit-service.ts-89-      ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
src\modules\import\application\import-commit-service.ts:90:      entityType: "ImportDedupRecord",
src\modules\import\application\import-commit-service.ts-91-      tenantId: ctx.tenant.tenantId,
src\modules\import\application\import-commit-service.ts-92-      externalId: dedupExternalId,
src\modules\import\application\import-commit-service.ts-93-      subjectId: "", // preenchido abaixo, após createSubject() confirmar - placeholder aceitável aqui pois a claim em si (não o valor) é o que garante idempotência.
src\modules\import\application\import-commit-service.ts-94-      createdAt: deps.now(),
--
src\modules\import\application\import-commit-service.ts-103-          tags: entry.row.tags,
src\modules\import\application\import-commit-service.ts-104-        });
src\modules\import\application\import-commit-service.ts-105-        await deps.store.update<ImportDedupRecord>({
src\modules\import\application\import-commit-service.ts-106-          ...importDedupKey(ctx.tenant.tenantId, dedupExternalId),
src\modules\import\application\import-commit-service.ts:107:          entityType: "ImportDedupRecord",
src\modules\import\application\import-commit-service.ts-108-          tenantId: ctx.tenant.tenantId,
src\modules\import\application\import-commit-service.ts-109-          externalId: dedupExternalId,
src\modules\import\application\import-commit-service.ts-110-          subjectId: subject.subjectId,
src\modules\import\application\import-commit-service.ts-111-          createdAt: deps.now(),
--
src\modules\import\domain\import-job.ts-28-  | "EXPIRED";
src\modules\import\domain\import-job.ts-29-
src\modules\import\domain\import-job.ts-30-export interface ImportJob extends EntityKey {
src\modules\import\domain\import-job.ts-31-  SK: "META";
src\modules\import\domain\import-job.ts:32:  entityType: "ImportJob";
src\modules\import\domain\import-job.ts-33-  jobId: string;
src\modules\import\domain\import-job.ts-34-  tenantId: string;
src\modules\import\domain\import-job.ts-35-  targetEntityType: ImportTargetEntityType;
src\modules\import\domain\import-job.ts-36-  status: ImportJobStatus;
--
src\modules\import\domain\import-dedup.ts-7- */
src\modules\import\domain\import-dedup.ts-8-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\import\domain\import-dedup.ts-9-
src\modules\import\domain\import-dedup.ts-10-export interface ImportDedupRecord extends EntityKey {
src\modules\import\domain\import-dedup.ts:11:  entityType: "ImportDedupRecord";
src\modules\import\domain\import-dedup.ts-12-  tenantId: string;
src\modules\import\domain\import-dedup.ts-13-  externalId: string;
src\modules\import\domain\import-dedup.ts-14-  subjectId: string;
src\modules\import\domain\import-dedup.ts-15-  createdAt: string;
--
src\modules\subject\application\subject-service.ts-72-      const now = this.now();
src\modules\subject\application\subject-service.ts-73-      const displayNameNormalized = normalizeDisplayName(input.displayName);
src\modules\subject\application\subject-service.ts-74-      const subject: TrackedSubject = {
src\modules\subject\application\subject-service.ts-75-        ...subjectKey(ctx.tenant.tenantId, subjectId),
src\modules\subject\application\subject-service.ts:76:        entityType: "TrackedSubject",
src\modules\subject\application\subject-service.ts-77-        subjectId,
src\modules\subject\application\subject-service.ts-78-        tenantId: ctx.tenant.tenantId,
src\modules\subject\application\subject-service.ts-79-        type: input.type,
src\modules\subject\application\subject-service.ts-80-        displayName: input.displayName,
--
src\modules\document\application\document-service.ts-135-
src\modules\document\application\document-service.ts-136-    if (begin === "ACQUIRED") {
src\modules\document\application\document-service.ts-137-      const document: Document = {
src\modules\document\application\document-service.ts-138-        ...documentKey(ctx.tenant.tenantId, itemId, documentId),
src\modules\document\application\document-service.ts:139:        entityType: "Document",
src\modules\document\application\document-service.ts-140-        tenantId: ctx.tenant.tenantId,
src\modules\document\application\document-service.ts-141-        itemId,
src\modules\document\application\document-service.ts-142-        documentId,
src\modules\document\application\document-service.ts-143-        uploadSlotId,
--
src\modules\document\application\document-service.ts-153-        updatedAt: now,
src\modules\document\application\document-service.ts-154-      };
src\modules\document\application\document-service.ts-155-      const slot: UploadSlot = {
src\modules\document\application\document-service.ts-156-        ...uploadSlotKey(ctx.tenant.tenantId, uploadSlotId),
src\modules\document\application\document-service.ts:157:        entityType: "UploadSlot",
src\modules\document\application\document-service.ts-158-        tenantId: ctx.tenant.tenantId,
src\modules\document\application\document-service.ts-159-        uploadSlotId,
src\modules\document\application\document-service.ts-160-        documentId,
src\modules\document\application\document-service.ts-161-        itemId,
--
src\modules\expiration\domain\item-watch.ts-9-
src\modules\expiration\domain\item-watch.ts-10-export type ItemWatchStatus = "ACTIVE" | "REMOVED";
src\modules\expiration\domain\item-watch.ts-11-
src\modules\expiration\domain\item-watch.ts-12-export interface ItemWatch extends EntityKey {
src\modules\expiration\domain\item-watch.ts:13:  entityType: "ItemWatch";
src\modules\expiration\domain\item-watch.ts-14-  itemId: string;
src\modules\expiration\domain\item-watch.ts-15-  tenantId: string;
src\modules\expiration\domain\item-watch.ts-16-  userId: string;
src\modules\expiration\domain\item-watch.ts-17-  status: ItemWatchStatus;
--
src\modules\subject\application\requirement-service.ts-54-    const assignmentId = this.ids.newAssignmentId();
src\modules\subject\application\requirement-service.ts-55-    const now = this.now();
src\modules\subject\application\requirement-service.ts-56-    const assignment: RequirementAssignment = {
src\modules\subject\application\requirement-service.ts-57-      ...requirementAssignmentKey(subject.tenantId, subjectId, assignmentId),
src\modules\subject\application\requirement-service.ts:58:      entityType: "RequirementAssignment",
src\modules\subject\application\requirement-service.ts-59-      assignmentId,
src\modules\subject\application\requirement-service.ts-60-      subjectId,
src\modules\subject\application\requirement-service.ts-61-      tenantId: subject.tenantId,
src\modules\subject\application\requirement-service.ts-62-      requirementName: input.requirementName,
--
src\modules\subject\application\document-request-service.ts-127-    const issued = issueGuestToken(this.pepper);
src\modules\subject\application\document-request-service.ts-128-
src\modules\subject\application\document-request-service.ts-129-    const request: DocumentRequest = {
src\modules\subject\application\document-request-service.ts-130-      ...documentRequestKey(assignment.tenantId, subjectId, assignmentId, documentRequestId),
src\modules\subject\application\document-request-service.ts:131:      entityType: "DocumentRequest",
src\modules\subject\application\document-request-service.ts-132-      documentRequestId,
src\modules\subject\application\document-request-service.ts-133-      tenantId: assignment.tenantId,
src\modules\subject\application\document-request-service.ts-134-      subjectId,
src\modules\subject\application\document-request-service.ts-135-      assignmentId,
--
src\modules\subject\application\document-request-service.ts-148-      version: 1,
src\modules\subject\application\document-request-service.ts-149-    };
src\modules\subject\application\document-request-service.ts-150-    const pointer: GuestTokenPointer = {
src\modules\subject\application\document-request-service.ts-151-      ...guestTokenPointerKey(issued.selectorHash),
src\modules\subject\application\document-request-service.ts:152:      entityType: "GuestTokenPointer",
src\modules\subject\application\document-request-service.ts-153-      selectorHash: issued.selectorHash,
src\modules\subject\application\document-request-service.ts-154-      secretHash: issued.secretHash,
src\modules\subject\application\document-request-service.ts-155-      tenantId: assignment.tenantId,
src\modules\subject\application\document-request-service.ts-156-      subjectId,
--
src\modules\subject\application\document-request-service.ts-265-      : [
src\modules\subject\application\document-request-service.ts-266-          {
src\modules\subject\application\document-request-service.ts-267-            Put: buildVersionedCreate(this.tableName, {
src\modules\subject\application\document-request-service.ts-268-              ...key,
src\modules\subject\application\document-request-service.ts:269:              entityType: "DocumentRequestDeliveryPreference",
src\modules\subject\application\document-request-service.ts-270-              tenantId: ctx.tenant.tenantId,
src\modules\subject\application\document-request-service.ts-271-              initialInviteDeliveryDefault: mode,
src\modules\subject\application\document-request-service.ts-272-              updatedByUserId: ctx.principal.userId,
src\modules\subject\application\document-request-service.ts-273-              createdAt: now,
--
src\modules\subject\application\document-request-service.ts-405-    // documentRequestId sozinho não endereça a partição (precisa de assignmentId) - varre a
src\modules\subject\application\document-request-service.ts-406-    // coleção de requirements do subject e localiza pelo SK. Aceitável no v1 (poucos
src\modules\subject\application\document-request-service.ts-407-    // documentRequests por subject); revisar se o volume crescer.
src\modules\subject\application\document-request-service.ts-408-    const rows = await this.store.queryByPk<DocumentRequest>(`TENANT#${tenantId}#SUBJECT#${subjectId}`, REQUIREMENT_ASSIGNMENT_SK_PREFIX);
src\modules\subject\application\document-request-service.ts:409:    const found = rows.find((r) => r.entityType === "DocumentRequest" && r.documentRequestId === documentRequestId);
src\modules\subject\application\document-request-service.ts-410-    if (!found) throw new NotFoundError("DocumentRequest not found.", { subjectId, documentRequestId });
src\modules\subject\application\document-request-service.ts-411-    return found;
src\modules\subject\application\document-request-service.ts-412-  }
src\modules\subject\application\document-request-service.ts-413-}
--
src\modules\subject\application\document-chasing-materializer.ts-99-      });
src\modules\subject\application\document-chasing-materializer.ts-100-
src\modules\subject\application\document-chasing-materializer.ts-101-      const occurrence: DocumentChasingOccurrence = {
src\modules\subject\application\document-chasing-materializer.ts-102-        ...documentChasingOccurrenceKey(input.tenantId, input.subjectId, input.assignmentId, input.documentRequestId, scheduledAt, occurrenceId),
src\modules\subject\application\document-chasing-materializer.ts:103:        entityType: "DocumentChasingOccurrence",
src\modules\subject\application\document-chasing-materializer.ts-104-        occurrenceId,
src\modules\subject\application\document-chasing-materializer.ts-105-        tenantId: input.tenantId,
src\modules\subject\application\document-chasing-materializer.ts-106-        subjectId: input.subjectId,
src\modules\subject\application\document-chasing-materializer.ts-107-        assignmentId: input.assignmentId,
--
src\modules\expiration\domain\expiration-item.ts-11-export type ExpirationItemStatus = "ACTIVE" | "ARCHIVED" | "RENEWED" | "DELETED";
src\modules\expiration\domain\expiration-item.ts-12-
src\modules\expiration\domain\expiration-item.ts-13-export interface ExpirationItem extends EntityKey {
src\modules\expiration\domain\expiration-item.ts-14-  SK: "META";
src\modules\expiration\domain\expiration-item.ts:15:  entityType: "ExpirationItem";
src\modules\expiration\domain\expiration-item.ts-16-  itemId: string;
src\modules\expiration\domain\expiration-item.ts-17-  tenantId: string;
src\modules\expiration\domain\expiration-item.ts-18-  name: string;
src\modules\expiration\domain\expiration-item.ts-19-  category: string;
--
src\modules\subject\application\initial-invite-rate-limiter.ts-16-import { createHash } from "node:crypto";
src\modules\subject\application\initial-invite-rate-limiter.ts-17-
src\modules\subject\application\initial-invite-rate-limiter.ts-18-export interface InitialInviteRateLimitRecord extends EntityKey {
src\modules\subject\application\initial-invite-rate-limiter.ts-19-  SK: "RATE" | "RATE_DAILY";
src\modules\subject\application\initial-invite-rate-limiter.ts:20:  entityType: "InitialInviteRateLimit";
src\modules\subject\application\initial-invite-rate-limiter.ts-21-  limit: number;
src\modules\subject\application\initial-invite-rate-limiter.ts-22-  windowSeconds: number;
src\modules\subject\application\initial-invite-rate-limiter.ts-23-  count: number;
src\modules\subject\application\initial-invite-rate-limiter.ts-24-  resetAt: string;
--
src\modules\subject\application\initial-invite-rate-limiter.ts-58-      const existing = await this.store.get<InitialInviteRateLimitRecord>(key);
src\modules\subject\application\initial-invite-rate-limiter.ts-59-      if (!existing) {
src\modules\subject\application\initial-invite-rate-limiter.ts-60-        const created = await this.store.putIfAbsent<InitialInviteRateLimitRecord>({
src\modules\subject\application\initial-invite-rate-limiter.ts-61-          ...key,
src\modules\subject\application\initial-invite-rate-limiter.ts:62:          entityType: "InitialInviteRateLimit",
src\modules\subject\application\initial-invite-rate-limiter.ts-63-          limit,
src\modules\subject\application\initial-invite-rate-limiter.ts-64-          windowSeconds,
src\modules\subject\application\initial-invite-rate-limiter.ts-65-          count: 1,
src\modules\subject\application\initial-invite-rate-limiter.ts-66-          resetAt,
--
src\modules\subject\application\advance-after-submission-evidence.ts-32-    const key = documentSubmissionKey(input.tenantId, input.subjectId, input.assignmentId, input.submissionId);
src\modules\subject\application\advance-after-submission-evidence.ts-33-    const submission = await deps.store.get<DocumentSubmission>(key);
src\modules\subject\application\advance-after-submission-evidence.ts-34-    if (!submission) return "IGNORED_STALE";
src\modules\subject\application\advance-after-submission-evidence.ts-35-
src\modules\subject\application\advance-after-submission-evidence.ts:36:    const knownObject = submission.uploadEvidence?.object ?? submission.malwareEvidence?.object;
src\modules\subject\application\advance-after-submission-evidence.ts-37-
src\modules\subject\application\advance-after-submission-evidence.ts-38-    if (knownObject && knownObject.key === input.expectedObject.key && !sameObjectVersion(knownObject, input.expectedObject)) {
src\modules\subject\application\advance-after-submission-evidence.ts-39-      return "IGNORED_WRONG_VERSION";
src\modules\subject\application\advance-after-submission-evidence.ts-40-    }
src\modules\subject\application\advance-after-submission-evidence.ts-41-
src\modules\subject\application\advance-after-submission-evidence.ts-42-    const decision = decideNextAction({
src\modules\subject\application\advance-after-submission-evidence.ts-43-      currentStatus: submission.status,
src\modules\subject\application\advance-after-submission-evidence.ts:44:      uploadValid: submission.uploadEvidence?.valid,
src\modules\subject\application\advance-after-submission-evidence.ts:45:      uploadEvidence: submission.uploadEvidence,
src\modules\subject\application\advance-after-submission-evidence.ts:46:      malwareEvidence: submission.malwareEvidence,
src\modules\subject\application\advance-after-submission-evidence.ts-47-    });
src\modules\subject\application\advance-after-submission-evidence.ts-48-
src\modules\subject\application\advance-after-submission-evidence.ts-49-    if (decision.action === "IGNORE_STALE_EVENT") return "IGNORED_STALE";
src\modules\subject\application\advance-after-submission-evidence.ts-50-    if (decision.action === "AWAIT_MORE_EVIDENCE") return "AWAITING";
--
src\modules\subject\application\advance-after-submission-evidence.ts-62-    }
src\modules\subject\application\advance-after-submission-evidence.ts-63-
src\modules\subject\application\advance-after-submission-evidence.ts-64-    const sourceObject = knownObject ?? submission.quarantineObject;
src\modules\subject\application\advance-after-submission-evidence.ts-65-    const cleanKey = `clean/${submission.tenantId}/${submission.submissionId}`;
src\modules\subject\application\advance-after-submission-evidence.ts:66:    const cleanObject = await deps.objects.copyObject(sourceObject, deps.cleanBucket, cleanKey);
src\modules\subject\application\advance-after-submission-evidence.ts:67:    const verify = await deps.objects.headObject(cleanObject);
src\modules\subject\application\advance-after-submission-evidence.ts-68-    if (!verify || verify.contentLength !== submission.contentLength) {
src\modules\subject\application\advance-after-submission-evidence.ts-69-      throw new Error(`Promotion copy verification failed for submission ${submission.submissionId}`);
src\modules\subject\application\advance-after-submission-evidence.ts-70-    }
src\modules\subject\application\advance-after-submission-evidence.ts-71-
src\modules\subject\application\advance-after-submission-evidence.ts-72-    const entries: TransactWriteEntry[] = [
src\modules\subject\application\advance-after-submission-evidence.ts:73:      { Update: buildVersionedUpdate({ tableName: deps.tableName, key, tenantId: input.tenantId, expectedVersion: submission.version, set: { status: "CLEAN", cleanObject } }) },
src\modules\subject\application\advance-after-submission-evidence.ts-74-    ];
src\modules\subject\application\advance-after-submission-evidence.ts-75-    try {
src\modules\subject\application\advance-after-submission-evidence.ts-76-      await deps.store.transactWrite(entries);
src\modules\subject\application\advance-after-submission-evidence.ts-77-      try {
--
src\modules\identity\persistence\user-repository.ts-12-
src\modules\identity\persistence\user-repository.ts-13-export interface UserProfile {
src\modules\identity\persistence\user-repository.ts-14-  PK: string;
src\modules\identity\persistence\user-repository.ts-15-  SK: "PROFILE";
src\modules\identity\persistence\user-repository.ts:16:  entityType: "User";
src\modules\identity\persistence\user-repository.ts-17-  userId: string;
src\modules\identity\persistence\user-repository.ts-18-  tenantId: string;
src\modules\identity\persistence\user-repository.ts-19-  identitySubject: string;
src\modules\identity\persistence\user-repository.ts-20-  emailNormalized: string;
--
src\modules\identity\persistence\user-repository.ts-34-
src\modules\identity\persistence\user-repository.ts-35-export interface DeviceSession {
src\modules\identity\persistence\user-repository.ts-36-  PK: string;
src\modules\identity\persistence\user-repository.ts-37-  SK: string; // SESSION#<deviceId>
src\modules\identity\persistence\user-repository.ts:38:  entityType: "DeviceSession";
src\modules\identity\persistence\user-repository.ts-39-  tenantId: string;
src\modules\identity\persistence\user-repository.ts-40-  userId: string;
src\modules\identity\persistence\user-repository.ts-41-  deviceId: string;
src\modules\identity\persistence\user-repository.ts-42-  sessionId: string;
--
src\modules\identity\persistence\user-repository.ts-65-  async getProfile(tenantId: string, userId: string): Promise<UserProfile | undefined> {
src\modules\identity\persistence\user-repository.ts-66-    return this.store.get<UserProfile>(userProfileKey(tenantId, userId));
src\modules\identity\persistence\user-repository.ts-67-  }
src\modules\identity\persistence\user-repository.ts-68-
src\modules\identity\persistence\user-repository.ts:69:  async createProfileIfAbsent(profile: Omit<UserProfile, "PK" | "SK" | "entityType" | "createdAt" | "updatedAt" | "version">): Promise<UserProfile> {
src\modules\identity\persistence\user-repository.ts-70-    const key = userProfileKey(profile.tenantId, profile.userId);
src\modules\identity\persistence\user-repository.ts-71-    const existing = await this.getProfile(profile.tenantId, profile.userId);
src\modules\identity\persistence\user-repository.ts-72-    if (existing) {
src\modules\identity\persistence\user-repository.ts-73-      return existing;
--
src\modules\identity\persistence\user-repository.ts-75-    const now = this.now();
src\modules\identity\persistence\user-repository.ts-76-    const item: UserProfile = {
src\modules\identity\persistence\user-repository.ts-77-      ...key,
src\modules\identity\persistence\user-repository.ts-78-      SK: "PROFILE",
src\modules\identity\persistence\user-repository.ts:79:      entityType: "User",
src\modules\identity\persistence\user-repository.ts-80-      ...profile,
src\modules\identity\persistence\user-repository.ts-81-      createdAt: now,
src\modules\identity\persistence\user-repository.ts-82-      updatedAt: now,
src\modules\identity\persistence\user-repository.ts-83-      version: 1,
--
src\modules\document\application\advance-after-evidence.ts-1-/**
src\modules\document\application\advance-after-evidence.ts-2- * Orchestration around `decideNextAction()` — called identically by UploadFinalizerWorker
src\modules\document\application\advance-after-evidence.ts:3: * (after persisting uploadEvidence) and MalwareResultWorker (after persisting malwareEvidence).
src\modules\document\application\advance-after-evidence.ts-4- * Owns the OCC read-decide-write loop and the actual promotion copy; the pure decision logic
src\modules\document\application\advance-after-evidence.ts-5- * itself lives in document-state-machine.ts.
src\modules\document\application\advance-after-evidence.ts-6- */
src\modules\document\application\advance-after-evidence.ts-7-import { buildVersionedUpdate, isTransactionCanceled } from "../../../shared/dynamodb/occ.js";
--
src\modules\document\application\advance-after-evidence.ts-62-
src\modules\document\application\advance-after-evidence.ts-63-    // `doc.quarantineObject.versionId` is always "" (reserveUpload sets it before the real
src\modules\document\application\advance-after-evidence.ts-64-    // object exists - the actual versionId is only known once evidence arrives) - the real,
src\modules\document\application\advance-after-evidence.ts-65-    // current object reference lives in whichever evidence has already been persisted.
src\modules\document\application\advance-after-evidence.ts:66:    const knownObject = doc.uploadEvidence?.object ?? doc.malwareEvidence?.object;
src\modules\document\application\advance-after-evidence.ts-67-
src\modules\document\application\advance-after-evidence.ts-68-    // A late event for a superseded object version (e.g. a re-upload reused the slot before
src\modules\document\application\advance-after-evidence.ts-69-    // this event was processed) is never applied to the current document state.
src\modules\document\application\advance-after-evidence.ts-70-    if (knownObject && knownObject.key === input.expectedObject.key && !sameObjectVersion(knownObject, input.expectedObject)) {
--
src\modules\document\application\advance-after-evidence.ts-72-    }
src\modules\document\application\advance-after-evidence.ts-73-
src\modules\document\application\advance-after-evidence.ts-74-    const decision = decideNextAction({
src\modules\document\application\advance-after-evidence.ts-75-      currentStatus: doc.status,
src\modules\document\application\advance-after-evidence.ts:76:      uploadValid: doc.uploadEvidence?.valid,
src\modules\document\application\advance-after-evidence.ts:77:      uploadEvidence: doc.uploadEvidence,
src\modules\document\application\advance-after-evidence.ts:78:      malwareEvidence: doc.malwareEvidence,
src\modules\document\application\advance-after-evidence.ts-79-    });
src\modules\document\application\advance-after-evidence.ts-80-
src\modules\document\application\advance-after-evidence.ts-81-    if (decision.action === "IGNORE_STALE_EVENT") return "IGNORED_STALE";
src\modules\document\application\advance-after-evidence.ts-82-    if (decision.action === "AWAIT_MORE_EVIDENCE") return "AWAITING";
--
src\modules\document\application\advance-after-evidence.ts-97-
src\modules\document\application\advance-after-evidence.ts-98-    // decision.action === "PROMOTE": copy quarantine -> clean, verify, then confirm CLEAN.
src\modules\document\application\advance-after-evidence.ts-99-    // Real bug found via Camada 3 (2026-08-22): copying from `doc.quarantineObject` crashed
src\modules\document\application\advance-after-evidence.ts-100-    // with "Version id cannot be the empty string" - that field's versionId is always "" (see
src\modules\document\application\advance-after-evidence.ts:101:    // comment above), never the real S3 version. PROMOTE is only reachable once uploadEvidence
src\modules\document\application\advance-after-evidence.ts-102-    // exists (uploadValid === true is required by decideNextAction), so `knownObject` here is
src\modules\document\application\advance-after-evidence.ts-103-    // guaranteed to hold the real, observed object reference.
src\modules\document\application\advance-after-evidence.ts-104-    const sourceObject = knownObject ?? doc.quarantineObject;
src\modules\document\application\advance-after-evidence.ts-105-    // M7 (ExtractionStarterWorker, D-035 §12.5): the clean-bucket S3 event that triggers
--
src\modules\document\application\advance-after-evidence.ts-107-    // itemId to look up. Extending the key to mirror the quarantine key's item-anchored shape
src\modules\document\application\advance-after-evidence.ts-108-    // (parseQuarantineKey's own pattern) closes that gap at the source instead of adding a
src\modules\document\application\advance-after-evidence.ts-109-    // GSI just to look up Document by documentId alone.
src\modules\document\application\advance-after-evidence.ts-110-    const cleanKey = `clean/${doc.tenantId}/${doc.itemId}/${doc.documentId}`;
src\modules\document\application\advance-after-evidence.ts:111:    const cleanObject = await deps.objects.copyObject(sourceObject, deps.cleanBucket, cleanKey);
src\modules\document\application\advance-after-evidence.ts:112:    const verify = await deps.objects.headObject(cleanObject);
src\modules\document\application\advance-after-evidence.ts-113-    if (!verify || verify.contentLength !== doc.contentLength) {
src\modules\document\application\advance-after-evidence.ts-114-      // Copy landed but didn't verify - never confirm CLEAN on unverified data. The
src\modules\document\application\advance-after-evidence.ts-115-      // reconciler's TIMEOUT path recovers a document stuck here; a bare retry of this
src\modules\document\application\advance-after-evidence.ts-116-      // function attempt is also safe (copyObject is not required to be idempotent across
--
src\modules\document\application\advance-after-evidence.ts-124-          tableName: deps.tableName,
src\modules\document\application\advance-after-evidence.ts-125-          key,
src\modules\document\application\advance-after-evidence.ts-126-          tenantId: input.tenantId,
src\modules\document\application\advance-after-evidence.ts-127-          expectedVersion: doc.version,
src\modules\document\application\advance-after-evidence.ts:128:          set: { status: "CLEAN", cleanObject },
src\modules\document\application\advance-after-evidence.ts-129-        }),
src\modules\document\application\advance-after-evidence.ts-130-      },
src\modules\document\application\advance-after-evidence.ts-131-    ];
src\modules\document\application\advance-after-evidence.ts-132-    await appendSlotConsumption(deps, entries, input.tenantId, doc.uploadSlotId);
--
src\modules\expiration\domain\audit-event.ts-12-
src\modules\expiration\domain\audit-event.ts-13-export type AuditAction = "CREATE" | "UPDATE" | "ARCHIVE" | "RENEW" | "DELETE";
src\modules\expiration\domain\audit-event.ts-14-
src\modules\expiration\domain\audit-event.ts-15-export interface AuditEvent extends EntityKey {
src\modules\expiration\domain\audit-event.ts:16:  entityType: "AuditEvent";
src\modules\expiration\domain\audit-event.ts-17-  auditEventId: string;
src\modules\expiration\domain\audit-event.ts-18-  tenantId: string;
src\modules\expiration\domain\audit-event.ts-19-  resourceType: "ExpirationItem";
src\modules\expiration\domain\audit-event.ts-20-  resourceId: string;
--
src\modules\expiration\domain\audit-event.ts-59-export function buildAuditEvent(input: BuildAuditEventInput): AuditEvent {
src\modules\expiration\domain\audit-event.ts-60-  const key = auditKey(input.tenantId, input.occurredAt, input.auditEventId);
src\modules\expiration\domain\audit-event.ts-61-  return {
src\modules\expiration\domain\audit-event.ts-62-    ...key,
src\modules\expiration\domain\audit-event.ts:63:    entityType: "AuditEvent",
src\modules\expiration\domain\audit-event.ts-64-    auditEventId: input.auditEventId,
src\modules\expiration\domain\audit-event.ts-65-    tenantId: input.tenantId,
src\modules\expiration\domain\audit-event.ts-66-    resourceType: "ExpirationItem",
src\modules\expiration\domain\audit-event.ts-67-    resourceId: input.itemId,
--
src\modules\subject\application\guest-submission-service.ts-152-    const quarantineKey = `tenant/${tenantId}/subject/${subjectId}/assignment/${assignmentId}/submission/${submissionId}/document/${submissionId}/slot/${submissionId}/${randomUUID()}`;
src\modules\subject\application\guest-submission-service.ts-153-
src\modules\subject\application\guest-submission-service.ts-154-    const submission: DocumentSubmission = {
src\modules\subject\application\guest-submission-service.ts-155-      ...documentSubmissionKey(tenantId, subjectId, assignmentId, submissionId),
src\modules\subject\application\guest-submission-service.ts:156:      entityType: "DocumentSubmission",
src\modules\subject\application\guest-submission-service.ts-157-      submissionId,
src\modules\subject\application\guest-submission-service.ts-158-      tenantId,
src\modules\subject\application\guest-submission-service.ts-159-      subjectId,
src\modules\subject\application\guest-submission-service.ts-160-      assignmentId,
--
src\modules\reminder\domain\reminder-policy.ts-34-}
src\modules\reminder\domain\reminder-policy.ts-35-
src\modules\reminder\domain\reminder-policy.ts-36-export interface ReminderPolicy extends EntityKey, ReminderRule {
src\modules\reminder\domain\reminder-policy.ts-37-  SK: "META";
src\modules\reminder\domain\reminder-policy.ts:38:  entityType: "ReminderPolicy";
src\modules\reminder\domain\reminder-policy.ts-39-  policyId: string;
src\modules\reminder\domain\reminder-policy.ts-40-  tenantId: string;
src\modules\reminder\domain\reminder-policy.ts-41-  scope: ReminderPolicyScope;
src\modules\reminder\domain\reminder-policy.ts-42-  itemId?: string;
--
src\modules\reminder\domain\reminder-policy.ts-66-
src\modules\reminder\domain\reminder-policy.ts-67-export const POLICY_REF_SK_PREFIX = "POLICYREF#";
src\modules\reminder\domain\reminder-policy.ts-68-
src\modules\reminder\domain\reminder-policy.ts-69-export interface PolicyRef extends EntityKey {
src\modules\reminder\domain\reminder-policy.ts:70:  entityType: "ReminderPolicyRef";
src\modules\reminder\domain\reminder-policy.ts-71-  policyId: string;
src\modules\reminder\domain\reminder-policy.ts-72-}
src\modules\reminder\domain\reminder-policy.ts-73-
src\modules\reminder\domain\reminder-policy.ts-74-export interface PutPolicyInput {
--
src\modules\subject\application\guest-rate-limiter.ts-11-import type { EntityKey, SubjectStore } from "../ports/subject-store.js";
src\modules\subject\application\guest-rate-limiter.ts-12-
src\modules\subject\application\guest-rate-limiter.ts-13-export interface GuestRateLimitRecord extends EntityKey {
src\modules\subject\application\guest-rate-limiter.ts-14-  SK: "RATE";
src\modules\subject\application\guest-rate-limiter.ts:15:  entityType: "GuestTokenRateLimit";
src\modules\subject\application\guest-rate-limiter.ts-16-  selectorHash: string;
src\modules\subject\application\guest-rate-limiter.ts-17-  limit: number;
src\modules\subject\application\guest-rate-limiter.ts-18-  windowSeconds: number;
src\modules\subject\application\guest-rate-limiter.ts-19-  count: number;
--
src\modules\subject\application\guest-rate-limiter.ts-44-      const existing = await this.store.get<GuestRateLimitRecord>(key);
src\modules\subject\application\guest-rate-limiter.ts-45-      if (!existing) {
src\modules\subject\application\guest-rate-limiter.ts-46-        const created = await this.store.putIfAbsent<GuestRateLimitRecord>({
src\modules\subject\application\guest-rate-limiter.ts-47-          ...key,
src\modules\subject\application\guest-rate-limiter.ts:48:          entityType: "GuestTokenRateLimit",
src\modules\subject\application\guest-rate-limiter.ts-49-          selectorHash: input.selectorHash,
src\modules\subject\application\guest-rate-limiter.ts-50-          limit: input.limit,
src\modules\subject\application\guest-rate-limiter.ts-51-          windowSeconds: input.windowSeconds,
src\modules\subject\application\guest-rate-limiter.ts-52-          count: 1,
--
src\modules\identity\persistence\identity-mapping-repository.ts-10-
src\modules\identity\persistence\identity-mapping-repository.ts-11-export interface IdentityMapping {
src\modules\identity\persistence\identity-mapping-repository.ts-12-  PK: string;
src\modules\identity\persistence\identity-mapping-repository.ts-13-  SK: "MAP";
src\modules\identity\persistence\identity-mapping-repository.ts:14:  entityType: "IdentityMapping";
src\modules\identity\persistence\identity-mapping-repository.ts-15-  cognitoSub: string;
src\modules\identity\persistence\identity-mapping-repository.ts-16-  userId: string;
src\modules\identity\persistence\identity-mapping-repository.ts-17-  tenantId: string;
src\modules\identity\persistence\identity-mapping-repository.ts-18-  createdAt: string;
--
src\modules\identity\persistence\identity-mapping-repository.ts-51-    const key = identityMappingKey(cognitoSub);
src\modules\identity\persistence\identity-mapping-repository.ts-52-    const candidate: IdentityMapping = {
src\modules\identity\persistence\identity-mapping-repository.ts-53-      ...key,
src\modules\identity\persistence\identity-mapping-repository.ts-54-      SK: "MAP",
src\modules\identity\persistence\identity-mapping-repository.ts:55:      entityType: "IdentityMapping",
src\modules\identity\persistence\identity-mapping-repository.ts-56-      cognitoSub,
src\modules\identity\persistence\identity-mapping-repository.ts-57-      userId: newUserId,
src\modules\identity\persistence\identity-mapping-repository.ts-58-      tenantId: newTenantId,
src\modules\identity\persistence\identity-mapping-repository.ts-59-      createdAt: this.now(),
--
src\modules\reminder\domain\reminder-occurrence.ts-8-
src\modules\reminder\domain\reminder-occurrence.ts-9-export type ReminderOccurrenceStatus = "SCHEDULED" | "CLAIMED" | "CANCELLED" | "TRIGGERED" | "ACKED";
src\modules\reminder\domain\reminder-occurrence.ts-10-
src\modules\reminder\domain\reminder-occurrence.ts-11-export interface ReminderOccurrence extends EntityKey {
src\modules\reminder\domain\reminder-occurrence.ts:12:  entityType: "ReminderOccurrence";
src\modules\reminder\domain\reminder-occurrence.ts-13-  occurrenceId: string;
src\modules\reminder\domain\reminder-occurrence.ts-14-  tenantId: string;
src\modules\reminder\domain\reminder-occurrence.ts-15-  itemId: string;
src\modules\reminder\domain\reminder-occurrence.ts-16-  policyId: string;
--
src\modules\reminder\domain\notification-intent.ts-30-  | "CHANNEL_UNAVAILABLE";
src\modules\reminder\domain\notification-intent.ts-31-
src\modules\reminder\domain\notification-intent.ts-32-export interface NotificationIntent extends EntityKey {
src\modules\reminder\domain\notification-intent.ts-33-  SK: "META";
src\modules\reminder\domain\notification-intent.ts:34:  entityType: "NotificationIntent";
src\modules\reminder\domain\notification-intent.ts-35-  intentId: string;
src\modules\reminder\domain\notification-intent.ts-36-  tenantId: string;
src\modules\reminder\domain\notification-intent.ts-37-  kind: NotificationIntentKind;
src\modules\reminder\domain\notification-intent.ts-38-  itemId: string;
--
src\modules\extraction\ports\extraction-execution-starter.ts-10-  documentId: string;
src\modules\extraction\ports\extraction-execution-starter.ts-11-  documentVersion: number;
src\modules\extraction\ports\extraction-execution-starter.ts-12-  runId: string;
src\modules\extraction\ports\extraction-execution-starter.ts-13-  pipelineVersion: string;
src\modules\extraction\ports\extraction-execution-starter.ts:14:  cleanObject: { bucket: string; key: string; versionId: string };
src\modules\extraction\ports\extraction-execution-starter.ts-15-  /** RunTextract's START_OCR operation (`start-ocr.ts`) requires these to classify the document
src\modules\extraction\ports\extraction-execution-starter.ts-16-   * type before calling Textract - real bug found 2026-08-27 verifying the pipeline end-to-end
src\modules\extraction\ports\extraction-execution-starter.ts-17-   * against `dev`: without them, `classifyDocumentType` throws on every real invocation
src\modules\extraction\ports\extraction-execution-starter.ts-18-   * (`fileName` is not optional there), which `toAppError` turns into a generic `InternalError`
--
src\modules\expiration\application\item-watch-service.ts-39-    if (existing?.status === "ACTIVE") return existing; // idempotente
src\modules\expiration\application\item-watch-service.ts-40-
src\modules\expiration\application\item-watch-service.ts-41-    const now = this.now();
src\modules\expiration\application\item-watch-service.ts-42-    if (!existing) {
src\modules\expiration\application\item-watch-service.ts:43:      const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src\modules\expiration\application\item-watch-service.ts-44-      const created = await this.store.putIfAbsent(watch);
src\modules\expiration\application\item-watch-service.ts-45-      if (created) return watch;
src\modules\expiration\application\item-watch-service.ts-46-      // Corrida com outra criação concorrente - relê e trata como reativação abaixo.
src\modules\expiration\application\item-watch-service.ts-47-    }
--
src\modules\expiration\application\item-watch-service.ts-53-    for (let attempt = 0; attempt < 5; attempt++) {
src\modules\expiration\application\item-watch-service.ts-54-      const current = await this.store.get<ItemWatch>(key);
src\modules\expiration\application\item-watch-service.ts-55-      if (!current) {
src\modules\expiration\application\item-watch-service.ts-56-        const now = this.now();
src\modules\expiration\application\item-watch-service.ts:57:        const watch: ItemWatch = { ...key, entityType: "ItemWatch", itemId, tenantId: ctx.tenant.tenantId, userId, status: "ACTIVE", createdAt: now, updatedAt: now, version: 1 };
src\modules\expiration\application\item-watch-service.ts-58-        if (await this.store.putIfAbsent(watch)) return watch;
src\modules\expiration\application\item-watch-service.ts-59-        continue;
src\modules\expiration\application\item-watch-service.ts-60-      }
src\modules\expiration\application\item-watch-service.ts-61-      if (current.status === "ACTIVE") return current;
--
src\modules\identity\application\quota.ts-21-
src\modules\identity\application\quota.ts-22-export interface TenantQuotaRecord {
src\modules\identity\application\quota.ts-23-  PK: string;
src\modules\identity\application\quota.ts-24-  SK: string;
src\modules\identity\application\quota.ts:25:  entityType: "TenantQuota";
src\modules\identity\application\quota.ts-26-  tenantId: string;
src\modules\identity\application\quota.ts-27-  quotaType: QuotaType;
src\modules\identity\application\quota.ts-28-  limit: number;
src\modules\identity\application\quota.ts-29-  windowSeconds: number;
--
src\modules\identity\application\quota.ts-79-      const existing = await this.store.get<TenantQuotaRecord>(key);
src\modules\identity\application\quota.ts-80-      if (!existing) {
src\modules\identity\application\quota.ts-81-        const created = await this.store.putIfAbsent({
src\modules\identity\application\quota.ts-82-          ...key,
src\modules\identity\application\quota.ts:83:          entityType: "TenantQuota",
src\modules\identity\application\quota.ts-84-          tenantId: input.tenantId,
src\modules\identity\application\quota.ts-85-          quotaType: input.quotaType,
src\modules\identity\application\quota.ts-86-          limit: input.limit,
src\modules\identity\application\quota.ts-87-          windowSeconds: input.windowSeconds,
--
src\modules\reminder\application\reminder-policy-service.ts-59-    const policyId = this.ids.newPolicyId();
src\modules\reminder\application\reminder-policy-service.ts-60-    const now = this.now();
src\modules\reminder\application\reminder-policy-service.ts-61-    const policy: ReminderPolicy = {
src\modules\reminder\application\reminder-policy-service.ts-62-      ...policyKey(tenantId, policyId),
src\modules\reminder\application\reminder-policy-service.ts:63:      entityType: "ReminderPolicy",
src\modules\reminder\application\reminder-policy-service.ts-64-      policyId,
src\modules\reminder\application\reminder-policy-service.ts-65-      tenantId,
src\modules\reminder\application\reminder-policy-service.ts-66-      scope: input.scope,
src\modules\reminder\application\reminder-policy-service.ts-67-      itemId: input.itemId,
--
src\modules\reminder\application\reminder-policy-service.ts-235-    if (!input.skipPointerWrite) {
src\modules\reminder\application\reminder-policy-service.ts-236-      entries.push({
src\modules\reminder\application\reminder-policy-service.ts-237-        Put: {
src\modules\reminder\application\reminder-policy-service.ts-238-          TableName: this.tableName,
src\modules\reminder\application\reminder-policy-service.ts:239:          Item: { ...policyRefKey(input.tenantId, input.itemId, input.policyId), entityType: "ReminderPolicyRef", policyId: input.policyId },
src\modules\reminder\application\reminder-policy-service.ts-240-          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\reminder\application\reminder-policy-service.ts-241-        },
src\modules\reminder\application\reminder-policy-service.ts-242-      });
src\modules\reminder\application\reminder-policy-service.ts-243-    }
--
src\modules\reminder\application\reminder-materializer.ts-176-        : {};
src\modules\reminder\application\reminder-materializer.ts-177-
src\modules\reminder\application\reminder-materializer.ts-178-      const occurrence: ReminderOccurrence = {
src\modules\reminder\application\reminder-materializer.ts-179-        ...occurrenceKey(input.tenantId, input.itemId, schedule.scheduledAtUtc, occurrenceId),
src\modules\reminder\application\reminder-materializer.ts:180:        entityType: "ReminderOccurrence",
src\modules\reminder\application\reminder-materializer.ts-181-        occurrenceId,
src\modules\reminder\application\reminder-materializer.ts-182-        tenantId: input.tenantId,
src\modules\reminder\application\reminder-materializer.ts-183-        itemId: input.itemId,
src\modules\reminder\application\reminder-materializer.ts-184-        policyId: input.policy.policyId,
--
src\modules\bff\application\bff-auth-service.ts-69-    const now = this.deps.now();
src\modules\bff\application\bff-auth-service.ts-70-    const attempt: LoginAttempt = {
src\modules\bff\application\bff-auth-service.ts-71-      ...loginAttemptKey(issued.selectorHash),
src\modules\bff\application\bff-auth-service.ts-72-      SK: "POINTER",
src\modules\bff\application\bff-auth-service.ts:73:      entityType: "LoginAttempt",
src\modules\bff\application\bff-auth-service.ts-74-      selectorHash: issued.selectorHash,
src\modules\bff\application\bff-auth-service.ts-75-      secretHash: issued.secretHash,
src\modules\bff\application\bff-auth-service.ts-76-      state,
src\modules\bff\application\bff-auth-service.ts-77-      nonce,
--
src\modules\bff\application\bff-auth-service.ts-111-    const parsed = parseOpaqueToken(input.loginCookie);
src\modules\bff\application\bff-auth-service.ts-112-    if (!parsed) throw new AuthenticationError("Malformed login session.");
src\modules\bff\application\bff-auth-service.ts-113-
src\modules\bff\application\bff-auth-service.ts-114-    const attempt = await this.deps.sessionStore.get<LoginAttempt>(loginAttemptKey(hmacTokenCrypto.hash(this.deps.pepper, parsed.selector)));
src\modules\bff\application\bff-auth-service.ts:115:    if (!attempt || attempt.entityType !== "LoginAttempt" || attempt.consumedAt) {
src\modules\bff\application\bff-auth-service.ts-116-      throw new AuthenticationError("Login session not found or already used.");
src\modules\bff\application\bff-auth-service.ts-117-    }
src\modules\bff\application\bff-auth-service.ts-118-    if (!opaqueTokenSecretMatches(this.deps.pepper, parsed.secret, attempt.secretHash)) {
src\modules\bff\application\bff-auth-service.ts-119-      throw new AuthenticationError("Invalid login session.");
--
src\modules\bff\application\bff-auth-service.ts-177-    const deviceId = this.deps.newDeviceId();
src\modules\bff\application\bff-auth-service.ts-178-    await this.deps.users.upsertDeviceSession({
src\modules\bff\application\bff-auth-service.ts-179-      PK: `TENANT#${mapping.tenantId}#USER#${mapping.userId}`,
src\modules\bff\application\bff-auth-service.ts-180-      SK: `SESSION#${deviceId}`,
src\modules\bff\application\bff-auth-service.ts:181:      entityType: "DeviceSession",
src\modules\bff\application\bff-auth-service.ts-182-      tenantId: mapping.tenantId,
src\modules\bff\application\bff-auth-service.ts-183-      userId: mapping.userId,
src\modules\bff\application\bff-auth-service.ts-184-      deviceId,
src\modules\bff\application\bff-auth-service.ts-185-      sessionId: randomUUID(),
--
src\modules\bff\application\bff-auth-service.ts-196-    const absoluteExpiresAt = new Date(Date.parse(now) + SESSION_ABSOLUTE_TTL_SECONDS * 1000).toISOString();
src\modules\bff\application\bff-auth-service.ts-197-    const session: Session = {
src\modules\bff\application\bff-auth-service.ts-198-      ...sessionKey(issuedSession.selectorHash),
src\modules\bff\application\bff-auth-service.ts-199-      SK: "POINTER",
src\modules\bff\application\bff-auth-service.ts:200:      entityType: "Session",
src\modules\bff\application\bff-auth-service.ts-201-      selectorHash: issuedSession.selectorHash,
src\modules\bff\application\bff-auth-service.ts-202-      secretHash: issuedSession.secretHash,
src\modules\bff\application\bff-auth-service.ts-203-      tenantId: mapping.tenantId,
src\modules\bff\application\bff-auth-service.ts-204-      userId: mapping.userId,
--
src\modules\bff\application\bff-auth-service.ts-232-    if (!parsed) throw new AuthenticationError("Malformed session.");
src\modules\bff\application\bff-auth-service.ts-233-    const selectorHash = hmacTokenCrypto.hash(this.deps.pepper, parsed.selector);
src\modules\bff\application\bff-auth-service.ts-234-
src\modules\bff\application\bff-auth-service.ts-235-    let session = await this.deps.sessionStore.get<Session>(sessionKey(selectorHash));
src\modules\bff\application\bff-auth-service.ts:236:    if (!session || session.entityType !== "Session" || session.revokedAt) {
src\modules\bff\application\bff-auth-service.ts-237-      throw new AuthenticationError("Session not found.");
src\modules\bff\application\bff-auth-service.ts-238-    }
src\modules\bff\application\bff-auth-service.ts-239-    if (!opaqueTokenSecretMatches(this.deps.pepper, parsed.secret, session.secretHash)) {
src\modules\bff\application\bff-auth-service.ts-240-      throw new AuthenticationError("Invalid session.");
--
src\modules\bff\application\bff-auth-service.ts-447-    if (!sessionCookie) return;
src\modules\bff\application\bff-auth-service.ts-448-    const parsed = parseOpaqueToken(sessionCookie);
src\modules\bff\application\bff-auth-service.ts-449-    if (!parsed) return;
src\modules\bff\application\bff-auth-service.ts-450-    const session = await this.deps.sessionStore.get<Session>(sessionKey(hmacTokenCrypto.hash(this.deps.pepper, parsed.selector)));
src\modules\bff\application\bff-auth-service.ts:451:    if (!session || session.entityType !== "Session") return;
src\modules\bff\application\bff-auth-service.ts-452-    // Verifies the secret half of the token too, same as resolveSession() (found in Round D
src\modules\bff\application\bff-auth-service.ts-453-    // re-verification: without this, knowing only a session's selector - e.g. leaked via a
src\modules\bff\application\bff-auth-service.ts-454-    // log line or timing side-channel, never meant to be treated as sufficient on its own -
src\modules\bff\application\bff-auth-service.ts-455-    // was enough to force-revoke or globally log out an account this cookie never proved
--
src\modules\bff\application\bff-auth-service.ts-486-    if (!sessionCookie) return;
src\modules\bff\application\bff-auth-service.ts-487-    const parsed = parseOpaqueToken(sessionCookie);
src\modules\bff\application\bff-auth-service.ts-488-    if (!parsed) return;
src\modules\bff\application\bff-auth-service.ts-489-    const session = await this.deps.sessionStore.get<Session>(sessionKey(hmacTokenCrypto.hash(this.deps.pepper, parsed.selector)));
src\modules\bff\application\bff-auth-service.ts:490:    if (!session || session.entityType !== "Session") return;
src\modules\bff\application\bff-auth-service.ts-491-    // Same secret verification as logout() - a selector alone must never be sufficient to
src\modules\bff\application\bff-auth-service.ts-492-    // trigger a global logout of someone else's account.
src\modules\bff\application\bff-auth-service.ts-493-    if (!opaqueTokenSecretMatches(this.deps.pepper, parsed.secret, session.secretHash)) return;
src\modules\bff\application\bff-auth-service.ts-494-    // An already-revoked or already-expired session must never authorize a cross-device
--
src\modules\expiration\application\expiration-service.ts-150-    const itemId = this.ids.newItemId();
src\modules\expiration\application\expiration-service.ts-151-    const now = this.now();
src\modules\expiration\application\expiration-service.ts-152-    const item: ExpirationItem = {
src\modules\expiration\application\expiration-service.ts-153-      ...itemKey(ctx.tenant.tenantId, itemId),
src\modules\expiration\application\expiration-service.ts:154:      entityType: "ExpirationItem",
src\modules\expiration\application\expiration-service.ts-155-      itemId,
src\modules\expiration\application\expiration-service.ts-156-      tenantId: ctx.tenant.tenantId,
src\modules\expiration\application\expiration-service.ts-157-      name: input.name,
src\modules\expiration\application\expiration-service.ts-158-      category: input.category,
--
src\modules\expiration\application\expiration-service.ts-454-    const newVersion = expectedVersion + 1;
src\modules\expiration\application\expiration-service.ts-455-
src\modules\expiration\application\expiration-service.ts-456-    const newItem: ExpirationItem = {
src\modules\expiration\application\expiration-service.ts-457-      ...itemKey(source.tenantId, newItemId),
src\modules\expiration\application\expiration-service.ts:458:      entityType: "ExpirationItem",
src\modules\expiration\application\expiration-service.ts-459-      itemId: newItemId,
src\modules\expiration\application\expiration-service.ts-460-      tenantId: source.tenantId,
src\modules\expiration\application\expiration-service.ts-461-      name: source.name,
src\modules\expiration\application\expiration-service.ts-462-      category: source.category,
--
src\modules\expiration\application\expiration-service.ts-540-
src\modules\expiration\application\expiration-service.ts-541-      const newPolicyId = this.ids.newPolicyId();
src\modules\expiration\application\expiration-service.ts-542-      const newPolicy: ReminderPolicy = {
src\modules\expiration\application\expiration-service.ts-543-        ...policyKey(source.tenantId, newPolicyId),
src\modules\expiration\application\expiration-service.ts:544:        entityType: "ReminderPolicy",
src\modules\expiration\application\expiration-service.ts-545-        policyId: newPolicyId,
src\modules\expiration\application\expiration-service.ts-546-        tenantId: source.tenantId,
src\modules\expiration\application\expiration-service.ts-547-        scope: "ITEM",
src\modules\expiration\application\expiration-service.ts-548-        itemId: newItemId,
--
src\modules\expiration\application\expiration-service.ts-560-      entries.push({ Put: buildVersionedCreate(this.tableName, newPolicy as unknown as Record<string, unknown> & { PK: string; SK: string }) });
src\modules\expiration\application\expiration-service.ts-561-      entries.push({
src\modules\expiration\application\expiration-service.ts-562-        Put: {
src\modules\expiration\application\expiration-service.ts-563-          TableName: this.tableName,
src\modules\expiration\application\expiration-service.ts:564:          Item: { ...policyRefKey(source.tenantId, newItemId, newPolicyId), entityType: "ReminderPolicyRef", policyId: newPolicyId },
src\modules\expiration\application\expiration-service.ts-565-          ConditionExpression: "attribute_not_exists(PK) AND attribute_not_exists(SK)",
src\modules\expiration\application\expiration-service.ts-566-        },
src\modules\expiration\application\expiration-service.ts-567-      });
src\modules\expiration\application\expiration-service.ts-568-      copiedReminderPolicyIds.push(newPolicyId);
--
src\modules\document\domain\document.ts-2- * Document — data-model.md line 34: PK=`TENANT#t#ITEM#i`, SK=`DOC#d`. State set matches the
src\modules\document\domain\document.ts-3- * approved data model exactly: PENDING_UPLOAD/SCANNING/CLEAN/REJECTED/UNSUPPORTED/TIMEOUT/
src\modules\document\domain\document.ts-4- * DELETED (implementation-blueprint.md §12.2-12.3; TIMEOUT is driven by
src\modules\document\domain\document.ts-5- * UploadSlotReconciliationWorker for scans that never resolve, not by the malware/finalizer
src\modules\document\domain\document.ts:6: * workers directly). M6 design §2.1 adds uploadEvidence/malwareEvidence (independent evidence
src\modules\document\domain\document.ts-7- * fields, never presuming which arrives first - see document-state-machine.ts) and
src\modules\document\domain\document.ts-8- * retentionClass/purgeAfter (privacy-lgpd.md §4 USER_DOCUMENT class materialized on the entity
src\modules\document\domain\document.ts-9- * from day one, not bolted on later).
src\modules\document\domain\document.ts-10- */
--
src\modules\document\domain\document.ts-28-  observedAt: string;
src\modules\document\domain\document.ts-29-}
src\modules\document\domain\document.ts-30-
src\modules\document\domain\document.ts-31-export interface Document extends EntityKey {
src\modules\document\domain\document.ts:32:  entityType: "Document";
src\modules\document\domain\document.ts-33-  tenantId: string;
src\modules\document\domain\document.ts-34-  itemId: string;
src\modules\document\domain\document.ts-35-  documentId: string;
src\modules\document\domain\document.ts-36-  uploadSlotId: string;
--
src\modules\document\domain\document.ts-40-  checksumSha256: string;
src\modules\document\domain\document.ts-41-  status: DocumentStatus;
src\modules\document\domain\document.ts-42-  quarantineObject: DocumentObjectReference;
src\modules\document\domain\document.ts-43-  /** Only present once promoted - never populated before status === "CLEAN". */
src\modules\document\domain\document.ts:44:  cleanObject?: DocumentObjectReference;
src\modules\document\domain\document.ts:45:  uploadEvidence?: UploadEvidence;
src\modules\document\domain\document.ts:46:  malwareEvidence?: MalwareEvidence;
src\modules\document\domain\document.ts-47-  extractionRunId?: string; // M7, unused by M6, kept for schema forward-compatibility only.
src\modules\document\domain\document.ts-48-  retentionClass: Extract<DocumentRetentionClass, "USER_DOCUMENT">;
src\modules\document\domain\document.ts-49-  purgeAfter?: string; // set only once a terminal/deletion event establishes the clock.
src\modules\document\domain\document.ts-50-  version: number;
--
src\modules\bff\domain\session.ts-12- */
src\modules\bff\domain\session.ts-13-export interface Session {
src\modules\bff\domain\session.ts-14-  PK: string;
src\modules\bff\domain\session.ts-15-  SK: "POINTER";
src\modules\bff\domain\session.ts:16:  entityType: "Session";
src\modules\bff\domain\session.ts-17-  selectorHash: string;
src\modules\bff\domain\session.ts-18-  secretHash: string;
src\modules\bff\domain\session.ts-19-  tenantId: string;
src\modules\bff\domain\session.ts-20-  userId: string;
--
src\modules\bff\domain\session.ts-54- */
src\modules\bff\domain\session.ts-55-export interface LoginAttempt {
src\modules\bff\domain\session.ts-56-  PK: string;
src\modules\bff\domain\session.ts-57-  SK: "POINTER";
src\modules\bff\domain\session.ts:58:  entityType: "LoginAttempt";
src\modules\bff\domain\session.ts-59-  selectorHash: string;
src\modules\bff\domain\session.ts-60-  secretHash: string;
src\modules\bff\domain\session.ts-61-  state: string;
src\modules\bff\domain\session.ts-62-  nonce: string;
--
src\modules\document\domain\document-state-machine.ts-26-  /** True once the finalizer confirmed the object's size/checksum/mediaType AND the PDF
src\modules\document\domain\document-state-machine.ts-27-   * sandbox parser (when applicable) validated its structure. Undefined = no upload evidence
src\modules\document\domain\document-state-machine.ts-28-   * yet at all. */
src\modules\document\domain\document-state-machine.ts-29-  uploadValid?: boolean;
src\modules\document\domain\document-state-machine.ts:30:  uploadEvidence?: UploadEvidence;
src\modules\document\domain\document-state-machine.ts:31:  malwareEvidence?: MalwareEvidence;
src\modules\document\domain\document-state-machine.ts-32-}
src\modules\document\domain\document-state-machine.ts-33-
src\modules\document\domain\document-state-machine.ts-34-/**
src\modules\document\domain\document-state-machine.ts-35- * Rule order matches M6 design §2.3 exactly:
--
src\modules\document\domain\document-state-machine.ts-45-  if (TERMINAL_STATUSES.has(input.currentStatus)) {
src\modules\document\domain\document-state-machine.ts-46-    return { action: "IGNORE_STALE_EVENT" };
src\modules\document\domain\document-state-machine.ts-47-  }
src\modules\document\domain\document-state-machine.ts-48-
src\modules\document\domain\document-state-machine.ts:49:  if (input.malwareEvidence?.status === "THREATS_FOUND") {
src\modules\document\domain\document-state-machine.ts-50-    return { action: "REJECT", status: "REJECTED" };
src\modules\document\domain\document-state-machine.ts-51-  }
src\modules\document\domain\document-state-machine.ts-52-
src\modules\document\domain\document-state-machine.ts:53:  if (input.malwareEvidence?.status === "UNSUPPORTED") {
src\modules\document\domain\document-state-machine.ts-54-    return { action: "REJECT", status: "UNSUPPORTED" };
src\modules\document\domain\document-state-machine.ts-55-  }
src\modules\document\domain\document-state-machine.ts-56-
src\modules\document\domain\document-state-machine.ts-57-  if (input.uploadValid === false) {
src\modules\document\domain\document-state-machine.ts-58-    return { action: "REJECT", status: "REJECTED" };
src\modules\document\domain\document-state-machine.ts-59-  }
src\modules\document\domain\document-state-machine.ts-60-
src\modules\document\domain\document-state-machine.ts:61:  const malwareClean = input.malwareEvidence?.status === "NO_THREATS_FOUND";
src\modules\document\domain\document-state-machine.ts-62-  const uploadConfirmedValid = input.uploadValid === true;
src\modules\document\domain\document-state-machine.ts-63-
src\modules\document\domain\document-state-machine.ts-64-  if (uploadConfirmedValid && malwareClean) {
src\modules\document\domain\document-state-machine.ts-65-    return { action: "PROMOTE" };
--
src\modules\document\domain\upload-slot.ts-9-
src\modules\document\domain\upload-slot.ts-10-export type UploadSlotStatus = "RESERVED" | "CONSUMED" | "EXPIRED" | "RELEASED";
src\modules\document\domain\upload-slot.ts-11-
src\modules\document\domain\upload-slot.ts-12-export interface UploadSlot extends EntityKey {
src\modules\document\domain\upload-slot.ts:13:  entityType: "UploadSlot";
src\modules\document\domain\upload-slot.ts-14-  tenantId: string;
src\modules\document\domain\upload-slot.ts-15-  uploadSlotId: string;
src\modules\document\domain\upload-slot.ts-16-  documentId: string;
src\modules\document\domain\upload-slot.ts-17-  itemId: string;
--
src\modules\notification\domain\notification-preferences.ts-15-export type NotificationConsentSource = "ONBOARDING" | "USER_SETTINGS" | "MIGRATED_DEFAULT";
src\modules\notification\domain\notification-preferences.ts-16-
src\modules\notification\domain\notification-preferences.ts-17-export interface NotificationPreferences extends EntityKey {
src\modules\notification\domain\notification-preferences.ts-18-  SK: "NOTIFICATION_PREFERENCES";
src\modules\notification\domain\notification-preferences.ts:19:  entityType: "NotificationPreferences";
src\modules\notification\domain\notification-preferences.ts-20-  tenantId: string;
src\modules\notification\domain\notification-preferences.ts-21-  userId: string;
src\modules\notification\domain\notification-preferences.ts-22-  emailEnabled: boolean;
src\modules\notification\domain\notification-preferences.ts-23-  locale: string;
--
src\modules\notification\domain\notification-preferences.ts-46-  consentSource?: NotificationConsentSource;
src\modules\notification\domain\notification-preferences.ts-47-}): NotificationPreferences {
src\modules\notification\domain\notification-preferences.ts-48-  return {
src\modules\notification\domain\notification-preferences.ts-49-    ...notificationPreferencesKey(input.tenantId, input.userId),
src\modules\notification\domain\notification-preferences.ts:50:    entityType: "NotificationPreferences",
src\modules\notification\domain\notification-preferences.ts-51-    tenantId: input.tenantId,
src\modules\notification\domain\notification-preferences.ts-52-    userId: input.userId,
src\modules\notification\domain\notification-preferences.ts-53-    emailEnabled: true,
src\modules\notification\domain\notification-preferences.ts-54-    locale: input.locale,
--
src\modules\notification\domain\notification-attempt.ts-21-  | "NOT_SENT_STALE";
src\modules\notification\domain\notification-attempt.ts-22-
src\modules\notification\domain\notification-attempt.ts-23-export interface NotificationAttempt extends EntityKey {
src\modules\notification\domain\notification-attempt.ts-24-  // PK = TENANT#<tenantId>#INTENT#<intentId>, SK = ATTEMPT#<attemptNumber padded>#<attemptId>
src\modules\notification\domain\notification-attempt.ts:25:  entityType: "NotificationAttempt";
src\modules\notification\domain\notification-attempt.ts-26-  tenantId: string;
src\modules\notification\domain\notification-attempt.ts-27-  intentId: string;
src\modules\notification\domain\notification-attempt.ts-28-  attemptId: string;
src\modules\notification\domain\notification-attempt.ts-29-  attemptNumber: number;
--
src\modules\notification\domain\notification-attempt.ts-87- * Read with ConsistentRead=true by the callback worker, together with the attempt itself.
src\modules\notification\domain\notification-attempt.ts-88- */
src\modules\notification\domain\notification-attempt.ts-89-export interface NotificationAttemptLookup extends EntityKey {
src\modules\notification\domain\notification-attempt.ts-90-  // PK = TENANT#<tenantId>#ATTEMPT#<attemptId>, SK = LOOKUP
src\modules\notification\domain\notification-attempt.ts:91:  entityType: "NotificationAttemptLookup";
src\modules\notification\domain\notification-attempt.ts-92-  tenantId: string;
src\modules\notification\domain\notification-attempt.ts-93-  intentId: string;
src\modules\notification\domain\notification-attempt.ts-94-  attemptSk: string;
src\modules\notification\domain\notification-attempt.ts-95-  provider: "SES";
--
src\modules\notification\domain\notification-attempt.ts-104-  attempt: Pick<NotificationAttempt, "tenantId" | "intentId" | "attemptId" | "attemptNumber" | "provider" | "providerAccountId">,
src\modules\notification\domain\notification-attempt.ts-105-): NotificationAttemptLookup {
src\modules\notification\domain\notification-attempt.ts-106-  return {
src\modules\notification\domain\notification-attempt.ts-107-    ...notificationAttemptLookupKey(attempt.tenantId, attempt.attemptId),
src\modules\notification\domain\notification-attempt.ts:108:    entityType: "NotificationAttemptLookup",
src\modules\notification\domain\notification-attempt.ts-109-    tenantId: attempt.tenantId,
src\modules\notification\domain\notification-attempt.ts-110-    intentId: attempt.intentId,
src\modules\notification\domain\notification-attempt.ts-111-    attemptSk: notificationAttemptKey(attempt.tenantId, attempt.intentId, attempt.attemptNumber, attempt.attemptId).SK,
src\modules\notification\domain\notification-attempt.ts-112-    provider: attempt.provider,
--
src\modules\extraction\application\start-ocr.ts-47-  documentId: string;
src\modules\extraction\application\start-ocr.ts-48-  documentVersion: number;
src\modules\extraction\application\start-ocr.ts-49-  runId: string;
src\modules\extraction\application\start-ocr.ts-50-  pipelineVersion: string;
src\modules\extraction\application\start-ocr.ts:51:  cleanObject: { bucket: string; key: string; versionId: string };
src\modules\extraction\application\start-ocr.ts-52-  fileName: string;
src\modules\extraction\application\start-ocr.ts-53-  contentType?: string;
src\modules\extraction\application\start-ocr.ts-54-  magicBytes?: Uint8Array;
src\modules\extraction\application\start-ocr.ts-55-}
--
src\modules\extraction\application\start-ocr.ts-93-
src\modules\extraction\application\start-ocr.ts-94-  let jobId: string;
src\modules\extraction\application\start-ocr.ts-95-  try {
src\modules\extraction\application\start-ocr.ts-96-    const started = await deps.textract.startDocumentTextDetection({
src\modules\extraction\application\start-ocr.ts:97:      bucket: input.cleanObject.bucket,
src\modules\extraction\application\start-ocr.ts:98:      key: input.cleanObject.key,
src\modules\extraction\application\start-ocr.ts-99-      clientRequestToken,
src\modules\extraction\application\start-ocr.ts-100-      jobTag: input.runId,
src\modules\extraction\application\start-ocr.ts-101-      snsTopicArn: deps.snsTopicArn,
src\modules\extraction\application\start-ocr.ts-102-      snsRoleArn: deps.snsRoleArn,
--
src\modules\extraction\application\start-ocr.ts-112-  const now = deps.now?.() ?? new Date().toISOString();
src\modules\extraction\application\start-ocr.ts-113-  const taskTokenCiphertext = await deps.tokenEncryptor.encrypt(input.taskToken);
src\modules\extraction\application\start-ocr.ts-114-  const job: TextractJob = {
src\modules\extraction\application\start-ocr.ts-115-    ...textractJobKey(jobId),
src\modules\extraction\application\start-ocr.ts:116:    entityType: "TextractJob",
src\modules\extraction\application\start-ocr.ts-117-    jobId,
src\modules\extraction\application\start-ocr.ts-118-    tenantId: input.tenantId,
src\modules\extraction\application\start-ocr.ts-119-    itemId: input.itemId,
src\modules\extraction\application\start-ocr.ts-120-    documentId: input.documentId,
--
src\modules\notification\application\notification-router-workflow.ts-183-
src\modules\notification\application\notification-router-workflow.ts-184-  const newIntent: NotificationIntent = {
src\modules\notification\application\notification-router-workflow.ts-185-    PK: `TENANT#${intent.tenantId}#INTENT#${newIntentId}`,
src\modules\notification\application\notification-router-workflow.ts-186-    SK: "META",
src\modules\notification\application\notification-router-workflow.ts:187:    entityType: "NotificationIntent",
src\modules\notification\application\notification-router-workflow.ts-188-    intentId: newIntentId,
src\modules\notification\application\notification-router-workflow.ts-189-    tenantId: intent.tenantId,
src\modules\notification\application\notification-router-workflow.ts-190-    kind: correctiveKind,
src\modules\notification\application\notification-router-workflow.ts-191-    itemId: intent.itemId,
--
src\modules\notification\application\notification-router-workflow.ts-220-        TableName: deps.tableName,
src\modules\notification\application\notification-router-workflow.ts-221-        Item: {
src\modules\notification\application\notification-router-workflow.ts-222-          PK: idem.PK,
src\modules\notification\application\notification-router-workflow.ts-223-          SK: idem.SK,
src\modules\notification\application\notification-router-workflow.ts:224:          entityType: "IdempotencyRecord",
src\modules\notification\application\notification-router-workflow.ts-225-          tenantId: intent.tenantId,
src\modules\notification\application\notification-router-workflow.ts-226-          operation: "notification.corrective",
src\modules\notification\application\notification-router-workflow.ts-227-          requestHash: idemKey,
src\modules\notification\application\notification-router-workflow.ts-228-          status: "COMPLETED",
--
src\modules\notification\application\notification-router-workflow.ts-276-    const attemptId = deps.newAttemptId();
src\modules\notification\application\notification-router-workflow.ts-277-    const attemptNumber = 1;
src\modules\notification\application\notification-router-workflow.ts-278-    const attempt: NotificationAttempt = {
src\modules\notification\application\notification-router-workflow.ts-279-      ...notificationAttemptKey(intent.tenantId, intent.intentId, attemptNumber, attemptId),
src\modules\notification\application\notification-router-workflow.ts:280:      entityType: "NotificationAttempt",
src\modules\notification\application\notification-router-workflow.ts-281-      tenantId: intent.tenantId,
src\modules\notification\application\notification-router-workflow.ts-282-      intentId: intent.intentId,
src\modules\notification\application\notification-router-workflow.ts-283-      attemptId,
src\modules\notification\application\notification-router-workflow.ts-284-      attemptNumber,
--
src\modules\notification\application\notification-router-workflow.ts-334-  const eventId = attempt.attemptId;
src\modules\notification\application\notification-router-workflow.ts-335-  return {
src\modules\notification\application\notification-router-workflow.ts-336-    PK: `TENANT#${intent.tenantId}#OUTBOX#${shard}`,
src\modules\notification\application\notification-router-workflow.ts-337-    SK: `EVENT#${now}#${eventId}`,
src\modules\notification\application\notification-router-workflow.ts:338:    entityType: "OutboxEvent",
src\modules\notification\application\notification-router-workflow.ts-339-    // BLOCKER-B added tenantId as an explicit OutboxRecord field (previously only embedded
src\modules\notification\application\notification-router-workflow.ts-340-    // in PK) for a different destination's sender to use - added here too for the same
src\modules\notification\application\notification-router-workflow.ts-341-    // reason (and so every real OutboxEvent row is consistently shaped), even though this
src\modules\notification\application\notification-router-workflow.ts-342-    // destination's own payload already self-describes its tenant.
--
src\modules\extraction\domain\textract-job.ts-22-
src\modules\extraction\domain\textract-job.ts-23-export type TextractJobStatus = "STARTED" | "COMPLETED" | "FAILED";
src\modules\extraction\domain\textract-job.ts-24-
src\modules\extraction\domain\textract-job.ts-25-export interface TextractJob extends EntityKey {
src\modules\extraction\domain\textract-job.ts:26:  entityType: "TextractJob";
src\modules\extraction\domain\textract-job.ts-27-  jobId: string;
src\modules\extraction\domain\textract-job.ts-28-  tenantId: string;
src\modules\extraction\domain\textract-job.ts-29-  itemId: string;
src\modules\extraction\domain\textract-job.ts-30-  documentId: string;
--
src\modules\extraction\application\start-extraction-run.ts-21-export interface StartExtractionRunInput {
src\modules\extraction\application\start-extraction-run.ts-22-  tenantId: string;
src\modules\extraction\application\start-extraction-run.ts-23-  itemId: string;
src\modules\extraction\application\start-extraction-run.ts-24-  documentId: string;
src\modules\extraction\application\start-extraction-run.ts:25:  cleanObject: { bucket: string; key: string; versionId: string };
src\modules\extraction\application\start-extraction-run.ts-26-}
src\modules\extraction\application\start-extraction-run.ts-27-
src\modules\extraction\application\start-extraction-run.ts-28-export type StartExtractionRunOutcome = "STARTED" | "ALREADY_RUNNING" | "DOCUMENT_NOT_FOUND";
src\modules\extraction\application\start-extraction-run.ts-29-
--
src\modules\extraction\application\start-extraction-run.ts-51-  const now = deps.now?.() ?? new Date().toISOString();
src\modules\extraction\application\start-extraction-run.ts-52-
src\modules\extraction\application\start-extraction-run.ts-53-  const run: ExtractionRun = {
src\modules\extraction\application\start-extraction-run.ts-54-    ...extractionRunKey(input.tenantId, input.documentId, runId),
src\modules\extraction\application\start-extraction-run.ts:55:    entityType: "ExtractionRun",
src\modules\extraction\application\start-extraction-run.ts-56-    tenantId: input.tenantId,
src\modules\extraction\application\start-extraction-run.ts-57-    itemId: input.itemId,
src\modules\extraction\application\start-extraction-run.ts-58-    documentId: input.documentId,
src\modules\extraction\application\start-extraction-run.ts-59-    documentVersion,
--
src\modules\extraction\application\start-extraction-run.ts-85-      documentId: input.documentId,
src\modules\extraction\application\start-extraction-run.ts-86-      documentVersion,
src\modules\extraction\application\start-extraction-run.ts-87-      runId,
src\modules\extraction\application\start-extraction-run.ts-88-      pipelineVersion,
src\modules\extraction\application\start-extraction-run.ts:89:      cleanObject: input.cleanObject,
src\modules\extraction\application\start-extraction-run.ts-90-      // RunTextract's classifier needs these (see ExtractionExecutionInput's doc comment) -
src\modules\extraction\application\start-extraction-run.ts-91-      // `doc` was already fetched above, no extra read.
src\modules\extraction\application\start-extraction-run.ts-92-      fileName: doc.fileName,
src\modules\extraction\application\start-extraction-run.ts-93-      contentType: doc.mediaType,
--
src\modules\notification\application\ses-callback-workflow.ts-65-  const now = deps.now();
src\modules\notification\application\ses-callback-workflow.ts-66-  const inboxKey = webhookInboxKey(tenantId, deps.providerAccountId, event.snsMessageId);
src\modules\notification\application\ses-callback-workflow.ts-67-  const inboxCreated = await deps.store.putIfAbsent({
src\modules\notification\application\ses-callback-workflow.ts-68-    ...inboxKey,
src\modules\notification\application\ses-callback-workflow.ts:69:    entityType: "WebhookInbox",
src\modules\notification\application\ses-callback-workflow.ts-70-    tenantId,
src\modules\notification\application\ses-callback-workflow.ts-71-    provider: "SES",
src\modules\notification\application\ses-callback-workflow.ts-72-    providerAccountId: deps.providerAccountId,
src\modules\notification\application\ses-callback-workflow.ts-73-    providerEventId: event.snsMessageId,
--
src\modules\extraction\application\run-extraction-validation.ts-252-  const fields: ExtractedField[] = compared.map((cf) => {
src\modules\extraction\application\run-extraction-validation.ts-253-    const outcome = decideFieldOutcome({ agreement: cf.agreement, sources: cf.sources, candidateValue: cf.candidateValue, confidence: cf.confidence });
src\modules\extraction\application\run-extraction-validation.ts-254-    return {
src\modules\extraction\application\run-extraction-validation.ts-255-      ...extractedFieldKey(input.tenantId, input.documentId, cf.fieldName, input.runId),
src\modules\extraction\application\run-extraction-validation.ts:256:      entityType: "ExtractedField",
src\modules\extraction\application\run-extraction-validation.ts-257-      tenantId: input.tenantId,
src\modules\extraction\application\run-extraction-validation.ts-258-      documentId: input.documentId,
src\modules\extraction\application\run-extraction-validation.ts-259-      runId: input.runId,
src\modules\extraction\application\run-extraction-validation.ts-260-      fieldName: cf.fieldName,
--
src\modules\extraction\application\run-extraction-validation.ts-299-  const schema = getFieldSchema(input.pipelineVersion);
src\modules\extraction\application\run-extraction-validation.ts-300-
src\modules\extraction\application\run-extraction-validation.ts-301-  const fields: ExtractedField[] = schema.map((field) => ({
src\modules\extraction\application\run-extraction-validation.ts-302-    ...extractedFieldKey(input.tenantId, input.documentId, field.fieldName, input.runId),
src\modules\extraction\application\run-extraction-validation.ts:303:    entityType: "ExtractedField",
src\modules\extraction\application\run-extraction-validation.ts-304-    tenantId: input.tenantId,
src\modules\extraction\application\run-extraction-validation.ts-305-    documentId: input.documentId,
src\modules\extraction\application\run-extraction-validation.ts-306-    runId: input.runId,
src\modules\extraction\application\run-extraction-validation.ts-307-    fieldName: field.fieldName,
--
src\modules\extraction\domain\extraction-run.ts-15-
src\modules\extraction\domain\extraction-run.ts-16-export type ExtractionRunStatus = "RUNNING" | "COMPLETED" | "FAILED" | "DISCARDED";
src\modules\extraction\domain\extraction-run.ts-17-
src\modules\extraction\domain\extraction-run.ts-18-export interface ExtractionRun extends EntityKey {
src\modules\extraction\domain\extraction-run.ts:19:  entityType: "ExtractionRun";
src\modules\extraction\domain\extraction-run.ts-20-  tenantId: string;
src\modules\extraction\domain\extraction-run.ts-21-  itemId: string;
src\modules\extraction\domain\extraction-run.ts-22-  documentId: string;
src\modules\extraction\domain\extraction-run.ts-23-  documentVersion: number;
--
src\modules\extraction\domain\extracted-field.ts-24-
src\modules\extraction\domain\extracted-field.ts-25-export type ExtractedFieldState = "PENDING_CONFIRMATION" | "CONFIRMED" | "REJECTED";
src\modules\extraction\domain\extracted-field.ts-26-
src\modules\extraction\domain\extracted-field.ts-27-export interface ExtractedField extends EntityKey {
src\modules\extraction\domain\extracted-field.ts:28:  entityType: "ExtractedField";
src\modules\extraction\domain\extracted-field.ts-29-  tenantId: string;
src\modules\extraction\domain\extracted-field.ts-30-  documentId: string;
src\modules\extraction\domain\extracted-field.ts-31-  runId: string;
src\modules\extraction\domain\extracted-field.ts-32-  fieldName: string;
--
src\modules\notification\domain\notification-entitlements.ts-9-import type { EntityKey } from "../../../shared/dynamodb/occ.js";
src\modules\notification\domain\notification-entitlements.ts-10-
src\modules\notification\domain\notification-entitlements.ts-11-export interface NotificationEntitlements extends EntityKey {
src\modules\notification\domain\notification-entitlements.ts-12-  SK: "ENTITLEMENTS";
src\modules\notification\domain\notification-entitlements.ts:13:  entityType: "NotificationEntitlements";
src\modules\notification\domain\notification-entitlements.ts-14-  tenantId: string;
src\modules\notification\domain\notification-entitlements.ts-15-  email: { enabled: boolean; monthlyLimit?: number };
src\modules\notification\domain\notification-entitlements.ts-16-  whatsapp: { enabled: boolean };
src\modules\notification\domain\notification-entitlements.ts-17-  planVersion: number;

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content src/modules/document/domain/document.ts; rg -n -C 3 "interface UploadEvidence|interface MalwareEvidence|object:" src/modules/document; rg -n -C 5 "DocumentPurgeReceipt|receipt|purgeStatus|deleteObjectVersion|quarantineObject" docs/architecture/reviews/w3-06-user-document-purge-design/*.md' in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-08-28T17:50:21.413282Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.3 seconds
Output:
/**
 * Document â€” data-model.md line 34: PK=`TENANT#t#ITEM#i`, SK=`DOC#d`. State set matches the
 * approved data model exactly: PENDING_UPLOAD/SCANNING/CLEAN/REJECTED/UNSUPPORTED/TIMEOUT/
 * DELETED (implementation-blueprint.md Â§12.2-12.3; TIMEOUT is driven by
 * UploadSlotReconciliationWorker for scans that never resolve, not by the malware/finalizer
 * workers directly). M6 design Â§2.1 adds uploadEvidence/malwareEvidence (independent evidence
 * fields, never presuming which arrives first - see document-state-machine.ts) and
 * retentionClass/purgeAfter (privacy-lgpd.md Â§4 USER_DOCUMENT class materialized on the entity
 * from day one, not bolted on later).
 */
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import type { DocumentObjectReference } from "./document-object-reference.js";
import type { MalwareEvidence } from "./malware-scan-result.js";
import type { DocumentRetentionClass } from "./retention.js";

export type DocumentStatus = "PENDING_UPLOAD" | "SCANNING" | "CLEAN" | "REJECTED" | "UNSUPPORTED" | "TIMEOUT" | "DELETED";

export interface UploadEvidence {
  object: DocumentObjectReference;
  contentLength: number;
  mediaType: string;
  checksumSha256: string;
  /** Whether the observed object matched what was declared at reservation time AND (for PDFs)
   * passed the sandbox parser's structural validation. `advanceAfterEvidence()` reads THIS
   * field, never just "evidence is present" - presence alone would wrongly treat an invalid
   * upload as valid (real bug caught during implementation, see advance-after-evidence.ts). */
  valid: boolean;
  observedAt: string;
}

export interface Document extends EntityKey {
  entityType: "Document";
  tenantId: string;
  itemId: string;
  documentId: string;
  uploadSlotId: string;
  fileName: string;
  mediaType: string;
  contentLength: number;
  checksumSha256: string;
  status: DocumentStatus;
  quarantineObject: DocumentObjectReference;
  /** Only present once promoted - never populated before status === "CLEAN". */
  cleanObject?: DocumentObjectReference;
  uploadEvidence?: UploadEvidence;
  malwareEvidence?: MalwareEvidence;
  extractionRunId?: string; // M7, unused by M6, kept for schema forward-compatibility only.
  retentionClass: Extract<DocumentRetentionClass, "USER_DOCUMENT">;
  purgeAfter?: string; // set only once a terminal/deletion event establishes the clock.
  version: number;
  createdAt: string;
  updatedAt: string;
  deletedAt?: string;
}

export function documentKey(tenantId: string, itemId: string, documentId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `DOC#${documentId}` };
}
src/modules/document\domain\document.ts-15-
src/modules/document\domain\document.ts-16-export type DocumentStatus = "PENDING_UPLOAD" | "SCANNING" | "CLEAN" | "REJECTED" | "UNSUPPORTED" | "TIMEOUT" | "DELETED";
src/modules/document\domain\document.ts-17-
src/modules/document\domain\document.ts:18:export interface UploadEvidence {
src/modules/document\domain\document.ts:19:  object: DocumentObjectReference;
src/modules/document\domain\document.ts-20-  contentLength: number;
src/modules/document\domain\document.ts-21-  mediaType: string;
src/modules/document\domain\document.ts-22-  checksumSha256: string;
--
src/modules/document\domain\malware-scan-result.ts-11-
src/modules/document\domain\malware-scan-result.ts-12-export type GuardDutyScanStatus = "NO_THREATS_FOUND" | "THREATS_FOUND" | "UNSUPPORTED" | "ACCESS_DENIED" | "FAILED";
src/modules/document\domain\malware-scan-result.ts-13-
src/modules/document\domain\malware-scan-result.ts:14:export interface MalwareEvidence {
src/modules/document\domain\malware-scan-result.ts:15:  object: DocumentObjectReference;
src/modules/document\domain\malware-scan-result.ts-16-  status: GuardDutyScanStatus;
src/modules/document\domain\malware-scan-result.ts-17-  scanResultId: string;
src/modules/document\domain\malware-scan-result.ts-18-  observedAt: string;
rg: docs/architecture/reviews/w3-06-user-document-purge-design/*.md: IO error for operation on docs/architecture/reviews/w3-06-user-document-purge-design/*.md: A sintaxe do nome do arquivo, do nome do diretório ou do rótulo do volume está incorreta. (os error 123)

 exited 1 in 291ms:
/**
 * Document â€” data-model.md line 34: PK=`TENANT#t#ITEM#i`, SK=`DOC#d`. State set matches the
 * approved data model exactly: PENDING_UPLOAD/SCANNING/CLEAN/REJECTED/UNSUPPORTED/TIMEOUT/
 * DELETED (implementation-blueprint.md Â§12.2-12.3; TIMEOUT is driven by
 * UploadSlotReconciliationWorker for scans that never resolve, not by the malware/finalizer
 * workers directly). M6 design Â§2.1 adds uploadEvidence/malwareEvidence (independent evidence
 * fields, never presuming which arrives first - see document-state-machine.ts) and
 * retentionClass/purgeAfter (privacy-lgpd.md Â§4 USER_DOCUMENT class materialized on the entity
 * from day one, not bolted on later).
 */
import type { EntityKey } from "../../../shared/dynamodb/occ.js";
import type { DocumentObjectReference } from "./document-object-reference.js";
import type { MalwareEvidence } from "./malware-scan-result.js";
import type { DocumentRetentionClass } from "./retention.js";

export type DocumentStatus = "PENDING_UPLOAD" | "SCANNING" | "CLEAN" | "REJECTED" | "UNSUPPORTED" | "TIMEOUT" | "DELETED";

export interface UploadEvidence {
  object: DocumentObjectReference;
  contentLength: number;
  mediaType: string;
  checksumSha256: string;
  /** Whether the observed object matched what was declared at reservation time AND (for PDFs)
   * passed the sandbox parser's structural validation. `advanceAfterEvidence()` reads THIS
   * field, never just "evidence is present" - presence alone would wrongly treat an invalid
   * upload as valid (real bug caught during implementation, see advance-after-evidence.ts). */
  valid: boolean;
  observedAt: string;
}

export interface Document extends EntityKey {
  entityType: "Document";
  tenantId: string;
  itemId: string;
  documentId: string;
  uploadSlotId: string;
  fileName: string;
  mediaType: string;
  contentLength: number;
  checksumSha256: string;
  status: DocumentStatus;
  quarantineObject: DocumentObjectReference;
  /** Only present once promoted - never populated before status === "CLEAN". */
  cleanObject?: DocumentObjectReference;
  uploadEvidence?: UploadEvidence;
  malwareEvidence?: MalwareEvidence;
  extractionRunId?: string; // M7, unused by M6, kept for schema forward-compatibility only.
  retentionClass: Extract<DocumentRetentionClass, "USER_DOCUMENT">;
  purgeAfter?: string; // set only once a terminal/deletion event establishes the clock.
  version: number;
  createdAt: string;
  updatedAt: string;
  deletedAt?: string;
}

export function documentKey(tenantId: string, itemId: string, documentId: string): EntityKey {
  return { PK: `TENANT#${tenantId}#ITEM#${itemId}`, SK: `DOC#${documentId}` };
}
src/modules/document\domain\document.ts-15-
src/modules/document\domain\document.ts-16-export type DocumentStatus = "PENDING_UPLOAD" | "SCANNING" | "CLEAN" | "REJECTED" | "UNSUPPORTED" | "TIMEOUT" | "DELETED";
src/modules/document\domain\document.ts-17-
src/modules/document\domain\document.ts:18:export interface UploadEvidence {
src/modules/document\domain\document.ts:19:  object: DocumentObjectReference;
src/modules/document\domain\document.ts-20-  contentLength: number;
src/modules/document\domain\document.ts-21-  mediaType: string;
src/modules/document\domain\document.ts-22-  checksumSha256: string;
--
src/modules/document\domain\malware-scan-result.ts-11-
src/modules/document\domain\malware-scan-result.ts-12-export type GuardDutyScanStatus = "NO_THREATS_FOUND" | "THREATS_FOUND" | "UNSUPPORTED" | "ACCESS_DENIED" | "FAILED";
src/modules/document\domain\malware-scan-result.ts-13-
src/modules/document\domain\malware-scan-result.ts:14:export interface MalwareEvidence {
src/modules/document\domain\malware-scan-result.ts:15:  object: DocumentObjectReference;
src/modules/document\domain\malware-scan-result.ts-16-  status: GuardDutyScanStatus;
src/modules/document\domain\malware-scan-result.ts-17-  scanResultId: string;
src/modules/document\domain\malware-scan-result.ts-18-  observedAt: string;
rg: docs/architecture/reviews/w3-06-user-document-purge-design/*.md: IO error for operation on docs/architecture/reviews/w3-06-user-document-purge-design/*.md: A sintaxe do nome do arquivo, do nome do diretório ou do rótulo do volume está incorreta. (os error 123)

codex
## Achados bloqueantes

1. **A corrida de `legalHold` pós-claim continua aberta.** O OCC impede apenas dois commits concorrentes sobre a mesma versão. Ele não impede esta sequência válida:

   1. Purge claim lê versão `N` e commita `N+1`.
   2. Setter de hold lê a versão atual `N+1`.
   3. Setter commita `legalHold=true` como `N+2`.
   4. Worker, que já possui o claim, apaga o objeto S3.

   Portanto, o argumento de “exclusão mútua por construção” só cobre um setter que tenha lido antes do claim. Para fechar estruturalmente a corrida, toda escrita de hold precisa condicionar também que o documento não esteja `PURGE_CLAIMED`, ou a etapa imediatamente anterior ao delete S3 precisa revalidar/fixar o hold por um protocolo que impeça sua criação posterior. O OCC obrigatório de [AGENTS.md](C:/Users/Usuario/Desktop/projects/expiration-tracker/AGENTS.md) não fornece sozinho essa propriedade.

2. **O exemplo concreto de `extraConditions` continua inexequível como escrito.** [occ.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/dynamodb/occ.ts:39) já reserva `:now` para atualizar `updatedAt`. A proposta declara que colisões devem lançar erro, mas o próprio claim fornece `{ values: { ":now": nowIso } }` em `purgeAfter <= :now`. Logo, a chamada necessariamente falharia. Deve usar outro placeholder, por exemplo `:purgeCutoff`, ou definir explicitamente uma forma segura de reutilização com igualdade comprovada. Fora dessa colisão, a extensão aditiva é estruturalmente implementável.

## Achados não-bloqueantes

- Confirmado em [document-deletion-service.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/application/document-deletion-service.ts:47): o soft-delete altera somente `status`, `deletedAt` e `purgeAfter`. `cleanObject`, `uploadEvidence` e `malwareEvidence` sobrevivem. Os dois tipos de evidência realmente contêm `object`, conforme [document.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/domain/document.ts:18) e `malware-scan-result.ts`.

- O uso de `entityType` como discriminador é válido no design: [data-model.md](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/data-model.md:21) o define entre os atributos comuns, e `Document` materializa `entityType: "Document"`. Entretanto, o texto diz “design-target”; portanto, “já presente em toda linha, incluindo `DocumentPurgeReceipt`” não está confirmado pelo código real — o receipt ainda é uma entidade proposta. Sua implementação/schema deve tornar `entityType: "DocumentPurgeReceipt"` obrigatório e testar ambos os branches.

- A resolução do estado terminal `STUCK`, com remoção dos ponteiros GSI6 e alarme dedicado, fecha adequadamente o achado anterior sobre falhas repetidas.

## Nota: 8,3/10

## Veredito

**NÃO APROVADO.** O gate de 9,0 não foi atingido. Permanecem dois bloqueios concretos: a falha lógica no fechamento do hold pós-claim e a colisão inevitável de `:now` no exemplo normativo do builder.
tokens used
30.173
## Achados bloqueantes

1. **A corrida de `legalHold` pós-claim continua aberta.** O OCC impede apenas dois commits concorrentes sobre a mesma versão. Ele não impede esta sequência válida:

   1. Purge claim lê versão `N` e commita `N+1`.
   2. Setter de hold lê a versão atual `N+1`.
   3. Setter commita `legalHold=true` como `N+2`.
   4. Worker, que já possui o claim, apaga o objeto S3.

   Portanto, o argumento de “exclusão mútua por construção” só cobre um setter que tenha lido antes do claim. Para fechar estruturalmente a corrida, toda escrita de hold precisa condicionar também que o documento não esteja `PURGE_CLAIMED`, ou a etapa imediatamente anterior ao delete S3 precisa revalidar/fixar o hold por um protocolo que impeça sua criação posterior. O OCC obrigatório de [AGENTS.md](C:/Users/Usuario/Desktop/projects/expiration-tracker/AGENTS.md) não fornece sozinho essa propriedade.

2. **O exemplo concreto de `extraConditions` continua inexequível como escrito.** [occ.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/shared/dynamodb/occ.ts:39) já reserva `:now` para atualizar `updatedAt`. A proposta declara que colisões devem lançar erro, mas o próprio claim fornece `{ values: { ":now": nowIso } }` em `purgeAfter <= :now`. Logo, a chamada necessariamente falharia. Deve usar outro placeholder, por exemplo `:purgeCutoff`, ou definir explicitamente uma forma segura de reutilização com igualdade comprovada. Fora dessa colisão, a extensão aditiva é estruturalmente implementável.

## Achados não-bloqueantes

- Confirmado em [document-deletion-service.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/application/document-deletion-service.ts:47): o soft-delete altera somente `status`, `deletedAt` e `purgeAfter`. `cleanObject`, `uploadEvidence` e `malwareEvidence` sobrevivem. Os dois tipos de evidência realmente contêm `object`, conforme [document.ts](C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document/domain/document.ts:18) e `malware-scan-result.ts`.

- O uso de `entityType` como discriminador é válido no design: [data-model.md](C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/data-model.md:21) o define entre os atributos comuns, e `Document` materializa `entityType: "Document"`. Entretanto, o texto diz “design-target”; portanto, “já presente em toda linha, incluindo `DocumentPurgeReceipt`” não está confirmado pelo código real — o receipt ainda é uma entidade proposta. Sua implementação/schema deve tornar `entityType: "DocumentPurgeReceipt"` obrigatório e testar ambos os branches.

- A resolução do estado terminal `STUCK`, com remoção dos ponteiros GSI6 e alarme dedicado, fecha adequadamente o achado anterior sobre falhas repetidas.

## Nota: 8,3/10

## Veredito

**NÃO APROVADO.** O gate de 9,0 não foi atingido. Permanecem dois bloqueios concretos: a falha lógica no fechamento do hold pós-claim e a colisão inevitável de `:now` no exemplo normativo do builder.
