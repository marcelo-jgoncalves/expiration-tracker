# Multi-User B2B — Wave tracker

> Registro vivo de backlog (mesmo papel de `docs/engineering/pilot-readiness-program.md` para o Pilot Readiness Program) — DONE/IN PROGRESS/BLOCKED/NOT STARTED por wave, atualizado a cada marco. Não normativo sobre arquitetura/design (isso é `roadmap-evolution/17-multi-user-b2b-revised-strategy.md`) — só rastreia execução. Serve também como "todo list" persistida da iniciativa, já que o ambiente desta sessão não tem uma ferramenta de todo list de sessão — persistir em arquivo é preferível de qualquer forma (sobrevive a reset de contexto, mesma convenção de `decisions-log.md`/`session-log.md`).

Cada wave é avaliada contra `docs/engineering/definition-of-done.md` (E-012) antes de virar `DONE` — decompor em subitens quando a wave cruzar mais de uma camada/nível de risco, nunca fechar a wave inteira com uma única linha de evidência genérica.

| Wave | Nome | Status | Nota |
|---|---|---|---|
| B2B-0 | Current Truth + Inventory (read-only) | **DONE** (2026-08-29) | `docs/architecture/multi-user-b2b-wave-b2b0-inventory.md` |
| B2B-1 | Type 1 Design — physical model | **DONE** (2026-08-30, D-086) | `docs/architecture/multi-user-b2b-physical-model.md` — protocolo Claude↔Codex 5 rodadas (6,7/8,7 → 8,1/8,9 → 8,4/9,2 → 8,6/9,4 → 9,3/9,5), evidência em `reviews/multi-user-b2b-physical-model/` |
| B2B-2 | Global Identity Foundation | **DONE** (2026-08-30, D-087) | `User` global aditivo + bootstrap de identidade unificado entre os 2 caminhos de login (§1-3 do physical model, faseado — ver nota de sequenciamento abaixo). `IdentityMapping` continua com `tenantId` por enquanto (remoção real é Wave B2B-5, junto do cutover de `RequestContext`) — decisão consciente, não pendência esquecida |
| B2B-3 | Organization + Membership | **DONE** (2026-08-30, D-089/D-090/D-091) | `Organization`/`Membership`/GSI4 `MembershipByUser`/`ownerCount` (§4-6/8 do physical model), incluindo precondição de infra (GSI4 IAM), domain/persistence, e `CreateOrganizationService` — ver subitens abaixo. Não wireado a nenhum fluxo de login/onboarding ainda (capability isolada até Wave B2B-4) |
| B2B-4 | Onboarding | **NOT STARTED (unblocked)** | Remover tenant auto-provision silencioso, `roadmap-evolution/17` §109. Escopo ainda a definir com o Marcelo antes de codificar — ver nota abaixo |
| B2B-5 | RequestContext Cutover | NOT STARTED | Bloqueado por B2B-2/B2B-3 |
| B2B-6 | BFF Organization Context | NOT STARTED | Bloqueado por B2B-5; muda semântica que W3-07 assume (`roadmap-evolution/17` §125.4) — avaliar sequenciamento com a decisão do orquestrador do purge W3-07 (ver `NEXT_SESSION_PROMPT.md` gates) |
| B2B-7 | RBAC | NOT STARTED | Bloqueado por B2B-3 |
| B2B-8 | Invitations / Team | NOT STARTED | Bloqueado por B2B-7 |
| B2B-9 | W3-07 / Privacy Reconciliation | NOT STARTED | Usar `docs/architecture/w3-07-writer-inventory.md` como base, não re-derivar (per achado 125.4 do roadmap doc) |
| B2B-10 | Tenant-aware Frontend | NOT STARTED | Bloqueado por B2B-6; B2B-0 confirmou isolamento de cache hoje é zero (green-field, não modificação) |
| B2B-11 | Responsibility + Notifications | NOT STARTED | Bloqueado por B2B-7 |
| B2B-12 | Cutover de dev | NOT STARTED | Decisão de reset/reseed vs. migração one-shot, ver `roadmap-evolution/17` §62-68 |
| B2B-13 | E2E / Adversarial Security | NOT STARTED | Usa as 25 perguntas de §121 como checklist |
| B2B-14 | Operational Evidence | NOT STARTED | Evidência real contra `dev` via `aws --profile claude-dev` |
| B2B-15 | Documentation Reconciliation | NOT STARTED | Checklist de `AGENTS.md` §6 |

## Subitens de B2B-2 (decompostos per DoD E-012 — cada um gated antes do próximo)

**Nota de sequenciamento (judgment call nível 3-4, `change-risk-scale.md` — não é mudança do design aprovado em D-086, só de faseamento de rollout, documentado aqui em vez de reabrir o protocolo Claude↔Codex)**: `physical-model.md` §3 descreve o `bootstrapUser()` de ESTADO FINAL (2 itens, sem `TenantLifecycleRecord`) — implementá-lo literalmente agora deixaria todo novo usuário sem nenhum espaço de tenant, quebrando toda funcionalidade tenant-scoped existente, porque `Organization`/onboarding (B2B-3/B2B-4) ainda não existem para assumir a criação de tenant. O próprio roadmap já separa "Global Identity Foundation" (B2B-2) de "RequestContext Cutover... eliminar fallback tenantId=userId" (B2B-5) em waves distintas — B2B-2 nesta sessão implementa a fundação de forma ADITIVA: unifica os dois caminhos de login no `TenantBootstrapService` já existente e já fenced (fechando o gap real do BFF achado na Wave B2B-0), preservando a criação de `TenantLifecycleRecord`/tenant legado até B2B-4/B2B-5 poderem assumir com segurança.

| Subitem | Camada | Status |
|---|---|---|
| B2B-2.1 + B2B-2.2 | Domain/Persistence — entidade `User` global aditiva (`GlobalUserRepository`, `PK=USER#<userId>`, `SK=PROFILE`, `entityType: "GlobalUser"` para não colidir com `UserProfile` legado), criada como 4º item em `TenantBootstrapService.createAll()`'s `TransactWriteItems` existente | **DONE** (2026-08-30) — `DoD: item=entidade User global aditiva; risco=3 (implementação direta do design D-086 §1, sem novo access pattern — reaproveita a transação já fenced existente); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (385 módulos, 0 violação), testes-alvo 67/67 PASS (2 testes novos: linha GlobalUser criada no primeiro login via resolver E via BFF com e-mail verificado preservado; 2 testes pré-existentes corrigidos — colisão real de asserção por SK `#PROFILE` compartilhado entre o `UserProfile` legado e o novo `GlobalUser`, ambos agora contados separadamente por prefixo de PK); lacunas=nenhum consumidor lê esta linha ainda (intencional — Wave B2B-3 é o primeiro consumidor real)` |
| B2B-2.3 + B2B-2.4 | Application + call sites — unificar `bootstrap-identity.ts`/`bff-auth-service.ts` no `TenantBootstrapService` já existente (em vez de uma classe `IdentityBootstrapService` nova — mais conservador, DRY) | **DONE** (2026-08-30) — `DoD: item=unificar bootstrap BFF+API; risco=3 (implementação de padrão já aprovado/fencing existente, sem novo contrato); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (384 módulos, 0 violação), suíte-alvo 51/51 PASS incl. 2 testes adversariais novos (email verificado preservado; login rejeitado para tenant DELETING — capacidade que o caminho BFF nunca teve antes); lacunas=nenhuma` |
| B2B-2.5 | Testes — suíte completa (`npm test`) para os subitens acima | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-GlobalUser; risco=2 (verificação, não decisão); evidência=npm test 1108/1108 (120 arquivos), zero regressão real; lacunas=nenhuma`. Achado real encontrado e corrigido nesta rodada de verificação (não mascarado): `test/integration/cross-tenant.test.ts`'s checagem de crossover negativo acusou corretamente a nova linha `GlobalUser` (`PK=USER#<userId>#PROFILE`) como potencial vazamento de `tenantId`, porque `tenantId=userId` sob a regra MVP vigente — corrigido com uma exceção estreita e documentada para essa forma de chave exata conhecida, sem enfraquecer a checagem para nenhuma outra entidade. Registrado em `decisions-log.md` D-088 |

## Subitens de B2B-3 (decompostos per DoD E-012 — cada um gated antes do próximo)

**Nota de escopo (judgment call nível 3-4, não reabre o protocolo Claude↔Codex de D-086)**: o mecanismo transacional de `ownerCount` do physical model §8 (decrementar ao remover/demover/suspender um `Membership` `OWNER`) é implementado nesta wave só na parte que `CreateOrganization` precisa (seed `ownerCount=1` na criação) — a parte de decremento fica sem call site real até B2B-7/B2B-8 (RBAC/Invitations) existirem operações que mudem role/status de Membership. Não é lacuna esquecida: construir o caminho de decremento agora seria código morto sem nenhum writer real para exercitá-lo, contra `principles.md` #1 (proporcionalidade). Registrado explicitamente aqui como TODO de B2B-7/B2B-8, não implementado silenciosamente incompleto.

| Subitem | Camada | Status |
|---|---|---|
| B2B-3.1 | Infra — precondição de isolamento IAM do GSI4 antes de qualquer escrita real de Membership: removido de `tenant_facing_index_names`, política restrita dedicada `gsi4_read` (mesmo padrão de `gsi3_read`/`gsi6_read`), nenhuma role ainda o consome (Wave B2B-3 não fia consumidor) | **DONE** (2026-08-30) — `DoD: item=isolamento IAM do GSI4; risco=5 (muda superfície de acesso de um GSI, change-risk-scale.md nível 5 — mas implementação direta do design já aprovado em D-086 §6, não uma decisão nova); evidência=terraform validate PASS (módulo + root), terraform test PASS via AWS_PROFILE=claude-dev (módulo: 6/6, incluindo os 2 novos runs \`tenant_facing_policy_excludes_gsi4\`/\`gsi4_narrow_policy_references_only_its_own_index\`; root: 15/15, nenhuma regressão nas 27 funções Lambda); lacunas=nenhuma role usa \`gsi4_read_policy_json\` ainda (intencional — sem consumidor real nesta wave)` |
| B2B-3.2 | Domain/Persistence — `Organization`, `Membership` (3 estados, GSI4 `MembershipByUser`), novo módulo `src/modules/organization/{domain,ports,persistence}` seguindo o layout já usado por `subject`/`identity` | **DONE** (2026-08-30) — `DoD: item=domain/persistence Organization+Membership; risco=4 (implementação direta do design D-086 §4-6, judgment call de layout de módulo reversível); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (389 módulos, 0 violação), testes-alvo 10/10 PASS (key builders + round-trip do InMemoryOrganizationStore incl. transactWrite all-or-nothing e queryGsi4 cross-org); G-V3 aplicado per E-013 (comentário de mutação nomeada em cada um dos 10 testes, não só suíte verde); lacunas=nenhuma` |
| B2B-3.3 | Application — `CreateOrganizationService` (`src/modules/organization/application/create-organization.ts`, transação atômica de 4 itens: Organization+Membership OWNER+TenantLifecycleRecord+TenantEntitlement, reaproveitando `defaultEntitlement()` já existente em `subject/domain/entitlement.ts` em vez de redefinir; novo `OrganizationIdGenerator` port, `org_`/`membership_` prefixos garantindo `organizationId != userId` por construção) | **DONE** (2026-08-30) — `DoD: item=CreateOrganizationService; risco=4 (implementação direta do design D-086 §4, judgment call reversível de não gerar retry-on-race já que cada chamada cria recurso novo com ID fresco, diferente do bootstrap de identidade); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (391 módulos, 0 violação), testes-alvo 15/15 PASS (10 de B2B-3.2 + 5 novos de CreateOrganization); G-V3 aplicado desde a escrita per E-013 (mutação nomeada em cada um dos 5 testes novos); lacunas=decremento de `ownerCount` continua fora de escopo (nota de B2B-3 acima), não wireado a nenhum fluxo de login/onboarding ainda (capability isolada até B2B-4)` |
| B2B-3.4 | Testes — suíte completa (`npm test`) para toda a Wave B2B-3 | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-CreateOrganizationService; risco=2 (verificação, não decisão); evidência=npm test 1123/1123 (122 arquivos), zero regressão; lacunas=nenhuma`. Com este subitem, **Wave B2B-3 está DONE** |

## Nota de escopo pendente — Wave B2B-4 (Onboarding)

`roadmap-evolution/17` §109 define B2B-4 como "remover tenant auto-provision silencioso; new User → Create Organization ou Accept Invitation". **`Accept Invitation` depende de `Invitation`, que é escopo formal de Wave B2B-8** (§113) — implementar aceite de convite agora seria antecipar trabalho de uma wave posterior sem a base (`Invitation`/token pointer/dedup pointer) existir ainda. Escopo real provável de B2B-4 nesta fase: só o ramo "zero Memberships → oferecer `CreateOrganization`" (checado via GSI4), sem o ramo de aceite de convite. Esta wave também é a primeira desde B2B-2 a tocar wiring de fluxo de login real (`RequestContextResolver`/`bff-auth-service.ts`) — por isso o escopo exato foi deliberadamente apresentado ao Marcelo antes de qualquer código, em vez de decidido unilateralmente como as notas de sequenciamento anteriores (B2B-2/B2B-3).

## Achados/pendências laterais abertos durante a execução (não bloqueiam waves seguintes)

- BFF login path sem `TenantLifecycleRecord`/fencing (achado da Wave B2B-0) — **RESOLVIDO** por B2B-2.3+2.4 acima.
- Decremento transacional de `ownerCount` (physical model §8) — **TODO explícito de B2B-7/B2B-8** (ver nota de escopo de B2B-3 acima), não esquecido.
