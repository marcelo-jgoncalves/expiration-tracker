# Multi-User B2B — Wave tracker

> Registro vivo de backlog (mesmo papel de `docs/engineering/pilot-readiness-program.md` para o Pilot Readiness Program) — DONE/IN PROGRESS/BLOCKED/NOT STARTED por wave, atualizado a cada marco. Não normativo sobre arquitetura/design (isso é `roadmap-evolution/17-multi-user-b2b-revised-strategy.md`) — só rastreia execução. Serve também como "todo list" persistida da iniciativa, já que o ambiente desta sessão não tem uma ferramenta de todo list de sessão — persistir em arquivo é preferível de qualquer forma (sobrevive a reset de contexto, mesma convenção de `decisions-log.md`/`session-log.md`).

Cada wave é avaliada contra `docs/engineering/definition-of-done.md` (E-012) antes de virar `DONE` — decompor em subitens quando a wave cruzar mais de uma camada/nível de risco, nunca fechar a wave inteira com uma única linha de evidência genérica.

| Wave | Nome | Status | Nota |
|---|---|---|---|
| B2B-0 | Current Truth + Inventory (read-only) | **DONE** (2026-08-29) | `docs/architecture/multi-user-b2b-wave-b2b0-inventory.md` |
| B2B-1 | Type 1 Design — physical model | **DONE** (2026-08-30, D-086) | `docs/architecture/multi-user-b2b-physical-model.md` — protocolo Claude↔Codex 5 rodadas (6,7/8,7 → 8,1/8,9 → 8,4/9,2 → 8,6/9,4 → 9,3/9,5), evidência em `reviews/multi-user-b2b-physical-model/` |
| B2B-2 | Global Identity Foundation | **DONE** (2026-08-30, D-087) | `User` global aditivo + bootstrap de identidade unificado entre os 2 caminhos de login (§1-3 do physical model, faseado — ver nota de sequenciamento abaixo). `IdentityMapping` continua com `tenantId` por enquanto (remoção real é Wave B2B-5, junto do cutover de `RequestContext`) — decisão consciente, não pendência esquecida |
| B2B-3 | Organization + Membership | **DONE** (2026-08-30, D-089/D-090/D-091) | `Organization`/`Membership`/GSI4 `MembershipByUser`/`ownerCount` (§4-6/8 do physical model), incluindo precondição de infra (GSI4 IAM), domain/persistence, e `CreateOrganizationService` — ver subitens abaixo. Não wireado a nenhum fluxo de login/onboarding ainda (capability isolada até Wave B2B-4) |
| B2B-4 | Onboarding | **DONE** (2026-08-30, D-094) | `OnboardingStateResolver` (`src/modules/organization/application/onboarding-state.ts`) implementado exatamente per escopo `APPROVED` D-092 — ver subitem abaixo e `docs/architecture/multi-user-b2b-wave-b2b4-scope.md` |
| B2B-5 | RequestContext Cutover | **DONE** (2026-08-30, D-096) | `IdentityMapping` tenantless + `bootstrapUser()` 2 itens + `RequestContextResolver` reescrito + cap transacional em `POST /bff/organizations` — ver subitens abaixo e `docs/architecture/multi-user-b2b-wave-b2b5-scope.md` |
| B2B-6 | BFF Organization Context | **DONE** (2026-08-30, D-102) | `X-Organization-Id` (BFF-derived, nunca do browser) + `resolveWorkingOrganization()`/`OrganizationUnavailableError`/`OrganizationSelectionRequiredError` fecham o `InternalError` 500 que B2B-8 tornou explorável + `POST /bff/organization/select`/`GET /bff/organizations` — ver `docs/architecture/multi-user-b2b-wave-b2b6-scope.md` e subitens abaixo. Terceira aplicação real de `docs/engineering/research-protocol.md`/E-014 |
| B2B-7 | RBAC | **DONE** (2026-08-30, D-098) | `Role` ganha `ADMIN`, `notification:configure`/`tenant:configure-document-request-delivery` reclassificados, `resolveRoles()` aceita os 4 valores reais — ver `docs/architecture/multi-user-b2b-wave-b2b7-scope.md` e subitens abaixo. Primeira aplicação real de `docs/engineering/research-protocol.md` (E-014) |
| B2B-8 | Invitations / Team | **DONE** (2026-08-30, D-100) | `Invitation`/token pointer/dedup + `Accept`/`Revoke`/`ChangeRole`/`Remove`/`Leave`/`ListMembers`/`ListInvitations` + HTTP/Terraform reais — ver `docs/architecture/multi-user-b2b-wave-b2b8-scope.md` e subitens abaixo. Segunda aplicação real de `docs/engineering/research-protocol.md`/E-014 |
| B2B-9 | W3-07 / Privacy Reconciliation | **DONE** (2026-08-30, D-104) | Fix real de purga: `InvitationTokenPointer` (declara `organizationId`, não `tenantId`) ganha 3ª cláusula OR no scan/`PURGE_DELETE` — fechava órfão pós-exclusão de Organization. `privacy-lgpd.md` §4.1/§4.2 novos (User-level vs. Organization-level erasure, 3 fontes externas, invariante de último OWNER documentada) + 4 linhas de retenção novas. `GlobalUser`/session self-heal (B2B-6) confirmados por teste adversarial contra o estado terminal `DELETED`. Quarta aplicação real de `docs/engineering/research-protocol.md`/E-014 |
| B2B-10 | Tenant-aware Frontend | **DONE** (2026-08-30, D-106) | Fix de regressão real (`AuthContext` tratava todo usuário autenticado como deslogado desde B2B-5). Arquitetura de cache isolation (query keys escopadas por `organizationId` + `ActiveOrganizationProvider` com gate `switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta) fecha uma corrida real de vazamento de dado entre tenants na UI. Switcher/Members/Settings novos + `UpdateOrganizationSettingsService` (writer novo, `organization:update-settings`/`OWNER_ROLES`). Primeira aplicação do protocolo Claude↔Codex a uma decisão de frontend nesta sessão — ver `docs/architecture/multi-user-b2b-wave-b2b10-scope.md` |
| B2B-11 | Responsibility + Notifications | **DONE** (2026-08-30, D-108) | `NotificationRecipientResolver`/`resolveCandidateUserId` migrados de `UserProfile` (vestigial) para `Membership`+`GlobalUser` (2 condições, mesma regra dupla de `resolve-request-context.ts`); `assigneeUserId`/`ItemWatch.userId` agora validados contra `Membership` real (`MemberEligibilityChecker`, novo port). **Nota de escopo**: a supersessão de `UserProfile.requesterDisplayName` por `Organization.displayName` (GTR-01, mencionada como previsão em B2B-5/D-096) NÃO foi decidida nem implementada nesta wave — o escopo real, definido e `APPROVED` via protocolo Claude↔Codex (`multi-user-b2b-wave-b2b11-scope.md`), não incluiu esse item; continua pendente, ver seção de achados abaixo |
| B2B-12 | Cutover de dev | **DONE** (2026-08-30, D-111) — execução real destrutiva à parte, gated | `docs/architecture/multi-user-b2b-wave-b2b12-scope.md` — protocolo Claude↔Codex 3 rodadas (Claude 9,2/Codex 9,1), `scripts/reset-dev-data.ts` implementado e verificado (Fase A real rodada contra `dev`) + `LEGACY_TENANT_ONLY` removido; `--confirm`/`--include-cognito` reais aguardam confirmação explícita do Marcelo |
| B2B-13 | E2E / Adversarial Security | **DONE** (2026-08-30, D-113) | `docs/architecture/multi-user-b2b-wave-b2b13-scope.md` — protocolo Claude↔Codex 3 rodadas (Claude 9,1/Codex 9,2), fix real de TOCTOU (notification+document-chasing) implementado e testado, 2 testes novos (revogação/roles), auditoria de fixture IDs sem achado |
| B2B-14 | Operational Evidence | **IN PROGRESS** (2026-08-30, D-114 a D-119) | 6 achados reais severos corrigidos, cada um encontrado só por tentar o fluxo de verdade contra `dev`: (1) `app_origin` nunca saiu do placeholder, OAuth nunca funcionou (D-114); (2) `bff-handler` crashava em TODA invocação desde B2B-8/D-100, `GUEST_TOKEN_PEPPER` nunca wireado (D-115); (3) `gsi4_read_policy_json` nunca anexado aos 10 Lambdas reais que o consomem (D-116); (4) `GET /bff/organizations`/`POST /bff/organization/select` (B2B-6/D-102) nunca tiveram rota real no API Gateway (D-117); (5) nenhuma tela de "criar organização" existe no frontend — `roadmap-evolution/17` §36 já previa, B2B-10 nunca implementou — toda tela ficava presa em "Carregando…" para sempre para um usuário sem Organization (D-118, feature nova: `Onboarding.tsx`+`OnboardingGate.tsx`); (6) `ExpressionAttributeNames: {}` explícito quebrava `TransactWriteItems` real com `ValidationException` em 3 arquivos (`update-organization-settings.ts`, `owner-count-guard.ts` — o guard de última-proteção-de-OWNER, o mais severo — e `accept-invitation.ts`), nunca pego por nenhum fake em memória até agora (D-119, fake endurecido para pegar a classe inteira daqui pra frente). Cada achado só existe porque nenhuma sessão anterior tinha exercitado o backend/frontend reais juntos — exatamente o propósito desta wave. Pendente: Marcelo reexercitar o save de Settings em `dev` após o merge/CD deste D-119, depois continuar o roteiro manual (convite/2º membro, troca de role, switch de organização, revogação, exclusão de organização) |
| B2B-15 | Documentation Reconciliation | NOT STARTED | Checklist de `AGENTS.md` §6 |

## Subitens de B2B-2 (decompostos per DoD E-012 — cada um gated antes do próximo)

**Nota de sequenciamento (judgment call nível 3-4, `change-risk-scale.md` — não é mudança do design aprovado em D-086, só de faseamento de rollout, documentado aqui em vez de reabrir o protocolo Claude↔Codex)**: `physical-model.md` §3 descreve o `bootstrapUser()` de ESTADO FINAL (2 itens, sem `TenantLifecycleRecord`) — implementá-lo literalmente agora deixaria todo novo usuário sem nenhum espaço de tenant, quebrando toda funcionalidade tenant-scoped existente, porque `Organization`/onboarding (B2B-3/B2B-4) ainda não existem para assumir a criação de tenant. O próprio roadmap já separa "Global Identity Foundation" (B2B-2) de "RequestContext Cutover... eliminar fallback tenantId=userId" (B2B-5) em waves distintas — B2B-2 nesta sessão implementa a fundação de forma ADITIVA: unifica os dois caminhos de login no `TenantBootstrapService` já existente e já fenced (fechando o gap real do BFF achado na Wave B2B-0), preservando a criação de `TenantLifecycleRecord`/tenant legado até B2B-4/B2B-5 poderem assumir com segurança.

| Subitem | Camada | Status |
|---|---|---|
| B2B-2.1 + B2B-2.2 | Domain/Persistence — entidade `User` global aditiva (`GlobalUserRepository`, `PK=USER#<userId>`, `SK=PROFILE`, `entityType: "GlobalUser"` para não colidir com `UserProfile` legado), criada como 4º item em `TenantBootstrapService.createAll()`'s `TransactWriteItems` existente | **DONE** (2026-08-30) — `DoD: item=entidade User global aditiva; risco=3 (implementação direta do design D-086 §1, sem novo access pattern — reaproveita a transação já fenced existente); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (385 módulos, 0 violação), testes-alvo 67/67 PASS (2 testes novos: linha GlobalUser criada no primeiro login via resolver E via BFF com e-mail verificado preservado; 2 testes pré-existentes corrigidos — colisão real de asserção por SK `#PROFILE` compartilhado entre o `UserProfile` legado e o novo `GlobalUser`, ambos agora contados separadamente por prefixo de PK); lacunas=nenhum consumidor lê esta linha ainda (intencional — Wave B2B-3 é o primeiro consumidor real)` |
| B2B-2.3 + B2B-2.4 | Application + call sites — unificar `bootstrap-identity.ts`/`bff-auth-service.ts` no `TenantBootstrapService` já existente (em vez de uma classe `IdentityBootstrapService` nova — mais conservador, DRY) | **DONE** (2026-08-30) — `DoD: item=unificar bootstrap BFF+API; risco=3 (implementação de padrão já aprovado/fencing existente, sem novo contrato); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (384 módulos, 0 violação), suíte-alvo 51/51 PASS incl. 2 testes adversariais novos (email verificado preservado; login rejeitado para tenant DELETING — capacidade que o caminho BFF nunca teve antes); lacunas=nenhuma` |
| B2B-2.5 | Testes — suíte completa (`npm test`) para os subitens acima | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-GlobalUser; risco=2 (verificação, não decisão); evidência=npm test 1108/1108 (120 arquivos), zero regressão real; lacunas=nenhuma`. Achado real encontrado e corrigido nesta rodada de verificação (não mascarado): `test/integration/cross-tenant.test.ts`'s checagem de crossover negativo acusou corretamente a nova linha `GlobalUser` (`PK=USER#<userId>#PROFILE`) como potencial vazamento de `tenantId`, porque `tenantId=userId` sob a regra MVP vigente — corrigido com uma exceção estreita e documentada para essa forma de chave exata conhecida, sem enfraquecer a checagem para nenhuma outra entidade. Registrado em `decisions-log.md` D-088 |

## Subitens de B2B-3 (decompostos per DoD E-012 — cada um gated antes do próximo)

**Nota de escopo (judgment call nível 3-4, não reabre o protocolo Claude↔Codex de D-086)**: o mecanismo transacional de `ownerCount` do physical model §8 (decrementar ao remover/demover/suspender um `Membership` `OWNER`) é implementado nesta wave só na parte que `CreateOrganization` precisa (seed `ownerCount=1` na criação) — a parte de decremento fica sem call site real até B2B-7/B2B-8 (RBAC/Invitations) existirem operações que mudem role/status de Membership. Não é lacuna esquecida: construir o caminho de decremento agora seria código morto sem nenhum writer real para exercitá-lo, contra `principles.md` #1 (proporcionalidade). Registrado explicitamente aqui como TODO de B2B-7/B2B-8, não implementado silenciosamente incompleto.

| Subitem | Camada | Status |
|---|---|---|
| B2B-3.1 | Infra — precondição de isolamento IAM do GSI4 antes de qualquer escrita real de Membership: removido de `tenant_facing_index_names`, política restrita dedicada `gsi4_read` (mesmo padrão de `gsi3_read`/`gsi6_read`), nenhuma role ainda o consome (Wave B2B-3 não fia consumidor) | **DONE** (2026-08-30) — `DoD: item=isolamento IAM do GSI4; risco=5 (muda superfície de acesso de um GSI, change-risk-scale.md nível 5 — mas implementação direta do design já aprovado em D-086 §6, não uma decisão nova); evidência=terraform validate PASS (módulo + root), terraform test PASS via AWS_PROFILE=claude-dev (módulo: 6/6, incluindo os 2 novos runs \`tenant_facing_policy_excludes_gsi4\`/\`gsi4_narrow_policy_references_only_its_own_index\`; root: 15/15, nenhuma regressão nas 27 funções Lambda); lacunas=nenhuma role usa \`gsi4_read_policy_json\` ainda (intencional — sem consumidor real nesta wave)` |
| B2B-3.2 | Domain/Persistence — `Organization`, `Membership` (3 estados, GSI4 `MembershipByUser`), novo módulo `src/modules/organization/{domain,ports,persistence}` seguindo o layout já usado por `subject`/`identity` | **DONE** (2026-08-30) — `DoD: item=domain/persistence Organization+Membership; risco=4 (implementação direta do design D-086 §4-6, judgment call de layout de módulo reversível); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (389 módulos, 0 violação), testes-alvo 10/10 PASS (key builders + round-trip do InMemoryOrganizationStore incl. transactWrite all-or-nothing e queryGsi4 cross-org); G-V3 aplicado per E-013 (comentário de mutação nomeada em cada um dos 10 testes, não só suíte verde); lacunas=nenhuma` |
| B2B-3.3 | Application — `CreateOrganizationService` (`src/modules/organization/application/create-organization.ts`, transação atômica de 4 itens: Organization+Membership OWNER+TenantLifecycleRecord+TenantEntitlement, reaproveitando `defaultEntitlement()` já existente em `subject/domain/entitlement.ts` em vez de redefinir; novo `OrganizationIdGenerator` port, `org_`/`membership_` prefixos garantindo `organizationId != userId` por construção) | **DONE** (2026-08-30) — `DoD: item=CreateOrganizationService; risco=4 (implementação direta do design D-086 §4, judgment call reversível de não gerar retry-on-race já que cada chamada cria recurso novo com ID fresco, diferente do bootstrap de identidade); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (391 módulos, 0 violação), testes-alvo 15/15 PASS (10 de B2B-3.2 + 5 novos de CreateOrganization); G-V3 aplicado desde a escrita per E-013 (mutação nomeada em cada um dos 5 testes novos); lacunas=decremento de `ownerCount` continua fora de escopo (nota de B2B-3 acima), não wireado a nenhum fluxo de login/onboarding ainda (capability isolada até B2B-4)` |
| B2B-3.4 | Testes — suíte completa (`npm test`) para toda a Wave B2B-3 | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-CreateOrganizationService; risco=2 (verificação, não decisão); evidência=npm test 1123/1123 (122 arquivos), zero regressão; lacunas=nenhuma`. Com este subitem, **Wave B2B-3 está DONE** |

## Escopo de Wave B2B-4 (Onboarding) — `APPROVED`, D-092 (ver D-093 sobre framing)

Ver `docs/architecture/multi-user-b2b-wave-b2b4-scope.md` para o registro completo — protocolo Claude↔Codex 4 rodadas (submetido a pedido explícito do Marcelo, por ser a primeira wave desde B2B-2 a potencialmente tocar wiring de login real). Resumo: `Accept Invitation` fora de escopo (Wave B2B-8); wiring em login real e remoção do auto-provision legado redesignados para Wave B2B-5 (§110 do roadmap já define isso textualmente); deliverable desta wave é só `OnboardingStateResolver` puro e não wireado (4 estados, procedimento sequencial estrito).

**Estado real**: `D-093` (`AGENTS.md` §1) esclareceu que a razão de não wireado ainda é qualidade de engenharia (decomposição, não misturar escopo B2B-4/B2B-5), não proteção de usuário/produção (que não existem neste projeto) — não invocar essa segunda categoria em análises futuras.

### Subitem de B2B-4 (implementação, D-094)

| Subitem | Camada | Status |
|---|---|---|
| B2B-4.1 | Application — `OnboardingStateResolver` puro (`src/modules/organization/application/onboarding-state.ts`), procedimento sequencial estrito de 5 passos, hidratação contra a base em vez de confiar na projeção do GSI4 (physical model §6) | **DONE** (2026-08-30) — `DoD: item=OnboardingStateResolver; risco=3 (implementação direta do escopo já APPROVED em D-092 via protocolo completo, sem novo access pattern/contrato — reaproveita queryGsi4()/tenantLifecycleKey() já existentes); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (392 módulos, 0 violação), vitest run test/unit/organization/onboarding-state.test.ts 7/7 PASS (4 estados + precedência ACTIVE-vence-tudo + REMOVED-não-vira-SUSPENDED_ONLY + hidratação-contra-GSI4-stale via StaleGsi4Store fake), G-V3 aplicado desde a escrita (mutação nomeada em comentário por teste, per E-013), suíte completa npm test 1130/1130 (123 arquivos), zero regressão; lacunas=nenhuma` |

Com este subitem, **Wave B2B-4 (Onboarding) está `DONE`**.

## Escopo de Wave B2B-5 (RequestContext Cutover) — `APPROVED`, D-095

Ver `docs/architecture/multi-user-b2b-wave-b2b5-scope.md` para o registro completo — protocolo Claude↔Codex 3 rodadas (submetido por iniciativa própria, analogia direta com D-092: primeira wave desde B2B-4 a tocar `resolve-request-context.ts`/`bootstrap-identity.ts`/`bff-auth-service.ts` de verdade). O Marcelo deu o sinal para implementar assim que o escopo foi apresentado.

### Subitens de B2B-5 (implementação, D-096 — mantido como item coeso per `definition-of-done.md` "diff atomicamente revisável": domain/persistence/application não compilam de forma independente entre si, mas cada subparte reconhecível abaixo tem sua própria evidência)

| Subitem | Camada | Status |
|---|---|---|
| B2B-5.1 | Domain — `IdentityMapping` sem `tenantId`; `TenantBootstrapService` renomeado `IdentityBootstrapService`, `bootstrapUser()` com `TransactWriteItems` de 2 itens (`GlobalUser`+`IdentityMapping`); `UserProfile`/`user-repository.ts` **mantido** (achado real corrigido antes de escrever código: `UserProfile` não é legado descartável — `profile-service.ts`/guest flows/`document-chasing-dispatch` ainda o consomem para `requesterDisplayName`, W5-01/GTR-01; supersessão real por `Organization.displayName` é Wave B2B-11, não B2B-5) | **DONE** — `DoD: item=IdentityMapping tenantless + bootstrapUser() 2 itens; risco=5 (elimina o auto-provision de tenant, o gatilho central da wave); evidência=npm run typecheck PASS, npm run lint PASS; lacunas=nenhuma` |
| B2B-5.2 | Persistence — `DeviceSession`+`logoutAll`/`logoutDevice` migrados de `UserRepository` para `GlobalUserRepository` (`PK=USER#<userId>`, inatingível por W3-07/purge por construção de chave); `globalLogoutAfter` migrado de `UserProfile` para `GlobalUser` (mudança de contrato observável já aceita no design, physical model §10: logout global agora é por usuário, não por Organization) | **DONE** — `DoD: item=DeviceSession/logoutAll user-global; risco=4 (schema move mecânico + contrato observável já aceito); evidência=npm run typecheck PASS, npm run lint PASS; lacunas=nenhuma` |
| B2B-5.3 | Application — `RequestContextResolver` reescrito: bootstrap → `OnboardingStateResolver` → só `HAS_USABLE_MEMBERSHIP` prossegue (demais lançam `OnboardingRequiredError` nomeado); resolução de Membership única explícita via `resolve-active-membership.ts` (novo helper compartilhado com a BFF, não reabre o contrato de `OnboardingStateResolver`); **achado real corrigido antes de fechar**: adicionado o check de `TenantLifecycleRecord(organizationId)` ACTIVE que o design (§11) exige mas a primeira versão do código esqueceu; assert explícito (`UnsupportedMembershipRoleError`) para `Membership.role` não suportado (`ADMIN`); `UserProfile` provisionado preguiçosamente por Organization (não mais no bootstrap) para manter o invariante de `ProfileService` | **DONE** — `DoD: item=RequestContextResolver reescrito + gate de lifecycle da Organization + assert de role; risco=5 (o gate real de login); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (393 módulos, 0 violação), vitest run test/unit/identity/resolver.test.ts 14/14 PASS (bootstrap 2-item, onboarding gate, working context, revogação, lifecycle gate, role não suportado), G-V3 aplicado desde a escrita; lacunas=nenhuma` |
| B2B-5.4 | Application/HTTP — sessão BFF identity-only (`Session.tenantId`→`activeOrganizationId?`, `handleCallback` nunca falha por falta de organização); `POST /bff/organizations` com cap transacional (`GlobalUser.hasCreatedOrganization` via `buildAttributeOnceUpdate`, novo builder tenantless em `occ.ts`, na MESMA `TransactWriteItems` de `CreateOrganizationService.buildCreateEntries()` — refatoração aditiva, `TransactionCanceledException` distinguido por índice via `getCancellationReasonCodes()`, novo em `occ.ts`); `GET /bff/session` com self-heal (`resolveSessionWithOnboarding`); rota Terraform `POST /bff/organizations` nova | **DONE** — `DoD: item=BFF onboarding flow (sessão identity-only + POST /bff/organizations com cap + GET /bff/session self-heal); risco=5 (gate real de login, cap transacional); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS, vitest run test/unit/bff/ 84/84 PASS (criação de org, cap rejeita 2ª criação com ConflictError nomeado, self-heal quando sessão desatualizada, fast-path quando já setada), G-V3 aplicado desde a escrita; terraform validate PASS (módulo bff-api-gateway + root), terraform test via AWS_PROFILE=claude-dev PASS (5/5, incl. novo run `organizations_create_route_exists_for_the_first_onboarding_action`); lacunas=nenhuma` |
| B2B-5.5 | Testes — ajuste de todos os call sites afetados (8 arquivos de teste mecanicamente desatualizados pela mudança de contrato + `resolver.test.ts`/`bff-auth-service.test.ts` reescritos para cobrir o comportamento novo) + suíte completa + `build:lambdas` | **DONE** — `DoD: item=suíte completa pós-B2B-5 + build de Lambdas; risco=2 (verificação, não decisão); evidência=npm test 1134/1134 (123 arquivos), zero regressão (+4 testes líquidos vs. B2B-4: novos testes de cap/self-heal/onboarding gate/lifecycle gate, alguns testes obsoletos do modelo pré-cutover removidos); npm run build:lambdas PASS (27/27 Lambdas, incl. bff-handler); lacunas=nenhuma` |

Com este subitem, **Wave B2B-5 (RequestContext Cutover) está `DONE`**.

### Achados reais registrados durante a implementação (não estavam no escopo `APPROVED`, corrigidos antes de fechar)

- **`UserProfile` não é legado descartável** — a proposta de escopo (D-095) dizia "remove `UserProfile`/lógica tenant-scoped legada"; verificação contra o código real antes de escrever mostrou que `profile-service.ts`/`profile-handlers.ts`/guest flows/`document-chasing-dispatch.ts`/`email-templates.ts` ainda consomem `UserProfile.requesterDisplayName` (W5-01/GTR-01). Corrigido: `UserProfile` mantido, provisionamento movido do bootstrap (que não conhece mais nenhuma Organization) para `RequestContextResolver` (lazy, por Organization). Supersessão real por `Organization.displayName` continua sendo Wave B2B-11 (physical model §121 Q21), não B2B-5.
- **Gate de `TenantLifecycleRecord(organizationId)` ACTIVE estava faltando** — o design (§11) exige que a cadeia de resolução termine checando o lifecycle da Organization, não só da Membership; a primeira versão do `RequestContextResolver` reescrito pulava esse passo. Achado por revisão própria antes de rodar os testes, não pelo Codex — adicionado e coberto por teste dedicado (`resolver.test.ts`, describe "Organization lifecycle gate").

Ambos registrados aqui por honestidade de processo (mesmo padrão de E-013) — nenhum dos dois exigiu reabrir o protocolo Claude↔Codex (correções factuais/de implementação de um escopo já aprovado, não decisões de arquitetura novas).

## Escopo de Wave B2B-7 (RBAC) — `APPROVED`, D-097

Ver `docs/architecture/multi-user-b2b-wave-b2b7-scope.md` para o registro completo — protocolo Claude↔Codex 3 rodadas, **primeira aplicação real de `docs/engineering/research-protocol.md` (E-014)**. A Rodada 1 do Codex contestou o próprio checklist de critérios de nota (não só a nota do design) — reconciliado na Rodada 2 (régua 9,1/9,1) antes da nota do design contar para o fechamento. Evidência das 3 rodadas em `reviews/multi-user-b2b-wave-b2b7-scoping/`.

### Subitem de B2B-7 (implementação, D-098 — mantido como item coeso per `definition-of-done.md` "diff atomicamente revisável": domain e application não compilam de forma independente entre si)

| Subitem | Camada | Status |
|---|---|---|
| B2B-7.1+7.2 | Domain (`authorization.ts`) — `Role` ganha `ADMIN`; `READ_ONLY_ROLES`/`WRITE_ROLES`/`ADMIN_ROLES` passam a incluir `ADMIN`; novo tier `OWNER_ROLES`; `notification:configure`→`READ_ONLY_ROLES` (bug fix real, não escolha ADMIN-vs-OWNER); `tenant:configure-document-request-delivery`→`OWNER_ROLES`; branch de bypass de ownership estendido a `ADMIN`. Application (`resolve-request-context.ts`) — `resolveRoles()` aceita os 4 valores reais de `Membership["role"]` | **DONE** (2026-08-30) — `DoD: item=Role ADMIN + reclassificação de 2 actions + resolveRoles(); risco=5 (muda o contrato da matriz de autorização real em produção, change-risk-scale.md nível 5 — mas implementação direta do escopo já APPROVED em D-097 via protocolo completo); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (393 módulos, 0 violação), testes-alvo test/unit/identity/authorization.test.ts 12/12 PASS (4 novos: ADMIN paritário em item:delete, ADMIN negado em tenant:configure-document-request-delivery, VIEWER aceito em notification:configure, ADMIN bypassa ownership) + test/unit/identity/resolver.test.ts 15/15 PASS (substituindo o teste de D-095 que esperava UnsupportedMembershipRoleError para ADMIN por um par: ADMIN resolve normalmente + fail-closed preservado para um valor fora do domínio real), G-V3 aplicado desde a escrita (mutação nomeada em comentário por teste); lacunas=nenhuma` |
| B2B-7.3 | Testes — suíte completa (`npm test`) + `build:lambdas` para toda a Wave B2B-7 | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-B2B-7 + build de Lambdas; risco=2 (verificação, não decisão); evidência=npm test 1139/1139 (123 arquivos), zero regressão (+5 líquidos vs. B2B-5: 4 testes novos em authorization.test.ts, 1 líquido em resolver.test.ts); npm run build:lambdas PASS (33/33 Lambdas); lacunas=nenhuma` |

Com este subitem, **Wave B2B-7 (RBAC) está `DONE`**.

## Escopo de Wave B2B-8 (Invitations/Team) — `APPROVED`, D-099

Ver `docs/architecture/multi-user-b2b-wave-b2b8-scope.md` para o registro completo — protocolo Claude↔Codex 3 rodadas, **segunda aplicação real de `docs/engineering/research-protocol.md` (E-014)**, declaração `SIM PARCIAL` (mecanismo de dados de `Invitation` já pesquisado/aprovado em D-086; pesquisado nesta wave: last-owner protection convergência 4/4, hierarquia de gerência de membros convergência 3/4, hierarquia sobre a própria hierarquia). A Rodada 1 do Codex contestou o checklist (achados reais: privacidade de convites pendentes vs. lista de membros; token pointer não consumido na transação de aceite; colisão de chave de rate-limit) — reconciliado nas Rodadas 2/3. Evidência completa em `docs/architecture/reviews/multi-user-b2b-wave-b2b8-scoping/`.

### Subitens de B2B-8 (implementação, D-100 — mantido como item coeso per `definition-of-done.md` "diff atomicamente revisável": domain/ports/application/HTTP/infra não compilam de forma independente entre si, mas cada subparte reconhecível abaixo tem sua própria evidência)

| Subitem | Camada | Status |
|---|---|---|
| B2B-8.1 | Domain — `Invitation`, `InvitationTokenPointer` (com `consumedAt`, família `GuestTokenPointer` mas implementação paralela), `InvitationDedupPointer`, `MembershipAuditEvent` (agregado-irmão, partição própria sharded por mês — corrigido antes de testar: não a partição de `Organization`) | **DONE** (2026-08-30) — `DoD: item=domain B2B-8 (4 tipos novos); risco=5 (novos access patterns/chaves, implementação direta de D-086 §7); evidência=npm run typecheck PASS, npm run lint PASS; lacunas=nenhuma` |
| B2B-8.2 | Ports — `OrganizationStore.updateConditional()` (mesma assinatura de `SubjectStore.updateConditional`) | **DONE** (2026-08-30) — `DoD: item=updateConditional no port+adapter; risco=3 (extensão mecânica de padrão já testado); evidência=npm run typecheck PASS, npm run lint PASS; lacunas=nenhuma` |
| B2B-8.3 | Application — `MembershipInviteRateLimiter` (chave namespaced `#MEMBERSHIP-INVITE`, nunca colide com `initial-invite-rate-limiter.ts`) + `CreateInvitationService` (invite/resend + achado corrigido na própria Rodada 1: invite-como-OWNER exige OWNER chamador + e-mail best-effort via `emailProvider` opcional) + `RevokeInvitationService` + `AcceptInvitationService` (transação de 6 itens, token consumido com `attribute_not_exists(consumedAt) AND expiresAt > :now` — as DUAS cláusulas, achado corrigido no próprio self-grade da Rodada 2) | **DONE** (2026-08-30) — `DoD: item=CreateInvitation+RevokeInvitation+AcceptInvitation+rate-limiter; risco=5 (primeiro writer real de Membership além da criação, gatilho central da wave); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (411 módulos, 0 violação), test/unit/organization/{create,revoke,accept}-invitation.test.ts 4+2+7=13/13 PASS (anti-account-takeover, anti-replay incl. corrida de expiração, dedup/resend, incremento de ownerCount no aceite-como-OWNER), G-V3 aplicado desde a escrita; lacunas=envio real de e-mail via SES não conectado nesta wave (mecanismo pronto, `emailProvider` opcional sem valor passado na composição real — ativação requer decisão de kill-switch mesmo padrão D-049 + IAM de SES para o novo Lambda, registrado como follow-up explícito, não esquecido)` |
| B2B-8.4 | Application — `ListMembersService` (`membership:list-members`) + `ListInvitationsService` (`membership:list-invitations`, separado por privacidade — achado real da Rodada 1 do Codex) | **DONE** (2026-08-30) — `DoD: item=ListMembers+ListInvitations; risco=3 (reaproveita queryByPk já existente, sem novo access pattern); evidência=npm run typecheck PASS, npm run lint PASS, testes-alvo em membership-management.test.ts 3/3 PASS (VIEWER lista membros, VIEWER negado em convites, ADMIN autorizado); lacunas=nenhuma` |
| B2B-8.5 | Domain (`authorization.ts`) + Application — `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService`, builder `owner-count-guard.ts` compartilhado pelas 3 operações, `LastOwnerError`/`OwnerTierChangeRequiresOwnerError`/`InvitationTokenUnavailableError` nomeados (`app-error.ts`), 7 novas `Action`s (`membership:invite`/`revoke-invitation`/`list-members`/`list-invitations`/`role-change`/`remove`/`leave`) | **DONE** (2026-08-30) — `DoD: item=ChangeRole+Remove+Leave+owner-count-guard+7 Actions; risco=5 (decisão de autorização nova, informada por pesquisa — o núcleo do debate); evidência=npm run typecheck PASS, npm run lint PASS, test/unit/organization/membership-management.test.ts 13/13 PASS (last-owner protection nas 3 operações, hierarquia OWNER-tier vs. ADMIN-tier, soft-remove nunca hard-delete), G-V3 aplicado desde a escrita; lacunas=nenhuma` |
| B2B-8.6 | HTTP — `membership-handlers.ts` (7 handlers, resource API) + `handleAcceptInvitation` em `bff-handlers.ts` (identity-only, mesma classe de `POST /bff/organizations`) + `composition/organization.ts`/`memberships-handler.ts` (novo Lambda) + 3 schemas JSON (`create-invitation-request`/`change-membership-role-request`/`accept-invitation-request`) + entrada `organization-invitation` em `email-templates.ts` + Terraform (novo Lambda `memberships_handler` reaproveitando `GUEST_TOKEN_PEPPER`, 7 rotas API Gateway, allowlist do proxy BFF, log group explícito, `security_audit_observability` atualizado) | **DONE** (2026-08-30) — `DoD: item=HTTP+schemas+Terraform completos; risco=4 (novo endpoint/Lambda, mas reaproveita padrões já testados); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS, npm run validate-schemas PASS, test/contract/schemas.test.ts +6 casos novos PASS, terraform validate PASS (root + módulo api-gateway), terraform test via AWS_PROFILE=claude-dev PASS (root 15/15 incl. contagem de Lambdas atualizada, api-gateway 1/1 incl. 4 asserts novos de rota memberships), npm run build:lambdas PASS (34/34, incl. memberships-handler); lacunas=nenhuma` |
| B2B-8.7 | Testes — suíte completa (`npm test`) para toda a Wave B2B-8 | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-B2B-8; risco=2 (verificação, não decisão); evidência=npm test 1171/1171 (127 arquivos), zero regressão (+32 líquidos vs. B2B-7: 26 dos 4 arquivos de teste novos + 6 casos de contrato novos); lacunas=nenhuma` |

Com este subitem, **Wave B2B-8 (Invitations/Team) está `DONE`**.

## Escopo de Wave B2B-6 (BFF Organization Context) — `APPROVED`, D-101

Ver `docs/architecture/multi-user-b2b-wave-b2b6-scope.md` para o registro completo — protocolo Claude↔Codex 3 rodadas, **terceira aplicação real de `docs/engineering/research-protocol.md` (E-014)**. Achado real que motivou a wave agora: `RequestContextResolver.resolveActiveMembership()` lançava `InternalError` (500) para qualquer usuário com >1 `Membership` `ACTIVE` — inatingível até B2B-8, agora explorável de verdade por qualquer usuário que aceite um 2º convite. A Rodada 1 do Codex contestou o checklist (faltava OWASP como fonte; faltava critério de boundary BFF/browser; `select` sem CSRF explícito; lista sem filtro de lifecycle; estado ambíguo sem contrato tipado) — reconciliado nas Rodadas 2/3. Evidência completa em `docs/architecture/reviews/multi-user-b2b-wave-b2b6-scoping/`.

### Subitens de B2B-6 (implementação, D-102 — mantido como item coeso per `definition-of-done.md`)

| Subitem | Camada | Status |
|---|---|---|
| B2B-6.1 | Domain (`app-error.ts`) — `OrganizationUnavailableError` (AUTHORIZATION/403, substitui o `AuthenticationError` pré-existente para o mesmo caso — achado de inconsistência corrigido na Rodada 3)/`OrganizationSelectionRequiredError` (CONFLICT/409); Application — `resolveWorkingOrganization()` (resultado semântico, nunca lança) + `listUsableOrganizations()` (filtra Membership ACTIVE **e** lifecycle ACTIVE) | **DONE** (2026-08-30) — `DoD: item=2 erros nomeados + 2 helpers compartilhados; risco=5 (novo contrato de erro no gate real de login); evidência=npm run typecheck PASS, npm run lint PASS; lacunas=nenhuma` |
| B2B-6.2 | Application (`resolve-request-context.ts`) — `organizationIdHint: string \| undefined` OBRIGATÓRIO no input (não opcional, força o compilador a barrar os 56 call sites reais); `resolveActiveMembership()` reescrito usando `resolveWorkingOrganization()`, consolidando e substituindo a checagem de lifecycle separada que existia antes | **DONE** (2026-08-30) — `DoD: item=organizationIdHint + resolveActiveMembership() reescrito; risco=5 (muda o gate real de toda chamada autenticada); evidência=npm run typecheck PASS, npm run lint PASS, npm run check-boundaries PASS (412 módulos, 0 violação), test/unit/identity/resolver.test.ts 17/17 PASS (hint válido usado direto, hint inválido → OrganizationUnavailableError, >1 sem hint → OrganizationSelectionRequiredError em vez do InternalError antigo, regressão zero para organização única), G-V3 aplicado desde a escrita; lacunas=nenhuma` |
| B2B-6.3 | Application/HTTP (BFF) — `ProxyService.forward()` injeta `X-Organization-Id` só a partir da sessão (nunca lê o header do browser); `POST /bff/organization/select` com CSRF (mesmo padrão de `handleCreateOrganization`); `GET /bff/organizations` filtrado por lifecycle; `SessionWithOnboarding.organizationSelectionRequired` tipado com regra de cardinalidade explícita (0/1/>1 sobre a mesma lista filtrada) + invalid-selection-recovery (`activeOrganizationId` revalidado a cada `GET /bff/session`, limpo se inválido) | **DONE** (2026-08-30) — `DoD: item=header boundary + select + list + contrato tipado + self-heal estendido; risco=5 (novo endpoint + mudança de contrato de proxy, gate real de login); evidência=npm run typecheck PASS, npm run lint PASS, test/unit/bff/bff-organization-context.test.ts 7/7 PASS (header de browser descartado, select rejeita org alheia, cardinalidade >1 → organizationSelectionRequired, invalid-selection-recovery limpa e re-deriva, multi-sessão real com 2 logins independentes, lista filtra lifecycle), test/unit/bff/bff-auth-service.test.ts 46/46 PASS (zero regressão), G-V3 aplicado desde a escrita; lacunas=nenhuma` |
| B2B-6.4 | HTTP — threading obrigatório de `organizationIdHint` nos 56 call sites reais (13 arquivos, `grep -rl` exaustivo — contagem corrigida na Rodada 2 do debate de escopo após faltar `test-route-handler.ts` na primeira contagem) | **DONE** (2026-08-30) — `DoD: item=threading em 56 call sites/13 arquivos; risco=3 (mecânico, sem decisão nova por arquivo, mas volume real de edição alto); evidência=npm run typecheck PASS (o compilador barrou cada site esquecido, confirmando a cobertura completa); lacunas=nenhuma` |
| B2B-6.5 | Testes — suíte completa (`npm test`) para toda a Wave B2B-6 | **DONE** (2026-08-30) — `DoD: item=suíte completa pós-B2B-6; risco=2 (verificação, não decisão); evidência=npm test 1180/1180 (128 arquivos), zero regressão (+9 líquidos vs. B2B-8: 7 do arquivo de teste novo bff-organization-context.test.ts + 2 novos em resolver.test.ts); npm run build:lambdas PASS (34/34); lacunas=nenhuma` |

Com este subitem, **Wave B2B-6 (BFF Organization Context) está `DONE`**.

## Achados/pendências laterais abertos durante a execução (não bloqueiam waves seguintes)

- BFF login path sem `TenantLifecycleRecord`/fencing (achado da Wave B2B-0) — **RESOLVIDO** por B2B-2.3+2.4 acima.
- Decremento transacional de `ownerCount` (physical model §8) — **RESOLVIDO** por B2B-8 (`owner-count-guard.ts`, compartilhado por `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService`); o TODO explícito registrado em B2B-3/B2B-7 está fechado.
- Envio real de e-mail de convite via SES — **NÃO conectado nesta wave** (`CreateInvitationService` aceita `emailProvider`/`invitationBaseUrl` opcionais, mas a composição real (`memberships-handler.ts`) não os passa) — requer decisão de kill-switch (mesmo padrão D-049, "default false") + IAM de SES para o novo Lambda `memberships_handler`. Registrado explicitamente como follow-up, não esquecido.
- `InternalError` 500 de `RequestContextResolver` para >1 Membership ACTIVE (achado da Wave B2B-5/D-095) — **RESOLVIDO** por B2B-6 (`OrganizationSelectionRequiredError`).
- Transporte de `activeOrganizationId` do BFF até o `RequestContextResolver` do recurso (achado 2.1, D-095) — **RESOLVIDO** por B2B-6 (`X-Organization-Id`).
- `InvitationTokenPointer` inalcançável pelo scan/`PURGE_DELETE` de purga (declara `organizationId`, não `tenantId` — gap real introduzido por B2B-8, não coberto pelo fix B1 de D-082) — **RESOLVIDO** por B2B-9/D-104 (3ª cláusula OR em ambos).
- Orquestrador do purge pipeline (Step Functions vs. Lambda+EventBridge Scheduler, apontado como "próxima etapa real" em D-083) — **AINDA PENDENTE**, decisão de infraestrutura/operação (quem/quando dispara `purgeTenant()` de forma durável) explicitamente fora do escopo de B2B-9 (que tratou só do CONTEÚDO da purga e retenção de dados, não de quem a invoca) — não bloqueia nenhuma wave B2B seguinte, registrado para uma sessão dedicada futura.
- `frontend/test/routes/Settings.test.tsx` (linha 65, "conflict-specific message on a stale expectedVersion") flakou em CI **pela 2ª vez** (PR #112, B2B-12) mesmo após o timeout já ter sido alargado para 3000ms na correção anterior (PR de B2B-11, commit `b76970d`) — nunca reproduzido localmente nas duas ocasiões, e um simples `gh run rerun --failed` passou de novo sem mudança de código. Não bloqueou B2B-12 (rerun verde), mas 2 ocorrências do mesmo teste específico sugere uma causa raiz real (não só variância de CI) que um 3º timeout-bump não resolveria de forma definitiva — candidato a investigação dedicada (ex.: fake timers/microtask scheduling do próprio componente `Settings.tsx` sob carga do runner) antes de uma 3ª ocorrência.
- Supersessão de `UserProfile.requesterDisplayName` por `Organization.displayName` (GTR-01, prevista como item de B2B-11 em B2B-5/D-096) — **AINDA PENDENTE**. O escopo real de B2B-11, definido e `APPROVED` via protocolo Claude↔Codex (Rodada 1-3, `multi-user-b2b-wave-b2b11-scope.md`), tratou de responsible member/ItemWatch/notification routing (per o texto real do roadmap §116) — a supersessão de GTR-01 nunca foi levantada nem debatida nas 3 rodadas, não é a mesma coisa que "responsible member" (GTR-01 é sobre a identidade mostrada ao guest externo, não sobre quem é responsável/assignee interno). Não bloqueia nenhuma wave seguinte; registrado explicitamente para não ficar perdido entre a previsão antiga e o escopo real fechado agora.

## Escopo de Wave B2B-12 (Cutover de dev) — `APPROVED`, D-110

Ver `docs/architecture/multi-user-b2b-wave-b2b12-scope.md` para o registro completo — protocolo
Claude↔Codex 3 rodadas (Claude 9,2/Codex 9,1), **declaração E-014 NÃO** (política reset-vs-migração já
aprovada dentro do próprio `roadmap-evolution/17` §62-63, esta wave só aplica contra inventário real).
Inventário real via `aws --profile claude-dev` (não hipotético) confirma `dev` 100% sintético/descartável:
`exptrk-dev-table` 47 itens/zero `Organization`/`Membership`, `exptrk-dev-bff-session` 0 itens, Cognito 0
usuários reais, S3 só `extraction-transient` com conteúdo, 2 de 24 filas SQS com mensagens reais. A
Rodada 1 do Codex achou 7 achados reais (faltava snapshot/export, `BatchWriteItem` subespecificado,
`--table` sozinho não protege contra erro de operador, Cognito/filas fora do inventário, `bff-session`
subestimado, escopo de `LEGACY_TENANT_ONLY` sem o frontend) — corrigidos na Rodada 2, que por sua vez
teve 1 achado bloqueante grave (snapshot bruto em `docs/architecture/reviews/` arriscava commitar
PII/segredo real de `Session`/`GlobalUser`) corrigido na Rodada 3. Escopo final: `scripts/reset-dev-data.ts`
(fase A inventário+snapshot sempre, fase B delete real só com `--confirm`, allowlist de ambiente,
verificação final fail-loud) + remoção de `LEGACY_TENANT_ONLY` (backend+frontend+teste). **Execução real
(`--confirm`/`--include-cognito`/`s3 rm` reais) gated por confirmação explícita do Marcelo** — pendente,
registrado abaixo, não bloqueia implementação/teste do script contra fakes nem `--dry-run` real (leitura).

### Implementação (D-111) — `Wave B2B-12 DONE`

`scripts/reset-dev-data.ts` implementado exatamente per o escopo `APPROVED`: Fase A (sempre, read-only)
+ Fase B (`--confirm`) + allowlist (`assertAllowedTable`) + guarda de conta (`assertExpectedAccount` via
`sts:GetCallerIdentity`) + `deleteBatchWithRetry()` (backoff local, sem módulo novo em `shared/`) +
manifest redigido (`buildManifestEntry`/`hashItem`, nunca valor sensível) + verificação final fail-loud
(`assertAllEmpty`). **Verificação real, não só contra fakes**: Fase A rodada de fato contra `dev`
(`AWS_PROFILE=claude-dev npx tsx scripts/reset-dev-data.ts --user-pool-id us-east-1_NZlvr5IIn`, sem
`--confirm`) — confirmou 47 itens/0 sessões/21 objetos S3/0 usuários Cognito/24 filas (1 com 3 mensagens
reais), manifest gravado com sucesso em `docs/architecture/reviews/multi-user-b2b-wave-b2b12-scoping/
dev-reset-manifest-2026-08-30T16-22-38-931Z.json`, parou antes da Fase B exatamente como projetado.
`LEGACY_TENANT_ONLY` removido de `onboarding-state.ts`/`frontend/src/api/session.ts`/teste, confirmado
seguro (nenhum switch exaustivo nos 2 call sites reais). G-V3 verificado por mutação real em 3 pontos
(retry de `deleteBatchWithRetry`, ordenação de chaves de `hashItem`, allowlist de `--table`). Suíte
completa: backend `npm test` 1232/1232 (zero regressão), frontend `vitest` 139/139 + `playwright`
24/24 (zero regressão), `build:lambdas` 34/34. **Execução real destrutiva (`--confirm`/
`--include-cognito`) permanece pendente de confirmação explícita do Marcelo** — não bloqueia o
fechamento desta wave nem waves seguintes.

## Escopo de Wave B2B-13 (E2E/Adversarial Security) — `APPROVED`, D-112 — implementada, D-113

Ver `docs/architecture/multi-user-b2b-wave-b2b13-scope.md` para o registro completo — protocolo
Claude↔Codex 3 rodadas (Claude 9,1/Codex 9,2, régua E-014 9,3/10). Auditoria contra as 25 perguntas
de `roadmap-evolution/17` §121: **20/25 já cobertas** por testes reais das próprias waves B2B-5 a
B2B-12; **2 fecham com fix real de produção** (mesmo bug TOCTOU em 2 pontos —
`resolveRecipientEmail`/`notification.ts` e `resolveInternalUserEmail`/`subject.ts` liam só
`GlobalUser` no momento de ENVIO assíncrono, nunca revalidavam `Membership`, janela real entre
roteamento — que já checava elegibilidade — e entrega; achado #3 original + um 6º achado real do
próprio Codex na Rodada 1, mesma classe de bug em `document-chasing-dispatch`); **2 ganham teste
novo** (revogação de Membership nunca encadeada ponta-a-ponta com `resolve()` subsequente; nenhum
teste usava roles DIFERENTES por Organization do mesmo usuário); **1** é auditoria de fixture IDs
(zero achado real). Implementação: `ResolvedRecipient` ganha `email?: string`, os 2 composition
roots reaproveitam `DynamoDbNotificationRecipientResolver` já testado em vez de ler `GlobalUser`
sozinhos (evitando um 3º código não-testável com a mesma regra — composition roots nunca são
unit-testados diretamente neste projeto, precedente D-109). Achado real durante a implementação
(não estava no escopo `APPROVED`, honestidade de processo): o teste de revogação em
`resolver.test.ts` esperava `OrganizationUnavailableError`, mas o `OnboardingStateResolver` roda
ANTES e lança `OnboardingRequiredError` quando a Membership removida é a única do usuário —
corrigido dando ao membro uma segunda Organization própria, isolando a asserção à revogação
escopada. G-V3 verificado por mutação real em 4 pontos. Suíte completa `npm test` 1235/1235, zero
regressão, `build:lambdas` 34/34. Nenhuma mudança de frontend (fora de escopo). Com este item,
**Wave B2B-13 está `DONE`**.
