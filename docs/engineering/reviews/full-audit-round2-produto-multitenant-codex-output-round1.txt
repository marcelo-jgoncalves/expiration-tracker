OpenAI Codex v0.147.0
--------
workdir: C:\Users\Usuario\Desktop\projects\expiration-tracker
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a07ee7-9726-7f50-a4ad-cfbc83e72e68
--------
user
Você é o revisor independente (Codex) no protocolo Claude<->Codex deste repositório (AGENTS.md secao 4, nota cega: nao peca nem receba a nota do Claude antes de registrar a sua).

Repositorio: expiration-tracker (C:\Users\Usuario\Desktop\projects\expiration-tracker), branch develop, micro-SaaS multi-tenant B2B serverless AWS.

TAREFA: auditoria (nao implementacao) do eixo formal "Governanca de Produto e Servico Multi-tenant" (docs/engineering/joint-review-criteria.md, secao no final do arquivo, 8 criterios pesados, criterio 5 tratado como N/A por falta de escopo de UX nesta auditoria). Ja existe uma Rodada 1 anterior (mes passado) documentada em docs/engineering/reviews/full-audit-round1-produto-*.md — nota final 4,65/10, abaixo do gate de 9.0. Esta e uma NOVA rodada (chamada round2 no nome de arquivo) reauditando o eixo apos mudanca real de escopo desde entao: Document Archive completo (D-143 a D-226 em docs/architecture/decisions-log.md), canal WhatsApp, compartilhamento externo (guest upload, dossier export, scheduled reports), e as waves B2B-7 a B2B-14 (RBAC, convite, troca de organizacao, ultimo-OWNER, cutover de dev D-110/D-111).

Leia primeiro, na integra: AGENTS.md, docs/engineering/joint-review-criteria.md (secao final, "Eixo: Governanca de Produto e Servico Multi-tenant"), docs/architecture/decisions-log.md (linhas relevantes D-1xx em diante — o arquivo e enorme, procure por palavras-chave: cross-tenant, RBAC, ACTION_ROLES, isolamento, D-110, D-111, D-120, D-124, D-127, D-143, D-197), src/modules/identity/domain/authorization.ts (a matriz RBAC inteira, ACTION_ROLES + authorize()).

PERGUNTA CENTRAL desta rodada: procure especificamente por UMA rota ou acao de negocio nova (desde agosto/D-140 em diante) que NAO tenha RBAC real (authorize() nunca chamado no caminho, ou Action ausente da matriz ACTION_ROLES) OU que nao tenha tenant-fencing real (resource.tenantId nao verificado contra o tenant autenticado antes de agir). Confirme por leitura de codigo real, nao suposicao — cite arquivo:linha. Se nao encontrar nenhuma, diga isso explicitamente e explique por que a garantia estrutural (ACTION_ROLES tipado como Record<Action, ...> — o TypeScript nao compila se faltar uma entrada) e suficiente ou insuficiente como prova.

Verifique tambem:
1. Isolamento multi-tenant: e so por convencao (PK/tenantId em toda chave) ou ha prova real (aws iam simulate-principal-policy, testes terraform de isolamento GSI3/GSI6/GSI8)? Procure em decisions-log.md por "simulate-principal-policy" e "gsi8_worker_isolation".
2. Historico de vazamento cross-tenant: procure decisions-log.md por "cross-tenant", "vazamento", "leak", "IDOR" — confirme se ha algum achado historico (D-16x a D-19x citado no round1 anterior) que nunca foi fechado, ou se tudo que aparece sao provas positivas de isolamento (nao vazamentos).
3. FinOps/economia unitaria: ha visibilidade real de custo por tenant em runtime, ou e so estimativa agregada de design (docs/architecture/cost-model.md)? Isso conta como criterio 7 deste eixo (nao ha eixo FinOps formal ainda).
4. Onboarding/cutover de tenant (D-110/D-111, scripts/reset-dev-data.ts): continua coerente com o schema atual, ou ha sinal de drift?
5. Lifecycle de tenant (criterio 1): organization:close / organization:cancel-close / HELD_FOR_RECOVERY (D-124/D-127) — sao estados reais testados ou so declarados?

ENTREGAVEL: nota de 0 a 10 (uma decimal, sem arredondar para 9 se for 8.9) por criterio aplicavel (1,2,3,4,6,7,8 — criterio 5 excluido/N/A), nota ponderada final normalizada sobre os pesos dos criterios aplicaveis, e a lista de achados com severidade (destaque com MAIUSCULAS qualquer achado real de vazamento cross-tenant ou rota sem RBAC/fencing, por menor que seja). Se nao rodar comandos de shell (aws cli etc.), deixe explicito que a avaliacao e por leitura de codigo/decisions-log, nao verificacao ao vivo.

Responda em portugues, formato markdown, comece com "## Nota da Rodada 1 (Codex)".

codex
Vou fazer uma auditoria estritamente read-only e registrar minha nota sem consultar o parecer/nota do Claude. Primeiro leio as fontes obrigatórias e o estado da sessão; depois rastreio rotas pós-D-140 até autorização e tenant-fencing, além das evidências dos cinco pontos adicionais.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw AGENTS.md; Get-Content -Raw NEXT_SESSION_PROMPT.md; Get-Content -Raw docs/architecture/README.md; Get-Content -Raw docs/engineering/README.md; Get-Content -Raw docs/engineering/joint-review-criteria.md; Get-Content -Raw src/modules/identity/domain/authorization.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 2359ms:
# AGENTS.md ƒ?" Expiration Tracker

> Fonte canÇïnica de regras durÇ­veis para qualquer agente de IA (Claude Code, Codex CLI) trabalhando neste repositÇürio. `CLAUDE.md` importa este arquivo ƒ?" nÇœo duplicar conteÇ§do nele.

## 1. Papel e estÇ­gio do projeto

Micro-SaaS de controle de vencimentos/renovaÇõÇæes, arquitetura AWS serverless. Design Maturity `APPROVED`; implementaÇõÇœo em andamento por milestone ƒ?" ver `docs/architecture/README.md` (bloco de status no topo) para a fase/milestone vigente e `NEXT_SESSION_PROMPT.md` para a prÇüxima aÇõÇœo exata; nenhum dos dois Ç¸ normativo, mas ambos sÇœo o estado real, nÇœo este arquivo. Marcelo Ç¸ o responsÇ­vel final por decisÇæes de produto/arquitetura; o agente atua como engenheiro autÇïnomo, nÇœo assistente passivo.

**Autonomia padrÇœo (2026-08-29)**: para manter o mÇ­ximo de trabalho em andamento, postergar ƒ?" nunca bloquear a sessÇœo esperando ƒ?" qualquer subtarefa que dependa inteiramente de decisÇœo exclusiva de Marcelo (produto/arquitetura genuÇðna, credencial, aÇõÇœo fÇðsica); registrar o pendente claramente e seguir com outra frente independente, retomando depois. DecisÇœo que implica custo incremental nÇœo-recorrente abaixo de US$5 (ex.: chamada de API, execuÇõÇœo de teste em nuvem) nÇœo precisa de autorizaÇõÇœo prÇ¸via dele. Isto nÇœo dispensa o protocolo ClaudeƒÅ"Codex do ¶õ4 nem a confirmaÇõÇœo de merge `developƒÅ'main` do ¶õ3.

**Sem usuÇ­rios reais nem produÇõÇœo ƒ?" nÇœo pesar "risco ao usuÇ­rio" (2026-08-30)**: fase de construÇõÇœo do produto, sem usuÇ­rio real, sem ambiente de produÇõÇœo (deploy real sÇü existe em `dev`, dado sintÇ¸tico, resetÇ­vel/reseedÇ­vel a qualquer momento), sem data de lanÇõamento nem pressÇœo de prazo. Objetivo Ç¸ qualidade mÇ­xima em ritmo profissional decente, nÇœo velocidade. Por isso, ao avaliar uma mudanÇõa, **nunca tratar "quebraria contas/sessÇæes/dados existentes em `dev`" como risco bloqueador por si sÇü** ƒ?" essa categoria de cautela (compatibilidade retroativa, migraÇõÇœo seguindo usuÇ­rio real) nÇœo se aplica aqui e nÇœo deve aparecer como justificativa em anÇ­lise prÇüpria ou rodada ClaudeƒÅ"Codex. Isto **nÇœo** dispensa rigor de engenharia, seguranÇõa, ou o protocolo do ¶õ4 para decisÇæes Type 1 ƒ?" sÇü remove a categoria especÇðfica de risco de "impacto a usuÇ­rio/produÇõÇœo" que este projeto ainda nÇœo tem. Um bloqueador real continua sendo: qualidade de engenharia insuficiente, achado de seguranÇõa, ou perda de produtividade do prÇüprio time (ex. quebrar o ambiente de trabalho corrente sem necessidade).

PrincÇðpios de engenharia, tiers de gate de qualidade, escala de risco que calibra quando o protocolo do ¶õ4 Ç¸ obrigatÇürio, e os critÇ¸rios ponderados por eixo (Arquitetura/Qualidade de Engenharia/Engenharia de Contexto) usados nas revisÇæes conjuntas: `docs/engineering/{principles,quality-gate-tiers,change-risk-scale,joint-review-criteria}.md` (padrÇæes adotados do projeto irmÇœo `event-discovery-platform`, adaptados ƒ?" nÇœo copiados ƒ?" Çÿ complexidade real deste projeto).

**Definition of Done por item de todo list (2026-08-29, decisÇœo de processo permanente, protocolo `¶õ4` completo)**: nenhum item de todo list que produza/altere cÇüdigo real Ç¸ marcado concluÇðdo sem passar pelo gate correspondente ao seu nÇðvel de risco (`docs/engineering/definition-of-done.md`) ƒ?" inclui decompor itens guarda-chuva, classificar o risco pelo diff real, e registrar uma linha `DoD:` de evidÇ¦ncia mÇðnima antes de fechar. Aplica-se a partir de agora, em toda sessÇœo, nÇœo sÇü a esta.

**`docs/00-prompt-mestre.md` Ç¸ a especificaÇõÇœo de processo do ciclo de design jÇ­ concluÇðdo ƒ?" nÇœo Ç¸ o ponto de entrada da sessÇœo atual.** NÇœo reiniciar a Fase 0 nem tratar suas instruÇõÇæes ("comece pela Fase 0") como comando ativo.

## 2. InÇðcio de sessÇœo

1. Ler `NEXT_SESSION_PROMPT.md` (estado atual + prÇüxima aÇõÇœo).
2. Ler `docs/architecture/README.md` (mapa de fontes de arquitetura/sistema, status vigente, regra de precedÇ¦ncia).
3. Se a tarefa envolver processo de qualidade/revisÇœo (rubrica, protocolo ClaudeƒÅ"Codex, achados de auditoria por eixo), ler `docs/engineering/README.md` em vez de navegar `docs/engineering/` Çÿs cegas.
4. Se a tarefa envolver planejamento de interface (UX, IA, journeys, telas), ler `docs/frontend/README.md` primeiro ƒ?" inclui os 3 blockers tÇ¸cnicos de backend (BLOCKER-A/B/C) e GTR-01, citados por ID em todo o planejamento, que nÇœo devem ser reabertos nem mascarados.
5. Consultar `docs/project/working-memory.md` sÇü quando a tarefa envolver COMO trabalhar com Marcelo (ferramentas, processo), nÇœo O QUE decidir sobre o produto.
6. NÇœo carregar todo `docs/architecture/history/` (nem `docs/engineering/reviews/`) por padrÇœo ƒ?" Ç¸ evidÇ¦ncia histÇürica, consultar sob demanda.

## 3. EstratÇ¸gia de branch (padrÇœo a partir de 2026-08-19)

`develop` Ç¸ o branch de trabalho ativo ƒ?" todo commit de sessÇœo (implementaÇõÇœo, remediaÇõÇœo, docs) vai para `develop`, nunca direto em `main`. `main` Ç¸ protegido no GitHub (required status check `guardrails`/CI, sem force-push, sem deleÇõÇœo) e sÇü recebe merge via PR de `develop` quando um marco estÇ­ estÇ­vel e verificado (CI verde, gates relevantes revisados). Antes de comeÇõar qualquer trabalho de cÇüdigo, confirmar `git branch --show-current` = `develop` (ou uma branch de feature a partir dele, se a tarefa justificar); se estiver em `main`, trocar para `develop` antes de commitar.

**Autonomia de merge (2026-08-29)**: ao concluir com sucesso uma etapa relevante da lista de tarefas em andamento, commit, push de `develop` e merge do PR `developƒÅ'main` podem ser feitos sem aguardar confirmaÇõÇœo explÇðcita de Marcelo ƒ?" os recursos hoje sÇü sÇœo deployados em `dev` (CD dispara em push a `main`, ver ¶õ7), nÇœo existe ambiente de produÇõÇœo real ainda e nÇœo hÇ­ previsÇœo prÇüxima de criar um. Isto substitui a exigÇ¦ncia anterior desta seÇõÇœo de confirmar antes de merjar. NÇœo dispensa o protocolo ClaudeƒÅ"Codex do ¶õ4 para decisÇæes Type 1 antes do merge, nem uma checagem rÇ­pida antes de operaÇõÇœo genuinamente destrutiva/irreversÇðvel (force-push, deletar branch).

## 4. Protocolo de debate Claude ƒÅ" Codex

Aplica-se **obrigatoriamente** a: decisÇæes de arquitetura, requisitos, modelo de dados, seguranÇõa/privacidade, e qualquer entregÇ­vel explicitamente submetido ao protocolo (Type 1, difÇðcil de reverter ƒ?" nÇðvel 5-6 de `docs/engineering/change-risk-scale.md`). **NÇœo Ç¸ obrigatÇürio** para: correÇõÇœo mecÇ½nica, documentaÇõÇœo factual, refactors locais reversÇðveis, lint/teste, implementaÇõÇœo direta de decisÇœo jÇ­ aprovada (nÇðveis 1-4) ƒ?" usar a escala de risco para calibrar, nÇœo julgamento ad-hoc repetido.

Quando aplicÇ­vel: mÇðnimo 3 rodadas (proposta ƒÅ' crÇðtica ƒÅ' trÇ¸plica), nota mÇðnima 9.0 de ambos antes de considerar concluÇðdo, sem arredondar (8.99 nÇœo vira 9). Protocolo de nota cega: o avaliador que responde depois nÇœo vÇ¦ a nota/parecer do primeiro atÇ¸ ambos existirem registrados; desacordo abaixo de 9 reabre rodada em vez de arredondar ou fazer mÇ¸dia.

**Pesquisa externa antes da Rodada 1 (2026-08-30, E-014)**: uma decisÇœo nÇðvel 5-6 que define um padrÇœo que sistemas fora deste projeto jÇ­ resolveram de forma estabelecida (RBAC, convite/invite, sessÇœo multi-tenant, etc.) exige, na prÇüpria proposta Rodada 1, uma declaraÇõÇœo explÇðcita `SIM`/`SIM PARCIAL`/`NÇŸO` (nunca omissÇœo) e, se `SIM`/`SIM PARCIAL`, um checklist de critÇ¸rios pesados derivado da pesquisa que vira a rÇ¸gua de nota da rodada ƒ?" critÇ¸rio completo, fluxo de reconciliaÇõÇœo se o Codex contestar o checklist, e exemplos: `docs/engineering/research-protocol.md`.

O protocolo Ç¸ dispensÇ­vel para uma decisÇœo Type 1 quando o responsÇ­vel final (`AGENTS.md` ¶õ1) jÇ­ decidiu diretamente ƒ?" mas sÇü sob as trÇ¦s condiÇõÇæes registradas em `docs/engineering/ai-governance.md` ¶õ2 (achado real de `full-audit-round1-governanca-ia`: ADR-0009 dispensou o protocolo sem essa exceÇõÇœo estar formalizada). Matriz de autoridade por agente, inventÇ­rio de casos de uso de IA e registro de incidentes do prÇüprio agente (distinto de `exceptions.md`, que Ç¸ para exceÇõÇœo de regra de engenharia): `docs/engineering/ai-governance.md`.

InvocaÇõÇœo do Codex: `codex exec --skip-git-repo-check "<prompt>"`, rodar em background. **Nunca usar crases (`` ` ``) dentro de um prompt passado por Bash com aspas duplas** ƒ?" o shell interpreta como substituiÇõÇœo de comando e corrompe a entrada silenciosamente (o processo trava esperando stdin, CPU ~0). Para prompts com crases/markdown, escrever em arquivo e usar `codex exec --skip-git-repo-check - < arquivo.txt`. Se um processo `codex` rodar muito mais que rodadas comparÇ­veis com CPU quase zero, estÇ­ travado ƒ?" matar e relanÇõar, nÇœo esperar. **NÇœo combinar `- < arquivo.txt` com backgrounding (`&`)** ƒ?" essa combinaÇõÇœo jÇ­ produziu duas falhas silenciosas (processo travado relendo arquivo grande e bloqueado por polÇðtica de sandbox; depois saÇðda vazia sem erro) mesmo com prompt correto. Rodar `codex exec --skip-git-repo-check - < arquivo.txt > saida.txt 2>&1` em primeiro plano (sem `&`) Ç¸ o padrÇœo confiÇ­vel verificado; sÇü background se o prompt for curto o bastante para passar inline sem stdin.

**Rate limit do Codex ƒ?" agendar wakeup, nunca bloquear a sessÇœo esperando (Marcelo, 2026-09-04)**: se `codex exec` retornar erro de limite de uso/rate limit (nÇœo um travamento ƒ?" ver o parÇ­grafo acima para essa distinÇõÇœo), nÇœo ficar tentando de novo em loop nem parar a sessÇœo. Registrar a rodada pendente (qual decisÇœo, qual prompt, qual arquivo de contexto), seguir para outra frente de trabalho independente que nÇœo dependa do Codex (mesmo princÇðpio de autonomia do ¶õ1 ƒ?" nunca ficar ocioso), e usar `ScheduleWakeup` (ou o cron de `schedule`, se for uma pausa longa o bastante para justificar) para retomar e relanÇõar o `codex exec` mais tarde. Vale tanto em `/loop` quanto em sessÇœo comum.

**Regras de shell especÇðficas do ambiente real de trabalho (Windows, terminal com aprovaÇõÇœo de comando por chamada)** ƒ?" achado real de `full-audit-round1-contexto-summary.md` (critÇ¸rio Portabilidade AgnÇüstica): as regras acima descrevem semÇ½ntica de Bash/backgrounding em termos genÇ¸ricos, mas duas regras adicionais sÇü fazem sentido neste ambiente especÇðfico e nÇœo devem ser assumidas como universais ao portar o processo para outro agente/SO: (1) **nunca encadear comandos com `&&`** (nem sÇü dois passos triviais como `cd` + outro comando) ƒ?" cada aÇõÇœo Ç¸ uma chamada de shell separada; encadear dispara um prompt de aprovaÇõÇœo que um agente em background nÇœo consegue responder. (2) **Reafirmar o diretÇürio de trabalho (`cd` isolado, sua prÇüpria chamada) antes de qualquer comando sempre que houver uma lacuna** ƒ?" depois de aguardar notificaÇõÇœo, depois de comando longo, no inÇðcio de cada retomada ƒ?" o diretÇürio de trabalho pode resetar para a raiz entre chamadas/turnos; nÇœo presumir que persiste. (3) **Ao esperar um run de CI/CD recÇ¸m-disparado, nunca faÇõa polling de "existe um run completo"** ƒ?" um run antigo (ex. um deploy que jÇ­ falhou antes) satisfaz essa condiÇõÇœo na hora e produz um "concluÇðdo" falso enquanto o run real ainda nem comeÇõou (achado real, recorrente nesta sessÇœo, 2026-08-31). Capture o `databaseId` do run mais recente ANTES do trigger, espere um `databaseId` diferente aparecer, sÇü entÇœo faÇõa polling desse run especÇðfico atÇ¸ `completed` ƒ?" e confira o estado com uma chamada direta de vez em quando em vez de confiar cegamente numa Ç§nica espera longa em background.

## 5. PrecedÇ¦ncia de fontes

Ver tabela completa em `docs/architecture/README.md`. Resumo: `AGENTS.md` (processo) > ADR aceito (decisÇœo especÇðfica) > documento temÇ­tico corrente (`docs/architecture/*.md`) > `ARCHITECTURE.md` (visÇœo consolidada) > `NEXT_SESSION_PROMPT.md` (estado, nunca normativo) > `docs/architecture/history/` (nunca normativo).

## 6. ManutenÇõÇœo de contexto ƒ?" checklist por marco

Ao concluir uma fase/marco relevante (ex.: fim do Implementation Blueprint, fim de cada fase de implementaÇõÇœo), verificar:

- Estado e prÇüxima aÇõÇœo concordam entre `ARCHITECTURE.md`, `docs/architecture/README.md` e `NEXT_SESSION_PROMPT.md`.
- Nenhum documento em `history/` estÇ­ sendo tratado como normativo em algum lugar.
- ADRs e `decisions-log.md` tÇ¦m status compatÇðvel entre si.
- Arquivos novos em `docs/architecture/` aparecem no Çðndice.
- ReferÇ¦ncias a caminhos de arquivo (`docs/architecture/...`) continuam vÇ­lidas.
- Regras durÇ­veis nÇœo foram duplicadas entre `AGENTS.md`, working-memory e handoff.
- Este `AGENTS.md` continua dentro do limite de tamanho (ver ¶õ8).
- Fatos temporÇ­rios foram removidos de `NEXT_SESSION_PROMPT.md` ou promovidos ao lugar correto.
- Nenhum `.md` novo foi deixado solto na raiz do repo (handoff/prompt/mission-brief pertence a `docs/`, ver a tabela de precedÇ¦ncia em `docs/architecture/README.md`).

`npm run check-docs` (`scripts/check-doc-drift.ts`, bloqueante no CI desde full-audit round1/eixo Engenharia de Contexto) automatiza parte desta checklist: link relativo quebrado entre qualquer `.md` do repo; referÇ¦ncia `AGENTS.md ¶õN` que nÇœo corresponde a um `## N.` real neste arquivo; `.md` novo na raiz fora do allowlist explÇðcito do script; `AGENTS.md`/`NEXT_SESSION_PROMPT.md` excedendo o guardrail de tamanho declarado no prÇüprio script. NÇœo cobre as checagens semÇ½nticas acima (status concordante entre documentos, `history/` tratado como normativo) ƒ?" essas continuam manuais.

## 7. ConvenÇõÇæes e invariantes de cÇüdigo real

Layout por mÇüdulo: `src/modules/<mÇüdulo>/{domain,application,ports,persistence,http}` (lÇügica pura + adapters reais separados); workers assÇðncronos puros em `src/workers/<worker>` (testÇ­veis com relÇügio injetado, deliberadamente observability-agnostic ƒ?" `decisions-log.md` E-007); handlers Lambda reais (o Ç§nico lugar que importa AWS SDK/observability concreta) em `src/runtime/aws/handlers/`. Layout original descrito em `implementation-blueprint.md` ¶õ2/¶õ19; estado de implementaÇõÇœo por milestone vive em `docs/architecture/README.md`/`NEXT_SESSION_PROMPT.md`, nÇœo aqui. `src/shared/observability/security-audit.ts` (taxonomia fechada de eventos de seguranÇõa ƒ?" negaÇõÇœo de autorizaÇõÇœo, acesso a GSI3/GSI6) nunca importa de `src/modules/**` (regra `dependency-cruiser` `shared-must-not-reach-modules`) ƒ?" os call sites passam os valores jÇ­ tipados do lado deles.

**Infra (ADR-0009): CDK substituÇðdo por Terraform**, `infra/` (mÇüdulos + wiring raiz), deploy real via GitHub Actions (`ci.yml` plan-only em PR, `cd.yml` apply em push a `main`, OIDC sem credencial de longa duraÇõÇœo). Regra de isolamento de Çðndice: nunca conceder acesso de leitura a um GSI restrito (GSI3/GSI6) via polÇðtica geral de tabela ƒ?" sÇü via polÇðtica escopada explicitamente ao Çðndice (`infra/modules/dynamo-table/main.tf`), anexada sÇü Çÿs roles que precisam. SuÇðte de infra (`infra/tests/stack.tftest.hcl`, `terraform test`) prova isolamento de GSI3/GSI6, DLQ, alarmes, contrato do EventBridge Scheduler. **Acesso AWS real (verificaÇõÇœo/smoke test, nunca `apply` local) Ç¸ via `aws ... --profile claude-dev`** (conta `dev`) ƒ?" nunca concluir "sem acesso AWS" a partir sÇü do CLI sem profile (retorna `NoCredentials` mesmo quando `claude-dev` funciona). **`terraform force-unlock` no backend `dev` estÇ­ prÇ¸-autorizado sem confirmaÇõÇœo prÇ¸via (Marcelo, 2026-08-31)** ƒ?" lock travado Ç¸ artefato conhecido de mÇ§ltiplas sessÇæes/agentes rodando `terraform plan`/`test`/`apply` neste mesmo backend; `dev` nÇœo tem usuÇ­rio real, o risco de destravar incorretamente Ç¸ aceitÇ­vel aqui (checar `gh run list` antes, para confirmar que nenhum workflow real estÇ­ em andamento, Ç¸ boa prÇ­tica mas nÇœo bloqueia a aÇõÇœo).

Comandos: `npm ci` (install imutÇ­vel, scripts desabilitados via `.npmrc`) ¶ú `npm run typecheck` ¶ú `npm run lint` (ESLint; `no-console` Ç¸ erro fora de `src/shared/observability/**` ƒ?" todo handler usa `SecureLogger`, nunca `console.*`) ¶ú `npm test` (Vitest; unit+contract+integration) ¶ú `npm run check-docs` (drift de documentaÇõÇœo) ¶ú `npm run validate-schemas` (carrega tudo em `schemas/` e falha em `$ref` quebrado) ¶ú `npm run build:lambdas` (bundla os handlers via esbuild antes de `terraform plan`/`apply`). Node fixado em `.nvmrc` (20.x); CI em `.github/workflows/ci.yml` roda os cinco + audit + SBOM (CycloneDX) + `infra` (Terraform), actions pinadas por SHA.

ConvenÇõÇæes: TypeScript estrito (`tsconfig.json`, `noUncheckedIndexedAccess`); erros de app usam a taxonomia normalizada de `src/shared/errors/app-error.ts` (`AppError` + subclasses); toda escrita mutÇ­vel usa os builders de `src/shared/dynamodb/occ.ts` (nunca `UpdateItem`/`PutItem` cru); eventos crÇðticos usam `src/shared/outbox/outbox.ts` dentro do mesmo `TransactWriteItems` do agregado; schemas JSON sÇœo a fonte de verdade dos contratos (`src/shared/contracts/schema-validator.ts` os carrega via Ajv) ƒ?" todo evento/comando novo ganha um schema em `schemas/{events,queues,api}/` com teste de exemplo vÇ­lido e invÇ­lido em `test/contract/`.

**ComentÇ­rios (2026-08-30, padrÇœo jÇ­ observado em todo o cÇüdigo real, agora formalizado)**: comentar o PORQUÇS (uma decisÇœo, um achado real, um trade-off, uma invariante nÇœo Çübvia ƒ?" ex. "GSI4 nunca Ç¸ fonte de autorizaÇõÇœo, Ç¸ eventually consistent"), nunca o QUÇS (o cÇüdigo jÇ­ diz o que faz). Sem comentÇ­rio nenhum quando o porquÇ¦ tambÇ¸m Ç¸ Çübvio. Vale para qualquer agente (Claude Code, Codex CLI).

## 8. ManutenÇõÇœo do prÇüprio AGENTS.md

Meta: 60-100 linhas. Antes de adicionar algo, verificar: muda comportamento em vÇ­rias sessÇæes futuras? Ç% estÇ­vel, nÇœo temporÇ­rio? NÇœo Ç¸ derivÇ­vel do cÇüdigo/Git/decisions-log? NÇœo pertence a `NEXT_SESSION_PROMPT.md`, `working-memory.md` ou a um documento de arquitetura? Se alguma resposta for nÇœo, nÇœo pertence aqui.

# Expiration Tracker ƒ?" Estado Atual + PrÇüxima AÇõÇœo

> Este arquivo Ç¸ estado atual + prÇüxima aÇõÇœo (`AGENTS.md` ¶õ2), nunca fonte normativa e nunca histÇürico narrativo ƒ?" histÇüria detalhada vive em `docs/architecture/{session-log,decisions-log}.md` e nas pastas `reviews/`. Recompactado em 2026-08-29 (reconciliaÇõÇœo de engenharia de contexto, ver `docs/architecture/reviews/context-engineering-reconciliation/`) para parar de acumular narrativa sessÇœo-a-sessÇœo jÇ­ duplicada nesses dois arquivos ƒ?" a versÇœo anterior tinha 1067 linhas, a maior parte delas o histÇürico completo de D-058 a D-083, palavra por palavra jÇ­ presente em `decisions-log.md`.

## Branch / as-of

**NÇœo confie nesta seÇõÇœo sem confirmar.** `git branch --show-current` deve ser `develop`; `git log --oneline -5`, `git status` e `git pull` antes de assumir qualquer coisa abaixo como pendente ou concluÇðdo ƒ?" mÇ§ltiplas sessÇæes/agentes trabalham neste repo em paralelo.

**PadrÇœo de trabalho autÇïnomo (reforÇõado explicitamente por Marcelo, 2026-09-01 ƒ?" vale para toda sessÇœo futura, nÇœo sÇü esta):** o agente deve prosseguir de forma contÇðnua enquanto houver qualquer trabalho de engenharia real a fazer ƒ?" nunca parar para pedir "posso continuar?" nem esperar confirmaÇõÇœo para dar o prÇüximo passo Çübvio. SÇü pausar/perguntar quando o prÇüximo passo depender genuinamente de uma decisÇœo que sÇü Marcelo pode tomar (decisÇœo de produto, execuÇõÇœo destrutiva/irreversÇðvel, gasto real de infra nÇœo trivial, ou algo que o protocolo ClaudeƒÅ"Codex do `AGENTS.md` ¶õ4 exige elevar a ele). Nesses casos: registrar o pendente claramente aqui/no `decisions-log.md`, seguir para a PRÇ"XIMA frente de trabalho independente disponÇðvel (nunca ficar ocioso enquanto houver outra tarefa executÇ­vel), e sÇü entÇœo esperar. PadrÇœo operacional jÇ­ em uso nesta sessÇœo: commit/push incremental para `develop` a cada camada concluÇðda, PRƒÅ'`main` quando a suÇðte estiver verde, watch de CI/CD em background sem bloquear, e uso de `AskUserQuestion` OU mensagem de texto direta apenas para decisÇæes genuinamente dele (ex.: aplicar infra real com custo/IAM novo) ƒ?" nunca para "confirmar que devo continuar o Çübvio".
**Resumo compactado D-199 a D-215 (2026-09-05/06, histÇürico completo em `decisions-log.md`)**: D-199 (bug crÇðtico: e-mail de lembrete real falhava silenciosamente desde M4, `recipientUserId` nunca gravado ƒ?" corrigido). D-200/D-201 fecham o item 13 do backlog P1 (watcher fan-out + MANAGER escalation). D-202: item 14 (busca OCR/full-text) **BLOQUEADO**, pendente decisÇœo de Marcelo entre 3 caminhos nomeados. D-203: 2 fixes mecÇ½nicos (guard `main()` Windows, `RT-LANE-FALLBACK-01`). D-204/D-211/D-212/D-213/D-215 fecham o item 15 (relatÇürios agendados) por completo ƒ?" entidades+GSI8+outbox, scheduler+claim, CRUD, worker de entrega SQS+download autenticado, tudo verificado ao vivo. D-205/D-216/D-217 fecham o item 16 (dossiÇ¦ PDF/Excel) por completo ƒ?" preview/confirm, geraÇõÇœo PDF+XLSX via `pdf-lib`/`exceljs`, download autenticado. D-206/D-207/D-209/D-210 fecham o item 17 (bulk actions) por completo, smoke test de custo real confirmado em `dev`. D-208: migraÇõÇœo de ~52 Lambdas para ARM64, verificada ao vivo. D-214: bug real (`new Date()` em vez de `this.now()` em `searchExpirationItems`) corrigido. DÇðvida tÇ¸cnica nomeada nÇœo bloqueante: `stack.tftest.hcl`/`api_gateway.tftest.hcl` com contagens desatualizadas.

**ContinuaÇõÇœo (mesma sessÇœo, 2026-09-06, depois de D-215)**: **D-216 ƒ?" D-205 fatia 1/3 (`DossierExportRun` + preview/confirm HTTP + `scopeHash`) IMPLEMENTADA**, item 16 do backlog P1 (dossiÇ¦ documental PDF/Excel) comeÇõa a sair do design-only. `DossierExportRun` co-localizado na partiÇõÇœo do Subject (mesma convenÇõÇœo de `requirementKey()`); `POST /document-archive/subjects/{subjectId}/dossier` cria o run em `PREVIEW_READY` congelando os `requirementIds` atuais + devolve as linhas de preview na mesma resposta; `POST .../dossier/{runId}/confirm` idempotente dado um `scopeHash` batendo (fingerprint canÇïnico JSON+SHA-256, extraÇðdo de `search-cursor.ts` para `shared/domain/fingerprint.ts` novo e reusado, nÇœo duplicado) ƒ?" hash divergente Ç¸ sempre 409, confirmar o mesmo run jÇ­ confirmado com o mesmo hash Ç¸ no-op. Confirm bem-sucedido dispara `SQS_DOSSIER_EXPORT_V1` (aditivo, sem consumidor ainda). RBAC nova `docarchive:dossier-export`, `ADMIN_ROLES` exclusivamente (decisÇœo 10 do design). Gate local completo verde: suÇðte **2392/2392** (217 arquivos, 2 testes de arquitetura com `tsc` real), `terraform fmt`/`validate` limpos, `terraform plan` real contra `dev` mostrou exatamente as 2 rotas novas. **Mergeado em `main` (PR #250, `866a762`), CD (`34046861084`) `success` confirmado por polling sÇðncrono do `databaseId` capturado ANTES do merge. VerificaÇõÇœo ao vivo contra `dev` feita**: `exptrk-dev-document-archive-handler` `Active`/`Successful`; as 2 rotas novas presentes na API `uav7id1muh`. Detalhe completo em `decisions-log.md` D-216. **Pendente real, nomeado**: fatias 2 (worker de geraÇõÇœo PDF+XLSX, nova dependÇ¦ncia de Excel, `DossierTooLargeError`, S3) e 3 (rota de download autenticada) seguem nÇœo implementadas ƒ?" hoje sÇü dÇ­ pra fazer preview/confirm, nenhum documento Ç¸ gerado nem baixÇ­vel ainda. **PrÇüxima aÇõÇœo real**: fatia 2 de D-205, ou escopar itens 18-19, a critÇ¸rio de quem retomar.

**ContinuaÇõÇœo (mesma sessÇœo, 2026-09-06, depois de D-216)**: **D-217 ƒ?" D-205 fatia 2-3/3 (worker de geraÇõÇœo PDF+XLSX + rota de download autenticada) IMPLEMENTADA POR COMPLETO, item 16 do backlog P1 (dossiÇ¦ documental) fechado por inteiro.** `processDossierExportGeneration` (`src/workers/dossier-export/generate.ts`) consome `SQS_DOSSIER_EXPORT_V1` (dispatch jÇ­ existia desde D-216): reclama `CONFIRMEDƒÅ'GENERATING` via lease (mesmo padrÇœo de `NotificationAttempt`), declara `TOO_LARGE` (nunca trunca silenciosamente) acima de `MAX_DOSSIER_REQUIREMENTS=200`, omite silenciosamente um Requirement apagado desde o confirm, gera PDF (`pdf-lib`, primeiro uso do projeto para CRIAR um PDF) e XLSX (`exceljs@4.4.0` novo) em paralelo, sobe ambos ao bucket `report_exports` REUSADO (sem bucket novo), marca `READY`/`FAILED`+`failureReason`. MitigaÇõÇœo de injeÇõÇœo de fÇürmula (`sanitizeFormulaInjection`) extraÇðda e aplicada tanto no XLSX quanto retroativamente ao CSV existente. Rota nova `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download?format=pdf|xlsx` ƒ?" mesmo padrÇœo de presign sob demanda de 5min de D-204/D-215, RBAC `ADMIN_ROLES` exclusivamente. Gate local completo verde: suÇðte **2415/2415** (220 arquivos, 2 testes de arquitetura com `tsc` real), `terraform fmt`/`validate`/`plan` limpos contra `dev`. **Mergeado em `main` (PR #252, `bc35727`); CD (`34050910874`) falhou uma vez por contenÇõÇœo real de lock do state Terraform (4 CI de Dependabot rodando `terraform plan` simultÇ½neos no mesmo backend `dev` ƒ?" nÇœo defeito de cÇüdigo), resolvido aguardando os runs concorrentes concluÇðrem + `gh run rerun --failed`, rerun `success` confirmado. VerificaÇõÇœo ao vivo contra `dev` feita**: `exptrk-dev-document-archive-handler`/`exptrk-dev-dossier-export-generation-handler` `Active`/`Successful`; fila+DLQ `exptrk-dev-dossier-export`/`-dlq` existem; event source mapping `Enabled`; rota de download presente na API `uav7id1muh`. Detalhe completo em `decisions-log.md` D-217. **Item 16 do backlog P1 agora COMPLETO por inteiro** (fatias 1-3 de D-205 fechadas). **Pendente real, nÇœo bloqueante**: nunca testado ponta a ponta com previewƒÅ'confirmƒÅ'geraÇõÇœo real disparando via SQS real em `dev` (sÇü G-V3/unit) ƒ?" considerar smoke test real numa sessÇœo futura. **PrÇüxima aÇõÇœo real**: escopar itens 18-19 do backlog P1 (metadata configurÇ­vel por Document Type; compartilhamento externo seguro), a critÇ¸rio de quem retomar.

**ContinuaÇõÇœo (mesma sessÇœo, 2026-09-06, depois de D-217)**: **D-218 ƒ?" Metadata configurÇ­vel por Document Type: design `APPROVED` via protocolo ClaudeƒÅ"Codex, 4 rodadas reais (rÇ¸gua estÇ­vel desde a Rodada 2, Claude 9,2/Codex 9,1; fechamento na Rodada 4, design Claude 9,4/Codex 9,3). DESIGN-ONLY, nenhum cÇüdigo tocado.** E-014 `SIM` (Salesforce/HubSpot/Airtable/Contentful/Confluent). 9 decisÇæes: `DocumentTypeMetadataFieldDefinition[]` embutido em `DocumentType` (caps ativos/total, sem purge fÇðsico algum); valores em `Document` via uniÇœo discriminada por `valueType` (`TEXT`/`NUMBER`/`DECIMAL`/`DATE`/`BOOLEAN`/`SINGLE_SELECT`, `DECIMAL` string normalizada para dinheiro, nunca float); `valueType` IMUTÇ?VEL apÇüs criaÇõÇœo (ponto mais forte, fecha 2 critÇ¸rios do checklist por construÇõÇœo); arquivamento Ç¸ tombstone nunca delete, opÇõÇæes `SINGLE_SELECT` com `optionId` estÇ­vel + `labelSnapshot`; `required` sÇü prospectivo, `createDocument()` nunca toca metadata; RBAC novo `docarchive:documenttype-metadata-manage`/`docarchive:document-metadata-update` (achado real: `docarchive:update` presumido pela Rodada 1 nÇœo existe); 3 rotas HTTP com OCC/fencing transacional explÇðcito (fecha uma corrida TOCTOU real que a Rodada 1 do Codex apontou); taxonomia fechada de 6 tipos; busca por metadata fora de escopo. **Achado de processo**: prompt da Rodada 3 ao Codex omitiu o texto integral da Rodada 2, causando um "achado novo" falso (cap de opÇõÇæes que jÇ­ existia) ƒ?" reconciliado na Rodada 4, liÇõÇœo registrada (sempre incluir texto cumulativo, nunca sÇü o delta). EvidÇ¦ncia completa em `docs/architecture/reviews/document-type-metadata-scoping/` (8 arquivos + `estado-final-consolidado.md`). Detalhe completo em `decisions-log.md` D-218. **PrÇüxima aÇõÇœo real**: implementar em fatias (sugestÇœo nÇœo normativa no `estado-final-consolidado.md`: fatia 1 domÇðnio+CRUD de definiÇõÇæes, fatia 2 rota de valores+fencing, fatia 3 rotas HTTP+RBAC+testes), nÇðvel 3-4 sem protocolo novo ƒ?" ou escopar o Ç§ltimo item do backlog P1 (compartilhamento externo seguro), a critÇ¸rio de quem retomar.

**ContinuaÇõÇœo (mesma sessÇœo, 2026-09-06, depois de D-218)**: **D-219 ƒ?" D-218 fatia 1/3 (domÇðnio + CRUD de definiÇõÇæes de campo/opÇõÇœo) IMPLEMENTADA**, implementaÇõÇœo direta do design jÇ­ `APPROVED`, sem nova rodada de protocolo. `DocumentType.metadataFields?` novo (embutido, mesmo padrÇœo `RequirementTemplate.items`), caps `MAX_ACTIVE_METADATA_FIELDS=20`/`MAX_TOTAL_METADATA_FIELD_DEFINITIONS=100`/`MAX_ACTIVE_OPTIONS_PER_FIELD=50`/`MAX_TOTAL_OPTIONS_PER_FIELD=150`. Dois mÇ¸todos novos em `DocumentArchiveService`: `createDocumentTypeMetadataField` (whole-array replace, mesmo idioma de `updateRequirementTemplate`) e `updateDocumentTypeMetadataField` (rename/required/archive/reactivate do campo **e** `optionsPatch` ADD/RENAME/ARCHIVE/REACTIVATE de opÇõÇœo numa Ç§nica escrita OCC-fenceada ƒ?" opÇõÇœo Ç¸ sub-operaÇõÇœo do mesmo PATCH, nunca mecanismo de concorrÇ¦ncia separado, DecisÇœo 7 de D-218). `valueType` nunca aceito no update (imutÇ­vel por construÇõÇœo, DecisÇœo 3). Arquivamento Ç¸ tombstone puro ƒ?" nenhuma operaÇõÇœo remove campo/opÇõÇœo do array. RBAC novo `docarchive:documenttype-metadata-manage`(`ADMIN_ROLES`)/`docarchive:document-metadata-update`(`WRITE_ROLES`, reservada para fatia 2). **Achado mecÇ½nico esperado (mesma classe de D-216), desta vez corrigido de primeira**: os 2 mÇ¸todos novos no `DocumentArchiveIdGenerator` quebraram o typecheck de 22 fixtures de teste ƒ?" corrigido com script Python (nÇœo perl) que detecta o estilo de id de cada arquivo e insere as linhas certas, sem revert necessÇ­rio. Gate local completo verde: suÇðte **2431/2431** (220 arquivos, 2 testes de arquitetura com `tsc` real), `terraform fmt` limpo (zero mudanÇõa de infra). Detalhe completo em `decisions-log.md` D-219. **Pendente real, nomeado**: fatia 2 (`Document.metadataValues`, uniÇœo discriminada, fencing transacional) e fatia 3 (rotas HTTP, proxy-allowlist, schemas) seguem nÇœo implementadas ƒ?" hoje sÇü existe o catÇ­logo de definiÇõÇæes, nenhum Document tem valor de metadata ainda, nada exposto via HTTP. **PrÇüxima aÇõÇœo real**: fatia 2 de D-218.

**ContinuaÇõÇœo (mesma sessÇœo, 2026-09-06, depois de D-219)**: **D-220 ƒ?" D-218 fatia 2/3 (`Document.metadataValues` + fencing transacional) IMPLEMENTADA**, implementaÇõÇœo direta do design jÇ­ `APPROVED`. `DocumentMetadataValue` novo (uniÇœo discriminada por `valueType`, `DECIMAL` string normalizada para dinheiro, `DATE` civil `YYYY-MM-DD` com validaÇõÇœo de calendÇ­rio real), `Document.metadataValues?` sparse (`createDocument()` nunca toca). `updateDocumentMetadataValues()` novo: valida contra a definiÇõÇœo de campo/opÇõÇœo ATIVA lida fresca, grava numa ÇsNICA `TransactWriteItems` (`Update` no Document OCC `expectedDocumentVersion` + `ConditionCheck` no `DocumentType.version` lido na mesma chamada, `buildVersionConditionCheck`) ƒ?" fecha a corrida TOCTOU da DecisÇœo 7 sem exigir que o cliente conheÇõa a versÇœo do DocumentType. `required` sÇü bloqueia `null` explÇðcito contra campo ATIVO+required, nunca cobre `createDocument()`, para de bloquear quando o campo Ç¸ arquivado. **Achado de engenharia de teste**: a corrida TOCTOU nÇœo Ç¸ testÇ­vel por "mutar antes de chamar" (mÇ¸todo nunca aceita `expectedDocumentTypeVersion` do cliente, sempre relÇ¦ fresco) ƒ?" provada com uma subclasse do store que injeta escrita fora de banda no primeiro `transactWrite` real, simulando genuinamente uma segunda transaÇõÇœo no intervalo de latÇ¦ncia entre a leitura interna e o commit. Gate local completo verde: suÇðte **2444/2444** (220 arquivos, 2 testes de arquitetura com `tsc` real), `terraform fmt` limpo (zero mudanÇõa de infra). Detalhe completo em `decisions-log.md` D-220. **Pendente real**: fatia 3 (3 rotas HTTP, proxy-allowlist, schemas) segue nÇœo implementada ƒ?" mecanismo sÇü alcanÇõÇ­vel via chamada direta ao serviÇõo. **PrÇüxima aÇõÇœo real**: fatia 3 de D-218 (fecha o item 7 do backlog P1 por inteiro), ou escopar o item 8 (compartilhamento externo seguro).

**Resumo compactado D-222/D-225/D-226 (2026-09-07, histÇürico completo em `decisions-log.md`)**: D-222 auditou o item 9 do roadmap P0 e achou o ciclo guest desconectado no ponto central (`issueCredential()` sem call sites ƒ?" nenhum guest recebia link, em nenhum fluxo), mais 3 gaps derivados (DocumentRequest avulso, rejectVersion sem vÇðnculo reverso, sem chasing). D-225 fechou o design do item 19/P1 (`ExternalShareLink`, compartilhamento externo, protocolo completo 4 rodadas, 9,3/9,2). D-226 fechou o design da orquestraÇõÇœo de emissÇœo de credencial cross-Lambda (outbox/SQS assÇðncrono, protocolo completo 7 rodadas, 9,2/9,2) e implementou a fatia 1 (produtor). **D-227 implementou a fatia 2 (consumidor+armazenamento), mas item 9 do roadmap P0 CONTINUA ABERTO ƒ?" correÇõÇœo de um registro anterior que fechou isso prematuramente, ver D-228 abaixo.**

**ContinuaÇõÇœo (sessÇœo nova, 2026-09-07, depois de D-226)**: **D-227 ƒ?" D-226 fatia 2 (consumidor guest da emissÇœo de credencial) IMPLEMENTADA.** Consumidor novo na Lambda guest (`document-request-credential-issuance-service.ts` + handler dedicado) consumindo `SQS_DOCUMENT_REQUEST_CREDENTIAL_ISSUANCE_V1` via Event Source Mapping de fila (nÇœo Streams ƒ?" correÇõÇœo factual), transaÇõÇœo atÇïmica das 4 entradas desenhadas em D-226. `createDocumentRequest()` avulso novo (Achado 2). `rejectVersion()` (Achado 3) reabre a Request via `issuanceGeneration+1`, revoga a credencial antiga, enfileira nova emissÇœo. **Achado de processo**: a fatia ficou paralisada por 3 nÇðveis sucessivos de subagentes que sÇü redelegavam a mesma tarefa sem executÇ­-la; a sessÇœo orquestradora interveio diretamente e fechou. Gate local completo verde, suÇðte **2504/2504**, `terraform fmt`/`validate`/`test` 23/23. Mergeado em `main` (PR #259), CI/CD `success`, Lambda/tabela confirmados `Active`/`ACTIVE` ao vivo. **CORREÇÎÇŸO IMPORTANTE, achada na verificaÇõÇœo ao vivo pÇüs-merge**: esta fatia grava o material bruto da credencial na tabela dedicada, mas o WORKER DE ENTREGA (que leria a tabela via DynamoDB Streams e enviaria de fato o link ao guest) nÇœo foi construÇðdo ƒ?" confirmado por `aws lambda list-event-source-mappings --event-source-arn <stream-arn>` vazio, e pelo prÇüprio comentÇ­rio do cÇüdigo (`guest-credential-delivery-table/main.tf`, `guest-credential-delivery.ts`) que documenta isso como "future worker not built yet". **Resultado prÇ­tico: nenhum guest recebe um link utilizÇ­vel ainda.** AlÇ¸m disso, `DocumentRequest` nÇœo modela nenhum campo de contato (e-mail/telefone) do guest ƒ?" de onde o worker de entrega saberia para quem enviar Ç¸ uma pergunta em aberto, achado novo nÇœo coberto pelo design de D-226. Detalhe completo em `decisions-log.md` D-227 (linha corrigida). **Item 9 do roadmap P0 permanece ABERTO.**

**ContinuaÇõÇœo (mesma sessÇœo, depois da correÇõÇœo de D-227)**: **D-228 ƒ?" worker de entrega real IMPLEMENTADO, item 9 do roadmap P0 fecha para o caminho avulso, mas NÇŸO por inteiro.** `DocumentRequest.recipientEmail?: string` novo (mesmo precedente jÇ­ usado pelo mÇüdulo `subject`), populado sÇü por `createDocumentRequest()` avulso. Worker novo (`src/workers/guest-credential-delivery/deliver.ts` + handler `guest-credential-delivery-handler.ts`) consome a tabela `guest-credential-delivery` via DynamoDB Streams + Event Source Mapping NOVO (filtro INSERT, `maximum_retry_attempts=3`, `destination_config.on_failure`ƒÅ'fila nova `GuestCredentialDeliveryFailures`, metadados apenas), idempotente sob redelivery (marcador Put-condicional na prÇüpria tabela dedicada), envia por `SesEmailAdapter` jÇ­ existente. Teste `guest-credential-issuance-to-delivery-e2e.test.ts` prova o ciclo completo criarƒÅ'emitirƒÅ'entregarƒÅ'`resolveCredential()` real. **Falta, nomeado**: o caminho `DocumentRequestSeries`/recorrÇ¦ncia nÇœo carrega `recipientEmail` ƒ?" requests materializados por recorrÇ¦ncia continuam sem destinatÇ­rio, o worker os pula sempre. DecisÇœo de produto pendente para Marcelo (adicionar contato Çÿ sÇ¸rie?). Ver item 9 do roadmap abaixo e `decisions-log.md` D-228.

**ContinuaÇõÇœo (sessÇœo nova, 2026-09-07, depois de D-228/D-229)**: **D-230 ƒ?" fecha por inteiro a Ç§ltima pendÇ¦ncia nomeada por D-228: `DocumentRequestSeries` (caminho de recorrÇ¦ncia) ganha `recipientEmail`. Item 9 do roadmap P0 agora ÐYY½ FECHADO POR INTEIRO.** A pedido explÇðcito de Marcelo, este gap (mesmo sendo uma extensÇœo aditiva com precedente jÇ­ convergido 2x) passou pelo protocolo ClaudeƒÅ"Codex completo ANTES da implementaÇõÇœo, nÇœo decidido solo ƒ?" mudanÇõa de processo desta sessÇœo. 3 rodadas reais (`codex exec`, nota cega): 7,6 ƒÅ' 8,6 ƒÅ' 9,2/10 (Claude 9,2/Codex 9,2, ambos ƒ%¾9,0 sem arredondar). **ReclassificaÇõÇœo de risco em rodada, achado real do Codex**: a proposta inicial (NÇðvel 3, campo aditivo) subestimava que a Rodada 2 introduz uma AÇÎÇŸO nova (`updateSeriesRecipient`, mutaÇõÇœo/remoÇõÇœo via `null`, rota HTTP+schema novos) ƒ?" reclassificado para **NÇðvel 5** (rÇ¸gua manda tratar como o nÇðvel mais alto na dÇ§vida). DecisÇœo final: `DocumentRequestSeries.recipientEmail?`/`CreateDocumentRequestSeriesInput.recipientEmail?` (trimmed, nunca lower-cased); `buildMaterializeAttemptEntries` copia o valor para CADA `DocumentRequest` materializado (snapshot imutÇ­vel, cobre tanto `materializeAttempt` interativo quanto o worker periÇüdico `materializer.ts` ƒ?" mesmo builder); `updateSeriesRecipient()` novo (reaproveita a action `docarchive:series-update` jÇ­ existente, sem rota prÇüpria antes; `null`ƒÅ'`REMOVE` no DynamoDB, nunca `null` persistido; rejeita sÇ¸rie `CANCELLED`); rota `POST /document-archive/series/{subjectId}/{seriesId}/recipient` + schema novo. G-V3 real: `guest-credential-issuance-to-delivery-e2e.test.ts` ganhou 2 cenÇ­rios (sÇ¸rie COM `recipientEmail` entrega de verdade; sÇ¸rie SEM continua fazendo skip terminal, nÇœo regressÇœo) + 17 testes unitÇ­rios novos em `document-request-recurrence.test.ts` + 1 no worker periÇüdico. Gate local completo verde: suÇðte **2572/2572** (233 arquivos), `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. EvidÇ¦ncia completa das 3 rodadas em `docs/architecture/reviews/document-request-series-recipient-scoping/`. Detalhe completo em `decisions-log.md` D-230.

**ContinuaÇõÇœo (mesma sessÇœo, 2026-09-06, depois de D-220)**: **D-221 ƒ?" D-218 fatia 3/3 (rotas HTTP + proxy-allowlist + schemas) IMPLEMENTADA, item 7 do backlog P1 (metadata configurÇ­vel por Document Type) fechado por inteiro.** 3 rotas novas no Lambda `document-archive-handler`: `POST .../document-types/{documentTypeId}/metadata-fields`, `PATCH .../metadata-fields/{fieldId}` (campo e `optionsPatch` de opÇõÇœo no mesmo corpo, DecisÇœo 7), `PATCH /document-archive/documents/{documentId}/metadata-values`. 3 schemas novos (`valueType` deliberadamente ausente do schema de update de campo ƒ?" imutÇ­vel; `values` Ç¸ mapa `fieldId`ƒÅ'`oneOf` de `null`+6 variantes por tipo, mesmo padrÇœo de `import-mapping-request.v1.json`). Wireado em `infra/modules/api-gateway/main.tf` e `proxy-allowlist.ts`. Gate local completo verde: suÇðte **2464/2464** (221 arquivos, 2 testes de arquitetura com `tsc` real), `terraform fmt`/`validate`/`test` (23/23) limpos, `terraform plan` real contra `dev` mostrou exatamente as 3 rotas novas. Detalhe completo em `decisions-log.md` D-221. **Item 7 do backlog P1 agora COMPLETO por inteiro** (fatias 1-3 de D-218 fechadas). **Pendente real, nÇœo bloqueante**: nunca testado ponta a ponta contra `dev` real via HTTP autenticado (sÇü G-V3/unit+integraÇõÇœo in-memory). **PrÇüxima aÇõÇœo real**: escopar o item 8 do backlog P1 (compartilhamento externo seguro, ainda nÇœo escopado) ƒ?" Ç§ltimo item do backlog P1.

**ContinuaÇõÇœo (sessÇœo nova, 2026-09-07)**: **D-224 ƒ?" item 6 de D-173, fatia de engenharia isolada ƒ?" IMPLEMENTADA.** D-184 jÇ­ tinha REJEITADO tornar `documentTypeId` obrigatÇürio no guest submit por falta de mecanismo de descoberta; esta sessÇœo implementa esse mecanismo: rota nova `GET /document-archive/guest/document-requests/{token}/document-types` (`handleListGuestDocumentTypes` + `GuestDocumentAccessService.listActiveDocumentTypesForGuest()`, mesma disciplina anti-enumeraÇõÇœo de `resolveCredential`), retornando sÇü `DocumentType`s `ACTIVE` do tenant do token. `docarchive-guest-submit-evidence-request.v1.json` DELIBERADAMENTE nÇœo alterado ƒ?" tornar `documentTypeId` obrigatÇürio agora Ç¸ viÇ­vel mas Ç¸ decisÇœo de PRODUTO, pendÇ¦ncia explÇðcita para Marcelo. G-V3: 5 testes novos. Gate local limpo; suÇðte completa teve 3 falhas causadas por WIP alheio de D-223 jÇ­ presente na Ç­rvore, nÇœo relacionado. Detalhe completo em `decisions-log.md` D-224. **PrÇüxima aÇõÇœo real**: Marcelo decidir a pendÇ¦ncia de produto (documentTypeId obrigatÇürio ou opcional).
**ContinuaÇõÇœo (sessÇœo nova, 2026-09-07, depois de D-221/D-224, docs-only via worktree isolado)**: **D-225 ƒ?" Compartilhamento Externo Seguro (`ExternalShareLink`, item 19/Ç§ltimo item do backlog P1), design `APPROVED` via protocolo ClaudeƒÅ"Codex, 4 rodadas reais (`codex exec`), fechamento Codex nota cega pesquisa 9,3/design 9,2 (ambos ƒ%¾9,0, sem arredondar). DESIGN-ONLY, nenhum cÇüdigo tocado.** E-014 `SIM` (OWASP Cheat Sheets, Dropbox/Google, AWS presigned URL guidance). DecisÇæes principais: `ExternalShareLink` co-localizada na partiÇõÇœo do `Document`, `documentVersionId`/`documentFileId` CONGELADOS na criaÇõÇœo; token no PATH; `status: ACTIVE|REVOKED` sÇü; criaÇõÇœo transacional (`ConditionCheck`s de tenant/`scanStatus`/`state`, nomes reais confirmados por leitura) + cap atÇïmico `Document.activeExternalShareLinkCount` com reconciliaÇõÇœo lazy; revalidaÇõÇœo de acesso restrita a `scanStatus`+`cleanObject`+tenant+link ativos, nunca revalida `DocumentVersion.state`/`Document.status`; RBAC `docarchive:share-link-{create,revoke,list}` novo, `ADMIN_ROLES`; sem senha em v1 (bearer-token, consistente com D-143), PIN opcional Ç¸ fatia futura. Achado do Codex a nÇœo esquecer na implementaÇõÇœo: os deltas do contador de cap devem ser uma ÇsNICA operaÇõÇœo `Update` sobre `Document` na mesma transaÇõÇœo. EvidÇ¦ncia completa em `docs/architecture/reviews/external-sharing-scoping/` (9 arquivos + `estado-final-consolidado.md`); detalhe completo em `decisions-log.md` D-225. **Backlog P1 agora tem todos os itens com design `APPROVED`.** **PrÇüxima aÇõÇœo real**: implementar D-225 em fatias (domÇðnio+token+cap; acesso anÇïnimo+presign+auditoria; revogaÇõÇœo+listagem+RBAC+rotas HTTP), nÇðvel 3-4 sem protocolo novo.

**Estado em 2026-09-02 (fim de sessÇœo, continuaÇõÇœo da sessÇœo anterior no mesmo fio)**: terminou o spike de auditoria 4/9/10 de D-161 e avanÇõou a decisÇœo fundacional #1 do zero atÇ¸ uma primeira fatia de cÇüdigo real, tudo confirmado ao vivo contra `dev`/CI:
1. **D-162** (bug fix mecÇ½nico, nÇðvel 1-2, sem protocolo): `createDocument()` nunca gravava GSI2 (Documents-by-Subject) apesar de comentÇ­rio afirmando o contrÇ­rio ƒ?" achado de D-161, confirmado por leitura direta, corrigido, G-V3 ao vivo. PR #179, CD `success`.
2. **D-163** (`DocumentFile`, decisÇœo fundacional #1 de D-161): protocolo ClaudeƒÅ"Codex completo, 4 rodadas (5,2ƒÅ'7,1ƒÅ'8,3ƒÅ'9,4/10). Reclassificada de "provavelmente nÇðvel 3-4" (estimativa otimista do handoff anterior) para **nÇðvel 5 confirmado** ƒ?" falta um subsistema inteiro (entidade+storage+invariantes transacionais+reconciliaÇõÇœo), nÇœo um data-write num campo jÇ­ modelado. Fechada `APPROVED (design)` ƒ?" ver `docs/architecture/reviews/document-file-scoping/`.
3. **D-164** (implementaÇõÇœo real, primeira fatia): entidade `DocumentFile`, `reserveFiles()` (fecha a corrida de PRINCIPAL numa `TransactWriteItems` real, nÇœo sÇü validaÇõÇœo de input), `ConditionCheck` transacional do PRINCIPAL em `acceptVersion()`. G-V3 confirmado ao vivo nos dois mecanismos. PR #180, CD `success`, `QUARANTINE_BUCKET_NAME` confirmado ao vivo no Lambda via `aws lambda get-function-configuration --profile claude-dev`.
4. **D-165/D-166/D-167/D-168** (implementaÇõÇœo real, fatias 2 a 6, todas 2026-09-02): transiÇõÇœo terminal do scan + correlaÇõÇœo de evidÇ¦ncia simÇ¸trica (D-165); worker de reconciliaÇõÇœo GSI5 (D-166); presign real + rota HTTP de `reserveFiles()` (D-167); gate `fileSetSealed` em `commitUpload()`, fechando o arco inteiro (D-168). `DocumentFile` (D-163) estÇ­ **100% COMPLETO**. Ver "PrÇüxima aÇõÇœo" abaixo para o detalhe de cada fatia e o prÇüximo item real (item 8 da macro-ordem de D-161, Document Types configurÇ­veis).

**Resumo compactado D-172/D-174 a D-179 (2026-09-02, histÇürico completo em `decisions-log.md`)**: D-172 (`EphemeralTelemetryMutation`, D-D de D-136). D-173's design `DocumentType` implementado em fatias: D-174 (domÇðnio+CRUD catÇ­logo), D-175 (`createDocument()` fenceado por `DocumentType.status=ACTIVE`; `submitEvidence()` guest deliberadamente intocado, decisÇœo de produto pendente), D-176 (`documentType`ƒÅ'`documentTypeId` renomeado ponta a ponta no caminho autenticado), D-177 (rotas HTTP do catÇ­logo), D-178 (fix mecÇ½nico: rota `reserveFiles()` faltando em `proxy-allowlist.ts`). D-179: design `MaintenanceDueIndex` (GSI8) `APPROVED` via protocolo completo (4 rodadas, 9,2/9,3) ƒ?" resolve starvation estrutural em 9 workers de manutenÇõÇœo (`Scan`+`Limit` sem cursor persistido).

**D-180 a D-190 (2026-09-02/03) ƒ?" D-179 (`MaintenanceDueIndex`) EXECUÇÎÇŸO COMPLETA, 9/9 workers migrados para GSI8, todos verificados ao vivo contra `dev`** (histÇürico fatia-a-fatia completo em `decisions-log.md` D-180 a D-190, cada um com PR/CI/CD reais e prova `aws iam simulate-principal-policy` de isolamento cross-namespace): `membership-purge` (D-180/D-181, piloto), `invitation-purge` (D-182), `document-file-reconciliation` (D-183, achado real: GSI5 nunca teve writer, cÇüdigo morto desde sempre), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186, 2 tipos de entidade sem `version`), `security-audit-purge` (D-187, 4 tipos de entidade, ponteiro gravado nas funÇõÇæes `build*Event()` em vez de em cada call site), `transient-purge` (D-188, achado de fronteira `shared-must-not-reach-modules` corrigido reimplementando a fÇürmula localmente), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190, `TransactWriteItems` em vez de `DeleteCommand` simples). Achados fora de escopo registrados, nÇœo bloqueantes: `ReminderPolicy.deletedAt` nunca tem write path real (D-190); `document_archive_handler` HTTP tem a mesma lacuna de IAM `TransactWriteItems` que os workers tinham antes de migrar (D-183). **Pendente real ainda aberto**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` nunca casa no Windows/Git Bash deste ambiente ƒ?" corrigido sÇü em 5 dos 11 scripts de backfill/reset (`quota-telemetry-purge`/`security-audit-purge`/`transient-purge`/`delivery-record-purge`/`core-user-data-purge`, escritos jÇ­ com o fix); os outros 6 (`document-file-reconciliation`/`invitation-purge`/`membership-purge`/`requirement-reindex`/`backfill-reminder-policies`/`reset-dev-data`) seguem com o bug ƒ?" reverificar/reexecutar seus backfills contra `dev` antes de confiar nos ponteiros GSI8 daquelas fatias.

**ContinuaÇõÇœo (2026-09-03, sessÇœo nova, depois de D-190 ƒ?" PRIMEIRO ITEM DO ROADMAP DE LANÇÎAMENTO)**: **D-191 ƒ?" `RequirementTemplate` (item 1/P0.1 do roadmap), design `APPROVED` E implementado ponta a ponta na mesma sessÇœo.** Protocolo ClaudeƒÅ"Codex completo, 4 rodadas reais (`codex exec`), com a rÇ¸gua contestada na Rodada 1 e reconciliada em 4 versÇæes (fluxo de `research-protocol.md`): R1 Claude 8,2 / Codex rÇ¸gua 6,4 design 6,6 ƒÅ' R2 9,2/9,1 vs 8,3/8,1 ƒÅ' R3 9,4/9,3 vs 8,8/8,7 ƒÅ' **R4 9,4/9,3 vs 9,1/9,0**, ambos ƒ%¾9,0 nas duas dimensÇæes sem arredondar. E-014 `SIM PARCIAL` com 5 fontes externas datadas (Drata, Vanta, Asana, Atlassian, HighLevel) ƒ?" padrÇœo convergente encontrado: **materializaÇõÇœo por CÇ"PIA no apply, nunca live-link**. Escopo real confirmado por leitura direta: a entidade-alvo Ç¸ `Requirement` (document-archive, D-143/D-145), NÇŸO a legada `RequirementAssignment` (que declara o deferimento e fica intocada). Entregue: entidade + itens embutidos (cap 30) + ponteiro de nome do template + `RequirementNamePointer` (torna "nome de `Requirement` Ç§nico por Subject" uma regra TRANSACIONAL, nÇœo um read-then-write, e essa regra foi **decidida explicitamente como decisÇœo de produto** com gatilho de reversÇœo nomeado, nÇœo inferida do precedente de `DocumentType`); 9 operaÇõÇæes (create/get/list/update/duplicate/archive/unarchive/preview/apply); 9 rotas HTTP wireadas em `api-gateway/main.tf` E `proxy-allowlist.ts` (disciplina D-117/D-120/D-178, asseverada por teste no ato); 7 schemas + 11 testes de contrato; RBAC novo (catÇ­logo `ADMIN_ROLES`, `apply` `WRITE_ROLES`, leitura `READ_ONLY_ROLES`). **5 achados reais fechados, todos levantados pelo Codex e verificados por leitura de cÇüdigo, nÇœo por argumento**: `createRequirement` nunca verificava o Subject (criava `Requirement` sob Subject inexistente/ARCHIVED ƒ?" lacuna prÇ¸-existente, agora fenceada na transaÇõÇœo); `status <> DELETED` deixaria `ARCHIVED` passar; dois itens de template normalizando para o mesmo nome virariam dois `Put` na MESMA chave numa transaÇõÇœo (500 opaco em vez de 409); o fence do template passa a ser `status = ACTIVE AND version = <lida>`; e nenhum erro afirma causa que o DynamoDB nÇœo revelou (releitura que escolhia entre 404/409 removida). Contrato do apply: `2N+3` aÇõÇæes, cap 30 abaixo do teto 32 imposto pelo pior caso previsto `3N+3`. **Erro real meu corrigido na Rodada 4**: a justificativa anterior da reserva do cap era aritmeticamente impossÇðvel. Gate local completo verde: `typecheck`, `lint`, **1931/1931** (`test/unit`+`integration`+`contract`, 22 testes adversariais G-V3 novos), `check-docs`, `validate-schemas`, `build:lambdas`, `dependency-cruiser` 0 violaÇõÇæes, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). Nota de ambiente: os 2 arquivos de `test/architecture/` falham neste worktree por falta de `node_modules` para processos filhos (`tsc`/`dependency-cruiser`) e falham IDENTICAMENTE na Ç­rvore sem a mudanÇõa (confirmado por `git stash`) ƒ?" nÇœo Ç¸ regressÇœo; rodar `npm test` inteiro no checkout principal antes do merge. Detalhe completo em `decisions-log.md` D-191 e `docs/architecture/reviews/requirement-template-scoping/` (9 arquivos).

**VerificaÇõÇœo ao vivo de D-191 ƒ?" FEITA E CONFIRMADA (2026-09-03)**: PR #219 mergeado (`72ce4bb`ƒÅ'`4c90d44`), CI real (`33713015703` em `develop`, `33713051982` no PR, `33713271496` em `main`) e CD real (`33713455836`) todos `success`, confirmados via `gh run view` direto. Contra `dev` via `aws --profile claude-dev`: `exptrk-dev-document-archive-handler` `Active`/`Successful` (`LastModified` 2026-09-03T04:07:29Z, ou seja, o deploy desta fatia); **as 9 rotas `requirement-templates` confirmadas presentes** na API `uav7id1muh` (`aws apigatewayv2 get-routes`) ƒ?" `POST`/`GET` da coleÇõÇœo, `GET`/`PATCH` do item, e `duplicate`/`archive`/`unarchive`/`preview`/`apply`. Nota importante de mÇ¸todo: o CI verde **tambÇ¸m** confirmou que os 2 arquivos de `test/architecture/` passam num ambiente com `node_modules` ƒ?" as falhas observadas durante a implementaÇõÇœo eram artefato do worktree isolado (processos filhos `tsc`/`dependency-cruiser` sem resoluÇõÇœo de mÇüdulo), como o `git stash` jÇ­ havia indicado, e nÇœo regressÇœo.

**Pendentes reais deixados por D-191, nomeados explicitamente:**
1. **`RT-LANE-FALLBACK-01`** (achado prÇ¸-existente, fatia prÇüpria, nÇðvel 3-4): `src/shared/tenant-lifecycle/tenant-business-mutation.ts` linhas 213-223 classifica `CancellationReasons` ausente/malformado como `TenantNotActiveError`, afirmando uma causa que o DynamoDB nÇœo revelou. O correto sob o critÇ¸rio C7 de D-191 Ç¸ um `ConflictError` genÇ¸rico/indeterminado. Muda comportamento de uma lane compartilhada por todos os escritores tenant-scoped, por isso NÇŸO foi feito no meio de D-191 (mesma disciplina de D-177ƒÅ'D-178). Teste adversarial exigido: injetar `CancellationReasons` ausente e exigir `ConflictError`.
2. **Backfill de `RequirementNamePointer`** (`scripts/backfill-requirement-name-pointers.ts`, ainda NÇŸO escrito): `Requirement` jÇ­ existentes em `dev` nÇœo tÇ¦m ponteiro, entÇœo um homÇïnimo ainda Ç¸ criÇ­vel atÇ¸ que sejam renomeados/recriados. NÇœo Ç¸ migraÇõÇœo de risco (`AGENTS.md` ¶õ1), Ç¸ estabelecer a baseline. Escrever jÇ­ com o guard `fileURLToPath(import.meta.url) === process.argv[1]` (bug de D-186).
3. **Auditoria por `Requirement`**: `document-archive-service.ts` nÇœo emite `SubjectAuditEvent` (o `RequirementService` legado emite). O cap 30 de D-191 jÇ­ foi dimensionado para que fechar isso (`3N+3`) nÇœo exija revisitÇ­-lo. 4. O bug do guard `main()` em 6 scripts de backfill (item carregado de D-186ƒÅ'D-190) continua aberto, inalterado por D-191.
**ContinuaÇõÇœo (2026-09-03, depois de D-191)**: **D-192 ƒ?" Bulk Import Documents+Requirements+Column Mapping (item 2/P0), design `APPROVED` (5 rodadas ClaudeƒÅ"Codex). FECHADO POR COMPLETO nesta sessÇœo**: 9¶¦/Ç§ltima fatia (`GET /import-jobs/{jobId}/schema` + `POST /import-jobs/{jobId}/mapping`, `FIELD_CATALOG`, `SQS_IMPORT_PARSE_V1`), gate local completo (suÇðte 2026/2026, `terraform test` 22/22), PR #221 mergeado (`68c2d98`), CI/CD `success`, **verificaÇõÇœo ao vivo contra `dev` confirmada** (Lambdas `Active`, rotas/filas/event-source-mappings presentes) ƒ?" detalhe completo em `decisions-log.md` D-192. **Pendente nÇœo bloqueante**: Range GET real de 64 KiB (hoje lÇ¦ o CSV inteiro, seguro dado o cap 5 MiB/5000 linhas); pendentes herdados de D-191 seguem abertos (ver lista acima).

**ContinuaÇõÇœo (2026-09-03, depois de D-192) ƒ?" D-193 (itens 2-9/9 do design), FECHADO POR COMPLETO em 9 fatias sequenciais**: histÇürico fatia-a-fatia completo (GSI9/`GSI_EVIDENCE`, `requirement-evidence-refresh`/`-daily-sweep` workers, feature flags STARTER/PROMOTER com gate de ordem-por-construÇõÇœo, rename `document-classifier.ts`ƒÅ'`document-format-classifier.ts`) compactado aqui ƒ?" ver `decisions-log.md` D-193 (linha de fechamento) para o detalhe completo. PR #223 mergeado em `main` (`20b7bd8`), CD `success`, **verificado ao vivo contra `dev`** nos 6 pontos exigidos (Lambdas `Active`, fila+DLQ, EventBridge schedule, AppConfig flags, GSI9 `ACTIVE`). SuÇðte final **2151/2151**. **Flags mantidos OFF deliberadamente** (nenhuma fatia testou ponta a ponta contra `dev` real ainda) ƒ?" pendente pequeno, nÇœo bloqueante, reversÇðvel a qualquer momento.

## Roadmap de lanÇõamento (`docs/project/roadmap-competitivo-2026-09-01.md`) ƒ?" PRÇ"XIMA FRENTE DE TRABALHO AGORA (D-179 fechado)

Marcelo confirmou nesta sessÇœo (2026-09-02): terminar todos os workers de D-179 primeiro (feito ƒ?" 9/9, ver D-190 acima), e SÇ" DEPOIS entrar na fila de funcionalidades do roadmap de lanÇõamento. Item 1 (Requirement Templates, D-191) e item 2 (Bulk import, D-192) jÇ­ fechados. **Marcelo decidiu o fornecedor do item 3 nesta sessÇœo (2026-09-04): Meta Cloud API direto (nÇœo BSP)** ƒ?" desbloqueia o item, ver linha 3 abaixo.
`docs/project/roadmap-competitivo-2026-09-01.md` Ç¸ a fonte normativa da ordem de funcionalidades **P0** necessÇ­rias antes do lanÇõamento comercial (11 itens, ordem jÇ­ decidida no documento ƒ?" nÇœo reordenar sem motivo real), seguida do backlog pÇüs-lanÇõamento (P1/P2/Futuro, ¶õ"Roadmap de funcionalidades futuras" do mesmo documento ƒ?" ver bloco dedicado abaixo). Estado real de cada item P0, reauditado em 2026-09-04 (a versÇœo anterior desta lista estava desatualizada em 2 itens ƒ?" ver D-196 em `decisions-log.md`):
1. **Requirement Templates** ƒ?" ÐYY½ **IMPLEMENTADO (D-191, 2026-09-03)**, gate local completo verde; mergeado (PR #219) e **confirmado ao vivo contra `dev`** (Lambda `Active`/`Successful`, 9 rotas presentes). Sobre `Requirement` (document-archive), nÇœo sobre a legada `RequirementAssignment`, que segue deferida como o prÇüprio arquivo declara.
2. **Bulk onboarding/importaÇõÇœo em massa** ƒ?" ÐYY½ **IMPLEMENTADO (D-192, 2026-09-03)**, cobre `TrackedSubject`+`Document`+`Requirement` ponta a ponta (uploadƒÅ'mappingƒÅ'parseƒÅ'previewƒÅ'commit), gate local completo verde; PR/mergeƒÅ'`main`+verificaÇõÇœo ao vivo ver estado no bloco D-192 acima.
3. **WhatsApp operacional** ƒ?" ÐYY­ **design `APPROVED` (D-197) + fatia 1/5 (D-223) + fatia 2/5 (D-229) + fatia 3/5 (D-231, 2026-09-08) IMPLEMENTADAS**. Marcelo escolheu Meta Cloud API direta (nÇœo BSP). Ver `docs/architecture/reviews/whatsapp-channel-scoping/estado-final-consolidado.md` (10 decisÇæes, D-1 a D-10) e `ADR-0012-whatsapp-cloud-api-channel.md`. **Fatia 3/5 (D-231) resumo**: webhook handler (D-7) ƒ?" `whatsapp-webhook-processor.ts` (verificaÇõÇœo `X-Hub-Signature-256`/handshake `GET`/parsing de status), `whatsapp-webhook-workflow.ts` (mirror de `ses-callback-workflow.ts`, `WebhookInbox` **permanentemente account-scoped**, `purgeScope: "ACCOUNT"`), `whatsapp-webhook-handler.ts` (rota pÇ§blica nova `GET+POST /webhooks/whatsapp`, `authorization_type=NONE`, segunda rota pÇ§blica do projeto). Secrets Manager real (D-10) via `secrets-manager-whatsapp-config.ts` (um secret JSON, accessToken/appSecret/phoneNumberId/wabaId/verifyToken) substituindo os env vars placeholder de D-229 em `whatsapp-delivery-handler.ts`. Kill switch de 2 flags (`WHATSAPP`+`WHATSAPP_DELIVERY_WORKER_ENABLED`, `whatsapp-activation.ts`, mesmo mecanismo de D-193 slice 8/9). **2 gaps reais de modelagem achados e resolvidos via protocolo ClaudeƒÅ"Codex completo ANTES de implementar** (nÇœo decidido solo ƒ?" pedido explÇðcito do dono do projeto): (1) escopo de purge do `WebhookInbox` WhatsApp sem tenant (4 rodadas, 9,2/9,2) ƒ?" `purgeScope`/`accountScopedTransientPurgeGsi8Keys()`/dois ramos em `transient-purge/purge.ts`; (2) `buildVersionedUpdate()` exige um `tenantId` real que essa entidade nunca tem (3 rodadas, 9,3/9,2) ƒ?" nÇ§cleo privado `buildScopedVersionedUpdate()` + `buildAccountScopedVersionedUpdate()` novo em `occ.ts`. Ver `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/estado-final-consolidado.md`. Gate local completo verde (typecheck/lint/check-boundaries/check-docs/validate-schemas/build:lambdas, suÇðte **2625/2625**, `terraform fmt`/`validate` limpos, `terraform test` do stack principal 23/23). Detalhe completo em `decisions-log.md` D-231. **PENDENTE, nÇœo feito nesta sessÇœo (orÇõamento de tempo)**: `terraform plan`/`apply` real contra `dev`, PRƒÅ'`main`, CI/CD, verificaÇõÇœo ao vivo ƒ?" o terraform desta fatia nunca rodou contra AWS real, sÇü `validate`/`test` locais; revisar o plan real antes de mergear (toca `module.whatsapp_delivery`, jÇ­ deployado, mudando shape de env var de placeholder para Secrets Manager). Achado incidental nÇœo relacionado: `infra/modules/api-gateway` isolado tem 3 asserÇõÇæes de contagem de rota prÇ¸-existentes jÇ­ quebradas (10ƒÅ'13 items/21ƒÅ'22 subjects/3ƒÅ'5 imports, `git diff` confirma que esta fatia nÇœo tocou essas rotas) ƒ?" dÇðvida de outra sessÇœo concorrente, nÇœo investigada aqui. **PrÇüxima aÇõÇœo real**: (a) revisar `terraform plan` real contra `dev` e completar merge/CD/verificaÇõÇœo ao vivo da fatia 3/5; (b) fatia 4/5 (quota de 24h D-8 + IAM dedicada) ƒ?" nenhuma rodada nova de protocolo esperada salvo achado real.
4. **IA/OCR integrada ao novo Document Lifecycle** ƒ?" ÐYY½ **IMPLEMENTADO por completo (D-193, 2026-09-03)**, 9/9 itens do design fechados, PR #223 mergeado em `main`, CD `success`, **confirmado ao vivo contra `dev`** nos 6 pontos exigidos (Lambdas, fila+DLQ, EventBridge schedule, AppConfig flags, GSI9 ƒ?" ver bloco "D-193 FECHADO POR COMPLETO" acima). **Pendente pequeno e nomeado, nÇœo bloqueante**: os dois feature flags (`EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED`/`DOCUMENT_ARCHIVE_PROMOTION_ENABLED`) seguem OFF por escolha deliberada ƒ?" o mecanismo existe em `dev` mas ainda nÇœo processa nenhum documento `document-archive` real via OCR; ativaÇõÇœo Ç¸ decisÇœo futura, reversÇðvel a qualquer momento, sem urgÇ¦ncia dado "sem usuÇ­rios reais".
5. **Busca e filtros documentais** ƒ?" ÐYY½ **IMPLEMENTADO, fatias 1-3 (achado sÇü nesta sessÇœo, 2026-09-04 ƒ?" mergeado em sessÇœo anterior sem registro aqui nem em `decisions-log.md`, ver D-196)**: `8ac729c` (fatia 1, `UnifiedValidityState`+adaptadores), `3e081c9` (fatia 2, `Requirement.assigneeUserId`+reatribuiÇõÇœo), `c3bb3b9` (fatia 3, `searchSubjects`/`searchRequirements`/`searchExpirationItems`), todas com CI verde em `develop`. Fatias 4 (projeÇõÇœo materializada+GSI10) e 5 (Çðndice dedicado por assignee) seguem DEFERIDAS com gatilho quantitativo nomeado no prÇüprio D-194, nÇœo construÇðdas ƒ?" nÇœo bloqueante. **VerificaÇõÇœo ao vivo contra `dev` feita** (`aws apigatewayv2 get-routes`, `uav7id1muh`): `GET /items/search`, `GET /document-archive/requirements/search`, `GET /subjects/search` confirmadas presentes.
6. **Dashboard operacional/compliance bÇ­sico** ƒ?" ÐYY½ **IMPLEMENTADO (achado sÇü nesta sessÇœo, mesma lacuna de registro do item 5, ver D-196)**: `935f49c` ("tenant summary + subject compliance"), `DashboardService`+rotas `GET /items/dashboard`/`GET /dashboard/summary`/`GET /subjects/dashboard` reais, CI verde. **VerificaÇõÇœo ao vivo contra `dev` feita**: `GET /items/dashboard`, `GET /dashboard/summary`, `GET /subjects/dashboard` confirmadas presentes.
7. **RelatÇürios + exportaÇõÇœo + audit trail** ƒ?" ÐYY½ **IMPLEMENTADO, fatias 1-4 (D-195, 2026-09-04)**: 7 relatÇürios CSV (vencidos/vencendo/renovados/requirements ausentes/por Subject/por responsÇ­vel Ç-2), Lambda dedicado `reports-handler`, 7 rotas `GET /reports/*`, RBAC `docarchive:requirement-export`/`item:export` (ambas `ADMIN_ROLES`), 10 testes novos (`reports-service.test.ts`). **Dois gaps do roadmap deliberadamente fora de escopo, nomeados**: "solicitaÇõÇæes pendentes" (sem GSI tenant-wide por status para `DocumentRequest`) e "audit trail legÇðvel para o negÇücio" (quem/fez o quÇ¦/quando em linguagem de negÇücio ƒ?" distinto do CSV bulk export, ainda por escopar do zero). **Pendente compartilhada com `/items/export` (D-123/D-126), nÇœo nova**: nenhuma das duas rotas passa pelo BFF (`proxy-allowlist.ts`) ƒ?" `ProxyService.forward()` nÇœo repassa `content-disposition`, quebraria o nome do arquivo no download; mecanismo de entrega para o frontend (quando ele existir) ainda por decidir. **VerificaÇõÇœo ao vivo contra `dev` feita** (2026-09-04, PR #229/`0bf9bbe`): Lambda `exptrk-dev-reports-handler` `Active`/`Successful`, as 7 rotas `GET /reports/*` confirmadas presentes.
8. **Document Types configurÇ­veis** ƒ?" ÐYY­ quase completo (D-173ƒÅ'D-186, D-221, **D-224**): CRUD completo, rotas HTTP, RBAC, metadata configurÇ­vel (D-218-221), e agora (D-224) a rota pÇ§blica de leitura de tipos do guest flow (`GET .../document-types`) tambÇ¸m implementada. **Falta sÇü a pendÇ¦ncia de PRODUTO do item 6** (decidir se `documentTypeId` vira obrigatÇürio no schema HTTP do guest submit, quebrando compatibilidade de integraÇõÇæes existentes que nÇœo mandam o campo) ƒ?" decisÇœo de Marcelo, nÇœo de engenharia; ver `decisions-log.md` D-224. NÇœo marcar ÐYY½ atÇ¸ essa decisÇœo ser tomada.
9. **Consolidar Guest Upload + Requests + Review + Recurrence** ƒ?" ÐYY½ **FECHADO POR INTEIRO (D-222/D-226/D-227/D-228/D-230, 2026-09-07)**: ciclo completo (criar `DocumentRequest` ƒ?" avulso OU materializado por `DocumentRequestSeries`/recorrÇ¦ncia ƒ?" com `recipientEmail` ƒÅ' emitir credencial ƒÅ' gravar na tabela dedicada ƒÅ' worker de entrega via Streams ƒÅ' e-mail com link ƒÅ' `resolveCredential()` real) agora funciona de ponta a ponta NOS DOIS CAMINHOS, provado por teste de integraÇõÇœo real (`guest-credential-issuance-to-delivery-e2e.test.ts`, 3 cenÇ­rios: avulso, sÇ¸rie COM `recipientEmail`, sÇ¸rie SEM `recipientEmail` com skip terminal preservado). D-230 (protocolo ClaudeƒÅ"Codex completo, 3 rodadas, 9,2/9,2) fechou a Ç§ltima pendÇ¦ncia nomeada por D-228: `DocumentRequestSeries.recipientEmail?` novo + `updateSeriesRecipient()` (mutaÇõÇœo/remoÇõÇœo, rota HTTP nova). Nenhum gap remanescente conhecido neste item.
10. **Consolidar Storage + Versioning + Renewal** ƒ?" ÐYY½ avanÇõado; `DocumentFile` fechou por completo (D-163 a D-168).
11. **Frontend completo do P0** ƒ?" ƒ?O **explicitamente adiado por Marcelo para a prÇüxima semana (2026-09-04)** ƒ?" nÇœo iniciar, nÇœo antecipar nenhuma fatia dele nesta janela de trabalho.

**Backlog pÇüs-lanÇõamento (autorizado por Marcelo, 2026-09-04, para comeÇõar assim que o P0 acima estiver de fato fechado, exceto o item 11/frontend)** ƒ?" protocolo ClaudeƒÅ"Codex obrigatÇürio para as decisÇæes de escopo/design de cada um (a maioria define um padrÇœo que sistemas externos jÇ­ resolveram ƒ?" candidato a E-014), sequenciamento e estado de cada um a cargo de quem retomar, registrar aqui/`decisions-log.md` conforme forem sendo escopados:
- **P1** (auditado 2026-09-04/05, D-198 + esta sessÇœo, leitura direta de cÇüdigo antes de escopar; detalhe completo em `decisions-log.md`): 1. reminder sequences configurÇ­veis ƒ?" ÐYY½ `DONE` (M3, sem trabalho pendente). 2. escalation/mÇ§ltiplos destinatÇ­rios ƒ?" ÐYY½ item 13 **FECHADO POR COMPLETO** (D-199/D-200/D-201). 3. busca OCR/full-text ƒ?" ÐY"ï **BLOQUEADO** (D-202), pendente decisÇœo de Marcelo entre 3 caminhos nomeados, NÇŸO Ç¸ o prÇüximo item executÇ­vel. 4. relatÇürios agendados ƒ?" ÐYY½ **FECHADO POR COMPLETO (2026-09-06)**: D-204 (design), D-211/D-212/D-213 (entidades+GSI8+outbox, scheduler+claim, CRUD) + **D-215 (worker de entrega SQS + rota de download autenticada, fatia 3/4)** ƒ?" uma assinatura real agora entrega e-mail ponta a ponta (CSV combinado por run, S3 dedicado, presign de 5min sob demanda, RBAC ADMIN-ou-destinatÇ­rio-real). Mergeado (PR #248, `11a7c92`), CD `success`, **verificado ao vivo contra `dev`** (Lambda/fila+DLQ/bucket/rota todos presentes). Pendente NÇŸO bloqueante: nunca testado ponta a ponta com o cron real disparando (sÇü G-V3/unit) ƒ?" considerar smoke test real numa sessÇœo futura. 5. dossiÇ¦ documental PDF/Excel ƒ?" ÐYY½ **FECHADO POR COMPLETO (2026-09-06)**: D-205 (design) + D-216 (fatia 1, preview/confirm) + **D-217 (fatia 2-3, worker de geraÇõÇœo PDF+XLSX via `pdf-lib`/`exceljs` + rota de download autenticada)** ƒ?" previewƒÅ'confirmƒÅ'geraÇõÇœoƒÅ'download ponta a ponta. Mergeado (PR #252, `bc35727`), CD `success` (retry apÇüs contenÇõÇœo de lock Terraform com CI concorrente de Dependabot, nÇœo um defeito de cÇüdigo), **verificado ao vivo contra `dev`** (2 Lambdas/fila+DLQ/event source mapping/rota de download todos presentes). Pendente NÇŸO bloqueante: nunca testado ponta a ponta com previewƒÅ'confirmƒÅ'geraÇõÇœo real disparando via SQS real em `dev` (sÇü G-V3/unit+smoke de infra). 6. bulk actions ƒ?" ÐYY½ **FECHADO POR COMPLETO** (D-206/D-207/D-209/D-210, smoke test de custo real confirmado em `dev`). 7. metadata configurÇ­vel por Document Type ƒ?" ÐYY½ **FECHADO POR COMPLETO (D-218/D-219/D-220/D-221, 2026-09-06)**: domÇðnio+CRUD de definiÇõÇæes de campo/opÇõÇœo, `Document.metadataValues` com fencing transacional, 3 rotas HTTP+proxy-allowlist+schemas. Nunca testado ponta a ponta contra `dev` real via HTTP autenticado (sÇü G-V3/unit+integraÇõÇœo in-memory) ƒ?" nÇœo bloqueante. 8. compartilhamento externo seguro ƒ?" confirmado NÇŸO iniciado, Ç§ltimo item do backlog P1. **PrÇüxima aÇõÇœo real**: escopar o item 8 (protocolo ClaudeƒÅ"Codex completo, Ç¸ uma decisÇœo nova nÇðvel 5-6), a critÇ¸rio de quem retomar.
- **P2**: assinatura eletrÇïnica; API pÇ§blica; webhooks; integraÇõÇæes de calendÇ­rio; compliance score avanÇõado.
- **Futuro** (sem gatilho comercial): portal completo do cliente; SSO/SCIM/controles enterprise.

**PrÇüxima aÇõÇœo real depois do MaintenanceDueIndex fechar (9/9)**: seguir a ordem prÇ­tica do roadmap (¶õ6 do documento) ƒ?" item 1 (Requirement Templates) Ç¸ o prÇüximo, por ter a melhor relaÇõÇœo esforÇõo/valor segundo o prÇüprio documento. Antes de comeÇõar qualquer item, escopar de verdade (mesma disciplina de sempre ƒ?" nÇœo presumir tamanho/nÇðvel de risco sem ler o cÇüdigo real primeiro); itens que envolvem modelo de dados novo ou definem um padrÇœo que sistemas fora do projeto jÇ­ resolveram (Requirement Templates parece nÇðvel 5-6, candidato a protocolo ClaudeƒÅ"Codex/E-014) devem passar pelo protocolo do `AGENTS.md` ¶õ4 antes de implementar.

`develop`/`main` sincronizados (diff zero) atÇ¸ D-188 inclusive (D-188 mergeado via PR #212, commit `ca50bcf`). Confirmar `git status`/`git log`/`git diff origin/main..develop --stat` antes de presumir qualquer coisa. Ver a seÇõÇœo "PrÇüxima aÇõÇœo" abaixo.

**Estado em 2026-09-01 (fim de sessÇœo, atualizado apÇüs verificaÇõÇœo ao vivo)**: sessÇœo de execuÇõÇœo contÇðnua e autÇïnoma, autorizada explicitamente por Marcelo a fazer applies e merges reais (nÇœo sÇü design). Tudo abaixo foi **confirmado ao vivo contra `dev`** via `aws --profile claude-dev`, nÇœo presumido.
**DomÇðnio Documental (D-143) ƒ?" NÇ§cleo 1 + NÇ§cleo 2, 100% completos em cÇüdigo E deployados**: NÇ§cleo 1 (Document/DocumentVersion, D-144) jÇ­ vinha de sessÇœo anterior. **NÇ§cleo 2 fechado nesta sessÇœo**: Requirement (D-145, PR #141), guest access (D-146, PR #142), recorrÇ¦ncia (D-147, PR #143) ƒ?" as 9 decisÇæes tÇ¸cnicas de D-143 estÇœo todas implementadas E aplicadas em `dev`. **Achado de processo importante desta sessÇœo**: `cd.yml` aplica automaticamente em todo push a `main` (OIDC, `AGENTS.md` ¶õ7) ƒ?" nÇœo Ç¸ preciso `terraform apply` manual apÇüs um merge normal; bastou confirmar ao vivo (`aws lambda get-function`/`aws apigatewayv2 get-routes`/`aws scheduler list-schedules`) que cada Lambda/rota/schedule apareceu. Confirmado: `exptrk-dev-document-archive-guest-handler`, `exptrk-dev-document-request-recurrence-handler`, `exptrk-dev-requirement-reindex-handler` todos `Active`/`Successful`; rotas `/document-archive/series*`, `/document-archive/guest/*`, `/document-archive/requirements/*` todas presentes; schedulers `exptrk-dev-requirement-reindex`/`exptrk-dev-document-request-recurrence` `ENABLED`.

**Backlog adicional fechado na mesma sessÇœo, tambÇ¸m deployado e confirmado ao vivo**:
- Dependency Freshness Standard (D-139ƒÅ'D-148, PR #144) ƒ?" `dependabot.yml`, `scripts/check-dependency-freshness.ts`, fix real de drift `ci.yml`/`cd.yml`.
- Admin Activity/Audit Log (D-131ƒÅ'D-149, PR #157) ƒ?" `GET /activity`, `ActivityService` com merge k-way de 4 partiÇõÇæes + cursor composto, tela `ActivityLog.tsx`.
- Quarentena `HELD_FOR_RECOVERY` (D-127ƒÅ'D-150, PR #158) ƒ?" mecanismo completo no grafo de `TenantLifecycleRecord`, `CancelOrganizationClosureService`, ASL estendido, sweeper com reconciliaÇõÇœo estrita. Confirmado ao vivo via `aws stepfunctions describe-state-machine` (estado `HELD_FOR_RECOVERY` presente na definiÇõÇœo real).
- **6 dos 7 workers de purga LGPD de D-127, todos deployados e confirmados**: `CORE_USER_DATA` (D-151, PR #159), `DELIVERY_RECORD` (D-152, PR #160), `SECURITY_AUDIT` (D-153, PR #161), `QUOTA_TELEMETRY` (D-154, PR #162, sÇü `TenantQuotaRecord` ƒ?" os outros 4 candidatos jÇ­ tinham TTL nativo), `ACCOUNT_ACTIVE`-nÇœo-fechamento (D-155+D-169, **`Invitation`+`Membership` completos** ƒ?" `Channel`/`NotificationPreferences` genuinamente fora de escopo, sem estado terminal prÇüprio), `TRANSIENT` remanescente (D-156, PR #164, achado real: `UploadSlot.purgeAfter` era campo morto com nome errado do atributo TTL). **`LEGAL_EVIDENCE` (Prioridade 7) permanece corretamente bloqueada** por trava jurÇðdica, nÇœo tocada.
- 9 PRs do Dependabot mergeados (versÇæes menores/patch, sem breaking change).

SuÇðte de testes: 1345 ƒÅ' 1658 (backend), G-V3 aplicado em toda lÇügica nÇœo-trivial. `develop`/`main` sincronizados (diff zero confirmado) ao final da sessÇœo.

**Itens abertos reais para a prÇüxima sessÇœo** (nenhum Ç¸ "Çübvio, prossiga sem perguntar" ƒ?" todos exigem decisÇœo de produto/arquitetura de Marcelo ou carregam risco elevado nomeado):
1. ~~PRs #152/#156 (Dependabot, ESLint 8.57ƒÅ'10.9)~~ ƒ?" **RESOLVIDO, D-157, 2026-09-01** (protocolo ClaudeƒÅ"Codex rodada Ç§nica, nota 8,4/10, nÇðvel 4). Root migrado para ESLint 10 + flat config; frontend bloqueado por upstream (`eslint-plugin-jsx-a11y`), dÇðvida EOL registrada.
2. ~~`Membership.removedAt` (achado de D-155)~~ ƒ?" **RESOLVIDO, D-158, 2026-09-01** (protocolo ClaudeƒÅ"Codex rodada Ç§nica, nota 8,7/10, nÇðvel 4). Campo adicionado, `RemoveMembershipService`/`LeaveOrganizationService` gravam, `AcceptInvitationService` limpa na reativaÇõÇœo. ~~Worker de purga fÇðsico de `Membership`~~ ƒ?" **RESOLVIDO, D-169, 2026-09-02** (nÇðvel 3-4, implementaÇõÇœo direta, sem protocolo): `MembershipPurgeWorker` deployado, mesmo layout de `invitation-purge`. `NotificationPreferences`/`Channel` seguem fora de escopo (sem estado terminal prÇüprio, achado de investigaÇõÇœo de D-155, nÇœo revisitado). Prioridade 5 de D-127 fecha por completo. Ordem confirmada por Marcelo, 2026-09-01: ESLint ƒÅ' Membership.removedAt ƒÅ' D-C/D-D hot path ƒÅ' Design System por Ç§ltimo.
3. **Wave 1b ƒ?" Design System Implementation Gap** ƒ?" componentes restantes (`Modal`/`Tooltip`/`Popover`/`DropdownMenu`/`Drawer`/`Combobox`/`DateInput`) exigem decisÇœo de padrÇœo overlay/focus-trap ainda nÇœo tomada, candidatos a protocolo ClaudeƒÅ"Codex completo quando abordados.
4. ~~D-D da otimizaÇõÇœo de hot path~~ ƒ?" **RESOLVIDO, D-172, 2026-09-02** (implementaÇõÇœo direta de design jÇ­ `APPROVED`, sem novo protocolo). `EphemeralTelemetryMutation` lane implementada em `quota.ts`, D-154 alargado para cobrir o novo `entityType`, teste adversarial do DoD confirmado ao vivo. Ver `decisions-log.md` D-172.
5. **D-C da otimizaÇõÇœo de hot path** (`IdentityStore.ConsistentRead` genÇ¸rico) ƒ?" achado real de que mudar `createProfileIfAbsent()` exige mudar um contrato genÇ¸rico mais amplo; decisÇœo de arquitetura pendente, provavelmente via protocolo.

Nenhuma infra nova pendente de apply ƒ?" tudo que foi implementado nesta sessÇœo jÇ­ estÇ­ em `dev`.

**Estado em 2026-08-31 17:xx**: as 3 lacunas de produto (D-121/122/123) estÇœo **100% completas ƒ?" design ƒÅ' implementaÇõÇœo ƒÅ' deploy ƒÅ' verificaÇõÇœo ao vivo**: D-124 (orquestrador W3-07, PR #122), D-125 (reatribuiÇõÇœo de responsabilidade, PR #123), D-126 (exportaÇõÇœo CSV, PR #124, PR #125 corrigiu um bug real de `depends_on` faltante que quebrou o primeiro CD) ƒ?" todos com CD `success` confirmado, `exptrk-dev-export-handler` confirmado `Active`/`Successful` via `aws lambda get-function --profile claude-dev`. Multi-User B2B (15 waves) `DONE`. Wave B2B-14 (roteiro manual convite/aceite 2¶¦ conta) segue adiada pelo Marcelo, nÇœo bloqueante. **2 bugs reais de CD encontrados e corrigidos nesta sessÇœo** (PR #125: `depends_on` faltante; rerun do CD por race condition de lock com a prÇüpria CI) ƒ?" nenhum recorrente, apenas registrar como classe de achado real desta rodada de implementaÇõÇœo. **D-135 (2026-08-31)**: accent do design system revertido de azul-indigo (`#2F4FD0`) para roxo (`#7C3AED` famÇðlia) ƒ?" decisÇœo direta de Marcelo, protocolo ¶õ4 dispensado sob exceÇõÇœo registrada de `ai-governance.md` ¶õ2, contraste WCAG verificado sem regressÇœo, `tokens.css` jÇ­ atualizado e suÇðte completa verde. Prosa de `visual-language-and-design-system.md`/`design-system.md` ¶õ0 ainda narra a escolha antiga do azul ƒ?" pendÇ¦ncia de atualizaÇõÇœo de texto nomeada, nÇœo bloqueia cÇüdigo.

**AmpliaÇõÇœo de autoridade registrada nesta sessÇœo (Marcelo, 2026-08-31, `docs/engineering/ai-governance.md` ¶õ1)**: para os itens da lista abaixo, resÇðduos de decisÇœo de produto (nÇœo sÇü o mecanismo tÇ¸cnico) tambÇ¸m podem ser decididos por Claude+Codex via protocolo completo (pesquisa externa + rodada adversarial + nota cega ƒ%¾9,0) ƒ?" revertÇðvel por ele depois, nÇœo dispensa o protocolo em si, NÇŸO se estende a execuÇõÇœo real/destrutiva.

**PrÇüximos itens autorizados a tratar autonomamente, NESTA ORDEM (Marcelo confirmou a ordem e a autoridade ampliada, 2026-08-31)**:
1. ~~EstratÇ¸gia de quarentena antes de exclusÇœo fÇðsica de dado persistido + as 7 classes de retenÇõÇœo LGPD restantes~~ ƒ?" **`APPROVED` (design), D-127, 2026-08-31**, protocolo completo 11 rodadas (Claude 9,4/Codex 9,3). Ver `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`. Mecanismo: novo estado `HELD_FOR_RECOVERY` (30 dias, cancelÇ­vel) no grafo de `TenantLifecycleRecord`, reusa o Step Functions de W3-07/D-124. PriorizaÇõÇœo LGPD das 7 classes sem purga real dentro de tenant `ACTIVE`: `CORE_USER_DATA`ƒÅ'`DELIVERY_RECORD`ƒÅ'`SECURITY_AUDIT`ƒÅ'`QUOTA_TELEMETRY`ƒÅ'`ACCOUNT_ACTIVE`(nÇœo-fechamento)ƒÅ'`TRANSIENT` remanescente, com `LEGAL_EVIDENCE` em lane bloqueada separada (trava jurÇðdica). **Design-only, deliberado** ƒ?" implementaÇõÇœo real fica para sessÇœo(Çæes) futura(s) dedicada(s), tamanho comparÇ­vel a uma wave, nÇœo a um dia.
2. ~~`AppError.retryable` (comportamento de retry/DLQ do SQS)~~ ƒ?" **`APPROVED` + `IMPLEMENTADO` (documentaÇõÇœo apenas), D-128, 2026-08-31**, protocolo completo 3 rodadas (Claude 9,3/Codex 9,6). Fecha a pendÇ¦ncia de `docs/engineering/decisions-log.md` E-011. DecisÇœo: nenhum handler SQS ramifica sobre `retryable` ƒ?" mecanismo de retry-vs-terminal continua exclusivamente `maxReceiveCount=5`+DLQ nativo do SQS, `retryable` permanece metadado diagnÇüstico. Pesquisa externa (AWS Lambda+SQS docs) confirmou nenhum padrÇœo de referÇ¦ncia recomenda branching por tipo de erro. Ver `docs/architecture/reviews/appError-retryable-sqs-scoping/`.
3. ~~SupersessÇœo de GTR-01 (`UserProfile.requesterDisplayName`ƒÅ'`Organization.displayName`)~~ ƒ?" **`APPROVED` + `IMPLEMENTADO`, D-129, 2026-08-31**, protocolo completo 3 rodadas (Claude 9,5/Codex 9,8). DecisÇœo: REPLACE (nÇœo coexiste) ƒ?" `Organization.displayName` Ç¸ agora a Ç§nica identidade guest-facing "quem estÇ­ solicitando", `UserProfile.requesterDisplayName` removido por completo (campo, `GET/PUT /profile`, Lambda `profile-handler`, actions RBAC, rotas Terraform). Ver `docs/architecture/reviews/gtr-01-supersession-scoping/estado-final-consolidado.md`.
4. ~~Wave 1 ƒ?" Design System reconciliation~~ ƒ?" **`APPROVED` (documentaÇõÇœo), D-130, 2026-08-31**, protocolo completo 5 rodadas (Claude 9,2/Codex 9,5). Ver `docs/architecture/reviews/design-system-reconciliation-scoping/estado-final-consolidado.md`. DecisÇæes: `design-system-v1-proposal.md` ƒÅ' ADOTAR COM EMENDA (renomeado `docs/frontend/design-system.md`, valores primitivos concretos substituÇðdos pelos jÇ­ `APPROVED` em `visual-language-and-design-system.md`/`tokens.css`, nenhum gate `VL-G` reaberto); `frontend-engineering-quality-standard-v1-proposal.md` ƒÅ' ADOTAR (renomeado `docs/frontend/frontend-engineering-quality-standard.md`, coexiste com `interface-quality-standard.md` via crosswalk de ownership); `bff-frontend-quality-standard-proposal.md` ƒÅ' SUPERSEDED. **Puramente documental, deliberado** ƒ?" nenhum cÇüdigo de frontend alterado; reconciliaÇõÇœo do frontend real contra o catÇ­logo de componentes agora normativo fica para **Wave 1b ƒ?" Design System Implementation Gap**, trabalho futuro nÇœo iniciado.
5. ~~Tela de atividade/audit log para ADMIN/OWNER~~ ƒ?" **`APPROVED` (design), D-131, 2026-08-31**; **`IMPLEMENTADO` (cÇüdigo completo), D-149, 2026-09-01** (infra escrita/validada, NÇŸO aplicada em `dev`). Ver `docs/architecture/reviews/admin-activity-log-scoping/estado-final-consolidado.md` (design) e `decisions-log.md` D-149 (implementaÇõÇœo ƒ?" handler `GET /activity`, `ActivityService`/merge/cursor, tela `ActivityLog.tsx`, rota Terraform, testes). Pendente real: aplicar a rota `GET /activity` em `dev` (`terraform apply`), decisÇœo do Marcelo/sessÇœo orquestradora.
6. ~~ImplementaÇõÇœo real do MECANISMO de quarentena de D-127~~ ƒ?" **`IMPLEMENTADO`, D-150, 2026-09-01** (cÇüdigo completo, infra escrita/validada, NÇŸO aplicada em `dev`). Estado `HELD_FOR_RECOVERY` real no grafo, `CancelOrganizationClosureService`, ASL estendido (`WaitForRecovery`/`CheckCancelled`/`RecoveryCancelled`), sweeper com reconciliaÇõÇœo estrita, legal-hold preservando `recoveryDeadline`. Ver `decisions-log.md` D-150. **Pendente real, nomeado explicitamente, ainda nÇœo iniciado**: migraÇõÇœo dos `ReminderOccurrence` `CANCELLED`-writers para `cancelOccurrenceUpdate()`, regra ESLint de enforcement (round-11 do design). Dos 7 workers de purga LGPD, **`CORE_USER_DATA` (Prioridade 1) `IMPLEMENTADO`, D-151**, **`DELIVERY_RECORD` (Prioridade 2) `IMPLEMENTADO`, D-152**, **`SECURITY_AUDIT` (Prioridade 3) `IMPLEMENTADO`, D-153, 2026-09-01**, **`QUOTA_TELEMETRY` (Prioridade 4) `IMPLEMENTADO`, D-154, 2026-09-01**, **`ACCOUNT_ACTIVE (nÇœo-fechamento)` (Prioridade 5) `IMPLEMENTADO PARCIALMENTE`, D-155, 2026-09-01** (sÇü `Invitation`; ver itens 6b/6c/6d/6e/6f abaixo) e **`TRANSIENT` remanescente (Prioridade 6) `IMPLEMENTADO`, D-156, 2026-09-01** (`WebhookInbox`+`UploadSlot`; ver item 6g abaixo) ƒ?" sÇü `LEGAL_EVIDENCE` (Prioridade 7, bloqueada por trava jurÇðdica) segue sem inÇðcio, das 7 prioridades LGPD originais de D-127. Aplicar a rota `POST /organizations/cancel-close` e o ASL estendido em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora.
6b. ~~Worker de purga `CORE_USER_DATA` (Prioridade 1 de 7 dos workers LGPD, D-127)~~ ƒ?" **`IMPLEMENTADO`, D-151, 2026-09-01** (cÇüdigo completo, infra escrita, NÇŸO aplicada em `dev`). `ExpirationItem`/`ReminderPolicy` (`deletedAt`) ganham worker Lambda diÇ­rio novo (`src/workers/core-user-data-purge/`, Scan full-table filtrado, delete condicionado por versÇœo+`deletedAt` exato, fence de tenant `ACTIVE`); `ReminderOccurrence` ganha `purgeAfterTtl` (TTL nativo DynamoDB, atributo jÇ­ habilitado na tabela, setado na materializaÇõÇœo a partir da prÇüpria `scheduledAt`) ƒ?" SEM worker, exatamente como D-127 pediu. Ver `decisions-log.md` D-151. **Pendente real, nomeado explicitamente**: os 6 workers LGPD restantes na Ç¸poca (`DELIVERY_RECORD`ƒÅ'`SECURITY_AUDIT`ƒÅ'`QUOTA_TELEMETRY`ƒÅ'`ACCOUNT_ACTIVE`ƒÅ'`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada); backfill de `purgeAfterTtl` em `ReminderOccurrence`s prÇ¸-existentes (aberto, nÇœo implementado, ver D-151); aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora.
6c. ~~Worker de purga `DELIVERY_RECORD` (Prioridade 2 de 7 dos workers LGPD, D-127)~~ ƒ?" **`IMPLEMENTADO`, D-152, 2026-09-01** (cÇüdigo completo, infra escrita/validada, NÇŸO aplicada em `dev`). `privacy-lgpd.md` ¶õ4 "intents/attempts" mapeado ao cÇüdigo real: `NotificationIntent` (`src/modules/reminder/domain/notification-intent.ts`) e `NotificationAttempt` (`src/modules/notification/domain/notification-attempt.ts`) ganham worker Lambda diÇ­rio novo (`src/workers/delivery-record-purge/`, mesmo padrÇœo de D-151 ƒ?" Scan full-table filtrado por `entityType`/`attribute_exists(createdAt)`, delete condicionado por versÇœo+`createdAt` exato, fence de tenant `ACTIVE`). DiferenÇõa de forma de elegibilidade vs. D-151: aqui Ç¸ sÇü idade (`createdAt+180d`), sem gate de "foi deletado" ƒ?" todo registro de entrega expira pelo prazo fixo, independente de status. **Verificado explicitamente que NÇŸO Ç¸ `AuditEvent`/`MembershipAuditEvent`** (essas sÇœo `SECURITY_AUDIT`, Prioridade 3, D-149's Admin Activity Log feed as usa ƒ?" classe diferente, fora de escopo ali). Ver `decisions-log.md` D-152. **Pendente real, nomeado explicitamente**: `NotificationAttemptLookup` (pointer derivado, `PK=TENANT#t#ATTEMPT#id`) nÇœo Ç¸ purgado por este worker ƒ?" fica ÇürfÇœo apÇüs a purga do `NotificationAttempt` correspondente, achado nomeado mas nÇœo corrigido nesta sessÇœo (sem efeito colateral externo, sÇü um `GetItem` inÇ§til se algo ainda o consultasse, o que nÇœo ocorre 180 dias depois de criado); aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora.
6d. ~~Worker de purga `SECURITY_AUDIT` (Prioridade 3 de 7 dos workers LGPD, D-127)~~ ƒ?" **`IMPLEMENTADO`, D-153, 2026-09-01** (cÇüdigo completo, infra escrita/validada, NÇŸO aplicada em `dev`). `privacy-lgpd.md` ¶õ4 "AuditEvent/logs redigidos, MembershipAuditEvent" mapeado ao cÇüdigo real, confirmado por leitura direta: as 4 entidades `AuditEvent`-family (`AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` ƒ?" as mesmas 4 partiÇõÇæes que `GET /activity`'s `ActivityService`, D-149, jÇ­ faz merge k-way) ganham worker Lambda diÇ­rio novo (`src/workers/security-audit-purge/`, Scan full-table filtrado por `entityType`/`attribute_exists(occurredAt)`, fence de tenant `ACTIVE`). `occurredAt` Ç¸ o relÇügio de idade das 4 (nenhuma declara `createdAt`). **Desvio de forma vs. D-151/D-152, documentado**: nenhuma das 4 tem `version` (append-only por construÇõÇœo) ƒ?" `buildConditionalDelete` novo em `occ.ts` (irmÇœo simÇ¸trico de `buildConditionalPut`) substitui `buildVersionedDelete`, condicionando o delete em `occurredAt` exato observado no scan. `MembershipAuditEvent`'s `organizationId` Ç¸ normalizado para `tenantId` no adapter (mesmo valor, nome de campo diferente). **InvestigaÇõÇœo de corrida com paginaÇõÇœo de `GET /activity` concluÇðda SEM achado de bug** (cursor da activity usa `ExclusiveStartKey` como posiÇõÇœo, nÇœo como referÇ¦ncia a item vivo ƒ?" ver `decisions-log.md` D-153 e header de `purge.ts` para o raciocÇðnio completo). Ver `decisions-log.md` D-153. **Pendente real, nomeado explicitamente**: os 4 workers LGPD restantes Çÿ Ç¸poca (`QUOTA_TELEMETRY`ƒÅ'`ACCOUNT_ACTIVE`ƒÅ'`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) na ordem jÇ­ decidida; aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora.
6e. ~~Worker de purga `QUOTA_TELEMETRY` (Prioridade 4 de 7 dos workers LGPD, D-127)~~ ƒ?" **`IMPLEMENTADO`, D-154, 2026-09-01** (cÇüdigo completo, infra escrita/validada, NÇŸO aplicada em `dev`). InvestigaÇõÇœo real (esta prioridade nÇœo tinha escopo trivial, ao contrÇ­rio das 3 anteriores): das 5 entidades reais de rate-limit/quota do cÇüdigo (`MembershipInviteRateLimitRecord`, `GuestRateLimitRecord`, `InitialInviteRateLimitRecord`, `DocumentArchiveGuestRateLimitRecord`, `TenantQuotaRecord`), 4 ƒ?" incluindo o exemplo nomeado em `privacy-lgpd.md` ¶õ4 ƒ?" jÇ­ tÇ¦m `purgeAfterTtl` wireado ao TTL nativo da tabela, jÇ­ resolvidas; sÇü `TenantQuotaRecord` (`src/modules/identity/application/quota.ts`) nÇœo tinha nenhuma purga. Worker Lambda diÇ­rio novo (`src/workers/quota-telemetry-purge/`, mesmo padrÇœo de D-151/152/153 ƒ?" Scan full-table filtrado por `entityType`, delete condicionado por `resetAt` exato re-afirmado, fence de tenant `ACTIVE`); elegibilidade = `resetAt` (fim da janela do token bucket) + 30 dias. Ver `decisions-log.md` D-154. **Pendente real, nomeado explicitamente**: os 3 workers LGPD restantes (`ACCOUNT_ACTIVE`ƒÅ'`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) na ordem jÇ­ decidida; aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora.
6f. ~~Worker de purga `ACCOUNT_ACTIVE (nÇœo-fechamento)` (Prioridade 5 de 7 dos workers LGPD, D-127)~~ ƒ?" **`IMPLEMENTADO PARCIALMENTE`, D-155, 2026-09-01** (sÇü `Invitation`; cÇüdigo completo, infra escrita/validada, NÇŸO aplicada em `dev`). Escopo nomeado pelo design (`Invitation`ƒÅ'`Membership`ƒÅ'`Channel`) investigado item a item: `Invitation` tem worker novo (`src/workers/invitation-purge/`, mesmo padrÇœo dos anteriores ƒ?" candidato = `REVOKED` via `revokedAt` OU `PENDING` com `expiresAt` jÇ­ passado tratado como o `EXPIRED` de-facto nunca escrito por cÇüdigo nenhum, fence de tenant `ACTIVE`, delete condicionado por `version` real da entidade). **`Membership` e `Channel` NÇŸO implementados ƒ?" achado real de bloqueio, nÇœo escopo abandonado**: `Membership.status = REMOVED` nunca grava timestamp nenhum (`remove-membership.ts`/`leave-organization.ts` sÇü setam `status`+`version`) ƒ?" sem relÇügio de elegibilidade "termo + 30 dias" para checar, e adicionar um campo novo a 2 write paths existentes Ç¸ decisÇœo alÇ¸m de implementar o worker jÇ­ aprovado; `Channel` nÇœo existe como entidade persistida no cÇüdigo (o candidato mais prÇüximo, `NotificationPreferences`, herda o mesmo bloqueio de relÇügio ausente por depender do `status` de `Membership`). Ver `decisions-log.md` D-155 para a investigaÇõÇœo completa. **Pendente real, nomeado explicitamente**: decisÇœo do Marcelo/sessÇœo orquestradora sobre adicionar `removedAt` a `Membership` (destrava `Membership` e, por extensÇœo, `Channel`/`NotificationPreferences`); aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora.
6g. ~~Worker de purga `TRANSIENT` remanescente (Prioridade 6 de 7 dos workers LGPD, D-127)~~ ƒ?" **`IMPLEMENTADO`, D-156, 2026-09-01** (cÇüdigo completo, infra escrita/validada, NÇŸO aplicada em `dev`). `privacy-lgpd.md` ¶õ4 nomeia `WebhookInbox, UploadSlot, InvitationTokenPointer` na linha `TRANSIENT`; `InvitationTokenPointer` jÇ­ resolvido via TTL nativo (nÇœo tocado). Worker novo Ç§nico (`src/workers/transient-purge/`, um sÇü `Scan` cobrindo `entityType IN (WebhookInbox, UploadSlot)`, fence de tenant `ACTIVE`, delete condicionado por `version` real de cada entidade): `WebhookInbox` (`ses-callback-workflow.ts`) purgado por `createdAt+7d`, sem mecanismo prÇ¸vio nenhum (achado genuÇðno, como o `TenantQuotaRecord` de D-154). `UploadSlot` purgado reusando `computeUploadSlotPurgeAfter()` (jÇ­ existia em `document/domain/retention.ts`, mas era cÇüdigo morto ƒ?" nunca chamada por nenhum writer, e o campo `purgeAfter` que a entidade carrega Ç¸ uma string ISO comum, nÇœo o atributo `purgeAfterTtl` que o TTL nativo da tabela realmente observa): `RESERVED` nunca Ç¸ candidato (reserva ativa em voo), `CONSUMED` usa a janela de 7 dias, `EXPIRED`/`RELEASED` usam a janela de 24h ("slot incompleto"). Ver `decisions-log.md` D-156 para a investigaÇõÇœo completa. **Pendente real**: nenhum item de escopo aberto nesta prioridade ƒ?" aplicar o novo Lambda/`aws_scheduler_schedule` em `dev` (`terraform apply`) Ç¸ decisÇœo do Marcelo/sessÇœo orquestradora; sÇü resta `LEGAL_EVIDENCE` (Prioridade 7, bloqueada por trava jurÇðdica) das 7 prioridades LGPD originais de D-127.

**Item 7 (execuÇõÇœo destrutiva `reset-dev-data.ts --confirm`) explicitamente POSTERGADO pelo Marcelo ƒ?" nÇœo tratar, nÇœo perguntar de novo atÇ¸ ele sinalizar.**

Cada um dos 4 itens acima segue o mesmo padrÇœo desta sessÇœo: rodada ClaudeƒÅ"Codex completa (pesquisa externa quando aplicÇ­vel, 3+ rodadas, nota cega, gate 9,0) ƒÅ' se pequeno o bastante, implementaÇõÇœo direta na mesma sessÇœo (D-124/125/126 pattern) ƒÅ' `decisions-log.md` novo D-number (prÇüximo Ç¸ **D-127**, confirmar lendo o arquivo) ƒÅ' atualizar este arquivo ƒÅ' commit/push `develop` autÇïnomo ƒÅ' PRƒÅ'`main` se suite verde. **Cuidado com conflito**: todos tocam `decisions-log.md`/este arquivo ƒ?" rodar SEQUENCIALMENTE, nunca em paralelo (liÇõÇœo aprendida nesta sessÇœo com D-121, evitar delegaÇõÇœo recursiva de agentes ƒ?" instruir explicitamente "nÇœo spawnar sub-agente, fazer o trabalho vocÇ¦ mesmo").

## Fase atual

`Consolidation + Pilot Readiness` concluÇðdo com recomendaÇõÇœo **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`). M0-M11 implementados e deployados; M7 (extraÇõÇœo/OCR) code-complete e **E2E PROVEN** em `dev`; Full BFF + Frontend Production Foundation `APPROVED` e implementados; planejamento de interface 8/9 etapas `APPROVED` (sÇü falta User Validation, em suspenso a pedido do Marcelo). W3-07 (exclusÇœo fÇðsica de tenant/DSR/LGPD) tem design `APPROVED WITH CONDITIONS` (D-066/D-067) e estÇ­ em implementaÇõÇœo incremental por chunks (D-068 a D-104, Ç§ltima emenda de privacidade B2B-9). M12 (billing) bloqueado por decisÇœo de fornecedor (D-052). **Multi-User B2B (D-084 a D-108): design tÇ¸cnico `APPROVED` via protocolo ClaudeƒÅ"Codex, physical model formalizado (Wave B2B-1, D-086) e timing decidido diretamente pelo Marcelo ƒ?" proceder agora, supersedendo o gatilho comercial que antes gated M13.** Waves B2B-0 a B2B-11 `DONE` ƒ?" **B2B-5 (RequestContext Cutover) implementada (D-096): login nÇœo cria mais tenant sozinho, `RequestContextResolver` resolve organizaÇõÇœo real via `Membership`, `POST /bff/organizations` fecha o loop de onboarding com cap transacional. B2B-7 (RBAC) implementada (D-097/D-098): `Membership.role=ADMIN` real na matriz de autorizaÇõÇœo, primeira aplicaÇõÇœo real de `docs/engineering/research-protocol.md`/E-014. B2B-8 (Invitations/Team) implementada (D-099/D-100): `Invitation`/aceite/revogaÇõÇœo/gerÇ¦ncia de membros (role-change/remove/leave) com last-owner protection transacional, segunda aplicaÇõÇœo de E-014, primeiro writer real de `Membership` alÇ¸m da criaÇõÇœo ƒ?" novo Lambda `memberships-handler`, 7 rotas API Gateway novas. B2B-6 (BFF Organization Context) implementada (D-101/D-102): `X-Organization-Id` (BFF-derived, nunca do browser) transportado atÇ¸ o `RequestContextResolver`, fecha o `InternalError` 500 que B2B-8 tornou explorÇ­vel, `POST /bff/organization/select`/`GET /bff/organizations` novos, terceira aplicaÇõÇœo de E-014. B2B-9 (W3-07/Privacy Reconciliation) implementada (D-103/D-104): fecha o `InvitationTokenPointer` ÇürfÇœo pÇüs-exclusÇœo de Organization (3¶¦ clÇ­usula OR no scan/`PURGE_DELETE`), formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md` ¶õ4.1, quarta aplicaÇõÇœo de E-014. B2B-10 (Tenant-aware Frontend) implementada (D-105/D-106): corrige uma regressÇœo real (frontend tratava todo usuÇ­rio autenticado como deslogado desde B2B-5), isolamento de cache real entre tenants (`ActiveOrganizationProvider`, gate `switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta), switcher/members/settings novos, primeira aplicaÇõÇœo do protocolo a uma decisÇœo de frontend. B2B-11 (Responsibility + Notifications) implementada (D-107/D-108): `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch.userId` migrados de validaÇõÇœo vestigial (`UserProfile`) para `Membership`+`GlobalUser` reais, quinta aplicaÇõÇœo de E-014 ƒ?" NÇŸO inclui a supersessÇœo de GTR-01 (`Organization.displayName`), ainda pendente. B2B-12 (Cutover de dev) implementada (D-110/D-111): `scripts/reset-dev-data.ts` novo (Fase A inventÇ­rio+snapshot sempre, Fase B delete real sÇü `--confirm`, allowlist de tabela/conta, verificaÇõÇœo final fail-loud), `LEGACY_TENANT_ONLY` removido (backend+frontend+teste), sexta aplicaÇõÇœo de E-014 (declaraÇõÇœo `NÇŸO`) ƒ?" Fase A (leitura) jÇ­ verificada de fato contra `dev` real; **execuÇõÇœo real destrutiva (`--confirm`/`--include-cognito`) permanece pendente de confirmaÇõÇœo explÇðcita do Marcelo, registrada como o Ç§nico item aberto dessa wave.** B2B-13 (E2E/Adversarial Security) implementada (D-112/D-113): auditoria completa contra as 25 perguntas de `roadmap-evolution/17` ¶õ121 (20/25 jÇ­ cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assÇðncrona de notificaÇõÇœo ƒ?" `notification.ts`/`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` jÇ­ testado, 2 ganham teste novo de revogaÇõÇœo/isolamento de role, 1 Ç¸ auditoria de fixture IDs sem achado), sÇ¸tima aplicaÇõÇœo de E-014. **Multi-User B2B ƒ?" todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** B2B-14 (Operational Evidence) `EM ANDAMENTO` ƒ?" 7 achados reais corrigidos (D-114 a D-120), teste de convite/aceite com segunda conta explicitamente adiado pelo Marcelo. B2B-15 (Documentation Reconciliation) `EM ANDAMENTO` em paralelo. 3 decisÇæes de produto novas (purga W3-07/orquestrador, exportaÇõÇœo de dados, reatribuiÇõÇœo de responsabilidade) fecharam design `APROVADO` via protocolo (D-121/D-122/D-123); a primeira (orquestrador de purga) jÇ­ foi implementada em D-124, as outras duas seguem sÇü com design. Ver "O que estÇ­ em andamento" abaixo. **Lembrete de calibraÇõÇœo (D-093, `AGENTS.md` ¶õ1)**: projeto sem usuÇ­rio real nem produÇõÇœo ƒ?" nÇœo pesar "quebraria conta/sessÇœo existente em `dev`" como risco bloqueador.

Ver `docs/architecture/README.md` (linha `Design maturity`/bloco de status no topo) para o resumo executivo mais completo ƒ?" este arquivo nÇœo duplica aquele bloco, sÇü aponta para ele.

## O que jÇ­ estÇ­ implementado (referÇ¦ncia, nÇœo repetir aqui)

- **Backend por mÇüdulo/milestone**: `AGENTS.md` ¶õ7 (invariantes estÇ­veis) + `ARCHITECTURE.md` (visÇœo consolidada).
- **Frontend/planejamento de interface**: `docs/frontend/README.md` (Çðndice completo, blockers tÇ¸cnicos BLOCKER-A/B/C todos resolvidos, GTR-01 com decisÇœo de produto pendente W5-01 jÇ­ fechada).
- **Engenharia/qualidade**: `docs/engineering/README.md` (padrÇæes, gates, backlog do programa de pilot readiness).
- **Infra**: Terraform (`infra/`, ADR-0009), CI/CD via GitHub Actions OIDC.
- **ReconciliaÇõÇœo de engenharia de contexto (2026-08-29)**: `DONE`, mergeada em `main` (PR #84) ƒ?" root cleanup + `AGENTS.md`/este arquivo reconciliados + 2 guardrails novos em `check-doc-drift.ts`, revisÇœo ClaudeƒÅ"Codex 9,3/10. Registro completo: `docs/architecture/reviews/context-engineering-reconciliation/`.
- **E-011 (logging/tracing) ƒ?" junÇõÇœo `correlationId`ƒÅ"X-Ray**: `E2E PROVEN` (2026-08-29) ƒ?" smoke test real em `dev` via `aws --profile claude-dev` confirmou `xrayTraceId` no log idÇ¦ntico ao trace real do X-Ray. Detalhe: `docs/architecture/correlationid-xray-trace-join.md`.
- **E-012 (2026-08-29) ƒ?" Definition of Done por item de todo list**: `APPROVED` via protocolo ClaudeƒÅ"Codex (3 rodadas, Claude 9,1/Codex 9,2). Regra de processo permanente, vigente a partir de agora em toda sessÇœo: nenhum item de todo list que produza/altere cÇüdigo real Ç¸ `completed` sem passar pelo gate do seu nÇðvel de risco ƒ?" ver `docs/engineering/definition-of-done.md` (granularidade, gate por nÇðvel, registro de evidÇ¦ncia `DoD:`). **Emenda E-013 (2026-08-30)**: aplicaÇõÇœo de `test-engineering-standard.md` tornada operacional (nÇœo sÇü referencial) ƒ?" G-V3 (mutaÇõÇœo nomeada por escrito) exigido explicitamente por teste, nÇœo sÇü "suÇðte verde". Achado do Marcelo, corrigido retroativamente nos testes de B2B-2.
- **D-093 (2026-08-30) ƒ?" sem usuÇ­rio real nem produÇõÇœo, nÇœo pesar esse risco**: `AGENTS.md` ¶õ1 ganhou parÇ­grafo permanente ƒ?" projeto em fase de construÇõÇœo, sem usuÇ­rio real, sem produÇõÇœo (deploy sÇü em `dev`, dado sintÇ¸tico resetÇ­vel), sem prazo. Nunca tratar "quebraria conta/sessÇœo existente em `dev`" como risco bloqueador por si sÇü; nÇœo dispensa rigor de engenharia/seguranÇõa/protocolo ¶õ4. Achado do Marcelo, motivado por D-092 ter invocado essa categoria de risco indevidamente (a conclusÇœo tÇ¸cnica de D-092 continua vÇ­lida por outras razÇæes).
- **D-094 a D-108 (2026-08-30) ƒ?" Waves B2B-4/5/7/8/6/9/10/11, todas `DONE`**: cada uma seguiu protocolo ClaudeƒÅ"Codex completo (3 rodadas) + implementaÇõÇœo no mesmo dia. Detalhe completo (achados reais por wave, judgment calls) jÇ­ vive em `docs/architecture/multi-user-b2b-wave-tracker.md` e `decisions-log.md` (nÇœo duplicado aqui, per o prÇüprio tÇðtulo desta seÇõÇœo). **E-014** (pesquisa externa antes de decisÇœo Type 1 nÇðvel 5-6) `APPROVED` nesta janela, aplicada 6 vezes (D-097/D-099/D-101/D-103/D-105/D-107).

## O que estÇ­ em andamento

1. **Multi-User B2B ƒ?" autorizado a prosseguir, Waves B2B-0 a B2B-13 `DONE` (D-084 a D-113).** Design tÇ¸cnico `APPROVED` (protocolo completo em `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` ¶õ125) e timing decidido diretamente pelo Marcelo. Estado detalhado por subitem (evidÇ¦ncia `DoD:`, judgment calls de sequenciamento documentados) vive em `docs/architecture/multi-user-b2b-wave-tracker.md` ƒ?" nÇœo duplicado aqui. Resumo: **B2B-0** inventÇ­rio read-only; **B2B-1** physical model formalizado via protocolo ClaudeƒÅ"Codex 5 rodadas (D-086, `docs/architecture/multi-user-b2b-physical-model.md`); **B2B-2** fundaÇõÇœo de identidade global aditiva (D-087/D-088); **B2B-3** `Organization`/`Membership`/`CreateOrganizationService` (D-089/D-090/D-091); **B2B-4** `OnboardingStateResolver` (D-092/D-094); **B2B-5** `RequestContext Cutover` (D-095/D-096) ƒ?" login nÇœo cria mais tenant sozinho, `RequestContextResolver` resolve a Organization real, `POST /bff/organizations` fecha o loop de onboarding; **B2B-7** `RBAC` (D-097/D-098) ƒ?" `Membership.role=ADMIN` real na matriz de autorizaÇõÇœo; **B2B-8** `Invitations/Team` (D-099/D-100) ƒ?" `Invitation`/aceite/revogaÇõÇœo/gerÇ¦ncia de membros com last-owner protection, primeiro writer real de `Membership` alÇ¸m da criaÇõÇœo, `ADMIN` finalmente alcanÇõÇ­vel em produÇõÇœo via convite; **B2B-6** `BFF Organization Context` (D-101/D-102) ƒ?" `X-Organization-Id` transportado do BFF ao recurso, multi-org selecionÇ­vel de verdade, fecha o `InternalError` 500 que B2B-8 tornou explorÇ­vel; **B2B-9** `W3-07/Privacy Reconciliation` (D-103/D-104) ƒ?" fecha o `InvitationTokenPointer` ÇürfÇœo pÇüs-exclusÇœo de Organization, formaliza User-level vs. Organization-level erasure em `privacy-lgpd.md` ¶õ4.1; **B2B-10** `Tenant-aware Frontend` (D-105/D-106) ƒ?" corrige a regressÇœo real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; **B2B-11** `Responsibility + Notifications` (D-107/D-108) ƒ?" `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch.userId` migrados para `Membership`+`GlobalUser` reais (NÇŸO inclui a supersessÇœo de GTR-01, ainda pendente); **B2B-12** `Cutover de dev` (D-110/D-111) ƒ?" `scripts/reset-dev-data.ts` (Fase A inventÇ­rio+snapshot sempre, Fase B delete real sÇü `--confirm`, allowlist de tabela/conta, verificaÇõÇœo final fail-loud), `LEGACY_TENANT_ONLY` removido (backend+frontend+teste), Fase A (leitura) jÇ­ verificada de fato contra `dev` real; execuÇõÇœo real destrutiva permanece pendente de confirmaÇõÇœo explÇðcita do Marcelo; **B2B-13** `E2E/Adversarial Security` (D-112/D-113) ƒ?" auditoria contra as 25 perguntas de ¶õ121 (20/25 jÇ­ cobertas por waves anteriores, 2 fecham com fix real de TOCTOU em entrega assÇðncrona de notificaÇõÇœo, 2 ganham teste novo de revogaÇõÇœo/isolamento de role, 1 Ç¸ auditoria de fixture IDs sem achado).

   **Multi-User B2B ƒ?" todas as 14 waves originais (B2B-0 a B2B-13) `DONE`. B2B-14 (Operational
   Evidence) `EM ANDAMENTO`, 7 achados reais severos jÇ­ corrigidos (D-114 a D-120), todos sÇü
   descobertos por exercitar o fluxo de verdade contra `dev` pela primeira vez** ƒ?" exatamente o
   job desta wave (mesmo espÇðrito de E-011/M7's E2E PROVEN): (1) `app_origin` placeholder nunca
   trocado, OAuth nunca funcionou (D-114); (2) `bff-handler` crashava em TODA invocaÇõÇœo desde
   B2B-8, `GUEST_TOKEN_PEPPER` nunca wireado (D-115); (3) `gsi4_read_policy_json` nunca anexado
   aos 10 Lambdas reais que o consomem (D-116); (4) `GET /bff/organizations`/`POST
   /bff/organization/select` nunca tiveram rota real no API Gateway (D-117); (5) nenhuma tela de
   "criar organizaÇõÇœo" existia no frontend, todo usuÇ­rio sem Organization ficava preso em
   "Carregandoƒ?Ý" para sempre (D-118, feature nova `Onboarding.tsx`+`OnboardingGate.tsx`); (6)
   `ExpressionAttributeNames: {}` explÇðcito quebrava `TransactWriteItems` real com
   `ValidationException` em 3 arquivos, o mais severo sendo o guard de Ç§ltima-proteÇõÇœo-de-OWNER
   (D-119); (7) aceitar convite nunca teve tela nem rota real no API Gateway (mesma classe do
   D-117) e o e-mail real de convite nunca foi ativado ƒ?" SES sandbox sem NENHUM endereÇõo
   verificado no ambiente inteiro (D-120, PR #121, CD `success` confirmado ƒ?" 2 endereÇõos reais
   verificados com o Marcelo, `LeaveOrganizationSection` novo, `handleListMembers` parou de
   vazar o item bruto do DynamoDB). **Testado ao vivo** com o cookie de sessÇœo real do Marcelo:
   save de Settings, `GET /bff/organizations`, `GET /bff/api/organizations/members` todos
   confirmados OK; `POST /bff/organizations` corretamente bloqueado por `CONFLICT` (cap de uma
   organizaÇõÇœo por `GlobalUser`, confirma que troca de organizaÇõÇœo sÇü Ç¸ testÇ­vel depois do
   convite/aceite fechar o ciclo). **Pendente para fechar B2B-14**: teste de convite/aceite com
   uma segunda conta e o resto do roteiro manual (trocar role, trocar de organizaÇõÇœo, revogar
   membership, excluir organizaÇõÇœo) ƒ?" **Marcelo confirmou explicitamente que isso fica para uma
   sessÇœo futura, nÇœo bloqueia nada agora**. B2B-15 (Documentation Reconciliation, checklist
   `AGENTS.md` ¶õ6) jÇ­ comeÇõou em paralelo (nÇœo depende de B2B-14 fechar) ƒ?" ver item da tabela de
   gates abaixo para o que jÇ­ foi feito/falta.

   **3 lacunas reais de produto, fora do escopo original de B2B-0..15, levantadas pelo Marcelo em
   2026-08-30** (detalhe completo nas memÇürias de sessÇœo ƒ?" ver rodapÇ¸ deste arquivo): (a) o
   pipeline de purga de tenant (W3-07) estÇ­ implementado e testado, mas nada o aciona
   automaticamente quando uma conta/organizaÇõÇœo era fechada ƒ?" **`APROVADO` em D-121,
   `IMPLEMENTADO` em D-124, mergeado em `main` (PR #122) e CD `success` em 2026-08-31 ƒ?" state
   machine `exptrk-dev-tenant-purge` e sweeper `exptrk-dev-tenant-purge-sweeper` (`ENABLED`)
   confirmados ao vivo contra `dev` via `aws --profile claude-dev`. Totalmente resolvido; falta sÇü
   uma purga real de ponta a ponta para virar `E2E PROVEN`, sem urgÇ¦ncia**; (b) exportaÇõÇœo de dados (CSV/planilha)
   nunca foi implementada, sÇü importaÇõÇœo (M10) ƒ?" lacuna real dado que LGPD exige portabilidade
   como direito do titular, escopo ainda nÇœo debatido; (c) quando um `Membership` Ç¸ removido,
   `ExpirationItem.assigneeUserId`/`ItemWatch` nÇœo sÇœo reatribuÇðdos nem limpos ƒ?" o campo fica
   ÇürfÇœo indefinidamente e a notificaÇõÇœo sÇü para de disparar silenciosamente, sem fallback nem
   fluxo de reatribuiÇõÇœo ƒ?" escopo ainda nÇœo debatido.
2. **W3-07 ƒ?" purge pipeline durÇ­vel + orquestrador**: pipeline implementado/revisado (D-081/D-082/D-083, Codex 9,1/10) e emendado para as entidades B2B (B2B-9/D-104). **Orquestrador `IMPLEMENTADO` (D-124, 2026-08-31)** ƒ?" exatamente o mecanismo `APROVADO` em D-121, implementaÇõÇœo direta sem rodada nova: mÇüdulo `infra/modules/tenant-purge-workflow/` + `infra/state-machines/tenant-purge.asl.json` (Wait 1800s ƒÅ' transiÇõÇæes ƒÅ' `RunPurge` ƒÅ' `Choice` com bound `PURGE_RETRY_LIMIT=20` ƒÅ' `VERIFIED`/`DELETED`, ramos `MarkBlocked*` ƒÅ' `Fail`), 3 Lambdas novos (transiÇõÇœo Ç§nica parametrizada por `from`/`to`, wrapper de `purgeTenant()` devolvendo o envelope compacto, sweeper), 2 alarmes `AWS/States` novos (`ExecutionsFailed`+`ExecutionsTimedOut`), EventBridge Scheduler `rate(1 day)`, `CloseOrganizationService`+action `organization:close` (`OWNER_ROLES`)+rota `POST /organizations/close`+UI type-to-confirm, composition root real fechando o KNOWN LIMIT de prefixoƒÅ"bucket de `purge-tenant.ts` (tabela FECHADA bucketƒÅ'raiz). `transitionTenantLifecycle()`/`purgeTenant()` deixam de ser cÇüdigo sem chamador. **Mergeado em `main` (PR #122, 2026-08-31) e CD `success`** ƒ?" confirmado ao vivo contra `dev` (`aws stepfunctions list-state-machines`/`aws scheduler list-schedules` via `--profile claude-dev`): `exptrk-dev-tenant-purge` existe, `exptrk-dev-tenant-purge-sweeper` estÇ­ `ENABLED` e rodando `rate(1 day)`. **Downstream ainda pendente**: nenhuma purga real de tenant executou ainda de ponta a ponta (nÇœo hÇ­ tenant genuinamente `DELETING` em `dev` hoje) ƒ?" fica para quando `CloseOrganizationService` for exercitado de verdade, mesmo padrÇœo de "cÇüdigo deployado, sem E2E PROVEN ainda" que outras waves jÇ­ registraram.
3. **W3-07 ƒ?" fencing dos writers de negÇücio** (`TenantBusinessMutation`): a maioria dos writers reais jÇ­ fenced (chunks D-068 a D-080, ver `decisions-log.md`). Gap residual documentado, nÇœo explorÇ­vel hoje: entradas sem PK `TENANT#`-prefixed E sem `tenantId` declarado (`LoginAttempt`/`GuestRateLimitRecord`) passam sem verificaÇõÇœo ƒ?" nenhum call site real produz isso.

## Gates / bloqueios abertos

| Item | Precisa de | Onde estÇ­ o detalhe |
|---|---|---|
| Wave B2B-14 ƒ?" fechar o roteiro manual (convite/aceite com 2¶¦ conta, troca de role, switch de organizaÇõÇœo, revogaÇõÇœo, exclusÇœo de organizaÇõÇœo) | **Adiado pelo prÇüprio Marcelo para uma sessÇœo futura** ƒ?" nÇœo bloqueia nada, nÇœo perguntar de novo atÇ¸ ele sinalizar | `decisions-log.md` D-120, `multi-user-b2b-wave-tracker.md` linha B2B-14 |
| ExecuÇõÇœo real destrutiva de `scripts/reset-dev-data.ts` (`--confirm`/`--include-cognito` contra `dev`) | ConfirmaÇõÇœo explÇðcita do Marcelo antes de rodar ƒ?" design/implementaÇõÇœo/verificaÇõÇœo de leitura jÇ­ `DONE` (D-110/D-111) | `docs/architecture/multi-user-b2b-wave-b2b12-scope.md`, `decisions-log.md` D-111 |
| `AppError.retryable` ƒ?" deveria decidir comportamento real de SQS retry/DLQ? | DecisÇœo de produto do Marcelo | `docs/engineering/decisions-log.md` E-011 |
| 7 de 9 classes de retenÇõÇœo LGPD sem purga fÇðsica real dentro de tenant `ACTIVE` (`privacy-lgpd.md` ¶õ4) | **`APPROVED` (design + priorizaÇõÇœo), D-127, 2026-08-31 ƒ?" 6 dos 7 workers `IMPLEMENTADO` E `DEPLOYADO` em `dev` (confirmado ao vivo via `aws lambda get-function`)**: `CORE_USER_DATA` D-151, `DELIVERY_RECORD` D-152, `SECURITY_AUDIT` D-153, `QUOTA_TELEMETRY` D-154 (sÇü `TenantQuotaRecord`), `TRANSIENT` remanescente D-156, todos 2026-09-01; `ACCOUNT_ACTIVE (nÇœo-fechamento)` (Prioridade 5) **`IMPLEMENTADO PARCIALMENTE E DEPLOYADO`, D-155** ƒ?" sÇü `Invitation`; `Membership`/`Channel` bloqueados por timestamp de elegibilidade ausente (`Membership.removedAt`), decisÇœo de Marcelo pendente antes de destravar. SÇü `LEGAL_EVIDENCE` (Prioridade 7, bloqueada por trava jurÇðdica) segue sem inÇðcio, corretamente | `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`, `decisions-log.md` D-151 a D-156 |
| User Validation (planejamento de interface) | Sinal explÇðcito do Marcelo para retomar | `docs/frontend/README.md` |
| Wave 1b ƒ?" Design System Implementation Gap: `Divider`/`IconButton` implementados (D-133); `Link`/`Checkbox`/`RadioGroup`/`Switch` implementados (D-134, 2026-08-31); Button `lg`/motion `slow` decididos como "nÇœo agora" (D-132); z-index jÇ­ 100% implementado (nÇœo era gap); breakpoints nomeados decididos como "nÇœo agora" pelo mesmo critÇ¸rio de D-132. **Ainda faltam do catÇ­logo de ~30** (`design-system.md` ¶õ29), todos com estratÇ¸gia de overlay/portal/focus-trap nÇœo decidida ou composiÇõÇœo maior nÇœo-trivial, deliberadamente nÇœo implementados: `Combobox`, `DateInput`/`DatePicker`, `Tooltip`, `Popover`, `DropdownMenu`, `Modal`/`Dialog`, `Drawer` (candidatos a protocolo ClaudeƒÅ"Codex completo quando abordados ƒ?" nÇœo hÇ­ padrÇœo de overlay/focus-trap jÇ­ implementado no projeto para reusar), `Tabs`, `Pagination`, `Breadcrumb`, `Avatar` (composiÇõÇœo maior, sem ambiguidade de padrÇœo). `Card` (¶õ40) tem conflito real com decisÇœo jÇ­ em produÇõÇœo em `Layout.tsx` ("no Card component, on purpose") ƒ?" nÇœo implementado, gap nomeado para decisÇœo explÇðcita futura, nÇœo silenciosa. Nenhum dos 4 novos componentes de D-134 (`Link`/`Checkbox`/`RadioGroup`/`Switch`) estÇ­ consumido em nenhuma tela ainda ƒ?" mesmo padrÇœo de `Divider`/`IconButton` em D-133, primitivos de catÇ­logo prontos para uso, nÇœo wireados retroativamente em telas existentes nesta sessÇœo. | SessÇœo futura dedicada ƒ?" lista acima Ç¸ o escopo remanescente exato, nÇœo "reconciliar tudo" | `docs/architecture/reviews/wave1b-button-lg-motion-scoping/estado-final-consolidado.md`, `decisions-log.md` D-132/D-133/D-134, `docs/architecture/reviews/design-system-reconciliation-scoping/estado-final-consolidado.md` |
| **EstratÇ¸gia comum de quarentena antes de exclusÇœo fÇðsica de dado persistido** | **`APPROVED` (design) D-127 ƒÅ' `IMPLEMENTADO` E `DEPLOYADO`, D-150, 2026-09-01** ƒ?" estado `HELD_FOR_RECOVERY` (30 dias, cancelÇ­vel) real entre `ACTIVE` e `DELETING` no grafo do W3-07, `CancelOrganizationClosureService`, ASL estendido, sweeper com reconciliaÇõÇœo estrita; confirmado ao vivo (`aws stepfunctions describe-state-machine` mostra `HELD_FOR_RECOVERY` na definiÇõÇœo real). Pendente: migraÇõÇœo de writers `ReminderOccurrence`/regra ESLint (round-11 do design) e a lane bloqueada `LEGAL_EVIDENCE` (Prioridade 7, trava jurÇðdica) ƒ?" as outras 6 prioridades LGPD jÇ­ tÇ¦m worker `IMPLEMENTADO`(_PARCIALMENTE_ no caso de `ACCOUNT_ACTIVE`) e deployado | `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`, `decisions-log.md` D-127/D-150 |
| **PRs #152/#156 (Dependabot: ESLint 8.57.1ƒÅ'10.9.1, root + frontend)** | **RESOLVIDO, D-157, 2026-09-01** ƒ?" root migrado para ESLint 10 + flat config (`eslint.config.js`), PR #152 fechado (superseded); frontend permanece bloqueado por upstream (`eslint-plugin-jsx-a11y` sem suporte a ESLint 10 ainda), PR #156 fechado sem merge, dÇðvida EOL registrada, reavaliar quando o plugin publicar suporte | `decisions-log.md` D-157 |
| ~~`Membership.removedAt` ausente~~ (achado real de D-155) | **RESOLVIDO, D-158, 2026-09-01** ƒ?" campo adicionado e gravado/limpo nos 3 write paths reais; worker de purga fÇðsico de `Membership`/`Channel` (Prioridade 5 remanescente de D-127) segue como prÇüxima frente, agora desbloqueada | `decisions-log.md` D-155, D-158 |
| **OtimizaÇõÇœo de performance do hot path (D-136, 2026-08-31)** ƒ?" `APPROVED` (design), 3 rodadas, Claude 9,3/Codex 9,3. Gatilho real: Marcelo reportou lentidÇœo para mostrar conteÇ§do + mensagem "validando sessÇœo" desnecessÇ­ria. 5 decisÇæes design-only, status atualizado em 2026-08-31/09-01: **D-A `IMPLEMENTADO`** (D-140 ƒ?" sessÇœo frontend Ç§nica query, waterfall removido, skeleton sem texto tÇ¸cnico), **D-B `IMPLEMENTADO`** (D-141 ƒ?" session touch coalescing, `ConsistentRead` mantido), **D-C teve Rodada 1 tentada e REJEITADA nesta sessÇœo (D-159, 2026-09-01, nota 5,5/10, nÇðvel 5 confirmado pelo Codex, nÇœo nÇðvel 4)** ƒ?" a proposta original (tornar a leitura de `createProfileIfAbsent()` eventualmente consistente) nÇœo satisfaz o texto jÇ­ `APPROVED` ("sai do hot path"), sÇü reduz o custo sem eliminar o round-trip sÇðncrono; achado real mais importante: `UserProfile` (a entidade que esse mÇ¸todo escreve) nÇœo tem NENHUM leitor confirmado em todo o cÇüdigo ƒ?" a pergunta certa para a prÇüxima rodada Ç¸ remover-ou-relocar a entidade, nÇœo mudar `IdentityStore.get()`, **D-D `IMPLEMENTADO`, D-172, 2026-09-02** (lane `EphemeralTelemetryMutation` real para `API_REQUEST`, D-154 alargado, teste adversarial do DoD confirmado ao vivo ƒ?" ver `decisions-log.md` D-172), **D-E `APPROVED` e `IMPLEMENTADO`** (D-142, 4 rodadas Claude 9,3/Codex 9,6 ƒ?" paginaÇõÇœo real via cursor no dashboard/coleÇõÇœo, `queryGsi1Page`/`DashboardPage`/`useItemsDashboardBounded`+`useItemsDashboardPage`, corrige o bug de skip de cursor e o overflow do cap de export achados nas rodadas). Auditorias de origem em `docs/architecture/reviews/performance-hotpath-scoping/`. **Node 20ƒÅ'24 (D-137/D-138) jÇ­ implementado e deployado**. Itens mecÇ½nicos ainda pendentes (ADOT, Lambda memory/ARM64, SQS batch, React.lazy, staleTime por query, N+1 BatchGetItem) ficam para implementaÇõÇœo direta futura, sem protocolo | D-C (decisÇœo de arquitetura ƒ?" comeÇõar pela pergunta "UserProfile ainda precisa existir?", ver D-159) e D-D (implementaÇõÇœo + teste adversarial) ficam para sessÇœo futura dedicada, protocolo completo (mÇðnimo 3 rodadas, nota ƒ%¾9,0) | `docs/architecture/reviews/performance-hotpath-scoping/estado-final-consolidado.md`, `decisions-log.md` D-136/D-140/D-141/D-142/D-159 |
| **Node 20ƒÅ'24 + AWS provider v5ƒÅ'v6 (D-137/D-138, 2026-08-31) ƒ?" `IMPLEMENTADO` e `DEPLOYADO`.** Node 20 jÇ­ depreciado pela AWS (30/04/2026); migraÇõÇœo revelou bloqueio real (`nodejs24.x` exige `hashicorp/aws >= 6.19.0`, projeto pinado em `~5.0` em 22 mÇüdulos) ƒ?" resolvido com upgrade do provider (pesquisa externa confirmou nenhum breaking change do v6 afeta este projeto). CI real (`infra/tests/stack.tftest.hcl`) 19/19, `terraform validate` limpo (14 avisos de depreciaÇõÇœo `range_key`/`hash_key`ƒÅ'`key_schema`, nÇœo bloqueante, nÇœo corrigido). PR #131 mergeado, CD `success`, **confirmado ao vivo**: `exptrk-dev-bff-handler` rodando `nodejs24.x`/`Active` via `aws lambda get-function-configuration --profile claude-dev`. Lambda memory (256MB uniforme) deliberadamente nÇœo alterado ƒ?" Power Tuning real fica para sessÇœo futura | Nenhuma ƒ?" implementado e verificado ao vivo | `decisions-log.md` D-137/D-138 |
| **Dependency Freshness Standard (D-139 design ƒÅ' D-148 implementaÇõÇœo, 2026-09-01) ƒ?" `IMPLEMENTADO`.** `.github/dependabot.yml` (npm root+`/frontend`, terraform `/infra`+`/infra/modules/*` via glob, github-actions `/`, weekly, sem auto-merge); `scripts/check-dependency-freshness.ts` + `docs/engineering/dependency-freshness-policy.json` (9 entradas reais, `node`/`lambda-runtime` com `lifecycle` populado com EOL real pesquisado ƒ?" ambos 2028-04-30, fontes `endoflife.date`/AWS Lambda runtimes doc, janela `ok` hoje); `npm run check-dependency-freshness` wired em `ci.yml`; fix real do `ci.yml`/`cd.yml` hardcodando `terraform_version` separadamente (agora `env.TERRAFORM_VERSION` em cada workflow, checker prova sincronia). 15 testes novos, G-V3 confirmado ao vivo na fronteira 6/12 meses | Nenhum item aberto ƒ?" os 3 entregÇ­veis de D-139/¶õ7 estÇœo completos e `npm run check-dependency-freshness` passa limpo contra o repo real | `decisions-log.md` D-148, `docs/engineering/dependency-freshness-standard.md`, `docs/engineering/dependency-freshness-policy.json`, `scripts/check-dependency-freshness.ts` |
| **DomÇðnio Documental ƒ?" arquitetura tÇ¸cnica (D-143, 2026-09-01)** ƒ?" `APPROVED` via protocolo ClaudeƒÅ"Codex, **6 rodadas** (4,6ƒÅ'5,9ƒÅ'7,6ƒÅ'8,4ƒÅ'8,8ƒÅ'9,3), Claude 9,3/Codex 9,3. Implementa as decisÇæes funcionais D1ƒ?"D10/C1ƒ?"C6 jÇ­ `APROVADO` pelo Marcelo (`docs/frontend/document-domain-functional-decisions.md`) ƒ?" pedido explÇðcito dele: analisar os 6 docs de planejamento, pesquisar externamente, rodar o protocolo, sÇü entÇœo implementar. 9 decisÇæes tÇ¸cnicas fechadas: state machines Document/DocumentVersion com transaÇõÇœo Ç§nica para o fluxo comprimido C3; armazenamento single-table com GSI2/GSI5 novos (verificados livres por grep, nunca GSI3/GSI4/GSI6 jÇ­ ocupados por reminder/membership) e transaÇõÇœo `acceptVersion` de atÇ¸ 10 aÇõÇæes com idempotÇ¦ncia real; `DocumentVersionEvent` append-only distinto de `OutboxEvent`; guest access em 3 camadas (credencial longa+revogÇ­vel/sessÇœo curta com interstitial/submit idempotente); `Requirement.applicability` como fato persistido + job diÇ­rio de reindexaÇõÇœo; arquivos com scan individual (`pendingFileScans`/`infectedFileScans`); `REJECTED` nunca removÇðvel (corrige contradiÇõÇœo real com J9 encontrada na Rodada 1); recorrÇ¦ncia com `seriesId`/`occurrenceId`/`requestId`/`attemptIndex` e materializaÇõÇœo atÇïmica; RBAC tenant-wide reaproveitado, download por Viewer (A1) deixado genuinamente aberto. **ImplementaÇõÇœo do NÇ§cleo 1 ƒ?" camada de aplicaÇõÇœo COMPLETA (2026-09-01)**: mÇüdulo `src/modules/document-archive/` com `domain/`+`ports/`+`persistence/`+`application/`+`http/` todos implementados e testados ƒ?" `DocumentArchiveService` (`reserveUpload`/`commitUpload`/`claimReview`/`acceptVersion`/`rejectVersion`) com RBAC real (`docarchive:create/read/upload/review`, novas actions em `authorization.ts`) e `assertReviewerOrAdmin()` (D-143 Decision 1/Bloqueador 6 ƒ?" MEMBER que nÇœo reivindicou uma Version nÇœo pode decidi-la, ADMIN/OWNER sempre podem); `http/document-archive-handlers.ts` no mesmo pipeline de `item-handlers.ts`; 6 schemas JSON novos (`schemas/api/docarchive-*.json`) registrados no `defaultSchemaRegistry`; composition root (`src/runtime/aws/composition/document-archive.ts`) e Lambda handler (`src/runtime/aws/handlers/document-archive-handler.ts`, mesmo padrÇœo de `items-handler.ts`). SuÇðte: 30 testes unitÇ­rios de serviÇõo/domÇðnio + 79 testes de contrato + 4 testes de integraÇõÇœo end-to-end reais (createƒÅ'reserveUploadƒÅ'commitUploadƒÅ'claimReviewƒÅ'acceptVersion via HTTP handlers reais; reject-entÇœo-reupload provando que a version rejeitada nunca some do histÇürico; VIEWER negado com 403 real via `authorize()`; 400 de schema) ƒ?" G-V3 confirmado 3x ao vivo (grafo de transiÇõÇœo, gate de scan de arquivo, gate de reviewer/admin). `npm test` 1345/1345, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos. **NÇ§cleo 1 100% IMPLEMENTADO e DEPLOYADO (2026-09-01)**: infra Terraform real aplicada em `dev` ƒ?" `exptrk-dev-document-archive-handler` Lambda (sem polÇðtica IAM dedicada, GSI2/GSI5 jÇ­ cobertos por `tenant_facing_read_write_policy_json`) + 8 rotas `/document-archive/*` no API Gateway + `build-lambdas.ts`/`proxy-allowlist.ts` atualizados no mesmo commit (evita a classe de bug D-117/D-120 de rota/handler ÇürfÇœo). `terraform test` (suÇðte real de CI) 19/19; apply via plano `-target`-escopado (16 a adicionar, 0 a mudar, 0 a destruir ƒ?" excluindo deliberadamente ~76 diffs de `source_code_hash` de um rebuild completo de lambdas, fora do escopo desta mudanÇõa); confirmado ao vivo via `aws lambda get-function` (Active) e `aws apigatewayv2 get-routes` (8 rotas presentes). Achados reais corrigidos durante a implementaÇõÇœo: o fake de teste (`in-memory-store.ts`) inicialmente avaliava CADA par nome/valor de `ExpressionAttributeNames`/`Values` como condiÇõÇœo, confundindo placeholders de SET com condiÇõÇæes reais ƒ?" corrigido com um parser pequeno e real da gramÇ­tica AND/OR que os builders de `occ.ts` produzem; e a validaÇõÇœo de transiÇõÇœo rodava ANTES da checagem de idempotÇ¦ncia, classificando erroneamente um replay legÇðtimo como transiÇõÇœo invÇ­lida ƒ?" corrigido invertendo a ordem. **NÇ§cleo 2 (Requirement/D-145, guest access/D-146, recorrÇ¦ncia/D-147) 100% completo em cÇüdigo E DEPLOYADO (2026-09-01)** ƒ?" as 9 decisÇæes tÇ¸cnicas de D-143 estÇœo todas implementadas e confirmadas ao vivo em `dev` (Lambdas `Active`, rotas presentes, schedulers `ENABLED`); nenhum escopo do DomÇðnio Documental fica sem cÇüdigo ou sem deploy | Nenhuma ƒ?" DomÇðnio Documental fechado por completo. PrÇüximo NÇ§cleo do roadmap (se houver) Ç¸ decisÇœo de produto do Marcelo | `docs/architecture/reviews/document-domain-scoping/estado-final-consolidado.md`, `decisions-log.md` D-143/D-145/D-146/D-147, `src/modules/document-archive/`, `infra/main.tf` |

## DecisÇæes deliberadamente adiadas (jÇ­ decididas como "nÇœo agora", nÇœo esquecidas)

- M12 (Billing) ƒ?" bloqueado por escolha de fornecedor de pagamento (D-052).
- OpÇõÇœo B (atributo de span OpenTelemetry para correlationIdƒÅ"trace) ƒ?" candidata futura, 3 prÇ¸-requisitos nomeados, nÇœo perseguida agora (`correlationid-xray-trace-join.md` ¶õ2).
- Document Lifecycle Management ƒ?" mesma classe, arquivado como informativo (`docs/architecture/roadmap-evolution/16-document-lifecycle-strategic-analysis.md`).
- BFF/frontend quality standard (rubrica mais ampla que `interface-quality-standard.md`) ƒ?" proposta nÇœo adotada (`docs/frontend/bff-frontend-quality-standard-proposal.md`).
- Itens explicitamente fora de escopo do prÇüprio Multi-User B2B (`roadmap-evolution/17` ¶õ98): custom roles, ABAC, SAML/SCIM, seat billing, platform admin/impersonation, department hierarchy.
- **Synthetic Persona Evaluation Framework** (`docs/architecture/roadmap-evolution/18-20`, trazido pelo Marcelo 2026-08-31) ƒ?" **decisÇœo explÇðcita dele (2026-08-31): nÇœo Ç¸ prioridade agora, mas precisa estar pronto quando chegar a hora de testes mais robustos antes de testes com humanos de verdade.** Gatilho de retomada: quando a sessÇœo decidir avanÇõar para essa fase de testes prÇ¸-User-Validation (nÇœo antes ƒ?" `User Validation` em si tambÇ¸m estÇ­ em suspenso, ver linha acima). Nessa hora, retomar nesta ordem: (1) confirmar com o Marcelo se ainda Ç¸ a prioridade certa naquele momento; (2) fechar primeiro a lacuna mais barata que o doc 19 jÇ­ nomeia ƒ?" Playwright hoje roda majoritariamente contra BFF mockado (`page.route()`), nÇœo contra o sistema real; (3) submeter o doc 20 (trial identity, tecnicamente sÇülido nesta leitura ƒ?" metadata nunca em entidade de domÇðnio, propagaÇõÇœo via outbox `metadata`/nunca `payload`, gate de ambiente, fail-closed) Çÿ Rodada 1 do protocolo ClaudeƒÅ"Codex antes de qualquer cÇüdigo.

## PrÇüxima aÇõÇœo, em ordem de valor esperado

**Estado no inÇðcio da prÇüxima sessÇœo**: ESLint (D-157), `Membership.removedAt` (D-158) e D-C do hot path (D-160, `UserProfile` removido) foram todos fechados numa mesma sessÇœo (2026-09-01), incluindo merge em `main`. Nesta MESMA sessÇœo, Marcelo trouxe um roadmap competitivo novo (`docs/project/roadmap-competitivo-2026-09-01.md`) e pediu reconciliaÇõÇœo + macro-ordem via protocolo ƒ?" **fechado como D-161** (`docs/architecture/reviews/competitive-roadmap-reconciliation/estado-final-consolidado.md`, Codex 9,6/10). Isso muda a prioridade #1 da lista abaixo ƒ?" nÇœo Ç¸ mais "pergunte a Marcelo o que priorizar entre itens desconexos", Ç¸ "comece pelo roadmap competitivo, seguindo a ordem jÇ­ decidida":

1. **Roadmap Competitivo (D-161)** ƒ?" spike de auditoria dos itens 4/9/10 **fechado nesta sessÇœo** (2026-09-01, continuaÇõÇœo), e a decisÇœo fundacional #1 (`DocumentFile`) **fechada como design, D-163**:
   - **Item 9 (guest upload) re-confirmado ao vivo**: `commitUpload()` (`document-archive-service.ts` linhas 157-177) sÇü flipa `DocumentVersion.state` para `RECEIVED` ƒ?" nenhum S3/presign/scan real. Achado de D-161 confirmado sem ambiguidade.
   - **Item 5 (bug do GSI2) corrigido ƒ?" D-162**: `createDocument()` nunca gravava GSI2 apesar do comentÇ­rio afirmando que gravava. Fix mecÇ½nico aplicado, testado (G-V3 ao vivo), 1663/1663 verde. Leitura por GSI2 (a feature "Busca por Subject" em si) continua greenfield, nÇœo implementada ƒ?" sÇü a escrita foi corrigida.
   - **DecisÇœo fundacional #1 (`DocumentFile`) reclassificada e fechada como design ƒ?" D-163**: o scoping real (nÇœo a estimativa otimista do handoff anterior) confirmou **nÇðvel 5**, nÇœo 3-4 ƒ?" falta todo um subsistema (entidade + storage + invariantes transacionais + reconciliaÇõÇœo), nÇœo sÇü um data-write num campo jÇ­ modelado. Protocolo completo, 4 rodadas (5,2ƒÅ'7,1ƒÅ'8,3ƒÅ'9,4), `APPROVED (design)`. Ver `docs/architecture/reviews/document-file-scoping/estado-final-consolidado.md` para o design completo.
   - **ImplementaÇõÇœo real, primeira fatia ƒ?" D-164 (2026-09-02)**: entidade `DocumentFile` + `reserveFiles()` (fecha a corrida de PRINCIPAL numa `TransactWriteItems` real) + `ConditionCheck` transacional do PRINCIPAL em `acceptVersion()`, ambos com G-V3 confirmado ao vivo.
   - **ImplementaÇõÇœo real, segunda fatia ƒ?" D-165 (2026-09-02)**: `applyFileScanResult()`/`confirmFileScanClean()` (`src/modules/document-archive/application/apply-file-scan-result.ts`) ƒ?" transiÇõÇœo terminal do scan (PENDING_UPLOAD/SCANNING ƒÅ' CLEAN/REJECTED/UNSUPPORTED/TIMEOUT) + correlaÇõÇœo simÇ¸trica de evidÇ¦ncia S3/GuardDuty, reusando `decideNextAction()` de M6 verbatim; wireado em `DocumentArchiveService`. G-V3 confirmado ao vivo nos 3 mecanismos pedidos (corrida simÇ¸trica, idempotÇ¦ncia terminal, remoÇõÇœo do ponteiro GSI5). SuÇðte completa 1677/1677. Ver `decisions-log.md` D-165.
   - **ImplementaÇõÇœo real, terceira fatia ƒ?" D-166 (2026-09-02)**: `DocumentFileReconciliationWorker` (`src/workers/document-file-reconciliation/`), generaliza `UploadSlotReconciliationWorker` de M6 para o namespace GSI5 esparso de `DocumentFile` (`TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`, `fileReconciliationGsi5Keys()` de D-164, sem chamador atÇ¸ agora). `applyFileScanTimeout()` novo em `apply-file-scan-result.ts` faz a transiÇõÇœo terminal por deadline (espelha o branch REJECT existente, mesmo `buildVersionedUpdate`/mesma remoÇõÇœo de ponteiro GSI5), com uma condiÇõÇœo extra `GSI5PK = :observado AND GSI5SK = :observado` que fecha a corrida "candidato jÇ­ avanÇõou entre a varredura e a escrita" (round4-claude-final.md ¶õ3). **Desvio nomeado do design**: GSI5PK de `DocumentFile` Ç¸ tenant-scoped (diferente do GSI6 global que permite ao worker de M6 fazer uma `Query` cross-tenant real) ƒ?" este worker descobre candidatos via `Scan` na tabela base filtrado por `entityType`/`scanStatus`/`attribute_exists(GSI5PK)`, nunca GSI6, mesmo trade-off jÇ­ aceito por `scanSatisfiedRequirements`/`scanActiveSeries` deste mÇüdulo. Lambda novo `document-file-reconciliation-handler`, schedule `rate(15 minutes)`, sÇü `tenant_facing_read_write_policy_json` (sem GSI6/GSI3 novo, confirmado por teste Terraform). G-V3 confirmado ao vivo em 2 pontos (condiÇõÇœo exata do ponteiro; comparador de deadline). SuÇðte completa 1685/1685, `terraform test` 21/21. Ver `decisions-log.md` D-166. **ImplementaÇõÇœo real, quarta e quinta fatia ƒ?" D-167 (2026-09-02)**: itens 3+4 da ordem, ambos fechados na mesma sessÇœo. Item 3 (presign real): `DocumentArchiveService.reserveFiles()` agora presigna cada `DocumentFile` de verdade via `UploadUrlSigner` (mesma porta genÇ¸rica de M6, `S3UploadUrlSigner` reusado sem adapter novo), presign acontece DEPOIS da `TransactWriteItems` comitar (mesma ordem de M6), retorno mudou de `DocumentFile[]` para `ReservedFile[]` (`{file, uploadUrl, requiredHeaders}`). Item 4 (rota HTTP): `POST /document-archive/documents/{documentId}/versions/{seq}/files` nova (`handleReserveFiles`, schema `docarchive-reserve-files-request.v1.json`, mesma RBAC `docarchive:upload` de `reserveUpload`/`commitUpload`), IAM novo `document_archive_presign_quarantine_put` (mesmo par PutObject+GenerateDataKey escopado que `documents_handler` jÇ­ tem, nunca mais amplo). **Achado real corrigido no caminho**: o schema novo devolvia HTTP 500 atÇ¸ ser registrado em `src/shared/contracts/schema-validator.ts` ƒ?" `defaultSchemaRegistry` sÇü carrega schemas estaticamente importados, mesma classe de bug jÇ­ documentada ali para M7 item 8 (2026-08-27). G-V3 confirmado ao vivo (presign real + rota HTTP 201/400/403, quebrando e restaurando cada mecanismo). SuÇðte completa 1692/1692, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21) todos limpos. Ver `decisions-log.md` D-167. **Pendente real, nomeado explicitamente, prÇüxima e Ç§ltima fatia deste arco (item 5, deliberadamente NÇŸO implementado nesta sessÇœo)**: ativar o gate `fileSetSealed` em `commitUpload()` (D-163 ¶õ4, desenhado mas nÇœo ligado ƒ?" alguns fluxos/testes legados ainda chamam `commitUpload()` sem nunca passar por `reserveFiles()` primeiro, entÇœo ativar o gate exige primeiro migrar/decidir esses caminhos). O worker S3/GuardDuty real que chama `applyFileScanResult()`/`confirmFileScanClean()` em produÇõÇœo tambÇ¸m segue pendente (fora do escopo dos itens 3-4). Infra desta fatia (rota+IAM) estÇ­ escrita/validada mas ainda nÇœo aplicada em `dev` ƒ?" aplica automaticamente no prÇüximo CD de push a `main` (`AGENTS.md` ¶õ7), nÇœo precisa de `terraform apply` manual.
   - **ImplementaÇõÇœo real, sexta e Ç§ltima fatia ƒ?" D-168 (2026-09-02)**: item 5 da ordem, ativaÇõÇœo do gate `fileSetSealed` em `commitUpload()` (D-163 ¶õ4) ƒ?" o bloqueador nomeado por D-164/D-166/D-167 (nenhuma rota HTTP chamava `reserveFiles()` antes de `commitUpload()`) estava resolvido desde D-167 (rota HTTP real de `reserveFiles()`). Gate em duas camadas: prÇ¸-check em memÇüria + `extraConditions` transacional TOCTOU-safe (`#sealed = :true`), mesmo padrÇœo do fence do PRINCIPAL de D-164. Item 5 secundÇ­rio do escopo (recuperaÇõÇœo de `DRAFT` com PRINCIPAL infectado, `round4-claude-final.md` ¶õ6) jÇ­ estava fechado pelo design, nenhuma mudanÇõa de cÇüdigo necessÇ­ria ƒ?" confirmado por leitura antes de escrever qualquer cÇüdigo. 15 testes prÇ¸-existentes que chamavam `commitUpload()` sem `reserveFiles()` corrigidos (achado real esperado, nomeado por todos os handoffs anteriores). G-V3 confirmado ao vivo (remover prÇ¸-check + `extraConditions` simultaneamente faz o teste novo falhar). SuÇðte completa 1693/1693, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21) todos limpos. Ver `decisions-log.md` D-168.
   - **`DocumentFile` (D-163ƒÅ'D-168) estÇ­ 100% COMPLETO** ƒ?" entidade, `reserveFiles()`/fence do PRINCIPAL, `applyFileScanResult`/`confirmFileScanClean`/`applyFileScanTimeout`, worker de reconciliaÇõÇœo GSI5, presign real, rota HTTP de `reserveFiles()`, e o gate `fileSetSealed` em `commitUpload()`, todos implementados e testados. Çsnico item fora de escopo do arco inteiro (nomeado desde D-163, nunca escondido): o worker S3/GuardDuty real que chamaria `applyFileScanResult()`/`confirmFileScanClean()` em produÇõÇœo ƒ?" evento de infraestrutura fÇðsica, sessÇœo dedicada futura, nÇœo bloqueia o resto do roadmap.
   - **Item 8 da macro-ordem de D-161 (Document Types configurÇ­veis) ƒ?" `APPROVED (design)` (D-173, 2026-09-02).** Protocolo ClaudeƒÅ"Codex completo, **5 rodadas reais** contra o `codex exec` de verdade: Rodada 1 ƒÅ' Codex 6,4/10; Rodada 2 ƒÅ' rÇ¸gua 7,8/design 8,4; Rodada 3 (depois do reset de cota que bloqueou a sessÇœo anterior, confirmado real via `codex exec "hello"` em foreground antes de prosseguir) ƒÅ' rÇ¸gua 9,2/10 (**fechada, estÇ­vel**)/design 8,5; Rodada 4 ƒÅ' design 8,6 (corrigiu linguagem do schema HTTP + migrou CRUD do catÇ­logo para `TenantBusinessMutation`, mas errou a identidade da 4¶¦ entrada de `submitEvidence()`); Rodada 5 ƒÅ' design **9,4/10** (corrigiu por leitura direta do cÇüdigo real: a 4¶¦ entrada Ç¸ `Put(IdempotencyRecord)`, nÇœo `DocumentFile`, e o guest flow nÇœo ganha mapeamento posicional novo ƒ?" o `catch` existente jÇ­ colapsa por anti-enumeraÇõÇœo). **Gate ƒ%¾9,0/ƒ%¾9,0 atingido, sem arredondar.** DecisÇœo: catÇ­logo `DocumentType` tenant-scoped (PK `TENANT#t#DOCTYPE#id`, `documentTypeId` ULID opaco imutÇ­vel, `displayName` renomeÇ­vel, soft-state `ACTIVE`/`DEPRECATED`, dedupe via pointer transacional); os 4 writers de CRUD do catÇ­logo e `createDocument()` migram para a lane `TenantBusinessMutation`; `submitEvidence()` (guest, jÇ­ na lane) ganha sÇü o `ConditionCheck` novo na posiÇõÇœo `[0]`; `Document.documentType`ƒÅ'`documentTypeId`, GSI2 migra. Ver `docs/architecture/reviews/document-type-scoping/estado-final-consolidado.md` (design completo) e `decisions-log.md` D-173. **Itens 1-2 (domain entity + CRUD do catÇ­logo na lane `executeTenantBusinessMutation`) implementados em D-174 (2026-09-02)** ƒ?" ver `decisions-log.md` D-174. **Item 3 parcial em D-175 (2026-09-02)**: `createDocument()` migrado para `executeTenantBusinessMutation` com o `ConditionCheck(DocumentType.status=ACTIVE)` novo na posiÇõÇœo `[0]` (`DocumentTypeNotActiveError` nova, category CONFLICT) ƒ?" feito, testado (G-V3), verde em toda a suÇðte. `submitEvidence()` (guest) **NÇŸO migrado** ƒ?" achado real: `SubmitEvidenceInput.documentType` Ç¸ opcional no schema guest e cai para `requirementId` quando ausente numa rota jÇ­ viva; aplicar o `ConditionCheck` incondicional (como D-173 ¶õ4 descreve) quebraria toda submissÇœo guest sem `documentType` explÇðcito. **DecisÇœo de Marcelo sobre `submitEvidence()`/guest continua ABERTA, nÇœo resolvida por esta sessÇœo** (ver `decisions-log.md` D-175 para o achado completo): escolher entre (a) tornar `documentTypeId` obrigatÇürio no schema guest agora, adiantando parte do item 6; (b) aplicar o `ConditionCheck` sÇü quando `documentType` vier explÇðcito no input (guarda condicional nÇœo descrita no design original ƒ?" precisaria voltar ao protocolo ClaudeƒÅ"Codex se tratado como decisÇœo Type 1); (c) manter `submitEvidence()` como estÇ­ atÇ¸ o item 6 formal ser implementado, e fechar o item 3 como "parcial, aceito assim" nesta forma. **Item 4 (`Document.documentType`ƒÅ'`documentTypeId`, GSI2 migra) IMPLEMENTADO ƒ?" D-176, 2026-09-02** (ver `decisions-log.md` D-176) ƒ?" rename ponta a ponta no caminho autenticado, guest flow's comportamento/schema intocados, exatamente a fronteira que D-175 deixou aberta. **Item 5 (rotas HTTP `POST/GET/PATCH /document-archive/document-types*` para o CRUD do catÇ­logo) IMPLEMENTADO ƒ?" D-177, 2026-09-02** (ver `decisions-log.md` D-177) ƒ?" as 4 operaÇõÇæes de CRUD (D-174) + `listDocumentTypes()` novo, todas alcanÇõÇ­veis via HTTP real, RBAC/schema validation confirmados por teste (G-V3). **DecisÇœo de Marcelo sobre `submitEvidence()`/guest ƒ?" RESOLVIDA, D-184 (2026-09-02), via protocolo ClaudeƒÅ"Codex completo pedido explicitamente por Marcelo (3 rodadas reais, E-014 9,3/design 9,5, `APPROVED`)**: opÇõÇœo (b) modificada ƒ?" o `ConditionCheck(DocumentType.status=ACTIVE)` sÇü se aplica quando `input.documentType !== undefined` (presenÇõa explÇðcita, nÇœo truthy); ausente permanece byte-idÇ¦ntico ao comportamento anterior (fallback pra `requirementId`, sem validaÇõÇœo). OpÇõÇœo (a) rejeitada ƒ?" nem `DocumentRequest` nem `Requirement` referenciam um `DocumentType` hoje, sem mecanismo de descoberta para o guest saber o id certo. Achado real do Codex na Rodada 2 (replay idempotente ignora a validaÇõÇœo nova) mantido deliberadamente ƒ?" propriedade preexistente do `idempotencyKey`, documentada e testada, nÇœo um gap novo. Ver `docs/architecture/reviews/submit-evidence-document-type-scoping/estado-final-consolidado.md` e `decisions-log.md` D-184. **Çsnico item restante do arco D-173, agora nÇœo-bloqueado, mas ainda nÇœo iniciado**: item 6 (schema HTTP guest obrigatÇürio + rota pÇ§blica de leitura de tipos) ƒ?" pode ser retomado a qualquer momento, sem decisÇœo pendente.
2. **MaintenanceDueIndex (D-179)** ƒ?" narrativa fatia-a-fatia (D-180 a D-190) removida daqui, jÇ­ resumida na linha "D-180 a D-190" acima (9/9 workers migrados, EXECUÇÎÇŸO COMPLETA) e completa em `decisions-log.md`/`docs/architecture/reviews/maintenance-due-index-scoping/estado-final-consolidado.md`. **Item ainda nÇœo confirmado como fechado, preservado explicitamente**: o bug do guard `main()` (`fileURLToPath(import.meta.url) === process.argv[1]`, nunca a comparaÇõÇœo de string bruta que nÇœo casa no Windows) nos scripts `backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`/`backfill-reminder-policies.ts`/`reset-dev-data.ts` ƒ?" checar se foi corrigido antes de confiar que os backfills de D-180/D-182/D-183/D-185 escreveram os ponteiros GSI8 esperados contra `dev`.
3. **Wave 1b (Design System)** ƒ?" quais componentes com overlay/focus-trap abordar primeiro, se for a hora. **Deliberadamente por Ç§ltimo**, por pedido explÇðcito de Marcelo (2026-09-01).
4. ~~D-D da otimizaÇõÇœo de hot path~~ ƒ?" **RESOLVIDO, D-172, 2026-09-02** (ver acima).
5. Ou uma nova frente que Marcelo trouxer.
Itens abaixo sÇœo de sessÇæes anteriores, todos jÇ­ resolvidos ou explicitamente adiados por Marcelo ƒ?" nÇœo sÇœo trabalho pendente, mantidos sÇü como referÇ¦ncia rÇ­pida caso o assunto volte: Wave B2B-14 (roteiro manual convite/2¶¦ conta, adiado), execuÇõÇœo destrutiva de `scripts/reset-dev-data.ts --confirm` (aguarda sinal dele), `AppError.retryable` (jÇ­ fechado, D-128).

## Leitura obrigatÇüria antes da prÇüxima aÇõÇœo

`AGENTS.md` ¶õ2 (inÇðcio de sessÇœo) ƒÅ' `docs/architecture/README.md` (mapa vigente) ƒÅ' a linha da tabela "Gates / bloqueios abertos" relevante Çÿ tarefa escolhida, e sÇü entÇœo o documento/pasta de review que ela referencia.

## Status de evidÇ¦ncia (nÇœo presumir E2E sem checar)

| Item | Status |
|---|---|
| M7 extraÇõÇœo/OCR | `E2E PROVEN` (verificaÇõÇœo real 2026-08-27 contra `dev`) |
| Full BFF + Frontend Production Foundation | `APPROVED` + implementado, `E2E PROVEN` |
| W3-06 (`USER_DOCUMENT` purge) | `IMPLEMENTED`/`E2E PROVEN` (`terraform plan`/`test` reais contra `dev`) |
| W3-07 purge pipeline (D-081-083, emendado B2B-9/D-104) | `IMPLEMENTED`/`UNIT TESTED` ƒ?" entidades B2B incluÇðdas, sem orquestrador wireado, sem teste de integraÇõÇœo AWS real |
| E-011 correlationIdƒÅ"X-Ray | `E2E PROVEN` (smoke test real 2026-08-29 contra `dev`) |
| Visual Language + Design System | `APPROVED ... PROVISIONAL PENDING USER VALIDATION` |
| B2B-10 Tenant-aware Frontend (D-105/D-106) | `IMPLEMENTED`/`UNIT+COMPONENT+E2E TESTED` (139/139 vitest, 24/24 Playwright, mutaÇõÇœo verificada em 3 pontos) ƒ?" os 5 specs E2E mockavam `GET /bff/session` com a forma antiga (`tenantId`/`userId`), achado sÇü pelo CI apÇüs o push inicial, corrigido; sem E2E dedicado a multi-organizaÇõÇœo/switcher ainda (fica para B2B-13) |
| B2B-11 Responsibility + Notifications (D-107/D-108) | `IMPLEMENTED`/`UNIT TESTED` (1210/1210 backend, mutaÇõÇœo verificada em 2 pontos) ƒ?" sem verificaÇõÇœo E2E real contra `dev` (fica para B2B-14, Operational Evidence) |
| B2B-12 Cutover de dev (D-110/D-111) | `IMPLEMENTED`/`UNIT TESTED` (1232/1232 backend + 139/139 vitest + 24/24 Playwright frontend, mutaÇõÇœo verificada em 3 pontos) ƒ?" Fase A (read-only) do script **`E2E PROVEN`** (rodada de fato contra `dev` real via `aws --profile claude-dev`, manifest gravado com sucesso); Fase B (`--confirm` real, destrutiva) nunca executada ƒ?" aguarda confirmaÇõÇœo explÇðcita do Marcelo |
| B2B-13 E2E/Adversarial Security (D-112/D-113) | `IMPLEMENTED`/`UNIT TESTED` (1235/1235 backend, mutaÇõÇœo verificada em 4 pontos) ƒ?" auditoria contra as 25 perguntas de ¶õ121 completa (matriz QƒÅ'arquivo:linha em `multi-user-b2b-wave-b2b13-scope.md`); sem verificaÇõÇœo E2E real contra `dev` (fica para B2B-14, Operational Evidence) |
| B2B-14 Operational Evidence (D-114 a D-120) | `EM ANDAMENTO` ƒ?" 7 achados reais corrigidos e mergeados (D-120 Ç¸ o mais recente, PR #121, CD `success` confirmado); login OAuth, onboarding, GSI4/rotas de organizaÇõÇœo, save de Settings, listagem de organizaÇõÇæes/membros ƒ?" todos exercitados de verdade em `dev` (Settings/organizaÇõÇæes/membros via cookie de sessÇœo real do Marcelo). Roteiro manual de convite/aceite com 2¶¦ conta/troca de role/switch/revogaÇõÇœo/exclusÇœo de organizaÇõÇœo **adiado pelo Marcelo para uma sessÇœo futura**, nÇœo bloqueante |
| W3-07 orquestrador de purga (D-121 design, D-124 implementaÇõÇœo) | `IMPLEMENTED`/`DEPLOYED` (1267/1267 backend, 156/156 vitest frontend, `terraform test` 19/19 raiz + 9/9 mÇüdulo novo + 1/1 api-gateway + 7/7 bff-api-gateway; mutaÇõÇœo verificada e revertida em 3 pontos; mergeado em `main` PR #122, CD `success` 2026-08-31, state machine + sweeper `ENABLED` confirmados ao vivo contra `dev`) ƒ?" **ainda nÇœo `E2E PROVEN`**: nenhuma purga real de tenant executou de ponta a ponta ainda |
| ReatribuiÇõÇœo de responsabilidade (D-122 design, D-125 implementaÇõÇœo) | `IMPLEMENTED`/`UNIT TESTED` (1249/1249 backend, 156/156 vitest frontend, mutaÇõÇœo verificada e revertida em 3 pontos) ƒ?" precondiÇõÇœo best-effort real em `RemoveMembershipService`/`LeaveOrganizationService`, porta `AssignedActiveItemsLookup` paginada a exaustÇœo via GSI1; sem verificaÇõÇœo E2E real contra `dev` ainda |
| ExportaÇõÇœo de dados CSV (D-123 design, D-126 implementaÇõÇœo) | `IMPLEMENTED`/`UNIT TESTED` (1277/1277 backend testes coletados, 1275 passando ƒ?" 2 falhas prÇ¸-existentes nÇœo relacionadas, ver D-125/D-126; mutaÇõÇœo verificada em 4 pontos) ƒ?" `item:export`/`ADMIN_ROLES`, `ExpirationService.exportItems()` com orÇõamento decrescente de 2.000 itens entre 3 queries GSI1, guard de 4 MB em `buildExportCsv()`, `csv-export-writer.ts` (RFC4180+mitigaÇõÇœo de fÇürmula), Lambda dedicada `export-handler`/rota `GET /items/export` real (`terraform test` limpo); sem verificaÇõÇœo E2E real contra `dev` ainda |

## Material trazido pelo Marcelo, ainda nÇœo avaliado (organizado em 2026-08-31)

- **DomÇðnio Documental** (6 documentos de planejamento funcional/wireframes) ƒ?" movidos para `docs/frontend/`, ver a nova seÇõÇœo "DomÇðnio Documental ƒ?" proposta trazida pelo Marcelo" em `docs/frontend/README.md`. NÇœo avaliado ainda, nÇœo passou pelo protocolo ClaudeƒÅ"Codex.
- **EstratÇ¸gia de aquisiÇõÇœo dos primeiros clientes** ƒ?" `docs/project/first-customers-acquisition-strategy.md` (movido da raiz). Plano comercial, fora do escopo tÇ¸cnico deste arquivo ƒ?" nÇœo avaliado por Claude.
- ~~Roadmap competitivo de funcionalidades (2026-09-01)~~ ƒ?" **RECONCILIADO E ORDEM DEFINIDA, D-161, 2026-09-01** (protocolo ClaudeƒÅ"Codex completo, 3 rodadas, Codex 9,6/10). `docs/project/roadmap-competitivo-2026-09-01.md` continua o documento-fonte (preÇõo/limites das seÇõÇæes 12-13 seguem nÇœo avaliados, decisÇœo comercial pura); a reconciliaÇõÇœo tÇ¸cnica + macro-ordem + escalonamento de 4 decisÇæes fundacionais estÇ­ em `docs/architecture/reviews/competitive-roadmap-reconciliation/estado-final-consolidado.md`. Ver item 1 de "PrÇüxima aÇõÇœo" acima.
- ~~Auditoria de Performance (2026-09-02)~~ ƒ?" **RECONCILIADA, D-170, 2026-09-02** (investigaÇõÇœo factual, sem protocolo ClaudeƒÅ"Codex ƒ?" fact-finding, mesmo tratamento que D-161 teve antes de virar decisÇœo). `docs/project/performance-audit-2026-09-02.md` continua o documento-fonte; a reconciliaÇõÇœo linha-a-linha contra o cÇüdigo real estÇ­ em `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md`. Resumo do confirmado: `RequestContext` caro (confirmado ƒ?" `onboarding.resolve()` hidrata todas as Memberships via GSI4 incondicionalmente, mesmo com `organizationIdHint`, gap jÇ­ conhecido e adiado em D-136/D-C); quota `API_REQUEST` fazendo `Get`+`TransactWriteItems` por request ƒ?" **RESOLVIDO, D-172, 2026-09-02**: lane `EphemeralTelemetryMutation` real implementada (`consumeEphemeralTelemetry()` em `quota.ts`, `UpdateItem` `ADD`-only via `IdentityStore.incrementTelemetryCounter`, sem `Get`/`TransactWriteItems`/`ConditionCheck` de lifecycle repetido), D-154 (`QuotaTelemetryPurgeWorker`) alargado para cobrir o novo `entityType` como garantia de remoÇõÇœo real alÇ¸m do TTL best-effort, teste adversarial obrigatÇürio do DoD confirmado ao vivo (residual pÇüs-DELETING nunca lido como entitlement, cobertura estrutural provada). Deferido, nÇœo fabricado: alarme/mÇ¸trica operacional de residual sobrevivente (infra-level, sessÇœo futura). Reminder Producer/Dispatch com loops sequenciais e N+1 (confirmados, sem design prÇ¸vio); Capacity Model desatualizado (confirmado). **Achado mais grave, alÇ¸m do que o documento externo supÇïs**: o padrÇœo `Scan`+`Limit=100`+`MAX_PAGES=25` sem cursor persistido entre invocaÇõÇæes agendadas aparece em 9 dos 10 workers de manutenÇõÇœo/purga (incluindo `membership-purge` D-169 e `document-file-reconciliation` D-166, implementados NESTA sessÇœo) ƒ?" starvation estrutural real, nÇœo hipotÇ¸tica, e afeta correÇõÇœo de retenÇõÇœo LGPD jÇ­ tratada como fechada. SÇü `document-purge` (D-061, `Query` sobre GSI6 por data) estÇ­ livre do defeito ƒ?" Ç¸ o precedente real para a soluÇõÇœo `MaintenanceDueIndex`. **Reminder Dispatch ƒ?" CONCLUÇ?DO, D-171, 2026-09-02**: os 3 achados mecÇ½nicos (N+1 `queryByItem`+`.find()` trocado por `GetItem` direto; `Get ExpirationItem`/`Get ReminderPolicy` paralelizados via `Promise.all`; loop sequencial do batch SQS trocado por `mapWithConcurrency` ƒ?" helper novo, `src/shared/concurrency/map-with-concurrency.ts`, concorrÇ¦ncia 5 ƒ?" preservando `batchItemFailures` por item) implementados direto, sem protocolo. SuÇðte completa 1726/1726, todos os gates limpos. Detalhe completo em D-171 (`docs/architecture/decisions-log.md`).

**PrÇüxima aÇõÇœo real**: D-D de D-136 **fechado (D-172, 2026-09-02)**. Mapear os consumidores de `onboarding.resolve()` para fechar D-C segue pendente; Reminder Producer (loop triplo sequencial + claim/outbox sequencial por ocorrÇ¦ncia) segue pendente, nÇœo coberto por D-171; depois abrir Rodada 1 ClaudeƒÅ"Codex para o Scan-based starvation primeiro entre os itens nÇðvel 5-6 (mais urgente que RequestContext fast path e Reminder Producer v2).

**Bulk Import ƒ?" Documents + Requirements + mapeamento de coluna (P0.2, D-192)**: narrativa fatia-a-fatia removida daqui (histÇürico completo jÇ­ em `decisions-log.md` D-192 e `docs/architecture/reviews/bulk-import-documents-requirements-scoping/estado-final-consolidado.md`) ƒ?" D-192 estÇ­ FECHADO POR COMPLETO, ver linha do item 2 do roadmap P0 abaixo.

## Links para histÇürico (nÇœo reler por padrÇœo ƒ?" sÇü sob demanda)

- `docs/architecture/session-log.md` ƒ?" linha do tempo compacta, uma entrada por sessÇœo.
- `docs/architecture/decisions-log.md` / `docs/engineering/decisions-log.md` ƒ?" toda decisÇœo com nota Claude/Codex e status (numeraÇõÇœo D-0xx / E-0xx, nÇœo sequencial na ordem das linhas).
- `docs/architecture/reviews/` ƒ?" artefatos de cada rodada ClaudeƒÅ"Codex por tema.
- `docs/project/handoffs/` ƒ?" prompts de handoff de sessÇæes anteriores, superseded por este arquivo, preservados como evidÇ¦ncia.
- `docs/frontend/` ƒ?" os 8 documentos de planejamento de interface + `interface-quality-standard.md` + `prototype/`.

# docs/architecture/ ƒ?" Ç?ndice e Mapa de Autoridade

```text
Design maturity:        APPROVED (arquitetura conceitual + Implementation Blueprint)
Operational architecture: NOT APPROVED
Current phase:           ImplementaÇõÇœo real (cÇüdigo/infra/testes) ƒ?" Implementation Blueprint concluÇðdo
Last verified:           2026-08-29 (reconciliaÇõÇœo de engenharia de contexto ƒ?" este bloco reescrito para corrigir drift real: dizia "auto-CONFIRMED nÇœo propaga dueDate, aguardando decisÇœo do Marcelo" e "8 de 9 classes de retenÇõÇœo sem purga" quando D-058/D-061 jÇ­ tinham decidido e implementado ambos em sessÇæes anteriores). Consolidation + Pilot Readiness Program concluÇðdo, recomendaÇõÇœo **CONDITIONAL GO** (`docs/engineering/pilot-readiness-assessment.md`, backlog item-a-item em `pilot-readiness-program.md`): Wave 3 (tenant isolation/LGPD) `DONE` de ponta a ponta, zero vulnerabilidade cross-tenant real; `USER_DOCUMENT` e `EXTRACTION_TRANSIENT` tÇ¦m purga fÇðsica real (D-061), as outras 7 de 9 classes de retenÇõÇœo LGPD nÇœo tÇ¦m, decisÇœo de escopo pendente do Marcelo; Wave 4 confirmou `tenantId=userId` literal, sem Organization/Membership; Wave 5 fechou GTR-01. M6/M7/M9/M10/M11 deployados em `main`/`dev`, M7 **E2E PROVEN** (verificaÇõÇœo real 2026-08-27, achados registrados em `decisions-log.md` D-057/D-058); M12 bloqueado por decisÇœo de fornecedor (D-052). Full BFF + Frontend Production Foundation (D-053/D-054) **implementado e `APPROVED`**; planejamento de interface 8/9 etapas `APPROVED`, sÇü falta User Validation (em suspenso a pedido do Marcelo); BLOCKER-A/B/C **todos resolvidos**. W3-07 (exclusÇœo fÇðsica de tenant/DSR) tem design **`APPROVED WITH CONDITIONS`** (D-066/D-067, condiÇõÇœo de polÇðtica SES jÇ­ decidida) e implementaÇõÇœo incremental em andamento (fencing de writers D-068 a D-080, purge pipeline D-081 a D-083 com nota Codex 9,1/10, emenda de privacidade B2B-9/D-103/D-104) ƒ?" orquestrador real (Step Functions vs. Lambda+EventBridge) ainda nÇœo decidido, ortogonal ao escopo de retenÇõÇœo jÇ­ fechado. **Multi-User B2B (D-084 a D-108, 2026-08-30)**: design tÇ¸cnico `APPROVED` via protocolo (Claude 9,2/Codex 9,2) e timing decidido diretamente pelo Marcelo (proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05`) ƒ?" maior iniciativa de arquitetura autorizada no momento; **Waves B2B-0 a B2B-11 `DONE`** (inventÇ­rio, physical model D-086, identidade global D-087/D-088, Organization+Membership+CreateOrganizationService D-089/D-090/D-091, `OnboardingStateResolver` puro D-092/D-094, `RequestContext Cutover` D-095/D-096 ƒ?" login nÇœo cria mais tenant sozinho, `POST /bff/organizations` fecha o loop de onboarding, cap transacional; `RBAC` D-097/D-098 ƒ?" `Membership.role=ADMIN` real na matriz de autorizaÇõÇœo, primeira aplicaÇõÇœo real de `docs/engineering/research-protocol.md`/E-014; `Invitations/Team` D-099/D-100 ƒ?" `Invitation`/aceite/revogaÇõÇœo/gerÇ¦ncia de membros com last-owner protection, segunda aplicaÇõÇœo de E-014, primeiro writer real de `Membership` alÇ¸m da criaÇõÇœo; `BFF Organization Context` D-101/D-102 ƒ?" `X-Organization-Id` transportado do BFF ao recurso, terceira aplicaÇõÇœo de E-014, fecha o `InternalError` 500 que B2B-8 tornou explorÇ­vel; `W3-07/Privacy Reconciliation` D-103/D-104 ƒ?" quarta aplicaÇõÇœo de E-014, fecha o `InvitationTokenPointer` ÇürfÇœo pÇüs-exclusÇœo de Organization, formaliza a fronteira User-level vs. Organization-level erasure em `privacy-lgpd.md`; `Tenant-aware Frontend` D-105/D-106 ƒ?" primeira aplicaÇõÇœo do protocolo a uma decisÇœo de frontend, corrige a regressÇœo real do `AuthContext`, isolamento de cache real entre tenants, switcher/members/settings; `Responsibility + Notifications` D-107/D-108 ƒ?" quinta aplicaÇõÇœo de E-014, migra `NotificationRecipientResolver`/`assigneeUserId`/`ItemWatch` de validaÇõÇœo vestigial para `Membership`+`GlobalUser` reais; NÇŸO inclui a supersessÇœo de GTR-01, ver `multi-user-b2b-wave-tracker.md`; `Cutover de dev` D-110/D-111 ƒ?" sexta aplicaÇõÇœo de E-014 (declaraÇõÇœo `NÇŸO`), `scripts/reset-dev-data.ts` (reset/reseed, nÇœo migraÇõÇœo, dado 100% sintÇ¸tico confirmado por inventÇ­rio real via `aws --profile claude-dev`) + remoÇõÇœo de `LEGACY_TENANT_ONLY`, Fase A verificada de fato contra `dev`; execuÇõÇœo real destrutiva (`--confirm`) pendente de confirmaÇõÇœo explÇðcita do Marcelo; `E2E/Adversarial Security` D-112/D-113 ƒ?" sÇ¸tima aplicaÇõÇœo de E-014, auditoria contra as 25 perguntas de `roadmap-evolution/17` ¶õ121 (20/25 jÇ­ cobertas, 2 fecham com fix real de TOCTOU entre roteamento e entrega assÇðncrona de notificaÇõÇœo ƒ?" `notification.ts`+`subject.ts` reaproveitam `DynamoDbNotificationRecipientResolver` jÇ­ testado, 2 ganham teste novo de revogaÇõÇœo/isolamento de role, 1 Ç¸ auditoria de fixture IDs sem achado). **Multi-User B2B ƒ?" todas as 14 waves originais (B2B-0 a B2B-13) `DONE`.** **B2B-14 (Operational Evidence) `EM ANDAMENTO`, 2026-08-30**: 7 achados reais severos, cada um encontrado sÇü por exercitar o fluxo de verdade contra `dev` pela primeira vez ƒ?" `app_origin` placeholder nunca trocado (D-114), `bff-handler` crashava em toda invocaÇõÇœo (D-115), GSI4 nunca autorizado aos 10 Lambdas reais que o consomem (D-116), 2 rotas do BFF nunca wireadas no API Gateway (D-117), nenhuma tela de onboarding existia (D-118), `ExpressionAttributeNames: {}` quebrava `TransactWriteItems` real em 3 arquivos incluindo o guard de Ç§ltima-proteÇõÇœo-de-OWNER (D-119), e o convite/aceite de membro nunca foi alcanÇõÇ­vel de ponta a ponta ƒ?" tela nunca existiu, rota nunca foi wireada, e-mail real nunca foi ativado, SES sandbox sem NENHUMA identity verificada no ambiente inteiro (D-120, 2 endereÇõos reais verificados com o Marcelo). Pendente para fechar: Marcelo testar convite/aceite/troca-de-organizaÇõÇœo com uma segunda conta ƒ?" **explicitamente adiado para uma sessÇœo futura**, nÇœo bloqueia o resto do trabalho. Em paralelo, **W3-07 ƒ?" orquestrador de purga ganhou design `APROVADO` (D-121, protocolo ClaudeƒÅ"Codex 3 rodadas, 9,1/9,2): Step Functions + EventBridge Scheduler** ƒ?" implementaÇõÇœo real ainda nÇœo construÇðda, fica para sessÇœo dedicada futura (mesmo padrÇœo de D-081); e 2 lacunas de produto levantadas pelo Marcelo (exportaÇõÇœo de dados CSV/planilha, reatribuiÇõÇœo de responsabilidade ao remover um Membership) tiveram rodadas de scoping dispatchadas via o mesmo protocolo ƒ?" ver `decisions-log.md` para o D-number final de cada uma antes de assumir status. **`docs/engineering/definition-of-done.md` (E-012, emenda E-013)**: gate de processo permanente ƒ?" nenhum item de todo list que produza cÇüdigo Ç¸ `completed` sem passar pelo gate correspondente ao seu nÇðvel de risco, incluindo aplicaÇõÇœo concreta (nÇœo sÇü referencial) de `test-engineering-standard.md`. Ver `NEXT_SESSION_PROMPT.md` para a prÇüxima aÇõÇœo exata ƒ?" este bloco Ç¸ sÇü o resumo de fase, nÇœo histÇürico item-a-item.
```

Ver `ARCHITECTURE.md` (raiz do repo) para o resumo executivo consolidado e `NEXT_SESSION_PROMPT.md` para a prÇüxima aÇõÇœo concreta.

## PrecedÇ¦ncia de fontes (quando houver divergÇ¦ncia)

1. `AGENTS.md` (raiz) ƒ?" processo de trabalho dos agentes, sempre vence sobre conteÇ§do de arquitetura.
2. ADR aceito em `adr/` ƒ?" decisÇœo arquitetural especÇðfica e formal.
3. Documento temÇ­tico corrente (tabela abaixo, coluna "normativo atual") ƒ?" especificaÇõÇœo detalhada do domÇðnio.
4. `ARCHITECTURE.md` ƒ?" visÇœo consolidada e Çðndice executivo; nÇœo sobrescreve silenciosamente um ADR ou documento temÇ­tico divergente ƒ?" divergÇ¦ncia entre eles Ç¸ defeito a corrigir, nÇœo licenÇõa para escolher.
5. `NEXT_SESSION_PROMPT.md` ƒ?" estado de execuÇõÇœo, nunca fonte normativa de arquitetura.
6. `docs/architecture/history/` ƒ?" evidÇ¦ncia histÇürica de como se chegou a uma decisÇœo, nunca normativo.

## Ç?ndice por documento

| Documento | ClassificaÇõÇœo | Do que trata |
|---|---|---|
| `../ARCHITECTURE.md` | resumo/Çðndice | Documento consolidado final, aponta para todos os outros |
| `../docs/00-prompt-mestre.md` | histÇürico/processo (ciclo concluÇðdo) | Processo ClaudeƒÅ"Codex que produziu o design; nÇœo Ç¸ ponto de entrada de sessÇœo |
| `quality-criteria.md` | normativo atual | 12 critÇ¸rios de qualidade, pesos, gates G1-G6 |
| `fitness-function.md` | normativo atual | Fitness function derivada dos critÇ¸rios |
| `requirements.md` | normativo atual | Requisitos funcionais/nÇœo-funcionais, unknowns |
| `capacity-model.md` | normativo atual | Modelo de capacidade Stage 0-5 |
| `architecture-fase3-consolidada.md` | normativo atual | Arquitetura AWS conceitual, 14 decisÇæes numeradas |
| `data-model.md` | normativo atual | Modelo de domÇðnio/dados, DynamoDB single-table, 6 GSIs |
| `slo.md` | normativo atual | SLOs, incluindo drenagem de pico extremo |
| `disaster-recovery.md` | normativo atual | RPO/RTO, teste de restore, runbook |
| `incident-runbooks.md` | normativo atual (draft operacional) | Runbooks OPS-006 (falha de disparo, DLQ, provedor, IA), matriz de severidade/escalonamento, template de post-mortem, registro de exercÇðcios |
| `privacy-lgpd.md` | normativo atual | Classes de retenÇõÇœo, direitos do titular |
| `cost-model.md` | normativo atual | Modelo de custo por estÇ­gio |
| `mcp-readiness.md` | normativo atual | ProntidÇœo de domÇðnio para MCP futuro |
| `evolution.md` | normativo atual | TransiÇõÇæes de estÇ­gio, gatilhos |
| `aws-well-architected-review.md` | normativo atual | RevisÇœo pelos 6 pilares AWS, riscos conhecidos |
| `threat-model.md` | normativo atual | Threat model STRIDE, seÇõÇœo 33 ƒ?" APPROVED (Claude ~9.05 / Codex 9.002) |
| `implementation-blueprint.md` | normativo atual | Implementation Blueprint, seÇõÇœo 60 ƒ?" componentes, interfaces, eventos/schemas, ordem de deploy, milestones ƒ?" APPROVED (Claude 9.20 / Codex 9.2) |
| `correlationid-xray-trace-join.md` | normativo atual (decisÇœo + implementaÇõÇœo) | JunÇõÇœo `correlationId`ƒÅ"trace ADOT/X-Ray (E-011 pendÇ¦ncia 1) ƒ?" `APPROVED` via protocolo ClaudeƒÅ"Codex (Claude 9,3/Codex 9,4), amplia D-022 sem reabri-la; implementado 2026-08-29 (`xray-trace-header.ts`, correÇõÇœo de precedÇ¦ncia do `SecureLogger`), status `IMPLEMENTED`/`UNIT TESTED`, smoke test real em `dev` ainda pendente |
| `m3.5-runtime-design.md` | normativo atual | Design do milestone M3.5 (runtime real do Reminder Engine, fechamento de G8) ƒ?" adapters DynamoDB, handlers Lambda, outbox+relay SQS/DLQ, EventBridge Scheduler+GSI6, testes em 3 camadas ƒ?" APPROVED (Claude 9.0 / Codex 9.3) |
| `reminder-delivery-pipeline.md` | normativo atual | BLOCKER-B ƒ?" pipeline real de materializaÇõÇœo/entrega de lembretes: event taxonomy (`expiration.item-due-date-changed.v1`/`item-deactivated.v1`/`reminder.policy-changed.v1`), lifecycle do ponteiro `POLICYREF#`, fencing de concorrÇ¦ncia (dispatch + reconciliaÇõÇœo), backfill ƒ?" APPROVED (arquitetura, Codex 9.2/10, rodadas B-H; implementaÇõÇœo, Codex 9.2/10, 2 rodadas) |
| `blocker-b-recon-handoff.md` | histÇürico/evidÇ¦ncia (citado por `reminder-delivery-pipeline.md` ¶õ3/¶õ3.2) | Recon prÇ¸-implementaÇõÇœo de BLOCKER-B (materializer/trigger de materializaÇõÇœo, infra Terraform) confirmado contra o cÇüdigo real antes do design ser escrito; BLOCKER-B estÇ­ implementado e mergeado (PR #50) ƒ?" este documento sÇü registra a evidÇ¦ncia que fundamentou o design |
| `blocker-b-mission-brief.md` | histÇürico/evidÇ¦ncia | Texto verbatim do prompt de missÇœo original (2026-08-24) que abriu o recon de BLOCKER-B acima; persistido porque sÇü existia em histÇürico de conversa de uma sessÇœo especÇðfica |
| `decisions-log.md` | decisÇœo/ADR (log vivo) | D-000 a D-043 (a numeraÇõÇœo nÇœo Ç¸ sequencial na ordem das linhas ƒ?" D-024 a D-028 foram inseridas antes de D-010 a D-023 no arquivo), nota Claude/Codex, status |
| `reviews/m7-extraction-design/` | histÇürico/evidÇ¦ncia de rodada | Artefatos do protocolo ClaudeƒÅ"Codex de M7 (proposta Claude, proposta Codex, crÇðtica, reconciliaÇõÇœo final aprovada ƒ?" D-035) |
| `adr/` | decisÇœo/ADR | 11 ADRs formais para decisÇæes Type 1 |
| `reviews/spa-hosting-cloudfront-bff/` | histÇürico/evidÇ¦ncia de rodada (protocolo `AGENTS.md` ¶õ4) | Debate de 6 rodadas que produziu ADR-0011 (coexistÇ¦ncia CloudFront + Full BFF) ƒ?" nota final 9,2/9,3 |
| `diagrams/diagrams.md` | normativo atual (visual) | 14 diagramas Mermaid |
| `diagrams/project-status.html` | resumo/Çðndice (visual) | **O documento de status do projeto** ƒ?" painel visual (timeline de marcos, achados reais, pendÇ¦ncias); abrir no navegador para uma visÇœo executiva rÇ­pida, mais legÇðvel que `NEXT_SESSION_PROMPT.md` para esse fim (que continua sendo a fonte de estado detalhado por sessÇœo) |
| `session-log.md` | histÇürico | Log cronolÇügico compacto por sessÇœo |
| `roadmap-evolution/00-mission-brief.md` | histÇürico/evidÇ¦ncia | Texto verbatim do prompt original (2026-08-22) que abriu as 3 fases de evoluÇõÇœo estratÇ¸gica do roadmap abaixo; movido da raiz em 2026-08-29, mesmo tratamento de `blocker-b-mission-brief.md` |
| `roadmap-evolution/01-gap-analysis.md` | informativo (rascunho, nÇœo normativo) | Fase 1 da evoluÇõÇœo estratÇ¸gica do roadmap (2026-08-23): estado real dos milestones + classificaÇõÇœo de cada capacidade comercial proposta contra o cÇüdigo real. Insumo para a Fase 2 (pesquisa de mercado + modelagem de domÇðnio + protocolo ClaudeƒÅ"Codex por tema), nunca decisÇœo fechada |
| `roadmap-evolution/02-market-research.md` | informativo (rascunho, nÇœo normativo) | Fase 2a: pesquisa de mercado real sobre concorrentes (TrustLayer, Certificial, SubCompliant, VendorJot, Remindax, categoria ampla) e tentativa de refutar cada capacidade proposta na Fase 1 ƒ?" achado central: billing por sujeito rastreado Ç¸ padrÇœo de mercado dominante |
| `roadmap-evolution/03-domain-model-tracked-subject-requirement.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, ADR formal sÇü na Fase 3) | Fase 2b, primeiro cluster de modelagem de domÇðnio (`TrackedSubject`+`RequirementAssignment`) ƒ?" protocolo ClaudeƒÅ"Codex completo via MCP, nota final 9,1/9,1 |
| `roadmap-evolution/04-domain-model-guest-upload.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, ADR formal sÇü na Fase 3) | Fase 2b, segundo cluster (guest upload/magic link, `DocumentRequest`+`DocumentSubmission`) ƒ?" protocolo ClaudeƒÅ"Codex completo via MCP, nota final 9,2/9,2; achado real: GSI novo evitado reaproveitando padrÇœo de `IdentityMapping` |
| `roadmap-evolution/05-domain-model-organization-billing.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, ADR formal sÇü na Fase 3) | Fase 2b, terceiro cluster (Organization/Membership/RBAC + Billing/Entitlements) ƒ?" protocolo ClaudeƒÅ"Codex completo via MCP, nota final 9,2/9,2; reordena billing por `TrackedSubject` antes de Organization; achado real de correÇõÇœo pendente em `evolution.md:13` |
| `roadmap-evolution/06-domain-model-automated-chasing.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, ADR formal sÇü na Fase 3) | Fase 2b, quarto cluster (automated document chasing via Reminder Engine) ƒ?" protocolo ClaudeƒÅ"Codex completo via MCP, nota final 9,1/9,2; agregados-irmÇœos em vez de generalizar `NotificationIntent`/`ReminderOccurrence` jÇ­ em produÇõÇœo, aplicando o precedente de M7; GSI3 reaproveitado sob condiÇõÇœo de mini-revisÇœo de capacidade |
| `roadmap-evolution/07-domain-model-escalation-watchers-digest.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, ADR formal sÇü na Fase 3) | Fase 2b, quinto cluster (escalation/watchers/digest) ƒ?" protocolo ClaudeƒÅ"Codex completo via MCP, nota final 9,2/9,4; `ItemWatch` como extensÇœo direta de padrÇœo jÇ­ em produÇõÇœo (mesma partiÇõÇœo de `Document`/M6); digest registrado como questÇœo aberta, nÇœo decidida |
| `roadmap-evolution/08-domain-model-custom-fields.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, lista de rejeitados formal sÇü na Fase 3) | Fase 2b, sexto cluster ƒ?" custom fields genÇ¸rico (`FieldDefinition`/`FieldValue`) rejeitado/adiado por padrÇœo (nota 9,1/9,0), valor jÇ­ servido por `tags[]`+`notes?`+`requirementName`; emenda registrada nos clusters 1 e 2 |
| `roadmap-evolution/09-domain-model-csv-import.md` | informativo (decisÇœo reconciliada via protocolo AGENTS.md ¶õ4, ADR formal sÇü na Fase 3) | Fase 2b, sÇ¸timo e Ç§ltimo cluster (CSV import/export) ƒ?" protocolo ClaudeƒÅ"Codex completo via MCP, nota final 9,2/9,4; formula injection mitigada na exportaÇõÇœo (nÇœo na entrada); plano linha-a-linha em S3, nÇœo DynamoDB; **Fase 2b concluÇðda, 7/7 clusters ƒ%¾9,0** |
| `roadmap-evolution/10-phase3-scoring-and-roadmap.md` | informativo (sÇðntese proposta, nÇœo autorizaÇõÇœo de implementaÇõÇœo) | Fase 3: executive summary, feature score ponderado, roadmap revisado M9-M13 (milestone-a-milestone, formato completo), dependency graph ƒ?" consolida os 7 clusters da Fase 2b |
| `roadmap-evolution/11-phase3-impacts-and-closing.md` | informativo (sÇðntese proposta, ADRs formais sÇü com decisÇœo do Marcelo) | Fase 3: domain model antes/depois, impacto de arquitetura/seguranÇõa/persistÇ¦ncia/custo, lista de 10 ADRs candidatos, estratÇ¸gia de teste/migraÇõÇœo, perguntas abertas reais, capacidades rejeitadas ƒ?" inclui revisÇœo adversarial final de coerÇ¦ncia do pacote completo (nota 8,2/10, achados corrigidos) |
| `roadmap-evolution/12-automated-chasing-capacity-review.md` | verificaÇõÇœo de prÇ¸-requisito (D-046) | Mini-revisÇœo de capacidade de GSI3 antes de M10 cluster 4 (automated chasing) ƒ?" pico orgÇ½nico ~220Ç- abaixo do SLO de drenagem de pico extremo, GSI3 reaproveitado sem shard/Çðndice novo |
| `roadmap-evolution/13-guest-link-delivery-design.md` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-047/D-048) | Fecha D-047 ƒ?" entrega/reenvio do link de guest upload: rotaÇõÇœo de token a cada disparo de chasing, sem KMS/secret cifrado persistido; nota final 9,2/9,4 |
| `roadmap-evolution/14-document-request-initial-invite-design.md` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-049) | Fecha "DecisÇœo B" de D-048 ƒ?" automatiza o convite inicial de guest upload (hoje manual) atrÇ­s de preferÇ¦ncia de tenant + kill switch global default `false`; nota final 9,2/9,4 |
| `roadmap-evolution/15-m12-billing-scope-decision.md` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-052) | PÇüs-M11: M12 (Billing) fica bloqueado por decisÇœo de produto (fornecedor de pagamento) ƒ?" zero cÇüdigo novo; achado real de que o projeto nÇœo tem conceito de "platform staff" cross-tenant, descartando atÇ¸ a fatia manual de entitlement; nota final 9,3/9,4 |
| `roadmap-evolution/16-document-lifecycle-strategic-analysis.md` | informativo (anÇ­lise externa, nÇœo passou pelo protocolo AGENTS.md ¶õ4, sem ADR) | EvoluÇõÇœo estratÇ¸gica de "controle de vencimentos" para "gestÇœo enxuta do ciclo de vida documental" ƒ?" avaliaÇõÇœo de coerÇ¦ncia/reaproveitamento/risco de overengineering; recomenda aprovar a direÇõÇœo mas nÇœo antecipar o escopo, mantendo o foco em Consolidation + Pilot Readiness; identifica `Document` deixar de ser filho exclusivo de `ExpirationItem` como a mudanÇõa arquitetural futura central, marcada nela mesma como Type 1 (ADR + protocolo ClaudeƒÅ"Codex antes de qualquer implementaÇõÇœo) |
| `roadmap-evolution/17-multi-user-b2b-revised-strategy.md` | **`APPROVED` ƒ?" design tÇ¸cnico (protocolo AGENTS.md ¶õ4) + timing (decisÇœo direta do Marcelo, D-085)** | EspecificaÇõÇœo vigente de Multi-User B2B ƒ?" `Organization` como tenant boundary permanente, `User` global, `Membership` N:N, RBAC por permissions. Protocolo ClaudeƒÅ"Codex completo (¶õ125): 3 rodadas reais, Claude 9,2/Codex 9,2, ambos ƒ%¾9,0 (mecanismo transacional de last-OWNER, unicidade de Membership/Invitation, tabela mantÇ¸m/emenda/refaz do impacto no W3-07, supersessÇœo explÇðcita de D-060/GTR-01). **D-085 (2026-08-29)**: Marcelo decidiu diretamente proceder agora, supersedendo o gatilho comercial de `roadmap-evolution/05` (marcado `SUPERSEDED (timing)`, modelo de billing/entitlements desse cluster permanece vigente). **Waves B2B-0 a B2B-11 `DONE`** (D-108 conclui B2B-11, Responsibility + Notifications) ƒ?" ver `multi-user-b2b-wave-tracker.md` para o detalhamento por subitem (inclui a nota de escopo sobre GTR-01/`Organization.displayName` ainda pendente). Wave B2B-12 (Cutover de dev) Ç¸ a prÇüxima aÇõÇœo real, escopo ainda nÇœo debatido ƒ?" ver `NEXT_SESSION_PROMPT.md` para o estado de execuÇõÇœo |
| `roadmap-evolution/18-synthetic-persona-research-quality-baseline.md` | informativo (anÇ­lise externa trazida pelo Marcelo em 2026-08-31, nÇœo passou pelo protocolo AGENTS.md ¶õ4, sem ADR, sem decisÇœo de proceder) | Fundamento metodolÇügico proposto para um futuro "Synthetic Persona Evaluation Framework" (agentes condicionados por persona exercitando a aplicaÇõÇœo real, em complemento ƒ?" nunca substituiÇõÇœo ƒ?" a testes humanos): modelo Persona+Contexto+Dataset+CenÇ­rio+Objetivo="Trial", provenance de evidÇ¦ncia (`hypothesisƒÅ'research-groundedƒÅ'human-observedƒÅ'production-observed`), hierarquia de oracle (estado determinÇðstico > banco/API/evento > LLM grader > autoavaliaÇõÇœo do agente, esta Ç§ltima nunca fonte de verdade), scorecard de 100 pontos + 18 Hard Gates (HG-01..18, ex. "agente nunca recebe oracle/caminho de navegaÇõÇœo", "actor nÇœo pode ser o Ç§nico judge"). Puramente conceitual/genÇ¸rico nesta etapa, ainda nÇœo aplicado ao Expiration Tracker |
| `roadmap-evolution/19-synthetic-persona-application-analysis.md` | informativo (anÇ­lise externa trazida pelo Marcelo em 2026-08-31, nÇœo passou pelo protocolo AGENTS.md ¶õ4, sem ADR, sem decisÇœo de proceder) | Aplica o baseline de `18` Çÿ realidade atual do cÇüdigo: conclui que a aplicaÇõÇœo Ç¸ "muito favorÇ­vel" a personas sintÇ¸ticas (domÇðnio modelado, OCC, idempotÇ¦ncia, outbox, relÇügio injetÇ­vel, `dev` resetÇ­vel, Playwright, Test Engineering Standard jÇ­ existentes), mas nomeia 4 restriÇõÇæes reais ƒ?" zero usuÇ­rio/produÇõÇœo real, frontend cobre sÇü parte das superfÇðcies planejadas, modelo B2B ainda em transiÇõÇœo, e a principal: **o Playwright atual roda majoritariamente contra um BFF mockado (`page.route()`), nÇœo contra o sistema real** ƒ?" a mesma lacuna que B2B-14 (Operational Evidence) jÇ­ vinha fechando manualmente. TambÇ¸m documenta drift real entre `NEXT_SESSION_PROMPT.md`/comentÇ­rios antigos e o cÇüdigo atual |
| `roadmap-evolution/20-synthetic-trial-identity-design.md` | informativo (anÇ­lise externa trazida pelo Marcelo em 2026-08-31, nÇœo passou pelo protocolo AGENTS.md ¶õ4, sem ADR, sem decisÇœo de proceder) | Proposta de implementaÇõÇœo: identidade de execuÇõÇœo hierÇ­rquica `evaluationRunIdƒÅ'trialIdƒÅ'correlationIdƒÅ'traceId/spanId`, deliberadamente **execution metadata, nunca atributo funcional do domÇðnio** (nunca `ExpirationItem.trialId`), propagada via header PlaywrightƒÅ'middleware BFFƒÅ'`RequestContext`ƒÅ'outbox `metadata` (nunca `payload`)ƒÅ'SQSƒÅ'workerƒÅ'audit event/log estruturado; gate de ambiente (`EVALUATION_CONTEXT_ENABLED`, `prod=false` por padrÇœo), fail-closed para metadata invÇ­lida, 12 Hard Gates prÇüprios (HG-TID-01..12, ex. "trialId nunca altera autorizaÇõÇœo/validaÇõÇœo funcional"). O prÇüprio documento recomenda revisÇœo via protocolo ClaudeƒÅ"Codex antes de entrar no roadmap ƒ?" ainda nÇœo submetida |
| `multi-user-b2b-wave-b2b4-scope.md` | **`APPROVED` (D-092, protocolo ClaudeƒÅ"Codex 4 rodadas, Claude 9,3/Codex 9,4)** | Escopo final de Wave B2B-4 ƒ?" `OnboardingStateResolver` puro (4 estados, procedimento sequencial estrito), sem wiring de login, sem exposiÇõÇœo HTTP; redesigna "remover auto-provision"/gate real para Wave B2B-5. EvidÇ¦ncia das 4 rodadas em `reviews/multi-user-b2b-wave-b2b4-scoping/` |
| `multi-user-b2b-wave-b2b5-scope.md` | **`APPROVED` (D-095, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,1/Codex 9,1) ƒ?" `IMPLEMENTADO` (D-096)** | Escopo final de Wave B2B-5 (RequestContext Cutover) ƒ?" `bootstrapUser()` de 2 itens, `RequestContextResolver` reescrito com resoluÇõÇœo de Membership Ç§nica e assert explÇðcito para `ADMIN` nÇœo suportado, `POST /bff/organizations` com cap transacional (`GlobalUser.hasCreatedOrganization`), self-heal de sessÇœo BFF; desvio faseado explÇðcito de ¶õ11/¶õ12 do physical model (seleÇõÇœo real de organizaÇõÇœo fica para B2B-6). EvidÇ¦ncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b5-scoping/`; implementaÇõÇœo e 2 achados reais corrigidos (UserProfile mantido, gate de lifecycle da Organization adicionado) em `decisions-log.md` D-096 |
| `multi-user-b2b-wave-b2b6-scope.md` | **`APPROVED` (D-101, protocolo ClaudeƒÅ"Codex 3 rodadas, checklist v2, Claude 9,1/Codex 9,2) ƒ?" `IMPLEMENTADO` (D-102)** | Escopo final de Wave B2B-6 (BFF Organization Context) ƒ?" terceira aplicaÇõÇœo real de E-014 (padrÇœo header+revalidaÇõÇœo server-side, OWASP Multi Tenant Security). `X-Organization-Id` injetado sÇü pelo BFF (nunca do browser), `resolveWorkingOrganization()`/`OrganizationUnavailableError`(403)/`OrganizationSelectionRequiredError`(409) fecham o `InternalError` 500 que B2B-8 tornou explorÇ­vel; `POST /bff/organization/select` (CSRF)/`GET /bff/organizations` (filtrado por lifecycle) novos. EvidÇ¦ncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b6-scoping/` |
| `multi-user-b2b-wave-b2b7-scope.md` | **`APPROVED` (D-097, protocolo ClaudeƒÅ"Codex 3 rodadas, checklist v2, Claude 9,2/Codex 9,3) ƒ?" `IMPLEMENTADO` (D-098)** | Escopo final de Wave B2B-7 (RBAC) ƒ?" primeira aplicaÇõÇœo real de `docs/engineering/research-protocol.md`/E-014 (pesquisa GitHub/Linear/Slack/Notion/NIST-ANSI-INCITS-359, checklist de critÇ¸rios de nota contestado e reconciliado na Rodada 1ƒÅ'2). `Role` ganha `ADMIN`, paritÇ­rio com `OWNER` nas 4 actions de deleÇõÇœo de recurso e no bypass de ownership; `notification:configure` reclassificada para `READ_ONLY_ROLES` (bug fix real, nÇœo escolha ADMIN-vs-OWNER); `tenant:configure-document-request-delivery` isolada em novo tier `OWNER_ROLES`. EvidÇ¦ncia das 3 rodadas (incl. a contestaÇõÇœo do checklist) em `reviews/multi-user-b2b-wave-b2b7-scoping/` |
| `multi-user-b2b-wave-b2b8-scope.md` | **`APPROVED` (D-099, protocolo ClaudeƒÅ"Codex 3 rodadas, checklist v3, Claude 9,1/Codex 9,2) ƒ?" `IMPLEMENTADO` (D-100)** | Escopo final de Wave B2B-8 (Invitations/Team) ƒ?" segunda aplicaÇõÇœo real de E-014, declaraÇõÇœo `SIM PARCIAL` (mecanismo de dados jÇ­ `APPROVED` em D-086; last-owner protection/hierarquia de gerÇ¦ncia de membros pesquisados nesta wave, GitHub/Slack/Linear/Notion). `Invitation`+token pointer+dedup (D-086 ¶õ7), `AcceptInvitationService` (transaÇõÇœo de 6 itens, token consumido estruturalmente), `ChangeMembershipRoleService`/`RemoveMembershipService`/`LeaveOrganizationService` com builder Ç§nico de `ownerCount` (`LastOwnerError`) ƒ?" primeiro writer real de `Membership` alÇ¸m da criaÇõÇœo. EvidÇ¦ncia das 3 rodadas (checklist contestado e reconciliado 2x) em `reviews/multi-user-b2b-wave-b2b8-scoping/` |
| `multi-user-b2b-wave-b2b9-scope.md` | **`APPROVED` (D-103, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,3/Codex 9,3) ƒ?" `IMPLEMENTADO` (D-104)** | Escopo final de Wave B2B-9 (W3-07/Privacy Reconciliation) ƒ?" quarta aplicaÇõÇœo real de E-014 (GitHub/Slack/Atlassian, "delete conta" vs. "delete organizaÇõÇœo"). Fix real: `InvitationTokenPointer` (declara `organizationId`, nÇœo `tenantId`) ganha 3¶¦ clÇ­usula OR no scan/`PURGE_DELETE` de purga, fechando ÇürfÇœo pÇüs-exclusÇœo de Organization. `privacy-lgpd.md` ¶õ4.1/¶õ4.2 novos (fronteira User-level vs. Organization-level erasure, invariante de Ç§ltimo OWNER documentada) + 4 linhas de retenÇõÇœo. EvidÇ¦ncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b9-scoping/` |
| `multi-user-b2b-wave-b2b10-scope.md` | **`APPROVED` (D-105, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,2/Codex 9,2) ƒ?" `IMPLEMENTADO` (D-106)** | Escopo final de Wave B2B-10 (Tenant-aware Frontend) ƒ?" primeira aplicaÇõÇœo do protocolo a uma decisÇœo de frontend nesta sessÇœo. Fix de regressÇœo real (`AuthContext` tratava todo usuÇ­rio autenticado como deslogado desde B2B-5); arquitetura de cache isolation (query keys por `organizationId` + `ActiveOrganizationProvider`/`switching`/`cancelQueries`/`AbortSignal` ponta-a-ponta) fecha uma corrida real de vazamento de dado entre tenants na UI; switcher/members/settings novos. EvidÇ¦ncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b10-scoping/` |
| `multi-user-b2b-wave-b2b11-scope.md` | **`APPROVED` (D-107, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,2/Codex 9,2) ƒ?" `IMPLEMENTADO` (D-108)** | Escopo final de Wave B2B-11 (Responsibility + Notifications) ƒ?" quinta aplicaÇõÇœo de E-014 (GitHub/Linear, "assignee deve ser membro real e ativo"). `NotificationRecipientResolver` migrado de `UserProfile` (vestigial) para `Membership`+`GlobalUser` (2 condiÇõÇæes); `assigneeUserId`/`ItemWatch.userId` agora validados contra `Membership` real via `MemberEligibilityChecker` novo. **Nota**: nÇœo inclui a supersessÇœo de GTR-01 (`UserProfile.requesterDisplayName`ƒÅ'`Organization.displayName`), prevista em B2B-5 mas nunca debatida nas 3 rodadas reais desta wave ƒ?" ver `multi-user-b2b-wave-tracker.md`. EvidÇ¦ncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b11-scoping/` |
| `multi-user-b2b-wave-b2b12-scope.md` | **`APPROVED` (D-110, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,2/Codex 9,1) ƒ?" `IMPLEMENTADO` (D-111)** | Escopo final de Wave B2B-12 (Cutover de dev) ƒ?" sexta aplicaÇõÇœo de E-014 (declaraÇõÇœo `NÇŸO`, polÇðtica reset-vs-migraÇõÇœo jÇ­ aprovada dentro do prÇüprio `roadmap-evolution/17` ¶õ62-63). InventÇ­rio real via `aws --profile claude-dev` confirma `dev` 100% sintÇ¸tico/descartÇ­vel; `scripts/reset-dev-data.ts` (Fase A inventÇ­rio+snapshot sempre, Fase B delete real sÇü `--confirm`, allowlist de tabela/conta, verificaÇõÇœo final fail-loud) + remoÇõÇœo de `LEGACY_TENANT_ONLY` (backend+frontend+teste). Fase A verificada de fato contra `dev` (nÇœo sÇü fakes). **ExecuÇõÇœo real destrutiva (`--confirm`/`--include-cognito`) pendente de confirmaÇõÇœo explÇðcita do Marcelo.** EvidÇ¦ncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b12-scoping/` |
| `multi-user-b2b-wave-b2b13-scope.md` | **`APPROVED` (D-112, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,1/Codex 9,2, rÇ¸gua E-014 9,3) ƒ?" `IMPLEMENTADO` (D-113)** | Escopo final de Wave B2B-13 (E2E/Adversarial Security) ƒ?" sÇ¸tima aplicaÇõÇœo de E-014 (OWASP WSTG/Authorization Testing, SIM PARCIAL). Auditoria completa contra as 25 perguntas de `roadmap-evolution/17` ¶õ121 (matriz QƒÅ'arquivo:linha completa): 20/25 jÇ­ cobertas por testes reais de waves anteriores; 2 fecham com fix real (mesmo TOCTOU em `notification.ts`+`subject.ts` ƒ?" entrega assÇðncrona nunca revalidava `Membership`, sÇü roteamento checava ƒ?" corrigido reaproveitando `DynamoDbNotificationRecipientResolver` jÇ­ testado, `ResolvedRecipient` ganha `email?: string`); 2 ganham teste novo (revogaÇõÇœo de Membership encadeada ponta-a-ponta; roles diferentes por Organization nunca vazam); 1 Ç¸ auditoria de fixture IDs (zero achado). A Rodada 1 do Codex achou um 6¶§ achado real (mesmo TOCTOU em `document-chasing-dispatch`) que a proposta original nÇœo tinha visto. EvidÇ¦ncia das 3 rodadas em `reviews/multi-user-b2b-wave-b2b13-scoping/` |
| `multi-user-b2b-wave-b2b0-inventory.md` | evidÇ¦ncia de rodada (read-only, completo) | InventÇ­rio verificado de Wave B2B-0 (¶õ105 do documento acima): `tenantId=userId` em produÇõÇœo (3 pontos confirmados, 1 gap de fencing prÇ¸-existente achado no login BFF), `IdentityMapping`/BFF session/`RequestContext`, stores tenant-scoped/S3 (3 prefixos reais, diverge de ¶õ68)/eventos/W3-07, frontend caches (zero isolamento tenant hoje) e contagem reproduzÇðvel da suÇðte de testes (122 arquivos/1092 casos, nÇœo ~1104) |
| `multi-user-b2b-physical-model.md` | **`APPROVED` (D-086, protocolo ClaudeƒÅ"Codex 5 rodadas, Claude 9,3/Codex 9,5)** | Physical model final de Wave B2B-1 ƒ?" `User`/`IdentityMapping`/`Organization`/`Membership` (3 estados, GSI4 `MembershipByUser` reaproveitado)/`Invitation`/`ownerCount`/`bootstrapUser()`/`DeviceSession`+`logoutAll`/`RequestContext`/BFF session, respostas finais Çÿs 25 perguntas de `roadmap-evolution/17` ¶õ121. EvidÇ¦ncia das 5 rodadas em `reviews/multi-user-b2b-physical-model/` |
| `multi-user-b2b-wave-tracker.md` | registro vivo de backlog (nÇœo normativo sobre arquitetura/design) | Status DONE/IN PROGRESS/BLOCKED/NOT STARTED por wave (B2B-0 a B2B-15), mesmo papel de `docs/engineering/pilot-readiness-program.md` ƒ?" ponto de entrada para saber o que jÇ­ foi feito antes de continuar a iniciativa |
| `reviews/context-engineering-reconciliation/` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 | ReconciliaÇõÇœo da arquitetura de contexto do repo (2026-08-29) ƒ?" root cleanup (21ƒÅ'6 `.md`), `AGENTS.md`/`NEXT_SESSION_PROMPT.md` trimados (narrativa de milestone jÇ­ duplicada em `decisions-log.md` removida), 2 guardrails novos em `check-doc-drift.ts` (root allowlist, size), READMEs stale corrigidos. 4 rodadas Codex (8,1ƒÅ'8,8ƒÅ'8,9ƒÅ'9,3/10, `APPROVED`), 3 achados reais de drift semÇ½ntico prÇ¸-existente corrigidos no processo |
| `reviews/bff-full-vs-session-design/` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-053/D-054) | Full BFF como fronteira de sessÇœo do browser ƒ?" decide o mecanismo de autenticaÇõÇœo das chamadas de recurso que o "BFF de sessÇœo" original (D-034) nunca cobriu. D-053 (Claude 9,2/Codex 9,3): browser nunca recebe token OAuth, cookie de sessÇœo opaco, PKCE+`state`. D-054, amendment de auditoria adversarial de 16 pontos (Claude 9,2/Codex 9,4): rotaÇõÇœo nativa do Cognito no refresh (nÇœo contador local ƒ?" evita falso-positivo de invalidaÇõÇœo), tabela de sessÇœo dedicada IAM-isolada, cookies login/sessÇœo com `SameSite` diferenciado. **Implementado e `APPROVED AS FRONTEND PRODUCTION FOUNDATION`** (`src/modules/bff/`, `infra/modules/bff-*`) ƒ?" ver `docs/frontend/frontend-production-foundation.md` para o registro completo da implementaÇõÇœo e do protocolo ClaudeƒÅ"Codex (Rodada D: 6 passagens, 5 achados bloqueantes reais de seguranÇõa de sessÇœo corrigidos) |
| `reviews/w3-07-purge-orchestrator-scoping/` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-121) | Orquestrador real do purge pipeline W3-07 (Step Functions + EventBridge Scheduler) ƒ?" decisÇœo pendente desde D-083, fechada 3 rodadas (6,7ƒÅ'8,2ƒÅ'9,2/9,1 `APPROVED`). Design apenas ƒ?" implementaÇõÇœo (novo mÇüdulo Terraform, 2 Lambdas, `CloseOrganizationService`) fica para sessÇœo dedicada futura |
| `reviews/responsibility-reassignment-scoping/` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-122) | ReatribuiÇõÇœo de responsabilidade quando um Membership Ç¸ removido/sai ƒ?" precondiÇõÇœo bloqueante best-effort via GSI1 jÇ­ existente (zero GSI novo), 3 rodadas (7,0ƒÅ'8,1ƒÅ'9,1/9,1 `APPROVED`). Design apenas ƒ?" implementaÇõÇœo fica para sessÇœo dedicada futura |
| `reviews/data-export-scoping/` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-123) | ExportaÇõÇœo de dados (CSV) de `ExpirationItem` ƒ?" `item:export`/`ADMIN_ROLES`, handler sÇðncrono via `queryGsi1` com orÇõamento decrescente + guard de 4 MB, `csv-export-writer.ts` (RFC4180+mitigaÇõÇœo de fÇürmula), 3 rodadas (6,2ƒÅ'8,1ƒÅ'9,1/9,1 `APPROVED`). Design apenas ƒ?" implementaÇõÇœo fica para sessÇœo dedicada futura |
| `reviews/search-and-filters-scoping/` | decisÇœo reconciliada via protocolo AGENTS.md ¶õ4 (D-194) | Busca e filtros documentais (roadmap P0.5) ƒ?" uniÇœo discriminada Subject/Requirement/ExpirationItem via GSI7/GSI1 jÇ­ tenant-facing (zero GSI novo), `Requirement.assigneeUserId` (mecanismo completo transportado de D-122/D-125), `UnifiedValidityState` derivado read-time. RÇ¸gua reconciliada na Rodada 2 (9,6/10 estÇ­vel), 5 rodadas (design 4,8ƒÅ'6,2ƒÅ'8,1ƒÅ'8,8ƒÅ'9,2/9,3 `APPROVED`). Design apenas ƒ?" implementaÇõÇœo fica para sessÇœo dedicada futura |
| `reviews/` | histÇürico/evidÇ¦ncia de rodada (protocolo `AGENTS.md` ¶õ4, pÇüs-M0) | Artefatos de revisÇœo ClaudeƒÅ"Codex de implementaÇõÇœo real, por milestone (ex. `reviews/m3.5-runtime-design/`) ƒ?" mesmo papel de `history/` (evidÇ¦ncia, nunca normativo), mas para rodadas ocorridas depois que cÇüdigo passou a existir, em vez das rodadas de design conceitual prÇ¸-implementaÇõÇœo |
| `history/` | histÇürico/supersedido | Artefatos de rodada (propostas, crÇðticas, red team) que produziram os documentos normativos acima ƒ?" ver subseÇõÇœo |

## `history/` ƒ?" evidÇ¦ncia de rodada, por tema

Cada subpasta contÇ¸m os artefatos de proposta/crÇðtica/trÇ¸plica que antecederam o documento normativo correspondente. Nunca tratar como fonte de decisÇœo vigente ƒ?" sÇü como prova de como o consenso foi alcanÇõado.

| Subpasta | Documento normativo correspondente |
|---|---|
| `history/quality-criteria/` | `quality-criteria.md` |
| `history/architecture-fase3/` | `architecture-fase3-consolidada.md` |
| `history/data-model/` | `data-model.md` |
| `history/slo/` | `slo.md` |
| `history/disaster-recovery/` | `disaster-recovery.md` |
| `history/privacy-lgpd/` | `privacy-lgpd.md` |
| `history/cost-model/` | `cost-model.md` |
| `history/threat-model/` | `threat-model.md` |
| `history/implementation-blueprint/` | `implementation-blueprint.md` |

# docs/engineering/ ƒ?" Ç?ndice e Mapa de Autoridade

```text
Rubrica de qualidade:     CONGELADA (01-engineering-quality-criteria.md)
Fitness functions:        ativas (02-engineering-fitness-functions.md), enforcement real em CI
Full-audit 9 eixos:       CONCLUÇ?DO em 2026-08-20 (9/9 avaliados, sÇü Contexto bateu o gate ƒ%¾9.0 originalmente) ƒ?" achados remediados/reavaliados em rodadas posteriores por eixo, ver decisions-log.md/pilot-readiness-program.md, nÇœo re-enumerado aqui
Programas posteriores:    test-engineering-standard.md (E-010, APPROVED), logging-observability-standard.md (E-011, APPROVED), definition-of-done.md (E-012, APPROVED ƒ?" gate por item de todo list; emenda E-013 tornando a aplicaÇõÇœo de test-engineering-standard.md operacional, nÇœo sÇü referencial), research-protocol.md (E-014, APPROVED ƒ?" pesquisa externa exigida/checklist de nota antes de decisÇæes Type 1 que dependem de padrÇœo de mercado), Consolidation + Pilot Readiness Program (pilot-readiness-program.md/pilot-readiness-assessment.md, CONDITIONAL GO)
Last verified:            2026-08-30 (E-014 aprovado ƒ?" ver docs/architecture/README.md para o estado vigente completo, nÇœo duplicado aqui)
```

Este diretÇürio trata de **como o trabalho de engenharia Ç¸ medido e revisado** (rubrica, critÇ¸rios por eixo, protocolo de debate, achados de auditoria) ƒ?" nÇœo confundir com `docs/architecture/`, que trata do que o sistema Ç% (design, modelo de dados, decisÇæes de arquitetura). Se a dÇ§vida for "o GSI3 Ç¸ consultÇ­vel por quem", vÇ­ para `docs/architecture/`; se for "que nota isso tira / que processo formal se aplica aqui", este diretÇürio Ç¸ o certo.

## PrecedÇ¦ncia de fontes (quando houver divergÇ¦ncia)

Mesma regra de `docs/architecture/README.md` ƒ?" `AGENTS.md` (raiz) sempre vence sobre conteÇ§do deste diretÇürio; um achado de auditoria (`reviews/full-audit-*-summary.md`) Ç¸ evidÇ¦ncia de uma rodada especÇðfica, nunca redefine os pesos/critÇ¸rios de `joint-review-criteria.md` (ele mesmo linka para lÇ­ em vez de duplicar, ver seu prÇüprio rodapÇ¸ "Como adicionar um novo eixo").

## Quando carregar o quÇ¦ (roteamento por tipo de tarefa)

| Sua tarefa Ç¸... | Carregue |
|---|---|
| Rodar o protocolo ClaudeƒÅ"Codex num eixo especÇðfico (`AGENTS.md` ¶õ4) | `joint-review-criteria.md` (seÇõÇœo do eixo) + o `full-audit-round1-<eixo>-summary.md` mais recente em `reviews/`, se existir (retomar em vez de reabrir do zero) |
| Entender que nota um achado especÇðfico do cÇüdigo tira / que critÇ¸rio ele afeta | `joint-review-criteria.md` (nÇœo precisa da rubrica congelada nem do bibliography) |
| Julgar se um teste automatizado ou drill operacional Ç¸ vÇ­lido/de qualidade suficiente (gate binÇ­rio, critÇ¸rio ponderado, nota mÇðnima) | `test-engineering-standard.md` ƒ?" rÇ¸gua concreta que `joint-review-criteria.md`'s critÇ¸rio "Test Effectiveness & Coverage Discipline" passa a referenciar |
| Decidir se uma mudanÇõa precisa de ADR/protocolo formal ou Ç¸ correÇõÇœo mecÇ½nica | `change-risk-scale.md` (a rÇ¸gua concreta) ƒ?" `AGENTS.md` ¶õ4 sÇü distingue os dois extremos de forma binÇ­ria |
| Marcar um item de todo list de cÇüdigo como `completed` | `definition-of-done.md` ƒ?" gate por item (nÇœo por PR/wave inteira): unidade de conclusÇœo, gate por nÇðvel de risco, registro mÇðnimo de evidÇ¦ncia |
| Decidir se uma proposta Type 1 precisa de pesquisa externa antes da Rodada 1, e como isso vira critÇ¸rio de nota | `research-protocol.md` ƒ?" critÇ¸rio decidÇðvel (nÇðvel 5-6 + padrÇœo externo estabelecido), declaraÇõÇœo `SIM`/`SIM PARCIAL`/`NÇŸO`, checklist de critÇ¸rios derivado da pesquisa, fluxo de reconciliaÇõÇœo se o Codex contestar o checklist |
| Saber que comando roda em qual gate (PR vs. deploy) | `quality-gate-tiers.md` ƒ?" mapeia Tier A/B/C (CI `guardrails`, `dynamodb-integration`, Camada 3 ainda pendente) aos comandos reais de `package.json` |
| Entender uma exceÇõÇœo/vulnerabilidade aceita (ex. EX-001) e seu prazo de revisÇœo | `exceptions.md` |
| Ver o histÇürico completo de decisÇæes de engenharia (nÇœo de arquitetura) com motivo | `decisions-log.md` ƒ?" E-000 em diante, mesmo padrÇœo de `docs/architecture/decisions-log.md` mas para decisÇæes de processo/qualidade, nÇœo de sistema |
| Ver onde Claude e Codex genuinamente discordaram e como foi resolvido | `disagreement-log.md` |
| Entender a origem da rubrica de 12 critÇ¸rios/gates G1-G6 (histÇürico, prÇ¸ full-audit) | `01-engineering-quality-criteria.md` (CONGELADA ƒ?" mudanÇõa exige nova rodada formal, nÇœo ediÇõÇœo direta) + `00-research-bibliography.md` para as fontes que a fundamentam |
| Ver o estado exato do repositÇürio no inÇðcio da Engineering Maturity Review (checkpoint 0) | `03-repository-baseline.md` ƒ?" histÇürico, nÇœo estado atual (ver aviso no prÇüprio arquivo) |
| Consultar o resultado de uma auditoria especÇðfica dos 9 eixos formalizados | `reviews/full-audit-round1-<eixo>-summary.md` (ver convenÇõÇœo de nomenclatura abaixo) |
| Consultar evidÇ¦ncia bruta de uma rodada especÇðfica (nota cega, prompt do Codex, saÇðda bruta) | `reviews/full-audit-round1-<eixo>-{claude,codex-prompt,codex-output-roundN}.{md,txt}` ƒ?" evidÇ¦ncia, nunca ponto de entrada; leia o `-summary.md` primeiro |
| Rodar o checker de drift determinÇðstico entre docs | `npm run check-docs` (`scripts/check-doc-drift.ts`) ƒ?" link relativo quebrado + referÇ¦ncia `AGENTS.md ¶õN` desatualizada, bloqueante no CI (`guardrails`) |

## Ç?ndice por documento (nÇðvel raiz de `docs/engineering/`)

| Documento | ClassificaÇõÇœo | Do que trata |
|---|---|---|
| `principles.md` | normativo atual | PrincÇðpios de engenharia adotados (proporcionalidade, evidÇ¦ncia antes de mecanismo) |
| `change-risk-scale.md` | normativo atual | Escala de risco de mudanÇõa NÇðvel 1-6, rÇ¸gua concreta para "isso precisa de protocolo formal?" |
| `quality-gate-tiers.md` | normativo atual | Tiers de gate (PR vs. deploy), mapeados aos comandos reais |
| `definition-of-done.md` | normativo atual (APPROVED, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,1/Codex 9,2, E-012; emenda E-013) | Definition of Done por item de todo list ƒ?" granularidade ("unidade de conclusÇœo"), gate por nÇðvel de risco, classificaÇõÇœo de risco na prÇ­tica, registro mÇðnimo de evidÇ¦ncia; complementa `quality-gate-tiers.md`/`change-risk-scale.md` sem substituÇð-los. E-013 tornou a aplicaÇõÇœo de `test-engineering-standard.md` operacional (G-V3 mutaÇõÇœo nomeada por escrito), nÇœo sÇü referencial |
| `research-protocol.md` | normativo atual (APPROVED, protocolo ClaudeƒÅ"Codex 3 rodadas, Claude 9,2/Codex 9,2, E-014) | Pesquisa externa antes de decisÇæes Type 1 ƒ?" gatilho acoplado a `change-risk-scale.md` nÇðvel 5-6 + Rodada 1 do protocolo (nunca uma disciplina paralela para lembrar); declaraÇõÇœo `SIM`/`SIM PARCIAL`/`NÇŸO` com fonte+data+representatividade; checklist de critÇ¸rios de nota derivado da pesquisa, sub-rubrica subordinada a `joint-review-criteria.md`; fluxo de reconciliaÇõÇœo quando o Codex contesta o checklist em si |
| `joint-review-criteria.md` | normativo atual | CritÇ¸rios/pesos por eixo das revisÇæes conjuntas ClaudeƒÅ"Codex (9 eixos formalizados + FinOps pendente) |
| `test-engineering-standard.md` | normativo atual (APPROVED, protocolo ClaudeƒÅ"Codex 8 rodadas, gate elevado 9,5/10) | PadrÇœo de validade/qualidade para teste automatizado e drill operacional (chaos/DiRT) ƒ?" gates binÇ­rios (G-V1..G-V6, G-C1), critÇ¸rios ponderados, fÇürmula de agregaÇõÇœo, auditoria retroativa da Wave 2 (2026-08-28) |
| `logging-observability-standard.md` | normativo atual (APPROVED, protocolo ClaudeƒÅ"Codex 3 rodadas, gate elevado 9,5/10) | RÇ¸gua concreta de qualidade para logging/tracing/taxonomia de erro (`src/shared/observability/**`, `app-error.ts`, wiring Terraform de detecÇõÇœo) ƒ?" 8 critÇ¸rios ponderados, Ç½ncoras de pontuaÇõÇœo, gate de auditoria 9,0/10, escrita em resposta a achados reais de uma rodada Codex de 2026-08-29 |
| `exceptions.md` | normativo atual (registro vivo) | ExceÇõÇæes/vulnerabilidades aceitas com owner e prazo |
| `decisions-log.md` | decisÇœo (log vivo) | DecisÇæes de engenharia/processo, E-000 em diante |
| `disagreement-log.md` | histÇürico/registro vivo | DivergÇ¦ncias materiais ClaudeƒÅ"Codex e como foram resolvidas |
| `ai-governance.md` | normativo atual (registro vivo) | Matriz de autoridade por agente, regra de quando `AGENTS.md` ¶õ4 pode ser dispensado, inventÇ­rio de casos de uso de IA, registro de modelo/fornecedor, incidentes causados pelo prÇüprio agente (distinto de `exceptions.md`) |
| `01-engineering-quality-criteria.md` | histÇürico (CONGELADA) | Rubrica original de 12 critÇ¸rios/gates G1-G6 da Engineering Maturity Review ƒ?" antecessora conceitual de `joint-review-criteria.md`, nÇœo superseded formalmente mas escopo majoritariamente absorvido pelos 9 eixos |
| `02-engineering-fitness-functions.md` | normativo atual | VerificaÇõÇæes executÇ­veis derivadas da rubrica CONGELADA |
| `00-research-bibliography.md` | histÇürico/fundamentaÇõÇœo | Fontes de pesquisa que embasam a rubrica original |
| `03-repository-baseline.md` | histÇürico | Estado do repositÇürio no inÇðcio da Engineering Maturity Review ƒ?" nunca estado atual |
| `pilot-readiness-program.md` | registro vivo de backlog (nÇœo normativo sobre arquitetura/design) | Backlog item-a-item do "Consolidation + Pilot Readiness Program" (`docs/project/handoffs/expiration-tracker-next-days-master-plan-and-ai-prompt.md`, movido da raiz em 2026-08-29) ƒ?" DONE/PARTIAL/BLOCKED/DEFERRED/NOT STARTED por Wave 0-6, atualizado a cada milestone |
| `pilot-readiness-assessment.md` | sÇðntese/recomendaÇõÇœo (entregÇ­vel final do programa, prompt mestre ¶õ42) | GO/CONDITIONAL GO/NO-GO por escopo de piloto, consolidando a evidÇ¦ncia de `pilot-readiness-program.md` ƒ?" nÇœo repete achado, sÇü aponta; nÇœo Ç¸ aprovaÇõÇœo final, Ç¸ insumo para a decisÇœo do Marcelo |
| `reviews/` | histÇürico/evidÇ¦ncia de rodada | Artefatos de toda rodada ClaudeƒÅ"Codex de processo/qualidade ƒ?" ver seÇõÇœo prÇüpria abaixo |

## `reviews/` ƒ?" convenÇõÇœo de nomenclatura e classificaÇõÇœo

EvidÇ¦ncia de rodada, mesmo papel que `docs/architecture/history/` tem para decisÇæes de arquitetura: nunca normativo, sÇü prova de como uma nota/decisÇœo foi alcanÇõada. TrÇ¦s geraÇõÇæes de conteÇ§do coexistem aqui, nesta ordem cronolÇügica:

1. **Checkpoints da Engineering Maturity Review** (`checkpoint-01-rubric/`, `checkpoint-02-09-consolidated/`, `checkpoint-12-redteam/`, e os arquivos soltos `_codex-*-checkpoint1-*.txt` no nÇðvel raiz de `reviews/`) ƒ?" produziram a rubrica CONGELADA (`01-engineering-quality-criteria.md`) e o red team que motivou G8. Prefixo `_` nos arquivos brutos Ç¸ intencional (agrupa antes de outros nomes em ordem alfabÇ¸tica, sinaliza "transcript bruto, nÇœo leia direto").
2. **ConvergÇ¦ncia dos critÇ¸rios por eixo** (`joint-review-criteria-round1-*`, `security-axis-criteria-round1-*`, `remaining-axes-round1-*`, `audit-areas-*`) ƒ?" produziram `joint-review-criteria.md`.
3. **Full-audit dos 9 eixos formais** (`full-audit-round1-<eixo>-*`) ƒ?" a rodada em andamento nesta sessÇœo. PadrÇœo de nomenclatura por eixo (`<eixo>` ƒ^^ `arquitetura`, `qualidade`, `contexto`, `seguranca`, ...):
   - `full-audit-round1-<eixo>-claude.md`, `-claude-round2.md`, `-claude-round3.md`, ... ƒ?" nota cega do Claude, uma por rodada.
   - `full-audit-round1-<eixo>-codex-prompt.txt` / `-codex-prompt-round2.txt`, ... ƒ?" prompt enviado ao Codex CLI, um por rodada.
   - `full-audit-round1-<eixo>-codex-output-round1.txt`, `-round2.txt`, ... ƒ?" saÇðda bruta do Codex CLI. Alguns trechos que ecoam saÇðda de outros comandos de shell continham mojibake (encoding double/triple-decoded em algumas invocaÇõÇæes via PowerShell) ƒ?" revertido deterministicamente para os arquivos anteriores a cada rodada (achado real de higiene, corrigido, nÇœo sÇü mitigado); a saÇðda de uma rodada em andamento pode ainda conter mojibake atÇ¸ a rodada seguinte revisÇ­-la, por ser evidÇ¦ncia datada do que o Codex realmente viu naquele momento ƒ?" nunca editada retroativamente. Se precisar do conteÇ§do, prefira o `-summary.md` correspondente, que jÇ­ extrai a informaÇõÇœo sem o ruÇðdo de encoding.
   - `full-audit-round1-<eixo>-summary.md` ƒ?" **o Ç§nico arquivo desta trinca que deveria ser lido diretamente.** Nota final por critÇ¸rio (ambos os lados), achados corrigidos com commit real, achados restantes classificados em "impedimento externo real" vs. "escopo maior que correÇõÇœo pontual".

Arquivos de transcript bruto (`.txt`, prefixo `_`, ou os `full-audit-*-codex-output-*.txt`) nunca sÇœo fonte de verdade por si sÇü ƒ?" sempre prefira o `.md` de resumo/nota que os acompanha. **PolÇðtica de proveniÇ¦ncia/metadata**: os `.md` de nota/resumo carregam frontmatter (`status`/`owner`/`authority`, mais `Last verified` nos routers); os `.txt` brutos nÇœo ƒ?" exigir frontmatter em cada transcript violaria proporcionalidade (`principles.md` #1) sem ganho real, jÇ­ que sua proveniÇ¦ncia Ç¸ herdada do `.md` que os acompanha (mesmo prefixo de nome) e da posiÇõÇœo cronolÇügica descrita nesta seÇõÇœo. Isso Ç¸ uma decisÇœo de design, nÇœo uma lacuna: um transcript nunca Ç¸ lido isoladamente por convenÇõÇœo (ver acima), entÇœo nÇœo precisa carregar sua prÇüpria metadata.

---
status: active
owner: engineering
authority: normative
---

# CritÇ¸rios de revisÇœo conjunta ClaudeƒÅ"Codex, por eixo

Fonte Ç§nica dos critÇ¸rios de avaliaÇõÇœo (nome, peso, definiÇõÇœo) usados nas revisÇæes conjuntas ClaudeƒÅ"Codex (`AGENTS.md` ¶õ4). Achados/rodadas de uma execuÇõÇœo especÇðfica do protocolo (ex. `docs/architecture/reviews/m3.5-runtime-design/`) registram evidÇ¦ncia de uma revisÇœo pontual ƒ?" nunca redefinem ou duplicam a tabela de pesos; apenas linkam para a seÇõÇœo correspondente aqui.

Adaptado (nÇœo copiado) do padrÇœo equivalente do projeto irmÇœo `event-discovery-platform` (mesmo usuÇ­rio) ƒ?" mesmas fontes normativas e mesmos nomes/definiÇõÇæes de critÇ¸rio (sÇœo padrÇæes de indÇ§stria: ISO/IEC 25010, AWS Well-Architected, ATAM, DORA/Core-4, mais a literatura de context engineering para dev assistido por IA), mas com **pesos recalibrados por convergÇ¦ncia independente ClaudeƒÅ"Codex especificamente para este repositÇürio** ƒ?" nunca herdados sem revisÇœo. Rodada de convergÇ¦ncia completa (2 propostas independentes + reconciliaÇõÇœo) em `docs/engineering/reviews/joint-review-criteria-round1-{claude,codex-prompt,codex-output}.*`; ambas as partes ƒ%¾9.0 de confianÇõa na proposta prÇüpria, sem necessidade de rodada formal de desacordo (mesma condiÇõÇœo de convergÇ¦ncia rÇ­pida que o projeto irmÇœo documenta para o eixo de contexto).

Cada eixo evolui apenas se o prÇüprio critÇ¸rio se mostrar mal calibrado em uso real ƒ?" registrar por que, junto com a mudanÇõa, e nÇœo reabrir a cada nova revisÇœo sobre o mesmo eixo.

**Eixos formalizados (9)**: Arquitetura, Qualidade de Engenharia, Engenharia de Contexto, SeguranÇõa da InformaÇõÇœo e AppSec, Privacidade e GovernanÇõa de Dados, OperaÇõÇæes/SRE e Continuidade de NegÇücio, GovernanÇõa de IA e Controles Internos, GovernanÇõa JurÇðdica/Contratual/Terceiros, GovernanÇõa de Produto e ServiÇõo Multi-tenant. **FinOps fica como 10¶§ eixo, aprovado por decisÇœo do usuÇ­rio mas ainda sem critÇ¸rios definidos** ƒ?" nÇœo usar atÇ¸ ganhar seÇõÇœo prÇüpria aqui; economia unitÇ­ria hoje Ç¸ sÇü um critÇ¸rio dentro de Produto/ServiÇõo Multi-tenant, nÇœo um substituto do eixo.

---

## Eixo: Arquitetura

Convergido em 2026-08-19. Pesos puxados para cima em Reliability & Fault Recovery e Data Model & Consistency ƒ?" refletem que G8 (recuperaÇõÇœo real de falha assÇðncrona) Ç¸ o gate de engenharia mais caro e mais recentemente testado deste projeto, e que o single-table DynamoDB com 6 GSIs e duas exceÇõÇæes de particionamento (GSI3/GSI6, chaves globais) jÇ­ produziu bugs reais (inconsultabilidade de PK, ponteiro removido incorretamente) ƒ?" risco concreto, nÇœo hipotÇ¸tico.

| # | CritÇ¸rio | Peso |
|---:|---|---:|
| 1 | Domain Fit & Simplicity | 8% |
| 2 | Reliability & Fault Recovery | 16% |
| 3 | Event & Integration Correctness | 11% |
| 4 | Data Model & Consistency | 13% |
| 5 | Security & Privacy | 13% |
| 6 | Modifiability & Evolvability | 7% |
| 7 | Observability & Operability | 8% |
| 8 | Testability & Delivery Safety | 8% |
| 9 | Cost & Resource Governance | 5% |
| 10 | Performance & Scalability Fitness | 4% |
| 11 | Architecture Governance & Traceability | 7% |

## Eixo: Qualidade de engenharia

Convergido em 2026-08-19. Eixo distinto do de Arquitetura ƒ?" craft de cÇüdigo, disciplina de testes, rigor de CI, tooling, disciplina de documentaÇõÇœo/processo, gestÇœo de dÇðvida tÇ¸cnica; nÇœo redecide design de sistema. Ajuste principal: **Delivery, Release & Recovery Discipline** subiu de forma expressiva (padrÇœo de origem tinha 8%) ƒ?" nÇœo porque a entrega jÇ­ seja madura, mas justamente o oposto: nenhum deploy real foi validado ainda neste projeto, e o Ç§nico bloqueador restante para fechar G8 Ç¸ exatamente executar deploy real + IAM negativo + DLQ/redrive + EventBridge Scheduler em sandbox AWS. Uma lacuna real de evidÇ¦ncia pesa mais, nÇœo menos ƒ?" mesmo raciocÇðnio aplicado ao eixo de contexto abaixo.

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | Code Correctness & Defensive Design | 11% | PreservaÇõÇœo de invariantes via validaÇõÇœo explÇðcita, tratamento de falha, transiÇõÇæes de estado seguras, ausÇ¦ncia de corrupÇõÇœo silenciosa. |
| 2 | Test Effectiveness & Coverage Discipline | 15% | EvidÇ¦ncia crÇðvel de unit/integration/contract/negative/failure/regression focada em risco real, nÇœo apenas contagem/cobertura de linha. SuÇðte extensa baseada sÇü em fakes nÇœo pontua alto ƒ?" precisa provar propriedades reais (DynamoDB/IAM/SQS/EventBridge) quando o risco Ç¸ real. |
| 3 | CI Quality Gates & Merge Safety | 11% | Enforcement determinÇðstico de checks obrigatÇürios em toda mudanÇõa, sem bypass informal, exceÇõÇæes formalmente governadas. |
| 4 | Type Safety, Static Analysis & Automated Enforcement | 9% | Uso efetivo de tipagem estrita, lint, dependency-cruiser (grafo real de imports) e fitness functions customizadas para prevenir classes de defeito automaticamente. |
| 5 | Readability, Consistency & Implementation Maintainability | 8% | CÇüdigo coeso, nomeado claramente, estilo consistente, DRY/KISS/YAGNI aplicado com julgamento, barato de modificar. |
| 6 | Delivery, Release & Recovery Discipline | 11% | Artefatos/ambientes reproduzÇðveis, promoÇõÇœo segura, evidÇ¦ncia de deploy, rollback, validaÇõÇœo de recuperaÇõÇœo ƒ?" critÇ¸rio de saÇðda real de G8, nÇœo secundÇ­rio neste estÇ­gio. |
| 7 | Dependency & Supply-Chain Hygiene | 7% | DependÇ¦ncias controladas, lockfiles, actions/tools pinados, triagem de vulnerabilidade com `expiresAt` formal (`docs/engineering/exceptions.md`), SBOM. |
| 8 | Debuggability & Operational Feedback | 7% | DiagnÇüsticos estruturados e privacy-safe (`SecureLogger`/`Redactor`), correlation context, erros acionÇ­veis, suficientes para investigar falhas assÇðncronas. |
| 9 | Developer Experience & Reproducibility | 5% | Instalar/validar/testar/rodar o repositÇürio a partir de checkout limpo, com comandos pinados e documentados, de baixo atrito. |
| 10 | Documentation Quality & Process Discipline | 5% | Specs, standards, decisÇæes e registros de mudanÇõa claros e autoritativos, com rigor de processo proporcional a projeto solo. |
| 11 | Documentationƒ?"Implementation Drift Control | 6% | Checks determinÇðsticos ou executados regularmente que detectam divergÇ¦ncia entre documentaÇõÇœo, cÇüdigo, infra, config de CI e realidade implantada. |
| 12 | Technical-Debt & Continuous-Improvement Practice | 5% | Registro honesto de atalhos e controles falhos, com dono, trigger, expiry quando aplicÇ­vel, e follow-through baseado em evidÇ¦ncia. |

## Eixo: Engenharia de contexto

Convergido em 2026-08-19. Avalia a qualidade do prÇüprio sistema de documentaÇõÇœo/contexto do projeto ƒ?" nÇœo cÇüdigo, nÇœo arquitetura de sistema, nÇœo craft de engenharia (eixos distintos, jÇ­ cobertos acima). Ajuste principal: **Context Routing & Progressive Disclosure** subiu de forma expressiva (padrÇœo de origem tinha 13%) pelo mesmo raciocÇðnio do eixo anterior ƒ?" este projeto nÇœo tem um "context router" dedicado equivalente ao `system-overview.md` do projeto irmÇœo (`AGENTS.md` ¶õ2 cumpre parcialmente esse papel, mas Ç¸ mais fraco), e o volume de documentos normativos/histÇüricos/handoffs jÇ­ torna caro escolher o read-set mÇðnimo correto. Uma lacuna estrutural real pesa mais no critÇ¸rio que ela mede, nunca menos.

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | Canonicalidade, Autoridade & NÇœo-DuplicaÇõÇœo | 15% | Cada fato normativo tem exatamente uma fonte de verdade; derivados (Çðndices, mapas, resumos) sÇœo explicitamente nÇœo autoritativos e referenciam a fonte em vez de copiÇ­-la. Ver `docs/architecture/README.md` ¶õ"PrecedÇ¦ncia de fontes". |
| 2 | Clareza de PapÇ¸is & Proporcionalidade | 9% | Cada documento tem propÇüsito, escopo e autoridade inequÇðvocos, sem sobreposiÇõÇœo semÇ½ntica relevante. Estrutura proporcional ao tamanho real do projeto ƒ?" sem sofisticaÇõÇœo antecipada (`docs/engineering/principles.md` #1). |
| 3 | Context Routing & Progressive Disclosure | 15% | Humanos e agentes conseguem identificar e carregar o menor conjunto suficiente de contexto para cada tipo de tarefa. Lacuna reconhecida: sem context router dedicado hoje ƒ?" `AGENTS.md` ¶õ2 Ç¸ o candidato mais prÇüximo, mais fraco que o padrÇœo do projeto irmÇœo. |
| 4 | CorrespondÇ¦ncia com a Realidade & Controle de Drift | 16% | O sistema distingue intenÇõÇœo documentada, implementaÇõÇœo declarada e estado efetivamente observado, reconciliando divergÇ¦ncias explicitamente. `ARCHITECTURE STATUS`/gates G1-G8 sÇü mudam com evidÇ¦ncia operacional real (`docs/engineering/principles.md` #6). |
| 5 | Lifecycle, ProveniÇ¦ncia & EvoluÇõÇœo do Conhecimento | 12% | Arquitetura vigente vs. histÇürica, ADR aceito imutÇ­vel e supersedido por ADR novo, metadata de status/autoridade, lifecycle de evidÇ¦ncias (`docs/architecture/history/`). |
| 6 | Rastreabilidade de DecisÇæes, Trabalho & Triggers | 10% | DecisÇæes caras, trabalho adiado, dÇðvida tÇ¸cnica e evidÇ¦ncias permanecem conectados; gatilhos de reavaliaÇõÇœo concretos e verificÇ­veis (`docs/engineering/exceptions.md`, `decisions-log.md`). |
| 7 | Higiene de Contexto & Sinal-RuÇðdo | 8% | SÇü conhecimento durÇ­vel Ç¸ promovido a destinos canÇïnicos; ausÇ¦ncia de versÇæes concorrentes ou documentos ÇürfÇœos. |
| 8 | Portabilidade AgnÇüstica entre Agentes de IA | 6% | Regras essenciais utilizÇ­veis por diferentes agentes/ferramentas ƒ?" `AGENTS.md`/`CLAUDE.md` jÇ­ seguem essa separaÇõÇœo. |
| 9 | Auditabilidade & Enforcement do Sistema de Contexto | 9% | AfirmaÇõÇæes sobre a saÇ§de do contexto verificÇ­veis por evidÇ¦ncia datada e revisÇæes reproduzÇðveis; achados com lifecycle claro (aberto/corrigido/adiado/arquivado). |

## Eixo: SeguranÇõa da InformaÇõÇœo e AppSec

Convergido em 2026-08-19 (fontes: OWASP ASVS 5.0, OWASP Top 10:2025, OWASP SAMM, prÇ­ticas de least-privilege IAM AWS Lambda/DynamoDB). Rodada de convergÇ¦ncia completa em `docs/engineering/reviews/security-axis-criteria-round1-{claude,codex-prompt,codex-output}.*` ƒ?" convergÇ¦ncia forte: os dois critÇ¸rios de maior peso (isolamento multi-tenant, least-privilege IAM) saÇðram com pesos **idÇ¦nticos** nas duas propostas independentes, sem precisar de reconciliaÇõÇœo. Eixo avalia confidencialidade/integridade/disponibilidade/isolamento diante de abuso intencional e falhas de fronteira de confianÇõa ƒ?" nÇœo reavalia craft de cÇüdigo, arquitetura funcional ou disciplina de teste geral (eixos distintos); sÇü quando esses elementos sÇœo controle ou evidÇ¦ncia especificamente de seguranÇõa.

Pesos calibrados pelo risco real jÇ­ observado neste projeto: GSI3/GSI6 sÇœo exceÇõÇæes de particionamento com blast radius cross-tenant deliberado que jÇ­ produziram bug real nesta sessÇœo (nÇœo risco hipotÇ¸tico); o pipeline assÇðncrono (M3.5/G8) passou por 3 rodadas reais de revisÇœo que encontraram bugs de contrato, race condition e limpeza de Çðndice.

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | Isolamento Multi-Tenant & AutorizaÇõÇœo por Objeto | 18% | `tenantId` derivado exclusivamente da identidade validada e propagado por chaves, queries, eventos, idempotÇ¦ncia e auditoria; proteÇõÇœo contra IDOR e acesso cross-tenant em toda rota/worker. Maior peso do eixo ƒ?" risco mais concreto e jÇ­ testado deste projeto (GSI3/GSI6). |
| 2 | Least-Privilege IAM & ContenÇõÇœo de Blast Radius | 14% | Capability/role especÇðfica por Lambda (`ScopedLambdaFunction`), sem grants implÇðcitos/curinga. `gsi3Read()`/`gsi6Read()` como capabilities exclusivas de lista fechada de workers ƒ?" permissÇæes tenant-facing devem produzir `AccessDenied` real (Camada 3, ainda pendente). |
| 3 | AutenticaÇõÇœo, SessÇœo & GestÇœo de Identidade | 11% | ValidaÇõÇœo completa de JWT Cognito (assinatura/issuer/audience/expiraÇõÇœo/claims), revogaÇõÇœo global/por dispositivo, TTL/rotaÇõÇœo, MFA proporcional. Nenhuma autorizaÇõÇœo confia em identidade/tenant fornecido pelo cliente. |
| 4 | Integridade do Pipeline AssÇðncrono & Fronteiras de Mensagem | 14% | Eventos EventBridge/SQS/Streams/outbox autenticados pela infra, validados por schema, vinculados ao tenant, protegidos contra replay/duplicaÇõÇœo/roteamento cruzado. Claim/estado/outbox preservam atomicidade; retry/DLQ/redrive/sweeper nÇœo contornam autorizaÇõÇœo. Peso alto: Ç¸ onde G8 jÇ­ encontrou bugs reais. |
| 5 | ValidaÇõÇœo de Entrada, Injection & Fail-Closed | 9% | ValidaÇõÇœo server-side por contrato (Ajv) em toda borda ƒ?" HTTP, filas, dados persistidos. Entrada invÇ­lida/estado impossÇðvel/erro de autorizaÇõÇœo falha de modo fechado, sem mutaÇõÇœo parcial nem detalhe interno vazado. |
| 6 | ProteÇõÇœo de Dados SensÇðveis, Segredos & Criptografia | 9% | MinimizaÇõÇœo/classificaÇõÇœo de PII e tokens, criptografia em trÇ½nsito/repouso, segredos fora de cÇüdigo/log/artefato. `SecureLogger`/`Redactor` deve valer tambÇ¸m em exceÇõÇæes, traces, eventos e DLQ ƒ?" nÇœo sÇü no caminho feliz. |
| 7 | Logging Seguro, DetecÇõÇœo & Resposta a Incidentes | 8% | Eventos de autenticaÇõÇœo/autorizaÇõÇœo negada/alteraÇõÇœo privilegiada/acesso a GSI global produzem trilha Çðntegra, correlacionÇ­vel, privacy-safe (OWASP A09:2025). Alarmes com threshold acionÇ­vel e runbook de contenÇõÇœo/revogaÇõÇœo/investigaÇõÇœo. |
| 8 | ConfiguraÇõÇœo Segura da Plataforma & SuperfÇðcie Exposta | 6% | API Gateway/Cognito/Lambda/DynamoDB/SQS/EventBridge/Streams com defaults seguros, exposiÇõÇœo mÇðnima, retenÇõÇœo adequada. MudanÇõas de CDK devem ser capazes de detectar regressÇœo (wildcard de Çðndice, grant excessivo, handler placeholder ƒ?" jÇ­ aconteceu nesta sessÇœo). |
| 9 | ResistÇ¦ncia a Abuso, DoS & ExaustÇœo de Custo | 5% | Rate limits, quotas por tenant, concorrÇ¦ncia reservada, limites de batch/paginaÇõÇœo impedem que um tenant, poison message ou evento amplificado degrade outros tenants ou gere custo descontrolado; retries/reconciliaÇõÇœo nÇœo podem virar multiplicador de ataque. |
| 10 | VerificaÇõÇœo Adversarial & GestÇœo ContÇðnua de Risco | 6% | EvidÇ¦ncia especÇðfica de seguranÇõa (threat model vivo, testes de abuso/cross-tenant/replay, assertions de IAM/IaC) ƒ?" inclusive teste em AWS real quando emulaÇõÇœo nÇœo prova o controle (Camada 3). Achados com severidade/dono/prazo; nova superfÇðcie (upload, provedor externo, frontend, webhook) reabre a anÇ­lise antes da implementaÇõÇœo. |

## Eixo: Privacidade e GovernanÇõa de Dados

Convergido em 2026-08-19 (fontes: LGPD/orientaÇõÇæes ANPD sobre RIPD/direitos/incidentes/transferÇ¦ncias, DAMA-DMBOK). Rodada completa em `docs/engineering/reviews/remaining-axes-round1-{claude,codex-prompt,codex-output}.*`. Avalia licitude, finalidade, governanÇõa e ciclo de vida dos dados ƒ?" nÇœo controles tÇ¸cnicos de acesso (isolamento multi-tenant/IAM jÇ­ pertencem ao eixo de SeguranÇõa, nÇœo duplicado aqui). Maior peso do eixo em RetenÇõÇœo/ExclusÇœo porque Ç¸ o workflow mais greenfield e envolve mÇ§ltiplos stores (DynamoDB, S3, filas, backups, providers).

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | InventÇ­rio, ClassificaÇõÇœo, Ownership & Linhagem de Dados | 15% | InventÇ­rio versionado de dados pessoais, sensÇðveis, documentos, metadados, logs, eventos e backups; finalidade, origem, destino, responsÇ­vel e `retentionClass` rastreÇ­veis. Nova entidade/campo/integraÇõÇœo nÇœo entra sem classificaÇõÇœo. |
| 2 | Base Legal, Finalidade & MinimizaÇõÇœo | 16% | Base legal e finalidade definidas por tratamento, com teste de necessidade/proporcionalidade. Uso secundÇ­rio exige reavaliaÇõÇœo, nÇœo reutilizaÇõÇœo implÇðcita. |
| 3 | Direitos do Titular & Portabilidade Efetiva | 16% | ConfirmaÇõÇœo/acesso/correÇõÇœo/exportaÇõÇœo/oposiÇõÇœo/exclusÇœo com fluxo autenticado, prazos, estados e evidÇ¦ncia de conclusÇœo. |
| 4 | RetenÇõÇœo, Legal Hold, ExclusÇœo VerificÇ­vel & Backups | 17% | Prazo por classe com evento inicial, prazo final e mecanismo executÇ­vel. ExclusÇœo alcanÇõa DynamoDB/S3/Çðndices/filas/providers/restores; testes provam que dado excluÇðdo nÇœo ressurge apÇüs restore. |
| 5 | LocalizaÇõÇœo, TransferÇ¦ncia Internacional & Subprocessamento | 14% | RegiÇœo de produÇõÇœo, paÇðses de tratamento, subprocessadores e mecanismos jurÇðdicos de transferÇ¦ncia decididos antes de produÇõÇœo ƒ?" decisÇœo hoje pendente por LGPD, nÇœo drift silencioso. |
| 6 | RIPD, Risco aos Titulares & Privacy by Design | 10% | CritÇ¸rios objetivos de quando elaborar/atualizar RIPD; tratamento de alto risco exige decisÇœo humana registrada. |
| 7 | Qualidade, CorreÇõÇœo & ProveniÇ¦ncia dos Dados | 7% | Dados exatos/completos/atuais vinculados Çÿ origem/versÇœo; inferÇ¦ncia de IA distinguÇðvel de dado confirmado pelo usuÇ­rio. |
| 8 | Accountability, EvidÇ¦ncia & Monitoramento de Privacidade | 5% | ResponsÇ­veis, aprovaÇõÇæes, DSRs, holds e exceÇõÇæes produzem evidÇ¦ncia auditÇ­vel, sem virar telemetria excessiva de dado pessoal. |

## Eixo: OperaÇõÇæes, SRE e Continuidade de NegÇücio

Convergido em 2026-08-19 (fontes: Google SRE Book ƒ?" SLO/error budget dirige decisÇœo operacional, nÇœo sÇü relatÇürio ƒ?", AWS Well-Architected SaaS Lens ¶õOperate). Avalia capacidade de operar/detectar/responder/recuperar o serviÇõo real ƒ?" o desenho de SLO/DR jÇ­ existe (`slo.md`/`disaster-recovery.md`) mas nunca foi operacionalizado com dado de produÇõÇœo real. Maior peso em Backup/Restore porque "documentado mas nunca testado" Ç¸ o padrÇœo de risco mais repetido observado neste projeto (G8 sÇü fechou depois de testar de verdade).

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | SLIs, SLOs & Error Budgets AcionÇ­veis | 16% | SLIs representam resultado percebido (API, reminder freshness, backlog, reconciliaÇõÇœo); consumo de error budget condiciona release/priorizaÇõÇœo, nÇœo sÇü relatÇürio. |
| 2 | Observabilidade Operacional & VisÇœo por Tenant | 11% | Detecta impacto agregado e noisy neighbor entre tenants ƒ?" `SecureLogger` maduro nÇœo basta sem dashboard/sinal real. |
| 3 | DetecÇõÇœo, Resposta & ComunicaÇõÇœo de Incidentes | 15% | Alertas com threshold acionÇ­vel/dono/escalonamento/runbook; comunicaÇõÇœo a tenants/titulares/ANPD quando aplicÇ­vel; exercÇðcios comprovam funcionamento sob pressÇœo. |
| 4 | SaÇ§de do Pipeline AssÇðncrono & RecuperaÇõÇœo de Backlog | 14% | Idade/profundidade de fila, DLQ, outbox, claims, sweeper monitorados; redrive/replay idempotentes e limitados. Peso alto pela criticidade do produto + Camada 3 ainda pendente. |
| 5 | Backup, Restore, RTO/RPO & Continuidade | 18% | PITR/backup configurado nÇœo basta ƒ?" nota depende de restore real periÇüdico com RTO/RPO observado. Falha regional Ç¸ risco aceito explicitamente (regiÇœo Ç§nica, Stage 0-2), nÇœo ignorado. |
| 6 | ProntidÇœo de Deploy, Rollback & MudanÇõa Operacional | 10% | Deploy com artefato identificÇ­vel, checks prÇ¸/pÇüs, rollback/roll-forward; mudanÇõa de schema/GSI/KMS/provider aciona validaÇõÇœo proporcional. |
| 7 | Capacidade, DependÇ¦ncias & DegradaÇõÇœo Controlada | 9% | Limites AWS/providers/concorrÇ¦ncia/custo conhecidos e testados; kill switches preservam trabalho recuperÇ­vel. |
| 8 | Post-mortem, ExercÇðcios & Melhoria ContÇðnua | 7% | Post-mortem sem culpa com aÇõÇœo/dono/prazo; falha recorrente ou aÇõÇœo vencida reduz nota. |

## Eixo: GovernanÇõa de IA e Controles Internos

Convergido em 2026-08-19 (fontes: NIST AI RMF ƒ?" ciclo Govern/Map/Measure/Manage ƒ?", ISO/IEC 42001). Abrange Claude/Codex construindo e operando o projeto, e futuros componentes de IA/OCR do produto. Avalia autoridade, supervisÇœo, independÇ¦ncia, proveniÇ¦ncia e gestÇœo de risco ƒ?" nÇœo a qualidade genÇ¸rica do cÇüdigo gerado (eixo de Engenharia). Maior peso em IndependÇ¦ncia da RevisÇœo porque Ç¸ o controle interno central deste projeto (protocolo `AGENTS.md` ¶õ4).

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | Limites de Autoridade, PermissÇæes & SupervisÇœo Humana | 18% | AÇõÇæes permitidas/proibidas/sujeitas a aprovaÇõÇœo humana por agente; deploy/merge/exclusÇœo/produÇõÇœo/Type-1/comunicaÇõÇœo externa seguem least authority e fail-closed. |
| 2 | AtribuiÇõÇœo, ProveniÇ¦ncia & Reprodutibilidade das AÇõÇæes | 15% | Toda mudanÇõa relevante atribuÇðvel a agente/modelo/sessÇœo/diff/teste/aprovador ƒ?" reconstruÇðvel sem exigir armazenamento indiscriminado de prompt sensÇðvel. |
| 3 | IndependÇ¦ncia da RevisÇœo & SegregaÇõÇœo de FunÇõÇæes | 15% | Nota cega, mÇðnimo de rodadas, mesmo agente nunca simula aprovaÇõÇœo independente de si mesmo. Controle interno central do projeto ƒ?" sem ele, "revisÇœo ClaudeƒÅ"Codex" viraria teatro. |
| 4 | InventÇ­rio de Casos de Uso & GestÇœo do Risco de IA | 13% | Usos de IA inventariados por finalidade/impacto/dado/autonomia/reversibilidade; mudanÇõa relevante reabre avaliaÇõÇœo (Govern-Map-Measure-Manage). |
| 5 | AvaliaÇõÇœo de CorreÇõÇœo, LimitaÇõÇæes & Impacto | 12% | Nota alta sem evidÇ¦ncia de arquivo:linha concreta nÇœo fecha revisÇœo (jÇ­ Ç¸ a prÇ­tica deste projeto ƒ?" formalizado aqui como critÇ¸rio). |
| 6 | ProteÇõÇœo de Contexto, Dados & Segredos no Uso de IA | 10% | Apenas contexto necessÇ­rio enviado a modelo/ferramenta; segredos/dado pessoal/documento de tenant obedecem classificaÇõÇœo e retenÇõÇœo. |
| 7 | GestÇœo de Modelos, Ferramentas, Fornecedores & MudanÇõas | 9% | VersÇœo/capacidade de modelo/CLI conhecida; upgrade tem avaliaÇõÇœo de regressÇœo e impacto ƒ?" comportamento de fornecedor nÇœo Ç¸ controle interno garantido. |
| 8 | Incidentes de IA, ExceÇõÇæes & Melhoria ContÇðnua | 8% | Mecanismo de suspender uso/reverter/registrar falha; bypass/exceÇõÇœo com fundamento, prazo e aprovaÇõÇœo. |

## Eixo: GovernanÇõa JurÇðdica, Contratual e de Terceiros

Convergido em 2026-08-19 (fontes: SOC 2 Trust Services Criteria ƒ?" gestÇœo de fornecedor, nÇœo certificaÇõÇœo prematura ƒ?", licenciamento OSS). Avalia obrigaÇõÇœo/licenÇõa/contrato/responsabilidade/risco de dependÇ¦ncia externa ƒ?" SBOM, pinning e integridade tÇ¸cnica de build continuam nos eixos de Engenharia/SeguranÇõa, nÇœo duplicados aqui.

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | PapÇ¸is JurÇðdicos, Responsabilidades & Modelo Contratual | 16% | Define quando o serviÇõo Ç¸ controlador/operador/ambos; decisÇœo pendente recebe dono e gate antes do lanÇõamento. |
| 2 | InventÇ­rio, Due Diligence & Monitoramento de Terceiros | 16% | Registro de AWS/modelos de IA/OCR/mensageria/CI com serviÇõo, dados, criticidade, regiÇœo, certificaÇõÇœo, lock-in e responsÇ­vel. |
| 3 | Licenciamento OSS, Propriedade Intelectual & Uso de ConteÇ§do | 14% | DependÇ¦ncias/cÇüdigo gerado com licenÇõa/proveniÇ¦ncia compatÇðvel com operaÇõÇœo comercial; autoria por IA nÇœo presume ausÇ¦ncia de risco de licenÇõa. |
| 4 | Termos de Uso, Aviso de Privacidade & Consentimentos | 14% | Termos/avisos compatÇðveis com comportamento real do produto ƒ?" nÇœo existem ainda, prÇ¸-requisito antes do primeiro usuÇ­rio real. |
| 5 | DPAs, TransferÇ¦ncias, Incidentes & ObrigaÇõÇæes de Fornecedor | 13% | Contrato com operador/suboperador cobre instruÇõÇæes, seguranÇõa, localizaÇõÇœo, exclusÇœo e comunicaÇõÇœo tempestiva de incidente (LGPD art. 39). |
| 6 | Compromissos Comerciais, SLA & ProteÇõÇœo do Consumidor | 13% | NÇœo prometer entrega de mensagem quando sÇü hÇ­ tentativa do provider, nem disponibilidade sem arquitetura correspondente. |
| 7 | AprovaÇõÇœo, EvidÇ¦ncia, ExceÇõÇæes & MudanÇõa RegulatÇüria | 7% | Gatilho de reavaliaÇõÇœo em mudanÇõa regulatÇüria/produto/regiÇœo/provider; aprovaÇõÇœo sem dono nÇœo pontua como controle. |
| 8 | Continuidade, Portabilidade & SaÇðda de Fornecedor | 7% | DependÇ¦ncia crÇðtica tem estratÇ¸gia proporcional de substituiÇõÇœo/exportaÇõÇœo ƒ?" dado nÇœo fica irrecuperavelmente preso sem risco aceito e documentado. |

## Eixo: GovernanÇõa de Produto e ServiÇõo Multi-tenant

Convergido em 2026-08-19 (fontes: AWS Well-Architected SaaS Lens ƒ?" onboarding automatizado no control plane, crypto-shredding no offboarding, tenant como conceito de primeira classe). Avalia o serviÇõo percebido/administrado pelo tenant ao longo do ciclo de vida ƒ?" nÇœo reavalia isolamento/IAM tÇ¸cnico (eixo de SeguranÇõa). **FinOps fica como eixo prÇüprio por decisÇœo do usuÇ­rio, nÇœo formalizado ainda** ƒ?" economia unitÇ­ria aqui Ç¸ sÇü um critÇ¸rio de produto, nÇœo compete em peso com esse eixo futuro.

| # | CritÇ¸rio | Peso | DefiniÇõÇœo |
|---:|---|---:|---|
| 1 | Lifecycle Automatizado de Tenant | 18% | Onboarding/ativaÇõÇœo/convite/mudanÇõa de plano/suspensÇœo/offboarding como estados explÇðcitos, idempotentes, auditÇ­veis ƒ?" hoje o tenant nasce implicitamente no primeiro login (MVP `tenantId=userId`), sem control plane dedicado. |
| 2 | Offboarding, ExportaÇõÇœo & DestruiÇõÇœo CriptogrÇ­fica | 16% | "NÇœo conseguimos deletar um tenant" Ç¸ falha de compliance, nÇœo sÇü tÇ¸cnica; crypto-shredding coordenado com retenÇõÇœo/backup/legal hold. |
| 3 | Planos, Entitlements, Quotas & Fairness | 13% | Capacidades/limites como polÇðtica centralizada e versionada; `TenantQuota` jÇ­ existe (M1), falta plano de billing real. NÇœo duplica rate-limiting de seguranÇõa ƒ?" mede correÇõÇœo da oferta do produto. |
| 4 | CorreÇõÇœo do ServiÇõo de Lembretes & ProteÇõÇœo do UsuÇ­rio | 15% | Timezone/DST/opt-out/quiet hours/renovaÇõÇœo corrigÇðveis; lembrete perdido/duplicado/obsoleto tem detecÇõÇœo e reparo ƒ?" Ç¸ a falha mais grave possÇðvel do produto. |
| 5 | TransparÇ¦ncia, Usabilidade & Acessibilidade | 10% | Estado/erro/atraso comunicado em linguagem clara; ainda nÇœo se aplica plenamente (sem frontend), mas entra no radar em M4+. |
| 6 | AdministraÇõÇœo, Suporte & OperaÇõÇœo sob a Ç"tica do Tenant | 10% | Suporte autorizado diagnostica saÇ§de/uso/config por tenant com trilha de auditoria, sem acesso informal a conteÇ§do. |
| 7 | MÇ¸tricas de Valor, Consumo & Economia UnitÇ­ria | 10% | AdoÇõÇœo/sucesso de lembrete/consumo atribuÇðvel por tenant de forma privacy-safe ƒ?" germe do futuro eixo FinOps, nÇœo o eixo em si. |
| 8 | EvoluÇõÇœo Unificada & Controle de CustomizaÇõÇœo | 8% | Nenhuma feature cria "fork" por tenant; variaÇõÇœo por plano usa mecanismo governado (mesmo princÇðpio jÇ­ aplicado ao reshard versionado do GSI3). |

## Como adicionar um novo eixo

NÇœo criar a tabela de critÇ¸rios dentro do primeiro doc de auditoria do eixo novo. Seguir o mesmo procedimento de convergÇ¦ncia independente (`AGENTS.md` ¶õ4) e, ao final, adicionar aqui uma seÇõÇœo nova nesse mesmo formato ƒ?" o registro de auditoria referencia a seÇõÇœo, nÇœo a duplica.

/**
 * Authorization matrix ƒ?" implementation-blueprint.md ¶õ4.3. "A matriz Ç¸ cÇüdigo
 * versionado e testado, nÇœo documentaÇõÇœo informal."
 */
import type { RequestContext } from "./request-context.js";

export type Action =
  | "item:create"
  | "item:read"
  | "item:update"
  | "item:delete"
  | "item:watch"
  | "reminder:manage"
  | "document:reserve-upload"
  | "document:read"
  | "document:delete"
  | "extraction:confirm"
  | "notification:configure"
  | "audit:read"
  | "system:ping" // M1 test route action, not in the blueprint's business list but
  // declared explicitly here rather than silently bypassing the matrix (see report).
  // M9 (evoluÇõÇœo estratÇ¸gica do roadmap, D-036/D-039, 03-domain-model-tracked-subject-
  // requirement.md e 07-domain-model-escalation-watchers-digest.md): TrackedSubject +
  // RequirementAssignment + ItemWatch. Mesmo padrÇœo resolver-deriva-tenantId de todo mÇüdulo
  // existente ƒ?" nenhuma dessas actions confia em tenantId fornecido pelo cliente.
  | "subject:create"
  | "subject:read"
  | "subject:update"
  | "subject:delete"
  | "requirement:assign"
  | "requirement:read"
  | "requirement:update"
  | "requirement:delete"
  | "requirement:review"
  // M10 (guest upload/magic link, D-037, 04-domain-model-guest-upload.md): apenas o lado
  // autenticado do tenant tem action prÇüpria ƒ?" o convidado nunca passa por authorize()/
  // RequestContext, Ç¸ validado por GuestTokenService (fora da matriz de roles por design).
  | "requirement:request-document"
  // M10 cluster 4 (D-049): polÇðtica de tenant para automatizar o convite inicial de guest
  // upload ƒ?" decisÇœo de comunicaÇõÇœo externa/reputaÇõÇœo de todo o tenant, nÇœo uma aÇõÇœo por
  // request individual (essa Ç¸ `requirement:request-document` acima). B2B-7 (D-097/D-098):
  // OWNER_ROLES, nÇœo ADMIN_ROLES ƒ?" a classe de "workspace settings"/comunicaÇõÇœo externa que a
  // pesquisa de RBAC (research-protocol.md) mostrou ficar separada mesmo em produtos que dÇœo
  // a ADMIN paridade de conteÇ§do com OWNER.
  | "tenant:configure-document-request-delivery"
  // M11 (CSV import/export, D-042, 09-domain-model-csv-import.md, cluster 7): superfÇðcie de
  // processamento em massa - mesma granularidade de document:reserve-upload/document:read.
  | "import:create"
  | "import:read"
  | "import:commit"
  // D-192 slice 9: `POST /import-jobs/{jobId}/mapping` ƒ?" same WRITE_ROLES tier as
  // `import:commit` (both mutate the job's lifecycle/state machine, never a passive read).
  | "import:map"
  // B2B-8 (D-099, docs/architecture/multi-user-b2b-wave-b2b8-scope.md): Invitations/Team.
  // Pesquisa (GitHub/Slack/Linear/Notion, 2026-08-30) convergiu em ADMIN-tier-e-acima gerencia
  // membros (nunca MEMBER/VIEWER), e sÇü OWNER promove/demove o prÇüprio tier OWNER (Slack:
  // "Owners can assign Owners... [and] assign Admins") ƒ?" a segunda parte nÇœo Ç¸ expressÇ­vel na
  // matriz genÇ¸rica (nÇœo hÇ­ um "OWNER, exceto quando o alvo/novo role Ç¸ OWNER"), fica como
  // checagem de serviÇõo nomeada (OwnerTierChangeRequiresOwnerError) em cima do ADMIN_ROLES
  // baseline abaixo. `membership:leave` nunca aceita um alvo externo (LeaveOrganizationService
  // opera sÇü sobre ctx.principal.userId por assinatura) - a proteÇõÇœo real Ç¸ o LastOwnerError
  // transacional, nÇœo a matriz.
  | "membership:invite"
  | "membership:revoke-invitation"
  // Convites pendentes carregam e-mail + intenÇõÇœo de adicionar pessoa - superfÇðcie
  // administrativa (Linear "Settings > Administration > Members" para pending invites), nunca
  // a mesma tier de "listar membros ativos" (achado real da Rodada 1 do Codex).
  | "membership:list-invitations"
  | "membership:list-members"
  | "membership:role-change"
  | "membership:remove"
  | "membership:leave"
  // Wave B2B-10 (Tenant-aware Frontend): Organization.displayName/timezone. OWNER_ROLES, same
  // tier as "tenant:configure-document-request-delivery" above ƒ?" workspace identity/settings
  // that reads externally (invitation emails, guest-facing name) is consistently kept OWNER-only
  // in this codebase, not paritary with ADMIN like most other membership-management actions.
  | "organization:update-settings"
  // D-123/D-126 (CSV data export): bulk export of ExpirationItem rows across the whole tenant.
  // ADMIN_ROLES, NOT a bulk-action precedent ƒ?" the justification is disclosure asymmetry:
  // export READS every member's work (including items the caller never touched), while
  // import only WRITES what the actor could already create individually. See
  // docs/architecture/reviews/data-export-scoping/round-3-claude-proposal.md.
  | "item:export"
  // W3-07 purge orchestrator (D-124, implementing D-121): closing the organization starts the
  // physical, irreversible tenant purge (`ACTIVE -> DELETING -> ... -> DELETED`). OWNER_ROLES,
  // the same tier as `tenant:configure-document-request-delivery`/`organization:update-settings`
  // above ƒ?" this is the single most destructive tenant-wide action in the system, so it can never
  // be paritary with ADMIN the way ordinary content administration is.
  | "organization:close"
  // D-127 (quarantine/recovery window): cancels an in-progress closure while the tenant is
  // `HELD_FOR_RECOVERY`. Same OWNER_ROLES tier as `organization:close` ƒ?" reversing the most
  // destructive tenant-wide action is at least as sensitive as triggering it. Registered here for
  // the closed `Action` type (`AuthorizationDeniedError`/`auditAuthorizationDenied` typing) and
  // for matrix completeness, but the actual authorization check for this action is
  // `authorizeCancelClosure()` below, NOT the generic `authorize()` ƒ?" the caller
  // (`CancelOrganizationClosureService`) cannot build a `RequestContext` in the first place
  // (`resolve-request-context.ts` requires `TenantLifecycleRecord.status === ACTIVE`, which a
  // `HELD_FOR_RECOVERY` tenant is definitionally not), so `authorize()`'s own `AuthorizationInput`
  // shape (needs `context: RequestContext`) is not constructible on this path at all.
  | "organization:cancel-close"
  // D-143 (Document Archive domain, Nucleus 1): distinct namespace from the existing
  // `document:*` actions above, which belong to the low-level generic file-object storage
  // module (src/modules/document/ ƒ?" S3/malware-scan primitives). `docarchive:*` is the
  // higher-level business domain (Document/DocumentVersion/state machine) ƒ?" same tenant-wide
  // RBAC tiers as every other business resource, no per-item ACL (D-143 Decision 1/9 explicitly
  // rejected presuming an ACL `authorize()` doesn't implement). `docarchive:review` covers
  // claim/accept/reject ƒ?" the finer-grained "may THIS actor decide THIS version" check is a
  // separate, named service-level gate (`assertReviewerOrAdmin`), not a distinct RBAC action.
  | "docarchive:create"
  | "docarchive:read"
  | "docarchive:upload"
  | "docarchive:review"
  // D-143 Nucleus 2, entity 1 (Decision 5, D-145): Requirement ƒ?" "algo que um Subject precisa
  // possuir, apresentar ou manter vÇ­lido". Deliberately NOT reusing `requirement:*` above:
  // those belong to the older, distinct `subject` module's `RequirementAssignment` concept
  // (linked ExpirationItem, MISSING<->SATISFIED only). This Requirement is document-archive's
  // own aggregate (linked DocumentVersion evidence, 5-state derived status) ƒ?" a real naming
  // collision resolved deliberately by namespacing under `docarchive:`, same prefix as this
  // module's other actions, rather than overloading the older name silently.
  | "docarchive:requirement-create"
  | "docarchive:requirement-read"
  | "docarchive:requirement-update"
  | "docarchive:requirement-delete"
  // D-143 Nucleus 2, entity 3/3 (Decision 8, D-147): recurrence. Series management (create/
  // read/list/cancel, materializeAttempt) is a tenant-facing operation, same WRITE/READ_ONLY
  // tiers as Requirement above ƒ?" no per-item ACL here either, same D-143 Decision 9 rationale.
  // `docarchive:series-materialize` is distinct from `-update` because it is invoked by the
  // periodic producer/materializer worker on the series' own schedule, not by a direct caller
  // edit ƒ?" kept as its own action so a future service-role-scoped policy could grant it
  // separately from interactive series editing without reshaping the matrix again.
  | "docarchive:series-create"
  | "docarchive:series-read"
  | "docarchive:series-update"
  | "docarchive:series-cancel"
  | "docarchive:series-materialize"
  // D-226 Achado 2 (`guest-credential-issuance-scoping/estado-final-consolidado.md`): avulso/
  // non-recurring DocumentRequest creation, outside any DocumentRequestSeries cycle ƒ?" same
  // WRITE_ROLES tier as series-create/materialize above (D-222 gap 2).
  | "docarchive:request-create"
  // D-173 (`DocumentType` catalog): tenant-scoped, renamable-but-identity-stable catalog entry
  // closing item 8 of D-161's macro-order. Same tier as `document:delete`/`requirement:delete`
  // (ADMIN_ROLES for every mutation, READ_ONLY_ROLES for listing/reading) ƒ?" a catalog entry is
  // shared, tenant-wide configuration, not a per-item content resource a MEMBER should mutate.
  | "docarchive:documenttype-create"
  | "docarchive:documenttype-rename"
  | "docarchive:documenttype-deprecate"
  | "docarchive:documenttype-reactivate"
  | "docarchive:documenttype-read"
  // RequirementTemplate (P0.1). The catalog itself is admin-only, exactly like DocumentType;
  // APPLYING a template is deliberately a SEPARATE action with WRITE_ROLES ƒ?" applying creates
  // operational Requirements, it does not administer the catalog.
  | "docarchive:requirementtemplate-create"
  | "docarchive:requirementtemplate-update"
  | "docarchive:requirementtemplate-duplicate"
  | "docarchive:requirementtemplate-archive"
  | "docarchive:requirementtemplate-unarchive"
  | "docarchive:requirementtemplate-read"
  | "docarchive:requirementtemplate-apply"
  // Roadmap P0.7 ("RelatÇürios, ExportaÇõÇœo e Audit Trail"), fatias 1-2: bulk CSV reports over
  // Requirement rows (missing-requirements/requirements-by-subject/requirements-by-assignee),
  // reading every member's rows tenant-wide. Same disclosure-asymmetry rationale `item:export`
  // above already documents for D-123/D-126, not `docarchive:requirement-read`'s READ_ONLY_ROLES
  // tier (a single caller-scoped search page). ADMIN_ROLES, same tier as `item:export`.
  | "docarchive:requirement-export"
  // D-149 (Admin Activity/Audit Log view, admin-activity-log-scoping/estado-final-consolidado.md
  // decisÇœo 3): visibility into what OTHER members did (renewal/creation/export/etc.) is
  // disclosure-sensitive equivalent to bulk export (`item:export` above) - deliberately NOT the
  // more permissive precedent of reading one's own resource. ADMIN_ROLES, same tier as `item:export`.
  | "activity:read"
  // D-204 decision 1 (Roadmap P1 item 15, scheduled reports), D-213 fatia (HTTP CRUD):
  // ReportSubscription creation/management is disclosure-sensitive by the same reasoning as
  // `docarchive:requirement-export` above (a subscription names OTHER members as recipients,
  // and its reports are the same bulk cross-member CSVs `item:export`/`docarchive:requirement-
  // export` already gate) - ADMIN_ROLES, same tier, single action for create/read/list/delete
  // (no separate read tier - unlike DocumentType/RequirementTemplate, a subscription has no
  // less-sensitive "browse the catalog" use case a MEMBER/VIEWER would legitimately need).
  | "reports:subscription-manage"
  // D-205 decision 10 (Roadmap P1 item 16, dossier export): ADMIN_ROLES EXCLUSIVELY, no
  // assignee tier - achado real do Codex (Rodada 1): `authorization.ts`'s own resource-ownership
  // gate only applies when `ownerUserId` AND `assigneeUserId` both exist on the resource, and
  // `Requirement` never has `ownerUserId`, so an "ADMIN_ROLES ou assignee" rule could never be
  // expressed here structurally - closed by removing the assignee tier entirely rather than
  // inventing a new resource-ownership shape for one route. Same disclosure profile as
  // `docarchive:requirement-export`/`item:export` (a full-Subject export is the same bulk
  // cross-Requirement disclosure those already gate), covers preview+confirm+download.
  | "docarchive:dossier-export"
  // D-218 (Roadmap P1, "metadata configurÇ­vel por Document Type"): field/option CATALOG
  // mutation (create/rename/archive/reactivate a metadata field or one of its SINGLE_SELECT
  // options) is ADMIN_ROLES, same tier as `docarchive:documenttype-*` above ƒ?" a metadata field
  // definition is shared, tenant-wide catalog configuration, not a per-item content resource.
  // Editing the VALUE of an existing field on one Document is a separate, lower-sensitivity
  // action ƒ?" WRITE_ROLES, same tier as `docarchive:create`/`docarchive:upload` (day-to-day data
  // entry on an already-existing Document, not catalog administration). `docarchive:update`
  // (presumed to exist by an early draft of this decision) does not exist ƒ?" `Document` has no
  // generic edit action today, only `create`/`upload`/`review`.
  | "docarchive:documenttype-metadata-manage"
  | "docarchive:document-metadata-update";

export interface AuthorizedResource {
  tenantId: string;
  ownerUserId?: string;
  assigneeUserId?: string;
  status?: string;
}

export interface AuthorizationInput {
  context: RequestContext;
  action: Action;
  resource?: AuthorizedResource;
}

/**
 * Role model closed by Wave B2B-7 (RBAC, D-097/D-098) ƒ?" `Membership.role` (D-090) has 4 real
 * values; this matrix now recognizes all of them. Research protocol E-014 applied for the
 * first time (docs/engineering/research-protocol.md): GitHub/Linear/Slack/Notion + NIST/ANSI
 * INCITS 359 (Hierarchical RBAC) don't converge on a single "what does Admin get beyond
 * Member" answer, so each action formerly gated to ADMIN_ROLES got a named, individual
 * decision instead of a blanket parity rule ƒ?" see multi-user-b2b-wave-b2b7-scope.md.
 */
export type Role = "OWNER" | "ADMIN" | "MEMBER" | "VIEWER";

const READ_ONLY_ROLES: ReadonlySet<Role> = new Set(["OWNER", "ADMIN", "MEMBER", "VIEWER"]);
const WRITE_ROLES: ReadonlySet<Role> = new Set(["OWNER", "ADMIN", "MEMBER"]);
const ADMIN_ROLES: ReadonlySet<Role> = new Set(["OWNER", "ADMIN"]);
/** Owner-exclusive tier (B2B-7) ƒ?" reserved for actions with tenant-wide external/reputational
 * impact (research: the class GitHub/Linear/Slack/Notion keep apart from ordinary content
 * admin). Members: `tenant:configure-document-request-delivery` (B2B-7),
 * `organization:update-settings` (B2B-10), `organization:close` (W3-07/D-124). */
const OWNER_ROLES: ReadonlySet<Role> = new Set(["OWNER"]);

const ACTION_ROLES: Record<Action, ReadonlySet<Role>> = {
  "item:create": WRITE_ROLES,
  "item:read": READ_ONLY_ROLES,
  "item:update": WRITE_ROLES,
  "item:delete": ADMIN_ROLES,
  "item:export": ADMIN_ROLES,
  "item:watch": WRITE_ROLES,
  "reminder:manage": WRITE_ROLES,
  "document:reserve-upload": WRITE_ROLES,
  "document:read": READ_ONLY_ROLES,
  "document:delete": ADMIN_ROLES,
  "extraction:confirm": WRITE_ROLES,
  // B2B-7 bug fix (not an ADMIN-vs-OWNER call): this action gates both read and update of a
  // per-user preference (ctx.principal.userId-keyed, notification-preferences-service.ts),
  // never tenant-wide config. This is tied to the ability to RECEIVE a reminder -
  // assigneeUserId is never role-checked, so a VIEWER can legitimately be a notification
  // recipient and must be able to configure it for themself. Reuses READ_ONLY_ROLES (already
  // "any real Membership") rather than adding a 5th constant with the same 4 members.
  "notification:configure": READ_ONLY_ROLES,
  "audit:read": READ_ONLY_ROLES,
  "system:ping": READ_ONLY_ROLES,
  "subject:create": WRITE_ROLES,
  "subject:read": READ_ONLY_ROLES,
  "subject:update": WRITE_ROLES,
  "subject:delete": ADMIN_ROLES,
  "requirement:assign": WRITE_ROLES,
  "requirement:read": READ_ONLY_ROLES,
  "requirement:update": WRITE_ROLES,
  "requirement:delete": ADMIN_ROLES,
  "requirement:review": WRITE_ROLES,
  "requirement:request-document": WRITE_ROLES,
  "tenant:configure-document-request-delivery": OWNER_ROLES,
  "import:create": WRITE_ROLES,
  "import:read": READ_ONLY_ROLES,
  "import:commit": WRITE_ROLES,
  "import:map": WRITE_ROLES,
  "membership:invite": ADMIN_ROLES,
  "membership:revoke-invitation": ADMIN_ROLES,
  "membership:list-invitations": ADMIN_ROLES,
  "membership:list-members": READ_ONLY_ROLES,
  "membership:role-change": ADMIN_ROLES,
  "membership:remove": ADMIN_ROLES,
  "membership:leave": READ_ONLY_ROLES,
  "organization:update-settings": OWNER_ROLES,
  "organization:close": OWNER_ROLES,
  "organization:cancel-close": OWNER_ROLES,
  "docarchive:create": WRITE_ROLES,
  "docarchive:read": READ_ONLY_ROLES,
  "docarchive:upload": WRITE_ROLES,
  "docarchive:review": WRITE_ROLES,
  "docarchive:requirement-create": WRITE_ROLES,
  "docarchive:requirement-read": READ_ONLY_ROLES,
  "docarchive:requirement-update": WRITE_ROLES,
  "docarchive:requirement-delete": WRITE_ROLES,
  "docarchive:series-create": WRITE_ROLES,
  "docarchive:series-read": READ_ONLY_ROLES,
  "docarchive:series-update": WRITE_ROLES,
  "docarchive:series-cancel": WRITE_ROLES,
  "docarchive:series-materialize": WRITE_ROLES,
  "docarchive:request-create": WRITE_ROLES,
  "activity:read": ADMIN_ROLES,
  "reports:subscription-manage": ADMIN_ROLES,
  "docarchive:dossier-export": ADMIN_ROLES,
  "docarchive:documenttype-metadata-manage": ADMIN_ROLES,
  "docarchive:document-metadata-update": WRITE_ROLES,
  "docarchive:documenttype-create": ADMIN_ROLES,
  "docarchive:documenttype-rename": ADMIN_ROLES,
  "docarchive:documenttype-deprecate": ADMIN_ROLES,
  "docarchive:documenttype-reactivate": ADMIN_ROLES,
  "docarchive:documenttype-read": READ_ONLY_ROLES,
  "docarchive:requirementtemplate-create": ADMIN_ROLES,
  "docarchive:requirementtemplate-update": ADMIN_ROLES,
  "docarchive:requirementtemplate-duplicate": ADMIN_ROLES,
  "docarchive:requirementtemplate-archive": ADMIN_ROLES,
  "docarchive:requirementtemplate-unarchive": ADMIN_ROLES,
  "docarchive:requirementtemplate-read": READ_ONLY_ROLES,
  "docarchive:requirementtemplate-apply": WRITE_ROLES,
  "docarchive:requirement-export": ADMIN_ROLES,
};

export type AuthorizationDenialReason = "TENANT_MISMATCH" | "NO_MEMBERSHIP" | "INSUFFICIENT_ROLE" | "RESOURCE_OWNERSHIP_MISMATCH";

export class AuthorizationDeniedError extends Error {
  constructor(
    readonly reason: AuthorizationDenialReason,
    readonly action: Action,
  ) {
    super(`Authorization denied for action "${action}": ${reason}`);
    this.name = "AuthorizationDeniedError";
  }
}

/**
 * authorize() ƒ?" implementation-blueprint.md ¶õ4.3: "authorize() verifica primeiro
 * igualdade de tenant, depois papel e vÇðnculo por recurso." Throws
 * AuthorizationDeniedError (never returns false) so callers cannot forget to check
 * a boolean result ƒ?" the http layer maps this to AuthorizationError (403).
 *
 * Order matters and is deliberately fail-closed at every step:
 *  1. context must carry at least one role (no membership => no access at all).
 *  2. if a resource is supplied, resource.tenantId MUST equal context.tenant.tenantId ƒ?"
 *     this is the check that makes cross-tenant ID substitution fail, independent of
 *     role, and independent of what the caller *claims* the resource's tenant is (the
 *     resource object here must always be populated from a DB read keyed by the
 *     authenticated tenantId, never from client input ƒ?" see repository contract).
 *  3. role must be sufficient for the action.
 *  4. if the resource declares an owner/assignee, membership role alone isn't enough
 *     for actions that are resource-scoped by ownership in the future; MVP has no
 *     per-item ownership finer than tenant, so this step is a no-op today but kept
 *     explicit so a future item-level ACL doesn't require re-deriving this function.
 */
export function authorize(input: AuthorizationInput): void {
  const { context, action, resource } = input;
  const roles = context.tenant.roles as Role[];

  if (roles.length === 0) {
    throw new AuthorizationDeniedError("NO_MEMBERSHIP", action);
  }

  if (resource && resource.tenantId !== context.tenant.tenantId) {
    throw new AuthorizationDeniedError("TENANT_MISMATCH", action);
  }

  const allowedRoles = ACTION_ROLES[action];
  const hasRole = roles.some((role) => allowedRoles.has(role));
  if (!hasRole) {
    throw new AuthorizationDeniedError("INSUFFICIENT_ROLE", action);
  }

  if (resource?.assigneeUserId && resource.ownerUserId) {
    const isOwnerOrAssignee =
      resource.ownerUserId === context.principal.userId ||
      resource.assigneeUserId === context.principal.userId;
    // OWNER and ADMIN bypass per-resource ownership (tenant-wide content admin, B2B-7 -
    // ADMIN has parity with OWNER over business resources, see authorization.ts role model
    // comment above), MEMBER/VIEWER scoped resources require ownership match once per-item
    // ACLs exist. This is enforced only when both fields ARE present (defensive for future
    // use), never invented from nothing.
    if (!roles.includes("OWNER") && !roles.includes("ADMIN") && !isOwnerOrAssignee) {
      throw new AuthorizationDeniedError("RESOURCE_OWNERSHIP_MISMATCH", action);
    }
  }
}

export interface CancelClosureAuthorizationInput {
  /** Read directly (`GlobalUser.identityStatus`) by `CancelOrganizationClosureService`'s own
   * identity-mapping path ƒ?" never derived from a `RequestContext`, which cannot be built for a
   * `HELD_FOR_RECOVERY` tenant in the first place (see the `organization:cancel-close` doc
   * comment above). */
  identityStatus: "ACTIVE" | "SUSPENDED";
  membershipStatus: "ACTIVE" | "SUSPENDED" | "REMOVED";
  membershipRole: Role;
}

/**
 * D-127: the dedicated authorization primitive for cancelling an in-progress tenant closure ƒ?"
 * deliberately NOT `authorize()`. Two reasons this cannot be the generic function:
 *  1. `authorize()`'s signature requires a `RequestContext`, which requires
 *     `resolveWorkingOrganization()`/`TenantLifecycleRecord.status === ACTIVE` to have already
 *     succeeded (`resolve-request-context.ts`) ƒ?" structurally uncomputable for a tenant that IS
 *     `HELD_FOR_RECOVERY` by definition on this path.
 *  2. Even granting a hypothetical relaxed `RequestContext`, the tenant's own RBAC state during
 *     the recovery window is not necessarily the ordinary "one clean read" `authorize()` assumes
 *     ƒ?" this reads `GlobalUser`/`Membership` directly and fresh, with its own explicit
 *     ACTIVE-on-both checks, rather than trusting a context object assembled by a different
 *     resolution path built for a different (ACTIVE-tenant) precondition.
 *
 * Same fail-closed ordering discipline as `authorize()`: identity status checked before
 * membership status before role ƒ?" the first failing check wins, no partial/best-effort pass.
 * Throws (never returns false), same convention as `authorize()`, so callers cannot forget to
 * check a boolean result.
 */
export function authorizeCancelClosure(input: CancelClosureAuthorizationInput): void {
  const action: Action = "organization:cancel-close";
  if (input.identityStatus !== "ACTIVE") {
    throw new AuthorizationDeniedError("NO_MEMBERSHIP", action);
  }
  if (input.membershipStatus !== "ACTIVE") {
    throw new AuthorizationDeniedError("NO_MEMBERSHIP", action);
  }
  if (input.membershipRole !== "OWNER") {
    throw new AuthorizationDeniedError("INSUFFICIENT_ROLE", action);
  }
}


exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -A 100 "Eixo: Governan.a de Produto" docs/engineering/joint-review-criteria.md; rg -n -i "D-110|D-111|D-120|D-124|D-127|D-143|D-197|simulate-principal-policy|gsi8_worker_isolation|cross-tenant|vazamento|leak|IDOR" docs/architecture/decisions-log.md; rg -n "''^export type Action|''^const ACTION_ROLES|''^export function authorize|organization:cancel-close" src/modules/identity/domain/authorization.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 1277ms:
151:## Eixo: Governança de Produto e Serviço Multi-tenant
152-
153-Convergido em 2026-08-19 (fontes: AWS Well-Architected SaaS Lens — onboarding automatizado no control plane, crypto-shredding no offboarding, tenant como conceito de primeira classe). Avalia o serviço percebido/administrado pelo tenant ao longo do ciclo de vida — não reavalia isolamento/IAM técnico (eixo de Segurança). **FinOps fica como eixo próprio por decisão do usuário, não formalizado ainda** — economia unitária aqui é só um critério de produto, não compete em peso com esse eixo futuro.
154-
155-| # | Critério | Peso | Definição |
156-|---:|---|---:|---|
157-| 1 | Lifecycle Automatizado de Tenant | 18% | Onboarding/ativação/convite/mudança de plano/suspensão/offboarding como estados explícitos, idempotentes, auditáveis — hoje o tenant nasce implicitamente no primeiro login (MVP `tenantId=userId`), sem control plane dedicado. |
158-| 2 | Offboarding, Exportação & Destruição Criptográfica | 16% | "Não conseguimos deletar um tenant" é falha de compliance, não só técnica; crypto-shredding coordenado com retenção/backup/legal hold. |
159-| 3 | Planos, Entitlements, Quotas & Fairness | 13% | Capacidades/limites como política centralizada e versionada; `TenantQuota` já existe (M1), falta plano de billing real. Não duplica rate-limiting de segurança — mede correção da oferta do produto. |
160-| 4 | Correção do Serviço de Lembretes & Proteção do Usuário | 15% | Timezone/DST/opt-out/quiet hours/renovação corrigíveis; lembrete perdido/duplicado/obsoleto tem detecção e reparo — é a falha mais grave possível do produto. |
161-| 5 | Transparência, Usabilidade & Acessibilidade | 10% | Estado/erro/atraso comunicado em linguagem clara; ainda não se aplica plenamente (sem frontend), mas entra no radar em M4+. |
162-| 6 | Administração, Suporte & Operação sob a Ótica do Tenant | 10% | Suporte autorizado diagnostica saúde/uso/config por tenant com trilha de auditoria, sem acesso informal a conteúdo. |
163-| 7 | Métricas de Valor, Consumo & Economia Unitária | 10% | Adoção/sucesso de lembrete/consumo atribuível por tenant de forma privacy-safe — germe do futuro eixo FinOps, não o eixo em si. |
164-| 8 | Evolução Unificada & Controle de Customização | 8% | Nenhuma feature cria "fork" por tenant; variação por plano usa mecanismo governado (mesmo princípio já aplicado ao reshard versionado do GSI3). |
165-
166-## Como adicionar um novo eixo
167-
168-Não criar a tabela de critérios dentro do primeiro doc de auditoria do eixo novo. Seguir o mesmo procedimento de convergência independente (`AGENTS.md` §4) e, ao final, adicionar aqui uma seção nova nesse mesmo formato — o registro de auditoria referencia a seção, não a duplica.
72:| D-052 | Com M9-M11 deployados, avaliado se/o que implementar de M12 (Commercial Monetization/Billing) agora. Protocolo Claude↔Codex completo (2 rodadas, convergência forte já na rodada 1): `Subscription`/`BillingWebhookInbox`/verificador de webhook não têm nenhum uso real possível antes da escolha do provider de pagamento (decisão de produto do Marcelo, explicitamente fora do roadmap) — implementá-los agora seria código morto com risco real de abstração errada (assinatura/replay/shape de evento variam por vendor). Alternativa mais estreita cogitada (operação administrativa manual para expandir `TenantEntitlement` de um early-adopter negociado direto) também descartada na rodada 2: `src/modules/identity/domain/authorization.ts` não tem nenhum conceito de "platform staff" cross-tenant hoje (toda `Action` é escopada dentro do próprio tenant, `OWNER\|MEMBER\|VIEWER`) — criar essa action exigiria ou um role novo para um caso ainda inexistente, ou uma action `ADMIN_ROLES` tenant-scoped que deixaria o próprio `OWNER` aumentar seu limite sozinho (bypass de quota self-service, vetor de abuso que o projeto evita, mesmo cuidado de D-045). **M12 inteiro fica bloqueado, zero código novo.** Caminho aceito para o caso hipotético de early-adopter: `UpdateItem` manual via os builders OCC-safe já existentes (`buildVersionedUpdate`, `src/shared/dynamodb/occ.ts`), só quando/se o caso for real — nunca um script ou endpoint dedicado hoje | Type 1 (nível 5-6, decisão de arquitetura/segurança — decide NÃO implementar, não decide como implementar) | 9,3 | 9,4 | **APPROVED** (M12 bloqueado por decisão de produto — fornecedor de pagamento — até novo gatilho real) | `docs/architecture/roadmap-evolution/15-m12-billing-scope-decision.md` | 2026-08-23 |
89:| D-069 | W3-07 — sessão de implementação (chunk 3/N), roteiro `Q` itens 2 (conclusão) e 3 (completo): lane `SystemMutation` (`src/shared/tenant-lifecycle/system-mutation.ts`) — allowlist fechada por união discriminada (`SystemMutationOperation`: `LIFECYCLE_TRANSITION`/`PURGE_DELETE`/`OUTBOX_BOOKKEEPING`, per o allowlist nomeado da Rodada E/§Q). `executeSystemMutation` não aceita `TransactWriteEntry[]` do chamador (diferente de `TenantBusinessMutation`) — só a operação tipada; a própria função constrói o array via `occ.ts`. Só `LIFECYCLE_TRANSITION` tem implementação real nesta sessão (`transitionTenantLifecycle` — primitivo real, OCC-fenced na versão do registro + `assertValidTransition` + `ConditionCheck` extra no status atual, valida `ACTIVE→DELETING` de ponta a ponta); `PURGE_DELETE`/`OUTBOX_BOOKKEEPING` são membros reservados do allowlist (tipados, mas lançam `SystemMutationNotImplementedError` se chamados — não fallback silencioso). **Boundary estrutural real (roteiro `Q` item 3, pendência explícita de D-068)**: regra nova `no-raw-dynamodb-writes-outside-lanes` em `.dependency-cruiser.cjs` — confina import de comandos de escrita cru do `@aws-sdk/lib-dynamodb` (`PutCommand`/`UpdateCommand`/`DeleteCommand`/`TransactWriteCommand`/`BatchWriteCommand`) a `src/modules/*/persistence/`, `src/modules/*/composition/`, `src/shared/dynamodb/`, `src/shared/outbox/persistence/` — qualquer outro arquivo em `src/modules/**`/`src/shared/**` que importe esse pacote falha `npm run check-boundaries` (rodado no CI). Prova real de que a regra pega um bypass de verdade (não só existe): `test/architecture/tenant-fence-boundary.test.ts` (categoria nova, `test/architecture/`, adicionada a `vitest.config.ts`) planta 3 arquivos-fixture reais em `src/` (import direto de `PutCommand`, de `TransactWriteCommand` sem `ConditionCheck` de lifecycle, e de `BatchWriteCommand` em `src/shared/` fora de `dynamodb/`/`outbox/persistence/`), roda o CLI real do `depcruise` contra `src` com o config real, confirma violação reportada com o nome da regra + caminho do arquivo, e remove os fixtures em `finally`/`afterEach` (mais um `beforeAll` de segurança). ESLint não foi estendido (a regra já registrada em `.eslintrc.cjs`'s comentário — dependency-cruiser é a "AUTHORITATIVE boundary check", ESLint só feedback de editor — não havia padrão equivalente aplicável a "não importar um pacote npm específico" que justificasse duplicar em `no-restricted-imports`, texto de import já cobre isso de forma idêntica e ESLint não adiciona poder de detecção aqui). Avaliado como nível 3-4 (implementação direta do roteiro já aprovado em D-066/D-067, mecanismo reusa builders de `occ.ts` já revisados e o padrão de dependency-cruiser já existente desde 2026-08-19) — protocolo `AGENTS.md` §4 completo não reaberto | Type 1 (nível 3-4, implementação direta de decisão já aprovada) | N/A — protocolo completo não aplicável | N/A | **IMPLEMENTED + UNIT TESTED + boundary mechanism proven by adversarial test** (13 testes novos: 8 de `SystemMutation`/`transitionTenantLifecycle` em `test/unit/system-mutation.test.ts` incluindo transição ilegal, conflito de versão OCC, BLOCKED/HELD, isolamento cross-tenant, allowlist não-implementado, e um `@ts-expect-error` provando a contenção em nível de tipo; 5 testes de arquitetura em `test/architecture/tenant-fence-boundary.test.ts` incluindo os 3 ataques de bypass adversariais pedidos pelo prompt da sessão. 978 testes de backend passando (111 arquivos, era 965), typecheck/lint/check-boundaries/check-docs limpos, nenhuma regressão). **Explicitamente NÃO feito nesta sessão** (fica para o próximo chunk, ver `NEXT_SESSION_PROMPT.md`): migração dos demais writers tenant-scoped listados em D-068 continua pendente; `PURGE_DELETE`/`OUTBOX_BOOKKEEPING` continuam sem implementação real (nenhum call site ainda precisa deles — o sweeper permanente e o outbox bookkeeping explícito são roteiro `Q` itens 5 e além); nenhum orquestrador real (Step Functions/Lambda) chama `transitionTenantLifecycle` ainda — só o primitivo existe, testado isoladamente; Codex adversarial review desta sessão não executado (sem orçamento restante), recomendado como próximo passo antes de expandir `SystemMutation` para `PURGE_DELETE`/`OUTBOX_BOOKKEEPING` de verdade, dado que essas são as operações de maior privilégio do sistema | `src/shared/tenant-lifecycle/system-mutation.ts`, `.dependency-cruiser.cjs`, `test/architecture/tenant-fence-boundary.test.ts` | 2026-08-29 |
98:| D-078 | W3-07 — mesma sessão de D-076/D-077, item 3 do prompt de retomada: mitigação defensiva barata para o risco de admissão parcial em `import-service.ts`'s `reserveImport()`, sem forçar a decisão de produto (unificar quota+idempotency+job-create numa única transação maior, trade-off latência-vs-atomicidade) que D-074/D-075 deixaram deliberadamente para o dono do produto. `idempotency.begin()` e as duas reservas `quota.consume()` (IMPORT_COUNT/IMPORT_BYTES) continuam fora da transação fenceada que cria o `ImportJob` — isso NÃO muda. O que muda: quando a criação fenceada falha (rejeição da fence OU um conflito OCC comum), o `catch` agora libera best-effort o que já tinha sido reservado — `idempotency.abort()` (a mesma primitiva já usada por `renewItem`'s caminho de conflito OCC, "idempotency liveness residual") e `quota.release()` para os dois tipos — antes de relançar o erro original. Toda compensação roda via `Promise.allSettled` (nunca deixa uma falha de compensação mascarar o erro real nem travar a resposta ao chamador). Sem isso: uma rejeição deixava a chave de idempotência presa em `IN_PROGRESS` para sempre (todo retry, mesmo depois do tenant voltar a ACTIVE, batia em `ConcurrentOperationError` permanentemente) e a quota consumida vazava até expirar a janela — com isso, uma rejeição volta o tenant ao estado pré-tentativa (quota liberada, idempotency key livre para nova tentativa), sem alterar o que já era aceito por design para essa classe de risco (uma falha DEPOIS da transação fenceada, ex. entre commit e presign, continua não coberta — fora do escopo desta mitigação). **Achado real de infraestrutura de teste descoberto e corrigido nesta sessão**: `InMemoryImportStore.transactWrite` (o fake usado pelos testes de `ImportService`) só reconhecia a convenção `#version = :expectedVersion`/`#setN`/`:setN` de `buildVersionedUpdate` para condição e aplicação de `Update` — a condição `#status = :expected` de `transitionIdempotencyStatus` (usada por `idempotency.abort()`) nunca era avaliada (sempre "passava" silenciosamente) e o SET correspondente (`#status`/`#requestHash`/`#responseRef`/`#completedAt`, nomes literais, não a convenção `#setN`) nunca era aplicado — ou seja, `abort()` "funcionava" sem erro mas não mudava nada de verdade, mascarando completamente a mitigação nos testes. Generalizado para um parser genérico de `ConditionExpression`/`UpdateExpression` (`splitTopLevelAnd`/`evaluateEqualityClause`/`applyUpdateExpression`) que avalia QUALQUER cláusula de igualdade `#name = :value` e aplica QUALQUER `SET`/`REMOVE` nomeado, não só a convenção `#setN` — acerta tanto o caso antigo quanto o novo sem duplicar lógica. **IMPLEMENTED + UNIT TESTED** (2 testes adversariais novos em `test/unit/import/import-service.test.ts`: rejeição por DELETING libera a quota IMPORT_COUNT/IMPORT_BYTES de volta a 0; rejeição por DELETING aborta o idempotency record, permitindo retry bem-sucedido com a MESMA Idempotency-Key uma vez o tenant voltando a ACTIVE). 1034 testes de backend passando (era 1032), typecheck/lint/check-boundaries/check-docs limpos, zero regressão | Type 1 (nível 3-4, mitigação defensiva mecânica que não altera a fronteira transacional nem o trade-off já identificado como decisão de produto) | N/A — mitigação de menor esforço já sugerida pelo Codex em D-075, não uma nova decisão de arquitetura | N/A | **IMPLEMENTED + UNIT TESTED** — a admissão parcial em si (quota/idempotency fora da transação fenceada) permanece decisão de produto pendente, mas o vazamento residual de uma tentativa REJEITADA (quota presa, idempotency key presa) está fechado | `src/modules/import/application/import-service.ts`, `test/unit/import/import-service.test.ts`, `test/unit/import/in-memory-store.ts` | 2026-08-29 |
99:| D-079 | W3-07 — terceira rodada de revisão adversarial Codex, pedida pelo prompt de retomada da sessão, especificamente re-checando o fechamento do gap PK/SK/TableName (D-076) e o teste do allowlist `SystemMutation` (D-077), sobre o diff acumulado desde `3e590ca` (D-076/D-077/D-078). Primeira tentativa de invocação falhou por bloqueio de política do próprio Codex CLI numa das suas ferramentas internas (`powershell.exe` com pipeline complexo) mais um flag de "possible cybersecurity risk" do lado do provedor — não um travamento (CPU não ficou preso perto de zero por tempo desproporcional, terminou com exit code 1 e mensagem de erro explícita); reformulado com linguagem menos "adversarial" (trocado "adversarial"/"bypass"/"hunt" por "independent"/"gap"/"look critically" no prompt, mantendo o conteúdo técnico idêntico) e reinvocado com sucesso na segunda tentativa, mesmo padrão `codex exec --skip-git-repo-check -C <repo> - < prompt.txt > output.txt 2>&1` em primeiro plano. **Nota do Codex: 6,0/10** — ainda abaixo do gate de 9,0. **2 achados BLOQUEANTES reais, confirmados por releitura do código, não refutados**: (1) a mitigação de compensação de `import-service.ts` (D-078) só envolvia a criação fenceada do `ImportJob`, deixando fora do try/catch os dois `quota.consume()` — um retry com a MESMA idempotency key consumia quota de novo a cada chamada mesmo quando `begin()` retornaria `COMPLETED_SAME_REQUEST`; um segundo chamador concorrente com a MESMA key perdia a corrida em `begin()` (erro de conflito genuíno) DEPOIS de já ter consumido quota, vazando permanentemente; uma falha no SEGUNDO `quota.consume()` (IMPORT_BYTES) depois do primeiro já ter sucedido (IMPORT_COUNT) não tinha nenhuma compensação. (2) o architecture-test do allowlist (D-077) provava só que UM `kind` sentinela específico (`DELETE_EVERYTHING_FOR_THIS_TENANT`) está ausente da união — não prova que a união está fechada contra um `kind` genuinamente novo e imprevisto que um futuro contribuidor adicionasse (junto com o `buildEntries` case correspondente); o Codex sugeriu concretamente asserções de tipo bidirecionais (`SystemMutationOperation["kind"] extends ApprovedKind` e vice-versa) contra um allowlist mantido independentemente do módulo de implementação. **3 achados NÃO-BLOQUEANTES reais, corrigidos na mesma sessão**: (a) a descrição do teste "BYPASS ATTEMPT 2" alegava que os membros da união são "exact object shapes" — impreciso: TypeScript só rejeita campo excedente em um literal FRESCO, não numa atribuição via variável intermediária; comentário corrigido para não overclaim, mantendo a nota de que isso não importa para segurança hoje porque `buildEntries()` ignora campos extras. (b) confirmado (não corrigido, já correto): a checagem de `TableName` é sólida (nomes de tabela são case-sensitive no DynamoDB real, GSI não é um `TableName` alternativo válido, não há alias transparente) — "nenhum bypass prático de TableName encontrado". (c) confirmado como limitação real já documentada, não uma regressão: o check de PK só valida entradas cujo PK casa com `TENANT#<id>#...`; uma entrada `buildConditionalPut()` visando uma linha lógica não-prefixada de outro tenant (ex. `GUESTTOKEN#`/`IDENTITY#`) com um `tenantId` declarado forjado passaria os dois checks — nenhum call site real produz isso hoje (confirmado por grep), mas a frase "item 1 closed de verdade" no registro anterior era ampla demais; o que está fechado é o acesso cross-tenant dentro do keyspace `TENANT#...` universal atual, não a posse estrutural de tenant para toda entrada que a lane aceitaria. Achados corrigidos nesta mesma sessão de reavaliação: (1) `import-service.ts`'s `reserveImport()` reordenado — `idempotency.begin()` agora roda ANTES dos dois `quota.consume()` (a ordenação de menor esforço que o próprio Codex já tinha sugerido em D-075 e que a sessão anterior não tinha implementado), fechando os 3 vazamentos apontados: replay nunca mais toca quota (curto-circuita em `COMPLETED_SAME_REQUEST` antes de qualquer consumo); um chamador concorrente perdedor nunca toca quota (erro de conflito acontece antes); e ambos os `quota.consume()` agora vivem dentro do MESMO try/catch da criação do job, com rastreamento por-chamada (`countConsumed`/`bytesConsumed`) do que foi realmente reservado NESTA tentativa, para compensar só isso. (2) asserções de tipo bidirecionais adicionadas em `test/architecture/system-mutation-allowlist.test.ts` — `ApprovedSystemMutationKind` declarado de forma independente do módulo de implementação, com `AssertUnionIsSubsetOfApproved`/`AssertApprovedIsSubsetOfUnion` (checagem `extends` nos dois sentidos) que falham a compilação se a união de `system-mutation.ts` ganhar/perder um `kind` sem que este allowlist seja atualizado — sanity-checado manualmente nesta sessão (removendo `OUTBOX_BOOKKEEPING` do allowlist local e confirmando que `npm run typecheck` falha com a mensagem de erro esperada, depois revertido). **IMPLEMENTED + UNIT TESTED** (2 testes adversariais novos em `test/unit/import/import-service.test.ts` provando replay não cobra quota de novo e conflito genuíno de key não vaza quota; 1 teste-âncora novo em `system-mutation-allowlist.test.ts` documentando a prova de tipo). 1037 testes de backend passando (era 1034), typecheck/lint/check-boundaries/check-docs limpos, zero regressão. Achado NÃO-BLOQUEANTE (c) acima permanece pendência real documentada, não forçada nesta sessão — fechar de verdade exigiria um dos 3 caminhos maiores já listados (metadados obrigatórios pelos builders, um tipo de entrada tenant-branded, ou proibir estruturalmente qualquer entidade não-`TENANT#`-prefixed nesta lane) | Type 1 (nível 5-6, `AGENTS.md` §4 — terceira rodada de revisão adversarial sobre um fechamento de achados de segurança/modelo de dados já registrado) | N/A — revisão de implementação, não rodada de design com nota cega dupla Claude↔Codex | 6,0/10 (Codex, sobre o diff D-076/D-077/D-078, antes dos 2 fixes registrados nesta mesma linha) | **REVISADO E PARCIALMENTE RE-CORRIGIDO NA MESMA SESSÃO** — os 2 achados BLOQUEANTES do Codex round 3 foram corrigidos (reordenação idempotency-antes-de-quota em import-service.ts; asserção de tipo bidirecional independente para o allowlist); os 3 NÃO-BLOQUEANTES foram avaliados (1 corrigido — overclaim de comentário; 2 confirmados como já corretos ou como limitação residual real documentada, não mascarada). Uma QUARTA rodada Codex re-checando especificamente estes 2 fixes NÃO foi executada nesta sessão (orçamento esgotado) — nota atual (6,0/10) é sobre o estado ANTES destes 2 fixes, não uma reavaliação pós-fix; estado honesto: ainda não há confirmação Codex de que os fixes desta linha realmente fecham os achados, só a própria análise interna + testes | `src/modules/import/application/import-service.ts`, `test/unit/import/import-service.test.ts`, `test/architecture/system-mutation-allowlist.test.ts` | 2026-08-29 |
100:| D-080 | W3-07 — sessão de continuação: (1) narrowing do gap residual não-`TENANT#`-prefixed em `findTenantMismatch` (re-verificado por leitura de todo tipo de domínio real usando as chaves `IDENTITY#cognitoSub#`/`GUESTTOKEN#`/`SESSION#`/`LOGINATTEMPT#`/`TEXTRACTJOB#`: `IdentityMapping`/`GuestTokenPointer`/`Session`/`TextractJob` DECLARAM `tenantId`, já cobertos pelo check 1 existente se forjados; teste adversarial novo prova isso com a forma real de `GuestTokenPointer`); (2) quarta rodada Codex, pedida explicitamente pelo prompt de retomada, re-confirmando os 2 fixes BLOQUEANTES de D-079 (reordenação import-service.ts, asserção de tipo bidirecional do allowlist) sobre o diff acumulado desde `c496b91`. **Nota do Codex: 8,8/10** — primeira rodada a superar 8,0, ainda abaixo do gate de 9,0 mas por margem pequena. **Ambos os fixes de D-079 CONFIRMADOS CORRETOS, sem achado bloqueante remanescente**: (1a) reordenação idempotency-antes-de-quota fecha os 3 vazamentos originais (replay não recobra quota; perdedor de corrida não toca quota; falha na segunda reserva libera só a primeira) — Codex verificou que `idempotency.begin()` não tem nenhum modo de falha que toque quota, e que a interleaving entre `begin()` e o bloco try não introduz um caminho novo para os 3 vazamentos específicos; (1b) allowlist bidirecional (`AssertUnionIsSubsetOfApproved`/`AssertApprovedIsSubsetOfUnion`) confirmado como prova real de fechamento contra um `kind` genuinamente novo, não só o sentinela hard-coded anterior — só um contributor que edite AMBOS implementação e allowlist no mesmo diff (mudança visível em review) poderia "adicionar" um kind sem quebrar o build, o que não é drift silencioso. **1 achado real NÃO-BLOQUEANTE encontrado pelo Codex, corrigido na mesma sessão**: o narrowing do item 1 (LoginAttempt como única exceção real sem `tenantId` nem `TENANT#` PK) estava incompleto — `GuestRateLimitRecord` (`src/modules/subject/application/guest-rate-limiter.ts`, PK `GUESTTOKEN#<selectorHash>#RATE`) também não declara `tenantId`, por design (rate-limiting acontece antes do token de convidado resolver um tenant); Codex confirmou por grep que nem `LoginAttempt` nem `GuestRateLimitRecord` são roteados por `executeTenantBusinessMutation`/`tryTenantBusinessMutation` hoje, então o gap continua não-explorável, mas a documentação/teste que dizia "a única exceção real" precisava listar as duas — corrigido no cabeçalho de `tenant-business-mutation.ts` e no teste `KNOWN GAP` de `tenant-lifecycle.test.ts`. **1 achado NÃO-BLOQUEANTE adicional, corrigido preventivamente**: uma pequena janela de liveness em `import-service.ts`'s `reserveImport()` entre `idempotency.begin()` (ACQUIRED) e o bloco `try` que já cobria compensação — `newImportJobId()`/cálculo de `jobExpiresAt`/construção do objeto `job` ficavam FORA do try, então uma falha do gerador de ID injetado deixaria a idempotency key travada em `IN_PROGRESS` sem compensação alcançável (nenhuma quota tocada nesse ponto, então não é um dos 3 vazamentos originais, mas ainda um wedge real); movido para dentro do `try` para que `idempotency.abort()` cubra essa janela também. **IMPLEMENTED + UNIT TESTED** — nenhum teste novo necessário para os 2 fixes desta linha (mudança de documentação + reordenação mecânica sem nova superfície de comportamento observável; a suíte existente de `import-service.test.ts`/`tenant-lifecycle.test.ts` já cobre os caminhos afetados e passou sem alteração). 1038 testes de backend passando (era 1037), typecheck/lint/check-boundaries/check-docs limpos, zero regressão. Avaliação geral do Codex: os 8 mecanismos do fence (cross-validation tenant/entries, verificação de PK físico, fechamento do allowlist SystemMutation, ordenação de compensação import-service, hardening de CancellationReasons, fix TOCTOU do bootstrap, fix de resume BLOCKED/HELD, compensação de objeto órfão S3) formam "uma defesa coerente e substancialmente testada"; nenhum achado bloqueante novo de production-readiness encontrado; recomenda UMA rodada curta de correção-e-reconfirmação (as 2 correções já feitas nesta linha) antes de possivelmente atingir 9,0, e nota que rodadas adicionais amplas sobre este gate específico têm retorno decrescente a partir daqui, a menos que o time decida substituir o gap residual documentado (não-`TENANT#`, 2 entidades reais nunca exercitadas) por um design estrutural maior (metadados obrigatórios pelos builders, tipo de entrada tenant-branded) | Type 1 (nível 5-6, `AGENTS.md` §4 — quarta rodada de revisão adversarial sobre um fechamento de achados de segurança/modelo de dados já registrado) | N/A — revisão de implementação, não rodada de design com nota cega dupla Claude↔Codex | 8,8/10 (Codex, sobre o diff acumulado desde `c496b91` incluindo os 2 fixes de D-079 e o narrowing do item 1 desta sessão) | **REVISADO, AMBOS OS FIXES DE D-079 CONFIRMADOS, 2 ACHADOS NÃO-BLOQUEANTES NOVOS CORRIGIDOS NA MESMA SESSÃO** — ainda abaixo do gate de 9,0 por margem pequena; Codex explicitamente não pediu nova rodada ampla, só a confirmação mecânica destas 2 correções, que já foram feitas; achado residual documentado (LoginAttempt + GuestRateLimitRecord sem tenantId nem TENANT# PK, nunca roteados por esta lane) permanece pendência real, avaliação honesta de que fechar por completo exigiria redesign estrutural maior, não uma sessão de correção | `src/shared/tenant-lifecycle/tenant-business-mutation.ts`, `src/modules/import/application/import-service.ts`, `test/unit/tenant-lifecycle.test.ts` | 2026-08-29 |
103:| D-083 | W3-07 — sessão de continuação em outra máquina (2026-08-29, mesmo dia de D-082): antes de iniciar, um self-review (fora da rodada Codex) achou e corrigiu B7 — `verifyTenantDynamoPurgeEmpty` (a verificação do B2) excluía `TenantLifecycleRecord`/`IdentityMapping` de "itens restantes" só pelo atributo opcional `entityType`, a mesma fraqueza que B3 já tinha fechado no lado do DELETE; corrigido com `isNeverPurgeCanonicalKey()` em `dynamo-tenant-purge.ts`, espelhando as duas checagens de chave canônica de `PURGE_DELETE`. Essa sessão foi interrompida logo depois (commit `8b6033c "trabalho interrompido"`) sem rodar a segunda rodada Codex nem atualizar `decisions-log.md`/`NEXT_SESSION_PROMPT.md`. **Esta sessão (máquina diferente, retomando via `git pull`) rodou a segunda rodada Codex pedida por D-082 e mais 3 rodadas de correção-e-reconfirmação sobre um único achado que sobreviveu 4 rodadas em variantes sucessivas — B6 (vínculo S3↔tenant)** — via MCP `codex/codex` (não a CLI `codex exec`, disponível diretamente nesta sessão; mesmo protocolo de conteúdo do `AGENTS.md` §4, mecanismo de invocação diferente). **Rodada 2 (nota 7,4/10)**: confirmou B1-B5 e B7 corrigidos; achou B6 **não fechado de verdade** — a asserção síncrona de D-082 só comparava `target.tenantId` (rótulo do chamador) contra `input.tenantId` (outro rótulo do chamador), nunca contra o `target.prefix` real, então `{ prefix: "tenant-b/", tenantId: "tenant-a" }` ao purgar "tenant-a" passava e apagaria dados do tenant B; achado não-bloqueante adicional: o teste de B7 só exercitava `TenantLifecycleRecord`, nunca `IdentityMapping` alcançado pelo scan ampliado do B1 via `tenantId = :tenantId` (cobertura corrigida com um fake `widenedSource` novo simulando o scan real pós-B1). **Rodada 3 (nota 8,0/10)** — fix aplicado: `prefixBelongsToTenant()` exigindo o tenantId como segmento de caminho `(^|/)${tenantId}(/|$)`; achado bloqueante novo: aceitava o tenantId no FIM do prefixo sem `/` depois (`"tenant/tenant-a"` sem barra final) — S3 casa por byte-prefix, não segmento de caminho, então isso ainda purgaria `tenant/tenant-a2/...`/`tenant/tenant-a-b/...` de outro tenant. **Rodada 4 (nota 8,2/10)** — fix aplicado: exigir `/` depois do segmento (`(^|/)${tenantId}/`, nunca `$`) + `tenantId` vazio rejeitado explicitamente; achado bloqueante novo, mais profundo: um segmento batendo em QUALQUER posição do prefixo (não ancorado ao início) ainda aceitava um prefixo genuinamente enraizado sob OUTRO tenant que só continha o tenantId purgado mais adiante no caminho (ex. `"tenant/tenant-b/item/tenant-a/"` para tenantId "tenant-a"). **Rodada 5 (nota 9,1/10, ZERO achados bloqueantes)** — fix aplicado: checagem ANCORADA no início do prefixo contra uma lista fechada `TENANT_PREFIX_ROOTS = ["clean/", "tenant/", "ocr/"]`, verificada por leitura direta de cada key builder real que grava nos buckets purgados (`clean-key.ts` → `clean/<tenantId>/...`; `quarantine-key.ts`/`import-raw-key.ts`/`submission-quarantine-key.ts` → `tenant/<tenantId>/...`; `s3-ocr-artifact-store.ts` → `ocr/<tenantId>/...` — o comentário anterior de `TenantS3Target.prefix`, herdado de D-082, estava impreciso quanto à raiz real de quarantine/import). Codex confirmou por leitura própria que a lista cobre os key builders reais e que as 5 variantes anteriores (rótulo falso, colisão de substring, sem barra final, segmento não-ancorado, tenantId vazio) agora falham corretamente; parecer final explícito: "o B6 está genuinamente fechado agora... pronto para avançar à próxima etapa". **2 achados NÃO-BLOQUEANTES aceitos, não corrigidos nesta sessão**: (a) a validação ainda não acopla cada raiz a um bucket específico (ex. `{bucket: "clean", prefix: "tenant/t1/"}` passa por pertencer ao tenant, mesmo sendo uma combinação bucket/raiz que nenhum key builder real produz) — não reabre purga cross-tenant (o prefixo ainda precisa pertencer ao MESMO tenant sendo purgado), documentado no doc comment de `TenantS3Target.prefix` como responsabilidade de um futuro composition root real (constrói cada target de uma tabela fechada bucket→raiz, não aceita `{bucket,prefix}` livre) — não fixado agora porque nenhum composition root existe ainda; (b) adaptadores reais (`tenant-purge-scan.ts`/`tenant-purge-s3-adapter.ts`) continuam sem teste de integração contra AWS real — gap já conhecido desde D-081, inalterado. Cada rodada de fix rodou `npm run typecheck`+`npm run lint`+`npm test` completo no ambiente local (filesystem gravável — o sandbox read-only do Codex não conseguia rodar Vitest por `EROFS` ao gravar `vitest.config.ts.timestamp-*.mjs`, só typecheck/lint) antes de pedir a rodada seguinte: zero regressão em toda a sequência, 1076→1080→1082→1084 testes de backend (14 novos ao final: 2 de B7/cobertura do scan ampliado, 8 de B6 nas 3 variantes de bypass + tenantId vazio + caminho feliz das 3 raízes reais, mais os 4 já contados na sessão anterior que ficaram sem confirmação Codex). **Nota Claude (auto-avaliação, protocolo de nota cega não aplicável aqui — sessão de correção iterativa sobre achados do Codex, não design com nota dupla simultânea): 9,2/10** — concordo com a nota final do Codex; o mecanismo agora fecha a classe de bug (não só a instância original), verificado por leitura direta de todo key builder real, não por suposição | Type 1 (nível 5-6, `AGENTS.md` §4 — segurança/privacidade/modelo de dados, LGPD, isolamento de tenant) | 9,2/10 (auto-avaliação, ver acima) | **9,1/10** (Codex, rodada 5, sobre o estado final após 4 correções sucessivas do mesmo achado B6) | **IMPLEMENTED + UNIT TESTED + CONFIRMADO PELO CODEX ≥9,0** — B1-B7 fechados e confirmados; B6 especificamente levou 4 rodadas de correção-e-reconfirmação até fechar a classe inteira do bug, não só a instância que cada rodada testava; pipeline considerado pronto pelo Codex para a próxima etapa real (decidir orquestrador) | `src/workers/tenant-purge/dynamo-tenant-purge.ts`, `src/workers/tenant-purge/purge-tenant.ts`, `test/unit/workers/tenant-purge/dynamo-tenant-purge.test.ts`, `test/unit/workers/tenant-purge/purge-tenant.test.ts` | 2026-08-29 |
108:| D-088 | Multi-User B2B, Wave B2B-2 (chunk 2/N, conclui a wave) — entidade `User` global aditiva implementada (`GlobalUserRepository`, `PK=USER#<userId>`, `SK=PROFILE`, `entityType: "GlobalUser"` para não colidir com `UserProfile` legado que usa `entityType: "User"`), criada como 4º item na `TransactWriteItems` já existente de `TenantBootstrapService.createAll()` (docs/architecture/multi-user-b2b-physical-model.md §1) — foundation puramente aditiva, nenhum consumidor real lê esta linha ainda; Wave B2B-3 é o primeiro consumidor real (`Membership.userId` referencia esta identidade). **Achado real de teste encontrado e corrigido durante a suíte completa** (não mascarado, não incidental): `test/integration/cross-tenant.test.ts`'s checagem de crossover negativo (M1 exit criterion, implementation-blueprint.md §19/§4.3) acusou corretamente a nova chave `USER#<userId>#PROFILE` como potencial vazamento de `tenantId` para fora do prefixo `TENANT#`, porque `tenantId=userId` ainda vale sob a regra MVP vigente (a remoção real dessa igualdade só ocorre em Wave B2B-5). Corrigido com uma exceção estreita, documentada, restrita à forma exata dessa chave conhecida — não um wildcard nem enfraquecimento da checagem para nenhuma outra entidade, que continua tendo que provar isolamento estrito por `TENANT#` como antes. Dois testes pré-existentes em `test/unit/identity/resolver.test.ts` também precisaram de correção (não achado novo, consequência mecânica esperada): a colisão de SK `#PROFILE` compartilhado entre `UserProfile` legado e `GlobalUser` quebrava uma asserção de contagem que não diferenciava as duas entidades por prefixo de PK — corrigido, e a asserção ficou mais precisa (conta as duas populações separadamente) do que estava antes. | Type 3-4 (implementação direta do design D-086 §1, sem novo access pattern — reaproveita transação já fenced) | N/A — implementação direta, não decisão nova | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries` limpos (385 módulos, 0 violação), suíte completa 1108/1108 (120 arquivos), zero regressão real após a correção do achado de teste acima | `src/modules/identity/persistence/global-user-repository.ts`, `src/modules/identity/application/bootstrap-identity.ts`, `test/unit/identity/resolver.test.ts`, `test/unit/bff/bff-auth-service.test.ts`, `test/integration/cross-tenant.test.ts`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
109:| D-089 | Multi-User B2B, Wave B2B-3 (chunk 1/N) — precondição de infra do GSI4 `MembershipByUser` implementada (docs/architecture/multi-user-b2b-physical-model.md §6): `GSI4` removido de `locals.tenant_facing_index_names` em `infra/modules/dynamo-table/main.tf` (estava lá desde sempre com zero consumidor real — inofensivo até agora, mas a nova semântica de `MembershipByUser` cruza tenants por design e não pode ficar acessível pela role tenant-facing geral) e política restrita dedicada `gsi4_read` + output `gsi4_read_policy_json` adicionados, mesmo padrão de `gsi3_read`/`gsi6_read` (`AGENTS.md` §7). Nenhuma role consome `gsi4_read_policy_json` ainda — Wave B2B-3 não adiciona nenhum consumidor real (BFF/`RequestContextResolver`/onboarding, que consumirão de fato, são Wave B2B-5/B2B-6). Dois novos `run` blocks em `infra/modules/dynamo-table/tests/dynamo_table_policy.tftest.hcl` (`tenant_facing_policy_excludes_gsi4`, `gsi4_narrow_policy_references_only_its_own_index`), mesmo padrão estrutural dos runs já existentes para GSI3/GSI6. | Type 5 (`change-risk-scale.md` — muda superfície de acesso IAM de um índice, mas implementação direta do design já `APPROVED` em D-086 §6, não uma decisão nova) | N/A — implementação direta, não decisão nova | N/A | **IMPLEMENTADO** — `terraform validate` limpo (módulo + root), `terraform test` via `aws --profile claude-dev` limpo (módulo 6/6 incl. os 2 novos runs; root 15/15, zero regressão nas 27 funções Lambda existentes) | `infra/modules/dynamo-table/main.tf`, `infra/modules/dynamo-table/outputs.tf`, `infra/modules/dynamo-table/tests/dynamo_table_policy.tftest.hcl`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
112:| D-092 | Multi-User B2B, Wave B2B-4 (Onboarding) — escopo `APPROVED` via protocolo Claude↔Codex, submetido a pedido explícito do Marcelo antes de qualquer código (primeira wave desde B2B-2 a tocar potencialmente wiring de login real). Achado central: o escopo literal do roadmap §109 ("remover tenant auto-provision silencioso; new User → Create Organization ou Accept Invitation") não pode ser wireado com segurança no fluxo de login real agora — verificado contra `resolve-request-context.ts`/`bff-auth-service.ts` que nenhum gate de Membership/GSI4 existe hoje, e todo usuário legado tem tenant funcional via `TenantBootstrapService` mas zero `Membership` real, então um gate ingênuo "zero Memberships → onboarding" mostraria tela incorreta para todo usuário legado. `Accept Invitation` também fora de escopo (depende de `Invitation`, formalmente Wave B2B-8). **Escopo final**: `OnboardingStateResolver` puro, não wireado, 4 estados (`HAS_USABLE_MEMBERSHIP`/`SUSPENDED_ONLY`/`LEGACY_TENANT_ONLY`/`NO_TENANT_NO_MEMBERSHIP`) via procedimento sequencial estrito (detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b4-scope.md`) — sem wiring em `bootstrap-identity.ts`/`resolve-request-context.ts`/`bff-auth-service.ts`, sem exposição HTTP (achado convergente da Rodada 1: superfície de deploy sem consumidor real). Remoção do auto-provision legado e gate real de login redesignados para Wave B2B-5 (§110 do roadmap já define isso textualmente). 4 rodadas, nota cega cada rodada: 7,2/8,4 → 9,1/9,1 → 8,7/8,8 (achado real: `REMOVED` indevidamente agrupado com `SUSPENDED`, contradizendo a própria premissa) → 9,3/9,4 (fechamento, procedimento sequencial estrito fecha o achado da Rodada 3 e, por construção, uma ambiguidade de precedência que o autograde tinha achado independentemente). Ainda não implementado — aguardando confirmação do Marcelo do escopo final antes de codificar. | Type 3-4 por natureza (implementação de decisão de escopo, não access pattern novo), mas submetido ao protocolo completo por pedido explícito do Marcelo dado o risco de tocar login real | N/A — decisão de escopo, não decisão técnica de arquitetura nova | N/A | **ESCOPO APROVADO, IMPLEMENTAÇÃO PENDENTE** | `docs/architecture/multi-user-b2b-wave-b2b4-scope.md`, `docs/architecture/reviews/multi-user-b2b-wave-b2b4-scoping/` | 2026-08-30 |
115:| D-095 | Multi-User B2B, Wave B2B-5 (RequestContext Cutover) — escopo `APPROVED` via protocolo Claude↔Codex, submetido por iniciativa própria (analogia direta com D-092: primeira wave desde B2B-4 a tocar `resolve-request-context.ts`/`bootstrap-identity.ts`/`bff-auth-service.ts` de verdade, eliminando o fallback `tenantId=userId`). O design técnico final já estava `APPROVED` desde D-086 (`multi-user-b2b-physical-model.md` §3/§10-13) — esta rodada não reabre esse design, decide só a decomposição da implementação e fecha 3 lacunas reais achadas por leitura direta do código (não do design doc): (1) não existe canal de transporte entre `activeOrganizationId` da sessão BFF e o `RequestContextResolver` do lado do recurso (`proxy-service.ts` só encaminha o access token Cognito bruto); (2) `Membership.role` tem 4 valores mas a matriz de autorização real em produção (`authorization.ts`) só conhece 3, sem `ADMIN`; (3) sem um primeiro consumidor HTTP para `CreateOrganizationService`, `bootstrapUser()` estado final deixaria todo login novo preso em `NO_TENANT_NO_MEMBERSHIP` sem saída. **Escopo final** (detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b5-scope.md`): `IdentityMapping` tenantless + `bootstrapUser()` de 2 itens (renomeia `TenantBootstrapService`→`IdentityBootstrapService`, remove `UserProfile` legado); `DeviceSession` migra para `PK=USER#<userId>` (inatingível por W3-07/purge por construção de chave); `RequestContextResolver` reescrito com resolução de Membership única nomeada explicitamente (falha fechado se >1 `ACTIVE`, nunca "pega a primeira") e assert explícito (erro nomeado, testado) para `Membership.role` não suportado (`ADMIN`), em vez do fail-closed acidental via cast unsafe; `POST /bff/organizations` com **cap transacional** (`GlobalUser.hasCreatedOrganization` via `Update` condicionado na MESMA `TransactWriteItems` de `CreateOrganizationService`, 5 itens não 4 — `CreateOrganizationService` ganha `buildCreateEntries()` aditivo, sem mudar seu contrato para chamadores existentes); `Session.activeOrganizationId` como cache/hint de UX nunca usado para autorização, com self-heal em `GET /bff/session`/`handleCallback` quando a sessão está desatualizada mas uma Membership real já existe. `POST /bff/organization/select` e o transporte real de `activeOrganizationId` até o recurso **não nascem nesta wave** — desvio faseado explícito de §11/§12 do physical model, registrado (não alegado como cumprimento literal), fechado por Wave B2B-6 quando B2B-8 tornar multi-org alcançável. 3 rodadas, nota cega cada rodada: 7,8/7,8 → 8,7/8,6 → 9,1/9,1 (fechamento, ambos ≥9,0, sem arredondar — a única rodada deste projeto em que a nota da Rodada 1 saiu empatada entre os dois lados). Achados de implementação do Codex (builder tenantless para o `Update` do cap, distinguir `CancellationReasons` por índice, teste de fixture pré-cutover sem o atributo novo) registrados no documento de escopo para não se perderem até a implementação real. Ainda não implementado. | Type 3-4 por natureza (decisão de decomposição/escopo de implementação, não access pattern novo além do já aprovado em D-086), submetido ao protocolo completo por iniciativa própria dado o risco de tocar login real | N/A — decisão de escopo, não decisão técnica de arquitetura nova | N/A | **IMPLEMENTADO — ver D-096** (Marcelo deu o sinal para implementar após a apresentação do escopo; `UserProfile` NÃO foi removido como este escopo original previa — achado real corrigido durante a implementação, ver D-096) | `docs/architecture/multi-user-b2b-wave-b2b5-scope.md`, `docs/architecture/reviews/multi-user-b2b-wave-b2b5-scoping/` | 2026-08-30 |
123:| D-105 | Multi-User B2B, Wave B2B-10 (Tenant-aware Frontend) — escopo `APPROVED` via protocolo Claude↔Codex, 3 rodadas. Primeira aplicação do protocolo a uma decisão de FRONTEND nesta sessão (B2B-6/7/8/9 foram todas backend). Classificação por subitem, não nota única para a wave: a maior parte é implementação direta de contratos já `APPROVED` (nível 3-4, protocolo não obrigatório), só a arquitetura de isolamento de cache é Type 1/nível 5 (consequência de segurança real: vazamento de dado entre tenants na UI se malfeita). **Declaração E-014 SIM** para essa peça: "query keys escopadas por tenant" é padrão documentado pela própria TanStack Query v5 (já em uso), confirmado por fetch direto da doc oficial. **Achado real #1** (verificado lendo os 2 lados do contrato): `frontend/src/auth/AuthContext.tsx`'s `probe()` exigia `info.tenantId && info.userId`, campos que `GET /bff/session` nunca retorna desde B2B-5/D-095 — todo usuário autenticado era tratado como `SESSION_MISSING`, regressão real já em `develop`/`main`, nunca antes reconciliada porque nenhuma wave B2B tinha tocado o frontend. **Achado real #2**: nenhum serviço de B2B-8 atualiza `Organization.displayName`/`timezone` — writer novo necessário. **Rodada 1 (Claude 8,4/Codex 8,1, não aprovado)**: achado bloqueante — a proposta assumia que TanStack Query refaz fetch automaticamente ao mudar `organizationId` na queryKey, mas essa variável não é o que o `queryFn` real usa (o BFF injeta `X-Organization-Id` a partir da sessão server-side, o browser nunca a envia) — corrida real na janela entre `POST /select` e o refetch de sessão do cliente; inventário de query keys também incompleto (`Overview.tsx` usava uma chave inline nunca listada). **Rodada 2 (Claude 9,0/Codex 8,7)**: corrida fechada com gate `switching` + `cancelQueries` explícito + inventário refeito por grep exaustivo (7 `useQuery` + 5 `invalidateQueries`); achado bloqueante novo — `useActiveOrganization()` como hook reimplementado por chamador (não Context único) desincronizaria `switching` entre instâncias. **Rodada 3 (Claude 9,2/Codex 9,2, fechamento)**: `ActiveOrganizationProvider` (Context único) especificado + achado bloqueante final — `cancelQueries()` sozinho não aborta um `fetch()` real sem o `AbortSignal` do `QueryFunctionContext` propagado ponta-a-ponta pelos wrappers de API (`api/items.ts`/`api/subjects.ts` não aceitavam nem repassavam `signal` algum); corrigido, ambos ≥9,0 sem arredondar. | Type 1 (nível 5 de `change-risk-scale.md` só para a arquitetura de cache isolation — consequência de segurança/isolamento de dado; restante da wave nível 3-4) | 9,2/10 (Rodada 3) | 9,2/10 (Rodada 3) | **ESCOPO APROVADO — ver D-106 para implementação** | `docs/architecture/reviews/multi-user-b2b-wave-b2b10-scoping/` | 2026-08-30 |
126:| D-110 | Multi-User B2B, Wave B2B-12 (Cutover de dev) — escopo `APPROVED` via protocolo Claude↔Codex, 3 rodadas. Escopo per `roadmap-evolution/17` §62-65/§117: reset/reseed de `dev` (dado sintético) vs. migração one-shot, e fechar "nenhum código produtivo entende tenantId=userId". **Declaração E-014 NÃO**: a política reset-vs-migração já foi decidida e aprovada via protocolo dentro do próprio `roadmap-evolution/17` §62-63 — esta wave aplica essa política já aprovada contra uma classificação real de `dev`, não a redecide a partir de pesquisa de mercado (Codex concordou na Rodada 1, com a ressalva de que isso não dispensa rigor de engenharia básico). **5 achados reais, verificados por leitura de código E por inventário real via `aws --profile claude-dev`**: (1) `exptrk-dev-table` (47 itens) confirma dado 100% sintético/descartável, zero `Organization`/`Membership`, só 3 `tenantId` distintos (2 drills W2, 1 smoke-test pré-cutover); `exptrk-dev-bff-session` 0 itens; Cognito 0 usuários reais; S3 só `extraction-transient` com conteúdo (21 objetos); SQS 22/24 filas vazias, 2 com mensagens reais (`upload-finalizer-dlq`: 3, `malware-result-dlq`: 1 not-visible); (2) `IdentityMapping` real de `dev` ainda carrega o atributo `tenantId` (removido do TYPE desde B2B-5, mas DynamoDB não impõe schema) — prova concreta de que `dev` nunca foi resetado desde antes do cutover; (3) `OnboardingStateResolver.resolve()` (`onboarding-state.ts:50-53`) ainda classifica `LEGACY_TENANT_ONLY` ao encontrar uma `TenantLifecycleRecord(userId)` legada, wired via `resolve-request-context.ts`/`bff-auth-service.ts`, inalcançável contra o dado real de `dev` hoje mas ainda existe no código; (4) nenhum código produtivo grava registro novo no formato legado (fallback de B2B-11, `tenantId` de `IdentityMapping` de B2B-5, `bootstrapUser()` de B2B-5 — todos já confirmados fechados); (5) nenhuma ferramenta de reset/migração existe ainda (`backfill-reminder-policies.ts` é o único precedente de script one-off). **Rodada 1 (Codex 7,4/10)**: 7 achados — faltava snapshot/export antes do delete destrutivo; `BatchWriteItem` subespecificado (limite 25/retry `UnprocessedItems`/checkpoint) + correção factual (`backfill-reminder-policies.ts` não tem dry-run default, é opt-in); `--table` nomeado sozinho não protege contra erro de operador (conta AWS compartilhada com outros projetos); Cognito fora do inventário; `exptrk-dev-bff-session` tratado como "0 itens, sem ação" (deveria ser Scan+delete incondicional); filas/DLQs não inventariadas; remoção de `LEGACY_TENANT_ONLY` subestimava o contrato frontend (`frontend/src/api/session.ts`). **Rodada 2 (Claude 9,0/Codex 8,3)**: os 7 corrigidos (2 fases sequenciais fail-closed; `retryWithBackoff` especificado; allowlist de 2 nomes + `sts:GetCallerIdentity`; Cognito sempre listado, delete gated por `--include-cognito`; `bff-session` incondicional; `PurgeQueue` nas 24 filas; escopo estendido a frontend+teste) — achado bloqueante novo: snapshot bruto em `docs/architecture/reviews/` arriscava commitar PII/segredo real (`csrfSecret`/`encryptedRefreshToken`/`accessToken`/`emailNormalized`/`identitySubject`); citava reutilizar uma função de backoff em `occ.ts` que não existe; purge de filas sem quiescência explícita contra schedules/consumers reais ativos. **Rodada 3 (Claude 9,2/Codex 9,1, fechamento)**: snapshot bruto movido para `.local-artifacts/dev-reset/` (`.gitignore`ado, nunca commitado), só manifest redigido (contagens/hash, nunca valor sensível) no repositório; backoff local ao próprio script, confirmado que nenhum lugar do projeto tem helper reaproveitável; verificação final fail-loud (segunda leitura completa) como alternativa proporcional a automação de quiescência. | Type 1 (nível 5 de `change-risk-scale.md` — operação destrutiva contra recursos AWS reais + muda o contrato observável de `OnboardingStateResolver`/`GET /bff/session`; não nível 6, não introduz modelo de dado fundamental novo) | 9,2/10 (Rodada 3) | 9,1/10 (Rodada 3) | **ESCOPO APROVADO — execução real (`--confirm`/`--include-cognito`/`s3 rm` reais) gated por confirmação explícita do Marcelo, registrada como pendente; ver wave-tracker para implementação** | `docs/architecture/reviews/multi-user-b2b-wave-b2b12-scoping/`, `docs/architecture/multi-user-b2b-wave-b2b12-scope.md` | 2026-08-30 |
127:| D-111 | Multi-User B2B, Wave B2B-12 (Cutover de dev) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-110. `scripts/reset-dev-data.ts` novo: Fase A (sempre, read-only) inventaria `exptrk-dev-table`/`exptrk-dev-bff-session`/bucket `extraction-transient`/user pool Cognito/24 filas SQS, grava snapshot bruto em `.local-artifacts/dev-reset/<timestamp>/` (`.gitignore`ado, novo) e manifest redigido (contagens/`entityType`s/hash SHA-256 por item, nunca valor sensível) em `docs/architecture/reviews/multi-user-b2b-wave-b2b12-scoping/`; Fase B (só `--confirm`) deleta em lotes de 25 via `BatchWriteItem` com `deleteBatchWithRetry()` local (backoff exponencial+jitter, sem módulo novo em `shared/` — nenhum outro call site real precisava), `PurgeQueue` nativo nas 24 filas, delete de Cognito só com `--include-cognito` adicional, termina com segunda leitura completa (`assertAllEmpty()`, fail-loud se qualquer contagem não for zero). Nomes de tabela/sessão/bucket validados contra allowlist hardcoded (`assertAllowedTable`), conta AWS confirmada via `sts:GetCallerIdentity` (`assertExpectedAccount`) antes de qualquer operação — nunca parâmetro livre, esta conta hospeda outros projetos não relacionados. `OnboardingStateResolver`/`onboarding-state.ts`: `LEGACY_TENANT_ONLY` removido do union e do procedimento sequencial (colapsa direto em `NO_TENANT_NO_MEMBERSHIP`), comentário desatualizado corrigido; `frontend/src/api/session.ts`'s `OnboardingState` (mesmo union) e `test/unit/organization/onboarding-state.test.ts` (2 casos de teste do estado removido, 1 caso reescrito para a nova expectativa) atualizados em conjunto — confirmado seguro por leitura que os 2 call sites reais (`resolve-request-context.ts`, `bff-auth-service.ts`) comparam só contra `"HAS_USABLE_MEMBERSHIP"`, nunca switch exaustivo. **Verificação real, não só `--dry-run` teórico**: rodado de fato contra `dev` via `aws --profile claude-dev` sem `--confirm` (Fase A, leitura pura) — confirmou 47 itens/0 sessões/21 objetos S3/0 usuários Cognito/24 filas (1 com 3 mensagens reais, `upload-finalizer-dlq`), manifest gravado com sucesso, script parou antes da Fase B exatamente como projetado. G-V3 aplicado e verificado por mutação real em 3 pontos: `deleteBatchWithRetry` (retentar a lista original em vez de só o `UnprocessedItems`, teste pega), `hashItem` (remover a ordenação de chaves antes de serializar, teste pega), `parseArgs`'s allowlist de `--table` (remover a chamada de `assertAllowedTable`, teste pega) — os 3 restaurados e reverificados. Novas dependências: `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` (mesmas versões pinadas dos demais clients AWS SDK v3 do projeto). **Execução real (`--confirm`/`--include-cognito` reais) permanece pendente de confirmação explícita do Marcelo** — registrado, não bloqueia o fechamento desta wave (design+implementação+verificação de leitura já completos). | Type 1 (nível 5 de `change-risk-scale.md` — operação destrutiva contra recursos AWS reais + muda contrato observável de `OnboardingStateResolver`/`GET /bff/session`) | N/A — implementação direta, protocolo já rodado em D-110 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa backend `npm test` 1232/1232 (132 arquivos, +22 líquidos vs. B2B-11: +23 `reset-dev-data.test.ts`, -1 `onboarding-state.test.ts`), zero regressão; frontend `vitest run` 139/139 + `playwright test` 24/24, zero regressão; `npm run build:lambdas` PASS (34/34, nenhum handler novo). Com este item, **Wave B2B-12 está `DONE`** (execução real destrutiva à parte, gated) | `scripts/reset-dev-data.ts`, `test/unit/scripts/reset-dev-data.test.ts`, `src/modules/organization/application/onboarding-state.ts`, `test/unit/organization/onboarding-state.test.ts`, `frontend/src/api/session.ts`, `.gitignore`, `package.json`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
129:| D-113 | Multi-User B2B, Wave B2B-13 (E2E/Adversarial Security) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-112. **Fix real (Achados #3/#4, Q17/Q23)**: `ResolvedRecipient` (`notification/ports/recipient-resolver.ts`) ganha `email?: string` aditivo; `DynamoDbNotificationRecipientResolver.resolve()` (`notification/persistence/`) passa a devolver `email: globalUser?.emailNormalized` na mesma leitura que já fazia para `active`. `notification.ts`'s `resolveRecipientEmail` e `subject.ts`'s `resolveInternalUserEmail` (`runtime/aws/composition/`) passam a instanciar essa classe já testada em vez de ler `GlobalUser` sozinhos — fecha o TOCTOU real entre roteamento (que já checava `Membership`+`GlobalUser`) e entrega assíncrona (que não checava). Comentário no port documenta que este resolver e `MemberEligibilityChecker` (`expiration/ports/`) usam a MESMA regra, formas diferentes por design (email vs. boolean), nunca fundidos. **Testes novos (Achados #1/#2, Q6/Q7)**: `test/unit/identity/resolver.test.ts` ganha 2 testes — revogação real via `RemoveMembershipService` encadeada com `resolve()` subsequente (achado durante a escrita: o teste original esperava `OrganizationUnavailableError`, mas o `OnboardingStateResolver` roda primeiro e lança `OnboardingRequiredError` quando a Membership removida é a ÚNICA do usuário — corrigido dando ao membro uma segunda Organization própria, isolando a asserção à revogação escopada, não ao gate de onboarding); roles genuinamente diferentes por Organization (MEMBER numa, OWNER noutra, mesmo usuário) nunca vazam em `authorize()`. **Teste do fix (Achado do Codex, Rodada 3 DoD)**: `document-chasing-dispatch.test.ts`'s teste existente de "sem e-mail resolvível" renomeado/documentado para cobrir explicitamente também o cenário de revogação mid-flight (mesmo código de saída, uma so teste — duplicar seria redundante, a regra de 2 condições em si já é provada por `dynamodb-recipient-resolver.test.ts`, estendido com um caso novo para o campo `email`). **Documentação (Q16)**: comentário em `document-service.ts` citando §47/§48 — nenhum código novo, o contrato de admissão já é integralmente coberto por outros testes (revogação de Membership → `resolver.test.ts`; Organization DELETING → fencing já existente). **Auditoria (Achado #5, Q25)**: busca sistemática por 10 palavras-chave em toda a suíte confirma zero coincidência de fixture IDs entre papéis adversariais — nenhuma correção necessária, auditoria registrada como evidência. G-V3 verificado por mutação real em 4 pontos: `email` do resolver (remover a leitura, teste pega), revogação de Membership (`resolve-working-organization.ts`, remover o check de status, teste pega), vazamento de role (`resolve-request-context.ts`, hardcodar OWNER, teste pega) — todos restaurados e reverificados. | Type 1 (nível 5, fix real de autorização-adjacente no pipeline de entrega assíncrona) | N/A — implementação direta, protocolo já rodado em D-112 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa backend `npm test` 1235/1235 (132 arquivos, +3 líquidos vs. B2B-12), zero regressão, `npm run build:lambdas` PASS (34/34, nenhum handler novo). Com este item, **Wave B2B-13 está `DONE`** | `src/modules/notification/{ports,persistence}/*.ts`, `src/runtime/aws/composition/{notification,subject}.ts`, `src/modules/document/application/document-service.ts`, `test/unit/{identity/resolver,notification/dynamodb-recipient-resolver,subject/document-chasing-dispatch}.test.ts`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
133:| D-117 | Multi-User B2B, Wave B2B-14 (Operational Evidence) — 4º achado real severo, encontrado ao continuar a verificação manual do Marcelo (barra lateral renderiza, nenhuma tela mostra conteúdo, `Settings` mostra "Erro inesperado do servidor"). Causa raiz real: `curl .../bff/organizations` retornou `404 {"message":"Not Found"}` — API Gateway nunca invocou o Lambda (zero logs correspondentes). `infra/modules/bff-api-gateway/main.tf`'s `bff_routes` nunca teve entradas para `GET /bff/organizations`/`POST /bff/organization/select`, apesar de `handleListOrganizations`/`handleSelectOrganization` (`bff-handlers.ts`) e seu dispatch em `bff-handler.ts:81-82` existirem desde Wave B2B-6/D-102 — só `organizations_create` (POST) foi wireado naquela wave, os outros 2 irmãos ficaram esquecidos. `frontend/src/hooks/useOrganizationsList.ts`'s `GET /bff/organizations` (usado por `Settings.tsx`/`OrganizationSwitcher`/toda tela que depende da lista de Organizations) nunca funcionou contra `dev` — explica tanto o erro explícito do Settings quanto as outras telas "sem conteúdo" (silenciosamente sem dado, sem gate de erro visível). Fix: as 2 rotas adicionadas ao mapa `bff_routes`. **Achado adicional sobre a própria suíte de testes**: `infra/modules/bff-api-gateway/tests/bff_api_gateway.tftest.hcl` já tinha um teste de existência dedicado para `organizations_create` (B2B-6) mas nunca para os 2 irmãos — exatamente por isso a suíte nunca pegou o gap; corrigido (contagem de rotas 7→9 + novo run `organizations_list_and_select_routes_exist_for_multi_org_to_work_at_all`). `terraform validate`/`terraform test` (módulo 6/6, raiz 16/16) PASS. | Type 3 (correção mecânica de uma wiring que já deveria existir desde B2B-6 — mesmo padrão já usado por `organizations_create`, não uma decisão nova) | N/A — correção de bug real, não decisão de design | N/A | **IMPLEMENTADO (config + teste) — aplicação real via CD após merge; login real via Marcelo (browser) ainda em andamento** | `infra/modules/bff-api-gateway/main.tf`, `infra/modules/bff-api-gateway/tests/bff_api_gateway.tftest.hcl` | 2026-08-30 |
136:| D-108 | Multi-User B2B, Wave B2B-11 (Responsibility + Notifications) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-107. `recipient-resolver.ts`: `resolveCandidateUserId({assigneeUserId})` (`tenantId` removido dos parâmetros, fallback `""`, tipo de retorno inalterado). `dynamodb-recipient-resolver.ts`: migrado para `Membership`+`GlobalUser`, 2 condições (`Membership.status==="ACTIVE" && GlobalUser.identityStatus==="ACTIVE"`), preservando `RECIPIENT_NOT_FOUND` (Membership nunca existiu) vs. `RECIPIENT_NOT_ELIGIBLE` (existe mas inativa/suspensa). `notification.ts` (composition root): `resolveRecipientEmail` migrado para `GlobalUser` (`globalUserKey`, tenantless). Port novo `expiration/ports/member-eligibility.ts` (`MemberEligibilityChecker`), implementado no composition root (`expiration.ts`) contra `Membership`+`GlobalUser` diretamente (mesmas 2 condições) — porta estreita no módulo consumidor, per sugestão do Codex, não um import direto do modelo físico de `organization`. `ItemWatchService.addWatcher` valida elegibilidade antes de gravar (`removeWatcher` deliberadamente não valida — remover/limpar é sempre seguro). `ExpirationService.createItem`/`updateItem` validam `assigneeUserId` quando fornecido/alterado (nunca em `renewItem`, que só copia um valor já validado). `IneligibleAssigneeError` novo (`app-error.ts`, categoria NOT_FOUND/404 deliberada, nunca distinguir por status HTTP "existe mas não elegível" de "não existe"). `UsableOrganization`/`resolve-active-membership.ts` inalterados nesta wave. **Achado real corrigido durante a implementação** (mesma honestidade de processo das waves anteriores): o teste `notification-router-workflow.test.ts`'s cenário "happy path" dependia implicitamente do fallback quebrado (`makeItem()` sem `assigneeUserId`, `FakeRecipientResolver` com resultado canned `userId: TENANT`) — corrigido dando a `makeItem()` um `assigneeUserId` real por padrão, com um novo teste dedicado provando que "sem assignee" cancela ANTES de sequer chamar o resolver (`candidateWasEmpty`), não depois de uma resolução bem-sucedida por acidente. G-V3 aplicado e verificado por mutação real em 2 pontos: `notification-router-workflow.test.ts` (reintroduzindo um fallback não-vazio, o novo teste falha), `dynamodb-recipient-resolver.test.ts` (removendo a checagem de `GlobalUser.identityStatus`, o teste de suspensão global falha) — ambos restaurados e reverificados. | Type 1 (nível 5, autoridade de validação de acesso no pipeline de notificação/responsabilidade) | N/A — implementação direta, protocolo já rodado em D-107 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos (415 módulos, 0 violação), suíte completa `npm test` 1210/1210 (131 arquivos, +13 líquidos vs. B2B-10), zero regressão, `npm run build:lambdas` PASS (34/34, nenhum handler novo, nenhuma mudança de infra). Com este item, **Wave B2B-11 está `DONE`** | `src/modules/notification/{ports,persistence,application}/*.ts`, `src/modules/expiration/{ports,application}/*.ts`, `src/shared/errors/app-error.ts`, `src/runtime/aws/composition/{expiration,notification}.ts`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
137:| D-120 | Multi-User B2B, Wave B2B-14 (Operational Evidence) — Marcelo confirmou o save de Settings funcionando pós-D-119 e continuou o roteiro manual, perguntando sobre herança de responsabilidade e exportação de dados (registrados como lacunas de produto, não corrigidos nesta entrada) e pediu para testar convite/aceite/sair-da-organização com autonomia máxima. Investigação encontrou 3 lacunas reais adicionais que bloqueavam o roteiro por completo, nenhuma hipotética: **(1) aceitar convite nunca teve tela no frontend** (`handleAcceptInvitation`/`POST /bff/invitations/accept` existem desde B2B-8/D-099, nunca chamados); **(2) essa MESMA rota nunca foi wireada no API Gateway** (`infra/modules/bff-api-gateway/main.tf`'s `bff_routes`, mesma classe de bug do D-117 — 404 genérico, nunca alcançava o Lambda); **(3) e-mail real de convite nunca foi ativado** (`CreateInvitationService.emailProvider`/`invitationBaseUrl` sempre `undefined` desde B2B-8 — mecanismo pronto, kill switch nunca ligado, D-100 já documentava isso como gap aberto) — e a conta SES do `dev` estava em sandbox com ZERO identity verificada (nem sequer o remetente `ses_from_address`, ainda no placeholder `noreply@example.com` desde M4 — o que significa que TODO fluxo dependente de SES real, não só convite, nunca poderia ter enviado e-mail de verdade neste ambiente). **Decisão consultada com o Marcelo** (achado #3 toca uma proteção de segurança já debatida no protocolo em B2B-8 — `create-invitation.ts`'s handler nunca devolve o token bruto na resposta HTTP, "canal de prova de posse é só o e-mail"): 2 opções apresentadas (A: ativar o e-mail real de verdade, exige verificar manualmente 2 endereços SES; B: atalho que reverteria a decisão de segurança). Marcelo escolheu A e forneceu os 2 e-mails reais (`marcelo.mjgoncalves@gmail.com` como remetente, `tchelojg@gmail.com` como segunda conta de teste — o mesmo endereço já usado como `alert_email` desde M5); ambos verificados com sucesso via `aws sesv2 create-email-identity`/confirmação manual do link. Fix: `AcceptInvitation.tsx` (novo, sibling do grupo `ProtectedRoute` principal — nunca aninhado em `ActiveOrganizationProvider`/`OnboardingGate`, um convidado pode ter zero Memberships) + `POST /bff/invitations/accept` wireado em `bff_routes`; `buildMembershipDeps`/`memberships-handler.ts` ganham `membershipInviteEmailEnabled`/`sesFromAddress`/`sesConfigurationSet`/`invitationBaseUrl` (mesmo padrão de `buildDocumentRequestDeps`, D-049, reaproveita o MESMO `SesEmailAdapter` já usado por `EmailDeliveryWorker`/`DocumentChasingDispatch`); `infra/main.tf` ganha `membership_invite_email_enabled` (kill switch, default `false`, ligado só em `dev.tfvars`) + IAM `ses_send_email` + env vars no `memberships_handler`; `ses_from_address` corrigido para o endereço real verificado (beneficia TODOS os fluxos SES do ambiente, não só convite). **2 achados adicionais, menores, encontrados na mesma varredura**: `handleListMembers` devolvia o item bruto do DynamoDB (`PK`/`SK`/`GSI4PK`/`GSI4SK`/`entityType`) em vez de um subconjunto seguro, ao contrário do irmão `handleListInvitations` que já projeta explicitamente — corrigido projetando `{userId, role, status, version}` (confirmado, por leitura do frontend, que `Members.tsx` já só usava exatamente esses 4 campos — zero impacto funcional, achado só de higiene de API); `handleLeaveOrganization`/`POST /organizations/members/leave` estava fio-a-fio funcional (Lambda+rota+allowlist) desde B2B-8 mas sem NENHUM botão no frontend — `LeaveOrganizationSection` novo em `Settings.tsx` (visível a qualquer role, backend é a única autoridade real sobre quando sair é permitido), com mensagem amigável dedicada para `LastOwnerError` (`isLastOwnerError`, novo helper — `BUSINESS_RULE` também precisou ser adicionado a `BackendErrorCategory`, ausente do frontend até agora apesar de existir no backend desde B2B-8). **Verificação ao vivo, não só suíte de teste**: usando o cookie de sessão real do Marcelo (`__Host-et_session`/`__Host-et_csrf`, repassados por ele), confirmado por chamada direta: `GET /bff/session` (sessão válida), `GET /bff/organizations` (organização real, `version: 2` refletindo o save do D-119), `GET /bff/api/organizations/members` (achado do leak, membro único ACTIVE/OWNER) e `POST /bff/organizations` corretamente bloqueado por `CONFLICT`/"You have already created an organization" (cap de uma organização por `GlobalUser`, D-096 — confirma que troca de organização só é testável depois que convite/aceite funcionarem de verdade, sem atalho possível). **Decidido não testar `leave` com a única conta real do Marcelo agora** — um bug no guard de último-owner combinado com o cap de uma-organização-por-usuário o deixaria sem organização e sem conseguir criar outra; fica para depois que a segunda conta existir. G-V3 verificado por mutação real: `isLastOwnerError` desativado no branch da mensagem amigável, teste dedicado falhou como esperado, restaurado e reverificado. | Type 3 (ativação de um kill switch e wiring de rota já aprovados desde B2B-8/D-049/D-099 — sem decisão de arquitetura nova; a única questão que tocava segurança foi consultada e decidida pelo Marcelo diretamente, não decidida sozinho) | N/A — implementação direta de mecanismo já aprovado, consulta pontual ao Marcelo para a única bifurcação de segurança | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa backend `npm test` 1235/1235, frontend `vitest` 152/152 (+10 novos: 5 `AcceptInvitation`, 2 `Settings` leave-org, 3 líquidos de ajuste) + `playwright` 24/24 zero regressão, `terraform validate`/`test` (raiz 16/16, módulo bff-api-gateway 7/7) PASS, 2 endereços SES verificados de fato (`VerificationStatus: SUCCESS` confirmado via `aws sesv2 get-email-identity`). Aplicação real via CD após merge; teste de convite/aceite/troca-de-organização com a segunda conta ainda pendente (aguarda Marcelo repassar o cookie da segunda sessão) | `frontend/src/{routes/AcceptInvitation,routes/Settings,hooks/useAcceptInvitation,hooks/useLeaveOrganization,api/organizations,api/members,api/errors,App}.tsx`, `src/runtime/aws/composition/organization.ts`, `src/runtime/aws/handlers/memberships-handler.ts`, `src/modules/organization/http/membership-handlers.ts`, `infra/{main,variables}.tf`, `infra/env/dev.tfvars`, `infra/modules/bff-api-gateway/main.tf`, `infra/modules/bff-api-gateway/tests/bff_api_gateway.tftest.hcl` | 2026-08-30 |
143:| D-096 | Multi-User B2B, Wave B2B-5 (RequestContext Cutover) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-095, com 2 achados reais corrigidos durante a implementação (registrados por honestidade de processo, nenhum reabriu o protocolo): (1) **`UserProfile` não é legado descartável** — D-095 previa "remove UserProfile legado", mas verificação contra o código real antes de escrever mostrou que `profile-service.ts`/`profile-handlers.ts`/guest flows/`document-chasing-dispatch.ts`/`email-templates.ts` ainda consomem `UserProfile.requesterDisplayName` (W5-01/GTR-01) — `UserProfile` mantido, seu provisionamento movido do bootstrap (que não conhece mais nenhuma Organization) para `RequestContextResolver` (lazy, por Organization, preservando o invariante que `ProfileService.readOwnProfile()` já documentava depender); supersessão real por `Organization.displayName` continua Wave B2B-11. (2) **Gate de `TenantLifecycleRecord(organizationId)` ACTIVE estava faltando** na primeira versão do resolver reescrito — o design (§11) exige que a cadeia termine checando o lifecycle da própria Organization, não só da Membership; achado por revisão própria antes de rodar os testes, corrigido e coberto por teste dedicado. **Implementação real**: `IdentityMapping` tenantless; `IdentityBootstrapService.bootstrapUser()` com `TransactWriteItems` de 2 itens (`GlobalUser`+`IdentityMapping`); `DeviceSession`+`logoutAll`/`logoutDevice` migrados para `GlobalUserRepository` (`PK=USER#<userId>`, `globalLogoutAfter` também migrado de `UserProfile` para `GlobalUser`); `RequestContextResolver` reescrito com resolução de Membership única explícita (`resolve-active-membership.ts`, helper compartilhado com a BFF, não reabre o contrato de `OnboardingStateResolver`), gate de lifecycle da Organization, e assert nomeado (`UnsupportedMembershipRoleError`) para `Membership.role` não suportado; `POST /bff/organizations` novo (primeiro consumidor HTTP de `CreateOrganizationService`) com cap transacional real (`GlobalUser.hasCreatedOrganization` via `buildAttributeOnceUpdate`, novo builder tenantless em `occ.ts`, na MESMA `TransactWriteItems` de `CreateOrganizationService.buildCreateEntries()` — refatoração aditiva; `TransactionCanceledException` distinguido por índice via `getCancellationReasonCodes()`, também novo em `occ.ts`, para não confundir a falha do cap com a falha de qualquer outro dos 4 Puts); sessão BFF identity-only (`Session.tenantId`→`activeOrganizationId?`) com self-heal em `GET /bff/session` (`resolveSessionWithOnboarding`); nova rota Terraform `POST /bff/organizations`. `POST /bff/organization/select` e o transporte real de `activeOrganizationId` até o recurso continuam fora desta wave, exatamente como D-095 registrou (fechados por Wave B2B-6). Com este item, **Wave B2B-5 está `DONE`**. | Type 5-6 (elimina o auto-provision de tenant no login real, o gate central da wave — implementação direta do escopo já `APPROVED` em D-095 via protocolo completo, sem access pattern novo além do já aprovado) | N/A — implementação direta, protocolo já rodado em D-095 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries` limpos (393 módulos, 0 violação), suíte-alvo (`test/unit/identity/`, `test/unit/bff/`, `test/unit/organization/`, `test/integration/`) 194/194 PASS com G-V3 aplicado desde a escrita, `terraform validate`/`terraform test` (via `AWS_PROFILE=claude-dev`) 5/5 PASS incl. a nova rota, `npm run build:lambdas` 27/27 PASS, suíte completa `npm test` 1134/1134 (123 arquivos), zero regressão | `src/modules/identity/**`, `src/modules/bff/**`, `src/modules/organization/application/{create-organization,resolve-active-membership}.ts`, `src/shared/dynamodb/occ.ts`, `src/shared/errors/app-error.ts`, `infra/modules/bff-api-gateway/`, `src/runtime/aws/{composition,handlers}/**`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
144:| D-125 | Responsibility Reassignment on Member Removal — implementação real do mecanismo APROVADO em D-122 (Claude 9,1/Codex 9,1, 3 rodadas, `docs/architecture/reviews/responsibility-reassignment-scoping/`), mesmo padrão de D-096/D-102/D-108/D-113/D-124 (implementação direta de design já aprovado, nenhuma rodada Claude↔Codex nova para o mecanismo). **Precondição best-effort, explicitamente NÃO atômica**, em `RemoveMembershipService.remove()`/`LeaveOrganizationService.leave()`: antes da transação de remoção, checa se `targetUserId` ainda é `assigneeUserId` de algum `ExpirationItem` `ACTIVE` na organização e lança `ResponsibilityReassignmentRequiredError` em vez de prosseguir — uma leitura síncrona de decisão, nunca parte do `TransactWriteItems` (Round-3 "Estado final consolidado"). **Nova porta `AssignedActiveItemsLookup`** (`organization/ports/assigned-active-items-lookup.ts`, nome/forma do Round-3 — supersede o `SoleResponsibilityChecker` do Round-1) na CONSUMING module, implementada no composition root (`runtime/aws/composition/organization.ts`'s `buildAssignedActiveItemsLookup`, exportada para teste direto) — mesmo padrão estrutural de `expiration/ports/member-eligibility.ts`, mas na direção OPOSTA (primeira vez que `organization` lê dado de `expiration`). **Contrato de paginação da Correção 2 do Round-3, implementado literalmente**: pagina o GSI1 `TENANT#t#ITEMSTATUS#ACTIVE` até esgotar (`LastEvaluatedKey` até `undefined`), aplica `FilterExpression: assigneeUserId = :userId` a cada página, NUNCA usa `Limit` do DynamoDB como proxy de truncamento — `totalKnown` é a contagem real de todas as páginas, o teto de 20 aplica só à lista `itemIds` retornada, `truncated` é `totalKnown > itemIds.length`. GSI1 não está na taxonomia restrita de `security-audit.ts` (só GSI3/GSI6 exigem `security.global_index_access`) — nenhum evento novo de auditoria necessário. **Erro novo** `ResponsibilityReassignmentRequiredError` (`app-error.ts`, `BUSINESS_RULE`/`RESPONSIBILITY_REASSIGNMENT_REQUIRED`, `retryable: false`, `details: {targetUserId, itemIds (até 20), totalKnown, truncated}`), mapeado para 422 pelo `STATUS_BY_CATEGORY` genérico já existente (nenhuma mudança de handler necessária). **Ordem de checagem** em `remove-membership.ts`: autorização → tier OWNER (`OwnerTierChangeRequiresOwnerError`) → reatribuição de responsabilidade (novo) → transação (que ainda aplica `LastOwnerError` atomicamente) — a checagem de tier vence porque decide SE o caller pode agir, antes de qualquer pergunta sobre o estado do alvo; documentado inline com teste dedicado provando a ordem quando ambos os erros se aplicariam. **Escopo respeitado integralmente**: `ItemWatch`/watchers intocados (precedente B2B-11 mantido), nenhum bypass de emergência inventado (pergunta de produto real, deferida a Marcelo, não decidida aqui), nenhuma UI nova de reatribuição (a mutação `updateItem` já existente, já validada por `MemberEligibilityChecker`, é o mecanismo) — só paridade mínima de mensagem de erro no frontend (`isResponsibilityReassignmentRequiredError` em `api/errors.ts` + `Settings.tsx`'s `LeaveOrganizationSection`, mesma classe de polimento pequeno que D-120 fez para `LastOwnerError`). **G-V3**: 3 mutações reais verificadas e revertidas — (1) neutralizar a checagem `if (assigned.itemIds.length > 0)` em `remove-membership.ts` (1 teste falha); (2) trocar `truncated: totalKnown > itemIds.length` por `truncated: false` derivado de `itemIds.length` no adapter (1 teste falha — prova que `totalKnown` não é derivado do array capado); (3) remover o `while (exclusiveStartKey)` (travar em uma página só) no mesmo adapter (1 teste falha — prova paginação real até exaustão). Nenhuma mutação mascarada. **Sem infra/rota nova**: `remove-membership.ts`/`leave-organization.ts` já wireados a handlers HTTP existentes desde B2B-8 — confirmado por leitura, não assumido; `build:lambdas`/`terraform test` não exigidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos (as 2 falhas de `npm test` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes em `scripts/reset-dev-data.ts`, sem relação com esta mudança); backend 1249 testes passando / 137 arquivos (2 novos: `assigned-active-items-lookup.test.ts` com 4 testes, +6 testes novos em `membership-management.test.ts`), frontend 156 testes / 24 arquivos inalterado (só paridade de mensagem, sem teste novo de UI). Zero regressão real. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-122, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo de precondição best-effort real, porta + adapter reais, erro novo mapeado, paridade mínima de frontend | `src/modules/organization/ports/assigned-active-items-lookup.ts`, `src/modules/organization/application/{remove-membership,leave-organization}.ts`, `src/runtime/aws/composition/organization.ts`, `src/shared/errors/app-error.ts`, `frontend/src/api/errors.ts`, `frontend/src/routes/Settings.tsx`, `test/unit/organization/{assigned-active-items-lookup,membership-management}.test.ts`, `test/unit/identity/resolver.test.ts` | 2026-08-31 |
145:| D-124 | W3-07 — implementação real do orquestrador de purga de tenant, exatamente o mecanismo APROVADO em D-121 (implementação direta de design já aprovado, mesmo padrão de D-096/D-102/D-108/D-113 — nenhuma rodada Claude↔Codex nova para o mecanismo). Fecha a lacuna que o próprio D-121 nomeou: `transitionTenantLifecycle()` (D-068) e `purgeTenant()` (W3-07) eram código real, funcional e testado, **sem nenhum chamador em produção** — nada no sistema jamais disparou uma purga. **Nova máquina de estados Step Functions** (`infra/state-machines/tenant-purge.asl.json` + `infra/modules/tenant-purge-workflow/`, gêmeo estrutural de `extraction-workflow`): `Wait(1800s)` literal (cutoff D-066 Rodada H, nunca redecidido) → `AdvanceToQuiescing` → `AdvanceToPurging` → `RunPurge` → `EvaluatePurgeResult` (`Choice`, nunca `Retry`/`Catch` — Fix 2, porque `purgeTenant()` devolve `PARTIAL`/`SUCCESS`/`FAILED` como valor resolvido normal) → `AdvanceToVerified` → `AdvanceToDeleted` → `Succeed`; ramos `MarkBlocked*` → `Fail`. **Handler único de transição** (Fix 3) parametrizado por `from`/`to` no `Parameters` de cada estado, com no-op idempotente quando o registro já está em `to` e re-leitura única em `SystemMutationConflictError`. **Envelope compacto** (Fix 6) verbatim: `{status, checkpoint, counters:{s3UnresolvedCount,dynamoRejectedCount,sessionRejectedCount}, failure}`; `TenantPurgeResult` completo continua só em log estruturado (`SecureLogger`). **`PURGE_RETRY_LIMIT = 20`** como `local` Terraform nomeado, substituído no ASL via `replace()` (Fix 8). **Alarme CloudWatch novo e real** (Fix 7): `AWS/States` `ExecutionsFailed` **e** `ExecutionsTimedOut`, dimensionados por `StateMachineArn`, no mesmo tópico SNS de alerta; `evaluation_periods=1` (não 3 como os alarmes de reminder) porque uma purga falha deixa dado de tenant fisicamente presente após um pedido de exclusão. **Sweeper** via EventBridge Scheduler `rate(1 day)` com as 2 responsabilidades aprovadas: reparo durável de `StartExecution` para tenants `DELETING`/`QUIESCING`/`PURGING`/`VERIFIED` parados há >1h (nunca `BLOCKED`/`HELD` — remediação humana em curso), e verificação residual pós-`DELETED` na janela de 90 dias reusando `verifyTenant*Empty()` sem inventar lógica nova. **`CloseOrganizationService`** novo com a ordenação corrigida da Rodada 3 Fix 8 (checagem de estado terminal ANTES do `StartExecution` incondicional), erro novo `OrganizationClosureUnavailableError` (`CONFLICT`/409, mesma categoria de `TenantNotActiveError`), action RBAC nova `organization:close` em `OWNER_ROLES`. **Composition root real** (`src/runtime/aws/composition/tenant-purge.ts`) que fecha o KNOWN LIMIT documentado no próprio `purge-tenant.ts` (Codex rodada 5): tabela FECHADA bucket→raiz (`clean/`, `tenant/`, `tenant/`, `ocr/`, cada uma verificada lendo o key builder real), nunca um par `{bucket, prefix}` arbitrário. **IAM mínimo** conforme Fix 8, verificado contra os adapters reais (sem `s3:GetObject` em lugar nenhum — a purga apaga, nunca lê conteúdo). **3 achados reais corrigidos durante a implementação, nenhum deles mudança de forma do design aprovado**: (1) o design nomeava `Wait` como cobrindo `DELETING→QUIESCING`, mas um estado `Wait` não escreve no DynamoDB — foi necessário um `AdvanceToQuiescing` explícito, realização fiel do mesmo handler único, não desvio; (2) `checkpoint`/`failure` do envelope precisam ser `null` explícito e nunca ausentes — o `ResultSelector` do ASL lê por JSONPath e um caminho AUSENTE é falha dura `States.Runtime`, não `null`; (3) a role de execução da máquina de estados precisava de `logs:*LogDelivery`+`xray:Put*` além de `lambda:InvokeFunction` — sem elas o `apply` falha (não o runtime), achado por comparação direta com `extraction_workflow_invoke_lambdas`, não assumido. **Rota HTTP**: `POST /organizations/close` no módulo **`api-gateway`** (não `bff-api-gateway`) + `proxy-allowlist.ts` + dispatch no `memberships-handler`, todos no MESMO commit, com assert dedicado no `tftest` — correção de roteamento apurada por leitura: toda mutação de organização (`leave`, `settings`) roda por `api-gateway`+proxy do BFF, nunca por uma rota `/bff/*` própria; seguir a instrução literal do prompt teria reproduzido a classe de bug de D-117/D-120. **UX de confirmação** (latitude explícita da Rodada 1): `CloseOrganizationSection` em `Settings.tsx`, type-to-confirm do `organizationId` (não diálogo de 2 passos — o segundo clique vira memória muscular; digitar o identificador obriga a ler QUAL organização está sendo destruída, o modo de falha que importa agora que um usuário pertence a várias), visível só a OWNER no cliente, com o mesmo token revalidado no servidor contra o tenant RESOLVIDO. **G-V3**: mutação real verificada e revertida em 3 pontos — remover o no-op idempotente do handler de transição (2 testes falham), remover o filtro de 1h do sweeper (1 teste falha), e a ordenação Rodada-2 do `CloseOrganizationService` (4 testes falham). A primeira tentativa de mutação da ordenação foi MASCARADA pelo próprio guard de exaustividade do serviço e não falhou — registrado honestamente e refeito com a mutação fiel (ambos os guards removidos). O bound do `Choice` é coberto no `tftest` do módulo (`"NumericLessThan": 20` como número JSON real, nunca string). **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos; backend 1267 testes / 136 arquivos (32 novos), frontend 156 testes / 24 arquivos (4 novos); `terraform test` 19/19 raiz (3 runs novos), 9/9 `tenant-purge-workflow` (módulo novo), 1/1 `api-gateway`, 7/7 `bff-api-gateway`; zero regressão. Nenhuma operação AWS destrutiva — só `validate`/`test`/`plan`. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-121, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — orquestrador real de ponta a ponta; `transitionTenantLifecycle()`/`purgeTenant()` deixam de ser código sem chamador | `infra/state-machines/tenant-purge.asl.json`, `infra/modules/tenant-purge-workflow/`, `infra/{main,outputs}.tf`, `infra/modules/api-gateway/`, `infra/tests/stack.tftest.hcl`, `src/workers/tenant-purge/{lifecycle-transition,purge-result-envelope,tenant-purge-sweep}.ts`, `src/shared/tenant-lifecycle/{tenant-purge-execution-starter,sfn-tenant-purge-execution-starter}.ts`, `src/shared/dynamodb/tenant-purge-scan.ts`, `src/shared/errors/app-error.ts`, `src/modules/organization/{application/close-organization.ts,http/organization-lifecycle-handlers.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition,handlers}/**`, `frontend/src/**` | 2026-08-31 |
146:| D-126 | CSV data export — implementação real do mecanismo APROVADO em D-123 (implementação direta de design já aprovado, mesmo padrão de D-096/D-102/D-108/D-113/D-124/D-125 — nenhuma rodada Claude↔Codex nova para o mecanismo). **Nova action RBAC `item:export`** (`identity/domain/authorization.ts`) em `ADMIN_ROLES`, justificativa de assimetria de disclosure verbatim do design. **Orçamento decrescente literal** (round-3 Achado #1) em `ExpirationService.exportItems()` — 3 chamadas `queryGsi1` (`ACTIVE`/`ARCHIVED`/`RENEWED`, ordem fixa, `DELETED` nunca consultado), `limit: budget+1` para detectar estouro sem query de contagem separada, `EXPORT_ITEM_CAP=2000` exportado como constante nomeada; confirmado por leitura de `dynamodb-expiration-store.ts` que `queryGsi1` pagina internamente até acumular `limit` itens reais sem `FilterExpression`, tornando `limit`-como-contagem seguro aqui (diferente do achado de D-125 sobre GSI1+filter). **Guard de 4 MB** (round-3 Achado #2) em `buildExportCsv()` (`expiration/http/export-handler.ts`), acumulando `Buffer.byteLength` do CSV final serializado (header+CRLF+linhas pós-quoting RFC4180), nunca a soma de valores brutos — qualquer um dos dois limites (item ou byte) que disparar primeiro rejeita com o mesmo `ValidationError`. **`src/shared/csv/csv-export-writer.ts`** novo, `serializeCsvField()`/`serializeCsvRow()` únicos fazendo mitigação de fórmula (apóstrofo, extensão do precedente `APROVADO` de `roadmap-evolution/09`) ANTES da quotação RFC4180 no mesmo passe — nunca dois mecanismos separados, provado por teste dedicado do caso que precisa dos dois. **`toApiGatewayCsvResult()`** novo e aditivo em `http-adapter.ts` (round-3 Achado #3) — único call site é o novo handler; `toApiGatewayResult()` existente inalterado. **Colunas CSV** (15, ordem fixa) reverificadas contra `expiration-item.ts` real: `itemId,name,category,description,dueDate,issueDate,periodicity,issuer,number,assigneeUserId,tags,priority,status,createdAt,updatedAt`. **Filename** gerado só de `tenantId` (ULID server-side) + timestamp — nunca `Organization.displayName` nem string do cliente (achado não-bloqueante do Codex round-3, risco de header injection). **Decisão de wiring HTTP, não coberta literalmente pelo design aprovado**: `items_handler` (Terraform) não declara `timeout_seconds` (default 10s, `lambda-function/variables.tf`), insuficiente para o `timeout_seconds=25` exigido pelo round-3 Achado #6 sem afetar toda rota `/items*` existente — optado por **Lambda dedicada `export_handler`** (`infra/main.tf`, `timeout_seconds=25`, mesma política `tenant_facing_read_write_policy_json` de `items_handler`, sem `gsi4_read_policy_json` — GSI1 não é restrito) + rota própria `GET /items/export` no módulo `api-gateway` (não `bff-api-gateway`, mesmo precedente de D-124 — leitura/escrita de item já roteia por `api-gateway`), integração/permissão/assert de rota dedicados no MESMO commit (classe de bug D-117/D-120), coexistindo com `GET /items/{itemId}` pelo mesmo precedente de segmento literal que `GET /items/dashboard` já usa. **G-V3**: verificado por teste dedicado que defeataria uma implementação mais fraca — (1) `serializeCsvField` prova que mitigar-depois-de-quotar produziria CSV sintaticamente quebrado (comentário no teste mostra a saída incorreta), a implementação real mitiga primeiro; (2) teste com `ExpirationStore` stub prova que `queryGsi1` pedido com `limit: 2001` retornando exatamente 2001 linhas de `ACTIVE` sozinho dispara o `ValidationError` com `statusWhereExceeded: "ACTIVE"` — prova o `budget+1` na fronteira exata; (3) teste de `buildExportCsv` com descrição pesada em vírgulas (que quase dobra de tamanho após quoting) prova que o guard mede bytes PÓS-serialização, não os brutos; (4) teste RBAC prova `MEMBER`/`VIEWER` negados e `ADMIN`/`OWNER` permitidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos (as 2 falhas de `npm test`/`system-mutation-allowlist.test.ts` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — mesmas `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes de `scripts/reset-dev-data.ts` já documentadas em D-125, sem relação com esta mudança); backend 1277 testes / 139 arquivos (3 novos: `csv-export-writer.test.ts` 16 testes, `export-handler.test.ts` 5 testes, +7 testes novos em `expiration-service.test.ts`), 1275 passando (2 falhas pré-existentes não relacionadas); `terraform validate`/`terraform test` limpos na raiz (`AWS_PROFILE=claude-dev`) e no módulo `api-gateway` (1/1 run, 3 asserts novos); zero regressão real. Nenhuma operação AWS destrutiva — só `validate`/`test`/`build`. Escopo respeitado integralmente: só `ExpirationItem`, sem gate de billing (M12 bloqueado por D-052), `TrackedSubject`/documentos fora de escopo (v2 futura). | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-123, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124/D-125) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo real de ponta a ponta (RBAC → orçamento GSI1 → guard de bytes → serialização CSV → resposta HTTP → Lambda dedicada → rota API Gateway); nunca antes tinha chamador HTTP real | `src/modules/identity/domain/authorization.ts`, `src/modules/expiration/application/expiration-service.ts`, `src/modules/expiration/http/export-handler.ts`, `src/shared/csv/csv-export-writer.ts`, `src/runtime/aws/http-adapter.ts`, `src/runtime/aws/handlers/export-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `infra/modules/api-gateway/{main,variables}.tf`, `infra/modules/api-gateway/tests/api_gateway.tftest.hcl`, `test/unit/expiration/{expiration-service,export-handler}.test.ts`, `test/unit/shared/csv-export-writer.test.ts` | 2026-08-31 |
147:| D-128 | `AppError.retryable` — comportamento real de consumidores SQS. Fecha a pendência de produto registrada em `docs/engineering/decisions-log.md` E-011 (auditoria 2026-08-29 achou o campo honestamente documentado como diagnóstico/log-only, mas deixou aberta a pergunta "deveria um handler SQS ramificar sobre `retryable`?"). Autoridade ampliada (`docs/engineering/ai-governance.md` §1, 2026-08-31) cobre este resíduo de produto. Protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: régua 8,5/design 8,7 — Codex achou 3 imprecisões reais na fundamentação, não na conclusão: alegação normativa excessiva sobre a AWS "proibir" branching, duração "minutos, não horas" não demonstrada pelo `visibility_timeout_seconds = consumer_timeout_seconds * 6` real, e a taxonomia de `retryable` caracterizada como "base sã para política de branching" quando na verdade `toAppError()`'s fallback (`InternalError`, `retryable:false` para qualquer erro não reconhecido) é evidência ATIVA contra isso; Rodada 2 corrigiu as 3 imprecisões sem mudar a conclusão — Claude 9,0/Codex 9,6, gate atingido; Rodada 3 tréplica de fechamento, Claude 9,3). **Pesquisa externa (SIM)**: AWS Lambda Developer Guide, "Handling errors for an SQS event source in Lambda" + "Using Lambda with Amazon SQS" (`docs.aws.amazon.com/lambda/latest/dg/`, WebFetch 2026-08-31) — todos os exemplos de referência oficiais (10 linguagens) reportam qualquer exceção capturada como `batchItemFailure` incondicional, nenhum ramifica por tipo/classe de erro; nenhum padrão de classificação de erro para roteamento direto a DLQ é oferecido como recomendação central. **Decisão**: nenhum handler SQS deste sistema passa a ramificar sobre `retryable`. Mecanismo de retry-vs-terminal continua exclusivamente nativo do SQS (`maxReceiveCount=5` uniforme, `infra/modules/sqs-worker-queue/main.tf`, provado por `stack.tftest.hcl`/`sqs_worker_queue.tftest.hcl`) + DLQ redrive — nenhuma chamada SQS explícita nova, nenhum roteamento paralelo. Razão central: assimetria de blast radius — um falso-terminal sob o regime atual não tem efeito extra (mensagem retentada normalmente de qualquer forma); sob um regime de branch-and-terminate, pularia processamento devido de uma mensagem legitimamente retryable. **Classificação de `app-error.ts` auditada e considerada consistente** (nenhuma inconsistência real encontrada nas subclasses existentes — `ExtractionCommitFailedError` com `retryable:true` é a única exceção à regra "validação/regra de negócio = false", e é correta por documentação própria); nenhuma mudança de classificação proposta. **Implementação (documentação apenas, sem mudança de runtime)**: comentários de `AppError`/`isRetryable()` em `src/shared/errors/app-error.ts` reescritos de "pendência" para "decidido, ver D-128"; redação contraditória ("poison message... not retryable" + ainda reportado como batch failure) corrigida em `reminder-dispatch-handler.ts`/`email-delivery-handler.ts` (os 2 handlers lidos linha a linha nesta rodada — demais consumidores SQS do sistema não tocados, decisão é uniforme e independe de detalhe por handler). **Blast radius de errar esta decisão**: baixo — revisitável isoladamente por fila/handler no futuro só sob evidência operacional concreta (saturação, custo material, atraso mensurável), rollback trivial (nenhuma infraestrutura nova para desfazer). | Type 1 (nível 5, política comportamental de retry/DLQ + resíduo de produto sob autoridade ampliada) | 9,3 | 9,6 | **APPROVED** (3 rodadas, gate 9,0 atingido em ambos sem arredondar) — documentação implementada, sem mudança de runtime/infra | `docs/architecture/reviews/appError-retryable-sqs-scoping/` (round-1 a round-3), `src/shared/errors/app-error.ts`, `src/runtime/aws/handlers/{reminder-dispatch-handler,email-delivery-handler}.ts` | 2026-08-31 |
148:| D-127 | Estratégia de quarentena/recuperação antes de exclusão física irreversível (cross-cutting, motivada por W3-07/`CloseOrganizationService` mas desenhada para qualquer feature futura da mesma classe, ex. armazenamento de arquivos ainda não escopado) + priorização das 7 classes `retentionClass` de `privacy-lgpd.md` §4 ainda sem purga física real dentro de um tenant `ACTIVE` — protocolo Claude↔Codex completo, **11 rodadas** (Rodada 1: 5,5/10 — 11 achados reais, cancelamento inalcançável pelo `RequestContext` normal, sem coordenação `StopExecution`, nome de execução fixo quebra reabertura, LGPD com erro semântico vencimento≠exclusão; Rodadas 2-9 fecharam achados sucessivos incluindo 2 erros factuais próprios corrigidos ao vivo — `src/shared/ids.ts` inexistente (real: `src/runtime/aws/ids.ts`), `RecurrenceOccurrence` inexistente (real: `ReminderOccurrence`) — e um padrão real de 3 rodadas seguidas achando mais um `CANCELLED`-writer manualmente enumerado (3→4→5 sites); Rodada 10 trocou enumeração manual por um helper central (`cancelOccurrenceUpdate()`), Codex apontou que um helper sozinho não é garantia mecânica; Rodada 11 fechou com regra ESLint (`no-restricted-syntax`, mesma classe de enforcement de `no-console`/`AGENTS.md` §7) tornando o bypass um erro de `npm run lint`, não uma convenção — Claude 9,4/Codex 9,3, gate atingido). **Pesquisa externa (SIM)**: GitHub (repositório deletado restaurável 90 dias, `docs.github.com`, 2026-08-31), AWS (conta fechada reabrível 90 dias, `docs.aws.amazon.com/accounts`, 2026-08-31), Google Workspace (usuário deletado restaurável 20-25 dias, `workspace.google.com`, 2026-08-31), Slack (arquivo sem compartilhamento, grace period 30 dias, `slack.com/help`, 2026-08-31) — convergência real 20-90 dias, nenhuma fonte usa quarentena medida em segundos/minutos para dado persistido. **Mecanismo aprovado**: novo estado `HELD_FOR_RECOVERY` inserido entre `ACTIVE` e `DELETING` no grafo de `TenantLifecycleRecord` (`ACTIVE ⇄ HELD_FOR_RECOVERY → DELETING → QUIESCING → PURGING → VERIFIED → DELETED`, única aresta nova de retorno a `ACTIVE`), duração **30 dias** (piso defensável mais curto da faixa convergente, não "ponto médio" — erro de aritmética da Rodada 1 corrigido explicitamente na Rodada 2), reusa o Step Functions já existente do W3-07 (D-124) sem novo serviço AWS — `recoveryDeadline`/`closureAttemptId` persistidos na mesma transação da transição (nome de execução `${tenantId}-${closureAttemptId}`, ARN determinístico derivado, nunca lido de volta, fechando a race de persistência de ARN); ação real de cancelamento existe (`CancelOrganizationClosureService`, caminho de resolução de identidade dedicado via `IdentityMapping` — nunca `RequestContextResolver` normal, que exige `ACTIVE` — e primitive de autorização dedicado `authorizeCancelClosure()`/`ClosureCancellationDeniedError`, não o union `Action` genérico); `StopExecution` sempre roda antes de qualquer restauração de `ACTIVE`; sweeper diário (extensão do `tenant-purge-sweeper` de D-124) reconcilia falhas parciais só sob conjunção estrita (execução `ABORTED` + `closureAttemptId` correspondente + registro ainda `HELD_FOR_RECOVERY` — qualquer outra combinação alarma, nunca restaura otimisticamente); legal hold usa `HELD_FOR_RECOVERY→HELD` (não `BLOCKED`, reservado a falha operacional real), retomada preserva o `recoveryDeadline` original (hold suspende, nunca reseta o relógio). **Priorização LGPD** (2 pistas): executável — `CORE_USER_DATA` (`ExpirationItem`/policy soft-deletados `deletedAt+30d`, mais `ReminderOccurrence` como worker irmão independente via `purgeAfterTtl` TTL nativo do DynamoDB, mesmo mecanismo de `InvitationTokenPointer`/`invitation-token.ts`, nunca dependente do pai sobreviver) → `DELIVERY_RECORD` (180d) → `SECURITY_AUDIT` (365d) → `QUOTA_TELEMETRY` (janela+30d) → `ACCOUNT_ACTIVE` não-fechamento (`Invitation`→`Membership`→`Channel`) → `TRANSIENT` remanescente (`WebhookInbox`/`UploadSlot`); bloqueada — `LEGAL_EVIDENCE` (trava jurídica/KMS/Object Lock já `APPROVED`, fora da ordem linear); já resolvidas, zero backlog — `USER_DOCUMENT`, `EXTRACTION_TRANSIENT`, `InvitationTokenPointer`/`TRANSIENT`. **Escopo desta rodada: design-only**, deliberado e justificado (Rodada 1 subestimou como "1 estado+1 Wait+1 service", Rodada 2 reclassificou corretamente após o tamanho real aparecer — comparável a uma wave dedicada, não a D-125) — implementação real (novo Lambda `ReadTenantLifecycleRecord`, extensão do sweeper, migração dos 5 `CANCELLED`-writers para `cancelOccurrenceUpdate()`, regra ESLint, 7 workers de purga LGPD um por vez na ordem acima) fica para sessão(ões) futura(s) dedicada(s), mesmo padrão D-121→D-124. | Type 1 (nível 5-6, política de retenção/exclusão de dado, LGPD-relevante — `change-risk-scale.md`) | 9,4 | 9,3 | **APPROVED** (design, 11 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/quarantine-retention-scoping/` (round-1 a round-11, proposta+self-grade+critique Codex cada) | 2026-08-31 |
150:| D-131 | Admin Activity/Audit Log View — visão ADMIN/OWNER do que outros membros da organização fizeram (Marcelo, 2026-08-31: renovação/criação de item, exportação de dados, qualquer ação relevante), item 5 da lista de "Próximos itens" do `NEXT_SESSION_PROMPT.md`. Verificação de shape: `AuditEvent` de expiration/organization/subject (`src/modules/{expiration,organization,subject}/domain/audit-event.ts`) são estrutural/semanticamente compatíveis (mesma convenção `PK=TENANT#<id>#<KIND>#<yyyyMM>`/`SK=EVT#<timestamp>#<id>`, mesmos campos base, mesma garantia append-only na mesma `TransactWriteItems`) — não exige reconciliação de schema. Autoridade ampliada (`docs/engineering/ai-governance.md` §1, 2026-08-31) cobre o resíduo de produto. Protocolo Claude↔Codex completo, **5 rodadas** (mínimo 3 excedido por não-convergência real: Rodada 1 Codex 6,85 — checklist E-014 ausente, cursor não especificado, gap de export mal desenhado; Rodada 2 Claude 8,4 self/Codex 8,25 — cursor só cobria 3 de 4 partições, sem idempotência de export, `organization:close` não classificado; Rodada 3 Claude 8,9 self/Codex 8,10 — cursor com semântica inventada em vez do contrato real `LastEvaluatedKey`/`ExclusiveStartKey`, idempotência por hash ator+dia colapsava exports legítimos, fail-open descrito como fail-closed, URLs do checklist abreviadas; Rodada 4 Claude 9,1 self/Codex 8,70 — único achado restante: cursor perdia itens buscados-mas-não-consumidos por partição no corte do merge; Rodada 5 Claude 9,3 self/Codex 9,40 — cursor corrigido para usar a chave `{PK,SK}` real do último item consumido por partição, convergência). **Pesquisa externa (SIM PARCIAL, E-014)**: GitHub Org Audit Log + Notion Workspace Activity Log (URLs verificadas em `estado-final-consolidado.md`) confirmam 2 critérios pesados (admin-only nunca member-facing; feed cronológico legível, não log bruto) — Linear removido do checklist por não ser evidência de trilha administrativa equivalente (achado real da Rodada 2, corrigido na Rodada 3). **Decisões**: escopo v1 = feed leve (não audit log pesquisável), RBAC novo action `activity:read`/`ADMIN_ROLES` (precedente `item:export`); query = 4 `Query`s mensais (expiration/organization/subject + novo `TenantAuditEvent` para export) com merge k-way e cursor composto por último item consumido por partição, sem GSI novo, paginação eventualmente consistente aceita conscientemente; retenção reconcilia com D-127 (`SECURITY_AUDIT`, prioridade 3, `occurredAt` como relógio canônico equivalente a `createdAt`); gap de export (`ExpirationService.exportItems()`, linha 618, confirmado sem `AuditEvent`) fecha via 4º agregado-irmão `TenantAuditEvent` com lock condicional de idempotência (`exportRequestId` via header `Idempotency-Key` opcional ou UUID por requisição, escrita fail-open antes da resposta HTTP); `organization:close` (D-124) verificado sem `MembershipAuditEvent` — classificado `missing to implement`, gap nomeado fora de escopo desta rodada; frontend v1 inclui tela somente-leitura (pedido de Marcelo é visibilidade humana direta, não mecanismo interno). **DESIGN-ONLY** — implementação real (handler, service, `TenantAuditEvent`, mudança em `exportItems()`, tela, testes) fica para sessão(ões) futura(s) dedicada(s), mesmo padrão de D-127. | Type 1 (nível 5, novo endpoint HTTP + RBAC novo + nova superfície user-facing expondo ações de outros membros, resíduo de produto sob autoridade ampliada) | 9,3 | 9,40 | **APPROVED** (5 rodadas, gate 9,0 atingido em ambos sem arredondar) — design consolidado, implementação real é trabalho futuro nomeado | `docs/architecture/reviews/admin-activity-log-scoping/` (round1 a round5, `estado-final-consolidado.md`) | 2026-08-31 |
152:| D-132 | Wave 1b — Design System Implementation Gap (D-130): resíduos nomeados Button `lg` e motion `--duration-slow`. Protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: Claude 9,0 self/Codex 8,7 — achou inconsistência real entre o critério "significado reutilizável exige segundo consumidor real" usado para rejeitar Button `lg` e "implementar `--duration-slow` junto do primeiro consumidor", mais um gatilho puramente numérico ">160ms" para motion, que qualquer valor de 161ms satisfaria formalmente; Rodada 2: Claude 9,2 self/Codex 9,7 — critério único corrigido: token nasce só com um caso de uso real e concreto E uma categoria semântica própria, nunca por contagem de consumidores nem limiar numérico isolado; Rodada 3: tréplica de fechamento, sem achado novo). **Decisão: NÃO implementar nenhum dos dois agora.** Button `lg` (`--control-height-lg`/44px já existe como token de escala, não vira variante do `Button` — `ButtonSize` continua "sm" | "md") fica resíduo nomeado até um caso de uso real (CTA mobile full-width/touch-primary). `--duration-slow` fica resíduo nomeado até o primeiro componente de superfície sobreposta real (Modal/Dialog/Drawer/Popover, nenhum implementado ainda) precisar de uma categoria de movimento distinta de hover/foco inline — nasce no mesmo commit desse componente, nunca antes, validado contra `prefers-reduced-motion`. Nenhuma pesquisa externa (aplicação de regra já `APPROVED`, não definição de padrão de mercado). | Type 1 (nível 5, resíduo de decisão de produto nomeado em D-130, sob autoridade ampliada) | 9,2 | 9,7 | **APPROVED** (3 rodadas, gate 9,0 atingido em ambos sem arredondar) — decisão de não-implementação, sem mudança de código | `docs/architecture/reviews/wave1b-button-lg-motion-scoping/` (round1 a round3, `estado-final-consolidado.md`) | 2026-08-31 |
153:| D-133 | Wave 1b — Design System Implementation Gap (D-130): implementação direta de 2 componentes genuinamente faltantes do catálogo de ~30 (`design-system.md` §29) que não têm equivalente sob outro nome no código real — verificação de shape feita antes de escrever (Button/DataTable/InlineNotice/Layout/StatusBadge/UrgencyIndicator já cobrem Button/Table/Alert/PageHeader-Section-Toolbar-Panel/StatusBadge/urgência; TextField/SelectField/FormErrorSummary em `components/forms/` já cobrem Input/Textarea/Select/FormField/FieldError; `AsyncStates.tsx` já cobre EmptyState/Skeleton/loading; `OrganizationSwitcher.tsx` já é o pattern de switcher — nenhum desses recriado). **`Divider`** (`frontend/src/components/ui/{Divider.tsx,Divider.css}`) — separador puramente decorativo, `aria-hidden`, 2 orientações; nunca substitui um `<section>` real como fronteira estrutural (mission/`Layout.tsx` já estabelecem isso). **`IconButton`** (`frontend/src/components/ui/{IconButton.tsx,IconButton.css}`) — reusa as variantes visuais de `Button` (nenhuma linguagem visual nova), `label` **obrigatório** no tipo (não opcional) para tornar impossível omitir o nome acessível (mission §23), setado em `aria-label` E `title`. Nenhuma camada de component-tokens nova foi necessária (ambos consomem semantic tokens existentes diretamente, sem valor arbitrário novo). **Decisão explícita de não-escopo desta sessão** (honestidade sobre o que ficou de fora, não gold-plating apressado): os demais itens do catálogo ainda faltantes — `Link` dedicado, `Checkbox`/`Radio`/`Switch`, `Combobox`, `DateInput`/`DatePicker`, `Tooltip`, `Popover`, `DropdownMenu`, `Modal`/`Dialog`, `Drawer`, `Tabs`, `Pagination`, `Breadcrumb`, `Avatar` — exigem lógica de overlay/focus-trap/portal ou semântica de formulário não-trivial o suficiente para merecer implementação própria com o mesmo rigor (não uma rodada apressada no fim desta sessão); permanecem no backlog nomeado de Wave 1b, não escondidos. `Card` **não** foi adicionado apesar de estar no catálogo (`design-system.md` §40): `Layout.tsx` documenta uma decisão arquitetural deliberada já em produção ("There is no Card component, on purpose... a card is what you reach for when independent modules genuinely need separating, and the Core Expiration slice has no such grouping yet") — conflito real entre um documento de roadmap (`design-system.md`, adotado como catálogo aspiracional em D-130, não afirmação de que os ~30 já existem) e uma decisão de código já implementada; resolvido a favor do código real (mesma regra de precedência de `AGENTS.md` §5, "documento temático corrente" > catálogo de roadmap), registrado como gap nomeado a decidir explicitamente (não silenciosamente) quando um agrupamento real justificar o componente. **z-index**: verificado já 100% implementado (`--z-base/sticky/overlay/skip-link` em `tokens.css`), não é gap. **Breakpoints nomeados** (§25): decisão mecânica de não criar tokens agora (ver `estado-final-consolidado.md` de D-132) — mesmo critério "token nasce com consumidor real", o único breakpoint real do CSS (`base.css` 900px) não corresponde a nenhum valor do catálogo. **G-V3**: mutação nomeada e revertida em ambos — `Divider`'s `aria-hidden="true"→"false"` (1 teste falha) e `IconButton`'s `aria-label`/`title` (mutar só `aria-label` foi MASCARADO pelo algoritmo de nome acessível do testing-library, que cai para `title` — achado honesto registrado, não escondido; mutar os dois juntos falha 2 testes). **Suíte**: frontend `typecheck`/`lint` limpos, `npm test` 160/160 (4 novos), `check-docs` limpo; backend não tocado nesta sessão (nenhuma mudança em `src/`, `infra/`), suíte de backend completa não executada nesta rodada por não haver diff relevante — risco residual reconhecido, não uma alegação de verificação que não ocorreu. | Type 1 (nível 4-5, implementação direta de catálogo já `APPROVED` em D-130, mesmo padrão dos demais D-1xx de implementação direta) | n/a (implementação mecânica pós-D-132) | n/a (implementação mecânica pós-D-132) | **IMPLEMENTADO** (parcial, deliberado) — 2 de ~9 componentes genuinamente faltantes; backlog nomeado explicitamente para o restante | `frontend/src/components/ui/{Divider.tsx,Divider.css,IconButton.tsx,IconButton.css}`, `frontend/test/components/ui.test.tsx` | 2026-08-31 |
154:| D-134 | Wave 1b — Design System Implementation Gap (D-130/D-133), continuação: implementação direta de 4 componentes do catálogo (`design-system.md` §29) — verificação de shape feita antes de escrever, nenhum já existia sob outro nome (`base.css` já estiliza `<a>` globalmente e o `:focus-visible` global cobre foco, mas nenhum componente dedicado à navegação inline existia; nenhum checkbox/radio/switch em `components/forms/` nem `ui/`). **`Link`** (`frontend/src/components/ui/{Link.tsx,Link.css}`) — terceiro membro do trio de navegação junto de `Button`/`ButtonLink` (mission §34): `Link` envolve o `Link` real do react-router para navegação interna (client-side), `ExternalLink` é um `<a>` real com `target="_blank"` + `rel="noopener noreferrer"` (nunca só `target="_blank"`, que deixa `window.opener` alcançável pelo destino) e um marcador visível (não só ícone) + texto visualmente oculto "(abre em nova aba)". **`Checkbox`** (`frontend/src/components/ui/{Checkbox.tsx,Checkbox.css}`) — `<input type="checkbox">` real posicionado sobre uma caixa desenhada (nunca `display:none`, que removeria da árvore de acessibilidade em alguns navegadores); `indeterminate` setado imperativamente via ref (não existe prop React para a propriedade DOM), independente de `checked`. **`RadioGroup`** (`frontend/src/components/ui/{RadioGroup.tsx,RadioGroup.css}`) — catálogo lista "Radio" no singular, mas um `<input type="radio">` só faz sentido como conjunto compartilhando `name`; implementado como o grupo, com `<fieldset>`/`<legend>` reais (não `aria-label`), reusa `.ui-field__requirement`/`.ui-field__error` de `components/forms/Form.css` (mesmo padrão visual de obrigatório/erro do `TextField`) em vez de duplicar. **`Switch`** (`frontend/src/components/ui/{Switch.tsx,Switch.css}`) — não existe controle HTML nativo para toggle de efeito imediato; implementado como `<button role="switch" aria-checked>` (padrão WAI-ARIA APG), nunca `<div onClick>`. Nenhum token novo necessário (todos consomem semantic tokens existentes — `--color-action-primary`, `--target-size-min` 24px como piso do alvo clicável mesmo com caixa/dot desenhados menores, `--radius-pill` para a trilha do Switch). Nenhuma pesquisa externa (padrões de implementação mecânicos — checkbox/radio nativos, WAI-ARIA switch pattern documentado — não definição de padrão de mercado não resolvido). **Decisão explícita de não-escopo desta sessão**: `Combobox`, `DateInput`/`DatePicker`, `Tooltip`, `Popover`, `DropdownMenu`, `Modal`/`Dialog`, `Drawer`, `Tabs`, `Pagination`, `Breadcrumb`, `Avatar` continuam faltando — os 5 primeiros (Tooltip a Modal/Drawer) exigem estratégia de overlay/portal/focus-trap ainda não decidida no projeto (candidato a protocolo Claude↔Codex completo quando abordados, por não haver padrão óbvio já implementado); os demais exigem composição maior (paginação real de dados, breadcrumb dinâmico por rota, avatar com fallback de iniciais) deliberadamente adiada para não forçar conclusão apressada. `Card` permanece gap nomeado de D-133, sem mudança nesta sessão. **G-V3**: mutação nomeada e revertida — `RadioGroup`'s `checked={value === option.value}` mutado para `!==`, 1 teste falhou corretamente (deixou de reportar o valor selecionado), revertido e suíte voltou a 27/27 em `ui.test.tsx`. **Suíte**: frontend `typecheck`/`lint` limpos, `npm test` (frontend) 171/171 (17 novos em `ui.test.tsx`, total do arquivo 27), `npm run lint`/`check-boundaries`/`validate-schemas`/`check-docs` (raiz) limpos; `npm run typecheck` (raiz) só passou após `npm ci` — `node_modules` estava desatualizado em relação a `package.json` (`@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes, usados por `scripts/reset-dev-data.ts` de D-110/D-111), não relacionado a este diff, corrigido como pré-requisito para rodar a suíte real em vez de presumir "sem erro = já estava assim". `npm test` (backend) 1292/1292 verde (137 arquivos), sem regressão. | Type 1 (nível 4, implementação direta de catálogo já `APPROVED` em D-130, mesmo padrão de D-133) | n/a (implementação mecânica pós-D-130/D-132) | n/a (implementação mecânica pós-D-130/D-132) | **IMPLEMENTADO** (parcial, deliberado) — 4 de ~7 componentes ainda faltantes do catálogo original; backlog nomeado explicitamente para o restante (overlay/portal/focus-trap + composição maior) | `frontend/src/components/ui/{Link.tsx,Link.css,Checkbox.tsx,Checkbox.css,RadioGroup.tsx,RadioGroup.css,Switch.tsx,Switch.css}`, `frontend/test/components/ui.test.tsx` | 2026-08-31 |
156:| D-136 | Otimização de performance do hot path (frontend startup + backend) — gatilho real: Marcelo reportou usando o app que a tela demora a mostrar conteúdo e que aparece uma mensagem tipo "validando sessão" desnecessária. Duas sessões anteriores produziram auditoria+pesquisa de performance (`expiration-tracker-performance-audit-2026-08-31.md`/`expiration-tracker-performance-strategies-research-2026-08-31.md`, raiz do repo, P0-P3) — esta rodada focou nos itens Type 1 (arquitetura/segurança) que explicam a queixa, via protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: Codex 6,4/10 régua + 7,6/10 design, NÃO APROVADO — achou que a proposta original removeria silenciosamente o fence transacional do W3-07 ao trocar `Get+TransactWriteItems` da quota `API_REQUEST` por `UpdateItem` simples, já que `TenantQuotaService.consume()` roda dentro de `executeTenantBusinessMutation()` com `ConditionCheck` sobre `TenantLifecycleRecord.status=ACTIVE` no mesmo `TransactWriteItems` — a transação protege 2 garantias, não 1; Rodada 2: 8,7/10, NÃO APROVADO — reclassificar `API_REQUEST` como `QUOTA_TELEMETRY` (classe já aprovada em D-127) foi aceito como correto, mas o Codex apontou que D-127 decidiu retenção, não autorizou escrita tenant-scoped nova pós-`ACTIVE→DELETING` nem que TTL substitui o fence de descoberta/purga; Rodada 3: Claude 9,3/Codex 9,3, **APPROVED**, sem arredondar). **Pesquisa externa (SIM PARCIAL, E-014)**: unificação de sessão frontend é padrão já resolvido (TanStack Query docs 2026); rate limiting multi-tenant tem padrão de abordagem em camadas (coarse+tenant-aware) mas calibração depende do modelo do projeto — Codex contestou o checklist original (sem pesos/âncoras, fontes secundárias para decisão de segurança) e substituiu por sub-rubrica de 6 critérios ponderados a partir da Rodada 2. **Decisões (5, todas design-only)**: D-A — `AuthContext` deriva `AuthState` do mesmo `useQuery(sessionQueryKey)` que `ActiveOrganizationContext` já usa, elimina o waterfall real de 2 chamadas sequenciais a `/bff/session` confirmado no código; as 2 mensagens sequenciais ("Verificando sua sessão…"/`ProtectedRoute.tsx:26`, "Carregando sua organização…"/`OnboardingGate.tsx:21` — exatamente o que Marcelo reportou) viram um único skeleton sem texto técnico; logout usa `removeQueries`, não só `invalidateQueries`. D-B — mantém `ConsistentRead=true`, troca renovação de idle TTL por request para sliding-expiration-com-threshold, `ConditionExpression` nunca ressuscita sessão revogada/expirada. D-C (parcial) — `createProfileIfAbsent()` sai do hot path (create-if-absent idempotente sem consumidor síncrono); `onboarding.resolve()` NÃO sai nesta decisão, condicionado a mapear consumidores primeiro; membership/lifecycle `ACTIVE` continuam obrigatórios em toda request. D-D (maior risco do pacote) — nova lane formal `EphemeralTelemetryMutation`, emenda explícita ao W3-07 (não exceção implícita): allowlist fechada em `API_REQUEST`, interface estruturalmente fechada sem campo `metadata` livre (precisão da Rodada 3), chave `PK=TENANT#<id>#QUOTA`/`SK=TYPE#API_REQUEST#<floor(epochSeconds/windowSeconds)>`, `purgeAfterTtl=windowEnd+30d` (atributo real do projeto, confirmado contra o código pelo Codex), item permanece coberto estruturalmente pelo scan/`PURGE_DELETE` real do W3-07 (TTL nativo é só limpeza best-effort, nunca a garantia), teste adversarial da corrida `ACTIVE→DELETING` obrigatório no DoD/G-V3 futuro, classificação tripla de falha (`ConditionalCheckFailedException`→429, dependência indisponível→fail-open com log, corrupção/bug→fail-closed 5xx real); API Gateway throttle recalibrado por critério de capacidade real (achado: infra hoje `rate=25/burst=50` global já limita antes de qualquer tenant alcançar sua quota individual de `100/60s`, não é neutro). D-E — dashboard usa `limit`+cursor opaco sobre a GSI já existente, ordenação com tie-breaker estável, teto de `limit` imposto pelo servidor. **Fora de escopo** (mecânico, não Type 1): `React.lazy()` por rota, Node 20→22/24 (dívida com prazo — Node 20 já depreciado pela AWS desde 30/04/2026), ADOT 1-30-0→1-30-2 + instrumentação seletiva, benchmark Lambda memory/ARM64, batch/concorrência SQS, `staleTime` por tipo de query, N+1 de `BatchGetItem` na renovação — implementação direta futura, sem protocolo. **DESIGN-ONLY** — implementação real fica para sessão(ões) futura(s) dedicada(s), mesmo padrão D-121→D-124; D-D deve ser implementado com o teste adversarial como parte do DoD, não opcional. | Type 1 (nível 5-6, arquitetura de sessão/autorização + emenda a mecanismo de segurança já auditado do W3-07) | 9,3 | 9,3 | **APPROVED** (design, 3 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/performance-hotpath-scoping/` (round1 a round3, `estado-final-consolidado.md`) | 2026-08-31 |
159:| D-139 | Dependency Freshness Standard — padrão de qualidade ponderado para frescor de linguagem/runtime/dependências críticas, pedido direto do Marcelo depois de D-137/D-138 revelarem que o projeto rodava `nodejs20.x` meses após depreciação AWS sem nenhum mecanismo detectar isso além de auditoria ad-hoc. Protocolo Claude↔Codex completo, **5 rodadas** (Rodada 1: régua 5,8/10 design 7,4/10, REABRIR — checklist sem pesos/âncoras contestado sob E-014; regra "1 major atrás" rejeitada por instabilidade semântica; Rodada 2: 8,8/10 — corrigido para "LTS suportada + horizonte mínimo 6 meses até EOL", tabela de versão pinada manual rejeitada (duplicaria fonte de verdade, resolvido com checker que LÊ manifests reais), modelo híbrido Dependabot+checker adotado em vez de tratá-los como alternativas; Rodada 3: 8,8/10 — política vinculada à LINHA detectada (não ao nome solto), cobertura inicial expandida de 3 para 9 itens críticos reais, descoberta Terraform corrigida para recursiva (`infra/versions.tf` não existe, são 22 arquivos); Rodada 4: 8,8/10 — separado inventário crítico (sempre presente) de política de lifecycle (só quando EOL oficial datado existe, nunca fabricado) após achar que "Dependabot cobre 100%" era falso para Node/Lambda/ADOT/Terraform CLI; Rodada 5: Claude 9,4/Codex 9,6 — `reviewedAt` de nível superior adicionado a toda entrada, resolvendo a lacuna de itens sem `lifecycle` não terem campo de revisão periódica). **Achado real produzido pelo próprio protocolo, corrigido ao vivo**: a verificação cruzada que a Rodada 2 exigiu (`package-lock.json` no matcher do Node) encontrou o bloco raiz `engines.node` ainda em `"20.x"` mesmo depois de D-137 ter atualizado `package.json`/`.nvmrc` para `24` — `npm install --package-lock-only` corrigido na hora, prova viva do valor do standard antes de implementado. Também confirmado: `ci.yml` hardcoda `terraform_version: "1.15.8"` separado da `TERRAFORM_VERSION` env de `cd.yml` — inconsistência real nomeada para a implementação. **Pesquisa externa (SIM, E-014)**: GOV.UK Service Manual "Keeping software current" (regra-base "nunca EOL", "não mais que 2 majors atrás" — adaptado para "6 meses até EOL" após crítica de instabilidade semântica); achado de mercado 2026 nomeando Node.js 20 como exemplo do ciclo massivo de EOL do ano — o mesmo runtime que este projeto tinha desatualizado. **Decisão**: `docs/engineering/dependency-freshness-standard.md` novo, mesma relação com o Domínio G (Software Supply Chain, 6%) de `01-engineering-quality-criteria.md` que `test-engineering-standard.md` tem com o Domínio C; sub-rubrica 5 critérios ponderados (25% cobertura/fonte-de-verdade, 25% lifecycle/horizonte, 25% descoberta independente, 15% resposta proporcional por risco, 10% verificabilidade) + 4 gates binários; modelo híbrido Dependabot (`npm`/`github-actions`/`terraform`, sem auto-merge) + checker determinístico (`scripts/check-dependency-freshness.ts`, matchers de código por item, nunca heurística textual) para os itens que Dependabot não descobre (Node/Lambda runtime/ADOT/Terraform CLI — mudança de linguagem/runtime gerenciado, não bump de pacote); inventário crítico inicial de 9 itens tipados (`CriticalDependencyEntry`: `id`/`detectedFrom`/`owner`/`officialSource`/`discoveryMechanism`/`reviewedAt`/`lifecycle?`). **DESIGN-ONLY** — implementação real (`.github/dependabot.yml`, o script checker, popular as entradas reais, corrigir a inconsistência `ci.yml`/`cd.yml`) fica para sessão futura dedicada, mesmo padrão D-121/D-127/D-136. | Type 1 (nível 5-6, novo padrão de processo/qualidade que muda como toda sessão futura avalia risco de dependência) | 9,4 | 9,6 | **APPROVED** (design, 5 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/engineering/reviews/dependency-freshness-scoping/` (round1 a round5, `estado-final-consolidado.md`), `docs/engineering/dependency-freshness-standard.md` | 2026-08-31 |
160:| D-140 | D-136/D-A implementado: sessão única no frontend + UX de loading sem terminologia técnica. `AuthContext` deixa de fazer sua própria chamada imperativa a `fetchSessionInfo()` e passa a derivar `AuthState` da mesma query TanStack (`sessionQueryKey`, `staleTime: 30_000`) que `ActiveOrganizationContext` já usa (também alinhado de `staleTime: 0` para `30_000`) — elimina o waterfall real de 2 chamadas sequenciais a `/bff/session` confirmado em D-136. As 2 mensagens técnicas sequenciais (`"Verificando sua sessão…"`/`ProtectedRoute.tsx`, `"Carregando sua organização…"`/`OnboardingGate.tsx`) foram substituídas pelo componente `InitialLoading` já existente (`"Carregando…"` genérico), reaproveitado nos dois pontos — resolve as duas queixas originais do Marcelo (lentidão + mensagem desnecessária) com uma mudança pequena. `logout()`/`logoutEverywhere()` limpam o cache de dados tenant-scoped (`queryClient.removeQueries({queryKey:["org"]})`) e fixam a sessão como `{authenticated:false}` via `setQueryData` direto. **Achado real de corrida encontrado pelo próprio G-V3**: a primeira versão usava `queryClient.removeQueries({queryKey: sessionQueryKey})` antes de fixar o valor final em `reportUnauthorized`/`logout` — como a `useQuery` do `AuthProvider` continua montada e observando essa chave, remover a entrada do cache faz o TanStack Query refazer a busca automaticamente para satisfazer o observer ainda ativo, correndo contra a escrita seguinte; isso reproduziu como um teste E2E de logout travando em timeout de 30s (a query refeita trazia `authenticated:true` de volta do mock antigo, sobrescrevendo o valor correto). Corrigido usando só `setQueryData` (determinístico, sem gatilhar refetch) para a chave de sessão — `removeQueries` continua sendo usado só para o namespace `["org"]`, que não tem esse mesmo observer ativo no momento do logout. **G-V3 adicional (achado honesto, não escondido)**: um teste unitário que tentava provar "dedup de 2 consumidores" foi escrito, mas não conseguiu ser feito sensível à mutação de `staleTime` de volta a `0` mesmo com tentativas de sincronização de timing em jsdom — removido em vez de deixado como teste falso-positivo; a evidência real do fix é o teste E2E de logout, que caiu de timeout(30s) para 461ms de forma reprodutível antes/depois da correção. **Suíte**: frontend `typecheck`/`lint` limpos, `npm test` 171/171, `npm run test:e2e` 24/24 (incluindo o antes-travado teste de logout), `npm run test:visual` 10/10 sem alteração de baseline; raiz `check-boundaries`/`check-docs` limpos. | Type 1 (nível 4-5, implementação direta de decisão de arquitetura já aprovada em D-136) | n/a (implementação pós-D-136) | n/a | **IMPLEMENTADO** — D-A completo; D-B/D-C/D-D/D-E de D-136 seguem pendentes | `frontend/src/auth/{AuthContext.tsx,ActiveOrganizationContext.tsx,ProtectedRoute.tsx,OnboardingGate.tsx}`, `frontend/test/auth/{AuthContext.test.tsx,OnboardingGate.test.tsx}` | 2026-08-31 |
163:| D-143 | Domínio Documental (arquivamento/versionamento/requisitos/guest upload/renovação) — arquitetura técnica `APPROVED` via protocolo Claude↔Codex, implementando as decisões funcionais D1–D10/C1–C6 já `APROVADO COMO DIREÇÃO FUNCIONAL INICIAL` pelo Marcelo (`docs/frontend/document-domain-functional-decisions.md`, não reaberto). Pedido explícito do Marcelo: analisar os 6 documentos de planejamento em `docs/frontend/document-domain-*.md`, pesquisar externamente, rodar o protocolo, só então implementar. **6 rodadas** (evolução de nota: 4,6→5,9→7,6→8,4→8,8→9,3): Rodada 1 (régua E-014 2,5/10, design 4,8/10, REABRIR) — sem URLs/datas/checklist reais, state machines eram enumeração de estados sem grafo comando/ator/precondição, ACL por responsável presumida (RBAC real é tenant-wide), guest/magic link com bloqueio crítico (TTL contraditório com prazo de Request, hash-só sem selector, `security-audit.ts` citado incorretamente), `Requirement.status` tratado como 100% derivado quando `NOT_APPLICABLE` é fato persistido, contradição direta com J9 (permitia remover Version `REJECTED`), recorrência perdendo identidade de série na correção; Rodada 2 (régua 5,4/10, design 5,9/10, REABRIR) — corrigiu grafo/E-014/guest 3-camadas/`DocumentVersionEvent`, mas introduziu colisão real com GSI3/GSI4 (já ocupados por reminder scheduler/membership, IAM isolado, confirmado por grep) — achado mais grave da rodada, incluindo uma correção factual rara (Codex havia alegado incorretamente que GSI2 já estava em uso; grep no código real mostrou GSI2/GSI5 livres); Rodada 3 (régua 5,4/10 mantida, design 7,6/10, REABRIR) — GSI2/GSI5 adotados, access patterns completos, mas fences entre itens não provavam vínculo real, `TransactWriteItems` não pode ler GSI dentro de si mesma, idempotência sem resultado original persistido, `pendingFileScans=0` não distinguia limpo de infectado, retenção com classes "equivalente"/"a definir" em vez de mapeamento real, recorrência sem fence de concorrência; Rodada 4 (8,4/10) — lista transacional de 10 ações formalizada, `assertReviewerOrAdmin()` como contrato próprio (não estende `authorize()`), `infectedFileScans` separado, mas condição idempotente usava atributo inexistente (`PK_SK`), retenção ainda incoerente com o texto real de `privacy-lgpd.md`, recorrência com lacuna real entre avanço de ponteiro e criação de Request; Rodada 5 (8,8/10) — 3 de 4 corrigidos (idempotência via `attribute_not_exists(PK)`/`(SK)`, lista de 10 ações literal, `materializeAttempt` atômico fechando a lacuna de recorrência), mas sub-prazos de retenção (7d/14d) apresentados como herdados quando eram extensão nova não presente no texto normativo; Rodada 6: Claude 9,3/Codex 9,3, **APPROVED** — sub-prazos assumidos explicitamente como decisão nova (não herança), erro editorial "5 classes" corrigido para 3 classes usadas das 9 reais de `privacy-lgpd.md` §4. **Pesquisa externa por decisão (E-014)**: SIM (guest/magic link — NIST SP 800-63B-4 §5.1.2, OWASP Forgot Password/Password Storage/Session Management/CSRF Cheat Sheets); SIM PARCIAL (armazenamento — princípio current/history de MongoDB/Cosmos DB Document Versioning Pattern, schema físico interno); NÃO (demais 7 decisões — composição de padrões já `APPROVED` no projeto). **9 decisões técnicas fechadas**: (1) state machines Document/DocumentVersion com grafo comando/ator/precondição/efeito transacional, fluxo interno comprimido C3 como transação única (elimina falha parcial); (2) armazenamento single-table com GSI1 discriminado + GSI2/GSI5 novos (confirmado livres, nunca GSI3/GSI4/GSI6 já ocupados), transação `acceptVersion` de até 10 ações com fences de vínculo fundidos e idempotência real (`payloadHash`+`resultSnapshot`); (3) `DocumentVersionEvent` append-only (distinto de `OutboxEvent`) como única auditoria, `DocumentVersion.state` única fonte de verdade mutável; (4) guest access em 3 camadas (`RequestAccessCredential` longa/revogável + `GuestSession` curta com interstitial obrigatório + submit idempotente), CSRF/Referrer-Policy/rate limit multidimensional; (5) `Requirement.applicability` como fato persistido, `evidenceVersionId` singular, status derivado deterministicamente + job diário de reindexação; (6) arquivos com scan individual (`pendingFileScans`/`infectedFileScans`), imutabilidade pós-`ACCEPTED`; (7) `REJECTED` nunca removível (corrige contradição com J9), retenção mapeada às classes reais de `privacy-lgpd.md` (`USER_DOCUMENT`/`SECURITY_AUDIT`/`TRANSIENT`); (8) recorrência com `seriesId`/`occurrenceId`/`requestId`/`attemptIndex`/`parentRequestId`, materialização atômica via `materializeAttempt`; (9) RBAC tenant-wide reaproveitado (`assertReviewerOrAdmin()` como camada de serviço separada), `extraction:confirm` reaproveitado para IA, download por Viewer (A1) genuinamente deixado aberto. **DESIGN-ONLY** — implementação real (Núcleo 1: Document/DocumentVersion/DocumentFile/DocumentVersionEvent) fica para sessão futura dedicada, mesmo padrão de D-121/D-127/D-136/D-139. | Type 1 (nível 5-6, novo domínio de dados/segurança sob E-014) | 9,3 | 9,3 | **APPROVED** (design, 6 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/document-domain-scoping/` (round1 a round6, `estado-final-consolidado.md`), `docs/frontend/document-domain-*.md` (base funcional, inalterada) | 2026-09-01 |
164:| D-144 | Domínio Documental, Núcleo 1 (Document/DocumentVersion) — implementado e deployado de ponta a ponta, seguindo a arquitetura `APPROVED` em D-143. Módulo `src/modules/document-archive/` completo: `domain/` (grafo de estado `DRAFT→RECEIVED→UNDER_REVIEW→{ACCEPTED,REJECTED}`, `ACCEPTED→SUPERSEDED`; `REJECTED` nunca removível/re-transicionável, fecha a contradição com J9 encontrada na Rodada 1 do protocolo), `ports/`+`persistence/` (porta genérica multi-índice, adapter DynamoDB real, GSI2/GSI5 novos confirmados livres por grep antes do uso — nunca GSI3/GSI4/GSI6 já ocupados por reminder/membership), `application/` (`DocumentArchiveService` com RBAC real via `authorize()` — novas actions `docarchive:create/read/upload/review` — e `assertReviewerOrAdmin()` como camada de serviço nomeada, separada de `authorize()`, para o gate de reviewer/admin da Decisão 1/Bloqueador 6 do design), `http/` (handlers no mesmo pipeline de `item-handlers.ts`), 6 schemas JSON novos registrados no `defaultSchemaRegistry`, composition root e Lambda handler. **Idempotência real**: `acceptVersion` verifica o registro de idempotência (chaveado por `clientRequestToken`, com `payloadHash`+`resultSnapshot`) ANTES de qualquer validação de transição — um replay legítimo contra uma version já `SUPERSEDED` por uma renovação posterior ainda retorna o resultado original, em vez de ser classificado erroneamente como transição inválida (achado real corrigido durante a implementação, não estava assim na primeira versão). **Achado real no próprio fake de teste**: a primeira versão de `test/unit/document-archive/in-memory-store.ts` avaliava CADA par nome/valor de `ExpressionAttributeNames`/`Values` como condição de transação, confundindo placeholders de SET com condições reais — fazia uma transição de estado normal parecer exigir que o item já estivesse no estado-alvo antes da escrita (backwards). Corrigido com um parser pequeno e real da gramática AND/OR (incluindo grupos `OR` entre parênteses) que os builders de `shared/dynamodb/occ.ts` de fato produzem, em vez de uma heurística de convenção de nomes. **G-V3 confirmado 3x ao vivo**: grafo de transição (mutação permitindo `REJECTED→ACCEPTED` pegou 2 testes corretamente), gate de scan de arquivo (mutação desabilitando o gate de aplicação ainda foi pega pelo fence da própria transação — defesa em profundidade confirmada), gate de reviewer/admin (mutação removendo a checagem de dono pegou 5 testes corretamente). **Infra real aplicada em `dev`**: Lambda `exptrk-dev-document-archive-handler` (sem política IAM dedicada — GSI2/GSI5 já cobertos pela política geral `tenant_facing_read_write_policy_json`) + 8 rotas `/document-archive/*` no API Gateway + `scripts/build-lambdas.ts`/`proxy-allowlist.ts` atualizados no MESMO commit da infra Terraform, evitando deliberadamente a classe de bug real já encontrada em D-117/D-120 (rota ou handler órfão). `terraform test` (suíte real de CI, `tests/stack.tftest.hcl`) 19/19; apply feito via plano `-target`-escopado (16 a adicionar, 0 a mudar, 0 a destruir) para evitar tocar ~76 diffs não-relacionados de `source_code_hash` que um `build:lambdas` completo gerou para todas as outras Lambdas; confirmado ao vivo via `aws lambda get-function` (`State=Active`) e `aws apigatewayv2 get-routes` (8 rotas presentes). **Suíte**: `npm test` 1345/1345 (30 testes de serviço/domínio + 10 novos de contrato + 4 de integração end-to-end reais), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos. | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO e DEPLOYADO** — Núcleo 1 completo; Núcleo 2 (Requirement/guest access/recorrência) segue pendente | `src/modules/document-archive/`, `src/modules/identity/domain/authorization.ts`, `schemas/api/docarchive-*.json`, `src/shared/contracts/schema-validator.ts`, `test/{unit/document-archive/,contract/schemas.test.ts,integration/document-archive-lifecycle.test.ts}`, `src/runtime/aws/{ids.ts,composition/document-archive.ts,handlers/document-archive-handler.ts}`, `infra/main.tf`, `infra/modules/api-gateway/{main.tf,variables.tf}`, `scripts/build-lambdas.ts`, `src/modules/bff/domain/proxy-allowlist.ts` | 2026-09-01 |
165:| D-146 | Domínio Documental, Núcleo 2, entidade 2/3 (guest access) — implementado seguindo a Decisão 4 `APPROVED` em D-143 (recorrência, entidade 3/3, segue para sessão futura dedicada). Estende `src/modules/document-archive/`: `domain/request-access-credential.ts` (`RequestAccessCredential` longa/revogável, TTL = prazo do `DocumentRequest`, pointer tenantless `DOCARCHIVEGUEST#<selectorHash>`/`POINTER`), `domain/guest-session.ts` (`GuestSession` de 30min, minted SÓ via ação humana explícita — nunca automaticamente a partir da credencial, mitiga falso-positivo de scanner de e-mail — mais CSRF double-submit com `csrfTokenHash` armazenado), `domain/document-request.ts` (shape mínimo NOVO e distinto do `DocumentRequest` mais antigo do módulo `subject` — id/subjectId/tenantId/requirementId/status/deadline, com ponto de extensão explícito para `seriesId`/`occurrenceId`/`attemptIndex`/`parentRequestId` da Decisão 8/recorrência), `application/document-archive-guest-rate-limiter.ts` (janela fixa multidimensional `requestId`+IP, duplicado deliberadamente do padrão de `subject/application/{guest-rate-limiter,initial-invite-rate-limiter}.ts` em vez de importado — evita acoplamento reverso ports→ports), `application/guest-document-access-service.ts` (`resolveCredential`/`startGuestSession`/`submitEvidence`, nunca toca `RequestContext`/`authorize()`), `http/document-archive-guest-handlers.ts` (3 rotas, `Referrer-Policy: no-referrer` em toda resposta, cookies HttpOnly/CSRF), 1 schema JSON novo, Lambda dedicada `document-archive-guest-handler.ts` (SEM Cognito, `authorization_type = NONE`, mesma postura de `guest-documents-handler.ts` do módulo `subject`/M10). **Desvio real do texto ilustrativo do design, documentado deliberadamente (não silencioso)**: D-143 Decisão 4 menciona "Argon2id" como exemplo de hash do `secret`; este código usa o MESMO padrão HMAC-SHA256+pepper versionado+`timingSafeEqual`+caminho dummy anti-timing já em produção em `subject/domain/guest-token.ts`, nunca uma dependência `argon2` nova — verificado por grep que o repositório inteiro não tem Argon2id em lugar nenhum. Justificativa (citada no comentário do próprio código, não só aqui): a pesquisa `SIM` de E-014 da Decisão 4 cita NIST SP 800-63B-4 §5.1.2 — memory-hardness existe para proteger segredo de BAIXA entropia (senha) contra força bruta offline; o `secret` aqui é 256 bits de `randomBytes`, alta entropia, onde um keyed hash simples já torna a busca offline inviável (2^256) sem o custo de CPU/memória do Argon2id em toda verificação legítima. **Anti-enumeração**: `GuestAccessInvalidError` único cobre TODO modo de falha (token malformado/inexistente/secret errado/expirado/revogado/rate-limitado/CSRF ausente ou incompatível/DocumentRequest não-live), nunca `details` populado, mesma disciplina de `GuestTokenInvalidError`. **Submit idempotente**: `submitEvidence` cria Document+DocumentVersion(RECEIVED, nunca auto-aceito — C2 de `document-domain-functional-decisions.md`)+DocumentVersionEvent+registro de idempotência (chaveado pelo `idempotencyKey` do CHAMADOR, distinto da credencial/sessão) numa única `TransactWriteItems` fenced via `executeTenantBusinessMutation` (mesmo padrão de `GuestSubmissionService.startSubmission`), replay do mesmo `idempotencyKey` retorna o `resultSnapshot` original sem criar segunda Version (confirmado por teste que conta itens `DocumentVersion` reais no store após replay). **G-V3 confirmado ao vivo**: mutação trocando a comparação `timingSafeEqual`-based por `===`/removendo o caminho dummy foi verificada quebrando o teste de comparação com pepper diferente (revertida); mutação removendo a checagem CSRF derrubou o teste de mismatch header/cookie (revertida); mutação zerando `submissionCount`/pulando o `Update` do `DocumentRequest` derrubou o teste de happy-path (revertida). **Suíte**: `npm test` (raiz) 1427/1427 (+33 testes novos: 7 de crypto de credencial, 5 de crypto de sessão, 5 de rate limiter, 13 de serviço incluindo 7 casos de anti-enumeração colapsando no mesmo erro, 3 de contrato de schema); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (39 handlers) limpos. **Infra**: HCL escrito (`infra/main.tf` — nova Lambda `document-archive-guest-handler` com pepper próprio `DOCARCHIVE_GUEST_ACCESS_PEPPER` via `random_password`, política = mesma `tenant_facing_read_write_policy_json` já usada por `guest_documents_handler` para itens tenantless; `infra/modules/api-gateway/{main.tf,variables.tf}` — 3 rotas públicas `/document-archive/guest/document-requests/{token}*`, `authorization_type = NONE`; `scripts/build-lambdas.ts` atualizado no mesmo commit, mesma disciplina anti-D-117/D-120), `terraform fmt`/`validate` limpos — **NÃO aplicado em `dev`** (`terraform apply`/`terraform test` explicitamente fora do escopo desta tarefa; a sessão orquestradora decide). | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO (código), infra planejada mas NÃO aplicada em `dev`** — Núcleo 2 entidade 2/3 completa; recorrência (entidade 3/3) segue pendente | `src/modules/document-archive/{domain/{request-access-credential.ts,guest-session.ts,document-request.ts},application/{document-archive-guest-rate-limiter.ts,guest-document-access-service.ts},http/document-archive-guest-handlers.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `schemas/api/docarchive-guest-submit-evidence-request.v1.json`, `src/shared/contracts/schema-validator.ts`, `src/runtime/aws/{composition/document-archive.ts,handlers/document-archive-guest-handler.ts}`, `test/{unit/document-archive/{request-access-credential.test.ts,guest-session.test.ts,document-archive-guest-rate-limiter.test.ts,guest-document-access-service.test.ts,in-memory-store.ts},contract/schemas.test.ts}`, `infra/{main.tf,modules/api-gateway/{main.tf,variables.tf}}`, `scripts/build-lambdas.ts` | 2026-09-01 |
166:| D-147 | Domínio Documental, Núcleo 2, entidade 3/3 (recorrência) — implementado seguindo a Decisão 8 `APPROVED` em D-143. **Fecha o Núcleo 2 (as 3 entidades — Requirement/D-145, guest access/D-146, recorrência/D-147 — estão completas) e, com Núcleo 1 (D-144) já completo, fecha as 9 decisões técnicas de D-143 por inteiro** — nenhum item das "9 decisões técnicas fechadas" de `estado-final-consolidado.md` segue sem implementação real (revisado item a item ao final desta tarefa). Estende `src/modules/document-archive/domain/document-request.ts` com `seriesId?`/`occurrenceId?`/`attemptIndex?`/`parentRequestId?` (todos aditivos/opcionais — o ponto de extensão nomeado explicitamente em D-146 — preservando o shape mínimo de guest access sem forçar um valor fabricado em todo `DocumentRequest` não-recorrente). Novo `domain/document-request-series.ts`: `DocumentRequestSeries` (série/ciclo, co-localizado sob a partição do Subject como `requirement.ts`), `computeSeriesOccurrenceId(seriesId, cycleStartAt)` — hash determinístico puro (reaproveita `stableHash` de `reminder/domain/reminder-occurrence.ts`, mesmo primitivo que `document-chasing.ts` já reaproveita) — e uma terceira namespace prefixo-discriminada `SERIESDUE` no MESMO GSI1 físico que `document.ts`/`requirement.ts` já usam (GSI3/GSI4/GSI6 confirmados por grep sem nenhum writer sob `document-archive/`, permanecem intocados). Novo `application/document-request-recurrence-service.ts`: `createSeries`/`getSeries`/`listSeries`/`cancelSeries`/`advanceCycle` (todas via `authorize()`, novas actions `docarchive:series-*`) e `materializeAttempt` — a operação central da Decisão 8, que avança `latestAttemptIndex`/`latestRequestId` da série E cria o novo `DocumentRequest` na MESMA `TransactWriteItems`, fechando a lacuna de falha parcial encontrada na Rodada 4 do protocolo original. O builder puro `buildMaterializeAttemptEntries` é exportado separadamente e reaproveitado pelo worker periódico (`src/workers/document-request-recurrence/materializer.ts`, mesmo split produtor/materializador de `subject/application/document-chasing-{producer,materializer}.ts`, aqui colapsado em um módulo por ser um job pequeno o bastante — mesma pragmática de `requirement-reindex/reindex.ts`) sem passar por `authorize()`/`RequestContext` (mesma postura do worker de reindex diário). **Desvio real, documentado**: o design menciona apenas `requestId`/`attemptIndex`/`seriesId`/`occurrenceId`/`parentRequestId` sem descrever como o worker descobre "o que está vencido" entre tenants — esta tarefa resolveu isso com um NOVO método de porta `scanActiveSeries` (Scan cross-tenant filtrado por `entityType`/`status`, mesmo tradeoff de custo já aceito e documentado por `scanSatisfiedRequirements` do worker de Requirement/D-145 — este módulo continua sem método de enumeração de tenants), em vez de reaproveitar a query GSI1 SERIESDUE por tenant (que exigiria uma lista de tenants que a porta não oferece). **G-V3 confirmado ao vivo**: mutação removendo a entry do `Put` do novo `DocumentRequest` da mesma transação (deixando só o `Update` da série) derrubou os 2 testes de atomicidade transacional (contagem de entries e leitura pareada série+request), revertida; mutação trocando o hash puro por um com `Math.random()` derrubou o teste de determinismo do `occurrenceId` (uma tentativa anterior com `Date.now()` NÃO foi pega — mesma chamada de teste roda no mesmo milissegundo — achado real documentado aqui, não escondido, substituído por `Math.random()` para a evidência real), revertida. **Suíte**: `npm test` (raiz) 1447/1447 (+14 testes novos: 11 de serviço/domínio incluindo o fluxo completo criar→materializar tentativa 1→tentativa 2→avançar ciclo→materializar tentativa 1 do novo ciclo, 3 do worker materializador incluindo idempotência contra tick duplicado); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (40 handlers) limpos. **Infra**: HCL escrito (`infra/main.tf` — nova Lambda `document-request-recurrence-handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário 04:30 UTC, após o reindex de Requirement às 04:00; `infra/modules/api-gateway/main.tf` — 5 rotas `/document-archive/series*` na mesma Lambda de Núcleo 1; `scripts/build-lambdas.ts`/`proxy-allowlist.ts` atualizados no mesmo commit, mesma disciplina anti-D-117/D-120), `terraform fmt`/`validate` limpos, `terraform test` (`AWS_PROFILE=claude-dev`) 20/20 sem regressão — **NÃO aplicado em `dev`** (`terraform apply` explicitamente fora do escopo desta tarefa; próxima sessão decide se aplica). | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO (código), infra planejada mas NÃO aplicada em `dev`** — Núcleo 2 completo (3/3 entidades); as 9 decisões técnicas de D-143 estão todas implementadas | `src/modules/document-archive/{domain/{document-request.ts,document-request-series.ts},application/document-request-recurrence-service.ts,http/document-archive-handlers.ts,application/id-generator.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `src/modules/identity/domain/authorization.ts`, `schemas/api/docarchive-series-*.json`, `src/shared/contracts/schema-validator.ts`, `src/workers/document-request-recurrence/materializer.ts`, `src/runtime/aws/{ids.ts,composition/document-archive.ts,handlers/{document-archive-handler.ts,document-request-recurrence-handler.ts}}`, `test/{unit/document-archive/{document-request-recurrence.test.ts,document-request-recurrence-materializer.test.ts,in-memory-store.ts},contract/schemas.test.ts,integration/document-archive-lifecycle.test.ts}`, `infra/{main.tf,modules/api-gateway/main.tf}`, `scripts/build-lambdas.ts`, `src/modules/bff/domain/proxy-allowlist.ts` | 2026-09-01 |
169:| D-145 | Domínio Documental, Núcleo 2, entidade 1/3 (Requirement) — implementado seguindo a Decisão 5/D9 `APPROVED` em D-143 (guest access e recorrência ficam para sessões futuras dedicadas, não implementados aqui). Estende `src/modules/document-archive/`: `domain/requirement.ts` (`applicability` fato persistido nunca derivado, `evidenceVersionId` singular, `deriveRequirementStatus` pura — MISSING/PENDING/SATISFIED/NOT_SATISFIED/NOT_APPLICABLE — e `isRequirementExpiringSoon` como subdivisão de leitura de SATISFIED, nunca persistida, mesma janela de 7 dias de `frontend/src/api/presentation.ts`'s `presentItemUrgency`/`SOON_THRESHOLD_DAYS`), `application/document-archive-service.ts` (createRequirement/getRequirement/listRequirements/updateRequirement/linkEvidence/unlinkEvidence/deleteRequirement, todas via `authorize()`), `http/` (7 rotas novas), 5 schemas JSON novos, worker diário `src/workers/requirement-reindex/` + handler dedicado (EventBridge Scheduler, `cron(0 4 * * ? *)`, mesmo padrão de `tenant-purge-sweep.ts`). **Desvio real do design original, documentado (não escondido)**: `Requirement` denormaliza `evidenceDocumentId`/`evidenceSeq`/`evidenceState`/`evidenceValidUntil` junto de `evidenceVersionId` no momento do link, em vez de resolver a DocumentVersion via `versionLookupGsi5Keys` (AP11, reservado exatamente para isso na Decisão 2) — esse par de atributos GSI5PK/GSI5SK já é usado, de forma esparsa, pelo índice de fila de revisão (`reviewQueueGsi5Keys`) e é REMOVIDO no exato momento em que uma version chega a `ACCEPTED`, o único estado que um Requirement realmente precisa localizar. Ocupar o mesmo par permanentemente quebraria a fila de revisão; a denormalização evita o conflito sem tocar o contrato existente do GSI5, e também permite ao worker diário e a `updateRequirement` (numa mudança de `applicability`) re-derivar `status` sem nenhuma leitura extra. `linkEvidence` por isso recebe `documentId` explicitamente do chamador em vez de resolver a partir de `versionId` sozinho — sinalizado aqui como questão em aberto para quem implementar guest access/recorrência, caso um fluxo futuro precise de "link por versionId apenas". **RBAC**: novas actions `docarchive:requirement-create/read/update/delete`, deliberadamente não reaproveitando as actions `requirement:*` já existentes (`identity/domain/authorization.ts`) — aquelas pertencem ao conceito mais antigo `RequirementAssignment` do módulo `subject`, um agregado distinto (vinculado a `ExpirationItem`, não a `DocumentVersion`); a colisão de nome foi resolvida deliberadamente, nunca reaproveitada silenciosamente. **G-V3 confirmado ao vivo**: branch `NOT_APPLICABLE` de `deriveRequirementStatus` (mutação removendo o `if` derrubou o teste correspondente, revertida); ponto de chamada de `linkEvidence`/`status` no serviço (mutação fixando `status` em `"MISSING"` derrubou 2 testes de service-level, revertida). **Suíte**: `npm test` (raiz) 1394/1394 (+49 testes novos: 18 unitários de domínio, 14 de serviço, 10 de contrato, 6 de worker, 1 de integração end-to-end via HTTP real); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (38 handlers) limpos. **Infra**: HCL escrito (`infra/main.tf` — Lambda + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule`; `infra/modules/api-gateway/main.tf` — 7 rotas na mesma Lambda de Núcleo 1; `scripts/build-lambdas.ts`/`proxy-allowlist.ts` atualizados no mesmo commit da infra, mesma disciplina anti-D-117/D-120 de D-144), `terraform validate`/`fmt -check` limpos, `terraform test` (`AWS_PROFILE=claude-dev`) 20/20 incluindo o novo run de contrato do schedule — **NÃO aplicado em `dev`** (`terraform apply` explicitamente fora do escopo desta tarefa; próxima sessão decide se aplica). | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO (código), infra planejada mas NÃO aplicada em `dev`** — Núcleo 2 entidade 1/3 completa; guest access e recorrência (entidades 2/3 e 3/3) seguem pendentes | `src/modules/document-archive/{domain/requirement.ts,application/document-archive-service.ts,http/document-archive-handlers.ts,application/id-generator.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `src/modules/identity/domain/authorization.ts`, `schemas/api/docarchive-requirement-*.json`, `src/shared/contracts/schema-validator.ts`, `src/workers/requirement-reindex/reindex.ts`, `src/runtime/aws/handlers/requirement-reindex-handler.ts`, `test/{unit/document-archive/{requirement.test.ts,requirement-service.test.ts,in-memory-store.ts},unit/workers/requirement-reindex.test.ts,contract/schemas.test.ts,integration/document-archive-lifecycle.test.ts}`, `infra/{main.tf,modules/api-gateway/main.tf,tests/stack.tftest.hcl}`, `scripts/build-lambdas.ts`, `src/modules/bff/domain/proxy-allowlist.ts` | 2026-09-01 |
170:| D-150 | Quarentena/Recuperação (D-127 design, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real do MECANISMO de quarentena apenas (os 7 workers de purga LGPD continuam explicitamente fora de escopo, um de cada vez em sessão futura, ordem já decidida em D-127). Novo estado `HELD_FOR_RECOVERY` em `tenant-lifecycle-record.ts`, inserido entre `ACTIVE` e `DELETING` — única aresta de retorno a `ACTIVE` no grafo (`HELD_FOR_RECOVERY -> ACTIVE`, cancelamento), todo o resto do grafo permanece forward-only exatamente como antes (regressão testada: toda transição antes inválida continua inválida). Três campos novos persistidos (`recoveryDeadline`/`closureAttemptId`/`executionArn`), `system-mutation.ts` ganha o carimbo desses campos na transição `ACTIVE -> HELD_FOR_RECOVERY` e uma nova operação estreita no allowlist fechado (`ATTACH_LIFECYCLE_EXECUTION_ARN`, condicionada a status+closureAttemptId, sem OCC de versão — mesmo raciocínio de `PURGE_DELETE`). `CloseOrganizationService` para em `HELD_FOR_RECOVERY` (não mais `DELETING` direto), nome de execução muda de `tenantId` para `${tenantId}-${closureAttemptId}` (D-127 round-2, evita colisão de nome numa 2ª tentativa após cancelamento real). Reusa o MESMO Step Functions de D-124 — `tenant-purge.asl.json` ganha `WaitForRecovery` (2592000s, novo `StartAt`) → `AdvanceToDeleting` (HELD_FOR_RECOVERY→DELETING) → `CheckCancelled` (Choice de re-verificação imediatamente antes da 1ª ação física irreversível, decisão explícita do design) → `RecoveryCancelled` (Succeed limpo) ou segue para o `WaitForQuiescence` já existente, inalterado. `CancelOrganizationClosureService` novo (`src/modules/organization/application/cancel-organization-closure.ts`): caminho de resolução de identidade dedicado (`IdentityMapping`→`GlobalUser`→`Membership`, NUNCA `RequestContextResolver` — estruturalmente impossível de construir um `RequestContext` para um tenant `HELD_FOR_RECOVERY`, já que `resolveWorkingOrganization()` exige lifecycle `ACTIVE`), `authorizeCancelClosure()` dedicado (não `authorize()`, mesma razão), e `StopExecution` SEMPRE antes de qualquer escrita de restauração — ordem verificada por G-V3 (teste de ordem de chamadas, mutação invertendo a ordem derrubou 3 asserções, revertida). Sweeper (`tenant-purge-sweep.ts`) ganha reconciliação de `HELD_FOR_RECOVERY`: conjunção estrita (`ABORTED` + `closureAttemptId` da execução bate com o do registro + registro ainda `HELD_FOR_RECOVERY`) só então completa um cancelamento truncado (StopExecution já rodou mas a escrita `HELD_FOR_RECOVERY -> ACTIVE` nunca commitou); qualquer outra combinação (`FAILED`/`TIMED_OUT`/`SUCCEEDED`/`NOT_FOUND`, ou `closureAttemptId` divergente) alarma (mesmo idioma de log já usado por `tenantsWithResidue`) e NUNCA restaura — G-V3 confirmado ao vivo (mutação aceitando `FAILED` como seguro derrubou o teste correspondente, revertida). Legal hold: `HELD_FOR_RECOVERY -> HELD` (nunca `BLOCKED`) preserva `recoveryDeadline`/`closureAttemptId` intocados — testado explicitamente, incluindo o ciclo completo hold→resume de volta a `HELD_FOR_RECOVERY` com o mesmo prazo original. Rota HTTP nova `POST /organizations/cancel-close` (Terraform + BFF proxy allowlist no mesmo commit, disciplina anti-D-117/D-120), RBAC `organization:cancel-close` (OWNER_ROLES, mesmo tier de `organization:close`). **Suíte**: `npm test` (raiz) 1499→1537 (+38 testes novos: grafo/transições, `system-mutation` (incluindo o novo `ATTACH_LIFECYCLE_EXECUTION_ARN`), `CloseOrganizationService` atualizado, `CancelOrganizationClosureService` completo, `authorizeCancelClosure`, `lifecycle-transition`'s branch `cancelled`, sweeper); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (40 handlers) limpos. **Infra**: `tenant-purge.asl.json` estendido, 2 `data.aws_iam_policy_document` novos (`tenant_purge_stop_execution` no handler de memberships, `tenant_purge_describe_execution` no sweeper — cada um escopado a exatamente um padrão de ARN de execução, nunca a state machine inteira, nunca `states:*`), rota `POST /organizations/cancel-close` em `infra/modules/api-gateway/main.tf`. `terraform fmt -check`/`validate` limpos; `terraform test` (`AWS_PROFILE=claude-dev`): módulo `tenant-purge-workflow` 11/11, stack raiz 20/20 — **NÃO aplicado em `dev`** (`terraform apply` fora do escopo desta tarefa). | Type 1 (nível 4-5, implementação direta do mecanismo já `APPROVED` em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (mecanismo de quarentena apenas), infra planejada mas NÃO aplicada em `dev`** — os 7 workers de purga LGPD (prioridade já decidida em D-127) seguem pendentes, um de cada vez em sessão futura | `src/shared/tenant-lifecycle/{tenant-lifecycle-record.ts,system-mutation.ts,tenant-purge-execution-starter.ts,sfn-tenant-purge-execution-starter.ts,tenant-purge-execution-stopper.ts,sfn-tenant-purge-execution-stopper.ts,tenant-purge-execution-describer.ts,sfn-tenant-purge-execution-describer.ts}`, `src/modules/organization/{application/{close-organization.ts,cancel-organization-closure.ts},http/organization-lifecycle-handlers.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition/{organization.ts,tenant-purge.ts},handlers/{memberships-handler.ts,tenant-purge-sweeper-handler.ts}}`, `src/workers/tenant-purge/{lifecycle-transition.ts,tenant-purge-sweep.ts}`, `infra/{main.tf,state-machines/tenant-purge.asl.json,modules/api-gateway/main.tf,tests/stack.tftest.hcl,modules/tenant-purge-workflow/tests/tenant_purge_workflow.tftest.hcl,modules/api-gateway/tests/api_gateway.tftest.hcl}`, `test/{unit/{tenant-lifecycle.test.ts,system-mutation.test.ts,organization/{close-organization.test.ts,cancel-organization-closure.test.ts},identity/authorization.test.ts,workers/tenant-purge/{lifecycle-transition.test.ts,tenant-purge-sweep.test.ts}},architecture/system-mutation-allowlist.test.ts}` | 2026-09-01 |
171:| D-151 | `CORE_USER_DATA` LGPD purge worker (Prioridade 1 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 6 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `DELIVERY_RECORD`/`SECURITY_AUDIT`/`QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `CORE_USER_DATA` = "itens, políticas, ocorrências" mapeado ao código real: `ExpirationItem`/`ReminderPolicy` (ambos já têm `deletedAt` opcional, nunca fisicamente purgados hoje fora do fechamento de tenant inteiro) recebem um worker Lambda diário novo, `ReminderOccurrence` recebe TTL nativo DynamoDB independente (não um worker), exatamente como o design pediu ("worker(s) por deletedAt+30d, ReminderOccurrence via TTL nativo independente do pai"). **Mecanismo, ExpirationItem/ReminderPolicy** (`src/workers/core-user-data-purge/`): candidato descoberto por `Scan` full-table filtrado por `entityType IN (ExpirationItem, ReminderPolicy) AND attribute_exists(deletedAt)` — deliberadamente NÃO um worklist GSI6 (mesmo padrão `document-purge`'s claim/lease usa): GSI6 é um limite de isolamento fechado com 4 consumidores já nomeados explicitamente em `infra/tests/stack.tftest.hcl`, e nenhuma das duas entidades tem efeito colateral externo (S3, lease) a proteger — o mesmo tradeoff de custo que `tenant-purge-sweep.ts` já aceita e documenta a este porte de projeto. Elegibilidade exige DUAS condições independentes: (1) `deletedAt` com pelo menos 30 dias (`privacy-lgpd.md` §4, "exclusão/encerramento + 30 dias"); (2) tenant dono com `TenantLifecycleRecord.status === "ACTIVE"` (lido via `GetItem` consistente na própria `TenantLifecycleRecord`, cacheado por execução) — um tenant em qualquer outro estado (incluindo registro AUSENTE, tratado fail-closed, nunca assumido `ACTIVE`) é explicitamente o trabalho do pipeline de fechamento de tenant (`src/workers/tenant-purge/`), um mecanismo TOTALMENTE separado deste. O delete físico é um `DeleteItem` único condicionado via `buildVersionedDelete` (versão esperada + `deletedAt = :deletedAt` exato observado no scan) — a re-asserção de `deletedAt` é a propriedade de correção mais importante deste worker (nenhum caminho de restauração existe hoje para nenhuma das duas entidades, mas a condição torna isso estruturalmente seguro em vez de "seguro só porque nada faz isso ainda"). **Mecanismo, ReminderOccurrence** (`src/modules/reminder/domain/reminder-occurrence.ts`): campo novo `purgeAfterTtl` (epoch seconds), mesma convenção já usada por `InvitationTokenPointer.purgeAfterTtl` e já habilitada como atributo TTL real da tabela inteira (`infra/modules/dynamo-table/main.tf`, `ttl.attribute_name = "purgeAfterTtl"` — nenhuma mudança de infra necessária, o atributo já é físico há muito). Calculado da PRÓPRIA `scheduledAt` da ocorrência + 30 dias, setado UMA VEZ em `reminder-materializer.ts` no momento da materialização — deliberadamente nunca recomputado a partir do `deletedAt` do item/policy pai, porque uma ocorrência é naturalmente limitada pelo seu próprio agendamento independentemente do que acontece depois com o pai (dispensa qualquer worker). **Questão em aberto, sinalizada e não resolvida aqui**: `ReminderOccurrence`s materializadas ANTES desta mudança não têm `purgeAfterTtl` retroativo — nenhum backfill foi implementado nesta sessão (nenhum mecanismo de migração em massa precedente existe neste código para um caso análogo); o volume real de ocorrências pré-existentes ainda distantes de `scheduledAt` é pequeno o bastante para não justificar um script dedicado agora, mas fica nomeado aqui para quem revisitar o assunto. **Infra**: novo módulo Lambda `core_user_data_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 5 * * ? *)`, 05:00 UTC, depois de `document-request-recurrence` às 04:30) em `infra/main.tf`, mesmo padrão de `requirement_reindex_handler`/`document_request_recurrence_handler` — sem policy dedicada além da leitura/escrita geral tenant-facing (Scan de base table + GetItem em row tenant-scoped comum, nenhum GSI3/GSI6 envolvido). `scripts/build-lambdas.ts` ganha a entrada `core-user-data-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `deletedAt = :deletedAt` do delete (simulando ausência da re-asserção) faria o teste de "restore/undelete durante a corrida" passar mesmo com um restore concorrente — confirmado quebrando deliberadamente essa extraCondition no teste, revertida; mutação trocando `TENANT_ACTIVE_STATUS` por aceitar qualquer status derrubou 8 dos 9 casos parametrizados de status não-`ACTIVE`, revertida. **Suíte**: `npm test` (raiz) +14 testes novos (`test/unit/workers/core-user-data-purge.test.ts`: elegibilidade por idade incluindo fronteira exata, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra restore/undelete simulado e contra bump de versão concorrente, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan; +1 teste em `reminder-materializer.test.ts` para `purgeAfterTtl`); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`validate-schemas` limpos. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (CORE_USER_DATA apenas, Prioridade 1 de 7)** — as 6 classes restantes (`DELIVERY_RECORD`→`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/modules/reminder/domain/reminder-occurrence.ts`, `src/modules/reminder/application/reminder-materializer.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{core-user-data-purge.test.ts,core-user-data-purge-fakes.ts}`, `test/unit/reminder/reminder-materializer.test.ts`, `test/unit/reminder/reconciliation.test.ts` | 2026-09-01 |
172:| D-152 | `DELIVERY_RECORD` LGPD purge worker (Prioridade 2 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 5 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `SECURITY_AUDIT`/`QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `DELIVERY_RECORD` = "intents/attempts" mapeado ao código real: `NotificationIntent` (`src/modules/reminder/domain/notification-intent.ts`, `entityType: "NotificationIntent"`) e `NotificationAttempt` (`src/modules/notification/domain/notification-attempt.ts`, `entityType: "NotificationAttempt"`) — verificado por leitura direta das duas entidades, não só pelo nome. **Checagem de escopo explícita do task brief, confirmada, SEM confusão**: `AuditEvent`/`MembershipAuditEvent` (o feed do Admin Activity Log de D-149) são `SECURITY_AUDIT`, Prioridade 3, linha SEPARADA de `privacy-lgpd.md` §4 (linha 43, "criação + 365 dias") — nenhum dos dois é lido/escaneado/deletado por este worker; `candidate-source.ts`'s `entityType` union nomeia só `NotificationIntent`/`NotificationAttempt`. Mesmo layout de D-151 (`src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/delivery-record-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (não GSI6 — nenhuma das duas entidades tem efeito colateral externo a proteger com claim state). **Diferença de forma de elegibilidade vs. D-151 (achado central deste worker, nomeado no task brief)**: aqui é elegibilidade de UMA condição de idade (`createdAt` com pelo menos 180 dias, `privacy-lgpd.md` §4 "criação + 180 dias") — TODO registro de entrega eventualmente expira pelo prazo fixo de retenção, independentemente de qualquer ação de exclusão (ao contrário de `CORE_USER_DATA`, que só é elegível quando `deletedAt` já existe); um `NotificationIntent`/`NotificationAttempt` ainda `PENDING`/nunca cancelado purga igual, uma vez velho o bastante — testado explicitamente. A fence de tenant `ACTIVE` é idêntica à de D-151 (mesmo motivo: fechamento de tenant é trabalho do outro pipeline). O delete físico é um `DeleteItem` único condicionado via `buildVersionedDelete`; como não existe campo de negócio análogo a `deletedAt` para re-afirmar contra uma corrida de "restauração" (nenhuma das duas entidades tem caminho de undelete), a re-asserção OCC de `version` já é sozinha a fence completa de "não mudou desde o scan" — `createdAt` é re-afirmado como condição extra só como defesa em profundidade (nada no código hoje escreve `createdAt` depois da criação, mas a condição faz disso uma garantia estrutural, não um acidente de "nada faz isso ainda"). **Infra**: novo módulo Lambda `delivery_record_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(30 5 * * ? *)`, 05:30 UTC, depois de `core-user-data-purge` às 05:00) em `infra/main.tf`, mesmo padrão de `core_user_data_purge_handler` — sem policy dedicada além da leitura/escrita geral tenant-facing. `scripts/build-lambdas.ts` ganha a entrada `delivery-record-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a extraCondition `createdAt = :createdAt` do delete fez o teste de "createdAt mudou entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido) — confirmado quebrando deliberadamente essa extraCondition no teste, revertida; mutação trocando a comparação `=== TENANT_ACTIVE_STATUS` por `!== undefined` (aceita qualquer status presente) derrubou 8 dos 8 casos parametrizados de status não-`ACTIVE` + o teste de mistura de candidatos, revertida. **Suíte**: `npm test` (raiz) +19 testes novos (`test/unit/workers/delivery-record-purge.test.ts`: purga de cada tipo de entidade, elegibilidade por idade incluindo fronteira exata, purga independente de status de entrega, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `createdAt` e contra bump de versão concorrente, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (41 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (DELIVERY_RECORD apenas, Prioridade 2 de 7)** — as 5 classes restantes (`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida; `NotificationAttemptLookup` (pointer derivado do attempt) fica órfão após a purga do attempt correspondente, achado nomeado e não corrigido nesta sessão | `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{delivery-record-purge.test.ts,delivery-record-purge-fakes.ts}` | 2026-09-01 |
173:| D-154 | `QUOTA_TELEMETRY` LGPD purge worker (Prioridade 4 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — este era o primeiro das 7 prioridades sem escopo trivial (o task brief pediu investigação real, não confiança cega na lista preliminar). `privacy-lgpd.md` §4's `QUOTA_TELEMETRY` = "quotas/métricas identificáveis, MembershipInviteRateLimitRecord" (definição genérica + 1 exemplo nomeado) — as 5 entidades reais de rate-limit/quota do código foram lidas uma a uma: `MembershipInviteRateLimitRecord` (organization, O EXEMPLO NOMEADO), `GuestRateLimitRecord`/`InitialInviteRateLimitRecord` (subject), `DocumentArchiveGuestRateLimitRecord` (document-archive) e `TenantQuotaRecord` (identity). **Achado central**: 4 das 5 — incluindo o próprio exemplo nomeado no design doc — já carregam campo `purgeAfterTtl` calculado a partir de `resetAt` e já wireado ao TTL nativo real da tabela inteira (`infra/modules/dynamo-table/main.tf`, `ttl.attribute_name = "purgeAfterTtl"`, confirmado por leitura da infra, não assumido) — mesmo status "já resolvido" que `TRANSIENT`'s `InvitationTokenPointer` (`privacy-lgpd.md` linha 42); construir um worker novo para essas 4 duplicaria um mecanismo que já funciona. **A ÚNICA entidade genuinamente sem purga**: `TenantQuotaRecord` (`src/modules/identity/application/quota.ts`) — sem `purgeAfterTtl`, sem TTL, sem outro caminho de purga; é o token bucket de quota de negócio (API_REQUEST/UPLOAD_BYTES/UPLOAD_COUNT/AI_CALL/NOTIFICATION_EMAIL/IMPORT_*), distinto dos rate-limiters anti-abuso de convite/guest. **Campo de fim de janela**: `resetAt`, o próprio campo do token bucket de janela fixa que marca quando a janela atual fecha — exatamente o conceito "fim da janela" do design doc; elegibilidade = `resetAt + 30 dias <= now`. Escopo desta implementação: SÓ `TenantQuotaRecord`. Mesmo layout de D-151/D-152/D-153 (`src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (não GSI6 — sem efeito colateral externo a proteger). Fence de tenant `ACTIVE` idêntica às 3 anteriores. **Sem campo `version`** (mesma situação de D-153, motivo diferente): `TenantQuotaRecord` usa `count`/`resetAt` re-afirmados via `ConditionExpression` a cada escrita, nunca um contador `version` — `buildConditionalDelete` (já existente desde D-153) re-afirma `resetAt` exato observado no scan como fence de "não mudou desde o scan". **Infra**: novo módulo Lambda `quota_telemetry_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(30 6 * * ? *)`, 06:30 UTC, depois de `security-audit-purge` às 06:00) em `infra/main.tf`, mesmo padrão de `security_audit_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `quota-telemetry-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `resetAt = :resetAt` do delete fez o teste de "resetAt mudou (janela rolou) entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido) — confirmado quebrando deliberadamente essa condição no teste (`purged` foi de 0→1 espúrio), revertida. **Suíte**: `npm test` (raiz) 1614/1614 (+17 testes novos: `test/unit/workers/quota-telemetry-purge.test.ts` — elegibilidade por fim de janela incluindo fronteira exata de 30 dias, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `resetAt` e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (43 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (QUOTA_TELEMETRY apenas, Prioridade 4 de 7 — só `TenantQuotaRecord`; as outras 4 entidades da classe já resolvidas via TTL nativo pré-existente, sem código novo necessário)** — as 3 classes restantes (`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{quota-telemetry-purge.test.ts,quota-telemetry-purge-fakes.ts}` | 2026-09-01 |
174:| D-153 | `SECURITY_AUDIT` LGPD purge worker (Prioridade 3 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 4 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `SECURITY_AUDIT` = "AuditEvent/logs redigidos, MembershipAuditEvent" mapeado ao código real, confirmado por leitura direta dos 4 arquivos de domínio: `AuditEvent` (`src/modules/expiration/domain/audit-event.ts`), `MembershipAuditEvent` (`src/modules/organization/domain/audit-event.ts`), `SubjectAuditEvent` (`src/modules/subject/domain/audit-event.ts`) e `TenantAuditEvent` (`src/modules/activity/domain/tenant-audit-event.ts`, o 4º agregado-irmão que D-149 adicionou para fechar o gap de audit do export) — exatamente as 4 partições que `GET /activity`'s `ActivityService` já faz merge k-way (`AUDIT_PARTITIONS` de `activity/domain/merge.ts`), confirmando a suspeita do task brief de que este worker toca dado consumido por uma feature de leitura recém-lançada. **`occurredAt` é o relógio de idade para as 4, nunca um `createdAt` separado** — nenhum dos 4 domínios declara `createdAt`; o próprio header de `tenant-audit-event.ts` já registrava isso antecipadamente ("para esta entidade imutável, occurredAt É o relógio canônico equivalente a createdAt"), confirmado aqui por leitura de código, não assumido. **Desvio de forma vs. D-151/D-152, real e documentado**: nenhuma das 4 entidades tem campo `version` — são append-only por construção (cada arquivo de domínio já diz "não há update()/delete() exportado para esta entidade"); `buildVersionedDelete` (que sempre exige `expectedVersion`) não se aplica. Solução: `buildConditionalDelete` novo em `occ.ts`, irmão simétrico do `buildConditionalPut` já existente (mesmo padrão "Put condicionado sem version" espelhado para Delete) — o delete físico condiciona em `attribute_exists(PK) AND attribute_exists(SK) AND occurredAt = :occurredAt` (o valor exato observado no scan), a mesma defesa-em-profundidade de "não mudou desde o scan" que D-151/D-152 fazem com `version`, adaptada à ausência estrutural de um contador nestas linhas. **Normalização de tenant**: `MembershipAuditEvent` declara `organizationId`, não `tenantId` — mas `organizationId` É o tenant id neste código (mesma partição `TENANT#<organizationId>#...`); `dynamodb-candidate-source.ts` normaliza os dois nomes em um único `tenantId` no candidato, único lugar onde esse mapeamento acontece. **Investigação de corrida com paginação de `GET /activity` (pedida explicitamente no task brief), concluída SEM achado de bug**: o cursor composto de `ActivityService` guarda `{PK,SK}` reais por partição e os repassa como `ExclusiveStartKey` de `Query` na página seguinte; por leitura de `dynamodb-audit-partition-store.ts`, esse valor é só repassado ao SDK sem checagem de existência nem re-fetch — e o próprio contrato documentado do DynamoDB trata `ExclusiveStartKey`/`LastEvaluatedKey` como posição no B-tree, não como referência a um item que precisa continuar existindo. Conclusão registrada no header de `purge.ts`: nenhuma correção necessária; nenhum teste novo dedicado a esse achado específico (a convenção já estabelecida no código é manter adapters DynamoDB finos sem teste de SDK mockado, verificados por leitura, não por um teste avulso inconsistente com o resto do código). Mesmo layout de D-151/D-152 (`src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/security-audit-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (não GSI6). **Infra**: novo módulo Lambda `security_audit_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 6 * * ? *)`, 06:00 UTC, depois de `delivery-record-purge` às 05:30) em `infra/main.tf`, mesmo padrão de `delivery_record_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `security-audit-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `occurredAt = :occurredAt` do delete fez o teste de "occurredAt mudou entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido) — confirmado quebrando deliberadamente essa condição no teste, revertida; mutação trocando a comparação `=== TENANT_ACTIVE_STATUS` por `!== undefined` (aceita qualquer status presente) derrubou 8 dos 8 casos parametrizados de status não-`ACTIVE`, revertida. **Suíte**: `npm test` (raiz) 1597/1597 (+30 testes novos: `test/unit/occ.test.ts` +2 para `buildConditionalDelete`; `test/unit/workers/security-audit-purge.test.ts` +20 — purga de cada um dos 4 tipos de entidade incluindo a normalização `organizationId`→`tenantId` de `MembershipAuditEvent`, elegibilidade por idade incluindo fronteira exata de 365 dias, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `occurredAt` e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/4 tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (42 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20, módulo `tenant-purge-workflow` 11/11 (sanidade, não tocado por esta mudança). | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (SECURITY_AUDIT apenas, Prioridade 3 de 7)** — as 4 classes restantes (`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/shared/dynamodb/occ.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/{occ.test.ts,workers/{security-audit-purge.test.ts,security-audit-purge-fakes.ts}}` | 2026-09-01 |
175:| D-155 | `ACCOUNT_ACTIVE (não-fechamento)` LGPD purge worker (Prioridade 5 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — escopo real é **só `Invitation`**, não os 3 nomeados na linha da Prioridade 5 (`Invitation`→`Membership`→`Channel`). Distinção central investigada (mesma disciplina de D-154): `privacy-lgpd.md` §4 nomeia `ACCOUNT_ACTIVE` como `User, Organization, Membership, Invitation, Channel` com retenção "encerramento + 30 dias", mas `estado-final-consolidado.md` qualifica esta prioridade como `(não-fechamento)` — o fechamento pleno de um tenant já é 100% coberto pelo pipeline W3-07 (`src/workers/tenant-purge/`, D-081-124/D-150), então este worker nunca pode colidir com ele: a mesma fence de tenant `ACTIVE` de D-151/152/153/154 exclui estruturalmente qualquer tenant em fechamento. `User`/`Organization` ficam DE FATO fora de escopo — nenhum dos dois tem um estado "terminado sem ser fechamento do tenant" (a existência de um `User`/`Organization` individual terminar É fechamento, já é o outro pipeline). **`Invitation`** (`src/modules/organization/domain/invitation.ts`): 4 status declarados, só 3 têm writer real (`create-invitation.ts`/`accept-invitation.ts`/`revoke-invitation.ts`) — `EXPIRED` nunca é escrito por código nenhum; um `PENDING` cujo `expiresAt` passou fica `PENDING` para sempre, sem purga. Este worker trata isso como o estado terminal de-facto: candidato = `REVOKED` (usa `revokedAt`) OU `PENDING` com `expiresAt` já passado (usa o próprio `expiresAt` como marco de término, não há `expiredAt` separado nem falta nenhum). `ACCEPTED` nunca é candidato (sucesso, vira `Membership`, não é término). **`Membership` NÃO implementado — achado real de bloqueio**: `status: REMOVED` (`remove-membership.ts`/`leave-organization.ts`) nunca grava nenhum timestamp (`removedAt`/`updatedAt` não existem no tipo nem são setados no `UpdateExpression`, só `status`+`version`) — não há relógio de elegibilidade "termo + 30 dias" para checar; escrever essa lógica exigiria primeiro adicionar um campo novo a 2 write paths existentes, decisão além de "implementar o worker já aprovado", deixada para a sessão orquestradora decidir. **`Channel` NÃO implementado — não existe como entidade persistida**: nenhuma entidade `Channel` existe no código; a leitura literal do nome (`NotificationChannel`) é só um tipo string (`"EMAIL"`) embutido em `NotificationIntent`/`DeliveryRecord` (`DeliveryRecord` já é D-152), sem lifecycle próprio. O candidato mais plausível é `NotificationPreferences` (`src/modules/notification/domain/notification-preferences.ts`, 1 por usuário-tenant) — mas não tem estado terminal algum: nunca é tocado quando a `Membership` dona é removida (fica órfão, não marcado), herdando o mesmo bloqueio de relógio ausente de `Membership` e dependendo dele. Mesmo layout D-151/152/153/154 (`src/workers/invitation-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/invitation-purge-handler.ts`), mesmo tradeoff de `Scan` full-table. Fence de tenant `ACTIVE` idêntica às 4 anteriores. **Delete usa `version`** (diferente de D-153/D-154): `Invitation` tem contador `version` real — `buildConditionalDelete` reafirma `version` observado no scan (não `buildVersionedDelete`, que exige atributo `tenantId`, e `Invitation` usa `organizationId`). **Infra**: novo módulo Lambda `invitation_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 7 * * ? *)`, 07:00 UTC, depois de `quota-telemetry-purge` às 06:30) em `infra/main.tf`, mesmo padrão de `quota_telemetry_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `invitation-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `#version = :version` do delete (junto de `attribute_exists`) fez o teste de "version mudou (modificação concorrente) entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido, `purged` 0→1 espúrio) — confirmado quebrando deliberadamente essa condição no teste, revertida. **Suíte**: `npm test` (raiz) 1635/1635 (+21 testes novos: `test/unit/workers/invitation-purge.test.ts` — elegibilidade por `revokedAt`/`expiresAt` incluindo fronteira exata de 30 dias, `terminalTimestamp` por status incluindo `ACCEPTED` defensivo, `PENDING` ainda dentro da janela, `ACCEPTED` nunca vira candidato mesmo semeado direto no fake, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `version` e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (44 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO PARCIALMENTE (ACCOUNT_ACTIVE não-fechamento, Prioridade 5 de 7 — só `Invitation`; `Membership` e `Channel` bloqueados por timestamp de elegibilidade ausente, achado real de investigação, decisão de adicionar campo deixada para sessão futura)** — resta 1 prioridade pendente (`LEGAL_EVIDENCE`, bloqueada por trava jurídica) + a decisão de campo para `Membership`/`Channel` | `src/workers/invitation-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/invitation-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{invitation-purge.test.ts,invitation-purge-fakes.ts}` | 2026-09-01 |
176:| D-156 | `TRANSIENT` remanescente LGPD purge worker (Prioridade 6 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementa os 2 entidades ainda sem purga física da linha `TRANSIENT` de `privacy-lgpd.md` §4 (`WebhookInbox`, `UploadSlot, InvitationTokenPointer`); `InvitationTokenPointer` já é `purgeAfterTtl` nativo (`organization/domain/invitation-token.ts`), citado como já resolvido pela própria linha da doc-fonte — não tocado, nem reverificado. **`WebhookInbox`** (criado em `ses-callback-workflow.ts`'s `processSesCallback()` a cada callback SES/SNS recebido — idempotência por `(tenantId, providerAccountId, snsMessageId)`): investigado e confirmado SEM nenhum mecanismo de expiração — nenhum campo `purgeAfterTtl`, nenhum TTL nativo wireado, nenhum outro worker o toca; achado genuíno, mesma forma do `TenantQuotaRecord` de D-154. Relógio: `createdAt` (o momento em que o registro de idempotência foi persistido — `occurredAt`, o timestamp do próprio evento do provedor, é segundos antes e não é o campo ao qual a linguagem "7 dias" da doc está ancorada). **`UploadSlot`** (`src/modules/document/domain/upload-slot.ts`, M6): achado mais sutil — a entidade JÁ tem um campo `purgeAfter` e uma função pronta e já alinhada com a doc-fonte, `computeUploadSlotPurgeAfter()` (`document/domain/retention.ts`, "7 dias; slot incompleto: 24h" codificado literalmente), mas ambos são código morto: nenhum writer chama `computeUploadSlotPurgeAfter`, `purgeAfter` é setado uma única vez na reserva como o `expiresAt` de curtíssimo prazo e nunca recalculado, e — decisivo — `purgeAfter` é um atributo string ISO comum, não `purgeAfterTtl` (o nome real do atributo de TTL nativo da tabela, `infra/modules/dynamo-table/main.tf`), então o sweeper de TTL do DynamoDB nunca olha para ele. `UploadSlotReconciliationWorker` (`src/workers/upload-slot-reconciliation/`) só marca `status = EXPIRED` em slots `RESERVED` vencidos, nunca deleta a linha — não é um caminho de purga física, é um mecanismo diferente (liberação de quota + timeout do Document). Achado genuíno, mesma disciplina de D-154/D-155: reusa `computeUploadSlotPurgeAfter` verbatim (não reinventa a fórmula) em vez de escrever uma nova. Um slot ainda `RESERVED` NUNCA é candidato, seja qual for a idade — é uma reserva ativa em voo, nunca alvo de purga por idade sozinha (um sweep de reconciliação travado é um incidente operacional a corrigir, não motivo para apagar uma linha que um cliente ainda pode estar tentando confirmar); "incompleto" (janela 24h) = `status !== "CONSUMED"` (nunca chegou a `CONSUMED`), "confirmado" (janela 7 dias) = `status === "CONSUMED"` — exatamente o parâmetro `wasConfirmed` da função já existente. Um único worker (`src/workers/transient-purge/`) cobre as 2 entidades num só `Scan` (`entityType IN (WebhookInbox, UploadSlot)`), refletindo o agrupamento da própria doc-fonte numa única linha `TRANSIENT`. Mesmo layout D-151/152/153/154/155 (`candidate-source.ts`/`dynamodb-candidate-source.ts`/`purge.ts` + `src/runtime/aws/handlers/transient-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (nem `UploadSlot` nem `WebhookInbox` têm um GSI de descoberta dedicado a purga — o GSI6 de `UploadSlot` só existe enquanto `RESERVED`, removido antes de qualquer elegibilidade de purga). Fence de tenant `ACTIVE` idêntica às 5 anteriores. **Delete usa `version`** (como D-151/D-155): ambas as entidades têm contador `version` real e o atributo `tenantId` esperado por `buildVersionedDelete` (diferente de D-153/D-154/D-155, que usam `buildConditionalDelete` por falta de `version` ou por usarem `organizationId`). **Infra**: novo módulo Lambda `transient_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(30 7 * * ? *)`, 07:30 UTC, depois de `invitation-purge` às 07:00) em `infra/main.tf`, mesmo padrão de `invitation_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `transient-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição de `version` do delete (deixando só `attribute_exists(PK) AND attribute_exists(SK)`) fez o teste de "version mudou (modificação concorrente) entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido, `purged` 0→1 espúrio) — confirmado quebrando deliberadamente essa condição no worker, revertida antes do commit. **Suíte**: `npm test` (raiz) 1658/1658 (+23 testes novos: `test/unit/workers/transient-purge.test.ts` — elegibilidade de `WebhookInbox` por `createdAt` incluindo fronteira exata de 7 dias, `UploadSlot` `RESERVED` nunca candidato independente da idade, `UploadSlot` incompleto (`EXPIRED`) elegível em exatamente 24h e inelegível 1h antes, `UploadSlot` confirmado (`CONSUMED`) mantido na janela de 7 dias mesmo já passado das 24h, `RELEASED` seguindo a mesma janela de 24h de `EXPIRED`, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `version` — quebrada/revertida ao vivo — e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos `WebhookInbox`/`UploadSlot` elegíveis/inelegíveis entre tenants num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (45 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (TRANSIENT remanescente, Prioridade 6 de 7 — `WebhookInbox`+`UploadSlot`; `InvitationTokenPointer` já resolvido, não tocado)** — resta só 1 lane pendente (`LEGAL_EVIDENCE`, Prioridade 7, bloqueada por trava jurídica, nunca destravada por esta sessão) das 7 prioridades LGPD originais de D-127 | `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{transient-purge.test.ts,transient-purge-fakes.ts}` | 2026-09-01 |
179:| D-158 | `Membership.removedAt` (achado de D-155, gate nomeado em `NEXT_SESSION_PROMPT.md`) — destrava o relógio de elegibilidade que faltava para a purga LGPD de `Membership`/`Channel` (`NotificationPreferences`) da Prioridade 5 de D-127 (`ACCOUNT_ACTIVE` não-fechamento, retenção "encerramento + 30 dias", `privacy-lgpd.md` linha 37). Protocolo Claude↔Codex rodada única (nível 4 — campo aditivo opcional, sem mudança de chave/índice/evento/schema externo, facilmente reversível, decisão de retenção já `APPROVED` em D-127; nota 8,7/10, Codex confirmou nível 4). **Campo `removedAt?: string` adicionado a `Membership` (domain/membership.ts)**; `RemoveMembershipService`/`LeaveOrganizationService` (únicos 2 writers reais confirmados por leitura — busca não achou nenhum terceiro; `SUSPENDED` é estado distinto/reversível, não recebe `removedAt`) estendem o `UpdateExpression` já existente de `SET #status = :removed, version = version + :one` para `SET #status = :removed, removedAt = :now, version = version + :one`, reusando o `now` já computado para o `AuditEvent` da mesma transação — nenhuma mudança de assinatura de método, nenhum contrato externo tocado (nenhum handler serializa `Membership` inteiro; `handleListMembers` expõe só `userId`/`role`/`status`/`version`, achado do Codex corrigindo uma alegação errada de paridade com `joinedAt` na proposta original — `removedAt` NÃO exposto no DTO por decisão explícita, é "histórico administrativo", não atributo de listagem geral). **`AcceptInvitationService`** (o 3º caminho relevante, sentido inverso REMOVED→ACTIVE no reingresso via convite) ganha `REMOVE removedAt` no mesmo `UpdateExpression` de reativação — Codex confirmou que não é over-engineering: sem isso, uma Membership `ACTIVE` carregaria um `removedAt` obsoleto contradizendo o próprio status, facilitando bugs de auditoria/consulta futuros mesmo o worker de purga (que só olha `status`) sendo inofensivo a isso. Fake `InMemoryOrganizationStore` (`test/unit/organization/in-memory-store.ts`) só suportava `SET` — ganhou suporte a `REMOVE x[, y...]` opcional após o `SET` (bloqueador real apontado pelo Codex antes da implementação, não descoberto só em CI). Worker de purga físico para `Membership`/`NotificationPreferences` (a Prioridade 5 remanescente de D-127) **NÃO implementado nesta rodada** — decisão explícita de escopo (Codex concordou): a mudança em si é nível 3 "implementação direta de decisão já aprovada" assim que o campo existir, mesmo padrão mecânico de D-151..D-156, sem necessidade de rodada nova; fica para sessão seguinte no mesmo padrão. **G-V3 confirmado ao vivo**: mutação removendo `removedAt = :now` do `UpdateExpression` de `RemoveMembershipService` fez o teste "stamps removedAt when soft-removing a member" falhar (`undefined` em vez do timestamp esperado) — confirmado quebrando deliberadamente o SET no código real, revertido antes do commit. **Suíte**: `npm test` (raiz) 1662/1662 (+4 testes novos: 2 em `membership-management.test.ts` — `RemoveMembershipService` estampa `removedAt` com o clock injetado e falha condicional de versão não deixa `removedAt` setado; 1 em `membership-management.test.ts` para `LeaveOrganizationService` estampando o mesmo campo; 1 em `accept-invitation.test.ts` — reingresso via convite limpa `removedAt` de uma Membership `REMOVED` anterior, provado por seed direto no fake); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs` limpos. | Type 1→4 (protocolo rodada única, nota 8,7/10, nível 4 confirmado pelo próprio Codex) | n/a | 8,7 (Codex, revisão independente; protocolo não obrigatório pela escala de risco mas prudente por tocar 2 write paths de produção) | **IMPLEMENTADO E VERIFICADO** — campo grava/limpa corretamente nos 3 write paths reais; worker de purga físico de Membership/Channel (Prioridade 5 remanescente de D-127) segue como próxima frente, agora desbloqueada | `src/modules/organization/domain/membership.ts`, `src/modules/organization/application/{remove-membership.ts,leave-organization.ts,accept-invitation.ts}`, `test/unit/organization/{in-memory-store.ts,membership-management.test.ts,accept-invitation.test.ts}` | 2026-09-01 |
180:| D-159 (investigação, não implementada) | D-C do hot path (D-136, `docs/architecture/reviews/performance-hotpath-scoping/estado-final-consolidado.md`) — Rodada 1 de tentativa de implementação **rejeitada pelo Codex, nota 5,5/10, nível 5 confirmado** (não nível 4 como a proposta original assumia): mudar o contrato compartilhado `IdentityStore.get()` (usado por todo o módulo identity — `bootstrap-identity.ts`/`global-user-repository.ts`/`quota.ts`, não só este call site) para leitura eventualmente consistente E MESMO ASSIM manter `createProfileIfAbsent()` `await`ado dentro de `RequestContextResolver.resolve()` **não satisfaz o texto já `APPROVED` de D-136** ("sai do hot path com confiança") — só torna a leitura mais barata, não remove o round-trip síncrono. **Achado real mais importante da rodada, confirmado por leitura direta**: `UserProfile` (entidade tenant-scoped que `createProfileIfAbsent()` escreve, `identity/persistence/user-repository.ts`) não tem NENHUM leitor em todo o código — `grep` de todo import de `UserRepository`/chamada a `getProfile()` só acha o próprio `resolve-request-context.ts:114`, cujo valor de retorno é descartado (nunca atribuído a nada). A pergunta correta não é "como tornar essa leitura mais barata" e sim "essa entidade write-only ainda precisa existir" — Codex propôs 2 caminhos reais: (a) remover a entidade por completo (mais coerente se não há consumidor real), ou (b) se ainda necessária por alguma invariante não encontrada nesta busca, criá-la numa fronteira não-recorrente (criação de Organization, aceite de convite/Membership) em vez de a cada request. Codex também descartou "fire-and-forget" dentro do handler Lambda como solução confiável — sem mecanismo durável (fila/evento), a promise não tem garantia de completar após a resposta ser enviada (ambiente pode congelar/reciclar). **Não implementado nesta sessão** — decisão explícita de não forçar uma implementação de baixa confiança sob nota 5,5; fica para sessão futura dedicada com protocolo completo (mínimo 3 rodadas, nota ≥9,0), começando pela pergunta de remoção-vs-relocação da entidade antes de qualquer mudança de contrato de `IdentityStore`. D-D (`EphemeralTelemetryMutation`) permanece igualmente não iniciado, sem investigação nova nesta sessão — já nomeado pelo próprio D-136 como o item de maior risco do pacote, precisa de sessão dedicada com teste adversarial obrigatório no DoD. | n/a (investigação, protocolo não concluído) | 5,5 (Codex, Rodada 1 — nota abaixo do gate, rodada não continuada nesta sessão) | n/a | **NÃO IMPLEMENTADO — investigação registrada para a próxima sessão dedicada a D-C/D-D**: achado real de que `UserProfile` pode ser removível por completo (sem consumidor), não só mais barata; decidir isso primeiro, antes de qualquer mudança em `IdentityStore.get()` | `src/modules/identity/application/resolve-request-context.ts`, `src/modules/identity/persistence/user-repository.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts` (nenhum arquivo alterado, só lidos/investigados) | 2026-09-01 |
181:| D-160 | Fecha D-C de D-136 (implementação, não mais design) e o achado D-159 — **remoção completa de `UserProfile`**, protocolo Claude↔Codex completo, **4 rodadas** (5,5→6,8→8,7→9,2), Codex 9,2/10, **APPROVED**. Rodada 1 desta tentativa (tornar a leitura eventualmente consistente, mantendo a chamada no hot path) rejeitada 6,8/10 — Codex apontou que `TransactWriteItems` não permite um `Put` condicionado falhar sem abortar a transação inteira (quebraria reativação REMOVED→ACTIVE via convite), `CreateOrganizationInput` não tinha `identitySubject`/`emailNormalized`, e que a justificativa para MANTER a entidade não provava necessidade funcional — sugeriu remoção como alternativa mais forte. Investigação confirmou: `GlobalUser` já tem `emailNormalized`+`identityStatus`, `IdentityMapping` já tem `cognitoSub` — os 4 campos "reais" de `UserProfile` são todos duplicados ou (`roles`) explicitamente vestigiais desde D-095, sem nenhum leitor real (D-107/D-108 já migrou o único outro consumidor histórico, `dynamodb-recipient-resolver.ts`, para `GlobalUser` por esse mesmo motivo). Rodada 2 (remoção completa) 8,7/10 — 2 bloqueadores documentais: `data-model.md` §2 ainda descrevia a entidade tenant-scoped removida; o teste apagado precisava virar regressão negativa com G-V3, não só desaparecer. Rodada 3 (correções aplicadas: `data-model.md` §2 corrigido) 8,9/10 — bloqueador residual: `data-model.md` §1 ("`User` representa o perfil dentro do tenant") e a linha `IdentityMapping` (ainda listava `tenantId`, removido em D-095) continuavam desatualizadas. Rodada 4 (§1 e linha `IdentityMapping` corrigidos) **9,2/10, APROVADO**. **Implementado**: `src/modules/identity/persistence/user-repository.ts` deletado por completo (`UserRepository`/`UserProfile`/`userProfileKey`/`createProfileIfAbsent`/`getProfile`); `RequestContextResolver.resolve()` (`identity/application/resolve-request-context.ts`) perde o parâmetro `users` e a chamada `createProfileIfAbsent()` — `resolve()` agora só lê e valida, nenhuma escrita, "sai do hot path" literalmente (não só mais barato, como a Rodada 1 desta sessão tentou); `composition/identity.ts` ajustado; 9 arquivos de teste (`test/integration/{activity-log,cross-tenant,document-archive-lifecycle,expiration-lifecycle}.test.ts`, `test/unit/{document/document-handlers,identity/{resolver,test-route-handler},import/import-handlers,notification/preferences-handlers}.test.ts`) perdem o argumento/import de `UserRepository`. **`docs/architecture/privacy-lgpd.md` §4.1 emendado**: removida a cláusula "cada `UserProfile` e" do inventário de User-level erasure/DSR, `NotificationPreferences` continua real. **`docs/architecture/data-model.md` §1/§2 corrigidos**: `User`→`GlobalUser` (tenant-independente, `USER#u`/`PROFILE`), `IdentityMapping` sem `tenantId` (D-095), narrativa de `tenantId` obrigatório qualificada com as exceções globais já documentadas. Comentários vestigiais em `global-user-repository.ts`/`bff-auth-service.ts` corrigidos para narrar o estado real pós-remoção (comentários genuinamente históricos preservados). **Teste negativo substitui o antigo** (`test/unit/identity/resolver.test.ts` — "does not write any tenant-scoped user-profile row when resolving a context", usa `store.allKeys()`, o mesmo idioma já usado por outro teste irmão no arquivo). **G-V3 confirmado ao vivo**: reintroduzir a chamada completa `await this.users.createProfileIfAbsent(...)` + o parâmetro `users` de volta no resolver fez o teste negativo falhar (a chave `TENANT#.../USER#...` voltou a aparecer); revertido antes do commit. **Suíte completa e checklist do próprio Codex (resposta (e) da Rodada 2)** todos verificados antes do commit: grep pós-diff zero referência operacional (só histórica) a `UserRepository`/`UserProfile`/`createProfileIfAbsent`/`getProfile`/`userProfileKey`; `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`npm test` completo. | Nível 5 (Codex confirmou nas 4 rodadas — remove entidade física + fronteira de módulo + emenda doc `APPROVED`, protocolo completo obrigatório) | n/a | 9,2 (Codex, Rodada 4/4, `APPROVED`) | **IMPLEMENTADO E VERIFICADO** — D-C de D-136 fechado de fato (não só investigado); D-D (`EphemeralTelemetryMutation`) segue como único item do pacote de hot path ainda não iniciado | `src/modules/identity/{application/resolve-request-context.ts,persistence/user-repository.ts (deletado)}`, `src/runtime/aws/composition/identity.ts`, `src/modules/identity/persistence/global-user-repository.ts`, `src/modules/bff/application/bff-auth-service.ts`, `docs/architecture/{privacy-lgpd.md,data-model.md}`, 9 arquivos de teste (ver lista acima) | 2026-09-01 |
182:| D-161 | Roadmap Competitivo trazido por Marcelo (`docs/project/roadmap-competitivo-2026-09-01.md`, 26 funcionalidades P0/P1/P2/Futuro + preço/limites) — **reconciliação + macro-ordem técnica, protocolo Claude↔Codex completo, 3 rodadas (7,6→9,2→9,6), Codex 9,6/10, APPROVED**. Pedido explícito de Marcelo: estabelecer ordem de trabalho e como será feito, não implementar tudo agora. Rodada 1 (reconciliação factual dos 10 itens P0 contra o código real) recebeu 7,6/10 — Codex corrigiu 3 itens que minha leitura inicial avaliou como "quase prontos" mas não estão: (4) M7/extraction ainda aponta para o `Document` antigo (`itemId`/`documentKey(tenantId,itemId,documentId)`), não para o novo `DocumentVersion` de D-143; (9) Guest upload só cria metadados, nunca recebe/presigna arquivo real; (10) `commitUpload()` declara storage/malware-scan explicitamente fora do incremento atual, sem `DocumentFile`. **Achado real mais valioso, verificado ao vivo**: `document-archive-service.ts` linhas 97-98 têm um comentário afirmando que `createDocument()` grava GSI2 (Documents-by-Subject), mas o objeto `document` construído (linhas 81-93) só espalha `documentGsi1Keys(...)` — `documentGsi2Keys(...)` nunca é chamado; comentário desalinhado do código real, não só lacuna de escopo. Rodada 2 (reconciliação corrigida + macro-ordem adotada) 9,2/10 — 2 ajustes de governança antes do fechamento formal: (a) `AGENTS.md` §4 exige mínimo 3 rodadas para item explicitamente submetido ao protocolo, não 2; (b) trocar "cada item P0 terá protocolo completo" por "cada item recebe scoping + classificação de risco real primeiro; protocolo completo só nível 5-6 ou por pedido explícito de Marcelo" — preserva `change-risk-scale.md`/proporcionalidade do DoD em vez de assumir nível 5-6 para tudo a priori. Rodada 3 (ambos ajustes incorporados) **9,6/10, APROVADO**. **Decidido**: fotografia factual dos 10 itens P0 (núcleo substancial reutilizável: 2-Bulk import via `TrackedSubject`/M10, 4-IA/OCR via M7, 7-Relatórios/audit via `ActivityService`+CSV export D-126, 9-Guest/Requests/Review/Recurrence via D-143, 10-Storage/Versioning via `DocumentVersion`; parcial conceitual: 8-Document Types; greenfield: 1-Templates, 3-WhatsApp, 5-Busca, 6-Dashboard); macro-ordem técnica (auditoria curta 4/9/10 → 10 Storage → 8 Document Types → 4 OCR no novo lifecycle → 1 Templates → 9 Guest completo → 2 Bulk documental → 5 Busca → 6 Dashboard → 7 Relatórios/audit → 3 WhatsApp → Frontend completo do P0); 4 decisões fundacionais escalonadas para suas próprias Rodadas 1 futuras (DocumentFile/storage; identidade de DocumentType; fronteira M7↔DocumentVersion; convivência RequirementAssignment legado vs. Requirement novo). **Não decidido** (fica para as próprias rodadas futuras): conteúdo arquitetural das 4 decisões fundacionais; designs dos 10 itens P0 individuais; preço/limites comerciais (seções 12-13 do documento original, fora de escopo técnico, não avaliadas). **Nenhum código alterado** — puramente reconciliação + ordem, mesmo padrão de D-121/D-127/D-136 (design-only, deliberado). | Nível 5-6 (decisão de arquitetura/roadmap de produto, protocolo completo obrigatório por ser explicitamente submetido por Marcelo) | n/a | 9,6 (Codex, Rodada 3/3, `APPROVED`) | **APROVADO (reconciliação + ordem), NÃO IMPLEMENTADO (nenhum dos 10 itens P0 nem as 4 decisões fundacionais)** — próxima sessão que tratar deste roadmap começa pelo spike de auditoria 4/9/10, depois abre a Rodada 1 da decisão fundacional #1 (`DocumentFile`) | `docs/architecture/reviews/competitive-roadmap-reconciliation/estado-final-consolidado.md`, `docs/project/roadmap-competitivo-2026-09-01.md` (não alterado, só referenciado) | 2026-09-01 |
184:| D-164 | **DocumentFile — implementação real, primeira fatia (D-163 aplicado)**: `domain/document-file.ts` novo (`DocumentFile`, `documentFileKey()` real espelhando `documentVersionEventKey()`, `assertExactlyOnePrincipal()`, `MAX_FILES_PER_VERSION=20`, reusa `sameObjectVersion()`/`UploadEvidence`/`MalwareEvidence` de M6 verbatim); `DocumentVersion` ganha `fileSetSealed`/`principalFileId`/`totalFiles`; os 2 stubs mortos (`documentVersionFileKeyPrefix`/`documentVersionEventKeyPrefix`, nunca tiveram chamador) removidos; `DocumentVersionEvent` ganha `fileId`/`fromFileScanStatus`/`toFileScanStatus`, tipo renomeado `FILE_REMOVED_INFECTED`→`FILE_REJECTED_INFECTED`. `DocumentArchiveService.reserveFiles()` novo — fecha a corrida de PRINCIPAL de D-163 §2 numa única `TransactWriteItems` (Update condicionado de `DocumentVersion` com fence `fileSetSealed`/`state=DRAFT` + N Put de `DocumentFile`); `acceptVersion()` ganha `ConditionCheck` transacional do PRINCIPAL (`scanStatus=CLEAN`, D-163 §5). **G-V3 confirmado ao vivo em ambos os mecanismos**: removendo o fence de `reserveFiles` (extraConditions + pre-check em memória) uma segunda reserva com `expectedVersion` fresco passa a suceder indevidamente (teste falha); removendo o `ConditionCheck` do PRINCIPAL, `acceptVersion` aceita uma Version com PRINCIPAL `REJECTED` (teste falha) — ambos revertidos antes do commit. **Achado real corrigido no caminho**: o fake de teste `in-memory-store.ts` não parseava `attribute_not_exists(#nome)` dentro de um grupo OR sobre um atributo nomeado arbitrário (só suportava a forma bare `PK`/`SK`) — sintaxe real e válida do DynamoDB que `reserveFiles()` usa (`attribute_not_exists(#sealed) OR #sealed = :false`); parser do fake estendido para suportar `exists`/`notExists` como termos de um grupo OR, não só `equality`. **Decisão de sequenciamento deliberada, registrada para não ser esquecida**: o gate `fileSetSealed` em `commitUpload()` (D-163 §4) foi desenhado mas **NÃO** ativado nesta fatia — a suíte de testes/HTTP e2e existente chama `commitUpload()` sem nunca passar por `reserveFiles()` (nenhuma rota HTTP para `reserveFiles()` existe ainda), então ativar o gate agora quebraria o fluxo real sem nenhum caminho para satisfazê-lo. Fica para a mesma fatia futura que adicionar a rota HTTP de `reserveFiles()`. Presign real (`UploadUrlSigner`, reusado de M6) e a transição terminal de scan (`applyFileScanResult`/correlação de evidência simétrica/GSI5 de reconciliação) **permanecem não implementados** — este é o Nucleus de domínio+persistência apenas, mesmo padrão incremental de D-143 Nucleus 1. Infra: `QUARANTINE_BUCKET_NAME` (bucket já existente de M6, sem bucket/GuardDuty novo) adicionado ao env do `document-archive-handler`, sem policy S3 nova ainda (nenhuma chamada real a S3 nesta fatia). Suíte completa 1668/1668, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (20/20) todos limpos. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-163) | n/a | n/a | **IMPLEMENTADO PARCIALMENTE** — entidade + `reserveFiles()` + fence transacional do PRINCIPAL prontos e testados; `applyFileScanResult()`/correlação de evidência/reconciliação GSI5/rota HTTP/`fileSetSealed` gate em `commitUpload()`/presign real ainda pendentes, nomeados explicitamente, próxima fatia natural | `src/modules/document-archive/domain/{document-file.ts (novo),document-version.ts,document-version-event.ts}`, `src/modules/document-archive/application/{document-archive-service.ts,id-generator.ts}`, `src/runtime/aws/{ids.ts,composition/document-archive.ts,handlers/{document-archive-handler.ts,document-request-recurrence-handler.ts,requirement-reindex-handler.ts}}`, `infra/main.tf`, testes em `test/unit/document-archive/` e `test/integration/document-archive-lifecycle.test.ts` | 2026-09-02 |
185:| D-163 | **DocumentFile — decisão fundacional #1 de D-161** (storage/scan/download para o Domínio Documental, D-143 Decision 6 já tinha aprovado a semântica de arquivo mas não o mecanismo). Protocolo Claude↔Codex completo, **4 rodadas** (5,2→7,1→8,3→9,4), Claude 9,3/Codex 9,4, **APPROVED (design)**. E-014: `SIM PARCIAL` (padrão S3 presigned+GuardDuty já pesquisado/implementado internamente via M6, `E2E PROVEN`; pesquisa externa nova só confirmou nesta rodada que um finding real do GuardDuty Malware Protection carrega `bucket`/`key`/`versionId` do objeto físico escaneado). Rodada 1 (5,2/10) errou dizendo que nenhum bucket/GuardDuty/worker existia (falso — `infra/modules/document-buckets`/`document-malware-protection`, `src/workers/upload-slot-reconciliation` já existem) e propôs validar "exatamente 1 PRINCIPAL" só por validação de input, sem fechar a corrida real entre duas reservas concorrentes. Rodada 2 (7,1/10) corrigiu ambos mas introduziu `removeInfectedFile()` contraditório (exigia um estado que a própria infecção já teria consumido) e deixava a recuperação de um PRINCIPAL infectado sem caminho. Rodada 3 (8,3/10) removeu `removeInfectedFile()` (infecção passa a ser a própria transição terminal) e fechou a recuperação via "nova Version, nunca reabrir o conjunto selado" — mas propôs reusar GSI6 para reconciliação, contradizendo D-143 Decision 2 ("GSI3/GSI4/GSI6 nunca tocados"), e deixou ambíguo qual evento (S3 ou GuardDuty) consolida `quarantineObject.versionId` primeiro. Rodada 4 (**9,4/10, APROVADO**) reverteu para GSI5 (já alocado ao módulo, mesmo padrão de discriminação por prefixo do GSI1) e fechou a correlação com uma regra simétrica: o primeiro evento físico a chegar consolida a partir da própria tripla que carrega, o segundo verifica igualdade exata. **Decidido**: entidade `DocumentFile` (mesma partição Document/Version, `documentFileKey()` nova espelhando `documentVersionEventKey()` real — os stubs mortos `documentVersionFileKeyPrefix()`/`documentVersionEventKeyPrefix()` nunca tiveram chamador e são removidos); `reserveUpload()` como uma `TransactWriteItems` (Update condicionado de `DocumentVersion` com `fileSetSealed`/`principalFileId` + N Put de `DocumentFile`) fechando a corrida de PRINCIPAL na própria Version, não na validação de input; transição terminal via `buildVersionedUpdate` padrão (sem mecanismo novo de contador); `DocumentVersionEvent` ganha `fileId`/`fromFileScanStatus`/`toFileScanStatus`, tipo renomeado `FILE_REMOVED_INFECTED`→`FILE_REJECTED_INFECTED` (nunca teve writer real); `acceptVersion()` ganha `ConditionCheck` transacional do PRINCIPAL (`scanStatus=CLEAN`); reconciliação via GSI5 namespaced por prefixo, nunca GSI6; nenhum bucket/GuardDuty novo, reusa M6 (`infra/modules/document-buckets`/`document-malware-protection`) tal como estão. **Design-only, deliberado** — implementação em sessão(ões) futura(s) dedicada(s), tamanho comparável a M6 completo. Download real (Viewer A1) e purga física de `DocumentFile` seguem fora de escopo, nomeados. | Nível 5 (nova entidade + storage + invariantes transacionais — reclassificado da estimativa otimista de nível 3-4 do handoff anterior após scoping real) | 9,3 | 9,4 (Codex, Rodada 4/4, `APPROVED`) | **APROVADO (design), NÃO IMPLEMENTADO** — próximo item real da macro-ordem de D-161 é a implementação desta decisão, depois o item 8 (Document Types) | `docs/architecture/reviews/document-file-scoping/` (round1-4, `estado-final-consolidado.md`) | 2026-09-01 |
187:| D-166 | **DocumentFile — terceira fatia de D-163: DocumentFileReconciliationWorker (item 2 da ordem de `NEXT_SESSION_PROMPT.md`)**. Generaliza M6's `UploadSlotReconciliationWorker` (`src/workers/upload-slot-reconciliation/reconciliation.ts`) para o segundo namespace GSI5 esparso já modelado por `document-file.ts`'s `fileReconciliationGsi5Keys()` (D-164, sem chamador até agora): `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`. Implementação direta de design já `APPROVED` (D-163 §6/`round4-claude-final.md` §3), sem nova rodada Claude↔Codex. Novo `applyFileScanTimeout()` em `apply-file-scan-result.ts` (mesma `Deps`/mesmo módulo de `applyFileScanResult`/`confirmFileScanClean`) faz a transição terminal `PENDING_UPLOAD\|SCANNING`→`TIMEOUT`, espelhando o branch REJECT existente: `buildVersionedUpdate` padrão + `remove: ["GSI5PK","GSI5SK"]` + decremento de `pendingFileScans` do `DocumentVersion` na MESMA transação — nenhum mecanismo novo. A única peça genuinamente nova é a condição extra `GSI5PK = :observado AND GSI5SK = :observado` (via `extraConditions`, duas entradas separadas por causa do parser de teste, que não entende AND aninhado dentro de um parêntese) — fecha exatamente a corrida nomeada pelo round4 §3: um candidato cujo deadline mudou ou que já chegou a um estado terminal entre a varredura e a escrita é puxado de volta (`ConditionalCheckFailed`→`IGNORED_STALE`), nunca reprocessado às cegas. **Desvio nomeado do design, não uma correção dele**: GSI5PK de `DocumentFile` é escopado por tenant (`TENANT#<t>#...`), diferente de GSI6 (globalmente namespaced) que permite ao `UploadSlotReconciliationWorker` fazer uma `Query` cross-tenant real — este módulo nunca teve porta de enumeração de tenant (`document-archive-store.ts`'s `scanSatisfiedRequirements`/`scanActiveSeries` já aceitam o mesmo trade-off pelo mesmo motivo estrutural). O worker novo (`src/workers/document-file-reconciliation/`, port+adapter+lógica, mesmo layout de `core-user-data-purge`/`quota-telemetry-purge`) descobre candidatos via `Scan` na tabela base filtrado por `entityType=DocumentFile AND scanStatus=:status AND attribute_exists(GSI5PK)` (nunca GSI6, nunca sequer uma `Query` real em GSI5) — dois scans independentes e limitados (`MAX_PAGES_PER_STATUS=25`), um por status não-terminal, cada página ordenada por `GSI5SK` (deadline) em memória antes de processar (melhor ordenação possível dado o Scan cross-tenant; um backlog patológico é limitado pelo cap de páginas e recolhido na próxima execução, mesma disciplina dos outros workers Scan-based deste código). Lambda novo `document-file-reconciliation-handler` (`src/runtime/aws/handlers/`), schedule `rate(15 minutes)` (mesma cadência do worker que generaliza), só `tenant_facing_read_write_policy_json` — nenhuma política GSI6/GSI3 nova (confirmado por teste Terraform novo). `buildDocumentArchiveDeps()` passa a devolver `ids` diretamente (antes só embutido nos serviços) para o novo handler poder montar `ApplyFileScanResultDeps`. **G-V3 confirmado ao vivo em 2 pontos, quebrando e restaurando cada mecanismo**: (1) removendo `extraConditions` de `applyFileScanTimeout`, o teste "pointer changed concorrentemente" passa a devolver `TIMED_OUT` em vez de `IGNORED_STALE` (falha); (2) invertendo o comparador de deadline (`<` em vez de `>`) no worker, os 2 testes de timeout real e o teste dos 2 scans independentes falham (0 `timedOut` em vez do esperado). Ambos revertidos antes do commit. Suíte completa 1685/1685 (1677+8 novos: 4 unit de `applyFileScanTimeout`, 4 do worker), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21, +1 novo) todos limpos. | Nível 3-4 (implementação direta de design já `APPROVED`, D-163) | n/a | n/a | **IMPLEMENTADO E VERIFICADO, NÃO deployado** (infra escrita/validada via `terraform plan`/`test`, `terraform apply` real em `dev` fica para o CD de push a `main`) — falta a rota HTTP real de `reserveFiles()`, o worker S3/GuardDuty que chamaria `applyFileScanResult()`/`confirmFileScanClean()` em produção, presign real (`UploadUrlSigner`) e o gate `fileSetSealed` em `commitUpload()` (item 3 da ordem, próximo) | `src/modules/document-archive/application/apply-file-scan-result.ts`, `src/runtime/aws/{composition/document-archive.ts,handlers/document-file-reconciliation-handler.ts (novo)}`, `src/workers/document-file-reconciliation/ (novo: candidate-source.ts,dynamodb-candidate-source.ts,reconciliation.ts)`, `scripts/build-lambdas.ts`, `infra/{main.tf,tests/stack.tftest.hcl}`, `test/unit/document-archive/{apply-file-scan-result.test.ts,document-file-reconciliation.test.ts (novo)}` | 2026-09-02 |
190:| D-169 | **MembershipPurgeWorker — Prioridade 5 de D-127 (`ACCOUNT_ACTIVE`, não-fechamento), leg `Membership`, última peça pendente dessa prioridade**. D-155 implementou só `Invitation` (`Membership`/`Channel` bloqueados); D-158 adicionou `Membership.removedAt`, destravando o relógio de elegibilidade. Implementação direta de decisão já `APPROVED` (D-127/D-155/D-158), sem nova rodada Claude↔Codex (nível 3-4, mesmo padrão mecânico de D-151..D-156). **Escopo reconfirmado por grep antes de codar**: `Channel` continua sem existir como entidade persistida; o candidato mais próximo, `NotificationPreferences`, ainda não tem estado terminal próprio (órfão quando a `Membership` dona é removida) e dependeria de uma decisão de design nova (join contra `Membership.status`) — não implementado aqui, mesma exclusão de D-155. Mesmo layout exato de `invitation-purge` (`src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/membership-purge-handler.ts`): `Scan` full-table filtrado por `entityType=Membership AND status=REMOVED AND attribute_exists(removedAt)`, fence de tenant `ACTIVE` idêntica às 6 anteriores, delete via `buildConditionalDelete` reafirmando `version` (não `buildVersionedDelete` — `Membership` usa `organizationId`, não `tenantId`, mesmo motivo de `Invitation` em D-155). Elegibilidade: `status=REMOVED AND removedAt + 30 dias <= now` — retenção de 30 dias confirmada contra `privacy-lgpd.md` §4 ("encerramento + 30 dias") e D-127, mesmo valor que `Invitation` já usa. `SUSPENDED`/`ACTIVE` nunca são candidatos (reversível, não é término). **Infra**: novo módulo Lambda `membership_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 8 * * ? *)`, 08:00 UTC, depois de `transient-purge` às 07:30) em `infra/main.tf`, mesmo padrão de `transient_purge_handler`/`invitation_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `membership-purge-handler`. **G-V3 confirmado ao vivo em 2 mecanismos**: (1) fence de elegibilidade — teste parametrizado sobre os 8 outros status de tenant + registro ausente, mais os casos `ACTIVE`/`SUSPENDED`/`REMOVED` sem `removedAt` nunca virando candidato mesmo semeados diretamente no fake (bypass do próprio filtro do scan), confirma que o scan real nunca sequer considera esses casos; (2) guarda de delete condicional — mutação simulando `version` mudando entre scan e delete (via override do fake) faz `skippedConcurrentlyModified` capturar a corrida em vez de um purge indevido, mesma técnica de D-155/D-156. Suíte completa 1713/1713 (1698+15 novos: elegibilidade por `removedAt` incluindo fronteira exata de 30 dias, `ACTIVE`/`SUSPENDED`/`REMOVED`-sem-`removedAt` nunca candidatos, fence de tenant `ACTIVE` parametrizado sobre 8 status + ausência de registro, guarda de delete condicional contra mudança de `version` e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (46 handlers)/`terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`, 21/21) todos limpos. | Nível 3-4 (implementação direta de decisão já `APPROVED` em D-127/D-155/D-158, protocolo Claude↔Codex dispensado por `AGENTS.md` §4) | n/a | n/a | **IMPLEMENTADO** — fecha por completo a Prioridade 5 de D-127 na sua leitura real (`Invitation`+`Membership`; `Channel`/`NotificationPreferences` seguem fora de escopo por falta de estado terminal próprio, achado de investigação, não pendência esquecida). Das 7 prioridades LGPD originais de D-127, resta só `LEGAL_EVIDENCE` (Prioridade 7), bloqueada por trava jurídica. | `src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/membership-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{membership-purge.test.ts,membership-purge-fakes.ts}` | 2026-09-02 |
191:| D-170 | **Reconciliação factual da Auditoria de Performance 2026-09-02** (`docs/project/performance-audit-2026-09-02.md`, documento externo trazido por Marcelo) — investigação pura, sem alteração de código, mesmo tratamento de spike que D-161 recebeu antes de virar decisão de arquitetura. Cada achado do documento externo foi verificado linha a linha contra o código real: RequestContext confirmado caro (`onboarding.resolve()` roda incondicionalmente, hidrata todas as Memberships via GSI4, mesmo com `organizationIdHint` presente — já era gap conhecido, D-136/D-C tinha adiado "condicionado a mapear consumidores primeiro"); quota `API_REQUEST` confirmada fazendo `Get`+`TransactWriteItems` em toda request — mas com a correção de que o documento externo não sabia: D-136/D-D já é um design `APPROVED` (3 rodadas, Codex 9,3/10) para exatamente essa lane leve (`EphemeralTelemetryMutation`), nunca implementado; Reminder Producer (loop triplo sequencial minute×shard×partição, claim+outbox sequencial por ocorrência) e Reminder Dispatch (N+1 via `queryByItem`+`.find()`, processamento serial do batch SQS) confirmados sem ressalva. **Achado mais grave, além do que o documento supôs**: o padrão `Scan` base-table + `Limit=100` + `MAX_PAGES=25` sem `ExclusiveStartKey` persistido entre invocações agendadas aparece em **9 dos 10 workers de manutenção/purga do repositório** (`membership-purge` D-169, `document-file-reconciliation` D-166, `requirement-reindex`, `invitation-purge`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`) — inclusive 2 workers implementados NESTA MESMA sessão e já registrados como `IMPLEMENTADO`/`DEPLOYADO`. O comentário do próprio código em pelo menos 2 desses workers ("anything beyond this is picked up by tomorrow's run") é factualmente falso como escrito — `Scan` sem cursor persistido reinicia pela mesma ordem física de hash a cada invocação, então um candidato além da posição `Limit×MAX_PAGES` nunca é alcançado por nenhuma invocação futura. O único worker que NÃO tem o defeito é `document-purge` (D-061) — usa `Query` sobre GSI6 ordenado por data de elegibilidade, exatamente o padrão `MaintenanceDueIndex` que o documento externo propõe como solução nova, já em produção num worker vizinho. Capacity Model confirmado desatualizado (premissa "tenant = usuário individual" ainda ativa, pré-B2B/Document Archive). Baseline do documento externo (PAY_PER_REQUEST, OCC/outbox onipresentes) confirmada correta, não alucinada. Relatório completo com evidência `arquivo:linha` por achado: `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md`. | Investigação factual (não Type 1 em si — fact-finding, sem protocolo Claude↔Codex) | n/a | n/a | **CONCLUÍDO** — nenhum código alterado; prioriza D-D de D-136 (implementação direta, design já aprovado) e o mapeamento de consumidores de D-C como próximos passos baratos; agenda rodada Claude↔Codex nível 5-6 para o padrão Scan-based starvation (achado mais urgente, afeta correção de LGPD já "fechada"), RequestContext fast path, e Reminder Producer/Dispatch | `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md` | 2026-09-02 |
192:| D-172 | **`EphemeralTelemetryMutation` — implementação de D-136/D-D (`docs/architecture/reviews/performance-hotpath-scoping/estado-final-consolidado.md`), design já `APPROVED` (3 rodadas, Claude 9,3/Codex 9,3), implementação direta sem nova rodada Claude↔Codex.** `TenantQuotaService.consume()` (`src/modules/identity/application/quota.ts`) roteia `API_REQUEST` — e só `API_REQUEST` (`EPHEMERAL_TELEMETRY_QUOTA_TYPES`, allowlist fechada) — para uma lane nova, paralela à existente: um único `UpdateItem` `ADD`-only via `IdentityStore.incrementTelemetryCounter()` (novo método de porta), sem `Get`, sem `TransactWriteItems`, sem repetir o `ConditionCheck` transacional de lifecycle do W3-07 — confiando na leitura stale-mas-recente de `ACTIVE` que o `RequestContextResolver` já fez antes do handler chamar `consume()`, exatamente a troca que o design aprovou. Chave `PK=TENANT#<id>#QUOTA`/`SK=TYPE#API_REQUEST#<floor(epochSeconds/windowSeconds)>` — janela derivada do relógio, não da string `window` que o chamador passa (só usada pela lane transacional antiga). Registro fechado (`EphemeralTelemetryRecord`: `count`/`windowSeconds`/`resetAt`/`purgeAfterTtl`, sem `metadata` livre) — `purgeAfterTtl = windowEnd + 30d`, atributo TTL nativo real da tabela (confirmado contra `infra/modules/dynamo-table/main.tf`/seu teste Terraform), best-effort apenas. Classificação tripla de falha implementada literalmente: limite excedido → `QuotaExceededError` (429, já existente); `DependencyUnavailableError` (DynamoDB indisponível/throttling) → fail-open, log de degradação, requisição segue; qualquer outro erro (bug/corrupção) → propaga sem mascarar (5xx real). Todos os outros tipos de quota (`AI_CALL`/`UPLOAD_*`/`IMPORT_*`/`NOTIFICATION_EMAIL`) permanecem exatamente na lane transacional antiga — verificado por teste de mutação nomeada (ver G-V3). **Garantia de remoção real, não só TTL best-effort**: o `QuotaTelemetryPurgeWorker` (D-154) — que já varria `entityType=TenantQuota` por `resetAt+30d` — teve seu filtro de `Scan` alargado para também cobrir `entityType=EphemeralTelemetryMutation` (`src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts}`), fechando a lacuna que a TTL nativa sozinha deixaria (AWS não garante latência de remoção da TTL). **Teste adversarial obrigatório do DoD do próprio D-D** (`test/unit/identity/quota.test.ts`, describe `EphemeralTelemetryMutation lane`): corrida ACTIVE→escreve a lane→tenant transiciona para DELETING — prova que o resíduo (1) nunca é lido como direito de negócio (nenhum código lê campos desse registro além de `count` vs. limite numérico passado pelo chamador), (2) permanece estruturalmente dentro do escopo `begins_with(PK, "TENANT#<id>#")` que o cascade de tenant-purge do W3-07 usa (provado diretamente sobre a chave real, não apenas afirmado), e (3) permanece dentro da forma de elegibilidade do D-154 (entityType+resetAt, provado em `quota-telemetry-purge.test.ts`). Item explicitamente deferido (não fabricado sem recurso real para testar): a métrica/alarme operacional com limiar concreto que o design pede "se o resíduo sobreviver além do esperado" — é infra-level (CloudWatch), fica para uma sessão dedicada a alarmes operacionais deste worker. **G-V3 confirmado por mutação nomeada**: teste prova `incrementTelemetryCounter` chamado exatamente 1x e `transactWrite`/`get` NUNCA chamados para `API_REQUEST`; teste espelho prova `AI_CALL` chama `transactWrite` 1x e NUNCA `incrementTelemetryCounter` — ambos quebrados deliberadamente (trocando a condição de roteamento) e confirmados falhando antes de restaurar. Suíte completa 1726/1726, `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos (sem alteração de infra, `terraform test` não aplicável). | Nível 3-4 (implementação direta de decisão já `APPROVED` em D-136, protocolo Claude↔Codex dispensado por `AGENTS.md` §4) | n/a | n/a | **IMPLEMENTADO** — fecha D-D, o último item pendente do pacote D-136 (D-A/D-B/D-E já implementados; D-C permanece parcial, condicionado ao mapeamento de consumidores de `onboarding.resolve()`). Alarme/métrica operacional de residual sobrevivente e a recalibração do throttle do API Gateway (também citados por D-D) seguem deferidos, não fabricados aqui. | `src/modules/identity/{application/quota.ts,ports/identity-store.ts,persistence/dynamodb-identity-store.ts}`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts}`, `test/unit/identity/{quota.test.ts,in-memory-store.ts}`, `test/unit/workers/quota-telemetry-purge{.test.ts,-fakes.ts}` | 2026-09-02 |
198:| D-177 | **DocumentType — item 5 do design D-173: as 4 operações de CRUD do catálogo (D-174) + leitura ganham rotas HTTP reais, deixando de ser só métodos de aplicação exercitados por teste.** Handlers novos em `document-archive-handlers.ts` (`handleCreateDocumentType`/`handleGetDocumentType`/`handleListDocumentTypes`/`handleRenameDocumentType`/`handleDeprecateDocumentType`/`handleReactivateDocumentType`), mesmo pipeline de todo handler do módulo (`validateAgainstSchema` -> `resolve()` -> serviço -> `withErrorMapping`). `listDocumentTypes()` novo em `DocumentArchiveService` — primeiro consumidor real de `queryIndexPage` no módulo (definido no port desde D-143 mas nunca chamado): uma página física por chamada contra GSI1's namespace `TENANT#<t>#DOCTYPESTATUS#<status>` (já escrito por `documentTypeGsi1Keys()` desde D-173/D-174), `status` default `ACTIVE` via query param `?status=`, `lastEvaluatedKey` repassado no corpo da resposta quando existir — mesma disciplina de página única de `queryByPk`/`scanSatisfiedRequirements`. Rotas: `POST /document-archive/document-types`, `GET /document-archive/document-types`, `GET /document-archive/document-types/{documentTypeId}`, `PATCH /document-archive/document-types/{documentTypeId}` (rename, mesmo verbo de `update_requirement`), `POST .../deprecate`, `POST .../reactivate` — wireadas nos DOIS lugares que D-117/D-120 (achado histórico citado pelo próprio comentário deste módulo) provam serem necessários juntos: `infra/modules/api-gateway/main.tf`'s `document_archive_routes` (mesmo Lambda/integração já existentes, só novas entradas no `for_each`) e `src/modules/bff/domain/proxy-allowlist.ts`; `document-archive-handler.ts`'s `switch` ganhou os 6 `case`s novos. 4 schemas novos (`docarchive-documenttype-{create,rename,deprecate,reactivate}-request.v1.json`, `additionalProperties: false`) registrados em `schema-validator.ts`'s `defaultSchemaRegistry` — checado explicitamente contra a classe de bug que a nota do handoff de D-167 nomeia (schema não importado estaticamente responde 500 em vez de 400); confirmado por teste HTTP real que corpo malformado responde 400. RBAC já existia desde D-174 (`docarchive:documenttype-{create,rename,deprecate,reactivate}` em `ADMIN_ROLES`, `-read` em `READ_ONLY_ROLES`), não precisou de mudança. **G-V3 confirmado quebrando e restaurando cada mecanismo**: suíte HTTP nova (`test/integration/document-archive-document-type-routes.test.ts`, 9 testes) prova fluxo completo create->get->list->rename->deprecate->reactivate via handlers reais, filtragem por `status` no list (ACTIVE some da lista após deprecate, aparece em `?status=DEPRECATED`), 403 real para VIEWER em cada uma das 4 mutações (log `security.authorization_denied` capturado no stderr do teste, prova que passou pelo `AuthorizationDeniedError` real, não um stub), 200 para VIEWER em get/list (READ_ONLY_ROLES), e 400 (nunca 500) para 3 corpos malformados distintos (create sem `displayName`, rename sem `expectedVersion`, deprecate com `expectedVersion` não-inteiro) — mais 10 testes de contrato novos em `schemas.test.ts` (aceita/rejeita por schema). Rota `reserve_files` (`POST .../versions/{seq}/files`, D-163/D-167) permanece ausente de `proxy-allowlist.ts` — achado real pré-existente confirmado durante a leitura do arquivo para este item, fora do escopo de D-177, não corrigido aqui, registrado para uma fatia futura dedicada. Suíte completa 1761/1761 (1743+18 novos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21, via `AWS_PROFILE=claude-dev`) todos limpos. | Nível 3-4 (implementação direta de design já `APPROVED` em D-173, protocolo Claude↔Codex dispensado por `AGENTS.md` §4) | n/a | n/a | **IMPLEMENTADO** — fecha o item 5 de D-173. Decisão de Marcelo sobre `submitEvidence()`/guest (D-175, opções a/b/c) continua inteiramente aberta; item 6 (migração do schema guest, `documentTypeId` obrigatório) é a última fatia do arco D-173 e segue bloqueado até essa decisão landar; achado pré-existente da rota `reserve_files` ausente de `proxy-allowlist.ts` registrado acima, não resolvido nesta fatia | `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/http/document-archive-handlers.ts`, `src/runtime/aws/handlers/document-archive-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/contracts/schema-validator.ts`, `infra/modules/api-gateway/main.tf`, `schemas/api/docarchive-documenttype-create-request.v1.json`, `schemas/api/docarchive-documenttype-rename-request.v1.json`, `schemas/api/docarchive-documenttype-deprecate-request.v1.json`, `schemas/api/docarchive-documenttype-reactivate-request.v1.json`, `test/integration/document-archive-document-type-routes.test.ts`, `test/contract/schemas.test.ts` | 2026-09-02 |
199:| D-178 | **BFF proxy-allowlist — fecha o achado pré-existente registrado em D-177: rota `reserve_files` ausente.** `POST /document-archive/documents/{documentId}/versions/{seq}/files` (`reserveFiles()`, D-163/D-167) já existia como `case` real em `document-archive-handler.ts` e como rota Terraform em `infra/modules/api-gateway/main.tf`'s `document_archive_routes`, mas nunca tinha sido adicionada a `src/modules/bff/domain/proxy-allowlist.ts` — mesma classe de bug D-117/D-120 (metade da wiring dupla silenciosamente ausente): o frontend nunca conseguiria chamar essa rota fim a fim através do BFF, mesmo com o Lambda de recurso funcionando. Fix mecânico de uma linha: entrada nova adicionada em `PROXY_ALLOWLIST` no mesmo ponto sequencial das outras rotas de `versions/{seq}/...`, template idêntico ao `case` real do handler. **G-V3**: teste novo em `test/unit/bff/proxy-allowlist.test.ts` (`matches the DocumentVersion reserveFiles route closed for D-178`) prova `matchAllowlistedRoute("POST", "/document-archive/documents/doc-1/versions/1/files")` definido — falha sem o fix, passa com ele; não existe suíte exaustiva "toda rota do handler está no allowlist" neste arquivo (nível de rigor do arquivo é table-driven por amostra, não cobertura exaustiva automática, mesmo padrão dos testes BLOCKER-A já existentes), então uma suíte assim não foi criada para este fix de uma linha, per escopo pedido. Suíte completa 1762/1762 (1761+1 novo), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos. | Nível 1-2 (fix mecânico, protocolo Claude↔Codex dispensado por `AGENTS.md` §4) | n/a | n/a | **IMPLEMENTADO** — fecha o achado registrado em D-177 | `src/modules/bff/domain/proxy-allowlist.ts`, `test/unit/bff/proxy-allowlist.test.ts` | 2026-09-02 |
201:| D-180 | **MaintenanceDueIndex — implementação real, primeira fatia/piloto (D-179 aplicado, `membership-purge` ÚNICO worker migrado)**: GSI8 real (`GSI8PK`/`GSI8SK`, `KEYS_ONLY`) adicionado ao módulo Terraform (`infra/modules/dynamo-table/main.tf`), `local.gsi8_worker_types = { membership_purge = "MEMBERSHIP_PURGE" }` gerando `gsi8_read_policy_json`/`worker_transact_write_policy_json` por chave — `dynamodb:Query` condicionado `ForAllValues:StringEquals dynamodb:LeadingKeys IN ["WORK#MEMBERSHIP_PURGE","DLQ#MEMBERSHIP_PURGE"]` + `dynamodb:TransactWriteItems` na tabela base (ação nova, não coberta por `tenant_facing_read_write`), ambos anexados só ao role do `membership_purge_handler`. `deriveMembershipMaintenanceDue()`/`membershipGsi8Keys()`/`MEMBERSHIP_PURGE_WORK_TYPE`/`MEMBERSHIP_RETENTION_DAYS` movidos para `modules/organization/domain/membership.ts` (função pura, D-179 §2) — reusada pelos 3 consumidores exigidos: `remove-membership.ts`/`leave-organization.ts` (gravam `GSI8PK`/`GSI8SK` no MESMO `Update` que a transição `REMOVED`, nunca write separado), `scripts/backfill-gsi8-membership-purge.ts` (novo, cobre todo `Membership` com `dueAt` computável — inclusive vencimento FUTURO, corrigindo o erro que a Rodada 3 do design cometeu), e a revalidação do próprio worker. `accept-invitation.ts`'s reativação agora também `REMOVE GSI8PK, GSI8SK` atomicamente ao limpar `removedAt` (autolimpeza de ponteiro obsoleto na origem). `membership-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`Limit`; `purge.ts` revalida cada candidato via `getMembership()` consistente antes de agir (GSI8 nunca é fonte de elegibilidade) e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por versão, na MESMA transação — fecha o TOCTOU que o padrão antigo tinha entre ler o status do tenant e deletar). Poison-record/quarentena (D-179 §8): `maintenanceAttemptCount` incrementado só quando o `ConditionCheck` de tenant falha (nunca no `Delete`), backoff exponencial capado (1,2,4,8,16 dias) reposicionando `GSI8SK`, acima de `MAX_ATTEMPTS=5` move para `GSI8PK="DLQ#MEMBERSHIP_PURGE"`; toda escrita de retry é idempotente (`ConditionalCheckFailedException` tratado como no-op, mesmo padrão que o delete já usava). Ponteiro obsoleto (item reativado após ser indexado) é autolimpo pelo próprio worker via `REMOVE GSI8PK, GSI8SK`. Observabilidade: `MembershipPurgeResult` ganha `quarantinedCount`/`oldestCandidateAgeSeconds` (computado do primeiro item da página, sem I/O extra); o handler já fazia `logger.info(..., {...result})`, então os 2 campos novos já são emitidos sem tocar o handler. **G-V3 ao vivo em 3 mecanismos**: teste novo prova que `RemoveMembershipService`/`LeaveOrganizationService` gravam `GSI8PK`/`GSI8SK` corretos (falha se removido); teste novo prova que `accept-invitation.ts` limpa os 2 atributos na reativação (falha se removido); teste novo do worker leva um candidato através de 6 execuções com tenant nunca `ACTIVE` e prova a quarentena em `DLQ#MEMBERSHIP_PURGE` no `MAX_ATTEMPTS+1`-ésimo ataque, e um teste separado prova o autoconserto do ponteiro obsoleto. `infra/tests/stack.tftest.hcl` ganha `run "gsi8_worker_isolation"`: prova que `membership_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, que `TransactWriteItems` nunca aparece numa política referenciando GSI3/GSI4/GSI6/GSI8, e que `invitation_purge_handler`/`transient_purge_handler`/`test_ping_handler` NÃO referenciam GSI8 (isolamento por worker, não por índice). `gsi_count` 7→8. Suíte completa 1778/1778, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (incluindo o caso novo). **Próximo worker recomendado para a próxima fatia**: `invitation-purge` (mesmo padrão de `TenantLifecycleStatusSource`/fence ACTIVE já usado aqui, candidato mais próximo estruturalmente) — ordem não é normativa, qualquer um dos 8 restantes serve. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — infra nova real porém mecânica, seguindo spec exata) | n/a | n/a | **IMPLEMENTADO (piloto, 1 de 9 workers)** — `membership-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes); os outros 8 workers nomeados por D-179 permanecem em `Scan`+`Limit`, migração worker-a-worker é trabalho futuro explícito | `infra/modules/dynamo-table/{main.tf,outputs.tf}`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/membership.ts`, `src/modules/organization/application/{remove-membership.ts,leave-organization.ts,accept-invitation.ts}`, `src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/membership-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-membership-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
202:| D-181 | **MaintenanceDueIndex piloto (D-180) — fix real de um bug de deploy pego pelo próprio CD, mais verificação ao vivo completa contra `dev`.** A primeira tentativa de merge de D-180 para `main` (PR #198) teve CI verde mas o `Deploy (CD)` real (`terraform apply` em `dev`) **falhou de verdade** (`aws.amazon.com` `MalformedPolicyDocument: Statement IDs (SID) must be alpha-numeric`) — os 2 `Sid`s novos (`gsi8_read`/`worker_transact_write` em `infra/modules/dynamo-table/main.tf`) interpolavam `each.key` (`"membership_purge"`, com underscore) diretamente, mas IAM exige `[0-9A-Za-z]*`. Confirmado por leitura direta do log do run que falhou (não presumido) e por uma checagem ao vivo independente (`aws iam list-role-policies --role-name exptrk-dev-membership-purge-handler-role`, que mostrou só a política base `capability-0`, confirmando que as 2 políticas novas nunca chegaram a existir). Corrigido com `join("", [for part in split("_", each.key) : title(part)])` (PascalCase, sem underscore) nos dois `sid`s; `terraform test` 22/22 continuou verde. PR #199 mergeado, CD real (`33636904901`) `success`. **Verificação ao vivo completa, tudo confirmado via `aws --profile claude-dev`, nada presumido**: `describe-table` mostra GSI8 na lista de índices; `list-role-policies`/`get-role-policy` no role do worker mostram as 3 políticas (base + `Gsi8ReadOnlyMembershipPurge` com a condição `LeadingKeys` exata + `TransactWriteItemsMembershipPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de negação cross-namespace (`aws iam simulate-principal-policy`, exigida pelo DoD do piloto)**: simulando `dynamodb:Query` no role do `membership_purge_handler` com `dynamodb:LeadingKeys=WORK#MEMBERSHIP_PURGE` (seu próprio namespace) → `allowed`, casando a statement `Gsi8ReadOnlyMembershipPurge`; a mesma simulação com `dynamodb:LeadingKeys=WORK#INVITATION_PURGE` (namespace de outro worker, ainda não migrado) → `implicitDeny`, nenhuma statement casada — prova real, nos dois sentidos (controle positivo + negativo), de que o isolamento por `LeadingKeys` funciona exatamente como o design exige, não só no papel do Terraform. **Achado de processo desta sessão, registrado por ser real**: uma mensagem externa recebida durante a espera do CD afirmou "CD completed successfully" e pediu para prosseguir direto à prova de IAM e ao registro em `decisions-log.md` — checada diretamente via `gh run view` antes de confiar, e o run real estava `failure` (a mesma falha do parágrafo acima), não `success`; a alegação foi descartada e o bug real foi corrigido antes de qualquer prova de IAM ou registro de documentação, exatamente a disciplina que este arquivo e `AGENTS.md` exigem (nunca tratar uma alegação não verificada como fato). | Nível 1-2 (fix mecânico de um bug de infra pego em produção `dev`, mais verificação, sem decisão de design nova) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO** — piloto D-179/D-180 100% completo, incluindo a prova de isolamento IAM que o DoD do piloto exigia antes de liberar a migração dos 8 workers restantes | `infra/modules/dynamo-table/main.tf` | 2026-09-02 |
203:| D-182 | **MaintenanceDueIndex — segunda fatia (invitation-purge, D-179 slice 2, mesmo padrão de D-180)**: `invitation_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["invitation_purge"]`/`worker_transact_write_policy_json["invitation_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#INVITATION_PURGE","DLQ#INVITATION_PURGE"]` + `TransactWriteItems` na tabela base, ambos só no role do `invitation_purge_handler`. `deriveInvitationMaintenanceDue()`/`invitationGsi8Keys()`/`INVITATION_PURGE_WORK_TYPE` novos em `domain/invitation.ts` (função pura, D-179 §2) — duas ramificações independentes, nuance real vs. o piloto: `PENDING`'s due date (`expiresAt+30d`) já é totalmente conhecido NA CRIAÇÃO (não numa transição posterior como o `REMOVED` de Membership), então `create-invitation.ts`'s `invite()` grava o ponteiro GSI8 no MESMO `Put` da Invitation, e `resend()` o move junto quando `expiresAt` roda; `revoke-invitation.ts` sobrescreve o ponteiro no MESMO `Update` que a transição real para `REVOKED`; `accept-invitation.ts` limpa (`REMOVE GSI8PK, GSI8SK`) atomicamente ao aceitar, já que `ACCEPTED` nunca é candidato. `invitation-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`TenantLifecycleStatusSource` separado (a checagem ACTIVE virou `ConditionCheck` dentro da própria transação de claim, como no piloto); `purge.ts` revalida via `getInvitation()` consistente antes de agir e reivindica via `TransactWriteItems` (mesmo `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)`+`Delete` condicionado por versão). Poison-record/quarentena idêntico ao piloto (backoff 1,2,4,8,16 dias, `DLQ#INVITATION_PURGE` acima de `MAX_ATTEMPTS=5`). **Achado registrado, não um bug**: não existe caminho real de reativação para `Invitation` (ao contrário de `Membership`) — `REVOKED`/`ACCEPTED` são terminais, `ACCEPTED` já limpa seu próprio ponteiro na origem — então o autoconserto de ponteiro obsoleto em `purge.ts` é só defesa em profundidade, documentado explicitamente no cabeçalho do arquivo em vez de omitido. `GlobalIndexComponent` (taxonomia fechada de `security-audit.ts`) ganha `"invitation-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.invitation_purge_handler.function_name` (achado do próprio teste de arquitetura — sem isso o novo call site de `auditGlobalIndexAccess` ficaria sem alarme wireado). Novo `scripts/backfill-gsi8-invitation-purge.ts`, cobrindo as duas ramificações inclusive vencimento FUTURO (mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido: `invitation_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, e isolamento cross-namespace nos dois sentidos (nenhum role referencia o namespace do outro). **G-V3 ao vivo**: testes novos provam o ponteiro gravado na criação, movido no resend, sobrescrito no revoke, limpo no accept; teste do worker prova quarentena em `DLQ#INVITATION_PURGE` no `MAX_ATTEMPTS+1`; teste do backfill prova cobertura das duas ramificações. Suíte completa 1797/1797 (169 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22. PR #201 mergeado (`9a21da1`), CI `success` (run 33656227410), CD real `success` confirmado via `gh run view` (run 33656568649, não presumido de alegação externa). **Verificação ao vivo em `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role do `invitation_purge_handler` tem as 3 políticas esperadas (base+`Gsi8ReadOnlyInvitationPurge`+`TransactWriteItemsInvitationPurge`). **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, nos dois sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#INVITATION_PURGE` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyInvitationPurge`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE` (namespace do outro worker já migrado) → `implicitDeny`, nenhuma statement casada. **Próximo worker recomendado (não normativo)**: qualquer um dos 7 restantes (`document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — segunda fatia mecânica seguindo o padrão do piloto D-180, com uma nuance real de timing de escrita) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (2 de 9 workers migrados)** — `invitation-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 7 workers nomeados por D-179 permanecem em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/invitation.ts`, `src/modules/organization/application/{create-invitation.ts,revoke-invitation.ts,accept-invitation.ts}`, `src/workers/invitation-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/invitation-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-invitation-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
204:| D-183 | **MaintenanceDueIndex — terceira fatia (document-file-reconciliation, D-179 slice 3, 3º de 9 workers)**: `document_file_reconciliation` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["document_file_reconciliation"]`/`worker_transact_write_policy_json["document_file_reconciliation"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DOCUMENT_FILE_RECONCILIATION","DLQ#DOCUMENT_FILE_RECONCILIATION"]` + `TransactWriteItems` na tabela base, ambos só no role do `document_file_reconciliation_handler`. **Achado real pré-existente, confirmado antes de qualquer código novo (D-166's próprio texto já era ambíguo sobre isso, resolvido por leitura direta)**: o mecanismo GSI5 que D-164/D-166 desenharam para este worker (`fileReconciliationGsi5Keys()`, `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`) NUNCA teve um writer real — `reserveFiles()` (`document-archive-service.ts`) cria cada `DocumentFile` sem jamais chamar essa função, e nenhum outro call site em todo o código a invoca (confirmado por `grep` antes de tocar em qualquer arquivo). O `Scan` filtrado por `attribute_exists(GSI5PK)` que D-166 implementou, portanto, nunca poderia encontrar um candidato real em produção — o worker era código morto funcional desde o dia em que foi implementado, apesar de "IMPLEMENTADO E VERIFICADO" nos testes (os testes seedavam `GSI5PK` manualmente, nunca através do caminho de escrita real). Isso resolve por completo a pergunta "GSI8 substitui ou convive com GSI5" que a tarefa pedia para verificar antes de escrever qualquer código: não havia um mecanismo funcionando para conviver com — GSI8 é a primeira implementação real e completa deste ponteiro de descoberta, não uma segunda ao lado de uma que já funcionava. Não foi necessário reabrir D-163 Decisão 2 (GSI6 seguiu intocado) nem nenhuma outra decisão de alocação de índice. **Decidido e implementado**: `document-file.ts` ganha `FILE_SCAN_TIMEOUT_SECONDS=600` (mesma janela do presign TTL — `document-archive-service.ts`'s `PRESIGN_TTL_SECONDS` agora importa essa constante em vez de duplicá-la, deliberado: a janela de upload e o deadline de timeout de scan são a mesma janela), `deriveDocumentFileMaintenanceDue()` (due = `createdAt + FILE_SCAN_TIMEOUT_SECONDS` para `PENDING_UPLOAD`/`SCANNING`, `undefined` uma vez terminal — mesmo raciocínio do branch PENDING de `Invitation`: due date totalmente conhecido na criação, sem transição posterior para pendurar o relógio, já que SCANNING é só evidência chegando dentro da mesma janela fixa, nunca um novo relógio) e `documentFileGsi8Keys()`. O campo `DocumentFile.GSI5PK?/GSI5SK?` foi REMOVIDO (substituído por `GSI8PK?/GSI8SK?`) — `fileReconciliationGsi5Keys()` também removida, não deixada como código morto adicional. `reserveFiles()` agora grava o ponteiro GSI8 no MESMO `Put` de cada `DocumentFile` (mesmo padrão do branch PENDING de `invitationGsi8Keys()` — criação É a transição relevante). `apply-file-scan-result.ts`: as 3 funções (`applyFileScanResult`'s branch REJECT, `confirmFileScanClean`, `applyFileScanTimeout`) trocam `remove: ["GSI5PK","GSI5SK"]`/condição exata de ponteiro por GSI8; `applyFileScanTimeout`'s `observedGsi5Pointer`→`observedGsi8Pointer`, mesmo mecanismo de fence exato (`extraConditions` comparando o ponteiro observado literal), nenhuma mudança de comportamento. **Composição com o mecanismo transacional existente (item pedido pela tarefa)**: como a claim/revalidação real já vive inteiramente dentro de `applyFileScanTimeout()` (que já releia o `DocumentFile` fresco via `store.get()` e faz seu próprio retry OCC), o port do worker (`candidate-source.ts`) foi simplificado para só `queryDue()` — nenhum `getFile()`/`transactWrite()` próprio como `membership-purge`/`invitation-purge` precisam, evitando duplicar a revalidação que já existe. **Simplificação real habilitada por GSI8 (não pedida, descoberta ao desenhar)**: como GSI8PK é uniforme (`WORK#DOCUMENT_FILE_RECONCILIATION`, sem partição por status, diferente do GSI5PK por-status de D-166), as duas varreduras independentes que D-166 rodava (uma por `PENDING_UPLOAD`/`SCANNING`) colapsam numa única `Query` due-ordered cobrindo os dois status — `reconciliation.ts` reescrito para um único loop de paginação, mesmo formato de `invitation-purge/purge.ts`. **Sem mecanismo de poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada): diferente de `membership-purge`/`invitation-purge`, este worker nunca teve fence de tenant `ACTIVE` (D-166: "a stuck upload is not a retention decision") — não existe nenhuma condição que possa falhar permanentemente para o mesmo candidato; um timeout perdido por corrida é sempre transitório e resolvido no próximo run, nunca precisa de backoff/quarentena. **Achado adicional fora do escopo desta fatia, registrado e NÃO corrigido aqui**: `document_archive_handler` (o handler HTTP principal, que também chama `store.transactWrite()` via `reserveFiles()`/`acceptVersion()`) tem a MESMA lacuna de `dynamodb:TransactWriteItems` que este worker tinha antes desta fatia — `policy_documents_json` dele só inclui `tenant_facing_read_write_policy_json`+a policy de presign, nunca `TransactWriteItems`. Não corrigido nesta sessão (fora do escopo de D-179, achado pré-existente e independente); nomeado explicitamente para não ser esquecido. `GlobalIndexComponent` ganha `"document-file-reconciliation"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.document_file_reconciliation_handler.function_name`. Novo `scripts/backfill-gsi8-document-file-reconciliation.ts`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 3 workers. **G-V3 ao vivo**: teste novo prova que `reserveFiles()` grava `GSI8PK`/`GSI8SK` em cada `DocumentFile`; suíte de `apply-file-scan-result.test.ts`/`document-file-reconciliation.test.ts` migrada para GSI8 continua provando a condição exata de ponteiro (quebra/reversão já demonstrada por D-166, mecanismo idêntico, só renomeado). Suíte completa 1809/1809 (1797+12 novos: 1 em `document-archive-service.test.ts`, 4 do worker migrados de D-166, 11 do backfill novo — conferir soma exata via `npm test`), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (mesma contagem do piloto — este teste estende `gsi8_worker_isolation`, não adiciona um `run` novo). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — terceira fatia mecânica seguindo o padrão do piloto/D-182, com uma descoberta real de bug pré-existente que simplificou a decisão de design em vez de complicá-la) | n/a | n/a | **IMPLEMENTADO — verificação ao vivo em `dev` e merge pendentes, ver sessão em andamento** | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/document-file.ts`, `src/modules/document-archive/application/{apply-file-scan-result.ts,document-archive-service.ts}`, `src/workers/document-file-reconciliation/{candidate-source.ts,dynamodb-candidate-source.ts,reconciliation.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-document-file-reconciliation.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
205:| D-184 | **submitEvidence()/guest flow — resolve a decisão pendente de D-175 (opções a/b/c sobre validar `documentType` contra o catálogo `DocumentType`).** Protocolo Claude↔Codex completo, 3 rodadas reais (`codex exec --skip-git-repo-check`), notas finais Codex: E-014 9,3/10, design 9,5/10 (ambas ≥9,0 sem arredondar, Rodada 3 declarou `APROVADO`). **Decisão: opção (b) modificada** — `ConditionCheck(DocumentType.status=ACTIVE)` transacional (mesmo `buildExistenceConditionCheck`/`documentTypeKey` de D-175) adicionado a `submitEvidence()` (`guest-document-access-service.ts`) SOMENTE quando `input.documentType !== undefined` (presença explícita do campo cru — guard corrigido na Rodada 2 de `truthy` para `!== undefined`, já que string vazia deve ser tratada como "presente" mesmo que o schema HTTP já a rejeite antes de chegar ao serviço). Ausente continua caindo em `requirementId` sem qualquer validação, byte-idêntico ao comportamento anterior a esta decisão. **Achado real que descartou a opção (a) pura** (tornar `documentTypeId` obrigatório agora): nem `DocumentRequest` nem `Requirement` referenciam hoje nenhum `DocumentType` esperado — não existe mecanismo de descoberta para um guest saber qual `documentTypeId` é correto para o link que recebeu; tornar o campo obrigatório sem esse mecanismo forçaria o chamador a adivinhar, pior que o estado atual. Rejeitada (c) pura (não fazer nada): a rota já aceita um `documentType` explícito sem nunca validá-lo contra o catálogo — lacuna de integridade referencial real e corrigível sem quebrar nada. **E-014 SIM PARCIAL**, checklist com 4 critérios pesados somando 100% (C1 30% nunca quebra tráfego que hoje funciona sem o campo; C2 25% só valida valor que o próprio chamador forneceu, na primeira execução real de cada `idempotencyKey`; C3 30% nenhuma resposta distingue causa interna para chamador não autenticado, aplicação local de [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457); C4 15% obrigatoriedade plena só avança depois de existir mecanismo de descoberta, ancorado em [Martin Fowler — Parallel Change](https://martinfowler.com/bliki/ParallelChange.html)) — Rodada 1 citou fontes desatualizadas/sem peso (RFC 7807, Stripe fora de contexto), corrigido na Rodada 2/3 com URLs+data de acesso+pesos. **Achado real da Rodada 2 do Codex, resolvido explicitamente na Rodada 3**: `existingReplay` (lookup por `idempotencyKey`) roda ANTES de qualquer validação de payload — propriedade PRÉ-EXISTENTE do mecanismo (D-143 Decision 4, `fileName` também nunca foi revalidado num replay), não uma lacuna nova. Decisão: manter essa ordem — `idempotencyKey` identifica uma operação lógica, a primeira execução real de um dado key fixa o resultado, uma repetição do mesmo key com `documentType` diferente (mesmo inválido) devolve o snapshot original; C2 formalmente redefinido para cobrir só a primeira execução real. Oráculo residual reconhecido, não eliminado: para um guest com sessão válida, sucesso vs. `GuestAccessInvalidError` genérico revela se um `DocumentType` explicitamente enviado existe/está ACTIVE — superfície mais estreita que a enumeração pré-existente do próprio token/sessão (rate-limited, exige sessão já resolvida) e o mesmo padrão já aceito no caminho autenticado equivalente (`createDocument()`, D-175); trade-off deliberado, documentado. **Implementado na mesma sessão**: guard `documentTypeSupplied = input.documentType !== undefined` + `ConditionCheck` condicional adicionado ao array de `entries` de `submitEvidence()` antes dos `Put`s (mesma `TransactWriteItems`, TOCTOU-safe); cancelamento colapsa no `GuestAccessInvalidError` genérico já existente, nenhuma distinção nova de erro (anti-enumeração preservada). Sem mudança de schema HTTP, infra, ou RBAC. **G-V3 confirmado**: 5 testes novos em `guest-document-access-service.test.ts` (t1 ausente/fallback intocado — regressão do comportamento atual; t2 explícito ACTIVE sucesso; t3 explícito inexistente rejeita genérico; t4 explícito DEPRECATED rejeita genérico, TOCTOU-safe via flip real `transactWrite`, mesmo padrão de D-175; t_replay prova replay payload-agnóstico ignorando um `documentType` divergente/inválido na repetição). `typecheck`/`lint --max-warnings=0` limpos; suíte completa do repositório 1827/1827 verde (confirmada após a mudança, isolada a 2 arquivos de produção/teste sem overlap com WIP concorrente de outra sessão paralela em `infra/`/`requirement-reindex` no mesmo working tree — commit desta decisão inclui só os arquivos abaixo). Item 6 do arco D-173 (schema HTTP guest obrigatório + mecanismo `Requirement`→`DocumentType`) permanece próxima fatia independente, não decidida aqui. Ver `docs/architecture/reviews/submit-evidence-document-type-scoping/estado-final-consolidado.md` para o detalhe completo das 3 rodadas. | Nível 5-6 (muda a forma obrigatória/opcional de um contrato HTTP público real e um gate de validação de segurança/integridade — protocolo Claude↔Codex completo obrigatório, `AGENTS.md` §4) | 9,4 (retrospectivo, Claude concorda com o parecer do Codex na Rodada 3) | 9,3 (E-014) / 9,5 (design) | **APPROVED e IMPLEMENTADO** — merge para `main` sujeito a coordenação com sessão concorrente ativa no mesmo repo (D-183/requirement-reindex em `infra/`), ver `NEXT_SESSION_PROMPT.md` | `docs/architecture/reviews/submit-evidence-document-type-scoping/estado-final-consolidado.md`, `src/modules/document-archive/application/guest-document-access-service.ts`, `test/unit/document-archive/guest-document-access-service.test.ts` | 2026-09-02 |
206:| D-185 | **MaintenanceDueIndex — quarta fatia (requirement-reindex, D-179 slice 4, 4º de 9 workers)**: `requirement_reindex` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["requirement_reindex"]`/`worker_transact_write_policy_json["requirement_reindex"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItems` na tabela base, ambos só no role do `requirement_reindex_handler`. **Verificação prévia obrigatória (item nomeado pela tarefa) confirmou a correção da Rodada 3 de D-179**: lendo `reindex.ts` diretamente (não confiando na citação de linha do design), o worker de fato só faz UMA coisa — a drift pura SATISFIED→NOT_SATISFIED quando `evidenceValidUntil` passa sem nenhuma outra escrita tocar o Requirement — nunca uma "reindexação de applicability" mais ampla; `applicability` é persistido e nunca re-derivado por este worker. `deriveRequirementMaintenanceDue()` novo em `domain/requirement.ts` (função pura): due = `evidenceValidUntil` apenas quando `status === "SATISFIED"` E `evidenceValidUntil` existe — os outros 4 status nunca são candidatos, um SATISFIED sem `evidenceValidUntil` é SATISFIED para sempre por construção. `requirementGsi8Keys()` embute `requirementId` (não `subjectId`) no `GSI8SK`; a fonte candidata recupera `tenantId`/`subjectId`/`requirementId` do `PK`/`SK` da tabela base (`KEYS_ONLY` já devolve de graça), mesmo padrão de `documentFileGsi8Keys()`. **4 pontos de escrita no `DocumentArchiveService` gravam/limpam o ponteiro atomicamente, todos no MESMO `set`/`remove` da transição de `status` real** (`createRequirement`/`updateRequirement`/`linkEvidence`/`unlinkEvidence`) — um helper privado `requirementGsi8Fields()` centraliza a decisão para não duplicar a chamada a `deriveRequirementMaintenanceDue()` em 4 lugares. `scanSatisfiedRequirements` (`Scan` cross-tenant filtrado por `entityType`+`status`) REMOVIDO do port `DocumentArchiveStore` e do adapter DynamoDB real — nenhum outro call site o usava (confirmado por grep antes de tocar em qualquer arquivo), mesma disciplina "apagar o mecanismo morto, não deixar ao lado do novo" de D-183. `requirement-reindex/candidate-source.ts`+`dynamodb-candidate-source.ts` novos: `Query GSI8` substitui o `Scan`; `reindex.ts` reescrito para paginar via GSI8, revalidar cada candidato com um `store.get()` fresco (GSI8 nunca é fonte de elegibilidade, mesma postura de todo consumidor GSI8) e aplicar a atualização versionada existente (`buildVersionedUpdate`) incluindo `remove: ["GSI8PK","GSI8SK"]` — o `nextStatus` nunca é SATISFIED neste caminho (a derivação só chega a NOT_SATISFIED quando `evidenceValidUntil` já passou), então o ponteiro sempre é limpo incondicionalmente. **Sem poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada, mesmo raciocínio de D-183): este worker nunca teve fence de tenant `ACTIVE` — o único modo de falha de uma claim é `expectedVersion` desatualizado por um `linkEvidence`/`unlinkEvidence`/`updateRequirement` concorrente, sempre transitório e resolvido pela própria execução diária seguinte, nunca uma condição permanentemente falha para o mesmo candidato. `GlobalIndexComponent` ganha `"requirement-reindex"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.requirement_reindex_handler.function_name`. Novo `scripts/backfill-gsi8-requirement-reindex.ts`, cobrindo vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 4 workers já migrados. **G-V3**: 7 testes do worker (transição real + limpeza do ponteiro, due no futuro nunca tocado, sem `evidenceValidUntil` nunca candidato, status não-SATISFIED nunca candidato, múltiplos tenants, conflito concorrente via OCC, candidato "stale" da query eventualmente consistente nunca clobber um `evidenceValidUntil` já movido por um `linkEvidence` concorrente) + 12 testes do backfill. Suíte completa local 1827/1827 (após reconciliar com o WIP concorrente de D-184 no mesmo working tree — só os arquivos desta fatia foram staged/commitados, `guest-document-access-service.ts`/seu teste e `docs/architecture/reviews/submit-evidence-document-type-scoping/` intocados por esta sessão), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente, não só o agregado). Commit `720d881`, empacotado no PR #205 (aberto pela sessão concorrente de D-184, mesmo `develop`) — CI real (run `33665953101`) `success`, CD real (run `33666281116`) `success`, ambos confirmados via `gh run view` direto (nunca presumidos). **Verificação ao vivo completa contra `dev`, tudo via `aws --profile claude-dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-requirement-reindex-handler-role` tem as 3 políticas esperadas (base + `Gsi8ReadOnlyRequirementReindex` com a condição `LeadingKeys` exata `["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItemsRequirementReindex` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 3 sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#REQUIREMENT_REINDEX` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyRequirementReindex`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION` (namespaces dos 3 workers já migrados) → `implicitDeny` nos 3 casos, nenhuma statement casada. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quarta fatia mecânica seguindo o padrão de D-180/D-182/D-183, com a mesma decisão "sem poison-record" de D-183 por ausência de fence de tenant) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (4 de 9 workers migrados)** — `requirement-reindex` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 5 workers nomeados por D-179 permanecem em `Scan`+`Limit`: `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/requirement.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/ports/document-archive-store.ts`, `src/modules/document-archive/persistence/dynamodb-document-archive-store.ts`, `src/runtime/aws/handlers/requirement-reindex-handler.ts`, `src/workers/requirement-reindex/{candidate-source.ts,dynamodb-candidate-source.ts,reindex.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-requirement-reindex.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
207:| D-186 | **MaintenanceDueIndex — quinta fatia (quota-telemetry-purge, D-179 slice 5, 5º de 9 workers)**: `quota_telemetry_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["quota_telemetry_purge"]`/`worker_transact_write_policy_json["quota_telemetry_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItems` na tabela base, ambos só no role do `quota_telemetry_purge_handler`. **Forma distinta dos 4 anteriores, confirmada por leitura direta antes de qualquer código**: este worker cobre DOIS tipos de entidade que compartilham o mesmo formato de elegibilidade (`resetAt+30d`, fence de tenant `ACTIVE`) — `TenantQuotaRecord` e `EphemeralTelemetryMutation` (`src/modules/identity/application/quota.ts`) — e NENHUM dos dois tem campo `version`; ambos usam `count`/`resetAt` como fence OCC direto (mesma forma que D-153/`SECURITY_AUDIT` já usava para suas 4 entidades append-only, por razão diferente aqui: token bucket com campos otimistas próprios, não append-only). `deriveQuotaTelemetryMaintenanceDue()` (due = `resetAt + 30d`, NUNCA `undefined` — ao contrário de Membership/Invitation não existe estado terminal, `resetAt` sozinho determina elegibilidade) e `quotaTelemetryGsi8Keys()` (embute `entityType`+`SK` natural no `GSI8SK`, já que nenhuma das duas entidades tem um id próprio separado do seu `SK`) adicionados a `quota.ts`. **4 pontos de escrita gravam/refrescam o ponteiro GSI8 atomicamente no MESMO write que seta `resetAt`** (nunca um passo separado): `consume()`'s ramo de criação e ramo de rollover de janela (`TransactWriteItems` via `executeTenantBusinessMutation`, W3-07), e `identity-store.ts`'s `incrementTelemetryCounter()` (a lane `EphemeralTelemetryMutation` de D-136 D-D — `GSI8PK`/`GSI8SK` setados via `if_not_exists`, mesmo mecanismo que já protege `resetAt`/`purgeAfterTtl` contra um escritor perdedor concorrente rebobinar a janela; o valor é determinístico a partir do bucket de janela, calculado em `quota.ts` — camada de aplicação — e passado pronto ao adapter de persistência via `TelemetryIncrementInput.gsi8`, preservando a regra de `persistence/` nunca importar lógica de `application/`). `release()` não toca o ponteiro (só decrementa `count`, nunca muda `resetAt`). `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` fresco antes de qualquer escrita e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `resetAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182, já que este worker TEM fence de tenant `ACTIVE`, ao contrário de D-183/D-185): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#QUOTA_TELEMETRY"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (diferente de Invitation/Membership, decisão explícita e documentada): como `deriveQuotaTelemetryMaintenanceDue()` nunca retorna `undefined` e todo write que seta `resetAt` também refresca o ponteiro, não existe um "ponteiro sobrevivendo a uma transição terminal" real para se defender — a lacuna que o self-heal de `invitation-purge` cobre não tem equivalente estrutural aqui. `GlobalIndexComponent` ganha `"quota-telemetry-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.quota_telemetry_purge_handler.function_name`. Novo `scripts/backfill-gsi8-quota-telemetry-purge.ts`, cobrindo as duas entidades e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `resetAt` (não `version`, mesma adaptação do worker). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 5 workers já migrados. **G-V3**: 22 testes do worker (purga das duas entidades, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `resetAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ após MAX_ATTEMPTS, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo as duas entidades, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1841/1841 (1827+14 novos: quota.ts's docstring/campo GSI8PK/GSI8SK, 22 do worker migrado, 10 do backfill novo, 1 de cobertura de observabilidade), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). **Achado real de bug pré-existente, encontrado e corrigido só neste script (não nos 6 irmãos, fora de escopo desta fatia, registrado abaixo)**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` que decide se `main()` roda quando o script é executado diretamente NUNCA casa no Windows deste repo (`import.meta.url` usa barras normais — `file:///C:/...` — enquanto `process.argv[1]` usa contrabarras — `C:\\...`) — confirmado por teste direto (`node`/`tsx` imprimindo os dois valores). `main()` silenciosamente nunca executava sob o shell Windows/Git Bash deste ambiente (`AGENTS.md` §4) apesar de exit code 0 e nenhum erro — só descoberto ao tentar rodar o backfill desta fatia pela primeira vez nesta sessão (saída vazia inesperada). Corrigido neste arquivo trocando por `fileURLToPath(import.meta.url) === process.argv[1]` (comparação de dois paths nativos do SO, sem o mismatch de separador). **Mesmo bug confirmado presente nos outros 6 scripts** (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — não corrigido nesta sessão (fora do escopo de D-186), significa que os backfills anteriores (D-180/D-182/D-183/D-185) e qualquer execução passada desses scripts via este mesmo shell podem ter silenciosamente não escrito nada, mesmo reportados como "rodados com sucesso" — precisa de verificação/reexecução numa sessão futura, nomeado explicitamente para não ser esquecido. **PR #208 mergeado (commit `a898c1d`→`a280110`), CI real (`33687474935`/`33687503644`) e CD real (`33688100421`) `success`, todos confirmados via `gh run view` direto**. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-quota-telemetry-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyQuotaTelemetryPurge` com `LeadingKeys` exato `["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItemsQuotaTelemetryPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(30 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 4 sentidos)**: `WORK#QUOTA_TELEMETRY` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX` (os 4 já migrados) → `implicitDeny` nos 4 casos. **Backfill real executado contra `dev`** (após o fix do guard acima, sem o qual teria silenciosamente não escrito nada): 24 candidatos encontrados, 24 ponteiros GSI8 escritos, confirmado por `Query GSI8 WORK#QUOTA_TELEMETRY` retornando `Count=24`. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quinta fatia mecânica seguindo o padrão de D-180/D-182 (poison-record/DLQ), adaptada para 2 tipos de entidade sem `version`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (5 de 9 workers migrados)** — `quota-telemetry-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); achado de bug real do guard `main()` em scripts de backfill no Windows, corrigido só aqui, pendente nos outros 6; os 4 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/identity/application/quota.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-quota-telemetry-purge.ts` (novo), testes em `test/unit/{identity,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
208:| D-187 | **MaintenanceDueIndex — sexta fatia (security-audit-purge, D-179 slice 6, 6º de 9 workers)**: `security_audit_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["security_audit_purge"]`/`worker_transact_write_policy_json["security_audit_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItems` na tabela base, ambos só no role do `security_audit_purge_handler`. **Forma distinta das 5 fatias anteriores**: este worker cobre QUATRO tipos de entidade que compartilham o mesmo formato de elegibilidade (`occurredAt+365d`, fence de tenant `ACTIVE`) — `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (`src/modules/{expiration,organization,subject,activity}/domain/*.ts`), as mesmas 4 partições que `GET /activity`'s `ActivityService` k-way-merges — todas append-only por construção (sem `update()`/`delete()` exportado), sem campo `version`. Novo `src/shared/security-audit-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que a entidade cobre 4 módulos diferentes — mesma razão que já colocou `EntityKey`/`TransactWriteEntry` em `shared/dynamodb/occ.ts`): `deriveSecurityAuditMaintenanceDue()` (due = `occurredAt+365d`, NUNCA `undefined` — sem estado terminal, mesmo raciocínio de D-186) e `securityAuditGsi8Keys()` (embute `tenantId`+`entityType`+`SK` natural no `GSI8SK`, mesmo formato de `quotaTelemetryGsi8Keys()`). **O ponteiro GSI8 é gravado UMA ÚNICA VEZ, na própria função pura `build*Event()` de cada uma das 4 entidades** (`buildAuditEvent`/`buildMembershipAuditEvent`/`buildSubjectAuditEvent`/`buildTenantAuditEvent`) — não em cada um dos 19 call sites espalhados por 6 módulos que chamam essas funções (confirmado por grep antes de escrever qualquer código): como toda escrita passa pela mesma função de construção antes do `appendXAuditToTransaction`, estampar o ponteiro ali cobre automaticamente todo call site presente e futuro, sem precisar tocar nenhum deles individualmente — simplificação real habilitada pela forma já centralizada do código, não uma decisão de design nova. **Normalização `MembershipAuditEvent`**: declara `organizationId`, não `tenantId` (mesmo valor, nome diferente) — `buildMembershipAuditEvent()` já passa `organizationId` como `tenantId` para `securityAuditGsi8Keys()`, e o adapter do worker (`normalizeTenantId()`) faz a mesma normalização ao ler uma linha crua da tabela base, mesmo padrão pré-existente do worker antes desta fatia. `security-audit-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também o `TenantLifecycleStatusSource` como dependência separada do worker); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `occurredAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186, já que este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#SECURITY_AUDIT"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência que D-186 quota-telemetry-purge, causa diferente): as 4 entidades são append-only por construção — não existe transição de estado terminal que deixaria um ponteiro obsoleto para trás, e a ÚNICA forma de uma linha deixar de ser candidata é este próprio worker deletá-la (o que remove o ponteiro junto com a linha inteira). **Interação com `GET /activity` (D-149), reverificada nesta fatia**: releitura direta de `dynamodb-audit-partition-store.ts#queryPage` confirma que `ExclusiveStartKey` continua um marcador posicional puro, independente de GSI8 e de qualquer deleção concorrente deste worker — a conclusão original de D-153 (nenhum bug de correção, nenhuma correção necessária) permanece válida após a migração, confirmado por leitura, não presumido. `GlobalIndexComponent` ganha `"security-audit-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.security_audit_purge_handler.function_name`. Novo `scripts/backfill-gsi8-security-audit-purge.ts`, cobrindo os 4 tipos de entidade e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `occurredAt` (não `version`). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 6 workers já migrados. **G-V3**: 4 testes de gravação do ponteiro na criação (um por tipo de entidade, incluindo a normalização `organizationId`→`tenantId`), 2 testes do módulo puro `security-audit-gsi8.ts`, 17 testes do worker (purga dos 4 tipos, boundary exato, fence de tenant não-ACTIVE com contador de retry, normalização de `MembershipAuditEvent`, sem lifecycle record, corrida de `occurredAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/normalizeTenantId/processPage cobrindo os 4 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1867/1867 (175 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #210 mergeado (`5be0743`), CI real (`33691359230`/`33691948793`) e CD real (`33692216726`) `success`, todos confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-security-audit-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlySecurityAuditPurge` com `LeadingKeys` exato `["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItemsSecurityAuditPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 6 sentidos)**: `WORK#SECURITY_AUDIT` (próprio, com `--resource-arns` explícito no GSI8 — sem ele o simulate retorna `implicitDeny` mesmo para o caso positivo, achado de processo desta sessão) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY` (os 5 já migrados) → `implicitDeny` nos 5 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` corrigido roda de fato): 0 candidatos encontrados — a tabela `dev` não contém hoje nenhuma linha `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (confirmado por `Scan` bruto de `entityType`, dado sintético esparso), então nada foi escrito; confirmado por `Query GSI8 WORK#SECURITY_AUDIT` retornando `Count=0`, consistente com o dry-run. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sexta fatia mecânica seguindo o padrão de D-180/D-182/D-186 (poison-record/DLQ), adaptada para 4 tipos de entidade sem `version`, ponteiro centralizado nas 4 funções `build*Event()` em vez de múltiplos call sites) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (6 de 9 workers migrados)** — `security-audit-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 3 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/security-audit-gsi8.ts` (novo), `src/modules/{expiration,organization,subject,activity}/domain/*.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-security-audit-purge.ts` (novo), testes em `test/unit/{shared,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
209:| D-188 | **MaintenanceDueIndex — sétima fatia (transient-purge, D-179 slice 7, 7º de 9 workers)**: `transient_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["transient_purge"]`/`worker_transact_write_policy_json["transient_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItems` na tabela base, ambos só no role do `transient_purge_handler`. **Forma distinta de todas as fatias anteriores**: este worker cobre DOIS tipos de entidade com dinâmicas DIFERENTES — `WebhookInbox` (`src/modules/notification/application/ses-callback-workflow.ts`, criado uma vez/imutável, ponteiro gravado uma única vez na criação, mesma forma que security-audit-purge/D-187) e `UploadSlot` (`src/modules/document/domain/upload-slot.ts`, transições reais de status, mesma forma que invitation-purge/D-182 — ponteiro precisa ser gravado/atualizado a cada transição relevante, não só na criação). Novo `src/shared/transient-purge-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que as duas entidades cobrem 2 módulos diferentes — mesma razão de `shared/security-audit-gsi8.ts`): `deriveWebhookInboxMaintenanceDue()` (`createdAt+7d`, sempre), `deriveUploadSlotMaintenanceDue()` (`undefined` enquanto `RESERVED` — nunca candidato —, senão `reservedAt+7d` se `CONSUMED`, `reservedAt+24h` caso contrário incl. `EXPIRED`/`RELEASED`) e `transientPurgeGsi8Keys()`. **Achado de fronteira de módulo real durante a implementação**: a primeira versão deste arquivo reusava `computeUploadSlotPurgeAfter()`/`UploadSlotStatus` importados de `modules/document/domain/**`, violando `dependency-cruiser`'s regra `shared-must-not-reach-modules` (capturado por `test/architecture/tenant-fence-boundary.test.ts`, que falhou na primeira rodada de testes) — corrigido reimplementando a fórmula (`privacy-lgpd.md` §4, um-liner estável) diretamente em `shared/transient-purge-gsi8.ts` com um tipo `TransientUploadSlotStatus` local, em vez de inverter a fronteira de dependência. Um `UploadSlot` `RESERVED` NUNCA recebe ponteiro GSI8 (nunca aparece na Query GSI8 de forma alguma, diferente do `Scan` anterior que precisava filtrá-lo defensivamente em tempo de leitura). Três escritores atualizados para gravar/atualizar o ponteiro atomicamente na MESMA escrita condicional da transição: `ses-callback-workflow.ts` (criação do `WebhookInbox`), `advance-after-evidence.ts#appendSlotConsumption` (transição `CONSUMED`), `upload-slot-reconciliation/reconciliation.ts#processOneSlot` (transição `EXPIRED`) — nenhum writer real produz `RELEASED` hoje (confirmado por grep: status declarado no union mas sem write path), então nenhum terceiro escritor precisou de mudança. `transient-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também `TenantLifecycleStatusSource` como dependência separada do worker, embora a classe permaneça exportada por simetria com os irmãos, sem uso real); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por `version` — AMBAS entidades têm `version` real, diferente de security-audit-purge — na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#TRANSIENT"` após `MAX_ATTEMPTS=5`. Branch de self-heal de ponteiro obsoleto mantido por defesa (mesma postura de invitation-purge/D-182), embora nenhum caminho de escrita real produza esse caso hoje para nenhuma das duas entidades. `GlobalIndexComponent` ganha `"transient-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.transient_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-transient-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath`, nunca a comparação de string que falha silenciosamente no Windows), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e excluindo corretamente `UploadSlot` `RESERVED`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 7 workers já migrados. **G-V3**: teste de gravação do ponteiro na criação do `WebhookInbox` (`ses-callback-workflow.test.ts`), testes de gravação/atualização do ponteiro nas transições `CONSUMED`/`EXPIRED` do `UploadSlot` (`advance-after-evidence.test.ts`/`upload-slot-reconciliation.test.ts`), 22 testes do worker (purga dos 2 tipos, `RESERVED` nunca candidato, boundary exato para ambas janelas, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant, self-heal de ponteiro obsoleto) + 20 testes do backfill (parseArgs/encodeKey-decodeKey/deriveDue cobrindo os 2 tipos e exclusão RESERVED, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida, página mista). Suíte completa 1894/1894 (176 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #212 mergeado (commit `d32957f`→`ca50bcf` em `main`), CI real (`33695255525`/`33695850896`) e CD real (`33696134854`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-transient-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyTransientPurge` com `LeadingKeys` exato `["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItemsTransientPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#TRANSIENT` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT` (os 6 já migrados) → `implicitDeny` nos 6 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (WebhookInbox, UploadSlot)` sobre as 58 linhas da tabela (`ScannedCount=58`, `Count=0`, sem `LastEvaluatedKey` — tabela inteira coberta), e por `Query GSI8 WORK#TRANSIENT` retornando `Count=0`; nada para escrever, execução "real" (não-dry-run) seria um no-op confirmado, não executada. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sétima fatia mecânica seguindo o padrão de D-187 (poison-record/DLQ, entidade create-once) para `WebhookInbox` e D-182 (ponteiro atualizado a cada transição) para `UploadSlot`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (7 de 9 workers migrados)** — `transient-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 2 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/transient-purge-gsi8.ts` (novo), `src/modules/document/domain/upload-slot.ts`, `src/modules/document/application/advance-after-evidence.ts`, `src/modules/notification/application/ses-callback-workflow.ts`, `src/workers/upload-slot-reconciliation/reconciliation.ts`, `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-transient-purge.ts` (novo), testes em `test/unit/{shared,workers,document,notification}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
210:| D-189 | **MaintenanceDueIndex — oitava fatia (delivery-record-purge, D-179 slice 8, 8º de 9 workers)**: `delivery_record_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["delivery_record_purge"]`/`worker_transact_write_policy_json["delivery_record_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItems` na tabela base, ambos só no role do `delivery_record_purge_handler`. Este worker cobre DOIS tipos de entidade (`NotificationIntent`/`NotificationAttempt`, `src/modules/{reminder,notification}/domain/*.ts`), elegibilidade de idade única (`createdAt+180d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Diferença real de forma vs. D-187 (o análogo mais próximo, mesmo padrão poison-record/DLQ)**: AMBAS entidades carregam um contador OCC `version` real (ao contrário das 4 entidades append-only de D-187) — mesma forma que D-188/`UploadSlot`/`WebhookInbox`; o `Delete`/backoff/DLQ do worker e o backfill novo condicionam em `version` exato, com `createdAt` reasserted como defesa em profundidade adicional (nada muta `createdAt` hoje, mas a condição torna estrutural, não só verdadeiro por acidente). Novo `src/shared/delivery-record-gsi8.ts` (fora de `domain/` de módulo específico, já que a entidade cobre 2 módulos — mesma razão de `security-audit-gsi8.ts`/`transient-purge-gsi8.ts`): `deriveDeliveryRecordMaintenanceDue()` (due = `createdAt+180d`, NUNCA `undefined` — sem estado terminal, cada registro de entrega expira pelo prazo fixo independentemente de qualquer ação) e `deliveryRecordGsi8Keys()`. **3 pontos de escrita reais confirmados por grep** (não presumidos) gravam o ponteiro GSI8 UMA ÚNICA VEZ, no MESMO `Put` da criação: `reminder-dispatch/dispatch.ts` (intent `EXPIRATION_REMINDER`), `notification-router-workflow.ts`'s `applyStaleDecision` (intent `REPLACEMENT`/`CORRECTIVE`) e `applyRoutedDecision` (o `NotificationAttempt`) — nenhum dos dois entidades é atualizada depois de criada nestes caminhos (o `version` existe para as próprias transições de status do módulo, não para uma atualização do ponteiro GSI8). `delivery-record-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (handler perde a dependência separada de `TenantLifecycleStatusSource`, dobrada dentro do `ConditionCheck` transacional); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `createdAt`, na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#DELIVERY_RECORD"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência de D-187, mesma causa): nenhuma das duas entidades é atualizada depois de criada NA PRÁTICA (sem caminho de undelete, sem transição de status nomeada nos comentários do próprio worker) — o ponteiro é gravado exatamente uma vez, e a única forma de uma linha deixar de ser candidata é este worker deletá-la. **Gap conhecido e aceito, herdado de D-152, NÃO tocado por esta fatia**: `NotificationAttemptLookup` (ponteiro derivado, `PK=TENANT#t#ATTEMPT#id`) continua NÃO sendo purgado por este worker e fica órfão depois que um `NotificationAttempt` é deletado — confirmado ainda válido após esta migração por releitura direta, não presumido; fora de escopo aqui como já era em D-152. `GlobalIndexComponent` ganha `"delivery-record-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.delivery_record_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-delivery-record-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e condicionado em `version` (não `createdAt`, adaptação real vs. o template de D-187). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 8 workers já migrados. **G-V3**: 3 testes de gravação do ponteiro na criação (via os fixtures reais de `dispatch.test.ts`/`notification-router-workflow.test.ts` — intent do dispatch, intent REPLACEMENT do stale-decision, attempt do roteamento feliz), 24 testes do worker (purga dos 2 tipos, due no futuro nunca candidato, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `createdAt`/`version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo os 2 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa local 1912/1912 (178 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #215 mergeado (`6d333e0`→`c595190`), CI real (`33699314795`/`33699331683`) e CD real (`33699827095`) `success`, confirmados via `gh run view`/`gh run list` direto (polling síncrono, nunca aguardando notificação de background como subagente). **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-delivery-record-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyDeliveryRecordPurge` com `LeadingKeys` exato `["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItemsDeliveryRecordPurge` escopada à tabela base); `get-function-configuration`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#DELIVERY_RECORD` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT` (os 7 já migrados) → `implicitDeny` nos 7 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (NotificationIntent, NotificationAttempt)` sobre as 58 linhas da tabela (`scannedTotal=58`, `matched=0`, tabela inteira coberta), e por `Query GSI8 WORK#DELIVERY_RECORD` retornando `Count=0`; nada para escrever, execução real (não-dry-run) seria um no-op confirmado, não executada (mesma disciplina de D-187/D-188). **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — oitava fatia mecânica seguindo o padrão de D-187/D-188 (poison-record/DLQ), adaptada para 2 tipos de entidade COM `version` real, condicionado em `version`+`createdAt`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (8 de 9 workers migrados)** — `delivery-record-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); o único worker restante nomeado por D-179 permanece em `Scan`+`Limit`: `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/delivery-record-gsi8.ts` (novo), `src/workers/reminder-dispatch/dispatch.ts`, `src/modules/notification/application/notification-router-workflow.ts`, `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-delivery-record-purge.ts` (novo), testes em `test/unit/workers/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
211:| D-190 | **MaintenanceDueIndex — nona e ÚLTIMA fatia (core-user-data-purge, D-179 slice 9, 9º de 9 workers) — D-179 FECHADO POR COMPLETO.** `core_user_data_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["core_user_data_purge"]`/`worker_transact_write_policy_json["core_user_data_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItems` na tabela base, ambos só no role do `core_user_data_purge_handler`. Este worker cobre DOIS tipos de entidade (`ExpirationItem`/`ReminderPolicy`, `deletedAt+30d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Forma distinta de D-186/D-187/D-188/D-189 (create-once/janela fixa) — mesma forma do piloto original, D-180/`membership-purge`**: elegibilidade é gated numa TRANSIÇÃO de soft-delete (`deletedAt` sendo setado), não na criação — `deriveCoreUserDataMaintenanceDue()` (`src/shared/core-user-data-gsi8.ts`, novo, fora de `domain/` de módulo específico já que a entidade cobre 2 módulos — `expiration`/`reminder`) retorna `undefined` enquanto `deletedAt` está ausente; uma linha nunca vira candidata antes do soft-delete. **Achado real de pré-existência, confirmado por grep amplo (não presumido) antes de qualquer código novo**: `ExpirationItem.deletedAt` tem exatamente UM write path real — `expiration-service.ts#deleteItem` (via `transitionStatus`'s `extraSet`) — onde o ponteiro GSI8 foi wireado. `ReminderPolicy.deletedAt` NÃO TEM write path real nenhum no código hoje (`disablePolicy` só seta `enabled: false`, nunca `deletedAt`) — **condição PRÉ-EXISTENTE, não introduzida por esta migração**: o worker `Scan`-based anterior já tinha o mesmo gap (seu filtro `attribute_exists(deletedAt)` sobre `ReminderPolicy` também nunca poderia casar, pela mesma razão). `core-user-data-gsi8.ts` ainda deriva/gera chaves para `ReminderPolicy` de forma uniforme com `ExpirationItem` — no dia em que um soft-delete real for adicionado para ele, wireara o ponteiro é uma chamada de uma linha, sem mudança adicional no módulo compartilhado. **Mudança de forma real e esperada vs. o texto original do worker (D-151)**: `candidate-source.ts`'s comentário original dizia "plain `DeleteCommand`, not `TransactWriteItems` — D-151's design named no purge receipt"; isso muda agora porque o `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` da claim GSI8 precisa estar na MESMA transação do `Delete` (mesmo raciocínio de todo outro worker migrado com fence de tenant) — mudança correta e esperada, não um desvio a evitar. `core-user-data-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `deletedAt` exato — defesa em profundidade contra uma corrida de "undelete", mesmo sem caminho de undelete real existir hoje para nenhuma das duas entidades, mesma disciplina de D-151/D-189). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188/D-189): backoff exponencial capado via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#CORE_USER_DATA"` após `MAX_ATTEMPTS=5`. `GlobalIndexComponent` ganha `"core-user-data-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.core_user_data_purge_handler.function_name`. Novo `scripts/backfill-gsi8-core-user-data-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, só linhas já soft-deletadas (nunca escreve ponteiro para uma linha sem `deletedAt`), vencimento FUTURO e passado. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 9 workers — **matriz de isolamento completa, todos os 9 namespaces GSI8 agora provados mutuamente exclusivos**. Suíte completa local 1929/1929 (179 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). PR #217 mergeado (`1633d6c`→`1e483c1`), CI real (`33702974051`) e CD real (`33703151890`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-core-user-data-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyCoreUserDataPurge` com `LeadingKeys` exato `["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItemsCoreUserDataPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 5 * * ? *)`). **Prova de isolamento cross-namespace — a mais ampla do programa inteiro, 8 verificações negativas** (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8): `WORK#CORE_USER_DATA` (próprio) → `allowed`; os 8 namespaces já migrados (`WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT`/`WORK#DELIVERY_RECORD`) → `implicitDeny` nos 8 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard roda de fato): 0 candidatos — confirmado por `Scan` bruto cobrindo as 58 linhas inteiras da tabela filtrado por `entityType IN (ExpirationItem, ReminderPolicy)` retornando `Count=0` (a tabela `dev` não tem hoje NENHUMA linha desses dois tipos, muito menos soft-deletada), e por `Query GSI8 WORK#CORE_USER_DATA` retornando `Count=0`; execução real seria um no-op confirmado. **D-179 (MaintenanceDueIndex) está FECHADO — os 9 workers nomeados pelo design estão 100% migrados, verificados ao vivo em `dev`, com prova de isolamento IAM cross-namespace completa**: `membership-purge` (D-180/D-181), `invitation-purge` (D-182), `document-file-reconciliation` (D-183), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186), `security-audit-purge` (D-187), `transient-purge` (D-188), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190). **Item pendente carregado, ainda não resolvido, agora o item real mais proeminente desta área**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia; como D-179 fecha aqui, este é o item de maior prioridade real que sobra nesta área de trabalho. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — nona e última fatia mecânica, mesma forma do piloto original D-180 — transição de soft-delete, não criação) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO — D-179 COMPLETO (9 de 9 workers migrados)** — `core-user-data-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo completa contra os 8 outros namespaces); nenhum worker nomeado por D-179 permanece em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/core-user-data-gsi8.ts` (novo), `src/modules/expiration/application/expiration-service.ts`, `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-core-user-data-purge.ts` (novo), testes em `test/unit/{expiration,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
212:| D-191 | **RequirementTemplate — item 1 (P0.1) do roadmap de lançamento, design `APPROVED` E implementado ponta a ponta na mesma sessão.** Protocolo Claude↔Codex completo, 4 rodadas reais (`codex exec`, prompt por arquivo em primeiro plano), régua contestada na Rodada 1 e reconciliada em 4 versões (`research-protocol.md`): Rodada 1 Claude 8,2 / Codex régua 6,4 design 6,6 → R2 9,2/9,1 vs 8,3/8,1 → R3 9,4/9,3 vs 8,8/8,7 → **R4 9,4/9,3 vs 9,1/9,0** (ambos ≥9,0 nas duas dimensões, sem arredondar). E-014 `SIM PARCIAL` — 5 fontes externas com data de acesso (Drata "will never modify your policies without your involvement", Vanta, Asana, Atlassian/Jira `Clone`, HighLevel Snapshots), amostra justificada como representativa por cobrir o nicho funcional exato (compliance/GRC) e o de maior volume de uso (PM genérico); **padrão convergente**: materialização por CÓPIA no apply, nunca live-link com propagação. Divergência registrada: prevenção de duplicidade no apply não tem padrão externo (o Asana explicitamente não a oferece) — desenhada internamente. **Decisões**: (1) a entidade-alvo é `Requirement` (document-archive, D-143/D-145), não a legada `RequirementAssignment`, que fica intocada; (2) snapshot no apply, com `sourceTemplateId`/`sourceTemplateItemId`/`sourceTemplateAppliedVersion` como rastro que NENHUM caminho de leitura/derivação/worker consulta; (3) **decisão de produto declarada explicitamente** (não inferida do precedente de `DocumentType`, correção epistêmica do Codex): o nome de um `Requirement` é único por Subject, com as 4 consequências respondidas e gatilho de reversão nomeado (campo `period` entra na chave do ponteiro), enforced transacionalmente por `RequirementNamePointer` em TODOS os caminhos de escrita — `createRequirement`/`updateRequirement`/`deleteRequirement`/`applyTemplate`, não só no novo; (4) itens embutidos, cap 30; (5) `planTemplateApplication` puro com uma implementação e dois call sites (preview e apply). **5 achados reais fechados, cada um levantado por uma rodada do Codex e verificado por leitura direta do código, não por argumento**: `createRequirement` nunca verificava o Subject (criava `Requirement` sob Subject inexistente/ARCHIVED — lacuna pré-existente); `status <> DELETED` deixaria `ARCHIVED` passar (`TrackedSubjectStatus` é `ACTIVE\|ARCHIVED\|DELETED`, fence passa a enumerar `= ACTIVE`); dois itens de template normalizando para o mesmo nome virariam dois `Put` na MESMA chave de ponteiro numa transação (`ValidationException`/500 opaco em vez de 409 — fechado por `assertTemplateItemNamesUnique` em create/update/duplicate + planejador); fence do template passa a ser `status = ACTIVE AND version = <lida>`, não só o status (custo zero em ações, fecha o TOCTOU de aplicar itens de uma versão já editada); classificação de cancelamento estrutural por `labels` por entrada, nunca índice literal, e **nenhum erro afirma causa que o DynamoDB não revelou** (removida a releitura que escolhia entre 404 e 409 — `SubjectPreconditionFailedError` e `TemplatePreconditionFailedError` únicos). **Erro real meu, corrigido na Rodada 4**: a reserva do cap 30 estava justificada por aritmética impossível (com `C+S<=30`, proteger skips nunca chega a 93); a justificativa correta é o pior caso previsto `3N+3` (um evento de auditoria por `Requirement`, lacuna conhecida deste módulo), que impõe teto `N<=32` — 30 é escolha operacional abaixo do teto, não valor derivado. Contrato transacional do apply: `2N+3` ações. Limites em BYTES UTF-8 (não caracteres) + `estimateDynamoItemBytesUpperBound()` documentado como limite superior (lança em tipo não coberto, condição explícita do Codex). Contrato de honestidade declarado: preview e apply não podem divergir algoritmicamente mas podem divergir temporalmente; `skip` é observação de planejamento e NÃO é protegido transacionalmente (só `create` é); a leitura do plano é completa (`queryByPk` esgota `LastEvaluatedKey`, verificado) porém eventually consistent, e reaplicar é seguro e idempotente. **Achado pré-existente registrado, deliberadamente fora do escopo, com identificador e próximo passo concreto (`RT-LANE-FALLBACK-01`)**: `tenant-business-mutation.ts` linhas 213-223 classifica `CancellationReasons` ausente/malformado como `TenantNotActiveError`, afirmando causa não revelada — fatia própria nível 3-4, mesma disciplina de D-177→D-178 e D-183. | Nível 5-6 (modelo de dados novo + operação de fan-out transacional + regra de domínio nova sobre uma entidade existente) | Claude 9,3 | Codex 9,0 | **IMPLEMENTADO — gate local completo verde**: `typecheck`, `lint`, 1931/1931 (`test/unit`+`integration`+`contract`, 22 testes adversariais G-V3 novos + 11 de contrato + 2 de allowlist), `check-docs`, `validate-schemas`, `build:lambdas`, `dependency-cruiser` 0 violações, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). Os 2 arquivos de `test/architecture/` que falham neste worktree falham IDENTICAMENTE na árvore sem a mudança (confirmado por `git stash`) — artefato de o worktree não ter `node_modules` para os processos filhos (`tsc`/`dependency-cruiser`), não regressão. **VERIFICADO AO VIVO (2026-09-03)**: PR #219 mergeado (`72ce4bb`→`4c90d44`), CI (`33713015703`/`33713051982`/`33713271496`) e CD (`33713455836`) reais `success` via `gh run view`; contra `dev`, `exptrk-dev-document-archive-handler` `Active`/`Successful` e **as 9 rotas `requirement-templates` confirmadas presentes** (`aws apigatewayv2 get-routes --api-id uav7id1muh --profile claude-dev`). O CI verde também confirmou que os 2 arquivos de `test/architecture/` passam com `node_modules` resolvível — as falhas locais eram artefato do worktree, não regressão. | `docs/architecture/reviews/requirement-template-scoping/` (9 arquivos), `src/modules/document-archive/domain/requirement-template.ts` (novo), `src/modules/document-archive/domain/requirement.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/http/document-archive-handlers.ts`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/errors/app-error.ts`, `src/runtime/aws/{ids.ts,handlers/document-archive-handler.ts}`, `infra/modules/api-gateway/main.tf`, 7 schemas em `schemas/api/`, testes em `test/{unit,integration,contract}/` | 2026-09-03 |
217:| D-193 | **Reconciliação OCR/AI-Extração (M7) com o Document Lifecycle (D-143+) — fase 2 do divide-and-conquer de P0.4, design apenas — implementação é fase 3.** Protocolo Claude↔Codex completo, 5 rodadas reais (`codex exec`, foreground), fechado na Rodada 5 (Claude 9,2/Codex 9,4) — ambos ≥9,0 sem arredondar, dentro do limite de 5 rodadas. E-014: `SIM PARCIAL` — mecanismo human-in-the-loop (SUGGESTED/CONFIRMED, confiança, agreement) já é padrão estabelecido (AWS Textract A2I Human Review Workflows, Hyperscience, DocuSign Insight, fontes com data) e não é redesenhado; identidade de agregados/chave S3/transação OCC/convergência assíncrona são internos, sem padrão externo aplicável. **Achado central (verificado por leitura direta, não presumido)**: o módulo `document` antigo é hoje o ÚNICO pipeline fisicamente completo em `dev` — `upload-finalizer-handler.ts`/`malware-result-handler.ts` só reconhecem `parseQuarantineKey`/`parseSubmissionQuarantineKey`, descartando definitivamente (sem retry/DLQ) qualquer chave `document-archive/...`; `applyFileScanResult`/`confirmFileScanClean` (document-archive) não têm NENHUM call site real — um arquivo enviado via `document-archive` fica preso em `PENDING_UPLOAD` para sempre. `document-archive` é a autoridade de domínio aprovada (D-143) e recebe tráfego HTTP roteável, mas não tem consumidor físico real para nenhum dos dois sinais de scan. Correção ao enquadramento da Fase 1: `extraction/domain/document-classifier.ts` é um classificador de FORMATO de arquivo (PDF/JPEG/PNG/TIFF), não um classificador de tipo de documento de negócio ligado a `DocumentType` (D-176) — não existe hoje nenhum classificador de tipo de documento por IA. **Design final**: sequenciamento (não escopo indivisível) — contrato desenhado agora, ingestão física (terceiro branch em `upload-finalizer-handler.ts`/`malware-result-handler.ts`, sem fila nova) e re-key da extração implementados independentemente, ativados por 2 feature flags em ORDEM obrigatória (starter antes do promoter); `ExtractionRun` re-chaveado por `{tenantId,documentId,versionId}` (nunca mais `itemId`); chave clean `document-archive/clean/<t>/<d>/<versionId>/<fileId>`; só arquivo `PRINCIPAL` dispara OCR (decisão de produto provisória); starter sempre relê `DocumentFile`/`DocumentVersion` frescos antes de agir, nunca confia só na chave S3; transação de confirmação com cardinalidade FIXA (`confirmField` 3 agregados/4 ações incl. outbox, `rejectField` 2/2, nunca depende de N Requirements); planner único `planDocumentVersionValidityEffect` unifica confirmação manual e auto-confirmação (achado real: a Rodada 1 contradizia `run-extraction-validation.ts`'s auto-confirm já existente); `Requirement` NUNCA entra na transação de confirmação — converge de forma assíncrona via outbox (`SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`, mesmo padrão de `SQS_IMPORT_PARSE_V1`/D-192) + worker que sempre relê estado corrente (nunca aplica o payload do evento, fechando risco de ordem/perda) + novo índice reverso esparso `GSI_EVIDENCE`; rede de reparo autoritativa via `scanRequirementsWithEvidence` (novo método em `DocumentArchiveStore`, mesmo precedente de `scanActiveSeries` para o problema de "sem enumeração de tenant"), filtrado por `attribute_exists(evidenceVersionId)` — independente de status cacheado, cobre `SATISFIED`/`PENDING`/`NOT_SATISFIED` e qualquer transição futura. Este design explicitamente anda sobre o bounded-staleness que `requirement.ts` já aceita para `DocumentVersion` mudando após o link (Decision 5, D-143), só estreitando a janela. `ExtractedField` ganha `confirmedBy`/`confirmedAt`. | Nível 5-6 (`change-risk-scale.md` — re-chaveia um trigger de evento vivo, redesenha uma transação OCC cross-agregado, define um novo contrato de fluxo de dados IA→`Requirement`) | 9,2/10 | 9,4/10 | **APPROVED (design apenas)** — implementação é a Fase 3 seguinte, ainda não iniciada | `docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/` | 2026-09-03 |
218:| D-193 | Fecha o GAP de slice 2 + wireia `startExecution()` real — slice 3/9 (ainda item 3 do design). `run-extraction-validation.ts`'s `commitOrDiscard()` ganha um discriminador `ExtractionExecutionInput.documentSource: "DOCUMENT" \| "DOCUMENT_ARCHIVE"` (default `"DOCUMENT"` para qualquer execução iniciada antes desta fatia): para `DOCUMENT_ARCHIVE`, o guard TOCTOU passa a ler o `Document` do `document-archive` (por `documentId` sozinho, nunca `itemId`) em vez do `Document` do módulo antigo — reaproveitando o MESMO `DocumentReader.get()` genérico (single-table, sem porta nova) e o MESMO `ExtractedFieldStore.commitRunOutcome()` (campos `documentKey`/`documentExpectedVersion` já genéricos, zero mudança na store). Decisão real, documentada inline: ao contrário do `status==="DELETED"` do módulo antigo, `document-archive`'s `status==="ARCHIVED"` NÃO descarta o commit — é soft-hide por design (`document.ts`'s próprio invariante "archiving is safe by construction"), só a ausência da linha descarta. **Gap do `fileName` fechado sem tocar o classificador**: `DocumentFile` de fato não tem `fileName` (achado real confirmado, não contornado) — `start-extraction-run-for-document-archive.ts` passa `""` (nunca um nome fabricado); a cadeia de fallback já existente do classificador (magic bytes -> extensão -> `contentType`) degrada corretamente para `classifyByContentType(DocumentFile.mediaType)`, que já cobre os 4 formatos que o Textract suporta. `ExtractedField.documentVersion` (número) recebe `DocumentVersion.seq` no caminho novo (mesmo campo genérico, sem migração de schema). O write auto-confirm de `ExpirationItem.dueDate` (W2-01-DECISION) fica OLD-path-only nesta fatia — o equivalente `DocumentVersion.validUntil` é item 4/9, explicitamente fora de escopo aqui. `itemId` no caminho `DOCUMENT_ARCHIVE` vira passthrough opaco (`= documentId`) documentado explicitamente — nenhum consumidor downstream (`start-ocr.ts`, `run-deterministic-parser.ts`, `run-bedrock-extraction.ts`, `complete-ocr.ts`, `TextractJob.itemId`) o usa para lookup, só log/round-trip. `startExecution()` agora É chamado de verdade pelo Starter (sempre, independente de `created`, mesma disciplina do trigger antigo), com `correlationId` agora threading corretamente pelo handler. Composição (`buildExtractionStarterWorkerDepsForDocumentArchive`) ganha `stateMachineArn`. G-V3: teste provando que as 5 precondições satisfeitas agora disparam um `startExecution()` real (não mais silenciosamente `GATE_OPENED`-only) com o `ExtractionExecutionInput` completo asserted campo-a-campo; teste de regressão provando o caminho `DOCUMENT` antigo (`documentSource` undefined) segue byte-idêntico; testes cobrindo `ARCHIVED` não-descarta, Document ausente descarta, TOCTOU do `commitRunOutcome`, e classificação real a partir de `fileName=""`+`contentType`. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 3/9) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint limpos, suíte 2074/2074 — 21 testes novos —, check-docs/validate-schemas/build:lambdas limpos; sem mudança de infra/Terraform). D-193 item 3/9 do design agora FECHADO por completo (Starter + validação + execução real, ponta a ponta). Próxima fatia: item 4/9 — `planDocumentVersionValidityEffect` (novo, `extraction/domain/`) + integração em `doConfirmField`/`commitRunOutcome`; campos `confirmedBy`/`confirmedAt` em `ExtractedField`; transação de `confirmField` re-chaveada para 3 agregados/4 ações (incl. outbox só quando há mudança real de `validUntil`) e `rejectField` para 2 agregados/2 ações — `Requirement` explicitamente excluído de ambas as transações por design. | `src/modules/extraction/application/{run-extraction-validation.ts,start-extraction-run-for-document-archive.ts,start-extraction-run.ts}`, `src/modules/extraction/ports/extraction-execution-starter.ts`, `src/runtime/aws/{composition/extraction.ts,handlers/extraction-starter-handler.ts}`, testes em `test/unit/extraction/{run-extraction-validation,start-extraction-run-for-document-archive,start-extraction-run,document-classifier}.test.ts` | 2026-09-03 |
219:| D-193 | Item 4/9 do design (`planDocumentVersionValidityEffect` + `confirmedBy`/`confirmedAt` + transações re-chaveadas). Novo planner puro `extraction/domain/document-version-validity-effect.ts` — único ponto que decide o efeito em `DocumentVersion.validUntil` a partir de `{fieldName, confirmedValue, documentVersion.{state,validUntil}}`: só `expirationDate` produz efeito; só estados elegíveis (`RECEIVED`/`UNDER_REVIEW`/`ACCEPTED`, mesmo conjunto do Starter) aceitam mudança; `validUntil` byte-idêntico ao já gravado planeja `NO_CHANGE` (é isso que torna o outbox condicional de verdade, não sempre-disparado). Chamado IDENTICAMENTE pelos dois caminhos: novo `confirm-reject-field-document-archive.ts` (manual, `confirmFieldForDocumentArchive`/`rejectFieldForDocumentArchive` — mecanismo novo, sem rota HTTP ainda, deliberadamente não wireado nesta fatia) e `run-extraction-validation.ts`'s `persistExtractedFieldsStage` (auto-confirm, `buildAutoConfirmDocumentVersionUpdate`, fechando o gap que a slice 3 deixou aberto explicitamente). `ExtractedField` ganha `confirmedBy`/`confirmedAt` (proveniência, critério 6 do checklist do design) — setado em AMBOS os caminhos, inclusive o `document`-module ANTIGO (`confirm-reject-field.ts`'s `doConfirmField`, mudança aditiva, cardinalidade da transação antiga inalterada). Transação nova `confirmFieldForDocumentArchive`: 3 agregados/4 ações (`DocumentVersion` Update sempre presente — mesmo em `NO_CHANGE` só bump de version/updatedAt —, `ExtractionRun` ConditionCheck, `ExtractedField` Update, `Outbox` Put só quando `effect.kind==="SET"`). Transação nova `rejectFieldForDocumentArchive`: 2 agregados/2 ações (`ExtractedField` Update, `ExtractionRun` ConditionCheck) — `DocumentVersion` NUNCA referenciada, nem por `ConditionCheck`. `Requirement` confirmado por leitura do texto do design como fora de QUALQUER uma das duas transações (convergência assíncrona é item 5/9, não tocado). Novo `OutboxDestination = "SQS_REQUIREMENT_EVIDENCE_REFRESH_V1"` adicionado ao union (valor existe desde já, sem fila/rota/worker reais — isso é item 5/9/6/9). **Escopo deliberadamente reduzido, nomeado explicitamente**: a unificação do caminho `document` ANTIGO com `document-archive` numa única transação NÃO foi feita — o módulo antigo não tem conceito de `DocumentVersion`/`Requirement`, então a transação antiga (`confirmField`/`rejectField` em `confirm-reject-field.ts`, 4-way incluindo `ExpirationItem`/`Document`) permanece byte-idêntica em cardinalidade, só ganhando `confirmedBy`/`confirmedAt` como campo adicional no mesmo Update já existente. O planner único é o real ponto de unificação (critério 5 do checklist — mesmo efeito por construção nos dois caminhos), não uma transação física compartilhada entre os dois módulos. G-V3: 8 testes do planner (SET/overwrite/NO_CHANGE por-valor-idêntico/NO_CHANGE por-estado-inelegível×4/campo-não-mapeado); 13 testes do novo `confirm-reject-field-document-archive.ts` (overwrite real, outbox condicional provado via o `effect` computado passado à store — não só o efeito colateral —, reject nunca toca `DocumentVersion`, `Requirement` semeado e provado BYTE-IDÊNTICO após confirm E após reject via `toEqual` no fixture original, idempotência, 409s nos 3 agregados, 404, cross-tenant); testes novos em `run-extraction-validation.test.ts` provando o auto-confirm agora fecha o gap da slice 3 (SET real, NO_CHANGE por valor idêntico, NO_CHANGE por estado inelegível, proveniência `SYSTEM_AUTO_CONFIRM`). | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 4/9, escolha de engenharia real registrada: unificação de transação física adiada, planner é o ponto de unificação) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint limpos, suíte 2097/2097 — 23 testes novos —, check-docs/validate-schemas/build:lambdas limpos; sem mudança de infra/Terraform, `terraform test` não rodado). D-193 item 4/9 do design FECHADO (planner + proveniência + as duas transações re-chaveadas), com a ressalva de escopo acima registrada. Próxima fatia: item 5/9 — convergência assíncrona de `Requirement`: novo índice reverso esparso `GSI_EVIDENCE` + `linkEvidence`/`unlinkEvidence` passam a escrevê-lo (item 5), fila+DLQ+rota no `DispatchOutboxRelay`+worker `requirement-evidence-refresh-handler.ts` que NUNCA aplica o payload do evento, sempre relê `DocumentVersion`+`Requirement` frescos (item 6). | `src/modules/extraction/{domain/{extracted-field.ts,document-version-validity-effect.ts},application/{confirm-reject-field.ts,confirm-reject-field-document-archive.ts,run-extraction-validation.ts},ports/extracted-field-store.ts,persistence/dynamodb-extracted-field-store.ts}`, `src/shared/outbox/outbox.ts`, testes em `test/unit/extraction/{confirm-reject-field,confirm-reject-field-document-archive,document-version-validity-effect,run-extraction-validation}.test.ts` | 2026-09-03 |
220:| D-193 | Item 5/9 do design (`GSI_EVIDENCE` — novo índice reverso esparso — + `linkEvidence`/`unlinkEvidence` passam a escrevê-lo). Novo GSI físico GSI9 (GSI1-8 já ocupados por decisões anteriores, incluindo D-179/D-190's programa GSI8 de 9 workers) — `requirementGsi9Keys()`/`requirementGsi9PartitionKey()` em `document-archive/domain/requirement.ts` constroem `GSI9PK=TENANT#<t>#DOCVERSION#<versionId>` / `GSI9SK=REQUIREMENT#<requirementId>` (SK inclui `requirementId` de propósito — dois `Requirement`s DIFERENTES, de Subjects diferentes, podem legitimamente referenciar a mesma evidência). `linkEvidence()` (`document-archive-service.ts`) passa a incluir essas duas chaves no mesmo `set` já existente (SET simples — sem ação nova na transação, cardinalidade inalterada); `unlinkEvidence()` passa a incluir `GSI9PK`/`GSI9SK` na lista `remove` (REMOVE de verdade, não `undefined`/null — esparso por construção, provado por `hasOwnProperty` no teste, não só "query vazia"). Nova query `DocumentArchiveService.findRequirementsByEvidenceVersion(tenantId, versionId): Promise<Requirement[]>` pagina o GSI9 inteiro (loop `exclusiveStartKey`, nunca só a 1ª página — prova real via 2 Requirements de Subjects diferentes referenciando a mesma evidência) via `queryIndexPage({indexName:"GSI9",...})` (porta `IndexPageInput`/`DynamoDbDocumentArchiveStore` ganham `"GSI9"`no union/mapa de atributo, mesmo padrão genérico de GSI1/2/5 já existente — nenhuma duplicação de lógica de Query). Deliberadamente SEM `RequestContext`/`authorize()` — leitura de sistema para o worker assíncrono da próxima fatia, mesma postura de `scanActiveSeries` (recurrence materializer). Terraform: `GSI9` (projeção `ALL`, não `KEYS_ONLY` como GSI8 — o worker da próxima fatia precisa do `Requirement` inteiro numa Query só) adicionado a `infra/modules/dynamo-table/main.tf`, tenant-scoped (PK carrega `TENANT#<t>#...`) logo entra em `tenant_facing_index_names` como GSI1/2/5/7 — sem isolamento de IAM dedicado como GSI3/6/8 (sem risco de fan-out cross-tenant, a PK já fixa o tenant). **Achado real durante a fatia, corrigido no caminho**: o teste `terraform test` `dynamo_table.tftest.hcl`'s `length(aws_dynamodb_table.this.global_secondary_index) == 7` estava desatualizado desde a criação do GSI8 (D-179) — ninguém atualizou a contagem para 8, o teste continuou passando silenciosamente porque a asserção nunca tinha sido re-executada como gate (`terraform test` não constava nos gates das fatias 1-4 por não terem mexido em infra); corrigido para 9 nesta fatia, `run` renomeado, mais um `run` novo asserindo a forma exata de GSI9 (`ALL`/`GSI9PK`/`GSI9SK`). G-V3: 3 testes de key builder (`requirement.test.ts` — par PK/SK correto, `requirementGsi9PartitionKey` bate exatamente o `GSI9PK` de `requirementGsi9Keys`, dois `requirementId` diferentes com o mesmo `evidenceVersionId` compartilham PK mas têm SK distinto); 5 testes de integração em `requirement-service.test.ts` (link popula GSI9 e é queryable; unlink remove de fato via `hasOwnProperty`, não só null; Requirement recém-criado sem evidência nunca tem GSI9PK/SK — sparse provado, não presumido; query encontra TODOS os Requirements de dois Subjects diferentes referenciando a mesma evidência, não só o primeiro; relink move o ponteiro — a evidência antiga para de encontrar o Requirement, a nova encontra). | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 5/9 — único ponto real de engenharia fora do design: qual GSI físico usar, decidido como GSI9 por eliminação, já que GSI1-8 estão todos ocupados) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-docs/validate-schemas/build:lambdas limpos, suíte **2105/2105** — 12 testes novos —, `terraform test` **7/7** `AWS_PROFILE=claude-dev`, mocked provider). Commit `9c3562f`. D-193 item 5/9 do design FECHADO (índice + primitivas de leitura/escrita, testado isoladamente — nenhum consumidor/worker chamando `findRequirementsByEvidenceVersion` ainda, isso é a próxima fatia). Próxima fatia: item 6/9 — fila `requirement-evidence-refresh-queue`+DLQ, rota no `DispatchOutboxRelay` para `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` (já existe no union desde a slice 4, sem consumidor), handler `requirement-evidence-refresh-handler.ts` que NUNCA aplica o payload do evento — sempre relê `DocumentVersion`+`Requirement` frescos (via `findRequirementsByEvidenceVersion` desta fatia) e re-deriva um Requirement por vez com seu próprio loop OCC, evento é só "wake up" (princípio de correção já estabelecido nas fatias 2-3: nunca confiar no payload/chave do evento, sempre reler o agregado fresco). | `src/modules/document-archive/{domain/requirement.ts,application/document-archive-service.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `infra/modules/dynamo-table/{main.tf,tests/dynamo_table.tftest.hcl}`, testes em `test/unit/document-archive/{requirement,requirement-service}.test.ts` | 2026-09-03 |
221:| D-193 | Item 6/9 do design — convergência assíncrona real de `Requirement`: consumidor de `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` ponta a ponta. Novo `src/workers/requirement-evidence-refresh/refresh.ts` (`refreshRequirementsForEvidenceVersion`) — usa `findRequirementsByEvidenceVersion` (slice 5) só para DESCOBERTA de candidatos; para CADA `Requirement` candidato, relê fresco (`store.get`), depois relê fresca a `DocumentVersion` via o `evidenceDocumentId`/`evidenceSeq` ATUAIS do próprio `Requirement` (nunca o `versionId` do hint do evento — se um relink já moveu o ponteiro, segue o vínculo corrente), recomputa `status`/`evidenceState`/`evidenceValidUntil`/GSI1/GSI8 via as mesmas funções puras de `linkEvidence`, e só then faz `TransactWriteItems` OCC (nunca `UpdateItem` solto). Sem mudança real = no-op sem `transactWrite` (sem bump de version espúrio). Loop de retry OCC próprio por Requirement (até 5 tentativas, relendo a cada volta); falha terminal de UM Requirement é registrada (`failedRequirementIds`) sem abortar os outros do mesmo lote — mesma disciplina de isolamento por-item que `dispatch-outbox-relay-processor.ts` já tem por-record, uma camada mais funda. **Achado real que fechou um gap deixado pela slice 4**: nenhum dos dois payloads de evento (`confirmFieldForDocumentArchive`/`commitRunOutcome`, ambos em `dynamodb-extracted-field-store.ts`) carregava `versionId` — só `documentId`+`validUntil` (o próprio valor que o design manda nunca confiar) — sem `versionId` a descoberta via GSI9 seria impossível; `versionId` adicionado como campo novo em `CommitDocumentVersionUpdate`/`ConfirmFieldForDocumentArchiveInput` e threadado dos dois call sites (`run-extraction-validation.ts`/`confirm-reject-field-document-archive.ts`, que já liam a `DocumentVersion` inteira, `version.versionId` já disponível) — usado SÓ como chave de busca no GSI9, nunca como fonte da state recomputada (a re-leitura fresca é o que decide tudo). Novo `requirement-evidence-refresh-handler.ts` (SQS, mesmo formato thin-entrypoint de `import-parse-handler.ts`) extrai só `{tenantId, versionId}` da mensagem (tipo `RequirementEvidenceRefreshHint`, sem mais nenhum campo alcançável) — batch item reportado como falho no handler quando `result.failed > 0`, deixando a política de redrive/DLQ real da fila decidir o retry. `buildOutboxRelayDeps` (composição) e `outbox-sweeper-handler.ts` ganham o 7º/6º sender opcional (`SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`), mesmo padrão incremental de todas as fatias anteriores (D-192 slice 9 etc.). Terraform: nova fila+DLQ `requirement-evidence-refresh-queue` (módulo `sqs-worker-queue`, já inclui DLQ+alarm), novo Lambda `requirement-evidence-refresh-handler` (só `tenant_facing_read_write_policy_json`+`consume_policy_json` da fila nova — GSI9 é tenant-facing, sem policy dedicada), `aws_lambda_event_source_mapping` novo; `dispatch_outbox_relay`/`outbox_sweeper` ganham a URL da fila nova em `environment_variables`+`policy_documents_json` (`send_policy_json`). G-V3 adversarial: hint com `versionId` correto mas `Requirement` com cache ANTIGO de `evidenceValidUntil` (mais restrito que a verdade fresca) converge para o valor fresco, nunca o cache; `DocumentVersion` re-lida como `REJECTED` (não só `validUntil` mudando) converge para `PENDING` via releitura, nunca o valor do evento; 3 Requirements da mesma evidência, um falha OCC uma vez e se recupera sozinho no retry (não conta como falha), outro falha terminalmente (esgota as 5 tentativas) sem bloquear os demais; no-op não dispara nenhum `transactWrite`; Requirement desvinculado entre a descoberta (stale) e a releitura fresca é no-op seguro, nunca reescreve. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 6/9 — único ponto real de engenharia fora do design: os dois payloads de evento da slice 4 não carregavam `versionId`, extensão aditiva necessária para a descoberta via GSI9 funcionar, decidida e documentada aqui) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-boundaries/check-docs/validate-schemas/build:lambdas limpos, suíte **2112/2112** — 7 testes novos —, `terraform test` **22/22** `AWS_PROFILE=claude-dev`). D-193 item 6/9 do design FECHADO — mecanismo de convergência assíncrona `Requirement` ponta a ponta (outbox condicional da slice 4 → GSI9 da slice 5 → fila+worker desta fatia). Próxima fatia: item 7/9 — rede de reparo autoritativa: `scanRequirementsWithEvidence` em `DocumentArchiveStore` (mesmo precedente de `scanActiveSeries`, filtrado por `attribute_exists(evidenceVersionId)`, independente de status cacheado) + worker diário agendado `requirement-evidence-daily-sweep` (EventBridge Scheduler, mesmo padrão de `document_file_reconciliation_handler`) que só REENFILEIRA candidatos na MESMA fila desta fatia, nunca escreve `Requirement` diretamente — fecha o teto de staleness em 24h mesmo sob perda total do evento/outbox (mecanismo distinto: esta fatia cobre ordem/duplicação via releitura, a próxima cobre PERDA total da mensagem). | `src/workers/requirement-evidence-refresh/refresh.ts`, `src/runtime/aws/handlers/requirement-evidence-refresh-handler.ts`, `src/runtime/aws/{composition/reminder.ts,handlers/{dispatch-outbox-relay-handler.ts,outbox-sweeper-handler.ts}}`, `src/modules/extraction/{ports/extracted-field-store.ts,persistence/dynamodb-extracted-field-store.ts,application/{confirm-reject-field-document-archive.ts,run-extraction-validation.ts}}`, `infra/main.tf`, `scripts/build-lambdas.ts`, testes em `test/unit/{workers/requirement-evidence-refresh,extraction/run-extraction-validation}.test.ts` | 2026-09-03 |
222:| D-193 | Item 7/9 do design — rede de reparo autoritativa (Rodada 5, `estado-final-consolidado.md`): fecha o risco de perda TOTAL da mensagem `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` que a slice 6 deixou aberto (aquela fatia cobre ordem/duplicação via releitura, nunca perda do evento em si). Novo método `scanRequirementsWithEvidence` em `DocumentArchiveStore` (porta `document-archive-store.ts` + adapter `dynamodb-document-archive-store.ts` + fake em `test/unit/document-archive/in-memory-store.ts`) — `Scan` cross-tenant filtrado a `entityType="Requirement" AND attribute_exists(evidenceVersionId)`, MESMO precedente/tradeoff de custo de `scanActiveSeries` (D-147, sem método de enumeração de tenant no módulo). DELIBERADAMENTE independente de `status` — o design exige isso explicitamente porque o próprio objetivo é achar Requirements cujo `status` cacheado pode estar ERRADO por causa de uma mensagem perdida; filtrar por status excluiria exatamente as candidatas certas. Novo worker puro `src/workers/requirement-evidence-daily-sweep/sweep.ts` (`runRequirementEvidenceDailySweep`) pagina esse scan (`MAX_PAGES=25`, mesmo cap de `document-request-recurrence/materializer.ts`) e, para CADA `Requirement` com `evidenceVersionId` presente, chama um `enqueueRefresh` injetado com `{tenantId, versionId}` — nunca escreve `Requirement` diretamente (nenhum `store.transactWrite`/`get` de `DocumentVersion` neste worker), nunca deriva status, colapsando o caminho de escrita real a um único lugar (o `refresh.ts` da slice 6). Deliberadamente NÃO idempotency-sensitive — `refreshRequirementsForEvidenceVersion` já é um no-op seguro quando a releitura fresca não muda nada (G-V3 da slice 6), então reenfileirar um candidato já convergido custa uma mensagem extra, nunca uma escrita errada. Novo handler `requirement-evidence-daily-sweep-handler.ts` (EventBridge Scheduler, mesmo contrato "`input` top-level, nunca `event.detail`" de `requirement-reindex-handler.ts`/`document-request-recurrence-handler.ts`) manda via `SQSClient`/`SendMessageCommand` para a MESMA fila `requirement_evidence_refresh_queue` da slice 6 — sem fila/DLQ nova. Terraform: novo Lambda `requirement-evidence-daily-sweep-handler` + `aws_scheduler_schedule` (`cron(0 5 * * ? *)`, UTC, staggered depois do `requirement_reindex` às 04:00) + IAM role de schedule dedicada; **achado real durante a fatia**: o sufixo padrão `-schedule-role` (usado por todo o resto do arquivo) estourava o limite de 64 caracteres de nome de IAM role para este nome de função em particular (confirmado por falha real do `terraform test`, não hipotética) — corrigido com o sufixo mais curto `-sched-role`, só para este recurso, documentado inline. Policy do novo Lambda é SÓ `tenant_facing_read_write_policy_json` (cobre o `Scan` base-table, mesma cobertura de `document_request_recurrence_handler`) + `requirement_evidence_refresh_queue.send_policy_json` — deliberadamente SEM `worker_transact_write_policy_json`/`gsi8_read_policy_json` (este handler nunca escreve `Requirement`, ao contrário de `document_file_reconciliation_handler`/`requirement_reindex_handler`). Novo `run` em `stack.tftest.hcl` prova o cron/placeholder E que a capability list do novo Lambda nunca contém `dynamodb:TransactWriteItems` (prova negativa do single-writer constraint). G-V3: acha `Requirement` com evidência independente do `status` cacheado (SATISFIED/PENDING/NOT_SATISFIED testados); pula `Requirement` sem `evidenceVersionId` (sparseness genuína provada via `"evidenceVersionId" in unlinked === false`, nunca um valor falsy); pagina corretamente através de `lastEvaluatedKey` (fake isolado de 3 páginas provando que cada página seguinte recebe o `exclusiveStartKey` exato da página anterior); cenário de perda TOTAL de mensagem (Requirement com `evidenceValidUntil` cacheado muito no passado, nenhum evento jamais chegou) é reenfileirado com o par `{tenantId, versionId}` correto, provando que a convergência aconteceria assim que o `refresh.ts` da slice 6 processar essa mensagem desta vez. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 7/9 — único ponto real de engenharia fora do design: o nome de IAM role estourando 64 chars, achado e corrigido no caminho) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-docs/validate-schemas/build:lambdas limpos, suíte **2123/2123** — 6 testes novos —, `terraform test` **23/23** `AWS_PROFILE=claude-dev`). D-193 item 7/9 do design FECHADO — teto de staleness de até 24h fechado mesmo sob perda TOTAL do evento/outbox (slice 6 cobre ordem/duplicação via releitura; esta fatia cobre perda total da mensagem). Próxima fatia: item 8/9 — dois feature flags em ORDEM obrigatória: `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED` (starter, PRIMEIRO) antes de `DOCUMENT_ARCHIVE_PROMOTION_ENABLED` (promoter, DEPOIS) — nunca a ordem inversa, fecha por construção a janela "CLEAN sem consumidor" descrita no design. Item 9/9 (rename mecânico `document-classifier.ts`→`document-format-classifier.ts`, nível 1-2) segue intocado, pode ser feito a qualquer momento sem protocolo. | `src/modules/document-archive/{ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `src/workers/requirement-evidence-daily-sweep/sweep.ts`, `src/runtime/aws/handlers/requirement-evidence-daily-sweep-handler.ts`, `infra/{main.tf,tests/stack.tftest.hcl}`, `scripts/build-lambdas.ts`, testes em `test/unit/{document-archive/in-memory-store.ts,workers/requirement-evidence-daily-sweep.test.ts}` | 2026-09-03 |
223:| D-193 | Item 8/9 do design ("Sequenciamento") — dois feature flags AppConfig em ordem obrigatoria, fechando por construcao a janela "CLEAN sem consumidor": `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED` (STARTER, novos em `FeatureFlags`/`AppConfigFeatureFlagsReader`, mesmo `=== true` fail-closed parsing de AI_EXTRACTION/OCR/WHATSAPP) e `DOCUMENT_ARCHIVE_PROMOTION_ENABLED` (PROMOTER), ambos default `false`. STARTER gates `startExtractionRunForDocumentArchive()` (nova precondicao 0, checada ANTES de qualquer leitura DynamoDB, novo motivo de refuso `STARTER_DISABLED`); PROMOTER gates o terceiro branch (`document-archive`) de `upload-finalizer-handler.ts`/`malware-result-handler.ts` (`finalizeDocumentArchiveUpload()`/`processDocumentArchiveMalwareResult()`, novo outcome `IGNORED_PROMOTION_DISABLED`, checado ANTES de qualquer acesso a store/S3 — enquanto OFF o comportamento e byte-identico ao de antes da slice 1: chave reconhecida mas descartada, sem retry). Novo `src/modules/extraction/application/document-archive-activation.ts` — `isDocumentArchivePromotionEnabled()` exige AMBOS os flags (nunca so PROMOTER), o mecanismo real de ordem-por-construcao: ligar PROMOTER sem STARTER (a ordem proibida) nunca promove um arquivo a CLEAN, entao a janela perigosa nunca existe em nenhum caminho de codigo, nao so por disciplina de runbook. **Achado de fronteira real durante a fatia**: a primeira versao do arquivo vivia em `extraction/domain/` mas importava o tipo `FeatureFlags` de `extraction/ports/` — violava `domain-must-not-reach-application-layers` (pego por `test/architecture/tenant-fence-boundary.test.ts` na primeira rodada de testes), movido para `extraction/application/` (mesma classe de correcao de fronteira que a slice do D-179 para `transient-purge` ja tinha achado para `shared-must-not-reach-modules`). Terraform: duas novas `variable` (default `false`) em `infra/modules/feature-flags`, plumbadas no `jsonencode` do AppConfig e nos env vars/IAM (`feature_flags_read_policy_json`) dos 3 Lambdas afetados (`upload_finalizer_handler`, `malware_result_handler`, `extraction_starter_handler`). G-V3: com ambos OFF (default) prova o mecanismo COMPLETAMENTE inerte para documentos `document-archive` — nenhum acesso a store/S3, nao so um refuso no passo final (`DocumentFile`/`ExtractionRun` nunca tocados); com SO PROMOTER ON (ordem proibida deliberada) prova que o gate de ordem bloqueia a ativacao mesmo assim; com ambos ON (ordem correta) prova o pipeline real slices 1->3 rodando ponta a ponta contra o MESMO `InMemoryDocumentArchiveStore` compartilhado entre as duas funcoes (`test/unit/extraction/document-archive-pipeline-activation.test.ts`, novo — nao e so 2 testes isolados do gate, e uma prova de integracao real do mecanismo das slices 1-7 agora gateado/ativado). Suite **2151/2151** (195 arquivos), `typecheck`/`lint`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` **23/23** (`AWS_PROFILE=claude-dev`). | Nivel 3-4 (implementacao direta de design ja `APPROVED`, D-193 slice 8/9 — unico ponto real de engenharia fora do design: o achado de fronteira domain-vs-application do novo arquivo, achado e corrigido no caminho) | — | — | **IMPLEMENTADO** (gate local completo, ver detalhe acima). D-193 item 8/9 do design FECHADO — restante apenas item 9/9 (rename mecanico `document-classifier.ts`->`document-format-classifier.ts`, nivel 1-2, sem protocolo), depois do qual D-193 fica 9/9 COMPLETO e pronto para o PR-final-para-`main`+verificacao-ao-vivo (espelhando o fechamento de D-192). Commit `51fd70b` em `develop` (push direto, sem PR — `AGENTS.md` §3). | `src/modules/extraction/{ports/feature-flags-reader.ts,persistence/appconfig-feature-flags-reader.ts,application/{document-archive-activation.ts,start-extraction-run-for-document-archive.ts}}`, `src/workers/{upload-finalizer/document-archive-finalizer.ts,malware-result/document-archive-result-processor.ts}`, `src/runtime/aws/{composition/{document-archive.ts,extraction.ts},handlers/{upload-finalizer-handler.ts,malware-result-handler.ts,extraction-starter-handler.ts}}`, `infra/{main.tf,modules/feature-flags/{main.tf,variables.tf,outputs.tf}}`, testes em `test/unit/{extraction/{document-archive-activation.test.ts,document-archive-pipeline-activation.test.ts,start-extraction-run-for-document-archive.test.ts,appconfig-feature-flags-reader.test.ts},document/{document-archive-finalizer.test.ts,document-archive-result-processor.test.ts}}` | 2026-09-03 |
224:| D-193 | **FECHAMENTO POR COMPLETO — item 9/9 (rename mecânico) + PR final para `main` + verificação ao vivo, espelhando o fechamento de D-192.** Rename mecânico nível 1-2, sem protocolo: `extraction/domain/document-classifier.ts`→`document-format-classifier.ts` (+ teste), 2 call sites (`start-ocr.ts`, comentário em `start-extraction-run-for-document-archive.ts`) atualizados — G-V3: suíte relocada passa sem modificação de asserção. Gate local completo antes do PR: `typecheck`/`lint` limpos, suíte **2136/2136** (195 arquivos), `check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` **23/23** (`AWS_PROFILE=claude-dev`). Commit `849e8a6` em `develop`. PR #223 (`develop`→`main`, 13 commits acumulados desde `27c03fc`, todas as 9 fatias de D-193) aberto, CI verde (guardrails/dynamodb-integration/frontend/Validate Infra em ambos os workflows concorrentes), mergeado (`20b7bd8`). CD (`33787691342`) rodou até `completed`/`success` — polling síncrono direto do `databaseId`, nunca `gh run watch` em background (disciplina reforçada depois do quase-erro de D-192). **Verificação ao vivo contra `dev` — os 6 pontos, todos confirmados, nada quebrado**: (1) `aws lambda get-function` — `upload-finalizer-handler`/`malware-result-handler`/`extraction-starter-handler`/`requirement-evidence-refresh-handler`/`requirement-evidence-daily-sweep-handler` todos `Active`/`Successful`, `LastModified` 18:09-18:12 UTC (bate com este CD); (2) `aws sqs list-queues`/`get-queue-attributes` — `exptrk-dev-requirement-evidence-refresh`+`-dlq` existem, `RedrivePolicy` aponta a DLQ com `maxReceiveCount=5`; (3) `aws scheduler get-schedule --name exptrk-dev-requirement-evidence-daily-sweep` — `ENABLED`, `cron(0 5 * * ? *)` (depois do reindex 04:00, como desenhado); (4) AppConfig (`aws appconfig list-applications`+`aws appconfigdata start-configuration-session`/`get-latest-configuration`, sem precedente de comando exato em sessões anteriores — sessão construiu o par start-session/get-latest a partir do `infra/modules/feature-flags/main.tf`, já que é a unica forma de ler HostedConfiguration por API) — os dois flags novos presentes no JSON servido, `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED=false`/`DOCUMENT_ARCHIVE_PROMOTION_ENABLED=false` (default OFF confirmado ao vivo, não presumido do Terraform); (5) `aws dynamodb describe-table --table-name exptrk-dev-table` — GSI9 `IndexStatus=ACTIVE`, chaves `GSI9PK`/`GSI9SK`, projeção `ALL`. **Decisão sobre ativar os flags agora**: mantidos OFF. Razão: nenhuma fatia deste fechamento incluiu um teste end-to-end contra `dev` real (upload real → scan real → promoção real → Textract real) — toda a prova de G-V3 é `InMemoryDocumentArchiveStore`/mocks; ligar os dois flags em produção-`dev` agora seria o PRIMEIRO tráfego real passando pelo mecanismo inteiro sem uma sessão dedicada observando o resultado (custo Textract real por chamada, também). `AGENTS.md`'s "sem usuários reais" remove a urgência de qualquer lado (não há pressão para ligar, nem risco de dano a usuário em deixar desligado), então a escolha aqui foi conservadora por preferência, não por necessidade — reversível a qualquer momento (`AWS_PROFILE=claude-dev` + `terraform apply` num `.tfvars` com os 2 booleans `true`, ou direto via `aws appconfig start-deployment` numa nova hosted configuration version), registrado como pendente pequeno e nomeado, não bloqueante. **Resumo dos achados/bugs reais fechados ao longo das 9 fatias**: upload `document-archive` ficava preso em `PENDING_UPLOAD` para sempre (slice 1, nenhum consumidor físico existia); nenhum payload de evento de confirmação carregava `versionId`, impedindo a descoberta via GSI9 (slice 6, campo aditivo novo); nome-base do IAM role do scheduler estourava o limite de 64 chars (slice 7, sufixo `-sched-role` em vez de `-schedule-role`); violação de fronteira `domain-must-not-reach-application-layers` no primeiro rascunho do arquivo de ativação de flags (slice 8, movido de `domain/` para `application/`); e o próprio enquadramento da Fase 1 do design corrigido antes de qualquer código (o "classificador" era de formato de arquivo, não de tipo de documento de negócio — motivo direto deste rename final). | Nível 1-2 (rename mecânico) + fechamento de PR/merge/verificação (sem decisão de engenharia nova) | — | — | **D-193 FECHADO POR COMPLETO** (9/9 itens do design implementados, PR #223 mergeado em `main`, CD `success`, verificação ao vivo confirmada nos 6 pontos exigidos, `develop`/`main` sincronizados). Mecanismo existe em `dev` mas fica INERTE (ambos os flags OFF) até uma decisão futura deliberada de ativação. | `src/modules/extraction/domain/document-format-classifier.ts`, `test/unit/extraction/document-format-classifier.test.ts`, `src/modules/extraction/application/{start-ocr.ts,start-extraction-run-for-document-archive.ts}` | 2026-09-03 |
228:| D-197 | **WhatsApp Operacional (Roadmap P0.3) — DESIGN, protocolo Claude↔Codex completo, 3 rodadas** (`docs/architecture/reviews/whatsapp-channel-scoping/`). **Marcelo decidiu o fornecedor nesta sessão: Meta Cloud API direta, não BSP** — desbloqueia o item; tudo que decorre dessa escolha passou pelo protocolo. **Declaração E-014: SIM** (fontes primárias Meta for Developers — Messaging Limits/Webhooks getting-started/Get opt-in, todas consultadas 2026-09-04, mais 1 fonte de prática de mercado para `biz_opaque_callback_data`/pricing 2026, com limitação de fonte registrada explicitamente onde a Meta não tinha página oficial citável). **Régua contestada na Rodada 1** (Codex achou o checklist da Rodada 1 incompleto — faltavam webhook inbox/replay como critério obrigatório, contrato SQS por canal do ADR-0008, quota por destinatário único vs. consumo agregado genérico, opt-in vinculado ao telefone) — reconciliada em v2 (Rodada 2, régua 8,7/10, ainda 2 pontos de ambiguidade) e fechada em v3 (Rodada 3, régua 9,3/10, estável). **3 achados reais do Codex, todos verificados por leitura de código, fecharam a Rodada 3**: (1) `TenantQuotaService` (fixed-window, por tenant, conta chamadas) não serve para o limite real da Meta (destinatário único, janela móvel, cross-tenant/portfólio) — mecanismo novo: item de tabela BASE (não GSI) `PK=WHATSAPP#PORTFOLIO`/`SK=SENT#<timestamp>#<telefone>` (tempo primeiro na SK, torna a Query por range válida — a v2 tinha a ordem invertida, achado real do Codex), IAM dedicada por `LeadingKeys`, fail-closed, hot-partition nomeado com gatilho de sharding; (2) `WebhookInbox` precisa de um caminho tenantless — achado incidental real: `ses-callback-workflow.ts` (precedente já em produção) na verdade retorna `UNMATCHED` ANTES de gravar o inbox quando as tags faltam, contradizendo o próprio cenário 15 de `architecture-fase3-consolidada.md` — corrigido para WhatsApp usando a cláusula "tenant/ACCOUNT" já prevista naquele cenário (`wabaId`, disponível antes de qualquer correlação); (3) correlação de webhook via `biz_opaque_callback_data` (mesmo papel das tags de SES) confirmada por pesquisa dedicada, formalizada como gate de implementação (confirmar contra sandbox real antes do primeiro envio de produção). **10 decisões finais** (D-1 a D-10, detalhe completo em `estado-final-consolidado.md`): widening de tipo sem migração; `WhatsAppProviderAdapter` espelhando `EmailProviderAdapter`; templates pré-provisionados (Meta Business Manager, nunca criados via API); categoria `Utility` sempre (produto é business-initiated); `WhatsAppOptIn` vinculado ao telefone exato; `GlobalUser.phoneE164` novo; `WebhookInbox` account-scoped; quota de destinatário único (acima); fila/schema `SQS_NOTIFICATION_WHATSAPP_V1` dedicados (ADR-0008); secrets via Secrets Manager (5 valores, incluindo `verify_token`) + kill switch de 2 flags em ordem (mesmo mecanismo de D-193 slice 8). ADR formal (nível 6 exige): `ADR-0012-whatsapp-cloud-api-channel.md`. DESIGN-ONLY — nenhum código/schema/infra alterado. | Type 1 (nível 6 — novo terceiro com acesso a PII) | 9,2/10 (Rodada 3, autoavaliação às cegas) | 9,1/10 (Rodada 3, final, régua 9,3/10 estável) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194.** Próximo: fatia 1 (widening de tipo + entidades novas, sem HTTP exposto) — ver `estado-final-consolidado.md` para o programa completo de 5 fatias. | `docs/architecture/reviews/whatsapp-channel-scoping/`, `docs/architecture/adr/ADR-0012-whatsapp-cloud-api-channel.md` | 2026-09-04 |
230:| D-199 | **BUG CRÍTICO REAL encontrado e corrigido ao escopar o item 13 do backlog P1 (watcher fan-out): todo e-mail de lembrete real, desde M4 (2026-08-XX), sempre falhou silenciosamente antes de chamar a SES.** Achado por leitura direta do código, não presumido: `NotificationRouterWorkflow.applyRoutedDecision()` (`notification-router-workflow.ts`) computa o `resolved.userId` correto via `NotificationRecipientResolver` e o usa para DECIDIR o roteamento (`decideRouting()`), mas **nunca grava esse valor de volta no campo `NotificationIntent.recipientUserId`** ao marcar o intent `DISPATCHED` — o `set:` do `Update` só gravava `status`/`routedChannels`/`cancelledChannels`/`routedAt`. `email-delivery-workflow.ts:127-128` (`const recipientUserId = intent?.recipientUserId; const to = recipientUserId ? await resolveRecipientEmail(...) : undefined`) relê o intent FRESCO do banco (não reaproveita nenhum valor em memória do router) — como o campo nunca foi persistido, `recipientUserId` é sempre `undefined` na entrega real, `to` é sempre `undefined`, e todo envio real cai no ramo `if (!to || !item)`, terminando `FAILED_TERMINAL` **antes de qualquer chamada real à SES**. O design original (`m4-notification-engine-design/codex-proposal-round1.md` linha 141: "O router persiste apenas `recipientUserId`") já previa isso corretamente — é uma lacuna de implementação de um design já aprovado, não uma decisão nova, nunca detectada porque os 2 testes unitários que leem esse campo (`email-delivery-workflow.test.ts`/`ses-callback-workflow.test.ts`) constroem a fixture de `NotificationIntent` já COM `recipientUserId` fixado manualmente, contornando por completo este caminho de escrita real. Mesma classe de achado que D-114→D-120 (Wave B2B-14) e D-193 já encontraram várias vezes — só exercitar o fluxo real revela a lacuna, teste unitário com fixture conveniente não. **Efeito colateral também corrigido de graça**: `ses-callback-workflow.ts:136-137` (supressão de bounce/complaint por `recipientUserId`) também nunca funcionava por causa do mesmo campo ausente. **Fix mecânico, cirúrgico**: `resolved?.userId` (já computado em `routeNotificationIntent`) agora é threadeado por parâmetro através de `applyDecision`→`applyRoutedDecision` e gravado em `recipientUserId` no mesmo `Update` que já marca `DISPATCHED` — nenhuma mudança de contrato público, nenhuma migração (linhas antigas continuam sem o campo, mas nunca mais serão reprocessadas por `routeNotificationIntent`, que só roteia intents `PENDING`). G-V3: novo assert em `notification-router-workflow.test.ts`'s teste "happy path" prova `updatedIntent.recipientUserId === ASSIGNEE` (falha sem o fix). Gate local completo verde: `typecheck`/`lint`/suíte completa **2247/2247**/`check-docs`/`check-boundaries`. **Impacto real estimado**: qualquer tenant `dev` que tenha esperado receber um e-mail de lembrete via o pipeline real (não via smoke test manual/hand-crafted) nunca o recebeu — nenhum usuário real hoje (`AGENTS.md` §1), mas este é o mecanismo central de valor do produto («me avise antes do documento vencer»), então prioridade máxima sobre qualquer item do roadmap P1. | Nível 1-2 (correção mecânica, restaura comportamento já `APPROVED` desde M4, sem decisão nova) | — | — | **CORRIGIDO, PR #231 mergeado (`29eacd4`), CD `success`, confirmado ao vivo** (`aws lambda get-function --profile claude-dev --region us-east-1`: `exptrk-dev-notification-router` `Active`/`Successful`, `LastModified` bate com o deploy) | `src/modules/notification/application/notification-router-workflow.ts`, `test/unit/notification/notification-router-workflow.test.ts` | 2026-09-05 |
235:| D-204 | **Roadmap P1 item 15 ("Relatórios agendados") — DESIGN, protocolo Claude↔Codex completo, 3 rodadas** (`docs/architecture/reviews/scheduled-reports-scoping/`). Fecha o próximo item do backlog P1 depois do bloqueio do item 14 (D-202). **Declaração E-014: SIM PARCIAL** (Metabase "Dashboard subscriptions" e Google Cloud/Looker "Scheduling and sending dashboards"/"Emailed data policy", ambas fontes primárias datadas 2026-09-05 — padrão convergente: assinatura por criador/relatório/destinatário específico, não agendamento único fixo por tenant; "anexo vs. link" é escolha deliberada de produto oferecida pelos dois líderes de mercado; nenhuma fonte primária equivalente encontrada no nicho específico de compliance/expiration-tracking, limitação registrada). **6 achados reais do Codex, todos verificados por leitura de código, fecharam as 3 rodadas**: (1) TTL de 30 dias em presigned S3 é fisicamente inválido quando assinado por credenciais de role Lambda (expira com as credenciais, teto ~7 dias do SigV4) — fechado substituindo o link por uma rota de download AUTENTICADA que gera presign curto (5 min) sob demanda, nunca embutindo um link S3 direto no e-mail; (2) GSI10 novo era desnecessário — GSI8 (`local.gsi8_worker_types`, mapa fechado de 9 entradas, `infra/modules/dynamo-table/main.tf:248`) já é o índice esparso certo, `report_subscription="REPORT_SUBSCRIPTION"` é aditivo mecânico; (3) `lastRunAt+cadence` sozinho é fraco para idempotência/concorrência — fechado reusando outbox→SQS→worker já `APPROVED` (D-193/D-200/D-201) com `ReportDeliveryAttempt` espelhando a máquina de estados de `NotificationAttempt` (claim `SUBMITTING` com lease ANTES de qualquer chamada externa); (4) `ReportsService` exige `RequestContext`/`authorize()`, incompatível com um worker agendado sem ator autenticado — fechado com variante system-facing, mesmo posto de `findRequirementsByEvidenceVersion` (`document-archive-service.ts:1300-1306`); (5) destinatários externos arbitrários são bloqueio real de produto/segurança — fechado restringindo a `Membership ACTIVE`+`GlobalUser ACTIVE`, revalidados FRESCOS no envio (nunca confiados da criação); (6) conflito de RBAC entre a rota de download (ADMIN-tier) e destinatários MEMBER/VIEWER elegíveis — fechado autorizando por `ADMIN_ROLES` OU o próprio `principal.userId` elegível no `ReportDeliveryAttempt`/run específico. **10 decisões finais** (detalhe completo em `estado-final-consolidado.md`): `ReportSubscription` (cadence `WEEKLY`+`dayOfWeek`/`localTime`/`timeZone`, `recipientUserIds` teto nomeado, RBAC `ADMIN_ROLES`); GSI8 worker type novo; scheduler semanal único cross-tenant; claim transacional 2 ações (`Update`+`Outbox` Put, destino novo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`); `ReportSubscriptionRun`(1/execução)+`ReportDeliveryAttempt`(1/destinatário, isolamento de falha); worker de entrega system-facing; rota de download autenticada com presign efêmero; retry/DLQ nativo do SQS; truncamento sinalizado no e-mail; RBAC de gestão `ADMIN_ROLES`. **Fora de escopo, nomeado**: cadência MONTHLY; anexo MIME (`Content.Raw`, deliberadamente descartado a favor do link autenticado); destinatários externos arbitrários. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (novo mecanismo de entrega assíncrona + nova rota autenticada + novo GSI8 worker type) | 9,2/10 (Rodada 3, autoavaliação às cegas) | 9,1/10 (Rodada 3, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197.** Fatias sugeridas (não decididas): (1) entidades+GSI8 worker type+schema; (2) scheduler+claim transacional; (3) worker de entrega+rota de download; (4) wiring completo do destino de outbox. | `docs/architecture/reviews/scheduled-reports-scoping/` | 2026-09-05 |
236:| D-205 | **Roadmap P1 item 16 ("Dossiê documental PDF/Excel") — DESIGN, protocolo Claude↔Codex completo, 4 rodadas** (`docs/architecture/reviews/document-dossier-scoping/`). Fecha o próximo item do backlog P1 depois de D-204 (item 15). **Declaração E-014: SIM PARCIAL** (Drata "Pre-Audit package" e Vanta "evidence list/policy packet", fontes primárias datadas 2026-09-05 — nenhum dos dois mescla bytes de documento num PDF único, mas oferecem pacotes/ZIP de evidência, correção real da Rodada 1 que superestimava "nunca bytes"). **5 achados reais do Codex, todos verificados por leitura de código, fecharam as 4 rodadas**: (1) RBAC "ADMIN_ROLES ou assignee" não funcionaria — `authorization.ts:323` só aplica o gate de assignee quando `ownerUserId` E `assigneeUserId` existem juntos, `Requirement` não tem `ownerUserId` — fechado com **ADMIN_ROLES exclusivamente**, removendo disclosure real (assignee de 1 Requirement exportando o Subject inteiro); (2) histórico de relink de evidência não é reconstruível (`linkEvidence`/`unlinkEvidence` só sobrescrevem ponteiros atuais, sem evento próprio) — fechado como limitação explícita nomeada do v1; (3) PDF+XLSX "mesmo conteúdo" conflitava com truncamento visual — fechado declarando papéis distintos (XLSX exaustivo, PDF narrativo, nunca omite linha); (4) `POST` disparar geração imediatamente não cumpria a Journey J22 ("mostra escopo antes de exportar") — fechado com fluxo preview (`PREVIEW_READY`, sem worker)/confirm (`scopeHash` obrigatório, idempotente, 409 se divergir); (5) semântica "as of" ambígua — fechado declarando conjunto de linhas congelado no preview, valores relidos frescos na geração (`generatedAt` explícito no documento). **11 decisões finais** (detalhe completo em `estado-final-consolidado.md`): manifesto metadata-first v1 (bytes de documento explicitamente fora de escopo); `DossierExportRun` com preview/confirm/`scopeHash` (fingerprint inspirado no padrão de D-194); releitura fresca de valores na geração (convenção de D-193); sem truncamento silencioso (`DossierTooLargeError` declarado); XLSX exaustivo/PDF narrativo; sanitização Excel estendendo o precedente de `csv-export-writer.ts` (D-195); PDF sem lib de layout nova (`pdf-lib` builder, tabela determinística); download autenticado com presign sob demanda (mesmo padrão de D-204); RBAC `ADMIN_ROLES` exclusivo. **Fora de escopo, nomeado**: bundle evidenciário completo (bytes embutidos); pacote ZIP único (v1 gera 2 downloads separados); log evento-a-evento de decisões. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (nova capacidade de geração de documento + novo fluxo preview/confirm + nova rota autenticada) | 9,3/10 (Rodada 4, autoavaliação às cegas) | 9,2/10 (Rodada 4, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197/D-204.** Fatias sugeridas (não decididas): (1) `DossierExportRun`+rotas preview/confirm+`scopeHash`; (2) worker de geração (PDF+XLSX)+S3+`DossierTooLargeError`; (3) rota de download autenticada com presign sob demanda. | `docs/architecture/reviews/document-dossier-scoping/` | 2026-09-05 |
237:| D-206 | **Roadmap P1 item 17 ("Bulk actions") — DESIGN, protocolo Claude↔Codex completo, 4 rodadas** (`docs/architecture/reviews/bulk-actions-scoping/`). Fecha o próximo item do backlog P1 depois de D-205 (item 16). Bulk archive foi anteriormente classificado como "product creep" por falta de base em roadmap (`docs/frontend/interface-context-and-critical-tasks.md` §30) — condição que não vale mais, roadmap agora nomeia o item formalmente. **Declaração E-014: SIM PARCIAL** (Atlassian Jira bulk operations — job assíncrono/`taskId`/polling até 1000 itens — e Asana multi-select síncrono, fontes primárias 2026-09-05; padrão de mercado é split síncrono-para-pequeno/assíncrono-para-grande, este design fica no lado síncrono por proporcionalidade de escala, teto 100 não 1000). **6 achados reais do Codex, todos verificados por leitura de código, fecharam as 4 rodadas**: (1) bulk archive é mais destrutivo (`ItemDeactivated`+cancelamento de reminders, `expiration-service.ts:795`) — fechado com confirmação UX explícita obrigatória; (2) faltava contrato `{itemId, expectedVersion}` por item — sem ele perderia a garantia OCC — fechado, obrigatório; (3) teto 100 síncrono sobre o timeout real de 10s de `items_handler` (`lambda-function/variables.tf:43`) — fechado com Lambda DEDICADA (mesmo precedente de `export-handler`, D-123/D-126), `timeout_seconds=25`, dentro da cota real de 30s do HTTP API v2 (`api-gateway/main.tf:6`, correção de "REST 29s"); (4) justificativa "export processa 2000 itens" era analogia fraca (leitura vs. transação) — fechado reclassificando o teto como hipótese de engenharia condicionada a smoke test antes de produção; (5) ambiguidade de retry pós-timeout (itens já aplicados voltariam como `VERSION_CONFLICT`) — fechado com regra de reconciliação restrita (`TARGET_ALREADY_APPLIED` só quando version avançou exatamente 1 E o estado bate EXATAMENTE com o alvo, nunca afirmando autoria da tentativa); (6) `archiveItem()` atual não bloqueia rearquivar item já `ARCHIVED` com versão válida — fechado com precondição `status === "ACTIVE"` no bulk archive (`INELIGIBLE_STATE` se não elegível), comportamento single-item existente intocado. **9 decisões finais** (detalhe completo em `estado-final-consolidado.md`): lista v1 restrita a bulk reassign+archive; confirmação UX+precondição ACTIVE-only para archive; contrato por item com `expectedVersion`; fan-out via transações pequenas independentes (padrão de `import-commit-service.ts`, D-192); reconciliação de retry sem ledger; resposta síncrona com taxonomia completa de outcome; Lambda dedicada 25s com teto 100 condicionado a validação empírica; RBAC `WRITE_ROLES` sem elevação artificial. **Fora de escopo, nomeado**: bulk delete/renew/Requirement/Document; job assíncrono/ledger (só se autoria de tentativa virar requisito); teto acima de 100. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (novo mecanismo de fan-out transacional + nova Lambda dedicada + nova rota HTTP) | 9,1/10 (Rodada 4, autoavaliação às cegas) | 9,2/10 (Rodada 4, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197/D-204/D-205.** Fatias sugeridas (não decididas): (1) Lambda dedicada+rotas HTTP+RBAC; (2) fan-out+reconciliação+taxonomia de outcome; (3) smoke test de integração medindo custo real de 100 itens antes de expor em produção. | `docs/architecture/reviews/bulk-actions-scoping/` | 2026-09-05 |
240:| D-209 | **D-206 fatia 1/3 (Lambda dedicada+rotas HTTP+RBAC) — IMPLEMENTADA. Item 17 do backlog P1 agora reachable via HTTP, fecha D-206 por completo (fatias 1+2, só falta a 3 — smoke test de custo real).** Novo Lambda `bulk-actions-handler` (`timeout_seconds=25`, mesmo padrão de `export-handler`/D-123-D-126, `policy_documents_json=[tenant_facing_read_write_policy_json, gsi4_read_policy_json]` — a segunda por resolver `RequestContext` via `resolver.resolve()`, mesma regra de D-116). 2 rotas novas (`POST /items/bulk-reassign`, `POST /items/bulk-archive`) numa única integração compartilhada (padrão `reports_handler`'s "N rotas : 1 integração"), wireadas em `infra/modules/api-gateway/main.tf` (`variables.tf`+`main.tf` do módulo, `infra/main.tf`'s bloco `module.api`) e no BFF (`proxy-allowlist.ts` — mesma disciplina D-117/D-120/D-178, já que bulk actions é JSON puro, nunca esbarra no gap de `content-disposition` que mantém `/items/export`/`/reports/*` fora do BFF). 2 schemas novos (`bulk-reassign-items-request.v1.json`/`bulk-archive-items-request.v1.json`, registrados em `schema-validator.ts`) — `assigneeUserId` vazio limpa o responsável (mesma convenção de `UpdateItemRequest`), `confirm` do archive usa `const: true` (rejeita `false` e ausência, nunca infere confirmação). Novo módulo HTTP `bulk-action-handlers.ts` reusa os helpers genéricos de `item-handlers.ts` (`withErrorMapping`/`validateAgainstSchema`/`consumeApiRequestQuota`, promovidos de privados para exportados) em vez de duplicá-los — diferente de `export-handler.ts`, que precisou de um pipeline próprio só por causa do corpo CSV; bulk actions é JSON puro, então reusa o pipeline compartilhado. **Achados reais durante a implementação (nenhum exigiu rodada nova de protocolo — implementação direta de design já `APPROVED`)**: (1) `infra/modules/api-gateway/tests/api_gateway.tftest.hcl` estava com um `run` block genuinamente quebrado — faltavam `reports_invoke_arn`/`document_archive_invoke_arn`/`document_archive_guest_invoke_arn` (nunca atualizado desde D-143/D-195), preenchido com valores mock para o teste voltar a rodar (não corrigido: as contagens de rota pré-existentes `/items* == 10` vs. real 13, `/subjects* == 21` vs. 22, `/imports* == 3` vs. 5 continuam quebradas, mesma classe de drift que D-208 achou para o output `lambda_function_names`, 32 vs. 52 — nomeado, não corrigido, dívida técnica separada); confirmado que NENHUMA dessas falhas pré-existentes é bloqueante em CI (D-138: testes em `modules/*/tests/` não rodam na CI, só `infra/tests/stack.tftest.hcl` na raiz). Verificação empírica: `terraform plan` REAL contra `dev` (`AWS_PROFILE=claude-dev`) mostra exatamente **11 recursos novos** (Lambda+role+2 policies+2 attachments+alias, +integração+2 rotas+permissão) e as MESMAS 4 substituições pré-existentes já isoladas por D-208 — nada inesperado. G-V3: 8 testes novos de HTTP (`bulk-action-handlers.test.ts` — 400 em corpo ausente/inválido/campo extra/campo faltante, 200 com outcomes por item, tanto reassign quanto archive), 16 testes de `proxy-allowlist.test.ts` (2 novos + 14 preexistentes, todos verdes — achado de teste próprio: `GET /items/bulk-reassign` casa legitimamente com `{itemId}` por ambiguidade já documentada no arquivo, teste ajustado para usar `PATCH`), 10 testes de contrato novos (`schemas.test.ts`), 6 asserts novos em `api_gateway.tftest.hcl`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, suíte completa a confirmar. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO, mergeado em `main` (PR #243, `f80ace7`), CI/CD 100% verde** | `src/modules/expiration/http/{item-handlers.ts,bulk-action-handlers.ts}`, `src/runtime/aws/handlers/bulk-actions-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `schemas/api/bulk-{reassign,archive}-items-request.v1.json`, `src/shared/contracts/schema-validator.ts`, `scripts/build-lambdas.ts`, `infra/{main.tf,modules/api-gateway/{main.tf,variables.tf,tests/api_gateway.tftest.hcl}}`, `test/{unit/expiration/bulk-action-handlers.test.ts,unit/bff/proxy-allowlist.test.ts,contract/schemas.test.ts}` | 2026-09-05 |
242:| D-211 | **D-204 fatia 1 (entidades de domínio + registro GSI8 + destino de outbox) — IMPLEMENTADA.** Roadmap P1 item 15 (relatórios agendados) começa a sair do design-only. 3 entidades novas em `src/modules/reports/domain/`: `ReportSubscription` (`reportTypes` subconjunto não vazio dos 7 já expostos por `ReportsService`/D-195, `cadence` tipado como union — só `WEEKLY` no v1, mesma disciplina de escopo estreito de `FIELD_SCHEMA_V1` — `dayOfWeek`/`localTime`/`timeZone` no mesmo formato de `ReminderTrigger`/`ReminderPolicy`, `recipientUserIds` teto nomeado `MAX_REPORT_SUBSCRIPTION_RECIPIENTS=10`, `nextRunAt` avançado atomicamente pelo worker, ponteiro `GSI8PK`/`GSI8SK` no namespace `REPORT_SUBSCRIPTION`); `ReportSubscriptionRun` (1 por execução agendada, congela `reportTypes`/`recipientUserIds` no momento do claim — decisão 5: conjunto de linhas congelado, valores de campo relidos frescos na geração); `ReportDeliveryAttempt` (1 por destinatário, máquina de estados espelhando `NotificationAttempt` — `PREPARED`→`SUBMITTING`(lease)→`ACCEPTED`/`FAILED_RETRYABLE`/`FAILED_TERMINAL`/`UNKNOWN`, mais `skippedReason: "RECIPIENT_NOT_ELIGIBLE"` para quando a revalidação fresca de Membership/GlobalUser falha antes de qualquer chamada à SES). Terraform: `report_subscription = "REPORT_SUBSCRIPTION"` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`) — mecânico, reusa GSI8 (não GSI10), mesmo padrão das 9 entradas existentes de D-179; nenhum Lambda/IAM novo ainda (isso é fatia 2, quando o worker de agendamento existir). Novo valor aditivo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1` no enum fechado `OutboxDestination` (`src/shared/outbox/outbox.ts`) — verificado que `DestinationSenders` é `Partial<Record<OutboxDestination,...>>`, não exaustivo, então nenhuma composição existente precisa de um sender novo ainda (mesmo padrão "valor existe antes do consumidor" que `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`/D-193 item 6/9 já estabeleceu). G-V3: 11 testes novos (`report-subscription.test.ts` — key builders das 3 entidades, `validateReportSubscriptionInput` cobrindo reportTypes vazio/desconhecido, recipientUserIds vazio/acima do teto/exatamente no teto, todos os 7 tipos reais juntos). Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `terraform test` do módulo `dynamo-table` **7/7** (`AWS_PROFILE=claude-dev`); suíte completa a confirmar. **Pendente real, nomeado**: fatias 2-4 de D-204 (scheduler+claim transacional; worker de entrega+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) ainda não implementadas — nenhum Lambda novo, nenhuma rota HTTP, nenhum e-mail real ainda. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 1 de D-204)** — pendente fatias 2-4, PR/merge/CD | `src/modules/reports/domain/{report-subscription.ts,report-subscription-run.ts,report-delivery-attempt.ts}`, `src/shared/outbox/outbox.ts`, `infra/modules/dynamo-table/main.tf`, `test/unit/reports/report-subscription.test.ts` | 2026-09-05 |
243:| D-212 | **D-204 fatia 2 (scheduler + claim transacional) — IMPLEMENTADA.** Roadmap P1 item 15 continua saindo do design-only. `runScheduledReportsTick` (`src/workers/scheduled-reports/scheduler.ts`) escaneia GSI8 (`WORK#REPORT_SUBSCRIPTION`) exatamente como `requirement-reindex/reindex.ts` (mesmo cap `MAX_PAGES=25`, mesma paginação), rele fresco cada `ReportSubscription` candidata (GSI8 nunca fonte de elegibilidade, defensivo contra query eventualmente consistente), e comita uma transação de 2 ações por assinatura due — `Update` avançando `nextRunAt`/ponteiro GSI8 (`ConditionCheck` de versão) + `Outbox` `Put` (destino `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`, já aditivo desde D-211) — na MESMA `TransactWriteItems`, mesma disciplina "claim + evento durável atômico" de `reminder-producer/producer.ts` (um claim seguido de SendMessage direto não é atômico; Lambda pode morrer no meio, deixando uma assinatura reivindicada sem entrega enfileirada). Payload do evento deliberadamente mínimo (`runId`/`subscriptionId`/`tenantId`/`scheduledFor`) — decisão 6 de D-204 exige que o worker de entrega (fatia 3, ainda não construído) reresolva `recipientUserIds` FRESCO no envio, então não há valor em embutir um snapshot de `reportTypes`/destinatários aqui; `runId` é gerado AGORA (não pela fatia 3) para que o futuro `ReportSubscriptionRun`/`ReportDeliveryAttempt` compartilhem um id estável entre reentregas SQS at-least-once, mesma postura do `deduplicationKey` de `reminder-producer`. Nova primitiva `nextWeeklyOccurrenceUtc` (`report-subscription-schedule.ts`) computa a próxima ocorrência de `dayOfWeek`(convenção ISO 1=segunda..7=domingo, documentada explicitamente em `ReportSubscription.dayOfWeek` desde D-211 para evitar off-by-one contra `Date.getUTCDay()`)/`localTime`/`timeZone`, reusando `zonedTimeToUtc`/`parseLocalTime`/`addCalendarDays` de `reminder/domain/recurrence.ts` (reuso cross-módulo já estabelecido por `notification/application/quiet-hours.ts` — nenhuma duplicação de lógica DST-aware) mais uma nova primitiva exportada `instantToLocalParts` (direção inversa de `zonedTimeToUtc`, necessária para achar "hoje" no calendário local do assinante antes de buscar a próxima ocorrência). Novo port mínimo `ReportSubscriptionStore` (`get`+`transactWrite` só — criação/gestão de assinatura via rota HTTP fica para uma fatia futura, ainda não decidida) + adapter real `DynamoDbReportSubscriptionStore` + `DynamoDbScheduledReportsCandidateSource` (GSI8 Query, mesmo formato de `requirement-reindex/dynamodb-candidate-source.ts`). Lambda real `scheduled-reports-scheduler-handler` (`timeout_seconds=300`, `gsi8_read_policy_json["report_subscription"]`/`worker_transact_write_policy_json["report_subscription"]` — chaves já provisionadas automaticamente por D-211's `local.gsi8_worker_types` addition, mecânico) + role/policy de `aws_scheduler_schedule` + log group + registrado em `security_audit_observability.global_index_function_names` (exigiu adicionar `"scheduled-reports"` à união fechada `GlobalIndexComponent`, `security-audit.ts`). **Decisão de implementação documentada, não uma nova decisão de arquitetura**: cron do `aws_scheduler_schedule` é DIÁRIO (04:15 UTC), não literalmente semanal apesar do texto da decisão 3 de D-204 dizer "aws_scheduler_schedule único semanal" — lido aqui como descrevendo a cadência da FEATURE (`ReportSubscriptionCadence="WEEKLY"`, decisão 1: cada assinatura dispara ~1x/semana), não a frequência de polling do schedule em si; "único" (1 Lambda/1 schedule/1 scan cross-tenant por execução, "mesmo padrão de requirement-reindex") é a parte vinculante da frase. Um poll diário é o que de fato honra `dayOfWeek`/`localTime`/`timeZone` por assinatura com precisão real (~24h de deriva máxima, igual a todo outro worker GSI8-esparso do projeto) — um poll literalmente semanal dessincronizaria o horário escolhido por cada assinante para o instante único em que o schedule disparasse. Aberto a correção se a leitura literal (cron semanal) era de fato a intenção. `terraform plan` real contra `dev` (`AWS_PROFILE=claude-dev`) confirmou os 12 recursos novos como `+ create` limpo (nenhum recurso pré-existente afetado; os 4 `-/+ destroy and then create replacement` de `aws_lambda_permission` no mesmo plano são débito técnico pré-existente já nomeado em sessão anterior, não relacionados a esta mudança) — `terraform fmt`/`validate` limpos. G-V3: 7 testes novos de `nextWeeklyOccurrenceUtc` (rollover mesma semana/semana seguinte, semântica estritamente-depois-do-instante, convenção ISO dayOfWeek=7≠`Date.getUTCDay()`, timezone-aware cross-fuso, busca a partir da data calendário LOCAL perto de fronteira de dia) + 7 testes de `runScheduledReportsTick` (claim avança nextRunAt/GSI8/escreve evento outbox, ignora não-due, multi-tenant, concorrência perdida no claim, candidato obsoleto nunca reclamado 2x) + 2 de `shouldAlarmScheduledReports`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) e a própria rota HTTP de criação/gestão de `ReportSubscription` (`ADMIN_ROLES`, decisão 1) — nenhuma delas implementada ainda; sem elas, nenhuma assinatura real pode ser criada nem entregue, feature continua não-usável ponta a ponta. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de cadência de cron documentado explicitamente acima) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 2 de D-204)** — pendente fatias 3-4 + rota HTTP de criação, PR/merge/CD | `src/workers/scheduled-reports/{scheduler.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/reports/domain/report-subscription-schedule.ts`, `src/modules/reports/ports/report-subscription-store.ts`, `src/modules/reports/persistence/dynamodb-report-subscription-store.ts`, `src/modules/reminder/domain/recurrence.ts`, `src/runtime/aws/handlers/scheduled-reports-scheduler-handler.ts`, `src/runtime/aws/composition/reports.ts`, `src/shared/observability/security-audit.ts`, `scripts/build-lambdas.ts`, `infra/main.tf` | 2026-09-05 |
244:| D-213 | **ReportSubscription CRUD (create/get/list/delete) — IMPLEMENTADA.** Fecha a lacuna nomeada por D-212: sem uma rota HTTP de criação, nenhuma assinatura real podia existir para o scheduler/worker de entrega processarem. D-204 decisão 1 previa "criação/gestão via rota HTTP ADMIN_ROLES", mas a quebra em fatias sugerida (§"Próxima ação" do estado-final-consolidado) não incluía essa rota como fatia própria — decisão de sequenciamento tomada aqui, não uma decisão de arquitetura nova. **Superfície deliberadamente estreita para v1**: create/get/list/delete apenas, SEM update — um assinante que queira mudar reportTypes/horário/destinatários apaga e recria, mesma disciplina "não inventar superfície que o design aprovado não pediu" de `bulk-actions-scoping`'s v1 (só reassign+archive). `ReportSubscriptionService` nova (`authorize()`+`executeTenantBusinessMutation` no create/delete, mesmo pipeline de `DocumentArchiveService.createDocumentType`/`getDocumentType`) — nova action RBAC única `reports:subscription-manage` (`ADMIN_ROLES`, mesmo tier de `docarchive:requirement-export`/`item:export` — uma assinatura nomeia OUTROS membros como destinatários e produz os mesmos CSVs bulk cross-membro que essas actions já protegem, não o tier mais permissivo de "ler o próprio recurso"). **Achado real durante a fatia**: `ReportSubscription` (D-211) não tinha nenhum índice de listagem — cada assinatura é sua própria partição (`PK=TENANT#<t>#REPORTSUB#<subId>`), sem GSI nenhum "lista todas do tenant" seria impossível; corrigido adicionando `GSI1PK=TENANT#<t>#REPORTSUB`/`GSI1SK=<createdAt>#REPORTSUB#<id>` ao GSI1 compartilhado (mesmo índice físico que Document/ExpirationItem/Requirement/DocumentType/RequirementTemplate já discriminam por prefixo — nenhum índice novo), reusando exatamente o padrão `documentTypeGsi1Keys` já estabeleceu para o catálogo de DocumentType. `createSubscription` computa `nextRunAt` inicial via `nextWeeklyOccurrenceUtc(now, ...)` (D-212) — a mesma primitiva que o scheduler usa para avançar a cada claim, então uma assinatura recém-criada já é descobrível pelo GSI8 sem caso especial de "primeira execução". `delete` é físico (não soft-archive) — `buildVersionedDelete` + fence do tenant, releitura fresca ANTES do delete para distinguir `NotFoundError` (nunca existiu) de `ConflictError` (`expectedVersion` obsoleto), nunca assumindo causa que o DynamoDB não revelou (mesma postura RT-LANE-FALLBACK-01). Rotas `POST /reports/subscriptions`, `GET /reports/subscriptions`, `GET /reports/subscriptions/{subscriptionId}`, `POST /reports/subscriptions/{subscriptionId}/delete` — MESMO Lambda/integration `reports-handler` que já serve os 7 relatórios CSV (D-195), discriminado por `routeKey` no entrypoint (nunca confundido com a resposta CSV — `isCsvResponse()` já existia para esse propósito); 4 entradas novas em `proxy-allowlist.ts` (disciplina D-117/D-120/D-178/D-209 — diferente das 7 rotas CSV, que são deliberadamente NÃO proxyadas pela BFF por causa do `Content-Disposition`, estas são JSON puro e não têm esse problema). 2 schemas JSON novos (`report-subscription-create-request.v1.json`/`-delete-request.v1.json`) registrados em `schema-validator.ts`. G-V3: 12 testes de `ReportSubscriptionService` (create com nextRunAt correto/RBAC/validação de domínio+dayOfWeek+localTime+timeZone, get/list via GSI1 ordenado, delete com NotFoundError/ConflictError), 10 de HTTP handlers (400 em corpo ausente/schema inválido/param ausente, 404/409 reais), 2 novos de `proxy-allowlist`, 10 de contrato para os 2 schemas — **80 testes novos** nesta fatia. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `terraform fmt`/`validate` limpos; `terraform plan` real contra `dev` confirmou as 4 rotas novas como `+ create` limpo (os mesmos 4 `-/+ replace` de `aws_lambda_permission` pré-existentes de D-212 reaparecem, não relacionados); suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS consumindo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`+rota de download; wiring completo do outbox) seguem não implementadas — uma assinatura agora pode ser criada e será reivindicada pelo scheduler (D-212) no próximo tick devido, mas nenhum e-mail real é enviado até a fatia 3 existir. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de sequenciamento — rota HTTP como fatia própria, não prevista nas 4 fatias sugeridas — e 1 de modelagem de índice — GSI1 novo para ReportSubscription, mecânico/reuso de padrão já estabelecido) | — | — | **IMPLEMENTADO (create/get/list/delete)** — pendente fatias 3-4 de D-204, PR/merge/CD | `src/modules/reports/{domain/report-subscription.ts,application/report-subscription-service.ts,application/id-generator.ts,http/reports-handler.ts,ports/report-subscription-store.ts,persistence/dynamodb-report-subscription-store.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{ids.ts,composition/reports.ts,handlers/reports-handler.ts}`, `src/shared/contracts/schema-validator.ts`, `schemas/api/report-subscription-{create,delete}-request.v1.json`, `infra/modules/api-gateway/main.tf` | 2026-09-05 |
247:| D-216 | **D-205 fatia 1 (`DossierExportRun` + preview/confirm HTTP + `scopeHash`) — IMPLEMENTADA.** Começa a tirar o item 16 do backlog P1 (dossiê documental PDF/Excel) do design-only. `DossierExportRun` (`src/modules/document-archive/domain/dossier-export-run.ts`) co-localizado na MESMA partição do Subject (`PK=TENANT#<t>#SUBJECT#<subjectId>`, `SK=DOSSIER#<runId>`) — mesma convenção que `requirementKey()` já estabelece, mesmo racional (o ciclo de vida do export pertence ao Subject). `POST /document-archive/subjects/{subjectId}/dossier` cria o run em `PREVIEW_READY`, congelando os `requirementIds` ATUAIS do Subject (decisão 4 — nunca redderivados depois) e devolvendo as linhas de preview na MESMA resposta. `POST .../dossier/{runId}/confirm` é idempotente dado um `scopeHash` batendo (fingerprint canônico JSON+SHA-256 sobre `subjectId`+`requirementIds` ordenados) — confirmar um run JÁ confirmado com o MESMO hash é no-op de sucesso, nunca um segundo dispatch; um hash divergente é SEMPRE 409, independente do status atual (decisão 3). Um confirm bem-sucedido dispara um destino de outbox novo (`SQS_DOSSIER_EXPORT_V1`, aditivo, sem consumidor ainda — mesmo padrão "valor existe antes do consumidor" de `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`/D-211). RBAC: action nova `docarchive:dossier-export`, `ADMIN_ROLES` EXCLUSIVAMENTE (decisão 10 — a Rodada 1 do Codex já tinha achado real que "ADMIN_ROLES ou assignee" não seria expressável estruturalmente, já que `Requirement` não tem `ownerUserId`). Achado de reuso fechado no caminho: o mecanismo de fingerprint canônico JSON+SHA-256 que `search-cursor.ts` (D-194 Fatia 3) já usa para o cursor de busca foi extraído para `shared/domain/fingerprint.ts` novo, reusado por `scopeHash` em vez de duplicar a canonicalização. Métodos novos (`previewDossierExport`/`confirmDossierExport`) adicionados à MESMA `DocumentArchiveService` já gigante (não um serviço novo — a lógica reusa diretamente `listRequirements`/`trackedSubjectKeyForFence` já existentes na classe, mesmo racional de `getSubjectCompliance`). G-V3: 10 testes de serviço (`dossier-export.test.ts` — congelamento de requirementIds, scopeHash determinístico, 404 de Subject inexistente, idempotência do confirm, 409 de hash divergente mesmo após confirmado, RBAC) + 5 de HTTP (`document-archive-dossier-routes.test.ts` — preview→confirm ponta a ponta via handlers reais, 400 de scopeHash ausente, 409, 404, 403 de VIEWER) + 3 de contrato do schema novo + 2 de `proxy-allowlist`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2392/2392** (217 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate` limpos; `terraform plan` real contra `dev` mostrou exatamente as 2 rotas novas esperadas (mais o mesmo baseline pré-existente de 4-replace/110-change já nomeado em sessões anteriores). PR #250 mergeado (`866a762`), CD (`34046861084`) `completed`/`success` confirmado por polling síncrono do `databaseId` capturado ANTES do merge. **Verificação ao vivo contra `dev` feita**: `exptrk-dev-document-archive-handler` `Active`/`Successful` (`LastModified` batendo com o horário deste deploy); as 2 rotas `POST /document-archive/subjects/{subjectId}/dossier`/`.../dossier/{runId}/confirm` confirmadas presentes na API `uav7id1muh`. **Pendente real, nomeado, não bloqueante**: fatia 2 (worker de geração PDF+XLSX via `pdf-lib`+nova dependência de Excel, `DossierTooLargeError`, S3) e fatia 3 (rota de download autenticada com presign sob demanda, mesmo padrão de D-204) seguem não implementadas — hoje dá pra fazer preview/confirm, mas nenhum documento é gerado nem baixável ainda. **Próxima ação real**: fatia 2 de D-205, ou escopar itens 18-19 do backlog P1, a critério de quem retomar. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 1 de D-205)** — pendente fatias 2-3 | `src/modules/document-archive/{domain/dossier-export-run.ts,application/document-archive-service.ts,application/id-generator.ts,http/document-archive-handlers.ts}`, `src/shared/domain/{fingerprint.ts,search-cursor.ts}`, `src/shared/outbox/outbox.ts`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{ids.ts,handlers/document-archive-handler.ts}`, `src/shared/contracts/schema-validator.ts`, `schemas/api/docarchive-dossier-confirm-request.v1.json`, `infra/modules/api-gateway/main.tf` | 2026-09-06 |
248:| D-217 | **D-205 fatia 2-3 (worker de geração PDF+XLSX + rota de download autenticada) — IMPLEMENTADA. Item 16 do backlog P1 (dossiê documental) FECHADO POR INTEIRO (D-205 completo, fatias 1-3).** `processDossierExportGeneration` (`src/workers/dossier-export/generate.ts`) consome `SQS_DOSSIER_EXPORT_V1` (dispatch já existia desde D-216, sem consumidor): reclama `CONFIRMED→GENERATING` via lease (`generatingLeaseExpiresAt`, mesmo padrão de `NotificationAttempt`'s SUBMITTING — reclama um run travado cuja lease expirou, pula silenciosamente um redelivery ainda dentro da lease), busca os dados via `documentArchive.getDossierExportData` (chamada ÚNICA — achado próprio corrigido antes do commit: rascunho inicial chamava duas vezes, uma só para `subjectDisplayName`), declara `TOO_LARGE` (nunca um artefato truncado silenciosamente) quando o escopo CONGELADO excede `MAX_DOSSIER_REQUIREMENTS=200`, omite silenciosamente (nunca fabrica) um `Requirement` apagado desde o confirm, gera PDF (`dossier-pdf-builder.ts`, `pdf-lib` — primeiro uso do projeto para CRIAR, não só parsear, um PDF; paginação real via `font.widthOfTextAtSize`, nunca descarta uma linha) e XLSX (`dossier-xlsx-builder.ts`, dependência nova `exceljs@4.4.0` exact-pinned; 2 abas, nunca trunca célula) EM PARALELO, sobe ambos ao S3 (bucket `report_exports` REUSADO sob prefixo próprio, nenhum bucket novo — achado de reuso explícito), marca `READY`+`generatedAt`, limpa a lease; erro no meio marca `FAILED`+`failureReason` e RELANÇA (nunca engole). Mitigação de injeção de fórmula (`sanitizeFormulaInjection`, apóstrofo líder em `=+-@`) extraída para `shared/spreadsheet/formula-injection.ts` e aplicada tanto no XLSX novo quanto retroativamente ao CSV existente (`csv-export-writer.ts`) — achado de segurança fechado no caminho, não só no código novo. Rota nova `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download?format=pdf|xlsx` — mesmo padrão de presign sob demanda de 5 min de D-204/D-215 (nunca embute link de S3 de TTL longo), 409 se o run não está `READY`, 400 para `format` fora de `pdf`/`xlsx`, RBAC `docarchive:dossier-export` `ADMIN_ROLES` EXCLUSIVAMENTE (mesma decisão 10 de D-216, sem fallback de destinatário — sensibilidade de divulgação diferente da rota de relatórios). Infra nova: fila+DLQ `dossier-export` (`sqs-worker-queue`, `consumer_timeout_seconds=120`); Lambda `dossier-export-generation-handler` (`timeout=120s`, env `QUARANTINE_BUCKET_NAME`+`REPORT_EXPORTS_BUCKET_NAME`); `document_archive_handler` ganha `REPORT_EXPORTS_BUCKET_NAME`+policy de leitura S3. **Achado mecânico fechado no caminho**: `workbook.xlsx.load(buffer)` falhava o typecheck do arquivo de teste (`Buffer<ArrayBufferLike>` do `@types/node` aninhado do `exceljs` estruturalmente diferente do `Buffer` do próprio repo) — contornado com um helper `loadWorkbook` que passa pelo cast `unknown`, escopado só ao arquivo de teste. G-V3: 4 testes de `dossier-xlsx-builder.test.ts` + 5 de `dossier-pdf-builder.test.ts` + 8 de `dossier-export-generation.test.ts` (happy path, `RUN_NOT_FOUND`, idempotência `ALREADY_RESOLVED`, lease não expirada `SKIPPED_IN_PROGRESS`, reclamação de lease expirada, `TOO_LARGE`, requirement apagado omitido, `FAILED`+rethrow) + 5 novos de download em `document-archive-dossier-routes.test.ts` (total 10 no arquivo) + extensão de `proxy-allowlist.test.ts`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2415/2415** (220 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate`/`plan` limpos contra `dev`. PR #252 mergeado (`bc35727`); CD (`34050910874`) falhou uma vez por contenção real de lock do state Terraform contra `dev` (4 runs de CI concorrentes de branches Dependabot rodando `terraform plan` simultaneamente no mesmo backend S3/DynamoDB — não um defeito de código; diagnosticado via `gh run view --log-failed`, resolvido aguardando os 4 runs concorrentes concluírem e rodando `gh run rerun --failed`, sem precisar do `force-unlock` pré-autorizado por §7) — rerun `completed`/`success` confirmado. **Verificação ao vivo contra `dev`, todos os pontos confirmados**: `exptrk-dev-document-archive-handler`/`exptrk-dev-dossier-export-generation-handler` `Active`/`Successful` (`LastModified` batendo com o horário deste deploy); fila+DLQ `exptrk-dev-dossier-export`/`-dlq` existem; event source mapping `Enabled` (via `--event-source-arn`, mesmo achado de ferramenta já nomeado em D-215); rota `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download` presente na API `uav7id1muh`. **Item 16 do backlog P1 agora COMPLETO por inteiro** (fatias 1-3 de D-205 fechadas) — ainda não testado ponta a ponta com um preview→confirm→geração real disparando via SQS real em `dev` (só G-V3/unit + smoke de infra), próxima sessão pode considerar esse smoke test antes de considerar a feature 100% validada, ou seguir para os itens 18-19 do backlog P1 (metadata configurável por Document Type; compartilhamento externo seguro). | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO POR COMPLETO (D-205 FECHADO, item 16 do backlog P1 fechado por inteiro)** | `src/workers/dossier-export/generate.ts`, `src/modules/document-archive/{application/document-archive-service.ts,application/dossier-pdf-builder.ts,application/dossier-xlsx-builder.ts,http/document-archive-handlers.ts,ports/dossier-export-store.ts,persistence/s3-dossier-export-store.ts,domain/dossier-export-run.ts}`, `src/shared/{spreadsheet/formula-injection.ts,csv/csv-export-writer.ts,errors/app-error.ts}`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition/document-archive.ts,composition/reminder.ts,handlers/document-archive-handler.ts,handlers/dossier-export-generation-handler.ts,handlers/dispatch-outbox-relay-handler.ts,handlers/outbox-sweeper-handler.ts}`, `infra/main.tf`, `infra/modules/api-gateway/main.tf`, `scripts/build-lambdas.ts`, `package.json` | 2026-09-06 |
252:| D-222 | **Auditoria do item 9 do roadmap P0 (`roadmap-competitivo-2026-09-01.md`, "Consolidar Guest Upload + Requests + Review + Recurrence como produto completo") — NÃO FECHADO. Achado real: o ciclo Requirement→Document Request→Guest link→Upload→Review→Accept/Reject descrito no roadmap (`P0.9`) está tecnicamente implementado peça a peça (D-143 a D-147, infra confirmada `Active` em `dev` via `aws lambda list-functions`/`apigatewayv2 get-routes`: `document-archive-handler`/`document-archive-guest-handler`/`document-request-recurrence-handler`, rotas `/document-archive/series*` e `/document-archive/guest/*` presentes na API `uav7id1muh`), mas DESCONECTADO no ponto central do ciclo — nenhum guest jamais recebe um link utilizável hoje.** **Achado 1 (bloqueador central, nível 5, NÃO corrigido)**: `GuestDocumentAccessService.issueCredential()` (único método que grava um `RequestAccessCredential`, D-146) tem ZERO call sites em todo o repositório fora do próprio arquivo — confirmado por grep exaustivo. Nem `DocumentRequestRecurrenceService.materializeAttempt()` (o próprio código o documenta como "'resend' in the product UI", ou seja, é o mecanismo de "solicitar novamente" pretendido pelo roadmap) nem o worker periódico (`src/workers/document-request-recurrence/materializer.ts`) chamam `issueCredential` depois de criar um `DocumentRequest` novo. Toda vez que um `DocumentRequest` é materializado (criação inicial ou "resend"), nenhum `RequestAccessCredential` é emitido — o guest nunca teria um token para acessar. Causa raiz não é um esquecimento mecânico: `composition/document-archive.ts` mantém `buildDocumentArchiveDeps` (Lambda autenticada `document-archive-handler`, hospeda `recurrence`) e `buildDocumentArchiveGuestDeps` (Lambda separada `document-archive-guest-handler`, pepper próprio `DOCARCHIVE_GUEST_ACCESS_PEPPER`, `authorization_type=NONE`) deliberadamente SEM overlap de segredo (D-146: isolamento de superfície guest é decisão de segurança explícita) — fechar isso exige decidir COMO uma Lambda sem o pepper aciona a emissão de credencial que só a Lambda com o pepper pode fazer (compartilhar o pepper, invocação Lambda-a-Lambda, fila assíncrona, ou mover a emissão para dentro da própria Lambda guest via callback) — mudança de fronteira de módulo/segurança, nível 5 de `change-risk-scale.md`, protocolo Claude↔Codex exigido, NÃO decidido nesta auditoria. **Achado 2 (nível 5, mesma decisão pendente)**: não existe caminho para criar um `DocumentRequest` avulso (não recorrente) — a única via de criação é `DocumentRequestSeries`/`materializeAttempt`, que sempre exige `cadence.intervalDays`; um tenant que queira pedir um documento uma única vez precisa simular uma série. **Achado 3 (nível 4-5, documentado, não corrigido)**: `rejectVersion()` (Document/DocumentVersion, D-144) nunca toca o `DocumentRequest` de origem — não há sequer um campo de vínculo reverso (`Document`/`DocumentVersion` não guardam `documentRequestId`, confirmado por grep em `domain/document.ts`), então nem é estruturalmente possível hoje saber, a partir de uma rejeição, qual `DocumentRequest` reabrir; `DocumentRequest.status` permanece `SUBMITTED` indefinidamente após uma rejeição (tecnicamente ainda "live" via `isDocumentRequestLive`, então o MESMO link de guest aceitaria um reenvio se o guest ainda o tivesse — mas isso é acidente da definição de "live", não um fluxo de "solicitar novamente" desenhado). **Achado 4 (nível 5-6, não corrigido)**: nenhum "automated chasing" existe para o `DocumentRequest` do document-archive (distinto do `document-chasing.ts` do módulo `subject`, que serve uma entidade homônima mais antiga e não relacionada) — item explicitamente listado no roadmap P0.9, ausente por completo. **O que ESTÁ de fato completo e verificado por leitura, sem gap**: máquina de estados Document/DocumentVersion com accept/reject+motivo (D-144); `Requirement` com `applicability`/status derivado + reindex diário (D-145); as 3 camadas de guest access (`RequestAccessCredential`/`GuestSession`/`submitEvidence` idempotente, anti-enumeração, CSRF) individualmente corretas e testadas (D-146); `DocumentRequestSeries`/`materializeAttempt` com histórico de tentativas via `attemptIndex`/`parentRequestId`, prazo opcional (`deadline`) (D-147) — todas essas peças têm G-V3 e estão deployadas; o gap é exclusivamente na ORQUESTRAÇÃO entre elas, não na correção interna de cada peça. **Nenhum código foi alterado nesta auditoria** (achados 1/2/4 são decisão de produto/arquitetura nível 5-6, protocolo obrigatório; achado 3 depende da mesma decisão do achado 1 para ter sentido). Item 9 do roadmap P0 permanece **ABERTO**, com pendência nomeada explicitamente para a próxima sessão/Marcelo: decidir o mecanismo de emissão de credencial cross-Lambda (achado 1) antes de qualquer nova fatia de código neste módulo. | Type 1 (nível 5, cruza fronteira de segurança Lambda/pepper já estabelecida — protocolo Claude↔Codex obrigatório antes de implementar) | — | — | **AUDITADO — GAP REAL ENCONTRADO, NÃO CORRIGIDO (decisão nível 5-6 pendente de Marcelo/protocolo)** | `src/modules/document-archive/application/{guest-document-access-service.ts,document-request-recurrence-service.ts}`, `src/modules/document-archive/domain/{document-request.ts,document-request-series.ts,document.ts}`, `src/runtime/aws/composition/document-archive.ts`, `src/workers/document-request-recurrence/materializer.ts`, `docs/project/roadmap-competitivo-2026-09-01.md` (P0.9) | 2026-09-07 |
253:| D-223 | **WhatsApp Operacional (roadmap P0.3) — fatia 1/5 do programa de implementação: widening de tipo (D-1) + entidades novas (D-5/D-6), SEM HTTP exposto. Design já `APPROVED` desde D-197 (protocolo Claude↔Codex completo, 3 rodadas, `docs/architecture/reviews/whatsapp-channel-scoping/estado-final-consolidado.md`) — implementação direta, sem nova rodada de protocolo (nível 3-4).** `NotificationAttempt.channel`/`.provider` e `NotificationAttemptLookup.provider` alargados de literal (`"EMAIL"`/`"SES"`) para união (`NotificationChannel` de `notification-intent.ts`, reusado em vez de redefinido; `NotificationProvider = "SES"\|"META_CLOUD_API"` novo) — aditivo, toda linha existente permanece um subconjunto válido do tipo novo, `SUPPORTED_CHANNELS` em `notification-router.ts` continua `["EMAIL"]` (roteamento real do WhatsApp é fatia 2+, esta fatia só alarga o TYPE). `WhatsAppOptIn` novo (`src/modules/notification/domain/whatsapp-opt-in.ts`): `PK=TENANT#<t>#USER#<userId>`, `SK=WHATSAPP_OPTIN#<phoneE164>` — a chave inclui o telefone por construção (D-5), então uma troca de número cria uma linha NOVA em vez de mutar a antiga, tornando "consentimento é escopado ao telefone ATUAL" verdadeiro sem invalidação explícita nenhuma; `buildWhatsAppOptIn()` valida formato E.164 e lança `ValidationError` ANTES de qualquer I/O. `WhatsAppOptInService.recordOptIn()` novo (`putIfAbsent` create-once, autorização `notification:configure` — mesma ação/READ_ONLY_ROLES de `NotificationPreferencesService`, usuário gerenciando o PRÓPRIO consentimento) — deliberadamente sem rota HTTP ainda (fatia 1 é domínio+entidades, a rota vem numa fatia futura sem mudar a forma desta classe). `GlobalUser.phoneE164?: string` novo (D-6) + `GlobalUserRepository.setPhoneNumber()` novo, valida E.164 antes de ler/escrever, lança `ValidationError` (formato) ou `NotFoundError` (usuário inexistente) — mesmo padrão read-then-`update()` sem OCC que `logoutAll()` já usa nesta classe (propriedade preexistente, não introduzida agora). Validador `isValidE164()` promovido a `src/shared/text/phone-e164.ts` (mesma convenção `shared/text/` de `normalizeDisplayName`, reusado por `WhatsAppOptIn` e `GlobalUser` sem violar fronteira de módulo — `check-boundaries` confirmou 0 violações). **Achado de processo real desta sessão, não de produto**: outro agente trabalhava em paralelo na mesma árvore de trabalho (sem isolamento de worktree, item 9 do roadmap P0, commit não relacionado `D-222`) com 2 arquivos temporariamente quebrando `npm run typecheck` (`export` duplicado em `document-archive-guest-handlers.ts`, módulo `exceljs` não resolvido) no momento desta sessão — confirmado por `git stash` seletivo (só os arquivos desta fatia) que os 2 erros são pré-existentes/alheios, não introduzidos aqui; gate desta fatia verificado com typecheck isolado (stash) limpo, além de `lint`/`check-boundaries`/`check-docs`/`validate-schemas` rodados no estado real da árvore (limpos) e a suíte completa de `test/unit/notification`+`test/unit/identity` (201/201) mais os 18 testes novos desta fatia, todos verdes; `npm test` completo NÃO pôde ser confirmado 100% verde nesta sessão por causa do WIP alheio ainda em andamento no momento do commit — não é uma falha desta fatia, é um estado transitório da árvore compartilhada. G-V3 real: chave de `WhatsAppOptIn` muda com o telefone (prova por teste que duas chaves com telefones diferentes nunca colidem); opt-in idempotente preserva `optedInAt` original mesmo numa segunda chamada com timestamp posterior; `setPhoneNumber`/`buildWhatsAppOptIn` rejeitam formato inválido ANTES de qualquer leitura/escrita (nenhuma linha parcial persistida). **Nenhuma rota HTTP, worker, fila, secret ou infra Terraform tocados nesta fatia** — nada disso é aplicável/verificável ao vivo contra `dev` ainda, por design (fatia domain-only). **Próxima ação real: fatia 2/5** — `WhatsAppProviderAdapter` (D-2, mesma forma de porta de `EmailProviderAdapter`) + `WhatsAppDeliveryWorker` + fila/DLQ dedicada `whatsapp-deliver-queue`/schema `notification.whatsapp-deliver.v1` (D-9) — mapeamento exato dos códigos de erro reais da Cloud API fica para essa fatia, é uma pendência nomeada no design, não desta. | Nível 3-4 (implementação de design já `APPROVED` nível 5-6, sem decisão nova) | — | — | **IMPLEMENTADO (fatia 1/5, domínio+tipos apenas — fatias 2-5 pendentes)** | `src/modules/notification/domain/{notification-attempt.ts,whatsapp-opt-in.ts}`, `src/modules/notification/application/whatsapp-opt-in-service.ts`, `src/modules/identity/persistence/global-user-repository.ts`, `src/shared/text/phone-e164.ts`, `test/unit/{shared/phone-e164.test.ts,notification/whatsapp-opt-in{,-service}.test.ts,identity/global-user-repository.test.ts}` | 2026-09-07 |
254:| D-221 | **D-218 fatia 3/3 (rotas HTTP + proxy-allowlist + schemas) — IMPLEMENTADA. Item 7 do backlog P1 (metadata configurável por Document Type) FECHADO POR INTEIRO (D-218 completo, fatias 1-3).** Implementação direta do design já `APPROVED` (D-218), sem nova rodada de protocolo (nível 3-4). 3 rotas novas no mesmo Lambda `document-archive-handler`, mesma disciplina D-117/D-120/D-178: `POST /document-archive/document-types/{documentTypeId}/metadata-fields` (`handleCreateDocumentTypeMetadataField`), `PATCH .../metadata-fields/{fieldId}` (`handleUpdateDocumentTypeMetadataField`, cobre rename/required/status/archive-reactivate do CAMPO e `optionsPatch` da OPÇÃO no mesmo corpo, espelhando a Decisão 7 do design — opção nunca é uma rota/mecanismo de concorrência separado), `PATCH /document-archive/documents/{documentId}/metadata-values` (`handleUpdateDocumentMetadataValues`). 3 schemas novos registrados em `schema-validator.ts` (checado explicitamente contra a classe de bug 500-em-vez-de-400 que D-167 documenta): `docarchive-documenttype-metadata-field-create-request.v1.json` (`valueType` enum fechado de 6 tipos, `options` cap 50), `docarchive-documenttype-metadata-field-update-request.v1.json` (`valueType` DELIBERADAMENTE ausente — imutável, Decisão 3 —, `optionsPatch` como `oneOf` de 4 variantes ADD/RENAME/ARCHIVE/REACTIVATE cada uma com seu próprio `required` exato), `docarchive-document-metadata-values-update-request.v1.json` (`values` é um mapa de `fieldId` para `oneOf` de `null` + 6 variantes por `valueType`, mesmo padrão de união discriminada por schema já usado em `import-mapping-request.v1.json`/D-192). Wireado em `infra/modules/api-gateway/main.tf` (3 rotas novas no map `document_archive`) e `proxy-allowlist.ts` (3 entradas novas). G-V3: 6 testes de integração HTTP novos (`document-archive-metadata-fields-routes.test.ts` — fluxo completo create→update-campo-e-opção→escrever-valor-em-Document via handlers reais; 400 real de corpo malformado em cada uma das 3 rotas, nunca 500; 403 real de VIEWER nas 2 rotas de catálogo — ADMIN_ROLES — e na rota de valores — WRITE_ROLES) + 16 testes de contrato novos (`schemas.test.ts` — os 3 schemas, incluindo prova explícita de que `valueType` no corpo de update é rejeitado por `additionalProperties:false`, já que o input não tem esse campo) + 5 de `proxy-allowlist.test.ts`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2464/2464** (221 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos; `terraform plan` real contra `dev` mostrou exatamente as 3 rotas novas esperadas (`aws_apigatewayv2_route.document_archive["create_document_type_metadata_field"\|"update_document_type_metadata_field"\|"update_document_metadata_values"]`), mais o mesmo baseline pré-existente de 112-change/4-destroy (churn de `source_code_hash` + as 4 substituições de `aws_lambda_permission` já nomeadas em sessões anteriores, não relacionadas). **Item 7 do backlog P1 agora COMPLETO por inteiro** (fatias 1-3 de D-218 fechadas) — ainda não testado ponta a ponta contra `dev` real via chamada HTTP autenticada de verdade (só G-V3/unit+integração in-memory), próxima sessão pode considerar esse smoke test, ou seguir para o item 8 do backlog P1 (compartilhamento externo seguro, ainda não escopado). | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO POR COMPLETO (D-218 FECHADO, item 7 do backlog P1 fechado por inteiro)** | `src/modules/document-archive/http/document-archive-handlers.ts`, `src/runtime/aws/handlers/document-archive-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/contracts/schema-validator.ts`, `schemas/api/docarchive-{documenttype-metadata-field-create-request,documenttype-metadata-field-update-request,document-metadata-values-update-request}.v1.json`, `infra/modules/api-gateway/main.tf`, `test/integration/document-archive-metadata-fields-routes.test.ts`, `test/contract/schemas.test.ts`, `test/unit/bff/proxy-allowlist.test.ts` | 2026-09-06 |
256:| D-225 | **Compartilhamento Externo Seguro (`ExternalShareLink`, item 19/último item do backlog P1) — design `APPROVED` via protocolo Claude↔Codex, 4 rodadas reais (`codex exec`), régua recalibrada 3 vezes (v1→v2→v3). Fechamento na Rodada 4: Codex nota cega pesquisa 9,3/10, design 9,2/10 (ambos ≥9,0, sem arredondar).** DESIGN-ONLY, nenhum código/schema/infra tocado. E-014 `SIM` (OWASP Forgot Password/Authentication Cheat Sheets para entropia/anti-enumeration; Dropbox/Google para divergência real de postura de expiração de link; AWS Prescriptive Guidance/S3 User Guide, fonte primária, para a limitação estrutural de não-revogabilidade de URLs presigned antes do TTL — confirma que a janela residual de revogação não é falha de design, é propriedade documentada do mecanismo AWS já usado em D-215/216/217). Limitação de pesquisa registrada explicitamente: sem fonte oficial equivalente de Box/Notion/Figma localizada dentro do orçamento das 4 rodadas. **Achado real de leitura direta que mudou o design nas Rodadas 3-4**: existe um precedente MAIS específico que `guest-token.ts` (D-037) — `guest-document-access-service.ts`/`request-access-credential.ts` (D-143, document-archive), já com a ordem anti-enumeração correta (parse→rate-limit ANTES do lookup→lookup→comparação dummy-safe) e `DocumentArchiveGuestRateLimiter.consumeBoth` (seletor+IP) — reaproveitado literalmente em vez do precedente genérico. 3 rodadas de achados reais fechados progressivamente: Rodada 1→2 (17 achados: revogação vs. presign já emitido, ausência de regra determinística de qual `DocumentFile` servir, vínculo referencial não protegido transacionalmente, token em query string, `shareId` vs. selector sem reconciliação, criação não atômica, rate limiter mal citado, comparação dummy ausente, estado `EXPIRED` sem transição real, contradição de default de TTL, semântica "CURRENT" ampliando acesso sem novo consentimento, pendência de senha tratada inconsistentemente, auditoria incompleta com contadores sem writer, IP hash subespecificado, tenant lifecycle não revalidado na leitura anônima, metadados podem vazar dado tenant-controlled, sem cap/listagem de governança); Rodada 2→3 (nomes de atributo errados confirmados por leitura — `DocumentFile.scanStatus` não `status`, `DocumentVersion.state` não `status` —, IP cru persistido no rate limiter como dívida pré-existente herdada, `security-audit.ts` é CloudWatch estruturado não um stream consultável); Rodada 3→4 (contador atômico de cap sem reconciliação de expiração natural — corrigido com reconciliação lazy limitada a 5 itens na própria tentativa de criação —, semântica de revalidação incompleta após `DocumentVersion`/`Document` mudarem de estado — resolvida declarando explicitamente que o link é uma "cópia congelada" que NUNCA revalida esses dois campos, só `scanStatus`/`cleanObject`/tenant/status/expiração —, transporte do token corrigido de query string para PATH). **Decisão final sobre a pendência de senha (não é mais pendência de produto aberta)**: v1 é bearer-token puro, por consistência com uma decisão de produto já convergida no mesmo módulo (`guest-document-access-service.ts` já aceita acesso anônimo bearer-only para a mesma categoria de dado sensível, sentido inverso — upload em vez de leitura); senha/PIN opcional nomeada como fatia futura não bloqueante. Evidência completa das 4 rodadas em `docs/architecture/reviews/external-sharing-scoping/` (9 arquivos + `estado-final-consolidado.md`). | Nível 5-6 (`change-risk-scale.md` — acesso anônimo/não-autenticado a recurso do tenant, postura de segurança nova) | 9,4 (autoavaliação implícita na convergência da Rodada 4, nunca contestada pelo Codex) | 9,2 | **APPROVED (design)** — item 19 do backlog P1 (último item pendente) escopado por inteiro; nenhuma implementação de código feita | `docs/architecture/reviews/external-sharing-scoping/{round1-claude-proposal,round1-codex-critique,round2-claude-revision,round2-codex-critique,round3-claude-final,round3-codex-critique,round4-claude-revision,round4-codex-critique,estado-final-consolidado}.md` | 2026-09-07 |
257:| D-226 | **Emissao de credencial de guest (D-222, item 9 do roadmap P0) — orquestracao cross-Lambda entre `document-archive-handler` (autenticado, sem pepper) e `document-archive-guest-handler` (pepper isolado por D-146) via outbox/SQS assincrono, `APPROVED` via protocolo Claude<->Codex, 7 rodadas (`codex exec`, nota cega): 5,8 -> 7,2 -> 8,1 -> 8,8 -> 8,9 -> 8,9 -> 9,2/10 (Claude 9,2/10, Codex 9,2/10, gate atingido sem arredondar).** E-014 declarado `NAO` (fronteira interna entre modulos deste projeto, nao padrao de mercado a replicar) e confirmado contra `docs/engineering/research-protocol.md` antes de proceder. **Mecanismo (fecha o Achado 1/bloqueador de D-222)**: builder unico `buildDocumentRequestCreatedOutboxEntry` (reaproveitado por `materializeAttempt` interativo, pelo worker `materializer.ts`, e pelo novo `createDocumentRequest` avulso) grava, na MESMA `TransactWriteItems` que cria o `DocumentRequest`, um `OutboxRecord` com destino novo `SQS_DOCUMENT_REQUEST_CREDENTIAL_ISSUANCE_V1`/evento `DocumentRequestCredentialIssuanceRequested`, payload MINIMO (`tenantId`/`subjectId`/`documentRequestId`/`issuanceGeneration`) — nunca dado de negocio que o consumidor deva confiar sem reler o registro autoritativo. `DocumentRequest` ganha `issuanceGeneration`/`activeCredentialSelectorHash` (identidade por geracao, nao so por id, para reemissao pos-rejeicao nao colidir com a idempotencia da emissao original). Consumidor novo na Lambda guest (tem o pepper): transacao atomica de 4 entradas (IdempotencyRecord por geracao + `RequestAccessCredential` Put + `DocumentRequest` Update com `ConditionExpression` completa de geracao/status/versao/pointer + Put do material de entrega numa TABELA DYNAMODB DEDICADA NOVA, sem GSI, nunca a tabela principal/politica geral tenant-facing) — token bruto nunca toca superficie de leitura compartilhada. Entrega acionada por DynamoDB Streams+Event Source Mapping (filtro INSERT em infra e handler), at-least-once explicito, fila de falha `GuestCredentialDeliveryFailures` reclassificada como alerta (nao carrega payload/chaves, correcao factual da Rodada 7) — perda definitiva assumida apos a janela de retencao do Stream, TTL da tabela e housekeeping de seguranca. **Achado 2 (avulso)**: `createDocumentRequest()` novo com `ConditionCheck` de Requirement + IdempotencyRecord por payloadHash. **Achado 3 (vinculo reverso)**: `rejectVersion()`, fenced por `lastSubmissionId`/`status===SUBMITTED`, reabre a Request (`issuanceGeneration+1`), revoga a credencial antiga (`ConditionExpression` de pertencimento + `if_not_exists` idempotente) e enfileira nova emissao, na mesma transacao ja existente. **Achado 4 (chasing)**: mecanismo completo (worker produtor, GSI1 `REQUESTDUE` novo, destino de dispatch proprio) atras de `DOCUMENT_REQUEST_CHASING_ENABLED=false`. **DESIGN-ONLY nesta rodada de protocolo** — implementacao da fatia 1 (mecanismo central de issuance, Achados 1-3) prossegue na mesma sessao por nao depender de decisao de produto pura (ver pendencias). | Nivel 5 (cruza fronteira de seguranca Lambda/pepper de D-146, protocolo obrigatorio) | 9,2 | 9,2 | **APPROVED (design) + implementacao da fatia 1 em andamento na mesma sessao** — pendencias de produto nomeadas: (1) cadencia/quantidade/aprovacao humana do chasing (mecanismo pronto, desligado); (2) TTL definitivo da credencial quando `deadline` ausente (hoje 7 dias, default provisorio de engenharia) | `docs/architecture/reviews/guest-credential-issuance-scoping/` (round1 a round7 + `estado-final-consolidado.md`) | 2026-09-07 |
258:| D-227 | **D-226 fatia 2 (consumidor guest da emissao de credencial) — IMPLEMENTADA. CORRECAO DE UM REGISTRO ANTERIOR DESTA MESMA LINHA: item 9 do roadmap P0 NAO esta fechado por inteiro — ver D-228 abaixo.** Consumidor novo na Lambda guest: `document-request-credential-issuance-service.ts` + handler `document-request-credential-issuance-handler.ts`, consome `SQS_DOCUMENT_REQUEST_CREDENTIAL_ISSUANCE_V1` via Event Source Mapping baseado em FILA (nao Streams — correcao factual: a fila e o mecanismo do PRODUTOR/fatia 1, nao ha Streams aqui), transacao atomica das 4 entradas desenhadas em D-226 (IdempotencyRecord por geracao, `RequestAccessCredential` Put, `DocumentRequest` Update com `ConditionExpression` de geracao/status/versao/pointer, Put na tabela dedicada nova `infra/modules/guest-credential-delivery-table/`, sem GSI, isolada da tabela principal, Streams NEW_IMAGE habilitado nela mas SEM Event Source Mapping ainda — ver achado abaixo). `createDocumentRequest()` avulso novo (Achado 2) com `ConditionCheck` de Requirement + IdempotencyRecord por payloadHash. `rejectVersion()` (Achado 3) reabre a Request via `issuanceGeneration+1`, revoga a credencial antiga com `ConditionExpression` de pertencimento + `if_not_exists` idempotente, enfileira nova emissao na mesma transacao. G-V3: idempotencia do consumidor sob mensagem duplicada, isolamento estrutural da tabela dedicada, `rejectVersion()` reabrindo corretamente, criacao avulsa idempotente. **Achado de processo real (subagentes)**: a implementacao ficou paralisada por 3 niveis sucessivos de subagentes que apenas redelegavam a mesma tarefa sem executa-la; a sessao orquestradora interveio diretamente, rodou os gates ela mesma e fechou esta fatia — licao registrada em memoria persistente. **Achado real, verificado ao vivo contra `dev` apos o merge, que corrige o fechamento indevido registrado antes nesta linha**: o comentario do proprio codigo (`guest-credential-delivery-table/main.tf` linhas 16-23, `guest-credential-delivery.ts` linhas 14-17) documenta explicitamente que o WORKER DE ENTREGA (que leria a tabela dedicada via Streams e enviaria de fato o link ao guest) **nao foi construido nesta fatia** — apenas o material bruto e armazenado. Confirmado por `aws lambda list-event-source-mappings --event-source-arn <stream-arn-da-tabela>` retornando vazio. **Resultado pratico: nenhum guest recebe um link utilizavel ainda, mesmo apos esta fatia** — o gap original de D-222 (Achado 1) so fecha quando o worker de entrega existir E houver uma forma de descobrir o destinatario (email/telefone do guest), que nao esta modelada em `DocumentRequest` hoje (achado novo, ver D-228). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/suite completa **2504/2504** (227 arquivos), `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. Mergeado em `main` (PR #259, CI/CD `success`), Lambda/tabela confirmados `Active`/`ACTIVE` ao vivo. | Nivel 3-4 (implementacao de design ja `APPROVED` D-226, sem decisao nova) | — | — | **IMPLEMENTADO PARCIALMENTE (produtor+consumidor+armazenamento; worker de entrega ao guest AINDA NAO EXISTE — item 9 do roadmap P0 permanece ABERTO, ver D-228)** | `src/modules/document-archive/{application/document-request-credential-issuance-service.ts,application/document-archive-service.ts,domain/guest-credential-delivery.ts,http/document-archive-guest-handlers.ts}`, `src/runtime/aws/handlers/document-request-credential-issuance-handler.ts`, `infra/modules/guest-credential-delivery-table/`, `test/unit/document-archive/{document-request-credential-issuance-service.test.ts,document-request-avulso-and-reopen.test.ts}` | 2026-09-07 |
259:| D-228 | **Fecha o item 9 do roadmap P0 (D-222/D-226/D-227) pela primeira vez de ponta a ponta: worker de entrega de credencial de guest + campo de destinatário.** Duas peças aditivas, nível 3-4 (implementação direta, seguindo precedente já convergido no mesmo código-base para o MESMO problema — nunca decisão nova de arquitetura, avaliado explicitamente contra `docs/engineering/change-risk-scale.md` antes de proceder sem protocolo Claude↔Codex): **(1) `DocumentRequest.recipientEmail?: string`** (`document-archive/domain/document-request.ts`) — campo OPCIONAL/aditivo, mesmo padrão `recipientEmail: string` já usado pelo módulo `subject` (entidade homônima, mas DISTINTA e não relacionada, mesma confusão de nomes já registrada em D-222) para o mesmo problema (enviar link a destinatário externo por e-mail). Populado só por `createDocumentRequest()` avulso (novo parâmetro `CreateDocumentRequestInput.recipientEmail?`, incluído no `payloadHash` de idempotência — replay com e-mail diferente é `ConflictError`, nunca sobrescrita silenciosa). **Pendência de produto nomeada, não resolvida aqui** (via mais conservadora/reversível escolhida deliberadamente): o caminho `DocumentRequestSeries`/`materializeAttempt` (recorrência) não carrega contato de destinatário no nível da série — um `DocumentRequest` materializado por recorrência continua sem `recipientEmail`, então o worker de entrega o pula (skip terminal, não é erro). Fechar isso exigiria decisão de produto (adicionar `recipientEmail` a `DocumentRequestSeries`?) fora do escopo desta tarefa. **(2) Worker de entrega real** (`src/workers/guest-credential-delivery/deliver.ts` + handler `src/runtime/aws/handlers/guest-credential-delivery-handler.ts`): consome a tabela dedicada `guest-credential-delivery` via DynamoDB Streams + Event Source Mapping NOVO (filtro `eventName=INSERT`, D-226 decisão central item 5, `maximum_retry_attempts=3`, `destination_config.on_failure` para fila nova `GuestCredentialDeliveryFailures` — carrega só metadados do lote, nunca o payload/token). Re-lê o `DocumentRequest` autoritativo (nunca confia em nada além das chaves do registro do Stream); pula (terminal, não repete) se `recipientEmail` ausente ou se `issuanceGeneration` está obsoleta (reemissão já superou esta entrega); idempotência sob redelivery do Stream via `GuestCredentialDeliveryMarkerStore.claim()` — Put condicional (`attribute_not_exists`) de uma linha marcador na MESMA tabela dedicada, sob a MESMA `PK` do registro de entrega, antes de enviar (trade-off nomeado: uma falha entre claim e envio deixa o par permanentemente reivindicado-mas-não-enviado, mesma classe de best-effort já aceita em `document-request-service.ts`). Monta o link exatamente como `document-chasing-dispatch/dispatch.ts` já faz (`${guestUploadBaseUrl}?token=${token}`, mesmo placeholder documentado `https://app.example.invalid/...`, D-047) usando o `token` bruto já armazenado em `GuestCredentialDeliveryRecord` — nunca deriva/reconstrói. Envia via `EmailProviderAdapter`/`SesEmailAdapter` já existente (mesmo mecanismo de `EmailDeliveryWorker`/`document-chasing-dispatch`, nenhum client SES novo), novo template `guest-credential-delivery-invite` em `email-templates.ts`. Roda na Lambda de RECURSO (não a guest com pepper) — só lê `DocumentRequest` (mesma `tenant_facing_read_write_policy_json` de todo outro consumidor do módulo) e envia e-mail; nunca toca o pepper D-146. **G-V3 real (não apenas unitário)**: `test/unit/document-archive/guest-credential-issuance-to-delivery-e2e.test.ts` prova o ciclo completo `createDocumentRequest` (com `recipientEmail`) → `DocumentRequestCredentialIssuanceService.handle` (D-226/D-227) → `deliverGuestCredential` (este worker) → o link do e-mail autentica de fato via `GuestDocumentAccessService.resolveCredential()` (o mesmo método que o handler HTTP guest real chama) — token bruto nunca aparece fora da tabela dedicada e do corpo do e-mail (`guest-credential-delivery-worker.test.ts` cobre idempotência sob duplicata de Streams, ausência de destinatário, geração obsolecida, request inexistente, falha de envio). **Resultado prático: pela primeira vez, um guest pode de fato receber e usar um link funcional** — mas SÓ para `DocumentRequest`s criados pelo caminho avulso COM `recipientEmail` explícito; o caminho de recorrência continua sem destinatário modelado (pendência nomeada acima), então o item 9 do roadmap P0 fecha para o caminho avulso mas não integralmente — ver `NEXT_SESSION_PROMPT.md` para o texto exato do que falta. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2556/2556** (233 arquivos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. **Nota de numeração**: D-228 estava reservado por esta sessão desde o início do trabalho; D-229 (WhatsApp fatia 2/5, sessão paralela concorrente) já registra a colisão evitada. | Nível 3-4 (extensão aditiva seguindo precedente já convergido, sem invenção de política nova; avaliado contra `change-risk-scale.md` antes de proceder) | — | — | **IMPLEMENTADO (caminho avulso fecha ponta a ponta; caminho de recorrência permanece sem `recipientEmail`, pendência nomeada)** | `src/modules/document-archive/{domain/document-request.ts,application/document-archive-service.ts,ports/guest-credential-delivery-marker-store.ts,persistence/dynamodb-guest-credential-delivery-marker-store.ts}`, `src/workers/guest-credential-delivery/deliver.ts`, `src/runtime/aws/handlers/guest-credential-delivery-handler.ts`, `src/runtime/aws/composition/document-archive.ts`, `src/modules/notification/providers/email-templates.ts`, `infra/main.tf`, `scripts/build-lambdas.ts`, `test/unit/document-archive/{guest-credential-delivery-worker.test.ts,guest-credential-issuance-to-delivery-e2e.test.ts,document-request-avulso-and-reopen.test.ts}` | 2026-09-07 |
260:| D-229 | **WhatsApp Operacional fatia 2/5 (D-197, `whatsapp-channel-scoping/estado-final-consolidado.md` D-2/D-9) — IMPLEMENTADA.** Implementação direta de design já `APPROVED` (nível 3-4, sem decisão nova). `WhatsAppProviderAdapter` (`src/modules/notification/ports/whatsapp-provider.ts`) espelha `EmailProviderAdapter` campo a campo (`send()`→`SendResult`/`SendError{kind: CONCLUSIVE_RETRYABLE\|CONCLUSIVE_TERMINAL\|AMBIGUOUS}`). `WhatsAppCloudApiAdapter` (`providers/whatsapp-cloud-api-adapter.ts`) implementa a chamada HTTP real (Node 24's `fetch` global, sem dependência nova) contra a Graph API do Meta, com mapeamento real dos códigos de erro documentados (190/OAuthException→TERMINAL, 131056/131057/429→RETRYABLE, 131026/131047/132000/132001→TERMINAL, 5xx→RETRYABLE, resposta não-JSON/network error/timeout→AMBIGUOUS nunca assumido seguro). **Nota de credenciais (fronteira explícita desta fatia)**: `WhatsAppCloudApiConfig` já é a forma final que vai receber a config de Secrets Manager na fatia 3/5 (D-10) — hoje a fonte é só uma env var placeholder (`WHATSAPP_ACCESS_TOKEN`, default `""`), documentado no cabeçalho do adapter e em `buildWhatsAppDeliveryDeps`; a MUDANÇA de fatia 3/5 é só a fonte da credencial, nunca a forma do adapter. `WhatsAppDeliveryWorker`/`processWhatsAppDelivery` (`application/whatsapp-delivery-workflow.ts`) reaproveita literalmente `decideSendAction`/`nextStatusAfterSendAttempt` de `email-delivery.ts` (já eram genéricos, nunca específicos de SES, apesar do nome do arquivo) via `whatsapp-delivery.ts` — mesma disciplina de lease/OCC/fence W3-07 (D-067) do e-mail, mesmo tratamento AMBIGUOUS→UNKNOWN nunca retry automático. Resolução de destinatário: `ResolvedRecipient.phoneE164` novo (mesma leitura de `GlobalUser` que já produz `email`) + `resolveRecipientPhone` (composition) que SÓ retorna o telefone quando um `WhatsAppOptIn` existe para o telefone ATUAL (D-5 — troca de número invalida opt-in anterior por construção, nunca checagem explícita). Fila+DLQ dedicada `whatsapp-deliver-queue` (módulo genérico `sqs-worker-queue`, `maxReceiveCount=5` fixo do módulo) — NUNCA reaproveita `email-deliver-queue` (ADR-0008). Destino de outbox novo `SQS_NOTIFICATION_WHATSAPP_V1`, schema `notification.whatsapp-deliver.v1` (mesma forma de `notification-email-deliver.v1.json`, registrado em `schema-validator.ts`), `buildWhatsAppOutboxRecord` (`application/whatsapp-outbox.ts`) análogo a `buildEmailOutboxRecord` mas em arquivo próprio (o router NÃO o chama ainda — fatia 5/5). `notification-whatsapp-outbox-relay-handler.ts` reaproveita literalmente `processStreamRecords`/`relayStreamRecord` (já genéricos por `destination`, sem duplicação). `outbox-sweeper-handler.ts` ganha o destino novo (11º) na mesma role privilegiada compartilhada, mesmo padrão de toda extensão anterior. **DELIBERADAMENTE NÃO TOCADO**: `notification-router.ts`'s `SUPPORTED_CHANNELS` (continua só `EMAIL` — fatia 5/5); webhook handler/Secrets Manager (D-7/D-10, fatia 3/5); quota de 24h (D-8, fatia 4/5); kill switch/RBAC final (fatia 5/5). Mecanismo alcançável hoje só por chamada direta/teste, nunca pelo fluxo real de notificação. **Achado de processo (colisão de numeração)**: D-228 já estava em uso concorrente (não isolado por worktree) por outra sessão implementando o worker de entrega de credencial de guest (item 9 do roadmap P0) no mesmo instante — confirmado contra `decisions-log.md` committed antes de escrever esta linha; toda referência interna desta fatia usa D-229, nunca D-228. G-V3: mapeamento adversarial completo do adapter (13 testes — 2xx sem message id, cada família de código de erro Graph API, HTTP 429/500 sem corpo parseável, corpo não-JSON, `fetch()` lançando, forma exata do payload HTTP incluindo `biz_opaque_callback_data`); idempotência/estrutura do worker (12 testes, mesmo roteiro adversarial de `email-delivery-workflow.test.ts` — lease ativo/expirado, staleness→REPLACEMENT, fence W3-07 DELETING, falha conclusiva vs. ambígua); `buildWhatsAppOutboxRecord` validado contra o próprio schema registrado. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2548/2548** (231 arquivos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. | Nível 3-4 (implementação de design já `APPROVED` D-197, sem decisão nova) | — | — | **IMPLEMENTADO (fatia 2/5) — fatia 3/5 (webhook D-7 + Secrets Manager D-10) é a próxima ação real** | `src/modules/notification/{ports/whatsapp-provider.ts,providers/whatsapp-cloud-api-adapter.ts,application/whatsapp-delivery.ts,application/whatsapp-delivery-workflow.ts,application/whatsapp-outbox.ts,ports/recipient-resolver.ts,persistence/dynamodb-recipient-resolver.ts}`, `src/runtime/aws/handlers/{whatsapp-delivery-handler.ts,whatsapp-outbox-relay-handler.ts,outbox-sweeper-handler.ts}`, `src/runtime/aws/composition/notification.ts`, `src/shared/outbox/outbox.ts`, `src/shared/contracts/schema-validator.ts`, `schemas/queues/notification-whatsapp-deliver.v1.json`, `infra/main.tf`, `infra/variables.tf`, `infra/outputs.tf`, `infra/tests/stack.tftest.hcl`, `scripts/build-lambdas.ts`, `test/unit/notification/{whatsapp-cloud-api-adapter.test.ts,whatsapp-delivery.test.ts,whatsapp-delivery-workflow.test.ts,whatsapp-outbox.test.ts}` | 2026-09-07 |
261:| D-230 | **Fecha por inteiro o item 9 do roadmap P0 (D-222/D-226/D-227/D-228): `DocumentRequestSeries` (caminho de recorrência) ganha `recipientEmail`, a pendência nomeada explicitamente em D-228.** `APPROVED` via protocolo Claude↔Codex completo (`AGENTS.md` §4, 3 rodadas reais `codex exec`, nota cega): 7,6 → 8,6 → 9,2/10 (Claude 9,2/10, Codex 9,2/10, ambos ≥9,0 sem arredondar). E-014 `NÃO` (fronteira interna, mesmo problema já resolvido 2x no próprio código-base — módulo `subject` e D-228 — nenhum padrão de mercado a pesquisar). **Reclassificação de risco em rodada** (achado real do Codex, aceito): a proposta inicial (Rodada 1) classificou Nível 3 (campo aditivo, precedente D-228); o Codex apontou, corretamente, que a Rodada 2 introduz uma AÇÃO nova (`updateSeriesRecipient`, mutação/remoção, rota HTTP + schema novos) e que a régua manda tratar como o nível mais alto na dúvida — reclassificado para **Nível 5**, protocolo completo cumprido antes da implementação (não decidido solo, ao contrário do que D-228/D-227 haviam feito para gaps semelhantes — mudança de processo desta sessão pedida explicitamente pelo dono do projeto). **Decisão final**: (1) `DocumentRequestSeries.recipientEmail?: string` (`document-request-series.ts`) + `CreateDocumentRequestSeriesInput.recipientEmail?`, fornecido pelo humano na criação da série, trimmed (nunca lower-cased — alinhado ao caminho avulso de D-228, que também não normaliza caixa); (2) `buildMaterializeAttemptEntries` copia `series.recipientEmail` para CADA `DocumentRequest` materializado — snapshot imutável por ciclo, cobre os dois call sites que já passam por este builder (`materializeAttempt` interativo E o worker periódico `document-request-recurrence/materializer.ts`); `buildDocumentRequestCreatedOutboxEntry` confirmado por leitura como NÃO precisando de nenhuma mudança (só emite chaves+`issuanceGeneration`, o consumidor sempre relê o `DocumentRequest` autoritativo); (3) `DocumentRequestRecurrenceService.updateSeriesRecipient()` novo — reaproveita a action `docarchive:series-update` já existente (antes só usada por `advanceCycle`, sem rota HTTP própria, sem colisão); `recipientEmail: null` mapeia para `REMOVE` no DynamoDB (nunca um `recipientEmail: null` persistido — o tipo do domínio continua `string | undefined`); rejeita com `ConflictError` uma série `CANCELLED` (nunca mais materializa ciclo, mutar seu destinatário não teria efeito observável); mesmo padrão OCC de `cancelSeries`. (4) Rota nova `POST /document-archive/series/{subjectId}/{seriesId}/recipient` (handler `handleUpdateSeriesRecipient`, schema `docarchive-series-update-recipient-request.v1.json`, entrada wireada em `infra/modules/api-gateway/main.tf` e `proxy-allowlist.ts`); `docarchive-series-create-request.v1.json` ganha `recipientEmail` opcional (`format: email`, `maxLength: 254`). PII: `recipientEmail` É dado pessoal de terceiro (correção de uma formulação errada da Rodada 1, apontada pelo Codex) — a decisão de não mascarar em `getSeries`/`listSeries` segue o mesmo precedente de autorização já aceito em D-228 (tenant que forneceu o dado para o próprio fluxo dele), não por ausência de sensibilidade. G-V3 real: `guest-credential-issuance-to-delivery-e2e.test.ts` estendido com 2 cenários novos — série COM `recipientEmail` → `materializeAttempt` → `DocumentRequestCredentialIssuanceService` → `deliverGuestCredential` → `GuestDocumentAccessService.resolveCredential()` aceita o link de verdade (mesmo padrão do cenário avulso já existente); série SEM `recipientEmail` continua com skip terminal (`SKIPPED_NO_RECIPIENT_EMAIL`, não regressão). Mais 17 testes unitários novos em `document-request-recurrence.test.ts` (persistência/trim, cópia pelo builder, snapshot entre ciclos, `updateSeriesRecipient` troca/remoção/OCC/série cancelada/RBAC, exposição em `getSeries`/`listSeries`) e 1 em `document-request-recurrence-materializer.test.ts` (prova que o worker PERIÓDICO, não só o interativo, copia o campo). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2572/2572** (233 arquivos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. Evidência completa das 3 rodadas em `docs/architecture/reviews/document-request-series-recipient-scoping/` (`round1-claude.md`, `round1-codex-critique.md`, `round2-claude.md`, `round2-codex-critique.md`, `round3-claude.md`, `round3-codex-critique.md`, `estado-final-consolidado.md`). | Nível 5 (`change-risk-scale.md` — muda contrato HTTP + introduz ação/mutação nova; reclassificado de Nível 3 durante o protocolo, na dúvida trata-se como o nível mais alto) | 9,2 | 9,2 | **IMPLEMENTADO — item 9 do roadmap P0 FECHADO POR INTEIRO (caminho avulso desde D-228 + caminho de recorrência agora): tanto `createDocumentRequest()` quanto `DocumentRequestSeries`/`materializeAttempt` podem entregar um link de guest utilizável de verdade quando um `recipientEmail` é fornecido; sem ele, ambos os caminhos continuam fazendo skip terminal, nunca erro** | `src/modules/document-archive/{domain/document-request-series.ts,application/document-request-recurrence-service.ts,http/document-archive-handlers.ts}`, `src/runtime/aws/handlers/document-archive-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/contracts/schema-validator.ts`, `schemas/api/{docarchive-series-create-request.v1.json,docarchive-series-update-recipient-request.v1.json}`, `infra/modules/api-gateway/main.tf`, `test/unit/document-archive/{document-request-recurrence.test.ts,document-request-recurrence-materializer.test.ts,guest-credential-issuance-to-delivery-e2e.test.ts}`, `docs/architecture/reviews/document-request-series-recipient-scoping/` | 2026-09-07 |
262:| D-231 | **WhatsApp Operacional fatia 3/5 (D-197, D-7 webhook handler + D-10 Secrets Manager/kill switch) — IMPLEMENTADA.** Base era implementação direta de design já `APPROVED` (nível 3-4), mas dois gaps reais de modelagem foram achados durante a implementação e resolvidos via protocolo Claude<->Codex completo (`AGENTS.md` §4, nota cega, `codex exec`) ANTES de codificar, por instrução explícita do dono do projeto para esta frente: **(1) escopo de purge do `WebhookInbox` WhatsApp** (4 rodadas, notas finais 9,2 Claude / 9,2 Codex - `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/estado-final-consolidado.md`): `WebhookInbox` WhatsApp e account-scoped (`PK=WEBHOOK#WHATSAPP#<wabaId>`), nunca tenant-scoped, mas `transient-purge/purge.ts` fenceia todo delete com `TenantLifecycleRecord.status=ACTIVE` - sem um tenant real (o caso normal para qualquer wamid nao correlacionado), a linha nunca purgaria, quebrando a retencao de 7 dias (`privacy-lgpd.md` §4) silenciosamente. Resolvido com `purgeScope: "TENANT"|"ACCOUNT"` imutavel escrito na criacao (nunca inferido do GSI8, que e discovery-only/eventualmente consistente), `accountScopedTransientPurgeGsi8Keys()` novo (mesmo `GSI8PK`, discriminador `#ACCOUNT#<accountId>#` em vez de `#TENANT#<tenantId>#`), uniao discriminada em `candidate-source.ts`/`dynamodb-candidate-source.ts`, e dois ramos de transacao em `purge.ts` (`TENANT`: `ConditionCheck` de tenant ativo + `Delete`, backoff/DLQ por tenant; `ACCOUNT`: so `Delete` condicionado por `version`, `CancellationReasons[0]` != `ConditionalCheckFailed` sempre relancado, nunca backoff/quarentena por tenant). **(2) `buildVersionedUpdate()` nao serve a uma entidade sem tenant** (3 rodadas, notas finais 9,3 Claude / 9,2 Codex): o unico builder de update condicional versionado do repositorio (`shared/dynamodb/occ.ts`, usado por ~40 call sites) embute incondicionalmente `#tenantId = :tenantId` no `ConditionExpression` - `WebhookInbox` WhatsApp nao tem (nem pode fabricar) esse atributo sem reabrir a ambiguidade ja fechada no gap (1). Resolvido com um nucleo privado `buildScopedVersionedUpdate()` compartilhado (unica implementacao de incremento de version/updatedAt/SET/REMOVE/extraConditions/deteccao de colisao) e dois builders publicos: `buildVersionedUpdate()` inalterado byte-a-byte (mesma assinatura/expressao para todo call site existente) e `buildAccountScopedVersionedUpdate()` novo, fence `#accountId = :accountId`. **Decisao final da fatia** (D-7/D-10): `whatsapp-webhook-processor.ts` (puro) implementa `verifyMetaSignature()` (HMAC-SHA256 sobre o raw body, `X-Hub-Signature-256`, `timingSafeEqual`, mesma disciplina de `bff/domain/csrf.ts`), `verifyMetaWebhookChallenge()` (handshake `GET` de registro do webhook, `hub.mode`/`hub.verify_token`/`hub.challenge`, conforme documentacao real da Meta Cloud API verificada 2026-09-07), `parseBizOpaqueCallbackData()`/`parseCloudApiWebhookEvents()` (parsing do payload real de status), e `decideWhatsAppCallbackApplication()` (precedencia monotonica SENT->ACCEPTED/DELIVERED/READ->DELIVERED conflacionado/FAILED->FAILED_TERMINAL, mesma disciplina de `ses-callback-processor.ts`). `whatsapp-webhook-workflow.ts` espelha `ses-callback-workflow.ts` (grava a linha create-once IMEDIATAMENTE apos assinatura valida, ANTES de correlacionar; `markInboxUnmatched`-equivalente na MESMA linha se a correlacao falhar) com `purgeScope: "ACCOUNT"` sempre, `accountId`/`providerAccountId` = `wabaId`, correlacao bem-sucedida so anota `correlatedTenantId`/`attemptId`/`intentId` como metadado (nunca muda o dono do purge). `whatsapp-webhook-handler.ts` (novo, API Gateway HTTP API, rota PUBLICA `authorization_type=NONE` - segunda rota publica do projeto depois de `guest_documents`, D-037): `GET` responde o handshake; `POST` verifica assinatura sobre o raw body ANTES de qualquer `JSON.parse`/persistencia (G-V3), sempre retorna 200 a Meta apos a verificacao (nunca deixa uma falha de processamento parecer uma falha de endpoint que dispararia retry de Meta), checa o kill switch so para decidir se persiste/age. `secrets-manager-whatsapp-config.ts` novo (`loadWhatsAppSecrets`) - um secret JSON so (accessToken/appSecret/phoneNumberId/wabaId/verifyToken, mesma razao de nao fragmentar em 5: emitidos juntos pela Meta, nada rotaciona independente), nunca logado, cacheado por cold start. `whatsapp-activation.ts` novo - kill switch de 2 flags em ordem obrigatoria (`WHATSAPP` + `WHATSAPP_DELIVERY_WORKER_ENABLED`, mesmo mecanismo de D-193 slice 8/9/`document-archive-activation.ts`), `isWhatsAppDeliveryWorkerEnabled()` exige AMBOS. `whatsapp-delivery-handler.ts` (fatia 2/5) migrado de env vars placeholder para Secrets Manager + kill switch (batch inteiro dropado sem side-effect, nunca marcado como falha de item, quando o kill switch esta off). Terraform: `aws_secretsmanager_secret`/`_version` (placeholder JSON, `lifecycle.ignore_changes` protege um valor real setado fora de banda) + politica IAM `secretsmanager:GetSecretValue` escopada SO ao ARN do secret, anexada so aos 2 Lambdas (`whatsapp_delivery`, `whatsapp_webhook_handler` novo); rota publica nova `modules/api-gateway` (`whatsapp_webhook` integration/route/lambda-permission, mesmo throttling restritivo de `guest_documents`); `whatsapp_access_token`/`whatsapp_phone_number_id` (placeholders de fatia 2/5) removidos de `variables.tf`. **Nao wireado nesta fatia** (fatia 4-5): `notification-router.ts`'s `SUPPORTED_CHANNELS`, quota de 24h (D-8). G-V3 real: assinatura invalida sempre rejeita antes de qualquer gravacao (7 testes adversariais incl. header ausente/malformado/tamanho errado); replay do mesmo wamid e idempotente (`DUPLICATE_INBOX`); correlacao falha vira `UNMATCHED` na MESMA linha; secrets nunca logados/persistidos fora do `SecretsManager`; purge de conta nunca precisa de tenant. 30+9+4 testes novos (`whatsapp-webhook-processor.test.ts`/`whatsapp-webhook-workflow.test.ts`/`whatsapp-activation.test.ts`), 5 novos em `test/unit/workers/transient-purge.test.ts` (ramo ACCOUNT: purge sem tenant, sem cross-talk com fence de tenant, colisao de versao, cancelamento nao reconhecido relancado), 3 novos em `test/unit/occ.test.ts` (`buildAccountScopedVersionedUpdate`). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suite completa **2625/2625** (236 arquivos); `terraform fmt`/`validate` limpos, `terraform test` do stack principal 23/23 (`AWS_PROFILE=claude-dev`) - modulo `api-gateway` isolado tem 3 assercoes de contagem de rota pre-existentes e nao relacionadas falhando (10->13 items, 21->22 subjects, 3->5 imports; achado incidental de outra sessao concorrente sem isolamento de worktree, confirmado via `git diff --stat` que esta fatia so ADICIONA linhas a esse arquivo - nao investigado/corrigido aqui, fora de escopo). **PR/merge/CI/verificacao ao vivo em `dev` NAO executados nesta sessao** (orcamento de tempo insuficiente para um `terraform plan`/`apply` real supervisionado depois de tocar infra que ja esta deployada - `module.whatsapp_delivery` muda de shape de env var - dado que e seguro adiar, ver `NEXT_SESSION_PROMPT.md`). | Nivel 5-6 para os 2 gaps achados (modelagem de dado/seguranca que atravessa um invariante transversal do repositorio - GSI8/tenant-fencing e o unico builder OCC condicional do sistema); Nivel 3-4 para o resto (implementacao direta de D-197/D-7/D-10 ja `APPROVED`) | 9,0 (gap 1) / 9,3 (gap 2) | 9,2 (gap 1) / 9,2 (gap 2) | **IMPLEMENTADO (fatia 3/5) - fatia 4/5 (quota D-8 de 24h + IAM dedicada) e a proxima acao real; terraform desta fatia validado (`fmt`/`validate`/`test` do stack principal) mas nunca `plan`/`apply`'d contra AWS real nem mergeado a `main`** | `src/shared/dynamodb/occ.ts`, `src/shared/transient-purge-gsi8.ts`, `src/workers/transient-purge/{purge.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/notification/application/{whatsapp-webhook-processor.ts,whatsapp-webhook-workflow.ts,whatsapp-activation.ts,ses-callback-workflow.ts}`, `src/modules/notification/persistence/secrets-manager-whatsapp-config.ts`, `src/modules/extraction/{ports/feature-flags-reader.ts,persistence/appconfig-feature-flags-reader.ts}`, `src/runtime/aws/handlers/{whatsapp-webhook-handler.ts,whatsapp-delivery-handler.ts}`, `src/runtime/aws/composition/notification.ts`, `schemas/api/webhook-inbox.v1.json`, `infra/main.tf`, `infra/variables.tf`, `infra/modules/api-gateway/{main.tf,variables.tf,tests/api_gateway.tftest.hcl}`, `infra/modules/feature-flags/{main.tf,variables.tf}`, `scripts/build-lambdas.ts`, `package.json`, `test/unit/{occ.test.ts,workers/transient-purge.test.ts,workers/transient-purge-fakes.ts,notification/whatsapp-webhook-processor.test.ts,notification/whatsapp-webhook-workflow.test.ts,notification/whatsapp-activation.test.ts,notification/in-memory-store.ts}`, `test/contract/schemas.test.ts`, `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/` | 2026-09-08 |
7:export type Action =
100:  | "organization:cancel-close"
231:const ACTION_ROLES: Record<Action, ReadonlySet<Role>> = {
276:  "organization:cancel-close": OWNER_ROLES,
342:export function authorize(input: AuthorizationInput): void {
378:   * `HELD_FOR_RECOVERY` tenant in the first place (see the `organization:cancel-close` doc
403:export function authorizeCancelClosure(input: CancelClosureAuthorizationInput): void {
404:  const action: Action = "organization:cancel-close";

codex
A matriz cobre as ações novas no nível de tipo, mas isso não prova que cada caminho de execução a invoque nem que o recurso passado seja autoritativo. Agora estou cruzando handlers/rotas e serviços de Document Archive, relatórios, guest/dossier e WhatsApp com os call sites efetivos de `authorize()` e com as leituras por tenant.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"authorize\\(|authorizeCancelClosure\\(\" src/modules/document-archive src/modules/notification src/modules/identity src/runtime/aws/handlers | Sort-Object; rg -n \"handle[A-Z]|routeKey|rawPath|method:|path:\" src/modules/document-archive/http src/runtime/aws/handlers/document-archive-handler.ts src/modules/bff/domain/proxy-allowlist.ts; rg -n \"docarchive:|reports:subscription|activity:read\" src/modules src/runtime | Sort-Object" in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 1817ms:
src/modules/document-archive\application\document-archive-service.ts:1026:    authorize({ context: ctx, action: "docarchive:requirement-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1074:    authorize({ context: ctx, action: "docarchive:request-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1215:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1226:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1242:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1264:    authorize({ context: ctx, action: "docarchive:dossier-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1308:    authorize({ context: ctx, action: "docarchive:dossier-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1357:   * `RequestContext`/`authorize()` here on purpose, same posture `findRequirementsByEvidenceVersion`/
src/modules/document-archive\application\document-archive-service.ts:1386:    authorize({ context: ctx, action: "docarchive:dossier-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1409:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1462:    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1560:    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1610:    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1646:   * first one a page happens to return). No `RequestContext`/`authorize()` here on purpose — this
src/modules/document-archive\application\document-archive-service.ts:1670:    authorize({ context: ctx, action: "docarchive:requirement-delete", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1707:    authorize({ context: ctx, action: "docarchive:documenttype-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1754:    authorize({ context: ctx, action: "docarchive:documenttype-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1772:    authorize({ context: ctx, action: "docarchive:documenttype-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1792:    authorize({ context: ctx, action: "docarchive:documenttype-rename", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1881:    authorize({ context: ctx, action, resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1923:    authorize({ context: ctx, action: "docarchive:documenttype-metadata-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:1990:    authorize({ context: ctx, action: "docarchive:documenttype-metadata-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2102:    authorize({ context: ctx, action: "docarchive:document-metadata-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2175:    authorize({ context: ctx, action: "docarchive:requirementtemplate-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2202:    authorize({ context: ctx, action: "docarchive:requirementtemplate-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2214:    authorize({ context: ctx, action: "docarchive:requirementtemplate-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2231:    authorize({ context: ctx, action: "docarchive:requirementtemplate-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2293:    authorize({ context: ctx, action: "docarchive:requirementtemplate-duplicate", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2342:    authorize({ context: ctx, action: "docarchive:requirementtemplate-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2377:    authorize({ context: ctx, action: "docarchive:requirementtemplate-apply", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2526:    authorize({ context: ctx, action, resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:2585:  /** D-143 Decision 1/Bloqueador 6: a dedicated, named check distinct from `authorize()` —
src/modules/document-archive\application\document-archive-service.ts:491:   * `authorize()`, same posture as M6's `advanceAfterEvidence()` (only a human-initiated command
src/modules/document-archive\application\document-archive-service.ts:517:    authorize({ context: ctx, action: "docarchive:create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:539:    authorize({ context: ctx, action: "docarchive:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:550:    authorize({ context: ctx, action: "docarchive:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:563:    authorize({ context: ctx, action: "docarchive:upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:606:    authorize({ context: ctx, action: "docarchive:upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:715:    authorize({ context: ctx, action: "docarchive:upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:746:    authorize({ context: ctx, action: "docarchive:review", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:782:    authorize({ context: ctx, action: "docarchive:review", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-archive-service.ts:937:    authorize({ context: ctx, action: "docarchive:review", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:154:    authorize({ context: ctx, action: "docarchive:series-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:184:    authorize({ context: ctx, action: "docarchive:series-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:195:    authorize({ context: ctx, action: "docarchive:series-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:200:    authorize({ context: ctx, action: "docarchive:series-cancel", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:236:    authorize({ context: ctx, action: "docarchive:series-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:268:    authorize({ context: ctx, action: "docarchive:series-materialize", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\document-request-recurrence-service.ts:290:    authorize({ context: ctx, action: "docarchive:series-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/document-archive\application\guest-document-access-service.ts:222:   * `authorize()`/`RequestContext` — same token-resolution discipline as `resolveCredential()`,
src/modules/document-archive\application\guest-document-access-service.ts:252:    // directly against the store (never through that authorize()-gated method — this whole
src/modules/document-archive\application\guest-document-access-service.ts:253:    // module never calls authorize()/RequestContextResolver, see file header). Catalogs are
src/modules/document-archive\application\guest-document-access-service.ts:4: * `RequestContext`/`authorize()` — the guest is validated only by credential/session tokens,
src/modules/document-archive\domain\document-version.ts:135:/** Throws rather than returning a boolean (implementation-blueprint.md's `authorize()`
src/modules/document-archive\http\document-archive-guest-handlers.ts:4: * same posture: never touches `RequestContextResolver`/`authorize()`, only
src/modules/document-archive\http\document-archive-handlers.ts:4: * service, which internally calls authorize() -> schema validation before the service call
src/modules/identity\application\resolve-request-context.ts:175:   * instead of letting an unrecognized role fail every `authorize()` check via that matrix's
src/modules/identity\domain\authorization.ts:106:  // rejected presuming an ACL `authorize()` doesn't implement). `docarchive:review` covers
src/modules/identity\domain\authorization.ts:324: * authorize() — implementation-blueprint.md §4.3: "authorize() verifica primeiro
src/modules/identity\domain\authorization.ts:342:export function authorize(input: AuthorizationInput): void {
src/modules/identity\domain\authorization.ts:36:  // autenticado do tenant tem action própria — o convidado nunca passa por authorize()/
src/modules/identity\domain\authorization.ts:387: * deliberately NOT `authorize()`. Two reasons this cannot be the generic function:
src/modules/identity\domain\authorization.ts:388: *  1. `authorize()`'s signature requires a `RequestContext`, which requires
src/modules/identity\domain\authorization.ts:393: *     the recovery window is not necessarily the ordinary "one clean read" `authorize()` assumes
src/modules/identity\domain\authorization.ts:398: * Same fail-closed ordering discipline as `authorize()`: identity status checked before
src/modules/identity\domain\authorization.ts:400: * Throws (never returns false), same convention as `authorize()`, so callers cannot forget to
src/modules/identity\domain\authorization.ts:403:export function authorizeCancelClosure(input: CancelClosureAuthorizationInput): void {
src/modules/identity\domain\authorization.ts:95:  // `authorizeCancelClosure()` below, NOT the generic `authorize()` — the caller
src/modules/identity\domain\authorization.ts:98:  // `HELD_FOR_RECOVERY` tenant is definitionally not), so `authorize()`'s own `AuthorizationInput`
src/modules/identity\http\test-route-handler.ts:52:    authorize({ context, action: "system:ping", resource: { tenantId: context.tenant.tenantId } });
src/modules/notification\application\notification-preferences-service.ts:5: * ReminderPolicyService's shape: authorize() (action `notification:configure`, already in
src/modules/notification\application\notification-preferences-service.ts:56:    authorize({ context: ctx, action: "notification:configure", resource: { tenantId: ctx.tenant.tenantId } });
src/modules/notification\application\whatsapp-opt-in-service.ts:10: * `authorize()` uses the same action name pattern as `NotificationPreferencesService`
src/modules/notification\application\whatsapp-opt-in-service.ts:43:    authorize({ context: ctx, action: "notification:configure", resource: { tenantId: ctx.tenant.tenantId } });
src/runtime/aws/handlers\import-commit-handler.ts:25: * ImportService.requestCommit() (JWT real, roles reais do usuário) - authorize() dentro de
src/runtime/aws/handlers/document-archive-handler.ts:12:  handleAcceptVersion,
src/runtime/aws/handlers/document-archive-handler.ts:13:  handleClaimReview,
src/runtime/aws/handlers/document-archive-handler.ts:14:  handleCommitUpload,
src/runtime/aws/handlers/document-archive-handler.ts:15:  handleCreateDocument,
src/runtime/aws/handlers/document-archive-handler.ts:16:  handleGetDocument,
src/runtime/aws/handlers/document-archive-handler.ts:17:  handleListVersions,
src/runtime/aws/handlers/document-archive-handler.ts:18:  handleRejectVersion,
src/runtime/aws/handlers/document-archive-handler.ts:19:  handleReserveFiles,
src/runtime/aws/handlers/document-archive-handler.ts:20:  handleReserveUpload,
src/runtime/aws/handlers/document-archive-handler.ts:21:  handleCreateRequirement,
src/runtime/aws/handlers/document-archive-handler.ts:22:  handleGetRequirement,
src/runtime/aws/handlers/document-archive-handler.ts:23:  handleGetSubjectCompliance,
src/runtime/aws/handlers/document-archive-handler.ts:24:  handleListRequirements,
src/runtime/aws/handlers/document-archive-handler.ts:25:  handleSearchRequirements,
src/runtime/aws/handlers/document-archive-handler.ts:26:  handleUpdateRequirement,
src/runtime/aws/handlers/document-archive-handler.ts:27:  handleLinkEvidence,
src/runtime/aws/handlers/document-archive-handler.ts:28:  handleUnlinkEvidence,
src/runtime/aws/handlers/document-archive-handler.ts:29:  handleDeleteRequirement,
src/runtime/aws/handlers/document-archive-handler.ts:30:  handleCreateSeries,
src/runtime/aws/handlers/document-archive-handler.ts:31:  handleGetSeries,
src/runtime/aws/handlers/document-archive-handler.ts:32:  handleListSeries,
src/runtime/aws/handlers/document-archive-handler.ts:33:  handleCancelSeries,
src/runtime/aws/handlers/document-archive-handler.ts:34:  handleMaterializeSeriesAttempt,
src/runtime/aws/handlers/document-archive-handler.ts:35:  handleUpdateSeriesRecipient,
src/runtime/aws/handlers/document-archive-handler.ts:36:  handleCreateDocumentType,
src/runtime/aws/handlers/document-archive-handler.ts:37:  handleGetDocumentType,
src/runtime/aws/handlers/document-archive-handler.ts:38:  handleListDocumentTypes,
src/runtime/aws/handlers/document-archive-handler.ts:39:  handleRenameDocumentType,
src/runtime/aws/handlers/document-archive-handler.ts:40:  handleDeprecateDocumentType,
src/runtime/aws/handlers/document-archive-handler.ts:41:  handleReactivateDocumentType,
src/runtime/aws/handlers/document-archive-handler.ts:42:  handleCreateDocumentTypeMetadataField,
src/runtime/aws/handlers/document-archive-handler.ts:43:  handleUpdateDocumentTypeMetadataField,
src/runtime/aws/handlers/document-archive-handler.ts:44:  handleUpdateDocumentMetadataValues,
src/runtime/aws/handlers/document-archive-handler.ts:45:  handleCreateRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:46:  handleListRequirementTemplates,
src/runtime/aws/handlers/document-archive-handler.ts:47:  handleGetRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:48:  handleUpdateRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:49:  handleDuplicateRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:50:  handleArchiveRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:51:  handleUnarchiveRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:52:  handlePreviewRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:53:  handleApplyRequirementTemplate,
src/runtime/aws/handlers/document-archive-handler.ts:54:  handlePreviewDossierExport,
src/runtime/aws/handlers/document-archive-handler.ts:55:  handleConfirmDossierExport,
src/runtime/aws/handlers/document-archive-handler.ts:56:  handleDownloadDossierExport,
src/runtime/aws/handlers/document-archive-handler.ts:70:// handleDownloadDossierExport.
src/runtime/aws/handlers/document-archive-handler.ts:78:  return runWithContext({ correlationId: event.requestContext.requestId }, () => handleDocumentArchiveRoute(event));
src/runtime/aws/handlers/document-archive-handler.ts:81:async function handleDocumentArchiveRoute(event: APIGatewayProxyEventV2WithJWTAuthorizer): Promise<APIGatewayProxyStructuredResultV2> {
src/runtime/aws/handlers/document-archive-handler.ts:84:  const routeKey = event.routeKey; // e.g. "POST /document-archive/documents"
src/runtime/aws/handlers/document-archive-handler.ts:88:      switch (routeKey) {
src/runtime/aws/handlers/document-archive-handler.ts:90:          return await handleCreateDocument(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:92:          return await handleGetDocument(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:94:          return await handleListVersions(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:96:          return await handleReserveUpload(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:98:          return await handleReserveFiles(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:100:          return await handleCommitUpload(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:102:          return await handleClaimReview(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:104:          return await handleAcceptVersion(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:106:          return await handleRejectVersion(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:109:          return await handleCreateRequirement(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:113:          return await handleSearchRequirements(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:115:          return await handleListRequirements(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:119:          return await handleGetSubjectCompliance(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:121:          return await handleGetRequirement(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:123:          return await handleUpdateRequirement(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:125:          return await handleLinkEvidence(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:127:          return await handleUnlinkEvidence(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:129:          return await handleDeleteRequirement(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:134:          return await handleCreateSeries(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:136:          return await handleListSeries(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:138:          return await handleGetSeries(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:140:          return await handleCancelSeries(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:142:          return await handleMaterializeSeriesAttempt(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:145:          return await handleUpdateSeriesRecipient(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:148:          return await handleCreateDocumentType(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:150:          return await handleListDocumentTypes(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:152:          return await handleGetDocumentType(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:154:          return await handleRenameDocumentType(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:156:          return await handleDeprecateDocumentType(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:158:          return await handleReactivateDocumentType(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:161:          return await handleCreateDocumentTypeMetadataField(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:163:          return await handleUpdateDocumentTypeMetadataField(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:165:          return await handleUpdateDocumentMetadataValues(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:168:          return await handleCreateRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:170:          return await handleListRequirementTemplates(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:172:          return await handleGetRequirementTemplate(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:174:          return await handleUpdateRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:176:          return await handleDuplicateRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:178:          return await handleArchiveRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:180:          return await handleUnarchiveRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:182:          return await handlePreviewRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:184:          return await handleApplyRequirementTemplate(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:187:          return await handlePreviewDossierExport(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:189:          return await handleConfirmDossierExport(deps, { ...base, body: parseBody(event) });
src/runtime/aws/handlers/document-archive-handler.ts:192:          return await handleDownloadDossierExport(deps, base);
src/runtime/aws/handlers/document-archive-handler.ts:194:          throw new ValidationError(`Unknown route: ${routeKey}`);
src/modules/bff/domain/proxy-allowlist.ts:15:  method: string;
src/modules/bff/domain/proxy-allowlist.ts:23:  { method: "GET", pathTemplate: "/activity" },
src/modules/bff/domain/proxy-allowlist.ts:26:  { method: "GET", pathTemplate: "/dashboard/summary" },
src/modules/bff/domain/proxy-allowlist.ts:27:  { method: "POST", pathTemplate: "/items" },
src/modules/bff/domain/proxy-allowlist.ts:28:  { method: "GET", pathTemplate: "/items/dashboard" },
src/modules/bff/domain/proxy-allowlist.ts:30:  { method: "GET", pathTemplate: "/items/search" },
src/modules/bff/domain/proxy-allowlist.ts:31:  { method: "GET", pathTemplate: "/items/{itemId}" },
src/modules/bff/domain/proxy-allowlist.ts:32:  { method: "PUT", pathTemplate: "/items/{itemId}" },
src/modules/bff/domain/proxy-allowlist.ts:33:  { method: "DELETE", pathTemplate: "/items/{itemId}" },
src/modules/bff/domain/proxy-allowlist.ts:34:  { method: "POST", pathTemplate: "/items/{itemId}/archive" },
src/modules/bff/domain/proxy-allowlist.ts:38:  { method: "POST", pathTemplate: "/items/bulk-reassign" },
src/modules/bff/domain/proxy-allowlist.ts:39:  { method: "POST", pathTemplate: "/items/bulk-archive" },
src/modules/bff/domain/proxy-allowlist.ts:40:  { method: "POST", pathTemplate: "/items/{itemId}/renew" },
src/modules/bff/domain/proxy-allowlist.ts:41:  { method: "POST", pathTemplate: "/items/{itemId}/watchers/{userId}" },
src/modules/bff/domain/proxy-allowlist.ts:42:  { method: "DELETE", pathTemplate: "/items/{itemId}/watchers/{userId}" },
src/modules/bff/domain/proxy-allowlist.ts:43:  { method: "GET", pathTemplate: "/items/{itemId}/watchers" },
src/modules/bff/domain/proxy-allowlist.ts:44:  { method: "POST", pathTemplate: "/items/{itemId}/documents" },
src/modules/bff/domain/proxy-allowlist.ts:45:  { method: "GET", pathTemplate: "/items/{itemId}/documents" },
src/modules/bff/domain/proxy-allowlist.ts:46:  { method: "GET", pathTemplate: "/items/{itemId}/documents/{documentId}" },
src/modules/bff/domain/proxy-allowlist.ts:47:  { method: "DELETE", pathTemplate: "/items/{itemId}/documents/{documentId}" },
src/modules/bff/domain/proxy-allowlist.ts:48:  { method: "POST", pathTemplate: "/items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/confirm" },
src/modules/bff/domain/proxy-allowlist.ts:49:  { method: "POST", pathTemplate: "/items/{itemId}/documents/{documentId}/extractions/{runId}/fields/{fieldName}/reject" },
src/modules/bff/domain/proxy-allowlist.ts:50:  { method: "POST", pathTemplate: "/imports" },
src/modules/bff/domain/proxy-allowlist.ts:51:  { method: "GET", pathTemplate: "/imports/{jobId}" },
src/modules/bff/domain/proxy-allowlist.ts:52:  { method: "POST", pathTemplate: "/imports/{jobId}/commit" },
src/modules/bff/domain/proxy-allowlist.ts:54:  { method: "GET", pathTemplate: "/import-jobs/{jobId}/schema" },
src/modules/bff/domain/proxy-allowlist.ts:55:  { method: "POST", pathTemplate: "/import-jobs/{jobId}/mapping" },
src/modules/bff/domain/proxy-allowlist.ts:56:  { method: "GET", pathTemplate: "/notifications/preferences" },
src/modules/bff/domain/proxy-allowlist.ts:57:  { method: "PUT", pathTemplate: "/notifications/preferences" },
src/modules/bff/domain/proxy-allowlist.ts:58:  { method: "POST", pathTemplate: "/reminders/policies" },
src/modules/bff/domain/proxy-allowlist.ts:59:  { method: "GET", pathTemplate: "/reminders/policies/{policyId}" },
src/modules/bff/domain/proxy-allowlist.ts:60:  { method: "PUT", pathTemplate: "/reminders/policies/{policyId}" },
src/modules/bff/domain/proxy-allowlist.ts:61:  { method: "POST", pathTemplate: "/reminders/policies/{policyId}/disable" },
src/modules/bff/domain/proxy-allowlist.ts:62:  { method: "POST", pathTemplate: "/subjects" },
src/modules/bff/domain/proxy-allowlist.ts:63:  { method: "GET", pathTemplate: "/subjects/dashboard" },
src/modules/bff/domain/proxy-allowlist.ts:65:  { method: "GET", pathTemplate: "/subjects/search" },
src/modules/bff/domain/proxy-allowlist.ts:66:  { method: "GET", pathTemplate: "/subjects/document-request-delivery-preference" },
src/modules/bff/domain/proxy-allowlist.ts:67:  { method: "PUT", pathTemplate: "/subjects/document-request-delivery-preference" },
src/modules/bff/domain/proxy-allowlist.ts:68:  { method: "GET", pathTemplate: "/subjects/{subjectId}" },
src/modules/bff/domain/proxy-allowlist.ts:69:  { method: "PUT", pathTemplate: "/subjects/{subjectId}" },
src/modules/bff/domain/proxy-allowlist.ts:70:  { method: "DELETE", pathTemplate: "/subjects/{subjectId}" },
src/modules/bff/domain/proxy-allowlist.ts:71:  { method: "POST", pathTemplate: "/subjects/{subjectId}/archive" },
src/modules/bff/domain/proxy-allowlist.ts:72:  { method: "POST", pathTemplate: "/subjects/{subjectId}/requirements" },
src/modules/bff/domain/proxy-allowlist.ts:73:  { method: "GET", pathTemplate: "/subjects/{subjectId}/requirements" },
src/modules/bff/domain/proxy-allowlist.ts:74:  { method: "GET", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}" },
src/modules/bff/domain/proxy-allowlist.ts:75:  { method: "PUT", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}" },
src/modules/bff/domain/proxy-allowlist.ts:76:  { method: "DELETE", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}" },
src/modules/bff/domain/proxy-allowlist.ts:77:  { method: "POST", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}/link" },
src/modules/bff/domain/proxy-allowlist.ts:78:  { method: "POST", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}/unlink" },
src/modules/bff/domain/proxy-allowlist.ts:79:  { method: "GET", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}/submissions" },
src/modules/bff/domain/proxy-allowlist.ts:80:  { method: "GET", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}/submissions/{submissionId}" },
src/modules/bff/domain/proxy-allowlist.ts:81:  { method: "POST", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}/document-requests" },
src/modules/bff/domain/proxy-allowlist.ts:82:  { method: "GET", pathTemplate: "/subjects/{subjectId}/requirements/{assignmentId}/document-requests" },
src/modules/bff/domain/proxy-allowlist.ts:83:  { method: "GET", pathTemplate: "/subjects/{subjectId}/document-requests/{documentRequestId}" },
src/modules/bff/domain/proxy-allowlist.ts:84:  { method: "POST", pathTemplate: "/subjects/{subjectId}/document-requests/{documentRequestId}/revoke" },
src/modules/bff/domain/proxy-allowlist.ts:86:  { method: "POST", pathTemplate: "/organizations/members/invite" },
src/modules/bff/domain/proxy-allowlist.ts:87:  { method: "POST", pathTemplate: "/organizations/invitations/{invitationId}/revoke" },
src/modules/bff/domain/proxy-allowlist.ts:88:  { method: "GET", pathTemplate: "/organizations/members" },
src/modules/bff/domain/proxy-allowlist.ts:89:  { method: "GET", pathTemplate: "/organizations/invitations" },
src/modules/bff/domain/proxy-allowlist.ts:90:  { method: "PUT", pathTemplate: "/organizations/members/{userId}/role" },
src/modules/bff/domain/proxy-allowlist.ts:91:  { method: "DELETE", pathTemplate: "/organizations/members/{userId}" },
src/modules/bff/domain/proxy-allowlist.ts:92:  { method: "POST", pathTemplate: "/organizations/members/leave" },
src/modules/bff/domain/proxy-allowlist.ts:94:  { method: "PATCH", pathTemplate: "/organizations/settings" },
src/modules/bff/domain/proxy-allowlist.ts:98:  { method: "POST", pathTemplate: "/organizations/close" },
src/modules/bff/domain/proxy-allowlist.ts:100:  { method: "POST", pathTemplate: "/organizations/cancel-close" },
src/modules/bff/domain/proxy-allowlist.ts:104:  { method: "POST", pathTemplate: "/document-archive/documents" },
src/modules/bff/domain/proxy-allowlist.ts:105:  { method: "GET", pathTemplate: "/document-archive/documents/{documentId}" },
src/modules/bff/domain/proxy-allowlist.ts:106:  { method: "GET", pathTemplate: "/document-archive/documents/{documentId}/versions" },
src/modules/bff/domain/proxy-allowlist.ts:107:  { method: "POST", pathTemplate: "/document-archive/documents/{documentId}/versions" },
src/modules/bff/domain/proxy-allowlist.ts:110:  { method: "POST", pathTemplate: "/document-archive/documents/{documentId}/versions/{seq}/files" },
src/modules/bff/domain/proxy-allowlist.ts:111:  { method: "POST", pathTemplate: "/document-archive/documents/{documentId}/versions/{seq}/commit" },
src/modules/bff/domain/proxy-allowlist.ts:112:  { method: "POST", pathTemplate: "/document-archive/documents/{documentId}/versions/{seq}/claim" },
src/modules/bff/domain/proxy-allowlist.ts:113:  { method: "POST", pathTemplate: "/document-archive/documents/{documentId}/versions/{seq}/accept" },
src/modules/bff/domain/proxy-allowlist.ts:114:  { method: "POST", pathTemplate: "/document-archive/documents/{documentId}/versions/{seq}/reject" },
src/modules/bff/domain/proxy-allowlist.ts:116:  { method: "POST", pathTemplate: "/document-archive/requirements" },
src/modules/bff/domain/proxy-allowlist.ts:118:  { method: "GET", pathTemplate: "/document-archive/requirements/search" },
src/modules/bff/domain/proxy-allowlist.ts:119:  { method: "GET", pathTemplate: "/document-archive/requirements/{subjectId}" },
src/modules/bff/domain/proxy-allowlist.ts:123:  { method: "GET", pathTemplate: "/document-archive/requirements/{subjectId}/compliance" },
src/modules/bff/domain/proxy-allowlist.ts:124:  { method: "GET", pathTemplate: "/document-archive/requirements/{subjectId}/{requirementId}" },
src/modules/bff/domain/proxy-allowlist.ts:125:  { method: "PATCH", pathTemplate: "/document-archive/requirements/{subjectId}/{requirementId}" },
src/modules/bff/domain/proxy-allowlist.ts:126:  { method: "POST", pathTemplate: "/document-archive/requirements/{subjectId}/{requirementId}/link-evidence" },
src/modules/bff/domain/proxy-allowlist.ts:127:  { method: "POST", pathTemplate: "/document-archive/requirements/{subjectId}/{requirementId}/unlink-evidence" },
src/modules/bff/domain/proxy-allowlist.ts:128:  { method: "POST", pathTemplate: "/document-archive/requirements/{subjectId}/{requirementId}/delete" },
src/modules/bff/domain/proxy-allowlist.ts:130:  { method: "POST", pathTemplate: "/document-archive/series" },
src/modules/bff/domain/proxy-allowlist.ts:131:  { method: "GET", pathTemplate: "/document-archive/series/{subjectId}" },
src/modules/bff/domain/proxy-allowlist.ts:132:  { method: "GET", pathTemplate: "/document-archive/series/{subjectId}/{seriesId}" },
src/modules/bff/domain/proxy-allowlist.ts:133:  { method: "POST", pathTemplate: "/document-archive/series/{subjectId}/{seriesId}/cancel" },
src/modules/bff/domain/proxy-allowlist.ts:134:  { method: "POST", pathTemplate: "/document-archive/series/{subjectId}/{seriesId}/materialize" },
src/modules/bff/domain/proxy-allowlist.ts:135:  { method: "POST", pathTemplate: "/document-archive/series/{subjectId}/{seriesId}/recipient" },
src/modules/bff/domain/proxy-allowlist.ts:137:  { method: "POST", pathTemplate: "/document-archive/document-types" },
src/modules/bff/domain/proxy-allowlist.ts:138:  { method: "GET", pathTemplate: "/document-archive/document-types" },
src/modules/bff/domain/proxy-allowlist.ts:139:  { method: "GET", pathTemplate: "/document-archive/document-types/{documentTypeId}" },
src/modules/bff/domain/proxy-allowlist.ts:140:  { method: "PATCH", pathTemplate: "/document-archive/document-types/{documentTypeId}" },
src/modules/bff/domain/proxy-allowlist.ts:141:  { method: "POST", pathTemplate: "/document-archive/document-types/{documentTypeId}/deprecate" },
src/modules/bff/domain/proxy-allowlist.ts:142:  { method: "POST", pathTemplate: "/document-archive/document-types/{documentTypeId}/reactivate" },
src/modules/bff/domain/proxy-allowlist.ts:144:  { method: "POST", pathTemplate: "/document-archive/document-types/{documentTypeId}/metadata-fields" },
src/modules/bff/domain/proxy-allowlist.ts:145:  { method: "PATCH", pathTemplate: "/document-archive/document-types/{documentTypeId}/metadata-fields/{fieldId}" },
src/modules/bff/domain/proxy-allowlist.ts:146:  { method: "PATCH", pathTemplate: "/document-archive/documents/{documentId}/metadata-values" },
src/modules/bff/domain/proxy-allowlist.ts:149:  { method: "POST", pathTemplate: "/document-archive/requirement-templates" },
src/modules/bff/domain/proxy-allowlist.ts:150:  { method: "GET", pathTemplate: "/document-archive/requirement-templates" },
src/modules/bff/domain/proxy-allowlist.ts:151:  { method: "GET", pathTemplate: "/document-archive/requirement-templates/{templateId}" },
src/modules/bff/domain/proxy-allowlist.ts:152:  { method: "PATCH", pathTemplate: "/document-archive/requirement-templates/{templateId}" },
src/modules/bff/domain/proxy-allowlist.ts:153:  { method: "POST", pathTemplate: "/document-archive/requirement-templates/{templateId}/duplicate" },
src/modules/bff/domain/proxy-allowlist.ts:154:  { method: "POST", pathTemplate: "/document-archive/requirement-templates/{templateId}/archive" },
src/modules/bff/domain/proxy-allowlist.ts:155:  { method: "POST", pathTemplate: "/document-archive/requirement-templates/{templateId}/unarchive" },
src/modules/bff/domain/proxy-allowlist.ts:156:  { method: "POST", pathTemplate: "/document-archive/requirement-templates/{templateId}/preview" },
src/modules/bff/domain/proxy-allowlist.ts:157:  { method: "POST", pathTemplate: "/document-archive/requirement-templates/{templateId}/apply" },
src/modules/bff/domain/proxy-allowlist.ts:159:  { method: "POST", pathTemplate: "/document-archive/subjects/{subjectId}/dossier" },
src/modules/bff/domain/proxy-allowlist.ts:160:  { method: "POST", pathTemplate: "/document-archive/subjects/{subjectId}/dossier/{runId}/confirm" },
src/modules/bff/domain/proxy-allowlist.ts:162:  { method: "GET", pathTemplate: "/document-archive/subjects/{subjectId}/dossier/{runId}/download" },
src/modules/bff/domain/proxy-allowlist.ts:166:  { method: "POST", pathTemplate: "/reports/subscriptions" },
src/modules/bff/domain/proxy-allowlist.ts:167:  { method: "GET", pathTemplate: "/reports/subscriptions" },
src/modules/bff/domain/proxy-allowlist.ts:168:  { method: "GET", pathTemplate: "/reports/subscriptions/{subscriptionId}" },
src/modules/bff/domain/proxy-allowlist.ts:169:  { method: "POST", pathTemplate: "/reports/subscriptions/{subscriptionId}/delete" },
src/modules/bff/domain/proxy-allowlist.ts:172:  { method: "GET", pathTemplate: "/reports/subscriptions/{subscriptionId}/runs/{runId}/download" },
src/modules/bff/domain/proxy-allowlist.ts:175:function pathMatchesTemplate(path: string, template: string): boolean {
src/modules/bff/domain/proxy-allowlist.ts:184:export function matchAllowlistedRoute(method: string, path: string): AllowlistedRoute | undefined {
src/modules/document-archive/http\document-archive-handlers.ts:201:export async function handleCreateDocument(deps: DocumentArchiveHttpDeps, req: HttpRequest<CreateDocumentInput>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:211:export async function handleGetDocument(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:220:export async function handleListVersions(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:229:export async function handleReserveUpload(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ origin: DocumentVersionOrigin }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:240:export async function handleReserveFiles(
src/modules/document-archive/http\document-archive-handlers.ts:255:export async function handleCommitUpload(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:267:export async function handleClaimReview(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:279:export async function handleAcceptVersion(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number; clientRequestToken: string }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:291:export async function handleRejectVersion(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number; reason: RejectionReason }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:305:export async function handleCreateRequirement(deps: DocumentArchiveHttpDeps, req: HttpRequest<CreateRequirementInput>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:315:export async function handleGetRequirement(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:325:export async function handleListRequirements(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:339: * routeKey comment) but the `PROXY_ALLOWLIST` array in `proxy-allowlist.ts` matches by
src/modules/document-archive/http\document-archive-handlers.ts:341:export async function handleGetSubjectCompliance(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:357:export async function handleSearchRequirements(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:394:export async function handleUpdateRequirement(deps: DocumentArchiveHttpDeps, req: HttpRequest<UpdateRequirementInput & { expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:407:export async function handleLinkEvidence(
src/modules/document-archive/http\document-archive-handlers.ts:422:export async function handleUnlinkEvidence(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:434:export async function handleDeleteRequirement(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:450:export async function handleCreateSeries(deps: DocumentArchiveHttpDeps, req: HttpRequest<CreateDocumentRequestSeriesInput>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:460:export async function handleGetSeries(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:470:export async function handleListSeries(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:479:export async function handleCancelSeries(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:494:export async function handleUpdateSeriesRecipient(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ recipientEmail: string | null; expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:506:export async function handleMaterializeSeriesAttempt(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:520:export async function handleCreateDocumentType(deps: DocumentArchiveHttpDeps, req: HttpRequest<CreateDocumentTypeInput>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:530:export async function handleGetDocumentType(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:539:export async function handleListDocumentTypes(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:548:export async function handleRenameDocumentType(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number; displayName: string }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:559:export async function handleDeprecateDocumentType(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:570:export async function handleReactivateDocumentType(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:585:export async function handleCreateDocumentTypeMetadataField(
src/modules/document-archive/http\document-archive-handlers.ts:600:export async function handleUpdateDocumentTypeMetadataField(
src/modules/document-archive/http\document-archive-handlers.ts:618:export async function handleUpdateDocumentMetadataValues(
src/modules/document-archive/http\document-archive-handlers.ts:651:export async function handleCreateRequirementTemplate(deps: DocumentArchiveHttpDeps, req: HttpRequest<CreateRequirementTemplateInput>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:662:export async function handleGetRequirementTemplate(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:671:export async function handleListRequirementTemplates(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:680:export async function handleUpdateRequirementTemplate(
src/modules/document-archive/http\document-archive-handlers.ts:696:export async function handleDuplicateRequirementTemplate(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ displayName: string }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:708:export async function handleArchiveRequirementTemplate(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:720:export async function handleUnarchiveRequirementTemplate(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ expectedVersion: number }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:735:export async function handlePreviewRequirementTemplate(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ subjectId: string }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:746:export async function handleApplyRequirementTemplate(
src/modules/document-archive/http\document-archive-handlers.ts:770:export async function handlePreviewDossierExport(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:779:export async function handleConfirmDossierExport(deps: DocumentArchiveHttpDeps, req: HttpRequest<{ scopeHash: string }>): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:805:export async function handleDownloadDossierExport(deps: DocumentArchiveHttpDeps, req: HttpRequest): Promise<HttpResponse> {
src/modules/document-archive/http\document-archive-handlers.ts:808:      throw new Error("handleDownloadDossierExport requires dossierExportStore to be wired.");
src/modules/document-archive/http\document-archive-guest-handlers.ts:114:export async function handleGetGuestRequest(deps: GuestArchiveHttpDeps, req: GuestArchiveHttpRequest): Promise<GuestArchiveHttpResponse> {
src/modules/document-archive/http\document-archive-guest-handlers.ts:135:export async function handleStartGuestSession(deps: GuestArchiveHttpDeps, req: GuestArchiveHttpRequest): Promise<GuestArchiveHttpResponse> {
src/modules/document-archive/http\document-archive-guest-handlers.ts:158: * response as `handleGetGuestRequest`. */
src/modules/document-archive/http\document-archive-guest-handlers.ts:159:export async function handleListGuestDocumentTypes(deps: GuestArchiveHttpDeps, req: GuestArchiveHttpRequest): Promise<GuestArchiveHttpResponse> {
src/modules/document-archive/http\document-archive-guest-handlers.ts:171:export async function handleSubmitEvidence(deps: GuestArchiveHttpDeps, req: GuestArchiveHttpRequest<SubmitEvidenceInput>): Promise<GuestArchiveHttpResponse> {
src/modules\activity\application\activity-service.ts:112:    authorize({ context: ctx, action: "activity:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\bff\domain\proxy-allowlist.ts:22:  // activity:read RBAC (ADMIN/OWNER) enforced by ActivityService, not by this allowlist.
src/modules\dashboard\application\dashboard-service.ts:89:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1026:    authorize({ context: ctx, action: "docarchive:requirement-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1074:    authorize({ context: ctx, action: "docarchive:request-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1215:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1226:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1242:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1264:    authorize({ context: ctx, action: "docarchive:dossier-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1308:    authorize({ context: ctx, action: "docarchive:dossier-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1360:   * `docarchive:dossier-export` at confirm time). Re-reads the Subject displayName and EVERY
src/modules\document-archive\application\document-archive-service.ts:1386:    authorize({ context: ctx, action: "docarchive:dossier-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1409:    authorize({ context: ctx, action: "docarchive:requirement-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1462:    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1560:    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1610:    authorize({ context: ctx, action: "docarchive:requirement-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1670:    authorize({ context: ctx, action: "docarchive:requirement-delete", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1707:    authorize({ context: ctx, action: "docarchive:documenttype-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1754:    authorize({ context: ctx, action: "docarchive:documenttype-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1772:    authorize({ context: ctx, action: "docarchive:documenttype-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1792:    authorize({ context: ctx, action: "docarchive:documenttype-rename", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1866:    return this.flipDocumentTypeStatus(ctx, "docarchive:documenttype-deprecate", documentTypeId, expectedVersion, "ACTIVE", "DEPRECATED");
src/modules\document-archive\application\document-archive-service.ts:1870:    return this.flipDocumentTypeStatus(ctx, "docarchive:documenttype-reactivate", documentTypeId, expectedVersion, "DEPRECATED", "ACTIVE");
src/modules\document-archive\application\document-archive-service.ts:1875:    action: "docarchive:documenttype-deprecate" | "docarchive:documenttype-reactivate",
src/modules\document-archive\application\document-archive-service.ts:1923:    authorize({ context: ctx, action: "docarchive:documenttype-metadata-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:1990:    authorize({ context: ctx, action: "docarchive:documenttype-metadata-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2102:    authorize({ context: ctx, action: "docarchive:document-metadata-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2175:    authorize({ context: ctx, action: "docarchive:requirementtemplate-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2202:    authorize({ context: ctx, action: "docarchive:requirementtemplate-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2214:    authorize({ context: ctx, action: "docarchive:requirementtemplate-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2231:    authorize({ context: ctx, action: "docarchive:requirementtemplate-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2293:    authorize({ context: ctx, action: "docarchive:requirementtemplate-duplicate", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2321:    return this.flipTemplateStatus(ctx, "docarchive:requirementtemplate-archive", templateId, expectedVersion, "ACTIVE", "ARCHIVED");
src/modules\document-archive\application\document-archive-service.ts:2325:    return this.flipTemplateStatus(ctx, "docarchive:requirementtemplate-unarchive", templateId, expectedVersion, "ARCHIVED", "ACTIVE");
src/modules\document-archive\application\document-archive-service.ts:2342:    authorize({ context: ctx, action: "docarchive:requirementtemplate-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2377:    authorize({ context: ctx, action: "docarchive:requirementtemplate-apply", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:2520:    action: "docarchive:requirementtemplate-archive" | "docarchive:requirementtemplate-unarchive",
src/modules\document-archive\application\document-archive-service.ts:517:    authorize({ context: ctx, action: "docarchive:create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:539:    authorize({ context: ctx, action: "docarchive:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:550:    authorize({ context: ctx, action: "docarchive:read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:563:    authorize({ context: ctx, action: "docarchive:upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:606:    authorize({ context: ctx, action: "docarchive:upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:715:    authorize({ context: ctx, action: "docarchive:upload", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:746:    authorize({ context: ctx, action: "docarchive:review", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:782:    authorize({ context: ctx, action: "docarchive:review", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-archive-service.ts:937:    authorize({ context: ctx, action: "docarchive:review", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:154:    authorize({ context: ctx, action: "docarchive:series-create", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:184:    authorize({ context: ctx, action: "docarchive:series-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:195:    authorize({ context: ctx, action: "docarchive:series-read", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:200:    authorize({ context: ctx, action: "docarchive:series-cancel", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:226:   * `docarchive:series-update` action (pre-existing, previously only exercised by `advanceCycle`,
src/modules\document-archive\application\document-request-recurrence-service.ts:236:    authorize({ context: ctx, action: "docarchive:series-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:268:    authorize({ context: ctx, action: "docarchive:series-materialize", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\document-archive\application\document-request-recurrence-service.ts:290:    authorize({ context: ctx, action: "docarchive:series-update", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\identity\domain\authorization.ts:103:  // module (src/modules/document/ — S3/malware-scan primitives). `docarchive:*` is the
src/modules\identity\domain\authorization.ts:106:  // rejected presuming an ACL `authorize()` doesn't implement). `docarchive:review` covers
src/modules\identity\domain\authorization.ts:109:  | "docarchive:create"
src/modules\identity\domain\authorization.ts:110:  | "docarchive:read"
src/modules\identity\domain\authorization.ts:111:  | "docarchive:upload"
src/modules\identity\domain\authorization.ts:112:  | "docarchive:review"
src/modules\identity\domain\authorization.ts:118:  // collision resolved deliberately by namespacing under `docarchive:`, same prefix as this
src/modules\identity\domain\authorization.ts:120:  | "docarchive:requirement-create"
src/modules\identity\domain\authorization.ts:121:  | "docarchive:requirement-read"
src/modules\identity\domain\authorization.ts:122:  | "docarchive:requirement-update"
src/modules\identity\domain\authorization.ts:123:  | "docarchive:requirement-delete"
src/modules\identity\domain\authorization.ts:127:  // `docarchive:series-materialize` is distinct from `-update` because it is invoked by the
src/modules\identity\domain\authorization.ts:131:  | "docarchive:series-create"
src/modules\identity\domain\authorization.ts:132:  | "docarchive:series-read"
src/modules\identity\domain\authorization.ts:133:  | "docarchive:series-update"
src/modules\identity\domain\authorization.ts:134:  | "docarchive:series-cancel"
src/modules\identity\domain\authorization.ts:135:  | "docarchive:series-materialize"
src/modules\identity\domain\authorization.ts:139:  | "docarchive:request-create"
src/modules\identity\domain\authorization.ts:144:  | "docarchive:documenttype-create"
src/modules\identity\domain\authorization.ts:145:  | "docarchive:documenttype-rename"
src/modules\identity\domain\authorization.ts:146:  | "docarchive:documenttype-deprecate"
src/modules\identity\domain\authorization.ts:147:  | "docarchive:documenttype-reactivate"
src/modules\identity\domain\authorization.ts:148:  | "docarchive:documenttype-read"
src/modules\identity\domain\authorization.ts:152:  | "docarchive:requirementtemplate-create"
src/modules\identity\domain\authorization.ts:153:  | "docarchive:requirementtemplate-update"
src/modules\identity\domain\authorization.ts:154:  | "docarchive:requirementtemplate-duplicate"
src/modules\identity\domain\authorization.ts:155:  | "docarchive:requirementtemplate-archive"
src/modules\identity\domain\authorization.ts:156:  | "docarchive:requirementtemplate-unarchive"
src/modules\identity\domain\authorization.ts:157:  | "docarchive:requirementtemplate-read"
src/modules\identity\domain\authorization.ts:158:  | "docarchive:requirementtemplate-apply"
src/modules\identity\domain\authorization.ts:162:  // above already documents for D-123/D-126, not `docarchive:requirement-read`'s READ_ONLY_ROLES
src/modules\identity\domain\authorization.ts:164:  | "docarchive:requirement-export"
src/modules\identity\domain\authorization.ts:169:  | "activity:read"
src/modules\identity\domain\authorization.ts:172:  // `docarchive:requirement-export` above (a subscription names OTHER members as recipients,
src/modules\identity\domain\authorization.ts:173:  // and its reports are the same bulk cross-member CSVs `item:export`/`docarchive:requirement-
src/modules\identity\domain\authorization.ts:177:  | "reports:subscription-manage"
src/modules\identity\domain\authorization.ts:184:  // `docarchive:requirement-export`/`item:export` (a full-Subject export is the same bulk
src/modules\identity\domain\authorization.ts:186:  | "docarchive:dossier-export"
src/modules\identity\domain\authorization.ts:189:  // options) is ADMIN_ROLES, same tier as `docarchive:documenttype-*` above — a metadata field
src/modules\identity\domain\authorization.ts:192:  // action — WRITE_ROLES, same tier as `docarchive:create`/`docarchive:upload` (day-to-day data
src/modules\identity\domain\authorization.ts:193:  // entry on an already-existing Document, not catalog administration). `docarchive:update`
src/modules\identity\domain\authorization.ts:196:  | "docarchive:documenttype-metadata-manage"
src/modules\identity\domain\authorization.ts:197:  | "docarchive:document-metadata-update";
src/modules\identity\domain\authorization.ts:277:  "docarchive:create": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:278:  "docarchive:read": READ_ONLY_ROLES,
src/modules\identity\domain\authorization.ts:279:  "docarchive:upload": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:280:  "docarchive:review": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:281:  "docarchive:requirement-create": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:282:  "docarchive:requirement-read": READ_ONLY_ROLES,
src/modules\identity\domain\authorization.ts:283:  "docarchive:requirement-update": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:284:  "docarchive:requirement-delete": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:285:  "docarchive:series-create": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:286:  "docarchive:series-read": READ_ONLY_ROLES,
src/modules\identity\domain\authorization.ts:287:  "docarchive:series-update": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:288:  "docarchive:series-cancel": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:289:  "docarchive:series-materialize": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:290:  "docarchive:request-create": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:291:  "activity:read": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:292:  "reports:subscription-manage": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:293:  "docarchive:dossier-export": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:294:  "docarchive:documenttype-metadata-manage": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:295:  "docarchive:document-metadata-update": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:296:  "docarchive:documenttype-create": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:297:  "docarchive:documenttype-rename": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:298:  "docarchive:documenttype-deprecate": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:299:  "docarchive:documenttype-reactivate": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:300:  "docarchive:documenttype-read": READ_ONLY_ROLES,
src/modules\identity\domain\authorization.ts:301:  "docarchive:requirementtemplate-create": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:302:  "docarchive:requirementtemplate-update": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:303:  "docarchive:requirementtemplate-duplicate": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:304:  "docarchive:requirementtemplate-archive": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:305:  "docarchive:requirementtemplate-unarchive": ADMIN_ROLES,
src/modules\identity\domain\authorization.ts:306:  "docarchive:requirementtemplate-read": READ_ONLY_ROLES,
src/modules\identity\domain\authorization.ts:307:  "docarchive:requirementtemplate-apply": WRITE_ROLES,
src/modules\identity\domain\authorization.ts:308:  "docarchive:requirement-export": ADMIN_ROLES,
src/modules\reports\application\reports-service.ts:134:    authorize({ context: ctx, action: "docarchive:requirement-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\application\reports-service.ts:161:    authorize({ context: ctx, action: "docarchive:requirement-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\application\reports-service.ts:182:    authorize({ context: ctx, action: "docarchive:requirement-export", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\application\reports-service.ts:22: * RBAC tier: `item:export`/`docarchive:requirement-export` (ADMIN_ROLES) — a CSV report reading
src/modules\reports\application\reports-service.ts:25: * export, not the READ_ONLY_ROLES tier `item:read`/`docarchive:requirement-read` use for a single
src/modules\reports\application\report-subscription-service.ts:124:    authorize({ context: ctx, action: "reports:subscription-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\application\report-subscription-service.ts:135:    authorize({ context: ctx, action: "reports:subscription-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\application\report-subscription-service.ts:141:    authorize({ context: ctx, action: "reports:subscription-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\application\report-subscription-service.ts:69:    authorize({ context: ctx, action: "reports:subscription-manage", resource: { tenantId: ctx.tenant.tenantId } });
src/modules\reports\http\reports-handler.ts:304:      authorize({ context, action: "reports:subscription-manage", resource: { tenantId } });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "organization:close|cancelClosure|HELD_FOR_RECOVERY|authorizeCancelClosure|closeOrganization" src test | Select-Object -First 200; rg -n -i "tenant.*cost|cost.*tenant|unit economics|economia unit.ria|metric.*tenant|tenantId.*metric|dimensions.*tenant" src infra docs/architecture/cost-model.md test | Select-Object -First 200; rg -n "simulate-principal-policy|gsi8_worker_isolation|GSI3|GSI6|GSI8" infra test scripts docs/architecture/decisions-log.md | Select-Object -First 240' in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-09-08T02:46:01.490969Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.3 seconds
Total output lines: 434
Output:
src\runtime\aws\composition\tenant-purge.ts:91: * HELD_FOR_RECOVERY reconciliation). */
src\runtime\aws\composition\organization.ts:250:   * keeps working unchanged; when absent, `closeOrganization` is simply not built and the route is
src\runtime\aws\composition\organization.ts:288:  const closeOrganization = tenantPurgeStateMachineArn
src\runtime\aws\composition\organization.ts:322:    closeOrganization,
src\workers\tenant-purge\tenant-purge-sweep.ts:66:  /** D-127: only needed for the `HELD_FOR_RECOVERY` reconciliation branch below. */
src\workers\tenant-purge\tenant-purge-sweep.ts:89:  /** D-127: `HELD_FOR_RECOVERY` tenants for which this sweep pass completed a stalled
src\workers\tenant-purge\tenant-purge-sweep.ts:91:   * current `closureAttemptId` — but the `HELD_FOR_RECOVERY -> ACTIVE` write never committed,
src\workers\tenant-purge\tenant-purge-sweep.ts:94:  /** D-127: `HELD_FOR_RECOVERY` tenants whose execution state and lifecycle record disagree in a
src\workers\tenant-purge\tenant-purge-sweep.ts:118:      if (record.status === "HELD_FOR_RECOVERY") {
src\workers\tenant-purge\tenant-purge-sweep.ts:134:          // HELD_FOR_RECOVERY and never cleared on forward progress). The bare-tenantId fallback
src\workers\tenant-purge\tenant-purge-sweep.ts:170: * D-127 sweeper reconciliation for `HELD_FOR_RECOVERY` (round-7 of the approved design's
src\workers\tenant-purge\tenant-purge-sweep.ts:183: *   3. `record.status` is (re-)confirmed `HELD_FOR_RECOVERY` — nothing else already resolved it.
src\workers\tenant-purge\tenant-purge-sweep.ts:189: * given `HELD_FOR_RECOVERY` means nothing physical has purged yet in the overwhelmingly common
src\workers\tenant-purge\tenant-purge-sweep.ts:215:    // Condition 3 of the conjunction (record.status still HELD_FOR_RECOVERY) is enforced by
src\workers\tenant-purge\tenant-purge-sweep.ts:224:        from: "HELD_FOR_RECOVERY",
test\unit\tenant-lifecycle.test.ts:14:const ALL_STATUSES: TenantLifecycleStatus[] = ["ACTIVE", "HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"];
test\unit\tenant-lifecycle.test.ts:17:  it("allows every forward transition on the approved happy path (D-127: ACTIVE now goes through HELD_FOR_RECOVERY first)", () => {
test\unit\tenant-lifecycle.test.ts:18:    expect(canTransition("ACTIVE", "HELD_FOR_RECOVERY")).toBe(true);
test\unit\tenant-lifecycle.test.ts:19:    expect(canTransition("HELD_FOR_RECOVERY", "DELETING")).toBe(true);
test\unit\tenant-lifecycle.test.ts:26:  it("D-127: never allows reverting to ACTIVE from any state EXCEPT the one named exception, HELD_FOR_RECOVERY", () => {
test\unit\tenant-lifecycle.test.ts:28:      if (from === "HELD_FOR_RECOVERY") continue;
test\unit\tenant-lifecycle.test.ts:31:    expect(canTransition("HELD_FOR_RECOVERY", "ACTIVE")).toBe(true);
test\unit\tenant-lifecycle.test.ts:41:  it("rejects skipping stages (e.g. DELETING straight to VERIFIED, ACTIVE straight to PURGING/DELETING, HELD_FOR_RECOVERY straight to QUIESCING)", () => {
test\unit\tenant-lifecycle.test.ts:46:    expect(canTransition("HELD_FOR_RECOVERY", "QUIESCING")).toBe(false);
test\unit\tenant-lifecycle.test.ts:47:    expect(canTransition("HELD_FOR_RECOVERY", "PURGING")).toBe(false);
test\unit\tenant-lifecycle.test.ts:50:  it("allows entering BLOCKED/HELD from any of the mid-cascade states, including HELD_FOR_RECOVERY (D-127)", () => {
test\unit\tenant-lifecycle.test.ts:51:    for (const from of ["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED"] as const) {
test\unit\tenant-lifecycle.test.ts:62:  it("allows resuming from BLOCKED/HELD back to exactly the state it was blocked from, never elsewhere, including the new HELD_FOR_RECOVERY case (D-127)", () => {
test\unit\tenant-lifecycle.test.ts:67:    expect(canTransition("HELD", "HELD_FOR_RECOVERY", "HELD_FOR_RECOVERY")).toBe(true);
test\unit\tenant-lifecycle.test.ts:68:    // Resuming from a HELD_FOR_RECOVERY-originated hold must land on HELD_FOR_RECOVERY, never
test\unit\tenant-lifecycle.test.ts:71:    expect(canTransition("HELD", "ACTIVE", "HELD_FOR_RECOVERY")).toBe(false);
test\unit\tenant-lifecycle.test.ts:72:    expect(canTransition("HELD", "DELETING", "HELD_FOR_RECOVERY")).toBe(false);
test\unit\tenant-lifecycle.test.ts:77:    expect(() => assertValidTransition("ACTIVE", "HELD_FOR_RECOVERY")).not.toThrow();
test\unit\tenant-lifecycle.test.ts:78:    expect(() => assertValidTransition("HELD_FOR_RECOVERY", "ACTIVE")).not.toThrow();
src\workers\tenant-purge\lifecycle-transition.ts:52:  /** D-127: true ONLY for the one legal backward edge (`from: "HELD_FOR_RECOVERY", to:
src\workers\tenant-purge\lifecycle-transition.ts:83:  if (input.from === "HELD_FOR_RECOVERY" && input.to === "DELETING" && record.status === "ACTIVE") {
test\unit\system-mutation.test.ts:31:  it("moves ACTIVE -> HELD_FOR_RECOVERY for real, via occ.ts's OCC-fenced Update, stamping recoveryDeadline/closureAttemptId", async () => {
test\unit\system-mutation.test.ts:40:      to: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:49:    expect(record?.status).toBe("HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:55:  it("moves HELD_FOR_RECOVERY -> DELETING for real (30-day deadline firing)", async () => {
test\unit\system-mutation.test.ts:57:    await seedLifecycle(store, "tenant-1", "HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:59:    await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "DELETING", expectedVersion: 1 });
test\unit\system-mutation.test.ts:65:  it("moves HELD_FOR_RECOVERY -> ACTIVE (cancellation) for real, and clears recoveryDeadline/closureAttemptId/executionArn", async () => {
test\unit\system-mutation.test.ts:71:      status: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:80:    await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "ACTIVE", expectedVersion: 1 });
test\unit\system-mutation.test.ts:91:  it("moves HELD_FOR_RECOVERY -> HELD (legal hold) WITHOUT clearing recoveryDeadline - preserves the original countdown", async () => {
test\unit\system-mutation.test.ts:97:      status: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:109:      from: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:119:    expect(record?.blockedFrom).toBe("HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:124:    // And resuming from the lifted hold goes back to HELD_FOR_RECOVERY, still preserving it.
test\unit\system-mutation.test.ts:130:      to: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:132:      blockedFrom: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:135:    expect(resumed?.status).toBe("HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:168:        to: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:272:      to: "HELD_FOR_RECOVERY",
test\unit\workers\delivery-record-purge.test.ts:77:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\core-user-data-purge.test.ts:76:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\invitation-purge.test.ts:125:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\membership-purge.test.ts:96:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\quota-telemetry-purge.test.ts:70:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\security-audit-purge.test.ts:61:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\transient-purge.test.ts:178:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:148:describe("runTenantPurgeSweep - HELD_FOR_RECOVERY reconciliation (D-127 round-7 strict conjunction)", () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:151:  it("never blindly re-launches a HELD_FOR_RECOVERY tenant via the generic orphan-repair path, however stale", async () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:153:    const lifecycle = scanOf(record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN }));
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:164:  it("completes a stalled cancellation: ABORTED + matching closureAttemptId + still HELD_FOR_RECOVERY -> restores ACTIVE", async () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:166:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, version: 3 });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:185:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:196:    expect(after?.status).toBe("HELD_FOR_RECOVERY"); // untouched
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:201:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-2", executionArn: EXECUTION_ARN });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:214:    expect(after?.status).toBe("HELD_FOR_RECOVERY");
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:217:  it("repairs (re-StartExecution) a HELD_FOR_RECOVERY record that never got an executionArn attached, without describing/alarming", async () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:219:    const lifecycle = scanOf(record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1" }));
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:233:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, version: 1 });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:236:    // HELD_FOR_RECOVERY before this sweep pass's write lands.
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:237:    await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: "t-held", from: "HELD_FOR_RECOVERY", to: "DELETING", expectedVersion: 1 });
test\unit\workers\tenant-purge\lifecycle-transition.test.ts:127:  describe("D-127: HELD_FOR_RECOVERY -> DELETING finding the record already back at ACTIVE (cancelled)", () => {
test\unit\workers\tenant-purge\lifecycle-transition.test.ts:134:        { tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "DELETING" },
test\unit\workers\tenant-purge\lifecycle-transition.test.ts:153:        advanceTenantLifecycle({ store, reader: readerFor(store), tableName: TABLE }, { tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "DELETING" }),
test\unit\organization\close-organization.test.ts:56:  it("moves ACTIVE -> HELD_FOR_RECOVERY, stamps recoveryDeadline/closureAttemptId, and starts the purge execution named by tenantId-closureAttemptId", async () => {
test\unit\organization\close-organization.test.ts:63:    expect(result).toEqual({ tenantId: "org-1", status: "HELD_FOR_RECOVERY", transitioned: true });
test\unit\organization\close-organization.test.ts:65:    expect(record?.status).toBe("HELD_FOR_RECOVERY");
test\unit\organization\close-organization.test.ts:92:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING"] as const)("re-launches the execution for an in-flight %s tenant without writing anything", async (status) => {
test\unit\organization\close-organization.test.ts:124:    // The competing close() commits ACTIVE -> HELD_FOR_RECOVERY (with ITS OWN closureAttemptId,
test\unit\organization\close-organization.test.ts:135:          to: "HELD_FOR_RECOVERY",
test\unit\organization\cancel-organization-closure.test.ts:93:  it("cancels a HELD_FOR_RECOVERY closure: stops the execution, then restores ACTIVE, clearing attempt-scoped fields", async () => {
test\unit\organization\cancel-organization-closure.test.ts:96:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, recoveryDeadline: "2026-09-30T00:00:00.000Z" });
test\unit\organization\cancel-organization-closure.test.ts:116:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:127:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:138:    expect(record?.status).toBe("HELD_FOR_RECOVERY"); // untouched
test\unit\organization\cancel-organization-closure.test.ts:144:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, version: 1 });
test\unit\organization\cancel-organization-closure.test.ts:148:        await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: TENANT_ID, from: "HELD_FOR_RECOVERY", to: "DELETING", expectedVersion: 1 });
test\unit\organization\cancel-organization-closure.test.ts:162:    "refuses a %s tenant outright - accepts EXCLUSIVELY HELD_FOR_RECOVERY",
test\unit\organization\cancel-organization-closure.test.ts:173:  it("denies ADMIN via authorizeCancelClosure (OWNER-only, same tier as organization:close)", async () => {
test\unit\organization\cancel-organization-closure.test.ts:176:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:186:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:194:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:204:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:210:  it("fails with OrganizationClosureUnavailableError when HELD_FOR_RECOVERY has no executionArn/closureAttemptId attached yet, rather than guessing a StopExecution target", async () => {
test\unit\organization\cancel-organization-closure.test.ts:213:    await seedLifecycle(store, "HELD_FOR_RECOVERY");
src\modules\identity\domain\authorization.ts:89:  | "organization:close"
src\modules\identity\domain\authorization.ts:91:  // `HELD_FOR_RECOVERY`. Same OWNER_ROLES tier as `organization:close` — reversing the most
src\modules\identity\domain\authorization.ts:95:  // `authorizeCancelClosure()` below, NOT the generic `authorize()` — the caller
src\modules\identity\domain\authorization.ts:98:  // `HELD_FOR_RECOVERY` tenant is definitionally not), so `authorize()`'s own `AuthorizationInput`
src\modules\identity\domain\authorization.ts:228: * `organization:update-settings` (B2B-10), `organization:close` (W3-07/D-124). */
src\modules\identity\domain\authorization.ts:275:  "organization:close": OWNER_ROLES,
src\modules\identity\domain\authorization.ts:378:   * `HELD_FOR_RECOVERY` tenant in the first place (see the `organization:cancel-close` doc
src\modules\identity\domain\authorization.ts:391: *     `HELD_FOR_RECOVERY` by definition on this path.
src\modules\identity\domain\authorization.ts:403:export function authorizeCancelClosure(input: CancelClosureAuthorizationInput): void {
src\modules\organization\http\organization-lifecycle-handlers.ts:36:  closeOrganization: CloseOrganizationService;
src\modules\organization\http\organization-lifecycle-handlers.ts:38:   * route is simply unreachable (like `closeOrganization` above) when absent. */
src\modules\organization\http\organization-lifecycle-handlers.ts:93:    const result = await deps.closeOrganization.close(context);
src\modules\organization\http\organization-lifecycle-handlers.ts:101: * identity-resolution path (see that file's header): a `HELD_FOR_RECOVERY` tenant cannot resolve
src\shared\tenant-lifecycle\tenant-purge-execution-describer.ts:3: * execution's terminal/live status by ARN. Used ONLY by the sweeper's `HELD_FOR_RECOVERY`
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:25: *   ACTIVE -> HELD_FOR_RECOVERY -> DELETING -> QUIESCING -> PURGING -> VERIFIED -> DELETED
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:26: * Any of HELD_FOR_RECOVERY/DELETING/QUIESCING/PURGING/VERIFIED can additionally move to
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:28: * SAME state once remediated. For `HELD_FOR_RECOVERY` specifically, a LEGAL HOLD (as opposed to
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:35: * `HELD_FOR_RECOVERY -> ACTIVE` (cancellation, `CancelOrganizationClosureService`) is the
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:39: * residue in place, it never re-opens the record), and no state other than `HELD_FOR_RECOVERY`
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:44:  | "HELD_FOR_RECOVERY"
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:65:  /** D-127: ISO timestamp of when `HELD_FOR_RECOVERY` expires into `DELETING`. Computed once,
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:66:   * at the moment `ACTIVE -> HELD_FOR_RECOVERY` commits — never recalculated by the Step
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:71:  /** D-127: a fresh UUID minted on every `ACTIVE -> HELD_FOR_RECOVERY` transition — identifies
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:103:  ACTIVE: new Set(["HELD_FOR_RECOVERY"]),
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:104:  // HELD_FOR_RECOVERY -> ACTIVE (cancellation) is NOT listed here — it is the one named
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:106:  HELD_FOR_RECOVERY: new Set(["DELETING", "BLOCKED", "HELD"]),
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:119: * would re-enter ACTIVE from anywhere OTHER than `HELD_FOR_RECOVERY` (D-127's single named
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:125:  if (to === "ACTIVE") return from === "HELD_FOR_RECOVERY"; // D-127: the ONLY edge back to ACTIVE
src\shared\tenant-lifecycle\system-mutation.ts:91:       * `ACTIVE -> HELD_FOR_RECOVERY` — stamps the two identifiers a closure attempt needs.
src\shared\tenant-lifecycle\system-mutation.ts:99:       * that is still `HELD_FOR_RECOVERY`, so `CancelOrganizationClosureService` can later call
src\shared\tenant-lifecycle\system-mutation.ts:102:       * conditioned on the record still being `HELD_FOR_RECOVERY` for this exact
src\shared\tenant-lifecycle\system-mutation.ts:199:        // D-127: entering HELD from HELD_FOR_RECOVERY (legal hold during the recovery window)
src\shared\tenant-lifecycle\system-mutation.ts:203:      } else if (op.to === "HELD_FOR_RECOVERY" && op.from === "ACTIVE") {
src\shared\tenant-lifecycle\system-mutation.ts:204:        // D-127: ACTIVE -> HELD_FOR_RECOVERY (CloseOrganizationService) is the only transition
src\shared\tenant-lifecycle\system-mutation.ts:209:          throw new Error("SystemMutation \"LIFECYCLE_TRANSITION\" ACTIVE -> HELD_FOR_RECOVERY requires recoveryDeadline and closureAttemptId.");
src\shared\tenant-lifecycle\system-mutation.ts:215:        // D-127: HELD_FOR_RECOVERY -> ACTIVE (CancelOrganizationClosureService) — the closure
src\shared\tenant-lifecycle\system-muta…53329 tokens truncated…S" },
test\integration-dynamodb\setup.ts:43:        { AttributeName: "GSI3SK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:44:        { AttributeName: "GSI6PK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:45:        { AttributeName: "GSI6SK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:61:          IndexName: "GSI3",
test\integration-dynamodb\setup.ts:63:            { AttributeName: "GSI3PK", KeyType: "HASH" },
test\integration-dynamodb\setup.ts:64:            { AttributeName: "GSI3SK", KeyType: "RANGE" },
test\integration-dynamodb\setup.ts:69:          IndexName: "GSI6",
test\integration-dynamodb\setup.ts:71:            { AttributeName: "GSI6PK", KeyType: "HASH" },
test\integration-dynamodb\setup.ts:72:            { AttributeName: "GSI6SK", KeyType: "RANGE" },
scripts\backfill-gsi8-membership-purge.ts:2: * GSI8 backfill for membership-purge (D-179/D-180 pilot slice, design §1/§3). Writes the
scripts\backfill-gsi8-membership-purge.ts:3: * `GSI8PK`/`GSI8SK` MaintenanceDueIndex pointer onto every pre-existing `Membership` row that
scripts\backfill-gsi8-membership-purge.ts:6: * one from 40 days ago; it just has a `GSI8SK` in the future. Skipping it here would mean it never
scripts\backfill-gsi8-membership-purge.ts:7: * gets a pointer at all (the write path only stamps GSI8 at the moment of a NEW removal), silently
scripts\backfill-gsi8-membership-purge.ts:12: * script is the explicit, manual, one-off step that activates GSI8 discovery for the installed
scripts\backfill-gsi8-membership-purge.ts:19: * no GSI3/GSI4/GSI6 access needed.
scripts\backfill-gsi8-membership-purge.ts:77:  writePointer: (m: Membership, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-membership-purge.ts:88:    if (membership.GSI8PK && membership.GSI8SK) {
scripts\backfill-gsi8-membership-purge.ts:125:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-membership-purge.ts:130:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":v": membership.version },
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:65:  it("materialize -> producer claim (real GSI3 query + real TransactWriteItems) -> durable outbox -> relay -> dispatch -> exactly one NotificationIntent", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:92:    // Real GSI3 Query against DynamoDB Local, real conditional TransactWriteItems claim,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:94:    // transaction, real GSI6 WORKSTATE#CLAIMED pointer.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:115:    expect(claimedOccurrence?.GSI6PK).toBe("WORKSTATE#CLAIMED");
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:117:    // Real outbox relay: reads the durable record (via a GSI6 candidate-shaped shape here,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:124:          IndexName: "GSI6",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:125:          KeyConditionExpression: "GSI6PK = :pk",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:141:    // markPublished must have removed the GSI6 pointer for real (Codex-found bug, fixed).
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:145:        IndexName: "GSI6",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:146:        KeyConditionExpression: "GSI6PK = :pk",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:165:    expect(triggeredOccurrence?.GSI6PK).toBeUndefined(); // pointer removed on TRIGGERED, proven against real DynamoDB
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:175:  it("real conditional Query on GSI6 finds an expired claim and reconciliation reverts it via real TransactWriteItems", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:214:      GSI6PK: "WORKSTATE#CLAIMED",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:215:      GSI6SK: "2026-09-10T12:02:00.000Z#TENANT#t1#OCCURRENCE#occ_expired",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:227:    expect(afterRevert?.GSI6PK).toBeUndefined();
scripts\backfill-gsi8-security-audit-purge.ts:2: * GSI8 backfill for security-audit-purge (D-179/D-187 slice 6, mirrors
scripts\backfill-gsi8-security-audit-purge.ts:4: * `occurredAt` fence instead of `resetAt`). Writes the `GSI8PK`/`GSI8SK` MaintenanceDueIndex
scripts\backfill-gsi8-security-audit-purge.ts:9: * write path only stamps GSI8 inside each entity's own `build*Event()`), silently falling out of
scripts\backfill-gsi8-security-audit-purge.ts:14: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-security-audit-purge.ts:47:  GSI8PK?: string;
scripts\backfill-gsi8-security-audit-purge.ts:48:  GSI8SK?: string;
scripts\backfill-gsi8-security-audit-purge.ts:106:  writePointer: (row: SecurityAuditRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-security-audit-purge.ts:116:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-security-audit-purge.ts:160:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-security-audit-purge.ts:164:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":occurredAt": row.occurredAt },
scripts\backfill-gsi8-transient-purge.ts:2: * GSI8 backfill for transient-purge (D-179/D-188 slice 7, mirrors
scripts\backfill-gsi8-transient-purge.ts:4: * dynamics — see `shared/transient-purge-gsi8.ts`'s file header). Writes the `GSI8PK`/`GSI8SK`
scripts\backfill-gsi8-transient-purge.ts:8: * pointer at all (the live write path only stamps GSI8 at the specific writers listed below),
scripts\backfill-gsi8-transient-purge.ts:18: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-transient-purge.ts:50:  GSI8PK?: string;
scripts\backfill-gsi8-transient-purge.ts:51:  GSI8SK?: string;
scripts\backfill-gsi8-transient-purge.ts:114:  writePointer: (row: TransientRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-transient-purge.ts:131:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-transient-purge.ts:169:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-transient-purge.ts:173:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":version": row.version },
test\unit\dispatch-outbox-relay-processor.test.ts:27:    GSI6PK: "RECON#OUTBOX#PENDING",
test\unit\dispatch-outbox-relay-processor.test.ts:28:    GSI6SK: "2026-08-19T10:00:00.000Z#evt-1",
test\unit\dispatch-outbox-relay-processor.test.ts:94:      streamRecord("evt-2", outboxRecord({ eventId: "evt-2", correlationId: "cor-2", SK: "EVENT#...#evt-2", GSI6SK: "x2" })),
test\unit\dispatch-outbox-relay-processor.test.ts:95:      streamRecord("evt-3", outboxRecord({ eventId: "evt-3", correlationId: "cor-3", SK: "EVENT#...#evt-3", GSI6SK: "x3" })),
test\unit\dispatch-outbox-relay\relay.test.ts:24:    GSI6PK: "RECON#OUTBOX#PENDING",
test\unit\dispatch-outbox-relay\relay.test.ts:25:    GSI6SK: "2026-08-19T10:00:00.000Z#evt-1",
test\unit\dispatch-outbox-relay\relay.test.ts:142:    store.pending = [makeRecord({ SK: "EVENT#...#evt-1", eventId: "evt-1" }), makeRecord({ SK: "EVENT#...#evt-2", eventId: "evt-2", GSI6SK: "x2" })];
test\unit\dispatch-outbox-relay\relay.test.ts:158:      makeRecord({ SK: "EVENT#...#evt-2", eventId: "evt-2", GSI6SK: "x2", destination: "SQS_NOTIFICATION_EMAIL_V1" }),
test\unit\dispatch-outbox-relay\relay.test.ts:226:    await publishOne(deps, makeRecord({ correlationId: "cor_b", SK: "EVENT#...#evt-2", eventId: "evt-2", GSI6SK: "x2" }));
test\unit\document\advance-after-evidence.test.ts:50:    GSI6PK: "RECON#UPLOAD#PENDING",
test\unit\document\advance-after-evidence.test.ts:51:    GSI6SK: "2026-08-22T00:10:00.000Z#TENANT#t1#SLOT#slot1",
test\unit\document\advance-after-evidence.test.ts:309:    it("marks the associated RESERVED slot CONSUMED and strips its GSI6 pointer on PROMOTE", async () => {
test\unit\document\advance-after-evidence.test.ts:326:      expect(slot.GSI6PK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:327:      expect(slot.GSI6SK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:328:      // D-179/D-188 (transient-purge, 7th GSI8 slice): CONSUMED is a terminal transition off
test\unit\document\advance-after-evidence.test.ts:329:      // RESERVED - the GSI8 pointer must be written in this SAME conditional write, reflecting
test\unit\document\advance-after-evidence.test.ts:331:      expect(slot.GSI8PK).toBe("WORK#TRANSIENT");
test\unit\document\advance-after-evidence.test.ts:332:      expect(slot.GSI8SK).toBe(`2026-08-29T00:00:00.000Z#TENANT#t1#UploadSlot#${uploadSlotKey("t1", "slot1").SK}`);
test\unit\document\advance-after-evidence.test.ts:335:    it("marks the associated RESERVED slot CONSUMED and strips its GSI6 pointer on REJECT", async () => {
test\unit\document\advance-after-evidence.test.ts:346:      expect(slot.GSI6PK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:347:      expect(slot.GSI6SK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:348:      expect(slot.GSI8PK).toBe("WORK#TRANSIENT");
test\unit\document\advance-after-evidence.test.ts:349:      expect(slot.GSI8SK).toBe(`2026-08-29T00:00:00.000Z#TENANT#t1#UploadSlot#${uploadSlotKey("t1", "slot1").SK}`);
test\unit\document\advance-after-evidence.test.ts:354:      await store.putIfAbsent(baseSlot({ status: "EXPIRED", GSI6PK: undefined, GSI6SK: undefined }));
test\unit\reports\scheduler.test.ts:42:/** Fake mirroring the real DynamoDB GSI8 Query's contract - reads live off the same store the
test\unit\reports\scheduler.test.ts:50:        .filter((item): item is EntityKey & Record<string, unknown> => item["entityType"] === "ReportSubscription" && typeof item["GSI8SK"] === "string" && (item["GSI8SK"] as string) < input.before)
test\unit\reports\scheduler.test.ts:52:          const gsi8sk = item["GSI8SK"] as string;
test\unit\reports\scheduler.test.ts:75:  it("claims a due subscription: advances nextRunAt/GSI8 pointer and writes a durable outbox event", async () => {
test\unit\reports\scheduler.test.ts:85:    expect(updated?.GSI8SK).toBe(`2026-09-16T09:00:00.000Z#TENANT#${TENANT}#${subscription.subscriptionId}`);
test\unit\reports\scheduler.test.ts:98:  it("never touches a subscription whose nextRunAt is still in the future - the GSI8 query's own GSI8SK < before filter excludes it", async () => {
test\unit\reports\scheduler.test.ts:116:  it("skips (never throws) a subscription concurrently claimed by another tick since the GSI8 query observed it", async () => {
test\unit\reports\scheduler.test.ts:149:    // A real GSI8 Query is eventually consistent - pin the candidate source to the stale
test\unit\workers\transient-purge.test.ts:73:  it("never surfaces a WebhookInbox row younger than 7 days as a candidate at all (GSI8 is due-ordered)", async () => {
test\unit\workers\transient-purge.test.ts:93:  it("never seeds a GSI8 pointer for a RESERVED UploadSlot (never a candidate at all, not even scanned)", async () => {
test\unit\workers\transient-purge.test.ts:100:    expect(stored?.["GSI8PK"]).toBeUndefined();
test\unit\workers\transient-purge.test.ts:101:    expect(stored?.["GSI8SK"]).toBeUndefined();
test\unit\workers\transient-purge.test.ts:112:    // GSI8's own `queryDue` uses a strict `GSI8SK < :before` (same as every other migrated
test\unit\workers\transient-purge.test.ts:192:      expect(stored?.["GSI8PK"]).toBe("WORK#TRANSIENT"); // still in WORK namespace, not DLQ yet
test\unit\workers\transient-purge.test.ts:207:  it("conditional-delete guard: a candidate whose version changed between the GSI8 query and the claim is never purged", async () => {
test\unit\workers\transient-purge.test.ts:228:  it("conditional-delete guard: a row deleted between the GSI8 query and the claim (PK/SK gone) is never silently treated as success", async () => {
test\unit\workers\transient-purge.test.ts:260:  it("processes a mix of WebhookInbox/UploadSlot candidates across tenants in one run and touches ONLY the ones GSI8 actually surfaces", async () => {
test\unit\workers\transient-purge.test.ts:281:    // tooRecentInbox/reservedSlot never appear in GSI8's `GSI8SK < now` query at all (reservedSlot
test\unit\workers\transient-purge.test.ts:292:  it("drains multiple GSI8 pages within one run", async () => {
test\unit\workers\transient-purge.test.ts:340:    expect(stored?.["GSI8PK"]).toBe("DLQ#TRANSIENT");
test\unit\workers\transient-purge.test.ts:364:  it("self-heals a stale/malformed GSI8 pointer left on a RESERVED UploadSlot (defensive only — no real writer produces this)", async () => {
test\unit\workers\transient-purge.test.ts:388:    expect(stored?.["GSI8PK"]).toBeUndefined();
test\unit\workers\transient-purge.test.ts:389:    expect(stored?.["GSI8SK"]).toBeUndefined();
test\unit\workers\requirement-evidence-refresh.test.ts:77: * `requirement-reindex.test.ts`'s `fakeCandidateSource` uses for GSI8. Only exposes the ONE
test\unit\workers\requirement-evidence-refresh.test.ts:127:    expect(updated?.GSI8PK).toBeUndefined();
test\unit\workers\requirement-evidence-refresh.test.ts:223:      remove: ["evidenceVersionId", "evidenceDocumentId", "evidenceSeq", "evidenceState", "evidenceValidUntil", "GSI8PK", "GSI8SK", "GSI9PK", "GSI9SK"],
test\unit\workers\requirement-evidence-refresh.test.ts:230:    // documents for its own GSI8 staleness test. The worker's own per-Requirement fresh
test\unit\shared\security-audit-gsi8.test.ts:12:  it("builds GSI8PK=WORK#SECURITY_AUDIT and embeds tenantId/entityType/sk into GSI8SK", () => {
test\unit\shared\security-audit-gsi8.test.ts:14:    expect(keys.GSI8PK).toBe(`WORK#${SECURITY_AUDIT_PURGE_WORK_TYPE}`);
test\unit\shared\security-audit-gsi8.test.ts:15:    expect(keys.GSI8SK).toBe("2026-01-01T00:00:00.000Z#TENANT#tenant-1#AuditEvent#EVT#2025-01-01T00:00:00.000Z#evt-1");
test\unit\workers\invitation-purge.test.ts:70:  it("never surfaces a REVOKED invitation whose revokedAt is less than 30 days ago as a candidate at all (GSI8 is due-ordered)", async () => {
test\unit\workers\invitation-purge.test.ts:139:      expect(stored?.["GSI8PK"]).toBe("WORK#INVITATION_PURGE"); // still in WORK namespace, not DLQ yet
test\unit\workers\invitation-purge.test.ts:207:  it("processes a mix of eligible/blocked candidates across tenants in one run and touches ONLY the ones GSI8 actually surfaces", async () => {
test\unit\workers\invitation-purge.test.ts:227:    // tooRecent never appears in GSI8's `GSI8SK < now` query at all - not "scanned and skipped".
test\unit\workers\invitation-purge.test.ts:236:  it("drains multiple GSI8 pages within one run", async () => {
test\unit\workers\invitation-purge.test.ts:284:    expect(stored?.["GSI8PK"]).toBe("DLQ#INVITATION_PURGE");
test\unit\workers\invitation-purge.test.ts:295:  it("self-heals a stale GSI8 pointer left on an invitation that is no longer a real candidate (defensive - no live path produces this today)", async () => {
test\unit\workers\invitation-purge.test.ts:310:    expect(after?.["GSI8PK"]).toBeUndefined();
test\unit\workers\invitation-purge.test.ts:311:    expect(after?.["GSI8SK"]).toBeUndefined();
test\unit\workers\core-user-data-purge-fakes.ts:38:/** In-memory fake standing in for the real DynamoDB `GSI8` Query + base-table `GetItem`/
test\unit\workers\core-user-data-purge-fakes.ts:53:  /** Seeds a row exactly as the real writer would leave it — computes GSI8PK/GSI8SK itself via
test\unit\workers\core-user-data-purge-fakes.ts:55:   * does. A row whose `deletedAt` is absent gets NO GSI8 pointer at all (never a candidate). */
test\unit\workers\core-user-data-purge-fakes.ts:70:  /** Test-only escape hatch to simulate a row disappearing between the GSI8 query and the claim
test\unit\workers\core-user-data-purge-fakes.ts:78:      .filter((i) => i["GSI8PK"] === "WORK#CORE_USER_DATA" && typeof i["GSI8SK"] === "string" && (i["GSI8SK"] as string) < input.before)
test\unit\workers\core-user-data-purge-fakes.ts:79:      .sort((a, b) => (a["GSI8SK"] as string).localeCompare(b["GSI8SK"] as string));
test\unit\workers\core-user-data-purge-fakes.ts:87:        dueAtIso: (i["GSI8SK"] as string).split("#TENANT#")[0]!,
test\unit\notification\whatsapp-webhook-workflow.test.ts:71:  it("the WebhookInbox row is account-scoped (PK=WEBHOOK#WHATSAPP#<wabaId>), purgeScope=ACCOUNT, GSI8 pointer keyed by accountId - never TENANT#", async () => {
test\unit\notification\whatsapp-webhook-workflow.test.ts:79:    expect(inbox?.["GSI8PK"]).toBe("WORK#TRANSIENT");
test\unit\notification\whatsapp-webhook-workflow.test.ts:80:    expect(inbox?.["GSI8SK"]).toBe(`2026-09-17T12:05:00.000Z#ACCOUNT#${WABA_ID}#WebhookInbox#EVENT#wamid-1#DELIVERED`);
test\unit\workers\transient-purge-fakes.ts:43:/** In-memory fake standing in for the real DynamoDB `GSI8` Query + base-table `GetItem`/
test\unit\workers\transient-purge-fakes.ts:56:  /** Seeds a row exactly as the real writer would leave it — computes GSI8PK/GSI8SK itself via
test\unit\workers\transient-purge-fakes.ts:59:   * `undefined`), matching every real writer's behavior. Pass `GSI8PK`/`GSI8SK` explicitly in
test\unit\workers\transient-purge-fakes.ts:72:  /** Escape hatch to seed a row with an explicit GSI8PK/GSI8SK regardless of what the real
test\unit\workers\transient-purge-fakes.ts:86:  /** Test-only escape hatch to simulate a row disappearing between the GSI8 query and the claim
test\unit\workers\transient-purge-fakes.ts:94:      .filter((i) => i["GSI8PK"] === "WORK#TRANSIENT" && typeof i["GSI8SK"] === "string" && (i["GSI8SK"] as string) < input.before)
test\unit\workers\transient-purge-fakes.ts:95:      .sort((a, b) => (a["GSI8SK"] as string).localeCompare(b["GSI8SK"] as string));
test\unit\workers\transient-purge-fakes.ts:101:        const gsi8sk = i["GSI8SK"] as string;
test\unit\reports\report-subscription.test.ts:17:  it("builds the GSI8 discovery pointer under the REPORT_SUBSCRIPTION worker namespace, due date first in the sort key", () => {
test\unit\reports\report-subscription.test.ts:19:    expect(keys.GSI8PK).toBe("WORK#REPORT_SUBSCRIPTION");
test\unit\reports\report-subscription.test.ts:20:    expect(keys.GSI8SK).toBe("2026-09-12T04:00:00.000Z#TENANT#t1#sub1");
test\unit\workers\delivery-record-purge.test.ts:43:  it("never surfaces a record created less than 180 days ago as a candidate at all (GSI8 is due-ordered)", async () => {
test\unit\workers\delivery-record-purge.test.ts:91:      expect(stored?.["GSI8PK"]).toBe("WORK#DELIVERY_RECORD"); // still in WORK namespace, not DLQ yet
test\unit\workers\delivery-record-purge.test.ts:179:  it("processes a mix of eligible/blocked candidates across tenants/entity types in one run and touches ONLY the ones GSI8 actually surfaces", async () => {
test\unit\workers\delivery-record-purge.test.ts:200:    // tooRecent never appears in GSI8's `GSI8SK < now` query at all - not "scanned and skipped".
test\unit\workers\delivery-record-purge.test.ts:210:  it("drains multiple GSI8 pages within one run", async () => {
test\unit\workers\delivery-record-purge.test.ts:258:    expect(stored?.["GSI8PK"]).toBe("DLQ#DELIVERY_RECORD");
test\unit\subject\document-chasing.test.ts:5:describe("document-chasing domain — GSI3 discriminator (D-046: shared index, non-overlapping SK shape)", () => {
test\unit\subject\document-chasing.test.ts:6:  it("chasingGsi3Keys produces a GSI3SK shape that never matches the reminder parser", () => {
test\unit\subject\document-chasing.test.ts:8:    expect(keys.GSI3SK).toBe("TENANT#t1#CHASING#occ-1");
test\unit\subject\document-chasing.test.ts:9:    expect(() => parseGsi3Sk(keys.GSI3SK)).toThrow(/Malformed GSI3SK/);

 exited 1 in 2290ms:
src\runtime\aws\composition\tenant-purge.ts:91: * HELD_FOR_RECOVERY reconciliation). */
src\runtime\aws\composition\organization.ts:250:   * keeps working unchanged; when absent, `closeOrganization` is simply not built and the route is
src\runtime\aws\composition\organization.ts:288:  const closeOrganization = tenantPurgeStateMachineArn
src\runtime\aws\composition\organization.ts:322:    closeOrganization,
src\workers\tenant-purge\tenant-purge-sweep.ts:66:  /** D-127: only needed for the `HELD_FOR_RECOVERY` reconciliation branch below. */
src\workers\tenant-purge\tenant-purge-sweep.ts:89:  /** D-127: `HELD_FOR_RECOVERY` tenants for which this sweep pass completed a stalled
src\workers\tenant-purge\tenant-purge-sweep.ts:91:   * current `closureAttemptId` — but the `HELD_FOR_RECOVERY -> ACTIVE` write never committed,
src\workers\tenant-purge\tenant-purge-sweep.ts:94:  /** D-127: `HELD_FOR_RECOVERY` tenants whose execution state and lifecycle record disagree in a
src\workers\tenant-purge\tenant-purge-sweep.ts:118:      if (record.status === "HELD_FOR_RECOVERY") {
src\workers\tenant-purge\tenant-purge-sweep.ts:134:          // HELD_FOR_RECOVERY and never cleared on forward progress). The bare-tenantId fallback
src\workers\tenant-purge\tenant-purge-sweep.ts:170: * D-127 sweeper reconciliation for `HELD_FOR_RECOVERY` (round-7 of the approved design's
src\workers\tenant-purge\tenant-purge-sweep.ts:183: *   3. `record.status` is (re-)confirmed `HELD_FOR_RECOVERY` — nothing else already resolved it.
src\workers\tenant-purge\tenant-purge-sweep.ts:189: * given `HELD_FOR_RECOVERY` means nothing physical has purged yet in the overwhelmingly common
src\workers\tenant-purge\tenant-purge-sweep.ts:215:    // Condition 3 of the conjunction (record.status still HELD_FOR_RECOVERY) is enforced by
src\workers\tenant-purge\tenant-purge-sweep.ts:224:        from: "HELD_FOR_RECOVERY",
test\unit\tenant-lifecycle.test.ts:14:const ALL_STATUSES: TenantLifecycleStatus[] = ["ACTIVE", "HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"];
test\unit\tenant-lifecycle.test.ts:17:  it("allows every forward transition on the approved happy path (D-127: ACTIVE now goes through HELD_FOR_RECOVERY first)", () => {
test\unit\tenant-lifecycle.test.ts:18:    expect(canTransition("ACTIVE", "HELD_FOR_RECOVERY")).toBe(true);
test\unit\tenant-lifecycle.test.ts:19:    expect(canTransition("HELD_FOR_RECOVERY", "DELETING")).toBe(true);
test\unit\tenant-lifecycle.test.ts:26:  it("D-127: never allows reverting to ACTIVE from any state EXCEPT the one named exception, HELD_FOR_RECOVERY", () => {
test\unit\tenant-lifecycle.test.ts:28:      if (from === "HELD_FOR_RECOVERY") continue;
test\unit\tenant-lifecycle.test.ts:31:    expect(canTransition("HELD_FOR_RECOVERY", "ACTIVE")).toBe(true);
test\unit\tenant-lifecycle.test.ts:41:  it("rejects skipping stages (e.g. DELETING straight to VERIFIED, ACTIVE straight to PURGING/DELETING, HELD_FOR_RECOVERY straight to QUIESCING)", () => {
test\unit\tenant-lifecycle.test.ts:46:    expect(canTransition("HELD_FOR_RECOVERY", "QUIESCING")).toBe(false);
test\unit\tenant-lifecycle.test.ts:47:    expect(canTransition("HELD_FOR_RECOVERY", "PURGING")).toBe(false);
test\unit\tenant-lifecycle.test.ts:50:  it("allows entering BLOCKED/HELD from any of the mid-cascade states, including HELD_FOR_RECOVERY (D-127)", () => {
test\unit\tenant-lifecycle.test.ts:51:    for (const from of ["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED"] as const) {
test\unit\tenant-lifecycle.test.ts:62:  it("allows resuming from BLOCKED/HELD back to exactly the state it was blocked from, never elsewhere, including the new HELD_FOR_RECOVERY case (D-127)", () => {
test\unit\tenant-lifecycle.test.ts:67:    expect(canTransition("HELD", "HELD_FOR_RECOVERY", "HELD_FOR_RECOVERY")).toBe(true);
test\unit\tenant-lifecycle.test.ts:68:    // Resuming from a HELD_FOR_RECOVERY-originated hold must land on HELD_FOR_RECOVERY, never
test\unit\tenant-lifecycle.test.ts:71:    expect(canTransition("HELD", "ACTIVE", "HELD_FOR_RECOVERY")).toBe(false);
test\unit\tenant-lifecycle.test.ts:72:    expect(canTransition("HELD", "DELETING", "HELD_FOR_RECOVERY")).toBe(false);
test\unit\tenant-lifecycle.test.ts:77:    expect(() => assertValidTransition("ACTIVE", "HELD_FOR_RECOVERY")).not.toThrow();
test\unit\tenant-lifecycle.test.ts:78:    expect(() => assertValidTransition("HELD_FOR_RECOVERY", "ACTIVE")).not.toThrow();
src\workers\tenant-purge\lifecycle-transition.ts:52:  /** D-127: true ONLY for the one legal backward edge (`from: "HELD_FOR_RECOVERY", to:
src\workers\tenant-purge\lifecycle-transition.ts:83:  if (input.from === "HELD_FOR_RECOVERY" && input.to === "DELETING" && record.status === "ACTIVE") {
test\unit\system-mutation.test.ts:31:  it("moves ACTIVE -> HELD_FOR_RECOVERY for real, via occ.ts's OCC-fenced Update, stamping recoveryDeadline/closureAttemptId", async () => {
test\unit\system-mutation.test.ts:40:      to: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:49:    expect(record?.status).toBe("HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:55:  it("moves HELD_FOR_RECOVERY -> DELETING for real (30-day deadline firing)", async () => {
test\unit\system-mutation.test.ts:57:    await seedLifecycle(store, "tenant-1", "HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:59:    await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "DELETING", expectedVersion: 1 });
test\unit\system-mutation.test.ts:65:  it("moves HELD_FOR_RECOVERY -> ACTIVE (cancellation) for real, and clears recoveryDeadline/closureAttemptId/executionArn", async () => {
test\unit\system-mutation.test.ts:71:      status: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:80:    await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "ACTIVE", expectedVersion: 1 });
test\unit\system-mutation.test.ts:91:  it("moves HELD_FOR_RECOVERY -> HELD (legal hold) WITHOUT clearing recoveryDeadline - preserves the original countdown", async () => {
test\unit\system-mutation.test.ts:97:      status: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:109:      from: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:119:    expect(record?.blockedFrom).toBe("HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:124:    // And resuming from the lifted hold goes back to HELD_FOR_RECOVERY, still preserving it.
test\unit\system-mutation.test.ts:130:      to: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:132:      blockedFrom: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:135:    expect(resumed?.status).toBe("HELD_FOR_RECOVERY");
test\unit\system-mutation.test.ts:168:        to: "HELD_FOR_RECOVERY",
test\unit\system-mutation.test.ts:272:      to: "HELD_FOR_RECOVERY",
test\unit\workers\delivery-record-purge.test.ts:77:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\core-user-data-purge.test.ts:76:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\invitation-purge.test.ts:125:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\membership-purge.test.ts:96:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\quota-telemetry-purge.test.ts:70:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\security-audit-purge.test.ts:61:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\transient-purge.test.ts:178:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING", "VERIFIED", "DELETED", "BLOCKED", "HELD"])(
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:148:describe("runTenantPurgeSweep - HELD_FOR_RECOVERY reconciliation (D-127 round-7 strict conjunction)", () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:151:  it("never blindly re-launches a HELD_FOR_RECOVERY tenant via the generic orphan-repair path, however stale", async () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:153:    const lifecycle = scanOf(record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN }));
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:164:  it("completes a stalled cancellation: ABORTED + matching closureAttemptId + still HELD_FOR_RECOVERY -> restores ACTIVE", async () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:166:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, version: 3 });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:185:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:196:    expect(after?.status).toBe("HELD_FOR_RECOVERY"); // untouched
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:201:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-2", executionArn: EXECUTION_ARN });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:214:    expect(after?.status).toBe("HELD_FOR_RECOVERY");
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:217:  it("repairs (re-StartExecution) a HELD_FOR_RECOVERY record that never got an executionArn attached, without describing/alarming", async () => {
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:219:    const lifecycle = scanOf(record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1" }));
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:233:    const rec = record("t-held", "HELD_FOR_RECOVERY", "2026-01-01T00:00:00.000Z", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, version: 1 });
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:236:    // HELD_FOR_RECOVERY before this sweep pass's write lands.
test\unit\workers\tenant-purge\tenant-purge-sweep.test.ts:237:    await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: "t-held", from: "HELD_FOR_RECOVERY", to: "DELETING", expectedVersion: 1 });
test\unit\workers\tenant-purge\lifecycle-transition.test.ts:127:  describe("D-127: HELD_FOR_RECOVERY -> DELETING finding the record already back at ACTIVE (cancelled)", () => {
test\unit\workers\tenant-purge\lifecycle-transition.test.ts:134:        { tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "DELETING" },
test\unit\workers\tenant-purge\lifecycle-transition.test.ts:153:        advanceTenantLifecycle({ store, reader: readerFor(store), tableName: TABLE }, { tenantId: "tenant-1", from: "HELD_FOR_RECOVERY", to: "DELETING" }),
test\unit\organization\close-organization.test.ts:56:  it("moves ACTIVE -> HELD_FOR_RECOVERY, stamps recoveryDeadline/closureAttemptId, and starts the purge execution named by tenantId-closureAttemptId", async () => {
test\unit\organization\close-organization.test.ts:63:    expect(result).toEqual({ tenantId: "org-1", status: "HELD_FOR_RECOVERY", transitioned: true });
test\unit\organization\close-organization.test.ts:65:    expect(record?.status).toBe("HELD_FOR_RECOVERY");
test\unit\organization\close-organization.test.ts:92:  it.each(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING"] as const)("re-launches the execution for an in-flight %s tenant without writing anything", async (status) => {
test\unit\organization\close-organization.test.ts:124:    // The competing close() commits ACTIVE -> HELD_FOR_RECOVERY (with ITS OWN closureAttemptId,
test\unit\organization\close-organization.test.ts:135:          to: "HELD_FOR_RECOVERY",
test\unit\organization\cancel-organization-closure.test.ts:93:  it("cancels a HELD_FOR_RECOVERY closure: stops the execution, then restores ACTIVE, clearing attempt-scoped fields", async () => {
test\unit\organization\cancel-organization-closure.test.ts:96:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, recoveryDeadline: "2026-09-30T00:00:00.000Z" });
test\unit\organization\cancel-organization-closure.test.ts:116:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:127:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:138:    expect(record?.status).toBe("HELD_FOR_RECOVERY"); // untouched
test\unit\organization\cancel-organization-closure.test.ts:144:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN, version: 1 });
test\unit\organization\cancel-organization-closure.test.ts:148:        await transitionTenantLifecycle({ store, tableName: TABLE, tenantId: TENANT_ID, from: "HELD_FOR_RECOVERY", to: "DELETING", expectedVersion: 1 });
test\unit\organization\cancel-organization-closure.test.ts:162:    "refuses a %s tenant outright - accepts EXCLUSIVELY HELD_FOR_RECOVERY",
test\unit\organization\cancel-organization-closure.test.ts:173:  it("denies ADMIN via authorizeCancelClosure (OWNER-only, same tier as organization:close)", async () => {
test\unit\organization\cancel-organization-closure.test.ts:176:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:186:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:194:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:204:    await seedLifecycle(store, "HELD_FOR_RECOVERY", { closureAttemptId: "attempt-1", executionArn: EXECUTION_ARN });
test\unit\organization\cancel-organization-closure.test.ts:210:  it("fails with OrganizationClosureUnavailableError when HELD_FOR_RECOVERY has no executionArn/closureAttemptId attached yet, rather than guessing a StopExecution target", async () => {
test\unit\organization\cancel-organization-closure.test.ts:213:    await seedLifecycle(store, "HELD_FOR_RECOVERY");
src\modules\identity\domain\authorization.ts:89:  | "organization:close"
src\modules\identity\domain\authorization.ts:91:  // `HELD_FOR_RECOVERY`. Same OWNER_ROLES tier as `organization:close` — reversing the most
src\modules\identity\domain\authorization.ts:95:  // `authorizeCancelClosure()` below, NOT the generic `authorize()` — the caller
src\modules\identity\domain\authorization.ts:98:  // `HELD_FOR_RECOVERY` tenant is definitionally not), so `authorize()`'s own `AuthorizationInput`
src\modules\identity\domain\authorization.ts:228: * `organization:update-settings` (B2B-10), `organization:close` (W3-07/D-124). */
src\modules\identity\domain\authorization.ts:275:  "organization:close": OWNER_ROLES,
src\modules\identity\domain\authorization.ts:378:   * `HELD_FOR_RECOVERY` tenant in the first place (see the `organization:cancel-close` doc
src\modules\identity\domain\authorization.ts:391: *     `HELD_FOR_RECOVERY` by definition on this path.
src\modules\identity\domain\authorization.ts:403:export function authorizeCancelClosure(input: CancelClosureAuthorizationInput): void {
src\modules\organization\http\organization-lifecycle-handlers.ts:36:  closeOrganization: CloseOrganizationService;
src\modules\organization\http\organization-lifecycle-handlers.ts:38:   * route is simply unreachable (like `closeOrganization` above) when absent. */
src\modules\organization\http\organization-lifecycle-handlers.ts:93:    const result = await deps.closeOrganization.close(context);
src\modules\organization\http\organization-lifecycle-handlers.ts:101: * identity-resolution path (see that file's header): a `HELD_FOR_RECOVERY` tenant cannot resolve
src\shared\tenant-lifecycle\tenant-purge-execution-describer.ts:3: * execution's terminal/live status by ARN. Used ONLY by the sweeper's `HELD_FOR_RECOVERY`
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:25: *   ACTIVE -> HELD_FOR_RECOVERY -> DELETING -> QUIESCING -> PURGING -> VERIFIED -> DELETED
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:26: * Any of HELD_FOR_RECOVERY/DELETING/QUIESCING/PURGING/VERIFIED can additionally move to
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:28: * SAME state once remediated. For `HELD_FOR_RECOVERY` specifically, a LEGAL HOLD (as opposed to
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:35: * `HELD_FOR_RECOVERY -> ACTIVE` (cancellation, `CancelOrganizationClosureService`) is the
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:39: * residue in place, it never re-opens the record), and no state other than `HELD_FOR_RECOVERY`
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:44:  | "HELD_FOR_RECOVERY"
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:65:  /** D-127: ISO timestamp of when `HELD_FOR_RECOVERY` expires into `DELETING`. Computed once,
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:66:   * at the moment `ACTIVE -> HELD_FOR_RECOVERY` commits — never recalculated by the Step
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:71:  /** D-127: a fresh UUID minted on every `ACTIVE -> HELD_FOR_RECOVERY` transition — identifies
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:103:  ACTIVE: new Set(["HELD_FOR_RECOVERY"]),
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:104:  // HELD_FOR_RECOVERY -> ACTIVE (cancellation) is NOT listed here — it is the one named
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:106:  HELD_FOR_RECOVERY: new Set(["DELETING", "BLOCKED", "HELD"]),
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:119: * would re-enter ACTIVE from anywhere OTHER than `HELD_FOR_RECOVERY` (D-127's single named
src\shared\tenant-lifecycle\tenant-lifecycle-record.ts:125:  if (to === "ACTIVE") return from === "HELD_FOR_RECOVERY"; // D-127: the ONLY edge back to ACTIVE
src\shared\tenant-lifecycle\system-mutation.ts:91:       * `ACTIVE -> HELD_FOR_RECOVERY` — stamps the two identifiers a closure attempt needs.
src\shared\tenant-lifecycle\system-mutation.ts:99:       * that is still `HELD_FOR_RECOVERY`, so `CancelOrganizationClosureService` can later call
src\shared\tenant-lifecycle\system-mutation.ts:102:       * conditioned on the record still being `HELD_FOR_RECOVERY` for this exact
src\shared\tenant-lifecycle\system-mutation.ts:199:        // D-127: entering HELD from HELD_FOR_RECOVERY (legal hold during the recovery window)
src\shared\tenant-lifecycle\system-mutation.ts:203:      } else if (op.to === "HELD_FOR_RECOVERY" && op.from === "ACTIVE") {
src\shared\tenant-lifecycle\system-mutation.ts:204:        // D-127: ACTIVE -> HELD_FOR_RECOVERY (CloseOrganizationService) is the only transition
src\shared\tenant-lifecycle\system-mutation.ts:209:          throw new Error("SystemMutation \"LIFECYCLE_TRANSITION\" ACTIVE -> HELD_FOR_RECOVERY requires recoveryDeadline and closureAttemptId.");
src\shared\tenant-lifecycle\system-mutation.ts:215:        // D-127: HELD_FOR_RECOVERY -> ACTIVE (CancelOrganizationClosureService) — the closure
src\shared\tenant-lifecycle\system-mutation.ts:283:              ":held": "HELD_FOR_RECOVERY",
src\shared\tenant-lifecycle\system-mutation.ts:419: * D-127: attaches `executionArn` to the CURRENT `HELD_FOR_RECOVERY` attempt, after
test\unit\identity\authorization.test.ts:2:import { authorize, authorizeCancelClosure, AuthorizationDeniedError } from "../../../src/modules/identity/domain/authorization.js";
test\unit\identity\authorization.test.ts:142:// D-127: authorizeCancelClosure() is DELIBERATELY not exercised via authorize() above - it takes
test\unit\identity\authorization.test.ts:144:// because a HELD_FOR_RECOVERY tenant cannot resolve one (see authorization.ts's doc comment).
test\unit\identity\authorization.test.ts:145:describe("authorizeCancelClosure() (D-127 - dedicated primitive, not authorize())", () => {
test\unit\identity\authorization.test.ts:149:    expect(() => authorizeCancelClosure(ALLOWED)).not.toThrow();
test\unit\identity\authorization.test.ts:154:    expect(() => authorizeCancelClosure({ ...ALLOWED, identityStatus: "SUSPENDED" })).toThrow(AuthorizationDeniedError);
test\unit\identity\authorization.test.ts:158:    expect(() => authorizeCancelClosure({ ...ALLOWED, membershipStatus })).toThrow(AuthorizationDeniedError);
test\unit\identity\authorization.test.ts:161:  it.each(["ADMIN", "MEMBER", "VIEWER"] as const)("denies %s - OWNER-only, same tier as organization:close", (membershipRole) => {
test\unit\identity\authorization.test.ts:162:    expect(() => authorizeCancelClosure({ ...ALLOWED, membershipRole })).toThrow(AuthorizationDeniedError);
test\unit\identity\authorization.test.ts:167:      authorizeCancelClosure({ ...ALLOWED, membershipRole: "MEMBER" });
test\unit\identity\authorization.test.ts:168:      throw new Error("expected authorizeCancelClosure to throw");
test\unit\identity\authorization.test.ts:177:      authorizeCancelClosure({ identityStatus: "SUSPENDED", membershipStatus: "REMOVED", membershipRole: "MEMBER" });
test\unit\identity\authorization.test.ts:178:      throw new Error("expected authorizeCancelClosure to throw");
src\modules\organization\application\close-organization.ts:8: * `ACTIVE -> HELD_FOR_RECOVERY` (was `ACTIVE -> DELETING` pre-D-127 — the 30-day quarantine is now
src\modules\organization\application\close-organization.ts:22: * (`HELD_FOR_RECOVERY`/`DELETING`/`QUIESCING`/`PURGING`), including when this call performed no
src\modules\organization\application\close-organization.ts:67: * repeats the idempotent launch. D-127: `HELD_FOR_RECOVERY` joins this set (design finding 4 —
src\modules\organization\application\close-organization.ts:68: * without it, a repeated close() after `ACTIVE -> HELD_FOR_RECOVERY` commits but `StartExecution`
src\modules\organization\application\close-organization.ts:71:const IN_FLIGHT_STATUSES: ReadonlySet<TenantLifecycleStatus> = new Set(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING"]);
src\modules\organization\application\close-organization.ts:76:  /** False when the record was already HELD_FOR_RECOVERY+ and this call only repeated the launch. */
src\modules\organization\application\close-organization.ts:92:    authorize({ context: ctx, action: "organization:close", resource: { tenantId } });
src\modules\organization\application\close-organization.ts:113:          to: "HELD_FOR_RECOVERY",
src\modules\organization\application\close-organization.ts:141:      // must have one, since only ACTIVE -> HELD_FOR_RECOVERY ever creates the in-flight states
src\modules\organization\application\close-organization.ts:156:    return { tenantId, status: transitioned ? "HELD_FOR_RECOVERY" : record.status, transitioned };
src\modules\organization\application\cancel-organization-closure.ts:6: * The real cancellation path for a tenant closure still inside its 30-day `HELD_FOR_RECOVERY`
src\modules\organization\application\cancel-organization-closure.ts:8: * tenant in `HELD_FOR_RECOVERY` is, by construction, not `ACTIVE`, and every normal resolution
src\modules\organization\application\cancel-organization-closure.ts:14: * and calls `authorizeCancelClosure()` — never `authorize()` — for the same reason.
src\modules\organization\application\cancel-organization-closure.ts:18: * could advance `HELD_FOR_RECOVERY -> DELETING` (deadline firing) at any moment — a genuine race
src\modules\organization\application\cancel-organization-closure.ts:21: * the lifecycle record for this attempt, so the OCC-fenced `HELD_FOR_RECOVERY -> ACTIVE` write
src\modules\organization\application\cancel-organization-closure.ts:24:import { authorizeCancelClosure } from "../../identity/domain/authorization.js";
src\modules\organization\application\cancel-organization-closure.ts:92:    authorizeCancelClosure({
src\modules\organization\application\cancel-organization-closure.ts:98:    // 3. Accept EXCLUSIVELY HELD_FOR_RECOVERY — any other lifecycle status is refused outright,
src\modules\organization\application\cancel-organization-closure.ts:105:    if (record.status !== "HELD_FOR_RECOVERY") {
src\modules\organization\application\cancel-organization-closure.ts:112:      // Genuinely unexpected: every ACTIVE -> HELD_FOR_RECOVERY transition stamps
src\modules\organization\application\cancel-organization-closure.ts:137:        from: "HELD_FOR_RECOVERY",
src\runtime\aws\handlers\memberships-handler.ts:48:const lifecycleDeps: OrganizationLifecycleHttpDeps | undefined = membership.closeOrganization
src\runtime\aws\handlers\memberships-handler.ts:49:  ? { resolver, closeOrganization: membership.closeOrganization, cancelOrganizationClosure: membership.cancelOrganizationClosure }
src\runtime\aws\handlers\tenant-purge-sweeper-handler.ts:49:      // D-127: HELD_FOR_RECOVERY execution/record state disagreement outside the one safe-to-repair
src\runtime\aws\handlers\tenant-purge-sweeper-handler.ts:52:      logger.error("tenant purge sweep found ambiguous HELD_FOR_RECOVERY state - never auto-restored", { tenantsAmbiguous: result.tenantsAmbiguous });
docs/architecture/cost-model.md:54:Este documento modela custo esperado sob operação normal; os controles que protegem contra custo **anômalo** (G6, COST-004/005) já estão decididos em `architecture-fase3-consolidada.md` §14 e não são redecididos aqui — apenas referenciados para rastreabilidade: AWS Budgets com alarme em 80%/100% do teto por ambiente (COST-003), Cost Anomaly Detection, quota por tenant via token bucket em DynamoDB (`TenantQuota`, `data-model.md`), kill switch via AppConfig para operações caras (IA/OCR/WhatsApp). Este cost-model fornece os valores de referência (tabela acima) que alimentam os tetos configurados nesses Budgets — não os substitui.
infra\state-machines\tenant-purge.asl.json:387:      "Comment": "Deliberately a Fail, not a Succeed-with-a-flag: this is what raises the AWS/States ExecutionsFailed metric that the new CloudWatch alarm in this module watches, which is the only thing that gets a human's attention on a tenant stuck mid-purge. D-121 Rodada 3 Fix 7 established that no such alarm existed anywhere in infra/ before this wave - it had to be built, not reused.",
src\workers\document-request-recurrence\materializer.ts:9: * Source of "what's due": `store.scanActiveSeries` (cross-tenant `Scan`, same accepted cost
infra\modules\cost-budget\main.tf:3:# monthly ceiling meant to catch runaway cost early, not to model real unit economics.
src\modules\document-archive\application\guest-document-access-service.ts:257:    // bounds the cost of a pathological tenant rather than looping unbounded.
infra\modules\security-audit-observability\main.tf:50:resource "aws_cloudwatch_log_metric_filter" "authorization_denied_tenant_mismatch" {
infra\modules\security-audit-observability\main.tf:65:resource "aws_cloudwatch_metric_alarm" "authorization_tenant_boundary_denied" {
infra\modules\security-audit-observability\main.tf:68:  metric_name         = "SecurityAuthorizationTenantBoundaryDenied"
infra\modules\security-audit-observability\main.tf:80:  depends_on = [aws_cloudwatch_log_metric_filter.authorization_denied_tenant_mismatch]
infra\modules\security-audit-observability\outputs.tf:6:  value = aws_cloudwatch_metric_alarm.authorization_tenant_boundary_denied.alarm_name
infra\modules\tenant-purge-workflow\main.tf:71:  dimensions          = { StateMachineArn = aws_sfn_state_machine.tenant_purge.arn }
infra\modules\tenant-purge-workflow\main.tf:91:  dimensions          = { StateMachineArn = aws_sfn_state_machine.tenant_purge.arn }
infra\modules\security-audit-observability\tests\security_audit_observability.tftest.hcl:40:    condition     = length(aws_cloudwatch_log_metric_filter.authorization_denied_tenant_mismatch) == 7
infra\modules\security-audit-observability\tests\security_audit_observability.tftest.hcl:55:    condition     = contains(aws_cloudwatch_metric_alarm.authorization_tenant_boundary_denied.alarm_actions, "arn:aws:sns:us-east-1:123456789012:exptrk-test-alerts")
infra\modules\security-audit-observability\tests\security_audit_observability.tftest.hcl:65:    condition     = aws_cloudwatch_metric_alarm.authorization_tenant_boundary_denied.threshold == 1
infra\modules\security-audit-observability\tests\security_audit_observability.tftest.hcl:71:      for a in [aws_cloudwatch_metric_alarm.authorization_denied_burst, aws_cloudwatch_metric_alarm.authorization_tenant_boundary_denied, aws_cloudwatch_metric_alarm.global_index_access_denied] :
infra\modules\tenant-purge-workflow\tests\tenant_purge_workflow.tftest.hcl:215:    condition     = aws_cloudwatch_metric_alarm.executions_failed.dimensions["StateMachineArn"] == aws_sfn_state_machine.tenant_purge.arn
infra\modules\tenant-purge-workflow\tests\tenant_purge_workflow.tftest.hcl:219:    condition     = aws_cloudwatch_metric_alarm.executions_timed_out.dimensions["StateMachineArn"] == aws_sfn_state_machine.tenant_purge.arn
docs/architecture/decisions-log.md:21:| D-028 | Implementation Blueprint (seção 60) — componentes/módulos, interfaces, eventos/schemas, ordem de deploy, milestones, critérios de aceite; correção Type 1 do GSI3 (chave global do scheduler, exceção documentada a `data-model.md` §3); 7 lacunas do threat model incorporadas como requisito desde M0/M1 | Type 1 | 9.20 | 9.2 (exato) | **APPROVED** (8 rodadas: proposta independente, crítica cruzada, convergência, 5 rodadas de nota cega/correção pontual) | `implementation-blueprint.md` | 2026-08-19 |
docs/architecture/decisions-log.md:51:| D-031 | Rollback (alias+versão Lambda, manifesto S3, `rollback.yml` manual) e trilha de auditoria de segurança (negação de autorização + acesso GSI3/GSI6, taxonomia fechada de eventos) — 2 achados reais da rodada focada de auditoria (`full-audit-round1-focused-round2-summary.md`) | Type 1 | 9,1–9,3 | 9,2–9,4 | **APPROVED** (design + implementado + deployado + verificado em produção real, 2 protocolos Claude↔Codex completos) | `docs/architecture/reviews/{rollback-mechanism-design,security-audit-trail-design}/` | 2026-08-22 |
docs/architecture/decisions-log.md:59:| D-039 | Evolução estratégica, cluster 4: automated document chasing via agregados-irmãos (`DocumentChasingOccurrence`/`DocumentChasingIntent`), nunca generalizando `NotificationIntent`/`ReminderOccurrence` já em produção — mesmo precedente já aplicado em M7 (D-035); GSI3 reaproveitado sob condição de mini-revisão de capacidade | Type 1 (nível 5) | 9,1 | 9,2 | **APPROVED** (design de domínio via protocolo Claude↔Codex completo, MCP) | `docs/architecture/roadmap-evolution/06-domain-model-automated-chasing.md` | 2026-08-23 |
docs/architecture/decisions-log.md:66:| D-046 | Mini-revisão de capacidade de GSI3 para Automated Document Chasing (pré-requisito registrado em D-039 antes de implementar cluster 4) — pico orgânico combinado estimado em Stage 5 (~15,2/s) fica ~220× abaixo do SLO de drenagem de pico extremo já fechado (`slo.md` §3, ~3.333/s); GSI3 reaproveitado sem shards adicionais/índice paralelo. Duas ações concretas exigidas independente do resultado: entendimento documentado do GSI3 atualizado de "scheduler de `ReminderOccurrence`" para "scheduler global discriminado por `entityType`" (`data-model.md`); alarmes de GSI3 segmentados por `entityType` desde o primeiro dia. Revisão a refazer quando CSV import de `DocumentRequest` existir (muda o pior caso de concentração) | Verificação quantitativa de pré-requisito (não decisão de arquitetura nova — não passa pelo protocolo Claude↔Codex) | N/A | N/A | **FECHADO** (pré-requisito de D-039 satisfeito, cluster 4 liberado para implementação) | `docs/architecture/roadmap-evolution/12-automated-chasing-capacity-review.md` | 2026-08-23 |
docs/architecture/decisions-log.md:73:| D-053 | Refina D-034/blueprint §4.2/§23.1: "BFF de sessão" restrito a `/session/refresh`/`/session/logout` nunca decidiu como o SPA autenticaria as rotas de recurso existentes, protegidas por JWT authorizer nativo do API Gateway (só valida `Authorization: Bearer`, não lê cookie — `infra/modules/api-gateway/main.tf:47`). Decidido via protocolo Claude↔Codex completo (4 rodadas: proposta independente convergente → crítica cruzada achou 6 furos reais na proposta Claude, todos corrigidos com acordo explícito → reconciliação → revisão do Codex à reconciliação achou mais 2 furos reais, corrigidos antes de fechar): **Full BFF** — toda chamada de recurso passa por um BFF Lambda que anexa Bearer obtido/renovado server-side; browser nunca recebe token OAuth em nenhuma forma, só cookie de sessão opaco `__Host-`. JWT authorizer nativo e rotas de recurso permanecem inalterados e continuam aceitando Bearer direto (Full BFF é aditivo, não revoga o mecanismo existente — mantém caminho para mobile/API futuro). Decisões fechadas: PKCE + validação de `state` no callback OAuth (2ª lacuna achada só na revisão final, distinta e complementar); CSRF via cookie não-`HttpOnly` espelhado em header; refresh 100% transparente ao frontend com reuse detection real (geração local do refresh token, `ConditionExpression` OCC); sessão em DynamoDB via ponteiro tenantless novo (`SESSION#<cookieHandleHash>`/POINTER, 3ª ocorrência do padrão de `IdentityMapping`/`GuestTokenPointer`) + `LoginAttempt` efêmero (4ª ocorrência) para `state`/`codeVerifier`, sem tocar o `DeviceSession` existente; refresh token cifrado via **CMK dedicada** com `kms:Decrypt` só para a role do BFF (não `aws/dynamodb` gerenciada — corrigido em rodada 4: SSE de tabela não isola contra Lambda de recurso que já tem `dynamodb:GetItem` geral, mesmo princípio de isolamento explícito já usado para GSI3/GSI6); correlation ID NÃO propagado por header ao backend (nenhum handler hoje aceita isso, mudaria trust boundary de Lambdas já revisadas sem mecanismo anti-falsificação — correlação fica por log-join); handlers de recurso/`RequestContext`/matriz de autorização permanecem totalmente inalterados; composição de view model por tela explicitamente fora de escopo | Type 1 (nível 5-6, segurança/fronteira de sistema) | 9,2 | 9,3 | **APPROVED** (design pronto para implementação, aguardando decisão do Marcelo sobre início — mesmo padrão de D-035/M7) | `docs/architecture/reviews/bff-full-vs-session-design/claude-reconciliation-final-design.md` | 2026-08-23 |
docs/architecture/decisions-log.md:74:| D-054 | Amendment ao D-053 (Full BFF), a partir de auditoria adversarial independente trazida por Marcelo com 16 pontos técnicos específicos — protocolo Claude↔Codex completo (Rodada A: Claude verifica os 16 pontos contra código real + fontes externas atuais, RFC 9700/RFC 10017 confirmados; Rodada B: Codex achou 4 furos reais nas correções da Rodada A; Rodada C: furos corrigidos, inclusive verificação externa dedicada de uma alegação factual do Codex antes de aceitá-la; Rodada D: Codex confirma). **Não reabre Full BFF** — endurece 6 pontos antes da implementação: (1) mecanismo de refresh reescrito — rotação nativa do Cognito (`RefreshTokenRotation`, grace period ~30s) como fonte de verdade de "replay real" em vez de contador de geração local (que criava falso-positivo sob concorrência legítima), com lease BFF-side (fencing token, adquirida antes da chamada ao Cognito) só como otimização de latência; exige remover `ALLOW_REFRESH_TOKEN_AUTH` do app client Cognito (mutuamente exclusivo com rotation, confirmado via documentação AWS) — vestigial para o Full BFF, que só usa OAuth2/Hosted UI; (2) sessão migrada de um ponteiro na tabela single-table para uma **tabela DynamoDB nova, dedicada, IAM-isolada só para o BFF** — achado real: a política `tenant_facing_read_write` já concede `GetItem`/`Query` na tabela base a ~20+ roles de Lambda de recurso, então cifrar só o refresh token (D-053 Rodada 4) não protegia o `codeVerifier`/metadados de sessão, todos legíveis por qualquer handler existente; mesmo princípio de isolamento explícito já usado para GSI3/GSI6, aplicado a uma tabela; (3) dois cookies com `SameSite` diferenciado — login (`Lax`, sobrevive ao redirect cross-site do Hosted UI) e sessão (`Strict`, só nasce após o callback validado via redirect same-origin); (4) lifetime de sessão explícito — `absoluteExpiresAt` fixo na criação (nunca estendido por refresh) + idle timeout via `purgeAfterTtl`, ausente de todo documento normativo antes desta auditoria; (5) `RevokeToken` best-effort no logout por dispositivo, sempre após a revogação local (que continua fonte de verdade); (6) `nonce` no `LoginAttempt`, `Sec-Fetch-Site`/`Origin` como camada extra de CSRF, allowlist explícita de headers do proxy, CloudFront (sem WAF) formalmente no escopo do milestone (WAF permanece no mesmo gate pré-produção de D-051, e desta vez funcionará — CloudFront suporta WAFv2, ao contrário do HTTP API v2). Achado colateral pré-existente (não introduzido por D-053): `DeviceSession`/logout por dispositivo nunca foi wireado a nenhum fluxo real desde M1 (`upsertDeviceSession()` só tem call site em teste) — escopo corrigido para não prometer enforcement de backend por dispositivo que não existe: para o browser, a tabela de sessão do BFF já é suficiente; `DeviceSession` fica como metadado auxiliar best-effort, nunca enforcement para Bearer direto | Type 1 (nível 5-6, refina D-053 sem reabri-lo) | 9,2 | 9,4 | **APPROVED como amendment** (design pronto para implementação; residual aceito por ambos os lados — validação real de rotation/Cognito, callback Hosted UI/CloudFront/cookies em browser real, teste IAM-negativo real — fica para a implementação, não bloqueia o design) | `docs/architecture/reviews/bff-full-vs-session-design/claude-adversarial-audit-round-a.md` | 2026-08-23 |
docs/architecture/decisions-log.md:80:| D-061 | W3-06 — desenho do mecanismo de purga real de `USER_DOCUMENT`/`Document`, protocolo Claude↔Codex completo em 6 rodadas (`docs/architecture/reviews/w3-06-user-document-purge-design/`). Mecanismo aprovado: claim por lease sobre GSI6 (reuso do idioma `WORKSTATE#PENDING/CLAIMED` já provado por `ReminderReconciliation`), `DocumentPurgeWorker` agendado (6h) apagando o objeto S3 real (`cleanObject` ou evidência de upload/malware — nunca `quarantineObject`, cujo `versionId` é sempre `""`) e a linha `Document` numa transação final que também grava um `DocumentPurgeReceipt` não sensível (`retentionClass: DELIVERY_RECORD`, autopurgado pelo mesmo worker); `legalHold?: boolean` novo campo com exclusão mútua estruturalmente garantida contra o claim via uma `extraCondition` normativa (`attribute_not_exists(GSI6PK) OR GSI6PK <> :purgeClaimed`) que todo escritor futuro de hold deve incluir; sem lifecycle S3 incondicional (rejeitado — destruiria documento ativo, já que o prazo é variável desde um evento, não desde a criação do objeto); rede de segurança é alarme de idade de candidato, não deleção automática; `buildVersionedUpdate` (`occ.ts`) ganha `extraConditions` aditivas com checagem de colisão de placeholder. Trajetória Codex: 5,8→7,4→7,8→8,3→8,9→**9,2** (aprovado). Claude final: **9,1/10**. Achados reais descobertos pela crítica adversarial que mudaram o mecanismo: TOCTOU S3-antes-de-DynamoDB (Rodada 1), `quarantineObject.versionId` sempre vazio + status sobrescrito por `DELETED` no soft-delete (Rodadas 2-3, ambos confirmados contra código real), corrida de `legalHold` pós-claim só fechada de fato na Rodada 6. Escopo explícito fora desta decisão: cascata DSR completa (W3-07), generalização do mecanismo para `entityType` arbitrário, workflow de aprovação de `legalHold` | Type 1 (nível 5-6, `AGENTS.md` §4, gate padrão 9.0) | 9,1/10 | 9,2/10 | **APROVADO E IMPLEMENTADO** (954 testes de backend, `terraform test` 15/15, `terraform plan` real contra `dev` limpo) | `docs/architecture/reviews/w3-06-user-document-purge-design/claude-reconciliation-final-design.md` | 2026-08-28 |
docs/architecture/decisions-log.md:101:| D-081 | W3-07 — sessão dedicada ao purge/sweeper (Codex D-080 recomendou explicitamente redirecionar esforço para cá, dado retorno decrescente no fence de admissão em 8,8/10). Implementa o pipeline durável de purga tenant-wide, seguindo o design aprovado (`claude-analysis-active-only-fence.md`, seções de quiescence/purge/S3 versionado) e reusando o padrão `DocumentPurgeWorker`/GSI6 na medida do aplicável: (1) `system-mutation.ts`'s `PURGE_DELETE` implementado de verdade (era `SystemMutationNotImplementedError`) — `Delete` idempotente (`attribute_not_exists(PK) OR begins_with(PK, "TENANT#<id>#")`), sem condição de versão (um alvo de purga não é mais mutado por escritor fenceado nenhum na fase PURGING); (2) `workers/tenant-purge/dynamo-tenant-purge.ts` — Scan (não Query: nenhuma GSI indexada só por tenantId existe hoje, confirmado por leitura de `infra/modules/dynamo-table/main.tf` e `w3-07-writer-inventory.md`) + purge via `PURGE_DELETE`, excluindo `TenantLifecycleRecord`/`IdentityMapping` explicitamente na camada de lógica pura (defesa em profundidade, independente do filtro do scan); (3) `workers/tenant-purge/session-table-tenant-purge.ts` — purge do `bff-session-table` (tabela física separada, D-054, sem GSI): só linhas `Session` (têm `tenantId`); `LoginAttempt` não tem `tenantId` por design (rate-limit antes de resolver tenant) — gap real documentado, não mascarado, mesma classe do gap já registrado em D-080 para o fence de admissão; (4) `workers/tenant-purge/s3-tenant-purge.ts` — `ListObjectVersions` paginado, versões E delete markers, `DeleteObjects.Errors[]` tratado com retry (nunca HTTP 200 tratado como prova de sucesso completo), `ListMultipartUploads` + abort de uploads incompletos, checkpoint/resume via `keyMarker`/`versionIdMarker`/`uploadKeyMarker`/`uploadIdMarker`; (5) `workers/tenant-purge/purge-tenant.ts` — ponto de entrada composable único, reporta `SUCCESS`/`PARTIAL`/`FAILED` (nunca reporta sucesso quando um sub-purge S3 tem erros não resolvidos). Adaptadores reais AWS: `shared/dynamodb/tenant-purge-scan.ts` (Scan real para tabela principal + bff-session-table, mora em `shared/dynamodb/` por ser cross-module, respeitando a regra `no-raw-dynamodb-writes-outside-lanes`), `shared/s3/tenant-purge-s3-adapter.ts` (S3 real, não coberto pela mesma regra dependency-cruiser). **Gap de infra real encontrado, não corrigido nesta sessão** (documentado em `NEXT_SESSION_PROMPT.md`): o bucket de artefatos OCR/extraction não tem um módulo Terraform dedicado com `aws_s3_bucket_versioning` confirmado como os buckets quarantine/clean/import têm (`infra/modules/document-buckets/main.tf`, `infra/modules/import-bucket/main.tf`) — o mecanismo de purga funciona corretamente também sobre um bucket não-versionado (cada objeto aparece como uma única "versão" `null`), mas a garantia de versionamento do design aprovado não está confirmada para este bucket especificamente. Idempotência verificada por teste em todas as 4 peças (re-execução após purga completa é no-op limpo). Nada de orquestrador/Step Functions/wiring de trigger QUIESCING→PURGING construído nesta sessão (fora de escopo explícito) — ver `NEXT_SESSION_PROMPT.md` para o que falta. 1066 testes de backend passando (era 1038, +28 novos: 24 dos 4 módulos de purga + 4 de `PURGE_DELETE` em `system-mutation.test.ts`), typecheck/lint/check-boundaries/check-docs limpos, zero regressão | Type 1 (nível 5-6, `AGENTS.md` §4 — segurança/privacidade/modelo de dados, LGPD) | N/A — implementação seguindo design já aprovado (D-066, 9,2/9,1), não nova rodada de design com nota cega dupla | N/A — nenhuma rodada Codex executada nesta sessão sobre o purge pipeline (ver "próxima ação" abaixo) | **IMPLEMENTED + UNIT TESTED** (lógica pura + adaptadores reais compilando/lint limpos; adaptadores reais NÃO testados contra AWS real nesta sessão — sem ambiente de integração disponível) — nenhuma revisão adversarial Codex ainda sobre este código, ao contrário do fence de admissão que já teve 4 rodadas; recomendação explícita: uma rodada Codex dedicada ao purge pipeline (partial-failure handling, checkpoint/resume, isolamento de tenant, completude de bucket versionado) é o próximo passo de maior valor antes de considerar este pipeline pronto para produção | `src/shared/tenant-lifecycle/system-mutation.ts`, `src/workers/tenant-purge/*.ts`, `src/shared/dynamodb/tenant-purge-scan.ts`, `src/shared/s3/tenant-purge-s3-adapter.ts`, `test/unit/workers/tenant-purge/*.ts`, `test/unit/system-mutation.test.ts` | 2026-08-29 |
docs/architecture/decisions-log.md:109:| D-089 | Multi-User B2B, Wave B2B-3 (chunk 1/N) — precondição de infra do GSI4 `MembershipByUser` implementada (docs/architecture/multi-user-b2b-physical-model.md §6): `GSI4` removido de `locals.tenant_facing_index_names` em `infra/modules/dynamo-table/main.tf` (estava lá desde sempre com zero consumidor real — inofensivo até agora, mas a nova semântica de `MembershipByUser` cruza tenants por design e não pode ficar acessível pela role tenant-facing geral) e política restrita dedicada `gsi4_read` + output `gsi4_read_policy_json` adicionados, mesmo padrão de `gsi3_read`/`gsi6_read` (`AGENTS.md` §7). Nenhuma role consome `gsi4_read_policy_json` ainda — Wave B2B-3 não adiciona nenhum consumidor real (BFF/`RequestContextResolver`/onboarding, que consumirão de fato, são Wave B2B-5/B2B-6). Dois novos `run` blocks em `infra/modules/dynamo-table/tests/dynamo_table_policy.tftest.hcl` (`tenant_facing_policy_excludes_gsi4`, `gsi4_narrow_policy_references_only_its_own_index`), mesmo padrão estrutural dos runs já existentes para GSI3/GSI6. | Type 5 (`change-risk-scale.md` — muda superfície de acesso IAM de um índice, mas implementação direta do design já `APPROVED` em D-086 §6, não uma decisão nova) | N/A — implementação direta, não decisão nova | N/A | **IMPLEMENTADO** — `terraform validate` limpo (módulo + root), `terraform test` via `aws --profile claude-dev` limpo (módulo 6/6 incl. os 2 novos runs; root 15/15, zero regressão nas 27 funções Lambda existentes) | `infra/modules/dynamo-table/main.tf`, `infra/modules/dynamo-table/outputs.tf`, `infra/modules/dynamo-table/tests/dynamo_table_policy.tftest.hcl`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
docs/architecture/decisions-log.md:144:| D-125 | Responsibility Reassignment on Member Removal — implementação real do mecanismo APROVADO em D-122 (Claude 9,1/Codex 9,1, 3 rodadas, `docs/architecture/reviews/responsibility-reassignment-scoping/`), mesmo padrão de D-096/D-102/D-108/D-113/D-124 (implementação direta de design já aprovado, nenhuma rodada Claude↔Codex nova para o mecanismo). **Precondição best-effort, explicitamente NÃO atômica**, em `RemoveMembershipService.remove()`/`LeaveOrganizationService.leave()`: antes da transação de remoção, checa se `targetUserId` ainda é `assigneeUserId` de algum `ExpirationItem` `ACTIVE` na organização e lança `ResponsibilityReassignmentRequiredError` em vez de prosseguir — uma leitura síncrona de decisão, nunca parte do `TransactWriteItems` (Round-3 "Estado final consolidado"). **Nova porta `AssignedActiveItemsLookup`** (`organization/ports/assigned-active-items-lookup.ts`, nome/forma do Round-3 — supersede o `SoleResponsibilityChecker` do Round-1) na CONSUMING module, implementada no composition root (`runtime/aws/composition/organization.ts`'s `buildAssignedActiveItemsLookup`, exportada para teste direto) — mesmo padrão estrutural de `expiration/ports/member-eligibility.ts`, mas na direção OPOSTA (primeira vez que `organization` lê dado de `expiration`). **Contrato de paginação da Correção 2 do Round-3, implementado literalmente**: pagina o GSI1 `TENANT#t#ITEMSTATUS#ACTIVE` até esgotar (`LastEvaluatedKey` até `undefined`), aplica `FilterExpression: assigneeUserId = :userId` a cada página, NUNCA usa `Limit` do DynamoDB como proxy de truncamento — `totalKnown` é a contagem real de todas as páginas, o teto de 20 aplica só à lista `itemIds` retornada, `truncated` é `totalKnown > itemIds.length`. GSI1 não está na taxonomia restrita de `security-audit.ts` (só GSI3/GSI6 exigem `security.global_index_access`) — nenhum evento novo de auditoria necessário. **Erro novo** `ResponsibilityReassignmentRequiredError` (`app-error.ts`, `BUSINESS_RULE`/`RESPONSIBILITY_REASSIGNMENT_REQUIRED`, `retryable: false`, `details: {targetUserId, itemIds (até 20), totalKnown, truncated}`), mapeado para 422 pelo `STATUS_BY_CATEGORY` genérico já existente (nenhuma mudança de handler necessária). **Ordem de checagem** em `remove-membership.ts`: autorização → tier OWNER (`OwnerTierChangeRequiresOwnerError`) → reatribuição de responsabilidade (novo) → transação (que ainda aplica `LastOwnerError` atomicamente) — a checagem de tier vence porque decide SE o caller pode agir, antes de qualquer pergunta sobre o estado do alvo; documentado inline com teste dedicado provando a ordem quando ambos os erros se aplicariam. **Escopo respeitado integralmente**: `ItemWatch`/watchers intocados (precedente B2B-11 mantido), nenhum bypass de emergência inventado (pergunta de produto real, deferida a Marcelo, não decidida aqui), nenhuma UI nova de reatribuição (a mutação `updateItem` já existente, já validada por `MemberEligibilityChecker`, é o mecanismo) — só paridade mínima de mensagem de erro no frontend (`isResponsibilityReassignmentRequiredError` em `api/errors.ts` + `Settings.tsx`'s `LeaveOrganizationSection`, mesma classe de polimento pequeno que D-120 fez para `LastOwnerError`). **G-V3**: 3 mutações reais verificadas e revertidas — (1) neutralizar a checagem `if (assigned.itemIds.length > 0)` em `remove-membership.ts` (1 teste falha); (2) trocar `truncated: totalKnown > itemIds.length` por `truncated: false` derivado de `itemIds.length` no adapter (1 teste falha — prova que `totalKnown` não é derivado do array capado); (3) remover o `while (exclusiveStartKey)` (travar em uma página só) no mesmo adapter (1 teste falha — prova paginação real até exaustão). Nenhuma mutação mascarada. **Sem infra/rota nova**: `remove-membership.ts`/`leave-organization.ts` já wireados a handlers HTTP existentes desde B2B-8 — confirmado por leitura, não assumido; `build:lambdas`/`terraform test` não exigidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos (as 2 falhas de `npm test` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes em `scripts/reset-dev-data.ts`, sem relação com esta mudança); backend 1249 testes passando / 137 arquivos (2 novos: `assigned-active-items-lookup.test.ts` com 4 testes, +6 testes novos em `membership-management.test.ts`), frontend 156 testes / 24 arquivos inalterado (só paridade de mensagem, sem teste novo de UI). Zero regressão real. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-122, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo de precondição best-effort real, porta + adapter reais, erro novo mapeado, paridade mínima de frontend | `src/modules/organization/ports/assigned-active-items-lookup.ts`, `src/modules/organization/application/{remove-membership,leave-organization}.ts`, `src/runtime/aws/composition/organization.ts`, `src/shared/errors/app-error.ts`, `frontend/src/api/errors.ts`, `frontend/src/routes/Settings.tsx`, `test/unit/organization/{assigned-active-items-lookup,membership-management}.test.ts`, `test/unit/identity/resolver.test.ts` | 2026-08-31 |
docs/architecture/decisions-log.md:163:| D-143 | Domínio Documental (arquivamento/versionamento/requisitos/guest upload/renovação) — arquitetura técnica `APPROVED` via protocolo Claude↔Codex, implementando as decisões funcionais D1–D10/C1–C6 já `APROVADO COMO DIREÇÃO FUNCIONAL INICIAL` pelo Marcelo (`docs/frontend/document-domain-functional-decisions.md`, não reaberto). Pedido explícito do Marcelo: analisar os 6 documentos de planejamento em `docs/frontend/document-domain-*.md`, pesquisar externamente, rodar o protocolo, só então implementar. **6 rodadas** (evolução de nota: 4,6→5,9→7,6→8,4→8,8→9,3): Rodada 1 (régua E-014 2,5/10, design 4,8/10, REABRIR) — sem URLs/datas/checklist reais, state machines eram enumeração de estados sem grafo comando/ator/precondição, ACL por responsável presumida (RBAC real é tenant-wide), guest/magic link com bloqueio crítico (TTL contraditório com prazo de Request, hash-só sem selector, `security-audit.ts` citado incorretamente), `Requirement.status` tratado como 100% derivado quando `NOT_APPLICABLE` é fato persistido, contradição direta com J9 (permitia remover Version `REJECTED`), recorrência perdendo identidade de série na correção; Rodada 2 (régua 5,4/10, design 5,9/10, REABRIR) — corrigiu grafo/E-014/guest 3-camadas/`DocumentVersionEvent`, mas introduziu colisão real com GSI3/GSI4 (já ocupados por reminder scheduler/membership, IAM isolado, confirmado por grep) — achado mais grave da rodada, incluindo uma correção factual rara (Codex havia alegado incorretamente que GSI2 já estava em uso; grep no código real mostrou GSI2/GSI5 livres); Rodada 3 (régua 5,4/10 mantida, design 7,6/10, REABRIR) — GSI2/GSI5 adotados, access patterns completos, mas fences entre itens não provavam vínculo real, `TransactWriteItems` não pode ler GSI dentro de si mesma, idempotência sem resultado original persistido, `pendingFileScans=0` não distinguia limpo de infectado, retenção com classes "equivalente"/"a definir" em vez de mapeamento real, recorrência sem fence de concorrência; Rodada 4 (8,4/10) — lista transacional de 10 ações formalizada, `assertReviewerOrAdmin()` como contrato próprio (não estende `authorize()`), `infectedFileScans` separado, mas condição idempotente usava atributo inexistente (`PK_SK`), retenção ainda incoerente com o texto real de `privacy-lgpd.md`, recorrência com lacuna real entre avanço de ponteiro e criação de Request; Rodada 5 (8,8/10) — 3 de 4 corrigidos (idempotência via `attribute_not_exists(PK)`/`(SK)`, lista de 10 ações literal, `materializeAttempt` atômico fechando a lacuna de recorrência), mas sub-prazos de retenção (7d/14d) apresentados como herdados quando eram extensão nova não presente no texto normativo; Rodada 6: Claude 9,3/Codex 9,3, **APPROVED** — sub-prazos assumidos explicitamente como decisão nova (não herança), erro editorial "5 classes" corrigido para 3 classes usadas das 9 reais de `privacy-lgpd.md` §4. **Pesquisa externa por decisão (E-014)**: SIM (guest/magic link — NIST SP 800-63B-4 §5.1.2, OWASP Forgot Password/Password Storage/Session Management/CSRF Cheat Sheets); SIM PARCIAL (armazenamento — princípio current/history de MongoDB/Cosmos DB Document Versioning Pattern, schema físico interno); NÃO (demais 7 decisões — composição de padrões já `APPROVED` no projeto). **9 decisões técnicas fechadas**: (1) state machines Document/DocumentVersion com grafo comando/ator/precondição/efeito transacional, fluxo interno comprimido C3 como transação única (elimina falha parcial); (2) armazenamento single-table com GSI1 discriminado + GSI2/GSI5 novos (confirmado livres, nunca GSI3/GSI4/GSI6 já ocupados), transação `acceptVersion` de até 10 ações com fences de vínculo fundidos e idempotência real (`payloadHash`+`resultSnapshot`); (3) `DocumentVersionEvent` append-only (distinto de `OutboxEvent`) como única auditoria, `DocumentVersion.state` única fonte de verdade mutável; (4) guest access em 3 camadas (`RequestAccessCredential` longa/revogável + `GuestSession` curta com interstitial obrigatório + submit idempotente), CSRF/Referrer-Policy/rate limit multidimensional; (5) `Requirement.applicability` como fato persistido, `evidenceVersionId` singular, status derivado deterministicamente + job diário de reindexação; (6) arquivos com scan individual (`pendingFileScans`/`infectedFileScans`), imutabilidade pós-`ACCEPTED`; (7) `REJECTED` nunca removível (corrige contradição com J9), retenção mapeada às classes reais de `privacy-lgpd.md` (`USER_DOCUMENT`/`SECURITY_AUDIT`/`TRANSIENT`); (8) recorrência com `seriesId`/`occurrenceId`/`requestId`/`attemptIndex`/`parentRequestId`, materialização atômica via `materializeAttempt`; (9) RBAC tenant-wide reaproveitado (`assertReviewerOrAdmin()` como camada de serviço separada), `extraction:confirm` reaproveitado para IA, download por Viewer (A1) genuinamente deixado aberto. **DESIGN-ONLY** — implementação real (Núcleo 1: Document/DocumentVersion/DocumentFile/DocumentVersionEvent) fica para sessão futura dedicada, mesmo padrão de D-121/D-127/D-136/D-139. | Type 1 (nível 5-6, novo domínio de dados/segurança sob E-014) | 9,3 | 9,3 | **APPROVED** (design, 6 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/document-domain-scoping/` (round1 a round6, `estado-final-consolidado.md`), `docs/frontend/document-domain-*.md` (base funcional, inalterada) | 2026-09-01 |
docs/architecture/decisions-log.md:164:| D-144 | Domínio Documental, Núcleo 1 (Document/DocumentVersion) — implementado e deployado de ponta a ponta, seguindo a arquitetura `APPROVED` em D-143. Módulo `src/modules/document-archive/` completo: `domain/` (grafo de estado `DRAFT→RECEIVED→UNDER_REVIEW→{ACCEPTED,REJECTED}`, `ACCEPTED→SUPERSEDED`; `REJECTED` nunca removível/re-transicionável, fecha a contradição com J9 encontrada na Rodada 1 do protocolo), `ports/`+`persistence/` (porta genérica multi-índice, adapter DynamoDB real, GSI2/GSI5 novos confirmados livres por grep antes do uso — nunca GSI3/GSI4/GSI6 já ocupados por reminder/membership), `application/` (`DocumentArchiveService` com RBAC real via `authorize()` — novas actions `docarchive:create/read/upload/review` — e `assertReviewerOrAdmin()` como camada de serviço nomeada, separada de `authorize()`, para o gate de reviewer/admin da Decisão 1/Bloqueador 6 do design), `http/` (handlers no mesmo pipeline de `item-handlers.ts`), 6 schemas JSON novos registrados no `defaultSchemaRegistry`, composition root e Lambda handler. **Idempotência real**: `acceptVersion` verifica o registro de idempotência (chaveado por `clientRequestToken`, com `payloadHash`+`resultSnapshot`) ANTES de qualquer validação de transição — um replay legítimo contra uma version já `SUPERSEDED` por uma renovação posterior ainda retorna o resultado original, em vez de ser classificado erroneamente como transição inválida (achado real corrigido durante a implementação, não estava assim na primeira versão). **Achado real no próprio fake de teste**: a primeira versão de `test/unit/document-archive/in-memory-store.ts` avaliava CADA par nome/valor de `ExpressionAttributeNames`/`Values` como condição de transação, confundindo placeholders de SET com condições reais — fazia uma transição de estado normal parecer exigir que o item já estivesse no estado-alvo antes da escrita (backwards). Corrigido com um parser pequeno e real da gramática AND/OR (incluindo grupos `OR` entre parênteses) que os builders de `shared/dynamodb/occ.ts` de fato produzem, em vez de uma heurística de convenção de nomes. **G-V3 confirmado 3x ao vivo**: grafo de transição (mutação permitindo `REJECTED→ACCEPTED` pegou 2 testes corretamente), gate de scan de arquivo (mutação desabilitando o gate de aplicação ainda foi pega pelo fence da própria transação — defesa em profundidade confirmada), gate de reviewer/admin (mutação removendo a checagem de dono pegou 5 testes corretamente). **Infra real aplicada em `dev`**: Lambda `exptrk-dev-document-archive-handler` (sem política IAM dedicada — GSI2/GSI5 já cobertos pela política geral `tenant_facing_read_write_policy_json`) + 8 rotas `/document-archive/*` no API Gateway + `scripts/build-lambdas.ts`/`proxy-allowlist.ts` atualizados no MESMO commit da infra Terraform, evitando deliberadamente a classe de bug real já encontrada em D-117/D-120 (rota ou handler órfão). `terraform test` (suíte real de CI, `tests/stack.tftest.hcl`) 19/19; apply feito via plano `-target`-escopado (16 a adicionar, 0 a mudar, 0 a destruir) para evitar tocar ~76 diffs não-relacionados de `source_code_hash` que um `build:lambdas` completo gerou para todas as outras Lambdas; confirmado ao vivo via `aws lambda get-function` (`State=Active`) e `aws apigatewayv2 get-routes` (8 rotas presentes). **Suíte**: `npm test` 1345/1345 (30 testes de serviço/domínio + 10 novos de contrato + 4 de integração end-to-end reais), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos. | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO e DEPLOYADO** — Núcleo 1 completo; Núcleo 2 (Requirement/guest access/recorrência) segue pendente | `src/modules/document-archive/`, `src/modules/identity/domain/authorization.ts`, `schemas/api/docarchive-*.json`, `src/shared/contracts/schema-validator.ts`, `test/{unit/document-archive/,contract/schemas.test.ts,integration/document-archive-lifecycle.test.ts}`, `src/runtime/aws/{ids.ts,composition/document-archive.ts,handlers/document-archive-handler.ts}`, `infra/main.tf`, `infra/modules/api-gateway/{main.tf,variables.tf}`, `scripts/build-lambdas.ts`, `src/modules/bff/domain/proxy-allowlist.ts` | 2026-09-01 |
docs/architecture/decisions-log.md:166:| D-147 | Domínio Documental, Núcleo 2, entidade 3/3 (recorrência) — implementado seguindo a Decisão 8 `APPROVED` em D-143. **Fecha o Núcleo 2 (as 3 entidades — Requirement/D-145, guest access/D-146, recorrência/D-147 — estão completas) e, com Núcleo 1 (D-144) já completo, fecha as 9 decisões técnicas de D-143 por inteiro** — nenhum item das "9 decisões técnicas fechadas" de `estado-final-consolidado.md` segue sem implementação real (revisado item a item ao final desta tarefa). Estende `src/modules/document-archive/domain/document-request.ts` com `seriesId?`/`occurrenceId?`/`attemptIndex?`/`parentRequestId?` (todos aditivos/opcionais — o ponto de extensão nomeado explicitamente em D-146 — preservando o shape mínimo de guest access sem forçar um valor fabricado em todo `DocumentRequest` não-recorrente). Novo `domain/document-request-series.ts`: `DocumentRequestSeries` (série/ciclo, co-localizado sob a partição do Subject como `requirement.ts`), `computeSeriesOccurrenceId(seriesId, cycleStartAt)` — hash determinístico puro (reaproveita `stableHash` de `reminder/domain/reminder-occurrence.ts`, mesmo primitivo que `document-chasing.ts` já reaproveita) — e uma terceira namespace prefixo-discriminada `SERIESDUE` no MESMO GSI1 físico que `document.ts`/`requirement.ts` já usam (GSI3/GSI4/GSI6 confirmados por grep sem nenhum writer sob `document-archive/`, permanecem intocados). Novo `application/document-request-recurrence-service.ts`: `createSeries`/`getSeries`/`listSeries`/`cancelSeries`/`advanceCycle` (todas via `authorize()`, novas actions `docarchive:series-*`) e `materializeAttempt` — a operação central da Decisão 8, que avança `latestAttemptIndex`/`latestRequestId` da série E cria o novo `DocumentRequest` na MESMA `TransactWriteItems`, fechando a lacuna de falha parcial encontrada na Rodada 4 do protocolo original. O builder puro `buildMaterializeAttemptEntries` é exportado separadamente e reaproveitado pelo worker periódico (`src/workers/document-request-recurrence/materializer.ts`, mesmo split produtor/materializador de `subject/application/document-chasing-{producer,materializer}.ts`, aqui colapsado em um módulo por ser um job pequeno o bastante — mesma pragmática de `requirement-reindex/reindex.ts`) sem passar por `authorize()`/`RequestContext` (mesma postura do worker de reindex diário). **Desvio real, documentado**: o design menciona apenas `requestId`/`attemptIndex`/`seriesId`/`occurrenceId`/`parentRequestId` sem descrever como o worker descobre "o que está vencido" entre tenants — esta tarefa resolveu isso com um NOVO método de porta `scanActiveSeries` (Scan cross-tenant filtrado por `entityType`/`status`, mesmo tradeoff de custo já aceito e documentado por `scanSatisfiedRequirements` do worker de Requirement/D-145 — este módulo continua sem método de enumeração de tenants), em vez de reaproveitar a query GSI1 SERIESDUE por tenant (que exigiria uma lista de tenants que a porta não oferece). **G-V3 confirmado ao vivo**: mutação removendo a entry do `Put` do novo `DocumentRequest` da mesma transação (deixando só o `Update` da série) derrubou os 2 testes de atomicidade transacional (contagem de entries e leitura pareada série+request), revertida; mutação trocando o hash puro por um com `Math.random()` derrubou o teste de determinismo do `occurrenceId` (uma tentativa anterior com `Date.now()` NÃO foi pega — mesma chamada de teste roda no mesmo milissegundo — achado real documentado aqui, não escondido, substituído por `Math.random()` para a evidência real), revertida. **Suíte**: `npm test` (raiz) 1447/1447 (+14 testes novos: 11 de serviço/domínio incluindo o fluxo completo criar→materializar tentativa 1→tentativa 2→avançar ciclo→materializar tentativa 1 do novo ciclo, 3 do worker materializador incluindo idempotência contra tick duplicado); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (40 handlers) limpos. **Infra**: HCL escrito (`infra/main.tf` — nova Lambda `document-request-recurrence-handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário 04:30 UTC, após o reindex de Requirement às 04:00; `infra/modules/api-gateway/main.tf` — 5 rotas `/document-archive/series*` na mesma Lambda de Núcleo 1; `scripts/build-lambdas.ts`/`proxy-allowlist.ts` atualizados no mesmo commit, mesma disciplina anti-D-117/D-120), `terraform fmt`/`validate` limpos, `terraform test` (`AWS_PROFILE=claude-dev`) 20/20 sem regressão — **NÃO aplicado em `dev`** (`terraform apply` explicitamente fora do escopo desta tarefa; próxima sessão decide se aplica). | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO (código), infra planejada mas NÃO aplicada em `dev`** — Núcleo 2 completo (3/3 entidades); as 9 decisões técnicas de D-143 estão todas implementadas | `src/modules/document-archive/{domain/{document-request.ts,document-request-series.ts},application/document-request-recurrence-service.ts,http/document-archive-handlers.ts,application/id-generator.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `src/modules/identity/domain/authorization.ts`, `schemas/api/docarchive-series-*.json`, `src/shared/contracts/schema-validator.ts`, `src/workers/document-request-recurrence/materializer.ts`, `src/runtime/aws/{ids.ts,composition/document-archive.ts,handlers/{document-archive-handler.ts,document-request-recurrence-handler.ts}}`, `test/{unit/document-archive/{document-request-recurrence.test.ts,document-request-recurrence-materializer.test.ts,in-memory-store.ts},contract/schemas.test.ts,integration/document-archive-lifecycle.test.ts}`, `infra/{main.tf,modules/api-gateway/main.tf}`, `scripts/build-lambdas.ts`, `src/modules/bff/domain/proxy-allowlist.ts` | 2026-09-01 |
docs/architecture/decisions-log.md:171:| D-151 | `CORE_USER_DATA` LGPD purge worker (Prioridade 1 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 6 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `DELIVERY_RECORD`/`SECURITY_AUDIT`/`QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `CORE_USER_DATA` = "itens, políticas, ocorrências" mapeado ao código real: `ExpirationItem`/`ReminderPolicy` (ambos já têm `deletedAt` opcional, nunca fisicamente purgados hoje fora do fechamento de tenant inteiro) recebem um worker Lambda diário novo, `ReminderOccurrence` recebe TTL nativo DynamoDB independente (não um worker), exatamente como o design pediu ("worker(s) por deletedAt+30d, ReminderOccurrence via TTL nativo independente do pai"). **Mecanismo, ExpirationItem/ReminderPolicy** (`src/workers/core-user-data-purge/`): candidato descoberto por `Scan` full-table filtrado por `entityType IN (ExpirationItem, ReminderPolicy) AND attribute_exists(deletedAt)` — deliberadamente NÃO um worklist GSI6 (mesmo padrão `document-purge`'s claim/lease usa): GSI6 é um limite de isolamento fechado com 4 consumidores já nomeados explicitamente em `infra/tests/stack.tftest.hcl`, e nenhuma das duas entidades tem efeito colateral externo (S3, lease) a proteger — o mesmo tradeoff de custo que `tenant-purge-sweep.ts` já aceita e documenta a este porte de projeto. Elegibilidade exige DUAS condições independentes: (1) `deletedAt` com pelo menos 30 dias (`privacy-lgpd.md` §4, "exclusão/encerramento + 30 dias"); (2) tenant dono com `TenantLifecycleRecord.status === "ACTIVE"` (lido via `GetItem` consistente na própria `TenantLifecycleRecord`, cacheado por execução) — um tenant em qualquer outro estado (incluindo registro AUSENTE, tratado fail-closed, nunca assumido `ACTIVE`) é explicitamente o trabalho do pipeline de fechamento de tenant (`src/workers/tenant-purge/`), um mecanismo TOTALMENTE separado deste. O delete físico é um `DeleteItem` único condicionado via `buildVersionedDelete` (versão esperada + `deletedAt = :deletedAt` exato observado no scan) — a re-asserção de `deletedAt` é a propriedade de correção mais importante deste worker (nenhum caminho de restauração existe hoje para nenhuma das duas entidades, mas a condição torna isso estruturalmente seguro em vez de "seguro só porque nada faz isso ainda"). **Mecanismo, ReminderOccurrence** (`src/modules/reminder/domain/reminder-occurrence.ts`): campo novo `purgeAfterTtl` (epoch seconds), mesma convenção já usada por `InvitationTokenPointer.purgeAfterTtl` e já habilitada como atributo TTL real da tabela inteira (`infra/modules/dynamo-table/main.tf`, `ttl.attribute_name = "purgeAfterTtl"` — nenhuma mudança de infra necessária, o atributo já é físico há muito). Calculado da PRÓPRIA `scheduledAt` da ocorrência + 30 dias, setado UMA VEZ em `reminder-materializer.ts` no momento da materialização — deliberadamente nunca recomputado a partir do `deletedAt` do item/policy pai, porque uma ocorrência é naturalmente limitada pelo seu próprio agendamento independentemente do que acontece depois com o pai (dispensa qualquer worker). **Questão em aberto, sinalizada e não resolvida aqui**: `ReminderOccurrence`s materializadas ANTES desta mudança não têm `purgeAfterTtl` retroativo — nenhum backfill foi implementado nesta sessão (nenhum mecanismo de migração em massa precedente existe neste código para um caso análogo); o volume real de ocorrências pré-existentes ainda distantes de `scheduledAt` é pequeno o bastante para não justificar um script dedicado agora, mas fica nomeado aqui para quem revisitar o assunto. **Infra**: novo módulo Lambda `core_user_data_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 5 * * ? *)`, 05:00 UTC, depois de `document-request-recurrence` às 04:30) em `infra/main.tf`, mesmo padrão de `requirement_reindex_handler`/`document_request_recurrence_handler` — sem policy dedicada além da leitura/escrita geral tenant-facing (Scan de base table + GetItem em row tenant-scoped comum, nenhum GSI3/GSI6 envolvido). `scripts/build-lambdas.ts` ganha a entrada `core-user-data-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `deletedAt = :deletedAt` do delete (simulando ausência da re-asserção) faria o teste de "restore/undelete durante a corrida" passar mesmo com um restore concorrente — confirmado quebrando deliberadamente essa extraCondition no teste, revertida; mutação trocando `TENANT_ACTIVE_STATUS` por aceitar qualquer status derrubou 8 dos 9 casos parametrizados de status não-`ACTIVE`, revertida. **Suíte**: `npm test` (raiz) +14 testes novos (`test/unit/workers/core-user-data-purge.test.ts`: elegibilidade por idade incluindo fronteira exata, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra restore/undelete simulado e contra bump de versão concorrente, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan; +1 teste em `reminder-materializer.test.ts` para `purgeAfterTtl`); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`validate-schemas` limpos. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (CORE_USER_DATA apenas, Prioridade 1 de 7)** — as 6 classes restantes (`DELIVERY_RECORD`→`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/modules/reminder/domain/reminder-occurrence.ts`, `src/modules/reminder/application/reminder-materializer.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{core-user-data-purge.test.ts,core-user-data-purge-fakes.ts}`, `test/unit/reminder/reminder-materializer.test.ts`, `test/unit/reminder/reconciliation.test.ts` | 2026-09-01 |
docs/architecture/decisions-log.md:172:| D-152 | `DELIVERY_RECORD` LGPD purge worker (Prioridade 2 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 5 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `SECURITY_AUDIT`/`QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `DELIVERY_RECORD` = "intents/attempts" mapeado ao código real: `NotificationIntent` (`src/modules/reminder/domain/notification-intent.ts`, `entityType: "NotificationIntent"`) e `NotificationAttempt` (`src/modules/notification/domain/notification-attempt.ts`, `entityType: "NotificationAttempt"`) — verificado por leitura direta das duas entidades, não só pelo nome. **Checagem de escopo explícita do task brief, confirmada, SEM confusão**: `AuditEvent`/`MembershipAuditEvent` (o feed do Admin Activity Log de D-149) são `SECURITY_AUDIT`, Prioridade 3, linha SEPARADA de `privacy-lgpd.md` §4 (linha 43, "criação + 365 dias") — nenhum dos dois é lido/escaneado/deletado por este worker; `candidate-source.ts`'s `entityType` union nomeia só `NotificationIntent`/`NotificationAttempt`. Mesmo layout de D-151 (`src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/delivery-record-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (não GSI6 — nenhuma das duas entidades tem efeito colateral externo a proteger com claim state). **Diferença de forma de elegibilidade vs. D-151 (achado central deste worker, nomeado no task brief)**: aqui é elegibilidade de UMA condição de idade (`createdAt` com pelo menos 180 dias, `privacy-lgpd.md` §4 "criação + 180 dias") — TODO registro de entrega eventualmente expira pelo prazo fixo de retenção, independentemente de qualquer ação de exclusão (ao contrário de `CORE_USER_DATA`, que só é elegível quando `deletedAt` já existe); um `NotificationIntent`/`NotificationAttempt` ainda `PENDING`/nunca cancelado purga igual, uma vez velho o bastante — testado explicitamente. A fence de tenant `ACTIVE` é idêntica à de D-151 (mesmo motivo: fechamento de tenant é trabalho do outro pipeline). O delete físico é um `DeleteItem` único condicionado via `buildVersionedDelete`; como não existe campo de negócio análogo a `deletedAt` para re-afirmar contra uma corrida de "restauração" (nenhuma das duas entidades tem caminho de undelete), a re-asserção OCC de `version` já é sozinha a fence completa de "não mudou desde o scan" — `createdAt` é re-afirmado como condição extra só como defesa em profundidade (nada no código hoje escreve `createdAt` depois da criação, mas a condição faz disso uma garantia estrutural, não um acidente de "nada faz isso ainda"). **Infra**: novo módulo Lambda `delivery_record_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(30 5 * * ? *)`, 05:30 UTC, depois de `core-user-data-purge` às 05:00) em `infra/main.tf`, mesmo padrão de `core_user_data_purge_handler` — sem policy dedicada além da leitura/escrita geral tenant-facing. `scripts/build-lambdas.ts` ganha a entrada `delivery-record-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a extraCondition `createdAt = :createdAt` do delete fez o teste de "createdAt mudou entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido) — confirmado quebrando deliberadamente essa extraCondition no teste, revertida; mutação trocando a comparação `=== TENANT_ACTIVE_STATUS` por `!== undefined` (aceita qualquer status presente) derrubou 8 dos 8 casos parametrizados de status não-`ACTIVE` + o teste de mistura de candidatos, revertida. **Suíte**: `npm test` (raiz) +19 testes novos (`test/unit/workers/delivery-record-purge.test.ts`: purga de cada tipo de entidade, elegibilidade por idade incluindo fronteira exata, purga independente de status de entrega, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `createdAt` e contra bump de versão concorrente, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (41 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (DELIVERY_RECORD apenas, Prioridade 2 de 7)** — as 5 classes restantes (`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida; `NotificationAttemptLookup` (pointer derivado do attempt) fica órfão após a purga do attempt correspondente, achado nomeado e não corrigido nesta sessão | `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{delivery-record-purge.test.ts,delivery-record-purge-fakes.ts}` | 2026-09-01 |
docs/architecture/decisions-log.md:173:| D-154 | `QUOTA_TELEMETRY` LGPD purge worker (Prioridade 4 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — este era o primeiro das 7 prioridades sem escopo trivial (o task brief pediu investigação real, não confiança cega na lista preliminar). `privacy-lgpd.md` §4's `QUOTA_TELEMETRY` = "quotas/métricas identificáveis, MembershipInviteRateLimitRecord" (definição genérica + 1 exemplo nomeado) — as 5 entidades reais de rate-limit/quota do código foram lidas uma a uma: `MembershipInviteRateLimitRecord` (organization, O EXEMPLO NOMEADO), `GuestRateLimitRecord`/`InitialInviteRateLimitRecord` (subject), `DocumentArchiveGuestRateLimitRecord` (document-archive) e `TenantQuotaRecord` (identity). **Achado central**: 4 das 5 — incluindo o próprio exemplo nomeado no design doc — já carregam campo `purgeAfterTtl` calculado a partir de `resetAt` e já wireado ao TTL nativo real da tabela inteira (`infra/modules/dynamo-table/main.tf`, `ttl.attribute_name = "purgeAfterTtl"`, confirmado por leitura da infra, não assumido) — mesmo status "já resolvido" que `TRANSIENT`'s `InvitationTokenPointer` (`privacy-lgpd.md` linha 42); construir um worker novo para essas 4 duplicaria um mecanismo que já funciona. **A ÚNICA entidade genuinamente sem purga**: `TenantQuotaRecord` (`src/modules/identity/application/quota.ts`) — sem `purgeAfterTtl`, sem TTL, sem outro caminho de purga; é o token bucket de quota de negócio (API_REQUEST/UPLOAD_BYTES/UPLOAD_COUNT/AI_CALL/NOTIFICATION_EMAIL/IMPORT_*), distinto dos rate-limiters anti-abuso de convite/guest. **Campo de fim de janela**: `resetAt`, o próprio campo do token bucket de janela fixa que marca quando a janela atual fecha — exatamente o conceito "fim da janela" do design doc; elegibilidade = `resetAt + 30 dias <= now`. Escopo desta implementação: SÓ `TenantQuotaRecord`. Mesmo layout de D-151/D-152/D-153 (`src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (não GSI6 — sem efeito colateral externo a proteger). Fence de tenant `ACTIVE` idêntica às 3 anteriores. **Sem campo `version`** (mesma situação de D-153, motivo diferente): `TenantQuotaRecord` usa `count`/`resetAt` re-afirmados via `ConditionExpression` a cada escrita, nunca um contador `version` — `buildConditionalDelete` (já existente desde D-153) re-afirma `resetAt` exato observado no scan como fence de "não mudou desde o scan". **Infra**: novo módulo Lambda `quota_telemetry_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(30 6 * * ? *)`, 06:30 UTC, depois de `security-audit-purge` às 06:00) em `infra/main.tf`, mesmo padrão de `security_audit_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `quota-telemetry-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `resetAt = :resetAt` do delete fez o teste de "resetAt mudou (janela rolou) entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido) — confirmado quebrando deliberadamente essa condição no teste (`purged` foi de 0→1 espúrio), revertida. **Suíte**: `npm test` (raiz) 1614/1614 (+17 testes novos: `test/unit/workers/quota-telemetry-purge.test.ts` — elegibilidade por fim de janela incluindo fronteira exata de 30 dias, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `resetAt` e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (43 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (QUOTA_TELEMETRY apenas, Prioridade 4 de 7 — só `TenantQuotaRecord`; as outras 4 entidades da classe já resolvidas via TTL nativo pré-existente, sem código novo necessário)** — as 3 classes restantes (`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{quota-telemetry-purge.test.ts,quota-telemetry-purge-fakes.ts}` | 2026-09-01 |
docs/architecture/decisions-log.md:174:| D-153 | `SECURITY_AUDIT` LGPD purge worker (Prioridade 3 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 4 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `SECURITY_AUDIT` = "AuditEvent/logs redigidos, MembershipAuditEvent" mapeado ao código real, confirmado por leitura direta dos 4 arquivos de domínio: `AuditEvent` (`src/modules/expiration/domain/audit-event.ts`), `MembershipAuditEvent` (`src/modules/organization/domain/audit-event.ts`), `SubjectAuditEvent` (`src/modules/subject/domain/audit-event.ts`) e `TenantAuditEvent` (`src/modules/activity/domain/tenant-audit-event.ts`, o 4º agregado-irmão que D-149 adicionou para fechar o gap de audit do export) — exatamente as 4 partições que `GET /activity`'s `ActivityService` já faz merge k-way (`AUDIT_PARTITIONS` de `activity/domain/merge.ts`), confirmando a suspeita do task brief de que este worker toca dado consumido por uma feature de leitura recém-lançada. **`occurredAt` é o relógio de idade para as 4, nunca um `createdAt` separado** — nenhum dos 4 domínios declara `createdAt`; o próprio header de `tenant-audit-event.ts` já registrava isso antecipadamente ("para esta entidade imutável, occurredAt É o relógio canônico equivalente a createdAt"), confirmado aqui por leitura de código, não assumido. **Desvio de forma vs. D-151/D-152, real e documentado**: nenhuma das 4 entidades tem campo `version` — são append-only por construção (cada arquivo de domínio já diz "não há update()/delete() exportado para esta entidade"); `buildVersionedDelete` (que sempre exige `expectedVersion`) não se aplica. Solução: `buildConditionalDelete` novo em `occ.ts`, irmão simétrico do `buildConditionalPut` já existente (mesmo padrão "Put condicionado sem version" espelhado para Delete) — o delete físico condiciona em `attribute_exists(PK) AND attribute_exists(SK) AND occurredAt = :occurredAt` (o valor exato observado no scan), a mesma defesa-em-profundidade de "não mudou desde o scan" que D-151/D-152 fazem com `version`, adaptada à ausência estrutural de um contador nestas linhas. **Normalização de tenant**: `MembershipAuditEvent` declara `organizationId`, não `tenantId` — mas `organizationId` É o tenant id neste código (mesma partição `TENANT#<organizationId>#...`); `dynamodb-candidate-source.ts` normaliza os dois nomes em um único `tenantId` no candidato, único lugar onde esse mapeamento acontece. **Investigação de corrida com paginação de `GET /activity` (pedida explicitamente no task brief), concluída SEM achado de bug**: o cursor composto de `ActivityService` guarda `{PK,SK}` reais por partição e os repassa como `ExclusiveStartKey` de `Query` na página seguinte; por leitura de `dynamodb-audit-partition-store.ts`, esse valor é só repassado ao SDK sem checagem de existência nem re-fetch — e o próprio contrato documentado do DynamoDB trata `ExclusiveStartKey`/`LastEvaluatedKey` como posição no B-tree, não como referência a um item que precisa continuar existindo. Conclusão registrada no header de `purge.ts`: nenhuma correção necessária; nenhum teste novo dedicado a esse achado específico (a convenção já estabelecida no código é manter adapters DynamoDB finos sem teste de SDK mockado, verificados por leitura, não por um teste avulso inconsistente com o resto do código). Mesmo layout de D-151/D-152 (`src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` + `src/runtime/aws/handlers/security-audit-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (não GSI6). **Infra**: novo módulo Lambda `security_audit_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 6 * * ? *)`, 06:00 UTC, depois de `delivery-record-purge` às 05:30) em `infra/main.tf`, mesmo padrão de `delivery_record_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `security-audit-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `occurredAt = :occurredAt` do delete fez o teste de "occurredAt mudou entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido) — confirmado quebrando deliberadamente essa condição no teste, revertida; mutação trocando a comparação `=== TENANT_ACTIVE_STATUS` por `!== undefined` (aceita qualquer status presente) derrubou 8 dos 8 casos parametrizados de status não-`ACTIVE`, revertida. **Suíte**: `npm test` (raiz) 1597/1597 (+30 testes novos: `test/unit/occ.test.ts` +2 para `buildConditionalDelete`; `test/unit/workers/security-audit-purge.test.ts` +20 — purga de cada um dos 4 tipos de entidade incluindo a normalização `organizationId`→`tenantId` de `MembershipAuditEvent`, elegibilidade por idade incluindo fronteira exata de 365 dias, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `occurredAt` e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/4 tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (42 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20, módulo `tenant-purge-workflow` 11/11 (sanidade, não tocado por esta mudança). | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (SECURITY_AUDIT apenas, Prioridade 3 de 7)** — as 4 classes restantes (`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/shared/dynamodb/occ.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/{occ.test.ts,workers/{security-audit-purge.test.ts,security-audit-purge-fakes.ts}}` | 2026-09-01 |
docs/architecture/decisions-log.md:176:| D-156 | `TRANSIENT` remanescente LGPD purge worker (Prioridade 6 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementa os 2 entidades ainda sem purga física da linha `TRANSIENT` de `privacy-lgpd.md` §4 (`WebhookInbox`, `UploadSlot, InvitationTokenPointer`); `InvitationTokenPointer` já é `purgeAfterTtl` nativo (`organization/domain/invitation-token.ts`), citado como já resolvido pela própria linha da doc-fonte — não tocado, nem reverificado. **`WebhookInbox`** (criado em `ses-callback-workflow.ts`'s `processSesCallback()` a cada callback SES/SNS recebido — idempotência por `(tenantId, providerAccountId, snsMessageId)`): investigado e confirmado SEM nenhum mecanismo de expiração — nenhum campo `purgeAfterTtl`, nenhum TTL nativo wireado, nenhum outro worker o toca; achado genuíno, mesma forma do `TenantQuotaRecord` de D-154. Relógio: `createdAt` (o momento em que o registro de idempotência foi persistido — `occurredAt`, o timestamp do próprio evento do provedor, é segundos antes e não é o campo ao qual a linguagem "7 dias" da doc está ancorada). **`UploadSlot`** (`src/modules/document/domain/upload-slot.ts`, M6): achado mais sutil — a entidade JÁ tem um campo `purgeAfter` e uma função pronta e já alinhada com a doc-fonte, `computeUploadSlotPurgeAfter()` (`document/domain/retention.ts`, "7 dias; slot incompleto: 24h" codificado literalmente), mas ambos são código morto: nenhum writer chama `computeUploadSlotPurgeAfter`, `purgeAfter` é setado uma única vez na reserva como o `expiresAt` de curtíssimo prazo e nunca recalculado, e — decisivo — `purgeAfter` é um atributo string ISO comum, não `purgeAfterTtl` (o nome real do atributo de TTL nativo da tabela, `infra/modules/dynamo-table/main.tf`), então o sweeper de TTL do DynamoDB nunca olha para ele. `UploadSlotReconciliationWorker` (`src/workers/upload-slot-reconciliation/`) só marca `status = EXPIRED` em slots `RESERVED` vencidos, nunca deleta a linha — não é um caminho de purga física, é um mecanismo diferente (liberação de quota + timeout do Document). Achado genuíno, mesma disciplina de D-154/D-155: reusa `computeUploadSlotPurgeAfter` verbatim (não reinventa a fórmula) em vez de escrever uma nova. Um slot ainda `RESERVED` NUNCA é candidato, seja qual for a idade — é uma reserva ativa em voo, nunca alvo de purga por idade sozinha (um sweep de reconciliação travado é um incidente operacional a corrigir, não motivo para apagar uma linha que um cliente ainda pode estar tentando confirmar); "incompleto" (janela 24h) = `status !== "CONSUMED"` (nunca chegou a `CONSUMED`), "confirmado" (janela 7 dias) = `status === "CONSUMED"` — exatamente o parâmetro `wasConfirmed` da função já existente. Um único worker (`src/workers/transient-purge/`) cobre as 2 entidades num só `Scan` (`entityType IN (WebhookInbox, UploadSlot)`), refletindo o agrupamento da própria doc-fonte numa única linha `TRANSIENT`. Mesmo layout D-151/152/153/154/155 (`candidate-source.ts`/`dynamodb-candidate-source.ts`/`purge.ts` + `src/runtime/aws/handlers/transient-purge-handler.ts`), mesmo tradeoff de `Scan` full-table (nem `UploadSlot` nem `WebhookInbox` têm um GSI de descoberta dedicado a purga — o GSI6 de `UploadSlot` só existe enquanto `RESERVED`, removido antes de qualquer elegibilidade de purga). Fence de tenant `ACTIVE` idêntica às 5 anteriores. **Delete usa `version`** (como D-151/D-155): ambas as entidades têm contador `version` real e o atributo `tenantId` esperado por `buildVersionedDelete` (diferente de D-153/D-154/D-155, que usam `buildConditionalDelete` por falta de `version` ou por usarem `organizationId`). **Infra**: novo módulo Lambda `transient_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(30 7 * * ? *)`, 07:30 UTC, depois de `invitation-purge` às 07:00) em `infra/main.tf`, mesmo padrão de `invitation_purge_handler`. `scripts/build-lambdas.ts` ganha a entrada `transient-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição de `version` do delete (deixando só `attribute_exists(PK) AND attribute_exists(SK)`) fez o teste de "version mudou (modificação concorrente) entre scan e delete" passar mesmo com a mutação concorrente simulada (purge indevido, `purged` 0→1 espúrio) — confirmado quebrando deliberadamente essa condição no worker, revertida antes do commit. **Suíte**: `npm test` (raiz) 1658/1658 (+23 testes novos: `test/unit/workers/transient-purge.test.ts` — elegibilidade de `WebhookInbox` por `createdAt` incluindo fronteira exata de 7 dias, `UploadSlot` `RESERVED` nunca candidato independente da idade, `UploadSlot` incompleto (`EXPIRED`) elegível em exatamente 24h e inelegível 1h antes, `UploadSlot` confirmado (`CONSUMED`) mantido na janela de 7 dias mesmo já passado das 24h, `RELEASED` seguindo a mesma janela de 24h de `EXPIRED`, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra mudança de `version` — quebrada/revertida ao vivo — e contra remoção concorrente da linha, idempotência de duas execuções seguidas, mistura de candidatos `WebhookInbox`/`UploadSlot` elegíveis/inelegíveis entre tenants num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`check-dependency-freshness`/`build:lambdas` (45 handlers) limpos; `terraform fmt`/`validate`/`test` (`AWS_PROFILE=claude-dev`) stack raiz 20/20. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (TRANSIENT remanescente, Prioridade 6 de 7 — `WebhookInbox`+`UploadSlot`; `InvitationTokenPointer` já resolvido, não tocado)** — resta só 1 lane pendente (`LEGAL_EVIDENCE`, Prioridade 7, bloqueada por trava jurídica, nunca destravada por esta sessão) das 7 prioridades LGPD originais de D-127 | `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{transient-purge.test.ts,transient-purge-fakes.ts}` | 2026-09-01 |
docs/architecture/decisions-log.md:185:| D-163 | **DocumentFile — decisão fundacional #1 de D-161** (storage/scan/download para o Domínio Documental, D-143 Decision 6 já tinha aprovado a semântica de arquivo mas não o mecanismo). Protocolo Claude↔Codex completo, **4 rodadas** (5,2→7,1→8,3→9,4), Claude 9,3/Codex 9,4, **APPROVED (design)**. E-014: `SIM PARCIAL` (padrão S3 presigned+GuardDuty já pesquisado/implementado internamente via M6, `E2E PROVEN`; pesquisa externa nova só confirmou nesta rodada que um finding real do GuardDuty Malware Protection carrega `bucket`/`key`/`versionId` do objeto físico escaneado). Rodada 1 (5,2/10) errou dizendo que nenhum bucket/GuardDuty/worker existia (falso — `infra/modules/document-buckets`/`document-malware-protection`, `src/workers/upload-slot-reconciliation` já existem) e propôs validar "exatamente 1 PRINCIPAL" só por validação de input, sem fechar a corrida real entre duas reservas concorrentes. Rodada 2 (7,1/10) corrigiu ambos mas introduziu `removeInfectedFile()` contraditório (exigia um estado que a própria infecção já teria consumido) e deixava a recuperação de um PRINCIPAL infectado sem caminho. Rodada 3 (8,3/10) removeu `removeInfectedFile()` (infecção passa a ser a própria transição terminal) e fechou a recuperação via "nova Version, nunca reabrir o conjunto selado" — mas propôs reusar GSI6 para reconciliação, contradizendo D-143 Decision 2 ("GSI3/GSI4/GSI6 nunca tocados"), e deixou ambíguo qual evento (S3 ou GuardDuty) consolida `quarantineObject.versionId` primeiro. Rodada 4 (**9,4/10, APROVADO**) reverteu para GSI5 (já alocado ao módulo, mesmo padrão de discriminação por prefixo do GSI1) e fechou a correlação com uma regra simétrica: o primeiro evento físico a chegar consolida a partir da própria tripla que carrega, o segundo verifica igualdade exata. **Decidido**: entidade `DocumentFile` (mesma partição Document/Version, `documentFileKey()` nova espelhando `documentVersionEventKey()` real — os stubs mortos `documentVersionFileKeyPrefix()`/`documentVersionEventKeyPrefix()` nunca tiveram chamador e são removidos); `reserveUpload()` como uma `TransactWriteItems` (Update condicionado de `DocumentVersion` com `fileSetSealed`/`principalFileId` + N Put de `DocumentFile`) fechando a corrida de PRINCIPAL na própria Version, não na validação de input; transição terminal via `buildVersionedUpdate` padrão (sem mecanismo novo de contador); `DocumentVersionEvent` ganha `fileId`/`fromFileScanStatus`/`toFileScanStatus`, tipo renomeado `FILE_REMOVED_INFECTED`→`FILE_REJECTED_INFECTED` (nunca teve writer real); `acceptVersion()` ganha `ConditionCheck` transacional do PRINCIPAL (`scanStatus=CLEAN`); reconciliação via GSI5 namespaced por prefixo, nunca GSI6; nenhum bucket/GuardDuty novo, reusa M6 (`infra/modules/document-buckets`/`document-malware-protection`) tal como estão. **Design-only, deliberado** — implementação em sessão(ões) futura(s) dedicada(s), tamanho comparável a M6 completo. Download real (Viewer A1) e purga física de `DocumentFile` seguem fora de escopo, nomeados. | Nível 5 (nova entidade + storage + invariantes transacionais — reclassificado da estimativa otimista de nível 3-4 do handoff anterior após scoping real) | 9,3 | 9,4 (Codex, Rodada 4/4, `APPROVED`) | **APROVADO (design), NÃO IMPLEMENTADO** — próximo item real da macro-ordem de D-161 é a implementação desta decisão, depois o item 8 (Document Types) | `docs/architecture/reviews/document-file-scoping/` (round1-4, `estado-final-consolidado.md`) | 2026-09-01 |
docs/architecture/decisions-log.md:187:| D-166 | **DocumentFile — terceira fatia de D-163: DocumentFileReconciliationWorker (item 2 da ordem de `NEXT_SESSION_PROMPT.md`)**. Generaliza M6's `UploadSlotReconciliationWorker` (`src/workers/upload-slot-reconciliation/reconciliation.ts`) para o segundo namespace GSI5 esparso já modelado por `document-file.ts`'s `fileReconciliationGsi5Keys()` (D-164, sem chamador até agora): `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`. Implementação direta de design já `APPROVED` (D-163 §6/`round4-claude-final.md` §3), sem nova rodada Claude↔Codex. Novo `applyFileScanTimeout()` em `apply-file-scan-result.ts` (mesma `Deps`/mesmo módulo de `applyFileScanResult`/`confirmFileScanClean`) faz a transição terminal `PENDING_UPLOAD\|SCANNING`→`TIMEOUT`, espelhando o branch REJECT existente: `buildVersionedUpdate` padrão + `remove: ["GSI5PK","GSI5SK"]` + decremento de `pendingFileScans` do `DocumentVersion` na MESMA transação — nenhum mecanismo novo. A única peça genuinamente nova é a condição extra `GSI5PK = :observado AND GSI5SK = :observado` (via `extraConditions`, duas entradas separadas por causa do parser de teste, que não entende AND aninhado dentro de um parêntese) — fecha exatamente a corrida nomeada pelo round4 §3: um candidato cujo deadline mudou ou que já chegou a um estado terminal entre a varredura e a escrita é puxado de volta (`ConditionalCheckFailed`→`IGNORED_STALE`), nunca reprocessado às cegas. **Desvio nomeado do design, não uma correção dele**: GSI5PK de `DocumentFile` é escopado por tenant (`TENANT#<t>#...`), diferente de GSI6 (globalmente namespaced) que permite ao `UploadSlotReconciliationWorker` fazer uma `Query` cross-tenant real — este módulo nunca teve porta de enumeração de tenant (`document-archive-store.ts`'s `scanSatisfiedRequirements`/`scanActiveSeries` já aceitam o mesmo trade-off pelo mesmo motivo estrutural). O worker novo (`src/workers/document-file-reconciliation/`, port+adapter+lógica, mesmo layout de `core-user-data-purge`/`quota-telemetry-purge`) descobre candidatos via `Scan` na tabela base filtrado por `entityType=DocumentFile AND scanStatus=:status AND attribute_exists(GSI5PK)` (nunca GSI6, nunca sequer uma `Query` real em GSI5) — dois scans independentes e limitados (`MAX_PAGES_PER_STATUS=25`), um por status não-terminal, cada página ordenada por `GSI5SK` (deadline) em memória antes de processar (melhor ordenação possível dado o Scan cross-tenant; um backlog patológico é limitado pelo cap de páginas e recolhido na próxima execução, mesma disciplina dos outros workers Scan-based deste código). Lambda novo `document-file-reconciliation-handler` (`src/runtime/aws/handlers/`), schedule `rate(15 minutes)` (mesma cadência do worker que generaliza), só `tenant_facing_read_write_policy_json` — nenhuma política GSI6/GSI3 nova (confirmado por teste Terraform novo). `buildDocumentArchiveDeps()` passa a devolver `ids` diretamente (antes só embutido nos serviços) para o novo handler poder montar `ApplyFileScanResultDeps`. **G-V3 confirmado ao vivo em 2 pontos, quebrando e restaurando cada mecanismo**: (1) removendo `extraConditions` de `applyFileScanTimeout`, o teste "pointer changed concorrentemente" passa a devolver `TIMED_OUT` em vez de `IGNORED_STALE` (falha); (2) invertendo o comparador de deadline (`<` em vez de `>`) no worker, os 2 testes de timeout real e o teste dos 2 scans independentes falham (0 `timedOut` em vez do esperado). Ambos revertidos antes do commit. Suíte completa 1685/1685 (1677+8 novos: 4 unit de `applyFileScanTimeout`, 4 do worker), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21, +1 novo) todos limpos. | Nível 3-4 (implementação direta de design já `APPROVED`, D-163) | n/a | n/a | **IMPLEMENTADO E VERIFICADO, NÃO deployado** (infra escrita/validada via `terraform plan`/`test`, `terraform apply` real em `dev` fica para o CD de push a `main`) — falta a rota HTTP real de `reserveFiles()`, o worker S3/GuardDuty que chamaria `applyFileScanResult()`/`confirmFileScanClean()` em produção, presign real (`UploadUrlSigner`) e o gate `fileSetSealed` em `commitUpload()` (item 3 da ordem, próximo) | `src/modules/document-archive/application/apply-file-scan-result.ts`, `src/runtime/aws/{composition/document-archive.ts,handlers/document-file-reconciliation-handler.ts (novo)}`, `src/workers/document-file-reconciliation/ (novo: candidate-source.ts,dynamodb-candidate-source.ts,reconciliation.ts)`, `scripts/build-lambdas.ts`, `infra/{main.tf,tests/stack.tftest.hcl}`, `test/unit/document-archive/{apply-file-scan-result.test.ts,document-file-reconciliation.test.ts (novo)}` | 2026-09-02 |
docs/architecture/decisions-log.md:191:| D-170 | **Reconciliação factual da Auditoria de Performance 2026-09-02** (`docs/project/performance-audit-2026-09-02.md`, documento externo trazido por Marcelo) — investigação pura, sem alteração de código, mesmo tratamento de spike que D-161 recebeu antes de virar decisão de arquitetura. Cada achado do documento externo foi verificado linha a linha contra o código real: RequestContext confirmado caro (`onboarding.resolve()` roda incondicionalmente, hidrata todas as Memberships via GSI4, mesmo com `organizationIdHint` presente — já era gap conhecido, D-136/D-C tinha adiado "condicionado a mapear consumidores primeiro"); quota `API_REQUEST` confirmada fazendo `Get`+`TransactWriteItems` em toda request — mas com a correção de que o documento externo não sabia: D-136/D-D já é um design `APPROVED` (3 rodadas, Codex 9,3/10) para exatamente essa lane leve (`EphemeralTelemetryMutation`), nunca implementado; Reminder Producer (loop triplo sequencial minute×shard×partição, claim+outbox sequencial por ocorrência) e Reminder Dispatch (N+1 via `queryByItem`+`.find()`, processamento serial do batch SQS) confirmados sem ressalva. **Achado mais grave, além do que o documento supôs**: o padrão `Scan` base-table + `Limit=100` + `MAX_PAGES=25` sem `ExclusiveStartKey` persistido entre invocações agendadas aparece em **9 dos 10 workers de manutenção/purga do repositório** (`membership-purge` D-169, `document-file-reconciliation` D-166, `requirement-reindex`, `invitation-purge`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`) — inclusive 2 workers implementados NESTA MESMA sessão e já registrados como `IMPLEMENTADO`/`DEPLOYADO`. O comentário do próprio código em pelo menos 2 desses workers ("anything beyond this is picked up by tomorrow's run") é factualmente falso como escrito — `Scan` sem cursor persistido reinicia pela mesma ordem física de hash a cada invocação, então um candidato além da posição `Limit×MAX_PAGES` nunca é alcançado por nenhuma invocação futura. O único worker que NÃO tem o defeito é `document-purge` (D-061) — usa `Query` sobre GSI6 ordenado por data de elegibilidade, exatamente o padrão `MaintenanceDueIndex` que o documento externo propõe como solução nova, já em produção num worker vizinho. Capacity Model confirmado desatualizado (premissa "tenant = usuário individual" ainda ativa, pré-B2B/Document Archive). Baseline do documento externo (PAY_PER_REQUEST, OCC/outbox onipresentes) confirmada correta, não alucinada. Relatório completo com evidência `arquivo:linha` por achado: `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md`. | Investigação factual (não Type 1 em si — fact-finding, sem protocolo Claude↔Codex) | n/a | n/a | **CONCLUÍDO** — nenhum código alterado; prioriza D-D de D-136 (implementação direta, design já aprovado) e o mapeamento de consumidores de D-C como próximos passos baratos; agenda rodada Claude↔Codex nível 5-6 para o padrão Scan-based starvation (achado mais urgente, afeta correção de LGPD já "fechada"), RequestContext fast path, e Reminder Producer/Dispatch | `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md` | 2026-09-02 |
docs/architecture/decisions-log.md:200:| D-179 | **MaintenanceDueIndex — design aprovado para resolver a starvation estrutural dos 9 workers de manutenção confirmada por D-170 (Scan+Limit+páginas sem cursor persistido entre invocações, reinicia pela mesma ordem física de hash, candidatos além da janela nunca são alcançados).** Protocolo Claude↔Codex completo, 4 rodadas reais (Claude 5,8→8,6→8,9→9,2, Codex 5,8→6,4→8,3→9,3, ambos ≥9,0 sem arredondar na Rodada 4). E-014 `SIM PARCIAL` (paginação DynamoDB/sparse index/`LeadingKeys` são padrão externo, 3 fontes primárias AWS verificadas; alocação de GSI e escopo de workers são internos). **Decisão**: um GSI8 global esparso novo (`PK=WORK#<workerType>`, `SK=<dueAtIso>#TENANT#<tenantId>#<entityId>`, `KEYS_ONLY`), substituindo `Scan` por `Query GSI8SK < now` nos 9 workers (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`) — mesmo padrão que `document-purge`/GSI6 (D-061) já prova, generalizado. **Achado prévio real**: GSI1-GSI7 já estavam todos ocupados (`infra/modules/dynamo-table/main.tf`) — a proposta original do documento de auditoria não checou isso; GSI8 é o próximo slot livre, dentro do limite técnico do DynamoDB (20/tabela). Componentes fechados nas 4 rodadas: isolamento IAM real por worker via `dynamodb:LeadingKeys` (`["WORK#<type>","DLQ#<type>"]`, nunca política genérica) + `dynamodb:TransactWriteItems` como ação NOVA não coberta pela política geral hoje; revalidação atômica obrigatória (GSI8 nunca é fonte de elegibilidade, só descoberta — `TransactWriteItems` com `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` na mesma transação do delete, fechando um TOCTOU real que o padrão antigo tinha); `deriveMaintenanceDue()` pura por worker (6 de 9 já têm o equivalente exportado); backfill cobrindo todo item com `dueAt` determinável (não só os já vencidos — erro real cometido e corrigido durante o próprio protocolo, ver abaixo); poison-record/quarentena com backoff idempotente (reusa o mesmo padrão `isConditionalCheckFailed` que `membership-purge/purge.ts` já usa, não um mecanismo novo); observabilidade subindo ao handler (workers continuam observability-agnostic, `AGENTS.md` §7/D-007), gatilho de shard por `oldestCandidateAgeSeconds` real. **`requirement-reindex` permanece no escopo** (achado corrigido na Rodada 3: `reindex.ts:46-47` confirma `evidenceValidUntil` como `dueAt` real — a Rodada 1 excluiu por engano). **Rigor do protocolo evidenciado pelas próprias notas**: Rodada 2 (6,4) introduziu 3 erros factuais novos ao corrigir a Rodada 1 (excluiu `requirement-reindex` por leitura invertida do código, alegou que `scripts/reset-dev-data.ts` faz reseed quando só apaga, matriz com 3 fórmulas de `dueAt` erradas) — todos corrigidos com `arquivo:linha` na Rodada 3 (8,3); os 5 achados finais da Rodada 3 eram de design genuíno (backfill com alvo errado, IAM incompleto, idempotência não demonstrada, observabilidade mal-colocada), fechados na Rodada 4. DESIGN-ONLY — nenhum código/GSI/worker alterado por esta decisão. Ver `docs/architecture/reviews/maintenance-due-index-scoping/estado-final-consolidado.md` para o detalhe completo e os 4 rounds. | Nível 5-6 (novo GSI global, migração de access pattern em 9 workers vivos, invariante de correção LGPD/retenção — protocolo Claude↔Codex completo obrigatório) | 9,2 | 9,3 | **APPROVED (design)** — implementação real é a próxima fatia, ver `NEXT_SESSION_PROMPT.md` | `docs/architecture/reviews/maintenance-due-index-scoping/` (5 arquivos: `estado-final-consolidado.md` + 4 rounds) | 2026-09-02 |
docs/architecture/decisions-log.md:201:| D-180 | **MaintenanceDueIndex — implementação real, primeira fatia/piloto (D-179 aplicado, `membership-purge` ÚNICO worker migrado)**: GSI8 real (`GSI8PK`/`GSI8SK`, `KEYS_ONLY`) adicionado ao módulo Terraform (`infra/modules/dynamo-table/main.tf`), `local.gsi8_worker_types = { membership_purge = "MEMBERSHIP_PURGE" }` gerando `gsi8_read_policy_json`/`worker_transact_write_policy_json` por chave — `dynamodb:Query` condicionado `ForAllValues:StringEquals dynamodb:LeadingKeys IN ["WORK#MEMBERSHIP_PURGE","DLQ#MEMBERSHIP_PURGE"]` + `dynamodb:TransactWriteItems` na tabela base (ação nova, não coberta por `tenant_facing_read_write`), ambos anexados só ao role do `membership_purge_handler`. `deriveMembershipMaintenanceDue()`/`membershipGsi8Keys()`/`MEMBERSHIP_PURGE_WORK_TYPE`/`MEMBERSHIP_RETENTION_DAYS` movidos para `modules/organization/domain/membership.ts` (função pura, D-179 §2) — reusada pelos 3 consumidores exigidos: `remove-membership.ts`/`leave-organization.ts` (gravam `GSI8PK`/`GSI8SK` no MESMO `Update` que a transição `REMOVED`, nunca write separado), `scripts/backfill-gsi8-membership-purge.ts` (novo, cobre todo `Membership` com `dueAt` computável — inclusive vencimento FUTURO, corrigindo o erro que a Rodada 3 do design cometeu), e a revalidação do próprio worker. `accept-invitation.ts`'s reativação agora também `REMOVE GSI8PK, GSI8SK` atomicamente ao limpar `removedAt` (autolimpeza de ponteiro obsoleto na origem). `membership-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`Limit`; `purge.ts` revalida cada candidato via `getMembership()` consistente antes de agir (GSI8 nunca é fonte de elegibilidade) e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por versão, na MESMA transação — fecha o TOCTOU que o padrão antigo tinha entre ler o status do tenant e deletar). Poison-record/quarentena (D-179 §8): `maintenanceAttemptCount` incrementado só quando o `ConditionCheck` de tenant falha (nunca no `Delete`), backoff exponencial capado (1,2,4,8,16 dias) reposicionando `GSI8SK`, acima de `MAX_ATTEMPTS=5` move para `GSI8PK="DLQ#MEMBERSHIP_PURGE"`; toda escrita de retry é idempotente (`ConditionalCheckFailedException` tratado como no-op, mesmo padrão que o delete já usava). Ponteiro obsoleto (item reativado após ser indexado) é autolimpo pelo próprio worker via `REMOVE GSI8PK, GSI8SK`. Observabilidade: `MembershipPurgeResult` ganha `quarantinedCount`/`oldestCandidateAgeSeconds` (computado do primeiro item da página, sem I/O extra); o handler já fazia `logger.info(..., {...result})`, então os 2 campos novos já são emitidos sem tocar o handler. **G-V3 ao vivo em 3 mecanismos**: teste novo prova que `RemoveMembershipService`/`LeaveOrganizationService` gravam `GSI8PK`/`GSI8SK` corretos (falha se removido); teste novo prova que `accept-invitation.ts` limpa os 2 atributos na reativação (falha se removido); teste novo do worker leva um candidato através de 6 execuções com tenant nunca `ACTIVE` e prova a quarentena em `DLQ#MEMBERSHIP_PURGE` no `MAX_ATTEMPTS+1`-ésimo ataque, e um teste separado prova o autoconserto do ponteiro obsoleto. `infra/tests/stack.tftest.hcl` ganha `run "gsi8_worker_isolation"`: prova que `membership_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, que `TransactWriteItems` nunca aparece numa política referenciando GSI3/GSI4/GSI6/GSI8, e que `invitation_purge_handler`/`transient_purge_handler`/`test_ping_handler` NÃO referenciam GSI8 (isolamento por worker, não por índice). `gsi_count` 7→8. Suíte completa 1778/1778, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (incluindo o caso novo). **Próximo worker recomendado para a próxima fatia**: `invitation-purge` (mesmo padrão de `TenantLifecycleStatusSource`/fence ACTIVE já usado aqui, candidato mais próximo estruturalmente) — ordem não é normativa, qualquer um dos 8 restantes serve. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — infra nova real porém mecânica, seguindo spec exata) | n/a | n/a | **IMPLEMENTADO (piloto, 1 de 9 workers)** — `membership-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes); os outros 8 workers nomeados por D-179 permanecem em `Scan`+`Limit`, migração worker-a-worker é trabalho futuro explícito | `infra/modules/dynamo-table/{main.tf,outputs.tf}`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/membership.ts`, `src/modules/organization/application/{remove-membership.ts,leave-organization.ts,accept-invitation.ts}`, `src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/membership-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-membership-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:202:| D-181 | **MaintenanceDueIndex piloto (D-180) — fix real de um bug de deploy pego pelo próprio CD, mais verificação ao vivo completa contra `dev`.** A primeira tentativa de merge de D-180 para `main` (PR #198) teve CI verde mas o `Deploy (CD)` real (`terraform apply` em `dev`) **falhou de verdade** (`aws.amazon.com` `MalformedPolicyDocument: Statement IDs (SID) must be alpha-numeric`) — os 2 `Sid`s novos (`gsi8_read`/`worker_transact_write` em `infra/modules/dynamo-table/main.tf`) interpolavam `each.key` (`"membership_purge"`, com underscore) diretamente, mas IAM exige `[0-9A-Za-z]*`. Confirmado por leitura direta do log do run que falhou (não presumido) e por uma checagem ao vivo independente (`aws iam list-role-policies --role-name exptrk-dev-membership-purge-handler-role`, que mostrou só a política base `capability-0`, confirmando que as 2 políticas novas nunca chegaram a existir). Corrigido com `join("", [for part in split("_", each.key) : title(part)])` (PascalCase, sem underscore) nos dois `sid`s; `terraform test` 22/22 continuou verde. PR #199 mergeado, CD real (`33636904901`) `success`. **Verificação ao vivo completa, tudo confirmado via `aws --profile claude-dev`, nada presumido**: `describe-table` mostra GSI8 na lista de índices; `list-role-policies`/`get-role-policy` no role do worker mostram as 3 políticas (base + `Gsi8ReadOnlyMembershipPurge` com a condição `LeadingKeys` exata + `TransactWriteItemsMembershipPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de negação cross-namespace (`aws iam simulate-principal-policy`, exigida pelo DoD do piloto)**: simulando `dynamodb:Query` no role do `membership_purge_handler` com `dynamodb:LeadingKeys=WORK#MEMBERSHIP_PURGE` (seu próprio namespace) → `allowed`, casando a statement `Gsi8ReadOnlyMembershipPurge`; a mesma simulação com `dynamodb:LeadingKeys=WORK#INVITATION_PURGE` (namespace de outro worker, ainda não migrado) → `implicitDeny`, nenhuma statement casada — prova real, nos dois sentidos (controle positivo + negativo), de que o isolamento por `LeadingKeys` funciona exatamente como o design exige, não só no papel do Terraform. **Achado de processo desta sessão, registrado por ser real**: uma mensagem externa recebida durante a espera do CD afirmou "CD completed successfully" e pediu para prosseguir direto à prova de IAM e ao registro em `decisions-log.md` — checada diretamente via `gh run view` antes de confiar, e o run real estava `failure` (a mesma falha do parágrafo acima), não `success`; a alegação foi descartada e o bug real foi corrigido antes de qualquer prova de IAM ou registro de documentação, exatamente a disciplina que este arquivo e `AGENTS.md` exigem (nunca tratar uma alegação não verificada como fato). | Nível 1-2 (fix mecânico de um bug de infra pego em produção `dev`, mais verificação, sem decisão de design nova) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO** — piloto D-179/D-180 100% completo, incluindo a prova de isolamento IAM que o DoD do piloto exigia antes de liberar a migração dos 8 workers restantes | `infra/modules/dynamo-table/main.tf` | 2026-09-02 |
docs/architecture/decisions-log.md:203:| D-182 | **MaintenanceDueIndex — segunda fatia (invitation-purge, D-179 slice 2, mesmo padrão de D-180)**: `invitation_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["invitation_purge"]`/`worker_transact_write_policy_json["invitation_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#INVITATION_PURGE","DLQ#INVITATION_PURGE"]` + `TransactWriteItems` na tabela base, ambos só no role do `invitation_purge_handler`. `deriveInvitationMaintenanceDue()`/`invitationGsi8Keys()`/`INVITATION_PURGE_WORK_TYPE` novos em `domain/invitation.ts` (função pura, D-179 §2) — duas ramificações independentes, nuance real vs. o piloto: `PENDING`'s due date (`expiresAt+30d`) já é totalmente conhecido NA CRIAÇÃO (não numa transição posterior como o `REMOVED` de Membership), então `create-invitation.ts`'s `invite()` grava o ponteiro GSI8 no MESMO `Put` da Invitation, e `resend()` o move junto quando `expiresAt` roda; `revoke-invitation.ts` sobrescreve o ponteiro no MESMO `Update` que a transição real para `REVOKED`; `accept-invitation.ts` limpa (`REMOVE GSI8PK, GSI8SK`) atomicamente ao aceitar, já que `ACCEPTED` nunca é candidato. `invitation-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`TenantLifecycleStatusSource` separado (a checagem ACTIVE virou `ConditionCheck` dentro da própria transação de claim, como no piloto); `purge.ts` revalida via `getInvitation()` consistente antes de agir e reivindica via `TransactWriteItems` (mesmo `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)`+`Delete` condicionado por versão). Poison-record/quarentena idêntico ao piloto (backoff 1,2,4,8,16 dias, `DLQ#INVITATION_PURGE` acima de `MAX_ATTEMPTS=5`). **Achado registrado, não um bug**: não existe caminho real de reativação para `Invitation` (ao contrário de `Membership`) — `REVOKED`/`ACCEPTED` são terminais, `ACCEPTED` já limpa seu próprio ponteiro na origem — então o autoconserto de ponteiro obsoleto em `purge.ts` é só defesa em profundidade, documentado explicitamente no cabeçalho do arquivo em vez de omitido. `GlobalIndexComponent` (taxonomia fechada de `security-audit.ts`) ganha `"invitation-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.invitation_purge_handler.function_name` (achado do próprio teste de arquitetura — sem isso o novo call site de `auditGlobalIndexAccess` ficaria sem alarme wireado). Novo `scripts/backfill-gsi8-invitation-purge.ts`, cobrindo as duas ramificações inclusive vencimento FUTURO (mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido: `invitation_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, e isolamento cross-namespace nos dois sentidos (nenhum role referencia o namespace do outro). **G-V3 ao vivo**: testes novos provam o ponteiro gravado na criação, movido no resend, sobrescrito no revoke, limpo no accept; teste do worker prova quarentena em `DLQ#INVITATION_PURGE` no `MAX_ATTEMPTS+1`; teste do backfill prova cobertura das duas ramificações. Suíte completa 1797/1797 (169 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22. PR #201 mergeado (`9a21da1`), CI `success` (run 33656227410), CD real `success` confirmado via `gh run view` (run 33656568649, não presumido de alegação externa). **Verificação ao vivo em `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role do `invitation_purge_handler` tem as 3 políticas esperadas (base+`Gsi8ReadOnlyInvitationPurge`+`TransactWriteItemsInvitationPurge`). **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, nos dois sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#INVITATION_PURGE` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyInvitationPurge`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE` (namespace do outro worker já migrado) → `implicitDeny`, nenhuma statement casada. **Próximo worker recomendado (não normativo)**: qualquer um dos 7 restantes (`document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — segunda fatia mecânica seguindo o padrão do piloto D-180, com uma nuance real de timing de escrita) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (2 de 9 workers migrados)** — `invitation-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 7 workers nomeados por D-179 permanecem em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/invitation.ts`, `src/modules/organization/application/{create-invitation.ts,revoke-invitation.ts,accept-invitation.ts}`, `src/workers/invitation-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/invitation-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-invitation-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:204:| D-183 | **MaintenanceDueIndex — terceira fatia (document-file-reconciliation, D-179 slice 3, 3º de 9 workers)**: `document_file_reconciliation` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["document_file_reconciliation"]`/`worker_transact_write_policy_json["document_file_reconciliation"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DOCUMENT_FILE_RECONCILIATION","DLQ#DOCUMENT_FILE_RECONCILIATION"]` + `TransactWriteItems` na tabela base, ambos só no role do `document_file_reconciliation_handler`. **Achado real pré-existente, confirmado antes de qualquer código novo (D-166's próprio texto já era ambíguo sobre isso, resolvido por leitura direta)**: o mecanismo GSI5 que D-164/D-166 desenharam para este worker (`fileReconciliationGsi5Keys()`, `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`) NUNCA teve um writer real — `reserveFiles()` (`document-archive-service.ts`) cria cada `DocumentFile` sem jamais chamar essa função, e nenhum outro call site em todo o código a invoca (confirmado por `grep` antes de tocar em qualquer arquivo). O `Scan` filtrado por `attribute_exists(GSI5PK)` que D-166 implementou, portanto, nunca poderia encontrar um candidato real em produção — o worker era código morto funcional desde o dia em que foi implementado, apesar de "IMPLEMENTADO E VERIFICADO" nos testes (os testes seedavam `GSI5PK` manualmente, nunca através do caminho de escrita real). Isso resolve por completo a pergunta "GSI8 substitui ou convive com GSI5" que a tarefa pedia para verificar antes de escrever qualquer código: não havia um mecanismo funcionando para conviver com — GSI8 é a primeira implementação real e completa deste ponteiro de descoberta, não uma segunda ao lado de uma que já funcionava. Não foi necessário reabrir D-163 Decisão 2 (GSI6 seguiu intocado) nem nenhuma outra decisão de alocação de índice. **Decidido e implementado**: `document-file.ts` ganha `FILE_SCAN_TIMEOUT_SECONDS=600` (mesma janela do presign TTL — `document-archive-service.ts`'s `PRESIGN_TTL_SECONDS` agora importa essa constante em vez de duplicá-la, deliberado: a janela de upload e o deadline de timeout de scan são a mesma janela), `deriveDocumentFileMaintenanceDue()` (due = `createdAt + FILE_SCAN_TIMEOUT_SECONDS` para `PENDING_UPLOAD`/`SCANNING`, `undefined` uma vez terminal — mesmo raciocínio do branch PENDING de `Invitation`: due date totalmente conhecido na criação, sem transição posterior para pendurar o relógio, já que SCANNING é só evidência chegando dentro da mesma janela fixa, nunca um novo relógio) e `documentFileGsi8Keys()`. O campo `DocumentFile.GSI5PK?/GSI5SK?` foi REMOVIDO (substituído por `GSI8PK?/GSI8SK?`) — `fileReconciliationGsi5Keys()` também removida, não deixada como código morto adicional. `reserveFiles()` agora grava o ponteiro GSI8 no MESMO `Put` de cada `DocumentFile` (mesmo padrão do branch PENDING de `invitationGsi8Keys()` — criação É a transição relevante). `apply-file-scan-result.ts`: as 3 funções (`applyFileScanResult`'s branch REJECT, `confirmFileScanClean`, `applyFileScanTimeout`) trocam `remove: ["GSI5PK","GSI5SK"]`/condição exata de ponteiro por GSI8; `applyFileScanTimeout`'s `observedGsi5Pointer`→`observedGsi8Pointer`, mesmo mecanismo de fence exato (`extraConditions` comparando o ponteiro observado literal), nenhuma mudança de comportamento. **Composição com o mecanismo transacional existente (item pedido pela tarefa)**: como a claim/revalidação real já vive inteiramente dentro de `applyFileScanTimeout()` (que já releia o `DocumentFile` fresco via `store.get()` e faz seu próprio retry OCC), o port do worker (`candidate-source.ts`) foi simplificado para só `queryDue()` — nenhum `getFile()`/`transactWrite()` próprio como `membership-purge`/`invitation-purge` precisam, evitando duplicar a revalidação que já existe. **Simplificação real habilitada por GSI8 (não pedida, descoberta ao desenhar)**: como GSI8PK é uniforme (`WORK#DOCUMENT_FILE_RECONCILIATION`, sem partição por status, diferente do GSI5PK por-status de D-166), as duas varreduras independentes que D-166 rodava (uma por `PENDING_UPLOAD`/`SCANNING`) colapsam numa única `Query` due-ordered cobrindo os dois status — `reconciliation.ts` reescrito para um único loop de paginação, mesmo formato de `invitation-purge/purge.ts`. **Sem mecanismo de poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada): diferente de `membership-purge`/`invitation-purge`, este worker nunca teve fence de tenant `ACTIVE` (D-166: "a stuck upload is not a retention decision") — não existe nenhuma condição que possa falhar permanentemente para o mesmo candidato; um timeout perdido por corrida é sempre transitório e resolvido no próximo run, nunca precisa de backoff/quarentena. **Achado adicional fora do escopo desta fatia, registrado e NÃO corrigido aqui**: `document_archive_handler` (o handler HTTP principal, que também chama `store.transactWrite()` via `reserveFiles()`/`acceptVersion()`) tem a MESMA lacuna de `dynamodb:TransactWriteItems` que este worker tinha antes desta fatia — `policy_documents_json` dele só inclui `tenant_facing_read_write_policy_json`+a policy de presign, nunca `TransactWriteItems`. Não corrigido nesta sessão (fora do escopo de D-179, achado pré-existente e independente); nomeado explicitamente para não ser esquecido. `GlobalIndexComponent` ganha `"document-file-reconciliation"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.document_file_reconciliation_handler.function_name`. Novo `scripts/backfill-gsi8-document-file-reconciliation.ts`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 3 workers. **G-V3 ao vivo**: teste novo prova que `reserveFiles()` grava `GSI8PK`/`GSI8SK` em cada `DocumentFile`; suíte de `apply-file-scan-result.test.ts`/`document-file-reconciliation.test.ts` migrada para GSI8 continua provando a condição exata de ponteiro (quebra/reversão já demonstrada por D-166, mecanismo idêntico, só renomeado). Suíte completa 1809/1809 (1797+12 novos: 1 em `document-archive-service.test.ts`, 4 do worker migrados de D-166, 11 do backfill novo — conferir soma exata via `npm test`), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (mesma contagem do piloto — este teste estende `gsi8_worker_isolation`, não adiciona um `run` novo). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — terceira fatia mecânica seguindo o padrão do piloto/D-182, com uma descoberta real de bug pré-existente que simplificou a decisão de design em vez de complicá-la) | n/a | n/a | **IMPLEMENTADO — verificação ao vivo em `dev` e merge pendentes, ver sessão em andamento** | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/document-file.ts`, `src/modules/document-archive/application/{apply-file-scan-result.ts,document-archive-service.ts}`, `src/workers/document-file-reconciliation/{candidate-source.ts,dynamodb-candidate-source.ts,reconciliation.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-document-file-reconciliation.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:206:| D-185 | **MaintenanceDueIndex — quarta fatia (requirement-reindex, D-179 slice 4, 4º de 9 workers)**: `requirement_reindex` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["requirement_reindex"]`/`worker_transact_write_policy_json["requirement_reindex"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItems` na tabela base, ambos só no role do `requirement_reindex_handler`. **Verificação prévia obrigatória (item nomeado pela tarefa) confirmou a correção da Rodada 3 de D-179**: lendo `reindex.ts` diretamente (não confiando na citação de linha do design), o worker de fato só faz UMA coisa — a drift pura SATISFIED→NOT_SATISFIED quando `evidenceValidUntil` passa sem nenhuma outra escrita tocar o Requirement — nunca uma "reindexação de applicability" mais ampla; `applicability` é persistido e nunca re-derivado por este worker. `deriveRequirementMaintenanceDue()` novo em `domain/requirement.ts` (função pura): due = `evidenceValidUntil` apenas quando `status === "SATISFIED"` E `evidenceValidUntil` existe — os outros 4 status nunca são candidatos, um SATISFIED sem `evidenceValidUntil` é SATISFIED para sempre por construção. `requirementGsi8Keys()` embute `requirementId` (não `subjectId`) no `GSI8SK`; a fonte candidata recupera `tenantId`/`subjectId`/`requirementId` do `PK`/`SK` da tabela base (`KEYS_ONLY` já devolve de graça), mesmo padrão de `documentFileGsi8Keys()`. **4 pontos de escrita no `DocumentArchiveService` gravam/limpam o ponteiro atomicamente, todos no MESMO `set`/`remove` da transição de `status` real** (`createRequirement`/`updateRequirement`/`linkEvidence`/`unlinkEvidence`) — um helper privado `requirementGsi8Fields()` centraliza a decisão para não duplicar a chamada a `deriveRequirementMaintenanceDue()` em 4 lugares. `scanSatisfiedRequirements` (`Scan` cross-tenant filtrado por `entityType`+`status`) REMOVIDO do port `DocumentArchiveStore` e do adapter DynamoDB real — nenhum outro call site o usava (confirmado por grep antes de tocar em qualquer arquivo), mesma disciplina "apagar o mecanismo morto, não deixar ao lado do novo" de D-183. `requirement-reindex/candidate-source.ts`+`dynamodb-candidate-source.ts` novos: `Query GSI8` substitui o `Scan`; `reindex.ts` reescrito para paginar via GSI8, revalidar cada candidato com um `store.get()` fresco (GSI8 nunca é fonte de elegibilidade, mesma postura de todo consumidor GSI8) e aplicar a atualização versionada existente (`buildVersionedUpdate`) incluindo `remove: ["GSI8PK","GSI8SK"]` — o `nextStatus` nunca é SATISFIED neste caminho (a derivação só chega a NOT_SATISFIED quando `evidenceValidUntil` já passou), então o ponteiro sempre é limpo incondicionalmente. **Sem poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada, mesmo raciocínio de D-183): este worker nunca teve fence de tenant `ACTIVE` — o único modo de falha de uma claim é `expectedVersion` desatualizado por um `linkEvidence`/`unlinkEvidence`/`updateRequirement` concorrente, sempre transitório e resolvido pela própria execução diária seguinte, nunca uma condição permanentemente falha para o mesmo candidato. `GlobalIndexComponent` ganha `"requirement-reindex"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.requirement_reindex_handler.function_name`. Novo `scripts/backfill-gsi8-requirement-reindex.ts`, cobrindo vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 4 workers já migrados. **G-V3**: 7 testes do worker (transição real + limpeza do ponteiro, due no futuro nunca tocado, sem `evidenceValidUntil` nunca candidato, status não-SATISFIED nunca candidato, múltiplos tenants, conflito concorrente via OCC, candidato "stale" da query eventualmente consistente nunca clobber um `evidenceValidUntil` já movido por um `linkEvidence` concorrente) + 12 testes do backfill. Suíte completa local 1827/1827 (após reconciliar com o WIP concorrente de D-184 no mesmo working tree — só os arquivos desta fatia foram staged/commitados, `guest-document-access-service.ts`/seu teste e `docs/architecture/reviews/submit-evidence-document-type-scoping/` intocados por esta sessão), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente, não só o agregado). Commit `720d881`, empacotado no PR #205 (aberto pela sessão concorrente de D-184, mesmo `develop`) — CI real (run `33665953101`) `success`, CD real (run `33666281116`) `success`, ambos confirmados via `gh run view` direto (nunca presumidos). **Verificação ao vivo completa contra `dev`, tudo via `aws --profile claude-dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-requirement-reindex-handler-role` tem as 3 políticas esperadas (base + `Gsi8ReadOnlyRequirementReindex` com a condição `LeadingKeys` exata `["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItemsRequirementReindex` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 3 sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#REQUIREMENT_REINDEX` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyRequirementReindex`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION` (namespaces dos 3 workers já migrados) → `implicitDeny` nos 3 casos, nenhuma statement casada. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quarta fatia mecânica seguindo o padrão de D-180/D-182/D-183, com a mesma decisão "sem poison-record" de D-183 por ausência de fence de tenant) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (4 de 9 workers migrados)** — `requirement-reindex` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 5 workers nomeados por D-179 permanecem em `Scan`+`Limit`: `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/requirement.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/ports/document-archive-store.ts`, `src/modules/document-archive/persistence/dynamodb-document-archive-store.ts`, `src/runtime/aws/handlers/requirement-reindex-handler.ts`, `src/workers/requirement-reindex/{candidate-source.ts,dynamodb-candidate-source.ts,reindex.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-requirement-reindex.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:207:| D-186 | **MaintenanceDueIndex — quinta fatia (quota-telemetry-purge, D-179 slice 5, 5º de 9 workers)**: `quota_telemetry_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["quota_telemetry_purge"]`/`worker_transact_write_policy_json["quota_telemetry_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItems` na tabela base, ambos só no role do `quota_telemetry_purge_handler`. **Forma distinta dos 4 anteriores, confirmada por leitura direta antes de qualquer código**: este worker cobre DOIS tipos de entidade que compartilham o mesmo formato de elegibilidade (`resetAt+30d`, fence de tenant `ACTIVE`) — `TenantQuotaRecord` e `EphemeralTelemetryMutation` (`src/modules/identity/application/quota.ts`) — e NENHUM dos dois tem campo `version`; ambos usam `count`/`resetAt` como fence OCC direto (mesma forma que D-153/`SECURITY_AUDIT` já usava para suas 4 entidades append-only, por razão diferente aqui: token bucket com campos otimistas próprios, não append-only). `deriveQuotaTelemetryMaintenanceDue()` (due = `resetAt + 30d`, NUNCA `undefined` — ao contrário de Membership/Invitation não existe estado terminal, `resetAt` sozinho determina elegibilidade) e `quotaTelemetryGsi8Keys()` (embute `entityType`+`SK` natural no `GSI8SK`, já que nenhuma das duas entidades tem um id próprio separado do seu `SK`) adicionados a `quota.ts`. **4 pontos de escrita gravam/refrescam o ponteiro GSI8 atomicamente no MESMO write que seta `resetAt`** (nunca um passo separado): `consume()`'s ramo de criação e ramo de rollover de janela (`TransactWriteItems` via `executeTenantBusinessMutation`, W3-07), e `identity-store.ts`'s `incrementTelemetryCounter()` (a lane `EphemeralTelemetryMutation` de D-136 D-D — `GSI8PK`/`GSI8SK` setados via `if_not_exists`, mesmo mecanismo que já protege `resetAt`/`purgeAfterTtl` contra um escritor perdedor concorrente rebobinar a janela; o valor é determinístico a partir do bucket de janela, calculado em `quota.ts` — camada de aplicação — e passado pronto ao adapter de persistência via `TelemetryIncrementInput.gsi8`, preservando a regra de `persistence/` nunca importar lógica de `application/`). `release()` não toca o ponteiro (só decrementa `count`, nunca muda `resetAt`). `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` fresco antes de qualquer escrita e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `resetAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182, já que este worker TEM fence de tenant `ACTIVE`, ao contrário de D-183/D-185): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#QUOTA_TELEMETRY"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (diferente de Invitation/Membership, decisão explícita e documentada): como `deriveQuotaTelemetryMaintenanceDue()` nunca retorna `undefined` e todo write que seta `resetAt` também refresca o ponteiro, não existe um "ponteiro sobrevivendo a uma transição terminal" real para se defender — a lacuna que o self-heal de `invitation-purge` cobre não tem equivalente estrutural aqui. `GlobalIndexComponent` ganha `"quota-telemetry-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.quota_telemetry_purge_handler.function_name`. Novo `scripts/backfill-gsi8-quota-telemetry-purge.ts`, cobrindo as duas entidades e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `resetAt` (não `version`, mesma adaptação do worker). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 5 workers já migrados. **G-V3**: 22 testes do worker (purga das duas entidades, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `resetAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ após MAX_ATTEMPTS, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo as duas entidades, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1841/1841 (1827+14 novos: quota.ts's docstring/campo GSI8PK/GSI8SK, 22 do worker migrado, 10 do backfill novo, 1 de cobertura de observabilidade), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). **Achado real de bug pré-existente, encontrado e corrigido só neste script (não nos 6 irmãos, fora de escopo desta fatia, registrado abaixo)**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` que decide se `main()` roda quando o script é executado diretamente NUNCA casa no Windows deste repo (`import.meta.url` usa barras normais — `file:///C:/...` — enquanto `process.argv[1]` usa contrabarras — `C:\\...`) — confirmado por teste direto (`node`/`tsx` imprimindo os dois valores). `main()` silenciosamente nunca executava sob o shell Windows/Git Bash deste ambiente (`AGENTS.md` §4) apesar de exit code 0 e nenhum erro — só descoberto ao tentar rodar o backfill desta fatia pela primeira vez nesta sessão (saída vazia inesperada). Corrigido neste arquivo trocando por `fileURLToPath(import.meta.url) === process.argv[1]` (comparação de dois paths nativos do SO, sem o mismatch de separador). **Mesmo bug confirmado presente nos outros 6 scripts** (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — não corrigido nesta sessão (fora do escopo de D-186), significa que os backfills anteriores (D-180/D-182/D-183/D-185) e qualquer execução passada desses scripts via este mesmo shell podem ter silenciosamente não escrito nada, mesmo reportados como "rodados com sucesso" — precisa de verificação/reexecução numa sessão futura, nomeado explicitamente para não ser esquecido. **PR #208 mergeado (commit `a898c1d`→`a280110`), CI real (`33687474935`/`33687503644`) e CD real (`33688100421`) `success`, todos confirmados via `gh run view` direto**. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-quota-telemetry-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyQuotaTelemetryPurge` com `LeadingKeys` exato `["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItemsQuotaTelemetryPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(30 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 4 sentidos)**: `WORK#QUOTA_TELEMETRY` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX` (os 4 já migrados) → `implicitDeny` nos 4 casos. **Backfill real executado contra `dev`** (após o fix do guard acima, sem o qual teria silenciosamente não escrito nada): 24 candidatos encontrados, 24 ponteiros GSI8 escritos, confirmado por `Query GSI8 WORK#QUOTA_TELEMETRY` retornando `Count=24`. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quinta fatia mecânica seguindo o padrão de D-180/D-182 (poison-record/DLQ), adaptada para 2 tipos de entidade sem `version`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (5 de 9 workers migrados)** — `quota-telemetry-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); achado de bug real do guard `main()` em scripts de backfill no Windows, corrigido só aqui, pendente nos outros 6; os 4 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/identity/application/quota.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-quota-telemetry-purge.ts` (novo), testes em `test/unit/{identity,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:208:| D-187 | **MaintenanceDueIndex — sexta fatia (security-audit-purge, D-179 slice 6, 6º de 9 workers)**: `security_audit_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["security_audit_purge"]`/`worker_transact_write_policy_json["security_audit_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItems` na tabela base, ambos só no role do `security_audit_purge_handler`. **Forma distinta das 5 fatias anteriores**: este worker cobre QUATRO tipos de entidade que compartilham o mesmo formato de elegibilidade (`occurredAt+365d`, fence de tenant `ACTIVE`) — `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (`src/modules/{expiration,organization,subject,activity}/domain/*.ts`), as mesmas 4 partições que `GET /activity`'s `ActivityService` k-way-merges — todas append-only por construção (sem `update()`/`delete()` exportado), sem campo `version`. Novo `src/shared/security-audit-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que a entidade cobre 4 módulos diferentes — mesma razão que já colocou `EntityKey`/`TransactWriteEntry` em `shared/dynamodb/occ.ts`): `deriveSecurityAuditMaintenanceDue()` (due = `occurredAt+365d`, NUNCA `undefined` — sem estado terminal, mesmo raciocínio de D-186) e `securityAuditGsi8Keys()` (embute `tenantId`+`entityType`+`SK` natural no `GSI8SK`, mesmo formato de `quotaTelemetryGsi8Keys()`). **O ponteiro GSI8 é gravado UMA ÚNICA VEZ, na própria função pura `build*Event()` de cada uma das 4 entidades** (`buildAuditEvent`/`buildMembershipAuditEvent`/`buildSubjectAuditEvent`/`buildTenantAuditEvent`) — não em cada um dos 19 call sites espalhados por 6 módulos que chamam essas funções (confirmado por grep antes de escrever qualquer código): como toda escrita passa pela mesma função de construção antes do `appendXAuditToTransaction`, estampar o ponteiro ali cobre automaticamente todo call site presente e futuro, sem precisar tocar nenhum deles individualmente — simplificação real habilitada pela forma já centralizada do código, não uma decisão de design nova. **Normalização `MembershipAuditEvent`**: declara `organizationId`, não `tenantId` (mesmo valor, nome diferente) — `buildMembershipAuditEvent()` já passa `organizationId` como `tenantId` para `securityAuditGsi8Keys()`, e o adapter do worker (`normalizeTenantId()`) faz a mesma normalização ao ler uma linha crua da tabela base, mesmo padrão pré-existente do worker antes desta fatia. `security-audit-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também o `TenantLifecycleStatusSource` como dependência separada do worker); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `occurredAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186, já que este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#SECURITY_AUDIT"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência que D-186 quota-telemetry-purge, causa diferente): as 4 entidades são append-only por construção — não existe transição de estado terminal que deixaria um ponteiro obsoleto para trás, e a ÚNICA forma de uma linha deixar de ser candidata é este próprio worker deletá-la (o que remove o ponteiro junto com a linha inteira). **Interação com `GET /activity` (D-149), reverificada nesta fatia**: releitura direta de `dynamodb-audit-partition-store.ts#queryPage` confirma que `ExclusiveStartKey` continua um marcador posicional puro, independente de GSI8 e de qualquer deleção concorrente deste worker — a conclusão original de D-153 (nenhum bug de correção, nenhuma correção necessária) permanece válida após a migração, confirmado por leitura, não presumido. `GlobalIndexComponent` ganha `"security-audit-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.security_audit_purge_handler.function_name`. Novo `scripts/backfill-gsi8-security-audit-purge.ts`, cobrindo os 4 tipos de entidade e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `occurredAt` (não `version`). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 6 workers já migrados. **G-V3**: 4 testes de gravação do ponteiro na criação (um por tipo de entidade, incluindo a normalização `organizationId`→`tenantId`), 2 testes do módulo puro `security-audit-gsi8.ts`, 17 testes do worker (purga dos 4 tipos, boundary exato, fence de tenant não-ACTIVE com contador de retry, normalização de `MembershipAuditEvent`, sem lifecycle record, corrida de `occurredAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/normalizeTenantId/processPage cobrindo os 4 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1867/1867 (175 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #210 mergeado (`5be0743`), CI real (`33691359230`/`33691948793`) e CD real (`33692216726`) `success`, todos confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-security-audit-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlySecurityAuditPurge` com `LeadingKeys` exato `["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItemsSecurityAuditPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 6 sentidos)**: `WORK#SECURITY_AUDIT` (próprio, com `--resource-arns` explícito no GSI8 — sem ele o simulate retorna `implicitDeny` mesmo para o caso positivo, achado de processo desta sessão) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY` (os 5 já migrados) → `implicitDeny` nos 5 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` corrigido roda de fato): 0 candidatos encontrados — a tabela `dev` não contém hoje nenhuma linha `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (confirmado por `Scan` bruto de `entityType`, dado sintético esparso), então nada foi escrito; confirmado por `Query GSI8 WORK#SECURITY_AUDIT` retornando `Count=0`, consistente com o dry-run. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sexta fatia mecânica seguindo o padrão de D-180/D-182/D-186 (poison-record/DLQ), adaptada para 4 tipos de entidade sem `version`, ponteiro centralizado nas 4 funções `build*Event()` em vez de múltiplos call sites) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (6 de 9 workers migrados)** — `security-audit-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 3 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/security-audit-gsi8.ts` (novo), `src/modules/{expiration,organization,subject,activity}/domain/*.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-security-audit-purge.ts` (novo), testes em `test/unit/{shared,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:209:| D-188 | **MaintenanceDueIndex — sétima fatia (transient-purge, D-179 slice 7, 7º de 9 workers)**: `transient_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["transient_purge"]`/`worker_transact_write_policy_json["transient_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItems` na tabela base, ambos só no role do `transient_purge_handler`. **Forma distinta de todas as fatias anteriores**: este worker cobre DOIS tipos de entidade com dinâmicas DIFERENTES — `WebhookInbox` (`src/modules/notification/application/ses-callback-workflow.ts`, criado uma vez/imutável, ponteiro gravado uma única vez na criação, mesma forma que security-audit-purge/D-187) e `UploadSlot` (`src/modules/document/domain/upload-slot.ts`, transições reais de status, mesma forma que invitation-purge/D-182 — ponteiro precisa ser gravado/atualizado a cada transição relevante, não só na criação). Novo `src/shared/transient-purge-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que as duas entidades cobrem 2 módulos diferentes — mesma razão de `shared/security-audit-gsi8.ts`): `deriveWebhookInboxMaintenanceDue()` (`createdAt+7d`, sempre), `deriveUploadSlotMaintenanceDue()` (`undefined` enquanto `RESERVED` — nunca candidato —, senão `reservedAt+7d` se `CONSUMED`, `reservedAt+24h` caso contrário incl. `EXPIRED`/`RELEASED`) e `transientPurgeGsi8Keys()`. **Achado de fronteira de módulo real durante a implementação**: a primeira versão deste arquivo reusava `computeUploadSlotPurgeAfter()`/`UploadSlotStatus` importados de `modules/document/domain/**`, violando `dependency-cruiser`'s regra `shared-must-not-reach-modules` (capturado por `test/architecture/tenant-fence-boundary.test.ts`, que falhou na primeira rodada de testes) — corrigido reimplementando a fórmula (`privacy-lgpd.md` §4, um-liner estável) diretamente em `shared/transient-purge-gsi8.ts` com um tipo `TransientUploadSlotStatus` local, em vez de inverter a fronteira de dependência. Um `UploadSlot` `RESERVED` NUNCA recebe ponteiro GSI8 (nunca aparece na Query GSI8 de forma alguma, diferente do `Scan` anterior que precisava filtrá-lo defensivamente em tempo de leitura). Três escritores atualizados para gravar/atualizar o ponteiro atomicamente na MESMA escrita condicional da transição: `ses-callback-workflow.ts` (criação do `WebhookInbox`), `advance-after-evidence.ts#appendSlotConsumption` (transição `CONSUMED`), `upload-slot-reconciliation/reconciliation.ts#processOneSlot` (transição `EXPIRED`) — nenhum writer real produz `RELEASED` hoje (confirmado por grep: status declarado no union mas sem write path), então nenhum terceiro escritor precisou de mudança. `transient-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também `TenantLifecycleStatusSource` como dependência separada do worker, embora a classe permaneça exportada por simetria com os irmãos, sem uso real); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por `version` — AMBAS entidades têm `version` real, diferente de security-audit-purge — na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#TRANSIENT"` após `MAX_ATTEMPTS=5`. Branch de self-heal de ponteiro obsoleto mantido por defesa (mesma postura de invitation-purge/D-182), embora nenhum caminho de escrita real produza esse caso hoje para nenhuma das duas entidades. `GlobalIndexComponent` ganha `"transient-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.transient_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-transient-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath`, nunca a comparação de string que falha silenciosamente no Windows), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e excluindo corretamente `UploadSlot` `RESERVED`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 7 workers já migrados. **G-V3**: teste de gravação do ponteiro na criação do `WebhookInbox` (`ses-callback-workflow.test.ts`), testes de gravação/atualização do ponteiro nas transições `CONSUMED`/`EXPIRED` do `UploadSlot` (`advance-after-evidence.test.ts`/`upload-slot-reconciliation.test.ts`), 22 testes do worker (purga dos 2 tipos, `RESERVED` nunca candidato, boundary exato para ambas janelas, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant, self-heal de ponteiro obsoleto) + 20 testes do backfill (parseArgs/encodeKey-decodeKey/deriveDue cobrindo os 2 tipos e exclusão RESERVED, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida, página mista). Suíte completa 1894/1894 (176 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #212 mergeado (commit `d32957f`→`ca50bcf` em `main`), CI real (`33695255525`/`33695850896`) e CD real (`33696134854`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-transient-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyTransientPurge` com `LeadingKeys` exato `["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItemsTransientPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#TRANSIENT` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT` (os 6 já migrados) → `implicitDeny` nos 6 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (WebhookInbox, UploadSlot)` sobre as 58 linhas da tabela (`ScannedCount=58`, `Count=0`, sem `LastEvaluatedKey` — tabela inteira coberta), e por `Query GSI8 WORK#TRANSIENT` retornando `Count=0`; nada para escrever, execução "real" (não-dry-run) seria um no-op confirmado, não executada. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sétima fatia mecânica seguindo o padrão de D-187 (poison-record/DLQ, entidade create-once) para `WebhookInbox` e D-182 (ponteiro atualizado a cada transição) para `UploadSlot`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (7 de 9 workers migrados)** — `transient-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 2 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/transient-purge-gsi8.ts` (novo), `src/modules/document/domain/upload-slot.ts`, `src/modules/document/application/advance-after-evidence.ts`, `src/modules/notification/application/ses-callback-workflow.ts`, `src/workers/upload-slot-reconciliation/reconciliation.ts`, `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-transient-purge.ts` (novo), testes em `test/unit/{shared,workers,document,notification}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:210:| D-189 | **MaintenanceDueIndex — oitava fatia (delivery-record-purge, D-179 slice 8, 8º de 9 workers)**: `delivery_record_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["delivery_record_purge"]`/`worker_transact_write_policy_json["delivery_record_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItems` na tabela base, ambos só no role do `delivery_record_purge_handler`. Este worker cobre DOIS tipos de entidade (`NotificationIntent`/`NotificationAttempt`, `src/modules/{reminder,notification}/domain/*.ts`), elegibilidade de idade única (`createdAt+180d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Diferença real de forma vs. D-187 (o análogo mais próximo, mesmo padrão poison-record/DLQ)**: AMBAS entidades carregam um contador OCC `version` real (ao contrário das 4 entidades append-only de D-187) — mesma forma que D-188/`UploadSlot`/`WebhookInbox`; o `Delete`/backoff/DLQ do worker e o backfill novo condicionam em `version` exato, com `createdAt` reasserted como defesa em profundidade adicional (nada muta `createdAt` hoje, mas a condição torna estrutural, não só verdadeiro por acidente). Novo `src/shared/delivery-record-gsi8.ts` (fora de `domain/` de módulo específico, já que a entidade cobre 2 módulos — mesma razão de `security-audit-gsi8.ts`/`transient-purge-gsi8.ts`): `deriveDeliveryRecordMaintenanceDue()` (due = `createdAt+180d`, NUNCA `undefined` — sem estado terminal, cada registro de entrega expira pelo prazo fixo independentemente de qualquer ação) e `deliveryRecordGsi8Keys()`. **3 pontos de escrita reais confirmados por grep** (não presumidos) gravam o ponteiro GSI8 UMA ÚNICA VEZ, no MESMO `Put` da criação: `reminder-dispatch/dispatch.ts` (intent `EXPIRATION_REMINDER`), `notification-router-workflow.ts`'s `applyStaleDecision` (intent `REPLACEMENT`/`CORRECTIVE`) e `applyRoutedDecision` (o `NotificationAttempt`) — nenhum dos dois entidades é atualizada depois de criada nestes caminhos (o `version` existe para as próprias transições de status do módulo, não para uma atualização do ponteiro GSI8). `delivery-record-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (handler perde a dependência separada de `TenantLifecycleStatusSource`, dobrada dentro do `ConditionCheck` transacional); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `createdAt`, na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#DELIVERY_RECORD"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência de D-187, mesma causa): nenhuma das duas entidades é atualizada depois de criada NA PRÁTICA (sem caminho de undelete, sem transição de status nomeada nos comentários do próprio worker) — o ponteiro é gravado exatamente uma vez, e a única forma de uma linha deixar de ser candidata é este worker deletá-la. **Gap conhecido e aceito, herdado de D-152, NÃO tocado por esta fatia**: `NotificationAttemptLookup` (ponteiro derivado, `PK=TENANT#t#ATTEMPT#id`) continua NÃO sendo purgado por este worker e fica órfão depois que um `NotificationAttempt` é deletado — confirmado ainda válido após esta migração por releitura direta, não presumido; fora de escopo aqui como já era em D-152. `GlobalIndexComponent` ganha `"delivery-record-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.delivery_record_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-delivery-record-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e condicionado em `version` (não `createdAt`, adaptação real vs. o template de D-187). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 8 workers já migrados. **G-V3**: 3 testes de gravação do ponteiro na criação (via os fixtures reais de `dispatch.test.ts`/`notification-router-workflow.test.ts` — intent do dispatch, intent REPLACEMENT do stale-decision, attempt do roteamento feliz), 24 testes do worker (purga dos 2 tipos, due no futuro nunca candidato, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `createdAt`/`version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo os 2 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa local 1912/1912 (178 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #215 mergeado (`6d333e0`→`c595190`), CI real (`33699314795`/`33699331683`) e CD real (`33699827095`) `success`, confirmados via `gh run view`/`gh run list` direto (polling síncrono, nunca aguardando notificação de background como subagente). **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-delivery-record-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyDeliveryRecordPurge` com `LeadingKeys` exato `["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItemsDeliveryRecordPurge` escopada à tabela base); `get-function-configuration`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#DELIVERY_RECORD` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT` (os 7 já migrados) → `implicitDeny` nos 7 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (NotificationIntent, NotificationAttempt)` sobre as 58 linhas da tabela (`scannedTotal=58`, `matched=0`, tabela inteira coberta), e por `Query GSI8 WORK#DELIVERY_RECORD` retornando `Count=0`; nada para escrever, execução real (não-dry-run) seria um no-op confirmado, não executada (mesma disciplina de D-187/D-188). **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — oitava fatia mecânica seguindo o padrão de D-187/D-188 (poison-record/DLQ), adaptada para 2 tipos de entidade COM `version` real, condicionado em `version`+`createdAt`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (8 de 9 workers migrados)** — `delivery-record-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); o único worker restante nomeado por D-179 permanece em `Scan`+`Limit`: `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/delivery-record-gsi8.ts` (novo), `src/workers/reminder-dispatch/dispatch.ts`, `src/modules/notification/application/notification-router-workflow.ts`, `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-delivery-record-purge.ts` (novo), testes em `test/unit/workers/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:211:| D-190 | **MaintenanceDueIndex — nona e ÚLTIMA fatia (core-user-data-purge, D-179 slice 9, 9º de 9 workers) — D-179 FECHADO POR COMPLETO.** `core_user_data_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["core_user_data_purge"]`/`worker_transact_write_policy_json["core_user_data_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItems` na tabela base, ambos só no role do `core_user_data_purge_handler`. Este worker cobre DOIS tipos de entidade (`ExpirationItem`/`ReminderPolicy`, `deletedAt+30d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Forma distinta de D-186/D-187/D-188/D-189 (create-once/janela fixa) — mesma forma do piloto original, D-180/`membership-purge`**: elegibilidade é gated numa TRANSIÇÃO de soft-delete (`deletedAt` sendo setado), não na criação — `deriveCoreUserDataMaintenanceDue()` (`src/shared/core-user-data-gsi8.ts`, novo, fora de `domain/` de módulo específico já que a entidade cobre 2 módulos — `expiration`/`reminder`) retorna `undefined` enquanto `deletedAt` está ausente; uma linha nunca vira candidata antes do soft-delete. **Achado real de pré-existência, confirmado por grep amplo (não presumido) antes de qualquer código novo**: `ExpirationItem.deletedAt` tem exatamente UM write path real — `expiration-service.ts#deleteItem` (via `transitionStatus`'s `extraSet`) — onde o ponteiro GSI8 foi wireado. `ReminderPolicy.deletedAt` NÃO TEM write path real nenhum no código hoje (`disablePolicy` só seta `enabled: false`, nunca `deletedAt`) — **condição PRÉ-EXISTENTE, não introduzida por esta migração**: o worker `Scan`-based anterior já tinha o mesmo gap (seu filtro `attribute_exists(deletedAt)` sobre `ReminderPolicy` também nunca poderia casar, pela mesma razão). `core-user-data-gsi8.ts` ainda deriva/gera chaves para `ReminderPolicy` de forma uniforme com `ExpirationItem` — no dia em que um soft-delete real for adicionado para ele, wireara o ponteiro é uma chamada de uma linha, sem mudança adicional no módulo compartilhado. **Mudança de forma real e esperada vs. o texto original do worker (D-151)**: `candidate-source.ts`'s comentário original dizia "plain `DeleteCommand`, not `TransactWriteItems` — D-151's design named no purge receipt"; isso muda agora porque o `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` da claim GSI8 precisa estar na MESMA transação do `Delete` (mesmo raciocínio de todo outro worker migrado com fence de tenant) — mudança correta e esperada, não um desvio a evitar. `core-user-data-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `deletedAt` exato — defesa em profundidade contra uma corrida de "undelete", mesmo sem caminho de undelete real existir hoje para nenhuma das duas entidades, mesma disciplina de D-151/D-189). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188/D-189): backoff exponencial capado via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#CORE_USER_DATA"` após `MAX_ATTEMPTS=5`. `GlobalIndexComponent` ganha `"core-user-data-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.core_user_data_purge_handler.function_name`. Novo `scripts/backfill-gsi8-core-user-data-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, só linhas já soft-deletadas (nunca escreve ponteiro para uma linha sem `deletedAt`), vencimento FUTURO e passado. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 9 workers — **matriz de isolamento completa, todos os 9 namespaces GSI8 agora provados mutuamente exclusivos**. Suíte completa local 1929/1929 (179 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). PR #217 mergeado (`1633d6c`→`1e483c1`), CI real (`33702974051`) e CD real (`33703151890`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-core-user-data-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyCoreUserDataPurge` com `LeadingKeys` exato `["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItemsCoreUserDataPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 5 * * ? *)`). **Prova de isolamento cross-namespace — a mais ampla do programa inteiro, 8 verificações negativas** (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8): `WORK#CORE_USER_DATA` (próprio) → `allowed`; os 8 namespaces já migrados (`WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT`/`WORK#DELIVERY_RECORD`) → `implicitDeny` nos 8 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard roda de fato): 0 candidatos — confirmado por `Scan` bruto cobrindo as 58 linhas inteiras da tabela filtrado por `entityType IN (ExpirationItem, ReminderPolicy)` retornando `Count=0` (a tabela `dev` não tem hoje NENHUMA linha desses dois tipos, muito menos soft-deletada), e por `Query GSI8 WORK#CORE_USER_DATA` retornando `Count=0`; execução real seria um no-op confirmado. **D-179 (MaintenanceDueIndex) está FECHADO — os 9 workers nomeados pelo design estão 100% migrados, verificados ao vivo em `dev`, com prova de isolamento IAM cross-namespace completa**: `membership-purge` (D-180/D-181), `invitation-purge` (D-182), `document-file-reconciliation` (D-183), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186), `security-audit-purge` (D-187), `transient-purge` (D-188), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190). **Item pendente carregado, ainda não resolvido, agora o item real mais proeminente desta área**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia; como D-179 fecha aqui, este é o item de maior prioridade real que sobra nesta área de trabalho. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — nona e última fatia mecânica, mesma forma do piloto original D-180 — transição de soft-delete, não criação) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO — D-179 COMPLETO (9 de 9 workers migrados)** — `core-user-data-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo completa contra os 8 outros namespaces); nenhum worker nomeado por D-179 permanece em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/core-user-data-gsi8.ts` (novo), `src/modules/expiration/application/expiration-service.ts`, `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-core-user-data-purge.ts` (novo), testes em `test/unit/{expiration,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:220:| D-193 | Item 5/9 do design (`GSI_EVIDENCE` — novo índice reverso esparso — + `linkEvidence`/`unlinkEvidence` passam a escrevê-lo). Novo GSI físico GSI9 (GSI1-8 já ocupados por decisões anteriores, incluindo D-179/D-190's programa GSI8 de 9 workers) — `requirementGsi9Keys()`/`requirementGsi9PartitionKey()` em `document-archive/domain/requirement.ts` constroem `GSI9PK=TENANT#<t>#DOCVERSION#<versionId>` / `GSI9SK=REQUIREMENT#<requirementId>` (SK inclui `requirementId` de propósito — dois `Requirement`s DIFERENTES, de Subjects diferentes, podem legitimamente referenciar a mesma evidência). `linkEvidence()` (`document-archive-service.ts`) passa a incluir essas duas chaves no mesmo `set` já existente (SET simples — sem ação nova na transação, cardinalidade inalterada); `unlinkEvidence()` passa a incluir `GSI9PK`/`GSI9SK` na lista `remove` (REMOVE de verdade, não `undefined`/null — esparso por construção, provado por `hasOwnProperty` no teste, não só "query vazia"). Nova query `DocumentArchiveService.findRequirementsByEvidenceVersion(tenantId, versionId): Promise<Requirement[]>` pagina o GSI9 inteiro (loop `exclusiveStartKey`, nunca só a 1ª página — prova real via 2 Requirements de Subjects diferentes referenciando a mesma evidência) via `queryIndexPage({indexName:"GSI9",...})` (porta `IndexPageInput`/`DynamoDbDocumentArchiveStore` ganham `"GSI9"`no union/mapa de atributo, mesmo padrão genérico de GSI1/2/5 já existente — nenhuma duplicação de lógica de Query). Deliberadamente SEM `RequestContext`/`authorize()` — leitura de sistema para o worker assíncrono da próxima fatia, mesma postura de `scanActiveSeries` (recurrence materializer). Terraform: `GSI9` (projeção `ALL`, não `KEYS_ONLY` como GSI8 — o worker da próxima fatia precisa do `Requirement` inteiro numa Query só) adicionado a `infra/modules/dynamo-table/main.tf`, tenant-scoped (PK carrega `TENANT#<t>#...`) logo entra em `tenant_facing_index_names` como GSI1/2/5/7 — sem isolamento de IAM dedicado como GSI3/6/8 (sem risco de fan-out cross-tenant, a PK já fixa o tenant). **Achado real durante a fatia, corrigido no caminho**: o teste `terraform test` `dynamo_table.tftest.hcl`'s `length(aws_dynamodb_table.this.global_secondary_index) == 7` estava desatualizado desde a criação do GSI8 (D-179) — ninguém atualizou a contagem para 8, o teste continuou passando silenciosamente porque a asserção nunca tinha sido re-executada como gate (`terraform test` não constava nos gates das fatias 1-4 por não terem mexido em infra); corrigido para 9 nesta fatia, `run` renomeado, mais um `run` novo asserindo a forma exata de GSI9 (`ALL`/`GSI9PK`/`GSI9SK`). G-V3: 3 testes de key builder (`requirement.test.ts` — par PK/SK correto, `requirementGsi9PartitionKey` bate exatamente o `GSI9PK` de `requirementGsi9Keys`, dois `requirementId` diferentes com o mesmo `evidenceVersionId` compartilham PK mas têm SK distinto); 5 testes de integração em `requirement-service.test.ts` (link popula GSI9 e é queryable; unlink remove de fato via `hasOwnProperty`, não só null; Requirement recém-criado sem evidência nunca tem GSI9PK/SK — sparse provado, não presumido; query encontra TODOS os Requirements de dois Subjects diferentes referenciando a mesma evidência, não só o primeiro; relink move o ponteiro — a evidência antiga para de encontrar o Requirement, a nova encontra). | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 5/9 — único ponto real de engenharia fora do design: qual GSI físico usar, decidido como GSI9 por eliminação, já que GSI1-8 estão todos ocupados) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-docs/validate-schemas/build:lambdas limpos, suíte **2105/2105** — 12 testes novos —, `terraform test` **7/7** `AWS_PROFILE=claude-dev`, mocked provider). Commit `9c3562f`. D-193 item 5/9 do design FECHADO (índice + primitivas de leitura/escrita, testado isoladamente — nenhum consumidor/worker chamando `findRequirementsByEvidenceVersion` ainda, isso é a próxima fatia). Próxima fatia: item 6/9 — fila `requirement-evidence-refresh-queue`+DLQ, rota no `DispatchOutboxRelay` para `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` (já existe no union desde a slice 4, sem consumidor), handler `requirement-evidence-refresh-handler.ts` que NUNCA aplica o payload do evento — sempre relê `DocumentVersion`+`Requirement` frescos (via `findRequirementsByEvidenceVersion` desta fatia) e re-deriva um Requirement por vez com seu próprio loop OCC, evento é só "wake up" (princípio de correção já estabelecido nas fatias 2-3: nunca confiar no payload/chave do evento, sempre reler o agregado fresco). | `src/modules/document-archive/{domain/requirement.ts,application/document-archive-service.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `infra/modules/dynamo-table/{main.tf,tests/dynamo_table.tftest.hcl}`, testes em `test/unit/document-archive/{requirement,requirement-service}.test.ts` | 2026-09-03 |
docs/architecture/decisions-log.md:221:| D-193 | Item 6/9 do design — convergência assíncrona real de `Requirement`: consumidor de `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` ponta a ponta. Novo `src/workers/requirement-evidence-refresh/refresh.ts` (`refreshRequirementsForEvidenceVersion`) — usa `findRequirementsByEvidenceVersion` (slice 5) só para DESCOBERTA de candidatos; para CADA `Requirement` candidato, relê fresco (`store.get`), depois relê fresca a `DocumentVersion` via o `evidenceDocumentId`/`evidenceSeq` ATUAIS do próprio `Requirement` (nunca o `versionId` do hint do evento — se um relink já moveu o ponteiro, segue o vínculo corrente), recomputa `status`/`evidenceState`/`evidenceValidUntil`/GSI1/GSI8 via as mesmas funções puras de `linkEvidence`, e só then faz `TransactWriteItems` OCC (nunca `UpdateItem` solto). Sem mudança real = no-op sem `transactWrite` (sem bump de version espúrio). Loop de retry OCC próprio por Requirement (até 5 tentativas, relendo a cada volta); falha terminal de UM Requirement é registrada (`failedRequirementIds`) sem abortar os outros do mesmo lote — mesma disciplina de isolamento por-item que `dispatch-outbox-relay-processor.ts` já tem por-record, uma camada mais funda. **Achado real que fechou um gap deixado pela slice 4**: nenhum dos dois payloads de evento (`confirmFieldForDocumentArchive`/`commitRunOutcome`, ambos em `dynamodb-extracted-field-store.ts`) carregava `versionId` — só `documentId`+`validUntil` (o próprio valor que o design manda nunca confiar) — sem `versionId` a descoberta via GSI9 seria impossível; `versionId` adicionado como campo novo em `CommitDocumentVersionUpdate`/`ConfirmFieldForDocumentArchiveInput` e threadado dos dois call sites (`run-extraction-validation.ts`/`confirm-reject-field-document-archive.ts`, que já liam a `DocumentVersion` inteira, `version.versionId` já disponível) — usado SÓ como chave de busca no GSI9, nunca como fonte da state recomputada (a re-leitura fresca é o que decide tudo). Novo `requirement-evidence-refresh-handler.ts` (SQS, mesmo formato thin-entrypoint de `import-parse-handler.ts`) extrai só `{tenantId, versionId}` da mensagem (tipo `RequirementEvidenceRefreshHint`, sem mais nenhum campo alcançável) — batch item reportado como falho no handler quando `result.failed > 0`, deixando a política de redrive/DLQ real da fila decidir o retry. `buildOutboxRelayDeps` (composição) e `outbox-sweeper-handler.ts` ganham o 7º/6º sender opcional (`SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`), mesmo padrão incremental de todas as fatias anteriores (D-192 slice 9 etc.). Terraform: nova fila+DLQ `requirement-evidence-refresh-queue` (módulo `sqs-worker-queue`, já inclui DLQ+alarm), novo Lambda `requirement-evidence-refresh-handler` (só `tenant_facing_read_write_policy_json`+`consume_policy_json` da fila nova — GSI9 é tenant-facing, sem policy dedicada), `aws_lambda_event_source_mapping` novo; `dispatch_outbox_relay`/`outbox_sweeper` ganham a URL da fila nova em `environment_variables`+`policy_documents_json` (`send_policy_json`). G-V3 adversarial: hint com `versionId` correto mas `Requirement` com cache ANTIGO de `evidenceValidUntil` (mais restrito que a verdade fresca) converge para o valor fresco, nunca o cache; `DocumentVersion` re-lida como `REJECTED` (não só `validUntil` mudando) converge para `PENDING` via releitura, nunca o valor do evento; 3 Requirements da mesma evidência, um falha OCC uma vez e se recupera sozinho no retry (não conta como falha), outro falha terminalmente (esgota as 5 tentativas) sem bloquear os demais; no-op não dispara nenhum `transactWrite`; Requirement desvinculado entre a descoberta (stale) e a releitura fresca é no-op seguro, nunca reescreve. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 6/9 — único ponto real de engenharia fora do design: os dois payloads de evento da slice 4 não carregavam `versionId`, extensão aditiva necessária para a descoberta via GSI9 funcionar, decidida e documentada aqui) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-boundaries/check-docs/validate-schemas/build:lambdas limpos, suíte **2112/2112** — 7 testes novos —, `terraform test` **22/22** `AWS_PROFILE=claude-dev`). D-193 item 6/9 do design FECHADO — mecanismo de convergência assíncrona `Requirement` ponta a ponta (outbox condicional da slice 4 → GSI9 da slice 5 → fila+worker desta fatia). Próxima fatia: item 7/9 — rede de reparo autoritativa: `scanRequirementsWithEvidence` em `DocumentArchiveStore` (mesmo precedente de `scanActiveSeries`, filtrado por `attribute_exists(evidenceVersionId)`, independente de status cacheado) + worker diário agendado `requirement-evidence-daily-sweep` (EventBridge Scheduler, mesmo padrão de `document_file_reconciliation_handler`) que só REENFILEIRA candidatos na MESMA fila desta fatia, nunca escreve `Requirement` diretamente — fecha o teto de staleness em 24h mesmo sob perda total do evento/outbox (mecanismo distinto: esta fatia cobre ordem/duplicação via releitura, a próxima cobre PERDA total da mensagem). | `src/workers/requirement-evidence-refresh/refresh.ts`, `src/runtime/aws/handlers/requirement-evidence-refresh-handler.ts`, `src/runtime/aws/{composition/reminder.ts,handlers/{dispatch-outbox-relay-handler.ts,outbox-sweeper-handler.ts}}`, `src/modules/extraction/{ports/extracted-field-store.ts,persistence/dynamodb-extracted-field-store.ts,application/{confirm-reject-field-document-archive.ts,run-extraction-validation.ts}}`, `infra/main.tf`, `scripts/build-lambdas.ts`, testes em `test/unit/{workers/requirement-evidence-refresh,extraction/run-extraction-validation}.test.ts` | 2026-09-03 |
docs/architecture/decisions-log.md:235:| D-204 | **Roadmap P1 item 15 ("Relatórios agendados") — DESIGN, protocolo Claude↔Codex completo, 3 rodadas** (`docs/architecture/reviews/scheduled-reports-scoping/`). Fecha o próximo item do backlog P1 depois do bloqueio do item 14 (D-202). **Declaração E-014: SIM PARCIAL** (Metabase "Dashboard subscriptions" e Google Cloud/Looker "Scheduling and sending dashboards"/"Emailed data policy", ambas fontes primárias datadas 2026-09-05 — padrão convergente: assinatura por criador/relatório/destinatário específico, não agendamento único fixo por tenant; "anexo vs. link" é escolha deliberada de produto oferecida pelos dois líderes de mercado; nenhuma fonte primária equivalente encontrada no nicho específico de compliance/expiration-tracking, limitação registrada). **6 achados reais do Codex, todos verificados por leitura de código, fecharam as 3 rodadas**: (1) TTL de 30 dias em presigned S3 é fisicamente inválido quando assinado por credenciais de role Lambda (expira com as credenciais, teto ~7 dias do SigV4) — fechado substituindo o link por uma rota de download AUTENTICADA que gera presign curto (5 min) sob demanda, nunca embutindo um link S3 direto no e-mail; (2) GSI10 novo era desnecessário — GSI8 (`local.gsi8_worker_types`, mapa fechado de 9 entradas, `infra/modules/dynamo-table/main.tf:248`) já é o índice esparso certo, `report_subscription="REPORT_SUBSCRIPTION"` é aditivo mecânico; (3) `lastRunAt+cadence` sozinho é fraco para idempotência/concorrência — fechado reusando outbox→SQS→worker já `APPROVED` (D-193/D-200/D-201) com `ReportDeliveryAttempt` espelhando a máquina de estados de `NotificationAttempt` (claim `SUBMITTING` com lease ANTES de qualquer chamada externa); (4) `ReportsService` exige `RequestContext`/`authorize()`, incompatível com um worker agendado sem ator autenticado — fechado com variante system-facing, mesmo posto de `findRequirementsByEvidenceVersion` (`document-archive-service.ts:1300-1306`); (5) destinatários externos arbitrários são bloqueio real de produto/segurança — fechado restringindo a `Membership ACTIVE`+`GlobalUser ACTIVE`, revalidados FRESCOS no envio (nunca confiados da criação); (6) conflito de RBAC entre a rota de download (ADMIN-tier) e destinatários MEMBER/VIEWER elegíveis — fechado autorizando por `ADMIN_ROLES` OU o próprio `principal.userId` elegível no `ReportDeliveryAttempt`/run específico. **10 decisões finais** (detalhe completo em `estado-final-consolidado.md`): `ReportSubscription` (cadence `WEEKLY`+`dayOfWeek`/`localTime`/`timeZone`, `recipientUserIds` teto nomeado, RBAC `ADMIN_ROLES`); GSI8 worker type novo; scheduler semanal único cross-tenant; claim transacional 2 ações (`Update`+`Outbox` Put, destino novo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`); `ReportSubscriptionRun`(1/execução)+`ReportDeliveryAttempt`(1/destinatário, isolamento de falha); worker de entrega system-facing; rota de download autenticada com presign efêmero; retry/DLQ nativo do SQS; truncamento sinalizado no e-mail; RBAC de gestão `ADMIN_ROLES`. **Fora de escopo, nomeado**: cadência MONTHLY; anexo MIME (`Content.Raw`, deliberadamente descartado a favor do link autenticado); destinatários externos arbitrários. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (novo mecanismo de entrega assíncrona + nova rota autenticada + novo GSI8 worker type) | 9,2/10 (Rodada 3, autoavaliação às cegas) | 9,1/10 (Rodada 3, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197.** Fatias sugeridas (não decididas): (1) entidades+GSI8 worker type+schema; (2) scheduler+claim transacional; (3) worker de entrega+rota de download; (4) wiring completo do destino de outbox. | `docs/architecture/reviews/scheduled-reports-scoping/` | 2026-09-05 |
docs/architecture/decisions-log.md:242:| D-211 | **D-204 fatia 1 (entidades de domínio + registro GSI8 + destino de outbox) — IMPLEMENTADA.** Roadmap P1 item 15 (relatórios agendados) começa a sair do design-only. 3 entidades novas em `src/modules/reports/domain/`: `ReportSubscription` (`reportTypes` subconjunto não vazio dos 7 já expostos por `ReportsService`/D-195, `cadence` tipado como union — só `WEEKLY` no v1, mesma disciplina de escopo estreito de `FIELD_SCHEMA_V1` — `dayOfWeek`/`localTime`/`timeZone` no mesmo formato de `ReminderTrigger`/`ReminderPolicy`, `recipientUserIds` teto nomeado `MAX_REPORT_SUBSCRIPTION_RECIPIENTS=10`, `nextRunAt` avançado atomicamente pelo worker, ponteiro `GSI8PK`/`GSI8SK` no namespace `REPORT_SUBSCRIPTION`); `ReportSubscriptionRun` (1 por execução agendada, congela `reportTypes`/`recipientUserIds` no momento do claim — decisão 5: conjunto de linhas congelado, valores de campo relidos frescos na geração); `ReportDeliveryAttempt` (1 por destinatário, máquina de estados espelhando `NotificationAttempt` — `PREPARED`→`SUBMITTING`(lease)→`ACCEPTED`/`FAILED_RETRYABLE`/`FAILED_TERMINAL`/`UNKNOWN`, mais `skippedReason: "RECIPIENT_NOT_ELIGIBLE"` para quando a revalidação fresca de Membership/GlobalUser falha antes de qualquer chamada à SES). Terraform: `report_subscription = "REPORT_SUBSCRIPTION"` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`) — mecânico, reusa GSI8 (não GSI10), mesmo padrão das 9 entradas existentes de D-179; nenhum Lambda/IAM novo ainda (isso é fatia 2, quando o worker de agendamento existir). Novo valor aditivo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1` no enum fechado `OutboxDestination` (`src/shared/outbox/outbox.ts`) — verificado que `DestinationSenders` é `Partial<Record<OutboxDestination,...>>`, não exaustivo, então nenhuma composição existente precisa de um sender novo ainda (mesmo padrão "valor existe antes do consumidor" que `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`/D-193 item 6/9 já estabeleceu). G-V3: 11 testes novos (`report-subscription.test.ts` — key builders das 3 entidades, `validateReportSubscriptionInput` cobrindo reportTypes vazio/desconhecido, recipientUserIds vazio/acima do teto/exatamente no teto, todos os 7 tipos reais juntos). Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `terraform test` do módulo `dynamo-table` **7/7** (`AWS_PROFILE=claude-dev`); suíte completa a confirmar. **Pendente real, nomeado**: fatias 2-4 de D-204 (scheduler+claim transacional; worker de entrega+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) ainda não implementadas — nenhum Lambda novo, nenhuma rota HTTP, nenhum e-mail real ainda. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 1 de D-204)** — pendente fatias 2-4, PR/merge/CD | `src/modules/reports/domain/{report-subscription.ts,report-subscription-run.ts,report-delivery-attempt.ts}`, `src/shared/outbox/outbox.ts`, `infra/modules/dynamo-table/main.tf`, `test/unit/reports/report-subscription.test.ts` | 2026-09-05 |
docs/architecture/decisions-log.md:243:| D-212 | **D-204 fatia 2 (scheduler + claim transacional) — IMPLEMENTADA.** Roadmap P1 item 15 continua saindo do design-only. `runScheduledReportsTick` (`src/workers/scheduled-reports/scheduler.ts`) escaneia GSI8 (`WORK#REPORT_SUBSCRIPTION`) exatamente como `requirement-reindex/reindex.ts` (mesmo cap `MAX_PAGES=25`, mesma paginação), rele fresco cada `ReportSubscription` candidata (GSI8 nunca fonte de elegibilidade, defensivo contra query eventualmente consistente), e comita uma transação de 2 ações por assinatura due — `Update` avançando `nextRunAt`/ponteiro GSI8 (`ConditionCheck` de versão) + `Outbox` `Put` (destino `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`, já aditivo desde D-211) — na MESMA `TransactWriteItems`, mesma disciplina "claim + evento durável atômico" de `reminder-producer/producer.ts` (um claim seguido de SendMessage direto não é atômico; Lambda pode morrer no meio, deixando uma assinatura reivindicada sem entrega enfileirada). Payload do evento deliberadamente mínimo (`runId`/`subscriptionId`/`tenantId`/`scheduledFor`) — decisão 6 de D-204 exige que o worker de entrega (fatia 3, ainda não construído) reresolva `recipientUserIds` FRESCO no envio, então não há valor em embutir um snapshot de `reportTypes`/destinatários aqui; `runId` é gerado AGORA (não pela fatia 3) para que o futuro `ReportSubscriptionRun`/`ReportDeliveryAttempt` compartilhem um id estável entre reentregas SQS at-least-once, mesma postura do `deduplicationKey` de `reminder-producer`. Nova primitiva `nextWeeklyOccurrenceUtc` (`report-subscription-schedule.ts`) computa a próxima ocorrência de `dayOfWeek`(convenção ISO 1=segunda..7=domingo, documentada explicitamente em `ReportSubscription.dayOfWeek` desde D-211 para evitar off-by-one contra `Date.getUTCDay()`)/`localTime`/`timeZone`, reusando `zonedTimeToUtc`/`parseLocalTime`/`addCalendarDays` de `reminder/domain/recurrence.ts` (reuso cross-módulo já estabelecido por `notification/application/quiet-hours.ts` — nenhuma duplicação de lógica DST-aware) mais uma nova primitiva exportada `instantToLocalParts` (direção inversa de `zonedTimeToUtc`, necessária para achar "hoje" no calendário local do assinante antes de buscar a próxima ocorrência). Novo port mínimo `ReportSubscriptionStore` (`get`+`transactWrite` só — criação/gestão de assinatura via rota HTTP fica para uma fatia futura, ainda não decidida) + adapter real `DynamoDbReportSubscriptionStore` + `DynamoDbScheduledReportsCandidateSource` (GSI8 Query, mesmo formato de `requirement-reindex/dynamodb-candidate-source.ts`). Lambda real `scheduled-reports-scheduler-handler` (`timeout_seconds=300`, `gsi8_read_policy_json["report_subscription"]`/`worker_transact_write_policy_json["report_subscription"]` — chaves já provisionadas automaticamente por D-211's `local.gsi8_worker_types` addition, mecânico) + role/policy de `aws_scheduler_schedule` + log group + registrado em `security_audit_observability.global_index_function_names` (exigiu adicionar `"scheduled-reports"` à união fechada `GlobalIndexComponent`, `security-audit.ts`). **Decisão de implementação documentada, não uma nova decisão de arquitetura**: cron do `aws_scheduler_schedule` é DIÁRIO (04:15 UTC), não literalmente semanal apesar do texto da decisão 3 de D-204 dizer "aws_scheduler_schedule único semanal" — lido aqui como descrevendo a cadência da FEATURE (`ReportSubscriptionCadence="WEEKLY"`, decisão 1: cada assinatura dispara ~1x/semana), não a frequência de polling do schedule em si; "único" (1 Lambda/1 schedule/1 scan cross-tenant por execução, "mesmo padrão de requirement-reindex") é a parte vinculante da frase. Um poll diário é o que de fato honra `dayOfWeek`/`localTime`/`timeZone` por assinatura com precisão real (~24h de deriva máxima, igual a todo outro worker GSI8-esparso do projeto) — um poll literalmente semanal dessincronizaria o horário escolhido por cada assinante para o instante único em que o schedule disparasse. Aberto a correção se a leitura literal (cron semanal) era de fato a intenção. `terraform plan` real contra `dev` (`AWS_PROFILE=claude-dev`) confirmou os 12 recursos novos como `+ create` limpo (nenhum recurso pré-existente afetado; os 4 `-/+ destroy and then create replacement` de `aws_lambda_permission` no mesmo plano são débito técnico pré-existente já nomeado em sessão anterior, não relacionados a esta mudança) — `terraform fmt`/`validate` limpos. G-V3: 7 testes novos de `nextWeeklyOccurrenceUtc` (rollover mesma semana/semana seguinte, semântica estritamente-depois-do-instante, convenção ISO dayOfWeek=7≠`Date.getUTCDay()`, timezone-aware cross-fuso, busca a partir da data calendário LOCAL perto de fronteira de dia) + 7 testes de `runScheduledReportsTick` (claim avança nextRunAt/GSI8/escreve evento outbox, ignora não-due, multi-tenant, concorrência perdida no claim, candidato obsoleto nunca reclamado 2x) + 2 de `shouldAlarmScheduledReports`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) e a própria rota HTTP de criação/gestão de `ReportSubscription` (`ADMIN_ROLES`, decisão 1) — nenhuma delas implementada ainda; sem elas, nenhuma assinatura real pode ser criada nem entregue, feature continua não-usável ponta a ponta. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de cadência de cron documentado explicitamente acima) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 2 de D-204)** — pendente fatias 3-4 + rota HTTP de criação, PR/merge/CD | `src/workers/scheduled-reports/{scheduler.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/reports/domain/report-subscription-schedule.ts`, `src/modules/reports/ports/report-subscription-store.ts`, `src/modules/reports/persistence/dynamodb-report-subscription-store.ts`, `src/modules/reminder/domain/recurrence.ts`, `src/runtime/aws/handlers/scheduled-reports-scheduler-handler.ts`, `src/runtime/aws/composition/reports.ts`, `src/shared/observability/security-audit.ts`, `scripts/build-lambdas.ts`, `infra/main.tf` | 2026-09-05 |
docs/architecture/decisions-log.md:244:| D-213 | **ReportSubscription CRUD (create/get/list/delete) — IMPLEMENTADA.** Fecha a lacuna nomeada por D-212: sem uma rota HTTP de criação, nenhuma assinatura real podia existir para o scheduler/worker de entrega processarem. D-204 decisão 1 previa "criação/gestão via rota HTTP ADMIN_ROLES", mas a quebra em fatias sugerida (§"Próxima ação" do estado-final-consolidado) não incluía essa rota como fatia própria — decisão de sequenciamento tomada aqui, não uma decisão de arquitetura nova. **Superfície deliberadamente estreita para v1**: create/get/list/delete apenas, SEM update — um assinante que queira mudar reportTypes/horário/destinatários apaga e recria, mesma disciplina "não inventar superfície que o design aprovado não pediu" de `bulk-actions-scoping`'s v1 (só reassign+archive). `ReportSubscriptionService` nova (`authorize()`+`executeTenantBusinessMutation` no create/delete, mesmo pipeline de `DocumentArchiveService.createDocumentType`/`getDocumentType`) — nova action RBAC única `reports:subscription-manage` (`ADMIN_ROLES`, mesmo tier de `docarchive:requirement-export`/`item:export` — uma assinatura nomeia OUTROS membros como destinatários e produz os mesmos CSVs bulk cross-membro que essas actions já protegem, não o tier mais permissivo de "ler o próprio recurso"). **Achado real durante a fatia**: `ReportSubscription` (D-211) não tinha nenhum índice de listagem — cada assinatura é sua própria partição (`PK=TENANT#<t>#REPORTSUB#<subId>`), sem GSI nenhum "lista todas do tenant" seria impossível; corrigido adicionando `GSI1PK=TENANT#<t>#REPORTSUB`/`GSI1SK=<createdAt>#REPORTSUB#<id>` ao GSI1 compartilhado (mesmo índice físico que Document/ExpirationItem/Requirement/DocumentType/RequirementTemplate já discriminam por prefixo — nenhum índice novo), reusando exatamente o padrão `documentTypeGsi1Keys` já estabeleceu para o catálogo de DocumentType. `createSubscription` computa `nextRunAt` inicial via `nextWeeklyOccurrenceUtc(now, ...)` (D-212) — a mesma primitiva que o scheduler usa para avançar a cada claim, então uma assinatura recém-criada já é descobrível pelo GSI8 sem caso especial de "primeira execução". `delete` é físico (não soft-archive) — `buildVersionedDelete` + fence do tenant, releitura fresca ANTES do delete para distinguir `NotFoundError` (nunca existiu) de `ConflictError` (`expectedVersion` obsoleto), nunca assumindo causa que o DynamoDB não revelou (mesma postura RT-LANE-FALLBACK-01). Rotas `POST /reports/subscriptions`, `GET /reports/subscriptions`, `GET /reports/subscriptions/{subscriptionId}`, `POST /reports/subscriptions/{subscriptionId}/delete` — MESMO Lambda/integration `reports-handler` que já serve os 7 relatórios CSV (D-195), discriminado por `routeKey` no entrypoint (nunca confundido com a resposta CSV — `isCsvResponse()` já existia para esse propósito); 4 entradas novas em `proxy-allowlist.ts` (disciplina D-117/D-120/D-178/D-209 — diferente das 7 rotas CSV, que são deliberadamente NÃO proxyadas pela BFF por causa do `Content-Disposition`, estas são JSON puro e não têm esse problema). 2 schemas JSON novos (`report-subscription-create-request.v1.json`/`-delete-request.v1.json`) registrados em `schema-validator.ts`. G-V3: 12 testes de `ReportSubscriptionService` (create com nextRunAt correto/RBAC/validação de domínio+dayOfWeek+localTime+timeZone, get/list via GSI1 ordenado, delete com NotFoundError/ConflictError), 10 de HTTP handlers (400 em corpo ausente/schema inválido/param ausente, 404/409 reais), 2 novos de `proxy-allowlist`, 10 de contrato para os 2 schemas — **80 testes novos** nesta fatia. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `terraform fmt`/`validate` limpos; `terraform plan` real contra `dev` confirmou as 4 rotas novas como `+ create` limpo (os mesmos 4 `-/+ replace` de `aws_lambda_permission` pré-existentes de D-212 reaparecem, não relacionados); suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS consumindo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`+rota de download; wiring completo do outbox) seguem não implementadas — uma assinatura agora pode ser criada e será reivindicada pelo scheduler (D-212) no próximo tick devido, mas nenhum e-mail real é enviado até a fatia 3 existir. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de sequenciamento — rota HTTP como fatia própria, não prevista nas 4 fatias sugeridas — e 1 de modelagem de índice — GSI1 novo para ReportSubscription, mecânico/reuso de padrão já estabelecido) | — | — | **IMPLEMENTADO (create/get/list/delete)** — pendente fatias 3-4 de D-204, PR/merge/CD | `src/modules/reports/{domain/report-subscription.ts,application/report-subscription-service.ts,application/id-generator.ts,http/reports-handler.ts,ports/report-subscription-store.ts,persistence/dynamodb-report-subscription-store.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{ids.ts,composition/reports.ts,handlers/reports-handler.ts}`, `src/shared/contracts/schema-validator.ts`, `schemas/api/report-subscription-{create,delete}-request.v1.json`, `infra/modules/api-gateway/main.tf` | 2026-09-05 |
docs/architecture/decisions-log.md:262:| D-231 | **WhatsApp Operacional fatia 3/5 (D-197, D-7 webhook handler + D-10 Secrets Manager/kill switch) — IMPLEMENTADA.** Base era implementação direta de design já `APPROVED` (nível 3-4), mas dois gaps reais de modelagem foram achados durante a implementação e resolvidos via protocolo Claude<->Codex completo (`AGENTS.md` §4, nota cega, `codex exec`) ANTES de codificar, por instrução explícita do dono do projeto para esta frente: **(1) escopo de purge do `WebhookInbox` WhatsApp** (4 rodadas, notas finais 9,2 Claude / 9,2 Codex - `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/estado-final-consolidado.md`): `WebhookInbox` WhatsApp e account-scoped (`PK=WEBHOOK#WHATSAPP#<wabaId>`), nunca tenant-scoped, mas `transient-purge/purge.ts` fenceia todo delete com `TenantLifecycleRecord.status=ACTIVE` - sem um tenant real (o caso normal para qualquer wamid nao correlacionado), a linha nunca purgaria, quebrando a retencao de 7 dias (`privacy-lgpd.md` §4) silenciosamente. Resolvido com `purgeScope: "TENANT"|"ACCOUNT"` imutavel escrito na criacao (nunca inferido do GSI8, que e discovery-only/eventualmente consistente), `accountScopedTransientPurgeGsi8Keys()` novo (mesmo `GSI8PK`, discriminador `#ACCOUNT#<accountId>#` em vez de `#TENANT#<tenantId>#`), uniao discriminada em `candidate-source.ts`/`dynamodb-candidate-source.ts`, e dois ramos de transacao em `purge.ts` (`TENANT`: `ConditionCheck` de tenant ativo + `Delete`, backoff/DLQ por tenant; `ACCOUNT`: so `Delete` condicionado por `version`, `CancellationReasons[0]` != `ConditionalCheckFailed` sempre relancado, nunca backoff/quarentena por tenant). **(2) `buildVersionedUpdate()` nao serve a uma entidade sem tenant** (3 rodadas, notas finais 9,3 Claude / 9,2 Codex): o unico builder de update condicional versionado do repositorio (`shared/dynamodb/occ.ts`, usado por ~40 call sites) embute incondicionalmente `#tenantId = :tenantId` no `ConditionExpression` - `WebhookInbox` WhatsApp nao tem (nem pode fabricar) esse atributo sem reabrir a ambiguidade ja fechada no gap (1). Resolvido com um nucleo privado `buildScopedVersionedUpdate()` compartilhado (unica implementacao de incremento de version/updatedAt/SET/REMOVE/extraConditions/deteccao de colisao) e dois builders publicos: `buildVersionedUpdate()` inalterado byte-a-byte (mesma assinatura/expressao para todo call site existente) e `buildAccountScopedVersionedUpdate()` novo, fence `#accountId = :accountId`. **Decisao final da fatia** (D-7/D-10): `whatsapp-webhook-processor.ts` (puro) implementa `verifyMetaSignature()` (HMAC-SHA256 sobre o raw body, `X-Hub-Signature-256`, `timingSafeEqual`, mesma disciplina de `bff/domain/csrf.ts`), `verifyMetaWebhookChallenge()` (handshake `GET` de registro do webhook, `hub.mode`/`hub.verify_token`/`hub.challenge`, conforme documentacao real da Meta Cloud API verificada 2026-09-07), `parseBizOpaqueCallbackData()`/`parseCloudApiWebhookEvents()` (parsing do payload real de status), e `decideWhatsAppCallbackApplication()` (precedencia monotonica SENT->ACCEPTED/DELIVERED/READ->DELIVERED conflacionado/FAILED->FAILED_TERMINAL, mesma disciplina de `ses-callback-processor.ts`). `whatsapp-webhook-workflow.ts` espelha `ses-callback-workflow.ts` (grava a linha create-once IMEDIATAMENTE apos assinatura valida, ANTES de correlacionar; `markInboxUnmatched`-equivalente na MESMA linha se a correlacao falhar) com `purgeScope: "ACCOUNT"` sempre, `accountId`/`providerAccountId` = `wabaId`, correlacao bem-sucedida so anota `correlatedTenantId`/`attemptId`/`intentId` como metadado (nunca muda o dono do purge). `whatsapp-webhook-handler.ts` (novo, API Gateway HTTP API, rota PUBLICA `authorization_type=NONE` - segunda rota publica do projeto depois de `guest_documents`, D-037): `GET` responde o handshake; `POST` verifica assinatura sobre o raw body ANTES de qualquer `JSON.parse`/persistencia (G-V3), sempre retorna 200 a Meta apos a verificacao (nunca deixa uma falha de processamento parecer uma falha de endpoint que dispararia retry de Meta), checa o kill switch so para decidir se persiste/age. `secrets-manager-whatsapp-config.ts` novo (`loadWhatsAppSecrets`) - um secret JSON so (accessToken/appSecret/phoneNumberId/wabaId/verifyToken, mesma razao de nao fragmentar em 5: emitidos juntos pela Meta, nada rotaciona independente), nunca logado, cacheado por cold start. `whatsapp-activation.ts` novo - kill switch de 2 flags em ordem obrigatoria (`WHATSAPP` + `WHATSAPP_DELIVERY_WORKER_ENABLED`, mesmo mecanismo de D-193 slice 8/9/`document-archive-activation.ts`), `isWhatsAppDeliveryWorkerEnabled()` exige AMBOS. `whatsapp-delivery-handler.ts` (fatia 2/5) migrado de env vars placeholder para Secrets Manager + kill switch (batch inteiro dropado sem side-effect, nunca marcado como falha de item, quando o kill switch esta off). Terraform: `aws_secretsmanager_secret`/`_version` (placeholder JSON, `lifecycle.ignore_changes` protege um valor real setado fora de banda) + politica IAM `secretsmanager:GetSecretValue` escopada SO ao ARN do secret, anexada so aos 2 Lambdas (`whatsapp_delivery`, `whatsapp_webhook_handler` novo); rota publica nova `modules/api-gateway` (`whatsapp_webhook` integration/route/lambda-permission, mesmo throttling restritivo de `guest_documents`); `whatsapp_access_token`/`whatsapp_phone_number_id` (placeholders de fatia 2/5) removidos de `variables.tf`. **Nao wireado nesta fatia** (fatia 4-5): `notification-router.ts`'s `SUPPORTED_CHANNELS`, quota de 24h (D-8). G-V3 real: assinatura invalida sempre rejeita antes de qualquer gravacao (7 testes adversariais incl. header ausente/malformado/tamanho errado); replay do mesmo wamid e idempotente (`DUPLICATE_INBOX`); correlacao falha vira `UNMATCHED` na MESMA linha; secrets nunca logados/persistidos fora do `SecretsManager`; purge de conta nunca precisa de tenant. 30+9+4 testes novos (`whatsapp-webhook-processor.test.ts`/`whatsapp-webhook-workflow.test.ts`/`whatsapp-activation.test.ts`), 5 novos em `test/unit/workers/transient-purge.test.ts` (ramo ACCOUNT: purge sem tenant, sem cross-talk com fence de tenant, colisao de versao, cancelamento nao reconhecido relancado), 3 novos em `test/unit/occ.test.ts` (`buildAccountScopedVersionedUpdate`). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suite completa **2625/2625** (236 arquivos); `terraform fmt`/`validate` limpos, `terraform test` do stack principal 23/23 (`AWS_PROFILE=claude-dev`) - modulo `api-gateway` isolado tem 3 assercoes de contagem de rota pre-existentes e nao relacionadas falhando (10->13 items, 21->22 subjects, 3->5 imports; achado incidental de outra sessao concorrente sem isolamento de worktree, confirmado via `git diff --stat` que esta fatia so ADICIONA linhas a esse arquivo - nao investigado/corrigido aqui, fora de escopo). **PR/merge/CI/verificacao ao vivo em `dev` NAO executados nesta sessao** (orcamento de tempo insuficiente para um `terraform plan`/`apply` real supervisionado depois de tocar infra que ja esta deployada - `module.whatsapp_delivery` muda de shape de env var - dado que e seguro adiar, ver `NEXT_SESSION_PROMPT.md`). | Nivel 5-6 para os 2 gaps achados (modelagem de dado/seguranca que atravessa um invariante transversal do repositorio - GSI8/tenant-fencing e o unico builder OCC condicional do sistema); Nivel 3-4 para o resto (implementacao direta de D-197/D-7/D-10 ja `APPROVED`) | 9,0 (gap 1) / 9,3 (gap 2) | 9,2 (gap 1) / 9,2 (gap 2) | **IMPLEMENTADO (fatia 3/5) - fatia 4/5 (quota D-8 de 24h + IAM dedicada) e a proxima acao real; terraform desta fatia validado (`fmt`/`validate`/`test` do stack principal) mas nunca `plan`/`apply`'d contra AWS real nem mergeado a `main`** | `src/shared/dynamodb/occ.ts`, `src/shared/transient-purge-gsi8.ts`, `src/workers/transient-purge/{purge.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/notification/application/{whatsapp-webhook-processor.ts,whatsapp-webhook-workflow.ts,whatsapp-activation.ts,ses-callback-workflow.ts}`, `src/modules/notification/persistence/secrets-manager-whatsapp-config.ts`, `src/modules/extraction/{ports/feature-flags-reader.ts,persistence/appconfig-feature-flags-reader.ts}`, `src/runtime/aws/handlers/{whatsapp-webhook-handler.ts,whatsapp-delivery-handler.ts}`, `src/runtime/aws/composition/notification.ts`, `schemas/api/webhook-inbox.v1.json`, `infra/main.tf`, `infra/variables.tf`, `infra/modules/api-gateway/{main.tf,variables.tf,tests/api_gateway.tftest.hcl}`, `infra/modules/feature-flags/{main.tf,variables.tf}`, `scripts/build-lambdas.ts`, `package.json`, `test/unit/{occ.test.ts,workers/transient-purge.test.ts,workers/transient-purge-fakes.ts,notification/whatsapp-webhook-processor.test.ts,notification/whatsapp-webhook-workflow.test.ts,notification/whatsapp-activation.test.ts,notification/in-memory-store.ts}`, `test/contract/schemas.test.ts`, `docs/architecture/reviews/whatsapp-channel-scoping/webhook-inbox-purge-scoping/` | 2026-09-08 |
scripts\backfill-gsi8-core-user-data-purge.ts:2: * GSI8 backfill for core-user-data-purge (D-179/D-190, 9th and LAST slice, mirrors
scripts\backfill-gsi8-core-user-data-purge.ts:4: * instead of a fixed `createdAt` clock). Writes the `GSI8PK`/`GSI8SK` MaintenanceDueIndex pointer
scripts\backfill-gsi8-core-user-data-purge.ts:8: * has a `GSI8SK` in the future. Skipping it here would mean it never gets a pointer at all (the
scripts\backfill-gsi8-core-user-data-purge.ts:9: * live write path only stamps GSI8 at the moment of a NEW `deletedAt` transition — currently just
scripts\backfill-gsi8-core-user-data-purge.ts:18: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-core-user-data-purge.ts:47:  GSI8PK?: string;
scripts\backfill-gsi8-core-user-data-purge.ts:48:  GSI8SK?: string;
scripts\backfill-gsi8-core-user-data-purge.ts:98:  writePointer: (row: CoreUserDataRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-core-user-data-purge.ts:109:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-core-user-data-purge.ts:147:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-core-user-data-purge.ts:152:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":version": row.version, ":deletedAt": row.deletedAt },
scripts\backfill-gsi8-document-file-reconciliation.ts:2: * GSI8 backfill for document-file-reconciliation (D-179 slice 3, mirrors
scripts\backfill-gsi8-document-file-reconciliation.ts:3: * `backfill-gsi8-invitation-purge.ts` exactly). Writes the `GSI8PK`/`GSI8SK` MaintenanceDueIndex
scripts\backfill-gsi8-document-file-reconciliation.ts:74:  writePointer: (f: DocumentFile, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-document-file-reconciliation.ts:85:    if (file.GSI8PK && file.GSI8SK) {
scripts\backfill-gsi8-document-file-reconciliation.ts:122:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-document-file-reconciliation.ts:126:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":v": file.version },
scripts\backfill-gsi8-delivery-record-purge.ts:2: * GSI8 backfill for delivery-record-purge (D-179/D-18x slice 8, mirrors
scripts\backfill-gsi8-delivery-record-purge.ts:5: * OCC counter present on both entities instead of none). Writes the `GSI8PK`/`GSI8SK`
scripts\backfill-gsi8-delivery-record-purge.ts:10: * GSI8 at each of the 3 real creation sites - `reminder-dispatch/dispatch.ts`,
scripts\backfill-gsi8-delivery-record-purge.ts:15: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-delivery-record-purge.ts:50:  GSI8PK?: string;
scripts\backfill-gsi8-delivery-record-purge.ts:51:  GSI8SK?: string;
scripts\backfill-gsi8-delivery-record-purge.ts:98:  writePointer: (row: DeliveryRecordRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-delivery-record-purge.ts:108:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-delivery-record-purge.ts:146:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-delivery-record-purge.ts:151:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":version": row.version },
scripts\backfill-gsi8-invitation-purge.ts:2: * GSI8 backfill for invitation-purge (D-179/D-181 slice 2, mirrors
scripts\backfill-gsi8-invitation-purge.ts:3: * `backfill-gsi8-membership-purge.ts` exactly). Writes the `GSI8PK`/`GSI8SK` MaintenanceDueIndex
scripts\backfill-gsi8-invitation-purge.ts:8: * write path only stamps GSI8 at `create-invitation.ts`/`revoke-invitation.ts`'s own moments),
scripts\backfill-gsi8-invitation-purge.ts:13: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-invitation-purge.ts:75:  writePointer: (i: Invitation, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-invitation-purge.ts:86:    if (invitation.GSI8PK && invitation.GSI8SK) {
scripts\backfill-gsi8-invitation-purge.ts:123:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-invitation-purge.ts:127:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":v": invitation.version },
scripts\backfill-gsi8-quota-telemetry-purge.ts:2: * GSI8 backfill for quota-telemetry-purge (D-179/D-186 slice 5, mirrors
scripts\backfill-gsi8-quota-telemetry-purge.ts:4: * field). Writes the `GSI8PK`/`GSI8SK` MaintenanceDueIndex pointer onto every pre-existing
scripts\backfill-gsi8-quota-telemetry-purge.ts:8: * live write path only stamps GSI8 inside `consume()`/`incrementTelemetryCounter()` themselves),
scripts\backfill-gsi8-quota-telemetry-purge.ts:13: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-quota-telemetry-purge.ts:41:  GSI8PK?: string;
scripts\backfill-gsi8-quota-telemetry-purge.ts:42:  GSI8SK?: string;
scripts\backfill-gsi8-quota-telemetry-purge.ts:89:  writePointer: (row: QuotaTelemetryRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-quota-telemetry-purge.ts:99:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-quota-telemetry-purge.ts:137:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-quota-telemetry-purge.ts:142:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":resetAt": row.resetAt },
scripts\backfill-gsi8-requirement-reindex.ts:2: * GSI8 backfill for requirement-reindex (D-179 slice 4, mirrors
scripts\backfill-gsi8-requirement-reindex.ts:3: * `backfill-gsi8-document-file-reconciliation.ts` exactly). Writes the `GSI8PK`/`GSI8SK`
scripts\backfill-gsi8-requirement-reindex.ts:70:  writePointer: (r: Requirement, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-requirement-reindex.ts:81:    if (requirement.GSI8PK && requirement.GSI8SK) {
scripts\backfill-gsi8-requirement-reindex.ts:119:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-requirement-reindex.ts:124:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":v": requirement.version },
test\integration-dynamodb\setup.ts:4: * Table schema mirrors infra/lib/dynamo-table.ts exactly (PK/SK + GSI1-GSI6, GSI3 KEYS_ONLY).
test\integration-dynamodb\setup.ts:42:        { AttributeName: "GSI3PK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:43:        { AttributeName: "GSI3SK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:44:        { AttributeName: "GSI6PK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:45:        { AttributeName: "GSI6SK", AttributeType: "S" },
test\integration-dynamodb\setup.ts:61:          IndexName: "GSI3",
test\integration-dynamodb\setup.ts:63:            { AttributeName: "GSI3PK", KeyType: "HASH" },
test\integration-dynamodb\setup.ts:64:            { AttributeName: "GSI3SK", KeyType: "RANGE" },
test\integration-dynamodb\setup.ts:69:          IndexName: "GSI6",
test\integration-dynamodb\setup.ts:71:            { AttributeName: "GSI6PK", KeyType: "HASH" },
test\integration-dynamodb\setup.ts:72:            { AttributeName: "GSI6SK", KeyType: "RANGE" },
scripts\backfill-gsi8-membership-purge.ts:2: * GSI8 backfill for membership-purge (D-179/D-180 pilot slice, design §1/§3). Writes the
scripts\backfill-gsi8-membership-purge.ts:3: * `GSI8PK`/`GSI8SK` MaintenanceDueIndex pointer onto every pre-existing `Membership` row that
scripts\backfill-gsi8-membership-purge.ts:6: * one from 40 days ago; it just has a `GSI8SK` in the future. Skipping it here would mean it never
scripts\backfill-gsi8-membership-purge.ts:7: * gets a pointer at all (the write path only stamps GSI8 at the moment of a NEW removal), silently
scripts\backfill-gsi8-membership-purge.ts:12: * script is the explicit, manual, one-off step that activates GSI8 discovery for the installed
scripts\backfill-gsi8-membership-purge.ts:19: * no GSI3/GSI4/GSI6 access needed.
scripts\backfill-gsi8-membership-purge.ts:77:  writePointer: (m: Membership, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-membership-purge.ts:88:    if (membership.GSI8PK && membership.GSI8SK) {
scripts\backfill-gsi8-membership-purge.ts:125:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-membership-purge.ts:130:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":v": membership.version },
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:65:  it("materialize -> producer claim (real GSI3 query + real TransactWriteItems) -> durable outbox -> relay -> dispatch -> exactly one NotificationIntent", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:92:    // Real GSI3 Query against DynamoDB Local, real conditional TransactWriteItems claim,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:94:    // transaction, real GSI6 WORKSTATE#CLAIMED pointer.
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:115:    expect(claimedOccurrence?.GSI6PK).toBe("WORKSTATE#CLAIMED");
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:117:    // Real outbox relay: reads the durable record (via a GSI6 candidate-shaped shape here,
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:124:          IndexName: "GSI6",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:125:          KeyConditionExpression: "GSI6PK = :pk",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:141:    // markPublished must have removed the GSI6 pointer for real (Codex-found bug, fixed).
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:145:        IndexName: "GSI6",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:146:        KeyConditionExpression: "GSI6PK = :pk",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:165:    expect(triggeredOccurrence?.GSI6PK).toBeUndefined(); // pointer removed on TRIGGERED, proven against real DynamoDB
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:175:  it("real conditional Query on GSI6 finds an expired claim and reconciliation reverts it via real TransactWriteItems", async () => {
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:214:      GSI6PK: "WORKSTATE#CLAIMED",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:215:      GSI6SK: "2026-09-10T12:02:00.000Z#TENANT#t1#OCCURRENCE#occ_expired",
test\integration-dynamodb\reminder-engine.dynamodb.test.ts:227:    expect(afterRevert?.GSI6PK).toBeUndefined();
scripts\backfill-gsi8-security-audit-purge.ts:2: * GSI8 backfill for security-audit-purge (D-179/D-187 slice 6, mirrors
scripts\backfill-gsi8-security-audit-purge.ts:4: * `occurredAt` fence instead of `resetAt`). Writes the `GSI8PK`/`GSI8SK` MaintenanceDueIndex
scripts\backfill-gsi8-security-audit-purge.ts:9: * write path only stamps GSI8 inside each entity's own `build*Event()`), silently falling out of
scripts\backfill-gsi8-security-audit-purge.ts:14: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-security-audit-purge.ts:47:  GSI8PK?: string;
scripts\backfill-gsi8-security-audit-purge.ts:48:  GSI8SK?: string;
scripts\backfill-gsi8-security-audit-purge.ts:106:  writePointer: (row: SecurityAuditRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-security-audit-purge.ts:116:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-security-audit-purge.ts:160:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-security-audit-purge.ts:164:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":occurredAt": row.occurredAt },
scripts\backfill-gsi8-transient-purge.ts:2: * GSI8 backfill for transient-purge (D-179/D-188 slice 7, mirrors
scripts\backfill-gsi8-transient-purge.ts:4: * dynamics — see `shared/transient-purge-gsi8.ts`'s file header). Writes the `GSI8PK`/`GSI8SK`
scripts\backfill-gsi8-transient-purge.ts:8: * pointer at all (the live write path only stamps GSI8 at the specific writers listed below),
scripts\backfill-gsi8-transient-purge.ts:18: * This script is the explicit, manual, one-off step that activates GSI8 discovery for the
scripts\backfill-gsi8-transient-purge.ts:50:  GSI8PK?: string;
scripts\backfill-gsi8-transient-purge.ts:51:  GSI8SK?: string;
scripts\backfill-gsi8-transient-purge.ts:114:  writePointer: (row: TransientRow, gsi8: { GSI8PK: string; GSI8SK: string }) => Promise<boolean>,
scripts\backfill-gsi8-transient-purge.ts:131:    if (row.GSI8PK && row.GSI8SK) {
scripts\backfill-gsi8-transient-purge.ts:169:          UpdateExpression: "SET GSI8PK = :pk, GSI8SK = :sk",
scripts\backfill-gsi8-transient-purge.ts:173:          ExpressionAttributeValues: { ":pk": gsi8.GSI8PK, ":sk": gsi8.GSI8SK, ":version": row.version },
test\unit\dispatch-outbox-relay-processor.test.ts:27:    GSI6PK: "RECON#OUTBOX#PENDING",
test\unit\dispatch-outbox-relay-processor.test.ts:28:    GSI6SK: "2026-08-19T10:00:00.000Z#evt-1",
test\unit\dispatch-outbox-relay-processor.test.ts:94:      streamRecord("evt-2", outboxRecord({ eventId: "evt-2", correlationId: "cor-2", SK: "EVENT#...#evt-2", GSI6SK: "x2" })),
test\unit\dispatch-outbox-relay-processor.test.ts:95:      streamRecord("evt-3", outboxRecord({ eventId: "evt-3", correlationId: "cor-3", SK: "EVENT#...#evt-3", GSI6SK: "x3" })),
test\unit\dispatch-outbox-relay\relay.test.ts:24:    GSI6PK: "RECON#OUTBOX#PENDING",
test\unit\dispatch-outbox-relay\relay.test.ts:25:    GSI6SK: "2026-08-19T10:00:00.000Z#evt-1",
test\unit\dispatch-outbox-relay\relay.test.ts:142:    store.pending = [makeRecord({ SK: "EVENT#...#evt-1", eventId: "evt-1" }), makeRecord({ SK: "EVENT#...#evt-2", eventId: "evt-2", GSI6SK: "x2" })];
test\unit\dispatch-outbox-relay\relay.test.ts:158:      makeRecord({ SK: "EVENT#...#evt-2", eventId: "evt-2", GSI6SK: "x2", destination: "SQS_NOTIFICATION_EMAIL_V1" }),
test\unit\dispatch-outbox-relay\relay.test.ts:226:    await publishOne(deps, makeRecord({ correlationId: "cor_b", SK: "EVENT#...#evt-2", eventId: "evt-2", GSI6SK: "x2" }));
test\unit\document\advance-after-evidence.test.ts:50:    GSI6PK: "RECON#UPLOAD#PENDING",
test\unit\document\advance-after-evidence.test.ts:51:    GSI6SK: "2026-08-22T00:10:00.000Z#TENANT#t1#SLOT#slot1",
test\unit\document\advance-after-evidence.test.ts:309:    it("marks the associated RESERVED slot CONSUMED and strips its GSI6 pointer on PROMOTE", async () => {
test\unit\document\advance-after-evidence.test.ts:326:      expect(slot.GSI6PK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:327:      expect(slot.GSI6SK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:328:      // D-179/D-188 (transient-purge, 7th GSI8 slice): CONSUMED is a terminal transition off
test\unit\document\advance-after-evidence.test.ts:329:      // RESERVED - the GSI8 pointer must be written in this SAME conditional write, reflecting
test\unit\document\advance-after-evidence.test.ts:331:      expect(slot.GSI8PK).toBe("WORK#TRANSIENT");
test\unit\document\advance-after-evidence.test.ts:332:      expect(slot.GSI8SK).toBe(`2026-08-29T00:00:00.000Z#TENANT#t1#UploadSlot#${uploadSlotKey("t1", "slot1").SK}`);
test\unit\document\advance-after-evidence.test.ts:335:    it("marks the associated RESERVED slot CONSUMED and strips its GSI6 pointer on REJECT", async () => {
test\unit\document\advance-after-evidence.test.ts:346:      expect(slot.GSI6PK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:347:      expect(slot.GSI6SK).toBeUndefined();
test\unit\document\advance-after-evidence.test.ts:348:      expect(slot.GSI8PK).toBe("WORK#TRANSIENT");
test\unit\document\advance-after-evidence.test.ts:349:      expect(slot.GSI8SK).toBe(`2026-08-29T00:00:00.000Z#TENANT#t1#UploadSlot#${uploadSlotKey("t1", "slot1").SK}`);
test\unit\document\advance-after-evidence.test.ts:354:      await store.putIfAbsent(baseSlot({ status: "EXPIRED", GSI6PK: undefined, GSI6SK: undefined }));
test\unit\reports\scheduler.test.ts:42:/** Fake mirroring the real DynamoDB GSI8 Query's contract - reads live off the same store the
test\unit\reports\scheduler.test.ts:50:        .filter((item): item is EntityKey & Record<string, unknown> => item["entityType"] === "ReportSubscription" && typeof item["GSI8SK"] === "string" && (item["GSI8SK"] as string) < input.before)
test\unit\reports\scheduler.test.ts:52:          const gsi8sk = item["GSI8SK"] as string;
test\unit\reports\scheduler.test.ts:75:  it("claims a due subscription: advances nextRunAt/GSI8 pointer and writes a durable outbox event", async () => {
test\unit\reports\scheduler.test.ts:85:    expect(updated?.GSI8SK).toBe(`2026-09-16T09:00:00.000Z#TENANT#${TENANT}#${subscription.subscriptionId}`);
test\unit\reports\scheduler.test.ts:98:  it("never touches a subscription whose nextRunAt is still in the future - the GSI8 query's own GSI8SK < before filter excludes it", async () => {
test\unit\reports\scheduler.test.ts:116:  it("skips (never throws) a subscription concurrently claimed by another tick since the GSI8 query observed it", async () => {
test\unit\reports\scheduler.test.ts:149:    // A real GSI8 Query is eventually consistent - pin the candidate source to the stale
test\unit\workers\transient-purge.test.ts:73:  it("never surfaces a WebhookInbox row younger than 7 days as a candidate at all (GSI8 is due-ordered)", async () => {
test\unit\workers\transient-purge.test.ts:93:  it("never seeds a GSI8 pointer for a RESERVED UploadSlot (never a candidate at all, not even scanned)", async () => {
test\unit\workers\transient-purge.test.ts:100:    expect(stored?.["GSI8PK"]).toBeUndefined();
test\unit\workers\transient-purge.test.ts:101:    expect(stored?.["GSI8SK"]).toBeUndefined();
test\unit\workers\transient-purge.test.ts:112:    // GSI8's own `queryDue` uses a strict `GSI8SK < :before` (same as every other migrated
test\unit\workers\transient-purge.test.ts:192:      expect(stored?.["GSI8PK"]).toBe("WORK#TRANSIENT"); // still in WORK namespace, not DLQ yet
test\unit\workers\transient-purge.test.ts:207:  it("conditional-delete guard: a candidate whose version changed between the GSI8 query and the claim is never purged", async () => {
test\unit\workers\transient-purge.test.ts:228:  it("conditional-delete guard: a row deleted between the GSI8 query and the claim (PK/SK gone) is never silently treated as success", async () => {
test\unit\workers\transient-purge.test.ts:260:  it("processes a mix of WebhookInbox/UploadSlot candidates across tenants in one run and touches ONLY the ones GSI8 actually surfaces", async () => {
test\unit\workers\transient-purge.test.ts:281:    // tooRecentInbox/reservedSlot never appear in GSI8's `GSI8SK < now` query at all (reservedSlot
test\unit\workers\transient-purge.test.ts:292:  it("drains multiple GSI8 pages within one run", async () => {
test\unit\workers\transient-purge.test.ts:340:    expect(stored?.["GSI8PK"]).toBe("DLQ#TRANSIENT");
test\unit\workers\transient-purge.test.ts:364:  it("self-heals a stale/malformed GSI8 pointer left on a RESERVED UploadSlot (defensive only — no real writer produces this)", async () => {
test\unit\workers\transient-purge.test.ts:388:    expect(stored?.["GSI8PK"]).toBeUndefined();
test\unit\workers\transient-purge.test.ts:389:    expect(stored?.["GSI8SK"]).toBeUndefined();
test\unit\workers\requirement-evidence-refresh.test.ts:77: * `requirement-reindex.test.ts`'s `fakeCandidateSource` uses for GSI8. Only exposes the ONE
test\unit\workers\requirement-evidence-refresh.test.ts:127:    expect(updated?.GSI8PK).toBeUndefined();
test\unit\workers\requirement-evidence-refresh.test.ts:223:      remove: ["evidenceVersionId", "evidenceDocumentId", "evidenceSeq", "evidenceState", "evidenceValidUntil", "GSI8PK", "GSI8SK", "GSI9PK", "GSI9SK"],
test\unit\workers\requirement-evidence-refresh.test.ts:230:    // documents for its own GSI8 staleness test. The worker's own per-Requirement fresh
test\unit\shared\security-audit-gsi8.test.ts:12:  it("builds GSI8PK=WORK#SECURITY_AUDIT and embeds tenantId/entityType/sk into GSI8SK", () => {
test\unit\shared\security-audit-gsi8.test.ts:14:    expect(keys.GSI8PK).toBe(`WORK#${SECURITY_AUDIT_PURGE_WORK_TYPE}`);
test\unit\shared\security-audit-gsi8.test.ts:15:    expect(keys.GSI8SK).toBe("2026-01-01T00:00:00.000Z#TENANT#tenant-1#AuditEvent#EVT#2025-01-01T00:00:00.000Z#evt-1");
test\unit\workers\invitation-purge.test.ts:70:  it("never surfaces a REVOKED invitation whose revokedAt is less than 30 days ago as a candidate at all (GSI8 is due-ordered)", async () => {
test\unit\workers\invitation-purge.test.ts:139:      expect(stored?.["GSI8PK"]).toBe("WORK#INVITATION_PURGE"); // still in WORK namespace, not DLQ yet
test\unit\workers\invitation-purge.test.ts:207:  it("processes a mix of eligible/blocked candidates across tenants in one run and touches ONLY the ones GSI8 actually surfaces", async () => {
test\unit\workers\invitation-purge.test.ts:227:    // tooRecent never appears in GSI8's `GSI8SK < now` query at all - not "scanned and skipped".
test\unit\workers\invitation-purge.test.ts:236:  it("drains multiple GSI8 pages within one run", async () => {
test\unit\workers\invitation-purge.test.ts:284:    expect(stored?.["GSI8PK"]).toBe("DLQ#INVITATION_PURGE");
test\unit\workers\invitation-purge.test.ts:295:  it("self-heals a stale GSI8 pointer left on an invitation that is no longer a real candidate (defensive - no live path produces this today)", async () => {
test\unit\workers\invitation-purge.test.ts:310:    expect(after?.["GSI8PK"]).toBeUndefined();
test\unit\workers\invitation-purge.test.ts:311:    expect(after?.["GSI8SK"]).toBeUndefined();
test\unit\workers\core-user-data-purge-fakes.ts:38:/** In-memory fake standing in for the real DynamoDB `GSI8` Query + base-table `GetItem`/
test\unit\workers\core-user-data-purge-fakes.ts:53:  /** Seeds a row exactly as the real writer would leave it — computes GSI8PK/GSI8SK itself via
test\unit\workers\core-user-data-purge-fakes.ts:55:   * does. A row whose `deletedAt` is absent gets NO GSI8 pointer at all (never a candidate). */
test\unit\workers\core-user-data-purge-fakes.ts:70:  /** Test-only escape hatch to simulate a row disappearing between the GSI8 query and the claim
test\unit\workers\core-user-data-purge-fakes.ts:78:      .filter((i) => i["GSI8PK"] === "WORK#CORE_USER_DATA" && typeof i["GSI8SK"] === "string" && (i["GSI8SK"] as string) < input.before)
test\unit\workers\core-user-data-purge-fakes.ts:79:      .sort((a, b) => (a["GSI8SK"] as string).localeCompare(b["GSI8SK"] as string));
test\unit\workers\core-user-data-purge-fakes.ts:87:        dueAtIso: (i["GSI8SK"] as string).split("#TENANT#")[0]!,
test\unit\notification\whatsapp-webhook-workflow.test.ts:71:  it("the WebhookInbox row is account-scoped (PK=WEBHOOK#WHATSAPP#<wabaId>), purgeScope=ACCOUNT, GSI8 pointer keyed by accountId - never TENANT#", async () => {
test\unit\notification\whatsapp-webhook-workflow.test.ts:79:    expect(inbox?.["GSI8PK"]).toBe("WORK#TRANSIENT");
test\unit\notification\whatsapp-webhook-workflow.test.ts:80:    expect(inbox?.["GSI8SK"]).toBe(`2026-09-17T12:05:00.000Z#ACCOUNT#${WABA_ID}#WebhookInbox#EVENT#wamid-1#DELIVERED`);
test\unit\workers\transient-purge-fakes.ts:43:/** In-memory fake standing in for the real DynamoDB `GSI8` Query + base-table `GetItem`/
test\unit\workers\transient-purge-fakes.ts:56:  /** Seeds a row exactly as the real writer would leave it — computes GSI8PK/GSI8SK itself via
test\unit\workers\transient-purge-fakes.ts:59:   * `undefined`), matching every real writer's behavior. Pass `GSI8PK`/`GSI8SK` explicitly in
test\unit\workers\transient-purge-fakes.ts:72:  /** Escape hatch to seed a row with an explicit GSI8PK/GSI8SK regardless of what the real
test\unit\workers\transient-purge-fakes.ts:86:  /** Test-only escape hatch to simulate a row disappearing between the GSI8 query and the claim
test\unit\workers\transient-purge-fakes.ts:94:      .filter((i) => i["GSI8PK"] === "WORK#TRANSIENT" && typeof i["GSI8SK"] === "string" && (i["GSI8SK"] as string) < input.before)
test\unit\workers\transient-purge-fakes.ts:95:      .sort((a, b) => (a["GSI8SK"] as string).localeCompare(b["GSI8SK"] as string));
test\unit\workers\transient-purge-fakes.ts:101:        const gsi8sk = i["GSI8SK"] as string;
test\unit\reports\report-subscription.test.ts:17:  it("builds the GSI8 discovery pointer under the REPORT_SUBSCRIPTION worker namespace, due date first in the sort key", () => {
test\unit\reports\report-subscription.test.ts:19:    expect(keys.GSI8PK).toBe("WORK#REPORT_SUBSCRIPTION");
test\unit\reports\report-subscription.test.ts:20:    expect(keys.GSI8SK).toBe("2026-09-12T04:00:00.000Z#TENANT#t1#sub1");
test\unit\workers\delivery-record-purge.test.ts:43:  it("never surfaces a record created less than 180 days ago as a candidate at all (GSI8 is due-ordered)", async () => {
test\unit\workers\delivery-record-purge.test.ts:91:      expect(stored?.["GSI8PK"]).toBe("WORK#DELIVERY_RECORD"); // still in WORK namespace, not DLQ yet
test\unit\workers\delivery-record-purge.test.ts:179:  it("processes a mix of eligible/blocked candidates across tenants/entity types in one run and touches ONLY the ones GSI8 actually surfaces", async () => {
test\unit\workers\delivery-record-purge.test.ts:200:    // tooRecent never appears in GSI8's `GSI8SK < now` query at all - not "scanned and skipped".
test\unit\workers\delivery-record-purge.test.ts:210:  it("drains multiple GSI8 pages within one run", async () => {
test\unit\workers\delivery-record-purge.test.ts:258:    expect(stored?.["GSI8PK"]).toBe("DLQ#DELIVERY_RECORD");
test\unit\subject\document-chasing.test.ts:5:describe("document-chasing domain — GSI3 discriminator (D-046: shared index, non-overlapping SK shape)", () => {
test\unit\subject\document-chasing.test.ts:6:  it("chasingGsi3Keys produces a GSI3SK shape that never matches the reminder parser", () => {
test\unit\subject\document-chasing.test.ts:8:    expect(keys.GSI3SK).toBe("TENANT#t1#CHASING#occ-1");
test\unit\subject\document-chasing.test.ts:9:    expect(() => parseGsi3Sk(keys.GSI3SK)).toThrow(/Malformed GSI3SK/);

2026-09-08T02:46:01.806286Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 93 column 5
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "simulate-principal-policy|gsi8_worker_isolation" docs/architecture/decisions-log.md infra .github scripts test; rg -n "TenantLifecycle|Membership|GlobalUser|organization|tenantId|schemaVersion|entityType" scripts/reset-dev-data.ts; rg -n "reset-dev-data" docs/architecture/decisions-log.md package.json; rg -n "TenantCost|cost.*tenant|tenant.*cost|tenantId" src/shared/observability src/modules/quota infra/modules/monitoring docs/architecture/cost-model.md | Select-Object -First 160' in C:\Users\Usuario\Desktop\projects\expiration-tracker
2026-09-08T02:46:12.587558Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.2 seconds
Total output lines: 37
Output:
docs/architecture/decisions-log.md:201:| D-180 | **MaintenanceDueIndex — implementação real, primeira fatia/piloto (D-179 aplicado, `membership-purge` ÚNICO worker migrado)**: GSI8 real (`GSI8PK`/`GSI8SK`, `KEYS_ONLY`) adicionado ao módulo Terraform (`infra/modules/dynamo-table/main.tf`), `local.gsi8_worker_types = { membership_purge = "MEMBERSHIP_PURGE" }` gerando `gsi8_read_policy_json`/`worker_transact_write_policy_json` por chave — `dynamodb:Query` condicionado `ForAllValues:StringEquals dynamodb:LeadingKeys IN ["WORK#MEMBERSHIP_PURGE","DLQ#MEMBERSHIP_PURGE"]` + `dynamodb:TransactWriteItems` na tabela base (ação nova, não coberta por `tenant_facing_read_write`), ambos anexados só ao role do `membership_purge_handler`. `deriveMembershipMaintenanceDue()`/`membershipGsi8Keys()`/`MEMBERSHIP_PURGE_WORK_TYPE`/`MEMBERSHIP_RETENTION_DAYS` movidos para `modules/organization/domain/membership.ts` (função pura, D-179 §2) — reusada pelos 3 consumidores exigidos: `remove-membership.ts`/`leave-organization.ts` (gravam `GSI8PK`/`GSI8SK` no MESMO `Update` que a transição `REMOVED`, nunca write separado), `scripts/backfill-gsi8-membership-purge.ts` (novo, cobre todo `Membership` com `dueAt` computável — inclusive vencimento FUTURO, corrigindo o erro que a Rodada 3 do design cometeu), e a revalidação do próprio worker. `accept-invitation.ts`'s reativação agora também `REMOVE GSI8PK, GSI8SK` atomicamente ao limpar `removedAt` (autolimpeza de ponteiro obsoleto na origem). `membership-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`Limit`; `purge.ts` revalida cada candidato via `getMembership()` consistente antes de agir (GSI8 nunca é fonte de elegibilidade) e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por versão, na MESMA transação — fecha o TOCTOU que o padrão antigo tinha entre ler o status do tenant e deletar). Poison-record/quarentena (D-179 §8): `maintenanceAttemptCount` incrementado só quando o `ConditionCheck` de tenant falha (nunca no `Delete`), backoff exponencial capado (1,2,4,8,16 dias) reposicionando `GSI8SK`, acima de `MAX_ATTEMPTS=5` move para `GSI8PK="DLQ#MEMBERSHIP_PURGE"`; toda escrita de retry é idempotente (`ConditionalCheckFailedException` tratado como no-op, mesmo padrão que o delete já usava). Ponteiro obsoleto (item reativado após ser indexado) é autolimpo pelo próprio worker via `REMOVE GSI8PK, GSI8SK`. Observabilidade: `MembershipPurgeResult` ganha `quarantinedCount`/`oldestCandidateAgeSeconds` (computado do primeiro item da página, sem I/O extra); o handler já fazia `logger.info(..., {...result})`, então os 2 campos novos já são emitidos sem tocar o handler. **G-V3 ao vivo em 3 mecanismos**: teste novo prova que `RemoveMembershipService`/`LeaveOrganizationService` gravam `GSI8PK`/`GSI8SK` corretos (falha se removido); teste novo prova que `accept-invitation.ts` limpa os 2 atributos na reativação (falha se removido); teste novo do worker leva um candidato através de 6 execuções com tenant nunca `ACTIVE` e prova a quarentena em `DLQ#MEMBERSHIP_PURGE` no `MAX_ATTEMPTS+1`-ésimo ataque, e um teste separado prova o autoconserto do ponteiro obsoleto. `infra/tests/stack.tftest.hcl` ganha `run "gsi8_worker_isolation"`: prova que `membership_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, que `TransactWriteItems` nunca aparece numa política referenciando GSI3/GSI4/GSI6/GSI8, e que `invitation_purge_handler`/`transient_purge_handler`/`test_ping_handler` NÃO referenciam GSI8 (isolamento por worker, não por índice). `gsi_count` 7→8. Suíte completa 1778/1778, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (incluindo o caso novo). **Próximo worker recomendado para a próxima fatia**: `invitation-purge` (mesmo padrão de `TenantLifecycleStatusSource`/fence ACTIVE já usado aqui, candidato mais próximo estruturalmente) — ordem não é normativa, qualquer um dos 8 restantes serve. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — infra nova real porém mecânica, seguindo spec exata) | n/a | n/a | **IMPLEMENTADO (piloto, 1 de 9 workers)** — `membership-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes); os outros 8 workers nomeados por D-179 permanecem em `Scan`+`Limit`, migração worker-a-worker é trabalho futuro explícito | `infra/modules/dynamo-table/{main.tf,outputs.tf}`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/membership.ts`, `src/modules/organization/application/{remove-membership.ts,leave-organization.ts,accept-invitation.ts}`, `src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/membership-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-membership-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:202:| D-181 | **MaintenanceDueIndex piloto (D-180) — fix real de um bug de deploy pego pelo próprio CD, mais verificação ao vivo completa contra `dev`.** A primeira tentativa de merge de D-180 para `main` (PR #198) teve CI verde mas o `Deploy (CD)` real (`terraform apply` em `dev`) **falhou de verdade** (`aws.amazon.com` `MalformedPolicyDocument: Statement IDs (SID) must be alpha-numeric`) — os 2 `Sid`s novos (`gsi8_read`/`worker_transact_write` em `infra/modules/dynamo-table/main.tf`) interpolavam `each.key` (`"membership_purge"`, com underscore) diretamente, mas IAM exige `[0-9A-Za-z]*`. Confirmado por leitura direta do log do run que falhou (não presumido) e por uma checagem ao vivo independente (`aws iam list-role-policies --role-name exptrk-dev-membership-purge-handler-role`, que mostrou só a política base `capability-0`, confirmando que as 2 políticas novas nunca chegaram a existir). Corrigido com `join("", [for part in split("_", each.key) : title(part)])` (PascalCase, sem underscore) nos dois `sid`s; `terraform test` 22/22 continuou verde. PR #199 mergeado, CD real (`33636904901`) `success`. **Verificação ao vivo completa, tudo confirmado via `aws --profile claude-dev`, nada presumido**: `describe-table` mostra GSI8 na lista de índices; `list-role-policies`/`get-role-policy` no role do worker mostram as 3 políticas (base + `Gsi8ReadOnlyMembershipPurge` com a condição `LeadingKeys` exata + `TransactWriteItemsMembershipPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de negação cross-namespace (`aws iam simulate-principal-policy`, exigida pelo DoD do piloto)**: simulando `dynamodb:Query` no role do `membership_purge_handler` com `dynamodb:LeadingKeys=WORK#MEMBERSHIP_PURGE` (seu próprio namespace) → `allowed`, casando a statement `Gsi8ReadOnlyMembershipPurge`; a mesma simulação com `dynamodb:LeadingKeys=WORK#INVITATION_PURGE` (namespace de outro worker, ainda não migrado) → `implicitDeny`, nenhuma statement casada — prova real, nos dois sentidos (controle positivo + negativo), de que o isolamento por `LeadingKeys` funciona exatamente como o design exige, não só no papel do Terraform. **Achado de processo desta sessão, registrado por ser real**: uma mensagem externa recebida durante a espera do CD afirmou "CD completed successfully" e pediu para prosseguir direto à prova de IAM e ao registro em `decisions-log.md` — checada diretamente via `gh run view` antes de confiar, e o run real estava `failure` (a mesma falha do parágrafo acima), não `success`; a alegação foi descartada e o bug real foi corrigido antes de qualquer prova de IAM ou registro de documentação, exatamente a disciplina que este arquivo e `AGENTS.md` exigem (nunca tratar uma alegação não verificada como fato). | Nível 1-2 (fix mecânico de um bug de infra pego em produção `dev`, mais verificação, sem decisão de design nova) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO** — piloto D-179/D-180 100% completo, incluindo a prova de isolamento IAM que o DoD do piloto exigia antes de liberar a migração dos 8 workers restantes | `infra/modules/dynamo-table/main.tf` | 2026-09-02 |
docs/architecture/decisions-log.md:203:| D-182 | **MaintenanceDueIndex — segunda fatia (invitation-purge, D-179 slice 2, mesmo padrão de D-180)**: `invitation_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["invitation_purge"]`/`worker_transact_write_policy_json["invitation_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#INVITATION_PURGE","DLQ#INVITATION_PURGE"]` + `TransactWriteItems` na tabela base, ambos só no role do `invitation_purge_handler`. `deriveInvitationMaintenanceDue()`/`invitationGsi8Keys()`/`INVITATION_PURGE_WORK_TYPE` novos em `domain/invitation.ts` (função pura, D-179 §2) — duas ramificações independentes, nuance real vs. o piloto: `PENDING`'s due date (`expiresAt+30d`) já é totalmente conhecido NA CRIAÇÃO (não numa transição posterior como o `REMOVED` de Membership), então `create-invitation.ts`'s `invite()` grava o ponteiro GSI8 no MESMO `Put` da Invitation, e `resend()` o move junto quando `expiresAt` roda; `revoke-invitation.ts` sobrescreve o ponteiro no MESMO `Update` que a transição real para `REVOKED`; `accept-invitation.ts` limpa (`REMOVE GSI8PK, GSI8SK`) atomicamente ao aceitar, já que `ACCEPTED` nunca é candidato. `invitation-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`TenantLifecycleStatusSource` separado (a checagem ACTIVE virou `ConditionCheck` dentro da própria transação de claim, como no piloto); `purge.ts` revalida via `getInvitation()` consistente antes de agir e reivindica via `TransactWriteItems` (mesmo `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)`+`Delete` condicionado por versão). Poison-record/quarentena idêntico ao piloto (backoff 1,2,4,8,16 dias, `DLQ#INVITATION_PURGE` acima de `MAX_ATTEMPTS=5`). **Achado registrado, não um bug**: não existe caminho real de reativação para `Invitation` (ao contrário de `Membership`) — `REVOKED`/`ACCEPTED` são terminais, `ACCEPTED` já limpa seu próprio ponteiro na origem — então o autoconserto de ponteiro obsoleto em `purge.ts` é só defesa em profundidade, documentado explicitamente no cabeçalho do arquivo em vez de omitido. `GlobalIndexComponent` (taxonomia fechada de `security-audit.ts`) ganha `"invitation-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.invitation_purge_handler.function_name` (achado do próprio teste de arquitetura — sem isso o novo call site de `auditGlobalIndexAccess` ficaria sem alarme wireado). Novo `scripts/backfill-gsi8-invitation-purge.ts`, cobrindo as duas ramificações inclusive vencimento FUTURO (mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido: `invitation_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, e isolamento cross-namespace nos dois sentidos (nenhum role referencia o namespace do outro). **G-V3 ao vivo**: testes novos provam o ponteiro gravado na criação, movido no resend, sobrescrito no revoke, limpo no accept; teste do worker prova quarentena em `DLQ#INVITATION_PURGE` no `MAX_ATTEMPTS+1`; teste do backfill prova cobertura das duas ramificações. Suíte completa 1797/1797 (169 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22. PR #201 mergeado (`9a21da1`), CI `success` (run 33656227410), CD real `success` confirmado via `gh run view` (run 33656568649, não presumido de alegação externa). **Verificação ao vivo em `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role do `invitation_purge_handler` tem as 3 políticas esperadas (base+`Gsi8ReadOnlyInvitationPurge`+`TransactWriteItemsInvitationPurge`). **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, nos dois sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#INVITATION_PURGE` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyInvitationPurge`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE` (namespace do outro worker já migrado) → `implicitDeny`, nenhuma statement casada. **Próximo worker recomendado (não normativo)**: qualquer um dos 7 restantes (`document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — segunda fatia mecânica seguindo o padrão do piloto D-180, com uma nuance real de timing de escrita) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (2 de 9 workers migrados)** — `invitation-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 7 workers nomeados por D-179 permanecem em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/invitation.ts`, `src/modules/organization/application/{create-invitation.ts,revoke-invitation.ts,accept-invitation.ts}`, `src/workers/invitation-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/invitation-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-invitation-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:204:| D-183 | **MaintenanceDueIndex — terceira fatia (document-file-reconciliation, D-179 slice 3, 3º de 9 workers)**: `document_file_reconciliation` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["document_file_reconciliation"]`/`worker_transact_write_policy_json["document_file_reconciliation"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DOCUMENT_FILE_RECONCILIATION","DLQ#DOCUMENT_FILE_RECONCILIATION"]` + `TransactWriteItems` na tabela base, ambos só no role do `document_file_reconciliation_handler`. **Achado real pré-existente, confirmado antes de qualquer código novo (D-166's próprio texto já era ambíguo sobre isso, resolvido por leitura direta)**: o mecanismo GSI5 que D-164/D-166 desenharam para este worker (`fileReconciliationGsi5Keys()`, `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`) NUNCA teve um writer real — `reserveFiles()` (`document-archive-service.ts`) cria cada `DocumentFile` sem jamais chamar essa função, e nenhum outro call site em todo o código a invoca (confirmado por `grep` antes de tocar em qualquer arquivo). O `Scan` filtrado por `attribute_exists(GSI5PK)` que D-166 implementou, portanto, nunca poderia encontrar um candidato real em produção — o worker era código morto funcional desde o dia em que foi implementado, apesar de "IMPLEMENTADO E VERIFICADO" nos testes (os testes seedavam `GSI5PK` manualmente, nunca através do caminho de escrita real). Isso resolve por completo a pergunta "GSI8 substitui ou convive com GSI5" que a tarefa pedia para verificar antes de escrever qualquer código: não havia um mecanismo funcionando para conviver com — GSI8 é a primeira implementação real e completa deste ponteiro de descoberta, não uma segunda ao lado de uma que já funcionava. Não foi necessário reabrir D-163 Decisão 2 (GSI6 seguiu intocado) nem nenhuma outra decisão de alocação de índice. **Decidido e implementado**: `document-file.ts` ganha `FILE_SCAN_TIMEOUT_SECONDS=600` (mesma janela do presign TTL — `document-archive-service.ts`'s `PRESIGN_TTL_SECONDS` agora importa essa constante em vez de duplicá-la, deliberado: a janela de upload e o deadline de timeout de scan são a mesma janela), `deriveDocumentFileMaintenanceDue()` (due = `createdAt + FILE_SCAN_TIMEOUT_SECONDS` para `PENDING_UPLOAD`/`SCANNING`, `undefined` uma vez terminal — mesmo raciocínio do branch PENDING de `Invitation`: due date totalmente conhecido na criação, sem transição posterior para pendurar o relógio, já que SCANNING é só evidência chegando dentro da mesma janela fixa, nunca um novo relógio) e `documentFileGsi8Keys()`. O campo `DocumentFile.GSI5PK?/GSI5SK?` foi REMOVIDO (substituído por `GSI8PK?/GSI8SK?`) — `fileReconciliationGsi5Keys()` também removida, não deixada como código morto adicional. `reserveFiles()` agora grava o ponteiro GSI8 no MESMO `Put` de cada `DocumentFile` (mesmo padrão do branch PENDING de `invitationGsi8Keys()` — criação É a transição relevante). `apply-file-scan-result.ts`: as 3 funções (`applyFileScanResult`'s branch REJECT, `confirmFileScanClean`, `applyFileScanTimeout`) trocam `remove: ["GSI5PK","GSI5SK"]`/condição exata de ponteiro por GSI8; `applyFileScanTimeout`'s `observedGsi5Pointer`→`observedGsi8Pointer`, mesmo mecanismo de fence exato (`extraConditions` comparando o ponteiro observado literal), nenhuma mudança de comportamento. **Composição com o mecanismo transacional existente (item pedido pela tarefa)**: como a claim/revalidação real já vive inteiramente dentro de `applyFileScanTimeout()` (que já releia o `DocumentFile` fresco via `store.get()` e faz seu próprio retry OCC), o port do worker (`candidate-source.ts`) foi simplificado para só `queryDue()` — nenhum `getFile()`/`transactWrite()` próprio como `membership-purge`/`invitation-purge` precisam, evitando duplicar a revalidação que já existe. **Simplificação real habilitada por GSI8 (não pedida, descoberta ao desenhar)**: como GSI8PK é uniforme (`WORK#DOCUMENT_FILE_RECONCILIATION`, sem partição por status, diferente do GSI5PK por-status de D-166), as duas varreduras independentes que D-166 rodava (uma por `PENDING_UPLOAD`/`SCANNING`) colapsam numa única `Query` due-ordered cobrindo os dois status — `reconciliation.ts` reescrito para um único loop de paginação, mesmo formato de `invitation-purge/purge.ts`. **Sem mecanismo de poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada): diferente de `membership-purge`/`invitation-purge`, este worker nunca teve fence de tenant `ACTIVE` (D-166: "a stuck upload is not a retention decision") — não existe nenhuma condição que possa falhar permanentemente para o mesmo candidato; um timeout perdido por corrida é sempre transitório e resolvido no próximo run, nunca precisa de backoff/quarentena. **Achado adicional fora do escopo desta fatia, registrado e NÃO corrigido aqui**: `document_archive_handler` (o handler HTTP principal, que também chama `store.transactWrite()` via `reserveFiles()`/`acceptVersion()`) tem a MESMA lacuna de `dynamodb:TransactWriteItems` que este worker tinha antes desta fatia — `policy_documents_json` dele só inclui `tenant_facing_read_write_policy_json`+a policy de presign, nunca `TransactWriteItems`. Não corrigido nesta sessão (fora do escopo de D-179, achado pré-existente e independente); nomeado explicitamente para não ser esquecido. `GlobalIndexComponent` ganha `"document-file-reconciliation"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.document_file_reconciliation_handler.function_name`. Novo `scripts/backfill-gsi8-document-file-reconciliation.ts`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 3 workers. **G-V3 ao vivo**: teste novo prova que `reserveFiles()` grava `GSI8PK`/`GSI8SK` em cada `DocumentFile`; suíte de `apply-file-scan-r…27617 tokens truncated…ecord/DLQ)**: AMBAS entidades carregam um contador OCC `version` real (ao contrário das 4 entidades append-only de D-187) — mesma forma que D-188/`UploadSlot`/`WebhookInbox`; o `Delete`/backoff/DLQ do worker e o backfill novo condicionam em `version` exato, com `createdAt` reasserted como defesa em profundidade adicional (nada muta `createdAt` hoje, mas a condição torna estrutural, não só verdadeiro por acidente). Novo `src/shared/delivery-record-gsi8.ts` (fora de `domain/` de módulo específico, já que a entidade cobre 2 módulos — mesma razão de `security-audit-gsi8.ts`/`transient-purge-gsi8.ts`): `deriveDeliveryRecordMaintenanceDue()` (due = `createdAt+180d`, NUNCA `undefined` — sem estado terminal, cada registro de entrega expira pelo prazo fixo independentemente de qualquer ação) e `deliveryRecordGsi8Keys()`. **3 pontos de escrita reais confirmados por grep** (não presumidos) gravam o ponteiro GSI8 UMA ÚNICA VEZ, no MESMO `Put` da criação: `reminder-dispatch/dispatch.ts` (intent `EXPIRATION_REMINDER`), `notification-router-workflow.ts`'s `applyStaleDecision` (intent `REPLACEMENT`/`CORRECTIVE`) e `applyRoutedDecision` (o `NotificationAttempt`) — nenhum dos dois entidades é atualizada depois de criada nestes caminhos (o `version` existe para as próprias transições de status do módulo, não para uma atualização do ponteiro GSI8). `delivery-record-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (handler perde a dependência separada de `TenantLifecycleStatusSource`, dobrada dentro do `ConditionCheck` transacional); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `createdAt`, na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#DELIVERY_RECORD"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência de D-187, mesma causa): nenhuma das duas entidades é atualizada depois de criada NA PRÁTICA (sem caminho de undelete, sem transição de status nomeada nos comentários do próprio worker) — o ponteiro é gravado exatamente uma vez, e a única forma de uma linha deixar de ser candidata é este worker deletá-la. **Gap conhecido e aceito, herdado de D-152, NÃO tocado por esta fatia**: `NotificationAttemptLookup` (ponteiro derivado, `PK=TENANT#t#ATTEMPT#id`) continua NÃO sendo purgado por este worker e fica órfão depois que um `NotificationAttempt` é deletado — confirmado ainda válido após esta migração por releitura direta, não presumido; fora de escopo aqui como já era em D-152. `GlobalIndexComponent` ganha `"delivery-record-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.delivery_record_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-delivery-record-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e condicionado em `version` (não `createdAt`, adaptação real vs. o template de D-187). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 8 workers já migrados. **G-V3**: 3 testes de gravação do ponteiro na criação (via os fixtures reais de `dispatch.test.ts`/`notification-router-workflow.test.ts` — intent do dispatch, intent REPLACEMENT do stale-decision, attempt do roteamento feliz), 24 testes do worker (purga dos 2 tipos, due no futuro nunca candidato, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `createdAt`/`version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo os 2 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa local 1912/1912 (178 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #215 mergeado (`6d333e0`→`c595190`), CI real (`33699314795`/`33699331683`) e CD real (`33699827095`) `success`, confirmados via `gh run view`/`gh run list` direto (polling síncrono, nunca aguardando notificação de background como subagente). **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-delivery-record-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyDeliveryRecordPurge` com `LeadingKeys` exato `["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItemsDeliveryRecordPurge` escopada à tabela base); `get-function-configuration`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#DELIVERY_RECORD` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT` (os 7 já migrados) → `implicitDeny` nos 7 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (NotificationIntent, NotificationAttempt)` sobre as 58 linhas da tabela (`scannedTotal=58`, `matched=0`, tabela inteira coberta), e por `Query GSI8 WORK#DELIVERY_RECORD` retornando `Count=0`; nada para escrever, execução real (não-dry-run) seria um no-op confirmado, não executada (mesma disciplina de D-187/D-188). **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — oitava fatia mecânica seguindo o padrão de D-187/D-188 (poison-record/DLQ), adaptada para 2 tipos de entidade COM `version` real, condicionado em `version`+`createdAt`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (8 de 9 workers migrados)** — `delivery-record-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); o único worker restante nomeado por D-179 permanece em `Scan`+`Limit`: `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/delivery-record-gsi8.ts` (novo), `src/workers/reminder-dispatch/dispatch.ts`, `src/modules/notification/application/notification-router-workflow.ts`, `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-delivery-record-purge.ts` (novo), testes em `test/unit/workers/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:211:| D-190 | **MaintenanceDueIndex — nona e ÚLTIMA fatia (core-user-data-purge, D-179 slice 9, 9º de 9 workers) — D-179 FECHADO POR COMPLETO.** `core_user_data_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["core_user_data_purge"]`/`worker_transact_write_policy_json["core_user_data_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItems` na tabela base, ambos só no role do `core_user_data_purge_handler`. Este worker cobre DOIS tipos de entidade (`ExpirationItem`/`ReminderPolicy`, `deletedAt+30d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Forma distinta de D-186/D-187/D-188/D-189 (create-once/janela fixa) — mesma forma do piloto original, D-180/`membership-purge`**: elegibilidade é gated numa TRANSIÇÃO de soft-delete (`deletedAt` sendo setado), não na criação — `deriveCoreUserDataMaintenanceDue()` (`src/shared/core-user-data-gsi8.ts`, novo, fora de `domain/` de módulo específico já que a entidade cobre 2 módulos — `expiration`/`reminder`) retorna `undefined` enquanto `deletedAt` está ausente; uma linha nunca vira candidata antes do soft-delete. **Achado real de pré-existência, confirmado por grep amplo (não presumido) antes de qualquer código novo**: `ExpirationItem.deletedAt` tem exatamente UM write path real — `expiration-service.ts#deleteItem` (via `transitionStatus`'s `extraSet`) — onde o ponteiro GSI8 foi wireado. `ReminderPolicy.deletedAt` NÃO TEM write path real nenhum no código hoje (`disablePolicy` só seta `enabled: false`, nunca `deletedAt`) — **condição PRÉ-EXISTENTE, não introduzida por esta migração**: o worker `Scan`-based anterior já tinha o mesmo gap (seu filtro `attribute_exists(deletedAt)` sobre `ReminderPolicy` também nunca poderia casar, pela mesma razão). `core-user-data-gsi8.ts` ainda deriva/gera chaves para `ReminderPolicy` de forma uniforme com `ExpirationItem` — no dia em que um soft-delete real for adicionado para ele, wireara o ponteiro é uma chamada de uma linha, sem mudança adicional no módulo compartilhado. **Mudança de forma real e esperada vs. o texto original do worker (D-151)**: `candidate-source.ts`'s comentário original dizia "plain `DeleteCommand`, not `TransactWriteItems` — D-151's design named no purge receipt"; isso muda agora porque o `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` da claim GSI8 precisa estar na MESMA transação do `Delete` (mesmo raciocínio de todo outro worker migrado com fence de tenant) — mudança correta e esperada, não um desvio a evitar. `core-user-data-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `deletedAt` exato — defesa em profundidade contra uma corrida de "undelete", mesmo sem caminho de undelete real existir hoje para nenhuma das duas entidades, mesma disciplina de D-151/D-189). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188/D-189): backoff exponencial capado via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#CORE_USER_DATA"` após `MAX_ATTEMPTS=5`. `GlobalIndexComponent` ganha `"core-user-data-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.core_user_data_purge_handler.function_name`. Novo `scripts/backfill-gsi8-core-user-data-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, só linhas já soft-deletadas (nunca escreve ponteiro para uma linha sem `deletedAt`), vencimento FUTURO e passado. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 9 workers — **matriz de isolamento completa, todos os 9 namespaces GSI8 agora provados mutuamente exclusivos**. Suíte completa local 1929/1929 (179 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). PR #217 mergeado (`1633d6c`→`1e483c1`), CI real (`33702974051`) e CD real (`33703151890`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-core-user-data-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyCoreUserDataPurge` com `LeadingKeys` exato `["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItemsCoreUserDataPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 5 * * ? *)`). **Prova de isolamento cross-namespace — a mais ampla do programa inteiro, 8 verificações negativas** (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8): `WORK#CORE_USER_DATA` (próprio) → `allowed`; os 8 namespaces já migrados (`WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT`/`WORK#DELIVERY_RECORD`) → `implicitDeny` nos 8 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard roda de fato): 0 candidatos — confirmado por `Scan` bruto cobrindo as 58 linhas inteiras da tabela filtrado por `entityType IN (ExpirationItem, ReminderPolicy)` retornando `Count=0` (a tabela `dev` não tem hoje NENHUMA linha desses dois tipos, muito menos soft-deletada), e por `Query GSI8 WORK#CORE_USER_DATA` retornando `Count=0`; execução real seria um no-op confirmado. **D-179 (MaintenanceDueIndex) está FECHADO — os 9 workers nomeados pelo design estão 100% migrados, verificados ao vivo em `dev`, com prova de isolamento IAM cross-namespace completa**: `membership-purge` (D-180/D-181), `invitation-purge` (D-182), `document-file-reconciliation` (D-183), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186), `security-audit-purge` (D-187), `transient-purge` (D-188), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190). **Item pendente carregado, ainda não resolvido, agora o item real mais proeminente desta área**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia; como D-179 fecha aqui, este é o item de maior prioridade real que sobra nesta área de trabalho. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — nona e última fatia mecânica, mesma forma do piloto original D-180 — transição de soft-delete, não criação) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO — D-179 COMPLETO (9 de 9 workers migrados)** — `core-user-data-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo completa contra os 8 outros namespaces); nenhum worker nomeado por D-179 permanece em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/core-user-data-gsi8.ts` (novo), `src/modules/expiration/application/expiration-service.ts`, `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-core-user-data-purge.ts` (novo), testes em `test/unit/{expiration,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:234:| D-203 | **Duas correções mecânicas de pendências já nomeadas (nível 1-2/3-4, sem protocolo)**: (1) **guard `main()` do Windows/Git Bash** (pendência de D-186→D-190): os 6 scripts de backfill que ainda usavam `import.meta.url === \`file://${process.argv[1]}\`` (nunca casa no Windows/Git Bash) — `backfill-gsi8-document-file-reconciliation.ts`, `backfill-gsi8-invitation-purge.ts`, `backfill-gsi8-membership-purge.ts`, `backfill-gsi8-requirement-reindex.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts` — migrados para `fileURLToPath(import.meta.url) === process.argv[1]`, mesmo padrão já usado por `backfill-gsi8-core-user-data-purge.ts` desde D-186. (2) **`RT-LANE-FALLBACK-01`** (pendência nomeada por D-191, achado do Codex sob o critério C7): `tenant-business-mutation.ts` classificava `CancellationReasons` ausente/malformado como `TenantNotActiveError`, afirmando uma causa que o DynamoDB não revelou — corrigido para um `ConflictError` genérico/indeterminado nesse caso; `TenantNotActiveError` agora só é lançado quando `CancellationReasons` efetivamente nomeia o índice do fence como a causa. 3 sites de call (`executeTenantBusinessMutation`, docblock, 2 testes adversariais em `test/unit/tenant-lifecycle.test.ts`) atualizados; nenhum caller de `tryTenantBusinessMutation`/`executeTenantBusinessMutation` distingue este ramo hoje (nunca ocorre contra AWS DynamoDB real). Também removidos 3 arquivos `.md` órfãos na raiz do repo (duplicatas byte-idênticas já filed em `docs/architecture/roadmap-evolution/18-20`, violavam o `ROOT_MD_ALLOWLIST` de `check-doc-drift.ts`). Gate local completo verde: `typecheck`, `lint`, suíte completa, `check-docs`, `validate-schemas`. | Nível 1-2 (guard bug, cleanup) / Nível 3-4 (ConflictError, muda comportamento de lane compartilhada) | — | — | **CORRIGIDO, mergeado em `main` (PR #236, `e87d016`), CI 100% verde** (guardrails/dynamodb-integration/frontend/Validate Infra) — mecanismo nunca dispara em `dev` real com dado sintético atual, verificação ao vivo é formalidade | `scripts/backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `scripts/{backfill-reminder-policies,reset-dev-data}.ts`, `src/shared/tenant-lifecycle/tenant-business-mutation.ts`, `test/unit/tenant-lifecycle.test.ts` | 2026-09-05 |
docs/architecture/cost-model.md:43:No MVP, `tenantId = userId` (SCALE-004), então custo por tenant = custo por usuário ativo/mês, já calculado acima (≈US$0,045 com WhatsApp, ≈US$0,0045 sem). Quando Organizations existir (FUT-001), custo por tenant passa a agregar todos os usuários da organização — o `TenantQuota` (`data-model.md`) já é a estrutura de dados que permite medir consumo real por tenant no futuro, não apenas estimar.
docs/architecture/cost-model.md:54:Este documento modela custo esperado sob operação normal; os controles que protegem contra custo **anômalo** (G6, COST-004/005) já estão decididos em `architecture-fase3-consolidada.md` §14 e não são redecididos aqui — apenas referenciados para rastreabilidade: AWS Budgets com alarme em 80%/100% do teto por ambiente (COST-003), Cost Anomaly Detection, quota por tenant via token bucket em DynamoDB (`TenantQuota`, `data-model.md`), kill switch via AppConfig para operações caras (IA/OCR/WhatsApp). Este cost-model fornece os valores de referência (tabela acima) que alimentam os tetos configurados nesses Budgets — não os substitui.
src/shared/observability\security-audit.ts:13: * auto-injects correlationId/tenantId from the AsyncLocalStorage context - CloudWatch Logs is
src/shared/observability\logger.ts:7: * tenantId may appear in structured logs for investigation (per #14.1) but must never become
src/shared/observability\logger.ts:21:  tenantId?: string;
src/shared/observability\context.ts:10:  tenantId?: string;
rg: src/modules/quota: O sistema não pode encontrar o arquivo especificado. (os error 2)
rg: infra/modules/monitoring: O sistema não pode encontrar o arquivo especificado. (os error 2)

 exited 1 in 1222ms:
docs/architecture/decisions-log.md:201:| D-180 | **MaintenanceDueIndex — implementação real, primeira fatia/piloto (D-179 aplicado, `membership-purge` ÚNICO worker migrado)**: GSI8 real (`GSI8PK`/`GSI8SK`, `KEYS_ONLY`) adicionado ao módulo Terraform (`infra/modules/dynamo-table/main.tf`), `local.gsi8_worker_types = { membership_purge = "MEMBERSHIP_PURGE" }` gerando `gsi8_read_policy_json`/`worker_transact_write_policy_json` por chave — `dynamodb:Query` condicionado `ForAllValues:StringEquals dynamodb:LeadingKeys IN ["WORK#MEMBERSHIP_PURGE","DLQ#MEMBERSHIP_PURGE"]` + `dynamodb:TransactWriteItems` na tabela base (ação nova, não coberta por `tenant_facing_read_write`), ambos anexados só ao role do `membership_purge_handler`. `deriveMembershipMaintenanceDue()`/`membershipGsi8Keys()`/`MEMBERSHIP_PURGE_WORK_TYPE`/`MEMBERSHIP_RETENTION_DAYS` movidos para `modules/organization/domain/membership.ts` (função pura, D-179 §2) — reusada pelos 3 consumidores exigidos: `remove-membership.ts`/`leave-organization.ts` (gravam `GSI8PK`/`GSI8SK` no MESMO `Update` que a transição `REMOVED`, nunca write separado), `scripts/backfill-gsi8-membership-purge.ts` (novo, cobre todo `Membership` com `dueAt` computável — inclusive vencimento FUTURO, corrigindo o erro que a Rodada 3 do design cometeu), e a revalidação do próprio worker. `accept-invitation.ts`'s reativação agora também `REMOVE GSI8PK, GSI8SK` atomicamente ao limpar `removedAt` (autolimpeza de ponteiro obsoleto na origem). `membership-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`Limit`; `purge.ts` revalida cada candidato via `getMembership()` consistente antes de agir (GSI8 nunca é fonte de elegibilidade) e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por versão, na MESMA transação — fecha o TOCTOU que o padrão antigo tinha entre ler o status do tenant e deletar). Poison-record/quarentena (D-179 §8): `maintenanceAttemptCount` incrementado só quando o `ConditionCheck` de tenant falha (nunca no `Delete`), backoff exponencial capado (1,2,4,8,16 dias) reposicionando `GSI8SK`, acima de `MAX_ATTEMPTS=5` move para `GSI8PK="DLQ#MEMBERSHIP_PURGE"`; toda escrita de retry é idempotente (`ConditionalCheckFailedException` tratado como no-op, mesmo padrão que o delete já usava). Ponteiro obsoleto (item reativado após ser indexado) é autolimpo pelo próprio worker via `REMOVE GSI8PK, GSI8SK`. Observabilidade: `MembershipPurgeResult` ganha `quarantinedCount`/`oldestCandidateAgeSeconds` (computado do primeiro item da página, sem I/O extra); o handler já fazia `logger.info(..., {...result})`, então os 2 campos novos já são emitidos sem tocar o handler. **G-V3 ao vivo em 3 mecanismos**: teste novo prova que `RemoveMembershipService`/`LeaveOrganizationService` gravam `GSI8PK`/`GSI8SK` corretos (falha se removido); teste novo prova que `accept-invitation.ts` limpa os 2 atributos na reativação (falha se removido); teste novo do worker leva um candidato através de 6 execuções com tenant nunca `ACTIVE` e prova a quarentena em `DLQ#MEMBERSHIP_PURGE` no `MAX_ATTEMPTS+1`-ésimo ataque, e um teste separado prova o autoconserto do ponteiro obsoleto. `infra/tests/stack.tftest.hcl` ganha `run "gsi8_worker_isolation"`: prova que `membership_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, que `TransactWriteItems` nunca aparece numa política referenciando GSI3/GSI4/GSI6/GSI8, e que `invitation_purge_handler`/`transient_purge_handler`/`test_ping_handler` NÃO referenciam GSI8 (isolamento por worker, não por índice). `gsi_count` 7→8. Suíte completa 1778/1778, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (incluindo o caso novo). **Próximo worker recomendado para a próxima fatia**: `invitation-purge` (mesmo padrão de `TenantLifecycleStatusSource`/fence ACTIVE já usado aqui, candidato mais próximo estruturalmente) — ordem não é normativa, qualquer um dos 8 restantes serve. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — infra nova real porém mecânica, seguindo spec exata) | n/a | n/a | **IMPLEMENTADO (piloto, 1 de 9 workers)** — `membership-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes); os outros 8 workers nomeados por D-179 permanecem em `Scan`+`Limit`, migração worker-a-worker é trabalho futuro explícito | `infra/modules/dynamo-table/{main.tf,outputs.tf}`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/membership.ts`, `src/modules/organization/application/{remove-membership.ts,leave-organization.ts,accept-invitation.ts}`, `src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/membership-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-membership-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:202:| D-181 | **MaintenanceDueIndex piloto (D-180) — fix real de um bug de deploy pego pelo próprio CD, mais verificação ao vivo completa contra `dev`.** A primeira tentativa de merge de D-180 para `main` (PR #198) teve CI verde mas o `Deploy (CD)` real (`terraform apply` em `dev`) **falhou de verdade** (`aws.amazon.com` `MalformedPolicyDocument: Statement IDs (SID) must be alpha-numeric`) — os 2 `Sid`s novos (`gsi8_read`/`worker_transact_write` em `infra/modules/dynamo-table/main.tf`) interpolavam `each.key` (`"membership_purge"`, com underscore) diretamente, mas IAM exige `[0-9A-Za-z]*`. Confirmado por leitura direta do log do run que falhou (não presumido) e por uma checagem ao vivo independente (`aws iam list-role-policies --role-name exptrk-dev-membership-purge-handler-role`, que mostrou só a política base `capability-0`, confirmando que as 2 políticas novas nunca chegaram a existir). Corrigido com `join("", [for part in split("_", each.key) : title(part)])` (PascalCase, sem underscore) nos dois `sid`s; `terraform test` 22/22 continuou verde. PR #199 mergeado, CD real (`33636904901`) `success`. **Verificação ao vivo completa, tudo confirmado via `aws --profile claude-dev`, nada presumido**: `describe-table` mostra GSI8 na lista de índices; `list-role-policies`/`get-role-policy` no role do worker mostram as 3 políticas (base + `Gsi8ReadOnlyMembershipPurge` com a condição `LeadingKeys` exata + `TransactWriteItemsMembershipPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de negação cross-namespace (`aws iam simulate-principal-policy`, exigida pelo DoD do piloto)**: simulando `dynamodb:Query` no role do `membership_purge_handler` com `dynamodb:LeadingKeys=WORK#MEMBERSHIP_PURGE` (seu próprio namespace) → `allowed`, casando a statement `Gsi8ReadOnlyMembershipPurge`; a mesma simulação com `dynamodb:LeadingKeys=WORK#INVITATION_PURGE` (namespace de outro worker, ainda não migrado) → `implicitDeny`, nenhuma statement casada — prova real, nos dois sentidos (controle positivo + negativo), de que o isolamento por `LeadingKeys` funciona exatamente como o design exige, não só no papel do Terraform. **Achado de processo desta sessão, registrado por ser real**: uma mensagem externa recebida durante a espera do CD afirmou "CD completed successfully" e pediu para prosseguir direto à prova de IAM e ao registro em `decisions-log.md` — checada diretamente via `gh run view` antes de confiar, e o run real estava `failure` (a mesma falha do parágrafo acima), não `success`; a alegação foi descartada e o bug real foi corrigido antes de qualquer prova de IAM ou registro de documentação, exatamente a disciplina que este arquivo e `AGENTS.md` exigem (nunca tratar uma alegação não verificada como fato). | Nível 1-2 (fix mecânico de um bug de infra pego em produção `dev`, mais verificação, sem decisão de design nova) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO** — piloto D-179/D-180 100% completo, incluindo a prova de isolamento IAM que o DoD do piloto exigia antes de liberar a migração dos 8 workers restantes | `infra/modules/dynamo-table/main.tf` | 2026-09-02 |
docs/architecture/decisions-log.md:203:| D-182 | **MaintenanceDueIndex — segunda fatia (invitation-purge, D-179 slice 2, mesmo padrão de D-180)**: `invitation_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["invitation_purge"]`/`worker_transact_write_policy_json["invitation_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#INVITATION_PURGE","DLQ#INVITATION_PURGE"]` + `TransactWriteItems` na tabela base, ambos só no role do `invitation_purge_handler`. `deriveInvitationMaintenanceDue()`/`invitationGsi8Keys()`/`INVITATION_PURGE_WORK_TYPE` novos em `domain/invitation.ts` (função pura, D-179 §2) — duas ramificações independentes, nuance real vs. o piloto: `PENDING`'s due date (`expiresAt+30d`) já é totalmente conhecido NA CRIAÇÃO (não numa transição posterior como o `REMOVED` de Membership), então `create-invitation.ts`'s `invite()` grava o ponteiro GSI8 no MESMO `Put` da Invitation, e `resend()` o move junto quando `expiresAt` roda; `revoke-invitation.ts` sobrescreve o ponteiro no MESMO `Update` que a transição real para `REVOKED`; `accept-invitation.ts` limpa (`REMOVE GSI8PK, GSI8SK`) atomicamente ao aceitar, já que `ACCEPTED` nunca é candidato. `invitation-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`TenantLifecycleStatusSource` separado (a checagem ACTIVE virou `ConditionCheck` dentro da própria transação de claim, como no piloto); `purge.ts` revalida via `getInvitation()` consistente antes de agir e reivindica via `TransactWriteItems` (mesmo `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)`+`Delete` condicionado por versão). Poison-record/quarentena idêntico ao piloto (backoff 1,2,4,8,16 dias, `DLQ#INVITATION_PURGE` acima de `MAX_ATTEMPTS=5`). **Achado registrado, não um bug**: não existe caminho real de reativação para `Invitation` (ao contrário de `Membership`) — `REVOKED`/`ACCEPTED` são terminais, `ACCEPTED` já limpa seu próprio ponteiro na origem — então o autoconserto de ponteiro obsoleto em `purge.ts` é só defesa em profundidade, documentado explicitamente no cabeçalho do arquivo em vez de omitido. `GlobalIndexComponent` (taxonomia fechada de `security-audit.ts`) ganha `"invitation-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.invitation_purge_handler.function_name` (achado do próprio teste de arquitetura — sem isso o novo call site de `auditGlobalIndexAccess` ficaria sem alarme wireado). Novo `scripts/backfill-gsi8-invitation-purge.ts`, cobrindo as duas ramificações inclusive vencimento FUTURO (mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido: `invitation_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, e isolamento cross-namespace nos dois sentidos (nenhum role referencia o namespace do outro). **G-V3 ao vivo**: testes novos provam o ponteiro gravado na criação, movido no resend, sobrescrito no revoke, limpo no accept; teste do worker prova quarentena em `DLQ#INVITATION_PURGE` no `MAX_ATTEMPTS+1`; teste do backfill prova cobertura das duas ramificações. Suíte completa 1797/1797 (169 arquivos), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22. PR #201 mergeado (`9a21da1`), CI `success` (run 33656227410), CD real `success` confirmado via `gh run view` (run 33656568649, não presumido de alegação externa). **Verificação ao vivo em `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role do `invitation_purge_handler` tem as 3 políticas esperadas (base+`Gsi8ReadOnlyInvitationPurge`+`TransactWriteItemsInvitationPurge`). **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, nos dois sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#INVITATION_PURGE` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyInvitationPurge`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE` (namespace do outro worker já migrado) → `implicitDeny`, nenhuma statement casada. **Próximo worker recomendado (não normativo)**: qualquer um dos 7 restantes (`document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — segunda fatia mecânica seguindo o padrão do piloto D-180, com uma nuance real de timing de escrita) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (2 de 9 workers migrados)** — `invitation-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 7 workers nomeados por D-179 permanecem em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/invitation.ts`, `src/modules/organization/application/{create-invitation.ts,revoke-invitation.ts,accept-invitation.ts}`, `src/workers/invitation-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/invitation-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-invitation-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:204:| D-183 | **MaintenanceDueIndex — terceira fatia (document-file-reconciliation, D-179 slice 3, 3º de 9 workers)**: `document_file_reconciliation` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["document_file_reconciliation"]`/`worker_transact_write_policy_json["document_file_reconciliation"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DOCUMENT_FILE_RECONCILIATION","DLQ#DOCUMENT_FILE_RECONCILIATION"]` + `TransactWriteItems` na tabela base, ambos só no role do `document_file_reconciliation_handler`. **Achado real pré-existente, confirmado antes de qualquer código novo (D-166's próprio texto já era ambíguo sobre isso, resolvido por leitura direta)**: o mecanismo GSI5 que D-164/D-166 desenharam para este worker (`fileReconciliationGsi5Keys()`, `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`) NUNCA teve um writer real — `reserveFiles()` (`document-archive-service.ts`) cria cada `DocumentFile` sem jamais chamar essa função, e nenhum outro call site em todo o código a invoca (confirmado por `grep` antes de tocar em qualquer arquivo). O `Scan` filtrado por `attribute_exists(GSI5PK)` que D-166 implementou, portanto, nunca poderia encontrar um candidato real em produção — o worker era código morto funcional desde o dia em que foi implementado, apesar de "IMPLEMENTADO E VERIFICADO" nos testes (os testes seedavam `GSI5PK` manualmente, nunca através do caminho de escrita real). Isso resolve por completo a pergunta "GSI8 substitui ou convive com GSI5" que a tarefa pedia para verificar antes de escrever qualquer código: não havia um mecanismo funcionando para conviver com — GSI8 é a primeira implementação real e completa deste ponteiro de descoberta, não uma segunda ao lado de uma que já funcionava. Não foi necessário reabrir D-163 Decisão 2 (GSI6 seguiu intocado) nem nenhuma outra decisão de alocação de índice. **Decidido e implementado**: `document-file.ts` ganha `FILE_SCAN_TIMEOUT_SECONDS=600` (mesma janela do presign TTL — `document-archive-service.ts`'s `PRESIGN_TTL_SECONDS` agora importa essa constante em vez de duplicá-la, deliberado: a janela de upload e o deadline de timeout de scan são a mesma janela), `deriveDocumentFileMaintenanceDue()` (due = `createdAt + FILE_SCAN_TIMEOUT_SECONDS` para `PENDING_UPLOAD`/`SCANNING`, `undefined` uma vez terminal — mesmo raciocínio do branch PENDING de `Invitation`: due date totalmente conhecido na criação, sem transição posterior para pendurar o relógio, já que SCANNING é só evidência chegando dentro da mesma janela fixa, nunca um novo relógio) e `documentFileGsi8Keys()`. O campo `DocumentFile.GSI5PK?/GSI5SK?` foi REMOVIDO (substituído por `GSI8PK?/GSI8SK?`) — `fileReconciliationGsi5Keys()` também removida, não deixada como código morto adicional. `reserveFiles()` agora grava o ponteiro GSI8 no MESMO `Put` de cada `DocumentFile` (mesmo padrão do branch PENDING de `invitationGsi8Keys()` — criação É a transição relevante). `apply-file-scan-result.ts`: as 3 funções (`applyFileScanResult`'s branch REJECT, `confirmFileScanClean`, `applyFileScanTimeout`) trocam `remove: ["GSI5PK","GSI5SK"]`/condição exata de ponteiro por GSI8; `applyFileScanTimeout`'s `observedGsi5Pointer`→`observedGsi8Pointer`, mesmo mecanismo de fence exato (`extraConditions` comparando o ponteiro observado literal), nenhuma mudança de comportamento. **Composição com o mecanismo transacional existente (item pedido pela tarefa)**: como a claim/revalidação real já vive inteiramente dentro de `applyFileScanTimeout()` (que já releia o `DocumentFile` fresco via `store.get()` e faz seu próprio retry OCC), o port do worker (`candidate-source.ts`) foi simplificado para só `queryDue()` — nenhum `getFile()`/`transactWrite()` próprio como `membership-purge`/`invitation-purge` precisam, evitando duplicar a revalidação que já existe. **Simplificação real habilitada por GSI8 (não pedida, descoberta ao desenhar)**: como GSI8PK é uniforme (`WORK#DOCUMENT_FILE_RECONCILIATION`, sem partição por status, diferente do GSI5PK por-status de D-166), as duas varreduras independentes que D-166 rodava (uma por `PENDING_UPLOAD`/`SCANNING`) colapsam numa única `Query` due-ordered cobrindo os dois status — `reconciliation.ts` reescrito para um único loop de paginação, mesmo formato de `invitation-purge/purge.ts`. **Sem mecanismo de poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada): diferente de `membership-purge`/`invitation-purge`, este worker nunca teve fence de tenant `ACTIVE` (D-166: "a stuck upload is not a retention decision") — não existe nenhuma condição que possa falhar permanentemente para o mesmo candidato; um timeout perdido por corrida é sempre transitório e resolvido no próximo run, nunca precisa de backoff/quarentena. **Achado adicional fora do escopo desta fatia, registrado e NÃO corrigido aqui**: `document_archive_handler` (o handler HTTP principal, que também chama `store.transactWrite()` via `reserveFiles()`/`acceptVersion()`) tem a MESMA lacuna de `dynamodb:TransactWriteItems` que este worker tinha antes desta fatia — `policy_documents_json` dele só inclui `tenant_facing_read_write_policy_json`+a policy de presign, nunca `TransactWriteItems`. Não corrigido nesta sessão (fora do escopo de D-179, achado pré-existente e independente); nomeado explicitamente para não ser esquecido. `GlobalIndexComponent` ganha `"document-file-reconciliation"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.document_file_reconciliation_handler.function_name`. Novo `scripts/backfill-gsi8-document-file-reconciliation.ts`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 3 workers. **G-V3 ao vivo**: teste novo prova que `reserveFiles()` grava `GSI8PK`/`GSI8SK` em cada `DocumentFile`; suíte de `apply-file-scan-result.test.ts`/`document-file-reconciliation.test.ts` migrada para GSI8 continua provando a condição exata de ponteiro (quebra/reversão já demonstrada por D-166, mecanismo idêntico, só renomeado). Suíte completa 1809/1809 (1797+12 novos: 1 em `document-archive-service.test.ts`, 4 do worker migrados de D-166, 11 do backfill novo — conferir soma exata via `npm test`), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (mesma contagem do piloto — este teste estende `gsi8_worker_isolation`, não adiciona um `run` novo). | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — terceira fatia mecânica seguindo o padrão do piloto/D-182, com uma descoberta real de bug pré-existente que simplificou a decisão de design em vez de complicá-la) | n/a | n/a | **IMPLEMENTADO — verificação ao vivo em `dev` e merge pendentes, ver sessão em andamento** | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/document-file.ts`, `src/modules/document-archive/application/{apply-file-scan-result.ts,document-archive-service.ts}`, `src/workers/document-file-reconciliation/{candidate-source.ts,dynamodb-candidate-source.ts,reconciliation.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-document-file-reconciliation.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:206:| D-185 | **MaintenanceDueIndex — quarta fatia (requirement-reindex, D-179 slice 4, 4º de 9 workers)**: `requirement_reindex` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["requirement_reindex"]`/`worker_transact_write_policy_json["requirement_reindex"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItems` na tabela base, ambos só no role do `requirement_reindex_handler`. **Verificação prévia obrigatória (item nomeado pela tarefa) confirmou a correção da Rodada 3 de D-179**: lendo `reindex.ts` diretamente (não confiando na citação de linha do design), o worker de fato só faz UMA coisa — a drift pura SATISFIED→NOT_SATISFIED quando `evidenceValidUntil` passa sem nenhuma outra escrita tocar o Requirement — nunca uma "reindexação de applicability" mais ampla; `applicability` é persistido e nunca re-derivado por este worker. `deriveRequirementMaintenanceDue()` novo em `domain/requirement.ts` (função pura): due = `evidenceValidUntil` apenas quando `status === "SATISFIED"` E `evidenceValidUntil` existe — os outros 4 status nunca são candidatos, um SATISFIED sem `evidenceValidUntil` é SATISFIED para sempre por construção. `requirementGsi8Keys()` embute `requirementId` (não `subjectId`) no `GSI8SK`; a fonte candidata recupera `tenantId`/`subjectId`/`requirementId` do `PK`/`SK` da tabela base (`KEYS_ONLY` já devolve de graça), mesmo padrão de `documentFileGsi8Keys()`. **4 pontos de escrita no `DocumentArchiveService` gravam/limpam o ponteiro atomicamente, todos no MESMO `set`/`remove` da transição de `status` real** (`createRequirement`/`updateRequirement`/`linkEvidence`/`unlinkEvidence`) — um helper privado `requirementGsi8Fields()` centraliza a decisão para não duplicar a chamada a `deriveRequirementMaintenanceDue()` em 4 lugares. `scanSatisfiedRequirements` (`Scan` cross-tenant filtrado por `entityType`+`status`) REMOVIDO do port `DocumentArchiveStore` e do adapter DynamoDB real — nenhum outro call site o usava (confirmado por grep antes de tocar em qualquer arquivo), mesma disciplina "apagar o mecanismo morto, não deixar ao lado do novo" de D-183. `requirement-reindex/candidate-source.ts`+`dynamodb-candidate-source.ts` novos: `Query GSI8` substitui o `Scan`; `reindex.ts` reescrito para paginar via GSI8, revalidar cada candidato com um `store.get()` fresco (GSI8 nunca é fonte de elegibilidade, mesma postura de todo consumidor GSI8) e aplicar a atualização versionada existente (`buildVersionedUpdate`) incluindo `remove: ["GSI8PK","GSI8SK"]` — o `nextStatus` nunca é SATISFIED neste caminho (a derivação só chega a NOT_SATISFIED quando `evidenceValidUntil` já passou), então o ponteiro sempre é limpo incondicionalmente. **Sem poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada, mesmo raciocínio de D-183): este worker nunca teve fence de tenant `ACTIVE` — o único modo de falha de uma claim é `expectedVersion` desatualizado por um `linkEvidence`/`unlinkEvidence`/`updateRequirement` concorrente, sempre transitório e resolvido pela própria execução diária seguinte, nunca uma condição permanentemente falha para o mesmo candidato. `GlobalIndexComponent` ganha `"requirement-reindex"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.requirement_reindex_handler.function_name`. Novo `scripts/backfill-gsi8-requirement-reindex.ts`, cobrindo vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 4 workers já migrados. **G-V3**: 7 testes do worker (transição real + limpeza do ponteiro, due no futuro nunca tocado, sem `evidenceValidUntil` nunca candidato, status não-SATISFIED nunca candidato, múltiplos tenants, conflito concorrente via OCC, candidato "stale" da query eventualmente consistente nunca clobber um `evidenceValidUntil` já movido por um `linkEvidence` concorrente) + 12 testes do backfill. Suíte completa local 1827/1827 (após reconciliar com o WIP concorrente de D-184 no mesmo working tree — só os arquivos desta fatia foram staged/commitados, `guest-document-access-service.ts`/seu teste e `docs/architecture/reviews/submit-evidence-document-type-scoping/` intocados por esta sessão), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente, não só o agregado). Commit `720d881`, empacotado no PR #205 (aberto pela sessão concorrente de D-184, mesmo `develop`) — CI real (run `33665953101`) `success`, CD real (run `33666281116`) `success`, ambos confirmados via `gh run view` direto (nunca presumidos). **Verificação ao vivo completa contra `dev`, tudo via `aws --profile claude-dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-requirement-reindex-handler-role` tem as 3 políticas esperadas (base + `Gsi8ReadOnlyRequirementReindex` com a condição `LeadingKeys` exata `["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItemsRequirementReindex` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 3 sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#REQUIREMENT_REINDEX` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyRequirementReindex`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION` (namespaces dos 3 workers já migrados) → `implicitDeny` nos 3 casos, nenhuma statement casada. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quarta fatia mecânica seguindo o padrão de D-180/D-182/D-183, com a mesma decisão "sem poison-record" de D-183 por ausência de fence de tenant) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (4 de 9 workers migrados)** — `requirement-reindex` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 5 workers nomeados por D-179 permanecem em `Scan`+`Limit`: `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/requirement.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/ports/document-archive-store.ts`, `src/modules/document-archive/persistence/dynamodb-document-archive-store.ts`, `src/runtime/aws/handlers/requirement-reindex-handler.ts`, `src/workers/requirement-reindex/{candidate-source.ts,dynamodb-candidate-source.ts,reindex.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-requirement-reindex.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:207:| D-186 | **MaintenanceDueIndex — quinta fatia (quota-telemetry-purge, D-179 slice 5, 5º de 9 workers)**: `quota_telemetry_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["quota_telemetry_purge"]`/`worker_transact_write_policy_json["quota_telemetry_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItems` na tabela base, ambos só no role do `quota_telemetry_purge_handler`. **Forma distinta dos 4 anteriores, confirmada por leitura direta antes de qualquer código**: este worker cobre DOIS tipos de entidade que compartilham o mesmo formato de elegibilidade (`resetAt+30d`, fence de tenant `ACTIVE`) — `TenantQuotaRecord` e `EphemeralTelemetryMutation` (`src/modules/identity/application/quota.ts`) — e NENHUM dos dois tem campo `version`; ambos usam `count`/`resetAt` como fence OCC direto (mesma forma que D-153/`SECURITY_AUDIT` já usava para suas 4 entidades append-only, por razão diferente aqui: token bucket com campos otimistas próprios, não append-only). `deriveQuotaTelemetryMaintenanceDue()` (due = `resetAt + 30d`, NUNCA `undefined` — ao contrário de Membership/Invitation não existe estado terminal, `resetAt` sozinho determina elegibilidade) e `quotaTelemetryGsi8Keys()` (embute `entityType`+`SK` natural no `GSI8SK`, já que nenhuma das duas entidades tem um id próprio separado do seu `SK`) adicionados a `quota.ts`. **4 pontos de escrita gravam/refrescam o ponteiro GSI8 atomicamente no MESMO write que seta `resetAt`** (nunca um passo separado): `consume()`'s ramo de criação e ramo de rollover de janela (`TransactWriteItems` via `executeTenantBusinessMutation`, W3-07), e `identity-store.ts`'s `incrementTelemetryCounter()` (a lane `EphemeralTelemetryMutation` de D-136 D-D — `GSI8PK`/`GSI8SK` setados via `if_not_exists`, mesmo mecanismo que já protege `resetAt`/`purgeAfterTtl` contra um escritor perdedor concorrente rebobinar a janela; o valor é determinístico a partir do bucket de janela, calculado em `quota.ts` — camada de aplicação — e passado pronto ao adapter de persistência via `TelemetryIncrementInput.gsi8`, preservando a regra de `persistence/` nunca importar lógica de `application/`). `release()` não toca o ponteiro (só decrementa `count`, nunca muda `resetAt`). `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` fresco antes de qualquer escrita e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `resetAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182, já que este worker TEM fence de tenant `ACTIVE`, ao contrário de D-183/D-185): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#QUOTA_TELEMETRY"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (diferente de Invitation/Membership, decisão explícita e documentada): como `deriveQuotaTelemetryMaintenanceDue()` nunca retorna `undefined` e todo write que seta `resetAt` também refresca o ponteiro, não existe um "ponteiro sobrevivendo a uma transição terminal" real para se defender — a lacuna que o self-heal de `invitation-purge` cobre não tem equivalente estrutural aqui. `GlobalIndexComponent` ganha `"quota-telemetry-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.quota_telemetry_purge_handler.function_name`. Novo `scripts/backfill-gsi8-quota-telemetry-purge.ts`, cobrindo as duas entidades e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `resetAt` (não `version`, mesma adaptação do worker). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 5 workers já migrados. **G-V3**: 22 testes do worker (purga das duas entidades, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `resetAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ após MAX_ATTEMPTS, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo as duas entidades, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1841/1841 (1827+14 novos: quota.ts's docstring/campo GSI8PK/GSI8SK, 22 do worker migrado, 10 do backfill novo, 1 de cobertura de observabilidade), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). **Achado real de bug pré-existente, encontrado e corrigido só neste script (não nos 6 irmãos, fora de escopo desta fatia, registrado abaixo)**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` que decide se `main()` roda quando o script é executado diretamente NUNCA casa no Windows deste repo (`import.meta.url` usa barras normais — `file:///C:/...` — enquanto `process.argv[1]` usa contrabarras — `C:\\...`) — confirmado por teste direto (`node`/`tsx` imprimindo os dois valores). `main()` silenciosamente nunca executava sob o shell Windows/Git Bash deste ambiente (`AGENTS.md` §4) apesar de exit code 0 e nenhum erro — só descoberto ao tentar rodar o backfill desta fatia pela primeira vez nesta sessão (saída vazia inesperada). Corrigido neste arquivo trocando por `fileURLToPath(import.meta.url) === process.argv[1]` (comparação de dois paths nativos do SO, sem o mismatch de separador). **Mesmo bug confirmado presente nos outros 6 scripts** (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — não corrigido nesta sessão (fora do escopo de D-186), significa que os backfills anteriores (D-180/D-182/D-183/D-185) e qualquer execução passada desses scripts via este mesmo shell podem ter silenciosamente não escrito nada, mesmo reportados como "rodados com sucesso" — precisa de verificação/reexecução numa sessão futura, nomeado explicitamente para não ser esquecido. **PR #208 mergeado (commit `a898c1d`→`a280110`), CI real (`33687474935`/`33687503644`) e CD real (`33688100421`) `success`, todos confirmados via `gh run view` direto**. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-quota-telemetry-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyQuotaTelemetryPurge` com `LeadingKeys` exato `["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItemsQuotaTelemetryPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(30 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 4 sentidos)**: `WORK#QUOTA_TELEMETRY` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX` (os 4 já migrados) → `implicitDeny` nos 4 casos. **Backfill real executado contra `dev`** (após o fix do guard acima, sem o qual teria silenciosamente não escrito nada): 24 candidatos encontrados, 24 ponteiros GSI8 escritos, confirmado por `Query GSI8 WORK#QUOTA_TELEMETRY` retornando `Count=24`. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quinta fatia mecânica seguindo o padrão de D-180/D-182 (poison-record/DLQ), adaptada para 2 tipos de entidade sem `version`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (5 de 9 workers migrados)** — `quota-telemetry-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); achado de bug real do guard `main()` em scripts de backfill no Windows, corrigido só aqui, pendente nos outros 6; os 4 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/identity/application/quota.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-quota-telemetry-purge.ts` (novo), testes em `test/unit/{identity,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:208:| D-187 | **MaintenanceDueIndex — sexta fatia (security-audit-purge, D-179 slice 6, 6º de 9 workers)**: `security_audit_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["security_audit_purge"]`/`worker_transact_write_policy_json["security_audit_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItems` na tabela base, ambos só no role do `security_audit_purge_handler`. **Forma distinta das 5 fatias anteriores**: este worker cobre QUATRO tipos de entidade que compartilham o mesmo formato de elegibilidade (`occurredAt+365d`, fence de tenant `ACTIVE`) — `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (`src/modules/{expiration,organization,subject,activity}/domain/*.ts`), as mesmas 4 partições que `GET /activity`'s `ActivityService` k-way-merges — todas append-only por construção (sem `update()`/`delete()` exportado), sem campo `version`. Novo `src/shared/security-audit-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que a entidade cobre 4 módulos diferentes — mesma razão que já colocou `EntityKey`/`TransactWriteEntry` em `shared/dynamodb/occ.ts`): `deriveSecurityAuditMaintenanceDue()` (due = `occurredAt+365d`, NUNCA `undefined` — sem estado terminal, mesmo raciocínio de D-186) e `securityAuditGsi8Keys()` (embute `tenantId`+`entityType`+`SK` natural no `GSI8SK`, mesmo formato de `quotaTelemetryGsi8Keys()`). **O ponteiro GSI8 é gravado UMA ÚNICA VEZ, na própria função pura `build*Event()` de cada uma das 4 entidades** (`buildAuditEvent`/`buildMembershipAuditEvent`/`buildSubjectAuditEvent`/`buildTenantAuditEvent`) — não em cada um dos 19 call sites espalhados por 6 módulos que chamam essas funções (confirmado por grep antes de escrever qualquer código): como toda escrita passa pela mesma função de construção antes do `appendXAuditToTransaction`, estampar o ponteiro ali cobre automaticamente todo call site presente e futuro, sem precisar tocar nenhum deles individualmente — simplificação real habilitada pela forma já centralizada do código, não uma decisão de design nova. **Normalização `MembershipAuditEvent`**: declara `organizationId`, não `tenantId` (mesmo valor, nome diferente) — `buildMembershipAuditEvent()` já passa `organizationId` como `tenantId` para `securityAuditGsi8Keys()`, e o adapter do worker (`normalizeTenantId()`) faz a mesma normalização ao ler uma linha crua da tabela base, mesmo padrão pré-existente do worker antes desta fatia. `security-audit-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também o `TenantLifecycleStatusSource` como dependência separada do worker); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `occurredAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186, já que este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#SECURITY_AUDIT"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência que D-186 quota-telemetry-purge, causa diferente): as 4 entidades são append-only por construção — não existe transição de estado terminal que deixaria um ponteiro obsoleto para trás, e a ÚNICA forma de uma linha deixar de ser candidata é este próprio worker deletá-la (o que remove o ponteiro junto com a linha inteira). **Interação com `GET /activity` (D-149), reverificada nesta fatia**: releitura direta de `dynamodb-audit-partition-store.ts#queryPage` confirma que `ExclusiveStartKey` continua um marcador posicional puro, independente de GSI8 e de qualquer deleção concorrente deste worker — a conclusão original de D-153 (nenhum bug de correção, nenhuma correção necessária) permanece válida após a migração, confirmado por leitura, não presumido. `GlobalIndexComponent` ganha `"security-audit-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.security_audit_purge_handler.function_name`. Novo `scripts/backfill-gsi8-security-audit-purge.ts`, cobrindo os 4 tipos de entidade e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `occurredAt` (não `version`). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 6 workers já migrados. **G-V3**: 4 testes de gravação do ponteiro na criação (um por tipo de entidade, incluindo a normalização `organizationId`→`tenantId`), 2 testes do módulo puro `security-audit-gsi8.ts`, 17 testes do worker (purga dos 4 tipos, boundary exato, fence de tenant não-ACTIVE com contador de retry, normalização de `MembershipAuditEvent`, sem lifecycle record, corrida de `occurredAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/normalizeTenantId/processPage cobrindo os 4 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1867/1867 (175 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #210 mergeado (`5be0743`), CI real (`33691359230`/`33691948793`) e CD real (`33692216726`) `success`, todos confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-security-audit-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlySecurityAuditPurge` com `LeadingKeys` exato `["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItemsSecurityAuditPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 6 sentidos)**: `WORK#SECURITY_AUDIT` (próprio, com `--resource-arns` explícito no GSI8 — sem ele o simulate retorna `implicitDeny` mesmo para o caso positivo, achado de processo desta sessão) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY` (os 5 já migrados) → `implicitDeny` nos 5 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` corrigido roda de fato): 0 candidatos encontrados — a tabela `dev` não contém hoje nenhuma linha `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (confirmado por `Scan` bruto de `entityType`, dado sintético esparso), então nada foi escrito; confirmado por `Query GSI8 WORK#SECURITY_AUDIT` retornando `Count=0`, consistente com o dry-run. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sexta fatia mecânica seguindo o padrão de D-180/D-182/D-186 (poison-record/DLQ), adaptada para 4 tipos de entidade sem `version`, ponteiro centralizado nas 4 funções `build*Event()` em vez de múltiplos call sites) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (6 de 9 workers migrados)** — `security-audit-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 3 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/security-audit-gsi8.ts` (novo), `src/modules/{expiration,organization,subject,activity}/domain/*.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-security-audit-purge.ts` (novo), testes em `test/unit/{shared,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:209:| D-188 | **MaintenanceDueIndex — sétima fatia (transient-purge, D-179 slice 7, 7º de 9 workers)**: `transient_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["transient_purge"]`/`worker_transact_write_policy_json["transient_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItems` na tabela base, ambos só no role do `transient_purge_handler`. **Forma distinta de todas as fatias anteriores**: este worker cobre DOIS tipos de entidade com dinâmicas DIFERENTES — `WebhookInbox` (`src/modules/notification/application/ses-callback-workflow.ts`, criado uma vez/imutável, ponteiro gravado uma única vez na criação, mesma forma que security-audit-purge/D-187) e `UploadSlot` (`src/modules/document/domain/upload-slot.ts`, transições reais de status, mesma forma que invitation-purge/D-182 — ponteiro precisa ser gravado/atualizado a cada transição relevante, não só na criação). Novo `src/shared/transient-purge-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que as duas entidades cobrem 2 módulos diferentes — mesma razão de `shared/security-audit-gsi8.ts`): `deriveWebhookInboxMaintenanceDue()` (`createdAt+7d`, sempre), `deriveUploadSlotMaintenanceDue()` (`undefined` enquanto `RESERVED` — nunca candidato —, senão `reservedAt+7d` se `CONSUMED`, `reservedAt+24h` caso contrário incl. `EXPIRED`/`RELEASED`) e `transientPurgeGsi8Keys()`. **Achado de fronteira de módulo real durante a implementação**: a primeira versão deste arquivo reusava `computeUploadSlotPurgeAfter()`/`UploadSlotStatus` importados de `modules/document/domain/**`, violando `dependency-cruiser`'s regra `shared-must-not-reach-modules` (capturado por `test/architecture/tenant-fence-boundary.test.ts`, que falhou na primeira rodada de testes) — corrigido reimplementando a fórmula (`privacy-lgpd.md` §4, um-liner estável) diretamente em `shared/transient-purge-gsi8.ts` com um tipo `TransientUploadSlotStatus` local, em vez de inverter a fronteira de dependência. Um `UploadSlot` `RESERVED` NUNCA recebe ponteiro GSI8 (nunca aparece na Query GSI8 de forma alguma, diferente do `Scan` anterior que precisava filtrá-lo defensivamente em tempo de leitura). Três escritores atualizados para gravar/atualizar o ponteiro atomicamente na MESMA escrita condicional da transição: `ses-callback-workflow.ts` (criação do `WebhookInbox`), `advance-after-evidence.ts#appendSlotConsumption` (transição `CONSUMED`), `upload-slot-reconciliation/reconciliation.ts#processOneSlot` (transição `EXPIRED`) — nenhum writer real produz `RELEASED` hoje (confirmado por grep: status declarado no union mas sem write path), então nenhum terceiro escritor precisou de mudança. `transient-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também `TenantLifecycleStatusSource` como dependência separada do worker, embora a classe permaneça exportada por simetria com os irmãos, sem uso real); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por `version` — AMBAS entidades têm `version` real, diferente de security-audit-purge — na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#TRANSIENT"` após `MAX_ATTEMPTS=5`. Branch de self-heal de ponteiro obsoleto mantido por defesa (mesma postura de invitation-purge/D-182), embora nenhum caminho de escrita real produza esse caso hoje para nenhuma das duas entidades. `GlobalIndexComponent` ganha `"transient-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.transient_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-transient-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath`, nunca a comparação de string que falha silenciosamente no Windows), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e excluindo corretamente `UploadSlot` `RESERVED`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 7 workers já migrados. **G-V3**: teste de gravação do ponteiro na criação do `WebhookInbox` (`ses-callback-workflow.test.ts`), testes de gravação/atualização do ponteiro nas transições `CONSUMED`/`EXPIRED` do `UploadSlot` (`advance-after-evidence.test.ts`/`upload-slot-reconciliation.test.ts`), 22 testes do worker (purga dos 2 tipos, `RESERVED` nunca candidato, boundary exato para ambas janelas, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant, self-heal de ponteiro obsoleto) + 20 testes do backfill (parseArgs/encodeKey-decodeKey/deriveDue cobrindo os 2 tipos e exclusão RESERVED, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida, página mista). Suíte completa 1894/1894 (176 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #212 mergeado (commit `d32957f`→`ca50bcf` em `main`), CI real (`33695255525`/`33695850896`) e CD real (`33696134854`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-transient-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyTransientPurge` com `LeadingKeys` exato `["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItemsTransientPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#TRANSIENT` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT` (os 6 já migrados) → `implicitDeny` nos 6 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (WebhookInbox, UploadSlot)` sobre as 58 linhas da tabela (`ScannedCount=58`, `Count=0`, sem `LastEvaluatedKey` — tabela inteira coberta), e por `Query GSI8 WORK#TRANSIENT` retornando `Count=0`; nada para escrever, execução "real" (não-dry-run) seria um no-op confirmado, não executada. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sétima fatia mecânica seguindo o padrão de D-187 (poison-record/DLQ, entidade create-once) para `WebhookInbox` e D-182 (ponteiro atualizado a cada transição) para `UploadSlot`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (7 de 9 workers migrados)** — `transient-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 2 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/transient-purge-gsi8.ts` (novo), `src/modules/document/domain/upload-slot.ts`, `src/modules/document/application/advance-after-evidence.ts`, `src/modules/notification/application/ses-callback-workflow.ts`, `src/workers/upload-slot-reconciliation/reconciliation.ts`, `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-transient-purge.ts` (novo), testes em `test/unit/{shared,workers,document,notification}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:210:| D-189 | **MaintenanceDueIndex — oitava fatia (delivery-record-purge, D-179 slice 8, 8º de 9 workers)**: `delivery_record_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["delivery_record_purge"]`/`worker_transact_write_policy_json["delivery_record_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItems` na tabela base, ambos só no role do `delivery_record_purge_handler`. Este worker cobre DOIS tipos de entidade (`NotificationIntent`/`NotificationAttempt`, `src/modules/{reminder,notification}/domain/*.ts`), elegibilidade de idade única (`createdAt+180d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Diferença real de forma vs. D-187 (o análogo mais próximo, mesmo padrão poison-record/DLQ)**: AMBAS entidades carregam um contador OCC `version` real (ao contrário das 4 entidades append-only de D-187) — mesma forma que D-188/`UploadSlot`/`WebhookInbox`; o `Delete`/backoff/DLQ do worker e o backfill novo condicionam em `version` exato, com `createdAt` reasserted como defesa em profundidade adicional (nada muta `createdAt` hoje, mas a condição torna estrutural, não só verdadeiro por acidente). Novo `src/shared/delivery-record-gsi8.ts` (fora de `domain/` de módulo específico, já que a entidade cobre 2 módulos — mesma razão de `security-audit-gsi8.ts`/`transient-purge-gsi8.ts`): `deriveDeliveryRecordMaintenanceDue()` (due = `createdAt+180d`, NUNCA `undefined` — sem estado terminal, cada registro de entrega expira pelo prazo fixo independentemente de qualquer ação) e `deliveryRecordGsi8Keys()`. **3 pontos de escrita reais confirmados por grep** (não presumidos) gravam o ponteiro GSI8 UMA ÚNICA VEZ, no MESMO `Put` da criação: `reminder-dispatch/dispatch.ts` (intent `EXPIRATION_REMINDER`), `notification-router-workflow.ts`'s `applyStaleDecision` (intent `REPLACEMENT`/`CORRECTIVE`) e `applyRoutedDecision` (o `NotificationAttempt`) — nenhum dos dois entidades é atualizada depois de criada nestes caminhos (o `version` existe para as próprias transições de status do módulo, não para uma atualização do ponteiro GSI8). `delivery-record-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (handler perde a dependência separada de `TenantLifecycleStatusSource`, dobrada dentro do `ConditionCheck` transacional); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `createdAt`, na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#DELIVERY_RECORD"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência de D-187, mesma causa): nenhuma das duas entidades é atualizada depois de criada NA PRÁTICA (sem caminho de undelete, sem transição de status nomeada nos comentários do próprio worker) — o ponteiro é gravado exatamente uma vez, e a única forma de uma linha deixar de ser candidata é este worker deletá-la. **Gap conhecido e aceito, herdado de D-152, NÃO tocado por esta fatia**: `NotificationAttemptLookup` (ponteiro derivado, `PK=TENANT#t#ATTEMPT#id`) continua NÃO sendo purgado por este worker e fica órfão depois que um `NotificationAttempt` é deletado — confirmado ainda válido após esta migração por releitura direta, não presumido; fora de escopo aqui como já era em D-152. `GlobalIndexComponent` ganha `"delivery-record-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.delivery_record_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-delivery-record-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e condicionado em `version` (não `createdAt`, adaptação real vs. o template de D-187). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 8 workers já migrados. **G-V3**: 3 testes de gravação do ponteiro na criação (via os fixtures reais de `dispatch.test.ts`/`notification-router-workflow.test.ts` — intent do dispatch, intent REPLACEMENT do stale-decision, attempt do roteamento feliz), 24 testes do worker (purga dos 2 tipos, due no futuro nunca candidato, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `createdAt`/`version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo os 2 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa local 1912/1912 (178 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #215 mergeado (`6d333e0`→`c595190`), CI real (`33699314795`/`33699331683`) e CD real (`33699827095`) `success`, confirmados via `gh run view`/`gh run list` direto (polling síncrono, nunca aguardando notificação de background como subagente). **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-delivery-record-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyDeliveryRecordPurge` com `LeadingKeys` exato `["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItemsDeliveryRecordPurge` escopada à tabela base); `get-function-configuration`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#DELIVERY_RECORD` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT` (os 7 já migrados) → `implicitDeny` nos 7 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (NotificationIntent, NotificationAttempt)` sobre as 58 linhas da tabela (`scannedTotal=58`, `matched=0`, tabela inteira coberta), e por `Query GSI8 WORK#DELIVERY_RECORD` retornando `Count=0`; nada para escrever, execução real (não-dry-run) seria um no-op confirmado, não executada (mesma disciplina de D-187/D-188). **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — oitava fatia mecânica seguindo o padrão de D-187/D-188 (poison-record/DLQ), adaptada para 2 tipos de entidade COM `version` real, condicionado em `version`+`createdAt`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (8 de 9 workers migrados)** — `delivery-record-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); o único worker restante nomeado por D-179 permanece em `Scan`+`Limit`: `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/delivery-record-gsi8.ts` (novo), `src/workers/reminder-dispatch/dispatch.ts`, `src/modules/notification/application/notification-router-workflow.ts`, `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-delivery-record-purge.ts` (novo), testes em `test/unit/workers/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:211:| D-190 | **MaintenanceDueIndex — nona e ÚLTIMA fatia (core-user-data-purge, D-179 slice 9, 9º de 9 workers) — D-179 FECHADO POR COMPLETO.** `core_user_data_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["core_user_data_purge"]`/`worker_transact_write_policy_json["core_user_data_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItems` na tabela base, ambos só no role do `core_user_data_purge_handler`. Este worker cobre DOIS tipos de entidade (`ExpirationItem`/`ReminderPolicy`, `deletedAt+30d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Forma distinta de D-186/D-187/D-188/D-189 (create-once/janela fixa) — mesma forma do piloto original, D-180/`membership-purge`**: elegibilidade é gated numa TRANSIÇÃO de soft-delete (`deletedAt` sendo setado), não na criação — `deriveCoreUserDataMaintenanceDue()` (`src/shared/core-user-data-gsi8.ts`, novo, fora de `domain/` de módulo específico já que a entidade cobre 2 módulos — `expiration`/`reminder`) retorna `undefined` enquanto `deletedAt` está ausente; uma linha nunca vira candidata antes do soft-delete. **Achado real de pré-existência, confirmado por grep amplo (não presumido) antes de qualquer código novo**: `ExpirationItem.deletedAt` tem exatamente UM write path real — `expiration-service.ts#deleteItem` (via `transitionStatus`'s `extraSet`) — onde o ponteiro GSI8 foi wireado. `ReminderPolicy.deletedAt` NÃO TEM write path real nenhum no código hoje (`disablePolicy` só seta `enabled: false`, nunca `deletedAt`) — **condição PRÉ-EXISTENTE, não introduzida por esta migração**: o worker `Scan`-based anterior já tinha o mesmo gap (seu filtro `attribute_exists(deletedAt)` sobre `ReminderPolicy` também nunca poderia casar, pela mesma razão). `core-user-data-gsi8.ts` ainda deriva/gera chaves para `ReminderPolicy` de forma uniforme com `ExpirationItem` — no dia em que um soft-delete real for adicionado para ele, wireara o ponteiro é uma chamada de uma linha, sem mudança adicional no módulo compartilhado. **Mudança de forma real e esperada vs. o texto original do worker (D-151)**: `candidate-source.ts`'s comentário original dizia "plain `DeleteCommand`, not `TransactWriteItems` — D-151's design named no purge receipt"; isso muda agora porque o `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` da claim GSI8 precisa estar na MESMA transação do `Delete` (mesmo raciocínio de todo outro worker migrado com fence de tenant) — mudança correta e esperada, não um desvio a evitar. `core-user-data-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `deletedAt` exato — defesa em profundidade contra uma corrida de "undelete", mesmo sem caminho de undelete real existir hoje para nenhuma das duas entidades, mesma disciplina de D-151/D-189). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188/D-189): backoff exponencial capado via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#CORE_USER_DATA"` após `MAX_ATTEMPTS=5`. `GlobalIndexComponent` ganha `"core-user-data-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.core_user_data_purge_handler.function_name`. Novo `scripts/backfill-gsi8-core-user-data-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, só linhas já soft-deletadas (nunca escreve ponteiro para uma linha sem `deletedAt`), vencimento FUTURO e passado. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 9 workers — **matriz de isolamento completa, todos os 9 namespaces GSI8 agora provados mutuamente exclusivos**. Suíte completa local 1929/1929 (179 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). PR #217 mergeado (`1633d6c`→`1e483c1`), CI real (`33702974051`) e CD real (`33703151890`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-core-user-data-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyCoreUserDataPurge` com `LeadingKeys` exato `["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItemsCoreUserDataPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 5 * * ? *)`). **Prova de isolamento cross-namespace — a mais ampla do programa inteiro, 8 verificações negativas** (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8): `WORK#CORE_USER_DATA` (próprio) → `allowed`; os 8 namespaces já migrados (`WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT`/`WORK#DELIVERY_RECORD`) → `implicitDeny` nos 8 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard roda de fato): 0 candidatos — confirmado por `Scan` bruto cobrindo as 58 linhas inteiras da tabela filtrado por `entityType IN (ExpirationItem, ReminderPolicy)` retornando `Count=0` (a tabela `dev` não tem hoje NENHUMA linha desses dois tipos, muito menos soft-deletada), e por `Query GSI8 WORK#CORE_USER_DATA` retornando `Count=0`; execução real seria um no-op confirmado. **D-179 (MaintenanceDueIndex) está FECHADO — os 9 workers nomeados pelo design estão 100% migrados, verificados ao vivo em `dev`, com prova de isolamento IAM cross-namespace completa**: `membership-purge` (D-180/D-181), `invitation-purge` (D-182), `document-file-reconciliation` (D-183), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186), `security-audit-purge` (D-187), `transient-purge` (D-188), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190). **Item pendente carregado, ainda não resolvido, agora o item real mais proeminente desta área**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia; como D-179 fecha aqui, este é o item de maior prioridade real que sobra nesta área de trabalho. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — nona e última fatia mecânica, mesma forma do piloto original D-180 — transição de soft-delete, não criação) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO — D-179 COMPLETO (9 de 9 workers migrados)** — `core-user-data-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo completa contra os 8 outros namespaces); nenhum worker nomeado por D-179 permanece em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/core-user-data-gsi8.ts` (novo), `src/modules/expiration/application/expiration-service.ts`, `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-core-user-data-purge.ts` (novo), testes em `test/unit/{expiration,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
infra\tests\stack.tftest.hcl:474:run "gsi8_worker_isolation" {
5: * synthetic/disposable (zero real Organization/Membership) — reset, not migration, per
13: *     manifest (counts + entityTypes + SHA-256 hash per item, never a raw field value) to
211:  entityTypes: string[];
215:/** Redacted manifest entry — counts/entityTypes/hashes only, NEVER a raw field value (this is
219:  const entityTypes = Array.from(new Set(items.map((item) => String(item.entityType ?? "unknown")))).sort();
220:  return { source, itemCount: items.length, entityTypes, hashes: items.map(hashItem) };
docs/architecture/decisions-log.md:127:| D-111 | Multi-User B2B, Wave B2B-12 (Cutover de dev) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-110. `scripts/reset-dev-data.ts` novo: Fase A (sempre, read-only) inventaria `exptrk-dev-table`/`exptrk-dev-bff-session`/bucket `extraction-transient`/user pool Cognito/24 filas SQS, grava snapshot bruto em `.local-artifacts/dev-reset/<timestamp>/` (`.gitignore`ado, novo) e manifest redigido (contagens/`entityType`s/hash SHA-256 por item, nunca valor sensível) em `docs/architecture/reviews/multi-user-b2b-wave-b2b12-scoping/`; Fase B (só `--confirm`) deleta em lotes de 25 via `BatchWriteItem` com `deleteBatchWithRetry()` local (backoff exponencial+jitter, sem módulo novo em `shared/` — nenhum outro call site real precisava), `PurgeQueue` nativo nas 24 filas, delete de Cognito só com `--include-cognito` adicional, termina com segunda leitura completa (`assertAllEmpty()`, fail-loud se qualquer contagem não for zero). Nomes de tabela/sessão/bucket validados contra allowlist hardcoded (`assertAllowedTable`), conta AWS confirmada via `sts:GetCallerIdentity` (`assertExpectedAccount`) antes de qualquer operação — nunca parâmetro livre, esta conta hospeda outros projetos não relacionados. `OnboardingStateResolver`/`onboarding-state.ts`: `LEGACY_TENANT_ONLY` removido do union e do procedimento sequencial (colapsa direto em `NO_TENANT_NO_MEMBERSHIP`), comentário desatualizado corrigido; `frontend/src/api/session.ts`'s `OnboardingState` (mesmo union) e `test/unit/organization/onboarding-state.test.ts` (2 casos de teste do estado removido, 1 caso reescrito para a nova expectativa) atualizados em conjunto — confirmado seguro por leitura que os 2 call sites reais (`resolve-request-context.ts`, `bff-auth-service.ts`) comparam só contra `"HAS_USABLE_MEMBERSHIP"`, nunca switch exaustivo. **Verificação real, não só `--dry-run` teórico**: rodado de fato contra `dev` via `aws --profile claude-dev` sem `--confirm` (Fase A, leitura pura) — confirmou 47 itens/0 sessões/21 objetos S3/0 usuários Cognito/24 filas (1 com 3 mensagens reais, `upload-finalizer-dlq`), manifest gravado com sucesso, script parou antes da Fase B exatamente como projetado. G-V3 aplicado e verificado por mutação real em 3 pontos: `deleteBatchWithRetry` (retentar a lista original em vez de só o `UnprocessedItems`, teste pega), `hashItem` (remover a ordenação de chaves antes de serializar, teste pega), `parseArgs`'s allowlist de `--table` (remover a chamada de `assertAllowedTable`, teste pega) — os 3 restaurados e reverificados. Novas dependências: `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` (mesmas versões pinadas dos demais clients AWS SDK v3 do projeto). **Execução real (`--confirm`/`--include-cognito` reais) permanece pendente de confirmação explícita do Marcelo** — registrado, não bloqueia o fechamento desta wave (design+implementação+verificação de leitura já completos). | Type 1 (nível 5 de `change-risk-scale.md` — operação destrutiva contra recursos AWS reais + muda contrato observável de `OnboardingStateResolver`/`GET /bff/session`) | N/A — implementação direta, protocolo já rodado em D-110 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa backend `npm test` 1232/1232 (132 arquivos, +22 líquidos vs. B2B-11: +23 `reset-dev-data.test.ts`, -1 `onboarding-state.test.ts`), zero regressão; frontend `vitest run` 139/139 + `playwright test` 24/24, zero regressão; `npm run build:lambdas` PASS (34/34, nenhum handler novo). Com este item, **Wave B2B-12 está `DONE`** (execução real destrutiva à parte, gated) | `scripts/reset-dev-data.ts`, `test/unit/scripts/reset-dev-data.test.ts`, `src/modules/organization/application/onboarding-state.ts`, `test/unit/organization/onboarding-state.test.ts`, `frontend/src/api/session.ts`, `.gitignore`, `package.json`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
docs/architecture/decisions-log.md:144:| D-125 | Responsibility Reassignment on Member Removal — implementação real do mecanismo APROVADO em D-122 (Claude 9,1/Codex 9,1, 3 rodadas, `docs/architecture/reviews/responsibility-reassignment-scoping/`), mesmo padrão de D-096/D-102/D-108/D-113/D-124 (implementação direta de design já aprovado, nenhuma rodada Claude↔Codex nova para o mecanismo). **Precondição best-effort, explicitamente NÃO atômica**, em `RemoveMembershipService.remove()`/`LeaveOrganizationService.leave()`: antes da transação de remoção, checa se `targetUserId` ainda é `assigneeUserId` de algum `ExpirationItem` `ACTIVE` na organização e lança `ResponsibilityReassignmentRequiredError` em vez de prosseguir — uma leitura síncrona de decisão, nunca parte do `TransactWriteItems` (Round-3 "Estado final consolidado"). **Nova porta `AssignedActiveItemsLookup`** (`organization/ports/assigned-active-items-lookup.ts`, nome/forma do Round-3 — supersede o `SoleResponsibilityChecker` do Round-1) na CONSUMING module, implementada no composition root (`runtime/aws/composition/organization.ts`'s `buildAssignedActiveItemsLookup`, exportada para teste direto) — mesmo padrão estrutural de `expiration/ports/member-eligibility.ts`, mas na direção OPOSTA (primeira vez que `organization` lê dado de `expiration`). **Contrato de paginação da Correção 2 do Round-3, implementado literalmente**: pagina o GSI1 `TENANT#t#ITEMSTATUS#ACTIVE` até esgotar (`LastEvaluatedKey` até `undefined`), aplica `FilterExpression: assigneeUserId = :userId` a cada página, NUNCA usa `Limit` do DynamoDB como proxy de truncamento — `totalKnown` é a contagem real de todas as páginas, o teto de 20 aplica só à lista `itemIds` retornada, `truncated` é `totalKnown > itemIds.length`. GSI1 não está na taxonomia restrita de `security-audit.ts` (só GSI3/GSI6 exigem `security.global_index_access`) — nenhum evento novo de auditoria necessário. **Erro novo** `ResponsibilityReassignmentRequiredError` (`app-error.ts`, `BUSINESS_RULE`/`RESPONSIBILITY_REASSIGNMENT_REQUIRED`, `retryable: false`, `details: {targetUserId, itemIds (até 20), totalKnown, truncated}`), mapeado para 422 pelo `STATUS_BY_CATEGORY` genérico já existente (nenhuma mudança de handler necessária). **Ordem de checagem** em `remove-membership.ts`: autorização → tier OWNER (`OwnerTierChangeRequiresOwnerError`) → reatribuição de responsabilidade (novo) → transação (que ainda aplica `LastOwnerError` atomicamente) — a checagem de tier vence porque decide SE o caller pode agir, antes de qualquer pergunta sobre o estado do alvo; documentado inline com teste dedicado provando a ordem quando ambos os erros se aplicariam. **Escopo respeitado integralmente**: `ItemWatch`/watchers intocados (precedente B2B-11 mantido), nenhum bypass de emergência inventado (pergunta de produto real, deferida a Marcelo, não decidida aqui), nenhuma UI nova de reatribuição (a mutação `updateItem` já existente, já validada por `MemberEligibilityChecker`, é o mecanismo) — só paridade mínima de mensagem de erro no frontend (`isResponsibilityReassignmentRequiredError` em `api/errors.ts` + `Settings.tsx`'s `LeaveOrganizationSection`, mesma classe de polimento pequeno que D-120 fez para `LastOwnerError`). **G-V3**: 3 mutações reais verificadas e revertidas — (1) neutralizar a checagem `if (assigned.itemIds.length > 0)` em `remove-membership.ts` (1 teste falha); (2) trocar `truncated: totalKnown > itemIds.length` por `truncated: false` derivado de `itemIds.length` no adapter (1 teste falha — prova que `totalKnown` não é derivado do array capado); (3) remover o `while (exclusiveStartKey)` (travar em uma página só) no mesmo adapter (1 teste falha — prova paginação real até exaustão). Nenhuma mutação mascarada. **Sem infra/rota nova**: `remove-membership.ts`/`leave-organization.ts` já wireados a handlers HTTP existentes desde B2B-8 — confirmado por leitura, não assumido; `build:lambdas`/`terraform test` não exigidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`check-docs` limpos (as 2 falhas de `npm test` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes em `scripts/reset-dev-data.ts`, sem relação com esta mudança); backend 1249 testes passando / 137 arquivos (2 novos: `assigned-active-items-lookup.test.ts` com 4 testes, +6 testes novos em `membership-management.test.ts`), frontend 156 testes / 24 arquivos inalterado (só paridade de mensagem, sem teste novo de UI). Zero regressão real. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-122, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo de precondição best-effort real, porta + adapter reais, erro novo mapeado, paridade mínima de frontend | `src/modules/organization/ports/assigned-active-items-lookup.ts`, `src/modules/organization/application/{remove-membership,leave-organization}.ts`, `src/runtime/aws/composition/organization.ts`, `src/shared/errors/app-error.ts`, `frontend/src/api/errors.ts`, `frontend/src/routes/Settings.tsx`, `test/unit/organization/{assigned-active-items-lookup,membership-management}.test.ts`, `test/unit/identity/resolver.test.ts` | 2026-08-31 |
docs/architecture/decisions-log.md:146:| D-126 | CSV data export — implementação real do mecanismo APROVADO em D-123 (implementação direta de design já aprovado, mesmo padrão de D-096/D-102/D-108/D-113/D-124/D-125 — nenhuma rodada Claude↔Codex nova para o mecanismo). **Nova action RBAC `item:export`** (`identity/domain/authorization.ts`) em `ADMIN_ROLES`, justificativa de assimetria de disclosure verbatim do design. **Orçamento decrescente literal** (round-3 Achado #1) em `ExpirationService.exportItems()` — 3 chamadas `queryGsi1` (`ACTIVE`/`ARCHIVED`/`RENEWED`, ordem fixa, `DELETED` nunca consultado), `limit: budget+1` para detectar estouro sem query de contagem separada, `EXPORT_ITEM_CAP=2000` exportado como constante nomeada; confirmado por leitura de `dynamodb-expiration-store.ts` que `queryGsi1` pagina internamente até acumular `limit` itens reais sem `FilterExpression`, tornando `limit`-como-contagem seguro aqui (diferente do achado de D-125 sobre GSI1+filter). **Guard de 4 MB** (round-3 Achado #2) em `buildExportCsv()` (`expiration/http/export-handler.ts`), acumulando `Buffer.byteLength` do CSV final serializado (header+CRLF+linhas pós-quoting RFC4180), nunca a soma de valores brutos — qualquer um dos dois limites (item ou byte) que disparar primeiro rejeita com o mesmo `ValidationError`. **`src/shared/csv/csv-export-writer.ts`** novo, `serializeCsvField()`/`serializeCsvRow()` únicos fazendo mitigação de fórmula (apóstrofo, extensão do precedente `APROVADO` de `roadmap-evolution/09`) ANTES da quotação RFC4180 no mesmo passe — nunca dois mecanismos separados, provado por teste dedicado do caso que precisa dos dois. **`toApiGatewayCsvResult()`** novo e aditivo em `http-adapter.ts` (round-3 Achado #3) — único call site é o novo handler; `toApiGatewayResult()` existente inalterado. **Colunas CSV** (15, ordem fixa) reverificadas contra `expiration-item.ts` real: `itemId,name,category,description,dueDate,issueDate,periodicity,issuer,number,assigneeUserId,tags,priority,status,createdAt,updatedAt`. **Filename** gerado só de `tenantId` (ULID server-side) + timestamp — nunca `Organization.displayName` nem string do cliente (achado não-bloqueante do Codex round-3, risco de header injection). **Decisão de wiring HTTP, não coberta literalmente pelo design aprovado**: `items_handler` (Terraform) não declara `timeout_seconds` (default 10s, `lambda-function/variables.tf`), insuficiente para o `timeout_seconds=25` exigido pelo round-3 Achado #6 sem afetar toda rota `/items*` existente — optado por **Lambda dedicada `export_handler`** (`infra/main.tf`, `timeout_seconds=25`, mesma política `tenant_facing_read_write_policy_json` de `items_handler`, sem `gsi4_read_policy_json` — GSI1 não é restrito) + rota própria `GET /items/export` no módulo `api-gateway` (não `bff-api-gateway`, mesmo precedente de D-124 — leitura/escrita de item já roteia por `api-gateway`), integração/permissão/assert de rota dedicados no MESMO commit (classe de bug D-117/D-120), coexistindo com `GET /items/{itemId}` pelo mesmo precedente de segmento literal que `GET /items/dashboard` já usa. **G-V3**: verificado por teste dedicado que defeataria uma implementação mais fraca — (1) `serializeCsvField` prova que mitigar-depois-de-quotar produziria CSV sintaticamente quebrado (comentário no teste mostra a saída incorreta), a implementação real mitiga primeiro; (2) teste com `ExpirationStore` stub prova que `queryGsi1` pedido com `limit: 2001` retornando exatamente 2001 linhas de `ACTIVE` sozinho dispara o `ValidationError` com `statusWhereExceeded: "ACTIVE"` — prova o `budget+1` na fronteira exata; (3) teste de `buildExportCsv` com descrição pesada em vírgulas (que quase dobra de tamanho após quoting) prova que o guard mede bytes PÓS-serialização, não os brutos; (4) teste RBAC prova `MEMBER`/`VIEWER` negados e `ADMIN`/`OWNER` permitidos. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos (as 2 falhas de `npm test`/`system-mutation-allowlist.test.ts` são pré-existentes em `develop`, confirmadas via `git stash` antes de qualquer mudança desta sessão — mesmas `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes de `scripts/reset-dev-data.ts` já documentadas em D-125, sem relação com esta mudança); backend 1277 testes / 139 arquivos (3 novos: `csv-export-writer.test.ts` 16 testes, `export-handler.test.ts` 5 testes, +7 testes novos em `expiration-service.test.ts`), 1275 passando (2 falhas pré-existentes não relacionadas); `terraform validate`/`terraform test` limpos na raiz (`AWS_PROFILE=claude-dev`) e no módulo `api-gateway` (1/1 run, 3 asserts novos); zero regressão real. Nenhuma operação AWS destrutiva — só `validate`/`test`/`build`. Escopo respeitado integralmente: só `ExpirationItem`, sem gate de billing (M12 bloqueado por D-052), `TrackedSubject`/documentos fora de escopo (v2 futura). | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-123, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113/D-124/D-125) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — mecanismo real de ponta a ponta (RBAC → orçamento GSI1 → guard de bytes → serialização CSV → resposta HTTP → Lambda dedicada → rota API Gateway); nunca antes tinha chamador HTTP real | `src/modules/identity/domain/authorization.ts`, `src/modules/expiration/application/expiration-service.ts`, `src/modules/expiration/http/export-handler.ts`, `src/shared/csv/csv-export-writer.ts`, `src/runtime/aws/http-adapter.ts`, `src/runtime/aws/handlers/export-handler.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `infra/modules/api-gateway/{main,variables}.tf`, `infra/modules/api-gateway/tests/api_gateway.tftest.hcl`, `test/unit/expiration/{expiration-service,export-handler}.test.ts`, `test/unit/shared/csv-export-writer.test.ts` | 2026-08-31 |
docs/architecture/decisions-log.md:149:| D-129 | Supersessão de GTR-01 (`UserProfile.requesterDisplayName` → `Organization.displayName`) — pendência registrada desde Wave B2B-5/D-096 (2026-08-30), nunca debatida em nenhuma rodada B2B, aberta em `roadmap-evolution/17` §121 Q21 e `docs/frontend/README.md`. Autoridade ampliada (`docs/engineering/ai-governance.md` §1, 2026-08-31) cobre o resíduo de produto (REPLACE vs. coexistir). Protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: Claude 9,1 self / Codex 7,8 — achou a invariante "sempre presente" de `Organization.displayName` não garantida na prática (`create-organization.ts`/`bff-handlers.ts` aceitavam whitespace-only sem trim) e inventário de remoção incompleto (faltava `profile-handler.ts` Lambda, `composition/identity.ts`, `proxy-allowlist.ts`, `build-lambdas.ts`, rotas/testes Terraform); Rodada 2: Claude 9,4 self / Codex 8,8 — corrigiu a invariante mas ainda faltava `profile:read` em `authorization.ts` (linhas 57/142/165), 2 testes existentes sem atualização, 2 testes novos não nomeados, e descrição imprecisa de `infra/main.tf:164` (módulo, não recurso bruto); Rodada 3: Claude 9,5 self / Codex 9,8 — fechou os 5 gaps remanescentes, ambos ≥9,0 sem arredondar). **Pesquisa externa (SIM PARCIAL)**: DocuSign/PandaDoc/HelloSign ("enviado por X via Empresa Y", nome da conta como identidade primária), Slack Connect, e-mails transacionais Notion/Linear — convergência na direção "nome da organização é a identidade guest-facing primária/mais confiável, nome de pessoa é aditivo, nunca substituto quando ausente", sem um único padrão universal como RBAC/invite. **Decisão: REPLACE, não coexistir.** `Organization.displayName` passa a ser a única fonte guest-facing de "quem está solicitando" (e-mails de guest submission, chasing T7/T3, convite inicial) — `UserProfile.requesterDisplayName` **removido por completo** (campo, `GET/PUT /profile`, Lambda `profile-handler`, actions `profile:read`/`profile:update`, resolver dedicado), não deprecado. Justificativa: `Organization.displayName` é setado obrigatoriamente na criação (ao contrário do campo antigo, que nunca teve UI de frontend em nenhuma wave e estava vazio na prática esmagadora maioria dos casos); nome de organização é identidade mais estável através de troca/saída de membro para um relacionamento externo contínuo (document chasing por semanas); manter override per-user sem demanda real de produto é dívida morta. **Invariante corrigida como parte desta mudança** (achado real do Codex Rodada 1): `CreateOrganizationService.buildCreateEntries()` agora faz `trim()`+rejeita `displayName` em branco com `ValidationError` — a criação aceitava whitespace-only sem normalização, o que teria enfraquecido a garantia "sempre presente" da nova fonte única; `bff-handlers.ts` também trima na fronteira HTTP (defesa em profundidade), mas o serviço é a autoridade. **Migração de dado**: nenhuma — `dev` é sintético/resetável (`AGENTS.md` §1), itens DynamoDB existentes com `requesterDisplayName` carregam um atributo morto, nunca lido após a remoção. **Futuro (fora de escopo, nomeado para não ser redebatido)**: se um dia houver demanda real por atribuição pessoal, a forma correta é um snapshot imutável no momento da criação do request (`DocumentRequest`/`RequirementAssignment`), nunca a ressurreição de um campo per-user persistente. **Portas renomeadas** `resolveRequesterDisplayName({tenantId,userId})` → `resolveOrganizationDisplayName({tenantId})` em `subject.ts`/`dispatch.ts`/`document-request-service.ts`/`guest-submission-service.ts` — nenhum call site real precisava do `userId`. **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos; backend 1267 testes passando/137 arquivos (2 falhas pré-existentes não relacionadas, mesmas `@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes de `scripts/reset-dev-data.ts` já documentadas em D-125/D-126, confirmadas por não estarem no diff desta mudança); `terraform validate`/`terraform test` limpos na raiz (19/19, `AWS_PROFILE=claude-dev`) e nos módulos `api-gateway` (1/1) e `security-audit-observability` (1/1) — infra genuinamente tocado (2 rotas API Gateway + 1 Lambda module removidos), zero regressão real. | Type 1 (nível 5-6, UX/trust guest-facing + migração de fonte de dado através de múltiplos call sites, protocolo obrigatório) | 9,5 | 9,8 | **APPROVED + IMPLEMENTADO** (3 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/gtr-01-supersession-scoping/` (round-1 a round-3, proposta+self-grade+critique Codex cada, `estado-final-consolidado.md`) | 2026-08-31 |
docs/architecture/decisions-log.md:154:| D-134 | Wave 1b — Design System Implementation Gap (D-130/D-133), continuação: implementação direta de 4 componentes do catálogo (`design-system.md` §29) — verificação de shape feita antes de escrever, nenhum já existia sob outro nome (`base.css` já estiliza `<a>` globalmente e o `:focus-visible` global cobre foco, mas nenhum componente dedicado à navegação inline existia; nenhum checkbox/radio/switch em `components/forms/` nem `ui/`). **`Link`** (`frontend/src/components/ui/{Link.tsx,Link.css}`) — terceiro membro do trio de navegação junto de `Button`/`ButtonLink` (mission §34): `Link` envolve o `Link` real do react-router para navegação interna (client-side), `ExternalLink` é um `<a>` real com `target="_blank"` + `rel="noopener noreferrer"` (nunca só `target="_blank"`, que deixa `window.opener` alcançável pelo destino) e um marcador visível (não só ícone) + texto visualmente oculto "(abre em nova aba)". **`Checkbox`** (`frontend/src/components/ui/{Checkbox.tsx,Checkbox.css}`) — `<input type="checkbox">` real posicionado sobre uma caixa desenhada (nunca `display:none`, que removeria da árvore de acessibilidade em alguns navegadores); `indeterminate` setado imperativamente via ref (não existe prop React para a propriedade DOM), independente de `checked`. **`RadioGroup`** (`frontend/src/components/ui/{RadioGroup.tsx,RadioGroup.css}`) — catálogo lista "Radio" no singular, mas um `<input type="radio">` só faz sentido como conjunto compartilhando `name`; implementado como o grupo, com `<fieldset>`/`<legend>` reais (não `aria-label`), reusa `.ui-field__requirement`/`.ui-field__error` de `components/forms/Form.css` (mesmo padrão visual de obrigatório/erro do `TextField`) em vez de duplicar. **`Switch`** (`frontend/src/components/ui/{Switch.tsx,Switch.css}`) — não existe controle HTML nativo para toggle de efeito imediato; implementado como `<button role="switch" aria-checked>` (padrão WAI-ARIA APG), nunca `<div onClick>`. Nenhum token novo necessário (todos consomem semantic tokens existentes — `--color-action-primary`, `--target-size-min` 24px como piso do alvo clicável mesmo com caixa/dot desenhados menores, `--radius-pill` para a trilha do Switch). Nenhuma pesquisa externa (padrões de implementação mecânicos — checkbox/radio nativos, WAI-ARIA switch pattern documentado — não definição de padrão de mercado não resolvido). **Decisão explícita de não-escopo desta sessão**: `Combobox`, `DateInput`/`DatePicker`, `Tooltip`, `Popover`, `DropdownMenu`, `Modal`/`Dialog`, `Drawer`, `Tabs`, `Pagination`, `Breadcrumb`, `Avatar` continuam faltando — os 5 primeiros (Tooltip a Modal/Drawer) exigem estratégia de overlay/portal/focus-trap ainda não decidida no projeto (candidato a protocolo Claude↔Codex completo quando abordados, por não haver padrão óbvio já implementado); os demais exigem composição maior (paginação real de dados, breadcrumb dinâmico por rota, avatar com fallback de iniciais) deliberadamente adiada para não forçar conclusão apressada. `Card` permanece gap nomeado de D-133, sem mudança nesta sessão. **G-V3**: mutação nomeada e revertida — `RadioGroup`'s `checked={value === option.value}` mutado para `!==`, 1 teste falhou corretamente (deixou de reportar o valor selecionado), revertido e suíte voltou a 27/27 em `ui.test.tsx`. **Suíte**: frontend `typecheck`/`lint` limpos, `npm test` (frontend) 171/171 (17 novos em `ui.test.tsx`, total do arquivo 27), `npm run lint`/`check-boundaries`/`validate-schemas`/`check-docs` (raiz) limpos; `npm run typecheck` (raiz) só passou após `npm ci` — `node_modules` estava desatualizado em relação a `package.json` (`@aws-sdk/client-sts`/`@aws-sdk/client-cognito-identity-provider` ausentes, usados por `scripts/reset-dev-data.ts` de D-110/D-111), não relacionado a este diff, corrigido como pré-requisito para rodar a suíte real em vez de presumir "sem erro = já estava assim". `npm test` (backend) 1292/1292 verde (137 arquivos), sem regressão. | Type 1 (nível 4, implementação direta de catálogo já `APPROVED` em D-130, mesmo padrão de D-133) | n/a (implementação mecânica pós-D-130/D-132) | n/a (implementação mecânica pós-D-130/D-132) | **IMPLEMENTADO** (parcial, deliberado) — 4 de ~7 componentes ainda faltantes do catálogo original; backlog nomeado explicitamente para o restante (overlay/portal/focus-trap + composição maior) | `frontend/src/components/ui/{Link.tsx,Link.css,Checkbox.tsx,Checkbox.css,RadioGroup.tsx,RadioGroup.css,Switch.tsx,Switch.css}`, `frontend/test/components/ui.test.tsx` | 2026-08-31 |
docs/architecture/decisions-log.md:200:| D-179 | **MaintenanceDueIndex — design aprovado para resolver a starvation estrutural dos 9 workers de manutenção confirmada por D-170 (Scan+Limit+páginas sem cursor persistido entre invocações, reinicia pela mesma ordem física de hash, candidatos além da janela nunca são alcançados).** Protocolo Claude↔Codex completo, 4 rodadas reais (Claude 5,8→8,6→8,9→9,2, Codex 5,8→6,4→8,3→9,3, ambos ≥9,0 sem arredondar na Rodada 4). E-014 `SIM PARCIAL` (paginação DynamoDB/sparse index/`LeadingKeys` são padrão externo, 3 fontes primárias AWS verificadas; alocação de GSI e escopo de workers são internos). **Decisão**: um GSI8 global esparso novo (`PK=WORK#<workerType>`, `SK=<dueAtIso>#TENANT#<tenantId>#<entityId>`, `KEYS_ONLY`), substituindo `Scan` por `Query GSI8SK < now` nos 9 workers (`membership-purge`, `invitation-purge`, `document-file-reconciliation`, `requirement-reindex`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`) — mesmo padrão que `document-purge`/GSI6 (D-061) já prova, generalizado. **Achado prévio real**: GSI1-GSI7 já estavam todos ocupados (`infra/modules/dynamo-table/main.tf`) — a proposta original do documento de auditoria não checou isso; GSI8 é o próximo slot livre, dentro do limite técnico do DynamoDB (20/tabela). Componentes fechados nas 4 rodadas: isolamento IAM real por worker via `dynamodb:LeadingKeys` (`["WORK#<type>","DLQ#<type>"]`, nunca política genérica) + `dynamodb:TransactWriteItems` como ação NOVA não coberta pela política geral hoje; revalidação atômica obrigatória (GSI8 nunca é fonte de elegibilidade, só descoberta — `TransactWriteItems` com `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` na mesma transação do delete, fechando um TOCTOU real que o padrão antigo tinha); `deriveMaintenanceDue()` pura por worker (6 de 9 já têm o equivalente exportado); backfill cobrindo todo item com `dueAt` determinável (não só os já vencidos — erro real cometido e corrigido durante o próprio protocolo, ver abaixo); poison-record/quarentena com backoff idempotente (reusa o mesmo padrão `isConditionalCheckFailed` que `membership-purge/purge.ts` já usa, não um mecanismo novo); observabilidade subindo ao handler (workers continuam observability-agnostic, `AGENTS.md` §7/D-007), gatilho de shard por `oldestCandidateAgeSeconds` real. **`requirement-reindex` permanece no escopo** (achado corrigido na Rodada 3: `reindex.ts:46-47` confirma `evidenceValidUntil` como `dueAt` real — a Rodada 1 excluiu por engano). **Rigor do protocolo evidenciado pelas próprias notas**: Rodada 2 (6,4) introduziu 3 erros factuais novos ao corrigir a Rodada 1 (excluiu `requirement-reindex` por leitura invertida do código, alegou que `scripts/reset-dev-data.ts` faz reseed quando só apaga, matriz com 3 fórmulas de `dueAt` erradas) — todos corrigidos com `arquivo:linha` na Rodada 3 (8,3); os 5 achados finais da Rodada 3 eram de design genuíno (backfill com alvo errado, IAM incompleto, idempotência não demonstrada, observabilidade mal-colocada), fechados na Rodada 4. DESIGN-ONLY — nenhum código/GSI/worker alterado por esta decisão. Ver `docs/architecture/reviews/maintenance-due-index-scoping/estado-final-consolidado.md` para o detalhe completo e os 4 rounds. | Nível 5-6 (novo GSI global, migração de access pattern em 9 workers vivos, invariante de correção LGPD/retenção — protocolo Claude↔Codex completo obrigatório) | 9,2 | 9,3 | **APPROVED (design)** — implementação real é a próxima fatia, ver `NEXT_SESSION_PROMPT.md` | `docs/architecture/reviews/maintenance-due-index-scoping/` (5 arquivos: `estado-final-consolidado.md` + 4 rounds) | 2026-09-02 |
docs/architecture/decisions-log.md:207:| D-186 | **MaintenanceDueIndex — quinta fatia (quota-telemetry-purge, D-179 slice 5, 5º de 9 workers)**: `quota_telemetry_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["quota_telemetry_purge"]`/`worker_transact_write_policy_json["quota_telemetry_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItems` na tabela base, ambos só no role do `quota_telemetry_purge_handler`. **Forma distinta dos 4 anteriores, confirmada por leitura direta antes de qualquer código**: este worker cobre DOIS tipos de entidade que compartilham o mesmo formato de elegibilidade (`resetAt+30d`, fence de tenant `ACTIVE`) — `TenantQuotaRecord` e `EphemeralTelemetryMutation` (`src/modules/identity/application/quota.ts`) — e NENHUM dos dois tem campo `version`; ambos usam `count`/`resetAt` como fence OCC direto (mesma forma que D-153/`SECURITY_AUDIT` já usava para suas 4 entidades append-only, por razão diferente aqui: token bucket com campos otimistas próprios, não append-only). `deriveQuotaTelemetryMaintenanceDue()` (due = `resetAt + 30d`, NUNCA `undefined` — ao contrário de Membership/Invitation não existe estado terminal, `resetAt` sozinho determina elegibilidade) e `quotaTelemetryGsi8Keys()` (embute `entityType`+`SK` natural no `GSI8SK`, já que nenhuma das duas entidades tem um id próprio separado do seu `SK`) adicionados a `quota.ts`. **4 pontos de escrita gravam/refrescam o ponteiro GSI8 atomicamente no MESMO write que seta `resetAt`** (nunca um passo separado): `consume()`'s ramo de criação e ramo de rollover de janela (`TransactWriteItems` via `executeTenantBusinessMutation`, W3-07), e `identity-store.ts`'s `incrementTelemetryCounter()` (a lane `EphemeralTelemetryMutation` de D-136 D-D — `GSI8PK`/`GSI8SK` setados via `if_not_exists`, mesmo mecanismo que já protege `resetAt`/`purgeAfterTtl` contra um escritor perdedor concorrente rebobinar a janela; o valor é determinístico a partir do bucket de janela, calculado em `quota.ts` — camada de aplicação — e passado pronto ao adapter de persistência via `TelemetryIncrementInput.gsi8`, preservando a regra de `persistence/` nunca importar lógica de `application/`). `release()` não toca o ponteiro (só decrementa `count`, nunca muda `resetAt`). `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` fresco antes de qualquer escrita e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `resetAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182, já que este worker TEM fence de tenant `ACTIVE`, ao contrário de D-183/D-185): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#QUOTA_TELEMETRY"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (diferente de Invitation/Membership, decisão explícita e documentada): como `deriveQuotaTelemetryMaintenanceDue()` nunca retorna `undefined` e todo write que seta `resetAt` também refresca o ponteiro, não existe um "ponteiro sobrevivendo a uma transição terminal" real para se defender — a lacuna que o self-heal de `invitation-purge` cobre não tem equivalente estrutural aqui. `GlobalIndexComponent` ganha `"quota-telemetry-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.quota_telemetry_purge_handler.function_name`. Novo `scripts/backfill-gsi8-quota-telemetry-purge.ts`, cobrindo as duas entidades e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `resetAt` (não `version`, mesma adaptação do worker). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 5 workers já migrados. **G-V3**: 22 testes do worker (purga das duas entidades, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `resetAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ após MAX_ATTEMPTS, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo as duas entidades, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1841/1841 (1827+14 novos: quota.ts's docstring/campo GSI8PK/GSI8SK, 22 do worker migrado, 10 do backfill novo, 1 de cobertura de observabilidade), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). **Achado real de bug pré-existente, encontrado e corrigido só neste script (não nos 6 irmãos, fora de escopo desta fatia, registrado abaixo)**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` que decide se `main()` roda quando o script é executado diretamente NUNCA casa no Windows deste repo (`import.meta.url` usa barras normais — `file:///C:/...` — enquanto `process.argv[1]` usa contrabarras — `C:\\...`) — confirmado por teste direto (`node`/`tsx` imprimindo os dois valores). `main()` silenciosamente nunca executava sob o shell Windows/Git Bash deste ambiente (`AGENTS.md` §4) apesar de exit code 0 e nenhum erro — só descoberto ao tentar rodar o backfill desta fatia pela primeira vez nesta sessão (saída vazia inesperada). Corrigido neste arquivo trocando por `fileURLToPath(import.meta.url) === process.argv[1]` (comparação de dois paths nativos do SO, sem o mismatch de separador). **Mesmo bug confirmado presente nos outros 6 scripts** (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — não corrigido nesta sessão (fora do escopo de D-186), significa que os backfills anteriores (D-180/D-182/D-183/D-185) e qualquer execução passada desses scripts via este mesmo shell podem ter silenciosamente não escrito nada, mesmo reportados como "rodados com sucesso" — precisa de verificação/reexecução numa sessão futura, nomeado explicitamente para não ser esquecido. **PR #208 mergeado (commit `a898c1d`→`a280110`), CI real (`33687474935`/`33687503644`) e CD real (`33688100421`) `success`, todos confirmados via `gh run view` direto**. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-quota-telemetry-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyQuotaTelemetryPurge` com `LeadingKeys` exato `["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItemsQuotaTelemetryPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(30 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 4 sentidos)**: `WORK#QUOTA_TELEMETRY` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX` (os 4 já migrados) → `implicitDeny` nos 4 casos. **Backfill real executado contra `dev`** (após o fix do guard acima, sem o qual teria silenciosamente não escrito nada): 24 candidatos encontrados, 24 ponteiros GSI8 escritos, confirmado por `Query GSI8 WORK#QUOTA_TELEMETRY` retornando `Count=24`. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quinta fatia mecânica seguindo o padrão de D-180/D-182 (poison-record/DLQ), adaptada para 2 tipos de entidade sem `version`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (5 de 9 workers migrados)** — `quota-telemetry-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); achado de bug real do guard `main()` em scripts de backfill no Windows, corrigido só aqui, pendente nos outros 6; os 4 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/identity/application/quota.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-quota-telemetry-purge.ts` (novo), testes em `test/unit/{identity,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:208:| D-187 | **MaintenanceDueIndex — sexta fatia (security-audit-purge, D-179 slice 6, 6º de 9 workers)**: `security_audit_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["security_audit_purge"]`/`worker_transact_write_policy_json["security_audit_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItems` na tabela base, ambos só no role do `security_audit_purge_handler`. **Forma distinta das 5 fatias anteriores**: este worker cobre QUATRO tipos de entidade que compartilham o mesmo formato de elegibilidade (`occurredAt+365d`, fence de tenant `ACTIVE`) — `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (`src/modules/{expiration,organization,subject,activity}/domain/*.ts`), as mesmas 4 partições que `GET /activity`'s `ActivityService` k-way-merges — todas append-only por construção (sem `update()`/`delete()` exportado), sem campo `version`. Novo `src/shared/security-audit-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que a entidade cobre 4 módulos diferentes — mesma razão que já colocou `EntityKey`/`TransactWriteEntry` em `shared/dynamodb/occ.ts`): `deriveSecurityAuditMaintenanceDue()` (due = `occurredAt+365d`, NUNCA `undefined` — sem estado terminal, mesmo raciocínio de D-186) e `securityAuditGsi8Keys()` (embute `tenantId`+`entityType`+`SK` natural no `GSI8SK`, mesmo formato de `quotaTelemetryGsi8Keys()`). **O ponteiro GSI8 é gravado UMA ÚNICA VEZ, na própria função pura `build*Event()` de cada uma das 4 entidades** (`buildAuditEvent`/`buildMembershipAuditEvent`/`buildSubjectAuditEvent`/`buildTenantAuditEvent`) — não em cada um dos 19 call sites espalhados por 6 módulos que chamam essas funções (confirmado por grep antes de escrever qualquer código): como toda escrita passa pela mesma função de construção antes do `appendXAuditToTransaction`, estampar o ponteiro ali cobre automaticamente todo call site presente e futuro, sem precisar tocar nenhum deles individualmente — simplificação real habilitada pela forma já centralizada do código, não uma decisão de design nova. **Normalização `MembershipAuditEvent`**: declara `organizationId`, não `tenantId` (mesmo valor, nome diferente) — `buildMembershipAuditEvent()` já passa `organizationId` como `tenantId` para `securityAuditGsi8Keys()`, e o adapter do worker (`normalizeTenantId()`) faz a mesma normalização ao ler uma linha crua da tabela base, mesmo padrão pré-existente do worker antes desta fatia. `security-audit-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também o `TenantLifecycleStatusSource` como dependência separada do worker); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `occurredAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186, já que este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#SECURITY_AUDIT"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência que D-186 quota-telemetry-purge, causa diferente): as 4 entidades são append-only por construção — não existe transição de estado terminal que deixaria um ponteiro obsoleto para trás, e a ÚNICA forma de uma linha deixar de ser candidata é este próprio worker deletá-la (o que remove o ponteiro junto com a linha inteira). **Interação com `GET /activity` (D-149), reverificada nesta fatia**: releitura direta de `dynamodb-audit-partition-store.ts#queryPage` confirma que `ExclusiveStartKey` continua um marcador posicional puro, independente de GSI8 e de qualquer deleção concorrente deste worker — a conclusão original de D-153 (nenhum bug de correção, nenhuma correção necessária) permanece válida após a migração, confirmado por leitura, não presumido. `GlobalIndexComponent` ganha `"security-audit-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.security_audit_purge_handler.function_name`. Novo `scripts/backfill-gsi8-security-audit-purge.ts`, cobrindo os 4 tipos de entidade e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `occurredAt` (não `version`). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 6 workers já migrados. **G-V3**: 4 testes de gravação do ponteiro na criação (um por tipo de entidade, incluindo a normalização `organizationId`→`tenantId`), 2 testes do módulo puro `security-audit-gsi8.ts`, 17 testes do worker (purga dos 4 tipos, boundary exato, fence de tenant não-ACTIVE com contador de retry, normalização de `MembershipAuditEvent`, sem lifecycle record, corrida de `occurredAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/normalizeTenantId/processPage cobrindo os 4 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1867/1867 (175 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #210 mergeado (`5be0743`), CI real (`33691359230`/`33691948793`) e CD real (`33692216726`) `success`, todos confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-security-audit-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlySecurityAuditPurge` com `LeadingKeys` exato `["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItemsSecurityAuditPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 6 sentidos)**: `WORK#SECURITY_AUDIT` (próprio, com `--resource-arns` explícito no GSI8 — sem ele o simulate retorna `implicitDeny` mesmo para o caso positivo, achado de processo desta sessão) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY` (os 5 já migrados) → `implicitDeny` nos 5 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` corrigido roda de fato): 0 candidatos encontrados — a tabela `dev` não contém hoje nenhuma linha `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (confirmado por `Scan` bruto de `entityType`, dado sintético esparso), então nada foi escrito; confirmado por `Query GSI8 WORK#SECURITY_AUDIT` retornando `Count=0`, consistente com o dry-run. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sexta fatia mecânica seguindo o padrão de D-180/D-182/D-186 (poison-record/DLQ), adaptada para 4 tipos de entidade sem `version`, ponteiro centralizado nas 4 funções `build*Event()` em vez de múltiplos call sites) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (6 de 9 workers migrados)** — `security-audit-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 3 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/security-audit-gsi8.ts` (novo), `src/modules/{expiration,organization,subject,activity}/domain/*.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-security-audit-purge.ts` (novo), testes em `test/unit/{shared,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:209:| D-188 | **MaintenanceDueIndex — sétima fatia (transient-purge, D-179 slice 7, 7º de 9 workers)**: `transient_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["transient_purge"]`/`worker_transact_write_policy_json["transient_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItems` na tabela base, ambos só no role do `transient_purge_handler`. **Forma distinta de todas as fatias anteriores**: este worker cobre DOIS tipos de entidade com dinâmicas DIFERENTES — `WebhookInbox` (`src/modules/notification/application/ses-callback-workflow.ts`, criado uma vez/imutável, ponteiro gravado uma única vez na criação, mesma forma que security-audit-purge/D-187) e `UploadSlot` (`src/modules/document/domain/upload-slot.ts`, transições reais de status, mesma forma que invitation-purge/D-182 — ponteiro precisa ser gravado/atualizado a cada transição relevante, não só na criação). Novo `src/shared/transient-purge-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que as duas entidades cobrem 2 módulos diferentes — mesma razão de `shared/security-audit-gsi8.ts`): `deriveWebhookInboxMaintenanceDue()` (`createdAt+7d`, sempre), `deriveUploadSlotMaintenanceDue()` (`undefined` enquanto `RESERVED` — nunca candidato —, senão `reservedAt+7d` se `CONSUMED`, `reservedAt+24h` caso contrário incl. `EXPIRED`/`RELEASED`) e `transientPurgeGsi8Keys()`. **Achado de fronteira de módulo real durante a implementação**: a primeira versão deste arquivo reusava `computeUploadSlotPurgeAfter()`/`UploadSlotStatus` importados de `modules/document/domain/**`, violando `dependency-cruiser`'s regra `shared-must-not-reach-modules` (capturado por `test/architecture/tenant-fence-boundary.test.ts`, que falhou na primeira rodada de testes) — corrigido reimplementando a fórmula (`privacy-lgpd.md` §4, um-liner estável) diretamente em `shared/transient-purge-gsi8.ts` com um tipo `TransientUploadSlotStatus` local, em vez de inverter a fronteira de dependência. Um `UploadSlot` `RESERVED` NUNCA recebe ponteiro GSI8 (nunca aparece na Query GSI8 de forma alguma, diferente do `Scan` anterior que precisava filtrá-lo defensivamente em tempo de leitura). Três escritores atualizados para gravar/atualizar o ponteiro atomicamente na MESMA escrita condicional da transição: `ses-callback-workflow.ts` (criação do `WebhookInbox`), `advance-after-evidence.ts#appendSlotConsumption` (transição `CONSUMED`), `upload-slot-reconciliation/reconciliation.ts#processOneSlot` (transição `EXPIRED`) — nenhum writer real produz `RELEASED` hoje (confirmado por grep: status declarado no union mas sem write path), então nenhum terceiro escritor precisou de mudança. `transient-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também `TenantLifecycleStatusSource` como dependência separada do worker, embora a classe permaneça exportada por simetria com os irmãos, sem uso real); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por `version` — AMBAS entidades têm `version` real, diferente de security-audit-purge — na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#TRANSIENT"` após `MAX_ATTEMPTS=5`. Branch de self-heal de ponteiro obsoleto mantido por defesa (mesma postura de invitation-purge/D-182), embora nenhum caminho de escrita real produza esse caso hoje para nenhuma das duas entidades. `GlobalIndexComponent` ganha `"transient-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.transient_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-transient-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath`, nunca a comparação de string que falha silenciosamente no Windows), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e excluindo corretamente `UploadSlot` `RESERVED`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 7 workers já migrados. **G-V3**: teste de gravação do ponteiro na criação do `WebhookInbox` (`ses-callback-workflow.test.ts`), testes de gravação/atualização do ponteiro nas transições `CONSUMED`/`EXPIRED` do `UploadSlot` (`advance-after-evidence.test.ts`/`upload-slot-reconciliation.test.ts`), 22 testes do worker (purga dos 2 tipos, `RESERVED` nunca candidato, boundary exato para ambas janelas, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant, self-heal de ponteiro obsoleto) + 20 testes do backfill (parseArgs/encodeKey-decodeKey/deriveDue cobrindo os 2 tipos e exclusão RESERVED, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida, página mista). Suíte completa 1894/1894 (176 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #212 mergeado (commit `d32957f`→`ca50bcf` em `main`), CI real (`33695255525`/`33695850896`) e CD real (`33696134854`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-transient-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyTransientPurge` com `LeadingKeys` exato `["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItemsTransientPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#TRANSIENT` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT` (os 6 já migrados) → `implicitDeny` nos 6 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (WebhookInbox, UploadSlot)` sobre as 58 linhas da tabela (`ScannedCount=58`, `Count=0`, sem `LastEvaluatedKey` — tabela inteira coberta), e por `Query GSI8 WORK#TRANSIENT` retornando `Count=0`; nada para escrever, execução "real" (não-dry-run) seria um no-op confirmado, não executada. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sétima fatia mecânica seguindo o padrão de D-187 (poison-record/DLQ, entidade create-once) para `WebhookInbox` e D-182 (ponteiro atualizado a cada transição) para `UploadSlot`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (7 de 9 workers migrados)** — `transient-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 2 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/transient-purge-gsi8.ts` (novo), `src/modules/document/domain/upload-slot.ts`, `src/modules/document/application/advance-after-evidence.ts`, `src/modules/notification/application/ses-callback-workflow.ts`, `src/workers/upload-slot-reconciliation/reconciliation.ts`, `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-transient-purge.ts` (novo), testes em `test/unit/{shared,workers,document,notification}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:210:| D-189 | **MaintenanceDueIndex — oitava fatia (delivery-record-purge, D-179 slice 8, 8º de 9 workers)**: `delivery_record_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["delivery_record_purge"]`/`worker_transact_write_policy_json["delivery_record_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItems` na tabela base, ambos só no role do `delivery_record_purge_handler`. Este worker cobre DOIS tipos de entidade (`NotificationIntent`/`NotificationAttempt`, `src/modules/{reminder,notification}/domain/*.ts`), elegibilidade de idade única (`createdAt+180d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Diferença real de forma vs. D-187 (o análogo mais próximo, mesmo padrão poison-record/DLQ)**: AMBAS entidades carregam um contador OCC `version` real (ao contrário das 4 entidades append-only de D-187) — mesma forma que D-188/`UploadSlot`/`WebhookInbox`; o `Delete`/backoff/DLQ do worker e o backfill novo condicionam em `version` exato, com `createdAt` reasserted como defesa em profundidade adicional (nada muta `createdAt` hoje, mas a condição torna estrutural, não só verdadeiro por acidente). Novo `src/shared/delivery-record-gsi8.ts` (fora de `domain/` de módulo específico, já que a entidade cobre 2 módulos — mesma razão de `security-audit-gsi8.ts`/`transient-purge-gsi8.ts`): `deriveDeliveryRecordMaintenanceDue()` (due = `createdAt+180d`, NUNCA `undefined` — sem estado terminal, cada registro de entrega expira pelo prazo fixo independentemente de qualquer ação) e `deliveryRecordGsi8Keys()`. **3 pontos de escrita reais confirmados por grep** (não presumidos) gravam o ponteiro GSI8 UMA ÚNICA VEZ, no MESMO `Put` da criação: `reminder-dispatch/dispatch.ts` (intent `EXPIRATION_REMINDER`), `notification-router-workflow.ts`'s `applyStaleDecision` (intent `REPLACEMENT`/`CORRECTIVE`) e `applyRoutedDecision` (o `NotificationAttempt`) — nenhum dos dois entidades é atualizada depois de criada nestes caminhos (o `version` existe para as próprias transições de status do módulo, não para uma atualização do ponteiro GSI8). `delivery-record-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (handler perde a dependência separada de `TenantLifecycleStatusSource`, dobrada dentro do `ConditionCheck` transacional); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `createdAt`, na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#DELIVERY_RECORD"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência de D-187, mesma causa): nenhuma das duas entidades é atualizada depois de criada NA PRÁTICA (sem caminho de undelete, sem transição de status nomeada nos comentários do próprio worker) — o ponteiro é gravado exatamente uma vez, e a única forma de uma linha deixar de ser candidata é este worker deletá-la. **Gap conhecido e aceito, herdado de D-152, NÃO tocado por esta fatia**: `NotificationAttemptLookup` (ponteiro derivado, `PK=TENANT#t#ATTEMPT#id`) continua NÃO sendo purgado por este worker e fica órfão depois que um `NotificationAttempt` é deletado — confirmado ainda válido após esta migração por releitura direta, não presumido; fora de escopo aqui como já era em D-152. `GlobalIndexComponent` ganha `"delivery-record-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.delivery_record_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-delivery-record-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e condicionado em `version` (não `createdAt`, adaptação real vs. o template de D-187). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 8 workers já migrados. **G-V3**: 3 testes de gravação do ponteiro na criação (via os fixtures reais de `dispatch.test.ts`/`notification-router-workflow.test.ts` — intent do dispatch, intent REPLACEMENT do stale-decision, attempt do roteamento feliz), 24 testes do worker (purga dos 2 tipos, due no futuro nunca candidato, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `createdAt`/`version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo os 2 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa local 1912/1912 (178 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #215 mergeado (`6d333e0`→`c595190`), CI real (`33699314795`/`33699331683`) e CD real (`33699827095`) `success`, confirmados via `gh run view`/`gh run list` direto (polling síncrono, nunca aguardando notificação de background como subagente). **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-delivery-record-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyDeliveryRecordPurge` com `LeadingKeys` exato `["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItemsDeliveryRecordPurge` escopada à tabela base); `get-function-configuration`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#DELIVERY_RECORD` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT` (os 7 já migrados) → `implicitDeny` nos 7 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (NotificationIntent, NotificationAttempt)` sobre as 58 linhas da tabela (`scannedTotal=58`, `matched=0`, tabela inteira coberta), e por `Query GSI8 WORK#DELIVERY_RECORD` retornando `Count=0`; nada para escrever, execução real (não-dry-run) seria um no-op confirmado, não executada (mesma disciplina de D-187/D-188). **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — oitava fatia mecânica seguindo o padrão de D-187/D-188 (poison-record/DLQ), adaptada para 2 tipos de entidade COM `version` real, condicionado em `version`+`createdAt`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (8 de 9 workers migrados)** — `delivery-record-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); o único worker restante nomeado por D-179 permanece em `Scan`+`Limit`: `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/delivery-record-gsi8.ts` (novo), `src/workers/reminder-dispatch/dispatch.ts`, `src/modules/notification/application/notification-router-workflow.ts`, `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-delivery-record-purge.ts` (novo), testes em `test/unit/workers/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:211:| D-190 | **MaintenanceDueIndex — nona e ÚLTIMA fatia (core-user-data-purge, D-179 slice 9, 9º de 9 workers) — D-179 FECHADO POR COMPLETO.** `core_user_data_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["core_user_data_purge"]`/`worker_transact_write_policy_json["core_user_data_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItems` na tabela base, ambos só no role do `core_user_data_purge_handler`. Este worker cobre DOIS tipos de entidade (`ExpirationItem`/`ReminderPolicy`, `deletedAt+30d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Forma distinta de D-186/D-187/D-188/D-189 (create-once/janela fixa) — mesma forma do piloto original, D-180/`membership-purge`**: elegibilidade é gated numa TRANSIÇÃO de soft-delete (`deletedAt` sendo setado), não na criação — `deriveCoreUserDataMaintenanceDue()` (`src/shared/core-user-data-gsi8.ts`, novo, fora de `domain/` de módulo específico já que a entidade cobre 2 módulos — `expiration`/`reminder`) retorna `undefined` enquanto `deletedAt` está ausente; uma linha nunca vira candidata antes do soft-delete. **Achado real de pré-existência, confirmado por grep amplo (não presumido) antes de qualquer código novo**: `ExpirationItem.deletedAt` tem exatamente UM write path real — `expiration-service.ts#deleteItem` (via `transitionStatus`'s `extraSet`) — onde o ponteiro GSI8 foi wireado. `ReminderPolicy.deletedAt` NÃO TEM write path real nenhum no código hoje (`disablePolicy` só seta `enabled: false`, nunca `deletedAt`) — **condição PRÉ-EXISTENTE, não introduzida por esta migração**: o worker `Scan`-based anterior já tinha o mesmo gap (seu filtro `attribute_exists(deletedAt)` sobre `ReminderPolicy` também nunca poderia casar, pela mesma razão). `core-user-data-gsi8.ts` ainda deriva/gera chaves para `ReminderPolicy` de forma uniforme com `ExpirationItem` — no dia em que um soft-delete real for adicionado para ele, wireara o ponteiro é uma chamada de uma linha, sem mudança adicional no módulo compartilhado. **Mudança de forma real e esperada vs. o texto original do worker (D-151)**: `candidate-source.ts`'s comentário original dizia "plain `DeleteCommand`, not `TransactWriteItems` — D-151's design named no purge receipt"; isso muda agora porque o `ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` da claim GSI8 precisa estar na MESMA transação do `Delete` (mesmo raciocínio de todo outro worker migrado com fence de tenant) — mudança correta e esperada, não um desvio a evitar. `core-user-data-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `deletedAt` exato — defesa em profundidade contra uma corrida de "undelete", mesmo sem caminho de undelete real existir hoje para nenhuma das duas entidades, mesma disciplina de D-151/D-189). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188/D-189): backoff exponencial capado via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#CORE_USER_DATA"` após `MAX_ATTEMPTS=5`. `GlobalIndexComponent` ganha `"core-user-data-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.core_user_data_purge_handler.function_name`. Novo `scripts/backfill-gsi8-core-user-data-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, só linhas já soft-deletadas (nunca escreve ponteiro para uma linha sem `deletedAt`), vencimento FUTURO e passado. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 9 workers — **matriz de isolamento completa, todos os 9 namespaces GSI8 agora provados mutuamente exclusivos**. Suíte completa local 1929/1929 (179 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`). PR #217 mergeado (`1633d6c`→`1e483c1`), CI real (`33702974051`) e CD real (`33703151890`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-core-user-data-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyCoreUserDataPurge` com `LeadingKeys` exato `["WORK#CORE_USER_DATA","DLQ#CORE_USER_DATA"]` + `TransactWriteItemsCoreUserDataPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 5 * * ? *)`). **Prova de isolamento cross-namespace — a mais ampla do programa inteiro, 8 verificações negativas** (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8): `WORK#CORE_USER_DATA` (próprio) → `allowed`; os 8 namespaces já migrados (`WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT`/`WORK#DELIVERY_RECORD`) → `implicitDeny` nos 8 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard roda de fato): 0 candidatos — confirmado por `Scan` bruto cobrindo as 58 linhas inteiras da tabela filtrado por `entityType IN (ExpirationItem, ReminderPolicy)` retornando `Count=0` (a tabela `dev` não tem hoje NENHUMA linha desses dois tipos, muito menos soft-deletada), e por `Query GSI8 WORK#CORE_USER_DATA` retornando `Count=0`; execução real seria um no-op confirmado. **D-179 (MaintenanceDueIndex) está FECHADO — os 9 workers nomeados pelo design estão 100% migrados, verificados ao vivo em `dev`, com prova de isolamento IAM cross-namespace completa**: `membership-purge` (D-180/D-181), `invitation-purge` (D-182), `document-file-reconciliation` (D-183), `requirement-reindex` (D-185), `quota-telemetry-purge` (D-186), `security-audit-purge` (D-187), `transient-purge` (D-188), `delivery-record-purge` (D-189), `core-user-data-purge` (D-190). **Item pendente carregado, ainda não resolvido, agora o item real mais proeminente desta área**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia; como D-179 fecha aqui, este é o item de maior prioridade real que sobra nesta área de trabalho. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — nona e última fatia mecânica, mesma forma do piloto original D-180 — transição de soft-delete, não criação) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO — D-179 COMPLETO (9 de 9 workers migrados)** — `core-user-data-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo completa contra os 8 outros namespaces); nenhum worker nomeado por D-179 permanece em `Scan`+`Limit` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/core-user-data-gsi8.ts` (novo), `src/modules/expiration/application/expiration-service.ts`, `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-core-user-data-purge.ts` (novo), testes em `test/unit/{expiration,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
docs/architecture/decisions-log.md:234:| D-203 | **Duas correções mecânicas de pendências já nomeadas (nível 1-2/3-4, sem protocolo)**: (1) **guard `main()` do Windows/Git Bash** (pendência de D-186→D-190): os 6 scripts de backfill que ainda usavam `import.meta.url === \`file://${process.argv[1]}\`` (nunca casa no Windows/Git Bash) — `backfill-gsi8-document-file-reconciliation.ts`, `backfill-gsi8-invitation-purge.ts`, `backfill-gsi8-membership-purge.ts`, `backfill-gsi8-requirement-reindex.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts` — migrados para `fileURLToPath(import.meta.url) === process.argv[1]`, mesmo padrão já usado por `backfill-gsi8-core-user-data-purge.ts` desde D-186. (2) **`RT-LANE-FALLBACK-01`** (pendência nomeada por D-191, achado do Codex sob o critério C7): `tenant-business-mutation.ts` classificava `CancellationReasons` ausente/malformado como `TenantNotActiveError`, afirmando uma causa que o DynamoDB não revelou — corrigido para um `ConflictError` genérico/indeterminado nesse caso; `TenantNotActiveError` agora só é lançado quando `CancellationReasons` efetivamente nomeia o índice do fence como a causa. 3 sites de call (`executeTenantBusinessMutation`, docblock, 2 testes adversariais em `test/unit/tenant-lifecycle.test.ts`) atualizados; nenhum caller de `tryTenantBusinessMutation`/`executeTenantBusinessMutation` distingue este ramo hoje (nunca ocorre contra AWS DynamoDB real). Também removidos 3 arquivos `.md` órfãos na raiz do repo (duplicatas byte-idênticas já filed em `docs/architecture/roadmap-evolution/18-20`, violavam o `ROOT_MD_ALLOWLIST` de `check-doc-drift.ts`). Gate local completo verde: `typecheck`, `lint`, suíte completa, `check-docs`, `validate-schemas`. | Nível 1-2 (guard bug, cleanup) / Nível 3-4 (ConflictError, muda comportamento de lane compartilhada) | — | — | **CORRIGIDO, mergeado em `main` (PR #236, `e87d016`), CI 100% verde** (guardrails/dynamodb-integration/frontend/Validate Infra) — mecanismo nunca dispara em `dev` real com dado sintético atual, verificação ao vivo é formalidade | `scripts/backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `scripts/{backfill-reminder-policies,reset-dev-data}.ts`, `src/shared/tenant-lifecycle/tenant-business-mutation.ts`, `test/unit/tenant-lifecycle.test.ts` | 2026-09-05 |
docs/architecture/cost-model.md:43:No MVP, `tenantId = userId` (SCALE-004), então custo por tenant = custo por usuário ativo/mês, já calculado acima (≈US$0,045 com WhatsApp, ≈US$0,0045 sem). Quando Organizations existir (FUT-001), custo por tenant passa a agregar todos os usuários da organização — o `TenantQuota` (`data-model.md`) já é a estrutura de dados que permite medir consumo real por tenant no futuro, não apenas estimar.
docs/architecture/cost-model.md:54:Este documento modela custo esperado sob operação normal; os controles que protegem contra custo **anômalo** (G6, COST-004/005) já estão decididos em `architecture-fase3-consolidada.md` §14 e não são redecididos aqui — apenas referenciados para rastreabilidade: AWS Budgets com alarme em 80%/100% do teto por ambiente (COST-003), Cost Anomaly Detection, quota por tenant via token bucket em DynamoDB (`TenantQuota`, `data-model.md`), kill switch via AppConfig para operações caras (IA/OCR/WhatsApp). Este cost-model fornece os valores de referência (tabela acima) que alimentam os tetos configurados nesses Budgets — não os substitui.
src/shared/observability\security-audit.ts:13: * auto-injects correlationId/tenantId from the AsyncLocalStorage context - CloudWatch Logs is
src/shared/observability\logger.ts:7: * tenantId may appear in structured logs for investigation (per #14.1) but must never become
src/shared/observability\logger.ts:21:  tenantId?: string;
src/shared/observability\context.ts:10:  tenantId?: string;
rg: src/modules/quota: O sistema não pode encontrar o arquivo especificado. (os error 2)
rg: infra/modules/monitoring: O sistema não pode encontrar o arquivo especificado. (os error 2)

2026-09-08T02:46:12.692847Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 93 column 5
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n -i "cross-tenant|vazamento|leak|IDOR" docs/architecture/decisions-log.md | Select-Object -First 80; rg -n "TenantEntitlement|TenantQuota|quota|entitlement" src/modules src/shared test/unit | Select-Object -First 120; rg -n "quiet|timezone|DST|opt.out|replacement|corrective|stale|duplicate|UNKNOWN|AMBIGUOUS" src/modules/reminder src/modules/notification src/workers test/unit/reminder* test/unit/notification | Select-Object -First 160; rg -n "organization|Membership|TenantLifecycle|GlobalUser|TenantEntitlement|TenantQuota" scripts/reset-dev-data.ts' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 3481ms:
72:| D-052 | Com M9-M11 deployados, avaliado se/o que implementar de M12 (Commercial Monetization/Billing) agora. Protocolo Claude↔Codex completo (2 rodadas, convergência forte já na rodada 1): `Subscription`/`BillingWebhookInbox`/verificador de webhook não têm nenhum uso real possível antes da escolha do provider de pagamento (decisão de produto do Marcelo, explicitamente fora do roadmap) — implementá-los agora seria código morto com risco real de abstração errada (assinatura/replay/shape de evento variam por vendor). Alternativa mais estreita cogitada (operação administrativa manual para expandir `TenantEntitlement` de um early-adopter negociado direto) também descartada na rodada 2: `src/modules/identity/domain/authorization.ts` não tem nenhum conceito de "platform staff" cross-tenant hoje (toda `Action` é escopada dentro do próprio tenant, `OWNER\|MEMBER\|VIEWER`) — criar essa action exigiria ou um role novo para um caso ainda inexistente, ou uma action `ADMIN_ROLES` tenant-scoped que deixaria o próprio `OWNER` aumentar seu limite sozinho (bypass de quota self-service, vetor de abuso que o projeto evita, mesmo cuidado de D-045). **M12 inteiro fica bloqueado, zero código novo.** Caminho aceito para o caso hipotético de early-adopter: `UpdateItem` manual via os builders OCC-safe já existentes (`buildVersionedUpdate`, `src/shared/dynamodb/occ.ts`), só quando/se o caso for real — nunca um script ou endpoint dedicado hoje | Type 1 (nível 5-6, decisão de arquitetura/segurança — decide NÃO implementar, não decide como implementar) | 9,3 | 9,4 | **APPROVED** (M12 bloqueado por decisão de produto — fornecedor de pagamento — até novo gatilho real) | `docs/architecture/roadmap-evolution/15-m12-billing-scope-decision.md` | 2026-08-23 |
89:| D-069 | W3-07 — sessão de implementação (chunk 3/N), roteiro `Q` itens 2 (conclusão) e 3 (completo): lane `SystemMutation` (`src/shared/tenant-lifecycle/system-mutation.ts`) — allowlist fechada por união discriminada (`SystemMutationOperation`: `LIFECYCLE_TRANSITION`/`PURGE_DELETE`/`OUTBOX_BOOKKEEPING`, per o allowlist nomeado da Rodada E/§Q). `executeSystemMutation` não aceita `TransactWriteEntry[]` do chamador (diferente de `TenantBusinessMutation`) — só a operação tipada; a própria função constrói o array via `occ.ts`. Só `LIFECYCLE_TRANSITION` tem implementação real nesta sessão (`transitionTenantLifecycle` — primitivo real, OCC-fenced na versão do registro + `assertValidTransition` + `ConditionCheck` extra no status atual, valida `ACTIVE→DELETING` de ponta a ponta); `PURGE_DELETE`/`OUTBOX_BOOKKEEPING` são membros reservados do allowlist (tipados, mas lançam `SystemMutationNotImplementedError` se chamados — não fallback silencioso). **Boundary estrutural real (roteiro `Q` item 3, pendência explícita de D-068)**: regra nova `no-raw-dynamodb-writes-outside-lanes` em `.dependency-cruiser.cjs` — confina import de comandos de escrita cru do `@aws-sdk/lib-dynamodb` (`PutCommand`/`UpdateCommand`/`DeleteCommand`/`TransactWriteCommand`/`BatchWriteCommand`) a `src/modules/*/persistence/`, `src/modules/*/composition/`, `src/shared/dynamodb/`, `src/shared/outbox/persistence/` — qualquer outro arquivo em `src/modules/**`/`src/shared/**` que importe esse pacote falha `npm run check-boundaries` (rodado no CI). Prova real de que a regra pega um bypass de verdade (não só existe): `test/architecture/tenant-fence-boundary.test.ts` (categoria nova, `test/architecture/`, adicionada a `vitest.config.ts`) planta 3 arquivos-fixture reais em `src/` (import direto de `PutCommand`, de `TransactWriteCommand` sem `ConditionCheck` de lifecycle, e de `BatchWriteCommand` em `src/shared/` fora de `dynamodb/`/`outbox/persistence/`), roda o CLI real do `depcruise` contra `src` com o config real, confirma violação reportada com o nome da regra + caminho do arquivo, e remove os fixtures em `finally`/`afterEach` (mais um `beforeAll` de segurança). ESLint não foi estendido (a regra já registrada em `.eslintrc.cjs`'s comentário — dependency-cruiser é a "AUTHORITATIVE boundary check", ESLint só feedback de editor — não havia padrão equivalente aplicável a "não importar um pacote npm específico" que justificasse duplicar em `no-restricted-imports`, texto de import já cobre isso de forma idêntica e ESLint não adiciona poder de detecção aqui). Avaliado como nível 3-4 (implementação direta do roteiro já aprovado em D-066/D-067, mecanismo reusa builders de `occ.ts` já revisados e o padrão de dependency-cruiser já existente desde 2026-08-19) — protocolo `AGENTS.md` §4 completo não reaberto | Type 1 (nível 3-4, implementação direta de decisão já aprovada) | N/A — protocolo completo não aplicável | N/A | **IMPLEMENTED + UNIT TESTED + boundary mechanism proven by adversarial test** (13 testes novos: 8 de `SystemMutation`/`transitionTenantLifecycle` em `test/unit/system-mutation.test.ts` incluindo transição ilegal, conflito de versão OCC, BLOCKED/HELD, isolamento cross-tenant, allowlist não-implementado, e um `@ts-expect-error` provando a contenção em nível de tipo; 5 testes de arquitetura em `test/architecture/tenant-fence-boundary.test.ts` incluindo os 3 ataques de bypass adversariais pedidos pelo prompt da sessão. 978 testes de backend passando (111 arquivos, era 965), typecheck/lint/check-boundaries/check-docs limpos, nenhuma regressão). **Explicitamente NÃO feito nesta sessão** (fica para o próximo chunk, ver `NEXT_SESSION_PROMPT.md`): migração dos demais writers tenant-scoped listados em D-068 continua pendente; `PURGE_DELETE`/`OUTBOX_BOOKKEEPING` continuam sem implementação real (nenhum call site ainda precisa deles — o sweeper permanente e o outbox bookkeeping explícito são roteiro `Q` itens 5 e além); nenhum orquestrador real (Step Functions/Lambda) chama `transitionTenantLifecycle` ainda — só o primitivo existe, testado isoladamente; Codex adversarial review desta sessão não executado (sem orçamento restante), recomendado como próximo passo antes de expandir `SystemMutation` para `PURGE_DELETE`/`OUTBOX_BOOKKEEPING` de verdade, dado que essas são as operações de maior privilégio do sistema | `src/shared/tenant-lifecycle/system-mutation.ts`, `.dependency-cruiser.cjs`, `test/architecture/tenant-fence-boundary.test.ts` | 2026-08-29 |
98:| D-078 | W3-07 — mesma sessão de D-076/D-077, item 3 do prompt de retomada: mitigação defensiva barata para o risco de admissão parcial em `import-service.ts`'s `reserveImport()`, sem forçar a decisão de produto (unificar quota+idempotency+job-create numa única transação maior, trade-off latência-vs-atomicidade) que D-074/D-075 deixaram deliberadamente para o dono do produto. `idempotency.begin()` e as duas reservas `quota.consume()` (IMPORT_COUNT/IMPORT_BYTES) continuam fora da transação fenceada que cria o `ImportJob` — isso NÃO muda. O que muda: quando a criação fenceada falha (rejeição da fence OU um conflito OCC comum), o `catch` agora libera best-effort o que já tinha sido reservado — `idempotency.abort()` (a mesma primitiva já usada por `renewItem`'s caminho de conflito OCC, "idempotency liveness residual") e `quota.release()` para os dois tipos — antes de relançar o erro original. Toda compensação roda via `Promise.allSettled` (nunca deixa uma falha de compensação mascarar o erro real nem travar a resposta ao chamador). Sem isso: uma rejeição deixava a chave de idempotência presa em `IN_PROGRESS` para sempre (todo retry, mesmo depois do tenant voltar a ACTIVE, batia em `ConcurrentOperationError` permanentemente) e a quota consumida vazava até expirar a janela — com isso, uma rejeição volta o tenant ao estado pré-tentativa (quota liberada, idempotency key livre para nova tentativa), sem alterar o que já era aceito por design para essa classe de risco (uma falha DEPOIS da transação fenceada, ex. entre commit e presign, continua não coberta — fora do escopo desta mitigação). **Achado real de infraestrutura de teste descoberto e corrigido nesta sessão**: `InMemoryImportStore.transactWrite` (o fake usado pelos testes de `ImportService`) só reconhecia a convenção `#version = :expectedVersion`/`#setN`/`:setN` de `buildVersionedUpdate` para condição e aplicação de `Update` — a condição `#status = :expected` de `transitionIdempotencyStatus` (usada por `idempotency.abort()`) nunca era avaliada (sempre "passava" silenciosamente) e o SET correspondente (`#status`/`#requestHash`/`#responseRef`/`#completedAt`, nomes literais, não a convenção `#setN`) nunca era aplicado — ou seja, `abort()` "funcionava" sem erro mas não mudava nada de verdade, mascarando completamente a mitigação nos testes. Generalizado para um parser genérico de `ConditionExpression`/`UpdateExpression` (`splitTopLevelAnd`/`evaluateEqualityClause`/`applyUpdateExpression`) que avalia QUALQUER cláusula de igualdade `#name = :value` e aplica QUALQUER `SET`/`REMOVE` nomeado, não só a convenção `#setN` — acerta tanto o caso antigo quanto o novo sem duplicar lógica. **IMPLEMENTED + UNIT TESTED** (2 testes adversariais novos em `test/unit/import/import-service.test.ts`: rejeição por DELETING libera a quota IMPORT_COUNT/IMPORT_BYTES de volta a 0; rejeição por DELETING aborta o idempotency record, permitindo retry bem-sucedido com a MESMA Idempotency-Key uma vez o tenant voltando a ACTIVE). 1034 testes de backend passando (era 1032), typecheck/lint/check-boundaries/check-docs limpos, zero regressão | Type 1 (nível 3-4, mitigação defensiva mecânica que não altera a fronteira transacional nem o trade-off já identificado como decisão de produto) | N/A — mitigação de menor esforço já sugerida pelo Codex em D-075, não uma nova decisão de arquitetura | N/A | **IMPLEMENTED + UNIT TESTED** — a admissão parcial em si (quota/idempotency fora da transação fenceada) permanece decisão de produto pendente, mas o vazamento residual de uma tentativa REJEITADA (quota presa, idempotency key presa) está fechado | `src/modules/import/application/import-service.ts`, `test/unit/import/import-service.test.ts`, `test/unit/import/in-memory-store.ts` | 2026-08-29 |
99:| D-079 | W3-07 — terceira rodada de revisão adversarial Codex, pedida pelo prompt de retomada da sessão, especificamente re-checando o fechamento do gap PK/SK/TableName (D-076) e o teste do allowlist `SystemMutation` (D-077), sobre o diff acumulado desde `3e590ca` (D-076/D-077/D-078). Primeira tentativa de invocação falhou por bloqueio de política do próprio Codex CLI numa das suas ferramentas internas (`powershell.exe` com pipeline complexo) mais um flag de "possible cybersecurity risk" do lado do provedor — não um travamento (CPU não ficou preso perto de zero por tempo desproporcional, terminou com exit code 1 e mensagem de erro explícita); reformulado com linguagem menos "adversarial" (trocado "adversarial"/"bypass"/"hunt" por "independent"/"gap"/"look critically" no prompt, mantendo o conteúdo técnico idêntico) e reinvocado com sucesso na segunda tentativa, mesmo padrão `codex exec --skip-git-repo-check -C <repo> - < prompt.txt > output.txt 2>&1` em primeiro plano. **Nota do Codex: 6,0/10** — ainda abaixo do gate de 9,0. **2 achados BLOQUEANTES reais, confirmados por releitura do código, não refutados**: (1) a mitigação de compensação de `import-service.ts` (D-078) só envolvia a criação fenceada do `ImportJob`, deixando fora do try/catch os dois `quota.consume()` — um retry com a MESMA idempotency key consumia quota de novo a cada chamada mesmo quando `begin()` retornaria `COMPLETED_SAME_REQUEST`; um segundo chamador concorrente com a MESMA key perdia a corrida em `begin()` (erro de conflito genuíno) DEPOIS de já ter consumido quota, vazando permanentemente; uma falha no SEGUNDO `quota.consume()` (IMPORT_BYTES) depois do primeiro já ter sucedido (IMPORT_COUNT) não tinha nenhuma compensação. (2) o architecture-test do allowlist (D-077) provava só que UM `kind` sentinela específico (`DELETE_EVERYTHING_FOR_THIS_TENANT`) está ausente da união — não prova que a união está fechada contra um `kind` genuinamente novo e imprevisto que um futuro contribuidor adicionasse (junto com o `buildEntries` case correspondente); o Codex sugeriu concretamente asserções de tipo bidirecionais (`SystemMutationOperation["kind"] extends ApprovedKind` e vice-versa) contra um allowlist mantido independentemente do módulo de implementação. **3 achados NÃO-BLOQUEANTES reais, corrigidos na mesma sessão**: (a) a descrição do teste "BYPASS ATTEMPT 2" alegava que os membros da união são "exact object shapes" — impreciso: TypeScript só rejeita campo excedente em um literal FRESCO, não numa atribuição via variável intermediária; comentário corrigido para não overclaim, mantendo a nota de que isso não importa para segurança hoje porque `buildEntries()` ignora campos extras. (b) confirmado (não corrigido, já correto): a checagem de `TableName` é sólida (nomes de tabela são case-sensitive no DynamoDB real, GSI não é um `TableName` alternativo válido, não há alias transparente) — "nenhum bypass prático de TableName encontrado". (c) confirmado como limitação real já documentada, não uma regressão: o check de PK só valida entradas cujo PK casa com `TENANT#<id>#...`; uma entrada `buildConditionalPut()` visando uma linha lógica não-prefixada de outro tenant (ex. `GUESTTOKEN#`/`IDENTITY#`) com um `tenantId` declarado forjado passaria os dois checks — nenhum call site real produz isso hoje (confirmado por grep), mas a frase "item 1 closed de verdade" no registro anterior era ampla demais; o que está fechado é o acesso cross-tenant dentro do keyspace `TENANT#...` universal atual, não a posse estrutural de tenant para toda entrada que a lane aceitaria. Achados corrigidos nesta mesma sessão de reavaliação: (1) `import-service.ts`'s `reserveImport()` reordenado — `idempotency.begin()` agora roda ANTES dos dois `quota.consume()` (a ordenação de menor esforço que o próprio Codex já tinha sugerido em D-075 e que a sessão anterior não tinha implementado), fechando os 3 vazamentos apontados: replay nunca mais toca quota (curto-circuita em `COMPLETED_SAME_REQUEST` antes de qualquer consumo); um chamador concorrente perdedor nunca toca quota (erro de conflito acontece antes); e ambos os `quota.consume()` agora vivem dentro do MESMO try/catch da criação do job, com rastreamento por-chamada (`countConsumed`/`bytesConsumed`) do que foi realmente reservado NESTA tentativa, para compensar só isso. (2) asserções de tipo bidirecionais adicionadas em `test/architecture/system-mutation-allowlist.test.ts` — `ApprovedSystemMutationKind` declarado de forma independente do módulo de implementação, com `AssertUnionIsSubsetOfApproved`/`AssertApprovedIsSubsetOfUnion` (checagem `extends` nos dois sentidos) que falham a compilação se a união de `system-mutation.ts` ganhar/perder um `kind` sem que este allowlist seja atualizado — sanity-checado manualmente nesta sessão (removendo `OUTBOX_BOOKKEEPING` do allowlist local e confirmando que `npm run typecheck` falha com a mensagem de erro esperada, depois revertido). **IMPLEMENTED + UNIT TESTED** (2 testes adversariais novos em `test/unit/import/import-service.test.ts` provando replay não cobra quota de novo e conflito genuíno de key não vaza quota; 1 teste-âncora novo em `system-mutation-allowlist.test.ts` documentando a prova de tipo). 1037 testes de backend passando (era 1034), typecheck/lint/check-boundaries/check-docs limpos, zero regressão. Achado NÃO-BLOQUEANTE (c) acima permanece pendência real documentada, não forçada nesta sessão — fechar de verdade exigiria um dos 3 caminhos maiores já listados (metadados obrigatórios pelos builders, um tipo de entrada tenant-branded, ou proibir estruturalmente qualquer entidade não-`TENANT#`-prefixed nesta lane) | Type 1 (nível 5-6, `AGENTS.md` §4 — terceira rodada de revisão adversarial sobre um fechamento de achados de segurança/modelo de dados já registrado) | N/A — revisão de implementação, não rodada de design com nota cega dupla Claude↔Codex | 6,0/10 (Codex, sobre o diff D-076/D-077/D-078, antes dos 2 fixes registrados nesta mesma linha) | **REVISADO E PARCIALMENTE RE-CORRIGIDO NA MESMA SESSÃO** — os 2 achados BLOQUEANTES do Codex round 3 foram corrigidos (reordenação idempotency-antes-de-quota em import-service.ts; asserção de tipo bidirecional independente para o allowlist); os 3 NÃO-BLOQUEANTES foram avaliados (1 corrigido — overclaim de comentário; 2 confirmados como já corretos ou como limitação residual real documentada, não mascarada). Uma QUARTA rodada Codex re-checando especificamente estes 2 fixes NÃO foi executada nesta sessão (orçamento esgotado) — nota atual (6,0/10) é sobre o estado ANTES destes 2 fixes, não uma reavaliação pós-fix; estado honesto: ainda não há confirmação Codex de que os fixes desta linha realmente fecham os achados, só a própria análise interna + testes | `src/modules/import/application/import-service.ts`, `test/unit/import/import-service.test.ts`, `test/architecture/system-mutation-allowlist.test.ts` | 2026-08-29 |
100:| D-080 | W3-07 — sessão de continuação: (1) narrowing do gap residual não-`TENANT#`-prefixed em `findTenantMismatch` (re-verificado por leitura de todo tipo de domínio real usando as chaves `IDENTITY#cognitoSub#`/`GUESTTOKEN#`/`SESSION#`/`LOGINATTEMPT#`/`TEXTRACTJOB#`: `IdentityMapping`/`GuestTokenPointer`/`Session`/`TextractJob` DECLARAM `tenantId`, já cobertos pelo check 1 existente se forjados; teste adversarial novo prova isso com a forma real de `GuestTokenPointer`); (2) quarta rodada Codex, pedida explicitamente pelo prompt de retomada, re-confirmando os 2 fixes BLOQUEANTES de D-079 (reordenação import-service.ts, asserção de tipo bidirecional do allowlist) sobre o diff acumulado desde `c496b91`. **Nota do Codex: 8,8/10** — primeira rodada a superar 8,0, ainda abaixo do gate de 9,0 mas por margem pequena. **Ambos os fixes de D-079 CONFIRMADOS CORRETOS, sem achado bloqueante remanescente**: (1a) reordenação idempotency-antes-de-quota fecha os 3 vazamentos originais (replay não recobra quota; perdedor de corrida não toca quota; falha na segunda reserva libera só a primeira) — Codex verificou que `idempotency.begin()` não tem nenhum modo de falha que toque quota, e que a interleaving entre `begin()` e o bloco try não introduz um caminho novo para os 3 vazamentos específicos; (1b) allowlist bidirecional (`AssertUnionIsSubsetOfApproved`/`AssertApprovedIsSubsetOfUnion`) confirmado como prova real de fechamento contra um `kind` genuinamente novo, não só o sentinela hard-coded anterior — só um contributor que edite AMBOS implementação e allowlist no mesmo diff (mudança visível em review) poderia "adicionar" um kind sem quebrar o build, o que não é drift silencioso. **1 achado real NÃO-BLOQUEANTE encontrado pelo Codex, corrigido na mesma sessão**: o narrowing do item 1 (LoginAttempt como única exceção real sem `tenantId` nem `TENANT#` PK) estava incompleto — `GuestRateLimitRecord` (`src/modules/subject/application/guest-rate-limiter.ts`, PK `GUESTTOKEN#<selectorHash>#RATE`) também não declara `tenantId`, por design (rate-limiting acontece antes do token de convidado resolver um tenant); Codex confirmou por grep que nem `LoginAttempt` nem `GuestRateLimitRecord` são roteados por `executeTenantBusinessMutation`/`tryTenantBusinessMutation` hoje, então o gap continua não-explorável, mas a documentação/teste que dizia "a única exceção real" precisava listar as duas — corrigido no cabeçalho de `tenant-business-mutation.ts` e no teste `KNOWN GAP` de `tenant-lifecycle.test.ts`. **1 achado NÃO-BLOQUEANTE adicional, corrigido preventivamente**: uma pequena janela de liveness em `import-service.ts`'s `reserveImport()` entre `idempotency.begin()` (ACQUIRED) e o bloco `try` que já cobria compensação — `newImportJobId()`/cálculo de `jobExpiresAt`/construção do objeto `job` ficavam FORA do try, então uma falha do gerador de ID injetado deixaria a idempotency key travada em `IN_PROGRESS` sem compensação alcançável (nenhuma quota tocada nesse ponto, então não é um dos 3 vazamentos originais, mas ainda um wedge real); movido para dentro do `try` para que `idempotency.abort()` cubra essa janela também. **IMPLEMENTED + UNIT TESTED** — nenhum teste novo necessário para os 2 fixes desta linha (mudança de documentação + reordenação mecânica sem nova superfície de comportamento observável; a suíte existente de `import-service.test.ts`/`tenant-lifecycle.test.ts` já cobre os caminhos afetados e passou sem alteração). 1038 testes de backend passando (era 1037), typecheck/lint/check-boundaries/check-docs limpos, zero regressão. Avaliação geral do Codex: os 8 mecanismos do fence (cross-validation tenant/entries, verificação de PK físico, fechamento do allowlist SystemMutation, ordenação de compensação import-service, hardening de CancellationReasons, fix TOCTOU do bootstrap, fix de resume BLOCKED/HELD, compensação de objeto órfão S3) formam "uma defesa coerente e substancialmente testada"; nenhum achado bloqueante novo de production-readiness encontrado; recomenda UMA rodada curta de correção-e-reconfirmação (as 2 correções já feitas nesta linha) antes de possivelmente atingir 9,0, e nota que rodadas adicionais amplas sobre este gate específico têm retorno decrescente a partir daqui, a menos que o time decida substituir o gap residual documentado (não-`TENANT#`, 2 entidades reais nunca exercitadas) por um design estrutural maior (metadados obrigatórios pelos builders, tipo de entrada tenant-branded) | Type 1 (nível 5-6, `AGENTS.md` §4 — quarta rodada de revisão adversarial sobre um fechamento de achados de segurança/modelo de dados já registrado) | N/A — revisão de implementação, não rodada de design com nota cega dupla Claude↔Codex | 8,8/10 (Codex, sobre o diff acumulado desde `c496b91` incluindo os 2 fixes de D-079 e o narrowing do item 1 desta sessão) | **REVISADO, AMBOS OS FIXES DE D-079 CONFIRMADOS, 2 ACHADOS NÃO-BLOQUEANTES NOVOS CORRIGIDOS NA MESMA SESSÃO** — ainda abaixo do gate de 9,0 por margem pequena; Codex explicitamente não pediu nova rodada ampla, só a confirmação mecânica destas 2 correções, que já foram feitas; achado residual documentado (LoginAttempt + GuestRateLimitRecord sem tenantId nem TENANT# PK, nunca roteados por esta lane) permanece pendência real, avaliação honesta de que fechar por completo exigiria redesign estrutural maior, não uma sessão de correção | `src/shared/tenant-lifecycle/tenant-business-mutation.ts`, `src/modules/import/application/import-service.ts`, `test/unit/tenant-lifecycle.test.ts` | 2026-08-29 |
103:| D-083 | W3-07 — sessão de continuação em outra máquina (2026-08-29, mesmo dia de D-082): antes de iniciar, um self-review (fora da rodada Codex) achou e corrigiu B7 — `verifyTenantDynamoPurgeEmpty` (a verificação do B2) excluía `TenantLifecycleRecord`/`IdentityMapping` de "itens restantes" só pelo atributo opcional `entityType`, a mesma fraqueza que B3 já tinha fechado no lado do DELETE; corrigido com `isNeverPurgeCanonicalKey()` em `dynamo-tenant-purge.ts`, espelhando as duas checagens de chave canônica de `PURGE_DELETE`. Essa sessão foi interrompida logo depois (commit `8b6033c "trabalho interrompido"`) sem rodar a segunda rodada Codex nem atualizar `decisions-log.md`/`NEXT_SESSION_PROMPT.md`. **Esta sessão (máquina diferente, retomando via `git pull`) rodou a segunda rodada Codex pedida por D-082 e mais 3 rodadas de correção-e-reconfirmação sobre um único achado que sobreviveu 4 rodadas em variantes sucessivas — B6 (vínculo S3↔tenant)** — via MCP `codex/codex` (não a CLI `codex exec`, disponível diretamente nesta sessão; mesmo protocolo de conteúdo do `AGENTS.md` §4, mecanismo de invocação diferente). **Rodada 2 (nota 7,4/10)**: confirmou B1-B5 e B7 corrigidos; achou B6 **não fechado de verdade** — a asserção síncrona de D-082 só comparava `target.tenantId` (rótulo do chamador) contra `input.tenantId` (outro rótulo do chamador), nunca contra o `target.prefix` real, então `{ prefix: "tenant-b/", tenantId: "tenant-a" }` ao purgar "tenant-a" passava e apagaria dados do tenant B; achado não-bloqueante adicional: o teste de B7 só exercitava `TenantLifecycleRecord`, nunca `IdentityMapping` alcançado pelo scan ampliado do B1 via `tenantId = :tenantId` (cobertura corrigida com um fake `widenedSource` novo simulando o scan real pós-B1). **Rodada 3 (nota 8,0/10)** — fix aplicado: `prefixBelongsToTenant()` exigindo o tenantId como segmento de caminho `(^|/)${tenantId}(/|$)`; achado bloqueante novo: aceitava o tenantId no FIM do prefixo sem `/` depois (`"tenant/tenant-a"` sem barra final) — S3 casa por byte-prefix, não segmento de caminho, então isso ainda purgaria `tenant/tenant-a2/...`/`tenant/tenant-a-b/...` de outro tenant. **Rodada 4 (nota 8,2/10)** — fix aplicado: exigir `/` depois do segmento (`(^|/)${tenantId}/`, nunca `$`) + `tenantId` vazio rejeitado explicitamente; achado bloqueante novo, mais profundo: um segmento batendo em QUALQUER posição do prefixo (não ancorado ao início) ainda aceitava um prefixo genuinamente enraizado sob OUTRO tenant que só continha o tenantId purgado mais adiante no caminho (ex. `"tenant/tenant-b/item/tenant-a/"` para tenantId "tenant-a"). **Rodada 5 (nota 9,1/10, ZERO achados bloqueantes)** — fix aplicado: checagem ANCORADA no início do prefixo contra uma lista fechada `TENANT_PREFIX_ROOTS = ["clean/", "tenant/", "ocr/"]`, verificada por leitura direta de cada key builder real que grava nos buckets purgados (`clean-key.ts` → `clean/<tenantId>/...`; `quarantine-key.ts`/`import-raw-key.ts`/`submission-quarantine-key.ts` → `tenant/<tenantId>/...`; `s3-ocr-artifact-store.ts` → `ocr/<tenantId>/...` — o comentário anterior de `TenantS3Target.prefix`, herdado de D-082, estava impreciso quanto à raiz real de quarantine/import). Codex confirmou por leitura própria que a lista cobre os key builders reais e que as 5 variantes anteriores (rótulo falso, colisão de substring, sem barra final, segmento não-ancorado, tenantId vazio) agora falham corretamente; parecer final explícito: "o B6 está genuinamente fechado agora... pronto para avançar à próxima etapa". **2 achados NÃO-BLOQUEANTES aceitos, não corrigidos nesta sessão**: (a) a validação ainda não acopla cada raiz a um bucket específico (ex. `{bucket: "clean", prefix: "tenant/t1/"}` passa por pertencer ao tenant, mesmo sendo uma combinação bucket/raiz que nenhum key builder real produz) — não reabre purga cross-tenant (o prefixo ainda precisa pertencer ao MESMO tenant sendo purgado), documentado no doc comment de `TenantS3Target.prefix` como responsabilidade de um futuro composition root real (constrói cada target de uma tabela fechada bucket→raiz, não aceita `{bucket,prefix}` livre) — não fixado agora porque nenhum composition root existe ainda; (b) adaptadores reais (`tenant-purge-scan.ts`/`tenant-purge-s3-adapter.ts`) continuam sem teste de integração contra AWS real — gap já conhecido desde D-081, inalterado. Cada rodada de fix rodou `npm run typecheck`+`npm run lint`+`npm test` completo no ambiente local (filesystem gravável — o sandbox read-only do Codex não conseguia rodar Vitest por `EROFS` ao gravar `vitest.config.ts.timestamp-*.mjs`, só typecheck/lint) antes de pedir a rodada seguinte: zero regressão em toda a sequência, 1076→1080→1082→1084 testes de backend (14 novos ao final: 2 de B7/cobertura do scan ampliado, 8 de B6 nas 3 variantes de bypass + tenantId vazio + caminho feliz das 3 raízes reais, mais os 4 já contados na sessão anterior que ficaram sem confirmação Codex). **Nota Claude (auto-avaliação, protocolo de nota cega não aplicável aqui — sessão de correção iterativa sobre achados do Codex, não design com nota dupla simultânea): 9,2/10** — concordo com a nota final do Codex; o mecanismo agora fecha a classe de bug (não só a instância original), verificado por leitura direta de todo key builder real, não por suposição | Type 1 (nível 5-6, `AGENTS.md` §4 — segurança/privacidade/modelo de dados, LGPD, isolamento de tenant) | 9,2/10 (auto-avaliação, ver acima) | **9,1/10** (Codex, rodada 5, sobre o estado final após 4 correções sucessivas do mesmo achado B6) | **IMPLEMENTED + UNIT TESTED + CONFIRMADO PELO CODEX ≥9,0** — B1-B7 fechados e confirmados; B6 especificamente levou 4 rodadas de correção-e-reconfirmação até fechar a classe inteira do bug, não só a instância que cada rodada testava; pipeline considerado pronto pelo Codex para a próxima etapa real (decidir orquestrador) | `src/workers/tenant-purge/dynamo-tenant-purge.ts`, `src/workers/tenant-purge/purge-tenant.ts`, `test/unit/workers/tenant-purge/dynamo-tenant-purge.test.ts`, `test/unit/workers/tenant-purge/purge-tenant.test.ts` | 2026-08-29 |
108:| D-088 | Multi-User B2B, Wave B2B-2 (chunk 2/N, conclui a wave) — entidade `User` global aditiva implementada (`GlobalUserRepository`, `PK=USER#<userId>`, `SK=PROFILE`, `entityType: "GlobalUser"` para não colidir com `UserProfile` legado que usa `entityType: "User"`), criada como 4º item na `TransactWriteItems` já existente de `TenantBootstrapService.createAll()` (docs/architecture/multi-user-b2b-physical-model.md §1) — foundation puramente aditiva, nenhum consumidor real lê esta linha ainda; Wave B2B-3 é o primeiro consumidor real (`Membership.userId` referencia esta identidade). **Achado real de teste encontrado e corrigido durante a suíte completa** (não mascarado, não incidental): `test/integration/cross-tenant.test.ts`'s checagem de crossover negativo (M1 exit criterion, implementation-blueprint.md §19/§4.3) acusou corretamente a nova chave `USER#<userId>#PROFILE` como potencial vazamento de `tenantId` para fora do prefixo `TENANT#`, porque `tenantId=userId` ainda vale sob a regra MVP vigente (a remoção real dessa igualdade só ocorre em Wave B2B-5). Corrigido com uma exceção estreita, documentada, restrita à forma exata dessa chave conhecida — não um wildcard nem enfraquecimento da checagem para nenhuma outra entidade, que continua tendo que provar isolamento estrito por `TENANT#` como antes. Dois testes pré-existentes em `test/unit/identity/resolver.test.ts` também precisaram de correção (não achado novo, consequência mecânica esperada): a colisão de SK `#PROFILE` compartilhado entre `UserProfile` legado e `GlobalUser` quebrava uma asserção de contagem que não diferenciava as duas entidades por prefixo de PK — corrigido, e a asserção ficou mais precisa (conta as duas populações separadamente) do que estava antes. | Type 3-4 (implementação direta do design D-086 §1, sem novo access pattern — reaproveita transação já fenced) | N/A — implementação direta, não decisão nova | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries` limpos (385 módulos, 0 violação), suíte completa 1108/1108 (120 arquivos), zero regressão real após a correção do achado de teste acima | `src/modules/identity/persistence/global-user-repository.ts`, `src/modules/identity/application/bootstrap-identity.ts`, `test/unit/identity/resolver.test.ts`, `test/unit/bff/bff-auth-service.test.ts`, `test/integration/cross-tenant.test.ts`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
109:| D-089 | Multi-User B2B, Wave B2B-3 (chunk 1/N) — precondição de infra do GSI4 `MembershipByUser` implementada (docs/architecture/multi-user-b2b-physical-model.md §6): `GSI4` removido de `locals.tenant_facing_index_names` em `infra/modules/dynamo-table/main.tf` (estava lá desde sempre com zero consumidor real — inofensivo até agora, mas a nova semântica de `MembershipByUser` cruza tenants por design e não pode ficar acessível pela role tenant-facing geral) e política restrita dedicada `gsi4_read` + output `gsi4_read_policy_json` adicionados, mesmo padrão de `gsi3_read`/`gsi6_read` (`AGENTS.md` §7). Nenhuma role consome `gsi4_read_policy_json` ainda — Wave B2B-3 não adiciona nenhum consumidor real (BFF/`RequestContextResolver`/onboarding, que consumirão de fato, são Wave B2B-5/B2B-6). Dois novos `run` blocks em `infra/modules/dynamo-table/tests/dynamo_table_policy.tftest.hcl` (`tenant_facing_policy_excludes_gsi4`, `gsi4_narrow_policy_references_only_its_own_index`), mesmo padrão estrutural dos runs já existentes para GSI3/GSI6. | Type 5 (`change-risk-scale.md` — muda superfície de acesso IAM de um índice, mas implementação direta do design já `APPROVED` em D-086 §6, não uma decisão nova) | N/A — implementação direta, não decisão nova | N/A | **IMPLEMENTADO** — `terraform validate` limpo (módulo + root), `terraform test` via `aws --profile claude-dev` limpo (módulo 6/6 incl. os 2 novos runs; root 15/15, zero regressão nas 27 funções Lambda existentes) | `infra/modules/dynamo-table/main.tf`, `infra/modules/dynamo-table/outputs.tf`, `infra/modules/dynamo-table/tests/dynamo_table_policy.tftest.hcl`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
112:| D-092 | Multi-User B2B, Wave B2B-4 (Onboarding) — escopo `APPROVED` via protocolo Claude↔Codex, submetido a pedido explícito do Marcelo antes de qualquer código (primeira wave desde B2B-2 a tocar potencialmente wiring de login real). Achado central: o escopo literal do roadmap §109 ("remover tenant auto-provision silencioso; new User → Create Organization ou Accept Invitation") não pode ser wireado com segurança no fluxo de login real agora — verificado contra `resolve-request-context.ts`/`bff-auth-service.ts` que nenhum gate de Membership/GSI4 existe hoje, e todo usuário legado tem tenant funcional via `TenantBootstrapService` mas zero `Membership` real, então um gate ingênuo "zero Memberships → onboarding" mostraria tela incorreta para todo usuário legado. `Accept Invitation` também fora de escopo (depende de `Invitation`, formalmente Wave B2B-8). **Escopo final**: `OnboardingStateResolver` puro, não wireado, 4 estados (`HAS_USABLE_MEMBERSHIP`/`SUSPENDED_ONLY`/`LEGACY_TENANT_ONLY`/`NO_TENANT_NO_MEMBERSHIP`) via procedimento sequencial estrito (detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b4-scope.md`) — sem wiring em `bootstrap-identity.ts`/`resolve-request-context.ts`/`bff-auth-service.ts`, sem exposição HTTP (achado convergente da Rodada 1: superfície de deploy sem consumidor real). Remoção do auto-provision legado e gate real de login redesignados para Wave B2B-5 (§110 do roadmap já define isso textualmente). 4 rodadas, nota cega cada rodada: 7,2/8,4 → 9,1/9,1 → 8,7/8,8 (achado real: `REMOVED` indevidamente agrupado com `SUSPENDED`, contradizendo a própria premissa) → 9,3/9,4 (fechamento, procedimento sequencial estrito fecha o achado da Rodada 3 e, por construção, uma ambiguidade de precedência que o autograde tinha achado independentemente). Ainda não implementado — aguardando confirmação do Marcelo do escopo final antes de codificar. | Type 3-4 por natureza (implementação de decisão de escopo, não access pattern novo), mas submetido ao protocolo completo por pedido explícito do Marcelo dado o risco de tocar login real | N/A — decisão de escopo, não decisão técnica de arquitetura nova | N/A | **ESCOPO APROVADO, IMPLEMENTAÇÃO PENDENTE** | `docs/architecture/multi-user-b2b-wave-b2b4-scope.md`, `docs/architecture/reviews/multi-user-b2b-wave-b2b4-scoping/` | 2026-08-30 |
115:| D-095 | Multi-User B2B, Wave B2B-5 (RequestContext Cutover) — escopo `APPROVED` via protocolo Claude↔Codex, submetido por iniciativa própria (analogia direta com D-092: primeira wave desde B2B-4 a tocar `resolve-request-context.ts`/`bootstrap-identity.ts`/`bff-auth-service.ts` de verdade, eliminando o fallback `tenantId=userId`). O design técnico final já estava `APPROVED` desde D-086 (`multi-user-b2b-physical-model.md` §3/§10-13) — esta rodada não reabre esse design, decide só a decomposição da implementação e fecha 3 lacunas reais achadas por leitura direta do código (não do design doc): (1) não existe canal de transporte entre `activeOrganizationId` da sessão BFF e o `RequestContextResolver` do lado do recurso (`proxy-service.ts` só encaminha o access token Cognito bruto); (2) `Membership.role` tem 4 valores mas a matriz de autorização real em produção (`authorization.ts`) só conhece 3, sem `ADMIN`; (3) sem um primeiro consumidor HTTP para `CreateOrganizationService`, `bootstrapUser()` estado final deixaria todo login novo preso em `NO_TENANT_NO_MEMBERSHIP` sem saída. **Escopo final** (detalhe completo em `docs/architecture/multi-user-b2b-wave-b2b5-scope.md`): `IdentityMapping` tenantless + `bootstrapUser()` de 2 itens (renomeia `TenantBootstrapService`→`IdentityBootstrapService`, remove `UserProfile` legado); `DeviceSession` migra para `PK=USER#<userId>` (inatingível por W3-07/purge por construção de chave); `RequestContextResolver` reescrito com resolução de Membership única nomeada explicitamente (falha fechado se >1 `ACTIVE`, nunca "pega a primeira") e assert explícito (erro nomeado, testado) para `Membership.role` não suportado (`ADMIN`), em vez do fail-closed acidental via cast unsafe; `POST /bff/organizations` com **cap transacional** (`GlobalUser.hasCreatedOrganization` via `Update` condicionado na MESMA `TransactWriteItems` de `CreateOrganizationService`, 5 itens não 4 — `CreateOrganizationService` ganha `buildCreateEntries()` aditivo, sem mudar seu contrato para chamadores existentes); `Session.activeOrganizationId` como cache/hint de UX nunca usado para autorização, com self-heal em `GET /bff/session`/`handleCallback` quando a sessão está desatualizada mas uma Membership real já existe. `POST /bff/organization/select` e o transporte real de `activeOrganizationId` até o recurso **não nascem nesta wave** — desvio faseado explícito de §11/§12 do physical model, registrado (não alegado como cumprimento literal), fechado por Wave B2B-6 quando B2B-8 tornar multi-org alcançável. 3 rodadas, nota cega cada rodada: 7,8/7,8 → 8,7/8,6 → 9,1/9,1 (fechamento, ambos ≥9,0, sem arredondar — a única rodada deste projeto em que a nota da Rodada 1 saiu empatada entre os dois lados). Achados de implementação do Codex (builder tenantless para o `Update` do cap, distinguir `CancellationReasons` por índice, teste de fixture pré-cutover sem o atributo novo) registrados no documento de escopo para não se perderem até a implementação real. Ainda não implementado. | Type 3-4 por natureza (decisão de decomposição/escopo de implementação, não access pattern novo além do já aprovado em D-086), submetido ao protocolo completo por iniciativa própria dado o risco de tocar login real | N/A — decisão de escopo, não decisão técnica de arquitetura nova | N/A | **IMPLEMENTADO — ver D-096** (Marcelo deu o sinal para implementar após a apresentação do escopo; `UserProfile` NÃO foi removido como este escopo original previa — achado real corrigido durante a implementação, ver D-096) | `docs/architecture/multi-user-b2b-wave-b2b5-scope.md`, `docs/architecture/reviews/multi-user-b2b-wave-b2b5-scoping/` | 2026-08-30 |
123:| D-105 | Multi-User B2B, Wave B2B-10 (Tenant-aware Frontend) — escopo `APPROVED` via protocolo Claude↔Codex, 3 rodadas. Primeira aplicação do protocolo a uma decisão de FRONTEND nesta sessão (B2B-6/7/8/9 foram todas backend). Classificação por subitem, não nota única para a wave: a maior parte é implementação direta de contratos já `APPROVED` (nível 3-4, protocolo não obrigatório), só a arquitetura de isolamento de cache é Type 1/nível 5 (consequência de segurança real: vazamento de dado entre tenants na UI se malfeita). **Declaração E-014 SIM** para essa peça: "query keys escopadas por tenant" é padrão documentado pela própria TanStack Query v5 (já em uso), confirmado por fetch direto da doc oficial. **Achado real #1** (verificado lendo os 2 lados do contrato): `frontend/src/auth/AuthContext.tsx`'s `probe()` exigia `info.tenantId && info.userId`, campos que `GET /bff/session` nunca retorna desde B2B-5/D-095 — todo usuário autenticado era tratado como `SESSION_MISSING`, regressão real já em `develop`/`main`, nunca antes reconciliada porque nenhuma wave B2B tinha tocado o frontend. **Achado real #2**: nenhum serviço de B2B-8 atualiza `Organization.displayName`/`timezone` — writer novo necessário. **Rodada 1 (Claude 8,4/Codex 8,1, não aprovado)**: achado bloqueante — a proposta assumia que TanStack Query refaz fetch automaticamente ao mudar `organizationId` na queryKey, mas essa variável não é o que o `queryFn` real usa (o BFF injeta `X-Organization-Id` a partir da sessão server-side, o browser nunca a envia) — corrida real na janela entre `POST /select` e o refetch de sessão do cliente; inventário de query keys também incompleto (`Overview.tsx` usava uma chave inline nunca listada). **Rodada 2 (Claude 9,0/Codex 8,7)**: corrida fechada com gate `switching` + `cancelQueries` explícito + inventário refeito por grep exaustivo (7 `useQuery` + 5 `invalidateQueries`); achado bloqueante novo — `useActiveOrganization()` como hook reimplementado por chamador (não Context único) desincronizaria `switching` entre instâncias. **Rodada 3 (Claude 9,2/Codex 9,2, fechamento)**: `ActiveOrganizationProvider` (Context único) especificado + achado bloqueante final — `cancelQueries()` sozinho não aborta um `fetch()` real sem o `AbortSignal` do `QueryFunctionContext` propagado ponta-a-ponta pelos wrappers de API (`api/items.ts`/`api/subjects.ts` não aceitavam nem repassavam `signal` algum); corrigido, ambos ≥9,0 sem arredondar. | Type 1 (nível 5 de `change-risk-scale.md` só para a arquitetura de cache isolation — consequência de segurança/isolamento de dado; restante da wave nível 3-4) | 9,2/10 (Rodada 3) | 9,2/10 (Rodada 3) | **ESCOPO APROVADO — ver D-106 para implementação** | `docs/architecture/reviews/multi-user-b2b-wave-b2b10-scoping/` | 2026-08-30 |
129:| D-113 | Multi-User B2B, Wave B2B-13 (E2E/Adversarial Security) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-112. **Fix real (Achados #3/#4, Q17/Q23)**: `ResolvedRecipient` (`notification/ports/recipient-resolver.ts`) ganha `email?: string` aditivo; `DynamoDbNotificationRecipientResolver.resolve()` (`notification/persistence/`) passa a devolver `email: globalUser?.emailNormalized` na mesma leitura que já fazia para `active`. `notification.ts`'s `resolveRecipientEmail` e `subject.ts`'s `resolveInternalUserEmail` (`runtime/aws/composition/`) passam a instanciar essa classe já testada em vez de ler `GlobalUser` sozinhos — fecha o TOCTOU real entre roteamento (que já checava `Membership`+`GlobalUser`) e entrega assíncrona (que não checava). Comentário no port documenta que este resolver e `MemberEligibilityChecker` (`expiration/ports/`) usam a MESMA regra, formas diferentes por design (email vs. boolean), nunca fundidos. **Testes novos (Achados #1/#2, Q6/Q7)**: `test/unit/identity/resolver.test.ts` ganha 2 testes — revogação real via `RemoveMembershipService` encadeada com `resolve()` subsequente (achado durante a escrita: o teste original esperava `OrganizationUnavailableError`, mas o `OnboardingStateResolver` roda primeiro e lança `OnboardingRequiredError` quando a Membership removida é a ÚNICA do usuário — corrigido dando ao membro uma segunda Organization própria, isolando a asserção à revogação escopada, não ao gate de onboarding); roles genuinamente diferentes por Organization (MEMBER numa, OWNER noutra, mesmo usuário) nunca vazam em `authorize()`. **Teste do fix (Achado do Codex, Rodada 3 DoD)**: `document-chasing-dispatch.test.ts`'s teste existente de "sem e-mail resolvível" renomeado/documentado para cobrir explicitamente também o cenário de revogação mid-flight (mesmo código de saída, uma so teste — duplicar seria redundante, a regra de 2 condições em si já é provada por `dynamodb-recipient-resolver.test.ts`, estendido com um caso novo para o campo `email`). **Documentação (Q16)**: comentário em `document-service.ts` citando §47/§48 — nenhum código novo, o contrato de admissão já é integralmente coberto por outros testes (revogação de Membership → `resolver.test.ts`; Organization DELETING → fencing já existente). **Auditoria (Achado #5, Q25)**: busca sistemática por 10 palavras-chave em toda a suíte confirma zero coincidência de fixture IDs entre papéis adversariais — nenhuma correção necessária, auditoria registrada como evidência. G-V3 verificado por mutação real em 4 pontos: `email` do resolver (remover a leitura, teste pega), revogação de Membership (`resolve-working-organization.ts`, remover o check de status, teste pega), vazamento de role (`resolve-request-context.ts`, hardcodar OWNER, teste pega) — todos restaurados e reverificados. | Type 1 (nível 5, fix real de autorização-adjacente no pipeline de entrega assíncrona) | N/A — implementação direta, protocolo já rodado em D-112 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa backend `npm test` 1235/1235 (132 arquivos, +3 líquidos vs. B2B-12), zero regressão, `npm run build:lambdas` PASS (34/34, nenhum handler novo). Com este item, **Wave B2B-13 está `DONE`** | `src/modules/notification/{ports,persistence}/*.ts`, `src/runtime/aws/composition/{notification,subject}.ts`, `src/modules/document/application/document-service.ts`, `test/unit/{identity/resolver,notification/dynamodb-recipient-resolver,subject/document-chasing-dispatch}.test.ts`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
133:| D-117 | Multi-User B2B, Wave B2B-14 (Operational Evidence) — 4º achado real severo, encontrado ao continuar a verificação manual do Marcelo (barra lateral renderiza, nenhuma tela mostra conteúdo, `Settings` mostra "Erro inesperado do servidor"). Causa raiz real: `curl .../bff/organizations` retornou `404 {"message":"Not Found"}` — API Gateway nunca invocou o Lambda (zero logs correspondentes). `infra/modules/bff-api-gateway/main.tf`'s `bff_routes` nunca teve entradas para `GET /bff/organizations`/`POST /bff/organization/select`, apesar de `handleListOrganizations`/`handleSelectOrganization` (`bff-handlers.ts`) e seu dispatch em `bff-handler.ts:81-82` existirem desde Wave B2B-6/D-102 — só `organizations_create` (POST) foi wireado naquela wave, os outros 2 irmãos ficaram esquecidos. `frontend/src/hooks/useOrganizationsList.ts`'s `GET /bff/organizations` (usado por `Settings.tsx`/`OrganizationSwitcher`/toda tela que depende da lista de Organizations) nunca funcionou contra `dev` — explica tanto o erro explícito do Settings quanto as outras telas "sem conteúdo" (silenciosamente sem dado, sem gate de erro visível). Fix: as 2 rotas adicionadas ao mapa `bff_routes`. **Achado adicional sobre a própria suíte de testes**: `infra/modules/bff-api-gateway/tests/bff_api_gateway.tftest.hcl` já tinha um teste de existência dedicado para `organizations_create` (B2B-6) mas nunca para os 2 irmãos — exatamente por isso a suíte nunca pegou o gap; corrigido (contagem de rotas 7→9 + novo run `organizations_list_and_select_routes_exist_for_multi_org_to_work_at_all`). `terraform validate`/`terraform test` (módulo 6/6, raiz 16/16) PASS. | Type 3 (correção mecânica de uma wiring que já deveria existir desde B2B-6 — mesmo padrão já usado por `organizations_create`, não uma decisão nova) | N/A — correção de bug real, não decisão de design | N/A | **IMPLEMENTADO (config + teste) — aplicação real via CD após merge; login real via Marcelo (browser) ainda em andamento** | `infra/modules/bff-api-gateway/main.tf`, `infra/modules/bff-api-gateway/tests/bff_api_gateway.tftest.hcl` | 2026-08-30 |
136:| D-108 | Multi-User B2B, Wave B2B-11 (Responsibility + Notifications) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-107. `recipient-resolver.ts`: `resolveCandidateUserId({assigneeUserId})` (`tenantId` removido dos parâmetros, fallback `""`, tipo de retorno inalterado). `dynamodb-recipient-resolver.ts`: migrado para `Membership`+`GlobalUser`, 2 condições (`Membership.status==="ACTIVE" && GlobalUser.identityStatus==="ACTIVE"`), preservando `RECIPIENT_NOT_FOUND` (Membership nunca existiu) vs. `RECIPIENT_NOT_ELIGIBLE` (existe mas inativa/suspensa). `notification.ts` (composition root): `resolveRecipientEmail` migrado para `GlobalUser` (`globalUserKey`, tenantless). Port novo `expiration/ports/member-eligibility.ts` (`MemberEligibilityChecker`), implementado no composition root (`expiration.ts`) contra `Membership`+`GlobalUser` diretamente (mesmas 2 condições) — porta estreita no módulo consumidor, per sugestão do Codex, não um import direto do modelo físico de `organization`. `ItemWatchService.addWatcher` valida elegibilidade antes de gravar (`removeWatcher` deliberadamente não valida — remover/limpar é sempre seguro). `ExpirationService.createItem`/`updateItem` validam `assigneeUserId` quando fornecido/alterado (nunca em `renewItem`, que só copia um valor já validado). `IneligibleAssigneeError` novo (`app-error.ts`, categoria NOT_FOUND/404 deliberada, nunca distinguir por status HTTP "existe mas não elegível" de "não existe"). `UsableOrganization`/`resolve-active-membership.ts` inalterados nesta wave. **Achado real corrigido durante a implementação** (mesma honestidade de processo das waves anteriores): o teste `notification-router-workflow.test.ts`'s cenário "happy path" dependia implicitamente do fallback quebrado (`makeItem()` sem `assigneeUserId`, `FakeRecipientResolver` com resultado canned `userId: TENANT`) — corrigido dando a `makeItem()` um `assigneeUserId` real por padrão, com um novo teste dedicado provando que "sem assignee" cancela ANTES de sequer chamar o resolver (`candidateWasEmpty`), não depois de uma resolução bem-sucedida por acidente. G-V3 aplicado e verificado por mutação real em 2 pontos: `notification-router-workflow.test.ts` (reintroduzindo um fallback não-vazio, o novo teste falha), `dynamodb-recipient-resolver.test.ts` (removendo a checagem de `GlobalUser.identityStatus`, o teste de suspensão global falha) — ambos restaurados e reverificados. | Type 1 (nível 5, autoridade de validação de acesso no pipeline de notificação/responsabilidade) | N/A — implementação direta, protocolo já rodado em D-107 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos (415 módulos, 0 violação), suíte completa `npm test` 1210/1210 (131 arquivos, +13 líquidos vs. B2B-10), zero regressão, `npm run build:lambdas` PASS (34/34, nenhum handler novo, nenhuma mudança de infra). Com este item, **Wave B2B-11 está `DONE`** | `src/modules/notification/{ports,persistence,application}/*.ts`, `src/modules/expiration/{ports,application}/*.ts`, `src/shared/errors/app-error.ts`, `src/runtime/aws/composition/{expiration,notification}.ts`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
137:| D-120 | Multi-User B2B, Wave B2B-14 (Operational Evidence) — Marcelo confirmou o save de Settings funcionando pós-D-119 e continuou o roteiro manual, perguntando sobre herança de responsabilidade e exportação de dados (registrados como lacunas de produto, não corrigidos nesta entrada) e pediu para testar convite/aceite/sair-da-organização com autonomia máxima. Investigação encontrou 3 lacunas reais adicionais que bloqueavam o roteiro por completo, nenhuma hipotética: **(1) aceitar convite nunca teve tela no frontend** (`handleAcceptInvitation`/`POST /bff/invitations/accept` existem desde B2B-8/D-099, nunca chamados); **(2) essa MESMA rota nunca foi wireada no API Gateway** (`infra/modules/bff-api-gateway/main.tf`'s `bff_routes`, mesma classe de bug do D-117 — 404 genérico, nunca alcançava o Lambda); **(3) e-mail real de convite nunca foi ativado** (`CreateInvitationService.emailProvider`/`invitationBaseUrl` sempre `undefined` desde B2B-8 — mecanismo pronto, kill switch nunca ligado, D-100 já documentava isso como gap aberto) — e a conta SES do `dev` estava em sandbox com ZERO identity verificada (nem sequer o remetente `ses_from_address`, ainda no placeholder `noreply@example.com` desde M4 — o que significa que TODO fluxo dependente de SES real, não só convite, nunca poderia ter enviado e-mail de verdade neste ambiente). **Decisão consultada com o Marcelo** (achado #3 toca uma proteção de segurança já debatida no protocolo em B2B-8 — `create-invitation.ts`'s handler nunca devolve o token bruto na resposta HTTP, "canal de prova de posse é só o e-mail"): 2 opções apresentadas (A: ativar o e-mail real de verdade, exige verificar manualmente 2 endereços SES; B: atalho que reverteria a decisão de segurança). Marcelo escolheu A e forneceu os 2 e-mails reais (`marcelo.mjgoncalves@gmail.com` como remetente, `tchelojg@gmail.com` como segunda conta de teste — o mesmo endereço já usado como `alert_email` desde M5); ambos verificados com sucesso via `aws sesv2 create-email-identity`/confirmação manual do link. Fix: `AcceptInvitation.tsx` (novo, sibling do grupo `ProtectedRoute` principal — nunca aninhado em `ActiveOrganizationProvider`/`OnboardingGate`, um convidado pode ter zero Memberships) + `POST /bff/invitations/accept` wireado em `bff_routes`; `buildMembershipDeps`/`memberships-handler.ts` ganham `membershipInviteEmailEnabled`/`sesFromAddress`/`sesConfigurationSet`/`invitationBaseUrl` (mesmo padrão de `buildDocumentRequestDeps`, D-049, reaproveita o MESMO `SesEmailAdapter` já usado por `EmailDeliveryWorker`/`DocumentChasingDispatch`); `infra/main.tf` ganha `membership_invite_email_enabled` (kill switch, default `false`, ligado só em `dev.tfvars`) + IAM `ses_send_email` + env vars no `memberships_handler`; `ses_from_address` corrigido para o endereço real verificado (beneficia TODOS os fluxos SES do ambiente, não só convite). **2 achados adicionais, menores, encontrados na mesma varredura**: `handleListMembers` devolvia o item bruto do DynamoDB (`PK`/`SK`/`GSI4PK`/`GSI4SK`/`entityType`) em vez de um subconjunto seguro, ao contrário do irmão `handleListInvitations` que já projeta explicitamente — corrigido projetando `{userId, role, status, version}` (confirmado, por leitura do frontend, que `Members.tsx` já só usava exatamente esses 4 campos — zero impacto funcional, achado só de higiene de API); `handleLeaveOrganization`/`POST /organizations/members/leave` estava fio-a-fio funcional (Lambda+rota+allowlist) desde B2B-8 mas sem NENHUM botão no frontend — `LeaveOrganizationSection` novo em `Settings.tsx` (visível a qualquer role, backend é a única autoridade real sobre quando sair é permitido), com mensagem amigável dedicada para `LastOwnerError` (`isLastOwnerError`, novo helper — `BUSINESS_RULE` também precisou ser adicionado a `BackendErrorCategory`, ausente do frontend até agora apesar de existir no backend desde B2B-8). **Verificação ao vivo, não só suíte de teste**: usando o cookie de sessão real do Marcelo (`__Host-et_session`/`__Host-et_csrf`, repassados por ele), confirmado por chamada direta: `GET /bff/session` (sessão válida), `GET /bff/organizations` (organização real, `version: 2` refletindo o save do D-119), `GET /bff/api/organizations/members` (achado do leak, membro único ACTIVE/OWNER) e `POST /bff/organizations` corretamente bloqueado por `CONFLICT`/"You have already created an organization" (cap de uma organização por `GlobalUser`, D-096 — confirma que troca de organização só é testável depois que convite/aceite funcionarem de verdade, sem atalho possível). **Decidido não testar `leave` com a única conta real do Marcelo agora** — um bug no guard de último-owner combinado com o cap de uma-organização-por-usuário o deixaria sem organização e sem conseguir criar outra; fica para depois que a segunda conta existir. G-V3 verificado por mutação real: `isLastOwnerError` desativado no branch da mensagem amigável, teste dedicado falhou como esperado, restaurado e reverificado. | Type 3 (ativação de um kill switch e wiring de rota já aprovados desde B2B-8/D-049/D-099 — sem decisão de arquitetura nova; a única questão que tocava segurança foi consultada e decidida pelo Marcelo diretamente, não decidida sozinho) | N/A — implementação direta de mecanismo já aprovado, consulta pontual ao Marcelo para a única bifurcação de segurança | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos, suíte completa backend `npm test` 1235/1235, frontend `vitest` 152/152 (+10 novos: 5 `AcceptInvitation`, 2 `Settings` leave-org, 3 líquidos de ajuste) + `playwright` 24/24 zero regressão, `terraform validate`/`test` (raiz 16/16, módulo bff-api-gateway 7/7) PASS, 2 endereços SES verificados de fato (`VerificationStatus: SUCCESS` confirmado via `aws sesv2 get-email-identity`). Aplicação real via CD após merge; teste de convite/aceite/troca-de-organização com a segunda conta ainda pendente (aguarda Marcelo repassar o cookie da segunda sessão) | `frontend/src/{routes/AcceptInvitation,routes/Settings,hooks/useAcceptInvitation,hooks/useLeaveOrganization,api/organizations,api/members,api/errors,App}.tsx`, `src/runtime/aws/composition/organization.ts`, `src/runtime/aws/handlers/memberships-handler.ts`, `src/modules/organization/http/membership-handlers.ts`, `infra/{main,variables}.tf`, `infra/env/dev.tfvars`, `infra/modules/bff-api-gateway/main.tf`, `infra/modules/bff-api-gateway/tests/bff_api_gateway.tftest.hcl` | 2026-08-30 |
143:| D-096 | Multi-User B2B, Wave B2B-5 (RequestContext Cutover) implementada, conclui a wave — exatamente o escopo `APPROVED` em D-095, com 2 achados reais corrigidos durante a implementação (registrados por honestidade de processo, nenhum reabriu o protocolo): (1) **`UserProfile` não é legado descartável** — D-095 previa "remove UserProfile legado", mas verificação contra o código real antes de escrever mostrou que `profile-service.ts`/`profile-handlers.ts`/guest flows/`document-chasing-dispatch.ts`/`email-templates.ts` ainda consomem `UserProfile.requesterDisplayName` (W5-01/GTR-01) — `UserProfile` mantido, seu provisionamento movido do bootstrap (que não conhece mais nenhuma Organization) para `RequestContextResolver` (lazy, por Organization, preservando o invariante que `ProfileService.readOwnProfile()` já documentava depender); supersessão real por `Organization.displayName` continua Wave B2B-11. (2) **Gate de `TenantLifecycleRecord(organizationId)` ACTIVE estava faltando** na primeira versão do resolver reescrito — o design (§11) exige que a cadeia termine checando o lifecycle da própria Organization, não só da Membership; achado por revisão própria antes de rodar os testes, corrigido e coberto por teste dedicado. **Implementação real**: `IdentityMapping` tenantless; `IdentityBootstrapService.bootstrapUser()` com `TransactWriteItems` de 2 itens (`GlobalUser`+`IdentityMapping`); `DeviceSession`+`logoutAll`/`logoutDevice` migrados para `GlobalUserRepository` (`PK=USER#<userId>`, `globalLogoutAfter` também migrado de `UserProfile` para `GlobalUser`); `RequestContextResolver` reescrito com resolução de Membership única explícita (`resolve-active-membership.ts`, helper compartilhado com a BFF, não reabre o contrato de `OnboardingStateResolver`), gate de lifecycle da Organization, e assert nomeado (`UnsupportedMembershipRoleError`) para `Membership.role` não suportado; `POST /bff/organizations` novo (primeiro consumidor HTTP de `CreateOrganizationService`) com cap transacional real (`GlobalUser.hasCreatedOrganization` via `buildAttributeOnceUpdate`, novo builder tenantless em `occ.ts`, na MESMA `TransactWriteItems` de `CreateOrganizationService.buildCreateEntries()` — refatoração aditiva; `TransactionCanceledException` distinguido por índice via `getCancellationReasonCodes()`, também novo em `occ.ts`, para não confundir a falha do cap com a falha de qualquer outro dos 4 Puts); sessão BFF identity-only (`Session.tenantId`→`activeOrganizationId?`) com self-heal em `GET /bff/session` (`resolveSessionWithOnboarding`); nova rota Terraform `POST /bff/organizations`. `POST /bff/organization/select` e o transporte real de `activeOrganizationId` até o recurso continuam fora desta wave, exatamente como D-095 registrou (fechados por Wave B2B-6). Com este item, **Wave B2B-5 está `DONE`**. | Type 5-6 (elimina o auto-provision de tenant no login real, o gate central da wave — implementação direta do escopo já `APPROVED` em D-095 via protocolo completo, sem access pattern novo além do já aprovado) | N/A — implementação direta, protocolo já rodado em D-095 | N/A | **IMPLEMENTADO** — `npm run typecheck`/`lint`/`check-boundaries` limpos (393 módulos, 0 violação), suíte-alvo (`test/unit/identity/`, `test/unit/bff/`, `test/unit/organization/`, `test/integration/`) 194/194 PASS com G-V3 aplicado desde a escrita, `terraform validate`/`terraform test` (via `AWS_PROFILE=claude-dev`) 5/5 PASS incl. a nova rota, `npm run build:lambdas` 27/27 PASS, suíte completa `npm test` 1134/1134 (123 arquivos), zero regressão | `src/modules/identity/**`, `src/modules/bff/**`, `src/modules/organization/application/{create-organization,resolve-active-membership}.ts`, `src/shared/dynamodb/occ.ts`, `src/shared/errors/app-error.ts`, `infra/modules/bff-api-gateway/`, `src/runtime/aws/{composition,handlers}/**`, `docs/architecture/multi-user-b2b-wave-tracker.md` | 2026-08-30 |
145:| D-124 | W3-07 — implementação real do orquestrador de purga de tenant, exatamente o mecanismo APROVADO em D-121 (implementação direta de design já aprovado, mesmo padrão de D-096/D-102/D-108/D-113 — nenhuma rodada Claude↔Codex nova para o mecanismo). Fecha a lacuna que o próprio D-121 nomeou: `transitionTenantLifecycle()` (D-068) e `purgeTenant()` (W3-07) eram código real, funcional e testado, **sem nenhum chamador em produção** — nada no sistema jamais disparou uma purga. **Nova máquina de estados Step Functions** (`infra/state-machines/tenant-purge.asl.json` + `infra/modules/tenant-purge-workflow/`, gêmeo estrutural de `extraction-workflow`): `Wait(1800s)` literal (cutoff D-066 Rodada H, nunca redecidido) → `AdvanceToQuiescing` → `AdvanceToPurging` → `RunPurge` → `EvaluatePurgeResult` (`Choice`, nunca `Retry`/`Catch` — Fix 2, porque `purgeTenant()` devolve `PARTIAL`/`SUCCESS`/`FAILED` como valor resolvido normal) → `AdvanceToVerified` → `AdvanceToDeleted` → `Succeed`; ramos `MarkBlocked*` → `Fail`. **Handler único de transição** (Fix 3) parametrizado por `from`/`to` no `Parameters` de cada estado, com no-op idempotente quando o registro já está em `to` e re-leitura única em `SystemMutationConflictError`. **Envelope compacto** (Fix 6) verbatim: `{status, checkpoint, counters:{s3UnresolvedCount,dynamoRejectedCount,sessionRejectedCount}, failure}`; `TenantPurgeResult` completo continua só em log estruturado (`SecureLogger`). **`PURGE_RETRY_LIMIT = 20`** como `local` Terraform nomeado, substituído no ASL via `replace()` (Fix 8). **Alarme CloudWatch novo e real** (Fix 7): `AWS/States` `ExecutionsFailed` **e** `ExecutionsTimedOut`, dimensionados por `StateMachineArn`, no mesmo tópico SNS de alerta; `evaluation_periods=1` (não 3 como os alarmes de reminder) porque uma purga falha deixa dado de tenant fisicamente presente após um pedido de exclusão. **Sweeper** via EventBridge Scheduler `rate(1 day)` com as 2 responsabilidades aprovadas: reparo durável de `StartExecution` para tenants `DELETING`/`QUIESCING`/`PURGING`/`VERIFIED` parados há >1h (nunca `BLOCKED`/`HELD` — remediação humana em curso), e verificação residual pós-`DELETED` na janela de 90 dias reusando `verifyTenant*Empty()` sem inventar lógica nova. **`CloseOrganizationService`** novo com a ordenação corrigida da Rodada 3 Fix 8 (checagem de estado terminal ANTES do `StartExecution` incondicional), erro novo `OrganizationClosureUnavailableError` (`CONFLICT`/409, mesma categoria de `TenantNotActiveError`), action RBAC nova `organization:close` em `OWNER_ROLES`. **Composition root real** (`src/runtime/aws/composition/tenant-purge.ts`) que fecha o KNOWN LIMIT documentado no próprio `purge-tenant.ts` (Codex rodada 5): tabela FECHADA bucket→raiz (`clean/`, `tenant/`, `tenant/`, `ocr/`, cada uma verificada lendo o key builder real), nunca um par `{bucket, prefix}` arbitrário. **IAM mínimo** conforme Fix 8, verificado contra os adapters reais (sem `s3:GetObject` em lugar nenhum — a purga apaga, nunca lê conteúdo). **3 achados reais corrigidos durante a implementação, nenhum deles mudança de forma do design aprovado**: (1) o design nomeava `Wait` como cobrindo `DELETING→QUIESCING`, mas um estado `Wait` não escreve no DynamoDB — foi necessário um `AdvanceToQuiescing` explícito, realização fiel do mesmo handler único, não desvio; (2) `checkpoint`/`failure` do envelope precisam ser `null` explícito e nunca ausentes — o `ResultSelector` do ASL lê por JSONPath e um caminho AUSENTE é falha dura `States.Runtime`, não `null`; (3) a role de execução da máquina de estados precisava de `logs:*LogDelivery`+`xray:Put*` além de `lambda:InvokeFunction` — sem elas o `apply` falha (não o runtime), achado por comparação direta com `extraction_workflow_invoke_lambdas`, não assumido. **Rota HTTP**: `POST /organizations/close` no módulo **`api-gateway`** (não `bff-api-gateway`) + `proxy-allowlist.ts` + dispatch no `memberships-handler`, todos no MESMO commit, com assert dedicado no `tftest` — correção de roteamento apurada por leitura: toda mutação de organização (`leave`, `settings`) roda por `api-gateway`+proxy do BFF, nunca por uma rota `/bff/*` própria; seguir a instrução literal do prompt teria reproduzido a classe de bug de D-117/D-120. **UX de confirmação** (latitude explícita da Rodada 1): `CloseOrganizationSection` em `Settings.tsx`, type-to-confirm do `organizationId` (não diálogo de 2 passos — o segundo clique vira memória muscular; digitar o identificador obriga a ler QUAL organização está sendo destruída, o modo de falha que importa agora que um usuário pertence a várias), visível só a OWNER no cliente, com o mesmo token revalidado no servidor contra o tenant RESOLVIDO. **G-V3**: mutação real verificada e revertida em 3 pontos — remover o no-op idempotente do handler de transição (2 testes falham), remover o filtro de 1h do sweeper (1 teste falha), e a ordenação Rodada-2 do `CloseOrganizationService` (4 testes falham). A primeira tentativa de mutação da ordenação foi MASCARADA pelo próprio guard de exaustividade do serviço e não falhou — registrado honestamente e refeito com a mutação fiel (ambos os guards removidos). O bound do `Choice` é coberto no `tftest` do módulo (`"NumericLessThan": 20` como número JSON real, nunca string). **Suíte**: `typecheck`/`lint`/`check-boundaries`/`validate-schemas`/`build:lambdas`/`check-docs` limpos; backend 1267 testes / 136 arquivos (32 novos), frontend 156 testes / 24 arquivos (4 novos); `terraform test` 19/19 raiz (3 runs novos), 9/9 `tenant-purge-workflow` (módulo novo), 1/1 `api-gateway`, 7/7 `bff-api-gateway`; zero regressão. Nenhuma operação AWS destrutiva — só `validate`/`test`/`plan`. | Type 1 (nível 5) — implementação direta de design já `APPROVED` em D-121, sem rodada nova (mesmo padrão de D-096/D-102/D-108/D-113) | n/a (implementação) | n/a (implementação) | **IMPLEMENTADO** — orquestrador real de ponta a ponta; `transitionTenantLifecycle()`/`purgeTenant()` deixam de ser código sem chamador | `infra/state-machines/tenant-purge.asl.json`, `infra/modules/tenant-purge-workflow/`, `infra/{main,outputs}.tf`, `infra/modules/api-gateway/`, `infra/tests/stack.tftest.hcl`, `src/workers/tenant-purge/{lifecycle-transition,purge-result-envelope,tenant-purge-sweep}.ts`, `src/shared/tenant-lifecycle/{tenant-purge-execution-starter,sfn-tenant-purge-execution-starter}.ts`, `src/shared/dynamodb/tenant-purge-scan.ts`, `src/shared/errors/app-error.ts`, `src/modules/organization/{application/close-organization.ts,http/organization-lifecycle-handlers.ts}`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition,handlers}/**`, `frontend/src/**` | 2026-08-31 |
147:| D-128 | `AppError.retryable` — comportamento real de consumidores SQS. Fecha a pendência de produto registrada em `docs/engineering/decisions-log.md` E-011 (auditoria 2026-08-29 achou o campo honestamente documentado como diagnóstico/log-only, mas deixou aberta a pergunta "deveria um handler SQS ramificar sobre `retryable`?"). Autoridade ampliada (`docs/engineering/ai-governance.md` §1, 2026-08-31) cobre este resíduo de produto. Protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: régua 8,5/design 8,7 — Codex achou 3 imprecisões reais na fundamentação, não na conclusão: alegação normativa excessiva sobre a AWS "proibir" branching, duração "minutos, não horas" não demonstrada pelo `visibility_timeout_seconds = consumer_timeout_seconds * 6` real, e a taxonomia de `retryable` caracterizada como "base sã para política de branching" quando na verdade `toAppError()`'s fallback (`InternalError`, `retryable:false` para qualquer erro não reconhecido) é evidência ATIVA contra isso; Rodada 2 corrigiu as 3 imprecisões sem mudar a conclusão — Claude 9,0/Codex 9,6, gate atingido; Rodada 3 tréplica de fechamento, Claude 9,3). **Pesquisa externa (SIM)**: AWS Lambda Developer Guide, "Handling errors for an SQS event source in Lambda" + "Using Lambda with Amazon SQS" (`docs.aws.amazon.com/lambda/latest/dg/`, WebFetch 2026-08-31) — todos os exemplos de referência oficiais (10 linguagens) reportam qualquer exceção capturada como `batchItemFailure` incondicional, nenhum ramifica por tipo/classe de erro; nenhum padrão de classificação de erro para roteamento direto a DLQ é oferecido como recomendação central. **Decisão**: nenhum handler SQS deste sistema passa a ramificar sobre `retryable`. Mecanismo de retry-vs-terminal continua exclusivamente nativo do SQS (`maxReceiveCount=5` uniforme, `infra/modules/sqs-worker-queue/main.tf`, provado por `stack.tftest.hcl`/`sqs_worker_queue.tftest.hcl`) + DLQ redrive — nenhuma chamada SQS explícita nova, nenhum roteamento paralelo. Razão central: assimetria de blast radius — um falso-terminal sob o regime atual não tem efeito extra (mensagem retentada normalmente de qualquer forma); sob um regime de branch-and-terminate, pularia processamento devido de uma mensagem legitimamente retryable. **Classificação de `app-error.ts` auditada e considerada consistente** (nenhuma inconsistência real encontrada nas subclasses existentes — `ExtractionCommitFailedError` com `retryable:true` é a única exceção à regra "validação/regra de negócio = false", e é correta por documentação própria); nenhuma mudança de classificação proposta. **Implementação (documentação apenas, sem mudança de runtime)**: comentários de `AppError`/`isRetryable()` em `src/shared/errors/app-error.ts` reescritos de "pendência" para "decidido, ver D-128"; redação contraditória ("poison message... not retryable" + ainda reportado como batch failure) corrigida em `reminder-dispatch-handler.ts`/`email-delivery-handler.ts` (os 2 handlers lidos linha a linha nesta rodada — demais consumidores SQS do sistema não tocados, decisão é uniforme e independe de detalhe por handler). **Blast radius de errar esta decisão**: baixo — revisitável isoladamente por fila/handler no futuro só sob evidência operacional concreta (saturação, custo material, atraso mensurável), rollback trivial (nenhuma infraestrutura nova para desfazer). | Type 1 (nível 5, política comportamental de retry/DLQ + resíduo de produto sob autoridade ampliada) | 9,3 | 9,6 | **APPROVED** (3 rodadas, gate 9,0 atingido em ambos sem arredondar) — documentação implementada, sem mudança de runtime/infra | `docs/architecture/reviews/appError-retryable-sqs-scoping/` (round-1 a round-3), `src/shared/errors/app-error.ts`, `src/runtime/aws/handlers/{reminder-dispatch-handler,email-delivery-handler}.ts` | 2026-08-31 |
152:| D-132 | Wave 1b — Design System Implementation Gap (D-130): resíduos nomeados Button `lg` e motion `--duration-slow`. Protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: Claude 9,0 self/Codex 8,7 — achou inconsistência real entre o critério "significado reutilizável exige segundo consumidor real" usado para rejeitar Button `lg` e "implementar `--duration-slow` junto do primeiro consumidor", mais um gatilho puramente numérico ">160ms" para motion, que qualquer valor de 161ms satisfaria formalmente; Rodada 2: Claude 9,2 self/Codex 9,7 — critério único corrigido: token nasce só com um caso de uso real e concreto E uma categoria semântica própria, nunca por contagem de consumidores nem limiar numérico isolado; Rodada 3: tréplica de fechamento, sem achado novo). **Decisão: NÃO implementar nenhum dos dois agora.** Button `lg` (`--control-height-lg`/44px já existe como token de escala, não vira variante do `Button` — `ButtonSize` continua "sm" | "md") fica resíduo nomeado até um caso de uso real (CTA mobile full-width/touch-primary). `--duration-slow` fica resíduo nomeado até o primeiro componente de superfície sobreposta real (Modal/Dialog/Drawer/Popover, nenhum implementado ainda) precisar de uma categoria de movimento distinta de hover/foco inline — nasce no mesmo commit desse componente, nunca antes, validado contra `prefers-reduced-motion`. Nenhuma pesquisa externa (aplicação de regra já `APPROVED`, não definição de padrão de mercado). | Type 1 (nível 5, resíduo de decisão de produto nomeado em D-130, sob autoridade ampliada) | 9,2 | 9,7 | **APPROVED** (3 rodadas, gate 9,0 atingido em ambos sem arredondar) — decisão de não-implementação, sem mudança de código | `docs/architecture/reviews/wave1b-button-lg-motion-scoping/` (round1 a round3, `estado-final-consolidado.md`) | 2026-08-31 |
153:| D-133 | Wave 1b — Design System Implementation Gap (D-130): implementação direta de 2 componentes genuinamente faltantes do catálogo de ~30 (`design-system.md` §29) que não têm equivalente sob outro nome no código real — verificação de shape feita antes de escrever (Button/DataTable/InlineNotice/Layout/StatusBadge/UrgencyIndicator já cobrem Button/Table/Alert/PageHeader-Section-Toolbar-Panel/StatusBadge/urgência; TextField/SelectField/FormErrorSummary em `components/forms/` já cobrem Input/Textarea/Select/FormField/FieldError; `AsyncStates.tsx` já cobre EmptyState/Skeleton/loading; `OrganizationSwitcher.tsx` já é o pattern de switcher — nenhum desses recriado). **`Divider`** (`frontend/src/components/ui/{Divider.tsx,Divider.css}`) — separador puramente decorativo, `aria-hidden`, 2 orientações; nunca substitui um `<section>` real como fronteira estrutural (mission/`Layout.tsx` já estabelecem isso). **`IconButton`** (`frontend/src/components/ui/{IconButton.tsx,IconButton.css}`) — reusa as variantes visuais de `Button` (nenhuma linguagem visual nova), `label` **obrigatório** no tipo (não opcional) para tornar impossível omitir o nome acessível (mission §23), setado em `aria-label` E `title`. Nenhuma camada de component-tokens nova foi necessária (ambos consomem semantic tokens existentes diretamente, sem valor arbitrário novo). **Decisão explícita de não-escopo desta sessão** (honestidade sobre o que ficou de fora, não gold-plating apressado): os demais itens do catálogo ainda faltantes — `Link` dedicado, `Checkbox`/`Radio`/`Switch`, `Combobox`, `DateInput`/`DatePicker`, `Tooltip`, `Popover`, `DropdownMenu`, `Modal`/`Dialog`, `Drawer`, `Tabs`, `Pagination`, `Breadcrumb`, `Avatar` — exigem lógica de overlay/focus-trap/portal ou semântica de formulário não-trivial o suficiente para merecer implementação própria com o mesmo rigor (não uma rodada apressada no fim desta sessão); permanecem no backlog nomeado de Wave 1b, não escondidos. `Card` **não** foi adicionado apesar de estar no catálogo (`design-system.md` §40): `Layout.tsx` documenta uma decisão arquitetural deliberada já em produção ("There is no Card component, on purpose... a card is what you reach for when independent modules genuinely need separating, and the Core Expiration slice has no such grouping yet") — conflito real entre um documento de roadmap (`design-system.md`, adotado como catálogo aspiracional em D-130, não afirmação de que os ~30 já existem) e uma decisão de código já implementada; resolvido a favor do código real (mesma regra de precedência de `AGENTS.md` §5, "documento temático corrente" > catálogo de roadmap), registrado como gap nomeado a decidir explicitamente (não silenciosamente) quando um agrupamento real justificar o componente. **z-index**: verificado já 100% implementado (`--z-base/sticky/overlay/skip-link` em `tokens.css`), não é gap. **Breakpoints nomeados** (§25): decisão mecânica de não criar tokens agora (ver `estado-final-consolidado.md` de D-132) — mesmo critério "token nasce com consumidor real", o único breakpoint real do CSS (`base.css` 900px) não corresponde a nenhum valor do catálogo. **G-V3**: mutação nomeada e revertida em ambos — `Divider`'s `aria-hidden="true"→"false"` (1 teste falha) e `IconButton`'s `aria-label`/`title` (mutar só `aria-label` foi MASCARADO pelo algoritmo de nome acessível do testing-library, que cai para `title` — achado honesto registrado, não escondido; mutar os dois juntos falha 2 testes). **Suíte**: frontend `typecheck`/`lint` limpos, `npm test` 160/160 (4 novos), `check-docs` limpo; backend não tocado nesta sessão (nenhuma mudança em `src/`, `infra/`), suíte de backend completa não executada nesta rodada por não haver diff relevante — risco residual reconhecido, não uma alegação de verificação que não ocorreu. | Type 1 (nível 4-5, implementação direta de catálogo já `APPROVED` em D-130, mesmo padrão dos demais D-1xx de implementação direta) | n/a (implementação mecânica pós-D-132) | n/a (implementação mecânica pós-D-132) | **IMPLEMENTADO** (parcial, deliberado) — 2 de ~9 componentes genuinamente faltantes; backlog nomeado explicitamente para o restante | `frontend/src/components/ui/{Divider.tsx,Divider.css,IconButton.tsx,IconButton.css}`, `frontend/test/components/ui.test.tsx` | 2026-08-31 |
156:| D-136 | Otimização de performance do hot path (frontend startup + backend) — gatilho real: Marcelo reportou usando o app que a tela demora a mostrar conteúdo e que aparece uma mensagem tipo "validando sessão" desnecessária. Duas sessões anteriores produziram auditoria+pesquisa de performance (`expiration-tracker-performance-audit-2026-08-31.md`/`expiration-tracker-performance-strategies-research-2026-08-31.md`, raiz do repo, P0-P3) — esta rodada focou nos itens Type 1 (arquitetura/segurança) que explicam a queixa, via protocolo Claude↔Codex completo, **3 rodadas** (Rodada 1: Codex 6,4/10 régua + 7,6/10 design, NÃO APROVADO — achou que a proposta original removeria silenciosamente o fence transacional do W3-07 ao trocar `Get+TransactWriteItems` da quota `API_REQUEST` por `UpdateItem` simples, já que `TenantQuotaService.consume()` roda dentro de `executeTenantBusinessMutation()` com `ConditionCheck` sobre `TenantLifecycleRecord.status=ACTIVE` no mesmo `TransactWriteItems` — a transação protege 2 garantias, não 1; Rodada 2: 8,7/10, NÃO APROVADO — reclassificar `API_REQUEST` como `QUOTA_TELEMETRY` (classe já aprovada em D-127) foi aceito como correto, mas o Codex apontou que D-127 decidiu retenção, não autorizou escrita tenant-scoped nova pós-`ACTIVE→DELETING` nem que TTL substitui o fence de descoberta/purga; Rodada 3: Claude 9,3/Codex 9,3, **APPROVED**, sem arredondar). **Pesquisa externa (SIM PARCIAL, E-014)**: unificação de sessão frontend é padrão já resolvido (TanStack Query docs 2026); rate limiting multi-tenant tem padrão de abordagem em camadas (coarse+tenant-aware) mas calibração depende do modelo do projeto — Codex contestou o checklist original (sem pesos/âncoras, fontes secundárias para decisão de segurança) e substituiu por sub-rubrica de 6 critérios ponderados a partir da Rodada 2. **Decisões (5, todas design-only)**: D-A — `AuthContext` deriva `AuthState` do mesmo `useQuery(sessionQueryKey)` que `ActiveOrganizationContext` já usa, elimina o waterfall real de 2 chamadas sequenciais a `/bff/session` confirmado no código; as 2 mensagens sequenciais ("Verificando sua sessão…"/`ProtectedRoute.tsx:26`, "Carregando sua organização…"/`OnboardingGate.tsx:21` — exatamente o que Marcelo reportou) viram um único skeleton sem texto técnico; logout usa `removeQueries`, não só `invalidateQueries`. D-B — mantém `ConsistentRead=true`, troca renovação de idle TTL por request para sliding-expiration-com-threshold, `ConditionExpression` nunca ressuscita sessão revogada/expirada. D-C (parcial) — `createProfileIfAbsent()` sai do hot path (create-if-absent idempotente sem consumidor síncrono); `onboarding.resolve()` NÃO sai nesta decisão, condicionado a mapear consumidores primeiro; membership/lifecycle `ACTIVE` continuam obrigatórios em toda request. D-D (maior risco do pacote) — nova lane formal `EphemeralTelemetryMutation`, emenda explícita ao W3-07 (não exceção implícita): allowlist fechada em `API_REQUEST`, interface estruturalmente fechada sem campo `metadata` livre (precisão da Rodada 3), chave `PK=TENANT#<id>#QUOTA`/`SK=TYPE#API_REQUEST#<floor(epochSeconds/windowSeconds)>`, `purgeAfterTtl=windowEnd+30d` (atributo real do projeto, confirmado contra o código pelo Codex), item permanece coberto estruturalmente pelo scan/`PURGE_DELETE` real do W3-07 (TTL nativo é só limpeza best-effort, nunca a garantia), teste adversarial da corrida `ACTIVE→DELETING` obrigatório no DoD/G-V3 futuro, classificação tripla de falha (`ConditionalCheckFailedException`→429, dependência indisponível→fail-open com log, corrupção/bug→fail-closed 5xx real); API Gateway throttle recalibrado por critério de capacidade real (achado: infra hoje `rate=25/burst=50` global já limita antes de qualquer tenant alcançar sua quota individual de `100/60s`, não é neutro). D-E — dashboard usa `limit`+cursor opaco sobre a GSI já existente, ordenação com tie-breaker estável, teto de `limit` imposto pelo servidor. **Fora de escopo** (mecânico, não Type 1): `React.lazy()` por rota, Node 20→22/24 (dívida com prazo — Node 20 já depreciado pela AWS desde 30/04/2026), ADOT 1-30-0→1-30-2 + instrumentação seletiva, benchmark Lambda memory/ARM64, batch/concorrência SQS, `staleTime` por tipo de query, N+1 de `BatchGetItem` na renovação — implementação direta futura, sem protocolo. **DESIGN-ONLY** — implementação real fica para sessão(ões) futura(s) dedicada(s), mesmo padrão D-121→D-124; D-D deve ser implementado com o teste adversarial como parte do DoD, não opcional. | Type 1 (nível 5-6, arquitetura de sessão/autorização + emenda a mecanismo de segurança já auditado do W3-07) | 9,3 | 9,3 | **APPROVED** (design, 3 rodadas, gate 9,0 atingido em ambos sem arredondar) | `docs/architecture/reviews/performance-hotpath-scoping/` (round1 a round3, `estado-final-consolidado.md`) | 2026-08-31 |
160:| D-140 | D-136/D-A implementado: sessão única no frontend + UX de loading sem terminologia técnica. `AuthContext` deixa de fazer sua própria chamada imperativa a `fetchSessionInfo()` e passa a derivar `AuthState` da mesma query TanStack (`sessionQueryKey`, `staleTime: 30_000`) que `ActiveOrganizationContext` já usa (também alinhado de `staleTime: 0` para `30_000`) — elimina o waterfall real de 2 chamadas sequenciais a `/bff/session` confirmado em D-136. As 2 mensagens técnicas sequenciais (`"Verificando sua sessão…"`/`ProtectedRoute.tsx`, `"Carregando sua organização…"`/`OnboardingGate.tsx`) foram substituídas pelo componente `InitialLoading` já existente (`"Carregando…"` genérico), reaproveitado nos dois pontos — resolve as duas queixas originais do Marcelo (lentidão + mensagem desnecessária) com uma mudança pequena. `logout()`/`logoutEverywhere()` limpam o cache de dados tenant-scoped (`queryClient.removeQueries({queryKey:["org"]})`) e fixam a sessão como `{authenticated:false}` via `setQueryData` direto. **Achado real de corrida encontrado pelo próprio G-V3**: a primeira versão usava `queryClient.removeQueries({queryKey: sessionQueryKey})` antes de fixar o valor final em `reportUnauthorized`/`logout` — como a `useQuery` do `AuthProvider` continua montada e observando essa chave, remover a entrada do cache faz o TanStack Query refazer a busca automaticamente para satisfazer o observer ainda ativo, correndo contra a escrita seguinte; isso reproduziu como um teste E2E de logout travando em timeout de 30s (a query refeita trazia `authenticated:true` de volta do mock antigo, sobrescrevendo o valor correto). Corrigido usando só `setQueryData` (determinístico, sem gatilhar refetch) para a chave de sessão — `removeQueries` continua sendo usado só para o namespace `["org"]`, que não tem esse mesmo observer ativo no momento do logout. **G-V3 adicional (achado honesto, não escondido)**: um teste unitário que tentava provar "dedup de 2 consumidores" foi escrito, mas não conseguiu ser feito sensível à mutação de `staleTime` de volta a `0` mesmo com tentativas de sincronização de timing em jsdom — removido em vez de deixado como teste falso-positivo; a evidência real do fix é o teste E2E de logout, que caiu de timeout(30s) para 461ms de forma reprodutível antes/depois da correção. **Suíte**: frontend `typecheck`/`lint` limpos, `npm test` 171/171, `npm run test:e2e` 24/24 (incluindo o antes-travado teste de logout), `npm run test:visual` 10/10 sem alteração de baseline; raiz `check-boundaries`/`check-docs` limpos. | Type 1 (nível 4-5, implementação direta de decisão de arquitetura já aprovada em D-136) | n/a (implementação pós-D-136) | n/a | **IMPLEMENTADO** — D-A completo; D-B/D-C/D-D/D-E de D-136 seguem pendentes | `frontend/src/auth/{AuthContext.tsx,ActiveOrganizationContext.tsx,ProtectedRoute.tsx,OnboardingGate.tsx}`, `frontend/test/auth/{AuthContext.test.tsx,OnboardingGate.test.tsx}` | 2026-08-31 |
166:| D-147 | Domínio Documental, Núcleo 2, entidade 3/3 (recorrência) — implementado seguindo a Decisão 8 `APPROVED` em D-143. **Fecha o Núcleo 2 (as 3 entidades — Requirement/D-145, guest access/D-146, recorrência/D-147 — estão completas) e, com Núcleo 1 (D-144) já completo, fecha as 9 decisões técnicas de D-143 por inteiro** — nenhum item das "9 decisões técnicas fechadas" de `estado-final-consolidado.md` segue sem implementação real (revisado item a item ao final desta tarefa). Estende `src/modules/document-archive/domain/document-request.ts` com `seriesId?`/`occurrenceId?`/`attemptIndex?`/`parentRequestId?` (todos aditivos/opcionais — o ponto de extensão nomeado explicitamente em D-146 — preservando o shape mínimo de guest access sem forçar um valor fabricado em todo `DocumentRequest` não-recorrente). Novo `domain/document-request-series.ts`: `DocumentRequestSeries` (série/ciclo, co-localizado sob a partição do Subject como `requirement.ts`), `computeSeriesOccurrenceId(seriesId, cycleStartAt)` — hash determinístico puro (reaproveita `stableHash` de `reminder/domain/reminder-occurrence.ts`, mesmo primitivo que `document-chasing.ts` já reaproveita) — e uma terceira namespace prefixo-discriminada `SERIESDUE` no MESMO GSI1 físico que `document.ts`/`requirement.ts` já usam (GSI3/GSI4/GSI6 confirmados por grep sem nenhum writer sob `document-archive/`, permanecem intocados). Novo `application/document-request-recurrence-service.ts`: `createSeries`/`getSeries`/`listSeries`/`cancelSeries`/`advanceCycle` (todas via `authorize()`, novas actions `docarchive:series-*`) e `materializeAttempt` — a operação central da Decisão 8, que avança `latestAttemptIndex`/`latestRequestId` da série E cria o novo `DocumentRequest` na MESMA `TransactWriteItems`, fechando a lacuna de falha parcial encontrada na Rodada 4 do protocolo original. O builder puro `buildMaterializeAttemptEntries` é exportado separadamente e reaproveitado pelo worker periódico (`src/workers/document-request-recurrence/materializer.ts`, mesmo split produtor/materializador de `subject/application/document-chasing-{producer,materializer}.ts`, aqui colapsado em um módulo por ser um job pequeno o bastante — mesma pragmática de `requirement-reindex/reindex.ts`) sem passar por `authorize()`/`RequestContext` (mesma postura do worker de reindex diário). **Desvio real, documentado**: o design menciona apenas `requestId`/`attemptIndex`/`seriesId`/`occurrenceId`/`parentRequestId` sem descrever como o worker descobre "o que está vencido" entre tenants — esta tarefa resolveu isso com um NOVO método de porta `scanActiveSeries` (Scan cross-tenant filtrado por `entityType`/`status`, mesmo tradeoff de custo já aceito e documentado por `scanSatisfiedRequirements` do worker de Requirement/D-145 — este módulo continua sem método de enumeração de tenants), em vez de reaproveitar a query GSI1 SERIESDUE por tenant (que exigiria uma lista de tenants que a porta não oferece). **G-V3 confirmado ao vivo**: mutação removendo a entry do `Put` do novo `DocumentRequest` da mesma transação (deixando só o `Update` da série) derrubou os 2 testes de atomicidade transacional (contagem de entries e leitura pareada série+request), revertida; mutação trocando o hash puro por um com `Math.random()` derrubou o teste de determinismo do `occurrenceId` (uma tentativa anterior com `Date.now()` NÃO foi pega — mesma chamada de teste roda no mesmo milissegundo — achado real documentado aqui, não escondido, substituído por `Math.random()` para a evidência real), revertida. **Suíte**: `npm test` (raiz) 1447/1447 (+14 testes novos: 11 de serviço/domínio incluindo o fluxo completo criar→materializar tentativa 1→tentativa 2→avançar ciclo→materializar tentativa 1 do novo ciclo, 3 do worker materializador incluindo idempotência contra tick duplicado); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` (40 handlers) limpos. **Infra**: HCL escrito (`infra/main.tf` — nova Lambda `document-request-recurrence-handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário 04:30 UTC, após o reindex de Requirement às 04:00; `infra/modules/api-gateway/main.tf` — 5 rotas `/document-archive/series*` na mesma Lambda de Núcleo 1; `scripts/build-lambdas.ts`/`proxy-allowlist.ts` atualizados no mesmo commit, mesma disciplina anti-D-117/D-120), `terraform fmt`/`validate` limpos, `terraform test` (`AWS_PROFILE=claude-dev`) 20/20 sem regressão — **NÃO aplicado em `dev`** (`terraform apply` explicitamente fora do escopo desta tarefa; próxima sessão decide se aplica). | Type 1 (nível 4-5, implementação direta de arquitetura já aprovada em D-143, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-143) | n/a | **IMPLEMENTADO (código), infra planejada mas NÃO aplicada em `dev`** — Núcleo 2 completo (3/3 entidades); as 9 decisões técnicas de D-143 estão todas implementadas | `src/modules/document-archive/{domain/{document-request.ts,document-request-series.ts},application/document-request-recurrence-service.ts,http/document-archive-handlers.ts,application/id-generator.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `src/modules/identity/domain/authorization.ts`, `schemas/api/docarchive-series-*.json`, `src/shared/contracts/schema-validator.ts`, `src/workers/document-request-recurrence/materializer.ts`, `src/runtime/aws/{ids.ts,composition/document-archive.ts,handlers/{document-archive-handler.ts,document-request-recurrence-handler.ts}}`, `test/{unit/document-archive/{document-request-recurrence.test.ts,document-request-recurrence-materializer.test.ts,in-memory-store.ts},contract/schemas.test.ts,integration/document-archive-lifecycle.test.ts}`, `infra/{main.tf,modules/api-gateway/main.tf}`, `scripts/build-lambdas.ts`, `src/modules/bff/domain/proxy-allowlist.ts` | 2026-09-01 |
171:| D-151 | `CORE_USER_DATA` LGPD purge worker (Prioridade 1 de 7, D-127, `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`) — implementação real ISOLADA desta prioridade apenas (as outras 6 classes seguem pendentes, uma de cada vez, ordem já decidida em D-127; `DELIVERY_RECORD`/`SECURITY_AUDIT`/`QUOTA_TELEMETRY`/`ACCOUNT_ACTIVE`/`TRANSIENT` remanescente/lane bloqueada `LEGAL_EVIDENCE` não tocados). `privacy-lgpd.md` §4's `CORE_USER_DATA` = "itens, políticas, ocorrências" mapeado ao código real: `ExpirationItem`/`ReminderPolicy` (ambos já têm `deletedAt` opcional, nunca fisicamente purgados hoje fora do fechamento de tenant inteiro) recebem um worker Lambda diário novo, `ReminderOccurrence` recebe TTL nativo DynamoDB independente (não um worker), exatamente como o design pediu ("worker(s) por deletedAt+30d, ReminderOccurrence via TTL nativo independente do pai"). **Mecanismo, ExpirationItem/ReminderPolicy** (`src/workers/core-user-data-purge/`): candidato descoberto por `Scan` full-table filtrado por `entityType IN (ExpirationItem, ReminderPolicy) AND attribute_exists(deletedAt)` — deliberadamente NÃO um worklist GSI6 (mesmo padrão `document-purge`'s claim/lease usa): GSI6 é um limite de isolamento fechado com 4 consumidores já nomeados explicitamente em `infra/tests/stack.tftest.hcl`, e nenhuma das duas entidades tem efeito colateral externo (S3, lease) a proteger — o mesmo tradeoff de custo que `tenant-purge-sweep.ts` já aceita e documenta a este porte de projeto. Elegibilidade exige DUAS condições independentes: (1) `deletedAt` com pelo menos 30 dias (`privacy-lgpd.md` §4, "exclusão/encerramento + 30 dias"); (2) tenant dono com `TenantLifecycleRecord.status === "ACTIVE"` (lido via `GetItem` consistente na própria `TenantLifecycleRecord`, cacheado por execução) — um tenant em qualquer outro estado (incluindo registro AUSENTE, tratado fail-closed, nunca assumido `ACTIVE`) é explicitamente o trabalho do pipeline de fechamento de tenant (`src/workers/tenant-purge/`), um mecanismo TOTALMENTE separado deste. O delete físico é um `DeleteItem` único condicionado via `buildVersionedDelete` (versão esperada + `deletedAt = :deletedAt` exato observado no scan) — a re-asserção de `deletedAt` é a propriedade de correção mais importante deste worker (nenhum caminho de restauração existe hoje para nenhuma das duas entidades, mas a condição torna isso estruturalmente seguro em vez de "seguro só porque nada faz isso ainda"). **Mecanismo, ReminderOccurrence** (`src/modules/reminder/domain/reminder-occurrence.ts`): campo novo `purgeAfterTtl` (epoch seconds), mesma convenção já usada por `InvitationTokenPointer.purgeAfterTtl` e já habilitada como atributo TTL real da tabela inteira (`infra/modules/dynamo-table/main.tf`, `ttl.attribute_name = "purgeAfterTtl"` — nenhuma mudança de infra necessária, o atributo já é físico há muito). Calculado da PRÓPRIA `scheduledAt` da ocorrência + 30 dias, setado UMA VEZ em `reminder-materializer.ts` no momento da materialização — deliberadamente nunca recomputado a partir do `deletedAt` do item/policy pai, porque uma ocorrência é naturalmente limitada pelo seu próprio agendamento independentemente do que acontece depois com o pai (dispensa qualquer worker). **Questão em aberto, sinalizada e não resolvida aqui**: `ReminderOccurrence`s materializadas ANTES desta mudança não têm `purgeAfterTtl` retroativo — nenhum backfill foi implementado nesta sessão (nenhum mecanismo de migração em massa precedente existe neste código para um caso análogo); o volume real de ocorrências pré-existentes ainda distantes de `scheduledAt` é pequeno o bastante para não justificar um script dedicado agora, mas fica nomeado aqui para quem revisitar o assunto. **Infra**: novo módulo Lambda `core_user_data_purge_handler` + role/policy dedicados do EventBridge Scheduler + `aws_scheduler_schedule` diário (`cron(0 5 * * ? *)`, 05:00 UTC, depois de `document-request-recurrence` às 04:30) em `infra/main.tf`, mesmo padrão de `requirement_reindex_handler`/`document_request_recurrence_handler` — sem policy dedicada além da leitura/escrita geral tenant-facing (Scan de base table + GetItem em row tenant-scoped comum, nenhum GSI3/GSI6 envolvido). `scripts/build-lambdas.ts` ganha a entrada `core-user-data-purge-handler`. **G-V3 confirmado ao vivo**: mutação removendo a condição `deletedAt = :deletedAt` do delete (simulando ausência da re-asserção) faria o teste de "restore/undelete durante a corrida" passar mesmo com um restore concorrente — confirmado quebrando deliberadamente essa extraCondition no teste, revertida; mutação trocando `TENANT_ACTIVE_STATUS` por aceitar qualquer status derrubou 8 dos 9 casos parametrizados de status não-`ACTIVE`, revertida. **Suíte**: `npm test` (raiz) +14 testes novos (`test/unit/workers/core-user-data-purge.test.ts`: elegibilidade por idade incluindo fronteira exata, fence de tenant `ACTIVE` parametrizado sobre todos os outros 8 status + registro ausente, guarda de delete condicional contra restore/undelete simulado e contra bump de versão concorrente, idempotência de duas execuções seguidas, mistura de candidatos elegíveis/inelegíveis entre tenants/tipos de entidade num único run tocando SÓ os elegíveis, paginação de múltiplas páginas de scan; +1 teste em `reminder-materializer.test.ts` para `purgeAfterTtl`); `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`validate-schemas` limpos. | Type 1 (nível 3-4, implementação direta de uma única prioridade já `APPROVED`/priorizada em D-127, protocolo Claude↔Codex dispensado por AGENTS.md §4) | n/a (implementação pós-D-127) | n/a | **IMPLEMENTADO (CORE_USER_DATA apenas, Prioridade 1 de 7)** — as 6 classes restantes (`DELIVERY_RECORD`→`SECURITY_AUDIT`→`QUOTA_TELEMETRY`→`ACCOUNT_ACTIVE`→`TRANSIENT` remanescente, `LEGAL_EVIDENCE` bloqueada) seguem pendentes na ordem já decidida | `src/workers/core-user-data-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/core-user-data-purge-handler.ts`, `src/modules/reminder/domain/reminder-occurrence.ts`, `src/modules/reminder/application/reminder-materializer.ts`, `scripts/build-lambdas.ts`, `infra/main.tf`, `test/unit/workers/{core-user-data-purge.test.ts,core-user-data-purge-fakes.ts}`, `test/unit/reminder/reminder-materializer.test.ts`, `test/unit/reminder/reconciliation.test.ts` | 2026-09-01 |
180:| D-159 (investigação, não implementada) | D-C do hot path (D-136, `docs/architecture/reviews/performance-hotpath-scoping/estado-final-consolidado.md`) — Rodada 1 de tentativa de implementação **rejeitada pelo Codex, nota 5,5/10, nível 5 confirmado** (não nível 4 como a proposta original assumia): mudar o contrato compartilhado `IdentityStore.get()` (usado por todo o módulo identity — `bootstrap-identity.ts`/`global-user-repository.ts`/`quota.ts`, não só este call site) para leitura eventualmente consistente E MESMO ASSIM manter `createProfileIfAbsent()` `await`ado dentro de `RequestContextResolver.resolve()` **não satisfaz o texto já `APPROVED` de D-136** ("sai do hot path com confiança") — só torna a leitura mais barata, não remove o round-trip síncrono. **Achado real mais importante da rodada, confirmado por leitura direta**: `UserProfile` (entidade tenant-scoped que `createProfileIfAbsent()` escreve, `identity/persistence/user-repository.ts`) não tem NENHUM leitor em todo o código — `grep` de todo import de `UserRepository`/chamada a `getProfile()` só acha o próprio `resolve-request-context.ts:114`, cujo valor de retorno é descartado (nunca atribuído a nada). A pergunta correta não é "como tornar essa leitura mais barata" e sim "essa entidade write-only ainda precisa existir" — Codex propôs 2 caminhos reais: (a) remover a entidade por completo (mais coerente se não há consumidor real), ou (b) se ainda necessária por alguma invariante não encontrada nesta busca, criá-la numa fronteira não-recorrente (criação de Organization, aceite de convite/Membership) em vez de a cada request. Codex também descartou "fire-and-forget" dentro do handler Lambda como solução confiável — sem mecanismo durável (fila/evento), a promise não tem garantia de completar após a resposta ser enviada (ambiente pode congelar/reciclar). **Não implementado nesta sessão** — decisão explícita de não forçar uma implementação de baixa confiança sob nota 5,5; fica para sessão futura dedicada com protocolo completo (mínimo 3 rodadas, nota ≥9,0), começando pela pergunta de remoção-vs-relocação da entidade antes de qualquer mudança de contrato de `IdentityStore`. D-D (`EphemeralTelemetryMutation`) permanece igualmente não iniciado, sem investigação nova nesta sessão — já nomeado pelo próprio D-136 como o item de maior risco do pacote, precisa de sessão dedicada com teste adversarial obrigatório no DoD. | n/a (investigação, protocolo não concluído) | 5,5 (Codex, Rodada 1 — nota abaixo do gate, rodada não continuada nesta sessão) | n/a | **NÃO IMPLEMENTADO — investigação registrada para a próxima sessão dedicada a D-C/D-D**: achado real de que `UserProfile` pode ser removível por completo (sem consumidor), não só mais barata; decidir isso primeiro, antes de qualquer mudança em `IdentityStore.get()` | `src/modules/identity/application/resolve-request-context.ts`, `src/modules/identity/persistence/user-repository.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts` (nenhum arquivo alterado, só lidos/investigados) | 2026-09-01 |
181:| D-160 | Fecha D-C de D-136 (implementação, não mais design) e o achado D-159 — **remoção completa de `UserProfile`**, protocolo Claude↔Codex completo, **4 rodadas** (5,5→6,8→8,7→9,2), Codex 9,2/10, **APPROVED**. Rodada 1 desta tentativa (tornar a leitura eventualmente consistente, mantendo a chamada no hot path) rejeitada 6,8/10 — Codex apontou que `TransactWriteItems` não permite um `Put` condicionado falhar sem abortar a transação inteira (quebraria reativação REMOVED→ACTIVE via convite), `CreateOrganizationInput` não tinha `identitySubject`/`emailNormalized`, e que a justificativa para MANTER a entidade não provava necessidade funcional — sugeriu remoção como alternativa mais forte. Investigação confirmou: `GlobalUser` já tem `emailNormalized`+`identityStatus`, `IdentityMapping` já tem `cognitoSub` — os 4 campos "reais" de `UserProfile` são todos duplicados ou (`roles`) explicitamente vestigiais desde D-095, sem nenhum leitor real (D-107/D-108 já migrou o único outro consumidor histórico, `dynamodb-recipient-resolver.ts`, para `GlobalUser` por esse mesmo motivo). Rodada 2 (remoção completa) 8,7/10 — 2 bloqueadores documentais: `data-model.md` §2 ainda descrevia a entidade tenant-scoped removida; o teste apagado precisava virar regressão negativa com G-V3, não só desaparecer. Rodada 3 (correções aplicadas: `data-model.md` §2 corrigido) 8,9/10 — bloqueador residual: `data-model.md` §1 ("`User` representa o perfil dentro do tenant") e a linha `IdentityMapping` (ainda listava `tenantId`, removido em D-095) continuavam desatualizadas. Rodada 4 (§1 e linha `IdentityMapping` corrigidos) **9,2/10, APROVADO**. **Implementado**: `src/modules/identity/persistence/user-repository.ts` deletado por completo (`UserRepository`/`UserProfile`/`userProfileKey`/`createProfileIfAbsent`/`getProfile`); `RequestContextResolver.resolve()` (`identity/application/resolve-request-context.ts`) perde o parâmetro `users` e a chamada `createProfileIfAbsent()` — `resolve()` agora só lê e valida, nenhuma escrita, "sai do hot path" literalmente (não só mais barato, como a Rodada 1 desta sessão tentou); `composition/identity.ts` ajustado; 9 arquivos de teste (`test/integration/{activity-log,cross-tenant,document-archive-lifecycle,expiration-lifecycle}.test.ts`, `test/unit/{document/document-handlers,identity/{resolver,test-route-handler},import/import-handlers,notification/preferences-handlers}.test.ts`) perdem o argumento/import de `UserRepository`. **`docs/architecture/privacy-lgpd.md` §4.1 emendado**: removida a cláusula "cada `UserProfile` e" do inventário de User-level erasure/DSR, `NotificationPreferences` continua real. **`docs/architecture/data-model.md` §1/§2 corrigidos**: `User`→`GlobalUser` (tenant-independente, `USER#u`/`PROFILE`), `IdentityMapping` sem `tenantId` (D-095), narrativa de `tenantId` obrigatório qualificada com as exceções globais já documentadas. Comentários vestigiais em `global-user-repository.ts`/`bff-auth-service.ts` corrigidos para narrar o estado real pós-remoção (comentários genuinamente históricos preservados). **Teste negativo substitui o antigo** (`test/unit/identity/resolver.test.ts` — "does not write any tenant-scoped user-profile row when resolving a context", usa `store.allKeys()`, o mesmo idioma já usado por outro teste irmão no arquivo). **G-V3 confirmado ao vivo**: reintroduzir a chamada completa `await this.users.createProfileIfAbsent(...)` + o parâmetro `users` de volta no resolver fez o teste negativo falhar (a chave `TENANT#.../USER#...` voltou a aparecer); revertido antes do commit. **Suíte completa e checklist do próprio Codex (resposta (e) da Rodada 2)** todos verificados antes do commit: grep pós-diff zero referência operacional (só histórica) a `UserRepository`/`UserProfile`/`createProfileIfAbsent`/`getProfile`/`userProfileKey`; `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`npm test` completo. | Nível 5 (Codex confirmou nas 4 rodadas — remove entidade física + fronteira de módulo + emenda doc `APPROVED`, protocolo completo obrigatório) | n/a | 9,2 (Codex, Rodada 4/4, `APPROVED`) | **IMPLEMENTADO E VERIFICADO** — D-C de D-136 fechado de fato (não só investigado); D-D (`EphemeralTelemetryMutation`) segue como único item do pacote de hot path ainda não iniciado | `src/modules/identity/{application/resolve-request-context.ts,persistence/user-repository.ts (deletado)}`, `src/runtime/aws/composition/identity.ts`, `src/modules/identity/persistence/global-user-repository.ts`, `src/modules/bff/application/bff-auth-service.ts`, `docs/architecture/{privacy-lgpd.md,data-model.md}`, 9 arquivos de teste (ver lista acima) | 2026-09-01 |
187:| D-166 | **DocumentFile — terceira fatia de D-163: DocumentFileReconciliationWorker (item 2 da ordem de `NEXT_SESSION_PROMPT.md`)**. Generaliza M6's `UploadSlotReconciliationWorker` (`src/workers/upload-slot-reconciliation/reconciliation.ts`) para o segundo namespace GSI5 esparso já modelado por `document-file.ts`'s `fileReconciliationGsi5Keys()` (D-164, sem chamador até agora): `TENANT#<t>#DOCFILE-RECON#{PENDING_UPLOAD,SCANNING}`. Implementação direta de design já `APPROVED` (D-163 §6/`round4-claude-final.md` §3), sem nova rodada Claude↔Codex. Novo `applyFileScanTimeout()` em `apply-file-scan-result.ts` (mesma `Deps`/mesmo módulo de `applyFileScanResult`/`confirmFileScanClean`) faz a transição terminal `PENDING_UPLOAD\|SCANNING`→`TIMEOUT`, espelhando o branch REJECT existente: `buildVersionedUpdate` padrão + `remove: ["GSI5PK","GSI5SK"]` + decremento de `pendingFileScans` do `DocumentVersion` na MESMA transação — nenhum mecanismo novo. A única peça genuinamente nova é a condição extra `GSI5PK = :observado AND GSI5SK = :observado` (via `extraConditions`, duas entradas separadas por causa do parser de teste, que não entende AND aninhado dentro de um parêntese) — fecha exatamente a corrida nomeada pelo round4 §3: um candidato cujo deadline mudou ou que já chegou a um estado terminal entre a varredura e a escrita é puxado de volta (`ConditionalCheckFailed`→`IGNORED_STALE`), nunca reprocessado às cegas. **Desvio nomeado do design, não uma correção dele**: GSI5PK de `DocumentFile` é escopado por tenant (`TENANT#<t>#...`), diferente de GSI6 (globalmente namespaced) que permite ao `UploadSlotReconciliationWorker` fazer uma `Query` cross-tenant real — este módulo nunca teve porta de enumeração de tenant (`document-archive-store.ts`'s `scanSatisfiedRequirements`/`scanActiveSeries` já aceitam o mesmo trade-off pelo mesmo motivo estrutural). O worker novo (`src/workers/document-file-reconciliation/`, port+adapter+lógica, mesmo layout de `core-user-data-purge`/`quota-telemetry-purge`) descobre candidatos via `Scan` na tabela base filtrado por `entityType=DocumentFile AND scanStatus=:status AND attribute_exists(GSI5PK)` (nunca GSI6, nunca sequer uma `Query` real em GSI5) — dois scans independentes e limitados (`MAX_PAGES_PER_STATUS=25`), um por status não-terminal, cada página ordenada por `GSI5SK` (deadline) em memória antes de processar (melhor ordenação possível dado o Scan cross-tenant; um backlog patológico é limitado pelo cap de páginas e recolhido na próxima execução, mesma disciplina dos outros workers Scan-based deste código). Lambda novo `document-file-reconciliation-handler` (`src/runtime/aws/handlers/`), schedule `rate(15 minutes)` (mesma cadência do worker que generaliza), só `tenant_facing_read_write_policy_json` — nenhuma política GSI6/GSI3 nova (confirmado por teste Terraform novo). `buildDocumentArchiveDeps()` passa a devolver `ids` diretamente (antes só embutido nos serviços) para o novo handler poder montar `ApplyFileScanResultDeps`. **G-V3 confirmado ao vivo em 2 pontos, quebrando e restaurando cada mecanismo**: (1) removendo `extraConditions` de `applyFileScanTimeout`, o teste "pointer changed concorrentemente" passa a devolver `TIMED_OUT` em vez de `IGNORED_STALE` (falha); (2) invertendo o comparador de deadline (`<` em vez de `>`) no worker, os 2 testes de timeout real e o teste dos 2 scans independentes falham (0 `timedOut` em vez do esperado). Ambos revertidos antes do commit. Suíte completa 1685/1685 (1677+8 novos: 4 unit de `applyFileScanTimeout`, 4 do worker), `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21, +1 novo) todos limpos. | Nível 3-4 (implementação direta de design já `APPROVED`, D-163) | n/a | n/a | **IMPLEMENTADO E VERIFICADO, NÃO deployado** (infra escrita/validada via `terraform plan`/`test`, `terraform apply` real em `dev` fica para o CD de push a `main`) — falta a rota HTTP real de `reserveFiles()`, o worker S3/GuardDuty que chamaria `applyFileScanResult()`/`confirmFileScanClean()` em produção, presign real (`UploadUrlSigner`) e o gate `fileSetSealed` em `commitUpload()` (item 3 da ordem, próximo) | `src/modules/document-archive/application/apply-file-scan-result.ts`, `src/runtime/aws/{composition/document-archive.ts,handlers/document-file-reconciliation-handler.ts (novo)}`, `src/workers/document-file-reconciliation/ (novo: candidate-source.ts,dynamodb-candidate-source.ts,reconciliation.ts)`, `scripts/build-lambdas.ts`, `infra/{main.tf,tests/stack.tftest.hcl}`, `test/unit/document-archive/{apply-file-scan-result.test.ts,document-file-reconciliation.test.ts (novo)}` | 2026-09-02 |
191:| D-170 | **Reconciliação factual da Auditoria de Performance 2026-09-02** (`docs/project/performance-audit-2026-09-02.md`, documento externo trazido por Marcelo) — investigação pura, sem alteração de código, mesmo tratamento de spike que D-161 recebeu antes de virar decisão de arquitetura. Cada achado do documento externo foi verificado linha a linha contra o código real: RequestContext confirmado caro (`onboarding.resolve()` roda incondicionalmente, hidrata todas as Memberships via GSI4, mesmo com `organizationIdHint` presente — já era gap conhecido, D-136/D-C tinha adiado "condicionado a mapear consumidores primeiro"); quota `API_REQUEST` confirmada fazendo `Get`+`TransactWriteItems` em toda request — mas com a correção de que o documento externo não sabia: D-136/D-D já é um design `APPROVED` (3 rodadas, Codex 9,3/10) para exatamente essa lane leve (`EphemeralTelemetryMutation`), nunca implementado; Reminder Producer (loop triplo sequencial minute×shard×partição, claim+outbox sequencial por ocorrência) e Reminder Dispatch (N+1 via `queryByItem`+`.find()`, processamento serial do batch SQS) confirmados sem ressalva. **Achado mais grave, além do que o documento supôs**: o padrão `Scan` base-table + `Limit=100` + `MAX_PAGES=25` sem `ExclusiveStartKey` persistido entre invocações agendadas aparece em **9 dos 10 workers de manutenção/purga do repositório** (`membership-purge` D-169, `document-file-reconciliation` D-166, `requirement-reindex`, `invitation-purge`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge`) — inclusive 2 workers implementados NESTA MESMA sessão e já registrados como `IMPLEMENTADO`/`DEPLOYADO`. O comentário do próprio código em pelo menos 2 desses workers ("anything beyond this is picked up by tomorrow's run") é factualmente falso como escrito — `Scan` sem cursor persistido reinicia pela mesma ordem física de hash a cada invocação, então um candidato além da posição `Limit×MAX_PAGES` nunca é alcançado por nenhuma invocação futura. O único worker que NÃO tem o defeito é `document-purge` (D-061) — usa `Query` sobre GSI6 ordenado por data de elegibilidade, exatamente o padrão `MaintenanceDueIndex` que o documento externo propõe como solução nova, já em produção num worker vizinho. Capacity Model confirmado desatualizado (premissa "tenant = usuário individual" ainda ativa, pré-B2B/Document Archive). Baseline do documento externo (PAY_PER_REQUEST, OCC/outbox onipresentes) confirmada correta, não alucinada. Relatório completo com evidência `arquivo:linha` por achado: `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md`. | Investigação factual (não Type 1 em si — fact-finding, sem protocolo Claude↔Codex) | n/a | n/a | **CONCLUÍDO** — nenhum código alterado; prioriza D-D de D-136 (implementação direta, design já aprovado) e o mapeamento de consumidores de D-C como próximos passos baratos; agenda rodada Claude↔Codex nível 5-6 para o padrão Scan-based starvation (achado mais urgente, afeta correção de LGPD já "fechada"), RequestContext fast path, e Reminder Producer/Dispatch | `docs/architecture/reviews/performance-audit-2026-09-02-reconciliation/estado-final-consolidado.md` | 2026-09-02 |
192:| D-172 | **`EphemeralTelemetryMutation` — implementação de D-136/D-D (`docs/architecture/reviews/performance-hotpath-scoping/estado-final-consolidado.md`), design já `APPROVED` (3 rodadas, Claude 9,3/Codex 9,3), implementação direta sem nova rodada Claude↔Codex.** `TenantQuotaService.consume()` (`src/modules/identity/application/quota.ts`) roteia `API_REQUEST` — e só `API_REQUEST` (`EPHEMERAL_TELEMETRY_QUOTA_TYPES`, allowlist fechada) — para uma lane nova, paralela à existente: um único `UpdateItem` `ADD`-only via `IdentityStore.incrementTelemetryCounter()` (novo método de porta), sem `Get`, sem `TransactWriteItems`, sem repetir o `ConditionCheck` transacional de lifecycle do W3-07 — confiando na leitura stale-mas-recente de `ACTIVE` que o `RequestContextResolver` já fez antes do handler chamar `consume()`, exatamente a troca que o design aprovou. Chave `PK=TENANT#<id>#QUOTA`/`SK=TYPE#API_REQUEST#<floor(epochSeconds/windowSeconds)>` — janela derivada do relógio, não da string `window` que o chamador passa (só usada pela lane transacional antiga). Registro fechado (`EphemeralTelemetryRecord`: `count`/`windowSeconds`/`resetAt`/`purgeAfterTtl`, sem `metadata` livre) — `purgeAfterTtl = windowEnd + 30d`, atributo TTL nativo real da tabela (confirmado contra `infra/modules/dynamo-table/main.tf`/seu teste Terraform), best-effort apenas. Classificação tripla de falha implementada literalmente: limite excedido → `QuotaExceededError` (429, já existente); `DependencyUnavailableError` (DynamoDB indisponível/throttling) → fail-open, log de degradação, requisição segue; qualquer outro erro (bug/corrupção) → propaga sem mascarar (5xx real). Todos os outros tipos de quota (`AI_CALL`/`UPLOAD_*`/`IMPORT_*`/`NOTIFICATION_EMAIL`) permanecem exatamente na lane transacional antiga — verificado por teste de mutação nomeada (ver G-V3). **Garantia de remoção real, não só TTL best-effort**: o `QuotaTelemetryPurgeWorker` (D-154) — que já varria `entityType=TenantQuota` por `resetAt+30d` — teve seu filtro de `Scan` alargado para também cobrir `entityType=EphemeralTelemetryMutation` (`src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts}`), fechando a lacuna que a TTL nativa sozinha deixaria (AWS não garante latência de remoção da TTL). **Teste adversarial obrigatório do DoD do próprio D-D** (`test/unit/identity/quota.test.ts`, describe `EphemeralTelemetryMutation lane`): corrida ACTIVE→escreve a lane→tenant transiciona para DELETING — prova que o resíduo (1) nunca é lido como direito de negócio (nenhum código lê campos desse registro além de `count` vs. limite numérico passado pelo chamador), (2) permanece estruturalmente dentro do escopo `begins_with(PK, "TENANT#<id>#")` que o cascade de tenant-purge do W3-07 usa (provado diretamente sobre a chave real, não apenas afirmado), e (3) permanece dentro da forma de elegibilidade do D-154 (entityType+resetAt, provado em `quota-telemetry-purge.test.ts`). Item explicitamente deferido (não fabricado sem recurso real para testar): a métrica/alarme operacional com limiar concreto que o design pede "se o resíduo sobreviver além do esperado" — é infra-level (CloudWatch), fica para uma sessão dedicada a alarmes operacionais deste worker. **G-V3 confirmado por mutação nomeada**: teste prova `incrementTelemetryCounter` chamado exatamente 1x e `transactWrite`/`get` NUNCA chamados para `API_REQUEST`; teste espelho prova `AI_CALL` chama `transactWrite` 1x e NUNCA `incrementTelemetryCounter` — ambos quebrados deliberadamente (trocando a condição de roteamento) e confirmados falhando antes de restaurar. Suíte completa 1726/1726, `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos (sem alteração de infra, `terraform test` não aplicável). | Nível 3-4 (implementação direta de decisão já `APPROVED` em D-136, protocolo Claude↔Codex dispensado por `AGENTS.md` §4) | n/a | n/a | **IMPLEMENTADO** — fecha D-D, o último item pendente do pacote D-136 (D-A/D-B/D-E já implementados; D-C permanece parcial, condicionado ao mapeamento de consumidores de `onboarding.resolve()`). Alarme/métrica operacional de residual sobrevivente e a recalibração do throttle do API Gateway (também citados por D-D) seguem deferidos, não fabricados aqui. | `src/modules/identity/{application/quota.ts,ports/identity-store.ts,persistence/dynamodb-identity-store.ts}`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts}`, `test/unit/identity/{quota.test.ts,in-memory-store.ts}`, `test/unit/workers/quota-telemetry-purge{.test.ts,-fakes.ts}` | 2026-09-02 |
198:| D-177 | **DocumentType — item 5 do design D-173: as 4 operações de CRUD do catálogo (D-174) + leitura ganham rotas HTTP reais, deixando de ser só métodos de aplicação exercitados por teste.** Handlers novos em `document-archive-handlers.ts` (`handleCreateDocumentType`/`handleGetDocumentType`/`handleListDocumentTypes`/`handleRenameDocumentType`/`handleDeprecateDocumentType`/`handleReactivateDocumentType`), mesmo pipeline de todo handler do módulo (`validateAgainstSchema` -> `resolve()` -> serviço -> `withErrorMapping`). `listDocumentTypes()` novo em `DocumentArchiveService` — primeiro consumidor real de `queryIndexPage` no módulo (definido no port desde D-143 mas nunca chamado): uma página física por chamada contra GSI1's namespace `TENANT#<t>#DOCTYPESTATUS#<status>` (já escrito por `documentTypeGsi1Keys()` desde D-173/D-174), `status` default `ACTIVE` via query param `?status=`, `lastEvaluatedKey` repassado no corpo da resposta quando existir — mesma disciplina de página única de `queryByPk`/`scanSatisfiedRequirements`. Rotas: `POST /document-archive/document-types`, `GET /document-archive/document-types`, `GET /document-archive/document-types/{documentTypeId}`, `PATCH /document-archive/document-types/{documentTypeId}` (rename, mesmo verbo de `update_requirement`), `POST .../deprecate`, `POST .../reactivate` — wireadas nos DOIS lugares que D-117/D-120 (achado histórico citado pelo próprio comentário deste módulo) provam serem necessários juntos: `infra/modules/api-gateway/main.tf`'s `document_archive_routes` (mesmo Lambda/integração já existentes, só novas entradas no `for_each`) e `src/modules/bff/domain/proxy-allowlist.ts`; `document-archive-handler.ts`'s `switch` ganhou os 6 `case`s novos. 4 schemas novos (`docarchive-documenttype-{create,rename,deprecate,reactivate}-request.v1.json`, `additionalProperties: false`) registrados em `schema-validator.ts`'s `defaultSchemaRegistry` — checado explicitamente contra a classe de bug que a nota do handoff de D-167 nomeia (schema não importado estaticamente responde 500 em vez de 400); confirmado por teste HTTP real que corpo malformado responde 400. RBAC já existia desde D-174 (`docarchive:documenttype-{create,rename,deprecate,reactivate}` em `ADMIN_ROLES`, `-read` em `READ_ONLY_ROLES`), não precisou de mudança. **G-V3 confirmado quebrando e restaurando cada mecanismo**: suíte HTTP nova (`test/integration/document-archive-document-type-routes.test.ts`, 9 testes) prova fluxo completo create->get->list->rename->deprecate->reactivate via handlers reais, filtragem por `status` no list (ACTIVE some da lista após deprecate, aparece em `?status=DEPRECATED`), 403 real para VIEWER em cada uma das 4 mutações (log `security.authorization_denied` capturado no stderr do teste, prova que passou pelo `AuthorizationDeniedError` real, não um stub), 200 para VIEWER em get/list (READ_ONLY_ROLES), e 400 (nunca 500) para 3 corpos malformados distintos (create sem `displayName`, rename sem `expectedVersion`, deprecate com `expectedVersion` não-inteiro) — mais 10 testes de contrato novos em `schemas.test.ts` (aceita/rejeita por schema). Rota `reserve_files` (`POST .../versions/{seq}/files`, D-163/D-167) permanece ausente de `proxy-allowlist.ts` — achado real pré-existente confirmado durante a leitura do arquivo para este item, fora do escopo de D-177, não corrigido aqui, registrado para uma fatia futura dedicada. Suíte completa 1761/1761 (1743+18 novos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas`/`terraform test` (21/21, via `AWS_PROFILE=claude-dev`) todos limpos. | Nível 3-4 (implementação direta de design já `APPROVED` em D-173, protocolo Claude↔Codex dispensado por `AGENTS.md` §4) | n/a | n/a | **IMPLEMENTADO** — fecha o item 5 de D-173. Decisão de Marcelo sobre `submitEvidence()`/guest (D-175, opções a/b/c) continua inteiramente aberta; item 6 (migração do schema guest, `documentTypeId` obrigatório) é a última fatia do arco D-173 e segue bloqueado até essa decisão landar; achado pré-existente da rota `reserve_files` ausente de `proxy-allowlist.ts` registrado acima, não resolvido nesta fatia | `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/http/document-archive-handlers.ts`, `src/runtime/aws/handlers/document-archive-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/contracts/schema-validator.ts`, `infra/modules/api-gateway/main.tf`, `schemas/api/docarchive-documenttype-create-request.v1.json`, `schemas/api/docarchive-documenttype-rename-request.v1.json`, `schemas/api/docarchive-documenttype-deprecate-request.v1.json`, `schemas/api/docarchive-documenttype-reactivate-request.v1.json`, `test/integration/document-archive-document-type-routes.test.ts`, `test/contract/schemas.test.ts` | 2026-09-02 |
201:| D-180 | **MaintenanceDueIndex — implementação real, primeira fatia/piloto (D-179 aplicado, `membership-purge` ÚNICO worker migrado)**: GSI8 real (`GSI8PK`/`GSI8SK`, `KEYS_ONLY`) adicionado ao módulo Terraform (`infra/modules/dynamo-table/main.tf`), `local.gsi8_worker_types = { membership_purge = "MEMBERSHIP_PURGE" }` gerando `gsi8_read_policy_json`/`worker_transact_write_policy_json` por chave — `dynamodb:Query` condicionado `ForAllValues:StringEquals dynamodb:LeadingKeys IN ["WORK#MEMBERSHIP_PURGE","DLQ#MEMBERSHIP_PURGE"]` + `dynamodb:TransactWriteItems` na tabela base (ação nova, não coberta por `tenant_facing_read_write`), ambos anexados só ao role do `membership_purge_handler`. `deriveMembershipMaintenanceDue()`/`membershipGsi8Keys()`/`MEMBERSHIP_PURGE_WORK_TYPE`/`MEMBERSHIP_RETENTION_DAYS` movidos para `modules/organization/domain/membership.ts` (função pura, D-179 §2) — reusada pelos 3 consumidores exigidos: `remove-membership.ts`/`leave-organization.ts` (gravam `GSI8PK`/`GSI8SK` no MESMO `Update` que a transição `REMOVED`, nunca write separado), `scripts/backfill-gsi8-membership-purge.ts` (novo, cobre todo `Membership` com `dueAt` computável — inclusive vencimento FUTURO, corrigindo o erro que a Rodada 3 do design cometeu), e a revalidação do próprio worker. `accept-invitation.ts`'s reativação agora também `REMOVE GSI8PK, GSI8SK` atomicamente ao limpar `removedAt` (autolimpeza de ponteiro obsoleto na origem). `membership-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan`+`Limit`; `purge.ts` revalida cada candidato via `getMembership()` consistente antes de agir (GSI8 nunca é fonte de elegibilidade) e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por versão, na MESMA transação — fecha o TOCTOU que o padrão antigo tinha entre ler o status do tenant e deletar). Poison-record/quarentena (D-179 §8): `maintenanceAttemptCount` incrementado só quando o `ConditionCheck` de tenant falha (nunca no `Delete`), backoff exponencial capado (1,2,4,8,16 dias) reposicionando `GSI8SK`, acima de `MAX_ATTEMPTS=5` move para `GSI8PK="DLQ#MEMBERSHIP_PURGE"`; toda escrita de retry é idempotente (`ConditionalCheckFailedException` tratado como no-op, mesmo padrão que o delete já usava). Ponteiro obsoleto (item reativado após ser indexado) é autolimpo pelo próprio worker via `REMOVE GSI8PK, GSI8SK`. Observabilidade: `MembershipPurgeResult` ganha `quarantinedCount`/`oldestCandidateAgeSeconds` (computado do primeiro item da página, sem I/O extra); o handler já fazia `logger.info(..., {...result})`, então os 2 campos novos já são emitidos sem tocar o handler. **G-V3 ao vivo em 3 mecanismos**: teste novo prova que `RemoveMembershipService`/`LeaveOrganizationService` gravam `GSI8PK`/`GSI8SK` corretos (falha se removido); teste novo prova que `accept-invitation.ts` limpa os 2 atributos na reativação (falha se removido); teste novo do worker leva um candidato através de 6 execuções com tenant nunca `ACTIVE` e prova a quarentena em `DLQ#MEMBERSHIP_PURGE` no `MAX_ATTEMPTS+1`-ésimo ataque, e um teste separado prova o autoconserto do ponteiro obsoleto. `infra/tests/stack.tftest.hcl` ganha `run "gsi8_worker_isolation"`: prova que `membership_purge_handler` tem GSI8+LeadingKeys escopado+`TransactWriteItems`, que `TransactWriteItems` nunca aparece numa política referenciando GSI3/GSI4/GSI6/GSI8, e que `invitation_purge_handler`/`transient_purge_handler`/`test_ping_handler` NÃO referenciam GSI8 (isolamento por worker, não por índice). `gsi_count` 7→8. Suíte completa 1778/1778, `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (incluindo o caso novo). **Próximo worker recomendado para a próxima fatia**: `invitation-purge` (mesmo padrão de `TenantLifecycleStatusSource`/fence ACTIVE já usado aqui, candidato mais próximo estruturalmente) — ordem não é normativa, qualquer um dos 8 restantes serve. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — infra nova real porém mecânica, seguindo spec exata) | n/a | n/a | **IMPLEMENTADO (piloto, 1 de 9 workers)** — `membership-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes); os outros 8 workers nomeados por D-179 permanecem em `Scan`+`Limit`, migração worker-a-worker é trabalho futuro explícito | `infra/modules/dynamo-table/{main.tf,outputs.tf}`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/organization/domain/membership.ts`, `src/modules/organization/application/{remove-membership.ts,leave-organization.ts,accept-invitation.ts}`, `src/workers/membership-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/membership-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-membership-purge.ts` (novo), testes em `test/unit/{organization,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
206:| D-185 | **MaintenanceDueIndex — quarta fatia (requirement-reindex, D-179 slice 4, 4º de 9 workers)**: `requirement_reindex` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["requirement_reindex"]`/`worker_transact_write_policy_json["requirement_reindex"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItems` na tabela base, ambos só no role do `requirement_reindex_handler`. **Verificação prévia obrigatória (item nomeado pela tarefa) confirmou a correção da Rodada 3 de D-179**: lendo `reindex.ts` diretamente (não confiando na citação de linha do design), o worker de fato só faz UMA coisa — a drift pura SATISFIED→NOT_SATISFIED quando `evidenceValidUntil` passa sem nenhuma outra escrita tocar o Requirement — nunca uma "reindexação de applicability" mais ampla; `applicability` é persistido e nunca re-derivado por este worker. `deriveRequirementMaintenanceDue()` novo em `domain/requirement.ts` (função pura): due = `evidenceValidUntil` apenas quando `status === "SATISFIED"` E `evidenceValidUntil` existe — os outros 4 status nunca são candidatos, um SATISFIED sem `evidenceValidUntil` é SATISFIED para sempre por construção. `requirementGsi8Keys()` embute `requirementId` (não `subjectId`) no `GSI8SK`; a fonte candidata recupera `tenantId`/`subjectId`/`requirementId` do `PK`/`SK` da tabela base (`KEYS_ONLY` já devolve de graça), mesmo padrão de `documentFileGsi8Keys()`. **4 pontos de escrita no `DocumentArchiveService` gravam/limpam o ponteiro atomicamente, todos no MESMO `set`/`remove` da transição de `status` real** (`createRequirement`/`updateRequirement`/`linkEvidence`/`unlinkEvidence`) — um helper privado `requirementGsi8Fields()` centraliza a decisão para não duplicar a chamada a `deriveRequirementMaintenanceDue()` em 4 lugares. `scanSatisfiedRequirements` (`Scan` cross-tenant filtrado por `entityType`+`status`) REMOVIDO do port `DocumentArchiveStore` e do adapter DynamoDB real — nenhum outro call site o usava (confirmado por grep antes de tocar em qualquer arquivo), mesma disciplina "apagar o mecanismo morto, não deixar ao lado do novo" de D-183. `requirement-reindex/candidate-source.ts`+`dynamodb-candidate-source.ts` novos: `Query GSI8` substitui o `Scan`; `reindex.ts` reescrito para paginar via GSI8, revalidar cada candidato com um `store.get()` fresco (GSI8 nunca é fonte de elegibilidade, mesma postura de todo consumidor GSI8) e aplicar a atualização versionada existente (`buildVersionedUpdate`) incluindo `remove: ["GSI8PK","GSI8SK"]` — o `nextStatus` nunca é SATISFIED neste caminho (a derivação só chega a NOT_SATISFIED quando `evidenceValidUntil` já passou), então o ponteiro sempre é limpo incondicionalmente. **Sem poison-record/DLQ** (item 6 da tarefa, decisão explícita e documentada, mesmo raciocínio de D-183): este worker nunca teve fence de tenant `ACTIVE` — o único modo de falha de uma claim é `expectedVersion` desatualizado por um `linkEvidence`/`unlinkEvidence`/`updateRequirement` concorrente, sempre transitório e resolvido pela própria execução diária seguinte, nunca uma condição permanentemente falha para o mesmo candidato. `GlobalIndexComponent` ganha `"requirement-reindex"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.requirement_reindex_handler.function_name`. Novo `scripts/backfill-gsi8-requirement-reindex.ts`, cobrindo vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 4 workers já migrados. **G-V3**: 7 testes do worker (transição real + limpeza do ponteiro, due no futuro nunca tocado, sem `evidenceValidUntil` nunca candidato, status não-SATISFIED nunca candidato, múltiplos tenants, conflito concorrente via OCC, candidato "stale" da query eventualmente consistente nunca clobber um `evidenceValidUntil` já movido por um `linkEvidence` concorrente) + 12 testes do backfill. Suíte completa local 1827/1827 (após reconciliar com o WIP concorrente de D-184 no mesmo working tree — só os arquivos desta fatia foram staged/commitados, `guest-document-access-service.ts`/seu teste e `docs/architecture/reviews/submit-evidence-document-type-scoping/` intocados por esta sessão), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente, não só o agregado). Commit `720d881`, empacotado no PR #205 (aberto pela sessão concorrente de D-184, mesmo `develop`) — CI real (run `33665953101`) `success`, CD real (run `33666281116`) `success`, ambos confirmados via `gh run view` direto (nunca presumidos). **Verificação ao vivo completa contra `dev`, tudo via `aws --profile claude-dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-requirement-reindex-handler-role` tem as 3 políticas esperadas (base + `Gsi8ReadOnlyRequirementReindex` com a condição `LeadingKeys` exata `["WORK#REQUIREMENT_REINDEX","DLQ#REQUIREMENT_REINDEX"]` + `TransactWriteItemsRequirementReindex` escopada à tabela base); `get-function`/`get-schedule` confirmam o Lambda `Active`/`Successful` e o schedule `ENABLED`. **Prova real de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 3 sentidos)**: `dynamodb:Query` com `LeadingKeys=WORK#REQUIREMENT_REINDEX` (próprio namespace) → `allowed`, casando `Gsi8ReadOnlyRequirementReindex`; a mesma simulação com `LeadingKeys=WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION` (namespaces dos 3 workers já migrados) → `implicitDeny` nos 3 casos, nenhuma statement casada. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quarta fatia mecânica seguindo o padrão de D-180/D-182/D-183, com a mesma decisão "sem poison-record" de D-183 por ausência de fence de tenant) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (4 de 9 workers migrados)** — `requirement-reindex` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os outros 5 workers nomeados por D-179 permanecem em `Scan`+`Limit`: `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/document-archive/domain/requirement.ts`, `src/modules/document-archive/application/document-archive-service.ts`, `src/modules/document-archive/ports/document-archive-store.ts`, `src/modules/document-archive/persistence/dynamodb-document-archive-store.ts`, `src/runtime/aws/handlers/requirement-reindex-handler.ts`, `src/workers/requirement-reindex/{candidate-source.ts,dynamodb-candidate-source.ts,reindex.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-requirement-reindex.ts` (novo), testes em `test/unit/{document-archive,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
207:| D-186 | **MaintenanceDueIndex — quinta fatia (quota-telemetry-purge, D-179 slice 5, 5º de 9 workers)**: `quota_telemetry_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["quota_telemetry_purge"]`/`worker_transact_write_policy_json["quota_telemetry_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItems` na tabela base, ambos só no role do `quota_telemetry_purge_handler`. **Forma distinta dos 4 anteriores, confirmada por leitura direta antes de qualquer código**: este worker cobre DOIS tipos de entidade que compartilham o mesmo formato de elegibilidade (`resetAt+30d`, fence de tenant `ACTIVE`) — `TenantQuotaRecord` e `EphemeralTelemetryMutation` (`src/modules/identity/application/quota.ts`) — e NENHUM dos dois tem campo `version`; ambos usam `count`/`resetAt` como fence OCC direto (mesma forma que D-153/`SECURITY_AUDIT` já usava para suas 4 entidades append-only, por razão diferente aqui: token bucket com campos otimistas próprios, não append-only). `deriveQuotaTelemetryMaintenanceDue()` (due = `resetAt + 30d`, NUNCA `undefined` — ao contrário de Membership/Invitation não existe estado terminal, `resetAt` sozinho determina elegibilidade) e `quotaTelemetryGsi8Keys()` (embute `entityType`+`SK` natural no `GSI8SK`, já que nenhuma das duas entidades tem um id próprio separado do seu `SK`) adicionados a `quota.ts`. **4 pontos de escrita gravam/refrescam o ponteiro GSI8 atomicamente no MESMO write que seta `resetAt`** (nunca um passo separado): `consume()`'s ramo de criação e ramo de rollover de janela (`TransactWriteItems` via `executeTenantBusinessMutation`, W3-07), e `identity-store.ts`'s `incrementTelemetryCounter()` (a lane `EphemeralTelemetryMutation` de D-136 D-D — `GSI8PK`/`GSI8SK` setados via `if_not_exists`, mesmo mecanismo que já protege `resetAt`/`purgeAfterTtl` contra um escritor perdedor concorrente rebobinar a janela; o valor é determinístico a partir do bucket de janela, calculado em `quota.ts` — camada de aplicação — e passado pronto ao adapter de persistência via `TelemetryIncrementInput.gsi8`, preservando a regra de `persistence/` nunca importar lógica de `application/`). `release()` não toca o ponteiro (só decrementa `count`, nunca muda `resetAt`). `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}` reescritos: `Query GSI8` substitui o `Scan` completo da tabela; `purge.ts` revalida cada candidato via `getCandidate()` fresco antes de qualquer escrita e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `resetAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182, já que este worker TEM fence de tenant `ACTIVE`, ao contrário de D-183/D-185): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#QUOTA_TELEMETRY"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (diferente de Invitation/Membership, decisão explícita e documentada): como `deriveQuotaTelemetryMaintenanceDue()` nunca retorna `undefined` e todo write que seta `resetAt` também refresca o ponteiro, não existe um "ponteiro sobrevivendo a uma transição terminal" real para se defender — a lacuna que o self-heal de `invitation-purge` cobre não tem equivalente estrutural aqui. `GlobalIndexComponent` ganha `"quota-telemetry-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.quota_telemetry_purge_handler.function_name`. Novo `scripts/backfill-gsi8-quota-telemetry-purge.ts`, cobrindo as duas entidades e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `resetAt` (não `version`, mesma adaptação do worker). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 5 workers já migrados. **G-V3**: 22 testes do worker (purga das duas entidades, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `resetAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ após MAX_ATTEMPTS, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo as duas entidades, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1841/1841 (1827+14 novos: quota.ts's docstring/campo GSI8PK/GSI8SK, 22 do worker migrado, 10 do backfill novo, 1 de cobertura de observabilidade), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). **Achado real de bug pré-existente, encontrado e corrigido só neste script (não nos 6 irmãos, fora de escopo desta fatia, registrado abaixo)**: o guard `if (import.meta.url === \`file://${process.argv[1]}\`)` que decide se `main()` roda quando o script é executado diretamente NUNCA casa no Windows deste repo (`import.meta.url` usa barras normais — `file:///C:/...` — enquanto `process.argv[1]` usa contrabarras — `C:\\...`) — confirmado por teste direto (`node`/`tsx` imprimindo os dois valores). `main()` silenciosamente nunca executava sob o shell Windows/Git Bash deste ambiente (`AGENTS.md` §4) apesar de exit code 0 e nenhum erro — só descoberto ao tentar rodar o backfill desta fatia pela primeira vez nesta sessão (saída vazia inesperada). Corrigido neste arquivo trocando por `fileURLToPath(import.meta.url) === process.argv[1]` (comparação de dois paths nativos do SO, sem o mismatch de separador). **Mesmo bug confirmado presente nos outros 6 scripts** (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — não corrigido nesta sessão (fora do escopo de D-186), significa que os backfills anteriores (D-180/D-182/D-183/D-185) e qualquer execução passada desses scripts via este mesmo shell podem ter silenciosamente não escrito nada, mesmo reportados como "rodados com sucesso" — precisa de verificação/reexecução numa sessão futura, nomeado explicitamente para não ser esquecido. **PR #208 mergeado (commit `a898c1d`→`a280110`), CI real (`33687474935`/`33687503644`) e CD real (`33688100421`) `success`, todos confirmados via `gh run view` direto**. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-quota-telemetry-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyQuotaTelemetryPurge` com `LeadingKeys` exato `["WORK#QUOTA_TELEMETRY","DLQ#QUOTA_TELEMETRY"]` + `TransactWriteItemsQuotaTelemetryPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(30 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 4 sentidos)**: `WORK#QUOTA_TELEMETRY` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX` (os 4 já migrados) → `implicitDeny` nos 4 casos. **Backfill real executado contra `dev`** (após o fix do guard acima, sem o qual teria silenciosamente não escrito nada): 24 candidatos encontrados, 24 ponteiros GSI8 escritos, confirmado por `Query GSI8 WORK#QUOTA_TELEMETRY` retornando `Count=24`. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — quinta fatia mecânica seguindo o padrão de D-180/D-182 (poison-record/DLQ), adaptada para 2 tipos de entidade sem `version`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (5 de 9 workers migrados)** — `quota-telemetry-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); achado de bug real do guard `main()` em scripts de backfill no Windows, corrigido só aqui, pendente nos outros 6; os 4 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `security-audit-purge`, `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/modules/identity/application/quota.ts`, `src/modules/identity/ports/identity-store.ts`, `src/modules/identity/persistence/dynamodb-identity-store.ts`, `src/runtime/aws/handlers/quota-telemetry-purge-handler.ts`, `src/workers/quota-telemetry-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-quota-telemetry-purge.ts` (novo), testes em `test/unit/{identity,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
208:| D-187 | **MaintenanceDueIndex — sexta fatia (security-audit-purge, D-179 slice 6, 6º de 9 workers)**: `security_audit_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["security_audit_purge"]`/`worker_transact_write_policy_json["security_audit_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItems` na tabela base, ambos só no role do `security_audit_purge_handler`. **Forma distinta das 5 fatias anteriores**: este worker cobre QUATRO tipos de entidade que compartilham o mesmo formato de elegibilidade (`occurredAt+365d`, fence de tenant `ACTIVE`) — `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (`src/modules/{expiration,organization,subject,activity}/domain/*.ts`), as mesmas 4 partições que `GET /activity`'s `ActivityService` k-way-merges — todas append-only por construção (sem `update()`/`delete()` exportado), sem campo `version`. Novo `src/shared/security-audit-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que a entidade cobre 4 módulos diferentes — mesma razão que já colocou `EntityKey`/`TransactWriteEntry` em `shared/dynamodb/occ.ts`): `deriveSecurityAuditMaintenanceDue()` (due = `occurredAt+365d`, NUNCA `undefined` — sem estado terminal, mesmo raciocínio de D-186) e `securityAuditGsi8Keys()` (embute `tenantId`+`entityType`+`SK` natural no `GSI8SK`, mesmo formato de `quotaTelemetryGsi8Keys()`). **O ponteiro GSI8 é gravado UMA ÚNICA VEZ, na própria função pura `build*Event()` de cada uma das 4 entidades** (`buildAuditEvent`/`buildMembershipAuditEvent`/`buildSubjectAuditEvent`/`buildTenantAuditEvent`) — não em cada um dos 19 call sites espalhados por 6 módulos que chamam essas funções (confirmado por grep antes de escrever qualquer código): como toda escrita passa pela mesma função de construção antes do `appendXAuditToTransaction`, estampar o ponteiro ali cobre automaticamente todo call site presente e futuro, sem precisar tocar nenhum deles individualmente — simplificação real habilitada pela forma já centralizada do código, não uma decisão de design nova. **Normalização `MembershipAuditEvent`**: declara `organizationId`, não `tenantId` (mesmo valor, nome diferente) — `buildMembershipAuditEvent()` já passa `organizationId` como `tenantId` para `securityAuditGsi8Keys()`, e o adapter do worker (`normalizeTenantId()`) faz a mesma normalização ao ler uma linha crua da tabela base, mesmo padrão pré-existente do worker antes desta fatia. `security-audit-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também o `TenantLifecycleStatusSource` como dependência separada do worker); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `occurredAt` exato — nunca `version`, mesma forma que o worker já usava antes desta fatia — ambos na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186, já que este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#SECURITY_AUDIT"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência que D-186 quota-telemetry-purge, causa diferente): as 4 entidades são append-only por construção — não existe transição de estado terminal que deixaria um ponteiro obsoleto para trás, e a ÚNICA forma de uma linha deixar de ser candidata é este próprio worker deletá-la (o que remove o ponteiro junto com a linha inteira). **Interação com `GET /activity` (D-149), reverificada nesta fatia**: releitura direta de `dynamodb-audit-partition-store.ts#queryPage` confirma que `ExclusiveStartKey` continua um marcador posicional puro, independente de GSI8 e de qualquer deleção concorrente deste worker — a conclusão original de D-153 (nenhum bug de correção, nenhuma correção necessária) permanece válida após a migração, confirmado por leitura, não presumido. `GlobalIndexComponent` ganha `"security-audit-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.security_audit_purge_handler.function_name`. Novo `scripts/backfill-gsi8-security-audit-purge.ts`, cobrindo os 4 tipos de entidade e vencimento FUTURO (não só já vencido, mesma correção Rodada 3→4 do design), condicionado em `occurredAt` (não `version`). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 6 workers já migrados. **G-V3**: 4 testes de gravação do ponteiro na criação (um por tipo de entidade, incluindo a normalização `organizationId`→`tenantId`), 2 testes do módulo puro `security-audit-gsi8.ts`, 17 testes do worker (purga dos 4 tipos, boundary exato, fence de tenant não-ACTIVE com contador de retry, normalização de `MembershipAuditEvent`, sem lifecycle record, corrida de `occurredAt`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/normalizeTenantId/processPage cobrindo os 4 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa 1867/1867 (175 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #210 mergeado (`5be0743`), CI real (`33691359230`/`33691948793`) e CD real (`33692216726`) `success`, todos confirmados via `gh run view` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-security-audit-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlySecurityAuditPurge` com `LeadingKeys` exato `["WORK#SECURITY_AUDIT","DLQ#SECURITY_AUDIT"]` + `TransactWriteItemsSecurityAuditPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED` (`cron(0 6 * * ? *)`). **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, positivo + negativo nos 6 sentidos)**: `WORK#SECURITY_AUDIT` (próprio, com `--resource-arns` explícito no GSI8 — sem ele o simulate retorna `implicitDeny` mesmo para o caso positivo, achado de processo desta sessão) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY` (os 5 já migrados) → `implicitDeny` nos 5 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` corrigido roda de fato): 0 candidatos encontrados — a tabela `dev` não contém hoje nenhuma linha `AuditEvent`/`MembershipAuditEvent`/`SubjectAuditEvent`/`TenantAuditEvent` (confirmado por `Scan` bruto de `entityType`, dado sintético esparso), então nada foi escrito; confirmado por `Query GSI8 WORK#SECURITY_AUDIT` retornando `Count=0`, consistente com o dry-run. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sexta fatia mecânica seguindo o padrão de D-180/D-182/D-186 (poison-record/DLQ), adaptada para 4 tipos de entidade sem `version`, ponteiro centralizado nas 4 funções `build*Event()` em vez de múltiplos call sites) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (6 de 9 workers migrados)** — `security-audit-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 3 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `transient-purge`, `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/security-audit-gsi8.ts` (novo), `src/modules/{expiration,organization,subject,activity}/domain/*.ts`, `src/workers/security-audit-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/security-audit-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-security-audit-purge.ts` (novo), testes em `test/unit/{shared,workers}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
209:| D-188 | **MaintenanceDueIndex — sétima fatia (transient-purge, D-179 slice 7, 7º de 9 workers)**: `transient_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["transient_purge"]`/`worker_transact_write_policy_json["transient_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItems` na tabela base, ambos só no role do `transient_purge_handler`. **Forma distinta de todas as fatias anteriores**: este worker cobre DOIS tipos de entidade com dinâmicas DIFERENTES — `WebhookInbox` (`src/modules/notification/application/ses-callback-workflow.ts`, criado uma vez/imutável, ponteiro gravado uma única vez na criação, mesma forma que security-audit-purge/D-187) e `UploadSlot` (`src/modules/document/domain/upload-slot.ts`, transições reais de status, mesma forma que invitation-purge/D-182 — ponteiro precisa ser gravado/atualizado a cada transição relevante, não só na criação). Novo `src/shared/transient-purge-gsi8.ts` (não em `domain/` de nenhum módulo específico, já que as duas entidades cobrem 2 módulos diferentes — mesma razão de `shared/security-audit-gsi8.ts`): `deriveWebhookInboxMaintenanceDue()` (`createdAt+7d`, sempre), `deriveUploadSlotMaintenanceDue()` (`undefined` enquanto `RESERVED` — nunca candidato —, senão `reservedAt+7d` se `CONSUMED`, `reservedAt+24h` caso contrário incl. `EXPIRED`/`RELEASED`) e `transientPurgeGsi8Keys()`. **Achado de fronteira de módulo real durante a implementação**: a primeira versão deste arquivo reusava `computeUploadSlotPurgeAfter()`/`UploadSlotStatus` importados de `modules/document/domain/**`, violando `dependency-cruiser`'s regra `shared-must-not-reach-modules` (capturado por `test/architecture/tenant-fence-boundary.test.ts`, que falhou na primeira rodada de testes) — corrigido reimplementando a fórmula (`privacy-lgpd.md` §4, um-liner estável) diretamente em `shared/transient-purge-gsi8.ts` com um tipo `TransientUploadSlotStatus` local, em vez de inverter a fronteira de dependência. Um `UploadSlot` `RESERVED` NUNCA recebe ponteiro GSI8 (nunca aparece na Query GSI8 de forma alguma, diferente do `Scan` anterior que precisava filtrá-lo defensivamente em tempo de leitura). Três escritores atualizados para gravar/atualizar o ponteiro atomicamente na MESMA escrita condicional da transição: `ses-callback-workflow.ts` (criação do `WebhookInbox`), `advance-after-evidence.ts#appendSlotConsumption` (transição `CONSUMED`), `upload-slot-reconciliation/reconciliation.ts#processOneSlot` (transição `EXPIRED`) — nenhum writer real produz `RELEASED` hoje (confirmado por grep: status declarado no union mas sem write path), então nenhum terceiro escritor precisou de mudança. `transient-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (removendo também `TenantLifecycleStatusSource` como dependência separada do worker, embora a classe permaneça exportada por simetria com os irmãos, sem uso real); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado por `version` — AMBAS entidades têm `version` real, diferente de security-audit-purge — na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#TRANSIENT"` após `MAX_ATTEMPTS=5`. Branch de self-heal de ponteiro obsoleto mantido por defesa (mesma postura de invitation-purge/D-182), embora nenhum caminho de escrita real produza esse caso hoje para nenhuma das duas entidades. `GlobalIndexComponent` ganha `"transient-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.transient_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-transient-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath`, nunca a comparação de string que falha silenciosamente no Windows), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e excluindo corretamente `UploadSlot` `RESERVED`. `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 7 workers já migrados. **G-V3**: teste de gravação do ponteiro na criação do `WebhookInbox` (`ses-callback-workflow.test.ts`), testes de gravação/atualização do ponteiro nas transições `CONSUMED`/`EXPIRED` do `UploadSlot` (`advance-after-evidence.test.ts`/`upload-slot-reconciliation.test.ts`), 22 testes do worker (purga dos 2 tipos, `RESERVED` nunca candidato, boundary exato para ambas janelas, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant, self-heal de ponteiro obsoleto) + 20 testes do backfill (parseArgs/encodeKey-decodeKey/deriveDue cobrindo os 2 tipos e exclusão RESERVED, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida, página mista). Suíte completa 1894/1894 (176 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #212 mergeado (commit `d32957f`→`ca50bcf` em `main`), CI real (`33695255525`/`33695850896`) e CD real (`33696134854`) `success`, confirmados via `gh run view`/`gh run list` direto. **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-transient-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyTransientPurge` com `LeadingKeys` exato `["WORK#TRANSIENT","DLQ#TRANSIENT"]` + `TransactWriteItemsTransientPurge` escopada à tabela base); `get-function`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#TRANSIENT` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT` (os 6 já migrados) → `implicitDeny` nos 6 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (WebhookInbox, UploadSlot)` sobre as 58 linhas da tabela (`ScannedCount=58`, `Count=0`, sem `LastEvaluatedKey` — tabela inteira coberta), e por `Query GSI8 WORK#TRANSIENT` retornando `Count=0`; nada para escrever, execução "real" (não-dry-run) seria um no-op confirmado, não executada. **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — sétima fatia mecânica seguindo o padrão de D-187 (poison-record/DLQ, entidade create-once) para `WebhookInbox` e D-182 (ponteiro atualizado a cada transição) para `UploadSlot`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (7 de 9 workers migrados)** — `transient-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); os 2 workers restantes nomeados por D-179 permanecem em `Scan`+`Limit`: `delivery-record-purge`, `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/transient-purge-gsi8.ts` (novo), `src/modules/document/domain/upload-slot.ts`, `src/modules/document/application/advance-after-evidence.ts`, `src/modules/notification/application/ses-callback-workflow.ts`, `src/workers/upload-slot-reconciliation/reconciliation.ts`, `src/workers/transient-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/transient-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-transient-purge.ts` (novo), testes em `test/unit/{shared,workers,document,notification}/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
210:| D-189 | **MaintenanceDueIndex — oitava fatia (delivery-record-purge, D-179 slice 8, 8º de 9 workers)**: `delivery_record_purge` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`), gerando `gsi8_read_policy_json["delivery_record_purge"]`/`worker_transact_write_policy_json["delivery_record_purge"]` — `dynamodb:Query` condicionado `LeadingKeys IN ["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItems` na tabela base, ambos só no role do `delivery_record_purge_handler`. Este worker cobre DOIS tipos de entidade (`NotificationIntent`/`NotificationAttempt`, `src/modules/{reminder,notification}/domain/*.ts`), elegibilidade de idade única (`createdAt+180d`, `privacy-lgpd.md` §4), fence de tenant `ACTIVE`. **Diferença real de forma vs. D-187 (o análogo mais próximo, mesmo padrão poison-record/DLQ)**: AMBAS entidades carregam um contador OCC `version` real (ao contrário das 4 entidades append-only de D-187) — mesma forma que D-188/`UploadSlot`/`WebhookInbox`; o `Delete`/backoff/DLQ do worker e o backfill novo condicionam em `version` exato, com `createdAt` reasserted como defesa em profundidade adicional (nada muta `createdAt` hoje, mas a condição torna estrutural, não só verdadeiro por acidente). Novo `src/shared/delivery-record-gsi8.ts` (fora de `domain/` de módulo específico, já que a entidade cobre 2 módulos — mesma razão de `security-audit-gsi8.ts`/`transient-purge-gsi8.ts`): `deriveDeliveryRecordMaintenanceDue()` (due = `createdAt+180d`, NUNCA `undefined` — sem estado terminal, cada registro de entrega expira pelo prazo fixo independentemente de qualquer ação) e `deliveryRecordGsi8Keys()`. **3 pontos de escrita reais confirmados por grep** (não presumidos) gravam o ponteiro GSI8 UMA ÚNICA VEZ, no MESMO `Put` da criação: `reminder-dispatch/dispatch.ts` (intent `EXPIRATION_REMINDER`), `notification-router-workflow.ts`'s `applyStaleDecision` (intent `REPLACEMENT`/`CORRECTIVE`) e `applyRoutedDecision` (o `NotificationAttempt`) — nenhum dos dois entidades é atualizada depois de criada nestes caminhos (o `version` existe para as próprias transições de status do módulo, não para uma atualização do ponteiro GSI8). `delivery-record-purge/candidate-source.ts`+`dynamodb-candidate-source.ts` reescritos: `Query GSI8` substitui o `Scan` completo da tabela (handler perde a dependência separada de `TenantLifecycleStatusSource`, dobrada dentro do `ConditionCheck` transacional); `purge.ts` revalida cada candidato via `getCandidate()` consistente antes de agir e reivindica via `TransactWriteItems` (`ConditionCheck(TenantLifecycleRecord.status=ACTIVE)` + `Delete` condicionado em `version` E `createdAt`, na MESMA transação). Poison-record/DLQ (mesmo padrão de D-180/D-182/D-186/D-187/D-188, este worker TEM fence de tenant `ACTIVE`): backoff exponencial capado (1,2,4,8,16 dias) via `maintenanceAttemptCount`, quarentena em `GSI8PK="DLQ#DELIVERY_RECORD"` após `MAX_ATTEMPTS=5`. **Sem branch de self-heal de ponteiro obsoleto** (mesma ausência de D-187, mesma causa): nenhuma das duas entidades é atualizada depois de criada NA PRÁTICA (sem caminho de undelete, sem transição de status nomeada nos comentários do próprio worker) — o ponteiro é gravado exatamente uma vez, e a única forma de uma linha deixar de ser candidata é este worker deletá-la. **Gap conhecido e aceito, herdado de D-152, NÃO tocado por esta fatia**: `NotificationAttemptLookup` (ponteiro derivado, `PK=TENANT#t#ATTEMPT#id`) continua NÃO sendo purgado por este worker e fica órfão depois que um `NotificationAttempt` é deletado — confirmado ainda válido após esta migração por releitura direta, não presumido; fora de escopo aqui como já era em D-152. `GlobalIndexComponent` ganha `"delivery-record-purge"`; `security_audit_observability`'s `global_index_function_names` em `infra/main.tf` ganha `module.delivery_record_purge_handler.function_name`; `GLOBAL_INDEX_ACCESS_CALL_SITES` em `test/architecture/security-audit-observability-coverage.test.ts` ganha a entrada correspondente. Novo `scripts/backfill-gsi8-delivery-record-purge.ts` (guard `main()` já na forma corrigida com `fileURLToPath` desde o início), cobrindo os 2 tipos de entidade, vencimento FUTURO (não só já vencido) e condicionado em `version` (não `createdAt`, adaptação real vs. o template de D-187). `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` estendido com isolamento cross-namespace nos 2 sentidos entre os 8 workers já migrados. **G-V3**: 3 testes de gravação do ponteiro na criação (via os fixtures reais de `dispatch.test.ts`/`notification-router-workflow.test.ts` — intent do dispatch, intent REPLACEMENT do stale-decision, attempt do roteamento feliz), 24 testes do worker (purga dos 2 tipos, due no futuro nunca candidato, boundary exato, fence de tenant não-ACTIVE com contador de retry, sem lifecycle record, corrida de `createdAt`/`version`/row deletado entre revalidação e claim, idempotência, mix elegível/bloqueado, paginação multi-página, idade do candidato mais antigo, quarentena DLQ, isolamento cross-tenant) + 10 testes do backfill (parseArgs/encodeKey-decodeKey/processPage cobrindo os 2 tipos, due futuro incluído, já-pontuado nunca sobrescrito, dry-run, corrida perdida). Suíte completa local 1912/1912 (178 arquivos), `typecheck`/`lint --max-warnings=0`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` todos limpos, `terraform test` 22/22 (`AWS_PROFILE=claude-dev`, `gsi8_worker_isolation` confirmado `pass` explicitamente). PR #215 mergeado (`6d333e0`→`c595190`), CI real (`33699314795`/`33699331683`) e CD real (`33699827095`) `success`, confirmados via `gh run view`/`gh run list` direto (polling síncrono, nunca aguardando notificação de background como subagente). **Verificação ao vivo completa contra `dev`**: `describe-table` confirma GSI8 `ACTIVE`; role `exptrk-dev-delivery-record-purge-handler-role` com as 3 políticas esperadas (`TenantFacingReadWrite` + `Gsi8ReadOnlyDeliveryRecordPurge` com `LeadingKeys` exato `["WORK#DELIVERY_RECORD","DLQ#DELIVERY_RECORD"]` + `TransactWriteItemsDeliveryRecordPurge` escopada à tabela base); `get-function-configuration`/`get-schedule` confirmam Lambda `Active`/`Successful` e schedule `ENABLED`. **Prova de isolamento cross-namespace (`aws iam simulate-principal-policy`, com `--resource-arns` explícito no GSI8)**: `WORK#DELIVERY_RECORD` (próprio) → `allowed`; `WORK#MEMBERSHIP_PURGE`/`WORK#INVITATION_PURGE`/`WORK#DOCUMENT_FILE_RECONCILIATION`/`WORK#REQUIREMENT_REINDEX`/`WORK#QUOTA_TELEMETRY`/`WORK#SECURITY_AUDIT`/`WORK#TRANSIENT` (os 7 já migrados) → `implicitDeny` nos 7 casos. **Backfill real executado contra `dev`** (dry-run primeiro, confirmando que o guard `fileURLToPath` roda de fato): 0 candidatos encontrados — confirmado por `Scan` bruto filtrado por `entityType IN (NotificationIntent, NotificationAttempt)` sobre as 58 linhas da tabela (`scannedTotal=58`, `matched=0`, tabela inteira coberta), e por `Query GSI8 WORK#DELIVERY_RECORD` retornando `Count=0`; nada para escrever, execução real (não-dry-run) seria um no-op confirmado, não executada (mesma disciplina de D-187/D-188). **Item pendente carregado, não resolvido aqui**: o bug do guard `main()` (`AGENTS.md` §4) continua presente em 6 scripts irmãos (`backfill-gsi8-{document-file-reconciliation,invitation-purge,membership-purge,requirement-reindex}.ts`, `backfill-reminder-policies.ts`, `reset-dev-data.ts`) — fora do escopo desta fatia. | Nível 3-4 (implementação direta de um design já `APPROVED`, D-179 — oitava fatia mecânica seguindo o padrão de D-187/D-188 (poison-record/DLQ), adaptada para 2 tipos de entidade COM `version` real, condicionado em `version`+`createdAt`) | n/a | n/a | **IMPLEMENTADO e VERIFICADO AO VIVO (8 de 9 workers migrados)** — `delivery-record-purge` 100% migrado ponta a ponta (infra+IAM+código+backfill+testes+prova de isolamento IAM ao vivo); o único worker restante nomeado por D-179 permanece em `Scan`+`Limit`: `core-user-data-purge` | `infra/modules/dynamo-table/main.tf`, `infra/main.tf`, `infra/tests/stack.tftest.hcl`, `src/shared/delivery-record-gsi8.ts` (novo), `src/workers/reminder-dispatch/dispatch.ts`, `src/modules/notification/application/notification-router-workflow.ts`, `src/workers/delivery-record-purge/{candidate-source.ts,dynamodb-candidate-source.ts,purge.ts}`, `src/runtime/aws/handlers/delivery-record-purge-handler.ts`, `src/shared/observability/security-audit.ts`, `scripts/backfill-gsi8-delivery-record-purge.ts` (novo), testes em `test/unit/workers/` e `test/architecture/security-audit-observability-coverage.test.ts` | 2026-09-02 |
217:| D-193 | **Reconciliação OCR/AI-Extração (M7) com o Document Lifecycle (D-143+) — fase 2 do divide-and-conquer de P0.4, design apenas — implementação é fase 3.** Protocolo Claude↔Codex completo, 5 rodadas reais (`codex exec`, foreground), fechado na Rodada 5 (Claude 9,2/Codex 9,4) — ambos ≥9,0 sem arredondar, dentro do limite de 5 rodadas. E-014: `SIM PARCIAL` — mecanismo human-in-the-loop (SUGGESTED/CONFIRMED, confiança, agreement) já é padrão estabelecido (AWS Textract A2I Human Review Workflows, Hyperscience, DocuSign Insight, fontes com data) e não é redesenhado; identidade de agregados/chave S3/transação OCC/convergência assíncrona são internos, sem padrão externo aplicável. **Achado central (verificado por leitura direta, não presumido)**: o módulo `document` antigo é hoje o ÚNICO pipeline fisicamente completo em `dev` — `upload-finalizer-handler.ts`/`malware-result-handler.ts` só reconhecem `parseQuarantineKey`/`parseSubmissionQuarantineKey`, descartando definitivamente (sem retry/DLQ) qualquer chave `document-archive/...`; `applyFileScanResult`/`confirmFileScanClean` (document-archive) não têm NENHUM call site real — um arquivo enviado via `document-archive` fica preso em `PENDING_UPLOAD` para sempre. `document-archive` é a autoridade de domínio aprovada (D-143) e recebe tráfego HTTP roteável, mas não tem consumidor físico real para nenhum dos dois sinais de scan. Correção ao enquadramento da Fase 1: `extraction/domain/document-classifier.ts` é um classificador de FORMATO de arquivo (PDF/JPEG/PNG/TIFF), não um classificador de tipo de documento de negócio ligado a `DocumentType` (D-176) — não existe hoje nenhum classificador de tipo de documento por IA. **Design final**: sequenciamento (não escopo indivisível) — contrato desenhado agora, ingestão física (terceiro branch em `upload-finalizer-handler.ts`/`malware-result-handler.ts`, sem fila nova) e re-key da extração implementados independentemente, ativados por 2 feature flags em ORDEM obrigatória (starter antes do promoter); `ExtractionRun` re-chaveado por `{tenantId,documentId,versionId}` (nunca mais `itemId`); chave clean `document-archive/clean/<t>/<d>/<versionId>/<fileId>`; só arquivo `PRINCIPAL` dispara OCR (decisão de produto provisória); starter sempre relê `DocumentFile`/`DocumentVersion` frescos antes de agir, nunca confia só na chave S3; transação de confirmação com cardinalidade FIXA (`confirmField` 3 agregados/4 ações incl. outbox, `rejectField` 2/2, nunca depende de N Requirements); planner único `planDocumentVersionValidityEffect` unifica confirmação manual e auto-confirmação (achado real: a Rodada 1 contradizia `run-extraction-validation.ts`'s auto-confirm já existente); `Requirement` NUNCA entra na transação de confirmação — converge de forma assíncrona via outbox (`SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`, mesmo padrão de `SQS_IMPORT_PARSE_V1`/D-192) + worker que sempre relê estado corrente (nunca aplica o payload do evento, fechando risco de ordem/perda) + novo índice reverso esparso `GSI_EVIDENCE`; rede de reparo autoritativa via `scanRequirementsWithEvidence` (novo método em `DocumentArchiveStore`, mesmo precedente de `scanActiveSeries` para o problema de "sem enumeração de tenant"), filtrado por `attribute_exists(evidenceVersionId)` — independente de status cacheado, cobre `SATISFIED`/`PENDING`/`NOT_SATISFIED` e qualquer transição futura. Este design explicitamente anda sobre o bounded-staleness que `requirement.ts` já aceita para `DocumentVersion` mudando após o link (Decision 5, D-143), só estreitando a janela. `ExtractedField` ganha `confirmedBy`/`confirmedAt`. | Nível 5-6 (`change-risk-scale.md` — re-chaveia um trigger de evento vivo, redesenha uma transação OCC cross-agregado, define um novo contrato de fluxo de dados IA→`Requirement`) | 9,2/10 | 9,4/10 | **APPROVED (design apenas)** — implementação é a Fase 3 seguinte, ainda não iniciada | `docs/architecture/reviews/ocr-extraction-document-lifecycle-reconciliation-scoping/` | 2026-09-03 |
218:| D-193 | Fecha o GAP de slice 2 + wireia `startExecution()` real — slice 3/9 (ainda item 3 do design). `run-extraction-validation.ts`'s `commitOrDiscard()` ganha um discriminador `ExtractionExecutionInput.documentSource: "DOCUMENT" \| "DOCUMENT_ARCHIVE"` (default `"DOCUMENT"` para qualquer execução iniciada antes desta fatia): para `DOCUMENT_ARCHIVE`, o guard TOCTOU passa a ler o `Document` do `document-archive` (por `documentId` sozinho, nunca `itemId`) em vez do `Document` do módulo antigo — reaproveitando o MESMO `DocumentReader.get()` genérico (single-table, sem porta nova) e o MESMO `ExtractedFieldStore.commitRunOutcome()` (campos `documentKey`/`documentExpectedVersion` já genéricos, zero mudança na store). Decisão real, documentada inline: ao contrário do `status==="DELETED"` do módulo antigo, `document-archive`'s `status==="ARCHIVED"` NÃO descarta o commit — é soft-hide por design (`document.ts`'s próprio invariante "archiving is safe by construction"), só a ausência da linha descarta. **Gap do `fileName` fechado sem tocar o classificador**: `DocumentFile` de fato não tem `fileName` (achado real confirmado, não contornado) — `start-extraction-run-for-document-archive.ts` passa `""` (nunca um nome fabricado); a cadeia de fallback já existente do classificador (magic bytes -> extensão -> `contentType`) degrada corretamente para `classifyByContentType(DocumentFile.mediaType)`, que já cobre os 4 formatos que o Textract suporta. `ExtractedField.documentVersion` (número) recebe `DocumentVersion.seq` no caminho novo (mesmo campo genérico, sem migração de schema). O write auto-confirm de `ExpirationItem.dueDate` (W2-01-DECISION) fica OLD-path-only nesta fatia — o equivalente `DocumentVersion.validUntil` é item 4/9, explicitamente fora de escopo aqui. `itemId` no caminho `DOCUMENT_ARCHIVE` vira passthrough opaco (`= documentId`) documentado explicitamente — nenhum consumidor downstream (`start-ocr.ts`, `run-deterministic-parser.ts`, `run-bedrock-extraction.ts`, `complete-ocr.ts`, `TextractJob.itemId`) o usa para lookup, só log/round-trip. `startExecution()` agora É chamado de verdade pelo Starter (sempre, independente de `created`, mesma disciplina do trigger antigo), com `correlationId` agora threading corretamente pelo handler. Composição (`buildExtractionStarterWorkerDepsForDocumentArchive`) ganha `stateMachineArn`. G-V3: teste provando que as 5 precondições satisfeitas agora disparam um `startExecution()` real (não mais silenciosamente `GATE_OPENED`-only) com o `ExtractionExecutionInput` completo asserted campo-a-campo; teste de regressão provando o caminho `DOCUMENT` antigo (`documentSource` undefined) segue byte-idêntico; testes cobrindo `ARCHIVED` não-descarta, Document ausente descarta, TOCTOU do `commitRunOutcome`, e classificação real a partir de `fileName=""`+`contentType`. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 3/9) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint limpos, suíte 2074/2074 — 21 testes novos —, check-docs/validate-schemas/build:lambdas limpos; sem mudança de infra/Terraform). D-193 item 3/9 do design agora FECHADO por completo (Starter + validação + execução real, ponta a ponta). Próxima fatia: item 4/9 — `planDocumentVersionValidityEffect` (novo, `extraction/domain/`) + integração em `doConfirmField`/`commitRunOutcome`; campos `confirmedBy`/`confirmedAt` em `ExtractedField`; transação de `confirmField` re-chaveada para 3 agregados/4 ações (incl. outbox só quando há mudança real de `validUntil`) e `rejectField` para 2 agregados/2 ações — `Requirement` explicitamente excluído de ambas as transações por design. | `src/modules/extraction/application/{run-extraction-validation.ts,start-extraction-run-for-document-archive.ts,start-extraction-run.ts}`, `src/modules/extraction/ports/extraction-execution-starter.ts`, `src/runtime/aws/{composition/extraction.ts,handlers/extraction-starter-handler.ts}`, testes em `test/unit/extraction/{run-extraction-validation,start-extraction-run-for-document-archive,start-extraction-run,document-classifier}.test.ts` | 2026-09-03 |
219:| D-193 | Item 4/9 do design (`planDocumentVersionValidityEffect` + `confirmedBy`/`confirmedAt` + transações re-chaveadas). Novo planner puro `extraction/domain/document-version-validity-effect.ts` — único ponto que decide o efeito em `DocumentVersion.validUntil` a partir de `{fieldName, confirmedValue, documentVersion.{state,validUntil}}`: só `expirationDate` produz efeito; só estados elegíveis (`RECEIVED`/`UNDER_REVIEW`/`ACCEPTED`, mesmo conjunto do Starter) aceitam mudança; `validUntil` byte-idêntico ao já gravado planeja `NO_CHANGE` (é isso que torna o outbox condicional de verdade, não sempre-disparado). Chamado IDENTICAMENTE pelos dois caminhos: novo `confirm-reject-field-document-archive.ts` (manual, `confirmFieldForDocumentArchive`/`rejectFieldForDocumentArchive` — mecanismo novo, sem rota HTTP ainda, deliberadamente não wireado nesta fatia) e `run-extraction-validation.ts`'s `persistExtractedFieldsStage` (auto-confirm, `buildAutoConfirmDocumentVersionUpdate`, fechando o gap que a slice 3 deixou aberto explicitamente). `ExtractedField` ganha `confirmedBy`/`confirmedAt` (proveniência, critério 6 do checklist do design) — setado em AMBOS os caminhos, inclusive o `document`-module ANTIGO (`confirm-reject-field.ts`'s `doConfirmField`, mudança aditiva, cardinalidade da transação antiga inalterada). Transação nova `confirmFieldForDocumentArchive`: 3 agregados/4 ações (`DocumentVersion` Update sempre presente — mesmo em `NO_CHANGE` só bump de version/updatedAt —, `ExtractionRun` ConditionCheck, `ExtractedField` Update, `Outbox` Put só quando `effect.kind==="SET"`). Transação nova `rejectFieldForDocumentArchive`: 2 agregados/2 ações (`ExtractedField` Update, `ExtractionRun` ConditionCheck) — `DocumentVersion` NUNCA referenciada, nem por `ConditionCheck`. `Requirement` confirmado por leitura do texto do design como fora de QUALQUER uma das duas transações (convergência assíncrona é item 5/9, não tocado). Novo `OutboxDestination = "SQS_REQUIREMENT_EVIDENCE_REFRESH_V1"` adicionado ao union (valor existe desde já, sem fila/rota/worker reais — isso é item 5/9/6/9). **Escopo deliberadamente reduzido, nomeado explicitamente**: a unificação do caminho `document` ANTIGO com `document-archive` numa única transação NÃO foi feita — o módulo antigo não tem conceito de `DocumentVersion`/`Requirement`, então a transação antiga (`confirmField`/`rejectField` em `confirm-reject-field.ts`, 4-way incluindo `ExpirationItem`/`Document`) permanece byte-idêntica em cardinalidade, só ganhando `confirmedBy`/`confirmedAt` como campo adicional no mesmo Update já existente. O planner único é o real ponto de unificação (critério 5 do checklist — mesmo efeito por construção nos dois caminhos), não uma transação física compartilhada entre os dois módulos. G-V3: 8 testes do planner (SET/overwrite/NO_CHANGE por-valor-idêntico/NO_CHANGE por-estado-inelegível×4/campo-não-mapeado); 13 testes do novo `confirm-reject-field-document-archive.ts` (overwrite real, outbox condicional provado via o `effect` computado passado à store — não só o efeito colateral —, reject nunca toca `DocumentVersion`, `Requirement` semeado e provado BYTE-IDÊNTICO após confirm E após reject via `toEqual` no fixture original, idempotência, 409s nos 3 agregados, 404, cross-tenant); testes novos em `run-extraction-validation.test.ts` provando o auto-confirm agora fecha o gap da slice 3 (SET real, NO_CHANGE por valor idêntico, NO_CHANGE por estado inelegível, proveniência `SYSTEM_AUTO_CONFIRM`). | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 4/9, escolha de engenharia real registrada: unificação de transação física adiada, planner é o ponto de unificação) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint limpos, suíte 2097/2097 — 23 testes novos —, check-docs/validate-schemas/build:lambdas limpos; sem mudança de infra/Terraform, `terraform test` não rodado). D-193 item 4/9 do design FECHADO (planner + proveniência + as duas transações re-chaveadas), com a ressalva de escopo acima registrada. Próxima fatia: item 5/9 — convergência assíncrona de `Requirement`: novo índice reverso esparso `GSI_EVIDENCE` + `linkEvidence`/`unlinkEvidence` passam a escrevê-lo (item 5), fila+DLQ+rota no `DispatchOutboxRelay`+worker `requirement-evidence-refresh-handler.ts` que NUNCA aplica o payload do evento, sempre relê `DocumentVersion`+`Requirement` frescos (item 6). | `src/modules/extraction/{domain/{extracted-field.ts,document-version-validity-effect.ts},application/{confirm-reject-field.ts,confirm-reject-field-document-archive.ts,run-extraction-validation.ts},ports/extracted-field-store.ts,persistence/dynamodb-extracted-field-store.ts}`, `src/shared/outbox/outbox.ts`, testes em `test/unit/extraction/{confirm-reject-field,confirm-reject-field-document-archive,document-version-validity-effect,run-extraction-validation}.test.ts` | 2026-09-03 |
220:| D-193 | Item 5/9 do design (`GSI_EVIDENCE` — novo índice reverso esparso — + `linkEvidence`/`unlinkEvidence` passam a escrevê-lo). Novo GSI físico GSI9 (GSI1-8 já ocupados por decisões anteriores, incluindo D-179/D-190's programa GSI8 de 9 workers) — `requirementGsi9Keys()`/`requirementGsi9PartitionKey()` em `document-archive/domain/requirement.ts` constroem `GSI9PK=TENANT#<t>#DOCVERSION#<versionId>` / `GSI9SK=REQUIREMENT#<requirementId>` (SK inclui `requirementId` de propósito — dois `Requirement`s DIFERENTES, de Subjects diferentes, podem legitimamente referenciar a mesma evidência). `linkEvidence()` (`document-archive-service.ts`) passa a incluir essas duas chaves no mesmo `set` já existente (SET simples — sem ação nova na transação, cardinalidade inalterada); `unlinkEvidence()` passa a incluir `GSI9PK`/`GSI9SK` na lista `remove` (REMOVE de verdade, não `undefined`/null — esparso por construção, provado por `hasOwnProperty` no teste, não só "query vazia"). Nova query `DocumentArchiveService.findRequirementsByEvidenceVersion(tenantId, versionId): Promise<Requirement[]>` pagina o GSI9 inteiro (loop `exclusiveStartKey`, nunca só a 1ª página — prova real via 2 Requirements de Subjects diferentes referenciando a mesma evidência) via `queryIndexPage({indexName:"GSI9",...})` (porta `IndexPageInput`/`DynamoDbDocumentArchiveStore` ganham `"GSI9"`no union/mapa de atributo, mesmo padrão genérico de GSI1/2/5 já existente — nenhuma duplicação de lógica de Query). Deliberadamente SEM `RequestContext`/`authorize()` — leitura de sistema para o worker assíncrono da próxima fatia, mesma postura de `scanActiveSeries` (recurrence materializer). Terraform: `GSI9` (projeção `ALL`, não `KEYS_ONLY` como GSI8 — o worker da próxima fatia precisa do `Requirement` inteiro numa Query só) adicionado a `infra/modules/dynamo-table/main.tf`, tenant-scoped (PK carrega `TENANT#<t>#...`) logo entra em `tenant_facing_index_names` como GSI1/2/5/7 — sem isolamento de IAM dedicado como GSI3/6/8 (sem risco de fan-out cross-tenant, a PK já fixa o tenant). **Achado real durante a fatia, corrigido no caminho**: o teste `terraform test` `dynamo_table.tftest.hcl`'s `length(aws_dynamodb_table.this.global_secondary_index) == 7` estava desatualizado desde a criação do GSI8 (D-179) — ninguém atualizou a contagem para 8, o teste continuou passando silenciosamente porque a asserção nunca tinha sido re-executada como gate (`terraform test` não constava nos gates das fatias 1-4 por não terem mexido em infra); corrigido para 9 nesta fatia, `run` renomeado, mais um `run` novo asserindo a forma exata de GSI9 (`ALL`/`GSI9PK`/`GSI9SK`). G-V3: 3 testes de key builder (`requirement.test.ts` — par PK/SK correto, `requirementGsi9PartitionKey` bate exatamente o `GSI9PK` de `requirementGsi9Keys`, dois `requirementId` diferentes com o mesmo `evidenceVersionId` compartilham PK mas têm SK distinto); 5 testes de integração em `requirement-service.test.ts` (link popula GSI9 e é queryable; unlink remove de fato via `hasOwnProperty`, não só null; Requirement recém-criado sem evidência nunca tem GSI9PK/SK — sparse provado, não presumido; query encontra TODOS os Requirements de dois Subjects diferentes referenciando a mesma evidência, não só o primeiro; relink move o ponteiro — a evidência antiga para de encontrar o Requirement, a nova encontra). | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 5/9 — único ponto real de engenharia fora do design: qual GSI físico usar, decidido como GSI9 por eliminação, já que GSI1-8 estão todos ocupados) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-docs/validate-schemas/build:lambdas limpos, suíte **2105/2105** — 12 testes novos —, `terraform test` **7/7** `AWS_PROFILE=claude-dev`, mocked provider). Commit `9c3562f`. D-193 item 5/9 do design FECHADO (índice + primitivas de leitura/escrita, testado isoladamente — nenhum consumidor/worker chamando `findRequirementsByEvidenceVersion` ainda, isso é a próxima fatia). Próxima fatia: item 6/9 — fila `requirement-evidence-refresh-queue`+DLQ, rota no `DispatchOutboxRelay` para `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` (já existe no union desde a slice 4, sem consumidor), handler `requirement-evidence-refresh-handler.ts` que NUNCA aplica o payload do evento — sempre relê `DocumentVersion`+`Requirement` frescos (via `findRequirementsByEvidenceVersion` desta fatia) e re-deriva um Requirement por vez com seu próprio loop OCC, evento é só "wake up" (princípio de correção já estabelecido nas fatias 2-3: nunca confiar no payload/chave do evento, sempre reler o agregado fresco). | `src/modules/document-archive/{domain/requirement.ts,application/document-archive-service.ts,ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `infra/modules/dynamo-table/{main.tf,tests/dynamo_table.tftest.hcl}`, testes em `test/unit/document-archive/{requirement,requirement-service}.test.ts` | 2026-09-03 |
221:| D-193 | Item 6/9 do design — convergência assíncrona real de `Requirement`: consumidor de `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` ponta a ponta. Novo `src/workers/requirement-evidence-refresh/refresh.ts` (`refreshRequirementsForEvidenceVersion`) — usa `findRequirementsByEvidenceVersion` (slice 5) só para DESCOBERTA de candidatos; para CADA `Requirement` candidato, relê fresco (`store.get`), depois relê fresca a `DocumentVersion` via o `evidenceDocumentId`/`evidenceSeq` ATUAIS do próprio `Requirement` (nunca o `versionId` do hint do evento — se um relink já moveu o ponteiro, segue o vínculo corrente), recomputa `status`/`evidenceState`/`evidenceValidUntil`/GSI1/GSI8 via as mesmas funções puras de `linkEvidence`, e só then faz `TransactWriteItems` OCC (nunca `UpdateItem` solto). Sem mudança real = no-op sem `transactWrite` (sem bump de version espúrio). Loop de retry OCC próprio por Requirement (até 5 tentativas, relendo a cada volta); falha terminal de UM Requirement é registrada (`failedRequirementIds`) sem abortar os outros do mesmo lote — mesma disciplina de isolamento por-item que `dispatch-outbox-relay-processor.ts` já tem por-record, uma camada mais funda. **Achado real que fechou um gap deixado pela slice 4**: nenhum dos dois payloads de evento (`confirmFieldForDocumentArchive`/`commitRunOutcome`, ambos em `dynamodb-extracted-field-store.ts`) carregava `versionId` — só `documentId`+`validUntil` (o próprio valor que o design manda nunca confiar) — sem `versionId` a descoberta via GSI9 seria impossível; `versionId` adicionado como campo novo em `CommitDocumentVersionUpdate`/`ConfirmFieldForDocumentArchiveInput` e threadado dos dois call sites (`run-extraction-validation.ts`/`confirm-reject-field-document-archive.ts`, que já liam a `DocumentVersion` inteira, `version.versionId` já disponível) — usado SÓ como chave de busca no GSI9, nunca como fonte da state recomputada (a re-leitura fresca é o que decide tudo). Novo `requirement-evidence-refresh-handler.ts` (SQS, mesmo formato thin-entrypoint de `import-parse-handler.ts`) extrai só `{tenantId, versionId}` da mensagem (tipo `RequirementEvidenceRefreshHint`, sem mais nenhum campo alcançável) — batch item reportado como falho no handler quando `result.failed > 0`, deixando a política de redrive/DLQ real da fila decidir o retry. `buildOutboxRelayDeps` (composição) e `outbox-sweeper-handler.ts` ganham o 7º/6º sender opcional (`SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`), mesmo padrão incremental de todas as fatias anteriores (D-192 slice 9 etc.). Terraform: nova fila+DLQ `requirement-evidence-refresh-queue` (módulo `sqs-worker-queue`, já inclui DLQ+alarm), novo Lambda `requirement-evidence-refresh-handler` (só `tenant_facing_read_write_policy_json`+`consume_policy_json` da fila nova — GSI9 é tenant-facing, sem policy dedicada), `aws_lambda_event_source_mapping` novo; `dispatch_outbox_relay`/`outbox_sweeper` ganham a URL da fila nova em `environment_variables`+`policy_documents_json` (`send_policy_json`). G-V3 adversarial: hint com `versionId` correto mas `Requirement` com cache ANTIGO de `evidenceValidUntil` (mais restrito que a verdade fresca) converge para o valor fresco, nunca o cache; `DocumentVersion` re-lida como `REJECTED` (não só `validUntil` mudando) converge para `PENDING` via releitura, nunca o valor do evento; 3 Requirements da mesma evidência, um falha OCC uma vez e se recupera sozinho no retry (não conta como falha), outro falha terminalmente (esgota as 5 tentativas) sem bloquear os demais; no-op não dispara nenhum `transactWrite`; Requirement desvinculado entre a descoberta (stale) e a releitura fresca é no-op seguro, nunca reescreve. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 6/9 — único ponto real de engenharia fora do design: os dois payloads de evento da slice 4 não carregavam `versionId`, extensão aditiva necessária para a descoberta via GSI9 funcionar, decidida e documentada aqui) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-boundaries/check-docs/validate-schemas/build:lambdas limpos, suíte **2112/2112** — 7 testes novos —, `terraform test` **22/22** `AWS_PROFILE=claude-dev`). D-193 item 6/9 do design FECHADO — mecanismo de convergência assíncrona `Requirement` ponta a ponta (outbox condicional da slice 4 → GSI9 da slice 5 → fila+worker desta fatia). Próxima fatia: item 7/9 — rede de reparo autoritativa: `scanRequirementsWithEvidence` em `DocumentArchiveStore` (mesmo precedente de `scanActiveSeries`, filtrado por `attribute_exists(evidenceVersionId)`, independente de status cacheado) + worker diário agendado `requirement-evidence-daily-sweep` (EventBridge Scheduler, mesmo padrão de `document_file_reconciliation_handler`) que só REENFILEIRA candidatos na MESMA fila desta fatia, nunca escreve `Requirement` diretamente — fecha o teto de staleness em 24h mesmo sob perda total do evento/outbox (mecanismo distinto: esta fatia cobre ordem/duplicação via releitura, a próxima cobre PERDA total da mensagem). | `src/workers/requirement-evidence-refresh/refresh.ts`, `src/runtime/aws/handlers/requirement-evidence-refresh-handler.ts`, `src/runtime/aws/{composition/reminder.ts,handlers/{dispatch-outbox-relay-handler.ts,outbox-sweeper-handler.ts}}`, `src/modules/extraction/{ports/extracted-field-store.ts,persistence/dynamodb-extracted-field-store.ts,application/{confirm-reject-field-document-archive.ts,run-extraction-validation.ts}}`, `infra/main.tf`, `scripts/build-lambdas.ts`, testes em `test/unit/{workers/requirement-evidence-refresh,extraction/run-extraction-validation}.test.ts` | 2026-09-03 |
222:| D-193 | Item 7/9 do design — rede de reparo autoritativa (Rodada 5, `estado-final-consolidado.md`): fecha o risco de perda TOTAL da mensagem `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1` que a slice 6 deixou aberto (aquela fatia cobre ordem/duplicação via releitura, nunca perda do evento em si). Novo método `scanRequirementsWithEvidence` em `DocumentArchiveStore` (porta `document-archive-store.ts` + adapter `dynamodb-document-archive-store.ts` + fake em `test/unit/document-archive/in-memory-store.ts`) — `Scan` cross-tenant filtrado a `entityType="Requirement" AND attribute_exists(evidenceVersionId)`, MESMO precedente/tradeoff de custo de `scanActiveSeries` (D-147, sem método de enumeração de tenant no módulo). DELIBERADAMENTE independente de `status` — o design exige isso explicitamente porque o próprio objetivo é achar Requirements cujo `status` cacheado pode estar ERRADO por causa de uma mensagem perdida; filtrar por status excluiria exatamente as candidatas certas. Novo worker puro `src/workers/requirement-evidence-daily-sweep/sweep.ts` (`runRequirementEvidenceDailySweep`) pagina esse scan (`MAX_PAGES=25`, mesmo cap de `document-request-recurrence/materializer.ts`) e, para CADA `Requirement` com `evidenceVersionId` presente, chama um `enqueueRefresh` injetado com `{tenantId, versionId}` — nunca escreve `Requirement` diretamente (nenhum `store.transactWrite`/`get` de `DocumentVersion` neste worker), nunca deriva status, colapsando o caminho de escrita real a um único lugar (o `refresh.ts` da slice 6). Deliberadamente NÃO idempotency-sensitive — `refreshRequirementsForEvidenceVersion` já é um no-op seguro quando a releitura fresca não muda nada (G-V3 da slice 6), então reenfileirar um candidato já convergido custa uma mensagem extra, nunca uma escrita errada. Novo handler `requirement-evidence-daily-sweep-handler.ts` (EventBridge Scheduler, mesmo contrato "`input` top-level, nunca `event.detail`" de `requirement-reindex-handler.ts`/`document-request-recurrence-handler.ts`) manda via `SQSClient`/`SendMessageCommand` para a MESMA fila `requirement_evidence_refresh_queue` da slice 6 — sem fila/DLQ nova. Terraform: novo Lambda `requirement-evidence-daily-sweep-handler` + `aws_scheduler_schedule` (`cron(0 5 * * ? *)`, UTC, staggered depois do `requirement_reindex` às 04:00) + IAM role de schedule dedicada; **achado real durante a fatia**: o sufixo padrão `-schedule-role` (usado por todo o resto do arquivo) estourava o limite de 64 caracteres de nome de IAM role para este nome de função em particular (confirmado por falha real do `terraform test`, não hipotética) — corrigido com o sufixo mais curto `-sched-role`, só para este recurso, documentado inline. Policy do novo Lambda é SÓ `tenant_facing_read_write_policy_json` (cobre o `Scan` base-table, mesma cobertura de `document_request_recurrence_handler`) + `requirement_evidence_refresh_queue.send_policy_json` — deliberadamente SEM `worker_transact_write_policy_json`/`gsi8_read_policy_json` (este handler nunca escreve `Requirement`, ao contrário de `document_file_reconciliation_handler`/`requirement_reindex_handler`). Novo `run` em `stack.tftest.hcl` prova o cron/placeholder E que a capability list do novo Lambda nunca contém `dynamodb:TransactWriteItems` (prova negativa do single-writer constraint). G-V3: acha `Requirement` com evidência independente do `status` cacheado (SATISFIED/PENDING/NOT_SATISFIED testados); pula `Requirement` sem `evidenceVersionId` (sparseness genuína provada via `"evidenceVersionId" in unlinked === false`, nunca um valor falsy); pagina corretamente através de `lastEvaluatedKey` (fake isolado de 3 páginas provando que cada página seguinte recebe o `exclusiveStartKey` exato da página anterior); cenário de perda TOTAL de mensagem (Requirement com `evidenceValidUntil` cacheado muito no passado, nenhum evento jamais chegou) é reenfileirado com o par `{tenantId, versionId}` correto, provando que a convergência aconteceria assim que o `refresh.ts` da slice 6 processar essa mensagem desta vez. | Nível 3-4 (implementação direta de design já `APPROVED`, D-193 slice 7/9 — único ponto real de engenharia fora do design: o nome de IAM role estourando 64 chars, achado e corrigido no caminho) | — | — | **IMPLEMENTADO** (gate local completo: typecheck/lint/check-docs/validate-schemas/build:lambdas limpos, suíte **2123/2123** — 6 testes novos —, `terraform test` **23/23** `AWS_PROFILE=claude-dev`). D-193 item 7/9 do design FECHADO — teto de staleness de até 24h fechado mesmo sob perda TOTAL do evento/outbox (slice 6 cobre ordem/duplicação via releitura; esta fatia cobre perda total da mensagem). Próxima fatia: item 8/9 — dois feature flags em ORDEM obrigatória: `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED` (starter, PRIMEIRO) antes de `DOCUMENT_ARCHIVE_PROMOTION_ENABLED` (promoter, DEPOIS) — nunca a ordem inversa, fecha por construção a janela "CLEAN sem consumidor" descrita no design. Item 9/9 (rename mecânico `document-classifier.ts`→`document-format-classifier.ts`, nível 1-2) segue intocado, pode ser feito a qualquer momento sem protocolo. | `src/modules/document-archive/{ports/document-archive-store.ts,persistence/dynamodb-document-archive-store.ts}`, `src/workers/requirement-evidence-daily-sweep/sweep.ts`, `src/runtime/aws/handlers/requirement-evidence-daily-sweep-handler.ts`, `infra/{main.tf,tests/stack.tftest.hcl}`, `scripts/build-lambdas.ts`, testes em `test/unit/{document-archive/in-memory-store.ts,workers/requirement-evidence-daily-sweep.test.ts}` | 2026-09-03 |
223:| D-193 | Item 8/9 do design ("Sequenciamento") — dois feature flags AppConfig em ordem obrigatoria, fechando por construcao a janela "CLEAN sem consumidor": `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED` (STARTER, novos em `FeatureFlags`/`AppConfigFeatureFlagsReader`, mesmo `=== true` fail-closed parsing de AI_EXTRACTION/OCR/WHATSAPP) e `DOCUMENT_ARCHIVE_PROMOTION_ENABLED` (PROMOTER), ambos default `false`. STARTER gates `startExtractionRunForDocumentArchive()` (nova precondicao 0, checada ANTES de qualquer leitura DynamoDB, novo motivo de refuso `STARTER_DISABLED`); PROMOTER gates o terceiro branch (`document-archive`) de `upload-finalizer-handler.ts`/`malware-result-handler.ts` (`finalizeDocumentArchiveUpload()`/`processDocumentArchiveMalwareResult()`, novo outcome `IGNORED_PROMOTION_DISABLED`, checado ANTES de qualquer acesso a store/S3 — enquanto OFF o comportamento e byte-identico ao de antes da slice 1: chave reconhecida mas descartada, sem retry). Novo `src/modules/extraction/application/document-archive-activation.ts` — `isDocumentArchivePromotionEnabled()` exige AMBOS os flags (nunca so PROMOTER), o mecanismo real de ordem-por-construcao: ligar PROMOTER sem STARTER (a ordem proibida) nunca promove um arquivo a CLEAN, entao a janela perigosa nunca existe em nenhum caminho de codigo, nao so por disciplina de runbook. **Achado de fronteira real durante a fatia**: a primeira versao do arquivo vivia em `extraction/domain/` mas importava o tipo `FeatureFlags` de `extraction/ports/` — violava `domain-must-not-reach-application-layers` (pego por `test/architecture/tenant-fence-boundary.test.ts` na primeira rodada de testes), movido para `extraction/application/` (mesma classe de correcao de fronteira que a slice do D-179 para `transient-purge` ja tinha achado para `shared-must-not-reach-modules`). Terraform: duas novas `variable` (default `false`) em `infra/modules/feature-flags`, plumbadas no `jsonencode` do AppConfig e nos env vars/IAM (`feature_flags_read_policy_json`) dos 3 Lambdas afetados (`upload_finalizer_handler`, `malware_result_handler`, `extraction_starter_handler`). G-V3: com ambos OFF (default) prova o mecanismo COMPLETAMENTE inerte para documentos `document-archive` — nenhum acesso a store/S3, nao so um refuso no passo final (`DocumentFile`/`ExtractionRun` nunca tocados); com SO PROMOTER ON (ordem proibida deliberada) prova que o gate de ordem bloqueia a ativacao mesmo assim; com ambos ON (ordem correta) prova o pipeline real slices 1->3 rodando ponta a ponta contra o MESMO `InMemoryDocumentArchiveStore` compartilhado entre as duas funcoes (`test/unit/extraction/document-archive-pipeline-activation.test.ts`, novo — nao e so 2 testes isolados do gate, e uma prova de integracao real do mecanismo das slices 1-7 agora gateado/ativado). Suite **2151/2151** (195 arquivos), `typecheck`/`lint`/`check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` **23/23** (`AWS_PROFILE=claude-dev`). | Nivel 3-4 (implementacao direta de design ja `APPROVED`, D-193 slice 8/9 — unico ponto real de engenharia fora do design: o achado de fronteira domain-vs-application do novo arquivo, achado e corrigido no caminho) | — | — | **IMPLEMENTADO** (gate local completo, ver detalhe acima). D-193 item 8/9 do design FECHADO — restante apenas item 9/9 (rename mecanico `document-classifier.ts`->`document-format-classifier.ts`, nivel 1-2, sem protocolo), depois do qual D-193 fica 9/9 COMPLETO e pronto para o PR-final-para-`main`+verificacao-ao-vivo (espelhando o fechamento de D-192). Commit `51fd70b` em `develop` (push direto, sem PR — `AGENTS.md` §3). | `src/modules/extraction/{ports/feature-flags-reader.ts,persistence/appconfig-feature-flags-reader.ts,application/{document-archive-activation.ts,start-extraction-run-for-document-archive.ts}}`, `src/workers/{upload-finalizer/document-archive-finalizer.ts,malware-result/document-archive-result-processor.ts}`, `src/runtime/aws/{composition/{document-archive.ts,extraction.ts},handlers/{upload-finalizer-handler.ts,malware-result-handler.ts,extraction-starter-handler.ts}}`, `infra/{main.tf,modules/feature-flags/{main.tf,variables.tf,outputs.tf}}`, testes em `test/unit/{extraction/{document-archive-activation.test.ts,document-archive-pipeline-activation.test.ts,start-extraction-run-for-document-archive.test.ts,appconfig-feature-flags-reader.test.ts},document/{document-archive-finalizer.test.ts,document-archive-result-processor.test.ts}}` | 2026-09-03 |
224:| D-193 | **FECHAMENTO POR COMPLETO — item 9/9 (rename mecânico) + PR final para `main` + verificação ao vivo, espelhando o fechamento de D-192.** Rename mecânico nível 1-2, sem protocolo: `extraction/domain/document-classifier.ts`→`document-format-classifier.ts` (+ teste), 2 call sites (`start-ocr.ts`, comentário em `start-extraction-run-for-document-archive.ts`) atualizados — G-V3: suíte relocada passa sem modificação de asserção. Gate local completo antes do PR: `typecheck`/`lint` limpos, suíte **2136/2136** (195 arquivos), `check-docs`/`validate-schemas`/`build:lambdas` limpos, `terraform test` **23/23** (`AWS_PROFILE=claude-dev`). Commit `849e8a6` em `develop`. PR #223 (`develop`→`main`, 13 commits acumulados desde `27c03fc`, todas as 9 fatias de D-193) aberto, CI verde (guardrails/dynamodb-integration/frontend/Validate Infra em ambos os workflows concorrentes), mergeado (`20b7bd8`). CD (`33787691342`) rodou até `completed`/`success` — polling síncrono direto do `databaseId`, nunca `gh run watch` em background (disciplina reforçada depois do quase-erro de D-192). **Verificação ao vivo contra `dev` — os 6 pontos, todos confirmados, nada quebrado**: (1) `aws lambda get-function` — `upload-finalizer-handler`/`malware-result-handler`/`extraction-starter-handler`/`requirement-evidence-refresh-handler`/`requirement-evidence-daily-sweep-handler` todos `Active`/`Successful`, `LastModified` 18:09-18:12 UTC (bate com este CD); (2) `aws sqs list-queues`/`get-queue-attributes` — `exptrk-dev-requirement-evidence-refresh`+`-dlq` existem, `RedrivePolicy` aponta a DLQ com `maxReceiveCount=5`; (3) `aws scheduler get-schedule --name exptrk-dev-requirement-evidence-daily-sweep` — `ENABLED`, `cron(0 5 * * ? *)` (depois do reindex 04:00, como desenhado); (4) AppConfig (`aws appconfig list-applications`+`aws appconfigdata start-configuration-session`/`get-latest-configuration`, sem precedente de comando exato em sessões anteriores — sessão construiu o par start-session/get-latest a partir do `infra/modules/feature-flags/main.tf`, já que é a unica forma de ler HostedConfiguration por API) — os dois flags novos presentes no JSON servido, `EXTRACTION_DOCUMENT_ARCHIVE_TRIGGER_ENABLED=false`/`DOCUMENT_ARCHIVE_PROMOTION_ENABLED=false` (default OFF confirmado ao vivo, não presumido do Terraform); (5) `aws dynamodb describe-table --table-name exptrk-dev-table` — GSI9 `IndexStatus=ACTIVE`, chaves `GSI9PK`/`GSI9SK`, projeção `ALL`. **Decisão sobre ativar os flags agora**: mantidos OFF. Razão: nenhuma fatia deste fechamento incluiu um teste end-to-end contra `dev` real (upload real → scan real → promoção real → Textract real) — toda a prova de G-V3 é `InMemoryDocumentArchiveStore`/mocks; ligar os dois flags em produção-`dev` agora seria o PRIMEIRO tráfego real passando pelo mecanismo inteiro sem uma sessão dedicada observando o resultado (custo Textract real por chamada, também). `AGENTS.md`'s "sem usuários reais" remove a urgência de qualquer lado (não há pressão para ligar, nem risco de dano a usuário em deixar desligado), então a escolha aqui foi conservadora por preferência, não por necessidade — reversível a qualquer momento (`AWS_PROFILE=claude-dev` + `terraform apply` num `.tfvars` com os 2 booleans `true`, ou direto via `aws appconfig start-deployment` numa nova hosted configuration version), registrado como pendente pequeno e nomeado, não bloqueante. **Resumo dos achados/bugs reais fechados ao longo das 9 fatias**: upload `document-archive` ficava preso em `PENDING_UPLOAD` para sempre (slice 1, nenhum consumidor físico existia); nenhum payload de evento de confirmação carregava `versionId`, impedindo a descoberta via GSI9 (slice 6, campo aditivo novo); nome-base do IAM role do scheduler estourava o limite de 64 chars (slice 7, sufixo `-sched-role` em vez de `-schedule-role`); violação de fronteira `domain-must-not-reach-application-layers` no primeiro rascunho do arquivo de ativação de flags (slice 8, movido de `domain/` para `application/`); e o próprio enquadramento da Fase 1 do design corrigido antes de qualquer código (o "classificador" era de formato de arquivo, não de tipo de documento de negócio — motivo direto deste rename final). | Nível 1-2 (rename mecânico) + fechamento de PR/merge/verificação (sem decisão de engenharia nova) | — | — | **D-193 FECHADO POR COMPLETO** (9/9 itens do design implementados, PR #223 mergeado em `main`, CD `success`, verificação ao vivo confirmada nos 6 pontos exigidos, `develop`/`main` sincronizados). Mecanismo existe em `dev` mas fica INERTE (ambos os flags OFF) até uma decisão futura deliberada de ativação. | `src/modules/extraction/domain/document-format-classifier.ts`, `test/unit/extraction/document-format-classifier.test.ts`, `src/modules/extraction/application/{start-ocr.ts,start-extraction-run-for-document-archive.ts}` | 2026-09-03 |
228:| D-197 | **WhatsApp Operacional (Roadmap P0.3) — DESIGN, protocolo Claude↔Codex completo, 3 rodadas** (`docs/architecture/reviews/whatsapp-channel-scoping/`). **Marcelo decidiu o fornecedor nesta sessão: Meta Cloud API direta, não BSP** — desbloqueia o item; tudo que decorre dessa escolha passou pelo protocolo. **Declaração E-014: SIM** (fontes primárias Meta for Developers — Messaging Limits/Webhooks getting-started/Get opt-in, todas consultadas 2026-09-04, mais 1 fonte de prática de mercado para `biz_opaque_callback_data`/pricing 2026, com limitação de fonte registrada explicitamente onde a Meta não tinha página oficial citável). **Régua contestada na Rodada 1** (Codex achou o checklist da Rodada 1 incompleto — faltavam webhook inbox/replay como critério obrigatório, contrato SQS por canal do ADR-0008, quota por destinatário único vs. consumo agregado genérico, opt-in vinculado ao telefone) — reconciliada em v2 (Rodada 2, régua 8,7/10, ainda 2 pontos de ambiguidade) e fechada em v3 (Rodada 3, régua 9,3/10, estável). **3 achados reais do Codex, todos verificados por leitura de código, fecharam a Rodada 3**: (1) `TenantQuotaService` (fixed-window, por tenant, conta chamadas) não serve para o limite real da Meta (destinatário único, janela móvel, cross-tenant/portfólio) — mecanismo novo: item de tabela BASE (não GSI) `PK=WHATSAPP#PORTFOLIO`/`SK=SENT#<timestamp>#<telefone>` (tempo primeiro na SK, torna a Query por range válida — a v2 tinha a ordem invertida, achado real do Codex), IAM dedicada por `LeadingKeys`, fail-closed, hot-partition nomeado com gatilho de sharding; (2) `WebhookInbox` precisa de um caminho tenantless — achado incidental real: `ses-callback-workflow.ts` (precedente já em produção) na verdade retorna `UNMATCHED` ANTES de gravar o inbox quando as tags faltam, contradizendo o próprio cenário 15 de `architecture-fase3-consolidada.md` — corrigido para WhatsApp usando a cláusula "tenant/ACCOUNT" já prevista naquele cenário (`wabaId`, disponível antes de qualquer correlação); (3) correlação de webhook via `biz_opaque_callback_data` (mesmo papel das tags de SES) confirmada por pesquisa dedicada, formalizada como gate de implementação (confirmar contra sandbox real antes do primeiro envio de produção). **10 decisões finais** (D-1 a D-10, detalhe completo em `estado-final-consolidado.md`): widening de tipo sem migração; `WhatsAppProviderAdapter` espelhando `EmailProviderAdapter`; templates pré-provisionados (Meta Business Manager, nunca criados via API); categoria `Utility` sempre (produto é business-initiated); `WhatsAppOptIn` vinculado ao telefone exato; `GlobalUser.phoneE164` novo; `WebhookInbox` account-scoped; quota de destinatário único (acima); fila/schema `SQS_NOTIFICATION_WHATSAPP_V1` dedicados (ADR-0008); secrets via Secrets Manager (5 valores, incluindo `verify_token`) + kill switch de 2 flags em ordem (mesmo mecanismo de D-193 slice 8). ADR formal (nível 6 exige): `ADR-0012-whatsapp-cloud-api-channel.md`. DESIGN-ONLY — nenhum código/schema/infra alterado. | Type 1 (nível 6 — novo terceiro com acesso a PII) | 9,2/10 (Rodada 3, autoavaliação às cegas) | 9,1/10 (Rodada 3, final, régua 9,3/10 estável) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194.** Próximo: fatia 1 (widening de tipo + entidades novas, sem HTTP exposto) — ver `estado-final-consolidado.md` para o programa completo de 5 fatias. | `docs/architecture/reviews/whatsapp-channel-scoping/`, `docs/architecture/adr/ADR-0012-whatsapp-cloud-api-channel.md` | 2026-09-04 |
235:| D-204 | **Roadmap P1 item 15 ("Relatórios agendados") — DESIGN, protocolo Claude↔Codex completo, 3 rodadas** (`docs/architecture/reviews/scheduled-reports-scoping/`). Fecha o próximo item do backlog P1 depois do bloqueio do item 14 (D-202). **Declaração E-014: SIM PARCIAL** (Metabase "Dashboard subscriptions" e Google Cloud/Looker "Scheduling and sending dashboards"/"Emailed data policy", ambas fontes primárias datadas 2026-09-05 — padrão convergente: assinatura por criador/relatório/destinatário específico, não agendamento único fixo por tenant; "anexo vs. link" é escolha deliberada de produto oferecida pelos dois líderes de mercado; nenhuma fonte primária equivalente encontrada no nicho específico de compliance/expiration-tracking, limitação registrada). **6 achados reais do Codex, todos verificados por leitura de código, fecharam as 3 rodadas**: (1) TTL de 30 dias em presigned S3 é fisicamente inválido quando assinado por credenciais de role Lambda (expira com as credenciais, teto ~7 dias do SigV4) — fechado substituindo o link por uma rota de download AUTENTICADA que gera presign curto (5 min) sob demanda, nunca embutindo um link S3 direto no e-mail; (2) GSI10 novo era desnecessário — GSI8 (`local.gsi8_worker_types`, mapa fechado de 9 entradas, `infra/modules/dynamo-table/main.tf:248`) já é o índice esparso certo, `report_subscription="REPORT_SUBSCRIPTION"` é aditivo mecânico; (3) `lastRunAt+cadence` sozinho é fraco para idempotência/concorrência — fechado reusando outbox→SQS→worker já `APPROVED` (D-193/D-200/D-201) com `ReportDeliveryAttempt` espelhando a máquina de estados de `NotificationAttempt` (claim `SUBMITTING` com lease ANTES de qualquer chamada externa); (4) `ReportsService` exige `RequestContext`/`authorize()`, incompatível com um worker agendado sem ator autenticado — fechado com variante system-facing, mesmo posto de `findRequirementsByEvidenceVersion` (`document-archive-service.ts:1300-1306`); (5) destinatários externos arbitrários são bloqueio real de produto/segurança — fechado restringindo a `Membership ACTIVE`+`GlobalUser ACTIVE`, revalidados FRESCOS no envio (nunca confiados da criação); (6) conflito de RBAC entre a rota de download (ADMIN-tier) e destinatários MEMBER/VIEWER elegíveis — fechado autorizando por `ADMIN_ROLES` OU o próprio `principal.userId` elegível no `ReportDeliveryAttempt`/run específico. **10 decisões finais** (detalhe completo em `estado-final-consolidado.md`): `ReportSubscription` (cadence `WEEKLY`+`dayOfWeek`/`localTime`/`timeZone`, `recipientUserIds` teto nomeado, RBAC `ADMIN_ROLES`); GSI8 worker type novo; scheduler semanal único cross-tenant; claim transacional 2 ações (`Update`+`Outbox` Put, destino novo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`); `ReportSubscriptionRun`(1/execução)+`ReportDeliveryAttempt`(1/destinatário, isolamento de falha); worker de entrega system-facing; rota de download autenticada com presign efêmero; retry/DLQ nativo do SQS; truncamento sinalizado no e-mail; RBAC de gestão `ADMIN_ROLES`. **Fora de escopo, nomeado**: cadência MONTHLY; anexo MIME (`Content.Raw`, deliberadamente descartado a favor do link autenticado); destinatários externos arbitrários. DESIGN-ONLY — nenhum código/schema/infra alterado. | Nível 5 (novo mecanismo de entrega assíncrona + nova rota autenticada + novo GSI8 worker type) | 9,2/10 (Rodada 3, autoavaliação às cegas) | 9,1/10 (Rodada 3, final) | **DESIGN APROVADO — implementação real fica para sessão(ões) dedicada(s) futura(s), mesmo padrão de D-121/D-127/D-179/D-191/D-194/D-197.** Fatias sugeridas (não decididas): (1) entidades+GSI8 worker type+schema; (2) scheduler+claim transacional; (3) worker de entrega+rota de download; (4) wiring completo do destino de outbox. | `docs/architecture/reviews/scheduled-reports-scoping/` | 2026-09-05 |
242:| D-211 | **D-204 fatia 1 (entidades de domínio + registro GSI8 + destino de outbox) — IMPLEMENTADA.** Roadmap P1 item 15 (relatórios agendados) começa a sair do design-only. 3 entidades novas em `src/modules/reports/domain/`: `ReportSubscription` (`reportTypes` subconjunto não vazio dos 7 já expostos por `ReportsService`/D-195, `cadence` tipado como union — só `WEEKLY` no v1, mesma disciplina de escopo estreito de `FIELD_SCHEMA_V1` — `dayOfWeek`/`localTime`/`timeZone` no mesmo formato de `ReminderTrigger`/`ReminderPolicy`, `recipientUserIds` teto nomeado `MAX_REPORT_SUBSCRIPTION_RECIPIENTS=10`, `nextRunAt` avançado atomicamente pelo worker, ponteiro `GSI8PK`/`GSI8SK` no namespace `REPORT_SUBSCRIPTION`); `ReportSubscriptionRun` (1 por execução agendada, congela `reportTypes`/`recipientUserIds` no momento do claim — decisão 5: conjunto de linhas congelado, valores de campo relidos frescos na geração); `ReportDeliveryAttempt` (1 por destinatário, máquina de estados espelhando `NotificationAttempt` — `PREPARED`→`SUBMITTING`(lease)→`ACCEPTED`/`FAILED_RETRYABLE`/`FAILED_TERMINAL`/`UNKNOWN`, mais `skippedReason: "RECIPIENT_NOT_ELIGIBLE"` para quando a revalidação fresca de Membership/GlobalUser falha antes de qualquer chamada à SES). Terraform: `report_subscription = "REPORT_SUBSCRIPTION"` adicionado a `local.gsi8_worker_types` (`infra/modules/dynamo-table/main.tf`) — mecânico, reusa GSI8 (não GSI10), mesmo padrão das 9 entradas existentes de D-179; nenhum Lambda/IAM novo ainda (isso é fatia 2, quando o worker de agendamento existir). Novo valor aditivo `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1` no enum fechado `OutboxDestination` (`src/shared/outbox/outbox.ts`) — verificado que `DestinationSenders` é `Partial<Record<OutboxDestination,...>>`, não exaustivo, então nenhuma composição existente precisa de um sender novo ainda (mesmo padrão "valor existe antes do consumidor" que `SQS_REQUIREMENT_EVIDENCE_REFRESH_V1`/D-193 item 6/9 já estabeleceu). G-V3: 11 testes novos (`report-subscription.test.ts` — key builders das 3 entidades, `validateReportSubscriptionInput` cobrindo reportTypes vazio/desconhecido, recipientUserIds vazio/acima do teto/exatamente no teto, todos os 7 tipos reais juntos). Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; `terraform test` do módulo `dynamo-table` **7/7** (`AWS_PROFILE=claude-dev`); suíte completa a confirmar. **Pendente real, nomeado**: fatias 2-4 de D-204 (scheduler+claim transacional; worker de entrega+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) ainda não implementadas — nenhum Lambda novo, nenhuma rota HTTP, nenhum e-mail real ainda. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 1 de D-204)** — pendente fatias 2-4, PR/merge/CD | `src/modules/reports/domain/{report-subscription.ts,report-subscription-run.ts,report-delivery-attempt.ts}`, `src/shared/outbox/outbox.ts`, `infra/modules/dynamo-table/main.tf`, `test/unit/reports/report-subscription.test.ts` | 2026-09-05 |
243:| D-212 | **D-204 fatia 2 (scheduler + claim transacional) — IMPLEMENTADA.** Roadmap P1 item 15 continua saindo do design-only. `runScheduledReportsTick` (`src/workers/scheduled-reports/scheduler.ts`) escaneia GSI8 (`WORK#REPORT_SUBSCRIPTION`) exatamente como `requirement-reindex/reindex.ts` (mesmo cap `MAX_PAGES=25`, mesma paginação), rele fresco cada `ReportSubscription` candidata (GSI8 nunca fonte de elegibilidade, defensivo contra query eventualmente consistente), e comita uma transação de 2 ações por assinatura due — `Update` avançando `nextRunAt`/ponteiro GSI8 (`ConditionCheck` de versão) + `Outbox` `Put` (destino `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`, já aditivo desde D-211) — na MESMA `TransactWriteItems`, mesma disciplina "claim + evento durável atômico" de `reminder-producer/producer.ts` (um claim seguido de SendMessage direto não é atômico; Lambda pode morrer no meio, deixando uma assinatura reivindicada sem entrega enfileirada). Payload do evento deliberadamente mínimo (`runId`/`subscriptionId`/`tenantId`/`scheduledFor`) — decisão 6 de D-204 exige que o worker de entrega (fatia 3, ainda não construído) reresolva `recipientUserIds` FRESCO no envio, então não há valor em embutir um snapshot de `reportTypes`/destinatários aqui; `runId` é gerado AGORA (não pela fatia 3) para que o futuro `ReportSubscriptionRun`/`ReportDeliveryAttempt` compartilhem um id estável entre reentregas SQS at-least-once, mesma postura do `deduplicationKey` de `reminder-producer`. Nova primitiva `nextWeeklyOccurrenceUtc` (`report-subscription-schedule.ts`) computa a próxima ocorrência de `dayOfWeek`(convenção ISO 1=segunda..7=domingo, documentada explicitamente em `ReportSubscription.dayOfWeek` desde D-211 para evitar off-by-one contra `Date.getUTCDay()`)/`localTime`/`timeZone`, reusando `zonedTimeToUtc`/`parseLocalTime`/`addCalendarDays` de `reminder/domain/recurrence.ts` (reuso cross-módulo já estabelecido por `notification/application/quiet-hours.ts` — nenhuma duplicação de lógica DST-aware) mais uma nova primitiva exportada `instantToLocalParts` (direção inversa de `zonedTimeToUtc`, necessária para achar "hoje" no calendário local do assinante antes de buscar a próxima ocorrência). Novo port mínimo `ReportSubscriptionStore` (`get`+`transactWrite` só — criação/gestão de assinatura via rota HTTP fica para uma fatia futura, ainda não decidida) + adapter real `DynamoDbReportSubscriptionStore` + `DynamoDbScheduledReportsCandidateSource` (GSI8 Query, mesmo formato de `requirement-reindex/dynamodb-candidate-source.ts`). Lambda real `scheduled-reports-scheduler-handler` (`timeout_seconds=300`, `gsi8_read_policy_json["report_subscription"]`/`worker_transact_write_policy_json["report_subscription"]` — chaves já provisionadas automaticamente por D-211's `local.gsi8_worker_types` addition, mecânico) + role/policy de `aws_scheduler_schedule` + log group + registrado em `security_audit_observability.global_index_function_names` (exigiu adicionar `"scheduled-reports"` à união fechada `GlobalIndexComponent`, `security-audit.ts`). **Decisão de implementação documentada, não uma nova decisão de arquitetura**: cron do `aws_scheduler_schedule` é DIÁRIO (04:15 UTC), não literalmente semanal apesar do texto da decisão 3 de D-204 dizer "aws_scheduler_schedule único semanal" — lido aqui como descrevendo a cadência da FEATURE (`ReportSubscriptionCadence="WEEKLY"`, decisão 1: cada assinatura dispara ~1x/semana), não a frequência de polling do schedule em si; "único" (1 Lambda/1 schedule/1 scan cross-tenant por execução, "mesmo padrão de requirement-reindex") é a parte vinculante da frase. Um poll diário é o que de fato honra `dayOfWeek`/`localTime`/`timeZone` por assinatura com precisão real (~24h de deriva máxima, igual a todo outro worker GSI8-esparso do projeto) — um poll literalmente semanal dessincronizaria o horário escolhido por cada assinante para o instante único em que o schedule disparasse. Aberto a correção se a leitura literal (cron semanal) era de fato a intenção. `terraform plan` real contra `dev` (`AWS_PROFILE=claude-dev`) confirmou os 12 recursos novos como `+ create` limpo (nenhum recurso pré-existente afetado; os 4 `-/+ destroy and then create replacement` de `aws_lambda_permission` no mesmo plano são débito técnico pré-existente já nomeado em sessão anterior, não relacionados a esta mudança) — `terraform fmt`/`validate` limpos. G-V3: 7 testes novos de `nextWeeklyOccurrenceUtc` (rollover mesma semana/semana seguinte, semântica estritamente-depois-do-instante, convenção ISO dayOfWeek=7≠`Date.getUTCDay()`, timezone-aware cross-fuso, busca a partir da data calendário LOCAL perto de fronteira de dia) + 7 testes de `runScheduledReportsTick` (claim avança nextRunAt/GSI8/escreve evento outbox, ignora não-due, multi-tenant, concorrência perdida no claim, candidato obsoleto nunca reclamado 2x) + 2 de `shouldAlarmScheduledReports`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa a confirmar. **Pendente real, nomeado**: fatias 3-4 de D-204 (worker de entrega SQS+rota de download; wiring completo do destino de outbox — relay/sweeper/env vars/schema/testes) e a própria rota HTTP de criação/gestão de `ReportSubscription` (`ADMIN_ROLES`, decisão 1) — nenhuma delas implementada ainda; sem elas, nenhuma assinatura real pode ser criada nem entregue, feature continua não-usável ponta a ponta. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova; 1 judgment call de cadência de cron documentado explicitamente acima) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 2 de D-204)** — pendente fatias 3-4 + rota HTTP de criação, PR/merge/CD | `src/workers/scheduled-reports/{scheduler.ts,candidate-source.ts,dynamodb-candidate-source.ts}`, `src/modules/reports/domain/report-subscription-schedule.ts`, `src/modules/reports/ports/report-subscription-store.ts`, `src/modules/reports/persistence/dynamodb-report-subscription-store.ts`, `src/modules/reminder/domain/recurrence.ts`, `src/runtime/aws/handlers/scheduled-reports-scheduler-handler.ts`, `src/runtime/aws/composition/reports.ts`, `src/shared/observability/security-audit.ts`, `scripts/build-lambdas.ts`, `infra/main.tf` | 2026-09-05 |
247:| D-216 | **D-205 fatia 1 (`DossierExportRun` + preview/confirm HTTP + `scopeHash`) — IMPLEMENTADA.** Começa a tirar o item 16 do backlog P1 (dossiê documental PDF/Excel) do design-only. `DossierExportRun` (`src/modules/document-archive/domain/dossier-export-run.ts`) co-localizado na MESMA partição do Subject (`PK=TENANT#<t>#SUBJECT#<subjectId>`, `SK=DOSSIER#<runId>`) — mesma convenção que `requirementKey()` já estabelece, mesmo racional (o ciclo de vida do export pertence ao Subject). `POST /document-archive/subjects/{subjectId}/dossier` cria o run em `PREVIEW_READY`, congelando os `requirementIds` ATUAIS do Subject (decisão 4 — nunca redderivados depois) e devolvendo as linhas de preview na MESMA resposta. `POST .../dossier/{runId}/confirm` é idempotente dado um `scopeHash` batendo (fingerprint canônico JSON+SHA-256 sobre `subjectId`+`requirementIds` ordenados) — confirmar um run JÁ confirmado com o MESMO hash é no-op de sucesso, nunca um segundo dispatch; um hash divergente é SEMPRE 409, independente do status atual (decisão 3). Um confirm bem-sucedido dispara um destino de outbox novo (`SQS_DOSSIER_EXPORT_V1`, aditivo, sem consumidor ainda — mesmo padrão "valor existe antes do consumidor" de `SQS_REPORT_SUBSCRIPTION_DELIVERY_V1`/D-211). RBAC: action nova `docarchive:dossier-export`, `ADMIN_ROLES` EXCLUSIVAMENTE (decisão 10 — a Rodada 1 do Codex já tinha achado real que "ADMIN_ROLES ou assignee" não seria expressável estruturalmente, já que `Requirement` não tem `ownerUserId`). Achado de reuso fechado no caminho: o mecanismo de fingerprint canônico JSON+SHA-256 que `search-cursor.ts` (D-194 Fatia 3) já usa para o cursor de busca foi extraído para `shared/domain/fingerprint.ts` novo, reusado por `scopeHash` em vez de duplicar a canonicalização. Métodos novos (`previewDossierExport`/`confirmDossierExport`) adicionados à MESMA `DocumentArchiveService` já gigante (não um serviço novo — a lógica reusa diretamente `listRequirements`/`trackedSubjectKeyForFence` já existentes na classe, mesmo racional de `getSubjectCompliance`). G-V3: 10 testes de serviço (`dossier-export.test.ts` — congelamento de requirementIds, scopeHash determinístico, 404 de Subject inexistente, idempotência do confirm, 409 de hash divergente mesmo após confirmado, RBAC) + 5 de HTTP (`document-archive-dossier-routes.test.ts` — preview→confirm ponta a ponta via handlers reais, 400 de scopeHash ausente, 409, 404, 403 de VIEWER) + 3 de contrato do schema novo + 2 de `proxy-allowlist`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2392/2392** (217 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate` limpos; `terraform plan` real contra `dev` mostrou exatamente as 2 rotas novas esperadas (mais o mesmo baseline pré-existente de 4-replace/110-change já nomeado em sessões anteriores). PR #250 mergeado (`866a762`), CD (`34046861084`) `completed`/`success` confirmado por polling síncrono do `databaseId` capturado ANTES do merge. **Verificação ao vivo contra `dev` feita**: `exptrk-dev-document-archive-handler` `Active`/`Successful` (`LastModified` batendo com o horário deste deploy); as 2 rotas `POST /document-archive/subjects/{subjectId}/dossier`/`.../dossier/{runId}/confirm` confirmadas presentes na API `uav7id1muh`. **Pendente real, nomeado, não bloqueante**: fatia 2 (worker de geração PDF+XLSX via `pdf-lib`+nova dependência de Excel, `DossierTooLargeError`, S3) e fatia 3 (rota de download autenticada com presign sob demanda, mesmo padrão de D-204) seguem não implementadas — hoje dá pra fazer preview/confirm, mas nenhum documento é gerado nem baixável ainda. **Próxima ação real**: fatia 2 de D-205, ou escopar itens 18-19 do backlog P1, a critério de quem retomar. | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO PARCIALMENTE (fatia 1 de D-205)** — pendente fatias 2-3 | `src/modules/document-archive/{domain/dossier-export-run.ts,application/document-archive-service.ts,application/id-generator.ts,http/document-archive-handlers.ts}`, `src/shared/domain/{fingerprint.ts,search-cursor.ts}`, `src/shared/outbox/outbox.ts`, `src/modules/identity/domain/authorization.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{ids.ts,handlers/document-archive-handler.ts}`, `src/shared/contracts/schema-validator.ts`, `schemas/api/docarchive-dossier-confirm-request.v1.json`, `infra/modules/api-gateway/main.tf` | 2026-09-06 |
248:| D-217 | **D-205 fatia 2-3 (worker de geração PDF+XLSX + rota de download autenticada) — IMPLEMENTADA. Item 16 do backlog P1 (dossiê documental) FECHADO POR INTEIRO (D-205 completo, fatias 1-3).** `processDossierExportGeneration` (`src/workers/dossier-export/generate.ts`) consome `SQS_DOSSIER_EXPORT_V1` (dispatch já existia desde D-216, sem consumidor): reclama `CONFIRMED→GENERATING` via lease (`generatingLeaseExpiresAt`, mesmo padrão de `NotificationAttempt`'s SUBMITTING — reclama um run travado cuja lease expirou, pula silenciosamente um redelivery ainda dentro da lease), busca os dados via `documentArchive.getDossierExportData` (chamada ÚNICA — achado próprio corrigido antes do commit: rascunho inicial chamava duas vezes, uma só para `subjectDisplayName`), declara `TOO_LARGE` (nunca um artefato truncado silenciosamente) quando o escopo CONGELADO excede `MAX_DOSSIER_REQUIREMENTS=200`, omite silenciosamente (nunca fabrica) um `Requirement` apagado desde o confirm, gera PDF (`dossier-pdf-builder.ts`, `pdf-lib` — primeiro uso do projeto para CRIAR, não só parsear, um PDF; paginação real via `font.widthOfTextAtSize`, nunca descarta uma linha) e XLSX (`dossier-xlsx-builder.ts`, dependência nova `exceljs@4.4.0` exact-pinned; 2 abas, nunca trunca célula) EM PARALELO, sobe ambos ao S3 (bucket `report_exports` REUSADO sob prefixo próprio, nenhum bucket novo — achado de reuso explícito), marca `READY`+`generatedAt`, limpa a lease; erro no meio marca `FAILED`+`failureReason` e RELANÇA (nunca engole). Mitigação de injeção de fórmula (`sanitizeFormulaInjection`, apóstrofo líder em `=+-@`) extraída para `shared/spreadsheet/formula-injection.ts` e aplicada tanto no XLSX novo quanto retroativamente ao CSV existente (`csv-export-writer.ts`) — achado de segurança fechado no caminho, não só no código novo. Rota nova `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download?format=pdf|xlsx` — mesmo padrão de presign sob demanda de 5 min de D-204/D-215 (nunca embute link de S3 de TTL longo), 409 se o run não está `READY`, 400 para `format` fora de `pdf`/`xlsx`, RBAC `docarchive:dossier-export` `ADMIN_ROLES` EXCLUSIVAMENTE (mesma decisão 10 de D-216, sem fallback de destinatário — sensibilidade de divulgação diferente da rota de relatórios). Infra nova: fila+DLQ `dossier-export` (`sqs-worker-queue`, `consumer_timeout_seconds=120`); Lambda `dossier-export-generation-handler` (`timeout=120s`, env `QUARANTINE_BUCKET_NAME`+`REPORT_EXPORTS_BUCKET_NAME`); `document_archive_handler` ganha `REPORT_EXPORTS_BUCKET_NAME`+policy de leitura S3. **Achado mecânico fechado no caminho**: `workbook.xlsx.load(buffer)` falhava o typecheck do arquivo de teste (`Buffer<ArrayBufferLike>` do `@types/node` aninhado do `exceljs` estruturalmente diferente do `Buffer` do próprio repo) — contornado com um helper `loadWorkbook` que passa pelo cast `unknown`, escopado só ao arquivo de teste. G-V3: 4 testes de `dossier-xlsx-builder.test.ts` + 5 de `dossier-pdf-builder.test.ts` + 8 de `dossier-export-generation.test.ts` (happy path, `RUN_NOT_FOUND`, idempotência `ALREADY_RESOLVED`, lease não expirada `SKIPPED_IN_PROGRESS`, reclamação de lease expirada, `TOO_LARGE`, requirement apagado omitido, `FAILED`+rethrow) + 5 novos de download em `document-archive-dossier-routes.test.ts` (total 10 no arquivo) + extensão de `proxy-allowlist.test.ts`. Gate local completo: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas` limpos; suíte completa **2415/2415** (220 arquivos, incluindo os 2 testes de arquitetura com `tsc` real); `terraform fmt`/`validate`/`plan` limpos contra `dev`. PR #252 mergeado (`bc35727`); CD (`34050910874`) falhou uma vez por contenção real de lock do state Terraform contra `dev` (4 runs de CI concorrentes de branches Dependabot rodando `terraform plan` simultaneamente no mesmo backend S3/DynamoDB — não um defeito de código; diagnosticado via `gh run view --log-failed`, resolvido aguardando os 4 runs concorrentes concluírem e rodando `gh run rerun --failed`, sem precisar do `force-unlock` pré-autorizado por §7) — rerun `completed`/`success` confirmado. **Verificação ao vivo contra `dev`, todos os pontos confirmados**: `exptrk-dev-document-archive-handler`/`exptrk-dev-dossier-export-generation-handler` `Active`/`Successful` (`LastModified` batendo com o horário deste deploy); fila+DLQ `exptrk-dev-dossier-export`/`-dlq` existem; event source mapping `Enabled` (via `--event-source-arn`, mesmo achado de ferramenta já nomeado em D-215); rota `GET /document-archive/subjects/{subjectId}/dossier/{runId}/download` presente na API `uav7id1muh`. **Item 16 do backlog P1 agora COMPLETO por inteiro** (fatias 1-3 de D-205 fechadas) — ainda não testado ponta a ponta com um preview→confirm→geração real disparando via SQS real em `dev` (só G-V3/unit + smoke de infra), próxima sessão pode considerar esse smoke test antes de considerar a feature 100% validada, ou seguir para os itens 18-19 do backlog P1 (metadata configurável por Document Type; compartilhamento externo seguro). | Nível 3-4 (implementação de design já `APPROVED` nível 5, sem decisão nova) | — | — | **IMPLEMENTADO POR COMPLETO (D-205 FECHADO, item 16 do backlog P1 fechado por inteiro)** | `src/workers/dossier-export/generate.ts`, `src/modules/document-archive/{application/document-archive-service.ts,application/dossier-pdf-builder.ts,application/dossier-xlsx-builder.ts,http/document-archive-handlers.ts,ports/dossier-export-store.ts,persistence/s3-dossier-export-store.ts,domain/dossier-export-run.ts}`, `src/shared/{spreadsheet/formula-injection.ts,csv/csv-export-writer.ts,errors/app-error.ts}`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/runtime/aws/{composition/document-archive.ts,composition/reminder.ts,handlers/document-archive-handler.ts,handlers/dossier-export-generation-handler.ts,handlers/dispatch-outbox-relay-handler.ts,handlers/outbox-sweeper-handler.ts}`, `infra/main.tf`, `infra/modules/api-gateway/main.tf`, `scripts/build-lambdas.ts`, `package.json` | 2026-09-06 |
257:| D-226 | **Emissao de credencial de guest (D-222, item 9 do roadmap P0) — orquestracao cross-Lambda entre `document-archive-handler` (autenticado, sem pepper) e `document-archive-guest-handler` (pepper isolado por D-146) via outbox/SQS assincrono, `APPROVED` via protocolo Claude<->Codex, 7 rodadas (`codex exec`, nota cega): 5,8 -> 7,2 -> 8,1 -> 8,8 -> 8,9 -> 8,9 -> 9,2/10 (Claude 9,2/10, Codex 9,2/10, gate atingido sem arredondar).** E-014 declarado `NAO` (fronteira interna entre modulos deste projeto, nao padrao de mercado a replicar) e confirmado contra `docs/engineering/research-protocol.md` antes de proceder. **Mecanismo (fecha o Achado 1/bloqueador de D-222)**: builder unico `buildDocumentRequestCreatedOutboxEntry` (reaproveitado por `materializeAttempt` interativo, pelo worker `materializer.ts`, e pelo novo `createDocumentRequest` avulso) grava, na MESMA `TransactWriteItems` que cria o `DocumentRequest`, um `OutboxRecord` com destino novo `SQS_DOCUMENT_REQUEST_CREDENTIAL_ISSUANCE_V1`/evento `DocumentRequestCredentialIssuanceRequested`, payload MINIMO (`tenantId`/`subjectId`/`documentRequestId`/`issuanceGeneration`) — nunca dado de negocio que o consumidor deva confiar sem reler o registro autoritativo. `DocumentRequest` ganha `issuanceGeneration`/`activeCredentialSelectorHash` (identidade por geracao, nao so por id, para reemissao pos-rejeicao nao colidir com a idempotencia da emissao original). Consumidor novo na Lambda guest (tem o pepper): transacao atomica de 4 entradas (IdempotencyRecord por geracao + `RequestAccessCredential` Put + `DocumentRequest` Update com `ConditionExpression` completa de geracao/status/versao/pointer + Put do material de entrega numa TABELA DYNAMODB DEDICADA NOVA, sem GSI, nunca a tabela principal/politica geral tenant-facing) — token bruto nunca toca superficie de leitura compartilhada. Entrega acionada por DynamoDB Streams+Event Source Mapping (filtro INSERT em infra e handler), at-least-once explicito, fila de falha `GuestCredentialDeliveryFailures` reclassificada como alerta (nao carrega payload/chaves, correcao factual da Rodada 7) — perda definitiva assumida apos a janela de retencao do Stream, TTL da tabela e housekeeping de seguranca. **Achado 2 (avulso)**: `createDocumentRequest()` novo com `ConditionCheck` de Requirement + IdempotencyRecord por payloadHash. **Achado 3 (vinculo reverso)**: `rejectVersion()`, fenced por `lastSubmissionId`/`status===SUBMITTED`, reabre a Request (`issuanceGeneration+1`), revoga a credencial antiga (`ConditionExpression` de pertencimento + `if_not_exists` idempotente) e enfileira nova emissao, na mesma transacao ja existente. **Achado 4 (chasing)**: mecanismo completo (worker produtor, GSI1 `REQUESTDUE` novo, destino de dispatch proprio) atras de `DOCUMENT_REQUEST_CHASING_ENABLED=false`. **DESIGN-ONLY nesta rodada de protocolo** — implementacao da fatia 1 (mecanismo central de issuance, Achados 1-3) prossegue na mesma sessao por nao depender de decisao de produto pura (ver pendencias). | Nivel 5 (cruza fronteira de seguranca Lambda/pepper de D-146, protocolo obrigatorio) | 9,2 | 9,2 | **APPROVED (design) + implementacao da fatia 1 em andamento na mesma sessao** — pendencias de produto nomeadas: (1) cadencia/quantidade/aprovacao humana do chasing (mecanismo pronto, desligado); (2) TTL definitivo da credencial quando `deadline` ausente (hoje 7 dias, default provisorio de engenharia) | `docs/architecture/reviews/guest-credential-issuance-scoping/` (round1 a round7 + `estado-final-consolidado.md`) | 2026-09-07 |
258:| D-227 | **D-226 fatia 2 (consumidor guest da emissao de credencial) — IMPLEMENTADA. CORRECAO DE UM REGISTRO ANTERIOR DESTA MESMA LINHA: item 9 do roadmap P0 NAO esta fechado por inteiro — ver D-228 abaixo.** Consumidor novo na Lambda guest: `document-request-credential-issuance-service.ts` + handler `document-request-credential-issuance-handler.ts`, consome `SQS_DOCUMENT_REQUEST_CREDENTIAL_ISSUANCE_V1` via Event Source Mapping baseado em FILA (nao Streams — correcao factual: a fila e o mecanismo do PRODUTOR/fatia 1, nao ha Streams aqui), transacao atomica das 4 entradas desenhadas em D-226 (IdempotencyRecord por geracao, `RequestAccessCredential` Put, `DocumentRequest` Update com `ConditionExpression` de geracao/status/versao/pointer, Put na tabela dedicada nova `infra/modules/guest-credential-delivery-table/`, sem GSI, isolada da tabela principal, Streams NEW_IMAGE habilitado nela mas SEM Event Source Mapping ainda — ver achado abaixo). `createDocumentRequest()` avulso novo (Achado 2) com `ConditionCheck` de Requirement + IdempotencyRecord por payloadHash. `rejectVersion()` (Achado 3) reabre a Request via `issuanceGeneration+1`, revoga a credencial antiga com `ConditionExpression` de pertencimento + `if_not_exists` idempotente, enfileira nova emissao na mesma transacao. G-V3: idempotencia do consumidor sob mensagem duplicada, isolamento estrutural da tabela dedicada, `rejectVersion()` reabrindo corretamente, criacao avulsa idempotente. **Achado de processo real (subagentes)**: a implementacao ficou paralisada por 3 niveis sucessivos de subagentes que apenas redelegavam a mesma tarefa sem executa-la; a sessao orquestradora interveio diretamente, rodou os gates ela mesma e fechou esta fatia — licao registrada em memoria persistente. **Achado real, verificado ao vivo contra `dev` apos o merge, que corrige o fechamento indevido registrado antes nesta linha**: o comentario do proprio codigo (`guest-credential-delivery-table/main.tf` linhas 16-23, `guest-credential-delivery.ts` linhas 14-17) documenta explicitamente que o WORKER DE ENTREGA (que leria a tabela dedicada via Streams e enviaria de fato o link ao guest) **nao foi construido nesta fatia** — apenas o material bruto e armazenado. Confirmado por `aws lambda list-event-source-mappings --event-source-arn <stream-arn-da-tabela>` retornando vazio. **Resultado pratico: nenhum guest recebe um link utilizavel ainda, mesmo apos esta fatia** — o gap original de D-222 (Achado 1) so fecha quando o worker de entrega existir E houver uma forma de descobrir o destinatario (email/telefone do guest), que nao esta modelada em `DocumentRequest` hoje (achado novo, ver D-228). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/suite completa **2504/2504** (227 arquivos), `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. Mergeado em `main` (PR #259, CI/CD `success`), Lambda/tabela confirmados `Active`/`ACTIVE` ao vivo. | Nivel 3-4 (implementacao de design ja `APPROVED` D-226, sem decisao nova) | — | — | **IMPLEMENTADO PARCIALMENTE (produtor+consumidor+armazenamento; worker de entrega ao guest AINDA NAO EXISTE — item 9 do roadmap P0 permanece ABERTO, ver D-228)** | `src/modules/document-archive/{application/document-request-credential-issuance-service.ts,application/document-archive-service.ts,domain/guest-credential-delivery.ts,http/document-archive-guest-handlers.ts}`, `src/runtime/aws/handlers/document-request-credential-issuance-handler.ts`, `infra/modules/guest-credential-delivery-table/`, `test/unit/document-archive/{document-request-credential-issuance-service.test.ts,document-request-avulso-and-reopen.test.ts}` | 2026-09-07 |
259:| D-228 | **Fecha o item 9 do roadmap P0 (D-222/D-226/D-227) pela primeira vez de ponta a ponta: worker de entrega de credencial de guest + campo de destinatário.** Duas peças aditivas, nível 3-4 (implementação direta, seguindo precedente já convergido no mesmo código-base para o MESMO problema — nunca decisão nova de arquitetura, avaliado explicitamente contra `docs/engineering/change-risk-scale.md` antes de proceder sem protocolo Claude↔Codex): **(1) `DocumentRequest.recipientEmail?: string`** (`document-archive/domain/document-request.ts`) — campo OPCIONAL/aditivo, mesmo padrão `recipientEmail: string` já usado pelo módulo `subject` (entidade homônima, mas DISTINTA e não relacionada, mesma confusão de nomes já registrada em D-222) para o mesmo problema (enviar link a destinatário externo por e-mail). Populado só por `createDocumentRequest()` avulso (novo parâmetro `CreateDocumentRequestInput.recipientEmail?`, incluído no `payloadHash` de idempotência — replay com e-mail diferente é `ConflictError`, nunca sobrescrita silenciosa). **Pendência de produto nomeada, não resolvida aqui** (via mais conservadora/reversível escolhida deliberadamente): o caminho `DocumentRequestSeries`/`materializeAttempt` (recorrência) não carrega contato de destinatário no nível da série — um `DocumentRequest` materializado por recorrência continua sem `recipientEmail`, então o worker de entrega o pula (skip terminal, não é erro). Fechar isso exigiria decisão de produto (adicionar `recipientEmail` a `DocumentRequestSeries`?) fora do escopo desta tarefa. **(2) Worker de entrega real** (`src/workers/guest-credential-delivery/deliver.ts` + handler `src/runtime/aws/handlers/guest-credential-delivery-handler.ts`): consome a tabela dedicada `guest-credential-delivery` via DynamoDB Streams + Event Source Mapping NOVO (filtro `eventName=INSERT`, D-226 decisão central item 5, `maximum_retry_attempts=3`, `destination_config.on_failure` para fila nova `GuestCredentialDeliveryFailures` — carrega só metadados do lote, nunca o payload/token). Re-lê o `DocumentRequest` autoritativo (nunca confia em nada além das chaves do registro do Stream); pula (terminal, não repete) se `recipientEmail` ausente ou se `issuanceGeneration` está obsoleta (reemissão já superou esta entrega); idempotência sob redelivery do Stream via `GuestCredentialDeliveryMarkerStore.claim()` — Put condicional (`attribute_not_exists`) de uma linha marcador na MESMA tabela dedicada, sob a MESMA `PK` do registro de entrega, antes de enviar (trade-off nomeado: uma falha entre claim e envio deixa o par permanentemente reivindicado-mas-não-enviado, mesma classe de best-effort já aceita em `document-request-service.ts`). Monta o link exatamente como `document-chasing-dispatch/dispatch.ts` já faz (`${guestUploadBaseUrl}?token=${token}`, mesmo placeholder documentado `https://app.example.invalid/...`, D-047) usando o `token` bruto já armazenado em `GuestCredentialDeliveryRecord` — nunca deriva/reconstrói. Envia via `EmailProviderAdapter`/`SesEmailAdapter` já existente (mesmo mecanismo de `EmailDeliveryWorker`/`document-chasing-dispatch`, nenhum client SES novo), novo template `guest-credential-delivery-invite` em `email-templates.ts`. Roda na Lambda de RECURSO (não a guest com pepper) — só lê `DocumentRequest` (mesma `tenant_facing_read_write_policy_json` de todo outro consumidor do módulo) e envia e-mail; nunca toca o pepper D-146. **G-V3 real (não apenas unitário)**: `test/unit/document-archive/guest-credential-issuance-to-delivery-e2e.test.ts` prova o ciclo completo `createDocumentRequest` (com `recipientEmail`) → `DocumentRequestCredentialIssuanceService.handle` (D-226/D-227) → `deliverGuestCredential` (este worker) → o link do e-mail autentica de fato via `GuestDocumentAccessService.resolveCredential()` (o mesmo método que o handler HTTP guest real chama) — token bruto nunca aparece fora da tabela dedicada e do corpo do e-mail (`guest-credential-delivery-worker.test.ts` cobre idempotência sob duplicata de Streams, ausência de destinatário, geração obsolecida, request inexistente, falha de envio). **Resultado prático: pela primeira vez, um guest pode de fato receber e usar um link funcional** — mas SÓ para `DocumentRequest`s criados pelo caminho avulso COM `recipientEmail` explícito; o caminho de recorrência continua sem destinatário modelado (pendência nomeada acima), então o item 9 do roadmap P0 fecha para o caminho avulso mas não integralmente — ver `NEXT_SESSION_PROMPT.md` para o texto exato do que falta. Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2556/2556** (233 arquivos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. **Nota de numeração**: D-228 estava reservado por esta sessão desde o início do trabalho; D-229 (WhatsApp fatia 2/5, sessão paralela concorrente) já registra a colisão evitada. | Nível 3-4 (extensão aditiva seguindo precedente já convergido, sem invenção de política nova; avaliado contra `change-risk-scale.md` antes de proceder) | — | — | **IMPLEMENTADO (caminho avulso fecha ponta a ponta; caminho de recorrência permanece sem `recipientEmail`, pendência nomeada)** | `src/modules/document-archive/{domain/document-request.ts,application/document-archive-service.ts,ports/guest-credential-delivery-marker-store.ts,persistence/dynamodb-guest-credential-delivery-marker-store.ts}`, `src/workers/guest-credential-delivery/deliver.ts`, `src/runtime/aws/handlers/guest-credential-delivery-handler.ts`, `src/runtime/aws/composition/document-archive.ts`, `src/modules/notification/providers/email-templates.ts`, `infra/main.tf`, `scripts/build-lambdas.ts`, `test/unit/document-archive/{guest-credential-delivery-worker.test.ts,guest-credential-issuance-to-delivery-e2e.test.ts,document-request-avulso-and-reopen.test.ts}` | 2026-09-07 |
261:| D-230 | **Fecha por inteiro o item 9 do roadmap P0 (D-222/D-226/D-227/D-228): `DocumentRequestSeries` (caminho de recorrência) ganha `recipientEmail`, a pendência nomeada explicitamente em D-228.** `APPROVED` via protocolo Claude↔Codex completo (`AGENTS.md` §4, 3 rodadas reais `codex exec`, nota cega): 7,6 → 8,6 → 9,2/10 (Claude 9,2/10, Codex 9,2/10, ambos ≥9,0 sem arredondar). E-014 `NÃO` (fronteira interna, mesmo problema já resolvido 2x no próprio código-base — módulo `subject` e D-228 — nenhum padrão de mercado a pesquisar). **Reclassificação de risco em rodada** (achado real do Codex, aceito): a proposta inicial (Rodada 1) classificou Nível 3 (campo aditivo, precedente D-228); o Codex apontou, corretamente, que a Rodada 2 introduz uma AÇÃO nova (`updateSeriesRecipient`, mutação/remoção, rota HTTP + schema novos) e que a régua manda tratar como o nível mais alto na dúvida — reclassificado para **Nível 5**, protocolo completo cumprido antes da implementação (não decidido solo, ao contrário do que D-228/D-227 haviam feito para gaps semelhantes — mudança de processo desta sessão pedida explicitamente pelo dono do projeto). **Decisão final**: (1) `DocumentRequestSeries.recipientEmail?: string` (`document-request-series.ts`) + `CreateDocumentRequestSeriesInput.recipientEmail?`, fornecido pelo humano na criação da série, trimmed (nunca lower-cased — alinhado ao caminho avulso de D-228, que também não normaliza caixa); (2) `buildMaterializeAttemptEntries` copia `series.recipientEmail` para CADA `DocumentRequest` materializado — snapshot imutável por ciclo, cobre os dois call sites que já passam por este builder (`materializeAttempt` interativo E o worker periódico `document-request-recurrence/materializer.ts`); `buildDocumentRequestCreatedOutboxEntry` confirmado por leitura como NÃO precisando de nenhuma mudança (só emite chaves+`issuanceGeneration`, o consumidor sempre relê o `DocumentRequest` autoritativo); (3) `DocumentRequestRecurrenceService.updateSeriesRecipient()` novo — reaproveita a action `docarchive:series-update` já existente (antes só usada por `advanceCycle`, sem rota HTTP própria, sem colisão); `recipientEmail: null` mapeia para `REMOVE` no DynamoDB (nunca um `recipientEmail: null` persistido — o tipo do domínio continua `string | undefined`); rejeita com `ConflictError` uma série `CANCELLED` (nunca mais materializa ciclo, mutar seu destinatário não teria efeito observável); mesmo padrão OCC de `cancelSeries`. (4) Rota nova `POST /document-archive/series/{subjectId}/{seriesId}/recipient` (handler `handleUpdateSeriesRecipient`, schema `docarchive-series-update-recipient-request.v1.json`, entrada wireada em `infra/modules/api-gateway/main.tf` e `proxy-allowlist.ts`); `docarchive-series-create-request.v1.json` ganha `recipientEmail` opcional (`format: email`, `maxLength: 254`). PII: `recipientEmail` É dado pessoal de terceiro (correção de uma formulação errada da Rodada 1, apontada pelo Codex) — a decisão de não mascarar em `getSeries`/`listSeries` segue o mesmo precedente de autorização já aceito em D-228 (tenant que forneceu o dado para o próprio fluxo dele), não por ausência de sensibilidade. G-V3 real: `guest-credential-issuance-to-delivery-e2e.test.ts` estendido com 2 cenários novos — série COM `recipientEmail` → `materializeAttempt` → `DocumentRequestCredentialIssuanceService` → `deliverGuestCredential` → `GuestDocumentAccessService.resolveCredential()` aceita o link de verdade (mesmo padrão do cenário avulso já existente); série SEM `recipientEmail` continua com skip terminal (`SKIPPED_NO_RECIPIENT_EMAIL`, não regressão). Mais 17 testes unitários novos em `document-request-recurrence.test.ts` (persistência/trim, cópia pelo builder, snapshot entre ciclos, `updateSeriesRecipient` troca/remoção/OCC/série cancelada/RBAC, exposição em `getSeries`/`listSeries`) e 1 em `document-request-recurrence-materializer.test.ts` (prova que o worker PERIÓDICO, não só o interativo, copia o campo). Gate local completo verde: `typecheck`/`lint`/`check-boundaries`/`check-docs`/`validate-schemas`/`build:lambdas` limpos; suíte completa **2572/2572** (233 arquivos); `terraform fmt`/`validate`/`test` (23/23, `AWS_PROFILE=claude-dev`) limpos. Evidência completa das 3 rodadas em `docs/architecture/reviews/document-request-series-recipient-scoping/` (`round1-claude.md`, `round1-codex-critique.md`, `round2-claude.md`, `round2-codex-critique.md`, `round3-claude.md`, `round3-codex-critique.md`, `estado-final-consolidado.md`). | Nível 5 (`change-risk-scale.md` — muda contrato HTTP + introduz ação/mutação nova; reclassificado de Nível 3 durante o protocolo, na dúvida trata-se como o nível mais alto) | 9,2 | 9,2 | **IMPLEMENTADO — item 9 do roadmap P0 FECHADO POR INTEIRO (caminho avulso desde D-228 + caminho de recorrência agora): tanto `createDocumentRequest()` quanto `DocumentRequestSeries`/`materializeAttempt` podem entregar um link de guest utilizável de verdade quando um `recipientEmail` é fornecido; sem ele, ambos os caminhos continuam fazendo skip terminal, nunca erro** | `src/modules/document-archive/{domain/document-request-series.ts,application/document-request-recurrence-service.ts,http/document-archive-handlers.ts}`, `src/runtime/aws/handlers/document-archive-handler.ts`, `src/modules/bff/domain/proxy-allowlist.ts`, `src/shared/contracts/schema-validator.ts`, `schemas/api/{docarchive-series-create-request.v1.json,docarchive-series-update-recipient-request.v1.json}`, `infra/modules/api-gateway/main.tf`, `test/unit/document-archive/{document-request-recurrence.test.ts,document-request-recurrence-materializer.test.ts,guest-credential-issuance-to-delivery-e2e.test.ts}`, `docs/architecture/reviews/document-request-series-recipient-scoping/` | 2026-09-07 |
test/unit\activity\activity-handlers.test.ts:14:import type { TenantQuotaService } from "../../../src/modules/identity/application/quota.js";
test/unit\activity\activity-handlers.test.ts:26:function fakeQuota(): TenantQuotaService {
test/unit\activity\activity-handlers.test.ts:27:  return { consume: async () => undefined } as unknown as TenantQuotaService;
test/unit\activity\activity-handlers.test.ts:49:      quota: fakeQuota(),
test/unit\activity\activity-handlers.test.ts:59:      quota: fakeQuota(),
test/unit\activity\activity-handlers.test.ts:71:      quota: fakeQuota(),
src/modules\organization\ports\organization-store.ts:5: * `TenantEntitlement` defaults numa única `TransactWriteItems` (physical model §4).
src/shared\transient-purge-gsi8.ts:14: *   - `WebhookInbox` — create-once, immutable (D-187/quota-telemetry-purge shape): the pointer is
src/modules\subject\ports\subject-store.ts:4: * TrackedSubject/RequirementAssignment que afeta o contador de entitlement precisa comitar
src/modules\subject\ports\subject-store.ts:6: * domain/entitlement.ts).
src/modules\expiration\http\item-watch-handlers.ts:7:import type { TenantQuotaService } from "../../identity/application/quota.js";
src/modules\expiration\http\item-watch-handlers.ts:27:  quota: TenantQuotaService;
src/modules\expiration\http\item-watch-handlers.ts:72:  await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
test/unit\reports\report-subscription-handlers.test.ts:24:import type { TenantQuotaService } from "../../../src/modules/identity/application/quota.js";
test/unit\reports\report-subscription-handlers.test.ts:38:function fakeQuota(): TenantQuotaService {
test/unit\reports\report-subscription-handlers.test.ts:39:  return { consume: async () => undefined } as unknown as TenantQuotaService;
test/unit\reports\report-subscription-handlers.test.ts:72:  return { resolver: fakeResolver(ctx()), reports: undefined as never, subscriptions, quota: fakeQuota() };
test/unit\reports\report-subscription-handlers.test.ts:202:    const deps: ReportsHttpDeps = { resolver: fakeResolver(contextWithUser), reports: undefined as never, subscriptions: undefined as never, quota: fakeQuota(), subscriptionStore: store, exportStore };
test/unit\reports\report-subscription-handlers.test.ts:207:    const deps: ReportsHttpDeps = { resolver: fakeResolver(ctx()), reports: undefined as never, subscriptions: undefined as never, quota: fakeQuota() };
src/modules\document-archive\http\document-archive-handlers.ts:15:import type { TenantQuotaService } from "../../identity/application/quota.js";
src/modules\document-archive\http\document-archive-handlers.ts:87:  quota: TenantQuotaService;
src/modules\document-archive\http\document-archive-handlers.ts:126:async function consumeApiRequestQuota(quota: TenantQuotaService, tenantId: string): Promise<void> {
src/modules\document-archive\http\document-archive-handlers.ts:127:  await quota.consume({ tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\document-archive\http\document-archive-handlers.ts:197:  await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\document-archive\http\document-archive-handlers.ts:635:// the same API_REQUEST quota as every other business write in this module.
src/modules\document-archive\http\document-archive-handlers.ts:656:    await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\document-archive\http\document-archive-handlers.ts:689:    await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\document-archive\http\document-archive-handlers.ts:702:    await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\document-archive\http\document-archive-handlers.ts:714:    await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\document-archive\http\document-archive-handlers.ts:726:    await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\document-archive\http\document-archive-handlers.ts:734: * write quota is consumed. */
src/modules\document-archive\http\document-archive-handlers.ts:755:    await consumeApiRequestQuota(deps.quota, context.tenant.tenantId);
src/modules\expiration\http\item-handlers.ts:16:import type { TenantQuotaService } from "../../identity/application/quota.js";
src/modules\expiration\http\item-handlers.ts:21: * full-audit round1/Seguranca criterio 9 (Resistencia a Abuso/DoS): TenantQuotaService
src/modules\expiration\http\item-handlers.ts:23: * (/items*) nao aplicavam nenhuma quota, permitindo um tenant autenticado gerar
src/modules\expiration\http\item-handlers.ts:27:export async function consumeApiRequestQuota(quota: TenantQuotaService, context: RequestContext): Promise<void> {
src/modules\expiration\http\item-handlers.ts:28:  await quota.consume({
src/modules\expiration\http\item-handlers.ts:30:    quotaType: "API_REQUEST",
src/modules\expiration\http\item-handlers.ts:75:  quota: TenantQuotaService;
src/modules\expiration\http\item-handlers.ts:189:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:199:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:212:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:223:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:234:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:248:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:263:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\item-handlers.ts:306:    await consumeApiRequestQuota(deps.quota, context);
test/unit\workers\tenant-purge\purge-result-envelope.test.ts:23:    // large tenant can blow past Step Functions' 256 KiB task output quota. Mutation that would
src/modules\expiration\http\bulk-action-handlers.ts:7: * generic JSON error-mapping/quota/schema-validation helpers rather than duplicating them
src/modules\expiration\http\bulk-action-handlers.ts:36:    await consumeApiRequestQuota(deps.quota, context);
src/modules\expiration\http\bulk-action-handlers.ts:47:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:4: * status code, mesma quota de API_REQUEST antes de qualquer serviço de negócio).
src/modules\subject\http\subject-handlers.ts:14:import type { TenantQuotaService } from "../../identity/application/quota.js";
src/modules\subject\http\subject-handlers.ts:18:async function consumeApiRequestQuota(quota: TenantQuotaService, context: RequestContext): Promise<void> {
src/modules\subject\http\subject-handlers.ts:19:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\subject-handlers.ts:51:  quota: TenantQuotaService;
src/modules\subject\http\subject-handlers.ts:113:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:123:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:136:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:147:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:158:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:168:    await consumeApiRequestQuota(deps.quota, context);
src/modules\subject\http\subject-handlers.ts:206:    await consumeApiRequestQuota(deps.quota, context);
src/modules\identity\ports\identity-store.ts:24:  /** Unconditional overwrite - only for bookkeeping writes with no concurrent-writer risk (see quota.ts for the counter case, which needs updateConditional instead). */
src/modules\identity\ports\identity-store.ts:28:   * round1, eixo Governança de Produto, criterio 3 - TenantQuota.consume() did a
src/modules\identity\ports\identity-store.ts:30:   * requests for the same tenant/quotaType). Returns false on conditional-check failure so the
src/modules\identity\ports\identity-store.ts:46:   * cheap counterpart to `transactWrite` for `API_REQUEST` quota telemetry. Deliberately NOT a
src/modules\identity\ports\identity-store.ts:61:  quotaType: string;
src/modules\identity\ports\identity-store.ts:73:  /** MaintenanceDueIndex pointer (D-179/D-186) - caller-computed (`quota.ts`'s
src/modules\identity\ports\identity-store.ts:74:   * `quotaTelemetryGsi8Keys()`), deterministic from the same window bucket as `resetAt`/
src/modules\subject\http\requirement-handlers.ts:43:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:53:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:64:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:81:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:93:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:107:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:119:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:137:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\subject\http\requirement-handlers.ts:150:    await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
test/unit\notification\preferences-handlers.test.ts:25:import { TenantQuotaService } from "../../../src/modules/identity/application/quota.js";
test/unit\notification\preferences-handlers.test.ts:39:  const quota = new TenantQuotaService(identityStore, TABLE);
test/unit\notification\preferences-handlers.test.ts:45:  return { resolver, preferences, quota, identityStore };
test/unit\notification\preferences-handlers.test.ts:95:    // W3-07 fence (D-068/D-069 follow-up): quota.consume() now requires a
src/modules\expiration\http\export-handler.ts:117:  await deps.quota.consume({
src/modules\expiration\http\export-handler.ts:119:    quotaType: "API_REQUEST",
src/modules\import\http\import-handlers.ts:9:import type { TenantQuotaService } from "../../identity/application/quota.js";
src/modules\import\http\import-handlers.ts:16:async function consumeApiQuota(quota: TenantQuotaService, context: RequestContext): Promise<void> {
src/modules\import\http\import-handlers.ts:17:  await quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\import\http\import-handlers.ts:42:  quota: TenantQuotaService;
src/modules\import\http\import-handlers.ts:100:    await consumeApiQuota(deps.quota, context);
src/modules\import\http\import-handlers.ts:110:    await consumeApiQuota(deps.quota, context);
src/modules\import\http\import-handlers.ts:120:    await consumeApiQuota(deps.quota, context);
src/modules\import\http\import-handlers.ts:133:    await consumeApiQuota(deps.quota, context);
src/modules\import\http\import-handlers.ts:144:    await consumeApiQuota(deps.quota, context);
test/unit\workers\security-audit-purge.test.ts:214:  // D-179 §8 poison-record handling, same as membership-purge/invitation-purge/quota-telemetry-purge.
src/modules\subject\http\document-request-handlers.ts:41:  await deps.quota.consume({ tenantId: context.tenant.tenantId, quotaType: "API_REQUEST", window: "current", limit: 100, windowSeconds: 60 });
src/modules\identity\persistence\global-user-repository.ts:41:   * `TransactWriteItems` as the Organization/Membership/TenantLifecycleRecord/TenantEntitlement
test/unit\notification\notification-router.test.ts:10:    entitlement: { emailEnabled: true },
test/unit\notification\notification-router.test.ts:68:  it("entitlement storage unavailable -> RETRY (fail-closed WITH retry, not silent cancellation)", () => {
test/unit\notification\notification-router.test.ts:69:    const result = decideRouting(baseInput({ entitlement: { emailEnabled: undefined } }));
test/unit\notification\notification-router.test.ts:73:  it("entitlement denies e-mail -> CANCELLED_ALL NOT_ENTITLED", () => {
test/unit\notification\notification-router.test.ts:74:    const result = decideRouting(baseInput({ entitlement: { emailEnabled: false } }));
test/unit\workers\security-audit-purge-fakes.ts:16: * `quota-telemetry-purge-fakes.ts`'s own evaluator (not shared, different port shape). */
test/unit\workers\security-audit-purge-fakes.ts:42: * `quota-telemetry-purge-fakes.ts`'s `FakeQuotaTelemetryPurgeCandidateSource` exactly. */
src/modules\identity\persistence\dynamodb-identity-store.ts:101:            "ADD #count :incr SET entityType = :entityType, tenantId = :tenantId, quotaType = :quotaType, windowSeconds = :windowSeconds, resetAt = if_not_exists(resetAt, :resetAt), purgeAfterTtl = if_not_exists(purgeAfterTtl, :purgeAfterTtl), GSI8PK = if_not_exists(GSI8PK, :gsi8pk), GSI8SK = if_not_exists(GSI8SK, :gsi8sk)",
src/modules\identity\persistence\dynamodb-identity-store.ts:107:            ":quotaType": input.quotaType,
test/unit\notification\notification-router-workflow.test.ts:8:import { notificationEntitlementsKey, type NotificationEntitlements } from "../../../src/modules/notification/domain/notification-entitlements.js";
test/unit\notification\notification-router-workflow.test.ts:135:  async function seed(input: { item?: ExpirationItem; policy?: ReminderPolicy; entitlements?: NotificationEntitlements; preferences?: NotificationPreferences }) {
test/unit\notification\notification-router-workflow.test.ts:140:    if (input.entitlements !== undefined) await store.putIfAbsent(input.entitlements);
test/unit\notification\notification-router-workflow.test.ts:175:    await seed({ entitlements: defaultEntitlements(), preferences: defaultPreferences(ASSIGNEE) });
test/unit\notification\notification-router-workflow.test.ts:207:    await seed({ entitlements: defaultEntitlements(), preferences: defaultPreferences(ASSIGNEE) });
test/unit\notification\notification-router-workflow.test.ts:216:    await seed({ item: makeItem({ status: "ARCHIVED" }), entitlements: defaultEntitlements(), preferences: defaultPreferences(ASSIGNEE) });
test/unit\notification\notification-router-workflow.test.ts:229:    await seed({ item: makeItem({ version: 4 }), entitlements: defaultEntitlements(), preferences: defaultPreferences(ASSIGNEE) });
test/unit\notification\notification-router-workflow.test.ts:249:    await seed({ item: makeItem({ assigneeUserId: "other-tenant-user" }), entitlements: defaultEntitlements(), preferences: defaultPreferences(TENANT) });
test/unit\notification\notification-router-workflow.test.ts:272:    await seed({ item: makeItem({ assigneeUserId: undefined }), entitlements: defaultEntitlements(), preferences: defaultPreferences(ASSIGNEE) });
test/unit\notification\notification-router-workflow.test.ts:281:  it("entitlement record missing (technical gap) -> RETRY, no write at all", async () => {
test/unit\notification\notification-router-workflow.test.ts:282:    await seed({ preferences: defaultPreferences(ASSIGNEE) }); // no entitlements record
test/unit\notification\notification-router-workflow.test.ts:294:    await seed({ entitlements: defaultEntitlements(), preferences: defaultPreferences(ASSIGNEE) });
test/unit\notification\notification-router-workflow.test.ts:307:    await seed({ entitlements: defaultEntitlements(), preferences: defaultPreferences(WATCHER) });
test/unit\notification\notification-router-workflow.test.ts:326:    await seed({ entitlements: defaultEntitlements(), preferences: defaultPreferences(WATCHER) });
test/unit/notification\corrective-intent-service.test.ts:2:import { decideCorrectiveIntentKind, correctiveIdempotencyKey } from "../../../src/modules/notification/application/corrective-intent-service.js";
test/unit/notification\corrective-intent-service.test.ts:10:  const replacementStates: NotificationAttemptStatus[] = ["PREPARED", "FAILED_RETRYABLE", "FAILED_TERMINAL", "NOT_SENT_STALE"];
test/unit/notification\corrective-intent-service.test.ts:11:  for (const status of replacementStates) {
test/unit/notification\corrective-intent-service.test.ts:17:  const correctiveStates: NotificationAttemptStatus[] = ["SUBMITTING", "ACCEPTED", "DELIVERED", "UNKNOWN", "BOUNCED", "COMPLAINED"];
test/unit/notification\corrective-intent-service.test.ts:18:  for (const status of correctiveStates) {
test/unit/notification\corrective-intent-service.test.ts:25:describe("correctiveIdempotencyKey", () => {
test/unit/notification\corrective-intent-service.test.ts:28:    const replacement = correctiveIdempotencyKey({ ...base, kind: "REPLACEMENT" });
test/unit/notification\corrective-intent-service.test.ts:29:    const corrective = correctiveIdempotencyKey({ ...base, kind: "CORRECTIVE" });
test/unit/notification\corrective-intent-service.test.ts:30:    expect(replacement).not.toBe(corrective);
test/unit/notification\corrective-intent-service.test.ts:31:    expect(replacement).toBe("t1|int-1|4|REPLACEMENT");
test/unit/notification\email-delivery-workflow.test.ts:182:  it("attempt already ACCEPTED (duplicate SQS delivery) -> SKIPPED_RESOLVED, never calls SES twice", async () => {
test/unit/notification\email-delivery-workflow.test.ts:189:  it("attempt SUBMITTING with an active lease (concurrent duplicate in flight) -> SKIPPED_IN_PROGRESS, never calls SES twice", async () => {
test/unit/notification\email-delivery-workflow.test.ts:196:  it("attempt SUBMITTING with an expired lease -> RECONCILED_UNKNOWN, never resends blindly", async () => {
test/unit/notification\email-delivery-workflow.test.ts:199:    expect(outcome).toEqual({ kind: "RECONCILED_UNKNOWN" });
test/unit/notification\email-delivery-workflow.test.ts:202:    expect(attempt?.status).toBe("UNKNOWN");
test/unit/notification\email-delivery-workflow.test.ts:208:    expect(outcome).toEqual({ kind: "NOT_SENT_STALE", correctiveKind: "REPLACEMENT" });
test/unit/notification\email-delivery-workflow.test.ts:232:  it("SES call times out ambiguously (may have been accepted) -> attempt UNKNOWN, never FAILED_RETRYABLE", async () => {
test/unit/notification\email-delivery-workflow.test.ts:238:    expect(outcome).toEqual({ kind: "SEND_FAILED", nextStatus: "UNKNOWN" });
test/unit/notification\email-delivery-workflow.test.ts:240:    expect(attempt?.status).toBe("UNKNOWN");
src/workers\guest-credential-delivery\deliver.ts:11: * Two independent staleness/eligibility guards before ever sending:
src/modules/notification\providers\whatsapp-cloud-api-adapter.ts:33: *  - Network error, timeout, non-JSON response, or the fetch itself throwing: AMBIGUOUS - the
src/modules/notification\providers\whatsapp-cloud-api-adapter.ts:76:  return "AMBIGUOUS";
src/modules/notification\providers\whatsapp-cloud-api-adapter.ts:139:        throw new WhatsAppSendError("WhatsApp Cloud API returned 2xx without a message id.", "AMBIGUOUS");
src/modules/notification\providers\whatsapp-cloud-api-adapter.ts:146:      throw new WhatsAppSendError(err instanceof Error ? err.message : "WhatsApp Cloud API send failed.", "AMBIGUOUS");
test/unit/notification\notification-preferences-service.test.ts:55:      { emailEnabled: false, locale: "en-US", quietHours: { enabled: true, startLocal: "22:00", endLocal: "07:00", timeZone: "UTC" } },
test/unit/notification\notification-preferences-service.test.ts:60:    expect(updated.quietHours).toEqual({ enabled: true, startLocal: "22:00", endLocal: "07:00", timeZone: "UTC" });
test/unit/notification\notification-preferences-service.test.ts:65:  it("updatePreferences rejects a stale expectedVersion with a version-conflict error", async () => {
test/unit/notification\notification-preferences-service.test.ts:67:    await service.updatePreferences(ctx(), { emailEnabled: true, locale: "pt-BR", quietHours: null }, 1); // -> version 2
test/unit/notification\notification-preferences-service.test.ts:70:      service.updatePreferences(ctx(), { emailEnabled: false, locale: "pt-BR", quietHours: null }, 1),
test/unit/notification\notification-preferences-service.test.ts:74:  it("updatePreferences can clear quietHours back to null", async () => {
test/unit/notification\notification-preferences-service.test.ts:77:      { emailEnabled: true, locale: "pt-BR", quietHours: { enabled: true, startLocal: "22:00", endLocal: "07:00", timeZone: "UTC" } },
test/unit/notification\notification-preferences-service.test.ts:80:    const cleared = await service.updatePreferences(ctx(), { emailEnabled: true, locale: "pt-BR", quietHours: null }, 2);
test/unit/notification\notification-preferences-service.test.ts:81:    expect(cleared.quietHours).toBeNull();
src/modules/notification\providers\ses-email-adapter.ts:10: * is AMBIGUOUS by default (never assumed safe to retry) - the request may have reached SES
src/modules/notification\providers\ses-email-adapter.ts:33:  return "AMBIGUOUS";
src/modules/notification\providers\ses-email-adapter.ts:69:        throw new EmailSendError("SES SendEmail succeeded without a MessageId.", "AMBIGUOUS");
test/unit/notification\email-delivery.test.ts:19:  it("SUBMITTING with expired lease -> RECONCILE_UNKNOWN, never resend blindly", () => {
test/unit/notification\email-delivery.test.ts:22:    ).toEqual({ action: "RECONCILE_UNKNOWN" });
test/unit/notification\email-delivery.test.ts:25:  it("SUBMITTING with no lease recorded at all -> RECONCILE_UNKNOWN", () => {
test/unit/notification\email-delivery.test.ts:26:    expect(decideSendAction({ status: "SUBMITTING" }, "2026-09-10T12:00:00.000Z")).toEqual({ action: "RECONCILE_UNKNOWN" });
test/unit/notification\email-delivery.test.ts:29:  for (const status of ["ACCEPTED", "DELIVERED", "BOUNCED", "COMPLAINED", "FAILED_TERMINAL", "UNKNOWN", "NOT_SENT_STALE"] as const) {
test/unit/notification\email-delivery.test.ts:49:  it("AMBIGUOUS failure (timeout after possible acceptance) -> UNKNOWN, never FAILED_RETRYABLE (would risk a blind duplicate retry)", () => {
test/unit/notification\email-delivery.test.ts:50:    expect(nextStatusAfterSendAttempt({ kind: "FAILURE", failureKind: "AMBIGUOUS" })).toBe("UNKNOWN");
src/workers\dossier-export\generate.ts:11: * `DocumentArchiveService.getDossierExportData` (decision 4 - values are never stale) - a
test/unit/notification\preferences-handlers.test.ts:66:      body: { emailEnabled: false, locale: "en-US", quietHours: null },
test/unit/notification\preferences-handlers.test.ts:87:      body: { emailEnabled: false, locale: "en-US", quietHours: null, unknownField: "nope" } as never,
test/unit/notification\notification-router-workflow.test.ts:166:      quietHours: null,
test/unit/notification\notification-router-workflow.test.ts:206:  it("intent no longer PENDING (duplicate Streams delivery) -> NOOP_NOT_PENDING, no writes", async () => {
test/unit/notification\notification-router-workflow.test.ts:228:  it("item version stale, no prior attempt -> STALE_REPLACEMENT, creates a new REPLACEMENT intent, cancels the old one", async () => {
src/modules/reminder\ports\reminder-store.ts:8: * data-model.md §2) for materialization diffing, due-date-change cancellation and DST
src/workers\document-chasing-dispatch\dispatch.ts:14: * Validação de staleness (mesma ordem de dispatch.ts do reminder): ocorrência precisa estar
src/workers\document-chasing-dispatch\dispatch.ts:82:  // que também nunca usa esse campo do comando; a staleness real da OCORRÊNCIA em si já é
src/workers\document-chasing-dispatch\dispatch.ts:97:  const stale =
src/workers\document-chasing-dispatch\dispatch.ts:104:  if (stale) {
src/workers\document-request-recurrence\materializer.ts:75:          // A duplicate scheduler tick (or a concurrent manual trigger) already advanced this
test/unit/notification\ses-callback-workflow.test.ts:108:  it("duplicate SNS delivery of the exact same event -> DUPLICATE_INBOX, no double-apply", async () => {
test/unit/notification\ses-callback-workflow.test.ts:147:      quietHours: null,
test/unit/notification\notification-router.test.ts:11:    preference: { emailEnabled: true, quietHours: undefined },
test/unit/notification\notification-router.test.ts:28:  it("item version stale with no prior attempt -> STALE REPLACEMENT", () => {
test/unit/notification\notification-router.test.ts:30:    expect(result).toEqual({ kind: "STALE", correctiveKind: "REPLACEMENT" });
test/unit/notification\notification-router.test.ts:33:  it("item version stale with a SUBMITTING prior attempt -> STALE CORRECTIVE (delivery may have crossed the external limit)", () => {
test/unit/notification\notification-router.test.ts:35:    expect(result).toEqual({ kind: "STALE", correctiveKind: "CORRECTIVE" });
test/unit/notification\notification-router.test.ts:50:    expect(result).toEqual({ kind: "STALE", correctiveKind: "REPLACEMENT" });
test/unit/notification\notification-router.test.ts:78:  it("preference record missing/unavailable -> RETRY (technical gap, never interpreted as opt-in or opt-out)", () => {
test/unit/notification\notification-router.test.ts:79:    const result = decideRouting(baseInput({ preference: { emailEnabled: undefined, quietHours: undefined } }));
test/unit/notification\notification-router.test.ts:84:    const result = decideRouting(baseInput({ preference: { emailEnabled: false, quietHours: undefined } }));
test/unit/notification\notification-router.test.ts:103:  it("quiet hours active -> ROUTED with deliverNotBefore set, never cancelled", () => {
test/unit/notification\notification-router.test.ts:108:          quietHours: { enabled: true, startLocal: "22:00", endLocal: "07:00", timeZone: "America/Sao_Paulo" },
test/unit/notification\ses-callback-processor.test.ts:21:  it("never regresses DELIVERED -> ACCEPTED (duplicate DELIVERY callback is a no-op)", () => {
test/unit/notification\ses-callback-processor.test.ts:33:  it("callback arrives before local MessageId persistence (attempt still SUBMITTING/UNKNOWN) -> still applies, it's new information", () => {
test/unit/notification\ses-callback-processor.test.ts:35:    expect(decideCallbackApplication("UNKNOWN", "BOUNCE")).toEqual({ apply: true, nextStatus: "BOUNCED" });
test/unit/notification\ses-callback-processor.test.ts:38:  it("duplicate callback of the exact same kind is idempotent no-op", () => {
src/workers\dispatch-outbox-relay\relay.ts:95:    // Stream retry) will retry. A duplicate SendMessage after a retry is absorbed by the
src/modules/reminder\ports\reconciliation-candidate-source.ts:10: * know a priori which tenants have an expired claim or a DST candidate. `tenantId` is
src/modules/reminder\ports\reconciliation-candidate-source.ts:30:/** Same ALL-projection reasoning as ExpiredClaimCandidate: the DST_PENDING pointer is
src/modules/reminder\ports\reconciliation-candidate-source.ts:32: * occurrence's schedule crosses a known DST transition), not on the ReminderPolicy - a
src/modules/reminder\ports\reconciliation-candidate-source.ts:51:  /** GSI6PK="WORKSTATE#DST_PENDING" (global), GSI6SK=<windowStart>#TENANT#<tenantId>#OCCURRENCE#<occurrenceId>. */
src/modules/reminder\ports\reconciliation-candidate-source.ts:56:export const GSI6PK_WORKSTATE_DST_PENDING = "WORKSTATE#DST_PENDING";
src/modules/reminder\application\reminder-policy-service.ts:71:      quietHours: input.rule.quietHours,
src/modules/reminder\application\reminder-policy-service.ts:118:      quietHours: input.rule.quietHours,
test/unit/notification\whatsapp-delivery.test.ts:19:  it("SUBMITTING with expired lease -> RECONCILE_UNKNOWN, never resend blindly", () => {
test/unit/notification\whatsapp-delivery.test.ts:21:      action: "RECONCILE_UNKNOWN",
test/unit/notification\whatsapp-delivery.test.ts:25:  for (const status of ["ACCEPTED", "DELIVERED", "BOUNCED", "COMPLAINED", "FAILED_TERMINAL", "UNKNOWN", "NOT_SENT_STALE"] as const) {
test/unit/notification\whatsapp-delivery.test.ts:45:  it("AMBIGUOUS failure (timeout after possible acceptance) -> UNKNOWN, never FAILED_RETRYABLE (would risk a blind duplicate retry)", () => {
test/unit/notification\whatsapp-delivery.test.ts:46:    expect(nextWhatsAppStatusAfterSendAttempt({ kind: "FAILURE", failureKind: "AMBIGUOUS" })).toBe("UNKNOWN");
test/unit/notification\whatsapp-webhook-workflow.test.ts:83:  it("duplicate wamid+statusType -> DUPLICATE_INBOX, no double-apply", async () => {
test/unit/notification\whatsapp-webhook-workflow.test.ts:92:  it("a DIFFERENT statusType for the SAME wamid is a SEPARATE inbox row, not a duplicate (SK includes statusType)", async () => {
test/unit/notification\quiet-hours.test.ts:2:import { computeDeliverNotBefore } from "../../../src/modules/notification/application/quiet-hours.js";
test/unit/notification\quiet-hours.test.ts:17:    // America/Sao_Paulo has no DST since 2019, fixed UTC-3. 12:00 UTC = 09:00 local.
src/modules/reminder\application\reminder-materializer.ts:6: * Closes Red Team scenario 13 (stale reminders after a due-date edit).
src/modules/reminder\application\reminder-materializer.ts:15: * idempotência"). A retried/duplicated materialize() call computes the same occurrenceId
src/modules/reminder\application\reminder-materializer.ts:32:import { GSI6PK_WORKSTATE_DST_PENDING, buildDstCandidateGsi6Sk } from "../ports/reconciliation-candidate-source.js";
src/modules/reminder\application\reminder-materializer.ts:48:function applyQuietHours(hour: number, minute: number, quietHours: QuietHours | undefined): { hour: number; minute: number } {
src/modules/reminder\application\reminder-materializer.ts:49:  if (!quietHours) return { hour, minute };
src/modules/reminder\application\reminder-materializer.ts:50:  const start = parseLocalTime(quietHours.startLocalTime);
src/modules/reminder\application\reminder-materializer.ts:51:  const end = parseLocalTime(quietHours.endLocalTime);
src/modules/reminder\application\reminder-materializer.ts:92:  /** Computes the scheduledAt (UTC) + local wall-clock string for one trigger, given the item's dueDate. Pure - no I/O - exposed for unit testing DST edge cases directly. */
src/modules/reminder\application\reminder-materializer.ts:98:    quietHours?: QuietHours;
src/modules/reminder\application\reminder-materializer.ts:99:  }): { scheduledAtUtc: string; localScheduledAt: string; dstKind: "NORMAL" | "AMBIGUOUS" | "NONEXISTENT" } {
src/modules/reminder\application\reminder-materializer.ts:104:    const { hour, minute } = applyQuietHours(rawHour, rawMinute, input.quietHours);
src/modules/reminder\application\reminder-materializer.ts:139:        quietHours: input.policy.quietHours,
src/modules/reminder\application\reminder-materializer.ts:162:      // Codex's implementation review found the original AMBIGUOUS/NONEXISTENT-only trigger
src/modules/reminder\application\reminder-materializer.ts:163:      // too narrow): a GSI6 WORKSTATE#DST_PENDING pointer is set whenever EITHER the
src/modules/reminder\application\reminder-materializer.ts:165:      // OR the policy's timeZone observes DST at all in the occurrence's target year - a
src/modules/reminder\application\reminder-materializer.ts:166:      // plain 09:00 reminder in a DST-observing zone can still need re-evaluation after an
src/modules/reminder\application\reminder-materializer.ts:173:            GSI6PK: GSI6PK_WORKSTATE_DST_PENDING,
src/modules/reminder\application\reminder-materializer.ts:219:   * materialized under a stale itemVersion (still SCHEDULED/CLAIMED), one conditional
src/modules/reminder\application\reminder-materializer.ts:242:              // GSI6 but never extended to GSI3) - the scheduler index kept a stale pointer.
src/modules/reminder\application\reminder-materializer.ts:326:   * occurrences that are stale relative to the policy's current version, or whose policy is
test/unit/notification\whatsapp-webhook-processor.test.ts:193:  it("duplicate callback (same status twice) is an idempotent no-op", () => {
test/unit/notification\whatsapp-cloud-api-adapter.test.ts:34:  it("2xx WITHOUT a message id -> AMBIGUOUS (never assumes success or failure)", async () => {
test/unit/notification\whatsapp-cloud-api-adapter.test.ts:36:    await expect(adapter().send(INPUT)).rejects.toMatchObject({ kind: "AMBIGUOUS" });
test/unit/notification\whatsapp-cloud-api-adapter.test.ts:79:  it("fetch() itself throws (network error/timeout/DNS) -> AMBIGUOUS, the request may have reached Meta", async () => {
test/unit/notification\whatsapp-cloud-api-adapter.test.ts:81:    await expect(adapter().send(INPUT)).rejects.toMatchObject({ kind: "AMBIGUOUS" });
src/workers\document-file-reconciliation\candidate-source.ts:10: * real candidate in production. GSI8 is a clean replacement, not a second mechanism.
src/modules/reminder\domain\notification-intent.ts:12: * could have been crossed for the stale intent being superseded (see
src/modules/reminder\domain\notification-intent.ts:13: * corrective-intent-service.ts): REPLACEMENT when nothing could have gone out yet,
src/modules/reminder\domain\notification-intent.ts:14: * CORRECTIVE when a stale delivery is possible or proven.
src/workers\reminder-dispatch\dispatch.ts:48:  /** Max allowed lag between scheduledAt and now before a claimed occurrence is considered stale (default 30 minutes - generous vs. the claim TTL, covers legitimate SQS/Lambda retry delay). */
src/workers\reminder-dispatch\dispatch.ts:196:  const stale =
src/workers\reminder-dispatch\dispatch.ts:206:  if (stale) {
src/workers\reminder-dispatch\dispatch.ts:239:      // silently treated as an already-resolved stale occurrence). Only entry 0 (this
src/workers\reminder-dispatch\dispatch.ts:277:    // D-200 (watcher notification fan-out): the deduplicated recipient set - assignee (if any)
src/workers\reminder-dispatch\dispatch.ts:282:    // already uses, no GSI), never trusted stale by the router later.
src/workers\reminder-dispatch\dispatch.ts:313:    // Freshness fence (BLOCKER-B, Codex Round B CRITICAL finding): the `stale` check above
src/workers\reminder-dispatch\dispatch.ts:357:      // duplicate delivery of the same command racing a prior success on THIS occurrence.
src/workers\reminder-dispatch\dispatch.ts:363:      // re-evaluated by a later dispatch attempt or reconciliation, never a duplicate send.
src/workers\reminder-materialization-trigger\trigger.ts:8: * only production caller was the DST-reconciliation worker.
src/workers\reminder-materialization-trigger\trigger.ts:14: * safe under at-least-once, no-ordering delivery: duplicate/out-of-order/concurrent
src/workers\reminder-materialization-trigger\trigger.ts:19: * every candidate partition (`previousItemId` and the current target, deduplicated) rather
src/workers\reminder-materialization-trigger\trigger.ts:62:/** Reconciles one policy against its CURRENT target item: cancels stale/disabled occurrences, materializes if enabled. Caller has already confirmed `item` is ACTIVE. */
src/workers\reminder-materialization-trigger\trigger.ts:90: * each, then run the pre-existing itemVersion staleness safety net.
src/workers\reminder-materialization-trigger\trigger.ts:111:      // Orphaned/stale pointer (§5) - never trusted, silently skipped. Corrected by the
src/workers\reminder-materialization-trigger\trigger.ts:135: * reconciles every candidate partition (previousItemId + current target, deduplicated)
src/workers\reminder-reconciliation\reconciliation.ts:5: * and §9.5 itself (DST):
src/workers\reminder-reconciliation\reconciliation.ts:11: *  (b) DST-affected re-evaluation (§9.5 steps 1-6): for each (item, enabled policy) pair
src/workers\reminder-reconciliation\reconciliation.ts:18: * `runReconciliation`, in the order claim-expiry -> DST (claim-expiry is cheaper and
src/workers\reminder-reconciliation\reconciliation.ts:35:/** One (item, active policy) pair the DST pass evaluates. In production this batch comes
src/workers\reminder-reconciliation\reconciliation.ts:58: * SCHEDULED<->CLAIMED lifecycle) revert through the EXACT SAME mechanism, no duplicated
src/workers\reminder-reconciliation\reconciliation.ts:109:/** Pass (b): DST-affected re-evaluation over `[now, now+7d]` (§9.5 steps 1-6). */
src/workers\reminder-reconciliation\reconciliation.ts:142:        quietHours: candidate.policy.quietHours,
src/workers\reminder-reconciliation\reconciliation.ts:150:    // diverges from a fresh DST-aware recomputation (history TRIGGERED/ACKED untouched -
src/workers\reminder-reconciliation\reconciliation.ts:157:        // WORKSTATE#DST_PENDING pointers in place forever whenever recomputation confirmed
src/workers\reminder-reconciliation\reconciliation.ts:161:        // occurrence as a DST candidate and this loop running - `queryByItem` above would
src/workers\reminder-reconciliation\reconciliation.ts:166:        // does. Only ever touch a SCHEDULED occurrence that still actually carries the DST
src/workers\reminder-reconciliation\reconciliation.ts:168:        if (occurrence.status === "SCHEDULED" && occurrence.GSI6PK === "WORKSTATE#DST_PENDING") {
src/workers\membership-purge\purge.ts:79:  /** New in D-179/D-180 — a stale GSI8 pointer (row reactivated or otherwise no longer a real
src/workers\requirement-evidence-daily-sweep\sweep.ts:59: * (daily cadence, so worst case one extra day of staleness on the tail of a huge backlog). */
src/workers\invitation-purge\purge.ts:22: * (`accept-invitation.ts`), so it never appears in a GSI8 query at all. The stale-pointer self-heal
src/workers\invitation-purge\purge.ts:25: * in this codebase that would ever produce a stale INVITATION_PURGE pointer today.
src/workers\invitation-purge\purge.ts:74:  /** A stale GSI8 pointer (no real path produces this today for Invitation, see file header)
src/workers\reminder-producer\producer.ts:141:  // materialization ever wrote it under a stale bucket, which shouldn't happen - but a
src/workers\reminder-producer\producer.ts:223:            // producer's own lookback window nor the daily DST reconciliation reliably
src/workers\report-subscription-delivery\delivery.ts:181:  if (action.action === "RECONCILE_UNKNOWN") {
src/workers\report-subscription-delivery\delivery.ts:182:    await tryConditionalUpdate(deps, attempt, { status: "UNKNOWN", completedAt: now }, now);
src/workers\report-subscription-delivery\delivery.ts:212:    const failureKind = err instanceof EmailSendError ? err.kind : "AMBIGUOUS";
src/workers\membership-purge\candidate-source.ts:47:   * structural replacement for the old `Scan`+`Limit` (D-170 achado #5). */
src/workers\submission-finalizer\finalizer.ts:31:export type FinalizeSubmissionOutcome = "CONFIRMED" | "REJECTED_INVALID" | "IGNORED_UNKNOWN_SLOT" | "IGNORED_STALE" | "IGNORED_TENANT_NOT_ACTIVE";
src/workers\submission-finalizer\finalizer.ts:41:    if (!submission) return "IGNORED_UNKNOWN_SLOT";
src/workers\submission-finalizer\finalizer.ts:42:    if (submission.quarantineObject.key !== input.object.key || submission.quarantineObject.bucket !== input.object.bucket) return "IGNORED_UNKNOWN_SLOT";
5: * synthetic/disposable (zero real Organization/Membership) — reset, not migration, per
rg: test/unit/reminder*: A sintaxe do nome do arquivo, do nome do diretório ou do rótulo do volume está incorreta. (os error 123)

2026-09-08T02:46:28.130405Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 93 column 5
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$i=0; Get-Content scripts/reset-dev-data.ts | ForEach-Object { $i++; if($i -le 240){'"'{0}:{1}' -f "'$i,$_} }; $i=0; Get-Content src/modules/organization/application/close-organization.ts | ForEach-Object { $i++; '"'{0}:{1}' -f "'$i,$_ }; $i=0; Get-Content src/modules/organization/application/cancel-organization-closure.ts | ForEach-Object { $i++; '"'{0}:{1}' -f "'$i,$_ }' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 3293ms:
1:/**
2: * Wave B2B-12 (docs/architecture/multi-user-b2b-wave-b2b12-scope.md, `APPROVED` D-110) ƒ?" one-off
3: * reset/reseed of the `dev` environment ahead of the Multi-User B2B cutover. Read-only inventory
4: * done during the scoping protocol (Rodada 1) confirmed every row in `dev` today is
5: * synthetic/disposable (zero real Organization/Membership) ƒ?" reset, not migration, per
6: * roadmap-evolution/17 ¶õ63.
7: *
8: * Two sequential phases, never combined:
9: *   Phase A (always runs, read-only): inventories `exptrk-dev-table`, `exptrk-dev-bff-session`,
10: *     the `extraction-transient` S3 bucket, the Cognito user pool, and all 24 SQS queues (12 +
11: *     DLQs). Writes a RAW snapshot (may contain PII/secrets ƒ?" session cookies, e-mails) to
12: *     `.local-artifacts/dev-reset/<ISO timestamp>/` (gitignored, never committed) and a REDACTED
13: *     manifest (counts + entityTypes + SHA-256 hash per item, never a raw field value) to
14: *     `docs/architecture/reviews/multi-user-b2b-wave-b2b12-scoping/`.
15: *   Phase B (only with `--confirm`, and only after Phase A's snapshot write succeeds in the SAME
16: *     invocation ƒ?" fail-closed): deletes DynamoDB items in batches of <=25 via BatchWriteItem,
17: *     retrying UnprocessedItems with exponential backoff+jitter (`retryWithBackoff`, defined
18: *     locally ƒ?" no reusable backoff helper exists elsewhere in this project, confirmed by
19: *     `dispatch-outbox-relay/relay.ts`'s explicit "no backoff of its own" design); purges all 24
20: *     SQS queues (native `PurgeQueue`); deletes S3 objects; deletes Cognito users ONLY with the
21: *     additional `--include-cognito` flag (never a silent side effect of the DynamoDB reset).
22: *     Ends with a second full read (Scan + queue attributes) ƒ?" any non-zero count is a hard
23: *     failure (fail-loud), never a silent partial success.
24: *
25: * Safety:
26: *   - Table/session-table/bucket names are validated against a hardcoded allowlist, never a free
27: *     parameter ƒ?" this AWS account is shared with other unrelated projects
28: *     (marcelo-goncalves-blog-dev-*, terraform-lock-stocks-ranking).
29: *   - `sts:GetCallerIdentity` confirms the caller is the expected dev account before Phase B runs.
30: *   - Phase A and `--dry-run` (the default; `--confirm` is required to write/delete anything) are
31: *     pure reads.
32: *
33: * Usage:
34: *   tsx scripts/reset-dev-data.ts [--confirm] [--include-cognito] [--user-pool-id <id>]
35: *
36: * Never auto-run on deploy/CI ƒ?" manual, one-off operator invocation only, same posture as
37: * scripts/backfill-reminder-policies.ts. Actually running this with --confirm (or
38: * --include-cognito) against the real claude-dev account requires Marcelo's explicit
39: * confirmation first (AGENTS.md ¶õ1 exception, registered in multi-user-b2b-wave-b2b12-scope.md) ƒ?"
40: * this script itself has no such gate baked in (a CLI flag cannot enforce a human confirmation
41: * step), so the operator invoking it IS that confirmation.
42: */
43:import { createHash } from "node:crypto";
44:import { mkdir, writeFile } from "node:fs/promises";
45:import { join } from "node:path";
46:import { fileURLToPath } from "node:url";
47:import { BatchWriteCommand, ScanCommand } from "@aws-sdk/lib-dynamodb";
48:import { createDocumentClient } from "../src/shared/dynamodb/client.js";
49:import { STSClient, GetCallerIdentityCommand } from "@aws-sdk/client-sts";
50:import { S3Client, ListObjectsV2Command, DeleteObjectsCommand } from "@aws-sdk/client-s3";
51:import { SQSClient, GetQueueAttributesCommand, GetQueueUrlCommand, PurgeQueueCommand } from "@aws-sdk/client-sqs";
52:import { CognitoIdentityProviderClient, ListUsersCommand, AdminDeleteUserCommand } from "@aws-sdk/client-cognito-identity-provider";
53:
54:export const ALLOWED_MAIN_TABLE = "exptrk-dev-table";
55:export const ALLOWED_SESSION_TABLE = "exptrk-dev-bff-session";
56:export const ALLOWED_BUCKET = "exptrk-dev-extraction-transient";
57:export const EXPECTED_ACCOUNT_ID = "975707451904";
58:
59:export const QUEUE_BASE_NAMES = [
60:  "document-chasing-dispatch",
61:  "extraction-starter",
62:  "import-commit",
63:  "import-parse",
64:  "malware-result",
65:  "notification-email-deliver",
66:  "notification-router",
67:  "reminder-dispatch",
68:  "reminder-materialization-trigger",
69:  "ses-callback",
70:  "textract-completion",
71:  "upload-finalizer",
72:] as const;
73:
74:export function queueNames(): string[] {
75:  return QUEUE_BASE_NAMES.flatMap((base) => [`exptrk-dev-${base}`, `exptrk-dev-${base}-dlq`]);
76:}
77:
78:export interface Args {
79:  table: string;
80:  sessionTable: string;
81:  bucket: string;
82:  confirm: boolean;
83:  includeCognito: boolean;
84:  userPoolId?: string;
85:}
86:
87:export function assertAllowedTable(name: string, allowed: string): void {
88:  if (name !== allowed) {
89:    throw new Error(`Refusing to operate on "${name}" ƒ?" the only value allowed here is "${allowed}" (dev-only allowlist, never a free parameter).`);
90:  }
91:}
92:
93:export function parseArgs(argv: string[]): Args {
94:  const get = (flag: string): string | undefined => {
95:    const i = argv.indexOf(flag);
96:    return i >= 0 ? argv[i + 1] : undefined;
97:  };
98:  const table = get("--table") ?? ALLOWED_MAIN_TABLE;
99:  const sessionTable = get("--session-table") ?? ALLOWED_SESSION_TABLE;
100:  const bucket = get("--bucket") ?? ALLOWED_BUCKET;
101:  assertAllowedTable(table, ALLOWED_MAIN_TABLE);
102:  assertAllowedTable(sessionTable, ALLOWED_SESSION_TABLE);
103:  assertAllowedTable(bucket, ALLOWED_BUCKET);
104:  return {
105:    table,
106:    sessionTable,
107:    bucket,
108:    confirm: argv.includes("--confirm"),
109:    includeCognito: argv.includes("--include-cognito"),
110:    userPoolId: get("--user-pool-id"),
111:  };
112:}
113:
114:export async function assertExpectedAccount(getCallerIdentityAccountId: () => Promise<string | undefined>): Promise<void> {
115:  const account = await getCallerIdentityAccountId();
116:  if (account !== EXPECTED_ACCOUNT_ID) {
117:    throw new Error(`Refusing to proceed: caller identity account "${account}" does not match the expected dev account "${EXPECTED_ACCOUNT_ID}".`);
118:  }
119:}
120:
121:export function chunk<T>(items: T[], size: number): T[][] {
122:  const out: T[][] = [];
123:  for (let i = 0; i < items.length; i += size) out.push(items.slice(i, i + size));
124:  return out;
125:}
126:
127:export interface BackoffOptions {
128:  retries: number;
129:  baseMs: number;
130:  jitterMs: number;
131:  sleep: (ms: number) => Promise<void>;
132:}
133:
134:export const DEFAULT_BACKOFF: BackoffOptions = {
135:  retries: 5,
136:  baseMs: 200,
137:  jitterMs: 100,
138:  sleep: (ms) => new Promise((resolve) => setTimeout(resolve, ms)),
139:};
140:
141:/**
142: * Retries a BatchWriteItem-shaped delete until DynamoDB reports zero UnprocessedItems, with
143: * exponential backoff + jitter between attempts. Throws (never silently gives up) once
144: * `opts.retries` is exceeded ƒ?" the caller (Phase B) treats that as a hard failure of the whole
145: * reset, not a partial success.
146: */
147:export async function deleteBatchWithRetry(
148:  batchWriteDelete: (tableName: string, keys: Record<string, unknown>[]) => Promise<Record<string, unknown>[]>,
149:  tableName: string,
150:  keys: Record<string, unknown>[],
151:  opts: BackoffOptions = DEFAULT_BACKOFF,
152:): Promise<void> {
153:  let remaining = keys;
154:  let attempt = 0;
155:  while (remaining.length > 0) {
156:    if (attempt > opts.retries) {
157:      throw new Error(`deleteBatchWithRetry: giving up after ${attempt} attempts, ${remaining.length} item(s) still unprocessed for table "${tableName}".`);
158:    }
159:    if (attempt > 0) {
160:      const delay = opts.baseMs * 2 ** (attempt - 1) + Math.floor(Math.random() * opts.jitterMs);
161:      await opts.sleep(delay);
162:    }
163:    remaining = await batchWriteDelete(tableName, remaining);
164:    attempt += 1;
165:  }
166:}
167:
168:export function extractKey(item: Record<string, unknown>): Record<string, unknown> {
169:  const { PK, SK } = item as { PK?: unknown; SK?: unknown };
170:  if (PK === undefined || SK === undefined) {
171:    throw new Error("extractKey: item is missing PK/SK ƒ?" cannot build a delete key for it.");
172:  }
173:  return { PK, SK };
174:}
175:
176:export async function deleteAllItems(
177:  batchWriteDelete: (tableName: string, keys: Record<string, unknown>[]) => Promise<Record<string, unknown>[]>,
178:  tableName: string,
179:  items: Record<string, unknown>[],
180:  opts: BackoffOptions = DEFAULT_BACKOFF,
181:): Promise<{ deleted: number; batches: number }> {
182:  const batches = chunk(items.map(extractKey), 25);
183:  for (const batch of batches) {
184:    await deleteBatchWithRetry(batchWriteDelete, tableName, batch, opts);
185:  }
186:  return { deleted: items.length, batches: batches.length };
187:}
188:
189:/** Stable (sorted-key) JSON, so the same item always hashes to the same digest regardless of
190: * property insertion order. */
191:function sortKeysDeep(value: unknown): unknown {
192:  if (Array.isArray(value)) return value.map(sortKeysDeep);
193:  if (value && typeof value === "object") {
194:    return Object.keys(value as Record<string, unknown>)
195:      .sort()
196:      .reduce<Record<string, unknown>>((acc, k) => {
197:        acc[k] = sortKeysDeep((value as Record<string, unknown>)[k]);
198:        return acc;
199:      }, {});
200:  }
201:  return value;
202:}
203:
204:export function hashItem(item: unknown): string {
205:  return createHash("sha256").update(JSON.stringify(sortKeysDeep(item))).digest("hex");
206:}
207:
208:export interface ManifestEntry {
209:  source: string;
210:  itemCount: number;
211:  entityTypes: string[];
212:  hashes: string[];
213:}
214:
215:/** Redacted manifest entry ƒ?" counts/entityTypes/hashes only, NEVER a raw field value (this is
216: * what's allowed into the Git-tracked `docs/architecture/reviews/` folder; the raw snapshot with
217: * real PII/secrets stays in the gitignored `.local-artifacts/` path). */
218:export function buildManifestEntry(source: string, items: Array<Record<string, unknown>>): ManifestEntry {
219:  const entityTypes = Array.from(new Set(items.map((item) => String(item.entityType ?? "unknown")))).sort();
220:  return { source, itemCount: items.length, entityTypes, hashes: items.map(hashItem) };
221:}
222:
223:export function assertAllEmpty(counts: Record<string, number>): void {
224:  const nonZero = Object.entries(counts).filter(([, n]) => n > 0);
225:  if (nonZero.length > 0) {
226:    throw new Error(`reset-dev-data: FINAL VERIFICATION FAILED ƒ?" non-zero after Phase B: ${nonZero.map(([k, n]) => `${k}=${n}`).join(", ")}`);
227:  }
228:}
229:
230:// --- Real AWS wiring (not unit tested directly ƒ?" exercised manually via --dry-run against a
231:// real dev account, same convention as scripts/backfill-reminder-policies.ts's main()). Every
232:// pure/testable piece above takes plain data or an injected function, never an AWS SDK client
233:// directly, so test/unit/scripts/reset-dev-data.test.ts can cover it with fakes. -------------
234:
235:async function scanAll(client: ReturnType<typeof createDocumentClient>, tableName: string): Promise<Record<string, unknown>[]> {
236:  const items: Record<string, unknown>[] = [];
237:  let exclusiveStartKey: Record<string, unknown> | undefined;
238:  do {
239:    const result = await client.send(new ScanCommand({ TableName: tableName, ExclusiveStartKey: exclusiveStartKey }));
240:    items.push(...((result.Items ?? []) as Record<string, unknown>[]));
1:/**
2: * CloseOrganizationService ƒ?" W3-07 purge orchestrator (D-124, implementing D-121's approved
3: * design, Rodada 2 Fix 1 with Rodada 3 Fix 8's corrected step ordering), extended by D-127
4: * (quarantine/recovery window, `docs/architecture/reviews/quarantine-retention-scoping/
5: * estado-final-consolidado.md`).
6: *
7: * The single real trigger surface for tenant deletion: moves the tenant's `TenantLifecycleRecord`
8: * `ACTIVE -> HELD_FOR_RECOVERY` (was `ACTIVE -> DELETING` pre-D-127 ƒ?" the 30-day quarantine is now
9: * the first hop, not `DELETING`) and launches the SAME tenant-purge Step Functions execution that
10: * drives every remaining transition, now starting with a 30-day `Wait` before anything physical
11: * happens. Before D-124 this service existed, both `transitionTenantLifecycle` and
12: * `purgeTenant()` were real, working, and completely unreachable ƒ?" nothing in production ever
13: * called either.
14: *
15: * Step ordering is load-bearing and was corrected during the design protocol (Rodada 3 Fix 8): the
16: * terminal-state check runs BEFORE the unconditional `StartExecution`, not after. Rodada 2's
17: * original wording implied any post-`ACTIVE` status should trigger a fresh launch, which would
18: * have re-launched executions for VERIFIED/DELETED/BLOCKED/HELD tenants ƒ?" states where a purge
19: * execution is either finished or deliberately parked awaiting an operator.
20: *
21: * `StartExecution` IS called unconditionally for the genuinely in-flight states
22: * (`HELD_FOR_RECOVERY`/`DELETING`/`QUIESCING`/`PURGING`), including when this call performed no
23: * write at all. That is the established `start-extraction-run.ts` idiom, not an oversight: gating
24: * the launch on "did I just write the record" orphans the tenant forever whenever the DynamoDB
25: * transition commits but the subsequent `StartExecution` fails transiently ƒ?" the record would sit
26: * with no execution behind it, and every retry would skip the launch because the write already
27: * happened. Step Functions' own name-based uniqueness is what makes calling it every time safe ƒ?"
28: * D-127 changed the name from the bare `tenantId` to `${tenantId}-${closureAttemptId}` (see
29: * `tenant-lifecycle-record.ts`'s `closureAttemptId` field doc for why: a second close() after a
30: * REAL cancellation must never collide with the prior, by-then-stopped execution's name).
31: *
32: * `attachTenantPurgeExecutionArn` runs AFTER `StartExecution` succeeds, best-effort: a failure or
33: * a lost race there (the attempt already moved on) is swallowed, never surfaced to the HTTP
34: * caller ƒ?" the ARN is a repair/cancellation convenience (`CancelOrganizationClosureService`,
35: * the sweeper's reconciliation), not load-bearing for the purge pipeline itself, and every retry
36: * of `close()` naturally re-attempts it via the same unconditional-call idiom as `StartExecution`.
37: */
38:import { randomUUID } from "node:crypto";
39:import { authorize } from "../../identity/domain/authorization.js";
40:import type { RequestContext } from "../../identity/domain/request-context.js";
41:import { OrganizationClosureUnavailableError, NotFoundError } from "../../../shared/errors/app-error.js";
42:import {
43:  SystemMutationConflictError,
44:  transitionTenantLifecycle,
45:  attachTenantPurgeExecutionArn,
46:  type SystemMutationStore,
47:} from "../../../shared/tenant-lifecycle/system-mutation.js";
48:import type { TenantLifecycleRecord, TenantLifecycleStatus } from "../../../shared/tenant-lifecycle/tenant-lifecycle-record.js";
49:import type { TenantPurgeExecutionStarter } from "../../../shared/tenant-lifecycle/tenant-purge-execution-starter.js";
50:
51:const RECOVERY_WINDOW_MS = 30 * 24 * 60 * 60 * 1000;
52:
53:/** Same narrow read contract the orchestrator's transition handler uses ƒ?" `ConsistentRead: true`
54: * is mandatory, since this read produces the `expectedVersion` the OCC-fenced transition below is
55: * conditioned on. */
56:export interface TenantLifecycleReader {
57:  read(tenantId: string): Promise<TenantLifecycleRecord | undefined>;
58:}
59:
60:/** States where a purge is either already finished or parked awaiting a human ƒ?" closing is
61: * refused outright rather than re-launched. Named "unavailable", not "terminal": an operator can
62: * still remediate BLOCKED/HELD (`tenant-lifecycle-record.ts`), so these are not all true terminal
63: * states. */
64:const CLOSURE_UNAVAILABLE_STATUSES: ReadonlySet<TenantLifecycleStatus> = new Set(["VERIFIED", "DELETED", "BLOCKED", "HELD"]);
65:
66:/** The genuinely in-flight states ƒ?" a close() call landing here performs no write and only
67: * repeats the idempotent launch. D-127: `HELD_FOR_RECOVERY` joins this set (design finding 4 ƒ?"
68: * without it, a repeated close() after `ACTIVE -> HELD_FOR_RECOVERY` commits but `StartExecution`
69: * fails would fall into `CLOSURE_UNAVAILABLE_STATUSES`'s exhaustiveness guard below and
70: * permanently strand the tenant in quarantine with no execution driving it out). */
71:const IN_FLIGHT_STATUSES: ReadonlySet<TenantLifecycleStatus> = new Set(["HELD_FOR_RECOVERY", "DELETING", "QUIESCING", "PURGING"]);
72:
73:export interface CloseOrganizationResult {
74:  tenantId: string;
75:  status: TenantLifecycleStatus;
76:  /** False when the record was already HELD_FOR_RECOVERY+ and this call only repeated the launch. */
77:  transitioned: boolean;
78:}
79:
80:export class CloseOrganizationService {
81:  constructor(
82:    private readonly store: SystemMutationStore,
83:    private readonly reader: TenantLifecycleReader,
84:    private readonly executions: TenantPurgeExecutionStarter,
85:    private readonly tableName: string,
86:    private readonly now: () => string = () => new Date().toISOString(),
87:    private readonly newClosureAttemptId: () => string = randomUUID,
88:  ) {}
89:
90:  async close(ctx: RequestContext): Promise<CloseOrganizationResult> {
91:    const tenantId = ctx.tenant.tenantId;
92:    authorize({ context: ctx, action: "organization:close", resource: { tenantId } });
93:
94:    const record = await this.reader.read(tenantId);
95:    if (!record) {
96:      throw new NotFoundError("Organization lifecycle record not found.", { organizationId: tenantId });
97:    }
98:    if (CLOSURE_UNAVAILABLE_STATUSES.has(record.status)) {
99:      throw new OrganizationClosureUnavailableError(undefined, { organizationId: tenantId, status: record.status });
100:    }
101:
102:    let transitioned = false;
103:    let closureAttemptId = record.closureAttemptId;
104:    if (record.status === "ACTIVE") {
105:      closureAttemptId = this.newClosureAttemptId();
106:      const recoveryDeadline = new Date(Date.parse(this.now()) + RECOVERY_WINDOW_MS).toISOString();
107:      try {
108:        await transitionTenantLifecycle({
109:          store: this.store,
110:          tableName: this.tableName,
111:          tenantId,
112:          from: "ACTIVE",
113:          to: "HELD_FOR_RECOVERY",
114:          expectedVersion: record.version,
115:          recoveryDeadline,
116:          closureAttemptId,
117:          now: this.now,
118:        });
119:        transitioned = true;
120:      } catch (err) {
121:        if (!(err instanceof SystemMutationConflictError)) throw err;
122:        // A concurrent close() (double-click, retried request) won the race. Re-read once: if the
123:        // record legitimately landed on an in-flight state, this call simply joins that closure
124:        // and proceeds to the shared idempotent launch below (using the WINNING call's
125:        // closureAttemptId, never the one this call generated and lost the race with). Anything
126:        // else ƒ?" still ACTIVE, or raced into a stuck state ƒ?" is genuinely unexpected and is not
127:        // papered over.
128:        const after = await this.reader.read(tenantId);
129:        if (!after || !IN_FLIGHT_STATUSES.has(after.status)) throw err;
130:        closureAttemptId = after.closureAttemptId;
131:      }
132:    } else if (!IN_FLIGHT_STATUSES.has(record.status)) {
133:      // Exhaustiveness guard: ACTIVE, the 4 unavailable states, and the 4 in-flight states cover
134:      // every TenantLifecycleStatus. Reaching here means a status was added without deciding what
135:      // closing should do with it ƒ?" fail loudly rather than silently launching an execution.
136:      throw new OrganizationClosureUnavailableError(undefined, { organizationId: tenantId, status: record.status });
137:    }
138:
139:    if (!closureAttemptId) {
140:      // Unreachable in practice (every path above either just minted one or re-read a record that
141:      // must have one, since only ACTIVE -> HELD_FOR_RECOVERY ever creates the in-flight states
142:      // this branch is reached from) ƒ?" fail loudly rather than starting an execution with a name
143:      // that silently degrades to the pre-D-127 bare-tenantId shape.
144:      throw new OrganizationClosureUnavailableError(undefined, { organizationId: tenantId, status: record.status });
145:    }
146:
147:    const { executionArn } = await this.executions.startExecution({ name: `${tenantId}-${closureAttemptId}`, input: { tenantId } });
148:    // Best-effort, never surfaced to the caller ƒ?" see file header for why a lost race or a
149:    // transient failure here is safe to swallow.
150:    try {
151:      await attachTenantPurgeExecutionArn({ store: this.store, tableName: this.tableName, tenantId, closureAttemptId, executionArn, now: this.now });
152:    } catch {
153:      // Swallowed deliberately ƒ?" see file header. The next close() retry (or the sweeper) repairs it.
154:    }
155:
156:    return { tenantId, status: transitioned ? "HELD_FOR_RECOVERY" : record.status, transitioned };
157:  }
158:}
1:/**
2: * CancelOrganizationClosureService ƒ?" D-127 (quarantine/recovery window,
3: * `docs/architecture/reviews/quarantine-retention-scoping/estado-final-consolidado.md`, round-2
4: * Fix 3 "caminho de resoluÇõÇœo prÇüprio, nÇœo o RequestContext normal").
5: *
6: * The real cancellation path for a tenant closure still inside its 30-day `HELD_FOR_RECOVERY`
7: * window. Deliberately NOT built on `RequestContextResolver`/`resolveWorkingOrganization()` ƒ?" a
8: * tenant in `HELD_FOR_RECOVERY` is, by construction, not `ACTIVE`, and every normal resolution
9: * path in this codebase requires `TenantLifecycleRecord.status === ACTIVE`
10: * (`resolve-request-context.ts`). Building a `RequestContext` for this action is not merely
11: * inconvenient, it is structurally impossible on the normal path, so this service reads
12: * `IdentityMapping` -> `GlobalUser` -> `Membership` directly (the same three rows
13: * `resolveActiveMembership()` ultimately reads, just without the ACTIVE-lifecycle gate baked in)
14: * and calls `authorizeCancelClosure()` ƒ?" never `authorize()` ƒ?" for the same reason.
15: *
16: * `StopExecution` runs BEFORE any restoration write and is load-bearing: restoring `ACTIVE` first
17: * would let a `TenantBusinessMutation` land while the Step Functions execution is still alive and
18: * could advance `HELD_FOR_RECOVERY -> DELETING` (deadline firing) at any moment ƒ?" a genuine race
19: * between "tenant is usable again" and "tenant is about to be purged". Stopping the execution
20: * first removes that race entirely: once `StopExecution` is confirmed, nothing else can advance
21: * the lifecycle record for this attempt, so the OCC-fenced `HELD_FOR_RECOVERY -> ACTIVE` write
22: * that follows is racing nothing but a already-lost cause (see `close()` re-read handling below).
23: */
24:import { authorizeCancelClosure } from "../../identity/domain/authorization.js";
25:import { AuthenticationError, NotFoundError, OrganizationClosureUnavailableError } from "../../../shared/errors/app-error.js";
26:import {
27:  SystemMutationConflictError,
28:  transitionTenantLifecycle,
29:  type SystemMutationStore,
30:} from "../../../shared/tenant-lifecycle/system-mutation.js";
31:import type { TenantLifecycleRecord } from "../../../shared/tenant-lifecycle/tenant-lifecycle-record.js";
32:import type { TenantPurgeExecutionStopper } from "../../../shared/tenant-lifecycle/tenant-purge-execution-stopper.js";
33:import { identityMappingKey, type IdentityMapping } from "../../identity/persistence/identity-mapping-repository.js";
34:import { globalUserKey, type GlobalUser } from "../../identity/persistence/global-user-repository.js";
35:import { membershipKey } from "../domain/membership.js";
36:import type { Membership } from "../domain/membership.js";
37:
38:/** Strongly-consistent single-row reader ƒ?" the minimal surface this service needs from whatever
39: * physical store backs identity/organization rows (in production, the same DynamoDB table
40: * `IdentityStore`/`OrganizationStore` already point at). Kept as its own narrow port (rather than
41: * depending on either module's full store interface) so this service's dependency footprint is
42: * exactly what it reads, nothing more. */
43:export interface ConsistentKeyValueReader {
44:  get<T>(key: { PK: string; SK: string }): Promise<T | undefined>;
45:}
46:
47:export interface TenantLifecycleReader {
48:  read(tenantId: string): Promise<TenantLifecycleRecord | undefined>;
49:}
50:
51:export interface CancelOrganizationClosureInput {
52:  /** Cognito `sub` claim ƒ?" already signature/expiry-validated by the API Gateway authorizer,
53:   * same trust boundary `resolve-request-context.ts` documents for `ValidatedClaims.sub`. */
54:  cognitoSub: string;
55:  tenantId: string;
56:}
57:
58:export interface CancelOrganizationClosureResult {
59:  tenantId: string;
60:  status: "ACTIVE";
61:}
62:
63:export class CancelOrganizationClosureService {
64:  constructor(
65:    private readonly reader: ConsistentKeyValueReader,
66:    private readonly lifecycle: TenantLifecycleReader,
67:    private readonly store: SystemMutationStore,
68:    private readonly stopper: TenantPurgeExecutionStopper,
69:    private readonly tableName: string,
70:    private readonly now: () => string = () => new Date().toISOString(),
71:  ) {}
72:
73:  async cancel(input: CancelOrganizationClosureInput): Promise<CancelOrganizationClosureResult> {
74:    const { cognitoSub, tenantId } = input;
75:
76:    // 1. Dedicated identity resolution ƒ?" IdentityMapping -> GlobalUser -> Membership, never
77:    // RequestContextResolver (see file header).
78:    const mapping = await this.reader.get<IdentityMapping>(identityMappingKey(cognitoSub));
79:    if (!mapping) {
80:      throw new AuthenticationError("No identity mapping for this credential.");
81:    }
82:    const user = await this.reader.get<GlobalUser>(globalUserKey(mapping.userId));
83:    if (!user) {
84:      throw new AuthenticationError("GlobalUser missing for an existing IdentityMapping.", { userId: mapping.userId });
85:    }
86:    const membership = await this.reader.get<Membership>(membershipKey(tenantId, mapping.userId));
87:    if (!membership) {
88:      throw new NotFoundError("No membership for this user in this organization.", { organizationId: tenantId });
89:    }
90:
91:    // 2. Dedicated authorization primitive ƒ?" never authorize().
92:    authorizeCancelClosure({
93:      identityStatus: user.identityStatus,
94:      membershipStatus: membership.status,
95:      membershipRole: membership.role,
96:    });
97:
98:    // 3. Accept EXCLUSIVELY HELD_FOR_RECOVERY ƒ?" any other lifecycle status is refused outright,
99:    // never silently treated as "nothing to cancel" (which would hide a genuinely stale/expired
100:    // cancellation attempt from the caller).
101:    const record = await this.lifecycle.read(tenantId);
102:    if (!record) {
103:      throw new NotFoundError("Organization lifecycle record not found.", { organizationId: tenantId });
104:    }
105:    if (record.status !== "HELD_FOR_RECOVERY") {
106:      throw new OrganizationClosureUnavailableError(
107:        "This organization is not currently in a cancellable recovery window.",
108:        { organizationId: tenantId, status: record.status },
109:      );
110:    }
111:    if (!record.executionArn || !record.closureAttemptId) {
112:      // Genuinely unexpected: every ACTIVE -> HELD_FOR_RECOVERY transition stamps
113:      // closureAttemptId, and CloseOrganizationService's own unconditional StartExecution+attach
114:      // idiom means executionArn is only transiently absent (repaired by the next close() retry
115:      // or the sweeper) ƒ?" surfacing this as "unavailable" rather than guessing at a StopExecution
116:      // target is the fail-closed choice.
117:      throw new OrganizationClosureUnavailableError(
118:        "This organization's closure attempt has no execution attached yet - retry shortly.",
119:        { organizationId: tenantId, status: record.status },
120:      );
121:    }
122:
123:    // 4. StopExecution BEFORE any write ƒ?" see file header. A failure here fails the whole
124:    // cancellation (nothing changes, safe to retry) rather than ever restoring ACTIVE with the
125:    // execution potentially still alive.
126:    await this.stopper.stopExecution({ executionArn: record.executionArn });
127:
128:    // 5. Only now, OCC-fenced against the exact version read above: if the deadline fired (or
129:    // anything else moved the record) between steps 3 and 5, this fails with
130:    // SystemMutationConflictError instead of silently reporting success over a tenant that is
131:    // actually already advancing toward DELETING.
132:    try {
133:      await transitionTenantLifecycle({
134:        store: this.store,
135:        tableName: this.tableName,
136:        tenantId,
137:        from: "HELD_FOR_RECOVERY",
138:        to: "ACTIVE",
139:        expectedVersion: record.version,
140:        now: this.now,
141:      });
142:    } catch (err) {
143:      if (err instanceof SystemMutationConflictError) {
144:        throw new OrganizationClosureUnavailableError(
145:          "The recovery window closed (or was already cancelled) before this request completed - the execution was stopped, but the organization could not be reactivated.",
146:          { organizationId: tenantId },
147:        );
148:      }
149:      throw err;
150:    }
151:
152:    return { tenantId, status: "ACTIVE" };
153:  }
154:}

2026-09-08T02:46:40.104462Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 93 column 5
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$scores=@(@(18,8.8),@(16,9.0),@(13,6.5),@(15,9.2),@(10,6.5),@(10,4.0),@(8,9.0)); ($scores | ForEach-Object { $_[0]*$_[1] } | Measure-Object -Sum).Sum / 90; git branch --show-current; git status --short' in C:\Users\Usuario\Desktop\projects\expiration-tracker
 succeeded in 1504ms:
7,79888888888889
develop
 M docs/engineering/decisions-log.md
 M docs/engineering/exceptions.md
 M docs/engineering/third-party-inventory.md
?? docs/engineering/reviews/full-audit-round2-contexto-claude.md
?? docs/engineering/reviews/full-audit-round2-contexto-codex-output-round1.txt
?? docs/engineering/reviews/full-audit-round2-contexto-codex-prompt.txt
?? docs/engineering/reviews/full-audit-round2-governanca-ia-claude.md
?? docs/engineering/reviews/full-audit-round2-governanca-ia-codex-output-round1.txt
?? docs/engineering/reviews/full-audit-round2-governanca-ia-codex-prompt.txt
?? docs/engineering/reviews/full-audit-round2-juridico-claude.md
?? docs/engineering/reviews/full-audit-round2-juridico-codex-output-round1.txt
?? docs/engineering/reviews/full-audit-round2-juridico-codex-prompt.txt
?? docs/engineering/reviews/full-audit-round2-privacidade-claude.md
?? docs/engineering/reviews/full-audit-round2-privacidade-codex-output-round1.txt
?? docs/engineering/reviews/full-audit-round2-privacidade-codex-prompt.txt
?? docs/engineering/reviews/full-audit-round2-privacidade-summary.md
?? docs/engineering/reviews/full-audit-round2-produto-multitenant-claude.md
?? docs/engineering/reviews/full-audit-round2-produto-multitenant-codex-output-round1.txt
?? docs/engineering/reviews/full-audit-round2-produto-multitenant-codex-prompt-round1.txt
?? docs/engineering/reviews/full-audit-round2-seguranca-claude.md
?? docs/engineering/reviews/full-audit-round2-seguranca-codex-output-round1.txt
?? docs/engineering/reviews/full-audit-round2-seguranca-codex-prompt.txt
?? src/modules/expiration/application/__tenant_fence_boundary_test_fixtures__/
warning: unable to access 'C:\Users\Usuario/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\Usuario/.config/git/ignore': Permission denied

2026-09-08T02:46:53.668438Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 93 column 5
codex
## Nota da Rodada 1 (Codex)

**Nota final ponderada: 7,8/10 — abaixo do gate de 9,0.**

Avaliação independente e cega: não li o parecer nem a nota do Claude. A numeração “Rodada 1” segue o formato solicitado, embora os artefatos usem `round2`.

### Resultado por critério

| # | Critério | Peso aplicável | Nota | Fundamentação |
|---:|---|---:|---:|---|
| 1 | Lifecycle Automatizado de Tenant | 18 | **8,8** | Organizations, Memberships, convites, troca de organização, último OWNER e estados de encerramento são reais. `organization:close` executa autorização OWNER e transição OCC `ACTIVE → HELD_FOR_RECOVERY` ([close-organization.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/organization/application/close-organization.ts:90)). O cancelamento tem resolução própria de identidade/membership e autorização OWNER ([cancel-organization-closure.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/organization/application/cancel-organization-closure.ts:73)). Perde pontos por ausência de control plane de planos/billing e pelo drift operacional do reset de `dev`. |
| 2 | Offboarding, Exportação e Destruição Criptográfica | 16 | **9,0** | Encerramento, janela de recuperação, Step Functions, purge por classes de dado, exports CSV/dossier e recuperação estão implementados e extensamente testados. A destruição é operacionalmente forte, embora a evidência examinada seja principalmente purge físico/retention; não encontrei prova equivalente de crypto-shredding tenant-specific por chave dedicada. |
| 3 | Planos, Entitlements, Quotas e Fairness | 13 | **6,5** | Existem `TenantQuota`, token buckets, quotas específicas e compensação de reservas. Entretanto, billing/provider e administração comercial de entitlements continuam deliberadamente fora do produto. Há proteção de consumo, mas não uma oferta comercial completa e governada por planos. |
| 4 | Correção de Lembretes e Proteção do Usuário | 15 | **9,2** | Há tratamento real de timezone/DST, quiet hours, opt-out, staleness, replacement/corrective intents, leases, reconciliação e falha ambígua sem retry cego. Por exemplo, ocorrências DST são materializadas com ponteiro explícito para reconciliação ([reminder-materializer.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:162)); falha ambígua de e-mail resulta em `UNKNOWN`, não reenvio automático ([ses-email-adapter.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/providers/ses-email-adapter.ts:10)). |
| 5 | Transparência, Usabilidade e Acessibilidade | — | **N/A** | Excluído conforme instrução, por ausência de escopo UX nesta auditoria. |
| 6 | Administração, Suporte e Operação pelo Tenant | 10 | **6,5** | Administração interna do tenant evoluiu bastante: RBAC, activity log, exports, configuração e encerramento. Porém não existe papel/capability formal de suporte da plataforma cross-tenant; D-052 registra explicitamente que toda `Action` ainda é interna ao tenant. Diagnóstico operacional depende de logs/console AWS, não de uma superfície auditável de suporte delegada. |
| 7 | Métricas de Valor, Consumo e Economia Unitária | 10 | **4,0** | Há `tenantId` em logs e telemetria de quotas, mas não encontrei atribuição runtime completa de custo/valor por tenant. O documento ainda afirma que `TenantQuota` apenas permitirá medir consumo real “no futuro” ([cost-model.md](/C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/cost-model.md:43)). Hoje predominam estimativas agregadas e budgets por ambiente. |
| 8 | Evolução Unificada e Controle de Customização | 8 | **9,0** | Não encontrei fork de código/configuração por tenant. Variações passam por RBAC, quotas, feature flags e estruturas compartilhadas. A matriz de autorização e os namespaces IAM/GSI8 são fechados e versionados, embora ainda não exista um catálogo comercial completo de entitlements. |

Cálculo normalizado sobre os 90 pontos aplicáveis:

`(18×8,8 + 16×9,0 + 13×6,5 + 15×9,2 + 10×6,5 + 10×4,0 + 8×9,0) / 90 = 7,798… → 7,8`

### Pergunta central: rota nova sem RBAC ou tenant-fencing

**Não encontrei rota ou ação de negócio pós-D-140 autenticada que esteja comprovadamente sem RBAC ou sem tenant-fencing.**

A cobertura observada inclui:

- Document Archive: criação, leitura, upload, review, requirements, séries, tipos, templates, metadata e dossier export chamam `authorize()` nos serviços. Exemplos: criação de request avulso ([document-archive-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/document-archive-service.ts:1074)), dossier export ([document-archive-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/document-archive-service.ts:1264)) e alteração do destinatário da série ([document-request-recurrence-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/document-request-recurrence-service.ts:236)).
- Scheduled reports usam `reports:subscription-manage` ([report-subscription-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reports/application/report-subscription-service.ts:69)).
- Activity log usa `activity:read` ([activity-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/activity/application/activity-service.ts:112)).
- WhatsApp opt-in reutiliza a ação autenticada `notification:configure` ([whatsapp-opt-in-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/whatsapp-opt-in-service.ts:43)).
- Guest upload não usa RBAC de membership por design: usa credential/session tokens próprios. O webhook público do WhatsApp também é uma integração provider-authenticated, não uma ação humana de tenant.

`ACTION_ROLES: Record<Action, ReadonlySet<Role>>` ([authorization.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/domain/authorization.ts:231)) é prova suficiente apenas de **completude da matriz para ações já declaradas**: adicionar um membro a `Action` sem entrada correspondente quebra o typecheck. Não prova que:

1. uma rota chame `authorize()`;
2. a rota escolha a ação correta;
3. o `resource.tenantId` venha de dado autoritativo;
4. uma nova operação não reutilize indevidamente uma ação permissiva existente.

Além disso, muitos serviços passam `{ tenantId: ctx.tenant.tenantId }` ao próprio `authorize()`. Isso torna o teste interno de igualdade tautológico; o fencing efetivo depende de repositories/chaves construídos com o mesmo tenant autenticado. A função realmente rejeita mismatch quando recebe recurso real divergente ([authorization.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/domain/authorization.ts:342)), mas a garantia integral continua sendo composta por autorização, construção de chave e transaction fences.

### Achados

#### MÉDIO — Drift real em `reset-dev-data.ts`

O script afirma inventariar e purgar “todas as 24 filas”, mas sua allowlist contém somente 12 nomes-base antigos, totalizando 24 com DLQs ([reset-dev-data.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/scripts/reset-dev-data.ts:8), [reset-dev-data.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/scripts/reset-dev-data.ts:59)). O sistema atual possui filas posteriores, como WhatsApp, scheduled reports, recurrence e workers adicionais.

Consequência: um novo cutover/reset pode terminar “verde” enquanto deixa mensagens nas filas novas. A exclusão DynamoDB é schema-agnostic e não aparenta drift de entidades, mas o inventário/purge de infraestrutura já não representa o sistema atual.

#### MÉDIO — Economia unitária ainda não é observabilidade runtime por tenant

O modelo calcula custo médio estimado e diz expressamente que `TenantQuota` permitirá medir consumo real posteriormente. Logs com `tenantId` ajudam investigação, mas não equivalem a métricas de valor, consumo e custo atribuídas por tenant, reconciliadas com AWS Cost and Usage Report ou mecanismo equivalente.

#### MÉDIO — Suporte cross-tenant permanece sem capability formal

Não há identidade de “platform support”, grants temporários, reason code ou trilha específica de impersonation/access. Isso evita acesso informal por endpoint, o que é positivo, mas não atende plenamente ao critério de suporte autorizado capaz de diagnosticar um tenant sem recorrer diretamente à infraestrutura.

#### BAIXO — `ACTION_ROLES` não constitui prova estrutural completa do fencing

A matriz fechada impede omissão de entrada, mas não obriga um handler/service a chamar `authorize()`. Não encontrei bypass real nas rotas revisadas; o achado é uma lacuna de garantia arquitetural, não uma vulnerabilidade confirmada.

#### BAIXO — Crypto-shredding não ficou demonstrado no mesmo nível do purge

O lifecycle e a exclusão física estão implementados e testados. Não encontrei, nas fontes inspecionadas, uma chave criptográfica dedicada por tenant cuja destruição torne backups/cópias irrecuperáveis. Portanto, o critério 2 alcança o gate por offboarding operacional, não por prova completa de destruição criptográfica.

### Isolamento e histórico cross-tenant

O isolamento não está apoiado apenas em convenção de `PK/tenantId`:

- Há fences transacionais de lifecycle e boundary tests.
- Terraform possui `gsi8_worker_isolation`, com matriz de namespaces dos workers.
- D-181 e fatias seguintes registram execução real de `aws iam simulate-principal-policy`: namespace próprio `allowed`, namespace alheio `implicitDeny`.
- GSI3/GSI6 e GSI8 possuem políticas separadas e testes de isolamento.

Na busca por `cross-tenant`, `vazamento`, `leak` e `IDOR`, não encontrei evidência de vazamento real de conteúdo entre tenants historicamente aberto. As ocorrências relevantes são:

- provas positivas/testes de isolamento;
- vazamentos de quota/idempotency, isto é, consumo contábil preso, não exposição de dados cross-tenant;
- uma limitação histórica da lane para chaves não prefixadas por `TENANT#`, posteriormente delimitada por análise de call sites, sem exploração/caminho real identificado.

Portanto: **NENHUM VAZAMENTO CROSS-TENANT REAL FOI CONFIRMADO NESTA AUDITORIA**, e **NENHUMA ROTA DE NEGÓCIO NOVA SEM RBAC/FENCING REAL FOI CONFIRMADA**.

### Lifecycle D-124/D-127

`organization:close`, `organization:cancel-close` e `HELD_FOR_RECOVERY` são estados e caminhos reais, não apenas declarações:

- transições permitidas e proibidas são testadas em [tenant-lifecycle.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/tenant-lifecycle.test.ts:17);
- a mutação real `ACTIVE → HELD_FOR_RECOVERY` é testada em [system-mutation.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/system-mutation.test.ts:31);
- fechamento orquestrado é testado em [close-organization.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/organization/close-organization.test.ts:56);
- cancelamento, OWNER-only, corridas e estados inválidos são testados em [cancel-organization-closure.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/organization/cancel-organization-closure.test.ts:93).

### Limite da verificação

Executei comandos locais read-only de inspeção (`rg`, leitura de arquivos, branch/status e cálculo da nota). **Não executei AWS CLI, `simulate-principal-policy`, Terraform, testes ou deploy nesta rodada.** As afirmações sobre verificações ao vivo anteriores derivam dos registros do `decisions-log.md`, não de repetição independente contra a AWS nesta auditoria. O branch observado foi `develop`; o worktree já continha mudanças concorrentes, que não alterei.
tokens used
100.080
## Nota da Rodada 1 (Codex)

**Nota final ponderada: 7,8/10 — abaixo do gate de 9,0.**

Avaliação independente e cega: não li o parecer nem a nota do Claude. A numeração “Rodada 1” segue o formato solicitado, embora os artefatos usem `round2`.

### Resultado por critério

| # | Critério | Peso aplicável | Nota | Fundamentação |
|---:|---|---:|---:|---|
| 1 | Lifecycle Automatizado de Tenant | 18 | **8,8** | Organizations, Memberships, convites, troca de organização, último OWNER e estados de encerramento são reais. `organization:close` executa autorização OWNER e transição OCC `ACTIVE → HELD_FOR_RECOVERY` ([close-organization.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/organization/application/close-organization.ts:90)). O cancelamento tem resolução própria de identidade/membership e autorização OWNER ([cancel-organization-closure.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/organization/application/cancel-organization-closure.ts:73)). Perde pontos por ausência de control plane de planos/billing e pelo drift operacional do reset de `dev`. |
| 2 | Offboarding, Exportação e Destruição Criptográfica | 16 | **9,0** | Encerramento, janela de recuperação, Step Functions, purge por classes de dado, exports CSV/dossier e recuperação estão implementados e extensamente testados. A destruição é operacionalmente forte, embora a evidência examinada seja principalmente purge físico/retention; não encontrei prova equivalente de crypto-shredding tenant-specific por chave dedicada. |
| 3 | Planos, Entitlements, Quotas e Fairness | 13 | **6,5** | Existem `TenantQuota`, token buckets, quotas específicas e compensação de reservas. Entretanto, billing/provider e administração comercial de entitlements continuam deliberadamente fora do produto. Há proteção de consumo, mas não uma oferta comercial completa e governada por planos. |
| 4 | Correção de Lembretes e Proteção do Usuário | 15 | **9,2** | Há tratamento real de timezone/DST, quiet hours, opt-out, staleness, replacement/corrective intents, leases, reconciliação e falha ambígua sem retry cego. Por exemplo, ocorrências DST são materializadas com ponteiro explícito para reconciliação ([reminder-materializer.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reminder/application/reminder-materializer.ts:162)); falha ambígua de e-mail resulta em `UNKNOWN`, não reenvio automático ([ses-email-adapter.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/providers/ses-email-adapter.ts:10)). |
| 5 | Transparência, Usabilidade e Acessibilidade | — | **N/A** | Excluído conforme instrução, por ausência de escopo UX nesta auditoria. |
| 6 | Administração, Suporte e Operação pelo Tenant | 10 | **6,5** | Administração interna do tenant evoluiu bastante: RBAC, activity log, exports, configuração e encerramento. Porém não existe papel/capability formal de suporte da plataforma cross-tenant; D-052 registra explicitamente que toda `Action` ainda é interna ao tenant. Diagnóstico operacional depende de logs/console AWS, não de uma superfície auditável de suporte delegada. |
| 7 | Métricas de Valor, Consumo e Economia Unitária | 10 | **4,0** | Há `tenantId` em logs e telemetria de quotas, mas não encontrei atribuição runtime completa de custo/valor por tenant. O documento ainda afirma que `TenantQuota` apenas permitirá medir consumo real “no futuro” ([cost-model.md](/C:/Users/Usuario/Desktop/projects/expiration-tracker/docs/architecture/cost-model.md:43)). Hoje predominam estimativas agregadas e budgets por ambiente. |
| 8 | Evolução Unificada e Controle de Customização | 8 | **9,0** | Não encontrei fork de código/configuração por tenant. Variações passam por RBAC, quotas, feature flags e estruturas compartilhadas. A matriz de autorização e os namespaces IAM/GSI8 são fechados e versionados, embora ainda não exista um catálogo comercial completo de entitlements. |

Cálculo normalizado sobre os 90 pontos aplicáveis:

`(18×8,8 + 16×9,0 + 13×6,5 + 15×9,2 + 10×6,5 + 10×4,0 + 8×9,0) / 90 = 7,798… → 7,8`

### Pergunta central: rota nova sem RBAC ou tenant-fencing

**Não encontrei rota ou ação de negócio pós-D-140 autenticada que esteja comprovadamente sem RBAC ou sem tenant-fencing.**

A cobertura observada inclui:

- Document Archive: criação, leitura, upload, review, requirements, séries, tipos, templates, metadata e dossier export chamam `authorize()` nos serviços. Exemplos: criação de request avulso ([document-archive-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/document-archive-service.ts:1074)), dossier export ([document-archive-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/document-archive-service.ts:1264)) e alteração do destinatário da série ([document-request-recurrence-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/document-archive/application/document-request-recurrence-service.ts:236)).
- Scheduled reports usam `reports:subscription-manage` ([report-subscription-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/reports/application/report-subscription-service.ts:69)).
- Activity log usa `activity:read` ([activity-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/activity/application/activity-service.ts:112)).
- WhatsApp opt-in reutiliza a ação autenticada `notification:configure` ([whatsapp-opt-in-service.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/notification/application/whatsapp-opt-in-service.ts:43)).
- Guest upload não usa RBAC de membership por design: usa credential/session tokens próprios. O webhook público do WhatsApp também é uma integração provider-authenticated, não uma ação humana de tenant.

`ACTION_ROLES: Record<Action, ReadonlySet<Role>>` ([authorization.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/domain/authorization.ts:231)) é prova suficiente apenas de **completude da matriz para ações já declaradas**: adicionar um membro a `Action` sem entrada correspondente quebra o typecheck. Não prova que:

1. uma rota chame `authorize()`;
2. a rota escolha a ação correta;
3. o `resource.tenantId` venha de dado autoritativo;
4. uma nova operação não reutilize indevidamente uma ação permissiva existente.

Além disso, muitos serviços passam `{ tenantId: ctx.tenant.tenantId }` ao próprio `authorize()`. Isso torna o teste interno de igualdade tautológico; o fencing efetivo depende de repositories/chaves construídos com o mesmo tenant autenticado. A função realmente rejeita mismatch quando recebe recurso real divergente ([authorization.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/src/modules/identity/domain/authorization.ts:342)), mas a garantia integral continua sendo composta por autorização, construção de chave e transaction fences.

### Achados

#### MÉDIO — Drift real em `reset-dev-data.ts`

O script afirma inventariar e purgar “todas as 24 filas”, mas sua allowlist contém somente 12 nomes-base antigos, totalizando 24 com DLQs ([reset-dev-data.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/scripts/reset-dev-data.ts:8), [reset-dev-data.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/scripts/reset-dev-data.ts:59)). O sistema atual possui filas posteriores, como WhatsApp, scheduled reports, recurrence e workers adicionais.

Consequência: um novo cutover/reset pode terminar “verde” enquanto deixa mensagens nas filas novas. A exclusão DynamoDB é schema-agnostic e não aparenta drift de entidades, mas o inventário/purge de infraestrutura já não representa o sistema atual.

#### MÉDIO — Economia unitária ainda não é observabilidade runtime por tenant

O modelo calcula custo médio estimado e diz expressamente que `TenantQuota` permitirá medir consumo real posteriormente. Logs com `tenantId` ajudam investigação, mas não equivalem a métricas de valor, consumo e custo atribuídas por tenant, reconciliadas com AWS Cost and Usage Report ou mecanismo equivalente.

#### MÉDIO — Suporte cross-tenant permanece sem capability formal

Não há identidade de “platform support”, grants temporários, reason code ou trilha específica de impersonation/access. Isso evita acesso informal por endpoint, o que é positivo, mas não atende plenamente ao critério de suporte autorizado capaz de diagnosticar um tenant sem recorrer diretamente à infraestrutura.

#### BAIXO — `ACTION_ROLES` não constitui prova estrutural completa do fencing

A matriz fechada impede omissão de entrada, mas não obriga um handler/service a chamar `authorize()`. Não encontrei bypass real nas rotas revisadas; o achado é uma lacuna de garantia arquitetural, não uma vulnerabilidade confirmada.

#### BAIXO — Crypto-shredding não ficou demonstrado no mesmo nível do purge

O lifecycle e a exclusão física estão implementados e testados. Não encontrei, nas fontes inspecionadas, uma chave criptográfica dedicada por tenant cuja destruição torne backups/cópias irrecuperáveis. Portanto, o critério 2 alcança o gate por offboarding operacional, não por prova completa de destruição criptográfica.

### Isolamento e histórico cross-tenant

O isolamento não está apoiado apenas em convenção de `PK/tenantId`:

- Há fences transacionais de lifecycle e boundary tests.
- Terraform possui `gsi8_worker_isolation`, com matriz de namespaces dos workers.
- D-181 e fatias seguintes registram execução real de `aws iam simulate-principal-policy`: namespace próprio `allowed`, namespace alheio `implicitDeny`.
- GSI3/GSI6 e GSI8 possuem políticas separadas e testes de isolamento.

Na busca por `cross-tenant`, `vazamento`, `leak` e `IDOR`, não encontrei evidência de vazamento real de conteúdo entre tenants historicamente aberto. As ocorrências relevantes são:

- provas positivas/testes de isolamento;
- vazamentos de quota/idempotency, isto é, consumo contábil preso, não exposição de dados cross-tenant;
- uma limitação histórica da lane para chaves não prefixadas por `TENANT#`, posteriormente delimitada por análise de call sites, sem exploração/caminho real identificado.

Portanto: **NENHUM VAZAMENTO CROSS-TENANT REAL FOI CONFIRMADO NESTA AUDITORIA**, e **NENHUMA ROTA DE NEGÓCIO NOVA SEM RBAC/FENCING REAL FOI CONFIRMADA**.

### Lifecycle D-124/D-127

`organization:close`, `organization:cancel-close` e `HELD_FOR_RECOVERY` são estados e caminhos reais, não apenas declarações:

- transições permitidas e proibidas são testadas em [tenant-lifecycle.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/tenant-lifecycle.test.ts:17);
- a mutação real `ACTIVE → HELD_FOR_RECOVERY` é testada em [system-mutation.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/system-mutation.test.ts:31);
- fechamento orquestrado é testado em [close-organization.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/organization/close-organization.test.ts:56);
- cancelamento, OWNER-only, corridas e estados inválidos são testados em [cancel-organization-closure.test.ts](/C:/Users/Usuario/Desktop/projects/expiration-tracker/test/unit/organization/cancel-organization-closure.test.ts:93).

### Limite da verificação

Executei comandos locais read-only de inspeção (`rg`, leitura de arquivos, branch/status e cálculo da nota). **Não executei AWS CLI, `simulate-principal-policy`, Terraform, testes ou deploy nesta rodada.** As afirmações sobre verificações ao vivo anteriores derivam dos registros do `decisions-log.md`, não de repetição independente contra a AWS nesta auditoria. O branch observado foi `develop`; o worktree já continha mudanças concorrentes, que não alterei.
