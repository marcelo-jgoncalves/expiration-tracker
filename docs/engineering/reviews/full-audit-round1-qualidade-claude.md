---
status: round1-blind
owner: claude
authority: audit-record (referencia docs/engineering/joint-review-criteria.md; nao redefine pesos)
---

# Full-audit round1 — Eixo Qualidade de Engenharia — nota cega Claude (rodada 1)

Nota registrada ANTES de rodar/ver Codex, por `AGENTS.md` §4. Evidência real: `npm run typecheck` (limpo), `npm test` (152/152, 22 arquivos), `npm run lint` (limpo, `--max-warnings=0`), `npm run check-boundaries` (0 violações, 132 módulos/350 deps), `npm audit --omit=dev --audit-level=high` (0 vulns), `npm run validate-schemas` (ok). Todos executados nesta sessão, não assumidos de sessão anterior.

| # | Critério | Peso | Nota | Evidência |
|---:|---|---:|---:|---|
| 1 | Code Correctness & Defensive Design | 11% | 8.3 | `AppError` taxonomy (`src/shared/errors/app-error.ts`) com `retryable` central; builders OCC (`src/shared/dynamodb/occ.ts`) evitam `UpdateItem`/`PutItem` cru; `noUncheckedIndexedAccess` ligado (`tsconfig.json:8`). Achado real recente corrigido em axis Arquitetura (ponteiro órfão GSI3) mostra que a disciplina falha silenciosamente às vezes — não pontua 9+ sem mais evidência de mutation/property-based testing contra invariantes de estado. |
| 2 | Test Effectiveness & Coverage Discipline | 15% | 7.8 | 152 testes reais em 22 arquivos cobrindo unit/contract/integration/infra (CDK synth real via assertions), incluindo negativo (`cross-tenant.test.ts`) e failure path (`reconciliation.test.ts`, `producer.test.ts` per decisions-log E-007). Mas: nenhuma prova contra AWS real (DynamoDB Local/Testcontainers existe em `test/integration-dynamodb` mas não rodou nesta sessão — não invocado por `npm test`, só `npm run test:dynamodb`, e não faz parte do `guardrails` do CI, só de um job `dynamodb-integration` separado); sem coverage report gerado/gate (`test:coverage` existe mas não é rodado em CI, sem threshold enforced). Critério exige "não apenas contagem" — aqui a suíte é majoritariamente fakes/in-memory, propriedades reais de DynamoDB/IAM só parcialmente provadas (CDK assertions, não deploy). |
| 3 | CI Quality Gates & Merge Safety | 11% | 8.5 | `.github/workflows/ci.yml`: typecheck+lint+boundaries+schemas+test+audit(prod blocking)+SBOM, todos no job `guardrails`, `permissions: contents: read` mínimo, `persist-credentials: false`, actions pinadas por SHA com comentário do tag. Falta: nenhum branch protection automatizado confirmado (decisions-log E-005 registra isso como decisão consciente de não aplicar), e o dev-audit é só `|| echo warning` — bypass informal formalmente documentado (`exceptions.md` EX-001), não puramente "sem bypass". |
| 4 | Type Safety, Static Analysis & Automated Enforcement | 9% | 8.7 | `strict: true` + `noUncheckedIndexedAccess` + `noImplicitOverride`; ESLint com `no-console` erro fora de observability; `dependency-cruiser` com 3 regras reais de boundary rodando sobre grafo real (não só texto de import, decisions-log E-008 documenta bug real do ESLint que foi corrigido por isso). Mas dependency-cruiser é deliberadamente estreito (3 regras, só domain-isolation) — não há fitness function customizada além disso (ex.: nada impede um novo módulo de reinventar padrão OCC). |
| 5 | Readability, Consistency & Implementation Maintainability | 8% | 8.5 | Padrão de módulo consistente (`domain/application/ports/http/persistence`) repetido em identity/expiration/reminder; nomes claros; comentários de cabeçalho de arquivo explicam decisão, não só o quê. Sem achado concreto de duplicação/complexidade nesta leitura, mas não há linter de complexidade ciclomática nem detector de duplicação automatizado — julgamento manual apenas. |
| 6 | Delivery, Release & Recovery Discipline | 11% | 5.5 | Nenhum deploy real jamais executado (mesmo estado que axis Arquitetura documentou). Achado NOVO nesta sessão: `test/infra/stack.test.ts` (CDK synth real) emite warning real do CDK — `Runtime 'nodejs20.x' was deprecated on 2026-04-30. Creation was disabled on 2027-02-01 and update on 2027-03-03` — todas as 8 Lambdas (`infra/lib/expiration-tracker-stack.ts:52,64,72,90,101,133,147,167`) usam `lambda.Runtime.NODEJS_20_X`, hoje (2026-08-19) já depois da data de deprecação e apenas ~5 meses antes do bloqueio de update. Isso é evidência concreta e corrigível sem deploy real (é config CDK) de que a disciplina de "artefato/ambiente reproduzível" tem drift real não pego por nenhum gate automatizado — nada no CI falha por isso hoje. Peso alto deste critério (11%, justificado no próprio doc de critérios pela lacuna real de deploy) + achado novo real → nota baixa. |
| 7 | Dependency & Supply-Chain Hygiene | 7% | 8.6 | `npm ci` imutável, `.nvmrc` fixo, lockfile presente, SBOM CycloneDX gerado no CI, actions pinadas por SHA. `exceptions.md` EX-001 é exatamente o padrão exigido pelo critério (`expiraEm`, owner, tentativa de correção documentada com motivo técnico real de falha, compensating control). Único ponto fraco: owner "a definir formalmente" (não é uma pessoa/papel concreto ainda). |
| 8 | Debuggability & Operational Feedback | 7% | 7.5 | `SecureLogger`+`Redactor` centralizados, `no-console` enforced, `correlationId`/`tenantId` no contexto. Mas (decisions-log E-007) workers de lógica pura são deliberadamente "observability-agnostic" — nenhum logging real ainda existe nos workers/handlers Lambda reais para provar formato de evento contra CloudWatch; é decisão consciente e documentada, mas significa que "suficiente para investigar falhas assíncronas" (definição do critério) ainda não tem prova real, só teste unitário. |
| 9 | Developer Experience & Reproducibility | 5% | 9.0 | Checkout limpo → `npm ci` → 5 comandos padronizados e documentados no `package.json`/`AGENTS.md` §7, todos rodados nesta sessão sem fricção, sem segredo/config manual necessário. Único atrito: `npm test` sozinho levou quase 3 minutos nesta máquina (CDK synth pesado dentro de `test/infra/stack.test.ts`), sem indicação prévia disso em nenhum doc. |
| 10 | Documentation Quality & Process Discipline | 5% | 8.8 | `AGENTS.md` conciso (dentro do próprio limite de 60-100 linhas que se impõe), `decisions-log.md` com IDs sequenciais e motivo real, `exceptions.md` com formato rigoroso. Processo proporcional a projeto solo, sem sobre-formalização. |
| 11 | Documentation–Implementation Drift Control | 6% | 6.0 | Mecanismo determinístico real existe só para: schema (`validate-schemas`), boundary de módulo (`dependency-cruiser`), tipos (`tsc`). Não existe nenhum check automatizado que teria pego o drift real encontrado no critério 6 (runtime Lambda deprecado) antes de rodar `npm test` manualmente e ler o stderr — não está no `guardrails` do CI como falha, só aparece como warning textual dentro da saída de teste. Nenhum link-checker ou verificador de doc↔código citado em `AGENTS.md` §6 está implementado ("Não há automação disso ainda" — texto do próprio arquivo). |
| 12 | Technical-Debt & Continuous-Improvement Practice | 5% | 8.3 | `exceptions.md` + `decisions-log.md` registram atalhos com dono/trigger/prazo de forma honesta (EX-001 documenta até uma tentativa de correção que falhou e por quê). Falta: nenhum registro formal de débito para o runtime Lambda deprecado (achado desta sessão) nem para a ausência de coverage gate — não há um único lugar que agregue "dívida conhecida" fora desses dois arquivos, e nem tudo que deveria estar lá está (ex. achado 6 acima, novo, ainda não registrado antes desta auditoria). |

## Nota ponderada (rodada 1, Claude)

(8.3×11 + 7.8×15 + 8.5×11 + 8.7×9 + 8.5×8 + 5.5×11 + 8.6×7 + 7.5×7 + 9.0×5 + 8.8×5 + 6.0×6 + 8.3×5) / 100

= (91.3 + 117.0 + 93.5 + 78.3 + 68.0 + 60.5 + 60.2 + 52.5 + 45.0 + 44.0 + 36.0 + 41.5) / 100
= 787.8 / 100 = **7.878**

Achado mais acionável para correção pontual nesta sessão (sem depender de deploy real): upgrade `lambda.Runtime.NODEJS_20_X` → `NODEJS_24_X` (ou mínimo suportado não-deprecado) em `infra/lib/expiration-tracker-stack.ts`, e `.nvmrc`/`package.json engines` alinhados — corrige achado real de Delivery/Drift sem exigir AWS real (CDK synth já roda em `test/infra/stack.test.ts`).
