---
status: draft
owner: claude
authority: audit-record
---

# Full-audit round1 — Eixo Engenharia de Contexto — nota cega Claude (round1)

Protocolo `AGENTS.md` §4 contra os 9 critérios de `docs/engineering/joint-review-criteria.md` ("Eixo: Engenharia de contexto"). Nota cega, escrita antes de ver qualquer saída do Codex. Avaliação contra o estado real do repositório na branch `develop`, já incluindo 3 correções pontuais aplicadas nesta mesma sessão, antes desta nota (commit `48aad11`): (a) `docs/project/working-memory.md` e `docs/engineering/03-repository-baseline.md` citavam `AGENTS.md` §3 para a regra de invocação do Codex/crases, quando esse conteúdo vive em §4 desde que §3 virou "Estratégia de branch" — referência cruzada quebrada, não achada pelas auditorias anteriores (arquitetura/qualidade); (b) `docs/architecture/README.md` não indexava o diretório `docs/architecture/reviews/` (contém `m3.5-runtime-design/`, referenciado por `decisions-log.md` e pela própria linha de índice de `m3.5-runtime-design.md`) — diretório órfão não listado.

## Notas por critério

| # | Critério | Peso | Nota | Evidência |
|---:|---|---:|---:|---|
| 1 | Canonicalidade, Autoridade & Não-Duplicação | 15% | 8.7 | `docs/architecture/README.md` §"Precedência de fontes" é a fonte única e é referenciada (não copiada) por `AGENTS.md` §5. `joint-review-criteria.md` é fonte única dos pesos, achados de rodada apontam para lá em vez de duplicar (linha 9 do próprio doc formaliza essa regra). Achado corrigido nesta sessão (referência §3→§4 quebrada em 2 arquivos) mostra que o sistema não é imune a drift de referência — mas o padrão estrutural (uma fonte, resto aponta) está correto onde verificado. Resíduo: `working-memory.md` ainda contém o comando de invocação do Codex por extenso (linha 18) apesar de "já promovida" a `AGENTS.md` — é referência cruzada explícita, não duplicação normativa, mas está no limite. |
| 2 | Clareza de Papéis & Proporcionalidade | 9% | 8.8 | Frontmatter `status/owner/authority` presente e consistente nos docs de `docs/engineering/reviews/*-summary.md` e `joint-review-criteria.md`. `AGENTS.md` §1 declara explicitamente que `docs/00-prompt-mestre.md` não é ponto de entrada — clareza de papel ativa, não implícita. Estrutura proporcional: sem router dedicado, sem ontologia de tags, nada além do que o volume real de documentos justifica (`principles.md` #1). Doc sem esse frontmatter: `NEXT_SESSION_PROMPT.md`, `ARCHITECTURE.md`, `ENGINEERING.md`, `README.md` — aceitável, são documentos de nível raiz com papel autoexplicativo pelo próprio nome/posição, não uma inconsistência real. |
| 3 | Context Routing & Progressive Disclosure | 15% | 6.8 | O próprio `joint-review-criteria.md` (linha 58) já admite a lacuna: "este projeto não tem um context router dedicado". `AGENTS.md` §2 lista 4 passos, mas nenhum deles orienta quando consultar `docs/engineering/*` (12 arquivos + `reviews/` com dezenas de artefatos) — só `docs/architecture/README.md` tem mapa/precedência; `docs/engineering/` não tem README/índice equivalente, então uma tarefa de qualidade/gates/exceções não tem um ponto de entrada análogo ao que arquitetura tem. `NEXT_SESSION_PROMPT.md` (110 linhas) mistura estado vigente com 3 camadas de "histórico" sucessivas no mesmo arquivo — cada uma diz explicitamente que foi superada, o que é honesto, mas o custo de progressive disclosure é real: um agente novo lê ~70 linhas de histórico superado antes de chegar à ação vigente no topo (que está corretamente no topo, mitigando parcialmente). Sem índice de `docs/engineering/`, a tarefa "qual arquivo de critério eu uso para o eixo X" depende de já saber que `joint-review-criteria.md` existe. |
| 4 | Correspondência com a Realidade & Controle de Drift | 16% | 7.3 | Pontos fortes reais: `ARCHITECTURE STATUS`/`DESIGN MATURITY STATUS` mantidos deliberadamente distintos com justificativa extensa (`ARCHITECTURE.md` linha 116, `principles.md` #6) — não infla nota por conclusão de design. `NEXT_SESSION_PROMPT.md` linha 57 registra explicitamente uma divergência encontrada entre contagens de teste e "nada commitado" vs. realidade medida, sem reescrever a seção original — bom padrão de reconciliação explícita. Contra: esta própria auditoria achou 3 casos de drift não capturados por revisões anteriores (2 referências de seção quebradas, 1 diretório não indexado) — evidência de que não há mecanismo determinístico, só correção reativa por auditoria manual (mesmo padrão que o Eixo Qualidade já registrou como não resolvido, critério 11 daquele eixo). `docs/architecture/README.md` linha 7 diz "Last verified: 2026-08-19" — não fica automaticamente estampada a cada edição real do arquivo, é atualização manual (baixo risco aqui porque o arquivo é pequeno e frequentemente tocado, mas é o mesmo tipo de lacuna). |
| 5 | Lifecycle, Proveniência & Evolução do Conhecimento | 12% | 8.5 | `docs/architecture/history/` tem subseção dedicada mapeando cada pasta ao doc normativo correspondente, com regra explícita "nunca tratar como fonte de decisão vigente". ADRs (`adr/README.md`) documentados como imutáveis com superação por ADR novo (não verificado literalmente nesta sessão, mas a convenção é citada consistentemente em `decisions-log.md`). Achado corrigido nesta sessão: `docs/architecture/reviews/` cumpria o mesmo papel de `history/` para rodadas pós-implementação mas não estava documentado nem indexado — agora corrigido (commit `48aad11`). Resíduo: a diferença de papel entre `history/` (pré-implementação) e `reviews/` (pós-implementação) não estava articulada em lugar nenhum antes desta sessão; a explicação nova é curta e ainda não foi testada por um agente real seguindo-a. |
| 6 | Rastreabilidade de Decisões, Trabalho & Triggers | 10% | 8.9 | `exceptions.md` EX-001 é exemplar: owner nomeado, `expiraEm` com condição concreta ("antes de M4 ou em 30 dias, o que vier primeiro"), tentativa de correção já registrada com motivo real de reversão. `decisions-log.md` liga toda decisão a nota Claude/Codex/status/artefato-fonte. `principles.md` #5 formaliza a regra "gatilho concreto, não 'revisar depois' vago" e o exemplo (EX-001) de fato segue essa regra. Pequeno resíduo: gatilhos de reavaliação em `NEXT_SESSION_PROMPT.md` (ex. item 7 da lista final) são triggers reais mas menos formalizados que os de `exceptions.md` (sem campo `owner`/`expiraEm` estruturado, é prosa). |
| 7 | Higiene de Contexto & Sinal-Ruído | 8% | 7.2 | `docs/engineering/reviews/` contém pares de arquivo com encoding corrompido junto do original íntegro para a mesma rodada (ex. `audit-areas-convergence-output.txt`, `full-audit-round1-arquitetura-codex-output-round1.txt` têm trechos em mojibake/`Â§`/binário ao lado de versões limpas do mesmo conteúdo em outros arquivos) — não é um documento concorrente normativo, é ruído de captura de saída bruta do Codex, mas é volume real sem valor incremental que um agente pode carregar por engano ao fazer glob amplo em `reviews/`. Nenhum documento órfão duplicando conteúdo normativo foi encontrado (bom sinal), mas a ausência de qualquer nota "arquivo de saída bruta, não ler diretamente" nesses diretórios significa que a única defesa contra carregar ruído é o bom senso do agente, não uma convenção documentada. |
| 8 | Portabilidade Agnóstica entre Agentes de IA | 6% | 9.0 | `CLAUDE.md` é de fato só `@AGENTS.md` (1 linha, confirmado por leitura direta) — zero duplicação, portabilidade real. `AGENTS.md` linha 3 declara explicitamente ser a fonte canônica "para qualquer agente de IA (Claude Code, Codex CLI)". Convenção seguida de forma limpa e verificável, sem achado negativo. |
| 9 | Auditabilidade & Enforcement do Sistema de Contexto | 9% | 6.9 | Achados datados/reproduzíveis existem (esta auditoria, as de arquitetura/qualidade, com commits reais citados) — boa auditabilidade manual. Mas não existe nenhum mecanismo determinístico (link-checker, verificador de seção/âncora, teste de CI) que capturaria os 3 achados desta rodada automaticamente — `AGENTS.md` §6 admite isso ("Não há automação disso ainda") e condiciona reavaliação a "existir CI real" ou "reincidência de link quebrado" — a reincidência já aconteceu (2 rodadas de auditoria anteriores não acharam os mesmos 3 problemas, esta achou) mas ainda não foi tratada como gatilho disparado. Isso é lacuna estrutural real, maior que correção pontual (ver resumo). |

## Nota ponderada (Claude, round1)

15×8.7 + 9×8.8 + 15×6.8 + 16×7.3 + 12×8.5 + 10×8.9 + 8×7.2 + 6×9.0 + 9×6.9, tudo /100:

= (130.5 + 79.2 + 102.0 + 116.8 + 102.0 + 89.0 + 57.6 + 54.0 + 62.1) / 100
= 793.2 / 100
= **7.93/10**

Critérios abaixo de 9.0: todos os 9. Nenhum tem impedimento externo real (mesmo raciocínio de `NEXT_SESSION_PROMPT.md` linha 9: "Eixos sem esse tipo de impedimento... não têm desculpa para não chegar a 9.0"). #3 e #9 são os mais estruturais (precisam de router/índice novo e de automação de verificação, respectivamente — escopo maior que ponto-fix). #4, #5, #7 têm componente ponto-fix real ainda não esgotado (mais achados de drift/ruído prováveis com leitura mais profunda de `docs/engineering/reviews/`). #1, #2, #6, #8 estão perto do limiar e corrigíveis nesta sessão.
