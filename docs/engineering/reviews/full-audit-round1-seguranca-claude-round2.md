# Full audit round1 — Eixo Segurança — Claude (blind, round2)

Nota cega round2, registrada ANTES de ler a saída do Codex round2. Contexto: round1 Claude=8.259, Codex=6.953 (commit e89e255 aplicou 3 correções reais: Ajv na borda HTTP items/renew/policies, quota nas rotas de negócio, doc drift do threat model). Reavalio TODOS os critérios (Codex também achou reduções em 1-4, 6-8 que eu não havia registrado com peso suficiente no round1), não só os 3 corrigidos.

| # | Critério | Peso | Round1 | Round2 | Justificativa da mudança |
|---:|---|---:|---:|---:|---|
| 1 | Isolamento Multi-Tenant & Autorização por Objeto | 18% | 9.2 | 8.6 | Concordo com a redução do Codex: parte do "end-to-end" roda em store de memória, não DynamoDB real; aceito como razão válida para não estar em 9+ (não é craft ruim, é limite real de Camada 3). Mantenho perto do topo — é o critério com mais evidência direta (`cross-tenant.test.ts`). |
| 2 | Least-Privilege IAM & Contenção de Blast Radius | 14% | 8.3 | 7.2 | Codex tem razão: `tableAccessFor().readWriteKeys()`/`readKeys()` (`scoped-lambda-function.ts:63-84`) são só metadata — todo grant de tabela é largo (inclui `Scan`), documentado como limitação conhecida mas não fechado. Isso é real, não hipotético: uma Lambda de items comprometida pode ler/escrever qualquer entidade na tabela base. Reduzo minha nota para refletir isso com o mesmo peso que dei a GSI3/GSI6 (que estão corretamente isolados). |
| 3 | Autenticação, Sessão & Gestão de Identidade | 11% | 8.7 | 7.8 | Concordo com o achado do Codex: comentários em `cognito.ts`/`resolve-request-context.ts` referenciam "BFF session module"/rotação de refresh token que não encontrei implementados em código real (nem eu no round1). Isso é uma lacuna real de aspiração vs. implementação, não um erro de leitura minha - reduzo. |
| 4 | Integridade do Pipeline Assíncrono & Fronteiras de Mensagem | 14% | 8.8 | 7.6 | Confirmo o achado do Codex após checar: `dispatch-outbox-relay-handler.ts:30-31` faz `unmarshall` + cast para `OutboxRecord` validando só `entityType`, sem schema Ajv do envelope. É um trust boundary interno (stream da própria tabela, não input de cliente externo), o que limita a severidade, mas ainda é uma borda sem validação declarada pelo critério ("filas... validados por schema"). Não corrigido nesta sessão (seria expandir escopo além do achado prioritário HTTP) - registro como pendência real, não impedimento externo. |
| 5 | Validação de Entrada, Injection & Fail-Closed | 9% | 6.5 | 8.8 | Corrigido: `create/update/renew-item-request.v1.json` + `put-policy-request.v1.json`, validados antes do resolver em `item-handlers.ts`/`policy-handlers.ts` (commit e89e255). Não é 9+ porque `dashboard`'s `status` query param ainda faz cast sem validar enum (`item-handlers.ts` handleDashboard) e `expectedVersion` (`requireExpectedVersion`) aceita qualquer `Number()` parseável incl. negativos/frac/Infinity — gaps menores, não corrigidos ainda. |
| 6 | Proteção de Dados Sensíveis, Segredos & Criptografia | 9% | 9.1 | 8.6 | Codex acha razoável: a propriedade de redação é comprovada para o que passa pelo `SecureLogger`, não para o conteúdo bruto que SQS/DLQ armazenam automaticamente (mensagem inteira, não só o que logamos). Reduzo levemente - é uma distinção real entre "o que logamos é seguro" e "o que a infraestrutura persiste é seguro". |
| 7 | Logging Seguro, Detecção & Resposta a Incidentes | 8% | 7.5 | 6.0 | Concordo fortemente com a redução do Codex: alarmes (`reminder-observability.ts`) **não têm ação de notificação** (SNS/PagerDuty) — comentário no próprio arquivo confirma isso como decisão deliberadamente adiada, não lacuna escondida, mas o efeito prático é "ninguém é avisado automaticamente". Falhas de autenticação/autorização negada não geram evento de segurança dedicado. Não corrigido nesta sessão - decidir o alvo de notificação é decisão de produto (e-mail/PagerDuty/Slack) que não me cabe inventar sem o usuário. |
| 8 | Configuração Segura da Plataforma & Superfície Exposta | 6% | 7.0 | 7.3 | Pequeno ajuste positivo: `test/infra/stack.test.ts` cobre regressão de grants/placeholder, CORS não é wildcard. Concordo com o Codex que o CORS `allowMethods`/`allowHeaders` declarado não cobre `PUT`/`DELETE`/`If-Match`/`Idempotency-Key` explicitamente — real, mas efeito prático limitado (HTTP API v2 é mais permissivo que REST API na prática); não é urgente o bastante para eu tratar como bloqueador de nota, mas registro. |
| 9 | Resistência a Abuso, DoS & Exaustão de Custo | 5% | 7.2 | 8.7 | Corrigido: quota `API_REQUEST` agora aplicada em toda rota de `/items*` e `/reminders/policies*` (commit e89e255), antes só existia em `/test/ping`. Não é 9+ porque não há throttle explícito no nível do API Gateway (HTTP API tem default, não configurado) nem budget/alarm de custo. |
| 10 | Verificação Adversarial & Gestão Contínua de Risco | 6% | 7.8 | 8.3 | Corrigido parcialmente: `threat-model.md` ganhou seção "Status de implementação (atualizado)" que reconcilia a deriva "ainda não implementada" vs. M0-M3.5 real, com lacunas reavaliadas item a item (commit e89e255). Não é 9+ porque não fiz uma nova rodada de convergência formal do threat model em si (fora do escopo deste axis) nem adicionei testes adversariais novos além do que já existia. |

**Nota ponderada (Claude, round2):**
0.18×8.6 + 0.14×7.2 + 0.11×7.8 + 0.14×7.6 + 0.09×8.8 + 0.09×8.6 + 0.08×6.0 + 0.06×7.3 + 0.05×8.7 + 0.06×8.3
= 1.548 + 1.008 + 0.858 + 1.064 + 0.792 + 0.774 + 0.480 + 0.438 + 0.435 + 0.498
= **7.895 / 10**

Abaixo de 9.0 em todos os critérios ainda - não fechado, mais uma rodada necessária. Itens que considero "escopo maior que correção pontual" (não impedimento externo): IAM per-entity real (critério 2), módulo de sessão BFF/rotação de refresh (critério 3), Ajv em todas as bordas assíncronas (critério 4), notificação de alarme (critério 7). Itens que são impedimento externo real: qualquer prova de `AccessDenied` real em IAM AWS, redrive de DLQ real, execução real de Scheduler (Camada 3 - sem ambiente AWS nesta sessão, mesma limitação já registrada nos axes 1/2).
