# Full audit round1 — Eixo Segurança da Informação e AppSec — Claude (blind, round1)

Nota cega registrada ANTES de rodar Codex ou ver sua saída.

| # | Critério | Peso | Nota | Evidência |
|---:|---|---:|---:|---|
| 1 | Isolamento Multi-Tenant & Autorização por Objeto | 18% | 9.2 | `authorize()` (`src/modules/identity/domain/authorization.ts:94-125`) checa `resource.tenantId !== context.tenant.tenantId` antes de role; `resource` sempre vem de leitura DB por `ctx.tenant.tenantId` (nunca do DTO do cliente) — `expiration-service.ts:117-121` (`getItem` lê por tenant, autoriza depois). `test/integration/cross-tenant.test.ts` cobre ID substitution, GSI-shaped lookup, DTO tenantId malicioso, PK addressability. GSI3/GSI6 não têm prefixo de tenant por design documentado, mitigado por IAM (ver #2). |
| 2 | Least-Privilege IAM & Contenção de Blast Radius | 14% | 8.3 | `ExpirationTrackerTable` (`infra/lib/dynamo-table.ts:109-189`) constrói `PolicyStatement`s explícitas excluindo GSI3/GSI6 dos grants tenant-facing; `grantGsi3ReadTo`/`grantGsi6ReadTo` são os únicos caminhos. `test/infra/stack.test.ts` afirma isolamento sintetizado. Porém `ScopedLambdaFunction`/`tableAccessFor` (`infra/lib/scoped-lambda-function.ts:63-85`) documenta explicitamente que granularidade por entidade (SK prefix) e PutItem-only não são IAM-enforced — é grant de tabela inteira, mitigado só por disciplina de código. `appConfigAccessFor` usa `resources: ["*"]` (linha 141) sem justificativa forte além de "AppConfig não tem ARN previsível" — não tentado accessgroup por prefixo de nome. Camada 3 (AccessDenied real em AWS) pendente, é impedimento externo real (sem ambiente AWS neste projeto). |
| 3 | Autenticação, Sessão & Gestão de Identidade | 11% | 8.7 | `resolveRequestContext` (`resolve-request-context.ts:44-110`) rejeita tokens antes de `globalLogoutAfter`/`deviceLogoutAfter`, valida `profile.status==="ACTIVE"`. Cognito (`infra/lib/cognito.ts`) usa senha forte (12+ chars, todas classes), MFA TOTP opcional (UNK-006 pendente, documentado como decisão externa não tomada — não é bug), tokens de acesso/id de 15min, refresh 30d com rotação tratada no BFF (não visto neste axis — módulo de sessão HTTP do BFF não está no código lido, é referência a "M1's session module" no comentário; não localizei implementação real de refresh/rotation/reuse-detection no repo). Esse gap (rotação de refresh token mencionada em comentário mas sem módulo correspondente encontrado) reduz a nota. |
| 4 | Integridade do Pipeline Assíncrono & Fronteiras de Mensagem | 14% | 8.8 | DLQ com `maxReceiveCount:5`, alarme de idade (`infra/lib/reminder-queue.ts:26-47`); `reminder-dispatch-handler.ts` usa `defaultSchemaRegistry`/Ajv (único handler assíncrono confirmado a validar por schema). Não confirmei (sem ler exaustivamente todos os workers) se `dispatch-outbox-relay-handler.ts`/`outbox-sweeper-handler.ts`/`reminder-producer-handler.ts`/`reminder-reconciliation-handler.ts` também validam por Ajv na borda — só 2 arquivos em todo `src/` importam `schema-validator`/`schema-registry-disk` (reminder-dispatch-handler.ts e o registry em si), sugerindo que os demais handlers de fila/stream NÃO passam por validação de schema explícita. Isso é um risco real de borda não uniformemente fechada. |
| 5 | Validação de Entrada, Injection & Fail-Closed | 9% | 6.5 | **Achado real**: rota HTTP `/items` (`src/modules/expiration/http/item-handlers.ts:81-140`, `CreateItemInput`/`UpdateItemInput` em `expiration-item.ts:65-92`) não tem NENHUMA validação Ajv/runtime de schema — só checagem de presença (`if (!req.body)`). `name`, `category`, `dueDate` (deveria ser ISO-8601), `tags`, `priority` fluem direto ao DynamoDB sem checagem de tipo/formato/tamanho em runtime (TypeScript é só compile-time, não protege contra payload JSON arbitrário de um chamador autenticado malicioso). Contraria diretamente a definição do critério ("Validação server-side por contrato (Ajv) em toda borda — HTTP, filas..."). Não é hipotético: um `dueDate` não-ISO quebra silenciosamente `gsi1Keys`/ordenação do dashboard; um objeto gigante em `tags` não tem limite. |
| 6 | Proteção de Dados Sensíveis, Segredos & Criptografia | 9% | 9.1 | `Redactor` (`src/shared/observability/redactor.ts`) cobre objetos/arrays/erros/strings recursivamente, com `redactError` truncando stack a 1 linha, aplicado estaticamente (bundle-safe, cold-start-safe). DynamoDB `AWS_MANAGED` encryption + `grantEncryptDecrypt`, SQS `SQS_MANAGED`. CMK upgrade é follow-up documentado, não bug. Não vi todos os call sites confirmando que TODO handler usa `SecureLogger`/Redactor (lint `no-console` força isso fora de `observability/**`, mas não vi o `SecureLogger` em si neste round). |
| 7 | Logging Seguro, Detecção & Resposta a Incidentes | 8% | 7.5 | Alarme de idade de DLQ existe com descrição de runbook ("nunca redrive cego"). Não vi (não li neste round) alarmes específicos para autenticação/autorização negada em volume, nem CloudWatch metric filters/queries correlacionáveis por tenant para acesso a GSI3/GSI6. Sem evidência de trilha de auditoria de eventos de segurança distinta do audit log de negócio (`appendAudit` em `expiration-service.ts` parece ser audit de domínio, não de segurança). |
| 8 | Configuração Segura da Plataforma & Superfície Exposta | 6% | 7.0 | CORS em `infra/lib/api.ts:36-41` usa `allowOrigins: ["https://app.example.invalid"]` (placeholder documentado, não still-open `*`) e `allowCredentials:true` — combinação seria perigosa com wildcard real, mas hoje é fixo a um domínio único (ok). `test/infra/stack.test.ts` cobre isolamento de índice. Não vi verificação de que o placeholder de CORS bloqueia CI antes de produção real (risco de esquecer de trocar). Superfície exposta pequena (2 rotas reais + skeleton), difícil avaliar exaustivamente sem mais leitura. |
| 9 | Resistência a Abuso, DoS & Exaustão de Custo | 5% | 7.2 | `TenantQuota`/`application/quota.ts` existe (M1) mas não lido em detalhe neste round. SQS com long-polling e DLQ limita amplificação de retry. Billing on-demand (`PAY_PER_REQUEST`) sem budget/alarm de custo visto. Sem rate-limit visível no nível de API Gateway (throttle default do HTTP API, não configurado explicitamente em `api.ts`). |
| 10 | Verificação Adversarial & Gestão Contínua de Risco | 6% | 7.8 | `cross-tenant.test.ts` é evidência forte e específica de segurança (10 casos negativos). `test/infra/stack.test.ts` cobre isolamento de IAM/GSI. Não encontrei `docs/architecture/threat-model.md` neste round (não confirmei existência/ausência com certeza — não usei Glob dedicado). Camada 3 (teste real em AWS) é impedimento externo já registrado nos rounds anteriores, não recontado aqui como culpa deste axis. |

**Nota ponderada (Claude, round1):**
0.18×9.2 + 0.14×8.3 + 0.11×8.7 + 0.14×8.8 + 0.09×6.5 + 0.09×9.1 + 0.08×7.5 + 0.06×7.0 + 0.05×7.2 + 0.06×7.8
= 1.656 + 1.162 + 0.957 + 1.232 + 0.585 + 0.819 + 0.600 + 0.420 + 0.360 + 0.468
= **8.259 / 10**

Achado mais acionável (não impedimento externo, corrigível nesta sessão): critério 5 — ausência de validação Ajv na borda HTTP `/items` (e possivelmente `/reminders/policies`) é um gap real de fail-closed, não craft de código. Pretendo corrigir com um schema JSON para `CreateItemInput`/`UpdateItemInput` e validação explícita no handler antes de escrever no Codex round.
