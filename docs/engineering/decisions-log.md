# Engineering Decision Log

Decisões de engenharia tomadas durante a Engineering Maturity Review (Prompt Mestre §46), com IDs sequenciais.

## E-000 — Rubrica congelada com 16 domínios, pesos e gates G1-G11

Convergência Claude↔Codex em uma rodada (Codex rejeitou o draft inicial, todas as objeções aceitas). Ver `01-engineering-quality-criteria.md` e `reviews/checkpoint-01-rubric/`.

## E-001 — Regra anti-double-counting entre domínios M/B/I

M avalia semântica de persistência, B avalia correção local/de contrato, I avalia comportamento sob falha — mesma implementação (ex.: OCC) pode ser citada nos três, mas cada nota exige uma propriedade distinta comprovada.

## E-002 — Distinção formal entre N/A e NOT ENOUGH EVIDENCE (NEE)

N/A exige inaplicabilidade estrutural + concordância dos dois revisores; NEE é evidência insuficiente sobre algo aplicável, não sai do denominador, reduz a nota máxima possível.

## E-003 — Gate de dependency audit dividido em produção (bloqueante) e dev (informacional com exceção registrada)

Motivo: `npm audit --omit=dev` mostra 0 vulnerabilidades reais; as 5 encontradas (2 critical, 1 high, 2 moderate) são todas transitivas via `vitest`/`vite`/`esbuild`, relacionadas ao dev-server (não expostas em produção). Corrigir exige upgrade major do Vitest (1.x→4.x), desproporcional a aplicar sem verificar a suíte inteira. Decisão: tornar o audit de produção genuinamente bloqueante agora (estava `|| echo`, nunca bloqueava nada); registrar o achado de dev como exceção formal (`exceptions.md` EX-001) com prazo de revisão, não deixar como "warning eterno" sem dono nem prazo.

## E-004 — Enforcement de boundary de arquitetura via ESLint `no-restricted-imports`, não uma ferramenta nova

Motivo: o achado do Checkpoint 2-9 (G10 FAIL) é que os boundaries de módulo estavam limpos só por disciplina, sem mecanismo automatizado. Alternativas consideradas: `dependency-cruiser` (ferramenta dedicada, mais expressiva, mas nova dependência e nova curva de configuração) vs. `no-restricted-imports` do ESLint core (já presente, zero dependência nova, expressivo o suficiente para os três boundaries identificados: domain↛infra, domain↛aws-sdk, domain↛módulo-de-terceiros). Escolhido `no-restricted-imports` por proporcionalidade (Prompt Mestre §29, §37 — não adicionar ferramenta só para "parecer completo"). Verificado que passa hoje sem quebrar nada (`npm run lint` limpo) — prova que o estado real já respeitava o boundary, só faltava o mecanismo.

## E-005 — Branch protection não aplicada automaticamente

Motivo: mudar a configuração do GitHub (branch protection) altera o fluxo de trabalho do usuário (hoje push direto pra `main`) fora do escopo de uma edição de arquivo — tratado como decisão que precisa de confirmação explícita do usuário, não aplicada silenciosamente nesta sessão. Recomendação registrada em `ENGINEERING.md` (Known Gaps).

## E-006 — Correção do SHA truncado do `actions/setup-node` sem rodada de debate

Motivo: bug mecânico e objetivamente verificável (39 vs. 40 caracteres, confirmado via `gh api` contra o SHA real do tag `v4.0.2`) — não é uma decisão de engenharia sujeita a interpretação, é uma correção factual. Tratado como correção direta, não como Type 1 decision.

## E-007 — Não injetar `SecureLogger` diretamente na lógica pura dos workers (dispatch/producer/reconciliation); fechar G8 via testes dedicados em vez disso

Motivo: `producer.ts` documenta explicitamente que "this module stays observability-agnostic" — telemetria fica para a camada de handler Lambda real, que ainda não existe (mesmo padrão de M0-M3: lógica pura testável primeiro, wiring de runtime depois). Injetar logging agora romperia esse padrão arquitetural já estabelecido e adiantaria trabalho de M4+ sem o handler real para validar o formato de evento contra CloudWatch de verdade — risco de reabstração. Decisão: em vez disso, fechar a parte de G8 que É possível fechar hoje sem infraestrutura nova — "replay/reconciliation são testados" — com testes unitários dedicados que faltavam:

- `test/unit/reminder/reconciliation.test.ts`: `reconcileExpiredClaims` (claim expirado reverte pra SCHEDULED; claim ainda válido ou ocorrência não-CLAIMED não é tocada) e `reconcileDst` (cenário real de drift: ocorrência materializada sob uma regra antiga é cancelada e substituída por uma nova ocorrência recomputada sob a regra atual — antes disso, `reconcileDst` tinha ZERO cobertura de teste em qualquer lugar do repositório, direta ou indireta).
- `test/unit/reminder/producer.test.ts`: prova que uma falha genuína de store (não uma perda de race de OCC) numa claim específica cai em `failed`, sem abortar o resto do tick nem mutar a ocorrência (fica disponível pra retry) — o caminho `failed` do producer nunca tinha sido exercitado por nenhum teste antes (só afirmado vazio no caminho feliz).

G8 permanece **não-PASS pleno** — "observável em execução real" continua exigindo o handler Lambda + fila real de M4+, isso não muda com testes. Mas a parte de "replay/reconciliation testados" da definição do gate agora tem evidência real, o que não existia antes desta sessão.

**Nota de superação (2026-08-19, full-audit round1/qualidade)**: a premissa "handler Lambda real... ainda não existe" ficou desatualizada em M3.5 — `src/runtime/aws/handlers/` tem handlers reais hoje (achado real de drift documentação↔código, `AGENTS.md` §7 já corrigido). Entrada mantida como registro histórico do raciocínio na data em que foi tomada (não editada retroativamente); a decisão de fundo (workers puros continuam observability-agnostic, logging fica no handler) continua válida e não foi revertida.

## E-008 — Boundary transitivo real via `dependency-cruiser`, não só ESLint; achado real de código corrigido no processo

Motivo: o red team formal (Checkpoint 12) apontou que a regra `no-restricted-imports` do ESLint (E-004) só compara o texto literal do specifier do import, não o grafo real — não pega reexport transitivo, `import()` dinâmico, nem (descoberta ao testar de verdade) imports relativos dentro do MESMO módulo, porque a string `"../ports/foo.js"` nunca contém a substring `modules/*/ports` que os padrões glob esperam. Adicionado `dependency-cruiser` (`npm run check-boundaries`, wired no CI) como enforcement AUTORITATIVO — resolve o grafo de módulos de verdade. ESLint mantido só como feedback rápido no editor, comentário do arquivo agora deixa isso explícito.

Ao testar a nova ferramenta contra o código real (não só contra um caso sintético), ela encontrou **2 violações genuínas já existentes**: `src/modules/expiration/domain/expiration-item.ts` e `audit-event.ts` importavam `EntityKey`/`TransactPutEntry` de `../ports/expiration-store.ts` (domain→ports, boundary real) em vez do `shared/dynamodb/occ.ts` já usado corretamente pelo módulo `reminder` para o mesmo tipo — `expiration-store.ts` tinha uma cópia própria e estruturalmente duplicada de `EntityKey`/`TransactPutEntry`/`TransactUpdateEntry`/`TransactWriteEntry`/`isTransactionCanceled`. Também descoberto no processo: `reminder/ports/reminder-store.ts` reexportava esses mesmos tipos de `expiration/ports/expiration-store.ts` — uma dependência acidental ports→ports entre módulos irmãos. Corrigido na origem: os tipos/função agora vivem só em `shared/dynamodb/occ.ts`; `expiration-store.ts` e `reminder-store.ts` importam de lá diretamente; `dependency-cruiser` confirma zero violações após a correção (rodado contra o código real, não um exemplo isolado).

## E-009 — Full-audit round1, eixo Qualidade de Engenharia: achados reais corrigidos

Protocolo `AGENTS.md` §4 executado contra `docs/engineering/joint-review-criteria.md` ("Eixo: Qualidade de engenharia"). Ver `docs/engineering/reviews/full-audit-round1-qualidade-summary.md` para o registro completo (notas por critério, ambos os lados, achados restantes). Achados reais corrigidos nesta sessão:

1. **Bug real de bundle em `schema-validator.ts`** (mesma classe do bug já corrigido no Redactor, E-anterior/commit `494f4e5`): `defaultSchemaRegistry` usava `import.meta.url`+`readdirSync` em runtime, que resolve vazio sob o bundle esbuild-cjs (`infra/lib/scoped-lambda-function.ts`) — ficou dormant até esta sessão porque nenhum handler real importava `defaultSchemaRegistry`. Corrigido em duas etapas: primeiro `SchemaRegistry` ganhou um construtor explícito (`new SchemaRegistry(schemas)`) alimentado por imports estáticos para `defaultSchemaRegistry`; o warning cosmético do esbuild persistia porque o disk-walk (`import.meta.url`) ainda vivia no mesmo módulo/arquivo bundlado. Segunda correção (mesma sessão, round subsequente): o disk-walk (`repoRoot`/`schemasDir`/`walkJsonFiles`) foi extraído para `src/shared/contracts/schema-registry-disk.ts` (`loadAllSchemasFromDisk`), usado só por `scripts/validate-schemas.ts` e `test/contract/schemas.test.ts` (ESM real, nunca bundle). `schema-validator.ts` não referencia mais `import.meta.url`; o construtor de `SchemaRegistry` agora sempre exige a lista explícita de schemas.
2. **`reminder-dispatch-handler.ts` fazia cast não validado de `JSON.parse(record.body)`** apesar de já existir um schema (`schemas/queues/reminder-dispatch.v1.json`) nunca usado por nenhum handler real. Corrigido para validar via `defaultSchemaRegistry` antes do cast, tratando payload schema-inválido como poison message (não retryable).
3. **`as never` inseguro em `dispatch.ts`**: `TransactWriteEntry` (occ.ts) e `DynamoTransactPutEntry` (outbox.ts) eram tipos estruturalmente idênticos, declarados duas vezes (mesmo padrão do achado E-008) — `appendToTransaction` exigia o tipo mais estreito, forçando o cast. Corrigido: `outbox.ts` reusa `TransactPutEntry`/`TransactWriteEntry` de `occ.ts`; `appendToTransaction` aceita `TransactWriteEntry[]` (Put|Update), cast removido.
4. **CDK usava `lambda.Runtime.NODEJS_20_X`** (8 ocorrências, `infra/lib/expiration-tracker-stack.ts`) — CDK synth já emitia warning real de deprecação (`nodejs20.x` deprecado em 2026-04-30). Atualizado para `NODEJS_24_X`.
5. **CI só disparava em push para `main`**, nunca `develop` (branch de trabalho ativo per `AGENTS.md` §3) — `.github/workflows/ci.yml` agora dispara em `[main, develop]`.
6. **Drift real doc↔código em `AGENTS.md` §7**: afirmava "nenhum worker tem runtime Lambda real ainda", mas `src/runtime/aws/handlers/` já tinha 8 handlers reais desde M3.5. Corrigido (ver também nota de superação em E-007 acima).
7. **README.md** omitia `check-boundaries`/`test:dynamodb` e afirmava incorretamente que lint sozinho cobre enforcement de boundary (na verdade `dependency-cruiser` é o autoritativo, per E-008). Corrigido.
8. **`exceptions.md` EX-001** tinha owner "a definir formalmente" sem nunca ser definido — formalizado como Marcelo (projeto solo).

Achados adicionais corrigidos numa rodada seguinte (Codex round2, nota 7,788/10, revisão estática sem execução dos gates por sandbox read-only):

9. **Disk-walk residual em `schema-validator.ts`**: mesmo após o item 1, o branch dinâmico (`repoRoot`/`schemasDir`/`walkJsonFiles`, ainda com `import.meta.url`) continuava no mesmo arquivo que `defaultSchemaRegistry`, então esbuild ainda bundlava (e avisava sobre) código morto em runtime. Extraído para `src/shared/contracts/schema-registry-disk.ts`, usado só por `scripts/validate-schemas.ts`/`test/contract/schemas.test.ts`; `schema-validator.ts` não referencia mais `import.meta.url`.
10. **Casts `as TransactPutEntry[]` obsoletos em `expiration-service.ts`** (3 ocorrências: `updateItem`, `renewItem`, `appendAudit`): ficaram redundantes desde que `appendToTransaction`/o item 3 acima passou a aceitar `TransactWriteEntry[]` — `entries` já era `TransactWriteEntry[]`. `appendAuditToTransaction` (`domain/audit-event.ts`) também foi alargado de `TransactPutEntry[]` para `TransactWriteEntry[]` pelo mesmo motivo (mesma convenção de `outbox.ts`). Todos os 3 casts removidos; import não utilizado de `TransactPutEntry` removido.
11. **`README.md` linha 47** ainda afirmava boundary "enforced por ESLint (`no-restricted-imports`)" — desatualizado desde E-008 (`dependency-cruiser` é o enforcement autoritativo real, ESLint só feedback rápido de editor). Corrigido.

Todas verificadas com `npm run typecheck`, `npm run lint`, `npm run check-boundaries`, `npm test` (152/152) antes de cada commit.

## E-010 — `test-engineering-standard.md` APPROVED, protocolo Claude↔Codex 8 rodadas, gate elevado a 9,5/10

Novo documento normativo (`docs/engineering/test-engineering-standard.md`) formalizando, pela primeira vez neste projeto, gates binários de validade (determinismo/reprodutibilidade de veredito, isolamento de dado, asserção não-tautológica, intenção declarada — G-V1 a G-V4; adequação claim→evidência — G-C1; blast radius declarado e reversão tentada, específicos de drill operacional — G-V5/G-V6) e critérios ponderados de qualidade (0-10, com fórmula de agregação explícita por unidade de avaliação — teste individual, claim individual, suíte/componente, relatório) para tudo que este projeto trata como "teste" — automatizado ou drill operacional real (chaos/DiRT). Motivado pela Wave 2 do Pilot Readiness Program (2026-08-28, 6 drills reais contra `dev`, ver `pilot-readiness-program.md`), executada sem uma régua pré-registrada do que tornaria essa evidência válida.

Gate de aceitação elevado a **9,5/10** (acima do padrão de 9,0 deste projeto para protocolo `AGENTS.md` §4) por pedido explícito do Marcelo. Levou **8 rodadas** — a mais longa execução deste protocolo neste projeto até agora (a segunda mais longa, o Visual Language/Design System, levou 16 rodadas mas contra o gate padrão 9,0) — com trajetória de convergência real e monotônica do lado Codex: 6.35 → 7.85 → 8.70 → 9.18 → 9.34 → 9.46 → 9.48 → **9.62** (aprovado). Nota final Claude 9.9/10. Cada rodada corrigiu causas-raiz específicas nomeadas pela rodada anterior (nunca reformulação cosmética): rodada 1 achou defeito de arquitetura conceitual (gates binários que na verdade exigiam julgamento, contradição com a própria fonte citada, "929 arquivos" quando eram 929 casos de teste); rodadas 2-3 acharam inconsistências introduzidas pela própria reestruturação corretiva (referência órfã a gate renomeado, auditoria retroativa concedendo gate prospectivo sem evidência de pré-registro); rodadas 4-7 fecharam lacunas cada vez mais estreitas na fórmula de agregação/regra de nota 10/citação de fontes, terminando em achados de escopo muito pequeno (marcador de rodada desatualizado, lista fechada que esqueceu um critério).

Achado colateral corrigido no mesmo commit: drift real entre `pilot-readiness-program.md`/`pilot-readiness-assessment.md` e a evidência real da Wave 2 — a tabela-resumo e o addendum diziam "replay não duplica side effects"/"medir RPO/RTO"/"fechou por completo" de forma mais ampla do que os 6 drills realmente provaram (achado da Rodada 3/5 do próprio protocolo, corrigido nos dois arquivos).

Evidência completa (notas cegas Claude/Codex de todas as 8 rodadas, prompts, saída bruta): `docs/engineering/reviews/test-engineering-standard/`.

## E-011 — `logging-observability-standard.md` APPROVED (3 rodadas, gate 9,5/10) + auditoria real da implementação (7,0 → 9,0 → 8,7/10) + 1 decisão de produto registrada, pendente

Pedido explícito do Marcelo (2026-08-29): "máxima qualidade em logs e tracing para debugar o produto com clareza", avaliada em rodadas adversariais Claude↔Codex, mesmo padrão de `test-engineering-standard.md`.

**Rodada 1 (achado real, não planejada)**: antes de qualquer padrão formal existir, uma primeira rodada Codex sobre a trilha de auditoria de segurança (`security-audit.ts`/`security-audit-observability`) achou 5 emissores reais (`documents_handler`/`subjects_handler`/`imports_handler`/`document_purge_handler`/`upload_slot_reconciliation_handler`) sem nenhum CloudWatch metric filter wireado — nota 7,0/10. Corrigido em `infra/main.tf` (todos os 5 adicionados às listas certas, `aws_cloudwatch_log_group` explícito para os 2 nunca invocados em `dev`) + `test/architecture/security-audit-observability-coverage.test.ts` novo (cruza call sites reais contra o bloco Terraform específico, não substring livre no arquivo — achado de uma rodada de reconfirmação, também corrigido). Confirmado: **9,0/10**.

**`docs/engineering/logging-observability-standard.md` escrito e `APPROVED`** (3 rodadas de crítica aos CRITÉRIOS em si, não à implementação — Claude 9,5/Codex 9,6, gate elevado a 9,5/10 mesmo padrão de `test-engineering-standard.md`): 8 critérios ponderados (Emissão estruturada 11%; Redação 13%; Correlação de aplicação 11%; Tracing distribuído 10%; Taxonomia de erro 17%; Wiring de detecção 15%; Testes do subsistema 12%; Debugabilidade+documentação 11%), âncoras de pontuação, gate de auditoria de implementação 9,0/10. Registrado em `docs/engineering/README.md`.

**Auditoria formal da implementação real contra os critérios `APPROVED`**: primeira passada, **7,9/10** — abaixo do gate. 4 achados BLOQUEANTES: (1) `correlationId` não atravessava a fronteira Step Functions do pipeline de extração/OCR (cada um dos 4 task handlers gerava um `randomUUID()` novo, tornando os logs de uma mesma execução impossíveis de correlacionar entre si); (2) invocação síncrona Lambda→Lambda para o parser-sandbox sem nenhuma propagação/logging; (3) campo `AppError.retryable` documentado como "drives SQS consumer behavior" mas na prática nunca consultado por handler real (confirmado por grep: só um log, zero branching); (4) `email-delivery-handler.ts` logava schema-inválido/JSON malformado fora de qualquer `runWithContext`, sem correlationId nenhum.

**Corrigido nesta sessão (não a decisão de produto pendente, ver abaixo)**: propagação real de `correlationId` de ponta a ponta pelo pipeline de extração inteiro (`ExtractionExecutionInput`/`StartOcrInput`/`TextractJob`/`RunDeterministicParserInput-Output`/`RunBedrockExtractionInput-Output`/`ValidationContext`, todos ganharam o campo, sempre re-ecoado nos 2 pontos de retorno de `run-bedrock-extraction.ts`; os 4 task handlers passaram a ler o valor em vez de gerar um novo); `LambdaPdfParser`/`parser-sandbox-handler.ts` (que tinha ZERO logging antes) ganharam propagação real + `SecureLogger`; `email-delivery-handler.ts` ganhou o mesmo padrão de fallback-correlationId-antes-do-parse que `reminder-dispatch-handler.ts` já tinha; `reminder-dispatch-handler.ts`/`email-delivery-handler.ts` passaram a usar `toAppError`+logar `errorCode`/`retryable` (antes logavam `err.message` cru); comentário desatualizado em `textract-task-handler.ts` sobre `ErrorEquals` casar `.code` (deveria ser `errorType`, já corrigido de fato desde W2-02) corrigido; descrição de alarme em `security-audit-observability/main.tf` não cita mais "4 HTTP handlers" (número já obsoleto de novo). 8 testes novos/estendidos provam a propagação real, não só que compila. Rodada de reconfirmação: **8,7/10** — 2 dos 4 bloqueantes fechados de verdade (achados 3/4 acima), 2 permanecem.

**1 achado residual implementado nesta sessão (2026-08-29, sinal explícito do Marcelo) + 1 permanece decisão de produto/arquitetura (Type 1) pendente**:
1. **Tracing distribuído (correlationId ↔ trace ADOT/X-Ray)** — `IMPLEMENTED`/`UNIT TESTED`: design de 3 rodadas (Claude↔Codex, Claude 9,3/Codex 9,4, `APPROVED`) implementado de ponta a ponta — `src/shared/observability/xray-trace-header.ts` novo (parser determinístico/fail-open de `_X_AMZN_TRACE_ID`, `Root` validado por regex estrito, `Sampled` só `"0"`/`"1"`, `Parent` deliberadamente fora do v1 do código), precedência de `SecureLogger.write()` corrigida (achado real e já existente, independente da feature — `context` por chamada não sobrescreve mais `getContext()`/`baseContext`), 20 testes novos/estendidos, `typecheck`/`lint`/`check-docs`/`npm test` limpos. Ver `docs/architecture/correlationid-xray-trace-join.md` (movido da raiz) para o registro completo. **`E2E PROVEN`** (2026-08-29): smoke test real em `dev` via `aws --profile claude-dev` — `exptrk-dev-reminder-dispatch` invocado com payload sintético, log estruturado real com `correlationId`/`xrayTraceId`/`xraySampled:true`, `xrayTraceId` confirmado idêntico ao trace real (CloudWatch + `aws xray batch-get-traces`, mesmo `request_id`). Detalhe completo em `docs/architecture/correlationid-xray-trace-join.md`. Opção B (atributo de span via `@opentelemetry/api`) permanece candidata futura, não decidida, 3 pré-requisitos nomeados no design.
2. **`AppError.retryable` não decide comportamento real de nenhum handler SQS** — Marcelo decidiu explicitamente: manter como está, só documentação corrigida (não mais afirma falsamente que "drives SQS behavior" — `app-error.ts`'s comentários em `AppError`/`isRetryable()` agora são honestos sobre o escopo atual real: só diagnóstico/logging). **Decisão de produto pendente, registrada aqui, não implementada**: se/quando SQS handlers devem realmente parar de retentar um erro `retryable:false` (hoje todo handler reporta `batchItemFailure` incondicionalmente, deixando `maxReceiveCount`+DLQ nativos do SQS serem o árbitro real) é uma mudança de comportamento de produção genuína (o que aconteceria com uma mensagem não-retryable? dropar? rotear para um estado terminal explícito?) — não decidida, não implementada, mesma classe de `W2-01-DECISION`/`pilot-readiness-program.md`.

Todas as correções: `npm run typecheck`/`lint`/`check-docs` limpos, `terraform validate`/`test` (módulo + root) limpos contra `dev` real, `npm test` completo 1087/1087, zero regressão.

## E-012 — `definition-of-done.md` APPROVED (3 rodadas, gate 9,0/10) — Definition of Done por item de todo list, decisão de processo permanente

Pedido explícito do Marcelo (2026-08-29, no meio da sessão de Multi-User B2B): "cada etapa da todo list só deve de fato terminar quando ela for avaliada contra nossos padrões [engenharia, qualidade de código, logs e tracing]... faça o mapeamento e coloque isso no ciclo de desenvolvimento levando em consideração o padrão claude-codex", seguido de "pode envolver o codex na decisão de como isso vai funcionar... quando chegarem a decisão final, coloque isso no contexto para ser respeitado a partir de agora em todas as sessões". Tratado com o rigor do protocolo `AGENTS.md` §4 apesar de não ser decisão de arquitetura, porque muda como todo trabalho futuro (Claude e Codex) é avaliado antes de ser considerado concluído.

Novo documento normativo (`docs/engineering/definition-of-done.md`) fechando um gap real: os padrões de qualidade já existentes (`quality-gate-tiers.md` Tier A, `change-risk-scale.md`, `logging-observability-standard.md`, `test-engineering-standard.md`) eram consultados por PR/wave inteira, nunca por item individual de todo list da sessão — permitindo drift acumular antes de ser pego. O documento define: unidade de conclusão (regra decidível para decompor itens guarda-chuva que cruzam vários níveis de risco, em vez de tratar toda uma wave como um único item avaliável), gate mínimo por nível de risco (`change-risk-scale.md` 1-6), classificação de risco na prática (pelo diff real, não pela intenção original), registro mínimo de evidência (linha `DoD: item=...; risco=...; evidência=...; lacunas=...`, com ordem de preferência de onde persistir — commit, depois artefato de review, transcript da sessão só como último recurso), e dois exemplos concretos deste projeto (nível 3-4: teste adversarial DELETING num writer W3-07 já fenced; nível 5-6: Wave B2B-1 physical model do Multi-User B2B).

Protocolo de nota cega, 3 rodadas, convergência monotônica de ambos os lados: Rodada 1 (Claude 6,3/Codex 7,2) achou o rascunho inicial não indexado em lugar nenhum, granularidade de "item de todo list" indefinida (uma wave inteira cruza vários níveis de risco no mesmo item), "Tier A relevante ao módulo tocado" conflitando com a definição real de Tier A em `quality-gate-tiers.md`, mecanismo de verificação puramente autoatestado sem trilha auditável, e ausência de exemplos concretos — ambos os lados acharam achados reais independentemente, sem se copiarem. Rodada 2 (Claude 8,3/Codex 8,8), após reescrita incorporando os achados da Rodada 1 e indexação real em `docs/engineering/README.md`+`AGENTS.md` §1: ambos os lados acharam, de forma convergente e independente, que o documento alegava falsamente já estar "registrado em `docs/engineering/decisions-log.md`" antes do protocolo ter fechado — achado real de integridade do próprio processo que o documento define; Codex também achou uma brecha residual na regra de decomposição ("viável sem overhead desproporcional" ainda dependia de julgamento subjetivo). Rodada 3 (Claude 9,1/Codex 9,2, **ambos ≥9,0 — aprovado**): a alegação de registro prematuro foi corrigida para "submetida ao protocolo" com instrução explícita de citar esta entrada pelo número após aprovação; a regra de decomposição foi endurecida para um critério decidível (decompor por padrão quando o item toca mais de uma camada com gates diferentes OU mais de um gatilho de risco; só permanece guarda-chuva quando o diff é atomicamente revisável); a ordem de preferência de onde registrar evidência foi tornada explícita (commit → artefato de review citando o arquivo real → transcript só como último recurso), alinhada com a convenção do projeto de persistir estado em arquivo, não em conversa.

Aplicação prospectiva, não retroativa (itens já `completed` antes deste documento existir não são reabertos), e sem mecanismo automatizado (sem CI/hook dedicado) — disciplina autoaplicada pelo próprio agente, com a linha `DoD:` como trilha auditável mínima e proporcional (`principles.md` #1); automação real fica condicionada a evidência futura de que a disciplina manual não está sendo seguida. Vigente a partir de agora, em toda sessão futura, Claude e Codex.

## E-013 — `definition-of-done.md` corrigido: aplicação de `test-engineering-standard.md` era referencial, não concreta (achado do Marcelo, gap de execução, não de design)

Durante a implementação real de Wave B2B-2/B2B-3 do Multi-User B2B (2026-08-30), o Marcelo perguntou "quais critérios estão sendo usados no definition of done?" e, ao ouvir a resposta, apontou: "achei que na definition of done nós usaríamos os critérios de engenharia... definidos no projeto" — sinalizando corretamente que o E-012 estava sendo citado por nome, não aplicado em substância.

**Verificação honesta do que realmente aconteceu**: `definition-of-done.md` (E-012) já dizia, desde a v1 aprovada, "test-engineering-standard.md se o diff adicionar/alterar teste automatizado", e seu próprio exemplo de nível 3-4 já mencionava "precisa satisfazer os gates binários G-V1..G-V6 aplicáveis" — a REGRA já existia corretamente escrita. O gap real não estava no documento, estava na EXECUÇÃO: os testes novos escritos nos chunks de Wave B2B-2 (D-087/D-088, `resolver.test.ts`/`bff-auth-service.test.ts`/`cross-tenant.test.ts`) satisfaziam G-V1/G-V2/G-V4 por construção (determinísticos, dado sintético isolado, nomes descritivos longos), mas nenhum registrou por escrito a mutação nomeada que **G-V3** exige — o gate foi tratado como "documento consultado", não "gate binário aplicado com artefato escrito".

**Correção em duas partes**:
1. **Retroativa**: os testes já escritos em B2B-2 (`resolver.test.ts` — 3 testes; `bff-auth-service.test.ts` — 3 testes; `cross-tenant.test.ts` — 1 teste corrigido) e os recém-escritos em B2B-3.2 (`organization-persistence.test.ts` — 10 testes) ganharam comentário `G-V3` imediatamente acima de cada `it()`, nomeando uma mutação concreta real que faria a asserção falhar. `npm run typecheck`/`lint` limpos, 75/75 testes-alvo verdes após a correção — nenhum comportamento mudou, só a trilha auditável escrita que faltava.
2. **Prospectiva**: `definition-of-done.md` ganhou uma seção nova, "Como `test-engineering-standard.md` se aplica na prática", tornando operacional o que antes era só referencial — define os 3 gates que realmente se aplicam a teste unitário/regressão comum (G-V3 mutação nomeada por escrito; G-V4 intenção declarada; G-C1 adequação claim→evidência quando houver claim de sistema externo), distingue de G-V1/G-V2/G-V5/G-V6 (predominantemente drill operacional/chaos, não forçar onde não se aplica), e exige que a linha `DoD:` de um item que toca teste mencione explicitamente "G-V3 aplicado", não só resultado verde da suíte — resultado verde prova que o teste passa, não que ele detectaria um bug real.

Classificado como nível 2-3 da própria `change-risk-scale.md` (correção mecânica de execução + tightening operacional de uma regra já aprovada em E-012, não uma decisão nova) — não reabre o protocolo Claude↔Codex. Evidência: `docs/engineering/definition-of-done.md` atualizado (72 linhas, dentro do padrão dos documentos irmãos), 5 arquivos de teste corrigidos retroativamente (`test/unit/identity/resolver.test.ts`, `test/unit/bff/bff-auth-service.test.ts`, `test/integration/cross-tenant.test.ts`, `test/unit/organization/organization-persistence.test.ts`), `npm run typecheck`/`lint`/`check-docs` limpos, 75/75 testes-alvo verdes sem nenhuma mudança de comportamento.

## E-014 — `research-protocol.md` APPROVED (3 rodadas, gate 9,2/10) — pesquisa externa antes de decisões Type 1, decisão de processo permanente

Pedido explícito do Marcelo (2026-08-30, no fechamento da Wave B2B-5): formalizar quando pesquisa externa (padrões de mercado, RBAC, fluxos de convite, sessão multi-tenant) deve informar uma decisão Type 1, do mesmo jeito que `definition-of-done.md` (E-012) formalizou o gate por item de todo list — inclusive submetendo esta própria formalização ao protocolo Claude↔Codex até convergência, mesmo não sendo decisão de arquitetura em si (mesmo precedente de E-012). Duas exigências específicas motivaram o documento: (1) "como garantiríamos que esse protocolo seria de fato acionado e não cairia no esquecimento?" — resolvido acoplando o gatilho à régua de risco que já existe (`change-risk-scale.md` nível 5-6) e ao ponto de entrada que já é obrigatório (Rodada 1 do protocolo `AGENTS.md` §4), em vez de criar uma disciplina paralela para lembrar; (2) "essa pesquisa deve sempre estabelecer os critérios que posteriormente serão usados para dar nota no protocolo claude-codex" — resolvido exigindo que pesquisa declarada `SIM`/`SIM PARCIAL` produza um checklist de critérios pesados e âncorados, declarado ANTES de qualquer nota, que vira a régua da rodada (nunca julgamento livre).

Novo documento normativo (`docs/engineering/research-protocol.md`): declaração obrigatória na Rodada 1 (`SIM`/`SIM PARCIAL`/`NÃO`, nunca omissão silenciosa — `SIM PARCIAL` para decisões compostas, com escopo explícito de quais sub-decisões são informadas por pesquisa vs. internas); exigência de fonte+data+representatividade (RFC/OWASP preferível a só vendor docs para postura de segurança/identidade); checklist de critérios como sub-rubrica subordinada a `joint-review-criteria.md` (nunca um substituto da rubrica fixa dos 9 eixos); fluxo de reconciliação quando o Codex contesta o checklist em si, não só a nota (nota da régua separada da nota do design enquanto disputado, gate explícito de régua estável ≥9,0/≥9,0, registro das notas intermediárias restrito ao artefato da rodada — nunca uma coluna nova em `decisions-log.md`).

Protocolo de nota cega, 3 rodadas, convergência monotônica de ambos os lados: Rodada 1 (Claude 7,9/Codex 7,7) achou, de forma independente, que a classificação binária `SIM`/`NÃO` falha para decisão composta (testado contra D-086 — mistura layout de chave interno com last-OWNER/revogação de Membership, padrão externo — e D-053/D-054 — majoritariamente padrão externo OAuth/PKCE), que o documento não resolvia o que fazer quando o Codex contesta o próprio checklist proposto, e uma imprecisão factual na citação de `test-engineering-standard.md` (G-V1..G-V6 são validade binária, não peso). Rodada 2 (Claude 8,8/Codex 8,7), após incorporar `SIM PARCIAL` e o fluxo de reconciliação: Codex achou que a "nota da régua" ainda não tinha critério de aprovação explícito (risco de uma régua ruim sobreviver por exaustão textual em vez de concordância real), que o formato de nota dupla precisava de um lugar de registro explícito para não colidir com a convenção já existente de `decisions-log.md`, duas inconsistências textuais residuais (`SIM` sem `SIM PARCIAL` em duas linhas), e que o exemplo retrospectivo contra D-086 generalizava isolamento IAM de índice como categoricamente interno quando na verdade era interno por precedente já convergido (GSI3/GSI6, `AGENTS.md` §7), não por natureza. Rodada 3 (Claude 9,2/Codex 9,2, **ambos ≥9,0 — aprovado**): gate de régua estável fixado em "ambos ≥9,0", mesmo padrão numérico do protocolo em si; registro de notas intermediárias confinado ao artefato de review da rodada; as duas inconsistências textuais corrigidas; exemplo D-086 corrigido para atribuir a classificação interna ao precedente real, não a uma propriedade inerente de decisões de IAM/segurança.

Indexação em `AGENTS.md` §4/`docs/engineering/README.md` feita neste commit de fechamento (mesma sequência que E-012 seguiu — indexado ao aprovar, não antes). Vigente a partir de agora, em toda sessão futura, Claude e Codex — primeira aplicação real prevista para a Wave B2B-7 (RBAC), citada como exemplo ilustrativo no próprio documento.

## E-022 — Full-audit Round 2, eixo Engenharia de Contexto: regressão real desde o Round 1, gate ≥9,0 NÃO atingido (6,84/10)

Segunda rodada do ciclo de auditoria cíclica pedido por Marcelo, reusando o framework de `joint-review-criteria.md` (Round 1 fechou em 2026-08-20, Claude 9,078/Codex 9,092). Protocolo completo, 3 rodadas reais (`codex exec`, nota cega na Rodada 1): Claude 8,18→6,94→7,24; Codex 5,99→6,25→6,84. **Nota final reconciliada 6,84/10 — não atinge o gate.** Consistente com o precedente de D-159 (nota confirmada abaixo de 9 em vez de forçada): convergência aqui é acordo honesto sobre o estado real, não inflar a nota — a própria tarefa proibiu corrigir achados nível 3+ nesta auditoria.

**Achado central**: `NEXT_SESSION_PROMPT.md` satisfaz o guardrail de linhas de `scripts/check-doc-drift.ts` (277 < 300) mas não sua intenção — medição real ~134 KB/~15.600 palavras (Codex), ~59K tokens (Claude, ordem de grandeza compatível). É a mesma classe de narrativa acumulada sessão-a-sessão que a reconciliação de 2026-08-29 removeu (1067→78 linhas), reencarnada numa forma que um guardrail de contagem de linhas não detecta — o próprio guardrail dá "clean" para essa propriedade que não mede (achado de auditabilidade/enforcement, critério #9). Ambos os déficits — critério #3 (Context Routing, 6,0) e #7 (Higiene de Contexto, 3,5, o mais baixo do eixo) — são PENDENTES por exigirem decisão editorial de recompactação (não corrigível mecanicamente).

5 achados factuais mecânicos (nível 1-2) encontrados pelo Codex e corrigidos nesta sessão em `docs/architecture/README.md`: autocontradição interna sobre o orquestrador W3-07 (dizia "ainda não decidido" e "design `APROVADO`" no mesmo parágrafo); teto fixo "D-000 a D-043" no índice de `decisions-log.md` (já em D-231+) generalizado para "D-000 em diante"; status stale do smoke test correlationId/X-Ray (dizia "pendente", já `E2E PROVEN` desde 2026-08-29); frase sobrevivente afirmando que a implementação do orquestrador W3-07 "ainda não [foi] construída" (D-124 já `IMPLEMENTED`/`DEPLOYED`); linha do índice de `roadmap-evolution/17` apresentando a Wave B2B-12 como "próxima ação" sem deixar claro que descrevia o estado histórico do documento-fonte, não o atual (D-110/D-111 já a fecharam). Todos verificados por `npm run check-docs` limpo antes e depois.

**Achado central CORRIGIDO — D-236, 2026-09-08** (execução da recompactação editorial + instrumentação de guardrail que esta auditoria deixou pendente). `NEXT_SESSION_PROMPT.md` recompactado de ~145.804 bytes/~16.492 palavras/299 linhas para ~12.304 bytes/~1.449 palavras/87 linhas — cada entrada "Continuação.../D-xxx..." (todas já com linha completa em `decisions-log.md`) reduzida a 1-3 frases (o quê + status + referência D-xxx), nenhuma narrativa reescrita. Nenhum estado real/pendência real perdido: todos os 11 itens do roadmap P0, os 8 itens do backlog P1, o status de cada eixo aberto do full-audit-round2 e as 8 pendências reais que dependem de decisão de Marcelo foram preservados em forma compacta. Guardrail cego corrigido na mesma sessão: `scripts/check-doc-drift.ts` ganhou `checkDensityGuardrail()` (bytes via `Buffer.byteLength` + contagem de palavras, `NEXT_SESSION_PROMPT_MAX_BYTES=30_000`/`NEXT_SESSION_PROMPT_MAX_WORDS=4_000`, ~2,4x/~2,75x o tamanho reconciliado — generoso para crescimento real, apertado o bastante para pegar reacumulação descontrolada antes de chegar a seis dígitos de novo), testes novos em `test/architecture/check-doc-drift.test.ts` (incluindo um caso sintético reproduzindo a forma exata da regressão: poucas linhas, cada uma enorme). `npm run check-docs`/`typecheck`/`lint` limpos contra os dois guardrails (linha e densidade). Ver `docs/architecture/decisions-log.md` D-236 para o detalhe da decisão de reconciliação em si. **Eixo ainda não fechado** (gate ≥9,0 não reavaliado nesta sessão — só o achado central mecânico foi corrigido, não uma rodada Claude↔Codex nova) — os déficits de critério #3/#7 podem exigir uma rodada nova para confirmar se a correção estrutural do guardrail e a recompactação bastam para o gate.

Achado descartado/reclassificado: a ausência de frontmatter em 4 `estado-final-consolidado.md` recentes (`document-type-metadata-scoping/`, `external-sharing-scoping/`, `whatsapp-channel-scoping/`, `document-request-series-recipient-scoping/`) foi inicialmente classificada pelo Codex R1 como "regressão direta do remédio do baseline Round 1" (severidade ALTA); Claude contestou com evidência textual (a política de frontmatter citada em `docs/engineering/README.md` cobre explicitamente só `docs/engineering/reviews/`, nunca foi escrita para `docs/architecture/reviews/`); Codex R2 concordou e reclassificou para MÉDIA/BAIXA, gap de escopo real (decidir se estender a política), não violação de regra existente.

**PENDENTES nomeados para sessão futura** (nível 3+, não decididos aqui): (1) recompactação editorial de `NEXT_SESSION_PROMPT.md` (mesma classe de trabalho da reconciliação de 2026-08-29, ~150 decisões D-084 a D-231 para condensar); (2) instrumentação nova em `scripts/check-doc-drift.ts` para medir densidade/bytes/tokens, não só linhas; (3) decidir se a política de frontmatter se estende a `docs/architecture/reviews/`; (4) mecanismo de checagem de completude dos índices manuais dos 2 READMEs contra o volume real de `reviews/` (62 entradas em `docs/architecture/reviews/`, ~116 em `docs/engineering/reviews/`, ambos ainda cobrindo o volume atual corretamente por amostragem, sem defeito ativo hoje). Evidência completa: `docs/engineering/reviews/full-audit-round2-contexto-{claude,claude-round2,claude-round3,summary}.md`, `-codex-prompt{,-round2,-round3}.txt`, `-codex-output-round{1,2,3}.txt`.

## E-015 — Full-audit round2, eixo Privacidade e Governança de Dados — reabertura pós-purga real (D-179/D-190), gate ≥9.0 não atingido, achado real de cobertura incompleta

Reabertura do eixo (`AGENTS.md` §4), conforme a própria recomendação de fechamento do `full-audit-round1-privacidade-summary.md` (condição "b": reabrir quando M4+ implementar purge worker real). Desde o fechamento round1 (2026-08-19, Claude 4,885/Codex 3,82/10), `docs/architecture/decisions-log.md` passou a registrar D-179 a D-190 (nove workers de purga reais deployados sobre GSI8: `core-user-data-purge`, `delivery-record-purge`, `document-purge`, `invitation-purge`, `membership-purge`, `quota-telemetry-purge`, `security-audit-purge`, `transient-purge`, `tenant-purge`) e D-205/D-217/D-225 (dossiê PDF/XLSX, relatórios agendados, compartilhamento externo design).

Nota cega Claude R2 (evidência real de grep/leitura, não estimativa): 5,535/10. Nota cega Codex R2 (`codex exec`, instrução explícita de não ler a nota do Claude antes de terminar): 5,664/10 — a transcrição bruta atravessou dois erros reais de infraestrutura no meio do processo (`codex_models_manager::manager: failed to renew cache TTL: missing field 'supports_parallel_tool_calls'`, bug de compatibilidade de cache do `codex-cli 0.147.0`; e um erro de sintaxe de glob do `rg` no Windows), mas o processo continuou em background além da primeira checagem desta sessão e produziu nota completa por critério + veredito final, ao contrário do que pareceu inicialmente.

**Reconciliação factual real** (não apenas duas notas cegas lado a lado): a maior divergência foi o critério #4 — Retenção (Claude 4,5 vs. Codex 7,2). Verificado por leitura direta de `src/workers/membership-purge/purge.ts:107-240` e `src/workers/transient-purge/purge.ts`: os workers agora paginam via `queryDue()` sobre **GSI8** (não mais `Scan` na tabela base). O cursor ainda não persiste entre invocações agendadas, mas GSI8 ordena por elegibilidade/data de purga — cada invocação nova processa primeiro os candidatos mais atrasados, starvation monotonicamente decrescente, estruturalmente diferente do Scan por hash que D-170 criticava (starvation arbitrária, sem relação com urgência). **A leitura do Codex está mais correta**: a migração para GSI8 (D-179/D-190) já é correção estrutural real do problema central de D-170, não apenas "worker existe sem garantia de progresso" como a nota Claude original registrou — recomenda-se anotar esta reconciliação em D-170 na próxima sessão que tocar o `decisions-log.md` de arquitetura. Nota reconciliada do critério #4: 7,2/10 (adota Codex).

Achado real de concordância entre os dois lados, não corrigido nesta auditoria (é achado, não fix — escopo maior de implementação): `DossierExportRun` (`src/modules/document-archive/domain/dossier-export-run.ts`) e `ReportSubscription`/`ReportSubscriptionRun` (`src/modules/reports/domain/report-subscription*.ts`) — entidades novas desde o fechamento round1, geram PDF/XLSX de dado pessoal denso por Subject (D-205/D-217) — não têm nenhum campo de TTL/`purgeAfter`/`retentionClass` nem worker de purga dedicado (confirmado por ausência na lista completa de `src/workers/*purge*/purge.ts`), ficando em S3 sem expiração declarada.

`SecureLogger`/`Redactor` seguem íntegros, confirmado por ambos os lados: `grep -rn "console\.log|console\.error|console\.warn" src/ --include=*.ts` (exclui testes) só retorna o sink interno de `src/shared/observability/logger.ts:50`, que passa todo campo pelo `Redactor` antes de emitir — nenhuma regressão. `ExternalShareLink` (D-225) confirmado design-only (zero código em `src/`) por ambos os lados.

Demais critérios (#1, #5, #6, #7, #8) tiveram divergência real de nota entre Claude e Codex não reconciliada por debate factual completo nesta sessão (só #4 teve verificação de código dedicada) — média simples adotada como nota reconciliada provisória: #1 6,5, #2 7,5 (concordância), #3 2,0 (concordância), #5 4,5, #6 7,5, #7 5,0, #8 6,5. **Nota ponderada reconciliada: 5,774/10.**

**Gate do eixo (≥9,0, sem arredondar) NÃO atingido por nenhuma leitura (Claude/Codex/reconciliada).** Fechamento genuíno desta rodada, mesmo critério de proporcionalidade do round1: os 3 critérios de maior peso (#3 16%, #4 17%, #5 14% = 47% do eixo) continuam dependendo de trabalho de produto (DSR endpoints, TTL/purge para os exports novos) ou de decisão externa (região AWS/DPA/subprocessadores), não de correção documental. Detalhe completo, inclusive tabela lado a lado das duas notas cegas, em `docs/engineering/reviews/full-audit-round2-privacidade-summary.md`.

## E-016 — Full-audit round2, eixo Governança de Produto e Serviço Multi-tenant — melhora real desde round1, gate ≥9,0 não atingido, nenhum vazamento cross-tenant confirmado

Reabertura do eixo (`joint-review-criteria.md:151-164`) cobrindo tudo entregue desde o fechamento round1 (2026-08-30, nota final Codex 4,65/10): Document Archive completo (D-143 a D-226), canal WhatsApp (D-197/D-229/D-231), compartilhamento externo (guest upload, dossier export D-205, scheduled reports D-217), e as waves B2B-7 a B2B-14 (RBAC, convite, troca de organização, último-OWNER, cutover de dev D-110/D-111).

Protocolo de nota cega, 1 rodada real de proposta (Claude 7,1/10, Codex 7,8/10 — convergência forte, diferença 0,7, mesma classificação por critério, sem desacordo material a reconciliar). **Não reaberto para 2ª/3ª rodada de contestação**: mesmo padrão já usado no fechamento round1 deste eixo — a lacuna residual para 9,0 não é desacordo de avaliação, é trabalho de produto ainda não construído e já bloqueado por decisão anterior (catálogo de planos/billing e suporte cross-tenant bloqueados explicitamente em D-052 até existir provedor de pagamento; crypto-shredding por chave dedicada nunca desenhado; telemetria de custo runtime por tenant é lacuna FinOps conhecida e aceita, 10º eixo ainda não formalizado). Melhora real e concreta sobre os 4,65 do round1 nos critérios 1 (Lifecycle, 3,0→6,5/8,8), 2 (Offboarding/Exportação, 1,0→6,0/9,0) e 6 (Administração/Suporte, 2,0→4,0/6,5) — `Organization`/`Membership`/convite/aceite/último-OWNER/`organization:close`↔`organization:cancel-close`/`HELD_FOR_RECOVERY` (D-124/D-127) e Admin Activity Log (D-149) são código real e testado, não mais lacuna de design.

**Achado central da rodada (pergunta obrigatória): nenhuma rota/ação de negócio nova (D-140+) sem RBAC real ou sem tenant-fencing foi encontrada**, por Claude e Codex independentemente. `ACTION_ROLES` (`src/modules/identity/domain/authorization.ts:231`) é `Record<Action, ReadonlySet<Role>>` — TypeScript recusa compilar com qualquer `Action` (74 valores) sem entrada, incluindo as 27 actions `docarchive:*` novas. `authorize()` (linha 342) rejeita `TENANT_MISMATCH` antes de checar role, com `resource` sempre populado por leitura de banco já escopada ao tenant autenticado. Isolamento multi-tenant real por IAM (não só convenção de chave): D-180 a D-190 têm prova positiva+negativa via `aws iam simulate-principal-policy` para os workers GSI8, `infra/tests/stack.tftest.hcl`'s `gsi8_worker_isolation` cobre os dois sentidos. Histórico completo de `decisions-log.md` (D-16x a D-243) revisado — nenhuma ocorrência de "cross-tenant"/"vazamento"/"leak"/"IDOR" é um vazamento real não fechado (todas são provas positivas de isolamento, exceto D-120, já corrigido, que vazava estrutura de chave DynamoDB interna, não dado de outro tenant).

**Ressalva registrada por ambos (achado BAIXO, não um vazamento confirmado)**: a completude de `ACTION_ROLES` prova só que toda `Action` declarada tem tier — não prova por construção que toda rota chama `authorize()` com a ação correta (não há lint/dependency-cruiser enforçando isso). Nenhuma violação real encontrada nas rotas revisadas.

**Achado real não corrigido nesta sessão (nível 3+, fora do escopo de auditoria)**: `scripts/reset-dev-data.ts`'s `QUEUE_BASE_NAMES` (linha 59) ficou congelado no inventário do cutover B2B-12 (D-110/D-111, 12 filas base) e não inclui filas criadas depois (WhatsApp outbox/delivery, scheduled reports, document-request-recurrence) — uma futura execução `--confirm` terminaria "verde" sem purgar essas filas novas. Script é schema-agnostic para DynamoDB (sem drift ali), só o inventário de infraestrutura de fila está desatualizado.

Evidência completa: `docs/engineering/reviews/full-audit-round2-produto-multitenant-{claude,codex-prompt-round1,codex-output-round1,summary}.{md,txt}`.

Nível 3-4 (auditoria/documentação, sem código tocado exceto os próprios artefatos de review). Evidência: `docs/engineering/reviews/full-audit-round2-privacidade-{claude,codex-prompt,codex-output-round1,summary}.{md,txt}`. Reabrir quando: `DossierExportRun`/`ReportSubscription` ganharem TTL/purge real, o padrão Scan de D-170 for corrigido, DSR/decisão de região acontecerem, ou simplesmente para obter a nota cega do Codex que faltou (reexecução de CLI, não depende de código novo).

## E-017 — Full-audit round2, eixo Governança de IA e Controles Internos — gate ≥9,0 não atingido (Codex 6,74→7,10, Claude 7,195), 2 incidentes reais da própria sessão registrados retroativamente

Reabertura do eixo (`joint-review-criteria.md` §"Eixo: Governança de IA e Controles Internos") usando como evidência viva a própria sessão corrente (D-222 a D-231+): auto-auditoria de comportamento de agente de IA por agentes de IA. Protocolo de nota cega, 3 rodadas reais: Claude R1 7,195/10; Codex R1 6,74/10; Codex R2 (pós-fixes) 7,10/10 — melhora real e verificável, cada ponto citando arquivo:linha do fix aplicado, não arredondamento.

**Achado central, convergente entre Claude e Codex desde a Rodada 1**: dois incidentes reais desta mesma sessão não estavam registrados em `ai-governance.md` §5 — **AI-INC-003** (recorrência de AI-INC-001: 3 níveis de subagentes se auto-redelegando sem progresso em D-227, corrigido por intervenção direta da sessão orquestradora) e **AI-INC-004** (D-227 declarou fechado o item 9 do roadmap P0 sem que o worker de entrega guest existisse — "future worker not built yet" no próprio código —, só corrigido horas depois por D-228 via verificação ao vivo pós-merge, não por revisão humana nem pelo protocolo `AGENTS.md` §4). Ambos adicionados a `ai-governance.md` §5 nesta sessão, com a admissão honesta de que o próprio mecanismo "registrar antes do fim da sessão" não foi seguido — só cumprido retroativamente por esta auditoria.

**Fixes reais aplicados em `docs/engineering/ai-governance.md`**: (1) linha de merge `develop→main` em §1 corrigida (dizia "requer confirmação de Marcelo", desatualizada desde a autonomia de merge de 2026-08-29); (2) linha sobre `infra/` em §1 corrigida (dizia "proibido por padrão", resíduo pré-ADR-0009/Terraform) e separada da regra real que permanece proibida (`terraform apply`/`aws` de escrita real fora de `dev`) — achado NOVO do próprio Codex na Rodada 2, contradição introduzida pela correção da Rodada 1 entre §1 e §3, pega pela re-nota; (3) §3 corrigida (Claude Code não é mais "exceto `infra/`") e um gap admitido explicitamente (delegação de subagente não é modelada por nenhum documento); (4) AI-INC-001 com status corrigido para recorrente.

**Achado sobre calibração de autonomia (pergunta obrigatória da rodada)**: Codex classificou a autonomia concedida como "atualmente imprudente — não por conceder autonomia de engenharia em si, mas porque a autonomia cresceu além da capacidade demonstrada dos controles internos que deveriam limitá-la" (não crítico — sem produção real, tudo reversível). Claude chegou a conclusão convergente por caminho distinto: o QUANTO de autonomia parece proporcional; o que está atrasado é a disciplina de QUANDO um evento vira registro formal.

**Gap estrutural real, não corrigido (fora do escopo desta auditoria — documental/registro, não correção de processo)**: não existe gate exigindo evidência de fluxo PONTA A PONTA antes de declarar um item de ROADMAP fechado (`definition-of-done.md` E-012/E-013 cobre item de todo list de sessão, granularidade menor); não há limite de profundidade de redelegação de subagente nem verificação de progresso antes de redelegar (causa raiz de AI-INC-001/003, recorrente, não corrigida em nenhuma das duas ocorrências); registro de modelo/CLI (§4) segue ancorado em 2026-08-20; proveniência sistemática por commit (agente/modelo/sessão/aprovador) continua ausente. Registrados para rodada futura, candidatos a pesquisa externa (NIST AI RMF função Manage) se formalizados.

Nível 3-4 (auditoria/documentação + fixes de registro/matriz de autoridade, sem código de produto tocado). Evidência completa: `docs/engineering/reviews/full-audit-round2-governanca-ia-{claude,codex-prompt,codex-output-round1,codex-prompt-round2,codex-output-round2,summary}.{md,txt}`.

## E-018 — Full-audit round2, eixo Segurança da Informação e AppSec — melhora real desde round1 (média ~7,6→~8,02), gate ≥9,0 não atingido, 1 achado real novo (claim-before-send em guest credential delivery)

Segunda rodada formal deste eixo (`joint-review-criteria.md` §"Eixo: Segurança da Informação e AppSec"; baseline `reviews/full-audit-round1-seguranca-summary.md`, Claude 7,895/Codex 7,315). Entre as duas rodadas o sistema cresceu substancialmente: domínio documental inteiro (`document-archive`, guest access D-146, requirements D-145, recorrência D-147, dossier export D-217), activity log (D-149), canal WhatsApp operacional (webhook+Secrets Manager, D-231), migração de infra CDK→Terraform (ADR-0009), e o design ainda-não-implementado de `ExternalShareLink` (D-225, confirmado design-only por ambos os lados independentemente). Protocolo de nota cega, 3 rodadas reais: Claude R1 7,40/10; Codex R1 8,168/10 (achou SEC-R2-02, achado real que Claude não tinha); Claude R2 (pós-verificação direta do código) 7,95/10; Codex R2 (reconciliação) 8,068/10; convergência final na Rodada 3 com diferença de 0,12 pontos — parado ali por retorno decrescente, mesmo critério do Round1.

**Achado real novo mais importante da rodada**: `deliverGuestCredential()` (`src/workers/guest-credential-delivery/deliver.ts:66-81`) reivindica o marker de entrega (`markerStore.claim()`) ANTES de chamar `emailProvider.send()` — se o envio de e-mail falhar transitoriamente, o retry subsequente do mesmo record encontra o marker já reivindicado e retorna `ALREADY_DELIVERED` sem reenviar, causando perda PERMANENTE e silenciosa do link de credencial de um guest por uma falha transitória de SES. Encontrado pelo Codex na Rodada 1, verificado por leitura direta do código pelo Claude e aceito como real na Rodada 2. Correção exige uma máquina de estados com lease (`CLAIMED`→`SENT`, expirável) — mesmo padrão que `NotificationAttempt`/`dossier-export-generation-handler` já usam em outros workers deste repositório, não invenção de padrão novo — mas é mudança de comportamento (nível 3-4), fora do escopo de correção mecânica de uma auditoria. **PENDENTE para sessão futura.**

**Achado estrutural herdado do Round1, sem correção, agora com blast radius maior**: `infra/modules/dynamo-table/main.tf:266-296` (`tenant_facing_read_write`/`tenant_facing_read`) concede `dynamodb:Scan`+leitura/escrita completas sobre a TABELA BASE INTEIRA + GSI1/GSI2/GSI4/GSI5 a qualquer Lambda tenant-facing, sem `dynamodb:LeadingKeys` nem qualquer condição por tenant — a migração CDK→Terraform (ADR-0009) preservou o mesmo padrão do Round1 sem resolvê-lo. GSI3/GSI6/GSI4(exclusivo)/GSI8 continuam corretamente isolados via policy dedicada. Hoje esse grant amplo é compartilhado por 47 handlers Lambda (crescimento real desde Round1, quando a superfície era muito menor). **PENDENTE — decisão de arquitetura/segurança explícita do dono do projeto** (adotar condições por SK-prefix/capabilities mais estreitas, ou aceitar formalmente o risco residual); não é uma correção de sessão.

**O que corrigiu de verdade desde Round1 (verificado por leitura direta de código/infra por ambos os lados, não só alegado)**: (1) Autenticação/Sessão deixou de ser aspiração em comentário — Cognito com `refresh_token_rotation` real, grace period 30s, `ALLOW_REFRESH_TOKEN_AUTH` removido (`infra/modules/cognito/main.tf:68-92`), tabela BFF dedicada com KMS/TTL/revogação por dispositivo (D-136/D-140/D-141); (2) alarmes ganharam destino de notificação real em IaC (`infra/modules/alert-topic/main.tf`, tópico SNS + `alarm_actions` conectado em todos os módulos de observability) — a lacuna "alarme sem alvo" do Round1 foi fechada em código, ainda que a confirmação operacional da subscription (achado SEC-R2-05, Baixo/operacional, PENDENTE ação humana) não esteja comprovada; (3) webhook WhatsApp (D-231) implementado com `verifyMetaSignature()` (HMAC-SHA256+`timingSafeEqual` sobre raw body) verificado ANTES de qualquer parse/persistência, segredo em Secrets Manager com policy escopada ao ARN exato; (4) mitigação de injeção de fórmula (D-217) replicada corretamente do CSV existente para o XLSX novo.

**Achados menores adicionais, PENDENTES**: SEC-R2-03 (Médio, herdado do Round1) — `dispatch-outbox-relay-processor.ts:25-27`/`reminder-reconciliation-handler.ts:31-88` continuam sem schema Ajv do envelope interno, só cast TypeScript; SEC-R2-04 (Baixo, novo) — IP bruto do guest rate limiter persistido em texto claro na chave (`document-archive-guest-rate-limiter.ts:42-44`); cobertura de `TenantQuotaService` nas rotas de negócio adicionadas desde Round1 não foi verificada linha a linha por nenhum dos dois lados, fica como verificação pendente.

## E-020 — Full-audit round2, eixo Operações/SRE e Continuidade de Negócio — reabertura por crescimento de escopo, gate ≥9,0 não atingido (Claude 5,18→4,36, Codex 4,36, convergido em 2 rodadas), achado real de nível 5: mecanismo de rollback está quebrado

Segunda rodada formal deste eixo (`joint-review-criteria.md` §"Eixo: Operações, SRE e Continuidade de Negócio"; baseline `reviews/full-audit-round1-operacoes-summary.md`, Claude 5,11/Codex 3,92, ambos fechados na Rodada 1 como impedimento externo legítimo em todos os 8 critérios). Reaberto por volume de mudança de escopo desde 2026-08-20: de ~32 para **61 Lambdas reais** (`grep -c "source = \"./modules/lambda-function\"" infra/main.tf`), de poucas para **18 filas** via o módulo genérico `sqs-worker-queue` + 1 fila standalone (D-228), e um mecanismo de rollback novo (`rollback.yml`) inexistente na Rodada 1.

**Achado principal, nível 5, verificado por leitura estática (não depende de incidente real)**: `.github/workflows/rollback.yml` não é rollback de frontend — reverte aliases de Lambda via manifesto (`infra/outputs.tf`'s `lambda_published_versions`, mapa manualmente curado). O workflow hardcoda a expectativa de **exatamente 13 funções** no manifesto (`rollback.yml:128`, `if [ "$fn_count" != "13" ]`), mas o manifesto real hoje tem **34 entradas** — o rollback falha imediatamente com `"Manifest has 34 functions, expected exactly 13"` antes de reverter qualquer alias. Nunca atualizado desde que o manifesto cresceu de 13 para 34 funções rastreadas. Achado inicialmente encontrado pelo Codex (nota cega independente), verificado e confirmado por leitura direta do Claude na reconciliação — Claude havia lido errado na Rodada 1 blind, classificando o mecanismo como "rollback real de frontend, sem cobertura de Lambdas". **PENDENTE para sessão futura — não corrigido aqui por instrução explícita da auditoria (documentar achados nível 3+, não implementar).**

**Achado derivado, nível 4-5**: mesmo corrigindo o "13" hardcoded, **27 das 61 Lambdas reais (44%) não entram no manifesto de rollback nem no smoke test pós-deploy do `cd.yml`** — `lambda_published_versions`/`lambda_function_names` (`infra/outputs.tf`) são listas manualmente mantidas que pararam de crescer junto com `infra/main.tf`. Confirmado que `infra/tests/stack.tftest.hcl`'s asserção `length(output.lambda_function_names) == 34` **passa de fato** (`terraform test -var-file=env/dev.tfvars`, `AWS_PROFILE=claude-dev`, 23/23 real contra `dev`) — não é um teste quebrado, é uma asserção vazia quanto à completude (verifica que a lista manual tem 34 nomes distintos, nunca compara com as 61 Lambdas reais).

**Outros achados reais, nível 3-4, PENDENTES**: (1) alarme `GuestCredentialDeliveryFailures` (D-228, `infra/main.tf:564`) diz "investigate via runbook" mas `docs/architecture/incident-runbooks.md` não menciona essa fila em nenhum lugar; mesma lacuna para as filas WhatsApp novas (D-229); (2) `incident-runbooks.md` desatualizado desde 2026-08-21 — referência cruzada quebrada (§2 cita "rollback via §6.6", documento só tem 7 seções) e caminhos de arquivo CDK pré-Terraform ainda citados nos runbooks individuais; (3) cobertura DLQ+alarme é sistemática só para filas via o módulo genérico `sqs-worker-queue` (18 instâncias, todas com DLQ+alarme de idade por construção) — filas/destinos especiais (Streams, Step Functions) dependem de implementação manual caso a caso; (4) D-208 (migração ARM64) não teve plano de rollback específico documentado além da reversão genérica da variável `architectures`; (5) "verificação ao vivo pós-merge" (usada extensivamente em D-208 a D-230) é disciplina cultural registrada em `decisions-log.md`, não processo codificado em CI/CD nem checklist obrigatório.

**O que melhorou de verdade desde Round1**: cobertura DLQ+alarme sistemática por construção do módulo `sqs-worker-queue` (antes dependia de o autor lembrar de adicionar); rollback mechanism existe em desenho (manifesto imutável, confirmação textual, ordem de restauração) mesmo estando quebrado na prática — a Rodada 1 não tinha nenhum mecanismo de rollback documentado, real ou quebrado.

**Rodadas**: 2 rodadas reais de nota cega (blind: Claude 5,18/Codex 4,36; reconciliação: Claude aceita o achado do Codex sobre o rollback e revisa para 4,36, convergindo exatamente com o Codex) — não 3, decisão consciente de não forçar uma rodada puramente formal quando a divergência de fundo já convergiu por completo com evidência estática verificável por ambos os lados. Diferente da Rodada 1, nem todos os critérios têm impedimento externo desta vez: o achado do rollback (critério Deploy/Rollback) é um bug real e corrigível por leitura de código, não uma lacuna de "falta de oportunidade real". Evidência completa em `reviews/full-audit-round2-operacoes-{claude,claude-round2,codex-prompt,codex-output-round1,summary}.{md,txt}`. Eixo permanece **aberto** — condição de reabertura futura: correção do bug do `rollback.yml` + cobertura completa das 61 Lambdas no manifesto, `incident-runbooks.md` atualizado, ou primeiro exercício real de DLQ redrive/restore trimestral executado.

**Correção mecânica real feita nesta sessão (nível 1-2, dentro do permitido para uma auditoria)**: `docs/engineering/exceptions.md` — EX-001 estava desatualizada (contava 9 vulnerabilidades de `npm audit` quando já eram 11) e as 2 moderate adicionais (`exceljs@4.4.0→uuid@8.3.2`, cadeia de PRODUÇÃO nunca antes registrada) nunca tinham sido triadas; corrigido com recontagem de EX-001 + EX-003 nova.

Nível 3-4 (auditoria/documentação, sem código de produto tocado exceto o fix mecânico de `exceptions.md`). Evidência completa: `docs/engineering/reviews/full-audit-round2-seguranca-{claude,claude-round2,claude-round3,codex-prompt,codex-output-round1,codex-prompt-round2,codex-output-round2,summary}.{md,txt}`.

## E-019 — Full-audit round2, eixo Governança Jurídica, Contratual e de Terceiros — gate ≥9,0 não atingido (Claude 4,73→5,247, Codex 5,163→5,275), convergido em 2 rodadas

Reabertura do eixo (round1: nota final 5,015/10, `full-audit-round1-juridico-summary.md`) motivada por D-231 (2026-09-08): primeiro secret de vendor externo real do repositório (Meta Cloud API/WhatsApp Business Platform, `secrets-manager-whatsapp-config.ts`), que muda materialmente o estado de due diligence de terceiros e a superfície de licenciamento OSS desde o round1. Protocolo de nota cega, 2 rodadas reais (`codex exec`, com pesquisa externa real na internet — E-014 `SIM`): Claude R1 4,33 bruto/4,73 pós-fix mecânico; Codex R1 5,163; Claude R2 (tréplica) 5,247; Codex R2 (confirmação) 5,275 — convergência explícita declarada pelo Codex ("não há achado novo, aceito o fechamento nesta rodada").

**Fixes reais aplicados em `docs/engineering/third-party-inventory.md`**: (1) linha "BSP WhatsApp (não escolhido)", desatualizada desde D-229/D-231, substituída por linha nomeando o fornecedor real (Meta Platforms, Inc.), com dados tratados/criticidade/lock-in derivados de leitura direta do código; (2) 2 overclaims factuais no fix acima, achados pelo Codex na Rodada 1 e corrigidos na Rodada 2: a linha dizia "em dev" quando D-231 registra expressamente que `terraform plan`/`apply` desta fatia nunca rodou contra `dev` real nem foi mergeado a `main` (corrigido para distinguir "código/infra implementados" de "deployado/verificado ao vivo"), e a nota de rodapé "Lacunas conhecidas" ainda dizia "falta todo o adapter/BSP" (desatualizada desde D-229, corrigida); (3) 2 imprecisões também corrigidas na Rodada 2: "trocar de BSP" como descrição de lock-in (a integração é direta com a Cloud API da Meta, não via um Business Solution Provider intermediário) e a caracterização do DPA "padrão Meta" (existe instrumento real — "WhatsApp Business Data Processing Terms", atualizado 2025-08-22 — mas aplicabilidade é condicional ao papel contratual e aceite/entidade/versão para esta conta não confirmados).

**Pesquisa externa real (E-014, executada pelo Codex com acesso à internet, fonte+data citados)**: confirma existência de DPA padrão real da Meta (`whatsapp.com/legal/business-data-processing-terms`, 2025-08-22) e addendum de transferência internacional (2024-02-16); confirma que Meta oferece residência de dados configurável por número ("local storage", Brasil suportado mas não configurado nesta sessão, com ressalva de processamento transitório fora da região mesmo com local storage ativo); e corrige uma imprecisão do comentário em `whatsapp-cloud-api-adapter.ts` (D-4) — "só Utility, nunca Marketing" não é a regra geral da Meta (Marketing também pode iniciar conversa fora da janela de 24h com opt-in, e existe uma terceira categoria Authentication não citada) — o comportamento real do código permanece correto e mais conservador que o exigido; o achado é de precisão do comentário, não do comportamento, registrado para sessão futura que toque o módulo `notification` (fora do escopo de correção desta auditoria jurídica).

**Achados classificados, sem mudança de padrão em relação ao round1**: bloqueante real antes de habilitar WhatsApp para usuário real (aviso de privacidade/termos compatíveis com a política Meta, DPA/entidade contratante confirmados formalmente, residência de dados decidida/configurada); escopo maior (gate de CI de licenciamento OSS — `jszip@3.10.1` dual MIT/GPL-3.0-or-later sem eleição documentada, `buffers@0.1.1` com licença "Custom" não verificada —, runbook de saída de fornecedor Meta, processo de recebimento de mudança de termos — já há atualização anunciada para 2026-09-23 —, owner jurídico nominal); impedimento externo genuíno (parecer jurídico controlador/operador/LGPD, revisão jurídica formal do DPA para a conta brasileira, aceite contratual real pelo titular da conta Meta).

Nível 3-4 (auditoria/documentação + fixes de registro em inventário, sem código de produto tocado). Evidência completa: `docs/engineering/reviews/full-audit-round2-juridico-{claude,codex-prompt,codex-output-round1,round2-claude,codex-prompt-round2,codex-output-round2,summary}.{md,txt}`.

## E-021 — Full-audit round2, eixo Arquitetura — melhora real desde round1 (Claude 7,966→8,55, Codex 8,743→8,782), gate ≥9,0 não atingido, 1 achado real bloqueante novo (claim-before-send em guest credential delivery)

Segunda rodada formal deste eixo (`joint-review-criteria.md` §"Eixo: Arquitetura"; baseline `reviews/full-audit-round1-arquitetura-summary.md`, Claude 7,966/Codex 8,743, fechado em 2026-08-20 com 5 dos 11 critérios classificados como impedimento externo — decisão CDK vs Terraform pendente). Reaberto por volume de mudança de escopo desde então: D-084 a D-231+ (Multi-User B2B completo, Document Archive completo, ADR-0009 substitui CDK por Terraform, 9 GSIs, 30 workers assíncronos, 63 handlers Lambda).

**Verificação real executada nesta sessão (não presumida)**: `npm run check-boundaries` (0 violações, 630 módulos/2400 dependências); `npm test` completo, **2625/2625 passando** ao vivo; `AWS_PROFILE=claude-dev terraform test` na raiz `infra/`, **23/23 passando contra AWS real** (não fake/local-only) — confirma que a suíte de infra que provava isolamento de GSI3/GSI6/GSI8/DLQ/alarmes/EventBridge Scheduler na Rodada 1 continua verde contra a conta `dev` real depois de toda a expansão de escopo.

**O impedimento externo dominante da Rodada 1 deixou de existir como categoria**: ADR-0009 formalizou Terraform; verificação ao vivo contra `dev` (`aws --profile claude-dev`) é prática padrão em dezenas de decisões D-1xx/D-2xx, não exceção. Isso elevou Testability & Delivery Safety (8,0/8,6→9,2/9,4) e Security & Privacy parcialmente (7,5/8,4→9,0/9,2, IAM negativo GSI3/GSI6 comprovado real via `aws iam simulate-principal-policy` em múltiplas waves).

**1 dos 2 achados "Category B" da Rodada 1 corrigido**: AppConfig com `resources: ["*"]` sem construto real — `infra/modules/feature-flags/main.tf` agora cria `aws_appconfig_application`/`environment`/`configuration_profile` reais, `StartConfigurationSession` escopado ao ARN exato, só `GetLatestConfiguration` permanece `"*"` com justificativa tecnicamente correta (token de sessão opaco, não recurso endereçável por ARN). **EMF/`metrics.ts`/dashboard permanece sem correção** — confirmado por grep nesta sessão, nenhum arquivo de métrica customizada nem `aws_cloudwatch_dashboard` existe; alarmes de erro/idade de fila cresceram bem (11+ alarmes dedicados novos), mas não substituem visão agregada por tenant.

**Achado novo real, nível 5 (não corrigido nesta sessão, auditoria não implementa)**: `src/workers/guest-credential-delivery/deliver.ts:66` reivindica o marcador idempotente (`markerStore.claim`) ANTES de chamar `emailProvider.send()` (linha 71). Verificado por leitura direta do handler (`guest-credential-delivery-handler.ts:54`): `SEND_FAILED` aciona retry via redrive nativo do DynamoDB Streams Event Source Mapping, mas o retry chama `deliverGuestCredential` de novo, que chama `claim()` de novo, que já retorna `false` — produzindo `ALREADY_DELIVERED` em vez de reenviar. **Uma falha de SES entre o claim e o send suprime a entrega da credencial guest permanentemente e silenciosamente**, sem alarme dedicado, sem correção possível via o mecanismo de redrive que o resto do pipeline assíncrono do projeto usa com sucesso. O comentário do próprio código documenta a troca consciente mas descreve o efeito de forma equivocada ("unclaimed-but-unsent" — o estado real é "claimed-but-unsent"). Mesmo padrão `claim→efeito externo` citado como precedente em `document-request-service.ts`'s initial invite — indica risco de classe, não bug isolado. Achado inicialmente do Codex (nota cega independente na Rodada 1), verificado e confirmado por leitura direta do Claude (rastreamento completo do caminho de retry) na Rodada 2. **PENDENTE — decisão de Marcelo sobre o mecanismo de correção (state machine intermediária vs. idempotência pós-envio), nível 4-5, não implementado por instrução explícita da auditoria.**

**Achados novos menores, não-bloqueantes**: fan-out sem cap em `onboarding-state.ts:31`/`resolve-active-membership.ts:21` (hidratação de todas as Memberships via `Promise.all` sem limite, cresce com organizações por usuário) e a pequena duplicação de hidratação GSI4 entre os dois arquivos — **AMBOS CORRIGIDOS JUNTOS (`docs/architecture/decisions-log.md` D-274, 2026-09-12)**: extraído `organization/application/hydrate-memberships.ts` (`hydrateMembershipsFromGsi4`, único lugar que hidrata GSI4→partição base para os dois arquivos), usando `mapWithConcurrency` (concorrência 5, mesmo padrão já em produção em `reminder-dispatch-handler.ts`) em vez de `Promise.all` — preserva a semântica fail-fast original (erro de hidratação sempre relançado, nunca tratado silenciosamente como membership ausente). **PENDENTE ainda**: lifecycle incompleto de `DossierExportRun`/`ReportSubscriptionRun` (sem `purgeAfter`/TTL nem classificação de retenção LGPD explícita, mesma classe já fechada para outras entidades em D-151 a D-156 mas essas duas são posteriores a essa rodada).

**Rodadas**: 3 rodadas reais de nota cega (`codex exec`) — R1 blind: Claude 8,42/Codex 9,085 (Codex achou o bug de `deliver.ts` que a proposta Claude R1 não tinha visto); R2 réplica: Claude aceita o achado e propõe rebaixar Reliability/Event-Integration para 7,8/7,9 (Codex tinha mantido 8,8/9,2 apesar de classificar o achado como "bloqueante" — inconsistência interna); Codex concorda no mesmo racional (não por média — justificativa nova: retry neutralizado pelo próprio claim, sem alarme dedicado, padrão repetido em outro fluxo), convergindo Codex para 8,782; R3 fechamento: ambos confirmam SIM/SIM que não resta divergência factual e que o achado deve ficar PENDENTE para decisão futura, não corrigido na auditoria. Único critério em que os dois lados chegaram a valor idêntico: Reliability (7,8) e Event & Integration (7,9) — os dois de maior peso combinado (27%) e os diretamente afetados pelo achado novo.

Nível 3-4 (auditoria/documentação, nenhum código de produto tocado). Evidência completa: `docs/engineering/reviews/full-audit-round2-arquitetura-{claude-round1,claude-round2,codex-prompt-round1,codex-output-round1,codex-prompt-round2,codex-output-round2,codex-prompt-round3,codex-output-round3,summary}.{md,txt}`.

## E-023 — Full-audit round2, eixo Qualidade de Engenharia — melhora real desde round1 (Claude 8,199→8,32, Codex 7,788→8,341), gate ≥9,0 não atingido, achado real novo de suíte intermitente

Segunda rodada formal deste eixo (`joint-review-criteria.md` §"Eixo: Qualidade de engenharia", 12 critérios; baseline `reviews/full-audit-round1-qualidade-summary.md`, Claude 8,199/Codex 7,788, fechado em 2026-08-20). Reaberto pelo mesmo volume de mudança das demais rodadas desta sessão (D-084 a D-231+); suíte cresceu de 152 para **2625 casos em 236 arquivos** (~17x).

**Achado headline, nível 3-4, RESOLVIDO (`docs/architecture/decisions-log.md` D-272, 2026-09-11)**: `npm test` completo rodou **2624/2625** nesta sessão — `test/architecture/system-mutation-allowlist.test.ts` falhou por corrida com `test/architecture/tenant-fence-boundary.test.ts` (ambos plantam fixture sob `src/` e invocam `tsc` real como subprocesso). O erro cita um arquivo fixture do OUTRO teste, inexistente no momento da checagem. Achado relevante: a mitigação já documentada em `vitest.config.ts` (`fileParallelism: false`) e no próprio comentário de `system-mutation-allowlist.test.ts:56-61` (fixtures movidas para evitar exatamente essa corrida) **já foi tentada e não foi suficiente** — causa raiz real não identificada nesta sessão. Reexecução isolada do arquivo (`npx vitest run test/architecture/system-mutation-allowlist.test.ts`) passou **5/5**, confirmando não-regressão de código de produção. Nota: E-021 (eixo Arquitetura, mesma sessão, executado em momento diferente) registra a mesma suíte como 2625/2625 — consistente com a hipótese de corrida intermitente, não com falha determinística. **D-272 identificou a causa raiz real** (`tsc -p tsconfig.json --noEmit` compila a árvore inteira por causa do `include` de `tsconfig.json`, ficando exposto a fixtures de QUALQUER outro processo `npm test`/`vitest` concorrente, não só do mesmo processo vitest — `fileParallelism: false` nunca poderia ter corrigido isso sozinho) e corrigiu com um lock cross-processo (`test/architecture/fixture-lock.ts`), verificado por reprodução real com dois processos `vitest` genuinamente concorrentes (não só teorizado).

**3 achados mecânicos do Codex corrigidos nesta sessão (nível 1-2)**: `docs/engineering/test-engineering-standard.md` tinha contagem/duração de suíte desatualizada (107 arquivos/928 casos/~7-10s, de 2026-08-28) — atualizado com a contagem real de hoje, preservando o número histórico como contexto da v1; comentário de `.dependency-cruiser.cjs` dizia "three rules" quando já existem 5 fitness functions reais — corrigido; `npx --yes @cyclonedx/cyclonedx-npm` no CI sem versão pinada — corrigido para `@6.0.1`.

**Achado do Round1 corrigido de fato, não por argumento**: critério 8 (Debuggability) — Round1 apontava que o logger não propagava `correlationId`/tenant automaticamente via contexto. `docs/engineering/logging-observability-standard.md` (documento normativo próprio, 3 rodadas Claude↔Codex, 9,5/9,6) formaliza esse mecanismo como critério obrigatório e `grep -rl AsyncLocalStorage src/` confirma implementação real em 8 arquivos (`shared/observability/{logger,context}.ts` + 4 handlers reais) — reclassificado de 7,7 para 9,0.

**Maior salto de nota desde Round1**: critério 6 (Delivery/Release/Recovery, 6,3/4,2→8,4/8,4) — Wave 2 (6 drills reais contra `dev`, RTO medido) + `cd.yml`/`rollback.yml` com plano Terraform salvo e rollback compensável/auditável, achado citado pelo Codex e incorporado pelo Claude na Rodada 2. RPO real ainda não medido (só RTO) — pendência nomeada.

**Achados reconfirmados sem correção (nível 3-6, PENDENTES)**: sem `coverage.thresholds` em `vitest.config.ts` (mesma lacuna do Round1, 1+ ano sem correção, corrigível nesta sessão mas decisão de quanto exigir cabe a Marcelo); sem mecanismo determinístico de drift semântico doc↔código (`check-doc-drift.ts` prova ausência de link quebrado/guardrail de tamanho, não pegaria a própria contagem desatualizada do achado acima nem o drift de `exceptions.md` EX-001 corrigido por outra sessão paralela no mesmo dia); `expiration-service.ts` com transações verbosas (Round1, não reverificado por nenhum dos dois lados nesta rodada).

**Rodadas**: 2 rodadas reais (`codex exec`) — R1 nota cega: Claude 8,17 (registrada antes de ver o Codex)/Codex 8,843 (avaliado com sandbox próprio incapaz de rodar `npm test`/`check-docs`/`npm audit` — restrição de ambiente do Codex, não do projeto); R2 não-cega de reconciliação: Claude 8,32 (aceita 3 achados mecânicos do Codex e os corrige, mas não adota a nota do Codex nos critérios onde o sandbox dele falhou sem conseguir avaliar evidência real); Codex 8,341, aceitando explicitamente a evidência de execução real do Claude para os critérios que seu próprio sandbox não conseguiu confirmar. **CONVERGIDA por acordo mútuo explícito do Codex** ("Não há desacordo material que exija Rodada 3") apesar de ambas as notas ficarem abaixo de 9,0 — mesmo padrão de fechamento honesto dos demais eixos desta sessão.

Nível 3-4 (auditoria/documentação + 3 fixes mecânicos de nível 1-2, nenhuma mudança de comportamento de produto). Evidência completa: `docs/engineering/reviews/full-audit-round2-qualidade-{claude,claude-round2,codex-prompt,codex-output-round1,codex-prompt-round2,codex-output-round2,summary}.{md,txt}`.
