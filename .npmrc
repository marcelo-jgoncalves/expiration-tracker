# Supply-chain hardening (implementation-blueprint.md #16.2):
# install scripts are disabled by default; exceptions must be explicit and reviewed
# (none granted yet - add here with a comment justifying each one).
ignore-scripts=true
save-exact=true
fund=false
audit=false
