import type{SQSBatchResponse,SQSEvent}from"aws-lambda";import{SQSClient}from"@aws-sdk/client-sqs";import{createDocumentClient}from"../../../shared/dynamodb/client.js";import{DynamoDbScanControlStore}from"../../../workers/reminder-scan-control/store.js";import{DynamoDbReminderDueWorkStore}from"../../../modules/reminder/persistence/dynamodb-reminder-due-work-store.js";import{runControlScanPage}from"../../../workers/reminder-scan-control/scan-page.js";import type{ScanContinuationV2}from"../../../workers/reminder-scan-control/model.js";import{buildReminderClaimQueuePort}from"../composition/reminder.js";import{UlidIdGenerator,newCorrelationId}from"../ids.js";import{mapWithConcurrency}from"../../../shared/concurrency/map-with-concurrency.js";import{defaultSchemaRegistry}from"../../../shared/contracts/schema-validator.js";
const controlName=process.env["REMINDER_SCAN_CONTROL_TABLE_NAME"],dueName=process.env["REMINDER_DUE_WORK_TABLE_NAME"],queueUrl=process.env["REMINDER_CLAIM_QUEUE_URL"];if(!controlName||!dueName||!queueUrl)throw new Error("Reminder scan-page environment is incomplete");const client=createDocumentClient(),ids=new UlidIdGenerator();const deps={controlStore:new DynamoDbScanControlStore(client,controlName),dueStore:new DynamoDbReminderDueWorkStore(client,dueName),claimQueue:buildReminderClaimQueuePort(new SQSClient({}),queueUrl),tableName:controlName,pageSize:200,leaseDurationMs:200_000,rolloutEpoch:Number(process.env["SCAN_MODE_EPOCH"]??1),now:()=>new Date().toISOString(),newEventId:()=>ids.newEventId(),correlationId:()=>newCorrelationId(),sleep:(ms:number)=>new Promise<void>(resolve=>setTimeout(resolve,ms))};
export async function handler(event:SQSEvent):Promise<SQSBatchResponse>{const results=await mapWithConcurrency(event.Records,4,async record=>{const message=JSON.parse(record.body)as ScanContinuationV2;const validation=defaultSchemaRegistry.validate("https://expiration-tracker/schemas/queues/reminder-scan-continuation.v2.json",message);if(!validation.valid)throw new Error(`Invalid scan continuation: ${validation.errors.join(", ")}`);await runControlScanPage(deps,message);});return{batchItemFailures:results.flatMap((result,index)=>result.ok?[]:[{itemIdentifier:event.Records[index]!.messageId}])};}
