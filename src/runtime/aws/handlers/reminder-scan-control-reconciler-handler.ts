import{randomUUID}from"node:crypto";import{SQSClient,SendMessageCommand}from"@aws-sdk/client-sqs";import{createDocumentClient}from"../../../shared/dynamodb/client.js";import{DynamoDbScanControlStore}from"../../../workers/reminder-scan-control/store.js";import{reconcileControlPlane}from"../../../workers/reminder-scan-control/reconciler.js";import{UlidIdGenerator,newCorrelationId}from"../ids.js";
const tableNameValue=process.env["REMINDER_SCAN_CONTROL_TABLE_NAME"],queueUrlValue=process.env["REMINDER_SCAN_QUEUE_URL"];if(!tableNameValue||!queueUrlValue)throw new Error("Control reconciler environment is incomplete");const tableName:string=tableNameValue,queueUrl:string=queueUrlValue;const store=new DynamoDbScanControlStore(createDocumentClient(),tableName),sqs=new SQSClient({}),ids=new UlidIdGenerator();
export async function handler(){return reconcileControlPlane({store,tableName,rolloutEpoch:Number(process.env["SCAN_MODE_EPOCH"]??1),leaseDurationMs:200_000,now:()=>new Date().toISOString(),newEventId:()=>ids.newEventId(),correlationId:()=>newCorrelationId(),newOwnerToken:()=>randomUUID(),send:async payload=>{await sqs.send(new SendMessageCommand({QueueUrl:queueUrl,MessageBody:JSON.stringify(payload)}));}});}
