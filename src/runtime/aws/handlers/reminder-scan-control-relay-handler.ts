import type{DynamoDBBatchResponse,DynamoDBStreamEvent}from"aws-lambda";import{unmarshall}from"@aws-sdk/util-dynamodb";import{SQSClient,SendMessageCommand}from"@aws-sdk/client-sqs";import{createDocumentClient}from"../../../shared/dynamodb/client.js";import{DynamoDbScanControlStore}from"../../../workers/reminder-scan-control/store.js";import{relayControlOutbox}from"../../../workers/reminder-scan-control/relay.js";import type{ScanOutboxV2}from"../../../workers/reminder-scan-control/model.js";
const tableName=process.env["REMINDER_SCAN_CONTROL_TABLE_NAME"],queueUrl=process.env["REMINDER_SCAN_QUEUE_URL"];if(!tableName||!queueUrl)throw new Error("Control relay environment is incomplete");const store=new DynamoDbScanControlStore(createDocumentClient(),tableName),sqs=new SQSClient({});
export async function handler(event:DynamoDBStreamEvent):Promise<DynamoDBBatchResponse>{const failures:{itemIdentifier:string}[]=[];for(const record of event.Records){try{if(record.eventName!=="INSERT"&&record.eventName!=="MODIFY")continue;const image=record.dynamodb?.NewImage;if(!image)continue;const item=unmarshall(image as Record<string,never>)as ScanOutboxV2;if(item.entityType!=="REMINDER_SCAN_CONTINUATION_OUTBOX"||item.status!=="PENDING")continue;await relayControlOutbox({send:async payload=>{await sqs.send(new SendMessageCommand({QueueUrl:queueUrl,MessageBody:JSON.stringify(payload),MessageAttributes:{correlationId:{DataType:"String",StringValue:payload.correlationId}}}));},markPublished:store.markPublished.bind(store),now:()=>new Date().toISOString()},item);}catch{failures.push({itemIdentifier:record.eventID??"unknown"});}}return{batchItemFailures:failures};}
