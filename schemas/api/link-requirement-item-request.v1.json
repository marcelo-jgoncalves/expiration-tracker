{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://expiration-tracker/schemas/api/link-requirement-item-request.v1.json",
  "title": "LinkRequirementItemRequest v1",
  "description": "POST /subjects/{subjectId}/requirements/{assignmentId}/link request body. Vincula um ExpirationItem ja existente (confirmado via ExpirationItemLookup, nunca aceito so pelo itemId informado) marcando o requisito como SATISFIED.",
  "type": "object",
  "additionalProperties": false,
  "required": ["itemId"],
  "properties": {
    "itemId": { "type": "string", "minLength": 1, "maxLength": 200 }
  }
}
