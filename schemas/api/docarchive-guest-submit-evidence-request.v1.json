{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://expiration-tracker/schemas/api/docarchive-guest-submit-evidence-request.v1.json",
  "title": "DocArchiveGuestSubmitEvidenceRequest v1",
  "description": "POST /document-archive/guest/document-requests/{token}/uploads request body (SubmitEvidenceInput). D-143 Decision 4 / D-146. documentTypeId mandatory per D-243 clean cutover. mediaType/contentLength/checksumSha256 added by ADR-0013 (D-265) - same literal allowlist/range/pattern as document/M6's reserve-document-upload-request.v1.json, ported (never cross-referenced) so this schema stays self-contained.",
  "type": "object",
  "additionalProperties": false,
  "required": ["fileName", "documentTypeId", "mediaType", "contentLength", "checksumSha256", "idempotencyKey"],
  "properties": {
    "fileName": { "type": "string", "minLength": 1, "maxLength": 255 },
    "documentTypeId": { "type": "string", "minLength": 1, "maxLength": 100 },
    "mediaType": { "type": "string", "enum": ["application/pdf", "image/jpeg", "image/png"] },
    "contentLength": { "type": "integer", "minimum": 1, "maximum": 10485760 },
    "checksumSha256": { "type": "string", "pattern": "^[a-fA-F0-9]{64}$" },
    "idempotencyKey": { "type": "string", "minLength": 1, "maxLength": 128 }
  }
}
